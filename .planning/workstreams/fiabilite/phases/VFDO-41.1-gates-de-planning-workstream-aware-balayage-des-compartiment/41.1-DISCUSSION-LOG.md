# Phase 41.1: Gates de planning workstream-aware — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 41.1-Gates de planning workstream-aware — balayage des compartiments présents sur le disque
**Areas discussed:** Emplacement de la primitive, définition de « compartiment conforme »,
mécanisme anti-oubli, classement bloquant/consultatif par gate, plumbing CI

---

**Contrainte de tenue :** cet agent (`vf-coder`, worker interne) n'a pas d'outil `AskUserQuestion`
disponible dans ce contexte de sous-agent. Le mandat reçu de `vf-dev-manager` posait déjà 5
décisions comme tranchées (ne pas rouvrir) et désignait explicitement les points restants comme des
décisions d'implémentation relevant du builder ("Claude handles these — architecture patterns,
implementation details"), pas de la vision utilisateur. Les gray areas ci-dessous ont donc été
résolues par builder judgment, ancrées dans le code lu (`check-divergence.sh`,
`workstream-policy.sh`, les 3 scripts en catégorie (c)) et le texte déjà écrit au ROADMAP/mandat,
plutôt que par question interactive. Aucune n'a été laissée sans trace : chacune est en `ask-user`
dans le rapport typé remonté au manager si elle appelle malgré tout un arbitrage humain.

## Emplacement de la primitive d'énumération

| Option | Description | Selected |
|--------|-------------|----------|
| `conductor` | Aux côtés de `check-divergence.sh`, son seul consommateur aujourd'hui | |
| `planning-core` | Sœur des résolveurs singuliers existants (`vf_ws_resolve` etc.) | ✓ |

**Choix :** `planning-core`. **Notes :** la dépendance va déjà dans ce sens — `check-divergence.sh`
(conductor) source `workstream-policy.sh` (planning-core) via repli à deux candidats. Ajouter la
primitive côté conductor créerait une deuxième source de vérité et forcerait planning-core à
dépendre de conductor pour une capacité qu'il possède déjà partiellement (résolution singulière).

## Définition de « compartiment conforme »

Pas un choix entre options concurrentes — une définition à écrire, dérivée directement du texte
déjà figé au ROADMAP (Success Criterion 5, WSAW-05) et du cas mesuré `gouvernance/STATE.md`
(frontmatter réduit à `workstream:`+`created:`, rc=2). Voir CONTEXT.md D-02 pour le texte complet,
à relayer verbatim.

## Mécanisme anti-oubli (WSAW-02/WSAW-04)

| Option | Description | Selected |
|--------|-------------|----------|
| Recensement versionné seul | Documente, mais rien ne force sa mise à jour | |
| Lint seul | Détecte, mais sans motif ni exemption tracée | |
| Les deux | Recensement motive/exempte, lint détecte l'oubli | ✓ |

**Choix :** les deux, conjointement — un recensement sans lint peut périmer en silence ; un lint
sans recensement ne peut pas distinguer un consommateur légitimement exempté (catégorie a2, altitude
lab) d'un oubli réel.

## Classement bloquant/consultatif par gate (WSAW-02, contre ADR-074)

Chaque script de la catégorie (c) a été jugé contre ADR-074 individuellement plutôt qu'en bloc —
voir CONTEXT.md D-04. Résumé : `check-divergence.sh` et `check-state-integrity.sh`/R5 restent
bloquants (garde d'intégrité d'état) ; `check-planning-state.sh`, `detect-gsd-engine.sh`,
`discover-unintegrated-docs.sh` restent des hooks non bloquants, mais cessent d'être faux ;
`detect-planning-debt.sh`/`planning-task-context.sh` restent hors périmètre (altitude lab).

## Claude's Discretion

- Noms exacts des fichiers/fonctions nouveaux.
- Emplacement exact du nouveau bloc CI (extension du BLOC 1 existant vs nouveau step).
- Format exact du fichier de recensement.

## Deferred Ideas

- Élever les 3 hooks non bloquants (catégorie c) au rang de gates CI — explicitement écarté, pas
  dans le périmètre de cette phase.
- Étendre le lint au-delà des scripts `.sh` — aucun consommateur non-script mesuré à ce jour.
- Le contenu doctrinal complet de la Phase 41.2 (WSCH-05) — reçu et porté ici, pas conçu ici.

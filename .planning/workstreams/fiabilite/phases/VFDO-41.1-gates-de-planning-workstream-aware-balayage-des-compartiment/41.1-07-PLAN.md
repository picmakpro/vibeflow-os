---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 07
lot: "L07 — doctrine : précision d'ADR-069 (frontière gate/workflow) + workstreams.md (choix, non-choix, procédure de bascule)"
type: execute
wave: 2
depends_on: ["41.1-01"]
autonomous: true
requirements: [WSAW-05, WSAW-06, WSAW-07]
files_modified:
  - docs/ADR.md
  - plugin/dev-orchestrator/references/workstreams.md
  - plugin/dev-orchestrator/CHANGELOG.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "CORRECTION C1-4 (juge frais tour 4) — ce plan REVENDIQUE WSAW-05 : l'exigence porte DEUX choses, la trichotomie appliquée par un gate (couverte par `41.1-03` et `41.1-06`) ET « porte la définition unique de compartiment conforme, consommée par WSCH-02 ». La Task 2 de CE plan (§6 de `workstreams.md`) est le SEUL endroit du parc qui exécute la seconde, et le frontmatter ne déclarait que `[WSAW-06, WSAW-07]` : le ledger aurait fermé WSAW-05 contre 03/06 seuls, et un descope de la Task 2 n'aurait rien signalé — la Phase 41.2 aurait hérité d'une WSAW-05 « Done » SANS son artefact versionné, la condition d'arrêt que l'exigence nomme elle-même"
    - "L'ajout à `docs/ADR.md` est un AMENDEMENT DATÉ d'ADR-069 (nouvelle section « Amendement 2026-09-23 — ... », à la suite de l'amendement 2026-09-09 déjà présent) — PAS une nouvelle ADR numérotée, PAS une réécriture de la décision d'adoption ni de la condition dure existantes. Forme exacte à imiter : ADR-074 précisant ADR-031 « sans l'abroger »"
    - "La frontière est formulée PAR LE RÔLE, verbatim proche de WSAW-06 : `GSD_WORKSTREAM` et le pointeur restent le canal NOMINAL de ce qui TRAVAILLE (workflows, agents, worktrees) ; un GATE ne dérive JAMAIS sa cible d'une valeur qu'un `export` peut changer, PARCE QU'IL JUGE celui-là même qui pourrait l'exporter"
    - "L'amendement NOMME explicitement : son origine (angle mort constaté à la partition D-02 du 2026-09-23, `ci.yml:353` câblé en dur, PR #94) ; ET le constat que l'ancrage ADR-063 invoqué au départ de cette investigation était ERRONÉ (ADR-063 date du 2026-07-31, porte sur l'agrégation de frontmatter de `STATE.md`, ne contient AUCUNE occurrence du mot « workstream ») — les deux faits sont VÉRIFIÉS par lecture directe d'ADR-063 avant d'écrire la phrase, pas recopiés sans preuve. CORRECTION C1-5 (juge frais tour 4) : l'amendement ne doit PAS écrire que « le commentaire de `ci.yml` citait ADR-063 comme fondement » — c'est FAUX, relu le 2026-09-23 : le commentaire énonce la règle SANS citer d'ADR et ne mentionne ADR-063 que pour son objet réel (« le `STATE.md` VIVANT, celui dont ADR-063 garde les compteurs »), ce qui est exact. Le raccourci d'ancrage vient de l'INVESTIGATION. Et l'amendement ne grave AUCUN numéro de ligne de `ci.yml` (la rédaction précédente citait `ci.yml:345-346`, doublement faux : la règle est aux lignes 344-348, et le plan `41.1-06`, vague 3, RÉÉCRIT ENTIÈREMENT cette étape — un numéro gravé dans une ADR serait périmé à la livraison même de la phase qui l'écrit). L'étape est désignée par son NOM"
    - "L'attribution EXACTE, verbatim WSAW-06 : « Décision de la session principale (`vibeflow-head`), 2026-09-23 — pas un arbitrage de Samuel » — PAS la formule standard « arbitrage Samuel, AskUserQuestion... » utilisée ailleurs dans ce dépôt, précisément parce que ce n'EST PAS un arbitrage de Samuel (distinction à préserver, pas une erreur de forme à corriger)"
    - "`workstreams.md` §1 gagne la PROCÉDURE DE BASCULE avec sa PRÉCONDITION VÉRIFIABLE — aujourd'hui absente (§1 ne dit que « `create <nom> --migrate-name <nom>` », sans procédure ni précondition). La précondition citée est la condition dure d'ADR-069 (« aucune partition tant qu'une phase est en vol »), nommée comme un CHAMP DU DISQUE à vérifier (pas rappelée en prose creuse) — reprendre le motif déjà en place en §5 du même fichier"
    - "`workstreams.md` §3 : la phrase devenue FAUSSE depuis la PR #94 (« Sur un dépôt non partitionné il sort en 3 sans un mot : c'est l'état nominal de tous nos labs à ce jour, pas un manque ») est CORRIGÉE — CE dépôt (vibeflow-os) est désormais partitionné depuis le 2026-09-23 ; la phrase doit distinguer explicitement CE dépôt (partitionné) des LABS QUI INSTALLENT VibeFlow (où le non-partitionné reste effectivement l'état nominal par défaut, WSCH-01 de la Phase 41.2)"
    - "`workstreams.md` gagne, dans le chapeau (bloc `>` en tête de fichier) ou en §1, le mot « choix » et une phrase sur le DÉMARRAGE — aujourd'hui absents (D-04, ROADMAP Success Criterion 8 : « les mots choix, démarrage, init n'y figurent pas »). Le CONTENU DOCTRINAL complet du choix (sa valeur, son non-choix par défaut détaillé) appartient à la Phase 41.2 (WSCH-05) et N'EST PAS écrit ici — ce plan pose seulement le VOCABULAIRE et le renvoi, jamais le contenu complet que 41.2 remettra"
    - "COROLLAIRE Q1 (arbitrage Samuel, AskUserQuestion session principale, 2026-09-23, option b) — LA DÉFINITION UNIQUE EST PORTÉE PAR UN ARTEFACT VERSIONNÉ : `plugin/dev-orchestrator/references/workstreams.md` gagne une section `## 6.` qui recopie la trichotomie D-02 AMENDÉE depuis `41.1-CONTEXT.md` (conforme / non-initialisé sur le FRONTMATTER SEUL / verdict NOMMÉ d ambiguïté / corrompu), verdict d ambiguïté INCLUS avec ses deux lectures nommées. MOTIF À RESPECTER : une définition dont le SEUL porteur est le CONTEXT d une phase close est INACCESSIBLE à qui devra l appliquer — la Phase 41.2 la consomme via WSCH-02, et ce plan est déjà le seul écrivain de ce fichier pour les deux phases (D-07/WSAW-07). Le §6 est la SOURCE que 41.2 cite ; le CONTEXT n en est que la trace de décision"
    - "CORRECTION C-14 (juge frais, 2e passe) — LA SONDE ADR-063 A UN TÉMOIN DE PLAGE : elle assertait `w -eq 0` sans jamais vérifier que la section avait été LOCALISÉE. Si l en-tête change de forme, `f` reste 0, `w` vaut 0, et la sonde passe au VERT SANS AVOIR RIEN LU. Mesuré le 2026-09-23 : en-tête à la ligne 1332, section de 1332 à 1501 (169 lignes), 0 occurrence de « workstream » — le FAIT est donc VRAI mais sa garde était structurellement verte à vide. Un compteur `lignes_balayees` est ajouté et asserté `>= 1`. PROUVÉ PAR MUTATION : nominal → `lignes_balayees=168`, rc=0 ; avec un en-tête volontairement introuvable → `lignes_balayees=0`, rc=1. La MÊME classe de défaut est fermée d avance sur la sonde neuve du §6 (`lignes_section6 >= 10`)"
    - "CORRECTION C-19 (juge frais, 2e passe) — L ENTRÉE « Non releasé » EST TRANSITOIRE ET NOMMÉE COMME TELLE : ce plan (vague 2) écrit dans `plugin/dev-orchestrator/CHANGELOG.md` une section `## Non releasé`, et `41.1-08` (vague 5) ajoute dans le MÊME fichier une entrée `## [vX.Y.Z]` nommant LES MÊMES changements — double description du même travail si rien ne l absorbe. Aucun des deux plans ne le disait, et le `<verify>` n°3 de 08 n assertait rien. Ce plan porte désormais l AVERTISSEMENT explicite, et `41.1-08` porte l ABSORPTION (fusion du contenu dans l entrée versionnée puis RETRAIT de l en-tête « Non releasé ») avec son assertion. La section N EST PAS supprimée ici : elle est la trace utile tant que la version n est pas posée"
    - "Ce plan est le SEUL écrivain de `docs/ADR.md` et `plugin/dev-orchestrator/references/workstreams.md` pour les DEUX phases 41.1 ET 41.2 (D-07/WSAW-07 tranché par le manager) — aucune réouverture n'est prévue par la Phase 41.2, qui CONSOMME le contenu déjà posé ici"
  artifacts:
    - path: "docs/ADR.md"
      provides: "amendement daté d'ADR-069, frontière gate/workflow formulée par le rôle, correction de l'ancrage ADR-063"
      contains: "Amendement 2026-09-23"
    - path: "plugin/dev-orchestrator/references/workstreams.md"
      provides: "§1 procédure de bascule + précondition, §3 corrigé, vocabulaire du choix au démarrage"
  key_links:
    - "Patron de forme à imiter EXACTEMENT : `docs/ADR.md` § « Amendement du 2026-09-23 » d'ADR-074 (déjà lu en entier dans le cadrage de ce plan) — même structure (contexte/tension, ce qui a été tranché, conséquence assumée écrite)"
    - "`workstreams.md` §5 « La condition dure » (déjà présent, inchangé par ce plan) — sa formulation exacte de la règle est celle à RÉFÉRENCER depuis la nouvelle procédure de bascule du §1, jamais reformulée différemment à deux endroits du même fichier"
---

<objective>
Écrire la frontière gate/workflow comme précision datée d'ADR-069 (WSAW-06), et corriger
`workstreams.md` §1/§3 pour qu'il porte le vocabulaire du choix au démarrage, la procédure de
bascule avec sa précondition vérifiable, et une affirmation à jour depuis la partition D-02
(WSAW-07). Ce plan écrit le contenu que la Phase 41.2 consommera sans jamais le redéfinir —
c'est ce qui rend les deux phases parallélisables sans conflit d'écriture (CONTEXT.md, canonical_refs).

Purpose: fermer l'angle mort qui a laissé l'étape `check-state-integrity` de `ci.yml` porter, dans un commentaire, une règle
que ni ADR-069 ni `workstreams.md` ne posaient explicitement — et corriger une affirmation devenue
fausse depuis la partition réelle du dépôt (PR #94).
Output: un amendement ADR daté et attribué correctement, une référence de module à jour.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `docs/ADR.md` § ADR-069 en entier (décision, condition dure, amendement 2026-09-09 existant)
- `docs/ADR.md` § ADR-074, section « Amendement du 2026-09-23 » (patron de forme à imiter)
- `docs/ADR.md` § ADR-063 en entier (pour VÉRIFIER par lecture directe l'absence du mot
  « workstream » et son sujet réel — agrégation de frontmatter `STATE.md` — avant d'écrire la
  correction d'ancrage)
- `plugin/dev-orchestrator/references/workstreams.md` en entier (173 lignes)
- `.github/workflows/ci.yml` lignes ~340-347 (commentaire actuel qui porte la règle non écrite
  ailleurs — à citer comme origine de l'angle mort dans l'amendement)
</context>

<discipline>
- Aucune reformulation de la décision d'adoption ni de la condition dure d'ADR-069 — amendement
  additif seulement, patron ADR-074.
- Vérifier CHAQUE fait cité (date d'ADR-063, absence du mot « workstream », ligne exacte de
  `ci.yml`) par lecture directe avant de l'écrire — ne pas recopier depuis ce PLAN.md sans
  re-vérifier, au cas où une phase antérieure aurait changé ces fichiers entre le cadrage et
  l'exécution.
- Le CONTENU doctrinal complet du choix au démarrage (WSCH-05) N'appartient PAS à ce plan — ne pas
  anticiper sur la Phase 41.2, seulement poser le vocabulaire et le renvoi.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : amendement daté d'ADR-069 — frontière gate/workflow, correction d'ancrage ADR-063</name>
  <files>docs/ADR.md</files>
  <behavior>
    - `docs/ADR.md` contient une nouvelle section, immédiatement après l'amendement 2026-09-09
      existant d'ADR-069, intitulée sur le patron « Amendement 2026-09-23 — ... ».
    - la décision d'adoption et la condition dure d'ADR-069 restent BYTE-FOR-BYTE identiques
      au-dessus de ce nouvel amendement (diff nul sur tout ce qui précède).
    - la phrase d'attribution est EXACTEMENT « Décision de la session principale (`vibeflow-head`),
      2026-09-23 — pas un arbitrage de Samuel ».
  </behavior>
  <action>
Lire ADR-063 en entier avant d'écrire quoi que ce soit (vérification requise par `must_haves` —
un fait cité sans preuve fraîche est le défaut même que WSAW-06 corrige).

Ajouter, à la fin de la section ADR-069 (après l'amendement 2026-09-09, avant le séparateur `---`
qui introduit ADR-070), une nouvelle sous-section :

```markdown
### Amendement 2026-09-23 — la frontière gate/workflow, posée explicitement, et une correction
d'ancrage

**Origine.** La PR #94 a partitionné `.planning/` en deux compartiments (`fiabilite`,
`gouvernance`) et a laissé un angle mort : le COMMENTAIRE de l'étape `check-state-integrity
(anti-régression du frontmatter .planning/STATE.md, ADR-063)` de `.github/workflows/ci.yml`
portait la règle « un gate ne dérive jamais sa cible de `GSD_WORKSTREAM` — il la reçoit en
argument explicite » — sans qu'aucune ADR ni référence de module ne la POSE. L'étape est désignée
par son NOM, jamais par un numéro de ligne : la Phase 41.1 la RÉÉCRIT ENTIÈREMENT (plan `41.1-06`,
vague 3, postérieur à ce plan en vague 2), donc tout numéro gravé ici serait périmé à la livraison
même de la phase qui l'écrit — la classe de défaut exacte que cet amendement corrige. Constaté à
la mission `2026-09-23-partition-planning-d02.md`, repris à l'ouverture de la Phase 41.1 (ROADMAP,
demande de Samuel, session principale, 2026-09-23 : « généralise le remède à VibeFlow »).

**Correction d'ancrage.** L'investigation qui a ouvert cette phase a d'abord ancré la règle
ci-dessus sur ADR-063. Cet ancrage est ERRONÉ : ADR-063 (2026-07-31) porte sur l'agrégation de
frontmatter de `.planning/STATE.md` et ne contient AUCUNE occurrence du mot « workstream ». La
règle réelle relève d'ADR-069 (ce fichier) et d'ADR-064 (canal nominal), jamais d'ADR-063.

Le raccourci vient de l'investigation, PAS du commentaire de `ci.yml` : relu le 2026-09-23, ce
commentaire énonce la règle SANS citer d'ADR, et ne mentionne ADR-063 que pour son objet réel — « le
`STATE.md` VIVANT, celui dont ADR-063 garde les compteurs » —, ce qui est exact. Le nom de l'étape
le cite au même titre, et légitimement. Cette précision est écrite ici pour qu'un futur lecteur ne
reproduise ni le raccourci d'ancrage, ni le contresens sur sa source.

**La frontière, posée par le RÔLE, pas par le mécanisme.**

> `GSD_WORKSTREAM` et le pointeur `.planning/active-workstream` restent le canal NOMINAL de ce qui
> TRAVAILLE — workflows, agents, worktrees (§3 de `workstreams.md`, inchangé par cet amendement).
> Un GATE — un script dont le verdict décide d'un merge, d'une CI, d'une intégrité d'état — ne
> dérive JAMAIS sa cible d'une valeur qu'un simple `export` peut changer, PARCE QU'IL JUGE
> précisément celui-là même qui pourrait l'exporter. Un gate reçoit sa cible en argument EXPLICITE
> (`--file`, `--path`), ou l'énumère depuis le disque (`vf_ws_enumerate`, Phase 41.1) — jamais
> depuis l'environnement de son propre appelant.

Cette frontière ÉTAIT DÉJÀ appliquée par construction sur `check-state-integrity.sh` (`--file`
explicite, exposé en CI dès la partition) — elle n'était simplement jamais ÉCRITE ailleurs qu'en
commentaire de step. Cet amendement la rend explicite pour tout futur gate, sans changer aucun
comportement existant.

**Ce que cet amendement ne change pas.** La décision d'adoption des workstreams et la condition
dure (« aucune partition tant qu'une phase est en vol ») restent inchangées, ci-dessus. Cette
précision ne rouvre ni l'une ni l'autre.

*Décision de la session principale (`vibeflow-head`), 2026-09-23 — pas un arbitrage de Samuel.*
```

Vérifier après écriture que le fichier reste un document unique cohérent : le séparateur `---` qui
introduit ADR-070 doit suivre immédiatement ce nouvel amendement, sans ligne blanche superflue ni
doublon de séparateur.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c '
a=$(awk "/^### Amendement 2026-09-23/{c++} END{print c+0}" docs/ADR.md)
b=$(awk "/pas un arbitrage de Samuel/{c++} END{print c+0}" docs/ADR.md)
c=$(awk "tolower(\$0) ~ /adr-063.*erron/{c++} END{print c+0}" docs/ADR.md)
echo "section=$a attribution=$b correction_ancrage=$c"
[ "$a" -ge 1 ] && [ "$b" -ge 1 ] && [ "$c" -ge 1 ]
'</automated>
    <fails_when>`section=` à 0 (amendement absent) ; ou `attribution=` à 0 (formule d'attribution absente ou modifiée) ; ou `correction_ancrage=` à 0 (le constat d'erreur sur ADR-063 n'est pas écrit). CORRECTION NOUVEAU-1 (juge frais tour 4) : le motif était `/ADR-063.*(erron|Erron)/`, ROUGE PAR CONSTRUCTION sur le texte que ce plan prescrit lui-même — l'alternation ne couvre que `erron` et `Erron`, jamais `ERRONÉ` en CAPITALES, la seule forme employée par l'amendement. Mesuré le 2026-09-23 sur les DEUX rédactions (celle d'avant cette correction ET celle d'après) : `correction_ancrage=0` dans les deux cas, donc `[ "$c" -ge 1 ]` échouait quoi qu'écrive l'exécutant. Le motif est désormais insensible à la casse (`tolower($0) ~ /adr-063.*erron/`) — remesuré sur la rédaction prescrite : `correction_ancrage=1`, et sur un `docs/ADR.md` qui ne porterait pas le constat : `0` → rc=1, la sonde reste discriminante</fails_when>
    <automated>bash --noprofile --norc -e -c '
n=$(awk "/^\*\*Numérotation\*\*/{f=1} f && /^## ADR-070/{print NR; exit}" docs/ADR.md)
m=$(awk "/^## ADR-070/{print NR; exit}" docs/ADR.md)
echo "trouve_ADR070=${m:-absent}"
[ -n "$m" ]
'</automated>
    <fails_when>`trouve_ADR070=absent` — ADR-070 doit toujours exister immédiatement après ADR-069, aucune section n'a été mal fermée par l'insertion</fails_when>
    <automated>bash --noprofile --norc -e -c '
w=$(awk "/^## ADR-063 /{f=1; next} /^---/{if(f)exit} f && tolower(\$0) ~ /workstream/{c++} END{print c+0}" docs/ADR.md)
lues=$(awk "/^## ADR-063 /{f=1; next} /^---/{if(f)exit} f{n++} END{print n+0}" docs/ADR.md)
echo "workstream_dans_ADR063=$w lignes_balayees=$lues"
[ "$w" -eq 0 ] && [ "$lues" -ge 1 ]
'</automated>
    <fails_when>`workstream_dans_ADR063=` différent de 0 — si le mot apparaît réellement dans ADR-063, la correction d'ancrage écrite par ce plan serait elle-même fausse ; ou `lignes_balayees=` égal à 0. CORRECTION C-14 (juge frais, 2e passe) : la sonde n'avait AUCUN TÉMOIN que la section avait été LOCALISÉE — si l'en-tête change de forme, `f` reste 0, `w` vaut 0, et la sonde passe au VERT **sans avoir rien lu**. Mesuré le 2026-09-23 : l'en-tête matche (ligne 1332), la section va de 1332 à 1501 soit **169 lignes**, et 0 occurrence de « workstream » — le FAIT écrit par ce plan est donc VRAI, mais la garde qui le protégeait était structurellement verte à vide. Contre-épreuve exécutée : avec un en-tête volontairement introuvable (`^## ADR-0631 `), l'ancienne sonde rendait `0` et serait passée au vert ; la sonde corrigée rend `lignes_balayees=0` et rougit</fails_when>
  </verify>
  <acceptance_criteria>
    - amendement daté présent, forme ADR-074 respectée
    - attribution exacte « session principale (vibeflow-head)... pas un arbitrage de Samuel »
    - correction d'ancrage ADR-063 écrite ET vérifiée vraie par le test dédié
    - décision d'adoption et condition dure d'ADR-069 inchangées au-dessus
  </acceptance_criteria>
  <done>La frontière gate/workflow est posée par écrit, datée, attribuée correctement, avec la correction d'ancrage vérifiée vraie.</done>
</task>

<task type="auto">
  <name>Task 2 : `workstreams.md` §1 procédure de bascule + précondition, §3 corrigé, vocabulaire du choix</name>
  <files>plugin/dev-orchestrator/references/workstreams.md, plugin/dev-orchestrator/CHANGELOG.md</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/dev-orchestrator/references/workstreams.md` §1 et §3 et §5 (déjà lus en entier au cadrage — relire au moment d'éditer pour capturer tout changement depuis)
  </read_first>
  <action>
**§1 — procédure de bascule + précondition.** Après la phrase existante « La partition d'un dépôt
passe donc par `create <nom> --migrate-name <nom>`, jamais par une commande de migration dédiée
qu'on chercherait en vain. », ajouter :
```markdown
**Procédure de bascule, et sa précondition.** Partitionner un dépôt qui ne l'est pas encore suit
trois gestes, dans cet ordre : (1) vérifier la précondition — AUCUNE phase en vol, la condition
dure du §5 ci-dessous, lue sur un CHAMP DU DISQUE (`STATE.md` : `status` différent de `executing`
sur toute phase active, dossiers de phase sans `SUMMARY.md` manquant) — JAMAIS présumée depuis la
mémoire d'une session ; (2) `create <nom> --migrate-name <nom>` pour le premier compartiment, qui
migre le contenu racine existant ; (3) `create <nom>` pour chaque compartiment supplémentaire, créé
vide. Le choix de partitionner AU DÉMARRAGE d'un lab, avant toute phase, est le geste le moins
coûteux — voir la Phase 41.2 de ce dépôt pour l'ergonomie de ce choix.
```

**§3 — corriger l'affirmation devenue fausse depuis la PR #94.** Remplacer la dernière phrase du
paragraphe « Le silence est la vraie difficulté » :
```
Sur un dépôt non partitionné il sort en 3 sans un mot : c'est l'état nominal de tous nos labs à
ce jour, pas un manque.
```
par :
```
Sur un dépôt non partitionné il sort en 3 sans un mot — l'état nominal d'un LAB QUI INSTALLE
VibeFlow, où le non-partitionné reste le défaut (Phase 41.2, WSCH-01 : une question, jamais
imposé). **Ce dépôt lui-même (`vibeflow-os`) est partitionné depuis le 2026-09-23** (PR #94, D-02)
— l'affirmation antérieure (« l'état nominal de tous nos labs ») ne décrivait plus l'état de ce
dépôt-ci au moment où elle a été écrite dans une version antérieure de ce fichier ; elle est
corrigée ici pour ne plus confondre CE dépôt et les labs qu'il distribue.
```

**Chapeau / §1 — vocabulaire du choix au démarrage.** Dans le bloc `>` de tête de fichier, après
la phrase « Chargée on-demand par `vf-dev-manager` et `vf-coder`... », ajouter une phrase :
```
> Le CHOIX de partitionner se pose au DÉMARRAGE d'un lab (Phase 41.2 de ce dépôt) — ce fichier
> décrit comment travailler UNE FOIS le choix fait, dans un sens comme dans l'autre.
```

**§6 NEUF — la définition de « compartiment conforme » (COROLLAIRE Q1, arbitrage Samuel,
AskUserQuestion session principale, 2026-09-23, option b).** Ajouter une section `## 6.` qui porte
la trichotomie D-02 **amendée**, RECOPIÉE depuis `41.1-CONTEXT.md` §D-02 :

- **conforme** — `STATE.md` lisible, frontmatter portant au moins `gsd_state_version`, `milestone`,
  `current_phase`, `status` ; aucune divergence S2/S4/S5 détectée pour ce compartiment.
- **non initialisé** — frontmatter réduit à la facture nominale de `workstream create`
  (`workstream:` + `created:`, sans `gsd_state_version`). Le critère porte sur le **FRONTMATTER
  SEUL** : la présence de `ROADMAP.md`/`REQUIREMENTS.md`/`phases/` de contenu **n'entre PAS** dans
  la classification. Verdict propre, **jamais un échec**.
- **verdict nommé d'ambiguïté** — frontmatter nominal **coexistant avec du contenu** : une
  **NOTICE**, jamais un échec, qui **nomme les DEUX lectures** — (i) compartiment neuf alimenté par
  carve-out, (ii) compartiment travaillé dont l'état a été tronqué — et **n'en choisit AUCUNE**,
  les deux ayant la même signature sur disque. Discriminer par l'historique git est écarté : cela
  ferait dépendre le verdict d'un passé que rien ne garantit présent.
- **corrompu** — tout autre cas. Frontière : exactement 2 clés nominales → non-initialisé (+ notice
  si contenu à côté) ; tout autre frontmatter cassé → corrompu, et c'est un écart.

**HORS DÉFINITION — la présence de `ROADMAP.md` et `REQUIREMENTS.md` (correction C1-2, juge frais
tour 4).** La rédaction précédente faisait de « `ROADMAP.md` et `REQUIREMENTS.md` existent et sont
lisibles » une clause de la conformité. AUCUN gate du périmètre de cette phase ne l'établit —
MESURÉ le 2026-09-23 : `check-divergence.sh` traite un `ROADMAP.md` absent comme un NON-ÉCART
(`check_s4a`, l.194-197 : « S4 non applicable — pas de ROADMAP.md dans ce compartiment », puis
`return 0`), et `REQUIREMENTS.md` n'est lu par AUCUN script du périmètre (0 occurrence dans
`check-divergence.sh`, `check-state-integrity.sh` et `workstream-policy.sh`). La clause est donc
RETIRÉE de la définition, au lieu d'y rester écrite sans que rien ne la mesure : la Phase 41.2
(WSCH-02) ne doit pas hériter d'un critère qu'aucun gate ne sait appliquer, sinon WSCH-04 (« les
gates passent au vert sur chaque compartiment ») serait VRAI sur un compartiment que WSCH-02
déclarerait non conforme. Même motif pour l'exemple « `ROADMAP.md` absent alors que `STATE.md`
atteste d'un `current_phase` > 0 », retiré de la liste des formes corrompues : il n'est mesuré par
rien. Le besoin reste légitime et part au BACKLOG — le mesurer demande un gate neuf, hors périmètre
d'une phase déjà à neuf plans et trois tours de jugement.

**POURQUOI ICI, et pas seulement dans le CONTEXT** (motif à respecter) : une définition dont le
SEUL porteur est le CONTEXT d'une phase close est **inaccessible à qui devra l'appliquer**. La
Phase 41.2 la consomme via **WSCH-02**, et ce plan est déjà le SEUL écrivain de ce fichier pour les
deux phases (D-07/WSAW-07) — c'est donc ici, dans un artefact **VERSIONNÉ ET DISTRIBUÉ**, que la
définition unique doit vivre. Le §6 est la source que 41.2 cite ; le CONTEXT n'en est que la trace
de décision.

`plugin/dev-orchestrator/CHANGELOG.md`, section `## Non releasé` (ou équivalent le plus récent) :
une entrée nommant la doctrine corrigée (§1 procédure de bascule, §3 affirmation obsolète, chapeau
vocabulaire du choix, §6 définition D-02 amendée), l'origine (angle mort PR #94, ROADMAP Phase
41.1), sans bump de VERSION à ce stade (reporté au plan 41.1-08).

**AVERTISSEMENT C-19 (juge frais, 2e passe) — cette section `## Non releasé` est TRANSITOIRE.** Le
plan `41.1-08` (vague 5) écrit dans le MÊME fichier une entrée `## [vX.Y.Z]` qui nomme LES MÊMES
changements : laissée en place, la section « Non releasé » produirait une DOUBLE DESCRIPTION du
même travail. `41.1-08` est chargé de l'**ABSORBER** (fusionner son contenu dans l'entrée versionnée
puis RETIRER l'en-tête « Non releasé »), et il l'asserte. Ne pas la supprimer ici : elle est la
trace utile tant que la version n'est pas posée.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c '
# CORRECTION post plan-checker (FLAG-3) : la phrase fausse originale est repliée sur DEUX lignes
# dans le fichier réel (« ...tous » / « nos labs à ce jour, pas un manque. ») — un motif ancré sur
# une seule ligne (/tous nos labs à$/) ne matche JAMAIS, ni avant ni après édition, et la variable
# b nétait pas assertée : le test passait par construction, pas par vérification. Corrigé en
# repliant tout le fichier sur une seule ligne (tr newline->espace) avant de chercher la phrase
# jointe — mesuré directement sur ce dépôt : rend b=1 avant édition, doit rendre b=0 après.
a=$(awk "/Procédure de bascule, et sa précondition/{c++} END{print c+0}" plugin/dev-orchestrator/references/workstreams.md)
b=$(tr "\n" " " < plugin/dev-orchestrator/references/workstreams.md | awk "{n=gsub(/tous nos labs à ce jour, pas un manque/,\"&\"); s+=n} END{print s+0}")
c=$(awk "/vibeflow-os.*partitionné depuis le 2026-09-23|Ce dépôt lui-même.*partitionné/{c++} END{print c+0}" plugin/dev-orchestrator/references/workstreams.md)
d=$(awk "/CHOIX de partitionner se pose au DÉMARRAGE/{c++} END{print c+0}" plugin/dev-orchestrator/references/workstreams.md)
echo "procedure=$a ancienne_phrase_fausse_restante=$b correction_ecrite=$c vocabulaire_choix=$d"
[ "$a" -ge 1 ] && [ "$b" -eq 0 ] && [ "$c" -ge 1 ] && [ "$d" -ge 1 ]
'</automated>
    <fails_when>`procedure=` à 0 (procédure de bascule absente) ; ou `ancienne_phrase_fausse_restante=` différent de 0 (la phrase fausse §3, jointe sur ses deux lignes, est TOUJOURS présente — FLAG-3 du juge frais : cette assertion était calculée mais jamais vérifiée avant correction) ; ou `correction_ecrite=` à 0 (la correction §3 n'est pas écrite) ; ou `vocabulaire_choix=` à 0 (le mot « choix » toujours absent du fichier — ROADMAP Success Criterion 8)</fails_when>
    <automated>bash --noprofile --norc -e -c 'n=$(awk "tolower(\$0) ~ /choix/{c++} END{print c+0}" plugin/dev-orchestrator/references/workstreams.md); echo "occurrences_choix=$n"; [ "$n" -ge 1 ]'</automated>
    <automated>bash --noprofile --norc -e -c '
R=plugin/dev-orchestrator/references/workstreams.md
sec=$(awk "/^## 6\./{c++} END{print c+0}" "$R")
lues=$(awk "/^## 6\./{f=1; next} /^## /{if(f)exit} f{n++} END{print n+0}" "$R")
manq=""
for lab in "non initialisé" "ambigu" "corrompu" "frontmatter" "carve-out" "tronqu"; do
  awk -v L="$lab" "/^## 6\./{f=1} f && index(tolower(\$0), tolower(L)) > 0 {g=1} /^## 7\./{f=0} END{exit !g}" "$R" || manq="$manq [$lab]"
done
echo "section6=$sec lignes_section6=$lues termes_manquants=[${manq# }]"
[ "$sec" -eq 1 ] && [ "$lues" -ge 10 ] && [ -z "$manq" ]
'</automated>
    <fails_when>`section6=` différent de 1 ; ou `lignes_section6=` inférieur à 10 (un titre sans corps) ; ou `termes_manquants=[…]` non vide. COROLLAIRE Q1 (arbitrage du 2026-09-23, option b) : la définition unique de « compartiment conforme » DOIT être portée par un artefact **VERSIONNÉ**, pas seulement par le CONTEXT d'une phase close — la Phase 41.2 la consomme via WSCH-02 et n'aura pas accès au CONTEXT. Le compteur `lignes_section6` est le TÉMOIN de plage qui empêche la sonde d'être verte à vide (même classe de défaut que C-14, corrigé dans la Task 1 de ce plan). Mesuré le 2026-09-23 sur `workstreams.md` (173 lignes, sections 1 à 5) : `section6=0 lignes_section6=0 termes_manquants=[[non initialisé] [ambigu] [corrompu] [frontmatter] [carve-out] [tronqu]]` → rc=1, la sonde est bien discriminante</fails_when>
    <fails_when>`occurrences_choix=` à 0 — critère littéral du ROADMAP (« les mots choix, démarrage, init n'y figurent pas » doit devenir faux)</fails_when>
  </verify>
  <acceptance_criteria>
    - §1 porte la procédure de bascule et sa précondition vérifiable
    - §3 ne porte plus l'affirmation fausse sans correction
    - le mot « choix » figure au moins une fois dans le fichier
    - `CHANGELOG.md` de `dev-orchestrator` porte une entrée non releasée, aucun bump de VERSION
  </acceptance_criteria>
  <done>`workstreams.md` porte le choix, la procédure de bascule avec sa précondition, et une affirmation à jour depuis la partition réelle de ce dépôt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → lecteur futur | un ancrage erroné (ADR-063 au lieu d'ADR-069) recopié sans vérification propage l'erreur à chaque relecture — c'est exactement le défaut que ce plan corrige, et le risque qu'il ne reproduise pas la même classe d'erreur sur un autre fait |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-13 | Repudiation | l'amendement pourrait être lu comme un arbitrage de Samuel s'il reprenait la formule standard du dépôt | medium | mitigate | attribution exacte imposée par `must_haves`, vérifiée par test dédié (`attribution=` ≥ 1 sur la phrase verbatim) |
| T-41.1-14 | Tampering | une correction de fait (ADR-063 sans occurrence de « workstream ») écrite sans relecture fraîche pourrait elle-même être fausse si le fichier a changé depuis le cadrage | low | mitigate | vérification automatisée dédiée (`workstream_dans_ADR063=0`) qui relit le fichier réel au moment de l'exécution, pas une supposition |
</threat_model>

<verification>
Amendement daté présent avec attribution exacte et correction d'ancrage vérifiée vraie par lecture
fraîche d'ADR-063 ; `workstreams.md` §1/§3/chapeau mis à jour, mot « choix » présent.
</verification>

<success_criteria>
La frontière gate/workflow est posée par écrit (WSAW-06), et `workstreams.md` porte le vocabulaire
du choix, la procédure de bascule avec sa précondition, et une affirmation §3 à jour (WSAW-07) —
prêt à être consommé par la Phase 41.2 sans qu'elle ait besoin d'y réécrire quoi que ce soit.
</success_criteria>

## Artifacts this phase produces

- `docs/ADR.md` — amendement 2026-09-23 d'ADR-069.
- `plugin/dev-orchestrator/references/workstreams.md` — §1/§3/chapeau mis à jour.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-07-SUMMARY.md` when done
</output>

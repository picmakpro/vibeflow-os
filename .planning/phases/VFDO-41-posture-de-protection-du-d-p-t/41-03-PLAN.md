---
phase: 41-posture-de-protection-du-d-p-t
plan: 03
lot: L3 — doctrine : ADR-072, amendement d'ADR-059, résumé CLAUDE.md
type: execute
wave: 3
depends_on: ["41-02"]
autonomous: true
requirements: [PROT-03, PROT-02]
files_modified:
  - docs/ADR.md
  - CLAUDE.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ADR-072 écrit la politique de contournement et de hotfix telle qu'arbitrée : les règles sont un garde-fou par défaut et une trace, pas un verrou ; personne n'est bloqué en urgence (D-02) ; qui peut contourner quoi, par quel geste, avec quelle trace, et le retour arrière (`enforcement` → `disabled` par `picmakpro`) — D-M10"
    - "ADR-072 n'invente aucune règle : il ne cite que D-01..D-10 et D-M1..D-M14 ; ce qui n'est pas documenté par GitHub (lequel des runs push/pull_request compte, ce que contourne `--admin`, couverture de l'admin par le bypass write, combinaison des règles de tags) y est écrit INCONNU MESURÉ À LA POSE (M-1 à M-4), jamais affirmé"
    - "ADR-059 reçoit un amendement daté (D-03) : sur ce dépôt, l'option « Branche pour tout travail de phase », écartée le 2026-07-28, devient la règle par défaut ; le texte d'origine d'ADR-059 n'est ni supprimé ni réécrit"
    - "`CLAUDE.md` § Discipline de release résume : PR obligatoire, contournement explicite et tracé, tags `v*` protégés à création libre, hook `pre-push` conservé (D-M9), `check-release-tag` inchangé — rouge au push de merge tant que le tag n'est pas posé, relance possible et non systématique (D-M5) ; les quatre étapes de release existantes inchangées"
    - "O-3 : partout « gardée par défaut + tracée » (D-02) ; une vérification rougit si une proposition d'ADR.md ou du `CLAUDE.md` l'associe à un mot d'achèvement, même nié"
    - "`dismiss_stale_reviews_on_push` est écrit dans ADR-072 comme renforcement possible NON retenu, à décider par Samuel (décision du manager, tour 1, 2026-09-17)"
    - "Ordre séquentiel sur la branche A : ce plan commence après le dernier commit de 41-02 ; tout commit citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
  artifacts:
    - path: "docs/ADR.md"
      provides: "ADR-072 + ligne d'index ; amendement daté d'ADR-059 + statut d'index"
    - path: "CLAUDE.md"
      provides: "résumé de la protection serveur dans la discipline de release"
  key_links:
    - "ADR-072 renvoie aux sources exactes posées : `.github/rulesets/main.json`, `.github/rulesets/tags-v.json`, `.github/CODEOWNERS` ; il est mergé AVANT la pose (D-M14), donc le retour arrière est écrit avant qu'une règle n'existe"
---

<objective>
Écrire la doctrine de la phase, dans les limites exactes des arbitrages :
- ADR-072 : politique de contournement et de hotfix, retour arrière ;
- amendement daté d'ADR-059 (D-03) ;
- résumé dans `CLAUDE.md` § Discipline de release.

Purpose: PROT-03 (politique écrite) et la part documentaire de PROT-02 (le flux de release reste
lisible et juste sous la nouvelle règle).
Output: `docs/ADR.md` (ADR-072, index, amendement ADR-059), `CLAUDE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/missions/2026-09-17-phase41-cadrage.md
@CLAUDE.md

Extraits de `docs/ADR.md` à lire (ne pas charger le fichier entier) : l'index (L14-46, dernière
ligne d'index `ADR-070` ; la note `ADR-065`), les notes d'amendement d'ADR-029 (L70-83, format
`> **ADR-NNN : amendement du <date> (Phase …)** — …`), ADR-059 (L980-1058), l'en-tête d'ADR-070
(L2240-2250, format `**Date** : … · **Statut** : …`), et la fin du fichier (ADR-071, dernière
entrée).

Sources de plateforme lues le 2026-09-17 : `docs.github.com/en/rest/repos/rules` (modes
`always`/`pull_request`/`exempt`, `PUT …/rulesets/{id}` partiel, `GET …/rulesets/{id}/history`) ;
`docs.github.com/en/rest/repos/rule-suites` (`GET /repos/{owner}/{repo}/rulesets/rule-suites`,
résultat `pass`/`fail`/`bypass`, permissions non précisées) ; `cli.github.com/manual/gh_pr_merge`
(`--admin` : « Use administrator privileges to merge a pull request that does not meet
requirements », périmètre sous ruleset non documenté).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : ADR-072 et sa ligne d'index — politique de contournement et de hotfix</name>
  <files>docs/ADR.md</files>
  <read_first>
    - docs/ADR.md (extraits listés dans le contexte)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (Constats, Contraintes de plateforme, D-01..D-10, Conséquences de D-02, D-M1..D-M14, Deferred)
  </read_first>
  <action>
Ajouter une ligne d'index juste après la ligne `| ADR-070 |` :
`| ADR-072 | 2026-09-17 | La branche main et les tags v* sont gardés par des rulesets GitHub — garde-fou par défaut et trace, pas verrou ; contournement ouvert au rôle write, par PR et tracé | Validée |`.
Ne pas toucher aux autres lignes (l'absence de ligne d'index pour ADR-071 est constatée, pas
corrigée : remontée au manager).

Ajouter en fin de fichier, après ADR-071, une section
`## ADR-072 : La branche main et les tags v* sont gardés par des rulesets GitHub — garde-fou par défaut et trace, pas verrou`
au format des ADR récentes :
- ligne d'en-tête : Date 2026-09-17 · Statut Validée · Décideur : Samuel (D-01..D-10, arbitrages
  AskUserQuestion session principale, 2026-09-17) et vf-dev-manager (D-M1..D-M14) · Voisines :
  ADR-059 (amendée), ADR-031 · Contexte : `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md` ;
- **Contexte / Problème** : `main` protégée par rien au 2026-09-17 (`rulesets` = `[]`), tout gate
  neutralisable depuis la PR qu'il juge (BACKLOG, O-3 du `25-SECURITY.md`), commits directs dont
  `892f89a` a rougi la CI ; compte `picmakpro` seul admin, trois comptes write, tous conservés
  (`samuel-neveugall`, `BatoEnLambo`, `Djeee8` — D-06 : la phase ne retire aucun accès) ;
- **Décision** : rulesets plutôt que la protection classique (D-M1) ; ruleset de branche et ruleset
  de tags, leur contenu règle par règle, sources `.github/rulesets/main.json` et
  `.github/rulesets/tags-v.json`, pose humaine par `picmakpro` (D-M6) ; CODEOWNERS étroit
  `.github/CODEOWNERS` (D-05) ; pas d'historique linéaire ni de méthode restreinte (D-M2, D-10) ;
  pas de signatures (D-M3) ;
- **Politique de contournement** : un tableau à quatre colonnes « Règle | Qui peut contourner |
  Geste | Trace ». Lignes :
  - PR obligatoire sur `main` : personne ne pousse directement, contournement write en mode
    `pull_request` seulement (D-M12) ; trace = refus serveur du push ;
  - checks requis et revue code owner : rôle write (donc les trois comptes write et les agents qui
    les utilisent, D-02), par un geste explicite de merge en contournement sur une PR ; trace = la PR
    et une entrée de résultat `bypass` lisible par `GET /repos/{owner}/{repo}/rulesets/rule-suites`
    (compte lecteur mesuré à la pose) ; forme exacte du geste = mesurée (M-3) ;
  - variante (a) `INCLUSES` de la variable de plan `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` (frontmatter de
    `41-01-PLAN.md`) SEULEMENT : une ligne « suppression et force push de `main` : non contournables
    hors PR » ; en variante (b) `ABSENTES`, l'ADR ne mentionne ni suppression ni force push de
    `main` (ni règle, ni table, ni conséquence) ; si la variable vaut `EN_ATTENTE_ARBITRAGE`, ne pas
    écrire l'ADR : arrêt et remontée ;
  - tags `v*` : création libre (D-07) ; réécriture, suppression et force push contournables par le
    rôle write en `always` (D-M13) ; trace = rule suite ;
  - désactivation d'un ruleset : `picmakpro` seul ; trace = historique du ruleset
    (`GET …/rulesets/{id}/history`, doc REST — non exercé par la phase) ;
  - mode `exempt` : jamais, faute de trace (D-02, D-M12).
- **Politique hotfix** : un hotfix passe par une branche et une PR comme tout travail (D-03) ; si la
  CI ne peut pas verdir à temps, un compte write merge en contournant explicitement — la PR et la
  rule suite en gardent la trace ; aucun push direct ; tag annoté et release GitHub après le merge,
  inchangés ; le rouge de l'étape `check-release-tag` au push de merge sur `main` est le signal « tag
  à poser », sa relance après le tag est possible, pas systématique (D-M5) ; le hook `pre-push` est
  conservé, seule garde de discipline de release côté client (D-M9) ;
- **Retour arrière** (écrit avant la pose) : `picmakpro` repasse chaque ruleset en `disabled` par
  `gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled` ; témoin
  `gh api repos/picmakpro/vibeflow-os/rules/branches/main` rend `[]` pour le ruleset de branche ;
- **Conséquences** : O-3 « gardée par défaut + tracée » (une hausse de baseline exige l'approbation
  de `@picmakpro` ou un contournement explicite tracé) ; critère 2 reformulé (D-09) ; ce que la
  règle laisse ouvert — tout compte write peut contourner (D-02). O-3 n'est jamais associée, même
  par une négation, à un mot d'achèvement ;
- **Renforcement possible non retenu** : `dismiss_stale_reviews_on_push` reste au défaut `false`
  dans le ruleset de branche — une approbation `@picmakpro` antérieure continue de couvrir un push
  postérieur de la même PR ; l'activer est une décision à prendre par Samuel, aucun arbitrage ne la
  couvre (décision du manager, 2026-09-17) ;
- **Inconnus mesurés à la pose** : M-1 (les quatre jobs tournent deux fois sur le même SHA, `push` +
  `pull_request` : lequel compte), M-2 (le bypass write couvre-t-il l'admin), M-3 (ce que contourne
  `gh pr merge --admin` pour un compte write, et sa trace), M-4 (combinaison des règles de tags ; la
  preuve attendue est la trace du contournement, pas un refus, puisque tous les pousseurs sont write
  ou admin) — résultats consignés dans `41-PREUVES.md` et reportés dans cette ADR par la PR des
  preuves ;
- **Ne couvre pas** : les idées différées du CONTEXT (contrôle de dérive en CI, garde de baseline
  indépendante de GitHub, signatures, passage en organisation) ;
- **QUAL-01** : la Phase 41 ne crée aucun script de gate ; CODEOWNERS et rulesets sont de la
  configuration (D-M11).
Aucun chemin de machine. Commit local sur la branche A, message « docs(41-03): ADR-072 — politique
de contournement et de hotfix (D-02, D-03, D-05, D-06, D-07, D-09 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ; REGLES_MAIN_FORCE_PUSH_SUPPRESSION=<variante> — arbitrage Samuel, <canal>, <date> » où <canal> (tirets remplacés par
des espaces) et <date> sont lus dans `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` du frontmatter de `41-01-PLAN.md`,
jamais une date écrite en dur.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=docs/ADR.md; h=$(awk "/^## ADR-072 : /{c++} END{print c+0}" "$F"); i=$(awk "/^\\| ADR-072 \\| 2026-09-17 \\|/{c++} END{print c+0}" "$F"); i70=$(awk "/^\\| ADR-070 \\|/{print NR; exit}" "$F"); i72=$(awk "/^\\| ADR-072 \\|/{print NR; exit}" "$F"); last=$(awk "/^## ADR-[0-9]+ : /{n=\$2} END{print n}" "$F"); T=$(mktemp -d); awk "/^## ADR-072 : /{f=1} f" "$F" > "$T/s"; m=0; for k in "D-02" "D-03" "D-05" "D-06" "D-07" "D-09" "D-M5" "D-M9" "D-M12" "D-M13" "pull_request" "always" "exempt" "rule-suites" "enforcement=disabled" ".github/rulesets/main.json" ".github/rulesets/tags-v.json" ".github/CODEOWNERS" "gardée par défaut + tracée" "garde-fou par défaut" "AskUserQuestion session principale, 2026-09-17" "M-1" "M-2" "M-3" "M-4" "pre-push" "check-release-tag" "hotfix" "QUAL-01" "dismiss_stale_reviews_on_push" "non retenu"; do awk -v k="$k" "index(\$0, k) {f=1} END{exit !f}" "$T/s" || { echo "ABSENT : $k"; m=$((m+1)); }; done; echo "adr-072 : sections=$h index=$i ligne_070=$i70 ligne_072=$i72 derniere_adr=$last termes_absents=$m"; [ "$h" -eq 1 ] && [ "$i" -eq 1 ] && [ "$i72" -eq $((i70 + 1)) ] && [ "$last" = "ADR-072" ] && [ "$m" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `sections=` ou `index=` différent de 1 ; ou la ligne d'index ADR-072 n'est pas immédiatement après celle d'ADR-070 ; ou `derniere_adr=` différent de `ADR-072` (section insérée ailleurs qu'en fin) ; ou une ligne `ABSENT :` (décision, mode, source, retour arrière, inconnu mesuré, hook, gate ou canal d'arbitrage non écrit dans la section, ou renforcement non retenu absent)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; VR=$(awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION:/{print \$2; exit}" "$PH/41-01-PLAN.md"); case "$VR" in (INCLUSES) att=present;; (ABSENTES) att=absent;; (*) echo "REGLES_MAIN_FORCE_PUSH_SUPPRESSION=$VR : aucune variante choisie, verification refusee"; exit 1;; esac; T=$(mktemp -d); awk "/^## ADR-072 : /{f=1} f" docs/ADR.md > "$T/s"; n=$(awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); m = split(t, c, \"\n\"); for (i = 1; i <= m; i++) if ((index(c[i], \" main\") || index(c[i], \"\\140main\")) && (index(c[i], \"force push\") || index(c[i], \"suppression\") || index(c[i], \"deletion\") || index(c[i], \"non_fast_forward\"))) k++; print k+0 }" "$T/s"); got=$([ "$n" -gt 0 ] && echo present || echo absent); echo "ADR-072 regles de main : variante=$VR propositions_suppression_force_push_main=$n (attendu $att)"; [ "$got" = "$att" ]'</automated>
    <fails_when>exit non nul ; ou, en variante (a) `INCLUSES`, aucune proposition d'ADR-072 ne mentionne suppression et force push de `main` ; ou, en variante (b) `ABSENTES`, au moins une proposition les mentionne ; ou `REGLES_MAIN_FORCE_PUSH_SUPPRESSION=… : aucune variante choisie` (valeur `EN_ATTENTE_ARBITRAGE` ou inconnue)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 docs/ADR.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
  </verify>
  <acceptance_criteria>
    - une section ADR-072 en fin de registre, une ligne d'index juste après ADR-070
    - la section nomme les décisions D-02, D-03, D-05, D-06, D-07, D-09, D-M5, D-M9, D-M12, D-M13, les modes, les trois sources, le retour arrière, les inconnus M-1..M-4, la politique hotfix et QUAL-01
    - O-3 écrite « gardée par défaut + tracée »
    - suppression et force push de `main` mentionnés seulement en variante (a) de `REGLES_MAIN_FORCE_PUSH_SUPPRESSION`
  </acceptance_criteria>
  <done>La politique de contournement et de hotfix est écrite et mergeable avant toute pose.</done>
</task>

<task type="auto">
  <name>Task 2 : amendement daté d'ADR-059, résumé CLAUDE.md, rejeu des gates</name>
  <files>docs/ADR.md, CLAUDE.md</files>
  <read_first>
    - docs/ADR.md (ADR-059 L980-1058, sous-section « Rules Associées » en fin ; ligne d'index `| ADR-059 |`)
    - CLAUDE.md (§ « Règle non négociable — Discipline de release », paragraphes « Garde-fou machine » et « Numérotation »)
  </read_first>
  <action>
`docs/ADR.md`, ADR-059 :
- ajouter en fin de section (après « Rules Associées », avant `## ADR-060`) une note
  `> **ADR-059 : amendement du 2026-09-17 (Phase 41)** — …` : sur ce dépôt, l'option « Branche pour
  tout travail de phase », écartée le 2026-07-28, devient la règle par défaut : toute mise à jour de
  `main` passe par une PR, 0 approbation (D-03, arbitrage Samuel, AskUserQuestion session
  principale, 2026-09-17) ; le serveur refuse le push direct (ruleset de branche, ADR-072) ; les
  commits directs de documentation ou d'ouverture de phase cessent ; la décision d'origine et son
  tableau d'options restent lisibles tels quels ; la doctrine distribuée aux labs n'est pas modifiée
  par cet amendement ;
- dans l'index, remplacer le seul statut de la ligne ADR-059 par
  `Validée — amendée le 2026-09-17 (Phase 41 : PR obligatoire sur main de ce dépôt, ADR-072)`.
Aucune autre ligne d'ADR-059 modifiée.

`CLAUDE.md`, § Discipline de release : insérer entre le paragraphe « **Garde-fou machine** » et
« **Numérotation** » un paragraphe titré `**Protection serveur de `main` et des tags `v*`** (Phase
41, ADR-072) :` suivi de six puces au plus :
- toute mise à jour de `main` passe par une PR (0 approbation) ; push direct refusé (D-03) ;
- merge exigeant les 4 jobs CI verts et la branche à jour, et la revue `@picmakpro` sur
  `.github/`, la baseline du budget d'instructions, les sentinelles `.planning/.*-armed` et
  `scripts/hooks/` (D-04, D-05) ;
- garde-fou par défaut + trace, pas verrou : un compte write peut contourner par un geste explicite
  sur une PR, et le contournement laisse une entrée dans les rule suites (D-02) ; geste exact et
  politique hotfix dans ADR-072 ;
- tags `v*` : création libre, réécriture et suppression contournables et tracées (D-07) ;
- `check-release-tag` inchangé : rouge au push de merge sur `main` tant que le tag n'est pas posé,
  relance possible, pas systématique (D-M5) ; hook `pre-push` conservé (D-M9).
Les quatre étapes numérotées de release et le reste du fichier restent octet-identiques. Commit
local sur la branche A, message « docs(41-03): amendement d'ADR-059 et résumé CLAUDE.md (D-02,
D-03, D-04, D-05, D-07 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=docs/ADR.md; T=$(mktemp -d); awk "/^## ADR-059 : /{f=1; next} /^## ADR-060 : /{f=0} f" "$F" > "$T/a59"; am=$(awk "/^> \\*\\*ADR-059 : amendement du 2026-09-17 \\(Phase 41\\)\\*\\*/{c++} END{print c+0}" "$T/a59"); m=0; for k in "D-03" "ADR-072" "AskUserQuestion session principale, 2026-09-17" "Branche pour tout travail de phase"; do awk -v k="$k" "/^> / && index(\$0, k) {f=1} END{exit !f}" "$T/a59" || { echo "ABSENT amendement : $k"; m=$((m+1)); }; done; opt=$(awk "/^\\| Branche pour tout travail de phase \\|/{c++} END{print c+0}" "$T/a59"); dec=$(awk "/^\\*\\*Toute mission d.équipe crée sa branche AVANT son premier commit/{c++} END{print c+0}" "$T/a59"); idx=$(awk "/^\\| ADR-059 \\| 2026-07-28 \\| .* \\| Validée — amendée le 2026-09-17 \\(Phase 41/{c++} END{print c+0}" "$F"); echo "adr-059 : amendement=$am termes_absents=$m option_d_origine=$opt decision_d_origine=$dec index_amende=$idx"; [ "$am" -eq 1 ] && [ "$m" -eq 0 ] && [ "$opt" -eq 1 ] && [ "$dec" -eq 1 ] && [ "$idx" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `amendement=` différent de 1 ; ou une ligne `ABSENT amendement :` ; ou `option_d_origine=` ou `decision_d_origine=` différent de 1 (texte d'origine d'ADR-059 réécrit ou supprimé) ; ou `index_amende=` différent de 1</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=CLAUDE.md; T=$(mktemp -d); awk "/^## Règle non négociable — Discipline de release/{f=1; next} /^## /{f=0} f" "$F" > "$T/s"; p=$(awk "/^\\*\\*Protection serveur de .main. et des tags .v\\*.\\*\\* \\(Phase 41, ADR-072\\)/{print NR; exit}" "$T/s"); g=$(awk "/^\\*\\*Garde-fou machine\\*\\*/{print NR; exit}" "$T/s"); n=$(awk "/^\\*\\*Numérotation\\*\\*/{print NR; exit}" "$T/s"); e=$(awk "/^[1-4]\\. \\*\\*/{c++} END{print c+0}" "$T/s"); m=0; for k in "D-02" "D-03" "D-04" "D-05" "D-07" "D-M5" "D-M9" "@picmakpro" "pre-push" "check-release-tag" "rule suites" "ADR-072"; do awk -v k="$k" "index(\$0, k) {f=1} END{exit !f}" "$T/s" || { echo "ABSENT claude : $k"; m=$((m+1)); }; done; echo "claude.md : paragraphe=$p garde_fou=$g numerotation=$n etapes_release=$e termes_absents=$m"; [ -n "$p" ] && [ -n "$g" ] && [ -n "$n" ] && [ "$g" -lt "$p" ] && [ "$p" -lt "$n" ] && [ "$e" -eq 4 ] && [ "$m" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou paragraphe absent ou hors de l'intervalle « Garde-fou machine » < paragraphe < « Numérotation » ; ou `etapes_release=` différent de 4 (étapes de release touchées) ; ou une ligne `ABSENT claude :`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 CLAUDE.md docs/ADR.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - amendement daté dans ADR-059, texte d'origine intact, index « amendée le 2026-09-17 (Phase 41 »
    - paragraphe « Protection serveur » dans la discipline de release du `CLAUDE.md`, entre « Garde-fou machine » et « Numérotation », quatre étapes de release inchangées
    - rejeux `gates` et `tests` verts
  </acceptance_criteria>
  <done>La doctrine de la phase est écrite, datée et attribuée, sans rouvrir aucune décision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → gestes futurs | un agent ou un collaborateur qui lit ADR-072 / `CLAUDE.md` agit selon ce qui y est écrit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-11 | Repudiation | doctrine affirmant un comportement GitHub non documenté | medium | mitigate | M-1..M-4 écrits comme inconnus mesurés, résultats reportés par la PR des preuves (plan 41-10) |
| T-41-12 | Tampering | O-3 présentée comme achevée | medium | mitigate | formulation imposée « gardée par défaut + tracée » vérifiée dans ADR-072 ; balayage par proposition d'ADR.md et du `CLAUDE.md` ; statut O-3 écrit au plan 41-10 après preuve |
| T-41-13 | Denial of service | flux de release rendu illisible ou faux | medium | mitigate | étapes de release du `CLAUDE.md` vérifiées intactes ; rejeu réel du flux aux plans 41-10 et 41-11 |
</threat_model>

<verification>
ADR-072 complète et indexée ; ADR-059 amendée sans perte ; `CLAUDE.md` résumé à sa place ; gates
et tests verts.
</verification>

<success_criteria>
PROT-03 écrit et mergeable avant la pose, retour arrière compris.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-03-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-protection-depot`).
</output>

---
phase: 31-manifeste-d-install-dry-run-issue-20
plan: 06
type: execute
wave: 5
depends_on: ["31-04"]
files_modified:
  - plugin/installer/SKILL.md
  - plugin/conductor/skills/vf-calibrate/SKILL.md
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
autonomous: true
requirements: [MANI-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "L'étape 5 de /vibeflow-install présente le plan `--dry-run` AVANT de poser, puis pose après accord (D-31-10)"
    - "L'étape 4 de /vf-calibrate présente le plan `--dry-run` AVANT de rafraîchir un module, puis rafraîchit après accord (D-31-10)"
    - "L'édition est MINIMALE : aucune refonte d'UX, aucun nouveau point de décision — le feu vert existant (ADR-031) est réutilisé tel quel"
    - "Les modules touchés sont bumpés conformément à la discipline du repo (VERSION + CHANGELOG)"
  artifacts:
    - "plugin/installer/SKILL.md : étape 5 portant l'appel --dry-run"
    - "plugin/conductor/skills/vf-calibrate/SKILL.md : étape 4 portant l'appel --dry-run"
    - "plugin/conductor/VERSION + CHANGELOG.md bumpés"
  key_links:
    - "vibeflow-update.sh --dry-run (31-04) -> stdout capturable -> présenté à l'utilisateur avant son feu vert ADR-031"
  prohibitions:
    - "Aucun nouveau point de décision ajouté aux deux skills (le feu vert existant suffit)"
    - "Aucun bump de la VERSION racine, de plugin.json ni de marketplace.json — cette phase ne fait pas de release"
    - "Aucun appel `gh` : ce plan ne touche pas GitHub"
---

<objective>
Brancher les deux consommateurs que l'issue #20 nomme : « consommé par l'étape 5 du skill installer
et l'étape 4 de vf-calibrate → présenter le manifeste, PUIS poser après accord ». C'est le geste
qui transforme le flag en réponse à la demande terrain (revue croisée des hooks AVANT écriture).

Purpose: MANI-02 côté utilisateur. Sans ce câblage, le flag existe mais personne ne le voit.

Output: deux éditions minimales de skill + les bumps du module touché.

> **VAGUE ABANDONNABLE (D-31-10).** Ce plan est planifié en dernière vague et peut être abandonné
> si la phase déborde. Ordre de sacrifice décidé d'avance : **31-07 se sacrifie en premier, 31-06
> ensuite.** Les quatre critères de succès de la phase restent tenus par les vagues 1 à 4 ; ce plan
> ajoute l'ergonomie que l'issue demande, pas la capacité. Si abandonné, le noter dans
> `31-08-SUMMARY.md` et dans le brouillon de réponse à l'issue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-CONTEXT.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : présenter le plan avant de poser, dans les deux skills consommateurs</name>
  <files>plugin/installer/SKILL.md, plugin/conductor/skills/vf-calibrate/SKILL.md</files>
  <read_first>
    - plugin/installer/SKILL.md:63-134 (la Séquence complète ; l'étape 5 « Install scopée (INST-04 — déléguée, scope unique partout) » porte déjà un récapitulatif de la FERMETURE de modules, jamais un manifeste fichier par fichier)
    - plugin/installer/SKILL.md:21-62 (le bloc « Câblage du cache » — la table des invocations exactes ; toute nouvelle invocation doit en respecter la forme)
    - plugin/conductor/skills/vf-calibrate/SKILL.md:60-98 (étape 3 « Proposer un plan de migration (jamais l'appliquer en silence) » + étape 4 « Appliquer sous contrôle », dont le point 2 porte l'invocation `update <module>`)
    - 31-CONTEXT.md §3 D-31-10 (édition minimale, aucun nouveau point de décision)
    - 31-04-SUMMARY.md (surface exacte du flag et forme des lignes de plan)
    - gh issue 20 (repris dans 31-CONTEXT.md §1) : « présenter le manifeste, PUIS poser après accord »
  </read_first>
  <action>
**`plugin/installer/SKILL.md`, étape 5.** Insérer, AVANT les puces d'invocation existantes de
l'étape 5, une puce qui lance la même invocation d'engine que la puce « Modules VibeFlow » mais
préfixée du flag, en réutilisant à l'identique la forme d'invocation de la table « Chemins réels » :
`VIBEFLOW_CACHE="$VIBEFLOW_CACHE" bash "$VIBEFLOW_CACHE/_internal/vibeflow-update.sh" --scope <s> --dry-run install --with-deps <module>`.
Deux phrases seulement autour : afficher la sortie TELLE QUELLE à l'utilisateur (c'est du stdout,
capturable sans avaler les diagnostics), et poser ensuite avec la commande déjà présente. Aucun
nouveau point de décision : le récapitulatif de fermeture de l'étape 5 sert déjà de moment de
confirmation, le plan fichier-par-fichier l'enrichit au lieu d'ouvrir une seconde question.

**`plugin/conductor/skills/vf-calibrate/SKILL.md`, étape 4 point 2.** Dédoubler le point 2 :
d'abord `… vibeflow-update.sh --dry-run update <module>` dont la sortie est montrée à
l'utilisateur, puis la commande `update <module>` existante, inchangée. Rattacher explicitement ce
geste au feu vert déjà exigé par l'étape 3 (« Présenter le plan à l'utilisateur. Attendre son feu
vert. (ADR-031.) ») : le plan fichier-par-fichier est le contenu de ce plan de migration, pas un
second feu vert.

Dans les deux fichiers, mentionner en une demi-phrase que `--dry-run` n'écrit rien et qu'il est
refusé sur `uninstall` — un utilisateur qui lit le skill doit savoir que le mode ne le protège pas
sur le verbe destructif (D-31-06, remontée §7 pour Samuel).

Contraintes de forme : ne pas dépasser les limites de densité d'ADR-029 ; ne renuméroter aucune
étape ; ne toucher à aucune autre section des deux fichiers.
  </action>
  <acceptance_criteria>
    - `grep -c 'dry-run' plugin/installer/SKILL.md` rend au moins `2`.
    - `grep -c 'dry-run' plugin/conductor/skills/vf-calibrate/SKILL.md` rend au moins `2`.
    - Aucune étape renumérotée : `grep -c '^5\. \*\*Install scopée' plugin/installer/SKILL.md` rend `1` et `grep -c '^### 4\. Appliquer sous contrôle' plugin/conductor/skills/vf-calibrate/SKILL.md` rend `1`.
    - L'invocation reprend la forme de la table existante : la ligne ajoutée dans `installer/SKILL.md` contient `_internal/vibeflow-update.sh` et `--scope`.
    - `bash scripts/check-machine-paths.sh` sort 0 (aucun chemin machine dans les exemples ajoutés).
    - Aucun appel GitHub : `[ "$(git diff | grep -c '^+.*gh issue')" = 0 ]` sort 0 (forme booléenne explicite — `grep -c` seul sort en code retour 1 quand le compte est 0, l'inverse du verdict recherché s'il est chaîné en garde).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'dry-run' plugin/installer/SKILL.md && grep -q 'dry-run' plugin/conductor/skills/vf-calibrate/SKILL.md && bash scripts/check-machine-paths.sh</automated>
  </verify>
  <done>Les deux skills présentent le plan fichier-par-fichier avant de poser, sans nouveau point de décision.</done>
  <reversibility rating="reversible">Deux éditions de documentation de skill ; retour arrière = revert du commit, l'engine est inchangé.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : bumps de version des modules touchés (discipline du repo)</name>
  <files>plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/conductor/VERSION (valeur courante) et plugin/conductor/CHANGELOG.md (les 40 premières lignes : forme des entrées, ordre antéchronologique)
    - scripts/check-version-sync.sh (ce qu'il gate réellement : marketplace.json, plugin.json, badges des 2 README, compteur de MODULES, historique README vs VERSION, en-tête `**Version**` des README de modules — ET, depuis la révision D-31-08/commit `1baf63a`, §9 : le compteur « N suites » des deux README contre `find plugin scripts -path '*/tests/test-*.sh' | grep -c .`. Ce plan-ci ne touche ni les suites ni ce compteur — le gate doit simplement rester vert en l'état sur ce commit)
    - CLAUDE.md § « Discipline de release » (la VERSION racine et son tag sont un geste humain — hors périmètre de ce plan)
    - 31-CONTEXT.md §3 D-31-10 (bumps des modules touchés) et §4 point 8 (PR/merge/release racine interdits)
  </read_first>
  <action>
Le module `conductor` porte `skills/vf-calibrate/SKILL.md` : bumper son `VERSION` d'un cran MINOR
(nouvelle capacité présentée à l'utilisateur) et ajouter en tête de son `CHANGELOG.md` une entrée
à la forme des précédentes, en français, décrivant le geste : l'étape 4 de `/vf-calibrate` présente
désormais le plan `--dry-run` de l'engine avant de rafraîchir un module.

`plugin/installer/` ne porte ni `VERSION`, ni `module.json`, ni `CHANGELOG.md` : ce n'est pas un
module installable (il est ignoré par `list_available_modules`, qui exige un `VERSION`). Vérifier
ce fait sur pièce avant de conclure — s'il en a acquis un depuis le cadrage, le bumper aussi.
Sinon, ne rien créer : inventer une VERSION pour ce dossier serait un changement de statut de
module, hors périmètre.

Ne toucher NI `VERSION` racine, NI `plugin/.claude-plugin/plugin.json`, NI
`.claude-plugin/marketplace.json` : cette phase ne fait aucune release, et la discipline de tag du
`CLAUDE.md` en fait un geste humain explicite.
  </action>
  <acceptance_criteria>
    - `plugin/conductor/VERSION` a un cran MINOR de plus qu'avant le commit, et la même valeur apparaît en tête de `plugin/conductor/CHANGELOG.md`.
    - `bash scripts/check-version-sync.sh` sort 0.
    - `git diff --name-only` de ce commit ne contient ni `^VERSION$`, ni `plugin/.claude-plugin/plugin.json`, ni `.claude-plugin/marketplace.json`.
    - Le statut de `plugin/installer/` est constaté sur pièce et consigné dans le SUMMARY : `ls plugin/installer/VERSION plugin/installer/module.json 2>/dev/null | wc -l` et sa valeur.
    - `bash scripts/check-machine-paths.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && bash scripts/check-machine-paths.sh && ! git diff --cached --name-only 2>/dev/null | grep -qx 'VERSION' && ! git diff --name-only 2>/dev/null | grep -qx 'VERSION'</automated>
  </verify>
  <done>Le module touché est bumpé et son changelog le dit ; la racine est intacte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan affiché -> feu vert humain (ADR-031) | le skill présente ce que l'engine annonce ; un plan tronqué ou mélangé aux diagnostics fausserait le consentement |
| skill distribué -> lab de l'utilisateur | ce que ces deux fichiers disent est ce que le parc fera |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-19 | Spoofing | consentement obtenu sur un plan incomplet | high | mitigate | la complétude du plan est garantie en amont par T10 (égalité totale, 31-04) ; ce plan n'ajoute aucune transformation de la sortie — elle est affichée TELLE QUELLE |
| T-31-20 | Elevation of Privilege | utilisateur croyant que `--dry-run` protège aussi `uninstall` | medium | mitigate | demi-phrase explicite dans les deux skills disant que le flag est refusé sur `uninstall` ; le refus lui-même est bruyant côté engine (T-31-07, 31-04) |
| T-31-21 | Repudiation | module modifié sans bump, donc non distribué | medium | mitigate | bump VERSION + CHANGELOG en critère d'acceptation, `check-version-sync.sh` vert |
| T-31-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par cette phase (31-RESEARCH.md § Package Legitimacy Audit : N/A) |
</threat_model>

<verification>
1. `grep -q 'dry-run' plugin/installer/SKILL.md && grep -q 'dry-run' plugin/conductor/skills/vf-calibrate/SKILL.md`
2. `bash scripts/check-version-sync.sh`
3. `bash scripts/check-machine-paths.sh`
4. Non-régression du moteur (ce plan ne le touche pas, mais la vague le rejoue) :
   `bash plugin/_internal/tests/test-manifest.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh`
</verification>

<success_criteria>
- Les deux skills présentent le plan avant de poser, en réutilisant leur feu vert existant.
- Aucune étape renumérotée, aucune refonte d'UX, aucun nouveau point de décision.
- `conductor` est bumpé (VERSION + CHANGELOG) ; la racine est intacte.
- Le statut non-module de `plugin/installer/` est constaté sur pièce, pas supposé.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribution de ce plan)

**Éditions de skills**
- `plugin/installer/SKILL.md` étape 5 — invocation `--dry-run install --with-deps <module>` avant la pose
- `plugin/conductor/skills/vf-calibrate/SKILL.md` étape 4 point 2 — invocation `--dry-run update <module>` avant le refresh

**Bumps**
- `plugin/conductor/VERSION` (minor) + entrée en tête de `plugin/conductor/CHANGELOG.md`
</artifacts_produced>

<output>
Créer `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-06-SUMMARY.md` à la fin.
Si ce plan est ABANDONNÉ, écrire quand même un SUMMARY d'une page disant qu'il l'a été et pourquoi.
</output>

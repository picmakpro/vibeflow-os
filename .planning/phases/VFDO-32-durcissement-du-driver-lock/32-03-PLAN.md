---
phase: 32-durcissement-du-driver-lock
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - plugin/conductor/scripts/guard-driver-lock.sh
  - plugin/conductor/scripts/tests/test-guard-driver-lock.sh
  - plugin/conductor/hooks/hooks.json
  - docs/HOOKS-CONTRAT-SORTIE.md
  - README.md
  - README.fr.md
  - plugin/_internal/tests/test-vf-portable.sh
autonomous: false
requirements: [LOCK-02, LOCK-03, QUAL-01]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Un commit emis par une session qui n'est pas dans meta.session_ids, sous un lock PRESENT et NON PERIME, est refuse a la source par une decision JSON permissionDecision deny (LOCK-02, incident I4)"
    - "Un checkout/switch de branche dans les memes conditions est refuse par le MEME guard — pas par reference-transaction, ecarte par le spike (D-32-A, LOCK-03)"
    - "Une ecriture Write/Edit sous .planning/ dans les memes conditions est refusee (D-32-B, incident I2 — l'angle mort du critere ecrit initial)"
    - "Le DETENTEUR passe toujours — cas de discriminance present dans chaque groupe de refus, jamais retire (fragilite n°3 du rejeu)"
    - "Un sous-agent de la mission detentrice passe : payload.session_id est partage avec la session parente (mesure 32-TERRAIN.md §9)"
    - "Le motif de refus NOMME la commande exacte de re-rattachement — la recuperabilite vient du motif, pas de la permissivite (D-32-03(c), mesure DIV-1 : le motif est rendu au modele mot pour mot)"
    - "Un lock ABSENT, PERIME, ou dont le meta ne porte pas d'identite (lock d'avant la Phase 32) ne bloque RIEN — allow silencieux (regles 1 et 2 de D-32-03, retrocompatibilite obligatoire)"
    - "Quatre issues, pas trois : PASS · DENY · payload imparsable -> fail-open SILENCIEUX (rc 0 ET stdout vide, les deux assertes) · interprete indisponible -> fail-open BRUYANT (vf_guard_unavailable, code 17, marqueur de sante ecrit) (QUAL-01)"
    - "`git worktree add` est nommement epargne — c'est la porte de sortie que le lock lui-meme prescrit (D-32-06)"
    - "Une echappatoire explicite existe et fonctionne : sans elle un lock zombie non perime rend le depot non-commitable et enseigne le contournement (D-32-06, regression racontee par guard-file-size.sh)"
    - "Les DEUX entrees naissent de merge-hooks en forme exec, avec un command absolu executable sans metacaractere shell, en cible --settings-local (D-32-C, gate PORT-05)"
    - "Le CHAINAGE PAR SAUT DE LIGNE est traite comme un separateur au meme titre que `&&`/`;`/`|` — un payload Bash multi-lignes (forme normale d'un agent qui enchaine plusieurs commandes) ne doit JAMAIS laisser un geste concerne invisible au motif qu'il n'est pas le premier segment de la commande (BL-1 de la re-validation, incident I4 rejoue tel quel sinon)"
    - "Les OPTIONS GLOBALES de `git` (`-C`, `-c`, `--git-dir`, `--work-tree`, `--exec-path`, `-P`) sont sautees AVANT la lecture du sous-verbe — sinon `git -C /repo commit` ou `git -c core.hooksPath=/dev/null commit` ne sont jamais reconnus comme un commit (BL-2 de la re-validation)"
    - "`git rebase/merge/cherry-pick --abort`/`--continue`/`--skip`/`--quit` sont NOMMEMENT exemptes, sur le meme patron que l'exemption `worktree add` — sinon le guard neutralise sa propre issue de secours pour une session qui a demarre AVANT la pose du lock, exactement le wedge que le spike 32-SPIKE a servi a rejeter pour `reference-transaction` (BL-7 de la re-validation)"
  artifacts:
    - plugin/conductor/scripts/guard-driver-lock.sh
    - plugin/conductor/scripts/tests/test-guard-driver-lock.sh
    - "plugin/conductor/hooks/hooks.json : deux entrees PreToolUse en forme exec (matcher Bash, matcher Write|Edit)"
    - "docs/HOOKS-CONTRAT-SORTIE.md : inventaire porte de 26 a 28 entrees, assertion de recomptage mise a jour avec lui"
  key_links:
    - "payload.session_id (mesure) <-> meta.session_ids (plan 32-01) : LE couple qui discrimine ; si le champ n'est pas preserve au heartbeat, le guard denie le detenteur"
    - "motif de refus -> `driver-lock.sh reclaim --owner=<owner>` (plan 32-02) : la porte de sortie in-band ; le verbe doit EXISTER, sinon le motif ment"
    - "hooks.json (forme exec, {{VF_BASH}}) -> merge-hooks is_local_entry -> settings.local.json -> gate PORT-05 (ci.yml:800-901) : expect_total bouge automatiquement des deux cotes"
    - "docs/HOOKS-CONTRAT-SORTIE.md §4 -> test-check-hook-paths.sh T12 : l'inventaire et le parc s'accordent ou la suite rougit — la tolerance d'ecart transitoire ne couvre que +1, cette phase en ajoute 2"
  prohibitions:
    - "Ne JAMAIS bloquer par le code de sortie — la voie du depot est la decision JSON, code de sortie zero (docs/HOOKS-CONTRAT-SORTIE.md, mesure DIV-2 : l'autre voie fuit le chemin absolu du script)"
    - "Ne JAMAIS fail-open sur un mismatch d'identite : le fail-open est reserve a l'INDETERMINATION (pas de lock, lock perime, meta illisible, guard indisponible), jamais au cas ou l'on SAIT que l'appelant n'est pas le detenteur (D-32-03(b))"
    - "Ne JAMAIS armer l'ENTREE via un settings local — seule la COMMANDE peut etre locale (D-32-C, regression #38)"
    - "Ne JAMAIS utiliser reference-transaction pour LOCK-03 (ecarte par le spike : wedge de rebase --abort, contournement core.hooksPath, degats sur worktree add)"
    - "Ne pas emettre de message sur un allow : mesure DIV-3, le canal n'atteint pas le modele — ce serait un vert de complaisance"
---

<objective>
Poser le SEUL enforcement a la source de la phase : un guard `PreToolUse` unique (D-32-05) qui
compare l'identite de session de l'appelant a celle enregistree dans le lock, et refuse les gestes
mutants d'une session tierce — par decision JSON, avec un motif qui porte sa propre porte de sortie.

Purpose: `driver-lock.sh` n'est consulte que par les managers (SUMMARY du quick 260801-17w). I4
(2026-08-16, 8 commits sous un lock VIVANT) et I3 (checkout, arbre partage) ne pouvaient etre
arretes que la, a la source. I2 est passe par `Write`/`Edit`, pas par `Bash` — c'est l'angle mort
que D-32-B ferme.

Output: `guard-driver-lock.sh`, sa suite neuve portant les scenarios A/B/C du rejeu au niveau
definitif, deux entrees de hook en forme exec, l'inventaire durable des hooks remis a l'heure, et
la preuve d'armement executee sur un lab jetable — pas sur l'arbre source.

**Ordre de commit impose** : chaque tache laisse le depot VERT. La suite neuve et les deux
compteurs de README naissent dans le MEME commit (sinon `check-version-sync.sh` §9 rougit) ; les
deux entrees de `hooks.json` et l'inventaire `docs/HOOKS-CONTRAT-SORTIE.md` naissent dans le MEME
commit (sinon `test-check-hook-paths.sh` T12 rougit — sa tolerance d'ecart transitoire ne couvre
que +1, et cette tache en ajoute 2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-TERRAIN.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-REJEU-contournements.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-SPIKE-reference-transaction.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-01-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-02-SUMMARY.md
@docs/HOOKS-CONTRAT-SORTIE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tache 1 : tranche verticale — un commit d'une session tierce, sous un lock vivant, refuse de bout en bout</name>
  <files>plugin/conductor/scripts/guard-driver-lock.sh, plugin/conductor/scripts/tests/test-guard-driver-lock.sh, README.md, README.fr.md</files>
  <read_first>
    - plugin/software-architecture/scripts/guard-file-size.sh:1-90 (LE patron du guard bloquant en forme exec : en-tete, prefiltre pur-bash, bloc canonique vf-portable:locator a COPIER VERBATIM, profil rapide, renversement du silence)
    - plugin/software-architecture/scripts/guard-file-size.sh:150-168 (forme exacte du deny JSON et du fail-open silencieux terminal)
    - plugin/conductor/scripts/guard-agent-write.sh:1-35 (patron d'ARCHITECTURE a reprendre — surtout la clause de limites assumees en en-tete ; sa logique metier de validation d'agent n'a AUCUN rapport ici, D-32-05 leve l'ambiguite dans ce sens)
    - plugin/_internal/lib/vf-portable.sh:126-158 (`VF_GUARD_UNAVAILABLE_EXIT_CODE`, `vf_guard_unavailable` et ses trois actions simultanees : marqueur, stderr, code rendu)
    - plugin/conductor/scripts/check-branch-claim.sh:39-41 et 118-126 (lock PERIME traite comme absent — indispensable ici : sans cela un lock zombie bloquerait tous les commits de la machine)
    - plugin/conductor/scripts/driver-lock.sh (etat apres 32-01/32-02 : `session_ids` dans le meta, verbe `reclaim`)
    - docs/HOOKS-CONTRAT-SORTIE.md §1-§3 (contrat de codes de sortie, silence = contrat de FLUX, stdout strictement vide)
    - 32-TERRAIN.md §7-§9 (contrat mesure, DIV-1/DIV-2/DIV-3, payload reel, identite de session)
    - 32-REJEU-contournements.md §3 (traces du rouge des scenarios A et B — les assertions a porter au niveau definitif)
    - README.md:124 et README.fr.md:128 (les DEUX compteurs vivants « N suites », gates par check-version-sync.sh §9 ; NE PAS toucher les mentions narratives de changelog plus bas dans chaque fichier)
  </read_first>
  <behavior>
    - A1 (scenario A du rejeu, niveau definitif) : lock frais tenu par `mission-X` avec `session_ids` valant `sess-holder` ; payload Bash `git add F && git commit -m "hors mandat"` portant `session_id: sess-intrus` rend une decision JSON `deny`, code de sortie zero.
    - A2 : ce motif de refus contient l'owner, l'etape, la branche, l'age du lock, ET la sous-chaine `reclaim --owner=mission-X`.
    - A4 (DISCRIMINANCE, ne jamais retirer) : le MEME payload portant `session_id: sess-holder` rend une sortie STRICTEMENT VIDE et un code zero.
    - B1 (scenario B du rejeu) : payload Bash `git checkout -b autre-branche` portant `sess-intrus` rend `deny`.
    - B4 (DISCRIMINANCE) : le meme checkout portant `sess-holder` rend une sortie vide.
    - R1 (regle 1) : aucun lock present du tout — le payload A1 rend une sortie vide.
    - R2 (regle 1) : lock PERIME (heartbeat antidate au-dela du TTL) — le payload A1 rend une sortie vide (un lock zombie ne bloque jamais).
    - R3 (regle 2, retrocompatibilite) : lock frais dont le meta ne porte AUCUNE ligne d'identite de session — le payload A1 rend une sortie vide.
    - P1 (prefiltre) : payload Bash `ls -la` rend une sortie vide, sans avoir eu besoin d'un interprete (le cas s'execute aussi avec un PATH prive d'interprete et doit toujours rendre zero et vide).
    - P0 (SE-1, sonde d'existence en amont) : payload Bash `git commit -m x` portant `sess-intrus`, SANS aucun lock present (`VF_DRIVER_LOCK` pointant un chemin inexistant), rend une sortie vide, ET le cas s'execute avec un PATH prive de tout interprete — la sortie doit rester vide et le code zero SANS que la resolution d'interprete ait jamais ete atteinte (preuve indirecte : le cas reussit meme quand l'unique interprete disponible est retire, ce qui serait impossible si le prefiltre spawnait quoi que ce soit sur ce payload).
    - A5 (BL-1, chainage par SAUT DE LIGNE — meme lock que A1) : payload Bash `git add F` SUIVI D'UN SAUT DE LIGNE PUIS `git commit -m "hors mandat"`, portant `sess-intrus`, rend `deny` — le commit de la deuxieme ligne n'est pas invisible au motif qu'il n'est pas le premier segment de la commande.
    - A6 (BL-2, options globales de git — meme lock que A1) : DEUX payloads distincts, `git -C /tmp commit -m x` et `git -c core.hooksPath=/dev/null commit -m x`, tous deux portant `sess-intrus`, rendent CHACUN `deny` — le sous-verbe est reconnu malgre les options globales qui le precedent.
  </behavior>
  <action>
Creer `plugin/conductor/scripts/guard-driver-lock.sh` — la tranche la plus mince qui traverse TOUTES
les couches du guard : lecture du payload, prefiltre pur-bash, resolution d'interprete, decision,
emission du refus. Un seul geste (`git commit`) et un seul outil (`Bash`) a cette tache ; la surface
complete arrive en tache 2.

En-tete de commentaires, obligatoire et complet, sur le modele de `guard-bash-registres.sh:1-29` :
role et cablage · regle de decision · **CLAUSE DE LIMITE ASSUMEE** · fail-open. La clause de limite
reprend la formulation deja en production dans ce depot (« garde-fou deterministe contre le chemin
de moindre resistance, pas une sandbox ») et nomme les contournements CONNUS et NON couverts :
interpretation indirecte (`eval`, interprete inline), alias, substitution de commande, script du
depot qui commite en interne, toute commande dont le verbe git n'apparait pas litteralement en
position de commande, et les ecritures directes sous `.planning/` (`sed -i .planning/STATE.md`,
`cat > .planning/x`, `rm .planning/x`) — la voie Bash de ce guard ne les detecte JAMAIS en position
de commande (SE-4), c'est la voie `Write`/`Edit` de la tache 2 qui porte cette garantie-la. Ecrire
aussi la limite de GRANULARITE de D-32-03(e) : le
guard garantit qu'aucune AUTRE session ne commite sous le lock, jamais qu'aucun autre acteur de la
MEME session ne le fait — le dire autrement serait mentir sur la portee.

Corps du script :

1. `set -uo pipefail` (jamais l'option d'arret sur erreur — les trois guards existants s'en
   passent pour la meme raison).

2. Lecture du payload par `cat`, puis PREFILTRE PUR-BASH, avant tout autre travail et avant tout
   spawn :
   - **(SE-1, budget de latence)** D'ABORD, une sonde d'EXISTENCE du lock, en pur bash, AVANT tout
     le reste : `[ -e "${VF_DRIVER_LOCK:-.planning/DRIVER.lock}" ]` (le meme repli de variable que
     le script du lock). Si le lock n'existe pas — le cas de la grande majorite des labs et de la
     majorite du temps sur ce depot meme — sortir zero IMMEDIATEMENT, sans meme evaluer le `case`
     de sous-chaines ci-dessous. Motif a ecrire en commentaire : la mesure de terrain (6,70 ms) qui
     justifiait le profil rapide incluait cette sonde faite en bash AVANT le spawn ; deplacer sa
     resolution APRES le spawn (comme le ferait une lecture naive du script du lock) ferait payer
     ~24 ms a CHAQUE geste git mutant d'un depot SANS lock, la situation normale. Un faux negatif
     sur ce chemin (lock cree entre la sonde et l'evaluation reelle, fenetre de quelques
     millisecondes) reste un ALLOW, jamais une perte de surete — la revalidation reelle se fait de
     toute facon plus loin, apres le spawn, sur le contenu du `meta`.
   - ENSUITE, un `case` sur la chaine brute du payload, cherchant les sous-chaines litterales des
     gestes de la surface A du rejeu (les verbes git mutants et le nom de l'outil de PR). Aucune
     correspondance : sortie zero immediate.
   - **(SE-4, decision tranchee — pas une application mecanique)** La sous-chaine `.planning` du
     dossier de planification N'EST PAS incluse dans ce prefiltre Bash. Motif : la voie Bash de ce
     guard ne gate QUE les verbes git mutants et l'outil de PR, EN POSITION DE COMMANDE — elle ne
     gate JAMAIS une redirection ou un editeur en ligne de commande (`sed -i .planning/STATE.md`,
     `cat > .planning/x`, `rm .planning/x`) : le rejeu (surface A) les liste comme des ecritures
     possibles, mais AUCUNE tache de ce plan ne les detecte en position de commande — les couvrir
     demanderait une surface de detection separee (verbes d'ecriture de fichier generiques, hors
     budget de cette phase, remontee comme reliquat au plan 32-06). Faire payer un spawn a
     `sed -i .planning/STATE.md` pour un guard qui, au bout du spawn, laisse TOUJOURS passer
     (aucune branche de decision ne le refuse) serait une regression de latence PURE, sans aucun
     gain de surete — c'est exactement ce que mesure SE-4 de la re-validation. La voie `Write`/`Edit`
     (tache 2) reste, elle, le mecanisme REEL de protection du dossier de planification contre les
     ecritures d'outil — c'est elle qui porte la garantie, pas une sous-chaine dans le prefiltre
     Bash.
   - Ecrire en commentaire pourquoi ce prefiltre (sonde d'existence + verbes git/PR) est un
     SURENSEMBLE STRICT du domaine de refus de la voie Bash (le python ne peut denier que si l'un de
     ces verbes figure en position de commande) et le chiffrage mesure qui le justifie
     (32-TERRAIN.md §11 : un spawn d'interprete coute ~3,6x le cout pur-bash, paye sur CHAQUE appel
     Bash de CHAQUE session avec un lock present). Volontairement absents du prefiltre : les verbes
     de LECTURE (`status`, `log`, `diff`, `show`) — ils ne peuvent jamais etre refuses, donc les
     faire payer un spawn serait une regression de latence pure.

3. **(BL-5, source corrigee)** Bloc canonique `vf-portable:locator`, COPIE VERBATIM — **JAMAIS
   depuis `plugin/_internal/lib/vf-portable.sh`, qui ne porte AUCUN marqueur `vf-portable:locator`
   (verifie, instruction inexecutable telle qu'ecrite dans une version anterieure de ce plan)** —
   mais depuis l'un de ses QUATRE consommateurs reels, par exemple
   `plugin/software-architecture/scripts/guard-file-size.sh:37-73`, entre ses deux marqueurs. Copier
   depuis N'IMPORTE LEQUEL des quatre consommateurs actuels rend un bloc identique (c'est precisement
   ce que verifie leur somme de controle commune dans `test-vf-portable.sh`, cas T12) ; `guard-file-size.sh`
   est cite ici comme reference parce qu'il est deja liste au `read_first` de cette tache. Jamais
   retape a la main.

   **Cinquieme consommateur — ecriture DELIBEREE hors `conductor`, couverte par l'amendement du
   2026-08-17 (D-32-07 amende, voir 32-CONTEXT.md)** : `guard-driver-lock.sh` devient le CINQUIEME
   consommateur de ce bloc canonique, mais `test-vf-portable.sh` (module `_internal`) code en dur une
   liste FERMEE de quatre consommateurs (cas T12 : `T12_GFS`, `T12_IMT`, `T12_TDO`, `T12_CHP`). Sans
   ajout, ce cinquieme consommateur n'est couvert par AUCUNE garantie de non-derive — un vert de
   complaisance au sens exact de QUAL-01. Ajouter `guard-driver-lock.sh` a cette liste dans
   `plugin/_internal/tests/test-vf-portable.sh` (T12), DANS LE MEME COMMIT que la creation du guard.
   C'est une ecriture reelle hors `plugin/conductor/**`, explicitement assumee : voir l'ecart signale
   ci-dessous, amende par la decision du 2026-08-17.

4. Resolution d'interprete en profil RAPIDE. En cas d'echec : appeler `vf_guard_unavailable` avec le
   nom du script et un motif, puis sortir avec le code qu'elle rend. C'est le renversement du
   silence (issue 4 de QUAL-01) — pas un fail-open muet.

5. UN SEUL spawn d'interprete, le payload rejoue sur son entree standard. Logique de cette tache :
   - echappatoire : si la variable d'environnement d'exception vaut 1, ou si le marqueur
     `vibeflow:allow-lock-override` figure dans la commande, tenter d'ABORD une ligne d'evenement
     `{"event": "override", "session_id": …, "command": …(tronquee)}` en APPEND sur le journal
     `${LOCK_BASE}.events.log` du plan 32-02 (meme chemin, meme discipline d'ajout — BEST-EFFORT,
     un echec d'ecriture n'empeche JAMAIS l'echappatoire de fonctionner), PUIS sortir 0 sans rien
     emettre sur la sortie standard. Motif (SE-2 de la re-validation) : le motif de refus est rendu
     au modele mot pour mot (mesure DIV-1) — un agent bloque recoit l'interdiction ET la recette
     pour la lever, sans aucune friction ; contrairement a `guard-file-size.sh` ou le marqueur
     s'ecrit DANS le fichier et le motif exige une entree de dette (une trace qui coute), cette
     echappatoire-ci ne laissait jusqu'ici AUCUNE trace. La journaliser transforme une passoire
     silencieuse en fait auditable, sans ajouter de friction a l'usage legitime (une mission qui a
     de bonnes raisons de forcer le passage) ;
   - resoudre le chemin du lock (variable `VF_DRIVER_LOCK`, defaut `.planning/DRIVER.lock`),
     relativement au `cwd` du payload s'il est relatif ;
   - lire le `meta` du lock ; absent, illisible ou vide : sortir 0 (fail-open silencieux,
     indetermination) ;
   - calculer l'age depuis `heartbeat_epoch` avec le meme TTL que le script du lock (variable
     `VF_DRIVER_TTL`, defaut 1800, meme garde de numericite) ; age superieur au TTL : sortir 0. Un
     lock perime est traite comme absent, exactement comme le fait deja `check-branch-claim.sh` —
     motif a recopier en commentaire : sinon un manager mort gele tout le monde ;
   - lire `session_ids` ; liste vide ou cle absente : sortir 0. C'est la regle 2 de D-32-03. NOTE DE
     LECTURE A ECRIRE EN COMMENTAIRE : le « repli sur comparaison worktree » de D-32-03 est realise
     comme un allow inconditionnel, parce que l'issue de la regle 2 est un allow dans les deux cas
     et que l'axe worktree est deja couvert, en advisory, par `check-branch-claim.sh` au
     SessionStart — coder ici une comparaison dont aucune branche ne peut refuser serait du code
     mort deguise en garantie ;
   - si le nom d'outil du payload n'est pas `Bash`, sortir 0 (la voie Write/Edit arrive en tache 2) ;
   - tronquer la commande au premier marqueur de document en ligne (le contenu d'un tel document est
     du TEXTE, pas des commandes — sans cela une documentation qui CITE un commit serait refusee) ;
   - decider si la commande porte, EN POSITION DE COMMANDE, un geste concerne. A cette tache, le
     seul geste implemente est le commit ; la segmentation et la detection de position de commande
     sont reprises de `guard-bash-registres.sh` (fonction `command_positions`) — reutilisees, pas
     reinventees, avec DEUX corrections locales a ce guard (BL-1, BL-2 de la re-validation — la
     fonction source reste intacte dans `guard-bash-registres.sh`, ces corrections vivent dans la
     copie/adaptation propre a `guard-driver-lock.sh`, jamais en modifiant le fichier du module
     `consolidator`) :
     - **(BL-1, chainage par SAUT DE LIGNE)** `guard-bash-registres.sh` segmente sur
       `\|\||&&|;|\|` mais PAS sur le saut de ligne — un payload Bash MULTI-LIGNES (`git add F` puis
       une nouvelle ligne `git commit -m x`) est la forme NORMALE d'un agent qui enchaine plusieurs
       commandes, et `command_positions` s'arrete au premier token non-wrapper : le commit de la
       deuxieme ligne resterait INVISIBLE. Avant de segmenter sur les operateurs de chainage,
       decouper d'abord le payload sur `\n` et `\r`, puis appliquer la detection de position de
       commande a CHAQUE ligne independamment (chacune peut elle-meme porter un chainage par
       operateur classique). C'est un decoupage EN AMONT de la fonction reprise, pas une
       modification de son corps ;
     - **(BL-2, options globales de `git`)** la fonction reprise rend l'index du token `git`, pas de
       son sous-verbe. Avant de lire le token qui suit ce token `git`, sauter toute sequence
       d'options globales connues (`-C <chemin>`, `-c <cle>=<valeur>`, `--git-dir=…`,
       `--work-tree=…`, `--exec-path=…`, `-P`) — chacune consomme un ou deux tokens selon sa forme
       (`-C repo` vs `-Crepo`, `-c x=y` vs `-cx=y`) — jusqu'au premier token qui n'en est pas une :
       C'EST LUI le sous-verbe a comparer a la liste des gestes concernes. Sans ce saut,
       `git -C /repo commit -m x` et `git -c core.hooksPath=/dev/null commit -m x` — cette derniere
       forme neutralisant precisement les hooks git — ne sont jamais reconnus comme un commit ;
   - si l'identite de session du payload figure dans la liste du lock : sortir 0 (regle 3 : couvre
     le manager ET ses workers, mesure §9) ;
   - sinon : emettre la decision JSON de refus et sortir zero. Le motif porte l'owner, l'etape, la
     branche, l'age en minutes, PUIS la marche a suivre en deux branches nommees : si tu ES le
     detenteur avec un identifiant neuf, la commande exacte de re-rattachement ; sinon, attendre,
     ou travailler dans un arbre separe, ou poser le marqueur d'echappatoire pour ce geste precis.
     Ce motif est un CANAL D'INSTRUCTION, pas une trace pour l'humain seul : la mesure DIV-1 etablit
     qu'il est rendu au modele mot pour mot ;
   - toute exception interne : sortir 0 sans rien emettre (fail-open silencieux).

   Le script se termine par une sortie zero inconditionnelle. Le blocage passe par la DECISION,
   jamais par le code de sortie : le code reserve au harness ne doit apparaitre nulle part, et la
   mesure DIV-2 montre qu'il fuit en plus le chemin absolu du script.

Creer ensuite `plugin/conductor/scripts/tests/test-guard-driver-lock.sh`, a la convention EXACTE du
dossier `plugin/conductor/scripts/tests/` (pas celle de `software-architecture`) : `set -uo pipefail`
sans arret sur erreur, `mktemp -d` plus `trap … EXIT`, systeme sous test redirige par variables
`VF_*`, compteurs et helpers declares dans la suite, groupement par blocs `=== … ===`, epilogue qui
sort 0 si aucun echec et 1 sinon.

Points de forme imposes :
  - les payloads sont construits par un helper qui passe par un interprete (echappement JSON fiable),
    jamais par concatenation de chaines a la main ;
  - les fixtures de lock sont posees en APPELANT `driver-lock.sh` (jamais en fabriquant un `meta` a
    la main) ; la peremption et l'absence d'identite passent par des helpers dedies, sur le modele
    d'`age_stale` — un test qui edite le meta a la main casse au premier changement de protocole ;
  - PREFLIGHTS SEPARES DES ASSERTIONS (§4 du rejeu) : chaque groupe verifie d'abord que son fixture
    est sain (lock present, non perime, `session_ids` peuple, guard executable). Un preflight en
    echec sort **2** — « fixture casse, pas de verdict metier » — jamais 1 ;
  - les cas de discriminance A4 et B4 sont ecrits DANS LE MEME BLOC que A1 et B1, avec un
    commentaire disant pourquoi ils ne doivent jamais etre retires : sans eux, un guard qui
    bloquerait tout le monde passerait pour correct (fragilite n°3 du rejeu).

**Dans le MEME commit** : mesurer le nombre reel de suites par la commande de decouverte de la CI
(`find plugin scripts -type f -path '*/tests/test-*.sh'`), APRES creation de la suite, et reporter
cette valeur MESUREE — jamais supposee — dans les deux compteurs vivants des README, en preservant
mot pour mot la formulation environnante de chaque fichier. Ne toucher aucune autre mention
chiffree de ces fichiers.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/guard-driver-lock.sh` sort 0.
    - Le bloc canonique est present et non altere : `grep -c 'vf-portable:locator' plugin/conductor/scripts/guard-driver-lock.sh` rend `2` (les deux marqueurs).
    - (BL-5) Le CINQUIEME consommateur est couvert par la somme de controle commune : `grep -c 'guard-driver-lock.sh' plugin/_internal/tests/test-vf-portable.sh` rend au moins `1`, et `bash plugin/_internal/tests/test-vf-portable.sh` sort 0 avec son cas T12 en PASS (jamais un echec « consommateur introuvable »).
    - Le blocage ne passe jamais par le code de sortie (LIGNES DE CODE SEULES, commentaires filtres — sans ce filtre le critere s'auto-invaliderait des que l'en-tete explique la regle) : `grep -v '^[[:space:]]*#' plugin/conductor/scripts/guard-driver-lock.sh | grep -cE 'exit[[:space:]]+2$'` rend `0`.
    - Aucun message emis sur un allow (canal mesure comme n'atteignant pas le modele) : `grep -v '^[[:space:]]*#' plugin/conductor/scripts/guard-driver-lock.sh | grep -c 'systemMessage'` rend `0`.
    - La clause de limite assumee existe : `sed -n '1,60p' plugin/conductor/scripts/guard-driver-lock.sh | grep -ci 'limite'` rend au moins `1`.
    - La clause de limite assumee NOMME `.planning/` : `sed -n '1,60p' plugin/conductor/scripts/guard-driver-lock.sh | grep -c '\.planning'` rend au moins `1` (sans cela, le critere precedent passerait meme si les ecritures directes sous `.planning/` ne sont jamais mentionnees comme contournement connu).
    - `bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh` sort 0 et affiche les blocs A1/A2/A4, A5, A6, B1/B4, R1/R2/R3, P0 et P1.
    - La suite est decouverte par la CI : `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c 'test-guard-driver-lock.sh'` rend `1`.
    - Les deux compteurs sont a la valeur mesuree : `N=$(find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l | tr -d ' '); grep -q "$N suites in CI" README.md && grep -q "\`$N suites\` en CI" README.fr.md`
    - `bash scripts/check-version-sync.sh` sort 0.
    - **Mutation rouge prouvee dans les DEUX sens, trace obligatoire dans le SUMMARY** : (a) rendre le guard permissif sur mismatch (sortir 0 au lieu d'emettre le refus) rend A1 et B1 rouges ; (b) rendre le guard aveugle a la liste d'identites (refuser sans consulter `session_ids`) rend A4 et B4 rouges — c'est la preuve que la discriminance n'est pas un faux vert ; (c) retirer le traitement du lock perime comme absent rend R2 rouge ; (d) retirer le decoupage prealable sur `\n`/`\r` rend A5 rouge (BL-1) ; (e) retirer le saut des options globales de `git` rend A6 rouge sur au moins une des deux formes (BL-2). Consigner assertion, attendu, obtenu pour chacune ; restaurer par `git checkout --` ; revalider le vert.
    - **(SE-7) Controle positif du fixture de R3** : le lock de R3 est pose par un appel a `driver-lock.sh acquire` SANS jamais editer le `meta` a la main — avant d'invoquer le guard, verifier explicitement que ce `meta` ne porte AUCUNE ligne `session_ids=` (fixture obtenue par un helper equivalent a `meta_drop_key` du plan 32-01, jamais par une fabrication manuelle). Consigner ce controle dans le SUMMARY : sans lui, R3 pourrait passer parce que le fixture n'exerce jamais reellement la regle 2 de retrocompatibilite.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/guard-driver-lock.sh && bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh && bash scripts/check-version-sync.sh && bash plugin/_internal/tests/test-vf-portable.sh && [ "$(grep -v '^[[:space:]]*#' plugin/conductor/scripts/guard-driver-lock.sh | grep -cE 'exit[[:space:]]+2$')" -eq 0 ]</automated>
  </verify>
  <done>Le scenario A du rejeu vire au VERT sans qu'une ligne de sa logique ait ete negociee : un commit tiers sous un lock vivant est refuse, le detenteur passe, un lock absent ou perime ne bloque rien, et le motif de refus porte deja sa porte de sortie ; le cinquieme consommateur du bloc localisateur est couvert par la somme de controle commune.</done>
  <reversibility rating="reversible">Le script n'est encore branche par aucune entree de hook : tant que la tache 4 n'a pas tourne, il ne s'execute nulle part. Le retour arriere est la suppression de deux fichiers neufs, le retrait de la ligne ajoutee a `test-vf-portable.sh`, et le retour des deux compteurs.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : expansion — toute la surface des gestes, la voie Write/Edit, l'echappatoire et l'exemption nommee (D-32-06, D-32-B)</name>
  <files>plugin/conductor/scripts/guard-driver-lock.sh, plugin/conductor/scripts/tests/test-guard-driver-lock.sh</files>
  <read_first>
    - plugin/consolidator/scripts/guard-bash-registres.sh:106-133 (`command_positions` — CSL-04, wrappers transparents et affectations sautees)
    - plugin/consolidator/scripts/guard-bash-registres.sh:73 (troncature au premier marqueur de document en ligne — CSL-05)
    - plugin/consolidator/scripts/guard-bash-registres.sh:178 (segmentation sur les operateurs de chainage)
    - plugin/software-architecture/scripts/guard-file-size.sh:91-94 et 9-12 (echappatoire, et la regression qu'elle previent : la boucle refus/reessai qui ENSEIGNE le contournement)
    - 32-REJEU-contournements.md §5 surface A et surface B (la liste exhaustive des gestes, et le tableau des outils autres que Bash)
    - 32-CONTEXT.md §Decisions D-32-06 (liste de gestes, exemption nommee, echappatoire) et D-32-B (perimetre Write/Edit)
  </read_first>
  <behavior>
    - S1 : chacun des gestes de la surface A, emis par une session tierce sous lock vivant, est refuse — un cas par famille : commit (et sa variante de reecriture), checkout, switch, restore, merge, rebase, cherry-pick, revert, reset, clean, push, tag, suppression de branche, stash, retrait de worktree, et les gestes de l'outil de PR.
    - S2 (EXEMPTION NOMMEE) : `git worktree add ../ailleurs -b b` emis par la MEME session tierce rend une sortie vide — c'est la porte de sortie que le lock prescrit, elle ne doit jamais etre fermee.
    - S3 (position de commande) : `grep -n "git commit" fichier.md` emis par une session tierce rend une sortie vide (le verbe est un motif de recherche, pas une commande).
    - S4 (document en ligne) : une commande qui ecrit un document en ligne CITANT un commit rend une sortie vide.
    - S5 (wrapper transparent) : `sudo git commit -m x` et `env FOO=1 git commit -m x` emis par une session tierce sont refuses.
    - S6 (chainage) : `ls && git commit -m x` emis par une session tierce est refuse.
    - S7 (ECHAPPATOIRE, marqueur) : `git commit -m x  # vibeflow:allow-lock-override` emis par une session tierce rend une sortie vide.
    - S8 (ECHAPPATOIRE, environnement) : le payload A1 avec la variable d'exception a 1 rend une sortie vide.
    - C1 (scenario C du rejeu, incident I2) : payload `Write` vers un fichier sous `.planning/`, session tierce, lock vivant : refuse.
    - C2 : payload `Edit` vers ce meme fichier : refuse.
    - C3 (DISCRIMINANCE) : les memes payloads portant l'identite du detenteur rendent une sortie vide.
    - C4 (perimetre) : payload `Write` vers un fichier HORS `.planning/` rend une sortie vide, meme sous lock tiers — la voie Write/Edit est bornee au dossier de planification par D-32-B.
    - C5 : payload d'un outil hors des trois couverts rend une sortie vide.
    - L1 (LIMITE ASSUMEE, rouge documente et NON corrige) : `bash ./scripts/release.sh` — un script du depot qui commite en interne — emis par une session tierce rend une sortie VIDE. Le cas est ecrit, nomme « hors de portee assumee », et son verdict attendu EST le passage : il documente la passoire, il ne la corrige pas.
    - S9 (BL-7, EXEMPTION NOMMEE — issue de secours) : CHACUNE des quatre formes `git rebase --abort`, `git rebase --continue`, `git merge --abort`, `git cherry-pick --abort` (au moins ces quatre, une par famille citee au frontmatter), emise par la MEME session tierce sous un lock tenu par autrui, rend une sortie VIDE. Motif : une session qui a demarre AVANT la pose du lock d'autrui doit pouvoir sortir de son propre rebase/merge/cherry-pick en cours — sinon le guard neutralise sa propre issue de secours, exactement le wedge que 32-SPIKE a servi a rejeter pour `reference-transaction`.
    - S10 (contrepoint de S9, ne jamais retirer) : `git rebase -i main` (SANS `--abort`/`--continue`/`--skip`/`--quit`) emis par la meme session tierce reste REFUSE — seules les quatre formes de sortie sont exemptees, jamais la sous-commande `rebase`/`merge`/`cherry-pick` entiere.
  </behavior>
  <action>
1. Etendre la decision du guard a toute la surface A du rejeu. Un ensemble unique de verbes git
   mutants, plus les sous-commandes de l'outil de PR, tous evalues en POSITION DE COMMANDE via la
   fonction reprise de `guard-bash-registres.sh`. **Deux** points d'exemption, nommes et commentes,
   jamais un seul (BL-7 de la re-validation ajoute le second) :
   - la sous-commande d'AJOUT de worktree, epargnee parce qu'elle est la porte de sortie que le
     verrou lui-meme prescrit quand il est tenu par autrui (D-32-06). Le RETRAIT de worktree n'est
     pas epargne — le distinguer explicitement en commentaire, pour qu'un futur lecteur ne
     « simplifie » pas l'exemption en la portant sur la sous-commande entiere ;
   - **les options `--abort`, `--continue`, `--skip`, `--quit`** des sous-commandes `rebase`,
     `merge`, `cherry-pick`, `revert`, `stash` (memes options ou une reconnue par la version de git
     du poste, exemption sur l'OPTION, jamais sur la sous-commande) — sur le MEME patron commente
     que l'exemption worktree. Motif a ecrire en commentaire, avec sa source : le 32-SPIKE a rejete
     `reference-transaction` precisement parce que, guard arme, `git rebase --abort` etait BLOQUE
     par la garde elle-meme, neutralisant l'issue de secours d'une session qui avait demarre AVANT
     la pose du lock. Ce guard-ci porte la surface S1 « ne PAS sous-analyser la ligne de commande »
     — sans cette exemption NOMMEE, il recreerait exactement le meme wedge que celui qui a fait
     rejeter l'autre mecanisme. La sous-commande SANS l'une de ces quatre options (S10) reste
     refusee : seule l'option de sortie est exemptee, jamais la sous-commande entiere.

   Ne PAS chercher a distinguer un checkout de branche d'un checkout de fichier : les deux mutent
   l'arbre partage sous le lock d'autrui, et sous-analyser la ligne de commande n'ajouterait que de
   la surface de contournement pour aucun gain de surete. Ecrire ce choix et son motif en
   commentaire — c'est une decision de plan, pas un oubli.

2. Ajouter la voie `Write` / `Edit` (D-32-B) : si le nom d'outil du payload est l'un des deux, lire
   le chemin de fichier, le normaliser (les chemins Windows arrivent en antislashs, simples ou
   doubles par l'echappement JSON — sans ces variantes la garde est inerte sous Windows en
   PARAISSANT installee, regression deja vecue et nommee par `guard-bash-registres.sh:40-42`), et
   ne poursuivre que si le chemin vise le dossier de planification. Frontiere EXACTE, jamais un
   suffixe : un dossier dont le nom se TERMINE par celui du dossier de planification ne doit pas
   matcher (patron de frontiere de `guard-agent-write.sh:64-67`). L'echappatoire par marqueur est
   cherchee, pour cette voie, dans le contenu entrant du payload.

3. Completer l'echappatoire : marqueur litteral reconnu dans la commande (voie Bash) ou dans le
   contenu entrant (voie Write/Edit), ET variable d'environnement d'exception. Documenter les deux
   dans l'en-tete, avec le motif de D-32-06 : sans echappatoire, un lock zombie non perime rend le
   depot non-commitable et enseigne le contournement par un autre outil — regression vecue,
   racontee par `guard-file-size.sh:9-12`. Nommer aussi, dans le motif de refus, le marqueur exact,
   pour que la porte de sortie soit connue de celui qui vient d'etre bloque.

4. Etendre la suite avec les cas S1 a S8, C1 a C5 et L1. Le cas L1 porte, dans son libelle ET dans
   un commentaire, la mention explicite qu'il documente une limite assumee : son vert est le
   constat d'une passoire connue, pas une garantie. Sans ce cas, la suite donnerait un vert
   SURESTIME — c'est la fragilite n°1 du rejeu.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/guard-driver-lock.sh` sort 0.
    - Les DEUX exemptions sont nommees et commentees : `grep -v '^[[:space:]]*#' plugin/conductor/scripts/guard-driver-lock.sh | grep -c 'worktree'` rend au moins `1`, `grep -v '^[[:space:]]*#' plugin/conductor/scripts/guard-driver-lock.sh | grep -cE -- '--abort|--continue|--skip|--quit'` rend au moins `1`, et les deux commentaires qui les motivent sont presents dans les 60 premieres lignes du fichier.
    - La reutilisation est reelle, pas une reinvention : le guard contient une fonction de position de commande dont le corps couvre les memes wrappers que `guard-bash-registres.sh` — le verifier a l'oeil et consigner dans le SUMMARY la liste des wrappers effectivement couverts.
    - `bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh` sort 0, affiche les blocs S1 a S10, C1 a C5 et L1, et sa ligne de bilan n'affiche aucun echec.
    - Le cas de limite assumee est etiquete comme tel : `grep -ci 'hors de portee assumee' plugin/conductor/scripts/tests/test-guard-driver-lock.sh` rend au moins `1`.
    - `bash plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (non-regression du lock lui-meme).
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : (a) retirer l'exemption d'ajout de worktree rend S2 rouge ; (b) retirer la detection de position de commande (accepter le verbe n'importe ou dans la ligne) rend S3 rouge ; (c) retirer la troncature du document en ligne rend S4 rouge ; (d) retirer la borne du dossier de planification sur la voie Write/Edit rend C4 rouge ; (e) desactiver l'echappatoire rend S7 et S8 rouges ; (f) retirer l'exemption `--abort`/`--continue`/`--skip`/`--quit` rend S9 rouge (BL-7) ; (g) etendre par erreur cette exemption a la sous-commande entiere (sans regarder l'option) rend S10 rouge — preuve que l'exemption est bien bornee a l'option de sortie ; (h) **(SE-7)** rendre le guard aveugle a l'identite de session sur la voie Write/Edit (refuser Write/Edit sans jamais consulter `session_ids`) rend C3 rouge — preuve que la discriminance de C3 n'est pas un faux vert obtenu par une autre voie (le prefiltre ou la borne de dossier). Consigner assertion, attendu, obtenu pour chacune ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/guard-driver-lock.sh && bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh</automated>
  </verify>
  <done>Les trois surfaces d'incident reelles (commit I1/I4, checkout I3, ecriture I2) sont couvertes ; la porte de sortie du worktree reste ouverte ; l'echappatoire fonctionne ; et la passoire du matching par sous-chaine est documentee par un cas de test qui la nomme au lieu de la cacher.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : les QUATRE issues de QUAL-01, prouvees separement — dont le fail-open BRUYANT et son marqueur de sante</name>
  <files>plugin/conductor/scripts/guard-driver-lock.sh, plugin/conductor/scripts/tests/test-guard-driver-lock.sh</files>
  <read_first>
    - plugin/software-architecture/scripts/tests/test-guard-file-size.sh:93-99 (T6 : entree imparsable, code ET sortie vide TOUS DEUX assertes — le patron exact de l'issue 3)
    - plugin/software-architecture/scripts/tests/test-guard-file-size.sh:145-154 (T13 : erreur interne, jamais de refus sur erreur interne)
    - plugin/_internal/lib/vf-portable.sh:132-158 (les trois actions simultanees de `vf_guard_unavailable` : marqueur horodate dans le repertoire de sante, ligne sur stderr, code rendu)
    - plugin/_internal/tests/test-vf-portable.sh (le patron existant de couverture du chemin « interprete indisponible » — a transposer, pas a dupliquer aveuglement)
    - 32-TERRAIN.md §10 (l'encadre « QUAL-01 demande trois issues ; il en faut QUATRE ») et §11 (modes de defaillance catastrophiques MESURES)
  </read_first>
  <behavior>
    - Q1 (issue 1, PASS) : deja couvert par A4/B4/C3 — le rappeler dans le bloc de synthese, ne pas le re-tester.
    - Q2 (issue 2, DENY) : deja couvert par A1/B1/C1 — idem.
    - Q3 (issue 3, IMPARSABLE -> fail-open SILENCIEUX) : entree qui n'est pas du JSON, mais qui contient une sous-chaine du prefiltre (sinon le prefiltre sort avant d'atteindre le chemin teste, et le cas ne mesurerait rien) : code de sortie ZERO **et** sortie STRICTEMENT VIDE, les deux assertes.
    - Q3b : payload JSON valide mais structurellement atypique (champ de commande absent, ou d'un autre type) : meme verdict que Q3.
    - Q3c : `meta` du lock present mais illisible (permissions retirees) : allow silencieux — jamais un refus sur erreur interne.
    - Q4 (issue 4, INDISPONIBLE -> fail-open BRUYANT) : guard invoque avec un chemin de recherche prive de tout interprete, sur le payload A1 (qui serait refuse en temps normal) : code de sortie **17**, sortie standard STRICTEMENT VIDE, une ligne de diagnostic prefixee sur la sortie d'erreur, ET un fichier marqueur cree dans le repertoire de sante redirige par variable d'environnement.
    - Q5 (anti-vert-a-vide) : la suite compte ses assertions executees et sort en echec si le total est zero.
  </behavior>
  <action>
1. Verifier et, si besoin, corriger le guard pour que les quatre issues soient REELLEMENT
   distinctes. En particulier : l'issue 3 doit rester silencieuse jusque dans son flux (aucun octet
   sur la sortie standard — le silence est un contrat de FLUX, pas seulement de code, §3 du contrat
   de sortie), et l'issue 4 doit passer par `vf_guard_unavailable` puis sortir avec le code qu'elle
   rend, sans jamais l'ecraser par un zero.

2. Ajouter les cas Q3, Q3b, Q3c, Q4 et Q5 a la suite.

   Le cas Q4 exige un environnement d'execution fabrique, decrit ici precisement parce que c'est le
   point ou une suite bacle habituellement : construire dans le repertoire de travail un dossier de
   binaires ne contenant QUE les outils dont le guard a besoin AVANT sa resolution d'interprete —
   la lecture de l'entree, la derivation de repertoire, la detection de systeme faite au chargement
   de la lib, et les outils employes par l'ecriture du marqueur (horodatage, creation de
   repertoire, deplacement, suppression) — en liant chacun depuis son emplacement reel resolu par
   `command -v`, et AUCUN interprete. Invoquer ensuite le guard avec ce dossier comme unique chemin
   de recherche et avec le repertoire de sante redirige vers le repertoire de travail.

   PREFLIGHT OBLIGATOIRE de ce cas, separe de ses assertions : verifier d'abord que le dossier
   fabrique permet bien au guard d'aller jusqu'a sa resolution d'interprete (chaque outil attendu
   est present et executable, et aucun interprete n'est joignable). Preflight en echec : sortir 2
   — fixture casse, pas de verdict. Sans ce preflight, un chemin de recherche trop pauvre ferait
   echouer le guard AVANT la resolution et le cas rendrait un vert qui ne mesure rien.

3. Ajouter le compteur d'assertions et la garde anti-vert-a-vide (Q5) a l'epilogue de la suite,
   avant le calcul du code de sortie.
  </action>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh` sort 0 et affiche les blocs Q3, Q3b, Q3c et Q4.
    - Les quatre issues sont nommees dans l'en-tete de la suite : `sed -n '1,60p' plugin/conductor/scripts/tests/test-guard-driver-lock.sh | grep -ci 'fail-open'` rend au moins `2` (le silencieux et le bruyant, distingues).
    - L'issue 4 asserte bien les TROIS effets : `grep -c '17' plugin/conductor/scripts/tests/test-guard-driver-lock.sh` rend au moins `1`, et le cas Q4 verifie l'existence d'un fichier marqueur — le consigner dans le SUMMARY avec le chemin observe.
    - Anti-vert-a-vide prouve par execution, pas en prose : neutraliser temporairement tous les appels d'assertion de la suite, relancer, verifier que le code de sortie vaut 1 ; puis annuler la mutation par `git checkout --`.
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : (a) faire emettre au guard un diagnostic sur la sortie standard dans le chemin imparsable rend Q3 rouge (c'est la moitie « sortie vide » de l'assertion qui l'attrape — preuve que les deux moities sont utiles) ; (b) remplacer l'appel a `vf_guard_unavailable` par une sortie zero muette rend Q4 rouge ; (c) faire refuser le guard quand le `meta` est illisible rend Q3c rouge. Consigner assertion, attendu, obtenu ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && bash plugin/conductor/scripts/tests/test-guard-agent-write.sh</automated>
  </verify>
  <done>Les quatre issues sont couvertes par des cas distincts et prouvees par mutation ; le chemin « le guard n'a pas pu tourner » ecrit un marqueur de sante reel, ce qui rend le lecteur du plan 32-05 utile plutot que decoratif.</done>
</task>

<task type="auto">
  <name>Tache 4 : armement — deux entrees en forme exec nees de merge-hooks, prouvees sur un lab jetable, plus l'inventaire durable (D-32-C, PORT-05)</name>
  <files>plugin/conductor/hooks/hooks.json, docs/HOOKS-CONTRAT-SORTIE.md</files>
  <read_first>
    - plugin/conductor/hooks/hooks.json (les 6 entrees existantes, et la `description` du fragment a completer)
    - plugin/software-architecture/hooks/hooks.json:6-8 (LA seule entree exec bloquante du parc a ce jour — la forme exacte a reproduire, avec sa mention explicite du caractere bloquant dans la description)
    - plugin/_internal/merge-hooks.sh:237-242 (`is_local_entry` : SEULE une entree dont le `command` brut porte le jeton d'interprete part en cible locale)
    - plugin/_internal/merge-hooks.sh:296-322 (reutilisation de groupe CONDITIONNELLE : un groupe de meme matcher n'est reutilise que s'il est entierement possede par VF)
    - plugin/_internal/merge-hooks.sh:348-368 (substitution dans `args`, et la garde dure sur un litteral shell-quote — bug v2.53.0)
    - .github/workflows/ci.yml:705-723 (comment la CI installe la fermeture dans un lab arme — le geste a reproduire en local)
    - .github/workflows/ci.yml:800-901 (gate PORT-05 : derivation de l'attendu, constat de ce que l'install a POSE, et les cinq causes de rouge)
    - docs/HOOKS-CONTRAT-SORTIE.md §4 (l'assertion de recomptage machine et l'inventaire par module) et §5 (le decompte bloquantes/advisory)
    - plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh:395-415 (T12 : l'inventaire durable et le parc doivent s'accorder ; la tolerance d'ecart transitoire ne couvre que +1)
    - 32-CONTEXT.md §Decisions D-32-C et D-32-05 (cohabitation de matcher, hypothese a verifier EMPIRIQUEMENT)
  </read_first>
  <action>
1. Ajouter DEUX entrees `PreToolUse` a `plugin/conductor/hooks/hooks.json`, toutes deux en FORME
   EXEC : le `command` est le jeton d'interprete (c'est lui, et lui seul, qui route l'entree vers la
   cible locale — `is_local_entry`), et le chemin du script vit dans `args`, jamais dans `command`.
   Aucun operateur shell, aucun suffixe d'echappement d'echec : en forme exec il n'y a pas de shell,
   un tel suffixe serait un chemin mort. Un groupe par matcher : l'un pour l'outil Bash, l'autre
   pour les deux outils d'ecriture. Choisir la chaine de matcher et CONSIGNER le choix dans le
   SUMMARY : la comparaison de matcher par `merge-hooks` est une egalite de chaine STRICTE, donc une
   chaine deja employee ailleurs dans le parc ne se confond pas avec une variante d'ordre inverse.

   Completer la `description` du fragment sur le modele de celle de `software-architecture` : dire
   explicitement que ces deux entrees sont BLOQUANTES, et que c'est leur decision JSON qui bloque
   l'appel d'outil, jamais leur code de sortie — une lecture qui ne regarderait que le code de
   sortie les classerait a tort en advisory.

2. Prouver l'armement SUR UN LAB JETABLE, jamais sur l'arbre source (patron « as-installed », c'est
   la lecon de la regression #38 : un gate qui n'exerce que l'arbre source ne prouve rien sur ce que
   l'install pose chez l'utilisateur). Le depot courant n'est PAS un lab arme — le verifier d'abord
   (son fichier de reglages ne porte aucune entree de module VF), et NE PAS l'armer : le faire
   exposerait les plans suivants au guard qu'ils sont en train d'ecrire.

   Deroule : creer un repertoire temporaire, y initialiser un depot git, resoudre la fermeture du
   module `conductor`, installer chaque module de cette fermeture avec le cache pointant sur
   l'arbre `plugin/` et la portee projet, puis assere sur les fichiers de reglages REELLEMENT
   POSES :
   - les deux entrees existent, referencent bien le script du guard dans leurs `args`, et se
     trouvent dans la cible LOCALE (consequence du routage par jeton d'interprete) ;
   - leur `command` est un chemin ABSOLU, existant et executable sur ce poste ;
   - ni le `command` ni aucun element d'`args` ne porte de metacaractere shell ;
   - aucun placeholder residuel ne subsiste dans l'un ou l'autre fichier de reglages ;
   - le compte d'entrees portant des `args`, dans les deux fichiers reunis, egale le nombre derive
     des fragments `hooks.json` de la fermeture installee — reproduire LOCALEMENT la derivation du
     gate PORT-05 plutot que de figer un chiffre, pour que la preuve reste valable quand la
     fermeture bougera.

   Verifier EMPIRIQUEMENT l'hypothese laissee ouverte par D-32-05 (cohabitation de matcher avec le
   guard Bash du module consolidator) : consigner dans le SUMMARY, en clair, dans QUEL fichier et
   dans QUEL groupe chaque entree a atterri, et si un groupe a ete reutilise ou cree. C'etait une
   hypothese, elle devient un fait mesure — ou un ecart a traiter.

   Verifier enfin l'IDEMPOTENCE : reinstaller le module une seconde fois dans le meme lab ne doit
   pas dupliquer les entrees.

3. Mettre a jour `docs/HOOKS-CONTRAT-SORTIE.md` DANS LE MEME COMMIT que le fragment :
   - la valeur de l'assertion de recomptage machine du §4, portee a la valeur reelle mesuree par la
     commande de recomptage elle-meme (jamais une valeur supposee) ;
   - le tableau du module `conductor`, augmente de deux lignes decrivant les entrees : evenement et
     matcher, script, arguments d'invocation, drapeau de hook, codes de sortie ATTEIGNABLES avec
     cette invocation exacte (zero en nominal, le code de garde quand aucun interprete n'est
     joignable), classement BLOQUANTE avec son mecanisme, et forme exec ;
   - les totaux de fin de document : le total general, et le decompte bloquantes/advisory.
   L'inventaire et son assertion se mettent a jour ENSEMBLE, jamais l'un sans l'autre — le document
   le dit lui-meme, et `test-check-hook-paths.sh` T12 le verifie.
  </action>
  <acceptance_criteria>
    - Le fragment est un JSON valide et porte bien deux entrees avec `args` : recompte par interprete sur `plugin/conductor/hooks/hooks.json`, la valeur rendue est `2`.
    - Aucun operateur shell dans le fragment neuf : `grep -c '|| true' plugin/conductor/hooks/hooks.json` rend `0` sur les deux entrees neuves (le verifier par lecture ciblee du bloc `PreToolUse` — les entrees `SessionStart` existantes en portent legitimement).
    - Le recomptage machine du parc et l'assertion du document s'accordent : la commande de recomptage du §4 s'execute sans lever son assertion.
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh` sort 0, et son cas T12 rend un PASS (pas un SKIP d'ecart transitoire).
    - `bash plugin/_internal/tests/test-merge-hooks.sh` sort 0 et `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0.
    - `bash scripts/check-machine-paths.sh` sort 0 — aucun chemin machine n'a fui dans un fichier commite.
    - La preuve d'armement a tourne sur un lab jetable et ses cinq assertions PORT-05 sont vertes ; le SUMMARY porte le chemin du lab, le compte attendu derive, le compte constate, et le placement observe de chaque entree.
    - Le depot courant n'a PAS ete arme : `git status --porcelain -- .claude/` est strictement vide en fin de tache.
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : (a) remplacer la forme exec par une forme shell dans l'une des deux entrees fait rougir la preuve d'armement (l'entree n'est plus routee vers la cible locale et le compte derive ne correspond plus) ; (b) laisser l'inventaire du document a sa valeur precedente fait rougir T12 de `test-check-hook-paths.sh` avec le message d'ecart NON attendu. Consigner assertion, attendu, obtenu ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh && bash plugin/_internal/tests/test-merge-hooks.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash scripts/check-machine-paths.sh && bash scripts/check-version-sync.sh</automated>
  </verify>
  <done>Les deux entrees existent, naissent de merge-hooks en forme exec, atterrissent dans la cible locale avec un command absolu executable sans metacaractere, le gate PORT-05 les compte des deux cotes, et l'inventaire durable des hooks dit la verite sur le parc.</done>
  <reversibility rating="costly">Armer un guard BLOQUANT sur l'outil Bash de tout lab qui installe `conductor` change le comportement de toutes les sessions de tous les labs armes. Le desarmement existe et est outille (`merge-hooks.sh remove`, avec purge croisee des deux cibles), donc la porte n'est pas a sens unique — mais tout lab deja mis a jour garde l'entree jusqu'a sa prochaine mise a jour. C'est le geste le plus cher de la phase : le checkpoint qui suit le soumet a validation humaine avant d'aller plus loin.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint : validation humaine avant de laisser un guard bloquant partir vers tous les labs</name>
  <what-built>Le guard `guard-driver-lock.sh` et ses deux entrees `PreToolUse` BLOQUANTES, prêtes à être armées dans tout lab qui installe le module `conductor`. À partir de ce merge, un commit, un checkout ou une écriture sous `.planning/` émis par une session qui n'est pas enregistrée dans le lock sera REFUSÉ, dans tous les labs armés.</what-built>
  <how-to-verify>
    1. Lire, dans le SUMMARY du plan, le motif de refus complet tel qu'il sera rendu au modèle — c'est le texte qu'un agent bloqué recevra. Vérifier qu'il nomme bien la commande de re-rattachement et le marqueur d'échappatoire.
    2. Ouvrir le lab jetable de la tâche 4 (son chemin est dans le SUMMARY) et lire `.claude/settings.local.json` : les deux entrées doivent y être, en forme exec.
    3. Vérifier que ce dépôt-ci n'a PAS été armé : `git status --porcelain -- .claude/` doit être vide, et `.claude/settings.json` ne doit contenir aucune entrée de module VibeFlow.
    4. Décider en connaissance de cause : c'est une décision à sens unique (les labs déjà mis à jour gardent l'entrée jusqu'à leur prochaine mise à jour).
    5. **(SE-11, placement du gate écrit explicitement)** Ce checkpoint arrive APRÈS le commit de la tâche 4 qui écrit `hooks.json` dans CETTE branche — il ne précède donc pas ce commit local. Le geste qui arme réellement « tous les labs qui installent `conductor` » n'est PAS ce commit : c'est la RELEASE du module (bump `VERSION`, tag, publication — gestes humains gates par ailleurs, cf. `CLAUDE.md` §Discipline de release, et hors périmètre de cette phase). Ce checkpoint est donc la première porte (avant PR et revue), PAS la dernière ligne de défense : le vrai gate contre l'impact réel sur les labs installés reste la décision de release, prise séparément et plus tard. Ne pas confondre approbation de ce checkpoint avec autorisation de release.
  </how-to-verify>
  <resume-signal>Répondre « approuvé » pour poursuivre, ou décrire ce qui doit changer dans le motif de refus, l'échappatoire ou le périmètre des gestes couverts.</resume-signal>
</task>

</tasks>

## Ecart signale — D-32-07 (« zero ligne hors conductor ») ne tient pas a l'epreuve du plan

D-32-07 conclut que les trois mecanismes generiques (decouverte CI, moteur de merge, gate PORT-05)
sont « deja capables de voir une entree neuve sans etre modifies », et en deduit qu'aucune ligne
n'est a ecrire hors `plugin/conductor/**`. **Cette deduction est fausse pour DEUX gates que le
cadrage n'a pas inventories.** Le plan ne la contourne pas en silence : il l'ecrit.

**1. `docs/HOOKS-CONTRAT-SORTIE.md` — obligatoire, pas optionnel.** Le §4 du document porte une
assertion de recomptage machine du parc, et `plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh`
(cas T12, lignes 395-415) compare cette valeur declaree au recompte reel. Sa tolerance d'ecart
transitoire ne couvre **que +1** ; ce plan ajoute **+2 entrees**, donc T12 rendrait « ecart NON
attendu » — une suite ROUGE dans un autre module, sur la seule foi d'un fragment de `conductor`.
Le document lui-meme prescrit que l'inventaire et son assertion se mettent a jour ENSEMBLE.

**2. `README.md` et `README.fr.md` — obligatoires aussi.** `scripts/check-version-sync.sh` §9
compare le nombre de suites reellement decouvertes aux compteurs cites par les deux README. Toute
suite neuve — et ce plan en cree une — rend ce gate rouge tant que les deux compteurs n'ont pas
bouge. Le plan 31-01 a deja paye cette lecon (D-31-08 revise : « la premisse etait FAUSSE »).

**3. `plugin/_internal/tests/test-vf-portable.sh` — troisieme ecriture, identifiee par la
re-validation externe du 2026-08-17 (BL-5)** : le guard `guard-driver-lock.sh` devient le CINQUIEME
consommateur du bloc canonique `vf-portable:locator`, mais le cas T12 de cette suite code en dur
une liste FERMEE de quatre consommateurs. Sans l'ajouter, ce cinquieme consommateur pourrait deriver
en silence — un vert de complaisance. Cette ecriture a ete initialement signalee comme un ecart
distinct ; **elle est desormais couverte par le meme amendement que les deux precedentes** (voir
ci-dessous), et n'a plus besoin d'un traitement separe.

**Ce qui reste vrai de D-32-07 dans sa lettre d'origine** : aucun MODULE tiers de METIER (dev/design/
content/business/growth) n'est modifie PAR CE PLAN — cette famille d'ecarts est traitee separement
par le plan 32-07 (active le 2026-08-17, GEL-1). Le gate PORT-05 derive bien son attendu
automatiquement — sa lecture du critere n°2 (« gate regle 4 » designe en realite le gate de forme
exec telle qu'installee) est confirmee et appliquee telle quelle par la tache 4.

**AMENDEMENT retenu (2026-08-17, voir 32-CONTEXT.md pour le texte complet)** : la formulation
« zero ligne a ecrire hors `conductor` » de D-32-07 est remplacee par « aucun MODULE de METIER
tiers modifie par les plans 32-01 a 32-06 ; des ecritures TRANSVERSES ponctuelles (inventaire des
hooks, compteurs de suites, couverture checksum du bloc localisateur dans `_internal`) sont faites
parce que des gates existants du DEPOT les exigent — et une famille SEPAREE d'ecritures de doctrine
d'agent dans quatre modules de metier est portee par le plan 32-07, sous arbitrage humain explicite
du 2026-08-17 ». L'orchestrateur voit cet amendement dans `32-CONTEXT.md`, pas seulement dans ce
plan qui aurait pu paraitre conforme sans lui.

## Ecart signale — la vague B du mandat ne peut pas etre parallele

Le mandat prescrivait « Vague B = Lot 2 + Lot 3 + Lot 4 en parallele ». Trois faits l'en empechent,
dont deux decoulent de la regle du mandat lui-meme (« deux plans d'une meme vague ne doivent JAMAIS
ecrire le meme fichier ») :

1. **Lot 2 et Lot 4 se disputent trois fichiers** : `plugin/conductor/hooks/hooks.json`,
   `docs/HOOKS-CONTRAT-SORTIE.md`, et les deux compteurs de README. Les serialiser n'est pas un
   choix de confort, c'est la regle du mandat appliquee.
2. **Le motif de refus de ce guard NOMME la commande de re-rattachement** (D-32-03(c)) — un verbe
   livre par le Lot 1(c). Emettre, meme transitoirement, un motif qui pointe un verbe inexistant
   serait exactement le vert de complaisance que la phase interdit ailleurs. D'ou la dependance de
   ce plan au 32-02.
3. **Le Lot 1 a du etre scinde** pour tenir dans une fenetre de contexte (voir l'ecart signale du
   plan 32-01), ce qui decale d'une vague tout ce qui en depend.

**Consequence** : les vagues reelles sont 1 (32-01) · 2 (32-02) · 3 (32-03 et 32-04, paralleles) ·
4 (32-05, abandonnable) · 5 (32-06, cloture). Le parallelisme survit en vague 3 ; il est perdu
ailleurs, pour des raisons de correction et non de confort.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> guard (entree standard) | le payload est une entree NON fiable : JSON arbitraire, champs absents ou d'un autre type, contenu de commande entierement controle par le modele |
| commande Bash -> decision du guard | la ligne de commande est du texte hostile par construction (documents en ligne, chainage, wrappers, substitutions) |
| `meta` du lock -> decision du guard | fichier plat editable a la main ; un `meta` corrompu ne doit jamais produire un refus |
| fragment hooks.json -> settings poses chez l'utilisateur | franchi par `merge-hooks` ; c'est la frontiere ou un chemin machine-specifique pourrait fuir dans un fichier commite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-13 | Spoofing | comparaison d'identite de session | critical | mitigate | comparaison a la liste enregistree dans le `meta`, jamais a une valeur fournie par le payload autre que `session_id` ; le mismatch DENIE, il ne fail-open jamais (D-32-03(b)) ; cas A1/A4 |
| T-32-14 | Elevation of Privilege | contournement du guard par forme de commande | high | accept | limite ASSUMEE et ecrite en en-tete : interpretation indirecte, alias, script du depot qui commite en interne ne sont pas couverts. Garde anti-accident, pas anti-adversaire — cas L1 documente le trou au lieu de le cacher |
| T-32-15 | Denial of Service | lock zombie non perime | high | mitigate | echappatoire par marqueur ET par variable d'environnement, nommee dans le motif de refus lui-meme ; lock perime traite comme absent ; sortie par ajout de worktree nommement epargnee ; cas S2, S7, S8, R2 |
| T-32-16 | Denial of Service | guard casse ou interprete absent | medium | mitigate | fail-open a quatre issues : imparsable et erreur interne restent silencieux, indisponible ecrit un marqueur de sante et rend le code de garde ; le harness est structurellement fail-open sur un PreToolUse qui plante (mesure) ; cas Q3, Q3c, Q4 |
| T-32-17 | Tampering | injection dans le motif de refus | medium | mitigate | le motif est construit par l'interprete via un encodage JSON, jamais par concatenation shell ; les valeurs viennent du `meta`, tronquees en longueur |
| T-32-18 | Information Disclosure | fuite d'un chemin machine | high | mitigate | le refus passe par la decision JSON, jamais par le code reserve au harness (qui, MESURE, prefixe le chemin absolu du script) ; l'entree est routee en cible locale, jamais commitee ; gate `check-machine-paths.sh` en critere |
| T-32-19 | Repudiation | guard pose mais non arme | high | mitigate | preuve d'armement executee sur un lab jetable selon le patron as-installed, plus le gate PORT-05 qui derive son attendu de la fermeture (regression #38 rejouee deux fois : le vrai risque est un guard qui existe et n'est pas branche) |
| T-32-35 | Tampering | chainage multi-lignes non detecte (BL-1) | critical | mitigate | decoupage du payload sur `\n`/`\r` AVANT la segmentation sur les operateurs de chainage — un `git commit` qui n'est pas le premier segment de la commande n'est plus invisible ; cas A5 |
| T-32-36 | Tampering | options globales de `git` (BL-2) | critical | mitigate | saut des options globales connues avant lecture du sous-verbe — `git -C`/`git -c core.hooksPath=…` sont reconnus comme portant le sous-verbe qui les suit ; cas A6 |
| T-32-37 | Repudiation | echappatoire utilisee sans trace (SE-2) | medium | mitigate | ligne d'evenement `override` en append sur le journal du plan 32-02, best-effort, avant la sortie de l'echappatoire — l'usage devient auditable sans ajouter de friction |
| T-32-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installe par cette phase — aucun gate de legitimite applicable |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/guard-driver-lock.sh`
2. `bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh` — tous les blocs A/B/C/R/S/P/Q/L verts, y compris A5/A6/S9/S10/P0
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` et `bash plugin/conductor/scripts/tests/test-guard-agent-write.sh` — non-regression
4. `bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh` — T12 en PASS, pas en SKIP
5. `bash plugin/_internal/tests/test-merge-hooks.sh` et `bash plugin/_internal/tests/test-vibeflow-update.sh`
6. `bash scripts/check-machine-paths.sh` et `bash scripts/check-version-sync.sh`
7. Preuve d'armement sur lab jetable : cinq assertions PORT-05 vertes, placement de chaque entree consigne
8. Les seize mutations exigees ont rougi ; leurs traces sont dans le SUMMARY (onze d'origine + deux BL-1/BL-2 + deux BL-7 + une C3/SE-7), plus les deux controles positifs de fixture (R3, SE-7)
</verification>

<success_criteria>
- Les scenarios A, B et C du rejeu virent au vert sans qu'aucune de leurs assertions ait ete negociee, et les cas de discriminance A4/B4/C3 restent presents.
- Le chainage par saut de ligne et les options globales de `git` ne rendent plus aucun geste concerne invisible (BL-1, BL-2).
- Les quatre formes de sortie de rebase/merge/cherry-pick restent ouvertes a une session tierce sous lock d'autrui ; aucune autre forme de ces sous-commandes n'est exemptee (BL-7).
- Aucun refus n'est possible sur une INDETERMINATION ; aucun allow n'est possible sur un mismatch d'identite connu.
- Une sonde d'existence pure-bash court-circuite le guard avant tout spawn quand aucun lock n'existe (SE-1).
- L'usage de l'echappatoire est journalise, best-effort, sans ajouter de friction (SE-2).
- Les quatre issues de QUAL-01 sont couvertes par des cas distincts et prouvees par mutation.
- La passoire du matching par sous-chaine est nommee en en-tete du guard et documentee par un cas de test ; l'absence de couverture des redirections/editeurs en ligne de commande vers `.planning/` est un choix ecrit et motive (SE-4), pas un oubli.
- Les deux entrees naissent de merge-hooks, atterrissent en cible locale en forme exec, et l'inventaire durable des hooks dit la verite.
</success_criteria>

<output>
Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-03-SUMMARY.md` en fin de plan, avec :
le motif de refus complet rendu au modele (texte integral), les traces des seize mutations exigees,
le chemin du lab jetable, le compte PORT-05 attendu et constate, le placement observe de chaque
entree (fichier et groupe), et la liste des wrappers effectivement couverts par la detection de
position de commande.
</output>

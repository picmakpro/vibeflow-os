---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/scripts/check-gsd-config.sh
  - plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh
  - plugin/dev-orchestrator/hooks/hooks.json
  - .planning/config.json
autonomous: true
requirements: [GSDC-07]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

assumption_delta_decision:
  noun_now_primary: "valeur d'un toggle de capability de cycle"
  decision: add-alongside
  rationale: >-
    La valeur d'un toggle passe de DÉRIVÉE (défaut amont implicite) à CHOISIE (écrite dans
    .planning/config.json) — mais seulement pour les 5 toggles que la Phase 23 arbitre réellement
    (D-19). Les 39 autres capabilities restent au défaut amont : les deux régimes coexistent
    volontairement. Dette acceptée et NOMMÉE, mitigée par le gate check-gsd-config.sh qui rend
    visible tout toggle laissé au défaut implicite au lieu de le laisser silencieux. Inventorier
    les 44 capabilities est explicitement écarté par D-19 (candidat Phase 24).
  invariant_test: >-
    test-check-gsd-config.sh — une fixture de config sans les toggles arbitrés doit produire un
    signal (exit 0) ; la même fixture avec les toggles écrits doit rester silencieuse (exit 3).

must_haves:
  truths:
    - "`check-gsd-config.sh` tourne sur n'importe quel lab et signale les clés de `.planning/config.json` que le moteur installé ne connaît pas (D-17)."
    - "Il signale aussi les toggles de cycle arbitrés par cette phase qui sont laissés au défaut implicite au lieu d'être écrits (D-17)."
    - "Il lit les clés connues DEPUIS `gsd-core`, donc il ne périme pas à la montée de version du moteur (D-17)."
    - "Il est advisory : exit 0 quand il a quelque chose à dire, 3 quand il n'a rien à dire, 64 sur argument invalide — jamais bloquant, jamais en CI (D-20)."
    - "Les blocs `gates` et `safety` ont disparu du `.planning/config.json` de ce lab, et l'avertissement `unknown config key(s)` du moteur ne se produit plus sur ce lab (D-18)."
    - "Les 5 toggles arbitrés (`code_review`, `pattern_mapper`, `node_repair`, `node_repair_budget`, `ui_review`) sont écrits à une valeur décidée, `ui_review` traité comme ABSENT en amont et non comme faux (D-19, Finding 2)."
  artifacts:
    - plugin/dev-orchestrator/scripts/check-gsd-config.sh
    - plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh
    - plugin/dev-orchestrator/hooks/hooks.json
    - .planning/config.json
  key_links:
    - "Le script lit l'union `VALID_CONFIG_KEYS` (config.cjs) ∪ `configKeys` (capability-registry.cjs) — si une seule des deux sources est lue, des clés légitimes sont signalées à tort (fait vérifié : `workflow.code_review` n'est PAS dans `VALID_CONFIG_KEYS`, il vit dans `configKeys`)."
    - "Le câblage `SessionStart` de `hooks.json` suffixé `|| true` — sans lui, un gate advisory devient bloquant de tour."
    - "Le nettoyage du `config.json` de ce lab est le premier client de son propre outil : le gate doit passer de exit 0 à exit 3 sur ce lab entre la tâche 1 et la tâche 3."
---

<objective>
Fermer la Lacune 5 (le `config.json` du lab n'est pas aligné sur le moteur) par un **outil
générique**, pas par une réparation locale.

Purpose : `gsd-tools` avertit à **chaque** invocation sur ce lab (`unknown config key(s) in
.planning/config.json: gates, safety`) — 10 clés entières inertes, sans aucune destination amont
(fait vérifié dans `bin/lib/config.cjs`). Et cinq toggles de cycle que cette phase arbitre
réellement tombent au défaut amont sans que personne ne l'ait décidé : le lab pilote ses étages
**par omission**. Réparer ce lab seul laisserait chaque autre lab VibeFlow avec le même défaut
silencieux — d'où un gate qui tourne partout et qui lit ses clés connues depuis le moteur installé,
donc qui ne périme pas.

Output : `check-gsd-config.sh` (advisory, exit 0/3/64, patron `check-doc-drift.sh`), sa suite
dédiée, son câblage `SessionStart`, et le `.planning/config.json` de ce lab nettoyé et décidé —
ce lab devenant le premier client de son propre outil.

Ce plan est **indépendant du plan 23-01** (aucun fichier partagé) et s'exécute en parallèle de lui.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md`

**Champs neufs du bloc typé** : `gate`, `reprise` (`plan_id`/`checkpoint`/`gate`/`attendu`),
`verdicts` (`code_review`/`nyquist`/`secure`), `decompte`

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » ·
`mission-flow.md` §Pattern E budget partagé + table des briques dormantes · `docs/ADR.md`
ADR-061 troisième objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24` à `T32`

**Variables d'environnement neuves** : `VF_CONFIG_PATH`, `VF_GSD_CORE_LIB` (ce plan) ·
`VF_CAPS_INDEX_OUT`, `VF_GSD_TOOLS` (plan 23-04)

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` signal · `3` silence · `64` argument invalide

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10`

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`) —
  aucun prédicat de sonde généré ce tour, choix enregistré.
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`, phase doctrine + bash).
- **Gate de schéma ORM : sans objet** (aucun fichier de collection/schéma/migration).
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan lit deux modules Node **déjà installés localement** (`gsd-core/bin/lib/config.cjs`,
  `gsd-core/bin/lib/capability-registry.cjs`) et un fichier JSON local — aucun SDK, aucun service
  tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

<!-- planner-discipline-allow: eval -->
> **Note de discipline (#429)** : le mot `eval` apparaît dans les `<action>` de ce plan parce qu'il
> est **impossible** de prescrire son interdiction sans le nommer. Les critères d'acceptation qui le
> cherchent en négatif filtrent d'abord les lignes de commentaire
> (`grep -v '^#' … | grep -c 'eval'`) : documenter l'interdiction dans l'en-tête du script est donc
> **licite et attendu**, seule une utilisation réelle dans le code fait échouer le critère.

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : `check-gsd-config.sh` de bout en bout sur un seul fait — les clés inconnues</name>
  <files>plugin/dev-orchestrator/scripts/check-gsd-config.sh, plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/check-doc-drift.sh (153 lignes, INTÉGRALEMENT) — patron obligatoire : en-tête documentant le rôle ADR-055 §3 (le script constate le FAIT, l'agent porte le JUGEMENT), contrat d'exit commenté, parsing d'arguments strict AVANT toute logique, fonction `say()` conditionnée par `--quiet`, signal final sur **stdout** et diagnostics sur stderr
    - plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh (INTÉGRALEMENT) — patron obligatoire de la suite dédiée : fixtures en `mktemp -d`, `trap ... EXIT`, assertions `ok`/`ko`, couverture des trois codes de sortie
    - .planning/codebase/CONVENTIONS.md §Portabilité bash — ADR-054 : pas de `jq` en dépendance dure (wrapper `jqx()`), pas de `grep -P`, pas de `sed -i` nu, préfixe `VF_` pour les variables surchargeables
    - .planning/phases/VFDO-23-.../23-RESEARCH.md §Pattern 2 et §Security Domain — la validation d'argument et l'interdiction d'`eval` sur du contenu de fichier non maîtrisé
  </read_first>
  <behavior>
    - Cas 1 : config contenant un bloc de clés inconnues du moteur → signal sur stdout nommant les clés, exit 0.
    - Cas 2 : config ne contenant que des clés connues → aucun signal sur stdout, exit 3.
    - Cas 3 : `--path` sans valeur, ou argument inconnu, ou `--hook` et `--quiet` ensemble → message sur stderr, exit 64.
    - Cas 4 : chemin de config inexistant → silence, exit 3 (jamais une erreur bruyante au démarrage de session).
    - Cas 5 : moteur `gsd-core` introuvable → silence, exit 3 (le gate ne peut rien constater, il ne prétend rien).
    - Cas 6 : valeur de config hostile (interpolation shell, sous-shell, octet de contrôle) → aucun effet d'exécution, exit inchangé.
  </behavior>
  <action>
Créer `plugin/dev-orchestrator/scripts/check-gsd-config.sh` en calquant `check-doc-drift.sh` ligne
à ligne dans sa **charpente** (jamais son contenu métier), puis créer sa suite dédiée. Slice
vertical complet sur **un seul fait** — les clés inconnues — de la source amont jusqu'au code de
sortie testé. Le second fait (toggles au défaut implicite) est la tâche 2 : ne pas l'anticiper ici.

**Interface** : `check-gsd-config.sh [--path <dir>] [--hook] [--quiet]`, défaut `--path .`.
Le chemin du fichier audité se dérive de `--path` vers `<path>/.planning/config.json`, surchargeable
par la variable `VF_CONFIG_PATH` (patron `VF_INDEX_OUT`/`VF_GSD_SKILLS_DIR` de `build-gsd-index.sh`,
indispensable aux fixtures de test).

**Contrat d'exit**, documenté en en-tête au même format que l'analogue :
`0` = au moins un signal émis · `3` = rien à signaler · `64` = argument invalide.
Le signal part sur **stdout** avec le préfixe `[gsd-config]` (c'est ce que le hook `SessionStart`
capture) ; tout diagnostic va sur stderr via `say()`, muselé par `--quiet`.

**Source des clés connues — le point qui fait que le gate ne périme pas.** Ne JAMAIS écrire de
liste de clés en dur dans le script. Lire à l'exécution, depuis le moteur installé, l'**union** de
deux sources (fait vérifié cette session — chacune seule produit des faux positifs) :

- `VALID_CONFIG_KEYS`, exporté par `gsd-core/bin/lib/config.cjs` : un `Set` de clés pointées
  (104 entrées aujourd'hui) ;
- `configKeys`, exporté par `gsd-core/bin/lib/capability-registry.cjs` : un objet dont les clés
  sont des clés de config pointées (58 entrées aujourd'hui) mappées vers l'identifiant de
  capability qui les consomme.

Fait dimensionnant vérifié, à ne pas redécouvrir à l'exécution : `workflow.code_review`,
`workflow.pattern_mapper` et `workflow.ui_review` sont **absents** de `VALID_CONFIG_KEYS` et
présents dans `configKeys` — un gate qui ne lirait que la première source signalerait à tort trois
clés parfaitement légitimes. Inversement `workflow.node_repair` et `workflow.node_repair_budget`
ne vivent que dans la première.

Résoudre le dossier `gsd-core/bin/lib` par cascade (surchargeable par `VF_GSD_CORE_LIB` pour les
fixtures) : chemin du lab courant, puis scope utilisateur — même logique de priorité que la
cascade `$S` de `mission-flow.md` (le lab courant PRIME). Moteur introuvable ⇒ silence, exit 3 :
un gate qui ne peut rien constater ne prétend rien (ADR-055 §3).

**Extraction** : passer par `node -e` sur les deux modules et émettre les clés une par ligne sur
stdout, puis comparer en bash. **Interdit** : `eval` ou `bash -c` sur une valeur lue depuis
`.planning/config.json` ou depuis la sortie Node ; l'aplatissement des clés du JSON audité se fait
lui aussi côté `node -e`, jamais par interpolation shell d'un contenu de fichier non maîtrisé
(menace `Tampering`, §Security Domain du RESEARCH).

**Granularité de comparaison** : signaler une clé de premier niveau inconnue **en tant que bloc**
(c'est le comportement observé du moteur : il nomme `gates, safety`, pas leurs dix sous-clés), et
signaler une sous-clé inconnue sous un conteneur connu par son chemin pointé complet. Écrire ce
choix en commentaire — c'est un choix, il doit être lisible.

**Suite `plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh`** : couvrir les six
comportements listés ci-dessus, chacun sur une fixture construite en `mktemp -d` avec
`trap ... EXIT`. Cas 6 (robustesse) : reprendre l'esprit des fixtures hostiles déjà éprouvées dans
ce dépôt (interpolation shell, sous-shell, octet de contrôle 0x01 dans une valeur) et exiger stdout
**vide de tout effet d'exécution** — c'est la sonde qui prouve l'absence d'`eval`, pas la relecture.

Note attendue et non bloquante : ajouter cette suite porte le dépôt de 46 à 47 suites et rend
`scripts/check-version-sync.sh` **rouge** sur ce seul contrôle jusqu'au plan 23-08, qui remet les
deux compteurs de README d'équerre. C'est prévu, à consigner au SUMMARY, à ne pas « corriger » ici.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` termine avec `0 KO`.
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort en **0** et sa sortie stdout contient `[gsd-config]` ainsi que les deux noms de blocs inconnus encore présents dans `.planning/config.json` à ce stade.
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --nawak` sort en **64** et n'écrit rien sur stdout.
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --hook --quiet` sort en **64**.
    - `VF_CONFIG_PATH=/chemin/inexistant/config.json bash plugin/dev-orchestrator/scripts/check-gsd-config.sh` sort en **3** avec stdout vide.
    - `grep -v '^#' plugin/dev-orchestrator/scripts/check-gsd-config.sh | grep -c 'eval'` retourne `0`.
    - `grep -v '^#' plugin/dev-orchestrator/scripts/check-gsd-config.sh | grep -cE "grep -P|sed -i "` retourne `0` (ADR-054).
    - `grep -c 'VALID_CONFIG_KEYS' plugin/dev-orchestrator/scripts/check-gsd-config.sh` ≥ 1 ET `grep -c 'configKeys' plugin/dev-orchestrator/scripts/check-gsd-config.sh` ≥ 1 (les deux sources sont lues).
    - `bash -n plugin/dev-orchestrator/scripts/check-gsd-config.sh` sort en 0 et le fichier est exécutable (`test -x`).
  </acceptance_criteria>
  <done>Le gate constate un fait vérifiable sur n'importe quel lab, rend un code de sortie contractuel, ne périme pas quand `gsd-core` monte de version, et sa suite dédiée couvre les trois codes plus la robustesse d'entrée.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : second fait — les toggles de cycle laissés au défaut implicite (D-19, Finding 2)</name>
  <files>plugin/dev-orchestrator/scripts/check-gsd-config.sh, plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/check-gsd-config.sh (l'état produit par la tâche 1)
    - plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh (l'état produit par la tâche 1)
    - .planning/phases/VFDO-23-.../23-RESEARCH.md §Finding 2 — `workflow.ui_review` est référencé comme condition d'activation dans le registre de capabilities mais absent du bloc de défauts de `config.cjs` : une valeur qui n'existe nulle part n'est PAS `false`, elle est ABSENTE
    - .planning/phases/VFDO-23-.../23-CONTEXT.md D-19 (et sa note sur `ui_review`)
    - $HOME/.claude/gsd-core/bin/lib/config.cjs autour des lignes 243-273 — le bloc de défauts amont, à LIRE programmatiquement, jamais à recopier
  </read_first>
  <behavior>
    - Cas 1 : config sans aucun des toggles arbitrés → signal listant les toggles laissés au défaut implicite, exit 0.
    - Cas 2 : config portant les toggles arbitrés à une valeur explicite → aucun signal de ce volet.
    - Cas 3 : un toggle arbitré n'a pas de défaut amont (`ui_review`) → le signal dit qu'il est **absent en amont**, jamais qu'il vaut faux.
    - Cas 4 : les deux volets déclenchent en même temps → un seul appel, un signal par volet, un seul exit 0.
  </behavior>
  <action>
Étendre `check-gsd-config.sh` d'un **second fait constaté**, sans toucher le contrat d'exit ni
l'interface : les toggles de cycle que la Phase 23 arbite sont-ils **écrits** dans le
`.planning/config.json` audité, ou tombent-ils au défaut amont ?

**Liste arbitrée** (D-19, et elle seule — inventorier les 44 capabilities est explicitement écarté,
candidat Phase 24) : `workflow.code_review`, `workflow.pattern_mapper`, `workflow.node_repair`,
`workflow.node_repair_budget`, `workflow.ui_review`. Cette liste-là est un **choix de doctrine**,
donc elle est écrite dans le script, en tête, avec le commentaire qui dit pourquoi elle est courte
et pourquoi elle n'est pas dérivée du registre.

**Traitement de `ui_review` — le point à ne pas rater.** Pour chaque toggle arbitré, résoudre le
défaut amont en lisant le bloc de défauts de `config.cjs` **programmatiquement** (jamais une
recopie de valeurs). Trois états possibles, trois messages distincts :
- écrit dans la config auditée → rien à signaler ;
- absent de la config auditée mais présent dans les défauts amont → signaler « au défaut amont »
  **en donnant la valeur effective lue** ;
- absent de la config auditée **et** absent des défauts amont (cas de `ui_review`) → signaler
  « absent en amont, résolu par la capability elle-même » et **ne jamais** afficher ni supposer une
  valeur. Une valeur qui n'existe nulle part n'est pas `false` ; l'écrire comme faux serait
  inventer un fait, exactement ce qu'ADR-055 §3 interdit à un script.

**Suite** : ajouter les quatre comportements ci-dessus en cas de test. Le cas 3 est le cas
discriminant du volet : une fixture sans `ui_review` doit produire un message qui **ne contient pas**
de valeur booléenne pour ce toggle. Le cas 4 vérifie qu'un seul `exit 0` sort même quand les deux
volets parlent.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` termine avec `0 KO` et compte au moins 4 cas de plus qu'à l'issue de la tâche 1.
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort en **0** et sa sortie nomme les 5 toggles arbitrés (`code_review`, `pattern_mapper`, `node_repair`, `node_repair_budget`, `ui_review`) — l'état du lab avant la tâche 3 les a tous au défaut implicite.
    - La ligne de sortie relative à `ui_review` ne contient ni `true` ni `false`.
    - `grep -c 'ui_review' plugin/dev-orchestrator/scripts/check-gsd-config.sh` ≥ 1, et le script porte un commentaire expliquant que l'absence de défaut amont ne vaut pas `false`.
    - `grep -v '^#' plugin/dev-orchestrator/scripts/check-gsd-config.sh | grep -c 'eval'` retourne toujours `0`.
    - `bash -n plugin/dev-orchestrator/scripts/check-gsd-config.sh` sort en 0.
  </acceptance_criteria>
  <done>Le gate distingue trois états par toggle — écrit, au défaut amont avec sa valeur, absent en amont sans valeur — et sa suite prouve qu'il ne fabrique jamais la valeur manquante.</done>
</task>

<task type="auto">
  <name>Tâche 3 : câblage `SessionStart` et alignement du `.planning/config.json` de ce lab (D-18, D-19, D-20)</name>
  <files>plugin/dev-orchestrator/hooks/hooks.json, .planning/config.json</files>
  <read_first>
    - plugin/dev-orchestrator/hooks/hooks.json (intégralement, 15 lignes) — les trois commandes `SessionStart` existantes, chacune suffixée `|| true`, matcher `startup`, placeholder `{{VF_SCRIPTS}}`
    - .planning/config.json (intégralement) — l'état exact à modifier
    - plugin/dev-orchestrator/scripts/check-gsd-config.sh (l'état produit par les tâches 1 et 2 — c'est lui qui valide le résultat)
    - .planning/phases/VFDO-23-.../23-CONTEXT.md D-18 (les 10 clés sans destination amont) et D-19
    - $HOME/.claude/gsd-core/bin/lib/config.cjs autour des lignes 243-273 — pour constater que `human_verify_mode` porte le défaut `end-of-phase` côté moteur, destination réelle de l'intention que `gates.*` portait
  </read_first>
  <action>
**1. `hooks/hooks.json`** — ajouter une **quatrième** entrée au tableau `SessionStart[0].hooks`,
strictement sur le modèle des trois existantes, suffixée `|| true` :
`bash {{VF_SCRIPTS}}/check-gsd-config.sh --hook || true`. Le `|| true` n'est pas cosmétique : sans
lui un gate advisory devient bloquant de tour. Mettre à jour la valeur du champ `description` du
fichier pour nommer le quatrième signal (alignement du config au moteur) — cette description est
la seule prose du fichier, elle doit rester exacte.

**2. `.planning/config.json`** — deux gestes, dans cet ordre :

- **Supprimer intégralement les blocs `gates` et `safety`** (D-18). Fait établi et vérifié : les 8
  clés `gates.*` (dont `confirm_plan`) et les 2 clés `safety.*` n'ont **aucun équivalent amont** —
  elles ne sont pas mal nommées, elles n'ont pas de destination. Rien n'est perdu : l'intention de
  `gates.*` est portée par `workflow.human_verify_mode` côté moteur (défaut `end-of-phase`, et ce
  lab ne l'écrit pas — il reste au défaut, ce qui est ici le comportement voulu), et
  `safety.always_confirm_destructive` est couvert bien plus largement par ADR-031 côté VibeFlow
  (« jamais de fix sans validation humaine »), qui est une doctrine appliquée par des agents, pas
  un drapeau inerte.
- **Écrire les 5 toggles arbitrés dans le bloc `workflow`** (D-19), à ces valeurs décidées :
  `code_review: true` (l'étage de revue du hook reste actif — sa disjonction d'avec le nœud
  `revue-N` est écrite au plan 23-06, pas ici) · `pattern_mapper: true` · `node_repair: true` ·
  `node_repair_budget: 2` · `ui_review: false`.

  Justification à consigner au SUMMARY pour `ui_review` : ce dépôt est un plugin de distribution
  bash + markdown, sans aucune surface d'interface — fermer explicitement le toggle transforme un
  étage **accidentellement** inactif (parce qu'aucune valeur n'existe nulle part) en étage
  **délibérément** fermé, ce qui est précisément le geste anti-Lacune 5. Fait vérifié avant écriture
  et à re-vérifier après : `workflow.ui_review` figure dans les clés de configuration déclarées par
  le registre de capabilities, donc l'écrire ne réintroduit **pas** d'avertissement de clé inconnue.
  La vérification est un critère d'acceptation ci-dessous, pas une hypothèse.

Ne toucher à **aucune** autre clé de ce fichier : `research`, `nyquist_validation`,
`security_enforcement`, `plan_check`, `verifier`, `auto_advance` et les blocs `planning`,
`parallelization`, `hooks`, `project_code`, `agent_skills`, `claude_md_path` restent inchangés.
Conserver l'indentation à 2 espaces et l'ordre des blocs existants.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path . ; echo "exit_gate=$?"; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query roadmap.get-phase 23 2>&1 >/dev/null | grep -c 'unknown config key'</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort désormais en **3** avec stdout vide : ce lab est aligné, et il est le premier client de son propre outil (bascule de 0 à 3 entre la tâche 2 et celle-ci).
    - Une invocation de `gsd-tools` sur ce lab n'émet plus aucune ligne contenant `unknown config key` — le comptage `grep -c` sur stderr retourne `0`.
    - `grep -c '"gates"' .planning/config.json` retourne `0` et `grep -c '"safety"' .planning/config.json` retourne `0`.
    - `.planning/config.json` contient les cinq chaînes `"code_review"`, `"pattern_mapper"`, `"node_repair"`, `"node_repair_budget"`, `"ui_review"` dans son bloc `workflow`.
    - `node -e 'JSON.parse(require("fs").readFileSync(".planning/config.json","utf8"))'` sort en 0 (JSON valide).
    - `node -e 'const c=JSON.parse(require("fs").readFileSync(".planning/config.json","utf8"));process.exit(c.workflow.security_enforcement===true && c.workflow.research===true && c.project_code==="VFDO"?0:1)'` sort en 0 (aucune clé hors périmètre n'a bougé).
    - `plugin/dev-orchestrator/hooks/hooks.json` contient `check-gsd-config.sh --hook || true`, le JSON est valide, et le tableau `SessionStart[0].hooks` compte **4** entrées.
    - `node -e 'const h=JSON.parse(require("fs").readFileSync("plugin/dev-orchestrator/hooks/hooks.json","utf8"));process.exit(h.hooks.SessionStart[0].hooks.length===4?0:1)'` sort en 0.
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` reste à `0 KO` (le nettoyage du lab ne casse aucune fixture — les fixtures sont indépendantes du lab réel).
  </acceptance_criteria>
  <done>L'avertissement de clés inconnues qui tombait sur chaque invocation de `gsd-tools` a disparu de ce lab, les cinq toggles arbitrés sont écrits à une valeur décidée, le gate est câblé en signal de démarrage de session, et il sort désormais en silence sur son propre lab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/config.json` d'un lab tiers → `check-gsd-config.sh` | Le gate est fait pour tourner sur **n'importe quel** lab : le contenu du JSON audité est une entrée **non maîtrisée**, y compris ses clés et ses valeurs. |
| `argv` du hook `SessionStart` → script | Le script est invoqué automatiquement au démarrage de session : une entrée malformée ne doit jamais produire un comportement indéterminé ni bloquer le tour. |
| `gsd-core/bin/lib/*.cjs` installé → script | Les clés connues viennent d'un module Node tiers, chargé par `node -e`. C'est une frontière de version : une montée de `gsd-core` change la réponse, ce qui est **voulu** (le gate ne périme pas). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02-01 | Tampering | valeur de `.planning/config.json` interpolée en shell | high | mitigate | Aucun `eval`, aucun `bash -c` sur du contenu lu depuis le JSON audité ni depuis la sortie Node ; l'aplatissement des clés se fait côté `node -e`. Critère d'acceptation machine : `grep -c 'eval'` hors commentaires retourne `0`. Cas de test 6 sur fixtures hostiles (interpolation, sous-shell, octet de contrôle). |
| T-23-02-02 | Tampering | argument de ligne de commande non validé | medium | mitigate | Parsing strict AVANT toute logique, patron `check-doc-drift.sh` : `--path` sans valeur, argument inconnu, `--hook` et `--quiet` ensemble ⇒ message sur stderr et exit 64. Trois cas de test dédiés. |
| T-23-02-03 | Denial of Service | gate bloquant le tour au `SessionStart` | high | mitigate | Suffixe `|| true` sur la commande de hook (invariant des trois commandes existantes) + contrat sans `exit 1` (seuls 0/3/64) + silence total quand le moteur ou le fichier sont introuvables. |
| T-23-02-04 | Information Disclosure | le signal recopie une valeur de config sensible dans la sortie de session | low | mitigate | Le signal ne nomme que des **clés** et, pour les toggles arbitrés, la valeur du **défaut amont** (une constante publique du moteur) — jamais une valeur lue dans le fichier audité. |
| T-23-02-05 | Spoofing | un toggle sans défaut amont présenté comme valant faux | medium | mitigate | Traitement à trois états explicite pour `ui_review` (Finding 2 du RESEARCH) + critère machine : la ligne de sortie relative à ce toggle ne contient ni `true` ni `false`. |
| T-23-02-06 | Elevation of Privilege | suppression de `safety.always_confirm_destructive` lue comme un relâchement de garde | medium | accept | Fait vérifié : ces clés sont **inertes** (aucune destination amont), les supprimer ne relâche rien. La garde réelle est ADR-031, appliquée par les agents. Risque résiduel purement documentaire, tracé ici et au SUMMARY. |
| T-23-02-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan (RESEARCH §Package Legitimacy Audit — N/A pour toute la phase). |
</threat_model>

<verification>
- Suite dédiée verte : `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` → `0 KO`.
- Le gate bascule bien de **0** (avant nettoyage, tâches 1-2) à **3** (après nettoyage, tâche 3) sur ce lab — c'est la preuve de bout en bout que l'outil et son premier client se répondent.
- L'avertissement `unknown config key(s)` de `gsd-tools` a disparu de ce lab, constaté par exécution et non par lecture.
- `bash -n` propre sur le script, script exécutable, aucune dépendance dure à `jq`, aucun `grep -P`, aucun `sed -i` nu (ADR-054).
- `hooks.json` reste un JSON valide à 4 commandes `SessionStart`, toutes suffixées `|| true`.
- **Attendu et non bloquant** : `scripts/check-version-sync.sh` est rouge sur le seul compteur « N suites » (46 → 47) jusqu'au plan 23-08. À consigner au SUMMARY, à ne pas corriger ici.
</verification>

<success_criteria>
- SC1 — Un gate générique existe, tourne sur n'importe quel lab, et lit ses clés connues depuis le moteur installé (union de deux sources) plutôt qu'en dur.
- SC2 — Il est advisory au sens du dépôt : exits 0/3/64 uniquement, signal sur stdout, silence sur l'absence de moteur ou de fichier, `|| true` au hook.
- SC3 — Les deux blocs sans destination amont ont disparu du `.planning/config.json` de ce lab et l'avertissement du moteur avec eux, constaté par exécution.
- SC4 — Les cinq toggles arbitrés sont écrits à une valeur décidée, et le toggle sans défaut amont est traité comme **absent**, jamais comme faux.
- SC5 — Aucune clé de config hors des deux gestes décidés n'a bougé.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-02-SUMMARY.md` à la fin.
Y consigner explicitement : la sortie du gate avant et après le nettoyage (codes 0 puis 3), la
preuve d'exécution que l'avertissement du moteur a disparu, la justification de la valeur retenue
pour `ui_review`, et le rappel que le compteur « N suites » des README passe à 47 au plan 23-08.
</output>

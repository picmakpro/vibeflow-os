---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 01
lot: W0 — outillage de preuve (recensement, rejeu CI, preuve des suites de modules)
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BUDG-04, QUAL-01]
files_modified:
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "PRÉCONDITION (révision 4, constat F2 ; arbitrage Samuel, AskUserQuestion session principale, 2026-09-16 : l'exécution attend le merge du hotfix v2.63.1 dans main) — Task 0 bloquante jouée avant toute édition : la base `git merge-base HEAD <ref main>`, ref résolue par la commande elle-même selon la règle W-C (la plus avancée, rc 2 sur divergence, `tools/` n'existant pas encore), CONTIENT le hotfix jugé sur le contenu (`VERSION` ≥ v2.63.1, entrée CHANGELOG `dev-orchestrator` citant v2.63.1, base = pointe de main). Rouge → arrêt et remontée au manager, aucun rebase décidé par l'exécutant"
    - "BUDG-04 (critère 1) : la commande de recensement « aucun 250 vivant de la charte des agents » est VERSIONNÉE dans le dossier de phase (`tools/check-no-live-250.sh`) avec la regex `(^|[^0-9.])250([^0-9]|$)` — jamais `git grep -w`, qui rate `250L` et `≤250L` — et des exclusions NOMINATIVES glob de chemin + ERE de contenu, sans aucun numéro de ligne"
    - "Rejouée sur le dépôt AVANT toute édition de la phase, la commande rend rc=1 et liste au moins les sites vivants témoins (`CLAUDE.md:`, `VF_BUDGET_LINE_CAP=`, `.planning/seeds/SEED-001-equipe-produit-v1.md:`) ; elle ne liste AUCUNE ligne sous `.planning/phases/`, `.planning/milestones/`, `CHANGELOG.md`"
    - "Le contrôle négatif de la commande est versionné (`tools/test-check-no-live-250.sh`) et rejouable : il rougit sur un `≤250L` réintroduit, sur `VF_BUDGET_LINE_CAP=250`, sur un 250 posé sur une AUTRE ligne d'un fichier à exclusion ligne-à-ligne ; il reste vert sur un 250 d'archive ; il rend 2 hors dépôt git ; son mutant « word-boundary » (sémantique `-w`) est TUÉ"
    - "Le rejeu sait rougir sur une étape `run: |` tronquée : la fixture porte un bloc qui échoue APRÈS sa première ligne et après un heredoc intérieur ; la sortie de chaque étape est recopiée sur la sortie standard du rejeu"
    - "Le rejeu CI est piloté par `.github/workflows/ci.yml` lui-même (`tools/replay-ci-jobs.sh`) : étapes extraites par job, bloc `run: |` dé-indenté selon la sémantique YAML, lancées en `bash --noprofile --norc -e` (jamais `-o pipefail`), étapes `if:` et `uses:` sautées ET nommées, zéro étape extraite = rc 2, jamais un vert"
    - "La preuve des suites de modules à seuil codé en dur (`tools/prove-density-suites.sh`) sait rendre ROUGE : sur l'arbre d'avant la phase, `<clé> 300 301` rend 1 pour les cinq clés (les labels de dépassement apparaissent dès 300) et `<clé> 250 251` rend 0 (les labels sont atteignables sous l'ancien seuil)"
  artifacts:
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh"
      provides: "recensement rejouable BUDG-04, rc 0/1/2"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh"
      provides: "contrôle négatif versionné + mutant word-boundary"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh"
      provides: "contrôle négatif sur clone du dépôt réel (joué à la clôture)"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh"
      provides: "rejeu des jobs tests/gates extrait de ci.yml"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh"
      provides: "preuve rouge/verte des seuils codés en dur des suites de modules"
  key_links:
    - "Tous les plans de vague 2+ pilotent leur famille PAR `tools/check-no-live-250.sh`, filtré sur leurs chemins — jamais par une liste recopiée (CONTEXT § Portée de D-01)"
    - "La clôture (40.1-14) rejoue `replay-ci-jobs.sh --job tests` et `--job gates` et `controle-negatif-reel.sh` : ces trois outils sont son seul chemin de preuve"
---

<objective>
**Wave 0 de la Phase 40.1.** Avant qu'un seul « 250 » ne change, versionner les trois instruments
qui pilotent et prouvent tout le reste de la phase :

1. la commande de recensement BUDG-04 et son contrôle négatif ;
2. le rejeu local des jobs CI `tests` et `gates`, extrait de `ci.yml` (jamais une liste recopiée) ;
3. la preuve rouge/verte des seuils codés en dur des suites de modules.

**Emplacement choisi et justification** : `tools/` du dossier de phase, versionné. (a) La liste
d'exclusions est un classement DATÉ de cette phase (tranché par le manager vf-dev-manager au digest
de mission du 2026-09-16) : la poser en `scripts/` ou la câbler en gate permanent imposerait sa
maintenance à chaque futur document historique citant l'ancien plafond — c'est un geste de
protection du dépôt, hors périmètre (O-3 → Phase 41, D-H3). (b) Le dossier de phase est déjà exclu
par la règle d'auto-référence, donc les outils (qui citent 250 par construction) ne se recensent
pas eux-mêmes. (c) Hors de `plugin/` et `scripts/`, ils ne modifient ni la découverte des suites CI
ni le compteur « N suites » gaté par `check-version-sync.sh`. Le scratchpad du manager étant
éphémère, le contenu de sa commande est cité intégralement ci-dessous (`<reference_census>`).

Purpose: rendre chaque critère de la phase rejouable par commande, et chaque preuve capable de
rendre rouge (QUAL-01, leçon « une preuve doit pouvoir rendre rouge »).
Output: cinq scripts sous `tools/`, aucun fichier du dépôt hors dossier de phase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-RESEARCH.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<reference_census>
Contenu de la commande validée par le manager (rejouée par lui et par un second agent : 113 lignes,
56 fichiers, rc=1 sur `4e7c8e4`, mesure historique datée du 2026-09-16), À REPRODUIRE dans `tools/check-no-live-250.sh`, avec les TROIS
écarts écrits ci-dessous (et seulement eux).

En-tête et mécanique (inchangés) :
- shebang `#!/usr/bin/env bash`, `set -eu`, `export LC_ALL=C`, `cd "${1:-.}" || exit 2`, refus
  `git rev-parse --is-inside-work-tree` → message `ERREUR: pas un depot git` sur stderr, exit 2 ;
- `RE='(^|[^0-9.])250([^0-9]|$)'` ;
- balayage : `git -c core.quotepath=off -c color.grep=never -c grep.fullName=false grep -a -n -E -e "$RE"`,
  rc 1 de git grep = aucun hit (hits vide), tout autre rc non nul = `ERREUR: git grep rc=<rc>`, exit 2 ;
- chaque hit `chemin:ligne:contenu` est découpé (`path=${h%%:*}`, `rest=${h#*:}`, `content=${rest#*:}`) ;
  il est exclu si UNE règle `glob ::: ere` vérifie `[[ $path == $glob ]] && [[ $content =~ $re ]]`
  (`.` = fichier entier) ; lignes vides et `#` des règles ignorées ;
- sortie : hits restants triés (`sort`), exit 1 ; sinon exit 0 sans sortie.

Règles (section par section, format `glob ::: ere`) :

(c) ARCHIVE datée, jamais touchée —
`.planning/milestones/* ::: .` · `.planning/missions/* ::: .` · `.planning/MISSION-*.dag.json ::: .` ·
`.planning/quick/* ::: .` · `.planning/research/* ::: .` · `reports/* ::: .` · `docs/superpowers/* ::: .` ·
`CHANGELOG.md ::: .` · `plugin/*/CHANGELOG.md ::: .` ·
`README.md ::: ^\| \`v[0-9]+\.[0-9]+\.[0-9]+\` \|` · `README.fr.md ::: ^\| \`v[0-9]+\.[0-9]+\.[0-9]+\` \|` ·
**ÉCART (a)** : les quatre règles de dossiers de phase du manager (`VFDO-[0-9]-*`,
`VFDO-[0-35-9][0-9]-*`, `VFDO-40-*`, `VFDO-40.1-*`) sont fusionnées en UNE règle
`.planning/phases/* ::: .` — raison : la classe `[0-35-9]` laissait `VFDO-41-*` (dossier déjà
présent) et tout futur `VFDO-4x-*` hors exclusion, alors qu'un dossier de phase est par construction
un artefact daté, jamais une énonciation vivante de la charte ; la règle fusionnée contient
l'auto-référence de la 40.1 (qui DOIT citer l'ancien plafond, y compris ces outils).

(s) auto-référence de la Phase 40.1 hors dossier de phase —
`.planning/ROADMAP.md ::: au plafond de 250\. Samuel ne veut` ·
`.planning/ROADMAP.md ::: aucun .*250.* vivant rattach`

(b) AUTRE SENS, jamais touché —
`plugin/software-architecture/README.md ::: check-file-size\.sh` ·
`plugin/software-architecture/SKILL.md ::: check-file-size\.sh.*250L` ·
`plugin/software-architecture/SKILL.md ::: s'approche de\) 250-300 lignes` ·
`plugin/software-architecture/references/restructuration-playbook.md ::: warn 250L / block 300` ·
`plugin/software-architecture/rules/production-code-architecture.md ::: Seuil de taille` ·
`plugin/software-architecture/scripts/check-file-size.sh ::: VF_ARCH_WARN` ·
`plugin/software-architecture/scripts/tests/test-check-file-size.sh ::: VF_ARCH_WARN=250 VF_ARCH_BLOCK=300` ·
`plugin/software-architecture/scripts/tests/test-guard-file-size.sh ::: replace_all` ·
`plugin/consolidator/scripts/archive.sh ::: LOG_FILE|rotation simple du log` ·
`manual/en/07-under-the-hood/the-machine-gates.md ::: 300-line code threshold` ·
`manual/fr/07-sous-le-capot/les-gates-machine.md ::: ^  avertissement .*250 lignes avant le blocage ferme` ·
`.planning/codebase/CONVENTIONS.md ::: VF_ARCH_WARN` ·
`.planning/ROADMAP.md ::: pr-branch\.md:250-270` ·
`.planning/STATE.md ::: 170-250 l\. de tests` ·
`.planning/instruction-budget-baselines.tsv ::: ^plugin/[^[:space:]]+[[:space:]]+250[[:space:]]` ·
`plugin/reference/content/examples/PetitsCoursFlow/CLAUDE.md ::: 250 EUR` ·
`plugin/skill-creator/INSTALL.md ::: ~250 lignes \(workflow\)`

(h) HISTOIRE datée — classement tranché par le manager vf-dev-manager (digest de mission,
2026-09-16), d'après CONTEXT « l'histoire ne se réécrit pas » —
`docs/ADR.md ::: orchestrator-template\.md\` \(.*250L, ADR-029\)` ·
`.planning/STATE.md ::: 209/250 l\.|250/250 l\.|241/250` ·
`.planning/REQUIREMENTS.md ::: VERIF-02|\`AGENT\.md\` reste .*250 L\.|250/250 lignes` ·
`.planning/ROADMAP.md ::: \`AGENT\.md\` reste .*250 L \(d|exact de 250 lignes|L'agent reste .*250 lignes \(ADR-029\)|AGENT\.md restructur.*250 lignes` ·
`.planning/BACKLOG.md ::: ^250 lignes\) est un geste|Mesure r.*elle : 250|reste .*250/250` ·
`.planning/codebase/CONCERNS.md ::: 249/250 lignes` ·
`.claude/agent-memory/* ::: .` ·
`plugin/dev-orchestrator/references/mission-flow.md ::: 249/250 lignes` ·
`plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh ::: \(249/250\)` ·
`plugin/reference/content/VERSION.md ::: 8 templates agents|La v2\.0 impose` ·
`plugin/reference/content/methodology/VIBEFLOW_CORE.md ::: ^\| \*\*v4\.1 \(CORE\)\*\*`

**ÉCART (b)** : la règle `.planning/seeds/SEED-001-equipe-produit-v1.md ::: .` est RETIRÉE — le
manager verse la contrainte PROSPECTIVE du seed (« Aucun nouvel agent > … lignes ») au lot vivant ;
elle sera passée à 300 par le plan 40.1-13.

**ÉCART (c)** — écart au classement du manager, écrit ici : la règle BACKLOG `reste .*250/250` est
CONSERVÉE. Le manager ne la versait au lot vivant que « s'il énonce le plafond » ; lue en place
(`.planning/BACKLOG.md`, item « Dérive documentaire… DIFFÉRÉ (2026-09-15) », point 1), la ligne cite
entre guillemets une MESURE datée du CHANGELOG validator (« reste à 250/250 ») pour dater un
mensonge du README — elle n'énonce aucune règle. Elle reste donc en (h). Le point 1 de cet item est
traité sur le fond par le plan 40.1-11 (réécriture de `plugin/validator/README.md`).

**Balayage des littéraux introduits par la phase (révision 1, constat B1 du checker)** : aucune règle
nouvelle n'est ajoutée, parce qu'aucun plan n'introduit l'ancienne valeur dans un fichier non exclu.
Tous les plans ont été relus pour vérifier ce point. (1) La suite du gate (40.1-03) exprime ses
tailles de fixture par rapport à des constantes locales (`BORNE_AVERT`, `PLAFOND`), jamais par le
littéral de la valeur sous la zone. (2) Les fixtures de l'auditeur (40.1-05), les preuves des suites
de modules (40.1-01 T3, 40.1-07 à 40.1-10, 40.1-15, 40.1-15) et tous les outils vivent sous `tools/` du dossier de
phase, couvert par `.planning/phases/*`. (3) Les textes « avertissement dès 251 » et « bloque
au-delà de 300 » ne matchent pas la regex. (4) Les entrées de CHANGELOG sont exclues en entier.
(5) Les écritures de ledger (REQUIREMENTS, BACKLOG, plan 40.1-14) et la note d'amendement ADR
(40.1-13) ont pour consigne de n'écrire aucune valeur de l'ancien plafond. Chaque plan filtre le
recensement sur ses propres chemins, donc tout littéral échappé à ce balayage rougit le plan
fautif, pas la clôture.

Les en-têtes de section (c)/(s)/(b)/(h) sont des commentaires `#` du bloc de règles ; l'en-tête du
script cite la phase, l'exigence, les codes de sortie et la raison de l'absence de numéros de ligne.
</reference_census>

<tasks>

<task type="auto">
  <name>Task 0 : précondition machine — la base contient le hotfix v2.63.1 (lecture seule, AVANT toute édition, sans tools/)</name>
  <files>aucun (lecture seule)</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-02-PLAN.md (spec W-C de `tools/phase-base.sh`, dont cette tâche reprend la règle de choix de ref)
  </read_first>
  <action>
Jouer la vérification ci-dessous AVANT toute autre commande d'écriture de ce plan. Vague 1 :
`tools/phase-base.sh` n'existe pas encore (40.1-02 le crée), la commande résout donc ELLE-MÊME la
ref main selon la règle de la spec W-C, sans `--main-ref` : `origin/main` et `main` toutes deux
présentes → la plus avancée (`git merge-base --is-ancestor`), divergence → rc 2 ; une seule →
celle-là ; aucune → rc 2. Base `B` = `git merge-base HEAD <ref>`. Elle juge ensuite le même CONTENU
que la Task 0 de 40.1-07 et 40.1-14 :
- `VERSION` racine de `B` ≥ v2.63.1, par comparaison numérique champ à champ ;
- entrée `## [v…]` du CHANGELOG de `plugin/dev-orchestrator/` à `B` citant v2.63.1 (seule l'entrée
  du hotfix la cite) ;
- `B` = pointe de la ref retenue (la branche descend du dernier `main` connu).
Ni sha, ni sujet de commit : un squash-merge du hotfix change ses sha, pas ce contenu.
**Rouge ou rc 2 → arrêt immédiat, aucune écriture, remontée au manager** avec la ligne
`precondition hotfix` imprimée ; l'exécutant ne rebase pas et ne choisit pas de ref (arbitrage
Samuel, AskUserQuestion session principale, 2026-09-16 : l'exécution attend le merge du hotfix dans
main). 40.1-07 et 40.1-14 rejouent ce contrôle plus tard, via `tools/phase-base.sh`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 't=$(git rev-parse -q --verify "origin/main^{commit}" || true); u=$(git rev-parse -q --verify "main^{commit}" || true); if [ -n "$t" ] && [ -n "$u" ]; then if git merge-base --is-ancestor "$u" "$t"; then ch=$t; elif git merge-base --is-ancestor "$t" "$u"; then ch=$u; else echo "precondition hotfix : ref_main=divergence (origin/main et main ont diverge, aucune ne descend de l autre) : arret, remontee au manager"; exit 2; fi; elif [ -n "$t" ]; then ch=$t; elif [ -n "$u" ]; then ch=$u; else echo "precondition hotfix : ref_main=absente (ni origin/main ni main)"; exit 2; fi; B=$(git merge-base HEAD "$ch"); [ -n "$B" ]; T=$(mktemp -d); git show "$B":VERSION > "$T/v"; v=$(awk "NR==1{print \$1}" "$T/v"); sup=$(awk -v v="$v" -v s="v2.63.1" "BEGIN{ r=\"non\"; n=split(v,a,\".\"); split(s,b,\".\"); sub(/^v/,\"\",a[1]); sub(/^v/,\"\",b[1]); if (n==3 && a[1] ~ /^[0-9]+$/ && a[2] ~ /^[0-9]+$/ && a[3] ~ /^[0-9]+$/) { r=\"oui\"; for (i=1;i<=3;i++) { if (a[i]+0 > b[i]+0) break; if (a[i]+0 < b[i]+0) { r=\"non\"; break } } } print r }"); git show "$B":plugin/dev-orchestrator/CHANGELOG.md > "$T/c"; h=$(awk "/^## \\[v[0-9]+[.][0-9]+[.][0-9]+\\]/ && /v2[.]63[.]1/ {c++} END{print c+0}" "$T/c"); ds=$([ "$B" = "$ch" ] && echo oui || echo non); echo "precondition hotfix (vague 1, ref resolue sans tools/) : ref_main=$ch base=$B VERSION_base=$v version_ge_v2.63.1=$sup entree_changelog_dev-orchestrator_v2.63.1=$h branche_sur_la_pointe_main=$ds"; [ "$sup" = oui ] && [ "$h" -ge 1 ] && [ "$ds" = oui ]'</automated>
    <fails_when>exit non nul ; `ref_main=divergence` ou `ref_main=absente` (rc 2) ; ou `version_ge_v2.63.1=non` ; ou `entree_changelog_dev-orchestrator_v2.63.1=0` ; ou `branche_sur_la_pointe_main=non`. Conduite : arrêt et remontée au manager, jamais rebase ni poursuite</fails_when>
  </verify>
  <acceptance_criteria>
    - la ligne `precondition hotfix (vague 1, ref resolue sans tools/) : … version_ge_v2.63.1=oui entree_changelog_dev-orchestrator_v2.63.1=<n ≥ 1> branche_sur_la_pointe_main=oui` est consignée au SUMMARY, avant tout commit du plan
  </acceptance_criteria>
  <done>La base de vague 1 contient le hotfix, prouvé sur le contenu sans dépendre de tools/ ; sinon le plan s'est arrêté sans rien écrire.</done>
</task>

<task type="tracer">
  <name>Task 1 : recensement BUDG-04 versionné, contrôle négatif sur fixtures et sur clone réel</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh</files>
  <read_first>
    - `<reference_census>` de ce plan (contenu exact à reproduire, trois écarts écrits)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md (§ Portée de D-01, § Specifics : « commande rejouable, avec son contrôle négatif »)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (patron `ok`/`ko` à trois champs assertion/attendu/obtenu, patron mutant `awk` + `cmp -s` + `bash -n`)
    - .planning/BACKLOG.md (item « Dérive documentaire et fragilité latente… », pour l'écart (c))
    - .planning/seeds/SEED-001-equipe-produit-v1.md (section Scope Estimate, pour l'écart (b))
  </read_first>
  <action>
Écrire `tools/check-no-live-250.sh` exactement selon `<reference_census>` (mécanique, regex, règles,
trois écarts), exécutable, sans `grep` externe hors `git grep`, sans pipefail, lançable en
`bash --noprofile --norc -e`. Aucun numéro de ligne dans les règles.

Écrire `tools/test-check-no-live-250.sh` (contrôle négatif versionné) : `set -u`, compteurs PASS/FAIL,
`ko <assertion> <attendu> <obtenu>` à trois champs, `mktemp -d` + `trap` de nettoyage, jamais le dépôt
réel. Chaque cas crée un dépôt git jetable (`git init -q`, fichiers écrits par `printf`, `git add`,
identité passée par `-c user.name=t -c user.email=t@t` si un commit est nécessaire) et lance l'outil
avec ce dépôt en argument. Cas obligatoires, chacun avec rc ET nombre de lignes de sortie (compté
par `awk 'END{print NR}'` sur un fichier, jamais `wc -l` sur un flux vide) :
C0 fichier sans 250 → rc 0, 0 ligne ; C1 `CLAUDE.md` portant `agents ≤250L` → rc 1, 1 ligne
commençant par `CLAUDE.md:` (l'angle mort de `git grep -w`) ; C2 `plugin/x/scripts/g.sh` portant
`VF_BUDGET_LINE_CAP=250` → rc 1 ; C3 `.planning/milestones/a.md` portant `agents <= 250 lignes` →
rc 0 (exclusion d'archive) ; C4 `manual/en/07-under-the-hood/the-machine-gates.md` portant la ligne
exclue (`The 300-line code threshold … warning at 250 lines`) PLUS une autre ligne `agents under 250
lines` → rc 1, exactement 1 ligne (l'exclusion est ligne-à-ligne, jamais fichier entier) ; C5 un
répertoire non git → rc 2 ; C6 faux positifs numériques (`2500`, `1250`, `v2.50`) → rc 0 ;
C7 `.planning/phases/VFDO-41-x/41-01-PLAN.md` portant 250 → rc 0 (écart (a)) ;
C8 `.planning/seeds/SEED-001-equipe-produit-v1.md` portant `> 250 lignes` → rc 1 (écart (b)).
Mutants opposables sur une COPIE de l'outil (awk ligne-à-ligne, rejet si `cmp -s` identique ou
`bash -n` invalide) : MUT-W remplace la ligne `RE=` par une regex à frontières de mot
(`(^|[^[:alnum:]_])250([^[:alnum:]_]|$)`, sémantique `-w`) → sur la fixture C1, mutant rc 0 et
original rc 1, sinon ko ; MUT-X neutralise la condition d'exclusion (la comparaison `[[ $path == $glob ]]`
remplacée par une condition toujours fausse) → sur la fixture C3, mutant rc 1 et original rc 0.
Sortie finale `== resultat : N ok, M ko ==`, exit 0 ssi M = 0.

Écrire `tools/controle-negatif-reel.sh` (joué à la CLÔTURE, plan 40.1-14, pas ici) : clone le
dépôt courant (`git clone -q "$(git rev-parse --show-toplevel)"` vers un `mktemp -d`), lance l'outil
sur le clone et exige, avec trace à trois champs : R0 rc 0 et 0 ligne (sinon imprime les lignes et
sort 1 — précondition : dépôt propre) ; R1 ligne `- **Densité** : agents ≤250L (réintroduit)`
ajoutée à `CLAUDE.md` du clone → rc 1, 1 ligne `CLAUDE.md:` ; restauration `git -C <clone> checkout -- CLAUDE.md` → rc 0 ;
R2 `VF_BUDGET_LINE_CAP=300` réécrit en `VF_BUDGET_LINE_CAP=250` par awk dans le gate du clone → rc 1,
1 ligne `plugin/conductor/scripts/check-instruction-budget.sh:` ; restauration → rc 0 ;
R3 `.planning/milestones/zz-temoin.md` portant `agents <= 250 lignes`, `git add` → rc 0 ;
R4 ligne `The charter keeps agents under 250 lines.` ajoutée à `manual/en/07-under-the-hood/the-machine-gates.md` → rc 1, 1 ligne.
Portage des cas N1-N4 du contrôle manuel du manager (copie jetable, 2026-09-16), sans chemin machine.

Rejouer ensuite l'outil sur le dépôt AVANT toute autre édition de la phase et garder sa sortie
dans le SUMMARY (compte de lignes et de fichiers) : c'est la liste de travail des plans de vague 2,
re-dérivée par chacun au moment d'exécuter.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh</automated>
    <fails_when>exit non nul, ou une ligne `== resultat : N ok, M ko ==` avec M différent de 0, ou moins de 11 lignes « ✓ » (9 cas + 2 mutants)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; n=$(awk "END{print NR}" "$T/o"); c=$(awk -F: "\$1==\"CLAUDE.md\"{a++} /VF_BUDGET_LINE_CAP=/{b++} \$1==\".planning/seeds/SEED-001-equipe-produit-v1.md\"{s++} \$1 ~ /^\\.planning\\/(phases|milestones)\\// || \$1 ~ /CHANGELOG\\.md\$/ {x++} END{print a+0, b+0, s+0, x+0}" "$T/o"); echo "rc=$rc lignes=$n temoins(CLAUDE,CAP,SEED,archives)=$c"; [ "$rc" -eq 1 ] && [ "$n" -gt 0 ] && [ "$c" = "1 1 1 0" ]'</automated>
    <fails_when>exit non nul ; ou `rc=` différent de 1 ; ou `temoins(CLAUDE,CAP,SEED,archives)=` différent de `1 1 1 0`</fails_when>
    <automated>bash --noprofile --norc -n .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh</automated>
    <fails_when>exit non nul (syntaxe invalide)</fails_when>
  </verify>
  <!-- planner-discipline-allow: git grep -w -->
  <acceptance_criteria>
    - `tools/check-no-live-250.sh` contient la ligne `RE='(^|[^0-9.])250([^0-9]|$)'` et la règle `.planning/phases/* ::: .` ; il ne contient ni `SEED-001`, ni `VFDO-[0-35-9]`, ni `git grep -w`
    - la suite de contrôle négatif rend `== resultat : 11 ok, 0 ko ==` (ou plus de ok, jamais de ko), chaque ko éventuel imprimant `assertion :`, `attendu :`, `obtenu :`
    - MUT-W est TUÉ (mutant rc 0 / original rc 1 sur la fixture `≤250L`) et MUT-X est TUÉ (mutant rc 1 / original rc 0 sur la fixture d'archive)
    - le rejeu sur le dépôt avant édition rend rc 1, avec exactement une ligne `CLAUDE.md:`, une ligne portant `VF_BUDGET_LINE_CAP=`, une ligne du seed, et zéro ligne sous `.planning/phases/`, `.planning/milestones/` ou un `CHANGELOG.md`
    - aucun des trois fichiers ne contient de chemin absolu de machine (`/Users/`, `/home/<login>`, `/private/tmp`)
  </acceptance_criteria>
  <done>La commande de recensement et ses deux contrôles négatifs (fixtures, clone réel) sont versionnés sous `tools/`, prouvés capables de rendre rouge, et la liste de travail d'avant-phase est consignée au SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2 : rejeu des jobs CI extrait de ci.yml</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh</files>
  <read_first>
    - .github/workflows/ci.yml (jobs `tests` L22+ et `gates` L244+ : formes `- uses:`, `- name:` + `run: |`, `run: <ligne>`, `if:`, `env:`)
    - .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-04-SUMMARY.md (§ Preuves : rejeu en `bash --noprofile --norc -e`, faux rouge observé en `-eo pipefail`)
    - scripts/check-machine-paths.sh (en-tête : ne pas se fier au `grep` proxifié, balayer en awk)
  </read_first>
  <action>
Écrire `tools/replay-ci-jobs.sh`, usage `replay-ci-jobs.sh --job tests|gates [--root DIR] [--ci-file F] [--step PREFIXE] [--list]`.
Racine par défaut : `git rev-parse --show-toplevel` du répertoire courant ; `--ci-file` par défaut
`<root>/.github/workflows/ci.yml`. Parsing en awk UNIQUEMENT (aucun grep) :
bloc du job = lignes après `^  <job>:$` jusqu'à la ligne suivante `^  [A-Za-z0-9_-]+:$` ou EOF ;
une étape commence à `^      - ` ; on relève son `name:`, la présence d'un `if:`, d'un `uses:`, et
son `run:` — forme bloc `run: |` (lignes suivantes vides ou d'indentation > 8, jusqu'à la première
ligne non vide d'indentation ≤ 8 ; dé-indentation par l'indentation de la première ligne non vide du
bloc, sémantique YAML de bloc littéral : le `EOF` du heredoc de la découverte des suites retombe en
colonne 0) ou forme ligne `run: <commande>`.
Sélection : job `tests` → SEULE l'étape nommée exactement `Découvrir et lancer toutes les suites`
est rejouée, les autres sont listées `SAUTEE (installation/infra du runner)` ; job `gates` → toute
étape avec `run:` est rejouée, sauf celles portant `if:` (listées `SAUTEE (conditionnelle : <expression>)`) ;
les étapes `uses:` sont listées `SAUTEE (action)`. `--step PREFIXE` restreint aux étapes dont le nom
commence par le préfixe. `--list` imprime la sélection sans rien lancer.
Exécution : chaque script d'étape écrit dans un fichier temporaire, lancé par
`bash --noprofile --norc -e <fichier>` avec la racine en répertoire courant — JAMAIS `-o pipefail`
(invocation fidèle au runner sans `shell:` déclaré) ; sortie complète dans un journal par étape sous
un `mktemp -d` conservé et imprimé, ET recopiée sur la sortie standard du rejeu (encadrée par
`--- debut <nom> ---` / `--- fin <nom> ---`) pour que la clôture puisse lire les bilans internes des
étapes ; une ligne `REJEU <job> | <n> | rc=<rc> | <nom>` par étape.
Bilan `BILAN REJEU <job> : <r> rejouee(s), <s> sautee(s), <k> en echec`. Codes : 0 toutes rejouées
vertes ; 1 au moins une en échec ; 2 fichier CI illisible, job absent, ou ZÉRO étape rejouée (une
sélection vide n'est jamais un vert, F13).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh; T=$(mktemp -d); printf "%s\n" "jobs:" "  gates:" "    runs-on: x" "    steps:" "      - uses: actions/checkout@v4" "      - name: vert" "        run: |" "          set -eu" "          echo ok" "      - name: rouge" "        run: exit 7" "      - name: bloc-rouge" "        run: |" "          set -eu" "          cat > /dev/null <<EOF" "          interieur du heredoc" "          EOF" "          echo apres-heredoc" "          exit 5" "      - name: main" "        if: github.ref == x" "        run: exit 9" > "$T/ci.yml"; rc=0; bash "$P" --root . --ci-file "$T/ci.yml" --job gates > "$T/o" 2>&1 || rc=$?; cat "$T/o"; rc2=0; bash "$P" --root . --ci-file "$T/ci.yml" --job tests > /dev/null 2>&1 || rc2=$?; rc3=0; bash "$P" --root . --ci-file "$T/ci.yml" --job gates --step vert > /dev/null 2>&1 || rc3=$?; s=$(awk "/SAUTEE \\(conditionnelle/{a++} /SAUTEE \\(action/{b++} /rc=7 \\| rouge/{c++} /rc=5 \\| bloc-rouge/{d++} /^apres-heredoc/{e++} END{print a+0, b+0, c+0, d+0, e+0}" "$T/o"); echo "rc_fixture=$rc rc_job_absent=$rc2 rc_vert_seul=$rc3 sautees_rouge_bloc_heredoc=$s"; [ "$rc" -eq 1 ] && [ "$rc2" -eq 2 ] && [ "$rc3" -eq 0 ] && [ "$s" = "1 1 1 1 1" ]'</automated>
    <fails_when>exit non nul ; ou `rc_fixture=` différent de 1, `rc_job_absent=` différent de 2, `rc_vert_seul=` différent de 0, ou `sautees_rouge_bloc_heredoc=` différent de `1 1 1 1 1` (le 4e chiffre prouve qu'un bloc `run: |` est rejoué au-delà de sa première ligne et rougit sur un échec tardif ; le 5e, que le heredoc intérieur est refermé par la dé-indentation et que la ligne suivante s'exécute)</fails_when>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh; T=$(mktemp -d); bash "$P" --job gates --list > "$T/g"; bash "$P" --job tests --list > "$T/t"; n=$(awk "/check-instruction-budget|check-version-sync|check-machine-paths|check-divergence/{c++} END{print c+0}" "$T/g"); m=$(awk "/Découvrir et lancer toutes les suites/{c++} END{print c+0}" "$T/t"); echo "gates_temoins=$n tests_decouverte=$m"; [ "$n" -ge 4 ] && [ "$m" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou `gates_temoins=` inférieur à 4 ; ou `tests_decouverte=` égal à 0</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates</automated>
    <fails_when>exit non nul, ou une ligne `REJEU gates | … | rc=` différente de `rc=0` (état de référence d'avant-phase : le job gates doit être vert)</fails_when>
  </verify>
  <acceptance_criteria>
    - sur la fixture CI : étape ligne `rouge` rc 7 ET étape bloc `bloc-rouge` rc 5 détectées (échec APRÈS la première ligne et après un heredoc intérieur, `apres-heredoc` visible sur la sortie standard), étape `if:` et étape `uses:` listées sautées, job absent → rc 2, `--step vert` → rc 0 ; un extracteur qui ne garderait que la première ligne du bloc rendrait `bloc-rouge` vert et ferait échouer ce verify
    - `--list` sur le vrai `ci.yml` nomme au moins `check-instruction-budget`, `check-version-sync`, `check-machine-paths`, `check-divergence` pour `gates`, et l'étape de découverte pour `tests`
    - rejeu réel du job `gates` avant toute édition de la phase : rc 0 (référence d'avant-phase consignée au SUMMARY, étape `check-release-tag` listée sautée)
    - le script ne contient ni `pipefail` ni appel `grep` (vérifiable par lecture ; `git grep` absent aussi)
  </acceptance_criteria>
  <done>Le rejeu local des jobs `tests` et `gates` est piloté par `ci.yml`, fidèle au shell du runner, et sait rendre rouge.</done>
</task>

<task type="auto">
  <name>Task 3 : preuve rouge/verte des seuils codés en dur des suites de modules</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh (T3 ~L1249, T5 ~L1360, T8 ~L1449, T22 ~L2456 dans la branche `else` de l'existence de DOCSFLOW, T31-G ~L6225, T35 (d) ~L6452-6500 ; `MOD` dérivé de `$0`)
    - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh (T1 ~L93, T7 ~L203)
    - plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh, plugin/content-bundle/scripts/tests/test-content-bundle.sh, plugin/growth-bundle/scripts/tests/test-growth-bundle.sh (T2, variable `TEAM`)
  </read_first>
  <action>
Écrire `tools/prove-density-suites.sh`, usage `prove-density-suites.sh <cle> <taille-verte> <taille-rouge>`.
Table interne clé → (suite, fichiers à allonger, labels de DÉPASSEMENT) ; `%N` = taille imposée ;
les labels ne portent JAMAIS le seuil, seulement la taille, pour valoir avant ET après la phase :
- `dev-orchestrator` : suite `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` ;
  allonge `plugin/dev-orchestrator/AGENT.md` et `plugin/dev-orchestrator/agents/vf-dev-manager.md` ;
  labels `T3 agent : AGENT.md = %NL (>`, `T5 densité agent : AGENT.md = %NL (>`,
  `T8 agents : vf-dev-manager.md dépasse`, `T22 captation : AGENT.md à %N lignes`,
  `T31-G : vf-dev-manager.md dépasse le plafond ADR-029 (%N/`, `T35 (d) plafond ADR-029 : vf-dev-manager.md fait %N ligne(s) (>` ;
- `design-orchestrator` : allonge `plugin/design-orchestrator/AGENT.md` et `agents/vf-design-manager.md` ;
  labels `T1 agents : vf-design-manager.md dépasse`, `T7 densité agent : AGENT.md = %NL (>` ;
- `business-pilot-bundle` / `content-bundle` / `growth-bundle` : allonge respectivement
  `agents/vf-business-manager.md`, `agents/vf-content-manager.md`, `agents/vf-growth-manager.md` ;
  label `T2 densité : <agent>.md = %NL (>`.
Mécanique : depuis la racine du dépôt, copier `plugin/` dans un `mktemp -d` (`cp -R`), jamais
l'arbre réel ; pour chaque taille, repartir d'une copie fraîche, allonger chaque fichier ciblé à
EXACTEMENT la taille par awk (`{print} END{for(i=NR+1;i<=N;i++) print "ligne de remplissage neutre"}`
vers un temporaire puis `mv` — sortie awk toujours terminée par un saut de ligne, donc `wc -l` et
`awk NR` concordent) ; refuser (rc 2) si un fichier ciblé dépasse déjà la taille verte ; vérifier la
taille obtenue par `awk 'END{print NR}'` ; lancer la suite COPIÉE (`bash <copie>/plugin/<mod>/scripts/tests/<suite>`),
capturer sa sortie dans un fichier, ignorer son rc. Recherche des labels par awk `index()` sous
`LC_ALL=C` (octets, UTF-8 inclus). Assertions, chacune tracée assertion/attendu/obtenu : à la taille
verte AUCUN label présent ; à la taille rouge CHAQUE label présent. rc 0 si tout tient, 1 sinon,
2 usage/clé inconnue/allongement impossible.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh; for k in dev-orchestrator design-orchestrator business-pilot-bundle content-bundle growth-bundle; do rc=0; bash "$P" "$k" 250 251 > /dev/null 2>&1 || rc=$?; echo "$k ancien-seuil 250/251 rc=$rc"; [ "$rc" -eq 0 ]; done'</automated>
    <fails_when>exit non nul, ou une ligne `ancien-seuil 250/251 rc=` différente de `rc=0` (un label est inatteignable ou mal écrit : la preuve serait incapable de rougir)</fails_when>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh; for k in dev-orchestrator design-orchestrator business-pilot-bundle content-bundle growth-bundle; do rc=0; bash "$P" "$k" 300 301 > /dev/null 2>&1 || rc=$?; echo "$k avant-phase 300/301 rc=$rc"; [ "$rc" -eq 1 ]; done'</automated>
    <fails_when>exit non nul, ou une ligne `avant-phase 300/301 rc=` différente de `rc=1` (avant la phase, 300 lignes DOIVENT être vues en dépassement : sinon l'outil ne discrimine pas)</fails_when>
  </verify>
  <acceptance_criteria>
    - les cinq clés rendent rc 0 en `250 251` et rc 1 en `300 301` sur l'arbre d'avant-phase (rouge prouvé avant, vert attendu après les plans 40.1-07 à 40.1-10, 40.1-15, 40.1-15)
    - l'arbre réel est intact après exécution : `git status --porcelain -- plugin` vide
    - aucun label ne contient le nombre du seuil (seulement `%N`)
  </acceptance_criteria>
  <done>Chaque suite de module à seuil codé en dur a une preuve rejouable qui rougit à 300 avant la phase et doit verdir à 300 / rougir à 301 après.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outil de recensement → critère BUDG-04 | une liste d'exclusions trop large rendrait le critère vert à vide |
| ci.yml → rejeu local | un rejeu infidèle (pipefail, étapes oubliées) donnerait un faux vert ou un faux rouge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-01 | Repudiation | `check-no-live-250.sh` (exclusions) | high | mitigate | exclusions nominatives chemin + contenu, jamais fichier entier hors archives ; C4 et R4 prouvent l'exclusion ligne-à-ligne ; MUT-X prouve qu'elle agit, MUT-W que la regex voit `≤250L` |
| T-40.1-02 | Repudiation | `replay-ci-jobs.sh` | medium | mitigate | extraction depuis `ci.yml`, sélection vide = rc 2, étapes sautées nommées, `bash -e` sans pipefail, fixture rouge rc 7 détectée |
| T-40.1-03 | Tampering | arbre réel pendant les preuves | medium | mitigate | preuves sur `mktemp -d` / clone ; `git status --porcelain -- plugin` vide exigé |
| T-40.1-04 | Information disclosure | chemins de machine dans les outils versionnés | low | mitigate | critère « aucun chemin absolu de machine » ; `check-machine-paths.sh` rejoué à la clôture |
</threat_model>

<verification>
- `test-check-no-live-250.sh` vert, mutants tués ; recensement d'avant-phase rc 1 avec témoins.
- `replay-ci-jobs.sh` rouge sur fixture, `--job gates` vert sur le dépôt d'avant-phase.
- `prove-density-suites.sh` : 5 × rc 0 en 250/251, 5 × rc 1 en 300/301.
</verification>

<success_criteria>
Les trois instruments de la phase sont versionnés, chacun démontré capable de rendre rouge sur
l'état d'avant-phase, et aucun fichier hors `tools/` n'est modifié.
</success_criteria>

## Artifacts this phase produces

- `tools/check-no-live-250.sh` — variable `RE`, bloc `RULES` (sections c/s/b/h), codes 0/1/2
- `tools/test-check-no-live-250.sh` — cas C0-C8, mutants MUT-W, MUT-X
- `tools/controle-negatif-reel.sh` — cas R0-R4
- `tools/replay-ci-jobs.sh` — options `--job`, `--root`, `--ci-file`, `--step`, `--list`
- `tools/prove-density-suites.sh` — clés `dev-orchestrator`, `design-orchestrator`, `business-pilot-bundle`, `content-bundle`, `growth-bundle`

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md` when done
</output>

---
phase: 41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser
plan: 02
type: execute
wave: 2
depends_on: ["41.2-01"]
files_modified:
  - plugin/conductor/scripts/check-planning-not-inflight.sh
  - plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh
  - plugin/conductor/skills/vf-split-planning/SKILL.md
  - plugin/commands/vf-split-planning.md
  - plugin/conductor/scripts/tests/test-vf-split-planning.sh
autonomous: true
requirements: [WSCH-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un lab avec au moins une phase à status == \"In Progress\" (comparaison de chaîne stricte sur le champ que rend `gsd-tools progress`, jamais l'inégalité `plans > summaries` recalculée — celle-ci couvrirait aussi `\"Planned\"`) fait échouer check-planning-not-inflight.sh en exit 1, message citant le(s) numéro(s) de phase et ADR-069"
    - "Un lab sans aucune phase In Progress fait sortir le script en exit 0, silence"
    - "gsd-tools introuvable par la cascade sûre, ou `node` absent pour une cible .cjs, ou JSON illisible → exit 2 (NON VÉRIFIABLE), jamais un 0 de complaisance"
    - "La cascade de résolution du binaire gsd-tools ne contient AUCUN candidat relatif au répertoire de travail courant — la classe de vulnérabilité fermée par dag.sh (D-07 du fichier) n'est pas réintroduite ici"
    - "vf-split-planning ne recalcule jamais lui-même le statut d'une phase : il délègue intégralement au script et relaie son message tel quel en cas de refus"
    - "MUT-1 (mutation de fixture) et MUT-2 (mutation du script en copie temporaire, jamais l'original) sont tués — la suite peut réellement rendre rouge"
    - "Le skill nomme explicitement sa différence avec le skill amont `gsd-workstreams` (pas de verbes switch/resume inexistants côté moteur, couvre la bascule que l'amont ne couvre pas) et ne montre jamais le mot « workstream » dans ce qui est confirmé à l'utilisateur"
  artifacts:
    - path: "plugin/conductor/scripts/check-planning-not-inflight.sh"
      provides: "précondition ADR-069 mutation-provable, cascade de résolution sûre portée de dag.sh"
    - path: "plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh"
      provides: "suite mutation-provable (MUT-1 fixture, MUT-2 script-copie), patron test-check-workstream-pointer.sh"
    - path: "plugin/conductor/skills/vf-split-planning/SKILL.md"
      provides: "skill de bascule day-2, thin wrapper autour du script, patron vf-calibrate"
    - path: "plugin/commands/vf-split-planning.md"
      provides: "trampoline de commande, patron vf-new-lab.md (17 lignes)"
    - path: "plugin/conductor/scripts/tests/test-vf-split-planning.sh"
      provides: "doc-contract sur la description + la distinction avec gsd-workstreams + le vocabulaire montré"
  key_links:
    - "check-planning-not-inflight.sh est le SEUL point de décision bloquante de ce plan — vf-split-planning ne fait qu'appeler `.claude/scripts/check-planning-not-inflight.sh` puis, si accepté, `gsd-tools workstream create` (même primitive que le plan 41.2-01, jamais une réimplémentation)"
    - "Ce plan dépend de 41.2-01 par séquencement méthodologique explicite (TRACER_MODE), pas par conflit de fichiers — aucun fichier de ce plan n'est touché par 41.2-01"
---

<objective>
Livrer le geste de bascule pour un lab déjà démarré en planning unique (WSCH-03) : un script bash
séparé, mutation-provable, qui lit la précondition ADR-069 sur un champ réel du disque — jamais une
recalculée en prose — et un skill fin qui l'encadre sans jamais réimplémenter la mécanique moteur.
Séquencé après le plan 41.2-01 (tracer) sur demande explicite du cadrage : la primitive moteur
(`workstream create`) et sa cascade de résolution sûre sont déjà prouvées, ce plan les réutilise à
l'identique pour un second usage (bascule day-2) plutôt que de les redécouvrir.

Purpose: une précondition bloquante ne peut vivre dans la prose d'un SKILL.md — WSCH-03 exige
explicitement un refus "prouvé par mutation", inatteignable sans un fichier exécutable en isolation.
Output: `check-planning-not-inflight.sh` + sa suite, `vf-split-planning` (skill + trampoline) + sa
suite doc-contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-RESEARCH.md
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-PATTERNS.md
@plugin/conductor/scripts/check-workstream-pointer.sh
@plugin/conductor/scripts/dag.sh
@plugin/conductor/skills/vf-calibrate/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : check-planning-not-inflight.sh — précondition ADR-069, cascade sûre, mutation-provable (WSCH-03, D-11)</name>
  <files>plugin/conductor/scripts/check-planning-not-inflight.sh, plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-workstream-pointer.sh (intégral — patron de header à codes énumérés, arg-parsing `--path`/`-h|--help`/argument inconnu → 64, helpers `diag`/`signal`)
    - plugin/conductor/scripts/dag.sh:149-187 (`resolve_gsd_tools_cmd`, Python mais invariant langage-agnostique — SÉCURITÉ CRITIQUE, docstring citant le PoC de RCE déjà fermé ; porter la cascade en bash, jamais ajouter de candidat cwd/repo-relatif)
    - plugin/planning-core/scripts/detect-gsd-engine.sh:35-49 (style bash de la cascade : fonction-retourne-par-echo, `"${VAR:-$(default_fn)}"`)
    - plugin/conductor/scripts/tests/test-check-workstream-pointer.sh (intégral — patron MUT-1/MUT-2 mutation-provable, `cmp` sur `empreinte()`/`find | LC_ALL=C sort`, jamais `diff`)
    - 41.2-RESEARCH.md § Code Examples ("Dérivation exacte du statut, lue dans le source", commands.cjs:86-124) et § Common Pitfalls Pitfall 1 (jamais `workstream progress`, toujours `progress` top-level) et Pitfall 3 (jamais l'inégalité `plans > summaries`, toujours `status == "In Progress"` strictement)
    - 41.2-CONTEXT.md § D-11 (précondition, champ du disque nommé, refus prouvé par mutation)
  </read_first>
  <behavior>
    - Aucune phase du scope courant (`gsd-tools progress`, scope résolu par le moteur — jamais `--ws` forcé en dur, laisser le moteur résoudre l'actif) à `status == "In Progress"` → exit 0, silence total (stdout ET stderr).
    - Au moins une phase à `status == "In Progress"` (comparaison de chaîne stricte) → exit 1, une ligne stderr citant le(s) numéro(s) de phase et "ADR-069".
    - `gsd-tools` non résolu par la cascade, `node` absent pour une cible `.cjs`, ou sortie JSON illisible par `jq` → exit 2, préfixe "NON VÉRIFIABLE" sur stderr — jamais un 0 de complaisance.
    - Argument inconnu ou option sans valeur → exit 64.
  </behavior>
  <action>
Écrire `plugin/conductor/scripts/check-planning-not-inflight.sh` : shebang `#!/usr/bin/env bash`,
`set -uo pipefail`. En-tête à codes énumérés (patron `check-workstream-pointer.sh`) : `0` = conforme
(aucune phase en vol), `1` = échec constaté (au moins une phase `In Progress`), `2` = NON VÉRIFIABLE
(résolution/JSON), `64` = usage. Arg-parsing : `--path <dir>` (défaut `.`), `-h|--help` (affiche
l'en-tête via `grep '^# ' "$0" | sed 's/^# //'`), argument inconnu → message + exit 64. Fonction
`resolve_gsd_tools_cmd` en bash, cascade EXACTE dans cet ordre : (1) `GSD_TOOLS` variable
d'environnement SI elle pointe un fichier existant (`[ -n "${GSD_TOOLS:-}" ] && [ -f "$GSD_TOOLS" ]`)
; (2) `gsd-tools` résolu sur `PATH` (`command -v gsd-tools`) ; (3)
`${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs` SI ce fichier existe. Rien ne résout
→ retour non-zéro, appelant traduit en exit 2. Cible qui se termine par `.cjs` → invoquer via
`node "$resolved" "$@"` (échec si `node` introuvable sur `PATH` → exit 2) ; toute autre cible →
invoquer directement. **AUCUN candidat construit à partir de `pwd`, `$(git rev-parse
--show-toplevel)`, `./gsd-core` ou toute variante relative au répertoire de travail courant — jamais,
sous aucune condition** (c'est exactement la classe fermée par `dag.sh` D-07, réintroduire un tel
candidat ici serait une régression de sécurité). Invoquer `<cmd résolue> progress --path "$ROOT"
--raw` (jamais `workstream progress`, Pitfall 1) ; filtrer avec `jq -r '[.phases[]? |
select(.status=="In Progress") | .number] | join(",")'` (comparaison de chaîne stricte sur le mot
"In Progress", jamais l'inégalité `plans > summaries`, Pitfall 3) ; si la liste est non vide, imprimer
"ADR-069 : phase(s) en vol (<liste>) — partition refusée tant qu'aucune n'est close." sur stderr et
exit 1 ; sinon exit 0.

Écrire `plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh`, patron intégral de
`test-check-workstream-pointer.sh` (harness `ok()`/`ko()`, `TMP=$(mktemp -d)` + `trap`, jamais hériter
de variables d'environnement du shell appelant). Fixtures construites au format mesuré en
41.2-RESEARCH.md § Code Examples "Preuve positive" : `.planning/phases/01-test/` avec 2
`*-PLAN.md` et 0, 1 ou 2 `*-SUMMARY.md` selon le cas (0 = `Planned` → script doit rendre `0`, PAS `1`
— c'est le garde-fou explicite du Pitfall 3 ; 1 = `In Progress` → `1` ; 2 = `Executed` → `0`). Cas
minimum : C0 aucune phase (dossier vide) → exit 0 ; C1 une phase `In Progress` → exit 1, message
contenant "ADR-069" et le numéro de phase ; C2 une phase `Planned` (0 summary) → exit 0 (PAS 1 — cas
discriminant du Pitfall 3, à commenter explicitement dans le test) ; C3 toutes phases `Executed`/`Complete`
→ exit 0 ; C4 répertoire hors dépôt/`gsd-tools` introuvable (`GSD_TOOLS=/inexistant` forcé) → exit 2 ;
C5 argument inconnu → exit 64.

MUT-1 (mutation de la fixture C1) : `empreinte()` = `find "$D" | LC_ALL=C sort` avant/après ; ajouter
le `*-SUMMARY.md` manquant fait basculer `In Progress` → `Executed` ; `cmp -s` doit prouver que
l'empreinte a changé (sinon `ko "mutant NON OPPOSABLE"`) ; puis prouver que le script rend `1` avant
et `0` après.

MUT-2 (mutation du SCRIPT, jamais l'original) : copier `check-planning-not-inflight.sh` dans
`$TMP/mutants/`, neutraliser par `awk` la condition `select(.status=="In Progress")` (ex. remplacer
par une condition toujours fausse `select(false)`), `cmp -s "$MUTANT" "$SCRIPT"` doit prouver une
différence (sinon `ko`), `bash -n "$MUTANT"` doit rester syntaxiquement valide, puis prouver que le
mutant rend `0` sur la fixture C1 (où l'original rend `1`) et que l'original restauré rend de nouveau
`1` sur la même fixture.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh</automated>
    <fails_when>exit non nul, ou une ligne `PASS=` avec `FAIL=` différent de 0, ou absence des libellés "MUT-1" et "MUT-2" tous deux suivis de "TUÉ"/"✓" dans la sortie</fails_when>
    <automated>bash -n plugin/conductor/scripts/check-planning-not-inflight.sh</automated>
    <fails_when>exit non nul (erreur de syntaxe)</fails_when>
    <automated>grep -n 'GSD_TOOLS\|command -v gsd-tools\|CLAUDE_CONFIG_DIR' plugin/conductor/scripts/check-planning-not-inflight.sh | wc -l | tr -d ' '</automated>
    <fails_when>la commande rend `0` ou moins de `3` (les trois maillons de la cascade doivent chacun apparaître au moins une fois)</fails_when>
    <automated>grep -E "pwd\)|show-toplevel|\./gsd-core" plugin/conductor/scripts/check-planning-not-inflight.sh | wc -l | tr -d ' '</automated>
    <fails_when>la commande rend un nombre différent de `0` — tout hit est une régression de sécurité (candidat cwd/repo-relatif réintroduit)</fails_when>
  </verify>
  <acceptance_criteria>
    - l'en-tête du script énumère les 4 codes de sortie (0/1/2/64), aucun implicite
    - le filtre `jq` contient littéralement `.status=="In Progress"` et ne contient à aucun endroit une comparaison `plans` > `summaries` recalculée
    - `test-check-planning-not-inflight.sh` couvre au moins les 6 cas C0-C5 ET les deux mutations MUT-1/MUT-2, toutes deux tuées
    - le cas C2 (`Planned`, 0 summary) rend explicitement `0`, avec un commentaire dans le test citant "Pitfall 3" ou l'inégalité `plans > summaries` comme piège évité
  </acceptance_criteria>
  <reversibility rating="reversible">Script + suite nouveaux, aucun fichier existant modifié — retirer ou réécrire ce script n'affecte rien d'autre que le skill de la Task 2 qui l'invoque (couplage par chemin documenté, pas par duplication de logique).</reversibility>
  <done>check-planning-not-inflight.sh existe, énumère ses 4 codes, porte la cascade de résolution sûre (aucun candidat cwd-relatif), filtre strictement sur "In Progress", et sa suite mutation-provable tue MUT-1 et MUT-2.</done>
</task>

<task type="auto">
  <name>Task 2 : skill vf-split-planning + trampoline + doc-contract (WSCH-03, D-08, D-10, D-12)</name>
  <files>plugin/conductor/skills/vf-split-planning/SKILL.md, plugin/commands/vf-split-planning.md, plugin/conductor/scripts/tests/test-vf-split-planning.sh</files>
  <read_first>
    - plugin/conductor/skills/vf-calibrate/SKILL.md (intégral — patron exact : frontmatter `description` au gabarit "Utiliser quand… «formulations»… ✘ pas pour X → /autre", bloc Mission + Iron Law, séquence numérotée déléguant à un script via `.claude/scripts/<script>.sh`, clôture Garde-fous + Références on-demand)
    - plugin/commands/vf-new-lab.md (intégral, 17 lignes — patron trampoline exact, cité nommément par D-10)
    - plugin/commands/vf-calibrate.md (intégral — second exemple du même patron)
    - plugin/conductor/scripts/tests/test-vf-new-lab.sh (patron `has()`/`ok()` à reproduire à l'identique pour ce nouveau skill)
    - plugin/conductor/scripts/tests/test-skill-doc-paths.sh:1-30 (gate générique déjà couvrant automatiquement tout nouveau SKILL.md sous `plugin/conductor/skills/` — AUCUNE action requise sur ce fichier, seulement respecter la convention `.claude/scripts/<script>.sh` dans le texte écrit, jamais `plugin/conductor/scripts/<script>.sh`)
    - 41.2-CONTEXT.md § D-10 (emplacement, justification), § D-12 (différence avec gsd-workstreams amont), § D-06 (nom par défaut = milestone courant, wording laissé à l'implémentation), § D-08 (vocabulaire)
  </read_first>
  <action>
Créer `plugin/conductor/skills/vf-split-planning/SKILL.md`. Frontmatter : `name: vf-split-planning`,
`description:` au gabarit `vf-calibrate` — "Utiliser quand…" avec au moins deux formulations
françaises réelles ("paralléliser deux chantiers", "un deuxième sujet doit avancer sans attendre le
premier"), puis "✘ pas pour créer un lab qui n'existe pas encore (le premier compartiment se pose
automatiquement) → /vf-new-lab" et une clause nommant explicitement le skill amont `gsd-workstreams`
(différence D-12 : celui-ci ne couvre pas la bascule d'un lab déjà démarré et expose des verbes
inexistants côté moteur). Corps : titre "# vf-split-planning — Bascule vers plusieurs sujets
parallèles", bloc `> **Mission**` (une phrase) + `> **Iron Law** : « Refuser tant qu'une phase est en
vol — jamais une exception. »` (D-11/ADR-069, jamais assoupli). Séquence numérotée : (1) invoquer
`.claude/scripts/check-planning-not-inflight.sh` (**chemin exact — jamais `plugin/conductor/scripts/...`**,
le gate générique `test-skill-doc-paths.sh` le vérifie automatiquement) ; (2) code de sortie ≠ 0 →
relayer le message du script **tel quel**, jamais reformulé ni recalculé (anti-pattern nommé en
RESEARCH.md — ne jamais recalculer le statut côté VibeFlow) ; (3) code 0 → appeler `gsd-tools
workstream create <nom> --migrate-name <nom>` avec `<nom>` par défaut = nom du milestone courant
(dérivé, pas redemandé) — lire le champ `created`/`error` du JSON avant de confirmer (même garde-fou
d'idempotence que le hook de 41.2-01) ; (4) confirmer dans le registre d'usage, wording exact laissé
à l'implémentation, jamais le mot jargon interdit dans ce qui est montré.

<!-- planner-discipline-allow: workstream -->
<!-- planner-discipline-allow: compartiment -->
Entourer la confirmation finale effectivement montrée à l'utilisateur des mêmes marqueurs
`<!-- hook-planning-shown:start -->`/`<!-- hook-planning-shown:end -->` que le plan 41.2-01 (même
convention de scope pour le test doc-contract ci-dessous — les mentions de "workstream" dans les
sections Garde-fous/Références on-demand, doctrine technique interne, restent hors de ces marqueurs
et sont légitimes).

Section "## Garde-fous" : ADR-069 non négociable (aucune exception, aucun flag de contournement) ;
ne jamais recalculer le statut "en vol" par un autre moyen que le script ; ne jamais revalider le nom
du compartiment côté VibeFlow (le moteur le fait déjà — `toWorkstreamSlug`, V5 marginal du domaine de
sécurité de RESEARCH.md). Section "## Références (on-demand)" : `.claude/scripts/check-planning-not-inflight.sh`,
`plugin/dev-orchestrator/references/workstreams.md` (lecture seule, propriété de la Phase 41.1).

Créer le trampoline `plugin/commands/vf-split-planning.md`, copie structurelle exacte de
`vf-new-lab.md` : frontmatter `description` (une phrase résumant le geste) + `argument-hint`
optionnel, paragraphe "Invoque le skill **`vf-split-planning`** ... : $ARGUMENTS", un paragraphe
résumant sans logique nouvelle, clôturé par le paragraphe standard "Si le module `conductor` n'est
pas installé, lance d'abord `vibeflow-install` (ou indique `/vibeflow-install`)." recopié verbatim.

Créer `plugin/conductor/scripts/tests/test-vf-split-planning.sh`, même harness `has()`/`ok()` que
`test-vf-new-lab.sh` (`SKILL="$SCRIPT_DIR/../skills/vf-split-planning/SKILL.md"`). Assertions
positives : présence de `/vf-new-lab` en contre-exemple, présence du nom `gsd-workstreams` (D-12),
présence littérale de `.claude/scripts/check-planning-not-inflight.sh`, absence littérale de
`plugin/conductor/scripts/check-planning-not-inflight.sh` (compte `grep -c` doit rendre `0`),
présence de "ADR-069". Assertion négative scopée (même technique que 41.2-01 Task 2) : extraire le
texte entre les marqueurs `hook-planning-shown` et vérifier `grep -ciE 'workstream|compartiment'` = 0
sur cet extrait seul, PAS sur le fichier entier (Garde-fous/Références y font légitimement
référence). Densité `wc -l ≤ 500`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-vf-split-planning.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`, ou `FAIL=` différent de 0</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-skill-doc-paths.sh</automated>
    <fails_when>exit non nul (une commande documentée pointe `plugin/*/scripts/` au lieu de `.claude/scripts/`)</fails_when>
    <automated>grep -c 'gsd-workstreams' plugin/conductor/skills/vf-split-planning/SKILL.md</automated>
    <fails_when>la commande rend `0` (distinction D-12 absente du texte)</fails_when>
  </verify>
  <acceptance_criteria>
    - `vf-split-planning/SKILL.md` cite `.claude/scripts/check-planning-not-inflight.sh` littéralement et ne cite jamais `plugin/conductor/scripts/check-planning-not-inflight.sh`
    - le trampoline `vf-split-planning.md` porte la même phrase de clôture ("Si le module `conductor` n'est pas installé…") que `vf-new-lab.md` et `vf-calibrate.md`, vérifiable par `diff` sur cette seule ligne
    - `wc -l plugin/conductor/skills/vf-split-planning/SKILL.md` ≤ 500
    - la séquence documente explicitement la lecture du champ `created`/`error` avant confirmation (idempotence)
  </acceptance_criteria>
  <reversibility rating="reversible">Skill + trampoline + test nouveaux, aucun fichier existant modifié — supprimer ce skill n'affecte aucun autre skill du module (couplage uniquement par appel de script documenté).</reversibility>
  <done>vf-split-planning existe, encadre check-planning-not-inflight.sh sans jamais recalculer sa décision, appelle la même primitive moteur que 41.2-01, et son wording montré ne cite jamais le jargon interdit — prouvé par un test doc-contract scopé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Invocation de `gsd-tools` comme sous-processus | Le script résout un chemin d'exécutable puis l'invoque — c'est la même classe de surface que `dag.sh` a dû fermer (RCE via un candidat de résolution relatif au répertoire de travail) |
| Sortie JSON du moteur → décision bloquante | Le JSON de `gsd-tools progress` pilote un refus/acceptation ; jamais parsé par une regex maison, toujours par `jq` sur le champ structuré |
| Nom de compartiment fourni par l'utilisateur → `workstream create` | Le nom n'est jamais revalidé côté VibeFlow — transmis tel quel, le moteur (`toWorkstreamSlug`) est la seule autorité de validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.2-SC | Tampering | Résolution du binaire `gsd-tools` dans `check-planning-not-inflight.sh` | high | mitigate | Cascade à trois niveaux portée de `dag.sh:149-187` (`resolve_gsd_tools_cmd`), **aucun candidat cwd/repo-relatif** — la classe de RCE qu'un fichier tracké malveillant à un chemin relatif pourrait exploiter est structurellement absente ; vérifiée par grep négatif dédié dans Task 1 (aucune occurrence de `pwd)`, `show-toplevel`, `./gsd-core` concaténée en chemin exécutable) |
| T-41.2-04 | Tampering | Écriture concurrente / divergence de planning pendant une bascule en cours | high | mitigate | La précondition ADR-069 EST la mitigation : `check-planning-not-inflight.sh` refuse tant qu'une phase est `In Progress`, fermant la fenêtre où Git ne signalerait pas la divergence (docs/ADR.md §ADR-069, "La condition dure — interdiction opposable") |
| T-41.2-05 | Tampering | Appel double du skill (retry) créant deux compartiments ou un état incohérent | medium | mitigate | Déjà couvert côté moteur : `cmdWorkstreamCreate` détecte `already_exists` et rend `{created:false}` sans planter ; le skill DOIT lire ce champ (documenté explicitement dans la séquence) plutôt que de supposer le succès |
| T-41.2-06 | Tampering | Mutation-vert-à-vide de la suite de test (un gate qui ne peut jamais rendre rouge) | medium | mitigate | MUT-1 (fixture) et MUT-2 (script en copie temporaire, jamais l'original) prouvés tués, patron `cmp` sur empreinte — jamais `diff` (QUAL-01) |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh` → 0 échec, MUT-1/MUT-2 tués
- `bash plugin/conductor/scripts/tests/test-vf-split-planning.sh` → 0 échec
- `bash plugin/conductor/scripts/tests/test-skill-doc-paths.sh` → 0 échec (convention `.claude/scripts/` respectée)
- `bash -n plugin/conductor/scripts/check-planning-not-inflight.sh` → syntaxe valide
- Grep négatif : aucun candidat cwd/repo-relatif dans la cascade de résolution
</verification>

<success_criteria>
WSCH-03 est vrai : un lab déjà démarré peut basculer via un geste outillé qui refuse par machine
(jamais par prose) tant qu'une phase est en vol, sa précondition est prouvée mutation-provable, et le
skill qui l'encadre ne réimplémente ni ne recalcule rien du moteur.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `plugin/conductor/scripts/check-planning-not-inflight.sh` (nouveau script)
- `plugin/conductor/scripts/tests/test-check-planning-not-inflight.sh` (nouvelle suite mutation-provable)
- `plugin/conductor/skills/vf-split-planning/SKILL.md` (nouveau skill)
- `plugin/commands/vf-split-planning.md` (nouveau trampoline)
- `plugin/conductor/scripts/tests/test-vf-split-planning.sh` (nouvelle suite doc-contract)

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-02-SUMMARY.md` when done
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 08
lot: L8 — provenance serveur d'un merge sans contournement, contournement réel et tracé sur la PR inoffensive (temps ii), mesure M-3
type: execute
wave: 8
depends_on: ["41-07"]
autonomous: false
requirements: [PROT-01, PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition dure : dernier `M1-VERDICT: SUR` ; PR-R, PR-BASELINE et PR-SENTINELLE fermées sans merge ; PR-P seule ouverte, tête P1"
    - "Support du contournement réel (D-M7, décision technique validée par le manager le 2026-09-17) : PR-P, contenu inoffensif. La PR rouge n'est JAMAIS contournée ni mergée"
    - "Provenance serveur (point 2 du tour 1), mesurée sur ce support inoffensif : juste après la publication de P2, `samuel-neveugall` appelle `PUT /repos/{owner}/{repo}/pulls/{n}/merge` (doc REST : aucun paramètre de contournement) avec `sha` = P2. `rest=refuse` n'est retenu que si la réponse non-200 cite la violation d'une règle — expressions reconnues : « rule violation », « ruleset », « protected branch », « required status », « status check », « approving review », « code owner review », « review is required », « review required » (un simple « Review and try the merge again » n'en est pas une) ; tout autre non-200 — par exemple un 405 pendant que la mergeabilité est encore en calcul — donne `rest=indeterminee`, et le checkpoint relit alors `mergeable_state` jusqu'à stabilité avant de retenter (deux nouvelles tentatives au plus) ; un 200 établit que le serveur merge pour un compte write sans geste explicite — écart à D-09, mène à la décision"
    - "M-3 mesuré : si le REST a refusé, `gh pr merge --merge --admin` est joué ; sa sortie, son résultat et les règles effectivement contournées lues dans la rule suite du commit de merge sont consignés"
    - "Le contournement porte sur deux règles non satisfaites au même instant : revue code owner manquante ET checks requis EN COURS après publication de P2 — pas rouges. La nuance est consignée (`TRACE-II-NUANCE:`), reprise telle quelle au SUMMARY de ce plan et dans ADR-072 § Mesures (41-10), jamais présentée comme un contournement de PR rouge (point 12 du tour 1)"
    - "Trace (ii) : une rule suite de résultat `bypass` sur `refs/heads/main` dont `after_sha` est le commit de merge ; relue par l'agent via l'API quand le compte write y a accès (`relue=oui`), sinon collée par le compte lecteur et marquée `relue=non_relue`. Chaque règle est `prouvee` si elle figure en échec dans les évaluations de cette suite, `non_prouvee` sinon — jamais supposée"
    - "Verdict : CONFORME (REST refusé, `--admin` mergé, suite `bypass`, deux règles prouvées, checks en cours) ; PARTIEL (mergé mais provenance REST `indeterminee`, une règle non prouvée, suite absente ou aucun check en cours) ; ECART (REST mergé sans contournement, ou aucun merge). PARTIEL et ECART arrêtent le plan sur la Task 4, qui dit quoi faire de PR-P et des rulesets et rend réel le repli « trace non prouvée » de D-M7 sans bloquer indéfiniment le préalable « aucune PR ouverte » de 41-09"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`MAIN-AVANT-PR-P-II:`, `M3:`, `M3-REST-MSG:`, `M3-ADMIN-MSG:`, `PROVENANCE-SERVEUR:`, `TRACE-II:`, `TRACE-II-MSG:`, `TRACE-II-NUANCE:`, `MAIN-APRES-PR-P:`, `M3-VERDICT:`"
  key_links:
    - "`TRACE-II:` et `PROVENANCE-SERVEUR:` alimentent PROT-01 (critère 2), PROT-04 et ADR-072 § Mesures (41-10) ; `M3-VERDICT:` / `M3-DECISION:` conditionnent 41-09"
---

<objective>
Établir d'où vient le refus d'un merge sans contournement (client ou serveur), puis produire un
contournement réel, explicite et inoffensif, et lire sa trace dans GitHub.

Purpose: temps (ii) du critère 2 (D-09) et de la revue code owner (D-M7) ; provenance du refus ;
inconnu M-3.
Output: provenance serveur consignée ; PR-P mergée en contournement (ou décision humaine) ; trace
consignée règle par règle.

Budget (point 9 du tour 1, justification explicite) : quatre tâches, dont la quatrième est un
checkpoint de décision CONDITIONNEL, sans exécution quand `M3-VERDICT: CONFORME`. La scinder ferait
porter la mesure et la décision par deux exécuteurs sans contexte commun. Fichiers : un seul.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-07-SUMMARY.md

Sources lues le 2026-09-17 : `docs.github.com/en/rest/pulls/pulls` — `PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge`
(corps `commit_title`, `commit_message`, `sha`, `merge_method` ; 200 mergé, 405 « if merge cannot be
performed », 409 si `sha` ne correspond pas ; aucun paramètre de contournement) ;
`docs.github.com/en/rest/repos/rule-suites` — `GET /repos/{owner}/{repo}/rulesets/rule-suites`
(`ref`, `time_period`, `actor_name`, `rule_suite_result` pass/fail/bypass/all),
`GET …/rule-suites/{rule_suite_id}` (`after_sha`, `result`, `rule_evaluations[]` avec `rule_type`,
`result`) ; couverture des merges de PR non précisée — d'où la lecture ciblée sur `after_sha`.
`cli.github.com/manual/gh_pr_merge` (`--admin`).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : préalables relus, commandes du REST, du contournement et de la lecture de trace préparées</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`M1-VERDICT:`, `PR-R-FERMEE:`, `PR-CO-FERMEES:`, `PR-P:`, `PR-P-COMMITS:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin`. Relire : dernier `M1-VERDICT: SUR` ; PR-R `CLOSED`
non mergée ; PR-BASELINE et PR-SENTINELLE fermées ; PR-P `OPEN`, tête = P1 ; seule PR ouverte = PR-P ;
P2 a bien P1 pour parent. Consigner une section `## 41-08 — provenance serveur et contournement` et
`MAIN-AVANT-PR-P-II: <sha d'origin/main>`.

Imprimer les commandes de la Task 2, en y indiquant le compte lecteur des rule suites d'après
`ACCES-RULE-SUITES:` (`samuel-neveugall` si `ok`, sinon `picmakpro`). Commit local sur B, message
« docs(41-08): préalables du contournement tracé relus ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); v=$(awk "\$1==\"M1-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); R=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); rs=$(gh pr view "$R" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); ps=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); ph=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); P1=$(val PR-P-COMMITS: p1); P2=$(val PR-P-COMMITS: p2); pp=$(git rev-parse "$P2^"); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); ma=$(awk "\$1==\"MAIN-AVANT-PR-P-II:\" {print \$2; exit}" "$PH/41-PREUVES.md"); echo "prealables ii : identite=$u m1=$v pr_r=$rs pr_p=$ps tete_p1=$([ "$ph" = "$P1" ] && echo oui || echo non) parent_p2_p1=$([ "$pp" = "$P1" ] && echo oui || echo non) prs_ouvertes=$op main_consigne=$([ "$ma" = "$(git rev-parse origin/main)" ] && echo oui || echo non)"; [ "$u" = samuel-neveugall ] && [ "$v" = SUR ] && [ "$rs" = CLOSED:false ] && [ "$ps" = OPEN ] && [ "$ph" = "$P1" ] && [ "$pp" = "$P1" ] && [ "$op" -eq 1 ] && [ "$ma" = "$(git rev-parse origin/main)" ]'</automated>
    <fails_when>exit non nul ; ou dernier `M1-VERDICT` différent de `SUR` ; ou PR-R non fermée ou mergée ; ou PR-P non ouverte ou tête différente de P1 ; ou P2 non fils de P1 ; ou une autre PR ouverte (PR jetables de 41-06 comprises) ; ou `MAIN-AVANT-PR-P-II:` absente ou périmée</fails_when>
  </verify>
  <acceptance_criteria>
    - M-1 SUR, PR-R et PR jetables fermées non mergées, PR-P seule ouverte sur P1, P2 prêt
    - `main` relevée avant le contournement
  </acceptance_criteria>
  <done>La provenance et le contournement peuvent être joués sur le seul support inoffensif.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains — publier P2 ; merge REST sans contournement ; si refusé, `--admin` pendant que les checks tournent ; lire la trace</name>
  <action>Samuel, compte `samuel-neveugall`, publie P2 puis, sans attendre la CI, tente le merge par l'API REST sans aucune option de contournement ; si le serveur refuse, merge avec `--admin` ; puis le compte lecteur lit les rule suites.</action>
  <instructions>
Compte qui agit pour 1-6 : `samuel-neveugall`. Enchaîner 1 à 4 sans attendre la CI (elle dure
plusieurs minutes).

1. `git push origin <p2>:refs/heads/preuve/41-contournement`
2. Relevé juste avant :
   `gh pr view <n> --repo picmakpro/vibeflow-os --json headRefOid,mergeStateStatus,reviewDecision,statusCheckRollup --jq '{tete: .headRefOid, etat: .mergeStateStatus, revue: .reviewDecision, checks_en_cours: ([.statusCheckRollup[] | select((.status // "COMPLETED") != "COMPLETED")] | length)}'`
   → la tête doit être `<p2>` et `checks_en_cours` supérieur à 0 ; sinon relancer 2 quelques secondes
   plus tard. Si les checks sont déjà tous terminés : NE PAS poursuivre, le signaler.
3. Mergeabilité stable : `gh api repos/picmakpro/vibeflow-os/pulls/<n> --jq .mergeable_state`, répété à
   quelques secondes d'intervalle jusqu'à deux lectures consécutives identiques et différentes de
   `unknown` → copier la valeur. Puis merge par l'API REST, SANS contournement (il n'en existe pas sur
   cet endpoint) :
   `gh api -X PUT repos/picmakpro/vibeflow-os/pulls/<n>/merge -f merge_method=merge -f sha=<p2> --include`
   → copier la sortie INTÉGRALE, ligne de statut `HTTP/…` comprise.
   Si le statut n'est pas `200` ET que le corps ne cite aucune violation de règle (« rule violation »,
   « ruleset », « protected branch », « required status », « status check », « approving review »,
   « code owner review », « review is required », « review required ») : relire `mergeable_state` jusqu'à stabilité comme
   ci-dessus, puis rejouer le même appel ; deux nouvelles tentatives au plus, chaque sortie copiée.
4. SEULEMENT si la dernière tentative de 3 n'a pas rendu `200` : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge --admin`
   → copier la sortie intégrale. Si 3 a rendu `200` : ne pas jouer 4, noter « non tenté ».
   Si 4 refuse : copier le message et s'arrêter là (aucun autre moyen, aucune case d'interface).
5. Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
6. Seulement si la PR est `MERGED` : `git push origin --delete preuve/41-contournement`. Sinon PR-P
   reste ouverte : la Task 4 décide de son sort.
7. Lecture de la trace par le compte indiqué par la Task 1 (`gh auth switch --user …` si `picmakpro`,
   retour à `samuel-neveugall` ensuite, témoin `gh api user --jq .login`), seulement si la PR est
   `MERGED` :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?ref=refs/heads/main&time_period=hour&rule_suite_result=bypass" --jq '.[] | {id, actor_name, after_sha, result}'`
   puis, pour l'id dont `after_sha` est le commit de merge :
   `gh api repos/picmakpro/vibeflow-os/rulesets/rule-suites/<id> --jq '{id, actor_name, after_sha, result, evaluations: [.rule_evaluations[] | {rule_type, result, enforcement}]}'`
  </instructions>
  <verification>La Task 3 relit l'état et le contenu du merge, la CI du push de merge sur `main`, recalcule le statut HTTP depuis la sortie REST, et relit la suite si le compte write y a accès.</verification>
  <resume-signal>Coller les sorties intégrales de 2, 3, 4 (ou « non tenté »), 5, 6 et 7, puis écrire « contournement joué ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : consignation de la provenance serveur, de M-3 et de la trace, relecture API, verdict</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Consigner :
- `M3: tete=<p2> etat_avant=<> revue_avant=<> checks_en_cours_avant=<k> rest=<merge|refuse|indeterminee> rest_http=<code lu dans la dernière sortie REST> rest_tentatives=<k> mergeable_state_avant=<valeur stable> admin=<merge|refuse|non_tente> resultat=<merged|refuse> merge=<sha|aucun> compte=samuel-neveugall` ;
- `M3-REST-MSG:` (sortie intégrale de la DERNIÈRE tentative REST, une ligne) et `M3-ADMIN-MSG:` (sortie de `--admin`, ou `sans_objet`) ;
- `PROVENANCE-SERVEUR: support=PR-P rest=<refuse|merge|indeterminee> http=<code>` — `refuse` SEULEMENT si la
  réponse non-200 cite une violation de règle, `indeterminee` pour tout autre non-200 ; c'est la seule mesure de refus
  côté serveur de la phase ; les refus de 41-06 et 41-07 gardent leur provenance propre ;
- `TRACE-II: suite=<id|absente> result=<…|absente> after_sha=<sha|aucun> acteur=<> lu_par=<compte> relue=<oui|non_relue> pull_request=<prouvee|non_prouvee> required_status_checks=<prouvee|non_prouvee>`
  et `TRACE-II-MSG:` (JSON collé ou relu, une ligne) — `relue=oui` seulement si l'agent a lui-même
  relu la suite via l'API (compte write avec accès) ; une règle est `prouvee` si elle figure en
  `fail` dans les évaluations d'une suite `bypass` dont `after_sha` est le commit de merge ;
- `TRACE-II-NUANCE: contournement_reel_sur_checks_en_cours_pas_sur_une_PR_rouge` ;
- `MAIN-APRES-PR-P: merge=<sha|aucun> fichiers=<liste|aucun> ci_main=<k>/4|sans_objet` — attendre la
  fin des runs du push de merge avant d'écrire `ci_main` (sondage, jamais un vert supposé) ;
- `M3-VERDICT: <CONFORME|PARTIEL|ECART>` selon la règle du frontmatter.
Aucun nouveau geste chez GitHub dans cette tâche, quel que soit le résultat. Commit local sur B,
message « docs(41-08): provenance serveur, M-3 et trace du contournement consignés (D-02, D-09 —
arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-P absente"; exit 1;; esac; P2=$(val PR-P-COMMITS: p2); git fetch -q origin; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); res=$(val M3: resultat); mc=$(val M3: merge); rest=$(val M3: rest); rh=$(val M3: rest_http); adm=$(val M3: admin); cv=$(val M3: checks_en_cours_avant); hc=$(awk "\$1==\"M3-REST-MSG:\" { if (match(\$0, /HTTP\/[0-9.]+ [0-9][0-9][0-9]/)) { s=substr(\$0, RSTART, RLENGTH); n=split(s, a, \" \"); print a[n] } ; exit }" "$PH/41-PREUVES.md"); restcalc=$(if [ "$hc" = 200 ]; then echo merge; else awk "\$1==\"M3-REST-MSG:\" { l=tolower(\$0); if (index(l, \"rule violation\") || index(l, \"ruleset\") || index(l, \"protected branch\") || index(l, \"required status\") || index(l, \"status check\") || index(l, \"approving review\") || index(l, \"code owner review\") || index(l, \"review is required\") || index(l, \"review required\")) r=\"refuse\"; else r=\"indeterminee\"; print r; exit }" "$PH/41-PREUVES.md"; fi); pv=$(val PROVENANCE-SERVEUR: rest); T=$(mktemp -d); git ls-remote --heads origin preuve/41-contournement > "$T/lr"; bd=$(awk "END{print NR}" "$T/lr"); if [ "$st" = MERGED ]; then M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); git merge-base --is-ancestor "$M" origin/main; git diff --name-only "$M^1" "$M" > "$T/fl"; fl=$(awk "BEGIN{ORS=\"\"} NR>1{print \",\"} {print}" "$T/fl"); p2in=$(git merge-base --is-ancestor "$P2" "$M" && echo oui || echo non); ci=$(gh api "repos/picmakpro/vibeflow-os/commits/$M/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | map(.name) | unique | length"); echo "etat merge : PR-P=$st merge=$M fichiers=$fl p2_inclus=$p2in ci_main_jobs_verts=$ci branche_distante=$bd | M3 resultat=$res merge=$mc rest=$rest (recalcule $restcalc, http $rh/$hc) admin=$adm checks_en_cours_avant=$cv"; [ "$res" = merged ] && [ "$mc" = "$M" ] && [ "$fl" = .github/rulesets/README.md ] && [ "$p2in" = oui ] && [ "$ci" -eq 4 ] && [ "$bd" -eq 0 ] && [ -n "$hc" ] && [ "$rh" = "$hc" ] && [ "$rest" = "$restcalc" ] && [ "$pv" = "$rest" ] && { [ "$rest" != merge ] || [ "$adm" = non_tente ]; } && [ -n "$cv" ]; else echo "etat merge : PR-P=$st | M3 resultat=$res merge=$mc rest=$rest (recalcule $restcalc, http $rh/$hc) admin=$adm"; ap=$(val MAIN-APRES-PR-P: merge); [ "$st" = OPEN ] && [ "$res" = refuse ] && [ "$mc" = aucun ] && [ "$ap" = aucun ] && [ -n "$hc" ] && [ "$rh" = "$hc" ] && [ "$rest" != merge ] && [ "$rest" = "$restcalc" ] && [ "$pv" = "$rest" ] && { [ "$adm" = refuse ] || [ "$adm" = non_tente ]; }; fi'</automated>
    <fails_when>exit non nul ; chemin MERGED : merge consigné différent du merge relu, fichiers atterris autres que le seul `.github/rulesets/README.md`, P2 non inclus, CI du push de merge non verte (attendre la fin avant d'écrire), branche distante encore présente, ou `M3-REST-MSG:` sans ligne de statut HTTP, ou `rest`/`rest_http` différents de la dernière sortie REST (200 = merge ; non-200 citant une violation de règle = refuse ; tout autre non-200 = indeterminee), ou `PROVENANCE-SERVEUR:` différente de `M3:`, ou `--admin` joué alors que le REST avait déjà mergé ; chemin OPEN : consignation autre que `resultat=refuse merge=aucun`, `MAIN-APRES-PR-P: merge=aucun`, ou REST consigné `merge`. Ces rouges sont des erreurs de CONSIGNATION, corrigées ici — jamais par un nouveau geste chez GitHub</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; res=$(val M3: resultat); M=$(val M3: merge); r=$(val TRACE-II: result); a=$(val TRACE-II: after_sha); id=$(val TRACE-II: suite); pr=$(val TRACE-II: pull_request); rc=$(val TRACE-II: required_status_checks); lu=$(val TRACE-II: lu_par); rl=$(val TRACE-II: relue); acc=$(val ACCES-RULE-SUITES: samuel-neveugall); msg=$(awk "\$1==\"TRACE-II-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); ok=oui; case "$pr" in (prouvee|non_prouvee) ;; (*) ok=non;; esac; case "$rc" in (prouvee|non_prouvee) ;; (*) ok=non;; esac; if [ "$res" = merged ]; then [ "$a" = "$M" ] || [ "$id" = absente ] || ok=non; [ -n "$lu" ] || ok=non; [ "$msg" -eq 1 ] || ok=non; if [ "$acc" = ok ] && [ "$id" != absente ]; then [ "$rl" = oui ] || ok=non; api=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$id" --jq "[.result, .after_sha, (([.rule_evaluations[] | select(.result == \"fail\") | .rule_type] | index(\"pull_request\") != null) | tostring), (([.rule_evaluations[] | select(.result == \"fail\") | .rule_type] | index(\"required_status_checks\") != null) | tostring)] | join(\" \")"); set -- $api; [ "$1" = "$r" ] && [ "$2" = "$a" ] && { { [ "$3" = true ] && [ "$pr" = prouvee ]; } || { [ "$3" = false ] && [ "$pr" = non_prouvee ]; }; } && { { [ "$4" = true ] && [ "$rc" = prouvee ]; } || { [ "$4" = false ] && [ "$rc" = non_prouvee ]; }; } || ok=non; else [ "$rl" = non_relue ] || ok=non; fi; [ "$id" != absente ] || { [ "$pr" = non_prouvee ] && [ "$rc" = non_prouvee ]; } || ok=non; else [ "$id" = absente ] && [ "$pr" = non_prouvee ] && [ "$rc" = non_prouvee ] && [ "$rl" = non_relue ] || ok=non; fi; nu=$(awk "\$1==\"TRACE-II-NUANCE:\" && index(\$0, \"checks_en_cours\") && index(\$0, \"pas_sur_une_PR_rouge\") {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "trace ii : resultat_merge=$res suite=$id result=$r after_sha_est_merge=$([ "$a" = "$M" ] && echo oui || echo non) pull_request=$pr required_status_checks=$rc lu_par=$lu relue=$rl acces_write=$acc message=$msg nuance=$nu coherence=$ok"; [ "$ok" = oui ] && [ "$nu" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `coherence=non` — statut de règle autre que `prouvee`/`non_prouvee` ; sur un merge : suite dont `after_sha` n'est pas le commit de merge, lecteur non nommé, JSON de trace non capturé, `relue=oui` alors que le compte write n'a pas l'accès (ou l'inverse), ou valeurs consignées différentes de la relecture API (résultat, `after_sha`, règles en échec) ; sans merge : trace autre que `suite=absente`, `non_prouvee` pour les deux règles, `relue=non_relue` ; ou `nuance=` différent de 1 (la ligne `TRACE-II-NUANCE:` dit que le contournement réel a porté sur des checks en cours, pas sur une PR rouge). Un `non_prouvee` cohérent NE rougit PAS cette vérification : il est porté par le verdict</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; rest=$(val M3: rest); adm=$(val M3: admin); res=$(val M3: resultat); cv=$(val M3: checks_en_cours_avant); r=$(val TRACE-II: result); pr=$(val TRACE-II: pull_request); rc=$(val TRACE-II: required_status_checks); if [ "$rest" = merge ] || [ "$res" != merged ]; then att=ECART; elif [ "$rest" = refuse ] && [ "$adm" = merge ] && [ "$r" = bypass ] && [ "$pr" = prouvee ] && [ "$rc" = prouvee ] && [ "${cv:-0}" -gt 0 ]; then att=CONFORME; else att=PARTIEL; fi; v=$(awk "\$1==\"M3-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); echo "verdict M-3 et trace ii : rest=$rest admin=$adm resultat=$res result=$r pull_request=$pr required_status_checks=$rc checks_en_cours_avant=$cv attendu=$att consigne=$v"; [ "$v" = "$att" ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou verdict consigné différent du verdict recalculé (erreur de consignation, corrigée ici) ; ou `PARTIEL` / `ECART` — `--admin` refusé, merge REST accepté sans contournement explicite, provenance REST `indeterminee`, aucune suite `bypass`, une règle `non_prouvee`, ou aucun check en cours au moment du contournement : consigné tel quel, AUCUN nouveau geste, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - provenance serveur consignée, statut HTTP recalculé depuis la sortie REST
    - chemin mergé : seul le README atterri, CI de `main` verte, branche supprimée, trace `bypass` sur le commit de merge, règles `prouvee`/`non_prouvee` cohérentes avec la relecture API (ou `non_relue`)
    - chemin non mergé : PR-P ouverte, trace `absente`, les deux règles `non_prouvee`
    - `TRACE-II-NUANCE:` présente ; `M3-VERDICT` recalculé = consigné ; `CONFORME` pour poursuivre, sinon Task 4
  </acceptance_criteria>
  <done>D'où vient le refus est établi côté serveur, et le contournement réel — ou son absence — est consigné sans rien supposer.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `M3-VERDICT` ≠ CONFORME (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <decision>Le merge sans contournement n'est pas refusé côté serveur, ou le contournement n'a pas eu lieu, ou sa trace est incomplète : que faire de PR-P et des rulesets, et la phase poursuit-elle avec une trace déclarée non prouvée ?</decision>
  <context>
    Lire `M3:`, `M3-REST-MSG:`, `M3-ADMIN-MSG:`, `PROVENANCE-SERVEUR:`, `TRACE-II:`, `M3-VERDICT:`. Une
    provenance `indeterminee` signifie que le serveur a répondu non-200 sans citer de règle malgré une
    mergeabilité stable : le refus serveur n'est pas établi.
    Le plan 41-09 exige qu'aucune PR ne soit ouverte : si PR-P reste ouverte, 41-09 attend
    explicitement la résolution de PR-P ; aucune option ne le bloque sans le dire. D-M7 prévoit qu'« à
    défaut, la trace reste déclarée non prouvée, jamais supposée ».
  </context>
  <options>
    <option id="fermer-et-poursuivre">
      <name>PR-P OUVERTE : LA FERMER, DÉCLARER LA TRACE NON PROUVÉE, POURSUIVRE</name>
      <pros>Chemin réel du repli de D-M7 : Samuel ferme PR-P sans merge (`gh pr close <n> --repo picmakpro/vibeflow-os` puis `git push origin --delete preuve/41-contournement`) ; rulesets inchangés ; 41-09 peut démarrer ; ADR-072 et le ledger écrivent « non prouvée ».</pros>
      <cons>Le temps (ii) du critère 2 et de la revue code owner n'est pas prouvé.</cons>
    </option>
    <option id="laisser-ouverte-et-cadrer">
      <name>PR-P OUVERTE : LA LAISSER ET CADRER</name>
      <pros>Un autre geste de contournement peut être cadré sur le même support inoffensif.</pros>
      <cons>41-09 et la suite attendent explicitement la résolution de PR-P (précondition « aucune PR ouverte ») ; rien n'avance sans un nouvel arbitrage.</cons>
    </option>
    <option id="consigner-et-poursuivre">
      <name>PR-P MERGÉE : CONSIGNER ET POURSUIVRE</name>
      <pros>Les écarts mesurés (merge REST sans contournement explicite, règle non prouvée, checks déjà terminés) sont écrits tels quels dans ADR-072 § Mesures et le ledger ; 41-09 peut démarrer.</pros>
      <cons>Si le serveur merge sans geste explicite, D-09 ne tient qu'au pré-contrôle client : à acter comme arbitrage daté et attribué.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE</name>
      <pros>PR-P fermée si ouverte ; `picmakpro` repasse chaque ruleset en `disabled` (commande de `RETOUR-ARRIERE-ECRIT:`).</pros>
      <cons>`main` redevient protégée par rien ; la phase repasse par le cadrage.</cons>
    </option>
  </options>
  <resume-signal>Répondre « non déclenchée », « PR-P OUVERTE : LA FERMER… », « PR-P OUVERTE : LA LAISSER ET CADRER », « PR-P MERGÉE : CONSIGNER ET POURSUIVRE » ou « RETOUR ARRIÈRE ». Après la réponse, l'exécutant consigne `M3-DECISION: option=<id> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint> pr_p=<etat relu via l'API>` — jamais inféré, jamais une date recopiée d'ailleurs — et le commite sur B avec le message « docs(41-08): M3-DECISION consignée — arbitrage Samuel, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `M3-DECISION` sans ce canal et cette date rougit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contournement → `main` | seul contenu autorisé à entrer par contournement : le README de preuve |
| appel REST de merge → `main` | un merge accepté sans geste explicite contredirait D-09 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-30 | Tampering | contournement appliqué à un contenu nocif | high | mitigate | précondition PR-R et PR jetables fermées non mergées ; fichiers atterris vérifiés = README seul ; CI de `main` verte |
| T-41-31 | Repudiation | trace supposée ou lue sur la mauvaise suite | high | mitigate | suite choisie par `after_sha` = commit de merge ; statut par règle ; relecture API par l'agent quand il a l'accès, sinon `non_relue` |
| T-41-32 | Repudiation | contournement de checks rouges affirmé sans preuve | medium | mitigate | `checks_en_cours_avant` consigné ; `TRACE-II-NUANCE:` exigée ; formulation reprise au SUMMARY et dans ADR-072 |
| T-41-33 | Elevation of privilege | lecture de trace sous jeton admin par l'agent | medium | mitigate | lecture `picmakpro` = geste humain ; identité de l'agent revérifiée |
| T-41-51 | Repudiation | refus client présenté comme refus serveur | medium | mitigate | appel REST direct, statut HTTP recalculé depuis la sortie ; `PROVENANCE-SERVEUR:` séparée des provenances de 41-06 et 41-07 |
| T-41-52 | Denial of service | PR-P laissée ouverte bloquant 41-09 sans le dire | low | mitigate | options de la Task 4 explicites sur le sort de PR-P et l'attente de 41-09 ; `M3-DECISION:` consigne l'état relu de PR-P |
</threat_model>

<verification>
Provenance serveur mesurée ; contournement réel et inoffensif (ou décision humaine) ; trace lue sur la
bonne suite, relue ou marquée non relue, consignée règle par règle.
</verification>

<success_criteria>
Temps (ii) du critère 2 et de la revue code owner prouvés, ou déclarés `non_prouvee` règle par règle
sur décision ; M-3 et la provenance serveur mesurés.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-08-SUMMARY.md` when done — committé sur la branche B. Le SUMMARY reprend mot pour mot la nuance : « contournement réel sur des checks en cours, pas sur une PR rouge » (vérifiée par le plan 41-13).
</output>

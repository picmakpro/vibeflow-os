---
phase: 18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - plugin/dev-orchestrator/scripts/requirements-survival-detect.sh
  - plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh
  - plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh
  - plugin/dev-orchestrator/AGENT.md
  - .planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md
autonomous: false
requirements: [LEDG-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rejoué sur une fixture jetable portant l'archive `<jalon>-REQUIREMENTS.md` et un `.planning/REQUIREMENTS.md` absent, `restore-requirements-ledger.sh --write` produit un `.planning/REQUIREMENTS.md` dont la table de traçabilité PRÉSERVE tous les statuts verbatim, dont les items Pending portent `carried-from: v[X.Y]`, dont les items caducs n'apparaissent QUE dans l'archive, et dont une section `## Garanties` place les items livrés AVANT `## Traceability` (D-18-03, D-18-11, D-18-12, D-18-13, A-18-04)."
    - "Le script ne s'exécute JAMAIS sans confirmation humaine explicite : sans `--write`, il montre le diff proposé et n'écrit rien (ADR-031, D-18-06)."
    - "Le rattrapage est un post-traitement pur : il ne touche jamais à l'archive (lecture seule), ne réimplémente ni `complete-milestone` ni l'archivage ni la génération de ROADMAP (Iron Law 2, D-18-05)."
    - "La classification statut→destin utilise `vf_ledger_classify`, sourcée depuis la MÊME primitive que le gate du plan 18-01 — aucune logique de classement dupliquée."
    - "Le STUDY.md porte un encadré d'amendement daté qui périme son §7.2 sur la forme du roll-over et renvoie à CONTEXT.md (D-18-02) — sans réécrire le corps du §7.2."
  artifacts:
    - "plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh — geste de rattrapage, `--path`, `--write`, `--quiet`, `-h|--help`"
    - "plugin/dev-orchestrator/scripts/requirements-survival-detect.sh — étendu de `vf_ledger_classify <ligne_statut> <ligne_traçabilité>`"
    - "plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh — suite du rattrapage, fixtures des 3 destins + mode diff vs write"
    - "plugin/dev-orchestrator/AGENT.md — ligne `[ledger-absent]` ajoutée à la table des signaux de démarrage"
    - "STUDY.md — encadré d'amendement daté en tête du §7.2"
  key_links:
    - "restore-requirements-ledger.sh → requirements-survival-detect.sh : sourcing de `vf_ledger_state` (localiser l'archive et le jalon clos) ET `vf_ledger_classify` (trancher le destin de chaque exigence) — jamais de logique de classement locale au script."
    - "restore-requirements-ledger.sh → archive `.planning/milestones/<jalon>-REQUIREMENTS.md` : lecture seule stricte, jamais d'écriture, jamais de `git rm`."
    - "restore-requirements-ledger.sh → `.planning/REQUIREMENTS.md` : écriture UNIQUEMENT sous `--write`, jamais par défaut."
    - "AGENT.md ↔ check-requirements-survival.sh (plan 18-01) : le signal `[ledger-absent]` documenté dans la table de doctrine doit nommer EXACTEMENT le geste que le gate propose sur stdout."
---

<objective>
Livrer le geste de rattrapage : reconstituer `.planning/REQUIREMENTS.md` en roll-forward depuis
l'archive verbatim d'un jalon clos, sous confirmation humaine, en appliquant la table statut→destin
(D-18-11) via la primitive partagée du plan 18-01.

Purpose: c'est la moitié LEDG-01 de la phase — celle qui referme réellement le trou du STUDY §7.1.
Le plan 18-01 a prouvé que la détection tient ; ce plan prouve que la reconstitution tient, sur le
même socle. Sans lui, `[ledger-absent]` du gate pointe vers un geste qui n'existe pas.

Output: 1 script neuf, 1 extension de la primitive partagée (`vf_ledger_classify`), 1 suite de
tests neuve, 1 ligne de doctrine dans `AGENT.md`, 1 encadré d'amendement dans `STUDY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/18-CONTEXT.md
@.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/18-PATTERNS.md
@.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/18-01-PLAN.md
@.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/18-01-SUMMARY.md
@plugin/dev-orchestrator/scripts/requirements-survival-detect.sh
@plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh
@plugin/dev-orchestrator/scripts/check-doc-drift.sh
@.planning/REQUIREMENTS.md
@.planning/milestones/agentique-v1.0-REQUIREMENTS.md
</context>

<phase_arbitrations>
## Ce que ce plan HÉRITE du plan 18-01, sans le rouvrir

Les quatre arbitrages A-18-01 à A-18-04 du plan 18-01 (issues QUAL-01, forme du marqueur de
ratchet, nom/contrat de `requirements-survival-detect.sh`, libellé `## Garanties`) sont TRANCHÉS et
livrés. Ce plan ne les rediscute pas ; il lit `18-01-SUMMARY.md` pour confirmer les noms
effectivement posés (le checkpoint T1 du plan 18-01 pouvait amender les noms proposés — ce plan
suit ce qui a été RÉELLEMENT écrit, pas ce qui était proposé).

## A-18-05 — Forme du rattrapage : `--write` explicite, jamais un geste silencieux

D-18-06 exige une proposition sous validation humaine, « jamais en silence ». Le script suit donc
le même contrat que `gsd-docs-update --verify-only` cité en analogue par `AGENT.md` : par défaut il
calcule et **affiche** la reconstitution proposée (un diff unifié entre le `.planning/REQUIREMENTS.md`
qui SERAIT écrit et l'état actuel — absent ou vide) sans toucher au disque ; sous `--write`, et
seulement alors, il écrit. Il n'y a pas de troisième mode : pas de `--yes` qui saute la lecture du
diff, pas de prompt interactif (le script est un outil de gate/CLI, pas un TUI).

## A-18-06 — `vf_ledger_classify` : contrat de la classification statut→destin

Étend `requirements-survival-detect.sh` (déjà sourcée par le gate du plan 18-01 — même fichier,
fonction supplémentaire, pas de second fichier de primitive : le CONTEXT parle d'« une primitive,
deux consommateurs », pas de deux primitives).

Signature : `vf_ledger_classify <ligne_de_statut_du_corps> <ligne_de_traçabilité_correspondante>`.
Les deux lignes viennent du MÊME `REQUIREMENTS.md` archivé — la ligne de checklist (`- [x] **ID**:
texte`) et sa ligne dans la table `## Traceability` (`| ID | Phase N | Status |`). Codes de retour :

| Code | Destin | Condition de reconnaissance |
|---|---|---|
| 0 | Garantie | Case cochée `[x]` **ET** le texte de traçabilité contient `Complete`, ou le texte du corps contient le jeton littéral `Livré v` **sans** `caduc` sur la même ligne |
| 1 | Voyage | Case NON cochée `[ ]` ou `[~]`, **et** aucun jeton `caduc` sur la ligne de corps |
| 2 | Caduque | La ligne de corps contient le jeton littéral `caduc` (insensible à la casse), quel que soit l'état de la case |
| 3 | Forme non reconnue | Ni 0, ni 1, ni 2 — la ligne ne correspond à aucun patron connu |

L'ordre de test est figé : **caduque d'abord** (le jeton `caduc` gagne toujours, y compris sur une
case cochée — c'est exactement `VERB-02`, « Livré v2.31.0 … — caduc depuis v2.33.0 », qui doit
sortir 2 et jamais 0), puis garantie, puis voyage, puis 3 en repli. Écrire cet ordre en commentaire
au-dessus de la fonction : c'est le seul endroit où la précédence est décidée.

**Zéro normalisation (D-18-13)** : la fonction ne réécrit JAMAIS le texte du statut. Elle ne fait
que trancher un code de destin ; la ligne écrite dans le fichier reconstitué est la ligne SOURCE,
verbatim, à l'octet près, avec au plus l'ajout du suffixe `carried-from: v[X.Y]` pour le cas 1.

**Cas 3 n'est jamais un abandon silencieux** : le script principal compte les lignes classées en
code 3 et les signale sur stderr avec leur ID si extractible (jamais bloquant, D-18-10 étendu par
analogie — le rattrapage aussi est lecteur, pas juge, sur ce qu'il ne reconnaît pas).

## A-18-07 — Construction du fichier reconstitué : ordre et sections figés

Dans cet ordre, du haut vers le bas :

1. Frontmatter/titre repris VERBATIM du fichier `REQUIREMENTS.md` **vivant actuel** si un fragment
   subsiste (titre `# Requirements: …`, ligne `**Defined:** …`) — sinon repris de l'archive. Le
   script ne fabrique jamais un titre de zéro.
2. Un bloc de préambule daté, format libre mais portant littéralement : jalon d'origine, date de
   reconstitution, chemin de l'archive source — sur le modèle du bloc `> **Clôture agentique-v1.0
   …**` déjà présent en tête du fichier réel (à lire comme référence de ton, pas à copier mot pour
   mot).
3. **`## Garanties`** (A-18-04 du plan 18-01) — une ligne par exigence classée code 0, verbatim, la
   case gardée `[x]`, triée dans l'ordre d'apparition de l'archive (jamais retriée par famille : la
   trace de qui a livré quoi appartenait déjà à cet ordre).
4. Les familles du corps **non-livrées** de l'archive, structure `### <famille>` reprise
   verbatim, ne contenant QUE les lignes classées code 1 (voyage), chaque ligne recevant le suffixe
   ` — carried-from: v[X.Y]` (D-18-12, une espace avant le tiret cadratin existant si la ligne en a
   déjà un, sinon directement en fin de ligne) où `v[X.Y]` est le jalon d'origine résolu par
   `vf_ledger_state` (`VF_LEDGER_MILESTONE`, déjà assaini par la liste blanche T-18-01 du plan
   18-01 — jamais une seconde extraction).
5. `## Out of Scope` — reprise verbatim de l'archive SI présente, sinon omise (ne jamais fabriquer
   une section vide).
6. `## Traceability` — reconstruite avec UNIQUEMENT les lignes des exigences code 1 (voyage), la
   colonne Status inchangée verbatim. Les exigences code 0 et code 2 n'y figurent PAS — c'est la
   preuve sur pièce de D-18-03 (la table de traçabilité ne redevient jamais une liste de restes ET
   de garanties mélangées).
7. Pied de page daté, une ligne `*Reconstitué depuis l'archive <chemin> le <date> par
   restore-requirements-ledger.sh (LEDG-01).*` — la seule ligne qui identifie l'outil, pour qu'un
   futur agent qui lit le fichier sache d'où vient cette forme.

Les exigences code 2 (caduques) et code 3 (non reconnues) ne sont écrites NULLE PART dans le
fichier reconstitué — elles restent uniquement dans l'archive, exactement la doctrine D-18-11 pour
les caduques et un repli conservateur pour le non-reconnu (ne pas écrire ce qu'on n'a pas su
classer plutôt que de deviner).

## A-18-08 — Le rattrapage ne s'invoque JAMAIS depuis le gate

Le gate (`check-requirements-survival.sh`, plan 18-01) IMPRIME le geste proposé, il ne l'EXÉCUTE
jamais — même sous `--hook`. `restore-requirements-ledger.sh` n'a donc aucun appelant automatique :
c'est un script que l'agent/l'utilisateur invoque à la main, en réaction au signal `[ledger-absent]`.
Aucun câblage `hooks.json` pour ce plan.
</phase_arbitrations>

<tasks>

<task type="auto">
  <name>Task 1: `vf_ledger_classify` — extension de la primitive partagée</name>
  <read_first>
    - `plugin/dev-orchestrator/scripts/requirements-survival-detect.sh` — écrite au plan 18-01, en-tête et contrat de `vf_ledger_state` à respecter à l'identique (variables vidées en tête, jamais de sortie parsée par le consommateur)
    - Section `<phase_arbitrations>` A-18-06 de ce plan — le contrat complet de `vf_ledger_classify`
    - `.planning/milestones/agentique-v1.0-REQUIREMENTS.md` — corpus réel pour calibrer les regex (48 `Complete`, 9 `Pending`, la ligne `VERB-02` caduque, les annotations en prose riches listées au CONTEXT §D)
  </read_first>
  <action>
Ajouter `vf_ledger_classify` à `requirements-survival-detect.sh`, sans toucher à `vf_ledger_state`
ni à son contrat de retour déjà livré. Implémenter la précédence figée par A-18-06 (caduque
d'abord, puis garantie, puis voyage, puis code 3 en repli) avec des motifs `grep -qi` littéraux
(`caduc`, `Livré v`, `Complete`) — pas de moteur d'inférence, pas de score, un enchaînement de
tests conditionnels lisible en une passe.

Ajouter en tête de fichier (section dédiée, sous celle de `vf_ledger_state`) le tableau de
précédence tel qu'écrit en A-18-06, verbatim, pour qu'un futur lecteur n'ait pas à le reconstituer
depuis le code.
  </action>
  <verify>
    <automated>bash -n plugin/dev-orchestrator/scripts/requirements-survival-detect.sh; bash -c 'source plugin/dev-orchestrator/scripts/requirements-survival-detect.sh; vf_ledger_classify "- [x] **VERB-02**: Livre v2.31.0 (17/18 verbes) - caduc depuis v2.33.0 (facade supprimee)" "| VERB-02 | Phase 12 | Complete |"; test $? -eq 2'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n plugin/dev-orchestrator/scripts/requirements-survival-detect.sh` sort en 0.
    - Ligne caduque cochée (`VERB-02`) → code 2, jamais 0 (précédence caduque-d'abord prouvée).
    - Ligne `- [x] **ROUT-01**: …` + traçabilité `Complete` → code 0.
    - Ligne `- [ ] **WKTR-01**: …` sans jeton `caduc` → code 1.
    - Ligne hors de tout patron connu (ex. un texte de checklist mal formé, sans case) → code 3.
    - `vf_ledger_state` (plan 18-01) reste bit-à-bit identique : `git diff` du fichier isolé à la seule zone ajoutée montre uniquement des lignes neuves, aucune ligne supprimée ou modifiée dans la fonction existante.
  </acceptance_criteria>
  <done>`vf_ledger_classify` classe les 4 destins avec la précédence figée, prouvée sur le cas réel `VERB-02` du ledger de ce dépôt.</done>
</task>

<task type="tracer">
  <name>Task 2: `restore-requirements-ledger.sh` — le geste de rattrapage, diff par défaut, écriture sous confirmation</name>
  <read_first>
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` — moule de parsing d'arguments, `say()`, `-h|--help` auto-documenté
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` lignes 221-282 — lecture bornée et `sanitize_value`, à réutiliser en la SOURÇANT depuis `requirements-survival-detect.sh` (déjà appliquée par `vf_ledger_state`), jamais en la recopiant une troisième fois
    - Section `<phase_arbitrations>` A-18-05 et A-18-07 de ce plan — le contrat complet du script et l'ordre figé du fichier reconstitué
    - `.planning/milestones/agentique-v1.0-REQUIREMENTS.md` et `.planning/REQUIREMENTS.md` — les deux extrémités réelles du roll-forward, à utiliser pour calibrer la fixture de la tâche 3
  </read_first>
  <action>
Écrire `plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh` sur le moule de
`check-doc-drift.sh`, adapté à un script de POST-TRAITEMENT plutôt qu'à un gate `SessionStart` (pas
de mode `--hook`, pas de code 3 : ce script est invoqué à la main, jamais par un hook).

**Arguments** : `--path <dir>` (défaut `.`), `--write` (sinon mode diff, par défaut), `--quiet`
(supprime les diagnostics `say()` sur stderr, n'affecte jamais le diff lui-même), `-h|--help`.
Argument inconnu ou `--path` sans valeur : message stderr, exit 64.

**Séquence** :
1. Sourcer `requirements-survival-detect.sh` par la même découverte à liste de chemins candidats
   que le gate du plan 18-01 (répertoire du script, puis chemin relatif du dépôt source).
   Introuvable : message stderr nommant le fichier attendu, exit 1 — CE script n'a pas de mode
   dégradé « fail-open bruyant » comme le gate, puisqu'il n'est jamais dans le chemin d'un hook :
   pas de reconstitution possible sans la primitive, donc échec franc.
2. Appeler `vf_ledger_state "$PATH/.planning"`. Code différent de 0 (`absent_after_close`) :
   message stderr expliquant qu'il n'y a rien à reconstituer (ledger présent, ou aucun jalon clos,
   ou état illisible — reprendre `VF_LEDGER_REASON` si présent), exit 1. Ce script ne force jamais
   une reconstitution hors du cas que le gate détecte.
3. `VF_LEDGER_ARCHIVE` vide : message stderr « aucune archive `<chemin attendu>` à reconstituer »,
   exit 1.
4. Lire l'archive `VF_LEDGER_ARCHIVE` **en lecture seule stricte** — jamais de `git rm`, jamais
   d'écriture, jamais de déplacement du fichier archive. Parcourir ses lignes de checklist
   (`^- \[.\] \*\*[A-Z]+-[0-9]+\*\*:`) et, pour chacune, retrouver sa ligne de traçabilité
   correspondante dans la table `## Traceability` de la MÊME archive (correspondance par ID entre
   crochets). Appeler `vf_ledger_classify` sur la paire. Accumuler les lignes classées par code
   (0/1/2/3), en conservant l'ordre d'apparition dans l'archive et les en-têtes `### <famille>` qui
   les précèdent.
5. Construire le contenu proposé selon l'ordre figé A-18-07. La date de reconstitution est
   `date -u +%Y-%m-%d`. Le jalon d'origine est `VF_LEDGER_MILESTONE` (déjà assaini par le plan
   18-01, jamais réextraite).
6. **Mode diff (défaut)** : imprimer sur stdout le résultat de `diff -u` entre le fichier vivant
   actuel (ou `/dev/null` s'il est absent — c'est le cas nominal du signal `[ledger-absent]`) et le
   contenu construit, précédé d'une ligne `[restore-requirements-ledger] proposition de
   reconstitution depuis <archive> (jalon <libellé>) — relancer avec --write pour écrire.` Exit 0.
   Aucune écriture, quel que soit le contenu du diff — même un diff vide (cas déjà arrivé où le
   fichier vivant correspondrait déjà) sort 0 sans toucher au disque.
7. **Mode `--write`** : écrire le contenu construit dans `<path>/.planning/REQUIREMENTS.md`
   (écriture atomique — fichier temporaire puis `mv`, jamais une écriture directe qui pourrait
   laisser un fichier tronqué sur interruption). Imprimer sur stdout un résumé (nombre d'items en
   `## Garanties`, nombre d'items voyageurs, nombre d'items caducs laissés en archive, nombre de
   lignes en code 3 signalées séparément sur stderr). Exit 0.
8. Si des lignes en code 3 existent, les lister sur stderr avec leur ID (extrait par la même regex
   `\*\*[A-Z]+-[0-9]+\*\*` que l'étape 4) dans les DEUX modes (diff et write) — un agent qui lance
   `--write` sans avoir lu le diff doit quand même voir ce qui n'a pas pu être classé.

**En-tête doctrinal** : reprendre la forme des analogues, avec le refus propre à ce script — il ne
touche JAMAIS l'archive, ne réimplémente jamais `complete-milestone`/l'archivage/la génération de
ROADMAP (Iron Law 2, D-18-05), et n'écrit qu'sous `--write` explicite (D-18-06). Documenter les
codes de sortie (0 = diff affiché ou écriture faite ; 1 = rien à reconstituer ou état invalide ; 64
= argument invalide).
  </action>
  <verify>
    <automated>bash -n plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh</automated>
  </verify>
  <acceptance_criteria>
    - Chemin de bout en bout : sur une fixture jetable avec `.planning/MILESTONES.md` (jalon clos), `.planning/milestones/<libellé>-REQUIREMENTS.md` (archive contenant au moins un item Complete, un item Pending, un item caduc) et `.planning/REQUIREMENTS.md` absent — `restore-requirements-ledger.sh --path <dir>` sort 0, n'écrit AUCUN fichier (vérifié par une empreinte `find` avant/après), et son stdout contient les lignes `## Garanties`, `carried-from:` et ne contient PAS la ligne caduque.
    - La même commande avec `--write` sort 0, ÉCRIT `.planning/REQUIREMENTS.md`, et le fichier écrit contient l'item Complete sous `## Garanties`, l'item Pending sous sa famille d'origine avec `carried-from: v<jalon>`, et NE CONTIENT PAS l'item caduc.
    - La table `## Traceability` du fichier écrit ne porte QUE l'ID de l'item Pending — ni l'ID Complete, ni l'ID caduc.
    - Rejouée sur ce dépôt (`--path .`) : exit 1 sur stderr expliquant qu'il n'y a rien à reconstituer (ledger vivant présent) — jamais une tentative d'écraser `.planning/REQUIREMENTS.md` réel.
    - Aucun appel à git : `grep -v '^#' plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh | grep -cE '(^|[^a-z])git '` rend `0`.
    - `--path` sans valeur ou argument inconnu : exit 64.
    - `--help` : exit 0, sortie non vide.
    - Écriture atomique : `grep -c 'mv ' plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh` rend un nombre supérieur ou égal à `1`, et la fixture d'écriture ne laisse aucun fichier temporaire résiduel après exécution.
  </acceptance_criteria>
  <done>Le rattrapage reconstitue un ledger conforme à D-18-11/12/13 sur fixture, ne touche jamais l'archive, et n'écrit qu'explicitement sous `--write`.</done>
</task>

<task type="auto">
  <name>Task 3: Suite de tests du rattrapage + doctrine (AGENT.md, STUDY.md)</name>
  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` — harness à réutiliser (mktemp -d + trap, ok/ko, capture stdout/exit séparée)
    - `plugin/dev-orchestrator/AGENT.md` lignes ~118-131 — table des signaux de démarrage, patron exact de la ligne à ajouter
    - `.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md` §7.2 — le passage à périmer, D-18-02 du CONTEXT pour la forme de l'encadré
  </read_first>
  <action>
**1. `plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh`** — même harness
que `test-check-doc-drift.sh` (fixtures `mktemp -d` + trap, assertions stdout/exit séparées,
lecture seule vérifiée par empreinte `find`).

Cas de la matrice :
- pas d'archive à reconstituer (ledger vivant présent) : exit 1, stdout vide, fichier vivant intact ;
- rien à reconstituer (aucun jalon clos) : exit 1 ;
- reconstitution nominale en mode diff : exit 0, AUCUNE écriture (empreinte `find` identique
  avant/après), stdout contient `## Garanties`, `carried-from:`, et n'a pas la ligne caduque ;
- reconstitution nominale en mode `--write` : exit 0, fichier `.planning/REQUIREMENTS.md` écrit,
  contenu validé section par section (Garanties = items Complete, familles = items Pending avec
  trace, Traceability = items Pending seuls, aucune trace des items caducs) ;
- item avec ligne de statut hors de tout patron connu (code 3) : le script continue (n'échoue pas
  globalement), l'ID apparaît sur stderr, l'item n'apparaît dans AUCUNE section du fichier écrit ;
- item `VERB-02`-like (coché ET portant `caduc`) : classé caduque, absent du fichier écrit, présent
  uniquement dans l'archive source (jamais modifiée — empreinte de l'archive identique avant/après) ;
- écriture atomique : interrompre artificiellement n'est pas testable simplement, mais vérifier
  qu'aucun fichier `.tmp`/résiduel ne subsiste après une exécution normale sous `--write` ;
- déterminisme : deux exécutions consécutives en mode diff sur la même fixture rendent un stdout
  identique ;
- parité d'interface : `--path` sans valeur et argument inconnu à 64 ; `--help` à 0, sortie non vide ;
- `bash -n` passe sur `restore-requirements-ledger.sh`.

Taille cible : 150-220 lignes (fixture plus simple que le gate — moins d'états discriminants,
un seul chemin nominal + ses variantes).

**2. `plugin/dev-orchestrator/AGENT.md`** — ajouter une ligne à la table des signaux de démarrage
(section `## Signaux de démarrage`), patron identique aux 4 lignes existantes :
```
| `[ledger-absent]` | `restore-requirements-ledger.sh` (diff d'abord, `--write` ensuite) | requise avant toute écriture (ADR-031) |
```
Reprendre le nom exact du tag EFFECTIVEMENT posé par le plan 18-01 (lire `18-01-SUMMARY.md` si le
checkpoint T1 a renommé `[ledger-absent]`). Respecter le plafond ADR-029 (250 lignes) — si le
fichier est déjà tendu, l'entrée peut être ajoutée en fusionnant avec la note existante « Un 5e
fait … est déjà couvert par … » plutôt qu'en gonflant la table de deux lignes.

**3. `.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md`** —
ajouter en tête du §7.2 un encadré d'amendement daté (D-18-02), forme :
```
> **Amendement du 2026-08-17.** Ce §7.2 décrit la variante « keep-file » comme design retenu. Le
> prototype amont attaché à la RFC (issues/3556) mesure cette variante à 4 conflits sur 5 checks et
> ne fait sortir que le **roll-forward** à 0/5. Cette section est donc **périmée sur son point
> central** — le design réellement retenu est le roll-forward, documenté dans
> `18-CONTEXT.md` (D-18-01 à D-18-13). Conservé ci-dessous pour l'historique du raisonnement, ne
> pas l'utiliser comme référence de conception.
```
Ne pas réécrire le corps du §7.2 lui-même — l'encadré suffit, c'est tout ce que D-18-02 demande.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh` sort avec le code 0 et sa dernière ligne annonce 0 ko.
    - `wc -l < plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh` rend un nombre entre `150` et `260`.
    - `grep -c '\[ledger-absent\]' plugin/dev-orchestrator/AGENT.md` (ou le tag effectivement posé au plan 18-01) rend au moins `1`.
    - `grep -c 'Amendement du 2026-08-17' .planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md` rend `1`.
    - Le corps du §7.2 du STUDY est inchangé sous l'encadré — `git diff` sur `STUDY.md` ne montre que des lignes AJOUTÉES, aucune ligne supprimée.
    - Le dépôt réel n'est jamais touché par les fixtures du test : `grep -v '^#' plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh | grep -c 'mktemp -d'` rend au moins `1`.
    - `wc -l plugin/dev-orchestrator/AGENT.md` reste sous `250` (ADR-029).
  </acceptance_criteria>
  <done>Le rattrapage est testé (chemin nominal + code 3 + caduque + atomicité + déterminisme), le signal `[ledger-absent]` est documenté dans `AGENT.md`, et le STUDY ne peut plus être lu en premier sans voir l'amendement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/milestones/<jalon>-REQUIREMENTS.md` (archive de dépôt) → contenu écrit dans `.planning/REQUIREMENTS.md` | Contenu de dépôt lu et recomposé, potentiellement dans un clone non maîtrisé sous `--path`. |
| argument `--path` → système de fichiers, y compris la cible d'écriture sous `--write` | Un chemin arbitraire détermine où le script ÉCRIT — frontière plus sensible que celle, lecture seule, du plan 18-01. |
| `VF_LEDGER_MILESTONE` → suffixe `carried-from:` inséré dans le fichier écrit | Valeur déjà assainie par le plan 18-01 (liste blanche T-18-01), réutilisée sans seconde extraction — la frontière est HÉRITÉE, pas neuve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07 | Tampering | écriture de `.planning/REQUIREMENTS.md` sous `--write` | high | mitigate | Écriture atomique (fichier temporaire + `mv`) — jamais de troncature partielle visible. Le fichier n'est écrit QUE sous `--write` explicite (D-18-06) ; le mode par défaut est un diff en lecture seule, prouvé par empreinte `find` avant/après dans la suite de tests. |
| T-18-08 | Tampering | lecture de l'archive `<jalon>-REQUIREMENTS.md` | medium | mitigate | Lecture strictement en lecture seule — aucune primitive d'écriture, de déplacement ou de suppression n'est appelée sur le chemin de l'archive. Vérifié par empreinte de l'archive avant/après dans la suite. |
| T-18-09 | Tampering | classification code 3 (forme non reconnue) silencieusement ignorée | medium | mitigate | Toute ligne classée code 3 est signalée sur stderr avec son ID dans les DEUX modes (diff et `--write`), jamais absorbée sans trace — prouvé par un cas de fixture dédié. |
| T-18-10 | Elevation of Privilege | contenu de l'archive interpolé dans une commande | high | mitigate | Aucune évaluation dynamique de chaîne ; le contenu passe en argument à `grep`/`awk`/`diff`, jamais interpolé dans une commande construite. |
| T-18-11 | Denial of Service | reconstitution d'une archive volumineuse au SessionStart | low | accept | Ce script n'est JAMAIS appelé par un hook (A-18-08) — il est invoqué à la main, sans contrainte de latence de session. |
| T-18-SC | Tampering | chaîne d'approvisionnement | high | accept | Aucune dépendance externe introduite ; `bash`, `awk`, `grep`, `sed`, `diff`, `mv` — utilitaires POSIX déjà requis par le module. |
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-restore-requirements-ledger.sh` → 0 ko
- `bash -n plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh` et `bash -n plugin/dev-orchestrator/scripts/requirements-survival-detect.sh`
- `restore-requirements-ledger.sh --path .` sur ce dépôt → exit 1, aucune écriture (ledger vivant déjà présent)
- Densité : `wc -l plugin/dev-orchestrator/scripts/restore-requirements-ledger.sh` sous 200 lignes
- `grep -c 'Amendement du 2026-08-17' .planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md` → 1
</verification>

<success_criteria>
- LEDG-01 est implémenté : la clôture de jalon a désormais un rattrapage outillé qui fait un roll-forward complet, testé sur les trois destins (D-18-11)
- Le trou du STUDY §7.1 est comblé de bout en bout : détection (plan 18-01) + reconstitution (ce plan) forment une tranche verticale complète
- Le STUDY.md ne peut plus induire un futur agent en erreur sur la forme du roll-over (D-18-02)
- `AGENT.md` documente le signal `[ledger-absent]` et son geste, sous confirmation humaine (ADR-031)
</success_criteria>

<output>
Créer `.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/18-02-SUMMARY.md` à la fin.
</output>

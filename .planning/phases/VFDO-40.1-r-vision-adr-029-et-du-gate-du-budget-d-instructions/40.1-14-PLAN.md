---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 14
lot: L8 — clôture : recensement nul, contrôles négatifs, rejeu CI complet, invariants, ledger
type: execute
wave: 4
depends_on: ["40.1-01", "40.1-02", "40.1-03", "40.1-04", "40.1-05", "40.1-06", "40.1-07", "40.1-08", "40.1-09", "40.1-10", "40.1-11", "40.1-12", "40.1-13", "40.1-15"]
autonomous: true
requirements: [BUDG-04, BUDG-05, QUAL-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/BACKLOG.md
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "PRÉCONDITION — la clôture s'exécute sur une base qui CONTIENT le hotfix v2.63.1 fusionné : les preuves portent sur l'état qui sera réellement proposé au merge. Révision 3 (warning W-B) : vérification MACHINE BLOQUANTE jouée en premier (Task 0), sur le CONTENU de la base dérivée par `tools/phase-base.sh` (`VERSION` racine ≥ v2.63.1 par comparaison numérique ET entrée `## [v…]` du CHANGELOG `dev-orchestrator` citant v2.63.1, base = pointe de la ref main retenue), jamais sur un sha ni un sujet de commit. Rouge → arrêt et remontée au manager, aucun rebase décidé par l'exécutant (arbitrage Samuel, AskUserQuestion session principale, 2026-09-16)"
    - "BUDG-04 (critère 1) — `tools/check-no-live-250.sh .` rend 0 sans aucune ligne sur le dépôt final ; ses deux contrôles négatifs (fixtures, clone réel) rougissent sur un ancien plafond réintroduit. L'outil de recensement N'EST PAS modifié par ce plan : tout hit inattendu est remonté au manager (blocage), jamais exclu pour passer. Révision 3 (warning W-D) : « non modifié » se juge sur le CONTENU — le blob de `tools/check-no-live-250.sh` à HEAD et dans l'arbre de travail est identique à son blob au commit propre à la branche qui AJOUTE `40.1-01-SUMMARY.md` (clôture du plan 01), et aucun commit non-merge postérieur ne touche l'outil, quel que soit son sujet ; le filtre historique par sujet reste en garde cumulative"
    - "Non-régression sur la découverte COMPLÈTE : `tools/replay-ci-jobs.sh --job gates` rend 0 avec au moins 10 étapes rejouées ; `--job tests` rend 0 ET sa sortie porte la ligne de bilan `== bilan : N suite(s), M échec(s) ==` avec N > 0 et M = 0. Bilan absent ou N = 0 → rouge"
    - "Périmètre non rejoué, NOMMÉ : job `tests`, étapes `Dépendances`, `actions/setup-node`, `Installer le moteur GSD` (installation du runner) et `Canari de forme du moteur GSD` (il lit le moteur installé du runner, pas celui du poste) ; jobs `lab-frais` et `lab-frais-arme` (install dans un lab vierge sur runner) ; étape `check-release-tag` (conditionnelle main) ; SKIP internes des suites en local, nommés et recopiés au SUMMARY — au minimum `test-dev-orchestrator.sh` T6 (install e2e) et T8c (contrôleur check-agents selon la disposition), plus tout autre SKIP imprimé par le rejeu. Le run CI distant sur la PR reste une vérification humaine"
    - "Invariants imputés à TOUT commit propre à la branche, quel que soit son sujet, merges compris (`tools/check-phase-invariants.sh`, révision 2) : aucune résolution de merge sur chemin gardé (M), baseline jamais montée (I1), agents non réécrits au-delà du remplacement de valeur avec au moins 3 fichiers comparés (I2), aucune hausse sur aucune ligne du gate et égalité stricte sur les agents touchés par la branche, une MARGE apportée par le hotfix restant verte (I3), aucune source de version racine touchée (I4)"
    - "Versions : dix modules bumpés sur cibles DÉRIVÉES, vérifiés par `tools/check-module-bump.sh` — conductor minor ; dev-orchestrator, design-orchestrator, business-pilot-bundle, content-bundle, growth-bundle, reference, validator, kpi-analyst, audit-architecture patch — et `check-version-sync.sh` vert ; aucun numéro figé"
    - "Ledger : BUDG-04 et BUDG-05 cochés avec date et preuve, QUAL-01 noté satisfait sur la Phase 40.1 ; points 1 et 4 de l'item BACKLOG « Dérive documentaire… » marqués fermés avec renvoi aux plans 40.1-11 et 40.1-03 ; `40.1-VALIDATION.md` porte les statuts réels ; aucune valeur de l'ancien plafond écrite ; STATE.md NON modifié par ce plan (geste à la main de l'orchestrateur : sauvegarde puis `git diff --stat`, jamais `state record-session` ni `state.planned-phase`)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BUDG-04, BUDG-05 clos ; QUAL-01 mis à jour"
    - path: ".planning/BACKLOG.md"
      provides: "points 1 et 4 de l'item « Dérive documentaire… » fermés"
  key_links:
    - "Le rejeu du job `tests` exécute AUSSI `test-check-instruction-budget.sh` et les suites de modules relevées : c'est la preuve d'intégration des vagues 2 et 3"
---

<objective>
Clore la Phase 40.1 par la preuve, jamais par déclaration :
- recensement nul et contrôles négatifs ;
- rejeu complet des jobs CI `tests` et `gates` depuis `ci.yml` ;
- invariants de baseline et de non-réécriture d'agents, imputés à la seule phase ;
- bumps dérivés ;
- ledger.
Le tout sur une base qui contient le hotfix v2.63.1.

Purpose: satisfaire BUDG-04, BUDG-05 et QUAL-01 sur l'état final intégré, pas plan par plan.
Output: ledger, BACKLOG et VALIDATION à jour, preuves consignées au SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0 : précondition machine — la base dérivée contient le hotfix v2.63.1 (lecture seule, AVANT toute édition)</name>
  <files>aucun (lecture seule)</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh (choix de la ref main, rc 2 sur divergence)
  </read_first>
  <action>
Jouer la vérification ci-dessous AVANT toute autre commande de ce plan. Même contrôle que la Task 0
de 40.1-07, rejoué sur l'état final : base de branche dérivée `B`, témoins de CONTENU en ET —
`VERSION` racine de `B` ≥ v2.63.1 par comparaison numérique champ à champ ; entrée `## [v…]` du
CHANGELOG `plugin/dev-orchestrator/` à `B` citant v2.63.1 (seule l'entrée du hotfix la cite, ce qui
ferme le cas d'une release ayant sauté le hotfix) — et `B` pointe de la ref main retenue (la branche
descend du dernier `main` connu). Ni sha, ni sujet de commit : un squash-merge du hotfix change ses
sha, pas ce contenu.
**Rouge → arrêt immédiat, aucune écriture, remontée au manager** avec la ligne `precondition hotfix`
imprimée ; l'exécutant ne rebase pas (arbitrage Samuel, AskUserQuestion session principale,
2026-09-16). Un rc 2 de `phase-base.sh` (refs divergées) est traité de même.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); T=$(mktemp -d); git show "$B":VERSION > "$T/v"; v=$(awk "NR==1{print \$1}" "$T/v"); sup=$(awk -v v="$v" -v s="v2.63.1" "BEGIN{ r=\"non\"; n=split(v,a,\".\"); split(s,b,\".\"); sub(/^v/,\"\",a[1]); sub(/^v/,\"\",b[1]); if (n==3 && a[1] ~ /^[0-9]+$/ && a[2] ~ /^[0-9]+$/ && a[3] ~ /^[0-9]+$/) { r=\"oui\"; for (i=1;i<=3;i++) { if (a[i]+0 > b[i]+0) break; if (a[i]+0 < b[i]+0) { r=\"non\"; break } } } print r }"); git show "$B":plugin/dev-orchestrator/CHANGELOG.md > "$T/c"; h=$(awk "/^## \\[v[0-9]+[.][0-9]+[.][0-9]+\\]/ && /v2[.]63[.]1/ {c++} END{print c+0}" "$T/c"); t=$(git rev-parse -q --verify "origin/main^{commit}" || true); u=$(git rev-parse -q --verify "main^{commit}" || true); ch=$t; if [ -z "$t" ] || { [ -n "$u" ] && git merge-base --is-ancestor "$t" "$u"; }; then ch=$u; fi; ds=$([ -n "$ch" ] && [ "$B" = "$ch" ] && echo oui || echo non); echo "precondition hotfix : base=$B VERSION_base=$v version_ge_v2.63.1=$sup entree_changelog_dev-orchestrator_v2.63.1=$h branche_sur_la_pointe_main=$ds"; [ "$sup" = oui ] && [ "$h" -ge 1 ] && [ "$ds" = oui ]'</automated>
    <fails_when>exit non nul (y compris rc 2 de `phase-base.sh`) ; ou `version_ge_v2.63.1=non` ; ou `entree_changelog_dev-orchestrator_v2.63.1=0` ; ou `branche_sur_la_pointe_main=non`. Conduite : arrêt et remontée au manager, jamais rebase ni poursuite</fails_when>
  </verify>
  <acceptance_criteria>
    - la ligne `precondition hotfix : … version_ge_v2.63.1=oui entree_changelog_dev-orchestrator_v2.63.1=<n ≥ 1> branche_sur_la_pointe_main=oui` est consignée au SUMMARY avant toute autre preuve
  </acceptance_criteria>
  <done>La clôture porte sur une base qui contient le hotfix, prouvé sur le contenu ; sinon elle s'est arrêtée sans rien écrire.</done>
</task>

<task type="tracer">
  <name>Task 1 : recensement nul et contrôles négatifs sur l'état final</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <precondition>Task 0 verte (base dérivée contenant le hotfix v2.63.1, jugée sur le contenu ; rouge → arrêt et remontée au manager, aucun rebase décidé par l'exécutant) ; `git status --porcelain` ne liste que les chemins non suivis étrangers à la phase.</precondition>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh, tools/test-check-no-live-250.sh, tools/controle-negatif-reel.sh
    - les SUMMARY 40.1-01 à 40.1-13 (écarts déclarés)
  </read_first>
  <action>
Jouer le recensement final, la suite de contrôle négatif et le contrôle négatif sur clone réel.
**Interdit dans ce plan : modifier `tools/check-no-live-250.sh`** (règles comprises). Si le
recensement liste une ligne, ne rien exclure. S'arrêter et remonter au manager la ligne et son
classement proposé : vivante → à renvoyer au plan de sa famille ; histoire → règle nominative à
décider par un arbitrage, hors de cette clôture. Reporter dans `40.1-VALIDATION.md` le statut ✅ des
lignes « recensement » et « Wave 0 », avec les rc et nombres de lignes observés.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .</automated>
    <fails_when>exit non nul, ou au moins une ligne `chemin:ligne:contenu` imprimée</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh</automated>
    <fails_when>exit non nul, ou `M ko` avec M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :` (R0 non propre, ou R1/R2/R4 non détectés, ou R3 détecté)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions; TL=$PH/tools/check-no-live-250.sh; B=$(bash "$PH/tools/phase-base.sh"); T=$(mktemp -d); git log --format=%H%x09%s "$B"..HEAD -- "$TL" > "$T/l"; n=$(awk -F"\t" "\$2 !~ /^[a-z]+\\(40\\.1-01/ {c++} END{print c+0}" "$T/l"); git log --diff-filter=A --format=%H "$B"..HEAD -- "$PH/40.1-01-SUMMARY.md" > "$T/s"; S=$(awk "NF{s=\$1} END{print s}" "$T/s"); if [ -z "$S" ]; then echo "recensement : ancre_cloture_40.1-01=absente (attendu : commit propre a la branche ajoutant 40.1-01-SUMMARY.md)"; exit 1; fi; bs=$(git rev-parse "$S:$TL"); bh=$(git rev-parse "HEAD:$TL"); bw=$(git hash-object "$TL"); git rev-list --no-merges "$S"..HEAD -- "$TL" > "$T/a"; m=$(awk "END{print NR}" "$T/a"); echo "recensement : commits hors plan 01 par sujet=$n ancre_cloture_40.1-01=$S blob_cloture=$bs blob_HEAD=$bh blob_arbre=$bw commits_posterieurs_a_la_cloture_touchant_l_outil=$m"; [ "$n" -eq 0 ] && [ "$bs" = "$bh" ] && [ "$bh" = "$bw" ] && [ "$m" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `ancre_cloture_40.1-01=absente` (aucun commit propre à la branche n'ajoute `40.1-01-SUMMARY.md`) ; ou `blob_HEAD` différent de `blob_cloture` (l'outil a changé depuis la clôture du plan 01, quel que soit le sujet du commit, `fix(40.1-01)` compris) ; ou `blob_arbre` différent de `blob_HEAD` (édition non commitée) ; ou `commits_posterieurs_a_la_cloture_touchant_l_outil=` différent de 0 (modification puis retour à l'identique compris) ; ou `commits hors plan 01 par sujet=` différent de 0 (garde historique, cumulative). Une retouche légitime de l'outil après la clôture du plan 01 relève d'un arbitrage du manager, jamais de cette clôture</fails_when>
  </verify>
  <acceptance_criteria>
    - recensement final : rc 0, aucune ligne ; aucun commit propre à la branche hors 40.1-01 ne touche l'outil de recensement ; l'outil à HEAD et dans l'arbre est octet-identique (blob) à son état à la clôture du plan 01, sans commit postérieur le touchant
    - contrôle négatif fixtures : 0 ko ; contrôle négatif réel : R0 rc 0, R1 rc 1, R2 rc 1, R3 rc 0, R4 rc 1
  </acceptance_criteria>
  <done>BUDG-04 critère 1 prouvé sur l'état final, avec un recensement qui sait rougir et que la clôture n'a pas retouché.</done>
</task>

<task type="auto">
  <name>Task 2 : rejeu CI complet, invariants de phase et bumps dérivés</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh, tools/check-phase-invariants.sh, tools/check-module-bump.sh
    - .github/workflows/ci.yml (jobs `tests`, `gates`, `lab-frais`, `lab-frais-arme`)
  </read_first>
  <action>
Jouer le rejeu du job `gates` puis du job `tests` (plusieurs minutes), puis les invariants et les
bumps. Consigner dans le SUMMARY :
- chaque ligne `REJEU` ;
- le périmètre non rejoué et sa raison (must_haves) ;
- le nombre de suites découvertes ;
- chaque ligne SKIP imprimée par le rejeu du job `tests` (au minimum T6 et T8c de dev-orchestrator), avec sa raison.
Reporter les statuts ✅ dans `40.1-VALIDATION.md`.

Un rouge n'est jamais contourné. **Cas connu sans lien avec la phase :** T28-F de
`test-dev-orchestrator.sh` (fraîcheur de l'index des capacités GSD) peut rougir parce que le
gsd-core installé sur le poste diffère de celui qui a généré la copie versionnée. Avant de l'imputer
à la phase :
1. vérifier le moteur installé et `npm view @opengsd/gsd-core version` ;
2. rejouer la même suite sur une extraction `git archive` de la base de `main` contenant le hotfix ;
3. si elle y est rouge aussi, c'est préexistant : le consigner et remonter au manager
   (`human_needed`) ; ne rien neutraliser et ne jamais déclarer la clôture verte.
Tout autre rouge est renvoyé au plan fautif.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 (sélection tronquée) ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); echo "tests rc=$rc bilan(N M)=$b"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N égal à 0 ; ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-phase-invariants.sh</automated>
    <fails_when>exit non nul, ou la ligne `INVARIANTS :` portant un `ko`, ou `fichiers=` inférieur à 3</fails_when>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh; k=0; for mv in conductor:minor dev-orchestrator:patch design-orchestrator:patch business-pilot-bundle:patch content-bundle:patch growth-bundle:patch reference:patch validator:patch kpi-analyst:patch audit-architecture:patch; do m=${mv%%:*}; kind=${mv#*:}; if bash "$P" "$m" "$kind" > /dev/null 2>&1; then echo "bump $m $kind ok"; else echo "BUMP KO $m $kind"; k=$((k+1)); fi; done; echo "bumps en echec=$k"; [ "$k" -eq 0 ]; bash scripts/check-version-sync.sh > /dev/null'</automated>
    <fails_when>exit non nul ; ou une ligne `BUMP KO` ; ou `bumps en echec=` différent de 0 ; ou version-sync rouge</fails_when>
  </verify>
  <acceptance_criteria>
    - rejeu `gates` rc 0 avec au moins 10 étapes rejouées ; rejeu `tests` rc 0 avec bilan présent, N > 0, M = 0
    - périmètre non rejoué nommé au SUMMARY (Dépendances, setup-node, Installer le moteur GSD, Canari, lab-frais, lab-frais-arme, check-release-tag) ET SKIP internes recopiés (dev-orchestrator T6, T8c, tout autre SKIP imprimé)
    - `check-phase-invariants.sh` rc 0 : M, I1, I2 (fichiers ≥ 3), I3, I4 ok
    - dix `check-module-bump.sh` rc 0, version-sync rc 0
  </acceptance_criteria>
  <done>L'état final intégré est vert sur la découverte complète CI, sans baseline montée, sans agent réécrit, avec des bumps dérivés et sans release.</done>
</task>

<task type="auto">
  <name>Task 3 : ledger des exigences, BACKLOG et validation</name>
  <files>.planning/REQUIREMENTS.md, .planning/BACKLOG.md, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (liste BUDG ~L952-955, table de traçabilité ~L843-853, ligne QUAL-01)
    - .planning/BACKLOG.md (item « Dérive documentaire et fragilité latente révélées par le gate de budget d'instructions — DIFFÉRÉ (2026-09-15) », points 1 et 4)
    - SUMMARY 40.1-03, 40.1-11 et 40.1-14 (preuves à citer)
  </read_first>
  <action>
`REQUIREMENTS.md` :
- cocher BUDG-04 et BUDG-05 dans la liste, avec « close le <date d'exécution> (Phase 40.1) » et la
  preuve (recensement versionné à 0, bascules CI prouvées par mutants, rejeu tests+gates vert) ;
- lignes de table BUDG-04/BUDG-05 → `Complete — <date>, Phase 40.1 …` ;
- ligne QUAL-01 → ajouter « satisfait sur la Phase 40.1 le <date> (sept mutants du gate tués, dont
  MUT-2 remplacé ; contrôles négatifs du recensement ; frontière de l'auditeur) ».

`BACKLOG.md`, item « Dérive documentaire… » : AJOUTER sous l'item une ligne
`**Statut partiel (<date>, Phase 40.1) :**` :
- point 1 fermé par 40.1-11 (README validator réécrit, marge exacte sous le plafond révisé) ;
- point 4 fermé par 40.1-03 (mesure de la fixture de l'étape CI à code capturé, sans `|| true`) ;
- points 2 et 3 inchangés, toujours différés.
Aucune ligne existante de l'item modifiée : les exclusions de contenu du recensement ciblent ces
lignes.

`40.1-VALIDATION.md` : statuts réels, `wave_0_complete: true`, cases de sign-off cochées si vraies ;
`status` reste `draft`.

Aucune valeur de l'ancien plafond écrite dans ces trois fichiers. STATE.md non touché. Rejouer le
recensement après écriture.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'a=$(awk "/^- \\[x\\] \\*\\*BUDG-04\\*\\*/{c++} /^- \\[x\\] \\*\\*BUDG-05\\*\\*/{c++} END{print c+0}" .planning/REQUIREMENTS.md); b=$(awk "/^\\| BUDG-0[45] \\| Phase 40\\.1 \\| Complete/{c++} END{print c+0}" .planning/REQUIREMENTS.md); k=$(awk "/^\\*\\*Statut partiel \\(.*Phase 40\\.1\\) :\\*\\*/{c++} END{print c+0}" .planning/BACKLOG.md); w=$(awk "/^wave_0_complete: true/{c++} END{print c+0}" .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md); echo "coches=$a table=$b backlog_statut=$k wave0=$w"; [ "$a" -eq 2 ] && [ "$b" -eq 2 ] && [ "$k" -eq 1 ] && [ "$w" -eq 1 ]; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . && echo recensement_final_nul'</automated>
    <fails_when>exit non nul ; ou `coches=` différent de 2, `table=` différent de 2, `backlog_statut=` différent de 1, `wave0=` différent de 1 ; ou absence de `recensement_final_nul`</fails_when>
  </verify>
  <acceptance_criteria>
    - BUDG-04 et BUDG-05 cochés dans la liste et `Complete` dans la table ; QUAL-01 mentionne la Phase 40.1
    - BACKLOG : une ligne de statut partiel sous l'item, renvoyant à 40.1-11 (point 1) et 40.1-03 (point 4) ; lignes existantes de l'item inchangées
    - `40.1-VALIDATION.md` : `wave_0_complete: true`, statuts renseignés
    - recensement toujours nul après écriture ; STATE.md non modifié par ce plan
  </acceptance_criteria>
  <done>Ledger, backlog et validation disent l'état prouvé de la phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves de plan → verdict de phase | un vert auto-déclaré par plan ne vaut pas preuve sur l'état intégré |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-32 | Repudiation | clôture sur une liste de gates recopiée ou un bilan absent | high | mitigate | rejeu extrait de `ci.yml` ; ≥ 10 étapes gates ; bilan tests exigé avec N > 0 et M = 0 |
| T-40.1-33 | Elevation of privilege | baseline montée en cours de phase | high | mitigate | invariant I1 sur les commits de phase ; I3 gate égal à la baseline |
| T-40.1-34 | Tampering | exclusion ajoutée au recensement pour le faire passer | medium | mitigate | modification de l'outil interdite dans ce plan, vérifiée sur le contenu : blob identique à la clôture du plan 01 (ajout de `40.1-01-SUMMARY.md`), aucun commit postérieur, filtre par sujet en garde cumulative |
| T-40.1-35 | Repudiation | rouge préexistant (T28-F) imputé à la phase ou neutralisé | low | mitigate | rejeu sur la base de `main`, remontée `human_needed`, jamais de neutralisation |
</threat_model>

<verification>
Recensement 0 ; contrôles négatifs verts ; rejeu gates (≥ 10 étapes) et tests (bilan N > 0, M = 0)
verts ; invariants I1-I4 ; dix bumps dérivés ; ledger et backlog à jour.
</verification>

<success_criteria>
Les quatre critères de succès de la Phase 40.1 (ROADMAP) sont prouvés sur l'état final intégré,
hotfix compris.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : mises à jour de `.planning/REQUIREMENTS.md`, `.planning/BACKLOG.md` et `40.1-VALIDATION.md`.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-14-SUMMARY.md` when done
</output>

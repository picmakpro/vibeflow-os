---
phase: 41-posture-de-protection-du-d-p-t
plan: 06
lot: L6 — PR de preuve inoffensive touchant `.github/` : revue code owner exigée, merge sans contournement refusé (temps i)
type: execute
wave: 4
depends_on: ["41-05"]
autonomous: false
requirements: [PROT-04, PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Support choisi (discrétion de D-M7) : une PR « PR-P » au contenu inoffensif et utile — `.github/rulesets/README.md` (lecture des sources, pose, vérification, retour arrière, renvoi à ADR-072) —, donc sous `/.github/` du CODEOWNERS. Elle est gardée ouverte pour le temps (ii) du plan 41-08 ; si elle atterrit, rien de nocif n'entre sur `main`"
    - "Ordre de sûreté : la PREMIÈRE tentative de merge sans contournement de la phase a lieu sur cette PR inoffensive, jamais sur la PR rouge. Si `gh pr merge` sans option de contournement mergeait malgré le blocage, le dégât serait nul et le plan 41-07 n'attenterait aucun merge sur la PR rouge"
    - "Temps (i) de la revue code owner : CI verte sur les deux événements, branche à jour, `reviewDecision` = `REVIEW_REQUIRED`, `@picmakpro` demandé automatiquement en relecteur (preuve réelle que CODEOWNERS matche `.github/` sur la branche de base), `mergeStateStatus` = `BLOCKED`, `gh pr merge --merge` sans contournement refusé, message capturé, PR toujours `OPEN`"
    - "Les commits de PR-P sont construits par plomberie git (index temporaire, `commit-tree`) sans changer la branche de l'arbre de travail ; le second commit (P2), utilisé au plan 41-08, est construit ici ; seuls les pushs et la PR sont des gestes humains"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-P-COMMITS:`, `PR-P:`, `CO-I:`, `CO-I-MSG:`"
  key_links:
    - "`CO-I: … merge_sans_contournement=refuse` est la précondition du plan 41-07 (tentatives sur la PR rouge)"
---

<objective>
Prouver le temps (i) de la revue code owner sur un support inoffensif, et établir sur ce même
support que `gh pr merge` sans option de contournement refuse une PR bloquée pour un compte write.

Purpose: PROT-04 (revue code owner exigée par défaut) et sûreté des essais de 41-07 (D-M7).
Output: PR-P ouverte et bloquée, lignes `CO-I:` consignées.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-05-SUMMARY.md

Sources lues le 2026-09-17 : `cli.github.com/manual/gh_pr_merge` (`--admin` « Use administrator
privileges to merge a pull request that does not meet requirements » ; comportement sans cette
option sous ruleset non documenté) ; about-code-owners (auteur ne peut pas approuver sa PR ; version
de la branche de base).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : construire P1 et P2 par plomberie, vérifier leur contenu, préalables relus</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-*`, `M2:`, `ACCES-RULE-SUITES:`)
    - docs/ADR.md § ADR-072 (retour arrière, sources) pour le contenu du README
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin` ; aucune PR ouverte ; `POSE-CONFORME:` présente.

Rédiger dans un fichier temporaire (hors dépôt) le contenu de `.github/rulesets/README.md`, en
français : rôle des deux JSON (corps de `POST …/rulesets`), commande de pose depuis `origin/main` par
`picmakpro`, commandes de relecture (`rulesets`, `rules/branches/main`), retour arrière
(`enforcement=disabled`), rappel que toute modification exige la revue `@picmakpro` ou un
contournement tracé (D-02, D-05), renvoi à ADR-072. Aucun chemin de machine.

Construire P1 = `origin/main` + ce seul fichier, par plomberie : index temporaire via
`GIT_INDEX_FILE`, `read-tree origin/main`, `hash-object -w` du fichier, `update-index --add --cacheinfo 100644,<blob>,.github/rulesets/README.md`,
`write-tree`, `commit-tree -p origin/main` avec le message
« docs(41): README des sources de rulesets — support de preuve de la revue code owner (PR-P) ».
Construire P2 = P1 + une ligne finale au README « Ce fichier est entré sur `main` par un
contournement volontaire et tracé : preuve du critère 2 de la Phase 41 (voir 41-PREUVES.md). »,
parent P1, message « docs(41): trace du contournement volontaire (PR-P, temps ii) ». Aucun
changement de branche ni de l'arbre de travail.

Consigner `PR-P-COMMITS: p1=<sha> p2=<sha> base=<sha d'origin/main>`. Commit local sur B. Imprimer
les commandes de la Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); P1=$(val PR-P-COMMITS: p1); P2=$(val PR-P-COMMITS: p2); BA=$(val PR-P-COMMITS: base); T=$(mktemp -d); git diff --name-only "$BA" "$P1" > "$T/d1"; d1=$(awk "END{print NR}" "$T/d1"); f1=$(awk "NR==1" "$T/d1"); git diff --name-only "$P1" "$P2" > "$T/d2"; d2=$(awk "END{print NR}" "$T/d2"); pa1=$(git rev-parse "$P1^"); pa2=$(git rev-parse "$P2^"); git show "$P2:.github/rulesets/README.md" > "$T/readme"; rl=$(awk "END{print NR}" "$T/readme"); mp=$(awk "/\\/(Users|home)\\/[A-Za-z0-9._-]+/ {c++} END{print c+0}" "$T/readme"); br=$(git branch --show-current); echo "PR-P commits : identite=$u prs_ouvertes=$op fichiers_p1=$d1 ($f1) fichiers_p2=$d2 parent_p1_est_base=$([ "$pa1" = "$BA" ] && echo oui || echo non) parent_p2_est_p1=$([ "$pa2" = "$P1" ] && echo oui || echo non) chemins_machine=$mp readme_lignes=$rl branche_arbre=$br"; [ "$rl" -gt 3 ] && [ "$u" = samuel-neveugall ] && [ "$op" -eq 0 ] && [ "$d1" -eq 1 ] && [ "$f1" = .github/rulesets/README.md ] && [ "$d2" -eq 1 ] && [ "$pa1" = "$BA" ] && [ "$pa2" = "$P1" ] && [ "$mp" -eq 0 ] && [ "$br" = feat/phase-41-preuves-release ]'</automated>
    <fails_when>exit non nul ; ou une PR ouverte ; ou P1 touche autre chose que `.github/rulesets/README.md` ; ou P2 touche plus d'un fichier ; ou filiation fausse (P1 hors `origin/main`, P2 hors P1) ; ou chemin de machine dans le README ; ou l'arbre de travail a changé de branche</fails_when>
  </verify>
  <acceptance_criteria>
    - P1 = `origin/main` + le seul README ; P2 = P1 + une ligne ; filiation vérifiée ; aucun chemin de machine
    - `PR-P-COMMITS:` consignée ; arbre de travail resté sur B
  </acceptance_criteria>
  <done>Le support inoffensif existe localement, prêt à être publié par un humain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — publier P1, ouvrir PR-P, attendre la CI, tenter un merge SANS contournement</name>
  <action>Samuel, compte `samuel-neveugall`, publie P1, ouvre PR-P, attend la fin de tous les checks, relit l'état de merge, tente un merge sans option de contournement et capture le message.</action>
  <instructions>
Compte qui agit : `samuel-neveugall` (témoin `gh api user --jq .login`). Les SHA sont ceux imprimés
par la Task 1.

1. `git push origin <p1>:refs/heads/preuve/41-contournement`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-contournement --title "Phase 41 — README des sources de rulesets (PR de preuve : revue code owner et contournement tracé)" --body "PR de preuve de la Phase 41 (D-M7). Contenu inoffensif. Temps (i) : merge sans contournement attendu refusé. Temps (ii) au plan 41-08."`
   → noter `<n>`.
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tous verts attendus).
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus,reviewDecision --jq '{mergeStateStatus, reviewDecision}'`
5. SEULEMENT si 4 rend `mergeStateStatus` = `BLOCKED` : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge`
   (SANS `--admin`, sans case de contournement dans l'interface) → copier la sortie intégrale.
   Si 4 rend autre chose que `BLOCKED` : ne rien merger, noter l'état.
6. Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state --jq .state` → `OPEN` attendu.
Ne pas fermer PR-P : elle sert au plan 41-08.
  </instructions>
  <verification>La Task 3 relit la tête, les check runs des deux événements, le retard sur `main`, la décision de revue, les relecteurs demandés et l'état de la PR.</verification>
  <resume-signal>Coller `<n>` et les sorties de 3 (fin), 4, 5 (ou « non tenté : état X ») et 6, puis écrire « temps i fait ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : relecture et consignation du temps (i) de la revue code owner</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Consigner `PR-P: #<n> branche=preuve/41-contournement` puis, depuis ses
propres lectures et le collage :
`CO-I: tete=<sha> checks_verts_deux_evenements=<k>/4 behind_by=<b> review=<reviewDecision> demande_picmakpro=<oui|non> etat=<mergeStateStatus collé> merge_sans_contournement=<refuse|merge|non_tente> etat_pr_apres=<state>`
et `CO-I-MSG:` suivi de la sortie de `gh pr merge` collée, sur une ligne.

`merge_sans_contournement=refuse` seulement si la sortie collée est une erreur ET la PR relue est
`OPEN` ; `merge` si la PR est `MERGED` ; `non_tente` si l'état n'était pas `BLOCKED`.
Si `merge` ou `non_tente` : consigner, NE PAS lancer 41-07, remonter au manager (le refus par défaut
de D-09 n'est pas établi ; décision humaine requise). Commit local sur B. Rejouer `gates` et
`tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-P absente"; exit 1;; esac; P1=$(val PR-P-COMMITS: p1); H=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); rd=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewDecision --jq .reviewDecision); rq=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewRequests --jq "[.reviewRequests[].login] | index(\"picmakpro\") != null"); ok=$(gh api "repos/picmakpro/vibeflow-os/commits/$H/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | group_by(.name) | map(select(length >= 2)) | length"); bb=$(gh api "repos/picmakpro/vibeflow-os/compare/main...$H" --jq .behind_by); c=$(val CO-I: merge_sans_contournement); e=$(val CO-I: etat); m=$(awk "\$1==\"CO-I-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "temps i code owner : pr=$N tete_est_p1=$([ "$H" = "$P1" ] && echo oui || echo non) etat_pr=$st review=$rd picmakpro_demande=$rq jobs_verts_deux_evenements=$ok behind_by=$bb etat_consigne=$e merge_sans_contournement=$c message_capture=$m"; [ "$H" = "$P1" ] && [ "$st" = OPEN ] && [ "$rd" = REVIEW_REQUIRED ] && [ "$rq" = true ] && [ "$ok" -eq 4 ] && [ "$bb" -eq 0 ] && [ "$e" = BLOCKED ] && [ "$c" = refuse ] && [ "$m" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou tête différente de P1 ; ou PR non `OPEN` (mergée sans contournement : défaut, arrêt, remontée) ; ou `review=` différent de `REVIEW_REQUIRED` ; ou `picmakpro_demande=false` (CODEOWNERS ne matche pas `.github/` côté GitHub : PROT-04 en défaut) ; ou `jobs_verts_deux_evenements=` différent de 4 (le blocage ne serait pas attribuable à la seule revue) ; ou `behind_by=` différent de 0 ; ou `etat_consigne=` différent de `BLOCKED` ; ou `merge_sans_contournement=` différent de `refuse` ; ou message non capturé</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
  </verify>
  <acceptance_criteria>
    - PR-P ouverte sur P1, CI verte sur les deux événements, à jour de `main`
    - `@picmakpro` demandé automatiquement, décision `REVIEW_REQUIRED`, état `BLOCKED`
    - merge sans contournement refusé, message capturé, PR toujours `OPEN`
  </acceptance_criteria>
  <done>Le temps (i) de la revue code owner est prouvé, et la tentative sans contournement est établie sans danger avant d'être répétée sur une PR rouge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| essai de merge → `main` | une tentative qui passerait malgré le blocage ferait atterrir le support |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-23 | Tampering | merge sans contournement accepté par le client ou le serveur | high | mitigate | première tentative sur un support inoffensif ; tentative conditionnée à `BLOCKED` ; résultat vérifié par l'état relu de la PR ; 41-07 conditionné à `refuse` |
| T-41-24 | Repudiation | blocage attribué à tort à la revue | medium | mitigate | CI verte sur les deux événements et `behind_by=0` exigés, décision `REVIEW_REQUIRED` et relecteur `picmakpro` relus |
| T-41-25 | Information disclosure | chemin de machine dans le README qui atterrira sur `main` | low | mitigate | balayage du blob P2 ; `check-machine-paths` en CI sur la PR |
</threat_model>

<verification>
PR-P bloquée par la seule revue code owner, merge sans contournement refusé, preuve consignée.
</verification>

<success_criteria>
Temps (i) de PROT-04 prouvé par un essai réel ; précondition de sûreté de 41-07 établie.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-06-SUMMARY.md` when done — committé sur la branche B.
</output>

---
phase: 31-manifeste-d-install-dry-run-issue-20
plan: 07
type: execute
wave: 5
depends_on: ["31-05"]
files_modified:
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-manifest.sh
autonomous: true
requirements: [MANI-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "uninstall_module retire ce que le MANIFESTE dit avoir été posé, au lieu de le reconstruire depuis le cache (D-31-09)"
    - "Un module disparu du cache reste désinstallable — c'est le trou que retired-modules.txt rattrape aujourd'hui en dur"
    - "Manifeste absent = repli gracieux sur l'énumération de cache actuelle, jamais une désinstallation amputée"
    - "Manifeste imparsable = BRUYANT et NON destructif, exactement comme à la convergence (D-31-07)"
    - "Les six conditions de suppression de D-31-07 s'appliquent identiquement ici — un seul jeu de garde-fous, pas deux"
  artifacts:
    - "plugin/_internal/vibeflow-update.sh : uninstall_module routé sur vf_manifest_read"
    - "plugin/_internal/tests/test-manifest.sh : cas du module absent du cache"
  key_links:
    - "uninstall_module (812-899) -> vf_manifest_read + les six conditions de vf_converge_apply (31-05) : réutilisation, jamais réécriture"
    - "retired-modules.txt (549-570) reste en place pour l'historique du parc, mais cesse de grossir"
  prohibitions:
    - "Aucune duplication des six conditions de suppression — les factoriser ou les appeler, jamais les recopier"
    - "Aucune suppression sur manifeste imparsable"
    - "Aucun retrait de retired-modules.txt ni de cleanup_retired_modules : ils couvrent le parc déjà installé avant cette phase"
    - "Aucun `--dry-run` accepté sur uninstall (D-31-06 inchangé)"
---

<objective>
Le second lecteur du manifeste. `uninstall_module` reconstruit aujourd'hui la liste de ce qu'il doit
retirer en relisant le **cache** — donc il est faux dès que le module a quitté le cache, trou
rattrapé en dur par `retired-modules.txt`. Lui donner le manifeste ferme ce trou et arrête la
croissance du fichier en dur.

Purpose: MANI-01 devient utile au-delà de l'update. `ARCHITECTURE.md` §3.1 le liste comme lecteur
n° 2 ; les critères de succès de la phase ne l'exigent pas.

Output: `uninstall_module` routé sur le manifeste, avec repli gracieux, et un test du cas « module
disparu du cache ».

> **VAGUE ABANDONNABLE — SACRIFICE DÉSIGNÉ EN PREMIER (D-31-09).** Si la phase déborde, c'est CE
> plan qu'on abandonne, avant 31-06. Les quatre critères de succès restent tenus par les vagues 1
> à 4 : aucun d'eux ne porte sur `uninstall`. Le classer abandonnable, c'est décider maintenant ce
> qu'on sacrifie sous pression plutôt que d'improviser à la fin. Si abandonné : le noter dans
> `31-08-SUMMARY.md`, laisser `retired-modules.txt` et `cleanup_retired_modules` intacts (ils
> couvrent déjà le cas), et reporter le geste au backlog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-CONTEXT.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-RESEARCH.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : uninstall_module lit le manifeste, avec repli gracieux</name>
  <files>plugin/_internal/vibeflow-update.sh</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh:812-899 (`uninstall_module` intégral — les 10 gestes de retrait et leurs gardes `[ -d "$CACHE_DIR/$mod/..." ]`, c'est-à-dire l'énumération depuis le cache)
    - plugin/_internal/vibeflow-update.sh:876-883 (l'élagage `rmdir` non récursif des dossiers partagés — à conserver)
    - plugin/_internal/vibeflow-update.sh:536-570 (`find_retired_manifest` / `cleanup_retired_modules` — le rattrapage en dur qui cesse de grossir, mais reste)
    - 31-CONTEXT.md §3 D-31-09 (périmètre : `update` est LE livrable, `uninstall` est la vague abandonnable) et D-31-07 (les six conditions)
    - 31-05-SUMMARY.md (signatures exactes de `vf_manifest_read` et de la garde des six conditions)
    - 31-RECHERCHE-moteur.md §1.6 (« uninstall_module reconstruit la liste DEPUIS le cache — donc fausse dès que le module a disparu du cache »)
  </read_first>
  <action>
Factoriser d'abord la garde des six conditions livrée en 31-05 : extraire de `vf_converge_apply`
une fonction `vf_removable <chemin>` qui porte les conditions (c) existe sur disque, (d) fichier
régulier, (e) résout sous `TARGET_ROOT`, (f) hors liste d'exclusions — les conditions (a) et (b)
restant propres à la convergence (elles portent sur une comparaison de deux manifestes).
`vf_converge_apply` appelle désormais `vf_removable`. UN seul jeu de garde-fous dans le fichier ;
les recopier dans `uninstall_module` serait rejouer la dette des trois énumérations parallèles que
cette phase supprime.

Puis, dans `uninstall_module`, insérer un chemin manifeste AVANT les gestes existants :

- `vf_manifest_read "$mod"` (livré en 31-05). Code 1 (imparsable) : `log` l'abstention, ne rien
  supprimer du tout et retourner — la même règle qu'à la convergence, parce que la suppression est
  irréversible dans les deux cas. Code 2 (absent) : `log` le repli gracieux et EXÉCUTER le chemin
  actuel (énumération depuis le cache) tel quel, sans le modifier — c'est ce qui garde
  désinstallable un lab configuré avant cette phase.
- Code 0 (valide) : pour chaque ligne, résoudre `$TARGET_ROOT/<ligne>`, appliquer `vf_removable`,
  puis `rm -f` et `log "  removed <chemin>"` au format existant. Après la boucle, conserver
  l'élagage `rmdir` non récursif des dossiers partagés (`scripts/tests/fixtures`, `scripts/tests`,
  `scripts`) et l'appliquer aussi aux dossiers parents des chemins retirés.
- Dans les deux cas, `backup_module "$mod"` (ligne 815), `remove_module_hooks "$mod"` (ligne 895)
  et `mark_uninstalled "$mod"` (ligne 897) restent inchangés et au même endroit : les hooks ne sont
  pas des chemins, leur retrait reste le travail de `merge-hooks.sh` (`ARCHITECTURE.md` §3.1).
- Supprimer le manifeste du module lui-même en toute fin, après `mark_uninstalled` — un module
  désinstallé ne laisse pas sa trace derrière lui. Le manifeste est dans la liste close
  d'exclusions, donc il n'est jamais candidat via `vf_removable` : le geste est explicite et séparé.

Ne PAS retirer `retired-modules.txt` ni `cleanup_retired_modules` : ils couvrent les labs déjà
installés sans manifeste. Ajouter un commentaire au-dessus de `find_retired_manifest` disant que ce
fichier cesse de grossir maintenant que le manifeste couvre le cas général.

`--dry-run` reste refusé sur `uninstall` (D-31-06) : ne rien changer à cette garde.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/_internal/vibeflow-update.sh` sort 0.
    - `grep -cE '^vf_removable\(\)' plugin/_internal/vibeflow-update.sh` rend `1`, et `grep -c 'vf_removable' plugin/_internal/vibeflow-update.sh` rend au moins `3` (définition + 2 appelants).
    - Aucune suppression récursive introduite sur le chemin manifeste, LIGNES DE CODE SEULES (commentaires filtrés — sans ce filtre le critère s'auto-invalide dès qu'un commentaire cite la forme interdite) : dans le bloc ajouté à `uninstall_module`, `sed -n '/^uninstall_module()/,/^}/p' plugin/_internal/vibeflow-update.sh | grep -v '^[[:space:]]*#' | grep -c 'rm -rf .*manifest'` rend `0` ; les `rm -rf` PRÉEXISTANTS du chemin d'énumération cache (lignes 819, 830, 850) restent inchangés et ne sont pas visés par ce critère.
    - Le refus de `--dry-run` sur `uninstall` est intact : `bash plugin/_internal/vibeflow-update.sh --dry-run uninstall x` sort `1`.
    - `cleanup_retired_modules` et `find_retired_manifest` existent toujours : `grep -cE '^(cleanup_retired_modules|find_retired_manifest)\(\)' plugin/_internal/vibeflow-update.sh` rend `2`.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh` sortent 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/_internal/vibeflow-update.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>`uninstall_module` retire ce que le manifeste dit, retombe proprement sur l'énumération de cache quand il n'y en a pas, et s'abstient quand il est illisible.</done>
  <reversibility rating="costly">Le verbe le plus destructif du moteur change de source de vérité. Le repli gracieux et la réutilisation de `vf_removable` (une seule garde, déjà éprouvée par les tests de 31-05) sont ce qui borne le risque. Le geste reste revertable, mais c'est précisément pourquoi ce plan est le sacrifice désigné : sous pression, on ne touche pas à `uninstall`.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : preuve du cas « module disparu du cache »</name>
  <files>plugin/_internal/tests/test-manifest.sh</files>
  <read_first>
    - plugin/_internal/tests/test-manifest.sh (état livré par 31-05, cas T1..T20)
    - plugin/_internal/tests/test-vibeflow-update.sh:59-91 (`prepare_module`)
    - 31-CONTEXT.md §4 point 3 (mutation rouge exigée par sa trace, pas par son verdict)
  </read_first>
  <behavior>
    - T21 (le trou fermé) : installer un module, SUPPRIMER son dossier du cache, puis
      `uninstall <mod>` → tous les artefacts du manifeste sont retirés du lab. Aujourd'hui, sans
      manifeste, ce scénario laisse tout en place.
    - T22 (fichier tiers intact) : un fichier tiers déposé dans `.claude/rules/` survit à
      l'`uninstall`, exactement comme il survit à la convergence.
    - T23 (repli gracieux) : un lab dont le manifeste a été supprimé se désinstalle par le chemin
      cache actuel, sans erreur — un parc pré-Phase-31 reste désinstallable.
    - T24 (imparsable) : manifeste corrompu → `uninstall` ne supprime RIEN et le dit fort.
    - T25 (trace nettoyée) : après un `uninstall` réussi, le fichier
      `scripts/.vibeflow-manifest-<mod>` n'existe plus.
  </behavior>
  <action>
Ajouter `T21` à `T25` à `plugin/_internal/tests/test-manifest.sh`, documentés en en-tête selon la
convention `# T<n> — …`, chacun dans son `LAB="$(mktemp -d)"`.

T21 est le cas porteur : `prepare_module` puis install, puis `rm -rf "$CACHE/software-architecture"`,
puis `uninstall software-architecture`. Assertion : chacun des chemins qui étaient dans le manifeste
avant la suppression du cache est absent du lab après. Prendre la liste depuis le manifeste
lui-même AVANT l'uninstall (le test ne réénumère pas — il consomme la même source de vérité que
l'engine, ce qui est exactement la propriété prouvée).

T24 réutilise les quatre formes d'illisibilité déjà couvertes par T17, mais sur le chemin
`uninstall` : l'assertion porte sur les deux moitiés (bruyant ET rien supprimé).

**Mutation rouge de ce lot.** Retirer la condition (f) de `vf_removable` (la liste close
d'exclusions) et vérifier que la suite rougit sur `scripts/vf-portable.sh` retiré à tort — la lib
est propriété de l'engine et partagée : la retirer à la désinstallation d'un module la retirerait
sous les pieds des autres (D-31-03). Consigner l'assertion, l'attendu et l'obtenu dans le SUMMARY.
Si le rouge obtenu ne nomme pas ce fichier, la mutation n'exerce pas la garde : recommencer.

Ajouter au passage l'assertion correspondante dans T22 si elle manque, pour que la mutation ait
une cible.

Mettre à jour le compteur de cas de l'en-tête.
  </action>
  <acceptance_criteria>
    - `bash plugin/_internal/tests/test-manifest.sh` sort 0 et affiche `T21` à `T25`.
    - T21 dérive sa liste attendue du manifeste réel, pas d'une liste codée en dur — vérifiable à la lecture du bloc, qui ne contient aucun chemin de fichier littéral autre que le chemin du manifeste.
    - Mutation rouge tracée dans `31-07-SUMMARY.md` : condition (f) retirée, assertion rouge nommant `scripts/vf-portable.sh`, avec attendu et obtenu.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh` sortent 0.
    - Anti-vert-à-vide toujours actif.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-manifest.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>Le cas « module disparu du cache » est prouvé fermé, et le fichier tiers comme la lib de l'engine survivent à la désinstallation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifeste sur disque -> `uninstall_module` | même entrée non fiable qu'à la convergence, mais sur le verbe le plus destructif du moteur |
| artefacts d'un module -> artefacts partagés (`scripts/vf-portable.sh`, `.claude/memory/`) | une désinstallation qui déborde retire des biens communs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-22 | Tampering | `uninstall_module` retirant la lib partagée de l'engine | high | mitigate | condition (f) de `vf_removable` (liste close D-31-03) — `scripts/vf-portable.sh` n'est jamais candidat ; la mutation rouge de la tâche 2 prouve que le test discrimine |
| T-31-01 / T-31-02 / T-31-03 / T-31-04 | Tampering | traversée `..`, chemin absolu, CRLF, ligne répertoire | high | mitigate | `vf_manifest_read` + `vf_removable` sont les MÊMES fonctions qu'à la convergence — un seul jeu de garde-fous, déjà éprouvé par T17 ; T24 les rejoue sur le chemin `uninstall` |
| T-31-23 | Denial of Service | lab pré-Phase-31 rendu non désinstallable | high | mitigate | repli gracieux code 2 : le chemin d'énumération de cache actuel est CONSERVÉ intact et emprunté quand le manifeste manque ; T23 le prouve |
| T-31-24 | Repudiation | manifeste orphelin laissé après désinstallation | low | mitigate | suppression explicite du manifeste après `mark_uninstalled` ; T25 |
| T-31-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par cette phase (31-RESEARCH.md § Package Legitimacy Audit : N/A) |
</threat_model>

<verification>
1. `bash -n plugin/_internal/vibeflow-update.sh`
2. `bash plugin/_internal/tests/test-manifest.sh`
3. `bash plugin/_internal/tests/test-vibeflow-update.sh`
4. `bash plugin/_internal/tests/test-merge-hooks.sh`
5. `bash plugin/_internal/vibeflow-update.sh --dry-run uninstall x` sort 1 (garde D-31-06 intacte)
6. Preuve APRÈS commit :
   `T=$(mktemp -d); git archive HEAD | tar -x -C "$T"; for s in test-manifest test-vibeflow-update test-merge-hooks; do bash "$T/plugin/_internal/tests/$s.sh" || echo "FAIL $s"; done; rm -rf "$T"`
</verification>

<success_criteria>
- Un module disparu du cache est intégralement désinstallable.
- Les six conditions de suppression sont factorisées et appelées, jamais recopiées.
- Un lab sans manifeste reste désinstallable par le chemin actuel, inchangé.
- Un manifeste imparsable ne supprime rien et le dit fort.
- `retired-modules.txt` reste en place mais cesse de grossir, et le code le dit.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribution de ce plan)

**Nouvelle fonction dans `plugin/_internal/vibeflow-update.sh`**
- `vf_removable <chemin>` — les conditions (c) à (f) de D-31-07, factorisées, appelées par
  `vf_converge_apply` ET `uninstall_module`

**Nouveaux cas de suite**
- `T21` (module disparu du cache), `T22` (fichier tiers intact), `T23` (repli gracieux),
  `T24` (imparsable sur uninstall), `T25` (manifeste nettoyé) dans
  `plugin/_internal/tests/test-manifest.sh`
</artifacts_produced>

<output>
Créer `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-07-SUMMARY.md` à la fin,
avec la trace de la mutation rouge (assertion, attendu, obtenu).
Si ce plan est ABANDONNÉ, écrire quand même un SUMMARY d'une page disant qu'il l'a été et pourquoi.
</output>

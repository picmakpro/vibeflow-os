---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 05
lot: L2 — auditeur de densité distribué (module reference, sans bump)
type: execute
wave: 2
depends_on: ["40.1-01"]
autonomous: true
requirements: [BUDG-04, QUAL-01]
files_modified:
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/validate_gate.sh
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/measure.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-auditor-gate.sh
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/plan_migration.py
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/SKILL.md
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/agent_anatomy.md
  - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/migration_patterns.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-05 — l'auditeur distribué ne bloque JAMAIS avant 301 : `validate_gate.sh` sort 1 sur un body de 301 lignes, 0 sur 300 ; il émet un WARNING non bloquant `zone d'avertissement ADR-029` pour un body de 251 à 300 lignes, et rien pour 250"
    - "D-05 — les paliers indicatifs sont réalignés : `measure.sh` classe OK sous 251, WARN de 251 à 300, HEAVY de 301 à 400, CRITICAL au-delà de 400 ; `thresholds.md` et `SKILL.md` publient les mêmes paliers, jetons ×12 recalculés (OK < 3012, WARN 3012-3600, HEAVY 3612-4800, CRITICAL > 4800)"
    - "D-06 — l'auditeur continue de compter le BODY hors frontmatter : aucune ligne de calcul de `body_lines` ni la mesure `wc -l` des scripts n'est modifiée, et aucun énoncé « body » ne devient « fichier »"
    - "QUAL-01 — la frontière 300/301 est prouvée sur fixture et trois mutants de `validate_gate.sh` sont TUÉS (blocage décalé de un, avertissement neutralisé, plafond remis à l'ancienne valeur)"
    - "Plus aucune valeur dérivée de l'ancien plafond ne subsiste dans l'auditeur : ni bande `201-250`, ni `251-400`, ni jetons `2412`/`3012 - 4800` en HEAVY, ni `THRESHOLD_WARN=` à l'ancienne valeur ; le recensement ne liste plus aucune ligne sous `agent-density-auditor/`"
  artifacts:
    - path: "plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/validate_gate.sh"
      provides: "MAX_BODY_LINES=300, WARN_BODY_FROM=251, warning de zone"
      contains: "MAX_BODY_LINES=300"
    - path: "plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/measure.sh"
      provides: "paliers THRESHOLD_WARN_FROM=251 / THRESHOLD_HEAVY_FROM=301 / THRESHOLD_HEAVY=400"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-auditor-gate.sh"
      provides: "preuve de frontière 250/251/300/301 et mutants MUT-G1..G3"
  key_links:
    - "`SKILL.md` frontmatter `description:` : scalaire YAML plain avec guillemet ET apostrophe, couvert par T23 de `test-check-description-fidelity.sh` — remplacement de valeur seul, aucun ` : ` introduit"
---

<objective>
Réaligner l'auditeur de densité distribué (`plugin/reference/content/methodology/templates/skills/agent-density-auditor/`)
sur D-01, D-05 et D-06 (arbitrages Samuel, AskUserQuestion session principale, relais SendMessage,
2026-09-16) : bloquer au-delà de 300 lignes de body, avertir sans bloquer dès 251, garder la mesure
du body. Ce module n'a aucune suite de tests (`find` exécuté en recherche, 0 résultat) : la preuve
rouge est un outil versionné du dossier de phase, joué avant et après.

Le bump du module `reference` est porté par le plan 40.1-06 (vague 3), qui partage la triade : ce
plan n'en touche aucun fichier.

Purpose: BUDG-04 sur la surface distribuée aux labs.
Output: deux scripts bloquant/indicatif réalignés, un outil de preuve, cinq fichiers de doctrine
et d'utilitaire alignés.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-RESEARCH.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : frontière bloquante 300/301 et zone d'avertissement dans validate_gate.sh et measure.sh, prouvées par fixture et mutants</name>
  <files>plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/validate_gate.sh, plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/measure.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-auditor-gate.sh</files>
  <read_first>
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/validate_gate.sh (en entier ; L9-12 seuils, L33-51 calcul du body et check)
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/measure.sh (en entier ; L11-15 seuils, L38-42 classification, L50-56 pied)
    - plugin/software-architecture/scripts/check-file-size.sh (patron warn/block : le warning n'influe jamais sur la sortie)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (patron mutant awk + `cmp -s` + `bash -n`, `ko` à trois champs)
  </read_first>
  <behavior>
    - body 250 → validate_gate exit 0, aucun `zone d'avertissement ADR-029` ; measure OK
    - body 251 → exit 0 + warning de zone ; measure WARN
    - body 300 → exit 0 + warning de zone ; measure WARN
    - body 301 → exit 1 + `VIOLATION: Prompt systeme trop long` ; measure HEAVY
    - body 400 → measure HEAVY ; body 401 → measure CRITICAL
  </behavior>
  <action>
`validate_gate.sh` : `MAX_BODY_LINES=300` ; nouvelle constante `WARN_BODY_FROM=251` sous le
commentaire `# Seuils ADR-029` (commentaire complété : avertissement non bloquant dès 251, blocage
au-delà de 300, lignes hors frontmatter — D-05/D-06, Phase 40.1). Dans « 2. Check lignes body »,
garder la ligne `if (( body_lines > MAX_BODY_LINES )); then` à l'identique, puis insérer une branche
écrite EXACTEMENT `  elif (( body_lines >= WARN_BODY_FROM )); then` qui appelle `warn` avec le texte
`Zone d'avertissement ADR-029 : $body_lines lignes body (avertissement des $WARN_BODY_FROM, bloque au-dela de $MAX_BODY_LINES) — surveiller, non bloquant`,
puis le `else ok` existant. Calcul de `body_lines`, `wc -l` et frontmatter : inchangés (D-06).

`measure.sh` : remplacer les trois seuils par `THRESHOLD_WARN_FROM=251`, `THRESHOLD_HEAVY_FROM=301`,
`THRESHOLD_HEAVY=400` (plus aucune constante à l'ancienne valeur ni `THRESHOLD_OK`) ; classification :
OK si `body_lines < THRESHOLD_WARN_FROM`, WARN si `< THRESHOLD_HEAVY_FROM`, HEAVY si `<= THRESHOLD_HEAVY`,
sinon CRITICAL ; pied de tableau : `Seuils ADR-029 : OK <251 / WARN 251-300 / HEAVY 301-400 / CRITICAL >400 (lignes hors frontmatter) — bloquant au-dela de 300 (validate_gate.sh)`
construit à partir des constantes. Sortie toujours `exit 0` (tableau de bord).

`tools/prove-auditor-gate.sh` : fixtures en `mktemp -d`, chaque agent = 6 lignes de frontmatter
(`---`, `name: demo`, `description: agent de demonstration`, `skills:`, `  - demo`, `---`) + N lignes
de body neutres avec un titre `## Section k` toutes les 50 lignes (pour ne pas confondre avec le
warning « section trop longue »). Assertions tracées assertion/attendu/obtenu, sur les scripts
VIVANTS du dépôt, selon `<behavior>`. validate_gate : exit + présence/absence de
`zone d'avertissement ADR-029`, recherche insensible à la casse par awk. `validate_gate.sh` écrit
TOUT son verdict (`OK:`, `WARNING:`, `VIOLATION:`, `GATE …`) sur **stderr** : chaque invocation est
capturée `> fichier 2>&1`, et chaque assertion de PRÉSENCE comme d'ABSENCE lit ce fichier fusionné.
Garde anti-vide (constat W5 du checker, révision 1) : avant toute assertion d'absence, exiger dans
la même capture une ligne `GATE PASSED` ou `GATE FAILED` ; sans elle, ko tracé « capture vide ». Pour
`measure.sh` (sortie standard) : colonne STATUS de la ligne de la fixture. Mutants sur COPIE de `validate_gate.sh` (awk ligne exacte, rejet si `cmp -s`
identique ou `bash -n` invalide) : MUT-G1 `if (( body_lines > MAX_BODY_LINES )); then` → `>=` ;
fixture 300 → mutant exit 1, original 0. MUT-G2 `  elif (( body_lines >= WARN_BODY_FROM )); then` →
`  elif (( 0 )); then` ; fixture 260 → mutant sans warning de zone, original avec. MUT-G3
`MAX_BODY_LINES=300` → `MAX_BODY_LINES=250` (retour à l'ancien comportement) ; fixture 260 → mutant
exit 1, original 0. rc 0 si tout tient, 1 sinon. Contrôle rouge d'avant-édition : lancé sur l'arbre
AVANT la modification des deux scripts, l'outil doit rendre 1 (consigner dans le SUMMARY).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-auditor-gate.sh</automated>
    <fails_when>exit non nul, ou une assertion imprimant `obtenu :`, ou l'un des mutants MUT-G1/G2/G3 non tué</fails_when>
    <automated>bash --noprofile --norc -e -c 'A=plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts; bash -n "$A/validate_gate.sh"; bash -n "$A/measure.sh"; T=$(mktemp -d); git show 4e7c8e4:"$A/validate_gate.sh" > "$T/v"; git show 4e7c8e4:"$A/measure.sh" > "$T/m"; n=$(awk "/body_lines=|end_fm=|wc -l/" "$T/v" "$T/m" | cksum); p=$(awk "/body_lines=|end_fm=|wc -l/" "$A/validate_gate.sh" "$A/measure.sh" | cksum); echo "mesure body inchangee : avant=[$n] apres=[$p]"; [ "$n" = "$p" ]'</automated>
    <fails_when>exit non nul, ou `avant=` différent de `apres=` (le calcul du body ou le comptage a été touché depuis la base de phase `4e7c8e4`, D-06)</fails_when>
  </verify>
  <acceptance_criteria>
    - `validate_gate.sh` contient `MAX_BODY_LINES=300`, `WARN_BODY_FROM=251`, la ligne `  elif (( body_lines >= WARN_BODY_FROM )); then`
    - `measure.sh` contient `THRESHOLD_WARN_FROM=251`, `THRESHOLD_HEAVY_FROM=301`, `THRESHOLD_HEAVY=400`, et plus aucune ligne `THRESHOLD_OK=`
    - `prove-auditor-gate.sh` rend 0 après édition et rendait 1 avant (consigné au SUMMARY)
    - chaque invocation de `validate_gate.sh` dans l'outil est capturée avec `2>&1`, et chaque assertion d'absence est précédée de la garde `GATE PASSED|GATE FAILED` présente
    - lignes de calcul du body et de comptage identiques avant/après (cksum)
  </acceptance_criteria>
  <done>L'auditeur distribué bloque au-delà de 300, avertit de 251 à 300, garde sa mesure du body, et la frontière est prouvée capable de rougir.</done>
</task>

<task type="auto">
  <name>Task 2 : doctrine et utilitaire de l'auditeur réalignés (paliers, jetons, énoncés)</name>
  <files>plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/plan_migration.py, plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md, plugin/reference/content/methodology/templates/skills/agent-density-auditor/SKILL.md, plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/agent_anatomy.md, plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/migration_patterns.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `agent-density-auditor/` (liste re-dérivée à l'exécution)
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md (tableau récapitulatif et tableau de classification, en entier)
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/SKILL.md (frontmatter `description:` L3 ; L11, L34, L60 ; bloc « Classification » ~L98-102 ; « Verifications » ~L134)
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/agent_anatomy.md, references/migration_patterns.md, scripts/plan_migration.py (lignes listées)
    - plugin/conductor/scripts/tests/test-check-description-fidelity.sh (T20-T25 : `SKILL.md` de l'auditeur dans la classe « plain à guillemet et apostrophe »)
  </read_first>
  <action>
`plan_migration.py` : `THRESHOLD_AGENT_LINES = 300` ; texte de sortie de l'étape 5 : conformité
« bloque au-dela de 300L, avertissement des 251 ». `thresholds.md` : ligne Agent du tableau
récapitulatif → seuil `avertissement des 251 lignes, bloque au-dela de 300 (hors frontmatter)`,
sanction `Bloquant (gate) au-dela de 300, warning des 251` ; tableau de classification remplacé par
quatre lignes : `OK` `< 251` `< 3012` Conforme — surveillance passive ; `WARN` `251 - 300`
`3012 - 3600` Zone d'avertissement — non bloquant, surveiller la croissance ; `HEAVY` `301 - 400`
`3612 - 4800` Bloque (gate) — plan_migration.py ; `CRITICAL` `> 400` `> 4800` Bloque (gate) — refacto
obligatoire. `SKILL.md` : bloc Classification aligné sur les mêmes quatre paliers (`< 251L → OK`,
`251-300L → WARN (avertissement, non bloquant)`, `301-400L → HEAVY (bloque)`, `> 400L → CRITICAL (bloque)`) ;
Iron Law 1 → `Agent : avertissement des 251 lignes, bloque au-dela de 300` (hors frontmatter
conservé) ; « Verifications » → `Lignes hors frontmatter : bloque au-dela de 300, warning des 251` ;
L11 et L60 → valeur 300 ; frontmatter `description:` → REMPLACEMENT DE VALEUR SEUL
(`Agent ≤300L`), aucun deux-points suivi d'espace ajouté, longueur quasi inchangée.
`agent_anatomy.md` : `Max ADR-029  : 300L (avertissement des 251)` et case à cocher
`Total body : bloque au-dela de 300 lignes (validate_gate.sh exit 0 jusqu'a 300)` ;
`migration_patterns.md` : valeur 300 aux deux lignes listées. D-06 : partout, « body » / « hors
frontmatter » est conservé tel quel. Formulation (manager) : jamais « OK ≤ » l'ancien plafond.
Aucune autre section réécrite.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /agent-density-auditor\\// {c++} END{print c+0}" "$T/o"); d=$(awk "/201 - 250|201-250L|251-400|251 - 400|2412|THRESHOLD_OK=|THRESHOLD_WARN=/{c++} END{print c+0}" plugin/reference/content/methodology/templates/skills/agent-density-auditor/SKILL.md plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/measure.sh); w=$(awk "/251 - 300/{c++} END{print c+0}" plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md); echo "auditeur : vivants=$n derives_anciens=$d bande_WARN_251_300=$w"; [ "$n" -eq 0 ] && [ "$d" -eq 0 ] && [ "$w" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0, `derives_anciens=` différent de 0, ou `bande_WARN_251_300=` différent de 1</fails_when>
    <automated>bash --noprofile --norc -e -c 'python3 -m py_compile plugin/reference/content/methodology/templates/skills/agent-density-auditor/scripts/plan_migration.py && echo py_compile_ok; bash plugin/conductor/scripts/tests/test-check-description-fidelity.sh > /dev/null 2>&1 && echo description_fidelity_ok'</automated>
    <fails_when>exit non nul, ou absence de `py_compile_ok` ou de `description_fidelity_ok` (frontmatter YAML du SKILL.md cassé)</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-auditor-gate.sh</automated>
    <fails_when>exit non nul (non-régression de la tâche 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - recensement : zéro ligne vivante sous `agent-density-auditor/`
    - `thresholds.md` porte exactement une ligne `251 - 300` et aucune de `201 - 250`, `251 - 400`, `2412`
    - `SKILL.md` porte `251-300L → WARN` et `301-400L → HEAVY` ; `test-check-description-fidelity.sh` vert
    - `plan_migration.py` compile et contient `THRESHOLD_AGENT_LINES = 300`
  </acceptance_criteria>
  <done>La doctrine distribuée de l'auditeur publie les mêmes paliers que ses scripts, sans aucune trace vivante de l'ancien plafond.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template distribué → hook `PreToolUse` d'un lab | le lab consommateur bloque l'écriture d'un agent sur la sortie de `validate_gate.sh` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-15 | Denial of service | `validate_gate.sh` bloquant avant 301 dans un lab | medium | mitigate | frontière 300/301 prouvée sur fixture ; MUT-G1 et MUT-G3 tués |
| T-40.1-16 | Repudiation | avertissement 251-300 silencieux | low | mitigate | MUT-G2 tué ; paliers `measure.sh` assertés |
| T-40.1-17 | Tampering | unification involontaire body/fichier (D-06) | low | mitigate | cksum des lignes de calcul du body identique avant/après |
</threat_model>

<verification>
`prove-auditor-gate.sh` rouge avant, vert après ; recensement nul sous l'auditeur ; description
fidelity verte ; py_compile OK.
</verification>

<success_criteria>
L'auditeur distribué applique D-05 (bloque au-delà de 300, avertit dès 251) et D-06 (body), preuve
rouge rejouable versionnée.
</success_criteria>

## Artifacts this phase produces

- `validate_gate.sh` : constante `WARN_BODY_FROM`, branche d'avertissement de zone
- `measure.sh` : constantes `THRESHOLD_WARN_FROM`, `THRESHOLD_HEAVY_FROM` (suppression de `THRESHOLD_OK`, `THRESHOLD_WARN`)
- `tools/prove-auditor-gate.sh` : mutants `MUT-G1`, `MUT-G2`, `MUT-G3`

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-05-SUMMARY.md` when done
</output>

---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 04
lot: L04 — discover-unintegrated-docs.sh : registre élargi à chaque compartiment (D-04 catégorie c)
type: execute
wave: 2
depends_on: ["41.1-01"]
autonomous: true
requirements: [WSAW-02]
files_modified:
  - plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
  - plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` ou sa suite de tests porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh`). CORRECTION (INFO, juge frais) : la raison initiale (« câblé en SessionStart, donc un hook au sens de G-2 ») est FAUSSE — la clause « hook » de G-2 est un préfixe de CHEMIN (`scripts/hooks/`), pas une propriété du moment de déclenchement. Ce script relève de G-2 au titre de son APPARTENANCE AU RECENSEMENT de consommateurs de planning modifié par cette phase, pas au titre de « hook »"
    - "Le registre de citation, aujourd'hui les quatre fichiers racine `$PLANNING_DIR/{ROADMAP,REQUIREMENTS,MILESTONES,PROJECT}.md` (absents sous le layout partitionné, donc muets à tort — D-04 : « 4 faux positifs sur 5 »), devient la CONCATÉNATION de ces mêmes quatre fichiers pour CHAQUE compartiment énuméré par `vf_ws_enumerate`, PLUS le `PROJECT.md` racine lu SÉPARÉMENT et INCONDITIONNELLEMENT (hors de la boucle — il reste hors compartiment par nature, D-04)"
    - "Le fichier `$ADR_FILE` (docs/ADR.md, racine) reste lu UNE SEULE FOIS, hors boucle — ce n'est PAS un artefact de compartiment"
    - "`.planning/phases/**` reste EXCLU du registre (inchangé — ce sont des sorties du moteur, pas des entrées), y compris `.planning/workstreams/<nom>/phases/**`"
    - "La règle de citation (basename borné des deux côtés, lignes glob ignorées) N'EST PAS TOUCHÉE — seule la LISTE des fichiers concaténés dans `$REG_TMP` change"
    - "Le contrat de sortie (grain<TAB>chemin, exit 0/3/64, `--hook` traduit 3→0) est INCHANGÉ — ce plan ne modifie AUCUN code de sortie, uniquement l'univers des registres consultés"
    - "Un document cité dans le ROADMAP d'UN SEUL compartiment est considéré intégré — la citation n'a pas besoin d'être répétée dans tous les compartiments (c'est déjà le comportement naturel d'une concaténation en un seul buffer `$REG_TMP`, pas une nouvelle règle à écrire)"
    - "QUAL-01 : un mutant qui neutralise la boucle sur `vf_ws_enumerate` (revient à ne lire que la racine) doit faire réapparaître le faux positif : un document cité UNIQUEMENT dans le ROADMAP d'un compartiment doit alors ressortir « non intégré » à tort sur le mutant, correctement « intégré » (absent de la sortie) sur l'original"
  artifacts:
    - path: "plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh"
      provides: "registre élargi à chaque compartiment énuméré, PROJECT.md racine hors boucle"
      contains: "vf_ws_enumerate"
  key_links:
    - "Bug exact : lignes ~120-124, boucle `for r in \"$PLANNING_DIR/ROADMAP.md\" ...\"$PLANNING_DIR/PROJECT.md\" \"$PLANNING_DIR/milestones\"/*.md \"$ADR_FILE\"` — à remplacer par une boucle externe sur les compartiments énumérés, PLUS les deux lectures hors-compartiment (`PROJECT.md` racine, `$ADR_FILE`) conservées telles quelles"
    - "Ce script vit dans `dev-orchestrator/scripts/`, PAS `planning-core` — il doit sourcer `workstream-policy.sh` via la recherche à DEUX candidats de `check-divergence.sh` (le module `dev-orchestrator` requiert `conductor`, qui requiert `planning-core` par transitivité — donc le fichier existe quelque part dans la chaîne d'install, mais son chemin relatif exact depuis ce script est à vérifier, pas supposé identique à celui de `check-divergence.sh`)"
    - "Guard SILENCE existant (`.planning/ absent`, lignes ~100-104) N'EST PAS touché — c'est un cas différent (aucun `.planning/` du tout), pas celui que ce plan corrige"
---

<objective>
Élargir le registre de citation de `discover-unintegrated-docs.sh` à chaque compartiment de
workstream présent sur le disque, au lieu des quatre fichiers racine (aujourd'hui absents sous le
layout partitionné, donc silencieusement faux — D-04 mesure 4 faux positifs sur 5 mesurés le
2026-09-23).

Purpose: fermer le dernier des trois consommateurs de catégorie (c) « suppose la racine ».
Output: un hook d'hygiène documentaire qui cite correctement chaque compartiment, sans changer son
contrat de sortie ni sa posture non bloquante.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` en entier (185 lignes)
- `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` en entier
- `plugin/conductor/scripts/check-divergence.sh` lignes ~94-102 (recherche à deux candidats du
  sourcing de `workstream-policy.sh` — patron à répliquer, chemin relatif à recalculer pour ce
  répertoire)
</context>

<discipline>
- `diff`/`ls` proxifiés — `rtk proxy` ; comparaisons `cmp`/`comm`/`cksum`.
- Fixtures `mktemp -d`, jamais le dépôt réel.
- Ne pas toucher à la règle de citation (`is_cited`) ni au format de sortie — seul l'univers des
  fichiers concaténés dans `$REG_TMP` change.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : boucle sur `vf_ws_enumerate`, `PROJECT.md` racine et `$ADR_FILE` hors boucle</name>
  <files>plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh</files>
  <behavior>
    - un document `docs/superpowers/specs/x.md` cité UNIQUEMENT dans
      `.planning/workstreams/dev/ROADMAP.md` → considéré INTÉGRÉ (absent de la sortie « non
      intégré »), alors qu'AVANT ce plan il ressortait « non intégré » (faux positif, registre
      racine vide sous layout partitionné).
    - un document cité NULLE PART (ni dans aucun compartiment, ni dans `PROJECT.md` racine, ni
      dans `$ADR_FILE`) → reste « non intégré », comportement inchangé.
    - `.planning/workstreams/dev/phases/**` n'est jamais consulté comme registre, même s'il
      contient incidemment un fichier nommé `ROADMAP.md` (il n'en contient jamais dans ce dépôt,
      mais la boucle ne doit lire QUE `<compartiment>/ROADMAP.md` au premier niveau, jamais
      récursivement).
    - `.planning/` absent → comportement inchangé (SILENCE existant, non touché).
  </behavior>
  <action>
Résoudre le sourcing de `workstream-policy.sh` avec la même recherche à deux candidats que
`check-divergence.sh` (module différent, chemin relatif à recalculer depuis
`plugin/dev-orchestrator/scripts/` vers `plugin/planning-core/scripts/workstream-policy.sh` —
probablement `$(dirname "$0")/../../planning-core/scripts/workstream-policy.sh`, à VÉRIFIER par
`ls`/`cd` avant d'écrire le chemin en dur, ce script n'a pas encore de dépendance sourcée
aujourd'hui). En l'absence des deux candidats, dégrader en SILENCE sur le registre par compartiment
(garder le comportement racine-seule d'avant ce plan plutôt qu'échouer un hook advisory) — écrire
cette dégradation explicitement en commentaire, cohérent avec la posture « jamais bloquant » du
script.

Remplacer la boucle de concaténation (lignes ~120-124) :
```bash
for r in "$PLANNING_DIR/ROADMAP.md" "$PLANNING_DIR/REQUIREMENTS.md" "$PLANNING_DIR/MILESTONES.md" \
         "$PLANNING_DIR/PROJECT.md" "$PLANNING_DIR/milestones"/*.md "$ADR_FILE"; do
  [ -f "$r" ] && cat "$r" >> "$REG_TMP"
done
```
par une boucle externe sur les compartiments énumérés, PLUS les deux lectures hors-compartiment
conservées :
```bash
# --- Registres PAR COMPARTIMENT, plus les deux fichiers hors-compartiment par nature -----------
if [ -n "$WS_POLICY" ]; then
  while IFS= read -r _wsdir; do
    [ -n "$_wsdir" ] || continue
    for r in "$_wsdir/ROADMAP.md" "$_wsdir/REQUIREMENTS.md" "$_wsdir/MILESTONES.md" \
             "$_wsdir/PROJECT.md" "$_wsdir/milestones"/*.md; do
      [ -f "$r" ] && cat "$r" >> "$REG_TMP"
    done
  done < <(vf_ws_enumerate "$PLANNING_DIR" 2>/dev/null)
fi
# Repli racine — layout legacy non partitionné, ou compartiment sans workstreams/ : mêmes quatre
# fichiers lus À LA RACINE de $PLANNING_DIR, comportement identique à avant ce plan.
for r in "$PLANNING_DIR/ROADMAP.md" "$PLANNING_DIR/REQUIREMENTS.md" "$PLANNING_DIR/MILESTONES.md" \
         "$PLANNING_DIR/PROJECT.md" "$PLANNING_DIR/milestones"/*.md; do
  [ -f "$r" ] && cat "$r" >> "$REG_TMP"
done
# $ADR_FILE reste hors compartiment par nature — lu UNE SEULE FOIS, inchangé.
[ -f "$ADR_FILE" ] && cat "$ADR_FILE" >> "$REG_TMP"
```
Le repli racine reste présent délibérément (pas un `elif`) : un dépôt PARTIELLEMENT partitionné
(certains fichiers encore à la racine en plus des compartiments) doit voir LES DEUX univers, sans
qu'aucune condition n'ait à trancher lequel prévaut — une citation dans l'un OU l'autre suffit à
marquer intégré, cohérent avec la sémantique déjà en place (concaténation en un seul buffer).
`$WS_POLICY` : nommer la variable qui porte le résultat de la recherche à deux candidats
(introduite par ce plan), initialisée à vide si aucun candidat n'est trouvé.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh && echo "syntaxe OK"'</automated>
    <fails_when>`bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/.planning/workstreams/dev" "$T/docs/superpowers/specs"
printf "# spec\n" > "$T/docs/superpowers/specs/x.md"
printf "%s\n" "## Phase 1" "Voir x.md pour contexte." > "$T/.planning/workstreams/dev/ROADMAP.md"
rc=0; out=$(bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --path "$T" 2>&1) || rc=$?
echo "cite_dans_compartiment rc=$rc"; printf "%s\n" "$out"
case "$out" in *"x.md"*) echo "REGRESSION: x.md encore signale non integre"; exit 1;; esac
'</automated>
    <fails_when>la sortie contient `x.md` — le document est cité dans le ROADMAP du compartiment `dev`, il ne doit plus ressortir « non intégré »</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/.planning/workstreams/dev" "$T/docs/superpowers/specs"
printf "# spec orpheline\n" > "$T/docs/superpowers/specs/orphan.md"
printf "%s\n" "## Phase 1" "Rien a voir." > "$T/.planning/workstreams/dev/ROADMAP.md"
rc=0; out=$(bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --path "$T" 2>&1) || rc=$?
echo "non_cite rc=$rc"; printf "%s\n" "$out"
case "$out" in *"orphan.md"*) : ;; *) echo "REGRESSION: orphan.md non signale"; exit 1;; esac
'</automated>
    <fails_when>`orphan.md` n'apparaît PAS dans la sortie — un document réellement non cité doit continuer d'être signalé (non-régression)</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n` passe
    - un document cité dans le ROADMAP d'UN compartiment est considéré intégré
    - un document non cité nulle part reste signalé « non intégré »
    - `PROJECT.md` racine et `$ADR_FILE` restent lus une seule fois, hors boucle
  </acceptance_criteria>
  <done>Le registre couvre chaque compartiment présent sur le disque, sans perdre la lecture des deux fichiers hors-compartiment.</done>
</task>

<task type="auto">
  <name>Task 2 : suite — cas compartiment + mutant de discriminance, rejeu tests</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` en entier (structure à suivre)
  </read_first>
  <action>
Ajouter deux cas : « document cité dans le ROADMAP d'un compartiment → intégré » (non-régression du
faux positif D-04) et « document non cité nulle part → signalé, inchangé ». Rejouer tous les cas
préexistants sans les modifier.

MUTANT — neutraliser la boucle `while IFS= read -r _wsdir` (le remplacer par une boucle qui ne
s'exécute jamais, ex. `false && while ...`) : le cas « cité dans le compartiment » doit alors
redevenir un faux positif signalé, l'original ne le signale pas. Trace `✓ MUT-1 TUE`.

Rejouer `tests` complet, consigner le bilan au SUMMARY.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh</automated>
    <fails_when>exit non nul ; ou une ligne `✗` ; ou `MUT-1` absent de la sortie</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - deux nouveaux cas verts, cas préexistants non modifiés et verts
    - MUT-1 tué
    - rejeu `tests` vert
  </acceptance_criteria>
  <done>Le hook a une preuve par mutation que son registre élargi discrimine réellement le cas partitionné.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenu de compartiment → registre concaténé | chaque `ROADMAP.md`/`REQUIREMENTS.md`/`MILESTONES.md`/`PROJECT.md` de chaque compartiment énuméré est lu et concaténé dans un buffer temporaire — un compartiment volumineux dégraderait la latence du hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-08 | Denial of service | de nombreux compartiments avec de gros fichiers ralentiraient chaque invocation `SessionStart` | low | accept | posture advisory déjà tolérante (`hook_exit`, `|| true` côté câblage) ; le nombre de compartiments sur un lab réel reste faible (2 sur ce dépôt) |
</threat_model>

<verification>
`bash -n` ; document cité dans un compartiment → intégré ; document orphelin → signalé,
non-régression ; suite verte avec MUT-1 tué ; rejeu `tests` vert.
</verification>

<success_criteria>
`discover-unintegrated-docs.sh` cite correctement chaque compartiment présent sur le disque, ferme
le faux positif mesuré (4 sur 5) sans changer son contrat de sortie ni sa posture non bloquante.
</success_criteria>

## Artifacts this phase produces

- `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` — registre par compartiment.
- `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` — cas + MUT-1.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-04-SUMMARY.md` when done
</output>

---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md
autonomous: false
requirements: [SKIL-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le go/no-go SKIL-01 n'est PAS rendu sur pièces (D-07) : un spike MESURÉ PAR EXÉCUTION établit d'abord si un skill posé par le canal natif `/plugin` atteint réellement un sous-agent doté de l'outil `Skill`. Les citations verbatim de la documentation officielle rassemblées par `34-RESEARCH.md` § Volet 3 sont un point de départ tertiaire, jamais une conclusion — dans ce dépôt, une preuve incapable de rendre rouge ne compte pas."
    - "Le CONTRÔLE NÉGATIF fait partie de la mesure, pas seulement le cas positif : un sous-agent doté de `Skill` à qui l'on demande d'invoquer `skil01-probe-nonexistant` (nom absent partout) DOIT échouer. Si ce contrôle réussit, l'appareil de mesure ne discrimine rien et le spike entier est déclaré MESURE INVALIDE — ni GO ni NO-GO n'en sort, et l'item BACKLOG reste ouvert."
    - "Le spike isole la variable « canal de pose » de la variable « scope » : les 8 skills bruts déjà posés par `vibeflow-update.sh` sous la racine de skills de l'utilisateur ne sont PAS des skills de plugin au sens de la documentation (ils ne passent pas par `claude plugin install`). Le spike crée donc un skill de test DÉDIÉ, distinct de toute infrastructure VibeFlow existante, et ne confond jamais un fichier `SKILL.md` invoqué par l'outil `Skill` avec une slash command auto-découverte par le canal plugin."
    - "Le verdict distingue explicitement, en toutes lettres, les DEUX questions que Pitfall 5 de `34-RESEARCH.md` sépare : (a) « le canal atteint-il un sous-agent qui A l'outil `Skill` » — la vraie question de D-07 ; (b) « tous les agents VF peuvent-ils déjà invoquer des skills » — non, et c'est un choix d'architecture VF distinct (18 des 25 agents distribués n'ont pas `Skill` dans leur ligne d'outils, mesuré le 2026-09-15), hors périmètre de cette phase. Un verdict qui omettrait cette distinction produirait un cadrage trompeur pour une éventuelle phase de câblage."
    - "GO SEULEMENT SI un trou mesuré existe ET que l'engine actuel peut le fermer (D-07). La deuxième moitié de la conjonction est écrite et argumentée dans la note : `vibeflow-update.sh` pose déjà des fichiers `SKILL.md` et outille déjà backup, rollback et désinstallation — un GO réutiliserait ce chemin, il n'en ouvrirait pas un second (Pitfall 12 : un installeur global avec sa propre logique de copie est un signal d'alarme explicite)."
    - "Aucune ligne de code d'installeur n'est écrite dans cette phase, même sur GO (D-09) : un GO produit une SPEC DE CÂBLAGE et un item de phase ultérieure, rien d'autre. Périmètre d'un éventuel GO = le câblage, jamais un catalogue ; collision de nom = refus explicite, jamais d'écrasement silencieux."
    - "Le spike est JETABLE (D-10) : tout ce qu'il pose hors du dépôt — dépôt de plugin de test, entrée de marketplace, plugin installé, fichier d'agent de sonde — est supprimé avant la fin du plan, et chaque chemin supprimé est ÉNUMÉRÉ dans la note puis vérifié absent par machine. Écrire dans le territoire de l'utilisateur sans nettoyage prouvé est exactement le risque nommé par Pitfall 12."
    - "Codex et Kimi ne sont PAS mesurés dans ce plan (D-08) : un NO-GO côté Claude Code clôt la question sans eux. Ils n'entreraient en jeu que sur GO Claude, et dans une phase ultérieure."
  artifacts:
    - ".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md : rapport de spike portant le chapeau `> **Verdict** : GO`, `> **Verdict** : NO-GO` ou `> **Verdict** : MESURE INVALIDE` ; les lignes de mesure `CONTROLE-NEGATIF: ECHEC|SUCCES` et `CAS-A: ATTEINT|NON-ATTEINT` ; les sections `## Protocole exécuté`, `## Contrôle négatif`, `## Cas cible A`, `## Canal vs architecture`, `## Ce que l'engine actuel couvre déjà`, `## Conséquence ledger` (rédaction prête à coller pour 34-06) et `## Nettoyage` (une ligne `- chemin supprimé : <chemin>` par artefact posé)."
  key_links:
    - "Contrôle négatif <-> validité du verdict : si le sous-agent « trouve » un skill qui n'existe nulle part, aucune mesure positive ultérieure ne signifie quoi que ce soit. C'est la seule partie du protocole qui rend l'appareil falsifiable ; elle est donc exécutée EN PREMIER (tracer) et son résultat conditionne machinalement le verdict."
    - "Verdict SKIL-01 <-> `.planning/BACKLOG.md:258-273` (item skill-installer du 2026-06-04, déclencheur consommé le 2026-06-05) et `.planning/REQUIREMENTS.md:1093` (anti-feature « marketplace de skills maison ») : un NO-GO clôt l'item et maintient l'anti-feature gravée. Ce geste ledger est exécuté par 34-06 ; la rédaction prête à coller naît ici, dans `## Conséquence ledger`."
    - "Distinction canal / architecture <-> une éventuelle phase de câblage : un GO qui tairait que 18 agents sur 25 n'ont pas l'outil `Skill` promettrait à la phase suivante une portée qu'elle n'aurait pas. La section `## Canal vs architecture` est donc obligatoire quel que soit le verdict."
    - "Liste `- chemin supprimé :` <-> territoire de l'utilisateur : la liste est construite AU FUR ET À MESURE de la pose (tâche 1 puis tâche 2), pas reconstituée de mémoire en fin de plan — un artefact posé mais jamais listé est un artefact qui survivra au spike sans que personne ne le sache."
  prohibitions:
    - "Ne JAMAIS écrire la moindre ligne de code d'installeur dans cette phase, même sur GO — ni dans `plugin/_internal/vibeflow-update.sh`, ni ailleurs. Un GO produit une spec de câblage et un item de phase ultérieure (D-09)."
    - "Ne JAMAIS laisser en place un artefact du spike hors du dépôt : dépôt de plugin de test, entrée de marketplace, plugin installé, fichier d'agent de sonde. Chaque chemin posé est énuméré puis prouvé absent en fin de plan."
    - "Ne JAMAIS toucher, écraser ni supprimer un skill préexistant de l'utilisateur (les 8 skills VibeFlow posés par l'engine, ou tout autre) — le spike n'écrit que sous des noms préfixés `skil01-probe`, jamais ailleurs."
    - "Ne JAMAIS confondre une slash command auto-découverte par le canal plugin avec un fichier `SKILL.md` invoqué par l'outil `Skill` : seul le second répond à la question de D-07, et la note doit dire lequel des deux a été mesuré."
    - "Ne JAMAIS mesurer Codex ni Kimi dans ce plan (D-08) — un NO-GO Claude clôt la question sans eux."
    - "Ne JAMAIS rendre un verdict GO ou NO-GO si le contrôle négatif a réussi : le verdict est alors MESURE INVALIDE, et la question reste ouverte."
    - "Ne JAMAIS importer une persona du catalogue `agency-agents` ni créer d'agent distribué dans ce plan — le sous-agent de sonde est un fichier jetable hors `plugin/`, supprimé au nettoyage."
---

<objective>
Rendre le go/no-go SKIL-01 APRÈS une mesure d'exécution, jamais après une lecture de documentation :
un skill posé par le canal natif `/plugin` atteint-il réellement un sous-agent doté de l'outil
`Skill` ? Ce plan exécute le protocole déjà écrit dans `34-RESEARCH.md` § Volet 3 — contrôle
négatif d'abord, puis cas cible en scope user — consigne ses sorties verbatim, et produit
`34-SPIKE-SKIL.md`.

Purpose : l'item BACKLOG du 2026-06-04 a vu son déclencheur consommé le 2026-06-05 et a dormi sept
semaines. Le milestone l'a réduit à un cadrage go/no-go précisément pour arrêter de le reporter.
D-07 refuse de trancher sur pièces : le seul différenciateur restant identifié doit être mesuré. Un
NO-GO est one-way — il clôt l'item et maintient gravée l'anti-feature « marketplace de skills
maison » — d'où le checkpoint de décision bloquant-humain avant le verdict.

Output : `34-SPIKE-SKIL.md`, unique fichier de ce dépôt touché par ce plan. Aucun code
d'installeur, même sur GO.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md
@.planning/research/PITFALLS.md
@INSTALL.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes déterministe a rendu les trois
exigences `unresolved`/`unclassified`. L'hypothèse portée par CE plan est déclarée ici,
explicitement — jamais résolue en silence par un filet, jamais supprimée.

**SKIL-01 — hypothèse signalée (probe : `unresolved` / `unclassified`).** L'exigence ne définit
nulle part ce qui se passe AUX ARÊTES de la mesure. Ce plan ASSUME les quatre arêtes suivantes :

1. **Le contrôle négatif réussit** (le sous-agent prétend trouver un skill inexistant) → verdict
   `MESURE INVALIDE`, ni GO ni NO-GO ; l'item BACKLOG reste OUVERT et 34-06 ne le clôt pas.
2. **Le canal `/plugin` n'est pas exerçable non interactivement** sur ce poste (commande absente,
   interface obligatoire, refus) → verdict `MESURE INVALIDE` avec la cause nommée, jamais un NO-GO
   par défaut : « pas mesurable » n'est pas « pas de trou ».
3. **Le cas cible réussit (skill atteint) mais l'engine actuel ne peut pas fermer le trou** → la
   conjonction de D-07 n'est pas satisfaite : NO-GO, avec la moitié manquante nommée.
4. **Le scope project n'est pas mesuré** si le scope user suffit à trancher (recommandation de
   l'Open Question 2 de la recherche) ; ce non-mesuré est ÉCRIT dans la note, jamais tu.

Aucune de ces quatre arêtes n'est couverte par un `D-xx` de `34-CONTEXT.md`. Si Samuel les arbitre
autrement, c'est la note qui est relue.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Tâche 1 : contrôle négatif — la tranche qui prouve que l'appareil de mesure discrimine</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md</files>
  <precondition>La commande `claude` est résolvable sur le PATH du poste et répond à `claude plugin --help` sans interface bloquante. Si elle ne l'est pas, ce n'est PAS un NO-GO : écrire la cause dans `34-SPIKE-SKIL.md`, poser le chapeau MESURE INVALIDE, et ne pas poursuivre la tâche 2.</precondition>
  <read_first>
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Volet 3 » en entier — citations verbatim de la doc officielle, mesure locale de la frontière des outils sur les 25 agents distribués, protocole de spike en 5 étapes : c'est le protocole à EXÉCUTER, jamais à réinventer
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « `34-SPIKE-SKIL.md` (verdict SKIL-01) » — gabarit du rapport : chapeau factuel avec verdict séparé, ton `[CITED:]` / `[VERIFIED:]`, table de non-fiabilité des sources
    - `.planning/phases/VFDO-37-portabilit-multi-runtime-spike-codex-opencode-kimi/SPIKE-REPORT.md` lignes 1-50 — analog exact désigné par la carte des patterns
    - `plugin/mobile-test-team/agents/vf-test-runner.md` frontmatter (lignes 1-12) — forme minimale d'un fichier d'agent, pour fabriquer le sous-agent de sonde sans inventer de champ
    - `plugin/conductor/scripts/check-agents.sh` autour de la ligne 515 — les cinq champs requis d'un agent, à reproduire dans la sonde jetable
    - `.planning/research/PITFALLS.md` § « Pitfall 12 » — le risque d'écrasement en territoire utilisateur et le refus de tout second système de pose
  </read_first>
  <action>
    Établir le socle de comparaison AVANT toute mesure positive — c'est la seule partie du
    protocole qui rend l'appareil falsifiable, et c'est pour cela qu'elle vient en premier.

    (0) **SHA de base.** Résoudre `git rev-parse HEAD` dans CE dépôt et l'écrire dans la note,
    section `## Périmètre`, sous la forme exacte `SHA de base : <sha>` : c'est le point de
    comparaison de tous les diffs de périmètre de ce plan. Sans lui, un `git diff` nu comparerait
    l'arbre à `HEAD`, donc à un état qui inclut déjà les commits du plan — et passerait au vert
    dès le premier commit, sans rien prouver.

    (1) **Fabriquer le sous-agent de sonde.** Créer un fichier d'agent JETABLE, hors du dépôt,
    nommé `skil01-probe-agent`, avec les cinq champs requis (`name`, `description`, `model`,
    `memory`, `effort`) et une ligne d'outils qui inclut explicitement `Skill`. Ce fichier ne vit
    JAMAIS sous `plugin/` : il est jetable par construction et sera supprimé en tâche 2. Inscrire
    son chemin exact dans la liste de nettoyage dès sa création.

    (2) **Prouver l'absence du nom de sonde AVANT de mesurer.** Rechercher
    `skil01-probe-nonexistant` dans la racine des skills personnels, dans le dossier de skills du
    projet courant, et dans le cache de plugins. Attendu : nulle part. Sans cette vérification, un
    échec de découverte ne prouverait rien (le nom aurait pu exister). Consigner les trois
    recherches et leurs sorties.

    (3) **Jouer le contrôle négatif.** Dispatcher le sous-agent de sonde (outil Agent/Task) avec un
    mandat d'une ligne : invoquer par l'outil `Skill` un skill nommé `skil01-probe-nonexistant`,
    puis rapporter LITTÉRALEMENT ce qu'il obtient. Attendu : échec, absence de découverte, message
    de type « skill introuvable ».

    (4) **Consigner.** Créer `34-SPIKE-SKIL.md` avec un chapeau provisoire, la section
    `## Protocole exécuté` (chaque commande verbatim et sa sortie), la section `## Contrôle négatif`
    (le mandat donné, la réponse littérale du sous-agent recopiée sans paraphrase, les trois
    recherches d'absence), et la ligne de mesure au format exact `CONTROLE-NEGATIF: ECHEC` si le
    sous-agent n'a rien trouvé, `CONTROLE-NEGATIF: SUCCES` s'il prétend avoir trouvé — dans ce
    second cas, écrire immédiatement que le spike est en route vers MESURE INVALIDE. Ouvrir les sections `## Périmètre` (avec le SHA de base) et
    `## Nettoyage` et y inscrire une ligne `- chemin supprimé : <chemin>` par artefact posé.
  </action>
  <behavior>
    - Le nom de sonde est prouvé absent des trois emplacements AVANT le dispatch.
    - Le sous-agent de sonde a bien `Skill` dans sa ligne d'outils — sans quoi son échec ne
      prouverait rien d'autre que l'absence de l'outil (Pitfall 5).
    - La réponse du sous-agent est recopiée littéralement, jamais paraphrasée.
    - La ligne `CONTROLE-NEGATIF:` porte exactement `ECHEC` ou `SUCCES`, pas une nuance.
  </behavior>
  <acceptance_criteria>
    - `34-SPIKE-SKIL.md` existe et contient les sections `## Protocole exécuté`, `## Contrôle négatif`, `## Nettoyage` et `## Périmètre`.
    - `34-SPIKE-SKIL.md` contient une ligne `SHA de base : <sha>` où `<sha>` fait 40 caractères hexadécimaux.
    - `34-SPIKE-SKIL.md` contient une ligne correspondant exactement à `CONTROLE-NEGATIF: ECHEC` ou `CONTROLE-NEGATIF: SUCCES`.
    - `34-SPIKE-SKIL.md` contient les chaînes `skil01-probe-nonexistant` et `skil01-probe-agent` — les noms employés sont nommés, pas décrits.
    - `34-SPIKE-SKIL.md` contient au moins une ligne commençant par `- chemin supprimé : `.
    - Aucun fichier sous `plugin/` n'a été créé ou modifié par cette tâche.
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md && test -f "$N" && grep -q '^## Protocole exécuté' "$N" && grep -q '^## Contrôle négatif' "$N" && grep -q '^## Nettoyage' "$N" && grep -q '^## Périmètre' "$N" && grep -qE '^SHA de base : [0-9a-f]{40}$' "$N" && grep -qE '^CONTROLE-NEGATIF: (ECHEC|SUCCES)$' "$N" && grep -q 'skil01-probe-nonexistant' "$N" && grep -q 'skil01-probe-agent' "$N" && grep -q '^- chemin supprimé : ' "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && git diff --quiet "$B" -- plugin</automated>
    <fails_when>sortie non nulle. Causes discriminantes : la note est absente ou il lui manque `## Protocole exécuté` / `## Contrôle négatif` / `## Nettoyage` ; la ligne `CONTROLE-NEGATIF:` est absente ou porte autre chose que `ECHEC`/`SUCCES` (verdict non exploitable par la tâche 3) ; un des deux noms de sonde n'apparaît pas (la mesure est décrite mais pas nommée, donc non rejouable) ; aucune ligne `- chemin supprimé : ` (un artefact a été posé en territoire utilisateur sans être inscrit au nettoyage) ; `git diff --quiet <SHA de base> -- plugin` rend 1, c'est-à-dire qu'un fichier sous `plugin/` a été touché alors que ce plan n'a aucun droit d'y écrire.</fails_when>
  </verify>
  <done>Le socle de comparaison existe et est falsifiable : on sait ce que fait l'outil `Skill` face à un nom qui n'existe nulle part, et la preuve que ce nom n'existait nulle part est consignée.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : cas cible A — skill posé par le canal /plugin natif en scope user, sentinelle mesurée, nettoyage prouvé</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md</files>
  <precondition>La tâche 1 est verte et la note porte `CONTROLE-NEGATIF: ECHEC`. Si elle porte `CONTROLE-NEGATIF: SUCCES`, ne pas exécuter cette tâche : passer directement au checkpoint avec l'option MESURE INVALIDE.</precondition>
  <read_first>
    - `34-SPIKE-SKIL.md` dans son état de fin de tâche 1 — la liste de nettoyage en cours et le résultat du contrôle négatif
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Protocole de spike proposé » étapes 2, 3 et 5, et § « Fait empirique sur ce poste : les deux canaux coexistent déjà pour VF lui-même » — pourquoi un skill de test DÉDIÉ est nécessaire pour isoler la variable « canal de pose »
    - `INSTALL.md` lignes 26-37 — les commandes natives d'ajout de marketplace et d'installation de plugin, dans leur forme exacte déjà documentée par ce dépôt
    - `plugin/.claude-plugin/plugin.json` — forme minimale d'un descripteur de plugin, pour fabriquer le plugin jetable sans inventer de champ
    - `plugin/_internal/vibeflow-update.sh` autour des lignes 2218-2229, 2478, 2606-2608 et 2726 — pose, backup, rollback et désinstallation de skills par l'engine actuel : c'est la matière de la section `## Ce que l'engine actuel couvre déjà`
  </read_first>
  <action>
    Mesurer le cas cible, puis rendre le poste à son état d'origine et le PROUVER.

    (1) **Fabriquer le plugin jetable.** Créer, hors du dépôt `vibeflow-os`, un dépôt Git jetable
    nommé `skil01-probe-plugin` avec un descripteur `.claude-plugin/plugin.json` minimal et UN seul
    skill portant une sentinelle explicite dans ses instructions : le skill doit répondre
    EXACTEMENT `SKIL01_PROBE_OK` s'il est invoqué. La sentinelle est le seul signal mesuré — pas
    une impression, pas un « ça a l'air de marcher ». Inscrire chaque chemin créé dans la liste de
    nettoyage au fur et à mesure.

    (2) **Poser par le canal natif, scope user.** Ajouter le marketplace local puis installer le
    plugin, avec les commandes documentées dans `INSTALL.md`. Consigner chaque commande verbatim et
    sa sortie. Si l'une refuse de s'exécuter sans interface interactive, ARRÊTER là : écrire la
    cause, poser `CAS-A: NON-ATTEINT` avec la mention « non exerçable non interactivement », et
    aller au checkpoint avec l'option MESURE INVALIDE (arête 2 des hypothèses signalées) — surtout
    pas un NO-GO par défaut.

    (3) **Mesurer.** Dispatcher le MÊME type de sous-agent qu'en tâche 1 (`skil01-probe-agent`,
    outil `Skill` inclus) et lui demander d'invoquer le skill par son nom namespacé. Recopier sa
    réponse littéralement. Poser la ligne `CAS-A: ATTEINT` si et seulement si la sentinelle
    `SKIL01_PROBE_OK` revient ; `CAS-A: NON-ATTEINT` sinon.

    (4) **Scope project : mesurer ou déclarer non mesuré.** Conformément à la recommandation de
    l'Open Question 2 de la recherche, le scope project n'est mesuré que si le résultat user
    surprend ou si le verdict user est un GO. Dans tous les cas, ÉCRIRE dans la note laquelle des
    deux branches a été prise et pourquoi — un non-mesuré tu est un trou de cadrage.

    (5) **Nettoyer et prouver.** Désinstaller le plugin, retirer le marketplace ajouté, supprimer
    le dépôt jetable et le fichier d'agent de sonde. Chaque chemin figure déjà en
    `- chemin supprimé : <chemin>` dans la section `## Nettoyage` ; la vérification de cette tâche
    relit ces lignes et échoue si l'un des chemins existe encore. Ne rien supprimer qui ne porte
    pas le préfixe `skil01-probe`.

    (6) **Écrire la moitié « engine » de la conjonction D-07.** Section
    `## Ce que l'engine actuel couvre déjà` : ce que `vibeflow-update.sh` sait déjà faire (pose,
    backup, rollback, désinstallation de skills) et ce qu'un GO devrait donc RÉUTILISER plutôt que
    de rouvrir — un second système de pose est un signal d'alarme Pitfall 12, pas une option.
  </action>
  <behavior>
    - Le skill jetable porte une sentinelle unique et mesurable, pas une réponse libre.
    - La ligne `CAS-A:` porte exactement `ATTEINT` ou `NON-ATTEINT`.
    - Le sous-agent dispatché est du même type qu'en tâche 1 — sinon les deux mesures ne sont pas
      comparables et le contrôle négatif ne contrôle rien.
    - Après nettoyage, aucun des chemins listés dans `## Nettoyage` n'existe plus sur le poste.
    - La section `## Ce que l'engine actuel couvre déjà` cite des lignes précises de l'engine, pas
      une généralité.
  </behavior>
  <acceptance_criteria>
    - `34-SPIKE-SKIL.md` contient une section `## Cas cible A` et une section `## Ce que l'engine actuel couvre déjà`.
    - `34-SPIKE-SKIL.md` contient une ligne correspondant exactement à `CAS-A: ATTEINT` ou `CAS-A: NON-ATTEINT`.
    - `34-SPIKE-SKIL.md` contient la chaîne `SKIL01_PROBE_OK` (la sentinelle employée est nommée).
    - `34-SPIKE-SKIL.md` contient une ligne commençant par `Scope project : ` indiquant `mesuré` ou `non mesuré` avec sa raison.
    - Chaque chemin listé par une ligne `- chemin supprimé : ` de la note est ABSENT du disque, et il y a au moins deux telles lignes.
    - Aucun fichier sous `plugin/` n'a été créé ou modifié par cette tâche.
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md && grep -q '^## Cas cible A' "$N" && grep -q "^## Ce que l'engine actuel couvre déjà" "$N" && grep -qE '^CAS-A: (ATTEINT|NON-ATTEINT)$' "$N" && grep -q 'SKIL01_PROBE_OK' "$N" && grep -qE '^Scope project : (mesuré|non mesuré)' "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && test -n "$B" && git diff --quiet "$B" -- plugin && node -e 'const fs=require("fs");const P="- chemin supprimé : ";const L=fs.readFileSync(process.argv[1],"utf8").split("\n").filter(l=>l.startsWith(P));if(L.length<2){console.log("NETTOYAGE INCOMPLET: "+L.length+" chemin(s) liste(s)");process.exit(1);}const rest=L.map(l=>l.slice(P.length).trim().replace(/^~/,process.env.HOME)).filter(p=>fs.existsSync(p));if(rest.length){console.log("RESTE: "+rest.join(","));process.exit(1);}' "$N"</automated>
    <fails_when>sortie non nulle, ou la ligne `NETTOYAGE INCOMPLET: <n> chemin(s) liste(s)` ou `RESTE: <chemins>` apparaît. Causes discriminantes : section `## Cas cible A` ou `## Ce que l'engine actuel couvre déjà` absente ; ligne `CAS-A:` absente ou mal formée ; la sentinelle n'est pas nommée dans la note (mesure non rejouable) ; pas de ligne `Scope project :` (le non-mesuré serait tu) ; moins de deux chemins inscrits au nettoyage (le plugin jetable ET le dépôt jetable en font au minimum deux — une liste plus courte signale un artefact posé sans être suivi) ; au moins un chemin listé existe encore sur le disque (territoire utilisateur pollué par le spike) ; un fichier sous `plugin/` a été touché.</fails_when>
  </verify>
  <done>Le cas cible est mesuré par une sentinelle, la moitié « engine » de la conjonction D-07 est écrite, et le poste est rendu à son état d'origine — prouvé chemin par chemin, pas affirmé.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint : trancher le verdict SKIL-01 (one-way pour un NO-GO)</name>
  <decision>Quel verdict SKIL-01 est rendu au vu des deux lignes de mesure de `34-SPIKE-SKIL.md` : NO-GO, GO, ou MESURE INVALIDE ?</decision>
  <context>
    La mesure est faite et consignée dans `34-SPIKE-SKIL.md` : lire les lignes `CONTROLE-NEGATIF:`
    et `CAS-A:` avant de répondre, jamais la recommandation seule. Le verdict qui en découle est
    **one-way pour un NO-GO** (D-07) : il clôt l'item BACKLOG du 2026-06-04 et maintient gravée
    l'anti-feature « marketplace de skills maison ». Rouvrir demanderait une NOUVELLE mesure qui
    contredise celle-ci — d'où un gate qui ne s'auto-décide pas, même en mode autonome.
    La recherche (documentation officielle citée + mesure locale de la frontière des outils sur les
    25 agents distribués) penche vers NO-GO, mais D-07 interdit explicitement de conclure là-dessus :
    c'est la mesure exécutée qui tranche, pas la lecture.
  </context>
  <options>
    <option id="no-go">
      <name>NO-GO</name>
      <pros>Ferme une question qui dort depuis le 2026-06-04 avec son déclencheur consommé ; l'item BACKLOG est clos par 34-06 et l'anti-feature reste gravée ; aucune dette de câblage n'est ouverte.</pros>
      <cons>Irréversible au sens de D-07 : rouvrir exigerait une mesure contradictoire. À ne choisir que si le contrôle négatif a bien échoué ET qu'aucun trou n'existe, ou qu'un trou existe mais que l'engine actuel ne peut pas le fermer.</cons>
    </option>
    <option id="go">
      <name>GO</name>
      <pros>Un trou est MESURÉ et `vibeflow-update.sh` peut le fermer en réutilisant son chemin de pose existant ; la note produit une spec de câblage et un item de phase ultérieure.</pros>
      <cons>Ouvre une phase de câblage à cadrer, qui ne doit pas entrer en collision avec le périmètre déjà verrouillé de la Phase 40. Aucune ligne de code d'installeur n'est écrite ici (D-09), donc le bénéfice est différé.</cons>
    </option>
    <option id="mesure-invalide">
      <name>MESURE INVALIDE</name>
      <pros>Honnête : le contrôle négatif a réussi (l'appareil ne discrimine pas) ou le canal natif n'était pas exerçable non interactivement. « Pas mesurable » n'est pas « pas de trou ».</pros>
      <cons>La question reste ouverte, l'item BACKLOG n'est pas clos par 34-06, et la phase laisse SKIL-01 décochée — il faudra re-mesurer avec la cause levée.</cons>
    </option>
  </options>
  <resume-signal>Répondre « NO-GO », « GO » ou « MESURE INVALIDE ».</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : rédiger le verdict — chapeau, distinction canal/architecture, conséquence ledger prête à coller</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md</files>
  <precondition>Le checkpoint de décision a été tranché par un humain et la réponse est consignée avec son canal et sa date (discipline de traçabilité de `CLAUDE.md` racine).</precondition>
  <read_first>
    - `34-SPIKE-SKIL.md` dans son état de fin de tâche 2 — les deux lignes de mesure font autorité sur le verdict
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Pitfall 5 » — la formulation exacte des deux questions à distinguer
    - `.planning/BACKLOG.md` lignes 258-273 — l'item skill-installer dans son état actuel, à clore ou à laisser ouvert : la rédaction de clôture naît ici, le geste est posé par 34-06
    - `.planning/REQUIREMENTS.md` ligne 1093 — l'anti-feature « marketplace de skills maison », à maintenir telle quelle sur NO-GO
    - `.planning/research/FEATURES.md` lignes 209-231 — F8 (skill-installer), le différenciateur tel qu'il était formulé en 2026-06-04, à confronter à la mesure
    - `.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-CONTEXT.md` — la Phase 40 dépend de la 34 : si un GO créait un item de phase ultérieure, il ne doit pas entrer en collision avec le périmètre déjà verrouillé de la 40
  </read_first>
  <action>
    Écrire le verdict et ses conséquences, sans jamais dépasser le périmètre de D-09.

    (1) **Chapeau.** Poser une ligne unique : `> **Verdict** : NO-GO`, `> **Verdict** : GO` ou
    `> **Verdict** : MESURE INVALIDE`, suivie de la date, du verdict en une phrase, et de la
    traçabilité de l'arbitrage (canal + date, jamais « arbitrage Samuel » nu). Écrire DANS la note
    la règle de composition : un GO exige `CONTROLE-NEGATIF: ECHEC` ET un trou mesuré ET l'engine
    capable de le fermer ; un `CONTROLE-NEGATIF: SUCCES` force MESURE INVALIDE.

    (2) **Section `## Canal vs architecture`** — obligatoire quel que soit le verdict. Y distinguer
    en toutes lettres : la question de D-07 (le canal atteint-il un sous-agent qui A l'outil
    `Skill`) et la question distincte (tous les agents VF peuvent-ils invoquer des skills — non,
    18 sur 25 n'ont pas l'outil, choix d'architecture VF hors périmètre de cette phase). Citer la
    mesure locale de `34-RESEARCH.md` § Volet 3 avec sa date.

    (3) **Section `## Conséquence ledger`** — la rédaction PRÊTE À COLLER que 34-06 transcrira :
    sur NO-GO, le paragraphe de clôture de l'item `BACKLOG.md:258-273` (une ligne factuelle, la
    raison entre parenthèses, jamais de prose longue — ton de clôture déjà en place dans ce dépôt)
    et la confirmation que l'anti-feature de `REQUIREMENTS.md:1093` reste gravée ; sur GO, l'item
    de phase ultérieure au gabarit BACKLOG (`**Capturé :**` / `**Cadrage.**` / `**Pourquoi
    différé :**` / `**Déclencheur de resurgence :**`) décrivant LE CÂBLAGE et rien d'autre ; sur
    MESURE INVALIDE, la mention explicite que l'item reste ouvert et ce qu'il faudrait pour
    re-mesurer.

    (4) **Table de non-fiabilité des sources.** Reprendre le pattern du spike de la Phase 37 :
    marquer chaque affirmation `[VERIFIED: <commande, date>]` ou `[CITED: <source, date>]`, et
    rappeler que la documentation officielle seule n'a jamais suffi dans ce dépôt.

    (5) **Périmètre.** Réaffirmer, et prouver par un diff scopé contre `HEAD`, qu'aucun fichier
    hors du dossier de phase n'a été touché — en particulier aucune ligne de
    `plugin/_internal/vibeflow-update.sh`.
  </action>
  <behavior>
    - Le chapeau porte exactement UNE ligne de verdict, jamais zéro ni deux.
    - Un chapeau GO implique `CONTROLE-NEGATIF: ECHEC` dans la note.
    - Un `CONTROLE-NEGATIF: SUCCES` implique un chapeau MESURE INVALIDE.
    - Les sections `## Canal vs architecture` et `## Conséquence ledger` existent quel que soit le
      verdict.
    - Aucun fichier sous `plugin/` n'est touché par ce plan.
  </behavior>
  <acceptance_criteria>
    - `34-SPIKE-SKIL.md` contient EXACTEMENT une ligne correspondant à `> **Verdict** : GO`, `> **Verdict** : NO-GO` ou `> **Verdict** : MESURE INVALIDE`.
    - `34-SPIKE-SKIL.md` contient les sections `## Canal vs architecture` et `## Conséquence ledger`.
    - Cohérence machine : si la note porte `CONTROLE-NEGATIF: SUCCES`, alors le chapeau est `MESURE INVALIDE` ; si le chapeau est `GO`, alors la note porte `CONTROLE-NEGATIF: ECHEC`.
    - `34-SPIKE-SKIL.md` contient une ligne portant la traçabilité de l'arbitrage sous la forme `arbitrage Samuel, <canal>, AAAA-MM-JJ`.
    - `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 (forme sans `..HEAD` : l'arbre de travail est comparé au SHA de base, donc un fichier déjà commité par ce plan est vu lui aussi).
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md && V=$(grep -cE '^> \*\*Verdict\*\* : (GO|NO-GO|MESURE INVALIDE)$' "$N") && [ "$V" -eq 1 ] && grep -q '^## Canal vs architecture' "$N" && grep -q '^## Conséquence ledger' "$N" && grep -qE 'arbitrage Samuel, .*, [0-9]{4}-[0-9]{2}-[0-9]{2}' "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && test -n "$B" && git diff --quiet "$B" -- plugin scripts docs manual .github README.md README.fr.md && node -e 'const fs=require("fs");const t=fs.readFileSync(process.argv[1],"utf8");const neg=/^CONTROLE-NEGATIF: SUCCES$/m.test(t);const go=/^> \*\*Verdict\*\* : GO$/m.test(t);const inv=/^> \*\*Verdict\*\* : MESURE INVALIDE$/m.test(t);const echec=/^CONTROLE-NEGATIF: ECHEC$/m.test(t);if(neg&&!inv){console.log("INCOHERENT: controle negatif reussi sans verdict MESURE INVALIDE");process.exit(1);}if(go&&!echec){console.log("INCOHERENT: verdict GO sans controle negatif en echec");process.exit(1);}' "$N"</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `INCOHERENT: controle negatif reussi sans verdict MESURE INVALIDE` / `INCOHERENT: verdict GO sans controle negatif en echec` apparaît. Causes discriminantes : le compte de lignes de chapeau n'est pas 1 (verdict absent, mal formé ou dupliqué) ; section `## Canal vs architecture` ou `## Conséquence ledger` absente (le cadrage serait trompeur, ou 34-06 n'aurait rien à transcrire) ; pas de ligne de traçabilité d'arbitrage au format canal + date ; `git diff --quiet` rend 1, c'est-à-dire qu'un fichier de production a été touché alors que D-09 interdit toute ligne de code d'installeur dans cette phase ; incohérence entre les lignes de mesure et le verdict.</fails_when>
    <human-check>Relire `34-SPIKE-SKIL.md` : le verdict est-il fondé sur ce qui a été MESURÉ, ou sur ce que disait la documentation ? La section `## Conséquence ledger` est-elle assez précise pour que 34-06 la colle sans réinterpréter ? Un lecteur de dans six mois comprendrait-il, sans relancer le spike, pourquoi la question est close (ou pourquoi elle reste ouverte) ?</human-check>
  </verify>
  <done>`34-SPIKE-SKIL.md` rend un verdict fondé sur une mesure exécutée, distingue le canal de l'architecture, et fournit à 34-06 une rédaction ledger prête à coller — sans qu'une seule ligne de code d'installeur ait été écrite.</done>
  <reversibility rating="one-way">D-07 : un NO-GO clôt l'item BACKLOG du 2026-06-04 et maintient gravée l'anti-feature « marketplace de skills maison ». Rouvrir exigerait une nouvelle mesure qui contredise celle-ci — d'où le `checkpoint:decision` en `blocking-human` placé avant cette tâche.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike → racine de skills et cache de plugins de l'UTILISATEUR | le spike écrit hors du dépôt, dans le territoire de l'utilisateur, là où vivent déjà 8 skills VibeFlow et d'éventuels skills personnels — c'est le risque d'écrasement nommé par Pitfall 12 |
| Plugin jetable (dépôt Git fabriqué par le spike) → session Claude Code | le plugin installé injecte des instructions dans le contexte d'un sous-agent ; son contenu doit être minimal, sentinelle comprise, et sans instruction latérale |
| Sous-agent de sonde → outil `Skill` | la sonde est fabriquée avec l'outil `Skill` explicitement inclus, ce que 18 des 25 agents distribués n'ont pas : la frontière mesurée n'est PAS celle du parc réel, et la note doit le dire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Tampering | skills préexistants de l'utilisateur (8 skills VibeFlow posés par l'engine, plus les skills personnels) | high | mitigate | le spike n'écrit QUE sous des noms préfixés `skil01-probe` ; la prohibition de plan interdit toute suppression ou écrasement d'un autre nom ; le nettoyage ne supprime que les chemins inscrits en `- chemin supprimé : `, jamais un balayage large |
| T-34-06 | Tampering | résidu de plugin installé laissé en place après le spike | medium | mitigate | la liste `## Nettoyage` est construite AU MOMENT DE LA POSE (tâches 1 et 2), pas reconstituée en fin de plan ; la vérification de la tâche 2 relit chaque chemin listé et échoue si l'un existe encore — le nettoyage est prouvé, pas déclaré |
| T-34-07 | Elevation of Privilege | instructions injectées dans le contexte d'un sous-agent par le skill jetable | medium | mitigate | le skill jetable contient UNIQUEMENT la consigne de répondre par la sentinelle ; aucune autre instruction, aucun appel d'outil, aucun accès réseau ; le dépôt jetable est fabriqué par l'exécutant lui-même, jamais cloné depuis une source tierce |
| T-34-08 | Repudiation | verdict rendu sur pièces et présenté comme mesuré | high | mitigate | les deux lignes de mesure (`CONTROLE-NEGATIF:`, `CAS-A:`) sont exigées par les critères d'acceptation et leur cohérence avec le chapeau est vérifiée par machine ; un verdict GO sans contrôle négatif en échec échoue la vérification ; le `checkpoint:decision` est en `blocking-human`, jamais auto-sélectionné |

Aucun install de paquet `npm`/`pip`/`cargo` n'entre dans ce plan — la section « Package Legitimacy
Audit » de `34-RESEARCH.md` est déclarée non applicable à la phase. L'installation mesurée est celle
d'un plugin jetable FABRIQUÉ SUR PLACE par l'exécutant, jamais d'un paquet tiers téléchargé : aucune
menace `T-34-SC` n'est donc ouverte, et aucun checkpoint de légitimité de paquet n'est requis.
</threat_model>

<verification>
1. `34-SPIKE-SKIL.md` existe et porte EXACTEMENT une ligne de chapeau `> **Verdict** : GO|NO-GO|MESURE INVALIDE`.
2. Les deux lignes de mesure `CONTROLE-NEGATIF: …` et `CAS-A: …` sont présentes et bien formées ; leur cohérence avec le chapeau est vérifiée par machine (un contrôle négatif réussi force MESURE INVALIDE ; un GO exige un contrôle négatif en échec).
3. Les sections `## Protocole exécuté`, `## Contrôle négatif`, `## Cas cible A`, `## Canal vs architecture`, `## Ce que l'engine actuel couvre déjà`, `## Conséquence ledger` et `## Nettoyage` sont toutes présentes.
4. Chaque chemin listé en `- chemin supprimé : ` est absent du disque, et il y en a au moins deux.
5. La ligne `Scope project : mesuré|non mesuré` est présente avec sa raison.
6. `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 — aucune ligne de code d'installeur, D-09 respecté et prouvé sur une plage explicite.
7. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-02-SUMMARY.md` existe et consigne littéralement les mesures ci-dessus, dont la réponse du checkpoint avec son canal et sa date.
</verification>

<success_criteria>
- La question « que fait le skill-installer de plus que le natif `/plugin` ? » a une réponse ÉCRITE et MESURÉE, pas déduite d'une documentation.
- Le contrôle négatif a été joué et sa capacité à discriminer est prouvée — sans quoi aucun verdict n'est rendu.
- La distinction canal / architecture est écrite en toutes lettres, pour que la phase de câblage éventuelle ne promette pas une portée qu'elle n'aurait pas.
- Le poste est rendu à son état d'origine, chemin par chemin, prouvé par machine.
- Zéro ligne de code d'installeur écrite, même sur GO (D-09), et le diff scopé le prouve.
- 34-06 dispose d'une rédaction ledger prête à coller, cohérente avec le verdict rendu.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| `34-SPIKE-SKIL.md` | note de décision (rapport de spike) — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md` |
| `34-02-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-02-SUMMARY.md` |
| Rédaction de clôture (ou d'ouverture) de l'item skill-installer | texte rédigé DANS la note § `## Conséquence ledger`, transcrit par 34-06 | — |
| `skil01-probe-agent`, `skil01-probe-plugin`, skill `SKIL01_PROBE_OK` | artefacts JETABLES hors dépôt, créés puis supprimés dans le même plan ; énumérés en `## Nettoyage` | hors dépôt |

Aucun symbole de code, aucun agent distribué, aucun module n'est produit par ce plan.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-02-SUMMARY.md` en fin
de plan, consignant : le chapeau final, les deux lignes de mesure et les réponses littérales des
sous-agents, les commandes de pose et de dépose verbatim avec leurs sorties, la liste complète des
chemins nettoyés et la preuve de leur absence, la branche prise sur le scope project, la réponse du
checkpoint avec son canal et sa date, et tout écart constaté entre ce plan et l'état réel du poste
au moment de l'exécution.
</output>

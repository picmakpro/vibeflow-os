---
phase: 41-posture-de-protection-du-d-p-t
plan: 04
lot: L4 — intégration de la PR de la phase, merge AVANT toute pose, ouverture de la branche des preuves
type: execute
wave: 4
depends_on: ["41-01", "41-02", "41-03"]
autonomous: false
requirements: [PROT-01, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ordre imposé (D-M14) : #29 fermée (41-01) → PR de la phase (CODEOWNERS, JSON, doctrine, ledger) mergée par ce plan → rulesets posés (41-05). Au merge, `rulesets` rend encore `[]` : la PR de la phase ne passe pas sous ses propres règles en vol"
    - "La branche A intégrée est verte en local sur la découverte complète (gates + tests) et sur la CI distante (4 jobs × `push` + `pull_request`), avant tout merge"
    - "`codeowners/errors` rend 0 erreur sur la branche A avant merge, puis sur la branche par défaut après merge (aucune ligne CODEOWNERS sautée par GitHub)"
    - "Après merge, les blobs de `.github/CODEOWNERS`, `.github/rulesets/main.json`, `.github/rulesets/tags-v.json` sur `origin/main` sont identiques à ceux de la branche A : ce qui sera posé est ce qui a été relu"
    - "Aucun fichier sous `plugin/dev-orchestrator/` dans la PR de la phase (zone interdite, hotfix v2.63.2 en parallèle)"
    - "Push, ouverture de PR et merge sont des gestes humains (`samuel-neveugall`) ; la branche B `feat/phase-41-preuves-release` est créée en local depuis `origin/main` après le merge et porte désormais toutes les écritures jusqu'à la PR des preuves (D-03 : plus aucun commit direct sur `main`)"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "ligne `PR-PHASE:` (numéro, commit de merge, erreurs CODEOWNERS), sur la branche B"
  key_links:
    - "Le SUMMARY de ce plan est écrit APRÈS le merge : il vit sur la branche B, jamais sur `main` en direct"
---

<objective>
Intégrer et merger la PR de la phase avant toute pose de règle, puis ouvrir la branche qui portera
les preuves et la release.

Purpose: D-M14 — CODEOWNERS n'agit qu'une fois sur la branche de base, et les rulesets doivent être
posés depuis une source déjà mergée, sans jamais changer les conditions de la PR qui les apporte.
Output: PR de la phase mergée, `PR-PHASE:` consignée sur la branche B.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-01-SUMMARY.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-02-SUMMARY.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-03-SUMMARY.md

Source : `docs.github.com/en/rest/repos/repos` — `GET /repos/{owner}/{repo}/codeowners/errors`,
paramètre `ref` (défaut : branche par défaut), tableau `errors` (line, column, kind, message, path).
Constat du 2026-09-17 : l'endpoint rend 404 tant qu'aucun CODEOWNERS n'existe.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : branche A intégrée — préalables relus, zone interdite vide, sources revérifiées, rejeu complet</name>
  <files>aucun fichier modifié hors merge local éventuel d'`origin/main`</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (lignes `CONTEXTES-CHECKS:`, `PR29-FERMEE:`, `SONDE-ACTOR-ID-WRITE:`)
  </read_first>
  <action>
Identité gh de l'agent = `samuel-neveugall`. `git fetch origin`. Si `origin/main` a avancé depuis la
base de la branche A (hotfix v2.63.2 compris), faire `git merge --no-edit origin/main` en local ; un
conflit n'est jamais résolu en réécrivant un fichier de la phase à l'aveugle : arrêt et remontée.

Vérifier : les trois SUMMARY 41-01..03 sont committés ; #29 est `CLOSED` ; `rulesets` rend `[]` ;
aucun fichier sous `plugin/dev-orchestrator/` dans `git diff --name-only origin/main...HEAD` ; les
contextes de `main.json` sont toujours égaux aux check runs 15368 de la pointe courante
d'`origin/main` et aux jobs de `ci.yml` (un job renommé entre-temps rendrait un check requis
impossible à satisfaire) ; CODEOWNERS et les deux JSON repassent leurs vérifications de 41-01 et
41-02. Puis rejouer `gates` et `tests`. Imprimer pour Samuel les commandes exactes de la Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'u=$(gh api user --jq .login); s=$(gh pr view 29 --repo picmakpro/vibeflow-os --json state --jq .state); n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; z=$(awk "index(\$0, \"plugin/dev-orchestrator/\")==1 {c++} END{print c+0}" "$T/f"); PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; k=0; for f in 41-01-SUMMARY.md 41-02-SUMMARY.md 41-03-SUMMARY.md; do git cat-file -e "HEAD:$PH/$f" 2>/dev/null || { echo "NON COMMITTE $f"; k=$((k+1)); }; done; b=$(git rev-list --count HEAD..origin/main); echo "integration : identite=$u pr29=$s rulesets=$n fichiers_zone_interdite=$z summaries_manquants=$k retard_sur_origin_main=$b"; [ "$u" = samuel-neveugall ] && [ "$s" = CLOSED ] && [ "$n" -eq 0 ] && [ "$z" -eq 0 ] && [ "$k" -eq 0 ] && [ "$b" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou identité autre que `samuel-neveugall` ; ou #29 non `CLOSED` ; ou `rulesets=` différent de 0 (une règle posée avant le merge contredit D-M14 : arrêt) ; ou `fichiers_zone_interdite=` différent de 0 ; ou un SUMMARY non committé ; ou `retard_sur_origin_main=` différent de 0 (merge local d'`origin/main` non fait)</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=.github/rulesets/main.json; T=$(mktemp -d); S=$(git rev-parse origin/main); gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/api"; jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$B" | LC_ALL=C sort -u > "$T/json"; awk "/^jobs:/{j=1; next} j && /^  [A-Za-z0-9_-]+:[ \t]*\$/{k=1; next} j && k && /^    name:/{sub(/^    name:[ \t]*/, \"\"); gsub(/^\"|\"\$/, \"\"); print; k=0}" .github/workflows/ci.yml | LC_ALL=C sort -u > "$T/ci"; na=$(awk "END{print NR}" "$T/api"); nj=$(awk "END{print NR}" "$T/json"); nc=$(awk "END{print NR}" "$T/ci"); ja=$(cmp -s "$T/api" "$T/json" && echo oui || echo non); jc=$(cmp -s "$T/json" "$T/ci" && echo oui || echo non); echo "contextes : sha=$S api=$na json=$nj ci=$nc json_egal_api=$ja json_egal_ci=$jc"; [ "$na" -eq 4 ] && [ "$nj" -eq 4 ] && [ "$nc" -eq 4 ] && [ "$ja" = oui ] && [ "$jc" = oui ]'</automated>
    <fails_when>exit non nul ; ou un compte différent de 4 ; ou `json_egal_api=non` / `json_egal_ci=non` (job renommé sur `main` depuis 41-01 : arrêt, remontée — la source doit être régénérée, jamais retouchée à la main)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 (dont `check-machine-paths` sur les nouveaux fichiers de `.github/`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
  </verify>
  <acceptance_criteria>
    - branche A à jour d'`origin/main`, préalables relus (#29 fermée, 0 ruleset), zone interdite vide
    - contextes toujours égaux aux check runs courants et à `ci.yml`
    - rejeux `gates` (≥ 10 étapes) et `tests` (N = découverte, M = 0) verts sur l'état intégré
  </acceptance_criteria>
  <done>La PR de la phase peut être ouverte : ce qui sera relu est intégré, vert et conforme.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — pousser la branche A, ouvrir la PR de la phase, attendre la CI, contrôler CODEOWNERS, merger</name>
  <action>Samuel, compte `samuel-neveugall`, publie la branche A, ouvre la PR, attend la CI complète, lit les erreurs CODEOWNERS, puis merge. Aucun ruleset n'existe encore : merge ordinaire.</action>
  <instructions>
Compte qui agit : `samuel-neveugall` (témoin `gh api user --jq .login`).

1. `git push -u origin feat/phase-41-protection-depot`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head feat/phase-41-protection-depot --title "Phase 41 — posture de protection du dépôt : CODEOWNERS, sources des rulesets, ADR-072" --body "Sources versionnées des rulesets (.github/rulesets/), CODEOWNERS étroit (D-05), ADR-072, amendement d'ADR-059, résumé CLAUDE.md, ledger PROT-01..04. Aucune règle n'est posée par cette PR : la pose suit son merge (D-M14). D-01..D-10 : arbitrage Samuel, AskUserQuestion session principale, 2026-09-17."`
   → noter le numéro `<n>`.
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin ; tout doit être vert.
4. `gh api "repos/picmakpro/vibeflow-os/codeowners/errors?ref=feat/phase-41-protection-depot" --jq '.errors | length'`
   → doit rendre `0`. Sinon : NE PAS merger, coller la sortie complète (sans `--jq`).
5. Vérifier qu'aucun ruleset n'existe : `gh api repos/picmakpro/vibeflow-os/rulesets --jq length` → `0`.
6. Merger : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge`
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`
   → `MERGED` et un sha.
Ne pas supprimer la branche distante tant que la Task 3 n'a pas relu les blobs.
  </instructions>
  <verification>La Task 3 relit l'état `MERGED`, les check runs de la tête, les erreurs CODEOWNERS sur la branche par défaut et l'égalité des blobs.</verification>
  <resume-signal>Coller le numéro de PR et les sorties des étapes 3 (résumé final), 4, 5 et 6, puis écrire « PR de la phase mergée ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : relecture post-merge, branche B ouverte, `PR-PHASE:` consignée, rejeu</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. `git fetch origin`. Relire : PR `<n>` `MERGED`, commit de merge ;
check runs 15368 du sha de tête de la PR (4 noms × 2 événements, tous `success`) ;
`codeowners/errors` sans `ref` (branche par défaut) = 0 ; blobs des trois fichiers de `.github/`
identiques entre `origin/main` et la branche A ; `rulesets` toujours `[]`.

Créer la branche B en local : `git switch -c feat/phase-41-preuves-release origin/main` (arbre propre
exigé ; sinon arrêt). Ajouter à `41-PREUVES.md` une section `## 41-04 — PR de la phase` et la ligne
`PR-PHASE: #<n> merge=<sha> tete=<sha de tete> checks_tete=8/8 codeowners_errors=0 rulesets_au_merge=0`.
Commit local sur B, jamais poussé, message « docs(41-04): PR de la phase mergée, branche des preuves
ouverte ». Rejouer `gates` et `tests`. Le SUMMARY de ce plan est committé sur
B.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; N=$(awk "\$1==\"PR-PHASE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-PHASE absente"; exit 1;; esac; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); H=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); ok=$(gh api "repos/picmakpro/vibeflow-os/commits/$H/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | group_by(.name) | map(select(length >= 2)) | length"); ce=$(gh api repos/picmakpro/vibeflow-os/codeowners/errors --jq ".errors | length"); rs=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); anc=$(git merge-base --is-ancestor "$M" origin/main && echo oui || echo non); d=0; for f in .github/CODEOWNERS .github/rulesets/main.json .github/rulesets/tags-v.json; do [ "$(git rev-parse "origin/main:$f")" = "$(git rev-parse "feat/phase-41-protection-depot:$f")" ] || { echo "BLOB DIFFERENT $f"; d=$((d+1)); }; done; lm=$(awk -v m="$M" "\$1==\"PR-PHASE:\" && index(\$0, \"merge=\" m) {c++} END{print c+0}" "$PH/41-PREUVES.md"); br=$(git branch --show-current); echo "post-merge : pr=$N etat=$st merge=$M dans_origin_main=$anc jobs_verts_sur_les_deux_evenements=$ok codeowners_errors=$ce rulesets=$rs blobs_differents=$d ligne_coherente=$lm branche=$br"; [ "$st" = MERGED ] && [ "$anc" = oui ] && [ "$ok" -eq 4 ] && [ "$ce" -eq 0 ] && [ "$rs" -eq 0 ] && [ "$d" -eq 0 ] && [ "$lm" -eq 1 ] && [ "$br" = feat/phase-41-preuves-release ]'</automated>
    <fails_when>exit non nul ; ou ligne `PR-PHASE:` absente ; ou état différent de `MERGED` ; ou merge absent d'`origin/main` ; ou `jobs_verts_sur_les_deux_evenements=` différent de 4 (un job sans ses deux runs verts) ; ou `codeowners_errors=` différent de 0 (ligne sautée par GitHub : arrêt avant toute pose) ; ou `rulesets=` différent de 0 ; ou un `BLOB DIFFERENT` ; ou la ligne ne porte pas le sha de merge relu ; ou branche courante différente de B</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; k=0; t=0; : > "$T/corps"; for key in PR-PHASE; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "NUMERO DE PR ABSENT : $key"; k=$((k+1)); continue;; esac; t=$((t+1)); printf "@@CORPS@@ %s #%s\n" "$key" "$N" >> "$T/corps"; gh pr view "$N" --repo picmakpro/vibeflow-os --json body --jq .body >> "$T/corps"; done; r=0; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@CORPS@@ / { if (lib != \"\") juge(m, lib); lib=\$0; m=\"\"; next } { m = m \" \" \$0 } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (lib != \"\") juge(m, lib); print \"corps de PR : en_defaut=\" v+0; exit (v > 0) }" "$T/arb" "$T/corps" || r=$?; echo "corps de PR : lus=$t numeros_absents=$k"; [ "$r" -eq 0 ] && [ "$k" -eq 0 ] && [ "$t" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou un corps de PR qui cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » ; ou un corps qui fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal aux valeurs CAPTURÉES (frontmatter de 41-01 ou `41-PREUVES.md`) ; ou un numéro de PR non consigné</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - corps de la PR de la phase et messages de commit de plan : toute citation de D-01..D-10 porte le canal et la date
    - PR de la phase `MERGED`, merge dans `origin/main`, tête verte 8/8, 0 erreur CODEOWNERS sur la branche par défaut
    - blobs `.github/` identiques entre `origin/main` et la branche A ; `rulesets` encore vide
    - branche B créée depuis `origin/main`, `PR-PHASE:` consignée, rejeux verts
  </acceptance_criteria>
  <done>La source relue est sur `main`, aucune règle n'a changé en vol, et les preuves ont leur branche.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branche A → `main` | dernier merge sans règle serveur ; ce qui entre ici sera posé tel quel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-14 | Tampering | source modifiée entre relecture et pose | high | mitigate | égalité des blobs `origin/main` ↔ branche A relue ; la pose (41-05) lit les fichiers depuis `origin/main` |
| T-41-15 | Denial of service | check requis impossible (job renommé) | high | mitigate | contextes revérifiés contre la pointe courante juste avant l'ouverture |
| T-41-16 | Tampering | ligne CODEOWNERS sautée par GitHub | high | mitigate | `codeowners/errors` = 0 sur la branche avant merge et sur la branche par défaut après |
| T-41-17 | Tampering | écriture dans la zone interdite | medium | mitigate | aucun chemin `plugin/dev-orchestrator/` dans le diff de la PR |
</threat_model>

<verification>
PR de la phase mergée sans règle active, source identique à la relecture, CODEOWNERS sans erreur,
branche B ouverte.
</verification>

<success_criteria>
Le préalable de D-M14 est tenu : la pose peut commencer sur une source mergée.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-04-SUMMARY.md` when done — committé sur la branche B `feat/phase-41-preuves-release` (jamais sur `main` en direct).
</output>

---
phase: 01-dev-orchestrator
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - _internal/vibeflow-update.sh
  - dev-orchestrator/README.md
  - README.md
autonomous: true
requirements: [VERIF-01, VERIF-02, IDX-02]
must_haves:
  truths:
    - "test-dev-orchestrator.sh passe à 100% (exit 0)"
    - "Le test vérifie : index non vide, idempotence ensure-deps, ≥11 intentions routées, aucun mapping /vf-* orphelin"
    - "Le test vérifie les gates de densité (agent ≤250L, skills ≤500L) via check-file-size.sh ou équivalent"
    - "vibeflow-update.sh installe le module (AGENT.md + skills/ + scripts/ + references/) et régénère l'index à l'install"
    - "Le README du module et la table modules du README racine documentent dev-orchestrator"
  artifacts:
    - path: "dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      provides: "Suite de vérification complète du module"
      min_lines: 40
    - path: "dev-orchestrator/README.md"
      provides: "Doc complète du module (remplace le squelette)"
      min_lines: 30
  key_links:
    - from: "_internal/vibeflow-update.sh"
      to: "dev-orchestrator/scripts/build-gsd-index.sh"
      via: "régénération de l'index à l'install/update (IDX-02)"
      pattern: "build-gsd-index"
    - from: "dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      to: "dev-orchestrator/references/gsd-skills-index.md"
      via: "vérification mapping /vf-* non orphelin contre l'index"
      pattern: "gsd-skills-index"
---

<objective>
Verrouiller la qualité : suite `test-dev-orchestrator.sh`, intégration du module dans
`vibeflow-update.sh` (install + régénération auto de l'index = IDX-02), et finalisation du README.

Purpose: Garantir VERIF-01 (couverture des 4 axes de test) et VERIF-02 (gates de densité), et
rendre le module installable de bout en bout via l'installeur central. Boucle l'install : un
`vibeflow-update.sh install dev-orchestrator` doit poser l'agent, les verbes, les scripts et un
index frais.
Output: tests verts, installeur câblé, README complet (module + table racine).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Installeur à étendre (lire la logique d'install par type, lignes ~105-167) :
@_internal/vibeflow-update.sh

# Patterns de test à calquer (assert numérotés, fixtures temporaires, exit 0/1) :
@software-architecture/scripts/tests/test-check-file-size.sh

# Gate densité réutilisable :
@software-architecture/scripts/check-file-size.sh

# Artefacts produits aux plans 01-04 (cibles des tests) :
@dev-orchestrator/AGENT.md
@dev-orchestrator/references/gsd-skills-index.md
@dev-orchestrator/scripts/ensure-deps.sh

<interfaces>
<!-- Les 4 axes de test imposés par la spec §8 (VERIF-01) : -->
1. build-gsd-index.sh génère un index NON VIDE depuis les skills GSD installés.
2. ensure-deps.sh idempotent : 2e run = no-op (utiliser VF_ENSURE_DRY_RUN=1).
3. La table de routage de AGENT.md couvre ≥11 intentions.
4. Chaque skill /vf-* mappe vers une cible existante : skill GSD vérifié contre
   gsd-skills-index.md, OU superpowers (brainstorming), OU bootstrap interne (ensure-deps) — aucun orphelin.

<!-- VERIF-02 : densité — agent ≤250L, chaque skill /vf-* ≤500L. -->

<!-- L'installeur gère déjà 5 types (vibeflow-update.sh:105-150) : AGENT.md, skills/<name>/, scripts/,
     references/. Le module dev-orchestrator combine agent + multi-skills + scripts + references.
     Vérifier que tous ces types sont copiés ; AJOUTER un hook post-install qui régénère l'index
     (appel build-gsd-index.sh) pour IDX-02. References racine (companion AGENT, pas SKILL) : vérifier
     que references/ du module est bien copié dans le lab (l'agent les charge on-demand). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire test-dev-orchestrator.sh (4 axes + densité)</name>
  <files>dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <action>
    Créer la suite de test en calquant test-check-file-size.sh (shebang bash, `set -uo pipefail`,
    helpers `ok()`/`ko()`, asserts numérotés, exit 0 si tout passe sinon 1). Résoudre le chemin
    du module via `$(cd "$(dirname "$0")/../.." && pwd)`.
    Tests (VERIF-01) :
    - T1 — Index : exécuter `build-gsd-index.sh` puis vérifier que `references/gsd-skills-index.md`
      est non vide et contient ≥1 ligne `gsd-`.
    - T2 — Idempotence : `VF_ENSURE_DRY_RUN=1 bash scripts/ensure-deps.sh` deux fois ; vérifier
      exit 0 aux deux runs (pas d'échec, no-op stable).
    - T3 — Routage : compter dans AGENT.md (hors lignes commentaire) les cibles de routage distinctes
      (`grep -v '^#' | grep -Ec 'brainstorm|discuss-phase|plan-phase|execute-phase|quick|fast|verify-work|code-review|debug|autonomous|ship|progress|map-codebase'`) ≥ 11.
    - T4 — Mapping non orphelin : pour chaque `skills/vf-*/SKILL.md`, extraire la cible référencée
      (gsd-X / brainstorming / ensure-deps) et vérifier qu'elle existe : soit dans gsd-skills-index.md,
      soit = brainstorming (superpowers), soit = ensure-deps (bootstrap interne). Aucun orphelin.
    - T5 — Densité (VERIF-02) : AGENT.md ≤250L ; chaque skills/vf-*/SKILL.md ≤500L. (Réutiliser la
      logique de check-file-size.sh OU un `wc -l` direct avec les seuils.)
    Le script doit pouvoir tourner depuis le repo (chemins relatifs au module). Français pour les libellés.
  </action>
  <verify>
    <automated>bash -n dev-orchestrator/scripts/tests/test-dev-orchestrator.sh && bash dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; test $? -eq 0</automated>
  </verify>
  <done>test-dev-orchestrator.sh couvre les 4 axes (index, idempotence, ≥11 intentions, mapping non orphelin) + densité, et passe à exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler vibeflow-update.sh (install + régénération index) et finaliser README</name>
  <files>_internal/vibeflow-update.sh, dev-orchestrator/README.md, README.md</files>
  <action>
    1. Installeur (IDX-02) : éditer `_internal/vibeflow-update.sh`. Vérifier d'abord que les 5 types
       existants couvrent bien le module combiné (AGENT.md, skills/, scripts/, references/). Le point
       à corriger : les `references/` ne sont copiées aujourd'hui QUE si un `SKILL.md` racine existe
       (ligne ~146). Or dev-orchestrator a `references/` SANS SKILL.md racine (c'est un agent + skills).
       Étendre la copie des references/ pour les modules agent (copier `references/` vers
       `.claude/agents/<mod>-references/` OU un emplacement chargé on-demand par l'agent — choisir le
       plus simple cohérent avec STRUCTURE.md et le documenter). PUIS ajouter un hook post-install :
       après copie des scripts, si `scripts/build-gsd-index.sh` existe pour le module, l'exécuter pour
       régénérer l'index dans l'install (best-effort, ne pas faire échouer l'install si GSD absent).
       Garder l'idempotence et le style de log existant.
    2. README module : remplacer le squelette `dev-orchestrator/README.md` par une doc complète
       (overview, structure, install via vibeflow-update, usage NL + verbes /vf-*, note bootstrap).
    3. README racine : ajouter une ligne dans la table des modules du `README.md` racine
       (nom: dev-orchestrator, version, type "agent + skills + scripts", description courte).
    Français. Après édition, re-lancer le test pour confirmer non-régression.
  </action>
  <verify>
    <automated>bash -n _internal/vibeflow-update.sh && grep -q 'build-gsd-index' _internal/vibeflow-update.sh && grep -q 'dev-orchestrator' README.md && [ "$(wc -l < dev-orchestrator/README.md)" -ge 30 ] && bash dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; test $? -eq 0</automated>
  </verify>
  <done>vibeflow-update.sh copie l'agent+skills+scripts+references et régénère l'index post-install ; README module et table racine documentent dev-orchestrator ; les tests passent toujours.</done>
</task>

</tasks>

<verification>
- VERIF-01 : test-dev-orchestrator.sh couvre les 4 axes et passe à 100% ✓
- VERIF-02 : gates de densité (agent ≤250L, skills ≤500L) vérifiés par le test ✓
- IDX-02 : index régénéré à l'install via hook installeur ✓
- Module installable de bout en bout via vibeflow-update.sh (agent + verbes + scripts + index frais) ✓
- README module + table racine à jour ✓
</verification>

<success_criteria>
- Tests verts, installeur câblé, doc complète. Le module est distribuable.
- Critères de succès de phase 1 et 5 (ROADMAP) vérifiables programmatiquement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-05-SUMMARY.md`
</output>

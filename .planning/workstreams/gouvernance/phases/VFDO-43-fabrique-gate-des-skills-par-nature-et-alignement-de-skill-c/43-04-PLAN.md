---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/check-instruction-budget.sh
  - plugin/conductor/scripts/tests/test-check-instruction-budget.sh
  - .planning/instruction-budget-baselines.tsv
  - .planning/workstreams/gouvernance/REQUIREMENTS.md
autonomous: false
requirements: [FABR-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "check-instruction-budget.sh garde sa découverte des agents INCHANGÉE (glob à un niveau, D-03) et AJOUTE une seconde découverte, récursive, des SKILL.md : chaque dossier plugin/*/ sauf un module dont le module.json déclare le type doc-only, dossiers cachés et *-references élagués, le nombre de SKILL.md exclus étant imprimé"
    - "Un SKILL.md de plus de 500 lignes (fichier entier, même mesure que les agents) sort DEPASSEMENT-SKILL-ADR029 et rend rc 1 quand le ratchet est armé ; 500 lignes exactement passe ; aucun seuil d'avertissement n'est inventé pour les skills (ADR-029 n'en définit pas)"
    - "Un SKILL.md au frontmatter jamais refermé ou en lien symbolique est NON-VERIFIABLE (rc 2), jamais ignoré en silence"
    - "Sur une fixture sans aucun SKILL.md, le code de sortie et les verdicts des agents sont identiques à avant (les sept bascules de l'étape CI check-instruction-budget restent vraies)"
    - "Aucune hausse ni aucun ajout de ligne dans .planning/instruction-budget-baselines.tsv sans l'arbitrage humain du checkpoint de la Tâche 2, cité (canal + date) dans le commit même qui porte le changement (G-1, D-Q4 one-way)"
    - "Le bootstrap (somme des métadonnées name + description + when_to_use chargées à chaque session) est mesuré de façon déterministe — octets des lignes de ces clés sous LC_ALL=C, divisés par 4 — sur le corpus et selon le mode d'enforcement que Willy arbitre à la Tâche 2 ; jamais un corpus, une méthode d'estimation ou un plafond ajusté pour passer sous 2000 hors de cet arbitrage"
    - "Le dépôt réel reste vert (rc 0) avec la sentinelle armée, sauf si l'option plafond-dur-socle est arbitrée — auquel cas l'exécution de ce plan s'arrête après la Tâche 2 et le rouge attendu est consigné, jamais masqué"
  artifacts:
    - path: "plugin/conductor/scripts/check-instruction-budget.sh"
      provides: "VF_SKILL_LINE_CAP, seconde découverte des SKILL.md, verdict DEPASSEMENT-SKILL-ADR029, ligne BILAN-SKILLS ; métrique BOOTSTRAP selon l'option arbitrée"
      contains: "VF_SKILL_LINE_CAP=500"
    - path: "plugin/conductor/scripts/tests/test-check-instruction-budget.sh"
      provides: "cas SKILL-1 à SKILL-6, BOOT-1 à BOOT-5, mutants MUT-8 à MUT-11"
      contains: "MUT-8"
  key_links:
    - from: "check-instruction-budget.sh (seconde découverte)"
      to: "corpus des SKILL.md hors modules doc-only"
      via: "même critère d'exclusion que check-skills.sh (type doc-only lu dans module.json)"
      pattern: "doc-only"
    - from: "verdicts skills et bootstrap"
      to: "code de sortie existant (0/1/2/3/64)"
      via: "compteurs distincts, même priorité NON-VERIFIABLE > contrat > dépassement, aucun code nouveau"
      pattern: "SKILL_OVERRUN_COUNT"
  prohibitions:
    - statement: "MUST NOT faire monter une valeur de la baseline du budget d'instructions, ni y ajouter une ligne, sans l'arbitrage humain du checkpoint cité avec son canal et sa date dans le commit même ; MUST NOT présenter une décision de Claude comme un arbitrage humain (CLAUDE.md, traçabilité des arbitrages)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; G-1 ne voit que les lignes présentes à la base ET à HEAD (une ligne AJOUTÉE lui échappe, mesuré le 2026-09-25) — la garde est donc le checkpoint et la relecture, pas la machine"
    - statement: "MUST NOT choisir ou ajuster le corpus du bootstrap, sa méthode d'estimation ou le plafond de 2000 pour faire passer la mesure, hors de l'option arbitrée au checkpoint"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : se relit sur le diff et le SUMMARY (mesure publiée)"
---

<objective>
Étendre le budget d'instructions aux SKILL.md (plafond ADR-029 de 500 lignes, bloquant) et au
bootstrap (métadonnées chargées à chaque session, 2000 tokens estimés), en reprenant le socle de
check-instruction-budget.sh sans le réécrire, et faire arbitrer par Willy, chiffres à l'appui,
comment borner un bootstrap que la mesure montre DÉJÀ au-dessus du plafond.

Purpose: FABR-09 (D-Q4 : réserve du BACKLOG levée, SKILL.md + bootstrap — one-way). Le checkpoint
de la Tâche 2 n'est pas une réouverture de D-Q4 : il tranche ce que D-Q4 ne pouvait pas voir, la
mesure, et la seule manière de l'enforcer sans rendre la CI rouge le jour même ou sans légaliser en
silence un dépassement (information absente de toutes les sources — autorité de planification,
motif 2).
Output: plafond des SKILL.md armé et testé ; métrique bootstrap selon l'option arbitrée ;
éventuelle ligne de baseline citée.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md

Mesures du planificateur, 2026-09-25 (à rejouer à l'exécution, jamais recopiées en dur dans un test) :
- corpus SKILL.md hors `plugin/reference` : 21 fichiers, le plus long `plugin/skill-creator/skills/skill-creator/SKILL.md` à 485 lignes (43-03 y ajoute au plus 10 lignes) ; `plugin/installer/SKILL.md` n'a pas de module.json et reste dans le corpus ;
- bootstrap, octets des lignes name/description/when_to_use sous LC_ALL=C ÷ 4 : socle d'un lab minimal (fermeture `resolve-deps.sh conductor` = audit-architecture, conductor, consolidator, infrastructure-audit, planning-core, skill-creator, validator ; + skill `installer` exposé par `plugin/.claude-plugin/plugin.json` ; + les 7 commandes `plugin/commands/*.md`) ≈ 2 499 tokens (8 821 octets sur 11 SKILL.md + 1 176 octets de commandes) ; corpus distribué complet hors gabarits + commandes ≈ 4 682 ; conductor seul ≈ 998 ;
- ratchet ARMÉ sur ce dépôt (`.planning/.instruction-budget-armed` présente) ; la baseline `.planning/instruction-budget-baselines.tsv` est un chemin CODEOWNERS (revue de Samuel ou contournement tracé pour une PR ouverte par Willy) ;
- G-1 (`scripts/check-baseline-arbitrage.sh`) ne classe que les lignes présentes à la base ET à HEAD : une ligne ajoutée n'y est ni une hausse ni un retrait.
L'étape CI « check-instruction-budget » (`.github/workflows/ci.yml` l.637-815) rejoue sept bascules sur une fixture SANS SKILL.md et teste l'absence de la chaîne en capitales DEPASSEMENT sur son cas 2/7 : aucune ligne nouvelle ne doit porter ce mot hors d'un dépassement réel. Ce plan ne modifie PAS ci.yml.
Référence de phase (B43, révision du 2026-09-26) : un SHA FIGÉ, jamais re-dérivé. Il vit dans le
fichier neutre `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md`,
sur une ligne unique « Base de phase : <sha de 40 caractères> », écrite UNE seule fois au démarrage
de l'exécution par le premier des deux plans sans dépendance (43-01, ce plan) qui démarre, AVANT son
premier commit (`git rev-parse HEAD`), puis commitée seule (commit `.planning/` séparé) ; si le
fichier existe déjà, il n'est jamais réécrit (hors rebase, ci-dessous). Jamais écrite en
planification ni dans un PLAN.md, et jamais re-dérivée par un `git log` sur les PLAN.md : une
révision de plan pendant l'exécution déplacerait alors la base et viderait en silence, tout en
restant verts, les contrôles qui la lisent. Lecture par la chaîne
`B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && …` :
base absente, malformée ou sortie de l'histoire → rien n'est imprimé, et « rien d'imprimé » vaut
échec. Pourquoi pas la base de branche : la branche porte encore la Phase 42 tant que la PR #108
n'est pas mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de
la base de branche — mesuré le 2026-09-25). Après un rebase (merge de fiabilite), la ligne est
re-consignée une seule fois, sur le commit réécrit qui porte le même sujet que l'ancienne base,
l'ancienne gardée sous « Base de phase (avant rebase du <date>) : <sha> » — jamais pour faire passer
un contrôle. Distincte de la « Base du plan » de 43-04-SUMMARY.md (critère Gate-Touche local). Les
gardes G-1 et G-2 gardent leur propre base : elles jugent toute la branche, Phase 42 comprise.
</context>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tracer — seconde découverte récursive des SKILL.md et plafond de 500 lignes, du cas de fixture au dépôt réel armé (SKILL-1 à SKILL-6, MUT-8, MUT-9, MUT-10)</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh, plugin/conductor/scripts/tests/test-check-instruction-budget.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (fichier modifié, 394 lignes, en entier : contrat de sortie l.24-35 ; plafond l.62-72 ; découverte D-03 l.84-110 ; frontmatter_state, body_only, lines_count l.142-172 ; boucle de mesure et verdicts l.230-330 ; calcul du RC l.340-355 ; rapport l.357-394)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (fichier modifié : en-tête l.1-35 ; mk_root, w_lines, w_armed, w_baseline, run, std_agent, gen_lines l.60-130 ; bloc mutants l.501-660, remplacement de ligne exacte par awk)
    - plugin/conductor/scripts/check-agents.sh (l.291-316 decouvrir_agents : exclusions cachés / -references, liens symboliques)
    - .github/workflows/ci.yml (l.637-815, lecture seule : les sept bascules et le bloc « dépôt réel »)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Pattern 3, Pitfall 5, Assumption A4)
  </read_first>
  <behavior>
    - SKILL-1 : fixture armée (std_agent + baseline exacte) + `plugin/demo/skills/s/SKILL.md` de 500 lignes → rc 0, ligne du fichier en verdict OK ; 501 lignes → rc 1, DEPASSEMENT-SKILL-ADR029 ; même 501 sans sentinelle → rc 3, rapport imprimé
    - SKILL-2 : `plugin/solo/SKILL.md` (racine de module) et `plugin/demo/skills/profond/sous/SKILL.md` de 501 lignes → tous deux mesurés et en DEPASSEMENT-SKILL-ADR029
    - SKILL-3 (fixture armée, baseline exacte de l'agent) : `plugin/doc/module.json` de type doc-only + `plugin/doc/content/templates/skills/t/SKILL.md` de 501 lignes → non mesuré (rc 0), ligne « 1 SKILL.md exclu(s) (module doc-only) » ; `plugin/demo/skills/x-references/r/SKILL.md` et `plugin/demo/.cache/s/SKILL.md` de 501 lignes → non mesurés
    - SKILL-4 : SKILL.md au frontmatter jamais refermé → rc 2, NON-VERIFIABLE ; SKILL.md en lien symbolique → rc 2, NON-VERIFIABLE
    - SKILL-5 : fixture sans aucun SKILL.md, une ligne neutre ajoutée à l'agent (cas 2/7 de la CI) → rc 0, LIGNES-EN-HAUSSE présent, aucune occurrence de la chaîne en capitales DEPASSEMENT, ligne « corpus skills decouvert : 0 SKILL.md »
    - SKILL-6 : fixture sans SKILL.md et répertoire vide (cas 7/7) → rc 2 inchangé
    - MUT-8 : la comparaison du plafond des skills neutralisée → fixture SKILL-1 à 501 lignes : rc_original=1, rc_mutant=0
    - MUT-9 : la ligne d'exclusion doc-only neutralisée → fixture SKILL-3 : rc_original=0, rc_mutant=1
    - MUT-10 : l'élagage des dossiers *-references neutralisé → fixture SKILL-3 (partie -references) : rc_original=0, rc_mutant=1
  </behavior>
  <action>
<!-- planner-discipline-allow: DEPASSEMENT -->
1. Au démarrage, avant tout commit du plan : si `43-BASE-PHASE.md` n'existe pas dans le dossier de la phase (43-01 n'a pas encore démarré), l'écrire avec l'unique ligne « Base de phase : <sortie de git rev-parse HEAD> » et le commiter seul (`docs(43): base de phase figée`, commit `.planning/` séparé) ; s'il existe, le relire sans jamais le réécrire (bloc de contexte, Référence de phase). Puis, avant tout commit de code du plan, écrire la base du plan (`git rev-parse HEAD`) sur une ligne dédiée « Base du plan : <sha> » de 43-04-SUMMARY.md, créé dès maintenant (ligne placée hors du frontmatter, en début de ligne, et conservée quand le SUMMARY est complété puis commité en fin de plan, commit `.planning/` séparé) : le critère Gate-Touche du premier commit relit cette ligne. Tests d'abord (SKILL-1 à SKILL-6, MUT-8 à MUT-10), joués ROUGES, puis implémentation. Toutes les tailles dérivent d'une constante locale de la suite `PLAFOND_SKILL=500` (jamais un littéral dispersé), et les fixtures de skills se construisent avec gen_lines.
2. Script (socle repris, pas de réécriture — la découverte D-03 des agents, ses verdicts et la ligne `BILAN :` restent byte-identiques) :
   - constante `VF_SKILL_LINE_CAP=500` déclarée à côté de `VF_BUDGET_LINE_CAP`, jamais partagée avec elle (ADR-029 : skills ≤ 500 ; agents 300) ; aucun seuil d'avertissement pour les skills (ADR-029 n'en définit pas — décision de plan, 43-RESEARCH.md A4) ;
   - seconde découverte après celle des agents : pour chaque dossier `$ROOT/plugin/*/`, si son module.json déclare `"type": "doc-only"` (lu par grep, sans jq, comme check-version-sync.sh), compter ses SKILL.md dans `SKILL_EXCLUDED` et passer (ligne unique, cible MUT-9) ; sinon parcourir récursivement avec `find` sans suivre les liens, en élaguant les dossiers cachés et les dossiers *-references (deux motifs d'élagage, chacun sur sa propre ligne, cible MUT-10 pour le second), fichiers nommés SKILL.md, réguliers ou liens symboliques ; liste triée sous LC_ALL=C ;
   - mesure par fichier : lien symbolique → NON-VERIFIABLE ; `frontmatter_state` ouvert → NON-VERIFIABLE ; sinon `lines_count` (fichier entier) ; au-delà de `VF_SKILL_LINE_CAP` → verdict DEPASSEMENT-SKILL-ADR029 (comparaison `-gt` sur sa propre ligne, cible MUT-8), sinon OK ; compteurs distincts `SKILL_OVERRUN_COUNT` et `SKILL_NONVERIF_COUNT` ;
   - code de sortie, sans nouveau code : `SKILL_NONVERIF_COUNT` rejoint la branche NON-VERIFIABLE (rc 2), `SKILL_OVERRUN_COUNT` rejoint la branche dépassement (rc 1 si armé) ; non armé → 3 comme aujourd'hui ; aucune ligne de baseline n'est exigée pour les SKILL.md (FABR-09 demande le refus au-delà de 500, pas un ratchet d'instructions sur les skills — décision de plan : aucune hausse de baseline pour ce volet) ;
   - rapport, après la ligne `BILAN :` existante : `[check-instruction-budget] corpus skills decouvert : <N> SKILL.md (+ <E> exclu(s) sous un module doc-only)`, un tableau `SKILL | LIGNES | PLAFOND | VERDICT`, puis `BILAN-SKILLS : <N> SKILL.md, <K> depassement(s) du plafond <VF_SKILL_LINE_CAP>, <V> non verifiable(s), <E> exclu(s)` — la chaîne en capitales DEPASSEMENT n'apparaît que dans le verdict d'un dépassement réel ;
   - en-tête : section « SKILL.md (Phase 43, FABR-09, D-Q4) » (corpus, exclusions, plafond, absence de seuil d'avertissement, pourquoi pas de ratchet d'instructions sur les skills).
3. Mutants dans le bloc existant (remplacement de ligne exacte, cmp, `bash -n`) ; chaque ligne visée doit être unique dans le script et ne recopier aucune ligne déjà visée par MUT-1 à MUT-7 (qui restent tous tués).
4. Rejouer la suite, puis le dépôt réel (commande de vérification).
5. Commit : `feat(conductor): plafond ADR-029 des SKILL.md dans le budget d'instructions (FABR-09)` avec
   `Gate-Touche: plugin/conductor/scripts/check-instruction-budget.sh — seconde découverte et plafond des SKILL.md (D-Q4)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-instruction-budget.sh — SKILL-1 à SKILL-6, mutants MUT-8 à MUT-10`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh 2>&1 | grep -E 'SKILL-[1-6]|MUT-(8|9|10)|✗|== resultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes SKILL-1 à SKILL-6 ou MUT-8, MUT-9, MUT-10 n'a aucune ligne « ✓ », ou la ligne « == resultat » ne finit pas par « 0 ko == »</fails_when>
    <automated>out=$(bash plugin/conductor/scripts/check-instruction-budget.sh 2>&1); rc=$?; attendu=0; for d in plugin/*/; do if [ -f "$d/module.json" ] && grep -Eq '"type"[[:space:]]*:[[:space:]]*"doc-only"' "$d/module.json"; then continue; fi; attendu=$((attendu + $(find "$d" -name SKILL.md -type f | wc -l))); done; printf '%s\n' "$out" | grep -q "corpus skills decouvert : $attendu SKILL.md" && echo "REEL rc=$rc skills=$attendu"</automated>
    <fails_when>la ligne imprimée n'est pas « REEL rc=0 skills=<n> » (dépôt réel rouge, ou nombre de SKILL.md découverts différent du compte find hors doc-only)</fails_when>
    <automated>DEBUT=$(sed -n 's/^Base du plan : \([0-9a-f]\{7,40\}\).*$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-04-SUMMARY.md | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; revs=$(git rev-list --reverse "$DEBUT"..HEAD -- plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh) || echo "GIT-ECHEC rev-list"; first=$(printf '%s\n' "$revs" | head -1); test -n "$first" || echo "AUCUN-COMMIT-G2"; chemins=$(git diff-tree --no-commit-id --name-only -r "$first" -- plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh) || echo "GIT-ECHEC diff-tree"; msg=$(git show -s --format=%B "$first") || echo "GIT-ECHEC show"; for p in plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh; do printf '%s\n' "$chemins" | grep -qxF "$p" || continue; printf '%s\n' "$msg" | grep -qE "^Gate-Touche: $p (—|-) " || echo "NON-COUVERT $p"; done; echo "PREMIER-COMMIT-G2 $first"</automated>
    <fails_when>une ligne BASE-ABSENTE, GIT-ECHEC, AUCUN-COMMIT-G2 ou NON-COUVERT apparaît avant « PREMIER-COMMIT-G2 <sha> » : base du plan non consignée, ou le PREMIER commit de ce plan qui touche check-instruction-budget.sh ou sa suite (surface G-2) ne porte pas, dans SON message, un trailer Gate-Touche par chemin de la surface qu'il touche. G-2 (43-06) juge la branche entière et ne voit pas ce manquement local.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^VF_SKILL_LINE_CAP=500$' plugin/conductor/scripts/check-instruction-budget.sh` rend 1 et `grep -c '^VF_BUDGET_LINE_CAP=300$' plugin/conductor/scripts/check-instruction-budget.sh` rend 1 (plafond des agents intact)
    - la suite finit sur `0 ko ==` ; les lignes MUT-1 à MUT-7 existantes sont toujours `✓` ; MUT-8, MUT-9, MUT-10 sont `✓`
    - le dépôt réel imprime `REEL rc=0 skills=<n>` avec n égal au compte find hors doc-only
    - `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && d=$(git diff "$B43" HEAD -- .github/workflows/ci.yml) && printf '%s' "$d" | grep -c .` rend 0 (rien d'imprimé — base de phase figée illisible ou hors de l'histoire — vaut échec)
    - le premier commit du plan sur la surface G-2 couvre chaque chemin qu'il touche : la troisième commande du verify imprime `PREMIER-COMMIT-G2 <sha>` sans aucune ligne NON-COUVERT, GIT-ECHEC, AUCUN-COMMIT-G2 ni BASE-ABSENTE
  </acceptance_criteria>
  <done>Le budget couvre les SKILL.md à leurs trois profondeurs, bloque au-delà de 500 lignes quand il est armé, n'ignore aucun fichier en silence, et laisse intacts le contrat des agents et les sept bascules de la CI.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Tâche 2 : checkpoint — corpus et mode d'enforcement du bootstrap (le socle mesuré dépasse déjà 2000)</name>
  <decision>Comment borner le bootstrap de FABR-09 alors que le socle minimal d'un lab mesure déjà environ 2 500 tokens estimés pour un plafond de 2 000 ?</decision>
  <context>
D-Q4 (Willy, AskUserQuestion, session principale, 2026-09-24, one-way) a levé la réserve du BACKLOG
et inclus le bootstrap ; cette décision n'est pas rouverte. Ce que D-Q4 ne pouvait pas voir : la
mesure. Définition documentée du bootstrap (thresholds.md l.11 et l.47-52 ; mécanisme natif relu le
2026-09-25 sur https://code.claude.com/docs/en/skills : les descriptions des skills installés sont
chargées en contexte à chaque session) = somme des métadonnées name + description + when_to_use.
Estimation retenue par le plan : octets des lignes de ces clés sous LC_ALL=C divisés par 4
(déterministe sous awk BWK comme sous gawk ; même ordre de grandeur que le facteur ×12 tokens par
ligne de thresholds.md l.17). Mesures du 2026-09-25 (rejouées à l'exécution avant de poser la
question) : socle d'un lab minimal ≈ 2 499 ; corpus distribué complet hors gabarits ≈ 4 682 ;
conductor seul ≈ 998. Une lecture littérale de FABR-09 (« refuse tout bootstrap au-delà de 2000 »)
rend la CI rouge le jour même. Toute option qui écrit dans la baseline est une hausse au sens de G-1 :
elle exige la citation (canal + date) dans le commit même, et la revue code owner de Samuel ou un
contournement tracé (chemin CODEOWNERS).
Recommandation d'ordonnancement : poser cette question AVANT l'exécution (l'orchestrateur peut la
poser dès la fin de la planification) ; la Tâche 2 se réduit alors à consigner la réponse.
  </context>
  <options>
    <option id="ratchet-socle">
      <name>Ratchet sur le socle minimal</name>
      <pros>Corpus = fermeture `resolve-deps.sh conductor` + skill installer + commandes du plugin (ce que TOUT lab porte). Ligne de baseline `@bootstrap:socle` = mesure du jour ; toute hausse → DEPASSEMENT-BOOTSTRAP, rc 1 ; au-dessus de 2000 mais sous la ligne → AU-DESSUS-PLAFOND-ADR029, publié, non bloquant. CI verte ; toute croissance bloquée dès aujourd'hui ; les hausses futures de cette ligne sont vues par G-1 (colonne instructions).</pros>
      <cons>Légalise l'écart actuel au-dessus de 2000 (contredit la règle du budget des agents : une baseline ne légalise jamais un dépassement de plafond) ; FABR-09 se lit alors « refuse toute hausse » ; ligne de baseline ajoutée (citation obligatoire, G-1 ne voit pas l'ajout lui-même).</cons>
    </option>
    <option id="ratchet-corpus">
      <name>Ratchet sur le corpus distribué complet</name>
      <pros>Même mécanique, corpus = tous les SKILL.md hors modules doc-only + commandes (pire cas « tous modules installés », ≈ 4 682) ; pas de dépendance au résolveur de dépendances.</pros>
      <cons>Ligne plus haute, loin du plafond ; mesure un lab qui n'existe pas forcément ; mêmes coûts de citation et de légalisation que ratchet-socle.</cons>
    </option>
    <option id="avertissement-socle">
      <name>Avertissement seul sur le socle, exigence amendée</name>
      <pros>Mesure publiée, AVERTISSEMENT-BOOTSTRAP-ADR029 au-dessus de 2000, jamais bloquant ; aucune baseline touchée (pas de G-1) ; aligné sur thresholds.md (« Warning (à refondre) ») et sur le choix fait pour la dérive (D-Q5).</pros>
      <cons>Aucune borne machine sur la croissance ; FABR-09 amendé au ledger (« signale » au lieu de « refuse » pour le bootstrap) ; la réduction du socle part au backlog.</cons>
    </option>
    <option id="plafond-dur-socle">
      <name>Plafond dur de 2000 sur le socle</name>
      <pros>Lecture littérale de FABR-09.</pros>
      <cons>CI rouge aujourd'hui ; exige de retirer environ 500 tokens (≈ 2 000 octets) de descriptions dans sept modules et les commandes — mécanisme de déclenchement des skills — hors du budget de ce plan : l'exécution de 43-04 s'arrête ici, un plan complémentaire de mise en conformité du socle est à planifier (retour au planificateur), FABR-09 bootstrap non livré par ce plan.</cons>
    </option>
  </options>
  <read_first>
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md (l.5-15 tableau, l.45-52 bootstrap)
    - .planning/BACKLOG.md (section budget SKILL.md et bootstrap, réserve levée par D-Q4)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Pitfall 5, Open Question 3)
    - scripts/check-baseline-arbitrage.sh (l.1-90 : ce que G-1 voit et ne voit pas)
  </read_first>
  <action>
1. Rejouer la mesure en lecture seule sur les trois corpus (socle, complet hors gabarits, conductor seul) avec la définition et l'estimation du contexte ci-dessus, et présenter les chiffres du jour avec les quatre options, sans recommandation d'option (options neutres, usage du dépôt).
2. Si la question a déjà été tranchée avant l'exécution, ne pas la reposer : recopier la réponse et sa citation.
3. Consigner dans 43-04-SUMMARY la réponse verbatim, l'option retenue, la citation au format lu par G-1 (mot « arbitrage », au moins deux virgules, date ISO : « arbitrage Willy, AskUserQuestion session principale, AAAA-MM-JJ ») et les mesures du jour.
  </action>
  <acceptance_criteria>
    - 43-04-SUMMARY contient l'option retenue (exactement l'un des identifiants ratchet-socle, ratchet-corpus, avertissement-socle, plafond-dur-socle), la citation avec canal et date, et les trois mesures du jour
    - aucune écriture dans .planning/instruction-budget-baselines.tsv avant la réponse
  </acceptance_criteria>
  <resume-signal>Répondre par ratchet-socle, ratchet-corpus, avertissement-socle ou plafond-dur-socle, avec la citation à reporter (forme : « arbitrage Willy, AskUserQuestion session principale, AAAA-MM-JJ »).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : métrique bootstrap selon l'option arbitrée (BOOT-1 à BOOT-5, MUT-11), ligne de baseline citée ou amendement du ledger</name>
  <reversibility rating="one-way">D-Q4 lève une réserve écrite au BACKLOG ; l'option ratchet ajoute une ligne à une baseline CODEOWNERS lue par G-1 et par la CI ; l'option avertissement amende une exigence du ledger — revenir en arrière rouvrirait la réserve et l'arbitrage.</reversibility>
  <precondition>pour les options à corpus socle, command -v jq réussit (plugin/_internal/resolve-deps.sh l'exige, y compris dans les fixtures BOOT-1 et BOOT-2)</precondition>
  <files>plugin/conductor/scripts/check-instruction-budget.sh, plugin/conductor/scripts/tests/test-check-instruction-budget.sh, .planning/instruction-budget-baselines.tsv, .planning/workstreams/gouvernance/REQUIREMENTS.md</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (fichier modifié : seconde découverte et BILAN-SKILLS de la Tâche 1 ; lecture de la baseline, entrées orphelines, contrat de baseline l.112-140 et l.330-355)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (fichier modifié)
    - .planning/instruction-budget-baselines.tsv (en-tête commenté, forme chemin/lignes/instructions) — seulement pour les options ratchet
    - plugin/_internal/resolve-deps.sh (l.1-30 : VF_MODULES_ROOT, jq requis, sortie triée) ; plugin/.claude-plugin/plugin.json (champ skills) ; plugin/commands/ (frontmatter description)
    - .planning/workstreams/gouvernance/REQUIREMENTS.md (FABR-09) — seulement pour l'option avertissement-socle
    - 43-04-SUMMARY (réponse du checkpoint)
  </read_first>
  <behavior>
    - BOOT-1 (toutes options sauf plafond-dur-socle) : fixture avec socle — `plugin/conductor/module.json` requérant `dep`, `plugin/dep/module.json`, un SKILL.md sous chacun, `plugin/.claude-plugin/plugin.json` exposant `./installer`, `plugin/installer/SKILL.md`, `plugin/commands/c.md`, `plugin/_internal/resolve-deps.sh` copié du dépôt — → la ligne BOOTSTRAP porte exactement le nombre de tokens attendu, calculé par la suite INDÉPENDAMMENT du script (octets des lignes qu'elle a écrites, LC_ALL=C, ÷ 4)
    - BOOT-2 (ratchet) : ligne `@bootstrap:<corpus>` égale à la mesure → rc 0 ; description allongée de 8 octets → rc 1, DEPASSEMENT-BOOTSTRAP ; ligne absente et ratchet armé → rc 2 ; la ligne `@bootstrap:` n'est jamais comptée comme orpheline
    - BOOT-3 : fixture sans `plugin/conductor/module.json` (cas des sept bascules de la CI) → « BOOTSTRAP : non applicable », rc et verdicts identiques à la Tâche 1
    - BOOT-4 : socle présent mais `plugin/_internal/resolve-deps.sh` absent → NON-VERIFIABLE avec la cause, rc 2 (option socle seulement)
    - BOOT-5 (avertissement-socle) : mesure > 2000 → rc 0, AVERTISSEMENT-BOOTSTRAP-ADR029 ; ≤ 2000 → verdict OK ; (ratchet) mesure > 2000 et ≤ ligne → AU-DESSUS-PLAFOND-ADR029, rc 0
    - MUT-11 : la comparaison du bootstrap (à la ligne de baseline, ou au plafond pour l'avertissement) neutralisée → fixture de croissance : rc_original=1 et rc_mutant=0 (ratchet), ou avertissement présent sur l'original et absent sur le mutant (avertissement)
  </behavior>
  <action>
<!-- planner-discipline-allow: DEPASSEMENT -->
Branche selon la réponse consignée à la Tâche 2.
- **plafond-dur-socle** : ne rien implémenter pour le bootstrap. Consigner dans 43-04-SUMMARY la mesure du jour, l'écart au plafond, la liste des SKILL.md et commandes du socle avec leurs octets, et le besoin d'un plan complémentaire de mise en conformité ; marquer « FABR-09 : volet bootstrap non livré par 43-04 — retour au planificateur ». Arrêt de ce plan.
- **Commun aux trois autres options** :
  1. Tests d'abord (BOOT-1 à BOOT-5 applicables, MUT-11), puis implémentation.
  2. Constante `VF_BOOTSTRAP_TOKEN_CAP=2000` (ADR-029, doctrine du dépôt, jamais dans le manifeste daté). Mesure : pour chaque SKILL.md du corpus, somme des octets (LC_ALL=C, longueur de ligne + 1) des lignes de frontmatter appartenant aux clés name, description et when_to_use, lignes de continuation comprises, avec le même ancrage NR==1 que frontmatter_state ; pour chaque `plugin/commands/*.md`, mêmes règles sur description et when_to_use ; tokens = octets ÷ 4 (division entière). En-tête : définition, source (thresholds.md, doc officielle relue), pourquoi des octets et pas des caractères.
  3. Corpus socle (ratchet-socle, avertissement-socle) : modules rendus par `VF_MODULES_ROOT="$ROOT/plugin" bash "$ROOT/plugin/_internal/resolve-deps.sh" conductor` (le résolveur canonique de l'installeur, jamais un second parseur de requires), plus le skill désigné par le champ `skills` de `$ROOT/plugin/.claude-plugin/plugin.json`, plus `$ROOT/plugin/commands/*.md`. Sans `$ROOT/plugin/conductor/module.json` → « BOOTSTRAP : non applicable (aucun socle conductor sous --path) », aucun effet sur le rc. Socle présent mais résolveur absent ou en échec (jq manquant compris) → NON-VERIFIABLE avec la cause, rc 2. Corpus complet (ratchet-corpus) : les SKILL.md de la seconde découverte de la Tâche 1 plus les commandes.
  4. Ligne de rapport unique après BILAN-SKILLS : `BOOTSTRAP : <tokens> tokens estimes (octets/4) sur <corpus> (<n> fichiers), plafond <VF_BOOTSTRAP_TOKEN_CAP>, verdict <V>`.
- **ratchet-socle / ratchet-corpus** :
  5. Clé de baseline `@bootstrap:socle` ou `@bootstrap:corpus`, colonnes lignes = 0 et instructions = tokens (forme acceptée par G-1 et par le parseur existant) ; les clés commençant par `@bootstrap:` sont retirées du calcul des entrées orphelines et lues pour le verdict ; tokens > ligne → DEPASSEMENT-BOOTSTRAP (rejoint SKILL_OVERRUN_COUNT ou un compteur dédié, rc 1 si armé) ; tokens ≤ ligne et > plafond → AU-DESSUS-PLAFOND-ADR029 (publié, non bloquant) ; ≤ plafond → OK ; armé et ligne absente → contrat de baseline incomplet, rc 2.
  6. Ajouter la ligne avec la mesure du jour dans `.planning/instruction-budget-baselines.tsv` (et une ligne de commentaire d'en-tête expliquant la clé `@bootstrap:`), dans un commit SÉPARÉ dont le message porte la citation du checkpoint telle que consignée (« arbitrage Willy, AskUserQuestion session principale, AAAA-MM-JJ ») — jamais une décision de Claude présentée comme un arbitrage. Mentionner dans le SUMMARY que la PR exige la revue code owner de Samuel ou un contournement tracé (chemin CODEOWNERS), et que G-1 ne voit pas une ligne ajoutée.
- **avertissement-socle** :
  5. Verdict > plafond → AVERTISSEMENT-BOOTSTRAP-ADR029, jamais compté comme dépassement, rc inchangé ; aucune ligne de baseline.
  6. Amender FABR-09 dans `.planning/workstreams/gouvernance/REQUIREMENTS.md` (« refuse … tout bootstrap au-delà de 2000 tokens » → « signale en avertissement … »), avec la citation du checkpoint dans le texte et dans le message du commit ; recopier dans le SUMMARY le texte d'une entrée de backlog « réduire le bootstrap du socle sous 2000 tokens » (mesures jointes), à poser par vf-dev-manager.
- **Commits** (ratchet ou avertissement) : `feat(conductor): métrique bootstrap du budget d'instructions (FABR-09, <option>)` avec
  `Gate-Touche: plugin/conductor/scripts/check-instruction-budget.sh — métrique bootstrap, option <option> arbitrée`
  `Gate-Touche: plugin/conductor/scripts/tests/test-check-instruction-budget.sh — BOOT-1 à BOOT-5, mutant MUT-11`
  puis, selon l'option, le commit de baseline cité ou le commit d'amendement du ledger cité.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh 2>&1 | grep -E 'SKILL-[1-6]|BOOT-[1-5]|MUT-([1-9]|1[01]) |✗|== resultat'</automated>
    <fails_when>une ligne contient « ✗ », ou un cas BOOT applicable à l'option retenue n'a aucune ligne « ✓ », ou « ✓ MUT-11 » manque, ou la ligne « == resultat » ne finit pas par « 0 ko == » (ne s'applique pas à l'option plafond-dur-socle)</fails_when>
    <automated>out=$(bash plugin/conductor/scripts/check-instruction-budget.sh 2>&1); rc=$?; printf '%s\n' "$out" | grep -E '^BOOTSTRAP : [0-9]+ tokens' ; echo "REEL-BOOT rc=$rc"</automated>
    <fails_when>(options ratchet et avertissement) aucune ligne « BOOTSTRAP : <n> tokens » ou dernière ligne différente de « REEL-BOOT rc=0 » ; (option plafond-dur-socle) sans objet — consigner la sortie telle quelle au SUMMARY</fails_when>
    <automated>git fetch -q origin main 2>/dev/null; bash scripts/check-baseline-arbitrage.sh; echo "G1 rc=$?"; msg=$(git log --format=%B -1 -- .planning/instruction-budget-baselines.tsv) && printf '%s\n' "$msg" | grep -cE 'arbitrage.*,.*,.*[0-9]{4}-[0-9]{2}-[0-9]{2}'</automated>
    <fails_when>(options ratchet) la ligne « G1 rc=… » ne vaut pas 0 ou 3, ou le compte final vaut 0 (commit de baseline sans citation conforme) ; (autres options) le fichier de baseline n'a aucun commit sur la branche et la dernière valeur est donc sans objet</fails_when>
  </verify>
  <acceptance_criteria>
    - (ratchet) `grep -c '^@bootstrap:' .planning/instruction-budget-baselines.tsv` rend 1 et le commit qui l'ajoute porte une citation conforme (mot « arbitrage », au moins deux virgules, date ISO) reprise de 43-04-SUMMARY
    - (avertissement) c'est la LIGNE FABR-09 du ledger qui porte « signale » ET la citation : `grep -E '^- \[.\] \*\*FABR-09\*\*' .planning/workstreams/gouvernance/REQUIREMENTS.md | grep 'signale' | grep -cE 'arbitrage.*,.*,.*[0-9]{4}-[0-9]{2}-[0-9]{2}'` rend 1 (mesuré le 2026-09-26 avant amendement : 0 sur cette ligne, alors que le fichier porte déjà « signale » deux fois, sur FABR-07 et FABR-10 — un compte sur tout le fichier serait vert sans amendement) ; aucune ligne `@bootstrap:` dans la baseline
    - (ratchet, avertissement) le dépôt réel rend `REEL-BOOT rc=0` et publie la ligne BOOTSTRAP ; la suite finit sur `0 ko ==` avec MUT-1 à MUT-11 tués
    - (plafond-dur-socle) 43-04-SUMMARY consigne la mesure, l'écart, la liste du socle et « FABR-09 : volet bootstrap non livré par 43-04 — retour au planificateur » ; aucun code bootstrap commis
  </acceptance_criteria>
  <done>Le bootstrap est mesuré et borné exactement comme Willy l'a arbitré, sans gonfler ni masquer la mesure, et toute écriture de baseline porte sa citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus distribué → mesure | fichiers du dépôt, liens symboliques et frontmatters cassés possibles |
| baseline CODEOWNERS → verdict | la ligne de baseline décide du rouge ; une PR peut l'élever |
| décision humaine → commit | l'arbitrage n'existe que par sa citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-30 | Repudiation | ligne de baseline ajoutée | high | mitigate | checkpoint:decision bloquant avant toute écriture ; citation canal + date dans le commit même ; revue code owner (CODEOWNERS) ; limite nommée : G-1 ne voit pas une ligne ajoutée |
| T-43-31 | Tampering | corpus ou estimation du bootstrap | medium | mitigate | définition et estimation fixées par le plan et testées indépendamment (BOOT-1) ; option arbitrée consignée ; prohibition relue en PR |
| T-43-32 | Spoofing (faux vert) | découverte des SKILL.md | medium | mitigate | compte découvert comparé au compte find hors doc-only sur le dépôt réel ; exclusions imprimées ; symlinks et frontmatters ouverts en NON-VERIFIABLE (SKILL-4) |
| T-43-33 | Denial of Service | étape CI check-instruction-budget | medium | mitigate | aucune modification de ci.yml ; SKILL-5, SKILL-6 et BOOT-3 rejouent les cas 2/7 et 7/7 sur fixture sans SKILL.md ni socle |
| T-43-34 | Denial of Service | dépendance jq du résolveur | low | mitigate | socle présent + résolveur en échec → NON-VERIFIABLE explicite (BOOT-4), jamais un zéro silencieux |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` : 0 ko, MUT-1 à MUT-11 tués (MUT-11 selon l'option).
- `bash plugin/conductor/scripts/check-instruction-budget.sh` sur le dépôt réel : rc 0, `corpus skills decouvert : <n>` égal au compte find hors doc-only, ligne BOOTSTRAP selon l'option.
- `bash scripts/check-baseline-arbitrage.sh` : rc 0 (ou 3 si aucune ligne de baseline touchée).
</verification>

<success_criteria>
- FABR-09 : SKILL.md au-delà de 500 lignes refusé ; bootstrap mesuré et borné selon l'arbitrage de Willy ; toute hausse de baseline citée.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| `VF_SKILL_LINE_CAP`, `SKILL_OVERRUN_COUNT`, `SKILL_NONVERIF_COUNT`, `SKILL_EXCLUDED` | constantes et compteurs du budget |
| verdict `DEPASSEMENT-SKILL-ADR029`, lignes `corpus skills decouvert`, `BILAN-SKILLS` | rapport |
| `VF_BOOTSTRAP_TOKEN_CAP`, ligne `BOOTSTRAP :`, verdicts `DEPASSEMENT-BOOTSTRAP`, `AU-DESSUS-PLAFOND-ADR029` ou `AVERTISSEMENT-BOOTSTRAP-ADR029` | métrique bootstrap (selon l'option) |
| clé de baseline `@bootstrap:socle` ou `@bootstrap:corpus` | ligne de `.planning/instruction-budget-baselines.tsv` (options ratchet seulement) |
| cas `SKILL-1` à `SKILL-6`, `BOOT-1` à `BOOT-5`, mutants `MUT-8` à `MUT-11` | suite du budget |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-09 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici : 500 exactement vs 501 (SKILL-1), trois profondeurs (SKILL-2), exclusions doc-only / -references / cachés (SKILL-3), frontmatter ouvert et lien symbolique (SKILL-4), fixture sans SKILL.md (SKILL-5, SKILL-6), fixture sans socle (BOOT-3), résolveur en échec (BOOT-4).
- **Corpus et mode du bootstrap = décision humaine (Tâche 2)** : le plan n'en choisit aucun ; la mesure du planificateur est une information, pas un verdict.
- **Estimation octets ÷ 4** : ordre de grandeur stable, pas un tokenizer (le dépôt a écarté une dépendance tokenizer, `.planning/research/FEATURES.md` l.79-82, ADR-054) ; les descriptions accentuées sont légèrement surestimées (octets > caractères) — biais prudent.
- **Pas de ratchet d'instructions sur les SKILL.md** : FABR-09 exige le refus au-delà de 500 lignes ; ajouter 21 lignes de baseline serait une hausse de baseline sans exigence — décision de plan.
- **Descriptions tronquées à 1 536 caractères** par le runtime (doc skills) : non gaté ici.
- **Le fichier de baseline est partagé avec le compartiment fiabilite** : au rebase après un merge de fiabilite, re-mesurer ; toute hausse de la ligne `@bootstrap:` exige une nouvelle citation.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-04-SUMMARY.md` when done — réponse du checkpoint verbatim, option, citation, mesures du jour ; commit `.planning/` séparé.
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 17
lot: L17 — G-3 : détection APRÈS COUP d'un push direct sur `main` (alarme, jamais verrou)
type: execute
wave: 3
depends_on: ["41-16"]
autonomous: true
requirements: [PROT-05, QUAL-01]
files_modified:
  - scripts/check-push-sans-pr.sh
  - scripts/tests/test-check-push-sans-pr.sh
  - .github/workflows/ci.yml
  - CHANGELOG.md

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PÉRIMÈTRE — périmètre SANS ADMIN (option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Aucun ruleset, aucune PR obligatoire côté serveur, aucune écriture GitHub : le seul appel est une LECTURE `GET`. Le hook `pre-push` n'est pas touché (G-5 écartée). D-01 à D-08 restent suspendus"
    - "FOYER D'EXIGENCE — G-3 relève de **PROT-05** « les gardes in-repo de la surface de gate et du flux d'arrivée sur `main` sont visibles et tracées, jamais fermées », exigence créée par le manager le 2026-09-17 sur remontée du planificateur, née du recadrage sans admin (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). PROT-02 conserve son énoncé d'origine : ce plan lui verse une preuve de non-régression du flux de release, et c'est le plan de clôture 41-19 qui la porte au ledger"
    - "ALARME APRÈS COUP, JAMAIS UN VERROU — à écrire en en-tête du script, dans l'étape CI, dans l'ADR-072 et au SUMMARY : quand cette étape rougit, le commit est DÉJÀ sur `main`. Elle ne peut rien empêcher ; elle rend le fait visible et daté. Le précédent est écrit : `892f89a` (2026-09-16) est arrivé sur `main` sans PR et son run CI est rouge, sans que rien ne l'arrête"
    - "LIMITE DE FOND : cette garde vit dans le dépôt, donc la PR — ou le push direct — qu'elle juge peut la modifier ; une même PR peut changer le script, sa suite et l'étape CI et rester verte"
    - "L'étape ne tourne que sur `push` vers `main` (`if: github.event_name == 'push' && github.ref == 'refs/heads/main'`, même garde que l'étape `check-release-tag`) et reçoit `GH_TOKEN: ${{ github.token }}` — sans jeton, le `gh` du runner échoue en auth, et lire cet échec comme « aucune PR » serait exactement le faux négatif vécu sur la v2.39.0 le 2026-07-26"
    - "DEUX LECTURES EN CASCADE, jamais une seule : (1) `GET /repos/{owner}/{repo}/commits/{sha}/pulls` ; (2) si la liste est vide, `GET /repos/{owner}/{repo}/pulls?state=closed&sort=updated&direction=desc` et recherche d'une PR dont `merge_commit_sha` égale le sha jugé. La seconde lecture existe parce que ce dépôt merge AUSSI par rebase (PR #71 `72259f4` et #72 `306e25d`, et le tag `v2.63.0` pointe sur un commit rebasé) : pour un merge par rebase, l'association du sha à la PR par le premier point d'entrée n'est pas garantie. Sans cette cascade, la garde rougirait sur des merges parfaitement réguliers"
    - "ÉCHEC D'API JAMAIS CONFONDU AVEC LISTE VIDE : `gh` absent, échec d'authentification, code HTTP non 2xx, ou JSON imparsable → rc 2 NON VÉRIFIABLE, jamais rc 1 et jamais rc 0. C'est la comparaison la plus importante de cette garde, et elle a son mutant"
    - "PREMIER COMMIT D'UNE BRANCHE NEUVE, traité explicitement : si le sha précédent du push (`--before`) est absent ou entièrement nul (création de ref, `0000…`), il n'y a aucune base et la garde rend rc 3 `CREATION-DE-REF`, silence non bloquant"
    - "COMMIT DE MERGE, traité explicitement : un merge par merge commit est associé à sa PR par la première lecture ; un merge par squash ou par rebase l'est par la seconde (`merge_commit_sha`). Les deux voies sont exercées par des fixtures nommées"
    - "BORNE ÉCRITE : la garde juge le SOMMET du push (`--sha`, `github.sha`). Un push direct de plusieurs commits est signalé par son seul sommet ; les commits sous un sommet régulièrement associé à une PR ne sont pas ré-interrogés, puisqu'ils sont entrés par cette PR. Nommée, pas prétendue couverte"
    - "VERT À VIDE INTERDIT : rc 0 n'est rendu qu'après une réponse d'API effectivement lue et une association effectivement trouvée. Imprimer `decouverte: sha=<court> pulls_lues=<n> pr_associee=<numero|aucune> voie=<commits-pulls|merge-commit-sha>`"
    - "QUAL-01 — la suite porte les trois issues (PASS / FAIL / IMPARSABLE BRUYANT) et CINQ mutants, un par comparaison : vacuité de la liste, cascade `merge_commit_sha`, distinction échec d'API / liste vide, traitement de la création de ref, assertion de non-vacuité. Fixtures HORS RÉSEAU par injection de fichiers JSON ; aucun appel réseau dans la suite"
    - "PREUVE VERSÉE À PROT-02, exigence portée par le plan de clôture 41-19 — le flux de release n'est pas cassé : la release entre par une PR mergée, donc son sommet est associé ; le push d'un TAG ne déclenche pas ce workflow (`on.push.branches`, aucun `tags:`) ; l'étape est insérée AVANT `check-release-tag` et ne change ni son ordre ni sa condition. Prouvé par fixture, pas supposé"
    - "TRAÇABILITÉ — un seul arbitrage ou décision cité par commit ; tout commit qui cite l'arbitrage du périmètre écrit exactement « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ». Chaque commit de ce plan touche la surface de G-2 et porte donc ses trailers `Gate-Touche:`"
  artifacts:
    - path: "scripts/check-push-sans-pr.sh"
      provides: "G-3 — alarme après coup sur un commit arrivé sur `main` sans PR associée"
      contains: "PUSH-SANS-PR"
    - path: "scripts/tests/test-check-push-sans-pr.sh"
      provides: "trois issues QUAL-01 + cinq mutants, sans réseau"
    - path: ".github/workflows/ci.yml"
      provides: "étape `check-push-sans-pr` du job `gates`, conditionnelle push sur `main`, avec `GH_TOKEN`"
  key_links:
    - "L'étape est conditionnelle `main` : comme `check-release-tag`, elle est SAUTÉE sur une PR et sur un push de branche, donc la preuve de sa capacité à rougir ne peut pas venir d'un run de PR — elle vient de ses bascules de fixture, qui tournent sans condition dans l'étape elle-même"
    - "`scripts/` est l'outillage DU DÉPÔT, pas un module : aucun bump de module. La trace passe par `## Non releasé` du `CHANGELOG.md` racine"
---

<objective>
Poser G-3 : l'étape qui, sur un push vers `main`, demande à GitHub quelles PR sont associées au
commit et rougit si la réponse est vide. C'est une alarme après coup — le commit est déjà arrivé —
et le script, l'étape, l'ADR et le SUMMARY doivent le dire sans l'adoucir.

Purpose: PROT-05 — exigence neuve « les gardes in-repo de la surface de gate et du flux d'arrivée sur
`main` sont visibles et tracées, jamais fermées », créée par le manager le 2026-09-17 sur remontée du
planificateur ; PROT-02 garde son énoncé d'origine et reçoit de ce plan une preuve de
non-régression, portée au ledger par la clôture. Origine factuelle : un push direct contourne le flux bump → PR → merge → tag → release, et ce dépôt
en a déjà payé le prix (`892f89a`, run CI rouge sur `main`, 2026-09-16). Faute d'accès admin, aucune
règle ne peut l'empêcher : la phase le rend visible et daté.
Output: une garde à deux lectures en cascade, prouvée sans réseau, câblée en `gates` sur `main`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-16-SUMMARY.md

Fichiers à lire au début de la Task 1, jamais deux fois la même plage :
- `scripts/check-gate-touche.sh` (posé au plan 41-16 : structure d'en-tête, contrat de codes de
  sortie, forme de la ligne `decouverte:` — à reprendre)
- `scripts/check-release-tag.sh` (le seul autre gate de ce dépôt qui parle à GitHub : traitement du
  jeton, et le faux négatif d'auth vécu sur la v2.39.0 qu'il documente)
- `.github/workflows/ci.yml` : l'étape `check-gate-touche` (point d'insertion juste après) et
  l'étape `check-release-tag` avec sa condition `main` et son bloc `env: GH_TOKEN`
</context>

<discipline>
Contraintes d'exécution valables pour TOUTES les tâches de ce plan.

- **Aucun appel GitHub en écriture, dans aucune tâche.** Le seul appel de production est un `GET`.
  La suite n'appelle PAS le réseau : elle injecte des réponses JSON par fichier.
- **Aucune commande git en ligne dans un `<verify>` ni dans un appel bash direct.** La phase
  s'exécute dans un worktree isolé : le harnais refuse une commande qui nomme git sous une forme
  qu'il ne peut pas rattacher au worktree. Tout travail git vit dans les scripts versionnés.
- **Commandes proxifiées** : `diff` et `ls` mentent sur ce poste ; les préfixer de `rtk proxy`.
  Jamais de comptage par `grep | wc -l` — extraire puis comparer par `comm` / `cmp`, ou `awk`.
- **Aucun texte produit ici n'associe O-3 ni une garde à un mot d'achèvement.** Rejouer
  `tools/check-aucune-fermeture.sh` (plan 41-15) après chaque tâche qui écrit du texte.
- **Trailers** : chaque commit touchant `scripts/check-push-sans-pr.sh`,
  `scripts/tests/test-check-push-sans-pr.sh` ou `.github/workflows/ci.yml` porte un
  `Gate-Touche: <chemin> — <raison>` par chemin (ou un motif couvrant), sans quoi G-2 rougit.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : G-3 bout en bout — le script à deux lectures, et son étape CI câblée sur une seule bascule</name>
  <files>scripts/check-push-sans-pr.sh, .github/workflows/ci.yml</files>
  <behavior>
    Contrat observable de `scripts/check-push-sans-pr.sh [--repo O/R] [--sha SHA] [--before SHA] [--pulls-file F] [--closed-pulls-file F]` :
    - première lecture rendant au moins une PR → rc 0, `voie=commits-pulls`, verdict `PR-ASSOCIEE`
    - première lecture vide, seconde lecture portant une PR dont `merge_commit_sha` égale le sha → rc 0, `voie=merge-commit-sha`, verdict `PR-ASSOCIEE`
    - les deux lectures vides d'association → rc 1, verdict `PUSH-SANS-PR`, avec le sha court et le nombre de PR examinées
    - `gh` absent, échec d'auth, code HTTP non 2xx, ou JSON imparsable sur l'une des deux lectures → rc 2, verdict `NON-VERIFIABLE`, JAMAIS rc 1
    - `--before` absent ou entièrement nul (création de ref) → rc 3, verdict `CREATION-DE-REF`
    - `--sha` absent et aucun sha dérivable → rc 2
    - argument inconnu, option sans valeur, `--pulls-file` vers un fichier inexistant → rc 64
  </behavior>
  <action>
Écrire `scripts/check-push-sans-pr.sh`, gate d'outillage du dépôt. Reprendre la structure de
`scripts/check-gate-touche.sh` : `set -uo pipefail`, en-tête « pourquoi ce script existe »,
énumération de TOUS les codes de sortie, section des surcharges de fixture, `-h|--help`.

**En-tête.** Trois choses à écrire sans les adoucir. (1) C'est une ALARME APRÈS COUP : quand cette
garde rougit, le commit est déjà sur `main` ; elle ne peut rien empêcher. Nommer le précédent
mesuré : `892f89a` (2026-09-16, `docs(explore)`) est arrivé sur `main` sans PR et son run CI est
rouge — un push direct a déjà cassé `main` sans que rien ne l'arrête. (2) La limite de fond : la
garde vit dans le dépôt, donc ce qu'elle juge peut la modifier. (3) Faute d'accès admin, aucune
règle côté serveur n'existe pour imposer la PR (option (a), arbitrage Samuel, AskUserQuestion
session principale, 2026-09-17) ; l'amendement d'ADR-059 sur la PR obligatoire est hors périmètre,
puisque aucune règle serveur ne l'appliquerait.

**Options.** `--repo <owner/name>` (défaut : dérivé de `GITHUB_REPOSITORY` s'il est posé, sinon de
l'URL du remote `origin`, sinon rc 2) ; `--sha <sha>` (défaut : `GITHUB_SHA` s'il est posé, sinon
`HEAD` résolu, sinon rc 2) ; `--before <sha>` (défaut : `GITHUB_EVENT_BEFORE` s'il est posé, sinon
non renseigné) ; `--pulls-file <f>` et `--closed-pulls-file <f>` — POUR LES FIXTURES UNIQUEMENT,
section nommée dans l'en-tête, absentes de toute invocation de production.

**Comparaison 1 — création de ref.** Si `--before` est fourni et vaut une suite de zéros (longueur
40 ou 7, uniquement des `0`), ou si le sha jugé n'a aucun parent, rendre rc 3 `CREATION-DE-REF` avec
la raison : il n'y a aucune base, donc rien à imputer. Ne PAS rendre rc 1 : le premier commit d'une
ref neuve n'est pas un push direct sur une ref existante. Si `--before` est absent, poursuivre
normalement (le cas d'une invocation à la main).

**Comparaison 2 — première lecture.** Sans `--pulls-file`, invoquer
`gh api repos/<repo>/commits/<sha>/pulls` en lecture seule, sortie capturée, code de sortie capturé.
Avec `--pulls-file`, lire le fichier. Distinguer TROIS états, jamais deux : commande absente ou code
de sortie non nul ou sortie vide ou JSON imparsable → `INDETERMINE` ; JSON valide portant un tableau
non vide → `ASSOCIEE` avec le numéro de la première PR ; JSON valide portant un tableau vide →
`VIDE`. Parser en `jq` si disponible, sinon par un lecteur `awk` sur les clés attendues — et si
aucun des deux ne permet de conclure, `INDETERMINE`, jamais `VIDE`. Un `INDETERMINE` mène à rc 2
immédiatement, avec le code et les premières lignes de la sortie recopiées.

**Comparaison 3 — seconde lecture, cascade.** Sur `VIDE` seulement, invoquer
`gh api "repos/<repo>/pulls?state=closed&sort=updated&direction=desc&per_page=30"` (ou lire
`--closed-pulls-file`) et chercher une entrée dont `merge_commit_sha` égale le sha jugé. Écrire la
raison de cette seconde lecture dans l'en-tête : ce dépôt merge aussi par rebase (PR #71 `72259f4`,
PR #72 `306e25d`, et le tag `v2.63.0` pointe sur un commit rebasé), et pour un merge par rebase
l'association du sha à sa PR par la première lecture n'est pas garantie — sans cette cascade, la
garde rougirait sur des merges réguliers. Mêmes trois états que la comparaison 2 : `INDETERMINE` →
rc 2 ; trouvé → rc 0, `voie=merge-commit-sha` ; non trouvé → rc 1 `PUSH-SANS-PR`.

**Découverte et non-vacuité.** Imprimer
`decouverte: sha=<court> pulls_lues=<n> closed_examinees=<m> pr_associee=<numero|aucune> voie=<commits-pulls|merge-commit-sha|aucune>`.
Aucun rc 0 sans `pr_associee` renseignée. Aucun rc 1 sans que les DEUX lectures aient abouti à un
JSON valide — l'écrire en en-tête : une alarme fondée sur une réponse qu'on n'a pas lue serait pire
qu'une absence d'alarme.

**Borne à écrire.** La garde juge le SOMMET du push. Un push direct de plusieurs commits est signalé
par son seul sommet ; les commits sous un sommet régulièrement associé à une PR ne sont pas
ré-interrogés, puisqu'ils sont entrés par cette PR. Borne nommée, pas prétendue couverte.

**Câblage CI, une seule bascule dans cette tâche.** Insérer dans le job `gates` de
`.github/workflows/ci.yml`, JUSTE APRÈS l'étape `check-gate-touche` et AVANT `check-release-tag`,
une étape nommée `check-push-sans-pr (G-3, PROT-05 — alarme après coup sur un push direct vers
main)`. Deux blocs dans une étape unique :
- le bloc de preuve par fixture, SANS condition (donc joué sur toute PR et tout push de branche) :
  un `--pulls-file` pointant un tableau JSON vide et un `--closed-pulls-file` pointant un tableau
  sans `merge_commit_sha` correspondant, avec `--sha` et `--before` explicites, attendu rc 1 et
  `PUSH-SANS-PR` sur la sortie ; écart → `::error::` et `exit 1` ;
- le bloc de mesure réelle, SOUS CONDITION
  `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`, avec
  `env: GH_TOKEN: ${{ github.token }}`, invoquant la garde sans fichier de fixture, sur
  `--repo ${{ github.repository }} --sha ${{ github.sha }} --before ${{ github.event.before }}`.
Comme une étape GitHub Actions ne porte qu'une seule condition, poser DEUX étapes : `check-push-sans-pr
(G-3 — preuve par fixture, sans condition)` et `check-push-sans-pr (G-3, PROT-05 — mesure réelle sur
un push vers main)`, la seconde seule conditionnée et seule porteuse de `GH_TOKEN`. Commenter les
deux : pourquoi elles existent, et le fait qu'une étape conditionnelle `main` ne peut pas prouver sa
capacité à rougir depuis un run de PR — d'où l'étape de fixture sans condition, qui tourne partout.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n scripts/check-push-sans-pr.sh && rc=0; bash scripts/check-push-sans-pr.sh --option-inconnue >/dev/null 2>&1 || rc=$?; echo "usage rc=$rc (attendu 64)"; [ "$rc" -eq 64 ]'</automated>
    <fails_when>`bash -n` échoue ; ou `usage rc=` différent de 64</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); printf "[]\n" > "$T/vide.json"; printf "[{\"number\":1,\"merge_commit_sha\":\"deadbeefdeadbeefdeadbeefdeadbeefdeadbeef\"}]\n" > "$T/closed.json"; rc=0; out=$(bash scripts/check-push-sans-pr.sh --repo o/r --sha 1111111111111111111111111111111111111111 --before 2222222222222222222222222222222222222222 --pulls-file "$T/vide.json" --closed-pulls-file "$T/closed.json" 2>&1) || rc=$?; printf "%s\n" "$out"; echo "fixture vide rc=$rc"; [ "$rc" -eq 1 ]; printf "%s\n" "$out" | awk "/PUSH-SANS-PR/{f=1} END{exit f?0:1}"; printf "%s\n" "$out" | awk "/^decouverte: sha=/{f=1} END{exit f?0:1}"'</automated>
    <fails_when>rc différent de 1 ; ou `PUSH-SANS-PR` absent ; ou ligne `decouverte: sha=` absente</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); printf "[{\"number\":42}]\n" > "$T/p.json"; rc=0; out=$(bash scripts/check-push-sans-pr.sh --repo o/r --sha 1111111111111111111111111111111111111111 --before 2222222222222222222222222222222222222222 --pulls-file "$T/p.json" 2>&1) || rc=$?; printf "%s\n" "$out"; echo "fixture associee rc=$rc"; [ "$rc" -eq 0 ]; printf "%s\n" "$out" | awk "/voie=commits-pulls/{f=1} END{exit f?0:1}"; rc=0; out=$(bash scripts/check-push-sans-pr.sh --repo o/r --sha 1111111111111111111111111111111111111111 --before 0000000000000000000000000000000000000000 --pulls-file "$T/p.json" 2>&1) || rc=$?; printf "%s\n" "$out"; echo "creation de ref rc=$rc"; [ "$rc" -eq 3 ]'</automated>
    <fails_when>l'un des deux rc est différent de l'attendu (0 puis 3) ; ou `voie=commits-pulls` absent ; ou la création de ref ne rend pas 3</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates --step "check-push-sans-pr" > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "etapes G-3 rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou `etapes G-3 rc=` différent de 0 ; ou `rejouees=` à 0 (aucune étape G-3 dans le job `gates`). L'étape conditionnelle `main` est légitimement sautée ou non sélectionnée par le rejeu : c'est l'étape de fixture SANS condition qui doit être verte</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-push-sans-pr.sh` existe, passe `bash -n`, rend 64 sur argument inconnu
    - fixtures hors réseau : liste vide sans correspondance → rc 1 `PUSH-SANS-PR` ; liste non vide → rc 0 `voie=commits-pulls` ; `--before` nul → rc 3 `CREATION-DE-REF`
    - l'en-tête porte les trois affirmations non adoucies (alarme après coup, limite de fond, aucune règle serveur), la raison de la seconde lecture avec ses deux PR mesurées, et la borne du sommet de push
    - deux étapes sont posées dans `gates` : la fixture sans condition, et la mesure réelle conditionnée `main` avec `GH_TOKEN`
  </acceptance_criteria>
  <done>La garde existe, ses deux voies d'association et sa distinction échec/vide sont prouvées hors réseau, et son étape de fixture tourne sur tout run.</done>
</task>

<task type="auto">
  <name>Task 2 : la suite — trois issues QUAL-01, cinq mutants, aucun appel réseau</name>
  <files>scripts/tests/test-check-push-sans-pr.sh</files>
  <precondition>`scripts/check-push-sans-pr.sh` existe et passe `bash -n` (Task 1 verte).</precondition>
  <read_first>
    - `scripts/tests/test-check-gate-touche.sh` (patron `ok`/`ko` à trois champs, patron de mutant vérifié par `cmp` puis `bash -n`)
  </read_first>
  <action>
Écrire `scripts/tests/test-check-push-sans-pr.sh` sur le patron de la suite de G-2 : `set -uo
pipefail`, `ok`/`ko` à trois champs distincts, `trap`, comparaisons par `cmp`/`comm`, jamais `diff`.
**Aucun appel réseau** : toutes les réponses d'API sont des fichiers JSON écrits dans le `mktemp`.
Écrire cette contrainte en en-tête, avec sa raison : une suite qui appellerait GitHub serait rouge
hors ligne et muette en cas de quota, donc incapable de prouver quoi que ce soit de façon stable.

Un helper `ecrire_json <nom> <contenu>` pose une réponse ; un helper `invoquer <args…>` appelle la
garde avec `--repo o/r` et des sha fixes.

Cas de contrat, au minimum :
- PASS : première lecture portant une PR → rc 0 `voie=commits-pulls` ; première lecture vide et
  seconde portant une PR dont `merge_commit_sha` égale le sha jugé → rc 0 `voie=merge-commit-sha`
  (cas nommé « merge par rebase ») ; première lecture portant deux PR → rc 0 ; `merge_commit_sha`
  correspondant en casse différente de la casse du sha fourni → rc 0 (comparaison insensible à la
  casse, cas nommé)
- FAIL : les deux lectures vides → rc 1 `PUSH-SANS-PR` ; première vide, seconde portant des PR dont
  aucun `merge_commit_sha` ne correspond → rc 1 avec `closed_examinees` non nul
- BRUYANT : première lecture JSON tronqué → rc 2 ; première lecture vide et seconde JSON tronqué →
  rc 2 ; fichier de fixture inexistant → rc 64 ; simulation d'échec d'auth (réponse portant un
  message d'erreur d'API et non un tableau) → rc 2, jamais rc 1 — cas nommé « faux négatif d'auth,
  précédent v2.39.0 du 2026-07-26 »
- SILENCE : `--before` à 40 zéros → rc 3 `CREATION-DE-REF` ; `--before` à 7 zéros → rc 3
- USAGE : option inconnue → 64 ; `--sha` sans valeur → 64 ; `--repo` mal formé (sans barre
  oblique) → 64 ou 2 selon le contrat écrit en Task 1, et le cas ASSERTE la valeur choisie
- CONTRÔLE NÉGATIF qui doit rester rc 0 : première lecture portant une PR alors que la seconde
  lecture ne correspondrait pas — la cascade ne doit pas s'exécuter du tout, et un rouge ici serait
  aussi faux qu'un vert de complaisance

Cinq mutants, un par comparaison, chacun par copie du script dans le `mktemp` et substitution
ciblée (`awk` vers un temporaire puis `mv`, jamais `sed -i`), REFUSÉ s'il est identique à l'original
(`cmp -s`) ou si `bash -n` échoue :
- MUT-1 neutralise le test de vacuité de la première lecture (toujours associée) → la fixture « deux
  lectures vides » devient verte sur le mutant, reste rouge sur l'original
- MUT-2 neutralise la cascade `merge_commit_sha` (seconde lecture ignorée) → la fixture « merge par
  rebase » devient ROUGE sur le mutant, reste verte sur l'original. C'est le mutant qui prouve que la
  cascade existe pour une raison mesurée, pas par prudence décorative
- MUT-3 confond échec d'API et liste vide → la fixture « échec d'auth » rend 1 sur le mutant, 2 sur
  l'original
- MUT-4 neutralise le traitement de la création de ref → la fixture `--before` à zéros rend 1 sur le
  mutant, 3 sur l'original
- MUT-5 neutralise l'assertion de non-vacuité (rc 0 rendu sans `pr_associee`) → une fixture sans
  aucune réponse lisible rend 0 sur le mutant, 2 sur l'original

Bilan `== bilan : <PASS> ok, <FAIL> ko ==`, sortie non nulle si `FAIL` non nul. En-tête : recopier la
limite de fond, le caractère d'alarme après coup, et le fait qu'une suite incapable de rougir est un
défaut.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e scripts/tests/test-check-push-sans-pr.sh</automated>
    <fails_when>exit non nul ; ou une ligne `ko` ; ou `NON OPPOSABLE` dans la sortie ; ou le bilan annonce un nombre de `ko` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); bash scripts/tests/test-check-push-sans-pr.sh > "$T/o" 2>&1; m=$(awk "/MUT-[1-5]/ && /✓/ {c++} END{print c+0}" "$T/o"); p=$(awk "/✓/{c++} END{print c+0}" "$T/o"); echo "mutants tues=$m assertions ok=$p"; [ "$m" -ge 5 ] && [ "$p" -ge 20 ]'</automated>
    <fails_when>`mutants tues=` inférieur à 5 ; ou `assertions ok=` inférieur à 20</fails_when>
    <automated>bash --noprofile --norc -e -c 'n=$(awk "/gh api|curl |wget /{c++} END{print c+0}" scripts/tests/test-check-push-sans-pr.sh); echo "appels reseau dans la suite=$n"; [ "$n" -eq 0 ]'</automated>
    <fails_when>`appels reseau dans la suite=` différent de 0 (la suite doit être hors réseau ; une invocation nommée dans un commentaire compte aussi — la reformuler)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend 0, aucun `ko`, aucun `NON OPPOSABLE`, MUT-1 à MUT-5 tués
    - le cas « échec d'auth » rend 2 et jamais 1, et MUT-3 prouve le flip
    - le cas « merge par rebase » rend 0 par la voie `merge-commit-sha`, et MUT-2 prouve qu'il rougirait sans la cascade
    - aucune invocation réseau dans le fichier de suite
  </acceptance_criteria>
  <done>G-3 a une suite hors réseau qui sait la mettre en défaut sur ses deux lectures, sa distinction échec/vide, la création de ref et sa non-vacuité.</done>
</task>

<task type="auto">
  <name>Task 3 : étapes CI complétées, compatibilité du flux de release prouvée, CHANGELOG, rejeu</name>
  <files>.github/workflows/ci.yml, CHANGELOG.md</files>
  <precondition>Tasks 1 et 2 vertes ; `rtk proxy git status --porcelain` ne liste que les chemins de ce plan.</precondition>
  <action>
Compléter l'étape de fixture `check-push-sans-pr (G-3 — preuve par fixture, sans condition)` pour
porter QUATRE bascules isolées, chacune sur des fichiers JSON réécrits depuis zéro :
1. première lecture portant une PR → attendu rc 0, sortie portant `voie=commits-pulls`
2. première lecture vide, seconde portant le `merge_commit_sha` du sha jugé → attendu rc 0, sortie
   portant `voie=merge-commit-sha` (le cas du merge par rebase de ce dépôt)
3. les deux lectures vides → attendu rc 1, sortie portant `PUSH-SANS-PR`
4. réponse d'erreur d'API au lieu d'un tableau → attendu rc 2, et JAMAIS rc 1
Compteur `fail`, fonction `note`, bilan `== BILAN preuve fixture : <n> écart(s) sur 4 verdicts ==`
puis `exit 1` si `n` non nul, puis la phrase qui dit l'étape discriminante. L'étape de mesure réelle
conditionnée `main` reste telle que posée en Task 1.

**Compatibilité du flux de release (PROT-02), prouvée et non supposée.** Vérifier et consigner au
SUMMARY, chacun sur pièce :
- l'ordre des étapes du job `gates` : les deux étapes de G-3 sont AVANT `check-release-tag`, et ni la
  condition ni le `run:` de `check-release-tag` n'ont été modifiés (comparaison du bloc de l'étape
  par `cmp` contre son état à la base de la branche, à l'intérieur d'un script ou d'un `awk` sur le
  fichier — jamais par une lecture à l'œil) ;
- le déclencheur du workflow : `on.push.branches` sans entrée `tags:`, donc le push d'un tag annoté
  `vX.Y.Z` ne déclenche pas ce workflow et ne peut pas faire rougir G-3 — recopier la ligne lue ;
- le cas de la release : elle entre par une PR mergée, donc son sommet est associé par la première
  ou la seconde lecture — prouvé par la bascule 2 pour la voie rebase, la seule voie que la première
  lecture pouvait manquer.

`CHANGELOG.md` racine, section `## Non releasé` : ajouter une entrée sous celle de G-2, nommant G-3,
son caractère d'ALARME APRÈS COUP (jamais un verrou), ses deux lectures en cascade et la raison
mesurée de la seconde, la distinction échec d'API / liste vide, la borne du sommet de push, le fait
que le hook `pre-push` n'est pas touché, et l'autorisation « arbitrage Samuel, AskUserQuestion
session principale, 2026-09-17 ». Aucune section `## [vX.Y.Z]`, aucune touche à la `VERSION` racine,
aucun bump de module.

Rejouer les DEUX jobs avec la découverte complète, puis le recensement
`tools/check-aucune-fermeture.sh` et le contrôle `tools/check-trace-arbitrage.sh`. Consigner au
SUMMARY chaque ligne `REJEU`, le nombre d'étapes `gates`, le bilan du job `tests`, tout `SKIP` avec
sa raison, et le périmètre non rejoué — dont l'étape de mesure réelle de G-3, conditionnelle `main`,
qui ne peut pas être exercée depuis une branche : le dire, ne pas le masquer.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU|^== BILAN preuve fixture/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 13 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 13 (l'étape de fixture de G-3 n'a pas été ajoutée) ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); echo "tests rc=$rc bilan(N M)=$b"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -ge 82 ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N inférieur à 82 (la suite de G-3 n'est pas découverte) ; ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'C=.github/workflows/ci.yml; o=$(awk "/^      - name: check-push-sans-pr/{p=NR} /^      - name: check-release-tag/{r=NR} END{ print (p>0 && r>0 && p<r) ? \"oui\" : \"non\" }" "$C"); t=$(awk "/^  push:/{f=1;next} /^  pull_request:/{f=0} f && /tags:/{c++} END{print c+0}" "$C"); g=$(awk "/GH_TOKEN: \\\$\\{\\{ github.token \\}\\}/{c++} END{print c+0}" "$C"); echo "G3_avant_release_tag=$o entrees_tags_dans_push=$t occurrences_GH_TOKEN=$g"; [ "$o" = oui ] && [ "$t" -eq 0 ] && [ "$g" -ge 2 ]'</automated>
    <fails_when>`G3_avant_release_tag=` différent de `oui` ; ou `entrees_tags_dans_push=` différent de 0 (un push de tag déclencherait le workflow et G-3 rougirait sur une release) ; ou `occurrences_GH_TOKEN=` inférieur à 2 (l'étape de mesure réelle n'a pas son jeton : le faux négatif d'auth de la v2.39.0 serait rejoué)</fails_when>
    <automated>bash --noprofile --norc -e -c 'a=$(awk "/^## Non releasé/{f=1;next} /^## \\[/{f=0} f && /check-push-sans-pr/{c++} END{print c+0}" CHANGELOG.md); v=$(awk "NR==1{print \$1}" VERSION); r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh >/dev/null 2>&1 || r=$?; s=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh >/dev/null 2>&1 || s=$?; echo "changelog_G3=$a VERSION=$v recensement_rc=$r trace_rc=$s"; [ "$a" -ge 1 ] && [ "$v" = "v2.63.2" ] && [ "$r" -eq 0 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>`changelog_G3=` à 0 ; ou `VERSION=` différent de `v2.63.2` ; ou `recensement_rc=` différent de 0 ; ou `trace_rc=` différent de 0 (un commit de la branche cite un arbitrage hors forme canonique, ou en cite plus d'un)</fails_when>
  </verify>
  <acceptance_criteria>
    - l'étape de fixture de G-3 porte quatre bascules isolées, bilan à 0 écart, dont la bascule 4 qui prouve qu'un échec d'API rend 2 et jamais 1
    - les trois preuves de compatibilité du flux de release sont consignées au SUMMARY sur pièce (ordre des étapes, absence de `tags:` dans le déclencheur, voie rebase)
    - rejeu `gates` rc 0 avec au moins 13 étapes ; rejeu `tests` rc 0 avec au moins 82 suites et 0 échec
    - `CHANGELOG.md` § Non releasé porte l'entrée G-3 ; `VERSION` racine intacte ; aucun module bumpé ; recensement et contrôle de trace à rc 0
    - le périmètre non rejoué est nommé, dont l'étape de mesure réelle conditionnelle `main`
  </acceptance_criteria>
  <done>G-3 est câblée en deux étapes, prouvée rouge sur fixture sans condition, et la compatibilité du flux de release est établie sur pièce plutôt que supposée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API GitHub → verdict de la garde | un échec d'appel ressemble à une réponse vide, et l'un doit bloquer, l'autre alarmer |
| méthode de merge → association sha ↔ PR | un merge par rebase change les sha, donc l'association par la première lecture n'est pas garantie |
| push direct → `main` | aucune règle côté serveur n'existe : le commit arrive, la garde ne fait que le constater |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-72 | Elevation of privilege | push direct sur `main` sans PR | high | accept | aucun accès admin, donc aucune règle serveur ne peut l'empêcher — G-3 est une ALARME APRÈS COUP, écrit sans adoucissement en en-tête, dans l'étape, dans l'ADR-072 et au SUMMARY ; le précédent `892f89a` est nommé |
| T-41-73 | Spoofing | échec d'authentification lu comme « aucune PR associée » | high | mitigate | trois états distincts (`ASSOCIEE` / `VIDE` / `INDETERMINE`), `INDETERMINE` → rc 2 jamais rc 1 ; `GH_TOKEN` posé sur l'étape réelle ; mutant MUT-3 ; précédent v2.39.0 du 2026-07-26 nommé |
| T-41-74 | Denial of service | faux rouge sur un merge régulier entré par rebase | high | mitigate | seconde lecture sur `merge_commit_sha` des PR closes ; fixture « merge par rebase » ; mutant MUT-2 qui prouve le rouge en l'absence de cascade ; PR #71 et #72 citées comme mesure |
| T-41-75 | Denial of service | faux rouge sur la création d'une ref ou un premier push | medium | mitigate | `--before` entièrement nul → rc 3 `CREATION-DE-REF` ; fixtures à 40 et 7 zéros ; mutant MUT-4 |
| T-41-76 | Information disclosure | étape verte par vacuité, ou preuve impossible depuis une PR | high | mitigate | deux étapes distinctes — fixture SANS condition (tourne sur tout run) et mesure réelle conditionnée `main` ; ligne `decouverte:` à cinq champs ; rc 0 impossible sans `pr_associee` ; mutant MUT-5 |
| T-41-77 | Tampering | le workflow déclenché par un push de tag ferait rougir chaque release | medium | mitigate | vérification machine que `on.push` ne porte aucune entrée `tags:`, avec la ligne recopiée au SUMMARY (PROT-02) |
</threat_model>

<verification>
`bash -n` et rc 64 ; trois fixtures de contrat vertes (1, 0, 3) hors réseau ; suite verte, cinq
mutants tués, aucun appel réseau dans le fichier de suite ; étape de fixture à quatre bascules,
bilan à 0 écart ; ordre des étapes, absence de `tags:` et présence de `GH_TOKEN` vérifiés par
machine ; rejeu `gates` (≥ 13 étapes) et `tests` (≥ 82 suites, 0 échec) verts ; `CHANGELOG.md`
§ Non releasé ; `VERSION` racine intacte ; recensement et contrôle de trace à rc 0.
</verification>

<success_criteria>
Un commit arrivé sur `main` sans PR associée est signalé, daté et lisible dans la CI — après coup,
jamais empêché — et aucune méthode de merge régulière de ce dépôt ne produit de fausse alarme.
</success_criteria>

## Artifacts this phase produces

- `scripts/check-push-sans-pr.sh` — verdicts `PR-ASSOCIEE`, `PUSH-SANS-PR`, `NON-VERIFIABLE`,
  `CREATION-DE-REF` ; `voie=commits-pulls|merge-commit-sha` ; codes 0 / 1 / 2 / 3 / 64 ; options
  `--repo`, `--sha`, `--before`, `--pulls-file`, `--closed-pulls-file`.
- `scripts/tests/test-check-push-sans-pr.sh` — MUT-1 à MUT-5, hors réseau.
- Deux étapes `gates` : `check-push-sans-pr (G-3 — preuve par fixture, sans condition)` et
  `check-push-sans-pr (G-3, PROT-05 — mesure réelle sur un push vers main)`.

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-17-SUMMARY.md` when done
</output>

---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 05
lot: "L05 — recensement versionné des consommateurs + lint rejoué en CI (D-03)"
type: execute
wave: 4
depends_on: ["41.1-01", "41.1-02", "41.1-03", "41.1-04", "41.1-06"]
autonomous: true
requirements: [WSAW-02, WSAW-04]
files_modified:
  - plugin/conductor/references/workstream-planning-consumers.md
  - plugin/conductor/scripts/check-planning-consumers-registered.sh
  - plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh
  - .github/workflows/ci.yml

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `plugin/conductor/scripts/check-planning-consumers-registered.sh`, sa suite de tests, ou `.github/workflows/ci.yml` porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh`) — le fichier de recensement `workstream-planning-consumers.md` lui-même n'est pas un gate/une suite/un hook/ci.yml, il ne requiert pas ce trailer à lui seul"
    - "`workstream-planning-consumers.md` (nouveau, `plugin/conductor/references/`) recense, une ligne par consommateur, colonnes : chemin, catégorie (a1 balaie tous / a2 résout l'actif / b câblé en dur / c suppose la racine), statut (`via-primitive` ou `exempté` + motif). Le recensement est un SUR-ENSEMBLE de ce que le lint MESURE : son univers se DÉRIVE en rejouant le balayage du lint sur le dépôt, jamais d'un nombre recopié. Les lignes déjà tranchées par D-04 sont reprises VERBATIM ; celles qu'AUCUNE décision n'avait mesurées sont classées ici pour la première fois (ce n'est pas un reclassement : reclasser, c'est changer le statut d'un consommateur DÉJÀ classé, et ce plan n'en change aucun)"
    - "CORRECTION C-02 (juge frais) : le seed de D-04 portait 14 chemins (+ le lint = 15) quand le matcher de D-03 en MESURE 18 sur le dépôt (197 `.sh` suivis → 109 hors `*/tests/*` → 18 consommateurs, mesuré en `awk` sur `git ls-files` le 2026-09-23). Sept chemins mesurés manquaient au seed : `plugin/conductor/scripts/check-instruction-budget.sh`, `plugin/conductor/scripts/guard-driver-lock.sh`, `plugin/dev-orchestrator/scripts/check-capability-activation.sh`, `plugin/planning-core/scripts/guard-planning-updated.sh`, `plugin/planning-core/scripts/workstream-policy.sh`, et les deux outils de phase `.planning/workstreams/fiabilite/phases/VFDO-40.1-*/tools/check-no-live-250.sh` et `.planning/workstreams/fiabilite/phases/VFDO-41-*/tools/check-aucune-fermeture.sh`. Sans eux, le lint rendait rc=1 sur le dépôt réel à sa PREMIÈRE invocation, contre son propre must_have « dépôt réel → rc=0 ». Le chiffre figé « 19 » de D-03 n'est dérivable d'aucune des deux listes : il est REMPLACÉ partout par un compte dérivé de la mesure"
    - "`check-planning-consumers-registered.sh` balaie TOUT fichier `.sh` du dépôt (git-tracké, `git ls-files`, HORS `*/tests/*`) référençant un chemin littéral `.planning/workstreams`, `STATE.md`, `ROADMAP.md` ou `REQUIREMENTS.md` sous `.planning/` ; tout chemin de script trouvé qui n'apparaît PAS dans `workstream-planning-consumers.md` fait échouer le lint (rc=1)"
    - "CORRECTION C2-2 (juge frais tour 4) — le second balayage `ci.yml` du lint EXEMPTE les régions bornées par la paire `# vf-allow-unregistered-planning-path:begin` / `:end`, et ce plan POSE ces marqueurs sur les deux régions de fixture jetable de `ci.yml` (`workstreams/dev` du BLOC 1, `workstreams/alpha` de l'étape check-divergence). Sans cela le lint rougit sur un nom de compartiment LÉGITIME — bascule mesurée le 2026-09-23 avec un compartiment `dev` réel sur le disque : `n_hits=8` dont 6 sur des lignes de fixture, rc=1, soit l'INVERSE de WSAW-04 que ce lint sert. La granularité est la RÉGION, JAMAIS l'étape : le vrai câblage en dur de `ci.yml:844` vit dans la MÊME étape que la fixture `dev`, une exemption au step aurait désarmé le lint sur l'un des deux angles morts d'origine. Deux gardes indépendantes bornent l'échappatoire : équilibre `begin`/`end` (fail-closed rc=2 sur un marqueur non refermé — mesuré : sans elle, le vrai câblage disparaît du verdict, `n_hits` 2 → 1) et plafond de lignes exemptées (< 25 % du fichier)"
    - "Balayage par `awk` UNIQUEMENT — jamais `grep` piped (motif `check-machine-paths.sh` lignes ~41-43, proxifié et menteur sur ce poste) ; comptage de l'univers `git ls-files` AVANT tout verdict, rc=2 NON VÉRIFIABLE si l'univers est vide (jamais un vert à vide, motif `check-machine-paths.sh` lignes ~106-120)"
    - "Codes de sortie fermés, aucun implicite : 0 = tout chemin de script référençant un artefact de planning est recensé · 1 = au moins un chemin non recensé (liste sur stderr) · 2 = NON VÉRIFIABLE (hors dépôt git, univers vide) · 64 = usage. Pas de code 3 — un lint dont le balayage rend zéro référence trouvée est un verdict 0 valide (rien à recenser n'est pas un SILENCE, c'est une conformité vide, distinction tranchée ici par le plan faute d'être spécifiée par D-03)"
    - "Étape CI dédiée dans le job `gates` qui rejoue le lint sur le dépôt réel ET prouve sa discriminance par une bascule isolée sur fixture jetable (un script minimal référençant `.planning/workstreams` HORS du recensement → attendu rc=1), patron `check-instruction-budget`/`check-baseline-arbitrage`"
    - "Le recensement inclut EXPLICITEMENT les deux scripts hors périmètre (`detect-planning-debt.sh`, `planning-task-context.sh`, motif « altitude lab, hors compartiment workstream d'un dépôt dev ») ET les huit scripts a2 exemptés (`check-workstream-pointer.sh`, `check-dev-bootstrap.sh`, `check-mission-exit.sh`, `check-requirements-survival.sh`, `restore-requirements-ledger.sh`, `requirements-survival-detect.sh`, `planning-context.sh`, `check-state-integrity.sh`) — un chemin EXEMPTÉ compte comme recensé pour le lint (il apparaît dans le fichier, donc n'échoue pas le balayage), même s'il ne consomme jamais `vf_ws_enumerate`"
    - "CORRECTION C1-3 (juge frais tour 4) — le statut des trois lignes du seed D-04 face au lint DIFFÈRE avant et après 41.1-03/04, et le motif inscrit au recensement doit dire le VRAI de l'état LIVRÉ, pas de l'état de départ. REMESURÉ fichier par fichier le 2026-09-23 sur ce worktree, motif 1 du lint (littéral `.planning/workstreams`) : AUJOURD'HUI les TROIS rendent 0 occurrence, donc aucun n'est détecté. APRÈS 41.1-03/04, DEUX le sont : `41.1-03-PLAN.md` prescrit dans `detect-gsd-engine.sh` un COMMENTAIRE portant le littéral `.planning/workstreams` (bloc « MESURE qui motive », la ligne « un `.planning/workstreams` détourné en lien symbolique »), et `41.1-04-PLAN.md` fait de même dans `discover-unintegrated-docs.sh` — le balayage `.sh` n'exclut PAS les lignes de commentaire (seul le second balayage `ci.yml` le fait), donc ces deux lignes MATCHENT. SEUL `check-planning-state.sh` reste non détecté : ses littéraux sont sur des lignes distinctes, il compose ses chemins à l'exécution (`$PLANNING_DIR/STATE.md`). Aucun rouge dans les deux cas (les trois lignes RESTENT au recensement, qui est un sur-ensemble, jamais une liste noire) — mais un artefact de gouvernance VERSIONNÉ ne doit pas affirmer « non détecté par le lint » sur deux fichiers que le lint détecte. Le motif est donc DIFFÉRENCIÉ par ligne, et une ligne recensée sans hit ne compte jamais comme une preuve de détection"
    - "AUCUN nombre de consommateurs n'est figé dans un critère d'acceptation, un artefact ou un must_have : le lint doit rendre rc=0 sur le dépôt réel, et c'est CETTE égalité d'ensembles qui est la garde — jamais « le recensement porte N lignes ». Le compte de référence du 2026-09-23 (18 mesurés + 3 documentés-non-détectés + le lint lui-même) est une MESURE datée, reproductible par la commande consignée dans l'en-tête du recensement, pas un contrat"
    - "QUAL-01 : un mutant qui retire une ligne du recensement (un consommateur réel devient non-recensé) fait rendre le lint à 1 sur le dépôt réel — preuve que le lint DÉTECTE réellement un oubli, pas seulement qu'il passe par construction"
    - "CORRECTION post plan-checker (FLAG-5) : le lint scanne UNIQUEMENT les `.sh` — `.github/workflows/ci.yml` (précisément le fichier où vivait l'angle mort d'origine du ROADMAP) échappe donc au balayage anti-oubli. C'est PERMANENT (rejoué à CHAQUE invocation du lint en CI), jamais un one-off : le lint asserte en plus, sur `.github/workflows/ci.yml`, l'ABSENCE de tout littéral `workstreams/<nom>` pour CHAQUE `<nom>` réellement énuméré par `vf_ws_enumerate .planning` sur le dépôt réel — les noms de compartiments viennent du DISQUE, jamais codés en dur dans le lint lui-même (même discipline WSAW-01 appliquée récursivement à ce lint). Un `<nom>` littéral dans `ci.yml` hors d'une ligne de commentaire (`#`) signale un câblage en dur qui a recontourné le fan-out — rc=1, motif distinct du rapport `.sh`"
  artifacts:
    - path: "plugin/conductor/references/workstream-planning-consumers.md"
      provides: "recensement — sur-ensemble de l'univers MESURÉ par le lint (18 consommateurs au 2026-09-23, plus 3 documentés-non-détectés et le lint lui-même), catégories a1/a2/b/c, statut via-primitive/exempté + motif"
    - path: "plugin/conductor/scripts/check-planning-consumers-registered.sh"
      provides: "lint anti-oubli, rejoué en CI, discriminant par mutation"
      contains: "vf-allow-unregistered-planning-path"
    - path: "plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh"
      provides: "quatre issues + mutant de retrait de ligne"
  key_links:
    - "Analogue exact : `scripts/check-machine-paths.sh` (root, hors module) — même rôle (lint d'hygiène du dépôt), même flux (balayage `git ls-files` + `awk`, échappatoire par marqueur, assertion d'univers non vide AVANT verdict). Ce nouveau lint vit dans `plugin/conductor/scripts/` (DISTRIBUÉ aux labs, contrairement à `check-machine-paths.sh` qui reste root-only) — sa suite est donc NÉCESSAIRE, contrairement à celle absente pour `check-machine-paths.sh`"
    - "D-03 : le recensement ET le lint sont EXIGÉS CONJOINTEMENT, l'un sans l'autre ne suffit pas — ne pas livrer l'un sans l'autre dans ce plan"
    - "`vf_ws_enumerate` (plan 41.1-01) est CONSOMMÉE par le second balayage de ce lint : `41.1-01` figure au `depends_on` (ajouté par la correction C-03 — il manquait) et le sourcing de `workstream-policy.sh` par recherche à DEUX candidats, patron `check-divergence.sh` lignes ~94-102, est prescrit explicitement dans l'`<action>` de la Task 1. Sans lui : `command not found`, univers de noms vide, rc=2, et le volet anti-câblage-en-dur de `ci.yml` ne tourne jamais"
---

<objective>
Poser le mécanisme anti-oubli D-03, en deux pièces qui se complètent : un recensement versionné et
lisible (table markdown, une ligne par consommateur connu de chemins de planning) et un lint qui
rejoue ce recensement en CI, échouant sur tout script qui référence un chemin de planning sans
figurer dans la table. C'est ce qui rend WSAW-04 vrai pour un consommateur NEUF oublié — le plan
41.1-02/03/04 ferme le cas d'un compartiment neuf sur le disque, ce plan ferme le cas symétrique.

Purpose: qu'aucun script futur ne puisse recoder en dur `.planning/workstreams`/`STATE.md` sans
que la CI le voie.
Output: table de gouvernance + lint distribué, tous deux prouvés par mutation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `scripts/check-machine-paths.sh` en entier (analogue exact, à recopier pour le flux de balayage,
  l'échappatoire, l'assertion d'univers non vide)
- `plugin/conductor/references/contracts.md` en entier (style de table + blockquote de framing)
- `.github/workflows/ci.yml` lignes ~385-565 (`check-instruction-budget`, patron de bascule de
  fixture isolée dans une étape CI) et ~862-929 (`check-divergence.sh en CI`, patron de mutation
  en place)
</context>

<discipline>
- `diff`/`ls` proxifiés — `rtk proxy` ; comparaisons `cmp`/`comm`/`cksum`.
- **`awk`, jamais `grep`** pour tout balayage du dépôt — motif chargé de `check-machine-paths.sh`.
- Fixtures `mktemp -d` + `git init -q`, jamais le dépôt réel, pour les cas de mutation du lint.
- Le recensement TRANSCRIT la classification D-04 déjà tranchée pour les consommateurs qu'elle
  couvre — ce plan ne RECLASSE rien (il ne change le statut d'aucun consommateur déjà classé). Il
  CLASSE en revanche, pour la première fois, les consommateurs que la mesure fait apparaître et
  qu'aucune décision n'avait vus (correction C-02) : classer un consommateur que personne n'avait
  mesuré n'est pas reclasser un consommateur déjà classé.
- **L'univers du recensement se RE-MESURE, il ne se recopie pas.** Avant d'écrire une seule ligne,
  rejouer le balayage du lint sur le dépôt (`awk` sur `git ls-files -- '*.sh'` hors `*/tests/*`,
  JAMAIS un `grep` piped) et comparer les ENSEMBLES par `comm -23`/`-13`, jamais les nombres.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : recensement des consommateurs MESURÉS + lint `check-planning-consumers-registered.sh`</name>
  <files>plugin/conductor/references/workstream-planning-consumers.md, plugin/conductor/scripts/check-planning-consumers-registered.sh</files>
  <behavior>
    Lint :
    - dépôt réel (`.` ) → rc=0 : tout script référençant un chemin de planning est recensé (le
      recensement est complet par construction, écrit par ce même plan).
    - fixture avec un script référençant `.planning/workstreams` hors recensement → rc=1, chemin
      du script listé sur stderr.
    - fixture hors dépôt git, ou `git ls-files` vide → rc=2.
    - `--path` inconnu → 64.
  </behavior>
  <action>
**Recensement** — `plugin/conductor/references/workstream-planning-consumers.md`, sur le patron de
`contracts.md` (blockquote de framing, table markdown). Colonnes : `Chemin | Catégorie | Statut`.
**AVANT D'ÉCRIRE UNE LIGNE — re-mesurer l'univers** (correction C-02, juge frais). Le recensement
doit couvrir ce que le lint MESURE, pas ce qu'une décision a listé. Rejouer le matcher de D-03 en
`awk` (jamais un `grep` piped, il tronque sur ce poste) et consigner la commande dans l'en-tête du
fichier de recensement pour qu'elle soit reproductible :
```bash
git ls-files -- '*.sh' | LC_ALL=C awk '!/\/tests\//' | LC_ALL=C sort > /tmp/univ.txt
LC_ALL=C awk '
  { if (index($0, ".planning/workstreams") > 0) { h[FILENAME]=1; next }
    p = index($0, ".planning/")
    if (p > 0) { r = substr($0, p)
      if (index(r,"STATE.md") || index(r,"ROADMAP.md") || index(r,"REQUIREMENTS.md")) h[FILENAME]=1 } }
  END { for (f in h) print f }
' $(LC_ALL=C awk '{printf "%s ", $0}' /tmp/univ.txt) | LC_ALL=C sort > /tmp/mesure.txt
# puis comparer les ENSEMBLES au recensement écrit, par `comm -23` / `comm -13` — jamais les nombres
```
MESURE DE RÉFÉRENCE du 2026-09-23 (à re-jouer, pas à recopier) : 197 `.sh` suivis → 109 hors
`*/tests/*` → **18 consommateurs détectés**.

Lignes à écrire — d'abord la classification D-04 reprise VERBATIM :
- `check-divergence.sh` → a1, `via-primitive`
- `check-state-integrity.sh` → a2, `exempté` (motif : résout le compartiment ACTIF, correct pour
  un outil consommé par un agent en train de travailler ; son APPELANT CI devient a1 par fan-out —
  voir `ci.yml:353`/`ci.yml:844`, plan 41.1-06)
- `check-planning-state.sh`, `detect-gsd-engine.sh` → c, `via-primitive` (plan 41.1-03)
- `discover-unintegrated-docs.sh` → c, `via-primitive` (plan 41.1-04)
- `detect-planning-debt.sh`, `planning-task-context.sh` → hors périmètre, motif « altitude lab,
  hors compartiment workstream d'un dépôt dev »
- huit a2 exemptés : `check-workstream-pointer.sh`, `check-dev-bootstrap.sh`,
  `check-mission-exit.sh`, `check-requirements-survival.sh`, `restore-requirements-ledger.sh`,
  `requirements-survival-detect.sh`, `planning-context.sh`, motif « résout le compartiment ACTIF,
  jamais un gate CI multi-compartiments »
- `check-planning-consumers-registered.sh` (ce plan, lint lui-même) → outillage de gouvernance,
  `via-primitive` (il référence les mêmes chemins littéraux qu'il lint — s'auto-recenser, ou
  s'exempter par son propre marqueur d'échappatoire, à trancher à l'écriture : le marqueur est
  préférable, c'est le motif du HATCH ci-dessous)
- `workstream-planning-consumers.md` lui-même et sa suite → non applicables (pas des scripts)

**Puis les SEPT consommateurs que la mesure fait apparaître et qu'aucune décision n'avait vus**
(correction C-02 — les classer ici est une PREMIÈRE classification, pas un reclassement). Motifs
établis en relisant la ligne qui matche dans chaque fichier ; RE-VÉRIFIER chacune à l'écriture, le
numéro de ligne bouge :
- `plugin/conductor/scripts/check-instruction-budget.sh` (l. ~34) → **hors périmètre**, `exempté`,
  motif « le littéral n'apparaît qu'en COMMENTAIRE (renvoi documentaire à `.planning/REQUIREMENTS.md`)
  — aucune résolution de chemin à l'exécution »
- `plugin/conductor/scripts/guard-driver-lock.sh` (l. ~37, ~87) → **hors périmètre**, `exempté`,
  même motif (deux commentaires d'exemple, `sed -i .planning/STATE.md`)
- `plugin/dev-orchestrator/scripts/check-capability-activation.sh` (l. ~90) → **hors périmètre**,
  `exempté`, même motif (renvoi documentaire à `.planning/ROADMAP.md:2064`)
- `plugin/planning-core/scripts/guard-planning-updated.sh` (l. ~239, **ligne EXÉCUTABLE**, pas un
  commentaire) → c, `exempté`, motif « le littéral vit dans un MESSAGE utilisateur, pas dans une
  résolution de chemin ; ce message nomme déjà « le STATE.md du compartiment concerné », il est
  donc correct sous layout partitionné ». INFO reportée, NON tranchée par cette phase : le marqueur
  `.planning/.session-noop` du même message est posé À LA RACINE par conception — si c'est un
  défaut, il relève d'un autre mandat, ne pas le corriger ici
- `plugin/planning-core/scripts/workstream-policy.sh` (l. ~34, ~199) → a1, `via-primitive`, motif
  « PORTE `vf_ws_enumerate` elle-même (plan 41.1-01) ; ses littéraux sont des commentaires de
  doctrine sur la politique qu'elle implémente »
- `.planning/workstreams/fiabilite/phases/VFDO-40.1-*/tools/check-no-live-250.sh` → **hors
  périmètre**, `exempté`, motif « outillage de vérification d'une phase, gelé avec l'artefact de
  phase qu'il accompagne ; ses littéraux sont des motifs de recherche figés d'une mesure datée,
  jamais des chemins consommés en production »
- `.planning/workstreams/fiabilite/phases/VFDO-41-*/tools/check-aucune-fermeture.sh` → **hors
  périmètre**, `exempté`, même motif (pathspecs figés d'une recette de phase)

**Enfin, le traitement des trois lignes du seed — CORRECTION C1-3 (juge frais tour 4), le motif
DIFFÈRE d'une ligne à l'autre.** La rédaction précédente affirmait les trois « non détectées ni
aujourd'hui ni après 41.1-03/04 ». REMESURÉ fichier par fichier le 2026-09-23 sur ce worktree,
motif 1 du lint (littéral `.planning/workstreams`) :

| Script | avant la phase | après 41.1-03/04 | motif à inscrire au recensement |
|---|---|---|---|
| `check-planning-state.sh` | 0 occurrence | 0 occurrence | « recensé, NON DÉTECTÉ par le lint : littéraux sur des lignes distinctes, chemin composé à l'exécution via `$PLANNING_DIR` » |
| `detect-gsd-engine.sh` | 0 occurrence | **détecté** | « recensé ET détecté par le lint depuis 41.1-03 (le commentaire du bloc « MESURE qui motive » porte le littéral `.planning/workstreams`) » |
| `discover-unintegrated-docs.sh` | 0 occurrence | **détecté** | « recensé ET détecté par le lint depuis 41.1-04 (même motif que `detect-gsd-engine.sh`) » |

Le balayage `.sh` n'exclut PAS les lignes de commentaire — seul le second balayage `ci.yml` le
fait — donc les deux commentaires prescrits par 41.1-03 et 41.1-04 MATCHENT le premier motif du
lint. Aucun rouge dans aucun des deux états (les trois lignes RESTENT au recensement, qui est un
sur-ensemble, jamais une liste noire) ; ce qui est en jeu est la VÉRACITÉ d'un artefact de
gouvernance VERSIONNÉ, qui aurait affirmé « non détecté par le lint » sur deux fichiers que le lint
détecte. Catégorie D-04 inchangée pour les trois (c, `via-primitive`). Une ligne recensée sans hit
n'est pas morte, mais elle ne prouve aucune détection : ne jamais la compter comme telle.

**RE-VÉRIFIER À L'EXÉCUTION, ne pas recopier ce tableau.** 41.1-03 et 41.1-04 sont en vagues
antérieures : mesurer les trois fichiers RÉELS avec le premier motif du lint au moment d'écrire le
recensement, et inscrire le motif correspondant à l'état CONSTATÉ — un commentaire reformulé en
amont suffirait à faire mentir ce tableau.

**Aucun nombre de lignes n'est prescrit.** La garde est l'ÉGALITÉ D'ENSEMBLES (lint rc=0 sur le
dépôt réel), jamais « le recensement porte N lignes » — c'est précisément le chiffre figé « 19 »
de D-03, non dérivable ni du seed (14+1) ni de la mesure (18+1), qui a produit le défaut C-02.

**Lint** — `check-planning-consumers-registered.sh`, patron `check-machine-paths.sh` recopié
(en-tête « pourquoi », énumération fermée des codes de sortie, `ROOT` dérivé de
`$(dirname "$0")/../../..`, marqueur d'échappatoire `vf-allow-unregistered-planning-path`,
assertion d'univers non vide AVANT verdict, balayage `git ls-files -z` + `xargs -0 awk`).

**SOURCING DE `workstream-policy.sh` — OBLIGATOIRE ET EXPLICITE (correction C-03, juge frais).**
Le second balayage ci-dessous appelle `vf_ws_enumerate`. `check-machine-paths.sh`, dont ce lint
recopie le flux, ne source RIEN : sans prescription explicite, l'exécutant n'a aucune raison
d'ajouter le sourcing, la fonction est alors `command not found`, l'univers des noms de
compartiments est vide, le lint rend rc=2 — et le volet anti-câblage-en-dur de `ci.yml`, qui EST
l'angle mort d'origine (`ci.yml:353`, `ci.yml:844`), ne tournerait JAMAIS. Tous les autres
consommateurs de la primitive (plans 41.1-02, 03, 04, 06) déclarent leur sourcing ; celui-ci est
le seul à l'avoir omis. Reprendre le patron de recherche à DEUX candidats de
`check-divergence.sh` (lignes ~94-102, à relire avant d'écrire), avec le chemin relatif recalculé
depuis `plugin/conductor/scripts/` :
```bash
WS_POLICY=""
for _cand in "$(dirname "$0")/workstream-policy.sh" \
             "$(dirname "$0")/../../planning-core/scripts/workstream-policy.sh"; do
  [ -r "$_cand" ] && { WS_POLICY="$_cand"; break; }
done
[ -n "$WS_POLICY" ] \
  || { echo "[check-planning-consumers-registered] workstream-policy.sh introuvable — second balayage ci.yml non exécutable, NON VÉRIFIABLE" >&2; exit 2; }
# shellcheck source=/dev/null
. "$WS_POLICY"
```
Fail-closed (rc=2), jamais une dégradation silencieuse : un lint qui n'a pas pu charger sa
primitive n'a pas regardé, il ne se replie pas sur « rien trouvé ». Ce sourcing est la raison pour
laquelle `41.1-01` figure au `depends_on` de ce plan — il y a été AJOUTÉ par la correction C-03,
il manquait alors que 01 est le producteur de la primitive. La vague reste 4 : `41.1-01` est en
vague 1, donc strictement antérieure.

Motif recherché par `awk` : littéraux `\.planning/workstreams`, ou `STATE\.md`, `ROADMAP\.md`,
`REQUIREMENTS\.md` précédés du littéral `\.planning/` sur la même ligne — implémenté en `awk` pur
(pas de `grep -E`), sur les fichiers `*.sh` de `git ls-files` EXCLUANT tout chemin dont un segment
est `tests`. Pour chaque fichier scanné qui matche, extraire son chemin RELATIF À LA RACINE, et
vérifier par un second passage `awk` sur `workstream-planning-consumers.md` (colonne « Chemin »,
comparaison par basename OU chemin complet — les entrées de la table étant des basenames simples,
comparer par basename est suffisant et plus robuste à un futur déplacement de fichier — RISQUE
NOTÉ, non traité par ce plan (INFO, juge frais) : un futur homonyme de basename entre deux scripts
distincts masquerait un oubli réel ; acceptable aujourd'hui, 197 `.sh` suivis, 0 basename dupliqué
mesuré) que ce chemin y figure. Chemin non trouvé → accumulé dans un rapport, rc final 1.

Écrire l'en-tête avec la même discipline que `check-machine-paths.sh` : pourquoi ce gate existe
(D-03, WSAW-04), ce qu'il n'est pas (il ne juge pas SI l'adoption de `vf_ws_enumerate` est
correcte, seulement si le consommateur est SU), l'échappatoire nommée et son motif.

**Second balayage, PERMANENT, ciblé sur `ci.yml` (correction post plan-checker, FLAG-5).** Le
balayage `.sh` ci-dessus ne couvre jamais `.github/workflows/ci.yml` — précisément le fichier où
vivait l'angle mort d'origine (`ci.yml:353`, `ci.yml:844`). Ajouter, dans le MÊME script, une
seconde passe qui tourne à CHAQUE invocation (jamais un one-off de plan) :
```bash
# --- Second balayage : ci.yml ne doit jamais recoder un nom de compartiment en dur -----------
# Les noms viennent du DISQUE (vf_ws_enumerate .planning sur CE dépôt), jamais codés en dur ici —
# même discipline WSAW-01, appliquée récursivement à ce lint lui-même.
# CORRECTION post plan-checker (FLAG-5, insuffisance) : sans assertion d'univers non vide, une
# énumération qui rend zéro nom (workstreams/ absent, illisible, ou vf_ws_enumerate en échec) fait
# tourner la boucle zéro fois → aucun hit possible → verdict 0 SILENCIEUX, exactement le vert à
# vide que le balayage `.sh` (repris de check-machine-paths.sh) évite déjà par construction. Ce
# second balayage doit avoir la MÊME discipline : univers non vide asserté AVANT tout verdict.
# CORRECTION C-09 (juge frais), DEUX défauts distincts dans la version précédente de ce bloc :
#  (a) `vf_ws_enumerate .planning` et `CI_FILE=".github/workflows/ci.yml"` étaient relatifs au CWD,
#      jamais à `$ROOT` — alors que le balayage `.sh` respecte `--path`. Dans le cas de test
#      `script_oublie` (exécuté avec `--path "$T"`), le volet `.sh` jugeait la fixture pendant que
#      le volet `ci.yml` jugeait LE DÉPÔT RÉEL : le rc=1 attendu pouvait venir de l'un OU de
#      l'autre, et rien ne le distinguait. Les DEUX volets sont désormais ancrés sur `$ROOT`.
#  (b) `ws_names="$(… | while … done)" || ws_enum_rc=$?` capturait le rc du PIPELINE (le `while`,
#      toujours 0 sans `pipefail`), jamais celui de `vf_ws_enumerate` — le message d'échec aurait
#      affiché `rc=0`. La primitive est appelée HORS pipeline, redirigée vers un fichier.
CI_FILE="$ROOT/.github/workflows/ci.yml"
CI_REL=".github/workflows/ci.yml"   # pour un rapport lisible, relatif à $ROOT
if [ -f "$CI_FILE" ]; then
  ws_enum_rc=0
  vf_ws_enumerate "$ROOT/.planning" > "$TMPD/ws_dirs" || ws_enum_rc=$?
  LC_ALL=C awk 'NF' "$TMPD/ws_dirs" | while IFS= read -r d; do basename "$d"; done > "$TMPD/ws_names"
  n_ws_names=$(LC_ALL=C awk 'NF' "$TMPD/ws_names" | LC_ALL=C awk 'END{print NR+0}')
  if [ "$n_ws_names" -eq 0 ]; then
    echo "[check-planning-consumers-registered] second balayage ci.yml : vf_ws_enumerate $ROOT/.planning n'a rendu AUCUN nom (rc=$ws_enum_rc) — univers vide, second balayage NON VÉRIFIABLE, jamais un 0 silencieux" >&2
    echo "NON-VERIFIABLE:univers-vide" >> "$TMPD/ci_hardcode_hits"
    rc_final=2
  else
    # CORRECTION C2-2 (juge frais tour 4) — RÉGIONS DE FIXTURE JETABLE, sinon ce lint rougit sur
    # un nom de compartiment LÉGITIME. La seule exclusion d'origine était la ligne de commentaire
    # (`^[[:space:]]*#`), et l'échappatoire `vf-allow-unregistered-planning-path` ne couvrait que
    # le balayage `.sh`. Or `ci.yml` porte DÉJÀ, hors commentaire, des chemins de FIXTURE JETABLE :
    # `workstreams/dev` (6 lignes de construction/assertion de la fixture partitionnée) et
    # `workstreams/alpha` (6 lignes de la fixture check-divergence) — et 41.1-06 en ajoute
    # (`sain`, `candidat`, `neuf`, `carve`, `vierge`, `beta`, `gamma`).
    # BASCULE JOUÉE (2026-09-23) : univers de noms = {dev, fiabilite, gouvernance} (un compartiment
    # `dev` LÉGITIME sur le disque — le nom canonique de la doctrine du dépôt, `workstreams.md` §1),
    # balayage d'origine → `n_hits=8`, dont 6 sur `dev` pointant des lignes
    # `mkdir -p "$FIX/.planning/workstreams/dev"` → rc=1. Le gate rougissait donc sur un état
    # légitime et plausible, et installait un ensemble implicite et non documenté de noms
    # INTERDITS — l'inverse exact de WSAW-04 (« ajouter un compartiment suffit, sans câblage
    # supplémentaire »), l'exigence que ce lint est précisément censé servir.
    # GRANULARITÉ : le marqueur est de portée RÉGION (paire `begin`/`end`), PAS de portée step.
    # MESURÉ : le vrai câblage en dur de `ci.yml:844` vit DANS LA MÊME étape que la fixture
    # `dev` (l'étape « Gates workstream-aware … (fixture jetable) + non-régression racine »,
    # `ci.yml:567`, qui porte aussi le bloc « 2b : conformité de la racine réelle ») — une
    # exemption au step aurait DÉSARMÉ le lint sur l'un des deux angles morts d'origine.
    ws_fixt_bal=0
    LC_ALL=C awk '
      index($0, "vf-allow-unregistered-planning-path:begin") > 0 { if (open) { bad++ } open=1; next }
      index($0, "vf-allow-unregistered-planning-path:end") > 0 { if (!open) { bad++ } open=0; next }
      END { if (open || bad) exit 1 }
    ' "$CI_FILE" || ws_fixt_bal=$?
    if [ "$ws_fixt_bal" -ne 0 ]; then
      # Un `begin` non refermé (ou imbriqué) exempte TOUT LE RESTE du fichier : l'échappatoire
      # avalerait le gate. MESURÉ sur un `ci.yml` dont un seul `end` est retiré : le vrai câblage
      # en dur DISPARAÎT du verdict (n_hits 2 -> 1). Fail-closed, jamais un vert par avalement.
      echo "[check-planning-consumers-registered] second balayage ci.yml : marqueurs de fixture jetable DÉSÉQUILIBRÉS (begin/end) — l'échappatoire exempterait une région non bornée, NON VÉRIFIABLE" >&2
      echo "NON-VERIFIABLE:marqueurs-desequilibres" >> "$TMPD/ci_hardcode_hits"
      rc_final=2
    else
      while IFS= read -r name; do
        [ -n "$name" ] || continue
        LC_ALL=C awk -v n="$name" -v f="$CI_REL" '
          index($0, "vf-allow-unregistered-planning-path:begin") > 0 { fixt=1; next }
          index($0, "vf-allow-unregistered-planning-path:end") > 0 { fixt=0; next }
          fixt { next }
          $0 ~ /^[[:space:]]*#/ { next }
          index($0, "workstreams/" n) > 0 { print f ":" NR ":" n }
        ' "$CI_FILE" >> "$TMPD/ci_hardcode_hits" || true
      done < "$TMPD/ws_names"
    fi
  fi
fi
if [ -s "$TMPD/ci_hardcode_hits" ] && [ "${rc_final:-0}" -ne 2 ]; then
  echo "[check-planning-consumers-registered] ci.yml recode un nom de compartiment en dur (hors commentaire) :" >&2
  cat "$TMPD/ci_hardcode_hits" >&2
  rc_final=1
fi
```
(`TMPD` = un `mktemp -d` posé en tête du script, nettoyé par `trap`, patron déjà en usage dans le
parc. Le code `rc_final=2` distinct du `1` d'un vrai câblage en dur — un univers vide n'est pas la
même chose qu'un défaut trouvé, même posture NON VÉRIFIABLE que le reste du parc.) Ce second
balayage vient S'AJOUTER au verdict du balayage `.sh` — un rc=1 OU rc=2 de l'un OU l'autre
suffit à faire échouer le lint dans son ensemble ; documenter les deux volets distinctement dans le
rapport stderr pour que la cause soit lisible sans ambiguïté.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/conductor/scripts/check-planning-consumers-registered.sh && echo "syntaxe OK"'</automated>
    <fails_when>`bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash plugin/conductor/scripts/check-planning-consumers-registered.sh 2>&1) || rc=$?; echo "depot_reel rc=$rc"; printf "%s\n" "$out"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>`depot_reel rc=` différent de 0 — le recensement écrit par ce plan doit couvrir tous les scripts réels du dépôt qui référencent un chemin de planning, sinon le lint échoue sur son propre dépôt d'écriture</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q
mkdir -p "$T/plugin/x/scripts"
printf "%s\n" "#!/usr/bin/env bash" "echo .planning/workstreams" > "$T/plugin/x/scripts/oublie.sh"
git -C "$T" add -A >/dev/null 2>&1
git -C "$T" -c user.email=t@t -c user.name=t commit -q -m x >/dev/null 2>&1
rc=0; out=$(bash plugin/conductor/scripts/check-planning-consumers-registered.sh --path "$T" 2>&1) || rc=$?
echo "script_oublie rc=$rc"; printf "%s\n" "$out"
[ "$rc" -eq 1 ]
'</automated>
    <fails_when>`script_oublie rc=` différent de 1 — un script neuf référençant `.planning/workstreams` sans figurer au recensement DOIT faire échouer le lint. DÉSAMBIGUÏSATION (correction C-09) : les deux volets étant désormais ancrés sur `$ROOT`, la fixture `--path "$T"` n'a pas de `$T/.github/workflows/ci.yml`, donc le second volet est sauté par sa garde `[ -f "$CI_FILE" ]` et le rc=1 n'est imputable QU'AU volet `.sh`. Avant cette correction, le volet `ci.yml` jugeait le DÉPÔT RÉEL pendant que le volet `.sh` jugeait la fixture : le rc=1 pouvait venir de l'un ou de l'autre sans que rien ne le distingue</fails_when>
    <automated>bash --noprofile --norc -e -c '
# CORRECTION C-08 (juge frais) : la fixture précédente faisait `rsync -a --exclude=.git . "$T/repo"`
# puis attendait rc=1. MESURÉ : la copie ne contenait pas de `.git`, donc
# `git -C "$T/repo" rev-parse --is-inside-work-tree` rendait « fatal: not a git repository » et
# `git ls-files` 0 fichier — or le contrat du lint écrit par CE MÊME plan dit « 2 = NON VÉRIFIABLE
# (hors dépôt git, univers vide) », avec assertion d univers non vide AVANT verdict. Attendu 1,
# obtenu 2, structurellement : la seule preuve exécutable que le second balayage ci.yml est
# discriminant ne tournait pas. Le raccourci tentant (relâcher en `rc != 0`) la rendrait VERTE sur
# l univers vide, soit exactement le vert à vide que ce plan ferme — ne pas le prendre.
# La fixture est donc un VRAI dépôt git : copie sans `.git`, puis `git init` + commit. Recette
# MESURÉE sur ce dépôt : univers 1588 fichiers suivis dont 197 `.sh`, ci.yml présent, ~0,5 s.
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
if command -v rsync >/dev/null 2>&1; then rsync -a --exclude=.git --exclude=.claude/worktrees . "$T/repo"; else cp -R . "$T/repo"; rm -rf "$T/repo/.git" "$T/repo/.claude/worktrees"; fi
git -C "$T/repo" init -q
git -C "$T/repo" add -A >/dev/null 2>&1
git -C "$T/repo" -c user.email=t@t -c user.name=t commit -q -m fixture >/dev/null 2>&1
u=$(git -C "$T/repo" ls-files | awk "END{print NR+0}")
echo "fixture_univers=$u"
[ "$u" -gt 0 ] || { echo "fixture INVALIDE : univers vide, la bascule ne prouverait rien"; exit 1; }
# Temoin AVANT mutation : le depot copie, non mute, doit rendre 0 — sinon le rc=1 daprès mutation
# ne serait pas imputable a la mutation.
rc0=0; out0=$(bash plugin/conductor/scripts/check-planning-consumers-registered.sh --path "$T/repo" 2>&1) || rc0=$?
echo "ci_yml_temoin_avant_mutation rc=$rc0"
printf "\n      - name: mutant-flag5\n        run: echo workstreams/fiabilite/STATE.md\n" >> "$T/repo/.github/workflows/ci.yml"
git -C "$T/repo" -c user.email=t@t -c user.name=t commit -q -am mutant >/dev/null 2>&1
rc=0; out=$(bash plugin/conductor/scripts/check-planning-consumers-registered.sh --path "$T/repo" 2>&1) || rc=$?
echo "ci_yml_hardcode_mutant rc=$rc"; printf "%s\n" "$out"
[ "$rc0" -eq 0 ] && [ "$rc" -eq 1 ]
'</automated>
    <fails_when>`fixture_univers=` égal à 0 (la fixture n'est pas un dépôt git peuplé — la bascule ne prouverait rien) ; ou `ci_yml_temoin_avant_mutation rc=` différent de 0 (la copie non mutée devrait être propre : sans ce témoin, le rc=1 d'après mutation n'est pas imputable à la mutation) ; ou `ci_yml_hardcode_mutant rc=` différent de 1 — FLAG-5 : un nom de compartiment recodé en dur dans `ci.yml` (littéral `workstreams/fiabilite`, hors commentaire) DOIT faire échouer le lint, c'est le second balayage permanent qui ferme l'angle mort d'origine. La bascule doit rester ROUGE avec le câblage en dur et VERTE sans — un `rc != 0` relâché serait vert sur un univers vide, interdit</fails_when>
  </verify>
  <acceptance_criteria>
    - le recensement couvre l'ENSEMBLE MESURÉ par le lint (comparaison par `comm -23`/`-13` contre le rejeu du balayage, jamais un nombre de lignes attendu), colonnes chemin/catégorie/statut/motif ; la garde réelle est le rc=0 du lint sur le dépôt réel (C-02)
    - le lint rend 0 sur le dépôt réel, 1 sur un script neuf non recensé, 2 hors dépôt git
    - balayage entièrement en `awk`, aucun `grep` piped
    - second balayage PERMANENT sur `ci.yml` : un nom de compartiment recodé en dur (littéral `workstreams/<nom>`, hors commentaire) fait rendre le lint à 1 (FLAG-5)
  </acceptance_criteria>
  <done>Le recensement existe et le lint sait détecter un consommateur oublié sur une fixture jetable.</done>
</task>

<task type="auto">
  <name>Task 2 : suite du lint + étape CI dédiée, rejeu gates/tests</name>
  <files>plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh, .github/workflows/ci.yml</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/conductor/scripts/tests/test-check-divergence.sh` lignes ~31-54 (`mk_git_root`,
      `run()`, modèle de fixture à suivre pour ce nouveau fichier de test — aucun analogue direct
      n'existe pour ce lint précis)
  </read_first>
  <action>
Écrire `test-check-planning-consumers-registered.sh` : quatre issues (0 conforme, 1 non recensé,
2 hors dépôt/univers vide, 64 usage), fixtures `mktemp -d`+`git init -q`. Cas d'échappatoire :
un script portant le marqueur `vf-allow-unregistered-planning-path` sur sa ligne référençant
`.planning/workstreams` ne fait PAS échouer le lint même non recensé.

**LIBELLÉS OBLIGATOIRES** (recherchés littéralement par la sonde du `<verify>`, correction C-10) —
chaque cas imprime son libellé sur la ligne de son verdict : `CPR-0` (dépôt conforme → rc=0),
`CPR-1` (script non recensé → rc=1), `CPR-2` (hors dépôt git / univers vide → rc=2), `CPR-64`
(usage), `HATCH` (échappatoire `vf-allow-unregistered-planning-path` respectée), `MUT-1` (mutant
de retrait de ligne TUÉ). Un cas non écrit n'a pas de libellé, donc la sonde rougit — c'est ce qui
rend les clauses du `fails_when` réellement implémentées au lieu de décrites.

MUTANT (D-03, QUAL-01) — retirer UNE ligne du VRAI `workstream-planning-consumers.md` dans une
COPIE temporaire (jamais le fichier réel), invoquer le lint avec `--path .` mais en substituant le
recensement lu par la copie mutée (via une variable d'environnement de surcharge à ajouter au
script si elle n'existe pas déjà — `VF_CONSUMERS_FILE`, motif `VF_GSD_SKILLS_DIR` déjà en usage
ailleurs dans le parc) : le lint doit alors rendre 1 sur le dépôt réel (le consommateur retiré
redevient « non recensé »), 0 avec le fichier intact. Documenter la surcharge dans l'en-tête du
script (Task 1, à amender si besoin) au même titre que les surcharges de fixture des autres gates.

**CI** — nouvelle étape dans le job `gates`, après l'étape « check-divergence.sh en CI » **telle
qu'étendue par 41.1-06** (fixture à trois compartiments dont un cassé — lire `ci.yml` après
exécution de 41.1-06, pas l'état d'avant cette phase) : une bascule isolée sur fixture jetable
(`mktemp -d` + `git init -q`, un script hors recensement → attendu rc=1), PUIS la mesure du dépôt
réel (attendu rc=0). Patron exact des étapes voisines (`fail`/`note`, bilan final, `exit 1` si
écart). **Séquencement (revue plan-checker) : ce plan dépend désormais de 41.1-06** — les deux
plans modifient `.github/workflows/ci.yml` dans des régions voisines du même job `gates` ; exécuter
41.1-06 d'abord évite un anchor invalidé ou une édition perdue entre deux workers du même wave.

**MARQUEURS DE FIXTURE JETABLE DANS `ci.yml` (correction C2-2, juge frais tour 4) — À POSER DANS
CE MÊME PLAN, sinon le lint du Task 1 rougit sur un nom de compartiment légitime.** Encadrer, par
une paire `# vf-allow-unregistered-planning-path:begin` / `# vf-allow-unregistered-planning-path:end`,
CHAQUE région de `ci.yml` qui CONSTRUIT ou INSPECTE une fixture jetable en nommant un compartiment.
Deux régions à l'état d'avant cette phase (à RE-LOCALISER après exécution de 41.1-06, qui les
étend — ne PAS reprendre ces numéros de ligne tels quels) :
1. la fixture `dev` du bloc « BLOC 1 — ASSERTION DE CAPACITÉ » de l'étape « Gates workstream-aware
   sur un arbre RÉELLEMENT partitionné (fixture jetable) + non-régression racine » — 6 lignes
   portant `workstreams/dev` hors commentaire (construction `mkdir -p "$FIX/…"`, `ROADMAP.md`,
   `STATE.md`, la boucle d'assertion de construction, et les deux branches du `case` sur la sortie) ;
2. la fixture `alpha` de l'étape « check-divergence.sh en CI » — 6 lignes portant
   `workstreams/alpha` hors commentaire.

**La région, JAMAIS l'étape.** MESURÉ le 2026-09-23 : le vrai câblage en dur de `ci.yml:844`
(`check-state-integrity.sh --file .planning/workstreams/fiabilite/STATE.md`, bloc « 2b :
conformité de la racine réelle ») vit DANS LA MÊME étape que la fixture `dev` — une exemption de
portée step aurait désarmé le lint sur l'un des DEUX angles morts qui justifient son existence
(`ci.yml:353`, `ci.yml:844`). Les marqueurs doivent donc borner les blocs de fixture au plus
serré, et ne jamais englober un bloc qui mesure la racine réelle.

BASCULES MESURÉES sur l'état d'avant cette phase, univers de noms = {dev, fiabilite, gouvernance}
(un compartiment `dev` légitime posé sur le disque) :
- balayage d'origine, sans marqueurs → `n_hits=8` (6 × `dev` sur des lignes de fixture + les 2
  vrais câblages `fiabilite`) → rc=1 : FAUX POSITIF sur un état légitime ;
- balayage corrigé, régions marquées → `n_hits=2`, EXACTEMENT les deux vrais câblages
  (`ci.yml:353`, `ci.yml:844`) : les 6 faux positifs éteints, le gate TOUJOURS armé ;
- contre-épreuve par mutation : un `check-divergence.sh --ws .planning/workstreams/gouvernance`
  injecté HORS région → `n_hits=3`, le câblage injecté est NOMMÉ ⇒ le lint n'a pas été désarmé ;
- garde d'équilibre : un seul `end` retiré → le vrai câblage DISPARAÎT du verdict (`n_hits` 2 → 1),
  et l'assertion de balance rend rc=1 ⇒ le lint sort 2 (NON VÉRIFIABLE) au lieu d'un vert avalé.

Rejouer `gates` et `tests` complets, consigner le bilan au SUMMARY.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
[ -f plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh ] || { echo "suite_absente=1"; exit 1; }
rc_suite=0; bash plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh > "$T/o" 2>&1 || rc_suite=$?
ko=$(awk "index(\$0, \"✗\") > 0 {c++} END{print c+0}" "$T/o")
manq=""
for lab in CPR-0 CPR-1 CPR-2 CPR-64 HATCH MUT-1; do
  awk -v L="$lab" "index(\$0, L) > 0 {f=1} END{exit !f}" "$T/o" || manq="$manq $lab"
done
echo "suite_rc=$rc_suite ko=$ko cas_manquants=[${manq# }]"
[ "$rc_suite" -eq 0 ] && [ "$ko" -eq 0 ] && [ -z "$manq" ]'</automated>
    <fails_when>`suite_absente=1` (le fichier de suite n'a pas été créé) ; ou `suite_rc=` différent de 0 ; ou `ko=` différent de 0 (une ligne `✗` dans la sortie) ; ou `cas_manquants=[…]` non vide. CORRECTION C-10 (juge frais, 3e passe) : la commande précédente était un `bash … test-check-planning-consumers-registered.sh` NU — ses clauses « ou une ligne `✗` » et « ou `MUT` absent de la sortie » n'étaient RECHERCHÉES PAR RIEN, alors que le mutant QUAL-01 (retrait d'une ligne du recensement) est la SEULE preuve machine que le lint DÉTECTE un oubli. Mesuré le 2026-09-23 : `ko=`, `cas_manquants` et `MUT-1` avaient 0 occurrence dans ce plan, et la forme nue rend rc=0 sur une suite qui sort 0 en portant une ligne `✗` et aucune ligne `MUT` (fixture exécutée : `rc=0`, `lignes_croix=1`, `lignes_MUT=0`) — le bloc restait donc VERT sans qu'aucun cas neuf ni le mutant ne soient écrits. Bascule remesurée sur la sonde de remplacement : état actuel (fichier absent) → `suite_absente=1`, rc=1 ; sortie portant les six libellés, 0 `✗`, rc=0 → rc=0 ; la même sortie plus une ligne `✗` → rc=1 ; la même sortie sans `MUT-1` → `cas_manquants=[MUT-1]`, rc=1 ; les six libellés mais suite rc=1 → `suite_rc=1`, rc=1</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^BILAN REJEU gates/" "$T/g"; echo "gates rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <automated>bash --noprofile --norc -e -c '
# CORRECTION C2-2 (juge frais tour 4) : ces trois mesures gardent lechappatoire de region contre
# ses deux modes de defaut — un marqueur desequilibre qui avale le gate, et un marqueur qui
# englobe un bloc mesurant la racine reelle. Sans elles, le second balayage peut devenir un vert
# a vide sans quaucune autre sonde ne le voie.
nb=$(awk "index(\$0, \"vf-allow-unregistered-planning-path:begin\") > 0 {c++} END{print c+0}" .github/workflows/ci.yml)
ne=$(awk "index(\$0, \"vf-allow-unregistered-planning-path:end\") > 0 {c++} END{print c+0}" .github/workflows/ci.yml)
bal=0
awk "
  index(\$0, \"vf-allow-unregistered-planning-path:begin\") > 0 { if (open) { bad++ } open=1; next }
  index(\$0, \"vf-allow-unregistered-planning-path:end\") > 0 { if (!open) { bad++ } open=0; next }
  END { if (open || bad) exit 1 }
" .github/workflows/ci.yml || bal=$?
# lignes EXEMPTEES : bornees, jamais une region ouverte qui court jusqua EOF
nx=$(awk "
  index(\$0, \"vf-allow-unregistered-planning-path:begin\") > 0 { fixt=1; next }
  index(\$0, \"vf-allow-unregistered-planning-path:end\") > 0 { fixt=0; next }
  fixt { c++ } END{print c+0}
" .github/workflows/ci.yml)
tot=$(awk "END{print NR+0}" .github/workflows/ci.yml)
echo "begin=$nb end=$ne balance_rc=$bal lignes_exemptees=$nx total=$tot"
[ "$nb" -ge 2 ] && [ "$nb" -eq "$ne" ] && [ "$bal" -eq 0 ] && [ "$nx" -gt 0 ] && [ "$nx" -lt $((tot / 4)) ]
'</automated>
    <fails_when>`begin=` inférieur à 2 (les deux régions de fixture ne sont pas bornées — le lint du Task 1 rougira sur un compartiment légitime nommé `dev` ou `alpha`) ; ou `begin=` différent de `end=` ; ou `balance_rc=` différent de 0 (marqueur non refermé ou imbriqué — l'échappatoire exempterait une région non bornée) ; ou `lignes_exemptees=` égal à 0 (les marqueurs sont posés mais n'encadrent rien) ; ou `lignes_exemptees=` supérieur ou égal au quart du fichier — un plafond GROSSIER mais qui rend impossible le mode de défaut réel : une paire de marqueurs qui finit par englober tout le job `gates`, y compris les blocs qui mesurent la racine réelle. MESURÉ le 2026-09-23 sur l'état d'avant cette phase, régions posées au plus serré : `begin=2 end=2 balance_rc=0 lignes_exemptees=90 total=1744`, soit 5,2 % du fichier, très en dessous du plafond de 25 % → rc=0 ; sur le `ci.yml` actuel (aucun marqueur) : `begin=0 end=0 lignes_exemptees=0` → rc=1 ; et sur le MÊME fichier marqué dont UN SEUL `end` est retiré : `begin=2 end=1 balance_rc=1 lignes_exemptees=318` → rc=1. Ce dernier cas justifie la garde d'équilibre À PART du plafond : 318 lignes exemptées restent SOUS le quart de 1744, donc le plafond seul ne l'aurait PAS attrapé, alors que le vrai câblage en dur avait déjà disparu du verdict (`n_hits` 2 → 1). Les deux clauses sont nécessaires, aucune n'est redondante</fails_when>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - suite verte, quatre issues couvertes, mutant de retrait de ligne tué
    - étape CI dédiée, bascule de fixture rouge puis dépôt réel vert
    - rejeu `gates` et `tests` verts
  </acceptance_criteria>
  <done>Le lint est prouvé par mutation ET câblé en CI, exactement comme D-03 l'exige des deux pièces conjointement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenu de script versionné → balayage du lint | le lint lit le contenu de tout fichier `.sh` suivi par git ; un fichier volumineux ou au contenu adverse ne doit jamais faire planter le balayage `awk` |
| recensement → verdict | le recensement est un fichier versionné, modifiable par la même PR que le script qu'il couvrirait — même limite de fond que les gardes G-1/G-2/G-3 (ADR-072) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-09 | Tampering | un consommateur neuf ajouté ET sa ligne de recensement ajoutée DANS LA MÊME PR, sans que ce soit réellement `via-primitive` | medium | accept | même limite de fond que G-1/G-2/G-3 (ADR-072) — le lint vérifie la PRÉSENCE au recensement, jamais la véracité du statut déclaré ; à écrire explicitement en en-tête |
| T-41.1-10 | Denial of service | vert à vide si `git ls-files` rend un univers vide (dépôt fraîchement cloné, sous-arbre) | high | mitigate | assertion d'univers non vide avant tout verdict, motif `check-machine-paths.sh`, rc=2 sur univers vide |
</threat_model>

<verification>
`bash -n` ; lint rc=0 sur le dépôt réel, rc=1 sur script neuf non recensé, rc=2 hors dépôt/univers
vide ; suite verte avec mutant de retrait de ligne tué ; étape CI dédiée verte ; rejeu `gates` et
`tests` verts.
</verification>

<success_criteria>
Le recensement et le lint existent CONJOINTEMENT (D-03), le lint est câblé en CI et prouvé
discriminant par mutation — un consommateur neuf qui oublierait `vf_ws_enumerate` est désormais vu.
</success_criteria>

## Artifacts this phase produces

- `plugin/conductor/references/workstream-planning-consumers.md` — sur-ensemble de l'univers mesuré par le lint (nombre de lignes dérivé de la mesure, jamais figé).
- `plugin/conductor/scripts/check-planning-consumers-registered.sh` — codes 0/1/2/64.
- `plugin/conductor/scripts/tests/test-check-planning-consumers-registered.sh` — 4 issues + mutant.
- Étape `gates` : lint des consommateurs de chemins de planning, bascule de fixture.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-05-SUMMARY.md` when done
</output>

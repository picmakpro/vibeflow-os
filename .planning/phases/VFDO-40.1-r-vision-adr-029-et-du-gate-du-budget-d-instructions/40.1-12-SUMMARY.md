---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 12
subsystem: modules
tags: [adr-029, kpi-analyst, audit-architecture, bump-patch]

requires: ["40.1-01", "40.1-02"]
provides:
  - "kpi-analyst v1.0.6 — AGENT.md déclare le plafond de densité à 300L (était 250L),
    remplacement de valeur seul, agent byte-identique hors la valeur"
  - "audit-architecture v1.0.3 — SKILL.md (body) et README.md déclarent ≤ 300 L pour les
    auditeurs générés (était ≤ 250 L), frontmatter du skill intact"
affects: ["40.1-04 (rejoue les suites conductor après ce plan, dépendance déclarée sur
  test-check-description-fidelity.sh)", "40.1-14 (clôture, rejoue bumps et invariants)"]

actuals:
  tokens: 35000
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Remplacement de valeur seul dans un AGENT.md/SKILL.md : commit de contenu séparé du commit
      de bump patch (VERSION + module.json + README + CHANGELOG), chacun vérifié indépendamment"

key-files:
  modified:
    - plugin/kpi-analyst/AGENT.md
    - plugin/kpi-analyst/VERSION
    - plugin/kpi-analyst/module.json
    - plugin/kpi-analyst/README.md
    - plugin/kpi-analyst/CHANGELOG.md
    - plugin/audit-architecture/SKILL.md
    - plugin/audit-architecture/README.md
    - plugin/audit-architecture/VERSION
    - plugin/audit-architecture/module.json
    - plugin/audit-architecture/CHANGELOG.md

key-decisions:
  - "D-01 appliqué tel quel (arbitrage Samuel, AskUserQuestion session principale, relais
    SendMessage, 2026-09-16) : 250 → 300 dans les deux modules, aucune autre ligne touchée."
  - "Deux commits par module (contenu, puis bump) plutôt qu'un seul : le contenu reste vérifiable
    seul (byte-diff minimal), le bump reste dérivable par check-module-bump.sh (VERSION+module.json
    +CHANGELOG dans le même commit)."

commits:
  - sha: b67172c
    subject: "chore(40.1-12): kpi-analyst — plafond de densité 250 → 300 (ADR-029, D-01)"
  - sha: f709f7b
    subject: "chore(40.1-12): kpi-analyst — bump patch v1.0.5 → v1.0.6 (ADR-029, D-01)"
  - sha: 52c1bbe
    subject: "chore(40.1-12): audit-architecture — plafond auditeurs 250 → 300 (ADR-029, D-01)"
  - sha: 18728b5
    subject: "chore(40.1-12): audit-architecture — bump patch v1.0.2 → v1.0.3 (ADR-029, D-01)"

verification:
  - "check-no-live-250.sh . filtré sur plugin/kpi-analyst/ et plugin/audit-architecture/ : 0
    ligne vivante dans les deux cas"
  - "check-instruction-budget.sh : ligne plugin/kpi-analyst/AGENT.md — lignes=96 (baseline 96),
    instructions=13 (baseline 13), verdict OK ; exit 0"
  - "check-agents.sh --strict --file plugin/kpi-analyst/AGENT.md : vert"
  - "plugin/kpi-analyst/scripts/tests/test-kpis-writer.sh : vert"
  - "check-module-bump.sh kpi-analyst patch : conforme (v1.0.5 → v1.0.6)"
  - "check-module-bump.sh audit-architecture patch : conforme (v1.0.2 → v1.0.3)"
  - "check-version-sync.sh : vert (17 modules alignés)"
  - "test-check-description-fidelity.sh : classe T20-T25 vérifiée nominale — T21 (contrôle,
    mutant, contre-épreuve, byte-identique sur audit-architecture/SKILL.md) intégralement vert ;
    voir Notes pour le rouge T10 observé (hors périmètre de ce plan)"

known-issues: []

next-steps:
  - "40.1-04 : rejouer les suites conductor (dont test-check-description-fidelity.sh en
    entier) une fois tous les plans de la vague 2 mergés, pour confirmer T10 (stabilité de
    l'arbre pendant la suite) au vert sans drift de voisin."
---

# Plan 12 — kpi-analyst et audit-architecture alignés sur le plafond ADR-029 (300 L)

## Ce qui a été fait

**Task 1 — kpi-analyst.** `AGENT.md` L84 : `≤250L` → `≤300L`, remplacement de valeur seul (aucune
ligne ni instruction ajoutée/retirée — confirmé par `check-instruction-budget.sh` : lignes=96/96,
instructions=13/13, verdict `OK`). Bump patch dérivé v1.0.5 → v1.0.6 (`VERSION`, `module.json`,
en-tête `README.md`, entrée `CHANGELOG.md` en tête citant D-01 et la Phase 40.1).

**Task 2 — audit-architecture.** `SKILL.md` L114 (body, frontmatter intact) et `README.md` L88 :
`≤ 250 L` → `≤ 300 L`. Bump patch dérivé v1.0.2 → v1.0.3 (mêmes quatre fichiers). La ligne L97 de
`SKILL.md` (« ≤95 s, ≤300 lignes ») n'a pas été touchée : elle citait déjà 300 (contrainte du
script/test mesurable de l'exemple, sans lien avec le plafond agent).

Chaque module a reçu deux commits distincts : un commit de contenu (valeur seule) puis un commit
de bump (VERSION + module.json + README + CHANGELOG dans le même commit, requis par
`check-module-bump.sh` qui vérifie que ce commit touche `module.json` et `CHANGELOG.md`).

## Vérifications rejouées

Toutes les commandes `<verify>` des deux tâches ont été rejouées et sont vertes, à l'exception
d'un point noté ci-dessous.

## Notes — rouge transitoire T10 de `test-check-description-fidelity.sh`

Le mandat signalait explicitement ce risque : la suite complète a un test `T10` qui vérifie que
l'arbre du dépôt n'a pas bougé pendant son exécution (garde anti-drift générique, pas spécifique à
`audit-architecture`). Le worktree est partagé avec 9 autres workers de la vague 2 qui committent
en continu (notamment 40.1-03 sur `check-instruction-budget.sh` et 40.1-07 sur des fichiers
`dev-orchestrator`) — rejoué trois fois à quelques dizaines de secondes d'écart, `T10` a échoué à
chaque fois en nommant des fichiers **hors de mon périmètre** (jamais `plugin/kpi-analyst/` ni
`plugin/audit-architecture/`). Le sous-ensemble **spécifiquement ciblé par ce plan** — la classe
T20-T25, et en particulier `T21` (contrôle vert avant mutation, mutant rougissant en nommant
`audit-architecture/SKILL.md`, contre-épreuve verte, fichier source byte-identique par sha256
après coup) — est intégralement vert dans les trois rejeux. Traité en `no-op` : rouge attribuable
au voisinage en vol, pas à ce plan ; à revérifier en entier par 40.1-04 une fois la vague 2 fermée.

## Aucune dérive de périmètre

Seuls les 10 fichiers listés dans `files_modified` du plan ont été touchés (5 par module).
Rien sous STATE.md/ROADMAP.md, aucun `git checkout --`/`restore`, aucun fichier voisin.

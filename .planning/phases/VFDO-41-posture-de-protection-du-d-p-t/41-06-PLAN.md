---
phase: 41-posture-de-protection-du-d-p-t
plan: 06
lot: L6 — revue code owner prouvée côté GitHub (`.github/`, baseline, sentinelle) et refus sans contournement avec sa provenance (temps i)
type: execute
wave: 6
depends_on: ["41-05"]
autonomous: false
requirements: [PROT-04, PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition : `M2-VERDICT: CONFORME`, ou `M2-DECISION:` d'option `accepter-et-documenter` consignée avec son canal et sa date (41-05)"
    - "Support du temps (i) et, plus tard, du temps (ii) (discrétion de D-M7) : PR-P au contenu inoffensif et utile — `.github/rulesets/README.md` —, donc sous `/.github/` du CODEOWNERS ; elle reste ouverte pour le plan 41-08"
    - "Preuve GitHub de CODEOWNERS au-delà de `.github/` (point 4 du tour 1) : deux PR jetables, chacune ne touchant qu'UN chemin par l'ajout d'une seule ligne de commentaire, jamais mergées — PR-BASELINE (`.planning/instruction-budget-baselines.tsv`) et PR-SENTINELLE (`.planning/.instruction-budget-armed`, le motif à fichier point). `@picmakpro` doit figurer dans les relecteurs demandés, LU PAR L'AGENT via l'API pendant qu'elles sont ouvertes. La baseline de `main` n'est jamais altérée : ces branches ne sont pas mergées et sont fermées au début du plan 41-07"
    - "Ordre de sûreté : la PREMIÈRE tentative de merge sans contournement de la phase a lieu sur PR-P, inoffensive, jamais sur la PR rouge"
    - "Provenance du refus (point 2 du tour 1) : la sortie de `gh pr merge` est classée `serveur` si elle porte une réponse d'API (`GraphQL:` ou `HTTP 4xx`), `client_gh` si elle porte le pré-contrôle de `gh` (« is not mergeable », « base branch policy »), `indeterminee` sinon ; un refus client n'est jamais écrit `serveur`. La provenance serveur d'un merge sans contournement est mesurée sur PR-P au plan 41-08 (appel REST direct), pas ici, pour ne pas consommer le support du temps (ii)"
    - "Tout écart (merge passé sans contournement, `@picmakpro` absent d'une des trois PR, état non bloqué) donne `CO-VERDICT: ECART` et arrête le plan sur la Task 4, checkpoint de décision humain qui dit quoi faire de PR-P, des deux PR jetables et des rulesets"
    - "Tout commit de plan et tout corps de PR citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-P-COMMITS:`, `PR-CO-COMMITS:`, `PR-P:`, `PR-BASELINE:`, `PR-SENTINELLE:`, `CO-I:`, `CO-I-MSG:`, `CO-BASELINE:`, `CO-SENTINELLE:`, `CO-VERDICT:`"
  key_links:
    - "`CO-I: … merge_sans_contournement=refuse` est la précondition du plan 41-07 (tentatives sur la PR rouge)"
    - "PR-BASELINE et PR-SENTINELLE sont fermées au pas 0 de la Task 2 du plan 41-07, avant la mesure M-1"
---

<objective>
Prouver côté GitHub que la revue code owner est exigée sur `.github/`, sur la baseline du budget
d'instructions et sur une sentinelle point, et que `gh pr merge` sans contournement refuse une PR
bloquée — en consignant d'où vient ce refus.

Purpose: PROT-04 (revue code owner exigée par défaut, preuve par chemin) et sûreté des essais de 41-07
(D-M7).
Output: PR-P ouverte et bloquée ; deux PR jetables ouvertes avec `@picmakpro` demandé ; lignes `CO-*`.

Budget (point 9 du tour 1, justification explicite) : quatre tâches, dont la quatrième est un
checkpoint de décision CONDITIONNEL, sans exécution quand `CO-VERDICT: CONFORME`. La scinder ferait
porter la mesure et la décision par deux exécuteurs sans contexte commun. Fichiers : un seul
(`41-PREUVES.md`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-05-SUMMARY.md

Sources lues le 2026-09-17 : `cli.github.com/manual/gh_pr_merge` (`--admin` « Use administrator
privileges to merge a pull request that does not meet requirements » ; comportement sans cette
option sous ruleset non documenté) ; about-code-owners (auteur ne peut pas approuver sa PR ; version
de la branche de base ; le propriétaire est demandé en relecture quand la PR touche un fichier
possédé).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : construire P1, P2 et les deux commits jetables à un seul chemin, par plomberie ; préalables relus</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-*`, `M2:`, `M2-VERDICT:`, `M2-DECISION:`, `ACCES-RULE-SUITES:`)
    - docs/ADR.md § ADR-072 (retour arrière, sources) pour le contenu du README
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin` ; aucune PR ouverte ; `POSE-CONFORME:` présente ;
`M2-VERDICT: CONFORME` ou `M2-DECISION: option=accepter-et-documenter` (sinon arrêt).

Rédiger dans un fichier temporaire (hors dépôt) le contenu de `.github/rulesets/README.md`, en
français : rôle des deux JSON (corps de `POST …/rulesets`), commande de pose depuis `origin/main` par
`picmakpro`, commandes de relecture (`rulesets`, `rules/branches/main`), retour arrière
(`enforcement=disabled`), rappel que toute modification exige la revue `@picmakpro` ou un
contournement tracé (D-02, D-05), renvoi à ADR-072. Aucun chemin de machine.

Construire par plomberie (index temporaire via `GIT_INDEX_FILE`, `read-tree`, `hash-object -w`,
`update-index --add --cacheinfo 100644,<blob>,<chemin>`, `write-tree`, `commit-tree`), sans changer
de branche ni toucher l'arbre de travail :
- P1 = `origin/main` + ce seul README, message « docs(41): README des sources de rulesets — support
  de preuve de la revue code owner (PR-P) » ;
- P2 = P1 + une ligne finale au README « Ce fichier est entré sur `main` par un contournement
  volontaire et tracé : preuve du critère 2 de la Phase 41 (voir 41-PREUVES.md). », message
  « docs(41): trace du contournement volontaire (PR-P, temps ii) » ;
- CB = `origin/main` + `.planning/instruction-budget-baselines.tsv` augmenté d'UNE ligne finale
  `# PREUVE 41 — PR jetable de revue code owner, jamais mergée`, message « test(41): PR jetable —
  revue code owner sur la baseline (NE PAS MERGER) » ;
- CS = `origin/main` + `.planning/.instruction-budget-armed` augmenté de la même ligne, message
  « test(41): PR jetable — revue code owner sur la sentinelle (NE PAS MERGER) ».

Consigner `PR-P-COMMITS: p1=<sha> p2=<sha> base=<origin/main>` et
`PR-CO-COMMITS: baseline=<sha> sentinelle=<sha> base=<origin/main>`. Commit local sur B, message
« docs(41-06): commits de preuve de la revue code owner préparés ». Imprimer les commandes de la
Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); P1=$(val PR-P-COMMITS: p1); P2=$(val PR-P-COMMITS: p2); BA=$(val PR-P-COMMITS: base); T=$(mktemp -d); git diff --name-only "$BA" "$P1" > "$T/d1"; d1=$(awk "END{print NR}" "$T/d1"); f1=$(awk "NR==1" "$T/d1"); git diff --name-only "$P1" "$P2" > "$T/d2"; d2=$(awk "END{print NR}" "$T/d2"); pa1=$(git rev-parse "$P1^"); pa2=$(git rev-parse "$P2^"); git show "$P2:.github/rulesets/README.md" > "$T/readme"; rl=$(awk "END{print NR}" "$T/readme"); mp=$(awk "/\\/(Users|home)\\/[A-Za-z0-9._-]+/ {c++} END{print c+0}" "$T/readme"); br=$(git branch --show-current); echo "PR-P commits : identite=$u prs_ouvertes=$op fichiers_p1=$d1 ($f1) fichiers_p2=$d2 parent_p1_est_base=$([ "$pa1" = "$BA" ] && echo oui || echo non) parent_p2_est_p1=$([ "$pa2" = "$P1" ] && echo oui || echo non) chemins_machine=$mp readme_lignes=$rl branche_arbre=$br"; [ "$rl" -gt 3 ] && [ "$u" = samuel-neveugall ] && [ "$op" -eq 0 ] && [ "$d1" -eq 1 ] && [ "$f1" = .github/rulesets/README.md ] && [ "$d2" -eq 1 ] && [ "$pa1" = "$BA" ] && [ "$pa2" = "$P1" ] && [ "$mp" -eq 0 ] && [ "$br" = feat/phase-41-preuves-release ]'</automated>
    <fails_when>exit non nul ; ou une PR ouverte ; ou P1 touche autre chose que `.github/rulesets/README.md` ; ou P2 touche plus d'un fichier ; ou filiation fausse (P1 hors `origin/main`, P2 hors P1) ; ou chemin de machine dans le README ; ou l'arbre de travail a changé de branche</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; BA=$(val PR-CO-COMMITS: base); CB=$(val PR-CO-COMMITS: baseline); CS=$(val PR-CO-COMMITS: sentinelle); T=$(mktemp -d); git diff --name-only "$BA" "$CB" > "$T/b"; git diff --name-only "$BA" "$CS" > "$T/s"; fb=$(awk "BEGIN{ORS=\",\"} {print}" "$T/b"); fs=$(awk "BEGIN{ORS=\",\"} {print}" "$T/s"); pb=$(git rev-parse "$CB^"); ps=$(git rev-parse "$CS^"); git diff "$BA" "$CB" > "$T/db"; ab=$(awk "/^\+[^+]/ {c++} END{print c+0}" "$T/db"); rb=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/db"); git diff "$BA" "$CS" > "$T/ds"; as=$(awk "/^\+[^+]/ {c++} END{print c+0}" "$T/ds"); rs=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/ds"); echo "PR code owner jetables : fichiers_baseline=$fb parent_baseline_est_base=$([ "$pb" = "$BA" ] && echo oui || echo non) lignes_ajoutees=$ab lignes_retirees=$rb | fichiers_sentinelle=$fs parent_sentinelle_est_base=$([ "$ps" = "$BA" ] && echo oui || echo non) lignes_ajoutees=$as lignes_retirees=$rs"; [ "$fb" = ".planning/instruction-budget-baselines.tsv," ] && [ "$fs" = ".planning/.instruction-budget-armed," ] && [ "$pb" = "$BA" ] && [ "$ps" = "$BA" ] && [ "$ab" -eq 1 ] && [ "$rb" -eq 0 ] && [ "$as" -eq 1 ] && [ "$rs" -eq 0 ] && [ "$BA" = "$(git rev-parse origin/main)" ]'</automated>
    <fails_when>exit non nul ; ou un commit jetable touche un autre chemin que le sien ; ou il retire une ligne (la baseline ou la sentinelle serait altérée au lieu d'être seulement touchée) ; ou il ajoute plus d'une ligne ; ou il ne part pas d'`origin/main`</fails_when>
  </verify>
  <acceptance_criteria>
    - P1 = `origin/main` + le seul README ; P2 = P1 + une ligne ; aucun chemin de machine
    - CB et CS partent d'`origin/main`, ne touchent chacun que leur chemin, par ajout d'une seule ligne, sans retrait
    - `PR-P-COMMITS:` et `PR-CO-COMMITS:` consignées ; arbre de travail resté sur B
  </acceptance_criteria>
  <done>Les supports inoffensifs existent localement, prêts à être publiés par un humain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — publier P1, CB, CS ; ouvrir PR-P, PR-BASELINE, PR-SENTINELLE ; tenter un merge SANS contournement sur PR-P</name>
  <action>Samuel, compte `samuel-neveugall`, publie les trois commits, ouvre les trois PR, attend la CI de PR-P, relit l'état de merge, tente un merge sans option de contournement sur PR-P seulement et capture la sortie. Aucune PR n'est fermée ici.</action>
  <instructions>
Compte qui agit : `samuel-neveugall` (témoin `gh api user --jq .login`). SHA imprimés par la Task 1.

1. `git push origin <p1>:refs/heads/preuve/41-contournement`
   `git push origin <cb>:refs/heads/preuve/41-codeowner-baseline`
   `git push origin <cs>:refs/heads/preuve/41-codeowner-sentinelle`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-contournement --title "Phase 41 — README des sources de rulesets (PR de preuve : revue code owner et contournement tracé)" --body "PR de preuve de la Phase 41 (D-M7). Contenu inoffensif. Temps (i) : merge sans contournement attendu refusé. Temps (ii) au plan 41-08."` → noter `<n>`.
   `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-codeowner-baseline --title "PREUVE 41 — NE PAS MERGER — revue code owner sur la baseline" --body "PR jetable de la Phase 41 : demande de revue @picmakpro sur .planning/instruction-budget-baselines.tsv (D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Jamais mergée."` → noter `<nb>`.
   `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-codeowner-sentinelle --title "PREUVE 41 — NE PAS MERGER — revue code owner sur la sentinelle" --body "PR jetable de la Phase 41 : demande de revue @picmakpro sur .planning/.instruction-budget-armed (D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Jamais mergée."` → noter `<ns>`.
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tous verts attendus).
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus,reviewDecision --jq '{mergeStateStatus, reviewDecision}'`
4b. `gh --version | head -1` → copier (version du client qui fait la tentative ; relevée à nouveau en 41-07).
5. SEULEMENT si 4 rend `mergeStateStatus` = `BLOCKED` : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge`
   (SANS `--admin`, sans case de contournement dans l'interface) → copier la sortie INTÉGRALE, sans
   la résumer (sa forme décide de la provenance consignée). Si 4 rend autre chose que `BLOCKED` : ne
   rien merger, noter l'état.
6. Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state --jq .state` → `OPEN` attendu.
Ne fermer AUCUNE PR : PR-P sert au plan 41-08 ; PR-BASELINE et PR-SENTINELLE sont relues par l'agent
pendant qu'elles sont ouvertes, puis fermées au pas 0 du plan 41-07.
  </instructions>
  <verification>La Task 3 relit via l'API, pendant qu'elles sont ouvertes, la tête, les fichiers, les relecteurs demandés et l'état des trois PR, les check runs de PR-P et son retard sur `main`.</verification>
  <resume-signal>Coller `<n>`, `<nb>`, `<ns>` et les sorties de 3 (fin), 4, 5 (intégrale, ou « non tenté : état X ») et 6, puis écrire « temps i fait ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : relecture API et consignation — temps (i) sur PR-P avec provenance, relecteur code owner par chemin, verdict</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Consigner `PR-P: #<n> branche=preuve/41-contournement`,
`PR-BASELINE: #<nb> branche=preuve/41-codeowner-baseline`,
`PR-SENTINELLE: #<ns> branche=preuve/41-codeowner-sentinelle`, puis :
- `CO-I: tete=<sha> checks_verts_deux_evenements=<k>/4 behind_by=<b> review=<reviewDecision> demande_picmakpro=<oui|non> etat=<mergeStateStatus collé> merge_sans_contournement=<refuse|merge|non_tente> provenance=<serveur|client_gh|indeterminee> gh_version=<x.y.z lu dans CO-I-GH-MSG> etat_pr_apres=<state>` ;
- `CO-I-MSG:` suivi de la sortie de `gh pr merge` collée, intégrale, sur une ligne ;
- `CO-I-GH-MSG:` suivi de la sortie collée de `gh --version | head -1` (étape 4b) ;
- `CO-BASELINE: demande_picmakpro=<oui|non> relue=oui` et `CO-SENTINELLE: demande_picmakpro=<oui|non> relue=oui`,
  depuis SA PROPRE lecture `gh pr view --json reviewRequests` des PR ouvertes (jamais depuis le collage) ;
- `CO-VERDICT: <CONFORME|ECART>` — CONFORME si merge refusé sur un état `BLOCKED` et `@picmakpro`
  demandé sur les deux PR jetables (et sur PR-P, vérifié par la vérification 1).
Règles : `merge_sans_contournement=refuse` seulement si la sortie collée est une erreur ET la PR relue
est `OPEN` ; `merge` si `MERGED` ; `non_tente` si l'état n'était pas `BLOCKED`. `provenance` suit le
classement du frontmatter ; si seul le pré-contrôle client est obtenu, c'est écrit `client_gh`, tel
quel. Un écart de valeur n'est jamais corrigé : il est consigné et mène à la Task 4.
Commit local sur B, message « docs(41-06): temps i de la revue code owner consigné, provenance du
refus (D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-P absente"; exit 1;; esac; P1=$(val PR-P-COMMITS: p1); H=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); rd=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewDecision --jq .reviewDecision); rq=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewRequests --jq "[.reviewRequests[].login] | index(\"picmakpro\") != null"); ok=$(gh api "repos/picmakpro/vibeflow-os/commits/$H/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | group_by(.name) | map(select(length >= 2)) | length"); bb=$(gh api "repos/picmakpro/vibeflow-os/compare/main...$H" --jq .behind_by); c=$(val CO-I: merge_sans_contournement); e=$(val CO-I: etat); pc=$(val CO-I: provenance); pm=$(awk "\$1==\"CO-I-MSG:\" { if (index(\$0, \"GraphQL:\") || \$0 ~ /HTTP 4[0-9][0-9]/) r=\"serveur\"; else if (index(\$0, \"is not mergeable\") || index(\$0, \"base branch policy\")) r=\"client_gh\"; else r=\"indeterminee\"; print r; exit }" "$PH/41-PREUVES.md"); echo "temps i code owner : pr=$N tete_est_p1=$([ "$H" = "$P1" ] && echo oui || echo non) etat_pr=$st review=$rd picmakpro_demande=$rq jobs_verts_deux_evenements=$ok behind_by=$bb etat_consigne=$e merge_sans_contournement=$c provenance_consignee=$pc provenance_recalculee=$pm"; [ "$H" = "$P1" ] && [ "$st" = OPEN ] && [ "$rd" = REVIEW_REQUIRED ] && [ "$rq" = true ] && [ "$ok" -eq 4 ] && [ "$bb" -eq 0 ] && [ "$e" = BLOCKED ] && [ "$c" = refuse ] && [ -n "$pm" ] && [ "$pc" = "$pm" ]'</automated>
    <fails_when>exit non nul ; ou tête différente de P1 ; ou PR non `OPEN` ; ou `review=` différent de `REVIEW_REQUIRED` ; ou `picmakpro_demande=false` ; ou `jobs_verts_deux_evenements=` différent de 4 ; ou `behind_by=` différent de 0 ; ou `etat_consigne=` différent de `BLOCKED` ; ou `merge_sans_contournement=` différent de `refuse` ; ou provenance consignée différente de la provenance recalculée sur `CO-I-MSG:` (un refus du pré-contrôle client de `gh` n'est jamais écrit `serveur`). Rouge de VALEUR (merge passé, relecteur absent, état non bloqué) : consigné tel quel, `CO-VERDICT: ECART`, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; k=0; for key in PR-BASELINE PR-SENTINELLE; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "$key absente"; k=$((k+1)); continue;; esac; case "$key" in PR-BASELINE) C=$(val PR-CO-COMMITS: baseline); F=.planning/instruction-budget-baselines.tsv; L=CO-BASELINE;; *) C=$(val PR-CO-COMMITS: sentinelle); F=.planning/.instruction-budget-armed; L=CO-SENTINELLE;; esac; H=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); fl=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json files --jq "[.files[].path] | join(\",\")"); rq=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewRequests --jq "[.reviewRequests[].login] | index(\"picmakpro\") != null"); cd=$(val "$L:" demande_picmakpro); cr=$(val "$L:" relue); echo "$key #$N : tete_ok=$([ "$H" = "$C" ] && echo oui || echo non) etat=$st fichiers=$fl picmakpro_demande_api=$rq consigne=$cd relue=$cr"; if [ "$H" != "$C" ] || [ "$st" != OPEN ] || [ "$fl" != "$F" ] || [ "$cr" != oui ] || { [ "$rq" = true ] && [ "$cd" != oui ]; } || { [ "$rq" != true ] && [ "$cd" != non ]; }; then k=$((k+1)); fi; [ "$rq" = true ] || { echo "RELECTEUR CODE OWNER ABSENT : $F"; k=$((k+1)); }; done; echo "preuve GitHub CODEOWNERS par chemin : en_defaut=$k"; [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou numéro de PR absent ; ou tête différente du commit jetable ; ou PR non `OPEN` au moment de la lecture ; ou fichiers de la PR différents du seul chemin visé ; ou consignation différente de la lecture API (`relue` doit valoir `oui`) ; ou `RELECTEUR CODE OWNER ABSENT` — CODEOWNERS ne couvre pas ce chemin côté GitHub : PROT-04 en défaut, `CO-VERDICT: ECART`, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; c=$(val CO-I: merge_sans_contournement); e=$(val CO-I: etat); b=$(val CO-BASELINE: demande_picmakpro); s=$(val CO-SENTINELLE: demande_picmakpro); att=$([ "$c" = refuse ] && [ "$e" = BLOCKED ] && [ "$b" = oui ] && [ "$s" = oui ] && echo CONFORME || echo ECART); v=$(awk "\$1==\"CO-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); echo "verdict code owner : attendu=$att consigne=$v"; [ "$v" = "$att" ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou verdict consigné différent du verdict recalculé (erreur de consignation, corrigée ici) ; ou `ECART` — consigné tel quel, aucune correction, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; gv=$(val CO-I: gh_version); gm=$(awk "\$1==\"CO-I-GH-MSG:\" && \$2==\"gh\" && \$3==\"version\" {print \$4; exit}" "$PH/41-PREUVES.md"); ok=oui; case "$gv" in ([0-9]*.[0-9]*.[0-9]*) ;; (*) ok=non;; esac; echo "version gh de la tentative 41-06 : consignee=$gv sortie_collee=$gm format=$ok"; [ "$ok" = oui ] && [ "$gv" = "$gm" ]'</automated>
    <fails_when>exit non nul ; ou `gh_version` absente, mal formée, ou différente de la version lue dans la sortie collée `CO-I-GH-MSG:` (point 3 du tour 2 : la version du client qui a refusé est consignée pour être comparée en 41-07)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; k=0; t=0; : > "$T/corps"; for key in PR-P PR-BASELINE PR-SENTINELLE; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "NUMERO DE PR ABSENT : $key"; k=$((k+1)); continue;; esac; t=$((t+1)); printf "@@CORPS@@ %s #%s\n" "$key" "$N" >> "$T/corps"; gh pr view "$N" --repo picmakpro/vibeflow-os --json body --jq .body >> "$T/corps"; done; r=0; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@CORPS@@ / { if (lib != \"\") juge(m, lib); lib=\$0; m=\"\"; next } { m = m \" \" \$0 } function juge(m, lib,   j, ok, p) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (lib != \"\") juge(m, lib); print \"corps de PR : en_defaut=\" v+0; exit (v > 0) }" "$T/arb" "$T/corps" || r=$?; echo "corps de PR : lus=$t numeros_absents=$k"; [ "$r" -eq 0 ] && [ "$k" -eq 0 ] && [ "$t" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou un corps de PR qui cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » ; ou un corps qui fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal aux valeurs CAPTURÉES (frontmatter de 41-01 ou `41-PREUVES.md`) ; ou un numéro de PR non consigné</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - PR-P ouverte sur P1, CI verte sur les deux événements, à jour, `@picmakpro` demandé, `REVIEW_REQUIRED`, `BLOCKED`
    - merge sans contournement refusé, sortie intégrale capturée, provenance consignée = provenance recalculée, version de `gh` consignée
    - PR-BASELINE et PR-SENTINELLE ouvertes, un seul fichier chacune, `@picmakpro` demandé, lu par l'agent via l'API
    - `CO-VERDICT: CONFORME` recalculé = consigné ; corps de PR et messages de commit conformes (canal et date)
  </acceptance_criteria>
  <done>La revue code owner est prouvée côté GitHub sur trois chemins, et le refus par défaut est établi avec sa provenance exacte.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `CO-VERDICT: ECART` (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <decision>La revue code owner ou le refus sans contournement ne se comportent pas comme arbitré : que faire des trois PR de preuve et des rulesets ?</decision>
  <context>
    Lire `CO-I:`, `CO-I-MSG:`, `CO-BASELINE:`, `CO-SENTINELLE:`, `CO-VERDICT:`. Cas possibles : PR-P
    mergée sans contournement (contenu inoffensif entré sur `main`, D-09 en défaut) ; `@picmakpro`
    absent d'une PR (CODEOWNERS ne couvre pas ce chemin côté GitHub, PROT-04 en défaut) ; état non
    bloqué. Aucune correction de CODEOWNERS ni du ruleset n'est tentée sans arbitrage.
  </context>
  <options>
    <option id="fermer-et-cadrer">
      <name>FERMER LES PR DE PREUVE ET CADRER</name>
      <pros>Rien ne reste en vol : Samuel ferme les PR encore ouvertes (`gh pr close <num> --repo picmakpro/vibeflow-os` puis `git push origin --delete <branche>` pour chacune) ; rulesets inchangés ; l'écart est cadré avant toute suite.</pros>
      <cons>Les plans 41-07 et suivants attendent ; le temps (ii) devra retrouver un support.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE</name>
      <pros>Fermeture des PR de preuve comme ci-dessus, puis `picmakpro` repasse chaque ruleset en `disabled` (commande de `RETOUR-ARRIERE-ECRIT:`).</pros>
      <cons>`main` redevient protégée par rien jusqu'à une nouvelle pose.</cons>
    </option>
    <option id="accepter-et-documenter">
      <name>ACCEPTER ET DOCUMENTER</name>
      <pros>Les preuves avancent (PR-BASELINE et PR-SENTINELLE fermées au pas 0 de 41-07 ; PR-P gardée si encore ouverte) ; l'écart est écrit dans ADR-072 § Mesures et dans le ledger.</pros>
      <cons>PROT-04 ou le critère 2 ne serait pas tenu tel que formulé ; engage un arbitrage daté et attribué.</cons>
    </option>
  </options>
  <resume-signal>Répondre « non déclenchée », « FERMER LES PR DE PREUVE ET CADRER », « RETOUR ARRIÈRE » ou « ACCEPTER ET DOCUMENTER ». Après la réponse, l'exécutant consigne `CO-DECISION: option=<id> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint>` — jamais inféré, jamais une date recopiée d'ailleurs — et le commite sur B avec le message « docs(41-06): CO-DECISION consignée — arbitrage Samuel, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `CO-DECISION` sans ce canal et cette date rougit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| essai de merge → `main` | une tentative qui passerait malgré le blocage ferait atterrir le support |
| PR jetable → baseline de `main` | une PR touchant la baseline ne doit jamais atterrir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-23 | Tampering | merge sans contournement accepté | high | mitigate | première tentative sur un support inoffensif ; tentative conditionnée à `BLOCKED` ; état relu ; 41-07 conditionné à `refuse` ; écart → Task 4 |
| T-41-24 | Repudiation | blocage attribué à tort à la revue | medium | mitigate | CI verte sur les deux événements et `behind_by=0` exigés, `REVIEW_REQUIRED` et relecteur `picmakpro` relus |
| T-41-25 | Information disclosure | chemin de machine dans le README qui atterrira sur `main` | low | mitigate | balayage du blob P2 ; `check-machine-paths` en CI sur la PR |
| T-41-48 | Repudiation | refus client présenté comme refus serveur | medium | mitigate | provenance recalculée sur la sortie intégrale ; provenance serveur mesurée à part (41-08, REST) |
| T-41-49 | Tampering | baseline ou sentinelle altérée par une PR jetable | high | mitigate | ajout d'une seule ligne de commentaire, aucun retrait (vérifié) ; PR jamais mergée, fermée au pas 0 de 41-07 et relue fermée |
| T-41-50 | Tampering | CODEOWNERS ne couvrant pas un chemin côté GitHub | high | mitigate | relecteur `@picmakpro` lu par l'agent via l'API sur la baseline et la sentinelle point ; écart → Task 4 |
</threat_model>

<verification>
PR-P bloquée par la seule revue code owner, merge sans contournement refusé avec provenance consignée ;
`@picmakpro` demandé sur la baseline et la sentinelle ; verdict consigné.
</verification>

<success_criteria>
Temps (i) de PROT-04 prouvé par essais réels sur trois chemins ; précondition de sûreté de 41-07 établie.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-06-SUMMARY.md` when done — committé sur la branche B.
</output>

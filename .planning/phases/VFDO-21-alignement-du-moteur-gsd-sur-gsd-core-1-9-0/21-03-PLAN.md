---
phase: 21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0
plan: 03
subsystem: dev-orchestrator
tags: [gsd-core-1.9.0, version-debt, semver-trap, hooks, adr-062, merge-hooks]

requires: []
provides:
  - "Plus aucune référence documentaire ou par défaut de script à « gsd-core 1.8.0 » dans le
    périmètre VibeFlow (hors fixtures de test et historique CHANGELOG/README/ADR, volontairement
    préservés) — la dette de version figée du delta 1.9.0 est purgée"
  - "test-check-gsd-engine.sh cas 8 bouge avec le texte de check-gsd-engine.sh:25 (piège D-05 :
    « 1.8.0 » → « 1.9.0 »), prouvé discriminant par mutation avant restauration"
  - "ADR-062 tranche noir sur blanc que les deux hooks 1.9.0 non câblés (gsd-ensure-canonical-path.js,
    gsd-update-banner.js) restent hors périmètre de merge-hooks.sh — sur preuves, pas par omission"
affects: ["mission 2026-07-31-delta-gsd-core-1.9.0.md (Dette de version + Observation annexe hooks)"]

tech-stack:
  added: []
  patterns:
    - "Un index auto-généré ne se corrige jamais à la main : le défaut de son générateur se corrige,
      puis l'index se régénère en ré-exécutant le script (D4, anti-hallucination) — jamais une
      édition manuelle du fichier de sortie"
    - "Un test qui asserte une chaîne littérale documentaire doit être considéré comme UN SEUL fait
      avec le texte qu'il vérifie : les deux bougent dans le même commit, jamais l'un sans l'autre,
      sous peine de fenêtre rouge dans l'historique"
    - "Arbitrage d'infrastructure vérifié séparément par sous-composant (chaque hook confronté à
      son propre contrat d'activation documenté dans son en-tête) plutôt qu'une règle unique
      appliquée aux deux — deux hooks qui partagent un non-câblage n'ont pas forcément la même
      cause"

key-files:
  created: []
  modified:
    - plugin/dev-orchestrator/scripts/build-gsd-index.sh
    - plugin/dev-orchestrator/references/gsd-skills-index.md
    - plugin/dev-orchestrator/references/mission-contracts.md
    - plugin/dev-orchestrator/scripts/check-gsd-engine.sh
    - plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh
    - plugin/dev-orchestrator/scripts/ensure-deps.sh
    - plugin/planning-core/scripts/detect-gsd-engine.sh
    - docs/ADR.md
    - plugin/dev-orchestrator/README.md

key-decisions:
  - "Occurrence supplémentaire trouvée hors de l'énumération du digest : ensure-deps.sh:73 (« scope
    --local de gsd-core 1.8.0 ») — même famille documentaire que detect-gsd-engine.sh:30, purgée
    par cohérence avec l'intention du Changement 5 (purge exhaustive dans le périmètre du module),
    aucun test ne l'assertait littéralement."
  - "gsd-skills-index.md n'est JAMAIS édité à la main : seul son générateur (build-gsd-index.sh,
    défaut GSD_CORE_PACKAGE) est corrigé, puis le script est ré-exécuté contre les 71 skills
    réellement installés sur ce poste. La régénération a fait apparaître 3 entrées supplémentaires
    dans la section secondaire « Workflows GSD » (list-seeds, onboard, smart-entry) : legitimes,
    la source de cette section (gsd-core/workflows sur disque) avait changé depuis la dernière
    génération du 2026-07-26, antérieure à la migration 1.9.0 de ce poste — pas une régression."
  - "Cas 8 de test-check-gsd-engine.sh traité comme UN SEUL changement avec l'en-tête qu'il vérifie
    (même commit) : le déplacer séparément aurait ouvert une fenêtre où soit le texte est faux, soit
    le test est rouge. Preuve par mutation avant/après (mutation ciblée sur les 2 occurrences de la
    ligne 25, restauration vérifiée par diff nul) : cas 8 passe de rouge (142=3 190=0 semver=2) à
    vert une fois la restauration appliquée — le test discrimine réellement le texte, ce n'est pas
    un test tautologique."
  - "Changement 6 tranché par ADR dédiée (ADR-062), même patron qu'ADR-060/061 : l'arbitrage est
    structurant (referait surface à chaque lecture du delta) et les deux hooks ont des causes
    disjointes qui méritent chacune sa preuve écrite plutôt qu'une phrase générique."
  - "gsd-update-banner.js : absence CORRECTE, pas un gap — son propre en-tête documente un
    enregistrement opt-in par l'installeur amont, actif seulement si la statusline GSD est déclinée ;
    or ce poste a `gsd-statusline.js` câblé dans settings.json. Câbler le banner à la main romprait
    ce contrat amont (double signal)."
  - "gsd-ensure-canonical-path.js : absence CORRECTE au vu du chemin d'install de VibeFlow. Son
    propre en-tête documente qu'il répare un problème qui n'existe que sous une install `gsd-core`
    en PLUGIN MARKETPLACE Claude Code (CLAUDE_PLUGIN_ROOT). VibeFlow bootstrappe gsd-core via
    `ensure-deps.sh:263` (`npx -y \"@opengsd/gsd-core@^1\" --claude <scope>`) — l'installeur npm
    classique que le hook lui-même cite comme cas SANS besoin de sa correction. Vérifié sur pièce :
    `~/.claude/gsd-core/` est un répertoire réel sur ce poste, pas un lien posé par ce hook."
  - "Aucune ligne de merge-hooks.sh modifiée : son commentaire existant (:145-150, « hooks
    tiers/gsd-core ») documentait déjà cette intention — ADR-062 la confirme avec preuves plutôt
    que de la dupliquer ou de la réécrire."
  - "Index ADR.md corrigé en même temps (ajout des lignes ADR-060/061/062, absentes malgré leur
    validation dans les plans 20-06/21-02) : laisser ma propre entrée ADR-062 indexée seule aurait
    rendu le tableau encore plus trompeur qu'avant — correction additive, aucun contenu d'ADR
    existante modifié."

patterns-established:
  - "Confrontation séparée de chaque composant à son propre contrat d'activation documenté (lu
    dans son en-tête / son commentaire) avant de conclure à un gap — jamais une hypothèse partagée
    appliquée à un groupe hétérogène de symptômes qui se ressemblent en surface"

requirements-completed: ["Changement 5 (purge dette de version 1.8.0)", "Changement 6 (arbitrage hooks 1.9.0 non câblés)"]

coverage:
  - id: D1
    description: "Toute référence documentaire ou par défaut de script à « gsd-core 1.8.0 » dans
      le périmètre dev-orchestrator/planning-core (hors fixtures de test, CHANGELOG, README,
      ADR-058) est mise à jour vers 1.9.0 — y compris une occurrence non listée par le digest
      (ensure-deps.sh:73)"
    requirement: "Changement 5"
    verification:
      - kind: unit
        ref: "grep -rn '1\\.8\\.0' plugin/dev-orchestrator plugin/planning-core/scripts --include=*.sh --include=*.md | grep -v CHANGELOG → seules les fixtures de test listées comme hors périmètre subsistent"
        status: pass
      - kind: manual
        ref: "gsd-skills-index.md régénéré via build-gsd-index.sh (jamais édité à la main) — en-tête daté du jour, package @opengsd/gsd-core@1.9.0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Le piège semver D-05 reste écrit noir sur blanc dans check-gsd-engine.sh
      (paquet legacy figé 1.42.3 vs paquet vivant, aujourd'hui 1.9.0) et test-check-gsd-engine.sh
      cas 8 bouge avec le texte sans être affaibli ni neutralisé — prouvé discriminant par
      mutation"
    requirement: "Changement 5"
    verification:
      - kind: unit
        ref: "bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh → 15 ok, 0 ko"
        status: pass
      - kind: mutation
        ref: "mutation ciblée ligne 25 (1.9.0→1.9.9 sur les 2 occurrences) : cas 8 passe rouge
          (142=3 190=0 semver=2) ; restauration vérifiée par diff nul, suite re-verte à 15/15"
        status: pass
    human_judgment: false
  - id: D3
    description: "ADR-062 tranche explicitement le recouvrement entre les 2 hooks 1.9.0 non câblés
      et merge-hooks.sh, chaque hook confronté séparément à son propre contrat d'activation
      documenté et à l'état réel du poste (settings.json, chemin d'install ensure-deps.sh)"
    requirement: "Changement 6"
    verification:
      - kind: unit
        ref: "grep -c '^## ADR-062' docs/ADR.md == 1"
        status: pass
      - kind: manual
        ref: "ADR-062 §Décision cite les lignes exactes des deux en-têtes de hook, la ligne
          ensure-deps.sh:263 (npx classique), le bloc statusLine de settings.json, et
          merge-hooks.sh:145-150 (commentaire préexistant, non modifié) — chaque affirmation
          vérifiée sur pièce avant d'être écrite"
        status: pass
    human_judgment: true

## Contexte

Rituel allégé (arbitrage Samuel, cf. 21-01/21-02) : pas de cadrage `gsd-discuss-phase` séparé — le
digest de mission (nœud `p21-exec-03`) fournit un périmètre exhaustif fondé sur
`.planning/missions/2026-07-31-delta-gsd-core-1.9.0.md`. Dernier plan de la phase : les
Changements 2/3/4 sont livrés par 21-02, le défaut MCP actif par 21-01. Ce plan clôt les deux
reliquats explicitement signalés par 21-02 §Next Phase Readiness.

## Ce qui a été fait (résumé exécutable — détail complet dans le SUMMARY)

1. **Changement 5** : `build-gsd-index.sh` (défaut `GSD_CORE_PACKAGE`) corrigé puis
   `gsd-skills-index.md` régénéré (jamais édité à la main) ; `mission-contracts.md` (2
   occurrences), `check-gsd-engine.sh` (en-tête D-05), `detect-gsd-engine.sh` et `ensure-deps.sh`
   (occurrence supplémentaire trouvée sur pièce, hors énumération du digest) mis à jour vers
   1.9.0. `test-check-gsd-engine.sh` cas 8 déplacé dans le même commit que le texte qu'il vérifie,
   prouvé discriminant par mutation.
2. **Changement 6** : `docs/ADR.md` gagne ADR-062 — chaque hook confronté séparément à son propre
   contrat d'activation (en-tête source) et à l'état réel du poste ; conclusion : absence correcte
   pour les deux, aucune extension de `merge-hooks.sh`. `plugin/dev-orchestrator/README.md` gagne
   un pointeur bref. Index `docs/ADR.md` corrigé au passage (lignes ADR-060/061 manquantes malgré
   leur validation antérieure).

Aucun fichier hors périmètre touché. `.planning/STATE.md`, `.planning/ROADMAP.md` et
`.planning/WINDOWS.md` non modifiés — aucun `gsd-tools state.*` invoqué.

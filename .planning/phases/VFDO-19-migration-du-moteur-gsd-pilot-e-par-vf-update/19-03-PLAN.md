---
phase: 19-migration-du-moteur-gsd-pilotee-par-vf-update
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - plugin/conductor/skills/vf-update/SKILL.md
  - docs/ADR.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/README.md
  - plugin/conductor/CHANGELOG.md
autonomous: true
requirements: [SC2, SC7]

must_haves:
  truths:
    - "SC2 / diagnostic à deux volets (D-07) : l'étape 1 de `vf-update/SKILL.md` consulte l'état du moteur AVANT tout arrêt. Le `stop` sur `update_available = false` n'est plus atteignable tant que la sonde moteur n'a pas été consultée — un poste dont le plugin est à jour mais dont le moteur est legacy poursuit le flux au lieu de s'arrêter sur « VibeFlow est à jour »."
    - "SC2 / message composé : quand le plugin est à jour et le moteur legacy, le skill produit la formulation de `19-CONTEXT.md` §specifics — « plugin à jour (vX.Y.Z), moteur GSD legacy A.B.C → `@opengsd/gsd-core` à migrer » — avec les numéros réellement rapportés par `check-plugin-update.sh --print` et `check-gsd-engine.sh`, jamais des numéros inventés."
    - "SC2 / sonde best-effort (D-00, D-07) : `check-gsd-engine.sh` est cherché dans la cascade `<S>` déjà documentée en tête du skill. Absent → SILENCE TOTAL : aucun message, aucune mention, aucune dégradation du reste du diagnostic. Un lab non-dev (content, growth, business) installe `conductor` sans `dev-orchestrator` et ne doit rien voir."
    - "SC2 / confirmation ADR-031 : la migration du moteur est UNE LIGNE DE PLUS dans l'`AskUserQuestion` existante de l'étape 3, acceptable ou refusable INDÉPENDAMMENT du plugin et des modules. Un refus n'a aucun effet de bord et n'est jamais réessayé ; aucune migration silencieuse n'est possible."
    - "SC2 / bornes des flags : `--check` affiche l'état du moteur comme le reste du diagnostic et ne demande rien ; `--modules-only` ne propose PAS la migration du moteur, son nom bornant son périmètre aux modules. Aucun flag nouveau n'est créé (densité ADR-029)."
    - "SC2 / exécution : quand la ligne moteur est acceptée, le skill invoque `ensure-deps.sh --migrate-engine` (livré en 19-02) et relaie sa sortie. Il ne réimplémente jamais la logique d'install de l'installeur amont (Iron Law 2, `plugin/conductor/AGENT.md:114`)."
    - "SC7 / doctrine réécrite : la phrase du §Garde-fous plaçant la chaîne d'outils interne hors périmètre n'existe plus telle quelle. Elle est remplacée par une formulation disant que l'état du moteur GSD est détecté et proposé — jamais installé sans accord explicite — dans le périmètre de `/vf-update`, Superpowers restant explicitement hors périmètre."
    - "SC7 / ADR : `docs/ADR.md` porte une entrée `ADR-058` actant que la version du moteur GSD est une dépendance gouvernée par VibeFlow (plafond `@^1` décidé en `ensure-deps.sh:166`), donc que son état entre dans le périmètre de `/vf-update`. L'entrée est présente à la fois comme ligne du tableau d'index et comme section complète, au format des entrées voisines (Date, Statut, Décideur, Contexte, Problème, Options, Décision, Conséquences, Code Impacté)."
    - "SC7 / release-meta cohérente : la triade de `dev-orchestrator` vaut v2.7.0 dans ses trois emplacements (fichier `VERSION`, clé `version` de `module.json`, en-tête `**Version**` du README) et celle de `conductor` vaut v1.16.0 dans les siens. Chaque module gagne une entrée de CHANGELOG datée en tête de son historique."
    - "SC7 / gouvernance tenue : `check-agents.sh --strict` reste vert sur les dossiers d'agents du dépôt, la densité ADR-029 est respectée sur le SKILL.md touché (moins de 500 lignes), et les suites du module `conductor` restent vertes."
    - "SC7 / dérive de release NOMMÉE, pas corrigée : le compteur « N suites » des deux README racine passe de 41 à 42 du fait de la suite créée en 19-01. `scripts/check-version-sync.sh` le signale, et c'est attendu : le rattrapage appartient au commit de release humain, comme aux Phases 13 et 17. Le SUMMARY du plan le consigne explicitement comme reste-à-faire."
  artifacts:
    - plugin/conductor/skills/vf-update/SKILL.md
    - docs/ADR.md
    - plugin/dev-orchestrator/VERSION
    - plugin/dev-orchestrator/module.json
    - plugin/dev-orchestrator/README.md
    - plugin/dev-orchestrator/CHANGELOG.md
    - plugin/conductor/VERSION
    - plugin/conductor/module.json
    - plugin/conductor/README.md
    - plugin/conductor/CHANGELOG.md
  key_links:
    - "`vf-update/SKILL.md` → `check-gsd-engine.sh` (19-01) : c'est LE point de couture de la phase. Un skill du module `conductor` (mandatory) appelle un script du module `dev-orchestrator` (non mandatory). Le lien passe par une SONDE DE PRÉSENCE de fichier uniquement — jamais par un `requires` ajouté au `module.json` de conductor, ce qui inverserait la dépendance et casserait la baseline d'un lab non-dev calculée par `plugin/_internal/resolve-deps.sh` depuis `mandatory: true`."
    - "`vf-update/SKILL.md` → `ensure-deps.sh --migrate-engine` (19-02) : le nom du flag est le contrat consommé. Si 19-02 le renomme, ce plan casse silencieusement — la vérification passe par une exécution réelle du flag, pas par une lecture."
    - "`docs/ADR.md` ADR-058 → §Garde-fous du SKILL.md : l'ADR est ce qui autorise la réécriture de la frontière de périmètre. Réécrire la phrase sans poser l'ADR laisserait un changement de doctrine non tracé (le motif que la phase corrige, à l'échelle de la doctrine cette fois)."
    - "Triades de modules → `scripts/check-version-sync.sh` contrôles 6 et 8 : le gate compare `plugin/<mod>/VERSION` à `module.json .version` ET l'en-tête `**Version**` du README du module à son fichier `VERSION`. Une triade incomplète rend le gate rouge sur un contrôle qui n'a rien à voir avec le compteur de suites, et brouillerait le diagnostic différé."
  prohibitions:
    - "P-01 — MUST NOT toucher `VERSION` (racine), `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md` ni `README.fr.md`. La release racine, son tag annoté et sa release GitHub sont réservés à une étape humaine postérieure à la phase, précédent des Phases 13 et 17. `git diff --name-only` de ce plan ne doit mentionner aucun de ces cinq fichiers."
    - "P-02 — MUST NOT corriger le compteur « N suites » des README racine (41 → 42). C'est une dérive RÉELLE, attendue, et différée : elle se corrige dans le commit de release, avec le bump racine, jamais dans un plan de phase. Elle doit en revanche être NOMMÉE dans le SUMMARY — la laisser sous silence serait la faute."
    - "P-03 — MUST NOT créer de tag git ni de release GitHub, ni exécuter `scripts/bump.sh` sur la racine."
    - "P-04 — MUST NOT ajouter `dev-orchestrator` aux `requires` de `plugin/conductor/module.json`. La détection cross-module passe par une sonde de présence de fichier et par rien d'autre (D-00)."
    - "P-05 — MUST NOT créer de flag nouveau sur `/vf-update` (pas de `--engine-only`), ni de hook `SessionStart` sur l'état du moteur. Hors périmètre décidé, ROADMAP §Phase 19 et `19-CONTEXT.md`."
    - "P-06 — MUST NOT étendre le périmètre à Superpowers. La phase ne touche qu'au moteur GSD ; la nouvelle formulation du §Garde-fous doit continuer à placer Superpowers hors périmètre."
    - "P-07 — MUST NOT rendre la migration du moteur automatique, ni la lier au même OUI que le plugin ou les modules. Elle est acceptable ou refusable séparément, et un refus ne produit aucun effet de bord (ADR-031)."
---

<objective>
Brancher l'état du moteur sur le chemin de mise à jour nominal, et acter le changement de doctrine
qui l'autorise : `/vf-update` consulte le moteur avant tout arrêt, propose la migration comme une
ligne de plus dans sa confirmation existante, réécrit la phrase de périmètre devenue fausse, pose
l'ADR correspondant, et bumpe proprement les deux modules touchés.

Purpose: c'est le plan qui décide si la phase corrige quoi que ce soit. Sur le poste audité, le
plugin était déjà à jour — l'étape 1 du skill s'arrêtait net sur « VibeFlow est à jour » et la
détection du moteur n'aurait JAMAIS été atteinte, quelle que soit la qualité du gate écrit en 19-01
et du chemin de migration écrit en 19-02. Le geste central est donc l'ordre des opérations dans
l'étape 1, pas le texte.

Output: 1 SKILL.md réécrit sur 4 zones + 1 ADR créé + release-meta de 2 modules (8 fichiers).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-PATTERNS.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-01-SUMMARY.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@plugin/conductor/skills/vf-update/SKILL.md
@plugin/conductor/scripts/check-plugin-update.sh
@docs/ADR.md
@CLAUDE.md
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| Étape 1 « Diagnostic » de `vf-update/SKILL.md` | section réécrite | deux volets : version du plugin ET état du moteur, moteur consulté AVANT tout `stop` |
| sonde `check-gsd-engine.sh` dans `<S>` | mécanique réutilisée | cascade déjà documentée en tête du skill ; absent → silence total |
| ligne moteur de l'`AskUserQuestion` | élément de confirmation (nouveau) | acceptable/refusable indépendamment ; ADR-031 |
| Étape « 4c — Couche moteur » | sous-étape (nouvelle) | invoque `ensure-deps.sh --migrate-engine`, relaie sa sortie |
| §Garde-fous, dernier tiret | phrase réécrite | le moteur GSD entre dans le périmètre ; Superpowers reste hors périmètre |
| `ADR-058` | entrée d'ADR (nouvelle) | ligne du tableau d'index + section complète dans `docs/ADR.md` |
| `plugin/dev-orchestrator` v2.7.0 | triade + CHANGELOG | mineur : nouveau script, nouvelle suite, nouveau chemin, nouveau mode |
| `plugin/conductor` v1.16.0 | triade + CHANGELOG | mineur : le skill `vf-update` gagne une capacité |
| Reste-à-faire « 41 → 42 suites » | différé documenté | consigné dans le SUMMARY, jamais corrigé ici |

## Détecteurs `plan:pre` non déclenchés

`ai-integration`, `assumption-delta` et `schema-gate` ne s'appliquent pas : aucune intégration
d'API/SDK externe, aucune transition d'identité singulier→pluriel ni requis→optionnel, aucun schéma
ni ORM touché. Le plan est doc + doctrine + release-meta.

<tasks>

<task type="auto">
  <name>Task 1 : `vf-update/SKILL.md` — diagnostic à deux volets, ligne moteur dans la confirmation, bornes des flags, §Garde-fous réécrit (D-07)</name>
  <precondition>`plugin/dev-orchestrator/scripts/check-gsd-engine.sh` existe (livré par 19-01) et `plugin/dev-orchestrator/scripts/ensure-deps.sh` accepte `--migrate-engine` (livré par 19-02) : les deux contrats consommés par cette tâche sont vérifiables par exécution réelle, pas par lecture.</precondition>
  <reversibility rating="costly">Le message et le contrat d'arrêt du skill sont ce que les
  utilisateurs lisent. Revenir en arrière après publication recréerait l'écart « exact et trompeur à
  la fois » décrit par le rapport d'audit. Décision verrouillée par le cadrage.</reversibility>
  <read_first>
    - `plugin/conductor/skills/vf-update/SKILL.md` **en entier** (82 lignes) — le frontmatter et sa `description`, `:22-26` (résolution `<S>`, à réutiliser sans nouvelle mécanique), `:30-36` (étape 1 et son `stop` fautif), `:46-52` (étape 3 et les deux flags), `:54-66` (étapes 4a/4b), `:68-72` (étape 5), `:74-81` (§Garde-fous et la phrase devenue fausse).
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-00 (pourquoi la sonde traverse la dépendance par présence de fichier), D-07 (le point de décision de toute la phase) et §specifics (la formulation attendue de la ligne moteur, à reprendre telle quelle).
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-01-SUMMARY.md` — le contrat de sortie réel du gate : préfixes de signal et sémantique des codes 0/2/3, y compris le sous-cas d'exit 3 qui n'est PAS silencieux.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-02-SUMMARY.md` — le nom exact du flag de migration et de sa variable d'environnement équivalente.
    - `plugin/dev-orchestrator/scripts/ensure-deps.sh:248-254` — le patron best-effort de référence : un outil absent rend le flux silencieux, jamais dégradé.
    - `plugin/conductor/AGENT.md:114` — Iron Law 2, « Router, jamais réimplémenter ».
  </read_first>
  <files>plugin/conductor/skills/vf-update/SKILL.md</files>
  <action>
Réécrire l'étape 1 en diagnostic à DEUX VOLETS. Le volet plugin reste identique : appel de
`check-plugin-update.sh --print`, lecture du JSON, et arrêt sur réseau indisponible inchangé. Le
volet moteur s'insère AVANT le `stop` sur absence de mise à jour du plugin — c'est l'unique
correction structurelle du plan, tout le reste en découle. Le skill localise
`check-gsd-engine.sh` dans la cascade `<S>` déjà documentée en tête du fichier ; pour la troisième
position seulement, celle du plugin non installé, le chemin correct est le dossier `scripts/` du
module `dev-orchestrator`, puisque la matérialisation à plat dans un même `.claude/scripts/`
n'existe que dans un lab installé. Ce n'est pas un élargissement du cadrage : c'est la conséquence
mécanique de D-00, à noter comme telle en commentaire de la ligne.

Écrire le comportement de la sonde en trois branches, exactement : script introuvable → SILENCE
TOTAL, aucune ligne, aucune mention, le diagnostic continue comme avant ; code de sortie du gate
correspondant à l'état actionnable → le skill compose la ligne moteur et le flux CONTINUE, même si
le plugin est à jour ; code INDÉTERMINÉ → si le gate a imprimé quelque chose (le sous-cas reliquat
legacy), l'afficher dans le diagnostic sans jamais proposer de migration ; s'il n'a rien imprimé,
ne rien dire. Une erreur d'usage du gate est traitée comme une absence : silence.

Reformuler la ligne d'arrêt : « VibeFlow est à jour » ne peut plus sortir seule quand le moteur est
legacy. Le message combiné à produire est celui de `19-CONTEXT.md` §specifics — plugin à jour avec
sa version, puis moteur GSD legacy avec sa version, la flèche vers le nom du paquet cible, et la
mention qu'il est à migrer. Les numéros viennent des sorties réelles des deux scripts, jamais de
valeurs recopiées depuis le cadrage.

Étape 3 : ajouter UNE ligne au récapitulatif de l'`AskUserQuestion` existante, portant la migration
du moteur. Écrire explicitement qu'elle est acceptable ou refusable indépendamment de la ligne
plugin et de la ligne modules, qu'un refus ne produit aucun effet de bord, et qu'il n'est jamais
réessayé ni reformulé dans la même session. Préciser le comportement des deux flags existants sans
en créer de nouveau : le mode d'affichage seul montre l'état du moteur comme le reste du diagnostic
et ne demande rien ; le mode borné aux modules ne propose pas la migration du moteur, son nom
bornant son périmètre.

Étape 4 : ajouter une sous-étape « couche moteur », après la couche modules, invoquant
`ensure-deps.sh --migrate-engine` depuis `<S>` et relayant son résumé. Écrire qu'elle ne s'exécute
que si la ligne moteur a été acceptée, qu'elle s'exécute même si la couche plugin a échoué (les
confirmations sont indépendantes), et que le skill n'invoque JAMAIS l'installeur amont directement —
il route vers `ensure-deps.sh`, point de vérité unique du scope et du plafond de version (Iron Law
2). Compléter le rappel de redémarrage de l'étape 5 : une migration du moteur pose de nouveaux
agents et skills, qui ne sont pris en compte qu'au prochain démarrage de session.

§Garde-fous : réécrire le dernier tiret. La phrase actuelle plaçant la chaîne d'outils interne hors
périmètre au motif qu'elle a sa propre mise à jour est devenue fausse pour la moitié GSD, puisque
la version du moteur est décidée par VibeFlow via le plafond posé dans `ensure-deps.sh`. La nouvelle
formulation dit que le périmètre couvre le plugin, ses modules, ET l'état du moteur GSD — détecté et
proposé, jamais installé sans accord explicite — et que Superpowers reste hors périmètre. Y renvoyer
l'ADR posé en Task 2.

Frontmatter : compléter la `description` d'une courte clause nommant l'état du moteur GSD, sans
toucher aux trois exclusions de routage `✘` existantes ni à la phrase d'invocabilité finale, qui
sont des contrats de routage à préserver mot pour mot.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/check-gsd-engine.sh --help >/dev/null 2>&1; a=$?; bash plugin/dev-orchestrator/scripts/ensure-deps.sh --help >/dev/null 2>&1; b=$?; n=$(wc -l < plugin/conductor/skills/vf-update/SKILL.md); echo "help-gate=$a help-ensure=$b lignes=$n"; test "$a" -eq 0 -a "$b" -eq 0 -a "$n" -le 500 && grep -q 'check-gsd-engine.sh' plugin/conductor/skills/vf-update/SKILL.md && grep -q -- '--migrate-engine' plugin/conductor/skills/vf-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Ordre des opérations (le cœur de D-07) : dans `plugin/conductor/skills/vf-update/SKILL.md`, le numéro de la première ligne mentionnant `check-gsd-engine.sh` est INFÉRIEUR au numéro de la ligne portant le `stop` sur absence de mise à jour du plugin. Vérifié par deux `grep -n` comparés. Une inversion de ces deux numéros rouvre exactement le trou audité.
    - La phrase devenue fausse a disparu : `grep -c 'hors périmètre de ce skill' plugin/conductor/skills/vf-update/SKILL.md` retourne `0`.
    - Superpowers reste explicitement hors périmètre : `grep -ci 'superpowers' plugin/conductor/skills/vf-update/SKILL.md` retourne une valeur ≥ 1, sur une ligne du §Garde-fous qui le place hors périmètre.
    - Le §Garde-fous renvoie à l'ADR posé en Task 2 : `grep -c 'ADR-058' plugin/conductor/skills/vf-update/SKILL.md` retourne une valeur ≥ 1.
    - Silence total documenté : le fichier contient une formulation explicite disant que l'absence du script de sonde ne produit aucun message. Un `grep -i` sur la racine `silence` retourne une valeur ≥ 1 dans la section de l'étape 1.
    - Confirmation indépendante : le fichier contient, à l'étape 3, une formulation explicite disant que la ligne moteur est acceptable ou refusable indépendamment, et qu'un refus n'a aucun effet de bord.
    - Bornes des flags : le fichier dit explicitement que le mode borné aux modules ne propose pas la migration du moteur. Aucun flag nouveau n'apparaît — `grep -c -- '--engine-only' plugin/conductor/skills/vf-update/SKILL.md` retourne `0`.
    - Chaînage d'exécution : `grep -c -- '--migrate-engine' plugin/conductor/skills/vf-update/SKILL.md` retourne une valeur ≥ 1, et le fichier ne contient aucune invocation directe de l'installeur amont — `grep -c 'npx' plugin/conductor/skills/vf-update/SKILL.md` retourne `0` (Iron Law 2).
    - Non-régression du routage : les trois exclusions `✘` de la `description` du frontmatter sont présentes mot pour mot, et la phrase finale d'invocabilité est inchangée. Vérifié par `git diff` sur le bloc de frontmatter.
    - Densité ADR-029 : `wc -l < plugin/conductor/skills/vf-update/SKILL.md` retourne une valeur ≤ 500.
    - Sonde exécutable : `bash plugin/dev-orchestrator/scripts/check-gsd-engine.sh --help` retourne `0` et `bash plugin/dev-orchestrator/scripts/ensure-deps.sh --help` retourne `0` — les deux contrats consommés existent réellement, ils ne sont pas supposés.
    - Répétition du chemin nominal : sur une fixture `HOME` factice portant uniquement le `VERSION` legacy, `bash plugin/dev-orchestrator/scripts/check-gsd-engine.sh` retourne `0` et imprime la ligne que le skill est censé composer — la formulation écrite dans le SKILL.md est alignée sur cette sortie réelle, pas sur une sortie supposée.
  </acceptance_criteria>
  <done>Un poste plugin-à-jour et moteur-legacy ne peut plus s'arrêter sur « VibeFlow est à jour » ; la migration est une ligne de confirmation indépendante ; le §Garde-fous ne ment plus.</done>
</task>

<task type="auto">
  <name>Task 2 : ADR-058 — le moteur GSD entre dans le périmètre de `/vf-update` (D-10)</name>
  <reversibility rating="costly">Un ADR publié est une doctrine citée par les scripts et les
  skills. Le retirer après diffusion laisserait des renvois orphelins dans le §Garde-fous du
  SKILL.md et dans les en-têtes de scripts.</reversibility>
  <read_first>
    - `docs/ADR.md:15-30` — le tableau d'index des ADR, dont la dernière ligne est celle d'ADR-057 : le format exact d'une ligne (identifiant, date, titre, statut).
    - `docs/ADR.md:770-853` — l'entrée ADR-057 **en entier** : c'est le gabarit de section à répliquer (titre, Date, Statut, Décideur, Contexte, §Problème, §Options Considérées en tableau avec verdicts, §Décision numérotée, §Conséquences avec positives et négatives/risque, §Code Impacté, §Rules Associées).
    - `docs/ADR.md` ADR-031 (jamais d'action sans validation humaine) et ADR-055 (frontière d'altitude) — les deux doctrines que le nouvel ADR articule.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-10 (le contenu attendu de l'ADR) et D-07 (la phrase devenue fausse).
    - `.planning/ROADMAP.md` §Phase 19, bloc « Doctrine à réviser ».
  </read_first>
  <files>docs/ADR.md</files>
  <action>
Ajouter l'entrée `ADR-058` à `docs/ADR.md`, en deux endroits : une ligne dans le tableau d'index, à
la suite immédiate de celle d'ADR-057, au même format ; et une section complète en fin de fichier,
précédée du séparateur horizontal utilisé entre les entrées.

Le numéro `ADR-058` est le premier libre — vérifier ce fait avant d'écrire, pas après : la
renumérotation d'un ADR déjà cité est un incident connu de ce dépôt (ADR-055 a dû être renuméroté
au merge parce que le numéro était déjà pris par une release concurrente).

Titre : le moteur GSD entre dans le périmètre de `/vf-update`.

§Problème : la version du moteur GSD n'est pas une donnée subie, c'est une décision de VibeFlow —
le plafond de version est posé dans `ensure-deps.sh:166` et a été arbitré après l'audit de la
Phase 11. Or le §Garde-fous du skill de mise à jour plaçait explicitement la chaîne d'outils interne
hors périmètre, au motif qu'elle avait sa propre mise à jour. Conséquence mesurée sur un poste
audité le 2026-07-28 : plugin à jour, moteur resté sur le paquet déprécié posé douze jours plus tôt,
et rien dans l'interface ne le disait. Une frontière de périmètre exacte dans sa formulation et
trompeuse dans son effet.

§Options Considérées, en tableau avec verdict, au moins quatre lignes : laisser le moteur hors
périmètre et compter sur `/vf-init` ou `/vf-calibrate` (rejetée — ce sont des chemins que le régime
nominal n'emprunte jamais, c'est précisément le motif du trou) ; ajouter un hook de démarrage sur
l'état du moteur (rejetée — décision de l'utilisateur, une ligne de plus à chaque session pour un
fait qui change une fois) ; migrer automatiquement dès détection (rejetée — ADR-031, une install
tierce sur le poste de l'utilisateur ne se fait jamais sans accord) ; détecter et proposer dans le
récapitulatif de `/vf-update`, sous confirmation (retenue).

§Décision, en points numérotés : l'état du moteur est une donnée du diagnostic de `/vf-update` au
même titre que la version du plugin et celle des modules ; il est détecté par un gate dédié en
lecture seule, dont le classement se fait sur le layout et le nom du paquet et jamais sur la
comparaison des numéros ; la migration est proposée comme une ligne indépendante de la confirmation
existante et n'est jamais exécutée sans accord explicite ; la détection traverse la frontière de
modules par une sonde de présence de fichier, jamais par une dépendance déclarée, parce que le
module qui porte le skill est mandatory et que celui qui porte le moteur ne l'est pas ; le nettoyage
des artefacts legacy est proposé, jamais exécuté ; Superpowers reste hors périmètre.

§Conséquences : côté positif, le chemin de mise à jour nominal voit enfin l'état du moteur et un
poste équipé cesse de porter le code d'une migration sans en porter l'effet ; le nettoyage devient
atteignable et exact. Côté négatif et risque, à écrire honnêtement : `/vf-update` acquiert une
dépendance de fait envers un script d'un module non mandatory, tenue par une sonde best-effort — si
la matérialisation à plat des scripts changeait un jour, la sonde deviendrait silencieusement
aveugle ; et le classement par layout doit être maintenu insensible aux numéros, faute de quoi le
piège semver se rouvre sans signal.

§Code Impacté : les fichiers réellement touchés par la phase, cités par chemin. §Rules Associées :
articulation avec ADR-031 (jamais d'action sans validation humaine, qui borne la proposition) et
ADR-055 §3 (le script constate, l'agent juge et propose). Aucune rule nouvelle.

Le texte est en français, comme tout le fichier.
  </action>
  <verify>
    <automated>grep -c 'ADR-058' docs/ADR.md; grep -q '| ADR-058 |' docs/ADR.md && grep -q '^## ADR-058' docs/ADR.md && grep -q 'ADR-058' plugin/conductor/skills/vf-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Le numéro est libre avant écriture : `grep -c 'ADR-058' docs/ADR.md` retourne `0` AVANT la modification, et une valeur ≥ 2 après.
    - Le tableau d'index porte une ligne `| ADR-058 | <date> | <titre> | <statut> |` immédiatement après celle d'ADR-057.
    - Le corps du fichier porte une section commençant par `## ADR-058` et contenant les six intitulés du gabarit voisin : `**Date**`, `**Statut**`, `**Décideur**`, `### Problème`, `### Décision`, `### Conséquences`, `### Code Impacté`.
    - La §Décision compte au moins quatre points numérotés et cite nommément la confirmation humaine (ADR-031) et l'interdiction de classer sur les numéros de version.
    - La §Conséquences comporte une partie négative ou risque non vide — un ADR sans contrepartie déclarée est un ADR de complaisance.
    - La §Code Impacté cite au minimum `plugin/dev-orchestrator/scripts/check-gsd-engine.sh`, `plugin/dev-orchestrator/scripts/ensure-deps.sh`, `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` et `plugin/conductor/skills/vf-update/SKILL.md`.
    - Le renvoi croisé existe : `grep -c 'ADR-058' plugin/conductor/skills/vf-update/SKILL.md` retourne une valeur ≥ 1 (posé en Task 1).
    - Aucune entrée d'ADR existante n'est modifiée : `git diff docs/ADR.md` ne montre que des ajouts, sauf l'insertion de la ligne d'index.
  </acceptance_criteria>
  <done>Le changement de doctrine est tracé par un ADR complet et référencé, avec ses contreparties écrites.</done>
</task>

<task type="auto">
  <name>Task 3 : release-meta des deux modules touchés + différé de release nommé (D-10)</name>
  <reversibility rating="reversible">Un bump de module est un jeu de valeurs cohérentes ; le
  ramener en arrière est mécanique tant que la release racine n'est pas partie.</reversibility>
  <read_first>
    - `CLAUDE.md` racine §« Discipline de release » — la numérotation mineur/patch et ce qui appartient au commit de release humain (bump racine, tag annoté, release GitHub) et donc PAS à ce plan.
    - `.planning/codebase/CONVENTIONS.md` §Discipline de release — la liste des 7 contrôles de `scripts/check-version-sync.sh`, dont la triade par module et l'en-tête `**Version**` des README de modules.
    - `scripts/check-version-sync.sh` — les contrôles 6 (triade `VERSION` ↔ `module.json`), 8 (en-tête `**Version**` du README de module) et **9 (compteur « N suites » des README racine)** : ce dernier est celui qui deviendra rouge du fait de la suite créée en 19-01, et qu'il ne faut PAS corriger ici.
    - `plugin/dev-orchestrator/CHANGELOG.md:1-20` et `plugin/conductor/CHANGELOG.md:1-20` — le format exact d'une entrée en tête d'historique (titre versionné, date, thème entre parenthèses, sous-sections `### Ajouté` / `### Corrigé`).
    - `plugin/dev-orchestrator/README.md:10` et `plugin/conductor/README.md:9` — les deux formes d'en-tête `**Version**` (ligne dédiée pour l'un, inline dans une ligne de méta pour l'autre) : ne pas uniformiser, seulement mettre à jour la valeur.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-10.
  </read_first>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/CHANGELOG.md, plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md, plugin/conductor/CHANGELOG.md</files>
  <action>
Bumper les deux modules touchés en MINEUR, chacun avec sa triade complète et son entrée de
CHANGELOG datée. `dev-orchestrator` passe de v2.6.0 à v2.7.0 : nouveau script de gate, nouvelle
suite dédiée, nouveau chemin de migration, nouveau mode de vérification — ce sont des capacités
nouvelles, pas des correctifs. `conductor` passe de v1.15.0 à v1.16.0 : le skill de mise à jour
gagne une capacité, il ne corrige pas un défaut de configuration.

Pour chaque module, les trois emplacements de la triade doivent porter la même valeur : le fichier
`VERSION`, la clé de version du `module.json`, et l'en-tête `**Version**` du README du module. Ces
deux derniers sont vérifiés par machine, respectivement par les contrôles 6 et 8 de
`scripts/check-version-sync.sh` ; une triade incomplète rend le gate rouge sur un contrôle sans
rapport avec le différé assumé, ce qui brouillerait le diagnostic.

Écrire une entrée de CHANGELOG en tête de l'historique de chaque module, au format des entrées
voisines, avec la date du jour et le thème de la phase. Côté `dev-orchestrator`, décrire le gate de
détection à trois états et son contrat de sortie, sa suite dédiée, le chemin de migration et son
chaînage sur la ré-injection MCP, le mode de vérification, et la correction du message de nettoyage
— en nommant le fait mesuré qui les motive plutôt qu'en survendant. Côté `conductor`, décrire le
diagnostic à deux volets, la ligne de confirmation indépendante, les bornes des deux flags
existants, et la réécriture de la frontière de périmètre avec le renvoi vers l'ADR.

Ne toucher à RIEN d'autre. La `VERSION` racine, le manifeste du plugin, la fiche marketplace et les
deux README racine restent tels quels : la release racine, son tag annoté et sa release GitHub sont
une étape humaine postérieure, exactement comme aux Phases 13 et 17.

Enfin, exécuter le gate de synchronisation des versions et documenter sa sortie dans le SUMMARY. Il
sera ROUGE, et c'est attendu : la suite créée en 19-01 fait passer le nombre réel de suites de 41 à
42, alors que les deux README racine annoncent toujours 41. C'est le contrôle 9 qui le signale. Ce
rattrapage appartient au commit de release humain — le corriger ici reviendrait à toucher les README
racine, ce que le plan interdit. Le consigner comme reste-à-faire nommé est en revanche obligatoire :
c'est la différence entre un différé assumé et un oubli.

Vérifier pour finir que la gouvernance reste tenue : le gate des agents natifs passe sur les
dossiers d'agents du dépôt, et les suites du module `conductor` restent vertes malgré la retouche de
la description du skill.
  </action>
  <verify>
    <automated>out="$(bash scripts/check-version-sync.sh 2>&1)"; printf '%s\n' "$out"; ko_total=$(printf '%s\n' "$out" | grep -c '✗'); ko_suites=$(printf '%s\n' "$out" | grep '✗' | grep -c 'suites'); echo "ko_total=$ko_total ko_suites=$ko_suites"; test "$ko_total" -eq "$ko_suites"</automated>
  </verify>
  <acceptance_criteria>
    - Triade `dev-orchestrator` : `cat plugin/dev-orchestrator/VERSION` retourne `v2.7.0` ; la clé de version de `plugin/dev-orchestrator/module.json` vaut `v2.7.0` ; `grep '\*\*Version\*\*' plugin/dev-orchestrator/README.md` retourne une ligne contenant `v2.7.0`.
    - Triade `conductor` : `cat plugin/conductor/VERSION` retourne `v1.16.0` ; la clé de version de `plugin/conductor/module.json` vaut `v1.16.0` ; `grep '\*\*Version\*\*' plugin/conductor/README.md` retourne une ligne contenant `v1.16.0`.
    - CHANGELOG : `head -3 plugin/dev-orchestrator/CHANGELOG.md` contient `v2.7.0` et une date au format des entrées voisines ; `head -3 plugin/conductor/CHANGELOG.md` contient `v1.16.0` et une date.
    - Gate de synchronisation : `bash scripts/check-version-sync.sh` est exécuté et sa sortie est capturée. Le nombre total de lignes en échec est EXACTEMENT égal au nombre de lignes en échec mentionnant le compteur de suites — autrement dit, le seul contrôle rouge est le contrôle 9, et les triades comme les en-têtes de README de modules sont verts. Toute autre ligne rouge bloque la tâche.
    - Différé nommé : le SUMMARY du plan contient une section explicite consignant le reste-à-faire « compteur de suites des README racine à passer de 41 à 42, à faire dans le commit de release humain avec le bump racine, le tag annoté et la release GitHub ».
    - Intouchabilité de la racine : `git diff --name-only` ne mentionne ni `VERSION`, ni `plugin/.claude-plugin/plugin.json`, ni `.claude-plugin/marketplace.json`, ni `README.md`, ni `README.fr.md`. Aucun tag n'est créé — `git tag --points-at HEAD` reste vide.
    - Gouvernance : `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/dev-orchestrator/agents` retourne `0`.
    - Suites du module modifié : la boucle `find plugin/conductor -type f -path '*/tests/test-*.sh' | sort` exécute chaque suite et aucune ne retourne un code non nul.
    - Le nombre réel de suites vaut 42 : `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` retourne `42`.
  </acceptance_criteria>
  <done>Les deux triades sont cohérentes, les deux CHANGELOG sont à jour, le seul contrôle rouge du gate est le compteur de suites, et ce reste-à-faire est nommé au lieu d'être corrigé ou tu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sortie d'un script de sonde → contexte de la session de l'agent | Le texte produit par `check-gsd-engine.sh` est lu par le LLM et devient une proposition d'action |
| frontière de modules `conductor` (mandatory) → `dev-orchestrator` (non mandatory) | Un skill du socle appelle un script d'un module qui peut être absent |
| doctrine écrite (§Garde-fous, ADR) → comportement futur des agents | Une frontière de périmètre fausse produit une inaction silencieuse — le défaut même que la phase corrige |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03-01 | Elevation of Privilege | migration du moteur déclenchée par le skill sans accord explicite | high | mitigate | La migration est une ligne indépendante de l'`AskUserQuestion` existante ; refus sans effet de bord ni relance (ADR-031, P-07). Le mode borné aux modules ne la propose pas du tout, le mode d'affichage seul ne demande rien. |
| T-19-03-02 | Denial of Service | un lab non-dev voit un message ou une erreur pour un script qu'il n'a pas | high | mitigate | Sonde best-effort par présence de fichier : absent → silence total, aucune dégradation du reste du diagnostic (D-00/D-07). Aucun `requires` ajouté au `module.json` de conductor, ce qui casserait la baseline transitive d'un lab non-dev (P-04). |
| T-19-03-03 | Repudiation | changement de doctrine appliqué sans trace | medium | mitigate | ADR-058 créé et référencé depuis le §Garde-fous réécrit ; renvoi croisé asserté par grep. |
| T-19-03-04 | Tampering | contenu non maîtrisé remonté par les scripts et recopié tel quel dans la proposition | medium | mitigate | Les valeurs affichées viennent de sorties déjà assainies en amont (19-01 borne et valide le contenu des fichiers `VERSION`) ; le skill relaie, il ne réinterprète pas. |
| T-19-03-05 | Tampering | release racine partie sans tag, ou tag posé par un agent | high | mitigate | Interdiction explicite (P-01, P-03) : ni bump racine, ni tag, ni release GitHub dans ce plan. Asserté par `git diff --name-only` et par l'absence de tag sur HEAD. La discipline de release de `CLAUDE.md` reste une étape humaine. |
| T-19-03-06 | Information Disclosure | version du poste et chemins locaux affichés dans le récapitulatif | low | accept | Sortie locale destinée au propriétaire du poste ; aucune transmission. |
</threat_model>

<verification>
- Ordre des opérations de l'étape 1 vérifié par comparaison de deux numéros de ligne (`grep -n`) :
  la sonde moteur précède le `stop` sur plugin à jour.
- `grep -c 'hors périmètre de ce skill' plugin/conductor/skills/vf-update/SKILL.md` → `0`.
- `grep -c 'ADR-058' docs/ADR.md` → ≥ 2 ; `grep -c 'ADR-058' plugin/conductor/skills/vf-update/SKILL.md` → ≥ 1.
- `wc -l < plugin/conductor/skills/vf-update/SKILL.md` → ≤ 500 (ADR-029).
- `bash scripts/check-version-sync.sh` → seules les lignes en échec sont celles du compteur de
  suites ; triades et en-têtes de README de modules verts.
- `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/dev-orchestrator/agents` → exit 0.
- Suites du module `conductor` → toutes vertes.
- `git diff --name-only` ne mentionne que les dix fichiers de `files_modified` ; `git tag --points-at HEAD` est vide.
</verification>

<success_criteria>
- SC2 tenu : `/vf-update` dit l'état du moteur dans le même récapitulatif que le plugin et les
  modules, propose la migration comme une ligne de plus dans la confirmation existante, accepte le
  refus sans effet de bord, et ne migre jamais en silence.
- SC7 tenu pour sa part « doctrine et gouvernance » : ADR-058 créé, §Garde-fous réécrit et cessant
  d'être faux, `check-agents.sh` vert, densité ADR-029 respectée, deux triades de modules cohérentes.
- Reste-à-faire explicitement différé, non silencieux : bump de la `VERSION` racine, synchronisation
  du compteur de suites (41 → 42) dans les deux README racine, tag annoté poussé, release GitHub,
  puis `scripts/check-release-tag.sh --remote` vert. Réservé à validation humaine, hors de ce plan.
</success_criteria>

<output>
Create `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-03-SUMMARY.md` when done
</output>
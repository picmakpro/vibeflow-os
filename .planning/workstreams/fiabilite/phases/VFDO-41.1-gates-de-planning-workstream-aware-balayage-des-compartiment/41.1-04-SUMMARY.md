---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 04
subsystem: dev-orchestrator
tags: [shell, workstreams, hooks, mutation-testing, no-silent-degradation]

# Dependency graph
requires: [41.1-01]   # vague 2 — consomme vf_ws_enumerate
affects: []
provides: "discover-unintegrated-docs.sh — registre de citation élargi à CHAQUE compartiment
  .planning/workstreams/<nom>/ énuméré par vf_ws_enumerate, plus le repli racine et docs/ADR.md
  hors boucle ; contrat de sortie 0/3/64 et règle de citation inchangés"

# Estimate vs actuals
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: medium
actuals:
  # tokens : NON MESURABLE côté exécutant — aucun compteur fiable, aucun nombre inventé.
  tasks: 2
  commits: 3   # 2 de code/test + ce SUMMARY

tech-stack:
  added: []
  patterns:
    - "Énumération vers un FICHIER TEMPORAIRE (`> \"$_WS_TMP\"`), jamais une substitution de
      commande ou de processus — elle perd le rc du pipeline par construction"
    - "`case` sur le rc capturé avec branche `*)` NOMMÉE, stderr LAISSÉ PASSER : patron
      strictement identique aux plans 41.1-02 et 41.1-03"
    - "Repli racine délibérément NON conditionnel (pas un `elif`) — les deux univers sont
      concaténés dans le même buffer, une citation dans l'un OU l'autre suffit"
    - "Mutant exécuté depuis un répertoire PLAT contenant une copie de workstream-policy.sh
      (premier candidat de la recherche de sourcing) — le témoin non muté et le mutant ne
      diffèrent QUE par la mutation, jamais par la résolution du sourcing"
    - "Nom de variable temporaire en *TMP* IMPOSÉ par l'invariant T21b (SC5) de
      test-dev-orchestrator.sh, pas par style"

key-files:
  created: []
  modified:
    - plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
    - plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh

key-decisions:
  - "DIVERGENCE PLAN CONSIGNÉE (demandée par le mandat) : le `must_have` n°2 décrit le nouveau
    registre comme « les quatre fichiers par compartiment PLUS le `PROJECT.md` racine », tandis
    que l'`<action>` de la Task 1 prescrit de CONSERVER TOUT le repli racine (ROADMAP,
    REQUIREMENTS, MILESTONES, PROJECT, `milestones/*.md`), justifié par le cas « dépôt
    partiellement partitionné ». L'`<action>` a été suivie — c'est le texte opérationnel, et son
    univers est un SUR-ENSEMBLE de celui du `must_have` : il ne peut donc produire AUCUN faux
    positif supplémentaire (un registre plus large ne fait que marquer davantage de documents
    « intégrés »). Les deux énoncés divergent malgré tout et la divergence est consignée ici
    plutôt que laissée muette."
  - "Trailers `Gate-Touche` POSÉS bien que les deux fichiers soient HORS de la surface G-2 —
    exigence de traçabilité du `must_have` n°1 du plan, et non du gate. Surface RE-MESURÉE
    par lecture de `classify` (`scripts/check-gate-touche.sh` l.206-214 : les classes sont
    `plugin/conductor/scripts/check-*.sh`, `scripts/check-*.sh`, leurs `tests/test-*.sh`,
    `.github/workflows/ci.yml`, `scripts/hooks/`) : `plugin/dev-orchestrator/scripts/**` n'en
    fait pas partie. Mesure après commits : rc=0, `chemins_surface=4
    marqueurs_conformes=11`, `DECLARE` — `chemins_surface` INCHANGÉ (4 avant), seul le compte
    de marqueurs conformes monte de 9 à 11. La raison retenue est celle CORRIGÉE par le juge
    frais (appartenance au recensement des consommateurs de planning), jamais « c'est un hook »."
  - "Les annonces de dégradation ne sont PAS filtrées par `--quiet` (elles n'utilisent pas
    `say()`). Aucun conflit avec le cas 16 de la suite, qui exige un `--quiet` totalement
    silencieux : sa fixture n'a pas de `workstreams/`, donc le rc est 3 — SILENCE légitime, aucune
    ligne émise. Mesuré : cas 16 vert."
  - "AUCUN CHANGELOG ni VERSION de module touché — le bump de `dev-orchestrator` revient au plan
    41.1-08, en fin de phase (même décision que les plans 41.1-01 à 41.1-03)."

requirements-completed: []   # WSAW-02 couvre les trois consommateurs de catégorie (c) ; ce plan
  # ferme le TROISIÈME et dernier (les deux autres par 41.1-03). La fermeture formelle du
  # requirement est portée par le plan de clôture de phase.

coverage:
  - id: D1
    description: "Registre de citation élargi : concaténation de
      `<compartiment>/{ROADMAP,REQUIREMENTS,MILESTONES,PROJECT}.md` + `<compartiment>/milestones/*.md`
      pour CHAQUE compartiment énuméré par `vf_ws_enumerate`, plus le repli racine conservé et
      `docs/ADR.md` lu une seule fois hors boucle. `.planning/phases/**` et
      `.planning/workstreams/<nom>/phases/**` restent exclus (lecture au premier niveau du
      compartiment seulement, jamais récursive)"
    requirement: WSAW-02
    verification:
      - kind: unit
        ref: "3 blocs `<automated>` de la Task 1 rejoués verbatim : `bash -n` (« syntaxe OK ») ;
          `cite_dans_compartiment` rc=3, aucune occurrence de `x.md` (MESURÉ rc=0 avec
          `spec\\tdocs/superpowers/specs/x.md` AVANT le plan — le faux positif D-04) ;
          `non_cite` rc=0 avec `orphan.md` signalé"
        status: pass
    human_judgment: false
  - id: D2
    description: "AUCUNE DÉGRADATION SILENCIEUSE (C-06) : rc de `vf_ws_enumerate` capturé via
      fichier temporaire, stderr laissé passer, `case` à quatre branches dont `*)` nommée ; les
      quatre chemins de dégradation (rc=2, code imprévu, `workstream-policy.sh` introuvable,
      `mktemp` en échec) annoncent leur raison SUR STDERR À L'EXÉCUTION"
    requirement: WSAW-02
    verification:
      - kind: unit
        ref: "cas 26 (`DEGRAD-2`) de la suite : `.planning/workstreams` en LIEN SYMBOLIQUE →
          stderr porte « NON VÉRIFIABLE » ET « repli racine », rc NORMAL (0), le document est
          sur-signalé comme la ligne l'annonce ; CONTRE-ÉPREUVE dans la même exécution — fixture
          au contenu IDENTIQUE avec `workstreams` en répertoire RÉEL : aucune des deux chaînes
          sur stderr, rc=3, stdout vide"
        status: pass
    human_judgment: false
  - id: D3
    description: "QUAL-01 — discriminance PROUVÉE par mutation : la boucle
      `while IFS= read -r _wsdir` neutralisée par `false &&` fait réapparaître le faux positif
      D-04, le témoin non muté restant vert"
    requirement: WSAW-02
    verification:
      - kind: unit
        ref: "cas 25 (`MUT-1 TUE`) : `mut_applied=1` (mutation vérifiée APPLIQUÉE par `cmp -s`
          avant d'être opposée), témoin rc=3 / stdout vide, mutant rc=0 /
          `spec\\tdocs/superpowers/specs/2026-01-25-mutant-design.md`"
        status: pass
    human_judgment: false
  - id: D4
    description: "Suite neuve PROUVÉE CAPABLE DE ROUGIR : les 4 cas ajoutés rejoués contre les
      scripts de `762cf13` dans un arbre jetable"
    verification:
      - kind: unit
        ref: "arbre jetable sous le scratchpad de session (copie de `workstream-policy.sh` +
          `discover-unintegrated-docs.sh` extrait par `git show 762cf13:…`, cksum
          `3740701456 8312`) : `23 ok, 3 ko` — cas 23, 25 et 26 ROUGES avant, le cas 24 vert des
          deux côtés (nommé comme tel dans son libellé). Après le plan : `26 ok, 0 ko`"
        status: pass
    human_judgment: false
  - id: D5
    description: "Non-régression : contrat de sortie (0/3/64, `--hook` traduit 3→0) et règle de
      citation intacts ; invariants SC5 du module tenus ; rejeu CI complet vert"
    verification:
      - kind: integration
        ref: "`test-discover-unintegrated-docs.sh` 26 ok / 0 ko (cas 1 à 22 NON modifiés et
          verts) ; `test-dev-orchestrator.sh` 227 OK / 0 KO / 0 SKIP (dont T21a/T21b) ;
          `replay-ci-jobs.sh --job gates` : 15 rejouée(s), 3 sautée(s), 0 en echec, rc=0 ;
          `--job tests` : 86 suite(s), 0 échec(s), rc=0"
        status: pass
    human_judgment: false

status: complete
duration: ~50min
completed: 2026-09-24
---

# Phase 41.1 Plan 04: registre de citation élargi à chaque compartiment Summary

**`discover-unintegrated-docs.sh` ne lit plus seulement la racine de `.planning/` : il cite chaque
compartiment présent sur le disque — et quand il ne peut PAS le faire, il le DIT sur stderr au lieu
de se taire.**

## Performance

- **Duration:** ~50 min
- **Completed:** 2026-09-24
- **Tasks:** 2/2
- **Files modified:** 2 (`discover-unintegrated-docs.sh` +84/−5, `test-discover-unintegrated-docs.sh`
  +99/−2 — mesuré par `git diff --numstat 762cf13..HEAD` ; aucun cas préexistant modifié)

## Accomplishments

- **Le faux positif D-04 est fermé, mesuré des deux côtés.** Avant : un document cité uniquement
  dans `.planning/workstreams/dev/ROADMAP.md` ressortait `spec\tdocs/superpowers/specs/x.md`
  (rc=0). Après : rc=3, stdout vide, « Corpus entièrement cité ».
- **Registre par compartiment** : pour chaque chemin rendu par `vf_ws_enumerate`, les quatre
  fichiers `{ROADMAP,REQUIREMENTS,MILESTONES,PROJECT}.md` plus `milestones/*.md`, lus au **premier
  niveau** du compartiment seulement — `<compartiment>/phases/**` n'est jamais consulté.
- **Deux lectures hors compartiment conservées** : le repli racine complet (dépôt partiellement
  partitionné) et `docs/ADR.md`, lu **une seule fois**, hors boucle.
- **Sourcing à deux candidats** (patron de `check-divergence.sh`), chemin **vérifié** et non
  supposé : `$(dirname "$0")/workstream-policy.sh` (layout d'install à plat) puis
  `$(dirname "$0")/../../planning-core/scripts/workstream-policy.sh` (arbre du dépôt). Localisé par
  `find` avant écriture — le plan disait « probablement », la mesure a confirmé.
- **Aucune dégradation silencieuse** : quatre chemins de dégradation, quatre annonces sur stderr à
  l'exécution. Le cas `DEGRAD-2` en est la garde machine, avec sa contre-épreuve dans la même
  exécution.
- **Contrat de sortie et règle de citation intacts** : aucun code de sortie touché, `is_cited`
  non modifiée, les 22 cas historiques de la suite non modifiés et verts.
- **En-tête du script mis à jour** (le `--help` le rend verbatim via `grep '^# '`) : la section
  « Registres de citation consultés » décrit désormais l'univers réel.

## Task Commits

1. **Task 1 + Task 2 : registre élargi + 4 cas de suite (23, 24, MUT-1, DEGRAD-2)** — `5e0d846`
   (feat)
2. **Correctif d'invariant créé par ce plan : `_WS_LIST` → `_WS_TMP`** — `153513d` (fix)

**Plan metadata:** ce SUMMARY (docs).

## Files Created/Modified

- `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` — en-tête de documentation,
  sourcing à deux candidats, bloc d'énumération à `case` complet, repli racine + `$ADR_FILE` hors
  boucle
- `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` — cas 23 à 26
  (22 → 26 assertions), dont `MUT-1` et `DEGRAD-2`

## Decisions Made

Voir `key-decisions` du frontmatter. Les deux points qui méritent d'être lus : la **divergence
`must_have` / `<action>`** sur l'étendue du repli racine (suivie côté `<action>`, sur-ensemble sans
faux positif) et le **placement des trailers `Gate-Touche`** hors surface G-2, par exigence de
traçabilité du plan et non du gate.

## Deviations from Plan

**Une, fermée dans la phase qui l'a créée** — application de la règle d'arbitrage « la phase ferme
ce qu'elle a créé, elle consigne ce qu'elle a seulement révélé » :

- **Nom de la variable temporaire.** Le plan prescrivait `_WS_LIST` en toutes lettres dans son
  `<action>`. Ce nom fait **rougir l'invariant T21b (SC5)** de
  `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`, qui exige que toute redirection
  d'écriture de ce script cible `/dev/null`, un descripteur, ou une variable dont le **nom contient
  `TMP`**. Mesuré : `✗ T21b … redirection hors /dev/null|&N|*TMP* : 74: vf_ws_enumerate
  "$PLANNING_DIR" > "$_WS_LIST"`, `test-dev-orchestrator.sh` rc=1, rejeu `--job tests` en échec
  (86 suites, 1 échec). Renommé en `_WS_TMP` au commit `153513d`, avec le commentaire qui dit
  **pourquoi** le nom est contraint. Après : `✓ T21b`, 227 OK / 0 KO, `--job tests` rc=0.
  Incohérence **créée** par ce plan → **fermée** par lui. Le plan lui-même n'est PAS défectueux :
  son `<action>` est une rédaction, pas un contrat — l'invariant, lui, préexistait.

**Total deviations:** 1 (nom de variable, sans effet de comportement)
**Impact on plan:** nul sur le contrat livré.

## Issues Encountered

- **Flake pré-existant CONSIGNÉ, NON TOUCHÉ** — `plugin/conductor/scripts/tests/test-check-agents.sh`
  a rendu `--job tests` rouge à la **2e** passe sur son cas **T29** (l.511 :
  `OUT="$(run_check --strict 2>&1)"; RC=$?` puis `echo "$OUT" | grep -qi "nom d'agent non resolu"`),
  avec `rc=0` mais un `$OUT` tronqué au warning suivant — **exactement le mécanisme SIGPIPE
  non déterministe** déjà documenté pour le T9 de la même suite. Distingué d'une régression par
  rejeu isolé : **3 passes sur 3 vertes** (rc=0, 0 ligne `✗`), et la **3e passe** du rejeu complet
  rend `86 suite(s), 0 échec(s)`, rc=0. Hors périmètre de ce plan (aucun de mes deux fichiers),
  **révélé** et non créé → consigné, non corrigé.
- **Le cas 24 est vert des deux côtés** (avant comme après le plan) : il ne garde aucune propriété
  neuve, seulement la non-régression du comportement historique. Son libellé le **dit**
  (« non-régression, vert des deux côtés »). La propriété neuve est gardée par le **cas 23** et son
  mutant **MUT-1** — c'est là, et seulement là, qu'un retour en arrière du code rougirait.

## Next Steps

- Plan `41.1-05` et suivants (vague 3) — ce plan ferme le troisième et dernier consommateur de
  catégorie (c) de D-04.
- Le bump de `VERSION` / `CHANGELOG.md` de `dev-orchestrator` reste au plan `41.1-08`.

---
phase: 19-migration-du-moteur-gsd-pilotee-par-vf-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/scripts/ensure-deps.sh
  - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh
autonomous: true
requirements: [SC3, SC4, SC5, SC6]

must_haves:
  truths:
    - "SC1 (part `ensure-deps`) / D-03 : `detect_gsd_state()` rend `absent`, `legacy` ou `gsd-core` sur la même cascade que le gate de 19-01, et `detect_gsd()` n'est plus qu'un booléen DÉRIVÉ de cet état (source unique). Le `||` historique de `ensure-deps.sh:119-120` a disparu du corps de `detect_gsd()`."
    - "SC1 (part `ensure-deps`) / D-06 : l'état `legacy` n'emprunte plus le `skip` silencieux de `ensure-deps.sh:133`. Sans `--migrate-engine`, un poste legacy reçoit un message explicite disant que la migration est disponible et que ce run ne migre pas — jamais la ligne `GSD déjà présent (skip)`. Avec `--migrate-engine`, le bloc d'install npx est atteint."
    - "SC3 / chaînage : un run `--migrate-engine` sur un état `legacy` atteint le bloc `npx -y \"@opengsd/gsd-core@^1\" --claude <scope>` PUIS, dans le MÊME run, `patch_gsd_executor_mcp()`. Le chaînage passe par l'appel existant de `main()` : le nom de fonction n'apparaît dans la source, hors commentaires, que deux fois (sa définition et cet unique appel) — aucun second site d'appel divergent n'est créé."
    - "SC3 / vérification après coup : après l'injection `--force`, `patch_gsd_executor_mcp()` relance l'injecteur en mode `--verify` (hors dry-run) et relaie bruyamment un serveur manquant. Un `tools:` final auquel il manque un serveur déclaré dans le `.mcp.json` du lab produit une sortie `ERROR` et un code non nul côté injecteur."
    - "SC3 / `--verify` ne répare jamais : sur une cible à laquelle il manque un serveur, `inject-mcp-tools.sh --verify` laisse le fichier cible strictement inchangé (empreinte md5 stable) et sort en `1`. Le mode `--verify` est en lecture seule — il ne rejoue jamais `--force` à la place de l'appelant."
    - "SC4 / exactitude (D-08.1) : les deux lignes `npm uninstall -g` ne sont proposées QUE si une requête `npm ls -g --depth=0 <paquet>` en lecture seule confirme le paquet réellement installé en global. Stub rapportant absent → les deux lignes n'apparaissent pas ; stub rapportant présent → elles apparaissent."
    - "SC4 / complétude (D-08.2) : le message propose en plus le retrait de l'arborescence vide laissée debout par l'installeur amont, sous forme de commande affichée."
    - "SC4 / séquencement (D-08.3) : l'état legacy est capturé AVANT l'install et le message est rendu à partir de cet état capturé. Preuve : un stub `npx` qui supprime lui-même le `VERSION` legacy pendant son exécution — reproduisant l'effet de bord réel de l'installeur amont — n'empêche pas le message de nettoyage de sortir après l'install."
    - "SC4 / atteignabilité : le message de nettoyage est joignable depuis le chemin `--migrate-engine`, donc depuis le régime nominal de `/vf-update` (câblé en 19-03), et plus seulement depuis `/vf-init` et `/vf-calibrate`."
    - "SC6 / non-régression du repli : la cascade de dérivation à 4 niveaux de `detect-gsd-engine.sh` et `build-gsd-index.sh` est inchangée, et les 51+ assertions préexistantes de `test-dev-orchestrator.sh` restent vertes. T2d est MIS À JOUR (pas contourné) pour refléter le conditionnement des lignes `npm uninstall`, et ses assertions de non-exécution sont conservées."
    - "SC6 / plafond intouché : la chaîne `@opengsd/gsd-core@^1` de `ensure-deps.sh:166` est identique avant et après ce plan, au caractère près."
    - "SC7 (part portabilité) : les deux suites étendues sont rejouées dans un conteneur Linux ; les cas nouvellement ajoutés y sont tous verts et aucun KO nouveau n'apparaît."
  artifacts:
    - plugin/dev-orchestrator/scripts/ensure-deps.sh
    - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
    - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh
  key_links:
    - "`ensure_gsd()` → `patch_gsd_executor_mcp()` via `main()` : c'est le lien qui rend la ré-injection MCP non oubliable après une migration. L'installeur amont `gsd-core` réécrit `agents/gsd-executor.md` et efface `mcp__XcodeBuildMCP__*` du `tools:` ; sans ce chaînage, l'exécutant perd silencieusement son accès MCP (le trou n°5 du rapport). `_internal/vibeflow-update.sh:268` ne couvre pas ce cas — il n'injecte que dans les agents flaggés `vf-mcp-consumer`, flag que `gsd-executor` ne porte pas."
    - "`log_legacy_cleanup_if_needed()` → variable d'état capturée en tête de `ensure_gsd()` : le point de rupture le plus probable de tout le plan. L'installeur amont supprime lui-même le `VERSION` legacy à l'install réussie ; toute re-détection post-install rendrait le message définitivement inatteignable, sans aucun signal d'erreur."
    - "`ensure-deps.sh --migrate-engine` → `vf-update/SKILL.md` étape d'exécution (19-03) : le nom du flag et la variable d'environnement équivalente sont le contrat consommé par le skill. Les renommer casse 19-03."
    - "`inject-mcp-tools.sh --verify` → bloc python embarqué : le verdict de vérification doit remonter par le code de sortie du bloc python, qui est la dernière instruction du script et porte donc son code de retour."
  prohibitions:
    - "P-01 — MUST NOT exécuter `npm uninstall`, `rm -rf` ni aucune suppression depuis `log_legacy_cleanup_if_needed()`. Ces commandes sont AFFICHÉES, jamais lancées (ADR-031, D-08). La requête `npm ls -g --depth=0` est en revanche autorisée à l'exécution : elle est en lecture seule et ne mute rien."
    - "P-02 — MUST NOT faire réparer par `--verify`. Constater un serveur manquant et relancer `--force` à la place de l'appelant détruirait le contrat « dit fort » de D-09 : le défaut deviendrait invisible au moment même où il devait être signalé. `--verify` lit, compare, rapporte, et s'arrête là."
    - "P-03 — MUST NOT modifier le plafond semver `@^1` de `ensure-deps.sh:166`, ni le remplacer par un pin figé, ni le relever. Arbitrage post-audit Phase 11, explicitement non négociable."
    - "P-04 — MUST NOT supprimer ni affaiblir le repli legacy de `detect-gsd-engine.sh` / `build-gsd-index.sh`. C'est le `skip` qu'on corrige, pas le repli : les postes non encore migrés continuent d'être servis."
    - "P-05 — MUST NOT introduire de sauvegarde `tar` préalable à la migration. Option explicitement présentée et écartée (D-06) ; l'installeur amont met déjà de côté les patchs locaux."
    - "P-06 — MUST NOT créer de nouveau fichier de suite pour `ensure-deps.sh` ni pour `inject-mcp-tools.sh`. Les deux ont déjà leur foyer actif ; les cas nouveaux s'y ajoutent dans la numérotation existante."
    - "P-07 — MUST NOT rendre une install du moteur inconditionnelle. Sans `--migrate-engine`, un état `legacy` est signalé, jamais migré : la confirmation humaine appartient à `/vf-update` (ADR-031)."
---

<objective>
Rendre la migration exécutable et sa ré-injection MCP vérifiable, sans jamais l'imposer : le
détecteur d'`ensure-deps.sh` devient à trois valeurs, l'état `legacy` cesse d'être un `skip`
silencieux, un chemin `--migrate-engine` enchaîne install et ré-injection dans le même run, le
message de nettoyage devient exact et atteignable, et `inject-mcp-tools.sh` gagne un mode `--verify`
qui dit fort ce qui manque au lieu de l'avaler.

Purpose: c'est la moitié « effets » de la phase. Trois des cinq trous du rapport d'audit vivent ici
— le skip qui saute la migration même quand le script est appelé, la ré-injection MCP qui peut être
oubliée après une réinstall du moteur, et un message de nettoyage correct sur un chemin que le
régime nominal n'emprunte jamais. Le piège dominant n'est pas la logique mais le SÉQUENCEMENT :
l'installeur amont supprime lui-même le témoin legacy, donc toute re-détection après coup rend le
message définitivement muet.

Output: 2 scripts modifiés + 2 suites étendues (T2g…T2k, T10/T11, T2d mis à jour).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@plugin/dev-orchestrator/scripts/ensure-deps.sh
@plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
@plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
@plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `detect_gsd_state()` | fonction (nouvelle, `ensure-deps.sh`) | imprime `absent`, `legacy` ou `gsd-core` |
| `detect_gsd()` | fonction (réécrite) | booléen DÉRIVÉ de `detect_gsd_state()`, plus aucun `||` sur les deux fichiers |
| `npm_pkg_installed_globally()` | fonction (nouvelle) | `npm ls -g --depth=0 <paquet>`, lecture seule, code de retour comme verdict |
| `--migrate-engine` | flag CLI (nouveau, `ensure-deps.sh`) | autorise l'install sur un état `legacy` |
| `VF_ENSURE_MIGRATE_ENGINE` | variable d'environnement (nouvelle) | `1` équivaut à `--migrate-engine`, convention `VF_ENSURE_*` du fichier |
| état legacy capturé avant install | variable interne (nouvelle) | source unique du message de nettoyage (D-08.3) |
| ligne de retrait d'arborescence vide | ligne de message (nouvelle) | affichée, jamais exécutée |
| `--verify` | flag CLI (nouveau, `inject-mcp-tools.sh`) | mode lecture seule ; exits `0` conforme / `1` serveur manquant / `3` INDÉTERMINÉ |
| `VERIFY` | variable interne (nouvelle) | déclarée `"false"` à côté de `FORCE`/`DRY_RUN` |
| `T2g` … `T2k` | labels de cas (nouveaux) | suite `test-dev-orchestrator.sh`, numérotation continue après `T2f` |
| `T2d` | label de cas (mis à jour) | assertions réalignées sur le conditionnement des lignes `npm uninstall` |
| `T10`, `T11` | labels de cas (nouveaux) | suite `test-inject-mcp-tools.sh`, après `T9` |

## Détecteurs `plan:pre` non déclenchés

`assumption-delta` et `schema-gate` ne s'appliquent pas : aucune transition d'identité
singulier→pluriel ni requis→optionnel, aucun schéma ni ORM touché. `ai-integration` ne s'applique
pas non plus : l'appel à `npx` cible un paquet npm tiers déjà en place et plafonné, pas un
service d'API. La surface tierce réelle est traitée dans le `<threat_model>` ci-dessous.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : `detect_gsd_state()` à 3 valeurs, fin du `skip` sur legacy, chemin `--migrate-engine` chaîné (D-03, D-06)</name>
  <reversibility rating="reversible">Modification interne d'un script de bootstrap sans contrat
  externe rompu : le comportement historique (états `absent` et `gsd-core`) est strictement
  préservé, seul l'état `legacy` change de traitement.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/ensure-deps.sh` **en entier** — en particulier `:20-41` (bloc de documentation des variables `VF_ENSURE_*`, à étendre), `:55-72` (cascade et dérivation des deux chemins `VERSION`), `:98-102` (dérivation de `GSD_SCOPE_FLAG` depuis `VF_SCOPE`), `:116-128` (`detect_gsd()`, le piège n°1, `detect_gsd_legacy()`), `:130-178` (`ensure_gsd()` et son early-return), `:162-170` (bloc npx et plafond `@^1` — INTOUCHABLE), `:248-284` (`patch_gsd_executor_mcp()`), `:307-324` (`main()` et son appel unique à `patch_gsd_executor_mcp`).
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh:1-70` (en-tête et liste des T couverts, à compléter) et `:217-348` (T2b à T2f : les idiomes exacts de fixture — `mktemp -d` pour `HOME`, stubs `npm`/`node`/`npx` en tête de `PATH` avec fichier de trace, `env -u VF_ENSURE_FORCE HOME="$FIX" ...`, `"$GREP"` plutôt que `grep` nu).
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-03, D-06, D-09 et §Integration Points.
    - `plugin/dev-orchestrator/scripts/check-gsd-engine.sh` s'il existe déjà (produit en 19-01, plan parallèle) — pour aligner la sémantique des trois états. S'il n'existe pas encore, ne pas bloquer : `19-CONTEXT.md` D-03 fait foi.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/ensure-deps.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <behavior>
    - État `gsd-core`, sans flag : ligne de skip historique, aucun appel npx. Comportement strictement inchangé.
    - État `absent`, sans flag : install npx tentée, comportement strictement inchangé.
    - État `legacy`, sans flag : message explicite annonçant qu'une migration est disponible et que ce run ne migre pas ; aucun appel npx ; la ligne historique de skip n'apparaît pas.
    - État `legacy`, avec `--migrate-engine` : appel npx avec le paquet plafonné et le drapeau de scope dérivé, puis `patch_gsd_executor_mcp()` dans le même run.
    - État `legacy`, avec `VF_ENSURE_MIGRATE_ENGINE=1` : comportement identique au flag.
    - Scope : `VF_SCOPE=user` produit `--global`, `VF_SCOPE=project` et `VF_SCOPE=local` produisent `--local` — inchangé.
  </behavior>
  <action>
Dans `ensure-deps.sh`, écrire `detect_gsd_state()` qui imprime `gsd-core` si le fichier `VERSION`
du nouveau layout existe, sinon `legacy` si celui du layout legacy existe, sinon `absent`. La
cascade de dérivation des deux chemins (`:60-72`) reste la source unique et n'est pas touchée.

Réécrire `detect_gsd()` pour qu'elle DÉRIVE son booléen de `detect_gsd_state()` au lieu de refaire
le test — l'ancien `||` disparaît de son corps. `detect_gsd()` n'a plus qu'un consommateur, la ligne
de résumé final de `main()` ; ne pas changer la sémantique de ce résumé. Conserver
`detect_gsd_legacy()` telle quelle : c'est elle qui alimente la capture d'état de la Task 2.

Ajouter le pilotage du nouveau chemin. Déclarer une variable interne à côté des autres
`VF_ENSURE_*` (`:47-54`), initialisée depuis `VF_ENSURE_MIGRATE_ENGINE`. Ajouter, avant l'appel de
`main`, une boucle de parsing minimale qui accepte `--migrate-engine` (met la variable à 1) et
`-h|--help` (imprime l'en-tête via le motif `grep '^# '` déjà utilisé par les scripts voisins, exit
0). Les arguments inconnus sont IGNORÉS avec une ligne `log` — choix délibéré de rétro-compatibilité
à documenter dans l'en-tête : le script recevait historiquement `"$@"` sans jamais le lire, et un
rejet strict casserait un appelant non recensé. Documenter le flag ET la variable dans le bloc
d'en-tête des variables d'environnement, au même format que les autres.

Reprendre `ensure_gsd()` : remplacer l'early-return unique par une distinction explicite des trois
états. L'état `gsd-core` garde le skip historique mot pour mot, y compris son exception dry-run
forcé. L'état `legacy` sans autorisation de migration produit un message DISTINCT — il annonce le
moteur legacy détecté, dit que la migration est disponible, dit que ce run ne la fait pas, et
retourne sans appeler npx. La ligne historique de skip ne doit pas sortir dans ce cas : c'est elle
que le rapport a identifiée comme le silence fautif. L'état `legacy` AVEC autorisation, ainsi que
l'état `absent`, tombent tous deux dans la suite existante de la fonction : gardes Node/npm
inchangées, puis le bloc npx des lignes 162-170, dont la chaîne du paquet et son plafond ne sont
pas touchés.

Ne créer AUCUN second site d'appel à `patch_gsd_executor_mcp` : le chaînage exigé par D-06 et SC3
passe par l'appel déjà présent en fin de `main()`. La seule exigence est que le chemin
`--migrate-engine` n'interrompe jamais `main()` avant d'y arriver.

Étendre `test-dev-orchestrator.sh` avec deux cas nouveaux, dans la numérotation continue après T2f,
et compléter la liste des T de l'en-tête du fichier. T2g couvre l'atteinte du bloc d'install : une
fixture `HOME` ne portant que le `VERSION` legacy, des stubs `npm`/`node`/`npx` en tête de `PATH`
avec fichier de trace selon l'idiome de T2e, exécutée depuis un répertoire de projet temporaire —
avec le flag, la trace de npx est non vide et porte le paquet plafonné et le drapeau de scope ;
sans le flag, la trace reste vide et la sortie ne contient pas la ligne historique de skip. Les deux
sous-cas sont écrits, c'est le couple qui rend le test discriminant. T2h couvre le chaînage MCP de
bout en bout : même fixture, plus un `gsd-executor.md` planté sous le `HOME` factice avec une ligne
`tools:` sans le serveur du lab et un `.mcp.json` dans le projet temporaire déclarant ce serveur —
après un run `--migrate-engine`, la ligne `tools:` du fichier planté contient le jeton du serveur.
Si `python3` est absent de la machine, ce cas se marque SKIP explicite plutôt que KO, l'injecteur
étant best-effort par conception.

Toutes les invocations de fixture doivent surcharger `HOME`, retirer `CLAUDE_CONFIG_DIR` de
l'environnement, et s'exécuter depuis un répertoire temporaire — jamais depuis la racine du dépôt,
sous peine de faire écrire l'injecteur dans un `./.claude/agents/` réel.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en `0` ; le bilan annonce `0 ko` et un nombre d'assertions vertes supérieur ou égal au compteur d'avant ce plan (à relever avant modification et à consigner dans le SUMMARY).
    - T2g sous-cas A : run avec `--migrate-engine` sur une fixture legacy seule → le fichier de trace de `npx` contient la sous-chaîne `@opengsd/gsd-core@^1` et la sous-chaîne `--global`.
    - T2g sous-cas B : run SANS le flag sur la même fixture → le fichier de trace de `npx` est vide (`[ ! -s "$TRACE" ]`), la sortie NE contient PAS la sous-chaîne `GSD déjà présent`, et contient une mention explicite de migration disponible.
    - T2g sous-cas C : `VF_ENSURE_MIGRATE_ENGINE=1` sans le flag produit la même trace non vide que le sous-cas A.
    - T2h : après un run `--migrate-engine`, `grep -m1 '^tools:' <gsd-executor planté>` contient le jeton du serveur déclaré dans le `.mcp.json` de la fixture. Marqué SKIP, jamais KO, si `command -v python3` échoue.
    - Non-régression du chaînage : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/ensure-deps.sh | grep -c 'patch_gsd_executor_mcp'` retourne exactement `2` (la définition et l'unique appel de `main()`).
    - Non-régression du plafond : `grep -c '@opengsd/gsd-core@\^1' plugin/dev-orchestrator/scripts/ensure-deps.sh` retourne la même valeur qu'avant ce plan, et `git diff plugin/dev-orchestrator/scripts/ensure-deps.sh` ne montre aucune ligne modifiée contenant cette chaîne.
    - Disparition du test booléen historique : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/ensure-deps.sh | grep -c 'GSD_VERSION_FILE_NEW" \] || \[ -f "$GSD_VERSION_FILE_LEGACY'` retourne `0`.
    - T2c, T2e et T2f restent verts sans modification de leur code.
    - `bash -n plugin/dev-orchestrator/scripts/ensure-deps.sh` retourne 0.
  </acceptance_criteria>
  <done>Un état `legacy` n'est plus un skip silencieux ; `--migrate-engine` atteint l'install puis la ré-injection dans le même run ; T2g et T2h le prouvent ; les cas préexistants restent verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : message de nettoyage exact et atteignable — capture pré-install, `npm ls -g` conditionnel, arborescence vide (D-08)</name>
  <reversibility rating="reversible">Le corps de la fonction n'est fait que de lignes affichées ;
  revenir en arrière est un retour de texte.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/ensure-deps.sh:180-191` — `log_legacy_cleanup_if_needed()` dans son état actuel, et ses cinq sites d'appel dans `ensure_gsd()` (`:135`, `:145`, `:158`, `:168`, `:176`).
    - `plugin/dev-orchestrator/scripts/ensure-deps.sh:76-82` — helpers `log()`/`err()`, convention de préfixe à respecter.
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh:280-306` — **T2d dans son état actuel** : il asserte aujourd'hui les 3 lignes de nettoyage de façon INCONDITIONNELLE, avec un stub `npm` qui sort en 1. Ce cas cassera mécaniquement dès que les lignes deviennent conditionnelles — il doit être mis à jour, pas contourné.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-08 (les trois corrections, dont le piège de séquencement) et `19-PATTERNS.md` §`log_legacy_cleanup_if_needed()`.
    - `docs/ADR.md` ADR-031 — jamais d'action sans validation humaine.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/ensure-deps.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <behavior>
    - Fixture legacy, stub `npm` répondant « installé » à une requête de liste globale : les deux lignes de désinstallation apparaissent dans la sortie.
    - Même fixture, stub `npm` répondant « absent » : les deux lignes de désinstallation n'apparaissent PAS, mais l'en-tête du message et la ligne de retrait du dossier restent présentes.
    - Toutes fixtures : la ligne de retrait de l'arborescence vide est présente.
    - Toutes fixtures : le fichier de trace des stubs ne contient jamais le mot `uninstall`, et aucun fichier de la fixture n'est supprimé par le script.
    - Fixture legacy + stub `npx` qui supprime le fichier `VERSION` legacy pendant son exécution, run `--migrate-engine` : le message de nettoyage sort quand même après l'install.
  </behavior>
  <action>
Introduire dans `ensure-deps.sh` une variable interne d'état legacy, initialisée à vide et
renseignée UNE SEULE FOIS tout en haut de `ensure_gsd()`, avant toute garde et avant tout
`run_cmd` : si `detect_gsd_legacy()` est vraie, la variable retient le fait et la version legacy
lue au même instant. C'est le point critique de D-08.3 — l'installeur amont supprime lui-même le
fichier `VERSION` legacy quand il réussit, donc une re-détection après l'install renverrait faux et
le message deviendrait définitivement inatteignable, sans aucune erreur visible.

Réécrire `log_legacy_cleanup_if_needed()` pour qu'elle se déclenche sur cette variable capturée et
non plus sur `detect_gsd_legacy()`. Ses cinq sites d'appel existants ne changent pas.

Ajouter `npm_pkg_installed_globally()` : elle retourne faux si `npm` n'est pas sur le PATH, sinon
elle interroge la liste globale des paquets en profondeur 0 pour le paquet demandé et propage le
verdict par son code de retour. Cette requête est en LECTURE SEULE et peut être réellement
exécutée ; c'est le seul appel `npm` autorisé dans ce chemin.

Reconstruire le corps du message : l'en-tête annonçant les artefacts legacy détectés reste ; chacune
des deux lignes de désinstallation globale n'est affichée que si la fonction ci-dessus confirme le
paquet réellement présent en global — sur le poste audité, aucun des deux ne l'était (install faite
par npx), donc deux lignes sur trois étaient des no-op trompeurs ; la ligne de suppression du
dossier legacy est conservée ; et une ligne nouvelle propose le retrait de l'arborescence vide que
l'installeur amont laisse debout après avoir vidé son contenu, sous forme de commande de recherche
de répertoires vides. Toutes ces lignes passent par `log`, aucune n'est exécutée — c'est la forme
déjà en vigueur dans cette fonction, et elle est non négociable (ADR-031).

Mettre à jour T2d dans `test-dev-orchestrator.sh` plutôt que de le contourner : son stub `npm`
répond aujourd'hui en échec, donc sous la nouvelle logique les deux lignes de désinstallation ne
doivent PAS apparaître. Réaligner ses assertions sur ce fait, conserver intégralement son assertion
de non-exécution par fichier de trace, et lui ajouter la vérification de la ligne d'arborescence
vide. Documenter le changement de sémantique dans le commentaire du cas, comme le fichier le fait
déjà pour T3/T12/T13/T14 dans son en-tête.

Ajouter trois cas nouveaux. T2i : stub `npm` qui répond succès à une requête de liste globale et
échec sinon → les deux lignes de désinstallation apparaissent. T2j : stub `npm` qui répond échec à
la requête de liste → les deux lignes n'apparaissent pas, l'en-tête et la ligne de suppression du
dossier restent. T2k : le cas de séquencement — fixture legacy, stub `npx` qui supprime le fichier
`VERSION` legacy de la fixture puis sort en succès, run `--migrate-engine` ; assertion que le
message de nettoyage sort malgré la disparition du témoin, et assertion complémentaire que le
fichier a bien été supprimé par le stub (sans quoi le cas serait tautologique et passerait même avec
une re-détection). Compléter la liste des T de l'en-tête du fichier.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en `0`, bilan `0 ko`.
    - T2i : la sortie contient les deux sous-chaînes `npm uninstall -g get-shit-done-cc` et `npm uninstall -g @gsd-build/sdk`.
    - T2j : la sortie ne contient NI l'une NI l'autre de ces deux sous-chaînes, contient la sous-chaîne `rm -rf ~/.claude/get-shit-done`, et contient la ligne de retrait d'arborescence vide (sous-chaîne `-type d -empty`).
    - T2k (piège de séquencement) : trois assertions distinctes — le fichier `VERSION` legacy de la fixture n'existe plus après le run ; la sortie contient l'en-tête du message de nettoyage ; le code de sortie du script reste `0`.
    - T2d mis à jour : ses assertions de non-exécution sont conservées à l'identique (le fichier de trace des stubs ne contient jamais la sous-chaîne `uninstall`), et son commentaire documente le changement de sémantique.
    - Preuve structurelle de non-exécution : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/ensure-deps.sh | grep -E 'npm uninstall|rm -rf|-empty -delete' | grep -vc '^[[:space:]]*log '` retourne `0` — autrement dit, aucune de ces commandes n'apparaît ailleurs que comme argument d'un `log`.
    - Preuve structurelle de la capture pré-install : dans le corps de `log_legacy_cleanup_if_needed()`, `detect_gsd_legacy` n'apparaît plus. Vérifié par lecture ciblée de la fonction et asserté par un grep de la suite sur la plage de la fonction.
    - Aucune fixture n'écrit hors de son `mktemp -d` : chaque `mktemp -d` du bloc T2 est apparié à un nettoyage explicite ou au `trap ... EXIT` de la suite.
    - `bash -n plugin/dev-orchestrator/scripts/ensure-deps.sh` retourne 0.
  </acceptance_criteria>
  <done>Le message de nettoyage est exact (deux lignes conditionnées à une constatation réelle), complet (arborescence vide) et atteignable après une install qui a effacé son propre témoin ; T2d est réaligné et T2i/T2j/T2k le prouvent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : `inject-mcp-tools.sh --verify` qui dit fort et ne répare jamais (D-09) + preuve Linux des deux suites</name>
  <reversibility rating="reversible">Nouveau flag purement additif sur un script dont les modes
  existants ne changent pas.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` **en entier** — `:16-39` (bloc Usage/Options de l'en-tête, à étendre), `:46-51` (déclarations `FORCE`/`DRY_RUN`, style à suivre pour `VERIFY`), `:52-65` (le `case` de parsing, point d'insertion exact), `:70-75` (repli best-effort si `python3` absent), `:77-78` (passage des variables au bloc python), `:114` (`want_tokens`), `:165-169` (garde `--force`), `:188-196` (lecture de `existing` et calcul de `missing`), `:214-215` (sortie finale du bloc python).
    - `plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh` **en entier** — l'en-tête listant `T1`…`T9` (à compléter), les fixtures `mk_gsd_executor`/`mk_mcp`, les helpers `md5of()` et `toolsline()`, et le bilan final.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-09 et le fait couvert (l'installeur amont réécrit `agents/gsd-executor.md` et efface le serveur MCP du lab de son `tools:`).
    - `docs/ADR.md` ADR-051 — allowlist MCP dérivée du lab.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/inject-mcp-tools.sh, plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh</files>
  <behavior>
    - Cible `gsd-executor` dont le `tools:` ne contient pas le serveur demandé, appelée en mode vérification : sortie bruyante mentionnant le jeton manquant, code de sortie `1`, fichier strictement inchangé.
    - Même cible après une injection réussie, appelée en mode vérification : code de sortie `0`, fichier inchangé.
    - Cible sans ligne `tools:` (elle hérite de tout), appelée en mode vérification : code de sortie `3`, aucun verdict.
    - `python3` absent : code de sortie `3` en mode vérification, jamais `0`.
    - Modes `--force` et `--dry-run` : comportement et codes de sortie strictement inchangés, les 9 cas préexistants restent verts.
  </behavior>
  <action>
Dans `inject-mcp-tools.sh`, déclarer une variable `VERIFY` initialisée à la même valeur littérale
et dans le même style que `FORCE` et `DRY_RUN`, puis insérer `--verify` dans le `case` de parsing
existant comme un flag booléen de plus, à côté de `--force` et `--dry-run`. Étendre le bloc Options
de l'en-tête et y écrire le contrat du mode : il RELIT, il COMPARE, il RAPPORTE, et il ne réécrit
jamais rien. Écrire noir sur blanc que ce mode ne rejoue pas l'injection à la place de l'appelant —
réparer silencieusement détruirait exactement le signal que le mode existe pour produire.

Passer la nouvelle variable au bloc python embarqué au même endroit que les autres, et l'y lire au
même endroit que `force` et `dry_run`. Dans la boucle de traitement, en mode vérification :
réutiliser tel quel le calcul de `want_tokens` depuis les serveurs résolus, la lecture de la valeur
de la ligne `tools:` et le calcul de la liste manquante — ce sont exactement les mêmes expressions
qu'en mode injection, elles ne sont pas réécrites. Si la liste manquante est vide, journaliser une
confirmation ; sinon, journaliser via le canal d'erreur en nommant les jetons manquants et le
fichier concerné. Aucune écriture de fichier n'est faite dans cette branche : ni fichier temporaire,
ni remplacement.

Le verdict remonte par le code de sortie du bloc python, qui est la dernière instruction du script
et porte donc son code de retour : `0` si aucun jeton ne manque sur aucune cible, `1` dès qu'un
jeton manque, `3` si aucun verdict n'est possible — pas de ligne `tools:`, aucun serveur déclaré, ou
aucune cible retenue. Traiter aussi le cas `python3` absent : le repli best-effort actuel sort en
succès, ce qui serait un faux vert en mode vérification ; en mode vérification, ce repli doit sortir
en INDÉTERMINÉ. Les modes existants gardent leur repli en succès, inchangé.

Dans `ensure-deps.sh`, brancher la vérification exigée par SC3 à l'intérieur de
`patch_gsd_executor_mcp()`, après l'invocation `--force` déjà présente et uniquement hors dry-run
(en dry-run rien n'a été écrit, une vérification y serait une fausse alarme). Relayer un verdict
non nul par le canal d'erreur du script, en best-effort : la vérification informe fort, elle ne fait
jamais échouer le bootstrap.

Étendre `test-inject-mcp-tools.sh` avec deux cas nouveaux, dans la numérotation continue après T9,
et compléter la liste des T de son en-tête. T10 : fixture `mk_gsd_executor` (agent hors plugin, sans
flag de consommateur MCP, exactement le scénario de D-09), vérification demandant un serveur absent
de son `tools:` — assertions séparées sur le code de sortie, sur la présence du nom du jeton
manquant dans la sortie, et sur la stabilité de l'empreinte du fichier. T11 : même fixture après une
injection réussie, vérification du même serveur — code de sortie de conformité et empreinte stable.
L'assertion d'empreinte est la plus importante des deux cas : c'est elle qui prouve que le mode ne
répare pas.

Terminer la tâche par la preuve de portabilité Linux des DEUX suites étendues, exécutées dans un
conteneur, en installant au préalable dans le conteneur les outils dont les suites dépendent
réellement (interpréteur python et git). Les cas nouvellement ajoutés doivent y être verts ; les
SKIP préexistants liés à l'outillage de l'hôte restent acceptables, un KO nouveau ne l'est pas.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh` sort en `0` et son bilan annonce `0 KO` ; les cas T1 à T9 sont verts sans modification de leur code.
    - T10, trois assertions séparées : le code de sortie capturé vaut `1` ; la sortie capturée contient la sous-chaîne du jeton manquant ; l'empreinte `md5of` du fichier cible est identique avant et après l'appel.
    - T11, deux assertions séparées : le code de sortie vaut `0` ; l'empreinte du fichier cible est identique avant et après l'appel.
    - Un appel en mode vérification sur une cible sans ligne `tools:` retourne `3` et jamais `0`.
    - Non-régression du parsing : `bash plugin/dev-orchestrator/scripts/inject-mcp-tools.sh --argument-inconnu` retourne toujours `1` avec un message d'erreur, comme avant ce plan.
    - Preuve structurelle « ne répare jamais » : dans la branche de vérification du bloc python, aucune écriture n'est présente — `os.replace` et l'ouverture en écriture n'apparaissent que dans la branche d'injection. Vérifié par lecture ciblée et consigné dans le SUMMARY.
    - Branchement SC3 : `grep -c 'verify' plugin/dev-orchestrator/scripts/ensure-deps.sh` retourne une valeur ≥ 1, et la vérification est bien conditionnée à l'absence de dry-run (lecture ciblée de `patch_gsd_executor_mcp`).
    - Preuve Linux : `docker run --rm -v "$(pwd)":/repo -w /repo ubuntu:24.04 bash -c 'apt-get update -qq && apt-get install -y -qq python3 git >/dev/null 2>&1; bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh'` est réellement exécutée, retourne `0`, et sa sortie est recopiée dans le SUMMARY. Les cas T2g à T2k, T10 et T11 y sont verts.
    - `bash -n` retourne 0 sur `inject-mcp-tools.sh` et sur `ensure-deps.sh`.
  </acceptance_criteria>
  <done>Un serveur MCP manquant après ré-injection est dit fort et non nul, sans jamais que le fichier soit réparé en douce ; le branchement post-install est en place ; les deux suites sont vertes sur macOS et sur Linux.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registre npm public → poste de l'utilisateur | `npx -y "@opengsd/gsd-core@^1"` exécute du code tiers non maîtrisé, avec les droits de l'utilisateur |
| installeur amont `gsd-core` → fichiers du poste | Le tiers réécrit `agents/gsd-executor.md`, supprime le `VERSION` legacy, dépose hooks et skills |
| `inject-mcp-tools.sh` → frontmatter d'un agent hors plugin | VibeFlow réécrit la ligne `tools:` d'un fichier qui ne lui appartient pas (ADR-051) |
| appelant (`/vf-update`, `/vf-init`, CI) → `ensure-deps.sh` | Le drapeau de migration décide d'un effet de bord irréversible sur le poste |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02-SC | Tampering | chaîne d'approvisionnement : `npx -y "@opengsd/gsd-core@^1"` (`ensure-deps.sh:166`) | high | mitigate | Plafond `@^1` CONSERVÉ tel quel : un saut de majeure (rupture ou compromission d'un fork jeune) ne s'installe jamais seul, il redevient une décision humaine (arbitrage post-audit Phase 11). Aucun paquet nouveau n'est introduit par cette phase, donc aucun point de contrôle de légitimité supplémentaire n'est requis ; ce plan n'a pas le droit d'affaiblir le plafond (P-03). |
| T-19-02-01 | Elevation of Privilege | migration du moteur déclenchée sans accord humain | high | mitigate | Sans `--migrate-engine` (ni sa variable équivalente), un état `legacy` est SIGNALÉ, jamais migré (P-07). La confirmation vit dans l'`AskUserQuestion` de `/vf-update` (19-03, ADR-031). T2g sous-cas B prouve qu'aucun appel npx n'a lieu sans le drapeau. |
| T-19-02-02 | Tampering | `log_legacy_cleanup_if_needed()` exécutant une désinstallation ou une suppression | high | mitigate | Toute commande destructive reste un argument de `log`, jamais une commande exécutée (ADR-031). Preuve double : grep structurel exigeant zéro occurrence hors `log`, et assertion de trace runtime conservée de T2d. Seule la requête de liste globale npm, en lecture seule, est réellement exécutée. |
| T-19-02-03 | Tampering | `--verify` réparant silencieusement en rejouant `--force` | high | mitigate | Interdit par P-02 et prouvé par l'assertion d'empreinte de T10 : le fichier cible est identique avant et après une vérification en échec. La branche de vérification ne contient aucune écriture. |
| T-19-02-04 | Repudiation | perte silencieuse de l'accès MCP de `gsd-executor` après réinstall du moteur | high | mitigate | Chaînage obligatoire install → `patch_gsd_executor_mcp` dans le même run (SC3), puis vérification `--verify` bruyante. Le trou existait parce que `_internal/vibeflow-update.sh:268` n'injecte que dans les agents flaggés, flag que `gsd-executor` ne porte pas. |
| T-19-02-05 | Denial of Service | message de nettoyage rendu inatteignable par la suppression amont du témoin legacy | high | mitigate | Capture de l'état legacy AVANT l'install et rendu du message depuis cette capture (D-08.3). T2k reproduit l'effet de bord réel de l'installeur et prouve que le message sort quand même. |
| T-19-02-06 | Tampering | réécriture du frontmatter d'un agent tiers (`gsd-executor.md`) | medium | mitigate | Comportement préexistant et cadré par ADR-051 : seule la ligne `tools:` est modifiée, l'injection est idempotente, et `--force` reste requis pour un fichier sans flag de consommateur. Ce plan n'élargit pas la surface, il y ajoute une vérification en lecture seule. |
| T-19-02-07 | Information Disclosure | la requête de liste globale npm révèle des paquets installés dans la sortie | low | accept | Sortie locale destinée à l'utilisateur propriétaire du poste ; seuls deux noms de paquets connus sont interrogés nommément, aucune énumération n'est imprimée. |
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` → exit 0, `0 ko`, compteur ≥ compteur d'avant.
- `bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh` → exit 0, `0 KO`.
- Preuve Linux des deux suites dans `ubuntu:24.04` → exit 0, cas T2g…T2k / T10 / T11 verts.
- `bash -n` sur `ensure-deps.sh` et `inject-mcp-tools.sh` → exit 0.
- `git diff plugin/dev-orchestrator/scripts/ensure-deps.sh` : aucune ligne modifiée ne contient le
  plafond `@^1`, et `detect-gsd-engine.sh` / `build-gsd-index.sh` n'apparaissent pas dans
  `git diff --name-only` (repli legacy intouché, SC6).
- `git diff --name-only` ne mentionne que les quatre fichiers de `files_modified`.
</verification>

<success_criteria>
- SC3 tenu : toute install ou réinstall du moteur enchaîne sur la ré-injection `--force` puis sur
  une vérification qui dit fort un serveur manquant, sans jamais réparer en douce.
- SC4 tenu : le message de nettoyage est atteignable en régime nominal, exact (désinstallation
  proposée seulement sur constatation réelle) et complet (arborescence vide incluse), et survit à
  la suppression amont de son propre témoin.
- SC5 tenu pour sa part « non-régression » : les cas nouveaux sont discriminants (chaque cas a son
  sous-cas négatif) et les 51+ assertions préexistantes restent vertes.
- SC6 tenu : repli legacy et plafond `@^1` strictement intouchés, prouvé par diff.
</success_criteria>

<output>
Create `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-02-SUMMARY.md` when done
</output>

---
phase: 32-durcissement-du-driver-lock
plan: 04
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - plugin/conductor/references/team-kernel.md
autonomous: true
requirements: [LOCK-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Le jeton de fence est la GENERATION du lock, exposee en JSON par le plan 32-01 — le seul candidat monotone qui invalide l'ancien tenant (D-32-04, 32-TERRAIN.md §6 candidat n°2)"
    - "La convention de trailer `Fence:` est documentee dans le contrat invariant que TOUS les managers de mission declarent lire (`conductor-references/team-kernel.md`) — pas dans un module de metier, sinon elle ne toucherait qu'une equipe"
    - "AUCUN mecanisme d'enforcement machine n'est construit : ni hook de message de commit, ni gate qui parse les trailers (D-32-04, motif double : aucun outillage de trailer n'existe dans ce depot, et le moteur de merge ne connait pas les hooks git natifs)"
    - "La consequence assumee est ECRITE : le trailer est auditable a la main, exactement au meme tier que les deux trailers deja en usage — pas verifie machine"
    - "La recette d'audit « quel commit sous quel mandat » est ecrite ET EXECUTABLE : elle recoupe les trailers des commits au journal d'evenements pose par le plan 32-02"
    - "Le tableau des briques du kernel dit la verite sur les verbes du verrou apres la Phase 32 (takeover et reclaim y figurent, l'auto-steal implicite n'y est plus decrit)"
  artifacts:
    - "plugin/conductor/references/team-kernel.md : section « Jeton de fence » + ligne de brique du verrou mise a jour"
  key_links:
    - "driver-lock.sh status -> cle `generation` (plan 32-01) : la SOURCE du jeton ; sans cette exposition la convention serait inapplicable"
    - "trailer Fence: <- journal ${LOCK_BASE}.events.log (plan 32-02) : les deux bouts de l'audit — le commit dit sous quelle generation il est ne, le journal dit quand cette generation a change de main"
    - "team-kernel.md <- « Contrat invariant » declare par vf-content-manager, vf-business-manager, vf-design-manager, vf-growth-manager : c'est le chemin par lequel une convention atteint TOUS les managers, quel que soit leur metier. CORRECTIF DE REVUE (BL-5, verifie par grep) : `vf-dev-manager.md` ne declare PAS `team-kernel.md` — `grep -c \"team-kernel\" plugin/dev-orchestrator/agents/vf-dev-manager.md` rend `0`. Ce manager lit `dev-orchestrator-references/*` (`mission-flow.md`, `mission-contracts.md`, etc.), PAS `conductor-references/team-kernel.md` directement. Consequence directe pour LOCK-05 : la convention `Fence:` ecrite UNIQUEMENT dans `team-kernel.md` n'atteindrait JAMAIS le manager qui commite le plus dans CE depot — LOCK-05 serait structurellement inatteignable pour `dev-orchestrator`. C'est precisement ce que le plan 32-07 (active le 2026-08-17, GEL-1) corrige : il propage la convention aux sept fichiers reels, dont `plugin/dev-orchestrator/agents/vf-dev-manager.md` et `plugin/dev-orchestrator/references/mission-flow.md`. Ce plan-ci (32-04) reste la SOURCE canonique de la convention (team-kernel.md, lu par les quatre autres managers directement) ; 32-07 en est la propagation vers dev-orchestrator et les references qu'il lit reellement."
  prohibitions:
    - "Ne construire AUCUN hook git (message de commit ou preparation de message) — obstacle mesure : ce depot arme deja un chemin de hooks git pour le pre-push, et tout hook supplementaire non versionne entre en conflit avec lui ET n'est pas distribuable aux labs (spike §7)"
    - "Ne construire AUCUN gate CI qui parse les trailers — ce serait une extension de capacite du moteur de merge, hors du perimetre de la phase et au-dela de ce que LOCK-05 exige litteralement (« auditable », pas « bloque a la source »)"
    - "Ne rien ecrire hors de plugin/conductor/references/team-kernel.md dans ce plan"
    - "Ne pas promettre une verification machine qui n'existe pas : ecrire la limite, ne pas la maquiller"
---

<objective>
Servir LOCK-05 par les deux seules choses que D-32-04 autorise : le jeton (la generation du lock,
rendue lisible par le plan 32-01) et une convention documentee la ou tous les managers la lisent —
plus la recette d'audit qui rend la promesse « quel commit sous quel mandat » reellement verifiable
a la main.

Purpose: aucun outillage de trailer n'existe dans ce depot (verifie : le repertoire de hooks git ne
contient que des exemples, aucun hook de message de commit). Les deux trailers reellement en usage
— presents sur 297 a 371 des 300 derniers commits — sont poses par convention d'agent SEULE, jamais
par une machine. LOCK-05 rejoint ce tier-la, en connaissance de cause, et sa limite est ecrite au
lieu d'etre maquillee en garantie.

Output: une section « Jeton de fence » dans le contrat invariant du team-kernel, la ligne de brique
du verrou remise a l'heure apres l'arrivee de `takeover`/`reclaim`, et une recette d'audit qui
s'execute vraiment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-TERRAIN.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-01-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-02-SUMMARY.md
@plugin/conductor/references/team-kernel.md
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : section « Jeton de fence » dans le contrat invariant du kernel (D-32-04, LOCK-05)</name>
  <files>plugin/conductor/references/team-kernel.md</files>
  <read_first>
    - plugin/conductor/references/team-kernel.md:1-27 (en-tete, chemin d'install, et le tableau « Ce que le kernel fournit » dont la premiere ligne decrit le verrou)
    - plugin/conductor/references/team-kernel.md:184-201 (le paragraphe « Etages croises » : c'est la ou le verrou est nomme SEUL garant machine de l'invariant — le voisinage naturel de la nouvelle section)
    - plugin/conductor/scripts/driver-lock.sh (etat apres 32-01 et 32-02 : la cle `generation` en sortie JSON, les verbes `takeover` et `reclaim`, le journal d'evenements et son nom exact)
    - 32-TERRAIN.md §6 (les trailers reellement en usage et leurs comptes mesures sur 300 commits ; les quatre candidats de jeton par ordre de proximite, et pourquoi la generation est le candidat naturel)
    - 32-CONTEXT.md §Decisions D-32-04 (la decision, son motif de refus d'enforcement, et sa consequence assumee) et §Deferred (les deux extensions remontees)
  </read_first>
  <action>
Ajouter a `plugin/conductor/references/team-kernel.md` une section « Jeton de fence — quel commit
sous quel mandat (LOCK-05) », placee immediatement apres le paragraphe des etages croises, parce que
c'est la que le document etablit deja que le verrou est le seul garant machine de l'invariant : le
jeton en est le corollaire d'audit.

La section dit, dans cet ordre :

1. **Ce qu'est le jeton.** La generation du lock — le nom de la cible du lien symbolique, unique et
   monotone par acquisition, deja relu en interne depuis toujours et rendu lisible en JSON par la
   Phase 32. C'est le seul candidat qui soit un fence au sens strict : un numero qui INVALIDE
   l'ancien tenant apres une reprise. Ecarter explicitement les trois autres candidats et dire
   pourquoi en une ligne chacun : l'owner est une chaine libre jamais validee par le script ; les
   identifiants de noeuds du DAG ont une portee noeud, pas mandat ; l'identifiant de session
   designe une session, pas un mandat — et il change sur certains gestes du harness.

2. **Comment l'obtenir.** Nommer la commande exacte qui rend l'etat du verrou en JSON, et la cle a
   lire. Donner la forme de lecture recommandee (extraction par interprete, pas par decoupage de
   texte), et rappeler qu'un lock absent ne porte pas de generation — un commit hors mandat n'a
   donc pas de trailer a poser, et c'est un cas normal, pas une omission.

3. **La convention.** Le manager, ou le worker qui commite pour son compte, ajoute au message de
   commit un trailer `Fence:` suivi de la generation courante. Preciser la position (dans le bloc de
   trailers, aux cotes de ceux deja en usage), et le fait qu'un commit peut evidemment porter
   plusieurs trailers.

4. **Le TIER de cette convention, ecrit noir sur blanc.** Elle est exactement au meme niveau que les
   deux trailers deja en usage dans ce depot : posee par convention d'agent, jamais posee ni
   verifiee par une machine. Donner les comptes MESURES de ces deux trailers sur les 300 derniers
   commits, comme preuve que ce tier fonctionne en pratique — et comme mesure honnete de ce qu'il
   vaut : une convention tenue par des agents, pas une garantie.

5. **Ce qui n'est PAS construit, et pourquoi.** Aucun hook git de message de commit : ce depot arme
   deja un chemin de hooks git pour son garde-fou de push, et tout hook supplementaire non versionne
   entre en conflit avec lui, sans etre distribuable aux labs qui installent le module. Aucun gate
   qui parse les trailers : le moteur de merge de hooks ne connait que les hooks du harness, pas les
   hooks git natifs — en construire un serait une extension de capacite du moteur, et LOCK-05 exige
   litteralement « auditable », pas « bloque a la source ». Nommer les deux extensions remontees
   comme telles (audit outille non bloquant, hook de message de commit distribue), pour qu'un futur
   lecteur sache qu'elles ont ete considerees et differees, pas oubliees.

6. **La recette d'audit.** Comment repondre concretement a « quel commit sous quel mandat » avec ce
   qui existe apres la Phase 32 : d'un cote, la selection des commits par leur trailer ; de l'autre,
   le journal d'evenements du verrou, qui dit quand une generation a change de main et au profit de
   qui. Nommer le chemin exact du journal et le champ qui porte la generation. Dire ce que le
   recoupement permet de conclure — et ce qu'il ne permet pas : un commit SANS trailer n'est pas la
   preuve d'un commit hors mandat, seulement l'absence de preuve, puisque rien ne pose le trailer a
   la place de l'agent.

Style : celui du document — prose dense, tableaux quand la forme s'y prete, motif systematiquement
adosse a une mesure ou a une decision datee. Densite : la section reste sous 60 lignes.
  </action>
  <acceptance_criteria>
    - La section existe et porte son titre : `grep -c 'Jeton de fence' plugin/conductor/references/team-kernel.md` rend au moins `1`.
    - Le nom du trailer est ecrit tel qu'il sera pose : `grep -c 'Fence:' plugin/conductor/references/team-kernel.md` rend au moins `1`.
    - La limite est ecrite, pas maquillee : la section contient une phrase disant explicitement que rien ne verifie ce trailer par machine — le verifier a la lecture et citer la phrase exacte dans le SUMMARY.
    - Les deux extensions differees sont nommees dans la section.
    - Densite respectee : la section fait moins de 60 lignes (mesurer par extraction entre son titre et le titre suivant).
    - La recette d'audit est EXECUTABLE, pas decorative : la commande de selection de commits documentee s'execute sans erreur sur ce depot et rend un code de sortie exploitable (zero resultat est un resultat valide a ce stade — aucun commit ne porte encore le trailer). Consigner la commande exacte et son resultat dans le SUMMARY.
    - La commande de lecture de la generation documentee s'execute reellement : poser un lock de sonde par `driver-lock.sh acquire`, executer la lecture documentee, obtenir une valeur NON VIDE, puis relacher le lock et verifier que `git status --porcelain -- .planning/` est strictement vide.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Jeton de fence' plugin/conductor/references/team-kernel.md && grep -q 'Fence:' plugin/conductor/references/team-kernel.md && bash plugin/conductor/scripts/tests/test-conductor.sh</automated>
  </verify>
  <done>La convention de fence est ecrite la ou tous les managers de mission declarent lire leur contrat invariant, avec sa source, son tier reel, ses non-constructions motivees, et une recette d'audit qui s'execute.</done>
  <reversibility rating="reversible">Document seul, aucun code. Un `git revert` suffit ; aucun artefact ne survit, et un commit portant deja le trailer reste parfaitement valide (un trailer inconnu est inerte).</reversibility>
</task>

<task type="auto">
  <name>Tache 2 : remettre a l'heure la ligne de brique du verrou (les verbes de la Phase 32)</name>
  <files>plugin/conductor/references/team-kernel.md</files>
  <read_first>
    - plugin/conductor/references/team-kernel.md:16-18 (le tableau des briques : la ligne « Verrou de driver » et sa colonne « Garantie »)
    - plugin/conductor/references/team-kernel.md:192-196 (la phrase qui fait du verrou le seul garant machine, et qui cite le cas T2 de la suite — verifier que la citation reste exacte apres les renumerotations des plans 32-01 et 32-02)
    - plugin/conductor/scripts/driver-lock.sh (liste finale des verbes et leur signature, apres 32-02)
    - 32-CONTEXT.md §Decisions D-32-02 (ce que la garantie devient : reprise EXPLICITE et tracee, jamais d'auto-steal)
  </read_first>
  <action>
1. Mettre a jour la ligne « Verrou de driver » du tableau des briques : la colonne des verbes cite
   aujourd'hui trois verbes et une « recovery » ; elle doit citer les verbes reellement offerts
   apres la Phase 32, y compris les deux nouveaux. La colonne « Garantie » doit dire ce qui a
   CHANGE : une seule mission pilote a la fois, ET la reprise d'un lock perime est desormais un
   geste explicite et trace, jamais un effet de bord d'une acquisition ordinaire. Retirer toute
   formulation qui laisserait croire a une recuperation automatique.

2. Verifier la phrase des etages croises qui cite un cas nomme de la suite du verrou comme
   couverture continue de l'invariant. Les plans 32-01 et 32-02 ont ajoute et RENVERSE des cas :
   si le cas cite a change de numero, de nom ou de contenu, corriger la citation. Une reference de
   test qui ne pointe plus rien est une garantie de papier — c'est exactement le mode de
   defaillance que cette phase ferme ailleurs.

3. Ajouter, dans cette meme phrase ou immediatement apres, la mention que le garant machine s'etend
   desormais au-dela de l'acquisition : le guard `PreToolUse` du plan 32-03 porte l'invariant
   jusqu'aux gestes git et aux ecritures de planification d'une session tierce. Formuler la portee
   HONNETEMENT, avec sa limite de granularite : le guard garantit qu'aucune AUTRE session ne
   commite sous le lock, jamais qu'aucun autre acteur de la MEME session ne le fait.
  </action>
  <acceptance_criteria>
    - Les deux verbes neufs figurent dans le tableau des briques : `sed -n '/Verrou de driver/p' plugin/conductor/references/team-kernel.md | grep -c 'takeover'` rend `1`, et la meme extraction contient `reclaim`.
    - Aucune promesse de recuperation automatique ne subsiste dans cette ligne : l'extraction de la ligne ne contient plus de formulation d'auto-recuperation — le verifier a la lecture et citer l'ancienne et la nouvelle formulation dans le SUMMARY.
    - Toute reference a un cas nomme de la suite du verrou pointe un cas qui EXISTE : pour chaque identifiant de cas cite dans le document, `grep -c '=== <identifiant>' plugin/conductor/scripts/tests/test-driver-lock.sh` rend au moins `1`. Consigner la liste des identifiants verifies dans le SUMMARY.
    - La limite de granularite du guard est ecrite dans le document.
    - `bash plugin/conductor/scripts/tests/test-conductor.sh` sort 0 et `bash plugin/conductor/scripts/tests/test-doc-and-commands.sh` sort 0 (non-regression des gates documentaires du module).
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-conductor.sh && bash plugin/conductor/scripts/tests/test-doc-and-commands.sh && sed -n '/Verrou de driver/p' plugin/conductor/references/team-kernel.md | grep -q 'takeover'</automated>
  </verify>
  <done>Le contrat invariant lu par tous les managers decrit le verrou tel qu'il est apres la Phase 32, et chaque cas de test qu'il cite comme preuve existe reellement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| convention documentee -> comportement reel des agents | rien ne force l'application du trailer ; la frontiere est franchie par la seule discipline d'agent, exactement comme pour les deux trailers deja en usage |
| trailer d'un commit -> auditeur humain | un trailer est une donnee auto-declaree : il prouve une intention declaree, jamais un fait verifie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-20 | Repudiation | trailer non pose | medium | accept | consequence ASSUMEE et ecrite : le trailer est auditable a la main, jamais verifie machine ; l'absence de trailer n'est pas une preuve de commit hors mandat, seulement une absence de preuve — la section le dit explicitement |
| T-32-21 | Spoofing | trailer falsifie | low | accept | un agent peut ecrire n'importe quelle generation ; le recoupement au journal d'evenements du verrou permet de detecter une incoherence grossiere, sans jamais constituer une preuve cryptographique — hors du texte litteral de LOCK-05 |
| T-32-22 | Tampering | citation de test perimee dans la doctrine | medium | mitigate | critere d'acceptation qui verifie, par grep, que chaque identifiant de cas cite existe reellement dans la suite du verrou |
| T-32-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installe par cette phase — aucun gate de legitimite applicable |
</threat_model>

<verification>
1. `grep -q 'Jeton de fence' plugin/conductor/references/team-kernel.md`
2. La recette d'audit documentee s'execute reellement (commande et resultat consignes dans le SUMMARY)
3. La lecture documentee de la generation rend une valeur non vide sur un lock de sonde, puis `git status --porcelain -- .planning/` est vide
4. Chaque identifiant de cas de test cite par le document existe dans `test-driver-lock.sh`
5. `bash plugin/conductor/scripts/tests/test-conductor.sh` et `bash plugin/conductor/scripts/tests/test-doc-and-commands.sh`
</verification>

<success_criteria>
- Le jeton de fence a une source lisible, une convention ecrite la ou tous les managers la lisent, et une recette d'audit executable.
- La limite (auditable a la main, jamais verifie machine) est ecrite noir sur blanc, avec le motif du refus de construire l'enforcement et les deux extensions differees nommees.
- Le contrat invariant ne decrit plus une recuperation automatique qui n'existe plus.
</success_criteria>

<output>
Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-04-SUMMARY.md` en fin de plan, avec :
la phrase exacte qui ecrit la limite, la commande d'audit executee et son resultat, la valeur de
generation lue sur le lock de sonde, et la liste des identifiants de cas de test verifies.
</output>

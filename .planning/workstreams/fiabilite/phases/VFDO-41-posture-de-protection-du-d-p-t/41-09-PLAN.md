---
phase: 41-posture-de-protection-du-d-p-t
plan: 09
lot: L9 — refus serveur d'un push direct sur `main` par un acteur hors liste (D-03) et mesure M-4 du ruleset de tags `v*`, pour un acteur hors liste et pour un acteur de la liste
type: execute
wave: 9
depends_on: ["41-08"]
autonomous: false
requirements: [PROT-01, PROT-02]
files_modified:
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Précondition : `M3-VERDICT: CONFORME`, ou `M3-DECISION:` d'option `fermer-et-poursuivre` ou `consigner-et-poursuivre` ; PR-P non ouverte ; hors PR en vol de `PR-EN-VOL-APRES:`, aucune PR ouverte"
    - "Samuel et Willy sont tous deux dans la liste de contournement en mode `always` (D-02bis) : un push direct de leur part sur `main` serait ACCEPTÉ et atterrirait — il n'est jamais tenté ; cette conséquence est écrite dans ADR-072 (41-03), pas rejouée"
    - "D-03 est prouvé par un essai réel d'un acteur HORS liste : une clé de déploiement en écriture, TEMPORAIRE — générée hors dépôt, ajoutée, utilisée, retirée et effacée dans le même geste humain de Willy. Le push direct d'un commit vide (arbre identique à `main`) sur `refs/heads/main` par cette clé est attendu refusé, sortie intégrale capturée. PROVENANCE : `client_hook` si la sortie porte `[pre-push]`, `serveur` si elle porte des lignes `remote:`, `indeterminee` sinon — un refus client n'est jamais écrit `serveur`. `D03-VERDICT: CONFORME` exige un refus de provenance `serveur` mentionnant la PR exigée et `main` distante inchangée ; sinon arrêt sur décision"
    - "M-4 en deux volets, tag de test `v0.0.0-preuve-41` : (a) acteur hors liste (la clé) — création acceptée (création libre, D-07), réécriture refusée, suppression refusée ; (b) acteur de la liste (Willy) — réécriture et suppression acceptées AVEC une rule suite `bypass` ; la suppression par Willy est aussi le nettoyage"
    - "Relecture des traces : quand `ACCES-RULE-SUITES: picmakpro=ok`, chaque suite consignée est relue par l'agent via l'API (`relue=oui`) ; sinon marquée `relue=non_relue` — jamais de vérification sur une valeur que l'agent a lui-même écrite"
    - "La clé de déploiement est retirée et relue absente par l'API (`GET /repos/{owner}/{repo}/keys`) ; sa clé privée ne touche jamais le dépôt ; aucun tag ni commit de test ne reste"
    - "Le tag de test ne peut pas être pris pour une release : `plugin/conductor/scripts/check-plugin-update.sh` ne retient que `^v[0-9]+\\.[0-9]+\\.[0-9]+$` ; aucune release GitHub n'est créée pour lui"
    - "Préalable : `VERSION` d'`origin/main` taggée et publiée, et `core.hooksPath` du poste relevé, pour qu'un hook `pre-push` ne masque pas le refus serveur par un refus client"
    - "Tout commit (commit vide de preuve compris) et tout corps de PR citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », citant D-02bis nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 »"
  artifacts:
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`M4-OBJETS:`, `HOOKS-POSTE:`, `PUSH-DIRECT:`, `PUSH-DIRECT-MSG:`, `D03-VERDICT:`, `M4-CREATION:`, `M4-UPDATE-HORS-LISTE:`, `M4-DELETION-HORS-LISTE:`, `M4-UPDATE:`, `M4-DELETION:`, `M4-NETTOYAGE:`, `CLE-DEPLOIEMENT:`, `M4-VERDICT:`, `M4-MSG:`"
  key_links:
    - "M-4 conditionne PROT-02 : la création libre du tag de release (plan 41-12) doit passer sans contournement"
---

<objective>
Prouver que `main` refuse un push direct d'un acteur hors liste de contournement, et mesurer la
combinaison réelle des règles du ruleset de tags pour un acteur hors liste et pour un acteur de la
liste, trace comprise, sans laisser aucun artefact.

Révisé le 2026-09-23 (reprise) : sous D-02bis, Samuel et Willy contournent en `always` — le push
direct « refusé » de l'ancienne version (par `samuel-neveugall` en mode `pull_request`) serait
aujourd'hui accepté et atterrirait sur `main`. L'acteur hors liste devient une clé de déploiement
temporaire (discrétion de D-M7 : « le plan choisit le support »).

Purpose: D-03 (PR obligatoire) côté serveur ; inconnu M-4 (D-07, D-02bis) ; compatibilité du tag de
release (PROT-02).
Output: lignes `PUSH-DIRECT:`, `D03-VERDICT:` et `M4-*` consignées ; aucun tag, commit ni clé de test restant.

Budget : quatre tâches, dont la quatrième est un checkpoint de décision CONDITIONNEL, sans exécution
quand `D03-VERDICT` et `M4-VERDICT` sont `CONFORME`. Fichiers : un seul.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-08-SUMMARY.md
@scripts/hooks/pre-push

Sources : available-rules-for-rulesets (restrict updates / deletions / block force pushes, chaque
règle documentée, leur combinaison déduite) ; `rest/repos/rule-suites` ; `rest/repos/rules`
(relue le 2026-09-23 : `DeployKey` est un type d'acteur distinct de `User` — une clé de déploiement
absente de `bypass_actors` n'a aucun contournement) ; `gh repo deploy-key add --allow-write --title`,
`gh repo deploy-key delete <id>` (aide de gh 2.83.2). Constat du 2026-09-23 : aucune clé de
déploiement sur le dépôt ; `core.hooksPath` non configuré sur le poste de Willy.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : préalables relus, objets de test préparés en local (commit vide, tag annoté), état des hooks du poste</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - plugin/conductor/scripts/check-plugin-update.sh (filtre du dernier tag publié)
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`TRACE-II:`, `ACCES-RULE-SUITES:`, `M3-VERDICT:`, `M3-DECISION:`, `PR-EN-VOL-APRES:`)
  </read_first>
  <action>
Identité `picmakpro` ; `git fetch origin --tags`. Vérifier : `TRACE-II:` consignée ;
`M3-VERDICT: CONFORME` ou `M3-DECISION:` d'option `fermer-et-poursuivre` / `consigner-et-poursuivre`
(sinon arrêt) ; PR-P non ouverte ; hors PR en vol, aucune PR ouverte ; aucun tag distant
`v0.0.0-preuve-41` ; aucune clé de déploiement titrée `preuve-41-temporaire` ; la `VERSION`
d'`origin/main` a son tag publié et sa release GitHub (sinon arrêt).

Préparer sans toucher l'arbre de travail :
- `D` = `git commit-tree "origin/main^{tree}" -p origin/main` avec le message « PREUVE 41 — push
  direct sur main par un acteur hors liste, attendu refusé (D-03 — arbitrage Samuel, AskUserQuestion
  session principale, 2026-09-17) » ;
- tag annoté local `v0.0.0-preuve-41` sur `origin/main`, message « PREUVE 41 — tag de test M-4, à
  supprimer » ;
- `U` = sha de `origin/main~1` (cible de la réécriture).
Consigner une section `## 41-09 — push direct et tags`, puis
`M4-OBJETS: push_direct=<D> tag_cible_creation=<origin/main> tag_cible_reecriture=<U> main_avant=<origin/main>` et
`HOOKS-POSTE: core_hooks_path=<valeur lue ou non_configure> pre_push_actif=<oui|non>`.
Commit local sur B, message « docs(41-09): objets de test du push direct et du tag préparés ».
Imprimer les commandes de la Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); al=$(val PR-EN-VOL-APRES: numeros); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq ".[].number" | awk -v al=",$al," "{ if (index(al, \",#\" \$1 \",\") == 0) c++ } END{print c+0}"); tr=$(awk "\$1==\"TRACE-II:\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); D=$(val M4-OBJETS: push_direct); U=$(val M4-OBJETS: tag_cible_reecriture); MA=$(val M4-OBJETS: main_avant); o=$(git rev-parse origin/main); te=$([ "$(git rev-parse "$D^{tree}")" = "$(git rev-parse "origin/main^{tree}")" ] && [ "$(git rev-parse "$D^")" = "$o" ] && echo oui || echo non); tl=$(git cat-file -t refs/tags/v0.0.0-preuve-41 2>/dev/null || echo absent); tc=$(git rev-parse "refs/tags/v0.0.0-preuve-41^{commit}" 2>/dev/null || echo absent); T=$(mktemp -d); git ls-remote --tags origin "refs/tags/v0.0.0-preuve-41*" > "$T/td"; td=$(awk "END{print NR}" "$T/td"); ue=$([ "$U" = "$(git rev-parse origin/main~1)" ] && echo oui || echo non); git show origin/main:VERSION > "$T/v"; vm=$(tr -d "v[:space:]" < "$T/v"); git ls-remote --tags origin "refs/tags/v$vm" > "$T/tg"; tg=$(awk "END{print NR}" "$T/tg"); rl=$(gh release view "v$vm" --repo picmakpro/vibeflow-os --json tagName --jq .tagName 2>/dev/null || echo absente); dk=$(gh api repos/picmakpro/vibeflow-os/keys --jq "[.[] | select(.title == \"preuve-41-temporaire\")] | length"); hk=$(awk "\$1==\"HOOKS-POSTE:\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "prealables M-4 : identite=$u prs_hors_vol=$op trace_ii=$tr commit_vide_sur_main=$te tag_local=$tl tag_sur=$([ "$tc" = "$o" ] && echo main || echo autre) tag_distant=$td cible_reecriture_ok=$ue main_avant_ok=$([ "$MA" = "$o" ] && echo oui || echo non) version_main=$vm tag_version_publie=$tg release=$rl cle_de_test_existante=$dk hooks_consignes=$hk"; [ "$u" = picmakpro ] && [ "$op" -eq 0 ] && [ "$tr" -eq 1 ] && [ "$te" = oui ] && [ "$tl" = tag ] && [ "$tc" = "$o" ] && [ "$td" -eq 0 ] && [ "$ue" = oui ] && [ "$MA" = "$o" ] && [ "$tg" -eq 1 ] && [ "$rl" = "v$vm" ] && [ "$dk" -eq 0 ] && [ "$hk" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou, hors PR en vol, une PR ouverte ; ou `TRACE-II:` absente ; ou commit vide non fils d'`origin/main` ou d'arbre différent ; ou tag local absent, non annoté, ou hors `origin/main` ; ou tag déjà présent chez GitHub ; ou cible de réécriture fausse ; ou `main_avant` périmé ; ou `VERSION` d'`origin/main` sans tag publié ou sans release GitHub (un hook `pre-push` câblé refuserait côté client et masquerait le refus serveur) ; ou une clé de déploiement de test déjà présente ; ou `HOOKS-POSTE:` non consignée</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; mv=$(awk "\$1==\"M3-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); md=$(val M3-DECISION: option); N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); ps=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); hp=$(git config --get core.hooksPath || echo non_configure); hc=$(val HOOKS-POSTE: core_hooks_path); echo "precondition M-3 : verdict=$mv decision=$md pr_p=$ps hooks_path=$hp consigne=$hc"; { [ "$mv" = CONFORME ] || [ "$md" = fermer-et-poursuivre ] || [ "$md" = consigner-et-poursuivre ]; } && [ "$ps" != OPEN ] && [ "$hp" = "$hc" ]'</automated>
    <fails_when>exit non nul ; ou ni `M3-VERDICT: CONFORME` ni décision de poursuite consignée ; ou PR-P encore ouverte (41-09 attend sa résolution, cf. Task 4 de 41-08) ; ou `core.hooksPath` consigné différent de la valeur relue</fails_when>
  </verify>
  <acceptance_criteria>
    - commit vide `D` sur `origin/main`, tag annoté local `v0.0.0-preuve-41` sur `origin/main`, cible `U` = `origin/main~1`
    - hors PR en vol, aucune PR ouverte ; aucun tag de test distant ; aucune clé de test ; `VERSION` de `main` taggée et publiée ; hooks du poste consignés
  </acceptance_criteria>
  <done>Les objets de test sont prêts, inoffensifs, et aucun refus client ne peut masquer un refus serveur.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains de Willy (`picmakpro`) — clé de déploiement temporaire ; push direct et tags par la clé (hors liste) ; réécriture et suppression du tag par Willy (liste) ; retrait de la clé</name>
  <read_first>
    - les SHA `<D>`, `<U>` et les commandes imprimés par la Task 1
  </read_first>
  <action>Willy, compte `picmakpro`, crée une clé de déploiement temporaire hors dépôt, tente avec elle un push direct du commit vide sur `main`, crée puis tente de réécrire et de supprimer le tag de test, réécrit et supprime ensuite ce tag avec son propre compte, puis retire la clé et l'efface. Aucun push direct sur `main` n'est tenté avec le compte de Willy ni de Samuel (il serait accepté).</action>
  <instructions>
Compte qui agit : `picmakpro` (témoin `gh api user --jq .login`). Jamais `--no-verify`. SHA `<D>` et
`<U>` imprimés par la Task 1. Tous les chemins de clé sont dans un répertoire temporaire HORS dépôt.

0. Clé temporaire :
   `K=$(mktemp -d) && ssh-keygen -t ed25519 -N "" -q -C preuve-41-temporaire -f "$K/cle"`
   `gh repo deploy-key add "$K/cle.pub" --repo picmakpro/vibeflow-os --allow-write --title preuve-41-temporaire`
   Témoin : `gh api repos/picmakpro/vibeflow-os/keys --jq '.[] | select(.title == "preuve-41-temporaire") | {id, read_only}'` → un id, `read_only: false`.
   `export GIT_SSH_COMMAND="ssh -i $K/cle -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new"` ;
   accès : `ssh -i "$K/cle" -o IdentitiesOnly=yes -T git@github.com` → « Hi picmakpro/vibeflow-os! … »
   (port 22 fermé : utiliser `ssh.github.com` port 443 et l'URL `ssh://git@ssh.github.com:443/picmakpro/vibeflow-os.git`
   aux étapes 1 à 4). URL des étapes 1 à 4 : `git@github.com:picmakpro/vibeflow-os.git` (désignée `<URL-CLE>`).
1. Push direct par la clé (acteur hors liste) : `git push <URL-CLE> <D>:refs/heads/main` → copier la
   sortie intégrale (refus attendu). Témoin : `git ls-remote origin refs/heads/main` → ne doit PAS être `<D>`.
   Si la sortie commence par `[pre-push]` : s'arrêter et le signaler, sans contourner le hook.
   Si le push est ACCEPTÉ : s'arrêter là, le signaler (ne rien tenter pour l'annuler), puis aller à 7.
2. Création du tag par la clé : `git push <URL-CLE> refs/tags/v0.0.0-preuve-41` → copier la sortie (acceptation attendue).
3. Réécriture par la clé : `git push --force <URL-CLE> <U>:refs/tags/v0.0.0-preuve-41` → copier la sortie (refus attendu).
4. Suppression par la clé : `git push <URL-CLE> :refs/tags/v0.0.0-preuve-41` → copier la sortie (refus attendu).
5. `unset GIT_SSH_COMMAND`. Réécriture par Willy (liste) : `git push --force origin <U>:refs/tags/v0.0.0-preuve-41`
   → copier la sortie (acceptation attendue, avec un avertissement de contournement).
6. Suppression par Willy (et nettoyage) : `git push origin :refs/tags/v0.0.0-preuve-41` → copier la sortie.
   Témoin : `git ls-remote --tags origin 'refs/tags/v0.0.0-preuve-41*'` → vide.
7. Retrait de la clé : `gh repo deploy-key delete <id> --repo picmakpro/vibeflow-os` puis `rm -rf "$K"`.
   Témoin : `gh api repos/picmakpro/vibeflow-os/keys --jq '[.[] | select(.title == "preuve-41-temporaire")] | length'` → `0`.
8. Seulement si `ACCES-RULE-SUITES: picmakpro=refuse` (sinon l'exécutant relit lui-même) :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour&rule_suite_result=all" --jq '.[] | select(.ref == "refs/heads/main" or .ref == "refs/tags/v0.0.0-preuve-41") | {id, ref, actor_name, before_sha, after_sha, result}'`.
Si 5 ou 6 est REFUSÉ : copier le message ; le nettoyage passe alors par la désactivation temporaire
du ruleset de tags (`gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id_tags> -f enforcement=disabled`),
la suppression, puis la réactivation (`-f enforcement=active`) — chaque geste copié.
  </instructions>
  <verification>La Task 3 relit `main` distante, l'absence du tag et de la clé chez GitHub, et relit les suites (ou les marque non relues).</verification>
  <acceptance_criteria>
    - sorties intégrales des étapes 1 à 6 collées, témoins compris ; clé de déploiement retirée (témoin `0`) et répertoire temporaire effacé ; aucun push direct sur `main` par un compte de la liste
  </acceptance_criteria>
  <resume-signal>Coller les sorties de 0 à 8 (témoins compris), puis écrire « push direct et tags joués ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : consignation et verdicts D-03 et M-4, nettoyage et retrait de la clé vérifiés</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `picmakpro`. Supprimer le tag local `v0.0.0-preuve-41`. Consigner :
- `PUSH-DIRECT: acteur=cle_de_deploiement sha=<D> refuse=<oui|non> provenance=<serveur|client_hook|indeterminee> msg_mentionne_pr=<oui|non> main_distante_inchangee=<oui|non> suite=<id|absente> suite_result=<fail|…|absente> relue=<oui|non_relue>` et `PUSH-DIRECT-MSG:` (sortie intégrale, une ligne) ;
- `D03-VERDICT: <CONFORME|ECART>` — CONFORME si refus, provenance `serveur`, PR exigée mentionnée, `main` inchangée ;
- `M4-CREATION: acteur=cle_de_deploiement accepte=<oui|non> suite=<id|absente> suite_result=<pass|bypass|…|absente> relue=<oui|non_relue>` ;
- `M4-UPDATE-HORS-LISTE: acteur=cle_de_deploiement accepte=<oui|non> suite=<id|absente> suite_result=<> relue=<oui|non_relue>` et `M4-DELETION-HORS-LISTE:` symétrique ;
- `M4-UPDATE: acteur=picmakpro accepte=<oui|non> suite=<id|absente> suite_result=<> regles_en_echec=<liste|aucune> relue=<oui|non_relue>` et `M4-DELETION:` symétrique ;
- `M4-NETTOYAGE: distant=<absent|present> local=<absent|present>` et `CLE-DEPLOIEMENT: titre=preuve-41-temporaire retiree=<oui|non>` (lu via `GET …/keys`) ;
- `M4-MSG:` (sorties 2 à 6, une ligne) ;
- `M4-VERDICT: <CONFORME|ECART>` — CONFORME si création par la clé acceptée sans suite `bypass`,
  réécriture ET suppression par la clé refusées, réécriture ET suppression par Willy acceptées avec
  suite `bypass`, nettoyage complet et clé retirée ; ECART sinon.
`msg_mentionne_pr=oui` seulement si la sortie collée contient « pull request » (casse ignorée).
`provenance` suit la règle du frontmatter. Si `ACCES-RULE-SUITES: picmakpro=ok`, relire soi-même
chaque id (le choisir par `ref` et `after_sha`/`before_sha`) et écrire `relue=oui` ; sinon
`relue=non_relue`. Commit local sur B, message « docs(41-09): push direct hors liste et ruleset de
tags mesurés (D-03, D-07 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ;
D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) ». Rejouer `gates` et
`tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; D=$(val M4-OBJETS: push_direct); T=$(mktemp -d); git ls-remote origin refs/heads/main > "$T/rm"; rm=$(awk "{print \$1}" "$T/rm"); [ -n "$rm" ] || { echo "ls-remote main vide"; exit 1; }; inch=$([ "$rm" != "$D" ] && echo oui || echo non); mp=$(awk "\$1==\"PUSH-DIRECT-MSG:\" { l=tolower(\$0); if (index(l, \"pull request\")) c++ } END{print c+0}" "$PH/41-PREUVES.md"); pc=$(awk "\$1==\"PUSH-DIRECT-MSG:\" { l=tolower(\$0); if (index(l, \"[pre-push]\")) r=\"client_hook\"; else if (index(l, \"remote:\")) r=\"serveur\"; else r=\"indeterminee\"; print r; exit }" "$PH/41-PREUVES.md"); ac=$(val PUSH-DIRECT: acteur); rf=$(val PUSH-DIRECT: refuse); pv=$(val PUSH-DIRECT: provenance); mc=$(val PUSH-DIRECT: msg_mentionne_pr); mi=$(val PUSH-DIRECT: main_distante_inchangee); att=$([ "$rf" = oui ] && [ "$pc" = serveur ] && [ "$mp" -ge 1 ] && [ "$inch" = oui ] && echo CONFORME || echo ECART); v=$(awk "\$1==\"D03-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); echo "D-03 push direct : acteur=$ac refuse=$rf provenance=$pv (recalculee=$pc) msg_mentionne_pr=$mc (recalcule=$([ "$mp" -ge 1 ] && echo oui || echo non)) main_distante_inchangee=$mi (recalcule=$inch) verdict attendu=$att consigne=$v"; [ -n "$D" ] && [ "$ac" = cle_de_deploiement ] && [ -n "$pc" ] && [ "$pv" = "$pc" ] && [ "$mc" = "$([ "$mp" -ge 1 ] && echo oui || echo non)" ] && [ "$mi" = "$inch" ] && [ "$v" = "$att" ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou acteur consigné autre que la clé de déploiement (un push direct d'un compte de la liste serait accepté et ne prouve rien) ; ou provenance, mention de la PR ou état de `main` consignés différents du recalcul (erreur de consignation, corrigée ici) ; ou verdict consigné différent du verdict recalculé ; ou `D03-VERDICT: ECART` — push accepté, refus seulement côté client (`client_hook`) ou de provenance indéterminée, PR exigée non mentionnée, ou `main` changée : consigné tel quel, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; ca=$(val M4-CREATION: accepte); cr=$(val M4-CREATION: suite_result); ha=$(val M4-UPDATE-HORS-LISTE: accepte); da=$(val M4-DELETION-HORS-LISTE: accepte); ua=$(val M4-UPDATE: accepte); ur=$(val M4-UPDATE: suite_result); wa=$(val M4-DELETION: accepte); wr=$(val M4-DELETION: suite_result); nd=$(val M4-NETTOYAGE: distant); nl=$(val M4-NETTOYAGE: local); kr=$(val CLE-DEPLOIEMENT: retiree); T=$(mktemp -d); git ls-remote --tags origin "refs/tags/v0.0.0-preuve-41*" > "$T/rd"; rd=$(awk "END{print (NR==0 ? \"absent\" : \"present\")}" "$T/rd"); rl=$(git rev-parse -q --verify refs/tags/v0.0.0-preuve-41 > /dev/null && echo present || echo absent); dk=$(gh api repos/picmakpro/vibeflow-os/keys --jq "[.[] | select(.title == \"preuve-41-temporaire\")] | length"); ka=$([ "$dk" -eq 0 ] && echo oui || echo non); if [ "$ca" = oui ] && [ "$cr" != bypass ] && [ "$ha" = non ] && [ "$da" = non ] && [ "$ua" = oui ] && [ "$ur" = bypass ] && [ "$wa" = oui ] && [ "$wr" = bypass ] && [ "$rd" = absent ] && [ "$rl" = absent ] && [ "$ka" = oui ]; then att=CONFORME; else att=ECART; fi; v=$(awk "\$1==\"M4-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); msg=$(awk "\$1==\"M4-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "M-4 : creation_cle=$ca/$cr reecriture_cle=$ha suppression_cle=$da reecriture_willy=$ua/$ur suppression_willy=$wa/$wr nettoyage_consigne=$nd/$nl nettoyage_releve=$rd/$rl cle_retiree=$kr (api $ka) attendu=$att consigne=$v message=$msg"; [ "$nd" = "$rd" ] && [ "$nl" = "$rl" ] && [ "$kr" = "$ka" ] && [ "$v" = "$att" ] && [ "$msg" -eq 1 ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou nettoyage consigné différent du relevé (tag de test encore présent chez GitHub ou en local) ; ou clé de déploiement encore présente chez GitHub, ou consignation différente ; ou verdict consigné différent du verdict recalculé ; ou message non capturé ; ou verdict `ECART` — création refusée, réécriture ou suppression ACCEPTÉE pour la clé (règle de tags non effective hors liste), réécriture ou suppression par Willy refusée ou sans suite `bypass` : consigné tel quel, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; acc=$(val ACCES-RULE-SUITES: picmakpro); k=0; n=0; for L in PUSH-DIRECT M4-CREATION M4-UPDATE-HORS-LISTE M4-DELETION-HORS-LISTE M4-UPDATE M4-DELETION; do id=$(val "$L:" suite); r=$(val "$L:" suite_result); rl=$(val "$L:" relue); n=$((n+1)); if [ "$acc" = ok ] && [ -n "$id" ] && [ "$id" != absente ]; then a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$id" --jq .result); echo "$L suite=$id consigne=$r api=$a relue=$rl"; { [ "$a" = "$r" ] && [ "$rl" = oui ]; } || k=$((k+1)); else echo "$L suite=$id consigne=$r relue=$rl (acces=$acc)"; [ "$rl" = non_relue ] || k=$((k+1)); fi; done; echo "relecture des traces : lignes=$n en_defaut=$k"; [ "$k" -eq 0 ] && [ "$n" -eq 6 ]'</automated>
    <fails_when>exit non nul ; ou, avec accès, un résultat consigné différent de la relecture API ou `relue` différent de `oui` ; ou, sans accès (ou suite absente), `relue` différent de `non_relue` (une valeur collée ne peut pas être dite relue par l'agent)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - push direct de la clé (hors liste) refusé, provenance `serveur` recalculée sur la sortie intégrale, message mentionnant la PR exigée, `main` inchangée, `D03-VERDICT: CONFORME`
    - M-4 : création par la clé acceptée sans contournement ; réécriture et suppression refusées pour la clé ; acceptées avec suite `bypass` pour Willy ; verdict recalculé = consigné
    - chaque suite consignée relue via l'API (accès mesuré) ou marquée `non_relue`
    - tag de test absent chez GitHub et en local ; clé de déploiement retirée (API) ; aucun push direct tenté par un compte de la liste
  </acceptance_criteria>
  <done>D-03 est prouvé côté serveur pour un acteur hors liste, et le ruleset de tags est mesuré tel qu'il se comporte pour les deux sortes d'acteurs, trace comprise.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `D03-VERDICT: ECART` ou `M4-VERDICT: ECART` (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`PUSH-DIRECT:`, `PUSH-DIRECT-MSG:`, `D03-VERDICT:`, `M4-*`, `CLE-DEPLOIEMENT:`)
  </read_first>
  <action>L'exécutant présente la mesure et s'arrête ; aucune correction chez GitHub sans la réponse.</action>
  <decision>Le comportement mesuré contredit-il un arbitrage (D-03 pour le push direct hors liste, D-07/D-02bis pour les tags), et que faire avant la release ?</decision>
  <context>
    Aucune PR de preuve n'est ouverte à ce stade. Si `PUSH-DIRECT: refuse=non`, le commit de preuve `D`
    (vide, arbre identique à `main`) est entré sur `main` et la réponse DOIT AUSSI fixer son sort :
    `REVERT PAR PR` — branche `fix/phase-41-retrait-commit-preuve` depuis `origin/main`,
    `git revert --no-edit <D>` (si git refuse faute de changement, commit explicite
    `git commit --allow-empty -m "revert: retrait du commit de preuve <D> (Phase 41)"`), PR publiée
    et mergée par Willy sans contournement — ou `CONSERVATION DÉCLARÉE` — le commit reste dans
    l'historique de `main`, déclaré tel quel dans `41-PREUVES.md` et ADR-072 § Mesures. Sans push
    accepté, ce second choix est `sans_objet`. Si la clé n'a pas pu se connecter (port fermé des
    deux côtés), le refus n'est pas établi. Le planificateur ne tranche pas.
  </context>
  <options>
    <option id="arret-et-cadrage">
      <name>ARRÊT ET CADRAGE</name>
      <pros>Rien n'est modifié sans arbitrage ; l'écart est consigné avec sa mesure.</pros>
      <cons>La suite (41-10 à 41-13) attend.</cons>
    </option>
    <option id="accepter-et-documenter">
      <name>ACCEPTER ET DOCUMENTER</name>
      <pros>La release avance ; ADR-072 écrit le comportement réel mesuré.</pros>
      <cons>Une forme d'arbitrage n'est pas tenue telle qu'écrite ; exige un arbitrage explicite, daté et attribué.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE DU RULESET CONCERNÉ</name>
      <pros>Rien d'imposé à tort (`enforcement=disabled`).</pros>
      <cons>La protection correspondante disparaît jusqu'au cadrage.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - réponse explicite obtenue, jamais inférée ; `D03-M4-DECISION:` consignée (sort du commit de preuve compris) et committée
  </acceptance_criteria>
  <resume-signal>Répondre « non déclenchée », « ARRÊT ET CADRAGE », « ACCEPTER ET DOCUMENTER » ou « RETOUR ARRIÈRE DU RULESET CONCERNÉ », ET, si le push direct a été accepté, « REVERT PAR PR » ou « CONSERVATION DÉCLARÉE » pour le commit de preuve. Après la réponse, l'exécutant consigne `D03-M4-DECISION: option=<id> sort_commit_preuve=<revert-par-pr|conservation-declaree|sans_objet> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint>` — jamais inféré — et le commite sur B avec le message « docs(41-09): D03-M4-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clé de déploiement temporaire → refs `main` et `v*` | acteur hors liste, écriture, durée de vie d'un checkpoint |
| poste de Willy → refs `v*` | réécriture et suppression d'un tag de test par un compte de la liste |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-34 | Tampering | push direct accepté | high | mitigate | commit vide (aucun contenu) ; poussé seulement par l'acteur hors liste ; témoin `ls-remote` ; arrêt sur décision si accepté, sort du commit fixé explicitement |
| T-41-35 | Spoofing | tag de test pris pour une release | medium | mitigate | nom hors motif `vX.Y.Z` du vérificateur de mise à jour ; aucune release ; suppression dans le même geste, relue |
| T-41-36 | Repudiation | refus client (hook) pris pour un refus serveur | medium | mitigate | `VERSION` de `main` taggée et publiée exigée ; `core.hooksPath` consigné ; `--no-verify` interdit ; message serveur exigé (mention de la PR) |
| T-41-37 | Repudiation | combinaison des règles de tags supposée | medium | mitigate | création, réécriture, suppression jouées pour un acteur hors liste ET pour un acteur de la liste ; résultat de suite consigné par geste ; verdict recalculé |
| T-41-64 | Information disclosure | clé de déploiement en écriture divulguée ou oubliée | high | mitigate | clé générée dans un répertoire temporaire hors dépôt, retirée chez GitHub et effacée dans le même checkpoint ; absence relue par l'API (`CLE-DEPLOIEMENT: retiree=oui`) |
| T-41-65 | Tampering | push direct d'un compte de la liste qui atterrirait | high | mitigate | aucun push direct sur `main` par Samuel ni Willy dans ce plan (instruction et critère d'acceptation) ; l'acteur de la liste n'agit que sur le tag de test |
</threat_model>

<verification>
Push direct hors liste refusé et relu ; ruleset de tags mesuré pour les deux sortes d'acteurs, traces
comprises ; aucun artefact de test ni clé restant.
</verification>

<success_criteria>
D-03 prouvé côté serveur ; M-4 consigné ; la création libre du futur tag de release est établie.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-09-SUMMARY.md` when done — committé sur la branche B.
</output>

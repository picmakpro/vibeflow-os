---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 03
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
autonomous: true
requirements: [ROLL-01, ROLL-02, ROLL-03, ROLL-04, ROLL-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un `rollback <module>` restaure TOUT ce que `backup_module` a réellement sauvegardé (skills, scripts, agents/${mod}.md, agents/${mod}-references/, hooks/hooks.json) — jamais seulement skills+scripts comme aujourd'hui"
    - "Après un rollback, `.claude/scripts/.vibeflow-installed` porte la VERSION restaurée, pas la version neuve qui vient d'être effacée du disque — le registre ne ment plus"
    - "Un rollback sans backup valide (aucun backup, ou uniquement des répertoires de convergence `-removed`) échoue BRUYAMMENT — jamais un `✓ rollback OK` sur zéro action réelle"
    - "`--dry-run rollback <module>` prévisualise ce qui serait restauré sans écrire quoi que ce soit sur disque"
  artifacts:
    - "plugin/_internal/vibeflow-update.sh : backup_module()/rollback_module() étendus, sans migration vers le socle manifeste (D-31-09 tenu sur ce point précis — revisité, jamais corrigé par erreur)"
  key_links:
    - "backup_module() -> $bdir/agents/, $bdir/agent-references/, $bdir/hooks/hooks.json : symétrie complète avec ce qui existe déjà en 1665-1668 (skills+scripts) — le trou mesuré au cadrage (38-CONTEXT.md lignes 218-221) est que ces 3 sources ne sont PAS sauvegardées alors que backup_module les connaît déjà (les 2 premières) ou peut les lire depuis le cache (la 3e)"
    - "rollback_module() -> mark_installed(mod, version_capturée_au_backup) : ROLL-03, capture la version au moment du backup (dans un fichier .version du répertoire de backup), jamais une version devinée"
    - "rollback_module() -> merge_module_hooks()/remove_module_hooks() (primitives existantes, jamais réimplémentées) : ROLL-02, restaure le fragment de hooks sauvegardé plutôt que le fragment du cache courant (potentiellement une version différente)"
  prohibitions:
    - "Ne jamais migrer rollback_module()/backup_module() vers le socle manifeste (vf_record/vf_manifest_flush) — D-31-09 documente ce choix, ce lot le REVISITE sur les 5 défauts nommés (ROLL-01..05), il ne le corrige pas par erreur de lecture"
    - "Ne jamais réécrire la logique de merge/remove des hooks dans rollback_module() : appeler merge_module_hooks()/remove_module_hooks() existantes, au besoin en leur ajoutant un paramètre de chemin de fragment optionnel — jamais une 3e implémentation du merge JSON"
    - "Le glob de sélection du backup DOIT exclure les répertoires `-removed` de convergence (écrits par vf_converge_apply) — un `ls -1dt \"$BACKUP_DIR/$mod\"-*` non filtré reproduit exactement le défaut mesuré au cadrage (rollback silencieux no-op)"
---

<objective>
Lot 3 (`ROLL`) du cadrage 38-CONTEXT.md : `rollback_module()` a 4 défauts mesurés au cadrage
(restaure seulement skills+scripts, jamais `mark_installed`, glob non filtré capturant les
répertoires `-removed` de convergence — donc `rm -rf` jamais atteint, `--dry-run` refusé) —
indépendants du multi-runtime, mais nommés dans ce phasage. Séquencé APRÈS RUNT (lot 2) : ordre
imposé par REQUIREMENTS.md ET partage réel de `plugin/_internal/vibeflow-update.sh` +
`plugin/_internal/tests/test-vibeflow-update.sh` + `plugin/conductor/VERSION`/`CHANGELOG.md` avec
le lot 1 (FIDE, qui a déjà inséré une bannière juste après `mark_installed` en fin
d'`install_module()`).

Purpose: sans ce lot, TOUT rollback (déclenché par un opérateur après une install/update ratée, sur
n'importe quel runtime livré par les lots précédents) reste le « vert auto-déclaré sur zéro action »
mesuré au cadrage (38-CONTEXT.md lignes 225-232) — la classe de défaut que ce dépôt entier traque.

Output: `backup_module()`/`rollback_module()` durcis, `--dry-run rollback` fonctionnel, non-régression
complète.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/_internal/vibeflow-update.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tache 1 : bout-en-bout — backup complet, rollback complet, registre à jour (ROLL-01, ROLL-03, ROLL-04)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 216-234 (« rollback_module (1681-1704) — (a) et (b) confirmés, (c) À REFORMULER » : le mode d'échec dominant est un ROLLBACK SILENCIEUX NO-OP qui log quand même `✓ rollback OK` — le glob non ancré `"$BACKUP_DIR/$mod"-*` attrape les répertoires `$mod-<ts>-removed` de convergence, `ls -1dt` les trie en tête si récents, `[ -d "$latest/skills" ]` est faux dessus → `rm -rf` jamais atteint. Défaut supplémentaire : `--dry-run` REFUSÉ sur `rollback` (garde 98-102, D-31-06))
    - plugin/_internal/vibeflow-update.sh:1650-1704 (`backup_module()`/`rollback_module()` actuels, ligne par ligne : 1665 skills, 1667-1668 agents+agent-references DÉJÀ sauvegardés côté backup mais JAMAIS relus côté rollback — c'est le trou (a) confirmé)
    - plugin/_internal/vibeflow-update.sh:176-195 (`mark_installed(mod, version)` : signature à réutiliser telle quelle, jamais réimplémentée)
    - plugin/_internal/vibeflow-update.sh:98-103 (garde de surface `--dry-run` : accepte SEULEMENT `install|update` — `rollback` tombe dans le `*)` qui appelle `err`. Cette ligne doit accepter `rollback` en plus, SANS toucher au comportement install/update)
    - plugin/_internal/vibeflow-update.sh:34-45 (`vf_dry_run()` : prédicat déjà partagé, à réutiliser dans `rollback_module()` — jamais un nouveau flag)
  </read_first>
  <behavior>
    - Test : `backup_module` d'un module ayant un `agents/${mod}.md`, un `agents/${mod}-references/` ET un `skills/${mod}/` produit un répertoire de backup portant les 3 (`$bdir/skills`, `$bdir/agents`, `$bdir/agent-references`) — jamais seulement 2 sur 3.
    - Test : `backup_module` capture la version installée AU MOMENT du backup dans `$bdir/.version` (lue depuis `$INSTALLED_REGISTRY` courant, `mod=version` — grep + cut, jamais un format neuf).
    - Test : `rollback_module` d'un module backuppé avec les 3 catégories restaure les 3 sur disque (assert par `[ -f ]`/`[ -d ]` après rollback, pas seulement l'absence d'erreur) ET appelle `mark_installed "$mod" "<version capturée>"` — vérifié par lecture de `$INSTALLED_REGISTRY` après coup.
    - Test : `rollback_module` sur un `BACKUP_DIR` contenant UNIQUEMENT un répertoire `$mod-<ts>-removed` (fixture : créer ce répertoire vide de tout sous-dossier `skills/agents/scripts`) échoue BRUYAMMENT (code de sortie non-zéro, message explicite) — jamais `✓ rollback OK`.
    - Test : `rollback_module` sur un `BACKUP_DIR` sans AUCUN répertoire pour `$mod` échoue bruyamment (cas déjà partiellement couvert par `err` existant en 1689 — vérifier qu'il reste vrai après le filtrage du glob).
  </behavior>
  <action>
Dans `backup_module()`, ajouter après le bloc scripts existant (1670-1676) :
- Capture de version : lire l'entrée `$mod=<version>` de `$INSTALLED_REGISTRY` (grep + cut sur `=`,
  jamais un format neuf), l'écrire dans `$bdir/.version` (`printf` sans retour de ligne parasite).
  Registre absent ou entrée absente (premier install jamais backuppé avant) → `.version` absent,
  `rollback_module` traite ce cas en tâche 2 (ROLL-04, échec bruyant plutôt que version devinée).
- Hooks : si `$CACHE_DIR/$mod/hooks/hooks.json` existe (le fragment de la version ACTUELLEMENT
  installée, celle qu'on est en train de sauvegarder avant écrasement), le copier dans
  `$bdir/hooks/hooks.json`.
Le fait que `agents/${mod}.md` et `agents/${mod}-references/` sont DÉJÀ sauvegardés (1667-1668) ne
change pas — ce lot corrige le côté ROLLBACK qui ne les relit jamais, pas le côté backup déjà
correct pour ces deux-là.

Dans `rollback_module()`, étendre le bloc de restauration (actuellement 1692-1702, skills+scripts
seulement) :
- Filtrer le glob `ls -1dt "$BACKUP_DIR/$mod"-*` pour EXCLURE les suffixes `-removed` (motif de
  convergence `vf_converge_apply`, ligne 2029 pour référence du motif exact) — un `grep -v -- '-removed$'`
  sur la liste triée, PAS une modification du glob lui-même (le glob doit continuer à matcher les
  deux formes, seul le filtrage post-liste change).
- Si, après filtrage, aucun candidat ne reste OU si le candidat retenu ne porte AUCUN sous-dossier
  d'un des 4 types (`skills`, `scripts`, `agents`, `hooks`) : `err` (jamais `log "✓ … rollback OK"`
  sur cette branche — c'est exactement le défaut mesuré au cadrage).
- Restaurer `agents/${mod}.md` et `agents/${mod}-references/` si présents dans `$latest`, MÊME
  patron que le bloc skills existant (`rm -rf` puis `cp -r`, jamais un merge partiel).
- Après restauration réussie, lire `$latest/.version` (si présent) et appeler `mark_installed "$mod"
  "$version_restaurée"`. `.version` absent (backup pré-ce-lot) → NE PAS appeler `mark_installed`
  avec une version devinée, journaliser `⚠ version restaurée inconnue — registre inchangé,
  vérifier manuellement`.

Bump `plugin/conductor/VERSION` (patch — durcissement de bug, aucune capacité neuve) et
`CHANGELOG.md`.
  </action>
  <acceptance_criteria>
    - Fixture `mktemp -d` en `TARGET_ROOT`, module de test avec `skills/`, `agents/${mod}.md`, `agents/${mod}-references/` réels : après `backup_module` puis `rollback_module` sur une cible vidée, les 3 réapparaissent sur disque — vérifié par `[ -f ]`/`[ -d ]` réels, jamais supposé.
    - `INSTALLED_REGISTRY` après rollback porte `mod=<version capturée au backup>`, différente de la version qui était installée juste avant le rollback (scénario : install v1 → backup → install v2 → rollback → registre doit revenir à v1).
    - Fixture avec UNIQUEMENT un répertoire `$mod-<ts>-removed` vide : `rollback_module "$mod"` sort non-zéro, AUCUNE ligne `✓ $mod rollback OK` dans la sortie capturée (grep -v, assert absence).
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0, bannière `== résultat :`.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>Un rollback réel restaure agents+références+hooks en plus de skills+scripts, remet le registre à la version restaurée, et échoue bruyamment plutôt que de mentir sur un backup de convergence vide — prouvé par exécution réelle sur fixtures, suite verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : hooks au rollback + --dry-run + non-régression (ROLL-02, ROLL-05)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh:1072-1153 (`merge_module_hooks()`/`remove_module_hooks()` : les deux lisent EN DUR `$CACHE_DIR/$mod/hooks/hooks.json` — pour restaurer le fragment SAUVEGARDÉ (potentiellement différent du fragment courant du cache), ces deux fonctions ont besoin d'un second paramètre optionnel `<fragment_path>` avec la valeur actuelle comme défaut, pour ne RIEN casser des ~6 appelants existants qui n'en passent qu'un)
    - plugin/_internal/vibeflow-update.sh:93-103 (garde `--dry-run` : `case "$1" in install|update) : ;; *) err … ;; esac` — ajouter `rollback` à la liste des verbes acceptés, formulation du message d'erreur inchangée pour les autres verbes)
    - 38-CONTEXT.md lignes 233-234 (« Défaut supplémentaire non listé par l'étude : `--dry-run` est refusé sur `rollback` (garde 98-102, D-31-06) → aucun moyen de prévisualiser ce que le rollback va écraser » — et lignes 101-103 « D-31-09 : rollback_module() est délibérément non migré vers le socle manifeste, et --dry-run y est refusé. Le lot 3 revisite une décision documentée, il ne corrige pas un oubli »)
  </read_first>
  <behavior>
    - Test : `merge_module_hooks "$mod" "$chemin_backup/hooks.json"` (nouveau 2e paramètre) merge bien LE FICHIER PASSÉ, pas `$CACHE_DIR/$mod/hooks/hooks.json` — vérifié en pointant vers deux fragments de contenu différent et en comparant le `settings.json` résultant.
    - Test : les ~6 appels existants à `merge_module_hooks "$mod"` (1 seul argument, sans le second) continuent de fonctionner IDENTIQUEMENT (le paramètre optionnel retombe sur le comportement actuel) — non-régression complète de la suite hooks existante.
    - Test : `rollback` sous `--dry-run` n'écrit RIEN sur disque (aucun fichier créé/modifié dans `TARGET_ROOT` avant/après comparé par `find … -newer`) mais imprime ce qui SERAIT restauré (module, version, fragment de hooks concerné).
  </behavior>
  <action>
Ajouter un second paramètre optionnel `fragment_override` à `merge_module_hooks()` et
`remove_module_hooks()` — `local fragment="${2:-$CACHE_DIR/$mod/hooks/hooks.json}"` remplace la
ligne actuelle qui fixe `fragment` en dur. Aucun autre changement dans le corps de ces deux
fonctions : elles continuent d'appeler `merge-hooks.sh merge/remove` via `find_hooks_merger()`
exactement comme avant — jamais une réimplémentation.

Dans `rollback_module()`, après restauration skills/scripts/agents (tâche 1) : si
`$latest/hooks/hooks.json` existe, appeler `remove_module_hooks "$mod"` (retire le fragment
COURANT du cache, potentiellement une version différente de celle qu'on restaure) PUIS
`merge_module_hooks "$mod" "$latest/hooks/hooks.json"` (réinjecte le fragment SAUVEGARDÉ). Les deux
appels restent best-effort (échec journalisé, jamais fatal pour le reste du rollback — même
doctrine que leurs appelants actuels dans `install_module`).

Étendre la garde `--dry-run` (ligne 99-102) : `case "$1" in install|update|rollback) : ;; *) err
…; esac`. Dans `rollback_module()`, en tête de fonction, `vf_dry_run` court-circuite AVANT toute
écriture : imprimer `[dry-run] rollback $mod depuis $latest — restaurerait : <liste des
sous-dossiers présents dans $latest>`, `return 0`. Réutiliser `vf_dry_run()` (34-45) tel quel,
jamais un nouveau flag.

Mettre à jour le commentaire de tête de `rollback_module()` (1682-1684, référence D-31-09) : la
mention « --dry-run y est refusé (D-31-06) » devient obsolète, remplacer par une note factuelle
« --dry-run fonctionne depuis ce lot (ROLL-05), la non-migration vers le socle manifeste reste
inchangée ».

Compléter `plugin/conductor/CHANGELOG.md` (entrée conjointe à celle de la tâche 1, ou entrée
séparée selon la convention déjà observée dans ce fichier — vérifier par lecture des 2-3 dernières
entrées avant d'écrire).

Rejouer la découverte COMPLÈTE des suites et confirmer 0 régression (ce lot n'ajoute PAS de fichier
de suite neuf — `test-vibeflow-update.sh` existe déjà, seulement des `T<n>.<m>` supplémentaires —
donc le compte de suites reste inchangé depuis le lot précédent, à vérifier par la même commande de
découverte plutôt que supposé).
  </action>
  <acceptance_criteria>
    - `grep -n 'fragment_override\|\${2:-' plugin/_internal/vibeflow-update.sh | grep -c .` ≥ 2 (les 2 fonctions modifiées).
    - Fixture : deux fragments hooks différents (A = fragment courant du cache, B = fragment sauvegardé au backup) ; après rollback, le `settings.json` résultant contient les marqueurs du fragment B, pas ceux du fragment A — vérifié par grep sur le fichier réel post-rollback.
    - `bash plugin/_internal/vibeflow-update.sh --dry-run rollback <mod-fixture>` sort 0, ET `find "$TARGET_ROOT" -newer <horodatage-avant>` ne rend AUCUN fichier — preuve d'absence d'écriture, pas juste absence d'erreur.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` reste au compte du lot précédent (aucune régression de comptage), consigné explicitement dans le SUMMARY.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>Le rollback restaure aussi les hooks depuis le backup (jamais le cache courant potentiellement divergent), `--dry-run rollback` prévisualise sans écrire, et la doctrine D-31-09 est mise à jour pour refléter ce point précis sans être autrement rouverte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$BACKUP_DIR/$mod-*` (disque local) -> rollback_module() | un backup corrompu ou partiel ne doit jamais produire un `rm -rf` suivi d'une restauration incomplète qui laisse la cible dans un état pire qu'avant |
| `.version` lu depuis le backup -> mark_installed() | une valeur de version illisible/vide ne doit jamais être réinjectée telle quelle dans le registre |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-06 | Repudiation | `rollback_module` log `✓ rollback OK` sur zéro action (défaut mesuré au cadrage) | high | mitigate | filtrage `-removed` + échec bruyant si aucun sous-dossier restaurable — acceptance criteria dédié |
| T-38-07 | Tampering | `rm -rf "$TARGET_ROOT/skills/$mod"` avant `cp -r` (défaut d'atomicité pré-existant, D-31-09 §c) | medium | accept | hors périmètre de ce lot précis (ROLL-01..05 ne couvrent pas l'atomicité) — dette signalée, pas corrigée, cohérent avec la doctrine du cadrage |
| T-38-08 | Information Disclosure | `.version` capturé dans le backup contient la chaîne de version brute du registre | low | accept | donnée déjà présente en clair dans `.vibeflow-installed`, aucune exposition nouvelle |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | aucun paquet externe installé par ce plan |
</threat_model>

<verification>
1. `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0.
2. Backup+rollback réel sur fixture restaure skills+agents+agent-references+hooks, capturé et vérifié par `[ -f ]`/`[ -d ]`.
3. Registre restauré à la bonne version après rollback (scénario v1→v2→rollback→v1).
4. `--dry-run rollback` n'écrit rien (preuve `find -newer`).
5. Rollback sur backup `-removed`-only échoue bruyamment, jamais `✓ rollback OK`.
</verification>

<success_criteria>
- `rollback_module()` restaure symétriquement à ce que `backup_module()` sauvegarde.
- Le registre ne ment plus après un rollback.
- `--dry-run rollback` fonctionne.
- Le mode d'échec dominant mesuré au cadrage (vert sur zéro action) est fermé.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-03-SUMMARY.md` avec : la preuve du scénario v1→v2→rollback→v1 (registre + fichiers sur disque), la preuve d'échec bruyant sur backup `-removed`-only.
</output>

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 03
subsystem: ci-gates
tags: [bash, awk, github-actions, adr-029, ratchet]

requires:
  - phase: 40.1-01
    provides: "tools/replay-ci-jobs.sh, tools/check-no-live-250.sh (recensement du littéral 250)"
provides:
  - "check-instruction-budget.sh révisé : VF_BUDGET_LINE_CAP=300, VF_BUDGET_LINE_WARN_FROM=251, verdicts AVERTISSEMENT-ADR029 et LIGNES-EN-HAUSSE, ratchet ne bloquant plus que sur les instructions par fichier"
  - "test-check-instruction-budget.sh : 38 cas (0 ko), constantes BORNE_AVERT/PLAFOND, MUT-2 remplacé, MUT-5/6/7 nouveaux"
  - "étape CI check-instruction-budget à sept bascules isolées, commentaires réalignés sur l'état réel (ratchet armé depuis a7f414a)"
  - "tools/prove-ci-budget-step.sh : rejoue six mutants du gate contre l'étape CI extraite de ci.yml"
affects: [40.1-05, 40.1-07, 40.1-12, 40.1-13]

actuals:
  tokens: 11750
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Warn/block symétrique check-file-size.sh (WARN_FROM inclusive -ge, CAP -gt) transposé au budget d'instructions"
    - "Bascules CI isolées reconstruites depuis une fonction write_base(), jamais cumulatives"

key-files:
  created:
    - ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh"
  modified:
    - "plugin/conductor/scripts/check-instruction-budget.sh"
    - "plugin/conductor/scripts/tests/test-check-instruction-budget.sh"
    - ".github/workflows/ci.yml"
    - ".planning/instruction-budget-baselines.tsv"

key-decisions:
  - "D-02/D-H6 appliqués à la lettre : la colonne lignes de la baseline n'est plus jamais comparée pour le rc, seulement publiée (verdict informatif LIGNES-EN-HAUSSE)"
  - "MUT-2 remplacé plutôt que supprimé : son ancre suit le nouveau code (ligne informative), son mutant vérifie qu'elle ne redevient jamais bloquante"
  - "Condition d'avertissement écrite sur UNE seule ligne physique (au lieu d'une continuation `\\`) pour rester une ancre de mutation exacte, comme MUT-1/MUT-2"
  - "Étape CI passée de 4 bascules cumulatives à 7 bascules isolées, chacune repartant d'une fixture de base neuve (write_base())"

patterns-established:
  - "Table `check_mutant <nom> <old> <new> <note>` dans prove-ci-budget-step.sh : réutilisable pour toute future preuve mutant-contre-étape-CI"

requirements-completed: [BUDG-05, BUDG-04, QUAL-01]

coverage:
  - id: D1
    description: "Plafond ADR-029 porté à 300 lignes, avertissement non bloquant dès 251"
    requirement: "BUDG-05"
    verification:
      - kind: unit
        ref: "plugin/conductor/scripts/tests/test-check-instruction-budget.sh (sous-zone, bord d'avertissement, 260, PLAFOND, plafond absolu)"
        status: pass
      - kind: integration
        ref: ".github/workflows/ci.yml step check-instruction-budget bascules 4/7 5/7"
        status: pass
  - id: D2
    description: "Ratchet ne bloque plus que sur les instructions (par fichier), la colonne lignes reste informative"
    requirement: "BUDG-05"
    verification:
      - kind: unit
        ref: "plugin/conductor/scripts/tests/test-check-instruction-budget.sh (cas BUDG-05, MUT-2 remplacé)"
        status: pass
  - id: D3
    description: "Sept bascules isolées prouvées en CI, chacune capable de rougir sous mutation du gate"
    requirement: "QUAL-01"
    verification:
      - kind: integration
        ref: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh"
        status: pass
  - id: D4
    description: "Commit atomique sans fenêtre rouge : gate rc 0 avant et après, baseline octet identique"
    requirement: "BUDG-04"
    verification:
      - kind: other
        ref: "vérification inline (git archive parent/commit + cmp des lignes de données de baseline)"
        status: pass
---

## Accomplissements

- `check-instruction-budget.sh` : `VF_BUDGET_LINE_CAP=300`, `VF_BUDGET_LINE_WARN_FROM=251`,
  verdicts `AVERTISSEMENT-ADR029` (zone 251-300, non bloquant) et `LIGNES-EN-HAUSSE` (informatif
  seul, jamais comparé pour le rc — D-02/D-H6). Suppression de `line_over`, `DEPASSEMENT-LIGNES`,
  `DEPASSEMENT-LIGNES+INSTR`. `WARN_COUNT` ajouté au bilan.
- `test-check-instruction-budget.sh` : constantes `BORNE_AVERT=251`/`PLAFOND=300` (aucun littéral
  de l'ancien plafond, BUDG-04), fonction `gen_lines` (remplace `gen_251`, affirme sa propre
  taille), cas BUDG-05 et bords de zone, MUT-2 remplacé (comparaison des lignes re-introduite
  comme bloquante — désormais tuée), MUT-5/6/7 nouveaux. 38 cas, 0 ko.
- `.github/workflows/ci.yml` : étape passée de 4 bascules cumulatives à 7 bascules isolées
  (`write_base()` reconstruit la fixture avant chaque mutation), commentaires réalignés sur l'état
  réel (ratchet armé sur `main` depuis `a7f414a`, plus de mention d'une calibration Phase 40
  attendue), warning par fichier en zone d'avertissement sur le dépôt réel armé.
- `tools/prove-ci-budget-step.sh` (nouveau) : copie l'arbre suivi courant (git ls-files -z + tar,
  modifications non committées incluses), rejoue T0 puis six mutants du gate contre l'étape CI
  EXTRAITE de `ci.yml` via `replay-ci-jobs.sh` — jamais une copie de l'étape. 7 ok, 0 ko.
- Un seul commit atomique (`52256c0`) : gate, suite, étape CI, en-tête de baseline et outil de
  preuve. Gate rc 0 vérifié sur le parent ET sur ce commit (épinglés par SHA, pas par HEAD relatif).
  Lignes de données de la baseline identiques octet pour octet.

## Écart signalé (non bloquant, hors périmètre du plan)

Le `<verify>` du plan invoque la suite via `bash --noprofile --norc -e ...`. Sous cette invocation,
`out="$(run "$D")"; rc=$?` déclenche la sortie immédiate de `set -e` dès le premier `rc` non nul
(comportement bash documenté : une substitution de commande en échec dans une affectation simple
compte comme échec de la commande englobante). Vérifié : ce comportement est IDENTIQUE sur la
version de la suite au commit parent (avant cette révision) — ce n'est pas une régression
introduite ici. La suite a été exécutée et vérifiée sous `bash --noprofile --norc` (sans `-e`),
cohérent avec son propre `set -uo pipefail` en tête de fichier et avec son usage historique
(Phase 25) : 38 ok, 0 ko. Signalé au manager pour arbitrage sur le texte du `<verify>`, jamais
corrigé de mon propre chef.

## Findings

- **auto-fix** : commentaire de fixture dans `test-check-instruction-budget.sh` citant le nom
  littéral `check-no-live-250.sh` — le `250` du nom d'outil se faisait lui-même détecter par le
  recensement BUDG-04 (faux positif auto-infligé). Reformulé sans le littéral, vérifié par
  re-exécution du recensement (0 hit sous mon périmètre).
- **no-op** : `check-no-live-250.sh .` rend encore 17 hits, tous hors du périmètre de ce plan
  (`plugin/conductor/AGENT.md`, `plugin/conductor/README.md`, `plugin/conductor/references/contracts.md`,
  `plugin/conductor/skills/vf-new-lab/SKILL.md`, `plugin/reference/content/**`) — relèvent d'autres
  plans de la vague 2 (40.1-05, 40.1-13, etc.), pas de ce lot.
- **no-op** : durant l'exécution, un `git stash push -- <mes 3 fichiers>` (pathspec explicite,
  jamais un stash nu) suivi d'un `git stash pop` immédiat dans le même tour d'outils, pour comparer
  le comportement de la suite pré-révision sous `-e`. Aucun autre appel entre les deux, `git stash
  list` ne montre aucun résidu (un seul stash ancien et sans rapport, antérieur à cette mission).
  Intégrité de mes fichiers reconfirmée par `git diff --stat` après coup et re-exécution complète
  de la suite (38 ok/0 ko). Le manager a été notifié en direct des deux alertes reçues à ce sujet ;
  consigne reçue de ne plus utiliser `git stash` du tout dans l'arbre partagé — appliquée pour la
  suite de ce mandat.

## Vérifications rejouées

- `bash --noprofile --norc plugin/conductor/scripts/tests/test-check-instruction-budget.sh` → 38 ok, 0 ko
- `bash --noprofile --norc .../tools/prove-ci-budget-step.sh` → 7 ok, 0 ko (T0 + 6 mutants)
- `bash --noprofile --norc .../tools/replay-ci-jobs.sh --job gates --step check-instruction-budget` → rc=0
- Vérification inline gate rc 0 parent/commit + 4/4 fichiers du contrat + baseline octet identique → toutes assertions vraies
- `tools/check-no-live-250.sh .` → 0 hit sous `plugin/conductor/scripts/`, `.github/`, `.planning/instruction-budget-baselines.tsv`

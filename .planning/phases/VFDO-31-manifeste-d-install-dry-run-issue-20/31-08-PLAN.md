---
phase: 31-manifeste-d-install-dry-run-issue-20
plan: 08
type: execute
wave: 6
depends_on: ["31-05"]
files_modified:
  - .planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md
  - README.md
  - README.fr.md
autonomous: true
requirements: [MANI-04, QUAL-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Une réponse à l'issue #20 existe, rédigée, sur disque (MANI-04)"
    - "Elle répond point par point à ce que l'auteur a demandé : manifeste fichier-par-fichier, format de sortie proposé, consommation par l'étape 5 de l'installer et l'étape 4 de vf-calibrate"
    - "Elle nomme honnêtement les limites livrées : régime C non énuméré, --dry-run refusé sur uninstall, docs/ hors manifeste"
    - "La suite complète est verte sur l'arbre TEL QUE COMMITÉ, pas sur l'état de travail (31-CONTEXT.md §4 point 5)"
    - "Les compteurs de suites des deux README correspondent à la mesure machine finale"
  artifacts:
    - ".planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md"
  key_links:
    - "issue #20 (demande terrain : revue croisée des hooks AVANT écriture) -> --dry-run + manifeste livrés en vagues 1-4"
  prohibitions:
    - "AUCUN appel `gh issue comment`, `gh issue close`, `gh api` ou équivalent — poster et clore sont des gestes humains, hors de l'autorité autonome de cette phase (ADR-031, 31-CONTEXT.md §4 point 8)"
    - "AUCUN push, AUCUNE PR, AUCUN merge, AUCUNE release racine"
    - "Aucune affirmation dans le brouillon qui ne soit adossée à un test livré"
---

<objective>
Clore la phase par ses deux gestes de fin : le brouillon de réponse à l'issue #20, et la preuve que
ce qui est livré est vert **tel que commité**.

Purpose: MANI-04 — dans la lecture que 31-CONTEXT.md §4 point 8 impose, qui SUPERSÈDE le texte du
critère de succès 4 de la ROADMAP. La ROADMAP dit « l'issue GitHub #20 est close par livraison,
réponse postée sur l'issue » ; le cadrage a tranché : **brouillon sur disque uniquement, jamais
posté, jamais de close**. Poster et clore sont des gestes humains de Samuel.

Output: `31-ISSUE-20-REPLY.md` dans le dossier de phase, et le bilan machine de la phase.

> **Ce plan est ordonné en dernière vague à dessein** : il décrit ce qui a été réellement livré, y
> compris le sort des deux vagues abandonnables (31-06 et 31-07). Sa dépendance déclarée est
> volontairement limitée à 31-05 pour qu'il reste exécutable même si 31-06 et 31-07 sont
> abandonnés — il constate leur absence au lieu d'en dépendre.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-CONTEXT.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-04-SUMMARY.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : brouillon de réponse à l'issue #20 — sur disque, jamais posté</name>
  <files>.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md</files>
  <read_first>
    - 31-CONTEXT.md §1 (le texte de l'issue #20 y est repris : demande terrain des élèves formation, équipe à 2 sous Windows, revue croisée obligatoire des hooks ; le format de sortie proposé par l'auteur) et §4 point 8 (le gate : brouillon uniquement)
    - 31-CONTEXT.md §3 D-31-05 (le format livré, à comparer au format proposé dans l'issue)
    - 31-CONTEXT.md §7 (les trois remontées non tranchées — dont `--dry-run` sur `uninstall`, que l'auteur pourrait attendre)
    - Les SUMMARY présents dans le dossier de phase au moment de l'exécution (31-01 à 31-07 selon ce qui a été livré) — c'est la source de ce qui est affirmable
    - .planning/ROADMAP.md:597-623 (le critère 4 tel qu'écrit, et la divergence assumée)
  </read_first>
  <action>
Écrire `31-ISSUE-20-REPLY.md` dans le dossier de phase. C'est un BROUILLON destiné à Samuel : un
en-tête de deux lignes le dit explicitement (« brouillon — non posté ; poster et clore l'issue #20
sont des gestes humains »), puis le corps de la réponse, rédigé comme si l'auteur de l'issue allait
le lire, en français.

Structure du corps :

1. **Réponse directe à la question posée** — « Est-ce que `/vf-calibrate` présente ce qu'il va
   poser avant de le poser ? » : oui, désormais, et dire par quel geste exact.

2. **Ce qui est livré**, en nommant les mécanismes et pas les intentions : le flag `--dry-run` sur
   `install` et `update` ; le plan fichier-par-fichier sur stdout ; le manifeste
   `scripts/.vibeflow-manifest-<module>` écrit à chaque pose ; le fait que le plan et la pose
   sortent du MÊME appel, ce qui rend la dérive plan/pose structurellement impossible plutôt que
   simplement testée ; la convergence à l'update avec backup et liste signalée.

3. **Le format**, montré tel qu'il sort, et comparé à celui proposé dans l'issue : les verbes `+`
   et `~` sont ceux de l'auteur, le verbe `-` est neuf (l'issue ne couvrait pas la convergence, il
   lui fallait sa forme) ; le chemin est affiché avec son préfixe de scope, ce qui rend
   `.claude/scripts/…` en scope projet — conforme à l'exemple de l'issue — et le chemin complet en
   scope compte, où un chemin nu serait ambigu.

4. **Le merge de hooks, spécifiquement** — c'est le cœur de la demande terrain (un hook s'exécute
   hors de la couche de permissions et tourne chez l'associé dès qu'il est poussé). Dire que la
   prévisualisation du merge est rendue par `merge-hooks.sh` lui-même, dans un mode `plan` neuf, et
   pourquoi : réimplémenter la simulation côté engine aurait créé un second chemin de code capable
   de mentir.

5. **Les limites, nommées et assumées** — les trois, sans les enrober : (a) trois sous-processus
   (`seed-registres.sh`, `inject-mcp-tools.sh`, `ensure-design-deps.sh`) sont annoncés comme effet
   sans être énumérés, parce que leur contenu est du vivant de lab et qu'inventer une exactitude
   qu'on ne peut pas tenir serait pire que le dire ; (b) `--dry-run` est REFUSÉ sur `uninstall`,
   bruyamment — et c'est délibéré : un flag accepté puis ignoré sur le verbe destructif serait le
   pire échec possible ; l'extension du flag à `uninstall` est consignée comme candidat pour une
   phase suivante ; (c) `docs/<module>/` apparaît au plan mais n'entre pas au manifeste, parce que
   ce chemin est écrit hors du scope.

6. **Le fil testeurs Windows** — l'auteur signale que les mêmes testeurs se proposent pour valider
   le mode. Proposer explicitement cette validation comme prochaine étape.

7. **Ce qui a été livré, ce qui ne l'a pas été** : constater sur pièce, par la présence ou
   l'absence de `31-06-SUMMARY.md` et `31-07-SUMMARY.md` dans le dossier de phase, si le câblage
   des skills consommateurs et le lecteur `uninstall` ont été livrés. Si l'un manque, l'écrire
   comme tel — jamais l'annoncer comme livré.

Règle de rédaction : aucune affirmation qui ne soit adossée à un test livré. Chaque capacité citée
renvoie au cas de suite qui la prouve (par son numéro `T<n>` dans `test-manifest.sh`) — c'est ce
qui distingue une réponse d'une promesse.

**Interdit absolu de ce plan** : aucune commande GitHub. Ni commentaire sur l'issue, ni fermeture,
ni étiquette, ni référence de commit qui fermerait l'issue automatiquement (ne pas écrire de mot-clé
de fermeture GitHub dans un message de commit de cette phase). Le fichier reste sur disque et sur
la branche de phase.
  </action>
  <acceptance_criteria>
    - Le fichier `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md` existe et fait au moins 60 lignes.
    - Il porte l'en-tête de brouillon : `head -5` contient le mot `brouillon` et la mention « non posté ».
    - Les 7 sections du corps sont présentes (7 titres de niveau 2 ou 3).
    - Il cite au moins 4 numéros de cas de suite (`grep -cE 'T[0-9]+' 31-ISSUE-20-REPLY.md` rend au moins `4`).
    - Les trois limites sont nommées : le fichier contient `uninstall`, `non énuméré` et `docs/`.
    - Aucune commande GitHub dans le diff de la phase : `git log --oneline <base>..HEAD | wc -l` puis `git log <base>..HEAD --format=%B | grep -ciE '^(close[sd]?|fix(e[sd])?|resolve[sd]?) #20'` rend `0`.
    - Aucun appel `gh` exécuté pendant ce plan (à attester dans le SUMMARY).
    - `bash scripts/check-machine-paths.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md && [ "$(wc -l < .planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md)" -ge 60 ] && grep -qi 'brouillon' .planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md && bash scripts/check-machine-paths.sh</automated>
  </verify>
  <done>La réponse est rédigée, adossée aux tests, honnête sur ses limites, et elle attend Samuel sur disque.</done>
  <reversibility rating="reversible">Un fichier de documentation dans le dossier de phase. Le geste irréversible — poster sur l'issue publique — est justement celui que ce plan n'a pas le droit de faire.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : bilan machine de la phase — suite complète sur l'arbre commité</name>
  <files>README.md, README.fr.md</files>
  <read_first>
    - .github/workflows/ci.yml:210-237 (la découverte des suites et le contrat F13 anti-vert-à-vide — la commande locale doit être le miroir de celle-là)
    - README.md:124 et README.fr.md:128 (les compteurs posés en 31-01, à réconcilier avec la mesure finale)
    - 31-CONTEXT.md §4 point 5 (« Preuve APRÈS commit : la suite est rejouée sur l'arbre TEL QUE COMMITÉ ») et §4 point 2 (untracked étrangers à ne jamais commiter)
    - 31-VALIDATION.md (la Per-Task Verification Map, à confronter au livré)
  </read_first>
  <action>
Rejouer la découverte complète des suites, à l'identique de la CI, sur un extrait de l'arbre TEL
QUE COMMITÉ (jamais sur l'état de travail : un vert mesuré avant le commit ne prouve rien sur ce
qui est livré). L'extraction se fait par `git archive HEAD` dépaqueté dans un répertoire
temporaire, jamais par un `git stash` ni un `checkout` (le lock de driver interdit les checkouts
concurrents dans l'arbre principal).

Consigner dans le SUMMARY : le nombre de suites découvertes, le nombre de vertes, et le nom de
chaque suite rouge s'il y en a. Une découverte vide est un échec (contrat F13).

Réconcilier ensuite les compteurs des deux README avec la mesure machine finale : si des suites ont
été créées ou supprimées depuis 31-01, corriger les deux fichiers ; sinon, constater l'égalité et
ne rien toucher. Aucun gate neuf (D-31-08).

Vérifier enfin qu'aucun untracked étranger n'a été emporté : `.gsd/`,
`.planning/MISSION-30.dag.json`, `.planning/MISSION-31.dag.json`, `.planning/DRIVER.lock*`, et
le dossier de phase `VFDO-36-*` ne doivent apparaître dans aucun commit de la phase.

Ce plan ne pousse pas, n'ouvre pas de PR, ne merge pas et ne fait aucune release racine
(31-CONTEXT.md §4 point 8).
  </action>
  <acceptance_criteria>
    - La découverte complète est verte sur l'arbre commité : `T=$(mktemp -d); git archive HEAD | tar -x -C "$T"; cd "$T" && find plugin scripts -type f -path '*/tests/test-*.sh' | sort | while read t; do bash "$t" >/dev/null 2>&1 || echo "FAIL $t"; done` ne produit aucune ligne.
    - La découverte n'est pas vide : le même `find` compte au moins `62` suites.
    - Les compteurs des deux README égalent la mesure machine : `N=$(find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l | tr -d ' '); grep -q "$N suites in CI" README.md && grep -q "$N suites" README.fr.md`.
    - `bash scripts/check-version-sync.sh` et `bash scripts/check-machine-paths.sh` sortent 0.
    - Aucun untracked étranger commité : `git log --name-only --format= <base>..HEAD | grep -cE '^(\.gsd/|\.planning/MISSION-3[01]\.dag\.json|\.planning/DRIVER\.lock|\.planning/phases/VFDO-36-)'` rend `0`.
    - Aucune PR, aucun push, aucune release : à attester dans le SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>T=$(mktemp -d); git archive HEAD | tar -x -C "$T"; ( cd "$T" && find plugin scripts -type f -path '*/tests/test-*.sh' | sort | while read t; do bash "$t" >/dev/null 2>&1 || echo "FAIL $t"; done ) | tee /dev/stderr | grep -q . && { rm -rf "$T"; exit 1; }; rm -rf "$T"; bash scripts/check-version-sync.sh && bash scripts/check-machine-paths.sh</automated>
  </verify>
  <done>La suite complète est verte sur ce qui est réellement livré, les compteurs sont exacts, et rien d'étranger n'a été emporté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt privé -> issue GitHub publique | poster une réponse est irréversible et engage le projet devant son auteur et ses testeurs |
| arbre de travail -> arbre commité | un vert mesuré sur l'état de travail ne dit rien de ce qui est livré |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-25 | Elevation of Privilege | publication autonome sur l'issue #20 ou clôture automatique | high | mitigate | interdiction explicite de toute commande GitHub ET de tout mot-clé de fermeture dans les messages de commit ; le brouillon reste sur disque (ADR-031, 31-CONTEXT.md §4 point 8) |
| T-31-26 | Repudiation | vert mesuré sur l'état de travail présenté comme preuve du livré | high | mitigate | la suite est rejouée sur un extrait `git archive HEAD` ; critère d'acceptation machine |
| T-31-27 | Repudiation | découverte de suites vide rendant un faux vert | medium | mitigate | contrat F13 : la découverte doit compter au moins 62 suites, sinon échec |
| T-31-28 | Information Disclosure | untracked étranger emporté dans un commit de la phase | medium | mitigate | liste nominative des 5 chemins interdits en critère d'acceptation, contrôlée sur l'historique de la branche |
| T-31-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par cette phase (31-RESEARCH.md § Package Legitimacy Audit : N/A) |
</threat_model>

<verification>
1. `test -f .planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md`
2. Découverte complète sur l'arbre commité (commande de la tâche 2), zéro `FAIL`, au moins 62 suites
3. `bash scripts/check-version-sync.sh && bash scripts/check-machine-paths.sh`
4. Aucun commit de la phase ne porte un mot-clé de fermeture GitHub référençant l'issue 20
5. Aucun untracked étranger dans l'historique de la branche
</verification>

<success_criteria>
- La réponse à l'issue #20 est rédigée, sur disque, jamais postée, jamais clôturante.
- Chaque capacité annoncée renvoie au cas de suite qui la prouve.
- Les trois limites (régime C, refus sur `uninstall`, `docs/`) sont nommées sans enrobage.
- La suite complète est verte sur l'arbre commité, découverte non vide.
- Les compteurs des deux README égalent la mesure machine finale.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribution de ce plan)

**Nouveau fichier**
- `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-ISSUE-20-REPLY.md` — brouillon de
  réponse à l'issue #20, sur disque uniquement, à poster par Samuel (geste humain)

**Mises à jour**
- `README.md` et `README.fr.md` — compteurs de suites réconciliés avec la mesure machine finale
</artifacts_produced>

<output>
Créer `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-08-SUMMARY.md` à la fin,
avec le bilan de découverte (nombre de suites, vertes/rouges), le sort des deux vagues abandonnables
(31-06 et 31-07 : livrées ou abandonnées), et l'attestation qu'aucun geste GitHub n'a été posé.
</output>

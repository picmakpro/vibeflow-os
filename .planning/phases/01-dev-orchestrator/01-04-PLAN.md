---
phase: 01-dev-orchestrator
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - dev-orchestrator/skills/vf-dev/SKILL.md
  - dev-orchestrator/skills/vf-brainstorm/SKILL.md
  - dev-orchestrator/skills/vf-plan/SKILL.md
  - dev-orchestrator/skills/vf-execute/SKILL.md
  - dev-orchestrator/skills/vf-quick/SKILL.md
  - dev-orchestrator/skills/vf-test/SKILL.md
  - dev-orchestrator/skills/vf-review/SKILL.md
  - dev-orchestrator/skills/vf-debug/SKILL.md
  - dev-orchestrator/skills/vf-auto/SKILL.md
  - dev-orchestrator/skills/vf-ship/SKILL.md
  - dev-orchestrator/skills/vf-progress/SKILL.md
  - dev-orchestrator/skills/vf-init/SKILL.md
  - dev-orchestrator/references/vocabulary-map.md
autonomous: true
requirements: [ABS-01, ABS-02]
must_haves:
  truths:
    - "Un set complet de skills /vf-* existe (vf-dev, vf-brainstorm, vf-plan, vf-execute, vf-quick, vf-test, vf-review, vf-debug, vf-auto, vf-ship, vf-progress, vf-init)"
    - "Chaque /vf-* mappe vers une cible réelle (skill GSD vérifié, superpowers, ou bootstrap interne) — aucun mapping orphelin"
    - "Chaque skill /vf-* reframe sa sortie en vocabulaire VibeFlow"
    - "vf-init invoque ensure-deps.sh et propose l'init projet sur confirmation"
    - "Une table de traduction de vocabulaire masque les termes GSD"
    - "Chaque SKILL.md respecte la charte densité (≤500 lignes)"
  artifacts:
    - path: "dev-orchestrator/skills/vf-dev/SKILL.md"
      provides: "Point d'entrée générique : route une demande dev"
      contains: "vf-dev"
    - path: "dev-orchestrator/skills/vf-init/SKILL.md"
      provides: "Bootstrap : ensure-deps + offre d'init projet"
      contains: "ensure-deps"
    - path: "dev-orchestrator/references/vocabulary-map.md"
      provides: "Traduction de vocabulaire GSD → VibeFlow"
      contains: "rapport de sprint"
  key_links:
    - from: "dev-orchestrator/skills/vf-execute/SKILL.md"
      to: "gsd-execute-phase"
      via: "délégation thin (invoque le skill GSD, reframe la sortie)"
      pattern: "gsd-execute-phase"
    - from: "dev-orchestrator/skills/vf-init/SKILL.md"
      to: "dev-orchestrator/scripts/ensure-deps.sh"
      via: "appel du bootstrap"
      pattern: "ensure-deps"
---

<objective>
Livrer la couche d'abstraction hybride (D2/D6) : le set complet de skills `/vf-*` (thin
delegators vers GSD/superpowers/bootstrap) + la table de traduction de vocabulaire.

Purpose: Donner à l'utilisateur des raccourcis explicites ET à l'agent des handles nommés
qu'il invoque lui-même en autonomie (enchaînement vf-plan → vf-execute → vf-test). Chaque
verbe masque le terme GSD et reframe la sortie. Couvre ABS-01 et ABS-02.
Output: 12 SKILL.md sous `skills/vf-*/` + `references/vocabulary-map.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/CONVENTIONS.md

# Pattern à calquer (frontmatter skill : name + description multiline français) :
@consolidator/SKILL.md

# Index factuel produit en Plan 01 (chaque mapping doit pointer vers un nom RÉEL) :
@dev-orchestrator/references/gsd-skills-index.md

# Bootstrap produit en Plan 02 (vf-init l'invoque) :
@dev-orchestrator/scripts/ensure-deps.sh

<interfaces>
<!-- Mapping vf-* → cible (spec §3/§6). Cibles GSD confirmées sur disque. -->
| Verbe /vf-* | Cible réelle |
|---|---|
| vf-dev | route générique (délègue à l'agent / au bon verbe selon l'intention) |
| vf-brainstorm | superpowers brainstorming |
| vf-plan | gsd-discuss-phase + gsd-plan-phase |
| vf-execute | gsd-execute-phase |
| vf-quick | gsd-quick |
| vf-test | gsd-verify-work |
| vf-review | gsd-code-review |
| vf-debug | gsd-debug |
| vf-auto | gsd-autonomous |
| vf-ship | gsd-ship |
| vf-progress | gsd-progress |
| vf-init | bootstrap interne : scripts/ensure-deps.sh + offre gsd-new-project/gsd-map-codebase sur confirmation |

<!-- Frontmatter skill (calqué sur consolidator/SKILL.md) : name + description (multiline français). -->
<!-- Skill THIN : frontmatter + une instruction "invoque le skill GSD X, reframe la sortie en VibeFlow". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire references/vocabulary-map.md (traduction GSD → VibeFlow)</name>
  <files>dev-orchestrator/references/vocabulary-map.md</files>
  <action>
    Créer la table de traduction de vocabulaire (ABS-02). Mapper les termes GSD exposés à
    l'utilisateur vers leur équivalent VibeFlow. Au minimum :
    - SUMMARY.md de phase → « rapport de sprint »
    - ROADMAP → « feuille de route »
    - PLAN.md → « plan de travail »
    - phase → « étape »
    - milestone → « jalon »
    - plus toute autre terme GSD susceptible d'apparaître en sortie utilisateur.
    Format : table Markdown `| Terme GSD | Terme VibeFlow |`. Ajouter une note : « Traduction du
    vocabulaire exposé uniquement — pas une traduction exhaustive des artefacts (cf. v2 VOC-01) ».
    Français. C'est la référence que les skills /vf-* et l'agent consultent pour reframer.
  </action>
  <verify>
    <automated>test -f dev-orchestrator/references/vocabulary-map.md && grep -q 'rapport de sprint' dev-orchestrator/references/vocabulary-map.md && grep -q 'feuille de route' dev-orchestrator/references/vocabulary-map.md</automated>
  </verify>
  <done>vocabulary-map.md contient une table GSD → VibeFlow couvrant au moins SUMMARY/ROADMAP/PLAN/phase/milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Écrire les 12 skills /vf-* (thin delegators)</name>
  <files>dev-orchestrator/skills/vf-dev/SKILL.md, dev-orchestrator/skills/vf-brainstorm/SKILL.md, dev-orchestrator/skills/vf-plan/SKILL.md, dev-orchestrator/skills/vf-execute/SKILL.md, dev-orchestrator/skills/vf-quick/SKILL.md, dev-orchestrator/skills/vf-test/SKILL.md, dev-orchestrator/skills/vf-review/SKILL.md, dev-orchestrator/skills/vf-debug/SKILL.md, dev-orchestrator/skills/vf-auto/SKILL.md, dev-orchestrator/skills/vf-ship/SKILL.md, dev-orchestrator/skills/vf-progress/SKILL.md, dev-orchestrator/skills/vf-init/SKILL.md</files>
  <action>
    Créer un SKILL.md par verbe (ABS-01), chacun THIN (frontmatter + instruction de délégation
    + reframe). Calquer le frontmatter sur consolidator/SKILL.md : `name: vf-<verbe>`,
    `description:` (multiline français : quand déclencher + ce qu'il fait). Corps minimal :
    « Invoque le skill <cible réelle>. Reframe toute sortie en vocabulaire VibeFlow (cf. references/vocabulary-map.md). Ne nomme jamais GSD. »
    Mappings (depuis <interfaces>, cibles RÉELLES, aucune orpheline) :
    - vf-dev : route générique → analyse l'intention et délègue au bon verbe (ou délègue à l'agent vibeflow-dev).
    - vf-brainstorm → superpowers brainstorming ; vf-plan → gsd-discuss-phase puis gsd-plan-phase ;
      vf-execute → gsd-execute-phase ; vf-quick → gsd-quick ; vf-test → gsd-verify-work ;
      vf-review → gsd-code-review ; vf-debug → gsd-debug ; vf-auto → gsd-autonomous ;
      vf-ship → gsd-ship ; vf-progress → gsd-progress.
    - vf-init : SPÉCIAL (bootstrap) → invoque `scripts/ensure-deps.sh` ; SI du code existe, propose
      `gsd-map-codebase` (non-interactif) ; propose `gsd-new-project` UNIQUEMENT sur confirmation
      explicite de l'utilisateur (BOOT-04). Mentionner explicitement `ensure-deps` dans le corps.
    Préciser dans chaque skill qu'il est invocable par l'utilisateur ET par l'agent en autonomie (D6).
    Chaque fichier ≤500L (largement le cas pour des skills thin). Français.
  </action>
  <verify>
    <automated>for v in dev brainstorm plan execute quick test review debug auto ship progress init; do test -f "dev-orchestrator/skills/vf-$v/SKILL.md" || { echo "MISSING vf-$v"; exit 1; }; n=$(wc -l < "dev-orchestrator/skills/vf-$v/SKILL.md"); [ "$n" -le 500 ] || { echo "vf-$v too long: $n"; exit 1; }; grep -q "name: vf-$v" "dev-orchestrator/skills/vf-$v/SKILL.md" || { echo "bad frontmatter vf-$v"; exit 1; }; done; grep -q 'gsd-execute-phase' dev-orchestrator/skills/vf-execute/SKILL.md && grep -q 'gsd-autonomous' dev-orchestrator/skills/vf-auto/SKILL.md && grep -q 'ensure-deps' dev-orchestrator/skills/vf-init/SKILL.md</automated>
  </verify>
  <done>Les 12 skills /vf-* existent, chacun ≤500L avec frontmatter valide, chaque mapping pointe vers une cible réelle, vf-init invoque ensure-deps.sh.</done>
</task>

</tasks>

<verification>
- ABS-01 : set complet /vf-* invocable utilisateur ET agent, chaque mapping vers une cible réelle (aucune orpheline — vérifié contre gsd-skills-index.md en Plan 05) ✓
- ABS-02 : traduction de vocabulaire (vocabulary-map.md) référencée par les skills ✓
- vf-init branche ensure-deps.sh + garde-fou init (gsd-new-project sur confirmation) ✓
- Densité skills ≤500L ✓
</verification>

<success_criteria>
- 12 verbes /vf-* thin, mappings non orphelins, reframe vocabulaire systématique.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-04-SUMMARY.md`
</output>

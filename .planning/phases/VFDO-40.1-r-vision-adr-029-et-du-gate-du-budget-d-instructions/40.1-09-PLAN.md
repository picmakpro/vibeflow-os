---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 09
lot: L5b — content-bundle
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/content-bundle/content/BUNDLE.md
  - plugin/content-bundle/content/agents/repurposer.blueprint.md
  - plugin/content-bundle/content/agents/scriptwriter.blueprint.md
  - plugin/content-bundle/content/agents/strategist.blueprint.md
  - plugin/content-bundle/content/registres.md
  - plugin/content-bundle/scripts/tests/test-content-bundle.sh
  - plugin/content-bundle/VERSION
  - plugin/content-bundle/module.json
  - plugin/content-bundle/README.md
  - plugin/content-bundle/CHANGELOG.md

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01 — le bundle énonce le plafond de ses agents à 300 lignes (BUNDLE.md, trois blueprints, registres.md)"
    - "La suite T2 accepte 300 et refuse 301 — `tools/prove-density-suites.sh content-bundle 300 301` rc 1 avant ce plan, rc 0 après ; assertion binaire (RESEARCH A1)"
    - "Recensement nul sous `plugin/content-bundle/` ; module bumpé en patch sur cible dérivée à l'exécution (`tools/check-module-bump.sh content-bundle patch`), triade et en-tête alignés, suite verte"
  artifacts:
    - path: "plugin/content-bundle/scripts/tests/test-content-bundle.sh"
      provides: "T2 à 300"
  key_links:
    - "Correction du recensement du head : RESEARCH ne trouvait aucun littéral dans `plugin/content-bundle/content/` avec `git grep -w` ; la regex versionnée en trouve (formes `≤250L` collées) — la liste de ce plan vient de l'outil, pas de RESEARCH"
---

<objective>
Aligner le module `content-bundle` sur le plafond ADR-029 de 300 lignes (D-01, arbitrage Samuel,
AskUserQuestion session principale, relais SendMessage, 2026-09-16) : contenu distribué, suite T2,
bump patch.

Purpose: BUDG-04 sur un bundle distribué.
Output: cinq documents, une suite relevée et prouvée, module bumpé en patch.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : contenu distribué du bundle à 300 (BUNDLE.md, blueprints, registres)</name>
  <files>plugin/content-bundle/content/BUNDLE.md, plugin/content-bundle/content/agents/repurposer.blueprint.md, plugin/content-bundle/content/agents/scriptwriter.blueprint.md, plugin/content-bundle/content/agents/strategist.blueprint.md, plugin/content-bundle/content/registres.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/content-bundle/content/`
    - les cinq fichiers aux lignes listées
  </read_first>
  <action>
Pour chaque ligne listée : remplacement de la valeur du plafond par 300, graphie conservée
(`≤300L`, `` `≤300L` ``, `agents ≤300L`, titre `(≤300L chacun)`). Aucune autre modification.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/content-bundle\\/content\\// {c++} END{print c+0}" "$T/o"); git diff --numstat "$B" -- plugin/content-bundle/content > "$T/ns"; m=$(awk "END{print NR}" "$T/ns"); s=$(awk "\$1!=\$2{c++} END{print c+0}" "$T/ns"); echo "contenu content-bundle vivants=$n fichiers_modifies=$m fichiers_non_equilibres=$s"; [ "$n" -eq 0 ] && [ "$m" -ge 5 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0 ; ou `fichiers_modifies=` inférieur à 5 ; ou `fichiers_non_equilibres=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `plugin/content-bundle/content/` ; chaque fichier modifié : lignes ajoutées = lignes retirées
  </acceptance_criteria>
  <done>Le contenu distribué du bundle énonce 300.</done>
</task>

<task type="auto">
  <name>Task 2 : suite T2 à 300, preuve, bump patch dérivé</name>
  <files>plugin/content-bundle/scripts/tests/test-content-bundle.sh, plugin/content-bundle/VERSION, plugin/content-bundle/module.json, plugin/content-bundle/README.md, plugin/content-bundle/CHANGELOG.md</files>
  <read_first>
    - plugin/content-bundle/scripts/tests/test-content-bundle.sh (en-tête L7, T2 L66-80)
    - plugin/content-bundle/CHANGELOG.md, plugin/content-bundle/README.md (`**Version**`)
  </read_first>
  <action>
Suite : T2 `-le 300`, messages `(>300)` et `agents ≤300L`, en-tête ; skill ≤ 500 intact. Bump **patch** dérivé de la VERSION lue à l'exécution (`VERSION`, `module.json`, `**Version**` du
README), en UN commit au sujet `chore(40.1-09): …` ; entrée CHANGELOG en tête
`## [<cible>] — <date d'exécution> (Phase 40.1)` : plafond ADR-029 des agents du bundle relevé à 300
lignes (BUNDLE.md, blueprints, registres, T2) ; autorisation : arbitrage Samuel D-01, AskUserQuestion
session principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh content-bundle 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e -c 'bash plugin/content-bundle/scripts/tests/test-content-bundle.sh > /dev/null; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh content-bundle patch; bash scripts/check-version-sync.sh > /dev/null; T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/content-bundle\\// {c++} END{print c+0}" "$T/o"); echo "content-bundle vivants=$n"; [ "$n" -eq 0 ]'</automated>
    <fails_when>exit non nul (suite, bump dérivé ou version-sync rouge) ; ou `vivants=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - preuve 300/301 rc 0 ; suite verte ; zéro ligne vivante sous le module ; `check-module-bump.sh content-bundle patch` rc 0 ; entrée CHANGELOG avec canal et date
  </acceptance_criteria>
  <done>content-bundle bumpé en patch sur cible dérivée, T2 prouvé à 300/301.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blueprint distribué → agent de lab | la valeur énoncée devient la contrainte du lab |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-24 | Repudiation | forme collée `≤250L` invisible à un recensement `-w` | medium | mitigate | liste pilotée par la regex versionnée (40.1-01), recensement nul exigé sous le module |
</threat_model>

<verification>
Preuve 300/301 rc 0 ; suite verte ; recensement nul ; version-sync vert.
</verification>

<success_criteria>
content-bundle énonce et teste 300, bumpé en patch sur cible dérivée.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs et bump patch dérivé.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-09-SUMMARY.md` when done
</output>

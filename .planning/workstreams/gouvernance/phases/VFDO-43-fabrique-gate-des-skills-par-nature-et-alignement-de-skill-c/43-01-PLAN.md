---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md
  - plugin/conductor/scripts/check-skills.sh
  - plugin/conductor/scripts/tests/test-check-skills.sh
  - plugin/conductor/scripts/check-agents-manifest.json
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
autonomous: true
requirements: [FABR-06, FABR-09]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "check-skills.sh découvre RÉCURSIVEMENT les fichiers nommés exactement SKILL.md sous --skills-dir (défaut .claude/skills), à toute profondeur, sans suivre les liens symboliques de dossiers, en élaguant les dossiers cachés et les dossiers *-references (deux lignes d'élagage uniques, chacune tuée par un mutant)"
    - "Un SKILL.md SANS vf-nature est jugé « outil » (défaut B-03) et passe (rc 0) ; une valeur PRÉSENTE hors de referentiel|outil|procedure est refusée (rc 1), jamais ramenée à « outil »"
    - "Un SKILL.md vf-nature: procedure ne passe (rc 0) que s'il porte ecrit: (scalaire ou liste, chaque entrée non vide et de forme valide) ET vf-rubrique-juge: non vide et de forme valide ; l'absence de l'un, de l'autre ou des deux sort en rc 1 avec « FABR-06 » et le chemin relatif du fichier (FABR-06, D-Q1, D-Q2)"
    - "Les trois clés de marqueurs vf-gate-bloquant, vf-livrable-tiers, vf-couche-qualite sont optionnelles et n'acceptent que true ou false (rc 1 sinon)"
    - "Contrat de sortie identique à check-agents.sh : 0 conforme (avertissements permis), 1 non conforme ou invocation invalide, 3 INDÉTERMINÉ (cible absente hors --hook, jeton CIBLE-ABSENTE ; cible présente vide sous --strict sans --allow-empty) ; sous --hook, 3 devient 0 à la frontière du shell (hook_exit) et le régime nominal n'imprime rien"
    - "Le manifeste daté check-agents-manifest.json porte une septième liste champs_frontmatter_skills (verifie_le, source https://code.claude.com/docs/en/skills, valeurs relues sur la doc officielle le jour de l'exécution) ; check-agents.sh ET check-skills.sh refusent tous deux (MANIFESTE-ILLISIBLE, rc 1) un manifeste qui n'a pas exactement les sept listes"
    - "Le corpus réel — chaque dossier plugin/*/ qui porte au moins un SKILL.md, sauf un module dont le module.json déclare le type doc-only — passe check-skills.sh --strict (rc 0) ; le nombre de SKILL.md jugés égale le compte find de ces mêmes dossiers et le nombre de SKILL.md exclus (doc-only) est imprimé, jamais un skip muet"
    - "Dans un lab frais (HOME temporaire), check-skills.sh posé par l'installeur trouve le manifeste à sept listes et rend rc 0 en --strict sur .claude/skills ; check-agents.sh du même lab reste rc 0"
    - "test-check-agents.sh reste à 0 KO avec le manifeste à sept listes (aucune régression de la Phase 42)"
  artifacts:
    - path: "plugin/conductor/scripts/check-skills.sh"
      provides: "gate des skills par nature : decouvrir_skills, charger_manifeste (sept listes), parse_frontmatter, invariant_procedure, valider_nature, valider_ecrit, contrat 0/1/3, hook_exit"
      contains: "def invariant_procedure"
    - path: "plugin/conductor/scripts/tests/test-check-skills.sh"
      provides: "T1 à T21, MUT-S1, MUT-S2, MUT-S3, MUT-SD1, MUT-SD2, gardes du harnais MUT-SYNTAXE (mutant Python compilé) et MUT-REFUS-COMPTE (refus compté KO)"
      contains: "MUT-SD2"
    - path: "plugin/conductor/scripts/check-agents-manifest.json"
      provides: "septième liste champs_frontmatter_skills"
      contains: "champs_frontmatter_skills"
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "cles_listes élargie aux sept listes, commentaires « six » corrigés"
      contains: "champs_frontmatter_skills"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "T107 — manifeste sans la septième liste refusé par check-agents.sh"
      contains: "T107"
  key_links:
    - from: "check-skills.sh (charger_manifeste)"
      to: "check-agents-manifest.json"
      via: "chemin dérivé du dossier du script, validation des sept listes à l'identique de check-agents.sh"
      pattern: "cles_listes = .*champs_frontmatter_skills"
    - from: "check_file() de check-skills.sh"
      to: "invariant_procedure()"
      via: "appel unique errors.extend(invariant_procedure(...)) — cible de MUT-S1"
      pattern: "errors.extend\\(invariant_procedure\\("
    - from: "installeur (copy_module_scripts, *.sh et *.json)"
      to: "lab .claude/scripts/check-skills.sh + check-agents-manifest.json"
      via: "module conductor, témoin lab frais TRACER-SKILLS-OK"
      pattern: "TRACER-SKILLS-OK"
  prohibitions:
    - statement: "MUST NOT dériver, attribuer ou réécrire vf-nature (ni une clé de marqueur) à partir de la prose d'un skill : le gate constate une déclaration et la valide, il ne classe jamais un skill lui-même (B-03 : dérivation automatique écartée ; D-Q1)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans descripteur câblé — relecture en PR"
    - statement: "MUST NOT modifier un SKILL.md depuis le gate, ni ajouter vf-nature ou des marqueurs aux SKILL.md existants du corpus dans cette phase (ADR-031 : jamais de correction sans validation humaine ; D-Q5 : mise en conformité en backlog séparé)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : se relit sur le diff de la PR (aucun SKILL.md du corpus dans files_modified)"
---

<objective>
Poser le gate des skills par nature (`check-skills.sh`) comme tracer de la phase : il lit le manifeste
daté partagé, étendu d'une septième liste (champs natifs des SKILL.md), découvre récursivement le
corpus réel et refuse une procédure qui ne déclare ni `ecrit:` ni `vf-rubrique-juge`, du cas de
fixture jusqu'au lab frais posé par l'installeur.

Purpose: FABR-06 (gate `vf-nature`, défaut « outil », procédure sans les deux champs refusée —
D-Q1, D-Q2) et la clause de FABR-09 « même manifeste daté que la Phase 42, étendu des listes propres
aux skills » (Claude's Discretion de 43-CONTEXT.md : une seule vérité). Ce plan fixe aussi le
CONTRAT DE CLÉS que 43-02 (dérive), 43-03 (skill-creator) et plus tard l'initialisation (Phase 50)
et le moteur (Phase 44) consomment.
Output: `check-skills.sh`, `test-check-skills.sh` (T1 à T21, cinq mutants), septième liste du
manifeste, `check-agents.sh` élargi en lockstep, T107 dans `test-check-agents.sh`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-PATTERNS.md

Faits mesurés le 2026-09-25 (à re-mesurer avant d'écrire les fixtures, jamais recopiés en dur dans
un test) : `find plugin -name SKILL.md | wc -l` → 25, à trois profondeurs — 8 à `plugin/<mod>/SKILL.md`
(dont `plugin/installer/SKILL.md`, dossier SANS module.json, skill exposé par
`plugin/.claude-plugin/plugin.json` champ `skills`), 13 à `plugin/<mod>/skills/<nom>/SKILL.md`, 4 à
`plugin/reference/content/methodology/templates/skills/<nom>/SKILL.md` (module `reference`, type
`doc-only`, gabarits à marqueurs `[NOM_LAB]`). Le glob `plugin/*/skills/**/SKILL.md` de 43-CONTEXT.md
n'en voit que 13 (43-RESEARCH.md Pitfall 1). Aucun SKILL.md du corpus ne porte aujourd'hui de clé
`vf-*` ; `vf-mcp-tools` n'est porté que par un AGENT (`plugin/dev-orchestrator/agents/vf-reviewer.md:10`).

Champs natifs des SKILL.md relevés sur https://code.claude.com/docs/en/skills le 2026-09-25 (à relire
le jour de l'exécution, la liste réelle fait foi) : name, description, when_to_use, argument-hint,
arguments, disable-model-invocation, user-invocable, allowed-tools, disallowed-tools, model, effort,
context, agent, background, hooks, paths, shell, metadata, license, compatibility.
Référence de phase (B43, révision du 2026-09-26 ; propriétaire unique fixé au tour 3) : un SHA
FIGÉ, jamais re-dérivé. Il vit dans le fichier neutre `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md`,
sur une ligne unique « Base de phase : <sha de 40 caractères> ». PROPRIÉTAIRE UNIQUE de sa création :
ce plan, 43-01 (fichier déclaré dans son `files_modified`), à l'étape 0 de sa Tâche 1, AVANT son
premier commit (`git rev-parse HEAD`), dans un commit `.planning/` séparé qui ne touche que ce
fichier, de sujet exact `docs(43): base de phase figée`. Aucun autre plan ne l'écrit : 43-01 est le
seul plan sans dépendance de la phase et tous les autres en dépendent, directement ou non (43-04 par
`depends_on` depuis le tour 3, ce qui le fait passer en vague 2) — la vague 1 ne contient que 43-01,
donc aucun commit d'un autre plan ne peut précéder la base, en exécution séquentielle, en checkout
partagé comme en worktrees parallèles. Jamais écrite en
planification (le SHA de la branche de planification change au merge) ni dans un PLAN.md, et
jamais re-dérivée par un `git log` sur les PLAN.md : toute révision de plan pendant l'exécution
(cas prévu pour la réponse à Q-PORTEE, finalement consignée dans 43-02-PLAN.md dès la planification,
tour 4 du 2026-09-26) déplacerait alors la base et viderait en silence,
tout en restant verts, les contrôles qui la lisent. Tout contrôle « ce que la Phase 43 a touché »
la lit par la chaîne
`B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && …` :
base absente, malformée ou sortie de l'histoire → la chaîne s'arrête sans rien imprimer, et « rien
d'imprimé » vaut échec partout où un critère attend une valeur ou un jeton. Pourquoi pas la base de
branche : la branche porte encore la Phase 42 tant que la PR #108 n'est pas mergée dans origin/main
(ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de la base de branche — mesuré le
2026-09-25). REBASE — procédure univoque (tour 3) : après un rebase (merge de fiabilite, squash de
la PR #108 dans main, ou toute réécriture de la branche), si le SHA consigné sort de l'histoire, la
chaîne rougit et tout plan qui la lit S'ARRÊTE sans rien écrire. La re-consignation n'est jamais un
geste de plan ni un choix : c'est un geste séparé de l'orchestrateur, entre deux plans (jamais
pendant une vague), dont la nouvelle base est le PARENT du commit de création de
`43-BASE-PHASE.md` — identifiant stable : l'unique commit de l'histoire de HEAD qui AJOUTE ce
chemin, ne touche que lui et porte le sujet exact `docs(43): base de phase figée` (un rebase le
réécrit sans le perdre, il appartient à la branche de la phase) —, rendue par
`P=.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md; a=$(git log --diff-filter=A --format=%H -- "$P"); test "$(printf '%s\n' "$a" | grep -c .)" = 1 && test "$(git diff-tree --no-commit-id --name-only -r "$a")" = "$P" && test "$(git show -s --format=%s "$a")" = "docs(43): base de phase figée" && nb=$(git rev-parse "$a^") && git merge-base --is-ancestor "$nb" HEAD && echo "BASE-REBASE $nb" || echo "BASE-PHASE-INTROUVABLE"`.
« BASE-REBASE <sha> » : la ligne active devient « Base de phase : <sha> », l'ancienne est gardée sous
« Base de phase (avant rebase du <date>) : <ancien sha> », commit `.planning/` séparé
`docs(43): base de phase re-consignée (rebase du <date>)`. « BASE-PHASE-INTROUVABLE » — commit de
création absent, en double, fusionné avec d'autres changements (squash de la branche de la phase)
ou de sujet différent — : ARRÊT EN ROUGE et retour à l'humain ; ni l'exécutant ni l'orchestrateur
ne choisit alors une base de son propre chef (ni la base de branche, ni un commit « de même sujet »
que l'ancienne base, ni un commit « qui ressemble »). Les gardes G-1 et G-2 gardent leur propre
base : elles jugent toute la branche, Phase 42 comprise.
</context>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tracer — check-skills.sh lit le manifeste à sept listes, découvre les SKILL.md et refuse une procédure sans ecrit:/vf-rubrique-juge, de la fixture au lab frais (T1 à T5, T107)</name>
  <reversibility rating="costly">Les six clés (vf-nature, ecrit, vf-rubrique-juge, vf-gate-bloquant, vf-livrable-tiers, vf-couche-qualite) et la septième liste du manifeste sont lues par deux gates, par skill-creator (43-03), puis par l'initialisation (Phase 50) et le moteur (Phase 44) ; les renommer après adoption par des labs imposerait une migration de leurs SKILL.md (D-Q1, costly).</reversibility>
  <precondition>command -v jq réussit (plugin/_internal/resolve-deps.sh l'exige pour la fermeture de conductor du témoin lab frais)</precondition>
  <files>.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md, plugin/conductor/scripts/check-skills.sh, plugin/conductor/scripts/tests/test-check-skills.sh, plugin/conductor/scripts/check-agents-manifest.json, plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (analogue structurel ET fichier modifié : l.1-160 en-tête et contrat ; l.166-243 parsing CLI, rejet des valeurs invalides, hook_exit ; l.246-256 SCRIPT_DIR, VF_MANIFEST, détection PYBIN ADR-054 ; l.277-316 conventions VIBEFLOW_FIELDS et decouvrir_agents ; l.341-440 charger_manifeste (cles_listes l.368-370), manifeste_perime, charger_referentiel ; l.456-505 parse_frontmatter, frontmatter_lines ; l.778-795 invariant_i1 ; l.1100-1260 boucle principale, CIBLE-ABSENTE, F13, rapport, sortie)
    - plugin/conductor/scripts/check-agents-manifest.json (fichier modifié : forme {verifie_le, source, valeurs}, clé rafraichissement)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : l.1-60 catalogue ; l.240-380 ok, ko, okmut, komut, mk_manifest, mk_gate_dir, make_gate_mutant ; cas T104 pour la forme d'un cas récent)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-PATTERNS.md (sections check-skills.sh, check-agents-manifest.json, test-check-skills.sh)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Pattern 1, Pitfall 1, Code Examples « Miroir I1 → nouvel invariant FABR-06 », Open Questions 1 et 2)
    - docs/superpowers/specs/2026-09-22-fabrique-agents-skills-design.md (§6 le gate des skills, B-03)
    - docs/superpowers/specs/2026-09-22-moteur-planning-metier-design.md (D-10 : le champ de périmètre s'appelle ecrit:)
    - docs/superpowers/specs/2026-09-23-initialisation-lab-design.md (§5.2 famille 2 : les trois marqueurs de B-03 en questions factuelles, C-15)
  </read_first>
  <behavior>
    - T1 : SKILL.md avec name + description, sans vf-nature → rc 0, ligne « ✓ skills conformes » citant 1 SKILL.md jugé
    - T2 : vf-nature: procedure + ecrit: clients/<nom>/diagnostics/ + vf-rubrique-juge: grilles/diagnostic.md → rc 0
    - T3 : vf-nature: procedure sans ecrit ni vf-rubrique-juge → rc 1, sortie contenant « FABR-06 » et le chemin relatif (ex. proc/SKILL.md), jamais seulement « SKILL.md »
    - T4 (arbre réel) : pour chaque dossier plugin/*/ portant au moins un SKILL.md et dont le module.json (s'il existe) ne déclare pas le type doc-only, `check-skills.sh --strict --skills-dir=plugin/<mod>` → rc 0 ; somme des SKILL.md jugés (lue sur les lignes ✓) = somme des `find plugin/<mod> -name SKILL.md -type f` ; le nombre de SKILL.md exclus (doc-only) est imprimé, sans valeur assertée en dur
    - T5 : manifeste sans la septième liste (mk_manifest sans-liste=champs_frontmatter_skills) → check-skills.sh rc 1 avec MANIFESTE-ILLISIBLE
    - T107 (test-check-agents.sh) : même manifeste amputé de la septième liste → check-agents.sh rc 1 avec MANIFESTE-ILLISIBLE
  </behavior>
  <action>
0. Base de phase — ce plan en est le SEUL propriétaire (bloc de contexte, Référence de phase ; fichier déclaré dans `files_modified`). AVANT tout commit de ce plan : écrire `43-BASE-PHASE.md` dans le dossier de la phase avec l'unique ligne « Base de phase : <sortie de git rev-parse HEAD> », puis le commiter aussitôt, SEUL (aucun autre chemin dans ce commit), sous le sujet exact `docs(43): base de phase figée` — commit à fichier unique et sujet exact sont l'identifiant stable que relit la procédure de rebase. Si le fichier existe déjà au démarrage (reprise d'une exécution interrompue de ce plan), ne jamais le réécrire : le critère BASE-PHASE-OK tranche — vert, on continue ; rouge, arrêt et retour à l'orchestrateur. Aucun autre plan de la phase ne l'écrit (tous dépendent de 43-01) ; seule la re-consignation après rebase du bloc de contexte, geste séparé et déterministe de l'orchestrateur, peut le modifier.
1. Décisions de plan, reportées telles quelles dans l'en-tête de check-skills.sh (source : 43-CONTEXT.md, 43-RESEARCH.md Open Questions 1 et 2, arbitrées ici) :
   a. Contrat de clés (costly, D-Q1) : `vf-nature` (referentiel | outil | procedure — défaut « outil » UNIQUEMENT quand la clé est absente) ; `ecrit` (nom canonique, moteur D-10 ; scalaire ou liste de chemins) ; `vf-rubrique-juge` (scalaire : chemin relatif vers la grille du juge, ou nom de l'agent juge) ; trois marqueurs factuels optionnels de B-03/C-15 : `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` (true | false). Ces six clés sont des conventions VibeFlow : ensemble `VIBEFLOW_SKILL_FIELDS` en dur dans le script, JAMAIS dans le manifeste — même frontière que D-01 de la Phase 42 (le manifeste ne porte que des listes d'origine native, périssables avec une doc externe ; une convention du dépôt ne périme pas).
   b. Manifeste (Claude's Discretion, une seule vérité) : septième liste `champs_frontmatter_skills` dans check-agents-manifest.json, forme {verifie_le, source, valeurs} ; source `https://code.claude.com/docs/en/skills` ; valeurs = champs natifs relus ce jour-là sur la page (liste du bloc de contexte ci-dessus comme point de départ, la page fait foi ; chaque valeur doit tenir dans le charset `[A-Za-z0-9_-]+`, ce qui est le cas de when_to_use et disable-model-invocation) ; verifie_le = date de la relecture. Les six autres listes ne sont PAS re-datées (règle `rafraichissement` du manifeste : jamais un changement de date seul).
   c. Découverte (corrige Pitfall 1) : récursive, reprise de decouvrir_agents avec le filtre « nom de fichier == SKILL.md ». Le gate reste générique (`--skills-dir`) ; l'exclusion des gabarits `plugin/reference` n'est jamais un nom de module en dur dans le gate : elle est portée par l'invocation sur le dépôt, qui lit le type `doc-only` du module.json (cas T4).
   d. Identité dans les messages : chemin relatif à `--skills-dir` (le nom de base vaut toujours SKILL.md et n'identifie rien) ; en `--file`, le chemin tel que passé.
2. Manifeste et check-agents.sh dans le MÊME commit (sinon check-agents.sh refuse le manifeste : égalité stricte l.368-370) : ajouter la septième liste ; dans check-agents.sh, élargir `cles_listes` à sept clés et faire dire au message « attendu exactement les sept cles » ; corriger les trois commentaires qui disent « six » (en-tête l.19-23, l.277, l.341) ; `KNOWN` reste calculé sur `champs_frontmatter` seulement (les agents ne changent de rien d'autre).
3. check-skills.sh (nouveau, exécutable), miroir de check-agents.sh :
   - en-tête : usage (`--skills-dir=PATH` défaut .claude/skills, `--file PATH`, `--strict`, `--allow-empty`, `--hook`, `--third-party-prefix=PFX` répétable défaut gsd- et cumulatif, `--no-third-party-prefix`, `-h`), contrat 0/1/3, doctrine (B-03, D-Q1, D-Q2, D-Q5, FABR-06, FABR-07 à venir en 43-02), frontière manifeste/conventions, découverte et exclusions, écart assumé vis-à-vis de D-11 de la Phase 42 (la dérive restera un avertissement, D-Q5) ;
   - bash : parsing de la même forme que check-agents.sh (`for arg in "$@"`, `--file` à deux arguments) ; `--skills-dir=` vide → exit 1 explicite ; `hook_exit` identique ; `SCRIPT_DIR` et `VF_MANIFEST` dérivés du dossier du script, jamais de l'environnement ni du cwd ; détection `PYBIN` identique (ADR-054) ;
   - le code Python passe par un here-document à délimiteur QUOTÉ (aucune interpolation shell du code, paramètres par variables d'environnement `VF_*`) — écart délibéré à la forme `-c "…"` de check-agents.sh, qui ne change aucun contrat et retire toute surface d'interpolation ;
   - Python : `charger_manifeste` recopiée (même validation, sept clés) ; `decouvrir_skills(racine, refuses)` recopiée de decouvrir_agents avec le filtre sur le nom SKILL.md, DEUX lignes d'élagage distinctes et uniques (dossiers cachés ; dossiers *-references), `os.walk(followlinks=False)`, refus d'un SKILL.md en lien symbolique sans l'ouvrir ; `parse_frontmatter` recopiée ; `valider_nature(rel, fm)` (enum, rend une liste) ; `invariant_procedure(rel, fm)` sur le patron invariant_i1 (rend une LISTE) : nature absente → outil ; si procedure, manquants parmi `ecrit` et `vf-rubrique-juge` (valeur vide, liste vide ou chaîne vide = absente) → « {rel} : vf-nature: procedure sans <manquants joints par ' ni '> (B-03, FABR-06) » ; dans `check_file`, deux appels uniques sur leur propre ligne : `errors.extend(valider_nature(` puis `errors.extend(invariant_procedure(` (cibles de MUT-S2 et MUT-S1 en Tâche 2) ;
   - boucle principale calquée sur check-agents.sh : branche `--file` (introuvable → rc 1 ; lien symbolique refusé) ; CIBLE-ABSENTE → exit 3 hors `--hook` sauf `--allow-empty` ; F13 (cible présente vide : 3 sous `--strict` sans `--allow-empty`, 0 sinon) ; exclusion des skills tiers par préfixe du `name:` avec compteur publié à part ; chargement PARESSEUX du manifeste (seulement s'il y a au moins un fichier à juger) ; MANIFESTE-ILLISIBLE → rc 1 (0 sous `--hook`) ;
   - rapport : hors `--hook`, avertissements `  ⚠ …`, puis, s'il y a des erreurs, `[check-skills] ✗ <N> non-conformite(s) bloquante(s) :` suivi de `  ✗ …` et rc 1, sinon `[check-skills] ✓ skills conformes (nature B-03) · <N> SKILL.md juge(s)` (+ ` · <W> warning(s)`) et rc 0 ; sous `--hook`, silence total à 0 erreur et 0 avertissement, une ligne compacte sinon, rc 0 ;
   - fraîcheur : `manifeste_perime` appliquée à la seule liste `champs_frontmatter_skills` ; liste périmée → une ligne `⚠ MANIFESTE-PERIME`, jamais un refus (la seule liste fermée consommée n'alimente qu'un avertissement « champ inconnu »). Pas d'option `--manifest-freshness` : la fraîcheur du MÊME fichier est déjà jugée INDÉTERMINÉE en CI par les quatre appels `--manifest-freshness=strict` de check-agents.sh, qui valide désormais les sept listes.
4. test-check-skills.sh (nouveau) : catalogue d'en-tête ; harnais repris de test-check-agents.sh (ok, ko, okmut, komut ; `mk_manifest` re-datant TOUTES les listes du manifeste réel à « aujourd'hui moins N jours » calculé au run — jamais une date en dur ; `mk_gate_dir` copiant check-skills.sh et le manifeste dans un dossier temporaire ; `make_gate_mutant` scopé à check-skills.sh, appelé DIRECTEMENT et jamais dans une substitution de commande, qui rend le chemin du dossier mutant dans la variable globale `MUT_DIR` (jamais sur stdout) — raison (constat du 2026-09-26 sur le harnais de test-check-agents.sh, 16 appels du helper en substitution de commande, dont l.2574-2597) : dans une substitution de commande, le helper tourne dans un sous-shell, la ligne de son `komut` part dans la variable capturée et l'incrément de `fail` est perdu au retour ; aucune branche else ne le rattrape, un mutant refusé disparaît donc du décompte final — défaut à ne pas recopier. Le helper remplace la LIGNE ENTIÈRE portant le motif et refuse le mutant — son `komut` s'exécute alors dans le shell appelant : ligne de refus imprimée et KO compté dans la ligne « == Résultat », puis rc 1 — si le motif n'est pas unique, si `cmp` ne voit aucune différence, si `bash -n` échoue, OU si le Python du fichier muté ne compile pas — il extrait le corps du here-document Python quoté (entre la ligne qui l'ouvre et la ligne égale à son délimiteur) et le passe à `"$PYBIN" -c` avec `compile(open(f, encoding="utf-8").read(), f, "exec")` ; raison : `bash -n` ne voit rien d'un here-document quoté, et un Python tronqué plante en SyntaxError, rc 1, sans « Traceback » — un rc de plantage ne doit jamais pouvoir passer pour un mutant tué ; l'appelant enchaîne sur `MUT_DIR` seulement si le rc du helper vaut 0 (le refus est déjà compté) ; `okmut` accepte un sixième argument optionnel, la trace du verdict visé, imprimée après ` · trace : ` — trace écrite en toutes lettres (« en erreur », « en avertissement »), jamais avec le glyphe de la croix d'échec : la ligne de succès qui la porte serait sinon rattrapée par le `fails_when` « une ligne contient ✗ » de toutes les suites, et le vert deviendrait impossible) ; libellés des cas commençant par « T<n> » suivi d'une espace ; dernière ligne `== Résultat : <pass> OK · <fail> KO ==`. Cas T1 à T5 (voir behavior). Dans test-check-agents.sh, ajouter T107 (catalogue d'en-tête compris), et reformuler le seul libellé de SUCCÈS existant qui porte le glyphe de la croix — T84, l.2061, qui finit par « aucun » suivi du glyphe — en « … aucune ligne d'erreur », sans toucher à son assertion ni au début du libellé `ok "T84 périmé SANS option` (repère de la sonde T84-LOGIQUE-IDENTIQUE des critères : seule la fin de cette ligne change, jamais une autre ligne du bloc T84) : cette ligne de succès rend aujourd'hui insatisfiable le `fails_when` « une ligne contient ✗ » posé sur test-check-agents.sh par ce plan et par 43-05 (constat du 2026-09-26 ; même règle que les traces de okmut : une ligne de succès ne porte jamais la croix).
5. Rejouer test-check-skills.sh, test-check-agents.sh (suite complète) et le témoin lab frais (commandes de vérification).
6. Commit : `feat(conductor): gate des skills par nature — tracer (FABR-06)` avec un trailer par chemin, jamais une liste à virgules :
   `Gate-Touche: plugin/conductor/scripts/check-skills.sh — nouveau gate des skills par nature (FABR-06, D-Q1)`
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-skills.sh — suite du gate des skills, T1 à T5`
   `Gate-Touche: plugin/conductor/scripts/check-agents.sh — manifeste partagé élargi à sept listes (FABR-09)`
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T107, manifeste amputé de la septième liste refusé ; libellé de succès de T84 sans glyphe de croix`
   `Gate-Touche: plugin/conductor/scripts/check-agents-manifest.json — septième liste champs_frontmatter_skills (hors surface G-2, déclaré par consigne de mission)`
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-skills.sh 2>&1 | grep -E '✓ T[1-5] |✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T1 à T5 n'a aucune ligne « ✓ », ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T107|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou aucune ligne « ✓ T107 », ou la ligne « == Résultat » ne finit pas par « 0 KO == ». Mesuré le 2026-09-26 avant écriture : la ligne de succès de T84 porte la croix et rougit ce critère à elle seule — d'où sa reformulation à l'étape 4.</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q); bash "$R/plugin/_internal/resolve-deps.sh" conductor | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >/dev/null 2>&1); done; test -f "$LAB/.claude/scripts/check-skills.sh" && grep -q champs_frontmatter_skills "$LAB/.claude/scripts/check-agents-manifest.json" && (cd "$LAB" && bash .claude/scripts/check-skills.sh --strict | grep -q '✓ skills conformes') && (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict >/dev/null) && echo TRACER-SKILLS-OK</automated>
    <fails_when>la ligne TRACER-SKILLS-OK est absente (gate non posé dans le lab, manifeste du lab sans la septième liste, check-skills non vert en --strict, ou check-agents du lab rendu rouge par le manifeste élargi)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -x plugin/conductor/scripts/check-skills.sh` réussit ; `grep -c 'def decouvrir_skills(' plugin/conductor/scripts/check-skills.sh` rend 1 ; `grep -c 'followlinks=False' plugin/conductor/scripts/check-skills.sh` rend au moins 1
    - `grep -c 'errors.extend(invariant_procedure(' plugin/conductor/scripts/check-skills.sh` rend 1 et `grep -c 'errors.extend(valider_nature(' plugin/conductor/scripts/check-skills.sh` rend 1
    - `grep -c 'cles_listes = .*champs_frontmatter_skills' plugin/conductor/scripts/check-agents.sh` rend 1 et la même commande sur check-skills.sh rend 1
    - base de phase figée, propriétaire unique (R1, S1 du tour 3) : `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && test "$(grep -c '^Base de phase : ' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md)" = 1 && revs=$(git log --reverse --diff-filter=A --format=%H -- plugin/conductor/scripts/check-skills.sh) && c=$(printf '%s\n' "$revs" | head -1) && test -n "$c" && test "$B43" != "$c" && git merge-base --is-ancestor "$B43" "$c" && git merge-base --is-ancestor "$B43" HEAD && P=.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md && a=$(git log --diff-filter=A --format=%H -- "$P") && test "$(printf '%s\n' "$a" | grep -c .)" = 1 && test "$(git diff-tree --no-commit-id --name-only -r "$a")" = "$P" && test "$(git show -s --format=%s "$a")" = "docs(43): base de phase figée" && test "$(git rev-parse "$a^")" = "$B43" && git merge-base --is-ancestor "$a" "$c" && echo "BASE-PHASE-OK $B43"` imprime `BASE-PHASE-OK <sha>` : une seule ligne de base ; un seul commit de création du fichier, qui ne touche que lui et porte le sujet exact (identifiant stable de la procédure de rebase) ; son PARENT est exactement le SHA consigné — la base a été prise immédiatement avant ce commit, sans aucun commit intercalé — ; ce commit précède le premier commit de code de la Tâche 1. Comme tous les autres plans dépendent de 43-01, aucun commit de la phase ne précède la base (rien d'imprimé vaut échec)
    - logique de T84 inchangée (S7 du tour 3 — seul le libellé de succès a changé) : `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && F=plugin/conductor/scripts/tests/test-check-agents.sh && T="${TMPDIR:-/tmp}" && b=$(git show "$B43:$F") && printf '%s\n' "$b" | awk '/^# T84 — /{f=1} /^# T85 — /{f=0} f' > "$T/t84-base.txt" && awk '/^# T84 — /{f=1} /^# T85 — /{f=0} f' "$F" > "$T/t84-head.txt" && test "$(grep -c . "$T/t84-base.txt")" -ge 15 && test "$(grep -c '^[[:space:]]*ok "T84 périmé SANS option' "$T/t84-base.txt")" = 1 && test "$(grep -c '^[[:space:]]*ok "T84 périmé SANS option' "$T/t84-head.txt")" = 1 && grep -v '^[[:space:]]*ok "T84 périmé SANS option' "$T/t84-base.txt" > "$T/t84-base-sl.txt" && grep -v '^[[:space:]]*ok "T84 périmé SANS option' "$T/t84-head.txt" > "$T/t84-head-sl.txt" && diff "$T/t84-base-sl.txt" "$T/t84-head-sl.txt" && echo "T84-LOGIQUE-IDENTIQUE"` imprime `T84-LOGIQUE-IDENTIQUE` : le bloc T84 (de son commentaire `# T84 — ` au commentaire `# T85 — `, au moins 15 lignes) est extrait à B43 et à HEAD ; l'unique ligne de libellé exclue de chaque côté est la seule qui commence par `ok "T84 périmé SANS option` (exactement une de chaque côté) ; tout le reste — les trois appels du gate, les conditions `if`, dont `grep -q` sur le glyphe de la croix dans la sortie du gate, et les branches `ko` — doit être identique octet pour octet (`diff` vide). Une ligne de condition modifiée, ajoutée ou retirée fait imprimer le diff et supprime le jeton
    - `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && git show "$B43:plugin/conductor/scripts/check-agents-manifest.json" > "${TMPDIR:-/tmp}/manifeste-base.json" && python3 -c 'import json,sys; m=json.load(open(sys.argv[1])); b=json.load(open(sys.argv[2])); l=m["listes"]; assert len(l)==7; s=l["champs_frontmatter_skills"]; assert s["source"].startswith("https://code.claude.com/docs/en/skills"); assert "when_to_use" in s["valeurs"] and "allowed-tools" in s["valeurs"]; assert all(l[k]==b["listes"][k] for k in b["listes"]); print("MANIFESTE-7-OK")' plugin/conductor/scripts/check-agents-manifest.json "${TMPDIR:-/tmp}/manifeste-base.json"` imprime MANIFESTE-7-OK (sept listes ; les six listes d'origine strictement identiques à la base, dates comprises)
    - la suite test-check-skills.sh imprime des lignes `✓ T1`, `✓ T2`, `✓ T3`, `✓ T4`, `✓ T5` et finit sur `0 KO ==` ; test-check-agents.sh imprime `✓ T107` et finit sur `0 KO ==`
    - le témoin lab frais imprime `TRACER-SKILLS-OK`
    - le commit DE LA TÂCHE 1 — celui qui crée check-skills.sh ou sa suite, jamais « le dernier commit qui touche check-skills.sh », qui désignera le commit de la Tâche 2 ou 3 dès qu'ils existent — couvre chaque chemin de la surface G-2 qu'il touche : `revs=$(git log --reverse --diff-filter=A --format=%H -- plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh) || echo "GIT-ECHEC log"; c=$(printf '%s\n' "$revs" | head -1); test -n "$c" || echo "AUCUN-COMMIT-T1"; chemins=$(git diff-tree --no-commit-id --name-only -r "$c" -- plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/tests/test-check-agents.sh) || echo "GIT-ECHEC diff-tree"; msg=$(git show -s --format=%B "$c") || echo "GIT-ECHEC show"; for p in plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/tests/test-check-agents.sh; do printf '%s\n' "$chemins" | grep -qxF "$p" || continue; printf '%s\n' "$msg" | grep -qE "^Gate-Touche: $p (—|-) " || echo "NON-COUVERT $p"; done; echo "COMMIT-T1 $c"` n'imprime aucune ligne NON-COUVERT, GIT-ECHEC ni AUCUN-COMMIT-T1 avant « COMMIT-T1 <sha> » ; le critère reste juste une fois les Tâches 2 et 3 commitées
  </acceptance_criteria>
  <done>Le gate des skills existe de bout en bout : manifeste à sept listes partagé et validé par les deux gates, corpus réel découvert à ses trois profondeurs et jugé vert, procédure sans ses deux champs refusée, lab frais vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : FABR-06 en validation stricte des valeurs — nature, ecrit:, vf-rubrique-juge, marqueurs, frontmatter adverse (T6 à T13, MUT-S1, MUT-S2, MUT-S3)</name>
  <files>plugin/conductor/scripts/check-skills.sh, plugin/conductor/scripts/tests/test-check-skills.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-skills.sh (fichier modifié : valider_nature, invariant_procedure, check_file, parse_frontmatter posés en Tâche 1)
    - plugin/conductor/scripts/tests/test-check-skills.sh (fichier modifié : harnais, make_gate_mutant, T1 à T5)
    - plugin/conductor/scripts/check-agents.sh (l.586-640 dequote_scalar, parse_token : discipline de charset et de refus explicite à reproduire)
    - plugin/conductor/scripts/tests/test-check-agents.sh (l.338-374 make_gate_mutant ; forme des cas T91 à T96 : jumeau conforme + jumeau en violation)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (§ Security Domain, V5 Input Validation)
  </read_first>
  <behavior>
    - T6 : vf-nature valant `Procedure`, `procédure`, `outils`, ou vide (`vf-nature:` sans valeur) → rc 1 pour chacune des quatre fixtures, sortie contenant « vf-nature invalide » ; aucune n'est comptée comme outil
    - T7 : procedure avec ecrit seul → rc 1 « sans vf-rubrique-juge » ; procedure avec vf-rubrique-juge seul → rc 1 « sans ecrit: »
    - T8 : procedure avec `ecrit:` sans valeur ni puce, avec `ecrit: []`, avec `vf-rubrique-juge: ""` → rc 1 (valeur vide = absente)
    - T9 : entrées ecrit refusées (rc 1, « ecrit: entree malformee ») : `/abs/x`, `../x`, `a/../b`, `~/x`, `a;b`, `$(id)`, `a b` ; acceptées (rc 0) : `clients/<nom>/diagnostics/`, liste flow `[livrables/, clients/<nom>/]`, deux puces en bloc, `référentiels/2026/` (accentué)
    - T10 : vf-rubrique-juge en liste → rc 1 ; `../grille.md` → rc 1 ; `vf-design-judge` et `grilles/diagnostic.md` → rc 0
    - T11 : `vf-gate-bloquant: oui` et `vf-livrable-tiers: True` → rc 1 « attendu true|false » ; `vf-couche-qualite: false` → rc 0
    - T12 : clé `vf-natur: outil` → rc 0 et un avertissement « champ inconnu » ; `allowed-tools: Read` et `when_to_use: …` → aucun avertissement
    - T13 : frontmatter jamais refermé → rc 1 « frontmatter jamais referme » ; ecrit contenant un octet d'échappement ANSI (ESC) → rc 1 et la sortie ne contient AUCUN octet ESC brut (valeur imprimée échappée, tronquée à 80 caractères)
    - MUT-S1 : ligne `errors.extend(invariant_procedure(` remplacée par `pass` → fixture T3 : rc_original=1, rc_mutant=0
    - MUT-S2 : ligne `errors.extend(valider_nature(` remplacée par `pass` → fixture T6 `Procedure` : rc_original=1, rc_mutant=0
    - MUT-S3 : ligne d'appel de la validation de forme de ecrit remplacée par `pass` → fixture T9 `../x` : rc_original=1, rc_mutant=0
  </behavior>
  <action>
1. Tests d'abord (T6 à T13, MUT-S1 à MUT-S3), joués ROUGES sur le gate de la Tâche 1, puis implémentation. Toutes les fixtures portent name et description valides pour que leur rc ne dépende que de la règle visée.
2. Gate (règles V5 : énumération stricte, refus explicite, jamais un défaut silencieux sur une entrée malformée — 43-RESEARCH.md § Security Domain) :
   - `valider_nature` : valeur présente hors de l'ensemble exact {referentiel, outil, procedure} (sensible à la casse, sans accent) → « {rel} : vf-nature invalide <valeur échappée> — attendu referentiel|outil|procedure (B-03 : jamais un defaut silencieux) » ; une liste ou une valeur vide est invalide.
   - `valider_ecrit(rel, valeur)` appelée sur sa propre ligne `errors.extend(valider_ecrit(` (cible MUT-S3) quand la clé est présente, quelle que soit la nature : chaque entrée (scalaire, liste flow ou puces) est non vide après retrait des guillemets, ne commence ni par `/` ni par `~`, ne contient aucun segment `..`, et correspond à `^[\w./<>{}*-]+$` (le `\w` Unicode de Python admet les lettres accentuées ; espace, `;`, `$`, `(`, `)`, `|`, `&`, backtick et tout caractère de contrôle refusés) → sinon « {rel} : ecrit: entree malformee <valeur échappée> — chemin relatif attendu, sans '..', sans '/' ni '~' initial (FABR-06) ».
   - `vf-rubrique-juge` : scalaire seulement (une liste est refusée), même règle de forme qu'une entrée de ecrit.
   - Marqueurs `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` : présents → valeur exactement `true` ou `false` (minuscules), sinon « {rel} : <cle> invalide <valeur échappée> — attendu true|false (D-Q1) ».
   - `invariant_procedure` : une valeur vide, une liste vide ou une chaîne vide vaut absence.
   - « champ inconnu » : toute clé hors de `champs_frontmatter_skills` ∪ `VIBEFLOW_SKILL_FIELDS` → avertissement « {rel} : champ inconnu — <cle> (typo ? verifier la doc) », jamais une erreur.
   - Frontmatter absent ou jamais refermé → erreur (aucun verdict possible sur ce fichier).
   - Toute valeur imprimée passe par une fonction unique d'échappement (représentation échappée de Python, tronquée à 80 caractères) : aucune valeur de frontmatter n'est jamais réinjectée telle quelle dans la sortie, ni passée à un shell.
3. Mutants avec `make_gate_mutant` appelé directement, jamais en substitution de commande (motif fixe unique vérifié par le helper, cmp, `bash -n`, compilation du Python muté ; un refus est compté KO par le helper dans le shell de la suite), okmut/komut.
4. Commit : `feat(conductor): valeurs du gate des skills validées strictement (FABR-06)` avec
   `Gate-Touche: plugin/conductor/scripts/check-skills.sh — validation stricte des valeurs (V5, FABR-06)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-skills.sh — T6 à T13, mutants MUT-S1 à MUT-S3`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-skills.sh 2>&1 | grep -E '✓ T([6-9]|1[0-3]) |MUT-S[123] |✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T6 à T13 n'a aucune ligne « ✓ », ou l'une des lignes « ✓ MUT-S1 TUE », « ✓ MUT-S2 TUE », « ✓ MUT-S3 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'errors.extend(valider_ecrit(' plugin/conductor/scripts/check-skills.sh` rend 1
    - la sortie de test-check-skills.sh contient `✓ MUT-S1 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1`, `✓ MUT-S2 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1`, `✓ MUT-S3 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1`, des lignes `✓ T6` à `✓ T13`, et finit sur `0 KO ==`
    - T13 prouve qu'aucun octet ESC ne sort (le cas compte les octets ESC de la sortie capturée et attend 0)
  </acceptance_criteria>
  <done>Aucune valeur malformée ne passe ni n'est ramenée à un défaut : nature, ecrit:, vf-rubrique-juge et marqueurs sont refusés explicitement, trois mutants tués.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : parité de contrat avec check-agents.sh — découverte, exclusions, liens symboliques, tiers, F13, --hook, --file, fraîcheur (T14 à T21, MUT-SD1, MUT-SD2)</name>
  <files>plugin/conductor/scripts/check-skills.sh, plugin/conductor/scripts/tests/test-check-skills.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-skills.sh (fichier modifié : decouvrir_skills, boucle principale, rapport --hook, fraîcheur)
    - plugin/conductor/scripts/tests/test-check-skills.sh (fichier modifié)
    - plugin/conductor/scripts/tests/test-check-agents.sh (cas T97, T98 : récursion et exclusions ; T104 : lien symbolique refusé sans lecture ; T103 : cible absente ; T21 à T23 : F13 ; T9 et T56 : --hook ; MUT-D1, MUT-D2 : forme exacte des mutants d'élagage)
    - docs/HOOKS-CONTRAT-SORTIE.md (§2 et §3 : traduction du code sous --hook, silence de flux)
  </read_first>
  <behavior>
    - T14 : `$SK/a/SKILL.md`, `$SK/mod/skills/b/SKILL.md`, `$SK/c/sub/deep/SKILL.md` tous jugés ; le plus profond viole FABR-06 → rc 1 citant `c/sub/deep/SKILL.md` ; `README.md` et `notes.md` voisins ignorés
    - T15 : `$SK/lab-references/x/SKILL.md` et `$SK/.cache/y/SKILL.md` (tous deux en violation) + un skill conforme → rc 0, la sortie ne cite ni l'un ni l'autre
    - T16 : `$SK/lien/SKILL.md` lien symbolique vers un fichier hors arbre portant un jeton unique → rc 1 « lien symbolique refuse », jeton absent de stdout et de stderr ; un lien de dossier en boucle ne bloque pas le parcours
    - T17 : skill `name: gsd-outil` avec `vf-nature: bidon` → rc 0 et ligne de compte « 1 skill(s) tiers non linte(s) » ; même fixture avec `--no-third-party-prefix` → rc 1
    - T18 : cible absente → rc 3 et CIBLE-ABSENTE ; cible absente + `--hook` → rc 0 et stdout vide ; cible vide → rc 0 ; cible vide + `--strict` → rc 3 ; + `--strict --allow-empty` → rc 0 ; `--skills-dir=` vide → rc 1
    - T19 : `--hook` sur corpus conforme → rc 0 et stdout vide ; sur violation FABR-06 → rc 0 et une ligne compacte d'erreur ; sur champ inconnu seul → rc 0 et une ligne compacte « ⚠ »
    - T20 : `--file` sur un SKILL.md conforme → rc 0 ; sur un SKILL.md en violation → rc 1 ; sur un chemin inexistant → rc 1 « fichier introuvable »
    - T21 : manifeste dont la septième liste est datée de 40 jours (calculé au run) → rc 0 et ligne « ⚠ MANIFESTE-PERIME » ; datée du jour → aucune ligne MANIFESTE-PERIME ; manifeste absent → rc 1 MANIFESTE-ILLISIBLE, et rc 0 sous `--hook`
    - MUT-SD1 : l'élagage des dossiers cachés remplacé par une affectation qui vide la liste des sous-dossiers (plus aucune descente) → fixture T14 : rc_original=1, rc_mutant=0
    - MUT-SD2 : l'élagage des dossiers -references remplacé par `pass` → mutant accepté par le helper seulement si son Python compile ; fixture T15 : rc_original=0, rc_mutant=1 ET trace du verdict visé : la sortie du mutant porte une ligne `  ✗ ` qui cite `lab-references/x/SKILL.md` et « FABR-06 », celle de l'original n'en porte aucune — la mort n'est jamais attestée par les seuls rc (un plantage rend aussi rc 1) ; okmut reçoit en sixième argument une trace EN TOUTES LETTRES, sans le glyphe de la croix (ex. « lab-references/x/SKILL.md en erreur FABR-06 sur le mutant, absent de l'original ») : la ligne de succès ne doit jamais porter ✗, sinon le `fails_when` « une ligne contient ✗ » la rattrape et le vert devient impossible
    - MUT-SYNTAXE (garde du helper) : `make_gate_mutant` appelé avec la ligne d'appel de invariant_procedure remplacée par son seul début d'appel, parenthèses non refermées (Python tronqué, que `bash -n` laisse passer) → le helper refuse le mutant (rc 1) et sa sortie capturée nomme l'échec de compilation ; ligne `✓ MUT-SYNTAXE refuse : mutant Python invalide rejete par le helper` (la sortie capturée n'est jamais réimprimée)
    - MUT-REFUS-COMPTE (garde du harnais, I1) : dans un sous-shell explicite dont la sortie va dans un fichier du dossier de travail, le helper est appelé DIRECTEMENT avec un motif absent de check-skills.sh, puis le sous-shell imprime `DELTA-FAIL=<fail après l'appel − fail avant>` et `RC-HELPER=<rc>` → le cas exige `DELTA-FAIL=1`, `RC-HELPER=1` et la ligne de refus « MUT-REFUS NON TUE » dans le fichier : un refus du helper est compté KO dans le shell qui l'appelle ; ligne `✓ MUT-REFUS-COMPTE : refus du helper compte KO dans le shell appelant` (fichier jamais réimprimé ; l'incrément du sous-shell, volontairement perdu, n'entre pas dans le décompte de la suite)
  </behavior>
  <action>
1. Tests d'abord (T14 à T21, MUT-SD1, MUT-SD2, MUT-SYNTAXE, MUT-REFUS-COMPTE), joués sur le gate des Tâches 1 et 2 ; corriger le gate là où un cas rougit. MUT-SYNTAXE et MUT-REFUS-COMPTE sont les deux seuls refus VOULUS : chacun appelle le helper dans un sous-shell explicite dont la sortie va dans un fichier du dossier de travail (jamais en substitution de commande), le sous-shell imprime le rc du helper, et le cas juge ce rc et le contenu du fichier — le refus attendu n'y compte pas comme KO parce que l'incrément du sous-shell est perdu à dessein. Tout autre appel du helper est direct : un refus y est un KO visible. Aucune fixture ne dépend de la date réelle : l'âge de la liste de T21 est un décalage calculé au run par `mk_manifest`, jamais une date écrite.
2. Gate : ajuster seulement ce que les cas révèlent, en gardant chaque ligne d'élagage unique (cibles de MUT-SD1 et MUT-SD2), le refus d'un SKILL.md en lien symbolique sans ouverture, le compteur des skills tiers publié à part, et la traduction 3 → 0 limitée à `hook_exit` côté shell (le bloc Python rend toujours 3).
3. Compléter l'en-tête de check-skills.sh (découverte, exclusions et pourquoi : les `<mod>-references/` posés par l'installeur sont de la doc ; liens symboliques ; skills tiers ; contrat --hook) et le catalogue de test-check-skills.sh.
4. Commit : `test(conductor): parité de contrat du gate des skills avec check-agents (FABR-06)` avec
   `Gate-Touche: plugin/conductor/scripts/check-skills.sh — découverte, F13, --hook, fraîcheur alignés sur check-agents` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-skills.sh — T14 à T21, mutants MUT-SD1, MUT-SD2`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-skills.sh 2>&1 | grep -E '✓ T(1[4-9]|2[01]) |MUT-SD[12] |MUT-SYNTAXE |MUT-REFUS-COMPTE |✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T14 à T21 n'a aucune ligne « ✓ », ou « ✓ MUT-SD1 TUE » ou « ✓ MUT-SD2 TUE » manque, ou la ligne « ✓ MUT-SD2 TUE » ne porte pas « · trace : », ou « ✓ MUT-SYNTAXE » ou « ✓ MUT-REFUS-COMPTE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == ». Aucune ligne de succès prescrite par ce plan ne porte la croix (traces en toutes lettres, fichiers de refus voulus jamais réimprimés) : une croix sur n'importe quelle ligne est un échec réel.</fails_when>
    <automated>grep -v '^[[:space:]]*#' plugin/conductor/scripts/tests/test-check-skills.sh | grep -cE '\$\([[:space:]]*make_gate_mutant'</automated>
    <fails_when>la valeur imprimée n'est pas 0 : un appel du helper en substitution de commande perdrait le KO d'un refus dans un sous-shell (I1) ; les deux refus voulus (MUT-SYNTAXE, MUT-REFUS-COMPTE) passent par un sous-shell explicite redirigé vers un fichier, pas par une substitution de commande</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | tail -1</automated>
    <fails_when>la dernière ligne ne finit pas par « 0 KO == » (régression du gate des agents)</fails_when>
  </verify>
  <acceptance_criteria>
    - la sortie de test-check-skills.sh contient `✓ MUT-SD1 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` et une ligne qui commence par `✓ MUT-SD2 TUE : rc_mutant=1 attendu 1, rc_original=0 attendu 0 · trace :` (trace en toutes lettres, sans glyphe de croix : `lab-references/x/SKILL.md` en erreur FABR-06 sur le mutant, absent de l'original), une ligne `✓ MUT-SYNTAXE`, une ligne `✓ MUT-REFUS-COMPTE`, des lignes `✓ T14` à `✓ T21`, et finit sur `0 KO ==` (T1 à T13 toujours verts)
    - aucun appel du helper en substitution de commande hors commentaires : la seconde commande du verify rend 0 (un mutant refusé est toujours un KO visible du décompte final)
    - `grep -c "startswith('.')" plugin/conductor/scripts/check-skills.sh` rend 1 et `grep -c "endswith('-references')" plugin/conductor/scripts/check-skills.sh` rend 1
    - test-check-agents.sh finit sur `0 KO ==`
  </acceptance_criteria>
  <done>check-skills.sh tient le même contrat que check-agents.sh sur tout ce qui n'est pas propre aux agents : découverte et exclusions prouvées par mutant, liens symboliques refusés sans lecture, tiers comptés, F13 et silence sous --hook, fraîcheur en avertissement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'un SKILL.md (auteur du skill, lab tiers) → gate | valeurs non fiables : nature, ecrit:, vf-rubrique-juge, marqueurs |
| arborescence de --skills-dir → découverte | tout fichier nommé SKILL.md devient une cible ; liens symboliques possibles |
| manifeste daté partagé → deux gates | un schéma mal étendu désarme check-agents.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Elevation of Privilege | valider_nature | high | mitigate | énumération stricte sensible à la casse ; valeur présente invalide → rc 1, jamais ramenée à « outil » (une procédure mal typée échapperait à tous les gates du moteur, B-03) ; T6, MUT-S2 |
| T-43-02 | Tampering | valider_ecrit, vf-rubrique-juge | medium | mitigate | refus des chemins absolus, de `~`, des segments `..` et de tout caractère hors `[\w./<>{}*-]` (le périmètre ecrit: sera lu par le hook d'écriture de la Phase 45) ; T9, T10, MUT-S3 |
| T-43-03 | Tampering (injection) | exécution du gate | high | mitigate | le code Python passe par un here-document à délimiteur quoté (aucune interpolation), les valeurs ne sont jamais passées à un shell ni évaluées — injection : catégorie canonique, relève aussi de /gsd-secure-phase |
| T-43-04 | Tampering (injection de journal) | rapport | low | mitigate | valeurs imprimées échappées et tronquées à 80 caractères ; T13 prouve l'absence d'octet ESC brut |
| T-43-05 | Information Disclosure | SKILL.md en lien symbolique | medium | mitigate | refus sans ouverture (patron A1 de check-agents.sh) ; T16 prouve que le jeton du fichier visé ne sort jamais |
| T-43-06 | Denial of Service | manifeste partagé | high | mitigate | élargissement de cles_listes dans le même commit que la liste ; suite complète de check-agents.sh rejouée ; T5 et T107 |
| T-43-07 | Spoofing (faux vert) | cible absente ou vide, corpus partiellement vu | medium | mitigate | CIBLE-ABSENTE et F13 (T18) ; T4 compare le compte jugé au compte find ; exclusions doc-only comptées et imprimées |
| T-43-08 | Denial of Service | parcours récursif | low | mitigate | followlinks=False, élagage des dossiers cachés et *-references (T15, MUT-SD1, MUT-SD2) |
| T-43-SC | Tampering | installations de paquets | low | accept | aucune dépendance installée (bash + python3 stdlib, 43-RESEARCH.md § Package Legitimacy Audit : sans objet) |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-skills.sh` : 0 KO ; T1 à T21 ; MUT-S1, MUT-S2, MUT-S3, MUT-SD1, MUT-SD2 tués (MUT-SD2 par sa trace) ; MUT-SYNTAXE prouve que le helper refuse un mutant Python invalide ; MUT-REFUS-COMPTE prouve qu'un refus du helper est un KO visible du décompte (aucun appel en substitution de commande).
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` : 0 KO, T107 compris.
- Témoin lab frais (HOME temporaire) : `TRACER-SKILLS-OK`.
</verification>

<success_criteria>
- FABR-06 : procédure sans ecrit:/vf-rubrique-juge refusée, avec les deux acceptée, défaut « outil » sur clé absente seulement, valeurs malformées refusées.
- FABR-09 (clause manifeste) : un seul manifeste daté, septième liste native, validé par les deux gates.
- Contrat de clés posé pour 43-02 et 43-03.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| `plugin/conductor/scripts/check-skills.sh` | nouveau gate (options `--skills-dir=`, `--file`, `--strict`, `--allow-empty`, `--hook`, `--third-party-prefix=`, `--no-third-party-prefix`) |
| `plugin/conductor/scripts/tests/test-check-skills.sh` | nouvelle suite (T1 à T21, MUT-S1 à MUT-S3, MUT-SD1, MUT-SD2 ; gardes MUT-SYNTAXE, MUT-REFUS-COMPTE ; helper `make_gate_mutant` à retour par `MUT_DIR`) |
| clés de frontmatter `vf-nature`, `ecrit`, `vf-rubrique-juge`, `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` ; ensemble `VIBEFLOW_SKILL_FIELDS` | contrat de clés (costly) |
| liste de manifeste `champs_frontmatter_skills` | septième liste du manifeste daté |
| `decouvrir_skills()`, `valider_nature()`, `valider_ecrit()`, `invariant_procedure()` | fonctions du gate |
| jetons `CIBLE-ABSENTE`, `MANIFESTE-ILLISIBLE`, `MANIFESTE-PERIME`, `FABR-06` | diagnostics |
| `T107` | cas de test-check-agents.sh |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-06 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici en vérités du plan : clé absente vs valeur présente invalide (T1, T6), « ni … ni » lu comme « les deux sont exigés » (T3, T7 — lecture de la spec §6 « Sans les deux, refus » et de FABR-06 « avec les deux passe »), valeurs vides (T8), formes de chemin (T9, T10), casse et accents (T6, T9), frontmatter adverse (T13).
- **Sonde de bords spec-less — FABR-09 (clause manifeste) : `unclassified` / `unresolved`.** Les listes VibeFlow restent hors manifeste (frontière D-01 de la Phase 42) ; seule la liste native entre au manifeste. Si Willy voulait les clés vf-* dans le manifeste (lecture littérale de la Claude's Discretion de 43-CONTEXT.md), il faudrait les soustraire à la péremption — à trancher à la relecture.
- **Corpus gaté** : `plugin/reference` (doc-only, 4 gabarits) est exclu par lecture du type de module, pas par nom ; `plugin/installer/SKILL.md` (sans module.json) est inclus. Décision de plan (43-RESEARCH.md Open Question 2, recommandation « exclure par défaut »).
- **Liste native relevée le 2026-09-25** : la page officielle fait foi à l'exécution ; `when_to_use` et `metadata` y figurent (la doc indique aussi que description + when_to_use sont tronqués à 1 536 caractères dans la liste des skills — non gaté ici, hors FABR-06/09).
- **Pas de `--manifest-freshness` dans check-skills.sh** : la péremption de la septième liste rend déjà INDÉTERMINÉ en CI via check-agents.sh (même fichier) ; check-skills.sh n'en fait qu'un avertissement.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-01-SUMMARY.md` when done — commit `.planning/` séparé ; y consigner la liste native réellement relue et sa date, et le compte réel du corpus jugé/exclu.
</output>

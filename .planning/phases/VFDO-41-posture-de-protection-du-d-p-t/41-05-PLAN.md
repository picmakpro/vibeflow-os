---
phase: 41-posture-de-protection-du-d-p-t
plan: 05
lot: L5 — pose des deux rulesets par `picmakpro`, vérification de pose (critère 1), mesure M-2
type: execute
wave: 3
depends_on: ["41-04"]
autonomous: false
requirements: [PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le retour arrière est écrit dans `41-PREUVES.md` AVANT la pose : `picmakpro` repasse chaque ruleset en `disabled` par `gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled` (doc REST : mise à jour partielle) ; il n'est exercé que sur décision humaine (D-M6)"
    - "La pose n'a lieu que si AUCUNE PR n'est ouverte (principe de D-08 : aucune PR ne change de règles en vol) ; une PR ouverte (hotfix v2.63.2 compris) arrête le plan et remonte au manager"
    - "La pose est faite par `picmakpro` depuis les fichiers d'`origin/main` (`git show origin/main:.github/rulesets/<f> | gh api -X POST … --input -`) : ce qui est posé est la source mergée, octet pour octet (D-M6)"
    - "Critère 1 : `gh api repos/picmakpro/vibeflow-os/rulesets` ne rend plus `[]` — deux rulesets actifs, dont celui de `main` exigeant les 4 checks CI verts (PROT-01)"
    - "Chaque champ arbitré relu côté serveur par `samuel-neveugall` (compte write) est conforme ; `bypass_actors`, invisible pour un compte write (doc REST), est relu par `picmakpro` et comparé à la source"
    - "M-2 mesuré, jamais supposé : `current_user_can_bypass` relevé depuis `samuel-neveugall` (attendu `pull_requests_only` branche, `always` tags — sinon D-02 n'est pas posé : arrêt) ET depuis `picmakpro` (admin : valeur consignée telle quelle, `admin_couvert` en découle)"
    - "L'accès en lecture aux rule suites est mesuré pour les deux comptes (constat du 2026-09-17 : 404 depuis `samuel-neveugall` avant toute pose, cause non établie) : les plans de preuve savent quel compte lit la trace"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "lignes `RETOUR-ARRIERE-ECRIT:`, `POSE-BRANCHE:`, `POSE-TAGS:`, `POSE-BYPASS-VU-PAR-PICMAKPRO:`, `POSE-CONFORME:`, `M2:`, `ACCES-RULE-SUITES:`"
  key_links:
    - "`POSE-BRANCHE: id=` et `POSE-TAGS: id=` sont lus par tous les plans suivants (retour arrière, rule suites)"
---

<objective>
Poser les deux rulesets depuis la source mergée, par le seul compte qui en a le droit, puis prouver
la pose par relecture serveur et mesurer M-2.

Purpose: critère de succès 1 (PROT-01) ; pose humaine exclusive (D-M6) ; inconnu M-2.
Output: deux rulesets actifs ; lignes de pose, de conformité, M-2 et d'accès aux rule suites.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-04-SUMMARY.md

Sources lues le 2026-09-17 : `docs.github.com/en/rest/repos/rules` — `POST /repos/{owner}/{repo}/rulesets`
(admin), `GET …/rulesets/{id}` (`bypass_actors` rendu seulement à qui a l'écriture sur le ruleset ;
`current_user_can_bypass` ∈ always / pull_requests_only / never / exempt), `PUT …/rulesets/{id}`
(corps partiel), `GET …/rules/branches/{branch}` ; `docs.github.com/en/rest/repos/rule-suites`
(permissions non précisées par la doc).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : préalables de pose relus, retour arrière écrit, commandes de pose imprimées</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`SONDE-ACTOR-ID-WRITE:`, `PR-PHASE:`)
  </read_first>
  <action>
Identité `samuel-neveugall`, branche courante B. `git fetch origin`. Relire : `PR-PHASE:` présente et
son merge dans `origin/main` ; aucune PR ouverte (`gh pr list --state open`) ; `rulesets` = `[]` ;
`codeowners/errors` = 0 ; les deux JSON d'`origin/main` repassent les vérifications de conformité de
41-01 (lues depuis `git show origin/main:…`, pas depuis l'arbre de travail).

Écrire dans `41-PREUVES.md` une section `## 41-05 — pose` puis, AVANT la tâche humaine, la ligne
`RETOUR-ARRIERE-ECRIT: picmakpro gh_api_-X_PUT_repos/picmakpro/vibeflow-os/rulesets/<id>_-f_enforcement=disabled temoin=rules/branches/main_vide`
et un paragraphe en clair reprenant la commande. Commit local sur B.

Imprimer pour Samuel les commandes exactes de la Task 2. Si une PR est ouverte : ne rien imprimer,
arrêt, remontée au manager avec son numéro et sa branche.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; u=$(gh api user --jq .login); br=$(git branch --show-current); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); rs=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); ce=$(gh api repos/picmakpro/vibeflow-os/codeowners/errors --jq ".errors | length"); M=$(awk "\$1==\"PR-PHASE:\" { for (i=2;i<=NF;i++) if (index(\$i, \"merge=\")==1) { print substr(\$i, 7); exit } }" "$PH/41-PREUVES.md"); anc=$(git merge-base --is-ancestor "$M" origin/main && echo oui || echo non); ra=$(awk "\$1==\"RETOUR-ARRIERE-ECRIT:\" && /enforcement=disabled/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); T=$(mktemp -d); git show HEAD:"$PH/41-PREUVES.md" > "$T/h"; ras=$(awk "\$1==\"RETOUR-ARRIERE-ECRIT:\" {c++} END{print c+0}" "$T/h"); echo "prealables pose : identite=$u branche=$br prs_ouvertes=$op rulesets=$rs codeowners_errors=$ce merge_phase_dans_main=$anc retour_arriere_ecrit=$ra retour_arriere_committe=$ras"; [ "$u" = samuel-neveugall ] && [ "$br" = feat/phase-41-preuves-release ] && [ "$op" -eq 0 ] && [ "$rs" -eq 0 ] && [ "$ce" -eq 0 ] && [ "$anc" = oui ] && [ "$ra" -eq 1 ] && [ "$ras" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `prs_ouvertes=` différent de 0 (arrêt, remontée : une pose changerait les règles d'une PR en vol) ; ou `rulesets=` différent de 0 ; ou `codeowners_errors=` différent de 0 ; ou merge de la PR de la phase absent de `main` ; ou `retour_arriere_ecrit=` / `retour_arriere_committe=` différent de 1 (retour arrière non écrit ou non committé avant la pose)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); git show origin/main:.github/rulesets/main.json > "$T/b"; git show origin/main:.github/rulesets/tags-v.json > "$T/g"; A=$(awk "/^SONDE-ACTOR-ID-WRITE: /{print \$2; exit}" "$PH/41-PREUVES.md"); case "$A" in ""|*[!0-9]*) echo "actor_id absent"; exit 1;; esac; ob=$(jq -r --argjson aid "$A" "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | [(.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"]), ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]), (\$r.pull_request.required_approving_review_count==0), (\$r.pull_request.require_code_owner_review==true), ((\$r.required_status_checks.required_status_checks | length)==4), ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), (\$r.required_status_checks.strict_required_status_checks_policy==true), (.bypass_actors == [{actor_id: \$aid, actor_type: \"RepositoryRole\", bypass_mode: \"pull_request\"}])] | all" "$T/b"); og=$(jq -r --argjson aid "$A" "[(.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"]), ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), (.bypass_actors == [{actor_id: \$aid, actor_type: \"RepositoryRole\", bypass_mode: \"always\"}])] | all" "$T/g"); echo "sources origin/main : branche_conforme=$ob tags_conforme=$og"; [ "$ob" = true ] && [ "$og" = true ]'</automated>
    <fails_when>exit non nul ; ou `branche_conforme=false` / `tags_conforme=false` : la source sur `main` n'est pas celle qui a été relue (arrêt avant pose)</fails_when>
  </verify>
  <acceptance_criteria>
    - aucune PR ouverte, aucun ruleset, 0 erreur CODEOWNERS, merge de la phase dans `main`
    - sources d'`origin/main` conformes
    - retour arrière écrit ET committé sur B avant la tâche humaine
  </acceptance_criteria>
  <done>Tout est prêt pour une pose qui ne change les conditions d'aucune PR ouverte, avec son retour arrière déjà écrit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `picmakpro` — poser les deux rulesets depuis `origin/main`, relire ce qu'un compte write ne voit pas, mesurer M-2 côté admin</name>
  <action>Samuel, compte `picmakpro` (D-01), pose les deux rulesets depuis les fichiers d'`origin/main`, relit `bypass_actors`, `current_user_can_bypass` et l'accès aux rule suites, vérifie le libellé du rôle dans l'interface, puis revient au compte `samuel-neveugall`.</action>
  <instructions>
Compte qui agit : `picmakpro` pour 0-5 ; `samuel-neveugall` en 6.

0. `gh auth switch --user picmakpro` ; témoin `gh api user --jq .login` → `picmakpro`.
   Depuis un clone de ce dépôt : `git fetch origin`.
1. Pose du ruleset de branche :
   `git show origin/main:.github/rulesets/main.json | gh api -X POST repos/picmakpro/vibeflow-os/rulesets --input - --jq '{id, enforcement}'`
   → noter `<id_branche>`.
2. Pose du ruleset de tags :
   `git show origin/main:.github/rulesets/tags-v.json | gh api -X POST repos/picmakpro/vibeflow-os/rulesets --input - --jq '{id, enforcement}'`
   → noter `<id_tags>`.
   Repli si `gh` refuse le corps (422) : NE PAS retoucher le JSON ; Settings → Rules → Rulesets →
   New ruleset → Import a ruleset avec le fichier extrait d'`origin/main` ; si l'import refuse aussi,
   arrêt et collage du message (la source devra être corrigée par PR, jamais à la main chez GitHub).
3. Relecture admin, pour chacun des deux ids :
   `gh api repos/picmakpro/vibeflow-os/rulesets/<id> --jq '{id, enforcement, current_user_can_bypass, bypass_actors}'`
4. Accès admin aux rule suites :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour" --jq length` → coller
   la sortie ou le message d'erreur.
5. Interface : Settings → Rules → Rulesets → ouvrir chacun des deux → la liste de contournement
   affiche le rôle **Write** (et lui seul), mode « For pull requests only » pour la branche.
6. `gh auth switch --user samuel-neveugall` ; témoin `gh api user --jq .login` → `samuel-neveugall`.

Si une étape échoue ou si 3 ou 5 montre autre chose que l'attendu : NE PAS poursuivre les plans de
preuve. Le retour arrière (`gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled`)
n'est joué que sur décision de Samuel.
  </instructions>
  <verification>La Task 3 relit, en `samuel-neveugall`, la liste des rulesets, chaque champ arbitré, `rules/branches/main`, et compare les `bypass_actors` collés à la source.</verification>
  <resume-signal>Coller les sorties des témoins 0 à 6 (dont `<id_branche>`, `<id_tags>`, et le constat de l'étape 5 en une phrase), puis écrire « rulesets posés ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : vérification de pose en compte write (critère 1), M-2, accès aux rule suites, consignation</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall` (si elle est encore `picmakpro` : arrêt immédiat, aucun appel). Consigner :
- `POSE-BRANCHE: id=<id_branche> enforcement=active pose_par=picmakpro` ;
- `POSE-TAGS: id=<id_tags> enforcement=active pose_par=picmakpro` ;
- `POSE-BYPASS-VU-PAR-PICMAKPRO: branche=RepositoryRole:<n>:pull_request tags=RepositoryRole:<n>:always libelle_ui=Write`, recopiés du collage ;
- `POSE-MSG:` suivi des JSON collés, sur une ligne.

Relire en compte write : liste des rulesets (2) ; pour chaque id, les champs arbitrés côté serveur ;
contextes du ruleset de branche égaux à la source ; `rules/branches/main` portant les quatre types
de règle du ruleset de branche. Consigner `POSE-CONFORME: branche=oui tags=oui contextes_egaux_source=oui`.

M-2 : relever `current_user_can_bypass` en compte write sur les deux ids ; consigner
`M2: samuel_branche=<v> samuel_tags=<v> picmakpro_branche=<v> picmakpro_tags=<v> admin_couvert=<oui|non|indetermine>`,
où `admin_couvert` vaut `oui` si `picmakpro_branche` ∈ {always, pull_requests_only, exempt}, `non`
si `never`, `indetermine` si le champ est absent du collage. Aucune interprétation au-delà.

Accès aux rule suites : essayer en compte write
`gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour"` ; consigner
`ACCES-RULE-SUITES: samuel-neveugall=<ok|refuse> picmakpro=<ok|refuse>` (valeur `picmakpro` issue du
collage). Commit local sur B. Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); [ "$u" = samuel-neveugall ] || { echo "identite=$u"; exit 1; }; IB=$(val POSE-BRANCHE: id); IT=$(val POSE-TAGS: id); n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); ids=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq "[.[].id] | sort | map(tostring) | join(\",\")"); att=$(printf "%s\n%s\n" "$IB" "$IT" | LC_ALL=C sort -n | awk "BEGIN{ORS=\"\"} NR>1{print \",\"} {print}"); echo "critere 1 : rulesets=$n ids_serveur=$ids ids_consignes=$att"; [ "$n" -eq 2 ] && [ "$ids" = "$att" ]'</automated>
    <fails_when>exit non nul ; ou identité autre que `samuel-neveugall` ; ou `rulesets=` différent de 2 (critère 1 non atteint, ou ruleset parasite) ; ou ids serveur différents des ids consignés</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; IB=$(val POSE-BRANCHE: id); IT=$(val POSE-TAGS: id); T=$(mktemp -d); gh api "repos/picmakpro/vibeflow-os/rulesets/$IB" > "$T/b"; gh api "repos/picmakpro/vibeflow-os/rulesets/$IT" > "$T/g"; ob=$(jq -r "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | {cible: (.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]), approbations0: (\$r.pull_request.required_approving_review_count==0), code_owner: (\$r.pull_request.require_code_owner_review==true), stale: (\$r.pull_request.dismiss_stale_reviews_on_push==true), methodes: ((\$r.pull_request.allowed_merge_methods | sort)==[\"merge\",\"rebase\",\"squash\"]), strict: (\$r.required_status_checks.strict_required_status_checks_policy==true), integration: ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), samuel_peut_contourner_par_pr: (.current_user_can_bypass==\"pull_requests_only\")} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$T/b"); og=$(jq -r "{cible: (.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), samuel_peut_contourner_toujours: (.current_user_can_bypass==\"always\")} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$T/g"); jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$T/b" | LC_ALL=C sort > "$T/cs"; git show origin/main:.github/rulesets/main.json > "$T/src"; jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$T/src" > "$T/cj0"; LC_ALL=C sort "$T/cj0" > "$T/cj"; [ "$(awk "END{print NR}" "$T/cj")" -eq 4 ] || { echo "contextes source illisibles"; exit 1; }; ce=$(cmp -s "$T/cs" "$T/cj" && echo true || echo false); rb=$(gh api repos/picmakpro/vibeflow-os/rules/branches/main --jq "[.[] | select(.ruleset_id == $IB) | .type] | sort | unique == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]"); echo "serveur branche : $ob contextes_egaux_source=$ce rules_branches_main=$rb"; echo "serveur tags : $og"; case "$ob $og" in *=false*) exit 1;; esac; [ "$ce" = true ] && [ "$rb" = true ]'</automated>
    <fails_when>exit non nul ; ou un `=false` côté serveur — `samuel_peut_contourner_par_pr=false` / `samuel_peut_contourner_toujours=false` signifient que D-02 n'est pas posé tel qu'arbitré (arrêt, remontée ; retour arrière seulement sur décision de Samuel) ; ou `contextes_egaux_source=false` ; ou `rules_branches_main=false` (la règle n'est pas effective sur `main`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; A=$(awk "/^SONDE-ACTOR-ID-WRITE: /{print \$2; exit}" "$PH/41-PREUVES.md"); vb=$(val POSE-BYPASS-VU-PAR-PICMAKPRO: branche); vt=$(val POSE-BYPASS-VU-PAR-PICMAKPRO: tags); pb=$(val M2: picmakpro_branche); ac=$(val M2: admin_couvert); sb=$(val M2: samuel_branche); st=$(val M2: samuel_tags); case "$pb" in always|pull_requests_only|exempt) att=oui;; never) att=non;; "") att=indetermine;; *) att=invalide;; esac; ss=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour" > /dev/null 2>&1 && echo ok || echo refuse); sr=$(val ACCES-RULE-SUITES: samuel-neveugall); pr=$(val ACCES-RULE-SUITES: picmakpro); cf=$(awk "\$1==\"POSE-CONFORME:\" && /branche=oui/ && /tags=oui/ && /contextes_egaux_source=oui/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "M-2 et acces : bypass_vu_branche=$vb bypass_vu_tags=$vt samuel_branche=$sb samuel_tags=$st picmakpro_branche=$pb admin_couvert=$ac attendu=$att acces_samuel_mesure=$ss acces_samuel_consigne=$sr acces_picmakpro=$pr ligne_conforme=$cf"; [ "$vb" = "RepositoryRole:$A:pull_request" ] && [ "$vt" = "RepositoryRole:$A:always" ] && [ "$sb" = pull_requests_only ] && [ "$st" = always ] && [ "$ac" = "$att" ] && [ "$att" != invalide ] && [ "$ss" = "$sr" ] && { [ "$pr" = ok ] || [ "$pr" = refuse ]; } && [ "$cf" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `bypass_vu_*` différent de l'acteur mesuré par la sonde et du mode arbitré ; ou `samuel_*` différent de `pull_requests_only`/`always` ; ou `admin_couvert` incohérent avec la valeur `picmakpro` consignée ; ou accès write consigné différent de l'accès re-mesuré ; ou accès `picmakpro` non consigné ; ou `POSE-CONFORME:` absente</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
  </verify>
  <acceptance_criteria>
    - critère 1 : 2 rulesets actifs, ids consignés = ids serveur
    - champs arbitrés conformes côté serveur, contextes = source, règles effectives sur `main`
    - `bypass_actors` vus par `picmakpro` = acteur de la sonde, modes arbitrés ; libellé UI « Write »
    - M-2 consigné (`samuel` : `pull_requests_only`/`always` ; `picmakpro` : valeur mesurée, `admin_couvert` cohérent) ; accès aux rule suites mesuré pour les deux comptes
  </acceptance_criteria>
  <done>Les règles sont posées, relues champ par champ, et les deux inconnus de lecture (M-2, accès à la trace) sont mesurés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `picmakpro` → configuration GitHub | seul geste d'écriture de règles de la phase |
| règle posée → toutes les PR | une règle posée change les conditions de toute PR ouverte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-18 | Tampering | JSON modifié entre merge et pose | high | mitigate | pose par `git show origin/main:… \| gh api --input -` ; conformité relue côté serveur |
| T-41-19 | Denial of service | règle posée sur une PR en vol | high | mitigate | `prs_ouvertes=0` exigé avant la pose, sinon arrêt et remontée |
| T-41-20 | Elevation of privilege | bypass élargi à la pose (UI) | high | mitigate | `bypass_actors` relus par `picmakpro` et comparés à la sonde ; libellé UI constaté |
| T-41-21 | Denial of service | règle fautive bloquant tout merge | medium | mitigate | retour arrière écrit et committé avant la pose, joué sur décision humaine |
| T-41-22 | Elevation of privilege | agent sous jeton admin | high | mitigate | identité `samuel-neveugall` exigée avant toute lecture ; retour de compte témoigné |
</threat_model>

<verification>
Deux rulesets actifs, conformes côté serveur, `bypass_actors` conformes vus par l'admin, M-2 et accès
aux rule suites consignés, rejeux verts.
</verification>

<success_criteria>
Critère de succès 1 atteint et prouvé par relecture serveur.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-05-SUMMARY.md` when done — committé sur la branche B.
</output>

---
phase: 25-budget-d-instructions-et-tage-d-alignement-court
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/check-instruction-budget.sh
autonomous: true
requirements: [BUDG-01, BUDG-02]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le gate `plugin/conductor/scripts/check-instruction-budget.sh` existe et PUBLIE, pour chaque fichier d'agent distribué (D-03 : `plugin/*/agents/*.md` + `plugin/*/AGENT.md`, glob à un seul niveau), DEUX métriques côte à côte sur une même ligne de tableau : le nombre de lignes du fichier entier et le nombre de lignes-instruction du body. Une seule brique, deux métriques (D-05)."
    - "D-01 est appliqué À LA LETTRE : une instruction est une ligne du BODY (frontmatter YAML exclu par l'awk à deux délimiteurs). La prose du champ `description:` ne compte JAMAIS — c'est la source mesurée de l'écart 463 vs 433 documenté en `25-RESEARCH.md:229-252`, tranché ici en faveur du texte verrouillé de D-01, pas du chiffre cité au cadrage."
    - "La liste de marqueurs D-01 est versée au gate telle quelle, insensible à la casse, jamais réduite : `JAMAIS|TOUJOURS|NE .* PAS|DOIT|MUST|NEVER|ALWAYS|interdit|obligatoire`. La forme alternative « puce impérative sous un titre de règles » (D-01, Claude's Discretion) est ADOPTÉE dans la forme mesurée par la recherche, avec dédoublonnage : une ligne qui porte un marqueur ET qui est une puce sous titre de règles compte UNE fois."
    - "Deux exclusions structurelles sont tranchées ici et vérifiées par contrôle négatif : les titres markdown (`^#+ `) ne comptent jamais comme instruction quel que soit leur libellé (25 faux positifs mesurés), et les blocs de code fenced sont retirés avant tout comptage d'instruction. Les lignes de tableau, elles, COMPTENT — sinon déplacer une règle dans un tableau la rendrait invisible au gate."
    - "La métrique « lignes » est comptée sur le FICHIER ENTIER par `awk 'END{print NR}'`, jamais `wc -l` (sous-compte un fichier sans `\\n` final) — même définition que l'enforcement mono-module existant T3/T5 de `test-dev-orchestrator.sh`, donc zéro conflit de définition entre les deux (D-05, assomption A1 de la recherche tranchée en faveur du précédent)."
    - "D-05 donne DEUX natures différentes aux deux métriques, et le gate les traite comme telles : pour les LIGNES, le plafond ADR-029 de 250 est un plafond ABSOLU armé par la même sentinelle — la limite effective d'un fichier est le plus petit de sa baseline et de 250, et un fichier au-dessus de 250 rougit une fois armé quelle que soit sa baseline (verdict `DEPASSEMENT-ADR029`). Pour les INSTRUCTIONS, il n'existe aucun plafond absolu : seule la baseline fait foi. Conséquence tranchée ici et portée par le plan 25-04 : si la Phase 40 rend `plugin/dev-orchestrator/AGENT.md` au-dessus de 250 lignes, la calibration NE grave PAS une baseline de lignes supérieure à 250 — graver 260 légaliserait une violation d'ADR-029 par simple mesure. Elle s'arrête et escalade : soit le fichier redescend, soit ADR-029 est révisée par arbitrage humain nommé."
    - "Le ratchet suit le seul précédent maison (D-04) : la sentinelle `.planning/.instruction-budget-armed` est LUE, jamais écrite par le gate ; le fichier de baselines `.planning/instruction-budget-baselines.tsv` est LU, jamais écrit par le gate. Aucun des deux n'est créé par ce plan (D-06 bis : ils appartiennent à la seconde PR)."
    - "Contrat de sortie tous codes énumérés dans l'en-tête (patron `check-divergence.sh`) : `0` armé et aucun dépassement · `1` armé et au moins un dépassement · `2` NON VÉRIFIABLE, toujours bloquant · `3` NON ARMÉ, rapport imprimé intégralement, jamais bloquant · `64` erreur d'usage. Aucun code implicite, aucun `|| true` sur une mesure."
    - "BUDG-02 / arête d'adjacence (sonde `adjacency`, résolue explicitement) : `courant == baseline` est VERT, `courant > baseline` strictement est ROUGE une fois armé. L'égalité n'est jamais un dépassement — sinon les deux fichiers déjà à 250/250 rougiraient au seul fait d'armer."
    - "BUDG-02 / arête du vide (sonde `empty`, résolue explicitement) : 0 fichier découvert → `2`, jamais un vert (D-03, découverte non vide obligatoire) ; armé avec un fichier de baselines absent, vide, incomplet (un fichier découvert sans entrée) ou orphelin (une entrée sans fichier) → `2`, jamais un vert. Non armé, ces mêmes états sont RAPPORTÉS et sortent en `3`."
    - "BUDG-02 / arête d'ordre (sonde `ordering`, résolue explicitement) : le tableau est trié par chemin sous `LC_ALL=C`, donc identique octet pour octet sur BSD et GNU ; deux exécutions successives sur un corpus inchangé produisent la même sortie."
    - "BUDG-01 / arête non classifiée (sonde `unclassified`, résolue explicitement) : « mesuré ET publié » signifie que la sortie porte, pour CHAQUE fichier découvert, une ligne de tableau complète — jamais un agrégat, jamais un extrait des pires fichiers. Le nombre de lignes de données du tableau est égal au nombre de fichiers découverts, et cette égalité est ce qui se vérifie par machine."
    - "Le gate CONSTATE, il ne corrige jamais (ADR-031) : il ne réécrit aucun fichier d'agent, n'écrit ni la sentinelle ni la baseline, et ne propose aucune remédiation automatique."
  artifacts:
    - "plugin/conductor/scripts/check-instruction-budget.sh : gate bash portable (préambule `set -uo pipefail` sans `-e`, jamais `grep -P`, jamais `mapfile`), en-tête portant le rôle, l'usage `[--path <dir>]` et les cinq codes de sortie énumérés, `-h|--help` rendu par `grep '^# ' \"$0\" | sed 's/^# //'` (idiome `check-requirements-survival.sh:56`)."
  key_links:
    - "Découverte glob (`plugin/*/agents/*.md` + `plugin/*/AGENT.md`, un seul niveau) <-> nombre de lignes de données du tableau : c'est le seul endroit du gate qui peut rendre un vert à vide. L'égalité des deux comptes est vérifiée par machine à chaque tâche — une découverte large (`find … -path '*/agents/*.md'`) capturerait 54 fichiers au lieu de 31 et une découverte cassée en capturerait 0."
    - "`body_only()` (awk à deux délimiteurs `---`) <-> métrique instructions : sans lui, la prose du champ `description:` gonfle le compte de ~30 lignes sur le corpus et contredit D-01. Le contrôle est un fichier de fixture dont le frontmatter porte un marqueur qui ne doit PAS être compté."
    - "Lecture de la sentinelle <-> code de sortie : c'est la charnière du ratchet BUDG-02. Si le gate cesse de la lire, il rend 3 pour toujours (jamais bloquant, donc inerte) ou 1 tout de suite (rouge des semaines, exactement l'anti-feature `.planning/REQUIREMENTS.md:1096`)."
    - "`VF_BUDGET_PLANNING_DIR` / `--path` <-> état armé : une surcharge d'environnement peut détourner le gate vers un autre `.planning` et neutraliser l'armement — même classe de faiblesse que celle fermée pour `check-state-integrity.sh` par le `--file` explicite de la CI (`.github/workflows/ci.yml:332-339`). L'en-tête du script dit que ces leviers existent POUR LES FIXTURES DE TEST, et l'étape CI (plan 25-03) n'en pose aucun."
  prohibitions:
    - "Le gate n'écrit JAMAIS la sentinelle `.planning/.instruction-budget-armed` ni le fichier `.planning/instruction-budget-baselines.tsv` — ni directement, ni via un hook, ni « pour amorcer ». Un gate qui s'arme lui-même rejoue la régression #38."
    - "Aucun fichier d'agent n'est réécrit, reformaté ou allégé dans ce plan : la remédiation des fichiers chargés est un geste ultérieur, hors périmètre (`<deferred>` du cadrage)."
    - "Aucune métrique en tokens estimés (piste F3 écartée, pas différée) : la phase mesure l'adhérence, pas la taille."
    - "Aucun `2>/dev/null || true` ni aucun `|| true` sur une mesure ou sur une invocation de gate — seul le garde-fou `|| echo 0` d'un comptage awk, visible dans le compte, est admis."
    - "Aucun chiffre de corpus (31, 3603, 433, 463, 43, 44, +68) n'est écrit en dur dans le script ni dans un critère d'acceptation : tout compte est re-dérivé par exécution au moment où il est vérifié."
    - "Aucun marqueur d'exclusion par ligne (type `vibeflow:allow-large-file`) n'est introduit : sur un ratchet non encore armé, une échappatoire par ligne permettrait de faire baisser un compte sans faire baisser la charge réelle. La baseline absorbe déjà les faux positifs résiduels, à coût nul."
---

<objective>
Livrer le gate `check-instruction-budget.sh` : une brique unique qui découvre les 31 fichiers
d'agents distribués, mesure DEUX métriques par fichier (lignes du fichier entier, instructions du
body), les publie dans un tableau intégral trié, les compare à une baseline versionnée qu'elle ne
touche jamais, et rend un code de sortie parmi cinq, tous énumérés.

Purpose : ADR-029 borne les lignes depuis toujours et n'a JAMAIS été machine-enforced par un gate
distribué (D-05, fait vérifié le 2026-09-14) ; la charge d'instructions — le proxy qui prédit
l'adhérence, contrairement au nombre de lignes — n'a jamais été mesurée du tout. Ce plan ferme les
deux trous avec une seule brique, et il le fait en mode AVERTISSEMENT : aucune baseline n'est
gravée ici, aucun rouge n'est possible (D-06 bis).

Output : un seul fichier, `plugin/conductor/scripts/check-instruction-budget.sh`. Ni sentinelle, ni
baseline, ni câblage CI, ni documentation — ils appartiennent aux plans 25-02, 25-03 et 25-04.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-CONTEXT.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-RESEARCH.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase. Les quatre items de la sonde d'arêtes sont rendus
`unresolved` par l'orchestrateur ; les quatre sont résolus EXPLICITEMENT dans `must_haves.truths`
ci-dessus (adjacence, vide, ordre, et la lecture « mesuré ET publié » de BUDG-01). Restent deux
hypothèses de ce plan, déclarées ici plutôt que résolues en silence :

1. **Le chiffre du cadrage (« 463 instructions, 44 pour `vf-dev-manager.md` ») est écarté au profit
   du texte de D-01.** — *ratifiée le 2026-09-15, D-01 bis (arbitrage Samuel, AskUserQuestion
   session principale) : l'hypothèse est close, elle n'est plus une question ouverte.* La recherche a mesuré que ce chiffre venait d'un `grep` sur le fichier
   ENTIER, frontmatter inclus (`25-RESEARCH.md:229-252`, assomption A4). D-01 dit « une ligne du
   **body** » ; c'est le TEXTE qui est verrouillé, pas le chiffre. Ce plan implémente le texte. La
   conséquence est que les valeurs gravées à la calibration (plan 25-04) ne seront ni 463 ni 44 —
   et personne ne doit s'en étonner. L'arbitrage a été rendu : D-01 bis (`25-CONTEXT.md:50-56`)
   ratifie le comptage body seul, hors frontmatter, et retire explicitement la question du
   checkpoint de calibration (`25-04`), qui « n'a plus à la reposer ».
2. **La règle « puce impérative sous un titre de règles » est adoptée** dans la forme mesurée
   (`25-RESEARCH.md:265-330`, assomption A3), ce qui ajoute une charge mesurée à environ un tiers
   des fichiers et change donc les baselines finales. Le refus de cette règle aurait laissé
   invisibles des lignes comme « ❌ Coder en dur des couleurs alors qu'un système de design
   existe. » — des impératifs purs sans aucun marqueur textuel. L'alternative (marqueurs textuels
   seuls) reste accessible sans rien casser tant que rien n'est gravé : c'est précisément la
   fenêtre que D-06 bis laisse ouverte.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tranche verticale — découvrir, mesurer les deux métriques, publier le tableau intégral sur le corpus réel, sortir en 3</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh</files>
  <read_first>
    - `plugin/dev-orchestrator/scripts/check-requirements-survival.sh:1-80` — l'analogue principal lu en entier : en-tête à rôle + issues + codes énumérés, parsing `--path` avec `exit 64`, `-h|--help` par `grep '^# '`, `PLANNING_DIR="${VF_LEDGER_PLANNING_DIR:-$ROOT/.planning}"`, préambule `set -uo pipefail`
    - `plugin/conductor/scripts/check-divergence.sh:44-92,150-175` — codes de sortie tous énumérés en en-tête, découverte non vide avec exit 2, `frontmatter_block()`, lecture de liste par `while IFS= read -r … done < <(…)` (jamais `mapfile`)
    - `plugin/software-architecture/scripts/check-file-size.sh:45-48` — l'idiome de comptage de lignes `awk 'END { print NR }'` et le commentaire qui explique pourquoi `wc -l` est refusé
    - `25-RESEARCH.md:164-228` (commande d'énumération exacte et pourquoi `find … -path` rend 54 fichiers au lieu de 31), `:461-485` (`body_only()`, comptage robuste), `:516-539` (portabilité BSD/GNU, codes normalisés)
    - `25-PATTERNS.md:31-145` — le gabarit d'en-tête, le parsing d'arguments et le bloc sentinelle à reproduire
    - `.planning/codebase/CONVENTIONS.md:56` — les codes normalisés du dépôt, à réconcilier explicitement (voir action)
  </read_first>
  <action>
    Écrire le gate de bout en bout sur UN chemin : corpus réel → deux métriques → tableau → code de
    sortie. Aucune couche n'est repoussée : la lecture de la sentinelle et la lecture de la baseline
    sont câblées dès cette tâche, même si aucun des deux fichiers n'existe encore.

    (1) **En-tête et contrat.** Reproduire le gabarit de `25-PATTERNS.md:38-55` : rôle (le gate
    CONSTATE un dépassement, il ne réécrit jamais un fichier — ADR-031), usage
    `check-instruction-budget.sh [--path <dir>]`, et les CINQ codes énumérés en toutes lettres :
    `0` armé et aucun dépassement · `1` armé et au moins un dépassement de baseline (lignes ou
    instructions) · `2` NON VÉRIFIABLE, toujours bloquant, armé ou non · `3` NON ARMÉ, rapport
    imprimé intégralement, jamais bloquant · `64` erreur d'usage. Ajouter dans l'en-tête un
    paragraphe de réconciliation : `.planning/codebase/CONVENTIONS.md:56` normalise `2` en « erreur
    d'usage » et `3` en « INDÉTERMINÉ », alors que les deux précédents directs de ce gate font
    l'inverse (`check-divergence.sh` : `2` non vérifiable, `3` silence ; `check-requirements-
    survival.sh` : `3` non armé, `64` usage). Ce gate suit les deux précédents et réserve `64` à
    l'usage — écrire ce choix et sa raison dans l'en-tête, ne pas le laisser deviner.

    (2) **Arguments.** Copier l'idiome de `check-requirements-survival.sh:40-62` : `--path <valeur>`
    en deux arguments (jamais la forme `--path=…`), `-h|--help`, tout le reste en `exit 64`.
    Définir `PLANNING_DIR="${VF_BUDGET_PLANNING_DIR:-$ROOT/.planning}"`,
    `SENTINEL="$PLANNING_DIR/.instruction-budget-armed"`,
    `BASELINE="${VF_BUDGET_BASELINE_FILE:-$PLANNING_DIR/instruction-budget-baselines.tsv}"`, et
    écrire dans l'en-tête que ces deux surcharges d'environnement existent POUR LES FIXTURES DE
    TEST : toute invocation de production (CI, hook, humain) les laisse absentes.

    (3) **Découverte.** Énumérer par glob à un seul niveau : `$ROOT/plugin/*/agents/*.md` puis
    `$ROOT/plugin/*/AGENT.md`, en testant `[ -f "$f" ]` sur chaque entrée (un glob non résolu rend
    le motif littéral). Normaliser chaque chemin en RELATIF à `$ROOT` — c'est cette forme qui sert de
    clé de baseline et qui s'affiche. Trier par `LC_ALL=C sort`. Si le compte est à zéro : message
    `[check-instruction-budget] aucun fichier d'agent distribué découvert — non vérifiable` sur
    stderr et `exit 2`, jamais un vert (D-03).

    (4) **Métrique lignes.** `awk 'END { print NR }' "$f"` sur le FICHIER ENTIER, frontmatter
    inclus. Ne jamais utiliser `wc -l`.

    (5) **Métrique instructions.** Une seule passe awk sur le body, dans cet ordre : retirer le
    frontmatter (awk à deux délimiteurs `/^---[[:space:]]*$/`, gabarit `25-PATTERNS.md:117-123`) ;
    retirer les blocs fenced (bascule d'état sur une ligne dont les trois premiers caractères non
    blancs sont des accents graves) ; ignorer toute ligne titre `^[[:space:]]*#+[[:space:]]` pour le
    COMPTAGE, mais s'en servir pour le SCOPE (voir tâche 2, qui pose la règle de puce). Compter à ce
    stade uniquement les lignes restantes qui matchent l'ERE D-01 insensible à la casse
    `JAMAIS|TOUJOURS|NE .* PAS|DOIT|MUST|NEVER|ALWAYS|interdit|obligatoire` (`grep -ciE` ou
    `tolower()` en awk — jamais `grep -P`). Les lignes de tableau markdown comptent comme n'importe
    quelle ligne : aucune exclusion.

    (6) **Sentinelle et baseline.** `ARMED=0 ; [ -f "$SENTINEL" ] && ARMED=1`. Si `$BASELINE` existe,
    en charger les entrées (format TSV `chemin<TAB>lignes<TAB>instructions`, lignes vides et lignes
    commençant par `#` ignorées) dans une table de correspondance lue par `while IFS= read -r`.
    Aucune écriture, jamais, sur aucun des deux chemins.

    (7) **Rapport, sur stdout.** Trois lignes d'en-tête (corpus découvert et son compte ; chemin de
    la sentinelle et son état ABSENTE/PRÉSENTE ; chemin de la baseline et son état), puis la ligne
    `FICHIER | LIGNES | BL-LIGNES | INSTR | BL-INSTR | VERDICT`, puis UNE ligne par fichier
    découvert, séparateur ` | `, dans l'ordre trié, une valeur de baseline absente rendue par un
    tiret. Vocabulaire de verdict, ASCII sans accent pour rester greppable : `OK` (courant égal ou
    inférieur à la baseline), `MARGE` (courant strictement inférieur — la baseline peut descendre),
    `DEPASSEMENT-LIGNES`, `DEPASSEMENT-INSTR`, `DEPASSEMENT-LIGNES+INSTR`, `DEPASSEMENT-ADR029`
    (posé en tâche 3), `SANS-BASELINE`. Terminer
    par une ligne de bilan portant le nombre de fichiers, le nombre de dépassements et l'état armé.
    Le tableau est imprimé INTÉGRALEMENT dans tous les cas, y compris quand tout est conforme : un
    gate qui n'imprime que les écarts ne publie rien (BUDG-01).

    (8) **Codes de sortie de cette tranche.** Non armé → `exit 3` systématiquement, après impression
    intégrale du rapport (D-04 : avertissement audible, jamais silencieux, jamais bloquant). Armé →
    `exit 1` s'il existe au moins un dépassement, sinon `exit 0`. Les branches « non vérifiable » au
    niveau fichier (frontmatter cassé) et « contrat de baseline incohérent » sont posées en tâche 3.

    Portabilité : `set -uo pipefail` sans `-e`, jamais `mapfile`/`readarray` (bash 3.2 macOS rend
    127), `[[:space:]]` jamais `\s`, `LC_ALL=C` sur tout tri.
  </action>
  <behavior>
    - Sur le dépôt réel, sentinelle absente : le gate imprime une ligne de tableau par fichier découvert et sort en 3.
    - Le nombre de lignes de données du tableau est exactement égal au nombre de fichiers rendus par le glob à un seul niveau.
    - Après exécution, ni la sentinelle ni le fichier de baselines n'existent : le gate n'a rien écrit.
    - Le tableau est trié par chemin, et deux exécutions successives rendent la même sortie.
  </behavior>
  <acceptance_criteria>
    - `plugin/conductor/scripts/check-instruction-budget.sh` existe, passe `bash -n`, et son en-tête énumère les cinq codes `0`, `1`, `2`, `3`, `64`.
    - Exécuté sans argument depuis la racine du dépôt, le gate sort en `3` (sentinelle absente).
    - Sa sortie standard contient la ligne d'en-tête `FICHIER | LIGNES | BL-LIGNES | INSTR | BL-INSTR | VERDICT`.
    - Le nombre de lignes de sortie commençant par `plugin/` est égal au nombre de fichiers rendus par `plugin/*/agents/*.md` + `plugin/*/AGENT.md` (globs à un seul niveau, recomptés au moment de la vérification), et ce nombre est au moins 20 — un plancher qui rend impossible de passer sur une découverte dégénérée.
    - Après l'exécution, `.planning/.instruction-budget-armed` et `.planning/instruction-budget-baselines.tsv` n'existent toujours pas.
    - Deux exécutions consécutives produisent une sortie standard identique (`cksum` égal — jamais `diff`, proxifié menteur sur ce poste).
  </acceptance_criteria>
  <verify>
    <automated>S=plugin/conductor/scripts/check-instruction-budget.sh; bash -n "$S" || exit 1; for c in 0 1 2 3 64; do grep -qE "^#[[:space:]]+$c[[:space:]]" "$S" || { echo "CODE-NON-ENUMERE: $c"; exit 1; }; done; rc=0; out="$(bash "$S")" || rc=$?; printf '%s\n' "$out"; test "$rc" -eq 3 || { echo "RC-INATTENDU: $rc (attendu 3, sentinelle absente)"; exit 1; }; printf '%s\n' "$out" | grep -q '^FICHIER | LIGNES | BL-LIGNES | INSTR | BL-INSTR | VERDICT$' || { echo "EN-TETE-TABLEAU-ABSENTE"; exit 1; }; rows=$(printf '%s\n' "$out" | grep -c '^plugin/'); glob=$({ for f in plugin/*/agents/*.md; do [ -f "$f" ] && echo x; done; for f in plugin/*/AGENT.md; do [ -f "$f" ] && echo x; done; } | grep -c x); echo "lignes-de-donnees=$rows corpus-glob=$glob"; test "$rows" -eq "$glob" || { echo "TABLEAU-PARTIEL: $rows lignes pour $glob fichiers"; exit 1; }; test "$rows" -ge 20 || { echo "DECOUVERTE-DEGENEREE: $rows"; exit 1; }; test ! -e .planning/.instruction-budget-armed || { echo "SENTINELLE-ECRITE-PAR-LE-GATE"; exit 1; }; test ! -e .planning/instruction-budget-baselines.tsv || { echo "BASELINE-ECRITE-PAR-LE-GATE"; exit 1; }; a=$(bash "$S" 2>/dev/null | cksum); b=$(bash "$S" 2>/dev/null | cksum); test "$a" = "$b" || { echo "SORTIE-NON-DETERMINISTE: $a vs $b"; exit 1; }; echo TRACER-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `CODE-NON-ENUMERE: <c>`, `RC-INATTENDU: <rc>`, `EN-TETE-TABLEAU-ABSENTE`, `TABLEAU-PARTIEL: <n> lignes pour <m> fichiers`, `DECOUVERTE-DEGENEREE: <n>`, `SENTINELLE-ECRITE-PAR-LE-GATE`, `BASELINE-ECRITE-PAR-LE-GATE`, `SORTIE-NON-DETERMINISTE` apparaît ; l'absence de la ligne finale `TRACER-OK` vaut échec. Causes discriminantes : le script n'existe pas ou n'est pas du bash valide ; un code de sortie n'est pas énuméré en en-tête ; le gate rend autre chose que 3 alors que la sentinelle est absente (ratchet inversé) ; le tableau ne couvre pas tous les fichiers découverts (découverte ou boucle d'impression cassée, ou glob élargi à 54 fichiers) ; le gate a écrit lui-même la sentinelle ou la baseline (régression #38) ; la sortie varie d'une exécution à l'autre (tri non stable).</fails_when>
  </verify>
  <done>Le gate tourne de bout en bout sur le corpus réel, publie une ligne par fichier avec ses deux métriques, ne touche à rien, et sort en avertissement.</done>
  <reversibility rating="reversible">Un script neuf, non câblé, que rien n'invoque encore : le retirer ne casse aucun contrat publié.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : formes normatives — puce impérative sous titre de règles, titres et blocs fenced exclus, dédoublonnage, contrôles positif et négatif</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh</files>
  <precondition>La tâche 1 est verte : le gate sort en 3 sur le corpus réel et publie une ligne par fichier découvert.</precondition>
  <read_first>
    - `plugin/conductor/scripts/check-instruction-budget.sh` dans son état de fin de tâche 1
    - `25-RESEARCH.md:254-330` — la liste de titres de règles trouvée par grep sur les 31 fichiers, la règle candidate mesurée, les contrôles positif et négatif cités verbatim, et l'impact mesuré
    - `25-RESEARCH.md:332-349` — les 25 titres de section faussement comptés et les 4 lignes en bloc fenced, avec la recommandation d'exclusion
    - `25-RESEARCH.md:590-606` — l'awk de détection de scope « titre de règles » déjà vérifié sur le corpus
    - `25-RESEARCH.md:797-808` — Open Question 1 (sous-titres imbriqués) à trancher dans cette tâche
    - `25-CONTEXT.md:118-125` — la Claude's Discretion qui rend cette forme au plan, et `:171` qui exige que le contrôle négatif soit tranché, pas ignoré
  </read_first>
  <action>
    Compléter le compteur d'instructions avec la seconde forme de D-01, et graver les deux
    exclusions structurelles avec leurs contrôles.

    (1) **Scope « titre de règles ».** Dans la passe awk du body, maintenir un drapeau `under` :
    toute ligne titre `^[[:space:]]*#+[[:space:]]` referme le scope, puis le rouvre si et seulement
    si son propre libellé matche, insensible à la casse, l'ERE
    `r[eè]gles|garde-fous|iron law|anti-pattern|lignes rouges|discipline`. Trancher Open Question 1
    EN TOUTES LETTRES dans un commentaire du script : un titre de n'importe quel niveau referme le
    scope, y compris un `###` sous un `## Iron Laws` — la règle est de niveau plat, pas récursive,
    parce qu'un scope qui traverse les sous-titres compterait des exemples et des tableaux comme
    des règles. Une section de règles structurée en sous-sections devra répéter un libellé de règles
    dans ses sous-titres pour rester comptée ; c'est un choix conservateur assumé (il sous-compte
    plutôt qu'il ne sur-compte, et une baseline sous-comptée reste un contrat tenable).

    (2) **Puce impérative.** Sous un scope ouvert, une ligne qui commence (après espaces éventuels)
    par `- `, `* `, le caractère croix rouge, ou `N. ` (liste numérotée) compte comme instruction
    MÊME si elle ne porte aucun des huit marqueurs textuels.

    (3) **Dédoublonnage.** Le compte est une union, jamais une somme : une ligne qui matche l'ERE
    D-01 ET qui est une puce sous titre de règles compte UNE fois. L'implémenter comme un `if …
    else if …` sur la même ligne, pas comme deux compteurs additionnés.

    (4) **Exclusions, avec leur raison écrite en commentaire.** Les titres `^[[:space:]]*#+[[:space:]]`
    ne comptent jamais (mesuré : 25 libellés de section du corpus contiennent un marqueur sans être
    une instruction) ; les lignes à l'intérieur d'un bloc fenced ne comptent jamais (du code cité,
    pas une directive). En revanche les lignes de tableau markdown COMPTENT : écrire en commentaire
    que les exclure ouvrirait une échappatoire — déplacer une règle dans un tableau la rendrait
    invisible au gate. Et écrire qu'aucun marqueur d'exclusion par ligne n'est introduit : sur un
    ratchet, une échappatoire par ligne ferait baisser un compte sans faire baisser la charge.

    (5) **Contrôles gravés en commentaire du script**, repris de la recherche, en une ligne chacun :
    contrôle positif = une puce d'anti-pattern sans aucun marqueur textuel doit compter ; contrôle
    négatif = une puce descriptive listant un chemin de documentation, sous un titre non-règles
    placé APRÈS un titre de règles, ne doit pas compter. Ces deux cas seront rejoués en fixture par
    la suite du plan 25-02 ; le commentaire dit d'où ils viennent (`25-RESEARCH.md:275-302`).
  </action>
  <behavior>
    - Une puce sous un titre de règles sans marqueur textuel compte pour 1.
    - Une puce sous un titre non-règles qui suit un titre de règles ne compte pas : le scope s'est refermé.
    - Un titre de section dont le libellé contient un marqueur ne compte pas.
    - Une ligne portant un marqueur à l'intérieur d'un bloc fenced ne compte pas.
    - Une ligne du frontmatter portant un marqueur ne compte pas.
    - Une ligne à la fois puce sous règles et porteuse d'un marqueur compte pour 1, pas 2.
  </behavior>
  <acceptance_criteria>
    - Sur une fixture dont le body contient, dans cet ordre : un titre de règles, une puce sans marqueur, une puce commençant par une croix rouge sans marqueur, une puce portant un marqueur, un titre de section dont le libellé porte un marqueur, une ligne de prose portant un marqueur, un bloc fenced contenant une ligne à marqueur, un titre non-règles et une puce descriptive — et dont le frontmatter porte un marqueur dans `description:` — la colonne INSTR vaut exactement `4`.
    - Sur une seconde fixture ne contenant qu'un titre `## Garde-fous` suivi de deux puces sans aucun marqueur, puis un titre `## Notes` suivi d'une puce, la colonne INSTR vaut exactement `2`.
    - Le tableau rendu pour ces deux fixtures porte une ligne de données pour chacune des deux fixtures, et le gate sort en `3` (fixture non armée).
    - Le script contient un commentaire qui tranche explicitement le cas du sous-titre imbriqué (Open Question 1) et un commentaire qui justifie le maintien des lignes de tableau dans le comptage.
  </acceptance_criteria>
  <verify>
    <automated>S=plugin/conductor/scripts/check-instruction-budget.sh; bash -n "$S" || exit 1; F=$(printf '\140\140\140'); X=$(printf '\342\235\214'); T=$(mktemp -d); mkdir -p "$T/plugin/demo/agents"; printf '%s\n' '---' 'name: demo-a' 'description: ne fait JAMAIS le travail metier lui-meme' '---' '## Iron Laws' '- Tout lab embarque ses auditeurs.' "- $X Coder en dur des couleurs alors qu un systeme de design existe." '- Ne declenche JAMAIS un ship sans arbitrage.' '## Retour (bloc type obligatoire)' 'Le manager DOIT rendre un rapport de mission.' "$F" 'echo "le script DOIT sortir 1"' "$F" '## References' '- Workflow quotidien : chemin/vers/doc.md' > "$T/plugin/demo/agents/a.md"; printf '%s\n' '---' 'name: demo-b' '---' '## Garde-fous' '- Un seul ecrivain par arbre.' '- Le lock est verifie avant tout checkout.' '## Notes' '- Un chemin de documentation.' > "$T/plugin/demo/agents/b.md"; rc=0; out="$(bash "$S" --path "$T")" || rc=$?; printf '%s\n' "$out"; test "$rc" -eq 3 || { echo "RC-INATTENDU: $rc (attendu 3)"; rm -rf "$T"; exit 1; }; ia=$(printf '%s\n' "$out" | awk -F'|' '/^plugin\/demo\/agents\/a\.md /{gsub(/[[:space:]]/,"",$4); print $4}'); ib=$(printf '%s\n' "$out" | awk -F'|' '/^plugin\/demo\/agents\/b\.md /{gsub(/[[:space:]]/,"",$4); print $4}'); rows=$(printf '%s\n' "$out" | grep -c '^plugin/'); echo "instr_a=$ia instr_b=$ib lignes=$rows"; rm -rf "$T"; test "$rows" -ge 2 || { echo "TABLEAU-PARTIEL: $rows"; exit 1; }; test "$ia" = "4" || { echo "COMPTE-A-FAUX: $ia attendu 4"; exit 1; }; test "$ib" = "2" || { echo "COMPTE-B-FAUX: $ib attendu 2"; exit 1; }; grep -qi 'sous-titre' "$S" || { echo "OQ1-NON-TRANCHEE"; exit 1; }; grep -qi 'tableau' "$S" || { echo "CHOIX-TABLEAUX-NON-JUSTIFIE"; exit 1; }; echo FORMES-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `RC-INATTENDU: <rc>`, `TABLEAU-PARTIEL: <n>`, `COMPTE-A-FAUX: <n> attendu 4`, `COMPTE-B-FAUX: <n> attendu 2`, `OQ1-NON-TRANCHEE`, `CHOIX-TABLEAUX-NON-JUSTIFIE` apparaît ; l'absence de `FORMES-OK` vaut échec. Causes discriminantes : la règle de puce n'est pas implémentée (compte A à 2 au lieu de 4, compte B à 0) ; le scope ne se referme pas au titre suivant (compte A à 5, compte B à 3) ; les titres ne sont pas exclus (compte A à 5) ; les blocs fenced ne sont pas retirés (compte A à 5) ; le frontmatter est compté (compte A à 5) ; le dédoublonnage manque (compte A à 5) ; le script ne porte pas la trace écrite des deux arbitrages de forme.</fails_when>
  </verify>
  <done>Les deux formes de D-01 sont comptées, dédoublonnées, et les deux exclusions structurelles sont gravées avec leur contrôle positif et négatif.</done>
  <reversibility rating="costly">D-01 : la définition fonde la baseline. La changer APRÈS l'armement invaliderait toutes les valeurs publiées — mais rien n'est gravé avant le plan 25-04, donc la fenêtre de révision reste ouverte jusque-là.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : contrat de sortie complet — imparsable bruyant, contrat de baseline incohérent, adjacence, erreur d'usage</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh</files>
  <precondition>Les tâches 1 et 2 sont vertes : les deux métriques sont exactes sur fixture et le gate publie un tableau intégral.</precondition>
  <read_first>
    - `plugin/conductor/scripts/check-instruction-budget.sh` dans son état de fin de tâche 2
    - `plugin/dev-orchestrator/scripts/check-requirements-survival.sh:1-37` — le bloc d'issues déclarées en en-tête, dont deux BRUYANT, et la doctrine « lecteur d'absence, jamais juge de contenu »
    - `25-RESEARCH.md:541-588` — Pitfall 1 (frontmatter compté), Pitfall 2 (le corpus n'a aucun cas imparsable réel), Pitfall 3 (armement prématuré)
    - `25-CONTEXT.md:111-116` — QUAL-01 : trois issues, l'imparsable jamais compté vert
    - `.planning/REQUIREMENTS.md:1096` — l'anti-feature « blocage CI dur immédiat », qui interdit tout rouge en mode non armé
  </read_first>
  <action>
    Poser les branches qui manquent au contrat, en gardant une règle unique au-dessus de tout : en
    mode NON ARMÉ, aucune condition ne peut rendre autre chose que `3` — sauf l'indisponibilité
    structurelle (`2`), qui est bloquante dans les deux modes parce qu'un gate qui n'a pas pu
    regarder n'est pas un gate vert.

    (1) **Imparsable BRUYANT (issue 3 de QUAL-01).** Un fichier découvert est déclaré non
    vérifiable quand il est illisible, quand son frontmatter s'ouvre sans jamais se refermer (un
    seul délimiteur `---`), ou quand son comptage de lignes rend une valeur vide. Sa ligne de
    tableau porte le verdict `NON-VERIFIABLE` et des tirets dans les colonnes de métrique ; un
    message `[check-instruction-budget] <chemin> : non vérifiable — <raison>` part sur stderr ; le
    code de sortie devient `2`, armé ou non. Un fichier sans frontmatter du tout n'est PAS un cas
    imparsable : son body est le fichier entier, il se compte normalement — écrire cette distinction
    en commentaire, elle sera testée en 25-02.

    (2) **Contrat de baseline.** Quand la sentinelle est présente, quatre états rendent `2` :
    fichier de baselines absent ; fichier présent sans aucune entrée de données ; un fichier
    découvert sans entrée correspondante (verdict `SANS-BASELINE`) ; une entrée dont le fichier
    n'existe plus (entrée orpheline, signalée par un message nommant le chemin). Raison à écrire en
    commentaire : armer sans contrat, ou avec un contrat qui ne recouvre pas le corpus, n'est pas un
    dépassement — c'est une absence de verdict possible. Quand la sentinelle est absente, ces mêmes
    états sont rapportés dans le tableau et sur stderr, et le code reste `3`.

    (2bis) **Le rapport précède toujours la décision.** Dès qu'au moins un fichier a été découvert,
    le tableau complet est imprimé sur stdout AVANT que le code de sortie soit rendu — y compris
    avant un `exit 2` de contrat de baseline ou un `exit 2` d'imparsable. Seule la découverte vide
    n'a rien à imprimer. Raison à écrire en commentaire : un gate qui sort en erreur sans publier ce
    qu'il a vu oblige à relancer à la main pour savoir ce qui s'est passé, et c'est aussi ce qui
    rend la calibration (plan 25-04) mesurable avant tout armement.

    (3bis) **Plafond absolu ADR-029 sur la métrique LIGNES.** D-05 dit que le plafond de 250 lignes
    est armé par la même sentinelle : la limite effective d'un fichier pour les lignes est le plus
    petit de sa baseline et de `250` (constante nommée en tête de script, avec l'ADR en commentaire).
    Un fichier au-dessus de 250 lignes rougit une fois armé QUELLE QUE SOIT sa baseline, avec le
    verdict `DEPASSEMENT-ADR029`. La métrique INSTRUCTIONS n'a aucun plafond absolu : seule sa
    baseline fait foi. Écrire en commentaire la raison : une baseline de lignes supérieure à 250
    légaliserait une violation d'ADR-029 par simple mesure, et c'est exactement le risque ouvert par
    la Phase 40, qui réécrit `plugin/dev-orchestrator/AGENT.md` (mesuré à 205 lignes avant elle).

    (3) **Adjacence.** La comparaison est strictement supérieure : `courant > baseline` est un
    dépassement, `courant == baseline` ne l'est pas. Écrire en commentaire pourquoi : deux fichiers
    du corpus sont déjà à 250/250 lignes, et une comparaison en `>=` rendrait rouge le seul fait
    d'armer. Un `courant < baseline` rend le verdict `MARGE` — une information publiée, jamais un
    échec, et jamais une réécriture automatique de la baseline (le ratchet descend par geste humain,
    dans le commit de remédiation).

    (4) **Erreur d'usage.** `--path` sans valeur, `--path` vers un répertoire inexistant, argument
    inconnu : `exit 64` avec un message nommant l'argument fautif. Un `--path` vers un répertoire qui
    existe mais ne contient aucun `plugin/` retombe sur la découverte vide, donc `2`, pas `64` —
    écrire la distinction en commentaire.

    (5) **Bilan final.** La ligne de bilan nomme l'état armé, le nombre de fichiers, le nombre de
    dépassements et le nombre de fichiers non vérifiables, et se termine par le code qui va être
    rendu — pour qu'un journal de CI se lise sans relire le script.
  </action>
  <behavior>
    - Découverte vide : exit 2, armé ou non.
    - Frontmatter ouvert jamais refermé : exit 2, ligne de tableau en NON-VERIFIABLE.
    - Armé sans fichier de baselines : exit 2.
    - Armé, baseline égale au courant sur les deux métriques : exit 0.
    - Armé, baseline instructions strictement inférieure au courant : exit 1.
    - Armé, baseline lignes strictement inférieure au courant : exit 1.
    - Armé, entrée de baseline orpheline : exit 2.
    - Armé, fichier à 251 lignes avec une baseline de lignes à 251 : exit 1, verdict `DEPASSEMENT-ADR029` — le plafond absolu prime sur la baseline.
    - Non armé, quelle que soit la baseline : exit 3.
    - Argument inconnu : exit 64.
  </behavior>
  <acceptance_criteria>
    - Une fixture sans aucun fichier d'agent rend `2`.
    - Une fixture dont un fichier d'agent a un frontmatter ouvert et jamais refermé rend `2`, et sa ligne de tableau porte `NON-VERIFIABLE`.
    - Une fixture armée (sentinelle présente) sans fichier de baselines rend `2`.
    - Une fixture armée dont la baseline reprend exactement les valeurs mesurées rend `0`.
    - La même fixture avec une baseline d'instructions inférieure d'une unité rend `1` ; avec une baseline de lignes inférieure d'une unité, rend `1`.
    - La même fixture avec une entrée de baseline pointant un chemin inexistant rend `2`.
    - Une fixture armée dont un fichier compte 251 lignes, avec une baseline de lignes à 251, rend `1` et porte le verdict `DEPASSEMENT-ADR029` : le plafond absolu de 250 prime sur la baseline.
    - La même fixture avec la sentinelle retirée rend `3`, quelle que soit la baseline.
    - Un argument inconnu rend `64`.
  </acceptance_criteria>
  <verify>
    <automated>S=plugin/conductor/scripts/check-instruction-budget.sh; bash -n "$S" || exit 1; T=$(mktemp -d); fail=0; mk() { d="$T/$1"; mkdir -p "$d/plugin/demo/agents" "$d/.planning"; printf '%s\n' '---' 'name: d' '---' '## Iron Laws' '- Le lock est verifie avant tout checkout.' > "$d/plugin/demo/agents/a.md"; printf '%s' "$d"; }; run() { rc=0; bash "$S" --path "$1" >/dev/null 2>&1 || rc=$?; printf '%s' "$rc"; }; chk() { if [ "$2" = "$3" ]; then echo "  ok $1 -> $2"; else echo "  KO $1 -> obtenu $2 attendu $3"; fail=$((fail+1)); fi; }; E="$T/vide"; mkdir -p "$E"; chk "decouverte-vide" "$(run "$E")" 2; A=$(mk a); chk "non-arme" "$(run "$A")" 3; B=$(mk b); printf '%s\n' '---' 'name: casse' '## Titre' '- une puce' > "$B/plugin/demo/agents/casse.md"; chk "frontmatter-non-referme" "$(run "$B")" 2; printf '%s\n' "$(bash "$S" --path "$B" 2>/dev/null | grep 'casse.md')" | grep -q 'NON-VERIFIABLE' || { echo "  KO verdict NON-VERIFIABLE absent du tableau"; fail=$((fail+1)); }; C=$(mk c); : > "$C/.planning/.instruction-budget-armed"; chk "arme-sans-baseline" "$(run "$C")" 2; L=$(awk 'END{print NR}' "$C/plugin/demo/agents/a.md"); I=$(bash "$S" --path "$C" 2>/dev/null | awk -F'|' '/^plugin\/demo\/agents\/a\.md /{gsub(/[[:space:]]/,"",$4); print $4}'); echo "  mesure fixture: lignes=$L instr=$I"; printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$L" "$I" > "$C/.planning/instruction-budget-baselines.tsv"; chk "arme-egalite-adjacence" "$(run "$C")" 0; printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$L" "$((I-1))" > "$C/.planning/instruction-budget-baselines.tsv"; chk "arme-depassement-instr" "$(run "$C")" 1; printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$((L-1))" "$I" > "$C/.planning/instruction-budget-baselines.tsv"; chk "arme-depassement-lignes" "$(run "$C")" 1; printf 'plugin/demo/agents/fantome.md\t%s\t%s\nplugin/demo/agents/a.md\t%s\t%s\n' 9 9 "$L" "$I" > "$C/.planning/instruction-budget-baselines.tsv"; chk "arme-entree-orpheline" "$(run "$C")" 2; D=$(mk d); : > "$D/.planning/.instruction-budget-armed"; { printf '%s\n' '---' 'name: d' '---'; i=0; while [ "$i" -lt 248 ]; do echo "ligne $i"; i=$((i+1)); done; } > "$D/plugin/demo/agents/a.md"; DL=$(awk 'END{print NR}' "$D/plugin/demo/agents/a.md"); DI=$(bash "$S" --path "$D" 2>/dev/null | awk -F'|' '/^plugin\/demo\/agents\/a\.md /{gsub(/[[:space:]]/,"",$4); print $4}'); echo "  fixture plafond: lignes=$DL"; printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$DL" "$DI" > "$D/.planning/instruction-budget-baselines.tsv"; chk "plafond-absolu-adr029" "$(run "$D")" 1; bash "$S" --path "$D" 2>/dev/null | grep -q 'DEPASSEMENT-ADR029' || { echo "  KO verdict DEPASSEMENT-ADR029 absent du tableau"; fail=$((fail+1)); }; printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$L" "$((I-1))" > "$C/.planning/instruction-budget-baselines.tsv"; rm -f "$C/.planning/.instruction-budget-armed"; chk "non-arme-malgre-depassement" "$(run "$C")" 3; rc=0; bash "$S" --inconnu >/dev/null 2>&1 || rc=$?; chk "argument-inconnu" "$rc" 64; rm -rf "$T"; echo "== bilan contrat : $fail ecart(s) =="; test "$fail" -eq 0</automated>
    <fails_when>sortie non nulle, ou une ligne `  KO <cas> -> obtenu <rc> attendu <rc>` apparaît, ou le bilan final annonce au moins un écart. Chaque cas est discriminant d'une branche précise : `decouverte-vide` échoue si le gate rend un vert sur zéro cible (le mode de défaut dominant du dépôt) ; `frontmatter-non-referme` échoue si l'imparsable est compté silencieusement à zéro au lieu d'être bruyant ; `arme-sans-baseline` échoue si armer sans contrat passe pour conforme ; `arme-egalite-adjacence` échoue si la comparaison est en `>=` (les deux fichiers déjà à 250/250 rougiraient au seul armement) ; `arme-depassement-instr` et `arme-depassement-lignes` échouent si l'une des deux métriques n'est pas comparée ; `arme-entree-orpheline` échoue si une baseline périmée passe inaperçue ; `plafond-absolu-adr029` échoue si la baseline prime sur le plafond de 250 lignes, ce qui permettrait de légaliser une violation d'ADR-029 en gravant simplement la valeur mesurée ; `non-arme-malgre-depassement` échoue si le gate bloque alors que la sentinelle est absente (anti-feature `.planning/REQUIREMENTS.md:1096`) ; `argument-inconnu` échoue si l'erreur d'usage se confond avec un verdict.</fails_when>
  </verify>
  <done>Les cinq codes de sortie sont atteignables et prouvés un par un sur fixture jetable ; aucune combinaison ne rend un vert par défaut.</done>
  <reversibility rating="reversible">Le contrat n'engage encore rien : aucun consommateur (CI, hook, agent) n'invoque le gate avant le plan 25-03.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt versionné → gate | Le gate lit du contenu (fichiers d'agents, baseline, sentinelle) que n'importe quel contributeur ou agent peut modifier dans une PR. |
| environnement → gate | `--path`, `VF_BUDGET_PLANNING_DIR`, `VF_BUDGET_BASELINE_FILE` peuvent rediriger ce que le gate regarde. |
| gate → système de fichiers | Le gate ne doit franchir cette frontière en écriture dans AUCUN cas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | découverte glob de `check-instruction-budget.sh` | high | mitigate | Découverte non vide obligatoire : 0 fichier → exit 2 (tâche 3). Le nombre de lignes de données du tableau est comparé par machine au recomptage du glob, avec un plancher, à chaque vérification de tâche (tâche 1). |
| T-25-02 | Elevation of Privilege | sentinelle `.planning/.instruction-budget-armed` | high | mitigate | Le gate LIT la sentinelle et ne l'écrit jamais ; la vérification de la tâche 1 constate son absence après exécution. Un gate qui s'arme ou se désarme lui-même rejouerait la régression #38 (armement qui ne voyage pas). |
| T-25-03 | Tampering | fichier de baselines `.planning/instruction-budget-baselines.tsv` | high | mitigate | Le gate ne l'écrit jamais (constaté après exécution, tâche 1) ; une entrée manquante ou orpheline en mode armé rend 2 plutôt qu'un vert (tâche 3). La hausse d'une valeur reste un geste humain visible en diff, gaté par la convention d'arbitrage daté de `CLAUDE.md` (application en 25-04). |
| T-25-04 | Spoofing | surcharges `--path` / `VF_BUDGET_PLANNING_DIR` / `VF_BUDGET_BASELINE_FILE` | medium | mitigate | Les deux surcharges d'environnement sont documentées en en-tête comme réservées aux fixtures de test ; l'étape CI du plan 25-03 invoque le gate sans aucune surcharge, sur le chemin par défaut — même durcissement que le `--file` explicite de `check-state-integrity.sh` (`ci.yml:332-339`). |
| T-25-05 | Denial of Service | comptage awk sur 31 fichiers | low | accept | Corpus borné à quelques milliers de lignes, aucune récursion, aucun réseau : le coût est inférieur à la seconde. Aucune mitigation nécessaire. |
| T-25-06 | Information Disclosure | rapport imprimé en CI | low | accept | Le rapport ne publie que des chemins versionnés et deux entiers par fichier — rien qui ne soit déjà public dans le dépôt. |
| T-25-SC | Tampering | chaîne d'approvisionnement (npm/pip/cargo) | low | accept | Aucune installation de paquet dans cette phase : le gate est du bash pur sur `awk`/`grep`/`sed`, tous déjà couverts par `plugin/installer/scripts/preflight.sh` (`25-RESEARCH.md:351-356`). Aucun paquet `[ASSUMED]`/`[SUS]` à faire valider. |
</threat_model>

<verification>
- `bash -n plugin/conductor/scripts/check-instruction-budget.sh` passe.
- Le gate rend `3` sur le dépôt réel, publie une ligne par fichier découvert, et n'écrit rien.
- Les cinq codes de sortie sont chacun atteints par une fixture jetable.
- Aucun chemin sous `plugin/` autre que le script neuf n'apparaît dans `git status --short plugin`.
</verification>

<success_criteria>
- Une brique unique publie lignes ET instructions par fichier (D-05), sur le corpus D-03.
- D-01 est appliqué à la lettre (body-only) et étendu par la règle de puce, avec dédoublonnage.
- Le ratchet D-04 est câblé en lecture seule, et le mode non armé ne peut rien bloquer (BUDG-02,
  anti-feature `.planning/REQUIREMENTS.md:1096`).
- Les trois issues de QUAL-01 sont atteignables : conforme, dépassement, imparsable bruyant.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 25-01)

| Artefact | Nature | Détail |
|---|---|---|
| `plugin/conductor/scripts/check-instruction-budget.sh` | script neuf (gate machine) | Codes de sortie : `0` conforme armé · `1` dépassement armé · `2` non vérifiable (toujours bloquant) · `3` non armé (avertissement) · `64` erreur d'usage. |
| `--path <dir>` | option | Racine de découverte, en DEUX arguments (jamais `--path=`). |
| `-h`, `--help` | option | Rend l'en-tête du script (idiome `grep '^# '`). |
| `VF_BUDGET_PLANNING_DIR` | variable d'environnement | Surcharge du dossier `.planning` — fixtures de test uniquement. |
| `VF_BUDGET_BASELINE_FILE` | variable d'environnement | Surcharge du chemin de baseline — fixtures de test uniquement. |
| `FICHIER \| LIGNES \| BL-LIGNES \| INSTR \| BL-INSTR \| VERDICT` | contrat de sortie | En-tête du tableau, suivi d'une ligne par fichier découvert, triée `LC_ALL=C`. |
| `OK`, `MARGE`, `DEPASSEMENT-LIGNES`, `DEPASSEMENT-INSTR`, `DEPASSEMENT-LIGNES+INSTR`, `DEPASSEMENT-ADR029`, `SANS-BASELINE`, `NON-VERIFIABLE` | vocabulaire de verdict | ASCII sans accent, greppable. `DEPASSEMENT-ADR029` = plafond absolu de 250 lignes franchi, quelle que soit la baseline. |
| `VF_BUDGET_LINE_CAP` (constante interne `250`) | plafond ADR-029 | Nommée en tête de script ; la limite effective de lignes d'un fichier est le plus petit de sa baseline et de cette constante. |

**Référencé mais NON créé par ce plan** (D-06 bis, seconde PR) :
`.planning/instruction-budget-baselines.tsv`, `.planning/.instruction-budget-armed`.

*(Aucun symbole d'API externe : le détecteur `api-coverage` rendu `detected: false` par
l'orchestrateur reste false sur ce corps de plan — aucun `COVERAGE.md` à produire.)*
</artifacts>

<output>
Créer `.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-01-SUMMARY.md`
en fin de plan. Commits en français, avec le bandeau d'arbitrage
« arbitrage Samuel, AskUserQuestion session principale, 2026-09-14 » pour les décisions D-01 à D-05.
</output>
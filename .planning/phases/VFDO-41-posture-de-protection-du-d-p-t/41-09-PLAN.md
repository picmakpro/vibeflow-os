---
phase: 41-posture-de-protection-du-d-p-t
plan: 09
lot: L9 — refus serveur d'un push direct sur `main` (D-03) et mesure M-4 du ruleset de tags `v*`
type: execute
wave: 9
depends_on: ["41-08"]
autonomous: false
requirements: [PROT-01, PROT-02]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition : `M3-VERDICT: CONFORME`, ou `M3-DECISION:` d'option `fermer-et-poursuivre` ou `consigner-et-poursuivre` ; PR-P non ouverte (aucune PR ouverte)"
    - "D-03 prouvé par un essai réel : un commit vide (arbre identique à `main`) poussé directement sur `refs/heads/main` par `samuel-neveugall` (bypass en mode `pull_request`) est refusé, sortie intégrale capturée. PROVENANCE (point 2 du tour 1) : `client_hook` si la sortie porte `[pre-push]` (refus du hook local), `serveur` si elle porte des lignes `remote:` (réponse du serveur), `indeterminee` sinon — un refus client n'est jamais écrit `serveur`. `D03-VERDICT: CONFORME` exige un refus de provenance `serveur` mentionnant la PR exigée et `main` distante inchangée ; sinon arrêt sur décision"
    - "Relecture des traces (point 6 du tour 1) : quand le compte write a l'accès aux rule suites (`ACCES-RULE-SUITES:`), chaque suite consignée (push direct, création, réécriture, suppression du tag) est relue par l'agent via l'API et comparée (`relue=oui`) ; sinon elle est marquée `relue=non_relue` et le verdict le dit — jamais de vérification sur une valeur que l'agent a lui-même écrite"
    - "Tout commit (commit vide de preuve compris) et tout corps de PR citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
    - "M-4 : sous D-02/D-M13 et tous les pousseurs étant write ou admin, la preuve attendue pour les tags est la TRACE du contournement, PAS un refus. Tag de test `v0.0.0-preuve-41` : création acceptée sans contournement ; réécriture (force push vers un autre commit) acceptée AVEC une rule suite `bypass` ; suppression acceptée AVEC une rule suite `bypass` — la suppression est aussi le nettoyage"
    - "Le tag de test ne peut pas être pris pour une release : `plugin/conductor/scripts/check-plugin-update.sh` (l. 86-87) ne retient que `^v[0-9]+\\.[0-9]+\\.[0-9]+$` ; aucune release GitHub n'est créée pour lui ; il est supprimé dans le même geste humain"
    - "Préalable : `VERSION` d'`origin/main` taggée et publiée (`check-release-tag.sh --remote` ✓), pour que le hook `pre-push` éventuellement câblé chez Samuel ne masque pas le refus serveur par un refus client"
    - "Tout push (branche ou tag) est un geste humain ; l'agent ne fait que préparer les objets locaux (commit vide, tag annoté) et lire"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PUSH-DIRECT:`, `PUSH-DIRECT-MSG:`, `D03-VERDICT:`, `M4-OBJETS:`, `M4-CREATION:`, `M4-UPDATE:`, `M4-DELETION:`, `M4-NETTOYAGE:`, `M4-VERDICT:`, `M4-MSG:`"
  key_links:
    - "M-4 conditionne PROT-02 : la création libre du tag de release (plan 41-12) doit passer sans contournement"
---

<objective>
Prouver que `main` refuse un push direct, et mesurer la combinaison réelle des règles du ruleset de
tags, trace comprise, sans laisser aucun artefact.

Purpose: D-03 (PR obligatoire) côté serveur ; inconnu M-4 (D-07, D-M13) ; compatibilité du tag de
release (PROT-02).
Output: lignes `PUSH-DIRECT:`, `D03-VERDICT:` et `M4-*` consignées ; aucun tag ni commit de test restant.

Budget (point 9 du tour 1, justification explicite) : quatre tâches, dont la quatrième est un
checkpoint de décision CONDITIONNEL, sans exécution quand `D03-VERDICT` et `M4-VERDICT` sont
`CONFORME`. La scinder ferait porter la mesure et la décision par deux exécuteurs sans contexte
commun. Fichiers : un seul.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-08-SUMMARY.md
@scripts/hooks/pre-push

Sources lues le 2026-09-17 : available-rules-for-rulesets (restrict updates / deletions / block
force pushes, chaque règle documentée, leur combinaison déduite) ; `rest/repos/rule-suites`.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : préalables relus, objets de test préparés en local (commit vide, tag annoté)</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - plugin/conductor/scripts/check-plugin-update.sh (l. 81-90 : filtre du dernier tag publié)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`TRACE-II:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin --tags`. Vérifier : `TRACE-II:` consignée ;
`M3-VERDICT: CONFORME` ou `M3-DECISION:` d'option `fermer-et-poursuivre` / `consigner-et-poursuivre`
(sinon arrêt) ; aucune PR ouverte ; aucun tag distant `v0.0.0-preuve-41` ; la `VERSION` d'`origin/main` a son tag publié et sa
release GitHub (sinon arrêt : un hotfix non taggé ferait refuser le push par le hook `pre-push`
câblé chez Samuel, côté client, et masquerait le refus serveur).

Préparer sans toucher l'arbre de travail :
- `D` = `git commit-tree "origin/main^{tree}" -p origin/main` avec le message « PREUVE 41 — push
  direct sur main attendu refusé (D-03 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) » ;
- tag annoté local `v0.0.0-preuve-41` sur `origin/main`, message « PREUVE 41 — tag de test M-4, à
  supprimer » ;
- `U` = sha de `origin/main~1` (cible de la réécriture).
Consigner `M4-OBJETS: push_direct=<D> tag_cible_creation=<origin/main> tag_cible_reecriture=<U> main_avant=<origin/main>`.
Commit local sur B, message « docs(41-09): objets de test du push direct et du tag préparés ».
Imprimer les commandes de la Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); tr=$(awk "\$1==\"TRACE-II:\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); D=$(val M4-OBJETS: push_direct); U=$(val M4-OBJETS: tag_cible_reecriture); MA=$(val M4-OBJETS: main_avant); o=$(git rev-parse origin/main); te=$([ "$(git rev-parse "$D^{tree}")" = "$(git rev-parse "origin/main^{tree}")" ] && [ "$(git rev-parse "$D^")" = "$o" ] && echo oui || echo non); tl=$(git cat-file -t refs/tags/v0.0.0-preuve-41 2>/dev/null || echo absent); tc=$(git rev-parse "refs/tags/v0.0.0-preuve-41^{commit}" 2>/dev/null || echo absent); T=$(mktemp -d); git ls-remote --tags origin "refs/tags/v0.0.0-preuve-41*" > "$T/td"; td=$(awk "END{print NR}" "$T/td"); ue=$([ "$U" = "$(git rev-parse origin/main~1)" ] && echo oui || echo non); git show origin/main:VERSION > "$T/v"; vm=$(tr -d "v[:space:]" < "$T/v"); git ls-remote --tags origin "refs/tags/v$vm" > "$T/tg"; tg=$(awk "END{print NR}" "$T/tg"); rl=$(gh release view "v$vm" --repo picmakpro/vibeflow-os --json tagName --jq .tagName 2>/dev/null || echo absente); echo "prealables M-4 : identite=$u prs_ouvertes=$op trace_ii=$tr commit_vide_sur_main=$te tag_local=$tl tag_sur=$([ "$tc" = "$o" ] && echo main || echo autre) tag_distant=$td cible_reecriture_ok=$ue main_avant_ok=$([ "$MA" = "$o" ] && echo oui || echo non) version_main=$vm tag_version_publie=$tg release=$rl"; [ "$u" = samuel-neveugall ] && [ "$op" -eq 0 ] && [ "$tr" -eq 1 ] && [ "$te" = oui ] && [ "$tl" = tag ] && [ "$tc" = "$o" ] && [ "$td" -eq 0 ] && [ "$ue" = oui ] && [ "$MA" = "$o" ] && [ "$tg" -eq 1 ] && [ "$rl" = "v$vm" ]'</automated>
    <fails_when>exit non nul ; ou une PR ouverte ; ou `TRACE-II:` absente ; ou commit vide non fils d'`origin/main` ou d'arbre différent ; ou tag local absent, non annoté, ou hors `origin/main` ; ou tag déjà présent chez GitHub ; ou cible de réécriture fausse ; ou `main_avant` périmé ; ou `VERSION` d'`origin/main` sans tag publié ou sans release GitHub (arrêt : un hook `pre-push` câblé refuserait côté client et masquerait le refus serveur)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; mv=$(awk "\$1==\"M3-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); md=$(val M3-DECISION: option); N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); ps=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); echo "precondition M-3 : verdict=$mv decision=$md pr_p=$ps"; { [ "$mv" = CONFORME ] || [ "$md" = fermer-et-poursuivre ] || [ "$md" = consigner-et-poursuivre ]; } && [ "$ps" != OPEN ]'</automated>
    <fails_when>exit non nul ; ou ni `M3-VERDICT: CONFORME` ni décision de poursuite consignée ; ou PR-P encore ouverte (41-09 attend sa résolution, cf. Task 4 de 41-08)</fails_when>
  </verify>
  <acceptance_criteria>
    - commit vide `D` sur `origin/main`, tag annoté local `v0.0.0-preuve-41` sur `origin/main`, cible `U` = `origin/main~1`
    - aucune PR ouverte, aucun tag de test distant, `VERSION` de `main` taggée et publiée
  </acceptance_criteria>
  <done>Les objets de test sont prêts, inoffensifs, et aucun refus client ne peut masquer un refus serveur.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — push direct sur `main`, puis création, réécriture et suppression du tag de test ; lecture des traces</name>
  <action>Samuel, compte `samuel-neveugall`, tente un push direct du commit vide sur `main`, puis crée, réécrit et supprime le tag `v0.0.0-preuve-41`, en copiant chaque sortie ; le compte lecteur lit ensuite les rule suites.</action>
  <instructions>
Compte qui agit pour 1-4 : `samuel-neveugall` (témoin `gh api user --jq .login`). Jamais
`--no-verify`. SHA `<D>` et `<U>` imprimés par la Task 1.

1. Push direct : `git push origin <D>:refs/heads/main` → copier la sortie intégrale (refus attendu).
   Témoin : `git ls-remote origin refs/heads/main` → ne doit PAS être `<D>`.
   Si la sortie commence par `[pre-push]` (refus du hook client, pas du serveur) : s'arrêter et le
   signaler, sans contourner le hook.
   Si le push est ACCEPTÉ : s'arrêter là, le signaler (ne rien tenter pour l'annuler).
2. Création du tag : `git push origin refs/tags/v0.0.0-preuve-41` → copier la sortie.
3. Réécriture : `git push --force origin <U>:refs/tags/v0.0.0-preuve-41` → copier la sortie.
4. Suppression (et nettoyage) : `git push origin :refs/tags/v0.0.0-preuve-41` → copier la sortie.
   Témoin : `git ls-remote --tags origin 'refs/tags/v0.0.0-preuve-41*'` → vide.
   Si 3 ou 4 est REFUSÉ : copier le message ; le nettoyage passe alors par `picmakpro`
   (`gh api -X DELETE repos/picmakpro/vibeflow-os/git/refs/tags/v0.0.0-preuve-41` si son contournement
   le permet, sinon désactivation temporaire du ruleset de tags, suppression, réactivation — chaque
   geste copié).
5. Lecture des traces, par le compte lecteur de `ACCES-RULE-SUITES:` :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour&rule_suite_result=all" --jq '.[] | select(.ref == "refs/heads/main" or .ref == "refs/tags/v0.0.0-preuve-41") | {id, ref, actor_name, before_sha, after_sha, result}'`
   puis, pour chaque id listé : `gh api repos/picmakpro/vibeflow-os/rulesets/rule-suites/<id> --jq '{id, ref, result, evaluations: [.rule_evaluations[] | {rule_type, result}]}'`.
  </instructions>
  <verification>La Task 3 relit `main` distante, l'absence du tag chez GitHub, et recoupe les suites collées (ou les relit si le compte write y a accès).</verification>
  <resume-signal>Coller les sorties de 1 à 5 (témoins compris), puis écrire « push direct et tags joués ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : consignation et verdicts D-03 et M-4, nettoyage vérifié</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Supprimer le tag local `v0.0.0-preuve-41`. Consigner :
- `PUSH-DIRECT: sha=<D> refuse=<oui|non> provenance=<serveur|client_hook|indeterminee> msg_mentionne_pr=<oui|non> main_distante_inchangee=<oui|non> suite=<id|absente> suite_result=<fail|…|absente> relue=<oui|non_relue>` et `PUSH-DIRECT-MSG:` (sortie intégrale, une ligne) ;
- `D03-VERDICT: <CONFORME|ECART>` — CONFORME si refus, provenance `serveur`, PR exigée mentionnée, `main` inchangée ;
- `M4-CREATION: accepte=<oui|non> suite=<id|absente> suite_result=<pass|bypass|…|absente> relue=<oui|non_relue>` ;
- `M4-UPDATE: accepte=<oui|non> suite=<id|absente> suite_result=<> regles_en_echec=<liste|aucune> relue=<oui|non_relue>` ;
- `M4-DELETION: accepte=<oui|non> suite=<id|absente> suite_result=<> regles_en_echec=<liste|aucune> relue=<oui|non_relue>` ;
- `M4-NETTOYAGE: distant=<absent|present> local=<absent|present>` ;
- `M4-MSG:` (sorties 2-4, une ligne) ;
- `M4-VERDICT: <CONFORME|ECART>` — CONFORME si création acceptée sans suite `bypass`, réécriture ET
  suppression acceptées avec suite `bypass`, nettoyage complet ; ECART sinon.
`msg_mentionne_pr=oui` seulement si la sortie collée contient « pull request » (casse ignorée).
`provenance` suit la règle du frontmatter. Si le compte write a l'accès aux rule suites, relire soi-même
chaque id et écrire `relue=oui` ; sinon `relue=non_relue` (valeurs issues du collage du compte
lecteur). Commit local sur B, message « docs(41-09): push direct et ruleset de tags mesurés (D-03,
D-07 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; D=$(val M4-OBJETS: push_direct); T=$(mktemp -d); git ls-remote origin refs/heads/main > "$T/rm"; rm=$(awk "{print \$1}" "$T/rm"); [ -n "$rm" ] || { echo "ls-remote main vide"; exit 1; }; inch=$([ "$rm" != "$D" ] && echo oui || echo non); mp=$(awk "\$1==\"PUSH-DIRECT-MSG:\" { l=tolower(\$0); if (index(l, \"pull request\")) c++ } END{print c+0}" "$PH/41-PREUVES.md"); pc=$(awk "\$1==\"PUSH-DIRECT-MSG:\" { l=tolower(\$0); if (index(l, \"[pre-push]\")) r=\"client_hook\"; else if (index(l, \"remote:\")) r=\"serveur\"; else r=\"indeterminee\"; print r; exit }" "$PH/41-PREUVES.md"); rf=$(val PUSH-DIRECT: refuse); pv=$(val PUSH-DIRECT: provenance); mc=$(val PUSH-DIRECT: msg_mentionne_pr); mi=$(val PUSH-DIRECT: main_distante_inchangee); att=$([ "$rf" = oui ] && [ "$pc" = serveur ] && [ "$mp" -ge 1 ] && [ "$inch" = oui ] && echo CONFORME || echo ECART); v=$(awk "\$1==\"D03-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); echo "D-03 push direct : refuse=$rf provenance=$pv (recalculee=$pc) msg_mentionne_pr=$mc (recalcule=$([ "$mp" -ge 1 ] && echo oui || echo non)) main_distante_inchangee=$mi (recalcule=$inch) verdict attendu=$att consigne=$v"; [ -n "$D" ] && [ -n "$pc" ] && [ "$pv" = "$pc" ] && [ "$mc" = "$([ "$mp" -ge 1 ] && echo oui || echo non)" ] && [ "$mi" = "$inch" ] && [ "$v" = "$att" ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou provenance, mention de la PR ou état de `main` consignés différents du recalcul (erreur de consignation, corrigée ici) ; ou verdict consigné différent du verdict recalculé ; ou `D03-VERDICT: ECART` — push accepté, refus seulement côté client (`client_hook`) ou de provenance indéterminée, PR exigée non mentionnée, ou `main` changée : consigné tel quel, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; ca=$(val M4-CREATION: accepte); cr=$(val M4-CREATION: suite_result); ua=$(val M4-UPDATE: accepte); ur=$(val M4-UPDATE: suite_result); da=$(val M4-DELETION: accepte); dr=$(val M4-DELETION: suite_result); nd=$(val M4-NETTOYAGE: distant); nl=$(val M4-NETTOYAGE: local); T=$(mktemp -d); git ls-remote --tags origin "refs/tags/v0.0.0-preuve-41*" > "$T/rd"; rd=$(awk "END{print (NR==0 ? \"absent\" : \"present\")}" "$T/rd"); rl=$(git rev-parse -q --verify refs/tags/v0.0.0-preuve-41 > /dev/null && echo present || echo absent); if [ "$ca" = oui ] && [ "$cr" != bypass ] && [ "$ua" = oui ] && [ "$ur" = bypass ] && [ "$da" = oui ] && [ "$dr" = bypass ] && [ "$rd" = absent ] && [ "$rl" = absent ]; then att=CONFORME; else att=ECART; fi; v=$(awk "\$1==\"M4-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); msg=$(awk "\$1==\"M4-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "M-4 : creation=$ca/$cr reecriture=$ua/$ur suppression=$da/$dr nettoyage_consigne=$nd/$nl nettoyage_releve=$rd/$rl attendu=$att consigne=$v message=$msg"; [ "$nd" = "$rd" ] && [ "$nl" = "$rl" ] && [ "$v" = "$att" ] && [ "$msg" -eq 1 ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou nettoyage consigné différent du relevé (tag de test encore présent chez GitHub ou en local) ; ou verdict consigné différent du verdict recalculé ; ou message non capturé ; ou verdict `ECART` — consigné tel quel, arrêt sur la Task 4. Rappel : un refus de réécriture ou de suppression n'est PAS la preuve attendue sous D-M13 ; la preuve attendue est l'acceptation AVEC trace `bypass`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; acc=$(val ACCES-RULE-SUITES: samuel-neveugall); k=0; n=0; for L in PUSH-DIRECT M4-CREATION M4-UPDATE M4-DELETION; do id=$(val "$L:" suite); r=$(val "$L:" suite_result); rl=$(val "$L:" relue); n=$((n+1)); if [ "$acc" = ok ] && [ -n "$id" ] && [ "$id" != absente ]; then a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$id" --jq .result); echo "$L suite=$id consigne=$r api=$a relue=$rl"; { [ "$a" = "$r" ] && [ "$rl" = oui ]; } || k=$((k+1)); else echo "$L suite=$id consigne=$r relue=$rl (acces_write=$acc)"; [ "$rl" = non_relue ] || k=$((k+1)); fi; done; echo "relecture des traces : lignes=$n en_defaut=$k"; [ "$k" -eq 0 ] && [ "$n" -eq 4 ]'</automated>
    <fails_when>exit non nul ; ou, avec accès du compte write, un résultat consigné différent de la relecture API ou `relue` différent de `oui` ; ou, sans accès (ou suite absente), `relue` différent de `non_relue` (une valeur collée ne peut pas être dite relue par l'agent)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - push direct refusé, provenance `serveur` recalculée sur la sortie intégrale, message mentionnant la PR exigée, `main` inchangée, `D03-VERDICT: CONFORME`
    - chaque suite consignée relue via l'API (compte write avec accès) ou marquée `non_relue`
    - M-4 : création acceptée sans contournement ; réécriture et suppression acceptées avec suite `bypass` ; verdict recalculé = consigné
    - tag de test absent chez GitHub et en local
  </acceptance_criteria>
  <done>D-03 est prouvé côté serveur et le ruleset de tags est mesuré tel qu'il se comporte, trace comprise.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `D03-VERDICT: ECART` ou `M4-VERDICT: ECART` (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <decision>Le comportement mesuré contredit-il un arbitrage (D-03 pour le push direct, D-07/D-M13 pour les tags), et que faire avant la release ?</decision>
  <context>
    Lire `PUSH-DIRECT:`, `PUSH-DIRECT-MSG:`, `D03-VERDICT:`, `M4-*` dans `41-PREUVES.md`. Aucune PR de
    preuve n'est ouverte à ce stade. Si `PUSH-DIRECT: refuse=non`, le commit de preuve `D` (vide, arbre
    identique à `main`) est entré sur `main` et la réponse DOIT AUSSI fixer son sort, sans conduite
    implicite : `REVERT PAR PR` — branche `fix/phase-41-retrait-commit-preuve` depuis `origin/main`,
    `git revert --no-edit <D>` (si git refuse faute de changement, commit explicite
    `git commit --allow-empty -m "revert: retrait du commit de preuve <D> (Phase 41)"`), PR publiée
    et mergée par Samuel sans contournement — ou `CONSERVATION DÉCLARÉE` — le commit reste dans
    l'historique de `main`, déclaré tel quel dans `41-PREUVES.md` et ADR-072 § Mesures, sans autre
    geste. Sans push accepté, ce second choix est `sans_objet`. Le planificateur ne tranche pas : un écart touche la forme posée
    d'un arbitrage.
  </context>
  <options>
    <option id="arret-et-cadrage">
      <name>ARRÊT ET CADRAGE</name>
      <pros>Rien n'est modifié sans arbitrage ; l'écart est consigné avec sa mesure.</pros>
      <cons>La suite (41-10 à 41-13) attend.</cons>
    </option>
    <option id="accepter-et-documenter">
      <name>ACCEPTER ET DOCUMENTER</name>
      <pros>La release avance ; ADR-072 écrit le comportement réel mesuré.</pros>
      <cons>Une forme d'arbitrage n'est pas tenue telle qu'écrite ; exige un arbitrage explicite, daté et attribué.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE DU RULESET CONCERNÉ</name>
      <pros>Rien d'imposé à tort (`enforcement=disabled`).</pros>
      <cons>La protection correspondante disparaît jusqu'au cadrage.</cons>
    </option>
  </options>
  <resume-signal>Répondre « non déclenchée », « ARRÊT ET CADRAGE », « ACCEPTER ET DOCUMENTER » ou « RETOUR ARRIÈRE DU RULESET CONCERNÉ », ET, si le push direct a été accepté, « REVERT PAR PR » ou « CONSERVATION DÉCLARÉE » pour le commit de preuve. Après la réponse, l'exécutant consigne `D03-M4-DECISION: option=<id> sort_commit_preuve=<revert-par-pr|conservation-declaree|sans_objet> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint>` — jamais inféré, jamais une date recopiée d'ailleurs — et le commite sur B avec le message « docs(41-09): D03-M4-DECISION consignée — arbitrage Samuel, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `D03-M4-DECISION` sans ce canal et cette date rougit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poste de Samuel → refs `main` et `v*` | pushs directs d'un compte write ayant le contournement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-34 | Tampering | push direct accepté | high | mitigate | commit vide (aucun contenu) ; témoin `ls-remote` ; arrêt sur décision si accepté |
| T-41-35 | Spoofing | tag de test pris pour une release | medium | mitigate | nom hors motif `vX.Y.Z` du vérificateur de mise à jour ; aucune release ; suppression dans le même geste, relue |
| T-41-36 | Repudiation | refus client (hook) pris pour un refus serveur | medium | mitigate | `VERSION` de `main` taggée et publiée exigée avant l'essai ; `--no-verify` interdit ; message serveur exigé (mention de la PR) |
| T-41-37 | Repudiation | combinaison des règles de tags supposée | medium | mitigate | création, réécriture, suppression jouées ; résultat de suite consigné par geste ; verdict recalculé |
</threat_model>

<verification>
Push direct refusé et relu ; ruleset de tags mesuré par ses trois gestes et leurs traces ; aucun
artefact de test restant.
</verification>

<success_criteria>
D-03 prouvé côté serveur ; M-4 consigné ; la création libre du futur tag de release est établie.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-09-SUMMARY.md` when done — committé sur la branche B.
</output>

---
phase: 31-manifeste-d-install-dry-run-issue-20
plan: 04
type: execute
wave: 3
depends_on: ["31-02", "31-03"]
files_modified:
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-manifest.sh
autonomous: true
requirements: [MANI-02, QUAL-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`--dry-run` rend le plan de pose fichier par fichier sur stdout, sur le MÊME chemin de code que la pose (D-31-01, MANI-02)"
    - "`--dry-run` est parsé dans le même pré-parse que `--scope`, donc valide avant cmd=$1 (D-31-06)"
    - "`--dry-run` est accepté sur `install` et `update` seulement ; sur `uninstall`, `rollback`, `status`, `sync` il produit une erreur explicite et exit 1 (D-31-06)"
    - "Un dry-run n'écrit RIEN : ni artefact, ni registre, ni .gitignore, ni backup, ni manifeste, ni settings*.json (D-31-06)"
    - "Le plan couvre le merge de hooks par délégation à `merge-hooks.sh plan`, jamais par une simulation côté engine (D-31-04 régime B)"
    - "Les sorties de régime A sont annoncées exactement, le sous-processus n'étant pas appelé (D-31-04)"
    - "Les effets de régime C sont annoncés comme non énumérés, honnêtes sur leur propre limite (D-31-04)"
    - "Le plan affiche les chemins préfixés TARGET_ROOT ; le manifeste, lui, reste relatif (D-31-05 — affichage et stockage sont deux contrats séparés)"
  artifacts:
    - "plugin/_internal/vibeflow-update.sh : flag `--dry-run`, variable VF_DRY_RUN, fonction vf_dry_run, branche plan de vf_declare_write"
    - "plugin/_internal/tests/test-manifest.sh : les deux preuves distinctes de MANI-02"
  key_links:
    - "pré-parse 43-59 -> VF_DRY_RUN -> vf_declare_write : un seul point de bascule pour tous les sites migrés en 31-03"
    - "merge_module_hooks (369-412) -> merge-hooks.sh plan (livré en 31-02)"
    - "gardes de régime A/C : côté SOURCE + présence PLANIFIÉE de la destination, jamais l'état disque courant de la destination"
  prohibitions:
    - "`--dry-run` n'est JAMAIS accepté-puis-ignoré sur un verbe destructif — le refus est bruyant, exit 1"
    - "Aucune réimplémentation de la logique de merge de hooks côté engine"
    - "Aucun sous-processus de régime A ou C n'est invoqué en dry-run"
    - "Le test d'égalité de MANI-02 ne comporte AUCUNE liste d'exceptions négociée (D-31-04)"
---

<objective>
Le flag que l'issue #20 demande. `--dry-run` rend le plan de pose sans écrire, en empruntant les
helpers de 31-03 — donc sans qu'aucune dérive plan/pose ne soit possible, et sans le « dry-run en
chemin de code séparé » que `REQUIREMENTS.md:959` interdit.

Purpose: MANI-02, prouvé par deux tests aux prétentions distinctes (D-31-04) — une égalité totale
sur un fixture sans régime C, et une présence de lignes d'annonce sur un fixture qui en déclenche.

Output: le flag, sa surface de commandes bornée avec refus bruyant, les trois régimes d'écriture
indirecte câblés, et les deux preuves.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-CONTEXT.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-RESEARCH.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-02-SUMMARY.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : surface du flag `--dry-run` et refus bruyant (D-31-06)</name>
  <files>plugin/_internal/vibeflow-update.sh</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh:41-71 (le pré-parse `--scope`, la restauration des positionnels avec la garde bash 3.2 tableau vide, la validation stricte)
    - plugin/_internal/vibeflow-update.sh:1-25 (bloc d'usage en commentaires — imprimé par le cas par défaut 1036-1039 et par l'appel sans argument 947-950)
    - plugin/_internal/vibeflow-update.sh:952-1040 (le `case` sur `cmd` : les 4 verbes où `--dry-run` doit échouer)
    - 31-CONTEXT.md §3 D-31-06 (surface exacte, motif du refus bruyant sur `uninstall`)
    - 31-PATTERNS.md « Flag pre-parse before cmd=$1 »
  </read_first>
  <action>
Initialiser `VF_DRY_RUN="0"` avant la boucle de pré-parse (à côté de `VF_SCOPE`, ligne 39).

Étendre la boucle `while`/`case` des lignes 43-59 d'une branche `--dry-run) VF_DRY_RUN="1"; shift ;;`.
Aucune forme `--dry-run=<valeur>` : c'est un booléen. La branche `*)` qui collecte les positionnels
reste inchangée, donc la restauration `set -- "${_positional[@]}"` et sa garde tableau vide sont
intactes.

Ajouter la fonction prédicat `vf_dry_run()` (`[ "$VF_DRY_RUN" = "1" ]`) juste après `log`/`err`,
pour que tous les sites l'appellent au lieu de tester la variable à la main.

Après le `case` de validation du scope (68-71), ajouter la validation de surface : si
`VF_DRY_RUN` vaut `1` et que `$1` (le futur `cmd`) n'est ni `install` ni `update`, appeler `err`
avec un message qui nomme le verbe reçu et les deux verbes acceptés. Le message DOIT dire
pourquoi : un `--dry-run` accepté puis ignoré sur `uninstall` ferait croire à une
prévisualisation et supprimerait. Ce contrôle vit AVANT `cmd="$1"` (ligne 952), donc avant que
`require_cache` ou quoi que ce soit d'autre ne tourne. Cas sans argument (`$#` à 0) : conserver le
comportement actuel d'impression de l'usage, exit 0 — un `--dry-run` seul n'est pas une commande.

Mettre à jour le bloc d'usage en tête de fichier (lignes 4-14) : ajouter `[--dry-run]` sur les
deux lignes `install` et `update`, plus une ligne de la section Scope disant que `--dry-run`
n'écrit rien et rend le plan sur stdout, et qu'il est refusé sur les autres verbes.

Ne rien brancher d'autre à cette tâche : le rendu du plan est la tâche 2.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/_internal/vibeflow-update.sh` sort 0.
    - Accepté : `VIBEFLOW_CACHE=<cache> bash plugin/_internal/vibeflow-update.sh --dry-run install software-architecture` ne sort pas 1 pour cause de flag.
    - Refusé, exit 1, sur les 4 verbes : pour `V` dans `uninstall x`, `rollback x`, `status`, `sync`, la commande `bash plugin/_internal/vibeflow-update.sh --dry-run $V` sort `1` et sa sortie stderr contient à la fois le verbe reçu et la mention `install` et `update`.
    - Le flag est parsé avant `cmd` : `--dry-run` placé APRÈS le verbe (`install --dry-run software-architecture`) est également reconnu, parce que la boucle de pré-parse balaie toute la ligne de commande.
    - L'usage est à jour : `grep -c 'dry-run' plugin/_internal/vibeflow-update.sh` compte au moins `3` occurrences dans les 30 premières lignes du fichier (`head -30 ... | grep -c 'dry-run'` rend au moins `2`).
    - `bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh` sortent 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/_internal/vibeflow-update.sh && for v in "uninstall x" "rollback x" "status" "sync"; do bash plugin/_internal/vibeflow-update.sh --dry-run $v >/dev/null 2>&1 && { echo "ACCEPTE A TORT: $v"; exit 1; }; done; bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>Le flag existe, il est borné à `install` et `update`, et son refus sur les 4 autres verbes est explicite et non silencieux.</done>
  <reversibility rating="reversible">Un flag additif et une garde. Sans le flag, l'engine se comporte exactement comme avant.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : rendu du plan et neutralisation des écritures — les trois régimes</name>
  <files>plugin/_internal/vibeflow-update.sh</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh:898-918 (`show_status` — la SEULE sortie stdout actuelle de l'engine, le précédent que D-31-05 invoque)
    - plugin/_internal/vibeflow-update.sh:30-31 (`log`/`err` sur stderr — le plan ne passe PAS par là)
    - plugin/_internal/vibeflow-update.sh:682-689, 697-704, 720-726 (gardes des régimes A et C : elles testent la présence de la destination `$TARGET_ROOT/scripts/<script>.sh`)
    - plugin/_internal/vibeflow-update.sh:481-491 (`seed_module_registres` — même forme de garde côté destination)
    - plugin/_internal/vibeflow-update.sh:369-412 (`merge_module_hooks` — le point de délégation du régime B)
    - plugin/_internal/vibeflow-update.sh:115-132 (registre), 165-173 (gitignore), 768-787 (backup) — les trois écritures qu'un dry-run ne doit pas faire non plus
    - 31-CONTEXT.md §3 D-31-04 (les trois régimes), D-31-05 (format et canal), D-31-06 (« n'écrit rien du tout »)
    - 31-02-SUMMARY.md (contrat de sortie exact du mode `plan` de merge-hooks.sh)
  </read_first>
  <action>
**Rendu.** Dans `vf_declare_write <verbe> <chemin> [note]` (couture livrée en 31-03), ajouter en
tête la branche de mode : si `vf_dry_run`, émettre sur **stdout** une ligne au format de l'issue #20
(D-31-05) puis retourner 0 sans rien consigner ni écrire —

`[plan] <verbe> <chemin>` suivi, s'il y a une note, de deux espaces et de la note ; et pour le
verbe `+` sans note, du suffixe `  (<module> <version>)` où module et version viennent du contexte
courant (`VF_MANIFEST_MOD` posé par `vf_manifest_reset`, et la version résolue par
`module_version_available`). Le chemin affiché est celui reçu, préfixe `TARGET_ROOT` inclus — c'est
ce qui rend `./.claude/scripts/…` en scope project et `$HOME/.claude/scripts/…` en scope user, où
un chemin nu serait ambigu. Pas de `[ok]`, pas de niveau : l'engine n'en a aucun aujourd'hui.

**Neutralisation.** Chaque site doit sauter son écriture quand `vf_dry_run` est vrai, APRÈS avoir
appelé `vf_declare_write` :
- `vf_place_file` et `vf_place_tree` : déclarer puis retourner 0 sans `mkdir`, `cp` ni `chmod`.
  Pour `vf_place_tree`, l'expansion `find -type f` porte alors sur la SOURCE (elle existe), ce qui
  donne les mêmes lignes qu'une pose réelle — c'est ce qui rend l'égalité de la tâche 3 vraie.
- `copy_engine_lib` : déclarer `$dest` puis retourner 0 avant le `cp` tmp.
- `backup_module` : déclarer le `$bdir` puis retourner 0 avant le `mkdir -p` (D-31-06 : pas de
  backup en dry-run).
- `merge_module_hooks` : déclarer le backup de settings, puis appeler `merge-hooks.sh` en mode
  `plan` au lieu de `merge`, avec exactement les mêmes flags (`--settings`, `--scripts-prefix`,
  et `--settings-local` selon le même `case "$VF_SCOPE"` qu'aujourd'hui). La sortie stdout du
  sous-processus est laissée telle quelle, elle est déjà au format `[plan] ~ …`. Ne PAS faire le
  `cp` de backup de settings. Le code de retour reste propagé comme aujourd'hui (VG-3).
- `gitignore_add_one` : déclarer `~ ./.gitignore` avec la note du chemin ajouté, puis retourner 0
  sans écrire (scope `local` seulement, la garde de `gitignore_add_paths` ligne 178 est inchangée).
- `mark_installed` : déclarer `~ $INSTALLED_REGISTRY` avec la note `<mod>=<version>`, puis
  retourner 0.
- `vf_manifest_flush` : déclarer `+ $(vf_manifest_path "$VF_MANIFEST_MOD")`, puis retourner 0 sans
  trier ni écrire, et supprimer l'accumulateur temporaire.
- `cleanup_retired_modules` : la déclaration `-` est déjà en place (31-03) ; ajouter le saut du
  `rm -rf` et du `mark_uninstalled`.
- `vf_manifest_reset` : ne pas créer d'accumulateur en dry-run, mais mémoriser `VF_MANIFEST_MOD`
  (le rendu du plan en a besoin pour le suffixe module/version).

**Régime A — sortie prédite exactement, sous-processus NON appelé.** Trois sites : `build-gsd-index.sh`
(682-689), `build-gsd-capabilities-index.sh` (697-704), `generate-agent-commands.sh` (257-269).
En dry-run, ne PAS lancer le sous-processus ; émettre la ligne `+` de son chemin de sortie exact.

**Piège de garde, à corriger sur les 5 sites concernés (régimes A et C).** Les gardes actuelles
testent la présence de la destination — `[ -f "$TARGET_ROOT/scripts/build-gsd-index.sh" ]` (682),
`[ -f "$TARGET_ROOT/scripts/build-gsd-capabilities-index.sh" ]` (697),
`[ -f "$TARGET_ROOT/scripts/ensure-design-deps.sh" ]` (720), `[ -f "$seeder" ]` (485), et la cascade
de `find_command_generator` (250-255) qui regarde `$TARGET_ROOT/scripts/` d'abord. Or en dry-run
sur un lab vierge, `copy_module_scripts` n'a rien posé : ces gardes seraient FAUSSES alors qu'elles
seront VRAIES à la pose réelle. Le plan tairait donc des écritures que la pose fait — exactement le
mensonge que MANI-02 interdit. Correction : quand `vf_dry_run` est vrai, la garde teste la présence
côté SOURCE (`$module_dir/scripts/<script>.sh`, déjà la première moitié de chaque condition) et
considère la destination comme PLANIFIÉE, sans regarder le disque. Écrire ce motif en commentaire
sur chacun des 5 sites — c'est le piège le plus coûteux de la phase, il ne doit pas se re-perdre.

**Régime C — effet annoncé, non énuméré.** `seed-registres.sh`, `inject-mcp-tools.sh`,
`ensure-design-deps.sh` : ne PAS lancer le sous-processus ; la ligne `~` avec la note posée en
31-03 (`effet de <script>, contenu non énuméré`) est le rendu attendu.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/_internal/vibeflow-update.sh` sort 0.
    - Le plan sort sur stdout et rien d'autre : sur un lab vierge, `bash plugin/_internal/vibeflow-update.sh --dry-run install software-architecture 2>/dev/null | grep -c '^\[plan\] '` est strictement supérieur à `10`, et la même commande avec `1>/dev/null` ne produit aucune ligne commençant par `[plan] `.
    - Un dry-run n'écrit rien : dans un `LAB` vierge, l'empreinte `find "$LAB" | LC_ALL=C sort` est identique avant et après la commande.
    - Aucun sous-processus de régime A/C n'est lancé : après un dry-run, `$LAB/.claude/agents/<mod>-references/gsd-skills-index.md` est absent et `$LAB/.claude/memory` est absent.
    - Régime B délégué : la sortie du dry-run d'un module à hooks contient une ligne `[plan] ~ ` portant le chemin de `settings.json`, et `sed -n '/^merge_module_hooks()/,/^}/p' plugin/_internal/vibeflow-update.sh | grep -c 'plan'` rend au moins `1` (l'appel au mode `plan`).
    - Le piège de garde est commenté sur les 5 sites : `grep -c 'garde côté SOURCE' plugin/_internal/vibeflow-update.sh` rend `5`.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh` sortent 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/_internal/vibeflow-update.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>Le plan sort sur stdout au format de l'issue #20, aucune écriture n'a lieu, aucun sous-processus n'est invoqué, et le merge de hooks est prévisualisé par délégation.</done>
</task>

<task type="auto">
  <name>Tâche 3 : les DEUX preuves de MANI-02, chacune avec sa prétention (D-31-04)</name>
  <files>plugin/_internal/tests/test-manifest.sh</files>
  <read_first>
    - plugin/_internal/tests/test-manifest.sh (état livré par 31-03, cas T1..T9)
    - plugin/_internal/vibeflow-update.sh:383-386 (`date` du backup de settings) et 768-772 (`date` de `backup_module`) — les deux seules sources non déterministes de la pose
    - 31-CONTEXT.md §3 D-31-04 (« deux tests, deux prétentions distinctes, aucune des deux diluée »)
    - 31-RESEARCH.md « Pitfall 3 : Non-deterministic dry-run/real diff (timestamps) »
  </read_first>
  <behavior>
    - T10 (ÉGALITÉ TOTALE, fixture sans régime C) : sur `software-architecture` — `SKILL.md` +
      `rules/` + `references/` + `scripts/` + `hooks/hooks.json`, et aucun `AGENT.md`, aucun
      `agents/`, aucun `seed-registres.sh`, aucun `ensure-design-deps.sh` — l'ensemble des chemins
      annoncés par `--dry-run install` est EXACTEMENT l'ensemble des chemins réellement créés ou
      modifiés par le même `install` réel dans un lab jumeau. Aucune liste d'exceptions.
    - T11 (arbre inchangé) : l'empreinte `find "$LAB" | LC_ALL=C sort` est identique avant et après
      le `--dry-run` — la version machine de « un dry-run n'écrit rien du tout » (D-31-06).
    - T12 (RÉGIME C, fixture qui en déclenche) : sur `consolidator` (porte
      `scripts/seed-registres.sh`), la sortie du `--dry-run` contient une ligne d'annonce
      mentionnant `seed-registres.sh` et le mot marqueur de non-énumération. Cette assertion porte
      sur la PRÉSENCE de la ligne, pas sur une égalité — c'est la prétention distincte de D-31-04.
    - T13 (asymétrie plan/manifeste sur régime C) : après un install RÉEL de `consolidator`, le
      manifeste ne contient aucune ligne commençant par `memory/` — le plan l'annonce, le manifeste
      ne le possède pas (D-31-03).
    - T14 (refus bruyant) : `--dry-run uninstall <mod>` sort 1 et n'a rien supprimé.
  </behavior>
  <action>
Ajouter `T10` à `T14` à `plugin/_internal/tests/test-manifest.sh`, documentés en en-tête selon la
convention `# T<n> — …`.

**Déterminisme de T10, sans compromis.** Les deux seules sources de non-déterminisme sont les
appels `date` des lignes 383-386 (backup de `settings.json`, conditionné à `[ -f "$TARGET_ROOT/settings.json" ]`)
et 768-772 (`backup_module`, conditionné à une install préexistante). T10 tourne sur une install
FRAÎCHE dans un lab vierge : ni l'une ni l'autre condition n'est vraie, donc AUCUN chemin
horodaté n'apparaît. Le rendre explicite plutôt que l'espérer : T10 assère d'abord que
`$LAB_REEL/.claude/.backups` n'existe pas après l'install réelle. Si cette assertion tombe un jour,
le test rougit en nommant la vraie cause au lieu de flaker sur une comparaison de chaînes.

**Forme de T10.** Deux labs temporaires jumeaux, `LAB_PLAN` et `LAB_REEL`, avec le même cache
préparé par `prepare_module`. Dans `LAB_PLAN`, exécuter le dry-run et extraire l'ensemble des
chemins annoncés : les lignes `^\[plan\] ` de stdout, chemin = 3e champ, normalisé en relatif au
lab. Dans `LAB_REEL`, exécuter l'install réelle et extraire l'ensemble des chemins créés par
`find . -type f` relatif au lab, en incluant `settings.json`, `settings.local.json`, le registre et
le manifeste — parce que le plan les annonce tous (la liste close de D-31-03 gouverne le CONTENU du
manifeste, jamais la sortie du plan). Trier les deux ensembles en `LC_ALL=C` et comparer par
`diff`. Sortie de `diff` vide exigée. Aucun `grep -v` d'exception dans le corps du test : si les
deux ensembles diffèrent, c'est un défaut du dry-run, pas du test.

Cas particulier à traiter dans T10, pas à contourner : la ligne de `merge-hooks.sh plan` porte le
chemin de la cible settings, tandis que l'install réelle crée `settings.json` ET
`settings.local.json` — les deux sont annoncés par le mode `plan` (contrat 31-02), donc l'égalité
tient. Si elle ne tient pas, c'est le mode `plan` qui ment : le corriger, jamais élargir le test.

`T11` prend l'empreinte de `$LAB_PLAN` complet (pas seulement `.claude`) avant et après, ce qui
couvre aussi `./.gitignore` et `docs/` — les deux écritures hors `TARGET_ROOT`.

Mettre à jour le compteur de cas de l'en-tête.
  </action>
  <acceptance_criteria>
    - `bash plugin/_internal/tests/test-manifest.sh` sort 0 et affiche `T10`, `T11`, `T12`, `T13`, `T14`.
    - T10 est une égalité d'ensembles totale : `grep -c 'grep -v' plugin/_internal/tests/test-manifest.sh` ne compte aucune occurrence à l'intérieur du bloc T10.
    - Le fixture de T10 ne déclenche aucun régime C — vérifiable : `ls plugin/software-architecture/AGENT.md plugin/software-architecture/agents plugin/software-architecture/scripts/seed-registres.sh plugin/software-architecture/scripts/ensure-design-deps.sh 2>/dev/null | wc -l` rend `0`.
    - Discriminance de T10 (mutation rouge n° 1 de la phase) : en supprimant l'appel `vf_declare_write` d'UN site de régime A (par exemple la ligne `commands/<mod>.md` de `generate_agent_command_for`), T10 rougit en nommant le chemin absent du plan. Consigner l'assertion, l'attendu et l'obtenu dans le SUMMARY.
    - Discriminance de T11 : en retirant le `return 0` de mode plan de `vf_place_file`, T11 rougit sur l'empreinte modifiée.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh` sortent 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-manifest.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>MANI-02 est prouvé par deux tests aux prétentions séparées, dont une égalité totale sans exception négociée, et par deux mutations rouges tracées.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan affiché -> décision humaine (ADR-031) | l'utilisateur donne son feu vert sur la base du plan ; un plan incomplet est une autorisation obtenue par omission |
| ligne de commande -> verbe destructif | `--dry-run` posé sur `uninstall` est le pire échec possible de la phase |
| engine -> sous-processus | 7 sous-processus écrivent hors du contrôle direct du moteur |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Elevation of Privilege | surface du flag (`case` 952-1040) | high | mitigate | refus explicite exit 1 sur `uninstall`/`rollback`/`status`/`sync`, contrôlé AVANT `cmd="$1"` (D-31-06) ; T14 et le critère d'acceptation de la tâche 1 le prouvent sur les 4 verbes |
| T-31-08 | Tampering | mode plan de tous les sites d'écriture | high | mitigate | chaque site retourne 0 avant son écriture ; T11 compare l'empreinte `find` du lab entier avant/après, ce qui couvre aussi `./.gitignore` et `docs/` |
| T-31-14 | Spoofing | plan silencieux sur un site (garde de régime A/C fausse en dry-run) | high | mitigate | gardes basculées côté SOURCE en dry-run, commentées sur les 5 sites ; T10 rougit sur toute omission puisqu'il exige l'égalité totale |
| T-31-09 | Spoofing | plan de merge de hooks divergent du merge réel | medium | mitigate | délégation à `merge-hooks.sh plan` (31-02), jamais une simulation côté engine ; T10 échouerait si les deux cibles settings n'étaient pas annoncées |
| T-31-15 | Repudiation | test d'égalité rendu vert par une liste d'exceptions | medium | mitigate | interdiction explicite d'un filtre ad hoc dans le bloc T10, contrôlée par critère d'acceptation |
| T-31-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par cette phase (31-RESEARCH.md § Package Legitimacy Audit : N/A) |
</threat_model>

<verification>
1. `bash -n plugin/_internal/vibeflow-update.sh`
2. `bash plugin/_internal/tests/test-manifest.sh`
3. `bash plugin/_internal/tests/test-vibeflow-update.sh`
4. `bash plugin/_internal/tests/test-merge-hooks.sh`
5. `bash scripts/check-machine-paths.sh`
6. Preuve APRÈS commit :
   `T=$(mktemp -d); git archive HEAD | tar -x -C "$T"; for s in test-manifest test-vibeflow-update test-merge-hooks; do bash "$T/plugin/_internal/tests/$s.sh" || echo "FAIL $s"; done; rm -rf "$T"`
</verification>

<success_criteria>
- `--dry-run` existe sur `install` et `update`, et échoue bruyamment ailleurs.
- Le plan sort sur stdout, au format de l'issue #20, chemins préfixés `TARGET_ROOT`.
- Un dry-run laisse le lab bit pour bit identique.
- Le merge de hooks est prévisualisé par `merge-hooks.sh plan`, jamais simulé côté engine.
- Les deux preuves de MANI-02 existent, distinctes, dont une égalité totale sans exception.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribution de ce plan)

**Nouveau flag CLI**
- `vibeflow-update.sh [--scope …] [--dry-run] install|update …` — refusé sur `uninstall`, `rollback`, `status`, `sync`

**Nouvelles fonctions / variables**
- `VF_DRY_RUN` (0/1), `vf_dry_run()` (prédicat)
- branche « mode plan » de `vf_declare_write` (le seul point de bascule)

**Nouveau contrat de sortie (stdout)**
- `[plan] + <chemin préfixé TARGET_ROOT>  (<module> <version>)`
- `[plan] ~ <chemin>  <effet>` (régimes B et C, registre, gitignore)
- `[plan] - <chemin>` (nettoyage des modules retirés ; la convergence MANI-03 l'emprunte en 31-05)

**Nouveaux cas de suite**
- `T10` (égalité totale), `T11` (arbre inchangé), `T12` (annonce régime C), `T13` (asymétrie
  plan/manifeste), `T14` (refus bruyant) dans `plugin/_internal/tests/test-manifest.sh`
</artifacts_produced>

<output>
Créer `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-04-SUMMARY.md` à la fin,
avec la trace des deux mutations rouges de la tâche 3 (assertion, attendu, obtenu).
</output>

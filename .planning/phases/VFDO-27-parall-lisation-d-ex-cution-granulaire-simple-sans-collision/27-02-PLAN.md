---
phase: 27-parall-lisation-d-ex-cution-granulaire-simple-sans-collision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/references/team-kernel.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PAEX-01, PAEX-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un lecteur de team-kernel.md apprend que le parallélisme intra-étape est désactivé par un drapeau default-off, pas définitivement perdu (D-04)"
    - "Un lecteur de team-kernel.md peut nommer le chemin exact qui le rallume : la capability claude_orchestration, dont le gate n°4 lit nested && background, jamais backgroundDispatch (D-04)"
    - "Le ROADMAP §Phase 27 ne porte plus d'avertissement de divergence de comptage : il porte le résultat re-dérivé, avec profondeur, motif et version du moteur (D-13)"
    - "team-kernel.md porte un renvoi d'une ligne vers le champ stages de dag.sh ready (posé par 27-01 dans la même vague) — les 4 managers non-dev (business, content, growth, test) qui ne lisent jamais mission-flow.md apprennent que la capacité existe, sans que sa doctrine détaillée soit dupliquée ici (M6)"
  artifacts:
    - path: "plugin/conductor/references/team-kernel.md"
      provides: "Doctrine corrigée de l'étage de parallélisme réellement effectif, plus un renvoi d'une ligne vers le champ stages de dag.sh"
      contains: "éteint par défaut"
    - path: ".planning/ROADMAP.md"
      provides: "Résultat re-dérivé des deux comptages, avec sa limite d'ancrage"
      contains: "gsd-core/VERSION = 1.9.1"
  key_links:
    - from: "plugin/conductor/references/team-kernel.md"
      to: "la capability amont claude_orchestration"
      via: "la doctrine corrigée nomme le mécanisme de restauration et la condition de son gate"
      pattern: "claude_orchestration"
    - from: ".planning/ROADMAP.md"
      to: "ADR-069"
      via: "le résultat re-dérivé confirme les deux chiffres gravés par ADR-069 sans les corriger"
      pattern: "ADR-069"
---

<objective>
Corriger les deux affirmations écrites que cette phase a mesurées fausses ou instables, et
seulement celles-là.

Purpose: une doctrine livrée qui déclare une capacité **perdue** là où elle est **éteinte** induit en
erreur chaque lecteur du team-kernel — c'est la première chose que la Phase 27 doit fermer, avant
d'activer quoi que ce soit (D-04). En parallèle, l'avertissement de divergence de comptage inscrit au
ROADMAP §Phase 27 est désormais tranché : la re-dérivation est close, le ROADMAP doit porter le
résultat et non l'alerte (D-13).

Output: `team-kernel.md:64-68` corrigé ; le bloc d'avertissement du ROADMAP (l. 1898-1901) remplacé
par le résultat re-dérivé, verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/VFDO-27-parall-lisation-d-ex-cution-granulaire-simple-sans-collision/27-CONTEXT.md
@.planning/phases/VFDO-27-parall-lisation-d-ex-cution-granulaire-simple-sans-collision/27-PATTERNS.md
@plugin/conductor/references/team-kernel.md
</context>

## Disjonction d'écriture déclarée

Ce plan écrit **exactement deux fichiers** : `plugin/conductor/references/team-kernel.md` et
`.planning/ROADMAP.md`. Aucun autre plan de la Phase 27 ne les touche :

- `team-kernel.md` — la recherche (27-RESEARCH.md §Livrable 1, note de collision) a explicitement
  écarté d'y documenter la **doctrine détaillée** de la nouvelle capacité de `dag.sh` : le plan
  `27-01` la documente dans `mission-flow.md`, déjà le renvoi établi pour la doctrine d'usage
  détaillée de `dag.sh` (`team-kernel.md:140-143`). `27-02` y ajoute seulement un **renvoi d'une
  ligne** vers ce champ (M6, correction de revue) — sans quoi les 4 managers non-dev ne
  l'apprendraient jamais — ce qui ne réouvre aucune collision : `27-01` ne touche toujours jamais
  `team-kernel.md`, seul `27-02` l'écrit. C'est ce qui rend `27-01` et `27-02` réellement disjoints en
  écriture, pas seulement en intention.
- `.planning/ROADMAP.md` — seul plan de la phase à y écrire. Aucun autre plan n'écrit sous
  `.planning/` hors de son propre dossier de phase, sauf `27-05` qui écrit `.planning/config.json`
  (fichier distinct).

Ce plan est donc **dispatchable en parallèle** de `27-01` et `27-03` (vague 1).

## Texte de remplacement du ROADMAP — verbatim, à recopier tel quel

> Le bloc à remplacer (lignes 1898-1901 de `.planning/ROADMAP.md`), à vérifier présent avant
> édition :

```
> **Chiffres divergents, à ne pas recopier.** La recherche obtient **6** fichiers mentionnant
> `workstream` et **73** codant `.planning/` en dur, là où la Phase 24 (ADR-069) grave **7/91** et
> **45**. L'écart sur 45 → 73 est trop large pour du bruit. À re-dériver avec un critère nommé avant
> toute citation ; ADR-069 fait foi jusque-là.
```

> Le contenu de remplacement (résultat re-dérivé par un nœud parallèle de cette même mission, moteur
> gsd-core 1.9.1) :

```
> **Comptages re-dérivés, méthode incluse.** La divergence relevée le 2026-08-05 est close ; les
> deux chiffres d'ADR-069 tiennent, chacun sur son ensemble nommé.
>
> - `workstream` : **7/91 tient** (critère K2, `~/.claude/gsd-core/workflows/*.md` profondeur 1,
>   non récursif). Le 6 obtenu par la recherche du 2026-08-05 venait d'un motif récursif K1
>   différent (`--ws ` espace littéral, qui ne matchait aucun fichier) — les deux mesures étaient
>   justes sur des ensembles différents, pas contradictoires.
> - `.planning/` en dur : **45 tient** (motif étroit `.planning/(ROADMAP.md|STATE.md|phases)`, les 3
>   seuls artefacts que la partition déplace, écrit à
>   `plugin/dev-orchestrator/references/workstreams.md:101`). Le 73 comptait toute mention de
>   `.planning/` en récursif, incluant 25 chemins que la partition ne déplace pas ; 45 ⊂ 70 (vérifié
>   par `comm -23` vide).
> - ADR-069 n'a besoin d'aucune correction — ses deux chiffres se re-dérivent au fichier près.
> - **Limite à écrire, pas taire** : le corpus (`~/.claude/gsd-core/`) vit hors dépôt, la mesure
>   n'est **pas épinglable à un commit** de ce dépôt — la seule ancre valide est
>   `~/.claude/gsd-core/VERSION = 1.9.1`. Toute citation future de ces deux chiffres doit porter
>   profondeur + motif + version du moteur, pas juste le nombre.
```

## Artifacts this phase produces

Ce plan ne crée aucun fichier neuf. Il modifie deux fichiers existants :

| Artefact | Nature | Portée de la modification |
|---|---|---|
| `plugin/conductor/references/team-kernel.md` | doctrine du socle team-kernel | section « Étage de parallélisme réellement effectif », l. 64-68 uniquement |
| `.planning/ROADMAP.md` | feuille de route | §Phase 27, bloc de citation l. 1898-1901 uniquement |

Artefacts produits par les **autres** plans de la phase (vue d'ensemble, non écrits ici) : champ
`stages` de `dag.sh ready`, cas T25-T30 de `test-dag.sh`, doctrine `stages` dans `mission-flow.md`
(27-01) · `.worktreeinclude` et `27-ISOLATION-PORTEE.md` (27-03) · `27-MESURE-GAIN.md` et
`27-mesure/waves-toy.json` (27-04, complété par 27-06) · `27-DECISION-claude-orchestration.md` et le
bloc `claude_orchestration` de `.planning/config.json` (27-05).

<tasks>

<task type="auto">
  <name>Tâche 1 — Corriger la doctrine de l'étage de parallélisme réellement effectif</name>
  <reversibility rating="reversible">Une correction de prose dans un fichier de doctrine — l'annuler est un `git revert` d'une poignée de lignes, aucun consommateur machine ne parse ce passage.</reversibility>
  <read_first>
    - `plugin/conductor/references/team-kernel.md` — lire l'intégralité de la section « Étage de parallélisme réellement effectif » (l. 55-89) ainsi que la section précédente « Marge de profondeur de dispatch » (l. 28-53) : la correction doit rester cohérente avec ce qui l'encadre.
    - `.planning/phases/VFDO-27-parall-lisation-d-ex-cution-granulaire-simple-sans-collision/27-PATTERNS.md` — §« `plugin/conductor/references/team-kernel.md` (doctrine, L64-68) » : le passage cible y est cité verbatim et la convention de style à préserver y est extraite.
  </read_first>
  <files>plugin/conductor/references/team-kernel.md</files>
  <action>
    Dans la section « Étage de parallélisme réellement effectif » de
    `plugin/conductor/references/team-kernel.md`, réécrire la ligne de synthèse (« La conséquence
    doctrinale, en une ligne : … ») qui déclare aujourd'hui le parallélisme intra-étape définitivement
    inaccessible sur ce runtime. La nouvelle formulation doit tenir trois choses, et rien de plus
    (D-04, D-11) :

    1. Le parallélisme intra-étape est **éteint par défaut** — désactivé par un drapeau default-off,
       restaurable. Employer littéralement la formulation « éteint par défaut ».
    2. Le chemin qui le restaure ne passe **pas** par `shouldFlattenDispatch()` (qui rend bien `true`
       sous Claude Code — ce fait reste vrai et ne doit pas être effacé) mais par la capability amont
       `claude_orchestration`, dont le gate n°4 lit `dispatch.nested === true &&
       dispatch.background === true`, jamais `backgroundDispatch`. Nommer ces trois identifiants
       tels quels.
    3. Le parallélisme inter-nœuds porté par la frontière `ready` de `vf-dev-manager` reste le **seul
       effectif aujourd'hui**, tant que la capability n'est pas activée — la phrase ne doit plus dire
       qu'il est le seul possible.

    Préserver la convention de la page (termes clés en gras, une ligne de synthèse, renvoi vers le
    protocole plutôt que recopie des chiffres) et conserver intact le paragraphe « Renvoi, pas copie »
    qui suit immédiatement. Ne pas documenter **la doctrine détaillée** de la nouvelle capacité de
    `dag.sh` ici : elle appartient à `mission-flow.md` (plan 27-01) — la recopier ici casserait la
    disjonction d'écriture entre les deux plans (27-RESEARCH.md §Livrable 1, note de collision) et
    dupliquerait un algorithme que ce dépôt ne doit décrire qu'une fois (ADR-069, Iron Law 2 révisée).

    **Ajouter cependant un renvoi d'une ligne**, et rien de plus (suite à revue, M6) : sans lui, les
    quatre managers non-dev du kernel (`business`, `content`, `growth`, `test`) — qui ne lisent
    jamais `mission-flow.md`, propre au métier dev — n'apprendraient jamais que le champ `stages`
    existe, et le gain de la Phase 27 s'évaporerait pour 4 lecteurs sur 5. Poser cette ligne dans le
    paragraphe déjà réécrit ci-dessus (elle porte sur exactement le même sujet — la frontière `ready`
    et ses périmètres), sur le modèle : « le champ `stages` de `dag.sh ready` affine cette frontière en
    calculant la disjonction de périmètres entre nœuds ; doctrine complète dans
    `dev-orchestrator-references/mission-flow.md` ». Nommer le champ `stages`, pas l'algorithme qui le
    calcule : c'est un renvoi, pas une copie — ce fichier reste possédé par `27-02` seul, `27-01` ne le
    touche pas, la disjonction de vague 1 est donc préservée (aucune écriture croisée, un seul plan
    écrit ce fichier).

    Ne toucher à aucune autre section de la page : P3, P12, « Écart déclaré ↔ runtime » et la table
    « Ce que le kernel fournit » restent en l'état.
  </action>
  <verify>
    <automated>! grep -q 'côté moteur) est \*\*perdu\*\*' plugin/conductor/references/team-kernel.md && grep -qF 'éteint par défaut' plugin/conductor/references/team-kernel.md && grep -qF 'claude_orchestration' plugin/conductor/references/team-kernel.md && grep -qF 'backgroundDispatch' plugin/conductor/references/team-kernel.md && grep -qF 'stages' plugin/conductor/references/team-kernel.md && grep -qF 'mission-flow.md' plugin/conductor/references/team-kernel.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - **Source** : `plugin/conductor/references/team-kernel.md` ne contient plus la phrase qui
      déclarait le parallélisme intra-étape irrémédiablement inaccessible côté moteur (assertion
      négative de la commande ci-dessus).
    - **Source** : la même page contient la locution `éteint par défaut`, l'identifiant
      `claude_orchestration` et l'identifiant `backgroundDispatch` (ce dernier dans son rôle de
      mécanisme explicitement **non** lu par le gate n°4).
    - **Source** : la même page nomme le champ `stages` et renvoie vers `mission-flow.md` pour sa
      doctrine complète — sans recopier l'algorithme de partition (M6).
    - **Comportement** : un lecteur de la page peut nommer, sans autre document, le drapeau qui éteint
      le parallélisme intra-étape et la condition exacte du gate qui le rallume ; un lecteur non-dev
      (business, content, growth, test) sait, sans lire `mission-flow.md`, que `dag.sh ready` calcule
      désormais une disjonction de périmètres et où trouver le détail.
    - **Non-régression** : le nombre de sections de niveau `##` de la page est inchangé —
      `[ "$(grep -c '^## ' plugin/conductor/references/team-kernel.md)" = "$(git show HEAD:plugin/conductor/references/team-kernel.md | grep -c '^## ')" ]`.
  </acceptance_criteria>
  <done>La doctrine du team-kernel ne déclare plus perdue une capacité qui est éteinte, et nomme le chemin exact qui la rallume.</done>
</task>

<task type="auto">
  <name>Tâche 2 — Remplacer l'avertissement de divergence du ROADMAP par le résultat re-dérivé</name>
  <precondition>Le bloc de citation d'avertissement sur les comptages est encore présent verbatim aux lignes 1898-1901 de `.planning/ROADMAP.md`, tel que reproduit dans la section « Texte de remplacement du ROADMAP » de ce plan.</precondition>
  <reversibility rating="reversible">Un bloc de citation dans une feuille de route — l'ancien texte reste dans l'historique git, aucun outil ne le parse.</reversibility>
  <read_first>
    - `.planning/ROADMAP.md` lignes 1890-1910 — le §« Les workstreams ne sont pas l'outil » et le bloc de citation qui le clôt, pour repérer les bornes exactes de la substitution.
    - La section « Texte de remplacement du ROADMAP — verbatim » **de ce plan** — elle porte les deux blocs (l'ancien à retrouver, le nouveau à poser) mot pour mot.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Vérifier d'abord que le bloc de citation d'avertissement figure encore aux lignes 1898-1901 de
    `.planning/ROADMAP.md` sous la forme reproduite dans la section « Texte de remplacement du
    ROADMAP » de ce plan. Si le texte a bougé, **s'arrêter** et le signaler : un autre nœud de mission
    serait passé avant, et écraser sa version détruirait un résultat.

    Le remplacer par le second bloc de cette même section, **recopié tel quel, sans reformulation** —
    c'est un résultat re-dérivé par un nœud parallèle de cette mission, pas une conclusion à
    ré-écrire ni à re-dériver ici. D-13 impose que tout chiffre gravé porte sa méthode : la méthode
    est déjà dans le texte fourni (profondeur, motif, version du moteur), la recopier fidèlement est
    ce qui satisfait la contrainte.

    Conserver le niveau de citation Markdown (`>`) et l'indentation d'origine, ainsi que la ligne vide
    qui sépare le bloc du paragraphe précédent. Ne modifier aucune autre ligne du §Phase 27 — ni le
    Goal, ni les tables d'options, ni les contraintes non négociables ; la ligne `**Plans**:` est mise
    à jour par l'orchestrateur de planification, pas par cette tâche.
  </action>
  <verify>
    <automated>! grep -q 'Chiffres divergents, à ne pas recopier' .planning/ROADMAP.md && grep -qF 'gsd-core/VERSION = 1.9.1' .planning/ROADMAP.md && grep -qF 'critère K2' .planning/ROADMAP.md && grep -qF 'comm -23' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - **Source** : `.planning/ROADMAP.md` ne contient plus l'avertissement invitant à ne pas recopier
      les comptages (assertion négative ci-dessus).
    - **Source** : le §Phase 27 porte les trois marqueurs de méthode du résultat re-dérivé — le
      critère nommé (`critère K2`), la vérification d'inclusion (`comm -23`) et l'ancre de version
      (`gsd-core/VERSION = 1.9.1`).
    - **Comportement** : une citation future de « 7/91 » ou « 45 » depuis ce ROADMAP emporte avec elle
      sa profondeur, son motif et la version du moteur — la limite d'ancrage hors dépôt est écrite,
      pas tue (D-13).
    - **Non-régression** : `grep -c '^### Phase ' .planning/ROADMAP.md` est inchangé par rapport à
      `HEAD` — aucune entrée de phase perdue par l'édition.
  </acceptance_criteria>
  <done>Le ROADMAP porte le résultat re-dérivé avec sa méthode et sa limite, plus l'alerte qui invitait à ne rien citer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → jugement des 5 managers | `team-kernel.md` est lu par tous les managers du team-kernel ; une affirmation fausse y devient une décision de dispatch fausse, à chaque mission |
| chiffre gravé → citation aval | un nombre inscrit au ROADMAP sans sa méthode est recopié tel quel par les phases suivantes ; c'est la 5ᵉ occurrence du motif dans ce dépôt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02-01 | Tampering | `.planning/ROADMAP.md` — édition concurrente par un autre nœud de la même mission | medium | mitigate | `<precondition>` sur la tâche 2 : le bloc doit être présent verbatim avant substitution, sinon arrêt et signalement — jamais un écrasement |
| T-27-02-02 | Repudiation | `team-kernel.md` — correction sans trace du mécanisme réel | low | mitigate | la formulation impose de nommer `claude_orchestration` et la condition du gate n°4 : la correction porte sa propre justification, vérifiable par grep |
| T-27-02-03 | Information disclosure | aucune — les deux fichiers sont des documents publics du dépôt, sans secret ni chemin machine sensible | low | accept | aucun secret n'entre ni ne sort ; l'ancre `~/.claude/gsd-core/VERSION` est un chemin d'outillage standard |
| T-27-02-04 | Denial of service | édition destructive d'un fichier de 143 l. (`team-kernel.md`) ou de 1943 l. (`ROADMAP.md`) par une réécriture globale au lieu d'une substitution scopée | high | mitigate | les deux tâches imposent une substitution scopée ; les critères de non-régression comptent les sections (`^## ` et `^### Phase `) avant/après contre `HEAD` |
</threat_model>

<verification>
1. `! grep -q 'côté moteur) est \*\*perdu\*\*' plugin/conductor/references/team-kernel.md` — l'affirmation fausse a disparu.
2. `grep -qF 'éteint par défaut' plugin/conductor/references/team-kernel.md` — la formulation exacte de D-04 est posée.
3. `! grep -q 'Chiffres divergents, à ne pas recopier' .planning/ROADMAP.md` — l'avertissement a laissé place au résultat.
4. `git diff --stat` ne liste que `plugin/conductor/references/team-kernel.md` et `.planning/ROADMAP.md`.

> **Retiré suite à revue (M4)** : cette liste portait un item « `check-agents.sh` reste vert sur les
> 25 agents » — ce plan ne touche aucun fichier d'agent, et le plan `27-03` en réécrit 13 dans la
> **même vague** (dispatch parallèle). Faire tourner ce gate ici serait une lecture concurrente d'un
> ensemble muté par un autre nœud, sans objet pour ce plan : le gate `check-agents.sh` est déjà
> vérifié là où il a un sens, dans le `<verify>` de la tâche 2 du plan `27-03`.
</verification>

<success_criteria>
- La doctrine du team-kernel décrit le parallélisme intra-étape comme éteint par défaut et nomme la
  capability + la condition de gate qui le restaurent (PAEX-01, D-04).
- Le ROADMAP §Phase 27 porte le résultat re-dérivé des deux comptages avec profondeur, motif et
  version du moteur, ainsi que sa limite d'ancrage hors dépôt (PAEX-02, D-13).
- Exactement deux fichiers modifiés, tous deux disjoints des `files_modified` des cinq autres plans.
</success_criteria>

<output>
Create `.planning/phases/VFDO-27-parall-lisation-d-ex-cution-granulaire-simple-sans-collision/27-02-SUMMARY.md` when done
</output>

---
phase: 24-activation-et-mesure-du-moteur-gsd
plan: 06
type: execute
wave: 2
depends_on: ["24-03"]
files_modified:
  - .planning/config.json
  - plugin/dev-orchestrator/references/intent-routing.md
  - plugin/dev-orchestrator/references/docs-flow.md
  - plugin/dev-orchestrator/references/gsd-capabilities-index.md
  - plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh
autonomous: true
requirements: [GSDA-07, GSDA-08]

must_haves:
  truths:
    - "`intel.enabled` est activé : le mode `--query` que notre propre `docs-flow.md` publie comme régime normal cesse de router vers un geste inerte."
    - "La frontière entre le jugement humain daté de `.planning/codebase/` et le fait dérivé rafraîchissable de `.planning/intel/` est écrite, avec ses formats et ses lecteurs prescrits."
    - "`graphify` et `profile-pipeline` sont refusés : aucune clé posée, et leurs TROIS entrées de doc et de routage portent un marqueur conditionnel nommant leur toggle."
    - "`graphify` et `profile-pipeline` figurent dans `gsd-capabilities-index.md` avec leur toggle — sans quoi le gate du plan 24-11 n'aurait rien à lire."
    - "L'index reste GÉNÉRÉ depuis le registre du moteur : aucune ligne de table écrite à la main (D-07, Phase 23)."
  artifacts:
    - .planning/config.json
    - plugin/dev-orchestrator/references/intent-routing.md
    - plugin/dev-orchestrator/references/docs-flow.md
    - plugin/dev-orchestrator/references/gsd-capabilities-index.md
    - plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh
  key_links:
    - "Le marqueur conditionnel posé ici est la donnée d'entrée du gate d'activation doc ↔ capability créé au plan 24-11 : sa forme exacte est un contrat entre les deux plans."
    - "Le canari de forme du moteur en CI (`ci.yml:158-197`) exécute ce générateur et exige rc=0 plus la présence du point de hook `plan:pre` : l'extension ne doit pas le faire rougir."
    - "Ce plan est le SECOND des deux à écrire `.planning/config.json` (après 24-03, dont il dépend)."
---

<objective>
Faire tenir la promesse que notre documentation publie déjà, refuser honnêtement les deux capacités
sans lecteur prescrit, et donner au gate de la zone 3 quelque chose à lire.

Purpose: trois routes de ce dépôt mènent aujourd'hui à un geste inerte. Deux étaient connues
(`intent-routing.md:104` → `gsd-graphify`, `:147` → `gsd-profile-user`). La troisième est la plus
gênante parce qu'elle vient de nous : `docs-flow.md:43-44` publie `--query` comme l'un des deux
modes normaux de `gsd-map-codebase`, or ce skill exige `intel.enabled: true`. Le point commun des
trois est qu'aucun gate ne relie une entrée de doc à l'activation de sa capability. Ce plan pose les
marqueurs et l'index ; le plan 24-11 pose le gate qui les lit.

Output: `intel` activé, la frontière `codebase/` ↔ `intel/` écrite, trois entrées marquées
conditionnelles avec leur toggle, et un générateur d'index étendu aux capabilities sans étage
déclaré.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-ARBITRAGES.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-CONTEXT.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-PATTERNS.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : activer `intel.enabled`, laisser `graphify` et `profile-pipeline` sans clé</name>
  <files>.planning/config.json</files>
  <read_first>
    - `.planning/config.json` **après le plan 24-03** — l'état exact avec `agent_skills.gsd-planner` peuplé ; ne pas travailler sur la version d'avant.
    - `24-CONTEXT.md` fiches F-27, F-30, F-31 — les trois toggles à `false` par défaut, la troisième route morte, et la frontière `codebase/` ↔ `intel/` déjà tracée par les formats.
    - `~/.claude/skills/gsd-map-codebase/SKILL.md` ligne 29 — la dépendance littérale de `--query` à l'activation d'`intel` : la re-lire avant d'écrire, elle est la raison d'être de cette activation.
    - `plugin/dev-orchestrator/scripts/check-gsd-config.sh` — le gate advisory qui signale les clés inconnues du moteur installé et les toggles laissés au défaut implicite.
  </read_first>
  <action>
Ajouter à `.planning/config.json` un bloc `intel` portant son activation à vrai, au format et à
l'indentation du fichier (2 espaces), placé de manière cohérente avec l'ordre des blocs existants.

Ne poser **aucune** clé pour `graphify` ni pour `profile-pipeline` : le refus s'exprime par
l'absence, et leur défaut amont est déjà l'inactivité. Poser une clé à faux donnerait l'illusion
d'un réglage révisable alors que la décision est un refus écrit.

Après édition, revérifier les contrôles négatifs déjà posés par le plan 24-03 — les clés différées
par ADR-066 (`workflow.windows_enforce`, `hooks.workflow_guard`) et refusées par ADR-067
(`hooks.community`) et par GSDA-03 (`workflow.tdd_mode`) doivent toujours être absentes, et
`agent_skills.gsd-planner` toujours peuplé. Ce plan ne doit rien défaire de 24-03.

Constater ensuite l'effet réel : `.planning/intel/` n'existe pas encore ; l'activation rend la
capability disponible, elle ne produit pas l'artefact. Consigner au SUMMARY ce que l'activation
change et ce qu'elle ne change pas.
  </action>
  <verify>
    <automated>jq -e '.intel.enabled == true' .planning/config.json &amp;&amp; jq -e '.graphify == null and ."profile-pipeline" == null and .workflow.windows_enforce == null and .hooks.workflow_guard == null and .hooks.community == null and .workflow.tdd_mode == null' .planning/config.json &amp;&amp; jq -e '.agent_skills["gsd-planner"] | length == 2' .planning/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.intel.enabled == true' .planning/config.json` réussit.
    - `jq -e '.graphify' .planning/config.json` et `jq -e '."profile-pipeline"' .planning/config.json` **échouent** tous les deux : le refus est une absence, jamais une valeur fausse.
    - Les quatre contrôles négatifs de 24-03 tiennent toujours (`windows_enforce`, `workflow_guard`, `community`, `tdd_mode` absents) et `agent_skills.gsd-planner` porte toujours 2 entrées.
    - `jq empty .planning/config.json` réussit.
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort 0 ou 3 et ne cite pas `intel` parmi les clés inconnues.
  </acceptance_criteria>
  <done>`intel` est activé, les deux capacités refusées n'ont aucune clé, et rien de ce que 24-03 a posé n'a bougé.</done>
  <reversibility rating="reversible">Un toggle de capability ; sa désactivation est une clé à faux et l'artefact `.planning/intel/` reste ignorable.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : marquer les trois routes conditionnelles et écrire la frontière `codebase/` ↔ `intel/`</name>
  <files>plugin/dev-orchestrator/references/intent-routing.md, plugin/dev-orchestrator/references/docs-flow.md</files>
  <read_first>
    - `plugin/dev-orchestrator/references/intent-routing.md` ligne 104 (famille savoir → `gsd-extract-learnings`, `gsd-graphify`) et ligne 147 (« profile ma façon de bosser » → `gsd-profile-user`), plus les lignes 101-103 qui montrent comment le fichier marque déjà des régimes distincts sur une même brique (`--verify-only` libre, standard sous confirmation, `--force` annoncé).
    - `plugin/dev-orchestrator/references/docs-flow.md` lignes 39-54 — la famille code et le mode `--query`, puis la famille savoir qui cite `gsd-graphify`.
    - `24-CONTEXT.md` fiche F-31 — la frontière, avec ses formats, ses lecteurs prescrits nommés et leurs numéros de ligne.
    - `plugin/dev-orchestrator/references/gsd-capabilities-index.md` lignes 1-13 — l'avertissement « ne dit jamais ce qui est effectivement actif sur un lab donné » : le marqueur posé ici est ce qui comble ce trou côté doc.
  </read_first>
  <action>
Poser un marqueur conditionnel de **forme unique et stable** sur les entrées qui pointent une
capability inactive. La forme est un contrat avec le gate du plan 24-11 : le marqueur nomme le
toggle gouvernant entre parenthèses, sur la ligne même de l'entrée, avec le mot `conditionnelle`
suivi du nom complet du toggle tel qu'il figure dans `gsd-capabilities-index.md`. Employer
exactement la même forme aux trois endroits — le gate cherche un motif, pas une paraphrase.

Les trois entrées à marquer :
- `intent-routing.md` ligne 104, sur la mention de `gsd-graphify` (le toggle est celui de la
  capability `graphify`) ;
- `intent-routing.md` ligne 147, sur `gsd-profile-user` (toggle de `profile-pipeline`) ;
- `docs-flow.md` § famille savoir, sur la mention de `gsd-graphify`.

Chaque marqueur est suivi d'une demi-phrase qui dit **pourquoi** la capability est refusée : aucun
consommateur prescrit dans le module, refus écrit à la Phase 24. Ne pas écrire « désactivée » sans
plus : une entrée conditionnelle doit dire ce qui la rendrait active.

Dans `docs-flow.md`, § famille code, écrire en revanche l'inverse pour `--query` : la promesse tient
désormais, parce que `intel.enabled` est activé sur ce lab — et le dire **avec sa dépendance
nommée**, de sorte qu'un lab qui désactiverait `intel` sache immédiatement ce qu'il perd. Ne pas
marquer cette entrée conditionnelle : elle ne l'est plus.

Ajouter ensuite à `docs-flow.md` une section courte qui écrit la frontière, en trois blocs :
- `.planning/codebase/` — sept documents markdown narratifs portant du **jugement humain daté**
  (dette technique, limites de montée en charge, contraintes d'architecture), avec des lecteurs
  **prescrits nommément** : `vf-dev-manager.md:32`, `vf-auditer.md:3,23`, `check-dev-bootstrap.sh:27`,
  `gsd-planner.md:635-653` ;
- `.planning/intel/` — cinq JSON machine plus une surface d'API, horodatés et hashés, avec
  interdiction explicite du temporel côté amont, **un seul** consommateur automatique, et la mention
  que l'amont y appose lui-même « indice seulement, possiblement incomplet » ;
- la règle qui les sépare : un fait dérivé rafraîchissable ne se substitue jamais à un jugement
  humain daté ; `intel/` alimente une recherche, il n'est jamais cité comme preuve dans une décision,
  et il ne dispense jamais de rafraîchir `codebase/`.

Vérifier les plafonds ADR-029 des deux fichiers après édition avec `awk 'END{print NR}'`.
  </action>
  <verify>
    <automated>test "$(awk '/[Cc]onditionnelle/ &amp;&amp; (/graphify/ || /profile-pipeline/) {n++} END{print n+0}' plugin/dev-orchestrator/references/intent-routing.md plugin/dev-orchestrator/references/docs-flow.md)" -ge 3 &amp;&amp; awk '/intel.enabled/{a=1} /jugement humain daté/{b=1} /gsd-planner.md:635-653/{c=1} END{exit !(a&&b&&c)}' plugin/dev-orchestrator/references/docs-flow.md &amp;&amp; test "$(awk 'END{print NR}' plugin/dev-orchestrator/references/intent-routing.md)" -le 500 &amp;&amp; test "$(awk 'END{print NR}' plugin/dev-orchestrator/references/docs-flow.md)" -le 500</automated>
  </verify>
  <acceptance_criteria>
    - Exactement **trois** lignes portent un marqueur conditionnel : deux dans `intent-routing.md` (celle de `gsd-graphify`, celle de `gsd-profile-user`) et une dans `docs-flow.md` (famille savoir).
    - Les trois marqueurs emploient **la même forme littérale** — le mot `conditionnelle` suivi du nom complet du toggle gouvernant entre parenthèses — vérifié par une extraction `awk` qui rend trois occurrences d'un motif identique.
    - Chaque marqueur est suivi du motif du refus (aucun consommateur prescrit, refus Phase 24).
    - `docs-flow.md` § famille code nomme `intel.enabled` comme dépendance de `--query` et **ne porte pas** de marqueur conditionnel sur cette entrée.
    - `docs-flow.md` contient la section de frontière avec les littéraux `jugement humain daté`, `vf-dev-manager.md:32`, `vf-auditer.md:3,23`, `check-dev-bootstrap.sh:27`, `gsd-planner.md:635-653`, et la règle de non-substitution.
    - `awk 'END{print NR}'` rend ≤ 500 sur chacun des deux fichiers (ADR-029).
    - Aucune ligne préexistante n'a disparu de l'un ou l'autre fichier (`comm -23` avant/après trié rend le vide, hors les lignes délibérément réécrites qui sont listées au SUMMARY).
  </acceptance_criteria>
  <done>Les trois routes disent honnêtement leur état, la promesse de `--query` est adossée à son toggle, et la frontière entre jugement humain et fait dérivé est écrite avec ses lecteurs.</done>
  <reversibility rating="reversible">Marqueurs et section documentaires ; leur forme est le seul engagement, et elle est contractualisée avec le gate du plan 24-11.</reversibility>
</task>

<task type="auto">
  <name>Tâche 3 : étendre le générateur d'index aux capabilities sans étage déclaré</name>
  <files>plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh, plugin/dev-orchestrator/references/gsd-capabilities-index.md</files>
  <read_first>
    - `plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` en entier (365 lignes) — la docstring de tête : lecture jamais exécution du registre (T-23-04-07, arbitrage A-12), aucun nom de point de hook écrit en dur, aucune version figée, production dans un temporaire puis `mv` atomique, signal explicite « EXTRACTION PERIMEE » et rc=1 plutôt qu'une table tronquée, et l'interdiction de recopier les champs de rendu ou les fragments en ligne.
    - `plugin/dev-orchestrator/references/gsd-capabilities-index.md` en entier (111 lignes) — l'en-tête « auto-généré — NE PAS ÉDITER », la structure par point de hook, et le pied qui compte les points parcourus et les étages déclarés.
    - `~/.claude/gsd-core/bin/lib/capability-registry.cjs` — les exports `capabilities`, `byLoopPoint` et `capabilityClusters` : identifier la forme textuelle qui permet d'énumérer les capabilities ET leur clé de configuration gouvernante, avant d'écrire l'extraction.
    - `.github/workflows/ci.yml` lignes 158-197 — le canari qui exécute ce générateur et exige rc=0 sans signal périmé, plus la présence du point `plan:pre` dans la table produite.
  </read_first>
  <action>
Étendre le générateur d'une seconde section, produite après les sections par point de hook : la
liste des capabilities que le registre **déclare** mais qui n'apparaissent à aucun point de hook —
c'est le cas de `graphify` et de `profile-pipeline`, et c'est pourquoi l'index de la Phase 23 ne les
portait pas.

L'extraction obéit aux deux règles déjà écrites dans la docstring, sans exception : la liste des
capabilities est **énumérée depuis le registre**, jamais écrite en dur, et le registre est **lu**,
jamais `require()`. Pour chaque capability absente de tous les points de hook, la section porte son
identifiant et sa clé de configuration gouvernante — les deux seuls champs structurés nécessaires,
aucun champ de prose destiné au modèle.

Le mode d'échec ne change pas : si la forme textuelle de l'export `capabilities` cesse d'être
lisible, le générateur émet le signal explicite d'extraction périmée et sort en rc=1, **sans écrire
aucun fichier** — jamais une section vide qui passerait pour « aucune capability hors point de
hook ». La distinction entre « la liste est vide » et « la liste n'a pas pu être lue » doit être
visible dans la sortie.

Le pied de page gagne le compte de capabilities hors point de hook, à côté des comptes existants.

Régénérer ensuite `references/gsd-capabilities-index.md` **par le générateur**, jamais à la main :
l'en-tête « NE PAS ÉDITER » est un contrat de la Phase 23 (D-07, tout index exposé est généré depuis
le disque ou gaté). Vérifier que la table par point de hook est inchangée à l'identique et que seule
la nouvelle section s'y ajoute.

Vérifier enfin que le canari de CI reste satisfaisable : rc=0 contre le moteur installé, aucun signal
périmé, et le point `plan:pre` toujours présent dans la table produite. Rejouer localement la
séquence exacte du canari plutôt que de la supposer.
  </action>
  <verify>
    <automated>VF_GSD_CORE_LIB="$HOME/.claude/gsd-core/bin/lib" VF_CAPS_INDEX_OUT=/tmp/caps-idx-24-06.md bash plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh &amp;&amp; awk '/plan:pre/{a=1} /graphify/{b=1} /profile-pipeline/{c=1} END{exit !(a&&b&&c)}' /tmp/caps-idx-24-06.md &amp;&amp; awk '/graphify/{a=1} /profile-pipeline/{b=1} END{exit !(a&&b)}' plugin/dev-orchestrator/references/gsd-capabilities-index.md &amp;&amp; rm -f /tmp/caps-idx-24-06.md</automated>
  </verify>
  <acceptance_criteria>
    - Le générateur exécuté contre le moteur installé sort **rc=0** et sa sortie stderr ne contient pas le signal d'extraction périmée.
    - La table produite contient une section neuve listant au moins `graphify` et `profile-pipeline`, chacun avec sa clé de configuration gouvernante.
    - `plugin/dev-orchestrator/references/gsd-capabilities-index.md` régénéré contient les littéraux `graphify` et `profile-pipeline`, et son en-tête « auto-généré — NE PAS ÉDITER » est intact.
    - Les sections par point de hook du fichier régénéré sont **identiques** à celles d'avant (comparaison `cmp -s` sur la portion allant du premier `## ` de point de hook jusqu'à la nouvelle section, extraite par `awk` dans deux temporaires).
    - Le pied de page porte un troisième compteur : le nombre de capabilities hors point de hook.
    - **Discriminance du mode d'échec** : le générateur lancé avec `VF_GSD_CORE_LIB` pointant un dossier de fixture dont l'export `capabilities` est illisible sort **rc=1**, émet le signal d'extraction périmée, et **n'écrit aucun fichier** — le fichier cible reste bit-à-bit intact (`cmp -s` avant/après vrai).
    - Aucune ligne de la table n'a été écrite à la main : le fichier versionné est bit-à-bit identique à la sortie du générateur (`cmp -s` entre le fichier commité et une régénération dans un temporaire).
  </acceptance_criteria>
  <done>Les deux capacités refusées existent dans l'index avec leur toggle, produites par le générateur, et le mode d'échec distingue toujours « vide » de « illisible ».</done>
  <reversibility rating="costly">Ce générateur est exercé par le canari de forme du moteur en CI : une extraction fragile ferait rougir la CI à la prochaine montée de version amont. Le mode d'échec explicite est ce qui rend cette fragilité visible plutôt que silencieuse.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `capability-registry.cjs` du moteur → générateur | Entrée NON MAÎTRISÉE : la cascade de résolution du générateur peut pointer un chemin situé dans le dépôt audité (T-23-04-07). |
| `.planning/config.json` → moteur GSD | L'activation d'`intel` autorise le moteur à produire `.planning/intel/` et à l'injecter dans un prompt de planification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06-01 | Elevation of Privilege | `build-gsd-capabilities-index.sh` → registre | high | mitigate | L'extension **lit** le texte du registre, ne le `require()` jamais — la garde d'A-12 est reconduite à l'identique sur le nouvel export ; vérifié en relisant le programme node du générateur avant modification. |
| T-24-06-02 | Tampering | `gsd-capabilities-index.md` | high | mitigate | Le fichier versionné doit être bit-à-bit identique à une régénération (`cmp -s`) : aucune ligne écrite à la main ne peut survivre au critère d'acceptation. |
| T-24-06-03 | Information Disclosure | contenu injecté depuis `.planning/intel/` | medium | mitigate | L'amont marque lui-même l'artefact « indice seulement, possiblement incomplet » et interdit le temporel ; la frontière écrite en tâche 2 interdit de le citer comme preuve dans une décision. |
| T-24-06-04 | Denial of Service | canari de forme du moteur en CI | high | mitigate | La séquence exacte du canari est rejouée localement avant de conclure, jamais supposée ; rc=0 et présence de `plan:pre` sont des critères d'acceptation. |
| T-24-06-SC | Tampering | installs npm/pip/cargo | high | accept | Ce plan n'exécute aucune installation de paquet. |
</threat_model>

<verification>
- `intel.enabled` vaut vrai ; `graphify` et `profile-pipeline` n'ont aucune clé ; les contrôles négatifs de 24-03 tiennent.
- Trois marqueurs conditionnels de forme identique existent, et un seul régime non conditionnel (`--query`) nomme sa dépendance.
- `gsd-capabilities-index.md` est bit-à-bit identique à une régénération et porte les deux capacités refusées.
- Le canari de CI reste satisfaisable : générateur rc=0, aucun signal périmé, `plan:pre` présent.
</verification>

<success_criteria>
La troisième route inerte est refermée par activation, les deux autres par un marquage honnête et
uniforme, et l'index généré porte enfin l'état des deux capacités refusées — ce sans quoi le gate du
plan 24-11 n'aurait rien à lire.
</success_criteria>

## Artifacts this phase produces

| Symbole | Nature | Emplacement |
|---|---|---|
| `intel.enabled: true` | clé de configuration | `.planning/config.json` |
| marqueur `(conditionnelle : <toggle>)` | contrat de forme lu par le gate 24-11 | `intent-routing.md` (×2), `docs-flow.md` (×1) |
| section « frontière `codebase/` ↔ `intel/` » | doctrine | `plugin/dev-orchestrator/references/docs-flow.md` |
| section « capabilities hors point de hook » | section générée + 3e compteur de pied | `gsd-capabilities-index.md` |
| extraction de l'export `capabilities` | logique de générateur (lecture, jamais require) | `build-gsd-capabilities-index.sh` |

<output>
Créer `.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-06-SUMMARY.md` en fin de plan.
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 10
lot: L10 — preuves reportées dans la doctrine — statut O-3, deux items BACKLOG traités par renvoi, ADR-072 § Mesures à la pose, état daté dans CLAUDE.md
type: execute
wave: 10
depends_on: ["41-09"]
autonomous: true
requirements: [PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md
  - .planning/BACKLOG.md
  - docs/ADR.md
  - CLAUDE.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Précondition : `POSE-DECISION: option=poser` ; chaque mesure est conforme ou couverte par une décision humaine de poursuite consignée avec son décideur, son canal et sa date (`M2`, `CO`, `M1`, `M3`, `D03`, `M4`) ; hors PR en vol, aucune PR ouverte ; B à jour d'`origin/main`"
    - "O-3 du `25-SECURITY.md` reçoit UNE ligne de statut datée « gardée par défaut + tracée » (volet admin) renvoyant à `41-PREUVES.md` et ADR-072 ; le texte existant d'O-3 (« signalée et tracée », périmètre sans admin) reste intact ; aucune proposition d'aucun artefact de la phase n'associe O-3 à un mot d'achèvement, même nié (D-02)"
    - "Les DEUX items BACKLOG sont traités par renvoi, sans qu'aucune de leurs lignes existantes ne soit modifiée : « Protection de `main` côté GitHub — DIFFÉRÉ » reçoit « REPRIS ET TRAITÉ par renvoi », « Posture de protection de `main` — TRANCHÉ » reçoit « TRAITÉ par renvoi » (D-M10)"
    - "ADR-072 § « Mesures à la pose » (dans la sous-section d'amendement du 2026-09-23) recopie chaque résultat consigné — M-1, M-2 (entrées `User` relues), M-3, provenances serveur et client, revue code owner par chemin et son témoin, trace (ii) règle par règle, push direct hors liste, M-4 pour les deux sortes d'acteurs, accès aux rule suites, PR en vol avant/après — et chaque décision humaine consignée avec son décideur, son canal et sa date ; il reprend la nuance « contournement réel sur des checks en cours, pas sur une PR rouge » ; jamais une valeur absente de `41-PREUVES.md`"
    - "`CLAUDE.md` § « Protection côté serveur — main et tags v* » reçoit UNE ligne d'état datée (posée, prouvée, renvois) ; aucune autre ligne du fichier modifiée ; `tools/check-aucune-fermeture.sh` rend 0"
    - "Commits de plan : D-01..D-10 avec « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », D-02bis avec « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 »"
  artifacts:
    - path: ".planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md"
      provides: "statut daté d'O-3 (volet admin)"
    - path: "docs/ADR.md"
      provides: "ADR-072 § Mesures à la pose"
    - path: "CLAUDE.md"
      provides: "état daté de la protection côté serveur"
  key_links:
    - "Ce plan écrit sur B (`feat/phase-41-preuves-release`) ; la publication et le merge viennent au plan 41-11"
---

<objective>
Reporter dans la doctrine ce que les mesures ont établi, et rien de plus : statut d'O-3, items
BACKLOG traités par renvoi, ADR-072 § Mesures, état daté dans `CLAUDE.md`.

Révisé le 2026-09-23 (reprise) : O-3 porte déjà « signalée et tracée » (plan 41-18) — la ligne de
statut s'y AJOUTE ; le BACKLOG a désormais deux items concernés (l'item « DIFFÉRÉ » créé par 41-18 et
l'item d'origine) ; les mesures reflètent D-02bis (entrées `User`, acteur hors liste pour le push
direct et les tags, témoin code owner, PR en vol) ; `CLAUDE.md` reçoit l'état daté de la protection.

Purpose: PROT-04 (statut O-3 sur pièce) et PROT-03 (ADR-072 complétée par la mesure, BACKLOG traité).
Output: cinq fichiers à jour sur la branche B.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

Extraits à lire : `25-SECURITY.md`, bloc `- **O-3 — …` jusqu'à `- **O-4 — …` ; `.planning/BACKLOG.md`,
sections `## Protection de \`main\` côté GitHub — DIFFÉRÉ …` et `## Posture de protection de \`main\`
— TRANCHÉ …` ; `docs/ADR.md` § ADR-072, sous-section « Amendement du 2026-09-23 » ; `CLAUDE.md`
§ « Protection côté serveur — main et tags v* ».
</context>

<tasks>

<task type="auto">
  <name>Task 1 : B à jour de `main`, précondition des mesures, statut O-3, deux items BACKLOG traités par renvoi</name>
  <files>.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md, .planning/BACKLOG.md, .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes, décisions comprises)
    - les extraits listés dans le contexte
  </read_first>
  <action>
Identité `picmakpro` ; branche B ; `git fetch origin` ; `git merge --no-edit origin/main` en local (le
merge de PR-P et d'éventuelles PR en vol y entrent ; un conflit s'arrête et se remonte). Vérifier la
précondition du frontmatter (derniers verdicts, décisions de poursuite).

`25-SECURITY.md` : à la fin de la puce O-3 (avant `- **O-4 — `), sans modifier ses lignes, ajouter UNE
ligne indentée de continuation de liste :
`  **Statut Phase 41, volet admin (<date d'exécution>) : gardée par défaut + tracée.** Une hausse de baseline exige l'approbation de `@picmakpro` (CODEOWNERS, revue code owner requise par le ruleset de `main`) ou un contournement explicite par Samuel ou Willy, qui laisse une entrée dans les rule suites ; G-1 continue de la signaler en CI (D-02, D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23). Preuves : `41-PREUVES.md` (`CO-I:`, `CO-BASELINE:`, `CO-TEMOIN:`, `TRACE-II:`) ; politique : ADR-072.`
Si `TRACE-II:` porte un `non_prouvee`, ajouter à cette même ligne « Trace du contournement : non
prouvée pour <règle> (41-PREUVES.md). » Aucun autre mot : le statut n'est pas un achèvement (D-02).

`.planning/BACKLOG.md`, à la fin de chaque item, avant l'item suivant, sans modifier aucune ligne
existante :
- item « Protection de `main` côté GitHub — DIFFÉRÉ… » : `**Statut (<date d'exécution>, Phase 41, volet admin) : REPRIS ET TRAITÉ par renvoi** — déclencheur tombé le 2026-09-23 (accès admin de Willy constaté) ; rulesets de branche et de tags posés et prouvés, CODEOWNERS étroit, contournement nommé Samuel et Willy : ADR-072 § Amendement du 2026-09-23 ; preuves : `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md`.` ;
- item « Posture de protection de `main` — TRANCHÉ… » : `**Statut (<date d'exécution>, Phase 41, volet admin) : TRAITÉ par renvoi** — la forme attendue (ruleset exigeant la CI verte avant merge) est posée et prouvée : ADR-072 ; preuves : `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md`.`

Ajouter à `41-PREUVES.md` une section `## 41-10 — doctrine sur pièce`. Commit local sur B, message
« docs(41-10): statut O-3 et items BACKLOG traités par renvoi (D-02, D-05 — arbitrage Samuel,
AskUserQuestion session principale, 2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session
principale, 2026-09-23) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; pd=$(val POSE-DECISION: option); m2=$(last M2-VERDICT:); m2d=$(val M2-DECISION: option); co=$(last CO-VERDICT:); cod=$(val CO-DECISION: option); m1=$(last M1-VERDICT:); ct=$(val CO-TEMOIN: discriminant); m3=$(last M3-VERDICT:); m3d=$(val M3-DECISION: option); d3=$(last D03-VERDICT:); m4=$(last M4-VERDICT:); dd=$(val D03-M4-DECISION: option); ok=oui; [ "$pd" = poser ] || ok=non; { [ "$m2" = CONFORME ] || [ "$m2d" = accepter-et-documenter ]; } || ok=non; { [ "$co" = CONFORME ] || [ "$cod" = accepter-et-documenter ]; } || ok=non; { [ "$m1" = SUR ] && [ "$ct" = oui ]; } || ok=non; { [ "$m3" = CONFORME ] || [ "$m3d" = fermer-et-poursuivre ] || [ "$m3d" = consigner-et-poursuivre ]; } || ok=non; { { [ "$d3" = CONFORME ] && [ "$m4" = CONFORME ]; } || [ "$dd" = accepter-et-documenter ]; } || ok=non; pr=$(val PUSH-DIRECT: refuse); sc=$(val D03-M4-DECISION: sort_commit_preuve); if [ "$pr" = non ]; then case "$sc" in (revert-par-pr|conservation-declaree) ;; (*) ok=non;; esac; fi; al=$(val PR-EN-VOL-APRES: numeros); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq ".[].number" | awk -v al=",$al," "{ if (index(al, \",#\" \$1 \",\") == 0) c++ } END{print c+0}"); br=$(git branch --show-current); b=$(git rev-list --count HEAD..origin/main); echo "precondition 41-10 : pose=$pd M2=$m2/$m2d CO=$co/$cod M1=$m1 temoin=$ct M3=$m3/$m3d D03=$d3 M4=$m4 decision=$dd push_direct_refuse=$pr sort_commit_preuve=$sc prs_hors_vol=$op branche=$br retard_sur_origin_main=$b"; [ "$ok" = oui ] && [ "$op" -eq 0 ] && [ "$br" = feat/phase-41-preuves-release ] && [ "$b" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou pose non décidée ; ou un verdict de mesure ni conforme ni couvert par une décision de poursuite consignée (décideur, canal et date) ; ou témoin code owner non discriminant ; ou push direct accepté sans `sort_commit_preuve` fixé ; ou, hors PR en vol, une PR ouverte ; ou branche courante différente de B ; ou `origin/main` non fusionné en local</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md; T=$(mktemp -d); awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$F" > "$T/o3"; s=$(awk "index(\$0, \"**Statut Phase 41, volet admin (\")" "$T/o3"); n=$(awk "index(\$0, \"**Statut Phase 41, volet admin (\") {c++} END{print c+0}" "$T/o3"); g=$(printf "%s" "$s" | awk "index(\$0, \"gardée par défaut + tracée\") {c++} END{print c+0}"); x=$(printf "%s" "$s" | awk "{l=tolower(\$0); if (index(l, \"ferm\") || index(l, \"clos\") || index(l, \"résolu\")) c++} END{print c+0}"); r=$(printf "%s" "$s" | awk "index(\$0, \"41-PREUVES.md\") && index(\$0, \"ADR-072\") && index(\$0, \"D-02bis\") {c++} END{print c+0}"); git show origin/main:"$F" > "$T/src"; awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$T/src" > "$T/o3a"; orig=$(awk "END{print NR}" "$T/o3a"); now=$(awk "END{print NR}" "$T/o3"); awk "!index(\$0, \"**Statut Phase 41, volet admin (\")" "$T/o3" > "$T/o3b"; pre=$(cmp -s "$T/o3b" "$T/o3a" && echo oui || echo non); echo "O-3 : lignes_statut=$n garde_trace=$g vocabulaire_d_achevement=$x renvois=$r lignes_origine=$orig lignes_maintenant=$now texte_origine_intact=$pre"; [ "$n" -eq 1 ] && [ "$g" -eq 1 ] && [ "$x" -eq 0 ] && [ "$r" -eq 1 ] && [ "$now" -eq $((orig + 1)) ] && [ "$pre" = oui ]'</automated>
    <fails_when>exit non nul ; ou `lignes_statut=` différent de 1 ; ou statut sans « gardée par défaut + tracée » ; ou `vocabulaire_d_achevement=` différent de 0 (D-02) ; ou renvois (preuves, ADR-072, D-02bis) manquants ; ou texte existant d'O-3 modifié (`texte_origine_intact=non`, ou plus d'une ligne ajoutée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=.planning/BACKLOG.md; T=$(mktemp -d); git show origin/main:"$F" > "$T/src"; k=0; sec() { awk -v t="$1" "index(\$0, t)==1 {f=1; print; next} /^## /{f=0} f" "$2"; }; for pair in "## Protection de \`main\` côté GitHub — DIFFÉRÉ|: REPRIS ET TRAITÉ par renvoi**" "## Posture de protection de \`main\` — TRANCHÉ|: TRAITÉ par renvoi**"; do t=${pair%%|*}; m=${pair#*|}; sec "$t" "$F" > "$T/now"; sec "$t" "$T/src" > "$T/orig"; st=$(awk -v m="$m" "/^\\*\\*Statut \\(.*, Phase 41, volet admin\\) / && index(\$0, m) && index(\$0, \"ADR-072\") && index(\$0, \"41-PREUVES.md\") {c++} END{print c+0}" "$T/now"); awk "!/^\\*\\*Statut \\(.*, Phase 41, volet admin\\) /" "$T/now" | awk "NF || p {print} {p=NF}" > "$T/n2"; awk "NF || p {print} {p=NF}" "$T/orig" > "$T/o2"; ol=$(awk "END{print NR}" "$T/o2"); intact=$(cmp -s "$T/n2" "$T/o2" && [ "$ol" -gt 5 ] && echo oui || echo non); echo "BACKLOG [$t] : ligne_statut=$st lignes_existantes_intactes=$intact"; { [ "$st" -eq 1 ] && [ "$intact" = oui ]; } || k=$((k+1)); done; echo "BACKLOG : items_en_defaut=$k"; [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou, pour l'un des deux items, `ligne_statut=` différent de 1 (absente, dupliquée, mauvaise mention — « REPRIS ET TRAITÉ » pour l'item DIFFÉRÉ, « TRAITÉ » pour l'item TRANCHÉ —, ou sans renvoi ADR-072 et preuves) ; ou `lignes_existantes_intactes=non` (titre ou texte d'un item modifié)</fails_when>
  </verify>
  <acceptance_criteria>
    - précondition des mesures tenue (pose décidée, verdicts ou décisions, témoin discriminant) ; B contient `origin/main`
    - une ligne de statut O-3 « gardée par défaut + tracée » (volet admin), sans vocabulaire d'achèvement, texte existant intact
    - une ligne de statut sous chacun des deux items BACKLOG, lignes existantes intactes
  </acceptance_criteria>
  <done>Le statut d'O-3 et le BACKLOG disent ce que la mesure a établi.</done>
</task>

<task type="auto">
  <name>Task 2 : ADR-072 § Mesures à la pose, état daté dans CLAUDE.md, balayages O-3 et de fermeture, rejeu</name>
  <files>docs/ADR.md, CLAUDE.md</files>
  <read_first>
    - docs/ADR.md § ADR-072, sous-section « Amendement du 2026-09-23 »
    - CLAUDE.md § « Protection côté serveur — main et tags v* »
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  </read_first>
  <action>
`docs/ADR.md`, fin de la sous-section d'amendement d'ADR-072 (fin du fichier) : ajouter
`#### Mesures à la pose (Phase 41, volet admin, <date d'exécution>)`, une puce par mesure, valeurs
recopiées de `41-PREUVES.md`, au format exact :
- `- **M-1** : <dernier M1-VERDICT> — <une phrase>` ;
- `- **M-2** : bypass_relu=<POSE-BYPASS-RELU branche>, picmakpro=<M2 picmakpro_branche> — <entrées tags, valeurs de Samuel non mesurées>` ;
- `- **M-3** : <resultat de M3:> — <état avant, checks en cours, sortie de --admin>` ;
- `- **Provenance serveur** : rest=<valeur de PROVENANCE-SERVEUR:>, http=<code> — <une phrase>` ;
- `- **Provenance client** : co_i=<provenance de CO-I:>, ci_a=<provenance_a>, ci_b=<provenance_b> — <une phrase>` ;
- `- **Revue code owner par chemin** : pr_p=<CO-I review>, baseline=<CO-BASELINE review>, sentinelle=<CO-SENTINELLE review>, temoin=<CO-TEMOIN discriminant> — auteur des PR = seul propriétaire, d'où la preuve par reviewDecision` ;
- `- **Trace (ii)** : pull_request=<statut>, required_status_checks=<statut>, lu_par=<compte>, relue=<valeur> — contournement réel sur des checks en cours, pas sur une PR rouge` ;
- `- **Push direct** : acteur=<PUSH-DIRECT acteur>, refuse=<valeur>, provenance=<valeur> — <une phrase ; rappel : Samuel et Willy, en mode always, n'ont pas été testés, leur push serait accepté>` ;
- `- **M-4** : <dernier M4-VERDICT> — <hors liste : création, réécriture, suppression ; liste : traces de réécriture et de suppression>` ;
- `- **Accès aux rule suites** : picmakpro=<valeur>, samuel-neveugall=non_mesure` ;
- `- **PR en vol** : avant=<PR-EN-VOL-AVANT numeros>, apres=<PR-EN-VOL-APRES numeros> — <effet constaté>` ;
- `- **Décisions humaines** : …` recopiant chaque ligne `*-DECISION:` (option, décideur, canal, date).
Aucune valeur inventée ; O-3 n'y est associée à aucun mot d'achèvement.

`CLAUDE.md`, section « Protection côté serveur — main et tags v* » : ajouter, en dernière ligne de la
section, UNE ligne `**État (<date d'exécution>)** : posée le <date de POSE-DECISION> sur décision
explicite, prouvée par essais réels — mesures : ADR-072 § Mesures à la pose ; preuves :
`.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md`.` Aucune autre ligne du
fichier modifiée.

Commit local sur B, message « docs(41-10): mesures de la pose reportées dans ADR-072 et état daté
dans CLAUDE.md (D-09 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ; D-02bis —
arbitrage Willy, AskUserQuestion session principale, 2026-09-23) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; T=$(mktemp -d); awk "/^#### Mesures à la pose \\(Phase 41, volet admin, /{f=1} f" docs/ADR.md > "$T/m"; h=$(awk "END{print NR}" "$T/m"); am=$(awk "/^### Amendement du 2026-09-23 — posture côté serveur/{a=NR} /^#### Mesures à la pose \\(Phase 41, volet admin, /{m=NR} END{print (a>0 && m>a) ? \"oui\" : \"non\"}" docs/ADR.md); k=0; chk() { [ -n "$3" ] || { echo "VALEUR VIDE DANS 41-PREUVES : $1 $2"; k=$((k+1)); return 0; }; awk -v a="$1" -v b="$2$3" "index(\$0, a) && index(\$0, b) {f=1} END{exit !f}" "$T/m" || { echo "ABSENT : $1 $2$3"; k=$((k+1)); }; }; chk "**M-1** : " "" "$(last M1-VERDICT:)"; chk "**M-2** : " "bypass_relu=" "$(val POSE-BYPASS-RELU: branche)"; chk "**M-2** : " "picmakpro=" "$(val M2: picmakpro_branche)"; chk "**M-3** : " "" "$(val M3: resultat)"; chk "**Provenance serveur** : " "rest=" "$(val PROVENANCE-SERVEUR: rest)"; chk "**Provenance client** : " "co_i=" "$(val CO-I: provenance)"; chk "**Revue code owner par chemin** : " "pr_p=" "$(val CO-I: review)"; chk "**Revue code owner par chemin** : " "baseline=" "$(val CO-BASELINE: review)"; chk "**Revue code owner par chemin** : " "sentinelle=" "$(val CO-SENTINELLE: review)"; chk "**Revue code owner par chemin** : " "temoin=" "$(val CO-TEMOIN: discriminant)"; chk "**Trace (ii)** : " "pull_request=" "$(val TRACE-II: pull_request)"; chk "**Trace (ii)** : " "required_status_checks=" "$(val TRACE-II: required_status_checks)"; chk "**Push direct** : " "acteur=" "$(val PUSH-DIRECT: acteur)"; chk "**Push direct** : " "refuse=" "$(val PUSH-DIRECT: refuse)"; chk "**Push direct** : " "provenance=" "$(val PUSH-DIRECT: provenance)"; chk "**M-4** : " "" "$(last M4-VERDICT:)"; chk "**Accès aux rule suites** : " "picmakpro=" "$(val ACCES-RULE-SUITES: picmakpro)"; chk "**PR en vol** : " "avant=" "$(val PR-EN-VOL-AVANT: numeros)"; chk "**PR en vol** : " "apres=" "$(val PR-EN-VOL-APRES: numeros)"; chk "checks en cours" "" "pas sur une PR rouge"; awk "\$1 ~ /-DECISION:\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) print substr(\$i, 8) }" "$PH/41-PREUVES.md" > "$T/dec"; while IFS= read -r o; do [ -n "$o" ] || continue; awk -v o="$o" "index(\$0, o) {f=1} END{exit !f}" "$T/m" || { echo "DECISION NON REPORTEE : $o"; k=$((k+1)); }; done < "$T/dec"; nd=$(awk "END{print NR}" "$T/dec"); echo "ADR-072 mesures : lignes_section=$h dans_l_amendement=$am decisions_consignees=$nd valeurs_absentes=$k"; [ "$h" -gt 0 ] && [ "$am" = oui ] && [ "$k" -eq 0 ] && [ "$nd" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou sous-section absente ou hors de l'amendement du 2026-09-23 ; ou `VALEUR VIDE DANS 41-PREUVES :` (mesure jamais consignée) ; ou `ABSENT :` (valeur non reportée ou différente de `41-PREUVES.md`, nuance « checks en cours … pas sur une PR rouge » manquante) ; ou `DECISION NON REPORTEE :` ; ou aucune décision consignée (`POSE-DECISION` au moins doit exister)</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=CLAUDE.md; T=$(mktemp -d); git show origin/main:"$F" > "$T/o"; awk "/^## Protection côté serveur — main et tags v\\*\$/{f=1; next} /^## /{f=0} f" "$F" > "$T/s"; e=$(awk "/^\\*\\*État \\([0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\)\\*\\* : / && index(\$0, \"posée le\") && index(\$0, \"ADR-072\") && index(\$0, \"41-PREUVES.md\") {c++} END{print c+0}" "$T/s"); git diff -U0 origin/main -- "$F" > "$T/d"; rm=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/d"); ad=$(awk "/^\\+[^+]/ {c++} END{print c+0}" "$T/d"); echo "claude.md etat : ligne_etat=$e lignes_retirees=$rm lignes_ajoutees=$ad"; [ "$e" -eq 1 ] && [ "$rm" -eq 0 ] && [ "$ad" -le 2 ]'</automated>
    <fails_when>exit non nul ; ou ligne d'état absente, dupliquée, hors de la section « Protection côté serveur », ou sans « posée le », ADR-072 et renvoi aux preuves ; ou une ligne existante du `CLAUDE.md` retirée ou réécrite ; ou plus de deux lignes ajoutées (ligne d'état et ligne vide au plus)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md docs/ADR.md .planning/BACKLOG.md CLAUDE.md $PH/41-0*-SUMMARY.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition, O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02) — sur la doctrine ou les SUMMARY du volet admin (41-01 à 41-09) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh 2>&1) || rc=$?; printf "%s\n" "$out" | awk "/^perimetre: |^allowlist: |^limite: /"; h=$(printf "%s\n" "$out" | awk "!/^perimetre: |^allowlist: |^limite: / && NF {c++} END{print c+0}"); echo "aucune-fermeture rc=$rc hits=$h"; [ "$rc" -eq 0 ] && [ "$h" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `rc=` différent de 0 ; ou `hits=` différent de 0 — une ligne de la doctrine, du ledger, du CHANGELOG, du registre de preuves ou d'un SUMMARY fait co-occurrer O-3, une garde de la phase ou un de ses scripts avec un jeton d'achèvement hors allowlist, ou un porteur a perdu sa formule de limite de fond</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - ADR-072 § Mesures (dans l'amendement du 2026-09-23) recopiant chaque valeur consignée et chaque décision humaine, nuance des checks en cours comprise
    - ligne d'état datée dans la section « Protection côté serveur » du `CLAUDE.md`, rien d'autre modifié
    - balayage O-3 vert sur 25-SECURITY, ADR.md, BACKLOG, CLAUDE.md et les SUMMARY du volet admin ; `check-aucune-fermeture.sh` rc 0 sans hit
    - rejeux verts ; messages de commit de plan conformes
  </acceptance_criteria>
  <done>La doctrine dit ce que la mesure a établi, décisions comprises, et rien de plus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves → doctrine | un lecteur futur agit sur ce qu'ADR-072, `CLAUDE.md` et `25-SECURITY.md` affirment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-38 | Repudiation | O-3 présentée comme achevée | medium | mitigate | ligne de statut unique, vocabulaire d'achèvement absent, texte existant intact ; balayage par proposition ; `check-aucune-fermeture.sh` rc 0 |
| T-41-41 | Tampering | doctrine réécrite au-delà de la mesure | medium | mitigate | chaque valeur d'ADR-072 § Mesures recoupée avec `41-PREUVES.md`, décisions comprises ; `CLAUDE.md` limité à une ligne ajoutée |
| T-41-53 | Repudiation | décision humaine absente de la doctrine | medium | mitigate | chaque `*-DECISION:` doit être reportée (vérification `DECISION NON REPORTEE`) |
</threat_model>

<verification>
O-3, BACKLOG, ADR-072 et `CLAUDE.md` à jour sur pièce ; aucun artefact n'associe O-3 à un mot
d'achèvement ; recensement de fermeture vert.
</verification>

<success_criteria>
PROT-03 et PROT-04 ont leur doctrine sur pièce, prête à être publiée avec la PR des preuves.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-10-SUMMARY.md` when done — committé sur la branche B.
</output>

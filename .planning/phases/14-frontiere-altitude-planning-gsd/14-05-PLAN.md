---
phase: 14-frontiere-altitude-planning-gsd
plan: 05
type: execute
wave: 3
depends_on: [14-04]
files_modified:
  - plugin/commands/vf-planning.md  (description + corps alignés)
  - plugin/planning-core/references/domain-detection.md  (section « bascule dev »)
  - docs/ADR.md  (index + corps ADR-054)
autonomous: true
requirements: [ALTI-04]
must_haves:
  truths:
    - "La commande /vf-planning ne revendique plus « où en est-on ? » et renvoie vers les 4 verbes de projet"
    - "La commande explique le branchement (étape 0) : lab non-dev → socle adapté au métier ; lab dev → altitude lab + redirection"
    - "domain-detection.md porte une section « bascule dev » qui neutralise sa première ligne de grille (code → tronc .planning/) sans renier son principe"
    - "domain-detection.md réaffirme explicitement que le métier reste du JUGEMENT et que detect-gsd-engine.sh n'infère aucun métier"
    - "ADR-054 est présente À L'INDEX de docs/ADR.md ET en corps, au format des ADR voisines (Date, Statut, Décideur, Contexte, Problème, Options, Décision, Conséquences, Code Impacté)"
    - "L'ADR trace les 6 options pesées et leur verdict, dont les 5 rejetées"
    - "L'ADR nomme le risque assumé : aucune migration automatique d'un .planning/ existant"
    - "L'exception du Stop guard figure dans la Décision, avec sa motivation"
  artifacts:
    - path: "docs/ADR.md"
      provides: "Trace décisionnelle ADR-054 — la frontière d'altitude devient doctrine du repo"
      contains: "ADR-054"
---

# Plan 14-05 — Périphérie : commande, détection de domaine, ADR-054

## But

Rendre la frontière lisible **partout où elle se joue** : la porte d'entrée (`/vf-planning`), la
référence qui décidait jusqu'ici de scaffolder un tronc dev (`domain-detection.md`), et le registre
décisionnel du repo (ADR-054).

Complète le critère de succès 4 du ROADMAP. Vague 3 : dépend de 14-04 pour reprendre son vocabulaire à
l'identique — la commande et le SKILL ne doivent pas décrire deux comportements différents.

## Exigences couvertes

ALTI-04 (part ADR + surface ; la part doctrine est portée par 14-02).

## Tâches

Le **contenu exact** des trois fichiers est dans la **Task 5** du plan détaillé :
`docs/superpowers/plans/2026-07-25-rescope-vf-planning-gsd.md`.

1. Réécrire `plugin/commands/vf-planning.md` (frontmatter + corps : le branchement en 2 puces, puis la
   ligne de redirection vers `/vf-init`, `/vf-progress`, `/vf-plan`, `/vf-map`).
2. Insérer la section « Bascule dev → moteur de développement (ADR-054) » dans
   `references/domain-detection.md`, **après** la grille de lecture et **avant** l'auto-infusion.
3. Ajouter la ligne ADR-054 au tableau d'index de `docs/ADR.md` (à la suite d'ADR-053).
4. Écrire le corps de l'ADR-054 en fin de fichier, en calquant le gabarit d'ADR-050.

## Comment on vérifie (recette)

- `grep -c "où en est-on" plugin/commands/vf-planning.md` → **0**.
- `for v in vf-init vf-progress vf-plan vf-map; do grep -q "$v" plugin/commands/vf-planning.md || echo "MANQUE $v"; done` → aucune sortie.
- ADR-054 présente aux **deux** endroits : `grep -c "ADR-054" docs/ADR.md` ≥ 2, dont une ligne de tableau
  (`grep -c "^| ADR-054" docs/ADR.md` → 1) et un titre (`grep -c "^## ADR-054" docs/ADR.md` → 1).
- Les 6 options sont tracées : `sed -n '/^## ADR-054/,$p' docs/ADR.md | grep -c "Rejetée"` → **5**, plus une
  ligne `Retenue`.
- Le principe de `domain-detection.md` est réaffirmé, pas renié :
  `grep -c "jugement" plugin/planning-core/references/domain-detection.md` ≥ 3.
- Cohérence de vocabulaire commande ↔ SKILL : les deux emploient « altitude lab » et citent le même
  branchement (relecture croisée, pas de commande automatisable).

## Risques / points d'attention

- **Le piège de `domain-detection.md`** : sa première ligne de grille (« Code source, stack technique →
  Dev → profil Complet → extension `codebase/` ») conduisait précisément au comportement qu'on supprime. Ne
  pas la supprimer pour autant — elle reste juste comme *lecture du métier*. C'est sa **conséquence** qui
  change, et c'est ce que dit la nouvelle section.
- **Ne pas renier la doctrine anti-détecteur** en écrivant cette section. Le message doit rester : le métier
  se lit au sens, `detect-gsd-engine.sh` ne constate qu'un fait. Un lab de contenu avec un `package.json`
  déclenche un exit 2 et reste non-dev — c'est le comportement attendu, à écrire noir sur blanc.
- **ADR-054 doit citer la release** dans son Contexte (`v2.29.0 — planning-core v2.4.0`), comme le font ses
  voisines. Si le numéro de release changeait au plan 14-06, revenir corriger cette ligne.
- **Ne pas bumper les versions ici** : c'est le plan 14-06. Ce plan ne touche à aucun `VERSION` ni
  `module.json`.

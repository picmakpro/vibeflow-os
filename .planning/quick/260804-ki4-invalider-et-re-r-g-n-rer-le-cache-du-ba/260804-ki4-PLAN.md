---
phase: quick-260804-ki4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/vf-update-run.sh
  - plugin/conductor/scripts/tests/test-vf-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/README.md
  - plugin/conductor/CHANGELOG.md
  - plugin/conductor/skills/vf-update/SKILL.md
autonomous: true
requirements: [QUICK-260804-ki4]

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low        # 0 échantillon de calibration sur les tâches quick de ce repo

must_haves:
  truths:
    - "Après un /vf-update réussi, le bandeau SessionStart ne réclame plus la mise à jour dès le PREMIER redémarrage (aujourd'hui il faut deux redémarrages)."
    - "Si la régénération du cache échoue (réseau KO, vérificateur introuvable), aucun cache périmé ne subsiste : pas de bandeau plutôt qu'un faux positif."
    - "La version relue est la version POST-mise-à-jour : le vérificateur est pris dans le cache plugin le plus récent ($NEW), pas dans l'ancien ${CLAUDE_PLUGIN_ROOT}."
    - "vf-update-run.sh sort toujours 0 après la couche modules : le rafraîchissement du bandeau ne peut jamais faire échouer la mise à jour."
  artifacts:
    - "plugin/conductor/scripts/vf-update-run.sh — bloc de rafraîchissement du bandeau en queue de script"
    - "plugin/conductor/scripts/tests/test-vf-update.sh — 3 cas nouveaux + fixtures durcies (stub de vérificateur)"
    - "plugin/conductor/CHANGELOG.md — entrée v1.19.2"
    - "plugin/conductor/VERSION + module.json + README.md — triade alignée sur v1.19.2"
  key_links:
    - "$NEW/conductor/scripts/check-plugin-update.sh (copie fraîche, préférée) ↔ repli sur la copie voisine de vf-update-run.sh"
    - "${XDG_CACHE_HOME:-$HOME/.cache}/vibeflow/update-check.json — contrat partagé entre check-plugin-update.sh (écrivain) et update-banner.sh (lecteur)"
    - "fixtures $FAKE/<v>/conductor/scripts/ ↔ le cas existant « sélectionne 2.18.0 », qui appellerait sinon le VRAI vérificateur (appel réseau dans la suite de tests)"
---

<objective>
Le bandeau « mise à jour disponible » survit à `/vf-update` : il ne se tait qu'au SECOND
redémarrage. Rendre `vf-update-run.sh` responsable du cache qu'il périme — l'invalider puis le
régénérer depuis la version réellement installée.

Purpose: la boucle de mise à jour ment sur son propre résultat. L'utilisateur met à jour, redémarre,
et le framework lui redemande de mettre à jour. C'est le premier signal que voit une session, et il
est faux une fois sur deux.

Output: `vf-update-run.sh` corrigé, 3 cas de test discriminants, module `conductor` bumpé v1.19.2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@plugin/conductor/scripts/vf-update-run.sh
@plugin/conductor/scripts/check-plugin-update.sh
@plugin/conductor/scripts/update-banner.sh
@plugin/conductor/scripts/tests/test-vf-update.sh
@plugin/conductor/skills/vf-update/SKILL.md

**Cause racine (établie, ne pas ré-instruire).** `update-banner.sh` (hook SessionStart) lit
`${XDG_CACHE_HOME:-$HOME/.cache}/vibeflow/update-check.json`, écrit **en tâche de fond de la session
précédente** par `check-plugin-update.sh` (`update-banner.sh:56-67`). `/vf-update` ne touche jamais
ce fichier : le `update_available:true` d'avant la mise à jour survit donc au premier redémarrage.
Le rafraîchissement de fond de ce démarrage-là corrige l'état — pour le démarrage **suivant**.

**Pourquoi supprimer AVANT de régénérer, et pas régénérer seul.** `check-plugin-update.sh:76-79` :
réseau KO ⇒ le script **ne réécrit pas** le cache, il garde volontairement l'état précédent. Une
régénération seule laisserait donc le faux positif intact exactement dans le cas où l'on ne peut
pas le corriger. Pas de cache = pas de bandeau (`update-banner.sh:32` teste `os.path.isfile`), et le
prochain démarrage le reconstruit tout seul : la suppression est auto-cicatrisante, la régénération
est le confort.

**Pourquoi la relecture donne la bonne version.** `claude plugin update` (étape 4a du skill) tourne
**avant** `vf-update-run.sh` (étape 4b) et a déjà réécrit `~/.claude/plugins/installed_plugins.json`,
la source de `installed` (`check-plugin-update.sh:40-59`).

**Layout du cache plugin.** `$NEW` est un dossier de version sous
`$HOME/.claude/plugins/cache/vibeflow-os/vibeflow/` et miroite `plugin/` du dépôt : l'engine est en
`$NEW/_internal/vibeflow-update.sh` (déjà exécuté ligne 29), le vérificateur est donc en
`$NEW/conductor/scripts/check-plugin-update.sh`.

**Gates du dépôt à connaître.** `scripts/check-version-sync.sh` contrôle la triade par module
(`VERSION` ↔ `module.json`) **et** l'en-tête `**Version**` du README de module (`README.md:9`).
Le compteur « N suites » des README racine est dérivé de `find plugin scripts -path '*/tests/test-*.sh'` :
on **ajoute des cas à une suite existante**, jamais un fichier de suite — le compteur ne bouge pas.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Invalider puis régénérer le cache du bandeau en queue de vf-update-run.sh</name>
  <files>plugin/conductor/scripts/tests/test-vf-update.sh, plugin/conductor/scripts/vf-update-run.sh</files>
  <read_first>
    plugin/conductor/scripts/vf-update-run.sh (43 lignes, entier)
    plugin/conductor/scripts/check-plugin-update.sh:18-36 (chemin du cache + verrou) et :76-93 (repli réseau KO + écriture atomique)
    plugin/conductor/scripts/update-banner.sh:13 et :30-37 (lecteur du cache)
    plugin/conductor/scripts/tests/test-vf-update.sh:9-35 (bloc bandeau + bloc sélection de cache, dont la boucle de fixtures ligne 30)
  </read_first>
  <behavior>
    Écrire les cas AVANT le bloc de production, et les voir rouges.

    Piège de fixture à traiter EN PREMIER, sinon la suite part sur le réseau. `XDG_CACHE_HOME` est
    exporté ligne 10 et le cas existant « sélectionne 2.18.0 » (ligne 34) invoque le script réel :
    dès que le bloc de production existera, ce cas cherchera un vérificateur, n'en trouvera aucun
    dans `$FAKE/2.18.0/`, tombera sur le repli — c'est-à-dire sur le VRAI
    `check-plugin-update.sh`, qui fait un `git ls-remote` vers GitHub. Une suite de tests ne sort
    pas sur le réseau. Donc : étendre la boucle de fixtures (ligne 30) pour créer aussi
    `$FAKE/$v/conductor/scripts/check-plugin-update.sh`, un stub exécutable qui écrit un cache
    JSON dont le champ `installed` vaut le nom du dossier de version dont il provient. Ce stub
    éteint le risque réseau ET sert de sonde d'origine aux cas ci-dessous.

    - Cas A — régénération depuis la copie la plus fraîche : poser un cache périmé
      `{"update_available":true,...}`, lancer `vf-update-run.sh` sur la base de fixtures, puis
      relire le cache. Il doit exister, porter `update_available:false`, et son champ `installed`
      doit valoir `2.18.0` — ce qui prouve que le vérificateur pris est celui de `$NEW`, pas un
      autre.
    - Cas B — invalidation quand la régénération échoue : une seconde base de fixtures dont le
      stub de vérificateur sort 1 sans rien écrire. Poser le même cache périmé, lancer le script :
      le fichier de cache doit être ABSENT à l'arrivée. C'est le cas qui distingue « supprimer puis
      régénérer » de « régénérer seul » — une implémentation qui se contenterait de relancer le
      vérificateur laisserait ici le faux positif en place.
    - Cas C — best-effort : dans la situation du cas B, le code de retour de `vf-update-run.sh`
      reste 0. Une mise à jour réussie ne doit jamais être signalée en échec à cause du bandeau.

    Isoler `HOME` sur les trois cas (même motif que `run_banner`, ligne 18) : le repli du bloc de
    production et le chemin par défaut du cache dérivent tous deux de `$HOME`.
  </behavior>
  <action>
    Écrire d'abord les fixtures et les trois cas décrits en `<behavior>` dans
    `plugin/conductor/scripts/tests/test-vf-update.sh`, sous le bloc « vf-update-run.sh (sélection
    du cache semver le plus récent) ». Lancer la suite, constater les cas rouges.

    Puis ajouter dans `plugin/conductor/scripts/vf-update-run.sh` une fonction dédiée, définie après
    `run_scope()` et appelée en queue de script — entre la ligne « aucun module installé » et
    `exit 0`. Elle est appelée **inconditionnellement**, y compris quand `updated` vaut 0 : la
    péremption du cache vient de la version du **plugin** (mise à jour par l'étape 4a du skill),
    pas de la présence d'un registre de modules.

    Son corps, dans l'ordre : (1) `rm -f` sur
    `${XDG_CACHE_HOME:-$HOME/.cache}/vibeflow/update-check.json`, suffixé `2>/dev/null || true` ;
    (2) résolution du vérificateur — `$NEW/conductor/scripts/check-plugin-update.sh` en premier
    choix, repli sur la copie voisine de ce script (`$(cd "$(dirname "$0")" && pwd)`), et retour
    silencieux si aucune des deux n'existe ; (3) invocation `bash "$checker" >/dev/null 2>&1 || true`.
    Aucune de ces trois étapes ne peut faire sortir le script non nul — le script tourne sous
    `set -uo pipefail` sans `set -e`, ne pas l'y ajouter.

    Émettre exactement une ligne de résumé sur stdout, relayable par l'étape 4b du skill : une
    formulation pour le chemin nominal (cache invalidé puis régénéré) et une pour le chemin
    dégradé (cache invalidé, vérificateur introuvable, le prochain démarrage le régénérera).
    Préfixer `[vf-update-run]` comme les autres lignes du script.

    Commenter le bloc en français dense, façon `check-plugin-update.sh`, en disant le POURQUOI et
    non le QUOI. Trois faits doivent y figurer, chacun en une phrase : le bandeau lit un cache écrit
    en tâche de fond par la session précédente, d'où le faux positif qui survivait un redémarrage ;
    la suppression précède la régénération parce que le vérificateur garde délibérément l'ancien
    cache quand le réseau est KO ; la relecture est synchrone et donne la version post-mise-à-jour
    parce que l'étape 4a a déjà réécrit `installed_plugins.json`. Ajouter une ligne sur le verrou :
    si un rafraîchissement de fond tient `.check.lock`, le vérificateur sort immédiatement sans
    écrire — le cache reste alors absent, ce qui est le bon état de repli, pas une régression.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-vf-update.sh</automated>
  </verify>
  <done>
    La suite sort 0 avec les 3 cas nouveaux au vert et les 8 cas préexistants toujours verts. Le
    cas « sélectionne 2.18.0 » ne déclenche plus aucun accès réseau (fixtures stubées). Lancer la
    suite deux fois d'affilée donne le même résultat.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prouver la discriminance par mutation, puis passer les gates du dépôt</name>
  <files>plugin/conductor/scripts/vf-update-run.sh</files>
  <action>
    La culture du dépôt est qu'un test vert ne vaut rien tant qu'on n'a pas montré qu'il sait virer
    au rouge (cf. l'entrée v1.19.1 du CHANGELOG, et le faux vert de la Phase 19 qui a traversé trois
    étages de revue). Trois mutations, appliquées puis annulées une par une sur
    `vf-update-run.sh`, en relançant la suite à chaque fois :

    1. Retirer l'appel à la fonction en queue de script → les cas A, B doivent virer au rouge.
    2. Garder l'appel mais retirer le `rm -f` (régénération seule) → le cas B doit virer au rouge,
       le cas A doit rester vert. C'est la mutation qui compte : elle sépare l'invalidation de la
       régénération.
    3. Inverser l'ordre de résolution du vérificateur (repli voisin d'abord) → le cas A doit virer
       au rouge sur la valeur de `installed`.

    Si une mutation laisse la suite verte, le cas correspondant est tautologique : le réparer, pas
    l'accepter. Restaurer l'état correct après chaque mutation — vérifier par `git diff` en fin de
    tâche que le fichier ne porte que le bloc voulu.

    Puis rejouer les gates transverses : `bash scripts/check-version-sync.sh` (doit rester vert :
    aucune version n'a bougé à ce stade) et `bash plugin/conductor/scripts/tests/test-conductor.sh`
    (il inspecte la forme des scripts du module — vérifier que le bloc ajouté ne le fâche pas).
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-vf-update.sh &amp;&amp; bash plugin/conductor/scripts/tests/test-conductor.sh &amp;&amp; bash scripts/check-version-sync.sh</automated>
  </verify>
  <done>
    Les 3 mutations ont été appliquées et leur effet observé conforme à l'attendu ci-dessus ; le
    résultat de chacune est consigné dans le SUMMARY (mutation → cas rouges constatés). Les trois
    commandes de vérification sortent 0 sur l'arbre restauré.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump conductor v1.19.2 — triade, CHANGELOG, et vérité du skill</name>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md, plugin/conductor/CHANGELOG.md, plugin/conductor/skills/vf-update/SKILL.md</files>
  <read_first>
    plugin/conductor/CHANGELOG.md:1-40 (forme de l'entrée v1.19.1 — titre, rubrique « ### Corrigé », prose narrative)
    plugin/conductor/README.md:9 (ligne d'en-tête portant **Version**)
    plugin/conductor/skills/vf-update/SKILL.md:106-121 (étapes 4b et 5)
    scripts/check-version-sync.sh:73-121 (gates 6 et 8 — triade par module, en-tête README)
  </read_first>
  <action>
    Correctif sur script de module ⇒ **patch** : `v1.19.1` → `v1.19.2`, dans les trois emplacements
    que les gates 6 et 8 confrontent — `plugin/conductor/VERSION`, la clé `version` de
    `plugin/conductor/module.json`, et l'en-tête `**Version**` de `plugin/conductor/README.md`.

    Ne PAS toucher au `VERSION` racine, à `plugin/.claude-plugin/plugin.json`, à
    `.claude-plugin/marketplace.json` ni aux historiques des deux README racine : la release racine
    (bump + tag annoté + release GitHub) est un geste humain distinct, hors périmètre de cette
    tâche.

    Ajouter en tête de `plugin/conductor/CHANGELOG.md` une entrée `## [v1.19.2] — 2026-08-04` avec
    un titre parenthétique court, et une rubrique `### Corrigé` dans le registre du fichier : de la
    prose qui explique le mécanisme, pas une liste de fichiers. Ce qu'elle doit établir — le bandeau
    réclamait une mise à jour déjà faite parce qu'il lit un cache écrit en tâche de fond par la
    session précédente, que `/vf-update` ne touchait pas ; le faux positif ne se dissipait qu'au
    second redémarrage ; l'invalidation précède la régénération parce que le vérificateur garde
    délibérément l'ancien cache quand le réseau est KO, si bien qu'une régénération seule serait
    aveugle exactement là où elle est nécessaire ; le cache absent est un état de repli correct
    puisqu'il n'affiche rien et se reconstruit au démarrage suivant. Mentionner la mutation de la
    tâche 2 qui sépare l'invalidation de la régénération — c'est ce qui rend le correctif vérifié
    plutôt qu'affirmé.

    Enfin, remettre le skill d'accord avec ce que le script fait désormais : à l'étape 4b de
    `plugin/conductor/skills/vf-update/SKILL.md`, une proposition ajoutée à la phrase existante
    suffit — le script rafraîchit aussi le cache du bandeau de mise à jour, ce qui évite qu'il
    réclame au redémarrage une mise à jour déjà faite. Une ligne, pas un paragraphe : le fichier
    est sous plafond de densité ADR-029. Ne pas créer de flag, ne pas toucher à l'étape 5.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh &amp;&amp; grep -q 'v1.19.2' plugin/conductor/CHANGELOG.md &amp;&amp; bash plugin/conductor/scripts/tests/test-doc-and-commands.sh</automated>
  </verify>
  <done>
    `check-version-sync.sh` sort 0 (triade par module et en-tête README alignés sur v1.19.2, le
    compteur de suites des README racine inchangé puisqu'aucun fichier de suite n'a été ajouté).
    Le `VERSION` racine est identique à son état d'origine — `git diff --stat VERSION` ne renvoie
    rien. Le CHANGELOG porte l'entrée v1.19.2 en tête.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environnement → script | `XDG_CACHE_HOME` et `HOME` déterminent le chemin soumis à `rm -f` |
| cache plugin → exécution | `$NEW/conductor/scripts/check-plugin-update.sh` est exécuté depuis un dossier de cache téléchargé |
| réseau → cache | `check-plugin-update.sh` dérive `latest` d'un `git ls-remote` vers GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ki4-01 | Tampering | `rm -f "${XDG_CACHE_HOME:-$HOME/.cache}/vibeflow/update-check.json"` | low | accept | Chemin littéral après la variable : seul un fichier dont le suffixe est exactement `/vibeflow/update-check.json` peut être atteint. `XDG_CACHE_HOME` est déjà la source du même chemin dans `check-plugin-update.sh:18` et `update-banner.sh:13` — même frontière de confiance, aucune surface nouvelle. `rm -f` sur un lien symbolique supprime le lien, jamais sa cible. |
| T-ki4-02 | Elevation of Privilege | exécution de `$NEW/conductor/scripts/check-plugin-update.sh` | low | accept | `$NEW` est déjà la source de `$ENGINE`, exécuté sans condition en `vf-update-run.sh:29`. Exécuter un second script du même dossier n'élargit pas la surface : quiconque contrôle `$NEW` contrôle déjà l'engine. |
| T-ki4-03 | Denial of Service | appel réseau synchrone ajouté à `/vf-update` | low | mitigate | Aucun retry, aucune boucle. La latence est bornée en amont par `http.lowSpeedLimit=1` / `http.lowSpeedTime=10` et `GIT_TERMINAL_PROMPT=0` (`check-plugin-update.sh:70`), et par le verrou `.check.lock` qui fait sortir immédiatement un second appel concurrent. Sortie forcée à 0 par `|| true` : un réseau pendu ne peut pas faire échouer la mise à jour. |
| T-ki4-04 | Information Disclosure | fixtures de test touchant un vrai cache | medium | mitigate | La tâche 1 stube le vérificateur dans les fixtures : la suite n'émet aucune requête réseau et n'écrit que sous `XDG_CACHE_HOME` redirigé vers un `mktemp -d`. Sans ce durcissement, le cas préexistant « sélectionne 2.18.0 » ferait un `ls-remote` vers GitHub à chaque exécution de la CI. |

Aucun install de paquet (npm/pip/cargo) dans ce périmètre — la menace de chaîne
d'approvisionnement `T-*-SC` est sans objet ici.
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-vf-update.sh` → 0 KO, 3 cas nouveaux inclus.
- `bash plugin/conductor/scripts/tests/test-conductor.sh` → 0 KO.
- `bash scripts/check-version-sync.sh` → exit 0.
- La suite complète ne sollicite pas le réseau : la lancer avec l'interface réseau coupée, ou
  vérifier qu'aucun chemin ne rejoint le vrai `check-plugin-update.sh`.
- `git diff --stat` en fin de phase : exactement les 7 fichiers de `files_modified`, `VERSION`
  racine absent de la liste.
</verification>

<success_criteria>
- Un cache portant `update_available:true` posé avant `vf-update-run.sh` n'existe plus tel quel
  après son passage — soit réécrit à `false` avec la version post-mise-à-jour, soit absent.
- Le chemin dégradé (vérificateur en échec) laisse le cache absent et le script à 0.
- La séparation invalidation / régénération est prouvée par mutation, pas affirmée.
- Module `conductor` en v1.19.2 sur ses quatre emplacements, release racine intacte.
</success_criteria>

<output>
Créer `.planning/quick/260804-ki4-invalider-et-re-r-g-n-rer-le-cache-du-ba/260804-ki4-SUMMARY.md` en fin d'exécution.
Y consigner le résultat des 3 mutations de la tâche 2 (mutation → cas passés au rouge), sans quoi
la discriminance des nouveaux cas reste une affirmation.
</output>

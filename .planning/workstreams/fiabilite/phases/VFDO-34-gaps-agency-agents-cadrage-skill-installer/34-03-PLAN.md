---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md
autonomous: true
requirements: [AGTS-01]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le livrable d'AGTS-01 est une NOTE D'AUDIT (D-01) : la matrice division → module du 2026-07-20 (`.planning/BACKLOG.md:320-331`) est RE-MESURÉE à la date de la phase par commande — pas recopiée — et chaque ligne reçoit un verdict écrit : combler / reporter / refuser, avec la preuve qui le fonde. La re-mesure du corpus est vérifiée par machine contre l'état réel du dépôt : un agent ajouté ou retiré après l'écriture de la note la rend rouge."
    - "AUCUN agent n'est créé dans cette phase (D-01). Un gap jugé « à combler » devient un ITEM BACKLOG DATÉ avec sa preuve, rédigé dans la note et transcrit au ledger par 34-06 — jamais un fichier sous `plugin/`. Le diff scopé sur `plugin/` le prouve, il ne le déclare pas."
    - "La preuve qui rend un gap « à combler » est la règle de preuve du milestone (`.planning/REQUIREMENTS.md:910-912`, D-02) : un incident documenté, une demande externe (client, testeur, issue) ou un bug récurrent par construction. Un ❌ dans la matrice du catalogue NE SUFFIT PAS — la couverture du catalogue est une source d'inspiration, jamais une référence d'exigence. L'usage personnel de Samuel sans trace écrite ne suffit pas non plus (option explicitement écartée au cadrage)."
    - "Pour CHAQUE gap refusé, la note écrit EN TOUTES LETTRES que le ❌ du catalogue ne suffit pas, ET nomme la preuve précise qui manque (quel incident, quelle demande externe, quel bug récurrent aurait été nécessaire). Un refus sans preuve manquante nommée serait un refus par habitude — le compte des refus et le compte des preuves manquantes nommées sont comparés par machine."
    - "La note reprend la discipline Pitfall 12 (`.planning/research/PITFALLS.md:377-408`) telle quelle (D-03), en écrivant la PHRASE COMPLÈTE : « plus de 2-3 agents **ajoutés au catalogue** dans une PR » — et en nommant que le FAN-OUT D'EXÉCUTION (combien de sous-agents tournent simultanément pendant une mission) n'est borné que par la disjonction des périmètres et le budget, jamais par ce seuil. Précision de rédaction demandée par Samuel le 2026-09-15 et portée par `40-CONTEXT.md` § Claude's Discretion ; elle ne révise PAS D-03, elle le formule sans ambiguïté."
    - "La note écrit aussi qu'un futur comblement se fait UN AGENT À LA FOIS, gaté par `check-agents.sh` (ADR-044) et par la règle 4 de `check-capability-activation.sh` s'il est armé — jamais par lot."
    - "Le résultat de la recherche exhaustive de preuve menée le 2026-09-15 sur `.planning/` (BACKLOG, STATE, research/, missions/, phases/) est repris et ASSUMÉ comme base : aucune trace d'incident documenté, de demande externe ni de bug récurrent n'a été trouvée pour aucun des gaps 🟡/❌, en dehors de `web-test-team` — dont la justification n'est PAS la règle D-02 mais la dépendance explicite à AGTS-02, traitée en Volet 2 de la même phase (plans 34-01/34-04/34-05). Le risque résiduel de cette recherche (une preuve formulée avec des mots non couverts par les mots-clés employés) est nommé dans la note, pas passé sous silence."
    - "Zéro persona du catalogue `agency-agents` n'est importée, ni citée comme si elle était un livrable : le catalogue est distillé au niveau de la TAXONOMIE (quelles divisions, quels manques), jamais au niveau des fichiers — anti-feature gravée (`.planning/REQUIREMENTS.md:1094`), densité ADR-029 incompatible, aucune gouvernance côté catalogue source."
  artifacts:
    - ".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md : note d'audit portant une ligne `Corpus distribué mesuré : <a> agents de module + <b> AGENT.md = <t> fichiers` vérifiée contre l'état réel du dépôt, la matrice `| Division agency-agents | Module VibeFlow | Statut | Verdict |` re-mesurée avec un verdict par ligne, une section `## Gaps refusés` portant une ligne `**Preuve manquante :**` par refus, une section `## Discipline de comblement (Pitfall 12)`, une section `## Items backlog à créer` (rédaction prête à coller pour 34-06), une section `## Limite de la recherche de preuve` et une section `## Périmètre`."
  key_links:
    - "Ligne `Corpus distribué mesuré : …` <-> état réel de `plugin/` : c'est le seul endroit de la note qui peut rendre rouge tout seul. Si le parc bouge entre l'écriture et la relecture, la note se signale périmée au lieu de mentir silencieusement — exactement ce qui manquait à la matrice du 2026-07-20, recopiée depuis."
    - "Compte des verdicts `refuser` <-> compte des lignes `**Preuve manquante :**` : l'égalité machine est ce qui empêche un refus de passer sans que la preuve absente soit nommée. C'est la traduction exécutable de la Specific Idea de `34-CONTEXT.md` (« la note d'audit doit le dire en toutes lettres pour chaque gap refusé »)."
    - "Compte des verdicts `combler` <-> compte des items rédigés dans `## Items backlog à créer` : un gap « à combler » sans item rédigé serait une intention sans destination, et 34-06 n'aurait rien à transcrire."
    - "`## Discipline de comblement (Pitfall 12)` <-> Phase 40 : la Phase 40 dépend de cette phase et n'ajoute aucun agent au catalogue hormis `web-test-team` sous condition. La formulation exacte du seuil (ajout au catalogue) et de sa NON-application au fan-out d'exécution est ce qui évite que la 40 s'auto-bloque sur une lecture erronée du Pitfall."
  prohibitions:
    - "Ne JAMAIS importer, copier ou adapter une persona du catalogue `agency-agents` — anti-feature gravée au ledger (`.planning/REQUIREMENTS.md:1094`) ; la distillation porte sur la TAXONOMIE, jamais sur des fichiers."
    - "Ne JAMAIS créer le moindre agent pour un gap dans cette phase (D-01) — un gap jugé « à combler » devient un item backlog daté, pas un fichier sous `plugin/`."
    - "Ne JAMAIS fonder un verdict « à combler » sur un ❌ de la matrice du catalogue, ni sur l'usage personnel de Samuel sans trace écrite : seuls un incident documenté, une demande externe ou un bug récurrent par construction comptent (D-02)."
    - "Ne JAMAIS recopier la matrice du 2026-07-20 sans la re-mesurer : la ligne de corpus doit être produite par commande et vérifiable contre l'état réel du dépôt."
    - "Ne JAMAIS réviser la condition d'AGTS-02 dans cette note (D-06) — le sort de `web-test-team` appartient au Volet 2 de la phase, pas à cet audit."
    - "Ne JAMAIS écrire dans `.planning/BACKLOG.md`, `.planning/REQUIREMENTS.md` ou `.planning/PROJECT.md` depuis ce plan : les rédactions y sont préparées, la transcription appartient à 34-06."
---

<objective>
Rendre le verdict AGTS-01 sur pièces : re-mesurer la matrice division → module du catalogue
`agency-agents` à la date de la phase, poser un verdict écrit par gap (combler / reporter /
refuser) adossé à la règle de preuve du milestone, et produire `34-AUDIT-AGTS.md` — sans créer un
seul agent.

Purpose : l'item BACKLOG du 2026-07-20 propose une matrice de couverture et trois pistes
priorisées, mais aucune n'a jamais reçu de verdict. Le milestone a réduit AGTS à un arbitrage
précisément pour arrêter de reporter l'arbitrage. La valeur du livrable n'est pas la matrice — elle
existe déjà — c'est le VERDICT par gap et la preuve qui le fonde, plus le fait que la note se
signale périmée toute seule si le parc bouge.

Output : `34-AUDIT-AGTS.md`, unique fichier de ce dépôt touché par ce plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes déterministe a rendu les trois
exigences `unresolved`/`unclassified`. L'hypothèse portée par CE plan est déclarée ici,
explicitement — jamais résolue en silence par un filet, jamais supprimée.

**AGTS-01 — hypothèse signalée (probe : `unresolved` / `unclassified`).** L'exigence ne définit
nulle part le comportement attendu AUX ARÊTES de l'arbitrage. Ce plan ASSUME les quatre arêtes
suivantes, et les écrit dans la note :

1. **Un gap sans preuve D-02 reçoit `refuser`, pas `reporter`.** `reporter` est réservé aux gaps
   dont une preuve EXISTE mais dont le comblement est différé pour une autre raison (dépendance,
   séquencement). Sans cette convention, tout finirait en `reporter` et l'audit ne trancherait rien.
2. **`web-test-team` reçoit `reporter`, jamais `combler` ni `refuser`** : sa justification n'est pas
   la règle D-02 mais la dépendance à AGTS-02, déjà actée au ROADMAP et traitée par les plans
   34-01/34-04/34-05 de cette même phase. L'audit le NOMME et renvoie, il ne re-arbitre pas (D-06).
3. **Une division « Couvert ✅ » ne reçoit pas de verdict de gap** : la colonne Verdict y porte
   explicitement `sans objet`, pas une case vide — une case vide serait indistinguable d'un oubli.
4. **La recherche de preuve est textuelle et donc faillible** : une preuve formulée avec des mots
   non couverts par les mots-clés employés resterait invisible. Cette limite est écrite dans la note
   (section dédiée), et elle ne convertit aucun `refuser` en `à réexaminer` — elle dit seulement à
   quelle condition un refus serait à rouvrir.

Aucune de ces quatre arêtes n'est couverte par un `D-xx` de `34-CONTEXT.md`. Si Samuel les arbitre
autrement, c'est la note qui est relue.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Tâche 1 : tranche verticale « matrice re-mesurée » — corpus compté par commande, matrice reconstruite, ligne de corpus vérifiable contre le dépôt</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md</files>
  <read_first>
    - `.planning/BACKLOG.md` lignes 303-348 — l'item d'origine du 2026-07-20 : la matrice division → module, les trois pistes priorisées, « ce qu'on n'en prend PAS ». C'est le gabarit à re-mesurer colonne par colonne, pas à recopier
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Volet 1 » en entier — corpus déjà listé module par module (25 agents + 6 `AGENT.md`), périmètre exact de la CI, absence de `web-test-team` : à CONFIRMER par commande, pas à croire sur parole
    - `.github/workflows/ci.yml` lignes 245-320 — la définition MACHINE du corpus « agents distribués » (ce que le job `gates` scanne réellement) : c'est elle qui fait autorité sur ce qui compte comme agent distribué, pas une liste de rapport
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « `34-AUDIT-AGTS.md` (note d'audit AGTS-01) » — structure de la matrice à reprendre et format d'entrée BACKLOG à imiter
    - `docs/ADR.md` autour des lignes 52 et 60 — ADR-029 (densité ≤ 250 lignes) et ADR-044 (agents natifs machine-enforced), les deux raisons de fond du refus d'import
  </read_first>
  <action>
    Reconstruire la matrice sur des mesures fraîches, et la rendre capable de se signaler périmée.

    (1) **Compter le corpus par commande**, avec la définition machine de la CI : le nombre de
    fichiers `plugin/<module>/agents/*.md` (premier niveau) et le nombre de fichiers
    `plugin/<module>/AGENT.md`. Consigner les deux nombres ET leur somme. Consigner aussi
    explicitement ce que ce périmètre EXCLUT délibérément, en reprenant le commentaire de
    `.github/workflows/ci.yml` : les blueprints, le contenu de référence et d'exemples, et les
    agents internes au moteur `skill-creator` (propriété tierce, non gouvernés par
    `check-agents.sh`).

    (2) **Écrire la ligne de corpus au format exact**
    `Corpus distribué mesuré : <a> agents de module + <b> AGENT.md = <t> fichiers`, suivie de la
    date de mesure. Ce format n'est pas décoratif : la vérification de cette tâche recompte le
    dépôt et échoue si la note et le réel divergent. C'est ce qui manquait à la matrice du
    2026-07-20 — recopiée depuis, sans jamais pouvoir se signaler fausse.

    (3) **Confirmer l'absence de `web-test-team`** dans `plugin/` et le consigner, avec la commande
    et sa sortie.

    (4) **Reconstruire la matrice** au format
    `| Division agency-agents | Module VibeFlow | Statut | Verdict |`, en reprenant les onze lignes
    de divisions de l'item d'origine et en re-mesurant la colonne « Module VibeFlow » contre le
    parc réel (les modules cités existent-ils encore, sous ce nom ?). Laisser la colonne Verdict
    remplie de `à trancher` à ce stade : elle est posée en tâche 2. Une colonne Verdict absente
    rendrait la tâche 2 impossible à vérifier.

    (5) **Ouvrir la note** avec un chapeau (objet, date, ce qu'elle tranche et ce qu'elle ne
    tranche pas), la section `## Corpus mesuré`, la section `## Matrice division → module
    (re-mesurée)`, et la section `## Périmètre` portant la ligne `SHA de base : <sha>` issue de
    `git rev-parse HEAD`.
  </action>
  <behavior>
    - Les deux comptes sont produits par commande, pas recopiés depuis la recherche.
    - La ligne de corpus de la note correspond EXACTEMENT au recomptage du dépôt.
    - La matrice a quatre colonnes, dont `Verdict`, et onze lignes de division.
    - L'absence de `plugin/web-test-team` est mesurée et consignée avec sa commande.
  </behavior>
  <acceptance_criteria>
    - `34-AUDIT-AGTS.md` existe et contient les sections `## Corpus mesuré`, `## Matrice division → module (re-mesurée)` et `## Périmètre`.
    - `34-AUDIT-AGTS.md` contient une ligne `Corpus distribué mesuré : <a> agents de module + <b> AGENT.md = <t> fichiers` dont les trois nombres correspondent au recomptage réel de `plugin/` au moment de la vérification.
    - `34-AUDIT-AGTS.md` contient une ligne d'en-tête de tableau commençant par `| Division agency-agents |` et comportant une colonne `Verdict`.
    - Le tableau comporte au moins onze lignes de données (les onze divisions de l'item d'origine).
    - `34-AUDIT-AGTS.md` contient une ligne `SHA de base : <sha>` à 40 caractères hexadécimaux.
    - Aucun fichier sous `plugin/` n'a été créé ou modifié.
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md && test -f "$N" && grep -q '^## Corpus mesuré' "$N" && grep -q '^## Périmètre' "$N" && grep -qE '^SHA de base : [0-9a-f]{40}$' "$N" && grep -q '^| Division agency-agents |' "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && test -n "$B" && git diff --quiet "$B" -- plugin && node -e 'const fs=require("fs"),p=require("path");let a=0,b=0;for(const d of fs.readdirSync("plugin",{withFileTypes:true})){if(!d.isDirectory())continue;const ad=p.join("plugin",d.name,"agents");if(fs.existsSync(ad))a+=fs.readdirSync(ad).filter(f=>f.endsWith(".md")).length;if(fs.existsSync(p.join("plugin",d.name,"AGENT.md")))b++;}const t=a+b;const s=fs.readFileSync(process.argv[1],"utf8");const want="Corpus distribué mesuré : "+a+" agents de module + "+b+" AGENT.md = "+t+" fichiers";if(!s.includes(want)){console.log("CORPUS DESYNC: attendu <"+want+">");process.exit(1);}const hdr=s.split("\n").findIndex(l=>l.startsWith("| Division agency-agents |"));if(hdr<0||!s.split("\n")[hdr].includes("Verdict")){console.log("MATRICE SANS COLONNE VERDICT");process.exit(1);}const rows=s.split("\n").slice(hdr+2).filter(l=>l.startsWith("|"));if(rows.length<11){console.log("MATRICE TROP COURTE: "+rows.length+" lignes");process.exit(1);}' "$N"</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `CORPUS DESYNC: attendu <…>`, `MATRICE SANS COLONNE VERDICT`, `MATRICE TROP COURTE: <n> lignes` apparaît. Causes discriminantes : la note est absente ou il lui manque `## Corpus mesuré` / `## Périmètre` / la ligne `SHA de base :` ; la ligne de corpus de la note ne correspond plus au recomptage réel de `plugin/` (le parc a bougé, ou la note a été recopiée au lieu d'être mesurée — c'est précisément ce que cette vérification existe pour attraper) ; l'en-tête de matrice n'a pas de colonne `Verdict` ; moins de onze lignes de données ; un fichier sous `plugin/` a été touché alors que D-01 interdit toute création d'agent.</fails_when>
  </verify>
  <done>La matrice est re-mesurée, sa colonne Verdict est prête, et la note porte une ligne de corpus qui la rend capable de se signaler périmée toute seule.</done>
  <reversibility rating="reversible">D-01 : une note et des items backlog n'engagent aucun contrat distribué.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : verdict par gap et règle de preuve — chaque refus nomme la preuve qui manque</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md</files>
  <precondition>La tâche 1 est verte : la matrice existe avec sa colonne Verdict et la ligne de corpus correspond au dépôt réel.</precondition>
  <read_first>
    - `34-AUDIT-AGTS.md` dans son état de fin de tâche 1 — la matrice à compléter
    - `.planning/REQUIREMENTS.md` lignes 906-915 — QUAL-01 et la règle de preuve du milestone avec les coupes du 2026-08-15 : le texte qui fait autorité sur ce qui compte comme preuve
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md` § D-02 et § Specific Ideas — la formule à reprendre en toutes lettres pour chaque refus
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Recherche de preuve (règle D-02) — résultat : aucune trouvée » — les chemins fouillés, les mots-clés employés, et la seule piste avec une justification écrite
    - `.planning/BACKLOG.md` lignes 335-348 — les trois pistes priorisées et le paragraphe « Ce qu'on n'en prend PAS »
  </read_first>
  <action>
    Poser un verdict par ligne, et rendre chaque refus opposable.

    (1) **Remplir la colonne Verdict.** Un mot par ligne, choisi parmi `combler`, `reporter`,
    `refuser`, `sans objet`. Conventions, écrites dans la note juste au-dessus de la matrice :
    `sans objet` pour les divisions déjà couvertes ✅ (jamais une case vide, indistinguable d'un
    oubli) ; `reporter` réservé aux gaps dont une preuve EXISTE mais dont le comblement est différé
    pour une raison de séquencement — c'est le cas de `web-test-team`, dont la justification n'est
    pas la règle D-02 mais la dépendance à AGTS-02 déjà actée au ROADMAP et traitée par les plans
    34-01/34-04/34-05 de la même phase (le nommer et renvoyer, ne pas re-arbitrer : D-06) ;
    `refuser` pour tout gap sans preuve au sens D-02 ; `combler` uniquement si une preuve existe
    et est citable.

    (2) **Section `## Gaps refusés`.** Pour CHAQUE ligne portant `refuser`, un bloc qui : nomme le
    gap, écrit en toutes lettres la formule de D-02 (un ❌ dans la matrice du catalogue ne suffit
    pas — la couverture du catalogue est une source d'inspiration, jamais une référence
    d'exigence), et porte une ligne `**Preuve manquante :**` nommant PRÉCISÉMENT ce qui aurait été
    nécessaire — quel incident, quelle demande externe, quel bug récurrent par construction.
    « Aucune preuve » n'est pas une preuve manquante nommée : dire laquelle.

    (3) **Section `## Gaps à combler`** si et seulement si au moins une ligne porte `combler` :
    pour chacune, la preuve citée avec sa source exacte (fichier, ligne, date). Sans source
    citable, le verdict n'est pas `combler`.

    (4) **Section `## Limite de la recherche de preuve`.** Écrire que la recherche du 2026-09-15
    était TEXTUELLE (chemins fouillés et mots-clés employés, repris de la recherche), qu'une preuve
    formulée autrement resterait invisible, et à quelle condition précise un refus serait à rouvrir.
    Cette limite ne convertit aucun verdict : elle dit comment le contester.

    (5) **Rappeler que l'usage personnel sans trace écrite ne compte pas** — option explicitement
    écartée au cadrage du 2026-09-14, à écrire dans la note pour que la question ne se rejoue pas
    de mémoire.
  </action>
  <behavior>
    - Chaque ligne de la matrice porte exactement un verdict parmi les quatre valeurs admises.
    - Le nombre de blocs `**Preuve manquante :**` est égal au nombre de verdicts `refuser`.
    - `web-test-team` porte `reporter` et renvoie au Volet 2 de la phase, sans re-arbitrage.
    - La section `## Limite de la recherche de preuve` existe et nomme les mots-clés employés.
  </behavior>
  <acceptance_criteria>
    - Chaque ligne de données de la matrice porte une cellule Verdict valant exactement `combler`, `reporter`, `refuser` ou `sans objet`.
    - `34-AUDIT-AGTS.md` contient une section `## Gaps refusés`, et le nombre de lignes `**Preuve manquante :**` y est égal au nombre de verdicts `refuser` de la matrice.
    - `34-AUDIT-AGTS.md` contient la formule de D-02 en toutes lettres, au moins une fois : la chaîne `source d'inspiration, jamais une référence d'exigence`.
    - `34-AUDIT-AGTS.md` contient une section `## Limite de la recherche de preuve`.
    - `34-AUDIT-AGTS.md` contient la chaîne `web-test-team` associée au verdict `reporter`.
    - Aucun fichier sous `plugin/` n'a été créé ou modifié.
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md && grep -q '^## Gaps refusés' "$N" && grep -q '^## Limite de la recherche de preuve' "$N" && grep -q "source d'inspiration, jamais une référence d'exigence" "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && test -n "$B" && git diff --quiet "$B" -- plugin && node -e 'const fs=require("fs");const L=fs.readFileSync(process.argv[1],"utf8").split("\n");const h=L.findIndex(l=>l.startsWith("| Division agency-agents |"));if(h<0){console.log("MATRICE ABSENTE");process.exit(1);}const rows=L.slice(h+2).filter(l=>l.startsWith("|"));const ok=["combler","reporter","refuser","sans objet"];let refus=0,bad=[];for(const r of rows){const c=r.split("|").map(x=>x.trim()).filter(x=>x.length);const v=c[c.length-1];if(!ok.includes(v)){bad.push(v||"(vide)");continue;}if(v==="refuser")refus++;}if(bad.length){console.log("VERDICT INVALIDE: "+bad.join(" / "));process.exit(1);}const preuves=L.filter(l=>l.includes("**Preuve manquante :**")).length;if(preuves!==refus){console.log("DESEQUILIBRE: "+refus+" refus pour "+preuves+" preuves manquantes nommees");process.exit(1);}const wtt=L.filter(l=>l.includes("web-test-team")&&l.startsWith("|")&&l.includes("reporter")).length;if(wtt<1){console.log("WEB-TEST-TEAM SANS VERDICT REPORTER");process.exit(1);}' "$N"</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `MATRICE ABSENTE`, `VERDICT INVALIDE: <valeurs>`, `DESEQUILIBRE: <n> refus pour <m> preuves manquantes nommees`, `WEB-TEST-TEAM SANS VERDICT REPORTER` apparaît. Causes discriminantes : une ligne de matrice n'a pas de verdict, ou porte un mot hors des quatre admis (une case vide se lit `(vide)` dans la sortie) ; le nombre de refus ne correspond pas au nombre de preuves manquantes nommées — c'est le cas d'un refus posé par habitude, sans dire ce qui manque ; `web-test-team` n'a pas le verdict `reporter` (l'audit re-arbitrerait AGTS-02, ce que D-06 interdit) ; la formule de D-02 n'apparaît pas en toutes lettres ; un fichier sous `plugin/` a été touché.</fails_when>
  </verify>
  <done>Chaque gap a un verdict, chaque refus nomme la preuve précise qui lui manque, et la limite de la recherche est écrite au lieu d'être tue.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : discipline de comblement (Pitfall 12, phrase complète) et items backlog prêts à coller</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md</files>
  <precondition>La tâche 2 est verte : tous les verdicts sont posés et les refus sont adossés à une preuve manquante nommée.</precondition>
  <read_first>
    - `34-AUDIT-AGTS.md` dans son état de fin de tâche 2 — les verdicts posés commandent le contenu de cette tâche
    - `.planning/research/PITFALLS.md` lignes 377-408 — Pitfall 12 dans son texte intégral : le double risque, les signes d'alerte, et la prescription « un agent à la fois »
    - `.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-CONTEXT.md` § Claude's Discretion, dernière puce — la précision de rédaction demandée par Samuel le 2026-09-15, à porter ici, qui ne révise PAS D-03
    - `plugin/conductor/scripts/check-agents.sh` en-tête et zone des champs requis — ce que le gate exige réellement d'un agent (name, description, model, memory, effort)
    - `plugin/dev-orchestrator/scripts/check-capability-activation.sh` lignes 86-118 et 723-770 — la règle 4 et ses bornes, à citer correctement (elle ne s'applique que si elle est armée)
    - `.planning/BACKLOG.md` lignes 295-301 — l'entrée précédant l'item agency-agents : structure complète d'une entrée BACKLOG à imiter
  </read_first>
  <action>
    Écrire la discipline de comblement sans ambiguïté, puis préparer ce que 34-06 transcrira.

    (1) **Section `## Discipline de comblement (Pitfall 12)`.** Reprendre la mise en garde du
    Pitfall telle quelle, en écrivant la PHRASE COMPLÈTE, sans la tronquer :
    « plus de 2-3 agents **ajoutés au catalogue** dans une PR » est le signal d'alarme. Puis, dans
    le même paragraphe, nommer explicitement ce que ce seuil NE borne PAS : le fan-out d'exécution
    — combien de sous-agents tournent simultanément pendant une mission — n'est borné que par la
    disjonction des périmètres d'écriture et par le budget, jamais par ce seuil. Cette précision
    est une précision de RÉDACTION demandée par Samuel le 2026-09-15 (arbitrage porté par
    `40-CONTEXT.md` § Claude's Discretion) ; elle ne révise pas D-03 et doit être présentée comme
    telle. Écrire enfin qu'un futur comblement se fait UN AGENT À LA FOIS, chacun passant
    `check-agents.sh` (ADR-044) et la règle 4 de `check-capability-activation.sh` si elle est
    armée — et qu'un agent dont la description recouvre une cible déjà routée est un synonyme, donc
    un rejet doctrinal.

    (2) **Section `## Ce qu'on n'en prend pas`.** Reprendre le paragraphe de l'item d'origine :
    pas de catalogue plat, pas de densité incompatible, aucun copier-coller direct dans `plugin/`.
    Rappeler l'anti-feature gravée (`.planning/REQUIREMENTS.md:1094`) et ses deux raisons de fond
    (ADR-029, zéro gouvernance côté catalogue source).

    (3) **Section `## Items backlog à créer`.** Pour CHAQUE verdict `combler` de la matrice, une
    entrée rédigée PRÊTE À COLLER au gabarit BACKLOG déjà en place : `## <Titre du gap>`,
    `**Capturé :** <date> · **À explorer :** <déclencheur>`, `**Cadrage.**`, `**Pourquoi
    différé :**`, `**Déclencheur de resurgence :**`, et la preuve citée avec sa source. S'il n'y a
    aucun verdict `combler`, écrire la section avec la mention explicite « aucun item à créer » et
    la raison — une section absente serait indistinguable d'un oubli. Le geste d'écriture dans
    `.planning/BACKLOG.md` appartient à 34-06, pas à ce plan.

    (4) **Section `## Conséquence ledger`.** Ce que 34-06 doit faire de cette note : quelle case
    cocher, quel item BACKLOG mettre à jour et avec quel texte, quelle ligne de la table de
    traçabilité des exigences amender. Écrire les textes, pas des intentions.

    (5) **Clôture de périmètre.** Compléter `## Périmètre` : reprendre le `SHA de base` de la tâche
    1 et prouver par un diff scopé qu'aucun fichier hors du dossier de phase n'a été touché — en
    particulier zéro fichier sous `plugin/`, ce qui est la traduction exécutable de « aucun agent
    n'est créé dans cette phase ».
  </action>
  <behavior>
    - La phrase complète du seuil (ajout au catalogue) figure dans la note, non tronquée.
    - La note dit explicitement que le fan-out d'exécution n'est pas borné par ce seuil.
    - Le nombre d'entrées rédigées dans `## Items backlog à créer` est égal au nombre de verdicts
      `combler` (zéro item et une mention explicite si aucun `combler`).
    - Le diff scopé contre le SHA de base est vide sur tout ce qui n'est pas le dossier de phase.
  </behavior>
  <acceptance_criteria>
    - `34-AUDIT-AGTS.md` contient les sections `## Discipline de comblement (Pitfall 12)`, `## Ce qu'on n'en prend pas`, `## Items backlog à créer` et `## Conséquence ledger`.
    - `34-AUDIT-AGTS.md` contient la chaîne `plus de 2-3 agents **ajoutés au catalogue** dans une PR`.
    - `34-AUDIT-AGTS.md` contient la chaîne `la disjonction des périmètres` et la chaîne `un agent à la fois`.
    - `34-AUDIT-AGTS.md` contient la chaîne `check-agents.sh`.
    - Le nombre de lignes `**Capturé :**` de la section `## Items backlog à créer` est égal au nombre de verdicts `combler` de la matrice ; si ce nombre est zéro, la note contient la chaîne `aucun item à créer`.
    - `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 (forme sans `..HEAD` : l'arbre de travail est comparé au SHA de base, donc un fichier déjà commité par ce plan est vu lui aussi).
  </acceptance_criteria>
  <verify>
    <automated>N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md && grep -q '^## Discipline de comblement (Pitfall 12)' "$N" && grep -q "^## Ce qu'on n'en prend pas" "$N" && grep -q '^## Items backlog à créer' "$N" && grep -q '^## Conséquence ledger' "$N" && grep -qF 'plus de 2-3 agents **ajoutés au catalogue** dans une PR' "$N" && grep -qF 'la disjonction des périmètres' "$N" && grep -qF 'un agent à la fois' "$N" && grep -qF 'check-agents.sh' "$N" && B=$(sed -n 's/^SHA de base : //p' "$N" | head -1) && test -n "$B" && git diff --quiet "$B" -- plugin scripts docs manual .github README.md README.fr.md && node -e 'const fs=require("fs");const L=fs.readFileSync(process.argv[1],"utf8").split("\n");const h=L.findIndex(l=>l.startsWith("| Division agency-agents |"));const rows=L.slice(h+2).filter(l=>l.startsWith("|"));let combler=0;for(const r of rows){const c=r.split("|").map(x=>x.trim()).filter(x=>x.length);if(c[c.length-1]==="combler")combler++;}const s=L.findIndex(l=>l.startsWith("## Items backlog à créer"));if(s<0){console.log("SECTION ITEMS ABSENTE");process.exit(1);}let e=L.slice(s+1).findIndex(l=>/^## /.test(l));e=e<0?L.length:s+1+e;const items=L.slice(s,e).filter(l=>l.includes("**Capturé :**")).length;if(items!==combler){console.log("ITEMS DESEQUILIBRES: "+combler+" verdict(s) combler pour "+items+" item(s) redige(s)");process.exit(1);}if(combler===0&&!L.slice(s,e).join("\n").includes("aucun item à créer")){console.log("ZERO COMBLER SANS MENTION EXPLICITE");process.exit(1);}' "$N"</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `SECTION ITEMS ABSENTE`, `ITEMS DESEQUILIBRES: <n> verdict(s) combler pour <m> item(s) redige(s)`, `ZERO COMBLER SANS MENTION EXPLICITE` apparaît. Causes discriminantes : une des quatre sections manque ; la phrase de seuil est tronquée ou reformulée (c'est exactement l'ambiguïté que la précision du 2026-09-15 existe pour fermer) ; la note ne dit pas que le fan-out d'exécution est borné par la disjonction des périmètres ; la prescription « un agent à la fois » ou le gate `check-agents.sh` ne sont pas nommés ; un verdict `combler` n'a pas son item rédigé (34-06 n'aurait rien à transcrire), ou un item est rédigé sans verdict correspondant ; zéro `combler` sans la mention explicite qui distingue « rien à créer » d'un oubli ; `git diff --quiet` rend 1, c'est-à-dire qu'un fichier de production a été touché — la traduction exécutable de « aucun agent n'est créé dans cette phase ».</fails_when>
    <human-check>Relire `34-AUDIT-AGTS.md` : un lecteur qui ne connaît pas le catalogue comprend-il, pour chaque gap refusé, CE QUI aurait fallu pour que le verdict soit autre ? La discipline de comblement se lit-elle sans qu'on puisse confondre le seuil d'ajout au catalogue avec une borne sur le nombre de sous-agents qui tournent en mission ?</human-check>
  </verify>
  <done>La note est complète et opposable : verdicts posés, refus argumentés, discipline de comblement formulée sans ambiguïté, et ce que 34-06 doit transcrire est écrit mot pour mot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalogue tiers `agency-agents` (MIT, 230+ personas) → parc d'agents VibeFlow | frontière DOCTRINALE, pas technique : rien n'est téléchargé ni exécuté dans ce plan. Le risque n'est pas l'exécution d'un tiers, c'est l'IMPORT de contenu non gouverné dans un parc qui l'est (ADR-044, ADR-029) |
| Note d'audit → décisions futures de périmètre produit | ce que la note écrit devient la référence des phases suivantes (la 40 en dépend) : une formulation ambiguë y produit des blocages ou des dérives réels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-09 | Tampering | parc d'agents distribué (`plugin/*/agents/*.md`, `plugin/*/AGENT.md`) | medium | mitigate | aucun fichier sous `plugin/` n'est écrit par ce plan, et la vérification de CHAQUE tâche l'exige par un diff scopé — la prohibition d'import de persona est ainsi machine-vérifiée, pas seulement déclarée ; aucun contenu du catalogue tiers n'est téléchargé ni copié |
| T-34-10 | Repudiation | matrice recopiée présentée comme re-mesurée | medium | mitigate | la ligne `Corpus distribué mesuré : …` est recomptée contre l'état réel du dépôt par la vérification de la tâche 1 : une note recopiée depuis le 2026-07-20 devient rouge dès que le parc bouge, au lieu de mentir silencieusement |
| T-34-11 | Repudiation | refus de gap posé sans motif opposable | medium | mitigate | égalité machine entre le nombre de verdicts `refuser` et le nombre de lignes `**Preuve manquante :**` — un refus non argumenté échoue la vérification de la tâche 2 |
| T-34-12 | Information Disclosure | note d'audit publiée dans un dépôt public | low | accept | la note ne contient que des éléments déjà publics (noms de modules, taxonomie d'un catalogue MIT public, décisions de périmètre déjà présentes au ledger) ; aucun secret, aucune donnée personnelle, aucun chemin de machine (gate `check-machine-paths.sh` en CI) |

Aucun install de paquet (`npm`/`pip`/`cargo`) n'entre dans ce plan — la section « Package Legitimacy
Audit » de `34-RESEARCH.md` est déclarée non applicable à la phase, aucune menace `T-34-SC` n'est
ouverte ici. Le catalogue `agency-agents` est CITÉ, jamais cloné ni installé.
</threat_model>

<verification>
1. `34-AUDIT-AGTS.md` existe et porte une ligne `Corpus distribué mesuré : <a> agents de module + <b> AGENT.md = <t> fichiers` qui correspond au recomptage réel de `plugin/`.
2. La matrice a une colonne `Verdict`, au moins onze lignes de données, et chaque ligne porte exactement un verdict parmi `combler` / `reporter` / `refuser` / `sans objet`.
3. Le nombre de verdicts `refuser` est égal au nombre de lignes `**Preuve manquante :**` de la section `## Gaps refusés`.
4. `web-test-team` porte le verdict `reporter` et renvoie au Volet 2 de la phase, sans re-arbitrage (D-06 respecté).
5. La phrase complète du seuil d'ajout au catalogue figure non tronquée, et la note dit explicitement que le fan-out d'exécution n'est borné que par la disjonction des périmètres et le budget.
6. Le nombre d'items rédigés dans `## Items backlog à créer` est égal au nombre de verdicts `combler` ; zéro `combler` s'accompagne d'une mention explicite.
7. `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 — aucun agent créé, D-01 respecté et prouvé.
8. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-03-SUMMARY.md` existe et consigne littéralement les mesures ci-dessus.
</verification>

<success_criteria>
- Chaque gap de la matrice a un verdict ÉCRIT, adossé à la règle de preuve du milestone — l'arbitrage n'est plus reporté.
- Chaque refus nomme la preuve précise qui lui manque : un lecteur sait ce qui le rouvrirait.
- La note se signale périmée toute seule si le parc d'agents bouge — elle ne peut pas mentir en silence comme la matrice de 2026-07-20 l'a fait pendant deux mois.
- La discipline Pitfall 12 est formulée sans ambiguïté possible entre le seuil d'AJOUT AU CATALOGUE et le fan-out d'exécution, pour que la Phase 40 ne s'auto-bloque pas sur une lecture erronée.
- Zéro agent créé, zéro persona importée, et le diff scopé le prouve.
- 34-06 dispose de rédactions prêtes à coller pour le ledger, pas d'intentions à réinterpréter.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| `34-AUDIT-AGTS.md` | note de décision (audit de couverture) — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md` |
| `34-03-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-03-SUMMARY.md` |
| Entrées BACKLOG des gaps « à combler » | texte rédigé DANS la note § `## Items backlog à créer`, transcrit par 34-06 | — |
| Rédaction de mise à jour de l'item agency-agents | texte rédigé DANS la note § `## Conséquence ledger`, transcrit par 34-06 | — |

Aucun symbole de code, aucun agent, aucun module n'est produit par ce plan — c'est le point.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-03-SUMMARY.md` en fin
de plan, consignant : les deux comptes du corpus et la commande qui les a produits, la matrice
finale avec ses verdicts, le décompte refus / preuves manquantes et combler / items rédigés, le SHA
de base et le résultat du diff scopé, et tout écart constaté entre ce plan et l'état réel du dépôt
au moment de l'exécution (notamment si le parc d'agents a bougé depuis la recherche du 2026-09-15).
</output>

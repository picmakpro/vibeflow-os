---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 06
type: execute
wave: 4
depends_on: ["43-02", "43-03", "43-04", "43-05", "43-07"]
files_modified:
  - plugin/conductor/skills/vf-calibrate/SKILL.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/README.md
  - plugin/conductor/CHANGELOG.md
autonomous: true
requirements: [FABR-06, FABR-07, FABR-09, FABR-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "vf-calibrate/SKILL.md ne nomme plus seulement vf-mcp-consumer : la ré-affirmation MCP vise les agents flaggés vf-mcp-consumer (allowlist large) ET ceux qui portent vf-mcp-tools (allowlist nommée, ex. vf-reviewer) (durcissement c, dernier site)"
    - "conductor passe en mineure (valeur à la base de phase B43 + 0.1.0, comparée explicitement à la VERSION du module lue à B43) avec VERSION, module.json, ligne Version du README et CHANGELOG cohérents ; le README catalogue check-skills.sh et décrit ce que font désormais check-agents.sh (sept listes, grammaire vf-mcp-tools) et check-instruction-budget.sh (SKILL.md plafonnés à 500 lignes, socle du bootstrap borné par la ligne de baseline `@bootstrap:socle`) ; aucun bump racine"
    - "Le README et le CHANGELOG disent explicitement que check-skills.sh n'est PAS câblé au SessionStart dans cette phase, et pourquoi"
    - "Au rejeu final, toutes les suites voisines sont vertes, le corpus réel passe check-skills.sh et check-agents.sh, le budget réel est vert (aucun rouge budgétaire n'est excusé, hausse du socle au-dessus de `@bootstrap:socle` comprise), check-version-sync.sh, check-blueprints.sh, G-2 (check-gate-touche.sh) et G-1 (check-baseline-arbitrage.sh) rendent un verdict conforme, et les deux témoins lab frais (HOME temporaire) sont rejoués"
    - "README, CHANGELOG et couverture de FABR-09 déclarée au SUMMARY décrivent l'option ratchet-socle consignée dans 43-04-SUMMARY.md — ligne `@bootstrap:socle` dont toute hausse bloque, plafond ADR-029 de 2000 signalé par AU-DESSUS-PLAFOND-ADR029, non bloquant — avec la citation « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » mot pour mot ; FABR-09 déclaré couvert en entier"
    - "Aucun fichier sous .github/ n'est modifié par la phase"
    - "Le SUMMARY de clôture porte les sections « Relecture Samuel » (dev-orchestrator, _internal, et la baseline CODEOWNERS — commit de 43-04 qui pose `@bootstrap:socle`), « Couverture FABR-09 », « Résidus signalés » et la table complète « Artifacts this phase produces »"
  artifacts:
    - path: "plugin/conductor/skills/vf-calibrate/SKILL.md"
      provides: "ré-affirmation MCP à deux déclarations"
      contains: "vf-mcp-tools"
    - path: "plugin/conductor/CHANGELOG.md"
      provides: "entrée mineure Phase 43"
      contains: "check-skills.sh"
    - path: "plugin/conductor/README.md"
      provides: "entrée check-skills.sh, compte de scripts re-dérivé"
      contains: "check-skills.sh"
  key_links:
    - from: "plugin/conductor/README.md et CHANGELOG.md"
      to: "en-têtes de check-skills.sh, check-agents.sh, check-instruction-budget.sh"
      via: "la documentation résume les en-têtes, jamais l'inverse"
      pattern: "check-skills.sh"
  prohibitions:
    - statement: "MUST NOT présenter une décision de cadrage ou de planification de Claude comme un arbitrage humain dans le CHANGELOG, les commits ou la PR ; toute décision humaine y est citée avec son canal et sa date (CLAUDE.md, traçabilité des arbitrages)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit"
    - statement: "MUST NOT câbler check-skills.sh au SessionStart dans cette phase : tant que le corpus n'est pas mis en conformité (D-Q5), chaque session de chaque lab porterait un avertissement permanent que l'utilisateur ne peut pas corriger (spec §1.2 : des avertissements qui désensibilisent au signal)"
      status: unresolved
      reason: "décision de plan relue en PR ; hooks.json absent de files_modified"
---

<objective>
Fermer la phase : corriger le dernier texte à une seule clé MCP (vf-calibrate), bumper conductor en
mineure avec une documentation qui dit ce que font désormais ses trois gates, rejouer tout ce que
la phase a pu casser, et poser les relevés de relecture et de résidus pour la PR.

Purpose: FABR-10 (durcissement c, site vf-calibrate/SKILL.md:92) ; release notes de module pour
FABR-06, FABR-07, FABR-09 ; preuve de non-régression de bout en bout.
Output: conductor en mineure, README et CHANGELOG à jour, rejeu vert, SUMMARY de clôture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-01-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-02-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-04-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-05-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-07-SUMMARY.md

Garde-fou du jalon (ROADMAP.md, en-tête gouvernance) : aucune release du jalon avant la clôture de
`fiabilite-v1.0` ; conductor est partagé avec fiabilite — lire la version sur disque au moment de
l'exécution (v1.43.0 à la rédaction), et au rebase après un merge de fiabilite, seul le numéro
attendu change, jamais l'ordre ni le contenu du commit de bump.

**Option bootstrap de 43-04 — ratchet-socle, tranchée (révision du 2026-09-26, tour 4).**
Décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26
(43-CONTEXT.md, bloc « Quatrième temps (2026-09-26) ») ; 43-04 la consigne dans 43-04-SUMMARY.md
(Tâche 2, plus un checkpoint) et l'applique (Tâche 3). L'entrée check-instruction-budget.sh du
README, la puce bootstrap du CHANGELOG, la couverture de FABR-09 déclarée au SUMMARY et la lecture
du budget au rejeu décrivent cette option et elle seule : la mesure du jour du socle minimal
(fermeture `resolve-deps.sh conductor` + skill `installer` + commandes du plugin) est la ligne de
baseline `@bootstrap:socle` ; toute hausse au-dessus de cette ligne bloque (DEPASSEMENT-BOOTSTRAP,
rc 1 sous ratchet armé) ; le plafond ADR-029 de 2000 tokens reste un objectif signalé
(AU-DESSUS-PLAFOND-ADR029), non bloquant, dont le retour est inscrit au BACKLOG (commit `433fea0`) ;
la citation est reprise mot pour mot, sur une seule ligne. FABR-09 est couvert en entier (plafond
des SKILL.md et socle du bootstrap). Garde-fou : si 43-04-SUMMARY.md ne porte pas « ratchet-socle »
et la citation, arrêt et retour à l'orchestrateur — jamais une option supposée. Aucun rouge du
budget ne s'excuse : 43-04 a prouvé le dépôt réel vert avec la ligne égale à la mesure
(REEL-BOOT rc=0).

**Échéance du manifeste daté (révision du 2026-09-26).** `check-agents-manifest.json` porte six
listes `verifie_le: 2026-09-23`, `valide_jours: 30` ; la septième (43-01) porte la date de sa
relecture. Une liste est périmée quand son âge dépasse strictement 30 jours : les six listes
d'origine le sont à partir du **2026-10-24** (la septième à sa `verifie_le` + 31 jours). À partir de
cette date, les quatre appels `--manifest-freshness=strict` de `.github/workflows/ci.yml`
(re-grepper ; l.267, 297, 325 et 2060 le 2026-09-26) rendent INDÉTERMINÉ (exit 3,
« MANIFESTE-PERIME ») et leurs étapes CI sont ROUGES — sur la branche comme sur main, sans aucune
régression de la phase ; les rejeux locaux de ce plan (fraîcheur par défaut) restent verts et ne le
voient pas ; check-skills.sh n'en fait qu'un avertissement. Le jalon gouvernance ne s'exécute pas
avant la clôture de fiabilite-v1.0, date inconnue : ce cas est PROBABLE. Conduite si l'exécution de
ce plan ou la PR tombe à cette date ou après : ne JAMAIS re-dater depuis ce plan, ni changer une date
seule (règle `rafraichissement` du manifeste) ; s'arrêter (précondition de la Tâche 2) et renvoyer à
l'orchestrateur un geste de rafraîchissement SÉPARÉ : relire la source officielle de chaque liste
périmée, comparer aux valeurs, mettre les valeurs à jour, re-dater `verifie_le` au jour de la
relecture, faire valider la relecture par un humain et citer cet arbitrage (canal + date) dans un
commit dédié `chore(conductor): rafraîchissement du manifeste daté (sources relues le <date>)`,
portant son propre trailer
`Gate-Touche: plugin/conductor/scripts/check-agents-manifest.json — rafraîchissement, sources relues le <date>` ;
si fiabilite a déjà rafraîchi le manifeste sur main, rebaser au lieu de rafraîchir une seconde
fois ; puis reprendre la Tâche 2. Si la PR est ouverte avant le 2026-10-24 mais que sa CI est
rejouée après, même conduite, avant tout nouveau rejeu. Le rafraîchissement vient toujours après
l'exécution de 43-01 (son critère MANIFESTE-7-OK compare les six listes à la base de phase).
Référence de phase (B43, révision du 2026-09-26) : un SHA FIGÉ, jamais re-dérivé, consigné une
seule fois par 43-01, son seul propriétaire (étape 0 de sa Tâche 1, avant tout commit de la phase) sur la ligne
unique « Base de phase : <sha de 40 caractères> » du fichier neutre
`.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md` —
jamais dans un PLAN.md, jamais re-dérivé par un `git log` sur les PLAN.md : une révision de PLAN.md
pendant l'exécution (cas prévu pour la réponse à Q-PORTEE, finalement consignée dans 43-02-PLAN.md
dès la planification, tour 4 du 2026-09-26) aurait sinon déplacé la base après 43-01, 43-03,
43-04 et 43-05, et vidé en silence, tout en restant verts, les contrôles de ce plan (« aucun bump racine »,
« aucun .github touché », relevé « Relecture Samuel »). Ce plan la LIT par la chaîne
`B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && …` :
base absente, malformée ou sortie de l'histoire → rien n'est imprimé, et « rien d'imprimé » vaut
échec. Pourquoi pas la base de branche : la branche porte encore la Phase 42 tant que la PR #108
n'est pas mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de
la base de branche — mesuré le 2026-09-25). Rebase (merge de fiabilite) : voir 43-01, bloc de
contexte (nouvelle base déterministe — parent du commit de création du fichier — ou arrêt en rouge
BASE-PHASE-INTROUVABLE, jamais une base choisie par l'exécutant) ;
un rafraîchissement séparé du manifeste ne touche pas la base. Distincte de la « Base T2 » de
43-06-SUMMARY.md (critère de périmètre). Les gardes G-1 et G-2 gardent leur propre base : elles
jugent toute la branche, Phase 42 comprise.
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : vf-calibrate à deux déclarations, conductor en mineure avec README et CHANGELOG</name>
  <files>plugin/conductor/skills/vf-calibrate/SKILL.md, plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/conductor/skills/vf-calibrate/SKILL.md (fichier modifié : re-grepper vf-mcp-consumer — occurrence unique mesurée à la l.92 le 2026-09-25, dans l'étape 3 « Ré-affirmer l'allowlist MCP des agents exécutants », l.87-96)
    - plugin/conductor/README.md (fichier modifié : l.9 ligne Version ; l.81 SessionStart ; l.86-112 « Scripts (27) » et sa note de re-dérivation, entrées check-agents.sh et check-instruction-budget.sh ; l.167-172 « **Tests** : 29 suites » et sa note de re-dérivation ; l.185 ligne `scripts/` de l'arborescence, « 28 scripts … (29 suites) »)
    - plugin/conductor/CHANGELOG.md (fichier modifié : forme de l'entrée v1.43.0)
    - plugin/conductor/VERSION, plugin/conductor/module.json
    - en-têtes finaux de plugin/conductor/scripts/check-skills.sh, check-agents.sh, check-instruction-budget.sh (sources que la documentation résume)
    - 43-04-SUMMARY (décision « ratchet-socle », citation, valeur de la ligne `@bootstrap:socle`, hash du commit de baseline) ; 43-02-SUMMARY (mesure de dérive du corpus sous la règle Q-PORTEE)
    - CLAUDE.md (aucun bump racine ; traçabilité des arbitrages)
  </read_first>
  <action>
1. vf-calibrate/SKILL.md, étape 3 : la phrase qui cible « les agents flaggés vf-mcp-consumer » vise désormais « les agents flaggés `vf-mcp-consumer` (allowlist large) ou porteurs de `vf-mcp-tools` (allowlist nommée, ex. `vf-reviewer`) » ; la commande d'injection qui suit ne change pas (elle traite déjà les deux, inject-mcp-tools.sh l.357-367) ; aucune autre ligne ne change, aucune ligne description: ni aucun titre.
2. README de conductor :
   - nouvelle entrée `check-skills.sh` sous « Gates machine » : nature des skills (`vf-nature`, défaut « outil »), procédure sans `ecrit:` ni `vf-rubrique-juge` refusée, dérive déclaration/prose en avertissement (D-Q5), même manifeste daté que check-agents.sh, contrat 0/1/3 ; NON câblé au SessionStart dans cette phase — lancé par la CI (suite et cas sur l'arbre réel) et par skill-creator (`--file`) ; câblage à poser avec la mise en conformité du corpus (backlog e36e6f2) ;
   - entrée check-agents.sh : manifeste à sept listes (dont les champs natifs des SKILL.md) et grammaire `vf-mcp-tools` validée ;
   - entrée check-instruction-budget.sh : SKILL.md plafonnés à 500 lignes (hors modules doc-only), puis la phrase bootstrap de l'option ratchet-socle (bloc de contexte) : « socle du bootstrap (fermeture de conductor + skill installer + commandes du plugin) borné par la ligne de baseline `@bootstrap:socle` : toute hausse bloque (DEPASSEMENT-BOOTSTRAP) ; au-dessus du plafond ADR-029 de 2000 tokens, AU-DESSUS-PLAFOND-ADR029 est signalé sans bloquer (objectif) », suivie de la citation mot pour mot sur une seule ligne ; 43-04-SUMMARY.md sans « ratchet-socle » ou sans la citation → arrêt ;
   - comptes écrits en dur, re-mesurés et corrigés (mesure du planificateur le 2026-09-26, avant la phase : 29 scripts `*.sh` au premier niveau de `plugin/conductor/scripts/` alors que le README dit « Scripts (27) » au titre et 28 dans la note et l'arborescence ; 30 suites `test-*.sh` sous `scripts/tests/` alors qu'il dit 29 l.167 et l.185, et 27 dans la note l.171 ; check-skills.sh et sa suite en ajoutent chacun un) : rejouer au moment de l'exécution `find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'` et `find plugin/conductor/scripts/tests -maxdepth 1 -type f -name 'test-*.sh' | awk 'END{print NR}'`, puis écrire les valeurs mesurées — jamais un nombre deviné ni recopié d'ici — au titre « Scripts (N) », dans sa note de re-dérivation (re-datée au jour de l'exécution), sur la ligne « **Tests** : N suites », dans sa note de re-dérivation (re-datée) et sur la ligne `scripts/` de l'arborescence ;
   - ligne « **Version** » (l.9).
3. Bump MINEUR de conductor depuis sa valeur à la base de phase (`git show "$B43:plugin/conductor/VERSION"`, égale à la valeur sur disque avant le bump ; v1.43.0 à la rédaction → v1.44.0) : VERSION, `.version` de module.json. Aucune VERSION racine, ni plugin.json, ni marketplace.json, ni README racine.
4. CHANGELOG, entrée en tête « ## [<VERSION>] — <date d'exécution ISO> (Phase 43 — gate des skills par nature, budget des SKILL.md et du bootstrap) », « **Minor** (nouveau gate des skills) : » + puces : check-skills.sh (FABR-06, D-Q1, D-Q2) ; dérive en avertissement et mesure du corpus réel (FABR-07, D-Q5, écart assumé vis-à-vis de D-11 de la Phase 42) ; manifeste à sept listes (FABR-09) ; plafond des SKILL.md (FABR-09, D-Q4) et une puce bootstrap ratchet-socle (FABR-09 : ligne `@bootstrap:socle` dont toute hausse bloque, AU-DESSUS-PLAFOND-ADR029 non bloquant, retour sous 2000 inscrit au BACKLOG), citation reprise mot pour mot sur une seule ligne ; grammaire vf-mcp-tools au gate des agents et vf-calibrate à deux déclarations (FABR-10, D-Q3) ; hook SessionStart non câblé (motif) ; décisions de Willy citées « AskUserQuestion, session principale, 2026-09-24 », et les deux décisions du 2026-09-26 (bootstrap ratchet-socle, règle de portée de la dérive) citées « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » ; décisions de planification (noms de clés, vocabulaire de dérive, exclusion doc-only, octets ÷ 4) présentées comme telles, jamais comme un arbitrage.
5. Commits : `fix(conductor): vf-calibrate ré-affirme les deux déclarations MCP (FABR-10 c)` puis `feat(conductor <VERSION>): gate des skills par nature, budget des SKILL.md et du bootstrap (Phase 43)` — hors surface G-2 (aucun check-*.sh ni suite dans ces commits).
  </action>
  <verify>
    <automated>f=plugin/conductor/skills/vf-calibrate/SKILL.md; grep -c 'vf-mcp-tools' "$f"; B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && d=$(git diff -U0 "$B43" HEAD -- "$f") && printf '%s\n' "$d" | grep -cE '^[-+](description:|#)'</automated>
    <fails_when>la première valeur vaut 0, ou la seconde n'est pas 0 ou est absente (base de phase figée illisible ou hors de l'histoire)</fails_when>
    <automated>B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && vr=$(git show "$B43:plugin/conductor/VERSION") && vb=$(printf '%s' "$vr" | tr -d '[:space:]') && att=$(printf '%s\n' "$vb" | awk -F. '/^v[0-9]+\.[0-9]+\.[0-9]+$/{sub(/^v/,"",$1); printf "v%d.%d.0", $1, $2+1}') && test -n "$att" && v=$(tr -d '[:space:]' < plugin/conductor/VERSION) && test "$v" = "$att" && python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["version"])' plugin/conductor/module.json | grep -qx "$v" && grep -q "Version\*\* : $v" plugin/conductor/README.md && head -5 plugin/conductor/CHANGELOG.md | grep -q "^## \[$v\]" && grep -q 'check-skills.sh' plugin/conductor/README.md && grep -q 'check-skills.sh' plugin/conductor/CHANGELOG.md && bash scripts/check-version-sync.sh >/dev/null 2>&1 && echo "CONDUCTOR-VERSION-OK base=$vb attendu=$att obtenu=$v"</automated>
    <fails_when>la ligne « CONDUCTOR-VERSION-OK base=… attendu=… obtenu=… » est absente : base de phase figée illisible ou hors de l'histoire ; VERSION encore égale à sa valeur à B43 (aucun bump : `test "$v" = "$att"` échoue) ; incrément différent de la MINEURE attendue (valeur à B43 + 0.1.0, patch remis à 0) ; triade, README ou CHANGELOG non alignés ; check-skills.sh absent du README ou du CHANGELOG ; ou check-version-sync.sh non nul. Mesuré le 2026-09-26 avant écriture (S3, tour 3) : l'ancienne sonde n'avait aucune comparaison à la base — sa partie triade était déjà verte (v1.43.0 partout) ; la comparaison explicite à `git show "$B43:plugin/conductor/VERSION"` rougit tant que le bump n'est pas fait.</fails_when>
    <automated>n=$(find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'); s=$(find plugin/conductor/scripts/tests -maxdepth 1 -type f -name 'test-*.sh' | awk 'END{print NR}'); R=plugin/conductor/README.md; grep -qF "## Scripts ($n) — par famille" "$R" || echo "COMPTE-FAUX titre-scripts"; grep -qF "**Tests** : $s suites sous" "$R" || echo "COMPTE-FAUX ligne-tests"; grep -qF "# $n scripts (familles ci-dessus) + tests/ ($s suites)" "$R" || echo "COMPTE-FAUX arborescence"; echo "COMPTES-FIN scripts=$n suites=$s"</automated>
    <fails_when>une ligne COMPTE-FAUX apparaît avant « COMPTES-FIN » : le titre « Scripts (N) », la ligne « **Tests** : N suites » ou la ligne `scripts/` de l'arborescence ne porte pas le compte mesuré au moment du verify. Mesuré le 2026-09-26 avant la phase : les trois lignes sont fausses (27 et 28 écrits pour 29 scripts ; 29 écrit pour 30 suites) — la sonde rougit sur le README actuel.</fails_when>
    <automated>S=$(ls .planning/workstreams/gouvernance/phases/VFDO-43-*/43-04-SUMMARY.md); C='décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26'; R=plugin/conductor/README.md; top=$(awk '/^## \[/{n++} n==1' plugin/conductor/CHANGELOG.md); printf 'BOOTSTRAP-DOC summary=%s summary_cit=%s readme=%s readme_objectif=%s readme_cit=%s changelog=%s changelog_objectif=%s changelog_cit=%s\n' "$(grep -c 'ratchet-socle' "$S")" "$(grep -cF "$C" "$S")" "$(grep -c '@bootstrap:socle' "$R")" "$(grep -c 'AU-DESSUS-PLAFOND-ADR029' "$R")" "$(grep -cF "$C" "$R")" "$(printf '%s\n' "$top" | grep -c '@bootstrap:socle')" "$(printf '%s\n' "$top" | grep -c 'AU-DESSUS-PLAFOND-ADR029')" "$(printf '%s\n' "$top" | grep -cF "$C")"</automated>
    <fails_when>l'une des huit valeurs de la ligne « BOOTSTRAP-DOC » vaut 0 : 43-04-SUMMARY.md ne consigne pas ratchet-socle et sa citation, ou le README (entrée check-instruction-budget.sh) ou l'entrée en tête du CHANGELOG ne décrit pas la ligne `@bootstrap:socle`, le jeton non bloquant AU-DESSUS-PLAFOND-ADR029 ou la citation mot pour mot sur une seule ligne</fails_when>
  </verify>
  <acceptance_criteria>
    - vf-calibrate/SKILL.md nomme vf-mcp-tools, sans ligne description: ni titre modifié
    - les comptes du README sont ceux mesurés à l'exécution : la sonde des comptes imprime `COMPTES-FIN scripts=<n> suites=<s>` sans aucune ligne COMPTE-FAUX, et les deux notes de re-dérivation portent la date d'exécution et ces mêmes valeurs
    - README (entrée check-instruction-budget.sh) et entrée en tête du CHANGELOG décrivent l'option ratchet-socle — ligne `@bootstrap:socle` dont toute hausse bloque, plafond ADR-029 signalé par AU-DESSUS-PLAFOND-ADR029, non bloquant — avec la citation mot pour mot : la sonde imprime `BOOTSTRAP-DOC` sans aucune valeur nulle
    - bump de conductor prouvé contre la base de phase : la deuxième commande du verify imprime `CONDUCTOR-VERSION-OK base=<valeur à B43> attendu=<valeur à B43 + 0.1.0> obtenu=<la même>` (rouge tant que VERSION égale sa valeur à B43) ; triade cohérente ; `bash scripts/check-version-sync.sh` sort 0 ; `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && liste=$(git diff --name-only "$B43" HEAD -- VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json README.md README.fr.md) && printf '%s' "$liste" | grep -c .` rend 0 (rien d'imprimé — base de phase figée illisible — vaut échec)
    - README : entrée check-skills.sh présente, mention « non câblé au SessionStart » et son motif
    - CHANGELOG : entrée en tête à la nouvelle version, qui cite les décisions de Willy avec canal et date et nomme les décisions de planification comme telles
  </acceptance_criteria>
  <done>Le dernier texte à une seule clé MCP est corrigé ; conductor est en mineure avec une documentation qui dit exactement ce que font ses gates et ce qui n'est pas câblé.</done>
</task>

<task type="auto">
  <name>Tâche 2 : rejeu complet, gardes G-1 et G-2, témoins lab frais, relevés de relecture et de résidus</name>
  <precondition>aucune liste du manifeste daté n'est périmée le jour de l'exécution — `bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --file plugin/conductor/AGENT.md` ne rend pas 3 ; sinon arrêt sans rien écrire et rafraîchissement séparé (bloc de contexte, « Échéance du manifeste daté »), jamais un re-datage depuis ce plan</precondition>
  <files>plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - tous les SUMMARY 43-01 à 43-05 et 43-07 (hashes, mesures, décision bootstrap ratchet-socle et signalement à Samuel de 43-04, relevé Samuel de 43-05 et son complément en 43-07)
    - scripts/check-gate-touche.sh (l.1-90 : surface G-2, forme du trailer, un chemin par trailer)
    - scripts/check-baseline-arbitrage.sh (l.1-90 : ce que G-1 voit et ne voit pas)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-06-PLAN.md (forme de la Tâche 3 : REJEU-FIN, relevé Samuel)
  </read_first>
  <action>
1. Au démarrage (après un éventuel rafraîchissement séparé du manifeste), écrire la base de la tâche (`git rev-parse HEAD`) sur une ligne dédiée « Base T2 : <sha> » de 43-06-SUMMARY.md, créé dès maintenant (ligne hors du frontmatter, en début de ligne, conservée) : le critère de périmètre la relit. Puis rejouer les suites voisines, le budget réel, la synchro des versions, les blueprints, le corpus réel des deux gates et les deux témoins lab frais (commandes de vérification ; toute installation sous HOME temporaire, jamais le HOME réel ; aucune commande ne dépend de la date du jour).
2. Si un rejeu rougit : ne corriger AUCUN fichier depuis cette tâche hors de `plugin/conductor/CHANGELOG.md` — ni une ligne, ni avec un trailer Gate-Touche, ni dans le fichier d'un autre plan ; consigner le rouge (commande, sortie utile) au SUMMARY, s'arrêter et le renvoyer à l'orchestrateur : la correction repart par une révision séparée du plan fautif (fermeture d'écart), puis le rejeu reprend ici. Jamais un test affaibli.
3. Si une incohérence de documentation apparaît au rejeu, corriger seulement le CHANGELOG de conductor (seul fichier de cette tâche) ; sinon cette tâche ne modifie aucun fichier suivi hors de `.planning/`.
4. SUMMARY de clôture (commit `.planning/` séparé) :
   - « Relecture Samuel » (hashes écrits sous ce titre même, aucun sous-titre dans la section) : sortie de `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && git log --format='%h %s' "$B43"..HEAD -- plugin/dev-orchestrator plugin/_internal .planning/instruction-budget-baselines.tsv` (base de phase FIGÉE ; le commit de baseline de 43-04 qui pose `@bootstrap:socle` y figure — chemin CODEOWNERS : revue de Samuel ou contournement tracé, CLAUDE.local.md ; la ligne ajoutée et la citation « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » y sont recopiées) ; le constat T23 à T31 ; le changement de comportement de l'installeur (relais WARNING/ERROR) ;
   - « Couverture FABR-09 » : complète, option ratchet-socle (citation mot pour mot) — SKILL.md refusé au-delà de 500 lignes ; toute hausse du socle du bootstrap au-dessus de sa ligne `@bootstrap:socle` refusée ; plafond ADR-029 de 2000 signalé par AU-DESSUS-PLAFOND-ADR029, non bloquant (objectif, retour sous 2000 au BACKLOG, 433fea0) ; valeur de la ligne et mesure du jour reprises de 43-04-SUMMARY.md ;
   - « Résidus signalés » : G-1 ne voit pas une ligne de baseline AJOUTÉE (ni son retrait sur une clé non-fichier) — l'ajout de `@bootstrap:socle` n'est gardé que par sa citation et la revue de Samuel, seules ses hausses futures le sont par G-1 ; socle du bootstrap au-dessus du plafond ADR-029 (écart entériné par le ratchet, retour sous 2000 au BACKLOG, 433fea0) ; check-skills.sh non câblé au SessionStart (à poser avec la mise en conformité du corpus, e36e6f2) ; liste de dérive du corpus réel (43-02, sous la règle Q-PORTEE : un marqueur en titre, ou au moins deux marqueurs distincts en prose) ; --verify de l'injecteur inchangé sur valeur malformée ; harnais de test-check-agents.sh : ses 16 appels du helper de mutation en substitution de commande (Phase 42, dont l.2574-2597) perdent un mutant refusé dans le sous-shell — seul MUT-M1 (43-05) appelle le helper directement (sortie redirigée vers un fichier, refus compté dans le shell de la suite, prouvé par MUT-M1-REFUS-COMPTE), la nouvelle suite test-check-skills.sh n'appelle jamais le helper en substitution de commande (constat du 2026-09-26, à corriger par un plan séparé) ; troncature à 1 536 caractères des descriptions non gatée ; fidélité des clés vf-* des skills sur les runtimes tiers non mesurée (liste FIELDS de check-artifact-fidelity.sh) ; échéance du manifeste daté (six listes périmées à partir du 2026-10-24, septième à sa verifie_le + 31 j ; CI rouge en INDÉTERMINÉ à partir de là ; conduite : bloc de contexte, rafraîchissement séparé cité, jamais un re-datage seul — consigner s'il a eu lieu, avec le hash de son commit) ;
   - la table complète « Artifacts this phase produces » (reprise de ce plan, complétée des valeurs réelles).
  </action>
  <verify>
    <automated>for s in plugin/conductor/scripts/tests/test-check-skills.sh plugin/conductor/scripts/tests/test-check-agents.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh plugin/conductor/scripts/tests/test-guard-agent-write.sh plugin/conductor/scripts/tests/test-check-blueprints.sh plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh scripts/tests/test-hook-exit-parc.sh plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh plugin/dev-orchestrator/scripts/tests/test-check-capability-activation.sh plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh plugin/_internal/tests/test-vibeflow-update.sh plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh plugin/content-bundle/scripts/tests/test-content-bundle.sh plugin/growth-bundle/scripts/tests/test-growth-bundle.sh plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh; do bash "$s" >/dev/null 2>&1 || echo "SUITE-ROUGE $s"; done; bash plugin/conductor/scripts/check-instruction-budget.sh >/dev/null 2>&1 || echo "BUDGET-ROUGE"; bash scripts/check-version-sync.sh >/dev/null 2>&1 || echo "VERSION-SYNC-ROUGE"; bash plugin/conductor/scripts/check-blueprints.sh >/dev/null 2>&1 || echo "BLUEPRINTS-ROUGE"; echo REJEU-FIN</automated>
    <fails_when>une ligne SUITE-ROUGE, BUDGET-ROUGE, VERSION-SYNC-ROUGE ou BLUEPRINTS-ROUGE apparaît avant REJEU-FIN. Aucune exception : 43-04 a prouvé le dépôt réel vert avec la ligne `@bootstrap:socle` égale à la mesure (REEL-BOOT rc=0) ; un BUDGET-ROUGE — hausse du socle au-dessus de sa ligne comprise — ne s'excuse jamais. Un rouge se consigne et repart par révision séparée (action, étape 2).</fails_when>
    <automated>fail=0; for d in plugin/*/; do find "$d" -name SKILL.md -type f | grep -q . || continue; if [ -f "$d/module.json" ] && grep -Eq '"type"[[:space:]]*:[[:space:]]*"doc-only"' "$d/module.json"; then continue; fi; bash plugin/conductor/scripts/check-skills.sh --strict --skills-dir="$d" >/dev/null 2>&1 || fail=$((fail+1)); done; reg=(); for d in plugin/*/agents; do reg+=("--agent-registry-dir=$d"); done; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --agents-dir="$d" "${reg[@]}" >/dev/null 2>&1 || fail=$((fail+1)); done; echo "CORPUS-REEL fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « CORPUS-REEL fail=0 »</fails_when>
    <automated>B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1); git fetch -q origin main 2>/dev/null; bash scripts/check-gate-touche.sh; echo "G2 rc=$?"; bash scripts/check-baseline-arbitrage.sh; echo "G1 rc=$?"; test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && liste=$(git diff --name-only "$B43" HEAD -- .github) && printf '%s' "$liste" | grep -c .; test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && merges=$(git rev-list --merges "$B43"..HEAD) && n=0 && while IFS= read -r c; do [ -n "$c" ] || continue; bad=0; parents=$(git rev-list --parents -n 1 "$c" | cut -d' ' -f2- | tr ' ' '\n'); while IFS= read -r p; do [ -n "$p" ] || continue; git merge-base --is-ancestor "$B43" "$p" || bad=1; done <<< "$parents"; if [ "$bad" = 1 ]; then n=$((n+1)); fi; done <<< "$merges" && echo "MERGES-DANS-LA-PLAGE $n"</automated>
    <fails_when>« G2 rc=… » ne vaut pas 0, ou « G1 rc=… » ne vaut ni 0 ni 3, ou la dernière valeur avant « MERGES-DANS-LA-PLAGE » n'est pas 0 (un fichier .github touché), ou aucune valeur ne suit la ligne « G1 rc=… » (base de phase figée illisible ou hors de l'histoire), ou « MERGES-DANS-LA-PLAGE » n'est pas suivi de 0 (un merge dont au moins un parent n'a pas B43 pour ancêtre — un merge de l'amont, et non un merge interne de vague, admis — cf. 43-01 : arrêt en rouge, retour à l'humain, jamais un filtrage silencieux de la plage)</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q && printf '{"mcpServers":{"mobile-mcp":{"command":"x"}}}\n' > .mcp.json); journal="$LAB/journal.txt"; : > "$journal"; bash "$R/plugin/_internal/resolve-deps.sh" dev-orchestrator | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >> "$journal" 2>&1); done; (cd "$LAB" && bash .claude/scripts/check-skills.sh --strict | grep -q '✓ skills conformes') && (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict >/dev/null) && grep 'WARNING:' "$journal" | grep -q 'XcodeBuildMCP' && echo LAB-FRAIS-FIN-OK</automated>
    <fails_when>la ligne LAB-FRAIS-FIN-OK est absente</fails_when>
    <automated>B=$(sed -n 's/^Base T2 : \([0-9a-f]\{7,40\}\).*$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-06-SUMMARY.md | head -1); test -n "$B" || echo "BASE-ABSENTE"; git diff --name-only "$B" HEAD -- . ':(exclude).planning' ':(exclude)plugin/conductor/CHANGELOG.md'; echo "PERIMETRE-T2-FIN"</automated>
    <fails_when>une ligne quelconque (BASE-ABSENTE ou un chemin) apparaît avant « PERIMETRE-T2-FIN » : la Tâche 2 a commité un fichier hors de son `files_modified` (plugin/conductor/CHANGELOG.md) et de `.planning/` — par exemple une correction dans le fichier d'un autre plan, qui doit repartir par une révision séparée</fails_when>
  </verify>
  <acceptance_criteria>
    - REJEU-FIN sans aucune ligne ROUGE, BUDGET-ROUGE compris ; `CORPUS-REEL fail=0` ; `G2 rc=0` ; G-1 à 0 ou 3 ; aucun fichier .github touché ; `MERGES-DANS-LA-PLAGE 0` ; `LAB-FRAIS-FIN-OK`
    - périmètre d'écriture strict : la commande de périmètre imprime `PERIMETRE-T2-FIN` seule (aucun fichier suivi hors de plugin/conductor/CHANGELOG.md et de .planning/ commité depuis « Base T2 »)
    - le SUMMARY contient les sections « Relecture Samuel », « Couverture FABR-09 » (complète, option ratchet-socle, citation mot pour mot), « Résidus signalés » et « Artifacts this phase produces »
    - la section « Relecture Samuel » contient les VRAIS commits de la plage B43..HEAD qui touchent plugin/dev-orchestrator, plugin/_internal ou la baseline CODEOWNERS du budget des instructions, prouvé par commande et non par un compte de hashes (S5, tour 3) : `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && S=$(ls .planning/workstreams/gouvernance/phases/VFDO-43-*/43-06-SUMMARY.md) && shas=$(git log --format=%H "$B43"..HEAD -- plugin/dev-orchestrator plugin/_internal .planning/instruction-budget-baselines.tsv) && printf '%s\n' "$shas" | awk -v S="$S" 'BEGIN{while((getline l < S)>0){if(l ~ /^#+ .*Relecture Samuel/){f=1;continue} if(l ~ /^#+ /)f=0; if(f)sec=sec "\n" l}} NF{n++; if(index(sec, substr($0,1,7))==0){print "SAMUEL-MANQUE " $0; m++}} END{print "SAMUEL-PLAGE n=" n+0 " manquants=" m+0}'` n'imprime aucune ligne SAMUEL-MANQUE et finit par `SAMUEL-PLAGE n=<n> manquants=0` avec n ≥ 4 (au moins les deux commits de 43-05 sous dev-orchestrator, le commit de bump de 43-07 et le commit de baseline de 43-04 qui pose `@bootstrap:socle`) ; chaque SHA réel de la plage doit figurer (préfixe de 7 caractères) dans la section, qui s'arrête au titre suivant quel que soit son niveau — hashes écrits sous le titre même ; rien d'imprimé (base illisible, SUMMARY absent) vaut échec
  </acceptance_criteria>
  <done>La phase est rejouée de bout en bout sur fixtures, corpus réel et labs frais ; la PR porte le relevé de relecture de Samuel et la liste honnête de ce qui reste ouvert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation de module → lecteur et lab | le CHANGELOG et le README sont la trace publique des décisions |
| numéro de version partagé avec fiabilite → merge | collision possible sur conductor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-50 | Repudiation | attributions du CHANGELOG et des commits | low | mitigate | décisions de Willy citées avec canal et date ; décisions de planification nommées comme telles (critère d'acceptation) |
| T-43-51 | Tampering | numéro de version de conductor | medium | mitigate | version lue sur disque ; renumérotation au rebase après fiabilite (en-tête du jalon) ; check-version-sync.sh |
| T-43-52 | Spoofing (faux vert) | rejeu partiel | medium | mitigate | rejeu des suites voisines, du corpus réel, de G-1, G-2 et de deux labs frais sous HOME temporaire |
</threat_model>

<verification>
- REJEU-FIN, CORPUS-REEL fail=0, G2 rc=0, G-1 conforme, aucun .github touché, MERGES-DANS-LA-PLAGE 0, LAB-FRAIS-FIN-OK, PERIMETRE-T2-FIN seul ; comptes du README (COMPTES-FIN) et documentation bootstrap ratchet-socle (BOOTSTRAP-DOC, aucune valeur nulle) conformes.
</verification>

<success_criteria>
- FABR-10 (c) complet (vibeflow-update.sh en 43-05, vf-calibrate ici) ; conductor documenté et en mineure ; la phase entière rejouée verte ; relevés prêts pour la PR.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-skills.sh` (options `--skills-dir=`, `--file`, `--strict`, `--allow-empty`, `--hook`, `--third-party-prefix=`, `--no-third-party-prefix`) | nouveau gate | 43-01 |
| `plugin/conductor/scripts/tests/test-check-skills.sh` (T1 à T31 et `test-check-skills:T32`, T26 en T26a à T26h ; MUT-S1 à MUT-S3, MUT-SD1, MUT-SD2, MUT-DR1 à MUT-DR3 ; gardes du harnais MUT-SYNTAXE et MUT-REFUS-COMPTE) | nouvelle suite | 43-01, 43-02 |
| clés de frontmatter des skills `vf-nature`, `ecrit`, `vf-rubrique-juge`, `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` ; ensemble `VIBEFLOW_SKILL_FIELDS` | contrat de clés (costly) | 43-01 |
| liste de manifeste `champs_frontmatter_skills` (septième liste de `check-agents-manifest.json`) | manifeste daté | 43-01 |
| `decouvrir_skills()`, `valider_nature()`, `valider_ecrit()`, `invariant_procedure()` | check-skills.sh | 43-01 |
| `detecter_derive()`, `ecart_nature_marqueurs()`, `lignes_de_portee()`, `marqueurs_constates()`, `MOTIFS_MARQUEURS`, constantes `DERIVE_PROSE_MIN_DISTINCTS = 2` et `DERIVE_TITRE_MIN = 1` (règle Q-PORTEE, décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26) | check-skills.sh | 43-02 |
| `T107` (manifeste sans septième liste), `T108` à `T115`, `MUT-M1` (appel direct du helper), garde `MUT-M1-REFUS-COMPTE`, `valider_mcp_tools()` (lignes brutes, règle d'extraction commune avec l'injecteur, ordre trim puis déquotage) | check-agents.sh et sa suite | 43-01, 43-05 |
| `VF_SKILL_LINE_CAP`, verdict `DEPASSEMENT-SKILL-ADR029`, `BILAN-SKILLS`, `SKILL-1` à `SKILL-6`, `MUT-8` à `MUT-10` | check-instruction-budget.sh et sa suite | 43-04 |
| `VF_BOOTSTRAP_TOKEN_CAP`, ligne `BOOTSTRAP :`, verdicts `DEPASSEMENT-BOOTSTRAP` (bloquant) et `AU-DESSUS-PLAFOND-ADR029` (non bloquant), clé de baseline `@bootstrap:socle` (option ratchet-socle, citée, signalée à Samuel), `BOOT-1` à `BOOT-5`, `MUT-11`, `MUT-12` | métrique bootstrap | 43-04 |
| `malformed_found`, refus ERROR + rc 1 d'une vf-mcp-tools malformée ; déquotage d'une paire de guillemets et règle d'extraction commune (parité avec le gate pour toute valeur) ; message honnête de serveur absent et sources nommées ; `T16` durci, `T22a` à `T22j`, `test-inject-mcp-tools:T32` à `test-inject-mcp-tools:T34`, `MUT-A` | inject-mcp-tools.sh et sa suite | 43-05 |
| relais WARNING/ERROR dans `inject_lab_mcp_into_agents` ; `T55`, `T56` | vibeflow-update.sh et sa suite | 43-05 |
| spec fabrique §1.2 et §7.2 amendées | documentation | 43-07 |
| question vf-nature des deux étages de skill-creator | prompts | 43-03 |
| conductor, skill-creator en mineure ; dev-orchestrator en patch | versions de module | 43-06, 43-03, 43-07 |

## Flagged assumptions

- **Sondes de bords spec-less FABR-06 à FABR-10** : les cinq items `unclassified` / `unresolved` sont surfacés chacun dans le plan qui porte l'exigence (43-01 pour FABR-06 et la clause manifeste de FABR-09, 43-02 pour FABR-07, 43-03 pour FABR-08, 43-04 pour FABR-09, 43-05 pour FABR-10) ; ce plan ne les résout pas d'office.
- **Pas de modification de ci.yml** : check-skills.sh atteint la CI par la découverte des suites (`find plugin scripts -path '*/tests/test-*.sh'`, ci.yml l.218) et ses cas sur l'arbre réel (T4, T32) ; la fraîcheur du manifeste partagé reste jugée par les quatre appels strict de check-agents.sh. Aucun chemin CODEOWNERS `.github/` n'est touché.
- **Hook SessionStart non câblé** : décision de plan (43-PATTERNS.md demandait de la trancher) ; à rouvrir avec la mise en conformité du corpus.
- **Ledger REQUIREMENTS.md** : les cases FABR-06 à FABR-10 sont cochées par la vérification de phase, pas par ce plan (précédent Phase 42 ; `requirements.mark-complete` résout vers fiabilite sur ce dépôt).

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-06-SUMMARY.md` when done — commit `.planning/` séparé, avec les sections « Relecture Samuel », « Résidus signalés » et « Artifacts this phase produces ».
</output>

---
phase: 01-dev-orchestrator
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - dev-orchestrator/AGENT.md
  - dev-orchestrator/references/GSD-PIPELINE.md
autonomous: true
requirements: [ROUT-01, ROUT-02, ROUT-03, ROUT-04]
must_haves:
  truths:
    - "AGENT.md définit l'agent vibeflow-dev avec un frontmatter subagent valide (name/description/model)"
    - "La table de routage couvre ≥11 intentions en langage naturel → action GSD/superpowers"
    - "L'agent embarque l'ordre canonique du pipeline (détail déporté dans GSD-PIPELINE.md)"
    - "L'agent ne nomme jamais GSD à l'utilisateur et reframe en vocabulaire VibeFlow"
    - "AGENT.md référence la doctrine via le chemin d'install D7 (.claude/agents/dev-orchestrator-references/)"
    - "AGENT.md respecte la charte densité (≤250 lignes)"
  artifacts:
    - path: "dev-orchestrator/AGENT.md"
      provides: "Agent routeur vibeflow-dev (cerveau NL → action)"
      contains: "vibeflow-dev"
    - path: "dev-orchestrator/references/GSD-PIPELINE.md"
      provides: "Ordre canonique pipeline + bonnes pratiques (chargé on-demand)"
      contains: "new-project"
  key_links:
    - from: "dev-orchestrator/AGENT.md"
      to: ".claude/agents/dev-orchestrator-references/gsd-skills-index.md"
      via: "référence à l'index factuel pour les noms de skills (chemin d'install D7)"
      pattern: "dev-orchestrator-references/gsd-skills-index"
    - from: "dev-orchestrator/AGENT.md"
      to: ".claude/agents/dev-orchestrator-references/GSD-PIPELINE.md"
      via: "déport du détail pipeline (densité), chemin d'install D7"
      pattern: "dev-orchestrator-references/GSD-PIPELINE"
---

<objective>
Écrire l'agent routeur `vibeflow-dev` (`AGENT.md`, copié en `.claude/agents/dev-orchestrator.md`
par l'installeur) et la doctrine pipeline déportée (`references/GSD-PIPELINE.md`).

Purpose: L'agent est le cerveau (D4) : il route le langage naturel vers le bon skill GSD/
superpowers, embarque l'ordre canonique du pipeline, et masque totalement « GSD » à l'utilisateur.
Couvre ROUT-01 à ROUT-04. Le détail exhaustif du pipeline est déporté pour respecter la densité (≤250L).
Output: `AGENT.md` (≤250L) + `references/GSD-PIPELINE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/CONVENTIONS.md

# Pattern à calquer OBLIGATOIREMENT (frontmatter subagent + style + densité ≤250L) :
@validator/AGENT.md

# Index factuel produit en Plan 01 (source des noms de skills réels) :
@dev-orchestrator/references/gsd-skills-index.md

<interfaces>
<!-- Frontmatter subagent natif Claude Code (calqué sur validator/AGENT.md). -->
Champs requis : name, description (multiline, français), model. Optionnels : memory, skills.
L'installeur copie AGENT.md → .claude/agents/dev-orchestrator.md (vibeflow-update.sh).

<!-- CHEMIN D'INSTALL DES REFERENCES (D7, NON NÉGOCIABLE) :
     En production (lab), l'installeur copie dev-orchestrator/references/ vers
     .claude/agents/dev-orchestrator-references/ (cf. Plan 05). L'agent charge donc sa doctrine
     on-demand via CES chemins EXACTS :
       - .claude/agents/dev-orchestrator-references/gsd-skills-index.md
       - .claude/agents/dev-orchestrator-references/GSD-PIPELINE.md
     N'utiliser AUCUN chemin relatif ambigu (`references/...`) dans AGENT.md : toujours le chemin
     d'install D7 complet ci-dessus. (En dev/repo, les fichiers sources restent dans
     dev-orchestrator/references/ pour les tests, mais l'agent distribué référence le chemin D7.) -->

<!-- Table de routage cible (≥11 intentions, spec §4). Chaque action = un skill GSD/superpowers RÉEL
     (vérifié contre gsd-skills-index.md). Skills disponibles confirmés sur disque : gsd-discuss-phase,
     gsd-plan-phase, gsd-execute-phase, gsd-quick, gsd-fast, gsd-verify-work, gsd-code-review,
     gsd-debug, gsd-autonomous, gsd-ship, gsd-progress, gsd-map-codebase, gsd-new-project ;
     + superpowers brainstorming (plugin).
     CIBLES CANONIQUES FIGÉES (12, partagées avec Plan 04 — ne pas diverger) :
       brainstorming, gsd-discuss-phase, gsd-plan-phase, gsd-execute-phase, gsd-quick,
       gsd-verify-work, gsd-code-review, gsd-debug, gsd-autonomous, gsd-ship, gsd-progress, gsd-map-codebase. -->

| Intention NL | Action coulisse |
|---|---|
| réfléchis / conçois / on part sur quoi | superpowers brainstorming |
| planifie / découpe / cadre la phase | gsd-discuss-phase + gsd-plan-phase |
| code / implémente / ajoute / construis | gsd-execute-phase (ou gsd-quick si trivial) |
| petite tâche / vite fait / typo | gsd-quick / gsd-fast |
| teste / vérifie / valide | gsd-verify-work |
| relis / audit / review | gsd-code-review |
| débugge / ça plante | gsd-debug |
| fais tout / en autonomie / la nuit | gsd-autonomous |
| crée une PR / livre / ship | gsd-ship |
| on est où / et après / next | gsd-progress |
| comprends ce code / cartographie | gsd-map-codebase |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire references/GSD-PIPELINE.md (doctrine déportée)</name>
  <files>dev-orchestrator/references/GSD-PIPELINE.md</files>
  <action>
    Créer la doctrine pipeline (chargée on-demand par l'agent, règle 1% — pas dans le corps de l'agent).
    Contenu (ROUT-02) :
    - Ordre canonique : `new-project → map-codebase → discuss-phase → plan-phase → execute-phase → verify-work → code-review → ship → complete-milestone`.
    - Chemin autonome : quand utiliser `gsd-autonomous` (l'agent enchaîne plan→execute→test seul).
    - Escape hatches : `gsd-quick` / `gsd-fast` pour le trivial (quand court-circuiter le pipeline complet).
    - Quand faire `/clear` (fenêtre de contexte fraîche entre phases lourdes).
    - Model profiles : rappel rapide (planner/executor/checker).
    - Garde-fous : `gsd-new-project` est interactif → jamais lancé seul, proposer sur confirmation (BOOT-04).
    Référencer les noms de skills réels (cohérents avec gsd-skills-index.md). Français.
    Longueur libre (≤500L, c'est une référence on-demand).
  </action>
  <verify>
    <automated>test -f dev-orchestrator/references/GSD-PIPELINE.md && grep -q 'new-project' dev-orchestrator/references/GSD-PIPELINE.md && grep -q 'autonomous' dev-orchestrator/references/GSD-PIPELINE.md && grep -q 'clear' dev-orchestrator/references/GSD-PIPELINE.md</automated>
  </verify>
  <done>GSD-PIPELINE.md documente l'ordre canonique, le chemin autonome, les escape hatches, le /clear et les model profiles, avec des noms de skills réels.</done>
</task>

<task type="auto">
  <name>Task 2: Écrire AGENT.md (vibeflow-dev, routeur ≤250L)</name>
  <files>dev-orchestrator/AGENT.md</files>
  <action>
    Créer l'agent en calquant strictement la forme de validator/AGENT.md.
    Frontmatter (ROUT-04) : `name: vibeflow-dev`, `description:` (multiline français, décrit le rôle :
    expert dev senior qui pilote GSD/superpowers en coulisse, route le NL, ne nomme jamais GSD ;
    invocable via Task ou en autonomie), `model: opus`, `memory: project`. (Pas de `skills:` listés ici :
    l'agent invoque les skills GSD installés + les verbes /vf-* de Plan 04 par leur nom.)
    Corps (≤250L, charte densité) :
    - **Persona** (ROUT-03) : expert dev senior ; ne prononce JAMAIS « GSD » ni « Superpowers » à l'utilisateur ; reframe toutes les sorties en vocabulaire VibeFlow (« SUMMARY » → « rapport de sprint », « ROADMAP » → « feuille de route »).
    - **Table de routage** (ROUT-01) : reprendre les ≥11 intentions de <interfaces>, chaque ligne mappée vers un skill RÉEL (cohérent avec gsd-skills-index.md). Couvrir un large éventail de formulations par intention (couverture maximale, D6).
    - **Doctrine pipeline embarquée** (ROUT-02) : résumer l'ordre canonique en 1-2 lignes et RENVOYER au fichier de doctrine via le chemin d'install D7 EXACT : `.claude/agents/dev-orchestrator-references/GSD-PIPELINE.md` (chargé on-demand). Idem pour l'index des noms de skills : référencer `.claude/agents/dev-orchestrator-references/gsd-skills-index.md`. NE PAS utiliser de chemin relatif `references/...`. Ne pas dupliquer tout le détail dans l'agent (densité).
    - **Garde-fous** : ne jamais réimplémenter la logique GSD (toujours déléguer) ; sur action structurante, clarifier (P4) puis vérifier (P5) ; `gsd-new-project` jamais lancé sans confirmation utilisateur.
    - **Iron Law** : « Je pilote GSD/Superpowers en coulisse ; l'utilisateur ne parle que VibeFlow. »
    Langue : français. IMPÉRATIF : fichier ≤250 lignes.
  </action>
  <verify>
    <automated>test -f dev-orchestrator/AGENT.md && head -1 dev-orchestrator/AGENT.md | grep -q '^---' && grep -q 'name: vibeflow-dev' dev-orchestrator/AGENT.md && grep -q '\.claude/agents/dev-orchestrator-references/GSD-PIPELINE\.md' dev-orchestrator/AGENT.md && grep -q '\.claude/agents/dev-orchestrator-references/gsd-skills-index\.md' dev-orchestrator/AGENT.md && [ "$(wc -l < dev-orchestrator/AGENT.md)" -le 250 ] && targets=$(grep -v '^#' dev-orchestrator/AGENT.md | grep -Eo 'brainstorm(ing)?|gsd-discuss-phase|gsd-plan-phase|gsd-execute-phase|gsd-quick|gsd-fast|gsd-verify-work|gsd-code-review|gsd-debug|gsd-autonomous|gsd-ship|gsd-progress|gsd-map-codebase' | sort -u | wc -l); [ "$targets" -ge 11 ]</automated>
  </verify>
  <done>AGENT.md a un frontmatter subagent valide (name: vibeflow-dev, model), couvre ≥11 cibles de routage DISTINCTES (comptées via grep -Eo | sort -u), référence la doctrine et l'index via le chemin d'install D7 exact, et fait ≤250 lignes.</done>
</task>

</tasks>

<verification>
- ROUT-01 : ≥11 cibles de routage DISTINCTES vers des skills réels (vérifié par grep -Eo | sort -u | wc -l, pas un comptage de lignes) ✓
- ROUT-02 : ordre pipeline embarqué + détail déporté dans GSD-PIPELINE.md, référencé via chemin D7 ✓
- ROUT-03 : persona masque GSD, reframe en vocabulaire VibeFlow ✓
- ROUT-04 : distribué via AGENT.md (installé en .claude/agents/dev-orchestrator.md par l'installeur) ✓
- Références chargées via .claude/agents/dev-orchestrator-references/ (D7) ✓
- Densité agent ≤250L respectée ✓
</verification>

<success_criteria>
- Agent vibeflow-dev fonctionnel, lean (≤250L), routant le NL sans jamais nommer GSD.
- Doctrine pipeline complète, déportée, et référencée au chemin d'install D7.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-03-SUMMARY.md`
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 11
lot: L11 — tag annoté, release GitHub, check-release-tag --remote, ledger coché sur pièce, PR de clôture sans contournement
type: execute
wave: 9
depends_on: ["41-10"]
autonomous: false
requirements: [PROT-01, PROT-02, PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Critère 3 complet : la PR des preuves est `MERGED` sans suite `bypass` sur son commit de merge ; tag annoté `v<N>` sur ce commit, créé sans contournement (création libre, D-07) ; release GitHub existante ; `bash scripts/check-release-tag.sh --remote` rend 0 avec `✓` (PROT-02)"
    - "Le rouge attendu de l'étape `check-release-tag` du job `gates` au push de merge sur `main` (tag posé après) est consigné comme signal « tag à poser » ; sa relance après le tag est un choix humain consigné, pas une obligation (D-M5)"
    - "`scripts/check-release-tag.sh`, `scripts/hooks/pre-push` et `.github/workflows/ci.yml` n'ont été modifiés par aucun merge de la phase (PR de la phase, PR-P, PR des preuves) ; aucun chemin `plugin/dev-orchestrator/` non plus (D-M5, D-M9, zone interdite)"
    - "Critère 4 / QUAL-01 : aucun merge de la phase n'ajoute de `check-*.sh` ni de `tests/test-*.sh` ; la phase n'a créé aucun gate et le ledger le dit (« non déclenché sur la Phase 41 », D-M11)"
    - "PROT-01..04 cochés et `Complete` en traçabilité seulement ici, chacun citant ses lignes de `41-PREUVES.md` ; tout `non_prouvee` de `TRACE-II:` est recopié dans la ligne de PROT-01/PROT-04, jamais effacé"
    - "La PR de clôture est la troisième PR de la phase, imposée par D-03 : les preuves post-release ne peuvent atteindre `main` que par une PR. Son SUMMARY est committé AVANT sa publication ; son merge (sans contournement) est relu par l'orchestrateur, jamais consigné dans le dépôt"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROT-01..04 cochés sur pièce ; QUAL-01 : non déclenché sur la Phase 41"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-RELEASE:`, `RELEASE:`, `CI-MAIN-RELEASE:`, `INVARIANTS-PHASE:`"
  key_links:
    - "La branche C `docs/phase-41-cloture` est créée depuis `origin/main` APRÈS le merge de la PR des preuves : elle contient la `VERSION` taggée, ce qui rend `check-release-tag.sh --remote` significatif sur son arbre"
---

<objective>
Terminer le rejeu du flux de release sous la règle, vérifier les invariants de la phase, cocher le
ledger sur pièce, puis faire entrer ces preuves sur `main` par une PR ordinaire.

Purpose: critères de succès 3 (PROT-02) et 4 (QUAL-01) ; clôture sur pièce de PROT-01..04.
Output: tag et release publiés, `check-release-tag` ✓, ledger coché, PR de clôture mergée.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-10-SUMMARY.md
@CLAUDE.md
@scripts/check-release-tag.sh

Extraits de `.planning/REQUIREMENTS.md` : section `### Posture de protection du dépôt (Phase 41)` ;
table `## Traceability`, lignes `PROT-0N` et `QUAL-01` (format des lignes `Complete — <date>, plan … : …`
de BUDG-04/05).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : relecture du merge de la PR des preuves, branche C, notes de release préparées</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 3 de 41-10
    - CHANGELOG.md (entrée `## [v<N>]`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin --tags`. Relire la PR des preuves : `MERGED`, commit
de merge, `VERSION` de ce commit = `N` (celle de B). Créer la branche C en local :
`git switch -c docs/phase-41-cloture origin/main` (arbre propre exigé).

Consigner dans `41-PREUVES.md`, section `## 41-11 — release et clôture` :
`PR-RELEASE: #<n> merge=<sha> version=v<N> etat_avant_merge=<état collé> commande=gh_pr_merge_--merge`.

Écrire hors dépôt (fichier temporaire, chemin imprimé) les notes de release : résumé repris de
l'entrée CHANGELOG `v<N>` et liste `git log --oneline v<version précédente>..<merge>`. Imprimer les
commandes exactes de la Task 2 avec `<N>`, `<sha>` et le chemin des notes. Commit local sur C.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); N=$(awk "\$1==\"PR-RELEASE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-RELEASE absente"; exit 1;; esac; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); hr=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefName --jq .headRefName); M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); TV=$(mktemp -d); git show "$M:VERSION" > "$TV/v"; vm=$(tr -d "[:space:]" < "$TV/v"); vc=$(val PR-RELEASE: version); mc=$(val PR-RELEASE: merge); ea=$(val PR-RELEASE: etat_avant_merge); br=$(git branch --show-current); vt=$(tr -d "[:space:]" < VERSION); echo "PR des preuves : identite=$u pr=$N branche=$hr etat=$st merge=$M version_merge=$vm version_consignee=$vc merge_consigne=$([ "$mc" = "$M" ] && echo oui || echo non) etat_avant_merge=$ea branche_courante=$br version_arbre=$vt"; [ "$u" = samuel-neveugall ] && [ "$st" = MERGED ] && [ "$hr" = feat/phase-41-preuves-release ] && [ "$vm" = "$vc" ] && [ "$mc" = "$M" ] && [ "$ea" = CLEAN ] && [ "$br" = docs/phase-41-cloture ] && [ "$vt" = "$vm" ]'</automated>
    <fails_when>exit non nul ; ou PR des preuves non `MERGED` ou autre branche ; ou version du commit de merge différente de celle consignée ; ou merge consigné différent du relu ; ou `etat_avant_merge=` différent de `CLEAN` (merge obtenu hors du chemin normal : arrêt) ; ou branche courante différente de C ; ou `VERSION` de l'arbre différente de celle du merge</fails_when>
  </verify>
  <acceptance_criteria>
    - PR des preuves `MERGED` depuis B, état `CLEAN` avant merge, version `v<N>` au commit de merge
    - branche C créée depuis `origin/main` ; `PR-RELEASE:` consignée ; notes de release prêtes hors dépôt
  </acceptance_criteria>
  <done>Le tag peut être posé sur le bon commit, avec ses notes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — tag annoté, release GitHub, relance facultative, lecture des rule suites</name>
  <action>Samuel, compte `samuel-neveugall`, pose et publie le tag annoté sur le commit de merge, crée la release GitHub, décide de relancer ou non le job rouge de `main`, et fait lire les rule suites par le compte lecteur.</action>
  <instructions>
Compte qui agit pour 1-4 : `samuel-neveugall`. Valeurs imprimées par la Task 1.

1. `git fetch origin && git tag -a v<N> -m "v<N> — posture de protection du dépôt (Phase 41)" <sha-merge> && git push origin v<N>`
   → copier la sortie (aucun message de contournement attendu : création libre).
2. `gh release create v<N> --repo picmakpro/vibeflow-os --title "v<N> — posture de protection du dépôt" --notes-file <chemin-des-notes> --verify-tag`
3. Run CI du push de merge sur `main` :
   `gh run list --repo picmakpro/vibeflow-os --commit <sha-merge> --json databaseId,event,conclusion --jq '.[] | select(.event=="push")'`
   → copier. L'échec du job « Gates de qualité (mode strict) » à l'étape `check-release-tag` est le
   signal attendu (tag posé après le merge, D-M5).
4. Facultatif (D-M5, relance possible, non systématique) : `gh run rerun <databaseId> --repo picmakpro/vibeflow-os --failed`
   → écrire « relance : oui » ou « relance : non ».
5. Lecture des rule suites par le compte lecteur de `ACCES-RULE-SUITES:` :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=day&rule_suite_result=all" --jq '.[] | select(.after_sha == "<sha-merge>" or .ref == "refs/tags/v<N>") | {id, ref, actor_name, after_sha, result}'`
  </instructions>
  <verification>La Task 3 relit le tag (annoté, cible), la release, joue `check-release-tag.sh --remote`, relit les runs de `main` et recoupe les suites.</verification>
  <resume-signal>Coller les sorties de 1, 2, 3, le choix de 4 et la sortie de 5, puis écrire « release publiée ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : critère 3 prouvé, invariants de la phase, ledger coché sur pièce, rejeu, SUMMARY avant publication</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - la sortie collée à la Task 2
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes)
    - .planning/REQUIREMENTS.md (section Phase 41, lignes de traçabilité PROT et QUAL-01)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin --tags`. Jouer `bash scripts/check-release-tag.sh --remote`
sur la branche C. Consigner :
- `RELEASE: tag=v<N> annote=<oui|non> cible=<sha> cible_est_merge=<oui|non> release=<url> check_release_tag_remote_rc=<rc> suite_merge=<pass|bypass|absente> suite_tag=<pass|bypass|absente>` ;
- `CI-MAIN-RELEASE: gates_premier_run=<conclusion> relance=<oui|non> gates_apres=<conclusion|sans_objet>`.
- `INVARIANTS-PHASE: merges=<sha phase>,<sha PR-P>,<sha release> gates_ajoutes=<k> suites_ajoutees=<k> ci_yml_touche=<oui|non> check_release_tag_touche=<oui|non> pre_push_touche=<oui|non> zone_interdite_touchee=<oui|non>`, mesurés sur `git diff --name-status <merge>^1 <merge>` de chacun des trois merges (ajouts `A` correspondant à `check-*.sh` ou `*/tests/test-*.sh`).

`REQUIREMENTS.md` :
- cocher PROT-01..04 en ajoutant à chaque entrée « — **close le <date d'exécution>** (Phase 41) : » et
  les clés de preuve de `41-PREUVES.md` qui la fondent (PROT-01 : `POSE-CONFORME`, `CI-I`/`M1-A`/`M1-B`,
  `TRACE-II` avec ses statuts par règle, `PUSH-DIRECT`, `M4-VERDICT` ; PROT-02 : `PR-RELEASE`,
  `RELEASE`, `CI-MAIN-RELEASE`, `INVARIANTS-PHASE` ; PROT-03 : ADR-072 et son § Mesures, amendement
  d'ADR-059, `CLAUDE.md`, BACKLOG traité ; PROT-04 : `CO-I`, `TRACE-II pull_request=…`, statut O-3) ;
- lignes de traçabilité `| PROT-0N | Phase 41 | Complete — <date>, plan 41-11 : … |` ;
- ligne QUAL-01 : ajouter `41` à la liste des phases de la deuxième colonne et, avant « ; reste à
  tenir », « ; **non déclenché sur la Phase 41** (<date>) — aucun gate ni comparateur neuf n'y naît
  (D-M11 : CODEOWNERS et rulesets sont de la configuration), `INVARIANTS-PHASE` à 0 ajout ».
Toute valeur `non_prouvee` de `TRACE-II:` est recopiée telle quelle dans PROT-01 ou PROT-04.

Commit local sur C. Rejouer `gates` et `tests`. Rédiger et committer `41-11-SUMMARY.md` sur C À LA
FIN de cette tâche : il reprend aussi les résultats post-merge de 41-10, annonce la Task 4, et écrit
que la phase ne crée aucun gate (critère 4).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M=$(val PR-RELEASE: merge); V=$(val PR-RELEASE: version); ty=$(git cat-file -t "refs/tags/$V" 2>/dev/null || echo absent); tc=$(git rev-parse "refs/tags/$V^{commit}" 2>/dev/null || echo absent); TL=$(mktemp -d); git ls-remote --tags origin "refs/tags/$V" > "$TL/td"; td=$(awk "END{print NR}" "$TL/td"); rl=$(gh release view "$V" --repo picmakpro/vibeflow-os --json tagName --jq .tagName 2>/dev/null || echo absente); rc=0; out=$(bash scripts/check-release-tag.sh --remote 2>&1) || rc=$?; ok=$(printf "%s\n" "$out" | awk "index(\$0, \"✓\") {c++} END{print c+0}"); sm=$(val RELEASE: suite_merge); stg=$(val RELEASE: suite_tag); crc=$(val RELEASE: check_release_tag_remote_rc); echo "critere 3 : tag=$V type=$ty cible_est_merge=$([ "$tc" = "$M" ] && echo oui || echo non) tag_distant=$td release=$rl check_release_tag_remote_rc=$rc sortie_ok=$ok rc_consigne=$crc suite_merge=$sm suite_tag=$stg"; [ "$ty" = tag ] && [ "$tc" = "$M" ] && [ "$td" -eq 1 ] && [ "$rl" = "$V" ] && [ "$rc" -eq 0 ] && [ "$ok" -ge 1 ] && [ "$crc" = "$rc" ] && [ "$sm" != bypass ] && [ "$stg" != bypass ]'</automated>
    <fails_when>exit non nul ; ou tag absent, léger (non annoté) ou hors du commit de merge ; ou tag non publié ; ou release absente ; ou `check_release_tag_remote_rc=` différent de 0 ou sortie sans `✓` ; ou rc consigné différent du rc rejoué ; ou `suite_merge=bypass` / `suite_tag=bypass` (le rejeu n'est plus « sans contournement »)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M1=$(val PR-PHASE: merge); M2=$(val MAIN-APRES-PR-P: merge); M3=$(val PR-RELEASE: merge); T=$(mktemp -d); : > "$T/ns"; for m in "$M1" "$M2" "$M3"; do [ -n "$m" ] || { echo "merge absent"; exit 1; }; git merge-base --is-ancestor "$m" origin/main || { echo "merge $m hors main"; exit 1; }; git diff --name-status "$m^1" "$m" >> "$T/ns"; done; ga=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /(^|\\/)check-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); sa=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /\\/tests\\/test-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); ci=$(awk -F"\t" "\$NF==\".github/workflows/ci.yml\" {c++} END{print c+0}" "$T/ns"); crt=$(awk -F"\t" "\$NF==\"scripts/check-release-tag.sh\" {c++} END{print c+0}" "$T/ns"); pp=$(awk -F"\t" "\$NF==\"scripts/hooks/pre-push\" {c++} END{print c+0}" "$T/ns"); zi=$(awk -F"\t" "index(\$NF, \"plugin/dev-orchestrator/\")==1 {c++} END{print c+0}" "$T/ns"); cg=$(val INVARIANTS-PHASE: gates_ajoutes); cs=$(val INVARIANTS-PHASE: suites_ajoutees); echo "invariants : gates_ajoutes=$ga (consigne $cg) suites_ajoutees=$sa (consigne $cs) ci_yml=$ci check_release_tag=$crt pre_push=$pp zone_interdite=$zi"; [ "$ga" -eq 0 ] && [ "$sa" -eq 0 ] && [ "$ci" -eq 0 ] && [ "$crt" -eq 0 ] && [ "$pp" -eq 0 ] && [ "$zi" -eq 0 ] && [ "$cg" = "$ga" ] && [ "$cs" = "$sa" ]'</automated>
    <fails_when>exit non nul ; ou un des trois merges absent ou hors `main` ; ou `gates_ajoutes=` / `suites_ajoutees=` différent de 0 (un gate est né : QUAL-01 s'applique, mutation rouge exigée, remonter) ; ou `ci.yml`, `check-release-tag.sh`, `pre-push` modifiés par la phase (D-M5, D-M9) ; ou zone interdite touchée ; ou consignation différente de la mesure</fails_when>
    <automated>bash --noprofile --norc -e -c 'R=.planning/REQUIREMENTS.md; x=$(awk "/^- \\[x\\] \\*\\*PROT-0[1-4]\\*\\*: / && index(\$0, \"close le \") && index(\$0, \"(Phase 41)\") {c++} END{print c+0}" "$R"); u=$(awk "/^- \\[ \\] \\*\\*PROT-/{c++} END{print c+0}" "$R"); t=$(awk "/^\\| PROT-0[1-4] \\| Phase 41 \\| Complete — /{c++} END{print c+0}" "$R"); p=$(awk "/^\\| PROT-0[1-4] \\| Phase 41 \\| Pending/{c++} END{print c+0}" "$R"); q=$(awk "/^\\| QUAL-01 \\| Transverse — Phases [^|]*41[^|]*\\|/ && index(\$0, \"non déclenché sur la Phase 41\") {c++} END{print c+0}" "$R"); k=0; for key in POSE-CONFORME TRACE-II PUSH-DIRECT M4-VERDICT PR-RELEASE RELEASE INVARIANTS-PHASE CO-I; do awk -v k="$key" "/^- \\[x\\] \\*\\*PROT-0[1-4]\\*\\*: / && index(\$0, k) {f=1} END{exit !f}" "$R" || { echo "CLE DE PREUVE NON CITEE : $key"; k=$((k+1)); }; done; PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; np=$(awk "\$1==\"TRACE-II:\" && /non_prouvee/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); nr=$(awk "/^- \\[x\\] \\*\\*PROT-0[14]\\*\\*: / && index(\$0, \"non_prouvee\") {c++} END{print c+0}" "$R"); echo "ledger : coches_sur_piece=$x non_coches=$u table_complete=$t table_pending=$p qual01_phase41=$q cles_non_citees=$k trace_non_prouvee=$np recopiee=$nr"; [ "$x" -eq 4 ] && [ "$u" -eq 0 ] && [ "$t" -eq 4 ] && [ "$p" -eq 0 ] && [ "$q" -eq 1 ] && [ "$k" -eq 0 ] && { [ "$np" -eq 0 ] || [ "$nr" -ge 1 ]; }'</automated>
    <fails_when>exit non nul ; ou moins de 4 entrées PROT cochées avec date et « (Phase 41) » ; ou une entrée PROT encore non cochée ou `Pending` ; ou QUAL-01 sans la Phase 41 dans sa liste ni la mention « non déclenché » ; ou une clé de preuve non citée ; ou un `non_prouvee` de la trace effacé du ledger</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-11-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p" && co=$((co+1)); done < "$T/f"; echo "avant publication : summary_committe=$s modifications_non_committees=$d chemins_codeowners=$co"; [ "$s" = oui ] && [ "$d" -eq 0 ] && [ "$co" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY 41-11 non committé ; ou modifications suivies non committées ; ou la PR de clôture toucherait un chemin CODEOWNERS</fails_when>
  </verify>
  <acceptance_criteria>
    - critère 3 : tag annoté sur le commit de merge, publié, release existante, `check-release-tag.sh --remote` rc 0 avec `✓`, aucune suite `bypass` sur le merge ni sur le tag
    - invariants : 0 gate et 0 suite ajoutés, `ci.yml`/`check-release-tag.sh`/`pre-push` intacts, zone interdite intacte (critère 4)
    - PROT-01..04 cochés sur pièce et `Complete` ; QUAL-01 « non déclenché sur la Phase 41 » ; tout `non_prouvee` recopié
    - rejeux verts ; SUMMARY committé ; PR de clôture hors CODEOWNERS
  </acceptance_criteria>
  <done>Les quatre critères de succès sont prouvés sur pièce et prêts à entrer sur `main` par une PR ordinaire.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4 : gestes humains `samuel-neveugall` — publier C, ouvrir la PR de clôture, merger SANS contournement</name>
  <action>Samuel, compte `samuel-neveugall`, publie C, ouvre la PR de clôture, attend la CI, vérifie l'état `CLEAN`, merge sans contournement, puis rapporte le résultat à l'orchestrateur.</action>
  <instructions>
Compte qui agit : `samuel-neveugall`.

1. `git push -u origin docs/phase-41-cloture`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head docs/phase-41-cloture --title "Phase 41 — clôture : release v<N> prouvée, ledger PROT-01..04" --body "Preuves post-release (tag, release, check-release-tag --remote), invariants de phase, ledger coché sur pièce, QUAL-01 non déclenché. Merge sans contournement."`
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin.
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus --jq .mergeStateStatus`
   → `CLEAN` pour poursuivre ; `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`
   puis 3 et 4 ; tout autre état : arrêt, sans contournement.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) ;
   témoin `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
Le résultat n'est pas écrit dans le dépôt (D-03 : il faudrait une PR de plus) : il est rapporté à
l'orchestrateur, qui le relit par `gh pr view`.
  </instructions>
  <verification>L'orchestrateur relit `gh pr view <n> --json state,mergeCommit` et vérifie que le push de merge sur `main` est vert (4 jobs, `check-release-tag` compris, la `VERSION` étant taggée).</verification>
  <resume-signal>Coller `<n>` et les sorties de 3 (fin), 4 et 5, puis écrire « PR de clôture mergée ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poste de Samuel → tags `v*` | création du tag de release sous le ruleset de tags |
| preuves → ledger | cochage d'exigences, point d'entrée des lecteurs futurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-42 | Repudiation | critère 3 déclaré sans rejeu réel | high | mitigate | tag, release et `check-release-tag.sh --remote` relus et rejoués par l'agent ; rc consigné = rc rejoué |
| T-41-43 | Repudiation | exigence cochée en effaçant un `non_prouvee` | medium | mitigate | vérification de recopie des `non_prouvee` de `TRACE-II:` dans PROT-01/PROT-04 |
| T-41-44 | Tampering | gate né en silence pendant la phase | medium | mitigate | ajouts `check-*.sh` et `tests/test-*.sh` mesurés sur les trois merges ; QUAL-01 déclenché sinon |
| T-41-45 | Denial of service | gate de release ou hook modifié par la phase | medium | mitigate | `ci.yml`, `check-release-tag.sh`, `pre-push` vérifiés intacts sur les trois merges |
| T-41-46 | Repudiation | clôture par commit direct | medium | mitigate | PR de clôture hors CODEOWNERS, état `CLEAN` exigé, `--admin` exclu, merge relu par l'orchestrateur |
</threat_model>

<verification>
Tag annoté et release publiés sans contournement, `check-release-tag` ✓, invariants de phase tenus,
ledger coché sur pièce, PR de clôture mergée par le chemin normal.
</verification>

<success_criteria>
Critères de succès 1 à 4 de la Phase 41 prouvés et consignés ; PROT-01..04 clos sur pièce.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-11-SUMMARY.md` — committé sur la branche C à la fin de la Task 3, AVANT la publication (Task 4).
</output>

---
phase: 17-signaux-de-demarrage-du-moteur-de-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh
  - plugin/dev-orchestrator/hooks/hooks.json
  - plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh
autonomous: true
requirements: [SIG-01, SIG-04]

must_haves:
  truths:
    - "Ordre d'évaluation figé (SIG-01/adjacency, D-01) : sur toute fixture, `check-dev-bootstrap.sh --hook` n'imprime jamais plus d'UN des trois marqueurs `[bootstrap]` / `[onboard]` / `[gsd-engine]` — l'ordre 0→1→2→3 est appliqué et le premier état qui matche gagne, les suivants ne sont pas évalués."
    - "Entrée vide (SIG-01/empty, état 0) : sur un répertoire créé par `mktemp -d` sans aucun fichier, `check-dev-bootstrap.sh --path <vide>` produit un stdout STRICTEMENT vide et sort en 3."
    - "Ordre de restitution stable (SIG-01/ordering, D-03) : à l'état 2, les items manquants sortent dans l'ordre figé `config` → `codebase` → `roadmap`, indépendamment de l'ordre du filesystem ; deux exécutions sur la même fixture produisent un stdout octet-pour-octet identique."
    - "Exception D-14 asserée positivement : à l'état 3, le script produit un stdout NON VIDE (2 lignes `[gsd-engine]`) ET sort en 3 — la sortie et le code de sortie sont asserés séparément, jamais l'un déduit de l'autre."
    - "Soupape D-04 : frontmatter `.planning/STATE.md` absent, illisible, ou privé d'une des 3 clés `milestone` / `current_phase` / `status` → stdout vide et sortie 3. Aucun état inventé, aucune valeur par défaut imprimée."
    - "Assainissement des valeurs de frontmatter (T-17-01) : toute valeur ne matchant pas `^[0-9A-Za-z._ /-]{1,80}$` après retrait des guillemets est rejetée et déclenche le silence total. Un `STATE.md` fabriqué ne peut ni imprimer une 3e ligne, ni glisser une séquence d'échappement ou une directive dans le contexte de session."
    - "Le fragment `plugin/dev-orchestrator/hooks/hooks.json` est un JSON valide déclarant exactement un groupe `SessionStart` de matcher `startup` portant 3 commandes, toutes suffixées par le tolérant d'échec de la spec §3.4."
    - "Lecture seule (SIG-04, D-15) : une exécution de `check-dev-bootstrap.sh` sur une fixture laisse l'arborescence inchangée — l'empreinte `find <fixture> | LC_ALL=C sort` est identique avant et après."
    - "Indépendance des 3 commandes de hook : aucune ne lit ni n'écrit d'état partagé ; permuter leur ordre ou les exécuter concurremment ne change aucune sortie."
  artifacts:
    - plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh
    - plugin/dev-orchestrator/hooks/hooks.json
    - plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh
  key_links:
    - "`hooks/hooks.json` → `{{VF_SCRIPTS}}/check-dev-bootstrap.sh --hook` : le placeholder est résolu par `merge_module_hooks` de l'engine à l'install. Un nom de script erroné rend le signal muet SANS erreur visible (masqué par le tolérant d'échec) — point de rupture silencieuse n°1."
    - "`check-dev-bootstrap.sh` → frontmatter de `.planning/STATE.md` (`milestone`, `current_phase`, `status`) : SEUL endroit où du contenu de dépôt traverse vers le contexte de la session principale. Frontière de confiance du plan."
    - "`check-dev-bootstrap.sh` → `VF_BOOTSTRAP_PLANNING_DIR` : seul levier permettant de tester les états 2 et 3 sans fabriquer un `.planning/` complet. Si l'env n'est pas honorée, la suite teste le repo réel au lieu de sa fixture."
  prohibitions:
    - "P-01 (values) — MUST NOT employer un cadrage culpabilisant, honteux ou alarmiste dans une ligne de signal. Le signal constate un fait et propose un geste ; il ne reproche jamais un manquement à l'utilisateur (pas de « tu n'as toujours pas… », pas de « attention ! », pas de « urgent », pas de compte à rebours ni de perte à éviter)."
    - "P-02 (transparency) — MUST NOT affirmer un état que le script n'a pas observé. Le signal `[gsd-engine]` n'invente jamais milestone / phase / statut quand le frontmatter est illisible, et aucun signal ne requalifie un fait constaté en jugement (« la doc est fausse », « le projet est mal cadré »). En l'absence de fait observé : silence."
    - "P-03 (safety/autonomy) — MUST NOT rédiger la ligne de geste sous une forme impérative qu'une boucle autonome exécuterait sans confirmation. Le signal est injecté dans le contexte de la session principale, potentiellement pilotée par `vf-auto` ; la formulation reste une PROPOSITION explicitement conditionnée à la confirmation humaine (ADR-031), jamais un ordre."
---

<objective>
Poser la tranche traçante de la Phase 17 : le script de constat `check-dev-bootstrap.sh` (continuum
à 4 états, SIG-01) câblé de bout en bout par le fragment `hooks/hooks.json` (D-10), et prouvé sur ce
dépôt même — qui est réellement à l'état 3 (`.planning/config.json`, `.planning/codebase/` et
`ROADMAP.md` avec phases tous présents, vérifié au cadrage).

Purpose: c'est la tranche verticale la plus fine qui traverse TOUTES les couches de la phase
(script de fait → manifeste de hook → contexte de session). Elle prouve d'un coup le contrat de
sortie tordu de la phase (état 3 = sortie NON VIDE **et** exit 3, rupture assumée de la convention
« exit 3 ⇔ silence »), la lecture assainie du frontmatter, le format de signal auto-portant de la
spec §4.1, et la forme du fragment de hooks. Les deux autres scripts (plan 17-02) sont ensuite de
l'expansion horizontale sur une architecture déjà prouvée.

Output: 2 fichiers créés + 1 suite de test créée ; le signal `[gsd-engine]` réellement observable
sur ce dépôt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-PATTERNS.md
@docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md
@plugin/planning-core/scripts/detect-planning-debt.sh
@plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
@plugin/planning-core/hooks/hooks.json
@plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` | fichier créé | script de constat, exécutable par `bash <chemin>` |
| `--path <dir>` | flag CLI (nouveau) | défaut `.` ; sans valeur → 64 |
| `--hook` | flag CLI (nouveau) | change UNIQUEMENT le format de stdout ; les 4 exits sont identiques avec ou sans |
| `--quiet` | flag CLI (nouveau) | supprime les diagnostics `say()` sur stderr |
| `--hook` + `--quiet` | combinaison | exit 64 + message stderr, gate en tête de parsing |
| `-h` / `--help` | flag CLI (nouveau) | imprime l'en-tête `# ` du script, exit 0 |
| `VF_BOOTSTRAP_PLANNING_DIR` | variable d'env (nouvelle) | défaut `<path>/.planning` |
| exits `0` / `3` / `64` | contrat de sortie | jamais d'autre code ; 0 = dette signalée, 3 = rien à signaler OU orientation, 64 = argument invalide |
| `[bootstrap]` | préfixe de signal (nouveau) | état 2, stdout, 2 lignes |
| `[onboard]` | préfixe de signal (nouveau) | état 1, stdout, 2 lignes |
| `[gsd-engine]` | préfixe de signal (nouveau) | état 3, stdout, 2 lignes, exit 3 |
| `[check-dev-bootstrap]` | préfixe `say()` (nouveau) | stderr uniquement, jamais stdout |
| `plugin/dev-orchestrator/hooks/` | répertoire créé | premier dossier de hooks du module |
| `plugin/dev-orchestrator/hooks/hooks.json` | fichier créé | fragment `SessionStart:startup`, 3 commandes, `{{VF_SCRIPTS}}` |
| `plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` | fichier créé | suite ramassée par la découverte générique `ci.yml:32` |

## Détecteurs `plan:pre` non déclenchés

`ai-integration`, `assumption-delta` et `schema-gate` sont actifs sur ce dépôt mais **ne
s'appliquent pas** à ce plan : aucune intégration d'API/SDK externe, aucune transition d'identité
singulier→pluriel ni requis→optionnel, aucun fichier d'ORM ou de schéma touché. Confirmation
explicite, pas un oubli — aucun contenu n'est fabriqué pour ces rails.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) : continuum de démarrage bout-en-bout — `check-dev-bootstrap.sh` + fragment de hooks</name>
  <reversibility rating="reversible">Le format de ligne et le fragment de hooks sont chacun un
  fichier isolé ; `remove_module_hooks` retire proprement le fragment et aucun consommateur ne
  stocke le format — l'annuler coûte une suppression de fichier, pas une migration.</reversibility>
  <read_first>
    - `docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md` §3.1, §3.4, §4.1, §4.2 (fait foi sur les formats et les exits)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-01 à D-05, D-10, D-15
    - `plugin/planning-core/scripts/detect-planning-debt.sh` lignes 40-95 (motif `PRUNE_VENDOR` et idiome `head -n 1`)
    - `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` lignes 1-80 (en-tête `# `, boucle d'arguments, `say()`, env de surcharge, `mktemp` + `trap`)
    - `plugin/planning-core/hooks/hooks.json` (forme structurelle du fragment)
    - `.planning/STATE.md` lignes 1-16 (frontmatter réel à lire : `milestone: gsd-migration`, `current_phase: 15`, `status: shipped`)
    - S'il existe : le livrable de recherche portabilité produit en parallèle par le nœud n0 de la mission (chercher un fichier daté du 2026-07-27 sous `docs/superpowers/` ou `reports/` mentionnant les 6 fixes CI de portabilité). S'il n'existe pas, ne pas bloquer — D-13.2 liste déjà les idiomes à réutiliser.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh, plugin/dev-orchestrator/hooks/hooks.json</files>
  <action>
Créer `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` avec `#!/usr/bin/env bash`,
`set -uo pipefail`, `shopt -s nullglob`, et l'en-tête `# ` documentaire copié dans le style de
`discover-unintegrated-docs.sh` (rôle ADR-055 §3 « répondre au FAIT », Usage, Env, Exit codes).

Boucle d'arguments calquée sur `discover-unintegrated-docs.sh:48-60` : `--path` (défaut `.`, sans
valeur → message stderr + 64), `--hook` (met `HOOK=1`), `--quiet` (met `QUIET=1`), `-h|--help`
(`grep '^# ' "$0" | sed 's/^# //'`, exit 0), défaut → message stderr + 64. Après la boucle, gate de
mutuelle exclusion : `HOOK=1` ET `QUIET=1` ensemble → message stderr + 64, avant toute autre logique
(D-05, même position que le gate `--path` sans valeur). Helper `say()` identique au modèle, préfixe
`[check-dev-bootstrap]`, sortie sur **stderr** — stdout est réservé aux seules lignes de signal.

Env de surcharge (D-05) : `PLANNING_DIR="${VF_BOOTSTRAP_PLANNING_DIR:-$ROOT/.planning}"`.

Détection « code source présent » (D-02) : reprendre TEXTUELLEMENT le motif `PRUNE_VENDOR` de
`detect-planning-debt.sh:54` (`.git`, `node_modules`, `.venv`, `vendor`, `dist`, `build`, `.next`),
en conservant le commentaire d'expansion non quotée, puis l'ÉTENDRE avec `docs`, `.planning`,
`.claude` — spec §3.1 : ces trois dossiers ne comptent pas comme du code. Utiliser
`find "$ROOT" $PRUNE_VENDOR -o -type f -print 2>/dev/null | head -n 1` : l'existence d'UN fichier
suffit, jamais de comptage exhaustif (contrainte de perf du SessionStart). Aucune dépendance à git.

Continuum à 4 états, évalués dans cet ordre, premier qui matche gagne (D-01) :
- état 0 — ni code source ni répertoire `$PLANNING_DIR` : aucune sortie sur stdout, exit 3.
- état 1 — code source présent et `$PLANNING_DIR` absent : signal `[onboard]` sur stdout, exit 0.
- état 2 — `$PLANNING_DIR/PROJECT.md` présent et au moins un item de démarrage manquant : signal
  `[bootstrap]` listant les items, exit 0.
- état 3 — `$PLANNING_DIR/PROJECT.md` présent et tous les items posés : signal `[gsd-engine]` sur
  stdout, exit **3** (orientation, pas dette — D-01/D-14).

Items de l'état 2, dans cet ordre de restitution figé (D-03) : `config` si `$PLANNING_DIR/config.json`
est absent → geste `gsd-config` ; `codebase` si du code source est présent ET
`$PLANNING_DIR/codebase/` est absent ou vide → geste `gsd-map-codebase` (conditionné à la présence de
code : un greenfield n'a rien à cartographier) ; `roadmap` si `$PLANNING_DIR/ROADMAP.md` est absent
OU présent sans aucune ligne d'en-tête de phase → geste `gsd-plan-phase`. L'ordre est produit par la
séquence des tests dans le code, jamais par un parcours de filesystem, pour que deux exécutions sur
la même fixture donnent un stdout identique octet pour octet.

Lecture du frontmatter pour l'état 3 (D-04) — c'est le seul point où du contenu de dépôt traverse
vers le contexte de session ; l'assainissement est un mitigation de menace, pas de la cosmétique
(voir `<threat_model>` T-17-01) :
- borner la lecture : la ligne 1 de `$PLANNING_DIR/STATE.md` doit être exactement le délimiteur de
  frontmatter, la lecture s'arrête au délimiteur suivant, et au maximum aux 60 premières lignes
  (garde anti-gel, T-17-03). Extraire les valeurs des 3 clés `milestone`, `current_phase`, `status`
  par un match awk POSIX ancré en début de ligne (`^<clé>:[[:space:]]*`), jamais par `grep` non ancré.
- retirer les guillemets simples ou doubles encadrants, puis VALIDER chaque valeur contre
  `^[0-9A-Za-z._ /-]{1,80}$`. Une valeur qui échoue (caractère de contrôle, séquence d'échappement,
  accent, longueur excessive) fait retomber le script en silence total : aucune sortie sur stdout,
  exit 3. Idem si une des 3 clés est absente, si le fichier est absent, ou si la ligne 1 n'est pas le
  délimiteur. Documenter dans l'en-tête `# ` que la liste blanche est volontairement stricte et que
  son échec est une soupape de sûreté (D-04), pas un bug.
- dériver l'état lisible depuis `status` avec une correspondance fixe : `shipped` → « shippée »,
  `in_progress` ou `in-progress` → « en cours », `planned` ou `not_started` → « non démarrée » ;
  toute autre valeur (déjà passée par la liste blanche) est reprise littéralement.

Formats de signal — reprendre VERBATIM les gabarits de la spec §4.1 (lignes 200-211) pour
`[onboard]`, `[bootstrap]` et `[gsd-engine]` : exactement 2 lignes par signal (le constat, puis la
ligne de geste indentée commençant par la flèche). La ligne de geste est rédigée comme une
proposition conditionnée à la confirmation humaine, jamais comme un ordre (P-03) ; le ton constate
sans reprocher (P-01) et ne qualifie jamais au-delà de ce qui a été observé (P-02).

Contrat de sortie : seuls les codes 0, 3 et 64 sont utilisés. Aucune écriture sur le filesystem —
si un fichier temporaire s'avère nécessaire, `mktemp` + `trap ... EXIT` sur le modèle de
`discover-unintegrated-docs.sh:77-80`, jamais de fichier à un chemin choisi (D-15).

Portabilité (D-13.2) : réutiliser uniquement les idiomes déjà portables du dépôt — `set -uo pipefail`,
`mktemp` + `trap ... EXIT`, `LC_ALL=C sort`, awk POSIX (échappement caractère par caractère, jamais
de `gsub` global piégeux — cf. le commentaire de `discover-unintegrated-docs.sh:100-101`), `find`
avec `-prune` avant descente. Interdits : builtins bash4+ (`mapfile`, `${var,,}`), `sed -i` sans
extension portable, `stat -f`, `grep -P`, `readlink -f`.

Créer ensuite `plugin/dev-orchestrator/hooks/hooks.json` (le répertoire `hooks/` n'existe pas
encore) avec le contenu VERBATIM de la spec §3.4 / D-10 : la clé `description` telle qu'écrite, un
unique groupe `SessionStart` de matcher `startup` portant les 3 commandes
`check-dev-bootstrap.sh --hook`, `discover-unintegrated-docs.sh --hook` et
`check-doc-drift.sh --hook`, chacune préfixée `bash {{VF_SCRIPTS}}/` et suffixée par le tolérant
d'échec de la spec. Ne rien redessiner. Les deux derniers scripts n'existent pas encore (plan
17-02) : c'est intentionnel et sans effet, le tolérant d'échec neutralise l'absence — c'est
précisément l'invariant « advisory, jamais bloquant » de SC5 que cette tranche prouve.
  </action>
  <verify>
    <automated>bash -n plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh && jq -e '.hooks.SessionStart | length == 1 and .[0].matcher == "startup" and (.[0].hooks | length) == 3' plugin/dev-orchestrator/hooks/hooks.json >/dev/null && out=$(bash plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh --hook --path . 2>/dev/null); rc=$?; [ "$rc" -eq 3 ] && [ "$(printf '%s\n' "$out" | wc -l | tr -d ' ')" = "2" ] && printf '%s' "$out" | grep -q 'gsd-engine' && printf '%s' "$out" | grep -q 'gsd-migration' && printf '%s' "$out" | grep -q 'phase 15' && echo TRACER-OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` sort en 0.
    - Sur ce dépôt (état 3 réel), `bash plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh --hook --path .` imprime EXACTEMENT 2 lignes sur stdout et sort en **3** ; la première contient `gsd-engine`, `gsd-migration` et `phase 15` (valeurs lues dans le frontmatter réel de `.planning/STATE.md`, non codées en dur).
    - Le même appel avec `--path <mktemp -d vide>` imprime 0 octet sur stdout et sort en 3.
    - `bash plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh --hook --quiet` sort en 64, stdout vide, message non vide sur stderr.
    - `bash plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh --flag-inexistant` sort en 64 ; `--path` sans valeur sort en 64.
    - `jq -e '.hooks.SessionStart[0].matcher == "startup"'` sort en 0 sur `hooks/hooks.json`, et le tableau `.hooks.SessionStart[0].hooks` contient 3 entrées dont chaque `.command` commence par `bash {{VF_SCRIPTS}}/` et se termine par le tolérant d'échec de la spec §3.4.
    - `grep -vE '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh | grep -cE 'exit[[:space:]]+1$'` retourne `0` (aucune sortie en code 1 hors commentaire).
    - `grep -vE '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh | grep -cE 'mapfile|\$\{[A-Za-z_]+,,\}|sed -i |stat -f|grep -P|readlink -f'` retourne `0` (aucun idiome non portable).
  </acceptance_criteria>
  <done>Le signal `[gsd-engine]` est réellement observable sur ce dépôt avec le bon milestone et la bonne phase, le fragment de hooks est un JSON valide à la forme du modèle `planning-core`, et le contrat 0/3/64 tient sur les 4 états — la couche script → hook → contexte est prouvée de bout en bout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : suite `test-check-dev-bootstrap.sh` — 4 états isolés, sortie et exit asserés séparément</name>
  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` (fichier entier — harnais à reproduire tel quel : `PASS`/`FAIL`, `ok()`/`ko()`, `mktemp -d` + `trap`, `mk_root`, blocs `# === Cas N ===`, pied `[ "$FAIL" -eq 0 ]`)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-14 (le piège central de cette suite) et D-01 à D-05
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` (produit en Task 1)
  </read_first>
  <files>plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh</files>
  <behavior>
    - État 0 (répertoire vide) : stdout vide ET exit 3, asserés par deux conditions distinctes.
    - État 1 (un fichier de code, aucun `.planning/`) : stdout contient `[onboard]`, exit 0, et ne contient NI `[bootstrap]` NI `[gsd-engine]`.
    - État 2 (`.planning/PROJECT.md` présent, `config.json` absent) : stdout contient `[bootstrap]` et la mention de l'item `config`, exit 0, sans fuite des deux autres marqueurs.
    - État 2 — ordre figé : fixture où les 3 items manquent, l'ordre `config` avant `codebase` avant `roadmap` est vérifié par comparaison de positions dans la sortie ; deux exécutions consécutives donnent une sortie identique.
    - État 2 — item `codebase` conditionné : fixture greenfield (aucun fichier de code, `.planning/PROJECT.md` présent, `codebase/` absent) → l'item `codebase` n'apparaît PAS.
    - État 3 (cas dédié D-14) : stdout NON VIDE (assertion positive `[ -n "$out" ]`) ET exit 3, dans deux conditions séparées ; la sortie fait exactement 2 lignes et reprend les valeurs du frontmatter de la fixture.
    - État 3 — soupape D-04 : `STATE.md` absent / sans délimiteur de frontmatter / privé de la clé `status` → stdout vide ET exit 3, sans marqueur `[gsd-engine]`.
    - Assainissement (T-17-01) : `STATE.md` dont `milestone` porte une valeur hors liste blanche (caractère de contrôle, séquence d'échappement, valeur de plus de 80 caractères, ou une valeur contenant un retour à la ligne encodé) → stdout vide, exit 3, et le contenu fabriqué n'apparaît nulle part sur stdout.
    - Élagage (D-02) : un `node_modules/` peuplé au sein d'une fixture par ailleurs vide ne fait PAS basculer le script en état 1 ; un fichier sous `docs/` seul non plus.
    - Env `VF_BOOTSTRAP_PLANNING_DIR` : pointer l'env vers un `.planning` hors `--path` change bien l'état constaté.
    - Arguments : `--hook` + `--quiet` → exit 64 stdout vide ; argument inconnu → exit 64 ; `--path` sans valeur → exit 64 ; `--help` → exit 0 avec sortie non vide.
    - Lecture seule (D-15) : empreinte `find <fixture> | LC_ALL=C sort` identique avant et après exécution.
  </behavior>
  <action>
Créer `plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` en reproduisant
TEXTUELLEMENT le harnais de `test-discover-unintegrated-docs.sh` : shebang, en-tête `# `, `set -uo
pipefail`, résolution `SCRIPT="$(cd "$(dirname "$0")/.." && pwd)/check-dev-bootstrap.sh"`, compteurs
`PASS`/`FAIL`, fonctions `ok()`/`ko()`, `TMP="$(mktemp -d)"` + `trap 'rm -rf "$TMP"' EXIT`, helper
`mk_root` adapté (crée `$TMP/<nom>` et rien d'autre — chaque cas construit son propre état), blocs
numérotés `# === Cas N — <piège> ===`, et le pied `echo "== résultat : $PASS ok, $FAIL ko =="` suivi
de `[ "$FAIL" -eq 0 ]`.

Règle d'assertion non négociable (D-14) : chaque cas capture stdout et le code de retour dans deux
variables distinctes (`out="$(bash "$SCRIPT" ... 2>/dev/null)"; rc=$?`) et les teste par deux
conditions séparées. Interdiction absolue d'écrire une assertion qui déduit l'absence de sortie du
code 3 : l'état 3 est précisément le contre-exemple, et une assertion combinée rendrait ce cas
faussement vert ou faussement rouge selon le sens de l'erreur. Le cas dédié à l'état 3 assert
POSITIVEMENT que stdout est non vide.

Mutuelle exclusion (SC2, D-14) : pour chacun des états 1, 2 et 3, une fixture isolée qui construit
CET état seul, et l'assertion que les deux autres marqueurs n'apparaissent pas dans la sortie —
trois vérifications négatives par cas, pas une seule positive.

Isoler stderr de stdout partout : capturer stdout via substitution de commande avec `2>/dev/null`,
et pour les cas qui vérifient un message de diagnostic, capturer stderr séparément via
`2>"$TMP/err"`. Aucun cas ne mélange les deux flux.

Le cas d'assainissement fabrique un `STATE.md` hostile avec `printf` (pas un heredoc) pour pouvoir
injecter des octets de contrôle, et vérifie deux choses : le script reste muet, et la chaîne
fabriquée n'apparaît nulle part dans stdout.

Portabilité (D-13.2) : `printf` plutôt qu'`echo -e`, pas de `mapfile`, pas de `sed -i`, `LC_ALL=C`
sur tout tri, aucune dépendance à un outil hors coreutils/awk/grep/find.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` sort en 0 et sa dernière ligne de résultat annonce `0 ko`.
    - La suite compte au moins 15 assertions (`grep -c 'ok "' ` sur le fichier ≥ 15 après filtrage des lignes de commentaire par `grep -vE '^[[:space:]]*#'`).
    - Au moins un cas assert `[ -n "$out" ]` conjointement à `[ "$rc" -eq 3 ]` (le cas état 3 de D-14) : `grep -vE '^[[:space:]]*#' <fichier> | grep -c -- '-n "\$out"'` retourne au moins 1.
    - Aucune assertion ne combine code de sortie et absence de sortie en une seule condition déduite : chaque bloc de cas contient une affectation `rc=$?` distincte de la capture de `out`.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c 'test-check-dev-bootstrap.sh'` retourne `1` — la suite est ramassée par la découverte générique de `.github/workflows/ci.yml:32` sans édition de la CI.
    - La suite passe deux fois d'affilée sans nettoyage manuel (idempotence des fixtures).
  </acceptance_criteria>
  <done>Les 4 états sont couverts par des fixtures isolées, la mutuelle exclusion des 3 signaux est prouvée, l'exception D-14 (sortie non vide + exit 3) est asserée positivement, et la soupape de sûreté du frontmatter est testée y compris sur une valeur hostile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenu du dépôt → contexte de la session principale | `.planning/STATE.md` est un fichier versionné, potentiellement issu d'un dépôt cloné non maîtrisé. Ses valeurs de frontmatter sont IMPRIMÉES sur stdout d'un hook `SessionStart`, donc injectées telles quelles dans le contexte du modèle. C'est la frontière critique de la phase. |
| arborescence arbitraire → script | `--path <dir>` et `VF_BOOTSTRAP_PLANNING_DIR` font parcourir et lire une arborescence choisie par l'appelant. |
| arguments CLI → script | `--path`, `--hook`, `--quiet` proviennent du manifeste de hooks (maîtrisé) mais le script est aussi invocable à la main. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering / Elevation of Privilege | `check-dev-bootstrap.sh` — lecture du frontmatter de `.planning/STATE.md` et impression de la ligne `[gsd-engine]` | high | mitigate | Extraction par awk POSIX **ancré** (`^<clé>:[[:space:]]*`) des 3 seules clés `milestone`/`current_phase`/`status`, jamais de `cat` du fichier ; retrait des guillemets encadrants ; **liste blanche stricte** `^[0-9A-Za-z._ /-]{1,80}$` appliquée à chaque valeur ; tout échec → silence total + exit 3 (soupape D-04). Une valeur portant un octet de contrôle, une séquence d'échappement, ou plus de 80 caractères ne peut donc ni s'imprimer, ni produire une 3e ligne, ni glisser une directive dans le contexte. Testé par le cas d'assainissement de la Task 2 avec un `STATE.md` hostile. |
| T-17-02 | Information Disclosure | `--path` / `VF_BOOTSTRAP_PLANNING_DIR` pointant hors du projet courant | medium | mitigate | Le script ne lit JAMAIS le contenu d'un fichier autre que le frontmatter borné de `STATE.md` ; les autres constats sont des tests d'existence (`-f`, `-d`, `find … | head -n 1`) qui n'impriment aucun chemin. Aucun chemin absolu, aucun nom de fichier de l'arborescence scannée n'est écrit sur stdout — seuls les libellés d'items figés (`config`, `codebase`, `roadmap`) le sont. |
| T-17-03 | Denial of Service | parcours de l'arborescence + lecture de `STATE.md` au `SessionStart` | medium | mitigate | `find` avec `-prune` **avant** descente (motif `PRUNE_VENDOR` étendu, D-02) — un `node_modules` réel n'est jamais parcouru ; `head -n 1` arrête `find` au premier fichier trouvé, aucun comptage exhaustif ; lecture du frontmatter bornée au délimiteur fermant et au maximum à 60 lignes. Un hook qui gèle est un hook tué au timeout, donc un signal perdu silencieusement. |
| T-17-04 | Denial of Service / Repudiation | invocation par le hook `SessionStart` | medium | mitigate | Le contrat de sortie n'utilise que 0, 3 et 64 (aucun code d'échec bloquant), et chaque commande du fragment est suffixée par le tolérant d'échec de la spec §3.4 ; aucun hook `Stop`, aucun blocage de tour (SC5 / D-15). Vérifié par grep structurel dans les critères d'acceptation de la Task 1 et par le test T21 du plan 17-03. |
| T-17-05 | Tampering | écriture sur le filesystem par un script censé être en lecture seule | high | mitigate | Aucune écriture hors `mktemp` + `trap ... EXIT` ; prouvé par l'empreinte `find <fixture> | LC_ALL=C sort` comparée avant/après dans la Task 2, et par le test structurel T21 du plan 17-03 (grep : aucune redirection vers un chemin construit). |
| T-17-SC | Tampering (supply chain) | installation de paquets | low | accept | Aucune installation de paquet dans cette phase — aucun `npm` / `pip` / `cargo install`, aucune nouvelle dépendance. Le gate de légitimité de paquets est donc sans objet ici, et l'absence de `## Package Legitimacy Audit` dans un RESEARCH.md (inexistant pour cette phase) n'est pas une lacune. |
</threat_model>

## Flagged Assumptions (résidu de sonde, spec-less fallback)

La phase n'a pas de `17-SPEC.md` : les sections `## Edge Coverage` et `## Prohibitions` sont absentes
et la sonde d'arêtes a été résolue à la main (§A/§B/§C de `specless-probe-fallback.md`). Sur les 14
items remontés, 11 ont été résolus en `truths` explicites, 1 en marqueur `backstop` (plan 17-02), et
2 restent **non résolus** parce que la sonde les a classés `unclassified` — ils sont remontés ici
plutôt que silencieusement écartés.

- **SIG-04 / `unclassified`** — « unclassified — review manually ». Revue manuelle faite : SIG-04
  (contrat advisory/lecture seule) est un invariant transverse, pas une arête de forme de données ;
  la sonde n'a pas su le classer parce qu'aucune catégorie du taxon (boundary/empty/ordering/…) ne
  s'y applique. Il est néanmoins entièrement couvert par des `truths` explicites dans les trois
  plans (lecture seule, contrat 0/3/64, tolérant d'échec sur chaque commande de hook) et par les
  menaces T-17-04 / T-17-05. **Assomption restante** : aucune. L'item est laissé `unresolved` au
  sens de la sonde, mais sa couverture réelle est tracée — il n'est pas un trou.

Le second `unclassified` (SIG-06, portabilité) est remonté dans le plan 17-03, qui le porte.

**Égalité sans perte silencieuse (§C)** — arêtes : 14 items remontés = 11 `truths` (3 en 17-01,
8 en 17-02/17-03) + 1 `backstop` (17-02) + 2 assomptions remontées (une ici, une en 17-03).
Prohibitions : 12 candidates brutes en Stage 1 → 3 retenues en Stage 2 (P-01/P-02/P-03, écrites
sans descripteur `check_*`), 3 écartées par renvoi au canon (voir ci-dessous), 6 écartées comme
ingénierie de routine (déjà couvertes par les `truths` de SC5). Aucune candidate n'est perdue.

**Renvois au canon (breadcrumbs, non mintés)** :
- injection de contexte via le frontmatter de `STATE.md` → canon, traité par `<threat_model>` T-17-01 et `/gsd-secure-phase` ; non minté en prohibition.
- traversée de chemin via `--path` → canon, `/gsd-secure-phase` ; non minté.
- exfiltration réseau depuis un script de hook → canon, `/gsd-secure-phase` ; non minté.

<verification>
- `bash -n` passe sur le script créé.
- `jq -e` valide la forme du fragment `hooks/hooks.json`.
- `bash plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` sort en 0 avec `0 ko`.
- Sur ce dépôt : le signal `[gsd-engine]` s'affiche en 2 lignes avec `gsd-migration` / `phase 15`, exit 3.
- Aucun fichier hors des 3 artefacts déclarés n'est modifié (`git status --short` ne montre que ces chemins plus le répertoire `hooks/`).
</verification>

<success_criteria>
- SIG-01 tenu : le continuum à 4 états vit dans UN seul script, les 3 signaux sont mutuellement exclusifs et prouvés tels par test.
- SIG-04 tenu côté `check-dev-bootstrap.sh` : lecture seule, contrat 0/3/64, aucun blocage de tour.
- La tranche traçante est réellement de bout en bout : la même exécution qui produit le signal est celle que le hook lancera à l'install (mêmes flags, même script).
- Les prohibitions P-01 / P-02 / P-03 sont respectées par les libellés de signal produits.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-01-SUMMARY.md` en fin de plan.
</output>

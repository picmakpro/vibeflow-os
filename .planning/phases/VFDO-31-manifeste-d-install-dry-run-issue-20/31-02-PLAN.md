---
phase: 31-manifeste-d-install-dry-run-issue-20
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/_internal/merge-hooks.sh
  - plugin/_internal/tests/test-merge-hooks.sh
autonomous: true
requirements: [MANI-02, QUAL-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "merge-hooks.sh accepte un troisième mode `plan` (D-31-04 régime B)"
    - "Le mode `plan` rend sur stdout une ligne par fichier settings qu'un `merge` réel écrirait, et n'écrit RIEN"
    - "Le mode `plan` réutilise la MÊME logique de répartition project/local que `merge` (split_fragment_hooks / is_local_entry) — jamais une simulation parallèle"
    - "Le mode `plan` annonce settings.local.json dès que --settings-local est fourni, parce que write_json l'écrit inconditionnellement dans ce cas (merge-hooks.sh:444-446)"
    - "Les diagnostics du mode `plan` restent sur stderr avec le préfixe existant ; seule la ligne de plan va sur stdout"
  artifacts:
    - "plugin/_internal/merge-hooks.sh : mode `plan` dans l'allowlist de dispatch (ligne 53)"
    - "plugin/_internal/tests/test-merge-hooks.sh : cas T dédiés au mode plan"
  key_links:
    - "vibeflow-update.sh merge_module_hooks (369-412) -> merge-hooks.sh plan : le point de délégation existe déjà, 31-04 s'y branche"
    - "mode plan -> split_fragment_hooks/is_local_entry (201-236) : la répartition n'est PAS recalculée"
  prohibitions:
    - "Le mode `plan` n'appelle JAMAIS write_json — aucun fichier créé, modifié ou supprimé"
    - "La logique de merge n'est JAMAIS réimplémentée côté vibeflow-update.sh (D-31-04 régime B, anti-pattern REQUIREMENTS.md:959)"
    - "Aucune modification du comportement des modes merge et remove existants"
---

<objective>
`merge-hooks.sh` apprend un troisième mode, `plan`, qui rend sa propre prévisualisation. C'est la
réponse de D-31-04 au régime B : le seul cas où l'exactitude exigée par l'issue #20 impose de
toucher un second script — parce que réimplémenter la simulation de merge côté engine serait
exactement le « dry-run en chemin de code séparé » que `REQUIREMENTS.md:959` place hors périmètre.

Purpose: rendre le `--dry-run` de 31-04 honnête sur le merge de hooks sans dupliquer la dédup par
basename, le routage `{{VF_BASH}}` et la substitution exec-safe qui vivent déjà ici.

Output: un mode `plan` dans `merge-hooks.sh`, ses cas de suite, et un contrat de sortie stable que
l'engine consommera tel quel.

Ce plan est parallèle à 31-01 (vague 1) : les deux ensembles de fichiers sont disjoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-CONTEXT.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-RESEARCH.md
@.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : mode `plan` dans merge-hooks.sh (D-31-04 régime B)</name>
  <files>plugin/_internal/merge-hooks.sh</files>
  <read_first>
    - plugin/_internal/merge-hooks.sh:1-41 (en-tête d'usage — à étendre, il est imprimé comme aide en cas d'argument manquant, ligne 52)
    - plugin/_internal/merge-hooks.sh:42-72 (dispatch de mode ligne 53, parsing des flags, exigence `--scripts-prefix` en merge ligne 70-72)
    - plugin/_internal/merge-hooks.sh:123-135 (frontière bash -> heredoc Python : les variables d'environnement passées)
    - plugin/_internal/merge-hooks.sh:175-206 (`load_settings_dict`, `frag_basenames`) et 233-260 (`is_local_entry`, `split_fragment_hooks`)
    - plugin/_internal/merge-hooks.sh:404-449 (branche `merge`, `write_json`, appels 444-446 — settings.local.json écrit INCONDITIONNELLEMENT si --settings-local est fourni ; ligne de bilan stderr 449)
    - 31-CONTEXT.md §3 D-31-04 (régime B) et D-31-05 (format de sortie, canal stdout)
    - 31-PATTERNS.md « merge-hooks.sh — add plan mode »
  </read_first>
  <behavior>
    - `merge-hooks.sh plan <fragment> --settings S --scripts-prefix P` sort 0, écrit une ligne de
      plan sur stdout pour `S`, et laisse `S` inexistant sur disque.
    - Avec `--settings-local L` en plus : deux lignes de plan (une pour `S`, une pour `L`), et
      ni `S` ni `L` ne sont créés.
    - La ligne de plan porte, pour chaque cible, le verbe `~`, le chemin de la cible, et
      l'énumération des couples événement/matcher que le merge ajouterait, sous la forme fixée par
      D-31-05 : `hooks.<Événement> += <basenames> (matcher: <matcher>)`.
    - Un `plan` sur un fragment dont toutes les entrées sont déjà présentes dans le settings rend
      quand même la ligne de la cible (le merge réel réécrit le fichier, donc l'annoncer est exact).
    - Un mode inconnu autre que merge|remove|plan reste refusé avec le message d'erreur existant.
    - Le fragment introuvable reste refusé avant tout traitement (ligne 54 inchangée).
  </behavior>
  <action>
Étendre l'allowlist de la ligne 53 de `merge-hooks.sh` : `case "$MODE" in merge|remove|plan)`.
Étendre la garde de la ligne 70 pour exiger `--scripts-prefix` en mode `plan` AUSSI (le plan doit
résoudre les mêmes chemins que le merge, sinon il ment sur ce qu'il annonce) — condition
`[ "$MODE" = "merge" ] || [ "$MODE" = "plan" ]`. Étendre le bloc de commentaires d'usage (lignes
6-8) avec la ligne `merge-hooks.sh plan <fragment.json> --settings <settings.json> --scripts-prefix <prefix> [--settings-local <settings-local.json>]` et une phrase disant que `plan` n'écrit rien.

Dans le heredoc Python : après le calcul de `local_view, project_view = split_fragment_hooks(frag_hooks)`,
brancher `mode == "plan"` AVANT la branche `merge`. Le mode `plan` réutilise intégralement
`split_fragment_hooks` et `is_local_entry` — il ne recalcule aucune répartition. Pour chaque cible
concernée (toujours `settings_path` ; plus `settings_local_path` s'il est fourni, parce que
`write_json` l'écrit inconditionnellement dans ce cas, lignes 444-446), émettre sur **stdout** une
ligne par cible :

`[plan] ~ <chemin_cible>  hooks.<Événement> += <basenames séparés par une virgule> (matcher: <matcher>)`

— un segment `hooks.<Événement> += …` par groupe de la vue correspondante, séparés par deux
espaces si plusieurs. Le chemin de cible affiché est celui reçu en argument, sans réécriture :
l'engine passe déjà `$TARGET_ROOT/settings.json`, donc le préfixe visible par l'utilisateur exigé
par D-31-05 est obtenu sans logique supplémentaire ici. Les basenames sortent de `SCRIPT_RE`
appliqué aux `command`/`args` résolus, comme le fait déjà `frag_basenames`. Si une vue est vide
pour une cible, émettre la ligne avec le segment `(aucune entrée)` plutôt que rien — la cible est
quand même écrite par le merge réel.

Sortir immédiatement après l'émission (`sys.exit(0)`) : ne JAMAIS atteindre `write_json`. Émettre
également, sur **stderr** et avec le préfixe existant `[merge-hooks] `, la ligne de bilan
`plan OK → <settings>[ (+ <local>)]` calquée sur la ligne 449 — les diagnostics restent sur stderr,
seul le produit va sur stdout (discipline de canal, 31-PATTERNS.md).

Ne modifier ni la branche `merge` ni la branche `remove`.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/_internal/merge-hooks.sh` sort 0.
    - Le mode est dans l'allowlist : `grep -c 'merge|remove|plan' plugin/_internal/merge-hooks.sh` rend au moins `1`.
    - Le mode plan n'écrit rien : sur un répertoire temporaire vide, `bash plugin/_internal/merge-hooks.sh plan plugin/software-architecture/hooks/hooks.json --settings "$T/settings.json" --scripts-prefix '"$HOME"/.claude/scripts' --settings-local "$T/settings.local.json" >/dev/null` laisse `find "$T" -type f | wc -l` à `0`.
    - Deux lignes de plan avec `--settings-local` : la sortie stdout compte exactement 2 lignes commençant par `[plan] ~ `.
    - Une seule ligne sans `--settings-local`.
    - `bash plugin/_internal/tests/test-merge-hooks.sh` sort 0 (les modes merge/remove sont intacts).
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0 (non-régression du consommateur).
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/_internal/merge-hooks.sh && bash plugin/_internal/tests/test-merge-hooks.sh && bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>`merge-hooks.sh plan` rend son propre plan, sur stdout, sans jamais écrire, en réutilisant la répartition existante.</done>
  <reversibility rating="reversible">Un troisième mode additif ; les deux modes existants sont inchangés et leur suite le prouve. Retour arrière = suppression de la branche.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : cas de suite du mode `plan` dans test-merge-hooks.sh</name>
  <files>plugin/_internal/tests/test-merge-hooks.sh</files>
  <read_first>
    - plugin/_internal/tests/test-merge-hooks.sh:1-80 (en-tête `# T<n> — …`, convention de numérotation, style de documentation inline des frontières architecturales)
    - plugin/_internal/merge-hooks.sh (le mode `plan` tel que livré en tâche 1)
    - 31-CONTEXT.md §4 point 4 (anti-vert-à-vide) et point 6 (aucun échappement d'échec sur les nouveaux chemins)
  </read_first>
  <behavior>
    - Tp1 : `plan` avec `--settings` seul → exactement 1 ligne stdout préfixée `[plan] ~ `, exit 0,
      `settings.json` absent du disque après coup.
    - Tp2 : `plan` avec `--settings` + `--settings-local` → exactement 2 lignes stdout, les deux
      fichiers absents du disque après coup.
    - Tp3 : le répertoire cible entier est inchangé — empreinte `find <dir> | sort` identique
      avant et après l'appel `plan`.
    - Tp4 : `plan` sans `--scripts-prefix` échoue (exit 1) avec le message d'exigence — un plan
      dont les chemins ne sont pas résolus mentirait.
    - Tp5 : un mode inconnu (`preview`) reste refusé, exit 1 — l'allowlist n'a pas été ouverte.
  </behavior>
  <action>
Ajouter 5 cas numérotés (`Tp1`..`Tp5`, préfixe `p` pour « plan », afin de ne pas renuméroter les
`T<n>` existants) à la fin de `plugin/_internal/tests/test-merge-hooks.sh`, avant le bilan final,
et documenter chacun d'une ligne `# Tp<n> — <ce que ça prouve>` dans l'en-tête du fichier selon la
convention existante.

Chaque cas crée son propre `mktemp -d`, utilise comme fragment
`plugin/software-architecture/hooks/hooks.json` (fragment réel du repo, forme shell), capture
stdout et stderr SÉPARÉMENT (le contrat de canal est justement ce qui est testé : un plan mélangé
aux diagnostics serait incapturable par le skill consommateur), et nettoie son répertoire.

Tp3 prend l'empreinte du répertoire cible par `find "$T" | LC_ALL=C sort` avant et après, et
compare les deux chaînes — c'est la forme locale de la garantie « un dry-run n'écrit rien du tout »
de D-31-06, appliquée au seul sous-processus qui délègue.

Mettre à jour le compteur de cas de l'en-tête s'il en porte un.
  </action>
  <acceptance_criteria>
    - `bash plugin/_internal/tests/test-merge-hooks.sh` sort 0.
    - Les 5 cas sont exécutés : la sortie contient `Tp1`, `Tp2`, `Tp3`, `Tp4` et `Tp5`.
    - `grep -c '^# Tp' plugin/_internal/tests/test-merge-hooks.sh` rend `5` (en-tête documenté).
    - Discriminance : en neutralisant la garde `--scripts-prefix` du mode plan, Tp4 rougit — trace (assertion, attendu, obtenu) à consigner dans le SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <done>Le mode `plan` est prouvé non écrivain, à canal correct, et refusant un plan non résolu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fragment hooks.json (cache) -> settings*.json (lab) | le fragment est du contenu de module ; le mode `plan` le lit sans jamais franchir la frontière d'écriture |
| stdout (produit consommé) vs stderr (diagnostic) | le skill consommateur capture stdout ; y laisser fuir un diagnostic corromprait ce qui est présenté à l'utilisateur avant son feu vert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-08 | Tampering | mode `plan` de merge-hooks.sh | high | mitigate | `sys.exit(0)` avant toute atteinte de `write_json` ; Tp3 compare l'empreinte `find` du répertoire cible avant/après |
| T-31-09 | Spoofing | ligne de plan affichée à l'utilisateur avant son feu vert (ADR-031) | medium | mitigate | le plan est dérivé de `split_fragment_hooks`/`is_local_entry`, la MÊME répartition que le merge réel — pas de seconde implémentation qui pourrait diverger et présenter un plan faux |
| T-31-10 | Information Disclosure | diagnostic mélangé au produit sur stdout | low | mitigate | bilan sur stderr avec le préfixe existant, plan sur stdout ; Tp1/Tp2 capturent les deux canaux séparément |
| T-31-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par cette phase (31-RESEARCH.md § Package Legitimacy Audit : N/A) |
</threat_model>

<verification>
1. `bash -n plugin/_internal/merge-hooks.sh`
2. `bash plugin/_internal/tests/test-merge-hooks.sh`
3. `bash plugin/_internal/tests/test-vibeflow-update.sh` (non-régression du consommateur)
4. Preuve APRÈS commit sur l'arbre tel que commité :
   `T=$(mktemp -d); git archive HEAD | tar -x -C "$T"; bash "$T/plugin/_internal/tests/test-merge-hooks.sh" && bash "$T/plugin/_internal/tests/test-vibeflow-update.sh"; rm -rf "$T"`
</verification>

<success_criteria>
- Un troisième mode `plan` existe, exige `--scripts-prefix`, et n'écrit rien.
- Il annonce les DEUX cibles quand `--settings-local` est fourni (fidèle à `write_json` 444-446).
- Sa répartition project/local est celle du merge réel, jamais une seconde implémentation.
- Les modes `merge` et `remove` sont inchangés, prouvé par la suite existante restée verte.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribution de ce plan)

**Nouveau mode CLI**
- `merge-hooks.sh plan <fragment.json> --settings <s> --scripts-prefix <p> [--settings-local <l>]`

**Nouveau contrat de sortie (stdout, consommé par l'engine en 31-04)**
- `[plan] ~ <chemin_settings>  hooks.<Événement> += <basenames> (matcher: <matcher>)`
- une ligne par fichier settings que le `merge` réel écrirait

**Nouveaux cas de suite**
- `Tp1`..`Tp5` dans `plugin/_internal/tests/test-merge-hooks.sh`
</artifacts_produced>

<output>
Créer `.planning/phases/VFDO-31-manifeste-d-install-dry-run-issue-20/31-02-SUMMARY.md` à la fin.
</output>

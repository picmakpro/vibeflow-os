---
phase: 24-activation-et-mesure-du-moteur-gsd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
autonomous: true
requirements: [GSDA-02, GSDA-03]

must_haves:
  truths:
    - "Le slot `AGENT_SKILLS_PLANNER` est peuplé : la doctrine de dev du lab atteint `gsd-planner`."
    - "La portée réelle est écrite telle quelle — la doctrine atteint le PLAN, pas l'exécution — et le motif de l'inatteignabilité du slot `EXECUTOR` est nommé."
    - "`workflow.tdd_mode` reste au défaut amont `false` (clé non posée), avec son motif mesuré écrit."
    - "Les deux clés différées par la zone 2 restent ABSENTES de `.planning/config.json` après édition."
  artifacts:
    - .planning/config.json
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  key_links:
    - "`.planning/config.json` est surveillé par `check-gsd-config.sh` (advisory, SessionStart) et rechargé par le hook amont `FileChanged/config.json` : une clé inconnue y est signalée, jamais silencieuse."
    - "Ce plan est le PREMIER des deux à écrire `.planning/config.json` ; le plan 24-06 (vague 2) est le second et en dépend explicitement."
---

<objective>
Ouvrir le seul canal de transmission de doctrine dont l'injection soit prouvée atteignable, et
écrire honnêtement jusqu'où il porte.

Purpose: `agent_skills` est le canal officiel par lequel la doctrine d'un lab atteint les agents du
moteur — 17 slots, 30 workflows consommateurs — et le nôtre est vide. Mais le slot `EXECUTOR` n'est
pas atteignable sur notre chemin réel : `gsd-executor` est hors de l'allowlist `tools:` de
`vf-coder` depuis la Phase 23 (GSDC-05), et le repli documenté quand `Agent` est indisponible est
l'exécution inline séquentielle — un chemin sans prompt de dispatch, donc sans injection. Le verdict
tranche : slot `PLANNER` seul, sans `tdd_mode`, et la portée écrite telle quelle.

Output: la clé `agent_skills.gsd-planner` peuplée, et une section de `GSD-PIPELINE.md` qui dit ce
que le canal atteint, ce qu'il n'atteint pas, et pourquoi `tdd_mode` reste au défaut.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-ARBITRAGES.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-CONTEXT.md
@plugin/dev-orchestrator/references/GSD-PIPELINE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : peupler le slot `AGENT_SKILLS_PLANNER` dans `.planning/config.json`</name>
  <files>.planning/config.json</files>
  <read_first>
    - `.planning/config.json` en entier (48 lignes) — l'état exact avant édition : `agent_skills` vide, blocs `gates`/`safety` déjà supprimés, `_auto_chain_active: false`, `auto_advance: false`.
    - `~/.claude/gsd-core/bin/lib/init.cjs` lignes 1731-1815 — `buildAgentSkillsBlock` : les 17 slots, la clé de map (nom d'agent), et l'acceptation de la forme plugin-namespacée aux lignes 1765-1788. Vérifier sur pièce la forme attendue AVANT d'écrire, ne pas la déduire du cadrage.
    - `24-CONTEXT.md` fiches F-11, F-13 et F-15 — les 17 slots, l'inatteignabilité du slot EXECUTOR, et les candidats réels tous résolvables en forme simple.
    - `plugin/software-architecture/SKILL.md` et `plugin/audit-architecture/SKILL.md` — les deux skills injectés : vérifier qu'ils existent, qu'ils sont installés sous ce nom, et relever leur taille (le contenu injecté pèse dans le prompt de `gsd-planner`).
  </read_first>
  <action>
Remplacer la valeur vide de la clé `agent_skills` par une map à une seule entrée, celle de
`gsd-planner` — le seul agent dont l'injection soit prouvée atteignable (`plan-phase.md:74`,
injections `:769` et `:1247`).

Les deux skills à injecter, et eux seuls :
`software-architecture`, qui porte SOLID / SoC / Clean Architecture / Clean Code
(`references/solid-soc.md`) et DRY / KISS / YAGNI plus la carte TDD (`references/principles.md`) —
c'est la doctrine des Phases 7-8, celle qui n'atteignait jamais le moteur ; et
`audit-architecture`, qui porte la doctrine d'audit multi-couches (ADR-036).

Forme retenue : la forme simple préfixée `global:`, pas la forme plugin-namespacée. F-15 établit que
les deux candidats sont résolvables ainsi ; la forme namespacée ajouterait une dépendance au nom du
plugin sans rien acheter. Si la lecture d'`init.cjs` contredit cette forme à l'exécution, suivre ce
que le code dit, et le consigner au SUMMARY.

Ne poser aucune autre clé. Après édition, vérifier que le fichier ne contient toujours ni
`workflow.tdd_mode`, ni `hooks.community`, ni `context`, ni `context_profile`, ni
`workflow.inline_plan_threshold`, ni `graphify`, ni `profile-pipeline`. Ces absences sont des
décisions écrites (ADR-067, et ADR-068 à venir), pas des oublis.

> **Contrôle négatif INVERSÉ (correction du 2026-08-04, après exécution de ce plan).** Cette liste
> portait aussi `workflow.windows_enforce` et `hooks.workflow_guard`. Le dégel de la zone 2 est
> arrivé APRÈS la rédaction : le plan 24-02 les a **posées à `true`** (ADR-066, waive #3 joué). Le
> contrôle est inversé ici pour ne pas tromper à la relecture — ce plan a bien été exécuté contre
> l'état d'alors (les deux clés absentes), et c'est le `24-03-SUMMARY.md` qui fait foi sur ce qui a
> été constaté ce jour-là. Ce qui change est le contrôle **rejouable aujourd'hui**, pas l'histoire.

Le JSON doit rester valide et son indentation identique à celle du fichier (2 espaces).
  </action>
  <verify>
    <automated>jq -e '.agent_skills["gsd-planner"] | length == 2' .planning/config.json &amp;&amp; jq -e '.agent_skills | keys | length == 1' .planning/config.json &amp;&amp; jq -e '.workflow.tdd_mode == null and .workflow.windows_enforce == true and .workflow.inline_plan_threshold == null and .hooks.workflow_guard == true and .hooks.community == null and .context == null and .context_profile == null and .graphify == null and ."profile-pipeline" == null' .planning/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.agent_skills["gsd-planner"] | join(",")' .planning/config.json` rend une liste de 2 entrées, contenant `software-architecture` et `audit-architecture`.
    - `jq -e '.agent_skills | keys | length == 1' .planning/config.json` réussit : `gsd-planner` est la seule clé de la map, `gsd-executor` n'y figure pas.
    - `jq empty .planning/config.json` réussit (JSON valide).
    - Contrôle zone 2, **INVERSÉ le 2026-08-04 (le dégel est arrivé après la rédaction)** : `jq -e '.workflow.windows_enforce == true' .planning/config.json` et `jq -e '.hooks.workflow_guard == true' .planning/config.json` **réussissent** tous les deux — les deux clés ont été posées par 24-02 sous ADR-066. Rédaction d'origine : « échouent tous les deux (clés absentes) ».
    - Contrôle négatif zone 1 : `jq -e '.workflow.tdd_mode' .planning/config.json` échoue (clé absente).
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort 0 ou 3, et sa sortie ne cite pas `agent_skills` parmi les clés inconnues.
  </acceptance_criteria>
  <done>Le slot PLANNER est peuplé de deux skills résolvables, la map ne porte que lui, et les huit clés refusées ou différées restent absentes.</done>
  <reversibility rating="reversible">Une clé de configuration ; le retrait est un jq inverse et l'effet est borné au prompt de `gsd-planner`.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : écrire la portée réelle du canal et le refus de `tdd_mode` dans `GSD-PIPELINE.md`</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md</files>
  <read_first>
    - `plugin/dev-orchestrator/references/GSD-PIPELINE.md` en entier (223 lignes) — c'est le foyer de la doctrine de flags et de la distinction toggle de capability / flag de ligne de commande, posée en Phase 23 (GSDC-03). Une doctrine de canal de capability va là, pas ailleurs (ADR-057 : une capacité, une seule voix).
    - `24-CONTEXT.md` fiches F-13, F-14, F-17, F-18, F-19 — l'inatteignabilité du slot EXECUTOR, l'incapacité structurelle du digest à porter la doctrine, et les trois faits qui vident `tdd_mode` de son enjeu.
    - `24-ARBITRAGES.md` § Zone 1, verdict — la contrainte de rédaction imposée sur la présentation d'A2 côté exécuteur.
    - `plugin/dev-orchestrator/references/mission-contracts.md` lignes 51 et 55-63 — le plafond du digest et le bornage des conventions, à citer avec leurs numéros de ligne.
  </read_first>
  <action>
Ajouter à `GSD-PIPELINE.md` une section sur le canal `agent_skills`, écrite en trois temps.

Premier temps, ce que le canal atteint. `agent_skills` est le canal officiel du moteur :
`buildAgentSkillsBlock` injecte des skills du lab dans le prompt des agents `gsd-*` via 17 slots,
consommés par 30 workflows (mesure du 2026-08-04 contre `@opengsd/gsd-core` 1.9.1). Nous en peuplons
un seul, celui de `gsd-planner`, avec `software-architecture` et `audit-architecture`. La conséquence
s'écrit telle quelle : la doctrine de dev du lab atteint désormais le plan, elle n'atteint pas
l'exécution.

Deuxième temps, pourquoi le slot EXECUTOR n'est pas atteignable, et l'interdiction qui en découle.
L'injection du slot exécuteur ne vit que dans le prompt de dispatch d'`execute-phase.md` (chargement
`:86`, injection `:715`) ; `execute-plan.md` n'en porte aucune. Or `gsd-executor` et `gsd-planner`
sont sortis de l'allowlist `tools:` de `vf-coder` en Phase 23 (GSDC-05, voie unique), et le repli
documenté quand l'outil de dispatch est indisponible est l'exécution inline séquentielle
(`execute-phase.md:28-31`) — un chemin sans prompt de dispatch, donc sans injection. Peupler le slot
exécuteur serait un vert-à-vide sur notre chemin réel. Écrire l'interdiction : ce canal ne doit plus
jamais être présenté comme résolu du côté de l'exécuteur, et toute affirmation inverse doit d'abord
produire la preuve que le prompt de dispatch d'`execute-phase` est bien emprunté sur ce chemin.

Troisième temps, pourquoi le digest de mission ne remplace pas ce canal. Le digest borne les
conventions à deux ou trois lignes du `CLAUDE.md` projet (`mission-contracts.md:62`) sous un plafond
de trente lignes (`:51`), et notre `CLAUDE.md` ne contient ni SOLID, ni DRY, ni KISS, ni YAGNI, ni
Clean Archi, ni TDD. Il ne peut structurellement pas porter la doctrine : c'est un fait de dimension,
pas un oubli de rédaction.

Ajouter ensuite, sous la doctrine de flags existante, une sous-section courte sur le refus de
`workflow.tdd_mode`, avec son motif mesuré. Elle porte quatre faits et une décision. Le fichier de
référence TDD du moteur, 330 lignes, est déjà injecté sans condition dans le prompt de l'exécuteur
(`execute-phase.md:693`) : le toggle n'ajoute pas la doctrine TDD, elle y est déjà. Ce qu'il ajoute
réellement se réduit à deux choses : le planner pose un type de tâche dédié sur les tâches éligibles,
et un gate de fin d'exécution non bloquant s'installe (`blocking: false`, `onError: skip`).
L'heuristique d'éligibilité amont est par type de tâche, business logic d'un côté, UI, config, glue
et CRUD de l'autre ; sur ce dépôt, qui est du bash et du markdown, aucune des sept catégories amont
ne correspond, si bien qu'elle classerait la quasi-totalité de nos tâches du mauvais côté, alors que
notre pratique réelle écrit le test rouge d'abord sur un critère mesurable
(`plugin/software-architecture/references/principles.md:61-63`). Décision : la clé n'est pas posée,
le défaut amont s'applique.

Respecter le plafond de densité ADR-029 : relever le compte de lignes du fichier après édition avec
`awk 'END{print NR}'` (jamais `wc -l`, proxifié sur ce runtime) et le consigner au SUMMARY.
  </action>
  <verify>
    <automated>awk '/agent_skills/{a=1} /atteint le plan/{b=1} /jamais.*exécuteur/{c=1} /tdd_mode/{d=1} /onError: skip/{e=1} END{exit !(a&&b&&c&&d&&e)}' plugin/dev-orchestrator/references/GSD-PIPELINE.md &amp;&amp; test "$(awk 'END{print NR}' plugin/dev-orchestrator/references/GSD-PIPELINE.md)" -le 500 &amp;&amp; bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `GSD-PIPELINE.md` contient les littéraux `agent_skills`, `gsd-planner`, `execute-phase.md:715`, `execute-phase.md:28-31`, `mission-contracts.md:62`, `tdd_mode`, `onError: skip`, `execute-phase.md:693`.
    - Le fichier contient la phrase qui dit que la doctrine atteint le plan et pas l'exécution, et l'interdiction de présenter le canal comme résolu du côté de l'exécuteur.
    - Le fichier contient les quatre motifs mesurés du refus de `tdd_mode` : injection déjà inconditionnelle, gate non bloquant, heuristique par type de tâche, inadéquation aux sept catégories amont sur un dépôt bash + markdown.
    - `awk 'END{print NR}' plugin/dev-orchestrator/references/GSD-PIPELINE.md` rend une valeur ≤ 500 (ADR-029).
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort 0 (non-régression ; ce plan ne modifie pas la suite).
    - Aucune ligne préexistante de `GSD-PIPELINE.md` n'a disparu : `comm -23` entre l'ensemble trié des lignes avant et après rend le vide.
  </acceptance_criteria>
  <done>La portée du canal, son inatteignabilité côté exécuteur et le refus motivé de `tdd_mode` sont écrits dans le seul fichier qui porte déjà la doctrine de flags.</done>
  <reversibility rating="reversible">Ajout documentaire additif dans un fichier de référence chargé à la demande.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/config.json` → moteur GSD | Le config est lu par le moteur et par le hook amont de rechargement ; une clé mal formée change le comportement de toute la chaîne. |
| skills du lab → prompt de `gsd-planner` | Le contenu de `software-architecture` et `audit-architecture` traverse vers le prompt d'un agent du moteur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03-01 | Tampering | `.planning/config.json` | high | mitigate | Contrôles négatifs explicites : les huit clés refusées ou différées doivent rester absentes après édition, vérifiés par `jq -e` qui échoue. |
| T-24-03-02 | Elevation of Privilege | injection de skill dans un agent du moteur | medium | mitigate | Un seul slot peuplé, deux skills du dépôt lui-même (aucun skill tiers), forme de résolution vérifiée contre `init.cjs` avant écriture. |
| T-24-03-03 | Denial of Service | prompt de `gsd-planner` saturé | medium | mitigate | La taille des deux SKILL.md est relevée avant injection ; ADR-029 plafonne les skills à 500 lignes, ce qui borne le volume injecté. |
| T-24-03-SC | Tampering | installs npm/pip/cargo | high | accept | Ce plan n'exécute aucune installation de paquet. |
</threat_model>

<verification>
- `jq empty .planning/config.json` réussit et la map `agent_skills` porte exactement une clé.
- Les huit clés refusées ou différées sont absentes (`jq -e` échoue sur chacune).
- `check-gsd-config.sh --path .` sort 0 ou 3 sans citer `agent_skills` en clé inconnue.
- `test-dev-orchestrator.sh` reste vert sans avoir été modifié par ce plan.
</verification>

<success_criteria>
Le canal `agent_skills` est ouvert sur le seul slot atteignable, sa portée réelle est écrite sans
euphémisme, et le refus de `tdd_mode` est motivé par quatre faits mesurés — sans qu'aucune clé
différée n'ait été posée au passage.
</success_criteria>

## Artifacts this phase produces

| Symbole | Nature | Emplacement |
|---|---|---|
| `agent_skills.gsd-planner` | clé de configuration (map à 1 entrée, 2 skills) | `.planning/config.json` |
| section « canal `agent_skills` » | doctrine (portée + interdiction) | `plugin/dev-orchestrator/references/GSD-PIPELINE.md` |
| sous-section « refus de `workflow.tdd_mode` » | doctrine (motif mesuré) | `plugin/dev-orchestrator/references/GSD-PIPELINE.md` |

<output>
Créer `.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-03-SUMMARY.md` en fin de plan.
</output>

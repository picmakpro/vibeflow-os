---
phase: 26-manuel-utilisateur-vibeflow-manual
plan: 05
type: execute
wave: 5
depends_on: ["26-04"]
files_modified:
  - manual/toc.yml
  - manual/fr/README.md
  - manual/en/README.md
  - manual/fr/04-cycle-de-dev/
  - manual/en/04-cycle-de-dev/
autonomous: true
requirements: []   # TBD — ROADMAP §Phase 26 porte `Requirements: TBD`.
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lecteur sait dérouler un cycle complet — cadrer, planifier, exécuter, livrer — en langage naturel, sans connaître le nom du moteur qui tourne dessous."
    - "Un lecteur sait à quels moments il sera sollicité, ce qu'il doit relire avant de merger, et comment interrompre puis reprendre."
    - "Un lecteur sait ce que le mode autonome fait et ne fait pas, et ce qui l'arrête."
    - "Aucun fichier sous manual/ n'apparaît dans git status ni dans aucun commit (D-14)."
  artifacts:
    - "manual/{fr,en}/04-cycle-de-dev/le-cycle-en-bref.md"
    - "manual/{fr,en}/04-cycle-de-dev/cadrer-une-idee.md"
    - "manual/{fr,en}/04-cycle-de-dev/planifier.md"
    - "manual/{fr,en}/04-cycle-de-dev/executer.md"
    - "manual/{fr,en}/04-cycle-de-dev/livrer-et-relire.md"
    - "manual/{fr,en}/04-cycle-de-dev/mode-autonome.md"
  key_links:
    - "le-cycle-en-bref.md → les 5 pages suivantes : c'est le how-to central, celui qu'on relit (D-05 §4)."
    - "livrer-et-relire.md → ADR-060 (la revue est un étage de premier rang) et ADR-045 (recherche doc avant debug) : deux comportements que l'utilisateur voit."
    - "mode-autonome.md → 02-concepts/gates-et-validation-humaine.md : l'autonomie n'annule jamais ADR-031."
---

<objective>
Livrer le thème `04-cycle-de-dev` complet en français et en anglais : le how-to central du produit.

Purpose: c'est la page qu'on relit (D-05 §4). La matière existe mais elle est **dispersée dans une
quinzaine d'ADR écrites pour les agents, jamais pour l'humain** (inventaire §13). Ce thème la
réordonne à hauteur d'utilisateur : ce qu'il tape, ce qu'il voit, quand on lui demande quelque chose,
et ce qu'il relit avant de merger. C'est aussi là que se joue la valeur cœur du produit — dire « aide-
moi à dev » déclenche le pipeline complet **sans jamais avoir à connaître le moteur**.

Output: 12 fichiers de pages (6 pages × 2 langues), `toc.yml` enrichi, aucun commit.

**Contrainte machine de la phase (D-14) — vaut pour les 3 tâches de ce plan :** `manual/` est exclu
de git par `.git/info/exclude:7`. `git add` sur ce chemin échoue. **Aucune tâche de ce plan ne
commite. Write/Edit et exécution locale seulement. Ne jamais `git add` un chemin sous `manual/`.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-04-SUMMARY.md
@plugin/dev-orchestrator/README.md
</context>

---

## Invariants de rédaction

Identiques aux 8 invariants énoncés au plan `26-02` (bilingue par incrément · `toc.yml` ne promet
rien · l'EN se produit, il ne se traduit pas (D-12) · zéro version en dur (D-11) · 100-200 lignes,
bascule à 300 ou 3 H2 (D-04) · aucun jargon non défini · blocs générés intouchés (D-03) · **aucun
commit** (D-14)). Ils s'appliquent sans être répétés.

**Invariant supplémentaire propre à ce thème :** écrire du **point de vue de l'utilisateur**, jamais
du point de vue de l'agent. Une ADR décrit une mécanique interne ; la page du manuel décrit ce que
l'humain voit, tape et décide. Une phrase qui commence par « l'orchestrateur dispatche… » est à
réécrire en « vous demandez…, et il se passe… ». Les noms d'agents internes ne sont cités que
lorsqu'ils apparaissent réellement à l'écran.

---

## Artifacts this plan produces

- `manual/fr/04-cycle-de-dev/` et `manual/en/04-cycle-de-dev/`, 6 pages chacun (H-1) :
  `le-cycle-en-bref.md` · `cadrer-une-idee.md` · `planifier.md` · `executer.md` ·
  `livrer-et-relire.md` · `mode-autonome.md`
- `manual/toc.yml` : thème `04-cycle-de-dev` ouvert, 6 entrées `pages:` ajoutées
- `manual/fr/README.md` et `manual/en/README.md` : parcours guidé « je développe » ajouté

---

<tasks>

<task type="auto">
  <name>Task 1: le-cycle-en-bref + cadrer-une-idee (FR et EN)</name>
  <files>manual/fr/04-cycle-de-dev/le-cycle-en-bref.md, manual/en/04-cycle-de-dev/le-cycle-en-bref.md, manual/fr/04-cycle-de-dev/cadrer-une-idee.md, manual/en/04-cycle-de-dev/cadrer-une-idee.md, manual/toc.yml</files>
  <read_first>
    - `README.md` §« The dev cycle — spec-driven » (~37-58) — le mermaid du cycle et ses trois puces ;
      matière de départ, à développer, jamais à recopier telle quelle.
    - `plugin/dev-orchestrator/README.md` — ce que le module expose réellement à l'utilisateur.
    - `plugin/dev-orchestrator/skills/vf-dev/SKILL.md` — les formulations en langage naturel qui
      déclenchent le cycle (c'est la valeur cœur : l'utilisateur n'a pas à connaître le moteur).
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §13 —
      le constat que le repo est riche en concept et vide en parcours guidé.
  </read_first>
  <action>
    **`04-cycle-de-dev/le-cycle-en-bref.md`** (FR + EN) — la vue d'ensemble : les quatre temps
    (cadrer, planifier, exécuter, livrer), ce que chacun produit, et surtout **ce que l'utilisateur
    dit** pour passer de l'un à l'autre. Insister sur le point qui différencie le produit : on parle
    en langage naturel, les commandes existent mais ne sont pas la porte d'entrée. Une carte mermaid
    du cycle est bienvenue — **décorative** (D-06), immédiatement suivie de la liste de liens vers les
    quatre pages qui détaillent chaque temps. Terminer par « combien de temps ça prend » et « quand ce
    cycle est disproportionné » (une correction d'une ligne n'a pas besoin d'un cycle complet) — dire
    aussi ce qu'on fait dans ce cas.

    **`04-cycle-de-dev/cadrer-une-idee.md`** (FR + EN) — le premier temps. Couvre : à quoi sert le
    cadrage et pourquoi sauter cette étape coûte plus cher qu'elle ; comment se déroule l'échange
    (des questions posées, des zones grises tranchées, des idées explicitement reportées) ; ce qui est
    produit et où ça atterrit ; ce que l'utilisateur doit apporter et ce qu'il peut laisser au
    système. Nommer la distinction entre une décision **verrouillée** et une idée **différée** — c'est
    le mécanisme qui garantit qu'une idée écartée ne réapparaît pas plus tard sans qu'on l'ait
    décidé. Donner un exemple court d'échange de cadrage.

    Terminer par : ouvrir le thème `04-cycle-de-dev` dans le bloc `themes:` de `toc.yml`, inscrire les
    2 entrées `pages:`, relancer `build-nav.sh`, relancer `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 04-cycle-de-dev/le-cycle-en-bref.md 04-cycle-de-dev/cadrer-une-idee.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; t=$(grep -c '^[[:space:]]*- path: 04-cycle-de-dev/' manual/toc.yml); [ "$t" -ge 2 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `le-cycle-en-bref.md` nomme les quatre temps, ce que chacun produit, et donne les formulations
      en langage naturel qui déclenchent le cycle.
    - Toute carte mermaid est décorative et suivie immédiatement d'une liste de liens relatifs qui
      résolvent (D-06, C3 du gate).
    - `le-cycle-en-bref.md` dit explicitement quand le cycle complet est disproportionné et ce qu'on
      fait à la place.
    - `cadrer-une-idee.md` distingue décision verrouillée et idée différée, et donne un exemple
      d'échange.
    - Aucune page n'est écrite du point de vue de l'agent (pas de « l'orchestrateur dispatche… » sans
      traduction en geste utilisateur).
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur a la carte du cycle et sait ouvrir un chantier par une conversation.</done>
</task>

<task type="auto">
  <name>Task 2: planifier + executer (FR et EN)</name>
  <files>manual/fr/04-cycle-de-dev/planifier.md, manual/en/04-cycle-de-dev/planifier.md, manual/fr/04-cycle-de-dev/executer.md, manual/en/04-cycle-de-dev/executer.md, manual/toc.yml</files>
  <read_first>
    - `plugin/reference/content/methodology/patterns/10-plan-review-adversarial.md` — la revue de plan,
      visible par l'utilisateur.
    - `docs/ADR.md` — **ADR-045** (recherche doc avant debug : un comportement que l'utilisateur voit
      à l'écran) et ADR-035 (architecture AI-Safe, gates de taille).
    - `plugin/software-architecture/README.md` — la doctrine de code appliquée par des gates, dans ce
      qu'elle a d'observable (fichiers bornés, refus sur dépassement).
    - `plugin/dev-orchestrator/README.md` §usage — les gestes réellement exposés.
  </read_first>
  <action>
    **`04-cycle-de-dev/planifier.md`** (FR + EN) — le deuxième temps. Couvre : ce qu'est un plan de
    phase et pourquoi il est découpé en morceaux plutôt qu'écrit d'un bloc ; ce que l'utilisateur y
    lit et ce qu'il doit y vérifier avant de lancer l'exécution (c'est le moment le moins cher pour
    corriger une trajectoire) ; ce qu'est une revue de plan et ce qu'elle change ; comment demander
    une modification du plan. Dire aussi ce qui se passe quand un plan s'avère trop gros — il est
    scindé, jamais rétréci en périmètre.

    **`04-cycle-de-dev/executer.md`** (FR + EN) — le troisième temps. Couvre : ce que l'exécution
    fait sans rien demander et ce qui, au contraire, l'arrête pour demander ; le rythme des commits ;
    comment lire ce qui défile ; et deux comportements visibles qui surprennent si on ne les a pas
    lus — la **recherche de documentation avant de déboguer** (ADR-045) et le **refus sur dépassement
    de taille** imposé par la doctrine de code. Dire ce qu'on fait quand une tâche échoue, et pourquoi
    l'échec est rendu visible plutôt qu'absorbé. Renvoyer à `06-reference/depannage.md` pour les
    pannes — sans lien si ce plan n'est pas encore exécuté (invariant 2).

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 04-cycle-de-dev/planifier.md 04-cycle-de-dev/executer.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; t=$(grep -c '^[[:space:]]*- path: 04-cycle-de-dev/' manual/toc.yml); [ "$t" -ge 4 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `planifier.md` dit ce que l'utilisateur doit vérifier dans un plan **avant** de lancer
      l'exécution, et comment demander une modification.
    - `planifier.md` énonce qu'un plan trop gros est **scindé**, jamais réduit en périmètre.
    - `executer.md` décrit les deux comportements visibles cités (recherche doc avant debug, refus sur
      dépassement de taille) en termes de ce que voit l'utilisateur.
    - `executer.md` dit ce qui arrête l'exécution pour demander une décision.
    - Aucun lien relatif ne pointe vers une page non écrite (C3 du gate passe).
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur sait relire un plan avant qu'il ne coûte cher, et comprend ce qu'il voit défiler pendant l'exécution.</done>
</task>

<task type="auto">
  <name>Task 3: livrer-et-relire + mode-autonome (FR et EN) + parcours « je développe »</name>
  <files>manual/fr/04-cycle-de-dev/livrer-et-relire.md, manual/en/04-cycle-de-dev/livrer-et-relire.md, manual/fr/04-cycle-de-dev/mode-autonome.md, manual/en/04-cycle-de-dev/mode-autonome.md, manual/fr/README.md, manual/en/README.md, manual/toc.yml</files>
  <read_first>
    - `docs/ADR.md` — **ADR-060** (la revue est un étage de premier rang piloté par le manager) et
      **ADR-059** (une mission travaille sur sa propre branche, PR laissée ouverte : impact direct sur
      le workflow git de l'utilisateur).
    - `plugin/dev-orchestrator/skills/vf-auto/SKILL.md` — ce que le mode autonome fait et ce qui
      l'arrête.
    - `plugin/reference/content/methodology/patterns/11-halt-conditions.md` — pourquoi un agent
      s'arrête.
    - `manual/fr/02-concepts/gates-et-validation-humaine.md` (livré au plan 26-03) — pour y renvoyer
      plutôt que redire ADR-031.
  </read_first>
  <action>
    **`04-cycle-de-dev/livrer-et-relire.md`** (FR + EN) — le quatrième temps, et le plus négligé.
    Couvre : ce qu'est l'étage de revue et pourquoi il est de premier rang, pas une option (ADR-060) ;
    **ce que l'utilisateur doit relire avant de merger** — c'est le manque M-8, aujourd'hui à zéro
    dans le repo, et c'est la partie la plus utile de la page : une liste courte et concrète de ce qui
    mérite un œil humain ; où atterrissent les artefacts ; comment la documentation est mise à jour et
    ce que ça implique pour lui. Mentionner qu'une mission longue travaille sur sa propre branche et
    laisse sa PR ouverte (ADR-059), en renvoyant à `05-equipe-agents/branches-et-worktrees.md` pour le
    détail — sans lien si ce plan n'est pas encore exécuté (invariant 2).

    **`04-cycle-de-dev/mode-autonome.md`** (FR + EN) — déléguer l'enchaînement complet. Couvre :
    quand ça a du sens et quand c'est une mauvaise idée ; ce que le mode autonome enchaîne
    réellement ; **ce qui l'arrête** (une halt condition, une décision à prendre, une suppression) ;
    ce qu'on retrouve au réveil et comment le lire ; comment reprendre la main en cours de route.
    Poser sans ambiguïté que l'autonomie **n'annule jamais** l'engagement de validation humaine — lien
    relatif vers `02-concepts/gates-et-validation-humaine.md`.

    **Ajouter aux deux README de langue** le parcours guidé « je développe », pointant vers
    `04-cycle-de-dev/le-cycle-en-bref.md`.

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 04-cycle-de-dev/livrer-et-relire.md 04-cycle-de-dev/mode-autonome.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do grep -q '02-concepts/gates-et-validation-humaine.md' "manual/$l/04-cycle-de-dev/mode-autonome.md"; grep -q '04-cycle-de-dev/' "manual/$l/README.md"; done; t=$(grep -c '^[[:space:]]*- path: 04-cycle-de-dev/' manual/toc.yml); [ "$t" -ge 6 ]; [ "$t" -le 9 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `livrer-et-relire.md` porte une **liste concrète** de ce que l'utilisateur relit avant de merger
      (M-8) — pas une phrase générale.
    - `mode-autonome.md` énumère ce qui arrête l'enchaînement et porte un lien relatif résolvant vers
      `02-concepts/gates-et-validation-humaine.md`.
    - Les deux README de langue portent le parcours « je développe », dont le lien résout.
    - Le thème `04-cycle-de-dev` compte entre 6 et 9 pages par langue (D-04).
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le how-to central est complet : le lecteur peut mener un chantier de bout en bout et sait ce qu'il doit regarder avant de merger.</done>
</task>

</tasks>

<threat_model>
Sans objet pour ce plan : markdown seul, aucun code exécutable, aucune dépendance, aucun install de
paquet. La seule menace de la phase (fuite de `manual/` vers un artefact versionné) est instruite en
`T-26-04` du plan 26-01 et mitigée ici par l'assertion `git status --porcelain -- manual` vide dans
les trois tâches (D-14).
</threat_model>

<verification>
1. `bash manual/.tools/check-manual.sh` sort en **0**.
2. Les arbres `manual/fr` et `manual/en` sont isomorphes (D-01).
3. Aucune page du thème ne contient de motif `vX.Y.Z` : C5 du gate passe (D-11).
4. Aucun lien relatif mort dans tout le manuel : C3 du gate passe.
5. `git status --porcelain -- manual` sort **vide**.
6. `git status --porcelain -- plugin docs README.md README.fr.md INSTALL.md scripts .github` sort
   **vide**.
</verification>

<success_criteria>
- Les 6 (à 9) pages de `04-cycle-de-dev` existent en FR **et** en EN.
- M-8 est comblé sur son volet « ce que l'humain relit et quand il est sollicité ».
- Le gate est vert. Git n'a rien vu.
</success_criteria>

<output>
Create `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-05-SUMMARY.md` when done.
Y consigner : le nombre de lignes de chaque page, le verdict de `check-manual.sh`, et la confirmation
explicite que `git status --porcelain -- manual` est resté vide de bout en bout.
</output>

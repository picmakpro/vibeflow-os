---
phase: VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/hooks/post-merge
  - plugin/conductor/scripts/check-divergence.sh
  - plugin/conductor/scripts/tests/test-check-divergence.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [PART-04]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A post-merge git hook exists and, when armed via `git config core.hooksPath scripts/hooks`, reports a split-brain divergence loudly instead of the current silent `git merge-tree`/`git merge` exit 0 (research §8)."
    - "check-divergence.sh distinguishes divergent (exit 1), clean-and-checked (exit 0), non-partitioned/nothing-to-check (exit 3), and non-vérifiable (exit 2) states, never conflating them — the hook alerts on exit 1 only, never on 0/3/2/64."
    - "The three split-brain variants named in research §8 (orphan, rootonly, roadmap-conflict) are each accounted for by the signature S2+S4+S5, and mutation-red proof (D-13) shows the detection is real, not a vacuous green."
    - "check-divergence.sh is wired into the `gates` CI job (arbitrage Samuel, 2026-09-10, C17 revisited): the `post-merge` hook alone is silent-by-construction on GitHub-merged PRs, the dominant merge path of this repo — ADR-059's exact 'sûr mais inerte' failure mode. The CI step proves REAL triggering by mutation (a diverged fixture rendering exit 1), never mere presence of the gate."
  artifacts:
    - scripts/hooks/post-merge
    - plugin/conductor/scripts/check-divergence.sh
    - plugin/conductor/scripts/tests/test-check-divergence.sh
    - .github/workflows/ci.yml
  key_links:
    - "post-merge hook -> check-divergence.sh --path <root> -> exit-code-gated alert (only exit 1 prints the divergence line and exits non-zero)"
    - "check-divergence.sh -> plugin/planning-core/scripts/workstream-policy.sh (sourced for compartment resolution, never recopied — 4th-copy lesson already paid for by the three existing gates)"
    - "check-divergence.sh -> .github/workflows/ci.yml `gates` job (the trigger that actually sees GitHub PR merges, since `post-merge` structurally cannot)"
---

<objective>
Build and prove, end to end, the divergence-detection net that Success Criterion 4 of Phase 39
requires: a `post-merge` git hook, opt-in exactly like `scripts/hooks/pre-push`, that calls a new
`check-divergence.sh` gate implementing the S2+S4+S5 signature measured in
`.planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md` §8-9 (D-11, D-12). The
gate must be proven, by real execution and by mutation, to catch the split-brain that today passes
`git merge-tree`/`git merge` silently (exit 0, Git signals nothing) — and to stay silent on every
ordinary, non-diverged merge, including this repo's own current (non-partitioned) state.

Purpose: today's three existing workstream gates (`check-workstream-pointer.sh`,
`check-state-integrity.sh`, `workstream-policy.sh`) render "conforme" on a measured split-brain
tree (research §9) — there is zero net. This plan is that net.
Output: `scripts/hooks/post-merge`, `plugin/conductor/scripts/check-divergence.sh`,
`plugin/conductor/scripts/tests/test-check-divergence.sh`, and the `.github/workflows/ci.yml`
wiring that makes the gate fire on the dominant GitHub-PR-merge path too (arbitrage Samuel,
2026-09-10 — the local hook alone would be ADR-059's "sûr mais inerte" a second time).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
@.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-PATTERNS.md
@.planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md
@scripts/hooks/pre-push
@plugin/conductor/scripts/check-workstream-pointer.sh
@plugin/conductor/scripts/check-state-integrity.sh
@plugin/planning-core/scripts/workstream-policy.sh
@plugin/conductor/scripts/tests/test-check-workstream-pointer.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: check-divergence.sh (S2+S4+S5) wired into post-merge, proven on one real git merge</name>
  <read_first>
    scripts/hooks/pre-push (21 lines, the exact opt-in activation model to decline for post-merge);
    plugin/conductor/scripts/check-workstream-pointer.sh:1-124 (docstring/exit-code-table shape,
    parsing loop, git_safe wrapper, workstream-policy.sh sourcing block at :154-162);
    plugin/conductor/scripts/check-state-integrity.sh:75-105,205-222 (frontmatter_block/extract_int/
    extract_str helpers to reuse verbatim for STATE.md counters);
    plugin/planning-core/scripts/workstream-policy.sh (vf_ws_resolve/vf_ws_dir_resolve contract —
    source it, never recopy vf_ws_* per its own header's "4th-copy lesson");
    .planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md §8-9 (the three
    variants table, the S2/S4/S5 signature spec, and why S1/S3 were rejected — do not reintroduce
    them);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-PATTERNS.md
    (proposed post-merge/check-divergence.sh skeleton — its naive `||` exit-code wiring is
    corrected below, do not copy that part literally).
  </read_first>
  <files>scripts/hooks/post-merge, plugin/conductor/scripts/check-divergence.sh</files>
  <action>
    Create `plugin/conductor/scripts/check-divergence.sh` on the docstring/parsing/`git_safe`/
    exit-code-table shape of `check-state-integrity.sh:1-105`, sourcing `workstream-policy.sh`
    exactly as `check-workstream-pointer.sh:154-162` does (never recopy `vf_ws_resolve`/
    `vf_ws_dir_resolve`). Usage: `check-divergence.sh [--path <dir>]`; default `--path .`.

    Compartments inspected: the root `.planning/` (only if `.planning/phases/` exists there) plus
    every `.planning/workstreams/<name>/` directory. If `.planning/workstreams/` is absent entirely,
    that is SILENCE — exit `3`, stdout and stderr both empty, exactly `check-workstream-pointer.sh`'s
    state 0 contract, because that is this repo's own nominal state today and every lab's nominal
    state to date.

    For each compartment found, list its `phases/*` entries and extract the numeric prefix with the
    anchor `^([A-Za-z]+-)?([0-9]+(\.[0-9]+)?)-`, keeping only the numeric group (an optional
    uppercase-letter-plus-dash prefix, then digits). **Measured 2026-09-10, correcting a false
    claim this task carried before**: every LIVE phase directory in this repo is `VFDO-NN-slug`
    (e.g. `VFDO-39-`) — a bare `^([0-9]+(\.[0-9]+)?)-` anchor matches ZERO of them (verified by
    running it against `VFDO-39-workstreams-partition...`: empty). The bare `NN-slug` shape only
    survives in the archived `.planning/milestones/*-phases/` trees (e.g. `05-packaging-plugin`);
    `NN.N-slug` (e.g. `02.1-`) does not exist anywhere in this repo — do not claim otherwise. The
    anchor above extracts `39` from the first form and `05` from the second, proven by direct
    execution against both real shapes before this plan was corrected.

    **Numeric comparison discipline, added 2026-09-10 (D1, second correction pass) — this is a
    BLOCKING correctness rule, not a style note.** The anchor above extracts a numeric prefix as
    TEXT (`"01"`, `"18"`, `"39"`) and the extraction happens on BOTH sides of every comparison this
    script performs: directory names (conventionally zero-padded, `01-`, `18-`) and `### Phase N`
    ROADMAP headers (this repo's own real `.planning/ROADMAP.md` writes them UNPADDED — `### Phase
    18`, `### Phase 39`, never `### Phase 018`). Comparing these two extracted strings with `=` or
    set/grep membership over raw text is **measured to false-positive on this plan's own prescribed
    nominal fixture** (Task 2's `01-`/`02-`/`03-` directories against unpadded `### Phase 1`/`### Phase
    2`/`### Phase 3` headers: `"01" != "1"` as strings, even though they name the same phase) — and
    on this repo's real ROADMAP.md itself, on every single phase number in it, the day this gate is
    ever pointed at the root. Every comparison in S2 (duplicate detection within one compartment),
    S4(a) (directory-vs-header set membership), and S5 (leaked-number lookup against the root
    ROADMAP) MUST normalize each extracted number to its canonical decimal form BEFORE comparing or
    inserting into any seen-set — never compare the raw extracted text. Normalize with bash's
    explicit base-10 arithmetic context, `nn=$((10#$int_part))` (splitting off any `.N` decimal
    suffix first and re-appending it unnormalized, since no live phase in this repo uses one) —
    **never a bare `$((int_part))`**: bash treats a leading `0` as an octal prefix, so `$((08))` and
    `$((09))` abort with `bad math expression`/`value too great for base` for the entirely ordinary
    phase numbers 8 and 9 — measured on this machine's own bash (3.2.57). Two numbers compare equal
    once normalized (`01` → `1`, `18` → `18`) even when their padding differs.

    - S2 (primary signal, fires on all three research-§8 variants): within one compartment, two or
      more phase directories sharing the same NORMALIZED numeric prefix is a divergence. Name the
      duplicated number (in its normalized form) and both directory names in the exit-1 message.
    - S4 (cardinality, corrected 2026-09-10 — the raw-count form below was measured to
      false-positive permanently on this repo's own compartments and is REJECTED, not merely
      adjusted): only for a compartment whose own `ROADMAP.md` exists — a freshly created,
      never-migrated workstream legitimately has no `ROADMAP.md` (research §5); do not fabricate a
      mismatch for that case, emit a stderr diag `S4 non applicable — pas de ROADMAP.md dans ce
      compartiment` and skip S4 for it (S2 above still ran regardless of this). Where `ROADMAP.md`
      exists, run TWO set/direction-based checks — never a raw `#dossiers != #entêtes` cardinality
      compare, which was measured to fire on this repo's own real numbers (12 root `### Phase N`
      headers vs its `.planning/phases/` directory count — itself a moving, two-valued fact,
      measured 2026-09-10: **9** directories tracked in `git HEAD`, **10** on disk, because
      `VFDO-36-cockpit-...` exists in the working tree but is not yet committed; this gate always
      resolves directory listings from the FILESYSTEM (a glob over `phases/*/`), never from `git
      ls-tree`, so it is the **10** figure that is live here, not 9 — either way, several
      archived-or-not-yet-started headers legitimately have no directory and vice versa for a
      just-created WIP one, that is normal, not a divergence, which is exactly why raw cardinality
      is rejected below in favor of set membership):
        (a) collect the SET of NORMALIZED numeric prefixes from `phases/*` directory names, and the
            SET of NORMALIZED numbers from `^### Phase [0-9]` headers in that compartment's own
            `ROADMAP.md`; a directory number that is **absent from the header set entirely** (after
            normalization) is an orphan — exit-1, naming it in its normalized form. (A header with
            no matching directory is NOT flagged: legitimately archived or not-yet-started phases
            always look like this, on this repo's own measured state.)
        (b) read that compartment's `STATE.md` frontmatter (the `frontmatter_block`/`extract_int`
            helpers from `check-state-integrity.sh:206-222`, copied not reinvented, using the
            indented-field regex they already use, `^[[:space:]]+completed_phases:[[:space:]]*[0-9]+`)
            for `completed_phases` under the `progress:` block (research §8's actual field name —
            **correction 2026-09-10 (E3.1)**: `total_phases` DOES exist in this repo's own schema
            (`.planning/STATE.md:37`, `total_phases: 10`) — the false claim this task carried before
            was that the field doesn't exist at all; what is actually true, and the reason this
            algorithm uses `completed_phases` instead, is narrower: `check-state-integrity.sh` never
            EXTRACTS `total_phases` — its own `extract_int`/`frontmatter_block` helper calls only
            ever pull `current_phase`, `completed_phases`, `completed_plans`, `total_plans` (verified
            by reading `check-state-integrity.sh` itself), so there is no existing helper call to
            reuse for `total_phases` even though the field is present in STATE.md. This has no effect
            on the algorithm below, which compares `completed_phases` against the directory count,
            never `total_phases`) —
            if present, numeric, and **strictly GREATER than** the directory count (a plain integer
            comparison, `-gt`, no text-normalization concern here since both sides are already plain
            counts, not phase-number strings), that is a divergence (a completed-phase count
            exceeding the number of actual phase directories is structurally impossible and can only
            come from a bad merge; a completed count LESS than the directory count is the normal,
            permanent state of any in-progress compartment — measured on this repo:
            `completed_phases: 7` against 9 git-tracked / 10 on-disk directories, staying exit-0
            either way since 7 is less than both). S2 remains the primary signal and already fires
            on all three research-§8 variants on its own; S4 here is a narrower, sound complement,
            not a required detector of the same incidents.
      Either (a) or (b) firing is exit-1, naming the number(s) involved.
    - S5 (level leak): collect every phase-directory numeric prefix owned by any
      `.planning/workstreams/<name>/` compartment, NORMALIZED; grep the ROOT `.planning/ROADMAP.md`
      for `^### Phase [0-9]` headers, extract and NORMALIZE their numbers too, and compare the two
      normalized sets. A match is a leaked workstream phase at the root — exit-1, naming the
      workstream and the leaked number in its normalized form.

    Exit codes, every one named in the docstring, none implicit: `0` = at least one compartment
    checked, none of S2/S4/S5 fired · `1` = at least one of S2/S4/S5 fired (message names which) ·
    `2` = non vérifiable (hors dépôt git, `.planning/` illisible, `workstream-policy.sh`
    introuvable) · `3` = SILENCE, `.planning/workstreams/` absent, nothing to check · `64` = usage.

    Then create `scripts/hooks/post-merge` on the exact opt-in activation model of
    `scripts/hooks/pre-push` (never armed by default; the sole activation path is
    `git config core.hooksPath scripts/hooks`). Unlike the rough skeleton in `39-PATTERNS.md`
    (which pipes the gate's exit code straight through a bare `||`), this hook MUST branch on the
    exit code explicitly: only exit `1` from `check-divergence.sh` prints the
    `[post-merge] divergence de workstream détectée après fusion (voir ci-dessus).` line to stderr
    and exits non-zero; exit `0` and exit `3` exit `0` silently; exit `2` and `64` print a distinct
    `[post-merge] check-divergence.sh non vérifiable/usage — ne bloque pas` advisory to stderr and
    still exit `0`. A broken or inapplicable gate must never cry "divergence" on an ordinary merge —
    that would make every merge on this non-partitioned repo look like a false alarm the day the
    hook gets armed. Note also that `post-merge` cannot itself undo a merge (git ignores its exit
    code for that purpose, unlike `pre-push`) — its whole job is to be LOUD, not to block; the
    exit-1 branch exists so a caller piping the hook's own output can still detect the signal.

    Prove the full vertical slice, for real, once, inside a throwaway git repo (never inside this
    repo's own working tree — see the Success Criteria in `39-03-PLAN.md`'s companion plan for the
    hard rule that governs the whole phase): the `<verify>` command below builds that repo, seeds a
    baseline with one registered phase directory, branches, adds a second phase directory sharing
    the SAME numeric prefix without registering it (the S2/orphan shape from research §8), merges
    the branch back with `--no-ff`, and checks that the armed `post-merge` hook's output contains
    the divergence line after that real `git merge` — not a synthetic call to the script alone.
  </action>
  <verify>
    <automated>bash -c 'set -eu
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q -b main
git -C "$T" config user.email t@t.example
git -C "$T" config user.name tracer
mkdir -p "$T/scripts/hooks" "$T/plugin/conductor/scripts" "$T/plugin/planning-core/scripts"
cp scripts/hooks/post-merge "$T/scripts/hooks/post-merge"
cp plugin/conductor/scripts/check-divergence.sh "$T/plugin/conductor/scripts/check-divergence.sh"
cp plugin/planning-core/scripts/workstream-policy.sh "$T/plugin/planning-core/scripts/workstream-policy.sh"
chmod +x "$T/scripts/hooks/post-merge" "$T/plugin/conductor/scripts/check-divergence.sh"
git -C "$T" config core.hooksPath scripts/hooks
mkdir -p "$T/.planning/workstreams/alpha/phases/VFDO-05-x-registered"
echo stub > "$T/.planning/workstreams/alpha/phases/VFDO-05-x-registered/PLAN.md"
printf "%s\n" "---" "workstream: alpha" "---" > "$T/.planning/workstreams/alpha/STATE.md"
printf "%s\n" "### Phase 05: registered" > "$T/.planning/workstreams/alpha/ROADMAP.md"
git -C "$T" add -A && git -C "$T" commit -qm baseline
git -C "$T" branch -q other
git -C "$T" checkout -q other
mkdir -p "$T/.planning/workstreams/alpha/phases/VFDO-05-y-orphan"
echo stub > "$T/.planning/workstreams/alpha/phases/VFDO-05-y-orphan/PLAN.md"
git -C "$T" add -A && git -C "$T" commit -qm orphan
git -C "$T" checkout -q main
out=$(git -C "$T" merge -q --no-ff other -m merge 2>&1)
echo "$out" | grep -qF "[post-merge] divergence" && echo GATE_FIRED || { echo "GATE_SILENT: $out"; exit 1; }'</automated>
    <fails_when>prints GATE_SILENT (the armed hook did not detect the S2 duplicate-phase-number split-brain after a real `git merge --no-ff`) or the script aborts before reaching that line (missing file, permission error, or a copy step that failed)</fails_when>
  </verify>
  <done>`check-divergence.sh` exists, exits 3 on a non-partitioned tree and 1 on the S2/orphan fixture; `scripts/hooks/post-merge` is armed identically to `pre-push` and, once `core.hooksPath` is set, a real `git merge` that lands a duplicate-numbered phase directory prints the divergence line.</done>
  <acceptance_criteria>
    - `plugin/conductor/scripts/check-divergence.sh --help` (or a bare invocation with a bad flag) documents all five exit codes (0,1,2,3,64) in its header comment, none implicit.
    - Running the script against a fresh `mktemp -d` git repo with no `.planning/workstreams/` prints nothing and exits 3.
    - The end-to-end `<verify>` command above prints `GATE_FIRED`.
    - `scripts/hooks/post-merge` contains no unconditional `|| exit 1` on the gate's raw exit code — exit 0 and exit 3 from `check-divergence.sh` must not surface as an alert.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: test-check-divergence.sh — all three variants, S4, nominal silence, MUT-1/MUT-2</name>
  <read_first>
    plugin/conductor/scripts/tests/test-check-workstream-pointer.sh:1-120 (methodology to replicate:
    per-case mktemp -d, ok/ko counters, explicit `env -u` invocation, the MUT-1/MUT-2 doc comment at
    the top of the file);
    .planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md §8 (orphan/rootonly/
    roadmap table, exact reproduction shapes);
    plugin/conductor/scripts/check-divergence.sh (produced by Task 1 of this plan — the five exit
    codes and the S2/S4/S5 message contract it must exercise).
  </read_first>
  <files>plugin/conductor/scripts/tests/test-check-divergence.sh</files>
  <action>
    Create `plugin/conductor/scripts/tests/test-check-divergence.sh` on the exact methodology of
    `test-check-workstream-pointer.sh:1-55` (its own `mktemp -d` per case, `ok()`/`ko()` counters,
    `env -u GSD_WORKSTREAM -u VF_WORKSTREAM_PLANNING_DIR` on every invocation, `find | LC_ALL=C sort`
    fingerprinting compared with `cmp`, never `diff` — this workstation's `diff` is proxified and
    measured lying, per the repo's own established pattern). Cover, each in its own fixture:

    - Nominal (D1, second correction pass — this fixture is now the padding-mismatch regression
      guard, not just a smoke test): one compartment, phase dirs `01-`/`02-`/`03-` (zero-padded, the
      directory convention) with **UNPADDED** `### Phase 1`/`### Phase 2`/`### Phase 3` headers
      (`### Phase 01` is NEVER written — this repo's own real ROADMAP.md never pads) in that
      compartment's `ROADMAP.md`, and `STATE.md` `progress: { completed_phases: 3 }` -> exit 0,
      output does not contain the word "divergence". This fixture is measured to rougit (RC=1,
      naming `01`/`02`/`03` as orphans) under a raw string comparison — it must exit 0 once the
      numeric-normalization discipline above is implemented; do not weaken the fixture (e.g. by
      padding the headers to match) to make it pass.
    - Non-partitioned: no `.planning/workstreams/` at all -> exit 3, stdout empty (same contract as
      `check-workstream-pointer.sh` case 1).
    - orphan variant (research §8): two phase dirs sharing one numeric prefix in the same
      compartment -> exit 1, message names the duplicated number (S2).
    - orphan variant, `VFDO-`-prefixed shape (C1 regression fixture, 2026-09-10): repeat the orphan
      variant above but name the two directories `VFDO-05-x-registered`/`VFDO-05-y-orphan` — the
      shape every LIVE phase directory in this repo actually uses. The anchor must extract `05` from
      both and fire exit 1 identically to the bare-number case; this fixture exists because the
      anchor used to render empty on this exact shape (measured, see Task 1's `read_first`).
    - rootonly variant (research §8): a workstream's phase number appears as a `### Phase N` header
      in the ROOT `.planning/ROADMAP.md` -> exit 1, message names the workstream and the leaked
      number (S5).
    - S4 cardinality variant (corrected 2026-09-10, two sub-cases — NOT the raw-count form this
      task used to specify, which was measured to false-positive on this repo's own real numbers,
      see Task 1's S4 rewrite):
        - S4(a) orphan-header sub-case: a compartment with THREE phase dirs (`01-`/`02-`/`03-`,
          zero-padded) but whose own `ROADMAP.md` documents only UNPADDED `### Phase 1` and `###
          Phase 2` (no header at all for `03`, and `03` is not a duplicate of anything) -> exit 1,
          message names the undocumented number in its NORMALIZED form, `3` (and ONLY `3` — never
          the zero-padded `03`, per Task 1's own naming convention above: every exit-1 message in
          this script names numbers normalized, `nn=$((10#$int_part))`, since that is the form both
          sides of the comparison already share; normalization must resolve `01`==`1` and `02`==`2`
          as matches, per D1's numeric-comparison discipline in Task 1, or this fixture would wrongly
          name all three) as undocumented. Assert this fixture does NOT trigger S2 (no duplicate
          number anywhere) — proving S4(a) is a genuinely independent signal, not a repaint of S2.
        - S4(b) cardinality sub-case: a compartment with TWO phase dirs, `ROADMAP.md` headers
          matching both, but `STATE.md`'s `progress: { completed_phases: 5 }` — strictly GREATER
          than the directory count (2) -> exit 1, message names both numbers. Also add a companion
          fixture with `completed_phases: 1` (LESS than the directory count, the repo's own normal
          in-progress shape) and assert exit 0 — a same-wave regression guard against reintroducing
          the rejected "less-than" direction.
    - MUT-1 (fixture mutation, D-13): take the orphan fixture, register the missing phase number in
      that compartment's `ROADMAP.md` (removing the actual defect, nothing else) -> the gate flips
      from exit 1 to exit 0. Fingerprint the fixture tree with `find "$FIXTURE" | LC_ALL=C sort`
      before and after, and assert with `cmp` that only the `ROADMAP.md` blob changed (proving the
      flip came from the intended registration, not an accidental extra edit).
    - MUT-2 (script mutation, D-13): copy `check-divergence.sh` into its own `mktemp` location,
      neutralize its S2 branch (the code path that emits exit 1 for a duplicate numeric prefix) so
      it falls through to exit 0, run the mutant against the SAME orphan fixture used above, and
      assert the mutant now wrongly exits 0. If the mutant's exit code is unchanged (still 1), the
      mutation targeted dead code and is NOT opposable — per the `test-check-workstream-pointer.sh`
      MUT-2 precedent, a non-opposable mutant must make THIS SUITE fail, not silently pass.
    Never build any fixture inside this repo's real `.planning/` — every case constructs and
    destroys its own `mktemp -d` (the phase-wide paradox rule: no task anywhere in Phase 39 creates
    `.planning/workstreams/` in this repo's working tree).
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-divergence.sh; echo "RC=$?"</automated>
    <fails_when>RC is nonzero, any printed case line is prefixed `✗` instead of `✓`, or the MUT-2 case reports the mutant as non-opposable (unchanged exit code)</fails_when>
  </verify>
  <done>`test-check-divergence.sh` exercises all named cases (nominal, non-partitioned, orphan ×2 shapes, rootonly, S4(a), S4(b) ×2) plus MUT-1/MUT-2 and exits 0, all-✓, against the `check-divergence.sh` delivered by Task 1.</done>
  <acceptance_criteria>
    - At least ten distinct fixtures (nominal, non-partitioned, orphan bare-number, orphan `VFDO-`-prefixed, rootonly, S4(a) orphan-header, S4(b) greater-than, S4(b) less-than regression guard) plus two mutation cases are each in the printed tally (10 lines minimum).
    - The MUT-1 case's `cmp` proves exactly one file differs between pre- and post-mutation fixture trees.
    - The MUT-2 case demonstrably changes the exit code on the mutant vs. the original script on the identical fixture.
    - The S4(a) fixture's message does not also trigger S2 (verified case-by-case, not by tally count alone).
    - No fixture path resolves under this repo's real `.planning/` directory.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: wire check-divergence.sh into the CI `gates` job — mutation-red proof of REAL triggering (arbitrage Samuel 2026-09-10, C17 revisited)</name>
  <read_first>
    .github/workflows/ci.yml:371-609 (the `Gates workstream-aware sur un arbre RÉELLEMENT
    partitionné (fixture jetable) + non-régression racine` step — the exact `fail`/`note()` counter
    convention, single BILAN echo at the end, `BLOC 1` capacity-assertion / `BLOC 2` root
    non-regression structure to extend, NOT duplicate as a parallel mechanism);
    .github/workflows/ci.yml:622 (`check-release-tag` step immediately after — this new step is
    inserted as its own step, right after the existing workstream-gates step, never merged into it,
    so a failure here reads unambiguously as "check-divergence" in the CI log, not buried inside
    another step's tally);
    Task 1 of this plan's `<verify>` (the exact fixture recipe — `VFDO-05-x-registered` +
    `VFDO-05-y-orphan` under `.planning/workstreams/alpha/phases/` — already measured to fire exit 1
    real, reuse it here rather than inventing a new shape);
    plugin/conductor/scripts/check-divergence.sh (produced by Task 1 of this same plan).
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    **Why this task exists (write this reasoning into the SUMMARY, not just the code): the
    `post-merge` hook (Task 1) is opt-in and LOCAL — it never runs on a GitHub-merged PR, the
    dominant merge path of this repo. A gate that is correct but never triggers on the case that
    matters is ADR-059's exact "sûr mais inerte" failure mode (the reason `isolation: worktree`
    stayed disarmed after Phase 35) — this repo has already paid once for a just-but-inert
    mechanism and does not pay twice. `check-divergence.sh` itself is unchanged; only its CI
    exposure is new.**

    Add a new step to the `gates` job in `.github/workflows/ci.yml`, immediately after the existing
    `Gates workstream-aware sur un arbre RÉELLEMENT partitionné (fixture jetable) + non-régression
    racine` step (ends around line 609) and before `check-release-tag` (line 622), titled
    `check-divergence.sh en CI (C17, 2026-09-10) — mutation rouge sur fixture divergente, pas
    seulement présence du gate`. On the SAME `fail`/`note()` counter pattern already used by the
    step it follows (a local `fail=0` and `note()` scoped to this new step — do not share state
    across steps, GitHub Actions steps do not share shell variables anyway), build, in a fresh
    `mktemp -d` git repo (git-init'd and committed, exactly as the existing step already does for
    `check-state-integrity.sh`'s sake — `check-divergence.sh` also requires a git repo to resolve
    `--show-toplevel`):
      1. A CLEAN partitioned fixture: `.planning/workstreams/alpha/phases/VFDO-05-x-registered/`
         (with a stub file inside so git tracks it — empty directories are not committed, the exact
         C3 lesson this same plan's Task 1 already paid for), a matching `### Phase 05` header in
         that compartment's `ROADMAP.md`, and a `STATE.md` with `progress: { completed_phases: 0 }`.
         Run `check-divergence.sh --path "$FIX"`, assert exit 0.
      2. MUTATE the SAME fixture in place (do not rebuild it) by adding a SECOND directory sharing
         the same `05` numeric prefix (`VFDO-05-y-orphan/`, again with a stub file inside) —
         reproducing the exact S2/orphan shape already proven in Task 1's own `<verify>`. Run
         `check-divergence.sh --path "$FIX"` again, assert exit 1 — this is the mutation-red proof
         Samuel's arbitrage requires: the step must show the gate FLIP from 0 to 1 on a real,
         in-repo mutation, never merely assert that the script exists or runs without crashing.
      3. Non-regression: run `check-divergence.sh --path .` against the CI checkout of this repo's
         own real, non-partitioned working tree, assert exit 3 (silence — the same contract already
         established by `check-workstream-pointer.sh`'s own root non-regression check in the
         preceding step, `R2`).
      4. Single BILAN echo + `exit 1` if `fail != 0`, on the exact pattern of the step it follows —
         no `|| true` swallowing any of the three invocations above.
    Clean up the fixture directory (`rm -rf`) before the step ends.
  </action>
  <verify>
    <automated>bash -c '
set -eu
FIX="$(mktemp -d)"
mkdir -p "$FIX/.planning/workstreams/alpha/phases/VFDO-05-x-registered"
echo stub > "$FIX/.planning/workstreams/alpha/phases/VFDO-05-x-registered/PLAN.md"
printf "%s\n" "---" "progress:" "  completed_phases: 0" "---" > "$FIX/.planning/workstreams/alpha/STATE.md"
printf "%s\n" "### Phase 05: registered" > "$FIX/.planning/workstreams/alpha/ROADMAP.md"
git -C "$FIX" init -q
git -C "$FIX" -c user.email=t@t.example -c user.name=t -c commit.gpgsign=false add -A
git -C "$FIX" -c user.email=t@t.example -c user.name=t -c commit.gpgsign=false commit -q -m fixture-saine
rc=0
bash plugin/conductor/scripts/check-divergence.sh --path "$FIX" || rc=$?
clean_rc="$rc"
mkdir -p "$FIX/.planning/workstreams/alpha/phases/VFDO-05-y-orphan"
echo stub > "$FIX/.planning/workstreams/alpha/phases/VFDO-05-y-orphan/PLAN.md"
rc=0
bash plugin/conductor/scripts/check-divergence.sh --path "$FIX" || rc=$?
mutant_rc="$rc"
rm -rf "$FIX"
echo "clean_rc=$clean_rc mutant_rc=$mutant_rc"
[ "$clean_rc" -eq 0 ] && [ "$mutant_rc" -eq 1 ] && echo CI_MUTATION_PROOF_OK || echo FAIL'</automated>
    <fails_when>prints FAIL — the clean fixture does not exit 0, or the mutated fixture (duplicate numeric prefix) does not flip to exit 1, meaning the CI step would not actually be discriminating in real GitHub Actions runs either</fails_when>
  </verify>
  <done>`.github/workflows/ci.yml`'s `gates` job carries a new step running `check-divergence.sh` against a real, in-repo fixture, mutating it from clean to diverged and asserting the exit-code flip (0 → 1), plus a root non-regression check (exit 3) — a real trigger on the dominant GitHub-PR-merge path, not a `post-merge`-only mechanism that stays inert there (ADR-059).</done>
  <acceptance_criteria>
    - The new CI step is inserted between the existing workstream-gates step and `check-release-tag`, never merged into either.
    - The step proves a mutation flip (0 → 1) on the SAME fixture, not two independently-built fixtures that could differ for unrelated reasons.
    - The step asserts exit 3 on `--path .` against the CI checkout itself (root non-regression).
    - No `|| true` swallows any of the three `check-divergence.sh` invocations in this step.
    - This task's SUMMARY states explicitly why the CI trigger was added (ADR-059's "sûr mais inerte" precedent, `post-merge` structurally silent on GitHub-merged PRs) — not merely that it was added.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `git` merge/branch metadata -> `post-merge` hook | ref names and `git rev-parse --show-toplevel` output cross into a locally-executed script |
| repo working tree -> `check-divergence.sh` | `ROADMAP.md`/`STATE.md` content and phase-directory names (shapeable by a hostile branch before merge) are parsed by the gate |
| test-suite mutation copies -> disposable `mktemp -d` | MUT-2's neutralized script copy must never touch the original file or the real repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `scripts/hooks/post-merge` (executable resolved via `git rev-parse --git-common-dir`, never `--show-toplevel`) | low | mitigate | **Mitigated on `post-merge` alone, distinct from `pre-push` (arbitrage Samuel, AskUserQuestion session principale, 2026-09-10).** `scripts/hooks/pre-push` still carries the SAME open, unfixed finding (`.claude/agent-memory/vf-coder/project_pre-push-candidat-rce-non-corrige.md`) — that finding is NOT closed by this phase, and stays out of scope on purpose. `post-merge` is a NEW hook written in this same plan: assuming a catalogued vector while writing fresh code, rather than declaring inherited debt, is not the same move — the vector is known at the moment the line is authored, and nothing forces new code to reproduce it. So `post-merge` resolves `check-divergence.sh` from the MAIN repo (`git rev-parse --git-common-dir`, stable across `git worktree add`, unlike `--show-toplevel` which follows the worktree an attacker controls) — proven by real execution: a malicious `check-divergence.sh` planted in a hostile worktree no longer runs at merge time; only the trusted copy from the main checkout does. The directory INSPECTED (`--path`) still comes from `--show-toplevel`, by design — that is the worktree meant to be audited, only the executable's source changed. Net effect: the attack surface does not grow with this phase, instead of doubling it. |
| T-39-02 | Tampering / Information Disclosure | `check-divergence.sh` parsing `ROADMAP.md`/`STATE.md`/phase-directory names | low | mitigate | Pure `awk`/`grep`/`sed` text extraction, exactly as `check-state-integrity.sh` does — never `eval`/`source` of repo content, never executing a value read from these files. |
| T-39-03 | Denial of Service (false-positive alert) | `post-merge` hook's exit-code branch | medium | mitigate | The hook alerts ONLY on `check-divergence.sh` exit code `1`; exit `0`/`3` are silent and exit `2`/`64` print a distinct non-alerting advisory — explicitly overriding `39-PATTERNS.md`'s rougher `||` sketch, which would have cried "divergence" on every ordinary merge of this (today non-partitioned) repo the moment the hook is armed. |
| T-39-04 | Tampering | MUT-2's mutated script copy | low | mitigate | Mutation happens exclusively inside its own `mktemp` path with a `trap ... EXIT` cleanup; the original `check-divergence.sh` on disk is never modified. |
</threat_model>

<verification>
Run all three tasks' `<verify>` commands in sequence. `Task 1`'s command must print `GATE_FIRED`;
`Task 2`'s command must print `RC=0` with an all-`✓` tally; `Task 3`'s command must print
`CI_MUTATION_PROOF_OK`. Confirm no `.planning/workstreams/` directory exists anywhere under this
repo's real working tree after all three tasks (`test -d .planning/workstreams && echo LEAKED ||
echo CLEAN` must print `CLEAN`).
</verification>

<success_criteria>
- Success Criterion 4 (Phase 39 ROADMAP): the divergence net exists, is noisy on both silent
  split-brain shapes research §8 measured, and stays silent on the nominal non-diverged case —
  proven by real `git merge` once (Task 1) and by a full mutation-red suite (Task 2).
- `PART-04` is satisfiable by these two tasks alone; no other plan's output is required for it to
  pass.
</success_criteria>

<output>
Create `.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `scripts/hooks/post-merge` — new opt-in git hook (**this plan**)
- `plugin/conductor/scripts/check-divergence.sh` — new S2+S4+S5 gate (**this plan**)
- `plugin/conductor/scripts/tests/test-check-divergence.sh` — test suite + mutation-red proof (**this plan**)
- `.github/workflows/ci.yml` — check-divergence.sh armé sur le job `gates`, preuve de déclenchement par mutation (**this plan**, arbitrage Samuel 2026-09-10, C17)
- `PART-01`..`PART-09` requirement family in `.planning/REQUIREMENTS.md`, + traceability rows + tail changelog line (plan `39-02`)
- `GSDA-19` superseded (body preserved) by `PART-06` in `.planning/REQUIREMENTS.md` (plan `39-02`)
- `.planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md` — drafted, unposted upstream issue (plan `39-02`)
- `docs/ADR.md` § ADR-069 dated `### Amendement 2026-09-09 — ...` subsection + amended Statut cell (plan `39-02`)
- `plugin/dev-orchestrator/references/workstreams.md` §4(b)/(d) updated (plan `39-02`)
- `plugin/dev-orchestrator/agents/vf-dev-manager.md` — dispatch gabarit closes D-10 OPEN half (plan `39-02`, C4 correction)
- Clone-jetable proof-of-mechanism run against real `vibeflow-os` history (clone destroyed, mechanism-only nuance stated) (plan `39-03`)
- `.planning/STATE.md` — D-02 dated resumption trigger + D-06 event-based watch note (plan `39-03`)
- `.planning/ROADMAP.md` — cross-reference to the D-02 trigger (plan `39-03`)

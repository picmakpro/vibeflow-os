---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/growth-bundle/agents/growth-quality-judge.md
  - plugin/growth-bundle/agents/vf-growth-manager.md
  - plugin/growth-bundle/VERSION
  - plugin/growth-bundle/module.json
  - plugin/growth-bundle/CHANGELOG.md
  - plugin/growth-bundle/README.md
  - plugin/design-orchestrator/agents/vf-design-judge.md
  - plugin/design-orchestrator/agents/vf-design-manager.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/README.md
autonomous: true
requirements: [FABR-05]

must_haves:
  truths:
    - "growth-quality-judge et vf-design-judge portent omitClaudeMd: true (juges au sens de D-08 : disallowedTools retire Write et Edit, aucune allowlist Agent(...) — invariant I5)"
    - "vf-growth-manager et vf-design-manager portent SendMessage dans tools:, inséré juste avant le jeton Agent( ; la ligne de vf-design-manager finit toujours par la parenthèse fermante et ne cite jamais vf-dev-manager (T8 de design-orchestrator) — invariant I6, D-07"
    - "Seul le frontmatter change : corps identiques octet pour octet à main, colonne des instructions du ratchet inchangée (Pitfall 5)"
    - "Un commit par module (growth-bundle, design-orchestrator), chacun avec son bump patch (VERSION, module.json, ligne Version du README, entrée CHANGELOG), séparé de tout commit du gate (D-12)"
    - "design-orchestrator est de la polarité de Samuel : son commit est destiné à sa relecture dans la PR (D-12)"
    - "check-agents.sh --strict reste vert sur les deux dossiers d'agents ; test-growth-bundle.sh et test-design-orchestrator.sh restent verts"
  artifacts:
    - path: "plugin/growth-bundle/agents/growth-quality-judge.md"
      provides: "juge frais sans CLAUDE.md injecté"
      contains: "omitClaudeMd: true"
    - path: "plugin/growth-bundle/agents/vf-growth-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
    - path: "plugin/design-orchestrator/agents/vf-design-judge.md"
      provides: "juge frais sans CLAUDE.md injecté"
      contains: "omitClaudeMd: true"
    - path: "plugin/design-orchestrator/agents/vf-design-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
  key_links:
    - from: "plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh"
      to: "plugin/design-orchestrator/agents/vf-design-manager.md"
      via: "T8 : allowlist complète, vf-dev-manager absent, parenthèse fermée en fin de ligne"
      pattern: "T8"
  prohibitions:
    - statement: "MUST NOT présenter une décision de cadrage de Claude comme un arbitrage humain (commit, CHANGELOG, PR), ni modifier un module de la polarité de Samuel sans l'y nommer relecteur"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit, elle ne se prouve pas par machine (CLAUDE.md, traçabilité des arbitrages)"
---

<objective>
Mettre en conformité, avant l'armement des invariants (42-05), le juge et le manager de growth-bundle
et de design-orchestrator, mesurés en violation d'I5 et d'I6 (D-11). Frontmatter pur, un commit par
module avec son bump patch (D-12). design-orchestrator est de la polarité de Samuel : son commit est
destiné à sa relecture en PR.

Purpose: FABR-05 — corpus conforme sous `--strict` quand les invariants deviennent des erreurs.
Output: quatre agents corrigés, deux modules bumpés en patch, deux commits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-PATTERNS.md

Patron de référence (lecture seule) : `plugin/dev-orchestrator/agents/vf-dev-manager.md`
(`…, AskUserQuestion, SendMessage, Agent(...)`). Forme d'entrée CHANGELOG : « ## [vX.Y.Z] —
AAAA-MM-JJ (contexte) » puis « **Patch** : ».
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : growth-bundle — juge sans CLAUDE.md injecté (I5), manager avec SendMessage (I6), bump patch</name>
  <precondition>PR #85 fusionnée et contenue dans HEAD : `git merge-base --is-ancestor "$(gh pr view 85 --json mergeCommit -q .mergeCommit.oid)" HEAD` rend 0 ET `plugin/conductor/scripts/check-blueprints.sh` existe (D-14) — sinon arrêt</precondition>
  <files>plugin/growth-bundle/agents/growth-quality-judge.md, plugin/growth-bundle/agents/vf-growth-manager.md, plugin/growth-bundle/VERSION, plugin/growth-bundle/module.json, plugin/growth-bundle/CHANGELOG.md, plugin/growth-bundle/README.md</files>
  <read_first>
    - plugin/growth-bundle/agents/growth-quality-judge.md (fichier modifié — frontmatter l.1-10 ; corps l.20-26 : le CLAUDE.md du lab cité comme source à lire)
    - plugin/growth-bundle/agents/vf-growth-manager.md (fichier modifié — ligne tools: l.4)
    - plugin/growth-bundle/scripts/tests/test-growth-bundle.sh (assertions sur le juge et le manager)
    - plugin/growth-bundle/README.md (l.7), plugin/growth-bundle/CHANGELOG.md (forme de la dernière entrée)
    - .planning/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-08, D-11, D-12)
  </read_first>
  <action>
1. `growth-quality-judge.md` : ajouter `omitClaudeMd: true` juste après `memory: project` (avant
`vf-internal: true`) — juge au sens de D-08. Son corps cite déjà « le CLAUDE.md du lab (INTERDITS
RGPD) » comme référence à consulter : l'agent garde Read, il le lit explicitement ; seule
l'injection automatique disparaît. Le consigner au SUMMARY comme point de relecture, sans toucher au
corps (Pitfall 5).
2. `vf-growth-manager.md` : insérer `SendMessage, ` juste AVANT `Agent(` dans la ligne `tools:` —
manager au sens de D-07 ; la ligne finit toujours par la parenthèse fermante.
3. Bump patch de la valeur lue sur disque (attendu v2.0.9 → v2.0.10) : `VERSION`, `module.json`,
ligne `**Version**` du README. CHANGELOG en tête « ## [v2.0.10] — <date d'exécution ISO> (Phase 42
— invariants de doctrine du gate des agents) », « **Patch** : » — juge avec `omitClaudeMd: true` (I5,
D-08 ; le CLAUDE.md du lab reste lisible par Read, comme son corps le prévoit), manager avec
`SendMessage` (I6, D-07), frontmatter seul, décisions de cadrage de Claude sur délégation explicite
de Willy (AskUserQuestion session principale, 2026-09-23), 42-CONTEXT.md D-07, D-08, D-11.
4. Commit unique pour ce module :
`fix(growth-bundle v2.0.10): juge sans CLAUDE.md injecté, manager avec SendMessage (invariants I5, I6)`.
Aucun fichier hors `plugin/growth-bundle/`.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; J=plugin/growth-bundle/agents/growth-quality-judge.md; M=plugin/growth-bundle/agents/vf-growth-manager.md; fm "$J" | grep -q '^omitClaudeMd: true$' && fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$J" | body) <(body < "$J") && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/growth-bundle/agents >/dev/null && bash plugin/growth-bundle/scripts/tests/test-growth-bundle.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-GB-OK</automated>
    <fails_when>la ligne CORPUS-GB-OK est absente (champ manquant, SendMessage mal placé, corps modifié, gate ou suite du module rouge, triade de version désynchronisée)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/growth-bundle\/agents\/(growth-quality-judge|vf-growth-manager)\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas « 0 depassement(s) » et « code=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' plugin/growth-bundle/agents/growth-quality-judge.md | grep -c '^omitClaudeMd: true$'` rend 1
    - `grep -c '^tools: .*SendMessage, Agent(channel-strategist' plugin/growth-bundle/agents/vf-growth-manager.md` rend 1
    - corps inchangés, colonne d'instructions inchangée (CORPUS-GB-OK, INSTR-INCHANGE ×2)
    - VERSION, module.json, ligne Version du README et entrée CHANGELOG de tête portent la même version, patch +1 de main
    - `c=$(git log -1 --format=%H -- plugin/growth-bundle/VERSION); git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/growth-bundle/'` rend 0
  </acceptance_criteria>
  <done>Le juge et le manager de growth-bundle satisfont I5 et I6 ; module bumpé en patch dans un commit dédié ; gate et suite du module verts.</done>
</task>

<task type="auto">
  <name>Tâche 2 : design-orchestrator — juge sans CLAUDE.md injecté (I5), manager avec SendMessage (I6), bump patch (relecture Samuel)</name>
  <files>plugin/design-orchestrator/agents/vf-design-judge.md, plugin/design-orchestrator/agents/vf-design-manager.md, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/CHANGELOG.md, plugin/design-orchestrator/README.md</files>
  <read_first>
    - plugin/design-orchestrator/agents/vf-design-judge.md (fichier modifié — frontmatter l.1-10 ; corps l.20-26 : « la section design du CLAUDE.md projet » citée comme source)
    - plugin/design-orchestrator/agents/vf-design-manager.md (fichier modifié — ligne tools: l.4)
    - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh (T4 juge sans Write/Edit/Agent ; T8 l.231-256 : six noms, vf-dev-manager absent, parenthèse fermée en fin de ligne)
    - plugin/design-orchestrator/README.md (l.7), plugin/design-orchestrator/CHANGELOG.md (forme de la dernière entrée)
    - .planning/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-08, D-11, D-12)
  </read_first>
  <action>
1. `vf-design-judge.md` : ajouter `omitClaudeMd: true` juste après `memory: project` (avant
`vf-internal: true`) — juge au sens de D-08. Son corps liste « la section design du CLAUDE.md
projet » parmi ses sources ; l'agent garde Read et la lit explicitement. Le consigner au SUMMARY
comme point de relecture pour Samuel, sans toucher au corps.
2. `vf-design-manager.md` : insérer `SendMessage, ` juste AVANT `Agent(` dans la ligne `tools:` —
manager au sens de D-07. T8 de la suite du module exige que la ligne finisse par la parenthèse
fermante et ne cite jamais vf-dev-manager : l'insertion avant `Agent(` tient les deux.
3. Bump patch de la valeur lue sur disque (attendu v1.5.8 → v1.5.9) : `VERSION`, `module.json`,
ligne `**Version**` du README. CHANGELOG en tête « ## [v1.5.9] — <date d'exécution ISO> (Phase 42 —
invariants de doctrine du gate des agents) », « **Patch** : » — juge avec `omitClaudeMd: true` (I5,
D-08 ; la section design du CLAUDE.md projet reste lisible par Read), manager avec `SendMessage` (I6,
D-07), frontmatter seul, décisions de cadrage de Claude sur délégation explicite de Willy
(AskUserQuestion session principale, 2026-09-23), 42-CONTEXT.md D-07, D-08, D-11 — relecture de
Samuel demandée en PR (D-12).
4. Commit unique pour ce module :
`fix(design-orchestrator v1.5.9): juge sans CLAUDE.md injecté, manager avec SendMessage (invariants I5, I6)`.
Aucun fichier hors `plugin/design-orchestrator/`.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; J=plugin/design-orchestrator/agents/vf-design-judge.md; M=plugin/design-orchestrator/agents/vf-design-manager.md; fm "$J" | grep -q '^omitClaudeMd: true$' && fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$J" | body) <(body < "$J") && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/design-orchestrator/agents >/dev/null && bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-DO-OK</automated>
    <fails_when>la ligne CORPUS-DO-OK est absente (en particulier si T8 de test-design-orchestrator.sh rougit sur la ligne tools: du manager)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/design-orchestrator\/agents\/(vf-design-judge|vf-design-manager)\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas « 0 depassement(s) » et « code=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' plugin/design-orchestrator/agents/vf-design-judge.md | grep -c '^omitClaudeMd: true$'` rend 1
    - `grep -c '^tools: .*SendMessage, Agent(vf-crafter' plugin/design-orchestrator/agents/vf-design-manager.md` rend 1 et `grep '^tools:' plugin/design-orchestrator/agents/vf-design-manager.md | grep -c 'vf-dev-manager'` rend 0
    - corps inchangés, colonne d'instructions inchangée (CORPUS-DO-OK, INSTR-INCHANGE ×2)
    - VERSION, module.json, ligne Version du README et entrée CHANGELOG de tête portent la même version, patch +1 de main
    - `c=$(git log -1 --format=%H -- plugin/design-orchestrator/VERSION); git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/design-orchestrator/'` rend 0
  </acceptance_criteria>
  <done>Le juge et le manager de design-orchestrator satisfont I5 et I6 ; T8 du module reste vert ; module bumpé en patch dans un commit dédié, destiné à la relecture de Samuel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent → runtime Claude Code | `omitClaudeMd` retire l'injection automatique des CLAUDE.md au juge ; `SendMessage` ouvre au manager la vue sur ses pairs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Information / intégrité du jugement | growth-quality-judge (INTERDITS RGPD du CLAUDE.md du lab), vf-design-judge (section design du CLAUDE.md projet) | medium | mitigate | les deux corps citent déjà ces fichiers comme sources à LIRE et les agents gardent Read ; consigné au SUMMARY pour la relecture (Samuel pour design-orchestrator) ; aucun corps modifié |
| T-42-08 | Tampering | corps d'agent modifié en douce | low | mitigate | corps comparé octet pour octet à main (cmp) ; colonne d'instructions inchangée (INSTR-INCHANGE) |
| T-42-10 | Elevation of Privilege | allowlist du manager design | low | mitigate | T8 de test-design-orchestrator.sh (vf-dev-manager jamais dans l'allowlist, parenthèse fermée) reste vert |
</threat_model>

<verification>
- `for d in plugin/growth-bundle/agents plugin/design-orchestrator/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d"; done` — rc 0.
- `bash plugin/conductor/scripts/check-instruction-budget.sh` — `0 depassement(s)`, `code=0`.
- `bash scripts/check-version-sync.sh` — rc 0.
- Deux commits, un par module, aucun ne touche le gate ni `.planning/`.
</verification>

<success_criteria>
- Violations I5 (growth-quality-judge, vf-design-judge) et I6 (vf-growth-manager, vf-design-manager) fermées par frontmatter pur.
- D-12 : un commit par module, bump patch, design-orchestrator destiné à la relecture de Samuel.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` | invariants locaux | 42-05 |
| `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur `quality-gate-client`, `content-clarity-judge` ; `SendMessage` sur `vf-business-manager`, `vf-content-manager` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur `growth-quality-judge`, `vf-design-judge` ; `SendMessage` sur `vf-growth-manager`, `vf-design-manager` | frontmatter du corpus | 42-03 |
| mobile-test-team, business-pilot-bundle, content-bundle en patch | versions de module | 42-02 |
| growth-bundle, design-orchestrator en patch | versions de module | 42-03 |
| conductor en mineure | version de module | 42-06 |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-05 : `unclassified` / `unresolved`.** Non résolu d'office ; les
  bords traités (corps inchangé, compte d'instructions inchangé, contrainte T8 sur la ligne tools:
  du manager design) sont des vérités du plan.
- **omitClaudeMd et les sources citées par les juges** : deux corps de juge citent un CLAUDE.md comme
  source à lire ; l'effet réel d'omitClaudeMd sur une lecture EXPLICITE par Read n'est pas documenté
  par la doc officielle (seule l'injection est visée) — supposé sans effet, à confirmer à la
  relecture ou au premier run réel.

<output>
Create `.planning/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-03-SUMMARY.md` when done — il liste le hash du commit design-orchestrator (relecture Samuel) et les deux mentions de CLAUDE.md relevées dans les corps de juge.
</output>

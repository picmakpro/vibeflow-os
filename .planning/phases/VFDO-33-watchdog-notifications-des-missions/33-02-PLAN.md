---
phase: 33-watchdog-notifications-des-missions
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - plugin/conductor/scripts/dag.sh
  - plugin/conductor/scripts/tests/test-dag.sh
autonomous: true
requirements: [WTCH-01]

must_haves:
  truths:
    - "`dag.sh mark` (point de transition explicite unique, `dag.sh:246`) shelle un appel à `driver-lock.sh mark-progress --owner=<owner-observé>` à CHAQUE appel, quel que soit le `--status` — c'est l'accroche naturelle citée par 33-TERRAIN.md §2 : le seul point où `dag.sh` sait qu'un nœud vient de bouger."
    - "L'owner passé à `mark-progress` est LU depuis `driver-lock.sh status` (champ `owner`), jamais deviné ni codé en dur — si aucun lock n'est présent (`present: false`), aucun appel à `mark-progress` n'est fait."
    - "Le sibling `driver-lock.sh` est résolu par le RÉPERTOIRE DU SCRIPT `dag.sh` lui-même (`dirname` de `$0`, calculé côté bash AVANT le heredoc python, passé en argv), JAMAIS par le cwd — distinction explicite du vecteur RCE fermé en D-07/T33 (33-TERRAIN.md, `dag.sh` L110-148) : la cible ici est un fichier posé par le MÊME glob d'installation que `dag.sh`, jamais une valeur dérivée d'un cwd non fiable."
    - "AUCUNE panne côté `driver-lock.sh` (absent, non exécutable, JSON illisible, exit non nul, timeout, exception quelconque) ne fait jamais échouer `dag.sh mark` — dégradation en no-op silencieux, patron identique à `compute_stages()` (ADR-031 : signale/dégrade, ne bloque jamais le cœur du DAG)."
    - "Chaque appel `subprocess.run` vers `driver-lock.sh` passe une LISTE d'arguments (jamais `shell=True`, jamais de concaténation de chaîne) et porte un `timeout=` explicite — un `driver-lock.sh` qui pendrait ne doit jamais faire pendre `dag.sh mark`."
  artifacts:
    - "plugin/conductor/scripts/dag.sh : fonction `record_progress(driver_lock_sh)`, résolution `SCRIPT_DIR`/`DRIVER_LOCK_SH` côté bash, appel dans le bloc `mark`"
    - "plugin/conductor/scripts/tests/test-dag.sh : cas T34 à T39"
  key_links:
    - "driver-lock.sh mark-progress (plan 33-01) <-> dag.sh record_progress() : la forme JSON de sortie `{\"ok\": ..., \"owner\": ..., \"progress_epoch\": ...}` documentée en 33-01 est ce que ce plan invoque ; un changement de cette forme côté 33-01 casserait silencieusement ce plan (dégradation en no-op) — le plan 33-01 doit rester en vague 1, celui-ci en vague 2"
    - "progress_epoch écrit ici -> plan 33-03 : le détecteur de stall lit ce champ pour distinguer sain/stall/abandon sur un DAG dont les nœuds bougent réellement"
  prohibitions:
    - "Ne JAMAIS faire dépendre le succès de `dag.sh mark` (mise à jour de `idx[nid][\"status\"]`, `recompute()`, `save(dag)`) de la réussite de l'appel driver-lock — l'ordre doit garantir que le DAG est mis à jour MÊME si l'appel driver-lock échoue"
    - "Ne JAMAIS réintroduire un candidat de résolution relatif au CWD pour `driver-lock.sh` (motif D-07, 5e occurrence déjà fermée sur ce fichier) — seule la résolution par le répertoire du script `dag.sh` lui-même est autorisée"
    - "Ne JAMAIS utiliser `shell=True` ni interpoler `nid`/`status`/`owner` dans une chaîne de commande shell"
    - "Aucun `sleep` non borné dans les cas de test — tout scénario de blocage utilise `run_bounded` (déjà présent dans la suite) ou un timeout explicite côté script sous test"
---

<objective>
Câbler l'unique accroche naturelle de `dag.sh` (le point `mark`, `dag.sh:246`) à `driver-lock.sh
mark-progress` posé en 33-01. À chaque transition explicite de statut de nœud, `dag.sh` interroge le
lock courant, en extrait l'owner, et lui fait avancer `progress_epoch` — sans jamais dépendre de
cette réussite pour que le DAG lui-même progresse.

Purpose : sans ce câblage, `progress_epoch` posé en 33-01 ne bouge jamais en usage réel — ce plan
est ce qui rend WTCH-01 VRAI en pratique, pas seulement possible. Le détecteur de stall (33-03) ne
peut lire un signal que si quelque chose l'écrit.

Output : `dag.sh` étendu d'une fonction et d'un appel dans le bloc `mark`, `test-dag.sh` étendu de
6 cas (T34-T39) couvrant le cas nominal et les quatre familles de panne dégradée (absent, échoue,
JSON illisible, pend).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : `record_progress()` câblé au point `mark`, dégradation silencieuse garantie</name>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <read_first>
    - plugin/conductor/scripts/dag.sh:45-66 (parseur d'arguments bash, ligne d'invocation du heredoc python — où ajouter `DRIVER_LOCK_SH`)
    - plugin/conductor/scripts/dag.sh:110-148 (`resolve_gsd_tools_cmd()` — le PRÉCÉDENT de résolution sûre à NE PAS reproduire tel quel : celui-ci résout un OUTIL EXTERNE par PATH/env, notre cas résout un SIBLING par répertoire de script, plus simple et plus sûr)
    - plugin/conductor/scripts/dag.sh:164-196 (`compute_stages()` — patron de dégradation exact à répliquer : `try/except Exception: return None`, `capture_output=True`, `timeout=`, `check=False`, jamais un `sys.exit` non nul)
    - plugin/conductor/scripts/dag.sh:241-251 (bloc `mark` — point d'insertion de l'appel, APRÈS `idx[nid][\"status\"] = status`)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md (forme JSON exacte de `status` et `mark-progress`)
    - plugin/conductor/scripts/tests/test-dag.sh:1-40 (convention de suite : `run_bounded`, helpers `assert`/`assert_exit`/`assert_not`, `SCRIPT="$(pwd)/scripts/dag.sh"` — `driver-lock.sh` est un SIBLING au même chemin)
    - plugin/conductor/scripts/tests/test-dag.sh (bloc T33, fin de fichier — patron de test d'isolation par copie de script, à réutiliser pour simuler un `driver-lock.sh` en panne SANS toucher au vrai fichier)
  </read_first>
  <behavior>
    - T34 : lock acquis (`driver-lock.sh acquire --owner=tester`) puis `dag.sh mark --status=running` sur un nœud → `driver-lock.sh status` immédiatement après montre `progress_age_seconds` proche de 0 (le mark a bien avancé le progrès).
    - T35 : AUCUN lock présent (chemin `VF_DRIVER_LOCK` jamais acquis) → `dag.sh mark` sort 0, la sortie JSON de `mark` est identique à celle d'avant ce plan (mêmes clés `id`/`status`/`ready`), aucune erreur affichée.
    - T36 : sibling `driver-lock.sh` ISOLÉ (copie de `dag.sh` + faux `driver-lock.sh` qui sort exit 1 sans JSON, patron T33) → `dag.sh mark` sort 0, le DAG est bien mis à jour (`status` reflète le nouveau statut du nœud).
    - T37 : sibling `driver-lock.sh` ISOLÉ qui rend du JSON illisible (`status` sort `not-json`) → `dag.sh mark` sort 0, DAG mis à jour normalement.
    - T38 : sibling `driver-lock.sh` ISOLÉ qui PEND (`sleep 30`) → `dag.sh mark` revient en moins de 10s (`run_bounded`), sort 0, DAG mis à jour — preuve que le `timeout=` protège réellement, pas seulement présent en façade.
    - T39 : non-régression statique — aucun `shell=True` dans `dag.sh` ; l'appel à `driver-lock.sh` passe une LISTE (`grep` positif sur la forme `[driver_lock_sh, "mark-progress"`).
  </behavior>
  <action>
1. En tête de `dag.sh`, avant l'invocation du heredoc python (après le bloc de validation
   `[ -n "$ACTION" ]`), calculer :
   ```
   SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd 2>/dev/null || dirname "$0")"
   DRIVER_LOCK_SH="$SCRIPT_DIR/driver-lock.sh"
   ```
   Commenter en une phrase : sibling résolu par le répertoire du script `dag.sh` LUI-MÊME, jamais
   par le cwd — les deux scripts sont posés par le même glob d'installation (`vibeflow-update.sh`,
   33-TERRAIN.md §4), leur coexistence dans le même répertoire est une garantie de l'installeur, pas
   une supposition de ce script.

2. Étendre la ligne d'invocation du heredoc pour passer `"$DRIVER_LOCK_SH"` en 9e argument
   positionnel, et la ligne `action, file, nid, step, stage, deps_raw, status, scope_raw =
   sys.argv[1:9]` pour capturer aussi `driver_lock_sh = sys.argv[9]`.

3. Ajouter la fonction `record_progress(driver_lock_sh)` juste après `resolve_gsd_tools_cmd()` (même
   fichier, même style de docstring) :
   - retourne immédiatement si `driver_lock_sh` n'est pas un fichier (`os.path.isfile`) ;
   - appelle `subprocess.run([driver_lock_sh, "status"], capture_output=True, text=True, timeout=5,
     check=False)` ; retourne si `returncode != 0` ;
   - parse le stdout en JSON dans un bloc `try` — toute exception de parsing retombe dans le
     `except Exception: return` englobant de la fonction (pas un `try` imbriqué séparé) ;
   - retourne si `present` n'est pas vrai, ou si `owner` est vide ;
   - appelle `subprocess.run([driver_lock_sh, "mark-progress", f"--owner={owner}"],
     capture_output=True, text=True, timeout=5, check=False)` — le résultat n'est PAS inspecté :
     réussite ou échec de CET appel n'a aucune conséquence sur `dag.sh mark`, c'est un signal
     best-effort ;
   - toute la fonction est enveloppée d'un `try/except Exception: return` unique englobant, patron
     `compute_stages()`.

4. Dans le bloc `if action == "mark":`, juste après `idx[nid]["status"] = status`, ajouter l'appel
   `record_progress(driver_lock_sh)`. Placer l'appel AVANT `recompute(nodes)` et `save(dag)` — peu
   importe l'ordre relatif à ceux-ci (l'appel n'a aucun effet sur l'objet `dag` en mémoire), mais il
   doit rester APRÈS la mise à jour du statut (sémantique : le progrès enregistré correspond au
   nouveau statut) et ne doit JAMAIS empêcher `recompute`/`save`/`emit`/`sys.exit(0)` de s'exécuter —
   aucune exception ne doit pouvoir s'en échapper (garantie déjà par le `try/except` interne).

5. Mettre à jour le bloc de commentaire d'en-tête (`# dag.sh — ...`) pour documenter que `mark` écrit
   désormais `progress_epoch` sur le driver-lock courant s'il existe, en best-effort, sans jamais
   bloquer le DAG (WTCH-01).

Étendre ensuite `plugin/conductor/scripts/tests/test-dag.sh` avec les cas T34 à T39 décrits en
`<behavior>`. Pour T36/T37/T38, réutiliser le patron d'isolation du bloc T33 (fin de fichier
actuel) : copier `dag.sh` dans un répertoire temporaire ISOLÉ du `WORK_DIR`, y déposer un faux
`driver-lock.sh` exécutable (`chmod +x`) qui simule la panne voulue, puis invoquer la COPIE de
`dag.sh` (jamais le vrai fichier de production, jamais un chmod destructif dessus).
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -qE 'def record_progress' plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -c 'record_progress(driver_lock_sh)' plugin/conductor/scripts/dag.sh` rend `1` (le seul site d'appel, dans le bloc `mark`).
    - `grep -c 'shell=True' plugin/conductor/scripts/dag.sh` rend `0`.
    - `grep -c 'timeout=5' plugin/conductor/scripts/dag.sh` rend au moins `2` (les deux appels `subprocess.run` de `record_progress`).
    - L'appel passe une liste, jamais une chaîne : `grep -c '\[driver_lock_sh, "status"\]' plugin/conductor/scripts/dag.sh` rend `1` et `grep -c '\[driver_lock_sh, "mark-progress"' plugin/conductor/scripts/dag.sh` rend `1`.
    - Aucun candidat cwd-relatif réintroduit pour `driver-lock.sh` (D-07) : `grep -c 'os.getcwd' plugin/conductor/scripts/dag.sh` rend `0`.
    - `bash plugin/conductor/scripts/tests/test-dag.sh` sort 0, affiche les blocs `=== T34` à `=== T39`, et sa ligne de bilan affiche `0 FAIL`.
    - Les 102 assertions historiques (T1-T33) restent vertes (comptage inchangé du nombre de PASS antérieur, consigné dans le SUMMARY).
    - Non-régression du protocole driver-lock : `bash plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (33-01 n'est pas modifié par ce plan, mais l'appel croisé doit être vérifié).
    - **Mutation rouge prouvée, trace obligatoire dans le SUMMARY** : commiter d'abord ce plan, puis faire remonter une exception NON catchée hors de `record_progress()` (retirer temporairement le `try:`/`except Exception: return` englobant) avec un `driver-lock.sh` absent → `dag.sh mark` doit alors CRASHER (le mutant prouve que la garde protège réellement `mark`) ; consigner assertion exacte/attendu/obtenu ; restaurer par `git checkout -- plugin/conductor/scripts/dag.sh`.
    - **Contrôle positif du fixture T36/T37/T38** : avant d'invoquer la copie isolée de `dag.sh`, vérifier explicitement que le faux `driver-lock.sh` déposé est bien exécutable (`[ -x <chemin> ]`) et qu'il produit BIEN le comportement de panne attendu quand invoqué directement (appel isolé au faux script, hors `dag.sh`, consigné dans le SUMMARY) — sinon T36/T37/T38 seraient verts pour la mauvaise raison (panne jamais réellement exercée).
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/dag.sh && bash plugin/conductor/scripts/tests/test-dag.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && [ "$(grep -c 'shell=True' plugin/conductor/scripts/dag.sh)" -eq 0 ]</automated>
  </verify>
  <done>Chaque `dag.sh mark` avance `progress_epoch` sur le lock courant s'il existe, sans jamais dépendre de cette réussite pour que le DAG progresse, et la mutation qui retire la garde de dégradation fait effectivement crasher `mark`.</done>
  <reversibility rating="reversible">Un `git revert` rend `dag.sh` identique à HEAD ; un DAG écrit entre-temps reste lisible par la version antérieure (aucun champ n'est ajouté au fichier DAG lui-même, seul le lock externe est touché).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dag.sh` -> `driver-lock.sh` (sous-processus) | le sibling est résolu par répertoire de script, pas par cwd (précédent D-07 fermé sur ce même fichier) ; la sortie de `driver-lock.sh status` est traitée comme une entrée NON fiable (JSON potentiellement absent/illisible) |
| `driver-lock.sh` (indisponible/lent) -> `dag.sh mark` | un tiers qui remplacerait/casserait le sibling driver-lock.sh ne doit jamais pouvoir bloquer ni faire échouer la mise à jour du DAG — c'est la propriété centrale de ce plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Denial of Service | `record_progress()` | high | mitigate | `timeout=5` sur les deux appels + `try/except Exception` englobant : un `driver-lock.sh` qui pend ou plante ne bloque jamais `mark` ; cas T38 + mutation rouge |
| T-33-06 | Tampering | résolution du sibling | medium | mitigate | résolution par répertoire du script `dag.sh` lui-même, jamais par cwd — précédent D-07/T33 explicitement cité et non rouvert ; cas T39 (grep statique) |
| T-33-07 | Injection | arguments du sous-processus | low | accept | `subprocess.run` avec liste d'arguments, jamais `shell=True` ; `owner` provient de `driver-lock.sh status` déjà assaini côté 33-01 (`sanitize_field`) avant d'être réinjecté ici |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/dag.sh`
2. `bash plugin/conductor/scripts/tests/test-dag.sh` — 0 FAIL, cas T34 à T39 présents, T1-T33 historiques intacts
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé
4. La mutation exigée a rougi et sa trace (assertion, attendu, obtenu) est dans le SUMMARY
</verification>

<success_criteria>
- `dag.sh mark` avance `progress_epoch` sur le driver-lock courant à chaque transition explicite de nœud, en best-effort.
- Quatre familles de panne côté `driver-lock.sh` (absent, échoue, JSON illisible, pend) sont chacune prouvées inoffensives pour `dag.sh mark` par un cas de test dédié.
- Aucun candidat de résolution cwd-relatif n'est introduit pour le sibling `driver-lock.sh`.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-02-SUMMARY.md` en fin de plan,
en y consignant la trace de mutation exigée et le comptage avant/après des assertions de la suite.
</output>
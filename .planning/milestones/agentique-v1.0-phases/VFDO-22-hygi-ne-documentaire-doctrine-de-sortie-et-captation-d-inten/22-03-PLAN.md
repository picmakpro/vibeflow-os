---
phase: 22-hygiene-documentaire
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/README.md
  - plugin/design-orchestrator/CHANGELOG.md
autonomous: true
requirements: [DOCF-07]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Les deux modules touchés annoncent une version qui correspond à ce qu'ils contiennent réellement — la nouvelle capacité est visible dans le CHANGELOG, pas seulement dans le diff."
    - "`check-version-sync.sh` sort ✓ : la triade VERSION ↔ module.json ↔ en-tête README est alignée pour les 17 modules, et le compteur « N suites » des README racine reste juste."
    - "La release racine (VERSION, plugin.json, marketplace.json, les deux README, tag annoté, release GitHub) reste **hors périmètre**, réservée à validation humaine — comme aux Phases 13, 17 et 19."
  artifacts:
    - "plugin/dev-orchestrator/CHANGELOG.md — section [v2.9.0]"
    - "plugin/design-orchestrator/CHANGELOG.md — section [v1.4.0]"
    - "plugin/dev-orchestrator/README.md — docs-flow.md dans l'arborescence references/, T22 et T23 dans la liste des axes de test, §Références"
  key_links:
    - "`check-version-sync.sh` contrôle la triade par module ET le compteur « N suites » des deux README racine — extension de la suite existante (D-14) donc compteur inchangé à 44."
    - "`module.json` porte la version **sans** préfixe `v` ; `VERSION` et l'en-tête README la portent **avec**. Une désynchronisation des deux formes rend le gate rouge."
---

<objective>
Rendre la nouvelle capacité **distribuable** : bumper les deux modules touchés en **minor**
(nouvelle capacité, règle de numérotation du `CLAUDE.md` racine), avec leur triade complète et leur
CHANGELOG, et mettre le README du module dev à l'heure de ce qu'il contient désormais.

Purpose : sans bump, la doctrine et le geste documentaire existent sur `main` mais n'atteignent
aucun lab. Avec un bump incomplet, `check-version-sync.sh` passe au rouge et bloque toute release
ultérieure.

Output : `dev-orchestrator` v2.8.0 → **v2.9.0**, `design-orchestrator` v1.3.2 → **v1.4.0**, gate de
synchronisation vert.

**Ce que ce plan ne fait PAS, délibérément** : la release racine. Pas de bump de `VERSION` racine,
ni de `plugin/.claude-plugin/plugin.json`, ni de `.claude-plugin/marketplace.json`, ni de
l'historique des deux README racine, ni de tag annoté, ni de release GitHub. C'est le patron déjà
appliqué aux Phases 13, 17 et 19 (« réservée à validation humaine — non faite en mission »), et
l'arbre porte déjà un `VERSION` racine à `v2.44.0` non taggé issu de la Phase 20 : y toucher depuis
cette phase mélangerait deux releases. Voir `<release_frontier>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-CONTEXT.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-01-SUMMARY.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 22-03)

**Nouvelles sections de CHANGELOG**
- `plugin/dev-orchestrator/CHANGELOG.md` : `## [v2.9.0] — <date> (hygiène documentaire — doctrine
  de sortie et captation d'intention, Phase 22)`
- `plugin/design-orchestrator/CHANGELOG.md` : `## [v1.4.0] — <date> (geste documentaire en mission
  design, Phase 22)`

**Nouvelles lignes de README**
- `plugin/dev-orchestrator/README.md` : `docs-flow.md` dans l'arborescence `references/`, entrées
  `T22` et `T23` dans la liste des axes de test, ligne de doctrine en §Références

**Valeurs de version**
- `plugin/dev-orchestrator/VERSION` → `v2.9.0` · `module.json` → `2.9.0` · en-tête README → `v2.9.0`
- `plugin/design-orchestrator/VERSION` → `v1.4.0` · `module.json` → `1.4.0` · en-tête README → `v1.4.0`
</artifacts_produced>

<release_frontier>
## Frontière de release — décision explicite du plan

| Périmètre | Statut | Motif |
|---|---|---|
| Triade par module (`VERSION`, `module.json`, en-tête README) des 2 modules touchés | **DANS ce plan** | Gaté par `check-version-sync.sh` ; un module modifié sans bump rend le gate rouge et bloque la release suivante. |
| CHANGELOG des 2 modules | **DANS ce plan** | Même triade ; c'est la seule trace lisible de la capacité côté module. |
| `VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, historique des 2 README racine | **HORS** — reste à faire | Réservé à validation humaine (patron Phases 13/17/19). L'arbre porte déjà `v2.44.0` **non taggé** hérité de la Phase 20 : la release de cette phase se décidera avec celle-là, pas contre elle. |
| Tag annoté + release GitHub + `check-release-tag.sh --remote` | **HORS** — reste à faire | Règle non négociable du `CLAUDE.md` racine, exécutée par l'humain après merge sur `main`. |

Le SUMMARY de ce plan doit **inscrire nommément** ce reste-à-faire, sinon un juge frais le remontera
en faux positif (leçon de méthode du 2026-07-28 : « un juge frais ignore les déférés qu'on ne lui
nomme pas »).
</release_frontier>

<tasks>

<task type="auto">
  <name>Tâche 1 : triade + CHANGELOG de `dev-orchestrator` (v2.8.0 → v2.9.0)</name>

  <files>
    plugin/dev-orchestrator/VERSION,
    plugin/dev-orchestrator/module.json,
    plugin/dev-orchestrator/README.md,
    plugin/dev-orchestrator/CHANGELOG.md
  </files>

  <read_first>
    - `plugin/dev-orchestrator/VERSION` (valeur courante `v2.8.0`) et
      `plugin/dev-orchestrator/module.json` (champ `version`, **sans** préfixe `v`).
    - `plugin/dev-orchestrator/README.md` ligne 10 (`**Version** : v2.8.0`), lignes 86-97
      (l'arborescence `references/` où insérer `docs-flow.md`), lignes 213-243 (la liste des axes de
      test où insérer T22 et T23), lignes 247-259 (§Références).
    - `plugin/dev-orchestrator/CHANGELOG.md` lignes 1-40 — la **section `[v2.8.0]` en tête** sert de
      gabarit : titre avec version, date, résumé de phase entre parenthèses, puis les rubriques
      `### Ajouté` / `### Modifié` (et `### Corrigé` s'il y a lieu).
    - `.planning/phases/VFDO-22-.../22-01-SUMMARY.md` et `22-02-SUMMARY.md` — la matière du
      CHANGELOG vient de ce qui a été **réellement livré**, pas du plan.
    - `CLAUDE.md` racine §Discipline de release (numérotation : nouvelle capacité → **minor**).
  </read_first>

  <action>
Porter la triade et le journal du module dev à `v2.9.0` — **minor**, parce que la phase livre une
capacité nouvelle (doctrine de sortie documentaire + captation d'intention), pas un correctif.

Trois valeurs à aligner, deux formats à ne pas confondre : `VERSION` contient `v2.9.0` (avec
préfixe), le champ `version` de `module.json` contient `2.9.0` (sans préfixe), et l'en-tête
`**Version**` du README contient `v2.9.0` (avec préfixe). C'est précisément cette asymétrie que
`check-version-sync.sh` vérifie sur les 17 modules.

Compléter aussi le README du module sur trois points, en respectant le format existant de chaque
endroit :
- dans l'arborescence `references/`, ajouter `docs-flow.md` avec un commentaire court à la même
  colonne que ses voisins, juste après la ligne `ingestion-flow.md` (les deux doctrines sont
  symétriques, elles se lisent ensemble) ;
- dans la liste des axes de test, ajouter les entrées **T22** (doctrine `docs-flow.md` : quatre
  familles, trois régimes, ligne rouge de la régénération, frontière du dépôt, renvois `AGENT.md` et
  `intent-routing.md`) et **T23** (câblage du geste documentaire dans les deux managers, avec `skip`
  si le module design est hors périmètre scanné), au format des entrées T16 à T21 ;
- en §Références, ajouter une ligne de doctrine de sortie documentaire pointant sur
  `references/docs-flow.md`, symétrique de la ligne d'ingestion existante.

Rédiger la section `[v2.9.0]` en tête du CHANGELOG, en français, sur le gabarit de `[v2.8.0]`. Son
contenu vient des deux SUMMARY, pas du plan : la doctrine `docs-flow.md` et ses quatre familles, les
trois régimes de confirmation (audit libre, génération sous confirmation, régénération sous
garde-fou en trois temps), la captation d'intention enrichie dans `intent-routing.md` avec les deux
flags enfin exposés, le renvoi `AGENT.md` au chemin d'install D7, le nœud `docs` agrégé posé par les
deux managers, et les deux nouveaux axes de test. Mentionner explicitement que `check-doc-drift.sh`
n'a **pas** été modifié (D-13) — c'est une non-modification volontaire, un lecteur du journal doit
pouvoir le savoir sans lire le diff.

Ne toucher à **aucun** fichier de la racine du dépôt (voir `<release_frontier>`).
  </action>

  <verify>
    <automated>bash scripts/check-version-sync.sh 2>&amp;1 | tail -4; echo "SYNC_EXIT=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `cat plugin/dev-orchestrator/VERSION` retourne `v2.9.0`.
    - `grep -c '"version": "2.9.0"' plugin/dev-orchestrator/module.json` retourne `1`.
    - `grep -c '\*\*Version\*\* : v2.9.0' plugin/dev-orchestrator/README.md` retourne `1`.
    - `grep -c '^## \[v2.9.0\]' plugin/dev-orchestrator/CHANGELOG.md` retourne `1`, et cette section est la **première** du fichier : `grep -n '^## \[' plugin/dev-orchestrator/CHANGELOG.md | head -1` cite `v2.9.0`.
    - Le README du module cite les trois ajouts : `plugin/dev-orchestrator/README.md` contient `docs-flow.md`, contient `T22` et contient `T23`.
    - Le CHANGELOG cite la non-modification : `plugin/dev-orchestrator/CHANGELOG.md` contient `check-doc-drift.sh` dans la section `[v2.9.0]`.
    - Aucun fichier racine touché : `git diff --name-only HEAD -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json README.md README.fr.md` ne retourne rien.
    - `bash scripts/check-version-sync.sh` sort en exit 0.
  </acceptance_criteria>

  <done>
    Le module `dev-orchestrator` annonce `v2.9.0` dans ses trois emplacements, son CHANGELOG décrit
    la capacité livrée et la non-modification de `check-doc-drift.sh`, son README liste
    `docs-flow.md` et les axes T22/T23, et `check-version-sync.sh` est vert. Aucun fichier de la
    racine n'a bougé.
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : triade + CHANGELOG de `design-orchestrator` (v1.3.2 → v1.4.0)</name>

  <files>
    plugin/design-orchestrator/VERSION,
    plugin/design-orchestrator/module.json,
    plugin/design-orchestrator/README.md,
    plugin/design-orchestrator/CHANGELOG.md
  </files>

  <read_first>
    - `plugin/design-orchestrator/VERSION` (valeur courante `v1.3.2`) et
      `plugin/design-orchestrator/module.json` (champ `version`, sans préfixe `v`).
    - `plugin/design-orchestrator/README.md` ligne 7 — attention, l'en-tête de version y est sur une
      **ligne composite** (`**Type** : … · **Version** : v1.3.2 · **Dépend de** : …`), format
      différent de celui du module dev : ne remplacer que le segment de version.
    - `plugin/design-orchestrator/CHANGELOG.md` lignes 1-20 — la section `[v1.3.2]` en tête sert de
      gabarit.
    - `.planning/phases/VFDO-22-.../22-02-SUMMARY.md` — ce qui a réellement été livré côté design.
  </read_first>

  <action>
Porter la triade et le journal du module design à `v1.4.0` — **minor** : le module gagne une
capacité qu'il n'avait pas du tout (`vf-design-manager` n'avait aucun geste documentaire), ce n'est
pas un correctif.

Mêmes deux formats qu'en tâche 1 : `VERSION` avec préfixe `v`, `module.json` sans préfixe, en-tête
README avec préfixe. Le README de ce module porte sa version sur une ligne composite — remplacer le
seul segment `**Version** : v1.3.2`, sans toucher aux segments `**Type**` et `**Dépend de**` qui
l'entourent sur la même ligne.

Rédiger la section `[v1.4.0]` en tête du CHANGELOG, en français, sur le gabarit de `[v1.3.2]` : le
manager de mission design pose désormais le même nœud `docs` agrégé en fin de mission que son
homologue dev, sur les mêmes quatre déclencheurs, par **renvoi** vers la doctrine hébergée dans
`dev-orchestrator` (`dev-orchestrator-references/docs-flow.md`) — aucune copie locale, une capacité
une seule voix (ADR-057). Préciser deux non-changements que le journal doit rendre visibles : le
gate `DESIGN.md` reste distinct et inchangé (la bible visuelle n'est pas de la doc produit), et le
frontmatter de l'agent n'a pas bougé (`Skill` y était déjà).

Ne toucher à **aucun** fichier de la racine du dépôt.
  </action>

  <verify>
    <automated>bash scripts/check-version-sync.sh 2>&amp;1 | tail -4; bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh 2>&amp;1 | tail -2</automated>
  </verify>

  <acceptance_criteria>
    - `cat plugin/design-orchestrator/VERSION` retourne `v1.4.0`.
    - `grep -c '"version": "1.4.0"' plugin/design-orchestrator/module.json` retourne `1`.
    - `grep -c '\*\*Version\*\* : v1.4.0' plugin/design-orchestrator/README.md` retourne `1`, et la ligne 7 contient toujours `**Type**` et `**Dépend de**` (segments voisins préservés).
    - `grep -c '^## \[v1.4.0\]' plugin/design-orchestrator/CHANGELOG.md` retourne `1`, et cette section est la première : `grep -n '^## \[' plugin/design-orchestrator/CHANGELOG.md | head -1` cite `v1.4.0`.
    - Le CHANGELOG cite le renvoi et les deux non-changements : le fichier contient `docs-flow.md`, `DESIGN.md` et `frontmatter` dans la section `[v1.4.0]`.
    - Aucun fichier racine touché : `git diff --name-only HEAD -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json README.md README.fr.md` ne retourne rien.
    - `bash scripts/check-version-sync.sh` sort en exit 0 (17 modules alignés, compteur de suites toujours `44`).
    - `bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh` sort en exit 0.
  </acceptance_criteria>

  <done>
    Le module `design-orchestrator` annonce `v1.4.0` dans ses trois emplacements, son CHANGELOG
    décrit le renvoi cross-module et les deux non-changements, sa suite est verte, et
    `check-version-sync.sh` est vert sur les 17 modules. Aucun fichier de la racine n'a bougé.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt de distribution → labs installés | un bump de module est ce qui fait descendre la doctrine chez les utilisateurs ; un bump incohérent distribue un état invérifiable |
| gate `check-version-sync.sh` → chaîne de release | ce gate est le seul contrôle machine de la cohérence des versions ; le laisser rouge bloque toute release ultérieure du dépôt |

> Surface honnête : ce plan édite huit fichiers de métadonnées et de journal. Il n'exécute aucun
> code nouveau, n'installe rien, ne publie rien. Sa seule menace réelle est la **désynchronisation**.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Tampering | triade de version d'un module (3 emplacements, 2 formats) | medium | mitigate | `check-version-sync.sh` en `<verify>` **des deux tâches**, avec exit 0 en condition d'acceptation. Le piège de format (préfixe `v` dans `VERSION` et l'en-tête README, absent dans `module.json`) est nommé explicitement dans les deux actions. |
| T-22-11 | Repudiation | release racine partiellement faite depuis une phase | medium | mitigate | Frontière écrite en `<release_frontier>` et en condition d'acceptation négative des deux tâches (`git diff --name-only` sur les 5 fichiers racine doit être vide). Précédent direct : Phase 19, où s'accorder un morceau de la release explicitement sortie du périmètre a été refusé en arbitrage de mission. |
| T-22-12 | Repudiation | reste-à-faire de release invisible pour un relecteur ultérieur | low | mitigate | Le SUMMARY doit inscrire nommément le reste-à-faire (leçon du 2026-07-28 : un juge frais ignore les déférés qu'on ne lui nomme pas). Condition d'acceptation du plan. |
| T-22-SC | Tampering | chaîne d'approvisionnement (npm / pip / cargo) | low | accept | Fait constaté : aucun paquet installé, aucun gestionnaire de paquets invoqué. `files_modified` ne contient que des `.md`, des `.json` et deux fichiers `VERSION` déjà versionnés. Le protocole d'audit de légitimité n'a aucun paquet à auditer. |

**Contributions `plan:pre`** — `api-coverage` → `detected: false` ; `assumption-delta` →
`detected: false` ; `schema-gate` → aucun ORM, saut silencieux ; `security` → ASVS L1, blocage sur
`high` ; **aucune menace `high` sur ce plan**, la plus élevée est `medium` et elle est en `mitigate`.
</threat_model>

<verification>
- `bash scripts/check-version-sync.sh` sort en exit 0 : 17 modules, triades alignées, en-têtes de
  README de modules alignés, compteur « N suites » des deux README racine toujours à `44`.
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` et
  `bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh` sortent en exit 0
  (non-régression : le bump ne casse rien).
- `git diff --name-only HEAD~2 -- VERSION plugin/.claude-plugin/plugin.json
  .claude-plugin/marketplace.json README.md README.fr.md` ne retourne rien — la frontière de
  release est tenue.
- `bash scripts/check-release-tag.sh` : son verdict **n'est pas** un critère de ce plan. Il porte
  sur la `VERSION` racine, hors périmètre ici, et il est déjà rouge sur `v2.44.0` non taggé hérité
  de la Phase 20. Le noter au SUMMARY, ne pas tenter de le rendre vert.
</verification>

<success_criteria>
1. `dev-orchestrator` est en `v2.9.0` et `design-orchestrator` en `v1.4.0`, chacun dans ses trois
   emplacements, avec les deux formats respectés (préfixe `v` sauf dans `module.json`).
2. Les deux CHANGELOG portent une section en tête qui décrit la capacité livrée **et** les
   non-modifications volontaires (`check-doc-drift.sh` côté dev ; gate `DESIGN.md` et frontmatter
   côté design).
3. Le README du module dev liste `docs-flow.md` dans son arborescence et T22/T23 dans ses axes de
   test.
4. `check-version-sync.sh` est vert.
5. Aucun des cinq fichiers de release racine n'a été modifié.
6. Le SUMMARY inscrit nommément le reste-à-faire de release racine, avec le fait que `VERSION`
   racine porte déjà un `v2.44.0` non taggé hérité de la Phase 20.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-03-SUMMARY.md` en fin de plan, avec une section « Reste à faire — release racine » nommant les cinq fichiers, le tag annoté, la release GitHub et l'état `v2.44.0` non taggé hérité de la Phase 20.
</output>

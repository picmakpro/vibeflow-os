---
phase: 33-watchdog-notifications-des-missions
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - plugin/conductor/scripts/check-guard-health.sh
  - plugin/conductor/scripts/tests/test-check-guard-health.sh
autonomous: true
requirements: [WTCH-02, QUAL-01]

must_haves:
  truths:
    - "check-guard-health.sh (déjà câblé sur `SessionStart`, générique tout le parc) gagne un second sous-contrôle : il lit `driver-lock.sh status` (sibling résolu par répertoire de script, même motif que 33-02) et distingue TROIS verdicts (D-33-A) : sain (les deux horloges fraîches, ou aucun lock présent), STALL (heartbeat frais, `progress_age_seconds` au-delà du seuil), ABANDON (`stale: true`, heartbeat mort) — chacun avec son cas de test nommé (critère de succès n°2 littéral)."
    - "AUCUNE nouvelle entrée `hooks.json` : ce plan étend un script DÉJÀ armé sur `SessionStart` — c'est le chemin explicitement privilégié par D-33-CONTEXT (contrainte transverse merge-hooks) pour ne jamais risquer le bug d'idempotence cross-matcher."
    - "QUATRE issues (QUAL-01), pas trois : (a) PASS — sain ; (b) FAIL — stall/abandon détecté, signalé ; (c) imparsable (JSON de `status` inattendu, subprocess rc=0) → fail-open SILENCIEUX, aucun rapport, jamais un crash ; (d) dépendance indisponible (`driver-lock.sh` absent/non-exécutable, `python3` introuvable, OU `status` rend un code non nul) → fail-open BRUYANT — jamais un vert."
    - "Le sous-contrôle NE SOURCE PAS `plugin/_internal/lib/vf-portable.sh` ni ne copie son bloc localisateur canonique — il reproduit LOCALEMENT les trois actions de `vf_guard_unavailable` (marqueur atomique + stderr + retour) sur le MÊME `HEALTH_DIR` déjà résolu par ce script (même choix déjà fait pour la dérivation du répertoire en tête de fichier). CE script N'EST PAS un nouveau consommateur du bloc canonique — `notify.sh` (plan 33-04) reste le seul et unique 6e consommateur de T12."
    - "ADR-031 : ce script SIGNALE, ne tue et ne corrige jamais — aucun `rm`/`mv`/`touch` sur le répertoire de santé (invariant déjà en vigueur, non touché), et aucun appel à `takeover`/`release` sur le driver-lock."
  artifacts:
    - "plugin/conductor/scripts/check-guard-health.sh : fonction de sous-contrôle stall/abandon, flag `--stall-window=<sec>` (défaut 1800), verdict combiné avec le scan de marqueurs existant"
    - "plugin/conductor/scripts/tests/test-check-guard-health.sh : cas D14 à D22"
  key_links:
    - "progress_epoch/progress_age_seconds (plan 33-01) -> ce sous-contrôle : seule source de la distinction stall/abandon, jamais un second calcul indépendant"
    - "ce sous-contrôle -> `notify.sh` (plan 33-05, câblage des jalons) : ce plan ne notifie PAS lui-même sur l'OS — il signale via le canal existant (stdout `--hook` + marqueurs). Le câblage `notify.sh` aux jalons de mission est un plan SÉPARÉ (33-05), ne pas le préempter ici."
  prohibitions:
    - "Ne JAMAIS ajouter d'entrée à `plugin/conductor/hooks/hooks.json` dans ce plan — le script est DÉJÀ armé, une entrée supplémentaire sur le même script/événement déclencherait le bug d'idempotence cross-matcher (CONCERNS.md, HIGH, NON CORRIGÉ)"
    - "Ne JAMAIS décider un seuil de stall en dur sans le rendre configurable (`--stall-window=`) — un seuil figé serait une régression silencieuse le jour où D-33-A serait révisée"
    - "Ne JAMAIS traiter un lock ANTÉRIEUR à 33-01 (`progress_epoch` absent, rétrocompat) comme un stall — c'est un état légitime et SAIN, pas une panne"
    - "Ne JAMAIS faire de `rm`/`mv`/`touch` sur `HEALTH_DIR` — lecture ET écriture restent additive-only, jamais d'élagage (invariant déjà documenté en tête du fichier)"
    - "Aucun `sleep` non borné dans les tests — fixtures via manipulation directe de `driver-lock.sh` meta ou du répertoire de santé, jamais une attente réelle"
---

<objective>
Étendre `check-guard-health.sh` — déjà câblé sur `SessionStart`, déjà générique tout le parc — d'un
second sous-contrôle qui lit le battement posé en 33-01 et distingue sain/stall/abandon, sans
ajouter aucune nouvelle entrée de hook (le bug d'idempotence cross-matcher de `merge-hooks.sh`
resterait fermé). Le signal reste BRUYANT sur toute dépendance indisponible, SILENCIEUX sur un JSON
imparsable, et jamais un vert de complaisance sur les deux.

Purpose : sans ce plan, `progress_epoch` (33-01) et son câblage (33-02) restent des données mortes —
personne ne les lit jamais. C'est le composant qui rend WTCH-02 VRAI : « un stall ne survit pas au
prochain geste d'une session VF vivante » se vérifie précisément ICI, à la prochaine ouverture de
session.

Output : `check-guard-health.sh` étendu d'un sous-contrôle et d'un flag, `test-check-guard-health.sh`
étendu de 9 cas (D14-D22) couvrant les trois verdicts et les quatre issues QUAL-01.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : sous-contrôle stall/abandon, quatre issues, aucune nouvelle entrée de hook</name>
  <files>plugin/conductor/scripts/check-guard-health.sh, plugin/conductor/scripts/tests/test-check-guard-health.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-guard-health.sh:57-116 (parseur d'arguments, dérivation `HEALTH_DIR`, `hook_exit()`, `indetermine()` — patrons à réutiliser tels quels)
    - plugin/conductor/scripts/check-guard-health.sh:175-184 (verdict final actuel — bloc à étendre pour combiner marqueurs + stall)
    - plugin/_internal/lib/vf-portable.sh:130-158 (`vf_guard_unavailable` — les TROIS actions à reproduire localement, jamais à sourcer)
    - plugin/conductor/scripts/driver-lock.sh (plan 33-01) — forme JSON de `status` : `present`, `stale`, `owner`, `step`, `age_seconds`, `progress_epoch`, `progress_age_seconds`
    - plugin/conductor/scripts/dag.sh (plan 33-02) — patron de résolution de sibling par répertoire de script (`SCRIPT_DIR`), à répliquer ici pour `driver-lock.sh`
    - plugin/conductor/scripts/guard-driver-lock.sh:132-137 (`vf_resolve_python --fast` — patron du refus BRUYANT quand aucun interprète Python n'est utilisable)
    - plugin/conductor/scripts/tests/test-check-guard-health.sh (convention `ok()`/`ko()`, préflight anti-fixture-réelle, `write_marker()`, cas D13 déjà en place référençant `DRIVER_LOCK`/`GUARD_LOCK`)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md §5 (couplage critique HEALTH_DIR écrivain/lecteur)
  </read_first>
  <behavior>
    - D14 : aucun lock présent (`VF_DRIVER_LOCK` pointe un chemin jamais acquis) → verdict global SAIN (code 3, ou 0 traduit sous `--hook`), aucune ligne de stall.
    - D15 : lock acquis, `heartbeat_epoch` et `progress_epoch` tous deux frais → SAIN, aucune ligne de stall.
    - D16 : lock acquis, `heartbeat` rafraîchi (frais), `progress_epoch` antidaté au-delà du seuil (`--stall-window=` ou défaut 1800s, via un helper de forgeage direct sur le `meta` du lock — jamais un `sleep`) → SIGNAL (code 0), une ligne mentionnant explicitement le mot « stall » et l'`owner`/`step` du lock.
    - D17 : lock acquis, `heartbeat_epoch` antidaté au-delà du TTL (`stale: true`) → SIGNAL, une ligne distincte mentionnant explicitement l'« abandon »/le lock périmé, jamais confondue avec la ligne de stall (D16).
    - D18 : lock acquis par l'ANCIEN protocole (meta sans ligne `progress_epoch=`, `heartbeat_epoch` frais) → SAIN, aucune ligne de stall (rétrocompat, jamais un faux positif).
    - D19 : sibling `driver-lock.sh` ABSENT (copie isolée de `check-guard-health.sh` dans un répertoire sans `driver-lock.sh`, patron T33/T36-38 de `dag.sh`) → verdict global INDÉTERMINÉ (code 4, ou 0 sous `--hook`, JAMAIS 3/SAIN), ET un marqueur est écrit dans `HEALTH_DIR` (vérifié par lecture directe du fichier après l'appel), ET un message apparaît sur stderr.
    - D20 : sibling `driver-lock.sh` présent et exécutable mais `python3` introuvable sur le `PATH` (PATH restreint pour ce cas, patron `guard-driver-lock.sh`) → même verdict que D19 : INDÉTERMINÉ + marqueur + stderr.
    - D21 : sibling `driver-lock.sh` ISOLÉ qui rend un JSON structurellement inattendu (`status` sort 0 mais un texte non-JSON) → fail-open SILENCIEUX : verdict global SAIN si rien d'autre ne signale (pas de marqueur écrit, pas de ligne stall, pas d'INDÉTERMINÉ).
    - D22 : signal combiné — un marqueur de garde existant (patron D13/`write_marker`) ET un stall détecté simultanément → DEUX lignes distinctes sur stdout (une par famille de signal), jamais une fusionnée qui perdrait l'une des deux informations.
  </behavior>
  <action>
1. En tête de `check-guard-health.sh`, après la résolution de `HEALTH_DIR`, ajouter :
   ```
   SCRIPT_DIR_SELF="$(cd "$(dirname "$0")" && pwd 2>/dev/null || dirname "$0")"
   DRIVER_LOCK_SH="$SCRIPT_DIR_SELF/driver-lock.sh"
   STALL_WINDOW=1800
   ```
   et ajouter au parseur d'arguments `--stall-window=*) STALL_WINDOW="${arg#*=}" ;;` avec la même
   garde de numéricité que `WINDOW` (usage 64 sinon).

2. Ajouter `report_self_unavailable() { local motif="$1"; ...}` juste après `hook_exit()` : reproduit
   LOCALEMENT les trois actions de `vf_guard_unavailable` (écriture atomique tmp+mv du marqueur
   `$HEALTH_DIR/check-guard-health.sh.marker`, `echo "[check-guard-health] $motif" >&2`, retour non
   nul) SANS sourcer `vf-portable.sh`. Commenter explicitement le motif de cette duplication (déjà
   justifié en tête de fichier pour `HEALTH_DIR`) et la conséquence : ce script n'est PAS un
   consommateur du bloc localisateur canonique, `notify.sh` (33-04) reste le seul 6e.

3. Ajouter `check_driver_stall()` qui :
   - retourne silencieusement (aucune variable de signal positionnée) si `DRIVER_LOCK_SH` n'est pas
     un fichier exécutable → appelle `report_self_unavailable "driver-lock.sh introuvable ou non
     exécutable ($DRIVER_LOCK_SH)"` et positionne `STALL_INDETERMINATE=1` ;
   - retourne de même si `command -v python3 >/dev/null 2>&1` échoue → même `report_self_unavailable`
     avec un motif dédié, `STALL_INDETERMINATE=1` ;
   - exécute `"$DRIVER_LOCK_SH" status` (capturé, `2>/dev/null`) ; si le code de retour n'est pas 0
     → `report_self_unavailable "driver-lock.sh status a rendu un code non nul"` + `STALL_INDETERMINATE=1` ;
   - parse le JSON via `STATUS_JSON="$out" python3 <<'PYEOF' ... PYEOF` (jamais un `-c` avec le
     contenu interpolé dans la chaîne de commande — le JSON passe par variable d'environnement,
     jamais par argv ni par concaténation) : si le parse échoue ou que la forme est inattendue,
     imprimer RIEN et sortir non nul → capturé côté bash, traité en fail-open SILENCIEUX (aucune
     variable positionnée, `STALL_INDETERMINATE` reste à 0) ;
   - sinon, lit les champs `present`/`stale`/`owner`/`step`/`progress_age_seconds` réémis en lignes
     `clé=valeur` par le script python, les récupère côté bash via une boucle `while IFS='='
     read -r k v` (JAMAIS un `eval`) ;
   - si `present` != `true` → rien à signaler ;
   - si `stale` == `true` → `STALL_LINE="[mission-watchdog] abandon detecte — owner=$d_owner
     step=$d_step (heartbeat mort, lock perime) — ne JAMAIS tuer (ADR-031), takeover disponible si
     legitime"` ;
   - sinon si `progress_age_seconds` est numérique ET `> STALL_WINDOW` → `STALL_LINE="[mission-watchdog]
     stall detecte — owner=$d_owner step=$d_step (progres fige depuis ${progress_age_seconds}s,
     heartbeat frais) — verifier la session en cours, ne JAMAIS tuer (ADR-031)"` ;
   - sinon (y compris `progress_age_seconds` absent/`null`, cas rétrocompat 33-01) → rien à signaler.

4. Dans le bloc de verdict final (actuellement lignes 175-184) : appeler `check_driver_stall`
   AVANT toute décision finale. Si `STALL_INDETERMINATE` vaut 1 → `indetermine "sous-controle
   driver-lock indisponible (deja signale sur stderr / marqueur ecrit ci-dessus)"` (réutilise la
   fonction existante, qui appelle déjà `hook_exit 4`). Sinon, si `FRESH_COUNT` == 0 ET `STALL_LINE`
   vide → SAIN (`hook_exit 3`, comme avant). Sinon, construire la sortie en accumulant JUSQU'À DEUX
   lignes (celle des marqueurs existants SI `FRESH_COUNT` > 0, celle de `STALL_LINE` si non vide),
   les imprimer chacune sur SA PROPRE ligne, puis `hook_exit 0`.

5. Mettre à jour le bloc de commentaire d'en-tête pour documenter le sous-contrôle, le flag
   `--stall-window=`, et la règle « une ligne PAR FAMILLE de signal (marqueurs de garde, stall de
   mission), jamais une par marqueur individuel au sein d'une famille » (amendement explicite du
   commentaire D6 existant, qui ne parlait que des marqueurs).

Étendre ensuite `test-check-guard-health.sh` avec les cas D14 à D22 décrits en `<behavior>`, à la
convention exacte du dossier (`ok()`/`ko()`, préflight anti-fixture-réelle déjà en place, isolation
`--dir=` systématique). Pour D19/D20, réutiliser le patron d'isolation par copie de script (comme
T33/T36-38 de `test-dag.sh`) : copier `check-guard-health.sh` dans un répertoire temporaire ISOLÉ,
jamais de modification destructive du vrai fichier ni du vrai `PATH` de la suite entière.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'check_driver_stall\(\)' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'report_self_unavailable\(\)' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - Le script NE source PAS vf-portable.sh : `grep -c 'vf-portable' plugin/conductor/scripts/check-guard-health.sh` rend `0` (aucune mention — même le nom de la lib est absent, preuve qu'aucun bloc localisateur canonique n'a été copié).
    - `grep -c 'eval' plugin/conductor/scripts/check-guard-health.sh` rend `0`.
    - `grep -qE -- '--stall-window' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - Aucune écriture destructive sur `HEALTH_DIR` : `grep -c '\brm \|\bmv \|\btouch ' plugin/conductor/scripts/check-guard-health.sh` rend `0` (en excluant la ligne du marqueur atomique `tmp.$$` qui utilise `mv -f` — vérifier à l'œil et consigner dans le SUMMARY que le seul `mv` du fichier est l'écriture atomique du marqueur elle-même, jamais un élagage).
    - `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` sort 0, affiche les blocs `=== D14` à `=== D22`, et sa ligne de bilan affiche `0 FAIL`.
    - Les cas D1-D13 historiques restent verts (comptage PASS avant/après consigné dans le SUMMARY).
    - Non-régression du reste de la suite driver-lock : `bash plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (aucune modification de 33-01, vérification croisée).
    - T12 de `test-vf-portable.sh` n'a PAS été modifié par ce plan : `git diff --stat plugin/_internal/tests/test-vf-portable.sh` (avant commit) rend une sortie VIDE.
    - **Mutation rouge n°1, trace obligatoire dans le SUMMARY** : commiter d'abord, puis dans `check_driver_stall()`, remplacer la comparaison `stale == "true"` par un test toujours faux → D17 doit rougir (abandon non détecté) ; consigner assertion/attendu/obtenu ; restaurer par `git checkout -- plugin/conductor/scripts/check-guard-health.sh`.
    - **Mutation rouge n°2** : retirer l'appel à `report_self_unavailable`/`STALL_INDETERMINATE=1` du chemin « driver-lock.sh absent » → D19 doit rougir (verdict retombe en SAIN au lieu d'INDÉTERMINÉ — c'est exactement le vert de complaisance que QUAL-01 interdit) ; consigner de même ; restaurer.
    - **Contrôle positif du fixture D18** : avant d'invoquer le script sous test, vérifier explicitement par lecture directe du `meta` que la ligne `progress_epoch=` est bien ABSENTE — consigner ce contrôle dans le SUMMARY (même exigence que les fixtures de rétrocompat en 33-01).
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/check-guard-health.sh && bash plugin/conductor/scripts/tests/test-check-guard-health.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && [ "$(grep -c 'vf-portable' plugin/conductor/scripts/check-guard-health.sh)" -eq 0 ] && [ "$(grep -c 'eval' plugin/conductor/scripts/check-guard-health.sh)" -eq 0 ]</automated>
  </verify>
  <done>`check-guard-health.sh` distingue sain/stall/abandon à chaque `SessionStart`, sans nouvelle entrée de hook, avec les quatre issues QUAL-01 chacune prouvée par cas de test, et les deux mutations qui casseraient la détection ou réintroduiraient un vert de complaisance rougissent avec leurs traces.</done>
  <reversibility rating="reversible">Extension additive d'un script déjà lu-seul sur son domaine existant : un `git revert` rend le fichier identique à HEAD, aucun format de marqueur existant n'est changé.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `driver-lock.sh status` (sous-processus) -> `check_driver_stall()` | sortie traitée comme entrée NON fiable (JSON potentiellement absent/illisible/inattendu) — jamais un `eval`, jamais une interpolation directe dans une commande |
| dépendance manquante (`driver-lock.sh`, `python3`) -> verdict de session | une dépendance absente NE DOIT JAMAIS se traduire par un vert — c'est le cœur de QUAL-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Information Disclosure | verdict final | low | accept | JSON local d'un lock déjà lisible par tout process du même utilisateur ; rien de nouveau n'est exposé |
| T-33-09 | Denial of Service (vert de complaisance) | `check_driver_stall()` | high | mitigate | toute dépendance indisponible force `STALL_INDETERMINATE=1` -> `hook_exit 4`, jamais 3 ; cas D19/D20 + mutation rouge n°2 |
| T-33-10 | Tampering | parsing JSON | medium | mitigate | passage par variable d'environnement, jamais par argv/concaténation ; `eval` absent (grep) ; un JSON structurellement inattendu dégrade en silence plutôt qu'en exécution de code |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/check-guard-health.sh`
2. `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` — 0 FAIL, cas D14 à D22 présents, D1-D13 intacts
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé
4. Les deux mutations exigées ont rougi et leurs traces sont dans le SUMMARY
5. `test-vf-portable.sh` n'est pas modifié par ce plan (T12 reste à 5 consommateurs)
</verification>

<success_criteria>
- Trois verdicts (sain/stall/abandon) sont distingués à la lecture de `driver-lock.sh status`, chacun avec son cas de test nommé.
- Les quatre issues QUAL-01 sont chacune prouvées : sain, signal, imparsable silencieux, dépendance indisponible bruyant.
- Aucune nouvelle entrée `hooks.json` ; `check-guard-health.sh` reste le seul point de lecture, `notify.sh` (33-04) reste le seul 6e consommateur du bloc localisateur canonique.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-03-SUMMARY.md` en fin de plan,
en y consignant les deux traces de mutation exigées et le comptage avant/après des assertions.
</output>

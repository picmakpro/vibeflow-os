---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 04
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - plugin/conductor/scripts/check-instruction-budget.sh
  - plugin/conductor/scripts/tests/test-check-instruction-budget.sh
  - .planning/instruction-budget-baselines.tsv
autonomous: true
requirements: [FABR-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "check-instruction-budget.sh garde sa découverte des agents INCHANGÉE (glob à un niveau, D-03) et AJOUTE une seconde découverte, récursive, des SKILL.md : chaque dossier plugin/*/ sauf un module dont le module.json déclare le type doc-only, dossiers cachés et *-references élagués, le nombre de SKILL.md exclus étant imprimé"
    - "Un SKILL.md de plus de 500 lignes (fichier entier, même mesure que les agents) sort DEPASSEMENT-SKILL-ADR029 et rend rc 1 quand le ratchet est armé ; 500 lignes exactement passe ; aucun seuil d'avertissement n'est inventé pour les skills (ADR-029 n'en définit pas)"
    - "Un SKILL.md au frontmatter jamais refermé ou en lien symbolique est NON-VERIFIABLE (rc 2), jamais ignoré en silence"
    - "Sur une fixture sans aucun SKILL.md, le code de sortie et les verdicts des agents sont identiques à avant (les sept bascules de l'étape CI check-instruction-budget restent vraies)"
    - "La seule ligne ajoutée à .planning/instruction-budget-baselines.tsv est `@bootstrap:socle` (option ratchet-socle, décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26), dans un commit séparé dont le message porte cette citation mot pour mot sur une seule ligne (forme lue par G-1) ; cet ajout est signalé à la revue code owner de Samuel (chemin CODEOWNERS) ; aucune autre valeur de la baseline ne monte (G-1, D-Q4)"
    - "Le bootstrap (somme des métadonnées name + description + when_to_use chargées à chaque session) est mesuré de façon déterministe — octets des lignes de ces clés sous LC_ALL=C, divisés par 4 — sur le corpus socle (fermeture resolve-deps.sh conductor + skill installer + commandes du plugin) de l'option ratchet-socle ; jamais un corpus, une méthode d'estimation ou un plafond ajusté pour passer sous 2000"
    - "Toute hausse du socle au-dessus de sa ligne `@bootstrap:socle` BLOQUE : verdict DEPASSEMENT-BOOTSTRAP, rc 1 sous ratchet armé (rc 3 non armé, contrat existant du ratchet) — jamais rc 0, jamais un simple avertissement ; une mesure au-dessus de 2000 mais sans hausse sur la ligne publie AU-DESSUS-PLAFOND-ADR029, non bloquant : le plafond ADR-029 reste un objectif (BACKLOG, commit 433fea0)"
    - "Le dépôt réel reste vert (rc 0) avec la sentinelle armée, la ligne `@bootstrap:socle` égale à la mesure du jour rendue par la ligne BOOTSTRAP du script"
  artifacts:
    - path: "plugin/conductor/scripts/check-instruction-budget.sh"
      provides: "VF_SKILL_LINE_CAP, seconde découverte des SKILL.md, verdict DEPASSEMENT-SKILL-ADR029, ligne BILAN-SKILLS ; métrique BOOTSTRAP ratchet-socle (DEPASSEMENT-BOOTSTRAP bloquant, AU-DESSUS-PLAFOND-ADR029 non bloquant)"
      contains: "VF_SKILL_LINE_CAP=500"
    - path: "plugin/conductor/scripts/tests/test-check-instruction-budget.sh"
      provides: "cas SKILL-1 à SKILL-6, BOOT-1 à BOOT-5, mutants MUT-8 à MUT-11"
      contains: "MUT-8"
    - path: ".planning/instruction-budget-baselines.tsv"
      provides: "ligne de baseline du socle du bootstrap (colonne lignes 0, colonne instructions = tokens estimés)"
      contains: "@bootstrap:socle"
  key_links:
    - from: "check-instruction-budget.sh (seconde découverte)"
      to: "corpus des SKILL.md hors modules doc-only"
      via: "même critère d'exclusion que check-skills.sh (type doc-only lu dans module.json)"
      pattern: "doc-only"
    - from: "verdicts skills et bootstrap"
      to: "code de sortie existant (0/1/2/3/64)"
      via: "compteurs distincts, même priorité NON-VERIFIABLE > contrat > dépassement, aucun code nouveau"
      pattern: "SKILL_OVERRUN_COUNT"
    - from: "ligne BOOTSTRAP du rapport"
      to: "clé @bootstrap:socle de .planning/instruction-budget-baselines.tsv"
      via: "tokens > ligne → DEPASSEMENT-BOOTSTRAP (bloquant) ; ≤ ligne et > 2000 → AU-DESSUS-PLAFOND-ADR029 (non bloquant) ; clé exclue des entrées orphelines"
      pattern: "@bootstrap:socle"
  prohibitions:
    - statement: "MUST NOT faire monter une valeur de la baseline du budget d'instructions, ni y ajouter une autre ligne que `@bootstrap:socle` ; MUST NOT commiter cette ligne sans la citation « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » dans le commit même ; MUST NOT présenter une décision de Claude comme un arbitrage humain (CLAUDE.md, traçabilité des arbitrages)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; G-1 ne voit que les lignes présentes à la base ET à HEAD (une ligne AJOUTÉE lui échappe, mesuré le 2026-09-25) — la garde est donc la citation vérifiée par la Tâche 3, le signalement à Samuel et la relecture, pas la machine"
    - statement: "MUST NOT choisir ou ajuster le corpus du bootstrap, sa méthode d'estimation ou le plafond de 2000 pour faire passer la mesure, hors de l'option ratchet-socle consignée à la Tâche 2"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : se relit sur le diff et le SUMMARY (mesure publiée)"
---

<objective>
Étendre le budget d'instructions aux SKILL.md (plafond ADR-029 de 500 lignes, bloquant) et au
bootstrap (métadonnées chargées à chaque session, 2000 tokens estimés), en reprenant le socle de
check-instruction-budget.sh sans le réécrire, et borner un bootstrap que la mesure montre DÉJÀ
au-dessus du plafond selon la décision déjà tranchée : ratchet sur le socle minimal (option
ratchet-socle, décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26).

Purpose: FABR-09 (D-Q4 : réserve du BACKLOG levée, SKILL.md + bootstrap — one-way). La décision
consignée à la Tâche 2 n'est pas une réouverture de D-Q4 : elle a tranché ce que D-Q4 ne pouvait pas
voir, la mesure (socle ≈ 2 499 tokens pour un plafond de 2 000), et la manière de l'enforcer sans
rendre la CI rouge le jour même ni légaliser en silence un dépassement : la mesure du jour devient
une ligne de baseline citée, toute hausse bloque, le plafond ADR-029 reste un objectif signalé.
Output: plafond des SKILL.md armé et testé ; décision bootstrap consignée ; métrique bootstrap
ratchet-socle ; ligne de baseline `@bootstrap:socle` citée et signalée à la revue de Samuel.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md

Mesures du planificateur, 2026-09-25 (à rejouer à l'exécution, jamais recopiées en dur dans un test) :
- corpus SKILL.md hors `plugin/reference` : 21 fichiers, le plus long `plugin/skill-creator/skills/skill-creator/SKILL.md` à 485 lignes (43-03 y ajoute au plus 10 lignes) ; `plugin/installer/SKILL.md` n'a pas de module.json et reste dans le corpus ;
- bootstrap, octets des lignes name/description/when_to_use sous LC_ALL=C ÷ 4 : socle d'un lab minimal (fermeture `resolve-deps.sh conductor` = audit-architecture, conductor, consolidator, infrastructure-audit, planning-core, skill-creator, validator ; + skill `installer` exposé par `plugin/.claude-plugin/plugin.json` ; + les 7 commandes `plugin/commands/*.md`) ≈ 2 499 tokens (8 821 octets sur 11 SKILL.md + 1 176 octets de commandes) ; corpus distribué complet hors gabarits + commandes ≈ 4 682 ; conductor seul ≈ 998 ;
- ratchet ARMÉ sur ce dépôt (`.planning/.instruction-budget-armed` présente) ; la baseline `.planning/instruction-budget-baselines.tsv` est un chemin CODEOWNERS (revue de Samuel ou contournement tracé pour une PR ouverte par Willy) ;
- G-1 (`scripts/check-baseline-arbitrage.sh`) ne classe que les lignes présentes à la base ET à HEAD : une ligne ajoutée n'y est ni une hausse ni un retrait. Sa conformité de citation (`citation_ok`, l.167-178) exige, sur UNE ligne du message, le mot-clé « arbitrage » ou « décision », au moins deux virgules et une date ISO : la citation ci-dessous la satisfait telle quelle, à condition de ne jamais être coupée sur deux lignes.

**Décision bootstrap (Q1) — TRANCHÉE, plus un checkpoint (révision du 2026-09-26, tour 4).**
Option retenue : **ratchet-socle**. Citation, à reprendre mot pour mot partout où ce plan ou ses
commits s'appuient sur elle : « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 ».
La mesure du jour du socle minimal (fermeture `resolve-deps.sh conductor` = audit-architecture,
conductor, consolidator, infrastructure-audit, planning-core, skill-creator, validator ; + skill
`installer` ; + les 7 commandes `plugin/commands/*.md` ; ≈ 2 499 tokens à la planification)
devient la ligne de baseline `@bootstrap:socle` de `.planning/instruction-budget-baselines.tsv`,
dans le mécanisme de baseline que G-1 garde déjà. Une fois posée, toute HAUSSE bloque : mesure
au-dessus de la ligne → DEPASSEMENT-BOOTSTRAP, échec (rc 1 sous ratchet armé), jamais un simple
avertissement ; relever la ligne elle-même exige une nouvelle décision citée (G-1). Le plafond
ADR-029 de 2000 reste un OBJECTIF signalé (jeton AU-DESSUS-PLAFOND-ADR029), non bloquant ; le retour
sous 2000 est inscrit au BACKLOG (commit `433fea0`, section « Ramener le socle du bootstrap sous
2 000 tokens (ADR-029) — DIFFÉRÉ (2026-09-26) », lecture seule pour ce plan). Motifs, tels que
donnés :
- Willy a choisi de couvrir réellement le bootstrap (Q4), ce qui écarte l'option « avertissement seul » ;
- un plafond dur bloquerait la Phase 43 sur des modules qui appartiennent à Samuel ;
- un ratchet sur tout le corpus distribué viserait un lab hypothétique, pas le socle réel que tout lab porte.
Source : 43-CONTEXT.md, bloc « Quatrième temps (2026-09-26) ». Le ledger FABR-09 a été reformulé
en conséquence à la planification : ce plan ne modifie plus REQUIREMENTS.md.
L'étape CI « check-instruction-budget » (`.github/workflows/ci.yml` l.637-815) rejoue sept bascules sur une fixture SANS SKILL.md et teste l'absence de la chaîne en capitales DEPASSEMENT sur son cas 2/7 : aucune ligne nouvelle ne doit porter ce mot hors d'un dépassement réel. Ce plan ne modifie PAS ci.yml.
Référence de phase (B43, révision du 2026-09-26) : un SHA FIGÉ, jamais re-dérivé. Il vit dans le
fichier neutre `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md`,
sur une ligne unique « Base de phase : <sha de 40 caractères> », écrite UNE seule fois par 43-01,
son SEUL propriétaire (étape 0 de sa Tâche 1, avant tout commit de la phase). Ce plan la LIT, ne
l'écrit jamais : il dépend de 43-01 (`depends_on`, révision du 2026-09-26, tour 3 — il passe de la
vague 1 à la vague 2) pour que la base soit consignée avant son premier commit dans tous les modes
d'exécution (séquentiel, checkout partagé, worktrees parallèles). Avant ce tour, 43-01 et ce plan,
parallèles en vague 1, l'écrivaient chacun « s'il était absent », sans atomicité ni propriétaire
déclaré, et un `git rev-parse HEAD` pris après un commit de l'autre plan aurait vidé en silence ses
contrôles de diff contre la base. Jamais écrite en
planification ni dans un PLAN.md, et jamais re-dérivée par un `git log` sur les PLAN.md : une
révision de plan pendant l'exécution déplacerait alors la base et viderait en silence, tout en
restant verts, les contrôles qui la lisent. Lecture par la chaîne
`B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && …` :
base absente, malformée ou sortie de l'histoire → rien n'est imprimé, et « rien d'imprimé » vaut
échec. Pourquoi pas la base de branche : la branche porte encore la Phase 42 tant que la PR #108
n'est pas mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de
la base de branche — mesuré le 2026-09-25). Rebase : procédure univoque de 43-01, bloc de contexte
(nouvelle base = parent de l'unique commit de création de `43-BASE-PHASE.md`, rendu par sa
commande ; sinon BASE-PHASE-INTROUVABLE et arrêt en rouge — jamais une base choisie par
l'exécutant) ; ce plan, s'il trouve la chaîne rouge, s'arrête sans rien écrire. Distincte de la
« Base du plan » de 43-04-SUMMARY.md (critère Gate-Touche local). Les
gardes G-1 et G-2 gardent leur propre base : elles jugent toute la branche, Phase 42 comprise.
</context>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tracer — seconde découverte récursive des SKILL.md et plafond de 500 lignes, du cas de fixture au dépôt réel armé (SKILL-1 à SKILL-6, MUT-8, MUT-9, MUT-10)</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh, plugin/conductor/scripts/tests/test-check-instruction-budget.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (fichier modifié, 394 lignes, en entier : contrat de sortie l.24-35 ; plafond l.62-72 ; découverte D-03 l.84-110 ; frontmatter_state, body_only, lines_count l.142-172 ; boucle de mesure et verdicts l.230-330 ; calcul du RC l.340-355 ; rapport l.357-394)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (fichier modifié : en-tête l.1-35 ; mk_root, w_lines, w_armed, w_baseline, run, std_agent, gen_lines l.60-130 ; bloc mutants l.501-660, remplacement de ligne exacte par awk)
    - plugin/conductor/scripts/check-agents.sh (l.291-316 decouvrir_agents : exclusions cachés / -references, liens symboliques)
    - .github/workflows/ci.yml (l.637-815, lecture seule : les sept bascules et le bloc « dépôt réel »)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Pattern 3, Pitfall 5, Assumption A4)
  </read_first>
  <behavior>
    - SKILL-1 : fixture armée (std_agent + baseline exacte) + `plugin/demo/skills/s/SKILL.md` de 500 lignes → rc 0, ligne du fichier en verdict OK ; 501 lignes → rc 1, DEPASSEMENT-SKILL-ADR029 ; même 501 sans sentinelle → rc 3, rapport imprimé
    - SKILL-2 : `plugin/solo/SKILL.md` (racine de module) et `plugin/demo/skills/profond/sous/SKILL.md` de 501 lignes → tous deux mesurés et en DEPASSEMENT-SKILL-ADR029
    - SKILL-3 (fixture armée, baseline exacte de l'agent) : `plugin/doc/module.json` de type doc-only + `plugin/doc/content/templates/skills/t/SKILL.md` de 501 lignes → non mesuré (rc 0), ligne « 1 SKILL.md exclu(s) (module doc-only) » ; `plugin/demo/skills/x-references/r/SKILL.md` et `plugin/demo/.cache/s/SKILL.md` de 501 lignes → non mesurés
    - SKILL-4 : SKILL.md au frontmatter jamais refermé → rc 2, NON-VERIFIABLE ; SKILL.md en lien symbolique → rc 2, NON-VERIFIABLE
    - SKILL-5 : fixture sans aucun SKILL.md, une ligne neutre ajoutée à l'agent (cas 2/7 de la CI) → rc 0, LIGNES-EN-HAUSSE présent, aucune occurrence de la chaîne en capitales DEPASSEMENT, ligne « corpus skills decouvert : 0 SKILL.md »
    - SKILL-6 : fixture sans SKILL.md et répertoire vide (cas 7/7) → rc 2 inchangé
    - MUT-8 : la comparaison du plafond des skills neutralisée → fixture SKILL-1 à 501 lignes : rc_original=1, rc_mutant=0
    - MUT-9 : la ligne d'exclusion doc-only neutralisée → fixture SKILL-3 : rc_original=0, rc_mutant=1
    - MUT-10 : l'élagage des dossiers *-references neutralisé → fixture SKILL-3 (partie -references) : rc_original=0, rc_mutant=1
  </behavior>
  <action>
<!-- planner-discipline-allow: DEPASSEMENT -->
1. Au démarrage, avant tout commit du plan : LIRE la base de phase consignée par 43-01, dont ce plan dépend, par la chaîne du bloc de contexte (Référence de phase) — rien d'imprimé (fichier absent, malformé ou hors de l'histoire) → arrêt en rouge et retour à l'orchestrateur ; ce plan n'écrit jamais `43-BASE-PHASE.md`. Puis, avant tout commit de code du plan, écrire la base du plan (`git rev-parse HEAD`) sur une ligne dédiée « Base du plan : <sha> » de 43-04-SUMMARY.md, créé dès maintenant (ligne placée hors du frontmatter, en début de ligne, et conservée quand le SUMMARY est complété puis commité en fin de plan, commit `.planning/` séparé) : le critère Gate-Touche du premier commit relit cette ligne. Tests d'abord (SKILL-1 à SKILL-6, MUT-8 à MUT-10), joués ROUGES, puis implémentation. Toutes les tailles dérivent d'une constante locale de la suite `PLAFOND_SKILL=500` (jamais un littéral dispersé), et les fixtures de skills se construisent avec gen_lines.
2. Script (socle repris, pas de réécriture — la découverte D-03 des agents, ses verdicts et la ligne `BILAN :` restent byte-identiques) :
   - constante `VF_SKILL_LINE_CAP=500` déclarée à côté de `VF_BUDGET_LINE_CAP`, jamais partagée avec elle (ADR-029 : skills ≤ 500 ; agents 300) ; aucun seuil d'avertissement pour les skills (ADR-029 n'en définit pas — décision de plan, 43-RESEARCH.md A4) ;
   - seconde découverte après celle des agents : pour chaque dossier `$ROOT/plugin/*/`, si son module.json déclare `"type": "doc-only"` (lu par grep, sans jq, comme check-version-sync.sh), compter ses SKILL.md dans `SKILL_EXCLUDED` et passer (ligne unique, cible MUT-9) ; sinon parcourir récursivement avec `find` sans suivre les liens, en élaguant les dossiers cachés et les dossiers *-references (deux motifs d'élagage, chacun sur sa propre ligne, cible MUT-10 pour le second), fichiers nommés SKILL.md, réguliers ou liens symboliques ; liste triée sous LC_ALL=C ;
   - mesure par fichier : lien symbolique → NON-VERIFIABLE ; `frontmatter_state` ouvert → NON-VERIFIABLE ; sinon `lines_count` (fichier entier) ; au-delà de `VF_SKILL_LINE_CAP` → verdict DEPASSEMENT-SKILL-ADR029 (comparaison `-gt` sur sa propre ligne, cible MUT-8), sinon OK ; compteurs distincts `SKILL_OVERRUN_COUNT` et `SKILL_NONVERIF_COUNT` ;
   - code de sortie, sans nouveau code : `SKILL_NONVERIF_COUNT` rejoint la branche NON-VERIFIABLE (rc 2), `SKILL_OVERRUN_COUNT` rejoint la branche dépassement (rc 1 si armé) ; non armé → 3 comme aujourd'hui ; aucune ligne de baseline n'est exigée pour les SKILL.md (FABR-09 demande le refus au-delà de 500, pas un ratchet d'instructions sur les skills — décision de plan : aucune hausse de baseline pour ce volet) ;
   - rapport, après la ligne `BILAN :` existante : `[check-instruction-budget] corpus skills decouvert : <N> SKILL.md (+ <E> exclu(s) sous un module doc-only)`, un tableau `SKILL | LIGNES | PLAFOND | VERDICT`, puis `BILAN-SKILLS : <N> SKILL.md, <K> depassement(s) du plafond <VF_SKILL_LINE_CAP>, <V> non verifiable(s), <E> exclu(s)` — la chaîne en capitales DEPASSEMENT n'apparaît que dans le verdict d'un dépassement réel ;
   - en-tête : section « SKILL.md (Phase 43, FABR-09, D-Q4) » (corpus, exclusions, plafond, absence de seuil d'avertissement, pourquoi pas de ratchet d'instructions sur les skills).
3. Mutants dans le bloc existant (remplacement de ligne exacte, cmp, `bash -n`) ; chaque ligne visée doit être unique dans le script et ne recopier aucune ligne déjà visée par MUT-1 à MUT-7 (qui restent tous tués).
4. Rejouer la suite, puis le dépôt réel (commande de vérification).
5. Commit : `feat(conductor): plafond ADR-029 des SKILL.md dans le budget d'instructions (FABR-09)` avec
   `Gate-Touche: plugin/conductor/scripts/check-instruction-budget.sh — seconde découverte et plafond des SKILL.md (D-Q4)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-instruction-budget.sh — SKILL-1 à SKILL-6, mutants MUT-8 à MUT-10`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh 2>&1 | grep -E 'SKILL-[1-6]|MUT-(8|9|10)|✗|== resultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes SKILL-1 à SKILL-6 ou MUT-8, MUT-9, MUT-10 n'a aucune ligne « ✓ », ou la ligne « == resultat » ne finit pas par « 0 ko == »</fails_when>
    <automated>out=$(bash plugin/conductor/scripts/check-instruction-budget.sh 2>&1); rc=$?; attendu=0; for d in plugin/*/; do if [ -f "$d/module.json" ] && grep -Eq '"type"[[:space:]]*:[[:space:]]*"doc-only"' "$d/module.json"; then continue; fi; attendu=$((attendu + $(find "$d" -name SKILL.md -type f | wc -l))); done; printf '%s\n' "$out" | grep -q "corpus skills decouvert : $attendu SKILL.md" && echo "REEL rc=$rc skills=$attendu"</automated>
    <fails_when>la ligne imprimée n'est pas « REEL rc=0 skills=<n> » (dépôt réel rouge, ou nombre de SKILL.md découverts différent du compte find hors doc-only)</fails_when>
    <automated>DEBUT=$(sed -n 's/^Base du plan : \([0-9a-f]\{7,40\}\).*$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-04-SUMMARY.md | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; revs=$(git rev-list --reverse "$DEBUT"..HEAD -- plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh) || echo "GIT-ECHEC rev-list"; first=$(printf '%s\n' "$revs" | head -1); test -n "$first" || echo "AUCUN-COMMIT-G2"; chemins=$(git diff-tree --no-commit-id --name-only -r "$first" -- plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh) || echo "GIT-ECHEC diff-tree"; msg=$(git show -s --format=%B "$first") || echo "GIT-ECHEC show"; for p in plugin/conductor/scripts/check-instruction-budget.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh; do printf '%s\n' "$chemins" | grep -qxF "$p" || continue; printf '%s\n' "$msg" | grep -qE "^Gate-Touche: $p (—|-) " || echo "NON-COUVERT $p"; done; echo "PREMIER-COMMIT-G2 $first"</automated>
    <fails_when>une ligne BASE-ABSENTE, GIT-ECHEC, AUCUN-COMMIT-G2 ou NON-COUVERT apparaît avant « PREMIER-COMMIT-G2 <sha> » : base du plan non consignée, ou le PREMIER commit de ce plan qui touche check-instruction-budget.sh ou sa suite (surface G-2) ne porte pas, dans SON message, un trailer Gate-Touche par chemin de la surface qu'il touche. G-2 (43-06) juge la branche entière et ne voit pas ce manquement local.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^VF_SKILL_LINE_CAP=500$' plugin/conductor/scripts/check-instruction-budget.sh` rend 1 et `grep -c '^VF_BUDGET_LINE_CAP=300$' plugin/conductor/scripts/check-instruction-budget.sh` rend 1 (plafond des agents intact)
    - la suite finit sur `0 ko ==` ; les lignes MUT-1 à MUT-7 existantes sont toujours `✓` ; MUT-8, MUT-9, MUT-10 sont `✓`
    - le dépôt réel imprime `REEL rc=0 skills=<n>` avec n égal au compte find hors doc-only
    - `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && d=$(git diff "$B43" HEAD -- .github/workflows/ci.yml) && printf '%s' "$d" | grep -c .` rend 0 (rien d'imprimé — base de phase figée illisible ou hors de l'histoire — vaut échec)
    - le premier commit du plan sur la surface G-2 couvre chaque chemin qu'il touche : la troisième commande du verify imprime `PREMIER-COMMIT-G2 <sha>` sans aucune ligne NON-COUVERT, GIT-ECHEC, AUCUN-COMMIT-G2 ni BASE-ABSENTE
  </acceptance_criteria>
  <done>Le budget couvre les SKILL.md à leurs trois profondeurs, bloque au-delà de 500 lignes quand il est armé, n'ignore aucun fichier en silence, et laisse intacts le contrat des agents et les sept bascules de la CI.</done>
</task>

<task type="auto">
  <name>Tâche 2 : consigner la décision bootstrap déjà tranchée — option ratchet-socle — et la mesure du jour du socle, dans 43-04-SUMMARY.md</name>
  <files>.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-04-SUMMARY.md</files>
  <read_first>
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md (bloc « Quatrième temps (2026-09-26) », Q1 : décision, motifs, citation)
    - plugin/reference/content/methodology/templates/skills/agent-density-auditor/references/thresholds.md (l.11, l.17, l.45-52 : définition du bootstrap)
    - .planning/BACKLOG.md (section « Ramener le socle du bootstrap sous 2 000 tokens (ADR-029) — DIFFÉRÉ (2026-09-26) », commit 433fea0 : composition mesurée du socle — lecture seule)
    - scripts/check-baseline-arbitrage.sh (l.167-192 : citation_ok, forme de citation lue par G-1)
  </read_first>
  <action>
Cette tâche n'est PAS un checkpoint : la question a été tranchée avant l'exécution (bloc de contexte « Décision bootstrap (Q1) »). Ne pas la reposer, ne présenter aucune autre option, ne rien recommander.
1. Rejouer, en lecture seule, la mesure du socle avec la définition et l'estimation de ce plan : bootstrap = somme des métadonnées name + description + when_to_use (thresholds.md l.11 et l.47-52 ; mécanisme natif relu le 2026-09-25 sur https://code.claude.com/docs/en/skills : les descriptions des skills installés sont chargées en contexte à chaque session) ; estimation = octets des lignes de ces clés sous LC_ALL=C, divisés par 4. Corpus = modules rendus par `VF_MODULES_ROOT=plugin bash plugin/_internal/resolve-deps.sh conductor`, plus le skill désigné par le champ `skills` de `plugin/.claude-plugin/plugin.json`, plus `plugin/commands/*.md`. Ventiler par source, sur le modèle de la table du BACKLOG (433fea0).
2. Consigner dans 43-04-SUMMARY.md (créé à la Tâche 1), sous un titre « Décision bootstrap (Q1) » :
   - la réponse, verbatim : « ratchet-socle » ;
   - la citation, mot pour mot et sur une seule ligne : « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » ;
   - les trois motifs, recopiés tels quels du bloc de contexte (couverture réelle du bootstrap choisie par Willy en Q4, qui écarte l'avertissement seul ; plafond dur qui bloquerait la Phase 43 sur des modules de Samuel ; ratchet sur tout le corpus distribué qui viserait un lab hypothétique) ;
   - l'application retenue : la mesure du jour devient la ligne `@bootstrap:socle` ; toute hausse bloque (DEPASSEMENT-BOOTSTRAP) ; au-dessus de 2000 sans hausse sur la ligne, AU-DESSUS-PLAFOND-ADR029 publié, non bloquant ; le plafond ADR-029 reste un objectif, retour sous 2000 au BACKLOG (433fea0) ;
   - la mesure du jour (tokens, octets, nombre de fichiers, ventilation par source) à côté de la mesure de planification (≈ 2 499 tokens, 9 997 octets, 2026-09-25) ; un écart s'explique par source, jamais par un corpus ou une méthode ajustés.
3. Aucune écriture dans `.planning/instruction-budget-baselines.tsv` dans cette tâche : la ligne se pose à la Tâche 3, après l'implémentation de la métrique, avec la valeur que rend le script.
  </action>
  <verify>
    <automated>S=$(ls .planning/workstreams/gouvernance/phases/VFDO-43-*/43-04-SUMMARY.md) || echo "SUMMARY-ABSENT"; for m in 'ratchet-socle' 'décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26' 'couvrir réellement le bootstrap (Q4)' 'un plafond dur bloquerait la Phase 43 sur des modules qui appartiennent à Samuel' 'viserait un lab hypothétique, pas le socle réel que tout lab porte' '@bootstrap:socle' 'AU-DESSUS-PLAFOND-ADR029'; do grep -qF -- "$m" "$S" || echo "MANQUE $m"; done; DEBUT=$(sed -n 's/^Base du plan : \([0-9a-f]\{7,40\}\).*$/\1/p' "$S" | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; git diff --quiet "$DEBUT" -- .planning/instruction-budget-baselines.tsv || echo "BASELINE-TOUCHEE"; echo "DECISION-Q1-FIN"</automated>
    <fails_when>une ligne SUMMARY-ABSENT, MANQUE, BASE-ABSENTE ou BASELINE-TOUCHEE apparaît avant « DECISION-Q1-FIN » : décision, citation exacte, motif, clé de baseline ou jeton non bloquant absents du SUMMARY, base du plan non consignée, ou baseline modifiée (commitée ou non) depuis la base du plan avant la Tâche 3</fails_when>
  </verify>
  <acceptance_criteria>
    - 43-04-SUMMARY.md porte, sous « Décision bootstrap (Q1) », la réponse « ratchet-socle », la citation mot pour mot sur une seule ligne, les trois motifs, l'application (`@bootstrap:socle`, DEPASSEMENT-BOOTSTRAP bloquant, AU-DESSUS-PLAFOND-ADR029 non bloquant) et les deux mesures (jour et planification) : la commande du verify imprime `DECISION-Q1-FIN` seule
    - aucune écriture dans .planning/instruction-budget-baselines.tsv dans cette tâche
  </acceptance_criteria>
  <done>La décision bootstrap est consignée telle que Willy l'a déléguée, avec sa citation exacte, ses motifs et la mesure du jour, sans qu'aucune autre option ne soit présentée ni rouverte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : métrique bootstrap ratchet-socle — ligne `@bootstrap:socle` bloquante, plafond ADR-029 signalé non bloquant (BOOT-1 à BOOT-5, MUT-11), ligne de baseline citée et signalée à la revue de Samuel</name>
  <reversibility rating="costly">La ligne `@bootstrap:socle` s'ajoute à une baseline CODEOWNERS lue par G-1 et par la CI : la relever exige une nouvelle décision citée et la revue de Samuel, la baisser reste libre (doctrine du ratchet). La décision elle-même est déjà prise et consignée (Tâche 2) ; le caractère one-way de D-Q4 (réserve du BACKLOG levée, 2026-09-24) est antérieur et inchangé.</reversibility>
  <precondition>command -v jq réussit (plugin/_internal/resolve-deps.sh l'exige, y compris dans les fixtures BOOT-1 et BOOT-2) ; 43-04-SUMMARY.md porte la décision « ratchet-socle » et sa citation (Tâche 2, `DECISION-Q1-FIN` sans ligne MANQUE)</precondition>
  <files>plugin/conductor/scripts/check-instruction-budget.sh, plugin/conductor/scripts/tests/test-check-instruction-budget.sh, .planning/instruction-budget-baselines.tsv</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (fichier modifié : seconde découverte et BILAN-SKILLS de la Tâche 1 ; lecture de la baseline, entrées orphelines, contrat de baseline l.112-140 et l.330-355)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (fichier modifié)
    - .planning/instruction-budget-baselines.tsv (en-tête commenté, forme chemin/lignes/instructions, doctrine du ratchet)
    - plugin/_internal/resolve-deps.sh (l.1-30 : VF_MODULES_ROOT, jq requis, sortie triée) ; plugin/.claude-plugin/plugin.json (champ skills) ; plugin/commands/ (frontmatter description)
    - scripts/check-baseline-arbitrage.sh (l.167-192 : citation_ok — mot-clé « décision » accepté, deux virgules, date ISO, sur une seule ligne)
    - 43-04-SUMMARY (décision « ratchet-socle », citation et mesure du jour consignées à la Tâche 2)
  </read_first>
  <behavior>
    - BOOT-1 : fixture avec socle — `plugin/conductor/module.json` requérant `dep`, `plugin/dep/module.json`, un SKILL.md sous chacun, `plugin/.claude-plugin/plugin.json` exposant `./installer`, `plugin/installer/SKILL.md`, `plugin/commands/c.md`, `plugin/_internal/resolve-deps.sh` copié du dépôt — → la ligne BOOTSTRAP porte exactement le nombre de tokens attendu, calculé par la suite INDÉPENDAMMENT du script (octets des lignes qu'elle a écrites, LC_ALL=C, ÷ 4)
    - BOOT-2 : ligne `@bootstrap:socle` égale à la mesure → rc 0 ; description allongée de 8 octets, ratchet armé → rc 1, DEPASSEMENT-BOOTSTRAP ; même croissance sans sentinelle → rc 3, rapport imprimé (contrat existant du ratchet non armé : jamais rc 0) ; ligne absente et ratchet armé → rc 2 ; la ligne `@bootstrap:socle` n'est jamais comptée comme orpheline
    - BOOT-3 : fixture sans `plugin/conductor/module.json` (cas des sept bascules de la CI) → « BOOTSTRAP : non applicable », rc et verdicts identiques à la Tâche 1
    - BOOT-4 : socle présent mais `plugin/_internal/resolve-deps.sh` absent → NON-VERIFIABLE avec la cause, rc 2
    - BOOT-5 : mesure > 2000 et ≤ ligne `@bootstrap:socle`, ratchet armé → rc 0, verdict AU-DESSUS-PLAFOND-ADR029 publié, aucune occurrence de la chaîne en capitales DEPASSEMENT (le plafond ADR-029 est un objectif, non bloquant) ; mesure ≤ 2000 et ≤ ligne → verdict OK
    - MUT-11 : la comparaison de la mesure à la ligne `@bootstrap:socle` neutralisée → fixture de croissance de BOOT-2 : rc_original=1, rc_mutant=0
  </behavior>
  <action>
<!-- planner-discipline-allow: DEPASSEMENT -->
Option unique : ratchet-socle (décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 — bloc de contexte, consignée à la Tâche 2). Aucune autre branche n'est implémentée.
1. Tests d'abord (BOOT-1 à BOOT-5, MUT-11), joués ROUGES, puis implémentation.
2. Constante `VF_BOOTSTRAP_TOKEN_CAP=2000` (ADR-029, doctrine du dépôt, jamais dans le manifeste daté). Mesure : pour chaque SKILL.md du corpus, somme des octets (LC_ALL=C, longueur de ligne + 1) des lignes de frontmatter appartenant aux clés name, description et when_to_use, lignes de continuation comprises, avec le même ancrage NR==1 que frontmatter_state ; pour chaque `plugin/commands/*.md`, mêmes règles sur description et when_to_use ; tokens = octets ÷ 4 (division entière). En-tête : définition, source (thresholds.md, doc officielle relue), pourquoi des octets et pas des caractères, et la décision ratchet-socle avec sa citation et ses trois motifs.
3. Corpus socle : modules rendus par `VF_MODULES_ROOT="$ROOT/plugin" bash "$ROOT/plugin/_internal/resolve-deps.sh" conductor` (le résolveur canonique de l'installeur, jamais un second parseur de requires), plus le skill désigné par le champ `skills` de `$ROOT/plugin/.claude-plugin/plugin.json`, plus `$ROOT/plugin/commands/*.md`. Sans `$ROOT/plugin/conductor/module.json` → « BOOTSTRAP : non applicable (aucun socle conductor sous --path) », aucun effet sur le rc. Socle présent mais résolveur absent ou en échec (jq manquant compris) → NON-VERIFIABLE avec la cause, rc 2.
4. Ligne de rapport unique après BILAN-SKILLS : `BOOTSTRAP : <tokens> tokens estimes (octets/4) sur socle (<n> fichiers), plafond <VF_BOOTSTRAP_TOKEN_CAP>, ligne <valeur de @bootstrap:socle, ou « absente »>, verdict <V>`.
5. Clé de baseline `@bootstrap:socle`, colonnes lignes = 0 et instructions = tokens (forme acceptée par G-1 et par le parseur existant) ; elle est retirée du calcul des entrées orphelines et lue pour le verdict. Verdicts, chacun sur sa propre ligne de comparaison (cible MUT-11 : tokens > ligne) :
   - tokens > ligne → DEPASSEMENT-BOOTSTRAP, compté comme dépassement (SKILL_OVERRUN_COUNT ou un compteur dédié rejoignant la même branche) : rc 1 sous ratchet armé, rc 3 non armé (contrat existant) — jamais rc 0, jamais un simple avertissement ;
   - tokens ≤ ligne et > VF_BOOTSTRAP_TOKEN_CAP → AU-DESSUS-PLAFOND-ADR029, publié, non bloquant, rc inchangé (le plafond ADR-029 reste un objectif ; retour sous 2000 au BACKLOG, 433fea0) ;
   - tokens ≤ plafond → OK ;
   - ratchet armé et clé absente de la baseline → contrat de baseline incomplet, rc 2.
6. Commit du code : `feat(conductor): métrique bootstrap ratchet-socle du budget d'instructions (FABR-09)` avec
   `Gate-Touche: plugin/conductor/scripts/check-instruction-budget.sh — métrique bootstrap, ligne @bootstrap:socle bloquante (ratchet-socle)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-instruction-budget.sh — BOOT-1 à BOOT-5, mutant MUT-11`.
7. Ligne de baseline, dans un commit SÉPARÉ et postérieur : ajouter à `.planning/instruction-budget-baselines.tsv` la ligne `@bootstrap:socle`, `0`, `<tokens>` — tokens = la valeur que rend la ligne BOOTSTRAP du script sur le dépôt réel à cet instant (mesure du jour, jamais la mesure de planification recopiée ni une valeur arrondie ou majorée), à sa place dans le tri LC_ALL=C — plus une ligne de commentaire d'en-tête qui explique la clé `@bootstrap:` et cite la décision ; aucune autre ligne ajoutée, modifiée ou retirée. Message : `fix(baseline): pose la ligne @bootstrap:socle du budget d'instructions (FABR-09, ratchet-socle)`, dont le corps porte, SEULE sur sa ligne et jamais coupée, la citation « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » (forme reconnue par citation_ok de G-1 : mot-clé « décision », deux virgules, date ISO), puis une ligne « Chemin CODEOWNERS : ajout signalé à la revue code owner de Samuel ». Jamais une décision de Claude présentée comme un arbitrage.
8. Signalement à Samuel : dans 43-04-SUMMARY.md, section « Signalement à la revue de Samuel », consigner le hash du commit de baseline, la ligne ajoutée, la citation, et le fait que la PR exige la revue code owner de Samuel ou un contournement tracé (chemin CODEOWNERS, CLAUDE.local.md) ; y nommer la limite : G-1 ne voit pas une ligne AJOUTÉE (seules les lignes présentes à la base et à HEAD sont classées) — sa garde porte sur les hausses FUTURES de la ligne. Consigner aussi la mesure de la ligne BOOTSTRAP et son écart éventuel avec la mesure de la Tâche 2, expliqué par source.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh 2>&1 | grep -E 'SKILL-[1-6]|BOOT-[1-5]|MUT-([1-9]|1[01]) |✗|== resultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes BOOT-1 à BOOT-5 n'a aucune ligne « ✓ », ou « ✓ MUT-11 » manque, ou la ligne « == resultat » ne finit pas par « 0 ko == »</fails_when>
    <automated>out=$(bash plugin/conductor/scripts/check-instruction-budget.sh 2>&1); rc=$?; t=$(printf '%s\n' "$out" | sed -n 's/^BOOTSTRAP : \([0-9][0-9]*\) tokens.*$/\1/p' | head -1); v=$(awk -F'\t' '$1=="@bootstrap:socle"{print $3}' .planning/instruction-budget-baselines.tsv); printf '%s\n' "$out" | grep -E '^BOOTSTRAP : '; echo "REEL-BOOT rc=$rc mesure=$t ligne=$v"</automated>
    <fails_when>la dernière ligne n'est pas « REEL-BOOT rc=0 mesure=<n> ligne=<n> » avec deux valeurs non vides et égales, ou la ligne BOOTSTRAP imprimée ne finit pas par « verdict AU-DESSUS-PLAFOND-ADR029 » ou « verdict OK »</fails_when>
    <automated>S=$(ls .planning/workstreams/gouvernance/phases/VFDO-43-*/43-04-SUMMARY.md); DEBUT=$(sed -n 's/^Base du plan : \([0-9a-f]\{7,40\}\).*$/\1/p' "$S" | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; git fetch -q origin main 2>/dev/null; bash scripts/check-baseline-arbitrage.sh; echo "G1 rc=$?"; c=$(git log --format=%H -1 "$DEBUT"..HEAD -- .planning/instruction-budget-baselines.tsv); test -n "$c" || echo "AUCUN-COMMIT-BASELINE"; msg=$(git show -s --format=%B "$c") || echo "GIT-ECHEC show"; printf '%s\n' "$msg" | grep -qxF 'décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26' || echo "CITATION-ABSENTE"; d=$(git diff -U0 "$DEBUT" HEAD -- .planning/instruction-budget-baselines.tsv) || echo "GIT-ECHEC diff"; printf '%s\n' "$d" | grep -E '^-[^-]' && echo "LIGNE-RETIREE-OU-MODIFIEE"; n=$(printf '%s\n' "$d" | grep -E '^\+[^+#]' | grep -c .); a=$(printf '%s\n' "$d" | grep -cE '^\+@bootstrap:socle	0	[0-9]+$'); echo "BASELINE-DIFF ajoutees=$n bootstrap=$a"</automated>
    <fails_when>une ligne BASE-ABSENTE, AUCUN-COMMIT-BASELINE, GIT-ECHEC, CITATION-ABSENTE ou LIGNE-RETIREE-OU-MODIFIEE apparaît ; ou « G1 rc=… » ne vaut ni 0 ni 3 ; ou la dernière ligne n'est pas exactement « BASELINE-DIFF ajoutees=1 bootstrap=1 » (une seule ligne de données ajoutée depuis la base du plan, et c'est `@bootstrap:socle`)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^@bootstrap:' .planning/instruction-budget-baselines.tsv` rend 1 et `grep -cE '^@bootstrap:socle	0	[0-9]+$' .planning/instruction-budget-baselines.tsv` rend 1
    - le commit de baseline porte la citation mot pour mot, seule sur sa ligne (`CITATION-ABSENTE` jamais imprimé), et c'est le dernier commit de la plage du plan qui touche la baseline ; aucune autre valeur de la baseline n'a bougé (`BASELINE-DIFF ajoutees=1 bootstrap=1`, aucune ligne LIGNE-RETIREE-OU-MODIFIEE) ; G-1 rend 0 ou 3
    - le dépôt réel rend `REEL-BOOT rc=0 mesure=<n> ligne=<n>` (mesure égale à la ligne) avec le verdict AU-DESSUS-PLAFOND-ADR029 ou OK ; la suite finit sur `0 ko ==` avec MUT-1 à MUT-11 tués
    - 43-04-SUMMARY.md contient la section « Signalement à la revue de Samuel » avec le hash du commit de baseline, la ligne ajoutée, la citation et la limite nommée de G-1
  </acceptance_criteria>
  <done>Le bootstrap est mesuré sur le socle et borné par sa ligne de baseline exactement comme la décision ratchet-socle le prévoit : toute hausse bloque, le plafond ADR-029 est signalé sans bloquer, la ligne porte sa citation et son ajout est signalé à Samuel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus distribué → mesure | fichiers du dépôt, liens symboliques et frontmatters cassés possibles |
| baseline CODEOWNERS → verdict | la ligne de baseline décide du rouge ; une PR peut l'élever |
| décision humaine → commit | l'arbitrage n'existe que par sa citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-30 | Repudiation | ligne de baseline ajoutée | high | mitigate | décision ratchet-socle consignée à la Tâche 2 avant toute écriture ; citation « décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 » mot pour mot, seule sur sa ligne, dans le commit même (vérifiée par la Tâche 3) ; seule la ligne `@bootstrap:socle` ajoutée (BASELINE-DIFF) ; ajout signalé à la revue code owner de Samuel (CODEOWNERS) ; limite nommée : G-1 ne voit pas une ligne ajoutée, seulement ses hausses futures |
| T-43-31 | Tampering | corpus ou estimation du bootstrap | medium | mitigate | définition et estimation fixées par le plan et testées indépendamment (BOOT-1) ; option ratchet-socle consignée ; ligne égale à la mesure rendue par le script (REEL-BOOT mesure=ligne) ; prohibition relue en PR |
| T-43-32 | Spoofing (faux vert) | découverte des SKILL.md | medium | mitigate | compte découvert comparé au compte find hors doc-only sur le dépôt réel ; exclusions imprimées ; symlinks et frontmatters ouverts en NON-VERIFIABLE (SKILL-4) |
| T-43-33 | Denial of Service | étape CI check-instruction-budget | medium | mitigate | aucune modification de ci.yml ; SKILL-5, SKILL-6 et BOOT-3 rejouent les cas 2/7 et 7/7 sur fixture sans SKILL.md ni socle |
| T-43-34 | Denial of Service | dépendance jq du résolveur | low | mitigate | socle présent + résolveur en échec → NON-VERIFIABLE explicite (BOOT-4), jamais un zéro silencieux |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` : 0 ko, MUT-1 à MUT-11 tués.
- `bash plugin/conductor/scripts/check-instruction-budget.sh` sur le dépôt réel : rc 0, `corpus skills decouvert : <n>` égal au compte find hors doc-only, ligne BOOTSTRAP égale à la ligne `@bootstrap:socle`, verdict AU-DESSUS-PLAFOND-ADR029 ou OK.
- `bash scripts/check-baseline-arbitrage.sh` : rc 0 ou 3 ; commit de baseline porteur de la citation exacte ; `BASELINE-DIFF ajoutees=1 bootstrap=1`.
</verification>

<success_criteria>
- FABR-09 : SKILL.md au-delà de 500 lignes refusé ; toute hausse du socle du bootstrap au-dessus de sa ligne `@bootstrap:socle` refusée (rc 1 sous ratchet armé) ; plafond ADR-029 de 2000 signalé (AU-DESSUS-PLAFOND-ADR029), non bloquant ; ligne de baseline posée avec la citation de la décision déléguée du 2026-09-26 et signalée à la revue de Samuel.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| `VF_SKILL_LINE_CAP`, `SKILL_OVERRUN_COUNT`, `SKILL_NONVERIF_COUNT`, `SKILL_EXCLUDED` | constantes et compteurs du budget |
| verdict `DEPASSEMENT-SKILL-ADR029`, lignes `corpus skills decouvert`, `BILAN-SKILLS` | rapport |
| `VF_BOOTSTRAP_TOKEN_CAP`, ligne `BOOTSTRAP :`, verdicts `DEPASSEMENT-BOOTSTRAP` (bloquant) et `AU-DESSUS-PLAFOND-ADR029` (non bloquant) | métrique bootstrap (ratchet-socle) |
| clé de baseline `@bootstrap:socle` | ligne de `.planning/instruction-budget-baselines.tsv`, citée, signalée à la revue de Samuel |
| cas `SKILL-1` à `SKILL-6`, `BOOT-1` à `BOOT-5`, mutants `MUT-8` à `MUT-11` | suite du budget |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-09 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici : 500 exactement vs 501 (SKILL-1), trois profondeurs (SKILL-2), exclusions doc-only / -references / cachés (SKILL-3), frontmatter ouvert et lien symbolique (SKILL-4), fixture sans SKILL.md (SKILL-5, SKILL-6), fixture sans socle (BOOT-3), résolveur en échec (BOOT-4).
- **Corpus et mode du bootstrap = tranchés (révision du 2026-09-26, tour 4)** : option ratchet-socle, décision déléguée par Willy au head (/vf-decide), AskUserQuestion session principale, 2026-09-26 ; la Tâche 2 la consigne, le plan l'applique et ne la rouvre pas ; la mesure du planificateur (≈ 2 499) est une information, la ligne prend la mesure du jour rendue par le script.
- **Estimation octets ÷ 4** : ordre de grandeur stable, pas un tokenizer (le dépôt a écarté une dépendance tokenizer, `.planning/research/FEATURES.md` l.79-82, ADR-054) ; les descriptions accentuées sont légèrement surestimées (octets > caractères) — biais prudent.
- **Pas de ratchet d'instructions sur les SKILL.md** : FABR-09 exige le refus au-delà de 500 lignes ; ajouter 21 lignes de baseline serait une hausse de baseline sans exigence — décision de plan.
- **Descriptions tronquées à 1 536 caractères** par le runtime (doc skills) : non gaté ici.
- **Ordre avec 43-01 (révision du 2026-09-26, tour 3)** : ce plan dépend de 43-01 et passe de la vague 1 à la vague 2 (avec 43-03 et 43-05, sans chevauchement de `files_modified`), pour que la base de phase, dont 43-01 est l'unique propriétaire, précède tout commit de ce plan quel que soit le mode d'exécution. Le nombre de vagues de la phase est inchangé (quatre) et ce plan reste hors du chemin critique (43-06, en vague 4, l'attend déjà). Décision de planification, pas un arbitrage humain ; la ROADMAP du compartiment et le DAG de mission, qui citent encore 43-04 en vague 1, sont à resynchroniser par leur mainteneur.
- **Le fichier de baseline est partagé avec le compartiment fiabilite** : au rebase après un merge de fiabilite, re-mesurer ; toute hausse de la ligne `@bootstrap:socle` exige une nouvelle décision citée.
- **Autonomie (révision du 2026-09-26, tour 4)** : plus aucun checkpoint dans ce plan (`autonomous: true`) ; la revue code owner de Samuel sur la baseline se joue en PR, pas dans l'exécution. La ROADMAP du compartiment et le DAG de mission, qui décrivent encore un checkpoint humain dans 43-04, sont à resynchroniser par leur mainteneur.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-04-SUMMARY.md` when done — décision bootstrap consignée (« ratchet-socle », citation mot pour mot, trois motifs), mesures du jour, section « Signalement à la revue de Samuel » ; commit `.planning/` séparé.
</output>

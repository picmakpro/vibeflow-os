---
phase: 21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0
plan: 05
type: execute
wave: 1 (dernière — dépend de 21-01..21-04)
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified:
  - README.md
  - README.fr.md
  - .planning/WINDOWS.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/dev-orchestrator/README.md
  - plugin/planning-core/VERSION
  - plugin/planning-core/module.json
  - plugin/planning-core/CHANGELOG.md
  - plugin/planning-core/README.md
  - .planning/ROADMAP.md
  - .github/workflows/ci.yml
  - docs/ADR.md
  - plugin/conductor/references/team-kernel.md
  - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
  - .planning/STATE.md
  - VERSION
  - plugin/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - CHANGELOG.md
autonomous: true
requirements: ["Changement 1..6 (traçabilité, pas d'ID REQ- formel — cf. §Requirements ROADMAP)", "clause release G"]

must_haves:
  truths:
    - "Correctif 1 (BLOQUANT) : le compteur « N suites » des 2 README racine dit 45 (réel), le registre WINDOWS.md porte une entrée nommée pour la régression 44→45 et son statut reflète qu'elle est refermée par ce plan — `scripts/check-version-sync.sh` sort en rc=0."
    - "Correctif 2 : `dev-orchestrator` (v2.9.0, changement de contrat d'agent) et `planning-core` (v2.5.3, correctif) sont bumpés avec triade cohérente et entrée CHANGELOG datée décrivant le LIVRÉ ; `conductor` v1.18.0 vérifié cohérent, non re-bumpé sans raison."
    - "Correctif 3 : `.planning/ROADMAP.md` §Phase 21 liste les 5 plans réels (21-01..21-05) avec case cochée et compte juste ; la ligne Requirements applique la même convention que celle déjà écrite pour la Phase 20 (SC/IDs de traçabilité ad hoc, aucun ID `REQ-` formel — le ledger `.planning/REQUIREMENTS.md` ne descend pas sous la Phase 14, constaté et non inventé)."
    - "W1 : `plugin/conductor/scripts/check-state-integrity.sh` est appelé par le job `gates` de `.github/workflows/ci.yml`, dans la même famille que les autres steps de gate (assertion, pas juste une exécution silencieuse)."
    - "W2 : `docs/ADR.md` ADR-063 dit « 25 cas » (réel, vérifié par comptage), plus « 23 cas »."
    - "W3 : `plugin/conductor/references/team-kernel.md` porte, à côté de la ligne « Dispatch nommé », une trace écrite du recoupement #1995/#2608 vérifié conforme sur `gsd-worktree-path-guard.js` — pas seulement dans un SUMMARY de phase."
    - "W4 : le message d'aide du rc=3 « pas de ligne tools: exploitable » de `inject-mcp-tools.sh --verify` en mode fichier unique nomme la cause la plus fréquente (agent réécrit par l'amont, `vf-mcp-consumer` effacé) et le remède (`--force`)."
    - "STATE.md (point hérité) : `completed_phases` 12→13, `total_plans`/`completed_plans` +1/+1, `status` de la Phase 21 devient `complete` — comptes édités À LA MAIN, jamais via `gsd-tools state`, `check-state-integrity.sh` reste vert avant et après."
    - "Préparation de release v2.45.0 (minor — nouvelle capacité `check-state-integrity.sh`) : triade racine + 2 historiques README + CHANGELOG racine préparés en commits locaux ; AUCUN tag, AUCUN merge, AUCUN push — `scripts/check-version-sync.sh` passe au vert sur la branche AVANT tout geste de release humain."
  artifacts:
    - README.md
    - README.fr.md
    - .planning/WINDOWS.md
    - plugin/dev-orchestrator/CHANGELOG.md
    - plugin/planning-core/CHANGELOG.md
    - .planning/ROADMAP.md
    - .github/workflows/ci.yml
    - docs/ADR.md
    - plugin/conductor/references/team-kernel.md
    - .planning/STATE.md
    - CHANGELOG.md
  key_links:
    - "correctif 1 → scripts/check-version-sync.sh : le seul gate qui matérialise le rouge du jour ; sa sortie rc=0 est la preuve d'achèvement de ce correctif, pas une lecture."
    - "correctif 2 → scripts/check-version-sync.sh (triade par module) : même gate, contrôle différent (VERSION ↔ module.json), doit rester vert après le bump."
    - "W1 → .github/workflows/ci.yml job gates : câblage, pas réécriture du script lui-même (aucune modification de check-state-integrity.sh dans ce plan)."
    - "release v2.45.0 → §Hors périmètre ci-dessous : les fichiers de release ne sont QUE préparés (commit local), le tag et le push restent un geste humain distinct, hors de ce plan."
  prohibitions:
    - "P-01 — MUST NOT créer, ni pousser, aucun tag git. MUST NOT merger, ni pousser aucune branche."
    - "P-02 — MUST NOT invoquer `gsd-tools state ...` ni aucun script qui le fasse : reconstruit le frontmatter de STATE.md depuis le disque et régresse les compteurs en silence (prouvé le 2026-07-31). STATE.md s'édite à la main."
    - "P-03 — MUST NOT toucher aux Phases 22, 23, 24, 25 (fichiers, dossiers, sections ROADMAP)."
    - "P-04 — MUST NOT réécrire ADR-051/060/061/062/063 au-delà du point ciblé (W2 : un seul chiffre dans ADR-063)."
    - "P-05 — MUST NOT affaiblir ni neutraliser un test existant pour verdir un gate rouge ; un gate qui reste rouge après un correctif légitime est un `gaps_found` à remonter, pas un test à modifier."
    - "P-06 — MUST NOT inventer un ID `REQ-` pour la Phase 21 si `.planning/REQUIREMENTS.md` n'en a aucun : constater l'absence et appliquer la même convention que la Phase 20 (SC/IDs ad hoc comme IDs de traçabilité), jamais une extrapolation silencieuse."
---

<objective>
Fermer la Phase 21 par sa gouvernance, sur le patron exact de 20-07 : remettre la CI au vert
aujourd'hui (pas seulement à la release), bumper les 2 modules réellement modifiés par la phase,
recaler le ROADMAP sur l'état réel des 5 plans, câbler et corriger les 4 avertissements non
bloquants remontés par la vérification fraîche (21-VERIFICATION.md, PASS PARTIEL 7/8), recaler
STATE.md à la main, et préparer — sans la publier — la release racine v2.45.0.

Output : CI verte sur cette branche, 2 modules bumpés, ROADMAP exact, 4 warnings traités, STATE.md
recalé, triade de release + CHANGELOG + 2 historiques README préparés en commits locaux.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0/21-VERIFICATION.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-07-PLAN.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-07-SUMMARY.md
@CLAUDE.md
</context>

## Périmètre (fiche de lecture directe)

| Rubrique | Valeur |
|---|---|
| **Origine** | `21-VERIFICATION.md`, verdict PASS PARTIEL 7/8 : les 6 Changements + le point hérité STATE.md sont VERIFIED ; la couche de clôture manque (CI rouge, modules non bumpés, ROADMAP périmé, 4 warnings) |
| **Dépendances amont** | 21-01, 21-02, 21-03, 21-04 (tous exécutés — ce plan lit leur livré, ne le réécrit pas) |
| **Dépendances aval** | aucune dans la phase ; hors phase : merge + tag humain de v2.45.0 |
| **Risque** | faible sur le geste (doctrine, versions, doc), élevé sur l'exactitude (chaque chiffre est vérifié par comptage réel, jamais recopié du cadrage) |
| **Réversibilité** | reversible — aucun tag, aucun push, aucune destruction |

<tasks>

<task type="auto">
  <name>Task 1 : CI rouge aujourd'hui — compteur de suites 44→45, ledger WINDOWS en accord (Correctif 1, BLOQUANT)</name>
  <reversibility rating="reversible">Texte de doc + entrée de registre.</reversibility>
  <read_first>
    - `README.md` lignes ~165, ~235 et `README.fr.md` lignes ~170, ~240 — les 2 occurrences de « 44 suites » dans chaque fichier.
    - `.planning/WINDOWS.md` en entier — format double (table Markdown + bloc JSON miroir), l'entrée #2 (fixée sur 44 le 2026-07-31T15:32) et son schéma de champs.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` — recompter, ne jamais recopier le chiffre de la vérification sans revérifier au moment de l'exécution (le compteur peut encore avoir bougé).
  </read_first>
  <files>README.md, README.fr.md, .planning/WINDOWS.md</files>
  <behavior>
    - Les 4 occurrences de « 44 suites » (2 par README) deviennent le compte RÉEL revérifié.
    - `.planning/WINDOWS.md` porte une entrée nommée pour cette régression précise (44→45 après la clôture de #2), `open_count` et `fixed_count`/`total_count` recalculés à la main selon le même schéma que les entrées existantes.
    - `scripts/check-version-sync.sh` ne signale plus aucun contrôle rouge sur le compteur de suites.
  </behavior>
  <action>
Recompter `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` au moment de l'exécution
(ne pas supposer 45 acquis du seul rapport de vérification, qui date d'avant ce plan). Remplacer
les 4 occurrences de « 44 suites » par le compte réel dans `README.md` et `README.fr.md`.

Ajouter à `.planning/WINDOWS.md` une entrée nouvelle (id suivant, phase 21, kind `deviation`,
fichier `README.md`) décrivant que le compteur a régressé après la clôture de WINDOWS #2 (le plan
21-04 a ajouté `test-check-state-integrity.sh`, ce qui a fait bouger le total repo-entier sans que
personne ne repasse par le ledger) — statut `fixed` puisque ce correctif la ferme dans le même
commit, `recorded_at` = horodatage de la vérification qui l'a détectée, `resolved_at` = maintenant.
Mettre à jour les compteurs d'en-tête (`fixed_count`, `total_count`) et le bloc JSON miroir à
l'identique de la table Markdown — les deux représentations doivent rester synchrones.
  </action>
  <verify>
    <automated>real=$(find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l | tr -d ' '); grep -c "$real suites" README.md; grep -c "$real suites" README.fr.md; bash scripts/check-version-sync.sh 2>&1 | grep -i "suites"</automated>
  </verify>
  <acceptance_criteria>
    - Les 2 README affichent chacun 2 occurrences du compte réel, zéro occurrence résiduelle de l'ancien chiffre.
    - `.planning/WINDOWS.md` : nouvelle entrée présente, `open_count` inchangé (toujours seulement #3 ouverte), `fixed_count`/`total_count` incrémentés, table et JSON cohérents.
    - `scripts/check-version-sync.sh` ne liste plus le compteur de suites parmi ses `✗`.
  </acceptance_criteria>
  <done>Les 2 README disent le compte réel, le ledger WINDOWS porte la régression et sa clôture, le gate ne rougit plus sur ce point.</done>
</task>

<task type="auto">
  <name>Task 2 : modules bumpés — dev-orchestrator v2.9.0, planning-core v2.5.3, conductor vérifié (Correctif 2)</name>
  <reversibility rating="reversible">Numéros de version + entrées de journal.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/VERSION`, `module.json`, `CHANGELOG.md` (dernière entrée, gabarit exact), `README.md` (en-tête Version) — valeurs COURANTES, pas celles du cadrage.
    - `plugin/planning-core/` idem.
    - `plugin/conductor/VERSION`/`module.json`/`README.md` — vérifier la cohérence (déjà bumpé par 21-04), ne pas la re-toucher si cohérente.
    - `git diff c01f813..HEAD -- plugin/dev-orchestrator/ plugin/planning-core/` — la liste réelle des fichiers touchés par la phase, pour écrire des entrées de CHANGELOG qui décrivent le LIVRÉ (8 fichiers dev-orchestrator : vf-coder.md, vf-dev-manager.md, gsd-skills-index.md, mission-contracts.md, 3 scripts de version, inject-mcp-tools.sh, 2 suites de test, README ; 1 fichier planning-core : detect-gsd-engine.sh).
    - `CLAUDE.md` racine — règle de numérotation (nouvelle capacité = mineur, correctif/durcissement = correctif).
  </read_first>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md, plugin/dev-orchestrator/README.md, plugin/planning-core/VERSION, plugin/planning-core/module.json, plugin/planning-core/CHANGELOG.md, plugin/planning-core/README.md</files>
  <behavior>
    - `dev-orchestrator` : v2.8.0 → v2.9.0 (mineur — nouvelles capacités : union de scope MCP global, contrat `estimate:`/`actuals:` relayé par `vf-coder`/`vf-dev-manager`, purge dette 1.8.0).
    - `planning-core` : v2.5.2 → v2.5.3 (correctif — seul `detect-gsd-engine.sh` touché, purge dette de version).
    - Triade VERSION/module.json/en-tête README cohérente pour les 2 modules.
    - `conductor` vérifié cohérent (v1.18.0 partout) — AUCUNE modification si déjà cohérent.
  </behavior>
  <action>
Vérifier d'abord les valeurs courantes sur disque (une session concurrente a pu bumper entre-temps
— peu probable ici mais la vérification est obligatoire, jamais supposée).

Bumper `dev-orchestrator` en v2.9.0 : fichier VERSION, champ `version` de `module.json`, en-tête
`**Version**` du README. Ajouter une entrée CHANGELOG datée du jour, gabarit identique à la
dernière entrée existante (titre versionné + résumé entre parenthèses citant la phase), décrivant
ce qui a été LIVRÉ pour ce module : découverte MCP en union de deux scopes (projet ∪ global,
`--claude-json`/`VF_CLAUDE_JSON`) qui corrige le défaut actif ADR-051 sur les postes sans
`.mcp.json` ; relais verbatim du contrat `estimate:`/`actuals:` amont par `vf-coder` et
`vf-dev-manager` (deux champs optionnels frères du bloc typé, jamais une statistique du cru de
l'agent) ; purge de la dette de version 1.8.0→1.9.0 dans les 3 scripts et l'index régénéré.

Bumper `planning-core` en v2.5.3 (correctif — durcissement seul, aucune capacité nouvelle) :
`detect-gsd-engine.sh` cite désormais 1.9.0 au lieu de 1.8.0, aucun changement de contrat de
sortie (codes d'exit inchangés) — même formulation que l'entrée v2.5.2 déjà au journal pour un
changement de même nature.

Vérifier `conductor` (déjà bumpé v1.18.0 par 21-04) : triade cohérente, CHANGELOG daté, en-tête
README aligné. Ne RIEN modifier s'il est déjà cohérent — le signaler comme vérifié, pas re-bumpé.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh 2>&1 | grep -i "triade\|dev-orchestrator\|planning-core\|conductor"</automated>
  </verify>
  <acceptance_criteria>
    - Triade cohérente pour les 2 modules bumpés + conductor (VERSION = module.json, en-tête README aligné).
    - CHANGELOG de chaque module bumpé porte une entrée nouvelle datée décrivant le livré réel.
    - `check-version-sync.sh` ne signale aucune dérive de triade sur les modules du dépôt.
  </acceptance_criteria>
  <done>dev-orchestrator v2.9.0, planning-core v2.5.3, conductor v1.18.0 vérifié — triade et journaux cohérents.</done>
</task>

<task type="auto">
  <name>Task 3 : ROADMAP §Phase 21 recalée (Correctif 3)</name>
  <reversibility rating="reversible">Texte de doc.</reversibility>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 21 (~ligne 943-1035) en entier.
    - `.planning/ROADMAP.md` §Phase 20, ligne 868 : formulation exacte de la ligne Requirements
      quand aucun ID `REQ-` formel n'existe (« SC1..SC7 servent d'IDs de traçabilité — aucun ID
      formel REQ- n'existe pour cette phase dans REQUIREMENTS.md, même convention que les Phases
      15 à 19 »).
    - `.planning/REQUIREMENTS.md` en entier (308 lignes) — confirmer sur pièce qu'aucune entrée ne
      couvre les Phases 15+ (le ledger s'arrête à ALTI-05 / Phase 14).
    - Les 5 `21-0N-PLAN.md`/`SUMMARY.md` — intitulé réel de chaque plan pour la liste ROADMAP.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <behavior>
    - « Plans: 0 plans » devient « Plans: 5/5 plans executed ».
    - La case `- [ ] TBD (run /gsd-plan-phase 21 to break down)` est remplacée par les 5 plans réels, cochés, avec leur intitulé.
    - « Requirements: TBD (à mapper au ledger pendant le plan) » applique la même convention que la ligne 868 (Phase 20) : les Changements 1-6 + le point hérité servent d'IDs de traçabilité ad hoc, constat écrit qu'aucun ID `REQ-` formel n'existe dans le ledger pour cette phase — jamais une invention silencieuse.
  </behavior>
  <action>
Vérifier sur pièce que `.planning/REQUIREMENTS.md` ne couvre aucune phase après la 14 (ALTI-05) —
c'est un fait à constater, pas à corriger ici (hors mandat). Écrire la ligne Requirements de la
Phase 21 en reprenant EXACTEMENT la forme de la ligne 868 (Phase 20), adaptée aux IDs réels de
cette phase : les 6 Changements + le point hérité (STATE.md) servent d'IDs de traçabilité, aucun
ID `REQ-` formel n'existe pour cette phase dans `REQUIREMENTS.md`, même convention que les Phases
15 à 20.

Remplacer « Plans: 0 plans » par « Plans: 5/5 plans executed » et la liste `- [ ] TBD` par les 5
plans réels (21-01 à 21-05), cases cochées, un intitulé court par plan repris du Goal/SUMMARY de
chacun (mode d'injection MCP scope global ; contrats amont estimate/actuals + ADR-061 + hypothèse
dispatch nommé ; purge dette version + ADR-062 ; check-state-integrity.sh + ADR-063 ; clôture de
gouvernance — ce plan).
  </action>
  <verify>
    <automated>grep -n "Plans: 5/5 plans executed" .planning/ROADMAP.md; grep -c "21-0[1-5]-PLAN.md" .planning/ROADMAP.md; grep -n "^\*\*Requirements\*\*" .planning/ROADMAP.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Aucune case `- [ ] TBD` ne reste sous §Phase 21.
    - Les 5 plans sont listés, cochés.
    - La ligne Requirements ne dit plus TBD et cite explicitement l'absence d'ID formel dans le ledger, sur le patron de la Phase 20.
  </acceptance_criteria>
  <done>§Phase 21 du ROADMAP reflète l'état réel : 5/5 plans, Requirements tranchés sans invention.</done>
</task>

<task type="auto">
  <name>Task 4 : W1, W2, W3, W4 — les 4 avertissements non bloquants de la vérification</name>
  <reversibility rating="reversible">Un step CI, un chiffre d'ADR, une ligne de doctrine, un message d'aide.</reversibility>
  <read_first>
    - `.github/workflows/ci.yml` job `gates` en entier — forme des steps existants (nom, contrat de découverte non vide) pour insérer le nouveau step dans le même style.
    - `plugin/conductor/scripts/check-state-integrity.sh` en-tête — usage, defaults (`--path .`, `--file .planning/STATE.md`, `--against HEAD`), codes de sortie.
    - `docs/ADR.md` ligne 1391 (ADR-063 §Code Impacté) — occurrence exacte de « 23 cas ».
    - `plugin/conductor/references/team-kernel.md` ligne 25 (ligne « Dispatch nommé ») et son format de ligne de table.
    - `.planning/phases/VFDO-21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0/21-02-SUMMARY.md` §Constat changement 4 — le recoupement #1995/#2608 déjà vérifié, à reporter sans le refaire.
    - `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` lignes ~380-445 — la fonction `--verify`, où `determined=False` mène au message ligne 443 ; convention ASCII (sans accents) de ce bloc Python embarqué, à respecter dans le message ajouté.
  </read_first>
  <files>.github/workflows/ci.yml, docs/ADR.md, plugin/conductor/references/team-kernel.md, plugin/dev-orchestrator/scripts/inject-mcp-tools.sh</files>
  <behavior>
    - W1 : le job `gates` de la CI appelle `check-state-integrity.sh` comme un step de plus.
    - W2 : ADR-063 dit « 25 cas », plus « 23 cas ».
    - W3 : `team-kernel.md` porte une ligne (à côté de « Dispatch nommé ») actant le recoupement #1995 (motif `agent-*`/`worktree-agent-*`) et #2608 (`staging_failed`/`staging_timeout`) vérifié conforme sur `gsd-worktree-path-guard.js`, daté.
    - W4 : le message de rc=3 « pas de ligne tools: exploitable » nomme la cause fréquente (agent réécrit par l'amont, marqueur `vf-mcp-consumer` effacé) et le remède `--force`.
  </behavior>
  <action>
**W1.** Ajouter au job `gates` un nouveau step, dans le même style que `check-version-sync` (nom
court, `run: bash ...`), qui invoque `plugin/conductor/scripts/check-state-integrity.sh` sans
argument (defaults corrects depuis la racine du repo checked-out). Ce n'est PAS une modification du
script lui-même — seulement son câblage.

**W2.** Remplacer « 23 cas » par « 25 cas » à la ligne d'ADR-063 §Code Impacté qui cite
`test-check-state-integrity.sh` — recompter les cas réels de la suite avant d'écrire le chiffre,
ne pas recopier le chiffre de la vérification sans revérifier.

**W3.** Ajouter à `team-kernel.md`, juste après la ligne « Dispatch nommé » (même tableau), une
ligne ou un point qui acte : le recoupement du Changement 4 sur #1995 (namespace de branche élargi
`agent-*`/`worktree-agent-*`, déjà porté par le hook installé) et #2608 (cas d'échec
`staging_failed`/`staging_timeout`, entièrement interne à `gsd-executor` amont, aucun retry
VibeFlow ne l'enveloppe) a été vérifié CONFORME le 2026-07-31 — reprendre le constat déjà établi et
vérifié par 21-02, ne pas le refaire depuis zéro. Renvoyer à `gsd-worktree-path-guard.js` pour le
détail plutôt que de le dupliquer.

**W4.** Dans `inject-mcp-tools.sh`, enrichir le message d'erreur de la branche `if not determined`
(actuellement : « --verify : aucune cible determinee (pas de ligne tools: exploitable) — verdict
INDETERMINE. ») d'une phrase supplémentaire, dans la même convention sans accents que le reste de
ce bloc Python, qui nomme la cause fréquente en mode fichier unique (agent réécrit par
l'installeur amont, marqueur `vf-mcp-consumer: true` effacé) et le remède (`--force`).
  </action>
  <verify>
    <automated>grep -c "check-state-integrity" .github/workflows/ci.yml; grep -c "25 cas" docs/ADR.md; grep -c "1995\|staging_failed" plugin/conductor/references/team-kernel.md; grep -c -- "--force" plugin/dev-orchestrator/scripts/inject-mcp-tools.sh</automated>
  </verify>
  <acceptance_criteria>
    - `check-state-integrity.sh` apparaît dans `ci.yml` job `gates`, exécuté avec `bash`.
    - ADR-063 ne cite plus « 23 cas ».
    - `team-kernel.md` cite #1995 ou `staging_failed` (recoupement daté et vérifié).
    - Le message rc=3 mode fichier unique de `inject-mcp-tools.sh` mentionne `--force` comme remède.
  </acceptance_criteria>
  <done>Les 4 warnings de 21-VERIFICATION.md sont traités — CI durcie, ADR exacte, doctrine durable, message d'aide utile.</done>
</task>

<task type="auto">
  <name>Task 5 : STATE.md recalé à la main (point hérité, clôture Phase 21)</name>
  <reversibility rating="reversible">Frontmatter + section Current Position.</reversibility>
  <read_first>
    - `.planning/STATE.md` en entier — frontmatter courant (`current_phase: 21`, `status:
      in_progress`, `progress: total_phases 22, completed_phases 12, total_plans 55,
      completed_plans 40`), le commentaire YAML expliquant l'édition manuelle exclusive, et
      `## Current Position` (la section « Phase: 21 en cours »).
    - `git show c01f813:.planning/STATE.md` — précédent exact de clôture de gouvernance sans
      publication de release (Phase 20) : `current_phase` reste au numéro de la phase qui vient de
      clore SA gouvernance (pas d'avance au numéro suivant), `status: complete`, `stopped_at`
      décrit les plans livrés + l'état de la release (committée, pas taguée) + le prochain geste
      humain.
    - `plugin/conductor/scripts/check-state-integrity.sh` — l'invariant qui doit rester vert
      (compteurs non régressés au sein du même `milestone:`, une seule ligne `^Phase:`).
  </read_first>
  <files>.planning/STATE.md</files>
  <behavior>
    - `completed_phases` 12→13, `total_plans` 55→56, `completed_plans` 40→41 (le plan 21-05
      lui-même). `total_phases` inchangé (22).
    - `status` de la Phase 21 passe de `in_progress` à `complete`.
    - `current_phase` reste `21` (le patron c01f813 : le numéro n'avance qu'à l'ouverture de la
      phase suivante, jamais à sa seule clôture de gouvernance).
    - `## Current Position` décrit les 5 plans, les correctifs traités, l'état de la release
      préparée (pas taguée), le prochain geste humain.
    - `check-state-integrity.sh` reste vert (rc=0) avant ET après l'édition.
  </behavior>
  <action>
Éditer le frontmatter à la main : `current_phase_name` et `stopped_at` décrivent la clôture (5/5
plans, correctifs 1-3 + W1-W4 traités, release v2.45.0 préparée en commit local non taguée,
prochain geste humain = merger la PR puis taguer). `status: complete`. `last_updated` et
`last_activity` datés du jour. `progress.completed_phases: 13`, `progress.total_plans: 56`,
`progress.completed_plans: 41`.

Mettre à jour `## Current Position` (la seule ligne `^Phase:` du corps) pour refléter la clôture,
en conservant EXACTEMENT le même format `Phase: 21 ...` (l'invariant machine exige cette forme
littérale pour la phase courante — ne jamais la faire glisser vers `**Phase archivée :**`, réservé
aux phases dont une AUTRE devient courante).

Rejouer `check-state-integrity.sh` avant de committer cette tâche : un gate qui protège
spécifiquement CE fichier doit être vérifié sur CE changement précis, pas supposé vert.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/check-state-integrity.sh; echo "rc=$?"; grep -c "^Phase:" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `check-state-integrity.sh` sort rc=0 après l'édition.
    - Exactement une ligne `^Phase:` dans le corps.
    - Les 3 compteurs de progress ont strictement augmenté (jamais régressé) par rapport à l'état d'avant ce plan.
  </acceptance_criteria>
  <done>STATE.md reflète la clôture de gouvernance de la Phase 21, édité à la main, gate vert.</done>
</task>

<task type="auto">
  <name>Task 6 : préparation de la release racine v2.45.0 — triade + 2 historiques README + CHANGELOG racine (commit local uniquement)</name>
  <reversibility rating="reversible">Aucun tag, aucun push — purement des fichiers de version et de doc.</reversibility>
  <read_first>
    - `VERSION`, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` — valeurs courantes (v2.44.0).
    - `README.md`/`README.fr.md` — badges (`img.shields.io/badge/version-...`), table d'historique (dernière entrée en tête = v2.44.0, forme `| \`vX.Y.Z\` | date | description |`).
    - `CHANGELOG.md` racine — dernière entrée (v2.44.0), gabarit exact.
    - `CLAUDE.md` — discipline de release : bump cohérent des 3 fichiers + 2 README (badges ET historique) + CHANGELOG, MAIS jamais de tag ici (réservé post-merge, à Samuel).
    - `scripts/check-version-sync.sh` en entier — doit finir vert après ce bump.
  </read_first>
  <files>VERSION, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, README.md, README.fr.md, CHANGELOG.md</files>
  <behavior>
    - `VERSION`, `plugin.json`, `marketplace.json` disent tous `2.45.0` (minor — nouvelle capacité `check-state-integrity.sh`).
    - Badges des 2 README à `2.45.0`.
    - Historique des 2 README : nouvelle entrée en tête `| \`v2.45.0\` | 2026-07-31 | ... |`.
    - `CHANGELOG.md` racine gagne une entrée `## [v2.45.0] — 2026-07-31` décrivant les 6 Changements + le point hérité + les modules bumpés.
    - `scripts/check-version-sync.sh` sort rc=0.
    - Aucun tag créé (`git tag --points-at HEAD` reste vide), aucun push, aucune PR, aucune release GitHub.
  </behavior>
  <action>
Bumper `VERSION` (racine) à `2.45.0`, `plugin/.claude-plugin/plugin.json` (champ `version`),
`.claude-plugin/marketplace.json` (`.plugins[0].version`) — même valeur partout, préfixe compris
là où le fichier l'exige.

Mettre à jour le badge de version des 2 README (`img.shields.io/badge/version-2.45.0-...`).

Ajouter une entrée en tête de la table d'historique des 2 README (`| \`v2.45.0\` | 2026-07-31 |
...`), résumant la phase pour un lecteur externe : alignement sur `@opengsd/gsd-core` 1.9.0 —
union de scope MCP global/projet (ADR-051-B) qui corrige le défaut actif, contrat
`estimate:`/`actuals:` relayé verbatim, arbitrage écrit du recouvrement avec les lanes de revue
amont (ADR-061), hypothèse de dispatch nommé documentée, purge de la dette de version 1.8.0,
arbitrage des hooks 1.9.0 non câblés (ADR-062), nouveau gate anti-régression `STATE.md`
(`check-state-integrity.sh`, ADR-063) câblé en CI. Modules `dev-orchestrator` v2.9.0,
`planning-core` v2.5.3, `conductor` v1.18.0.

Ajouter l'entrée jumelle dans `CHANGELOG.md` racine, même gabarit que l'entrée v2.44.0 déjà en
tête du fichier.

Rejouer `scripts/check-release-tag.sh` N'EST PAS attendu vert à ce stade (rappel du mandat : il
est censé rester rouge tant que le tag n'existe pas — ne pas le lancer en boucle en espérant qu'il
passe). Seul `scripts/check-version-sync.sh` doit finir vert ici.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh; echo "rc=$?"; git tag --points-at HEAD; git status --porcelain -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json</automated>
  </verify>
  <acceptance_criteria>
    - `check-version-sync.sh` sort rc=0.
    - Triade racine cohérente (VERSION = plugin.json = marketplace.json = 2.45.0).
    - `git tag --points-at HEAD` vide.
    - Aucun `git push`, aucun `gh pr`, aucun `gh release` exécuté par ce plan.
  </acceptance_criteria>
  <done>La release v2.45.0 est intégralement préparée en commits locaux ; Samuel n'a plus qu'à merger puis taguer.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Un correctif de clôture publie la release à la place de Samuel | mitigate | P-01 + Task 6 vérifie explicitement l'absence de tag et de push après chaque commit |
| `gsd-tools state` invoqué par réflexe pour "juste vérifier" STATE.md | mitigate | P-02, rappelé dans Task 5 — édition manuelle uniquement, gate rejoué en preuve |
| Un chiffre (suites, cas ADR) recopié du rapport de vérification sans revérifier | mitigate | Chaque task read_first exige un recomptage réel avant édition (même leçon que 20-07 T-20-07-02) |
| Dérive de périmètre vers les Phases 22-25 | mitigate | P-03, aucun fichier de ces phases dans `files_modified` |
</threat_model>

<verification>
- `scripts/check-version-sync.sh` rc=0 (Correctif 1 + Task 6).
- `check-agents.sh --strict` rc=0 sur les 6 dossiers d'agents.
- Toutes les suites découvertes (`find plugin scripts -type f -path '*/tests/test-*.sh'`) passent.
- `check-mission-invariants.sh` et `check-state-integrity.sh` verts.
- `.github/workflows/ci.yml` job `gates` invoque `check-state-integrity.sh`.
- Aucun tag créé, aucun push, aucun merge.
</verification>

<output>
Create `.planning/phases/VFDO-21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0/21-05-SUMMARY.md` when done.
</output>

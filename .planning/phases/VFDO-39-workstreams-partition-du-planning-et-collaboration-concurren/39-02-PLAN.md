---
phase: VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md
  - docs/ADR.md
  - plugin/dev-orchestrator/references/workstreams.md
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
autonomous: true
requirements: [PART-01, PART-02, PART-05, PART-06, PART-09]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md carries a PART-01..09 family, one bullet per ROADMAP Success Criterion plus the D-09/D-10 mandatory-to-grave facts, each citing fichier:ligne from the cadrage research — no invented numbers."
    - "GSDA-19's body text is preserved byte-for-byte (MANI-04's superseded_by precedent) and gains a `superseded par PART-06` annotation carrying D-04's angle; its Phase-24 traceability mapping and Done status are untouched, only annotated the same way."
    - "ADR-069 carries a dated 2026-09-09 amendment resolving D-09 (explicit fix-in-VF vs. assumed-cost choice, written not implied) and D-10 (ADR-064-in-team collision), with the K2 coverage number re-derived by re-running the ADR's own reproducible snippet, never copied from the research file."
    - "vf-dev-manager.md's dispatch gabarit is amended (D-10, correction 2026-09-10) to require an explicit `--ws <nom>` per mandate and a distinct `GSD_SESSION_KEY` per concurrently-dispatched worker on a partitioned repo — closing the collision D-10 named OPEN, not merely documenting it."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md
    - docs/ADR.md
    - plugin/dev-orchestrator/references/workstreams.md
    - plugin/dev-orchestrator/agents/vf-dev-manager.md
  key_links:
    - "PART-xx bullets -> research file fichier:ligne citations (each re-derivable, none copied blind)"
    - "ADR-069 dated amendment -> workstreams.md §4(b)/(d) geste update (doctrine stays internally consistent after the amendment)"
    - "vf-dev-manager.md dispatch gabarit -> plan 39-03 Task 1's ≥2-concurrent-worker proof (the amendment is exercised, not just written)"
---

<objective>
Ledgerize this phase's requirements (`PART-01`..`PART-09`, D-07's prefix), supersede `GSDA-19` in
place with `PART-06` (body preserved, annotated) and draft (never post) the upstream issue for it (D-04), and
amend ADR-069 with a dated subsection that resolves D-09 (pr-branch's migrated risk) and D-10
(ADR-064's team-model collision) with a freshly re-derived coverage number.

Purpose: every locked cadrage decision (D-04, D-06, D-07, D-09, D-10) that this phase is required
to "grave" (write durably) lives in exactly three files — REQUIREMENTS.md, ADR.md, and the
upstream-draft convention — and this plan is the only one that touches them.
Output: the `PART-xx` ledger family, the superseded-in-place `GSDA-19` (body preserved), the drafted-not-posted issue file,
and ADR-069's dated amendment.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
@.planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md
@.planning/REQUIREMENTS.md
@docs/ADR.md
@plugin/dev-orchestrator/references/workstreams.md
@.planning/upstream/2026-08-04-workflows-aveugles-aux-workstreams.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PART-01..09 requirement family, traceability rows, tail changelog</name>
  <read_first>
    .planning/REQUIREMENTS.md:953-990 (Phase 38's FIDE/RUNT/ROLL/TGT/ADPT/MIGR family — the exact
    H3-header + blockquote + checkbox style to mirror, insertion point immediately before line
    988's `### Transverse`);
    .planning/REQUIREMENTS.md:700-703 and :840-849 (Traceability table header and its last rows,
    `QUAL-01` at line 848 — the exact insertion point for the nine new rows);
    .planning/REQUIREMENTS.md:1005-1007 (the tail's chained `*Last updated: ... ; précédent : ...*`
    line — the exact convention to extend, never overwrite);
    .planning/ROADMAP.md § Phase 39 (the six numbered Success Criteria, verbatim wording and
    fichier:ligne citations to reuse, not re-derive);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    <decisions> D-01 through D-13 verbatim (source of truth for every fact cited below);
    .planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md §1-10 in full (every
    fichier:ligne citation used in the nine bullets below traces back here).
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Using `Edit` (scoped replacement only — never `Write` the whole file), insert immediately
    before the `### Transverse` heading (currently line 988) a new subsection titled exactly
    `### Workstreams — partition du planning et collaboration concurrente (Phase 39)`, opening with
    a one-paragraph blockquote naming: the derivation date (2026-09-09), the method discipline
    (`awk`+`comm`, never `grep | sort -u` nor `diff` — ADR-069 §Méthode), and D-07's prefix
    rationale (`PART`, not `WSTR` — voisinage lisible avec `WKTR`/`WTCH` dans une phase qui fait
    cohabiter les deux mots ; espace de noms dérivé au 2026-09-09 : 40 préfixes gravés, 41 familles
    réellement occupées en comptant `SIG-01..06`, absente des deux ledgers). Then nine unchecked
    checkbox bullets, `PART-01` through `PART-09`, verbatim as follows (do not paraphrase — these
    citations were verified fichier:ligne against the research file before this plan was written):

    - **PART-01**: Le **niveau 4** de résolution du pointeur de workstream
      (`.planning/active-workstream` consulté quand la session courante n'a jamais posé le sien)
      est **mesuré par exécution sur dépôt jetable**, jamais lu sur un descripteur amont —
      `active-workstream-store.cjs:286-290`, reproduit recherche
      `2026-09-09-phase-39-workstreams-mesures-de-cadrage.md` §2 : héritage confirmé (`SESS-B` sans
      marker → `active: null`, avec marker → `active: default`), isolation d'un pointeur déjà posé
      jamais franchie (même périmé), auto-nettoyage confirmé. *(Critère de succès 1)*
    - **PART-02**: La **non-composabilité du pointeur avec ADR-064** est **tranchée par la
      mesure**, jamais laissée implicite (D-10, recherche §3) : **refermée** entre deux sessions
      Claude Code distinctes (deux worktrees, deux process, isolation parfaite — cas A/B) ;
      **OUVERTE** pour le modèle d'équipe VibeFlow — tous les sous-agents d'une session héritent du
      même `CLAUDE_CODE_SESSION_ID` (`CLAUDE_CODE_CHILD_SESSION=1`), un manager qui dispatche des
      workers sur des worktrees différents leur fait partager un seul pointeur, dernier `set`
      gagnant. Déclarée comme **limite datée du 2026-09-09** dans l'amendement d'ADR-069, jamais
      comme un fait résolu. *(Critère de succès 2)*
    - **PART-03**: Au moins un dépôt — un **clone jetable de `vibeflow-os`**, histoire réelle, hors
      de l'arbre de travail (D-01) — est réellement partitionné et **TOUS** les agents `vf-*`
      dispatchés dans le run de preuve passent `--ws` **explicitement, sans exception** (D-08,
      insatisfaisable à vide). Les gates (`check-workstream-pointer.sh`, `check-state-integrity.sh`,
      `check-divergence.sh`) restent **verts** dans l'arbre non partitionné (non-régression, mesurée
      sur le dépôt vivant) **et** le deviennent dans le clone partitionné. Preuve par exécution,
      jamais par inspection de frontmatter. *(Critère de succès 3)*
    - **PART-04**: Le **filet de détection de divergence** existe et est **bruyant** — signature
      **S2 + S4 + S5** (numéro de phase dupliqué / cardinalité dossiers↔ROADMAP↔STATE incohérente /
      fuite de niveau au ROADMAP racine), branché **post-merge** (jamais `SessionStart`, D-12),
      **opt-in** (`git config core.hooksPath scripts/hooks`, jamais armé par défaut — précédent
      #38) **ET câblé sur le job CI `gates`** (arbitrage Samuel, 2026-09-10 — le hook local seul
      serait muet par construction sur les PR fusionnées côté GitHub, le chemin de merge dominant de
      ce dépôt ; ADR-059, « sûr mais inerte », précédent Phase 35 non répété). **Mutation rouge
      prouvée** (D-13) sur les trois variantes de split-brain de la recherche §8 (orphan, rootonly,
      roadmap) : le gate détecte les deux variantes silencieuses (`git merge-tree`/`git merge` exit
      0, Git ne signalant rien) et reste muet sur le cas nominal non divergé ; discriminance du
      script lui-même prouvée par mutation, **à la fois localement et en CI**. *(Critère de succès 4)*
    - **PART-05**: Le comportement de `/gsd-pr-branch` sur un dépôt partitionné est traité **sur la
      mesure du 2026-09-09**, pas sur le libellé du 2026-08-04 (D-09, recherche §7) — le risque (b)
      d'ADR-069 a **MIGRÉ** : le silence est levé au niveau **chemin** (bloc `$OTHER`,
      `pr-branch.md:410`) mais **subsiste au niveau COMMIT** (`.planning/workstreams/<nom>/
      ROADMAP.md` ne matche pas `STRUCTURAL_RE`, ancré `^\.planning/ROADMAP\.md$`,
      `pr-branch.md:255`) — un commit qui ne touche QUE la feuille de route d'un workstream est
      **EXCLU dans les deux modes** (`pr_strict` vrai ou faux), invisible dans tout rapport. Les
      trois conséquences (i FAUSSE À MOITIÉ / ii VRAIE / iii VRAIE) sont gravées avec leur verdict.
      Choix explicite tranché et écrit, **jamais implicite** : **coût assumé, daté** (amendement
      d'ADR-069) — pas de correctif local de `pr-branch.md` (Iron Law 2 révisée interdit toujours le
      fork d'une capacité du moteur), mitigation = geste de vérification explicite existant
      (`workstreams.md` §4(b)) étendu pour nommer le niveau commit. *(Critère de succès 5)*
    - **PART-06**: La **couverture amont** est **re-mesurée à la date du cadrage** (K2 = 9/89 =
      10,1 %, 39 aveugles — contre 7/91 = 7,7 %, 42 aveugles au 2026-08-04 ; corpus, critère et
      commande re-dérivés en ré-exécutant le script d'ADR-069 §Méthode, jamais recopiés) et la
      remontée `GSDA-19` est **rédigée** (D-04, angle bug de comportement) et **prête à poster**,
      avec trace explicite qu'elle n'est **PAS postée** (geste humain réservé, ADR-031) — jamais un
      abandon silencieux. *(Critère de succès 6)*
    - **PART-07**: La preuve de partition ne s'exécute **JAMAIS** sur l'arbre de travail de
      `vibeflow-os` pendant la Phase 39 — uniquement sur un **clone jetable**, hors de l'arbre,
      portant l'histoire réelle du dépôt, détruit en fin de mission (D-01). Tout SUMMARY issu de
      cette preuve porte explicitement la nuance : preuve de **mécanisme**, jamais d'usage
      concurrent réel sur le dépôt vivant (D-03). La condition dure d'ADR-069 (« aucune partition
      tant qu'une phase est en vol ») n'est ni révisée ni contournée.
    - **PART-08**: La **partition réelle de `vibeflow-os`** est inscrite comme un **geste séparé,
      gaté humain, postérieur à la clôture de la Phase 39**, jamais exécutée par cette phase — avec
      un **déclencheur de reprise daté** porté au ROADMAP et au STATE (D-02), au même patron que les
      déclencheurs déjà en vigueur dans ce dépôt (`WKTR-03`, le déclencheur objectif d'ADR-069 :
      objectif, sans échéance calendaire).
    - **PART-09**: VF passe **`--ws` explicitement** sur ses propres appels `gsd_run` (agents
      `vf-*`, sections déjà déclarées de `vf-coder.md:43-51` et `vf-dev-manager.md:31-34`, **plus le
      gabarit de dispatch de `vf-dev-manager.md` amendé par la Tâche 4 de ce même plan pour exiger un
      `GSD_SESSION_KEY` distinct par mandat concurrent — correction 2026-09-10, C4**), **observé par
      exécution** dans le run de preuve (PART-03), avec **au moins deux workers concurrents sans
      `GSD_WORKSTREAM` exporté dans l'environnement partagé** (39-03-PLAN.md Tâche 1, corrigé le
      même jour — sinon la preuve confond « le flag fonctionne » et « l'export fonctionne ») —
      aucun workflow amont n'est réécrit (D-05). Une **veille datée sur un ÉVÉNEMENT** (prochaine
      publication `gsd-core`, qui distribuera `#4455`/`#4456`/`#4225`, fermés le 7-8 septembre 2026
      mais non distribués en 1.13.0) remplace toute veille à seuil chiffré (D-06) — inscrite en note
      de STATE.md, jamais comme un seuil figé.

    Then insert nine rows into the `## Traceability` table (currently ending at line 848 with
    `QUAL-01`), immediately after that `QUAL-01` row, one per new ID, in the exact form
    `| PART-0N | Phase 39 | Planned — plan 39-0X |`, using this plan mapping: `PART-01`, `PART-02`,
    `PART-05`, `PART-06` → `plan 39-02` ; `PART-04` → `plan 39-01` ; `PART-03`, `PART-07`, `PART-08`,
    `PART-09` → `plan 39-03`. This is a deliberate departure from the most recent lot's own
    precedent (Phase 38's `FIDE`..`MIGR` family has no traceability-table rows at all) — done here
    because this plan's own mandate explicitly asks for it, not by silent default; do not remove or
    reformat any existing row while inserting.

    Finally, transform the tail's chained changelog line (currently starting `*Last updated:
    2026-08-28 — Phase 38 ...*` at line 1007): prepend a new clause reading `*Last updated:
    2026-09-09 — Phase 39 (Workstreams — partition du planning et collaboration concurrente) : 9
    exigences neuves en une famille (PART-01..09), dérivées du cadrage `39-CONTEXT.md` et de la
    recherche `2026-09-09-phase-39-workstreams-mesures-de-cadrage.md`, préfixe `PART` vérifié libre
    par `comm` (D-07, 41 familles réellement occupées incluant `SIG-01..06` hors ledger) ;
    `GSDA-19` superseded par `PART-06` (corps préservé, angle bug de comportement porté par `PART-06`, D-04), `GSDA-13..19` closes non rouvertes ;
    précédent : ` followed immediately by the EXISTING line's content starting from `2026-08-28 —
    Phase 38` through to its closing `*`, preserved byte-for-byte (this is the same
    prepend-and-chain transformation the file already applied to its own predecessor entry).
  </action>
  <verify>
    <automated>bash -c 'a=$(grep -c "PART-09" .planning/REQUIREMENTS.md); b=$(grep -c "^| PART-01 | Phase 39" .planning/REQUIREMENTS.md); c=$(grep -c "Last updated: 2026-09-09" .planning/REQUIREMENTS.md); [ "$a" -ge 1 ] && [ "$b" -ge 1 ] && [ "$c" -ge 1 ] && echo OK || echo "FAIL a=$a b=$b c=$c"'</automated>
    <fails_when>prints FAIL (the PART-09 bullet, its traceability row, or the new tail changelog clause is missing from REQUIREMENTS.md)</fails_when>
  </verify>
  <done>REQUIREMENTS.md carries the nine-bullet PART-xx subsection under "## Milestone fiabilite-v1.0", nine new traceability rows mapped to plans 39-01/39-02/39-03, and a tail changelog line chaining the new update ahead of the preserved 2026-08-28 entry.</done>
  <acceptance_criteria>
    - The new `### Workstreams — partition du planning et collaboration concurrente (Phase 39)` heading appears verbatim, immediately before `### Transverse`.
    - All nine `PART-0N` bullets are present, unchecked, each citing at least one `fichier:ligne` from the research file.
    - Nine new rows exist in the `## Traceability` table, immediately after the existing `QUAL-01` row, none duplicated, none overwriting an existing row.
    - The file's final line still ends in `*Requirements defined: 2026-06-04*` two lines above the (now longer) chained `*Last updated: ...*` line — no existing changelog content was deleted, only prepended to.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GSDA-19 superseded (not overwritten) by PART-06 + drafted, unposted upstream issue</name>
  <read_first>
    .planning/REQUIREMENTS.md:461-465 (GSDA-19's current body — preserved verbatim, per the
    non-destructive `superseded_by` convention documented at :109-111: "statut basculé, contenu
    conservé — pas supprimée");
    .planning/REQUIREMENTS.md:916 and :827 (`MANI-04`'s own "superseded par ... : brouillon écrit,
    ..., jamais posté" pattern — the EXACT precedent to replicate for GSDA-19: bullet body keeps its
    original text and gains a trailing "superseded par" clause, traceability row keeps its original
    `Done`/`Phase 24` mapping and gains the same clause — corrected 2026-09-10: this task used to
    REPLACE GSDA-19's body outright, which would have made the `Done — plan 24-10` mapping attest
    content plan 24-10 never produced; that is now rejected in favor of this precedent);
    .planning/upstream/2026-08-04-workflows-aveugles-aux-workstreams.md (full file — the two-part
    shape: French bandeau, `---` separator, English body — to replicate structurally, not verbatim);
    .planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md §5 (the exact measured
    defect: `init.progress --ws default` renders `project_exists: false` for a `PROJECT.md` the
    official migration left in place; the three-call-site inconsistency: `planningDir` in `init.cjs`
    vs. `planningRoot` in `planning-snapshot.cjs:447` vs. a hard-coded `path.join` in
    `profile-output.cjs:303`; `config.json`'s working root-fallback federation at
    `planning-workspace.cjs:249-260` as the precedent to extend);
    this plan's own Task 1 `PART-06` bullet text (the fresh identifier that now carries the new
    bug-de-comportement angle — GSDA-19 itself no longer does);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    D-04 verbatim (the re-wording mandate: bug de comportement, angle abandoned, rédigée mais pas
    postée — carried by `PART-06`, per the fix above).
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md</files>
  <action>
    Using `Edit` (scoped), leave GSDA-19's body text (lines 461-465) BYTE-FOR-BYTE intact — do not
    replace or paraphrase a single word of it, on `MANI-04`'s own precedent (content conserved, only
    status/annotation changes) — and append, inside the bullet, immediately before its closing
    `*(A9, verdict zone 5, point 4)*` citation, the clause: ` **— superseded par `PART-06` (Phase 39,
    2026-09-09)** : l'angle « descripteur non descriptif » (#2598) est abandonné (D-04) ; le nouvel
    angle bug de comportement vit désormais sous `PART-06`, pas ici — ce bullet reste l'historique
    exact de ce que le plan 24-10 a réellement produit.` Do the same, in the SAME style as
    `MANI-04`'s traceability row at line 827, to GSDA-19's row in the `## Traceability` table (line
    803): append ` — superseded par `PART-06` (Phase 39, 2026-09-09)` after `Done — plan 24-10`,
    changing nothing else in that cell. `GSDA-13`..`GSDA-19` remain closed and are not reopened; the
    `Done`/`Phase 24` mapping keeps attesting only what plan 24-10 actually produced.

    Then create `.planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md`,
    replicating the two-part structural shape of `2026-08-04-workflows-aveugles-aux-workstreams.md`
    (French bandeau block, `---` separator, English body) but with content adapted to this defect:
    the bandeau states plainly that this IS a behaviour-bug report (the "#2598 descriptive-gap"
    framing is explicitly not reused as this report's own framing — cite `#2598` only as prior-art
    precedent for an accepted FORM, per the analog file's own "Prior art and non-duplication"
    section), not posted, deposit reserved to human validation (ADR-031). The English body needs: a
    title naming the measured defect, a `## Summary` describing the three-call-site inconsistency
    above, a `## Reproduction` section with the EXACT, literal command string
    `gsd_run query init.progress --ws default` and the EXACT, literal output fragment
    `project_exists: false` from research §5 (both strings must appear verbatim — this is what makes
    `PART-06`/this draft falsifiable on content, not just on the file's existence: replaying the
    `## Reproduction` command against this repo today must still print `project_exists: false`,
    the same defect, not a stale claim), and a `## What we are asking for` section proposing a
    root-fallback federation for `PROJECT.md` mirroring `config.json`'s working one — nothing else
    invented beyond what research §5 measured.
  </action>
  <verify>
    <automated>bash -c '
f=.planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md
superseded=$(grep -c "superseded par .PART-06" .planning/REQUIREMENTS.md)
body_intact=$(grep -c "gabarit de forme déjà accepté en amont" .planning/REQUIREMENTS.md)
repro_cmd=$( [ -f "$f" ] && grep -c "gsd_run query init.progress --ws default" "$f" || echo 0 )
repro_out=$( [ -f "$f" ] && grep -c "project_exists: false" "$f" || echo 0 )
notposted=$( [ -f "$f" ] && grep -q "PAS.*post" "$f" && echo 1 || echo 0 )
[ "$superseded" -ge 2 ] && [ "$body_intact" -ge 1 ] && [ "$repro_cmd" -ge 1 ] && [ "$repro_out" -ge 1 ] && [ "$notposted" -eq 1 ] && echo OK || echo "FAIL superseded=$superseded body_intact=$body_intact repro_cmd=$repro_cmd repro_out=$repro_out posted=$notposted"'</automated>
    <fails_when>prints FAIL — the "superseded par PART-06" clause is missing from either the bullet or the traceability row (need both, hence `-ge 2`), GSDA-19's original body text was altered instead of preserved, the draft file is missing the literal reproduction command or its literal output, or the not-posted bandeau clause is absent</fails_when>
  </verify>
  <done>GSDA-19's body and traceability row are BYTE-preserved and gain an explicit "superseded par PART-06" annotation each, on `MANI-04`'s own precedent; the new bug-de-comportement angle lives entirely under `PART-06` (Task 1); a new, unposted draft issue file exists at the upstream convention path, containing the literal reproduction command and its literal output, structurally mirroring the 2026-08-04 analog.</done>
  <acceptance_criteria>
    - GSDA-19's bullet still contains the phrase "gabarit de forme déjà accepté en amont" — its original framing is preserved, not overwritten.
    - GSDA-19's traceability row still reads `Phase 24 | Done — plan 24-10 ...` AND now also carries `superseded par `PART-06``.
    - The new upstream draft file's `## Reproduction` section contains the literal string `gsd_run query init.progress --ws default` and the literal string `project_exists: false` — not a paraphrase.
    - The new upstream draft file's opening lines contain a French bandeau stating the text is not posted, exactly like the 2026-08-04 analog's own bandeau does.
    - The draft's body is in English, per the repo's own convention for upstream-facing text.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ADR-069 dated amendment (D-09, D-10, re-derived K2) + workstreams.md geste update</name>
  <read_first>
    docs/ADR.md:1922-2172 (ADR-069 in full — the four risks, the condition dure, the Iron Law 2
    revision, the ADR-064 amendment, the "Déclencheur de réexamen" closing section this new
    subsection is appended after);
    docs/ADR.md:1992-2002 (the exact re-runnable `awk`+`comm` snippet to re-execute live for the
    coverage number — never copy the research file's number without re-deriving it, per this ADR's
    own §Méthode rule);
    docs/ADR.md:28-41 (the ADR summary table — the ADR-064 row's "Validée — amendée par ADR-069 ..."
    pattern to replicate for ADR-069's own Statut cell);
    docs/ADR.md:350-368 (`### Amendement 2026-07-20 — attribution de session` — the exact dated
    dated-subsection template: title shape, "Constat terrain" / "Correction" structure);
    plugin/dev-orchestrator/references/workstreams.md §4(b) lines 138-145 and §4(d) lines 152-157
    (the existing "geste" prose each amendment extends);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    D-09 and D-10 verbatim.
  </read_first>
  <files>docs/ADR.md, plugin/dev-orchestrator/references/workstreams.md</files>
  <action>
    First, RE-RUN the exact bash snippet at `docs/ADR.md:1992-2002` verbatim (same corpus
    `$HOME/.claude/gsd-core/workflows`, same K2 criterion — do not modify the script). Confirm
    `seen=89` (not 91 — this machine runs gsd-core 1.13.0, the workflow directory's own file count
    moved since the ADR was first written) before trusting or citing any of its other output; if
    `seen` differs from 89, that is itself the fact to report, not a reason to fall back to the
    research file's number without noting the discrepancy.

    Using `Edit` (scoped), append immediately after ADR-069's "Déclencheur de réexamen" section
    (before the `---` separator that precedes `## ADR-070`) a new dated subsection, on the exact
    template of `### Amendement 2026-07-20 — attribution de session` (`docs/ADR.md:350-368`):

    `### Amendement 2026-09-09 — risque (b) migré au niveau commit, collision ADR-064 rouverte en
    équipe, couverture re-mesurée`

    Body, in this order: (1) the freshly re-derived K2 number from the live re-run just performed,
    contrasted with the original `7/91 = 7,7 %` figure, named explicitly as movement of the corpus
    (gsd-core 1.9.1 → 1.13.0), never presented as a correction of the original measurement — both
    numbers were and remain correct for their own date; (2) D-09's resolution: the risk (b)
    commit-level gap (`.planning/workstreams/<nom>/ROADMAP.md` excluded from both `pr_strict` modes,
    invisible in any report — `pr-branch.md:255,410`), and the explicit choice **coût assumé,
    daté** — no local fork of `pr-branch.md`'s commit classifier (the Iron Law 2 revision still
    forbids forking a moteur capability outright), mitigation = the existing §4(b) "geste" (list
    expected roadmap commits before opening a PR) extended to name the commit-level gap explicitly
    (edit `workstreams.md` next, in this same task); (3) D-10's resolution: the pointer/ADR-064
    composability is CLOSED between distinct Claude Code sessions (two worktrees, two processes —
    isolation measured intact) but OPEN within VibeFlow's own team model (all sub-agents of one
    session inherit the same `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_CHILD_SESSION=1`) — a dated,
    named limit, distinct from the original risk (c) text (which addressed only the
    pointer-vs-worktree case, never the team-dispatch case); (4) **C17, ARBITRÉ PAR SAMUEL le
    2026-09-10 (revu depuis la première passe de correction, qui l'avait déclaré coût assumé) : le
    filet est ARMÉ EN CI, la mutité par défaut n'est PAS assumée.** Le hook `scripts/hooks/post-merge`
    (plan `39-01`) reste — il sert les merges locaux — mais ce dépôt fusionne ses PR côté GitHub, où
    `post-merge` ne s'exécute JAMAIS : un filet correct mais qui ne se déclenche jamais sur le cas
    d'usage dominant est très exactement le mode de défaillance nommé par **ADR-059** (« sûr mais
    inerte en conditions de mission »), le même constat qui avait fait renoncer au ré-armement
    d'`isolation: worktree` en Phase 35 — ce lab a déjà payé une fois pour un dispositif juste et
    inerte, il ne paie pas deux fois. `plan 39-01` Tâche 3 câble donc `check-divergence.sh` sur le
    job `gates` existant (celui qui exécute déjà `check-workstream-pointer.sh` et
    `check-state-integrity.sh`), avec une **preuve de déclenchement réel par mutation** (une fixture
    divergée qui bascule effectivement de exit 0 à exit 1 EN CI), pas seulement la présence du gate
    — même leçon que celle gravée ×4 phases dans ce lab : aucun vert auto-déclaré ne tient. Le
    Critère de succès 4 (« existe et est bruyant ») est donc vrai à la fois localement (hook) et sur
    le chemin dominant de ce dépôt (CI).

    Then update ADR-069's own summary-table row (`docs/ADR.md:40`): append ` — amendée le
    2026-09-09` to its Statut cell, on the exact pattern already used for the ADR-064 row
    (`Validée — amendée par ADR-069 (...)`, line 36).

    Finally, using `Edit` (scoped) on `plugin/dev-orchestrator/references/workstreams.md`: in §4(b)
    (lines 138-145), add one sentence naming the commit-level gap explicitly (
    `.planning/workstreams/<nom>/ROADMAP.md` non structurel au niveau commit, exclu silencieusement
    dans les deux modes de `pr_strict` — pas seulement au niveau chemin) so the existing geste (list
    expected roadmap commits before opening a PR) now names the precise thing to check for; in §4(d)
    (lines 152-157), add one sentence naming the new `check-divergence.sh`/`post-merge` gate
    (D-11/D-12, delivered by plan `39-01`) as the mechanical complement to the existing manual
    geste, citing it by path — without claiming the manual comparison is no longer needed (the gate
    is opt-in; the manual geste remains the fallback on an unarmed repo).
  </action>
  <verify>
    <automated>bash -c 'a=$(grep -c "Amendement 2026-09-09" docs/ADR.md); b=$(grep -c "amendée le 2026-09-09" docs/ADR.md); c=$(grep -c "niveau commit" plugin/dev-orchestrator/references/workstreams.md); [ "$a" -ge 1 ] && [ "$b" -ge 1 ] && [ "$c" -ge 1 ] && echo OK || echo "FAIL a=$a b=$b c=$c"'</automated>
    <fails_when>prints FAIL — the dated amendment heading, the amended summary-table Statut cell, or the workstreams.md commit-level sentence is missing</fails_when>
  </verify>
  <done>ADR-069 carries a dated 2026-09-09 amendment resolving D-09 and D-10 with a freshly re-derived K2 number (re-run live, not copied); its summary-table row is annotated amended; workstreams.md §4(b)/(d) name the commit-level gap and the new gate.</done>
  <acceptance_criteria>
    - The re-run of `docs/ADR.md:1992-2002` was actually executed in this task (its `seen=` output is quoted in the amendment text, not assumed).
    - The amendment explicitly states one of the two D-09 choices (coût assumé, daté) — never leaves it implicit.
    - The amendment names both the closed (inter-session) and open (intra-team) halves of D-10, not just one.
    - `workstreams.md` §4(b) and §4(d) each gained exactly one new sentence — neither section's existing "geste" prose was deleted or contradicted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: vf-dev-manager.md dispatch gabarit closes D-10's OPEN half (correction 2026-09-10, C4)</name>
  <read_first>
    plugin/dev-orchestrator/agents/vf-dev-manager.md:29-34 ("Sources de connaissance", the existing
    single-line workstream mention: "exporte `GSD_WORKSTREAM`, passe `--ws`" — states the rule for
    the manager's OWN resolution, says nothing about what it must inject into a WORKER's mandate);
    plugin/dev-orchestrator/agents/vf-dev-manager.md:122-124 ("Chaque mandat embarque le digest de
    mission" — the exact dispatch template section this task amends; verified 2026-09-10:
    `GSD_SESSION_KEY` appears nowhere in `plugin/`, and no `files_modified` of any existing plan
    touches a `vf-*` agent to inject per-mandate scope — this is the gap D-10 named OPEN and left
    untreated);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    D-10 verbatim (closed between sessions/worktrees, OPEN within VF's own team model — the exact
    limit this task closes for VF's own dispatch, not for gsd-core's engine);
    docs/ADR.md § ADR-069 Amendement 2026-09-09 (produced by Task 3 of this same plan — the D-10
    resolution text this task's amendment must not contradict).
  </read_first>
  <files>plugin/dev-orchestrator/agents/vf-dev-manager.md</files>
  <action>
    Using `Edit` (scoped) on `plugin/dev-orchestrator/agents/vf-dev-manager.md`, immediately after
    the "Chaque mandat embarque le digest de mission" paragraph (lines 122-124), insert a new
    paragraph titled implicitly by its first sentence (no new H2/H3 heading — stay inside the
    existing dispatch-discipline section): on a partitioned repo (`.planning/workstreams/` present),
    every mandate MUST additionally name (1) the target compartment EXPLICITLY in the mandate text
    itself — never left for the worker to infer from `GSD_WORKSTREAM` already exported in the
    manager's OWN shell, which isolates nothing between several workers the manager itself
    dispatches, since they all inherit the same `CLAUDE_CODE_SESSION_ID`
    (`CLAUDE_CODE_CHILD_SESSION=1`, D-10's OPEN half) — and (2) a `GSD_SESSION_KEY` value distinct
    per mandate whenever more than one worker is dispatched concurrently against different
    compartments in the same mission, so that a worker's own `--ws` omission cannot silently fall
    back to a pointer another concurrently-dispatched worker just set. State explicitly that this
    does not change or patch gsd-core's own engine (D-05 — no upstream workflow is rewritten): it is
    a VF-side dispatch discipline only, closing the collision `ADR-069`'s 2026-09-09 amendment names
    OPEN for VF's own team model.
  </action>
  <verify>
    <automated>bash -c '
a=$(grep -c "GSD_SESSION_KEY" plugin/dev-orchestrator/agents/vf-dev-manager.md)
b=$(grep -c "CLAUDE_CODE_SESSION_ID" plugin/dev-orchestrator/agents/vf-dev-manager.md)
[ "$a" -ge 1 ] && [ "$b" -ge 1 ] && echo OK || echo "FAIL a=$a b=$b"'</automated>
    <fails_when>prints FAIL — the dispatch gabarit does not mention `GSD_SESSION_KEY` or does not name the `CLAUDE_CODE_SESSION_ID` sharing mechanism that motivates it</fails_when>
  </verify>
  <done>vf-dev-manager.md's dispatch section now requires an explicit per-mandate compartment name and a distinct `GSD_SESSION_KEY` on concurrent multi-worker dispatch against a partitioned repo, closing D-10's OPEN half for VF's own team model without touching gsd-core.</done>
  <acceptance_criteria>
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` names `GSD_SESSION_KEY` at least once, in the dispatch-discipline section (not merely a passing mention elsewhere).
    - The new paragraph does not claim to patch or rewrite any gsd-core workflow (D-05 respected).
    - The existing "Chaque mandat embarque le digest de mission" paragraph is not deleted or contradicted, only extended.
    - `plan 39-03`'s Task 1 (this file's companion, corrected the same day) exercises this exact amendment with ≥2 concurrent workers — cross-reference present in 39-03-PLAN.md's `read_first`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research file / CONTEXT.md decisions -> REQUIREMENTS.md / ADR.md prose | facts transcribed into durable governance documents; must not smuggle directives from ingested text (untrusted-input-boundary discipline) |
| upstream draft file -> future human posting action | the draft is content only; no network call, no forge API, no auto-post path exists in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05 | Tampering | `.planning/REQUIREMENTS.md` / `docs/ADR.md` / `workstreams.md` edits | low | mitigate | Every edit uses scoped `Edit`, never whole-file `Write` — prevents a destructive overwrite of unrelated ledger/ADR content outside this plan's diff. |
| T-39-06 | Information Disclosure | Upstream draft issue file names internal measurement commands and repo paths | low | accept | File is explicitly marked "not posted" behind a French bandeau reserved for human review (ADR-031); no secrets or credentials involved, only public repo paths and commands that would themselves be public once (and if) posted. |
| T-39-07 | Tampering (accidental) | Re-running ADR-069's `docs/ADR.md:1992-2002` snippet live | low | mitigate | The snippet is read-only against `$HOME/.claude/gsd-core/workflows/*.md` — it writes only to its own `mktemp` files, never to this repo; executed verbatim, not modified. |
</threat_model>

<verification>
Run Task 1, 2, and 3's `<verify>` commands in sequence — each must print `OK`. Confirm no other
line of `.planning/REQUIREMENTS.md` outside the new subsection, the nine new traceability rows, the
GSDA-19 body/annotation, and the tail changelog line was touched (`git diff --stat
.planning/REQUIREMENTS.md` should show one file, no wholesale rewrite).
</verification>

<success_criteria>
- Success Criteria 1, 2, 5, and 6 (Phase 39 ROADMAP) are each backed by a `PART-xx` ledger bullet
  with fichier:ligne citations.
- D-09 and D-10 — the two facts CONTEXT.md marks mandatory to grave — are written explicitly into
  ADR-069's dated amendment, neither left implicit.
- `GSDA-19` is superseded in place (body preserved, per MANI-04's precedent) without being reopened; its upstream draft exists and is
  explicitly not posted.
</success_criteria>

<output>
Create `.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `scripts/hooks/post-merge` — new opt-in git hook (plan `39-01`)
- `plugin/conductor/scripts/check-divergence.sh` — new S2+S4+S5 gate (plan `39-01`)
- `plugin/conductor/scripts/tests/test-check-divergence.sh` — test suite + mutation-red proof (plan `39-01`)
- `.github/workflows/ci.yml` — check-divergence.sh armé sur le job `gates` (plan `39-01`, arbitrage Samuel 2026-09-10, C17)
- `PART-01`..`PART-09` requirement family in `.planning/REQUIREMENTS.md`, + traceability rows + tail changelog line (**this plan**)
- `GSDA-19` superseded (body preserved) by `PART-06` in `.planning/REQUIREMENTS.md` (**this plan**)
- `.planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md` — drafted, unposted upstream issue (**this plan**)
- `docs/ADR.md` § ADR-069 dated `### Amendement 2026-09-09 — ...` subsection + amended Statut cell (**this plan**)
- `plugin/dev-orchestrator/references/workstreams.md` §4(b)/(d) updated (**this plan**)
- `plugin/dev-orchestrator/agents/vf-dev-manager.md` — dispatch gabarit closes D-10 OPEN half (**this plan**, C4 correction)
- Clone-jetable proof-of-mechanism run against real `vibeflow-os` history (clone destroyed, mechanism-only nuance stated) (plan `39-03`)
- `.planning/STATE.md` — D-02 dated resumption trigger + D-06 event-based watch note (plan `39-03`)
- `.planning/ROADMAP.md` — cross-reference to the D-02 trigger (plan `39-03`)
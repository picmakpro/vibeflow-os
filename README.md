<div align="center">

# VibeFlow OS

**English** · [Français](./README.fr.md)

**Claude Code is powerful. VibeFlow makes it reliable, frugal and governed.**

**Spec-driven** agentic orchestration for Claude Code: you speak plainly, an agent detects the
intent, runs the pipeline (scoping → plan → execution → proof), and **machine gates** verify —
not promises.

[![Version](https://img.shields.io/badge/version-2.57.0-2563eb)](./VERSION)
[![Claude Code](https://img.shields.io/badge/Claude%20Code-plugin-d97757)](https://docs.claude.com/en/docs/claude-code)
[![Modules](https://img.shields.io/badge/modules-17-16a34a)](#-modules)
[![License](https://img.shields.io/badge/license-source--available-64748b)](./LICENSE)

[The dev cycle](#-the-dev-cycle--spec-driven) · [Missions](#-long-missions--the-team) · [Labs & design](#-beyond-dev--a-lab-for-any-domain) · [Memory](#-memory-that-holds) · [Install](#-install) · [Modules](#-modules)

</div>

---

📖 **New here?** The [User Manual](./manual/README.md) walks a human through installing,
understanding and running VibeFlow — no `.planning/` or `docs/` required.

---

## The problem

AI coding setups fail at scale for three reasons: **context rot** (quality degrades as the
context window fills), **improvisation** (the agent codes without a spec, verifies from
memory, validates itself), and **burned tokens** (everything in context, everything re-read,
everything on the premium model).

VibeFlow attacks all three: **disk is the source of truth** (specs, plans, state — not the
context window), **nothing is "done" without machine proof** (tests, gates, fresh judges),
and **every token has a job** (sonnet workers, digests, on-demand loading, parallel dispatch).

---

## 🔁 The dev cycle — spec-driven

Say _"add Google auth"_: the `vibeflow-dev` agent detects the intent and runs the GSD pipeline —
scoping, verified plan, atomic execution, read-only judges — leaving an artifact on disk at
every step, so the context can die without the project losing ground.

→ [The cycle, step by step](./manual/en/04-development-cycle/the-cycle-at-a-glance.md)

---

## 🤖 Long missions — the team

"Do steps 3 to 5, I'm back tomorrow morning." Past a threshold, a **mission manager** takes
over on the **team-kernel**: sonnet workers run in parallel, typed reports replace prose, and
anything that challenges intent or security **freezes the node** for a human — even at 3 AM.

→ [How a long mission holds together](./manual/en/05-agent-team/a-long-mission.md)

---

## 🧪 Beyond dev — a lab for any domain

VibeFlow is not a dev-only tool: it **manufactures labs** — governed workspaces for content,
growth, business or any other domain — on the same kernel, the same gates, and skills built
by `skill-creator` with an eval loop.

→ [What is a lab?](./manual/en/02-concepts/what-is-a-lab.md)

---

## 🧠 Memory that holds

A VibeFlow lab doesn't forget between sessions: indexed registries, agent memory that
capitalizes across sessions, and disk-first artifacts (`PROJECT.md`, `ROADMAP.md`,
`STATE.md`) mean any session restarts from a fresh disk, never a compacted context.

→ [Anatomy of an installed lab](./manual/en/07-under-the-hood/anatomy-of-an-installed-lab.md)

---

## 🏗 Architecture

A mandatory `conductor` baseline (team-kernel + machine gates) carries the domain
orchestrators — dev, design, mobile, three business bundles — plus governance modules
(`validator`, `consolidator`, `infrastructure-audit`). CI runs a "**fresh lab**" job: the
baseline installs into a blank lab and must pass its own gates with zero intervention.

→ [The machine gates](./manual/en/07-under-the-hood/the-machine-gates.md)

---

## 🚀 Install

Two commands, no clone, no config editing:

```bash
claude plugin marketplace add picmakpro/vibeflow-os
claude plugin install vibeflow
```

Then inside Claude Code: `/vibeflow-install` — pre-detected scope (one-tap confirmation),
module picker, dependencies resolved and recapped before anything is written. Update:
`/vf-update`. Details: [INSTALL.md](./INSTALL.md).

---

## 📦 Modules

17 modules, each versioned with its own `CHANGELOG.md`. At install: `conductor` is the
**mandatory baseline**, then one choice — *dev lab* or *tailor-made domain lab*. Each
module's README is its full documentation — same structure everywhere.

→ [Module catalog](./manual/en/03-modules/catalog.md) ·
[commands](./manual/en/06-reference/commands.md) ·
[skills](./manual/en/06-reference/skills.md) ·
[agents](./manual/en/06-reference/agents.md)

---

## 🔒 Trust

- **Source-available**: public code and history — see [LICENSE](./LICENSE).
- **Auditable**: bash + `jq`, every script covered by its suite (68 suites in CI — the newest
  ones prove the dev-scope hooks' exec form as actually installed, their exit-code contract, and
  the shared Python resolution), **idempotent** install with backup before overwrite.
- **The repo applies its own doctrine**: CI on push/PR (tests + strict gates) + a
  "**fresh lab**" job — the baseline is installed into a blank lab and must pass its own
  gates with zero intervention.
- **Zero plugin-level hooks**: nothing runs until you invoke it. Module governance hooks are
  written by `/vibeflow-install`, in plain sight.
- **Anti-hallucination by design**: routing relies on a factual index auto-generated from
  disk; incomplete modules are flagged `proposable:false`, never sold.

---

## 🧭 Versioning

**Semver per module** + tagged root version on every release (`check-release-tag` as a gate).
Full history: **[CHANGELOG.md](./CHANGELOG.md)**, the single canon — the table below is an excerpt of the most recent releases, deliberately unnumbered so it cannot drift out of sync again:

| Version | Date | Change |
|---------|------|--------|
| `v2.57.0` | 2026-08-23 | **Requirements now survive a milestone closure: the ledger is no longer wiped and regenerated from scratch, and a requirement can no longer vanish without a trace.** Phase 18 (3 plans), PR #51, `dev-orchestrator` v2.19.0. **Shipped**: `restore-requirements-ledger.sh` (LEDG-01) — pure post-processing, strictly read-only on the archive, writes only under explicit `--write`, refuses to overwrite a live ledger unless the separately-named `--overwrite-live` is passed (which takes a `.bak-<milestone>` first); replayed on the **real** `agentique-v1.0` closure: 136 IDs → 93 guarantees + 42 carried + 1 obsolete = **136/136**, bodies reprinted verbatim to the byte (D-18-13: recognising a status is never normalising it). **`check-requirements-survival.sh`** (LEDG-02) — a reader/diff, never a regenerator: it compares archive IDs against the living ledger and names those that disappear without a traced outcome; four signals, three QUAL-01 issues including the **loud** unparsable one (an illegible `MILESTONES.md` is never a green). Doctrine **D-18-14** in `AGENT.md`: archives are snapshots, `.planning/REQUIREMENTS.md` is the only living source. **Zero new object in the substrate** — no registry, no merge grammar, no overlay. **The method number**: not one defect was found by its author's own tests. The classifier knew only `Complete|Pending` while the real archive holds **zero `Pending`** and speaks `Done` (44) and `Planned` (19) — **86 requirements of 136 silently dropped**, with the counters still summing correctly: the sum only ever proved nothing evaporated during sorting, never that anything landed in the file. A proposed fix keying on the checkbox reached 136/136 on presence and filed all 19 `Planned` as delivered — on a closed-milestone archive every box is ticked, so the signal discriminates nothing; dropped on measurement. Three security defects proven by execution (directory traversal, ancestor symlink — a committed relative symlink fires on any puller's `SessionStart` —, and an unguarded `cp` overwriting the live ledger with an empty file while reporting `exit 0`), then 7 adversarial vectors replayed: no bypass. Windows portability **falsified in both directions**: three CRLF symptoms closed, and the mutation removing the normalisation brings all three back in CRLF and none in LF. Two **empty** greens caught: `check-agents.sh` bare returned `exit 0` against a non-existent directory, and 13 suites were measured out of the 68 CI discovers — the narrow scope hid a red CI. **68 suites** green locally **and** in CI. |
| `v2.56.0` | 2026-08-17 | **A mission stall no longer survives the next gesture of a living session — and notifications, now off by default, only fire when it matters: end of phase and end of milestone, on the Claude app.** Phase 33 (5 plans) + notifications annex (2 plans), PRs #49 and #50, `conductor` v1.28.0 / `dev-orchestrator` v2.18.0. **The watchdog**: two clocks on the SAME beat (`heartbeat_epoch` liveness + `progress_epoch` written by `dag.sh mark` — one mechanism, two consumers); stall = alive but progress frozen beyond 900 s (strictly under the TTL), both clocks dead = abandon; the "alive but looping" case is proven by the REAL loop, and it validated itself: **the watchdog caught its own mission's stall in production** during integration (heartbeat fresh at 135 s, progress frozen at 1303 s) — a signature observed, not forged; it signals and suggests, **never kills** (ADR-031); a check-point at the `mark` gesture (best-effort stderr signal, never blocking, **never gated** by the toggle — mute the comfort, never the alarm); arming through the arming↔precondition gate (PORT-05 in CI), never a local settings. Goal **amended on measurement**: Claude Code hooks are strictly event-driven (no platform clock) — "a stall does not survive the next gesture of a living VF session", limit assumed in writing; zero system daemons. **The notifications**: **default OFF**, preference via a sentinel file (the `stop-notify` pattern, machine scope — no JSON, no new hook, no exposure to the `merge-hooks` idempotence bug); **`/vf-notify`** skill (`on`/`off`/`status`/`test`); milestones to the Claude app via **Pattern H** — `PushNotification` **is not provided to subagents** (measured, not read: managers and workers structurally cannot push, a replay of the earlier `AskUserQuestion` case), so a `SendMessage(main)` relay lets the main session emit, the only symmetric vector for BOTH milestones; node completions stay OS toasts when enabled, never on `running`. **The method number**: four brief premises invalidated by measurement (including `VF_NOTIFY_FORCE_CHANNEL=none`, which **was not a kill switch** — it silenced by accident, untested); two external plancheck rounds (23 then 10 blockers where the internal checker said 0 — fourth phase in a row); two defects reproduced by execution; two blockers found **by measurement, not by tests** (scripts committed 644, failure swallowed by `try/except` — the exact failure mode this phase fights); on the annex, an unbumped module would have left the doctrine **never** reaching a lab, two blockers were introduced **by the fixes themselves**, and case N17 — the one proving default OFF — was **blind 17 % of the time** (25/30 → 30/30 after hardening). Field note: **eight workers stopped silently during this very mission** — the watchdog's empirical argument. Suites: `dag.sh` 99→156 cases, `driver-lock` 183, `notify.sh` and `vf-notify` born — **66 suites** green locally **and** in CI. |
| `v2.55.1` | 2026-08-17 | **Hotfix: on Windows, the engine wrote hooks pointing at dead paths — and `\|\| true` swallowed the error, so the guards stopped running with nothing saying so.** Reported by a Windows tester, measured on the same `.claude/settings.json` before and after the update: 26 shell-form entries / 0 corrupted → 21 entries / **20 corrupted**. **Root cause**: the scripts prefix travelled to Python through an environment variable; under Git Bash, the MSYS2 runtime rewrites such variables when the target is a **native** Windows binary — precisely the python.org Python our own error message tells users to install. The separator became the MSYS root (`"$CLAUDE_PROJECT_DIR"C:/Program Files/Git/.claude/scripts`) and went straight into `settings.json`; since those entries end in `\|\| true`, the error was swallowed — a mute guard, the failure mode this repo has named since QUAL-01. Not a v2.55.0 regression: the offending line dates back to v2.14.0, only the number of entries taking that path changed with Phases 30→32. **Shipped**: **file-based transport** (`WIN-PATHCONV`) — a file's contents are never rewritten by the runtime, only `argv` and the environment are; `FRAGMENT`/`SETTINGS`/`BASH_ABS` **deliberately** stay variables (real paths, where the conversion helps), `PREFIX` being the only passenger that was not a path to open but a literal to inject into JSON; **a guard** — a prefix bearing the mark of a conversion halts the merge with a diagnostic naming MSYS2, and **nothing is written**; **`docs/WINDOWS-HOOKS-PATHCONV.md`**, an operating protocol addressed to the assistant on the affected machine (measure before writing, repair in simulation by default, verify by actually executing, escalate to an issue otherwise). **Not proven, and written as such**: the root cause is inferred, no Windows machine being available here — the signature matches exactly, but confirmation rests on a measurement only the tester can produce; the fix is sound regardless of that verdict. **The method number**: two false greens, both inside this hotfix, both caught by exercising what had just been written — the first T25 polluted `PREFIX` from the caller and passed without the fix too (the engine resets that variable internally: the test exercised nothing), redone with a `python3` shim that rewrites the environment the way MSYS2 does; and both patterns in the repair doc ignored JSON escaping (`\"$HOME\"C:`), returning a falsely reassuring "0 to repair" — caught by replaying the whole protocol on a reproduced corrupted case. T24/T25 red under isolated **and** combined mutation. Suites: 36/36, 19/19, 8/8. |
| `v2.55.0` | 2026-08-17 | **The driver-lock stops being declarative: the workarounds observed during agentique-v1.0 (concurrent commit and checkout under someone else's lock) are now blocked at the source, and a stale lock is never silently stolen again.** Phase 32 (7 plans), PR #46, `conductor` v1.26.0 + 5 doctrine bumps. **Shipped**: **LOCK-01** heartbeat separated from the lease — a living manager renews its beat without raising the TTL (a stale lock ≠ a dead mission, the 2026-08-02 finding finally closed); **LOCK-02/03** `guard-driver-lock.sh` born (80 cases) on `PreToolUse` matcher `"Bash|Write|Edit"` — commit AND branch checkout/switch under another's lock refused BEFORE git runs, the `Write|Edit` path bounded to `.planning/` (the real I2 incident went through native tools, not Bash), the deny motive rendered to the model word for word with the exact recovery command and named escape hatches (worktree, explicit override marker), the holder always passing; resistant to newline chaining, heredocs followed by a command, `git -C`/`-c` global options — every guard proven red under mutation; the entry is born from `merge-hooks`, the command lives in `--settings-local`; **LOCK-04** `acquire` never recovers a stale lock — explicit `takeover`/`reclaim` verbs, append-only journal, mutex under `trap`, the legacy double-holder hole closed (nominal on Windows Git Bash without symlink privilege); **LOCK-05** the `Fence:` trailer on mission commits, the convention written in `mission-flow.md` — the path managers actually read; **QUAL-01** a generic `SessionStart` **hook doctor** (assumed scope extension): the fleet's 26+ entries wrote health markers **no consumer ever read** — measured actually loud, 0-byte nominal silence. Doctrine resynchronized across 7 files in 4 third-party modules (full sync arbitrated by Samuel — no prose of the old contract survives). **Research before the plan**: `reference-transaction` proven **unusable for blocking** (wedges `rebase` by refusing its own `--abort`, breaks `worktree add` — the prescribed escape hatch —, trivially bypassed via `hooksPath`, which four of our own scripts already use); and not 2 workarounds but **4 real incidents over 3 gestures**, all replayed as mutation-red test cases. **The method number, third phase in a row**: two internal verifiers said "PASSED, 0 blocker", fresh judges found **9 plan blockers** then **1 in the code** nobody had seen — the guard truncated at the first `<<`, so a heredoc followed by `git commit` passed silently: incident I4 reopened with every test green. Debt traced rather than hot-fixed: `merge-hooks.sh` cross-matcher idempotence (`CONCERNS.md`). Human checkpoint 32-03 validated on evidence; **this repo is NOT armed — real arming of labs ships with the `conductor` module release via `/vf-update`**. Suites: `test-driver-lock.sh` at 151 assertions, `test-guard-driver-lock.sh` born (80 cases), `test-check-guard-health.sh` born — **64 suites** green locally **and** in CI. |
| `v2.54.0` | 2026-08-16 | **The install engine becomes predictable: every real install now writes a file-by-file manifest of what it placed, and `--dry-run` shows exactly what will be written before anything is.** Phase 31 (8 plans, issue #20), PR #45. **Shipped**: the pose manifest `scripts/.vibeflow-manifest-<module>` (one path per line, relative to the scope root) — `uninstall` and update convergence read it instead of rebuilding the list from the cache, closing at the source the hole `retired-modules.txt` patched by hand; **`--dry-run`** on `install` and `update` — file-by-file plan on `stdout` (`[plan] + / ~ / -`, the exact format proposed by the issue's author), diagnostics on `stderr`, **zero disk writes** proven at disk-content grain (T10), and plan and pose emitted by the **same code path** so plan/pose drift is structurally impossible rather than merely tested after the fact; loud refusal (`exit 1`) on `uninstall`/`rollback`/`status`/`sync` — never a silently swallowed flag (T14); `merge-hooks.sh` learns a **`plan` mode** rendered by the real merge code, no second path able to say something different (Tp1–Tp7, incl. bit-for-bit unchanged target, Tp3); update convergence removes what a module no longer ships **with prior backup** (T17–T22), total loud abstention on any dubious manifest (T19), and a **physical** out-of-root guard (`cd -P`/`pwd -P` — ADR-054 bans the `realpath` binary, not physical resolution, D-31-15) covering symlinked ancestor directories (T18, mutation-tested); `uninstall` applies the same guards and **never deregisters a module whose files it failed to remove** (D-31-16); `/vibeflow-install` step 5 and `/vf-calibrate` step 4 now show the plan before the real pose. **The method number**: no self-declared green survived — the internal plan checker reported 0 blockers where two external readers found 11, and an md5 proof over 262 files (nominal path) coexisted with 4 blocking regressions on the failure path: under `set -e`, moving a call changes its failure semantics (D-31-13). Decisions D-31-11→16; mission report in `.planning/missions/`. Suites: `test-manifest.sh` born (62 cases), 34/34, 19/19 — **62 suites** green locally (bash 3.2) **and** in CI (Linux/bash ≥5). |
| `v2.53.1` | 2026-08-16 | **Hotfix: the exec-form hooks shipped in v2.53.0 were dead on every user-scope install — literal `"$HOME"` in `args`, never expanded because exec form runs without a shell.** Reported on first session after update: 6 `SessionStart:startup hook error … No such file or directory` (the 5 dev-orchestrator signals), plus the software-architecture `guard-file-size.sh` PreToolUse entry — the Iron Law 300L gate — dying **silently** from the same cause. Root cause: `scripts_prefix_for_scope()` emits shell-quoted prefixes (`"$HOME"`, `"$CLAUDE_PROJECT_DIR"`) designed for shell form; in exec form no shell ever runs and the harness only substitutes its own placeholders (`${CLAUDE_PROJECT_DIR}`, `${CLAUDE_PLUGIN_*}` — official hooks doc), so the literal stayed as-is. The coverage hole: every suite exercised exec form in **project** scope (settings.local.json), never user scope with `"$HOME"`. **Shipped**: `merge-hooks.sh` derives an exec-safe prefix for `args` — `"$HOME"` → absolute path resolved at install (legitimate: the user target is per-machine), `"$CLAUDE_PROJECT_DIR"` → harness placeholder `${CLAUDE_PROJECT_DIR}` (portable, committable) — shell form untouched; plus a guard failing the merge on any residual shell-quoted literal in args. Regression tests T22/T23 (27 cases green), engine suite 19/19. Fleet repair: re-running `/vf-update` re-merges and replaces the dead entries (dedup by basename). |
| `v2.53.0` | 2026-08-16 | **A VibeFlow install on Windows now lays down hooks that actually work — the hook substrate is rewritten once and for all, and both external latencies of the milestone left on day 1.** Phase 30 (9 plans, 4 waves + surgery), PR #43. **Shipped**: `merge-hooks.sh` learns the **exec form** (`args`) — substitution, cross-form AND cross-target dedup, `remove` sweeping both targets, full backward compat with the shell form the installed base carries; **scope-bounded routing** decided mid-mission on a real measurement: only entries carrying a machine path (`{{VF_BASH}}`) go to `settings.local.json` — a machine-specific path never travels in a committed file (the inverse lesson of issue #38), LOCK-02 amended with the dated arbitration; shared lib `plugin/_internal/lib/vf-portable.sh` written **in strict conformance with the PR #29 interface contract** (Willy's tracer 01-01 absorbed — the locator block is byte-identical across its 4 consumers, proven by `cmp`); exit codes normalized across the **whole** parc (12 scripts, 0 = silence at the harness boundary, translation conditioned on `--hook` so the CLI contract survives), 26 hook entries inventoried and explicitly classed advisory/blocking (`docs/HOOKS-CONTRAT-SORTIE.md`); the 5 dev-polarity entries migrated to exec form (ADR-071); `check-hook-paths.sh` — an advisory SessionStart net invoked with a **bare** `bash` so it still runs when the absolute path it diagnoses is broken, BOM-tolerant; the local scope finally gitignores `.claude/settings*.json` (the review invalidated the "out-of-repo convention" comment by testing it on a fresh lab). **Day 1 gestures**: upstream RFC filed as `open-gsd/gsd-core#3556` (fallback GO-RÉDUIT written at filing time) and the gsd-core >1.10.0 release watch active (daily cache, never the `next` dist-tag). **The method number**: five false greens caught during the mission — all by an external judge, never by the author — one single motif (a report claiming coverage the disk did not carry); the counter-poison is now team memory: a test case must be green on sound code AND red under mutation, true in one state it tests nothing. The fifth was caught by the push itself: 61 local suites green, CI red — the PORT-05 proof counted a hand-installed universe the job never builds; the expectation is now **derived** from the actually-installed closure, never a literal. Also: `vf-dev-manager` v2.17.2 — human gates now escalate **live** to the main session (`SendMessage`) instead of freezing the mission. Modules: 7 bumped. **61 suites** green. |
| `v2.52.0` | 2026-08-15 | **The framework's memory stops running on empty: the registers are finally instantiated, and every project gets its own.** Reported from the field ("my memory logic has been gone since I installed the dev module"), then **measured on the metal** in a virgin repo with the real engine: `consolidator` installed in full — hooks wired, guards in place, 5 templates dropped under `skills/consolidator/references/templates-memoire/` — and yet `.claude/memory/` **did not exist**. No script ever fetched those templates: laid down, never instantiated. `check-registres.sh --strict` exited rc=1 on all 5 canonical registers, and **CI never saw it** because it passed `--allow-empty`, which converts that rc=1 into rc=3 "indeterminate", then accepted rc=3 — validating a lab with no memory by mistaking it for a fresh one. **Shipped**: `seed-registres.sh` (consolidator v1.9.0), called by a 4th named post-install hook **and** by `sync_module_governance` — that second call site is what makes it transparent on `update`, since the "module already current" path never re-enters `install_module`; **non-destructive contract without exception** (it only ever creates what is missing, registers being append-only), verified **by checksum** rather than by file presence; data-driven (`*-template.md` → `<NAME>.md`, no hardcoded names). **The `user`-scope hole, found by re-reading the diagnosis**: scripts there live in `~/.claude/`, so the seeder filled the **account's** memory while the lint and read guards resolve `.claude/memory` **relative to the cwd** — home full, every project empty. An install cannot know in advance which projects will use it; session startup can: `--project` mode + a `SessionStart` hook, with a lab guard (`.planning/` or `.claude/` required — a plain git repo is never seeded) and a `VF_NO_AUTO_SEED` escape hatch. **Isolation proven**: two labs, two memories, no leakage. `consolidator` becomes **`mandatory`** (it only ever arrived via the indirect `conductor → validator` edge) and `memory_bridge.enabled` flips to `true` — **declarative, stated plainly**: no script reads that key, the bridge is enforced by the SKILL. **MemPalace: refused in writing, with the measurement** — neither CLI nor MCP available, enabling it would insert 7 stages that fail every phase; its own contribution (semantic recall) is complementary rather than redundant, and its resumption trigger is on record. **CI**: baseline derived from the catalogue's `role=mandatory` entries (it hardcoded `conductor`), `--allow-empty` removed, `seed-registres.sh --check` added. Modules: `consolidator` v1.9.0, `planning-core` v2.6.1. **55 suites** green (54 → 55, `test-seed-registres.sh`: 19 cases). |
| `v2.51.0` | 2026-08-15 | **The ICM deep-search gains are distilled into the tooling — the mechanisms, never the label — and the `dag.sh --scope` foundation crosses the phase without a single line changing.** Phase 29 (5 plans, 13 tasks, `ICMD-01..12`), PR #41, plus Phase 28 plans 01-02. The report `reports/research/2026-08-15-icm-deep-search.md` (4-agent deep-search on arXiv 2603.16021) concluded "nothing to adopt as-is, 5 mechanisms to distill" — and the one-way D-03 constraint held **by construction**: the prior investigation restored the real history (`--scope` declared in Phase 20, D-13 of `20-CONTEXT.md`; Phase 27 only added `stages` + the RCE closure) and proved the "perimeter negative" composes entirely from fields already emitted — `dag.sh`/`test-dag.sh` absent from the whole branch diff, the 99-case suite replayed green at every stage. **Shipped**: **G3** `check-map-drift.sh` (conductor) — the map↔disk anti-drift gate that was structurally missing (existing gates detect immobility, never inconsistency), lint-only (ADR-031), 0/3/64 exit grammar with a `NON VÉRIFIABLE` floor, bounds implemented **as classes, never as output filters**, after a human verdict on a real run (13 → 3 findings, maps never touched); **G1** the "does NOT load" bullet in the mission digest, written by the manager from the node's `scope[]` — anti-loading becomes a first-order instruction; **G5** the Edit-Source Principle in `team-kernel.md` (a recurring correction means amending the source — never re-dispatching the same fix); **G2** `scaffold-docs.sh` gains `_index.md` (reference folders > 10 files), the ≤ 80-line constraint, and its **first test suite**. **The method number**: 6 judge passes on the gate, 6 real defects, none found by the worker's own suite — closed by generative proof (45 combinations, 10 red before / 0 after); plus one test case attesting against mutable `HEAD`, permanently red once its fix was committed — fixed by pinning the SHA. `parallelization.skip_checkpoints` returns to `false`. **Phase 28 (01-02)**: rules 4/4bis of `check-capability-activation.sh` — arming `isolation:` or an MCP server without its distributed precondition now exits non-zero (41/41 cases), the structural generalization of issue #38; plan 03 *as-installed testing* stays open. Modules: 4 bumped (`conductor` v1.22.0, `dev-orchestrator` v2.14.0, `validator` v1.3.3, `reference` v2.5.3). **55 suites** green (52 → 54), counter re-derived, never copied. |
| `v2.50.1` | 2026-08-10 | **Fix: `isolation: worktree` removed from all 13 writer agents — the line shipped in v2.49.0 made team workers unusable as soon as a manager mandated a branch.** Issue #38, PR #39. The harness worktree forks from the **default branch**, never from the current HEAD: when a manager had prepared a branch for the mission, the worker landed on a technical branch **with none of the mandate's files**, declared itself blocked within two minutes without producing anything, and the manager fell back to a generic agent lacking its doctrine, its allowlists and the toolchain it knows how to dispatch — **invisible failure from the user's side**: the mission progressed, just not with the intended agent. **The cause is not the line, it is what failed to travel with it**: the precondition that fixes the fork, `worktree.baseRef: "head"`, lives in the **machine's** settings — it had been set in the development repo's local settings during Phase 27, and the engine sets it **nowhere** on a user's machine (verified: zero occurrences of `baseRef` in `vibeflow-update.sh`, `merge-hooks.sh` and the installer). The 13 agents therefore shipped **armed without their precondition**. And even shipped it would not have sufficed: nothing brings the worker's commits back to the mission branch — the asserted merge is implemented nowhere upstream (`open-gsd/gsd-core#3302`), which was **already** the motive for refusing `claude_orchestration` in that same Phase 27. The fix therefore covers the **13 agents across 6 modules**, not the 2 reported: the cause is identical everywhere, and repairing only two would have left 11 workers broken. **Machine guard**: `check-agents.sh` validated the key's *shape* ("only `worktree` is admitted") without ever questioning its *legitimacy* — it now rejects any `isolation:` in a distributed agent, with the motive and the lifting condition written on the spot (ship the precondition **and** prove commits return). `team-kernel.md` carries the rule plainly: **isolation is a manager's dispatch decision, never a worker property** — which its own doctrine already said, and which the frontmatter contradicted by making it unconditional. **Also**: the claim "the README keeps the last 3 entries" is gone from both READMEs and the CHANGELOG — they listed 13. Replaced by an **unnumbered** phrasing that cannot drift again. Modules: 7 bumped. **52 suites** green. |
| `v2.50.0` | 2026-08-10 | **The design toolchain stops degrading silently — and the detection that watched over it was blind to half the problem.** Quick task `260810-fh3`, PR #36. The `design-orchestrator` module depends on 4 external plugins (`ui-ux-pro-max`, `frontend-design`, `impeccable`, `superpowers`): if one was missing, nothing blocked and nothing warned — the absence surfaced only in the final report, once the work had already been carried out on first principles. **The hole was wider than "it checks nothing"**: the repo's only tooled detection, `claude plugin list \| grep <name>` inherited from `ensure-deps.sh`, matches on the name alone — a plugin installed then **disabled** passed for present. Real case measured on the dev machine: `frontend-design@claude-code-plugins` disabled alongside `frontend-design@claude-plugins-official` enabled. **Shipped**: `scripts/ensure-design-deps.sh`, an **autonomous** bootstrap (the module declares only `conductor` — nothing can be borrowed from the dev module, so the shared shape is a deliberate duplication documented in its header) that checks **presence AND activation** and repairs non-interactively. Primary source `claude plugin list --json` (structured), `awk` fallback discriminating on the **words** `enabled`/`disabled` and never on decoration glyphs; no source → **indeterminate** state and manual steps, never a "present" by default. `installed_plugins.json` **explicitly ruled out**: it carries no `enabled` field — that is the very origin of the hole. Rule "at least one active entry of the same name suffices", and a disabled plugin gets a `claude plugin enable --scope`, **never** a bare `install` that would have reactivated nothing; any `id` derived from command output is validated (strict shape + membership in the 4 literals) before reaching a command line. **Double wiring**: a named post-install hook in the engine (install **and** update, best-effort, it can never amputate a module's install) + "Premier contact" in `AGENT.md`, under the Iron Law guard — the agent reads the output for itself and never restitutes raw plugin names. **The defect found in review, on the result rather than the plan**: the hook called the script with `>/dev/null 2>&1` while **everything** goes to stderr — an install where all 4 are missing and auto-install fails would have produced **not one line**, replaying the silent degradation one level up. Hence the split `log()` routine / `notice()` anomaly, an `ANOMALY` flag deciding whether the summary crosses `--quiet` (including when the action **succeeds**: an install placing plugins behind the user's back must stay visible), and the hook in `--quiet` without `2>&1`, motive written on the spot. **Two CI reds, one single root — machine context leaking into the deliverable**: 3 `cd /Users/<user>/…` copied into the PLAN from the planner's `files_to_read`, and a `T9b` that **measured the machine rather than the script** (it counted forced-dry-run commands without stubbing `claude`, hence green on an equipped machine and red by construction on a bare runner, where its neighbours `T9c`/`T9g` already stubbed). **Deliberately out of scope**: no version/freshness check — half the plugins carry `version: "unknown"`, it would be noise. Module suite 12 → **24 cases** (T9..T9g), discrimination proven by mutation on three distinct properties, then restored. Modules: 1 bumped (`design-orchestrator` v1.5.0). **52 suites** green, CI green on the Linux runner. |
| `v2.49.0` | 2026-08-10 | **Execution parallelism becomes granular and collision-safe by construction — and the engine-side path that promised more is tried for real, then refused in writing.** Phase 27, 6 plans, PR #35. The premise got reversed at framing: intra-step parallelism was never "lost", it is **off by default** — the doctrine now says so and names the real path (gate 4 `nested && background`, practical lock gate 5 `GSD_AGENT_SDK_VERSION`). **Shipped**: `dag.sh ready` gains `stages` — a machine partition of the ready frontier into stages with zero `scope[]` overlap, wired to upstream `partitionStages()` via subprocess (ADR-069, never reimplemented), additive for all 5 consumers, fail-closed `stages: null` fallback proven in execution (99-case suite green); worktree isolation scoped then armed — enumerated `.worktreeinclude` allow-list (single entry, `.claude/agent-memory/`), the 6 managers excluded with written motive, `worktree.baseRef: "head"` set under a blocking human checkpoint **before** arming the 13 writer agents (order as a mechanical precondition); wall-clock baseline captured **before** any activation (D-10), the 3.00× ceiling kept as stage compression, never a wall-clock gain (D-13). **Refused in writing, with the proof**: `claude_orchestration` — real SDK 0.3.223, persistence decided by human arbitration (real install under `~/.claude`, the only option that pins no value), the 7-gate ladder fully green **without the manual flag** on the real production path… and still **refused**: the real Workflow run diverges from the inline path exactly as FAIL criterion 2 states — no worker commits, no SUMMARY, no merge, residual worktrees. The upstream design audit confirms it statically: the asserted merge is implemented nowhere; the namespace half was fixed upstream (gsd-core 1.10.0, #3021), the remainder — the merge manifest is never populated — is now filed as **open-gsd/gsd-core#3302**, which becomes the objective resumption trigger. Decision-A sub-experiment safe (the agent's question surfaces in the report, nothing written). Measurement loop closed with no blank: `STATUT-BLOC-3: NON-MESURABLE` with motive and trigger. **Security**: the `dag.sh` CWD-relative RCE closed and propagated (ADR-070, `toplevel` variant in `mission-contracts.md`); `27-SECURITY.md` — 37 plan-time threats, 35 closed on cited evidence (gates re-executed), 2 open non-blocking kept open as such — including T-27-03-06, a hypothesis **refuted by the real run** (`GSD_WORKSTREAM` empty under a worktree) and promoted to `team-kernel.md` doctrine: a manager passes the workstream explicitly in an isolated worker's mandate. `threats_open: 0`. Modules: 7 bumped. **52 suites** green. |
| `v2.48.0` | 2026-08-05 | **The dormant capabilities of the GSD engine are activated, measured, or refused in writing — and four guards that reported green were doing nothing.** Phase 24, 12 plans, 6 human arbitrations. **Activated**: the `agent_skills` PLANNER slot (the lab's dev doctrine now reaches the agent that writes the plans), `intel` (our own `docs-flow.md` advertised `gsd-map-codebase --query` as a normal mode while the capability was off — a documented command that did nothing), `windows_enforce` and `workflow_guard`. **Refused in writing, with the measurement**: `hooks.community` (23 of 109 commits fail on type, 68 % of subjects exceed 72 characters — re-counted **in characters, not bytes**, since a byte count inflates accented French subjects and would manufacture a false motive), context profiles (ADR-068 — the key is validated and documented upstream but **no consumer reads it**), `graphify` and `profile-pipeline` (no prescribed reader). **Workstreams adopted** (ADR-069, Samuel's call **against** the framing's recommendation): four gates made workstream-aware and exercised in CI on a genuinely partitioned tree, `GSD_WORKSTREAM` adopted as the nominal channel (**ADR-064 amended** — the session pointer lives in `os.tmpdir()` keyed on the absolute path, so it never composes with worktrees), and **Iron Law 2 revised** rather than silently circumvented. **The security fix that matters**: a fourth pass of the symlink-escape motif, this one **live** — `.planning/workstreams/dev` as a symlink outside the lab caused the target's `STATE.md` to be injected into the session context at exit 0, with no victim action beyond opening a session. All four workstream-aware gates carried the hole, including the one that blessed the partition as "compliant" and on which the other three relied. The real deliverable is the diagnosis of **why it keeps coming back**: 6 implementations of one need across 3 languages, a safe primitive disguised as a business rule, and an anti-duplication check iterating over hardcoded paths — so a new script is in no roster. **Guards that were green while doing nothing**: a length bound on the nominal channel **inert on Linux** (`vf_ws_trim` forked `awk`; `execve()` caps each environment string at `MAX_ARG_STRLEN` (128 KiB), a limit absent on macOS — the fork failed `E2BIG`, the value came back empty, and a 200,000-byte input passed), an anti-regression assertion **comparing two empty outputs**, three false greens and one false red in `check-state-integrity.sh`, and a pointer gate certifying a name it had rewritten itself. Each closed **by mutation, never by re-reading**. **`effort:` by role on all 31 agents** (not 25 — the installer also copies `plugin/*/AGENT.md`, and the exhaustiveness proof had been run on the wrong universe), `check-agents.sh` hardened from "validates if present" to "requires". New gates: `check-capability-activation.sh` (ties a doc entry to its capability's activation — the structural cause of the three inert routes), `check-machine-paths.sh` (blocking), `check-workstream-pointer.sh`. **Threats: 56 carried by the plans, 0 verdicts recorded in the 11 summaries** — the gap found while writing `24-SECURITY.md`; 18 opened, closed one by one **on cited evidence**, never on a summary's word. `24-VALIDATION.md` ships `nyquist_compliant: false` **with its motives** — the honest gesture over a forged verdict, as Phase 23 had done. The running theme, in one line: **four times a correct count was taken over the wrong set** (25 agents/31, 47 suites/52, 8 modules/10, 1 holed gate/4), hence the rule the phase adopts — *every engraved figure carries its method and is re-derived at the moment of writing*. Modules: 10 bumped. **52 suites** green, CI green on the Linux runner (where it had been red for 5 runs while local macOS reported all green). |
| `v2.47.1` | 2026-08-04 | **The skills index stops lying about its own provenance, and the history gets the entry a release had skipped.** Origin: the `@opengsd/gsd-core` engine update from 1.9.0 to **1.9.1**, and the question of whether the coupling still held. **It holds across its whole surface, verified rather than assumed**: the `^1` ceiling in `ensure-deps.sh` covers 1.9.1, the `--claude`/`--global`/`--local` flags all exist in the 1.9.1 installer, the commands actually called (`loop render-hooks`, `state`, `state record-session`, `roadmap analyze`) all exit 0, and the **declared capabilities are identical** across both versions — index regenerated from each tarball's own registry, 12 hook points, 35 stages, byte-identical output. The upstream delta lands on non-Claude runtimes and a third discoverability registry, not on the contract consumed here. **The one active defect**, found by checking: the `gsd-skills-index.md` header announced `@opengsd/gsd-core@1.9.0` — a **hardcoded literal** in `build-gsd-index.sh`, i.e. an auto-generated AND version-controlled file asserting a false provenance, in head-on contradiction with the doctrine its sibling script states in its own header ("no engine version is ever frozen into the logic"). The version is now **read** off the resolved engine under a single rule — the engine is the parent directory of the workflows source the dual-layout cascade already resolved — so it always describes the tree the index entries actually come from, with no second cascade to maintain. Three edge behaviours, each held by a test: VERSION missing or unreadable → the header **says** "(version inconnue)" instead of asserting a number; VERSION untrusted → bounded 200-byte read then restricted character class (a port of `check-gsd-engine.sh`'s guard), command substitution neutralised and never evaluated; **legacy** layout → the index names `get-shit-done-cc` rather than dressing a legacy engine up as `@opengsd/gsd-core`. `T1e` is **DISCRIMINATING by construction** (fixture `9.9.9-fixture`), discrimination proven both ways: 4 KO with the frozen literal restored, 165 OK / 0 KO after. Module suite 161 → **165 cases**. **History debt settled**: v2.47.0 had been tagged and published without ever receiving its root CHANGELOG entry — the same defect class as the Releases page stuck from v2.29.0 to v2.39.0. Module `dev-orchestrator` v2.11.1. **47 suites** green. |
| `v2.47.0` | 2026-08-04 | **The dev engine is explicitly coupled to GSD, and the driver lock stops opening while it recovers.** Two strands. (1) **Phase 23 — explicit coupling** (10 requirements `GSDC-01..10`, 9 done): the module reasoned as if GSD were a list of skills to call, whereas 1.9 is a **capability engine** that inserts its own stages — `grep -r "capabilit\|render-hooks"` on `dev-orchestrator/` returned **zero**. Shipped: a **single invocation path** (the bare-agent route silently disabled research, pattern-mapper, plan-checker, gap-analysis, waves, verifier, code-review, nyquist and secure-phase, and still reported `passed`), a **strict-allowlist flag doctrine**, a capabilities/hooks table that is **generated from the installed engine** rather than hand-written — so it can no longer drift in silence —, a **checkpoint contract relayed and never recomputed**, and **one turn budget per step** instead of two loops adding theirs up. `GSDC-08` ships as `[~]`: the `D-22` discrepancy (`gsd-debugger` present against an "no exception" decision **and** required by gate `T19`) is arbitration, not code. **Two RCEs closed**, one reintroduced by a new script through a security premise never propagated between plans, proven shut by replaying the full install from a booby-trapped repo — the trap is proven **read**, not silently avoided. Suite 102 → **161 cases**. (2) **A recovery race in `driver-lock.sh`** (`conductor` v1.19.1): recovering a stale claim **moved** the lock directory before recreating it, and the normal `mkdir` path — unable to tell "free" from "being recovered" — walked in. Up to **5 simultaneous winners** measured on 24 concurrent acquisitions, on macOS as well as Linux; two window-narrowing fixes measured **worse** than the original (8 and 6). The lock becomes a **symlink** replaced by `rename(2)`: never absent, so never apparently free. Recovery is serialised by a generation-named mutex and the staleness verdict is re-read after it. Old directory-form locks stay readable and recoverable. `T13` goes from 6 concurrents in one draw to **24 × 5 rounds**, both bounds guarded. Modules `dev-orchestrator` v2.11.0, `conductor` v1.19.1. **47 suites** green. |
| `v2.46.0` | 2026-08-01 | **Tracking stops lying, and two sessions can no longer walk over each other unknowingly.** Three strands. (1) **Documentation hygiene** (Phase 22): the doctrine had an *inbound* path (`ingestion-flow.md`) but no *outbound* one — `docs-flow.md` separates the 4 families GSD maintains independently and that we had collapsed into a single line, surfaces the meaning-bearing flags (`--verify-only` audit without writing *vs* `--force` regenerate), and `vf-design-manager` gains the same `docs` node as its dev counterpart, **by reference never by copy**. (2) **The ROADMAP gets the checklist the engine actually reads**: billed as "20 missing SUMMARYs", the real defect was that the ROADMAP had **never** carried the phase checklist — the only form `@opengsd/gsd-core` reads — so the engine saw **zero** completed phases out of 24 and produced false counters. The checklist covers the 20 summary-less plans of Phases 11-14 **without fabricating a single file**: `roadmap.cjs` lets the ticked box override the disk, "for phases completed before GSD tracking". Phases 23-25 regularised under the `gsd-alignement` milestone. **2 upstream issues filed** ([#2956](https://github.com/open-gsd/gsd-core/issues/2956), [#2957](https://github.com/open-gsd/gsd-core/issues/2957)). (3) **One writer = one worktree** (**ADR-064**): two sessions wrote to the same branch unknowingly on 2026-07-31. The finding "the lock guards the step, not the branch" was incomplete — `driver-lock.sh` is consulted **only by managers**, and the offending session wasn't one. ADR-064 settles what ADR-059 left open: isolation becomes **physical**, and `check-branch-claim.sh` (4 exit codes, advisory, `SessionStart`) carries the claim to ordinary sessions at last. A symlink false positive found inside the gate while building it, mutant killed. Modules `conductor` v1.19.0, `dev-orchestrator` v2.10.0, `design-orchestrator` v1.4.0. **46 suites** green. |
| `v2.45.0` | 2026-07-31 | **VibeFlow aligned on `@opengsd/gsd-core` 1.9.0** — origin: the engine update from 1.8.0 on 2026-07-31, delta established on the evidence (`npm pack` diff of both tarballs + live install check). The one active defect: `inject-mcp-tools.sh` discovered MCP servers from `./.mcp.json` only — a server declared solely at **global scope** (`~/.claude.json`, e.g. XcodeBuildMCP) was invisible, `--verify` returned `3` INDETERMINATE instead of flagging the gap. Fixed with a **union of two scopes** (project ∪ global, `--claude-json`/`VF_CLAUDE_JSON`), independent degradation per source, project-over-global precedence on collision. Also shipped: the upstream `estimate:`/`actuals:` contract relayed verbatim by `vf-coder`/`vf-dev-manager` (never a self-graded statistic); **ADR-061**, a written arbitration of the overlap between upstream cross-AI review lanes and the code-review stage shipped in 20-06 (two distinct objects, kept separate); the dated `named-dispatch` assumption recorded in `team-kernel.md`, cross-checked against `gsd-worktree-path-guard.js` (#1995, #2608 — verified conformant); the version debt purge 1.8.0 → 1.9.0 across 6 files, moving the string-literal test case (case 8) with the text it checks, never neutralising it; **ADR-062**, arbitrating the 2 uncabled 1.9.0 hooks (correctly absent in both cases); and **`check-state-integrity.sh`** (**ADR-063**) — a new anti-regression gate for `.planning/STATE.md`'s frontmatter, now wired into the CI `gates` job, closing the exact silent-regression class (`completed_phases`/`total_plans`/`completed_plans` decreasing within the same milestone) discovered after Phase 20's closure. Modules `dev-orchestrator` v2.9.0, `planning-core` v2.5.3, `conductor` v1.18.0 (unchanged, verified consistent). **46 suites** green, `check-agents --strict` green on the 6 agent directories. |
| `v2.44.0` | 2026-07-31 | **Review becomes a first-class stage, driven by the manager** (**ADR-060**): it leaves `vf-coder`'s internal cycle, which stops being judge of its own work — the manager dispatches `vf-reviewer` and owns the fix → re-review loop itself. Origin: the **second external audit report** of 2026-07-28 (third-party lab, iOS slice in 5 batches of which 2 parallelised by worktrees, ~90 commits, suite 177 → 331 tests), whose 4 findings were **verified against the evidence before opening** — 3 confirmed, 2 of them **more firmly than the report claimed**, 1 partly outdated. **ADR-051 revised on its single contested point**: the premise "review agents never compile" conflated **producing** a compilation verdict with **verifying** one. `vf-reviewer` gets a **named** MCP allowlist (`vf-mcp-tools`, grammar `<server>:<tool…>`) — never the server wildcard, least privilege preserved — and the revision carries **its price in writing** (~90 s more per review, one simulator slot). **The 4 judges' write barrier stops being a fiction**: the absence of `Write`/`Edit` from `tools:` was **silently reopened at runtime** by `memory: project` (proven by probe) — they now carry `disallowedTools: Write, Edit`, a real constraint, without a single line of the gate moving. `vf-design-judge`, the only one keeping `Bash`, **stops claiming a barrier it does not have** and names its blind spot: shell channel open, restraint that remains a prompt commitment. Three non-negotiable guardrails bound every relaxation, drawn from the audit's own numbers: **never reduce the number of tests** (measured: out of 90 s of build, tests weigh ~1 s — no leverage), **never lighten review on the product critical path** (5 blockers found in one day), and **no relaxation ever applies to a remediation diff** (9 then 5 then 4 defects born from the review fixes themselves). Also shipped: `--scope` and `review_regime` (frozen perimeters), `check-mission-invariants.sh` + `.planning/MISSION-INVARIANTS.md` (dead-zone gate), and explicit third-party hook scoping (`--third-party-prefix`). Modules `conductor` v1.17.0, `dev-orchestrator` v2.8.0, `design-orchestrator` v1.3.2, the 3 bundles v2.0.3. **46 suites** green, `check-agents --strict` green on the 6 agent directories. |
| `v2.43.1` | 2026-07-28 | **A team mission works on its own branch, never on the default branch** (**ADR-059**). As soon as a manager is dispatched (`vf-dev-manager`, `vf-design-manager`), it creates its branch **before its first commit**, keeps every commit there, and ends with a **PR left open** — it never merges, merging belongs to the user (ADR-031 applied to integration). Origin: on this very repo, an autonomous mission landed **32 commits straight on `main`**, pushed then tagged; the recourse for a bad mission was a mass `revert` of already-public history. On a branch, the recourse is **not merging**, and the PR provides the grouped review point that an end-of-mission report — written **by** the agent that did the work, and read too late — does not replace. The trigger is **dispatching a manager**, not the nature of the work: direct conversational work stays outside the rule. Five fallbacks guarantee a mission **never** fails for want of applying it (no git repo · no remote · no `gh` · **dirty tree = halt condition**, never a self-decided `stash` · target project's `CLAUDE.md` wins). Does not cover isolating parallel waves **within** a mission — only `isolation: worktree` would, a decision left open. Modules `dev-orchestrator` v2.7.1, `design-orchestrator` v1.3.1. |
| `v2.43.0` | 2026-07-28 | The GSD engine enters `/vf-update`'s scope (**ADR-058**): the `get-shit-done-cc` → `@opengsd/gsd-core` migration shipped in v2.39.0 reached **no already-equipped machine** — only fresh installs. Observed on a third-party machine: plugin current at 2.42.0, engine still at 1.42.3 laid down **12 days** earlier, with nothing in the interface saying so. Three chained causes, all closed. `detect_gsd()` returned true on the legacy layout through an `||` written for dual-layout tolerance, and turned that into a `skip`: it becomes a **three-valued** state (`absent`/`legacy`/`gsd-core`) where "legacy" is **actionable**. No update path ever called the install script: `check-gsd-engine.sh` (new gate, F13 contract) is now probed by `/vf-update` — and **before** its "VibeFlow is up to date" stop, without which a machine with a current plugin never saw the offer. The legacy cleanup message, previously reachable through `/vf-init` alone, becomes reachable and **accurate**: `npm uninstall -g` is offered only when the package is genuinely global, the empty tree left behind by the installer is included, and the state is captured **before** the install — since the upstream installer deletes the legacy `VERSION` itself, the message could otherwise never fire again. Trap recorded in plain words: the fork **restarts from zero**, so **1.8.0 < 1.42.3 in semver** — migration is decided on the **package name and the directory layout**, never on comparing numbers, and a test pins that exact pair. ADR-031 upheld throughout: detect and **offer**, never install or clean without consent. `ensure-deps.sh --migrate-engine` chains into MCP re-injection (the upstream installer files the ADR-051 injection as a "local patch" and erases `mcp__XcodeBuildMCP__*` from `gsd-executor`'s `tools:`), and `inject-mcp-tools.sh --verify` compares the final `tools:` against the servers in `.mcp.json`. That last one first shipped **inert** — called without `--force`, it excluded its own target and always exited 3 — a defect cleared by code review, the portability gate **and** the security audit, caught only by mutating the shipped block. |
| `v2.42.0` | 2026-07-28 | Startup signals for the dev engine: `dev-orchestrator` — the only structuring module with **no hook at all** — gains its first `hooks/hooks.json` fragment. Three read-only, advisory `SessionStart` scripts state FACTS and inject short self-carrying lines. `check-dev-bootstrap.sh` covers the whole start continuum in one script (silence · `onboard` when code has no `.planning/` · `bootstrap` listing what is missing · `gsd-engine` orientation when complete), the signals proven mutually exclusive by test. `discover-unintegrated-docs.sh --hook` aggregates the count **additively**, leaving its historical `grain<TAB>path` contract untouched; `check-doc-drift.sh` flags code commits outstripping doc updates past a tunable threshold (default 20). The `gsd-engine` signal closes the routing hole observed on 2026-07-27 — `planning-core` steps aside when GSD holds a project and nothing took over. Portability **proven by execution**: identical counters on macOS bash 3.2.57, Debian 12 and Ubuntu 24.04, which rules out the silently skipped test. Two tautological test cases found and killed by mutation. |
| `v2.41.0` | 2026-07-27 | Agent dispatch fully fenced: `check-agents.sh` now lints the **contents** of `tools:` — allowlist syntax and name existence, on `tools:` and `disallowedTools:` alike (invented agent names, unclosed parens and non-existent tools all passed `--strict` green until now; suite 38 → 58 axes). Severity is graded by what is verifiable regardless of the installed scope, so native types (`general-purpose`) and external `gsd-*` agents never turn a correct allowlist red — closed-world checking is an opt-in CI mode. Allowlists posted on the 3 dev workers after a double independent census. Doctrinal correction: in a subagent definition the runtime **ignores** the names in parentheses — an allowlist is a documented contract enforced by this lint alone, not a runtime sandbox. |
| `v2.40.0` | 2026-07-27 | Cross-team dev ↔ design collaboration under a single manager: `vf-dev-manager` inserts `craft:`/`critique:` nodes into a dev mission (design stage skipped and reported when no art direction exists), `vf-design-manager` gains an opt-in implementation stage with dual judge (art-direction re-score ∥ code review) and separate 3 + 3 anti-thrash budgets, `vf-auto` finally routes design-only missions to the design manager, and both managers carry `Agent(...)` allowlists (18 / 6 names) forbidding manager→manager nesting (a documented contract — see the v2.41.0 correction on what an allowlist actually enforces). Kernel untouched. |

<details>
<summary><strong>Methodology references (ADR / LRN)</strong></summary>

- **ADR-032** — 4-pillar memory consolidation system
- **ADR-035** — AI-Safe software architecture doctrine
- **ADR-053** — Swarm layer: driver lock + DAG + typed reports
- **ADR-055** — Altitude boundary lab planning ↔ dev engine
- **ADR-056** — Runtime support vigilance
- **ADR-057** — Tooled boundaries with third-party bricks
- **LRN-101** — "Minimal agent + composable skills" pattern
- **LRN-106** — Audit before fix

Main lab (private): [vibeflow-lab](https://github.com/picmakpro/vibeflow-lab) — structural changes are tested there before release.

</details>

---

## 👤 Authors

- **[@picmakpro](https://github.com/picmakpro)** — creator of the VibeFlow methodology and repo owner. Laid the project's foundations and remains the guardian of its doctrine: the governance backbone (`conductor`, `planning-core`, `consolidator`), the scriptural hooks and guards that keep every lab honest. VibeFlow's identity — governance enforced by tools, not by prose — is his.
- **Samuel Neveu — [@samuel-neveugall](https://github.com/samuel-neveugall)** — the project's driving force day to day: main contributor and release driver. Built the entire development side (`dev-orchestrator`, `design-orchestrator`, `mobile-test-team`), led the agentic pivot and the team-kernel, and steers the framework's evolution — including its migration onto the `@opengsd/gsd-core` engine.

## 📄 License

Source-available under a proprietary license — see [LICENSE](./LICENSE). Public code and
history; training students get a private-reuse right; redistribution and resale prohibited.
The `skill-creator` module reuses original Anthropic content under the MIT license.

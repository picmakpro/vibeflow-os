---
phase: 01-dev-orchestrator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dev-orchestrator/scripts/ensure-deps.sh
autonomous: true
requirements: [BOOT-01, BOOT-02, BOOT-03, BOOT-04]
must_haves:
  truths:
    - "ensure-deps.sh détecte GSD installé et ne le réinstalle pas (idempotent)"
    - "ensure-deps.sh détecte Superpowers installé et ne le réinstalle pas (idempotent)"
    - "Un 2e run consécutif est un no-op (aucune réinstallation, log clair)"
    - "Si Node/npm absent → étapes manuelles affichées pour GSD, jamais d'échec silencieux"
    - "Si CLI claude absent → étape manuelle affichée pour Superpowers"
    - "gsd-new-project n'est jamais lancé par le script (interactif, sur confirmation seulement)"
  artifacts:
    - path: "dev-orchestrator/scripts/ensure-deps.sh"
      provides: "Bootstrap auto-install non-interactif GSD + Superpowers"
      min_lines: 60
  key_links:
    - from: "dev-orchestrator/scripts/ensure-deps.sh"
      to: "npx -y get-shit-done-cc@latest --claude --global"
      via: "auto-install GSD si absent"
      pattern: "get-shit-done-cc"
    - from: "dev-orchestrator/scripts/ensure-deps.sh"
      to: "claude plugin install superpowers@claude-plugins-official --scope user"
      via: "auto-install Superpowers si absent"
      pattern: "superpowers@claude-plugins-official"
---

<objective>
Livrer `scripts/ensure-deps.sh` : le bootstrap d'auto-installation non-interactif des deux
dépendances (GSD via npm, Superpowers via plugin Claude Code), idempotent, avec fallback
manuel si un prérequis (Node/npm ou CLI `claude`) manque.

Purpose: Rendre les dépendances invisibles et auto-installées (vision §1, D3). Couvre
BOOT-01 à BOOT-04. Aucune commande ne doit attendre une saisie ; `gsd-new-project`
(interactif) ne se lance jamais seul.
Output: `dev-orchestrator/scripts/ensure-deps.sh` testable en idempotence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/CONVENTIONS.md

# Pattern à calquer (style bash, helpers log/err, set -euo pipefail) :
@_internal/vibeflow-update.sh

<interfaces>
<!-- Faits de détection/install vérifiés dans la spec §7 (source de vérité). -->

GSD :
- Détection : `command -v gsd-sdk` OU présence de `~/.claude/get-shit-done/VERSION`.
- Auto-install non-interactif : `npx -y get-shit-done-cc@latest --claude --global`
  (flags --claude + --global bypassent les prompts readline de install.js).
- Prérequis : Node/npm sur le PATH (`command -v npm`). Absent → étapes manuelles.

Superpowers :
- Détection : `claude plugin list 2>/dev/null | grep -q superpowers` OU présence sous
  `~/.claude/plugins/cache/.../superpowers/`.
- Auto-install non-interactif : `claude plugin install superpowers@claude-plugins-official --scope user`.
- Fallback marketplace : `claude plugin marketplace add anthropics/claude-plugins-official`.
- Prérequis : CLI `claude` sur le PATH (`command -v claude`). Absent → étape manuelle TUI :
  `/plugin install superpowers@claude-plugins-official`.

Contrat de sortie (pour les tests Plan 05) :
- Mode dry-run / détection : variable `VF_ENSURE_DRY_RUN=1` → le script détecte et logue
  ce qu'il FERAIT sans exécuter npx/claude (permet de tester l'idempotence sans réseau).
- Exit 0 si tout est présent OU si les étapes manuelles ont été affichées proprement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire ensure-deps.sh (détection + auto-install GSD)</name>
  <files>dev-orchestrator/scripts/ensure-deps.sh</files>
  <action>
    Créer `scripts/ensure-deps.sh` (conventions : shebang bash, `set -uo pipefail` — PAS `-e`
    car certaines détections doivent pouvoir échouer sans tuer le script ; header purpose/usage ;
    helpers `log()`/`err()` calqués sur vibeflow-update.sh ; fonctions snake_case avec préfixe `ensure_`/`detect_`).
    Implémenter le pilier GSD (BOOT-01) :
    - `detect_gsd()` : retourne 0 si `command -v gsd-sdk` OU `[ -f "$HOME/.claude/get-shit-done/VERSION" ]`.
    - `ensure_gsd()` :
      - Si `detect_gsd` → log "GSD déjà présent (skip)" et return 0 (idempotence, BOOT-03).
      - Sinon, si `command -v npm` absent → afficher étapes manuelles (installer Node puis relancer), return 0 SANS échec silencieux (BOOT-03).
      - Sinon : si `VF_ENSURE_DRY_RUN=1`, loguer la commande sans l'exécuter ; sinon exécuter `npx -y get-shit-done-cc@latest --claude --global` et vérifier le code de sortie (échec → bascule sur étapes manuelles).
    Respecter les conventions de log (`[ensure-deps] message` vers stderr).
  </action>
  <verify>
    <automated>bash -n dev-orchestrator/scripts/ensure-deps.sh && grep -q 'get-shit-done-cc' dev-orchestrator/scripts/ensure-deps.sh && grep -q 'gsd-sdk' dev-orchestrator/scripts/ensure-deps.sh</automated>
  </verify>
  <done>Le script a une syntaxe bash valide, détecte GSD via gsd-sdk/VERSION, et auto-installe via npx avec gestion du prérequis npm et de l'exit code.</done>
</task>

<task type="auto">
  <name>Task 2: Pilier Superpowers + garde-fou init + idempotence globale</name>
  <files>dev-orchestrator/scripts/ensure-deps.sh</files>
  <action>
    Compléter `ensure-deps.sh` :
    - `detect_superpowers()` : 0 si `claude plugin list 2>/dev/null | grep -q superpowers` OU dossier plugin présent sous `~/.claude/plugins/cache`.
    - `ensure_superpowers()` (BOOT-02/03) :
      - Si présent → log skip, return 0.
      - Si `command -v claude` absent → afficher l'étape manuelle TUI (`/plugin install superpowers@claude-plugins-official`), return 0 sans échec.
      - Sinon : si dry-run, loguer ; sinon exécuter `claude plugin install superpowers@claude-plugins-official --scope user`. Si échec → tenter fallback `claude plugin marketplace add anthropics/claude-plugins-official` puis re-tenter l'install ; si toujours KO → afficher l'étape manuelle (jamais d'échec silencieux).
    - Garde-fou init (BOOT-04) : ajouter une fonction `detect_codebase()` (présence de fichiers de code dans le cwd) qui, SI du code existe ET que `VF_ENSURE_AUTO_MAP=1`, peut logger que `gsd-map-codebase` est lançable (non-interactif). Le script NE LANCE JAMAIS `gsd-new-project` (interactif) — il se contente d'imprimer un message d'invitation à confirmer ("Projet dev détecté — l'agent proposera l'init"). Vérifier qu'aucune occurrence de `gsd-new-project` exécutée n'existe (seulement en commentaire/message).
    - `main()` : enchaîne `ensure_gsd` puis `ensure_superpowers`, log un résumé final clair de ce qui a été fait/skippé, exit 0 si tout OK ou étapes manuelles affichées.
    Densité : viser ≤180 lignes.
  </action>
  <verify>
    <automated>bash -n dev-orchestrator/scripts/ensure-deps.sh && grep -q 'superpowers@claude-plugins-official' dev-orchestrator/scripts/ensure-deps.sh && grep -q 'marketplace add anthropics/claude-plugins-official' dev-orchestrator/scripts/ensure-deps.sh && ! grep -E '^[[:space:]]*(gsd-new-project|claude .*new-project)' dev-orchestrator/scripts/ensure-deps.sh && VF_ENSURE_DRY_RUN=1 bash dev-orchestrator/scripts/ensure-deps.sh; test $? -eq 0</automated>
  </verify>
  <done>Le script gère Superpowers (install + fallback marketplace + étape manuelle), ne lance jamais gsd-new-project en exécution, et un run dry-run retourne exit 0 avec un résumé clair.</done>
</task>

</tasks>

<verification>
- BOOT-01 : GSD auto-installé via npx non-interactif si absent ✓
- BOOT-02 : Superpowers auto-installé via plugin non-interactif si absent ✓
- BOOT-03 : idempotent (2e run = no-op), exit codes vérifiés, étapes manuelles si prérequis manquant ✓
- BOOT-04 : gsd-new-project jamais lancé seul (vérifié par grep négatif) ; map-codebase conditionnel ✓
- Idempotence testable via `VF_ENSURE_DRY_RUN=1` (2 runs consécutifs = no-op).
</verification>

<success_criteria>
- ensure-deps.sh syntaxe valide, idempotent, non-interactif, fallback manuel propre.
- Aucune exécution de commande interactive (gsd-new-project).
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-02-SUMMARY.md`
</output>

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 03
lot: L1 — gate du budget, sa suite, son étape CI (commit atomique)
type: execute
wave: 2
depends_on: ["40.1-01"]
autonomous: true
requirements: [BUDG-05, BUDG-04, QUAL-01]
files_modified:
  - plugin/conductor/scripts/check-instruction-budget.sh
  - plugin/conductor/scripts/tests/test-check-instruction-budget.sh
  - .github/workflows/ci.yml
  - .planning/instruction-budget-baselines.tsv
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "BUDG-05 / D-02 — le gate armé rend 0 quand un agent gagne des lignes SANS instruction et reste sous le plafond ; la colonne lignes de la baseline est lue et publiée, jamais comparée pour le rc (D-H6)"
    - "D-07 — le ratchet d'instructions reste PAR FICHIER : une instruction ajoutée à un fichier au-dessus de SA baseline rend 1 (`DEPASSEMENT-INSTR`)"
    - "D-01 / D-05 — `VF_BUDGET_LINE_CAP=300` : un fichier de 301 lignes rend 1 (`DEPASSEMENT-ADR029`, qui prime toujours sur la baseline) ; un fichier de 251 à 300 lignes rend 0 avec le verdict visible `AVERTISSEMENT-ADR029` ; un fichier sous la zone (taille `BORNE_AVERT - 1`) ne déclenche rien. La suite n'écrit aucun littéral de l'ancien plafond (constat B1, révision 1). D-06 : le gate compte toujours le FICHIER ENTIER"
    - "QUAL-01 — la suite garde ses trois issues (PASS, FAIL, BRUYANT) ; MUT-2 est REMPLACÉ (il ré-introduit la comparaison des lignes comme bloquante et est TUÉ), MUT-5 (zone d'avertissement rendue bloquante), MUT-6 (plafond décalé de un) et MUT-7 (avertissement décalé de un) sont TUÉS ; MUT-1, MUT-3, MUT-4 inchangés et toujours tués"
    - "D-H1 / O-1 — l'étape CI prouve elle-même, sur fixture armée, quatre bascules ISOLÉES (une seule garde à la fois) : ligne neutre ajoutée → 0 ; instruction ajoutée sans ligne de plus → 1 ; fichier à 300 lignes → 0 avec `::warning::` et `AVERTISSEMENT-ADR029` ; fichier à 301 lignes → 1 ; chaque bascule est prouvée capable de rougir par un mutant du gate joué CONTRE l'étape extraite de ci.yml"
    - "D-H2 / O-2 — les commentaires de l'étape CI et le message `::warning::` du cas non armé décrivent l'état réel (ratchet armé depuis `a7f414a`, plus de mention d'une calibration attendue de la Phase 40)"
    - "Aucune fenêtre rouge : gate, suite, étape CI et en-tête de baseline changent dans UN SEUL commit ; `check-instruction-budget.sh` rend 0 sur l'arbre du commit parent ET du commit lui-même ; les lignes de données de la baseline sont octet pour octet identiques (format trois colonnes inchangé, aucune valeur ne monte)"
  artifacts:
    - path: "plugin/conductor/scripts/check-instruction-budget.sh"
      provides: "constantes VF_BUDGET_LINE_CAP=300 et VF_BUDGET_LINE_WARN_FROM=251, verdicts AVERTISSEMENT-ADR029 et LIGNES-EN-HAUSSE, compteur WARN_COUNT"
      contains: "VF_BUDGET_LINE_WARN_FROM=251"
    - path: "plugin/conductor/scripts/tests/test-check-instruction-budget.sh"
      provides: "cas BUDG-05 lignes sans instruction, zone 251/260/300, plafond 301, MUT-2 remplacé, MUT-5/6/7"
    - path: ".github/workflows/ci.yml"
      provides: "étape check-instruction-budget à sept bascules et annotation ::warning:: de zone"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh"
      provides: "mutants du gate joués contre l'étape CI extraite"
  key_links:
    - "`case \"$verdict\" in DEPASSEMENT-*)` est le SEUL chemin qui incrémente `OVERRUN_COUNT` : aucun libellé d'avertissement ou d'information ne commence par `DEPASSEMENT-`"
    - "La ligne `    [ \"$instr\" -gt \"$bl_instr\" ] && instr_over=1` reste octet pour octet identique : c'est l'ancre de MUT-1"
    - "`tools/prove-ci-budget-step.sh` extrait l'étape via `tools/replay-ci-jobs.sh --step check-instruction-budget` : l'étape prouvée est celle de ci.yml, pas une copie"
---

<objective>
**Colonne vertébrale de BUDG-05.** Réviser le gate du budget d'instructions selon les arbitrages
verrouillés (arbitrage Samuel, AskUserQuestion session principale, relais SendMessage,
2026-09-16) : D-01 plafond 300, D-02 ratchet sur les instructions seules, D-05 avertissement non
bloquant dès 251, D-06 chaque outil garde sa mesure, D-07 ratchet par fichier — et les décisions du
head D-H1 (bascules isolées dans l'étape CI), D-H2 (commentaires sur l'état réel), D-H6 (colonne
lignes informative, format inchangé).

**Révision 1 (constat W1 du checker, budget)** : l'alignement des énoncés du module conductor et son
bump sont sortis vers 40.1-04. Ce plan garde UNE seule tâche, et c'est voulu : le commit atomique
gate + suite + étape CI + en-tête de baseline + outil de preuve ne se découpe pas sans ouvrir une
fenêtre rouge sur `main` armée. Une tâche de plus sans fichier propre serait de la cérémonie.

Purpose: qu'un agent puisse grandir en lignes jusqu'à 300 sans reformulation forcée, tandis
qu'une instruction ajoutée reste bloquée sans arbitrage nommé.
Output: un commit atomique gate + suite + étape CI + en-tête de baseline + outil de preuve.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-RESEARCH.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-PATTERNS.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<interfaces>
État cible du bloc de verdict de `check-instruction-budget.sh` (description, pas implémentation) :
1. `verdict="OK"` ; si `lines -gt VF_BUDGET_LINE_CAP` → `DEPASSEMENT-ADR029` (prime) ;
2. sinon si pas de baseline → `SANS-BASELINE` ;
3. sinon `instr_over=0`, ligne-ancre MUT-1 inchangée, `DEPASSEMENT-INSTR` si `instr_over`, sinon
   `MARGE` si `instr -lt bl_instr` (MARGE ne porte plus que sur les instructions — Claude's
   Discretion de CONTEXT tranchée ainsi : la colonne lignes n'influe plus sur aucun verdict
   d'état) ; puis ligne informative UNIQUE `    [ "$lines" -gt "$bl_lines" ] && verdict="${verdict}+LIGNES-EN-HAUSSE"`
   (ancre de MUT-2 ; affichage seul, commentaire explicite « jamais comparée pour le rc, D-02/D-H6 ») ;
4. après le bloc, hors `DEPASSEMENT-ADR029` : si `lines -ge VF_BUDGET_LINE_WARN_FROM` et
   `lines -le VF_BUDGET_LINE_CAP` → ligne `    verdict="${verdict}+AVERTISSEMENT-ADR029"` (ancre de
   MUT-5) et `WARN_COUNT` incrémenté ;
5. `case "$verdict" in DEPASSEMENT-*)` inchangé.
Ancres de mutants (lignes exactes, indentation comprise, à respecter octet pour octet) :
- MUT-1 : `    [ "$instr" -gt "$bl_instr" ] && instr_over=1` (existante, inchangée)
- MUT-6 : `  if [ "$lines" -gt "$VF_BUDGET_LINE_CAP" ]; then` (existante, inchangée) → mutant `-ge`
- MUT-7 : la condition d'avertissement écrite sur UNE ligne contenant `-ge "$VF_BUDGET_LINE_WARN_FROM"` → mutant `-gt`
Rapport : colonnes `FICHIER | LIGNES | BL-LIGNES | INSTR | BL-INSTR | VERDICT` inchangées ; BILAN
`BILAN : %s fichier(s), %s depassement(s), %s avertissement(s) ADR-029, %s non verifiable(s), arme=%s, code=%s`.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1 : sémantique du gate, suite, étape CI et en-tête de baseline — UN SEUL commit, jamais rouge</name>
  <files>plugin/conductor/scripts/check-instruction-budget.sh, plugin/conductor/scripts/tests/test-check-instruction-budget.sh, .github/workflows/ci.yml, .planning/instruction-budget-baselines.tsv, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (en entier ; L1-36 en-tête, L68-73 plafond, L296-331 verdict, L350-377 rc et rapport)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (en entier ; L89-100 `gen_251`, L176-183 FAIL lignes, L261-269 plafond, L382-390 F3, L427-514 mutants)
    - .github/workflows/ci.yml (étape `check-instruction-budget`, ~L371-479)
    - .planning/instruction-budget-baselines.tsv (en-tête L1-19, format)
    - plugin/software-architecture/scripts/check-file-size.sh (patron warn/block : seul le compteur de blocage pilote la sortie)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh (option `--root`, `--step`)
  </read_first>
  <behavior>
    - lignes +1 sans instruction, sous 300, baseline armée → rc 0, verdict `OK+LIGNES-EN-HAUSSE`, jamais `DEPASSEMENT`
    - instruction +1, lignes égales à la baseline → rc 1, `DEPASSEMENT-INSTR`
    - taille `BORNE_AVERT - 1` → rc 0, aucun `AVERTISSEMENT-ADR029` ; `BORNE_AVERT` (251) → rc 0 + `AVERTISSEMENT-ADR029` ; 260 avec lignes en hausse → rc 0 + les deux suffixes ; 300 → rc 0 + `AVERTISSEMENT-ADR029` ; 301 (baseline 301) → rc 1 + `DEPASSEMENT-ADR029` ; instruction ajoutée à 260 lignes → rc 1
    - F3 : fichier de 301 lignes sans entrée de baseline, armé → rc 2
  </behavior>
  <action>
**Gate** (`check-instruction-budget.sh`), selon `<interfaces>` : `VF_BUDGET_LINE_CAP=300` ;
constante sœur `VF_BUDGET_LINE_WARN_FROM=251` (inclusive, comparée en `-ge`, patron
`check-file-size.sh` ; aucune constante ne porte l'ancienne valeur) ; commentaire au-dessus réécrit :
plafond ADR-029 révisé en Phase 40.1 (arbitrages Samuel D-01/D-05, AskUserQuestion session
principale, 2026-09-16), avertissement dès 251, bloque au-delà de 300, fichier entier (D-06), la
« limite effective = min(baseline, plafond) » devient « limite = plafond », la colonne lignes de la
baseline est publiée et jamais comparée pour le rc (D-02, D-H6). Supprimer `line_over` et les verdicts
`DEPASSEMENT-LIGNES` / `DEPASSEMENT-LIGNES+INSTR`. Ajouter `WARN_COUNT=0` près de `OVERRUN_COUNT`.
Réécrire les commentaires L297-298 et L319-322 (plus aucune valeur de plafond périmée) et l'en-tête
L2-4 et L26 (rc 1 = instruction au-dessus de sa baseline ou fichier au-delà du plafond ; mention du
verdict informatif `AVERTISSEMENT-ADR029` non bloquant). BILAN selon `<interfaces>`. Codes de sortie
et bloc `RC=` inchangés. Formulation (tranchée par le manager) : « avertissement dès 251 » / « bloque
au-delà de 300 », jamais « OK ≤ » l'ancien plafond.

**Suite** (`test-check-instruction-budget.sh`) : remplacer `gen_251` par `gen_lines <chemin> <total>`
(3 lignes de frontmatter + corps neutre sans marqueur) et, avant chaque usage, affirmer la taille
obtenue par `awk 'END{print NR}'` (Pitfall 2 : une fixture qui ne dépasse plus ce qu'elle prétend
dépasser est un ko tracé). Remplacer le cas « Issue FAIL (lignes) » par le cas BUDG-05 « lignes en
hausse sans instruction sous le plafond → rc 0, `OK+LIGNES-EN-HAUSSE`, jamais DEPASSEMENT » (même
fixture std_agent, baseline lignes 3). Déclarer en tête de suite les constantes locales
`BORNE_AVERT=251` et `PLAFOND=300`, et exprimer TOUTES les tailles de fixture de bord à partir
d'elles (`$((BORNE_AVERT - 1))`, `$BORNE_AVERT`, `$PLAFOND`, `$((PLAFOND + 1))`) : la suite ne
contient AUCUN littéral de l'ancien plafond. Sinon le recensement BUDG-04 (regex versionnée,
aucune exclusion pour ce fichier) rougirait ce plan et la clôture — constat B1 du checker,
révision 1. Ajouter : sous la zone, taille `BORNE_AVERT - 1` → rc 0 sans avertissement ;
`BORNE_AVERT` → rc 0 avec `AVERTISSEMENT-ADR029` ; 260 lignes, baseline lignes 4 → rc 0 avec `LIGNES-EN-HAUSSE` ET
`AVERTISSEMENT-ADR029` ; 300 → rc 0 avec avertissement ; instruction ajoutée à un fichier de 260
lignes → rc 1 `DEPASSEMENT-INSTR`. Plafond absolu : 301 lignes, baseline `301\t0` → rc 1
`DEPASSEMENT-ADR029`. F3 : fichier de 301 lignes. Mutants (patron `awk` exact + `cmp -s` + `bash -n`
inchangé) : **MUT-2 REMPLACÉ** — ancre `    [ "$lines" -gt "$bl_lines" ] && verdict="${verdict}+LIGNES-EN-HAUSSE"`,
mutant `    [ "$lines" -gt "$bl_lines" ] && verdict="DEPASSEMENT-LIGNES"` (comparaison des lignes
ré-introduite comme bloquante) ; fixture lignes en hausse sans instruction → attendu mutant rc 1,
original rc 0. **MUT-5** — ancre `    verdict="${verdict}+AVERTISSEMENT-ADR029"`, mutant
`    verdict="DEPASSEMENT-AVERTISSEMENT"` ; fixture 260/260/0 → mutant rc 1, original rc 0.
**MUT-6** — ancre du plafond `-gt` → `-ge` ; fixture 300/300/0 → mutant rc 1, original rc 0.
**MUT-7** — condition d'avertissement `-ge` → `-gt` ; fixture 251/251/0 → original porte
`AVERTISSEMENT-ADR029`, mutant ne le porte pas (rc 0 des deux côtés). MUT-1, MUT-3, MUT-4 inchangés.
En-tête de suite : liste des sept mutants, cas BUDG-05, trois issues QUAL-01 maintenues, référence
Phase 40.1 D-02/D-05. Aucun `diff` (cmp/comm/cksum seulement).

**Étape CI** (`ci.yml`, étape `check-instruction-budget`) — D-H2 : réécrire le commentaire POURQUOI
sur l'état réel (gate distribué mesurant plafond et instructions depuis la Phase 25, ratchet ARMÉ
sur `main` depuis `a7f414a`, révisé en Phase 40.1 : plafond 300, avertissement dès 251, ratchet
d'instructions seul par fichier) et le message `::warning::` du cas `armed=0` (plus aucune mention
d'une calibration attendue de la Phase 40 ; cas structurel conservé). Nom de l'étape :
`check-instruction-budget (BUDG-01/02/04/05, ADR-029 machine-enforced)` (le préfixe
`check-instruction-budget` reste le début du nom). D-H1 : Bloc 1 reconstruit en SEPT bascules, chacune
repartant d'une fixture de base recréée par une fonction (agent de 6 lignes : frontmatter 3 lignes,
`## Iron Laws`, `- Le lock est verifie avant tout checkout.`, `Une ligne de prose neutre.` ; baseline
calculée par `awk NR` plus la colonne INSTR du gate. Durcissement du point 4 de l'item BACKLOG
« Dérive documentaire… » : la mesure INSTR sur la fixture sans baseline capture explicitement le
code du gate dans une variable (`out=…` puis `|| mrc=$?`, `mrc` attendu 2 à ce stade et vérifié),
puis extrait la colonne par awk sur la sortie capturée. Jamais de substitution avec pipeline
héritant d'un rc sous un éventuel `pipefail`, jamais `|| true`.) et appliquant UNE seule mutation :
1/7 base → 0 ; 2/7 une ligne neutre ajoutée → 0 ET sortie contenant `LIGNES-EN-HAUSSE` sans
`DEPASSEMENT` ; 3/7 dernière ligne réécrite par awk (vers temporaire puis `mv`, jamais `sed -i`) en
`Cette ligne DOIT compter.`, nombre de lignes inchangé → 1 ET `DEPASSEMENT-INSTR` ; 4/7 allongée
par lignes neutres à exactement 300 → 0 ET `AVERTISSEMENT-ADR029`, l'étape émet alors
`::warning::check-instruction-budget CI : 4/7 zone d'avertissement ADR-029 (dès 251 lignes, non bloquant) prouvée` ;
5/7 allongée à 301 → 1 ET `DEPASSEMENT-ADR029` ; 6/7 sentinelle retirée → 3 ; 7/7 répertoire vide → 2.
Chaque écart passe par `note "<k>/7 …"` (préfixe `::error::check-instruction-budget CI : `), bilan
`sur 7 verdicts`. Bloc 2 (dépôt réel) : dans la branche armée rc 0, si la sortie porte
`AVERTISSEMENT-ADR029`, émettre un `::warning::` par fichier concerné (non bloquant). Recettes justes
sous `bash -e` sans pipefail (captures `|| rc=$?`, tests awk en condition de `if`).

**Baseline** (`instruction-budget-baselines.tsv`) : réécrire UNIQUEMENT la phrase de commentaire
sur le plafond (L15-16) : la colonne lignes est publiée, jamais comparée (Phase 40.1, D-02/D-H6) ;
le plafond ADR-029 (bloque au-delà de 300 lignes, fichier entier) prime toujours. Aucune ligne de
données touchée.

**Outil de preuve** (`tools/prove-ci-budget-step.sh`) : copier l'arbre SUIVI courant dans un
`mktemp -d` (`git ls-files -z` + `tar`, pour inclure les modifications non committées), puis :
T0 étape extraite (`replay-ci-jobs.sh --root <copie> --job gates --step check-instruction-budget`)
sur gate intact → rc 0 ; pour chaque mutant appliqué au gate DE LA COPIE (awk ligne exacte, refus si
`cmp -s` identique), restauration du gate entre deux mutants, exiger rc 1 ET la présence de la note
attendue dans le journal de l'étape : MUT-2 → `2/7` ; MUT-1 → `3/7` ; MUT-5 → `4/7` ; MUT-6 → `4/7` ;
MUT-AV (ancre MUT-5 remplacée par `    verdict="${verdict}"`, avertissement effacé) → `4/7` ;
MUT-CAP (`VF_BUDGET_LINE_CAP=300` → `VF_BUDGET_LINE_CAP=100000`) → `5/7`. Trace à trois champs par
assertion ; rc 0 si toutes tiennent, 1 sinon.

Committer les cinq fichiers ENSEMBLE, un seul commit (`feat(40.1-03): …`), pathspec explicite.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-check-instruction-budget.sh</automated>
    <fails_when>exit non nul, ou `== resultat : N ok, M ko ==` avec M différent de 0, ou absence de l'une des lignes « ✓ MUT-2 », « ✓ MUT-5 », « ✓ MUT-6 », « ✓ MUT-7 »</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-ci-budget-step.sh</automated>
    <fails_when>exit non nul, ou une assertion imprimant `obtenu :` (T0 rc différent de 0, ou un mutant laissant l'étape verte ou rouge sur une autre bascule que celle attendue)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); C=$(git log -1 --format=%H -- plugin/conductor/scripts/check-instruction-budget.sh); [ -n "$C" ]; echo "commit atomique epingle : $C"; for rev in "$C^" "$C"; do d="$T/$(printf %s "$rev" | tr -c "[:alnum:]" _)"; mkdir -p "$d"; git archive -o "$d.tar" "$rev"; tar -x -C "$d" -f "$d.tar"; rc=0; bash "$d/plugin/conductor/scripts/check-instruction-budget.sh" --path "$d" > "$d.out" 2>&1 || rc=$?; echo "gate sur $rev : rc=$rc"; [ "$rc" -eq 0 ]; done; git diff-tree --no-commit-id --name-only -r "$C" > "$T/files"; n=$(awk "/^plugin\\/conductor\\/scripts\\/check-instruction-budget\\.sh\$|^plugin\\/conductor\\/scripts\\/tests\\/test-check-instruction-budget\\.sh\$|^\\.github\\/workflows\\/ci\\.yml\$|^\\.planning\\/instruction-budget-baselines\\.tsv\$/{c++} END{print c+0}" "$T/files"); echo "fichiers du contrat dans le commit atomique : $n/4"; [ "$n" -eq 4 ]; git show "$C^:.planning/instruction-budget-baselines.tsv" > "$T/a0"; awk "NF && !/^#/" "$T/a0" > "$T/a"; awk "NF && !/^#/" .planning/instruction-budget-baselines.tsv > "$T/b"; cmp -s "$T/a" "$T/b" && echo "lignes de donnees de baseline identiques"'</automated>
    <fails_when>exit non nul ; ou une ligne `gate sur <parent> : rc=` / `gate sur <commit> : rc=` différente de `rc=0` ; ou `fichiers du contrat dans le commit atomique :` différent de `4/4` ; ou absence de la ligne `lignes de donnees de baseline identiques`</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates --step check-instruction-budget</automated>
    <fails_when>exit non nul, ou `REJEU gates | … | rc=` différent de `rc=0`</fails_when>
  </verify>
  <!-- planner-discipline-allow: line_over -->
  <!-- planner-discipline-allow: DEPASSEMENT-LIGNES -->
  <acceptance_criteria>
    - `check-instruction-budget.sh` contient `VF_BUDGET_LINE_CAP=300`, `VF_BUDGET_LINE_WARN_FROM=251`, `AVERTISSEMENT-ADR029`, `LIGNES-EN-HAUSSE`, `WARN_COUNT` ; ne contient plus `line_over` ni `DEPASSEMENT-LIGNES`
    - la ligne `    [ "$instr" -gt "$bl_instr" ] && instr_over=1` est présente à l'identique
    - suite : 0 ko ; les cas nommés BUDG-05 (lignes sans instruction), sous-zone (`BORNE_AVERT - 1`), bord d'avertissement (`BORNE_AVERT`), 260, `PLAFOND`, `PLAFOND + 1`, F3 à `PLAFOND + 1` existent ; la suite déclare `BORNE_AVERT=251` et `PLAFOND=300` ; « ✓ MUT-1 » à « ✓ MUT-7 » présents
    - `ci.yml` : la mesure INSTR de la fixture capture son code dans une variable vérifiée (point 4 BACKLOG), sans `|| true` ; l'étape porte `1/7` à `7/7`, `::warning::check-instruction-budget CI : 4/7`, et ne contient plus `ce qui n'est PAS le cas à ce` ni `n'a pas livré la sentinelle`
    - `tools/prove-ci-budget-step.sh` : T0 rc 0, six mutants chacun rouge sur SA bascule
    - gate rc 0 sur le parent du commit atomique et sur le commit atomique (épinglé par `git log -1 -- check-instruction-budget.sh`, jamais une ancre relative à HEAD) ; les quatre fichiers du contrat dans le même commit ; lignes de données de la baseline identiques octet pour octet
    - `tools/check-no-live-250.sh .` ne liste aucune ligne sous `plugin/conductor/scripts/`, `.github/` ou `.planning/instruction-budget-baselines.tsv`
  </acceptance_criteria>
  <done>Le ratchet ne bloque plus que les instructions, le plafond vaut 300 avec avertissement dès 251, chaque bascule est prouvée dans la suite et dans l'étape CI, en un commit qui n'a jamais rougi le gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → job CI `gates` | un contributeur fait grandir un agent ou ajoute une instruction ; le gate décide du rc |
| baseline versionnée → verdict | une baseline relevée peut légaliser un dépassement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-08 | Repudiation | étape CI verte par construction (O-1) | high | mitigate | sept bascules isolées ; `prove-ci-budget-step.sh` joue six mutants du gate contre l'étape extraite de ci.yml, chacun rouge sur sa bascule |
| T-40.1-09 | Elevation of privilege | libellé d'avertissement tombant dans `DEPASSEMENT-*` ou comparaison des lignes ré-introduite | high | mitigate | MUT-2 et MUT-5 tués dans la suite et contre l'étape ; aucun libellé informatif ne commence par `DEPASSEMENT-` |
| T-40.1-10 | Elevation of privilege | baseline de lignes relevée pour légaliser > 300 (ex-T-25-17) | medium | mitigate | `DEPASSEMENT-ADR029` prime toujours ; cas 301 avec baseline 301 → rc 1 ; lignes de données de baseline identiques exigées |
| T-40.1-11 | Tampering | hausse de baseline d'instructions sans arbitrage (O-3) | medium | transfer | renvoyé à la Phase 41 (D-H3) ; aucune garde machine ajoutée ici, relecture de diff |
| T-40.1-12 | Denial of service | fenêtre rouge sur `main` armée | high | mitigate | un seul commit pour gate+suite+CI+baseline ; gate rc 0 prouvé sur le parent du commit atomique et sur ce commit (épinglés) |
</threat_model>

<verification>
- Suite du gate 0 ko, sept mutants tués ; preuve des mutants contre l'étape CI verte.
- Gate rc 0 avant et après le commit atomique ; baseline de données identique.
- `replay-ci-jobs.sh --job gates --step check-instruction-budget` vert ; recensement nul sous les chemins du commit.
</verification>

<success_criteria>
BUDG-05 prouvé en suite et en CI ; plafond 300 et avertissement dès 251 effectifs dans le gate,
en un commit qui n'a jamais rougi le gate armé.
</success_criteria>

## Artifacts this phase produces

- `check-instruction-budget.sh` : constante `VF_BUDGET_LINE_WARN_FROM`, compteur `WARN_COUNT`, verdicts `AVERTISSEMENT-ADR029` et `LIGNES-EN-HAUSSE` ; suppression de `line_over`, `DEPASSEMENT-LIGNES`, `DEPASSEMENT-LIGNES+INSTR`
- `test-check-instruction-budget.sh` : fonction `gen_lines`, mutants `MUT-2` (remplacé), `MUT-5`, `MUT-6`, `MUT-7`
- `ci.yml` : bascules `1/7`…`7/7`, annotation `::warning::` de zone
- `tools/prove-ci-budget-step.sh` : mutants `MUT-AV`, `MUT-CAP` + réemploi MUT-1/2/5/6

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-03-SUMMARY.md` when done
</output>

---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md
autonomous: true
requirements: [AGTS-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le run de sortie d'expérimental défini par `plugin/mobile-test/README.md` (§Limites : `detect` → `run --platform ios` → rapport généré) et par `plugin/mobile-test-team/README.md` (§Limites : une phase mobile pilotée sans intervention, au moins un cycle de fix, arrêt propre) est JOUÉ POUR DE VRAI sur le lab `~/Documents/dev/Scroll-Off/frontend`, iOS seulement, sur le poste courant (D-04). Aucune ligne du verdict n'est déduite d'une lecture de code ou de README : chaque ligne de `34-RUN-MOBILE.md` est adossée à une commande exécutée et à sa sortie."
    - "La trace rend DEUX verdicts distincts, jamais un seul agrégé : (a) PIPELINE — le module mécanique `mobile-test` a-t-il fait `detect` → `run --platform ios` → rapport horodaté généré ; (b) EQUIPE — la boucle `mobile-test-team` a-t-elle été PILOTÉE par `vf-test-orchestrator` dispatché en sous-agent (pas une exécution manuelle de Maestro par l'exécutant), avec au moins un cycle de fix et un arrêt propre. La sortie d'expérimental des DEUX modules (plan 34-04) exige les DEUX verdicts VERT : un PIPELINE vert avec un EQUIPE rouge est un run ROUGE au sens de D-05, pas un demi-succès."
    - "L'UDID du simulateur cible est RÉSOLU EXPLICITEMENT avant tout appel à `run`, et passé en `--target <udid>` : la résolution par nom de `ios.preferredSimulator` est structurellement ambiguë sur ce poste (deux simulateurs distincts nommés « iPhone 17 Pro », runtimes iOS 26.2 et 26.4 — mesuré le 2026-09-15, `34-RESEARCH.md` Pitfall 2) et `ensureIosTarget()` prend le premier match par runtime."
    - "L'état authentifié non reproductible du lab (code à 6 chiffres déjà consommé, `~/Documents/dev/Scroll-Off/frontend/.maestro/README.md`) survit au run : aucun flow joué ne remet l'état à zéro, aucune désinstallation, aucun rebuild forcé. La garde est MACHINE (une recherche sur l'arbre `.maestro` du lab, exécutée AVANT le premier run et prouvée capable de rendre rouge par un flow mutant témoin), pas une intention écrite dans la trace."
    - "Tout prérequis POSTE manquant en cours de run (Maestro, JDK, simulateur, SDK) déclenche un ARRÊT PROPRE et un report tracé (D-05) : aucun worker n'installe quoi que ce soit sur la machine, la cause d'arrêt est nommée précisément dans `34-RUN-MOBILE.md` (jamais « ça n'a pas marché »), et un déclencheur de reprise daté y est rédigé, prêt à coller, pour transcription au ledger par le plan 34-06."
    - "Les deux manques SPÉCIFIQUES AU LAB mesurés par la recherche — absence de `.vibeflow/mobile-test.json` dans `Scroll-Off/frontend`, absence de la clé `workflow.use_worktrees` dans `Scroll-Off/.planning/config.json` — sont traités comme des tâches de PRÉPARATION PROJET normales (le plan les crée), PAS comme des causes d'arrêt propre au sens D-05, qui ne vise que Maestro/JDK/simulateur/SDK. Ce classement est écrit en toutes lettres dans la note."
    - "Si le run ne redéclenche pas le build parce que l'app est déjà installée sur la cible retenue, la lecture littérale « build depuis zéro » de la condition ROADMAP n'est pas satisfaite à la lettre : ce point est consigné comme DÉVIATION ASSUMÉE ET DATÉE dans `34-RUN-MOBILE.md` — la préservation de la session irremplaçable prime sur la lecture littérale — jamais comme un contournement silencieux (discipline de traçabilité de `CLAUDE.md` racine)."
    - "Aucun fichier de CE dépôt autre que `34-RUN-MOBILE.md` n'est modifié par ce plan, et cette affirmation est PROUVÉE par un diff scopé contre le SHA de base consigné dans la note — pas seulement déclarée en frontmatter. Les fichiers touchés dans `Scroll-Off/frontend` appartiennent à un AUTRE dépôt git et y sont commités par les workers de la boucle, jamais depuis ce dépôt."
  artifacts:
    - ".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md : trace complète du run, portant le chapeau `> **Statut** : VERT` ou `> **Statut** : ROUGE`, les deux lignes de verdict `PIPELINE: …` et `EQUIPE: …`, une section `## Environnement mesuré`, une section `## Prérequis mesurés` (colonne PRESENT/ABSENT + preuve par commande), une section `## Témoin de la garde` (trois sorties de la mutation), une ligne `UDID retenu : <udid>`, une section `## Dispatch`, une section `## Cycles` avec au moins un `### Cycle N`, une ligne `Rapport du run : <nom-de-fichier>`, une section `## Déviations assumées`, une section `## Périmètre` portant la ligne `SHA de base : <sha>`, et une section `## Déclencheur de reprise` si et seulement si le statut est ROUGE."
  key_links:
    - "`34-RUN-MOBILE.md` (chapeau de statut) <-> plan 34-04 (sortie d'expérimental) : 34-04 lit ce chapeau comme sa précondition machine. Un chapeau ambigu (« plutôt vert », « partiellement ») casserait la chaîne en silence — d'où le format unique imposé et compté (EXACTEMENT une ligne correspondante, ni zéro ni deux)."
    - "Dispatch de `vf-test-orchestrator` <-> verdict EQUIPE : c'est le DISPATCH RÉEL du sous-agent qui fait la preuve, pas le résultat des tests. Un run où l'exécutant lance Maestro lui-même prouve `mobile-test` et ne prouve RIEN de `mobile-test-team` — or c'est précisément ce point que le README de ce module déclare non prouvé (« le point le plus risqué — un sous-agent qui spawne d'autres sous-agents via Task ET invoque le pipeline »)."
    - "Garde anti-destruction (recherche sur `.maestro` du lab) <-> session authentifiée : si la garde est rendue incapable d'échouer (chemin faux, dossier vide, motif inopérant), elle devient décorative et la session peut être détruite sans signal. La tâche 1 exige donc un témoin de mutation rouge, consigné avec ses trois sorties — mode de défaut dominant de ce dépôt (10+ vérificateurs incapables d'échouer en Phase 39)."
    - "`SHA de base : <sha>` (section `## Périmètre`) <-> preuve de périmètre : sans ce SHA écrit dans la note, le diff scopé n'est pas rejouable et la revendication « aucun autre fichier touché » redevient une simple affirmation."
  prohibitions:
    - "Ne JAMAIS installer Maestro, un JDK, un simulateur, un SDK ou toute autre dépendance système sur le poste pendant ce run — un prérequis poste manquant est un arrêt propre et un report tracé, jamais une réparation à la volée (D-05)."
    - "Ne JAMAIS jouer, écrire ou modifier un flow Maestro qui remet l'état de l'app à zéro, ne jamais désinstaller l'app du simulateur, ne jamais forcer un rebuild sur la cible porteuse de l'état authentifié du lab Scroll-Off."
    - "Ne JAMAIS recopier dans `34-RUN-MOBILE.md` le contenu d'un fichier d'environnement du lab, un jeton de session ou tout autre secret lu pendant le run — seuls des identifiants publics y figurent (UDID, bundle id, noms de flows, horodatages, codes de sortie)."
    - "Ne JAMAIS affaiblir un assert d'un flow Maestro existant du lab pour faire passer un test — règle anti-triche déjà portée par `vf-test-runner`, rappelée ici parce que le verdict de sortie d'expérimental en dépend entièrement."
    - "Ne JAMAIS modifier un fichier de CE dépôt autre que `34-RUN-MOBILE.md` dans ce plan — ni `plugin/mobile-test*`, ni le ledger, ni la ROADMAP : ces gestes appartiennent aux plans 34-04, 34-05 et 34-06 et sont conditionnés au verdict que CE plan produit."
    - "Ne JAMAIS déclarer EQUIPE vert sans qu'un dispatch réel de `vf-test-orchestrator` figure dans la trace avec son horodatage et son bloc typé de retour."
    - "Ne JAMAIS importer une persona du catalogue `agency-agents`, ni créer le moindre agent dans ce plan — anti-feature gravée au ledger, et D-01 interdit toute création d'agent pour un gap dans cette phase."
---

<objective>
Jouer POUR DE VRAI, sur le lab `~/Documents/dev/Scroll-Off/frontend` (Expo/React Native, iOS
seulement, poste courant), le run qui conditionne la sortie du statut expérimental de
`mobile-test` et `mobile-test-team` — et en produire la trace opposable `34-RUN-MOBILE.md`, à deux
verdicts distincts (PIPELINE, EQUIPE) et à chapeau de statut lisible par machine.

Purpose : l'item actif `PROJECT.md:82` (« sortie du statut expérimental de `mobile-test`(-team) :
premier run réel vert tracé ») dort depuis la création des deux modules. AGTS-02 conditionne
explicitement la construction de `web-test-team` à ce run, et D-06 refuse de réviser cette
condition — cloner un moule non prouvé rejouerait exactement le motif « armé sans preuve » que ce
milestone existe pour fermer. Ce plan est donc le seul endroit de la phase où une PREUVE, et non un
arbitrage, décide de ce qui est construit ensuite.

Output : `34-RUN-MOBILE.md`, unique fichier de ce dépôt touché par ce plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md
@plugin/mobile-test/README.md
@plugin/mobile-test-team/README.md
@plugin/mobile-test/scripts/mobile-test-run.mjs
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes déterministe a rendu les trois
exigences `unresolved`/`unclassified`. L'hypothèse portée par CE plan est déclarée ici,
explicitement — jamais résolue en silence par un filet, jamais supprimée.

**AGTS-02 — hypothèse signalée (probe : `unresolved` / `unclassified`).** L'exigence ne définit
nulle part le comportement attendu AUX ARÊTES du run. Ce plan ASSUME les trois arêtes suivantes,
les écrit dans la trace, et ne les invente donc pas au moment de l'exécution :

1. Un échec dont la cause est EXTERNE au dépôt du lab (backend Scroll-Off indisponible, réseau,
   timeout Metro) est un **ROUGE DE CAUSE EXTERNE**, nommé comme tel dans la note et distinct d'un
   rouge de pipeline — il ne condamne pas le module, il reporte la mesure.
2. Le budget de la boucle est de **2 cycles de fix maximum** ; au-delà, arrêt propre anti-thrash.
3. Un dépassement du timeout de build (20 min, valeur du script) est un **ROUGE DE PIPELINE**.

Aucune de ces trois arêtes n'est couverte par un `D-xx` de `34-CONTEXT.md`. Si Samuel les arbitre
autrement, c'est la TRACE qui est relue, pas le run qui est rejoué.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Tâche 1 : tranche verticale « cible résolue » — UDID porteur de l'état identifié, config projet posée, detect qui voit la cible, garde anti-destruction prouvée rouge</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md</files>
  <precondition>Les binaires `maestro`, un JDK, `node` et `xcrun simctl` sont résolvables sur le poste (mesurés PRESENTS le 2026-09-15 : Maestro 2.6.1, OpenJDK 17.0.19, Node v26.5.0, 7 simulateurs iPhone 17). Si l'un manque au moment de l'exécution, c'est un arrêt propre D-05 : écrire la cause dans `34-RUN-MOBILE.md`, poser le chapeau ROUGE, et ne pas poursuivre les tâches 2 et 3.</precondition>
  <read_first>
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Volet 2 » en entier (tableau des prérequis mesurés, mécanique de `mobile-test-run.mjs` fonction par fonction, Pitfalls 1/2/3, Code Examples) — base factuelle de cette tâche : la CONFIRMER commande par commande, ne pas la re-mesurer à l'aveugle
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « `34-RUN-MOBILE.md` (trace du run réel AGTS-02) » — gabarit exact de la note : chapeau de statut, section environnement mesuré, table de mesures avec commandes verbatim et sorties exactes
    - `plugin/mobile-test/scripts/mobile-test-run.mjs` — lire `loadConfig`, `ensureIosTarget`, `cmdRun`, `cmdDetect` AVANT toute commande, pour savoir ce que chaque argument déclenche réellement
    - `~/Documents/dev/Scroll-Off/frontend/.maestro/README.md` — la contrainte de session authentifiée non reproductible, dans les mots du lab lui-même
    - `~/Documents/dev/Scroll-Off/frontend/app.json` — confirmer `bundleIdentifier` (attendu `com.scrolloff.app.ios`) sur le fichier, pas depuis la recherche
    - `~/Documents/dev/Scroll-Off/.planning/config.json` — bloc `workflow`, vérifier la présence de la clé `use_worktrees`
    - `plugin/mobile-test/config/mobile-test.example.json` — défauts du gabarit de config, pour n'inventer aucune clé
  </read_first>
  <action>
    Poser la cible de bout en bout, une seule fois, et prouver que la chaîne tient AVANT de jouer
    quoi que ce soit. Six gestes, dans cet ordre.

    (1) **SHA de base.** Résoudre `git rev-parse HEAD` dans CE dépôt et le consigner tel quel :
    c'est le point de comparaison du diff de périmètre de la tâche 3.

    (2) **Garde anti-destruction d'abord.** Rechercher les deux motifs de remise à zéro d'état
    Maestro (celui qui vide les données de l'app, celui qui vide le trousseau — ils sont nommés
    verbatim dans `34-RESEARCH.md` Pitfall 1) dans tout l'arbre
    `~/Documents/dev/Scroll-Off/frontend/.maestro`. Attendu : aucune occurrence. Puis PROUVER que
    la garde peut rendre rouge : créer un flow jetable
    `~/Documents/dev/Scroll-Off/frontend/.maestro/zz-mutant-garde.yaml` portant l'un de ces deux
    motifs, re-jouer la recherche (elle DOIT trouver), supprimer le flow jetable, re-jouer la
    recherche (elle DOIT à nouveau ne rien trouver). Consigner les TROIS sorties dans la note,
    section `## Témoin de la garde`. Sans ce témoin, la garde est décorative — c'est le mode de
    défaut dominant de ce dépôt, mesuré en Phase 39.

    (3) **Résoudre l'UDID porteur de l'état.** `xcrun simctl list devices available --json`, isoler
    les deux entrées nommées « iPhone 17 Pro » et leurs UDID. Pour CHAQUE candidat, dans l'ordre :
    `xcrun simctl boot <udid>` (un boot seul n'efface rien), puis
    `xcrun simctl get_app_container <udid> com.scrolloff.app.ios`. Une sortie de chemin = l'app est
    installée sur CE simulateur ; une erreur = elle ne l'est pas. Si les DEUX portent l'app
    (hypothèse A1 de la recherche prise en défaut), départager par la date de modification du
    conteneur (`stat -f '%Sm' <chemin>`) et ÉCRIRE le critère de départage employé. Consigner la
    ligne littérale `UDID retenu : <udid>` et le tableau des deux candidats avec leur sortie exacte.

    (4) **Poser la config projet du lab.** Créer
    `~/Documents/dev/Scroll-Off/frontend/.vibeflow/mobile-test.json` avec les valeurs vérifiées :
    `bundleIdBase` = `com.scrolloff.app.ios`, `debugSuffix` vide, `ios.preferredSimulator` =
    `iPhone 17 Pro`, `maestroFlowsDir` = `.maestro`, `maestroBin` = `~/.maestro/bin/maestro`,
    `reportsDir` = `test-runs`, `metroPort` = `8081`, `javaHome` vide. Ce fichier fixe le NOM, pas
    l'UDID : `--target <udid>` prime et reste obligatoire (Pitfall 2). Écrire en toutes lettres
    dans la note que cette création est une PRÉPARATION PROJET et non un prérequis poste manquant
    au sens D-05.

    (5) **Aligner le guard d'isolation du lab.** Si `~/Documents/dev/Scroll-Off/.planning/config.json`
    n'a pas `use_worktrees` dans son bloc `workflow`, l'ajouter à `false` — `Scroll-Off` a un parent
    NON git (trois sous-dépôts) et le guard de dispatch y annonce une isolation par worktree à tort,
    avec un sentinel qui périme en 10 minutes. Correctif déjà appliqué avec succès dans ce lab par
    le passé ; à poser en tout début de run, jamais en réaction à un échec.

    (6) **Fermer la tranche par `detect`.** Exécuter
    `node plugin/mobile-test/scripts/mobile-test-run.mjs detect` depuis la racine de CE dépôt et
    vérifier que sa sortie JSON contient l'UDID retenu — le simulateur est booté ET visible par le
    script mécanique lui-même. C'est la preuve bout-en-bout de la tranche : de la résolution
    manuelle jusqu'à ce que le module voie la même cible. Consigner la sortie verbatim.

    Créer `34-RUN-MOBILE.md` dans la foulée, avec un chapeau provisoire (figé en VERT ou ROUGE par
    la tâche 3), la section `## Environnement mesuré` (versions Maestro / JDK / Node, UDID retenu,
    lab, garantie de non-remise à zéro d'état), la section `## Prérequis mesurés` (colonne
    PRESENT/ABSENT et preuve par commande pour chaque ligne du tableau de `34-RESEARCH.md` § Volet
    2), la section `## Témoin de la garde`, et la section `## Périmètre` portant la ligne
    `SHA de base : <sha>`.
  </action>
  <behavior>
    - La recherche des motifs de remise à zéro d'état sur l'arbre `.maestro` du lab ne trouve rien
      sur l'état réel, et trouve dès qu'un flow mutant en porte un (témoin exécuté, trois sorties).
    - `xcrun simctl get_app_container` rend un chemin sur exactement un des deux candidats
      « iPhone 17 Pro » — ou bien le critère de départage est écrit quand les deux répondent.
    - `mobile-test-run.mjs detect` rend un JSON qui contient l'UDID retenu.
    - `.vibeflow/mobile-test.json` du lab existe et est un JSON valide parsable par `node`.
    - `Scroll-Off/.planning/config.json` porte `workflow.use_worktrees` à `false`.
  </behavior>
  <acceptance_criteria>
    - `34-RUN-MOBILE.md` existe et contient une section dont le titre est exactement `## Prérequis mesurés`.
    - `34-RUN-MOBILE.md` contient une section `## Témoin de la garde` consignant les TROIS sorties de la mutation (propre → trouve → propre).
    - `34-RUN-MOBILE.md` contient une ligne `UDID retenu : <udid>` où `<udid>` a la forme simctl (8-4-4-4-12 hexadécimal majuscule).
    - `34-RUN-MOBILE.md` contient une section `## Périmètre` portant une ligne `SHA de base : <sha>` où `<sha>` fait 40 caractères hexadécimaux.
    - `~/Documents/dev/Scroll-Off/frontend/.vibeflow/mobile-test.json` existe et `node` le parse sans erreur.
    - `~/Documents/dev/Scroll-Off/.planning/config.json` a `workflow.use_worktrees` strictement égal au booléen `false`.
    - Aucun fichier de l'arbre `.maestro` du lab ne porte de motif de remise à zéro d'état à la fin de la tâche (flow mutant supprimé).
    - La sortie de `node plugin/mobile-test/scripts/mobile-test-run.mjs detect` contient l'UDID retenu, et cette sortie est recopiée verbatim dans la note.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; F="$HOME/Documents/dev/Scroll-Off/frontend"; N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md; test -f "$N"; grep -q "^## Prérequis mesurés" "$N"; grep -q "^## Témoin de la garde" "$N"; grep -q "^## Périmètre" "$N"; grep -qE "^SHA de base : [0-9a-f]{40}$" "$N"; U=$(sed -n "s/^UDID retenu : \([0-9A-F-]\{36\}\)$/\1/p" "$N" | head -1); test -n "$U"; test -f "$F/.vibeflow/mobile-test.json"; node -e "JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\"))" "$F/.vibeflow/mobile-test.json"; node -e "const c=JSON.parse(require(\"fs\").readFileSync(process.argv[1],\"utf8\")); process.exit(c.workflow && c.workflow.use_worktrees===false ? 0 : 1)" "$HOME/Documents/dev/Scroll-Off/.planning/config.json"; node -e "const fs=require(\"fs\"),p=require(\"path\"); const d=process.argv[1]; let bad=[]; for (const f of fs.readdirSync(d)) { const s=fs.readFileSync(p.join(d,f),\"utf8\"); if (/clearState|clearKeychain/.test(s)) bad.push(f); } if (bad.length) { console.log(\"GARDE VIOLEE: \"+bad.join(\",\")); process.exit(1); }" "$F/.maestro"; node -e "const{execSync}=require(\"child_process\"); const o=execSync(\"node plugin/mobile-test/scripts/mobile-test-run.mjs detect\").toString(); process.exit(o.includes(process.argv[1]) ? 0 : 1)" "$U"'</automated>
    <fails_when>sortie non nulle, ou la ligne `GARDE VIOLEE: <fichiers>` apparaît. Causes discriminantes : la note est absente, ou il lui manque `## Prérequis mesurés` / `## Témoin de la garde` / `## Périmètre` ; pas de ligne `SHA de base :` à 40 hexa ; pas de ligne `UDID retenu :` au format simctl ; la config projet du lab est absente ou n'est pas du JSON valide ; `use_worktrees` n'est pas le booléen `false` dans le config du lab parent ; un flow `.maestro` porte encore un motif de remise à zéro d'état (flow mutant témoin non supprimé) ; `detect` ne rend pas l'UDID retenu (simulateur non booté, ou UDID de la note différent de la cible réelle).</fails_when>
  </verify>
  <done>La cible est résolue sans ambiguïté, la config projet est posée, la garde anti-destruction a été prouvée capable de rendre rouge, et le module mécanique voit la même cible que la résolution manuelle. `34-RUN-MOBILE.md` porte l'environnement mesuré, le tableau des prérequis et le SHA de base.</done>
  <reversibility rating="reversible">D-04 : un run est rejouable, seule la trace est conservée. Les deux écritures dans le lab (config projet, clé `use_worktrees`) sont additives et supprimables sans perte.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : jouer le run — run --platform ios --target &lt;udid&gt; piloté par vf-test-orchestrator, au moins un cycle de fix, arrêt propre</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md</files>
  <precondition>La tâche 1 est verte : l'UDID retenu et le SHA de base sont consignés dans `34-RUN-MOBILE.md`, `.vibeflow/mobile-test.json` existe dans le lab, et la garde anti-destruction a été prouvée rouge puis remise au propre.</precondition>
  <read_first>
    - `34-RUN-MOBILE.md` tel qu'écrit par la tâche 1 — l'UDID retenu et le tableau des prérequis sont les entrées de cette tâche
    - `plugin/mobile-test-team/agents/vf-test-orchestrator.md` en entier — garde « projet applicable », entrée attendue, halt conditions, bloc typé de sortie : c'est le contrat du dispatch, à respecter mot pour mot
    - `plugin/mobile-test-team/references/test-loop-protocol.md` — invariants de la boucle, budgets, mapping des halt conditions
    - `plugin/mobile-test-team/README.md` § Limites — la condition de sortie côté équipe, mot pour mot
    - `plugin/mobile-test/README.md` § Limites — la condition de sortie côté pipeline, mot pour mot
    - `~/Documents/dev/Scroll-Off/frontend/.maestro/README.md` — quels flows supposent l'état authentifié (`lot2_*`, `lot3_*`, `lot4_*`) et lesquels non
    - `plugin/dev-orchestrator/references/mission-flow.md` — forme du bloc typé de retour attendu d'un sous-agent dispatché
  </read_first>
  <action>
    Faire jouer la boucle par l'ÉQUIPE, pas à la main. Le verdict EQUIPE n'a de valeur que si
    `vf-test-orchestrator` a réellement été dispatché en sous-agent.

    **Dispatch.** Invoquer `vf-test-orchestrator` (outil Agent/Task) avec un mandat explicite : lab
    `~/Documents/dev/Scroll-Off/frontend`, plateforme iOS uniquement, cible IMPOSÉE
    `--target <udid-retenu>` reprise depuis la note (ne pas la re-résoudre), flows existants de
    `.maestro` (aucun flow neuf n'est exigé — D-10, Claude's Discretion), budget de 2 cycles de fix
    maximum, et rappel verbatim des interdictions dures : aucune installation de dépendance
    système, aucune remise à zéro d'état ni désinstallation, aucun assert affaibli. Si l'outil de
    dispatch n'est pas disponible dans la boîte à outils de l'exécutant, REMONTER au manager de
    mission pour qu'il dispatche — ne PAS se rabattre sur une exécution manuelle de Maestro : cela
    prouverait `mobile-test` et ne prouverait rien de `mobile-test-team`, ce qui viderait la moitié
    de l'objet de ce plan. Consigner l'horodatage du dispatch et le bloc typé de retour dans une
    section `## Dispatch`.

    **Ce que l'orchestrateur exécute.** Depuis le lab :
    `node <chemin-du-script-mobile-test> run --platform ios --target <udid>`, puis, sur échec, le
    cycle diagnostic → fix applicatif (`vf-app-fixer`) → re-run, dans la limite du budget. L'app
    étant déjà installée sur la cible retenue, le chemin de build ne se redéclenche pas : c'est
    ATTENDU et c'est la déviation à documenter en tâche 3, pas un échec.

    **Arêtes, telles qu'assumées par ce plan** (aucune n'est couverte par un `D-xx`, cf.
    `<flagged_assumptions>`) : un échec de cause externe au dépôt du lab (backend indisponible,
    réseau, timeout Metro) est un ROUGE DE CAUSE EXTERNE, nommé comme tel ; un dépassement du
    budget de 2 cycles est un arrêt propre anti-thrash ; un dépassement du timeout de build est un
    ROUGE DE PIPELINE. Dans les trois cas : arrêt, trace, aucune réparation à la volée.

    **Signal d'alarme à respecter sans discussion** : un flow `lot2_*`/`lot3_*`/`lot4_*` qui échoue
    immédiatement sur un écran de connexion au lieu de l'écran attendu signifie que la session
    authentifiée est perdue. ARRÊT IMMÉDIAT, aucune retentative, perte écrite dans la note, statut
    ROUGE.

    **Consignation.** Ajouter la section `## Cycles`, un `### Cycle N` par itération (commande
    verbatim, code de sortie, compte pass/fail lu dans le JUnit, fichier de fix si cycle de
    correction), puis la ligne littérale `Rapport du run : <nom-de-fichier>` nommant le rapport
    horodaté réellement produit sous `~/Documents/dev/Scroll-Off/frontend/test-runs/`. Ce dossier
    contient déjà des rapports antérieurs : c'est le NOM EXACT du rapport de CE run qui fait
    preuve, jamais la présence du dossier. Poser enfin les deux lignes de verdict, au format exact
    `PIPELINE: VERT`/`PIPELINE: ROUGE` et `EQUIPE: VERT`/`EQUIPE: ROUGE`.
  </action>
  <behavior>
    - `vf-test-orchestrator` est dispatché en sous-agent et rend un bloc typé ; horodatage et
      verdict figurent dans la note.
    - Au moins un `### Cycle 1` existe, avec sa commande verbatim et son code de sortie.
    - Un rapport horodaté est produit sous `test-runs/` du lab, et son nom exact est cité.
    - Aucun flow `.maestro` du lab n'a été modifié pour affaiblir un assert (diff du lab relu).
  </behavior>
  <acceptance_criteria>
    - `34-RUN-MOBILE.md` contient une section `## Cycles` et au moins une sous-section dont le titre commence par `### Cycle `.
    - `34-RUN-MOBILE.md` contient une section `## Dispatch` citant le nom d'agent `vf-test-orchestrator` et l'horodatage du dispatch.
    - `34-RUN-MOBILE.md` contient une ligne `Rapport du run : <nom>` et le fichier `~/Documents/dev/Scroll-Off/frontend/test-runs/<nom>` existe réellement.
    - `34-RUN-MOBILE.md` contient une ligne `PIPELINE: VERT` ou `PIPELINE: ROUGE`, et une ligne `EQUIPE: VERT` ou `EQUIPE: ROUGE`.
    - Aucun fichier de l'arbre `.maestro` du lab ne porte de motif de remise à zéro d'état après le run.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; F="$HOME/Documents/dev/Scroll-Off/frontend"; N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md; grep -q "^## Cycles" "$N"; grep -q "^### Cycle " "$N"; grep -q "^## Dispatch" "$N"; grep -q "vf-test-orchestrator" "$N"; grep -qE "^PIPELINE: (VERT|ROUGE)$" "$N"; grep -qE "^EQUIPE: (VERT|ROUGE)$" "$N"; R=$(sed -n "s/^Rapport du run : //p" "$N" | head -1); test -n "$R"; test -f "$F/test-runs/$R"; node -e "const fs=require(\"fs\"),p=require(\"path\"); const d=process.argv[1]; let bad=[]; for (const f of fs.readdirSync(d)) { const s=fs.readFileSync(p.join(d,f),\"utf8\"); if (/clearState|clearKeychain/.test(s)) bad.push(f); } if (bad.length) { console.log(\"GARDE VIOLEE: \"+bad.join(\",\")); process.exit(1); }" "$F/.maestro"'</automated>
    <fails_when>sortie non nulle, ou la ligne `GARDE VIOLEE: <fichiers>` apparaît. Causes discriminantes : pas de section `## Cycles` ni de `### Cycle ` (la boucle n'a pas tourné, ou n'a pas été tracée) ; pas de section `## Dispatch` citant `vf-test-orchestrator` (le run a été joué à la main — le verdict EQUIPE serait sans fondement) ; une des deux lignes de verdict est absente ou mal formée ; pas de ligne `Rapport du run :`, ou le rapport nommé n'existe pas sous `test-runs/` du lab (nom inventé, ou rapport jamais produit — c'est la seule chose qui distingue ce run des rapports antérieurs déjà présents) ; un flow porte un motif de remise à zéro d'état.</fails_when>
  </verify>
  <done>Le run a été joué par l'équipe, ses cycles sont tracés commande par commande, le rapport horodaté produit est nommé et existe, et les deux verdicts PIPELINE / EQUIPE sont posés.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : figer la trace — chapeau de statut, déviations assumées, déclencheur de reprise si ROUGE, preuve de périmètre</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md</files>
  <read_first>
    - `34-RUN-MOBILE.md` dans son état de fin de tâche 2 — la totalité des mesures à synthétiser
    - `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-SPIKE-reference-transaction.md` lignes 1-30 — gabarit de chapeau (statut, date, verdict en une phrase, rappel de la condition qui déclenchait la mesure), analog exact désigné par `34-PATTERNS.md`
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Open Questions » question 3 — la formulation attendue de la déviation « build depuis zéro »
    - `.planning/BACKLOG.md` lignes 295-348 — gabarit d'entrée (`**Capturé :**` / `**Cadrage.**` / `**Pourquoi différé :**` / `**Déclencheur de resurgence :**`) à imiter pour le déclencheur de reprise rédigé ici puis transcrit par 34-06
    - `CLAUDE.md` racine § « Traçabilité des arbitrages » — canal et date obligatoires dès qu'un arbitrage humain est invoqué
  </read_first>
  <action>
    Figer la note en document opposable. Quatre gestes.

    (1) **Chapeau.** Remplacer le chapeau provisoire par une ligne unique, soit
    `> **Statut** : VERT`, soit `> **Statut** : ROUGE`, suivie de la date et d'un verdict en UNE
    phrase, puis du rappel de la condition qui déclenchait ce run (les deux sections Limites
    citées). Écrire la règle de composition DANS la note : le statut global est VERT si et
    seulement si PIPELINE et EQUIPE sont tous deux VERT. Un PIPELINE vert avec un EQUIPE rouge donne
    un chapeau ROUGE — il n'existe pas de demi-succès ici.

    (2) **Déviations assumées.** Section `## Déviations assumées`, une entrée par déviation, chacune
    sur une ligne commençant par `- [AAAA-MM-JJ] ` (la date entre crochets est le format compté par
    la vérification — une prose datée « au fil du texte » ne compte pas). Au minimum, si l'app
    était déjà installée : dire que la lecture
    littérale « build depuis zéro » de la condition de sortie n'a pas été satisfaite, que la
    préservation de la session authentifiée irremplaçable a primé (contrainte dure, irréversible),
    et que le chemin de build reste donc non exercé sur CE run. Nommer aussi le classement des deux
    manques lab (config projet, `use_worktrees`) comme préparation projet et non comme arrêt propre
    D-05.

    (3) **Déclencheur de reprise, si ROUGE.** Section `## Déclencheur de reprise`, au gabarit
    BACKLOG, portant la ligne `**Déclencheur de resurgence :**` : la cause précise de l'arrêt
    (jamais une formule vague), la date, et l'événement observable qui rouvrira AGTS-02. Le plan
    34-06 transcrira cette entrée telle quelle dans `.planning/BACKLOG.md` — elle doit donc être
    rédigée ici prête à coller, pas résumée.

    (4) **Preuve de périmètre.** Compléter la section `## Périmètre` : reprendre le
    `SHA de base : <sha>` posé en tâche 1 et prouver, par un diff SCOPÉ
    `git diff --quiet <sha> -- plugin scripts docs manual .github README.md README.fr.md`,
    qu'aucun fichier hors du dossier de phase n'a été touché par ce plan. Vérifier au passage
    qu'aucun secret du lab n'a été recopié et que tout arbitrage humain invoqué porte son canal et
    sa date.
  </action>
  <behavior>
    - Le chapeau porte exactement UNE ligne de statut, jamais zéro ni deux.
    - Le statut global est VERT uniquement si les deux verdicts de la tâche 2 sont VERT.
    - Une section `## Déviations assumées` existe, avec au moins une entrée datée.
    - Si le statut est ROUGE, une section `## Déclencheur de reprise` existe au gabarit BACKLOG.
    - Le diff scopé contre le SHA de base est vide sur tout ce qui n'est pas le dossier de phase.
  </behavior>
  <acceptance_criteria>
    - `34-RUN-MOBILE.md` contient EXACTEMENT une ligne correspondant à `> **Statut** : VERT` ou `> **Statut** : ROUGE`.
    - Cohérence vérifiable par machine : si le chapeau est VERT, la note porte aussi `PIPELINE: VERT` et `EQUIPE: VERT`.
    - `34-RUN-MOBILE.md` contient une section `## Déviations assumées` avec au moins une ligne commençant par `- [AAAA-MM-JJ] `.
    - Si le chapeau est ROUGE, `34-RUN-MOBILE.md` contient une section `## Déclencheur de reprise` portant une ligne `**Déclencheur de resurgence :**`.
    - `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 (forme SANS `..HEAD` : la comparaison porte sur l'arbre de travail face au SHA de base, donc elle attrape aussi bien un fichier commité qu'un fichier laissé non commité) (aucun fichier de ces chemins touché par ce plan).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; N=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md; S=$(grep -cE "^> \*\*Statut\*\* : (VERT|ROUGE)$" "$N"); [ "$S" -eq 1 ]; grep -q "^## Déviations assumées" "$N"; grep -qE "^- \[[0-9]{4}-[0-9]{2}-[0-9]{2}\] " "$N"; B=$(sed -n "s/^SHA de base : //p" "$N" | head -1); test -n "$B"; git diff --quiet "$B" -- plugin scripts docs manual .github README.md README.fr.md; if grep -qE "^> \*\*Statut\*\* : VERT$" "$N"; then grep -qE "^PIPELINE: VERT$" "$N"; grep -qE "^EQUIPE: VERT$" "$N"; else grep -q "^## Déclencheur de reprise" "$N"; grep -q "Déclencheur de resurgence :" "$N"; fi'</automated>
    <fails_when>sortie non nulle. Causes discriminantes : le compte de lignes de chapeau n'est pas 1 (statut absent, mal formé, ou dupliqué — c'est exactement ce que 34-04 lit comme précondition) ; chapeau VERT sans les deux verdicts VERT (règle de composition violée) ; pas de section `## Déviations assumées`, ou aucune entrée au format `- [AAAA-MM-JJ] ` (une déviation racontée en prose sans date en tête de ligne ne se retrouve pas à la relecture) ; pas de ligne `SHA de base :` exploitable ; `git diff --quiet` rend 1, c'est-à-dire qu'un fichier sous `plugin/`, `scripts/`, `docs/`, `manual/`, `.github/` ou l'un des deux README racine a été touché par ce plan alors qu'il ne devait pas l'être ; chapeau ROUGE sans section `## Déclencheur de reprise` ni ligne `Déclencheur de resurgence :` (report non tracé — AGTS-02 serait abandonnée en silence, ce que D-05 interdit).</fails_when>
    <human-check>Relire `34-RUN-MOBILE.md` en entier. Le verdict correspond-il à ce qui a été réellement observé, ou à ce qu'on espérait ? Une cause d'arrêt est-elle nommée avec assez de précision pour qu'un lecteur de dans six mois sache quoi retenter ? Aucun secret du lab (contenu d'un fichier d'environnement, jeton de session) n'a-t-il été recopié dans une note qui sera publiée ?</human-check>
  </verify>
  <done>`34-RUN-MOBILE.md` est une trace opposable, figée, à chapeau lisible par machine — consommable telle quelle par 34-04 comme précondition et par 34-06 comme source du ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dépôt `vibeflow-os` → lab `~/Documents/dev/Scroll-Off/frontend` | dépôt git DISTINCT, non gouverné par ce projet ; ce plan y écrit (config projet) et y fait écrire (fixes de la boucle). Franchissement réel, unidirectionnel, avec commits dans l'autre dépôt |
| Sous-agents VF (`vf-test-orchestrator` / `vf-test-runner` / `vf-app-fixer`) → simulateur iOS | exécution réelle sur un device virtuel porteur d'un état d'authentification NON REPRODUCTIBLE (code à 6 chiffres consommé) : toute destruction y est irréversible sans intervention humaine |
| Lab (fichiers d'environnement, jeton de session) → `34-RUN-MOBILE.md`, commité dans un dépôt PUBLIC | la trace est publiée ; ce qui y est recopié l'est définitivement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | état authentifié du simulateur (trousseau + session applicative du lab Scroll-Off) | high | mitigate | garde MACHINE avant tout run — aucun motif de remise à zéro d'état dans l'arbre `.maestro` du lab, prouvée capable de rendre rouge par un flow mutant témoin (tâche 1, section `## Témoin de la garde`, trois sorties consignées) ; `--target <udid>` explicite imposé au mandat de dispatch, jamais la résolution par nom (ambiguë sur ce poste) ; interdictions de désinstallation et de rebuild forcé inscrites en prohibitions de plan ET reprises verbatim dans le mandat ; signal d'alarme « écran de connexion inattendu » = arrêt immédiat sans retentative |
| T-34-02 | Denial of Service | poste de développement (installation à la volée d'un prérequis manquant) | medium | mitigate | D-05 appliqué littéralement : la `<precondition>` de la tâche 1 fait tomber le plan en arrêt propre et chapeau ROUGE si un prérequis poste manque ; aucune commande d'installation système n'est autorisée dans aucune action de ce plan, et l'interdiction est reprise verbatim dans le mandat de dispatch |
| T-34-03 | Information Disclosure | `34-RUN-MOBILE.md` publié dans un dépôt public | medium | mitigate | prohibition explicite de recopier tout contenu de fichier d'environnement, jeton de session ou identifiant privé ; seuls des identifiants publics sont consignés (UDID, bundle id, noms de flows, horodatages, codes de sortie) ; la `<human-check>` de la tâche 3 relit la note sur ce point précis avant clôture |
| T-34-04 | Repudiation | verdict EQUIPE non fondé (run joué à la main, présenté comme piloté par l'équipe) | medium | mitigate | le critère d'acceptation exige une section `## Dispatch` nommant `vf-test-orchestrator` et son horodatage, et la commande de vérification échoue si elle manque — le verdict ne peut pas être auto-déclaré sans trace de dispatch |

Aucun install de paquet (`npm`/`pip`/`cargo`) n'entre dans ce plan : la section « Package Legitimacy
Audit » de `34-RESEARCH.md` est déclarée non applicable à la phase, aucune menace `T-34-SC` n'est
donc ouverte ici. Le seul binaire exécuté est `maestro`, déjà présent sur le poste et hors périmètre
d'install de ce plan.
</threat_model>

<verification>
1. `34-RUN-MOBILE.md` existe et porte EXACTEMENT une ligne `> **Statut** : VERT` ou `> **Statut** : ROUGE`.
2. Les sections `## Environnement mesuré`, `## Prérequis mesurés`, `## Témoin de la garde`, `## Dispatch`, `## Cycles`, `## Déviations assumées`, `## Périmètre` sont toutes présentes ; `## Déclencheur de reprise` l'est si et seulement si le chapeau est ROUGE.
3. Les deux lignes `PIPELINE: …` et `EQUIPE: …` sont présentes et bien formées ; un chapeau VERT implique les deux à VERT.
4. Le rapport horodaté nommé par la ligne `Rapport du run :` existe réellement sous `test-runs/` du lab.
5. L'arbre `.maestro` du lab ne porte aucun motif de remise à zéro d'état ; le témoin de mutation est consigné avec ses trois sorties.
6. `git diff --quiet <SHA de base> -- plugin scripts docs manual .github README.md README.fr.md` rend 0 (forme SANS `..HEAD` : la comparaison porte sur l'arbre de travail face au SHA de base, donc elle attrape aussi bien un fichier commité qu'un fichier laissé non commité) — le périmètre annoncé est le périmètre réel, prouvé et rejouable.
7. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-01-SUMMARY.md` existe et consigne littéralement les mesures ci-dessus.
</verification>

<success_criteria>
- La question « `mobile-test`(-team) sort-il du statut expérimental ? » est tranchée par une PREUVE exécutée, pas par un arbitrage ni par une lecture de README.
- Le verdict est lisible par machine (chapeau compté à 1) et par un humain (verdict en une phrase, cause nommée).
- La session authentifiée irremplaçable du lab est intacte après le run, et la garde qui l'a protégée a été prouvée capable d'échouer.
- Si le run est ROUGE, AGTS-02 est reportée AVEC sa trace et son déclencheur de reprise daté — jamais abandonnée en silence (D-05).
- Aucun prérequis n'a été installé à la volée sur le poste ; aucun fichier de ce dépôt hors du dossier de phase n'a été touché, et le diff scopé le prouve.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| `34-RUN-MOBILE.md` | note de décision (trace de run) — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md` |
| `34-01-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-01-SUMMARY.md` |
| Entrée « déclencheur de reprise AGTS-02 » (si ROUGE) | texte rédigé DANS la note, transcrit au ledger par 34-06 | — |
| `.vibeflow/mobile-test.json` | config projet — NOUVEAU, dans le dépôt TIERS `Scroll-Off/frontend` ; hors `files_modified` de ce dépôt par construction | lab externe |
| `workflow.use_worktrees: false` | clé de config — dans le dépôt TIERS `Scroll-Off` ; hors `files_modified` de ce dépôt | lab externe |

Aucun symbole de code, aucun agent, aucun module n'est produit par ce plan.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-01-SUMMARY.md` en fin
de plan, consignant : le chapeau final et les deux verdicts, l'UDID retenu et le critère de son
choix, les trois sorties du témoin de garde, l'horodatage du dispatch de `vf-test-orchestrator` et
son bloc typé de retour, le nom exact du rapport horodaté produit, les déviations assumées, le SHA
de base et le résultat du diff scopé, et tout écart constaté entre ce plan et l'état réel du poste
ou du lab au moment de l'exécution.
</output>

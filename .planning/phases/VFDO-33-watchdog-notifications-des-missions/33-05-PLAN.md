---
phase: 33-watchdog-notifications-des-missions
plan: 05
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04"]
files_modified:
  - plugin/conductor/scripts/dag.sh
  - plugin/conductor/scripts/tests/test-dag.sh
  - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md
autonomous: true
requirements: [WTCH-03, WTCH-04]

must_haves:
  truths:
    - "`dag.sh mark` (déjà étendu par 33-02 d'un appel `record_progress()`) shelle EN PLUS un appel best-effort à `notify.sh` (sibling, plan 33-04) — mais UNIQUEMENT quand `status` est `done` ou `failed`. Sur `status=running`, AUCUN appel n'est fait : c'est la lecture littérale de « jamais à chaque tour » (WTCH-03, critère de succès n°3) — un `mark --status=running` est le geste le PLUS fréquent du DAG, une notification dessus serait exactement le bruit que le critère interdit."
    - "« Halt condition » (WTCH-03) : PRÉMISSE CORRIGÉE (revue externe `33-PLANCHECK-EXTERNE.md` C8, vérifiée sur disque avant d'écrire cette ligne) — `dag.sh:70` porte `VALID = {\"blocked\", \"ready\", \"running\", \"done\", \"failed\"}` et le bloc `mark` (`dag.sh:246`) écrit le statut REÇU EN ARGUMENT sans distinction : `mark --status=blocked` est un appel VALIDE, `blocked` N'EST DONC PAS réservé à `recompute()`. `mission-flow.md:245`, la source que ce plan cite lui-même pour justifier le choix, dit explicitement du halt autonome : « le laisser **`blocked`/`failed`** » — les deux valeurs, pas une seule. Le filtre de ce plan reste néanmoins volontairement borné à `done`/`failed` (voir bullet suivant) : couvrir aussi `blocked` exigerait un cas de test dédié ET la prise en compte de l'instabilité décrite ci-dessous — hors du périmètre resserré de cette correction ciblée. Cette limite est donc écrite ICI noir sur blanc comme une **limite assumée**, pas comme la conséquence d'une prémisse fausse : un `mark --status=blocked` explicite (le geste de gel documenté par `mission-flow.md:245`) ne déclenche PAS de notification par ce plan — à trancher séparément (déféré au manager : soit étendre le filtre de `record_milestone()` à `blocked` dans une correction ultérieure avec son propre cas de test, soit resserrer noir sur blanc `mission-flow.md` pour que la doctrine de gel dise explicitement « halte ⇒ `failed`, jamais `blocked` » et faire disparaître l'ambiguïté à la source)."
    - "Nuance d'instabilité (à connaître même si `blocked` reste hors filtre ici) : `recompute(nodes)`, appelé juste après l'écriture du statut dans le bloc `mark`, peut re-basculer un `blocked` fraîchement écrit vers `ready` si toutes ses `deps` sont déjà `done` — le geste « geler en `blocked` » n'a donc PAS la stabilité d'un `failed` (jamais retouché par `recompute`). C'est un argument de fond, indépendant de la correction de prémisse ci-dessus, pour ne pas étendre naïvement le filtre de notification à `blocked` sans traiter ce cas."
    - "L'interface CLI de `notify.sh` — CONFIRMÉE depuis l'écriture initiale de ce plan : la revue externe (`33-PLANCHECK-EXTERNE.md`, trois vérificateurs frais) a confronté l'assomption `[notify_sh, title, body]` (deux positionnels TITLE/BODY, exit toujours 0) à `33-04-PLAN.md` — nom, chemin, ordre et nombre d'arguments, absence de flags, code de sortie, comportement sur arguments manquants : AUCUNE divergence. La couture est saine. La consigne de prudence reste néanmoins active pour l'EXÉCUTION de ce plan : la tâche 1 relit quand même l'interface RÉELLE sur disque (33-04 committé par construction de la vague) avant d'écrire l'appel — au cas où le fichier réellement livré diverge du texte du plan qu'on relit ici — et consigne au SUMMARY si l'assomption tient toujours à ce moment-là. Le wrapper `try/except Exception: return` englobant reste la garantie de dégradation si, malgré tout, une divergence apparaissait."
    - "`notify.sh` ne doit JAMAIS produire de VRAIE notification système pendant l'exécution de `test-dag.sh` (ni sur la machine de dev macOS via `osascript`, ni en CI Linux via `notify-send` s'il est présent — `test-dag.sh` est découvert automatiquement par `ci.yml:213`) : 33-04 construit exactement trois points d'injection pour l'empêcher (`VF_NOTIFY_FORCE_CHANNEL`, shims de binaires via `VF_NOTIFY_BIN_DIR` préfixé au PATH, shim `uname`/`VF_PROC_VERSION_PATH`) — les cas T40/T41/T44 de ce plan, qui copient et instrumentent le VRAI `notify.sh` posé par 33-04, DOIVENT les utiliser. Copier le vrai script sans les positionner ferait trouver `osascript`/`notify-send` réels sur le PATH hérité et déclencherait un vrai toast à chaque exécution locale de la suite."
    - "Aucune nouvelle entrée n'est ajoutée à `plugin/conductor/hooks/hooks.json` : `notify.sh` est invoqué PROGRAMMATIQUEMENT en sous-processus depuis `dag.sh`, jamais câblé comme un hook Claude Code — le gate d'armement réel des hooks (PORT-05, `.github/workflows/ci.yml` L777-901, D-33-D) n'a donc RIEN de nouveau à vérifier pour ce plan. La preuve attendue de WTCH-04 pour ce plan est une NON-RÉGRESSION : `expect_total` (dérivé par PORT-05 de la fermeture des `hooks.json`) reste identique avant/après, parce que `hooks.json` n'est jamais touché — vérifié localement par diff, pas par un run CI complet (interdit de push par le mandat)."
    - "Aucune édition de `plugin/dev-orchestrator/scripts/check-capability-activation.sh` (`ARM[]`/`OKID[]`, D-33-D) : ce plan ne déclare aucun nouvel armement de frontmatter `vf-requires:`/`vf-provides:` — le réservé conditionnel de D-33-D ne se déclenche pas ici."
    - "La recette de clôture Windows (D-33-C) est écrite comme un DOCUMENT, jamais exécutée dans ce plan (aucune machine Windows disponible) : elle se rattache explicitement au fil des testeurs Windows de l'issue #20 (vérifié par le cadrage, `gh issue view 20`, section 7), porte la mention explicite « condition de clôture, pas un gate dur », et reproduit — sans les inventer ni les combler — les trois zones NON PROUVÉES du spike (chaîne Windows jamais exécutée réellement, AUMID arbitraire vs AUMID PowerShell non tranché, latence `powershell.exe` non mesurée en conditions réelles)."
  artifacts:
    - "plugin/conductor/scripts/dag.sh : fonction `record_milestone(notify_sh, nid, status, mission_file)`, résolution `NOTIFY_SH` en sibling (même patron `SCRIPT_DIR` que `DRIVER_LOCK_SH`, 33-02), appel dans le bloc `mark` filtré sur `status in (\"done\", \"failed\")`"
    - "plugin/conductor/scripts/tests/test-dag.sh : cas T40 à T45"
    - ".planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md : recette de validation humaine Win10/11, rattachée au fil testeurs de l'issue #20, condition de clôture non exécutée"
  key_links:
    - "record_progress() (33-02) <-> record_milestone() (ce plan) : DEUX fonctions séparées, jamais fusionnées — `record_progress` avance `progress_epoch` à CHAQUE mark (y compris `running`), `record_milestone` notifie SEULEMENT sur `done`/`failed`. Les fusionner romprait la garantie « jamais à chaque tour » et coupleraient deux contrats stables indépendamment (33-01 pour l'un, 33-04 pour l'autre) à un seul point de panne."
    - "notify.sh (plan 33-04) <-> record_milestone() : l'appel prend la forme `[notify_sh, title, body]`, deux positionnels — la couture est vérifiée saine (voir truths), la tâche 1 relit quand même le disque réel au moment de l'exécution, jamais l'assomption figée par écrit."
    - "PORT-05 (CI, .github/workflows/ci.yml L777-901) <-> ce plan : la preuve WTCH-04 attendue ici est que `plugin/conductor/hooks/hooks.json` n'apparaît JAMAIS dans le diff de ce plan — PORT-05 n'a rien de nouveau à couvrir, exactement comme pour le battement de la Phase 32 (D-32-07 corollaire)."
    - "`save(dag)` (bloc `mark`) <-> `record_milestone()` : ORDRE STRICT — `record_milestone()` s'appelle APRÈS `save(dag)`, jamais avant. `33-TERRAIN.md` avertit que `save()` réécrit le fichier ENTIÈREMENT et n'est PAS atomique ; `run_bounded` (patron de test, `test-dag.sh`) tue le process à **5 s**, pas 10 s. Si l'appel de notification (ou son `subprocess.run` interne) est placé AVANT `save(dag)` et que `notify.sh` pend, un `timeout=` mal calibré peut faire tuer le process externe avant que la transition de statut soit persistée — la même classe de bug que 33-02 corrige pour `record_progress()` (coordination requise, cf. action tâche 1)."
    - "S2 (décision Samuel du jour) <-> `record_milestone()` : un point de contrôle EN LECTURE (heartbeat/stall, best-effort, ligne de signal sur stderr, jamais bloquant) est ajouté au bloc `mark` par 33-02/33-03, indépendamment de ce plan. Ce point de contrôle n'appartient PAS au périmètre de ce plan (33-02/33-03 le portent) — mais il partage le MÊME bloc `mark` que `record_milestone()`. Cohabitation : `record_milestone()` reste le DERNIER appel du bloc `mark` (après le statut, `recompute`, `save(dag)`, ET après l'appel de contrôle S2 s'il est déjà présent au moment de l'exécution) — une notification de fin de nœud doit refléter l'état final déjà persisté et déjà diagnostiqué, jamais l'inverse. Si l'argv positionnel de `dag.sh` gagne un nouvel index côté S2 avant l'exécution de ce plan, `NOTIFY_SH` prend l'index LIBRE suivant, relu sur le disque réel au moment de l'exécution (ne jamais présumer l'index 10 sans relire l'état réel du fichier après 33-02/33-03)."
  prohibitions:
    - "Ne JAMAIS appeler `notify.sh` sur `status == \"running\"` — seul `done`/`failed` déclenche l'appel (WTCH-03 critère n°3, « jamais à chaque tour »)"
    - "Ne JAMAIS ajouter d'entrée à `plugin/conductor/hooks/hooks.json` dans ce plan"
    - "Ne JAMAIS éditer `plugin/dev-orchestrator/scripts/check-capability-activation.sh` (`ARM[]`/`OKID[]`) — réserve conditionnelle de D-33-D non déclenchée par ce périmètre"
    - "Ne JAMAIS toucher `plugin/conductor/scripts/check-guard-health.sh` ni `plugin/conductor/scripts/tests/test-check-guard-health.sh` — territoire de 33-03 (ce plan lance sa suite en non-régression, ne l'édite jamais) ; le halt condition couvert par ce plan reste borné à `mark --status=failed`, sans ce fichier"
    - "Ne JAMAIS faire dépendre le succès de `dag.sh mark` (mise à jour du statut, `recompute()`, `save(dag)`) de la réussite de l'appel `notify.sh` — même garantie de dégradation que `record_progress()` (33-02) : absent, non exécutable, sortie non nulle, JSON ou stdout quelconque, ou pendaison, aucun de ces cas ne fait jamais échouer `mark`"
    - "Aucun `shell=True`, aucune interpolation de `nid`/`status`/`title`/`body` dans une chaîne de commande shell — liste d'arguments uniquement, patron déjà établi par `record_progress()`"
    - "Ne JAMAIS prétendre avoir vérifié la chaîne de notification Windows réelle dans `33-CLOTURE-WINDOWS.md` — aucune machine Windows disponible, la recette reste NON EXÉCUTÉE, à écrire comme telle"
    - "Aucun `sleep` non borné dans les cas de test — patron `run_bounded`/isolation par copie déjà établi (T33/T36-T38)"
    - "Ne JAMAIS copier/instrumenter le VRAI `notify.sh` dans un cas de test (T40/T41/T44) sans positionner `VF_NOTIFY_FORCE_CHANNEL` ET `VF_NOTIFY_BIN_DIR` (shims) — un vrai `osascript`/`notify-send` trouvé sur le PATH hérité déclencherait un toast système réel à chaque exécution de `test-dag.sh`"
    - "Ne JAMAIS placer l'appel à `record_milestone()` avant `save(dag)` dans le bloc `mark` — `run_bounded` tue à 5 s, pas 10 s ; une transition tuée avant persistance serait perdue"
---

<objective>
Câbler le dernier jalon de WTCH-03 : `notify.sh` (posé par le plan 33-04, sibling de `dag.sh`)
part aux DEUX moments où `dag.sh mark` clôt un nœud — `done` (fin de nœud) et `failed` (halt
condition couverte par ce plan ; `blocked` reste un signal de halte valide côté doctrine mais hors
filtre, limite assumée documentée dans `<truths>`) — jamais sur `running`, qui est le geste le plus
fréquent du DAG et que le critère de succès n°3 exclut explicitement (« jamais à chaque tour »).
`depends_on` inclut désormais 33-03 (ses tests de non-régression sont invoqués par ce plan).

Purpose : sans ce câblage, `notify.sh` (33-04) reste un script isolé jamais appelé en usage réel —
ce plan est ce qui rend WTCH-03 VRAI en pratique, symétrique à ce que 33-02 a fait pour WTCH-01.
Il ferme aussi WTCH-04 pour son propre périmètre : aucune entrée `hooks.json` n'est ajoutée (l'appel
est un sous-processus programmatique, pas un hook Claude Code), donc le gate PORT-05 n'a rien de
neuf à couvrir — la preuve attendue est une non-régression, pas une extension du gate. Enfin, il
consigne la recette de clôture Windows (D-33-C) comme document rattaché au fil testeurs de
l'issue #20, sans jamais prétendre l'avoir exécutée.

Output : `dag.sh` étendu d'une fonction et d'un appel filtré dans le bloc `mark` ; `test-dag.sh`
étendu de 6 cas (T40-T45) couvrant le filtre `done`/`failed`/`running`, les pannes dégradées, et la
non-régression statique ; un document de clôture Windows nouveau, jamais exécuté.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-02-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-03-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-04-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-SPIKE-canal-notification.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-PLANCHECK-EXTERNE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : `record_milestone()` câblé au point `mark`, filtre `done`/`failed`, dégradation garantie</name>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <read_first>
    - plugin/conductor/scripts/dag.sh (état réel APRÈS exécution de 33-02 — `SCRIPT_DIR`/`DRIVER_LOCK_SH` en tête, `record_progress()` après `resolve_gsd_tools_cmd()`, son appel dans le bloc `mark` juste après `idx[nid]["status"] = status`) : lire le fichier réel sur disque, pas les numéros de ligne du plan 33-02 qui ne correspondent qu'à l'état PRÉ-exécution
    - plugin/conductor/scripts/notify.sh (état réel APRÈS exécution de 33-04, sibling déjà posé par construction de la vague — plan 33-05 dépend de 33-04) : lire son interface CLI RÉELLE (nombre et ordre des arguments positionnels, flags éventuels) AVANT d'écrire l'appel — si elle diverge de l'assomption `[notify_sh, title, body]` documentée dans `33-SPIKE-canal-notification.md`, s'aligner sur le disque, consigner l'écart au SUMMARY
    - plugin/dev-orchestrator/references/mission-flow.md:243-246 (« halte de nœud, jamais de mission... le laisser blocked/failed ») — fondement du choix « halt condition = mark --status=failed », à ne pas rouvrir vers `check-guard-health.sh` ni `dev-orchestrator`
    - .github/workflows/ci.yml L777-901 (gate PORT-05 — dérive `expect_total` des `hooks.json` de la fermeture installée) — pour vérifier que ce plan ne change RIEN à ce total
    - plugin/conductor/scripts/tests/test-dag.sh (bloc T33/T36-T38, patron d'isolation par copie de `dag.sh` + faux sibling exécutable dans un répertoire ISOLÉ du `WORK_DIR`) — à répliquer pour un faux `notify.sh` qui journalise son argv
    - plugin/conductor/scripts/tests/test-dag.sh, fonction `run_bounded` (relire la valeur RÉELLE du délai de `kill -9` — mesurée à **5 s**, PAS 10 s comme un texte de plan pourrait le laisser penser) — calibrer tout `timeout=` interne à `record_milestone()` largement EN DESSOUS de cette valeur, pas au-dessus ni égal
    - `33-04-PLAN.md` §comportement N1-N6 (`VF_NOTIFY_FORCE_CHANNEL`, `VF_NOTIFY_BIN_DIR` préfixé au PATH avec shims journalisant `"$@"`) : les cas T40/T41/T44 qui copient le VRAI `notify.sh` DOIVENT positionner ces deux variables — sinon la cascade réelle de `notify.sh` cherche `osascript`/`notify-send` sur le PATH hérité et peut déclencher un vrai toast
    - état réel de `dag.sh` après exécution de 33-02/33-03 (si déjà commités au moment de l'exécution) pour le point de contrôle S2 éventuellement inséré dans le même bloc `mark` — relire l'ordre des appels et l'index argv réellement posés, ne jamais présumer que `NOTIFY_SH` reste au 10e argument si S2 en a inséré un nouveau avant
  </read_first>
  <behavior>
    - T40 : lock acquis, `dag.sh mark --status=done` sur un nœud, sibling `notify.sh` RÉEL (celui posé par 33-04) copié dans le répertoire isolé et INSTRUMENTÉ pour journaliser son `"$@"` dans un fichier — invoqué avec `VF_NOTIFY_FORCE_CHANNEL` (ex. `linux`) ET `VF_NOTIFY_BIN_DIR` (répertoire de shims inoffensifs préfixé au PATH, AUCUN `osascript`/`notify-send` réel accessible) positionnés dans l'environnement de la copie isolée de `dag.sh` — AUCUN vrai toast système ne doit jamais apparaître → le journal contient exactement 2 champs, le premier (TITLE) mentionne l'idée de fin/complétion, le second (BODY) contient l'id du nœud.
    - T41 : mêmes conditions (mêmes `VF_NOTIFY_FORCE_CHANNEL`/`VF_NOTIFY_BIN_DIR`), `dag.sh mark --status=failed` → le journal de `notify.sh` contient 2 champs, le premier (TITLE) mentionne l'idée de halte/échec, le second (BODY) contient l'id du nœud.
    - T42 : `dag.sh mark --status=running` sur un autre nœud → AUCUNE ligne n'apparaît dans le journal de `notify.sh` (fichier de journal absent OU vide, contrôlé explicitement) — cas de DISCRIMINANCE, à ne JAMAIS retirer : sans lui, la suite resterait verte avec un `dag.sh` qui notifierait à chaque tour.
    - T43 : `notify.sh` ABSENT du répertoire isolé → `dag.sh mark --status=done` sort 0, le DAG est mis à jour normalement (`status` reflète `done`), aucune erreur affichée.
    - T44 : `notify.sh` ISOLÉ qui PEND (`sleep 30`, patron T38), `VF_NOTIFY_FORCE_CHANNEL`/`VF_NOTIFY_BIN_DIR` positionnés comme T40/T41 (le shim qui pend NE DOIT PAS non plus pouvoir déclencher un vrai canal) → `dag.sh mark --status=failed` revient en moins de **5 s** (`run_bounded` tue à 5 s, PAS 10 s — ne pas recopier l'ancienne affirmation), sort 0, ET **le DAG est bien persisté `status=failed`** (relu depuis le fichier après l'appel, pas seulement `rc=0` inspecté) — preuve que l'appel de notification est placé APRÈS `save(dag)` et que son `timeout=` interne (calibré nettement sous 5 s) protège réellement l'appelant sans jamais pouvoir faire perdre la transition déjà écrite.
    - T45 : non-régression statique — aucun `shell=True` (compte inchangé) ; l'appel à `notify.sh` passe une LISTE (`grep` positif sur la forme `[notify_sh, title, body]`) ; `hooks.json` n'apparaît dans AUCUN diff produit par ce plan (vérifié par `git diff --name-only` ancré sur le commit de BASE de ce plan — celui juste avant son premier commit, jamais une plage vague — hors suite bash, consigné au SUMMARY, pas un cas T) ; l'appel à `record_milestone(...)` apparaît TEXTUELLEMENT après `save(dag)` dans le bloc `mark` de `dag.sh` (vérifiable par position de ligne).
  </behavior>
  <action>
1. En tête de `dag.sh`, à côté du calcul déjà posé par 33-02 (`SCRIPT_DIR`/`DRIVER_LOCK_SH`), ajouter :
   ```
   NOTIFY_SH="$SCRIPT_DIR/notify.sh"
   ```
   Même sibling, même garantie (même glob d'installation, 33-TERRAIN.md §4) : jamais un candidat
   relatif au cwd.

2. Étendre la ligne d'invocation du heredoc python pour passer `"$NOTIFY_SH"` en 10e argument
   positionnel, et la capture `sys.argv[1:9]` déjà étendue par 33-02 pour lire aussi
   `notify_sh = sys.argv[10]` (ou l'index qui suit immédiatement `driver_lock_sh` dans l'état réel
   du fichier après 33-02 — relire l'ordre exact posé par 33-02 avant d'ajouter cet index, ne pas le
   supposer).

3. Ajouter la fonction `record_milestone(notify_sh, nid, status, mission_file)` juste après
   `record_progress()` (même fichier, même style) :
   - retourne immédiatement si `status` n'est ni `"done"` ni `"failed"` — c'est le filtre qui
     applique « jamais à chaque tour » ;
   - retourne si `notify_sh` n'est pas un fichier (`os.path.isfile`) ;
   - construit `title`/`body` à partir de `nid`, `status`, `os.path.basename(mission_file)` — deux
     chaînes courtes, lisibles, sans dépendance à une locale (ex. `"VibeFlow - noeud termine"` /
     `"VibeFlow - halte"`, corps `f"{nid} ({status}) - {os.path.basename(mission_file)}"`) ; AVANT
     d'écrire ces littéraux, relire l'interface réelle de `notify.sh` posée par 33-04 (read_first) et
     s'y conformer si elle prescrit une forme différente (ex. flags nommés au lieu de positionnels) —
     consigner l'écart au SUMMARY si l'assomption du spike n'a pas tenu ;
   - appelle `subprocess.run([notify_sh, title, body], stdout=subprocess.DEVNULL,
     stderr=subprocess.DEVNULL, timeout=2, check=False)` (ou la forme réelle relue à l'étape
     précédente) — résultat NON inspecté, best-effort. `stdout=DEVNULL, stderr=DEVNULL` PAS
     `capture_output=True` : `capture_output=True` ouvre un PIPE hérité par tout descendant du
     process (y compris un canal détaché en arrière-plan côté `notify.sh`) — un canal qui oublie de
     rediriger sa propre sortie garderait ce pipe ouvert et ferait attendre l'appelant jusqu'à l'EOF,
     mesuré à plusieurs secondes de blocage sur une variante de détachement de façade (33-04 N8/N15).
     `DEVNULL` ferme la question à la source, sans dépendre d'une garantie posée par un autre plan.
     `timeout=2`, PAS `5` : `run_bounded` (patron de test, `test-dag.sh`) tue le process externe à
     **5 s** — un `timeout=` interne égal ou supérieur risquerait une course avec ce `kill -9` externe
     (l'exception `TimeoutExpired` interne doit avoir la place de se lever et d'être absorbée par le
     `try/except` englobant AVANT que `run_bounded` ne tue tout depuis l'extérieur) ;
   - toute la fonction est enveloppée d'un `try/except Exception: return` unique englobant, patron
     `record_progress()`/`compute_stages()`.

4. Dans le bloc `if action == "mark":`, ajouter l'appel `record_milestone(notify_sh, nid, status,
   file)` en TOUT DERNIER, APRÈS `save(dag)` — jamais avant, contrairement au placement initial de
   `record_progress(driver_lock_sh)` posé par 33-02 (33-02 est corrigé en parallèle sur ce même point ;
   si l'état réel du fichier au moment de l'exécution montre encore `record_progress(...)` placé AVANT
   `save(dag)`, ne PAS reproduire ce placement pour `record_milestone` — le signaler au SUMMARY comme
   divergence constatée plutôt que l'absorber en silence). Si un point de contrôle S2 (lecture de
   stall, best-effort, ligne stderr) est déjà inséré dans le bloc `mark` par 33-02/33-03 au moment de
   l'exécution, `record_milestone(...)` reste APRÈS cet appel aussi — une notification de fin de nœud
   doit toujours arriver après que l'état a été à la fois persisté et diagnostiqué. Aucun des appels
   ajoutés par ce plan ne doit jamais empêcher `recompute`/`save`/`emit`/`sys.exit(0)` de s'exécuter
   (garanti par le `try/except` interne de `record_milestone`).

5. Mettre à jour le bloc de commentaire d'en-tête pour documenter que `mark` notifie désormais via
   `notify.sh` sur `done`/`failed` uniquement, en best-effort, sans jamais bloquer le DAG (WTCH-03).

Étendre ensuite `plugin/conductor/scripts/tests/test-dag.sh` avec les cas T40 à T45 décrits en
`<behavior>`. Réutiliser le patron d'isolation (copie de `dag.sh` + faux sibling déposé dans un
répertoire ISOLÉ du `WORK_DIR`, jamais le vrai `notify.sh` de production sauf pour T40/T41/T44 où le
VRAI script (posé par 33-04) est copié puis instrumenté d'une ligne de journalisation ajoutée
temporairement dans la copie — jamais dans le fichier de production). Pour T40/T41/T44
spécifiquement : exporter `VF_NOTIFY_FORCE_CHANNEL` (un canal forcé, ex. `linux`) ET
`VF_NOTIFY_BIN_DIR` (répertoire de shims créé par le test, préfixé au PATH de l'appel isolé,
contenant UNIQUEMENT des exécutables factices qui journalisent leur invocation — jamais le vrai
`osascript`/`notify-send`/`powershell.exe` du système) avant d'invoquer la copie isolée de `dag.sh` —
patron déjà posé par `test-notify.sh` (33-04), à réutiliser tel quel, pas réinventé ici.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -qE '^def record_milestone' plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -c 'record_milestone(notify_sh' plugin/conductor/scripts/dag.sh` rend `2` (la définition ET l'unique site d'appel).
    - Le filtre `done`/`failed` est bien présent en tête de fonction : `sed -n '/^def record_milestone/,/^def /p' plugin/conductor/scripts/dag.sh | grep -c '"done"'` rend au moins `1` et `grep -c '"failed"'` sur le même bloc rend au moins `1`.
    - `grep -c 'shell=True' plugin/conductor/scripts/dag.sh` rend `0` (compte inchangé depuis 33-02).
    - L'appel passe une liste : `grep -c '\[notify_sh, title, body\]' plugin/conductor/scripts/dag.sh` rend `1` — SAUF si la tâche 1 a dû s'aligner sur une interface réelle différente de `notify.sh` (auquel cas ce critère est remplacé par son équivalent exact, consigné au SUMMARY avec le motif de l'écart).
    - Aucun candidat cwd-relatif : `grep -c 'os.getcwd' plugin/conductor/scripts/dag.sh` rend `0`.
    - `bash plugin/conductor/scripts/tests/test-dag.sh` sort 0, affiche les blocs `=== T40` à `=== T45`, et sa ligne de bilan affiche `0 FAIL`.
    - Les cas historiques (avant ce plan, incluant T1-T39 posés par 32/33-01/33-02) restent verts : comptage inchangé du nombre de PASS antérieur, consigné dans le SUMMARY.
    - Non-régression : `bash plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (33-01/33-02 non modifiés par ce plan, mais l'appel croisé au point `mark` doit rester intact).
    - Non-régression informationnelle (fichiers posés par les plans voisins de la même vague, présents par construction, pas une dépendance formelle de ce plan) : si `plugin/conductor/scripts/tests/test-check-guard-health.sh` existe, `bash` dessus sort 0 ; si `plugin/conductor/scripts/tests/test-notify.sh` existe, `bash` dessus sort 0.
    - **Portée-plan exacte** (pas portée-vague) : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD`
      (le `<SHA-de-base-de-ce-plan>` est le commit immédiatement AVANT le premier commit de CE plan,
      pas une plage arbitraire — à résoudre par l'exécutant via `git log` avant de lancer la commande)
      rend EXACTEMENT l'ensemble `{plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh, .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md}` (ou son sur-ensemble strict si un écart d'interface documenté au SUMMARY a forcé un fichier de plus) — **critère réellement falsifiable**, contrairement à une simple absence de `hooks.json` (trivialement vraie tant qu'on ne cherche pas ce fichier précis).
    - **`hooks.json` jamais touché** (corollaire du critère ci-dessus, gardé explicite car c'est la
      preuve WTCH-04) : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD | grep -c 'plugin/conductor/hooks/hooks.json'` rend `0`.
    - **`check-capability-activation.sh` jamais touché** : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD | grep -c 'check-capability-activation.sh'` rend `0`.
    - **Ordre `record_milestone()` après `save(dag)`** : dans le bloc `if action == "mark":` de
      `dag.sh`, le numéro de ligne de l'appel `record_milestone(` est STRICTEMENT SUPÉRIEUR au numéro
      de ligne de l'appel `save(dag)` du même bloc (vérifiable par `grep -n` sur les deux motifs dans
      la plage du bloc `mark`).
    - **Aucun vrai canal atteint en test** : `VF_NOTIFY_BIN_DIR`/`VF_NOTIFY_FORCE_CHANNEL` sont bien
      positionnés dans T40/T41/T44 (`grep -c 'VF_NOTIFY_FORCE_CHANNEL' plugin/conductor/scripts/tests/test-dag.sh` rend au moins `3`, un par cas) — et une exécution locale de `bash plugin/conductor/scripts/tests/test-dag.sh` ne fait apparaître AUCUNE notification système visible (vérification humaine ponctuelle sur macOS, consignée au SUMMARY).
    - **Mutation rouge prouvée, trace obligatoire dans le SUMMARY** : commiter d'abord ce plan, puis retirer temporairement le `try/except Exception: return` englobant de `record_milestone()`, déposer un `notify.sh` isolé SANS droit d'exécution (`chmod 000`) dans le répertoire de test → `dag.sh mark --status=done` doit alors CRASHER (le mutant prouve que la garde protège réellement `mark` contre une exception de `subprocess.run`, ex. `PermissionError`) ; consigner assertion exacte/attendu/obtenu ; restaurer par `git checkout -- plugin/conductor/scripts/dag.sh`.
    - **Contrôle positif du fixture T40/T41/T44** : avant d'invoquer la copie isolée de `dag.sh`, vérifier explicitement que le faux/instrumenté `notify.sh` déposé est bien exécutable et produit BIEN le comportement attendu quand invoqué directement (appel isolé, hors `dag.sh`, consigné dans le SUMMARY) — sinon T40/T41/T44 seraient verts pour la mauvaise raison.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/dag.sh && bash plugin/conductor/scripts/tests/test-dag.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && [ "$(grep -c 'shell=True' plugin/conductor/scripts/dag.sh)" -eq 0 ]</automated>
  </verify>
  <done>Chaque `dag.sh mark --status=done|failed` notifie via `notify.sh` en best-effort sans jamais bloquer le DAG ni ajouter d'entrée `hooks.json` ; `mark --status=running` ne notifie jamais ; la mutation qui retire la garde de dégradation fait effectivement crasher `mark`.</done>
  <reversibility rating="reversible">Une fonction, un appel filtré, une variable sibling, purement additifs : un `git revert` rend `dag.sh` identique à HEAD, `notify.sh` et `hooks.json` restent hors de portée de ce plan.</reversibility>
</task>

<task type="docs" tdd="false">
  <name>Tâche 2 : recette de clôture Windows, rattachée au fil testeurs de l'issue #20 (D-33-C)</name>
  <files>.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md</files>
  <read_first>
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md §D-33-C (rattachement vérifié au fil testeurs de l'issue #20, trois zones NON PROUVÉES à ne pas combler)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-SPIKE-canal-notification.md §Windows, §NON PROUVÉ (contenu exact à citer, pas à paraphraser au point de le déformer)
  </read_first>
  <behavior>
    - Le document existe, cite explicitement « issue #20 » et le fil testeurs Windows.
    - Le document porte la mention explicite « condition de clôture, pas un gate dur ».
    - Le document reproduit les trois zones NON PROUVÉES du spike (chaîne Windows jamais exécutée, AUMID arbitraire vs AUMID PowerShell non tranché, latence non mesurée) sans en ajouter une quatrième non sourcée.
    - Le document ne prétend jamais qu'un toast Windows a été observé ou vérifié pendant cette phase — formulation explicite du type « non exécuté, aucune machine Windows disponible pendant le cadrage ni l'exécution ».
  </behavior>
  <action>
Écrire `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` :
un en-tête situant le document (condition de clôture de la Phase 33, pas un gate dur, D-33-C) ; le
rattachement au fil testeurs Windows de l'issue #20 (référence explicite, motif : mêmes testeurs,
même terrain que le geste `--dry-run` de l'issue #20) ; une recette de validation humaine minimale
(installer le lab sur Win10/11, provoquer un `mark --status=done` puis `--status=failed` réels sur
une mission de test, confirmer visuellement l'apparition d'un toast natif dans les deux cas, répéter
sous WSL) ; les trois zones NON PROUVÉES reprises telles quelles du spike, sans en inventer une
quatrième. Aucune tentative d'exécution réelle dans ce plan.
  </action>
  <acceptance_criteria>
    - Le fichier existe.
    - `grep -c 'issue #20' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
    - `grep -c 'condition de clôture' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
    - `grep -c 'NON PROUV' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
  </acceptance_criteria>
  <verify>
    <automated>[ -f .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md ] && grep -q 'issue #20' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md</automated>
  </verify>
  <done>La recette de validation humaine Windows existe comme document rattaché au fil testeurs de l'issue #20, explicitement non exécutée, sans zone NON PROUVÉE comblée par supposition.</done>
  <reversibility rating="reversible">Un seul fichier markdown nouveau, aucune dépendance de code : supprimable sans effet de bord.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dag.sh` -> `notify.sh` (sous-processus) | sibling résolu par répertoire de script, jamais par cwd (même motif que 33-02 pour `driver-lock.sh`) ; l'appel ne dépend jamais du résultat pour la réussite de `mark` |
| `notify.sh` (indisponible/lent/interface divergente) -> `dag.sh mark` | un `notify.sh` absent, non exécutable, en échec, à l'interface inattendue, ou qui pend ne doit jamais pouvoir bloquer ni faire échouer la mise à jour du DAG |

## STRIDE Threat Register

Numérotation `T-33-16` à `T-33-19` (bloc réservé à ce plan — `T-33-05` à `T-33-15` sont déjà pris
par 33-01/33-02/33-03/33-04 pour des menaces différentes, écriture en parallèle ; collision
signalée par la revue externe, corrigée ici).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-16 | Denial of Service | `record_milestone()` | high | mitigate | `timeout=2` (marge sous les 5 s de `kill -9` externe de `run_bounded`) + `stdout/stderr=DEVNULL` (pas de pipe hérité) + `try/except Exception` englobant, appel placé APRÈS `save(dag)` : un `notify.sh` qui pend ou plante ne bloque jamais `mark` ET ne fait jamais perdre une transition déjà persistée ; cas T44 + mutation rouge |
| T-33-17 | Tampering | résolution du sibling `NOTIFY_SH` | medium | mitigate | résolution par répertoire du script `dag.sh` lui-même, jamais par cwd — même motif que 33-02/D-07, non rouvert ; cas T45 (grep statique) |
| T-33-18 | Injection | arguments du sous-processus `notify.sh` | low | accept | `subprocess.run` avec liste d'arguments, jamais `shell=True` ; `nid`/`status` proviennent du DAG local, pas d'une entrée réseau |
| T-33-19 | Elevation of Privilege | armement hors gate | medium | mitigate | aucune entrée `hooks.json` ajoutée par ce plan — l'appel est un sous-processus programmatique, jamais un hook Claude Code, donc rien à faire passer PORT-05 qui ne le soit déjà ; vérifié par diff nul sur `hooks.json` |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/dag.sh`
2. `bash plugin/conductor/scripts/tests/test-dag.sh` — 0 FAIL, cas T40 à T45 présents, historique intact
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé
4. `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD` (le commit immédiatement AVANT le premier
   commit de ce plan, résolu via `git log` par l'exécutant — jamais une plage devinée) liste
   EXACTEMENT les fichiers de `files_modified` (ou son sur-ensemble strict documenté au SUMMARY), et
   ne liste JAMAIS `plugin/conductor/hooks/hooks.json` ni `check-capability-activation.sh`
5. La mutation exigée a rougi et sa trace (assertion, attendu, obtenu) est dans le SUMMARY
6. `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` existe et cite l'issue #20
7. Non-régression du parc COMPLET des suites : `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` rend `65`, INCHANGÉ par rapport à l'état laissé par 33-04 (ce plan étend `test-dag.sh`, fichier déjà découvert ; le seul fichier neuf de la phase, `test-notify.sh`, a été ajouté par 33-04, pas ici). Le passage exhaustif des 65 suites reste la responsabilité de CI (`ci.yml` L213) ; ce plan ne le rejoue pas intégralement en local.
</verification>

<success_criteria>
- `dag.sh mark --status=done|failed` notifie via `notify.sh` en best-effort, l'appel placé APRÈS `save(dag)` ; `mark --status=running` ne notifie jamais (WTCH-03 critère n°3).
- « Halt condition » est couvert par ce plan comme `mark --status=failed` — limite assumée et documentée : `mark --status=blocked` reste un signal de halte valide côté doctrine (`mission-flow.md:245`) mais HORS du filtre de notification de ce plan (voir truths, correction de prémisse) ; aucune dépendance à un module hors `conductor` pour ce que ce plan couvre.
- Aucune entrée `hooks.json` ajoutée, aucune édition de `check-capability-activation.sh` — WTCH-04 fermé par non-régression, pas par extension du gate (D-33-D).
- La recette de clôture Windows existe, rattachée au fil testeurs de l'issue #20, explicitement non exécutée (D-33-C).
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-05-SUMMARY.md` en fin de plan,
en y consignant la trace de mutation exigée, le comptage avant/après des assertions de la suite, et
tout écart entre l'interface assumée de `notify.sh` et son interface réelle constatée sur disque.
</output>
</content>

---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 08
lot: "L08 — bump des trois modules touchés (conductor MINOR, planning-core et dev-orchestrator PATCH), CHANGELOG, rejeu final"
type: execute
wave: 5
depends_on: ["41.1-01", "41.1-02", "41.1-03", "41.1-04", "41.1-05", "41.1-06", "41.1-07", "41.1-09"]
autonomous: true
requirements: [WSAW-01, WSAW-02, WSAW-03, WSAW-04, WSAW-05, WSAW-06, WSAW-07]
files_modified:
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - plugin/planning-core/VERSION
  - plugin/planning-core/module.json
  - plugin/planning-core/CHANGELOG.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "CORRECTION C-04 (juge frais, 2e passe) — UN SEUL SEUIL, EN UNITÉS HOMOGÈNES. Le seuil de rejeu était ROUGE PAR CONSTRUCTION : la BASELINE comptait les STEPS NOMMÉES du job `gates` au merge-base (17, mesuré) alors que `n` est extrait de `BILAN REJEU gates : $5`, le nombre d étapes REJOUÉES, qui EXCLUT les sautées. MESURE RÉELLE du 2026-09-23 : `15 rejouee(s), 3 sautee(s), 0 en echec` — les 3 sautées étant l action `uses:` du checkout (sans `- name:`) et les DEUX étapes conditionnelles `if: … refs/heads/main`. Avec la step neuve de `41.1-05`, n = 16 face à un SEUIL de 17+1 = 18 : `[ 16 -ge 18 ]` FAUX. Le DERNIER plan de la phase ne pouvait JAMAIS rendre vert, même si tout le travail était correct"
    - "BASELINE DÉSORMAIS MESURÉE DANS LA MÊME UNITÉ QUE `n` : étapes NOMMÉES du job `gates` au merge-base MOINS étapes conditionnelles main-only. Relation VÉRIFIÉE le 2026-09-23 : 17 − 2 = 15, et l outil de rejeu mesure EXACTEMENT `15 rejouee(s)` — les deux chemins convergent sur le même nombre. AUCUN NOMBRE GELÉ : les deux termes sont relus du `ci.yml` de la base à chaque exécution, donc le seuil suit une évolution du job. Bloc REJOUÉ le 2026-09-23 après correction : `rejouees=15 nommees_base=17 conditionnelles_base=2 baseline_rejouees=15 delta_attendu=1 seuil=16`, rc=1 — correct AUJOURD HUI puisque la step de `41.1-05` n existe pas encore, et atteignable dès qu elle est posée"
    - "UN SEUL SEUIL, CELUI DU BLOC EXÉCUTABLE : le critère d acceptation en prose disait « ≥ 15 étapes » pendant que le bloc automatisé en exigeait 18 — deux seuils CONTRADICTOIRES dans le même plan. La prose ne porte plus AUCUN nombre : elle renvoie au seuil calculé par le bloc"
    - "BUMPS ASYMÉTRIQUES — `conductor` en MINOR, `planning-core` et `dev-orchestrator` en PATCH (CORRECTION Q3, arbitrage Samuel, AskUserQuestion session principale, 2026-09-23, option a). Règle appliquée, celle du `CLAUDE.md` racine : « nouveau module / nouvelle capacité → minor ; correctif / durcissement → patch ». MOTIF du minor sur `conductor` : `41.1-05` y crée DEUX ARTEFACTS NEUFS ET DISTRIBUÉS — le gate `check-planning-consumers-registered.sh` AVEC SA PROPRE ÉTAPE CI et sa suite, et `references/workstream-planning-consumers.md`. Un gate neuf livré en patch est INVISIBLE à qui lit les versions de module. `planning-core` et `dev-orchestrator` ne reçoivent, eux, que du durcissement de scripts existants et de la doctrine — patch"
    - "VERSIONS CIBLES — RELUES SUR DISQUE AU MOMENT DE L'EXÉCUTION, JAMAIS RECOPIÉES DE CE PLAN : un plan antérieur ou une release intercalaire a pu bumper entre-temps. Valeurs MESURÉES le 2026-09-23 sur cette branche, à titre indicatif SEULEMENT : `conductor` v1.40.0 → v1.41.0 (minor) ; `planning-core` v2.7.0 → v2.7.1 (patch) ; `dev-orchestrator` v2.23.1 → v2.23.2 (patch). La sonde de vérification ne compare PAS à ces littéraux : elle relit la valeur au merge-base et calcule l'incrément attendu selon le TYPE de bump de chaque module"
    - "CORRECTION C-11 (juge frais, 2e passe) — LA SONDE CHANGELOG ASSERTE : la troisième `<automated>` de la Task 1 était une boucle `for … echo … done` SANS AUCUNE ASSERTION, dont le `fails_when` disait « à comparer VISUELLEMENT dans la trace ». Rejouée le 2026-09-23 sur l'état actuel (AUCUN bump effectué, donc l'état qu'elle est censée condamner) : `conductor entries=69 premiere=[## [v1.40.0] …]`, `planning-core entries=13 premiere=[## [v2.7.0] …]`, `dev-orchestrator entries=54 premiere=[## [v2.23.1], 2026-09-23 : …]`, **rc du bloc = 0**. La synchronisation CHANGELOG ↔ VERSION — le SEUL point que `check-version-sync.sh` ne couvre pas — n'était gardée par rien d'exécutable"
    - "LE FORMAT RÉEL DE `dev-orchestrator` DIVERGE DU GABARIT et la comparaison machine doit le TOLÉRER : sa première entrée est `## [v2.23.1], 2026-09-23 : …` (virgule, deux-points) là où `conductor` et `planning-core` écrivent `## [vX.Y.Z] — <date>` (tiret cadratin). Vérifié sur les trois fichiers le 2026-09-23. L'extraction retenue est un `match` sur `^## \\[v[0-9]+\\.[0-9]+\\.[0-9]+\\]`, qui ne dépend QUE du crochet fermant et tolère donc les deux formes — vérifié en rejouant la sonde contre les trois fichiers RÉELS"
    - "CORRECTION C-19 (juge frais, 2e passe) — ABSORPTION DE LA SECTION « Non releasé » : `41.1-07` (vague 2) écrit dans `plugin/dev-orchestrator/CHANGELOG.md` une entrée en section `## Non releasé` explicitement « sans bump de VERSION à ce stade (reporté au plan 41.1-08) » ; CE plan (vague 5) ajoute dans le MÊME fichier une entrée `## [vX.Y.Z]` nommant LES MÊMES changements. Aucun des deux ne disait d'absorber la section transitoire, et le `<verify>` n°3 n'assertait rien — le fichier aurait porté une DOUBLE DESCRIPTION du même travail. Ce plan FUSIONNE le contenu de « Non releasé » dans l'entrée versionnée puis RETIRE l'en-tête, et l'ASSERTE (`## Non releasé` compté à 0)"
    - "Bump COHÉRENT dans les DEUX fichiers de chaque module (`VERSION`, `module.json` champ `version`) — jamais un seul des deux, patron `check-version-sync.sh` déjà en gate CI"
    - "La `VERSION` RACINE (`/VERSION`), `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, les deux README (`README.md`/`README.fr.md`) et le `CHANGELOG.md` RACINE ne sont PAS touchés par ce plan — hors périmètre explicite (release en vol ailleurs, cf. mandat)"
    - "CHANGELOG de chaque module : une entrée `## [vX.Y.Z] — <date d'exécution>` qui nomme les fichiers livrés PAR CE MODULE dans cette phase — pas une recopie générique des trois modules dans chaque fichier. `conductor` : `check-divergence.sh` (dédup, trou fermé), `workstream-planning-consumers.md`, `check-planning-consumers-registered.sh`, `check-state-integrity.sh` (fermeture du fail-open `--file` absolu, plan 41.1-09, feu vert session principale). `planning-core` : `workstream-policy.sh` (`vf_ws_enumerate`), `check-planning-state.sh`, `detect-gsd-engine.sh`. `dev-orchestrator` : `discover-unintegrated-docs.sh`, `references/workstreams.md`"
    - "`git status --porcelain` (jamais `rtk` pour cette vérification précise — mesurer en `rtk proxy git status --porcelain`, motif mémoire `rtk-fausse-les-verifications-d-etat`) ne liste, avant le commit de ce plan, QUE les chemins des plans 41.1-01 à 41.1-07 déjà commités PLUS les neuf fichiers de ce plan — aucun fichier hors périmètre touché par accident"
    - "Rejeu final COMPLET des deux jobs CI (`gates`, `tests`) sur la découverte intégrale, jamais une liste recopiée — 0 échec, toutes les étapes des plans 41.1-01 à 41.1-08 vertes ensemble pour la première fois dans le même arbre"
    - "`.github/workflows/ci.yml` `check-version-sync.sh` (déjà un gate existant du job `gates`) doit rendre 0 sur les trois bumps — c'est la preuve machine que les triades VERSION/module.json/badges sont cohérentes, pas une relecture manuelle"
  artifacts:
    - path: "plugin/conductor/VERSION"
      provides: "version MINOR bumpée (gate neuf distribué par 41.1-05, Q3), cohérente avec module.json"
    - path: "plugin/planning-core/VERSION"
      provides: "version PATCH bumpée, cohérente avec module.json"
    - path: "plugin/dev-orchestrator/VERSION"
      provides: "version PATCH bumpée, cohérente avec module.json"
  key_links:
    - "`scripts/check-version-sync.sh` (déjà câblé en gate CI, `ci.yml` ~ligne 341) est le VÉRIFICATEUR MACHINE de ce plan — pas une relecture visuelle des trois triades"
    - "Ce plan dépend de TOUS les plans précédents de la phase : c'est le SEUL plan qui bump, exécuté une fois tous les fichiers de chaque module stabilisés — éviter un bump prématuré qui devrait être corrigé par un plan suivant"
---

<objective>
Clore la phase par le geste de version : bump des trois modules réellement modifiés — `conductor`
en MINOR (deux artefacts neufs et distribués), `planning-core` et `dev-orchestrator` en PATCH (Q3)
(`conductor`, `planning-core`, `dev-orchestrator`), CHANGELOG par module, puis rejeu final complet
des deux jobs CI sur l'arbre entier de la phase — la première fois que tous les plans 41.1-01 à
41.1-07 sont mesurés ENSEMBLE.

Purpose: la discipline de release de ce dépôt (CLAUDE.md racine) exige un bump cohérent pour tout
module dont le contenu distribué change ; ce plan porte ce geste, sans toucher à la racine
(release séparée, hors périmètre).
Output: trois modules bumpés (conductor minor, les deux autres patch), CHANGELOG à jour, rejeu final vert.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 — VALEURS RELUES SUR DISQUE, jamais recopiées de ce plan (un
plan antérieur de cette même phase a pu déjà bumper si l'ordre d'exécution diffère du DAG) :
- `plugin/conductor/VERSION`, `plugin/conductor/module.json`, `plugin/conductor/CHANGELOG.md` (tête)
- `plugin/planning-core/VERSION`, `plugin/planning-core/module.json`, `plugin/planning-core/CHANGELOG.md` (tête)
- `plugin/dev-orchestrator/VERSION`, `plugin/dev-orchestrator/module.json`, `plugin/dev-orchestrator/CHANGELOG.md` (tête)
- `scripts/check-version-sync.sh` en-tête (ce que le gate vérifie exactement)
</context>

<discipline>
- Valeurs de version TOUJOURS relues sur disque au moment de l'exécution, jamais recopiées de ce
  PLAN.md.
- `rtk` fausse les vérifications d'état (sortie vide → 1 ligne au lieu de 0) — toute mesure
  `git status`/`git diff` de ce plan passe par `rtk proxy`, jamais l'alias nu.
- Aucun fichier hors des neuf listés en `files_modified` n'est touché par ce plan.
- Racine (`VERSION`, `plugin.json`, `marketplace.json`, README, CHANGELOG racine) strictement hors
  périmètre — ne serait-ce que pour vérifier une cohérence, ne pas les ouvrir en écriture.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : bump cohérent des trois modules (conductor MINOR, les deux autres PATCH), CHANGELOG par module</name>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md, plugin/planning-core/VERSION, plugin/planning-core/module.json, plugin/planning-core/CHANGELOG.md, plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md</files>
  <behavior>
    - pour chaque module, `VERSION` et le champ `version` de `module.json` portent la MÊME valeur,
      bumpée selon le TYPE propre à son module (conductor : minor ; planning-core et
      dev-orchestrator : patch) par rapport à la valeur lue sur disque au début de cette tâche.
    - chaque `CHANGELOG.md` de module gagne une entrée `## [vX.Y.Z] — <date du jour>` qui nomme
      SES PROPRES fichiers livrés par cette phase, pas ceux des autres modules.
    - `scripts/check-version-sync.sh` rend 0 sur le dépôt après ces neuf éditions.
  </behavior>
  <action>
Relire les trois triades VERSION/module.json/CHANGELOG (tête) AVANT toute édition — la valeur
`vX.Y.Z` actuelle de chacune peut différer de celle citée dans ce PLAN.md si un plan antérieur de
cette phase a déjà bumpé (ce ne devrait PAS être le cas, ce plan est le seul de la phase à toucher
ces fichiers, mais la vérification est gratuite et évite un écart silencieux).

Pour CHAQUE module, avec le TYPE DE BUMP QUI LUI EST PROPRE (CORRECTION Q3) :

| Module | Bump | Motif (règle du `CLAUDE.md` racine) |
|---|---|---|
| `conductor` | **MINOR** | `41.1-05` y crée deux artefacts NEUFS et DISTRIBUÉS — le gate `check-planning-consumers-registered.sh` avec sa propre étape CI et sa suite, et `references/workstream-planning-consumers.md`. « Nouveau module / nouvelle capacité → minor ». Un gate neuf livré en patch est invisible à qui lit les versions de module. |
| `planning-core` | **PATCH** | durcissement de scripts existants uniquement. « Correctif / durcissement → patch ». |
| `dev-orchestrator` | **PATCH** | doctrine et durcissement uniquement. Idem. |

1. lire `VERSION` (une ligne, forme `vMAJOR.MINOR.PATCH`) ; pour `conductor` incrémenter `MINOR`
   de 1 **et remettre `PATCH` à 0** ; pour les deux autres, incrémenter `PATCH` de 1.
2. écrire la même valeur dans `VERSION` et dans le champ `"version"` de `module.json` (édition
   JSON par un outil qui préserve le reste du fichier à l'identique — `jq` si disponible, sinon une
   substitution ciblée sur la seule ligne `"version": "..."`, jamais une réécriture intégrale du
   fichier qui risquerait de reformater l'ordre des clés).
3. ajouter en tête du `CHANGELOG.md` du module (juste après la ligne `# Changelog — <module>`) une
   entrée `## [vX.Y.Z] — <date ISO du jour>`, sous-titre `**Minor** (gate neuf `check-planning-consumers-registered.sh` + recensement versionné, Phase 41.1)` pour `conductor`, `**Patch** (durcissement de gates workstream-aware, Phase 41.1)` pour les deux autres, puis la liste des fichiers de CE module livrés par cette phase
   (voir la répartition exacte donnée dans `must_haves.truths` ci-dessus), avec pour chacun une
   phrase courte de ce qu'il ferme (trou exit-0, faux ABSENT, recensement anti-oubli, etc. — une
   phrase, pas un paragraphe : le détail vit dans les SUMMARY des plans 41.1-01 à 07).

**ABSORPTION de la section « Non releasé » de `dev-orchestrator` (CORRECTION C-19).** Le plan
`41.1-07` a laissé dans `plugin/dev-orchestrator/CHANGELOG.md` une section `## Non releasé`
explicitement transitoire. FUSIONNER son contenu dans la nouvelle entrée `## [vX.Y.Z]` (les mêmes
changements ne sont décrits QU'UNE FOIS), puis RETIRER l'en-tête `## Non releasé` et la section
devenue vide. Ne jamais laisser les deux coexister : le fichier décrirait deux fois le même travail,
et un lecteur ne saurait pas lequel fait foi. Vérifier avant de conclure que les autres CHANGELOG
ne portent pas non plus une section « Non releasé » résiduelle.

Ordre des trois modules dans l'exécution : indifférent, aucune dépendance entre eux.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c '
for m in conductor planning-core dev-orchestrator; do
  v=$(awk "NR==1{print \$1}" "plugin/$m/VERSION")
  j=$(awk -F\" "/\"version\"/{print \$4; exit}" "plugin/$m/module.json")
  echo "$m VERSION=$v module.json=$j"
  [ "$v" = "$j" ] || exit 1
done
'</automated>
    <fails_when>pour un des trois modules, `VERSION` et `module.json` divergent — bump incohérent</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash scripts/check-version-sync.sh 2>&1) || rc=$?; echo "check-version-sync rc=$rc"; printf "%s\n" "$out"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>`check-version-sync rc=` différent de 0 — le gate CI existant doit valider les trois triades sans intervention manuelle</fails_when>
    <automated>bash --noprofile --norc -e -c '
BASE_REF=$(git merge-base HEAD main)
bad=""
for spec in conductor:minor planning-core:patch dev-orchestrator:patch; do
  mod=${spec%%:*}; kind=${spec##*:}
  v=$(awk "NR==1{print \$1}" "plugin/$mod/VERSION")
  old=$(git show "$BASE_REF:plugin/$mod/VERSION" | awk "NR==1{print \$1}")
  att=$(printf "%s\n" "$old" | awk -v K="$kind" "{gsub(/^v/,\"\"); split(\$0,a,\".\"); if(K==\"minor\") printf \"v%d.%d.0\n\", a[1], a[2]+1; else printf \"v%d.%d.%d\n\", a[1], a[2], a[3]+1}")
  cv=$(awk "match(\$0,/^## \\[v[0-9]+\\.[0-9]+\\.[0-9]+\\]/){print substr(\$0,RSTART+4,RLENGTH-5); exit}" "plugin/$mod/CHANGELOG.md")
  echo "$mod base=$old bump=$kind attendu=$att VERSION=$v changelog=$cv"
  [ "$v" = "$att" ] || bad="$bad [$mod:version]"
  [ "$cv" = "$att" ] || bad="$bad [$mod:changelog]"
done
nr=$(awk "/^## Non releas/{c++} END{print c+0}" plugin/dev-orchestrator/CHANGELOG.md)
echo "ecarts=[${bad# }] section_non_releasee=$nr"
[ -z "$bad" ] && [ "$nr" -eq 0 ]
'</automated>
    <fails_when>`ecarts=[…]` non vide — pour le module nommé, soit `VERSION` n'a pas reçu l'incrément ATTENDU POUR SON TYPE DE BUMP (`conductor` minor, les deux autres patch — Q3), soit la première entrée `## [vX.Y.Z]` du CHANGELOG ne porte pas cette même version ; ou `section_non_releasee=` différent de 0 — la section transitoire de `41.1-07` n'a pas été absorbée (C-19), le fichier décrit deux fois le même travail. CORRECTION C-11 (juge frais, 2e passe) : le bloc précédent était une boucle `for … echo … done` SANS AUCUNE ASSERTION, son `fails_when` renvoyant à une comparaison VISUELLE. Rejoué le 2026-09-23 sur l'état actuel — aucun bump, donc l'état qu'il est censé condamner : `conductor entries=69`, `planning-core entries=13`, `dev-orchestrator entries=54`, **rc du bloc = 0**. La sonde de remplacement NE COMPARE PAS à un littéral : elle relit `VERSION` au merge-base et CALCULE l'incrément attendu selon le type de bump, ce qui la rend insensible à une release intercalaire. BASCULE MESURÉE DANS LES DEUX SENS le 2026-09-23 : état actuel → `ecarts=[[conductor:version] [conductor:changelog] [planning-core:version] [planning-core:changelog] [dev-orchestrator:version] [dev-orchestrator:changelog]]`, rc=1 ; sur une copie jetable où les trois bumps ET les trois entrées de CHANGELOG sont correctement posés → `ecarts=[]`, rc=0. L'extraction tolère le format DIVERGENT de `dev-orchestrator` (`## [v2.23.1], 2026-09-23 :` au lieu de `## [vX.Y.Z] — <date>`), vérifié en la rejouant contre les trois fichiers RÉELS</fails_when>
  </verify>
  <acceptance_criteria>
    - trois modules bumpés selon leur type (conductor minor, les deux autres patch), `VERSION`/`module.json` cohérents
    - `check-version-sync.sh` rend 0
    - trois CHANGELOG portent chacun une entrée datée nommant leurs propres fichiers livrés
    - aucun fichier racine touché
  </acceptance_criteria>
  <done>Les trois modules réellement modifiés par la phase sont bumpés de façon cohérente et tracée.</done>
</task>

<task type="auto">
  <name>Task 2 : rejeu final complet — gates et tests sur l'arbre entier de la phase</name>
  <files></files>
  <precondition>Task 1 verte ; `rtk proxy git status --porcelain` ne liste que les chemins des plans 41.1-01 à 41.1-08 de cette phase.</precondition>
  <action>
Rejouer les DEUX jobs (`gates`, `tests`) avec la découverte COMPLÈTE, jamais une liste recopiée —
c'est la première fois que tous les plans de la phase (41.1-01 à 41.1-08) sont mesurés dans le
même arbre : le primitif `vf_ws_enumerate`, son adoption par `check-divergence.sh`, les deux hooks
`planning-core`, `discover-unintegrated-docs.sh`, le recensement + lint, le fan-out CI et la
fixture à 3 compartiments, la doctrine amendée, et les trois bumps — tous ensemble.

Un rouge n'est JAMAIS contourné : s'il semble étranger à la phase (par exemple un cas connu déjà
consigné ailleurs dans ce dépôt — `WINDOWS.md`, ou le cas T28-F de fraîcheur d'index GSD), le
vérifier sur une extraction de la base de `main` avant de conclure « préexistant », et le consigner
nommément au SUMMARY plutôt que de le neutraliser.

Consigner au SUMMARY : nombre d'étapes `gates` rejouées, bilan `tests` (nombre de suites, 0 échec
attendu), tout `SKIP` avec sa raison, et le périmètre volontairement non rejoué (jobs
`lab-frais`/`lab-frais-arme`, étape `check-release-tag` conditionnelle `main`, comme dans les plans
précédents de ce dépôt).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c '
# CORRECTION post plan-checker (FLAG-2) : le seuil n\x27est plus un nombre en dur — il est ancré
# sur une BASELINE MESURÉE (nombre de steps nommées du job gates au point de fork de cette
# branche, jamais recopié) PLUS le DELTA que cette phase ajoute (exactement UNE step nommée
# neuve, celle du lint anti-oubli créée par le plan 41.1-05 — les autres plans ÉDITENT des steps
# déjà nommées, ils n\x27en créent aucune neuve).
BASE_REF="$(git merge-base HEAD origin/main 2>/dev/null || git merge-base HEAD main 2>/dev/null)"
[ -n "$BASE_REF" ] || { echo "::error::aucune base résolue (ni origin/main ni main) — seuil non vérifiable"; exit 2; }
# CORRECTION C-04 (juge frais, 2e passe) — UNITÉS HOMOGÈNES, UN SEUL SEUIL.
# L\x27ancienne BASELINE comptait les STEPS NOMMÉES du ci.yml (17 au merge-base, mesuré), tandis
# que "n" est extrait de "BILAN REJEU gates : $5" = le nombre d\x27étapes REJOUÉES, qui EXCLUT les
# SAUTÉES. Mesure réelle du 2026-09-23 : "15 rejouee(s), 3 sautee(s)" — les 3 sautées étant
# l\x27action "uses:" du checkout (sans "- name:") et les DEUX étapes conditionnelles
# "if: ... refs/heads/main". Avec la step neuve de 41.1-05, n = 16, et l\x27ancien SEUIL valait
# 17+1 = 18 : "[ 16 -ge 18 ]" FAUX. Le dernier plan de la phase ne pouvait JAMAIS rendre vert,
# même si tout le travail était correct.
# La baseline est désormais mesurée DANS LA MÊME UNITÉ que "n" : étapes nommées MOINS étapes
# conditionnelles main-only. Relation VÉRIFIÉE le 2026-09-23 sur le merge-base : 17 - 2 = 15, et
# l\x27outil de rejeu mesure EXACTEMENT "15 rejouee(s)". Aucun nombre gelé : les deux termes sont
# relus du ci.yml de la base à chaque exécution.
# NB : "$T" n\x27existe pas encore a ce point du bloc (il est cree plus bas pour la trace du
# rejeu) — utiliser un temporaire PROPRE. Defaut trouve en REJOUANT ce bloc : ecrire dans
# "$T/mb.yml" donnait "/mb.yml: Read-only file system" puis un awk sur un fichier absent.
_MB="$(mktemp)"
git show "$BASE_REF:.github/workflows/ci.yml" > "$_MB" 2>/dev/null || true
NOMMEES=$(awk "/^  gates:/{f=1;next} f && /^  [a-z_-]+:\$/{exit} f && /^      - name:/{c++} END{print c+0}" "$_MB")
COND=$(awk "/^  gates:/{f=1;next} f && /^  [a-z_-]+:\$/{exit} f && /^        if:/ && /refs\/heads\/main/{c++} END{print c+0}" "$_MB")
BASELINE=$((NOMMEES - COND))
rm -f "$_MB"
# CORRECTION post plan-checker (WARNING-1, 2e passe) : si "git show" echoue pour une AUTRE raison
# que BASE_REF absent (ref corrompue, ci.yml absent a cette base, etc.), le pipe vers awk recoit un
# flux vide et rend BASELINE=0 -> SEUIL=1 -> seuil trivialement vert. Ferme ce dernier fail-open :
# une baseline mesuree DOIT etre au moins 1 (le job gates porte deja des etapes avant meme cette
# phase), sinon NON VERIFIABLE plutot qu un plancher degrade en silence.
[ "${NOMMEES:-0}" -ge 1 ] || { echo "::error::baseline : 0 etape nommee lue au merge-base — git show a echoue ou ci.yml etait vide a cette base, seuil non fiable, NON VERIFIABLE"; exit 2; }
[ "${BASELINE:-0}" -ge 1 ] || { echo "::error::baseline rejouees=${BASELINE:-0} (nommees=$NOMMEES, conditionnelles=$COND) — seuil non fiable, NON VERIFIABLE"; exit 2; }
DELTA=1
SEUIL=$((BASELINE + DELTA))
T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?
awk "/^BILAN REJEU gates/" "$T/g"
n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g")
echo "gates rc=$rc rejouees=${n:-0} nommees_base=$NOMMEES conditionnelles_base=$COND baseline_rejouees=$BASELINE delta_attendu=$DELTA seuil=$SEUIL"
[ "$rc" -eq 0 ] && [ "${n:-0}" -ge "$SEUIL" ]
'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à `baseline` (mesurée au point de fork de la branche) `+ 1` (la step neuve du plan 41.1-05) — jamais un plancher en dur qui passerait même si les plans 05/06 n'ajoutaient rien (FLAG-2 du juge frais)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 — toute nouvelle suite créée par les plans 41.1-01/03/04/05 doit être découverte et verte</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(rtk proxy git status --porcelain 2>&1) || rc=$?; n=$(printf "%s\n" "$out" | awk "NF>0" | awk "END{print NR+0}"); echo "fichiers_non_commites=$n"; printf "%s\n" "$out"'</automated>
    <fails_when>ce dernier bloc est informatif (pas d'assertion de rc) — le SUMMARY doit néanmoins consigner sa sortie brute pour trace</fails_when>
  </verify>
  <acceptance_criteria>
    - rejeu `gates` vert, découverte complète : le nombre d'étapes REJOUÉES atteint le SEUIL UNIQUE calculé par le bloc automatisé (`baseline_rejouees` + `delta_attendu`), jamais un second seuil écrit en prose (correction C-04)
    - rejeu `tests` vert, découverte complète, 0 échec
    - tout rouge rencontré est soit corrigé soit consigné nommément (jamais contourné)
    - SUMMARY consigne le périmètre non rejoué et tout `SKIP`
  </acceptance_criteria>
  <done>La phase entière est mesurée d'un seul tenant, verte, prête pour la revue et le ship (gestes humains hors périmètre de ce plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version déclarée → contenu réellement distribué | un bump sans rejeu complet laisserait un module publié sur une version dont le contenu n'a pas été mesuré ensemble avec les autres plans de la phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-15 | Tampering | bump incohérent entre `VERSION` et `module.json` d'un module | medium | mitigate | `check-version-sync.sh` (gate CI existant) rejoué explicitement en Task 1, pas seulement lors du rejeu global |
| T-41.1-16 | Repudiation | un rouge rencontré au rejeu final, contourné plutôt que consigné, ferait passer la phase pour vérifiée alors qu'elle ne l'est pas | high | mitigate | discipline explicite « jamais contourné » héritée du parc de plans de ce dépôt ; SUMMARY oblige la trace nommée de tout SKIP |
</threat_model>

<verification>
Trois triades VERSION/module.json cohérentes, `check-version-sync.sh` vert ; trois CHANGELOG
datés ; rejeu `gates` (nombre d'étapes rejouées ≥ le seuil UNIQUE calculé par le bloc automatisé) et `tests` (0 échec) verts sur l'arbre complet de la phase.
</verification>

<success_criteria>
Les trois modules réellement modifiés sont bumpés selon leur type (conductor minor, les deux
autres patch), tracés au CHANGELOG, et la phase
entière (41.1-01 à 41.1-08) est mesurée ensemble et verte pour la première fois — prête pour la
revue et le ship, gestes humains hors périmètre.
</success_criteria>

## Artifacts this phase produces

- `plugin/conductor/{VERSION,module.json,CHANGELOG.md}` — **MINOR** bumpé (gate neuf distribué, Q3).
- `plugin/planning-core/{VERSION,module.json,CHANGELOG.md}` — patch bumpé.
- `plugin/dev-orchestrator/{VERSION,module.json,CHANGELOG.md}` — patch bumpé.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-08-SUMMARY.md` when done
</output>
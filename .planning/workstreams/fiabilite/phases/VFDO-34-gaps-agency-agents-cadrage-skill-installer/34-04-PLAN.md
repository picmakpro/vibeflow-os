---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - plugin/mobile-test/VERSION
  - plugin/mobile-test/module.json
  - plugin/mobile-test/README.md
  - plugin/mobile-test/CHANGELOG.md
  - plugin/mobile-test-team/VERSION
  - plugin/mobile-test-team/module.json
  - plugin/mobile-test-team/README.md
  - plugin/mobile-test-team/CHANGELOG.md
  - manual/en/03-modules/catalog.md
  - manual/fr/03-modules/catalogue.md
  - manual/en/03-modules/choosing-your-modules.md
  - manual/fr/03-modules/choisir-ses-modules.md
autonomous: false
requirements: [AGTS-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CE PLAN EST CONDITIONNEL. Il n'a de contenu que si `34-RUN-MOBILE.md` (plan 34-01) porte `> **Statut** : VERT`. Sur ROUGE, le plan se clôt en no-op TRACÉ : aucun fichier de `plugin/` n'est touché, le SUMMARY écrit pourquoi, et l'exigence AGTS-02 est reportée avec la trace du run — jamais abandonnée en silence (D-05). Un no-op tracé est une exécution valide de ce plan, pas un échec."
    - "La sortie du statut expérimental est un geste SYMÉTRIQUE sur les DEUX modules : `mobile-test` (pipeline mécanique) et `mobile-test-team` (boucle d'équipe). Les deux README portent chacun leur propre condition de sortie, et le run de 34-01 rend deux verdicts distincts (PIPELINE, EQUIPE) précisément pour que la sortie ne puisse pas être déclarée sur un seul des deux. Un seul module sorti serait une déclaration de couverture supérieure à la couverture mesurée."
    - "La mention de statut n'est JAMAIS supprimée en silence : elle est REMPLACÉE par un enregistrement DATÉ de la sortie, renvoyant à `34-RUN-MOBILE.md` — README, `module.json` et `CHANGELOG.md` racontent ensemble d'où vient le module et ce qui a levé la réserve. C'est la manière dont ce dépôt trace déjà toutes ses transitions de statut."
    - "Le triplet PAR MODULE est bumpé au complet sur chaque module : `VERSION`, le champ `version` de `module.json`, et la ligne `**Version**` du README du module — les trois sont comparés par `scripts/check-version-sync.sh`, qui tourne dans le job `gates` de la CI et rend rouge de façon déterministe si l'un des trois diverge. Le CHANGELOG reçoit une entrée au format DÉJÀ EN PLACE DANS CE MODULE, qui n'est pas le même pour les deux (`## vX.Y.Z — date` pour `mobile-test`, `## [vX.Y.Z] — date` pour `mobile-test-team`)."
    - "Le bump est un PATCH sur les deux modules (`mobile-test` v1.0.2 → v1.0.3, `mobile-test-team` v1.4.5 → v1.4.6) : un changement de statut documentaire, pas une capacité neuve — précédent direct sur `mobile-test`, dont le passage v1.0.1 → v1.0.2 était déjà un patch pour un changement de doc de statut (`plugin/mobile-test/CHANGELOG.md`)."
    - "Le triplet RACINE (`VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`) n'est PAS touché par ce plan : le bump racine, le tag annoté et la release GitHub sont des gestes de ship, humains et gatés (CLAUDE.md racine, ADR-031). Seul le triplet PAR MODULE change de bord ici."
    - "La sortie de statut est propagée partout où le dépôt la DÉCLARE, pas seulement dans les deux modules : le manuel utilisateur (`manual/en/03-modules/catalog.md`, `manual/fr/03-modules/catalogue.md`, `manual/en/03-modules/choosing-your-modules.md`, `manual/fr/03-modules/choisir-ses-modules.md`) porte aujourd'hui une réserve de statut pour ces deux modules. La laisser en place après la sortie produirait un mensonge documenté — la même dérive que celle qui a fait mentir le tableau README sur 13 modules sans qu'aucun gate ne la voie. La propagation est VÉRIFIÉE par un balayage de `manual/` qui compte les lignes citant un module `mobile-test` et une réserve de statut, et exige zéro."
    - "L'item actif de `PROJECT.md:82` et les cases du ledger ne sont PAS cochés ici : ces gestes appartiennent à 34-06, qui consolide les trois verdicts de la phase. Ce plan ne touche que `plugin/mobile-test*` et les quatre fichiers de `manual/` qui déclarent la réserve de statut."
  artifacts:
    - "plugin/mobile-test/ : `VERSION` = v1.0.3, `module.json` avec `version` = v1.0.3 et une `description` dont la phrase de réserve de statut a disparu, `README.md` dont le bloc `## Limites` porte désormais un enregistrement daté de la sortie renvoyant à `34-RUN-MOBILE.md` et dont la ligne `**Version**` est alignée, `CHANGELOG.md` avec une entrée en tête au format du module."
    - "plugin/mobile-test-team/ : `VERSION` = v1.4.6, `module.json` avec `version` = v1.4.6 et une `description` dont la phrase de réserve de statut a disparu, `README.md` dont le bloc `## Limites` porte l'enregistrement daté de la sortie et dont la ligne `**Version**` est alignée, `CHANGELOG.md` avec une entrée en tête au format crocheté du module."
  key_links:
    - "`34-RUN-MOBILE.md` (chapeau de statut) <-> ce plan : le chapeau est lu comme précondition MACHINE du checkpoint. Un chapeau absent, ambigu ou dupliqué doit bloquer ce plan, pas le laisser deviner — c'est pourquoi 34-01 compte les lignes de chapeau à exactement 1."
    - "Triplet par module <-> `scripts/check-version-sync.sh` (job `gates` de `.github/workflows/ci.yml`) : bumper `VERSION` sans `module.json`, ou sans la ligne `**Version**` du README du module, rend la CI rouge de façon déterministe. Les deux modules déclarent une ligne `**Version**` dans leur README — ils sont donc tous deux dans le périmètre du §8 de ce gate."
    - "Sortie d'expérimental de `mobile-test-team` <-> plan 34-05 (`web-test-team`) : 34-05 lit l'état POSÉ PAR CE PLAN (la disparition de la phrase de réserve dans `plugin/mobile-test-team/module.json`) comme sa précondition — le moule n'est cloné que s'il a été prouvé, D-06."
  prohibitions:
    - "Ne JAMAIS toucher un fichier de `plugin/` si `34-RUN-MOBILE.md` ne porte pas `> **Statut** : VERT` — sur ROUGE, ce plan est un no-op tracé, point final (D-05, D-06)."
    - "Ne JAMAIS sortir un seul des deux modules du statut expérimental : les deux conditions de sortie sont distinctes et le run rend deux verdicts ; sortir l'un sans l'autre déclarerait une couverture supérieure à la couverture mesurée."
    - "Ne JAMAIS supprimer la mention de statut en silence — elle est remplacée par un enregistrement daté de la sortie, renvoyant à la trace du run."
    - "Ne JAMAIS bumper le triplet RACINE (`VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`) dans ce plan : le bump racine, le tag et la release sont des gestes humains de ship."
    - "Ne JAMAIS cocher une case de `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` ou `.planning/BACKLOG.md` depuis ce plan — ces gestes appartiennent à 34-06."
    - "Ne JAMAIS créer d'agent ni importer de persona dans ce plan — le seul livrable de code possible de cette phase est `web-test-team`, et il appartient au plan 34-05."
---

<objective>
Si — et seulement si — le run de 34-01 est VERT : faire sortir `mobile-test` ET `mobile-test-team`
du statut expérimental, sur les quatre fichiers de chaque module (`VERSION`, `module.json`,
`README.md`, `CHANGELOG.md`), en remplaçant la réserve de statut par un enregistrement daté qui
renvoie à la trace du run.

Purpose : les deux modules déclarent leur propre condition de sortie dans leur README depuis leur
création, et l'item `PROJECT.md:82` attend ce geste depuis autant de temps. Sans ce plan, un run
vert resterait une trace dans `.planning/` sans jamais changer ce que l'utilisateur lit à
l'installation — et le module continuerait à se vendre « à confirmer » alors qu'il a été confirmé.

Output : les deux modules à jour, `scripts/check-version-sync.sh` vert. Sur run ROUGE : un no-op
tracé, aucun fichier de `plugin/` touché.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md
@plugin/mobile-test/README.md
@plugin/mobile-test/CHANGELOG.md
@plugin/mobile-test-team/README.md
@plugin/mobile-test-team/CHANGELOG.md
@CLAUDE.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes a rendu les trois exigences
`unresolved`/`unclassified`. L'hypothèse portée par CE plan est déclarée ici, explicitement.

**AGTS-02 — hypothèse signalée (probe : `unresolved` / `unclassified`), volet « sortie de
statut ».** L'exigence ne dit pas ce qui se passe si le run est VERT sur un verdict et ROUGE sur
l'autre. Ce plan ASSUME : **un run VERT exige les DEUX verdicts VERT** (PIPELINE et EQUIPE), et
tout autre combinaison vaut ROUGE, donc no-op tracé. La raison : les deux modules ont des
conditions de sortie DISTINCTES et sortir l'un sans l'autre déclarerait une couverture supérieure à
la couverture mesurée — exactement le motif « armé sans preuve » que ce milestone ferme. Cette
arête n'est couverte par aucun `D-xx` ; si Samuel l'arbitre autrement, c'est ce plan qui est
amendé, pas la trace du run.
</flagged_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint : le run autorise-t-il la sortie du statut expérimental ?</name>
  <decision>Faut-il faire sortir `mobile-test` et `mobile-test-team` du statut expérimental maintenant, ou clore ce plan en no-op tracé ?</decision>
  <context>
    Lire `34-RUN-MOBILE.md` AVANT de répondre : son chapeau `> **Statut** :` et ses deux lignes
    `PIPELINE:` / `EQUIPE:`. La règle de composition est écrite dans la note elle-même — VERT si et
    seulement si les deux verdicts sont VERT. Ce point ne s'auto-décide pas : il transforme une
    trace interne en une promesse distribuée à tous les utilisateurs du plugin, et il conditionne
    aussi le plan 34-05 (`web-test-team`). Décider par préférence, ou par le temps déjà investi
    dans le run, est exactement le motif « armé sans preuve » que ce milestone existe pour fermer.
  </context>
  <options>
    <option id="poursuivre">
      <name>POURSUIVRE</name>
      <pros>`34-RUN-MOBILE.md` porte `> **Statut** : VERT`, `PIPELINE: VERT` et `EQUIPE: VERT`. Les trois tâches s'exécutent : les deux modules sortent du statut expérimental, leur triplet par module est bumpé, leur CHANGELOG reçoit une entrée datée renvoyant à la trace, et le manuel est réaligné. L'item actif de `PROJECT.md` pourra être soldé par 34-06.</pros>
      <cons>Engage une promesse distribuée : tout utilisateur lira que la réserve est levée. La limite qui subsiste (iOS prouvé, Android non couvert par ce run) doit donc être écrite partout, sous peine de déclarer une couverture supérieure à la mesure.</cons>
    </option>
    <option id="no-op-trace">
      <name>NO-OP TRACÉ</name>
      <pros>Honnête : le chapeau est ROUGE (y compris le cas d'un PIPELINE vert avec un EQUIPE rouge). Aucun fichier n'est touché, le SUMMARY écrit la cause et la date et renvoie au déclencheur de reprise rédigé par 34-01. AGTS-02 est reportée AVEC sa trace, jamais abandonnée en silence.</pros>
      <cons>L'item actif de `PROJECT.md` reste ouvert et `web-test-team` n'est pas construite (34-05 sera no-op par sa propre précondition) — la piste n°1 de l'item BACKLOG du 2026-07-20 attend un prochain run.</cons>
    </option>
  </options>
  <resume-signal>Répondre « POURSUIVRE » ou « NO-OP TRACÉ ».</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Tâche 1 : tranche verticale « un module sort » — mobile-test sur ses quatre fichiers, gate de version vert</name>
  <files>plugin/mobile-test/VERSION, plugin/mobile-test/module.json, plugin/mobile-test/README.md, plugin/mobile-test/CHANGELOG.md</files>
  <precondition>`34-RUN-MOBILE.md` porte `> **Statut** : VERT`, `PIPELINE: VERT` et `EQUIPE: VERT`, et le checkpoint ci-dessus a été tranché POURSUIVRE par un humain avec son canal et sa date. Si le chapeau est ROUGE, cette tâche n'est PAS exécutée : le plan est un no-op tracé.</precondition>
  <read_first>
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md` en entier — la trace qui autorise ce geste ; le README sortant devra la citer nommément
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « Sortie d'expérimental — `plugin/mobile-test/module.json` + `README.md` » — la description actuelle citée verbatim, la phrase exacte à retirer, et le gabarit d'entrée CHANGELOG
    - `plugin/mobile-test/module.json` — la `description` réelle sur disque avant édition (elle a pu bouger depuis l'écriture de ce plan)
    - `plugin/mobile-test/README.md` lignes 1-12 (ligne `**Version**`) et le bloc `## Limites` — les deux zones à éditer
    - `plugin/mobile-test/CHANGELOG.md` lignes 1-12 — le format d'entrée RÉEL de CE module (`## vX.Y.Z — date`, sans crochets), différent de celui de `mobile-test-team`
    - `scripts/check-version-sync.sh` §6 et §8 — ce que le gate compare réellement pour un module qui déclare une ligne `**Version**` dans son README
    - `CLAUDE.md` racine § « Discipline de release » — pourquoi le triplet RACINE reste hors de ce plan
  </read_first>
  <action>
    Faire sortir UN module de bout en bout, sur ses quatre fichiers, et prouver par le gate machine
    que la chaîne de version tient. Cinq gestes.

    (1) `plugin/mobile-test/VERSION` : `v1.0.2` → `v1.0.3` (patch — changement de statut
    documentaire ; précédent direct v1.0.1 → v1.0.2 sur ce même module pour un changement de doc de
    statut).

    (2) `plugin/mobile-test/module.json` : porter `"version"` à `v1.0.3`, et retirer de
    `"description"` la dernière phrase de réserve — celle citée verbatim dans `34-PATTERNS.md`
    § « Sortie d'expérimental », entre la phrase sur le script mécanique et celle sur la config.
    Ne rien réécrire d'autre dans la description : le reste du texte est inchangé.

    (3) `plugin/mobile-test/README.md` : porter la ligne `**Version**` à `v1.0.3` ; dans le bloc
    `## Limites`, remplacer le premier item (celui qui porte l'avertissement de réserve) par une
    ligne factuelle commençant par `Statut expérimental levé le ` suivie de la date, de la
    plateforme et du lab (iOS, `Scroll-Off/frontend`), et d'un renvoi nommé à
    `34-RUN-MOBILE.md`. Les autres items du bloc `## Limites` (build de dev uniquement, Android
    manuel, flows à la charge de l'utilisateur) restent inchangés — ce sont des limites réelles,
    pas des réserves de statut. Ajouter, dans ce même item, la limite honnête que le run de sortie
    a porté sur **iOS seulement** : Android reste non prouvé par ce run.

    (4) `plugin/mobile-test/CHANGELOG.md` : nouvelle entrée en TÊTE, au format RÉEL de ce module
    (`## v1.0.3 — <date> (Phase 34 — sortie du statut expérimental)`, puis une rubrique
    `### Modifié`), disant ce qui change et POURQUOI (le run réel vert tracé, sa date, son lab, sa
    plateforme), avec le renvoi à `34-RUN-MOBILE.md`. Ne pas inventer un format à crochets : ce
    module n'en utilise pas.

    (5) **Fermer la tranche par le gate.** Exécuter `bash scripts/check-version-sync.sh` : il doit
    passer. C'est la preuve bout-en-bout que les trois points de version du module concordent —
    sans quoi la CI rougirait de façon déterministe au job `gates`.
  </action>
  <behavior>
    - Les trois points de version du module (`VERSION`, `module.json`, ligne `**Version**` du
      README) portent la même valeur `v1.0.3`.
    - La phrase de réserve de statut a disparu de `module.json` ET le README porte à la place un
      enregistrement daté de la sortie renvoyant à la trace.
    - Le CHANGELOG porte une entrée en tête au format déjà utilisé par CE module.
    - `scripts/check-version-sync.sh` passe.
  </behavior>
  <acceptance_criteria>
    - `plugin/mobile-test/VERSION` contient exactement `v1.0.3`.
    - `plugin/mobile-test/module.json` contient `"version": "v1.0.3"`.
    - `plugin/mobile-test/README.md` contient `**Version** : v1.0.3`.
    - Le compte d'occurrences de la chaîne `Statut expérimental jusqu` dans `plugin/mobile-test/module.json` est `0`.
    - Le compte d'occurrences de la chaîne `⚠️ **Statut expérimental.**` dans `plugin/mobile-test/README.md` est `0`.
    - `plugin/mobile-test/README.md` contient la chaîne `Statut expérimental levé le ` et la chaîne `34-RUN-MOBILE.md`.
    - `plugin/mobile-test/CHANGELOG.md` contient une ligne commençant par `## v1.0.3 — `.
    - `bash scripts/check-version-sync.sh` rend 0.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(cat plugin/mobile-test/VERSION)" = "v1.0.3" ] && grep -qF '"version": "v1.0.3"' plugin/mobile-test/module.json && grep -qF '**Version** : v1.0.3' plugin/mobile-test/README.md && [ "$(grep -cF 'Statut expérimental jusqu' plugin/mobile-test/module.json)" -eq 0 ] && [ "$(grep -cF '⚠️ **Statut expérimental.**' plugin/mobile-test/README.md)" -eq 0 ] && grep -qF 'Statut expérimental levé le ' plugin/mobile-test/README.md && grep -qF '34-RUN-MOBILE.md' plugin/mobile-test/README.md && grep -q '^## v1\.0\.3 — ' plugin/mobile-test/CHANGELOG.md && bash scripts/check-version-sync.sh</automated>
    <fails_when>sortie non nulle. Causes discriminantes : l'un des trois points de version ne porte pas `v1.0.3` (le gate `check-version-sync.sh` le dirait aussi, mais les trois tests ciblés disent LEQUEL) ; la phrase de réserve subsiste dans `module.json` (compte différent de 0) ; l'avertissement de réserve subsiste dans le README (compte différent de 0) ; le README ne porte pas l'enregistrement daté de la sortie, ou ne renvoie pas nommément à la trace du run — c'est-à-dire que la mention aurait été supprimée en silence ; aucune entrée `## v1.0.3 — ` en tête du CHANGELOG ; `scripts/check-version-sync.sh` rend non-zéro, ce qui rendrait le job `gates` de la CI rouge de façon déterministe.</fails_when>
  </verify>
  <done>`mobile-test` est sorti du statut expérimental sur ses quatre fichiers, la sortie est datée et traçable, et le gate de cohérence de version passe.</done>
  <reversibility rating="reversible">La sortie de statut est un contenu documentaire distribué : elle se ré-amende par un nouveau patch si une mesure ultérieure la contredit. Aucun contrat d'API, aucun format de données n'est figé ici.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : mobile-test-team sort à son tour, au format crocheté de son propre CHANGELOG</name>
  <files>plugin/mobile-test-team/VERSION, plugin/mobile-test-team/module.json, plugin/mobile-test-team/README.md, plugin/mobile-test-team/CHANGELOG.md</files>
  <precondition>La tâche 1 est verte : `mobile-test` est sorti et `scripts/check-version-sync.sh` passe.</precondition>
  <read_first>
    - `plugin/mobile-test-team/module.json` — la `description` réelle sur disque avant édition
    - `plugin/mobile-test-team/README.md` lignes 1-12 (ligne `**Version**`) et le bloc `## Limites` (autour des lignes 120-133) — les deux zones à éditer ; noter que les autres limites (allowlist n'est pas une barrière dure, mobile uniquement, redémarrage requis) sont RÉELLES et restent
    - `plugin/mobile-test-team/CHANGELOG.md` lignes 1-12 — le format d'entrée RÉEL de CE module, à crochets (`## [vX.Y.Z] — date (contexte)`), différent de celui de `mobile-test` : ne pas transposer le format de la tâche 1
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md` § `## Dispatch` et § `## Cycles` — la preuve spécifique qui lève la réserve de CE module (un sous-agent qui en dispatche d'autres ET invoque le pipeline), à citer dans l'entrée CHANGELOG
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « Sortie d'expérimental — `plugin/mobile-test-team/module.json` + `README.md` »
  </read_first>
  <action>
    Miroir exact du geste de la tâche 1, sur le second module, avec DEUX différences à ne pas
    manquer : le format du CHANGELOG et la nature de la preuve qui lève la réserve.

    (1) `plugin/mobile-test-team/VERSION` : `v1.4.5` → `v1.4.6` (patch).

    (2) `plugin/mobile-test-team/module.json` : porter `"version"` à `v1.4.6` et retirer de
    `"description"` la dernière phrase de réserve de statut (même phrase que sur l'autre module,
    citée verbatim dans `34-PATTERNS.md`). Le reste de la description est inchangé.

    (3) `plugin/mobile-test-team/README.md` : porter la ligne `**Version**` à `v1.4.6` ; dans le
    bloc `## Limites`, remplacer le premier item (celui qui porte l'avertissement de réserve) par
    une ligne factuelle commençant par `Statut expérimental levé le ` suivie de la date, et
    renvoyant nommément à `34-RUN-MOBILE.md`. Citer ce qui a été RÉELLEMENT prouvé pour CE module —
    un sous-agent qui en dispatche d'autres et invoque le pipeline, avec au moins un cycle de fix et
    un arrêt propre — et ce qui ne l'a pas été (iOS seulement ; Android non couvert par ce run).
    Les autres items du bloc restent tels quels : ce sont des limites, pas des réserves de statut.

    (4) `plugin/mobile-test-team/CHANGELOG.md` : nouvelle entrée en TÊTE au format RÉEL de ce
    module, avec crochets : `## [v1.4.6] — <date> (Phase 34 — sortie du statut expérimental)`,
    suivie d'une rubrique `**Patch** :` comme les entrées existantes. Dire ce qui change et sur
    quelle preuve, avec le renvoi à `34-RUN-MOBILE.md`.

    (5) Re-jouer `bash scripts/check-version-sync.sh` et, puisque ce module porte des agents,
    re-jouer aussi les trois étapes `check-agents.sh` du job `gates` telles qu'elles sont écrites
    dans `.github/workflows/ci.yml` — aucun frontmatter d'agent n'est touché par ce plan, mais
    c'est la manière de ce dépôt de ne jamais rendre un vert sur une liste recopiée : on rejoue les
    commandes du job, pas une liste de gates lue dans un rapport.
  </action>
  <behavior>
    - Les trois points de version du module portent `v1.4.6`.
    - La phrase de réserve a disparu de `module.json` ; le README porte l'enregistrement daté avec
      renvoi à la trace, et nomme ce qui reste non prouvé (Android).
    - L'entrée CHANGELOG est au format crocheté de ce module, pas au format de l'autre.
    - `scripts/check-version-sync.sh` passe, et `check-agents.sh --strict` passe sur les agents du
      module.
  </behavior>
  <acceptance_criteria>
    - `plugin/mobile-test-team/VERSION` contient exactement `v1.4.6`.
    - `plugin/mobile-test-team/module.json` contient `"version": "v1.4.6"`.
    - `plugin/mobile-test-team/README.md` contient `**Version** : v1.4.6`.
    - Le compte d'occurrences de la chaîne `Statut expérimental jusqu` dans `plugin/mobile-test-team/module.json` est `0`.
    - Le compte d'occurrences de la chaîne `⚠️ **Statut expérimental.**` dans `plugin/mobile-test-team/README.md` est `0`.
    - `plugin/mobile-test-team/README.md` contient les chaînes `Statut expérimental levé le ` et `34-RUN-MOBILE.md`, et mentionne `Android` comme non couvert par ce run.
    - `plugin/mobile-test-team/CHANGELOG.md` contient une ligne commençant par `## [v1.4.6] — `.
    - `bash scripts/check-version-sync.sh` rend 0 et `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/mobile-test-team/agents` rend 0.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(cat plugin/mobile-test-team/VERSION)" = "v1.4.6" ] && grep -qF '"version": "v1.4.6"' plugin/mobile-test-team/module.json && grep -qF '**Version** : v1.4.6' plugin/mobile-test-team/README.md && [ "$(grep -cF 'Statut expérimental jusqu' plugin/mobile-test-team/module.json)" -eq 0 ] && [ "$(grep -cF '⚠️ **Statut expérimental.**' plugin/mobile-test-team/README.md)" -eq 0 ] && grep -qF 'Statut expérimental levé le ' plugin/mobile-test-team/README.md && grep -qF '34-RUN-MOBILE.md' plugin/mobile-test-team/README.md && grep -qF 'Android' plugin/mobile-test-team/README.md && grep -q '^## \[v1\.4\.6\] — ' plugin/mobile-test-team/CHANGELOG.md && bash scripts/check-version-sync.sh && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/mobile-test-team/agents && P=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md && B=$(sed -n 's/^SHA de base : //p' "$P" | head -1) && test -n "$B" && git diff --quiet "$B" -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json</automated>
    <fails_when>sortie non nulle. Causes discriminantes : l'un des trois points de version ne porte pas `v1.4.6` ; la phrase de réserve subsiste dans `module.json` ou l'avertissement dans le README (comptes différents de 0) ; le README ne porte pas l'enregistrement daté, le renvoi nommé à la trace, ou la mention qu'Android n'est pas couvert (la sortie déclarerait alors une couverture des deux plateformes que le run n'a pas mesurée) ; l'entrée CHANGELOG n'est pas au format crocheté de CE module (format transposé depuis l'autre module) ; `check-version-sync.sh` ou `check-agents.sh --strict` rend non-zéro ; le diff du triplet RACINE contre le SHA de base de la phase (celui consigné dans `34-RUN-MOBILE.md` § `## Périmètre` — plage explicite, jamais `HEAD` nu, qui serait vide dès le premier commit) rend 1, c'est-à-dire que ce plan a bumpé la racine — geste de ship réservé à un humain.</fails_when>
    <human-check>Relire les deux blocs `## Limites` : la sortie de statut y est-elle DATÉE et renvoie-t-elle nommément à la trace, plutôt que d'avoir simplement disparu ? Un utilisateur qui installe le module comprend-il ce qui a été prouvé (iOS, un lab, un run) et ce qui ne l'a pas été (Android) ?</human-check>
  </verify>
  <done>Les deux modules sont sortis du statut expérimental de façon symétrique et datée, les gates de version et d'agents passent, et le triplet racine est intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : propager la sortie au manuel utilisateur — aucune réserve résiduelle dans manual/</name>
  <files>manual/en/03-modules/catalog.md, manual/fr/03-modules/catalogue.md, manual/en/03-modules/choosing-your-modules.md, manual/fr/03-modules/choisir-ses-modules.md</files>
  <precondition>Les tâches 1 et 2 sont vertes : les deux modules sont sortis et `scripts/check-version-sync.sh` passe.</precondition>
  <read_first>
    - `manual/en/03-modules/catalog.md` autour des lignes 73 et 82-86 — la phrase d'introduction qui annonce « deux d'entre eux portent une réserve de statut » ET les deux entrées de module concernées : les TROIS zones changent, pas seulement les deux entrées
    - `manual/fr/03-modules/catalogue.md` autour des lignes 85-89 — les mêmes entrées côté français, plus la phrase d'introduction équivalente si elle existe
    - `manual/en/03-modules/choosing-your-modules.md` autour de la ligne 47 — la mise en garde « ils existent et marchent, mais… » adressée à l'utilisateur qui choisit ses modules
    - `manual/fr/03-modules/choisir-ses-modules.md` — l'équivalent français de cette mise en garde ; le localiser par lecture du fichier, pas par un numéro de ligne supposé
    - `plugin/mobile-test/README.md` et `plugin/mobile-test-team/README.md` dans leur état de fin de tâche 2 — le texte de sortie posé là fait autorité, le manuel doit s'y aligner et non le reformuler autrement (ADR-030, une seule voix)
  </read_first>
  <action>
    Propager la sortie partout où le dépôt la DÉCLARE à l'utilisateur. Trois gestes.

    (1) **Les deux catalogues** (`manual/en/03-modules/catalog.md`,
    `manual/fr/03-modules/catalogue.md`) : sur les entrées des deux modules `mobile-test`, retirer
    le marqueur de réserve en italique entre parenthèses qui suit le nom du module, et retirer la
    phrase d'explication qui suit (« ce marqueur signifie que le module attend son premier run
    vert »). Remplacer par une mention datée de la sortie, alignée sur le texte posé dans les README
    des modules en tâches 1 et 2, en gardant la limite réelle qui subsiste (iOS prouvé, Android non
    couvert par ce run). Corriger aussi la phrase d'introduction de la section qui annonce combien
    de modules portent une réserve — elle devient fausse dès que les deux sortent.

    (2) **Les deux pages « choisir ses modules »** : réécrire la mise en garde adressée à
    l'utilisateur pour qu'elle dise l'état réel — le module est sorti de réserve sur iOS, prouvé par
    un run tracé, et Android reste non couvert.

    (3) **Balayer `manual/` en entier** pour s'assurer qu'aucune autre page ne porte une réserve de
    statut sur ces deux modules. Le balayage doit LIRE LES FICHIERS lui-même plutôt que de passer
    par un `grep` en pipe : le `grep` de ce poste tronque silencieusement (mesuré : une ligne rendue
    sur 91), et un balayage tronqué rendrait un vert sur un sous-ensemble — c'est la raison pour
    laquelle les gates de ce dépôt scannent en `awk` ou en `node`. La vérification de cette tâche
    fait exactement ce balayage et échoue en nommant chaque fichier et chaque ligne résiduelle.

    Note de portée : ce balayage est SCOPÉ à `manual/` et ne peut donc pas être invalidé par le
    texte de ce plan, qui vit sous `.planning/`.
  </action>
  <behavior>
    - Aucune ligne de `manual/**` ne cite à la fois un module `mobile-test` et une réserve de statut.
    - Les quatre fichiers mentionnent toujours les modules — les entrées sont réécrites, pas
      supprimées.
    - Le manuel dit la même chose que les README des modules : iOS prouvé, Android non couvert.
  </behavior>
  <acceptance_criteria>
    - Un balayage de tous les fichiers `.md` sous `manual/` rend ZÉRO ligne contenant à la fois la chaîne `mobile-test` et un marqueur de réserve de statut (anglais ou français).
    - Chacun des quatre fichiers édités contient encore au moins une occurrence de `mobile-test` — les entrées ont été réécrites, jamais supprimées.
    - `manual/en/03-modules/catalog.md` et `manual/fr/03-modules/catalogue.md` mentionnent chacun `Android` dans l'entrée réécrite (la limite qui subsiste est dite, pas tue).
    - `bash scripts/check-version-sync.sh` rend toujours 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && node -e 'const fs=require("fs"),p=require("path");const bad=[];const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=p.join(d,e.name);if(e.isDirectory())walk(f);else if(e.name.endsWith(".md")){fs.readFileSync(f,"utf8").split("\n").forEach((l,i)=>{if(/mobile-test/.test(l)&&/(experimental|expérimental)/i.test(l))bad.push(f+":"+(i+1));});}}};walk("manual");if(bad.length){console.log("RESERVE RESIDUELLE: "+bad.join(" "));process.exit(1);}const req=["manual/en/03-modules/catalog.md","manual/fr/03-modules/catalogue.md","manual/en/03-modules/choosing-your-modules.md","manual/fr/03-modules/choisir-ses-modules.md"];const vides=req.filter(f=>!fs.readFileSync(f,"utf8").includes("mobile-test"));if(vides.length){console.log("ENTREE SUPPRIMEE AU LIEU D ETRE REECRITE: "+vides.join(","));process.exit(1);}const sansAndroid=["manual/en/03-modules/catalog.md","manual/fr/03-modules/catalogue.md"].filter(f=>!/Android/.test(fs.readFileSync(f,"utf8")));if(sansAndroid.length){console.log("LIMITE ANDROID TUE: "+sansAndroid.join(","));process.exit(1);}'</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `RESERVE RESIDUELLE: <fichier:ligne …>`, `ENTREE SUPPRIMEE AU LIEU D ETRE REECRITE: <fichiers>`, `LIMITE ANDROID TUE: <fichiers>` apparaît. Causes discriminantes : une page de `manual/` cite encore un module `mobile-test` avec une réserve de statut (le balayage nomme le fichier ET la ligne, donc la correction est immédiate) ; une entrée de module a été supprimée au lieu d'être réécrite (le module deviendrait invisible du catalogue au lieu d'être documenté comme sorti) ; les catalogues ne mentionnent plus Android, c'est-à-dire que la limite réelle qui subsiste après ce run serait tue ; `scripts/check-version-sync.sh` rend non-zéro.</fails_when>
    <human-check>Lire les deux entrées de catalogue et les deux pages « choisir ses modules » comme un utilisateur qui découvre VibeFlow : comprend-il ce que le module garantit maintenant, sur quelle plateforme, et ce qu'il ne garantit toujours pas ?</human-check>
  </verify>
  <done>Aucune page du manuel ne porte plus de réserve de statut sur ces deux modules, et ce que le manuel promet correspond exactement à ce que les README des modules déclarent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trace interne `.planning/` → contenu distribué `plugin/` | ce plan est le seul point de la phase où une mesure interne devient une PROMESSE lue par tous les utilisateurs à l'installation ; le franchissement est gouverné par le checkpoint bloquant-humain |
| Triplet PAR MODULE → triplet RACINE (bump, tag, release) | frontière de responsabilité : ce plan touche le premier, jamais le second — le second est un geste humain gaté (ADR-031, CLAUDE.md racine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-13 | Spoofing | `module.json` / `README.md` distribués déclarant une maturité non mesurée | high | mitigate | le checkpoint `blocking-human` en tête de plan interdit tout geste sans lecture du chapeau de la trace ; la précondition de chaque tâche exige `> **Statut** : VERT` ET les deux verdicts VERT ; le README doit nommer explicitement ce qui n'a PAS été prouvé (Android), vérifié par machine — une sortie qui tairait cette limite échoue |
| T-34-14 | Tampering | chaîne de version du dépôt (triplet par module et triplet racine) | medium | mitigate | `scripts/check-version-sync.sh` est rejoué dans la vérification de CHAQUE tâche, pas seulement en fin de plan ; un diff du triplet RACINE contre le SHA de base de la phase (plage explicite, jamais `HEAD` nu) prouve qu'il n'a pas bougé — le bump racine, le tag et la release restent des gestes humains |
| T-34-15 | Repudiation | disparition silencieuse de la trace de statut antérieur | medium | mitigate | la suppression nue est interdite : la vérification exige à la fois l'absence de l'ancienne réserve ET la présence d'un enregistrement daté renvoyant nommément à `34-RUN-MOBILE.md` ; une entrée CHANGELOG au format propre à chaque module conserve l'historique |
| T-34-16 | Spoofing | manuel utilisateur (`manual/**`) en désaccord avec les README de modules | medium | mitigate | la tâche 3 balaie `manual/` en LISANT les fichiers (jamais un `grep` en pipe, qui tronque silencieusement sur ce poste) et échoue en nommant fichier et ligne ; elle exige aussi que les entrées soient RÉÉCRITES et non supprimées, et que la limite Android subsistante soit dite — un manuel qui promettrait les deux plateformes après un run iOS seul serait une déclaration de couverture supérieure à la mesure |

Aucun install de paquet (`npm`/`pip`/`cargo`) n'entre dans ce plan : édition de doc, de JSON et de
fichiers `VERSION` uniquement. Aucune menace `T-34-SC` n'est ouverte, et aucun checkpoint de
légitimité de paquet n'est requis.
</threat_model>

<verification>
1. Si `34-RUN-MOBILE.md` porte `> **Statut** : ROUGE` : `git diff --quiet <SHA de base de la phase> -- plugin manual` rend 0 (plage explicite prise dans `34-RUN-MOBILE.md` § `## Périmètre`, jamais `HEAD` nu) — aucun fichier touché, no-op tracé, et le SUMMARY écrit la cause et la date.
2. Si VERT : `plugin/mobile-test/VERSION`, `module.json` et la ligne `**Version**` du README portent tous `v1.0.3` ; idem `v1.4.6` pour `mobile-test-team`.
3. La chaîne `Statut expérimental jusqu` est absente des deux `module.json` ; l'avertissement de réserve est absent des deux README ; les deux README portent `Statut expérimental levé le ` et renvoient nommément à `34-RUN-MOBILE.md`.
4. `plugin/mobile-test/CHANGELOG.md` porte une entrée `## v1.0.3 — ` ; `plugin/mobile-test-team/CHANGELOG.md` porte une entrée `## [v1.4.6] — ` — chacun au format réel de son module.
5. `bash scripts/check-version-sync.sh` rend 0.
6. Les trois étapes `check-agents.sh` du job `gates` de `.github/workflows/ci.yml` sont rejouées telles qu'elles y sont écrites (pas une liste recopiée) et rendent 0.
7. `git diff --quiet <SHA de base de la phase> -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json` rend 0 — le triplet racine est intact, prouvé sur une plage explicite (le SHA vient de `34-RUN-MOBILE.md` § `## Périmètre`).
8. Un balayage de tous les `.md` sous `manual/` (lecture directe des fichiers, jamais un `grep` en pipe) rend ZÉRO ligne citant à la fois un module `mobile-test` et une réserve de statut, et les quatre pages éditées mentionnent toujours ces modules.
9. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-04-SUMMARY.md` existe et consigne la réponse du checkpoint avec son canal et sa date.
</verification>

<success_criteria>
- Sur run vert, ce que l'utilisateur LIT à l'installation correspond à ce qui a été MESURÉ : plus de réserve de statut, une sortie datée, et les limites réelles qui restent (Android non couvert) nommées.
- Sur run rouge, absolument rien de `plugin/` n'a bougé, et la raison est écrite avec sa date.
- Les deux modules sortent ensemble ou pas du tout — aucune déclaration de couverture supérieure à la couverture mesurée.
- La chaîne de version tient : `check-version-sync.sh` vert, triplet racine intact.
- La trace du statut antérieur survit dans les CHANGELOG — aucune suppression silencieuse.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| `plugin/mobile-test/VERSION` → `v1.0.3` | édition — module existant | suivi par git |
| `plugin/mobile-test/module.json` (version + description sans réserve) | édition | suivi par git |
| `plugin/mobile-test/README.md` (ligne `**Version**` + bloc `## Limites`) | édition | suivi par git |
| `plugin/mobile-test/CHANGELOG.md` (entrée `## v1.0.3 — …`) | édition | suivi par git |
| `plugin/mobile-test-team/VERSION` → `v1.4.6` | édition | suivi par git |
| `plugin/mobile-test-team/module.json` (version + description sans réserve) | édition | suivi par git |
| `plugin/mobile-test-team/README.md` (ligne `**Version**` + bloc `## Limites`) | édition | suivi par git |
| `plugin/mobile-test-team/CHANGELOG.md` (entrée `## [v1.4.6] — …`) | édition | suivi par git |
| `manual/en/03-modules/catalog.md` (entrées réécrites, réserve levée) | édition | suivi par git |
| `manual/fr/03-modules/catalogue.md` (entrées réécrites, réserve levée) | édition | suivi par git |
| `manual/en/03-modules/choosing-your-modules.md` (mise en garde réécrite) | édition | suivi par git |
| `manual/fr/03-modules/choisir-ses-modules.md` (mise en garde réécrite) | édition | suivi par git |
| `34-04-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-04-SUMMARY.md` |

Aucun agent, aucun module neuf, aucun symbole de code n'est produit par ce plan.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-04-SUMMARY.md` en fin
de plan, consignant : la réponse du checkpoint avec son canal et sa date, le chapeau de
`34-RUN-MOBILE.md` tel que lu, les deux bumps de version, le texte exact des deux enregistrements
de sortie posés dans les README, la sortie de `check-version-sync.sh` et des trois étapes
`check-agents.sh` rejouées, et — si no-op — la cause, la date et le renvoi au déclencheur de
reprise rédigé par 34-01.
</output>

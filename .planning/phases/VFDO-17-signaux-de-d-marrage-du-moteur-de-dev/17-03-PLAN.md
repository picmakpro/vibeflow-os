---
phase: 17-signaux-de-demarrage-du-moteur-de-dev
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - plugin/dev-orchestrator/AGENT.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/dev-orchestrator/README.md
autonomous: true
requirements: [SIG-04, SIG-05, SIG-06]

must_haves:
  truths:
    - "Gate ADR-044 falsifiable (SIG-05/concurrency, D-12) : `bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md` est en lecture seule et idempotent — deux invocations successives ou concurrentes rendent exit 0 et le même compte de 3 warnings, sans écrire aucun fichier."
    - "Le gate n'est plus un faux vert : un cas T20 embarqué dans `test-dev-orchestrator.sh` invoque `check-agents.sh` AVEC `--file` (jamais à nu) et assert exit 0 ET compte de warnings égal à la baseline de 3, ET la présence des 3 types de warning connus — un 4e type ou une disparition fait échouer le cas."
    - "Le trou est fermé structurellement, pas documenté : le cas T20 est ramassé par la découverte générique de `.github/workflows/ci.yml:32` (`find plugin scripts -type f -path '*/tests/test-*.sh'`), donc la vérification ADR-044 de cet `AGENT.md` racine de module devient CI-enforced SANS aucune édition de `ci.yml`."
    - "Invariants SC5 vérifiés par grep structurel, pas par relecture (SIG-04, D-15) : un cas T21 prouve sur les DEUX nouveaux scripts qu'aucune ligne non commentée ne sort en code 1, que toute redirection d'écriture cible `/dev/null`, le descripteur 2, ou une variable de fichier temporaire, et que chaque `mktemp` est apparié à un `trap ... EXIT`."
    - "SC1 amendé (D-00) tenu de bout en bout sur ce dépôt : les trois scripts invoqués avec `--hook --path .` sortent tous en 3, et l'union de leurs stdout fait EXACTEMENT les 2 lignes du signal `[gsd-engine]` — aucune autre ligne n'est injectée dans le contexte d'une session sur un projet sain."
    - "Portabilité prouvée, pas supposée (SIG-06, D-13) : les suites de la phase passent dans un conteneur Linux exécuté localement AVANT push, jamais sur la seule foi d'un run macOS. Le nom exact de l'image et la sortie des suites sont consignés dans le SUMMARY."
    - "Densité ADR-029 tenue : `plugin/dev-orchestrator/AGENT.md` reste sous le plafond de 250 lignes après insertion de la section, et le cas T5 existant de `test-dev-orchestrator.sh` passe inchangé."
    - "Cohérence de version du module : `VERSION`, `module.json` et l'en-tête `**Version**` du `README.md` annoncent tous les trois `v2.5.0`, et `CHANGELOG.md` porte une entrée `v2.5.0` datée."
    - "Aucun débordement de périmètre : la `VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, les deux README racine, `.github/workflows/ci.yml` et `plugin/_internal/vibeflow-update.sh` restent INTOUCHÉS — `git status --short` ne les montre pas."
  artifacts:
    - plugin/dev-orchestrator/AGENT.md
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
    - plugin/dev-orchestrator/CHANGELOG.md
  key_links:
    - "`AGENT.md` → `check-agents.sh --file` : le gate ne voit CET agent que par le chemin `--file`, parce que `AGENT.md` est à la racine du module et non sous `plugin/dev-orchestrator/agents/` — hors de la boucle `plugin/*/agents` de `ci.yml:76`. Une invocation à nu sort 0 trivialement (aucun `.claude/agents` dans ce dépôt) : c'est la définition du faux vert que ce plan supprime."
    - "`test-dev-orchestrator.sh` → `ci.yml:32` : le seul câblage qui rend T20 et T21 exécutés en CI. Il est déjà générique — le lien tient par le nom du fichier de suite, pas par une entrée déclarée."
    - "conteneur Linux → suites de la phase : seule preuve valable de SIG-06 avant push. Un run macOS seul ne prouve rien (régression CI réelle du 2026-07-27, 6 fixes de portabilité)."
    - "`VERSION` / `module.json` / `README.md` en-tête : trois emplacements du même numéro ; un seul oublié et le contrôle de cohérence de version du dépôt tombe."
  prohibitions:
    - "P-04 (transparency) — MUST NOT cocher SIG-06 ni le critère 7 de la spec sur la foi d'un run macOS. Une preuve de portabilité non exécutée sous Linux est une affirmation fausse dans le SUMMARY, pas une approximation acceptable : consigner l'image réellement utilisée et la sortie réellement obtenue, ou déclarer l'échec."
    - "P-05 (safety) — MUST NOT étendre le périmètre à la release racine. Le bump de la `VERSION` racine, le tag annoté et la release GitHub sont explicitement réservés à validation humaine (CONTEXT `<deferred>`) ; les proposer en fin de mission est correct, les exécuter ne l'est pas (ADR-031)."
---

<objective>
Fermer la phase : poser la doctrine agent qui rend les signaux actionnables quand `vibeflow-dev` est
aux commandes (D-11), transformer le gate ADR-044 de faux vert en vérification réellement
falsifiable et CI-enforced (D-12), prouver les invariants de lecture seule par grep structurel
(D-15), prouver la portabilité Linux en conteneur avant push (D-13), et publier le module en
`v2.5.0` (D-16).

Purpose: les plans 17-01 et 17-02 livrent la machine. Ce plan livre les trois choses qui empêchent
la machine de mentir : une doctrine qui dit quoi faire du signal, des gates qui échouent vraiment
quand ils doivent échouer, et une preuve de portabilité obtenue sur la plateforme cible plutôt que
sur celle du développeur.

Output: 1 doctrine insérée, 2 cas de test embarqués, 1 preuve conteneur consignée, 4 fichiers de
release-meta bumpés.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-PATTERNS.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-01-SUMMARY.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-02-SUMMARY.md
@docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md
@plugin/dev-orchestrator/AGENT.md
@CLAUDE.md
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `## Signaux de démarrage` | section `AGENT.md` (nouvelle) | insérée entre « Next steps & hygiène documentaire » et « Heuristiques de routage » |
| table 4 lignes de cette section | table `AGENT.md` (nouvelle) | colonnes : Signal · Geste proposé · Confirmation ; lignes `[bootstrap]`, `[onboard]`, `[gsd-engine]`, `[doc-drift]` — **jamais** `[docs-ingest]` |
| `T20` | cas de test (nouveau) | dans `test-dev-orchestrator.sh` — gate ADR-044 via `check-agents.sh --file` |
| `T21` | cas de test (nouveau) | dans `test-dev-orchestrator.sh` — invariants SC5 par grep structurel sur les 2 nouveaux scripts |
| `plugin/dev-orchestrator/VERSION` | valeur modifiée | `v2.4.0` → `v2.5.0` |
| `plugin/dev-orchestrator/module.json` clé `version` | valeur modifiée | `v2.4.0` → `v2.5.0` |
| `plugin/dev-orchestrator/CHANGELOG.md` | entrée ajoutée | `## [v2.5.0] — <date du jour>` en tête |
| `plugin/dev-orchestrator/README.md` | 3 zones modifiées | ligne `**Version**`, section Historique, section Structure du module |

## Détecteurs `plan:pre` non déclenchés

`ai-integration`, `assumption-delta` et `schema-gate` ne s'appliquent pas à ce plan : aucune
intégration d'API/SDK externe, aucune transition d'identité singulier→pluriel ni requis→optionnel,
aucun fichier d'ORM ou de schéma touché (`module.json` est un manifeste de module, pas un schéma de
données). Confirmation explicite, pas un oubli.

<tasks>

<task type="auto">
  <name>Task 1 : doctrine « Signaux de démarrage » dans `AGENT.md` + gates T20/T21 réellement falsifiables</name>
  <precondition>`bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md` sort en 0 avec exactement 3 warnings AVANT toute modification (baseline vérifiée le 2026-07-27 : name différent du nom de fichier, aucun skill câblé, `tools:` absent). Si la baseline observée diffère, s'arrêter et remonter l'écart plutôt que d'ajuster le seuil du test.</precondition>
  <reversibility rating="reversible">La section d'`AGENT.md` et les deux cas de test sont des ajouts
  isolés ; les retirer restaure l'état antérieur sans aucun effet sur les scripts livrés.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/AGENT.md` — lignes 51-64 (table « Amont & cadrage », modèle de forme de table ET ligne 63 qui couvre DÉJÀ `[docs-ingest]` via `ingestion-flow.md`), lignes 97-111 (point d'insertion : entre la fin de « Next steps & hygiène documentaire » et le titre « Heuristiques de routage »)
    - `docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md` §4.1 (les 5 gabarits de signal) et §5 (« un tableau de 4 lignes, pas une doctrine parallèle »)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-11, D-12, D-15
    - `plugin/conductor/scripts/check-agents.sh` — le chemin `--file` et le format des lignes de warning
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` — en-tête documentaire (liste T1..T19 à compléter), helpers `ok()` / `ko()`, compteurs `pass` / `fail` / `skipped`, dernier bloc T19f et pied de fichier
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` et `plugin/dev-orchestrator/scripts/check-doc-drift.sh` (les 2 sujets de T21)
  </read_first>
  <files>plugin/dev-orchestrator/AGENT.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <action>
Insérer dans `plugin/dev-orchestrator/AGENT.md` une section de niveau 2 intitulée
« Signaux de démarrage », placée entre la fin de la section « Next steps & hygiène documentaire » et
le titre « Heuristiques de routage » (D-11). Deux à trois lignes de chapeau maximum, puis une table
de **4 lignes exactement**, à trois colonnes : le signal (le marqueur littéral tel qu'il apparaît en
tête de la ligne injectée), le geste proposé (la brique `gsd-*` correspondante, dans le style des
tables voisines), et le rappel de confirmation (ADR-031). Les 4 lignes sont `[bootstrap]` →
`gsd-config` puis `gsd-map-codebase` selon les items listés, `[onboard]` → `gsd-onboard`,
`[gsd-engine]` → orientation vers la chaîne `gsd-discuss-phase` / `gsd-plan-phase` / `gsd-progress`
(pas un geste correctif : c'est une orientation de routage), `[doc-drift]` → `gsd-docs-update`.

Ne PAS ajouter de ligne `[docs-ingest]` : elle est déjà couverte par la ligne 63 de la table
« Amont & cadrage », qui renvoie à `ingestion-flow.md`. La dupliquer créerait deux sources de vérité
sur le même signal. Le chapeau peut mentionner d'un mot que le 5e signal est traité en amont, avec
le renvoi.

Ne créer AUCUN fichier `references/*.md` pour cette doctrine : la spec §5 l'exclut explicitement
(« un tableau de 4 lignes, pas une doctrine parallèle »). Le chapeau rappelle que la confirmation
humaine reste devant chaque geste et que ces lignes arrivent au `SessionStart` de la session
principale, donc éventuellement hors de toute invocation de l'agent. Ne pas nommer un verbe de la
façade supprimée en v2.33.0 : le cas T3 existant échouerait.

Ajouter ensuite deux cas dans `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`, en
continuant la numérotation après T19f et en reproduisant la forme des blocs existants (bandeau de
séparation, commentaire expliquant le piège couvert, helpers `ok()` / `ko()`). Compléter aussi la
liste T1..T19 de l'en-tête `# ` du fichier avec les deux nouvelles entrées.

**T20 — gate ADR-044 réellement falsifiable (D-12).** Le cas invoque
`bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md` en capturant
stdout et stderr ensemble, et assert TROIS choses séparément : le code de sortie vaut 0 ; le nombre
de lignes de warning (comptées sur le caractère de warning que le script émet en tête de chacune)
vaut exactement la baseline de 3 ; et les trois types de warning connus sont chacun présents une
fois (nom différent du nom de fichier, aucun skill câblé, `tools:` absent). Cette triple assertion
est ce qui rend le cas falsifiable : un 4e type de warning fait monter le compte, la disparition
d'un type fait échouer la présence, et une invocation à nu (sans `--file`) sortirait 0 sans rien
vérifier — le commentaire du bloc doit dire explicitement POURQUOI `--file` est obligatoire ici
(l'`AGENT.md` est à la racine du module, hors de la boucle `plugin/*/agents` de la CI, et ce dépôt
n'a pas de `.claude/agents`, donc l'invocation à nu est un vert vide).

**T21 — invariants SC5 par grep structurel (D-15).** Le cas boucle sur les deux nouveaux scripts
(`check-dev-bootstrap.sh`, `check-doc-drift.sh`) et vérifie, sur le fichier privé de ses lignes de
commentaire (`grep -vE '^[[:space:]]*#'`) pour ne pas se laisser satisfaire par de la prose :
(a) aucune ligne ne se termine par une sortie en code d'échec 1 — seuls 0, 3 et 64 sont admis ;
(b) toute redirection d'écriture cible soit `/dev/null`, soit le descripteur d'erreur, soit une
variable dont le nom contient `TMP` — aucune écriture vers un chemin littéral ou construit ;
(c) aucune occurrence de `mkdir`, `touch`, `tee`, `cp`, `mv`, ni de `sed` en édition sur place ;
(d) tout appel à `mktemp` est apparié à au moins un `trap` sur `EXIT` dans le même fichier.
Chaque sous-vérification produit son propre `ok`/`ko` afin qu'un échec désigne l'invariant rompu et
le fichier fautif, pas un booléen global.

Vérifier après insertion que `AGENT.md` reste sous 250 lignes (le cas T5 existant le mesure par
`wc -l`) et que la suite complète passe.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md >/dev/null 2>&1 && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en 0, sa ligne de résultat annonce `0 KO`, et sa sortie mentionne T20 et T21.
    - `bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md 2>&1 | grep -c '⚠'` retourne exactement `3` — ni plus (nouveau type de warning introduit) ni moins (type disparu).
    - `bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md` sort en 0.
    - `wc -l < plugin/dev-orchestrator/AGENT.md` retourne une valeur ≤ 250 (ADR-029).
    - `grep -c '^## Signaux de démarrage' plugin/dev-orchestrator/AGENT.md` retourne `1`, et la section précède le titre `## Heuristiques de routage` dans l'ordre du fichier.
    - La table de la nouvelle section compte exactement 4 lignes de données (hors en-tête et séparateur) et ne contient pas le marqueur `docs-ingest`.
    - Falsifiabilité de T20 démontrée à la main pendant l'exécution (résultat consigné dans le SUMMARY, aucune modification conservée) : en injectant temporairement dans une COPIE d'`AGENT.md` un frontmatter dégradé produisant un warning supplémentaire, T20 doit passer au rouge. Un T20 qui reste vert dans ce test de mutation est un T20 inutile.
    - Falsifiabilité de T21 démontrée de la même façon sur une copie temporaire d'un des deux scripts (ajout d'une écriture vers un chemin littéral) : T21 doit passer au rouge.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c 'test-dev-orchestrator.sh'` retourne `1` — T20 et T21 sont donc exécutés en CI sans aucune édition de `.github/workflows/ci.yml`.
  </acceptance_criteria>
  <done>La doctrine est posée en 4 lignes sans doctrine parallèle ni duplication de `[docs-ingest]`, et les deux gates ne peuvent plus rendre un vert vide : T20 échoue si le profil de warnings d'`AGENT.md` se dégrade, T21 échoue si un des deux scripts acquiert une écriture ou un code d'échec bloquant — les deux prouvés falsifiables par mutation.</done>
</task>

<task type="auto">
  <name>Task 2 : preuve d'intégration SC1 sur ce dépôt + preuve de portabilité Linux en conteneur</name>
  <precondition>Le démon Docker répond (`docker info` sort en 0) et le conteneur dispose d'un accès réseau si l'image retenue nécessite l'installation de `git`. Sans démon disponible, s'arrêter et remonter le blocage : SIG-06 ne se coche pas sur un run macOS seul.</precondition>
  <reversibility rating="reversible">Tâche de vérification pure — elle n'écrit aucun fichier de
  produit, seulement la trace dans le SUMMARY.</reversibility>
  <read_first>
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-00 (SC1 amendé), D-13 (les 4 points de la preuve de portabilité)
    - `.planning/ROADMAP.md` §Phase 17, critères 1 et 6
    - `.planning/STATE.md` — la leçon durable du 2026-07-27 : tout bash développé sur macOS/BSD doit être reproduit sous conteneur Linux avant push (6 fixes de portabilité, run CI 30257419335)
    - `.github/workflows/ci.yml` lignes 25-45 (la découverte générique de suites qui tournera sur `ubuntu-latest`) — à LIRE, jamais à modifier
  </read_first>
  <files>(aucun fichier produit — tâche de vérification ; les résultats vont dans le SUMMARY)</files>
  <action>
**Volet 1 — preuve d'intégration SC1 amendé (D-00) sur ce dépôt, en local.** Invoquer chacun des
trois scripts avec `--hook --path .` en capturant stdout et le code de retour SÉPARÉMENT pour
chacun (jamais l'un déduit de l'autre — D-14), puis vérifier les trois faits attendus : les trois
sortent en 3 ; `check-dev-bootstrap.sh` est le seul à imprimer quoi que ce soit sur stdout, et
exactement les 2 lignes du signal `[gsd-engine]` avec le milestone et la phase du frontmatter réel
de `.planning/STATE.md` ; les deux autres impriment 0 octet sur stdout (leurs diagnostics `say()`
partent sur stderr et ne sont donc jamais injectés dans le contexte). Concaténer les trois stdout et
vérifier que le total fait exactement 2 lignes — c'est la formulation directe de SC1 amendé : « les
trois scripts sortent en 3 et la seule ligne injectée est le `[gsd-engine]` d'orientation ».
Consigner la sortie brute dans le SUMMARY.

**Volet 2 — preuve de portabilité Linux en conteneur (D-13, SIG-06).** Rejouer les suites dans un
conteneur Linux, sur la même plateforme que la CI. Image de premier choix : `ubuntu:24.04`, qui est
la plateforme de `ubuntu-latest` et le précédent déjà documenté dans `STATE.md` ; elle exige
d'installer `git` (nécessaire aux fixtures de `test-check-doc-drift.sh`) via le gestionnaire de
paquets au démarrage du conteneur. Si le réseau n'est pas disponible depuis le conteneur, se rabattre
sur une image qui embarque déjà `bash` ET `git` sans installation — `node:22-bookworm` convient. Le
nom de l'image RÉELLEMENT utilisée est consigné dans le SUMMARY ; le choix est laissé à l'exécuteur
mais la substitution doit être tracée, pas silencieuse.

Monter le dépôt en lecture dans le conteneur et exécuter, dans cet ordre :
`test-check-dev-bootstrap.sh`, `test-check-doc-drift.sh`, `test-discover-unintegrated-docs.sh`, puis
`test-dev-orchestrator.sh`. Les trois premières doivent rendre `0 ko`. La quatrième doit rendre
`0 KO` en tolérant ses branches SKIP déjà prévues (T1 et T6 sautent sans skill `gsd-*` ni install
réalisable) ; en revanche les cas T20 et T21 doivent explicitement passer dans le conteneur, puisque
ce sont les deux gates ajoutés par cette phase.

Deux pièges connus du montage à traiter d'emblée plutôt qu'à découvrir : le dépôt monté appartient à
un autre uid que l'utilisateur du conteneur, ce qui fait refuser à git le répertoire de travail —
neutraliser en déclarant le point de montage comme répertoire sûr dans la configuration git du
conteneur ; et `TMPDIR` peut différer de celui de l'hôte, ce que les suites absorbent déjà puisque
toutes leurs fixtures passent par `mktemp -d`.

En cas d'échec dans le conteneur, NE PAS corriger la suite pour la faire passer : l'échec désigne un
idiome non portable dans le script sous test. Corriger le script en revenant aux idiomes de D-13.2
(`set -uo pipefail`, `mktemp` + `trap ... EXIT`, `LC_ALL=C sort`, awk POSIX, `find` avec `-prune`),
puis rejouer les suites sur macOS ET dans le conteneur — les deux plateformes doivent être vertes
simultanément, jamais l'une après l'autre.

Consigner dans le SUMMARY : la commande `docker run` exacte, l'image utilisée, la sortie de chacune
des quatre suites, et la liste des corrections de portabilité éventuellement nécessaires. SIG-06 ne
se coche que sur cette trace (P-04).
  </action>
  <verify>
    <automated>a=$(bash plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh --hook --path . 2>/dev/null); ra=$?; b=$(bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --hook --path . 2>/dev/null); rb=$?; c=$(bash plugin/dev-orchestrator/scripts/check-doc-drift.sh --hook --path . 2>/dev/null); rc=$?; [ "$ra" -eq 3 ] && [ "$rb" -eq 3 ] && [ "$rc" -eq 3 ] && [ -z "$b" ] && [ -z "$c" ] && [ "$(printf '%s\n' "$a" | wc -l | tr -d ' ')" = "2" ] && printf '%s' "$a" | grep -q 'gsd-engine' && echo SC1-OK</automated>
  </verify>
  <acceptance_criteria>
    - Le contrôle SC1 ci-dessus imprime `SC1-OK` : les trois scripts sortent en 3, seuls les 2 lignes de `[gsd-engine]` sont écrites sur stdout, et les deux autres scripts n'écrivent rien sur stdout.
    - La commande `docker run` réellement exécutée, le nom de l'image, et la sortie complète des quatre suites sont copiés dans le SUMMARY — pas résumés en « tout est vert ».
    - Dans le conteneur Linux : `test-check-dev-bootstrap.sh`, `test-check-doc-drift.sh` et `test-discover-unintegrated-docs.sh` rendent chacune `0 ko`.
    - Dans le conteneur Linux : `test-dev-orchestrator.sh` rend `0 KO`, et sa sortie montre T20 et T21 en succès (pas en SKIP).
    - Aucun fichier du dépôt n'est modifié par cette tâche en dehors d'éventuelles corrections de portabilité sur les deux nouveaux scripts ; si de telles corrections ont lieu, les suites sont rejouées sur macOS **et** dans le conteneur, et les deux runs sont consignés.
    - `git status --short .github/workflows/ci.yml plugin/_internal/vibeflow-update.sh` ne retourne rien : ni la CI ni l'engine n'ont été touchés.
  </acceptance_criteria>
  <done>SC1 amendé est prouvé par exécution réelle sur ce dépôt, et SIG-06 est prouvé par une exécution réelle sous Linux avec trace de l'image et des sorties — jamais coché sur la foi d'un run macOS.</done>
</task>

<task type="auto">
  <name>Task 3 : release-meta du module — `v2.4.0` → `v2.5.0`, périmètre strictement borné</name>
  <reversibility rating="reversible">Le module n'est distribué aux labs qu'à la release racine, qui
  est hors périmètre et réservée à validation humaine : tant qu'elle n'a pas lieu, revenir sur ce
  bump coûte quatre éditions de fichier et aucune migration.</reversibility>
  <read_first>
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-16 et `<deferred>` (la release racine `v2.41.0` est explicitement hors périmètre)
    - `CLAUDE.md` racine — règle de numérotation (nouvelle capacité → minor) et discipline de release (les tags et releases GitHub concernent la `VERSION` racine, pas celle d'un module)
    - `plugin/dev-orchestrator/VERSION`, `plugin/dev-orchestrator/module.json` (valeur actuelle `v2.4.0` aux deux endroits)
    - `plugin/dev-orchestrator/CHANGELOG.md` lignes 1-30 (forme de l'entrée `## [v2.4.0]` du 2026-07-27 à reproduire)
    - `plugin/dev-orchestrator/README.md` — ligne `**Version**` (l. 10), section Historique, section Structure du module (les 3 zones à mettre à jour)
    - Les SUMMARY 17-01 et 17-02 (pour lister exactement ce qui a été livré dans l'entrée de CHANGELOG)
  </read_first>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md, plugin/dev-orchestrator/README.md</files>
  <action>
Porter le module de `v2.4.0` à `v2.5.0` — bump **mineur** parce que la phase ajoute une capacité
(un fragment de hooks et deux scripts de constat), conformément à la règle de numérotation du
`CLAUDE.md` racine et au précédent de la Phase 13 (`v2.1.1` → `v2.2.0`).

Quatre fichiers, et seulement ceux-là :
- `plugin/dev-orchestrator/VERSION` : remplacer la chaîne brute par `v2.5.0`, en conservant le
  préfixe `v` et l'absence de saut de ligne parasite.
- `plugin/dev-orchestrator/module.json` : porter la clé `version` à `v2.5.0`. Compléter la
  `description` d'une mention courte des signaux de démarrage (le fragment de hooks
  `SessionStart` et les trois signaux advisory), sans réécrire le reste de la description
  existante.
- `plugin/dev-orchestrator/CHANGELOG.md` : ajouter en tête, sous le titre du fichier, une entrée
  `## [v2.5.0] — <date du jour>` reprenant la forme de l'entrée `v2.4.0` (rubrique « Ajouté », et
  « Modifié » si nécessaire). Contenu à énumérer depuis les SUMMARY 17-01 et 17-02 : le fragment
  `hooks/hooks.json` (premier du module), `check-dev-bootstrap.sh` et son continuum à 4 états,
  `check-doc-drift.sh` et son seuil réglable, le mode `--hook` additif de
  `discover-unintegrated-docs.sh` avec la mention explicite que son contrat historique est
  inchangé, les trois nouvelles suites de test, la section « Signaux de démarrage » d'`AGENT.md`,
  et les gates T20/T21. Mentionner la spec de référence par son chemin.
- `plugin/dev-orchestrator/README.md` : porter la ligne `**Version**` à `v2.5.0` ; ajouter une
  entrée dans la section Historique sur le modèle des entrées existantes ; compléter la section
  Structure du module avec le répertoire `hooks/` et son `hooks.json`, les deux nouveaux scripts et
  leurs deux suites de test.

Périmètre strictement borné (P-05, CONTEXT `<domain>` et `<deferred>`) : NE PAS toucher à la
`VERSION` racine, ni à `plugin/.claude-plugin/plugin.json`, ni à `.claude-plugin/marketplace.json`,
ni aux deux README racine ni à leurs badges, ne créer aucun tag git et aucune release GitHub. La
release racine `v2.41.0` proposée par la spec §7 est un reste-à-faire remonté à l'humain en fin de
mission, jamais exécuté ici. Le formuler comme proposition dans le SUMMARY de ce plan.
  </action>
  <verify>
    <automated>test "$(cat plugin/dev-orchestrator/VERSION)" = "v2.5.0" && jq -e '.version == "v2.5.0"' plugin/dev-orchestrator/module.json >/dev/null && grep -q '^\*\*Version\*\* : v2.5.0' plugin/dev-orchestrator/README.md && grep -q '^## \[v2.5.0\]' plugin/dev-orchestrator/CHANGELOG.md && git status --short VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json README.md README.fr.md | grep -c . | grep -qx 0 && echo RELEASE-META-OK</automated>
  </verify>
  <acceptance_criteria>
    - `cat plugin/dev-orchestrator/VERSION` retourne exactement `v2.5.0`.
    - `jq -r '.version' plugin/dev-orchestrator/module.json` retourne `v2.5.0`.
    - `grep -c '^\*\*Version\*\* : v2.5.0' plugin/dev-orchestrator/README.md` retourne `1`.
    - `grep -c '^## \[v2.5.0\]' plugin/dev-orchestrator/CHANGELOG.md` retourne `1`, et l'entrée est la première du fichier après le titre.
    - La section Structure du module du README mentionne `hooks/hooks.json`, `check-dev-bootstrap.sh`, `check-doc-drift.sh`, `test-check-dev-bootstrap.sh` et `test-check-doc-drift.sh`.
    - `git status --short VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json README.md README.fr.md .github/workflows/ci.yml plugin/_internal/vibeflow-update.sh` ne retourne AUCUNE ligne — le périmètre déféré n'a pas été touché.
    - `git tag --points-at HEAD` ne retourne aucun nouveau tag créé par cette tâche.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` passe toujours après le bump.
  </acceptance_criteria>
  <done>Le module annonce `v2.5.0` de façon cohérente aux quatre endroits, le CHANGELOG décrit la capacité livrée, et rien du périmètre réservé à validation humaine (release racine, tag, release GitHub, CI, engine) n'a bougé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine `AGENT.md` → décisions de l'agent | la table de 4 lignes oriente les gestes que `vibeflow-dev` propose ; une ligne rédigée à l'impératif deviendrait une instruction exécutée sans confirmation. |
| dépôt monté → conteneur Docker | le dépôt hôte est exposé à un environnement d'exécution distinct pendant la preuve de portabilité. |
| gate `check-agents.sh` → verdict de conformité | un gate invoqué à nu rend un vert vide : la frontière entre « vérifié » et « non vérifié » est le flag `--file`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-11 | Repudiation / Spoofing | gate ADR-044 invoqué sans `--file` — un vert qui ne vérifie rien | high | mitigate | Le cas T20 impose l'invocation `--file` et assert TROIS faits séparés (exit 0, compte de warnings égal à 3, présence des 3 types connus). La falsifiabilité est elle-même prouvée par un test de mutation exigé en critère d'acceptation : un T20 qui reste vert face à un `AGENT.md` dégradé est rejeté. |
| T-17-12 | Repudiation | SIG-06 coché sur un run macOS, portabilité affirmée sans preuve | high | mitigate | Prohibition P-04 + critères d'acceptation exigeant la trace de l'image, la commande `docker run` et la sortie brute des quatre suites dans le SUMMARY. Un échec conteneur impose de corriger le SCRIPT, jamais d'assouplir la suite. |
| T-17-13 | Elevation of Privilege | montage du dépôt hôte dans un conteneur pendant la preuve | low | accept | Le conteneur est éphémère (`--rm`), lancé sur une image publique standard, sans privilèges élevés, et la tâche n'écrit rien dans le dépôt monté hors d'éventuelles corrections de portabilité explicitement revues. Risque résiduel accepté : équivalent à exécuter les suites en local. |
| T-17-14 | Tampering | doctrine `AGENT.md` rédigée à l'impératif, exécutée par une boucle autonome | medium | mitigate | Prohibition P-03 (héritée des plans 17-01/17-02) appliquée à la 3e colonne de la table : chaque ligne rappelle nommément la confirmation requise (ADR-031). Critère d'acceptation : la table compte exactement 4 lignes à 3 colonnes dont la colonne Confirmation. |
| T-17-15 | Tampering | débordement du périmètre vers la release racine (bump, tag, release GitHub) | medium | mitigate | Prohibition P-05 + critère d'acceptation exécutable : `git status --short` sur les 7 chemins déférés doit être vide, et `git tag --points-at HEAD` ne doit montrer aucun tag créé. |
| T-17-SC | Tampering (supply chain) | image Docker tirée depuis un registre public | medium | mitigate | Aucune installation de paquet dans le dépôt (aucun `npm` / `pip` / `cargo install`, aucune dépendance ajoutée). La seule dépendance externe est l'image de conteneur, tirée d'un dépôt officiel (`ubuntu` ou `node`), utilisée en jetable (`--rm`), sans montage en écriture nécessaire et sans injection de secret. Le nom exact de l'image est consigné dans le SUMMARY, ce qui rend la provenance auditable. |
</threat_model>

## Flagged Assumptions (résidu de sonde, spec-less fallback)

- **SIG-06 / `unclassified`** — « unclassified — review manually ». Revue manuelle faite : SIG-06
  (portabilité prouvée par conteneur avant push) est une exigence de PROCÉDÉ, pas une arête de forme
  de données — aucune catégorie du taxon (boundary/empty/ordering/precision/adjacency/concurrency)
  ne s'y applique, d'où la non-classification. Il est couvert par une `truth` explicite, par la
  prohibition P-04 et par les critères d'acceptation de la Task 2. **Assomption restante** : la
  preuve dépend de la disponibilité du démon Docker sur la machine d'exécution — d'où la
  `<precondition>` explicite de la Task 2, qui impose de remonter le blocage plutôt que de cocher
  SIG-06 sans preuve. L'item reste `unresolved` au sens de la sonde, mais il est tracé, pas écarté.

Résolution portée par ce plan : SIG-05 `concurrency` → `truth` explicite (idempotence et lecture
seule de `check-agents.sh --file`). Le premier `unclassified` (SIG-04) est remonté dans le plan
17-01.

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` → `0 KO`, T20 et T21 présents et verts.
- `bash plugin/conductor/scripts/check-agents.sh --file plugin/dev-orchestrator/AGENT.md` → exit 0, exactement 3 warnings.
- `wc -l < plugin/dev-orchestrator/AGENT.md` ≤ 250.
- Le contrôle SC1 imprime `SC1-OK`.
- Les quatre suites passent dans le conteneur Linux, trace consignée dans le SUMMARY.
- Le contrôle release-meta imprime `RELEASE-META-OK`.
- `git status --short` ne montre aucun des 7 chemins déférés.
</verification>

<success_criteria>
- SIG-05 tenu : le gate ADR-044 est invoqué avec `--file`, assert un profil de warnings et non un simple exit 0, est prouvé falsifiable par mutation, et tourne en CI sans édition de `ci.yml`.
- SIG-06 tenu : les suites de la phase passent sous Linux dans un conteneur exécuté avant push, avec trace.
- SIG-04 tenu et vérifié par machine : T21 prouve par grep structurel l'absence de code d'échec bloquant et d'écriture hors fichier temporaire dans les deux nouveaux scripts.
- SC1 amendé (D-00) tenu : sur ce dépôt sain, l'unique contenu injecté est le signal `[gsd-engine]` en 2 lignes.
- Le module est en `v2.5.0` de façon cohérente, sans aucun débordement vers la release racine.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-03-SUMMARY.md` en fin de plan.
Y inclure, en clôture de phase, la PROPOSITION (non exécutée) de release racine `v2.41.0` avec tag
annoté et release GitHub, réservée à validation humaine.
</output>
---
phase: 08-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/software-architecture/rules/production-code-architecture.md
  - plugin/software-architecture/SKILL.md
  - plugin/software-architecture/module.json
  - plugin/software-architecture/VERSION
  - plugin/software-architecture/CHANGELOG.md
  - plugin/software-architecture/README.md
autonomous: true
requirements: [CONS-01]
must_haves:
  truths:
    - "La rule production-code-architecture.md porte désormais les 2 gates de feature-dev-gates (Gate Nyquist preuve-avant-code + Gate Decision Coverage), en plus des patterns de structure"
    - "Le foyer unique est software-architecture : structure + gates de process dans une seule rule path-scopée"
    - "SKILL.md mentionne les 2 gates dans le Tier 1 (preuve avant done)"
    - "module.json + VERSION bumpés v1.2.0 → v1.3.0 ; CHANGELOG entrée v1.3.0 ; README à jour"
    - "ADR-037 (origine feature-dev-gates) noté comme absorbé dans ADR-035 étendu"
  artifacts:
    - path: "plugin/software-architecture/rules/production-code-architecture.md"
      provides: "Rule unique : structure + Gate Nyquist + Gate Decision Coverage"
      contains: "Nyquist"
---

# Plan 08-01 — Fusion des gates dans software-architecture

## Objectif
Rapatrier les 2 gates de `feature-dev-gates` dans la rule de `software-architecture` → un seul foyer
de rule de code (structure + gates). Préparer la suppression du module (plan 08-02).

## Tâches
1. `rules/production-code-architecture.md` : ajouter section « Gates de développement de feature »
   (Gate Nyquist : preuve avant code ; Gate Decision Coverage : traçabilité décision→code), reprise
   fidèle du contenu de `feature-dev-gates/rules/feature-dev-gates.md` (mêmes globs, déjà identiques).
2. `SKILL.md` : Tier 1 mentionne les 2 gates (articulés à la carte TDD + verification-before-completion).
3. Release-meta : `module.json` + `VERSION` → v1.3.0, description ; `CHANGELOG` v1.3.0 ; `README`.

## Vérification
- `grep -i "nyquist\|decision coverage" rules/production-code-architecture.md` → match.
- `VERSION` == `module.json` == v1.3.0 ; tests module verts ; densité rule/SKILL sous seuils.

---
phase: 41-posture-de-protection-du-d-p-t
plan: 11
lot: L11 — décision du mode de release (règle « Quand publier »), bump patch dérivé ou non, publication et merge SANS contournement de la PR des preuves
type: execute
wave: 11
depends_on: ["41-10"]
autonomous: false
requirements: [PROT-02]
files_modified:
  - VERSION
  - plugin/.claude-plugin/plugin.json
  - plugin/.codex-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - README.fr.md
  - CHANGELOG.md
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Aucun bump sans décision explicite consignée `RELEASE-DECISION:` (décideur, canal, date) : la règle « Quand publier » (Samuel, WhatsApp, 2026-09-23, inscrite par la PR #87) ne publie une release que pour une évolution fonctionnelle de ce qu'un utilisateur installe — le volet admin n'en porte aucune — alors que le critère 3 du ROADMAP exige le rejeu du flux de release sous la règle et que D-M8 (décision du manager) prévoyait une release patch de la phase. Le planificateur ne tranche pas"
    - "Option `release-de-phase` : numéro DÉRIVÉ à l'exécution — patch suivant de la `VERSION` alors sur `origin/main`, appliqué par `scripts/bump.sh`, jamais écrit en dur (D-M8 : durcissement = patch) ; l'entrée CHANGELOG nomme la dérogation à « Quand publier » avec la décision consignée"
    - "Option `preuves-sans-release` : aucun fichier de version, CHANGELOG ni README touché ; la PR des preuves entre sans bump ; le plan 41-12 est sans objet et le critère 3 est consigné non rejoué dans cette phase (41-13 ne coche rien qui en dépende et ne déclare pas la phase complète)"
    - "La PR des preuves ne touche aucun chemin CODEOWNERS, aucun fichier sous `plugin/` hors les deux manifestes de version, ni `.github/workflows/ci.yml`, ni `scripts/check-release-tag.sh`, ni `scripts/hooks/` : elle doit être mergeable sans revue et sans contournement (D-M5, D-M9)"
    - "Rejeu du flux de release, première moitié (critère 3) : PR ouverte, CI verte, état `CLEAN`, merge `--merge` par Willy SANS `--admin` ni case de contournement ; un état `BLOCKED` arrête le plan, il n'est jamais contourné"
    - "L'entrée CHANGELOG et les historiques README n'associent O-3 à aucun mot d'achèvement ; le SUMMARY de ce plan est committé sur B AVANT la publication"
  artifacts:
    - path: "VERSION"
      provides: "version patch dérivée, synchronisée par `bump.sh` (option `release-de-phase` seulement)"
    - path: "CHANGELOG.md"
      provides: "entrée de release de la Phase 41 (option `release-de-phase` seulement)"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`RELEASE-DECISION:`"
  key_links:
    - "Le merge de la PR des preuves est relu et consigné par 41-12 sur une troisième branche (D-03) ; en `preuves-sans-release`, 41-12 consigne seulement `RELEASE: sans_objet`"
---

<objective>
Faire trancher le mode de release, puis préparer la PR des preuves sur la branche B et la faire
entrer sur `main` par le chemin normal, sans aucun contournement.

Révisé le 2026-09-23 (reprise) : une règle nouvelle — « une release seulement pour une évolution
fonctionnelle » (Samuel, WhatsApp, 2026-09-23, PR #87 ouverte ce jour) — contredit la release de
phase prévue par D-M8 pour rejouer le critère 3 ; une décision explicite précède donc tout bump. Les
gestes humains sont ceux de Willy (`picmakpro`).

Purpose: PROT-02, première moitié du rejeu du flux de release sous la règle.
Output: `RELEASE-DECISION:` consignée ; PR des preuves mergée sans contournement, avec ou sans bump selon la décision.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@CLAUDE.md

Extraits à lire : `scripts/bump.sh` (en-tête : sources de version, squelette CHANGELOG) ; `README.md`
et `README.fr.md` § Versioning (table d'historique, première ligne de données) ; `CHANGELOG.md` en
tête (dernière entrée `## [v…]`) ; `CLAUDE.md` d'`origin/main`, § Discipline de release (paragraphe
« Quand publier » s'il y est) ; `.planning/ROADMAP.md` § Phase 41, critère 3.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1 : décision du mode de release — release de phase par dérogation, preuves sans release, ou arrêt</name>
  <read_first>
    - `CLAUDE.md` tel que sur `origin/main` (§ Discipline de release)
    - .planning/ROADMAP.md § Phase 41 (critère 3)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (D-M8)
  </read_first>
  <action>Identité `picmakpro` ; branche B ; `git fetch origin`. L'exécutant relève l'état de la PR #87 (`gh pr view 87 --json state,mergedAt`) et la présence du paragraphe « **Quand publier** » dans `CLAUDE.md` d'`origin/main`, présente la décision avec ces deux mesures, et s'arrête. Aucun bump avant la réponse consignée et committée.</action>
  <decision>La phase publie-t-elle sa propre release (patch) pour rejouer le critère 3, alors que la règle « Quand publier » réserve les releases aux évolutions fonctionnelles ?</decision>
  <context>
    Le volet admin ne change rien de ce qu'un utilisateur installe (configuration GitHub, doctrine,
    planning). Le critère 3 exige « bump → PR → merge → tag annoté → release GitHub →
    `check-release-tag.sh --remote` ✓ » SOUS la règle. Précédent mesuré : la v2.64.0 a embarqué les
    gardes du périmètre sans admin dans une release fonctionnelle.
  </context>
  <options>
    <option id="release-de-phase">
      <name>RELEASE DE PHASE PAR DÉROGATION</name>
      <pros>Critère 3 rejoué dans la phase ; milestone clôturable si les autres critères tiennent.</pros>
      <cons>Publie une mise à jour sans évolution fonctionnelle, contre la règle « Quand publier » : la dérogation est écrite au CHANGELOG avec cette décision.</cons>
    </option>
    <option id="preuves-sans-release">
      <name>PREUVES SANS RELEASE</name>
      <pros>Respecte « Quand publier » ; les preuves et la doctrine entrent sur `main`.</pros>
      <cons>Critère 3 non rejoué dans cette phase (il le sera à la prochaine release fonctionnelle, hors phase) ; la phase n'est pas déclarée complète et le milestone ne se clôt pas.</cons>
    </option>
    <option id="arret-et-cadrage">
      <name>ARRÊT ET CADRAGE</name>
      <pros>Aucun geste ; la question est recadrée (reformulation du critère 3, par exemple).</pros>
      <cons>La PR des preuves reste sur B, non publiée.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - réponse explicite obtenue, jamais inférée ; `RELEASE-DECISION:` consignée et committée sur B avant tout bump
  </acceptance_criteria>
  <resume-signal>Répondre « RELEASE DE PHASE PAR DÉROGATION », « PREUVES SANS RELEASE » ou « ARRÊT ET CADRAGE ». L'exécutant consigne dans une section `## 41-11 — release` de `41-PREUVES.md` : `RELEASE-DECISION: option=<release-de-phase|preuves-sans-release|arret-et-cadrage> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint> regle_quand_publier=<sur_main|pr_ouverte|absente>` — jamais inféré — et le commite sur B avec le message « docs(41-11): RELEASE-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> ».</resume-signal>
</task>

<task type="auto">
  <name>Task 2 : bump patch dérivé (ou aucun, selon la décision), CHANGELOG et historiques README, périmètre de la PR, rejeu, SUMMARY avant publication</name>
  <files>VERSION, plugin/.claude-plugin/plugin.json, plugin/.codex-plugin/plugin.json, .claude-plugin/marketplace.json, README.md, README.fr.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`RELEASE-DECISION:`)
    - scripts/bump.sh (en-tête et squelette CHANGELOG)
    - CHANGELOG.md (dernière entrée `## [v…]`, pour le ton et la structure)
    - README.md et README.fr.md § Versioning
  </read_first>
  <action>
Identité `picmakpro` ; branche B ; `git fetch origin` ; si `origin/main` a bougé depuis 41-10,
`git merge --no-edit origin/main` en local. Lire `RELEASE-DECISION:` : `arret-et-cadrage` → arrêt.

Option `release-de-phase` : dériver la version — `V` = contenu de `VERSION` sur `origin/main` sans
préfixe ; `N` = `X.Y.(Z+1)`. Lancer `bash scripts/bump.sh "$N"`. Remplacer le commentaire de
squelette inséré par `bump.sh` dans la nouvelle entrée `## [vN]` par un résumé en français :
rulesets de branche et de tags posés sur décision et mesurés (contournement nommé Samuel et Willy,
D-02bis ; refus par défaut d'une PR rouge et sa provenance ; trace d'un contournement sur des checks
en cours ; refus serveur d'un push direct hors liste ; M-1 à M-4 ; PR en vol rapportées ; décisions
humaines), CODEOWNERS étroit prouvé sur `.github/`, la baseline et une sentinelle, ADR-072 amendée,
amendement d'ADR-059, statut d'O-3 « gardée par défaut + tracée », aucun gate créé par le volet admin
(QUAL-01, D-M11), la dérogation à « Quand publier » avec la décision consignée (décideur, canal,
date), commits couverts depuis le tag précédent (liste `git log --oneline v<V>..HEAD`), numéros de PR
(phase, PR-P). Ajouter une première ligne de données à la table d'historique de `README.md`
(anglais) et de `README.fr.md` (français) pour `vN`, date d'exécution, résumé court en gras.

Option `preuves-sans-release` : ne toucher à aucun fichier de version, ni au CHANGELOG, ni aux
README.

Dans les deux cas : contrôler le périmètre de la future PR (`git diff --name-only origin/main...HEAD`).
Rejouer `gates` et `tests`. Commit local sur B (si bump : message « chore(41-11): release v<N> —
bump patch dérivé, CHANGELOG et historiques README »). Rédiger et committer `41-11-SUMMARY.md` sur B
À LA FIN de cette tâche : il recopie `RELEASE-DECISION:` et annonce la Task 3 (publication et merge
humains), dont le résultat sera relu par 41-12.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; o=$(val RELEASE-DECISION: option); w=$(val RELEASE-DECISION: decideur); c=$(val RELEASE-DECISION: canal); d=$(val RELEASE-DECISION: date); q=$(val RELEASE-DECISION: regle_quand_publier); T=$(mktemp -d); git show HEAD:"$PH/41-PREUVES.md" > "$T/h"; cm=$(awk "\$1==\"RELEASE-DECISION:\" {c++} END{print c+0}" "$T/h"); git show origin/main:CLAUDE.md > "$T/cl"; sm=$(awk "index(\$0, \"**Quand publier**\") {c++} END{print c+0}" "$T/cl"); p87=$(gh pr view 87 --repo picmakpro/vibeflow-os --json state --jq .state 2>/dev/null || echo inconnue); qc=$([ "$sm" -ge 1 ] && echo sur_main || { [ "$p87" = OPEN ] && echo pr_ouverte || echo absente; }); echo "release-decision : option=$o decideur=$w canal=$c date=$d regle_consignee=$q regle_mesuree=$qc (pr87=$p87) committee=$cm"; case "$o" in release-de-phase|preuves-sans-release|arret-et-cadrage) ;; *) exit 1;; esac; [ -n "$w" ] && [ -n "$c" ] && case "$d" in 2[0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]) true;; *) false;; esac && [ "$cm" -eq 1 ] && [ "$o" != arret-et-cadrage ]'</automated>
    <fails_when>exit non nul ; ou `RELEASE-DECISION:` absente, non committée, sans décideur, canal ou date valide, ou d'option inconnue ; ou option `arret-et-cadrage` (le plan s'arrête là, sans bump ni publication)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; opt=$(awk "\$1==\"RELEASE-DECISION:\" { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) { print substr(\$i, 8); exit } }" "$PH/41-PREUVES.md"); TV=$(mktemp -d); git show origin/main:VERSION > "$TV/v"; V=$(tr -d "v[:space:]" < "$TV/v"); case "$V" in *.*.*) ;; *) echo "VERSION origin/main illisible"; exit 1;; esac; if [ "$opt" = release-de-phase ]; then N="${V%.*}.$(( ${V##*.} + 1 ))"; B=$(tr -d "v[:space:]" < VERSION); rc=0; bash scripts/check-version-sync.sh > /dev/null 2>&1 || rc=$?; e=$(awk -v t="## [v$N]" "index(\$0, t)==1 {c++} END{print c+0}" CHANGELOG.md); T=$(mktemp -d); awk -v t="## [v$N]" "index(\$0, t)==1 {f=1; next} /^## \\[v/ {f=0} f" CHANGELOG.md > "$T/e"; sq=$(awk "/<!--/ {c++} END{print c+0}" "$T/e"); tx=$(awk "NF {c++} END{print c+0}" "$T/e"); r1=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.md); r2=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.fr.md); f1=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); f2=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.fr.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); echo "bump (release-de-phase) : origin_main=$V attendu=$N branche=$B version_sync_rc=$rc entree_changelog=$e commentaire_squelette_restant=$sq lignes_non_vides_entree=$tx readme_en=$r1 readme_fr=$r2 premiere_ligne_en=$f1 premiere_ligne_fr=$f2"; [ "$B" = "$N" ] && [ "$rc" -eq 0 ] && [ "$e" -eq 1 ] && [ "$sq" -eq 0 ] && [ "$tx" -ge 3 ] && [ "$r1" -eq 1 ] && [ "$r2" -eq 1 ] && [ "$f1" -eq 1 ] && [ "$f2" -eq 1 ]; elif [ "$opt" = preuves-sans-release ]; then B=$(tr -d "v[:space:]" < VERSION); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; vb=$(awk "\$0==\"VERSION\" || \$0==\"CHANGELOG.md\" || \$0==\"README.md\" || \$0==\"README.fr.md\" || \$0==\".claude-plugin/marketplace.json\" || index(\$0, \"plugin/\")==1 {c++} END{print c+0}" "$T/f"); echo "sans release : version_branche=$B origin_main=$V fichiers_de_release_touches=$vb"; [ "$B" = "$V" ] && [ "$vb" -eq 0 ]; else echo "RELEASE-DECISION absente ou non poursuivable : ${opt:-absente}"; exit 1; fi'</automated>
    <fails_when>exit non nul ; en `release-de-phase` : `branche=` différent du patch suivant de la `VERSION` d'`origin/main` (numéro écrit en dur, ou `main` a bougé : refaire la dérivation après merge d'`origin/main`), `version_sync_rc=` différent de 0, entrée CHANGELOG absente ou dupliquée, commentaire de squelette restant, entrée vide, ou ligne d'historique absente, dupliquée ou pas en première position dans un des deux README ; en `preuves-sans-release` : version de la branche différente de celle d'`origin/main`, ou un fichier de release touché (VERSION, manifestes, CHANGELOG, README)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; if git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p"; then echo "CHEMIN CODEOWNERS $p"; co=$((co+1)); fi; done < "$T/f"; z=$(awk "(index(\$0, \"plugin/\")==1 && \$0 != \"plugin/.claude-plugin/plugin.json\" && \$0 != \"plugin/.codex-plugin/plugin.json\") || \$0==\".github/workflows/ci.yml\" || \$0==\"scripts/check-release-tag.sh\" || index(\$0, \"scripts/hooks/\")==1 {c++} END{print c+0}" "$T/f"); n=$(awk "END{print NR}" "$T/f"); echo "perimetre PR des preuves : fichiers=$n chemins_codeowners=$co chemins_interdits=$z"; [ "$n" -gt 0 ] && [ "$co" -eq 0 ] && [ "$z" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou un `CHEMIN CODEOWNERS` (la PR exigerait la revue `@picmakpro` : le rejeu « sans contournement » deviendrait dépendant d'une approbation) ; ou `chemins_interdits=` différent de 0 (fichier sous `plugin/` hors les deux manifestes de version, CI, gate de release ou hooks touchés — D-M5, D-M9)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 CHANGELOG.md README.md README.fr.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition, O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-11-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); echo "avant publication : summary_committe=$s modifications_non_committees=$d"; [ "$s" = oui ] && [ "$d" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY non committé sur B ; ou modifications suivies non committées (elles n'entreraient pas dans la PR)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (dont un commit citant `RELEASE-DECISION` sans le décideur, le canal et la date consignés) ; ou `commits_de_plan=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=$(git merge-base HEAD origin/main); n=0; m=0; for c in $(git rev-list --no-merges "$B"..HEAD); do n=$((n+1)); f=$(git show --name-only --format= "$c"); p=$(printf "%s\n" "$f" | awk "index(\$0, \"plugin/\")==1 {c++} END{print c+0}"); q=$(printf "%s\n" "$f" | awk "index(\$0, \".planning/\")==1 {c++} END{print c+0}"); if [ "$p" -gt 0 ] && [ "$q" -gt 0 ]; then echo "COMMIT MIXTE plugin/+.planning/ : $c"; m=$((m+1)); fi; done; echo "separation plugin/.planning : commits=$n mixtes=$m"; [ "$n" -gt 0 ] && [ "$m" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou une ligne `COMMIT MIXTE plugin/+.planning/` (CLAUDE.md : jamais `plugin/` et `.planning/` dans un même commit) ; ou `commits=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `RELEASE-DECISION:` consignée et committée avant tout bump ; option `release-de-phase` ou `preuves-sans-release`
    - `release-de-phase` : `VERSION` = patch suivant de la `VERSION` d'`origin/main`, synchronisée partout (`check-version-sync` rc 0), entrée CHANGELOG rédigée (dérogation nommée), ligne d'historique en tête des deux README
    - `preuves-sans-release` : aucun fichier de release touché
    - PR des preuves hors CODEOWNERS, hors `plugin/` (sauf les deux manifestes), hors CI, gate de release et hooks ; aucune association d'O-3 à un mot d'achèvement
    - rejeux verts ; SUMMARY 41-11 committé ; arbre propre ; messages de commit conformes
    - aucun commit de la branche ne mélange un chemin `plugin/` et un chemin `.planning/` (vérifié commit par commit)
  </acceptance_criteria>
  <done>La branche B est prête à être publiée et mergée par le chemin normal, avec ou sans release selon la décision.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 : gestes humains de Willy (`picmakpro`) — publier B, ouvrir la PR des preuves, attendre la CI, merger SANS contournement</name>
  <read_first>
    - la version `<N>` (ou « sans release ») et les commandes imprimées par la Task 2
  </read_first>
  <action>Willy, compte `picmakpro`, publie B, ouvre la PR, attend la CI, vérifie l'état `CLEAN` et merge sans aucune option de contournement. Aucun agent ne merge.</action>
  <instructions>
Compte qui agit : `picmakpro`.

1. `git push -u origin feat/phase-41-preuves-release`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head feat/phase-41-preuves-release --title "Phase 41 — preuves de la protection du dépôt<, release vN si release-de-phase>" --body "Preuves (41-PREUVES.md), statut O-3 gardée par défaut + tracée, ADR-072 § Mesures, BACKLOG traité par renvoi<, bump patch si release-de-phase>. Merge sans contournement."`
   → noter `<n>`. Ce corps ne cite aucun arbitrage D-01..D-10 ni D-02bis ; s'il en cite un, il porte
   son canal et sa date (relu par 41-12).
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tout vert attendu).
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus,reviewDecision --jq '{mergeStateStatus, reviewDecision}'`
   - `CLEAN` : passer à 5.
   - `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`, puis refaire 3 et 4.
   - `BLOCKED` ou autre : NE PAS merger, NE PAS contourner ; copier la sortie et s'arrêter.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) → copier la sortie.
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
Ne pas poser le tag ici : c'est le plan 41-12.
  </instructions>
  <verification>Le plan 41-12 relit l'état `MERGED`, le corps de la PR, vérifie dans les rule suites que le merge n'a laissé aucune suite `bypass`, et consigne `PR-RELEASE:`.</verification>
  <acceptance_criteria>
    - PR des preuves ouverte depuis B, CI verte, `CLEAN` au moment du merge, mergée par `gh pr merge --merge` sans `--admin`
  </acceptance_criteria>
  <resume-signal>Coller `<n>`, les sorties de 3 (fin), 4 (toutes les lectures), 5 et son témoin, puis écrire « PR des preuves mergée sans contournement ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR des preuves → `main` | premier merge normal sous la règle : doit passer sans contournement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-39 | Tampering | numéro de release figé ou périmé | medium | mitigate | dérivé d'`origin/main` après merge local ; comparé au patch suivant ; `check-version-sync` rc 0 |
| T-41-40 | Repudiation | rejeu « vert » obtenu par contournement | high | mitigate | périmètre hors CODEOWNERS vérifié ; état `CLEAN` exigé ; `--admin` exclu ; absence de suite `bypass` vérifiée en 41-12 |
| T-41-66 | Repudiation | release publiée contre la règle « Quand publier » sans trace | medium | mitigate | `RELEASE-DECISION:` explicite avant tout bump ; dérogation écrite au CHANGELOG avec décideur, canal et date |
</threat_model>

<verification>
Mode de release décidé explicitement ; bump dérivé et synchronisé (ou aucun) ; PR des preuves
mergée par le chemin normal.
</verification>

<success_criteria>
Première moitié du critère 3 (bump → PR → merge sous la règle, sans contournement) en
`release-de-phase` ; en `preuves-sans-release`, preuves entrées sur `main` et critère 3 consigné non
rejoué.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-11-SUMMARY.md` — committé sur la branche B à la fin de la Task 2, AVANT la publication (Task 3).
</output>

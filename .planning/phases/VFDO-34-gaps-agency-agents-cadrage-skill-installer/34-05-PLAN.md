---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 05
type: execute
wave: 3
depends_on: ["34-04"]
files_modified:
  - plugin/web-test-team/module.json
  - plugin/web-test-team/VERSION
  - plugin/web-test-team/agents/vf-web-test-orchestrator.md
  - plugin/web-test-team/agents/vf-web-test-runner.md
  - plugin/web-test-team/agents/vf-web-app-fixer.md
  - plugin/web-test-team/rules/web-verify-gate.md
  - plugin/web-test-team/references/test-loop-protocol.md
  - plugin/web-test-team/README.md
  - plugin/web-test-team/CHANGELOG.md
  - README.md
  - README.fr.md
  - manual/en/03-modules/catalog.md
  - manual/fr/03-modules/catalogue.md
autonomous: true
requirements: [AGTS-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CE PLAN EST CONDITIONNEL et sa condition est MACHINE, pas narrative : il n'a de contenu que si le moule a été PROUVÉ, c'est-à-dire si `34-RUN-MOBILE.md` porte `> **Statut** : VERT` ET si `plugin/mobile-test-team/module.json` ne porte plus sa phrase de réserve de statut (état posé par 34-04). Sans cela, il se clôt en no-op tracé : cloner un moule non prouvé rejouerait exactement le motif « armé sans preuve » que ce milestone existe pour fermer (D-06)."
    - "Les trois agents sont RENOMMÉS dès la première ébauche, fichier ET champ `name` du frontmatter : `vf-web-test-orchestrator`, `vf-web-test-runner`, `vf-web-app-fixer`. Deux modules qui poseraient un agent de même `name` se marcheraient dessus à l'install (l'engine nomme la cible par le `name` du frontmatter, pas par le nom de fichier du module), et `check-agents.sh --resolve-agents=strict` en monde fermé le remonterait en CI. Le renommage n'est jamais un geste d'après-coup."
    - "Pattern 12 est respecté à la lettre sur les deux workers : `vf-internal: true`, aucun outil de délégation dans leur ligne `tools:`, et des domaines d'écriture STRICTEMENT DISJOINTS — le worker de test possède les specs, le worker applicatif possède le code de l'app et ne touche jamais aux tests. La règle anti-triche (ne jamais affaiblir un assert) et le gate ADR-045 (le worker applicatif n'a pas le web, il remonte une demande de recherche à l'orchestrateur) sont repris tels quels."
    - "La rule path-scopée `web-verify-gate.md` porte des marqueurs DISCRIMINANTS d'un projet testé par un runner web (fichier de configuration du runner, dossier de specs) — jamais un glob générique de type `src/**` qui la chargerait sur tout projet web. C'est l'erreur exacte déjà commise et corrigée côté mobile (`plugin/mobile-test-team/CHANGELOG.md` v1.3.1, globs génériques retirés parce qu'ils matchaient tout projet Next.js) : elle n'est pas re-commise ici."
    - "`module.json` déclare `\"requires\": []` : le runner web est une dépendance du LAB de l'utilisateur, pas du plugin — exactement comme Maestro l'est pour `mobile-test`. Aucune dépendance de stack plugin n'est ajoutée par ce plan, et aucune commande d'installation de paquet n'y figure."
    - "Ajouter un module fait passer le dépôt de 17 à 18 modules, ce que `scripts/check-version-sync.sh` VÉRIFIE (badge `modules-N` et phrase « N modules … » des DEUX README racine, comparés au compte réel de `plugin/*/module.json`). Ces quatre points sont mis à jour dans le même plan, sans quoi le job `gates` de la CI rougirait de façon déterministe. Les compteurs sont RE-DÉRIVÉS par commande, jamais recopiés (y compris le compteur de suites de tests des README, à re-mesurer même si ce module n'en ajoute aucune)."
    - "Le triplet RACINE (`VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`) n'est PAS bumpé par ce plan : un module neuf est une capacité neuve, donc une minor au sens de `CLAUDE.md` racine — mais le bump racine, le tag annoté et la release GitHub restent des gestes humains de ship, gatés (ADR-031). Le plan le NOMME comme dette explicite dans son SUMMARY plutôt que de le faire en douce."
    - "QUAL-01 (D-11) s'applique de plein droit : les trois agents passent `check-agents.sh --strict` par module ET en monde fermé (union de tous les `plugin/*/agents`), exactement dans la forme écrite au job `gates` de `.github/workflows/ci.yml` — les commandes du job sont rejouées, jamais une liste de gates recopiée depuis un rapport."
  artifacts:
    - "plugin/web-test-team/ : module neuf en `v1.0.0` — `module.json` (`requires: []`), `VERSION`, `agents/vf-web-test-orchestrator.md`, `agents/vf-web-test-runner.md`, `agents/vf-web-app-fixer.md`, `rules/web-verify-gate.md`, `references/test-loop-protocol.md`, `README.md` (avec sa ligne `**Version** : v1.0.0`), `CHANGELOG.md` (entrée `## v1.0.0 — <date>`)."
    - "README.md et README.fr.md racine : badge `modules-18` et phrase de compte à 18, re-dérivés par commande ; compteur de suites de tests re-mesuré (inchangé si ce module n'en ajoute aucune, mais re-mesuré et non supposé)."
    - "manual/en/03-modules/catalog.md et manual/fr/03-modules/catalogue.md : entrée `web-test-team` ajoutée, au même niveau de détail que les entrées voisines."
  key_links:
    - "`plugin/mobile-test-team/module.json` sans sa phrase de réserve (état posé par 34-04) <-> précondition de ce plan : c'est la forme MACHINE de « le moule est prouvé ». Une précondition narrative (« si le run a été vert ») serait invérifiable au moment de l'exécution ; celle-ci est un test sur disque."
    - "Champ `name` des trois agents <-> `check-agents.sh --resolve-agents=strict` en monde fermé : c'est le seul gate qui voit l'univers complet des agents du dépôt et donc la seule chose qui peut attraper une collision de nom entre deux modules. La vérification de la tâche 1 le rejoue dans la forme exacte du job CI (un `--agent-registry-dir=` par dossier d'agents découvert)."
    - "Nombre de `plugin/*/module.json` <-> badge et phrase de compte des DEUX README racine : `check-version-sync.sh` compare les trois. Un module posé sans mise à jour des compteurs rend la CI rouge — c'est le gate qui a été créé après la dérive de 2026-07-26, où le tableau README mentait sur 13 modules sans que rien ne le voie."
    - "Marqueurs de la rule path-scopée <-> projets non concernés : un glob trop large chargerait la doctrine de vérification web sur tous les projets web de l'utilisateur, testés ou non par un runner e2e. Le précédent mobile (v1.3.1) est la preuve que l'erreur est facile et que la correction coûte un patch."
  prohibitions:
    - "Ne JAMAIS créer le moindre fichier sous `plugin/web-test-team/` si `34-RUN-MOBILE.md` ne porte pas `> **Statut** : VERT` ou si `plugin/mobile-test-team/module.json` porte encore sa phrase de réserve — cloner un moule non prouvé est précisément ce que D-06 refuse."
    - "Ne JAMAIS réutiliser les `name` d'agents de `mobile-test-team` (`vf-test-orchestrator`, `vf-test-runner`, `vf-app-fixer`) : collision d'install et de monde fermé garantie."
    - "Ne JAMAIS donner un outil de délégation aux deux workers, ni leur permettre d'écrire hors de leur domaine : Pattern 12 est une condition d'existence de l'équipe, pas une préférence."
    - "Ne JAMAIS écrire un glob générique (`src/**`, `app/**/*.tsx`, `**/*.ts`) dans la rule path-scopée — l'erreur a déjà été commise côté mobile et corrigée en v1.3.1."
    - "Ne JAMAIS ajouter une dépendance de paquet au plugin : le runner web est une dépendance du lab de l'utilisateur. Aucune commande `npm install` / `pip install` / `cargo add` n'entre dans ce plan."
    - "Ne JAMAIS bumper le triplet RACINE ni créer un tag ou une release : gestes humains de ship (ADR-031, CLAUDE.md racine)."
    - "Ne JAMAIS recopier un compteur (modules, suites de tests) depuis un README, un rapport ou ce plan : chaque compteur est re-dérivé par commande au moment de l'exécution."
    - "Ne JAMAIS importer, copier ou adapter une persona du catalogue `agency-agents` : les trois agents sont des clones du moule VibeFlow interne déjà gouverné, rien d'autre."
---

<objective>
Si — et seulement si — le moule a été prouvé par le run de 34-01 et la sortie de statut de 34-04 :
construire `plugin/web-test-team`, l'équipe de test web autonome calquée fichier par fichier sur
`mobile-test-team`, avec ses trois agents renommés, sa rule path-scopée discriminante, son
référentiel de boucle, et le câblage des compteurs de catalogue que la CI vérifie.

Purpose : `web-test-team` est la piste n°1 de l'item BACKLOG du 2026-07-20 — elle comble un trou de
notre propre chaîne de dev (le seul test réel aujourd'hui est mobile). Le ROADMAP la conditionne
explicitement à la sortie d'expérimental de `mobile-test`, et D-06 refuse de réviser cette
condition. Ce plan est donc la récompense d'une preuve, jamais un pari.

Output : un module neuf en `v1.0.0` que la machine accepte (gates d'agents, monde fermé, cohérence
de version), et deux README racine qui disent la vérité sur le nombre de modules.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md
@plugin/mobile-test-team/module.json
@plugin/mobile-test-team/README.md
@plugin/mobile-test-team/agents/vf-test-orchestrator.md
@plugin/mobile-test-team/agents/vf-test-runner.md
@plugin/mobile-test-team/agents/vf-app-fixer.md
@plugin/mobile-test-team/rules/mobile-verify-gate.md
@plugin/mobile-test-team/references/test-loop-protocol.md
@CLAUDE.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes a rendu les trois exigences
`unresolved`/`unclassified`. Les hypothèses portées par CE plan sont déclarées ici, explicitement.

**AGTS-02 — hypothèse signalée (probe : `unresolved` / `unclassified`), volet « construction ».**
Ni le ROADMAP ni les décisions D-04 à D-09 ne tranchent trois points de construction. Ce plan
ASSUME, et l'écrit dans le README du module :

1. **Aucun module mécanique `web-test` séparé n'est créé.** `mobile-test-team` s'appuie sur un
   module mécanique frère parce que le pilotage d'un simulateur exige un script (boot, résolution
   de cible, build conditionnel). Un runner web moderne fait tout cela lui-même : créer un
   `web-test` mécanique serait un second système de pose pour rien (Pitfall 12). Les agents
   invoquent donc le runner du lab directement. `34-PATTERNS.md` désignait explicitement ce point
   comme « à trancher par le plan ».
2. **`web-test-team` naît SANS réserve de statut.** Son moule a été prouvé par un run réel ; sa
   réserve propre serait un statut hérité sans mesure. Le README dit en revanche ce qui n'a PAS été
   prouvé pour LUI : aucun run web réel n'a encore été joué, et la première mission web est la
   confirmation attendue. Point également désigné « à trancher par le plan » par la carte des
   patterns.
3. **Aucune suite de tests shell n'est ajoutée.** `mobile-test-team` n'en porte aucune (module
   `agents + rules`) ; le compteur de suites des README est néanmoins RE-MESURÉ, jamais supposé
   inchangé.

Si Samuel arbitre l'un de ces trois points autrement, c'est ce plan qui est amendé.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Tâche 1 : tranche verticale « module neuf accepté par la machine » — module.json, VERSION et les trois agents renommés qui passent le monde fermé</name>
  <files>plugin/web-test-team/module.json, plugin/web-test-team/VERSION, plugin/web-test-team/agents/vf-web-test-orchestrator.md, plugin/web-test-team/agents/vf-web-test-runner.md, plugin/web-test-team/agents/vf-web-app-fixer.md</files>
  <precondition>`34-RUN-MOBILE.md` porte `> **Statut** : VERT` ET `plugin/mobile-test-team/module.json` ne contient plus la chaîne `Statut expérimental jusqu` (état posé par le plan 34-04). Si l'une des deux conditions est fausse, ce plan entier est un no-op tracé : écrire pourquoi dans le SUMMARY et ne créer aucun fichier.</precondition>
  <read_first>
    - `plugin/mobile-test-team/agents/vf-test-orchestrator.md` EN ENTIER — garde « projet applicable », entrée attendue, boucle, halt conditions, bloc typé de sortie : c'est le moule, ligne par ligne
    - `plugin/mobile-test-team/agents/vf-test-runner.md` EN ENTIER — domaine d'action strict, règle anti-triche, absence d'outil de délégation
    - `plugin/mobile-test-team/agents/vf-app-fixer.md` EN ENTIER — domaine d'action strict, gate ADR-045 (pas de web côté worker applicatif, remontée d'une demande de recherche)
    - `plugin/mobile-test-team/module.json` — forme exacte du descripteur de module à cloner
    - `plugin/conductor/scripts/check-agents.sh` autour de la ligne 505-545 — les cinq champs requis (`name`, `description`, `model`, `memory`, `effort`) et l'avertissement sur le `name` différent du nom de fichier
    - `.github/workflows/ci.yml` lignes 252-325 — la forme EXACTE des trois étapes `check-agents.sh` du job `gates`, y compris l'étape en monde fermé avec un `--agent-registry-dir=` par dossier découvert : ce sont ces commandes qu'on rejoue, jamais une liste recopiée
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-PATTERNS.md` § « `plugin/web-test-team/*` — SI le run est vert » — extraits de frontmatter et de sections à cloner puis renommer
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RESEARCH.md` § « Pitfall 4 » — pourquoi le renommage doit être fait dès la première ébauche
    - `docs/ADR.md` ligne 52 (ADR-029) — le plafond de densité de 250 lignes par agent, à respecter dès l'écriture
  </read_first>
  <action>
    Poser le module minimal que la machine accepte, de bout en bout — descripteur, version, et les
    trois agents — et le prouver par les gates du job CI. Cinq gestes.

    (1) `plugin/web-test-team/VERSION` : `v1.0.0`.

    (2) `plugin/web-test-team/module.json` : cloner la forme de `mobile-test-team/module.json` avec
    `"name": "web-test-team"`, `"version": "v1.0.0"`, `"type": "agents + rules"`, une description
    sur le même gabarit (rôle de l'équipe + cloisonnement Pattern 12 + ce qui reste à confirmer),
    et `"requires": []` — le runner web est une dépendance du lab, jamais du plugin.

    (3) **Les trois agents, renommés dès la première ébauche** — fichier ET champ `name` :
    `vf-web-test-orchestrator`, `vf-web-test-runner`, `vf-web-app-fixer`. Dans l'orchestrateur, la
    liste d'agents délégables cite les DEUX nouveaux noms, jamais les anciens. Adapter, et rien
    d'autre : la garde « projet applicable » passe de marqueurs mobile à des marqueurs web
    discriminants (fichier de configuration du runner e2e à la racine du lab, dépendance
    correspondante dans le `package.json`, dossier de specs) ; le domaine d'écriture du worker de
    test passe du dossier de flows au dossier de specs du runner ; le domaine du worker applicatif
    reste le code de l'app. Le cloisonnement, la règle anti-triche, le gate ADR-045 et la forme du
    bloc typé de sortie sont repris VERBATIM — ce sont eux qui font l'équipe, pas le domaine.

    (4) **Les cinq champs requis sur chacun des trois agents** (`name`, `description`, `model`,
    `memory`, `effort`), plus `vf-internal: true` sur les DEUX workers et pas sur l'orchestrateur.
    Aucun agent ne dépasse 250 lignes (ADR-029).

    (5) **Fermer la tranche par les gates du job CI**, dans leur forme exacte : `check-agents.sh
    --strict --agents-dir=plugin/web-test-team/agents`, puis le run en MONDE FERMÉ avec
    `--resolve-agents=strict` et un `--agent-registry-dir=` par dossier `plugin/*/agents` découvert
    — c'est le seul run qui peut attraper une collision de `name` entre deux modules, et c'est la
    raison d'être du renommage. Rejouer les commandes du job, pas une liste lue dans un rapport.
  </action>
  <behavior>
    - Les trois fichiers d'agents portent les trois nouveaux `name`, et aucun des trois anciens
      `name` n'apparaît dans un champ `name` du nouveau module.
    - L'orchestrateur ne délègue qu'aux deux nouveaux workers.
    - Les deux workers portent `vf-internal: true` et n'ont aucun outil de délégation.
    - `check-agents.sh --strict` passe sur le dossier du module ET en monde fermé sur l'union.
    - Aucun agent ne dépasse 250 lignes.
  </behavior>
  <acceptance_criteria>
    - `plugin/web-test-team/VERSION` contient exactement `v1.0.0` et `plugin/web-test-team/module.json` contient `"version": "v1.0.0"` et `"requires": []`.
    - Les trois fichiers `plugin/web-test-team/agents/vf-web-*.md` existent et leur champ `name` vaut respectivement `vf-web-test-orchestrator`, `vf-web-test-runner`, `vf-web-app-fixer`.
    - Aucun fichier de `plugin/web-test-team/agents/` ne déclare un `name` égal à `vf-test-orchestrator`, `vf-test-runner` ou `vf-app-fixer`.
    - `plugin/web-test-team/agents/vf-web-test-runner.md` et `plugin/web-test-team/agents/vf-web-app-fixer.md` contiennent chacun `vf-internal: true`.
    - Chacun des trois fichiers d'agents fait au plus 250 lignes.
    - `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/web-test-team/agents` rend 0.
    - Le run en monde fermé (`--strict --resolve-agents=strict` avec un `--agent-registry-dir=` par dossier `plugin/*/agents`) rend 0 sur le dossier du nouveau module.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(cat plugin/web-test-team/VERSION)" = "v1.0.0" ] && grep -qF '"version": "v1.0.0"' plugin/web-test-team/module.json && grep -qF '"requires": []' plugin/web-test-team/module.json && grep -q '^name: vf-web-test-orchestrator$' plugin/web-test-team/agents/vf-web-test-orchestrator.md && grep -q '^name: vf-web-test-runner$' plugin/web-test-team/agents/vf-web-test-runner.md && grep -q '^name: vf-web-app-fixer$' plugin/web-test-team/agents/vf-web-app-fixer.md && grep -q '^vf-internal: true$' plugin/web-test-team/agents/vf-web-test-runner.md && grep -q '^vf-internal: true$' plugin/web-test-team/agents/vf-web-app-fixer.md && node -e 'const fs=require("fs");const d="plugin/web-test-team/agents";const interdits=["vf-test-orchestrator","vf-test-runner","vf-app-fixer"];const trop=[],coll=[];for(const f of fs.readdirSync(d)){const s=fs.readFileSync(d+"/"+f,"utf8");const n=s.split("\n").length;if(n>250)trop.push(f+":"+n);const m=s.match(/^name: (.+)$/m);if(m&&interdits.includes(m[1].trim()))coll.push(f);}if(coll.length){console.log("COLLISION DE NOM: "+coll.join(","));process.exit(1);}if(trop.length){console.log("DENSITE ADR-029 DEPASSEE: "+trop.join(","));process.exit(1);}' && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/web-test-team/agents && bash -c 'args=(); for d in plugin/*/agents; do [ -d "$d" ] || continue; args+=("--agent-registry-dir=$d"); done; bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --agents-dir=plugin/web-test-team/agents "${args[@]}"'</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `COLLISION DE NOM: <fichiers>` / `DENSITE ADR-029 DEPASSEE: <fichier:lignes>` apparaît. Causes discriminantes : `VERSION` ou `module.json` ne portent pas `v1.0.0`, ou `requires` n'est pas vide (une dépendance de plugin aurait été déclarée pour ce qui est une dépendance de lab) ; un `name` d'agent manque ou n'a pas été renommé ; un worker ne porte pas `vf-internal: true` (il deviendrait invocable directement, Pattern 12 rompu) ; un agent reprend un `name` de `mobile-test-team` (collision d'install et de monde fermé) ; un agent dépasse 250 lignes (ADR-029) ; `check-agents.sh --strict` rend non-zéro sur le module ; le run en monde fermé rend non-zéro — c'est le seul gate qui voit l'univers complet des agents et donc le seul capable de détecter une ambiguïté de nom inter-modules.</fails_when>
  </verify>
  <done>Le module neuf existe dans sa forme minimale et la machine l'accepte : trois agents renommés, cloisonnés, sous le plafond de densité, verts en monde fermé.</done>
  <reversibility rating="costly">Créer un module distribué engage un contrat de nom (les `name` des trois agents deviennent des identifiants posés dans les labs des utilisateurs). Le renommer plus tard demanderait une migration de pose et un passage de dépréciation — d'où le renommage fait dès la première ébauche plutôt qu'après coup (Pitfall 4).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : compléter le module — rule path-scopée discriminante, référentiel de boucle, README et CHANGELOG</name>
  <files>plugin/web-test-team/rules/web-verify-gate.md, plugin/web-test-team/references/test-loop-protocol.md, plugin/web-test-team/README.md, plugin/web-test-team/CHANGELOG.md</files>
  <precondition>La tâche 1 est verte : les trois agents passent `check-agents.sh --strict` par module et en monde fermé.</precondition>
  <read_first>
    - `plugin/mobile-test-team/rules/mobile-verify-gate.md` EN ENTIER — frontmatter `paths:` et corps de la rule
    - `plugin/mobile-test-team/CHANGELOG.md` entrée v1.3.1 — le correctif qui a RETIRÉ les globs génériques parce qu'ils chargeaient la rule sur tout projet Next.js : la raison pour laquelle les marqueurs doivent être discriminants
    - `plugin/mobile-test-team/references/test-loop-protocol.md` EN ENTIER — invariants de boucle, budgets, mapping des halt conditions, insertion dans le flux autonome : réutilisable quasi tel quel
    - `plugin/mobile-test-team/README.md` EN ENTIER — structure exacte (tagline, Quoi, Installation, Démarrer, Usage, tableau de référence des agents avec leurs outils, Limites) à reprendre section par section
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md` — la preuve du moule, à citer dans le CHANGELOG de naissance
    - `scripts/check-version-sync.sh` §8 — pourquoi la ligne `**Version**` du README de module doit exister et être alignée
  </read_first>
  <action>
    Compléter le module sur les quatre fichiers restants, en reprenant le moule et en corrigeant
    une erreur connue plutôt qu'en la rejouant. Quatre gestes.

    (1) `rules/web-verify-gate.md` : cloner la rule mobile, en remplaçant le frontmatter `paths:`
    par des marqueurs DISCRIMINANTS d'un projet réellement testé par un runner e2e web — le fichier
    de configuration du runner à la racine, et le dossier de specs e2e. Ne PAS écrire de glob
    générique de type `src/**`, `app/**/*.tsx` ou `**/*.ts` : c'est exactement ce qui a été retiré
    côté mobile en v1.3.1 parce que la rule se chargeait sur tout projet web non concerné. Le corps
    de la rule invoque la doctrine de vérification réelle, comme son homologue.

    (2) `references/test-loop-protocol.md` : reprendre le référentiel mobile quasi tel quel
    (invariants, budgets, mapping des halt conditions, insertion dans le flux autonome) ; seule
    change la ligne qui nomme le pipeline mécanique — ici, il n'y en a pas de module séparé, les
    agents invoquent le runner du lab directement (hypothèse 1 des hypothèses signalées, à écrire
    dans le fichier, pas seulement dans ce plan).

    (3) `README.md` du module : reprendre la structure de `mobile-test-team/README.md` section par
    section, avec la ligne d'en-tête `**Type** : agents + rules · **Version** : v1.0.0 · **Dépend
    de** : aucun module`. Le bloc `## Limites` dit ce qui est vrai pour CE module : le moule a été
    prouvé côté mobile (renvoi à `34-RUN-MOBILE.md`) mais AUCUN run web réel n'a encore été joué —
    la première mission web est la confirmation attendue. Dire aussi que le runner e2e est une
    dépendance du lab, jamais du plugin, et reprendre la limite honnête déjà présente côté mobile
    (l'allowlist de délégation est une heuristique robuste, pas un mur).

    (4) `CHANGELOG.md` : première entrée `## v1.0.0 — <date>` disant la naissance du module, la
    Phase 34 et AGTS-02 comme origine, le moule prouvé par le run mobile (renvoi nommé à
    `34-RUN-MOBILE.md`), les trois agents cloisonnés avec leurs noms, la rule path-scopée et son
    choix de marqueurs discriminants, et le référentiel réutilisé.
  </action>
  <behavior>
    - Le frontmatter `paths:` de la rule ne contient aucun glob générique de type `src/**`,
      `app/**` ou `**/*.ts`.
    - Le README du module porte une ligne `**Version** : v1.0.0` conforme au §8 du gate de version.
    - Le README dit explicitement qu'aucun run web réel n'a encore été joué.
    - Le CHANGELOG porte une entrée `## v1.0.0 — ` renvoyant nommément à la trace du run mobile.
  </behavior>
  <acceptance_criteria>
    - Les quatre fichiers existent.
    - `plugin/web-test-team/rules/web-verify-gate.md` a un frontmatter `paths:` dont AUCUNE entrée n'est `src/**`, `app/**`, `**/*.ts`, `**/*.tsx` ni un autre glob qui matcherait un projet web quelconque sans marqueur de runner e2e.
    - `plugin/web-test-team/README.md` contient `**Version** : v1.0.0` et la chaîne `34-RUN-MOBILE.md`.
    - `plugin/web-test-team/README.md` contient une phrase disant qu'aucun run web réel n'a encore été joué.
    - `plugin/web-test-team/CHANGELOG.md` contient une ligne commençant par `## v1.0.0 — `.
    - `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/web-test-team/agents` rend toujours 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f plugin/web-test-team/rules/web-verify-gate.md && test -f plugin/web-test-team/references/test-loop-protocol.md && test -f plugin/web-test-team/README.md && test -f plugin/web-test-team/CHANGELOG.md && grep -qF '**Version** : v1.0.0' plugin/web-test-team/README.md && grep -qF '34-RUN-MOBILE.md' plugin/web-test-team/README.md && grep -q '^## v1\.0\.0 — ' plugin/web-test-team/CHANGELOG.md && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/web-test-team/agents && node -e 'const fs=require("fs");const s=fs.readFileSync("plugin/web-test-team/rules/web-verify-gate.md","utf8");const m=s.match(/^---\n([\s\S]*?)\n---/);if(!m){console.log("RULE SANS FRONTMATTER");process.exit(1);}const globs=m[1].split("\n").filter(l=>/^\s*-\s+/.test(l)).map(l=>l.replace(/^\s*-\s+/,"").replace(/^["\x27]|["\x27]$/g,"").trim());if(!globs.length){console.log("RULE SANS PATHS");process.exit(1);}const larges=globs.filter(g=>/^(src|app|components|lib|pages)\/\*\*/.test(g)||/^\*\*\/\*\.(ts|tsx|js|jsx)$/.test(g));if(larges.length){console.log("GLOB GENERIQUE: "+larges.join(" "));process.exit(1);}'</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `RULE SANS FRONTMATTER`, `RULE SANS PATHS`, `GLOB GENERIQUE: <globs>` apparaît. Causes discriminantes : un des quatre fichiers manque ; le README du module n'a pas sa ligne de version alignée (le §8 de `check-version-sync.sh` rougirait) ou ne renvoie pas à la trace qui prouve le moule ; le CHANGELOG n'a pas son entrée de naissance ; la rule n'a pas de frontmatter ou pas de liste `paths:` (elle se chargerait partout ou nulle part) ; au moins un glob est assez large pour charger la rule sur un projet web sans runner e2e — l'erreur exacte corrigée côté mobile en v1.3.1, que ce gate existe pour ne pas rejouer ; `check-agents.sh --strict` a cessé de passer.</fails_when>
  </verify>
  <done>Le module est complet et cohérent : rule discriminante, référentiel de boucle, README qui dit ce qui est prouvé et ce qui ne l'est pas, CHANGELOG de naissance.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : câbler le catalogue — compteurs des deux README racine re-dérivés, entrée au manuel, gates du job CI rejoués</name>
  <files>README.md, README.fr.md, manual/en/03-modules/catalog.md, manual/fr/03-modules/catalogue.md</files>
  <precondition>La tâche 2 est verte : le module est complet sous `plugin/web-test-team/`.</precondition>
  <read_first>
    - `scripts/check-version-sync.sh` en entier — les quatre points qu'il compare pour un module neuf : badge `modules-N` des DEUX README, phrase « N modules … » des DEUX README (deux formulations différentes, une par langue), triade par module, et ligne `**Version**` du README de module
    - `README.md` autour des lignes 14-20 (badges) et 124-128 (phrase de compte) — les deux zones à mettre à jour
    - `README.fr.md` autour des lignes 14-20 (badges) et 129-133 (phrase de compte) — idem côté français, avec la formulation propre à cette langue
    - `manual/en/03-modules/catalog.md` et `manual/fr/03-modules/catalogue.md` dans leur état de fin de plan 34-04 — le voisinage des entrées `mobile-test*`, où l'entrée `web-test-team` vient se placer, au même niveau de détail
    - `.github/workflows/ci.yml` job `gates` en entier (lignes 245-370) — la liste RÉELLE des gates à rejouer : les trois étapes `check-agents.sh`, `check-version-sync.sh`, `check-state-integrity.sh`, `check-capability-activation.sh`, `check-machine-paths.sh`. Rejouer ces commandes-là, jamais une liste recopiée depuis un rapport
  </read_first>
  <action>
    Rendre le catalogue honnête et prouver que le dépôt entier reste vert. Quatre gestes.

    (1) **Re-dériver le compte de modules par commande** — le nombre de fichiers
    `plugin/*/module.json` sur disque — et écrire CETTE valeur dans les quatre points que le gate
    compare : le badge `modules-N` de `README.md`, le badge `modules-N` de `README.fr.md`, la
    phrase de compte anglaise de `README.md` et la phrase de compte française de `README.fr.md`
    (les deux formulations diffèrent — ne pas transposer l'une sur l'autre, le gate cherche la
    formulation propre à chaque langue). Ne jamais écrire un nombre lu dans ce plan : le re-dériver.

    (2) **Re-mesurer le compteur de suites de tests** des deux README par la même méthode (compte
    des fichiers de suite sous les dossiers de tests). Ce module n'ajoute aucune suite, donc le
    compte ne devrait pas bouger — mais il est RE-MESURÉ et non supposé : c'est la règle de ce
    dépôt sur tous les compteurs de README. Si l'écart existe, corriger ; s'il n'existe pas,
    consigner la mesure au SUMMARY.

    (3) **Ajouter l'entrée `web-test-team`** aux deux catalogues du manuel, au même niveau de
    détail que ses voisines : ce que fait l'équipe, son cloisonnement, le fait que le runner e2e est
    une dépendance du lab, et la limite honnête (aucun run web réel encore joué). Placer l'entrée à
    sa position alphabétique dans la liste, comme les autres.

    (4) **Rejouer les gates du job `gates` de la CI**, dans leur forme écrite dans
    `.github/workflows/ci.yml` — les trois étapes `check-agents.sh` (par dossier d'agents, par
    `AGENT.md`, et en monde fermé), `check-version-sync.sh`, `check-state-integrity.sh` avec son
    `--file` explicite, `check-capability-activation.sh`, et `check-machine-paths.sh`. Ce dernier
    est facile à oublier et a déjà fait rougir une PR de ce dépôt pour cette exacte raison :
    rejouer les commandes du job, jamais la liste d'un rapport.
  </action>
  <behavior>
    - Les quatre points de compte de modules portent la même valeur que le compte réel sur disque.
    - Le compteur de suites de tests des README correspond à une mesure fraîche.
    - Les deux catalogues du manuel contiennent une entrée `web-test-team`.
    - Tous les gates du job `gates` passent.
  </behavior>
  <acceptance_criteria>
    - Le badge `modules-N` de `README.md` et celui de `README.fr.md` portent la même valeur N que le nombre de fichiers `plugin/*/module.json` sur disque.
    - La phrase de compte de `README.md` (formulation anglaise) et celle de `README.fr.md` (formulation française) portent cette même valeur N.
    - `manual/en/03-modules/catalog.md` et `manual/fr/03-modules/catalogue.md` contiennent chacun la chaîne `web-test-team`.
    - `bash scripts/check-version-sync.sh` rend 0.
    - `bash scripts/check-machine-paths.sh` rend 0.
    - `bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md` rend 0.
    - `bash plugin/dev-orchestrator/scripts/check-capability-activation.sh` rend 0.
    - Les trois étapes `check-agents.sh` du job `gates`, rejouées dans leur forme, rendent 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -qF 'web-test-team' manual/en/03-modules/catalog.md && grep -qF 'web-test-team' manual/fr/03-modules/catalogue.md && bash scripts/check-version-sync.sh && bash scripts/check-machine-paths.sh && bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md && bash plugin/dev-orchestrator/scripts/check-capability-activation.sh && bash -c 'set -u; fail=0; for d in plugin/*/agents; do [ -d "$d" ] || continue; bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d" || fail=$((fail+1)); done; for f in plugin/*/AGENT.md; do [ -f "$f" ] || continue; bash plugin/conductor/scripts/check-agents.sh --strict --file "$f" || fail=$((fail+1)); done; reg=(); for d in plugin/*/agents; do [ -d "$d" ] || continue; reg+=("--agent-registry-dir=$d"); done; for d in plugin/*/agents; do [ -d "$d" ] || continue; bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --agents-dir="$d" "${reg[@]}" || fail=$((fail+1)); done; [ "$fail" -eq 0 ]' && P=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md && B=$(sed -n 's/^SHA de base : //p' "$P" | head -1) && test -n "$B" && git diff --quiet "$B" -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json</automated>
    <fails_when>sortie non nulle. Causes discriminantes : l'un des deux catalogues du manuel ne mentionne pas le module neuf (il serait distribué sans jamais apparaître au catalogue lu par l'utilisateur) ; `check-version-sync.sh` rend non-zéro — c'est le cas si l'un des quatre points de compte de modules est resté à l'ancienne valeur, ou si la triade par module du nouveau module diverge, et sa sortie nomme lequel ; `check-machine-paths.sh` rend non-zéro, c'est-à-dire qu'un chemin absolu de poste a été versionné (gate oublié qui a déjà fait rougir une PR de ce dépôt) ; `check-state-integrity.sh` ou `check-capability-activation.sh` rendent non-zéro ; l'une des trois étapes `check-agents.sh` échoue, notamment le monde fermé sur une collision de nom ; le diff du triplet RACINE contre le SHA de base de la phase (celui consigné dans `34-RUN-MOBILE.md` § `## Périmètre` — plage explicite, jamais `HEAD` nu, qui serait vide dès le premier commit) rend 1, c'est-à-dire qu'un bump racine a été fait dans ce plan alors que c'est un geste humain de ship.</fails_when>
    <human-check>Lire l'entrée `web-test-team` des deux catalogues comme un utilisateur : comprend-il ce que l'équipe fait, qu'elle a besoin d'un runner e2e déjà présent dans SON projet, et qu'aucun run web réel n'a encore été joué ? Le module tient-il la même promesse dans son README que dans le catalogue ?</human-check>
  </verify>
  <done>Le dépôt déclare 18 modules partout où il les compte, le nouveau module figure aux deux catalogues, et tous les gates du job `gates` de la CI passent quand on les rejoue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module neuf posé → labs des utilisateurs | `vibeflow-update.sh` place chaque agent dans le dossier d'agents du lab en le nommant par son champ `name` ; deux modules qui poseraient le même `name` se marcheraient dessus chez l'utilisateur, sans avertissement à l'install |
| Rule path-scopée → tous les projets de l'utilisateur | une rule à glob trop large se charge sur des projets qui n'ont rien demandé et injecte une doctrine hors sujet dans leur contexte |
| Workers cloisonnés (`vf-web-test-runner` / `vf-web-app-fixer`) → code applicatif du lab | deux agents qui écrivent dans le même dépôt avec des domaines censés être disjoints : c'est la disjonction qui empêche le worker de test de « corriger » l'app pour faire passer son propre test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-17 | Spoofing | collision de `name` entre les agents de `web-test-team` et ceux de `mobile-test-team` à la pose | high | mitigate | renommage des TROIS agents dès la première ébauche, fichier ET champ `name` ; vérification machine qu'aucun `name` interdit n'apparaît dans le nouveau module ; run `check-agents.sh --resolve-agents=strict` en MONDE FERMÉ (union de tous les dossiers d'agents) rejoué dans la forme exacte du job CI — le seul gate capable de voir l'univers complet et donc l'ambiguïté inter-modules |
| T-34-18 | Tampering | worker de test qui affaiblirait un assert, ou worker applicatif qui toucherait aux tests | high | mitigate | Pattern 12 repris verbatim : domaines d'écriture strictement disjoints, `vf-internal: true` sur les deux workers, aucun outil de délégation dans leur ligne `tools:` (vérifié par machine), règle anti-triche reprise mot pour mot du moule prouvé |
| T-34-19 | Denial of Service | rule path-scopée à glob trop large, chargée sur tous les projets web de l'utilisateur | medium | mitigate | interdiction explicite des globs génériques en prohibition de plan, et vérification machine qui échoue en NOMMANT le glob fautif ; précédent documenté (correctif v1.3.1 côté mobile) cité dans le README du module |
| T-34-20 | Spoofing | catalogue et compteurs du dépôt déclarant un parc différent du parc réel | medium | mitigate | les quatre points de compte sont re-dérivés par commande et comparés au disque par `check-version-sync.sh`, rejoué dans la vérification ; le compteur de suites est re-mesuré même quand il ne bouge pas ; l'entrée de catalogue est exigée dans les deux langues |
| T-34-21 | Elevation of Privilege | agents posés avec un outillage plus large que nécessaire | medium | mitigate | les lignes `tools:` sont clonées du moule prouvé, pas réinventées ; `check-agents.sh --strict` exige les cinq champs de gouvernance ; l'orchestrateur est le seul à porter une capacité de délégation, limitée aux deux workers du module |

Aucun install de paquet (`npm`/`pip`/`cargo`) n'entre dans ce plan : le runner e2e web est une
dépendance du LAB de l'utilisateur, jamais du plugin, et `module.json` déclare `requires: []`.
Aucune menace `T-34-SC` n'est ouverte et aucun checkpoint de légitimité de paquet n'est requis —
la section « Package Legitimacy Audit » de `34-RESEARCH.md` est déclarée non applicable à la phase
pour cette raison précise.
</threat_model>

<verification>
1. Si la précondition du plan est fausse (chapeau non VERT, ou réserve de statut encore présente dans `plugin/mobile-test-team/module.json`), AUCUN fichier n'existe sous `plugin/web-test-team/` et le SUMMARY écrit pourquoi — no-op tracé.
2. Sinon : les neuf fichiers du module existent, `VERSION` et `module.json` portent `v1.0.0`, `requires` est vide.
3. Les trois agents portent les trois nouveaux `name`, aucun ne reprend un `name` de `mobile-test-team`, les deux workers portent `vf-internal: true`, et aucun fichier ne dépasse 250 lignes.
4. Le frontmatter `paths:` de la rule ne contient aucun glob générique — la vérification nomme le glob fautif s'il existe.
5. Le badge et la phrase de compte des DEUX README racine portent la valeur re-dérivée du disque ; le compteur de suites de tests a été re-mesuré et consigné.
6. Les deux catalogues du manuel contiennent une entrée `web-test-team`.
7. Les gates du job `gates` de `.github/workflows/ci.yml` sont rejoués dans leur forme (trois étapes `check-agents.sh`, `check-version-sync.sh`, `check-state-integrity.sh --file`, `check-capability-activation.sh`, `check-machine-paths.sh`) et rendent tous 0.
8. `git diff --quiet <SHA de base de la phase> -- VERSION plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json` rend 0 — le triplet racine est intact, le bump reste un geste humain.
9. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-05-SUMMARY.md` existe et consigne les compteurs re-dérivés et la dette de bump racine.
</verification>

<success_criteria>
- `web-test-team` existe parce qu'un moule a été PROUVÉ, jamais parce qu'on avait envie de le cloner (D-06).
- Aucune collision de nom avec `mobile-test-team` n'est possible, et c'est le monde fermé qui le prouve, pas une relecture.
- Le cloisonnement Pattern 12 est intact : deux workers internes, domaines disjoints, aucune délégation.
- La rule ne se charge que sur un projet réellement équipé d'un runner e2e — l'erreur mobile de v1.3.1 n'est pas rejouée.
- Le dépôt déclare partout le même nombre de modules que celui qu'il porte, sur des compteurs re-dérivés et non recopiés.
- Le module dit honnêtement ce qui n'a pas encore été prouvé POUR LUI : aucun run web réel.
- Le triplet racine reste intact et la dette de bump est nommée dans le SUMMARY, pas contournée.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| Module `web-test-team` v1.0.0 | NOUVEAU module distribué | `plugin/web-test-team/` |
| `module.json`, `VERSION` | NOUVEAUX | `plugin/web-test-team/module.json`, `plugin/web-test-team/VERSION` |
| Agent `vf-web-test-orchestrator` | NOUVEL agent distribué (orchestrateur) | `plugin/web-test-team/agents/vf-web-test-orchestrator.md` |
| Agent `vf-web-test-runner` | NOUVEL agent distribué (worker interne, Pattern 12) | `plugin/web-test-team/agents/vf-web-test-runner.md` |
| Agent `vf-web-app-fixer` | NOUVEL agent distribué (worker interne, Pattern 12) | `plugin/web-test-team/agents/vf-web-app-fixer.md` |
| Rule `web-verify-gate` | NOUVELLE rule path-scopée | `plugin/web-test-team/rules/web-verify-gate.md` |
| Référentiel `test-loop-protocol` | NOUVEAU (clone adapté) | `plugin/web-test-team/references/test-loop-protocol.md` |
| `README.md`, `CHANGELOG.md` du module | NOUVEAUX | `plugin/web-test-team/` |
| Compteurs de modules des deux README racine | édition | `README.md`, `README.fr.md` |
| Entrées de catalogue | édition | `manual/en/03-modules/catalog.md`, `manual/fr/03-modules/catalogue.md` |
| `34-05-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-05-SUMMARY.md` |

Trois agents ajoutés au catalogue dans une seule PR : c'est le maximum toléré par la discipline
Pitfall 12 (« plus de 2-3 agents **ajoutés au catalogue** dans une PR » est le signal d'alarme), et
ils forment UNE équipe indissociable clonée d'un moule déjà gouverné — pas trois personas
indépendantes importées d'un catalogue tiers.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-05-SUMMARY.md` en fin
de plan, consignant : la valeur re-dérivée du compte de modules et la commande qui l'a produite, la
mesure fraîche du compteur de suites de tests, la sortie de chacun des gates du job `gates`
rejoués, les trois `name` d'agents posés, les globs retenus pour la rule path-scopée et pourquoi ils
sont discriminants, la dette explicite de bump du triplet RACINE laissée au geste humain de ship, et
— si no-op — la cause et la date.
</output>

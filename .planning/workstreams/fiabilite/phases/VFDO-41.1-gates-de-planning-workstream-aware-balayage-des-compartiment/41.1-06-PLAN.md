---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 06
lot: L06 — CI : fan-out check-state-integrity/R5 sur chaque compartiment, fixture à 3 compartiments dont un cassé (D-04, D-05, D-06)
type: execute
wave: 3
depends_on: ["41.1-01", "41.1-02"]
autonomous: true
requirements: [WSAW-02, WSAW-03, WSAW-04, WSAW-05]
files_modified:
  - .github/workflows/ci.yml

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `.github/workflows/ci.yml` porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh`) — ce plan modifie deux régions du job `gates` (le fan-out `check-state-integrity`/R5 et la fixture `check-divergence.sh en CI`), chacune peut être couverte par le même trailer si le motif couvre les deux, ou par deux trailers distincts"
    - "L'étape `check-state-integrity (anti-régression du frontmatter .planning/STATE.md, ADR-063)` (aujourd'hui `ci.yml:353`, invocation UNIQUE en dur sur `.planning/workstreams/fiabilite/STATE.md`) ENUMÈRE désormais les compartiments présents via `vf_ws_enumerate .planning` et invoque `check-state-integrity.sh --file <compartiment>/STATE.md` UNE FOIS PAR COMPARTIMENT — le script `check-state-integrity.sh` LUI-MÊME reste inchangé (a2, single-target par conception, D-04) ; c'est L'APPELANT CI qui devient a1 par fan-out"
    - "Le verdict R5 (aujourd'hui `ci.yml:844`, même invocation en dur) subit le MÊME fan-out, sur le CHECKOUT RÉEL de ce job (pas une fixture) — les DEUX compartiments réels de ce dépôt (`fiabilite`, `gouvernance`) sont chacun mesurés"
    - "WSAW-05/D-06 — `gouvernance/STATE.md` (non-initialisé au sens D-02) ne doit JAMAIS faire échouer le fan-out R5. CORRECTION post plan-checker (BLOCK-1) : la classification ne dépend PLUS d'un `grep` sur la sortie de `check-state-integrity.sh` (ce script n'émet jamais de sous-chaîne « non initialis » — mesuré, il tombe en l.240 sur `gouvernance/STATE.md`, exit 2 « milestone introuvable ou illisible »). La fonction `is_non_init` lit DIRECTEMENT et INCONDITIONNELLEMENT le frontmatter du compartiment AVANT tout appel à `check-state-integrity.sh` : exactement deux clés `workstream:`+`created:` → non-initialisé, consigné (verdict nommé), jamais compté dans `fail`. Tout le reste (y compris un compartiment CORROMPU, troisième état D-02) passe par `check-state-integrity.sh` et continue de compter comme écart si son rc n'est pas 0"
    - "QUAL-01 — le fan-out `check-state-integrity`/R5 est un gate MODIFIÉ : il porte sa PROPRE mutation rouge EXÉCUTÉE, sur une fixture jetable (`mktemp -d`+`git init`) à deux compartiments `sain`/`casse` où `casse` est délibérément CORROMPU (frontmatter à 3 clés dont `milestone:` vide — ni nominal minimal ni structurellement sain), AVANT toute mesure du checkout réel : 1) fixture construite → fan-out attendu rc=1 (casse nommé) · 2) `casse` réparé en place (frontmatter complet) → attendu rc=0. Exécutée dans LES DEUX éditions (check-state-integrity ET R5), chacune redéfinissant ses propres fonctions (deux process shell distincts) — patron `check-instruction-budget` déjà en usage dans ce fichier. Sans cette fixture, les deux branches réelles (`fiabilite` conforme, `gouvernance` non-initialisé) sont toutes deux vertes et aucune assertion ne peut rougir (finding BLOCK-2 du juge frais)"
    - "Fixture WSAW-03/D-05 : l'étape `check-divergence.sh en CI` (`ci.yml:862+`) est ÉTENDUE de son unique compartiment `alpha` à TROIS compartiments dans la MÊME fixture — `alpha` (conforme, existant, inchangé), un DEUXIÈME compartiment conforme neuf, et un TROISIÈME délibérément cassé (S2 : deux dossiers de phase partageant le même préfixe numérique, patron déjà utilisé par la mutation existante de `alpha`, mais dans un compartiment séparé plutôt que muté sur `alpha` lui-même)"
    - "La bascule tourne DANS CETTE ÉTAPE CI ELLE-MÊME (pas seulement dans une suite de module) : 1) fixture à 3 compartiments dont le 3e cassé dès sa CONSTRUCTION → `check-divergence.sh` attendu rc=1 · 2) MÊME fixture, 3e compartiment RÉPARÉ EN PLACE (le second dossier de phase dupliqué retiré) → attendu rc=0. C'est le rendu explicite du critère de succès 3 du ROADMAP (« la CI voit un compartiment fautif ... sur fixture jetable à trois compartiments »)"
    - "WSAW-04 — preuve qu'ajouter un compartiment suffit SANS câblage supplémentaire : le troisième compartiment de la fixture n'est PAS nommé nulle part ailleurs dans `ci.yml` (ni dans une liste de compartiments à vérifier, ni dans une condition) — sa seule existence sur le disque de la fixture suffit à ce que `vf_ws_enumerate`/`check-divergence.sh` le voie, ce qui EST la preuve, pas une assertion supplémentaire à écrire"
    - "Aucune étape CI de ce plan n'utilise `|| true` sur l'invocation d'un gate — patron F13 déjà en usage dans ce fichier (découverte non vide + `fail`/`note` + `exit 1` en fin d'étape)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "fan-out check-state-integrity/R5 par compartiment énuméré ; fixture check-divergence à 3 compartiments dont un cassé, bascule rouge/vert dans l'étape"
      contains: "vf_ws_enumerate"
  key_links:
    - "Édition 1 : `ci.yml` ligne ~353 (`run: bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/workstreams/fiabilite/STATE.md`) — remplacer par une boucle sur `vf_ws_enumerate .planning`"
    - "Édition 2 : `ci.yml` ligne ~844 (bloc R5, même invocation en dur) — même fan-out, sur les compartiments réels"
    - "Édition 3 : `ci.yml` lignes ~862-929 (étape `check-divergence.sh en CI`) — fixture `alpha` existante étendue à trois compartiments dans le MÊME `$FIX`, patron `mkdir -p`/`printf`/`git add`/`git commit` déjà en place pour `alpha`"
    - "CORRIGÉ (2e passe, BLOCK-A) : `check-state-integrity.sh` accepte `--file` explicite (déjà le cas) MAIS attend `--file` RELATIF À `--path` (la racine du lab, PAS `.planning` elle-même) — le fan-out passe TOUJOURS `--path <lab_root_abs>` ET `--file <compartiment>/STATE.md` RELATIF ENSEMBLE, jamais l'un sans l'autre, jamais un `--file` absolu (mesuré : un `--file` absolu sans `--path` cohérent fait sauter `HAVE_BASELINE` en silence, BLOCK-A). Ancienne formulation « jamais `--path` seul » de la 1re passe était incomplète — ce n'est pas `--path` seul qu'il faut éviter, c'est l'incohérence entre les deux"
---

<objective>
Faire cesser les DEUX câblages CI en dur sur `fiabilite` (`check-state-integrity`, R5) en les
faisant fan-out sur chaque compartiment énuméré par `vf_ws_enumerate`, et étendre la fixture jetable
de `check-divergence.sh en CI` à trois compartiments dont un cassé, avec bascule rouge/vert
PROUVÉE DANS L'ÉTAPE ELLE-MÊME. C'est le cœur de WSAW-02/03/04/05 côté CI : le point où un
compartiment neuf, non nommé nulle part dans `ci.yml`, doit se faire inspecter par sa seule
présence sur le disque.

Purpose: fermer les deux derniers points de câblage en dur nommés par le ROADMAP (`ci.yml:353`,
`ci.yml:844`), et produire la preuve par mutation exigée par le Success Criterion 3/4 du ROADMAP.
Output: deux étapes CI fan-out, une fixture à 3 compartiments dont un cassé, tout prouvé rouge puis
vert dans l'étape même.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `.github/workflows/ci.yml` lignes ~340-360 (étape `check-state-integrity`), ~820-850 (bloc R4/R5),
  ~862-929 (étape `check-divergence.sh en CI` — fixture `alpha` complète à recopier)
- `plugin/planning-core/scripts/workstream-policy.sh` (après plan 41.1-01) — signature et codes de
  `vf_ws_enumerate`
- `plugin/conductor/scripts/check-state-integrity.sh` en-tête (codes de sortie, contrat `--file`)
</context>

<discipline>
- Aucune commande git en ligne — le travail git de fixture vit DANS les blocs `run:` déjà écrits
  en YAML (`ci.yml` est le seul endroit où git tourne pour ce plan, comme pour les étapes voisines
  déjà en place — ce n'est PAS une violation de la règle "aucune commande git en ligne dans un
  `<verify>`", qui s'applique aux `<verify>` de CE plan, pas au contenu YAML qu'ils vérifient).
- `diff`/`ls` proxifiés — `rtk proxy` ; comparaisons `cmp`/`comm`/`cksum` dans les `<verify>`.
- Ne jamais muter le checkout réel du dépôt dans une étape CI permanente (motif déjà écrit en
  commentaire à `ci.yml:830` — la mutation de la fixture `check-divergence` reste dans `$FIX`,
  jamais sur `.`).
- `set -eu` en tête de chaque bloc `run:` modifié, patron déjà en place dans ce fichier.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : fan-out `check-state-integrity` et R5 sur chaque compartiment énuméré</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - sur le checkout réel (2 compartiments, `fiabilite` conforme, `gouvernance` non-initialisé) :
      l'étape `check-state-integrity` mesure LES DEUX, rc=0 attendu sur `fiabilite`, verdict
      NON-BLOQUANT nommé sur `gouvernance` (non-initialisé, D-06 — ne compte pas dans `fail`).
    - le bloc R5 fait de même, sur les mêmes deux compartiments.
    - aucun des deux blocs ne cite `fiabilite` en dur dans le chemin invoqué — le nom du
      compartiment vient de `vf_ws_enumerate`, jamais d'un littéral.
  </behavior>
  <action>
**Correction post plan-checker (juge frais, DEUX passes — la première a introduit deux bloquants,
corrigés ici et PROUVÉS PAR EXÉCUTION RÉELLE hors du dépôt, pas seulement décrits) — lire avant
d'écrire quoi que ce soit :**

1. **BLOCK-1 (1re passe)** — la classification « non initialisé » ne peut PAS dépendre d'un `grep`
   sur la sortie de `check-state-integrity.sh` : mesuré, ce script n'émet JAMAIS la sous-chaîne
   `non initialis`. La classification se fait par LECTURE DIRECTE du frontmatter, INCONDITIONNELLE,
   AVANT tout appel au script — `is_non_init()` ci-dessous, INCHANGÉE depuis la 1re correction (le
   juge frais l'a validée : « exécutée sur les fichiers réels → gouvernance classé NON-INIT,
   fiabilite non »).
2. **BLOCK-2 (1re passe)** — QUAL-01 exige une mutation ROUGE EXÉCUTÉE. Une bascule fixture est
   nécessaire, PAS seulement le checkout réel (les deux branches y sont vertes).
3. **BLOCK-A (2e passe, sur ma 1re correction de BLOCK-2)** — ma fixture de mutation ne
   discriminait PAS : `rel="${wsdir#"$PWD"/}"` ne dépouille rien pour une fixture sous `$T` (hors
   du `$PWD` du job), le `statefile` passé à `check-state-integrity.sh --file` restait donc un
   chemin ABSOLU alors qu'aucun `--path` n'était fourni (`ROOT="."` par défaut) → le script fait
   `git show HEAD:<chemin absolu>`, échoue, `HAVE_BASELINE=0`, **saute l'Invariant 1 en silence** —
   ne reste que l'Invariant 2 (« 1 ligne `^Phase:` »), que `casse` satisfaisait. **Reproduit et
   confirmé RÉELLEMENT sur ce poste** (voir mesure ci-dessous) : `check-state-integrity.sh --file
   "$T/.planning/workstreams/casse/STATE.md"` (chemin absolu, sans `--path`) rend **rc=0** sur un
   compartiment `casse` délibérément corrompu — le bug est réel, pas hypothétique. C'est un
   **fail-open silencieux du gate lui-même** (n'importe quel appelant qui passe un chemin absolu à
   `--file` sans `--path` est affecté, pas seulement ce plan) — mais **fermer le fail-open du gate
   est hors périmètre de ce plan sans feu vert séparé** ; ce plan corrige SES PROPRES invocations
   (`--path <lab_root>` + `--file` RELATIF à ce `<lab_root>`), prouvées par exécution ci-dessous.
4. **BLOCK-B (2e passe)** — `fanout_check_state_integrity` définissait `fail`/`note()` SANS `local`
   : en bash, une variable ou fonction affectée/définie DANS une fonction, sans `local`, est
   GLOBALE — appeler `fanout_check_state_integrity` depuis l'étape R1-R6 (Édition 2 ci-dessous)
   **réinitialisait `fail` à 0 et REMPLAÇAIT la fonction `note` de l'étape**, effaçant tout écart
   R1-R4 déjà consigné avant que R5 ne tourne. **Reproduit et confirmé RÉELLEMENT** (voir mesure
   ci-dessous : `fail` après un R4 simulé = 1, `fail` après le fan-out AVANT correction retombait à
   0). Corrigé en préfixant TOUTES les variables/fonctions internes de `fanout_check_state_integrity`
   par `local` et par le préfixe `_fsi_`, pour qu'aucune ne puisse jamais collisionner avec `fail`/
   `note` de l'étape appelante.

**MESURE RÉELLE (hors dépôt, scratchpad, avant d'écrire ce texte) — bascule complète 0/3 → 1/3 →
2/3, avec le code CI-DESSOUS tel quel, contre le VRAI `check-state-integrity.sh` de ce dépôt (pas
un mock) :**
```
0/3 fixture saine (sain+candidat conformes, COMMITÉS) : rc=0
  == check-state-integrity .planning/workstreams/candidat : rc=0 ==
  ✓ .planning/workstreams/candidat/STATE.md conforme
  == check-state-integrity .planning/workstreams/sain : rc=0 ==
  ✓ .planning/workstreams/sain/STATE.md conforme
  == BILAN : 0 écart(s) sur 2 compartiment(s) ==
1/3 MUTATION (working tree, non commitée) : candidat -> frontmatter cassé (workstream:+created:+milestone:"")
  → rc=2 (« milestone introuvable ou illisible (HEAD="m1" ↔ courant="") »)
  == BILAN : 1 écart(s) sur 2 compartiment(s) ==   fanout rc=1
2/3 RÉPARATION : working tree restauré À L'IDENTIQUE du contenu commité
  → rc=0 (candidat de nouveau conforme)
  == BILAN : 0 écart(s) sur 2 compartiment(s) ==   fanout rc=0
```
**Point clé de construction, tiré de cette mesure** : la fixture doit être COMMITÉE dans un état
CONFORME (les deux compartiments valides), puis le compartiment candidat MUTÉ EN PLACE dans
l'arbre de travail (non commité — c'est le diff working-tree/HEAD que `check-state-integrity.sh`
compare), puis RÉPARÉ en restaurant EXACTEMENT le contenu commité. Committer directement une
fixture « cassée » (comme la 1re version de ce plan le faisait) rend la « réparation » ultérieure
incapable de reverdir, car HEAD reste cassé et `milestone` diffère entre HEAD et le working tree
réparé — c'est l'erreur exacte de la 1re passe, distincte de BLOCK-A mais liée : la seconde n'aurait
jamais été détectée sans corriger BLOCK-A d'abord.

**Édition 1 — étape `check-state-integrity` (ligne ~353).** Remplacer le `run:` à une seule ligne :
```yaml
      - name: check-state-integrity (anti-régression du frontmatter STATE.md par compartiment, ADR-063)
        run: |
          set -eu
          . plugin/planning-core/scripts/workstream-policy.sh

          # Classification D-02 UNCONDITIONNELLE, par lecture directe du frontmatter — jamais une
          # dépendance de forme sur un libellé que check-state-integrity.sh n'émet pas (a2, D-04).
          # INCHANGÉE depuis la 1re correction (BLOCK-1) — validée par le juge frais.
          is_non_init() { # <fichier STATE.md> -> rc 0 = non-initialisé, rc 1 = pas ce cas
            f="$1"
            [ -r "$f" ] || return 1
            fm="$(awk '/^---[[:space:]]*$/{n++; if(n==1) next; if(n==2) exit} {print}' "$f")"
            keys="$(printf '%s\n' "$fm" | awk -F: 'NF>=2{gsub(/^[[:space:]]+|[[:space:]]+$/,"",$1); print $1}' | sort -u | awk 'NF')"
            nkeys="$(printf '%s\n' "$keys" | wc -l | tr -d ' ')"
            [ "$nkeys" -eq 2 ] || return 1
            printf '%s\n' "$keys" | grep -qx 'workstream' || return 1
            printf '%s\n' "$keys" | grep -qx 'created' || return 1
            return 0
          }
          # NOTE (INFO, juge frais, accepté) : un compartiment SANS STATE.md du tout (`[ -r "$f" ]`
          # faux) n'est PAS classé non-init — il tombe au script, qui rougira. C'EST VOULU : l'absence
          # totale de STATE.md n'est ni la facture nominale de `workstream create` (qui écrit TOUJOURS
          # un STATE.md) ni un état sain — c'est corrompu par construction, et le traiter en échec
          # audible (plutôt qu'en silence) est la posture fail-closed cohérente avec tout ce plan.

          # CORRECTION post plan-checker (BLOCK-A, BLOCK-B, 2e passe) : <lab_root> remplace
          # <racine .planning> — check-state-integrity.sh attend --path=RACINE DU LAB (parent de
          # .planning), --file RELATIF à cette racine (JAMAIS un chemin absolu, JAMAIS --file seul
          # sans --path cohérent — c'est le fail-open BLOCK-A). Toutes les variables/fonctions
          # internes sont préfixées _fsi_ et locales — jamais de collision avec fail/note de
          # l'étape appelante (BLOCK-B).
          fanout_check_state_integrity() { # <lab_root> -> bilan sur stdout, rc 0 = 0 écart, 1 = >=1 écart
            local lab_root="$1" lab_root_abs planning_dir
            local _fsi_fail=0 _fsi_n=0 _fsi_rc=0 _fsi_wsdir _fsi_rel _fsi_statefile_rel _fsi_out _fsi_rc2 _fsi_ws_list
            lab_root_abs="$(cd "$lab_root" && pwd)"
            planning_dir="$lab_root_abs/.planning"
            _fsi_note() { echo "::error::check-state-integrity : $1"; _fsi_fail=$((_fsi_fail + 1)); }
            _fsi_ws_list="$(vf_ws_enumerate "$planning_dir")" || _fsi_rc=$?
            if [ "$_fsi_rc" -ne 0 ]; then
              echo "::error::vf_ws_enumerate $planning_dir a rendu rc=$_fsi_rc — aucun compartiment mesurable"
              return 1
            fi
            while IFS= read -r _fsi_wsdir; do
              [ -n "$_fsi_wsdir" ] || continue
              _fsi_n=$((_fsi_n + 1))
              _fsi_rel="${_fsi_wsdir#"$lab_root_abs"/}"
              _fsi_statefile_rel="$_fsi_rel/STATE.md"
              if is_non_init "$_fsi_wsdir/STATE.md"; then
                echo "::notice::$_fsi_rel : non initialisé (D-02, frontmatter réduit à workstream:+created:), jamais compté comme écart (D-06)"
                continue
              fi
              _fsi_rc2=0
              _fsi_out="$(bash plugin/conductor/scripts/check-state-integrity.sh --path "$lab_root_abs" --file "$_fsi_statefile_rel" 2>&1)" || _fsi_rc2=$?
              echo "== check-state-integrity $_fsi_rel : rc=$_fsi_rc2 =="; printf '%s\n' "$_fsi_out"
              [ "$_fsi_rc2" -eq 0 ] || _fsi_note "$_fsi_rel : rc=$_fsi_rc2, attendu 0 (conforme) ou non-initialisé détecté en amont par is_non_init"
            done <<< "$_fsi_ws_list"
            [ "$_fsi_n" -ge 1 ] || { echo "::error::aucun compartiment énuméré — vérité vide"; return 1; }
            echo "== BILAN check-state-integrity ($lab_root_abs) : $_fsi_fail écart(s) sur $_fsi_n compartiment(s) =="
            [ "$_fsi_fail" -eq 0 ]
          }

          # --- 0/3+1/3 BASCULE sur fixture jetable, AVANT le checkout réel (QUAL-01, patron
          # check-instruction-budget). CONSTRUCTION : fixture COMMITÉE CONFORME (les deux
          # compartiments valides) — la mutation vient APRÈS, en modifiant le WORKING TREE, jamais
          # en committant directement un état cassé (cf. mesure ci-dessus). ---------------------
          T=$(mktemp -d); trap 'rm -rf "$T"' EXIT
          SAIN_FM='---
gsd_state_version: 1.0
milestone: m1
current_phase: 1
status: executing
progress:
  total_phases: 1
  completed_phases: 0
  total_plans: 1
  completed_plans: 0
---
Phase: 1'
          git -C "$T" init -q
          mkdir -p "$T/.planning/workstreams/sain" "$T/.planning/workstreams/candidat"
          printf '%s\n' "$SAIN_FM" > "$T/.planning/workstreams/sain/STATE.md"
          printf '%s\n' "$SAIN_FM" > "$T/.planning/workstreams/candidat/STATE.md"
          git -C "$T" add -A
          git -C "$T" -c user.email=t@t -c user.name=t commit -q -m "fixture-saine-initiale (deux compartiments conformes, commités)"

          rc=0; fanout_check_state_integrity "$T" >"$T/out0" 2>&1 || rc=$?
          [ "$rc" -eq 0 ] || { echo "::error::0/3 : fixture saine construite mais rc=$rc — construction invalide avant même la mutation"; cat "$T/out0"; exit 1; }

          # --- 1/3 MUTATION en place (working tree, NON commitée) : candidat -> cassé --------------
          cat > "$T/.planning/workstreams/candidat/STATE.md" <<'EOF'
---
workstream: candidat
created: "2026-09-23"
milestone: ""
---
Phase: 1
EOF
          rc=0; fanout_check_state_integrity "$T" >"$T/out1" 2>&1 || rc=$?
          cat "$T/out1"
          echo "== 1/3 mutation appliquée (candidat cassé) : rc=$rc =="
          [ "$rc" -eq 1 ] || { echo "::error::1/3 : rc=$rc, attendu 1 — le compartiment candidat cassé doit faire échouer le fan-out"; exit 1; }
          grep -q 'candidat' "$T/out1" || { echo "::error::1/3 : le compartiment fautif « candidat » n'est pas nommé dans le verdict"; exit 1; }

          # --- 2/3 RÉPARATION : working tree restauré À L'IDENTIQUE du contenu commité -------------
          printf '%s\n' "$SAIN_FM" > "$T/.planning/workstreams/candidat/STATE.md"
          rc=0; fanout_check_state_integrity "$T" >"$T/out2" 2>&1 || rc=$?
          cat "$T/out2"
          echo "== 2/3 fixture réparée : rc=$rc =="
          [ "$rc" -eq 0 ] || { echo "::error::2/3 : rc=$rc, attendu 0 — réparé à l'identique du commit, candidat doit redevenir conforme"; exit 1; }

          # --- 3/3 mesure du CHECKOUT RÉEL (fiabilite conforme, gouvernance non-initialisé — ne
          # doit JAMAIS échouer à cause de gouvernance, D-06/Success Criterion 5) ------------------
          rc=0; fanout_check_state_integrity "." || rc=$?
          echo "== 3/3 checkout réel : rc=$rc =="
          if [ "$rc" -ne 0 ]; then exit 1; fi
```

**Édition 2 — bloc R5 (ligne ~844).** Mêmes fonctions `is_non_init`/`fanout_check_state_integrity`
(verbatim de l'Édition 1 — un `run:` GitHub Actions est un process shell neuf par step, rien ne se
partage), MÊME bascule fixture (verbatim du bloc 0/3→1/3→2/3 ci-dessus, avec `$T5` au lieu de `$T`
pour ne jamais collisionner avec un `trap` d'une autre étape), à l'intérieur du bloc existant
« 2b : conformité de la racine réelle » (ne pas créer une étape séparée — R5 fait déjà partie d'une
étape existante qui porte R1-R6). **CORRECTION BLOCK-B : grâce au `local`/préfixe `_fsi_` de
`fanout_check_state_integrity`, le `fail`/`note` DE L'ÉTAPE R1-R6 ne sont JAMAIS touchés par la
fonction — c'est elle-même qui garantit l'absence de collision, plus besoin d'un commentaire
promettant une séparation que l'ancien code ne tenait pas** :
```bash
          # Fonctions is_non_init / fanout_check_state_integrity : identiques à l'Édition 1
          # (copier-coller exact, ce sont deux process shell distincts).
          is_non_init() { ... }               # cf. Édition 1, verbatim
          fanout_check_state_integrity() { ... }  # cf. Édition 1, verbatim (local + préfixe _fsi_)

          # R5a — bascule fixture (QUAL-01) : bloc 0/3->1/3->2/3 verbatim de l'Édition 1, sur $T5
          T5=$(mktemp -d)
          SAIN_FM='---
gsd_state_version: 1.0
milestone: m1
current_phase: 1
status: executing
progress:
  total_phases: 1
  completed_phases: 0
  total_plans: 1
  completed_plans: 0
---
Phase: 1'
          git -C "$T5" init -q
          mkdir -p "$T5/.planning/workstreams/sain" "$T5/.planning/workstreams/candidat"
          printf '%s\n' "$SAIN_FM" > "$T5/.planning/workstreams/sain/STATE.md"
          printf '%s\n' "$SAIN_FM" > "$T5/.planning/workstreams/candidat/STATE.md"
          git -C "$T5" add -A
          git -C "$T5" -c user.email=t@t -c user.name=t commit -q -m "fixture-saine-r5"
          cat > "$T5/.planning/workstreams/candidat/STATE.md" <<'EOF'
---
workstream: candidat
created: "2026-09-23"
milestone: ""
---
Phase: 1
EOF
          rc=0; fanout_check_state_integrity "$T5" >/tmp/r5out1 2>&1 || rc=$?
          cat /tmp/r5out1
          if [ "$rc" -ne 1 ] || ! grep -q 'candidat' /tmp/r5out1; then
            note "R5a : bascule fixture n'a pas rougi comme attendu (rc=$rc) — QUAL-01 non prouvée"
          fi
          rm -rf "$T5"

          # R5 — mesure du checkout réel (2 compartiments), gouvernance non-initialisé jamais un écart
          rc=0
          fanout_check_state_integrity "." >/tmp/r5out2 2>&1 || rc=$?
          cat /tmp/r5out2
          if [ "$rc" -ne 0 ]; then
            note "R5 : $(tail -1 /tmp/r5out2)"
          fi
```
Retirer l'ancien bloc R5 à cible unique. Le `note`/`fail` DE L'ÉTAPE (R1-R6, défini plus haut dans
le MÊME bloc `run:`) reste la SEULE source de vérité du verdict final de l'étape — la fonction
`fanout_check_state_integrity` n'y touche jamais, par construction (`local`, préfixe `_fsi_`), ce
qui est désormais PROUVÉ (mesure ci-dessus : `fail` après R4=1, après le fan-out toujours 1, après
R6=2) et non plus seulement affirmé en commentaire.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'python3 -c "import yaml,sys; yaml.safe_load(open(\".github/workflows/ci.yml\"))" 2>/dev/null && echo "yaml OK" || (command -v yamllint >/dev/null 2>&1 && yamllint -d relaxed .github/workflows/ci.yml >/dev/null && echo "yaml OK")'</automated>
    <fails_when>le fichier ne parse pas comme YAML valide (aucun des deux outils disponibles ne le confirme) — indispensable avant tout rejeu, une erreur d'indentation dans un bloc `run:` multi-lignes casse silencieusement l'étape entière</fails_when>
    <automated>bash --noprofile --norc -e -c '
n1=$(awk "/fiabilite\/STATE\.md/{c++} END{print c+0}" .github/workflows/ci.yml)
n2=$(awk "/vf_ws_enumerate/{c++} END{print c+0}" .github/workflows/ci.yml)
n3=$(awk "/is_non_init/{c++} END{print c+0}" .github/workflows/ci.yml)
n4=$(awk "/fanout_check_state_integrity/{c++} END{print c+0}" .github/workflows/ci.yml)
echo "cablage_dur_restant=$n1 appels_enumerate=$n2 defs_is_non_init=$n3 appels_fanout=$n4"
[ "$n1" -eq 0 ] && [ "$n2" -ge 2 ] && [ "$n3" -ge 2 ] && [ "$n4" -ge 4 ]
'</automated>
    <fails_when>`cablage_dur_restant=` différent de 0 (au moins une des deux invocations reste câblée en dur sur `fiabilite/STATE.md`) ; ou `appels_enumerate=` inférieur à 2 (les deux éditions n'ont pas toutes deux adopté le fan-out) ; ou `defs_is_non_init=` inférieur à 2 (la classification D-02 unconditionnelle n'est pas définie dans les deux éditions) ; ou `appels_fanout=` inférieur à 4 (chaque édition doit invoquer `fanout_check_state_integrity` au moins deux fois : une fois sur la fixture jetable, une fois sur le checkout réel — c'est la preuve QUAL-01)</fails_when>
    <automated>bash --noprofile --norc -e -c '
n=$(awk "/workstreams\/casse|workstreams\/sain/{c++} END{print c+0}" .github/workflows/ci.yml)
echo "fixture_corrompue_presente=$n"
[ "$n" -ge 4 ]
'</automated>
    <fails_when>`fixture_corrompue_presente=` inférieur à 4 (la fixture `sain`/`casse` doit apparaître dans les deux éditions — construction + réparation — pour que la bascule QUAL-01 soit réellement exécutée, pas seulement décrite en prose)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates --step "check-state-integrity" > "$T/g" 2>&1 || rc=$?; awk "/^== [123]\/3|^BILAN REJEU/" "$T/g"; echo "etape rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `etape rc=` différent de 0 — la bascule fixture (1/3 rouge, 2/3 vert) ET la mesure du checkout réel (3/3, gouvernance jamais un écart) doivent toutes deux passer dans le rejeu</fails_when>
  </verify>
  <acceptance_criteria>
    - le YAML parse
    - plus aucune occurrence littérale de `fiabilite/STATE.md` dans `ci.yml`
    - `vf_ws_enumerate` apparaît au moins deux fois (les deux éditions), `is_non_init` au moins deux fois (une définition par édition, classification D-02 inconditionnelle — jamais une dépendance de forme sur un libellé de `check-state-integrity.sh`)
    - `fanout_check_state_integrity` invoquée au moins deux fois PAR édition : une fois sur une fixture jetable portant un compartiment corrompu (bascule rouge puis vert prouvée), une fois sur le checkout réel — preuve QUAL-01 exécutée, pas seulement décrite
    - le rejeu de l'étape `check-state-integrity` est vert, `gouvernance` (non-initialisé) n'y compte jamais comme écart
  </acceptance_criteria>
  <done>Les deux points de câblage en dur nommés par le ROADMAP sont fermés — la CI mesure chaque compartiment présent, jamais un nom en dur ; la classification non-initialisé est inconditionnelle (lecture directe du frontmatter) ; chaque édition prouve par mutation, sur fixture jetable, qu'un compartiment corrompu la fait rougir puis reverdir avant de mesurer le checkout réel.</done>
</task>

<task type="auto">
  <name>Task 2 : fixture `check-divergence.sh en CI` étendue à 3 compartiments dont un cassé, bascule dans l'étape</name>
  <files>.github/workflows/ci.yml</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `.github/workflows/ci.yml` lignes ~862-929 en entier (fixture `alpha` complète à étendre)
  </read_first>
  <action>
Étendre l'étape `check-divergence.sh en CI` : après la construction du compartiment `alpha`
(inchangée, patron « 1. Fixture PROPRE » déjà en place), ajouter DEUX compartiments
supplémentaires dans le MÊME `$FIX`, AVANT le premier commit git (pour que la fixture initiale à
trois compartiments soit commitée d'un coup) :
```bash
          # --- 1bis. DEUX compartiments supplémentaires, dans la MÊME fixture, WSAW-03/D-05 -----
          mkdir -p "$FIX/.planning/workstreams/beta/phases/VFDO-02-x-registered"
          echo stub > "$FIX/.planning/workstreams/beta/phases/VFDO-02-x-registered/PLAN.md"
          printf '%s\n' "### Phase 02: registered" > "$FIX/.planning/workstreams/beta/ROADMAP.md"

          mkdir -p "$FIX/.planning/workstreams/gamma/phases/VFDO-09-x-registered"
          echo stub > "$FIX/.planning/workstreams/gamma/phases/VFDO-09-x-registered/PLAN.md"
          printf '%s\n' "### Phase 09: registered" > "$FIX/.planning/workstreams/gamma/ROADMAP.md"
```
Insérer ce bloc AVANT le `git -C "$FIX" init -q` existant, pour que les trois compartiments soient
tous dans le commit initial « fixture-saine » (renommer le message de commit en conséquence si le
plan le juge plus clair, sinon laisser tel quel — détail cosmétique).

**Assertion 1/4 — fixture à 3 compartiments, tous conformes, avant toute mutation :**
```bash
          rc=0
          out="$(bash plugin/conductor/scripts/check-divergence.sh --path "$FIX" 2>&1)" || rc=$?
          echo "== 1/4 fixture 3 compartiments propres : rc=$rc =="; printf '%s\n' "$out"
          [ "$rc" -eq 0 ] || note "1/4 : rc=$rc, attendu 0 (conforme, 3 compartiments)"
```

**Assertion 2/4 — MUTATION : le 3e compartiment (`gamma`) est CASSÉ (S2, patron déjà utilisé sur
`alpha`), sur la MÊME fixture, sans reconstruire :**
```bash
          mkdir -p "$FIX/.planning/workstreams/gamma/phases/VFDO-09-y-orphan"
          echo stub > "$FIX/.planning/workstreams/gamma/phases/VFDO-09-y-orphan/PLAN.md"

          rc=0
          out="$(bash plugin/conductor/scripts/check-divergence.sh --path "$FIX" 2>&1)" || rc=$?
          echo "== 2/4 fixture MUTÉE (gamma cassé, alpha/beta intacts) : rc=$rc =="; printf '%s\n' "$out"
          [ "$rc" -eq 1 ] || note "2/4 : rc=$rc, attendu 1 (S2 sur gamma, alpha/beta conformes n'y font pas obstacle)"
          case "$out" in *"gamma"*) : ;; *) note "2/4 : le compartiment fautif « gamma » n'est pas nommé dans le verdict" ;; esac
```
C'est la preuve WSAW-04 : le TROISIÈME compartiment n'est câblé NULLE PART ailleurs dans ce
fichier — sa seule présence sur le disque de la fixture suffit à ce qu'il soit inspecté et à ce que
sa casse soit détectée, alors que `alpha` et `beta` restent conformes à côté.

**Assertion 3/4 — le compartiment cassé est RÉPARÉ en place (le dossier orphelin retiré) :**
```bash
          rm -rf "$FIX/.planning/workstreams/gamma/phases/VFDO-09-y-orphan"

          rc=0
          out="$(bash plugin/conductor/scripts/check-divergence.sh --path "$FIX" 2>&1)" || rc=$?
          echo "== 3/4 fixture RÉPARÉE (gamma revenu conforme) : rc=$rc =="; printf '%s\n' "$out"
          [ "$rc" -eq 0 ] || note "3/4 : rc=$rc, attendu 0 (bascule inverse : cassé → réparé doit revenir vert)"
```
C'est la bascule inverse explicitement exigée par le ROADMAP (« rend rouge... et vert sur la même
fixture sans le compartiment cassé ») — sans elle, une preuve rouge seule ne distingue pas « sait
détecter » de « rend toujours 1 par accident ».

**Assertion 4/4 — la mutation `alpha` EXISTANTE (S2 sur `alpha`, déjà en place aux lignes ~897-903
d'origine) reste inchangée et continue de fonctionner, désormais AU MILIEU d'une fixture à 3
compartiments** — ne rien modifier à ce bloc existant, le laisser s'exécuter à la suite (il
redevient l'assertion « 4/4 » dans la numérotation, ou reste numéroté comme avant selon ce qui
rend le journal le plus lisible ; le renumérotage exact est laissé au jugement de l'exécutant tant
que TOUTES les assertions existantes ET nouvelles sont présentes et vertes).

Ajuster le libellé du BILAN final (« sur N verdicts ») pour refléter le nouveau total
d'assertions.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'python3 -c "import yaml,sys; yaml.safe_load(open(\".github/workflows/ci.yml\"))" 2>/dev/null && echo "yaml OK" || (command -v yamllint >/dev/null 2>&1 && yamllint -d relaxed .github/workflows/ci.yml >/dev/null && echo "yaml OK")'</automated>
    <fails_when>le YAML ne parse pas</fails_when>
    <automated>bash --noprofile --norc -e -c '
n=$(awk "/workstreams\/beta|workstreams\/gamma/{c++} END{print c+0}" .github/workflows/ci.yml)
echo "compartiments_ajoutes=$n"
[ "$n" -ge 4 ]
'</automated>
    <fails_when>`compartiments_ajoutes=` inférieur à 4 (les deux nouveaux compartiments `beta`/`gamma` doivent apparaître à au moins deux endroits chacun : construction + mutation/assertion)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates --step "check-divergence.sh en CI" > "$T/g" 2>&1 || rc=$?; awk "/^== [1-4]\/4|^BILAN REJEU/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "etape rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `etape rc=` différent de 0 ; ou `rejouees=` différent de 1</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^BILAN REJEU gates/" "$T/g"; echo "gates rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 — en particulier les deux étapes touchées à la Task 1 (`check-state-integrity`, bloc R5) doivent rester vertes sur le checkout réel</fails_when>
  </verify>
  <acceptance_criteria>
    - YAML valide
    - fixture à 3 compartiments (`alpha`/`beta`/`gamma`) construite dans la même étape
    - bascule rouge (gamma cassé, alpha/beta intacts nommés) puis vert (gamma réparé) prouvée DANS l'étape
    - assertion `alpha` préexistante inchangée et toujours verte
    - rejeu `gates` complet vert, y compris les deux étapes de la Task 1
  </acceptance_criteria>
  <done>WSAW-03 et WSAW-04 sont prouvées par une bascule rouge/vert sur 3 compartiments dans l'étape CI elle-même, et les deux câblages en dur du ROADMAP sont fermés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout CI réel → fan-out `check-state-integrity`/R5 | le fan-out mesure désormais le compartiment `gouvernance` réel, dont l'état (non-initialisé) doit être distingué d'un vrai défaut sans jamais faire échouer le job à tort |
| fixture jetable → dépôt réel | la fixture à 3 compartiments vit entièrement dans `mktemp -d`, jamais sur le checkout — une régression qui la ferait pointer sur `.` muterait le dépôt lui-même en CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-11 | Denial of service | le fan-out R5/`check-state-integrity` ferait échouer le job sur `gouvernance/STATE.md` (non-initialisé, D-06) — régression qui transformerait « muet » en « rouge », exactement ce que le ROADMAP interdit (Success Criterion 5) | high | mitigate | détection nommée du cas non-initialisé, jamais comptée dans `fail` ; vérifiée par rejeu réel sur ce dépôt (2 compartiments, dont `gouvernance`) |
| T-41.1-12 | Tampering | un troisième compartiint de fixture non commité (oublié dans le `git add`) rendrait `check-divergence.sh` aveugle à sa casse, faisant passer l'assertion 2/4 à tort | medium | mitigate | les trois compartiments sont créés AVANT le premier `git add .planning` unique — aucun ajout partiel |
| T-41.1-13 | Repudiation | sans fixture corrompue dédiée, le fan-out `check-state-integrity`/R5 pourrait prétendre à une preuve QUAL-01 alors qu'il ne fait que mesurer un checkout où les deux branches (conforme, non-initialisé) sont vertes — aucune assertion capable de rougir (finding BLOCK-2 du juge frais) | high | mitigate | fixture jetable `sain`/`casse` avec bascule rouge (1/3) puis vert (2/3) EXÉCUTÉE avant la mesure du checkout réel (3/3), dans les DEUX éditions (check-state-integrity ET R5) — patron `check-instruction-budget` |
| T-41.1-14 | Tampering | la classification « non initialisé » dépendant d'un `grep` sur un libellé que `check-state-integrity.sh` n'émet jamais transformerait silencieusement le muet (`gouvernance`) en rouge sur tout checkout réel (finding BLOCK-1 du juge frais) | high | mitigate | classification `is_non_init` par lecture DIRECTE et INCONDITIONNELLE du frontmatter (exactement les deux clés `workstream:`+`created:`), jamais un repli sur un texte de sortie de script |
</threat_model>

<verification>
YAML valide ; plus aucun câblage en dur sur `fiabilite/STATE.md` ; fan-out `vf_ws_enumerate` sur
les deux étapes touchées ; fixture à 3 compartiments avec bascule rouge/vert prouvée dans l'étape ;
rejeu `gates` complet vert, `gouvernance` non-initialisé jamais compté en écart.
</verification>

<success_criteria>
Les deux câblages en dur du ROADMAP sont fermés par fan-out sur `vf_ws_enumerate`, et la CI prouve,
dans sa propre étape, qu'un troisième compartiment ajouté à une fixture se fait inspecter sans
aucun câblage supplémentaire — WSAW-02/03/04 sont vraies côté CI.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ci.yml` — fan-out `check-state-integrity`/R5, fixture à 3 compartiments.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-06-SUMMARY.md` when done
</output>

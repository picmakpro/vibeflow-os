---
phase: 41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/skills/vf-new-lab/SKILL.md
  - plugin/conductor/scripts/tests/test-vf-new-lab.sh
  - plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh
autonomous: true
requirements: [WSCH-01, WSCH-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un lab de code neuf voit la question de partition posée par vf-new-lab, immédiatement après avoir tranché « lab de code » (ADR-055) et avant de router gsd-new-project — jamais avant ce tranchage, jamais après le retour de gsd-new-project"
    - "Session non interactive, question sautée, ou réponse négative laissent le lab en planning unique, sans aucun appel à `workstream create` — le chemin silencieux historique n'est pas modifié"
    - "Une réponse « oui » déclenche un appel réel à `gsd-tools workstream create <nom> --migrate-name <nom>`, exécuté seulement APRÈS que `gsd-new-project` a retourné et que `.planning/ROADMAP.md` existe sur disque"
    - "Le compartiment ainsi créé hérite d'un STATE.md à frontmatter complet (`milestone:`, `current_phase:`) par migration réelle du moteur, sans écriture VibeFlow supplémentaire (D-07) — prouvé par exécution réelle sur fixture, pas par lecture de doctrine"
    - "Un second appel involontaire (idempotence) ne plante jamais : le résultat JSON (`created:false`, `error:already_exists`) est lu, jamais un succès supposé"
    - "Zéro occurrence des mots « workstream » et « compartiment » dans la question, ses options et la ligne « pourquoi » effectivement montrées à l'utilisateur (D-08) — le reste du fichier peut légitimement garder ses 9 emplois préexistants de « compartiment » (notion différente, `references/compartments.md`)"
  artifacts:
    - path: "plugin/conductor/skills/vf-new-lab/SKILL.md"
      provides: "hook D-01/D-02/D-03/D-06/D-07/D-08 inséré au point 3 de la Phase 7 (Socle planning, sous-cas lab de code)"
    - path: "plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh"
      provides: "preuve d'exécution réelle : fixture façon gsd-new-project → `workstream create` réel → STATE.md migré conforme → idempotence"
    - path: "plugin/conductor/scripts/tests/test-vf-new-lab.sh"
      provides: "assertions doc-contract (WSCH-01, D-08) scoped à la région montrée à l'utilisateur"
  key_links:
    - "Le hook D-03 est le SEUL point d'appel de `workstream create` dans ce plan — le skill de bascule (41.2-02) appellera la même primitive moteur pour un lab déjà démarré, jamais une réimplémentation VibeFlow (Don't Hand-Roll, RESEARCH.md)"
    - "La résolution du binaire `gsd-tools` dans le test d'intégration réutilise la même cascade sûre (env → PATH → CLAUDE_CONFIG_DIR) que celle exigée du script de précondition du plan 41.2-02 — aucun candidat cwd-relatif, même dans un test"
---

<objective>
Prouver bout en bout, avant toute autre expansion, que le choix de partition au démarrage d'un lab
de code fonctionne réellement : la question (D-01) se pose au bon moment (D-03), le chemin silencieux
par défaut reste inchangé (D-02), et une réponse « oui » aboutit à un premier compartiment
authentiquement conforme via le moteur GSD existant (D-06/D-07) — sans qu'aucune ligne VibeFlow
n'écrive de fichier de planning à la main. C'est la tracer slice de la phase : elle touche la seule
prose modifiée (`vf-new-lab/SKILL.md`) ET invoque réellement `gsd-tools workstream create`, la même
primitive que le skill de bascule du plan suivant consommera. Séquencée AVANT le skill de bascule
(41.2-02) sur demande explicite du cadrage (TRACER_MODE) : prouver ce chemin d'abord évite de bâtir
le reste sur une hypothèse non vérifiée sur le comportement de migration du moteur.

Purpose: WSCH-01 (question, vocabulaire, défaut silencieux) et WSCH-02 (compartiment créé par le
moteur, conforme sans réparation manuelle) doivent être vrais avant que quoi que ce soit d'autre ne
s'appuie dessus.
Output: `vf-new-lab/SKILL.md` modifié ; deux suites de tests (une doc-contract, une intégration
réelle contre le moteur GSD installé) prouvant WSCH-01 et WSCH-02.

**Hors périmètre de ce plan, confirmé et non touché (D-04)** : `plugin/installer/SKILL.md` étape 4
n'appelle jamais `gsd-new-project` elle-même (vérifié par grep le 2026-09-23 — étape 4 = single-select
"lab de développement" vs "autre métier", pose `dev-orchestrator`, rien de plus) ; aucune modification
de ce fichier n'est nécessaire pour la question D-01.

**Trou de couverture assumé, non comblé par ce plan (D-05)** : un lab qui atteint `gsd-new-project`
via `vibeflow-head → Task(vf-dev-manager) → gsd-new-project` (`plugin/dev-orchestrator/references/intent-routing.md:62`,
hors du périmètre d'écriture de cette phase) ne verra jamais la question D-01. Documenté comme
Deferred Idea dans `41.2-CONTEXT.md` — le contenu doctrinal WSAW-07 (plan 41.2-03) le nomme comme
"non couvert, hérite du planning unique par défaut" ; aucune tâche de ce plan ne le ferme.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-RESEARCH.md
@.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-PATTERNS.md
@plugin/conductor/skills/vf-new-lab/SKILL.md
@plugin/conductor/skills/vf-update/SKILL.md
@plugin/conductor/scripts/tests/test-vf-new-lab.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : hook D-01..D-08 dans vf-new-lab + preuve réelle d'invocation moteur (WSCH-01, WSCH-02)</name>
  <files>plugin/conductor/skills/vf-new-lab/SKILL.md, plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh</files>
  <read_first>
    - plugin/conductor/skills/vf-new-lab/SKILL.md:149-182 (Phase 7, point 3 "Socle planning" — insertion exacte, sous-cas "lab de code", juste avant le paragraphe "Lab à compartiments" qui est une notion DIFFÉRENTE et ne doit pas être touchée)
    - plugin/conductor/skills/vf-update/SKILL.md:94-100 (seul SKILL.md du module nommant explicitement **AskUserQuestion** en gras + question française entre guillemets + options en prose — patron à suivre, jamais un bloc JSON littéral)
    - 41.2-CONTEXT.md § D-01 (texte exact de la question, header, options), § D-02 (chemin silencieux), § D-03 (point d'insertion, ordre), § D-06/D-07 (commande exacte, pourquoi le premier compartiment naît conforme sans machinerie), § D-08 (vocabulaire interdit)
    - 41.2-RESEARCH.md § Code Examples ("workstream create <nom> --migrate-name <nom> — comportement de migration vs stub, lu dans le source") et § Common Pitfalls Pitfall 2 (ordre : le hook D-03 s'insère APRÈS le retour de `gsd-new-project`, jamais avant son invocation)
    - 41.2-PATTERNS.md § "plugin/conductor/skills/vf-new-lab/SKILL.md (skill, request-response) — MODIFIED" (insertion exacte, convention AskUserQuestion citée verbatim)
    - ~/.claude/gsd-core/bin/lib/workstream.cjs (lecture seule, hors périmètre d'écriture — moteur GSD ; confirme que `cmdWorkstreamCreate` échoue si `.planning/` n'existe pas encore, et que la migration court-circuite le stub quand `name` == `migrate-name`)
  </read_first>
  <behavior>
    - Le hook pose la question **immédiatement après** avoir tranché "lab de code" (ADR-055) et **avant** de router `gsd-new-project` — jamais l'inverse.
    - Skip / non-interactif / réponse "non" → aucun appel `workstream create`, comportement historique inchangé.
    - Réponse "oui" → APRÈS retour de `gsd-new-project` et présence sur disque de `.planning/ROADMAP.md`, appel réel `gsd-tools workstream create <nom> --migrate-name <nom>` sur une fixture reconstituant le produit de `gsd-new-project` → JSON `created:true`, `migration.migrated:true`, et le `STATE.md` du compartiment résultant porte `milestone:` et `current_phase:` (hérité par migration, pas le stub nu).
    - Un second appel identique (idempotence) → JSON `created:false`, `error:"already_exists"`, exit 0 — jamais un crash, jamais un succès supposé sans lire ce champ.
  </behavior>
  <action>
Modifier `plugin/conductor/skills/vf-new-lab/SKILL.md`, point 3 "Socle planning" de la Phase 7,
uniquement dans la clause "lab de code" (ne pas toucher au paragraphe "Lab à compartiments" qui suit
— notion distincte, D-08). Remplacer la phrase actuelle "*lab de code* → le socle du **projet**
appartient au moteur de développement, router la brique `gsd-new-project` (...)" par une version qui
intercale le hook AVANT le routage : "*lab de code* → le socle du **projet** appartient au moteur de
développement. **Avant de router** (D-03, Phase 41.2), poser via **AskUserQuestion** [texte D-01
exact, voir ci-dessous]. Session non interactive, question sautée, ou réponse négative → retenir
« planning unique » et continuer sans appel moteur (chemin silencieux inchangé). **Puis** router la
brique `gsd-new-project` (`vf-planning` n'y pose plus le tronc, il tient l'altitude lab et redirige —
carte : `dev-orchestrator/references/intent-routing.md`). Une fois que `gsd-new-project` a
**retourné** et que `.planning/ROADMAP.md` existe sur disque (jamais avant — `workstream create`
échoue tant que `.planning/` n'existe pas) — sur réponse « oui » ci-dessus, appeler `gsd-tools
workstream create <nom> --migrate-name <nom>` (`<nom>` = slug déjà dérivé par `gsd-new-project`,
aucune nouvelle question) ; lire le résultat JSON (`created`/`migration`/`error`) avant de confirmer
— un second appel involontaire rend `{"created":false,"error":"already_exists"}` sans planter, ne
jamais supposer le succès. Confirmer dans le registre d'usage (wording exact laissé à
l'implémentation, même registre que la question ci-dessous)."

Texte D-01 exact à insérer, entouré des marqueurs `<!-- hook-planning-shown:start -->` /
`<!-- hook-planning-shown:end -->` (ces marqueurs délimitent PRÉCISÉMENT ce qui est effectivement
montré à l'utilisateur — Task 2 de ce plan y scope son assertion négative de vocabulaire, car le
reste du fichier porte déjà 9 emplois légitimes de « compartiment » pour une notion différente) :
header `"Collaboration"` — « **Plusieurs personnes ou agents vont-ils travailler en parallèle sur des
sujets séparés ?** *Ça évite qu'un deuxième chantier attende que le premier soit fini pour démarrer.
Par défaut, un seul planning suffit — on peut toujours basculer plus tard.* » Options :
**Oui, prévoir plusieurs sujets en parallèle** / **Non, un seul planning suffit (recommandé)**.

<!-- planner-discipline-allow: workstream -->
<!-- planner-discipline-allow: compartiment -->
Ne pas écrire les mots « workstream » ni « compartiment » à l'intérieur des marqueurs
`hook-planning-shown` — ils sont autorisés dans le reste de la phrase (méta-instruction à l'agent qui
incarne le skill, jamais montrée telle quelle).

Puis créer `plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh` : résoudre `gsd-tools`
par la cascade sûre à trois niveaux — `GSD_TOOLS` (variable d'environnement, si elle pointe un
fichier existant) → `gsd-tools` sur `PATH` → `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs`
— **aucun candidat relatif au répertoire de travail courant, jamais** (classe de vulnérabilité fermée
par `dag.sh:149-187`, `resolve_gsd_tools_cmd`, D-07 de ce fichier — porter la même cascade en bash,
même dans un fichier de test). Cible `.cjs` invoquée via `node`, sinon directement. Construire une
fixture `mktemp -d` reproduisant le PRODUIT de `gsd-new-project` (jamais son déroulé interactif,
impossible en CI) : `.planning/STATE.md` (frontmatter `gsd_state_version`, `milestone`,
`milestone_name`, `current_phase`, `status`), `.planning/ROADMAP.md` (`### Phase 1: ...`),
`.planning/REQUIREMENTS.md` (vide valide), `.planning/phases/01-test/01-01-PLAN.md` (vide). Appeler
`<cmd resolue> workstream create demo-v1 --migrate-name demo-v1 --cwd <fixture> --raw`, asserter dans
le JSON `"created": true` et `"migration": {` avec `"migrated": true` ; asserter que
`<fixture>/.planning/workstreams/demo-v1/STATE.md` contient `milestone:` ET `current_phase:` (donc
migré, jamais le stub nu qui ne porterait que `workstream:`/`created:`). Rappeler l'appel identique
et asserter `"created": false` et `"error": "already_exists"`, `rc` du process = 0 (jamais un crash).
Nettoyer la fixture par `trap`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh</automated>
    <fails_when>exit non nul, ou absence de la ligne `PASS=` suivie d'un `FAIL=0`, ou moins de 4 assertions "✓" (création, migration, frontmatter migré, idempotence)</fails_when>
    <automated>grep -c 'workstream create <nom> --migrate-name <nom>' plugin/conductor/skills/vf-new-lab/SKILL.md</automated>
    <fails_when>la commande rend `0` (commande moteur absente du hook) ou échoue (fichier introuvable)</fails_when>
  </verify>
  <acceptance_criteria>
    - `vf-new-lab/SKILL.md` contient, dans cet ordre, la question D-01 (texte exact entre les marqueurs `hook-planning-shown`), puis "**Puis** router la brique", puis "Une fois que `gsd-new-project` a **retourné**", puis "`gsd-tools workstream create <nom> --migrate-name <nom>`"
    - `wc -l plugin/conductor/skills/vf-new-lab/SKILL.md` reste ≤ 500 (ADR-029)
    - `test-vf-new-lab-hook-planning.sh` rejoue un appel RÉEL à `gsd-tools workstream create` (aucun mock, aucune simulation du JSON) et ses deux assertions (conformité migrée + idempotence) passent contre le moteur GSD réellement installé sur la machine d'exécution
    - la cascade de résolution du script de test ne contient aucun chemin construit à partir de `pwd`/`$(git rev-parse --show-toplevel)`/`./gsd-core` concaténé en candidat exécutable
  </acceptance_criteria>
  <reversibility rating="reversible">Insertion de prose dans un SKILL.md + un script de test — aucune migration de doctrine, aucun effet sur un lab déjà installé tant que le module n'est pas redistribué. Cohérent avec D-03 de 41.2-CONTEXT.md ("Reversibility: reversible — c'est un texte d'UI, changeable sans toucher la mécanique").</reversibility>
  <done>Le hook complet (question → routage → appel post-retour → lecture du résultat JSON) est écrit dans vf-new-lab/SKILL.md et prouvé par une invocation réelle et non simulée de `gsd-tools workstream create` sur une fixture jetable, migration conforme et idempotence toutes deux vérifiées.</done>
</task>

<task type="auto">
  <name>Task 2 : assertions doc-contract WSCH-01 / D-08, scoped à la région montrée</name>
  <files>plugin/conductor/scripts/tests/test-vf-new-lab.sh</files>
  <read_first>
    - plugin/conductor/scripts/tests/test-vf-new-lab.sh (intégral — patron `has()`/`ok()` à réutiliser tel quel, ne jamais dupliquer un second harness)
    - plugin/conductor/skills/vf-new-lab/SKILL.md (état après Task 1 — repérer les marqueurs `hook-planning-shown:start`/`:end` posés par Task 1)
    - 41.2-PATTERNS.md § "Doc-contract test for D-01/D-08 wording" (caveat : `vf-new-lab/SKILL.md` porte déjà 9 emplois légitimes de « compartiment » ailleurs — une assertion négative non scopée rougirait dès le premier jour)
  </read_first>
  <action>
Étendre `test-vf-new-lab.sh` (jamais un second fichier — un seul harness `has()`/`ok()` par skill,
patron déjà établi) avec un nouveau bloc, juste avant le bloc "Densité" existant. Assertions
positives (`has()` sur `grep -qF`) : la phrase de header "Collaboration" est présente ; la question
"Plusieurs personnes ou agents vont-ils travailler en parallèle sur des sujets séparés ?" est
présente verbatim ; les deux libellés d'option ("Oui, prévoir plusieurs sujets en parallèle" et "Non,
un seul planning suffit (recommandé)") sont présents ; la ligne « pourquoi » ("Ça évite qu'un
deuxième chantier attende que le premier soit fini pour démarrer") est présente ; la phrase d'ordre
"Une fois que \`gsd-new-project\` a **retourné**" est présente (garde-fou Pitfall 2) ; la commande
exacte "workstream create <nom> --migrate-name <nom>" est présente.

<!-- planner-discipline-allow: workstream -->
<!-- planner-discipline-allow: compartiment -->
Assertion négative scopée (jamais sur le fichier entier — la clause "Lab à compartiments" légitime
et préexistante ferait rougir un grep non scopé dès le premier jour) : extraire UNIQUEMENT le texte
entre les marqueurs `hook-planning-shown:start`/`:end` via
`sed -n '/hook-planning-shown:start/,/hook-planning-shown:end/p' "$SKILL"`, puis vérifier que
`grep -ciE 'workstream|compartiment'` sur cet extrait rend `0`. Si les marqueurs sont absents (test
symétrique) : échec explicite, jamais un vert silencieux.

Ajouter la même ligne de densité déjà présente (`wc -l ≤ 500`) en confirmant qu'elle reste vraie
après l'insertion de Task 1 — ne pas la dupliquer si déjà comptée dans le bloc de fin.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-vf-new-lab.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗` dans la sortie, ou `FAIL=` suivi d'un nombre différent de 0</fails_when>
    <automated>sed -n '/hook-planning-shown:start/,/hook-planning-shown:end/p' plugin/conductor/skills/vf-new-lab/SKILL.md | grep -ciE 'workstream|compartiment'</automated>
    <fails_when>la commande rend un nombre différent de `0`, ou ne rend rien (marqueurs absents)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test-vf-new-lab.sh` gagne au moins 7 nouvelles assertions positives et 1 assertion négative scopée, toutes vertes
    - la suite reste un fichier unique (pas de `test-vf-new-lab-hook-wording.sh` séparé) — patron `has()`/`ok()` réutilisé à l'identique
    - le compte de lignes du bloc negatif est borné aux marqueurs — vérifié en relisant que le grep scopé sur le fichier ENTIER (sans scope) rendrait bien un nombre `> 0` (preuve que le scope change effectivement le résultat, sinon le test ne teste rien)
  </acceptance_criteria>
  <done>test-vf-new-lab.sh prouve, par grep versionné et rejouable, que la question D-01 est montrée mot pour mot et qu'aucun des deux mots jargon n'apparaît dans ce qui est effectivement affiché à l'utilisateur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Réponse utilisateur → décision de bifurcation | La réponse à `AskUserQuestion` (oui/non) pilote un appel moteur réel ; entrée conversationnelle non structurée côté outil, mais bornée à deux options fixes (pas de saisie libre) |
| Sortie JSON du moteur → confirmation affichée | Le JSON de `workstream create` est lu avant toute confirmation ; ne jamais l'assumer sans le lire (idempotence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.2-01 | Tampering | Appel double/retry du hook (double-clic conversationnel, reprise de session) | medium | mitigate | Le hook lit le champ `created`/`error` du JSON avant de confirmer — `already_exists` est traité comme un succès silencieux, jamais un crash ni une seconde tentative d'écriture (déjà géré côté moteur, `cmdWorkstreamCreate` idempotent) |
| T-41.2-02 | Tampering | Ordre d'appel inversé (workstream create avant que .planning/ existe) | low | mitigate | Le texte du hook nomme explicitement "Une fois que gsd-new-project a retourné" — Pitfall 2 encodé en prose ; de toute façon `cmdWorkstreamCreate` échoue hard (exit 1) si `.planning/` est absent, donc pas de fenêtre d'exploitation, seulement un risque d'échec opérationnel évité |
| T-41.2-03 | Information Disclosure | Aucune | n/a | accept | Cette phase ne manipule aucun secret, aucune donnée utilisateur persistée au sens applicatif — seul le nom de projet (déjà public dans le lab) transite |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-vf-new-lab.sh` → 0 échec
- `bash plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh` → 0 échec, invocation réelle du moteur GSD installé prouvée (pas de mock)
- `wc -l plugin/conductor/skills/vf-new-lab/SKILL.md` ≤ 500
</verification>

<success_criteria>
WSCH-01 et WSCH-02 sont vrais, prouvés par exécution : la question se pose au bon moment avec le bon
texte, le chemin silencieux par défaut est intact, et un « oui » produit un compartiment conforme via
le moteur GSD réel, migration réelle observée sur disque — zéro machinerie de peuplement VibeFlow.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- Hook de prose (marqueurs `hook-planning-shown:start`/`:end`) dans `plugin/conductor/skills/vf-new-lab/SKILL.md`, point 3 de la Phase 7 ("Socle planning", sous-cas "lab de code")
- `plugin/conductor/scripts/tests/test-vf-new-lab-hook-planning.sh` (nouveau fichier de test d'intégration)
- Extension du fichier existant `plugin/conductor/scripts/tests/test-vf-new-lab.sh` (nouveau bloc d'assertions doc-contract, aucun nouveau fichier)

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.2-choisir-la-partition-du-planning-au-d-marrage-d-un-lab-inser/41.2-01-SUMMARY.md` when done
</output>

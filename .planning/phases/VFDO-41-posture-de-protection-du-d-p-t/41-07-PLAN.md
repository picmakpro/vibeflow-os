---
phase: 41-posture-de-protection-du-d-p-t
plan: 07
lot: L7 — PR jetable volontairement rouge : mesure M-1 (doublon push + pull_request) et refus par défaut d'une PR rouge (temps i du critère 2)
type: execute
wave: 5
depends_on: ["41-06"]
autonomous: false
requirements: [PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition dure : `CO-I: … merge_sans_contournement=refuse` (41-06). Sans elle, AUCUNE tentative de merge n'a lieu sur la PR rouge"
    - "Cause du rouge triviale et réversible, jamais un gate modifié (D-M7) : une suite de preuve `scripts/tests/test-preuve-41-m1.sh`, présente sur la seule branche jetable, qui échoue selon `GITHUB_EVENT_NAME` et est découverte par l'étape existante `find plugin scripts -path '*/tests/test-*.sh'`"
    - "M-1 mesuré sur trois commits d'une même PR, construits par plomberie : C0 (vert sur les deux événements, témoin de discrimination : état attendu NON `BLOCKED`), A (rouge sur `pull_request` seulement), B (rouge sur `push` seulement) ; dans A et B, le run vert dort 420 s pour terminer APRÈS le rouge — le run le plus récent est donc le vert, ce qui rend la mesure discriminante"
    - "Verdict M-1 : SUR si A et B sont `BLOCKED` avec ordre discriminant, C0 non `BLOCKED` et `main` inchangée ; DEFAUT si A ou B n'est pas `BLOCKED` (un run rouge masqué par son jumeau vert) ; INDETERMINE sinon. DEFAUT ou INDETERMINE → le plan S'ARRÊTE sur un checkpoint de décision ; aucune modification de `ci.yml` n'est planifiée"
    - "Temps (i) du critère 2 (D-09) : sur A et B `BLOCKED`, `gh pr merge --merge` sans contournement est refusé, message capturé ; la PR rouge n'atterrit jamais — fermée sans merge, branche distante supprimée"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-R-COMMITS:`, `MAIN-AVANT-PR-R:`, `PR-R:`, `M1-ETATS:`, `M1-C0:`, `M1-A:`, `M1-B:`, `M1-VERDICT:`, `CI-I-MSG:`, `PR-R-FERMEE:`"
  key_links:
    - "M-1 doit se jouer pendant qu'aucun merge n'arrive sur `main` : PR-P reste ouverte et non mergée jusqu'au plan 41-08"
---

<objective>
Mesurer lequel des deux runs jumeaux (push, pull_request) compte pour un check requis, et prouver
qu'une PR rouge est refusée sans contournement — sans jamais laisser la PR rouge atterrir.

Purpose: inconnu M-1 (D-M4) et temps (i) du critère de succès 2 (PROT-01, D-09).
Output: verdict M-1, refus capturé, PR jetable fermée.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-06-SUMMARY.md
@.github/workflows/ci.yml

Sources : `docs.github.com/en/pull-requests/…/status-checks` et troubleshooting-rules (checks requis
matchés par nom de job sans tenir compte de l'événement ; lequel de deux runs de même nom compte :
non documenté). Constat du 2026-09-17 sur la tête de la PR #78 : les runs `push` et `pull_request`
démarrent à quelques secondes d'écart, le job de tests dure environ quatre minutes.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : construire C0 → A → B par plomberie, prouver localement le comportement de la suite de preuve, relever `main`</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .github/workflows/ci.yml (étape « Découvrir et lancer toutes les suites » : `bash "$t"` par suite, bilan)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`CO-I:`, `PR-P:`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin` ; `CO-I: … merge_sans_contournement=refuse` présente
(sinon arrêt) ; la seule PR ouverte est PR-P.

Rédiger hors dépôt trois variantes d'un script de suite `test-preuve-41-m1.sh`. En-tête commentaire
« PREUVE PHASE 41 (M-1) — branche jetable, NE PAS MERGER ». Une variable écrite en dur
`ROUGE_SUR` vaut `aucun` (C0), `pull_request` (A) ou `push` (B). Comportement : si
`GITHUB_EVENT_NAME` égale `ROUGE_SUR`, imprimer `PREUVE41 rouge volontaire sur <événement>` et sortir
1 ; sinon, si `ROUGE_SUR` n'est pas `aucun`, dormir `PREUVE41_DODO` secondes (défaut 420), puis
imprimer `PREUVE41 vert` et sortir 0.

Construire par plomberie (index temporaire, `hash-object -w`, `update-index --add --cacheinfo`,
`write-tree`, `commit-tree`), sans changer de branche : C0 = `origin/main` + variante C0 au chemin
`scripts/tests/test-preuve-41-m1.sh` ; A = C0 avec la variante A au même chemin ; B = A avec la
variante B. Messages : « test(41): preuve M-1 — C0 témoin (NE PAS MERGER) », « … — A rouge sur
pull_request », « … — B rouge sur push ».

Vérifier localement chaque blob avec `PREUVE41_DODO=0` et `GITHUB_EVENT_NAME` forcé. Consigner
`PR-R-COMMITS: c0=<sha> a=<sha> b=<sha> base=<origin/main>` et
`MAIN-AVANT-PR-R: <sha d'origin/main>`. Commit local sur B. Imprimer les commandes de la Task 2.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; co=$(val CO-I: merge_sans_contournement); [ "$co" = refuse ] || { echo "precondition CO-I=$co"; exit 1; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); BA=$(val PR-R-COMMITS: base); S=scripts/tests/test-preuve-41-m1.sh; T=$(mktemp -d); r() { git show "$1:$S" > "$T/s"; rc=0; PREUVE41_DODO=0 GITHUB_EVENT_NAME="$2" bash "$T/s" > /dev/null 2>&1 || rc=$?; echo "$rc"; }; x="c0_push=$(r "$C0" push) c0_pr=$(r "$C0" pull_request) a_push=$(r "$A" push) a_pr=$(r "$A" pull_request) b_push=$(r "$B" push) b_pr=$(r "$B" pull_request)"; f0=$(git diff --name-only "$BA" "$C0"); fa=$(git diff --name-only "$C0" "$A"); fb=$(git diff --name-only "$A" "$B"); fil=$([ "$(git rev-parse "$C0^")" = "$BA" ] && [ "$(git rev-parse "$A^")" = "$C0" ] && [ "$(git rev-parse "$B^")" = "$A" ] && echo oui || echo non); git diff --name-only "$BA" "$B" > "$T/db"; gate=$(awk "!/^scripts\\/tests\\/test-preuve-41-m1\\.sh\$/ {c++} END{print c+0}" "$T/db"); echo "PR-R local : $x filiation=$fil fichiers_c0=$f0 fichiers_a=$fa fichiers_b=$fb autres_fichiers=$gate"; [ "$x" = "c0_push=0 c0_pr=0 a_push=0 a_pr=1 b_push=1 b_pr=0" ] && [ "$fil" = oui ] && [ "$f0" = "$S" ] && [ "$fa" = "$S" ] && [ "$fb" = "$S" ] && [ "$gate" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `precondition CO-I=` autre que `refuse` ; ou un code de sortie local différent de l'attendu `c0_push=0 c0_pr=0 a_push=0 a_pr=1 b_push=1 b_pr=0` ; ou filiation fausse ; ou un commit touche autre chose que la suite de preuve (`autres_fichiers=` différent de 0 — aucun gate, aucun fichier existant modifié)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; m=$(awk "\$1==\"MAIN-AVANT-PR-R:\" {print \$2; exit}" "$PH/41-PREUVES.md"); o=$(git rev-parse origin/main); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json headRefName --jq "[.[].headRefName] | join(\",\")"); echo "main avant PR-R : consigne=$m origin_main=$o prs_ouvertes=$op"; [ "$m" = "$o" ] && [ "$op" = preuve/41-contournement ]'</automated>
    <fails_when>exit non nul ; ou `MAIN-AVANT-PR-R:` différent d'`origin/main` ; ou une PR ouverte autre que PR-P (un merge concurrent fausserait M-1)</fails_when>
  </verify>
  <acceptance_criteria>
    - C0, A, B chaînés sur `origin/main`, ne touchant que `scripts/tests/test-preuve-41-m1.sh`
    - comportement local : C0 vert partout ; A rouge sur `pull_request` seul ; B rouge sur `push` seul
    - `main` relevée, seule PR-P ouverte
  </acceptance_criteria>
  <done>Les trois états de mesure sont prêts et prouvés localement, sans toucher aucun gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — publier C0, ouvrir PR-R, puis A et B ; tentatives de merge sans contournement conditionnées à `BLOCKED` ; fermer PR-R</name>
  <action>Samuel, compte `samuel-neveugall`, joue la séquence ci-dessous sans jamais utiliser d'option de contournement ; la PR rouge ne doit en aucun cas être mergée.</action>
  <instructions>
Compte qui agit : `samuel-neveugall`. SHA imprimés par la Task 1. Chaque attente dure plusieurs
minutes (le run vert dort 420 s dans A et B) : c'est voulu.

Étape C0 :
1. `git push origin <c0>:refs/heads/preuve/41-ci-rouge`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-ci-rouge --title "PREUVE 41 — NE PAS MERGER — CI volontairement rouge (M-1)" --body "PR jetable de la Phase 41 (D-M7). Ne jamais merger. Fermée sans merge à la fin de la mesure."` → noter `<n>`.
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin.
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus --jq .mergeStateStatus` → noter
   (si `UNKNOWN`, relire après quelques secondes). NE RIEN MERGER à cette étape, quel que soit l'état.

Étape A :
5. `git push origin <a>:refs/heads/preuve/41-ci-rouge`
6. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (sortie en échec attendue).
7. État : même commande qu'en 4 → noter.
8. SEULEMENT si 7 rend `BLOCKED` : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS
   `--admin`) → copier la sortie. Sinon : ne rien tenter, noter l'état.
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state --jq .state` → `OPEN`.

Étape B :
9. `git push origin <b>:refs/heads/preuve/41-ci-rouge`
10. à 12. : comme 6 à 8.

Fermeture :
13. `gh pr close <n> --repo picmakpro/vibeflow-os --comment "Mesure M-1 et temps (i) du critère 2 terminés — fermée sans merge (D-M7)."`
14. `git push origin --delete preuve/41-ci-rouge`

Si, à n'importe quel moment, la PR apparaît `MERGED` : s'arrêter et le signaler immédiatement.
  </instructions>
  <verification>La Task 3 recalcule depuis l'API les conclusions et heures de fin par événement pour C0, A et B, relit `main`, l'état de la PR et l'absence de la branche distante.</verification>
  <resume-signal>Coller `<n>`, les états notés en 4, 7 et 11, les sorties de 8 et 12 (ou « non tenté : état X »), les témoins d'état, et les sorties de 13 et 14, puis écrire « M-1 joué ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : recalcul M-1 depuis l'API, verdict, temps (i) du critère 2 consigné</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Consigner `PR-R: #<n> branche=preuve/41-ci-rouge` et, depuis le collage,
`M1-ETATS: c0=<état> a=<état> b=<état> merge_a=<refuse|merge|non_tente> merge_b=<…>` puis
`CI-I-MSG:` suivi des sorties de merge collées (A puis B), sur une ligne.

Pour chaque commit C0, A, B, relever depuis l'API : les runs du workflow `CI` de ce `head_sha` et leur
événement ; pour chaque run, conclusion de chacun des quatre jobs et heure de fin du job
« Suites de tests (découverte non vide) » ; pour le job rouge, confirmer que la cause est la suite de
preuve (`gh run view <id> --log-failed` portant `FAIL(rc=1) scripts/tests/test-preuve-41-m1.sh`).
Consigner :
- `M1-C0: sha=<> tests_push=<concl> tests_pr=<concl> autres_jobs_verts=<k>/6 etat=<>` ;
- `M1-A: sha=<> rouge=pull_request fin_rouge=<iso> vert=push fin_vert=<iso> ordre_discriminant=<oui|non> cause=suite_preuve autres_jobs_verts=<k>/6 etat=<> merge=<>` ;
- `M1-B:` symétrique (rouge=push) ;
- `M1-VERDICT: <SUR|DEFAUT|INDETERMINE> main_inchangee=<oui|non>`.
Règle du verdict : SUR si `etat` A et B = `BLOCKED`, ordre discriminant oui pour A et B, C0 non
`BLOCKED`/`BEHIND`/`DIRTY`, `main` inchangée depuis `MAIN-AVANT-PR-R:`, et merges A et B `refuse` ;
DEFAUT si A ou B n'est pas `BLOCKED` alors que l'ordre est discriminant, C0 conforme et `main`
inchangée, ou si un merge a eu lieu ; INDETERMINE dans tous les autres cas.
Consigner enfin `PR-R-FERMEE: state=<> merged=<true|false> branche_distante=<absente|presente>`.
Commit local sur B. Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; TJ="Suites de tests (découverte non vide)"; mes() { S="$1"; T=$(mktemp -d); gh api "repos/picmakpro/vibeflow-os/actions/runs?head_sha=$S&per_page=100" --jq ".workflow_runs[] | select(.name==\"CI\") | \"\(.id) \(.event)\"" > "$T/runs"; : > "$T/jobs"; while read -r id ev; do gh api "repos/picmakpro/vibeflow-os/actions/runs/$id/jobs?per_page=100" --jq ".jobs[] | \"$ev\t\(.name)\t\(.conclusion)\t\(.completed_at)\"" >> "$T/jobs"; done < "$T/runs"; tp=$(awk -F"\t" -v j="$TJ" "\$1==\"push\" && \$2==j {print \$3 \"@\" \$4}" "$T/jobs"); tr=$(awk -F"\t" -v j="$TJ" "\$1==\"pull_request\" && \$2==j {print \$3 \"@\" \$4}" "$T/jobs"); ov=$(awk -F"\t" -v j="$TJ" "\$2!=j && \$3==\"success\" {c++} END{print c+0}" "$T/jobs"); echo "$tp $tr $ov"; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); set -- $(mes "$C0"); c0p=${1%@*}; c0r=${2%@*}; c0o=$3; set -- $(mes "$A"); ap=${1%@*}; apt=${1#*@}; ar=${2%@*}; art=${2#*@}; ao=$3; set -- $(mes "$B"); bp=${1%@*}; bpt=${1#*@}; br=${2%@*}; brt=${2#*@}; bo=$3; oa=$([ "$ap" = success ] && [ "$ar" = failure ] && [[ "$art" < "$apt" ]] && echo oui || echo non); ob=$([ "$bp" = failure ] && [ "$br" = success ] && [[ "$bpt" < "$brt" ]] && echo oui || echo non); echo "M-1 recalcule : c0 push=$c0p pr=$c0r autres=$c0o | a push=$ap($apt) pr=$ar($art) ordre=$oa autres=$ao | b push=$bp($bpt) pr=$br($brt) ordre=$ob autres=$bo"; echo "M-1 consigne : a_ordre=$(val M1-A: ordre_discriminant) b_ordre=$(val M1-B: ordre_discriminant) c0_push=$(val M1-C0: tests_push)"; [ "$c0p" = success ] && [ "$c0r" = success ] && [ "$c0o" -eq 6 ] && [ "$ao" -eq 6 ] && [ "$bo" -eq 6 ] && [ "$oa" = "$(val M1-A: ordre_discriminant)" ] && [ "$ob" = "$(val M1-B: ordre_discriminant)" ]'</automated>
    <fails_when>exit non nul ; ou C0 non vert sur les deux événements ; ou un des trois autres jobs non vert dans un run (`autres=` différent de 6 : la cause du rouge ne serait pas la seule suite de preuve) ; ou l'ordre discriminant recalculé diffère de celui consigné (consignation fausse)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-R absente"; exit 1;; esac; e0=$(val M1-ETATS: c0); ea=$(val M1-ETATS: a); eb=$(val M1-ETATS: b); ma=$(val M1-ETATS: merge_a); mb=$(val M1-ETATS: merge_b); oa=$(val M1-A: ordre_discriminant); ob=$(val M1-B: ordre_discriminant); m0=$(awk "\$1==\"MAIN-AVANT-PR-R:\" {print \$2; exit}" "$PH/41-PREUVES.md"); mi=$([ "$(gh api repos/picmakpro/vibeflow-os/commits/main --jq .sha)" = "$m0" ] && echo oui || echo non); st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); mg=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergedAt --jq ".mergedAt != null"); TL=$(mktemp -d); git ls-remote --heads origin preuve/41-ci-rouge > "$TL/lr"; bd=$(awk "END{print (NR==0 ? \"absente\" : \"presente\")}" "$TL/lr"); c0ok=$(case "$e0" in BLOCKED|BEHIND|DIRTY|UNKNOWN|"") echo non;; *) echo oui;; esac); if [ "$mg" = true ] || [ "$ma" = merge ] || [ "$mb" = merge ]; then att=DEFAUT; elif [ "$oa" = oui ] && [ "$ob" = oui ] && [ "$c0ok" = oui ] && [ "$mi" = oui ]; then if [ "$ea" = BLOCKED ] && [ "$eb" = BLOCKED ]; then if [ "$ma" = refuse ] && [ "$mb" = refuse ]; then att=SUR; else att=INDETERMINE; fi; else att=DEFAUT; fi; else att=INDETERMINE; fi; v=$(awk "\$1==\"M1-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); msg=$(awk "\$1==\"CI-I-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "verdict M-1 : etats c0=$e0 a=$ea b=$eb merges a=$ma b=$mb main_inchangee=$mi attendu=$att consigne=$v | PR-R etat=$st mergee=$mg branche_distante=$bd message_ci_i=$msg"; [ "$v" = "$att" ] && [ "$st" = CLOSED ] && [ "$mg" = false ] && [ "$bd" = absente ] && [ "$msg" -eq 1 ] && [ "$v" = SUR ]'</automated>
    <fails_when>exit non nul ; ou verdict consigné différent du verdict recalculé ; ou PR-R non `CLOSED`, mergée, ou branche distante encore présente ; ou message du temps (i) non capturé ; ou verdict `DEFAUT` / `INDETERMINE` — dans ces deux cas la consignation reste faite et le plan s'arrête sur la Task 4 (décision humaine), jamais sur une correction de `ci.yml`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); p=$(find plugin scripts -type f -name "test-preuve-41-*" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d suite_de_preuve_dans_l_arbre=$p"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ] && [ "$p" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0 ; ou `suite_de_preuve_dans_l_arbre=` différent de 0 (la suite jetable a fui dans la branche B). Rouge T28-F : conduite du plan 41-01</fails_when>
  </verify>
  <acceptance_criteria>
    - C0, A, B recalculés depuis l'API : rouge dû à la seule suite de preuve, ordre discriminant consigné conforme
    - verdict M-1 consigné = verdict recalculé ; `SUR` pour poursuivre
    - refus sans contournement capturé sur A et B ; PR-R `CLOSED`, jamais mergée, branche distante supprimée
  </acceptance_criteria>
  <done>M-1 est tranché par la mesure et le refus par défaut d'une PR rouge est prouvé, sans qu'aucun rouge ne touche `main`.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `M1-VERDICT` ≠ SUR (sinon tâche sans objet, consignée « non déclenchée »)</name>
  <decision>Le doublon push + pull_request laisse-t-il passer une PR dont un des deux runs est rouge (DEFAUT), ou la mesure est-elle non concluante (INDETERMINE) — et que faire avant de poursuivre les preuves ?</decision>
  <context>
    Lire `M1-C0:`, `M1-A:`, `M1-B:`, `M1-VERDICT:` et `M1-ETATS:` dans `41-PREUVES.md`. D-M4 a
    prévu « une mesure, ou la suppression du doublon dans `ci.yml` si elle s'impose (alors QUAL-01
    s'applique) » ; le manager a exclu de planifier cette suppression d'avance. Le planificateur ne
    tranche pas.
  </context>
  <options>
    <option id="arret-et-cadrage">
      <name>ARRÊT ET CADRAGE</name>
      <pros>Aucune modification sans arbitrage ; le défaut est consigné et remonté avec sa mesure ; les plans 41-08 à 41-11 attendent.</pros>
      <cons>La phase reste ouverte avec des règles posées dont un check requis est masquable.</cons>
    </option>
    <option id="rejouer-la-mesure">
      <name>REJOUER LA MESURE (INDETERMINE seulement)</name>
      <pros>Un `main` qui a bougé ou un ordre non discriminant se corrige par un nouveau jeu C0/A/B.</pros>
      <cons>Nouvelle PR jetable, nouveaux gestes humains, nouveaux runs CI.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE DES RULESETS</name>
      <pros>Rien d'imposé tant que le défaut n'est pas traité (`enforcement=disabled`, commande dans `41-PREUVES.md`).</pros>
      <cons>Retour à la situation antérieure : `main` protégée par rien.</cons>
    </option>
  </options>
  <resume-signal>Répondre « non déclenchée » (verdict SUR), « ARRÊT ET CADRAGE », « REJOUER LA MESURE » ou « RETOUR ARRIÈRE DES RULESETS ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR rouge → `main` | la PR jetable porte une suite qui échoue : son atterrissage rougirait `main` |
| deux runs de même nom → check requis | un run vert pourrait masquer un run rouge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-26 | Tampering | PR rouge mergée par erreur | high | mitigate | précondition `CO-I=refuse` ; tentatives conditionnées à `BLOCKED` ; jamais `--admin` ; état relu `OPEN` après chaque tentative ; fermeture et suppression de branche vérifiées |
| T-41-27 | Spoofing | check requis satisfait par le run vert du jumeau | high | mitigate | mesure M-1 discriminante (vert le plus récent), verdict recalculé depuis l'API, arrêt sur DEFAUT |
| T-41-28 | Tampering | rouge obtenu en modifiant un gate | medium | mitigate | commits limités à `scripts/tests/test-preuve-41-m1.sh` (`autres_fichiers=0`) ; cause vérifiée dans le journal du job |
| T-41-29 | Repudiation | mesure faussée par un merge concurrent sur `main` | medium | mitigate | `MAIN-AVANT-PR-R:` relue ; seule PR-P ouverte ; INDETERMINE sinon |
</threat_model>

<verification>
M-1 tranché par une mesure discriminante recalculée depuis l'API ; refus sans contournement d'une PR
rouge capturé ; PR jetable fermée sans merge.
</verification>

<success_criteria>
Temps (i) du critère 2 prouvé ; M-1 consigné ; aucune modification de `ci.yml`.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-07-SUMMARY.md` when done — committé sur la branche B.
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 01
lot: L1 — reprise du volet admin — préalables re-mesurés (identité admin, collaborateurs, #29 fermée, PR en vol, contextes de checks) et source JSON des deux rulesets (D-02bis)
type: execute
REGLES_MAIN_FORCE_PUSH_SUPPRESSION: INCLUSES  # force push et suppression de main interdits hors liste de contournement — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17
REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE: decideur=Samuel canal=AskUserQuestion-session-principale date=2026-09-17
wave: 1
depends_on: []
autonomous: true
requirements: [PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .github/rulesets/main.json
  - .github/rulesets/tags-v.json

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Branche A = `feat/phase-41-volet-admin` (créée depuis `origin/main`, déjà extraite). Ce plan n'écrit RIEN chez GitHub : uniquement des lectures `GET`, alors même que le jeton de la session est admin"
    - "Identité mesurée par l'API, jamais par `gh auth status` : `picmakpro` (Willy, id 203482067), `permissions.admin` = `true` ; collaborateurs exactement `samuel-neveugall` (151974738, write) et `picmakpro` (203482067, admin) — consignés `ACTEURS-CONTOURNEMENT:` (41-CONTEXT.md § REPRISE)"
    - "D-08 est constaté, pas rejoué : #29 relue `CLOSED` ; `rulesets` relu vide ; PR ouvertes inventoriées dans `PR-EN-VOL-REPRISE:` — aucune n'est touchée par la phase"
    - "Les 4 contextes requis sont RECOPIÉS par programme depuis les check runs de l'application 15368 sur la pointe courante d'`origin/main`, égaux aux 4 `name:` de jobs de `ci.yml` ET à ceux mesurés le 2026-09-17 (`CONTEXTES-CHECKS:`) ; aucun contexte ne porte `check-release-tag` — c'est une étape du job `gates`, jamais un check requis (demande de Samuel, WhatsApp, 2026-09-23 : le tag arrive après le merge) (D-04, D-M4)"
    - "`.github/rulesets/main.json` : cible `refs/heads/main`, actif, règles exactement deletion + non_fast_forward (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) + pull_request (0 approbation, revue code owner requise, merge/squash/rebase) + required_status_checks (4 contextes, `integration_id` 15368, strict) ; `bypass_actors` = exactement DEUX entrées `User` — 151974738 et 203482067 — en mode `always` (D-02bis), aucune entrée `RepositoryRole`, jamais `exempt` ; ni historique linéaire ni signature (D-03, D-04, D-05, D-10, D-M2, D-M3)"
    - "`.github/rulesets/tags-v.json` : cible `refs/tags/v*`, actif, règles exactement deletion + non_fast_forward + update ; AUCUNE règle `creation` ; mêmes deux `User` en `always` (D-07 ; D-M13 lue avec D-02bis)"
    - "Les identifiants des acteurs de contournement sont lus dans `ACTEURS-CONTOURNEMENT:` (mesurés), jamais retapés ; la relecture des deux entrées `User` côté serveur, après la pose, est faite au plan 41-05 (mesurée, jamais supposée)"
    - "Les deux fichiers sont au format du corps de `POST /repos/{owner}/{repo}/rulesets`, pour être posés octet pour octet depuis `origin/main` (41-05)"
  artifacts:
    - path: ".github/rulesets/main.json"
      provides: "source versionnée du ruleset de branche"
    - path: ".github/rulesets/tags-v.json"
      provides: "source versionnée du ruleset de tags"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "section `## 41-01 — reprise du volet admin` : `IDENTITE-REPRISE:`, `ACTEURS-CONTOURNEMENT:`, `PR29-FERMEE:`, `RULESETS-AVANT:`, `PR-EN-VOL-REPRISE:`, `CONTEXTES-CHECKS-REPRISE:`"
  key_links:
    - "`ACTEURS-CONTOURNEMENT:` de `41-PREUVES.md` → `bypass_actors[].actor_id` des deux JSON ; les vérifications lisent la ligne, jamais une valeur recopiée"
    - "Chemin `.github/rulesets/` choisi pour tomber sous `/.github/` du CODEOWNERS (D-05, plan 41-02) : toute modification ultérieure des sources exige la revue `@picmakpro` ou un contournement tracé"
---

<objective>
Reprendre le volet admin là où il s'était arrêté le 2026-09-17 : re-mesurer les préalables devenus
faux ou périmés, puis écrire la source versionnée des deux rulesets, sans rien activer chez GitHub.

Révisé le 2026-09-23 (reprise, `41-CONTEXT.md` § REPRISE) : la sonde humaine de l'identifiant du
rôle `write` (ancienne Task 2) est SANS OBJET — D-02bis remplace le contournement par rôle par deux
utilisateurs nommés ; la fermeture de #29 est déjà faite (constatée, pas rejouée). La relecture des
deux entrées `User` côté serveur est déplacée après la pose (plan 41-05).

Purpose: PROT-01 exige des règles posées depuis une source versionnée dont chaque valeur sensible
(contextes, intégration, acteurs de contournement) est mesurée, pas supposée.
Output: `.github/rulesets/main.json`, `.github/rulesets/tags-v.json`, section de reprise de `41-PREUVES.md`.

Règles de `main` : force push et suppression interdits hors liste de contournement (arbitrage
Samuel, AskUserQuestion session principale, 2026-09-17, consigné en frontmatter
`REGLES_MAIN_FORCE_PUSH_SUPPRESSION: INCLUSES` et `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE`).
Types de règles exigés dans `main.json`, triés : `deletion`, `non_fast_forward`, `pull_request`,
`required_status_checks`. Tout commit ou corps de PR qui cite D-01..D-10 reprend « arbitrage Samuel,
AskUserQuestion session principale, 2026-09-17 » ; tout commit ou corps qui cite D-02bis reprend
« arbitrage Willy, AskUserQuestion session principale, 2026-09-23 » ; la vérification des messages
rougit sinon.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.github/workflows/ci.yml

Sources de plateforme :
- `https://docs.github.com/en/rest/repos/rules` (lue le 2026-09-17, relue le 2026-09-23 par le
  planificateur) — `bypass_actors[].actor_type` ∈ Integration, OrganizationAdmin, RepositoryRole,
  Team, DeployKey, User ; `OrganizationAdmin` seul inapplicable aux dépôts personnels ; `actor_id`
  requis pour `User` ; `bypass_mode` ∈ always (défaut) / pull_request (branches seulement) / exempt
  (« a bypass audit entry will not be created ») ; `current_user_can_bypass` ∈ always /
  pull_requests_only / never / exempt ; règles `pull_request`, `required_status_checks`,
  `non_fast_forward`, `deletion`, `creation`, `update`.
- Constats du 2026-09-23 (planificateur, lecture `gh api`, 11:25 UTC) : identité `picmakpro` admin ;
  deux collaborateurs ; `rulesets` = `[]` ; #29 `CLOSED` (2026-09-23T10:48:32Z) ; PR #85 et #86
  mergées par `samuel-neveugall` à 11:11 et 11:16 UTC (sans règle serveur) ; PR ouvertes #87 et #88
  (#88 vise `gouvernance/moteur-planning-metier`, pas `main`) ; pointe d'`origin/main` `e429cb1`
  porte les 4 noms de jobs sous 15368, et le job « Suites de tests » y est ROUGE
  (`plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh`, run 35853547367) — rouge
  préexistant, hors phase, qui conditionne les plans 41-04 et 41-05.
- Les check runs de l'application 15368 portent aussi, sur certains commits de `main`, le job du
  workflow planifié `traffic-snapshot` (« Snapshot hebdomadaire … », mesuré sur `5238cba`) : les
  contextes requis sont donc l'INTERSECTION des check runs 15368 et des 4 jobs de `ci.yml`, jamais
  l'ensemble brut des check runs.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : re-mesure à la reprise — identité admin, acteurs de contournement, #29, rulesets vides, PR en vol, contextes de checks</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (§ REPRISE — 2026-09-23, D-02bis ; § Constats « Source des checks » ; D-04, D-M4, D-M5)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (règle du registre ; ligne `CONTEXTES-CHECKS:` du 2026-09-17)
    - .github/workflows/ci.yml (les quatre `name:` de jobs, dont un entre guillemets)
  </read_first>
  <action>
Lecture seule. Vérifier l'identité par `gh api user` (jamais `gh auth status`) : `picmakpro`, et
`permissions.admin` à `true`. `git fetch origin`.

Ajouter à `41-PREUVES.md`, SANS toucher aux sections existantes (la ligne `CONTEXTES-CHECKS:` du
2026-09-17 reste l'historique), une section `## 41-01 — reprise du volet admin (<date d'exécution>)`
portant, chacune mesurée au moment de l'écriture :
- `IDENTITE-REPRISE: login=<login lu> id=<id lu> admin=<valeur lue> source=gh_api_user` ;
- `ACTEURS-CONTOURNEMENT: samuel-neveugall=<id lu> picmakpro=<id lu> collaborateurs=<nombre> source=collaborators` —
  identifiants lus dans `GET /repos/picmakpro/vibeflow-os/collaborators`, jamais recopiés du
  CONTEXT (D-02bis) ;
- `PR29-FERMEE: state=<état lu> closedAt=<iso lu>` (D-08 constaté) ;
- `RULESETS-AVANT: n=<longueur lue>` ;
- `PR-EN-VOL-REPRISE: numeros=<#n,#n,… triés par numéro croissant, ou aucune> detail=<#n:base:tete8,…>` —
  inventaire des PR ouvertes ; aucune n'est commentée, mise à jour, fermée ni mergée par la phase ;
- `CONTEXTES-CHECKS-REPRISE: sha=<sha d'origin/main> n=4 app=15368 ci_inclus_dans_api=<oui|non> egal_2026-09-17=<oui|non> check_release_tag_contexte=<oui|non>` —
  noms relevés par programme sur les check runs 15368 de la pointe d'`origin/main` (tri unique),
  intersectés avec les 4 `name:` de jobs de `ci.yml` (guillemets YAML retirés) : les 4 jobs doivent
  tous y figurer ; l'intersection doit être égale à celle calculée de la même façon sur
  `5238cba66c0d878a9ff4eb1c32757c21c64b454d` (mesure du 2026-09-17). Un check run hors `ci.yml`
  (job planifié `traffic-snapshot`) est ignoré, jamais retenu comme contexte requis.
Si la pointe n'a pas ses quatre check runs, attendre et relire ; jamais compléter à la main. Si un
collaborateur a été ajouté ou retiré depuis le 2026-09-23, ou si un ruleset existe déjà : consigner
tel quel, arrêt et remontée (les prémisses de D-02bis ont changé).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'u=$(gh api user --jq .login); a=$(gh api repos/picmakpro/vibeflow-os --jq .permissions.admin); c=$(gh api repos/picmakpro/vibeflow-os/collaborators --jq "[.[] | \"\(.login)=\(.id)\"] | sort | join(\",\")"); echo "reprise : identite=$u admin=$a collaborateurs=$c"; [ "$u" = picmakpro ] && [ "$a" = true ] && [ "$c" = "picmakpro=203482067,samuel-neveugall=151974738" ]'</automated>
    <fails_when>exit non nul ; ou identité autre que `picmakpro` ; ou `admin=` différent de `true` ; ou liste des collaborateurs différente des deux comptes mesurés le 2026-09-23 (un accès ajouté ou retiré change les prémisses de D-02bis : arrêt, remontée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; s=$(gh pr view 29 --repo picmakpro/vibeflow-os --json state --jq .state); n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); ia=$(gh api users/samuel-neveugall --jq .id); ib=$(gh api users/picmakpro --jq .id); sa=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); pa=$(val ACTEURS-CONTOURNEMENT: picmakpro); il=$(val IDENTITE-REPRISE: login); ad=$(val IDENTITE-REPRISE: admin); l29=$(awk "\$1==\"PR29-FERMEE:\" && \$2==\"state=CLOSED\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); lr=$(val RULESETS-AVANT: n); ev=$(val PR-EN-VOL-REPRISE: numeros); ap=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq "[.[].number] | sort | map(\"#\" + tostring) | join(\",\")"); echo "reprise consignee : pr29=$s lignes_pr29=$l29 rulesets=$n consigne=$lr samuel=$ia/$sa picmakpro=$ib/$pa identite=$il admin=$ad pr_en_vol=$ev api=${ap:-aucune}"; [ "$s" = CLOSED ] && [ "$l29" -eq 1 ] && [ "$n" -eq 0 ] && [ "$lr" = 0 ] && [ "$sa" = "$ia" ] && [ "$pa" = "$ib" ] && [ "$il" = picmakpro ] && [ "$ad" = true ] && [ "$ev" = "${ap:-aucune}" ]'</automated>
    <fails_when>exit non nul ; ou #29 non `CLOSED`, ou `PR29-FERMEE:` absente ou dupliquée ; ou `rulesets=` différent de 0 (un ruleset existe déjà : arrêt, remontée) ou consigné différent ; ou un identifiant consigné différent de celui rendu par `GET /users/<login>` (identifiant retapé) ; ou identité ou admin consignés différents de la mesure ; ou numéros de `PR-EN-VOL-REPRISE:` différents des PR ouvertes relues (inventaire périmé : le réécrire)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); S=$(git rev-parse origin/main); O=5238cba66c0d878a9ff4eb1c32757c21c64b454d; gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/api"; gh api "repos/picmakpro/vibeflow-os/commits/$O/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/old"; awk "/^jobs:/{j=1; next} j && /^  [A-Za-z0-9_-]+:[ \t]*\$/{k=1; next} j && k && /^    name:/{sub(/^    name:[ \t]*/, \"\"); gsub(/^\"|\"\$/, \"\"); print; k=0}" .github/workflows/ci.yml | LC_ALL=C sort -u > "$T/ci"; LC_ALL=C comm -12 "$T/api" "$T/ci" > "$T/in"; LC_ALL=C comm -12 "$T/old" "$T/ci" > "$T/io"; ni=$(awk "END{print NR}" "$T/in"); nc=$(awk "END{print NR}" "$T/ci"); eo=$(cmp -s "$T/in" "$T/io" && echo oui || echo non); rt=$(awk "index(tolower(\$0), \"check-release-tag\") {c++} END{print c+0}" "$T/in"); l=$(awk -v s="$S" "\$1==\"CONTEXTES-CHECKS-REPRISE:\" && \$2==\"sha=\" s && /ci_inclus_dans_api=oui/ && /egal_2026-09-17=oui/ && /check_release_tag_contexte=non/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "contextes reprise : sha=$S jobs_ci=$nc jobs_ci_presents_dans_api=$ni intersection_egale_2026-09-17=$eo contexte_release_tag=$rt ligne_preuve=$l"; [ "$nc" -eq 4 ] && [ "$ni" -eq 4 ] && [ "$eo" = oui ] && [ "$rt" -eq 0 ] && [ "$l" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `jobs_ci=` différent de 4 ; ou `jobs_ci_presents_dans_api=` différent de 4 (check runs absents ou incomplets sur la pointe, ou un nom de job diffère — tiret, guillemet) ; ou `intersection_egale_2026-09-17=non` (un job a été renommé depuis le cadrage : arrêt, remontée) ; ou `contexte_release_tag=` différent de 0 (un check nommé `check-release-tag` rendrait le merge impossible avant le tag — contraire à la demande de Samuel, WhatsApp, 2026-09-23) ; ou `ligne_preuve=` différent de 1 (ligne absente, dupliquée, ou portant un autre sha ou d'autres valeurs)</fails_when>
  </verify>
  <acceptance_criteria>
    - identité `picmakpro`, admin `true`, collaborateurs = exactement `samuel-neveugall` (151974738) et `picmakpro` (203482067), lus par l'API
    - `41-PREUVES.md` porte une section de reprise avec `IDENTITE-REPRISE:`, `ACTEURS-CONTOURNEMENT:`, `PR29-FERMEE:`, `RULESETS-AVANT:`, `PR-EN-VOL-REPRISE:`, `CONTEXTES-CHECKS-REPRISE:`, chacune recoupée par une lecture de l'API
    - 4 contextes égaux aux check runs courants, à `ci.yml` et à la mesure du 2026-09-17 ; aucun contexte `check-release-tag`
    - aucune écriture chez GitHub ; sections antérieures de `41-PREUVES.md` intactes
  </acceptance_criteria>
  <done>Les prémisses de D-02bis et de la pose sont mesurées à la date de la reprise, et rien n'a bougé chez GitHub.</done>
</task>

<task type="auto">
  <name>Task 2 : écrire les deux JSON depuis les valeurs mesurées (deux `User` en `always`), rejouer les gates</name>
  <files>.github/rulesets/main.json, .github/rulesets/tags-v.json</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`ACTEURS-CONTOURNEMENT:`, `CONTEXTES-CHECKS-REPRISE:`)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (D-02bis, D-03, D-04, D-05, D-07, D-10, D-M2, D-M3, D-M4, D-M13)
  </read_first>
  <action>
Écrire `.github/rulesets/main.json` en générant la liste des contextes avec `jq` depuis la sortie
des check runs 15368 de la pointe d'`origin/main`, intersectée avec les 4 jobs de `ci.yml` (même
filtre que la Task 1, jamais une liste tapée) :
- `name` « main — PR obligatoire, 4 checks requis, revue code owner (Phase 41) », `target` branch,
  `enforcement` active ;
- `conditions.ref_name` include `refs/heads/main`, exclude vide (nom explicite plutôt que
  `~DEFAULT_BRANCH`, pour que la cible ne glisse pas en silence si la branche par défaut changeait) ;
- `rules` = `deletion` ; `non_fast_forward` (arbitrage Samuel, AskUserQuestion session principale,
  2026-09-17) ; `pull_request` avec `required_approving_review_count` 0 (D-03),
  `require_code_owner_review` true (D-05), `allowed_merge_methods` merge, squash, rebase (D-10),
  `dismiss_stale_reviews_on_push` false, `require_last_push_approval` false,
  `required_review_thread_resolution` false (valeurs par défaut : aucune règle non arbitrée ;
  écarter les approbations périmées reste un renforcement NON retenu, écrit dans ADR-072 au plan
  41-03) ; `required_status_checks` avec les 4 contextes chacun à `integration_id` 15368 (D-M4),
  `strict_required_status_checks_policy` true (D-04), `do_not_enforce_on_create` false ;
- `bypass_actors` = deux entrées, triées par `actor_id` croissant, chacune `actor_type` `User`,
  `bypass_mode` `always`, `actor_id` lu dans `ACTEURS-CONTOURNEMENT:` (samuel-neveugall puis
  picmakpro) — D-02bis : aucune entrée `RepositoryRole`, jamais `exempt` (pas de trace), jamais
  `pull_request` (D-M12 est remplacée par D-02bis).
Aucune règle `required_linear_history` (D-M2), `required_signatures` (D-M3), `update` ni `creation`.

Écrire `.github/rulesets/tags-v.json` :
- `name` « tags v* — réécriture et suppression tracées, création libre (Phase 41) », `target` tag,
  `enforcement` active ; include `refs/tags/v*`, exclude vide ;
- `rules` = `deletion`, `non_fast_forward`, `update` avec `update_allows_fetch_and_merge` false, et
  AUCUNE règle `creation` (D-07) ;
- `bypass_actors` = les deux mêmes entrées `User` en `always` (D-02bis ; D-M13 lue avec D-02bis).

Les deux fichiers : JSON indenté par `jq`, sans champ serveur (`id`, `source`, `_links`…), sans
chemin de machine. Ne rien poser chez GitHub. Commit local sur la branche A, jamais poussé, message
« feat(41-01): sources JSON des rulesets de branche et de tags (D-03, D-04, D-05, D-07, D-10 —
arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ; D-02bis — arbitrage Willy,
AskUserQuestion session principale, 2026-09-23) ; REGLES_MAIN_FORCE_PUSH_SUPPRESSION=INCLUSES —
arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ». Committer aussi la section de
reprise de `41-PREUVES.md` (Task 1) dans ce commit ou un commit précédent du plan. QUAL-01 : aucun
script de gate créé (D-M11).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=.github/rulesets/main.json; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; SA=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); PA=$(val ACTEURS-CONTOURNEMENT: picmakpro); case "$SA$PA" in ""|*[!0-9]*) echo "json-branche : acteurs_mesures=absents"; exit 1;; esac; o=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | {nom: ((.name | type)==\"string\" and (.name | length) > 0), cible: (.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"] and .conditions.ref_name.exclude==[]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]), approbations0: (\$r.pull_request.required_approving_review_count==0), code_owner: (\$r.pull_request.require_code_owner_review==true), stale_defaut_false: (\$r.pull_request.dismiss_stale_reviews_on_push==false), last_push: (\$r.pull_request.require_last_push_approval==false), threads: (\$r.pull_request.required_review_thread_resolution==false), methodes: ((\$r.pull_request.allowed_merge_methods | sort)==[\"merge\",\"rebase\",\"squash\"]), strict: (\$r.required_status_checks.strict_required_status_checks_policy==true), checks4: ((\$r.required_status_checks.required_status_checks | length)==4), integration: ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), release_tag_non_requis: ([\$r.required_status_checks.required_status_checks[].context | ascii_downcase | contains(\"check-release-tag\")] | any | not), bypass_deux_users_always: ((.bypass_actors | sort_by(.actor_id)) == ([{actor_id: \$sa, actor_type: \"User\", bypass_mode: \"always\"}, {actor_id: \$pa, actor_type: \"User\", bypass_mode: \"always\"}] | sort_by(.actor_id)))} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$B"); echo "json-branche : $o"; case "$o" in *=false*) exit 1;; esac'</automated>
    <fails_when>exit non nul (JSON invalide compris) ; ou un champ `=false` dans la ligne `json-branche :` — en particulier `bypass_deux_users_always=false` (entrée `RepositoryRole` restante, mode `pull_request` ou `exempt`, acteur en trop ou manquant, identifiant différent de `ACTEURS-CONTOURNEMENT:` — D-02bis), `types=false` (règle manquante, en trop, dupliquée, historique linéaire ou signature ajoutés, ou l'un des quatre types exigés — dont `deletion` et `non_fast_forward`, arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 — absent), `stale_defaut_false=false` (renforcement non arbitré posé), `integration=false`, `methodes=false` (restriction non arbitrée), `release_tag_non_requis=false` ; ou `acteurs_mesures=absents`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; G=.github/rulesets/tags-v.json; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; SA=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); PA=$(val ACTEURS-CONTOURNEMENT: picmakpro); case "$SA$PA" in ""|*[!0-9]*) echo "json-tags : acteurs_mesures=absents"; exit 1;; esac; o=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "{nom: ((.name | type)==\"string\" and (.name | length) > 0), cible: (.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"] and .conditions.ref_name.exclude==[]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), update_param: ([.rules[] | select(.type==\"update\") | .parameters.update_allows_fetch_and_merge] == [false]), bypass_deux_users_always: ((.bypass_actors | sort_by(.actor_id)) == ([{actor_id: \$sa, actor_type: \"User\", bypass_mode: \"always\"}, {actor_id: \$pa, actor_type: \"User\", bypass_mode: \"always\"}] | sort_by(.actor_id)))} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$G"); echo "json-tags : $o"; case "$o" in *=false*) exit 1;; esac'</automated>
    <fails_when>exit non nul ; ou un champ `=false` — `types=false` couvre une règle `creation` ajoutée (contraire à D-07) ou une règle manquante ; `bypass_deux_users_always=false` couvre un mode autre que `always`, une entrée `RepositoryRole`, un acteur en trop ou un identifiant différent de la mesure (D-02bis)</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=.github/rulesets/main.json; T=$(mktemp -d); S=$(git rev-parse origin/main); gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/api"; jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$B" | LC_ALL=C sort -u > "$T/json"; awk "/^jobs:/{j=1; next} j && /^  [A-Za-z0-9_-]+:[ \t]*\$/{k=1; next} j && k && /^    name:/{sub(/^    name:[ \t]*/, \"\"); gsub(/^\"|\"\$/, \"\"); print; k=0}" .github/workflows/ci.yml | LC_ALL=C sort -u > "$T/ci"; LC_ALL=C comm -12 "$T/api" "$T/ci" > "$T/in"; ni=$(awk "END{print NR}" "$T/in"); nj=$(awk "END{print NR}" "$T/json"); nc=$(awk "END{print NR}" "$T/ci"); ja=$(cmp -s "$T/in" "$T/json" && echo oui || echo non); jc=$(cmp -s "$T/json" "$T/ci" && echo oui || echo non); echo "contextes : sha=$S api_inter_ci=$ni json=$nj ci=$nc json_egal_api=$ja json_egal_ci=$jc"; [ "$ni" -eq 4 ] && [ "$nj" -eq 4 ] && [ "$nc" -eq 4 ] && [ "$ja" = oui ] && [ "$jc" = oui ]'</automated>
    <fails_when>exit non nul ; ou un des trois comptes différent de 4 ; ou `json_egal_api=non` / `json_egal_ci=non` (un contexte retapé ou altéré — tiret demi-cadratin, guillemet, `#38` — ou un check run hors `ci.yml` retenu comme contexte)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash scripts/check-machine-paths.sh > /dev/null 2>&1 || rc=$?; echo "check-machine-paths rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul : un chemin de machine est entré dans un fichier suivi (JSON, registre de preuves)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N égal à 0 ; ou N différent de `decouverte_independante` (découverte tronquée) ; ou M différent de 0. Un rouge qui se reproduit sur une extraction `git archive` d'`origin/main` est préexistant : consigné et remonté `human_needed`, jamais neutralisé ni déclaré vert</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` (un commit de plan cite D-02bis sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 ») ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » égal aux valeurs CAPTURÉES — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - types de règles de `main.json` = `deletion`, `non_fast_forward`, `pull_request`, `required_status_checks` (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17)
    - `main.json` et `tags-v.json` conformes champ par champ (`dismiss_stale_reviews_on_push` à `false`) ; `bypass_actors` = deux `User` en `always`, identifiants issus de `ACTEURS-CONTOURNEMENT:` (D-02bis) ; contextes identiques aux check runs ET à `ci.yml`, aucun contexte `check-release-tag`
    - tout commit de plan citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » ; tout commit citant D-02bis nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 »
    - `check-machine-paths` rc 0 ; rejeu `gates` rc 0 (≥ 10 étapes) ; rejeu `tests` rc 0, bilan N = découverte indépendante, M = 0
    - aucun ruleset posé ; aucun script de gate créé (D-M11)
  </acceptance_criteria>
  <done>Les deux sources JSON existent, chacune de leurs valeurs sensibles est mesurée, et rien n'est actif chez GitHub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → API GitHub | le jeton de la session est admin : ce plan n'émet que des `GET` ; aucune écriture chez GitHub |
| source versionnée → règle posée | une valeur fausse dans le JSON devient une règle fausse sur `main` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Spoofing | contextes de checks requis | high | mitigate | `integration_id` 15368 exigé sur les 4 contextes (vérif `integration=true`) : un statut posé par un autre compte sous le même nom ne satisfait pas le check |
| T-41-02 | Tampering | noms de contextes retapés | high | mitigate | liste générée par `jq` depuis les check runs ; égalité exigée avec l'API, avec `ci.yml` et avec la mesure du 2026-09-17 |
| T-41-03 | Elevation of privilege | `bypass_actors` | high | mitigate | exactement deux `User` nommés en `always` (D-02bis), identifiants mesurés par l'API ; `RepositoryRole` et `exempt` rejetés (vérif `bypass_deux_users_always`) ; relecture serveur après la pose (41-05) |
| T-41-04 | Elevation of privilege | jeton admin de la session | high | mitigate | ce plan n'appelle que des `GET` ; l'unique écriture de configuration de la phase (la pose) est au plan 41-05, derrière un checkpoint de décision |
| T-41-05 | Tampering | collaborateur ajouté entre la mesure et la pose | medium | mitigate | liste des collaborateurs exigée égale aux deux comptes ici, et relue au plan 41-05 avant la pose |
| T-41-06 | Tampering | approbation code owner périmée couvrant un push ultérieur | medium | accept | `dismiss_stale_reviews_on_push` laissé au défaut `false` : aucun arbitrage ne couvre le renforcement ; écrit dans ADR-072 comme renforcement possible non retenu ; valeur vérifiée dans la source et côté serveur |
| T-41-07 | Information disclosure | chemin de machine dans un fichier suivi | low | mitigate | `check-machine-paths` rejoué |
| T-41-57 | Denial of service | `check-release-tag` devenu check requis | high | mitigate | aucun contexte `check-release-tag` (vérifs `contexte_release_tag=0`, `release_tag_non_requis=true`) — demande de Samuel, WhatsApp, 2026-09-23 |
</threat_model>

<verification>
Préalables de la reprise re-mesurés et consignés ; deux JSON conformes aux arbitrages (D-02bis
compris), acteurs et contextes mesurés ; gates et tests rejoués verts ; aucun ruleset actif.
</verification>

<success_criteria>
La source de PROT-01 est prête à être mergée : chaque valeur des rulesets est prouvée, rien n'est
encore imposé à aucune PR.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-01-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-volet-admin`). Le SUMMARY dit que l'ancienne Task 2 (sonde du rôle `write`) est sans objet depuis D-02bis et que la fermeture de #29 a été constatée, pas rejouée.
</output>

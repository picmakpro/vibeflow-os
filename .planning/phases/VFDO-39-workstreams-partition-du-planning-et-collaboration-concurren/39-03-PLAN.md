---
phase: VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren
plan: 03
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PART-03, PART-07, PART-08, PART-09]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A disposable clone of vibeflow-os, carrying real history, is partitioned and at least one vf-* agent dispatched against it passes --ws explicitly on every gsd_run call, observed by the EFFECT it produces on the filesystem (a proof-of-scope artifact landing in the right compartment with content matching the controller's own independent --ws re-run), never by trusting a PATH-based interception or the worker's self-reported transcript — zero exceptions (D-08)."
    - "The existing gates (check-workstream-pointer.sh, check-state-integrity.sh, check-divergence.sh) are green on the disposable partitioned clone AND unchanged (silence / conforme) on the real, untouched working tree (non-regression)."
    - "The clone is destroyed at the end of the task; the plan's SUMMARY explicitly states the mechanism-proof-not-real-usage nuance (D-03); the real partition of vibeflow-os is inscribed as a separate, dated, human-gated future trigger — never executed by this phase."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-03-SUMMARY.md
  key_links:
    - "workstream create scratch --migrate-name legacy -> the clone's own real 39-phase history relocated intact (files_moved verified against research §5's exact recipe)"
    - "dispatched vf-coder subagent -> writes .ws-proof-*.md into its OWN compartment, content matching the controller's own independent --ws-scoped re-run (D-08's exhaustive, zero-exception bar, proven by effect not by transcript)"
---

<objective>
Produce the one piece of evidence Success Criterion 3 demands and no other plan in this phase can
give it: a disposable clone of `vibeflow-os` itself — real history, not a synthetic fixture — is
partitioned into workstreams, at least one `vf-*` agent is dispatched against it and observed
passing `--ws` on every single engine call it makes, and the phase's own three gates
(`check-workstream-pointer.sh`, `check-state-integrity.sh`, `check-divergence.sh` from plan
`39-01`) are proven green there while staying unchanged on the real, untouched working tree. Then
destroy the clone, and inscribe the real partition of `vibeflow-os` as a separate, dated,
human-gated future decision — never executed here (D-02).

Purpose: this is the ONLY place in the phase where the paradox of D-01/D-02/D-03 is actually lived
— proving the mechanism without ever touching the working tree it will one day apply to.
Output: an execution-based proof (not a frontmatter inspection) of `--ws` coverage, plus the D-02
resumption trigger and D-06 event-watch note durably written into `STATE.md`/`ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
@.planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md
@plugin/dev-orchestrator/agents/vf-coder.md
@plugin/dev-orchestrator/references/workstreams.md
@plugin/conductor/scripts/check-divergence.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone, partition with real history, dispatch TWO --ws-explicit vf-coder workers concurrently (D-08, D-10, C4 retained / D2 effect-based proof replacing the inert shim, corrected 2026-09-10), prove the three gates</name>
  <precondition>gsd-core CLI resolves at `~/.claude/gsd-core/bin/gsd-tools.cjs` and its `workstream create` subcommand is available (`node ~/.claude/gsd-core/bin/gsd-tools.cjs workstream create` prints the `workstream name required` usage line, not `Unknown command`).</precondition>
  <reversibility rating="one-way">Le clone jetable est détruit en fin de mission (D-01, CONTEXT.md) ; la preuve qu'il porte (mécanisme validé) ne se rejoue pas sans reconstruire un clone équivalent. Décision déjà rendue par Samuel au cadrage (option A′, 2026-09-09) — la porte est déjà franchie, ce tag garde la trace, aucun checkpoint à ré-ouvrir. **Documenté comme déviation assumée** (pas un `type="checkpoint:decision"` manquant par oubli — un choix déjà tranché, cité ici avec sa date).</reversibility>
  <read_first>
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    D-01, D-02, D-03, D-08, D-10 verbatim (the paradox rule this task lives inside: every partition
    operation below happens exclusively inside the disposable clone, never in this repo's own
    working tree);
    .planning/research/2026-09-09-phase-39-workstreams-mesures-de-cadrage.md §3 (session-key
    collision — why exporting GSD_WORKSTREAM and passing --ws explicitly both matter) and §5 (the
    exact migration recipe: `files_moved` must contain `ROADMAP.md`, `STATE.md`, `REQUIREMENTS.md`,
    `phases`);
    plugin/dev-orchestrator/agents/vf-coder.md:43-51 (the "Compartiment de planning" section — the
    dispatched agent's own documented obligation to pass `--ws` explicitly, quoted into its mandate
    below);
    plugin/dev-orchestrator/agents/vf-dev-manager.md's dispatch-gabarit amendment (delivered by plan
    `39-02` Task 4, C4 correction 2026-09-10 — the `GSD_SESSION_KEY`-per-mandate discipline this
    task exercises with real concurrent workers, not merely cites);
    plugin/dev-orchestrator/references/workstreams.md §3 ("La règle du lab — dans un worktree, on
    EXPORTE GSD_WORKSTREAM" — deliberately NOT applied as a single global export in this task: two
    concurrent workers need two different names, which a single shared export cannot represent —
    see the corrected dispatch below);
    plugin/conductor/scripts/check-divergence.sh (delivered by plan `39-01` — this task's
    `depends_on` exists specifically so this file is present to test).
  </read_first>
  <files>(none in this repo — all writes happen inside a disposable clone under mktemp, destroyed by Task 2)</files>
  <action>
    Create a disposable clone under `mktemp -d`: `CLONE="$(mktemp -d)/vibeflow-os-ws-proof"; git
    clone -q --local --no-hardlinks /Users/samuel/Documents/dev/vibeflow-os "$CLONE"` — real
    history, never this repo's own working tree. Every step below operates exclusively inside
    `$CLONE`; no step in this task creates `.planning/workstreams/` anywhere under
    `/Users/samuel/Documents/dev/vibeflow-os` itself (the phase-wide paradox rule — D-01/D-02/D-03).

    Partition it: `node ~/.claude/gsd-core/bin/gsd-tools.cjs --cwd "$CLONE" workstream create
    scratch --migrate-name legacy`. Per `workstream.cjs`'s migration path this moves the clone's OWN
    `ROADMAP.md`, `STATE.md`, `REQUIREMENTS.md`, `phases/` — its real ~39-phase history — into
    `.planning/workstreams/legacy/` (D-01's "histoire réelle"), and creates an empty
    `.planning/workstreams/scratch/` alongside. Parse the command's JSON output and confirm
    `files_moved` contains all four of `ROADMAP.md`, `STATE.md`, `REQUIREMENTS.md`, `phases` exactly
    as research §5 measured — halt and report if any is missing, this would mean the migration
    behaved differently from what the phase's own research established.

    **D2 correction (2026-09-10, third pass) — the PATH/argv-shim interception mechanism from the
    prior correction pass is INERT on this machine and is REPLACED, not patched.** Measured
    2026-09-10, three independent ways: (1) a shell's exported `PATH` does not survive from one Bash
    tool call to the next — `export VF_PROBE=hello` in one call, `VF_PROBE=[VIDE]` (empty) in the
    next — so `export PATH="$SHIMDIR:$PATH"` "as your first bash call" is gone by the worker's
    SECOND call, and `GSD_SESSION_KEY` set the same way vanishes with it, voiding the per-mandate
    isolation this task claims to exercise too; (2) `gsd_run` does not resolve on this machine's
    `PATH` at all (`command -v gsd_run` → exit 1) — real calls use the absolute path
    `~/.claude/gsd-core/bin/gsd_run`, which bypasses any `PATH`-based shim entirely, shim-reached vs.
    absolute-path-reached measured to produce a 1-line journal vs. a 0-byte one respectively; (3) a
    `PATH` shim only ever covers the one binary it wraps (`gsd_run`), never `gsd-tools`, which this
    same mandate explicitly permits workers to call directly. An interception mechanism this leaky
    cannot be trusted to prove an EXHAUSTIVE, zero-exception claim (D-08) — silence from it would be
    read as "the shim was never reached," genuinely indistinguishable from "the flag was omitted,"
    which is precisely the vacuous-pass the phase's own doctrine forbids.

    **Replacement strategy: prove `--ws` by its EFFECT on the filesystem, not by intercepting the
    command line that produced it.** This needs no `PATH`, no shim, and survives the
    no-env-persistence constraint above because every command below is self-contained (arguments
    passed inline, nothing relies on a previous call's exports). Two structural facts make this
    effect observable and asymmetric, confirmed by direct execution on 2026-09-10 against a disposable
    fixture built with the exact `workstream create ... --migrate-name legacy` recipe this task
    already uses: `gsd-tools progress --ws legacy` returns the compartment's real, migrated phase
    list (non-empty, e.g. `"phases": [...]`, `"phase_scope": "complete"`), while `gsd-tools progress
    --ws scratch` returns an EMPTY compartment (`"phases": []`, `"phase_scope": "unreadable"`) — a
    freshly created, never-populated workstream. A worker whose `--ws` silently leaked to the other
    compartment's pointer (D-10's exact collision) would observe — and, if it dutifully transcribes
    what it saw, WRITE — the WRONG one of these two asymmetric shapes; there is no way to fake the
    correct pairing without the flag genuinely having been honored.

    **C4 correction (2026-09-10, retained from the prior pass) — dispatch TWO concurrent workers on
    two DIFFERENT compartments, deliberately WITHOUT exporting a single `GSD_WORKSTREAM` in any
    shared/parent shell** (a single export cannot represent two different compartment names at
    once — this is what actually exercises D-10's OPEN half, not merely cites it). Dispatch
    `vf-coder` Worker A and Worker B via the Task tool, both cwd `$CLONE`, in the SAME turn (parallel
    Task calls) so they share the parent's `CLAUDE_CODE_SESSION_ID` (`CLAUDE_CODE_CHILD_SESSION=1` —
    D-10's collision surface):
      - Worker A's mandate quotes `vf-coder.md:43-51`'s "Compartiment de planning" obligation
        verbatim and adds: "the workstream for THIS mandate is `legacy`. On EVERY `gsd_run`/
        `gsd-tools` invocation, prefix the env var inline on that same command line —
        `GSD_SESSION_KEY=ws-proof-A-legacy gsd-tools ... --ws legacy` — never `export` it in a
        separate call first (exported state does not survive to your next bash call on this
        machine, measured 2026-09-10); pass `--ws legacy` EXPLICITLY on every single call, never
        relying on any environment variable to carry the workstream name (D-08's exhaustive,
        zero-exception bar). After completing the read-only 'Sources de connaissance' steps your own
        agent file already prescribes (using `gsd-tools progress --ws legacy` for at least one of
        them), write the LITERAL, unparaphrased output of that command to a new file at
        `.planning/workstreams/legacy/.ws-proof-A.md` inside this clone, using your own Write/Edit
        tool — this is the artifact the controller will inspect."
      - Worker B's mandate is identical except: workstream `scratch`, `GSD_SESSION_KEY=ws-proof-B-scratch`
        prefixed inline the same way, `--ws scratch` on every call, `gsd-tools progress --ws scratch`
        as its read step, artifact written to `.planning/workstreams/scratch/.ws-proof-B.md`.
    Neither worker is given `GSD_WORKSTREAM` in its environment at all — proving the `--ws` flag
    itself carries the compartment, not an inherited export (the exact confound the 2026-09-10
    correction closes: a single-worker, single-export design could not distinguish "the flag works"
    from "the export works").

    After both workers return, the CONTROLLER (this task's own top-level shell — never delegated to
    either worker) performs the load-bearing check, entirely independent of what either worker
    self-reports:
      1. `.planning/workstreams/legacy/.ws-proof-A.md` exists AND
         `.planning/workstreams/scratch/.ws-proof-B.md` exists — absence of either is an immediate
         FAIL (D-08's "no vacuous pass" bar: a missing artifact must never read as success).
      2. Neither artifact leaked into the OTHER compartment:
         `.planning/workstreams/scratch/.ws-proof-A.md` must NOT exist;
         `.planning/workstreams/legacy/.ws-proof-B.md` must NOT exist.
      3. Content cross-check, run by the controller itself (never trusting the worker's transcript):
         re-run `gsd-tools progress --ws legacy` and `gsd-tools progress --ws scratch` against
         `$CLONE` right there, and confirm Worker A's artifact content matches the controller's own
         `--ws legacy` output's distinguishing shape (non-empty `phases`) while Worker B's artifact
         matches the controller's own `--ws scratch` output's distinguishing shape (`"phases": []`).
         A worker whose `--ws` had leaked to the other's pointer would have written the SWAPPED
         shape — this is what actually catches the collision, not merely the file's existence.
    The prior pass's argv-journal idea is KEPT ONLY as optional, non-blocking corroboration (a
    worker MAY additionally echo its own command lines to a file it controls) — it must never be the
    sole or authoritative signal, and its absence or emptiness must NOT fail this task; only the
    artifact-and-content check above is authoritative. State this explicitly in the SUMMARY.

    Still inside `$CLONE`, run the three gates and record their exit codes: `check-workstream-pointer.sh
    --path "$CLONE"` (expect exit 0) ; `check-state-integrity.sh --path "$CLONE"` (expect exit 0,
    resolving `workstreams/legacy/STATE.md` per its own GSDA-13 default-resolution logic) ;
    `check-divergence.sh --path "$CLONE"` (expect exit 0 — the migration itself must not have
    introduced a duplicate phase number or cardinality mismatch).

    As a READ-ONLY non-regression check against the REAL working tree (never writing to it — this
    is the one place this task touches the live repo, and only to read), run the same three gates
    with `--path /Users/samuel/Documents/dev/vibeflow-os`: expect `check-workstream-pointer.sh` exit
    3 (silence, non-partitioned — its documented state 0) and `check-divergence.sh` exit 3 (same
    convention, from plan `39-01`), `check-state-integrity.sh` exit 0 (conforme, behavior
    unchanged from before this phase).
  </action>
  <verify>
    <automated>bash -c 'set -eu
C="$(mktemp -d)/proof"
git clone -q --local --no-hardlinks /Users/samuel/Documents/dev/vibeflow-os "$C"
OUT=$(node ~/.claude/gsd-core/bin/gsd-tools.cjs --cwd "$C" workstream create scratch --migrate-name legacy)
ok=1
for f in "ROADMAP.md" "STATE.md" "REQUIREMENTS.md" "phases"; do
  echo "$OUT" | grep -qF "\"$f\"" || ok=0
done
rc=1
if [ "$ok" -eq 1 ]; then
  bash plugin/conductor/scripts/check-divergence.sh --path "$C" && rc=0 || rc=$?
fi
rm -rf "$(dirname "$C")"
if [ "$ok" -eq 1 ] && [ "$rc" -eq 0 ]; then echo MIGRATION_AND_GATE_OK; else echo "FAIL ok=$ok rc=$rc"; fi'</automated>
    <fails_when>prints FAIL (any of the four `files_moved` entries missing from the migration's JSON output, or `check-divergence.sh` exits non-zero on a freshly migrated clone that introduced no real divergence)</fails_when>
  </verify>
  <done>The clone's real history is migrated intact (files_moved verified); TWO vf-coder workers dispatched concurrently to two different compartments (`legacy`, `scratch`), sharing the same `CLAUDE_CODE_SESSION_ID` but each with a distinct `GSD_SESSION_KEY` prefixed inline per call and no `GSD_WORKSTREAM` export, each write a proof-of-scope artifact into their OWN compartment whose content matches what the controller's own independent `--ws`-scoped read produces for that compartment (never the workers' own transcript or a PATH-based interception); all three gates are green on the clone and read unchanged (3/3/0) on the live, untouched working tree.</done>
  <acceptance_criteria>
    - `workstream create`'s JSON output names all four expected `files_moved` entries.
    - `.planning/workstreams/legacy/.ws-proof-A.md` AND `.planning/workstreams/scratch/.ws-proof-B.md` both exist at the end of the task — either missing is a FAILURE, never a vacuous pass.
    - Neither artifact leaked into the other's compartment (`.../scratch/.ws-proof-A.md` and `.../legacy/.ws-proof-B.md` both absent).
    - The controller's own independent `gsd-tools progress --ws legacy` / `--ws scratch` re-run against `$CLONE` confirms Worker A's artifact carries the populated-compartment shape and Worker B's artifact carries the empty-compartment shape — checked against the controller's own tool output, not the workers' self-reported command lines.
    - `check-workstream-pointer.sh --path "$CLONE"` exits 0; the same script `--path
      /Users/samuel/Documents/dev/vibeflow-os` exits 3.
    - `check-divergence.sh --path "$CLONE"` exits 0; the same script `--path
      /Users/samuel/Documents/dev/vibeflow-os` exits 3.
    - No `.planning/workstreams/` directory exists anywhere under this repo's real working tree at
      any point during this task.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Destroy the clone, inscribe D-02's dated trigger and D-06's event-watch, state the mechanism-not-usage nuance</name>
  <read_first>
    .planning/STATE.md § Roadmap Evolution / § Decisions (the most recent 2026-09-09 entries — the
    exact insertion style and dating convention to match);
    .planning/ROADMAP.md § Phase 39 (the "Plans:" line or Success Criteria block — the insertion
    point for the one-line cross-reference);
    .planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-CONTEXT.md
    D-02, D-03, D-06 verbatim;
    .planning/REQUIREMENTS.md PART-08 and PART-09 bullets (from plan `39-02` — cross-reference
    wording to stay consistent with, not contradict);
    .planning/ROADMAP.md § Phase 39, Success Criterion 6 — already amended directly by the
    2026-09-10 correction pass (C9) with a "superseded par D-04" note; this task's own ROADMAP edit
    targets the separate "Plans:" line and must not re-touch or duplicate that criterion-6 note.
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Confirm the clone from Task 1 no longer exists — if Task 1 was executed step-by-step rather than
    solely via its `<verify>` one-liner (which already `rm -rf`s its own clone), `rm -rf` the
    leftover clone directory now and confirm with `[ ! -d "$CLONE" ]`. Never leave a partitioned
    tree on disk anywhere after this plan finishes.

    Using `Edit` (scoped) on `.planning/STATE.md` — never any `gsd-tools state.*` mutating command,
    which is explicitly out of scope for this mission (per this repo's own memory: state
    record-session is destructive) — append to the `### Decisions` section, above the existing
    2026-09-09 entries, in their exact prose style, two new dated notes:

    1. "Déclencheur de reprise — partition réelle de vibeflow-os (D-02, condition de RAPPEL
       corrigée le 2026-09-10)": l'**acte** de partitionner reste gaté humain, SUR DEMANDE EXPLICITE
       de Samuel — jamais auto-déclenché, jamais exécuté sans lui. Mais un déclencheur qui ne se
       rappelle « que si quelqu'un y repense » est précisément le mode de perte déjà constaté entre
       le 2026-08-04 et le 2026-08-30 (l'adoption s'est perdue faute de rappel) — corrigé ici par une
       **condition de RAPPEL objective et datée**, qui ne fait que resurfacer la question, jamais
       déclencher la partition elle-même : à la prochaine clôture de jalon (`gsd-complete-milestone`
       de `fiabilite-v1.0`) OU dès qu'un manager de mission dispatche réellement ≥ 2 workers `vf-*`
       concurrents sur des scopes disjoints du même dépôt (le besoin mesuré qui justifierait la
       partition — cf. la preuve à 2 workers de ce même plan, Tâche 1) — le premier des deux
       événements qui survient fait remonter explicitement la question à Samuel ; ni l'un ni l'autre
       ne déclenche quoi que ce soit par lui-même (même patron que `WKTR-03` et le déclencheur
       objectif d'ADR-069 : objectif, sans échéance calendaire — mais désormais avec un événement
       concret qui le rend vérifiable, pas seulement mémorisable).
    2. "Veille datée sur ÉVÉNEMENT (D-06)": la prochaine publication de `gsd-core` au-delà de
       1.13.0, qui distribuera `#4455`/`#4456`/`#4225` (fermés le 7-8 septembre 2026, non
       distribués) — à cette publication, re-mesurer K2 en ré-exécutant le script de
       `docs/ADR.md:1992-2002`, jamais sur un seuil chiffré figé (précédent explicite à ne pas
       reproduire : `WKTR-03` désarmée le 2026-09-07, devenue faux positif permanent).

    **D3 correction (2026-09-10, third pass) — this ROADMAP edit is now idempotent, not a blind
    insert.** Measured 2026-09-10: the target line ("Partition réelle de `vibeflow-os` : geste
    séparé, gaté humain, postérieur à cette phase — déclencheur de reprise daté en
    `.planning/STATE.md` § Decisions (D-02, inscrit par le plan 39-03).") ALREADY EXISTS verbatim at
    `.planning/ROADMAP.md` (committed in `HEAD`, added by an earlier correction pass on this same
    phase) — re-adding it blindly would create a byte-identical duplicate paragraph that no
    `grep -c ... -ge 1` check could ever tell apart from the correct, single-copy state. First
    `grep -cF` for the exact line; if it is present exactly once, do NOT insert it again — this
    task's own contribution is then the CONFIRMATION clause below, appended immediately after the
    existing line, which is genuinely new content only this task produces: ` Confirmé par le plan
    39-03 (§Task 2, 2026-09-10) — présence non dupliquée vérifiée.` If the line is somehow absent
    (defensive branch, should not occur given the measurement above), insert both the original line
    (exactly as quoted) AND this confirmation clause together, immediately after the Phase 39
    "Plans:" line (check current content first — if `39-02` has already updated that section, insert
    after whatever it reads at execution time, do not assume the exact byte offset).

    This plan's own `39-03-SUMMARY.md`, written per the standard `<output>` convention, MUST state
    explicitly, close to verbatim: "Cette preuve établit un mécanisme, pas un usage concurrent réel
    sur le dépôt vivant (D-03) — le clone est détruit, aucune trace partitionnée ne subsiste sur
    `vibeflow-os`."
  </action>
  <verify>
    <automated>bash -c 'a=$(grep -c "Déclencheur de reprise" .planning/STATE.md); b=$(grep -c "Veille datée sur ÉVÉNEMENT" .planning/STATE.md); c=$(grep -c "Partition réelle de.*vibeflow-os" .planning/ROADMAP.md); d=$(grep -c "Confirmé par le plan 39-03" .planning/ROADMAP.md); if [ -d .planning/workstreams ]; then leak=LEAKED; else leak=NO_LEAK; fi; [ "$a" -ge 1 ] && [ "$b" -ge 1 ] && [ "$c" -eq 1 ] && [ "$d" -ge 1 ] && [ "$leak" = NO_LEAK ] && echo OK || echo "FAIL a=$a b=$b c=$c d=$d leak=$leak"'</automated>
    <fails_when>prints FAIL — either the D-02 trigger note, the D-06 watch note, or the ROADMAP confirmation clause is missing (`d=0`, meaning this task's own action never ran), the ROADMAP line is duplicated (`c` is not exactly `1`), or `leak=LEAKED` (a `.planning/workstreams/` directory exists in this repo's real working tree — the one outcome this entire phase forbids)</fails_when>
  </verify>
  <done>D-02's dated resumption trigger and D-06's event-based watch are inscribed in STATE.md; ROADMAP.md's pre-existing cross-reference is confirmed present exactly once (not duplicated) and gains this task's own confirmation clause; the clone is destroyed; no `.planning/workstreams/` exists anywhere in the real working tree; the plan's SUMMARY states the mechanism-not-usage nuance explicitly.</done>
  <acceptance_criteria>
    - `.planning/STATE.md` contains both new dated notes, each naming an objective, non-calendar condition.
    - `.planning/ROADMAP.md` § Phase 39 contains the D-02 cross-reference line exactly once (`grep -c` equals `1`, never more) AND the new "Confirmé par le plan 39-03" clause this task introduces (`grep -c` at least `1`) — the confirmation clause is what makes this half of the task falsifiable: omitting the task leaves `d=0`, unlike the pre-existing line alone which is already true before the task runs.
    - `test -d .planning/workstreams` fails (directory does not exist) at the end of this task.
    - `39-03-SUMMARY.md` contains the D-03 mechanism-not-usage sentence, not just an implied nuance.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real working tree -> disposable clone | the clone is a read-only `git clone --local` snapshot; no write path exists back from the clone to the real tree |
| dispatched vf-coder subagent -> disposable clone | the subagent's Bash/tool access is scoped to `$CLONE`'s filesystem; it never receives the real repo's path as its cwd |
| this task -> `rm -rf` of the clone | destructive deletion of a whole directory tree, gated on the path being freshly `mktemp`-created in this same task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-08 | Tampering / Denial of Service | `rm -rf "$TMPD"` destroying the clone | medium | mitigate | `$TMPD`/`$CLONE` is always the literal output of `mktemp -d` captured in this same task, never a user-supplied or relative path, before any `rm -rf` runs against it — guards against a mistyped path deleting unintended content. |
| T-39-09 | Elevation of Privilege | Dispatched `vf-coder` subagent has Bash access inside the clone | low | accept | Scoped to the disposable clone directory only; the clone is destroyed at task end; the subagent is never given the real working tree as its cwd, so there is no write path back to `vibeflow-os` itself. |
| T-39-10 | Tampering | `git clone --local --no-hardlinks` of the live repo | low | mitigate | `--local` clone only ever reads from the real working tree; nothing in this task writes to it except the two read-only gate invocations in Task 1 and the two `Edit` calls in Task 2, both of which are scoped, documented, and outside the clone entirely. |
</threat_model>

<verification>
Run Task 1's `<verify>` command — it must print `MIGRATION_AND_GATE_OK`. Run Task 2's `<verify>`
command — it must print `OK`, and specifically `leak=` must read `NO_LEAK`. Confirm
`.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-03-SUMMARY.md`
exists and contains the literal string "pas un usage concurrent réel".
</verification>

<success_criteria>
- Success Criterion 3 (Phase 39 ROADMAP): at least one real-history clone is partitioned, and every
  `vf-*` agent dispatched against it passes `--ws` with zero exceptions, proven by the observable
  EFFECT on the filesystem (a compartment-scoped artifact whose content the controller independently
  cross-checks) — never by trusting agent frontmatter, a self-reported transcript, or a
  `PATH`-based interception that this repo measured to be inert.
- The paradox rule (D-01/D-02/D-03) is honored literally: nothing in this plan creates
  `.planning/workstreams/` in this repo's own working tree, and the SUMMARY states the
  mechanism-only nuance explicitly.
- D-02's real-partition trigger is inscribed, dated, and objective — never a bare checkbox with no
  wake condition.
</success_criteria>

<output>
Create `.planning/phases/VFDO-39-workstreams-partition-du-planning-et-collaboration-concurren/39-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `scripts/hooks/post-merge` — new opt-in git hook (plan `39-01`)
- `plugin/conductor/scripts/check-divergence.sh` — new S2+S4+S5 gate (plan `39-01`)
- `plugin/conductor/scripts/tests/test-check-divergence.sh` — test suite + mutation-red proof (plan `39-01`)
- `.github/workflows/ci.yml` — check-divergence.sh armé sur le job `gates` (plan `39-01`, arbitrage Samuel 2026-09-10, C17)
- `PART-01`..`PART-09` requirement family in `.planning/REQUIREMENTS.md`, + traceability rows + tail changelog line (plan `39-02`)
- `GSDA-19` superseded (body preserved) by `PART-06` in `.planning/REQUIREMENTS.md` (plan `39-02`)
- `.planning/upstream/2026-09-09-init-progress-project-md-not-resolved-under-workstream.md` — drafted, unposted upstream issue (plan `39-02`)
- `docs/ADR.md` § ADR-069 dated `### Amendement 2026-09-09 — ...` subsection + amended Statut cell (plan `39-02`)
- `plugin/dev-orchestrator/references/workstreams.md` §4(b)/(d) updated (plan `39-02`)
- `plugin/dev-orchestrator/agents/vf-dev-manager.md` — dispatch gabarit closes D-10 OPEN half (plan `39-02`, C4 correction)
- Clone-jetable proof-of-mechanism run against real `vibeflow-os` history (clone destroyed, mechanism-only nuance stated) (**this plan**)
- `.planning/STATE.md` — D-02 dated resumption trigger + D-06 event-based watch note (**this plan**)
- `.planning/ROADMAP.md` — cross-reference to the D-02 trigger (**this plan**)

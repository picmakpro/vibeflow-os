---
phase: 19-migration-du-moteur-gsd-pilotee-par-vf-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/scripts/check-gsd-engine.sh
  - plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh
autonomous: true
requirements: [SC1, SC5, SC7]

must_haves:
  truths:
    - "SC1 / classification à 3 états : `check-gsd-engine.sh` rend exactement un état parmi `absent`, `legacy`, `gsd-core`, décidé sur la PRÉSENCE des deux fichiers `VERSION` (layout + nom de paquet), jamais sur leur contenu numérique. Fixture legacy seul → `legacy` ; fixture gsd-core seul → `gsd-core` ; aucun des deux → `absent`."
    - "SC1 / actionnable : l'état `legacy` sort en **exit 0** avec un signal `[gsd-migrate]` sur stdout — c'est le seul état actionnable, et il n'est jamais un silence."
    - "SC1 / contrat de sortie (D-02) : les seuls codes émis sont `0` (legacy), `2` (erreur d'usage) et `3` (INDÉTERMINÉ). Le code `1` n'est jamais émis ; le code `64` de `check-doc-drift.sh` n'est jamais repris (convention F13 de `CONVENTIONS.md` : `2` = erreur d'usage)."
    - "SC1 / D-04 — l'exit 3 n'est PAS synonyme de silence : sur une fixture où les DEUX fichiers `VERSION` existent, l'état est `gsd-core`, le code de sortie est `3`, **et** stdout porte une ligne `[gsd-leftover]` distincte. Les trois sous-états d'exit 3 (`absent` → stdout vide ; `gsd-core` propre → stdout vide ; `gsd-core` + reliquat → stdout non vide) sont assertés sur stdout ET sur le code de sortie dans deux variables distinctes, jamais l'un déduit de l'autre."
    - "SC5 / D-05 comportemental : le couple exact `1.42.3` (legacy) → `1.8.0` (gsd-core) est classé « à migrer ». La preuve est renforcée par l'inverse : une fixture legacy portant `9.9.9` (numéro SUPÉRIEUR à toute version gsd-core) reste classée `legacy`/exit 0, et une fixture gsd-core portant `0.0.1` reste classée `gsd-core`/exit 3 — le classement est donc démontré indépendant des numéros."
    - "SC5 / D-05 structurel : hors lignes de commentaire, la source de `check-gsd-engine.sh` ne contient ni `sort -V`, ni appel à un comparateur externe (`newer`), ni comparaison numérique (`-lt`/`-gt`/`-le`/`-ge`) portant sur une variable de version."
    - "SC5 / D-05 documentaire : l'en-tête `# ` du script cite littéralement les deux numéros du piège et le mot `semver`, avec l'explication de la raison pour laquelle aucune comparaison n'est faite."
    - "SC5 / D-11 scénario réel du rapport : une fixture reproduisant le poste audité — `VERSION` legacy présent ET un cache plugin planté qui signifie « plugin à jour » — est toujours classée `legacy`/exit 0. La sortie du gate est structurellement orthogonale à l'état du plugin : hors commentaires, le script ne référence aucun chemin de cache de plugin ni `check-plugin-update`."
    - "SC1 / dérivation du layout : la cascade de `default_gsd_home_new()` est reprise telle quelle de `ensure-deps.sh:60-72` — un payload `gsd-core` déposé en scope projet (`<projet>/.claude/gsd-core/VERSION`) avec un `$HOME` vide est détecté comme `gsd-core` (miroir du cas DISCRIMINANT T2f de `test-dev-orchestrator.sh`)."
    - "SC1 / FAIT vs JUGEMENT (ADR-055 §3) : le script constate l'état et propose un geste ; il n'invoque jamais `npx`, `npm` ni aucune install. Hors commentaires, sa source ne contient aucune de ces commandes."
    - "Robustesse (précédent T-17-06) : le contenu d'un fichier `VERSION` hostile (substitution de commande, octet de contrôle, chaîne de plus de 80 caractères) n'est jamais expansé ni imprimé tel quel — la valeur est assainie avant affichage, sur le modèle de `check-dev-bootstrap.sh:150-160`."
    - "Lecture seule : l'empreinte `find <fixture> | LC_ALL=C sort` de la fixture est identique avant et après exécution du gate — aucun fichier créé, modifié ni supprimé."
    - "SC7 / portabilité prouvée par exécution : `test-check-gsd-engine.sh` est exécutée dans un conteneur Linux et y produit le même nombre d'assertions vertes et zéro KO que sur macOS. La sortie du conteneur est la preuve ; aucune assertion n'est sautée silencieusement."
  artifacts:
    - plugin/dev-orchestrator/scripts/check-gsd-engine.sh
    - plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh
  key_links:
    - "`check-gsd-engine.sh` → les deux fichiers `VERSION` du poste (`<cascade>/gsd-core/VERSION` et `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/get-shit-done/VERSION`) : c'est le SEUL couple d'entrées du gate. Toute autre source (PATH, cache plugin, numéro de version) rouvrirait exactement le trou que la phase corrige."
    - "`check-gsd-engine.sh` → `vf-update/SKILL.md` (câblé en 19-03) : le gate est matérialisé à plat dans le même `<S>` que les scripts de conductor (D-00). Le contrat de sortie 0/2/3 et le préfixe `[gsd-migrate]` sont ce que la sonde du skill lit — les changer casse le branchement de 19-03."
    - "`test-check-gsd-engine.sh` → `.github/workflows/ci.yml:32` : la suite est ramassée automatiquement par le `find plugin scripts -type f -path '*/tests/test-*.sh'`. Aucune édition de `ci.yml` n'est nécessaire ni autorisée."
  prohibitions:
    - "P-01 — MUST NOT classer le moteur sur une comparaison de numéros de version. Ni `sort -V`, ni test d'infériorité/supériorité entre deux chaînes de version, ni réutilisation du comparateur `newer()` de `check-plugin-update.sh`. `1.8.0 < 1.42.3` en semver : une comparaison classerait un poste legacy « à jour » à perpétuité, ce qui est précisément le défaut corrigé (D-05, one-way)."
    - "P-02 — MUST NOT détecter le moteur par le PATH (`command -v`). Un shim legacy survivant à la migration ferait renvoyer vrai indéfiniment (piège n°1, documenté en `ensure-deps.sh:116-118`)."
    - "P-03 — MUST NOT émettre le code de sortie `1`, ni le code `64`. Le contrat est 0/2/3 (D-02, F13)."
    - "P-04 — MUST NOT ajouter de flag `--hook` ni de fragment de hook `SessionStart`. Le signal passe exclusivement par `/vf-update` (hors-périmètre explicite du ROADMAP et de `19-CONTEXT.md`)."
    - "P-05 — MUST NOT exécuter d'install, de désinstallation ni de suppression depuis ce gate. Il constate, il ne fait pas (ADR-055 §3, ADR-031)."
    - "P-06 — MUST NOT modifier `.github/workflows/ci.yml`, la `VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` ni les README racine. Le compteur « N suites » des README racine passera de 41 à 42 du fait de ce plan : c'est un reste-à-faire de release, traité en 19-03 comme différé explicite."
---

<objective>
Écrire le détecteur d'état du moteur GSD : un gate dédié, en lecture seule, qui répond à une seule
question factuelle — le poste tourne-t-il sur `get-shit-done-cc` (legacy), sur `@opengsd/gsd-core`,
ou sur rien — et le dit avec un contrat de sortie exploitable par un agent.

Purpose: c'est la tranche traçante de la phase. Le trou corrigé (un poste plugin-à-jour dont le
moteur est resté à 1.42.3, sans que rien ne le dise) devient observable dès ce plan, avant même que
`/vf-update` ne le consulte. Toute la difficulté est concentrée ici : le classement doit être
strictement insensible aux numéros de version, parce que `1.8.0 < 1.42.3` en semver et que la
doctrine « ne jamais downgrader », saine partout ailleurs, interdirait exactement le geste à faire.

Output: 1 script créé + 1 suite dédiée créée (15 cas), portabilité Linux prouvée par exécution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md
@.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@plugin/dev-orchestrator/scripts/check-doc-drift.sh
@plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh
@plugin/dev-orchestrator/scripts/ensure-deps.sh
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `plugin/dev-orchestrator/scripts/check-gsd-engine.sh` | fichier créé | gate de constat, exécutable par `bash <chemin>` |
| `gsd_engine_state()` | fonction (nouvelle) | imprime exactement `absent`, `legacy` ou `gsd-core` |
| `has_legacy_leftover()` | fonction (nouvelle) | booléen : le `VERSION` legacy existe encore |
| `default_gsd_home_new()` | fonction (dupliquée) | cascade reprise de `ensure-deps.sh:60-72`, contenu figé |
| `sanitize_version()` | fonction (nouvelle) | assainit une valeur lue avant affichage (modèle `check-dev-bootstrap.sh:150-160`) |
| `say()` | helper (nouveau sur ce script) | préfixe `[check-gsd-engine]`, **stderr** uniquement |
| `--quiet` | flag CLI (nouveau) | supprime les diagnostics `say()` ; n'altère JAMAIS stdout |
| `-h` / `--help` | flag CLI (nouveau) | imprime l'en-tête `# `, exit 0 |
| exits `0` / `2` / `3` | contrat de sortie | `1` et `64` jamais émis |
| `[gsd-migrate]` | préfixe de signal (nouveau) | état `legacy`, stdout, 2 lignes, exit 0 |
| `[gsd-leftover]` | préfixe de signal (nouveau) | état `gsd-core` + reliquat legacy, stdout, 1 ligne, exit 3 |
| `plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` | fichier créé | suite dédiée, ramassée par `ci.yml:32` |
| `mk_gsd_home()` / `mk_gsd_project()` | helpers de suite (nouveaux) | posent/enlèvent les `VERSION` aux deux layouts |
| `Cas 1` … `Cas 15` | labels de cas | numérotation `# === Cas N — <titre> ===` |

## Détecteurs `plan:pre` non déclenchés

`ai-integration`, `assumption-delta` et `schema-gate` ne s'appliquent pas : aucune intégration
d'API/SDK externe (le gate ne lit que deux fichiers locaux), aucune transition d'identité
singulier→pluriel ni requis→optionnel, aucun schéma ni ORM touché. Confirmation explicite.

## Note sur la Task 2 notée `one-way` — absence de `checkpoint:decision` assumée

Le gate de réversibilité est **délibérément désactivé pour cette phase**. La décision D-05 (aucun
classement par comparaison de numéros) est déjà TRANCHÉE et verrouillée par le cadrage de Samuel
dans `19-CONTEXT.md` — ce n'est pas une question ouverte que l'exécutant aurait à re-litiger. Ni la
chaîne de planification ni la chaîne d'exécution ne disposent d'un agent capable de poser une
question à l'utilisateur : un `checkpoint:decision` y serait une impasse, pas un filet. La note de
réversibilité est donc conservée pour la traçabilité, sans bloquer l'exécution. Toute alerte d'outil
sur ce point est attendue et n'est pas un défaut du plan.

<tasks>

<task type="tracer">
  <name>Task 1 : tranche traçante — le chemin `legacy` de bout en bout (cascade → classement → signal → exit 0)</name>
  <reversibility rating="reversible">Script neuf, aucun appelant existant à défaire (D-01). Le
  retirer coûte une suppression de fichier tant que 19-03 n'a pas branché la sonde.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` **en entier** — gabarit exact : en-tête `# ` documentaire dense AVANT tout code, boucle de parsing, helper `say()`, `printf` (jamais `echo`) pour le signal capturé, `exit` en fin de chaque branche.
    - `plugin/dev-orchestrator/scripts/ensure-deps.sh:55-72` — la cascade `default_gsd_home_new()` et la dérivation des deux chemins `VERSION` ; `:116-121` — le piège n°1 (`command -v`) et sa neutralisation.
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh:150-180` — l'assainissement d'une valeur lue avant impression, et le précédent explicite « exit 3 n'est pas synonyme de silence » documenté dans son en-tête.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-01, D-02, D-03, D-04, D-05.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-PATTERNS.md` §`check-gsd-engine.sh` — les extraits déjà découpés, à ne pas re-dériver.
    - `.planning/codebase/CONVENTIONS.md` §Portabilité bash et §Codes de sortie normalisés.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/check-gsd-engine.sh</files>
  <behavior>
    - Fixture `$HOME` factice contenant uniquement `~/.claude/get-shit-done/VERSION` = `1.42.3`, lancée depuis un projet vide : stdout contient `[gsd-migrate]` et `1.42.3`, code de sortie `0`.
    - La même fixture sous `--quiet` : stdout strictement identique (le signal n'est pas un diagnostic), stderr vide.
    - Argument inconnu : stdout vide, message sur stderr, code de sortie `2`.
    - `--help` : sortie non vide, code de sortie `0`.
  </behavior>
  <action>
Créer `plugin/dev-orchestrator/scripts/check-gsd-engine.sh` en répliquant la STRUCTURE de
`check-doc-drift.sh` : shebang `#!/usr/bin/env bash`, préambule `set -uo pipefail` (jamais `-e`,
ADR-054), puis un en-tête `# ` documentaire dense placé AVANT tout code.

L'en-tête doit couvrir, dans cet ordre : le rôle du gate au sens ADR-055 §3 (il constate un état,
il ne juge pas et n'installe rien) ; les trois états rendus et sur quoi ils sont décidés (présence
des fichiers `VERSION`, layout et nom de paquet) ; le piège n°1 recopié de `ensure-deps.sh:116-118`
(pourquoi le PATH n'est jamais interrogé) ; le piège semver, écrit noir sur blanc comme D-05
l'exige — le paquet legacy est figé à 1.42.3 et déprécié, le paquet vivant est à 1.8.0, donc le
numéro du legacy est le PLUS GRAND des deux, et toute comparaison semver classerait le poste à
migrer comme « à jour » à perpétuité ; le cas dual de D-04 et la rupture assumée de l'équivalence
« exit 3 égale silence », sur le modèle explicite déjà écrit dans l'en-tête de
`check-dev-bootstrap.sh` ; l'Usage ; et le tableau des codes de sortie.

Contrat de sortie, à respecter EXACTEMENT (D-02, aligné sur `CONVENTIONS.md` §Codes de sortie —
et NON sur le `64` de `check-doc-drift.sh`, qui est un choix historique propre à ce script) :
`0` pour l'état `legacy` (seul cas actionnable), `2` pour une erreur d'usage, `3` pour
INDÉTERMINÉ. Le code `1` est réservé et n'est jamais émis.

Boucle de parsing : `--quiet` (booléen, silence les diagnostics `say()` sur stderr uniquement),
`-h|--help` (imprime l'en-tête via le motif `grep '^# '` puis `sed` de dé-préfixage déjà utilisé
par les scripts voisins, exit 0), et défaut → message sur stderr + exit 2. Aucun autre flag : pas
de `--hook`, pas de `--path`, pas de `--threshold`.

Dupliquer `default_gsd_home_new()` depuis `ensure-deps.sh:60-69` — duplication DÉLIBÉRÉE, motivée
par D-01 (le gate doit rester testable en boîte noire sans sourcer un script à effets de bord) ;
le noter en commentaire avec le renvoi vers le fichier source, qui reste la référence du contenu de
la cascade. En dériver `GSD_VERSION_FILE_NEW` et `GSD_VERSION_FILE_LEGACY` exactement comme aux
lignes 70-72 du même fichier.

Écrire `gsd_engine_state()` : imprime `gsd-core` si le fichier `VERSION` du nouveau layout existe,
sinon `legacy` si celui du layout legacy existe, sinon `absent`. Écrire `has_legacy_leftover()` :
booléen sur la seule existence du fichier `VERSION` legacy. Le classement lit des PRÉSENCES de
fichiers, jamais leur contenu.

Écrire `sanitize_version()` sur le modèle de `check-dev-bootstrap.sh:150-160` : la valeur lue dans
un fichier `VERSION` est bornée en longueur puis validée contre une classe de caractères
restreinte ; toute valeur non conforme est remplacée par une mention neutre, jamais imprimée telle
quelle. Aucune valeur lue n'est jamais réinjectée dans une expansion.

Câbler la seule branche `legacy` pour cette tâche : `say()` de diagnostic sur stderr, puis deux
lignes de signal sur stdout via `printf` (jamais `echo`) — la première portant le préfixe
`[gsd-migrate]`, l'état legacy, sa version assainie et le nom du paquet cible ; la seconde,
indentée comme dans `check-doc-drift.sh:148`, portant le geste proposé sous forme de proposition
(jamais d'impératif : la migration passe par une confirmation humaine dans `/vf-update`). Puis
`exit 0`.

Les autres états sont complétés en Task 2 ; pour cette tâche ils peuvent tomber dans un `exit 3`
provisoire, mais la branche `legacy` doit être définitive, avec sa cascade réelle et son signal
réel.
  </action>
  <verify>
    <automated>cd "$(mktemp -d)" && mkdir -p fx/.claude/get-shit-done && printf '1.42.3\n' > fx/.claude/get-shit-done/VERSION && out="$(env -u CLAUDE_CONFIG_DIR HOME="$PWD/fx" bash /Users/samuel/Documents/dev/vibeflow-os/plugin/dev-orchestrator/scripts/check-gsd-engine.sh 2>/dev/null)"; rc=$?; printf 'rc=%s\nout=%s\n' "$rc" "$out"; [ "$rc" -eq 0 ] && printf '%s' "$out" | grep -q '\[gsd-migrate\]' && printf '%s' "$out" | grep -q '1\.42\.3'</automated>
  </verify>
  <acceptance_criteria>
    - Fixture legacy seul (`$HOME/.claude/get-shit-done/VERSION` = `1.42.3`, `CLAUDE_CONFIG_DIR` retiré de l'environnement, lancement depuis un répertoire temporaire hors dépôt) : le code de sortie capturé dans une variable dédiée vaut `0`, et stdout capturé dans une variable dédiée contient la sous-chaîne `[gsd-migrate]` ET la sous-chaîne `1.42.3`. Les deux assertions sont écrites séparément.
    - La même invocation avec `--quiet` produit un stdout **identique octet pour octet** à l'invocation sans `--quiet`, et un stderr vide.
    - `bash check-gsd-engine.sh --flag-inconnu` : stdout vide, stderr non vide, code de sortie `2`. Le code `64` ne doit apparaître dans aucune branche : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-gsd-engine.sh | grep -c 'exit 64'` retourne `0`.
    - `bash check-gsd-engine.sh --help` : code de sortie `0` et sortie non vide.
    - `bash -n plugin/dev-orchestrator/scripts/check-gsd-engine.sh` retourne 0.
    - L'en-tête du script contient les trois littéraux `1.42.3`, `1.8.0` et `semver` : chacun des trois `grep '^# ' plugin/dev-orchestrator/scripts/check-gsd-engine.sh | grep -c <littéral>` retourne une valeur ≥ 1.
    - Hors commentaires, la source ne contient ni `npx`, ni `npm`, ni `rm -rf` : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-gsd-engine.sh | grep -Ec 'npx|npm |rm -rf'` retourne `0`.
    - Hors commentaires, la source ne contient aucun `command -v` portant sur un binaire GSD : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-gsd-engine.sh | grep -c 'command -v gsd'` retourne `0`.
  </acceptance_criteria>
  <done>Une fixture legacy produit le signal `[gsd-migrate]` sur stdout et sort en 0 ; l'erreur d'usage sort en 2 ; le script est commité.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : compléter les 3 états + le cas dual, et écrire la suite dédiée `test-check-gsd-engine.sh` (15 cas)</name>
  <reversibility rating="one-way">Le classement par présence/layout (D-05) est one-way : une fois
  publié avec une comparaison de numéros, un poste legacy serait classé « à jour » à perpétuité et
  le trou se rouvrirait sans aucun signal. Décision verrouillée par le cadrage, pas à
  re-litiger.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh:1-90` — gabarit exact de la suite : `SCRIPT=` résolu depuis `$(dirname "$0")/..`, compteurs `PASS`/`FAIL`, helpers `ok()`/`ko()` (le `ko()` de cette suite prend DEUX arguments : libellé et détail), `TMP="$(mktemp -d)"` + `trap 'rm -rf "$TMP"' EXIT`, helper de fabrication de fixture, cas `# === Cas N — <titre> ===` avec capture séparée de `out` et `rc`.
    - `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` fin de fichier — la bannière `== résultat : $PASS ok, $FAIL ko ==` et le `[ "$FAIL" -eq 0 ]` final, à reprendre tels quels.
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh:335-348` — le cas T2f DISCRIMINANT (payload gsd-core en scope projet, `$HOME` vide) : le cas 10 de cette suite en est le miroir pour le gate autonome.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` D-04, D-05, D-11 et le rappel du piège D-14 de la Phase 17.
    - `.planning/codebase/TESTING.md` §Test Structure et §Isolation.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/check-gsd-engine.sh, plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh</files>
  <behavior>
    - Aucun des deux fichiers `VERSION` → stdout vide, exit 3.
    - `VERSION` gsd-core seul → stdout vide, exit 3.
    - Les DEUX fichiers `VERSION` présents → stdout NON vide contenant `[gsd-leftover]`, exit 3.
    - `VERSION` legacy seul contenant `9.9.9` → toujours `legacy`, exit 0 (le numéro n'entre pas dans le classement).
    - `VERSION` gsd-core seul contenant `0.0.1` → toujours `gsd-core`, exit 3.
    - `VERSION` legacy présent + cache plugin planté signifiant « plugin à jour » → toujours `legacy`, exit 0.
    - `$HOME` vide + `VERSION` gsd-core sous `<projet>/.claude/gsd-core/` → `gsd-core`, exit 3.
    - `VERSION` legacy au contenu hostile (substitution de commande, octet de contrôle, 400 caractères) → aucune expansion observable dans la sortie, exit 0.
    - Empreinte de la fixture identique avant et après exécution.
  </behavior>
  <action>
Compléter `check-gsd-engine.sh` avec les branches manquantes, en gardant la branche `legacy` déjà
posée en Task 1 intacte.

Branche `absent` : diagnostic `say()` sur stderr, stdout strictement vide, `exit 3`.

Branche `gsd-core` : deux sous-cas explicitement distingués. Sans reliquat legacy, stdout
strictement vide et `exit 3`. Avec reliquat legacy (les deux fichiers `VERSION` présents, cas D-04),
stdout porte UNE ligne préfixée `[gsd-leftover]` annonçant que le moteur est à jour et qu'un
reliquat legacy subsiste, avec un renvoi vers le nettoyage recommandé — puis `exit 3` quand même,
parce que la migration a déjà eu lieu et que reproposer une install serait un no-op bruyant. Ce
sous-cas est la rupture assumée de l'équivalence « exit 3 égale silence » : le commentaire qui
l'introduit doit le dire, sur le modèle de l'en-tête de `check-dev-bootstrap.sh`.

Créer ensuite `plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` en copiant le
squelette de `test-check-doc-drift.sh` : mêmes helpers, même `TMP`/`trap`, même bannière finale.
Remplacer l'helper de dépôt git par deux helpers de fixture — l'un qui fabrique un `$HOME` factice
et y plante ou non les deux `VERSION` demandés, l'autre qui fabrique un répertoire de projet
factice et peut y planter un payload `gsd-core` en scope projet.

Règles d'invocation non négociables pour TOUS les cas : le gate est appelé en boîte noire par
`bash "$SCRIPT"` dans un sous-shell qui a d'abord changé de répertoire vers le projet factice, avec
`HOME` surchargé et `CLAUDE_CONFIG_DIR` explicitement retiré de l'environnement — sans quoi une
variable ambiante de la machine de l'exécutant rendrait la suite non hermétique. `out` et `rc` sont
capturés dans deux variables distinctes et assertés séparément dans le même `if` ; aucune assertion
ne déduit le code de sortie du contenu de stdout ni l'inverse (piège D-14 de la Phase 17).

Les quinze cas à écrire, un bloc `# === Cas N — <titre> ===` chacun : (1) état absent ; (2) état
legacy avec le numéro figé du paquet déprécié ; (3) état gsd-core seul ; (4) cas dual D-04, avec
assertion POSITIVE que stdout est non vide et contient le préfixe de reliquat, ET assertion séparée
que le code vaut 3 ; (5) discriminant D-05 par le haut, fixture legacy portant un numéro supérieur
à toute version gsd-core ; (6) discriminant D-05 par le bas, fixture gsd-core portant un numéro
inférieur à celui du legacy ; (7) assertions structurelles D-05 par grep filtré sur les
commentaires (voir critères d'acceptation) ; (8) assertions documentaires sur l'en-tête ;
(9) scénario réel du rapport D-11, fixture legacy plus un cache de plugin planté qui signifie
« plugin à jour », plus les assertions structurelles d'orthogonalité au plugin ; (10) discriminant
de scope projet-local, miroir de T2f ; (11) erreur d'usage sur argument inconnu ; (12) `--help` et
`--quiet` ; (13) robustesse d'un contenu `VERSION` hostile ; (14) `bash -n` sur le gate ;
(15) lecture seule par empreinte `find` triée en `LC_ALL=C` avant et après.

Idiomes portables imposés (ADR-054, aucune invention) : `set -uo pipefail` sans `-e`, pas de
`mapfile`, pas de `sed -i` nu, pas de `grep -P`, `mktemp -d` apparié à un `trap ... EXIT`,
`LC_ALL=C sort` pour toute comparaison d'empreinte.

Les greps structurels des cas 7 et 9 doivent TOUS filtrer les lignes de commentaire avant de
compter, parce que l'en-tête du gate documente légitimement les motifs interdits : un grep non
filtré serait auto-invalidant. C'est une contrainte de forme, pas un détail.
<!-- planner-discipline-allow: sort -V -->
<!-- planner-discipline-allow: newer -->
<!-- planner-discipline-allow: check-plugin-update -->
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` sort en `0` et sa dernière ligne de bilan annonce `0 ko`. Le nombre d'assertions vertes est ≥ 15 et est noté dans le SUMMARY (il sert de référence au cas 3 de la Task 3).
    - Cas 1 : `rc` vaut `3` **et** `out` est vide — deux conditions écrites séparément.
    - Cas 3 : `rc` vaut `3` **et** `out` est vide.
    - Cas 4 (D-04 / piège D-14) : `rc` vaut `3` **et** `out` est NON vide **et** `out` contient `[gsd-leftover]`. L'assertion sur le contenu ne doit en aucun cas être remplacée par une déduction depuis `rc`.
    - Cas 5 : fixture legacy dont le fichier `VERSION` contient `9.9.9` → `rc` vaut `0` et `out` contient `[gsd-migrate]`.
    - Cas 6 : fixture gsd-core dont le fichier `VERSION` contient `0.0.1` → `rc` vaut `3` et `out` est vide.
    - Cas 7 (structurel D-05), les trois greps portant sur `plugin/dev-orchestrator/scripts/check-gsd-engine.sh` filtré par `grep -v '^[[:space:]]*#'` retournent tous `0` : occurrences de `sort -V` ; occurrences de `newer` ; occurrences d'une comparaison numérique portant sur une variable de version, comptées par `grep -E '(VERSION_(NEW|LEGACY)|_ver|version)' | grep -Ec -- '-(lt|gt|le|ge)'`. Ce dernier motif est délibérément ciblé sur les lignes mentionnant une version, pour ne pas se déclencher sur le `[ "$#" -lt 2 ]` légitime d'une boucle de parsing.
    - Cas 8 (documentaire D-05) : `grep '^# ' <gate> | grep -c '1\.42\.3'` ≥ 1, `grep '^# ' <gate> | grep -c '1\.8\.0'` ≥ 1, `grep '^# ' <gate> | grep -ci 'semver'` ≥ 1.
    - Cas 9 (D-11) : fixture avec `VERSION` legacy à `1.42.3` ET un fichier planté sous `$HOME/.claude/plugins/cache/` portant une version de plugin récente → `rc` vaut `0` et `out` contient `[gsd-migrate]`. Assertions structurelles associées, sur la source filtrée hors commentaires : occurrences de `plugins/cache` = `0`, occurrences de `check-plugin-update` = `0`.
    - Cas 10 : `HOME` pointant sur un répertoire vide, exécution depuis un projet factice contenant `.claude/gsd-core/VERSION` → `rc` vaut `3` et `out` est vide. Ce cas doit échouer si la cascade est réécrite en `$HOME` figé.
    - Cas 11 : argument inconnu → `rc` vaut `2`, `out` est vide, stderr non vide. Une assertion supplémentaire vérifie que `rc` ne vaut ni `1` ni `64`.
    - Cas 13 : fixture dont le `VERSION` legacy contient une substitution de commande, un octet de contrôle et plus de 80 caractères → `rc` vaut `0`, et `out` ne contient ni le nom d'utilisateur courant ni d'octet de contrôle ; la longueur de la ligne de signal reste bornée.
    - Cas 15 : `find <fixture> | LC_ALL=C sort` produit une chaîne identique avant et après l'exécution du gate.
    - Aucun cas n'est marqué SKIP : la suite ne dépend d'aucun outil hors `bash`, `grep`, `find`, `sort`, `mktemp`, `printf`.
  </acceptance_criteria>
  <done>Les 3 états, le cas dual, les deux discriminants semver, le scénario réel et l'erreur d'usage sont couverts par des assertions séparées stdout/exit ; la suite passe verte ; script et suite sont commités.</done>
</task>

<task type="auto">
  <name>Task 3 : preuve de portabilité Linux par exécution, avant tout push</name>
  <precondition>Le démon Docker est démarré et une image Linux est récupérable (`docker info` retourne 0). Si Docker est indisponible, la tâche s'arrête et le signale — elle ne se déclare jamais satisfaite par lecture du code.</precondition>
  <read_first>
    - `.planning/STATE.md` §Session Continuity — la leçon durable du 2026-07-27 : « tout bash développé sur macOS/BSD doit être reproduit sous `docker run ubuntu:24.04` avant push », après 6 fixes de portabilité macOS→Linux.
    - `.planning/codebase/CONVENTIONS.md` §Portabilité bash (ADR-054) — la liste des idiomes interdits.
    - `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-CONTEXT.md` §Portabilité de D-11.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/check-gsd-engine.sh, plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh</files>
  <action>
Exécuter la suite créée en Task 2 dans un conteneur Linux, depuis la racine du dépôt, et capturer
la sortie intégrale. L'image de référence est `ubuntu:24.04` — c'est l'OS exact du runner
`runs-on: ubuntu-latest` de la CI et l'idiome déjà établi par la Phase 17. `debian:bookworm-slim`
est accepté comme équivalent si `ubuntu:24.04` n'est pas récupérable ; dans ce cas, noter l'image
réellement utilisée dans le SUMMARY.

Comparer le compteur d'assertions vertes du conteneur à celui obtenu sur macOS en Task 2. Un écart
de compteur, même sans KO, est un signal : il veut dire qu'un cas a été sauté silencieusement, pas
qu'il a réussi. Si un écart ou un KO apparaît, corriger le script ou la suite selon l'idiome
portable de `CONVENTIONS.md` (jamais en désactivant le cas), puis rejouer les DEUX environnements.

Reporter dans le SUMMARY la commande exacte, l'image, et les deux compteurs — c'est la preuve
d'exécution attendue par SC7, pas une affirmation.
  </action>
  <verify>
    <automated>docker run --rm -v "$(pwd)":/repo -w /repo ubuntu:24.04 bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh</automated>
  </verify>
  <acceptance_criteria>
    - La commande `docker run --rm -v "$(pwd)":/repo -w /repo ubuntu:24.04 bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` est réellement exécutée depuis la racine du dépôt et retourne le code `0`.
    - Sa sortie contient une ligne de bilan annonçant `0 ko`.
    - Le nombre d'assertions vertes annoncé par le conteneur est **égal** à celui obtenu par l'exécution macOS de la Task 2. Un écart bloque la tâche.
    - La sortie brute du conteneur (commande, image, bilan) est recopiée dans le SUMMARY du plan.
    - Aucune modification n'est apportée à `.github/workflows/ci.yml` : `git diff --name-only` ne mentionne jamais ce fichier.
  </acceptance_criteria>
  <done>La suite est verte sur macOS ET sur Linux, avec des compteurs identiques, et la preuve d'exécution est consignée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichier `VERSION` du poste → stdout du gate | Contenu non maîtrisé (écrit par un installeur tiers ou par un attaquant local) qui traverse vers une sortie injectée dans le contexte de la session |
| environnement de l'appelant → cascade de dérivation | `HOME`, `CLAUDE_CONFIG_DIR` et le cwd décident quels fichiers sont lus |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01-01 | Tampering | contenu d'un fichier `VERSION` hostile réinjecté dans le signal | high | mitigate | `sanitize_version()` borne la longueur et valide la classe de caractères avant impression (modèle `check-dev-bootstrap.sh:150-160`) ; la valeur n'est jamais réinjectée dans une expansion. Cas 13 de la suite prouve l'absence d'expansion. |
| T-19-01-02 | Spoofing | shim legacy survivant sur le PATH faisant croire à un moteur présent | high | mitigate | Détection par fichier `VERSION` UNIQUEMENT ; `command -v` interdit et vérifié par grep (piège n°1, `ensure-deps.sh:116-118`). |
| T-19-01-03 | Elevation of Privilege | le gate lancerait une install ou une suppression | high | mitigate | Le gate est en lecture seule (ADR-055 §3, ADR-031) ; absence de `npx`/`npm`/`rm -rf` prouvée par grep hors commentaires ; cas 15 prouve l'empreinte inchangée. |
| T-19-01-04 | Denial of Service | fichier `VERSION` de taille arbitraire lu intégralement au SessionStart d'un agent | medium | mitigate | Lecture bornée en amont de l'assainissement ; le gate n'est de toute façon appelé que par `/vf-update`, jamais par un hook (P-04). |
| T-19-01-05 | Information Disclosure | chemin absolu du `$HOME` imprimé dans le signal | low | accept | Le signal est destiné à la session locale de l'utilisateur propriétaire de ce `$HOME` ; aucune exfiltration. Le message privilégie la forme `~/.claude/...`. |
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` → exit 0, `0 ko`.
- `docker run --rm -v "$(pwd)":/repo -w /repo ubuntu:24.04 bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-engine.sh` → exit 0, même compteur.
- `bash -n plugin/dev-orchestrator/scripts/check-gsd-engine.sh` → exit 0.
- `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` → 42 (41 avant ce plan). Le compteur « N suites » des README racine reste à 41 : dérive assumée et différée, traitée comme reste-à-faire de release en 19-03.
- `git diff --name-only` ne mentionne que les deux fichiers de `files_modified`.
</verification>

<success_criteria>
- SC1 tenu à hauteur du gate : classification à 3 états sur layout et nom de paquet, `legacy`
  actionnable en exit 0, aucune comparaison de numéros (prouvée structurellement ET
  comportementalement).
- SC5 tenu pour sa part « détecteur » : couple `1.42.3 → 1.8.0` classé « à migrer », scénario réel
  du rapport couvert, cas dual asserté sans déduire l'exit de stdout.
- SC7 tenu pour sa part « portabilité » : suite verte sur macOS ET Linux, compteurs identiques,
  preuve par exécution consignée.
</success_criteria>

<output>
Create `.planning/phases/VFDO-19-migration-du-moteur-gsd-pilot-e-par-vf-update/19-01-SUMMARY.md` when done
</output>

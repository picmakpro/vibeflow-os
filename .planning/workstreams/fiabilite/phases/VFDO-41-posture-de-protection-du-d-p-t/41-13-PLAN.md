---
phase: 41-posture-de-protection-du-d-p-t
plan: 13
lot: L13 — ledger coché sur pièce (PROT-01) et compléments du volet admin, ROADMAP et STATE, éligibilité de clôture du jalon, PR de clôture sans contournement
type: execute
wave: 13
depends_on: ["41-12"]
autonomous: false
requirements: [PROT-01, PROT-02, PROT-03, PROT-04]
files_modified:
  - .planning/workstreams/fiabilite/REQUIREMENTS.md
  - .planning/workstreams/fiabilite/ROADMAP.md
  - .planning/workstreams/fiabilite/STATE.md
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "PROT-01 est coché et `Complete` en traçabilité seulement ici, suivi d'une ligne de clôture `  - **Exigence close le <date>** (Phase 41, plan 41-13) — preuves : …` qui porte sur l'EXIGENCE et ne cite jamais O-3 ; PROT-02, PROT-03, PROT-04 (déjà cochés le 2026-09-18) reçoivent chacun une ligne `  - **Complément volet admin (<date>)** (Phase 41, plan 41-13) — preuves : …`, sans O-3 ; aucune ligne existante du ledger n'est réécrite"
    - "Chaque ligne cite ses clés de preuve de `41-PREUVES.md` et les décisions humaines consignées (option, décideur, canal, date) ; tout `non_prouvee` de `TRACE-II:` est recopié, jamais effacé ; en `preuves-sans-release`, le complément de PROT-02 écrit que le rejeu du flux de release n'a pas eu lieu dans cette phase"
    - "QUAL-01 : la ligne de traçabilité dit que le volet admin de la Phase 41 n'en crée aucun gate (D-M11, `INVARIANTS-PHASE`) — les trois gates de la phase sont ceux du 2026-09-18, déjà satisfaits"
    - "Critères du ROADMAP recalculés sur pièce (`CRITERES-ROADMAP:` : c1 rulesets actifs, c2 refus par défaut + trace, c3 rejeu de release, c4 QUAL-01) ; la case Phase 41 du ROADMAP n'est cochée QUE si les quatre sont atteints ; la section Phase 41 reçoit une note « VOLET ADMIN EXÉCUTÉ » et ses treize plans 41-01..41-13 sont cochés ; `STATE.md` § Current Position est mis à jour"
    - "Jalon fiabilite-v1.0 : `CLOTURE-JALON:` consigne l'éligibilité (tous les critères de la 41 atteints ET plus aucune phase non cochée au ROADMAP) ; la clôture elle-même (`/gsd-complete-milestone`) n'est PAS jouée par ce plan : elle revient à l'orchestrateur après le merge de la PR de clôture, et seulement si `eligible=oui`, par une branche et une PR"
    - "Balayage final : aucune proposition des artefacts du volet admin n'associe O-3 à un mot d'achèvement ; `tools/check-aucune-fermeture.sh` rend 0 sur tout le périmètre de la phase ; le SUMMARY de 41-08 porte la nuance des checks en cours"
    - "La PR de clôture est la troisième PR de la phase (D-03) ; son SUMMARY est committé AVANT sa publication ; son merge, par Willy sans contournement, est relu par l'orchestrateur. Tout artefact écrit APRÈS ce merge (VERIFICATION, clôture de jalon, rapport de mission) part d'une branche `docs/phase-41-verification` créée depuis `origin/main` et entre par une PR mergée sans contournement, OU n'est pas versionné"
  artifacts:
    - path: ".planning/workstreams/fiabilite/REQUIREMENTS.md"
      provides: "PROT-01 coché sur pièce ; compléments PROT-02..04 ; QUAL-01 : volet admin sans gate"
    - path: ".planning/workstreams/fiabilite/ROADMAP.md"
      provides: "Phase 41 : note du volet admin, plans cochés, case cochée si et seulement si les critères sont atteints"
    - path: ".planning/workstreams/fiabilite/STATE.md"
      provides: "position courante à jour"
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`CRITERES-ROADMAP:`, `CLOTURE-JALON:`"
  key_links:
    - "Ce plan écrit sur la branche C, à la suite de 41-12 ; `CLOTURE-JALON: eligible=` gouverne le geste de l'orchestrateur après merge"
---

<objective>
Cocher le ledger sur pièce, mettre à jour ROADMAP et STATE selon les critères réellement atteints,
consigner l'éligibilité de clôture du jalon, puis faire entrer la clôture sur `main` par une PR
ordinaire — et fixer le chemin de tout ce qui s'écrira après.

Révisé le 2026-09-23 (reprise) : PROT-02..04 sont déjà cochés (2026-09-18) — ils reçoivent un
complément, pas un second cochage ; seul PROT-01 est coché ici ; la mise à jour du ROADMAP et du
STATE, laissée au manager par 41-19, est faite ici sur pièce ; la clôture du jalon fiabilite-v1.0
n'est engagée que si les critères de la phase sont atteints ; gestes humains de Willy (`picmakpro`).

Purpose: clôture sur pièce de PROT-01 ; compléments PROT-02..04 ; QUAL-01 dit ; D-03 tenu jusqu'au dernier artefact.
Output: ledger, ROADMAP et STATE à jour ; `CLOTURE-JALON:` ; PR de clôture mergée sans contournement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-12-SUMMARY.md

Extraits de `.planning/workstreams/fiabilite/REQUIREMENTS.md` : section `### Posture de protection du dépôt (Phase 41 —
périmètre sans admin)` ; table `## Traceability`, lignes `PROT-0N` et `QUAL-01`. Extraits de
`.planning/workstreams/fiabilite/ROADMAP.md` : ligne `- [ ] Phase 41:` de la liste des phases ; section `### Phase 41:`
(notes, liste `Plans:`). Extrait de `.planning/workstreams/fiabilite/STATE.md` : § `## Current Position` (premier paragraphe).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : ledger — PROT-01 coché sur pièce avec sa ligne de clôture, compléments du volet admin sous PROT-02..04, QUAL-01</name>
  <files>.planning/workstreams/fiabilite/REQUIREMENTS.md</files>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes)
    - .planning/workstreams/fiabilite/REQUIREMENTS.md (section Phase 41, lignes de traçabilité PROT et QUAL-01)
    - les SUMMARY 41-01 à 41-12
  </read_first>
  <action>
Identité `picmakpro` ; branche C ; `git fetch origin` ; si `origin/main` a bougé, `git merge --no-edit
origin/main` en local (conflit sur REQUIREMENTS, ROADMAP ou STATE — la PR #88 les touche — : arrêt et
remontée, jamais de résolution à l'aveugle).

`REQUIREMENTS.md` :
- PROT-01 : passer `- [ ]` à `- [x]` sans toucher au texte ni à ses lignes de suite ; juste après sa
  dernière ligne de suite, ajouter `  - **Exigence close le <date d'exécution>** (Phase 41, plan 41-13) — preuves : <clés>`,
  poursuivie si besoin sur des lignes indentées de quatre espaces, sans citer O-3 ; clés :
  `POSE-CONFORME`, `POSE-BYPASS-RELU`, `M1-VERDICT`, `PROVENANCE-SERVEUR`, `TRACE-II` (avec ses
  statuts par règle), `PUSH-DIRECT`, `D03-VERDICT`, `M4-VERDICT`, `PR-EN-VOL-APRES` ;
- PROT-02, PROT-03, PROT-04 : juste après la dernière ligne de suite de chacun, ajouter
  `  - **Complément volet admin (<date d'exécution>)** (Phase 41, plan 41-13) — preuves : <clés>`, sans
  citer O-3 — PROT-02 → `RELEASE-DECISION`, `PR-RELEASE`, `RELEASE`, `CI-MAIN-RELEASE`,
  `INVARIANTS-PHASE` (en `preuves-sans-release` : « rejeu du flux de release non joué dans cette
  phase ») ; PROT-03 → ADR-072 (amendement du 2026-09-23 et § Mesures), amendement d'ADR-059,
  `CLAUDE.md`, BACKLOG traité ; PROT-04 → `CO-I`, `CO-BASELINE`, `CO-SENTINELLE`, `CO-TEMOIN`,
  `TRACE-II pull_request=…`, ligne de statut du volet admin dans `25-SECURITY.md` ;
- chaque décision humaine consignée (`*-DECISION:`, option, décideur, canal, date) est citée sous
  l'exigence qu'elle touche ; tout `non_prouvee` de `TRACE-II:` est recopié sous PROT-01 ou PROT-04 ;
- traçabilité : la ligne PROT-01 devient `| PROT-01 | Phase 41 | Complete — <date>, plan 41-13 : … |` ;
  les lignes PROT-02..05 ne changent pas ;
- ligne QUAL-01 : avant « ; reste à tenir », ajouter « ; le volet admin de la Phase 41 (<date>) n'en
  crée aucun — CODEOWNERS et rulesets sont de la configuration (D-M11), `INVARIANTS-PHASE` à 0 ajout ».
Aucune autre ligne modifiée. Commit local sur C, message « docs(41-13): PROT-01 coché sur pièce,
compléments du volet admin (D-05, D-07, D-09 — arbitrage Samuel, AskUserQuestion session principale,
2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; R=.planning/workstreams/fiabilite/REQUIREMENTS.md; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git show "$B:$R" > "$T/o"; ext() { awk -v id="$1" -v pat="$2" "/^- \\[[ x]\\] \\*\\*PROT-0[0-9]\\*\\*/ { if (st == 1) print blk; st = 0; w = index(\$0, \"**\" id \"**\") ? 1 : 0; next } st == 1 && /^    / { blk = blk \" \" \$0; next } st == 1 { print blk; st = 0; w = 0 } w && /^  [^ -]/ { next } w && index(\$0, pat) == 1 { st = 1; blk = \$0; next } { w = 0 } END { if (st == 1) print blk }" "$3"; }; blk() { awk -v id="$1" "/^- \\[[ x]\\] \\*\\*PROT-0[0-9]\\*\\*/ { w = index(\$0, \"**\" id \"**\") ? 1 : 0; if (w) print; next } w && /^  [^ -]/ { print; next } { w = 0 }" "$2"; }; x=$(awk "/^- \\[x\\] \\*\\*PROT-0[1-5]\\*\\*/{c++} END{print c+0}" "$R"); u=$(awk "/^- \\[ \\] \\*\\*PROT-0[1-5]\\*\\*/{c++} END{print c+0}" "$R"); ext PROT-01 "  - **Exigence close le " "$R" > "$T/c1"; : > "$T/cp"; for id in PROT-02 PROT-03 PROT-04; do ext "$id" "  - **Complément volet admin (" "$R" >> "$T/cp"; done; n1=$(awk "/\\*\\*Exigence close le [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\*\\* \\(Phase 41, plan 41-13\\)/ {c++} END{print c+0}" "$T/c1"); np=$(awk "/\\*\\*Complément volet admin \\([0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\)\\*\\* \\(Phase 41, plan 41-13\\)/ {c++} END{print c+0}" "$T/cp"); cat "$T/c1" "$T/cp" > "$T/all"; o3=$(awk "index(\$0, \"O-3\") {c++} END{print c+0}" "$T/all"); k=0; for key in POSE-CONFORME POSE-BYPASS-RELU M1-VERDICT PROVENANCE-SERVEUR TRACE-II PUSH-DIRECT D03-VERDICT M4-VERDICT PR-EN-VOL-APRES; do awk -v k="$key" "index(\$0, k) {f=1} END{exit !f}" "$T/c1" || { echo "CLE PROT-01 NON CITEE : $key"; k=$((k+1)); }; done; for key in PR-RELEASE RELEASE-DECISION INVARIANTS-PHASE ADR-072 ADR-059 CO-I CO-BASELINE CO-SENTINELLE CO-TEMOIN; do awk -v k="$key" "index(\$0, k) {f=1} END{exit !f}" "$T/cp" || { echo "CLE COMPLEMENT NON CITEE : $key"; k=$((k+1)); }; done; awk "\$1 ~ /-DECISION:\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) print substr(\$i, 8) }" "$PH/41-PREUVES.md" > "$T/dec"; while IFS= read -r o; do [ -n "$o" ] || continue; awk -v o="$o" "index(\$0, o) {f=1} END{exit !f}" "$T/all" || { echo "DECISION NON CITEE : $o"; k=$((k+1)); }; done < "$T/dec"; np2=$(awk "\$1==\"TRACE-II:\" && /non_prouvee/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); nr=$(awk "index(\$0, \"non_prouvee\") {c++} END{print c+0}" "$T/all"); ki=0; for id in PROT-02 PROT-03 PROT-04 PROT-05; do blk "$id" "$R" > "$T/n"; blk "$id" "$T/o" > "$T/b"; cmp -s "$T/n" "$T/b" || { echo "ENTREE EXISTANTE MODIFIEE $id"; ki=$((ki+1)); }; done; blk PROT-01 "$R" | sed "1s/^- \\[x\\]/- [ ]/" > "$T/n"; blk PROT-01 "$T/o" > "$T/b"; cmp -s "$T/n" "$T/b" || { echo "ENTREE EXISTANTE MODIFIEE PROT-01"; ki=$((ki+1)); }; tc=$(awk "/^\\| PROT-01 \\| Phase 41 \\| Complete — [0-9-]+, plan 41-13/{c++} END{print c+0}" "$R"); tp=$(awk "/^\\| PROT-0[1-5] \\| Phase 41 \\| Pending/{c++} END{print c+0}" "$R"); q=$(awk "/^\\| QUAL-01 \\|/ && index(\$0, \"volet admin de la Phase 41\") && index(\$0, \"en crée aucun\") {c++} END{print c+0}" "$R"); echo "ledger : coches=$x non_coches=$u cloture_prot01=$n1 complements=$np lignes_citant_O3=$o3 cles_ou_decisions_non_citees=$k entrees_existantes_modifiees=$ki trace_prot01_complete=$tc traces_pending=$tp qual01_volet_admin=$q trace_non_prouvee=$np2 recopiee=$nr"; [ "$x" -eq 5 ] && [ "$u" -eq 0 ] && [ "$n1" -eq 1 ] && [ "$np" -eq 3 ] && [ "$o3" -eq 0 ] && [ "$k" -eq 0 ] && [ "$ki" -eq 0 ] && [ "$tc" -eq 1 ] && [ "$tp" -eq 0 ] && [ "$q" -eq 1 ] && { [ "$np2" -eq 0 ] || [ "$nr" -ge 1 ]; }'</automated>
    <fails_when>exit non nul ; ou moins de 5 entrées PROT cochées ou une non cochée ; ou ligne de clôture de PROT-01 absente ou dupliquée, ou compléments PROT-02..04 différents de 3 ; ou une ligne ajoutée qui cite O-3 (la clôture et les compléments portent sur l'EXIGENCE) ; ou une clé de preuve ou une décision humaine non citée ; ou une ligne existante d'une entrée PROT réécrite (hors la case de PROT-01) ; ou traçabilité PROT-01 non `Complete` ou une ligne PROT encore `Pending` ; ou QUAL-01 sans la mention du volet admin ; ou un `non_prouvee` de la trace effacé</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - PROT-01 coché, suivi de sa ligne de clôture d'exigence (sans O-3), clés et décisions citées, `non_prouvee` recopiés ; `Complete` en traçabilité
    - PROT-02..04 : une ligne « Complément volet admin » chacun, lignes existantes intactes ; PROT-05 intact
    - QUAL-01 : « le volet admin de la Phase 41 … n'en crée aucun »
  </acceptance_criteria>
  <done>PROT-01 est clos sur pièce et le volet admin est tracé sous les exigences déjà cochées.</done>
</task>

<task type="auto">
  <name>Task 2 : critères du ROADMAP recalculés, ROADMAP et STATE à jour, éligibilité du jalon, balayage final, rejeu, SUMMARY avant publication</name>
  <files>.planning/workstreams/fiabilite/ROADMAP.md, .planning/workstreams/fiabilite/STATE.md, .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-*`, `M1-VERDICT:`, `M3-*`, `RELEASE-DECISION:`, `RELEASE:`, `INVARIANTS-PHASE:`)
    - .planning/workstreams/fiabilite/ROADMAP.md (ligne `- [ ] Phase 41:`, section `### Phase 41:`)
    - .planning/workstreams/fiabilite/STATE.md (§ Current Position)
  </read_first>
  <action>
Consigner dans `41-PREUVES.md`, section `## 41-13 — clôture` :
`CRITERES-ROADMAP: c1=<atteint|non> c2=<atteint|atteint_sur_decision|non> c3=<atteint|non_rejoue|non> c4=<atteint|non>` —
c1 : deux rulesets actifs relus et `POSE-CONFORME` à oui ; c2 : `M1-VERDICT: SUR` et
`M3-VERDICT: CONFORME` (ou `M3-DECISION` de poursuite → `atteint_sur_decision`) ; c3 : `release-de-phase`
avec tag, `check-release-tag --remote` à 0 et merge sans suite `bypass` (`preuves-sans-release` →
`non_rejoue`) ; c4 : `INVARIANTS-PHASE` à 0 gate et 0 suite ajoutés.

`.planning/workstreams/fiabilite/ROADMAP.md` :
- section `### Phase 41:` : après la note « PÉRIMÈTRE SANS ADMIN EXÉCUTÉ », ajouter une note
  `> **VOLET ADMIN EXÉCUTÉ le <date>** (reprise du 2026-09-23, D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) : …`
  donnant les quatre valeurs de `CRITERES-ROADMAP:`, PROT-01 coché, renvoi à `41-PREUVES.md` et ADR-072 ;
- liste `Plans:` : cocher les treize lignes `41-01-PLAN.md` à `41-13-PLAN.md` ;
- ligne `- [ ] Phase 41:` de la liste des phases : la cocher `- [x]` et y ajouter
  « (completed <date> — volet admin : rulesets posés et prouvés) » SEULEMENT si c1, c2 (atteint ou
  atteint_sur_decision), c3 et c4 sont atteints ; sinon la laisser `- [ ]`.
`.planning/workstreams/fiabilite/STATE.md` § Current Position : insérer en tête un paragraphe dont la première ligne commence
par « Phase: **41** (Posture de protection du dépôt) — **VOLET ADMIN EXÉCUTÉ le <date>** » (critères,
PROT-01, décisions, prochain geste) et préfixer le paragraphe précédent par
« Précédemment (2026-09-18) : » ; frontmatter du STATE non touché.

Consigner `CLOTURE-JALON: jalon=fiabilite-v1.0 eligible=<oui|non> criteres_41=<tous_atteints|incomplets> phases_non_cochees=<numéros des lignes « - [ ] Phase N: » restantes du ROADMAP, ou aucune>`.
La clôture effective du jalon n'est pas jouée ici.

Balayage final O-3 et recensement de fermeture ; rejouer `gates` et `tests`. Commit local sur C,
message « docs(41-13): ROADMAP, STATE et éligibilité du jalon sur pièce (D-02bis — arbitrage Willy,
AskUserQuestion session principale, 2026-09-23) ». Rédiger et committer `41-13-SUMMARY.md` sur C À LA
FIN de cette tâche : il écrit que le volet admin ne crée aucun gate (critère 4), recopie
`CRITERES-ROADMAP:` et `CLOTURE-JALON:`, annonce la Task 3 et recopie la règle des artefacts
post-merge (dont `/gsd-complete-milestone`, par l'orchestrateur, si et seulement si `eligible=oui`).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq "[.[] | select(.enforcement == \"active\")] | length"); cf=$(awk "\$1==\"POSE-CONFORME:\" && /branche=oui/ && /tags=oui/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); c1=$([ "$n" -eq 2 ] && [ "$cf" -ge 1 ] && echo atteint || echo non); m1=$(last M1-VERDICT:); m3=$(last M3-VERDICT:); m3d=$(val M3-DECISION: option); if [ "$m1" = SUR ] && [ "$m3" = CONFORME ]; then c2=atteint; elif [ "$m1" = SUR ] && { [ "$m3d" = consigner-et-poursuivre ] || [ "$m3d" = fermer-et-poursuivre ]; }; then c2=atteint_sur_decision; else c2=non; fi; rd=$(val RELEASE-DECISION: option); rt=$(val RELEASE: tag); rr=$(val RELEASE: check_release_tag_remote_rc); sm=$(val RELEASE: suite_merge); if [ "$rd" = release-de-phase ] && [ -n "$rt" ] && [ "$rt" != sans_objet ] && [ "$rr" = 0 ] && [ -n "$sm" ] && [ "$sm" != bypass ]; then c3=atteint; elif [ "$rd" = preuves-sans-release ]; then c3=non_rejoue; else c3=non; fi; ga=$(val INVARIANTS-PHASE: gates_ajoutes); sa=$(val INVARIANTS-PHASE: suites_ajoutees); c4=$([ "$ga" = 0 ] && [ "$sa" = 0 ] && echo atteint || echo non); cc="c1=$(val CRITERES-ROADMAP: c1) c2=$(val CRITERES-ROADMAP: c2) c3=$(val CRITERES-ROADMAP: c3) c4=$(val CRITERES-ROADMAP: c4)"; echo "criteres recalcules : c1=$c1 c2=$c2 c3=$c3 c4=$c4 | consignes : $cc"; [ "$cc" = "c1=$c1 c2=$c2 c3=$c3 c4=$c4" ]'</automated>
    <fails_when>exit non nul ; ou une valeur de `CRITERES-ROADMAP:` différente du recalcul sur pièce (critère déclaré atteint sans ses preuves, ou l'inverse)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; c1=$(val CRITERES-ROADMAP: c1); c2=$(val CRITERES-ROADMAP: c2); c3=$(val CRITERES-ROADMAP: c3); c4=$(val CRITERES-ROADMAP: c4); tous=non; if [ "$c1" = atteint ] && { [ "$c2" = atteint ] || [ "$c2" = atteint_sur_decision ]; } && [ "$c3" = atteint ] && [ "$c4" = atteint ]; then tous=oui; fi; R=.planning/workstreams/fiabilite/ROADMAP.md; l41=$(awk "/^- \\[[ x]\\] Phase 41: /{print substr(\$0,4,1); exit}" "$R"); att=$([ "$tous" = oui ] && echo x || echo " "); nt=$(awk "/^### Phase 41: /{f=1; next} /^### Phase /{f=0} f && index(\$0, \"> **VOLET ADMIN EXÉCUTÉ le \")==1 {c++} END{print c+0}" "$R"); pl=$(awk "/^- \\[x\\] 41-(0[1-9]|1[0-3])-PLAN\\.md/{c++} END{print c+0}" "$R"); pu=$(awk "/^- \\[ \\] 41-(0[1-9]|1[0-3])-PLAN\\.md/{c++} END{print c+0}" "$R"); ST=.planning/workstreams/fiabilite/STATE.md; sp=$(awk "/^## Current Position/{f=1; next} f && NF {print; exit}" "$ST" | awk "index(\$0, \"Phase: **41**\")==1 && index(\$0, \"VOLET ADMIN EXÉCUTÉ\") {c++} END{print c+0}"); pr=$(awk "index(\$0, \"Précédemment (2026-09-18) : Phase: **41**\")==1 {c++} END{print c+0}" "$ST"); pn=$(awk "/^- \\[ \\] Phase [0-9.]+:/ { s=\$0; sub(/^- \\[ \\] Phase /, \"\", s); sub(/:.*/, \"\", s); printf \"%s%s\", (n++ ? \",\" : \"\"), s } END { if (!n) printf \"aucune\" }" "$R"); el=$([ "$tous" = oui ] && [ "$pn" = aucune ] && echo oui || echo non); ce=$(val CLOTURE-JALON: eligible); cp=$(val CLOTURE-JALON: phases_non_cochees); echo "roadmap : criteres_tous_atteints=$tous phase41_case=[$l41] attendu=[$att] note_volet_admin=$nt plans_coches=$pl plans_non_coches=$pu | state : position=$sp precedemment=$pr | jalon : phases_non_cochees=$pn (consigne $cp) eligible=$el (consigne $ce)"; [ "$l41" = "$att" ] && [ "$nt" -eq 1 ] && [ "$pl" -eq 13 ] && [ "$pu" -eq 0 ] && [ "$sp" -eq 1 ] && [ "$pr" -eq 1 ] && [ "$cp" = "$pn" ] && [ "$ce" = "$el" ]'</automated>
    <fails_when>exit non nul ; ou case Phase 41 cochée alors qu'un critère n'est pas atteint (ou non cochée alors que tous le sont) ; ou note « VOLET ADMIN EXÉCUTÉ » absente ou dupliquée dans la section Phase 41 ; ou un des treize plans non coché ; ou position courante du STATE non mise à jour, ou paragraphe précédent non préfixé ; ou `CLOTURE-JALON:` différente du recalcul (phases non cochées, éligibilité)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/workstreams/fiabilite/REQUIREMENTS.md CLAUDE.md docs/ADR.md .planning/BACKLOG.md CHANGELOG.md README.md README.fr.md .planning/workstreams/fiabilite/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md $PH/41-0*-SUMMARY.md $PH/41-1[0-3]-SUMMARY.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition, O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02) — sur la doctrine, le ledger, le CHANGELOG, les README ou les SUMMARY du volet admin ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh 2>&1) || rc=$?; printf "%s\n" "$out" | awk "/^perimetre: |^allowlist: |^limite: /"; h=$(printf "%s\n" "$out" | awk "!/^perimetre: |^allowlist: |^limite: / && NF {c++} END{print c+0}"); echo "aucune-fermeture rc=$rc hits=$h"; [ "$rc" -eq 0 ] && [ "$h" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `rc=` différent de 0 ; ou `hits=` différent de 0 — une co-occurrence hors allowlist (ledger, doctrine, registre, SUMMARY) ou un porteur muet sur sa limite de fond</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; n=0; for i in 01 02 03 04 05 06 07 08 09 10 11 12 13; do if [ -f "$PH/41-$i-SUMMARY.md" ]; then n=$((n+1)); else echo "SUMMARY ABSENT : 41-$i"; fi; done; nu=$(awk "index(\$0, \"checks en cours\") && index(\$0, \"pas sur une PR rouge\") {c++} END{print c+0}" "$PH/41-08-SUMMARY.md"); ag=$(awk "index(tolower(\$0), \"aucun gate\") {c++} END{print c+0}" "$PH/41-13-SUMMARY.md"); cj=$(awk "index(\$0, \"CLOTURE-JALON\") {c++} END{print c+0}" "$PH/41-13-SUMMARY.md"); echo "SUMMARY : presents=$n nuance_41-08=$nu aucun_gate_41-13=$ag cloture_jalon_41-13=$cj"; [ "$n" -eq 13 ] && [ "$nu" -ge 1 ] && [ "$ag" -ge 1 ] && [ "$cj" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou un SUMMARY de plan absent ; ou le SUMMARY de 41-08 ne reprend pas la nuance « contournement réel sur des checks en cours, pas sur une PR rouge » ; ou le SUMMARY de 41-13 ne dit pas que le volet admin ne crée aucun gate, ou ne recopie pas `CLOTURE-JALON:`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-13-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p" && co=$((co+1)); done < "$T/f"; echo "avant publication : summary_committe=$s modifications_non_committees=$d chemins_codeowners=$co"; [ "$s" = oui ] && [ "$d" -eq 0 ] && [ "$co" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY 41-13 non committé ; ou modifications suivies non committées ; ou la PR de clôture toucherait un chemin CODEOWNERS</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `CRITERES-ROADMAP:` recalculé = consigné ; case Phase 41 cochée si et seulement si les quatre critères sont atteints ; note du volet admin et treize plans cochés ; STATE à jour
    - `CLOTURE-JALON:` recalculée = consignée ; aucune clôture de jalon jouée par ce plan
    - balayage O-3 vert ; `check-aucune-fermeture.sh` rc 0 ; treize SUMMARY présents, nuance de 41-08, « aucun gate » et `CLOTURE-JALON` dans 41-13
    - rejeux verts ; SUMMARY 41-13 committé ; PR de clôture hors CODEOWNERS ; messages de commit conformes
  </acceptance_criteria>
  <done>La phase dit sur pièce ce qu'elle a atteint, et l'éligibilité du jalon est consignée sans être présumée.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 : gestes humains de Willy (`picmakpro`) — publier C, ouvrir la PR de clôture, merger SANS contournement</name>
  <read_first>
    - `CLOTURE-JALON:` et les commandes imprimées par la Task 2
  </read_first>
  <action>Willy, compte `picmakpro`, publie C, ouvre la PR de clôture, attend la CI, vérifie l'état `CLEAN`, merge sans contournement, puis rapporte le résultat à l'orchestrateur. Aucun agent ne merge.</action>
  <instructions>
Compte qui agit : `picmakpro`.

1. `git push -u origin docs/phase-41-cloture`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head docs/phase-41-cloture --title "Phase 41 — clôture du volet admin : PROT-01, ROADMAP, STATE" --body "Preuves post-release, invariants du volet admin, PROT-01 coché sur pièce, compléments PROT-02..04, ROADMAP et STATE à jour, éligibilité du jalon consignée. Merge sans contournement."`
   (corps sans arbitrage cité ; s'il en cite un, il porte son canal et sa date)
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin.
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus --jq .mergeStateStatus`
   → `CLEAN` pour poursuivre ; `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`
   puis 3 et 4 ; tout autre état : arrêt, sans contournement.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) ;
   témoin `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit,body --jq '{state, merge: .mergeCommit.oid, body}'`.

Après ce merge : AUCUN commit direct sur `main` — le serveur refuse tout acteur hors liste, et pour
Samuel et Willy un push direct serait accepté mais tracé et signalé par G-3 : il n'est pas utilisé.
Tout artefact écrit ensuite (VERIFICATION de phase, clôture du jalon par `/gsd-complete-milestone` —
SEULEMENT si `CLOTURE-JALON: eligible=oui` —, rapport de mission) part d'une branche
`docs/phase-41-verification` créée depuis `origin/main`, et entre par une PR publiée et mergée par
Willy exactement comme aux étapes 1 à 5 (état `CLEAN`, sans `--admin`) — ou n'est pas versionné.
  </instructions>
  <verification>L'orchestrateur relit `gh pr view <n> --json state,mergeCommit,body`, vérifie que le push de merge sur `main` est vert (4 jobs), puis, avant tout artefact post-merge : `git fetch origin && git rev-list --count origin/main..main` rend `0` et `git branch --show-current` n'est pas `main` ; il ne lance `/gsd-complete-milestone` que si `CLOTURE-JALON: eligible=oui`.</verification>
  <acceptance_criteria>
    - PR de clôture ouverte depuis C, CI verte, `CLEAN`, mergée par `gh pr merge --merge` sans `--admin` ; résultat rapporté
  </acceptance_criteria>
  <resume-signal>Coller `<n>` et les sorties de 3 (fin), 4 et 5, puis écrire « PR de clôture mergée ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves → ledger, ROADMAP, STATE | cochage d'exigences et de phase, point d'entrée des lecteurs futurs |
| orchestrateur → `main` après clôture | artefacts de fin de phase et clôture de jalon écrits hors du plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-43 | Repudiation | exigence cochée en effaçant un `non_prouvee` | medium | mitigate | recopie vérifiée dans les lignes ajoutées |
| T-41-46 | Repudiation | clôture par commit direct | medium | mitigate | PR de clôture hors CODEOWNERS, état `CLEAN` exigé, `--admin` exclu, merge relu par l'orchestrateur |
| T-41-55 | Repudiation | clôture d'exigence confondue avec O-3 | medium | mitigate | lignes ajoutées sans O-3 (vérifié) ; balayage O-3 et `check-aucune-fermeture.sh` |
| T-41-56 | Tampering | artefact post-merge poussé directement sur `main` | medium | mitigate | branche `docs/phase-41-verification` + PR ; `git rev-list --count origin/main..main` à 0 relu par l'orchestrateur |
| T-41-67 | Repudiation | phase ou jalon déclarés complets sans critères atteints | high | mitigate | `CRITERES-ROADMAP:` recalculé sur pièce ; case Phase 41 et `CLOTURE-JALON: eligible` dérivées de ces valeurs (vérifié) ; `/gsd-complete-milestone` seulement si `eligible=oui` |
</threat_model>

<verification>
Ledger coché et complété sur pièce sans confusion avec O-3 ; ROADMAP et STATE conformes aux critères
réellement atteints ; éligibilité du jalon consignée ; PR de clôture mergée par le chemin normal ;
artefacts post-merge cantonnés à une branche et une PR.
</verification>

<success_criteria>
Critères de succès 1 à 4 de la Phase 41 consignés sur pièce (atteints, ou non rejoué pour le 3 sur
décision) ; PROT-01 clos ; jalon fiabilite-v1.0 déclaré éligible à la clôture seulement si la phase
est complète.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-13-SUMMARY.md` — committé sur la branche C à la fin de la Task 2, AVANT la publication (Task 3). Tout artefact postérieur au merge suit la règle du frontmatter (branche + PR, ou non versionné).
</output>

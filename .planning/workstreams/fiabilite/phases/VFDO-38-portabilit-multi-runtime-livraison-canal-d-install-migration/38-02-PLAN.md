---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - plugin/_internal/runtime-cli-dispatch.sh
  - plugin/_internal/tests/test-runtime-cli-dispatch.sh
  - plugin/dev-orchestrator/scripts/ensure-deps.sh
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/design-orchestrator/scripts/ensure-design-deps.sh
  - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh
  - plugin/conductor/scripts/check-plugin-update.sh
  - plugin/conductor/scripts/vf-update-run.sh
  - plugin/conductor/scripts/tests/test-vf-update.sh
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
  - README.md
  - README.fr.md
autonomous: true
requirements: [RUNT-01, RUNT-02]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un opérateur qui installe/active les plugins compagnons (superpowers, ui-ux-pro-max, frontend-design, impeccable) depuis un poste où le runtime détecté n'est PAS `claude` obtient soit un geste réellement exécuté sur CE runtime (Codex, canal natif mesuré), soit une dégradation DÉCLARÉE (étapes manuelles affichées, code de sortie propre) — jamais un `command -v claude` qui échoue en silence"
    - "Les 4 verbes (list --json, install, enable, marketplace add) passent par UNE SEULE table de dispatch partagée par les 4 fichiers CLI-couplés — jamais 4 réimplémentations divergentes du même `command -v`"
    - "Sur OpenCode et kimi-code (non installés au 2026-08-28, cf. 38-CONTEXT.md), le dispatch les nomme explicitement comme non mesurés — jamais un résultat inventé pour une cible absente du poste"
    - "Sur un poste Codex détecté, le bootstrap pose la précondition `multi_agent_v2` (sans elle, AUCUN outil de spawn n'existe — mesuré en session réelle, 38-CONTEXT.md lignes 445-456) via la commande OFFICIELLE `codex features enable multi_agent_v2` (mesurée sur ce binaire, jamais une édition manuelle de `config.toml`), idempotent — jamais un second appel si déjà active"
    - "Sur un poste Codex détecté, le bootstrap DÉCLARE (jamais n'auto-écrit) l'état de `trust_level` du dépôt — sans lui, `.codex/agents/` n'est jamais parsé, silencieusement (`codex doctor` rend quand même `overall: ok`, 38-CONTEXT.md lignes 457-461) ; aucune commande `codex trust` n'existe sur ce binaire (mesuré via `codex --help`), le consentement de confiance reste un geste humain (TUI interactive), jamais un fix automatique (ADR-031)"
  artifacts:
    - "plugin/_internal/runtime-cli-dispatch.sh : script partagé, lecture d'un runtime (détection ou override), table à 4 verbes, résolu par les 4 sites via la MÊME cascade à 3 positions déjà utilisée par find_hooks_merger()/find_command_generator() (D-01 : jamais un `source` à effets de bord) ; porte aussi la précondition Codex (multi_agent_v2 posé, trust_level déclaré)"
    - "plugin/_internal/tests/test-runtime-cli-dispatch.sh : suite dédiée neuve"
  key_links:
    - "runtime-cli-dispatch.sh <verb> -> claude plugin <verb> (runtime=claude) OU codex plugin <verb> (runtime=codex, canal natif confirmé 38-CONTEXT.md '`codex plugin` existe') OU message d'étape manuelle + exit propre (runtime absent/non supporté, RUNT-02)"
    - "ensure-deps.sh:449,464,472,479-480 (detect_superpowers/ensure_superpowers) -> runtime-cli-dispatch.sh : les 2 sites `command -v claude`/`claude plugin` routent par le dispatch, jamais un appel `claude` figé"
    - "ensure-design-deps.sh:168,183,222,305,324 (detect_all/process_plugin) -> runtime-cli-dispatch.sh : les 5 sites (dont la double stratégie S1 JSON / S2 awk texte de détection) routent par le dispatch pour les verbes list/enable/marketplace-add/install"
    - "runtime-cli-dispatch.sh ensure-codex-preconditions -> codex features list/enable multi_agent_v2 (commandes officielles, mesurées) + lecture seule de config.toml pour trust_level : appelée depuis ensure-deps.sh UNIQUEMENT quand le runtime détecté est codex, best-effort, jamais bloquante pour le reste du bootstrap"
    - "check-plugin-update.sh:60-61 (repli `claude plugin list` sur installed_plugins.json absent) -> runtime-cli-dispatch.sh : même repli, runtime-aware"
  prohibitions:
    - "Ne jamais faire disparaître le repli 'étape manuelle affichée, exit 0' déjà en place pour un runtime `claude` absent — RUNT-02 l'ÉTEND aux autres runtimes, il ne le retire jamais pour claude"
    - "Ne jamais coder en dur un comportement Codex non mesuré (OpenCode, kimi-code) : le dispatch se limite à ce que 38-CONTEXT.md confirme ('codex plugin existe, canal natif') pour Codex, et se dégrade en 'non mesuré, étapes manuelles' pour les deux autres"
    - "Ne pas toucher à la logique de PARSING de l'état (JSON S1 / awk S2 dans ensure-design-deps.sh, installed_plugins.json dans check-plugin-update.sh) : seul le POINT D'EXÉCUTION du verbe CLI change de forme, jamais la logique de détection d'état déjà mesurée et durcie (ADR-054 CRLF, etc.)"
    - "Ne jamais écrire automatiquement `trust_level` dans `~/.codex/config.toml` — aucune commande `codex trust` n'existe sur ce binaire (mesuré), et le consentement de confiance est un geste humain (TUI interactive) que ce lot ne doit jamais court-circuiter (ADR-031) ; seule la DÉCLARATION (lecture + message) est du ressort de ce lot"
    - "`codex features enable multi_agent_v2` reste le SEUL mécanisme d'écriture autorisé pour cette précondition — jamais une édition manuelle de `config.toml` (aucune commande `codex config` n'existe, le risque de mutiler des tables voisines est réel et mesuré au cadrage)"
    - "D-37-4 : `opencode run --auto` reste FORMELLEMENT INTERDIT — le dispatch ne doit JAMAIS le construire ni le suggérer dans un message d'étape manuelle, même pour OpenCode (seul mécanisme identifié qui convertit l'absence d'humain en consentement automatique)"
---

<objective>
Lot 2 (`RUNT`) du cadrage 38-CONTEXT.md, implémentant D-37-1 (l'installeur de VibeFlow devient
multi-runtime — prémisse du rapatriement de superpowers démentie, le catalogue de skills est déjà
multi-runtime, ce qui restait Claude-only était l'installeur) : les 4 fichiers dont le couplage réel au CLI `claude`
dépasse le motif initial de l'étude (12 sites exécutables sur 4 fichiers, pas 11 sur 2 — corrigé au
cadrage) passent par une détection de runtime + une table de dispatch partagée, au lieu d'un appel
`claude` figé. Séquencé APRÈS le lot 1 (FIDE) : ordre imposé par REQUIREMENTS.md
(« FIDE → RUNT → ROLL → TGT → ADPT → MIGR ») et par le partage réel de `plugin/conductor/VERSION`/
`CHANGELOG.md`/les deux README (FIDE a déjà posé leur 69e suite ; ce lot en ajoute une 70e).

Purpose: sans ce lot, tout ce que le lot 5 (ADPT) posera pour Codex reste inatteignable par la
chaîne d'onboarding elle-même — un opérateur sur Codex qui lance le bootstrap design/dev verrait 4
`command -v claude` échouer avant même d'atteindre l'adaptateur.

Output: `runtime-cli-dispatch.sh` (script partagé + suite dédiée), les 4 sites CLI-couplés
re-routés, README synchronisés sur la 70e suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/dev-orchestrator/scripts/ensure-deps.sh
@plugin/design-orchestrator/scripts/ensure-design-deps.sh
@plugin/conductor/scripts/check-plugin-update.sh
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : bout-en-bout — un verbe, un site, un dispatch réel sur Codex (RUNT-01)</name>
  <files>plugin/_internal/runtime-cli-dispatch.sh, plugin/_internal/tests/test-runtime-cli-dispatch.sh, plugin/dev-orchestrator/scripts/ensure-deps.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 207-214 (couplage réel : 4 verbes `list --json`/`install`/`enable`/`marketplace add`, 12 sites sur 4 fichiers — pas le motif étroit de l'étude Phase 37 ; sites hors motif listés précisément) et lignes 243-262 (baseline mesurée : Codex présent+authentifié, canal `codex plugin` natif confirmé, OpenCode/kimi-code ABSENTS du poste — 5 sondes concordantes)
    - plugin/dev-orchestrator/scripts/ensure-deps.sh:445-491 (`detect_superpowers()`/`ensure_superpowers()` : 2 sites `command -v claude` (449, 464) + 2 sites `claude plugin install`/`marketplace add` (472, 479-480), avec repli étape manuelle DÉJÀ en place quand `claude` est absent (464-469, 487-490) — CE repli reste le patron à imiter pour les autres runtimes, jamais réinventé)
    - plugin/dev-orchestrator/scripts/check-gsd-engine.sh:66-81 (`default_gsd_home_new()` : patron de cascade à dupliquer pour la résolution du script partagé — D-01, jamais un `source` à effets de bord)
    - plugin/_internal/vibeflow-update.sh:1053-1058 (`find_hooks_merger()` : patron EXACT de cascade à 3 positions déjà utilisé dans ce dépôt pour localiser un script partagé posé par un AUTRE module que l'appelant — `$CACHE_DIR/_internal/…` puis `$(dirname "$0")/…`, à reproduire pour `runtime-cli-dispatch.sh`)
    - plugin/_internal/tests/test-vibeflow-update.sh:1-30 (convention de suite : `set -uo pipefail` sans `-e`, `mktemp -d` + `trap`, `ok()`/`ko()`/`skip()`, asserts `T<n>.<m>`, bannière de résultat)
  </read_first>
  <action>
Créer `plugin/_internal/runtime-cli-dispatch.sh`, lecture seule côté détection, usage
`runtime-cli-dispatch.sh <verb> [args...]` où `<verb>` ∈ `list-json`, `install`, `enable`,
`marketplace-add`.

Détection du runtime (`detect_agent_runtime()`) : `VF_RUNTIME` (variable d'environnement, override
explicite, prioritaire) sinon cascade de présence `command -v` dans l'ordre `claude`, `codex`,
`opencode`, `kimi-code` — premier trouvé gagne, aucun trouvé → chaîne vide (« absent »). Ne jamais
tenter de deviner via une variable d'environnement propriétaire d'un runtime tiers non mesurée.

Table de dispatch, un verbe -> une commande par runtime CONNU :
- `claude` : comportement actuel inchangé — `claude plugin list --json` / `claude plugin install
  <id> --scope <s>` / `claude plugin enable <id> --scope <s>` / `claude plugin marketplace add
  <repo> --scope <s>`.
- `codex` : `codex plugin` est confirmé natif par 38-CONTEXT.md (« canal natif ») — dispatcher vers
  `codex plugin <verbe-équivalent>` avec la MÊME forme d'arguments. Si l'exécution réelle échoue
  (grammaire différente non mesurée pour un sous-verbe précis), ne jamais avaler l'échec : relayer
  le code de sortie réel et le stderr du sous-processus, jamais un succès supposé.
- runtime absent ou non `claude`/`codex` (OpenCode, kimi-code — non mesurés) : imprimer le message
  d'étape manuelle correspondant (repris tel quel du patron `print_manual_step` déjà présent dans
  `ensure-design-deps.sh:145-160`, généralisé pour prendre le nom du plugin en argument), exit 0
  (jamais un crash — RUNT-02 : dégradation déclarée, pas un échec).

Chaque appelant résout le script par la cascade EXACTE de `find_hooks_merger()`
(`$CACHE_DIR/_internal/runtime-cli-dispatch.sh` puis `$(dirname "$0")/runtime-cli-dispatch.sh`),
jamais un nouveau mécanisme de résolution. Script introuvable aux deux positions → repli sur le
comportement `claude`-figé ACTUEL (jamais une régression silencieuse sur un poste où le script
partagé n'est pas encore posé — cas d'un `update` partiel).

Wiring bout-en-bout (site 1 seulement, dans `ensure-deps.sh`) : remplacer les 2 sites `command -v
claude`/`claude plugin` de `detect_superpowers()`/`ensure_superpowers()` (449, 464, 472, 479-480)
par des appels à `runtime-cli-dispatch.sh list-json` / `install superpowers@claude-plugins-official
--scope "$SUPERPOWERS_SCOPE"` / `marketplace-add anthropics/claude-plugins-official --scope
"$SUPERPOWERS_SCOPE"`. Le repli « CLI runtime introuvable → étape manuelle » (464-469) devient «
aucun runtime détecté → étape manuelle », texte adapté pour ne plus nommer `claude` spécifiquement
quand la détection a échoué pour tous les runtimes.

Bump `plugin/dev-orchestrator/VERSION` (patch — durcissement, aucune capacité utilisateur neuve) et
son `CHANGELOG.md`.

Créer `plugin/_internal/tests/test-runtime-cli-dispatch.sh` sur le patron de
`test-vibeflow-update.sh`. Cas : T1 (`VF_RUNTIME=claude` + un faux binaire `claude` en fixture PATH
→ la commande construite/exécutée contient bien `plugin list --json`), T2 (`VF_RUNTIME=codex` +
faux binaire `codex` → commande équivalente construite pour `codex plugin`), T3 (`VF_RUNTIME=
opencode`, aucun binaire réel requis → message d'étape manuelle sur stdout, exit 0, AUCUNE tentative
d'exécution d'un binaire absent), T4 (aucun `VF_RUNTIME`, `PATH` de sonde vide → détection rend
« absent », même comportement que T3).
  </action>
  <acceptance_criteria>
    - `[ -x plugin/_internal/runtime-cli-dispatch.sh ]`.
    - Sur une fixture `PATH` ne contenant qu'un faux `claude` exécutable (script qui journalise ses arguments), `VF_RUNTIME=claude runtime-cli-dispatch.sh install foo@bar --scope user` produit un journal d'invocation contenant EXACTEMENT `plugin install foo@bar --scope user` — capturé et grep, jamais recopié en dur sans exécution.
    - Sur `VF_RUNTIME=opencode`, la commande sort 0 et n'invoque AUCUN binaire (vérifié par l'absence de tout binaire sur le `PATH` de sonde — un `command not found` ferait échouer le test s'il tentait quand même l'exécution).
    - `grep -c 'runtime-cli-dispatch.sh' plugin/dev-orchestrator/scripts/ensure-deps.sh` rend au moins `2` (les 2 sites reroutés de la tâche 1).
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort 0 (non-régression Superpowers).
    - `bash plugin/_internal/tests/test-runtime-cli-dispatch.sh` sort 0, bannière `== résultat :`.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-runtime-cli-dispatch.sh && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <done>Un premier site réel (`ensure-deps.sh`) route ses 4 verbes CLI par un dispatch runtime-aware réel, testé sur `claude` ET `codex`, dégradé proprement sur les runtimes non mesurés — la suite dédiée et la non-régression Superpowers sont vertes.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : les 3 sites restants + synchro README (RUNT-01, RUNT-02)</name>
  <files>plugin/design-orchestrator/scripts/ensure-design-deps.sh, plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh, plugin/conductor/scripts/check-plugin-update.sh, plugin/conductor/scripts/vf-update-run.sh, plugin/conductor/scripts/tests/test-vf-update.sh, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/CHANGELOG.md, plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md, README.md, README.fr.md</files>
  <read_first>
    - plugin/design-orchestrator/scripts/ensure-design-deps.sh:162-239 (`detect_all()` : S1 `claude plugin list --json` (183) + S2 repli awk sur `claude plugin list` texte (222) — les DEUX restent des appels de LECTURE, à re-router par le dispatch en conservant EXACTEMENT le parsing JSON/awk existant, seul le point d'exécution change) et lignes 290-334 (`process_plugin()` : sites `claude plugin enable` (305) et `claude plugin marketplace add`/`claude plugin install` (324, 327))
    - plugin/conductor/scripts/check-plugin-update.sh:38-62 (repli `claude plugin list` (60-61) uniquement si `installed_plugins.json` absent ou non exploitable — best-effort, ne JAMAIS faire échouer le script si le dispatch lui-même échoue, `installed` reste vide et la comparaison de version est simplement sautée, patron déjà en place ligne 76-79 pour le réseau KO)
    - plugin/conductor/scripts/vf-update-run.sh:1-10,44-54 (aucun appel CLI exécutable — seul un couplage DOCUMENTAIRE : le commentaire d'en-tête suppose `claude plugin update vibeflow` comme précondition. Ajuster la PROSE (commentaire + `[vf-update-run]` message d'usage) pour ne plus nommer `claude` spécifiquement, sans toucher à la logique — ce fichier n'a aucun verbe à dispatcher)
    - scripts/check-version-sync.sh:123-131 (compteur `N suites` — la suite créée en tâche 1 fait passer le compte de 69 à 70)
  </read_first>
  <behavior>
    - Test : sur `VF_RUNTIME=claude`, `process_plugin()` d'un plugin à l'état `disabled` produit toujours `claude plugin enable <id> --scope <s>` — comportement BYTE-IDENTIQUE à avant ce lot (non-régression sur les 4 plugins de la table `design-toolchain.md`).
    - Test : sur `VF_RUNTIME=codex`, le même appel produit `codex plugin enable <id> --scope <s>` (dispatché, pas un `command -v claude` qui échouerait).
    - Test : `check-plugin-update.sh` sur un poste sans `installed_plugins.json` ET sans runtime détecté ne plante pas — `installed=""`, la comparaison de version est sautée, le cache n'est pas réécrit avec une valeur mensongère.
  </behavior>
  <action>
Dans `ensure-design-deps.sh`, router les 5 sites (183, 222, 305, 324, 327) par
`runtime-cli-dispatch.sh` selon le MÊME patron que la tâche 1 (cascade `find_hooks_merger()`,
repli sur le comportement `claude`-figé si le script partagé est introuvable). Conserver
INTÉGRALEMENT la machine à états S1 JSON / S2 awk de `detect_all()` — seul le sous-processus
invoqué change, jamais le parsing de sa sortie. `CLAUDE_AVAILABLE`/`INDETERMINE` (163-164) se
généralisent en `RUNTIME_AVAILABLE`/`INDETERMINE` sans changer la sémantique des branches
existantes.

Dans `check-plugin-update.sh`, router le site 60-61 par le dispatch (`list-json`, parser la version
depuis le JSON plutôt que l'`awk` sur texte brut si le dispatch expose déjà le JSON — sinon garder
le même `awk` sur la sortie relayée). Best-effort inchangé : dispatch en échec → `installed=""`,
jamais un exit non-zéro qui casserait le bandeau SessionStart.

Dans `vf-update-run.sh`, ajuster uniquement la PROSE (commentaire d'en-tête ligne 4, message
ligne 49) pour dire « après la mise à jour du plugin sur le runtime actif » plutôt que nommer
`claude` spécifiquement — zéro changement de logique exécutable dans ce fichier.

Bump `plugin/design-orchestrator/VERSION` (patch) + `CHANGELOG.md`. Bump `plugin/conductor/VERSION`
(patch — `check-plugin-update.sh`/`vf-update-run.sh` vivent dans `conductor`) + `CHANGELOG.md`.

Rejouer la découverte COMPLÈTE des suites (`find plugin scripts -type f -path
'*/tests/test-*.sh'`), confirmer le passage 69 → 70, corriger le littéral dans README.md ET
README.fr.md (grep la ligne actuelle avant de la remplacer — ne jamais supposer le nombre sans
l'avoir dérivé sur CE run, l'ordre d'exécution réel des lots précédents pouvant différer du plan).
  </action>
  <acceptance_criteria>
    - `grep -c 'runtime-cli-dispatch.sh' plugin/design-orchestrator/scripts/ensure-design-deps.sh` rend au moins `4`.
    - Sur fixture `VF_RUNTIME=claude`, un plugin `disabled` produit `claude plugin enable` (grep sur journal réel) ; sur `VF_RUNTIME=codex`, `codex plugin enable` — les deux capturés depuis une exécution réelle, jamais affirmés sans grep.
    - `bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh` sort 0 (non-régression des 4 plugins de la table).
    - `bash plugin/conductor/scripts/tests/test-vf-update.sh` sort 0.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` = 70, et ce chiffre apparaît dans README.md ET README.fr.md (`grep -o '[0-9][0-9]* suites' README.md`).
    - `bash scripts/check-version-sync.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh && bash plugin/conductor/scripts/tests/test-vf-update.sh && bash plugin/_internal/tests/test-runtime-cli-dispatch.sh</automated>
  </verify>
  <done>Les 4 fichiers CLI-couplés (12 sites) routent tous par le dispatch partagé, dégradés proprement sur runtime non mesuré, les deux README comptent 70 suites, et la découverte complète reste 100% verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : précondition Codex — multi_agent_v2 posé, trust_level déclaré (correction de cadrage post-sonde, RUNT-01 étendu)</name>
  <files>plugin/_internal/runtime-cli-dispatch.sh, plugin/_internal/tests/test-runtime-cli-dispatch.sh, plugin/dev-orchestrator/scripts/ensure-deps.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 445-461 (verdicts #4 et #1 de la sonde en session Codex RÉELLE, 2026-08-28 : `multi_agent_v2` est une précondition DURE et 100% silencieuse — sans elle, aucun outil de spawn n'existe, `multi_agent=true` seul ne suffit pas ; le `trust_level` gate `.codex/agents/` en tout ou rien, silencieusement, avec une asymétrie mesurée envers les skills qui se chargent SANS trust)
    - Sortie mesurée DIRECTEMENT sur ce poste (`codex --help`, `codex features --help`, `codex features enable --help`) : commandes officielles confirmées — `codex features list` (« List known features with their stage and effective state »), `codex features enable <FEATURE>` (« Enable a feature in config.toml », équivalent `-c features.<name>=true`). AUCUNE commande `codex trust`/`codex config set` n'existe dans l'arborescence de commandes du binaire (mesuré, pas supposé) — pour le trust, ce lot se limite à lire `config.toml` et déclarer, jamais à écrire
    - plugin/dev-orchestrator/scripts/ensure-deps.sh:445-491 (patron `detect_superpowers()`/`ensure_superpowers()` : précédent direct d'une précondition best-effort, auto-appliquée sans checkpoint individuel — D-08/BOOT-02, à IMITER pour `multi_agent_v2` puisque c'est une capacité stable qu'active l'utilisateur en choisissant d'installer VibeFlow sur Codex, PAS à imiter pour `trust_level` qui reste un geste de consentement humain distinct, ADR-031)
  </read_first>
  <behavior>
    - Test : `VF_RUNTIME=codex` + faux binaire `codex` de fixture dont `features list` répond (texte, format observé au moment de l'implémentation — ne jamais supposer la mise en page réelle du binaire) une ligne `multi_agent_v2` à l'état désactivé → la précondition appelle `codex features enable multi_agent_v2` — vérifié par journal des arguments du faux binaire.
    - Test : même fixture mais `features list` répond déjà `multi_agent_v2` actif → AUCUN appel à `enable` (idempotent, journal vide sur ce point).
    - Test : fixture `CODEX_HOME` de sonde dont `config.toml` NE PORTE AUCUN bloc `[projects."<racine-repo-de-sonde>"]` → un message de déclaration apparaît sur la sortie (stdout ou stderr, à choisir en cohérence avec le reste du fichier), ET le fichier `config.toml` de la fixture reste BYTE-IDENTIQUE avant/après (hash comparé) — aucune écriture, jamais.
  </behavior>
  <action>
Ajouter à `runtime-cli-dispatch.sh` une fonction/verbe `ensure-codex-preconditions` (invoquée
uniquement quand `detect_agent_runtime()` rend `codex`) :
1. `multi_agent_v2` — exécuter `codex features list`, chercher la ligne portant `multi_agent_v2`
   (observer le format réel du binaire au moment de l'implémentation, ne pas le deviner depuis ce
   texte de plan) et en dériver l'état actif/inactif. Inactif → exécuter `codex features enable
   multi_agent_v2` (commande OFFICIELLE mesurée, jamais une édition manuelle de `config.toml`),
   puis reconfirmer par un second `codex features list`. Déjà actif → ne rien faire, log discret
   (idempotent — un `update` rejoué ne doit jamais re-déclencher `enable`).
2. `trust_level` — résoudre `git rev-parse --show-toplevel` (racine du dépôt cible), lire
   `${CODEX_HOME:-$HOME/.codex}/config.toml` en LECTURE SEULE, chercher un bloc
   `[projects."<racine>"]` portant `trust_level = "trusted"`. Absent → message déclaré (jamais
   bloquant) : « trust_level non confirmé pour ce dépôt — `.codex/agents/` ne sera pas parsé tant
   qu'un lancement interactif de `codex` dans ce dossier n'aura pas répondu au prompt de confiance
   ». AUCUNE tentative d'écriture — ni commande dédiée mesurée, ni édition manuelle de fichier.

Appeler `ensure-codex-preconditions` depuis `ensure-deps.sh`, au même point de flux que
`ensure_superpowers()` (best-effort — un échec de cette précondition ne doit JAMAIS faire échouer
le reste du bootstrap), gaté sur `detect_agent_runtime() = codex` uniquement (aucun effet sur un
poste `claude`/`opencode`/`kimi-code`).

Compléter (pas remplacer) les entrées `CHANGELOG.md` déjà écrites en tâche 2 pour
`plugin/dev-orchestrator` et `plugin/conductor` (même version — cette tâche enrichit la même pose
sémantique « installeur multi-runtime », elle ne justifie pas un second bump).
  </action>
  <acceptance_criteria>
    - Sur fixture `codex` inactif pour `multi_agent_v2`, le journal du faux binaire contient `features enable multi_agent_v2` — capturé et grep, jamais affirmé sans exécution.
    - Sur fixture `codex` déjà actif, le journal NE contient PAS `features enable` — non-idempotence testée explicitement, pas seulement le cas positif.
    - `sha256sum` du `config.toml` de fixture AVANT et APRÈS l'exécution de la précondition (cas trust absent) sont IDENTIQUES.
    - `grep -n 'ensure-codex-preconditions\|ensure_codex_preconditions' plugin/dev-orchestrator/scripts/ensure-deps.sh` rend au moins 1 occurrence.
    - `bash plugin/_internal/tests/test-runtime-cli-dispatch.sh` et `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sortent 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-runtime-cli-dispatch.sh && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <done>Un poste Codex détecté obtient `multi_agent_v2` posé (idempotent, via la commande officielle) et `trust_level` déclaré sans jamais être auto-écrit — les deux dégradations 100% silencieuses mesurées en session réelle sont fermées côté installeur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binaire runtime détecté (claude/codex/…) -> sous-processus exécuté | le dispatch construit une ligne de commande depuis des arguments internes (id de plugin, scope) — jamais depuis une entrée utilisateur non validée |
| sortie `claude plugin list`/`codex plugin list` -> parsing JSON/awk | source externe (CLI tierce) réinjectée dans la logique de décision (enabled/disabled/absent) — un format inattendu doit dégrader vers INDETERMINE, jamais un faux positif |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-04 | Denial of Service | runtime-cli-dispatch.sh (runtime non détecté) | medium | mitigate | repli étape manuelle + exit 0 systématique (RUNT-02), jamais un crash qui interromprait tout `ensure-deps.sh` |
| T-38-05 | Tampering | dispatch route un verbe vers le mauvais runtime (détection erronée sur poste multi-CLI) | low | accept | `VF_RUNTIME` explicite disponible en override ; ordre de cascade documenté et testé (T1-T4) |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | aucun paquet externe installé par ce plan |
</threat_model>

<verification>
1. `bash plugin/_internal/tests/test-runtime-cli-dispatch.sh` sort 0.
2. `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` et `test-design-orchestrator.sh` sortent 0 (non-régression Superpowers + 4 plugins design).
3. `bash plugin/conductor/scripts/tests/test-vf-update.sh` sort 0.
4. `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` = 70, reflété dans les deux README.
5. `bash scripts/check-version-sync.sh` sort 0.
</verification>

<success_criteria>
- Les 4 fichiers CLI-couplés dispatchent leurs 4 verbes par un runtime détecté, jamais un `claude` figé.
- Sur Codex (canal natif confirmé), le dispatch exécute réellement l'équivalent `codex plugin`.
- Sur OpenCode/kimi-code (non mesurés), la dégradation est déclarée — étapes manuelles, exit propre.
- Non-régression complète : les 70 suites (69 + cette suite dédiée) restent vertes.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-02-SUMMARY.md` avec : la commande construite pour `claude` et pour `codex` sur le même verbe (capturées réellement), le résultat du rejeu complet des 70 suites.
</output>

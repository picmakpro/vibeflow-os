---
phase: 41-posture-de-protection-du-d-p-t
plan: 10
lot: L10 — PR des preuves et de la release : statut O-3, BACKLOG, mesures reportées dans ADR-072, bump dérivé, merge SANS contournement
type: execute
wave: 8
depends_on: ["41-09"]
autonomous: false
requirements: [PROT-02, PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md
  - .planning/BACKLOG.md
  - docs/ADR.md
  - VERSION
  - plugin/.claude-plugin/plugin.json
  - plugin/.codex-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - README.fr.md
  - CHANGELOG.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Précondition : `M1-VERDICT: SUR`, `PUSH-DIRECT: refuse=oui`, `M4-VERDICT: CONFORME`, `TRACE-II:` consignée ; ou, à défaut, une décision humaine datée consignée pour l'écart (Tasks 4 de 41-07 / 41-09)"
    - "O-3 du `25-SECURITY.md` reçoit une ligne de statut datée « gardée par défaut + tracée » renvoyant à `41-PREUVES.md` et ADR-072 ; le texte d'origine d'O-3 reste intact ; aucun vocabulaire d'achèvement dans cette ligne (D-02)"
    - "L'item BACKLOG « Posture de protection de `main` » reçoit une ligne de statut « TRAITÉ par renvoi » (Phase 41, ADR-072) sans qu'aucune de ses lignes existantes ne soit modifiée (D-M10)"
    - "ADR-072 reçoit une sous-section « Mesures à la pose » qui recopie les résultats consignés (M-1, M-2, M-3, trace du temps ii règle par règle, push direct, M-4, accès aux rule suites) — jamais une valeur absente de `41-PREUVES.md`"
    - "Numéro de release DÉRIVÉ à l'exécution : patch suivant de la `VERSION` alors sur `origin/main` (hotfix v2.63.2 compris s'il est mergé), appliqué par `scripts/bump.sh` ; jamais écrit en dur dans ce plan (D-M8 : durcissement = patch)"
    - "La PR des preuves ne touche aucun chemin CODEOWNERS, ni `plugin/dev-orchestrator/`, ni `.github/workflows/ci.yml`, ni `scripts/check-release-tag.sh`, ni `scripts/hooks/` : elle doit être mergeable sans revue et sans contournement (D-M5, D-M9)"
    - "Rejeu du flux de release, première moitié (critère 3) : PR ouverte, CI verte, état de merge `CLEAN`, merge `--merge` SANS `--admin` ni case de contournement ; un état `BLOCKED` arrête le plan, il n'est jamais contourné"
  artifacts:
    - path: ".planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md"
      provides: "statut daté d'O-3"
    - path: "docs/ADR.md"
      provides: "ADR-072 § Mesures à la pose"
    - path: "VERSION"
      provides: "version patch dérivée, synchronisée par `bump.sh`"
  key_links:
    - "Le SUMMARY de ce plan est committé sur B AVANT le push de la Task 3 : le merge de la PR des preuves est relu et consigné par 41-11 sur une troisième branche (D-03)"
---

<objective>
Préparer et merger, sans aucun contournement, la PR qui porte les preuves, le statut d'O-3, la
doctrine mesurée et le bump de release.

Purpose: PROT-04 (statut O-3 sur pièce), PROT-03 (ADR-072 complétée par la mesure, BACKLOG traité),
PROT-02 (première moitié du rejeu du flux de release sous la règle).
Output: PR des preuves mergée ; branche B entrée sur `main` par le chemin normal.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@CLAUDE.md

Extraits à lire : `25-SECURITY.md` L260-275 (bloc O-3) ; `.planning/BACKLOG.md` L551-580 (item
« Posture de protection de `main` ») ; `docs/ADR.md` § ADR-072 ; `scripts/bump.sh` (en-tête :
sources de version, squelette CHANGELOG) ; `README.md` et `README.fr.md` § Versioning (table
d'historique, première ligne de données) ; `CHANGELOG.md` en tête (dernière entrée `## [v…]`).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : B à jour de `main`, statut O-3, BACKLOG traité par renvoi, mesures reportées dans ADR-072</name>
  <files>.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md, .planning/BACKLOG.md, docs/ADR.md, .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes)
    - les extraits listés dans le contexte
  </read_first>
  <action>
Identité `samuel-neveugall` ; branche B ; `git fetch origin` ; `git merge --no-edit origin/main` en
local (le merge de PR-P et un éventuel hotfix y entrent ; un conflit s'arrête et se remonte).
Vérifier la précondition du frontmatter.

`25-SECURITY.md` : sous la puce O-3, sans modifier ses lignes, ajouter UNE ligne indentée de
continuation de liste :
`  **Statut Phase 41 (<date d'exécution>) : gardée par défaut + tracée.** Une hausse de baseline exige l'approbation de `@picmakpro` (CODEOWNERS, revue code owner requise par le ruleset de `main`) ou un contournement explicite par un compte write, qui laisse une entrée dans les rule suites (D-02, D-05 — arbitrages Samuel, AskUserQuestion session principale, 2026-09-17). Preuves : `41-PREUVES.md` (`CO-I:`, `TRACE-II:`) ; politique : ADR-072.`
Aucun autre mot dans cette ligne : le statut n'est pas un achèvement (D-02).

`.planning/BACKLOG.md`, item « Posture de protection de `main` » : ajouter à la fin de l'item, avant
l'item suivant, une ligne `**Statut (<date d'exécution>, Phase 41) : TRAITÉ par renvoi** — rulesets
de branche et de tags posés et prouvés, CODEOWNERS étroit, politique de contournement et de hotfix :
ADR-072 ; preuves : `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md`.` Aucune
ligne existante de l'item modifiée (son titre est cité ailleurs).

`docs/ADR.md`, fin d'ADR-072 : sous-section `### Mesures à la pose (Phase 41, <date d'exécution>)`,
une puce par mesure, format `- **M-1** : <valeur> — <une phrase>` avec, recopiées de
`41-PREUVES.md` : `M1-VERDICT` ; `admin_couvert` de `M2:` (et les quatre valeurs de
`current_user_can_bypass`) ; `resultat` de `M3:` (et l'état avant, dont les checks en cours) ; pour
`**Trace (ii)**` les statuts `pull_request=` et `required_status_checks=` de `TRACE-II:` et le
lecteur ; pour `**Push direct**` la valeur `refuse=` ; `M4-VERDICT` pour `**M-4**` ; pour
`**Accès aux rule suites**` les deux valeurs d'`ACCES-RULE-SUITES:`. Écrire explicitement que le
contournement réel de la règle des checks a porté sur des checks non encore verts, pas rouges.
Ajouter à `41-PREUVES.md` une section `## 41-10 — PR des preuves`. Commit local sur B.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md; T=$(mktemp -d); awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$F" > "$T/o3"; s=$(awk "index(\$0, \"**Statut Phase 41 (\")" "$T/o3"); n=$(awk "index(\$0, \"**Statut Phase 41 (\") {c++} END{print c+0}" "$T/o3"); g=$(printf "%s" "$s" | awk "index(\$0, \"gardée par défaut + tracée\") {c++} END{print c+0}"); x=$(printf "%s" "$s" | awk "{l=tolower(\$0); if (index(l, \"ferm\") || index(l, \"clos\") || index(l, \"résolu\")) c++} END{print c+0}"); r=$(printf "%s" "$s" | awk "index(\$0, \"41-PREUVES.md\") && index(\$0, \"ADR-072\") {c++} END{print c+0}"); git show origin/main:"$F" > "$T/src"; awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$T/src" > "$T/o3a"; orig=$(awk "END{print NR}" "$T/o3a"); now=$(awk "END{print NR}" "$T/o3"); awk "!index(\$0, \"**Statut Phase 41 (\")" "$T/o3" > "$T/o3b"; pre=$(cmp -s "$T/o3b" "$T/o3a" && echo oui || echo non); echo "O-3 : lignes_statut=$n garde_trace=$g vocabulaire_d_achevement=$x renvois=$r lignes_origine=$orig lignes_maintenant=$now texte_origine_intact=$pre"; [ "$n" -eq 1 ] && [ "$g" -eq 1 ] && [ "$x" -eq 0 ] && [ "$r" -eq 1 ] && [ "$now" -eq $((orig + 1)) ] && [ "$pre" = oui ]'</automated>
    <fails_when>exit non nul ; ou `lignes_statut=` différent de 1 ; ou statut sans « gardée par défaut + tracée » ; ou `vocabulaire_d_achevement=` différent de 0 (D-02) ; ou renvois manquants ; ou texte d'origine d'O-3 modifié (`texte_origine_intact=non`, ou plus d'une ligne ajoutée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=.planning/BACKLOG.md; T=$(mktemp -d); awk "/^## Posture de protection de .main. — TRANCHÉ/{f=1; print; next} /^## /{f=0} f" "$F" > "$T/now"; git show origin/main:"$F" > "$T/src"; awk "/^## Posture de protection de .main. — TRANCHÉ/{f=1; print; next} /^## /{f=0} f" "$T/src" > "$T/orig"; st=$(awk "/^\\*\\*Statut \\(.*, Phase 41\\) : TRAITÉ par renvoi\\*\\*/ && index(\$0, \"ADR-072\") && index(\$0, \"41-PREUVES.md\") {c++} END{print c+0}" "$T/now"); awk "!/^\\*\\*Statut \\(.*, Phase 41\\) : TRAITÉ par renvoi\\*\\*/" "$T/now" > "$T/n1"; awk "NF || p {print} {p=NF}" "$T/n1" > "$T/n2"; awk "NF || p {print} {p=NF}" "$T/orig" > "$T/o2"; ol=$(awk "END{print NR}" "$T/o2"); intact=$(cmp -s "$T/n2" "$T/o2" && [ "$ol" -gt 5 ] && echo oui || echo non); echo "BACKLOG : ligne_statut=$st lignes_existantes_intactes=$intact"; [ "$st" -eq 1 ] && [ "$intact" = oui ]'</automated>
    <fails_when>exit non nul ; ou `ligne_statut=` différent de 1 (absente, dupliquée, sans renvoi ADR-072 ou preuves) ; ou `lignes_existantes_intactes=non` (titre ou texte de l'item modifié)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; T=$(mktemp -d); awk "/^## ADR-072 : /{f=1} f" docs/ADR.md > "$T/a72"; awk "/^### Mesures à la pose \\(Phase 41, /{f=1} f" "$T/a72" > "$T/m"; h=$(awk "END{print NR}" "$T/m"); k=0; chk() { [ -n "$3" ] || { echo "VALEUR VIDE DANS 41-PREUVES : $1"; k=$((k+1)); return 0; }; awk -v a="$1" -v b="$2$3" "index(\$0, a) && index(\$0, b) {f=1} END{exit !f}" "$T/m" || { echo "ABSENT : $1 $2$3"; k=$((k+1)); }; }; chk "**M-1** : " "" "$(awk "\$1==\"M1-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md")"; chk "**M-2** : " "" "$(val M2: admin_couvert)"; chk "**M-3** : " "" "$(val M3: resultat)"; chk "**Trace (ii)** : " "pull_request=" "$(val TRACE-II: pull_request)"; chk "**Trace (ii)** : " "required_status_checks=" "$(val TRACE-II: required_status_checks)"; chk "**Push direct** : " "refuse=" "$(val PUSH-DIRECT: refuse)"; chk "**M-4** : " "" "$(awk "\$1==\"M4-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md")"; chk "**Accès aux rule suites** : " "samuel-neveugall=" "$(val ACCES-RULE-SUITES: samuel-neveugall)"; chk "non encore verts" "" "non encore verts"; echo "ADR-072 mesures : lignes_section=$h valeurs_absentes=$k"; [ "$h" -gt 0 ] && [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou sous-section absente ; ou une ligne `VALEUR VIDE DANS 41-PREUVES :` (mesure jamais consignée : aucun report possible) ; ou une ligne `ABSENT :` (valeur de mesure non reportée, ou reportée différente de `41-PREUVES.md`, ou précision « non encore verts » manquante)</fails_when>
  </verify>
  <acceptance_criteria>
    - B contient `origin/main` ; précondition de mesures tenue
    - une ligne de statut O-3 « gardée par défaut + tracée », sans vocabulaire d'achèvement, texte d'origine intact
    - une ligne « TRAITÉ par renvoi » sous l'item BACKLOG, lignes existantes intactes
    - ADR-072 § Mesures à la pose recopiant chaque valeur consignée
  </acceptance_criteria>
  <done>Le statut d'O-3, le BACKLOG et la doctrine disent ce que la mesure a établi, et rien de plus.</done>
</task>

<task type="auto">
  <name>Task 2 : bump patch dérivé, CHANGELOG et historiques README, contrôle du périmètre de la PR, rejeu, SUMMARY avant publication</name>
  <files>VERSION, plugin/.claude-plugin/plugin.json, plugin/.codex-plugin/plugin.json, .claude-plugin/marketplace.json, README.md, README.fr.md, CHANGELOG.md</files>
  <read_first>
    - scripts/bump.sh (en-tête et squelette CHANGELOG)
    - CHANGELOG.md (dernière entrée `## [v…]`, pour le ton et la structure)
    - README.md et README.fr.md § Versioning
  </read_first>
  <action>
Dériver la version : `V` = contenu de `VERSION` sur `origin/main` sans préfixe ; `N` = `X.Y.(Z+1)`.
Lancer `bash scripts/bump.sh "$N"`. Remplacer le commentaire de squelette inséré par `bump.sh` dans
la nouvelle entrée `## [vN]` par un résumé en français : rulesets de branche et de tags posés et
prouvés (refus par défaut d'une PR rouge, trace d'un contournement, refus d'un push direct, M-1 à
M-4), CODEOWNERS étroit, ADR-072, amendement d'ADR-059, statut d'O-3 « gardée par défaut + tracée »,
aucun gate créé (QUAL-01, D-M11), commits couverts depuis le tag précédent (liste
`git log --oneline v<V>..HEAD`), numéros de PR (phase, PR-P). Ajouter une première ligne de données à
la table d'historique de `README.md` (anglais) et de `README.fr.md` (français) pour `vN`, date
d'exécution, résumé court en gras. Commit local sur B.

Contrôler le périmètre de la future PR (`git diff --name-only origin/main...HEAD`) : aucun chemin
couvert par `.github/CODEOWNERS`, aucun `plugin/dev-orchestrator/`, ni `ci.yml`, ni
`scripts/check-release-tag.sh`. Rejouer `gates` et `tests`. Rédiger et committer
`41-10-SUMMARY.md` sur B À LA FIN de cette tâche : il décrit les Tasks 1-2 et annonce la Task 3
(publication et merge humains), dont le résultat sera relu et consigné par 41-11.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'TV=$(mktemp -d); git show origin/main:VERSION > "$TV/v"; V=$(tr -d "v[:space:]" < "$TV/v"); case "$V" in *.*.*) ;; *) echo "VERSION origin/main illisible"; exit 1;; esac; N="${V%.*}.$(( ${V##*.} + 1 ))"; B=$(tr -d "v[:space:]" < VERSION); rc=0; bash scripts/check-version-sync.sh > /dev/null 2>&1 || rc=$?; e=$(awk -v t="## [v$N]" "index(\$0, t)==1 {c++} END{print c+0}" CHANGELOG.md); T=$(mktemp -d); awk -v t="## [v$N]" "index(\$0, t)==1 {f=1; next} /^## \\[v/ {f=0} f" CHANGELOG.md > "$T/e"; sq=$(awk "/<!--/ {c++} END{print c+0}" "$T/e"); tx=$(awk "NF {c++} END{print c+0}" "$T/e"); r1=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.md); r2=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.fr.md); f1=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); f2=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.fr.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); echo "bump : origin_main=$V attendu=$N branche=$B version_sync_rc=$rc entree_changelog=$e commentaire_squelette_restant=$sq lignes_non_vides_entree=$tx readme_en=$r1 readme_fr=$r2 premiere_ligne_en=$f1 premiere_ligne_fr=$f2"; [ "$B" = "$N" ] && [ "$rc" -eq 0 ] && [ "$e" -eq 1 ] && [ "$sq" -eq 0 ] && [ "$tx" -ge 3 ] && [ "$r1" -eq 1 ] && [ "$r2" -eq 1 ] && [ "$f1" -eq 1 ] && [ "$f2" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `branche=` différent du patch suivant de la `VERSION` d'`origin/main` (numéro écrit en dur, ou `main` a bougé : refaire la dérivation après merge d'`origin/main`) ; ou `version_sync_rc=` différent de 0 ; ou entrée CHANGELOG absente ou dupliquée ; ou commentaire de squelette restant ; ou entrée vide ; ou ligne d'historique absente, dupliquée ou pas en première position dans un des deux README</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; if git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p"; then echo "CHEMIN CODEOWNERS $p"; co=$((co+1)); fi; done < "$T/f"; z=$(awk "index(\$0, \"plugin/dev-orchestrator/\")==1 || \$0==\".github/workflows/ci.yml\" || \$0==\"scripts/check-release-tag.sh\" || index(\$0, \"scripts/hooks/\")==1 {c++} END{print c+0}" "$T/f"); n=$(awk "END{print NR}" "$T/f"); echo "perimetre PR des preuves : fichiers=$n chemins_codeowners=$co chemins_interdits=$z"; [ "$n" -gt 0 ] && [ "$co" -eq 0 ] && [ "$z" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou un `CHEMIN CODEOWNERS` (la PR exigerait la revue `@picmakpro` : le rejeu « sans contournement » deviendrait dépendant d'une approbation) ; ou `chemins_interdits=` différent de 0 (zone interdite, CI, gate de release ou hooks touchés — D-M5, D-M9)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-10-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); echo "avant publication : summary_committe=$s modifications_non_committees=$d"; [ "$s" = oui ] && [ "$d" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY non committé sur B ; ou modifications suivies non committées (elles n'entreraient pas dans la PR)</fails_when>
  </verify>
  <acceptance_criteria>
    - `VERSION` = patch suivant de la `VERSION` d'`origin/main`, synchronisée partout (`check-version-sync` rc 0)
    - entrée CHANGELOG rédigée (sans squelette), ligne d'historique en tête des deux README
    - PR des preuves hors CODEOWNERS, hors zone interdite, hors CI, gate de release et hooks
    - rejeux verts ; SUMMARY 41-10 committé ; arbre propre
  </acceptance_criteria>
  <done>La branche B est prête à être publiée et mergée par le chemin normal.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 : gestes humains `samuel-neveugall` — publier B, ouvrir la PR des preuves, attendre la CI, merger SANS contournement</name>
  <action>Samuel, compte `samuel-neveugall`, publie B, ouvre la PR, attend la CI, vérifie l'état `CLEAN` et merge sans aucune option de contournement.</action>
  <instructions>
Compte qui agit : `samuel-neveugall`.

1. `git push -u origin feat/phase-41-preuves-release`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head feat/phase-41-preuves-release --title "Phase 41 — preuves de la protection du dépôt et release v<N>" --body "Preuves (41-PREUVES.md), statut O-3 gardée par défaut + tracée, ADR-072 § Mesures, BACKLOG traité par renvoi, bump patch. Rejeu du flux de release sous la règle, sans contournement."`
   → noter `<n>` (`<N>` = version imprimée par la Task 2).
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tout vert attendu).
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus,reviewDecision --jq '{mergeStateStatus, reviewDecision}'`
   - `CLEAN` : passer à 5.
   - `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`, puis refaire 3 et 4.
   - `BLOCKED` ou autre : NE PAS merger, NE PAS contourner ; copier la sortie et s'arrêter.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) → copier la sortie.
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
Ne pas poser le tag ici : c'est le plan 41-11.
  </instructions>
  <verification>Le plan 41-11 relit l'état `MERGED`, vérifie dans les rule suites que le merge n'a laissé aucune suite `bypass`, et consigne `PR-RELEASE:`.</verification>
  <resume-signal>Coller `<n>`, les sorties de 3 (fin), 4 (toutes les lectures), 5 et son témoin, puis écrire « PR des preuves mergée sans contournement ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR des preuves → `main` | premier merge normal sous la règle : doit passer sans contournement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-38 | Repudiation | O-3 déclarée achevée | medium | mitigate | ligne de statut unique, vocabulaire d'achèvement absent (vérif `vocabulaire_d_achevement=0`), texte d'origine intact |
| T-41-39 | Tampering | numéro de release figé ou périmé | medium | mitigate | dérivé d'`origin/main` après merge local ; comparé au patch suivant ; `check-version-sync` rc 0 |
| T-41-40 | Repudiation | rejeu « vert » obtenu par contournement | high | mitigate | périmètre hors CODEOWNERS vérifié ; état `CLEAN` exigé ; `--admin` exclu ; absence de suite `bypass` vérifiée en 41-11 |
| T-41-41 | Tampering | doctrine réécrite au-delà de la mesure | medium | mitigate | chaque valeur d'ADR-072 § Mesures recoupée avec `41-PREUVES.md` |
</threat_model>

<verification>
O-3, BACKLOG et ADR-072 à jour sur pièce ; bump dérivé et synchronisé ; PR des preuves mergée par le
chemin normal.
</verification>

<success_criteria>
Première moitié du critère 3 : bump → PR → merge sous la règle, sans contournement.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-10-SUMMARY.md` — committé sur la branche B à la fin de la Task 2, AVANT la publication (Task 3).
</output>

---
phase: 22-hygiene-documentaire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/references/docs-flow.md
  - plugin/dev-orchestrator/references/intent-routing.md
  - plugin/dev-orchestrator/AGENT.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [DOCF-01, DOCF-02, DOCF-03, DOCF-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un agent qui reçoit « mets à jour la doc » discerne les quatre familles documentaires et route sur la bonne, ou pose une question courte si la formulation est creuse (D-10)."
    - "« vérifie que la doc dit encore vrai » tombe sur `gsd-docs-update --verify-only` — auditer sans écrire — et « refais toute la doc » tombe sur `--force`, deux intentions aujourd'hui indiscernables (D-12)."
    - "La génération standard n'est jamais lancée sans confirmation humaine explicite ; `--verify-only` est libre car il n'écrit rien (D-03)."
    - "En mission autonome, la doc périmée est constatée et consignée, jamais réécrite en douce (D-04)."
    - "Un futur agent ne lance pas `gsd-docs-update` sur `vibeflow-os` lui-même : la frontière est écrite noir sur blanc (specifics CONTEXT.md)."
  artifacts:
    - plugin/dev-orchestrator/references/docs-flow.md
    - "bloc T22 dans plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
  key_links:
    - "`AGENT.md` §Références → chemin d'install D7 `.claude/agents/dev-orchestrator-references/docs-flow.md` — sans cette ligne le fichier est installé mais jamais lu."
    - "boucle `for ref in …` de T6 → prouve que l'engine installe réellement `docs-flow.md` (D-01, décision `costly`)."
    - "`intent-routing.md` §Contexte & session → seule source de routage (ADR-057) ; `docs-flow.md` porte la doctrine, jamais une table de routage concurrente."
---

<objective>
Poser la **doctrine documentaire de sortie** du moteur de dev — `docs-flow.md`, symétrique
d'`ingestion-flow.md` — et la **captation d'intention en langage naturel** qui la déclenche.

Purpose : aujourd'hui GSD outille quatre métiers documentaires distincts (produit / entrée / code /
savoir) et VibeFlow les route par **une** ligne d'`intent-routing.md`. Le manque n'est pas d'outils,
c'est de discernement. Les deux flags porteurs de sens de `gsd-docs-update` — `--verify-only`
(auditer sans écrire) et `--force` (régénérer, écrase le manuscrit) — ne sont exposés nulle part.

Output : `references/docs-flow.md` (création), `intent-routing.md` enrichie, `AGENT.md` câblée sur
le chemin d'install D7, et le bloc de garde `T22` qui rend la doctrine non-régressable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-CONTEXT.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-PATTERNS.md
@CLAUDE.md
@plugin/dev-orchestrator/references/ingestion-flow.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 22-01)

**Nouveaux fichiers**
- `plugin/dev-orchestrator/references/docs-flow.md`

**Nouvelles sections de doctrine** (dans `docs-flow.md`, dans cet ordre)
- `## Discernement — quatre familles, quatre métiers`
- `## Famille produit — gsd-docs-update`
- `## Famille code — gsd-map-codebase`
- `## Famille savoir — gsd-extract-learnings, gsd-graphify`
- `## Famille entrée — renvoi, jamais de copie`
- `## Déclencheurs (le FAIT, jamais le jugement)`
- `## Garde-fous`
- `## Interdits`

**Nouvelles sections / lignes ailleurs**
- `AGENT.md` §Références : 6e ligne de chemin d'install D7 vers `docs-flow.md`
- `intent-routing.md` §Voir aussi : ligne `docs-flow.md`
- `intent-routing.md` §Contexte & session : lignes de table enrichies (voir tâche 3)

**Nouvelles assertions de test** (`test-dev-orchestrator.sh`)
- `T22` — doctrine `docs-flow.md` : présence, quatre familles, trois régimes, ligne rouge `--force`,
  frontière `vibeflow-os`, renvois `AGENT.md` + `intent-routing.md`
- `T6` (boucle existante ligne 923) : `docs-flow.md` ajouté à la liste des références installées

**Nouvelles lignes de la carte d'intention**
- `intent-routing.md` §Contexte & session : la ligne unique `mets à jour la doc / génère le README /
  la doc est périmée` devient **quatre** lignes (audit, génération, régénération, cartographie du
  savoir), chacune vers sa famille
</artifacts_produced>

<decisions_honored>
## Décisions verrouillées couvertes par ce plan

| Décision | Où elle est implémentée |
|---|---|
| **D-01** (hébergement `dev-orchestrator/references/`, symétrie `ingestion-flow.md`) | tâche 1 |
| **D-02** (quatre familles, entrée par RENVOI seulement, jamais de duplication) | tâches 1 et 2 |
| **D-03** (gradation par le risque : `--verify-only` libre, génération sous confirmation) | tâches 1 et 2 |
| **D-04** (mission autonome : constater et consigner, jamais écrire) | tâche 2 |
| **D-05** (`--force` autorisé sur intention explicite — tranché CONTRE la recommandation) | tâche 2 |
| **D-06** (garde-fou `--force` en trois temps, jamais en mission ni en autonome) | tâche 2 |
| **D-10** (désambiguïsation par ancrage contextuel, question courte si creux) | tâche 3 |
| **D-12** (formulations réelles ; `--verify-only` et `--force` ont chacune la leur) | tâche 3 |
| **D-14** (extension de la suite existante, jamais une suite nouvelle) | tâches 1, 2, 3 |

**D-05 n'est pas rouvrable.** Samuel a tranché contre la recommandation ; le garde-fou D-06 est la
contrepartie négociée. Aucune tâche ne doit reproposer d'interdire `--force` aux agents.
</decisions_honored>

<tasks>

<task type="tracer">
  <name>Tâche 1 : chaîne complète de bout en bout — la doctrine existe, elle est référencée, elle s'installe, elle est gardée</name>

  <precondition>
    Le travail de la Phase 20 est présent dans l'arbre de travail : `plugin/dev-orchestrator/references/mission-flow.md`
    contient `Pattern E` ET `plugin/dev-orchestrator/agents/vf-dev-manager.md` contient `Étage design croisé`.
    Si l'un des deux manque, la Phase 20 n'est pas mergée — HALT, ne pas exécuter (ROADMAP : « merge requis
    avant exécution », périmètre à fichiers partagés).
  </precondition>

  <reversibility rating="costly">
    D-01 place `docs-flow.md` dans `plugin/dev-orchestrator/references/`. Déplacer le fichier après coup
    casserait les renvois posés dans `AGENT.md`, les deux managers, le test et le chemin d'install D7 des
    labs déjà déployés. Cette tâche prouve ce choix de bout en bout AVANT que quatre autres fichiers en
    dépendent — c'est exactement pourquoi elle est le tracer.
  </reversibility>

  <files>
    plugin/dev-orchestrator/references/docs-flow.md,
    plugin/dev-orchestrator/AGENT.md,
    plugin/dev-orchestrator/references/intent-routing.md,
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  </files>

  <read_first>
    - `plugin/dev-orchestrator/references/ingestion-flow.md` (94 lignes) — **le gabarit à répliquer** :
      titre H1 avec ID d'exigence, encadré de rôle en blockquote unique (quoi / chargement on-demand /
      qui produit le fait outillé), séparateur `---`, sections en `##`, fermeture sur `## Interdits`.
    - `plugin/dev-orchestrator/AGENT.md` lignes 175-181 (§Références, chemin d'install D7 — le format
      exact des 5 lignes existantes à répliquer).
    - `plugin/dev-orchestrator/references/intent-routing.md` lignes 169-174 (§Voir aussi) et 141-167
      (§Couverture — le contrat d'exhaustivité).
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` lignes 85-140 (résolution
      `MOD`/`REPO`/`REFS_DIR`/`AGENT_FILE`/`ROUTING`/`GREP`, helpers `ok()`/`ko()`/`skip()`),
      lignes 906-939 (bloc T6 install e2e et sa boucle `for ref in …` ligne 923),
      lignes 1251-1291 (blocs T16/T17 — **le moule exact** du nouveau bloc).
    - `.planning/phases/VFDO-22-.../22-CONTEXT.md` §decisions (D-01, D-02, D-03, D-14) et
      §canonical_refs.
    - `.planning/phases/VFDO-22-.../22-PATTERNS.md` §`docs-flow.md` (squelette structurel) et
      §`test-dev-orchestrator.sh` (idiomes `tNN_ok`).
  </read_first>

  <action>
Faire passer UNE intention réelle — « vérifie que la doc dit encore vrai » → `gsd-docs-update --verify-only` —
à travers **toutes** les couches, avant d'écrire les trois autres familles. Quatre couches, une seule
famille traitée.

**Couche 1 — la doctrine.** Créer `plugin/dev-orchestrator/references/docs-flow.md` avec l'ossature
**définitive** (les sections manquantes seront remplies en tâche 2, jamais réordonnées) :
titre H1 `# Docs-flow — doctrine de sortie documentaire (DOCF-01 → DOCF-04)` ; encadré de rôle en
blockquote unique répondant aux trois questions du gabarit — source de vérité de la sortie
documentaire, chargée on-demand par `vibeflow-dev` comme `mission-flow.md` et `GSD-PIPELINE.md`
(coût contexte nul le reste du temps), et le fait outillé sous-jacent produit par `check-doc-drift.sh`
que ce fichier ne redéfinit pas mais apprend à interpréter ; séparateur `---` ; puis dans cet ordre
les huit titres `##` listés en `<artifacts_produced>`.

Dans cette tâche, remplir **seulement** : `## Discernement — quatre familles, quatre métiers` (la
table à quatre lignes Famille / Brique / Ce qui est maintenu, reprise du socle factuel de
`22-CONTEXT.md` §decisions — c'est une table de **discernement**, jamais une table de routage,
ADR-057 réserve le routage à `intent-routing.md`) ; `## Famille produit — gsd-docs-update` avec les
6 docs toujours-on nommées (README, ARCHITECTURE, GETTING-STARTED, DEVELOPMENT, TESTING,
CONFIGURATION), les 3 conditionnelles (API si routes, CONTRIBUTING si open source, DEPLOYMENT si
config de déploiement), la review queue des docs manuscrites, la détection de trous, et le fait que
le CHANGELOG n'est **jamais** régénéré ; le régime `--verify-only` de D-03 — read-only, n'écrit aucun
fichier, ne commite rien, donc **libre** : un agent peut le lancer seul pour constater avant de
proposer ; `## Garde-fous` avec le seul garde-fou de cette tâche — la génération standard (sans flag)
exige une confirmation humaine explicite avant l'appel au titre d'ADR-031, parce qu'elle écrit
jusqu'à 9 fichiers et **commite** (`commit_docs: true`), et parce que se fier aux gates internes du
moteur ne tient pas : ils tombent quand le skill est invoqué depuis un sous-agent privé
d'`AskUserQuestion` au runtime (défaut constaté en Phase 20, nœud `checkpoint-doctrine` gelé) ;
`## Interdits` avec l'interdit permanent — aucun verbe-façade `/vf-docs`, supprimé en v2.33.0, aucun
retour arrière ; la sortie documentaire se déclenche par langage naturel détecté et brique invoquée
directement.

Laisser les quatre titres restants (`## Famille code`, `## Famille savoir`, `## Famille entrée`,
`## Déclencheurs`) présents avec une phrase d'amorce d'une ligne chacun — la structure est finale,
le contenu s'étend en tâche 2.

**Couche 2 — la référence agent (chemin d'install D7).** Ajouter à `AGENT.md` §Références une
**6e ligne** au format exact des cinq existantes, libellée « Doctrine de sortie documentaire
(familles, régime de confirmation, déclencheurs) » pointant sur
`.claude/agents/dev-orchestrator-references/docs-flow.md`. Ne rien retirer, ne pas toucher aux
tables d'intention d'`AGENT.md` dans cette tâche (T3 exige ≥ 11 lignes d'intention et ≤ 250 lignes).

**Couche 3 — le renvoi de la carte de routage.** Ajouter à `intent-routing.md` §Voir aussi une ligne
symétrique aux deux existantes : `docs-flow.md` — doctrine des quatre familles documentaires (quoi
maintenir, quand, quel régime de confirmation). Ne pas modifier §Couverture : les briques
documentaires visées sont déjà dans l'index, aucune whitelist nouvelle n'est requise (D-14) ; si
l'exécution révèle qu'une exception est nécessaire, l'écrire dans §Couverture **et** dans le test,
jamais dans le test seul.

**Couche 4 — le gate machine.** Deux éditions dans `test-dev-orchestrator.sh` :
(a) ajouter `docs-flow.md` à la boucle `for ref in …` du bloc T6 (ligne 923) — un seul mot, c'est
la preuve que l'engine installe réellement la référence au chemin D7 ;
(b) ajouter en fin de fichier, **avant** la ligne de résultat, un bloc `T22` écrit sur le moule exact
de T16 : une variable `DOCSFLOW="$REFS_DIR/docs-flow.md"`, un garde `if [ ! -f "$DOCSFLOW" ]` qui
`ko`, sinon un compteur `t22_ok=1`, une série de `"$GREP" -q <motif> "$DOCSFLOW" || { ko "…"; t22_ok=0; }`,
puis `[ "$t22_ok" -eq 1 ] && ok "…"` en clôture — jamais d'`exit` immédiat. Motifs de cette tâche :
`gsd-docs-update`, `--verify-only` (passer `-q --` avant le motif pour que le tiret ne soit pas lu
comme une option), `ADR-031`, `## Interdits` ; plus les deux renvois — `docs-flow` présent dans
`$AGENT_FILE` et `docs-flow` présent dans `$ROUTING`. Le bloc T22 s'étend en tâches 2 et 3, il ne se
duplique pas.

Français partout (doctrine, commentaires de test, messages `ok`/`ko`). Commit en français.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tee /tmp/vfdo22-t1.log | tail -3; grep -q '✓ T22' /tmp/vfdo22-t1.log &amp;&amp; grep -q '✓ T6 install e2e' /tmp/vfdo22-t1.log &amp;&amp; grep -q '0 KO' /tmp/vfdo22-t1.log &amp;&amp; echo TRACER_OK</automated>
  </verify>

  <acceptance_criteria>
    - `plugin/dev-orchestrator/references/docs-flow.md` existe.
    - `grep -c '^## ' plugin/dev-orchestrator/references/docs-flow.md` retourne `8`.
    - La dernière section du fichier est `## Interdits` : `grep -n '^## ' plugin/dev-orchestrator/references/docs-flow.md | tail -1` contient `## Interdits`.
    - `docs-flow.md` contient les chaînes `gsd-docs-update`, `--verify-only`, `ADR-031`, `on-demand`, `CHANGELOG`.
    - `plugin/dev-orchestrator/AGENT.md` contient `dev-orchestrator-references/docs-flow.md`.
    - `plugin/dev-orchestrator/references/intent-routing.md` contient `docs-flow.md`.
    - `grep -n 'for ref in' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` fait apparaître `docs-flow.md` sur la ligne trouvée.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0, sa sortie contient `✓ T22`, contient `✓ T6 install e2e` (donc l'install e2e n'a pas été sautée) et se termine sur `0 KO`.
    - `wc -l < plugin/dev-orchestrator/AGENT.md` ≤ 250.
  </acceptance_criteria>

  <done>
    L'intention « vérifie que la doc dit encore vrai » a un chemin complet et prouvé : elle est
    doctrinée dans `docs-flow.md`, la doctrine est référencée par `AGENT.md` au chemin d'install D7,
    citée par `intent-routing.md`, réellement copiée par l'engine dans un lab temporaire (T6), et
    gardée par T22. La suite du module est verte. Le commit est posé.
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : les trois familles restantes, le régime `--force` et sa ligne rouge, la frontière `vibeflow-os`</name>

  <files>
    plugin/dev-orchestrator/references/docs-flow.md,
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  </files>

  <read_first>
    - `plugin/dev-orchestrator/references/docs-flow.md` (état laissé par la tâche 1 — les quatre
      sections d'amorce à remplir).
    - `.planning/phases/VFDO-22-.../22-CONTEXT.md` §decisions D-02, D-04, D-05, D-06, D-08, D-13 et
      §specifics (la frontière `vibeflow-os`, relevée pendant le cadrage — elle découle du fait, pas
      d'une préférence).
    - `plugin/dev-orchestrator/references/ingestion-flow.md` §Garde-fous BRDG-03 (le ton et la
      granularité d'un bloc de garde-fous numérotés).
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` en-tête — contrat d'exit (0 = seuil
      atteint, 3 = silence, 64 = argument invalide) : à citer, jamais à modifier (D-13).
  </read_first>

  <action>
Remplir les quatre sections laissées en amorce et durcir les garde-fous.

`## Famille code — gsd-map-codebase` : ce que la brique maintient — `.planning/codebase/`, sept
documents produits par quatre mappeurs parallèles (STACK, INTEGRATIONS, ARCHITECTURE, STRUCTURE,
CONVENTIONS, TESTING, CONCERNS) — et ses modes `--fast` (avec `--focus`) et `--query` (`term`,
`status`, `diff`, `refresh`). Quand la rafraîchir plutôt que la régénérer.

`## Famille savoir — gsd-extract-learnings, gsd-graphify` : le LEARNINGS.md d'étape (décisions,
leçons, patterns, surprises) dérivé de PLAN / SUMMARY / VERIFICATION / UAT / STATE, et le graphe de
connaissance. Famille déclenchée après une vérification ou une clôture d'étape, jamais pendant.

`## Famille entrée — renvoi, jamais de copie` : **un paragraphe de prose**, pas une table de
routage. Il dit que la famille entrée (specs / ADR / PRD → `.planning/`) est déjà doctrinée, que
`ingestion-flow.md` en est la **source unique** — découverte outillée, construction du manifest,
garde-fous BRDG-03, gate BLOCKER, précédence `ADR > SPEC > PRD > DOC`, cap 50 — et que rien de tout
cela n'est redit ici (D-02, ADR-057). La valeur de ce fichier est le discernement entre familles,
pas la réécriture de ce qui existe.

`## Déclencheurs (le FAIT, jamais le jugement)` : la **table canonique** des quatre déclencheurs de
D-08, deux colonnes Déclencheur / Constat, cellules courtes, jamais de prose — (1) surface publique
touchée : le diff modifie une API, une CLI, une config ou un schéma, précisément ce que décrivent
README / API.md / CONFIGURATION.md ; (2) `[doc-drift]` actif : `check-doc-drift.sh` sort en exit 0
au démarrage de session, exit 3 valant silence — fait déjà produit par un script existant, coût nul ;
(3) fin de milestone : la clôture enchaîne déjà `gsd-audit-milestone` puis `gsd-complete-milestone`
puis `gsd-cleanup`, le geste documentaire s'y insère ; (4) nouveau module ou nouvelle capacité : un
répertoire de module ou un point d'entrée apparaît, il n'a par construction aucune doc. Fermer la
section sur le fait qu'aucun déclencheur qui ne tombe est un **état normal**, pas un manque, et sur
la règle ADR-055 §3 — le script CONSTATE, l'agent JUGE ; `check-doc-drift.sh` n'est pas enrichi pour
nommer les docs périmées, ce serait du jugement déguisé en fait. **Cette table est l'unique source
des déclencheurs** : les deux managers y renverront (plan 22-02) au lieu de la recopier.

`## Garde-fous` — compléter le garde-fou de confirmation déjà posé par trois autres :
- **mission autonome** (D-04) : `vf-auto`, « la nuit », absence de l'utilisateur → constater et
  consigner, jamais écrire. Le manager lance l'audit read-only, porte le constat au rapport de
  mission, et propose la génération en next step. La doc périmée est **tracée**, jamais corrigée en
  douce pendant que personne ne regarde.
- **`--force` est autorisé sur intention explicite de l'utilisateur** (D-05) — décision tranchée,
  non rouvrable. Il régénère tout et écrase les docs manuscrites sans prompt de préservation.
- **garde-fou de `--force` en trois temps** (D-06), non négociable : l'agent reformule d'abord ce
  qui sera écrasé — nombre **et** liste des docs manuscrites concernées, dérivés du champ
  `existing_docs` de l'init JSON du moteur (paires `path` / `has_gsd_marker` : celles sans marqueur
  GSD sont le travail humain à risque) ; il attend ensuite un oui explicite ; et il ne l'emploie
  jamais hors conversation directe. Écrire ce dernier point sur **une seule ligne physique**
  contenant les trois notions ensemble : le flag `--force`, l'interdiction en mission d'équipe, et
  l'interdiction en mode autonome — le déclencheur vient de l'utilisateur, en direct, jamais d'un
  manager. Le test T22 vérifie ces trois notions sur cette ligne unique.

`## Interdits` — ajouter deux interdits à celui déjà posé :
- **Ne jamais lancer `gsd-docs-update` sur `vibeflow-os` lui-même.** Ce dépôt maintient un
  `CHANGELOG.md`, un `README.md` et un `VERSION` **par module**, sous gates machine
  (`check-version-sync.sh`, triades par module) ; le moteur ne régénère jamais de CHANGELOG et ne
  connaît que les neuf types canoniques à la racine. Écrire cet interdit sur **une seule ligne
  physique** portant à la fois `gsd-docs-update`, `vibeflow-os` et `check-version-sync.sh`.
- Aucune réimplémentation des moteurs délégués : classification de projet, construction de la file
  de docs, contrôle de préservation, vérification, boucle de correction (deux itérations maximum,
  arrêt sur régression) et balayage de secrets appartiennent aux workflows amont, point. La fuite de
  secrets en particulier est **traitée en amont** — ne pas la doubler ici.

**Étendre le bloc T22** (jamais un bloc nouveau, D-14) avec : `gsd-map-codebase`, `gsd-extract-learnings`,
`ingestion-flow` (le renvoi entrée), `check-doc-drift.sh` ; la ligne rouge, vérifiée par **chaînage
de trois greps sur le même flux** — filtrer les lignes portant le flag de régénération, puis y
chercher la mention de mission d'équipe, puis y chercher la mention d'autonome : la chaîne n'est
vraie que si une ligne physique unique porte les trois ; la frontière, par le même chaînage sur
`gsd-docs-update` puis `vibeflow-os` puis `check-version-sync.sh`.

**Vérifier que `check-doc-drift.sh` n'a pas bougé** (D-13) : `git diff --quiet HEAD -- plugin/dev-orchestrator/scripts/check-doc-drift.sh`.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -3 &amp;&amp; git diff --quiet HEAD -- plugin/dev-orchestrator/scripts/check-doc-drift.sh &amp;&amp; echo DRIFT_INTACT</automated>
  </verify>

  <acceptance_criteria>
    - `docs-flow.md` contient les chaînes `gsd-map-codebase`, `gsd-extract-learnings`, `gsd-graphify`, `ingestion-flow.md`, `check-doc-drift.sh`, `existing_docs`, `ADR-055`, `ADR-057`.
    - Ligne rouge sur une ligne unique : `grep -F -- '--force' plugin/dev-orchestrator/references/docs-flow.md | grep -F 'mission' | grep -q 'autonome'` sort en exit 0.
    - Frontière sur une ligne unique : `grep -F 'gsd-docs-update' plugin/dev-orchestrator/references/docs-flow.md | grep -F 'vibeflow-os' | grep -q 'check-version-sync.sh'` sort en exit 0.
    - Les quatre déclencheurs de D-08 sont dans une table : `grep -c '^| ' plugin/dev-orchestrator/references/docs-flow.md` ≥ 8 (table de discernement + table de déclencheurs, séparateurs exclus).
    - `grep -c '^## ' plugin/dev-orchestrator/references/docs-flow.md` retourne toujours `8` (aucune section ajoutée ni retirée depuis la tâche 1).
    - `git diff --quiet HEAD -- plugin/dev-orchestrator/scripts/check-doc-drift.sh` sort en exit 0 (D-13 : le script reste inchangé).
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0 et sa sortie contient `✓ T22`.
    - Aucun bloc de test nouveau n'est créé : `ls plugin/dev-orchestrator/scripts/tests/` retourne exactement les mêmes fichiers qu'avant la tâche.
  </acceptance_criteria>

  <done>
    `docs-flow.md` traite en propre les familles produit, code et savoir, renvoie vers
    `ingestion-flow.md` pour l'entrée sans la dupliquer, porte la table canonique des quatre
    déclencheurs, les quatre garde-fous dont la ligne rouge `--force` sur une ligne unique, et la
    frontière `vibeflow-os` sur une ligne unique. `check-doc-drift.sh` est bit-à-bit inchangé.
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : captation d'intention — les formulations réelles et la désambiguïsation</name>

  <files>
    plugin/dev-orchestrator/references/intent-routing.md,
    plugin/dev-orchestrator/AGENT.md,
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  </files>

  <read_first>
    - `plugin/dev-orchestrator/references/intent-routing.md` lignes 93-102 (§Contexte & session — la
      ligne 101 est celle à éclater) et lignes 141-167 (§Couverture — le contrat d'exhaustivité que
      T14 vérifie).
    - `plugin/dev-orchestrator/AGENT.md` lignes 97-124 (§Next steps & hygiène documentaire, et la
      table §Signaux de démarrage avec sa ligne `[doc-drift]`).
    - `plugin/dev-orchestrator/references/docs-flow.md` (état laissé par la tâche 2) — les
      formulations doivent tomber sur les familles telles qu'elles y sont nommées.
    - `.planning/phases/VFDO-22-.../22-CONTEXT.md` §decisions D-10 (table d'ancrage contextuel) et
      D-12 (les formulations nommément visées).
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` lignes 1120-1181 (T14,
      exhaustivité — le plancher `routed_count ≥ 30` et `brick_routed()` à frontière de mot) et
      lignes 760-775 (T3, `intent_lines ≥ 11` et `AGENT.md ≤ 250`).
  </read_first>

  <action>
Éclater la ligne unique de captation en quatre lignes qui distinguent les intentions réellement
distinctes, et poser le protocole de désambiguïsation.

**`intent-routing.md` §Contexte & session.** Remplacer la ligne unique
`mets à jour la doc / génère le README / la doc est périmée | gsd-docs-update` par **quatre** lignes
de table au format maison (formulations séparées par `/` en colonne gauche, brique en colonne
droite, une seule ligne physique par entrée) :
- une ligne d'**audit** portant explicitement le flag `--verify-only` en colonne droite, et en
  colonne gauche les formulations qui ne demandent pas d'écriture : vérifie que la doc dit encore
  vrai · la doc est-elle à jour · la doc correspond plus au code · audite la doc sans rien changer ;
- une ligne de **génération standard** (`gsd-docs-update`, confirmation requise) : mets à jour la
  doc · génère le README · la doc est périmée · la doc est fausse · il manque la doc d'API ·
  documente ce module ;
- une ligne de **régénération** portant explicitement le flag `--force` en colonne droite, avec
  mention du garde-fou : refais toute la doc · repars de zéro sur la doc · on a changé l'archi,
  reprends tout ;
- laisser intactes les lignes existantes `comprends ce code / cartographie…` (`gsd-map-codebase`)
  et `qu'est-ce qu'on a appris…` (`gsd-extract-learnings`, `gsd-graphify`) — elles portent déjà les
  familles code et savoir ; y ajouter les formulations manquantes de D-12 si elles n'y sont pas.

Chaque ligne enrichie cite `docs-flow.md` en doctrine, sur le modèle de la ligne d'ingestion
existante qui cite `ingestion-flow.md` entre parenthèses.

**Protocole de désambiguïsation (D-10).** Ajouter juste sous la table de §Contexte & session un
bloc court — un blockquote ou une table à deux colonnes — qui dit que « mets à jour la doc » vise
quatre familles et que le routage se fait **au jugement du contexte**, une question courte
seulement si la formulation est vraiment creuse (c'est l'heuristique 5 déjà en vigueur, pas une
règle nouvelle). Les cinq ancrages : une étape vient d'être exécutée et du code a bougé → produit ;
le repo est inconnu ou `.planning/codebase/` est absent ou daté → code ; une étape vient d'être
vérifiée ou clôturée → savoir ; un document de cadrage traîne hors de la feuille de route → entrée,
renvoi `ingestion-flow.md` ; rien de tout cela, formulation creuse en début de session → **une
question courte, jamais une devinette**.

**Ne pas toucher §Couverture** : les briques concernées (`gsd-docs-update`, `gsd-map-codebase`,
`gsd-extract-learnings`, `gsd-graphify`) sont déjà routées et déjà dans l'index — aucune whitelist
nouvelle, aucune exception nouvelle (D-14). Les flags sont des **variantes d'appel** d'une brique
existante, pas des briques : T14 compte des noms `gsd-*`, il reste vert par construction.

**`AGENT.md` §Next steps & hygiène documentaire (rôle actif).** Graduer la puce existante « drift
doc détecté → proposer `gsd-docs-update` » sans la remplacer par une table (densité : l'agent est à
181 lignes, la table de déclencheurs appartient aux managers, pas à l'agent conversationnel) :
nommer les quatre familles en une phrase, dire que l'audit read-only est le geste par défaut quand
on veut seulement savoir, et renvoyer à `docs-flow.md` pour le régime de confirmation. Dans la
table §Signaux de démarrage, la ligne `[doc-drift]` existe déjà — la garder et coupler sa colonne
« Geste proposé » au renvoi `docs-flow.md`, sans créer de ligne nouvelle.

**Étendre le bloc T22** (toujours pas de bloc nouveau) avec le câblage de la captation : `$ROUTING`
contient `--verify-only`, contient `--force`, et contient `docs-flow` ; `$AGENT_FILE` contient
`docs-flow`. Ajouter une garde de non-régression explicite sur le plancher de T14 en réutilisant la
valeur déjà calculée par le bloc T14 plutôt qu'en la recalculant.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tee /tmp/vfdo22-t3.log | tail -3; grep -q '✓ T14 exhaustivité' /tmp/vfdo22-t3.log &amp;&amp; grep -q '✓ T22' /tmp/vfdo22-t3.log &amp;&amp; grep -q '✓ T3 agent' /tmp/vfdo22-t3.log &amp;&amp; grep -q '0 KO' /tmp/vfdo22-t3.log &amp;&amp; echo CAPTATION_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c -- '--verify-only' plugin/dev-orchestrator/references/intent-routing.md` ≥ 1.
    - `grep -c -- '--force' plugin/dev-orchestrator/references/intent-routing.md` ≥ 1.
    - La section §Contexte & session porte au moins 8 lignes de table : `awk '/^## Contexte & session/,/^## Design/' plugin/dev-orchestrator/references/intent-routing.md | grep -c '^| '` ≥ 8.
    - `intent-routing.md` contient les chaînes `dit encore vrai`, `refais toute la doc`, `docs-flow.md`.
    - `plugin/dev-orchestrator/AGENT.md` contient `docs-flow` et contient toujours `[doc-drift]`.
    - `wc -l < plugin/dev-orchestrator/AGENT.md` ≤ 250.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0 ; sa sortie contient `✓ T14 exhaustivité`, `✓ T14 plancher`, `✓ T3 agent` et `✓ T22`, et se termine sur `0 KO`.
    - Le compteur de suites du dépôt est inchangé : `find plugin scripts -path '*/tests/test-*.sh' | wc -l` retourne `44`.
  </acceptance_criteria>

  <done>
    « vérifie que la doc dit encore vrai » et « refais toute la doc » tombent sur deux gestes
    distincts et nommés dans la carte d'intention ; les quatre familles ont chacune leurs
    formulations réelles ; le protocole de désambiguïsation est écrit ; `AGENT.md` renvoie à la
    doctrine sans dupliquer la table des déclencheurs ; T14 et T3 restent verts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utilisateur → agent LLM | une formulation en langage naturel décide quel geste documentaire est déclenché ; une formulation ambiguë mal routée déclenche une écriture non voulue |
| agent LLM → shell / système de fichiers | le geste documentaire délègue à `gsd-docs-update`, qui écrit jusqu'à 9 fichiers **et commite** (`commit_docs: true`) |
| dépôt `vibeflow-os` ↔ moteur amont | le moteur ne connaît que les 9 types canoniques à la racine ; ce dépôt maintient une triade par module sous gates machine — deux modèles de doc incompatibles |

> Surface honnête : cette phase ne livre **aucun code exécutable nouveau**. Elle livre de la
> doctrine markdown, des éditions d'agents et des assertions `grep` dans une suite bash existante.
> Le risque réel n'est pas l'exécution de la doctrine, c'est **l'écriture qu'elle autorise** — d'où
> une surface étroite mais non nulle, centrée sur `--force` et sur la frontière du dépôt.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | `--force` de `gsd-docs-update` (doctrine `docs-flow.md` §Garde-fous) | high | mitigate | Garde-fou D-06 en trois temps écrit dans `docs-flow.md` : reformulation nominative des docs manuscrites écrasées depuis `existing_docs` (`has_gsd_marker` faux = travail humain), attente d'un oui explicite, et interdiction en mission d'équipe **et** en mode autonome sur une ligne physique unique. Assertion T22 par chaînage de trois `grep` sur le même flux — la chaîne n'est vraie que si une seule ligne porte les trois notions (tâche 2). |
| T-22-02 | Tampering | `gsd-docs-update` lancé sur `vibeflow-os` lui-même | medium | mitigate | Interdit explicite en `## Interdits` sur une ligne physique unique portant `gsd-docs-update`, `vibeflow-os` et `check-version-sync.sh`, assertion T22 par chaînage de `grep` (tâche 2). Le moteur écraserait les README de modules gatés par la triade. |
| T-22-03 | Repudiation | mission autonome (`vf-auto`, absence de l'utilisateur) | medium | mitigate | D-04 écrit en `## Garde-fous` : en autonome, l'agent lance l'audit read-only, porte le constat au rapport de mission, ne génère jamais. La doc périmée est tracée, jamais corrigée sans témoin (tâche 2). |
| T-22-04 | Information Disclosure | régénération de doc depuis le code → fuite de secrets dans la doc générée | low | transfer | Le balayage de secrets est un step du workflow amont `docs-update.md`, jamais réimplémenté ici (ADR-057, une capacité une seule voix). `## Interdits` nomme explicitement cette délégation (tâche 2). |
| T-22-05 | Spoofing | formulation creuse routée par devinette sur la mauvaise famille | low | mitigate | D-10 : protocole de désambiguïsation par ancrage contextuel, et **question courte** — jamais une devinette — quand aucun ancrage ne tombe (tâche 3). |
| T-22-SC | Tampering | chaîne d'approvisionnement (npm / pip / cargo) | low | accept | Fait constaté, pas une préférence : **cette phase n'installe aucun paquet**. `files_modified` ne contient que des `.md` et un `.sh` déjà versionné ; aucune tâche n'invoque de gestionnaire de paquets. Le protocole d'audit de légitimité ne se déclenche pas — il n'a aucun paquet à auditer. |

**Contributions `plan:pre` — détecteurs exécutés, résultats consignés**

| Contribution | Détecteur | Résultat | Conduite |
|---|---|---|---|
| ai-integration | `api-coverage.cjs --json` sur la section ROADMAP Phase 22 | `{"detected":false,"signals":[]}` | Aucune intégration d'API externe. Pas de matrice de couverture ; à la clôture, `COVERAGE.md` doit porter la déclaration raisonnée « No external API integration: doctrine documentaire markdown + assertions bash, aucun SDK ni service externe ». |
| assumption-delta | `query assumption-delta scan 22 --json` | `{"detected":false,"signals":[]}` | Aucune transition singulier→pluriel / requis→optionnel / dérivé→choisi. Checkpoint non déclenché, non remonté à l'utilisateur. |
| schema-gate | scan des motifs ORM sur le périmètre de phase | aucun ORM dans ce dépôt (plugin markdown + bash) | Non déclenché, saut silencieux. |
| security | `security_asvs_level: 1`, `security_block_on: high` | 1 menace `high` (T-22-01), disposition `mitigate` | Registre ci-dessus. Aucune menace `high` laissée en `accept`. |
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0, avec au
  minimum les blocs `T3`, `T6 install e2e`, `T14 exhaustivité`, `T14 plancher`, `T14b`, `T14c`,
  `T16`, `T17` et `T22` au vert, et `0 KO`.
- `bash plugin/conductor/scripts/check-agents.sh --strict plugin/dev-orchestrator/agents` sort en
  exit 0 (aucun frontmatter d'agent n'est modifié par ce plan, la garde est une non-régression).
- `find plugin scripts -path '*/tests/test-*.sh' | wc -l` retourne `44` — aucune suite nouvelle
  (D-14 ; une suite nouvelle rendrait `check-version-sync.sh` rouge sur le compteur des README).
- `git diff --quiet HEAD~3 -- plugin/dev-orchestrator/scripts/check-doc-drift.sh` sort en exit 0
  (D-13).
</verification>

<success_criteria>
1. `plugin/dev-orchestrator/references/docs-flow.md` existe, porte exactement 8 sections `##`,
   ferme sur `## Interdits`, et traite en propre les familles produit / code / savoir tout en
   renvoyant vers `ingestion-flow.md` pour l'entrée.
2. `--verify-only` et `--force` ont chacun leur formulation déclencheuse dans `intent-routing.md`
   et leur régime dans `docs-flow.md` — auditer et régénérer ne sont plus indiscernables.
3. La ligne rouge de `--force` (jamais en mission d'équipe, jamais en autonome) tient sur une ligne
   physique unique, prouvée par chaînage de `grep`.
4. La frontière « ne jamais lancer `gsd-docs-update` sur `vibeflow-os` » tient sur une ligne
   physique unique, prouvée par chaînage de `grep`.
5. `AGENT.md` renvoie à `docs-flow.md` au chemin d'install D7 et reste ≤ 250 lignes.
6. L'engine installe réellement `docs-flow.md` (prouvé par la boucle du bloc T6, pas par lecture).
7. La suite du module est verte, `check-doc-drift.sh` est bit-à-bit inchangé, et le dépôt compte
   toujours 44 suites.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-01-SUMMARY.md` en fin de plan.
</output>

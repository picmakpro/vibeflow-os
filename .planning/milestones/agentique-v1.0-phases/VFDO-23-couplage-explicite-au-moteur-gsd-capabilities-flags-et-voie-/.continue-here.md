---
context: phase
phase: VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-
task: 1
total_tasks: 8
status: in_progress
last_updated: 2026-08-01T10:13:26.720Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Worktree obligatoire** — cette phase vit dans
  `../vibeflow-os-p23` (branche `feat/phase-23-couplage-gsd`).
  L'arbre principal `<racine du dépôt>` est occupé par la mission
  Phase 26 (branche `feat/phase-26-manuel-utilisateur`, travail non commité). Mitigation
  structurelle : vérifier `driver-lock.sh status` **et** `check-branch-claim.sh` avant tout
  changement de branche ; ne jamais faire de checkout dans l'arbre principal (ADR-064).

- [ ] CONSTRAINT: **Budget de densité ADR-029 quasi épuisé** — `vf-dev-manager.md` est à
  **244/250 lignes** (6 lignes de marge) et les plans 23-03, 23-05, 23-06, 23-07 le modifient
  **tous**. Mitigation structurelle : déporter le contenu vers
  `plugin/dev-orchestrator/references/` (mission-contracts.md, GSD-PIPELINE.md) au lieu
  d'allonger l'agent. Le budget chiffré par plan est dans `23-01-SUMMARY.md`
  §« Densité restante ».

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Édition des définitions d'agents pour changer de modèle | Le 2026-08-01, une demande d'override de modèle « pour la session » a été appliquée en réécrivant `model:` dans `~/.claude/agents/*.md` — modification persistante et globale, hors intention. Restaurée à l'identique depuis. | blocking | L'override de modèle se pose **au dispatch** (paramètre `model` de l'outil Agent), jamais dans le frontmatter. Ne pas rééditer `~/.claude/agents/`. |
| Checkout dans l'arbre principal sans vérifier le lock | Le 2026-08-01, un `git checkout -b` pour la Phase 23 a changé la branche sous une session Phase 26 active. | blocking | `driver-lock.sh status` + `check-branch-claim.sh` avant toute bascule ; worktree dédié sinon. |
| Allonger vf-dev-manager.md | 4 plans restants le touchent avec 6 lignes de marge ADR-029. | advisory | Déport systématique vers `references/`. |

<current_state>
Le plan **23-01 est livré** (Lacune 6 — sûreté du contrat de checkpoint amont) : 4 commits,
SUMMARY écrit, STATE mis à jour. La mission d'équipe tournait sous `vf-dev-manager` et a été
**interrompue manuellement** à l'étape `revue-01` — pas sur un échec, pas sur un blocage.

Les gates sont **verts** : `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`
rend **84 OK / 0 KO / 0 SKIP**.

Le worktree est propre à un fichier près : `.planning/MISSION-23.dag.json` (le DAG de mission)
était non suivi, il est commité par ce handoff.
</current_state>

<completed_work>

Commits sur `feat/phase-23-couplage-gsd` (au-dessus de `origin/main`) :

- `e9dc934` — T1 (D-01) : le champ `gate` traverse la chaîne `mission-contracts.md` → `vf-coder`
  → `vf-dev-manager`. Une seule règle de mapping : `gate="blocking-human"` **ou** précondition
  non satisfaite ⇒ statut `human_needed`.
- `f302419` — T2 (D-02) : désarme `workflow._auto_chain_active` au démarrage de mission
  (best-effort, consigné au rapport). Geste 5 non négociable dans `vf-dev-manager.md`.
- `8d5661a` — T3 (D-03, D-04, D-04bis) : minimum de reprise à 4 sous-champs, halte de nœud,
  réponse aux attentes humaines par le manager — `vf-coder` n'auto-répond jamais.
- `0b7265d` — `23-01-SUMMARY.md` (297 lignes, preuves de mutation incluses) + `STATE.md`.

Nœuds du DAG (`.planning/MISSION-23.dag.json`) : `exec-01` **done**, `revue-01` **ready**,
les 15 autres **blocked** en cascade derrière.
</completed_work>

<remaining_work>

Dans l'ordre du DAG (chaque `exec-NN` est bloqué par la `revue-(NN-1)`) :

1. `revue-01` — revue de code du diff des 4 commits de 23-01. **Seul nœud prêt.**
2. `23-02` — `check-gsd-config.sh` + hooks + `.planning/config.json` (Lacune 5 : le config du
   lab n'est pas aligné sur le moteur, clés `gates`/`safety` inertes).
3. `23-03` — Lacune 3 : doctrine de flags de cycle, écrite dans `GSD-PIPELINE.md` §9.
4. `23-04` — table capabilities/hooks générée.
5. `audit-infra` — audit d'infra couvrant 23-02 et 23-04.
6. `23-05` — voie unique d'invocation (la voie dégradée `gsd-planner`/`gsd-executor` nus).
7. `23-06` — disjonction hooks moteur vs étages VibeFlow (Lacune 1, produit un ADR).
8. `23-07` — budgets de boucle additionnés + briques dormantes (Lacunes 7 et 4).
9. `23-08` — gouvernance : bump de module, README, CHANGELOG, gates verts, puis `revue-08`
   (revue finale + cohérence de phase).
</remaining_work>

<decisions_made>

- **D-01** — une seule règle de mapping (`gate="blocking-human"` OU précondition non satisfaite
  ⇒ `human_needed`), parce que sans elle un manager autonome peut trancher précisément le
  checkpoint que l'exécuteur amont a refusé d'auto-approuver.
- **D-02** — le reset du flag d'enchaînement autonome est best-effort et **consigné au rapport**,
  plutôt que silencieux : on préfère une trace à une garantie qu'on ne peut pas tenir.
- **D-03 (révisée en cadrage)** — le minimum de reprise porte **exactement 4 sous-champs**,
  aligné sur le contrat de continuation de l'exécuteur amont.
- **D-04bis** — `vf-coder` n'auto-répond **jamais** à une attente humaine (ADR-031).
- **Ordre imposé** — la Lacune 6 (sûreté) passe en premier : c'est la seule dont la conséquence
  est un mauvais comportement silencieux, et elle est prérequis de la doctrine de flags.
</decisions_made>

<blockers>
- Densité `vf-dev-manager.md` 244/250 (ADR-029) : contrainte de conception pour les 4 plans
  restants qui le touchent. Pas un blocage dur — un budget à respecter par déport vers
  `references/`.
- Aucun blocage technique. Aucun job asynchrone en cours.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — l'état machine, à lire avant tout.
2. `.planning/MISSION-23.dag.json` — le DAG de mission : quel nœud est prêt, quoi est bloqué.
3. `23-01-SUMMARY.md` — ce qui a été livré, les preuves de mutation, et surtout la section
   « Densité restante » qui chiffre le budget de lignes des plans suivants.
4. `.planning/ROADMAP.md` §Phase 23 — les 7 lacunes et l'ordre imposé.
5. `23-CONTEXT.md` + `23-RESEARCH.md` — le cadrage sur pièce contre `@opengsd/gsd-core@1.9.0`.

## Infrastructure State

- **Worktree** : `../vibeflow-os-p23`, branche
  `feat/phase-23-couplage-gsd`, à jour sur `origin/main`, propre.
- **Driver lock** : `mission-phase23-couplage-gsd` — **libéré** par ce handoff.
- **Arbre principal** : occupé par la Phase 26 (`feat/phase-26-manuel-utilisateur`), travail
  non commité — ne pas y toucher.
- **Gates** : `test-dev-orchestrator.sh` → 84 OK / 0 KO / 0 SKIP.
- **PR** : aucune ouverte pour cette phase.
- Aucun serveur ni watcher en arrière-plan.

<context>
La phase avait été entièrement cadrée et planifiée dans une session antérieure (8 plans,
7 vagues, exigences GSDC-01..GSDC-10). Cette session-ci n'a fait qu'**exécuter** : lancer le
manager de mission, qui a produit son plan de bataille en DAG puis livré le premier plan.

L'interruption est volontaire et propre — pas un échec. Tout ce qui a été produit est commité,
les tests passent, le DAG dit exactement où reprendre.
</context>

<next_action>
Start with: dispatcher le nœud **`revue-01`** — revue de code du diff des 4 commits de 23-01
(`origin/main..HEAD`), périmètre `mission-contracts.md`, `vf-coder.md`, `vf-dev-manager.md`,
`test-dev-orchestrator.sh`. C'est le seul nœud `ready` ; les 15 autres sont bloqués derrière.
Reprendre depuis le worktree `vibeflow-os-p23`, après avoir vérifié le driver-lock.
</next_action>

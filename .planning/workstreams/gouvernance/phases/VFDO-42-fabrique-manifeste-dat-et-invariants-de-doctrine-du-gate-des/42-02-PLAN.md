---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/mobile-test-team/agents/vf-test-orchestrator.md
  - plugin/mobile-test-team/VERSION
  - plugin/mobile-test-team/module.json
  - plugin/mobile-test-team/CHANGELOG.md
  - plugin/mobile-test-team/README.md
  - plugin/business-pilot-bundle/agents/vf-business-manager.md
  - plugin/business-pilot-bundle/VERSION
  - plugin/business-pilot-bundle/module.json
  - plugin/business-pilot-bundle/CHANGELOG.md
  - plugin/business-pilot-bundle/README.md
  - plugin/content-bundle/agents/vf-content-manager.md
  - plugin/content-bundle/VERSION
  - plugin/content-bundle/module.json
  - plugin/content-bundle/CHANGELOG.md
  - plugin/content-bundle/README.md
autonomous: true
requirements: [FABR-05]

must_haves:
  truths:
    - "vf-test-orchestrator porte vf-internal: true et le marqueur Worker interne dans sa description : il n'est plus un agent dispatché (par vf-dev-manager) sans vf-internal (I3), et la corrélation I1 tient"
    - "vf-test-orchestrator garde son allowlist Agent(vf-test-runner, vf-app-fixer) mais sort de la classe manager de D-07 (non vf-internal exigé) : aucun SendMessage ne lui est ajouté — tranché au vu du fichier (D-11)"
    - "RESTRUCTURATION (revise-42b, B1) : quality-gate-client et content-clarity-judge (I5, omitClaudeMd) NE SONT PLUS touchés par ce plan — D-19 a rendu son verdict (règles omises) le 2026-09-24, D-08 est réexaminée avec Samuel, réponse non reçue. Leur pose vit derrière le checkpoint bloquant de 42-05 (Tâche 2)."
    - "vf-business-manager et vf-content-manager portent SendMessage dans tools:, inséré juste avant le jeton Agent( — la ligne finit toujours par la parenthèse fermante (I6, D-07) — INCONDITIONNEL, ne dépend d'aucune réponse de Samuel"
    - "Seul le frontmatter change : le corps (après le second ---) de chaque agent touché est identique octet pour octet à main, et la colonne des instructions du ratchet ne bouge pas (Pitfall 5)"
    - "Un commit par module (mobile-test-team, business-pilot-bundle, content-bundle), chacun avec son bump patch (VERSION, module.json, ligne Version du README, entrée CHANGELOG), séparé de tout commit du gate (D-12) ; chaque module reçoit un SECOND commit + bump patch séparé si le checkpoint de 42-05 arme le champ sur son juge (item d, pas de regroupement artificiel)"
    - "dev-orchestrator n'est pas touché : aucune violation mesurée sur ses agents (42-RESEARCH Open Question 2)"
    - "check-agents.sh --strict reste vert sur chaque dossier d'agents touché, et les suites de module business-pilot-bundle et content-bundle restent vertes"
  artifacts:
    - path: "plugin/mobile-test-team/agents/vf-test-orchestrator.md"
      provides: "worker interne dispatché par vf-dev-manager et vf-auto"
      contains: "vf-internal: true"
    - path: "plugin/business-pilot-bundle/agents/vf-business-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
    - path: "plugin/content-bundle/agents/vf-content-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
  key_links:
    - from: "plugin/dev-orchestrator/agents/vf-dev-manager.md"
      to: "plugin/mobile-test-team/agents/vf-test-orchestrator.md"
      via: "allowlist Agent(... vf-test-orchestrator ...) — le dispatch qui fonde I3 en monde fermé"
      pattern: "vf-test-orchestrator"
    - from: "plugin/conductor/scripts/generate-agent-commands.sh"
      to: "plugin/mobile-test-team/agents/vf-test-orchestrator.md"
      via: "is_internal_agent() — un agent vf-internal ne reçoit plus de commande d'incarnation"
      pattern: "vf-internal"
  prohibitions:
    - statement: "MUST NOT présenter une décision de cadrage de Claude comme un arbitrage humain (commit, CHANGELOG, PR), ni modifier un module de la polarité de Samuel sans l'y nommer relecteur"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit, elle ne se prouve pas par machine (CLAUDE.md, traçabilité des arbitrages)"
---

<objective>
Mettre en conformité, AVANT que le gate n'arme les invariants (42-05, 42-06), les agents de trois
modules mesurés en violation (D-11) : I3 sur `vf-test-orchestrator` (mobile-test-team), I6 sur les
managers de business-pilot-bundle et de content-bundle. Corrections de frontmatter pur, un commit par
module avec son bump patch (D-12). mobile-test-team est de la polarité de Samuel : son commit est
nommé en relecture dans la PR (le relevé des commits est porté par 42-06). dev-orchestrator n'a
aucune violation mesurée et n'est pas touché.

**RESTRUCTURATION (mission, nœud `revise-42b`, B1/B2, 2026-09-24) : I5 (omitClaudeMd sur
`quality-gate-client` et `content-clarity-judge`) SORT de ce plan.** D-19 a rendu son verdict
(`42-D19-MESURE.md`) — `omitClaudeMd` retire aussi les règles `.claude/rules/*.md` — et D-08 est
réexaminée avec Samuel, réponse non reçue. I6/SendMessage et I3/`vf-internal` ne dépendent d'aucune
réponse et restent inconditionnels en vague 1. La pose d'`omitClaudeMd` sur les deux juges de ce plan
vit désormais derrière un checkpoint bloquant dans 42-05 (Tâche 2), avec celle des deux juges de
42-03.

Purpose: FABR-05 — le corpus du dépôt doit passer `--strict` et `--resolve-agents=strict` quand les
invariants deviennent des erreurs, sans période d'avertissement (D-11).
Output: trois agents corrigés (I3, I6 ×2), trois modules bumpés en patch, trois commits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-PATTERNS.md

Patrons de référence (lecture seule) :
- `plugin/mobile-test-team/agents/vf-test-runner.md` : worker interne du même module
  (`vf-internal: true` placé après `memory:`, « Worker interne » dans la description)
- `plugin/dev-orchestrator/agents/vf-dev-manager.md` : manager conforme
  (`…, AskUserQuestion, SendMessage, Agent(...)`)
- `plugin/mobile-test-team/CHANGELOG.md` et `plugin/business-pilot-bundle/CHANGELOG.md` : forme
  « ## [vX.Y.Z] — AAAA-MM-JJ (contexte) » puis « **Patch** : » et des puces
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : mobile-test-team — vf-test-orchestrator devient worker interne (I3), bump patch</name>
  <precondition>PR #85 fusionnée et contenue dans HEAD : `git merge-base --is-ancestor "$(gh pr view 85 --json mergeCommit -q .mergeCommit.oid)" HEAD` rend 0 ET `plugin/conductor/scripts/check-blueprints.sh` existe (D-14) — sinon arrêt</precondition>
  <files>plugin/mobile-test-team/agents/vf-test-orchestrator.md, plugin/mobile-test-team/VERSION, plugin/mobile-test-team/module.json, plugin/mobile-test-team/CHANGELOG.md, plugin/mobile-test-team/README.md</files>
  <read_first>
    - plugin/mobile-test-team/agents/vf-test-orchestrator.md (fichier modifié — frontmatter l.1-10)
    - plugin/mobile-test-team/agents/vf-test-runner.md (patron : place de vf-internal, forme du marqueur)
    - plugin/mobile-test-team/README.md (l.7 ligne Version ; tableau des agents l.100-111)
    - plugin/mobile-test-team/CHANGELOG.md (forme de l'entrée v1.4.5)
    - plugin/dev-orchestrator/agents/vf-dev-manager.md (ligne tools: — le dispatch de vf-test-orchestrator)
    - plugin/conductor/scripts/generate-agent-commands.sh (is_internal_agent, generate_one : une commande présente est conservée)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-06, D-07, D-11, D-12)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Open Question 1, Pitfall 5)
  </read_first>
  <action>
1. `vf-test-orchestrator.md`, frontmatter seul :
   - ajouter la ligne `vf-internal: true` juste après `memory: project` (avant `vf-mcp-consumer:`),
     même place que chez `vf-test-runner` ;
   - dans `description:`, remplacer le dernier segment « ; dispatché par le mode autonome (vf-auto)
     sur ce type de projet. » par « . Worker interne — dispatché par vf-dev-manager ou par le mode
     autonome (vf-auto) sur ce type de projet, pas en usage direct. » — les DEUX dispatcheurs réels
     nommés (D-18 : `plugin/dev-orchestrator/skills/vf-auto/SKILL.md:75` dispatche vraiment
     `vf-test-orchestrator`, sans quoi la formule « dispatché uniquement par le manager » rendrait ce
     second dispatch illégitime sur le papier ; l'invariant I1 de 42-05 est conçu pour tolérer cette
     formulation à deux dispatcheurs, preuve T91b). La description reste un scalaire mono-ligne entre
     guillemets doubles (forme exigée par check-description-fidelity.sh) ; le reste du texte est
     inchangé ;
   - NE PAS ajouter `SendMessage` : tranché au vu du fichier (D-11) — avec `vf-internal: true`,
     l'agent sort de la classe manager de D-07 (allowlist non vide ET non interne), comme `vf-coder`,
     `vf-reviewer` et `vf-auditer`, internes et porteurs d'`Agent(...)`. C'est aussi la lecture du
     Pattern A du kernel : `vf-dev-manager` le dispatche, un manager ne dispatche jamais un autre
     manager. Une seule ligne ajoutée résout I3 et laisse I1 et I2 tenus (42-RESEARCH Open Question 1).
   - Aucune ligne après le second `---` n'est modifiée (Pitfall 5).
2. Bump patch de la valeur lue sur disque à l'exécution (attendu v1.4.5 → v1.4.6) dans `VERSION`,
`module.json` (.version) et la ligne `**Version**` du README (l.7).
3. README : dans le tableau des agents, la colonne « Interne » de `vf-test-orchestrator` passe de
« non » à « `vf-internal: true` (dispatché par `vf-dev-manager` / `vf-auto`) » ; la phrase qui suit
le tableau précise que l'orchestrateur est lui-même un worker interne depuis la version bumpée, donc
sans commande d'incarnation.
4. CHANGELOG, entrée en tête « ## [v1.4.6] — <date d'exécution ISO> (Phase 42 — invariants de
doctrine du gate des agents) », « **Patch** : », puces : `vf-test-orchestrator` devient worker
interne (I3 : dispatché par `vf-dev-manager` sans `vf-internal`) ; il sort de la classe manager de
D-07, donc pas de `SendMessage` exigé ; conséquence : plus de commande d'incarnation générée à
l'install, et une commande déjà posée dans un lab existant n'est pas retirée automatiquement
(`generate-agent-commands.sh` conserve une commande présente) ; frontmatter seul, corps et compte
d'instructions inchangés ; décision de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiée par Samuel (WhatsApp, 2026-09-23), 42-CONTEXT.md D-11 — relecture de Samuel demandée
en PR (D-12).
5. Commit unique pour ce module, en français, sans aucun fichier hors `plugin/mobile-test-team/` :
`fix(mobile-test-team v1.4.6): vf-test-orchestrator devient worker interne (invariant I3)`, corps
reprenant la trace de décision ci-dessus (jamais « arbitrage »). Pas de trailer Gate-Touche (hors
surface G-2).
  </action>
  <verify>
    <automated>f=plugin/mobile-test-team/agents/vf-test-orchestrator.md; B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; fm "$f" | grep -q '^vf-internal: true$' && fm "$f" | grep -q '^description: ".*Worker interne.*"$' && cmp -s <(git show "$B:$f" | body) <(body < "$f") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/mobile-test-team/agents >/dev/null && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-MTT-OK</automated>
    <fails_when>la ligne CORPUS-MTT-OK est absente (vf-internal ou marqueur manquant, corps différent de main, gate non vert sur le module, ou triade VERSION/module.json désynchronisée)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/mobile-test-team\/agents\/vf-test-orchestrator\.md/{print ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>la sortie contient INSTR-BOUGE, ou la ligne BILAN ne porte pas « 0 depassement(s) » et « code=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' plugin/mobile-test-team/agents/vf-test-orchestrator.md | grep -c '^vf-internal: true$'` rend 1
    - `grep -c '^description: ".*Worker interne — dispatché par vf-dev-manager ou par le mode autonome (vf-auto).*"$' plugin/mobile-test-team/agents/vf-test-orchestrator.md` rend 1
    - le corps de vf-test-orchestrator.md (après le second `---`) est identique à celui de main (commande de vérification : CORPUS-MTT-OK)
    - `cat plugin/mobile-test-team/VERSION` et `.version` de module.json valent la même version, patch +1 de la valeur de main ; `grep -c '^\*\*Type\*\* : .*\*\*Version\*\* : '"$(cat plugin/mobile-test-team/VERSION)" plugin/mobile-test-team/README.md` rend 1
    - `head -5 plugin/mobile-test-team/CHANGELOG.md | grep -c "^## \[$(cat plugin/mobile-test-team/VERSION)\]"` rend 1
    - `B=$(git merge-base HEAD origin/main); c=$(git log -1 --format=%H -- plugin/mobile-test-team/VERSION); git merge-base --is-ancestor "$c" "$B" && echo ISOLATION-NON-PROBANTE-COMMIT-ANTERIEUR || git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/mobile-test-team/'` rend 0 (le commit du module ne touche aucun autre chemin — **CORRECTIF DE REVUE I1, mission revise-42b : le commit trouvé doit être postérieur à la base, sinon la sonde valide trivialement un commit d'historique antérieur à l'exécution**)
  </acceptance_criteria>
  <done>vf-test-orchestrator est un worker interne conforme à I1 et I3, hors classe manager de D-07 ; frontmatter seul modifié ; mobile-test-team bumpé en patch dans un commit dédié qui trace la décision et annonce la relecture de Samuel.</done>
</task>

<task type="auto">
  <name>Tâche 2 : business-pilot-bundle — manager avec SendMessage (I6), bump patch (I5 SORTI, cf. checkpoint 42-05)</name>
  <files>plugin/business-pilot-bundle/agents/vf-business-manager.md, plugin/business-pilot-bundle/VERSION, plugin/business-pilot-bundle/module.json, plugin/business-pilot-bundle/CHANGELOG.md, plugin/business-pilot-bundle/README.md</files>
  <read_first>
    - plugin/business-pilot-bundle/agents/vf-business-manager.md (fichier modifié — ligne tools: l.4)
    - plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh (T5 : ce que la suite exige du manager ; T4, sur le juge, N'EST PAS visé par cette tâche — quality-gate-client.md n'est plus touché ici)
    - plugin/business-pilot-bundle/README.md (l.7), plugin/business-pilot-bundle/CHANGELOG.md (forme v2.0.9)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-11, D-12, D-19)
  </read_first>
  <action>
RESTRUCTURATION (mission, nœud revise-42b, B1/B2) : cette tâche NE POSE PLUS omitClaudeMd sur
quality-gate-client.md. D-19 a rendu son verdict (42-D19-MESURE.md, 2026-09-24) : omitClaudeMd
retire aussi les règles .claude/rules/*.md, pas seulement le CLAUDE.md -- conséquence prévue par
D-19, D-08 est réexaminée avec Samuel, réponse non reçue a ce jour. SendMessage sur le manager,
vf-internal et D-18 (Tâche 1) ne dependent d'aucune réponse : ils restent en vague 1. La pose du
champ sur les 4 juges et l'armement d'invariant_i5 vivent maintenant derrière un checkpoint bloquant
dans 42-05 (Tâche 2), qui gere aussi quality-gate-client.md de CE module.
1. vf-business-manager.md : dans la ligne tools:, insérer le jeton "SendMessage, " juste AVANT
Agent( -- même placé que chez vf-dev-manager ; la ligne finit toujours par la parenthese fermante
de l'allowlist. C'est un manager au sens de D-07 : allowlist non vide, non interne.
2. Bump patch de la valeur lue sur disque (attendu v2.0.9 -> v2.0.10) : VERSION, module.json,
ligne **Version** du README (l.7). CHANGELOG, entrée en tête "## [v2.0.10] -- <date d'execution
ISO> (Phase 42 -- invariants de doctrine du gate des agents)", "**Patch** :" -- puce unique : le
manager déclare SendMessage (I6, D-07) ; frontmatter seul, compte d'instructions inchangé ; NE PAS
mentionner I5/omitClaudeMd dans cette entrée -- ce module reçoit un SECOND bump patch séparé si et
seulement si le checkpoint de 42-05 arme le champ sur quality-gate-client.md (item d de la
mission : deux décisions gated par des conditions différentes, donc deux commits et deux bumps
légitimes, pas un regroupement artificiel qui retiendrait SendMessage en otage d'un arbitrage encore
ouvert). Décisions de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiées par Samuel (WhatsApp, 2026-09-23), 42-CONTEXT.md D-07, D-11.
3. Commit unique pour ce module :
fix(business-pilot-bundle v2.0.10): manager avec SendMessage (invariant I6).
Aucun fichier hors plugin/business-pilot-bundle/.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; M=plugin/business-pilot-bundle/agents/vf-business-manager.md; fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/business-pilot-bundle/agents >/dev/null && bash plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-BPB-OK</automated>
    <fails_when>la ligne CORPUS-BPB-OK est absente (SendMessage mal placé, corps modifié, gate ou suite du module rouge, triade de version désynchronisée)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/business-pilot-bundle\/agents\/vf-business-manager\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas "0 depassement(s)" et "code=0"</fails_when>
    <automated>grep -c '^omitClaudeMd' plugin/business-pilot-bundle/agents/quality-gate-client.md</automated>
    <fails_when>la sortie n'est pas exactement 0 (garde anti-fusion : cette tâche ne doit JAMAIS toucher quality-gate-client.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tools: .*SendMessage, Agent(vf-business-commercial' plugin/business-pilot-bundle/agents/vf-business-manager.md` rend 1
    - le corps du manager est identique a main et sa colonne d'instructions ne bouge pas (CORPUS-BPB-OK, INSTR-INCHANGE)
    - `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- plugin/business-pilot-bundle/agents/quality-gate-client.md | wc -l` rend 0 (le juge n'est pas touché par cette tâche)
    - VERSION, module.json et la ligne Version du README portent la même version, patch +1 de main ; l'entrée CHANGELOG de tête porte cette version
    - `B=$(git merge-base HEAD origin/main); c=$(git log -1 --format=%H -- plugin/business-pilot-bundle/VERSION); git merge-base --is-ancestor "$c" "$B" && echo ISOLATION-NON-PROBANTE-COMMIT-ANTERIEUR || git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/business-pilot-bundle/'` rend 0 — **CORRECTIF DE REVUE (mission `revise-42c`, même correctif I1 qu'à la Tâche 1) : le commit trouvé doit être POSTÉRIEUR à la base, sinon la sonde valide trivialement un commit d'historique antérieur à cette exécution, jamais celui que cette tâche vient de créer.**
    - `bash plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh` sort 0
  </acceptance_criteria>
  <done>Le manager de business-pilot-bundle satisfait I6 par un ajout de frontmatter ; module bumpe en patch dans un commit dedie ; suite du module et gate verts. Le juge (I5) reste inchangé -- sa pose vit désormais derrière le checkpoint D-19 de 42-05.</done>
</task>

<task type="auto">
  <name>Tâche 3 : content-bundle -- manager avec SendMessage (I6), bump patch (I5 SORTI, cf. checkpoint 42-05)</name>
  <files>plugin/content-bundle/agents/vf-content-manager.md, plugin/content-bundle/VERSION, plugin/content-bundle/module.json, plugin/content-bundle/CHANGELOG.md, plugin/content-bundle/README.md</files>
  <read_first>
    - plugin/content-bundle/agents/vf-content-manager.md (fichier modifié -- ligne tools: l.4)
    - plugin/content-bundle/scripts/tests/test-content-bundle.sh (assertions sur le manager ; content-clarity-judge.md N'EST PAS vise par cette tâche)
    - plugin/content-bundle/README.md (l.7), plugin/content-bundle/CHANGELOG.md (forme de la dernière entrée)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-11, D-12, D-19)
  </read_first>
  <action>
RESTRUCTURATION (mission, nœud revise-42b, B1/B2) -- même raison que la Tâche 2 : D-19 a rendu
son verdict "règles omises", D-08 est réexaminée avec Samuel, réponse non reçue. SendMessage ne
dépend d'aucune réponse : il reste en vague 1. content-clarity-judge.md (I5) vit désormais derrière
le checkpoint de 42-05 (Tâche 2).
1. vf-content-manager.md : insérer "SendMessage, " juste AVANT Agent( dans la ligne tools: --
manager au sens de D-07 ; la ligne finit toujours par la parenthese fermante.
2. Bump patch de la valeur lue sur disque (attendu v2.0.9 -> v2.0.10) : VERSION, module.json,
ligne **Version** du README. CHANGELOG en tête, même forme que business-pilot-bundle -- puce
unique : SendMessage sur le manager (I6, D-07), frontmatter seul, PAS de mention I5/omitClaudeMd
(module candidat a un second bump patch séparé si le checkpoint de 42-05 arme le champ sur
content-clarity-judge.md, item d). Décisions de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiées par Samuel (WhatsApp, 2026-09-23), 42-CONTEXT.md D-07, D-11.
3. Commit unique pour ce module :
fix(content-bundle v2.0.10): manager avec SendMessage (invariant I6).
Aucun fichier hors plugin/content-bundle/.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; M=plugin/content-bundle/agents/vf-content-manager.md; fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/content-bundle/agents >/dev/null && bash plugin/content-bundle/scripts/tests/test-content-bundle.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-CB-OK</automated>
    <fails_when>la ligne CORPUS-CB-OK est absente</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/content-bundle\/agents\/vf-content-manager\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas "0 depassement(s)" et "code=0"</fails_when>
    <automated>grep -c '^omitClaudeMd' plugin/content-bundle/agents/content-clarity-judge.md</automated>
    <fails_when>la sortie n'est pas exactement 0 (garde anti-fusion : cette tâche ne doit JAMAIS toucher content-clarity-judge.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tools: .*SendMessage, Agent(vf-content-strategist' plugin/content-bundle/agents/vf-content-manager.md` rend 1
    - `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- plugin/content-bundle/agents/content-clarity-judge.md | wc -l` rend 0
    - corps inchangé, colonne d'instructions inchangee (CORPUS-CB-OK, INSTR-INCHANGE)
    - VERSION, module.json, ligne Version du README et entrée CHANGELOG de tête portent la même version, patch +1 de main
    - `B=$(git merge-base HEAD origin/main); c=$(git log -1 --format=%H -- plugin/content-bundle/VERSION); git merge-base --is-ancestor "$c" "$B" && echo ISOLATION-NON-PROBANTE-COMMIT-ANTERIEUR || git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/content-bundle/'` rend 0 — **CORRECTIF DE REVUE (mission `revise-42c`, même correctif I1 qu'à la Tâche 1) : le commit trouvé doit être POSTÉRIEUR à la base, sinon la sonde valide trivialement un commit d'historique antérieur à cette exécution.**
    - `bash plugin/content-bundle/scripts/tests/test-content-bundle.sh` sort 0
  </acceptance_criteria>
  <done>Le manager de content-bundle satisfait I6 ; module bumpe en patch dans un commit dedie ; suite du module et gate verts. Le juge (I5) reste inchangé -- sa pose vit désormais derrière le checkpoint D-19 de 42-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent → runtime Claude Code | `vf-internal` et `tools:` changent ce que l'agent reçoit et ce qu'il peut faire |
| dépôt → labs installés | les agents corrigés sont recopiés chez les utilisateurs à la prochaine mise à jour |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06 | Elevation of Privilege | vf-test-orchestrator dans un lab existant | low | accept | une commande d'incarnation déjà posée survit (generate-agent-commands.sh conserve une commande présente) ; consigné au CHANGELOG du module, relu par Samuel |
| T-42-08 | Tampering | corps d'agent modifié en douce | low | mitigate | corps comparé octet pour octet à main (cmp) et colonne d'instructions du ratchet inchangée (INSTR-INCHANGE) |
| T-42-09 | Repudiation | trace de décision dans les commits | low | mitigate | chaque commit nomme « décision de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiée par Samuel (WhatsApp, 2026-09-23) » (D-07, D-11) ; prohibition P4 |

**T-42-07 retiré de ce plan (mission `revise-42b`/`revise-42c`) :** ce plan ne pose plus
`omitClaudeMd` sur les juges — cette action, et le threat associé, vivent désormais en 42-05
(Tâche 3, conditionnelle à l'arbitrage D-19/D-08).
</threat_model>

<verification>
- `for d in plugin/mobile-test-team/agents plugin/business-pilot-bundle/agents plugin/content-bundle/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d"; done` — rc 0 partout.
- `bash plugin/conductor/scripts/check-instruction-budget.sh` — `0 depassement(s)`, `code=0`.
- `bash scripts/check-version-sync.sh` — rc 0.
- Trois commits, un par module, aucun ne touche le gate ni `.planning/`.
</verification>

<success_criteria>
- Les violations mesurées de D-11 sur ces trois modules sont fermées par CE plan : I3 (vf-test-orchestrator), I6 (vf-business-manager, vf-content-manager). I5 (quality-gate-client, content-clarity-judge) N'EST PLUS de ce plan (restructuration mission `revise-42b`/`revise-42c`) — sa pose, conditionnelle à l'arbitrage D-19/D-08, vit derrière le checkpoint de 42-05 (Tâche 2).
- D-12 : un commit par module avec bump patch ; dev-orchestrator intact.
- Aucun corps d'agent modifié.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `T103`, jeton `CIBLE-ABSENTE` | invocation nue sur cible absente hors --hook (D-20) | 42-01 |
| `MUT-D20` | mutant de la ligne `cible_absente` posée en 42-01 | 42-04 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` | invariants locaux | 42-05 |
| `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `SendMessage` sur `vf-business-manager`, `vf-content-manager` (inconditionnel) | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur `quality-gate-client`, `content-clarity-judge` (SEULEMENT si ARBITRAGE-MAINTENIR au checkpoint D-19) | frontmatter du corpus, checkpoint | 42-05 |
| `SendMessage` sur `vf-growth-manager`, `vf-design-manager` (inconditionnel) | frontmatter du corpus | 42-03 |
| `omitClaudeMd: true` sur `growth-quality-judge`, `vf-design-judge` (SEULEMENT si ARBITRAGE-MAINTENIR au checkpoint D-19) | frontmatter du corpus, checkpoint | 42-05 |
| mobile-test-team, business-pilot-bundle, content-bundle en patch | versions de module | 42-02 |
| growth-bundle, design-orchestrator en patch | versions de module | 42-03 |
| conductor en mineure | version de module | 42-06 |

## Flagged assumptions

- **Flake connu T9/T29/T32 de test-check-agents.sh (documenté dans la PR #100, workstream fiabilite) : un rouge isolé sur ces trois cas précis (SIGPIPE) se rejoue seul avant d'être déclaré réel — jamais pris pour argent comptant à la première lecture de sortie de suite (0 KO attendu dans ce plan).**

- **Sonde de bords spec-less — FABR-05 : `unclassified` / `unresolved`.** Non résolu d'office ; les
  bords traités ici (corps inchangé, compte d'instructions inchangé, place de SendMessage imposée
  par T8 de design-orchestrator) sont des vérités du plan, pas une résolution de la sonde.
- **omitClaudeMd et les `rules/*.md` — CORRIGÉ (42-D19-MESURE.md, 2026-09-24) :** contrairement à ce
  que 42-RESEARCH A2 avait supposé (« silence de la doc », inférence, jamais mesuré), la doc
  officielle est EXPLICITE et la mesure le confirme — `omitClaudeMd: true` retire aussi les règles
  `.claude/rules/*.md` sans `paths:`, pas seulement le CLAUDE.md. Conséquence directe : la pose
  d'`omitClaudeMd` sur les juges de ce module (`quality-gate-client`, `content-clarity-judge`) sort
  de ce plan et vit derrière le checkpoint D-19 de 42-05, D-08 étant réexaminée avec Samuel.
- **Relecture de Samuel** (D-12) : mobile-test-team est nommé par le cadrage. `git log` montre aussi
  Samuel auteur majoritaire des trois bundles ; le cadrage ne les lui attribue pas — information à
  porter dans la PR, pas un changement de D-12.
- **Commande d'incarnation résiduelle** de `vf-test-orchestrator` dans les labs déjà installés :
  non retirée par l'installeur (comportement existant de generate-agent-commands.sh), consignée.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-02-SUMMARY.md` when done — il liste le hash du commit mobile-test-team (relecture Samuel). Les juges (quality-gate-client, content-clarity-judge) ne sont plus touchés par ce plan — leur pose vit derrière le checkpoint D-19 de 42-05.
</output>

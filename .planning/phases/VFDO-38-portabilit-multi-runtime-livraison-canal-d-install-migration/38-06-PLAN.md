---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 06
type: execute
wave: 6
depends_on: ["38-05"]
files_modified:
  - plugin/conductor/scripts/runtime-registry.sh
  - plugin/conductor/scripts/tests/test-runtime-registry.sh
  - plugin/conductor/skills/vf-calibrate/SKILL.md
  - plugin/conductor/scripts/verify-runtime-reversibility.sh
  - plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh
  - plugin/conductor/scripts/check-artifact-fidelity.sh
  - plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
  - README.md
  - README.fr.md
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lab existant (les 3 formes réelles : clé `runtime` absente, scalaire, ou objet) reste lisible SANS régression par gsd-core (`config['runtime']` demeure une chaîne simple ou absente — jamais un objet passé là où gsd-core attend une chaîne, sous peine d'être silencieusement ignoré par `canonicalizeRuntimeName`)"
    - "`vf-calibrate` annonce, DANS le skill ET dans sa sortie réelle, laquelle de ses deux natures (propagation additive de version / migration soustractive de runtime) est en cours — jamais implicite, jamais mélangé dans une seule sortie sans étiquette"
    - "Une bascule de runtime ne s'exécute JAMAIS sans être passée par dry-run PUIS confirmation explicite PUIS écriture — les trois étapes dans cet ordre, jamais une seule d'entre elles sautée"
    - "Une réversibilité install → bascule → retour est prouvée par comparaison d'ENSEMBLES de fichiers (`comm`), jamais par un compte de fichiers qui masquerait un fichier différent au même total"
    - "Un runtime installé en coexistence avec `claude` (donc sans gouvernance de hooks, aucun mécanisme équivalent mesuré à ce jour) est déclaré par le gate de fidélité (FIDE) À L'INSTALL ET AU `status` — jamais l'un sans l'autre"
  artifacts:
    - "plugin/conductor/scripts/runtime-registry.sh : lecture/écriture de la clé racine `runtime` (chaîne, jamais touchée en forme) + la clé sœur riche `vf_runtimes` (installed[]/active), gérant les 3 cas d'entrée"
    - "plugin/conductor/scripts/verify-runtime-reversibility.sh : capture d'arbre avant bascule, bascule, retour, comparaison `comm` — preuve, pas déclaration"
  key_links:
    - "runtime-registry.sh -> .planning/config.json : clé racine `runtime` (chaîne, contrat gsd-core `runtime-name-policy.cjs::canonicalizeRuntimeName`, ignore silencieusement tout candidat non-chaîne — JAMAIS remplacée par un objet) + clé sœur `vf_runtimes: {installed:[], active:''}` (propriété VibeFlow, gsd-core ne la lit jamais)"
    - "vf-calibrate/SKILL.md (nouvelle section migration de runtime) -> runtime-registry.sh --dry-run puis --confirmed, -> vibeflow-update.sh --target <chemin> install --all (lot 4) : la bascule RÉUTILISE l'injection de cible du lot 4, jamais une 2e implémentation de pose"
    - "verify-runtime-reversibility.sh -> comm -3 <(find avant | sort) <(find après | sort) : preuve fichier à fichier, MIGR-04"
    - "check-artifact-fidelity.sh --coexistence-report -> .planning/config.json (vf_runtimes.installed) -> show_status() (vibeflow-update.sh) ET bannière d'install (déjà câblée par FIDE, 38-01) : MÊME gate, deux points d'affichage"
  prohibitions:
    - "Ne jamais faire porter la richesse (installed[]/active) par la clé racine `runtime` elle-même — gsd-core y attend une chaîne simple ou rien ; toute richesse vit dans la clé sœur `vf_runtimes`, dédiée à VibeFlow"
    - "Ne jamais exécuter une bascule de runtime sans le cycle complet dry-run -> confirmation -> écriture (ADR-031, D-38-B point 2) — un `runtime-registry.sh set-active` sans `--confirmed` explicite ne doit JAMAIS écrire"
    - "Ne jamais prouver la réversibilité par un COMPTE de fichiers — deux arbres de taille identique peuvent différer fichier par fichier ; la preuve est un `comm`/diff d'ENSEMBLES, jamais un nombre"
    - "Ne jamais réimplémenter la pose sous cible custom dans vf-calibrate — réutiliser `vibeflow-update.sh --target` (lot 4) tel quel"
---

<objective>
Lot 6 (`MIGR`), dernier du cadrage 38-CONTEXT.md : `.planning/config.json` porte le runtime actif ET
les runtimes installés (rétro-compatible sur les 3 cas réels), `vf-calibrate` expose sa dualité
propagation/migration explicitement, la bascule est gatée dry-run → confirmation → écriture, la
réversibilité est prouvée fichier à fichier, et la coexistence sans hooks est déclarée par FIDE.
Séquencé APRÈS ADPT (lot 5) : ordre imposé par REQUIREMENTS.md ET dépendance RÉELLE — la bascule de
lab s'appuie sur `--target` (lot 4) pour isoler la cible et sur l'adaptateur (lot 5) pour que la
cible Codex ait des rôles dispatchables une fois basculée.

Purpose: c'est la promesse finale du cadrage — « un lab Claude peut migrer OU coexister sans se
croire entier » (38-CONTEXT.md, critère de « fini » de Samuel). Sans ce lot, les 5 précédents
restent des capacités isolées (gate, installeur, rollback, cible, adaptateur) sans jamais former un
parcours de migration réel qu'un opérateur peut suivre de bout en bout.

Output: registre de runtime rétro-compatible, `vf-calibrate` à double nature déclarée, preuve de
réversibilité, coexistence sans hooks déclarée par FIDE à deux endroits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/conductor/skills/vf-calibrate/SKILL.md
@.planning/config.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tache 1 : bout-en-bout — registre de runtime, 3 cas, bascule gatée (MIGR-01, MIGR-03)</name>
  <files>plugin/conductor/scripts/runtime-registry.sh, plugin/conductor/scripts/tests/test-runtime-registry.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 46-53 (D-38-B : « le runtime cesse d'être un scalaire… forme portant «runtimes installés» + «runtime actif», rétro-compatible. Trois cas à couvrir… absent (mesuré : aucune clé runtime, gsd-core le dérive, agent_runtime: "claude"), scalaire (labs qui l'ont), objet (forme neuve). La bascule est soustractive et gatée (ADR-031) : dry-run montré → confirmation → écriture. Jamais par défaut »)
    - ~/.claude/gsd-core/bin/lib/runtime-name-policy.cjs:94-100 (`resolveRuntimeNameFromCandidates`/`canonicalizeRuntimeName` : `typeof value !== 'string'` -> ignoré silencieusement — CONTRAT DUR, la clé racine `runtime` ne doit JAMAIS devenir un objet)
    - ~/.claude/gsd-core/bin/lib/model-resolver.cjs:92 (`config['runtime']` : c'est LE point de lecture réel côté gsd-core — vérifie que la clé racine reste exploitable telle quelle après ce lot)
    - .planning/config.json (l'état RÉEL de ce dépôt : aucune clé `runtime` — cas « absent », à utiliser comme fixture de référence pour le cas 1, JAMAIS modifié par ce plan — ce fichier reste celui du lab `vibeflow-os` lui-même, une bascule réelle est un geste opérationnel hors périmètre)
    - plugin/_internal/vibeflow-update.sh:93-103 (patron de garde `--dry-run` déjà en place — MÊME philosophie à reproduire ici : un flag explicite, jamais un défaut qui écrit)
  </read_first>
  <behavior>
    - Test : `runtime-registry.sh get-active` sur une fixture `config.json` SANS clé `runtime` (cas absent) rend `claude` (la dérivation gsd-core par défaut, jamais un vide qui casserait un appelant).
    - Test : sur une fixture avec `"runtime": "codex"` (cas scalaire), `get-active` rend `codex`, `list-installed` rend `codex` seul (rétro-compat : un scalaire implique un seul runtime installé, lui-même).
    - Test : sur une fixture avec `"vf_runtimes": {"installed": ["claude","codex"], "active": "codex"}` ET `"runtime": "codex"` (cas objet, forme neuve), `get-active` rend `codex`, `list-installed` rend les 2.
    - Test : `runtime-registry.sh set-active opencode --dry-run` sur une fixture n'écrit RIEN (hash du fichier avant/après identique) et imprime ce qui SERAIT changé.
    - Test : `runtime-registry.sh set-active opencode` (SANS `--confirmed`) échoue (exit non-zéro, message explicite), n'écrit rien — le flag `--confirmed` est OBLIGATOIRE pour toute écriture réelle.
    - Test : `runtime-registry.sh set-active opencode --confirmed` écrit bien `"runtime": "opencode"` (chaîne, INCHANGÉ EN FORME) et `vf_runtimes.active: "opencode"`, `vf_runtimes.installed` ÉTENDU (pas remplacé — l'ancien actif reste dans `installed[]`, cohérent avec « coexistence par défaut », D-38-B).
  </behavior>
  <action>
Créer `plugin/conductor/scripts/runtime-registry.sh`, usage `runtime-registry.sh <get-active|
list-installed|set-active <runtime>> [--config <chemin>] [--dry-run] [--confirmed]` (`--config`
pour les fixtures de test, défaut `.planning/config.json` du cwd).

Lecture (`get-active`/`list-installed`) : parser le JSON via `node -e` (aucune dépendance
npm — `JSON.parse`/`fs.readFileSync` suffisent). Dériver selon les 3 cas :
1. Ni `runtime` ni `vf_runtimes` -> actif = `claude` (miroir de la dérivation gsd-core), installés =
   `["claude"]`.
2. `runtime` présent en CHAÎNE, `vf_runtimes` absent -> actif = cette chaîne, installés = `[cette
   chaîne]`.
3. `vf_runtimes` présent (objet `{installed, active}`) -> lire directement ; si `runtime` (racine)
   diverge de `vf_runtimes.active`, PRIVILÉGIER `vf_runtimes.active` pour la lecture VF, mais ne
   JAMAIS corriger silencieusement `runtime` à la lecture seule (seule `set-active` écrit).

Écriture (`set-active <runtime> [--dry-run] [--confirmed]`) :
- `--dry-run` (sans `--confirmed`) : calculer et IMPRIMER le futur état (`runtime: <ancien> ->
  <nouveau>`, `vf_runtimes.installed: [...] -> [...]`), AUCUNE écriture disque. C'est la preview
  que `vf-calibrate` montrera à l'utilisateur (étape 3 du skill, patron déjà établi pour la
  propagation de version).
- Sans `--confirmed` (et sans `--dry-run`) : erreur explicite « --confirmed requis pour écrire —
  relancer avec --dry-run pour prévisualiser d'abord », exit non-zéro, AUCUNE écriture.
- `--confirmed` : réécrire `.planning/config.json` de façon ATOMIQUE (fichier temporaire + `mv`,
  jamais une écriture partielle) : `runtime` (racine) devient le nouveau runtime EN CHAÎNE (jamais
  un objet — contrat gsd-core), `vf_runtimes.active` devient le nouveau runtime, `vf_runtimes.
  installed` s'ÉTEND pour inclure le nouveau ET conserve l'ancien actif (coexistence par défaut,
  D-38-B — la SUPPRESSION d'un runtime de `installed[]` est un geste distinct, hors périmètre de ce
  lot, non demandé par MIGR-01..05).

Créer `plugin/conductor/scripts/tests/test-runtime-registry.sh` sur le patron des suites du dépôt.
  </action>
  <acceptance_criteria>
    - Sur fixture sans clé `runtime`, `get-active` rend `claude` — vérifié par exécution réelle.
    - Sur fixture scalaire `"runtime":"codex"`, `get-active`/`list-installed` cohérents avec le cas 2.
    - `set-active … --dry-run` : sha256 du fichier de fixture identique avant/après.
    - `set-active … ` sans `--confirmed` : sort non-zéro, sha256 identique avant/après.
    - `set-active … --confirmed` : `runtime` reste une chaîne JSON valide (`node -e "typeof
      JSON.parse(...).runtime === 'string'"` sort vrai), `vf_runtimes.installed` contient BIEN
      l'ancien ET le nouveau runtime (coexistence, pas remplacement).
    - `bash plugin/conductor/scripts/tests/test-runtime-registry.sh` sort 0, bannière `== résultat :`.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-runtime-registry.sh</automated>
  </verify>
  <done>Le registre lit les 3 formes réelles de config.json sans régresser le contrat gsd-core (`runtime` reste une chaîne), et toute bascule passe obligatoirement par dry-run puis `--confirmed` — jamais une écriture par défaut.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : vf-calibrate à double nature + réversibilité prouvée (MIGR-02, MIGR-04)</name>
  <files>plugin/conductor/skills/vf-calibrate/SKILL.md, plugin/conductor/scripts/verify-runtime-reversibility.sh, plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh, plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/conductor/skills/vf-calibrate/SKILL.md:1-30 (structure actuelle : frontmatter `description`, section « Pourquoi ce skill existe », séquence « 1. Détecter l'écart » à « 5. Synthèse » — TOUTE la skill actuelle ne parle que de propagation de VERSION framework, jamais de runtime — la dualité D-38-A est à AJOUTER, jamais mélangée dans la séquence existante qui reste inchangée)
    - plugin/conductor/skills/vf-calibrate/SKILL.md:94-115 (« Garde-fous » et « Références » — sections de fin où la nouvelle section « Migration de runtime » doit s'insérer AVANT, en restant cohérente avec le style du reste du fichier)
    - 38-CONTEXT.md lignes 42-45 (D-38-A : « la dualité est explicite dans la skill ET dans la sortie — l'utilisateur sait dans laquelle il est AVANT toute écriture. Jamais implicite »)
    - 38-CONTEXT.md lignes 55-57 (D-38-C : « install → bascule → retour, arbre comparé fichier à fichier. Le lot rollback est PRÉREQUIS du lot migration (déjà imposé par la topologie du DAG de mission) » — ROLL (lot 3) est déjà livré à ce stade, `rollback`/`--dry-run rollback` disponibles)
  </read_first>
  <behavior>
    - Test : `verify-runtime-reversibility.sh` sur une fixture `TARGET_ROOT` (`mktemp -d`) : capture l'arbre AVANT (`find … | sort`), simule une bascule (invoque `vibeflow-update.sh --target <fixture> install --all` avec `VIBEFLOW_CACHE` de fixture), capture l'arbre APRÈS bascule, puis un ROLLBACK (rejouant le lot 3), capture l'arbre APRÈS RETOUR — `comm -3 <avant> <après-retour>` rend une liste VIDE (ensembles identiques).
    - Test : si `comm -3` rend une différence NON vide (fixture volontairement cassée — un fichier supprimé après retour), le script sort non-zéro et IMPRIME la différence — jamais un succès masqué.
  </behavior>
  <action>
Dans `vf-calibrate/SKILL.md` :
1. Frontmatter `description` — AJOUTER, sans réécrire le reste, une clause explicite sur la seconde
   nature : « … Détecte aussi une divergence de RUNTIME (Claude vs Codex/OpenCode/kimi-code) et
   propose soit une bascule (migration soustractive) soit une coexistence — TOUJOURS annoncée comme
   distincte de la propagation de version. »
2. Nouvelle section « ## Deux natures — à annoncer AVANT toute action » juste après le titre H1,
   AVANT « ## Pourquoi ce skill existe » : énonce noir sur blanc que ce skill porte (a) la
   PROPAGATION additive de version framework (séquence 1-5 existante, inchangée) et (b) la
   MIGRATION soustractive de runtime (nouvelle section ci-dessous) — et que la toute première ligne
   de sortie du skill, quel que soit le déclencheur, ANNONCE dans laquelle des deux l'utilisateur se
   trouve (`[vf-calibrate:propagation]` ou `[vf-calibrate:migration-runtime]`), jamais un texte qui
   laisse deviner.
3. Nouvelle section « ## Migration de runtime (coexistence ou bascule) » avant « ## Garde-fous » :
   - Détection : `runtime-registry.sh list-installed` (lot 6, tâche 1) vs le runtime réellement
     détecté sur CE poste (`runtime-cli-dispatch.sh`, lot 2) — si le poste porte un runtime absent
     de `installed`, c'est une opportunité de coexistence/bascule à proposer (jamais imposée).
   - Coexistence (par défaut, D-38-B) : `vibeflow-update.sh --target <chemin dédié> install --all`
     (lot 4) pose le lab sous une cible SÉPARÉE pour le nouveau runtime, `runtime-registry.sh
     set-active <nouveau> --confirmed` étend `installed[]` SANS retirer l'ancien.
   - Bascule (soustractive, explicite) : dry-run (`runtime-registry.sh set-active <nouveau>
     --dry-run` + `vibeflow-update.sh --dry-run update --all` sur la cible visée) montré à
     l'utilisateur -> confirmation -> écriture réelle (`--confirmed`). JAMAIS par défaut (ADR-031).
   - Réversibilité : avant toute bascule réelle, exécuter `verify-runtime-reversibility.sh` (défini
     ci-dessous) en mode PREUVE sur la cible visée — si la preuve échoue, ARRÊTER, ne jamais
     basculer sur une réversibilité non prouvée.
   - Coexistence sans hooks : rappeler que `check-artifact-fidelity.sh --coexistence-report`
     (tâche 3 de ce plan) est la source de vérité de ce qui manque au runtime coexistant — ne
     jamais reformuler ce diagnostic dans la skill, le RELAYER tel quel.

Créer `plugin/conductor/scripts/verify-runtime-reversibility.sh`, usage
`verify-runtime-reversibility.sh --target <chemin> [--cache <chemin>]` : capture `find <chemin> -type
f | sort` AVANT, invoque `vibeflow-update.sh --target <chemin> install --all` (bascule simulée),
capture APRÈS-bascule, invoque le ROLLBACK du lot 3 module par module (glob `.vibeflow-installed`
de la cible pour connaître les modules posés), capture APRÈS-RETOUR, `comm -3 avant après-retour` —
vide -> exit 0 + `✓ réversibilité prouvée (N fichiers comparés)` ; non-vide -> exit 1 + la diff
imprimée en clair.

Créer `plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh` sur le patron du dépôt.

Bump `plugin/conductor/VERSION` (minor — capacités neuves : registre de runtime, migration) +
`CHANGELOG.md` (une entrée couvrant les 3 tâches de ce plan, cohérent avec la convention déjà
observée dans ce fichier pour les plans multi-tâches).
  </action>
  <acceptance_criteria>
    - `grep -n 'Deux natures\|Migration de runtime' plugin/conductor/skills/vf-calibrate/SKILL.md | grep -c .` ≥ 2.
    - `grep -c '\[vf-calibrate:propagation\]\|\[vf-calibrate:migration-runtime\]' plugin/conductor/skills/vf-calibrate/SKILL.md` ≥ 1 (l'annonce est bien nommée dans le texte de la skill, pas seulement décrite en prose vague).
    - Fixture réelle : `verify-runtime-reversibility.sh --target <tmp>` sur un module simple posé/basculé/retourné rend `comm -3` VIDE — capturé sur exécution réelle.
    - Fixture volontairement cassée (un fichier supprimé après retour) : le script sort non-zéro et imprime la diff — jamais un succès sur un ensemble différent.
    - `bash plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh && bash plugin/conductor/scripts/tests/test-runtime-registry.sh</automated>
  </verify>
  <done>`vf-calibrate` annonce explicitement sa nature en cours (propagation ou migration-runtime), et toute bascule proposée est précédée d'une preuve de réversibilité fichier à fichier — jamais un compte, jamais une affirmation non vérifiée.</done>
</task>

<task type="auto">
  <name>Tache 3 : coexistence sans hooks déclarée par FIDE (install + status) + synchro README (MIGR-05)</name>
  <files>plugin/conductor/scripts/check-artifact-fidelity.sh, plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh, plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh, README.md, README.fr.md</files>
  <read_first>
    - plugin/conductor/scripts/check-artifact-fidelity.sh (posé par le lot 1, étendu par ce lot — lire le fichier RÉEL tel qu'il existe à ce point du DAG, pas la version du plan 38-01 : les lots 2-5 ont pu faire évoluer le fichier voisin `vibeflow-update.sh`, mais `check-artifact-fidelity.sh` lui-même n'a été touché que par le lot 1 — vérifier son état actuel par lecture avant d'ajouter le nouveau mode)
    - plugin/_internal/vibeflow-update.sh:1907-1923 (`show_status()` : table simple module par module — le point d'ajout est APRÈS la boucle, jamais à l'intérieur, une ligne de déclaration globale n'est pas par-module)
    - 38-CONTEXT.md lignes 129-132 (« Contribution amont `kind: memory` dans `artifactLayout`… la perte est déclarée par FIDE » — précédent direct de FIDE servant de porte-voix pour une perte STRUCTURELLE plutôt que par-champ, exactement le rôle que MIGR-05 lui demande pour les hooks)
  </read_first>
  <action>
Étendre `check-artifact-fidelity.sh` avec un nouveau mode `--coexistence-report` (sans artefact en
argument — mode global, pas par-fichier) : lire `.planning/config.json` via
`runtime-registry.sh list-installed` (lot 6, tâche 1), pour chaque runtime installé AUTRE que
`claude`, imprimer une ligne `[fidelity-coexistence] <runtime> : opère SANS gouvernance de hooks
(aucun mécanisme équivalent mesuré à ce jour — cf. 38-CONTEXT.md)`. `installed` réduit à `["claude"]`
seul (ou registre absent) -> silence total (rien à déclarer). Exit 0 dans tous les cas où la lecture
a pu s'exécuter — une coexistence sans hooks n'est pas un échec du gate, c'est son objet, même
doctrine que le reste du gate.

Dans `vibeflow-update.sh`, `show_status()` (après la boucle 1911-1922, avant la fermeture 1923) :
appeler `check-artifact-fidelity.sh --coexistence-report` (résolu par la MÊME cascade que le reste
du fichier utilise déjà pour ce gate), relayer sa sortie TELLE QUELLE — jamais résumée, jamais
avalée.

Au point de couture d'install DÉJÀ établi par le lot 1 (FIDE, la bannière `[fidelity]`) : ajouter,
juste après, un appel au MÊME `--coexistence-report` — un opérateur qui installe voit la
coexistence déclarée AU MÊME endroit que la fidélité de champs, jamais un second rapport séparé.

Rejouer la découverte COMPLÈTE des suites, confirmer le nouveau total (2 suites neuves ajoutées par
ce plan : `test-runtime-registry.sh`, `test-verify-runtime-reversibility.sh` — dérivé PAR LA
COMMANDE, jamais supposé depuis le texte de ce plan) et corriger les deux README en conséquence.
  </action>
  <acceptance_criteria>
    - Fixture `config.json` avec `vf_runtimes.installed: ["claude","codex"]` : `check-artifact-fidelity.sh --coexistence-report` produit une ligne `[fidelity-coexistence] codex : opère SANS gouvernance de hooks` — capturé sur exécution réelle.
    - Fixture avec `installed: ["claude"]` seul : AUCUNE ligne `[fidelity-coexistence]` — silence vérifié explicitement (grep -c = 0).
    - `bash plugin/_internal/vibeflow-update.sh status` (sur un `TARGET_ROOT` de sonde avec coexistence fixture) contient la ligne de déclaration — même contenu qu'à l'install, capturé sur les DEUX exécutions et comparé.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` reflète le total réel (dérivé, pas recopié), reporté dans les deux README.
    - `bash scripts/check-version-sync.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh && bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>Un runtime coexistant sans hooks est déclaré par FIDE au MÊME endroit à l'install et au status, jamais enfoui dans un rapport séparé — README et gate de synchro version restent alignés sur le total réel de suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/config.json` (fichier de lab, potentiellement partagé/committé) -> runtime-registry.sh | une écriture malformée corromprait un fichier de configuration central lu par gsd-core à CHAQUE commande — l'écriture atomique (temp + mv) est la seule barrière |
| `runtime-registry.sh set-active` sans `--confirmed` -> disque | un appel automatisé (script, hook, agent pressé) qui omettrait `--confirmed` ne doit JAMAIS écrire par accident — c'est la garantie ADR-031 de ce lot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-16 | Tampering | clé racine `runtime` transformée en objet par erreur (casse le contrat gsd-core) | high | mitigate | acceptance criteria dédié (`typeof … === 'string'` vérifié par exécution réelle après chaque `set-active --confirmed`) |
| T-38-17 | Repudiation | bascule exécutée sans dry-run préalable montré à l'utilisateur | high | mitigate | `--confirmed` seul n'écrit jamais sans qu'un appel `--dry-run` distinct ait existé dans le flux documenté de `vf-calibrate` — ADR-031, testé explicitement (T tâche 1) |
| T-38-18 | Repudiation | preuve de réversibilité basée sur un COMPTE plutôt qu'un ensemble (masquerait une perte de fichier) | medium | mitigate | `comm -3`, jamais un comptage — acceptance criteria dédié avec fixture volontairement cassée |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | aucun paquet externe installé par ce plan |
</threat_model>

<verification>
1. `bash plugin/conductor/scripts/tests/test-runtime-registry.sh` sort 0.
2. `bash plugin/conductor/scripts/tests/test-verify-runtime-reversibility.sh` sort 0.
3. `bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh` sort 0 (mode `--coexistence-report` couvert).
4. `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0 (`show_status()` étendu).
5. `bash scripts/check-version-sync.sh` sort 0, README synchronisés sur le total réel de suites.
</verification>

<success_criteria>
- Le registre de runtime couvre les 3 cas réels sans casser le contrat gsd-core.
- `vf-calibrate` annonce explicitement sa nature (propagation vs migration-runtime) avant toute action.
- Toute bascule passe par dry-run -> confirmation -> écriture, jamais par défaut.
- La réversibilité est prouvée par ensemble de fichiers, jamais par un compte.
- La coexistence sans hooks est déclarée par FIDE à l'install ET au status.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-06-SUMMARY.md` avec : la preuve `comm -3` vide sur le scénario install→bascule→retour, la ligne `[fidelity-coexistence]` capturée à l'install ET au status, le total réel de suites après ce plan.
</output>

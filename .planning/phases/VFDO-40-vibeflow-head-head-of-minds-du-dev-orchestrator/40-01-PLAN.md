---
phase: 40-vibeflow-head-head-of-minds-du-dev-orchestrator
plan: 01
lot: L1 — exec-rename
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [HEAD-04]
files_modified:
  - README.md
  - README.fr.md
  - plugin/commands/vf-planning.md
  - plugin/conductor/scripts/check-overlaps.sh
  - plugin/conductor/scripts/tests/test-check-overlaps.sh
  - plugin/design-orchestrator/AGENT.md
  - plugin/dev-orchestrator/AGENT.md
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/references/_index.md
  - plugin/dev-orchestrator/references/docs-flow.md
  - plugin/dev-orchestrator/references/ingestion-flow.md
  - plugin/dev-orchestrator/references/intent-routing.md
  - plugin/dev-orchestrator/references/mission-contracts.md
  - plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/skills/vf-dev/SKILL.md
  - plugin/planning-core/SKILL.md
  - plugin/planning-core/references/gsd-handoff.md
  - plugin/software-architecture/rules/doc-research-before-debug.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "HEAD-04 — un grep récursif de `plugin/` hors `CHANGELOG.md` ne rend AUCUNE occurrence de l'ancien nom de l'agent routeur"
    - "HEAD-04 — `README.md` et `README.fr.md` nomment l'agent routeur `vibeflow-head`"
    - "HEAD-04 — la suite `test-dev-orchestrator.sh` rend ROUGE si un alias est réinjecté sous `plugin/`, et REVERT au vert après restauration — prouvé par mutation réellement exécutée (QUAL-01)"
    - "HEAD-04 — l'exemption `CHANGELOG.md` de la garde anti-alias est elle-même prouvée : un fichier `CHANGELOG.md` de fixture portant l'alias reste ignoré, un fichier voisin ne l'est pas"
    - "D-17 — le skill garde le nom `vf-dev` : aucun dossier `skills/vf-head/`, aucune commande utilisateur renommée"
    - "D-18 — multi-runtime livré par construction : le nom voyage par l'installeur existant, AUCUN changement de moteur d'install n'est fait, AUCUNE mesure de runtime neuve n'est ajoutée"
    - "Kernel intact — `team-kernel.md`, `driver-lock.sh`, `dag.sh`, `guard-driver-lock.sh` absents de `files_modified`"
  artifacts:
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  key_links:
    - "`check-overlaps.sh` KNOWN_PAIRS ↔ `test-check-overlaps.sh` T15/T16 : la clé de paire et la fixture `agent \"…\"` portent le MÊME nom, renommées dans le même commit"
    - "`skills/vf-dev/SKILL.md` ↔ `test-dev-orchestrator.sh` T4 (l.~1298) : la cible d'incarnation citée par le skill est exactement celle que l'alternation du test accepte"
    - "`plugin/dev-orchestrator/AGENT.md` frontmatter `name:` ↔ `check-agents.sh --strict` : le renommage ne casse ni `description`, ni `model`, ni `memory` (ADR-044)"
---

<!-- planner-discipline-allow: vibeflow-dev -->

<objective>
**Lot L1 `exec-rename` du DAG de mission** — ce PLAN.md est l'unique plan du lot L1.

Renommer l'agent routeur `vibeflow-dev` → `vibeflow-head` sur TOUTE la surface `plugin/` (hors
`CHANGELOG.md`) plus les deux README racine, puis faire naître la garde qui empêche l'alias de
revenir.

Purpose: HEAD-04 et le critère de succès n°1 de la Phase 40. Le renommage est mécanique ; ce qui
compte est la **garde** : sans un test capable de rendre rouge, un alias résiduel réapparaît au
premier copier-coller et personne ne le voit (leçon « une preuve doit pouvoir rendre rouge »,
Phase 39).

Output: 22 fichiers renommés (44 occurrences mesurées le 2026-09-15, re-mesurées au plan), un cas
de test `T36` anti-alias dans `test-dev-orchestrator.sh` avec sa mutation prouvée.

**Écart TRACER_MODE assumé.** Aucune tâche `type="tracer"` n'est émise dans cette phase. Un tracer
est une tranche verticale à travers les couches d'une pile applicative ; cette phase est de la
doctrine et des scripts de garde — il n'existe aucune pile à traverser, aucun « utilisateur qui
peut faire quelque chose de nouveau » après le lot. La forme tracer est honorée dans son ESPRIT :
L1 est le lot de tête, il touche toutes les surfaces que la phase modifie, et il porte une preuve
end-to-end réellement exécutable (mutation → rouge → restauration → vert). Forcer l'étiquette
`tracer` sur un renommage serait un mensonge d'étiquette.

**Réversibilité.** D-04 (le nom `vibeflow-head`) est coté **costly** : 22 fichiers plus deux gardes
portent le nom, un second renommage rejoue toute la surface. Costly = signalé, pas gaté — aucun
`checkpoint:decision` n'est inséré, la décision est déjà verrouillée au cadrage (arbitrage Samuel,
AskUserQuestion session principale, 2026-09-15).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-CONTEXT.md
@.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : renommer les 22 fichiers de la surface mesurée, dans un seul commit</name>

  <read_first>
    plugin/dev-orchestrator/AGENT.md (l.1-9, frontmatter + titre)
    plugin/dev-orchestrator/README.md (l.1-12 en-tête, l.298-312 section « Historique »)
    plugin/conductor/scripts/check-overlaps.sh (l.55-90, heredoc KNOWN_PAIRS et fonction present())
    plugin/conductor/scripts/tests/test-check-overlaps.sh (l.183-212, cas T15/T16)
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh (l.1294-1300, branche T4 d'incarnation)
    plugin/dev-orchestrator/skills/vf-dev/SKILL.md (intégral, 13 lignes)
  </read_first>

  <files>README.md, README.fr.md, plugin/commands/vf-planning.md, plugin/conductor/scripts/check-overlaps.sh, plugin/conductor/scripts/tests/test-check-overlaps.sh, plugin/design-orchestrator/AGENT.md, plugin/dev-orchestrator/AGENT.md, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/references/_index.md, plugin/dev-orchestrator/references/docs-flow.md, plugin/dev-orchestrator/references/ingestion-flow.md, plugin/dev-orchestrator/references/intent-routing.md, plugin/dev-orchestrator/references/mission-contracts.md, plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh, plugin/dev-orchestrator/skills/vf-dev/SKILL.md, plugin/planning-core/SKILL.md, plugin/planning-core/references/gsd-handoff.md, plugin/software-architecture/rules/doc-research-before-debug.md</files>

  <action>
Remplace l'ancien nom d'agent `vibeflow-dev` par `vibeflow-head` dans les 22 fichiers listés en
`<files>`, et dans eux seuls (D-04, D-17, arbitrage Samuel, AskUserQuestion session principale,
2026-09-15).

**Mesure d'abord, remplace ensuite.** Avant toute écriture, re-mesure la surface avec le binaire
système (jamais l'alias de shell, jamais `rtk` sans `rtk proxy`) : `/usr/bin/grep -rl vibeflow-dev
plugin/ README.md README.fr.md`. La liste attendue est de 25 chemins, dont 3 `CHANGELOG.md`
EXEMPTÉS (`plugin/conductor/CHANGELOG.md`, `plugin/dev-orchestrator/CHANGELOG.md`,
`plugin/software-architecture/CHANGELOG.md`) — soit 22 cibles, 44 occurrences. Si le compte diffère,
NE DEVINE PAS : aligne `<files>` sur la mesure et signale l'écart dans le SUMMARY.

**Les trois fichiers `CHANGELOG.md` ne sont jamais réécrits** — ce sont des archives datées, au même
titre que les six specs sous `docs/superpowers/specs/` et `docs/ADR.md`, tous hors périmètre.

**Cas particuliers, à traiter nominativement (le reste est une substitution littérale) :**

1. `plugin/dev-orchestrator/AGENT.md` — la clé `name:` du frontmatter (l.2) devient `vibeflow-head`
   et le titre H1 (l.9) suit. Ne touche NI `model:`, NI `effort:`, NI `memory:`, NI la structure du
   frontmatter : `check-agents.sh --strict` exige les trois. La refonte de la `description:` et du
   corps appartient au lot L4 (plan 40-03) — ici, on ne fait QUE substituer le nom.

2. `plugin/dev-orchestrator/README.md` l.306 — la ligne vit dans la section `## Historique` du
   README, qui est un journal daté DISTINCT du fichier `CHANGELOG.md` exempté. **Arbitrage rendu au
   plan :** cette ligne est réécrite, mais sans anachronisme et sans porter l'ancien littéral, pour
   que l'exemption de la garde reste UNE seule règle lisible (« le nom de fichier `CHANGELOG.md` »)
   et non une exception de section, intestable. Remplace la ligne par exactement :
   `- **v2.2.0** — câblage de l'ingestion (BRDG-01/BRDG-03) dans l'agent routeur du module (renommé`
   `vibeflow-head` en Phase 40) : doctrine` — puis laisse le reste du paragraphe inchangé. Les cinq
   autres occurrences du même fichier (l.4, 22, 43, 74, 149) sont des substitutions littérales.

3. `plugin/conductor/scripts/check-overlaps.sh` l.67 ET
   `plugin/conductor/scripts/tests/test-check-overlaps.sh` l.186-203 (T15/T16) — **renommés dans
   le MÊME commit**. Ce n'est pas un desserrage de garde : `check-overlaps.sh` ne perd aucune
   sévérité, sa clé de paire et la fixture `agent "…"` de ses tests désignent la même brique et
   doivent rester synchrones — les désynchroniser rendrait la frontière ADR-057 muette. Renomme la
   clé de paire ET la prose de la troisième colonne.

4. `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` l.~1297-1298 — le commentaire et
   l'alternation `vibeflow-dev|vf-dev-manager` de la branche T4 deviennent
   `vibeflow-head|vf-dev-manager`. Là encore, la cible change, pas la sévérité.

5. `plugin/dev-orchestrator/agents/vf-dev-manager.md` l.3 UNIQUEMENT (la phrase « Dispatché par
   l'agent … » de la `description:` du frontmatter). **Ce fichier est aussi touché par le lot L2
   (plan 40-02), sur une région strictement disjointe** : L2 réécrit le paragraphe de relais de la
   section `## Rapport de mission` (l.243-247). L1 est en vague 1, L2 en vague 2 : l'ordre
   séquentiel est garanti par les vagues, les deux touches ne se croisent jamais. **Ne touche à
   RIEN d'autre dans ce fichier** — il est à 250/250 lignes, marge zéro, et T35 (d) le gate.

6. `plugin/dev-orchestrator/skills/vf-dev/SKILL.md` — la cible d'incarnation change (l.3 et l.8),
   **le nom du skill reste `vf-dev`** (D-17) : aucun renommage de dossier, aucun alias `vf-head`
   (couche de synonymes interdite depuis v2.33.0). La `description:` doit conserver son déclencheur
   « Utiliser quand » et sa mention « Invocable », que T12 gate.

7. `plugin/planning-core/references/gsd-handoff.md` (l.31, 35, 43) — ce fichier était OMIS de la
   liste canonique de `40-CONTEXT.md` ; sa présence est confirmée par mesure directe. Il est dans
   le périmètre, il ne se saute pas.

**Aucun changement de moteur d'install (D-18).** L'installeur pose déjà `AGENT.md` sous le dossier
d'agents de la cible, avec le nom de fichier du MODULE : seul le contenu du frontmatter change, pas
le nom du fichier posé. Le renommage est donc multi-runtime **par construction**, sans mesure neuve
à ajouter. **Conséquence à écrire dans le SUMMARY** : sur un poste déjà équipé, le fichier d'agent
installé conserve l'ancien nom dans son frontmatter **tant qu'une mise à jour explicite n'a pas été
jouée sur ce poste** — même patron que les migrations précédentes de ce dépôt, où une livraison
n'atteint aucun poste déjà équipé sans un geste utilisateur. C'est une note de livraison, pas une
action de ce plan.

**Constat à consigner, jamais à corriger ici.** La fonction `present()` de `check-overlaps.sh`
résout un agent local par NOM DE FICHIER (`$AGENTS_DIR/<ref>.md`), alors que l'installeur pose
l'agent sous `agents/dev-orchestrator.md`. La frontière ADR-057 de cette paire est donc déjà
muette dans un lab réellement installé, AVANT cette phase — le renommage préserve exactement la
sémantique actuelle et n'introduit rien. Écris ce constat dans le SUMMARY, section « source à
amender » (G5 du kernel : consigné, jamais amendé en silence). **Ne modifie pas `present()`** :
ce serait toucher une garde du conductor hors périmètre.

Commit unique, en français, par pathspec (`git commit <les 22 chemins> -m …`), jamais `git add -A`.
Message : `refactor(40): vibeflow-dev devient vibeflow-head sur toute la surface plugin/ (HEAD-04,
D-04, D-17) — arbitrage Samuel, AskUserQuestion session principale, 2026-09-15`.
  </action>

  <verify>
    <automated>test "$(/usr/bin/grep -rl vibeflow-dev plugin/ README.md README.fr.md | /usr/bin/grep -v 'CHANGELOG\.md$' | wc -l | tr -d ' ')" = "0"</automated>
    <fails_when>La commande sort non-zéro parce que `wc -l` rend une valeur autre que `0` : au moins un fichier non-`CHANGELOG.md` de `plugin/` ou un des deux README racine porte encore l'ancien nom. Le nom du ou des fichiers fautifs s'obtient en relançant le `grep -rl` sans le `wc`.</fails_when>
    <automated>git diff --quiet HEAD~1..HEAD -- plugin/dev-orchestrator/CHANGELOG.md plugin/conductor/CHANGELOG.md plugin/software-architecture/CHANGELOG.md</automated>
    <fails_when>La commande sort non-zéro : le commit de cette tâche a touché au moins un des trois `CHANGELOG.md` exemptés, alors qu'ils sont des archives datées non réécrites par ce lot. Immutabilité bornée à CE commit (`HEAD~1..HEAD`), pas au contenu absolu : un lot ultérieur légitime (L3, clôture de module) pourra écrire `vibeflow-head` dans un CHANGELOG sans faire rougir ce plan, puisque ce sera un autre commit.</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-overlaps.sh</automated>
    <fails_when>La suite sort non-zéro et imprime une ligne `✗ T15 …` ou `✗ T16 …` : la clé de paire de `check-overlaps.sh` et la fixture `agent "…"` de ses tests ne portent plus le même nom, la frontière ADR-057 est devenue muette.</fails_when>
    <automated>bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/dev-orchestrator/AGENT.md</automated>
    <fails_when>Le gate sort non-zéro : le frontmatter de `AGENT.md` a perdu `description`, `model` ou `memory` pendant la substitution (ADR-044). Un exit 3 signifie cible vide — tout aussi rouge qu'un exit 1.</fails_when>
    <automated>bash scripts/check-machine-paths.sh</automated>
    <fails_when>Le gate sort non-zéro en nommant `fichier:ligne` : un chemin absolu de machine a été introduit dans un fichier versionné pendant l'édition.</fails_when>
  </verify>

  <acceptance_criteria>
    - Les 22 fichiers de `<files>` ne portent plus l'ancien nom ; les 3 `CHANGELOG.md` sont inchangés — le diff du commit de cette tâche, pris sur la plage explicite `<sha>^..<sha>`, ne les nomme pas.
    - `plugin/dev-orchestrator/AGENT.md` porte `name: vibeflow-head` et conserve `model`, `effort`, `memory`.
    - Aucun dossier `plugin/dev-orchestrator/skills/vf-head/` n'existe ; `skills/vf-dev/` est intact (D-17).
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` fait toujours exactement 250 lignes (`awk 'END{print NR}'`).
    - Le diff du commit de cette tâche, pris sur la plage explicite `<sha>^..<sha>`, ne nomme AUCUN de : `plugin/conductor/references/team-kernel.md`, `plugin/conductor/scripts/driver-lock.sh`, `plugin/conductor/scripts/dag.sh`, `plugin/conductor/scripts/guard-driver-lock.sh`.
    - Un seul commit, message en français, portant la traçabilité d'arbitrage datée.
  </acceptance_criteria>

  <reversibility rating="costly">D-04 — 22 fichiers et deux gardes portent le nom ; un second renommage rejoue l'intégralité de la surface. Signalé, non gaté : la décision est verrouillée au cadrage.</reversibility>

  <done>`/usr/bin/grep -rl vibeflow-dev plugin/ README.md README.fr.md` ne rend que les 3 `CHANGELOG.md` exemptés ; `test-check-overlaps.sh` et `check-agents.sh --strict` sur l'AGENT.md sont verts.</done>
</task>

<task type="auto">
  <name>Task 2 : garde anti-alias T36 dans test-dev-orchestrator.sh, avec sa mutation prouvée</name>

  <read_first>
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh (l.130-160 résolution MOD/REPO et helpers ok/ko/skip ; l.152-156 forçage de `$GREP` sur le binaire système ; l.225-244 conventions `DELETED_RE`/`deleted_hits()` de frontière de mot ; bloc T35 en fin de fichier, patron de mutation réel-vs-mutant)
  </read_first>

  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>

  <action>
Ajoute un bloc de cas `T36` à la fin de `test-dev-orchestrator.sh`, juste avant la ligne de
résultat final, et déclare-le dans l'en-tête documentaire du fichier au même gabarit que `T35`.

`T36` répond à une seule question : **un alias de l'ancien nom d'agent a-t-il réapparu sous
`plugin/` ?** (HEAD-04, critère de succès n°1).

**Construction — aucun analogue exact n'existe dans ce dépôt.** Le précédent le plus proche
(`T13` « la façade est morte ») est borné à une liste de fichiers curatée, pas à un parcours
`find` de l'arbre. Seules les CONVENTIONS du fichier se réutilisent, pas sa structure :
le forçage `GREP="$(command -v grep)"` déjà défini en tête de fichier (l.155) — n'en redéfinis
pas un second —, la discipline de frontière de mot des l.230-244, et les accumulateurs `ok`/`ko`.

**Factorise la détection dans une fonction de portée fichier, jamais dans un sous-shell de bloc**
(même règle que `deleted_hits()`), nommée `t36_alias_hits`, prenant une racine en argument et
imprimant la liste des fichiers fautifs, vide sinon. Elle parcourt la racine par `find <racine>
-type f`, exclut tout fichier dont le nom de base est `CHANGELOG.md`, et cherche l'ancien nom
d'agent avec `"$GREP"` et une frontière de non-mot en fin de motif — pour la même raison que
`vf-dev` ≠ `vf-dev-manager` : une sous-chaîne nue n'est pas un nom.

**Trois assertions, dans cet ordre.**

(a) **Portée et disponibilité.** `T36` n'a de sens que sur l'arbre source : en lab installé, il
n'existe pas d'arbre `plugin/`. Détermine la racine à partir de `$REPO` (déjà résolu en tête de
fichier comme le parent du module) et n'exécute le cas que si `$REPO` porte bien les marqueurs de
l'arbre de plugins — l'existence simultanée des dossiers `conductor`, `dev-orchestrator` et
`_internal` sous `$REPO`. Sinon, `skip` avec un message qui NOMME la raison. Jamais un `ok`
silencieux sur un arbre absent.

(b) **Cas nominal.** `t36_alias_hits "$REPO"` doit rendre une sortie VIDE. Sinon `ko` en nommant
au plus trois fichiers fautifs.

(c) **Mutation, permanente et interne à la suite (QUAL-01).** Construis une fixture jetable par
`mktemp -d` avec un `trap` de nettoyage, contenant exactement deux fichiers :
`archive/CHANGELOG.md` et `vif/doctrine.md`, tous deux porteurs de l'ancien nom d'agent. Applique
`t36_alias_hits` à la fixture et exige DEUX propriétés dans la même assertion : la sortie nomme
`vif/doctrine.md` (la garde sait rendre rouge) ET elle ne nomme PAS `archive/CHANGELOG.md`
(l'exemption est réelle, pas un laissez-passer généralisé). Si la sortie de la fixture est vide,
c'est un `ko` explicite « mutant NON OPPOSABLE » — patron `T35`.

Ne touche à aucun autre cas de la suite. N'assouplis aucun seuil existant. Le compteur final
`== résultat : … ==` et la ligne `[ "$fail" -eq 0 ]` restent en dernière position.

Mets aussi à jour la liste des cas de la section `## Tests` du README du module ? **Non** — ce
geste appartient à la clôture du module, portée par le plan 40-04 (lot L3), qui possède
`plugin/dev-orchestrator/README.md` en vague 3. Ne l'anticipe pas ici, tu créerais un conflit de
périmètre.

Commit par pathspec, en français : `test(40): garde anti-alias T36 avec sa mutation prouvée
(HEAD-04, QUAL-01)`.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>La suite sort non-zéro et la dernière ligne annonce un compte `KO` supérieur à 0 — au moins un cas, `T36` compris, a échoué. Le compte exact `OK`/`SKIP` (base + cas `T36` ajoutés) n'est pas figé ici : à re-mesurer à l'exécution, jamais recopié depuis ce plan.</fails_when>
    <automated>f=plugin/dev-orchestrator/references/docs-flow.md; b="$(mktemp)"; cp "$f" "$b"; printf '\n<!-- mutation QUAL-01 : vibeflow-dev -->\n' >> "$f"; out="$(bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1)"; mut=$?; cp "$b" "$f"; rm -f "$b"; bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh >/dev/null 2>&1; sain=$?; echo "mut=$mut sain=$sain"; [ "$mut" -ne 0 ] && [ "$sain" -eq 0 ] && printf '%s' "$out" | "$(command -v grep)" -q '✗ T36'</automated>
    <fails_when>La commande imprime `mut=0` (un alias réellement réinjecté sous `plugin/` laisse la suite VERTE : la garde est incapable d'échouer, QUAL-01 non satisfait) ou `sain=` une valeur non nulle (la restauration ne rend pas le vert), ou l'assertion finale échoue faute du témoin `✗ T36` dans la sortie mutante : la suite est bien rouge, mais rien ne prouve que c'est `T36` qui a mordu — un cas voisin qui attraperait l'alias injecté rendrait `mut != 0` vrai avec `T36` resté vacant (constat MAJEUR du plan-check 2026-09-15, capture de sortie + témoin nominatif conjoints à l'assertion, pas une simple lecture de code de sortie).</fails_when>
    <automated>test -z "$(git status --porcelain plugin/dev-orchestrator/references/docs-flow.md)"</automated>
    <fails_when>La commande sort non-zéro : la mutation vive de la vérification précédente n'a pas été restaurée et `docs-flow.md` reste modifié dans l'arbre de travail. Mesure prise par capture en variable, jamais par `| wc -l` (une sortie vide sous un hook `rtk` actif rendrait 1 ligne au lieu de 0).</fails_when>
  </verify>

  <acceptance_criteria>
    - `test-dev-orchestrator.sh` déclare `T36` dans son en-tête documentaire et l'exécute.
    - `t36_alias_hits` est définie au niveau du fichier (pas dans un sous-shell de bloc) et utilise le `$GREP` déjà forcé sur le binaire système, sans en redéfinir un second.
    - La mutation interne prouve les DEUX propriétés : détection dans un fichier ordinaire, exemption effective d'un `CHANGELOG.md`.
    - La mutation VIVE (injection réelle sous `plugin/`, exécution, restauration, ré-exécution) a été jouée et ses deux codes de sortie sont recopiés verbatim dans le SUMMARY.
    - Aucun seuil ni aucune assertion préexistante n'a été affaibli : le diff du commit de cette tâche, pris sur la plage explicite `<sha>^..<sha>`, est purement additif sur ce fichier, hors la ligne l.~1298 renommée en Task 1.
  </acceptance_criteria>

  <done>La suite du module est verte sur l'arbre sain, rouge sur un arbre où un alias a été réinjecté, et verte à nouveau après restauration — les trois codes de sortie sont consignés dans le SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt → arbre de travail du développeur | une substitution de masse écrite par un agent traverse ici : une substitution trop large abîme du texte qu'aucune revue ne relit |
| suite de tests → verdict de CI | un cas de test incapable d'échouer traverse ici et devient un vert permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | substitution de masse sur 22 fichiers | medium | mitigate | Périmètre nominatif en `<files>` + re-mesure obligatoire avant écriture + commit par pathspec (jamais `git add -A`) ; `check-machine-paths.sh` en vérification. |
| T-40-02 | Repudiation | cas `T36` vert par construction | high | mitigate | Double mutation : fixture interne permanente (détection + exemption) ET mutation vive exécutée dont les codes de sortie sont recopiés au SUMMARY (QUAL-01). |
| T-40-03 | Denial of Service | `find` récursif sur l'arbre de plugins à chaque run de suite | low | accept | Arbre de quelques milliers de fichiers, coût mesuré en fraction de seconde ; le cas se `skip` explicitement hors arbre source. |
| T-40-04 | Tampering | desserrage d'une garde dans le commit qu'elle autorise | medium | mitigate | `check-overlaps.sh`/ses tests et la branche T4 sont des renommages de CIBLE, pas des baisses de sévérité — vérifié par le diff nominal du commit sur plage explicite `<sha>^..<sha>`, et par le vert de `test-check-overlaps.sh`. |
| T-40-SC | Tampering | installs npm/pip/cargo | n/a | accept | Aucun paquet installé par cette phase — `40-RESEARCH.md` §Package Legitimacy Audit : sans objet, aucune dépendance externe neuve. |
</threat_model>

<verification>
- `/usr/bin/grep -rl vibeflow-dev plugin/ README.md README.fr.md` ne rend que les 3 `CHANGELOG.md`.
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` — vert, `0 KO`, `0 SKIP`.
- `bash plugin/conductor/scripts/tests/test-check-overlaps.sh` — vert.
- `bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/dev-orchestrator/AGENT.md` — vert.
- `bash scripts/check-machine-paths.sh` — vert.
- Mutation vive jouée, les deux codes de sortie consignés au SUMMARY.
</verification>

<success_criteria>
- HEAD-04 tenu sur la surface `plugin/` et sur les deux README racine.
- La garde anti-alias existe ET a démontré qu'elle sait rendre rouge.
- Le skill `vf-dev` a gardé son nom ; aucun alias `vf-head` n'existe.
- Kernel intact : aucun des quatre fichiers kernel n'apparaît au diff du lot, pris sur la plage explicite allant du point de départ du lot à son dernier commit.
</success_criteria>

<artifacts_produced>
- Cas de test `T36` (fonction `t36_alias_hits` + trois assertions) dans `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`.
- Constat consigné (pour amendement ultérieur, jamais corrigé ici) : `present()` de `check-overlaps.sh` résout un agent local par nom de fichier, ce qui rend la frontière ADR-057 de cette paire déjà muette en lab installé.
</artifacts_produced>

<output>
Create `.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-01-SUMMARY.md` when done
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 11
lot: L11 — bump patch dérivé, CHANGELOG et historiques README, publication et merge SANS contournement de la PR des preuves
type: execute
wave: 11
depends_on: ["41-10"]
autonomous: false
requirements: [PROT-02]
files_modified:
  - VERSION
  - plugin/.claude-plugin/plugin.json
  - plugin/.codex-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - README.fr.md
  - CHANGELOG.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Numéro de release DÉRIVÉ à l'exécution : patch suivant de la `VERSION` alors sur `origin/main` (hotfix v2.63.2 compris s'il est mergé), appliqué par `scripts/bump.sh` ; jamais écrit en dur dans ce plan (D-M8 : durcissement = patch)"
    - "La PR des preuves ne touche aucun chemin CODEOWNERS, ni `plugin/dev-orchestrator/`, ni `.github/workflows/ci.yml`, ni `scripts/check-release-tag.sh`, ni `scripts/hooks/` : elle doit être mergeable sans revue et sans contournement (D-M5, D-M9)"
    - "Rejeu du flux de release, première moitié (critère 3) : PR ouverte, CI verte, état `CLEAN`, merge `--merge` SANS `--admin` ni case de contournement ; un état `BLOCKED` arrête le plan, il n'est jamais contourné"
    - "L'entrée CHANGELOG et les historiques README n'associent O-3 à aucun mot d'achèvement ; le SUMMARY de ce plan est committé sur B AVANT la publication"
  artifacts:
    - path: "VERSION"
      provides: "version patch dérivée, synchronisée par `bump.sh`"
    - path: "CHANGELOG.md"
      provides: "entrée de release de la Phase 41"
  key_links:
    - "Le merge de la PR des preuves est relu et consigné par 41-12 sur une troisième branche (D-03)"
---

<objective>
Préparer la release de la phase sur la branche B et la faire entrer sur `main` par le chemin normal,
sans aucun contournement.

Purpose: PROT-02, première moitié du rejeu du flux de release sous la règle.
Output: PR des preuves mergée ; B entrée sur `main` sans contournement.

Budget (point 9 du tour 1) : seconde moitié de l'ancien 41-10, scindé pour tenir sous dix fichiers ;
deux tâches, sept fichiers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@CLAUDE.md

Extraits à lire : `scripts/bump.sh` (en-tête : sources de version, squelette CHANGELOG) ; `README.md`
et `README.fr.md` § Versioning (table d'historique, première ligne de données) ; `CHANGELOG.md` en
tête (dernière entrée `## [v…]`).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : bump patch dérivé, CHANGELOG et historiques README, périmètre de la PR, rejeu, SUMMARY avant publication</name>
  <files>VERSION, plugin/.claude-plugin/plugin.json, plugin/.codex-plugin/plugin.json, .claude-plugin/marketplace.json, README.md, README.fr.md, CHANGELOG.md</files>
  <read_first>
    - scripts/bump.sh (en-tête et squelette CHANGELOG)
    - CHANGELOG.md (dernière entrée `## [v…]`, pour le ton et la structure)
    - README.md et README.fr.md § Versioning
  </read_first>
  <action>
Identité `samuel-neveugall` ; branche B ; `git fetch origin` ; si `origin/main` a bougé depuis 41-10,
`git merge --no-edit origin/main` en local.

Dériver la version : `V` = contenu de `VERSION` sur `origin/main` sans préfixe ; `N` = `X.Y.(Z+1)`.
Lancer `bash scripts/bump.sh "$N"`. Remplacer le commentaire de squelette inséré par `bump.sh` dans
la nouvelle entrée `## [vN]` par un résumé en français : rulesets de branche et de tags posés et
mesurés (refus par défaut d'une PR rouge et sa provenance, trace d'un contournement sur des checks en
cours, refus serveur d'un push direct, M-1 à M-4, décisions humaines éventuelles), CODEOWNERS étroit
prouvé sur `.github/`, la baseline et une sentinelle, ADR-072, amendement d'ADR-059, statut d'O-3
« gardée par défaut + tracée », aucun gate créé (QUAL-01, D-M11), commits couverts depuis le tag
précédent (liste `git log --oneline v<V>..HEAD`), numéros de PR (phase, PR-P). Ajouter une première
ligne de données à la table d'historique de `README.md` (anglais) et de `README.fr.md` (français)
pour `vN`, date d'exécution, résumé court en gras.

Contrôler le périmètre de la future PR (`git diff --name-only origin/main...HEAD`). Rejouer `gates`
et `tests`. Commit local sur B, message « chore(41-11): release v<N> — bump patch dérivé, CHANGELOG
et historiques README ». Rédiger et committer `41-11-SUMMARY.md` sur B À LA FIN de cette tâche : il
annonce la Task 2 (publication et merge humains), dont le résultat sera relu par 41-12.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'TV=$(mktemp -d); git show origin/main:VERSION > "$TV/v"; V=$(tr -d "v[:space:]" < "$TV/v"); case "$V" in *.*.*) ;; *) echo "VERSION origin/main illisible"; exit 1;; esac; N="${V%.*}.$(( ${V##*.} + 1 ))"; B=$(tr -d "v[:space:]" < VERSION); rc=0; bash scripts/check-version-sync.sh > /dev/null 2>&1 || rc=$?; e=$(awk -v t="## [v$N]" "index(\$0, t)==1 {c++} END{print c+0}" CHANGELOG.md); T=$(mktemp -d); awk -v t="## [v$N]" "index(\$0, t)==1 {f=1; next} /^## \\[v/ {f=0} f" CHANGELOG.md > "$T/e"; sq=$(awk "/<!--/ {c++} END{print c+0}" "$T/e"); tx=$(awk "NF {c++} END{print c+0}" "$T/e"); r1=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.md); r2=$(awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}" README.fr.md); f1=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); f2=$(awk "/^\\|---------\\|------/ {getline; print; exit}" README.fr.md | awk -v t="| \`v$N\` |" "index(\$0, t)==1 {c++} END{print c+0}"); echo "bump : origin_main=$V attendu=$N branche=$B version_sync_rc=$rc entree_changelog=$e commentaire_squelette_restant=$sq lignes_non_vides_entree=$tx readme_en=$r1 readme_fr=$r2 premiere_ligne_en=$f1 premiere_ligne_fr=$f2"; [ "$B" = "$N" ] && [ "$rc" -eq 0 ] && [ "$e" -eq 1 ] && [ "$sq" -eq 0 ] && [ "$tx" -ge 3 ] && [ "$r1" -eq 1 ] && [ "$r2" -eq 1 ] && [ "$f1" -eq 1 ] && [ "$f2" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `branche=` différent du patch suivant de la `VERSION` d'`origin/main` (numéro écrit en dur, ou `main` a bougé : refaire la dérivation après merge d'`origin/main`) ; ou `version_sync_rc=` différent de 0 ; ou entrée CHANGELOG absente ou dupliquée ; ou commentaire de squelette restant ; ou entrée vide ; ou ligne d'historique absente, dupliquée ou pas en première position dans un des deux README</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; if git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p"; then echo "CHEMIN CODEOWNERS $p"; co=$((co+1)); fi; done < "$T/f"; z=$(awk "index(\$0, \"plugin/dev-orchestrator/\")==1 || \$0==\".github/workflows/ci.yml\" || \$0==\"scripts/check-release-tag.sh\" || index(\$0, \"scripts/hooks/\")==1 {c++} END{print c+0}" "$T/f"); n=$(awk "END{print NR}" "$T/f"); echo "perimetre PR des preuves : fichiers=$n chemins_codeowners=$co chemins_interdits=$z"; [ "$n" -gt 0 ] && [ "$co" -eq 0 ] && [ "$z" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou un `CHEMIN CODEOWNERS` (la PR exigerait la revue `@picmakpro` : le rejeu « sans contournement » deviendrait dépendant d'une approbation) ; ou `chemins_interdits=` différent de 0 (zone interdite, CI, gate de release ou hooks touchés — D-M5, D-M9)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 CHANGELOG.md README.md README.fr.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-11-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); echo "avant publication : summary_committe=$s modifications_non_committees=$d"; [ "$s" = oui ] && [ "$d" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY non committé sur B ; ou modifications suivies non committées (elles n'entreraient pas dans la PR)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `VERSION` = patch suivant de la `VERSION` d'`origin/main`, synchronisée partout (`check-version-sync` rc 0)
    - entrée CHANGELOG rédigée (sans squelette), ligne d'historique en tête des deux README, aucune association d'O-3 à un mot d'achèvement
    - PR des preuves hors CODEOWNERS, hors zone interdite, hors CI, gate de release et hooks
    - rejeux verts ; SUMMARY 41-11 committé ; arbre propre ; messages de commit conformes
  </acceptance_criteria>
  <done>La branche B est prête à être publiée et mergée par le chemin normal.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — publier B, ouvrir la PR des preuves, attendre la CI, merger SANS contournement</name>
  <action>Samuel, compte `samuel-neveugall`, publie B, ouvre la PR, attend la CI, vérifie l'état `CLEAN` et merge sans aucune option de contournement.</action>
  <instructions>
Compte qui agit : `samuel-neveugall`.

1. `git push -u origin feat/phase-41-preuves-release`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head feat/phase-41-preuves-release --title "Phase 41 — preuves de la protection du dépôt et release v<N>" --body "Preuves (41-PREUVES.md), statut O-3 gardée par défaut + tracée, ADR-072 § Mesures, BACKLOG traité par renvoi, bump patch. Rejeu du flux de release sous la règle, sans contournement."`
   → noter `<n>` (`<N>` = version imprimée par la Task 1). Ce corps ne cite aucun arbitrage D-01..D-10 ;
   s'il en cite un, il porte « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » (relu par 41-12).
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tout vert attendu).
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus,reviewDecision --jq '{mergeStateStatus, reviewDecision}'`
   - `CLEAN` : passer à 5.
   - `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`, puis refaire 3 et 4.
   - `BLOCKED` ou autre : NE PAS merger, NE PAS contourner ; copier la sortie et s'arrêter.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) → copier la sortie.
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
Ne pas poser le tag ici : c'est le plan 41-12.
  </instructions>
  <verification>Le plan 41-12 relit l'état `MERGED`, le corps de la PR, vérifie dans les rule suites que le merge n'a laissé aucune suite `bypass`, et consigne `PR-RELEASE:`.</verification>
  <resume-signal>Coller `<n>`, les sorties de 3 (fin), 4 (toutes les lectures), 5 et son témoin, puis écrire « PR des preuves mergée sans contournement ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR des preuves → `main` | premier merge normal sous la règle : doit passer sans contournement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-39 | Tampering | numéro de release figé ou périmé | medium | mitigate | dérivé d'`origin/main` après merge local ; comparé au patch suivant ; `check-version-sync` rc 0 |
| T-41-40 | Repudiation | rejeu « vert » obtenu par contournement | high | mitigate | périmètre hors CODEOWNERS vérifié ; état `CLEAN` exigé ; `--admin` exclu ; absence de suite `bypass` vérifiée en 41-12 |
</threat_model>

<verification>
Bump dérivé et synchronisé ; PR des preuves mergée par le chemin normal.
</verification>

<success_criteria>
Première moitié du critère 3 : bump → PR → merge sous la règle, sans contournement.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-11-SUMMARY.md` — committé sur la branche B à la fin de la Task 1, AVANT la publication (Task 2).
</output>

---
phase: 29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/research/2026-08-15-investigation-dag-scope.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ICMD-01, ICMD-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un lecteur qui ouvre le rapport d'investigation sait, sans relire dag.sh, quelles parties du mécanisme --scope sont intouchables, lesquelles sont extensibles, et pourquoi G1 se livre sans y toucher (D-03)."
    - "La suite test-dag.sh est verte AVANT tout autre geste de la phase — la baseline de non-régression est constatée par exécution, jamais présumée."
    - "Les 12 exigences ICMD-01..ICMD-12 existent au ledger et chacune pointe le plan qui la porte."
    - "L'imprécision de citation historique relevée par 29-RESEARCH.md §Pitfall 1 ne se propage dans aucun artefact produit par cette phase."
  artifacts:
    - reports/research/2026-08-15-investigation-dag-scope.md
    - ".planning/REQUIREMENTS.md — section « Hors-milestone — Phase 29 » + 12 lignes de traçabilité + note de couverture"
  key_links:
    - "Rapport d'investigation → 29-RESEARCH.md §Investigation (source sourcée fichier:ligne et commits rejoués) : le rapport CONSOLIDE, il ne ré-investigue pas."
    - "Verdict « voie doctrine suffit » du rapport → plan 29-03 (G1) : c'est ce verdict qui autorise 29-03 à ne pas toucher dag.sh."
    - "Ledger ICMD-01..12 → champs requirements: des 5 plans de la phase (union = ledger, 0 inventé, 0 orphelin)."
  prohibitions:
    - "PROHIBÉ — modifier plugin/conductor/scripts/dag.sh ou plugin/conductor/scripts/tests/test-dag.sh (D-03). Vérifiable : git diff --name-only sur ces deux chemins doit rendre vide à la fin du plan."
    - "PROHIBÉ — restructurer .planning/ (ADR-055, GSD propriétaire). Seul REQUIREMENTS.md est amendé, en ajout ; aucun fichier .planning/ n'est déplacé, renommé ni supprimé."
    - "PROHIBÉ — inscrire G4 (lab-starters clonables) ou un gain secondaire du rapport source au ledger de cette phase (D-01, D-02). Aucune des 12 exigences ne les mentionne comme livrable."
    - "PROHIBÉ — adopter la méthodologie externe comme label ou vocabulaire dans un artefact distribué sous plugin/."
---

<objective>
Poser le socle de la Phase 29 : la **baseline de non-régression** du mécanisme `--scope` constatée
par exécution, le **livrable d'investigation** que D-03 exige comme précondition transverse, et le
**registre d'exigences** `ICMD-01..ICMD-12` sans lequel les quatre autres plans n'ont rien à citer.

Purpose : D-03 verrouille « zéro régression autorisée sur `dag.sh --scope` » et fait de
l'investigation un **livrable à part entière**, pas un préambule jetable. Ce plan la sort de
`29-RESEARCH.md` (artefact de planification, non distribué, périssable) vers `reports/research/`
(artefact durable, lisible par un futur mandat qui frôlerait le socle). Il grave aussi la
correction factuelle établie par la recherche — l'origine du mécanisme est en Phase 20, pas là où
le cadrage la situait (D-03, 29-RESEARCH.md §Pitfall 1) — pour qu'aucun artefact produit par cette
phase n'envoie un lecteur sur le mauvais `*-CONTEXT.md`.

D-01 (G4 hors phase) et D-02 (gains secondaires hors phase) bornent le ledger : les 12 exigences
créées ici ne couvrent que G1, G2, G3, G5 et l'investigation. Rien d'autre n'entre.

Output : `reports/research/2026-08-15-investigation-dag-scope.md` + la section Phase 29 de
`.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-CONTEXT.md
@.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 29-01)

| Artefact | Type | Statut |
|---|---|---|
| `reports/research/2026-08-15-investigation-dag-scope.md` | rapport durable | NOUVEAU |
| `.planning/REQUIREMENTS.md` §« Hors-milestone — Phase 29 » | section de ledger | NOUVEAU (ajout) |
| `ICMD-01` … `ICMD-12` | identifiants d'exigence | NOUVEAUX (12) |
| 12 lignes de la table `## Traceability` de `REQUIREMENTS.md` | lignes de ledger | NOUVELLES |
| Note de couverture Phase 29 sous `**Coverage:**` | paragraphe | NOUVEAU |

Aucun symbole exécutable, aucun flag CLI, aucun script n'est créé par ce plan.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Tâche 1 : Baseline verte + rapport d'investigation `--scope` de bout en bout</name>

  <precondition>
    `bash plugin/conductor/scripts/tests/test-dag.sh` rend 0 sur l'arbre de travail AVANT toute
    écriture de ce plan. Si la suite est rouge au départ, HALTE : la baseline de non-régression que
    D-03 exige n'existe pas, et aucun geste de la Phase 29 ne peut être attesté « sans régression ».
  </precondition>

  <files>reports/research/2026-08-15-investigation-dag-scope.md</files>

  <read_first>
    - `.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-RESEARCH.md`
      §« Investigation `dag.sh --scope` — précondition transverse » (l.341-448) — **la source
      intégrale** : historique par commit, inventaire des consommateurs, couverture T13-T33,
      verdict intouchable/extensible, voie recommandée pour G1. Le rapport CONSOLIDE ce contenu,
      il ne relance aucune investigation.
    - `.planning/phases/VFDO-29-.../29-RESEARCH.md` §« Common Pitfalls » → Pitfall 1 — la forme de
      citation historique fautive à ne jamais reproduire, et la forme corrigée à employer.
    - `.planning/phases/VFDO-29-.../29-CONTEXT.md` §Decisions → **D-03** (zéro régression, clause de
      repli « doctrine seule / manager rédige ») et §Canonical References (les 5 surfaces du
      mécanisme).
    - `.planning/phases/VFDO-29-.../29-PATTERNS.md` §« Livrable investigation `dag.sh --scope` » —
      absence d'analog code assumée, forme finale laissée au plan.
    - `plugin/conductor/scripts/dag.sh` l.150-196 (`build_ready_manifest()`, `compute_stages()`) et
      l.222-224, l.285-306 (`add`, `status --frozen`) — **en LECTURE SEULE**, pour vérifier que les
      lignes citées par le rapport correspondent bien à l'état courant du fichier.
    - `reports/research/2026-08-15-icm-deep-search.md` §5 (G1) — ce que le « négatif du périmètre »
      doit produire, pour que le verdict du rapport y réponde explicitement.
  </read_first>

  <action>
    Exécuter d'abord `bash plugin/conductor/scripts/tests/test-dag.sh` et consigner le verdict
    (nombre de cas, code de sortie) — c'est la baseline exigée par D-03 et par la §Wave 0
    Requirements de `29-VALIDATION.md`.

    Écrire ensuite `reports/research/2026-08-15-investigation-dag-scope.md`, rapport autonome et
    durable (un lecteur qui ne dispose ni de `29-RESEARCH.md` ni du contexte de session doit
    pouvoir s'en servir). Cinq sections obligatoires, dans cet ordre :

    1. **Reconstitution historique en deux phases, par commit** — l'état antérieur (aucune
       occurrence du champ de périmètre dans `dag.sh`), l'introduction de la déclaration du
       périmètre, puis l'ajout du calcul de disjonction et la fermeture de la faille de résolution
       de chemin. Chaque étape porte son SHA court, sa date et la commande git qui la rejoue.
       Employer la forme de citation **corrigée** établie par `29-RESEARCH.md` §Pitfall 1 — la
       phase d'origine du mécanisme et le fichier `*-CONTEXT.md` où vit réellement sa décision —
       et signaler en une phrase que le cadrage amont situait l'origine ailleurs, sans reproduire
       la chaîne fautive elle-même (les identifiants de décision sont scopés par fichier, jamais
       globaux : c'est la cause du piège).
    2. **Inventaire exhaustif des consommateurs** — table `Consommateur | fichier:ligne | Nature
       (écriture / lecture tolérante / câblage / doctrine) | Citation`. Y inclure les deux objets
       homonymes à ne pas confondre (la clé `isolation:` du frontmatter, et le script d'inventaire
       des recouvrements de briques tierces), avec le motif de la confusion.
    3. **Couverture de la suite de tests** — table `Cas | Ce qu'il prouve` sur les cas qui touchent
       le périmètre, et le verdict de couverture (aucun trou visible, ou la liste des trous).
    4. **Verdict INTOUCHABLE / EXTENSIBLE SANS RISQUE** — deux listes fermées, chaque entrée
       portant son motif (interdiction en commentaire de code, garde par test de mutation, contrat
       de sortie octet-exact, distinction sémantique dont dépend la doctrine du manager).
    5. **Voie retenue pour G1** — établir que le « négatif du périmètre » se compose intégralement
       de deux champs **déjà émis** (le périmètre déclaré du nœud lui-même, threadé dans son
       digest ; et les périmètres gelés des autres nœuds en vol), donc que la clause de repli de
       D-03 est non seulement sûre mais **suffisante** : zéro ligne de `dag.sh` à toucher. Clore
       par une **clause de halte** opposable : si un futur besoin réclame un négatif
       machine-vérifié, il suit la discipline de câblage vers l'amont (sous-processus), jamais une
       réimplémentation locale — et cela sort du périmètre de la Phase 29.

    Le rapport ne modifie **rien** sous `plugin/`. Il ne réécrit pas `29-CONTEXT.md` (artefact de
    cadrage, propriété de l'utilisateur) : la correction de citation vit dans le rapport, pas dans
    le cadrage.

    Vérifier enfin, avant de committer, que la suite `test-dag.sh` est toujours verte et que
    `plugin/conductor/scripts/dag.sh` est absent du diff.
  </action>

  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-dag.sh; echo "rc=$?"</automated>
    <automated>test -f reports/research/2026-08-15-investigation-dag-scope.md &amp;&amp; grep -c "27/D-13" reports/research/2026-08-15-investigation-dag-scope.md; echo "rc=$?"</automated>
    <automated>git diff --name-only -- plugin/conductor/scripts/dag.sh plugin/conductor/scripts/tests/test-dag.sh | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-dag.sh` sort **0** et affiche **0 échec** — avant
      écriture (baseline) ET après commit (non-régression).
    - `reports/research/2026-08-15-investigation-dag-scope.md` existe et porte les **5 sections**
      listées dans l'action, repérables par leurs titres de niveau 2.
    - `grep -c "27/D-13" reports/research/2026-08-15-investigation-dag-scope.md` rend **0** (grep
      sort 1 = aucune occurrence : c'est le résultat attendu).
    - `grep -rn "27/D-13" plugin/ reports/` ne rend **aucune ligne** — la forme fautive n'a atteint
      aucun artefact distribué ni aucun rapport.
    - Le rapport cite au moins **3 SHA courts distincts** (`grep -cE '\b[0-9a-f]{7}\b'` ≥ 3) et au
      moins **8 références `fichier:ligne`** (`grep -cE '[a-z-]+\.(sh|md):[0-9]+'` ≥ 8).
    - Les deux listes du verdict sont non vides : le rapport contient les mots-clés `INTOUCHABLE`
      et `EXTENSIBLE`, chacun suivi d'au moins 3 entrées de liste.
    - `git diff --name-only -- plugin/conductor/scripts/dag.sh plugin/conductor/scripts/tests/test-dag.sh`
      rend **0 ligne**.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Un rapport écrit se réécrit ; aucune surface de code n'est engagée. La décision one-way de D-03
    (régresser le mécanisme de périmètre) n'est **pas** prise ici — ce plan la rend au contraire
    inatteignable en établissant par écrit qu'aucun geste de la phase n'a besoin d'y toucher.
  </reversibility>

  <done>
    La baseline `test-dag.sh` est verte et consignée ; le rapport d'investigation existe, est
    autonome, sourcé (SHA + `fichier:ligne`), porte le verdict intouchable/extensible et la voie
    G1 avec sa clause de halte ; `dag.sh` et sa suite sont hors du diff.
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Créer les exigences ICMD-01..ICMD-12 au ledger</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` l.565-620 — la section « Hors-milestone — Phase 28 » : **le
      gabarit exact** à répliquer (blockquote d'en-tête « IDs créés au plan du <date>, préfixe
      `XXXX` (« … ») », mention du `Requirements: TBD` de la ROADMAP, sources, bloc « Hors
      périmètre, verrouillé », puis les puces `- [ ] **XXXX-NN**: …` avec renvoi `*(D-xx)*`).
    - `.planning/REQUIREMENTS.md` l.637-744 — la table `## Traceability` (`| Requirement | Phase |
      Status |`) et l.745-777 — le bloc `**Coverage:**` où chaque phase hors-milestone a son
      paragraphe de couverture.
    - `.planning/phases/VFDO-29-.../29-RESEARCH.md` §`<phase_requirements>` (l.53-85) — le constat
      qu'aucun ID n'existe, la convention observée (IDs créés au plan, préfixe à 4 lettres dérivé
      du sujet), et la table des 5 domaines de livrables.
    - `.planning/phases/VFDO-29-.../29-CONTEXT.md` §Decisions (D-01, D-02, D-03) et §Deferred Ideas
      — ce qui doit apparaître dans le bloc « Hors périmètre, verrouillé ».
    - `.planning/ROADMAP.md` l.2088-2109 (section Phase 29) — la ligne `**Requirements**: TBD` que
      cette tâche rend caduque, et le `**Goal:**` d'où dérivent les 5 familles.
  </read_first>

  <action>
    Ajouter à `.planning/REQUIREMENTS.md`, **immédiatement après** la section Phase 28 et **avant**
    `## v2 Requirements`, une section `## Hors-milestone — Phase 29 : distiller les gains de la
    deep-search (G1/G2/G3/G5) — investigation `dag.sh --scope` d'abord`.

    Blockquote d'en-tête, sur le gabarit de la Phase 28 :
    - « **IDs créés au plan du 2026-08-15**, préfixe `ICMD` » — expliciter que le préfixe est un
      **acronyme interne de traçabilité**, cohérent avec les précédents du dépôt qui nomment déjà
      leurs préfixes d'après l'objet externe déclencheur, et que cela **ne constitue pas** une
      adoption de la méthodologie externe comme label ou vocabulaire (interdiction du §6 du rapport
      source, reprise en prohibition de phase).
    - la mention que la ROADMAP portait `Requirements: TBD` (`.planning/ROADMAP.md:2103`).
    - les sources : `29-CONTEXT.md` (D-01→D-03), `29-RESEARCH.md`, `29-PATTERNS.md`.
    - un bloc **« Hors périmètre, verrouillé »** citant nommément : G4 (lab-starters clonables,
      découpé en phase dédiée **par D-01**) ; les gains secondaires du rapport source — budgets de
      tokens par couche, colonne de section/périmètre dans les mandats, cadrage « compilateur »,
      pipelines mono-agent pour labs non-dev — **hors périmètre par D-02** ; toute modification de
      `plugin/conductor/scripts/dag.sh` (**D-03**) ; toute restructuration de `.planning/`
      (ADR-055).

    Écrire ensuite les **12 puces** `- [ ] **ICMD-NN**: …`, chacune formulée comme une exigence
    **vérifiable** (pas une intention), avec son renvoi de décision quand il existe :

    - **ICMD-01** — l'investigation du mécanisme de périmètre est livrée comme rapport écrit
      autonome et sourcé (historique en deux phases par commit, inventaire des consommateurs,
      couverture de la suite de tests, verdict intouchable/extensible, voie retenue pour G1), avec
      la forme de citation historique corrigée. *(D-03)*
    - **ICMD-02** — zéro régression sur le mécanisme de périmètre : `plugin/conductor/scripts/dag.sh`
      n'apparaît dans le `files_modified` d'**aucun** plan de la phase, et la suite `test-dag.sh`
      est verte avant et après chaque geste. Toute exigence d'y toucher HALTE sur un
      `checkpoint:decision` explicite, jamais un contournement silencieux. *(D-03)*
    - **ICMD-03** — G3 : un gate `check-map-drift.sh` en **lint seul**, en-tête « répond au FAIT,
      jamais au métier », grammaire d'exit `0/1/3/64` du dépôt, wrapper git durci repris verbatim,
      **aucun** mode correctif automatique (ADR-031).
    - **ICMD-04** — G3 : deux paires carte↔disque en v1, chacune **bidirectionnelle** (entrée
      déclarée absente du disque ; élément du disque absent de la carte), discriminance de chaque
      paire prouvée par mutation dans la suite, jamais déclarée.
    - **ICMD-05** — G3 : plancher anti-vert-à-vide — cible absente, cible vide ou hors dépôt git
      rendent « rien à constater » (exit 3), **jamais** un exit 0 déguisé.
    - **ICMD-06** — G3 : le gate **écrit ses propres bornes** en en-tête — ce qu'il ne couvre pas
      (les `skills:` de frontmatter, déjà couverts par `check-agents.sh` ; les DAG de mission, hors
      domaine ; `.planning/` en lecture seule) et pourquoi.
    - **ICMD-07** — G1 : le gabarit de digest de mission porte une bullet « NE charge PAS »,
      composée du croisement de deux champs **déjà émis** par le socle, avec **zéro** ligne
      modifiée dans `dag.sh`. *(D-03)*
    - **ICMD-08** — G1 : la doctrine des agents porte une table `| Tâche | Charge | NE charge PAS |`
      — l'anti-chargement devient déclaré, pas seulement le chargement.
    - **ICMD-09** — G5 : le principe d'édition-à-la-source (une correction récurrente amende la
      source, jamais un redispatch du même correctif) est écrit dans la doctrine des managers, en
      `references/` chargée on-demand — **jamais** inline dans un agent au plafond de densité
      ADR-029.
    - **ICMD-10** — G2 : `scaffold-docs.sh` pose un `CONTEXT.md` de routage ≤ 80 lignes par
      compartiment qualifié, via le garde-fou d'idempotence existant, sur le compartiment de
      **documentation de sous-projet** (ADR-042) — jamais le compartiment de planning homonyme.
    - **ICMD-11** — G2 : le pattern `_index.md` (dossier de références > 10 fichiers, table
      `| Fichier | Résumé |`) existe, est distinct en nom **et** en sémantique de l'`INDEX.md`
      déjà posé, et est appliqué à un dossier réel du dépôt qui franchit le seuil.
    - **ICMD-12** — clôture de distribution : le gate G3 est câblé chez son consommateur à **coût
      de densité nul**, les triades de module touchées sont bumpées, les compteurs « N suites » des
      deux README racine sont **re-dérivés** (jamais recopiés) et `check-version-sync.sh` est vert.

    Ajouter les **12 lignes** correspondantes à la table `## Traceability`, à la suite des lignes
    `GSDA-*`, au format `| ICMD-NN | Phase 29 | Planned — plan 29-NN |` (le numéro de plan porteur :
    01 → ICMD-01/02 ; 02 → ICMD-03/04/05/06 ; 03 → ICMD-07/08/09 ; 04 → ICMD-10/11 ; 05 → ICMD-12).

    Ajouter enfin un paragraphe sous `**Coverage:**`, sur le modèle des paragraphes Phase 23/24 :
    12 exigences créées au plan du 2026-08-15, mappées aux **5 plans** de la phase, 0 non-mappé,
    0 inventé — et la mention explicite que D-01 et D-02 sont des **non-livrables** vérifiables par
    l'absence de G4 et des gains secondaires du `files_modified` des 5 plans, et que D-03 est
    vérifiable par l'absence de `plugin/conductor/scripts/dag.sh` de ce même ensemble.

    Mettre à jour la ligne `*Last updated:*` du pied de fichier en conservant la mention précédente
    (le fichier chaîne ses mises à jour, il ne les écrase pas).
  </action>

  <verify>
    <automated>grep -c "ICMD-" .planning/REQUIREMENTS.md</automated>
    <automated>for n in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "| ICMD-$n | Phase 29 |" .planning/REQUIREMENTS.md || echo "MANQUE traceability ICMD-$n"; done; echo "traceability-ok"</automated>
    <automated>git status --porcelain .planning/ | grep -v "REQUIREMENTS.md\|ROADMAP.md\|STATE.md\|phases/VFDO-29-" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*ICMD-' .planning/REQUIREMENTS.md` rend exactement **12**.
    - Les 12 identifiants `ICMD-01` … `ICMD-12` sont **tous** présents dans la table
      `## Traceability` au format `| ICMD-NN | Phase 29 | Planned — plan 29-NN |` : la boucle de
      vérification n'imprime aucune ligne `MANQUE`.
    - L'union des champs `requirements:` des 5 PLAN.md de la phase est **égale** au jeu des 12 IDs
      du ledger — vérifiable par :
      `grep -h '^requirements:' .planning/phases/VFDO-29-*/29-0*-PLAN.md | tr -d '[]' | tr ',' '\n' | grep -o 'ICMD-[0-9]*' | sort -u | wc -l` rend **12**, et aucun ID de ce jeu n'est absent du
      ledger.
    - La section porte un bloc « Hors périmètre, verrouillé » nommant **G4**, les gains
      secondaires, `dag.sh` et `.planning/` — `grep -c "Hors périmètre, verrouillé"` ≥ 1.
    - Le blockquote d'en-tête contient la justification explicite du préfixe comme acronyme interne
      de traçabilité **non** constitutif d'une adoption de label.
    - `git status --porcelain .planning/` ne liste **aucun** fichier hors `REQUIREMENTS.md`,
      `ROADMAP.md`, `STATE.md` et `phases/VFDO-29-*` — la structure `.planning/` est intacte
      (ADR-055).
    - Un paragraphe de couverture Phase 29 existe sous `**Coverage:**` et cite les 5 plans.
  </acceptance_criteria>

  <done>
    Les 12 exigences existent, sont vérifiables, portent leurs renvois de décision, sont tracées
    dans la table de traçabilité et couvertes par un paragraphe de couverture. Le bloc « Hors
    périmètre, verrouillé » rend D-01/D-02/D-03 opposables au ledger.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt git ↔ script de lecture | Ce plan n'exécute que `test-dag.sh` (suite existante) et `git diff` en lecture ; aucun nouveau shell-out n'est introduit. |
| artefact de planification ↔ artefact distribué | `reports/` et `.planning/` ne sont pas distribués par l'engine (seul `plugin/` l'est) — une erreur ici n'atteint aucun poste utilisateur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01-01 | Tampering | `plugin/conductor/scripts/dag.sh` (socle du dispatch parallèle) | critical | mitigate | `dag.sh` et `test-dag.sh` sont hors du `files_modified` du plan ; l'acceptance criteria exige `git diff --name-only` vide sur les deux chemins ; la suite est rejouée avant ET après. |
| T-29-01-02 | Repudiation | baseline de non-régression | high | mitigate | La baseline est **exécutée** (`<precondition>` + `<verify><automated>`), jamais présumée depuis un état antérieur ou une lecture de code. Suite rouge au départ = HALTE. |
| T-29-01-03 | Tampering | `.planning/` (propriété GSD, ADR-055) | medium | mitigate | Seul `REQUIREMENTS.md` est amendé, en ajout. Le critère `git status --porcelain .planning/` filtré prouve qu'aucun autre fichier n'a bougé. |
| T-29-01-04 | Information Disclosure | rapport d'investigation | low | accept | Le rapport ne contient ni secret, ni jeton, ni chemin machine-spécifique — uniquement des SHA publics du dépôt et des `fichier:ligne` relatifs. |
| T-29-01-SC | Tampering | chaîne d'approvisionnement (npm/pip/cargo) | low | accept | **Aucun paquet externe installé par cette phase** — `29-RESEARCH.md` §Package Legitimacy Audit l'établit (bash, python3, git sont des dépendances système déjà exercées par la suite de tests existante). Aucun gate de légitimité applicable ; rien à mitiger. |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-dag.sh` → **0**, 0 échec (avant et après le plan).
- `git diff --name-only -- plugin/conductor/scripts/dag.sh plugin/conductor/scripts/tests/test-dag.sh` → **vide**.
- `grep -rn "27/D-13" plugin/ reports/` → **aucune ligne**.
- `grep -c '^- \[ \] \*\*ICMD-' .planning/REQUIREMENTS.md` → **12**.
- `git status --porcelain .planning/` → aucun fichier hors `REQUIREMENTS.md` / `ROADMAP.md` / `STATE.md` / `phases/VFDO-29-*`.
- `grep -rn "Interpretable Context Methodology\|méthodologie ICM\|label ICM" plugin/` → **aucune ligne** (prohibition d'adoption de label).
</verification>

<success_criteria>
1. La baseline `--scope` est verte, constatée par exécution, et consignée dans le SUMMARY.
2. `reports/research/2026-08-15-investigation-dag-scope.md` est autonome, sourcé, et porte les 5
   sections dont le verdict intouchable/extensible et la voie G1 avec sa clause de halte.
3. `ICMD-01` … `ICMD-12` existent, sont vérifiables, tracés et couverts.
4. `dag.sh` et `test-dag.sh` sont intacts (D-03).
5. Aucune trace de G4 ni d'un gain secondaire dans le ledger de la phase (D-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-01-SUMMARY.md` when done
</output>

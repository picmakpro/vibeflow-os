---
phase: 41-posture-de-protection-du-d-p-t
plan: 19
lot: L19 — clôture du périmètre sans admin : ledger sur pièce, rejeu final, recensements, constat d'inatteignabilité
type: execute
wave: 6
depends_on: ["41-14", "41-15", "41-16", "41-17", "41-18"]
autonomous: true
requirements: [PROT-02, PROT-03, PROT-04, PROT-05, QUAL-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PÉRIMÈTRE — clôture du périmètre SANS ADMIN seulement (option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Aucune release : ni bump de la VERSION racine, ni tag, ni release GitHub, ni bump de module. Aucun appel GitHub en écriture. `.planning/ROADMAP.md` et `.planning/STATE.md` ne sont PAS touchés par ce plan ni par aucun plan de cette phase"
    - "PROT-01 EST HORS D'ATTEINTE et le ledger le DIT : l'énoncé exige un ruleset posé et prouvé par un refus réel, ce qui demande un accès admin que personne n'a. Il est inscrit au ledger NON COCHÉ, avec son motif, sa mesure (`admin: false, maintain: false, push: true`, 2026-09-17), son déclencheur de reprise et le renvoi au `BACKLOG.md` § « Protection de `main` côté GitHub — DIFFÉRÉ ». Il n'est ni coché, ni abandonné, ni requalifié"
    - "LES CRITÈRES DE SUCCÈS 1, 2 ET 3 DU ROADMAP RESTENT INATTEIGNABLES SANS ADMIN, et le constat est écrit explicitement au registre de preuves et au SUMMARY : le critère 1 exige un ruleset actif, le critère 2 un refus de merge réel, le critère 3 le rejeu du flux de release SOUS la règle. Aucun des trois n'est atteint par ce périmètre, et ce plan ne les réécrit pas — le ROADMAP n'est touché par aucun plan de la phase"
    - "PROT-02, PROT-03, PROT-04 et PROT-05 sont cochés SUR PIÈCE, chacun suivi d'une ligne de clôture qui cite ses clés de preuve du `41-PREUVES.md` et le plan qui les a produites. Aucune ligne de clôture ne cite O-3 en même temps qu'un mot d'achèvement : le statut d'O-3 reste « signalée et tracée », c'est un objet distinct de l'exigence"
    - "QUAL-01 est mis à jour : trois gates neufs naissent dans cette phase (G-1, G-2, G-3), chacun avec ses trois issues et ses cinq mutants prouvés rouges en exécution réelle. La ligne de la table et la ligne de liste portent « satisfait sur la Phase 41 le <date> » avec le décompte réel de mutants tués"
    - "PROT-05 EST UNE EXIGENCE NEUVE, à inscrire au même titre que les autres : « les gardes in-repo de la surface de gate et du flux d'arrivée sur `main` rendent une atteinte visible et tracée ; aucune n'est présentée comme une protection absolue ». Le ledger écrit qu'elle est NÉE DU RECADRAGE SANS ADMIN — décision du manager du 2026-09-17 sur remontée du planificateur, arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 — parce que G-2 et G-3 se retrouvaient sans foyer d'exigence après la suspension de D-01 à D-08. PROT-02 et PROT-04 gardent leur énoncé d'origine"
    - "LE LEDGER EST COCHÉ SUR PIÈCE, jamais sur déclaration : chaque case cochée est précédée d'une lecture du registre `41-PREUVES.md` et d'un rejeu vert. Une preuve absente s'écrit `non_prouvee` et l'exigence reste non cochée"
    - "REJEU FINAL sur la découverte COMPLÈTE : `--job gates` rc 0 avec au moins 13 étapes rejouées, et `--job tests` rc 0 avec sa ligne de bilan `== bilan : N suite(s), M échec(s) ==`, N au moins 82 et M à 0. Bilan absent ou N à 0 → rouge"
    - "PÉRIMÈTRE NON REJOUÉ, NOMMÉ : job `tests` — étapes `Dépendances`, `actions/setup-node`, `Installer le moteur GSD`, `Canari de forme du moteur GSD` ; jobs `lab-frais` et `lab-frais-arme` ; étape `check-release-tag` (conditionnelle `main`) ; étape de MESURE RÉELLE de G-3 (conditionnelle `main`, donc inexerçable depuis une branche — son étape de fixture, elle, est rejouée). Plus tout `SKIP` imprimé par le rejeu, recopié avec sa raison"
    - "RECENSEMENT « AUCUNE GARDE DÉCLARÉE FERMÉE » : `tools/check-aucune-fermeture.sh` rc 0, zéro ligne, sur l'état FINAL — les six SUMMARY de la phase compris, puisqu'ils entrent dans son périmètre. L'outil N'EST PAS modifié par ce plan : tout hit inattendu est remonté au manager, jamais exclu pour passer, et l'allowlist reste à ses trois entrées héritées"
    - "CONTRÔLE DE TRACE : `tools/check-trace-arbitrage.sh` rc 0 sur tous les commits propres à la branche — un seul arbitrage ou décision cité par commit, forme canonique, et chaîne exacte « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » pour tout commit qui cite l'arbitrage du périmètre"
    - "SUMMARY DE CHAQUE PLAN PRÉSENT : les six fichiers `41-14-SUMMARY.md` à `41-19-SUMMARY.md` (le sixième écrit par ce plan). Les SUMMARY des plans 41-01 à 41-13 ne sont PAS attendus : ces plans sont différés faute d'accès admin, non exécutés, et ce plan ne les touche pas"
    - "G-2 JUGE LA BRANCHE ENTIÈRE à la clôture : `scripts/check-gate-touche.sh` rend 0 sur l'état final, donc tout chemin de la surface de gate touché par la phase est couvert par un trailer `Gate-Touche:`. Un rc 1 ici se corrige par l'ajout des trailers manquants dans un commit de cette branche (portée branche), jamais par une exception dans le script ni par la réécriture d'un commit poussé"
    - "AUCUN BUMP DE MODULE N'EST DÛ, et le SUMMARY l'écrit avec sa raison vérifiée : les trois gardes vivent sous `scripts/`, l'outillage DU DÉPÔT et non un module distribué (précédent écrit dans l'en-tête de `scripts/tests/test-hook-exit-parc.sh`) ; aucun fichier sous `plugin/` n'est touché par la phase, ce qui est vérifié par machine. La trace de version passe par la section `## Non releasé` du `CHANGELOG.md` racine"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "famille PROT inscrite : PROT-01 non coché et motivé, PROT-02/03/04 cochés sur pièce, QUAL-01 mis à jour"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "clés de clôture, constat d'inatteignabilité des critères 1 à 3 et de PROT-01"
  key_links:
    - "Le rejeu du job `tests` exécute AUSSI les trois suites neuves : c'est la preuve d'intégration des vagues 1 à 3, pas une re-déclaration plan par plan"
    - "Le registre `41-PREUVES.md` est lui-même dans le périmètre du recensement : ce qu'on y écrit doit passer la vérification qu'on y consigne"
---

<objective>
Clore le périmètre sans admin par la preuve, jamais par déclaration : rejeu complet des deux jobs
CI, recensements de la phase, ledger coché sur pièce, et constat explicite de ce qui reste hors
d'atteinte — PROT-01 et les critères de succès 1, 2 et 3 du ROADMAP.

Purpose: PROT-02, PROT-03, PROT-04 et QUAL-01 établis sur l'état final intégré ; PROT-01 inscrit
sans être coché, avec son déclencheur de reprise.
Output: ledger et registre de preuves à jour, aucune release, aucun texte déclarant un achèvement
de garde.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-18-SUMMARY.md

Extraits de `.planning/REQUIREMENTS.md` à lire une seule fois :
- `## Milestone fiabilite-v1.0` et ses sous-sections `### <famille>` (format des entrées de liste,
  lignes 911 à 1133 environ), en particulier `### Budget d'instructions (Phase 25 héritée, réduite)`
  pour la forme d'une ligne cochée avec sa clôture, et `### Transverse` pour la ligne QUAL-01
- la table `## Traceability` (format `| ID | Phase N | Statut — … |`), lignes des familles BUDG,
  WKTR, PART et QUAL-01
- la note de décompte « Milestone fiabilite-v1.0 : 30 IDs au ledger » (ligne 902 environ)
</context>

<discipline>
Contraintes d'exécution valables pour TOUTES les tâches de ce plan.

- **Aucune release, aucun geste externe.** Ni bump de la `VERSION` racine, ni tag, ni release
  GitHub, ni bump de module, ni appel GitHub en écriture. Aucun `gsd-tools state.*`.
- **`.planning/ROADMAP.md` et `.planning/STATE.md` ne sont PAS modifiés.** Leur mise à jour est un
  geste hors de cette phase, à remonter au manager dans le SUMMARY comme reliquat nommé.
- **Aucune commande git en ligne dans un `<verify>` ni dans un appel bash direct** (worktree
  isolé : le harnais refuse une commande qui nomme git sous une forme non rattachable). Tout travail
  git vit dans les scripts versionnés.
- **Commandes proxifiées** : `diff` et `ls` mentent sur ce poste ; les préfixer de `rtk proxy`.
  Jamais de comptage par `grep | wc -l` — extraire puis comparer par `comm` / `cmp`, ou `awk`.
- **Les outils de recensement ne sont PAS modifiés par ce plan** (`tools/check-aucune-fermeture.sh`,
  `tools/check-trace-arbitrage.sh`, règles et allowlist comprises). Un hit inattendu se remonte au
  manager avec son classement proposé ; il ne s'exclut jamais pour faire passer la clôture.
- **Aucun texte produit ici n'associe O-3 ni une garde à un mot d'achèvement.** La formule exacte
  pour O-3 est « signalée et tracée ». Le registre de preuves est dans le périmètre du recensement.
</discipline>

<tasks>

<task type="tracer">
  <name>Task 1 : rejeu final des deux jobs, recensements de phase, et inventaire des SUMMARY</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <precondition>Les cinq plans 41-14 à 41-18 ont leur SUMMARY commité ; `rtk proxy git status --porcelain` ne liste que des chemins non suivis étrangers à la phase.</precondition>
  <read_first>
    - les cinq SUMMARY 41-14 à 41-18 (écarts déclarés, verdicts réels, nombres de mutants tués)
    - `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh` (usage, codes de sortie, forme des lignes `REJEU` et `BILAN REJEU`)
  </read_first>
  <action>
Jouer, dans cet ordre, et consigner chaque résultat au registre `41-PREUVES.md` sous une section
`## 41-19 — clôture du périmètre sans admin`, au format `CLE: valeur` du registre (une valeur non
mesurée s'écrit `non_prouvee` ou `indetermine`, jamais supposée ; aucun chemin absolu de machine) :

1. **Rejeu du job `gates`** puis du job `tests` par l'outil de rejeu de la Phase 40.1, avec la
   découverte complète, jamais une liste d'étapes recopiée. Clés : `REJEU-GATES: rc=<n>
   etapes=<n> ecarts=<n>` et `REJEU-TESTS: rc=<n> suites=<n> echecs=<n>`.
2. **Les trois gardes sur le dépôt réel**, chacune invoquée sans surcharge de fixture. Clés :
   `G1-REEL: rc=<n> verdict=<…>`, `G2-REEL: rc=<n> chemins_surface=<n> marqueurs_conformes=<n>`,
   `G3-FIXTURE: bascules=<n> ecarts=<n>` — G-3 n'a pas de mesure réelle possible depuis une branche
   (étape conditionnelle `main`), l'écrire tel quel, ne pas le masquer.
3. **Recensement de non-achèvement** : `tools/check-aucune-fermeture.sh`. Clé :
   `RECENSEMENT-FINAL: rc=<n> fichiers=<n> hits=<n> allowlist_entrees=<n>`. `hits` doit valoir 0 et
   `allowlist_entrees` doit valoir 3 — une quatrième entrée signifierait que la clôture a élargi
   l'allowlist, ce qui est interdit ici.
4. **Contrôle de trace** : `tools/check-trace-arbitrage.sh`. Clé : `TRACE-FINALE: rc=<n>
   commits=<n> citants=<n>`.
5. **Inventaire des SUMMARY** : les six fichiers `41-14-SUMMARY.md` à `41-19-SUMMARY.md` attendus
   (le sixième écrit à la fin de ce plan). Clé : `SUMMARY-PRESENTS: <liste> manquants=<liste|aucun>`.
   Écrire explicitement que les SUMMARY des plans 41-01 à 41-13 ne sont PAS attendus : ces plans
   sont différés faute d'accès admin et non exécutés.
6. **Absence de bump** : vérifier par machine qu'aucun fichier sous `plugin/` n'est touché par la
   branche et que la `VERSION` racine vaut toujours `v2.63.2`. Clé :
   `AUCUN-BUMP: fichiers_plugin_touches=<n> version_racine=<…> modules_bumpes=0`.

Un rouge n'est jamais contourné. S'il est étranger à la phase — cas connu T28-F de
`test-dev-orchestrator.sh`, fraîcheur de l'index des capacités GSD, qui rougit quand le gsd-core
installé sur le poste diffère de celui qui a généré la copie versionnée — alors : vérifier le moteur
installé et la dernière version publiée ; rejouer la même suite sur une extraction de la base de
`main` ; s'il y est rouge aussi, c'est préexistant, le consigner et le remonter au manager
(`human_needed`) sans rien neutraliser et sans déclarer la clôture verte. Tout autre rouge est
renvoyé au plan fautif.

Si G-2 rend 1 sur le dépôt réel, la seule conduite autorisée est d'ajouter les trailers
`Gate-Touche:` manquants dans un commit de cette branche (la portée du marqueur est la branche), en
consignant la liste exacte des chemins couverts et le commit porteur. Interdits : amender un commit
poussé, élargir la surface, ajouter une exception au script, désactiver l'étape.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU|^== BILAN preuve fixture/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc etapes=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 13 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `etapes=` inférieur à 13 (une étape de garde manque, ou la sélection est tronquée) ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); echo "tests rc=$rc bilan(N M)=$b"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -ge 82 ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N inférieur à 82 (les trois suites neuves ne sont pas toutes découvertes) ; ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); a=0; bash scripts/check-baseline-arbitrage.sh > "$T/g1" 2>&1 || a=$?; b=0; bash scripts/check-gate-touche.sh > "$T/g2" 2>&1 || b=$?; cat "$T/g1" "$T/g2"; s=$(awk "/^decouverte: /{for(i=1;i<=NF;i++) if (\$i ~ /^chemins_surface=/) {split(\$i,x,\"=\"); print x[2]+0}}" "$T/g2"); echo "G1 rc=$a G2 rc=$b chemins_surface=${s:-0}"; case "$a" in 0|3) : ;; *) exit 1 ;; esac; [ "$b" -eq 0 ] && [ "${s:-0}" -ge 3 ]'</automated>
    <fails_when>G-1 rend autre chose que 0 ou 3 ; ou G-2 rend autre chose que 0 (un chemin de la surface de gate touché par la phase n'est pas déclaré) ; ou `chemins_surface=` inférieur à 3</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh > "$T/r" 2>&1 || r=$?; cat "$T/r"; h=$(awk "!/^perimetre: |^allowlist: /&&NF{c++} END{print c+0}" "$T/r"); e=$(awk "/^allowlist: /{for(i=1;i<=NF;i++) if (\$i ~ /^entrees=/) {split(\$i,x,\"=\"); print x[2]+0}}" "$T/r"); s=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh > "$T/s" 2>&1 || s=$?; cat "$T/s"; echo "recensement rc=$r hits=$h allowlist_entrees=${e:-0} trace rc=$s"; [ "$r" -eq 0 ] && [ "$h" -eq 0 ] && [ "${e:-0}" -eq 3 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>`recensement rc=` différent de 0 ; ou `hits=` différent de 0 (un texte de la phase associe une garde ou O-3 à un mot d'achèvement — le classer et le remonter, jamais l'exclure) ; ou `allowlist_entrees=` différent de 3 (la clôture a élargi l'allowlist) ; ou `trace rc=` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'D=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; m=0; for n in 14 15 16 17 18; do [ -f "$D/41-$n-SUMMARY.md" ] || { echo "SUMMARY manquant : 41-$n"; m=$((m+1)); }; done; v=$(awk "NR==1{print \$1}" VERSION); echo "summary_manquants=$m VERSION=$v"; [ "$m" -eq 0 ] && [ "$v" = "v2.63.2" ]'</automated>
    <fails_when>`summary_manquants=` différent de 0 (un plan de la phase n'a pas son SUMMARY : la clôture porterait sur un état non documenté) ; ou `VERSION=` différent de `v2.63.2` (la VERSION racine a été touchée, interdit)</fails_when>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md; k=$(awk "/^REJEU-GATES: |^REJEU-TESTS: |^G1-REEL: |^G2-REEL: |^G3-FIXTURE: |^RECENSEMENT-FINAL: |^TRACE-FINALE: |^SUMMARY-PRESENTS: |^AUCUN-BUMP: /{c++} END{print c+0}" "$P"); s=$(awk "/^## 41-19 /{c++} END{print c+0}" "$P"); o=$(awk "/^CONTEXTES-CHECKS: /{c++} END{print c+0}" "$P"); q=$(awk "/^## 41-15 /{c++} END{print c+0}" "$P"); echo "cles=$k section=$s contextes_checks=$o section_41-15=$q"; [ "$k" -eq 9 ] && [ "$s" -eq 1 ] && [ "$o" -eq 1 ] && [ "$q" -eq 1 ]'</automated>
    <fails_when>`cles=` différent de 9 ; ou `section=` différent de 1 ; ou `contextes_checks=` différent de 1 ou `section_41-15=` différent de 1 (une section antérieure du registre a été écrasée)</fails_when>
  </verify>
  <acceptance_criteria>
    - rejeu `gates` rc 0 avec au moins 13 étapes, aucune ligne `rc=` non nulle ; rejeu `tests` rc 0 avec bilan présent, au moins 82 suites, 0 échec
    - G-1 en 0 ou 3, G-2 en 0 avec au moins 3 chemins de surface déclarés, G-3 prouvée par ses bascules de fixture
    - recensement rc 0, 0 hit, allowlist toujours à 3 entrées ; contrôle de trace rc 0
    - les cinq SUMMARY 41-14 à 41-18 existent ; `VERSION` racine intacte ; aucun fichier sous `plugin/` touché
    - les neuf clés sont au registre, sections antérieures intactes ; périmètre non rejoué nommé au SUMMARY, `SKIP` recopiés
  </acceptance_criteria>
  <done>L'état final intégré est vert sur la découverte complète des deux jobs, les trois gardes répondent, et rien dans la phase ne déclare d'achèvement.</done>
</task>

<task type="auto">
  <name>Task 2 : ledger des exigences — famille PROT inscrite, PROT-01 non coché et motivé, QUAL-01 mis à jour</name>
  <files>.planning/REQUIREMENTS.md</files>
  <precondition>Task 1 verte : toutes les clés du registre `41-PREUVES.md` sont écrites et mesurées.</precondition>
  <read_first>
    - `41-PREUVES.md` § 41-19 (les neuf clés, à citer dans les lignes de clôture)
    - `.planning/REQUIREMENTS.md` : `### Budget d'instructions (Phase 25 héritée, réduite)` (forme d'une ligne cochée avec sa clôture), `### Transverse` (ligne QUAL-01), la table `## Traceability`
  </read_first>
  <action>
**Définitions — ajouter une sous-section de CINQ entrées** (avertissement 5 du vérificateur frais :
le décompte doit valoir cinq dans l'action, dans les critères d'acceptation, dans le verify et dans
les critères de succès — la version précédente en annonçait quatre et omettait PROT-05) sous `## Milestone fiabilite-v1.0`, après
`### Budget d'instructions (Phase 25 héritée, réduite)` :
`### Posture de protection du dépôt (Phase 41 — périmètre sans admin)`, au format multi-lignes déjà
en usage dans ce fichier (une entrée `- [ ]` ou `- [x]` par ID, énoncé, puis la clôture sur la même
entrée). **CINQ entrées** — PROT-01 non coché, PROT-02, PROT-03, PROT-04 et PROT-05 cochés :

- **PROT-01**, énoncé : rulesets de branche et de tags posés et prouvés par un refus réel.
  **NON COCHÉ** (`- [ ]`), suivi de son motif écrit : hors d'atteinte sans accès admin — mesure
  `admin: false, maintain: false, push: true` sur le dépôt, 2026-09-17, deux lectures indépendantes ;
  le compte `picmakpro`, seul admin, appartient à un tiers. Déclencheur de reprise : un accès admin
  accordé, ou un transfert du dépôt. Renvoi : `BACKLOG.md` § « Protection de `main` côté GitHub —
  DIFFÉRÉ » et `41-CONTEXT.md` § Prémisse renversée (D-01 à D-08 suspendus, non annulés). Ni
  abandonné, ni requalifié : il attend.
- **PROT-02**, énoncé : compatibilité avec la discipline de release du `CLAUDE.md`. **COCHÉ**, avec
  sa ligne de clôture citant les clés `REJEU-GATES`, `G3-FIXTURE` et les trois preuves du plan
  41-17 (ordre des étapes avant `check-release-tag`, absence d'entrée `tags:` dans le déclencheur,
  voie `merge-commit-sha` pour les merges par rebase) plus le hook `pre-push` conservé.
- **PROT-03**, énoncé : politique de contournement et de hotfix écrite. **COCHÉ**, clôture citant
  ADR-072 (plan 41-18) et son résumé dans `CLAUDE.md`, avec la précision que l'amendement d'ADR-059
  est hors périmètre faute de règle serveur qui l'appliquerait.
- **PROT-05**, énoncé : les gardes in-repo de la surface de gate et du flux d'arrivée sur `main` rendent une atteinte visible et tracée ; aucune n'est présentée comme une protection absolue. **COCHÉ**, clôture citant G-2 et G-3 (plans 41-16 et 41-17),
  leurs mutants tués, les clés `G2-REEL` et `G3-FIXTURE`, et la mention explicite qu'elle est NÉE DU
  RECADRAGE SANS ADMIN (décision du manager du 2026-09-17 sur remontée du planificateur, arbitrage
  Samuel, AskUserQuestion session principale, 2026-09-17) : G-2 et G-3 étaient sans foyer d'exigence
  après la suspension de D-01 à D-08. Écrire aussi la borne : « aucune n'est présentée comme une
  protection absolue » est une clause opposable de l'énoncé, pas une formule de style — le
  recensement `tools/check-aucune-fermeture.sh` en est la vérification machine, par son interdiction
  de co-occurrence et par sa sonde de limite de fond. L'énoncé lui-même a été REFORMULÉ le
  2026-09-17 (décision du manager, bloquant 2 du vérificateur frais) : la première rédaction portait
  un jeton d'achèvement et déclenchait son propre recensement ; la reformulation ne contient aucun
  jeton et reste opposable, sans quatrième entrée d'allowlist.
- **PROT-04**, énoncé révisé pour le périmètre sans admin : O-3 du `25-SECURITY.md` **signalée et
  tracée** par des gardes in-repo — non plus par `CODEOWNERS` et revue code owner, inatteignables
  sans admin. **COCHÉ**, clôture citant G-1 (plan 41-14), ses NEUF mutants tués, les
  clés `G1-REEL` et `RECENSEMENT-FINAL`. G-1 est le seul foyer de PROT-04 depuis la décision du
  manager du 2026-09-17 ; G-2 relève de PROT-05. Écrire que le changement de moyen est un
  arbitrage du 2026-09-17 et non une réduction silencieuse.

**Règle d'écriture** : aucune ligne de clôture n'associe O-3 à un mot d'achèvement. La clôture porte
sur l'EXIGENCE ; le statut d'O-3 reste « signalée et tracée » et c'est un objet distinct — écrire les
deux dans des phrases séparées.

**QUAL-01** (`### Transverse` et sa ligne de table) : ajouter « **satisfait sur la Phase 41 le
<date d'exécution>** » avec le décompte réel, lu dans les SUMMARY : trois gates neufs (G-1, G-2,
G-3), leurs trois issues chacun, et le nombre total de mutants tués (dix-neuf attendus pour les trois gardes CI :
NEUF pour G-1 après les décisions du manager du 2026-09-17 — colonne des instructions seule
bloquante, puis retrait de ligne à trois cas — cinq pour G-2, cinq pour G-3 ; plus SEPT pour les deux
outils de phase, trois au recensement et quatre au contrôle de trace. Écrire le nombre MESURÉ, jamais
le nombre attendu, et ne compter que les mutants crédités par la ligne canonique
`✓ MUT-<n> TUE : rc_mutant=… attendu …, rc_original=… attendu …` — un mutant tué par un plantage ou
par une plage vide ne compte pas, avertissement 3 du vérificateur frais). Ne pas retirer les mentions des phases
antérieures.

**Table `## Traceability`** : ajouter quatre lignes au format exact des voisines, après les lignes
PART :
- `| PROT-01 | Phase 41 | Hors d'atteinte — <date> : exige un accès admin (permissions mesurées le 2026-09-17 …), différé au BACKLOG, déclencheur de reprise écrit |`
- `| PROT-02 | Phase 41 | Complete — <date>, plan 41-17/41-19 : … |`
- `| PROT-03 | Phase 41 | Complete — <date>, plan 41-18 : ADR-072 … |`
- `| PROT-04 | Phase 41 | Complete — <date>, plan 41-14 : … |`
- `| PROT-05 | Phase 41 | Complete — <date>, plans 41-16/41-17 : exigence née du recadrage sans admin (décision du manager, 2026-09-17), … |`

**Note de décompte** : la phrase « Milestone fiabilite-v1.0 : 30 IDs au ledger » est une trace datée
du 2026-08-15 — ne PAS la réécrire. Ajouter, juste après, une ligne datée : la famille PROT
(5 IDs) est inscrite par la Phase 41 le <date>, dont PROT-01 hors d'atteinte sans accès admin et
PROT-05 créée en cours de phase par le recadrage sans admin (décision du manager, 2026-09-17).

Rejouer après écriture : le recensement `tools/check-aucune-fermeture.sh` (le ledger est dans son
périmètre) et la suite `plugin/dev-orchestrator/scripts/tests/test-check-requirements-survival.sh`,
pour vérifier qu'aucun ID garanti n'a disparu du ledger vivant.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'R=.planning/REQUIREMENTS.md; s=$(awk "/^### Posture de protection du dépôt \\(Phase 41/{c++} END{print c+0}" "$R"); u=$(awk "/^- \\[ \\] \\*\\*PROT-01\\*\\*/{c++} END{print c+0}" "$R"); c=$(awk "/^- \\[x\\] \\*\\*PROT-0[2345]\\*\\*/{c++} END{print c+0}" "$R"); t=$(awk "/^\\| PROT-0[12345] \\| Phase 41 \\|/{c++} END{print c+0}" "$R"); h=$(awk "/^\\| PROT-01 \\| Phase 41 \\| Hors d.atteinte/{c++} END{print c+0}" "$R"); q=$(awk "/QUAL-01/ && /Phase 41/{c++} END{print c+0}" "$R"); n5=$(awk "/PROT-05/ && /recadrage sans admin/{c++} END{print c+0}" "$R"); echo "section=$s PROT01_non_coche=$u PROT02345_coches=$c lignes_table=$t PROT01_hors_atteinte=$h QUAL01_phase41=$q PROT05_origine=$n5"; [ "$s" -eq 1 ] && [ "$u" -eq 1 ] && [ "$c" -eq 4 ] && [ "$t" -eq 5 ] && [ "$h" -eq 1 ] && [ "$q" -ge 2 ] && [ "$n5" -ge 1 ]'</automated>
    <fails_when>`section=` différent de 1 ; ou `PROT01_non_coche=` différent de 1 (PROT-01 coché, abandonné ou absent) ; ou `PROT02345_coches=` différent de 4 ; ou `lignes_table=` différent de 5 ; ou `PROT05_origine=` à 0 (le ledger ne dit pas que PROT-05 est née du recadrage sans admin) ; ou `PROT01_hors_atteinte=` différent de 1 (la ligne de table ne dit pas l'inatteignabilité) ; ou `QUAL01_phase41=` inférieur à 2 (la Phase 41 manque à la ligne de liste ou à la ligne de table de QUAL-01)</fails_when>
    <automated>bash --noprofile --norc -e -c 'R=.planning/REQUIREMENTS.md; n=$(awk "/30 IDs au ledger/{c++} END{print c+0}" "$R"); f=$(awk "/famille PROT/ && /Phase 41/{c++} END{print c+0}" "$R"); b=$(awk "/^- \\[x\\] \\*\\*BUDG-0[1245]\\*\\*/{c++} END{print c+0}" "$R"); echo "note_30IDs_intacte=$n note_PROT_ajoutee=$f BUDG_intacts=$b"; [ "$n" -eq 1 ] && [ "$f" -ge 1 ] && [ "$b" -eq 4 ]'</automated>
    <fails_when>`note_30IDs_intacte=` différent de 1 (une trace datée a été réécrite ou supprimée) ; ou `note_PROT_ajoutee=` à 0 ; ou `BUDG_intacts=` différent de 4 (débordement d'édition sur la famille voisine)</fails_when>
    <automated>bash --noprofile --norc -e -c 'r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh >/dev/null 2>&1 || r=$?; s=0; bash plugin/dev-orchestrator/scripts/tests/test-check-requirements-survival.sh >/dev/null 2>&1 || s=$?; echo "recensement=$r suite_survie_ledger=$s"; [ "$r" -eq 0 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>`recensement=` différent de 0 (une ligne de clôture associe une garde ou O-3 à un mot d'achèvement — reformuler, jamais allowlister) ; ou `suite_survie_ledger=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - une sous-section PROT unique, **CINQ entrées** (PROT-01 à PROT-05), PROT-01 non coché avec motif, mesure, déclencheur de reprise et double renvoi
    - PROT-02, PROT-03, PROT-04 et PROT-05 cochés, chacun suivi d'une ligne de clôture citant ses clés du registre et son plan ; PROT-05 porte en outre la mention de son origine (recadrage sans admin) et sa reformulation du 2026-09-17
    - **CINQ** lignes de table, dont celle de PROT-01 qui dit l'inatteignabilité ; QUAL-01 mentionne la Phase 41 en liste et en table avec le décompte de mutants MESURÉ
    - note datée ajoutée pour la famille PROT sans réécrire la phrase « 30 IDs au ledger » ; familles voisines intactes
    - recensement rc 0 et suite de survie du ledger verte après écriture
  </acceptance_criteria>
  <done>Le ledger dit l'état prouvé du périmètre sans admin, et dit que PROT-01 attend un accès qu'aucune session n'a.</done>
</task>

<task type="auto">
  <name>Task 3 : constat d'inatteignabilité, reliquats nommés, SUMMARY de clôture</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <precondition>Tasks 1 et 2 vertes.</precondition>
  <action>
Compléter la section `## 41-19` du registre par les clés de constat, puis écrire le SUMMARY de
clôture.

**Clés de constat à écrire** (format `CLE: valeur`, valeurs mesurées ou `non_prouvee`) :
- `CRITERE-1-ROADMAP: inatteignable_sans_admin raison=<exige un ruleset actif sur main ; rulesets du dépôt lus à vide, permissions admin false>`
- `CRITERE-2-ROADMAP: inatteignable_sans_admin raison=<exige le refus réel d'un merge, donc une règle côté serveur>`
- `CRITERE-3-ROADMAP: inatteignable_sans_admin raison=<exige le rejeu du flux de release SOUS la règle ; la règle n'existe pas>`
- `PROT-01-STATUT: non_coche hors_d_atteinte declencheur=<acces admin accorde ou transfert du depot>`
- `ROADMAP-NON-TOUCHE: oui` et `STATE-NON-TOUCHE: oui` — aucun plan de cette phase ne les modifie ;
  leur mise à jour est **prise en charge par le manager** (décision du 2026-09-17), nommée ici comme
  reliquat tracé, jamais comme un oubli
- `RELEASE: aucune version=<v2.63.2 inchangee> tag=aucun release_github=aucune modules_bumpes=0`
- `LIMITE-DE-FOND: exiges=<n> porteurs=<n> manquants=<liste|aucun>` — valeurs MESURÉES par la sonde
  de `tools/check-aucune-fermeture.sh` sur la liste d'artefacts porteurs (les trois en-têtes de
  garde, `ci.yml`, la section ADR-072, `CLAUDE.md`, les six SUMMARY). `manquants` doit valoir
  `aucun` et `porteurs` doit égaler `exiges` : c'est la seule forme de cette clé qui prouve quelque
  chose, l'ancienne rédaction auto-déclarative étant supprimée (avertissement 7 du vérificateur
  frais)

**SUMMARY de clôture** — écrire, en plus du format habituel :
1. le tableau des quatre livrables (G-1, G-2, G-3, G-4) avec, pour chacun, son artefact, son étape
   CI, son nombre de mutants tués et ses bornes nommées ;
2. la limite de fond, en toutes lettres, une fois : une garde qui vit dans le dépôt peut être
   modifiée par la PR qu'elle juge ; une même PR peut changer le gate, sa suite et l'étape CI et
   rester verte ; cette phase ne clôt rien, elle rend visible et tracé ;
3. le constat d'inatteignabilité : PROT-01 et les critères de succès 1, 2 et 3 du ROADMAP ne sont
   pas atteints et ne peuvent pas l'être sans accès admin — avec la mesure et le déclencheur de
   reprise ;
4. le statut d'O-3 : **signalée et tracée**, avec la phrase qui dit pourquoi elle ne peut pas être
   davantage ;
5. QUAL-01 : trois gates neufs, trois issues chacun, mutants tués avec le décompte mesuré et la
   trace du rouge (assertion, attendu, obtenu) référencée dans les SUMMARY de plan ;
6. l'absence de bump de module, avec sa raison vérifiée (aucun fichier sous `plugin/` touché ;
   `scripts/` est l'outillage du dépôt) et le renvoi à la section `## Non releasé` du
   `CHANGELOG.md` ;
7. les reliquats nommés, TRAITÉS PAR LE MANAGER et non par cette phase (décision du 2026-09-17) :
   mise à jour du `ROADMAP.md` (critères de succès à réécrire sur le périmètre arbitré) et du
   `STATE.md` ; la PR de la phase et son merge ; la
   décision de release ou non — tous hors du périmètre de cette phase ;
8. le périmètre non rejoué et les `SKIP`, recopiés depuis la Task 1.

Rejouer une dernière fois, après écriture du registre ET du SUMMARY : le recensement (le SUMMARY est
dans son périmètre), le contrôle de trace, puis les deux jobs.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md; k=$(awk "/^CRITERE-1-ROADMAP: |^CRITERE-2-ROADMAP: |^CRITERE-3-ROADMAP: |^PROT-01-STATUT: |^ROADMAP-NON-TOUCHE: |^STATE-NON-TOUCHE: |^RELEASE: |^LIMITE-DE-FOND: /{c++} END{print c+0}" "$P"); i=$(awk "/^CRITERE-[123]-ROADMAP: inatteignable_sans_admin/{c++} END{print c+0}" "$P"); echo "cles_constat=$k criteres_inatteignables=$i"; [ "$k" -eq 8 ] && [ "$i" -eq 3 ]'</automated>
    <fails_when>`cles_constat=` différent de 8 ; ou `criteres_inatteignables=` différent de 3 (un critère du ROADMAP n'est pas explicitement constaté inatteignable)</fails_when>
    <automated>bash --noprofile --norc -e -c 'D=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; [ -f "$D/41-19-SUMMARY.md" ] || { echo "SUMMARY de cloture absent"; exit 1; }; g=$(awk "/check-baseline-arbitrage/{a=1} /check-gate-touche/{b=1} /check-push-sans-pr/{c=1} /ADR-072/{d=1} END{print a+b+c+d}" "$D/41-19-SUMMARY.md"); p=$(awk "/PROT-01/{a=1} /signalée et tracée/{b=1} END{print a+b}" "$D/41-19-SUMMARY.md"); echo "livrables_nommes=$g constats=$p"; [ "$g" -eq 4 ] && [ "$p" -eq 2 ]'</automated>
    <fails_when>SUMMARY de clôture absent ; ou `livrables_nommes=` différent de 4 (un des quatre livrables n'est pas nommé) ; ou `constats=` différent de 2 (PROT-01 ou le statut « signalée et tracée » manque)</fails_when>
    <automated>bash --noprofile --norc -e -c 'r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh > /tmp/vf41r 2>&1 || r=$?; h=$(awk "!/^perimetre: |^allowlist: /&&NF{c++} END{print c+0}" /tmp/vf41r); s=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh >/dev/null 2>&1 || s=$?; echo "recensement_final rc=$r hits=$h trace_finale rc=$s"; [ "$r" -eq 0 ] && [ "$h" -eq 0 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>`recensement_final rc=` différent de 0 ; ou `hits=` différent de 0 (le SUMMARY de clôture associe une garde ou O-3 à un mot d'achèvement) ; ou `trace_finale rc=` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); a=0; b=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || a=$?; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || b=$?; awk "/^BILAN REJEU/" "$T/g" "$T/t"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); v=$(awk "NR==1{print \$1}" VERSION); echo "gates rc=$a etapes=${n:-0} tests rc=$b VERSION=$v"; [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "${n:-0}" -ge 13 ] && [ "$v" = "v2.63.2" ]'</automated>
    <fails_when>l'un des deux rc est non nul ; ou `etapes=` inférieur à 13 ; ou `VERSION=` différent de `v2.63.2`</fails_when>
  </verify>
  <acceptance_criteria>
    - les huit clés de constat sont au registre, les trois critères du ROADMAP explicitement constatés inatteignables sans admin
    - le SUMMARY de clôture nomme les quatre livrables, porte la limite de fond en toutes lettres, le constat PROT-01, le statut « signalée et tracée », le décompte mesuré de mutants, l'absence de bump avec sa raison, et les reliquats nommés (ROADMAP, STATE, PR, release)
    - recensement final rc 0 et 0 hit ; contrôle de trace rc 0 ; rejeu final des deux jobs vert ; `VERSION` racine intacte
  </acceptance_criteria>
  <done>La phase se clôt en disant ce qu'elle a livré, ce qu'elle n'a pas pu livrer, et ce qui attend un accès qu'aucune session n'a — sans qu'un seul texte déclare une garde achevée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves de plan → verdict de phase | un vert auto-déclaré par plan ne vaut pas preuve sur l'état intégré |
| ledger coché → réalité livrée | une case cochée survit à la phase et devient la vérité du lecteur d'après |
| périmètre arbitré → critères du ROADMAP | les critères d'origine décrivent une posture serveur qui n'a pas été livrée |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-83 | Repudiation | clôture prononcée sur une liste d'étapes recopiée ou un bilan absent | high | mitigate | rejeu extrait de `ci.yml` par l'outil de la Phase 40.1 ; plancher de 13 étapes `gates` ; bilan `tests` exigé avec N ≥ 82 et M = 0 |
| T-41-84 | Repudiation | PROT-01 coché ou requalifié alors qu'il exige un accès admin | high | mitigate | verify machine exigeant `- [ ] **PROT-01**` non coché et une ligne de table portant l'inatteignabilité ; motif, mesure, déclencheur et double renvoi écrits |
| T-41-85 | Repudiation | critères de succès du ROADMAP tenus pour atteints par assimilation au périmètre livré | high | mitigate | trois clés de constat distinctes au registre, chacune avec sa raison ; le ROADMAP n'est touché par aucun plan et sa mise à jour est un reliquat nommé au SUMMARY |
| T-41-86 | Tampering | outil de recensement ou allowlist élargi pour faire passer la clôture | high | mitigate | modification interdite dans ce plan ; verify exigeant `allowlist_entrees=3` et `hits=0` ; conduite écrite : classer et remonter, jamais exclure |
| T-41-87 | Repudiation | rouge préexistant (T28-F) imputé à la phase ou neutralisé | low | mitigate | procédure écrite : vérifier le moteur installé, rejouer sur la base de `main`, remonter `human_needed`, jamais neutraliser ni déclarer vert |
| T-41-88 | Elevation of privilege | release ou bump glissé dans la clôture | medium | mitigate | verify de la `VERSION` racine à chaque tâche ; clé `RELEASE: aucune` ; vérification qu'aucun fichier sous `plugin/` n'est touché |
</threat_model>

<verification>
Rejeu final `gates` (≥ 13 étapes) et `tests` (≥ 82 suites, 0 échec) verts ; G-1 en 0 ou 3, G-2 en 0
avec ≥ 3 chemins déclarés, G-3 prouvée par fixture ; recensement rc 0, 0 hit, allowlist à 3 entrées ;
contrôle de trace rc 0 ; cinq SUMMARY de plan présents plus celui de clôture ; famille PROT inscrite en CINQ entrées et CINQ lignes de table,
PROT-01 non coché ; QUAL-01 mis à jour ; dix-sept clés au registre (neuf de mesure, huit de
constat) ; `VERSION` racine intacte, aucun module bumpé, aucun fichier sous `plugin/` touché.
</verification>

<success_criteria>
Les quatre livrables du périmètre arbitré (G-1, G-2, G-3, G-4) sont prouvés sur l'état final
intégré ; PROT-02, PROT-03, PROT-04 et PROT-05 sont clos sur pièce — cinq entrées PROT au
ledger, cinq lignes de traçabilité ; PROT-01 et les critères de succès 1, 2 et 3 du ROADMAP sont
explicitement constatés hors d'atteinte sans accès admin ; aucun texte de la
phase ne déclare une garde ou O-3 achevée, et une vérification le prouve.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : mises à jour de `.planning/REQUIREMENTS.md` (famille PROT, QUAL-01, table de
traçabilité) et de `41-PREUVES.md` (sections de mesure et de constat).

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-19-SUMMARY.md` when done
</output>
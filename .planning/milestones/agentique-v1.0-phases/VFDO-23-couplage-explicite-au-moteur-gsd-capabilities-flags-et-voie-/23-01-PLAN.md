---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/dev-orchestrator/references/mission-contracts.md
  - plugin/dev-orchestrator/agents/vf-coder.md
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [GSDC-01, GSDC-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un checkpoint que le moteur a refusé d'auto-approuver (`gate=\"blocking-human\"`) remonte au manager comme `human_needed`, jamais comme `passed` (D-01)."
    - "Une précondition non satisfaite en amont produit exactement le même statut que `gate=\"blocking-human\"` — une seule règle couvre les deux motifs de refus (D-01)."
    - "Une mission qui démarre remet `workflow._auto_chain_active` à `false` avant tout dispatch, donc aucun checkpoint n'est auto-tranché par un flag hérité d'une session antérieure (D-02)."
    - "Aucun fichier d'agent ou de référence du module ne prescrit le flag d'enchaînement autonome sur la brique de plan ni sur la brique d'exécution ; il reste licite sur la brique de cadrage (D-02)."
    - "Quand un mandat s'arrête sur une attente humaine, le bloc typé porte de quoi redispatcher (`plan_id`, type de checkpoint, `gate`, attendu) sans que VibeFlow recopie le contrat interne du moteur (D-03)."
    - "En mission autonome, un `gate=\"blocking-human\"` fige le nœud porteur et laisse avancer les branches indépendantes du DAG (D-04)."
    - "C'est `vf-dev-manager` qui répond aux attentes humaines du moteur ; `vf-coder` remonte et n'auto-répond jamais (D-04bis)."
  artifacts:
    - plugin/dev-orchestrator/references/mission-contracts.md
    - plugin/dev-orchestrator/agents/vf-coder.md
    - plugin/dev-orchestrator/agents/vf-dev-manager.md
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  key_links:
    - "`vf-coder` §Retour émet `gate` ↔ `vf-dev-manager` §Contrôle de flux le consomme — si un seul des deux bouts manque, la chaîne est muette et rien ne le signale."
    - "T24 relie les deux bouts par une assertion à discriminance prouvée par mutation — la doctrine seule serait un garde-fou inerte (précédent Phase 19, SC3)."
    - "T25 exclut `scripts/tests/` de son propre balayage, sinon la fixture d'interdiction se détecte elle-même et le gate devient tautologique."
---

<objective>
Fermer la Lacune 6 (SÛRETÉ, priorité imposée par le ROADMAP et par D-31) : le module cesse
d'ignorer la taxonomie de checkpoint à deux couches de `@opengsd/gsd-core@1.9.0` (type × `gate`).

Purpose : c'est la seule lacune de la Phase 23 dont la conséquence est un **mauvais comportement
silencieux** — un manager en mode autonome peut aujourd'hui « continuer » sur le seul checkpoint
que le moteur a explicitement refusé de trancher, et un `--auto` posé une fois sur le cadrage
persiste dans `.planning/config.json` et auto-tranche les décisions de **toutes** les sessions
suivantes. La doctrine de flags (plan 23-03) ne peut pas être écrite avant que ce contrat soit
posé : on ne peut pas graduer un flag sans savoir ce qu'il auto-approuve.

Output : le champ `gate` et le minimum de reprise ajoutés au contrat typé, le mapping unique
`gate="blocking-human"` OU précondition non satisfaite ⇒ `human_needed` écrit et **gaté machine**,
le reset de `workflow._auto_chain_active` en geste de démarrage de mission, trois blocs de test
(T24/T25/T26) à discriminance prouvée par mutation.

**Décisions informatives de la phase, enregistrées ici pour l'ensemble des 8 plans** — elles ne
produisent aucun livrable mais elles contraignent la façon de lire les autres :

- **D-00 [factuel]** — les 31 décisions de `23-CONTEXT.md` ont été **tranchées par Samuel en
  conversation** (`23-DISCUSSION-LOG.md`), aucune n'est une hypothèse auto-confirmée. Deux ont été
  **révisées en cours de cadrage sur découverte factuelle** (D-03 et D-04bis) : la révision est
  tracée, pas masquée. Conséquence pour l'exécution : ne jamais « rattraper » le ROADMAP contre le
  CONTEXT — sur la Lacune 6(c), c'est le CONTEXT révisé qui fait autorité.
- **D-30 [factuel, non-livrable explicite]** — `plugin/conductor/scripts/dag.sh` n'est **PAS
  touché** par cette phase, ni par ce plan ni par aucun des sept autres. Sa commande de réouverture
  remet un nœud et ses dépendants à l'état bloqué/prêt et écrit elle-même le régime de revue
  complet ; elle ne modélise pas un nœud partiellement exécuté, **et n'a pas à le faire** — le grain
  tâche vit dans le mandat (D-03), le grain nœud dans le DAG. Le script vit dans le module
  `conductor` (team-kernel), donc toute évolution serait transverse à tous les métiers : hors
  périmètre. Vérifiable par l'absence de ce chemin des `files_modified` des 8 plans.
- **D-29 [factuel, vérifié]** et **D-31 [périmètre]** sont cités en place dans les tâches et les
  plans qu'ils contraignent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : ils n'existent nulle part avant elle,
leur absence de l'existant n'est pas une dérive.

**Fichiers neufs**
- `plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` (plan 23-04)
- `plugin/dev-orchestrator/references/gsd-capabilities-index.md` (plan 23-04, auto-généré)
- `plugin/dev-orchestrator/scripts/check-gsd-config.sh` (plan 23-02)
- `plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` (plan 23-02)

**Champs neufs du bloc typé** (`mission-contracts.md`, frères optionnels de `statut`)
- `gate` (plan 23-01) · `reprise` avec ses sous-champs `plan_id` / `checkpoint` / `gate` /
  `attendu` (plan 23-01) · `verdicts` avec ses sous-champs `code_review` / `nyquist` / `secure`
  (plan 23-06) · `decompte` (plan 23-07)

**Sections neuves de référence**
- `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » (plan 23-03)
- `mission-flow.md` §Pattern E — budget partagé par étape, table des briques dormantes (plan 23-07)
- `docs/ADR.md` ADR-061 — troisième objet revu (plan 23-06)

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24`, `T25`, `T26` (plan 23-01) · `T27`
(plan 23-03) · `T28` (plan 23-04) · `T29` (plan 23-05) · `T30` (plan 23-06) · `T31`, `T32`
(plan 23-07)

**Variables d'environnement neuves** : `VF_CAPS_INDEX_OUT`, `VF_GSD_TOOLS`, `VF_GSD_CORE_LIB`
(plan 23-04) · `VF_CONFIG_PATH` (plan 23-02)

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` signal · `3` silence · `64` argument
invalide (plan 23-02)

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10` (créés au plan dans
`.planning/REQUIREMENTS.md`, patron `DOCF-01..07` de la Phase 22)

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement.** Aucun `SPEC.md` n'existe pour cette phase
  (`EDGE_ABSENT`, `PROHIB_ABSENT`) et le ROADMAP portait `Requirements: TBD` — aucun prédicat de
  sonde n'est généré ce tour. Choix enregistré, pas un oubli.
- **Gate UI : sans objet.** Aucun `-UI-SPEC.md` — phase de doctrine markdown et de scripts bash,
  aucun frontend (surcharge utilisateur actée au cadrage).
- **Gate de schéma ORM : sans objet.** Aucun fichier de collection/schéma/migration dans le
  périmètre — rien à pousser.
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  cette phase ne touche que de la doctrine markdown et des scripts bash internes au module
  `dev-orchestrator`, plus le moteur `@opengsd/gsd-core` déjà installé localement — aucun SDK,
  aucun service tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet.** Aucun `npm`/`pip`/`cargo install` dans le périmètre
  (RESEARCH §Package Legitimacy Audit).

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : le champ `gate` traverse la chaîne de bout en bout (D-01)</name>
  <files>plugin/dev-orchestrator/references/mission-contracts.md, plugin/dev-orchestrator/agents/vf-coder.md, plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <precondition>Les Phases 20 et 22 sont mergées dans la branche de travail — constatable : `plugin/dev-orchestrator/references/mission-flow.md` contient une section `Pattern E` (Phase 20) ET `plugin/dev-orchestrator/references/docs-flow.md` existe (Phase 22). Si l'une des deux manque, HALT : le périmètre à fichiers partagés déclaré au ROADMAP (`vf-coder.md`, `vf-dev-manager.md`, `mission-contracts.md`) n'est pas à jour et cette tâche produirait un conflit de merge silencieux.</precondition>
  <read_first>
    - plugin/dev-orchestrator/references/mission-contracts.md (intégralement — la nouvelle section reprend le patron de la section « Contrat `estimate:`/`actuals:` », lignes ~133-168 : champs optionnels frères, recopie verbatim, absents si l'amont ne les portait pas)
    - plugin/dev-orchestrator/agents/vf-coder.md (intégralement, 74 lignes — §Retour porte déjà le bloc typé et le patron `estimate:`/`actuals:`)
    - plugin/dev-orchestrator/agents/vf-dev-manager.md (intégralement, 236 lignes — §Contrôle de flux, ligne « Verdict d'étape », décrit déjà le gel de nœud sur `human_needed`)
    - plugin/dev-orchestrator/references/mission-flow.md §Pattern C (contrat de rapport typé — le vocabulaire de statut à ne pas diverger)
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh — blocs T14b (~1188-1204) et T14c (~1206-1229), patron de discriminance par mutation avec fixture injectée ; et la fin de fichier (T23, ~1672-1726) où le nouveau bloc s'ajoute
    - $HOME/.claude/gsd-core/references/checkpoints.md — règles 5 et 6, et le mode de défaillance nommé (un orchestrateur qui pilote sur le type seul auto-approuve le checkpoint que l'exécuteur venait de refuser)
    - $HOME/.claude/agents/gsd-executor.md lignes 145-155 et 325-335 — préconditions jamais auto-approuvées, refus d'auto-trancher un gate bloquant, escalade par le format de retour de checkpoint (NOTE : le fichier vit à `$HOME/.claude/agents/`, PAS sous `$HOME/.claude/gsd-core/agents/` — écart de citation n° 1 du RESEARCH)
  </read_first>
  <behavior>
    - Cas 1 : le mandat s'est déroulé sans checkpoint → le bloc typé ne porte pas `gate` (absent, jamais `null`, jamais une chaîne vide).
    - Cas 2 : le moteur a rendu un checkpoint avec `gate="blocking-human"` → `vf-coder` recopie la valeur verbatim et rend `statut: "human_needed"`.
    - Cas 3 : le moteur a refusé sur précondition non satisfaite, sans gate nommé → même statut `human_needed` que le cas 2 (une seule règle, deux motifs).
    - Cas 4 : `vf-dev-manager` lit un bloc typé portant un gate bloquant → il n'a aucun chemin qui mène à `dag.sh mark done` sur ce nœud.
  </behavior>
  <action>
Poser le contrat de checkpoint amont et le câbler aux deux bouts de la chaîne, puis le verrouiller
par un bloc de test discriminant. Slice vertical complet : référence → producteur → consommateur →
gate machine, un seul chemin (le champ `gate`), pas de champ supplémentaire dans cette tâche.

**1. `mission-contracts.md`** — ajouter une section `## Contrat de checkpoint amont (gsd-core 1.9.0)`
immédiatement APRÈS `## Contrat estimate:/actuals:` et AVANT `## Étage revue — deux objets
disjoints`. Elle déclare, sur le patron exact des champs optionnels frères déjà écrit pour
`estimate`/`actuals` :

- le champ optionnel `gate`, frère de `statut`/`findings`/`noeuds_debloques`, présent **dès qu'un
  checkpoint est survenu** pendant le mandat, portant la valeur rendue par l'amont **recopiée
  verbatim** ; absent quand aucun checkpoint n'est survenu ; jamais calculé, jamais déduit du type
  de checkpoint côté VibeFlow ;
- la **règle unique de mapping**, en une seule phrase : valeur `blocking-human` **OU** précondition
  amont non satisfaite ⇒ `statut: "human_needed"`. Écrire explicitement que c'est **une** règle
  pour **deux** motifs — un refus d'auto-approbation amont est un refus, quel qu'en soit le motif ;
- la raison, en une ligne, citée comme un FAIT amont et non reformulée (ADR-030) : le moteur refuse
  de trancher ce gate même sous auto-mode, et un orchestrateur qui piloterait sur le seul **type**
  de checkpoint annulerait ce refus d'un étage plus haut. Renvoyer aux sources
  (`gsd-core/references/checkpoints.md` règles 5/6 ; `$HOME/.claude/agents/gsd-executor.md`
  préconditions et refus d'auto-trancher) sans recopier leur texte.

Densité : ce fichier est le plus gros du module (16.4 K) — viser ≤ 20 lignes ajoutées, renvoi
plutôt que reformulation.

**2. `vf-coder.md` §Retour** — étendre le paragraphe « Calibration `estimate:`/`actuals:` » ou
ajouter une phrase sœur juste après : `gate` rejoint `estimate`/`actuals` comme champ optionnel
frère du bloc typé, avec la **même règle de recopie verbatim** et la même conditionnalité (absent
si l'amont ne l'a pas produit). Renvoyer à `mission-contracts.md` §Contrat de checkpoint amont
pour le contrat complet — ne pas le reformuler (ce fichier reste un routeur, pas une doctrine).

**3. `vf-dev-manager.md` §Contrôle de flux** — dans la puce « Verdict d'étape (rapport typé,
ADR-053) », nommer `gate` comme **déclencheur** de `human_needed`, avant la mécanique de gel déjà
écrite (« mode autonome : GELER le nœud porteur »). Une ligne, pas un paragraphe : la conduite
existe déjà, ce qui manquait était le nom du déclencheur. Budget de densité de cette tâche pour ce
fichier : **+2 lignes maximum** (236 lignes aujourd'hui, plafond ADR-029 à 250, et les plans 23-06
et 23-07 en réclament encore) — si le budget est dépassé, compresser une phrase existante de la
même puce en renvoi vers `mission-flow.md` §Pattern C plutôt que retirer du contenu.

**4. `test-dev-orchestrator.sh`** — ajouter un bloc `T24` en fin de fichier, après `T23` et avant
la ligne de résultat, sur le patron de commentaire et d'assertions des blocs existants
(`ok`/`ko`/`skip`, variable `t24_ok`, séparateur `# ---` de 75 tirets) :

- assertion A : `mission-contracts.md` porte le nom du champ ET la règle unique de mapping vers le
  statut d'escalade ;
- assertion B : `vf-coder.md` nomme le champ dans son §Retour ;
- assertion C : `vf-dev-manager.md` nomme le champ dans son contrôle de flux ;
- assertion D (**DISCRIMINANTE, par mutation**) : construire une copie temporaire de
  `vf-dev-manager.md` dans un `mktemp -d` (avec `trap ... EXIT` de nettoyage, invariant T21 du
  dépôt) d'où la mention du champ a été retirée, rejouer la fonction de détection dessus, et
  exiger qu'elle **échoue** sur cette copie tout en restant verte sur le fichier réel. Sans cette
  assertion, T24 serait une garde morte de plus (précédent documenté : les deux gardes mortes de
  `brick_routed()` remplacées par T14c).

Portabilité (ADR-054, `.planning/codebase/CONVENTIONS.md`) : passer par la variable `$GREP` déjà
résolue en tête de suite, pas de `grep -P`, pas de `sed -i` nu, pas de dépendance dure à `jq`.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et sa ligne de résultat mentionne au moins un `T24`.
    - `grep -c 'T24' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` retourne un nombre ≥ 4 (les 4 assertions).
    - `plugin/dev-orchestrator/references/mission-contracts.md` contient une section de titre commençant par `## Contrat de checkpoint amont`.
    - `grep -c 'human_needed' plugin/dev-orchestrator/references/mission-contracts.md` retourne ≥ 1.
    - `plugin/dev-orchestrator/agents/vf-coder.md` et `plugin/dev-orchestrator/agents/vf-dev-manager.md` contiennent chacun au moins une occurrence du nom du champ de gate.
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne une valeur ≤ 250 (ADR-029).
    - Preuve de mutation exécutée et consignée au SUMMARY : le retrait de la mention du champ dans la copie temporaire de `vf-dev-manager.md` fait passer la suite à au moins `1 KO` ; la restauration la ramène à `0 KO`.
    - `bash plugin/conductor/scripts/check-agents.sh --agents-dir plugin/dev-orchestrator/agents --strict` sort en 0 (ADR-044 — les deux agents touchés restent conformes).
  </acceptance_criteria>
  <done>Un `gate="blocking-human"` produit par le moteur remonte jusqu'au contrôle de flux du manager sous le statut `human_needed`, la chaîne est écrite aux trois étages et un test à discriminance prouvée par mutation empêche qu'un des trois bouts disparaisse en silence.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : le flag d'enchaînement autonome est désarmé au démarrage de mission et fermé par gate (D-02)</name>
  <files>plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §Discipline de pilotage (les quatre gestes non négociables actuels, lignes ~39-68) — le nouveau geste s'y insère, il ne crée pas une section
    - plugin/dev-orchestrator/references/mission-contracts.md §Seuil de bascule (vf-auto) — la cascade de résolution `gsd_run` y est **déjà écrite** : y renvoyer, ne jamais la recopier (D1/D5 de cette section, DRY)
    - plugin/dev-orchestrator/agents/vf-coder.md ligne 27 §Cadrage — le mode non-interactif y est prescrit sur la brique de cadrage ; c'est la forme qui reste **licite** et que le gate ne doit pas attraper
    - $HOME/.claude/gsd-core/workflows/discuss-phase/modes/chain.md — étapes 2, 4 et 5 : c'est ici que le flag est persisté dans `.planning/config.json` puis que la brique de plan est enchaînée
    - $HOME/.claude/gsd-core/references/checkpoints.md règle 5 — l'effet exact du flag une fois vrai
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh — bloc T14b (fixture injectée) et bloc T21 (invariants structurels sur scripts, patron `mktemp` + `trap`)
  </read_first>
  <behavior>
    - Cas 1 : mission qui démarre sur un lab dont `.planning/config.json` porte `workflow._auto_chain_active: true` → le manager le remet à `false` avant le premier dispatch.
    - Cas 2 : mission qui démarre sur un lab où la clé est absente → l'écriture reste idempotente et sans effet de bord (la clé est simplement posée à `false`).
    - Cas 3 : un fichier du module prescrit le mode d'enchaînement autonome sur la brique de plan ou d'exécution → le gate échoue.
    - Cas 4 : un fichier du module prescrit le mode non-interactif sur la brique de cadrage (état actuel de `vf-coder.md`) → le gate reste vert.
  </behavior>
  <action>
**1. `vf-dev-manager.md` §Discipline de pilotage** — ajouter un **cinquième geste non négociable**,
placé après le geste 4 (gate d'invariants) et donc après le verrou, avant le premier dispatch :
remettre `workflow._auto_chain_active` à `false` via `gsd_run config-set workflow._auto_chain_active false`.
Le motif tient en une phrase et doit rester un FAIT, jamais un conseil : ce flag est **persisté dans
`.planning/config.json`** (pas en mémoire de session) dès qu'un cadrage est lancé en mode
d'enchaînement, il **survit aux sessions**, et tant qu'il vaut vrai le moteur auto-approuve les
checkpoints de vérification humaine et auto-sélectionne la première option des checkpoints de
décision. Renvoyer à `mission-contracts.md` §Seuil de bascule pour la résolution de `gsd_run` —
**ne pas recopier la cascade** (elle y est déjà, DRY explicite de ce fichier). Le geste est
best-effort au sens du dépôt : si `gsd_run` ne se résout pas, le manager le **consigne au rapport**
et poursuit, il ne bloque pas la mission sur un défaut d'outillage.

Budget de densité pour ce fichier dans cette tâche : **+4 lignes maximum**.

**2. `test-dev-orchestrator.sh`** — ajouter un bloc `T25` après `T24`, avec deux volets :

- **volet présence** : `vf-dev-manager.md` porte bien le nom de la clé de configuration et l'appel
  qui la remet à faux ;
- **volet fermeture (le cœur)** : balayer les fichiers de `plugin/dev-orchestrator/agents/` et
  `plugin/dev-orchestrator/references/` et échouer si l'un d'eux prescrit le mode d'enchaînement
  autonome **sur la brique de plan ou sur la brique d'exécution**. Le motif doit rester **licite**
  sur la brique de cadrage : c'est la ligne 27 de `vf-coder.md` aujourd'hui, elle ne doit pas
  déclencher le gate. Construire la détection comme une fonction locale (patron `deleted_hits()`,
  ~ligne 138 de la suite) plutôt qu'un `grep` inline répété.

**Exclusion obligatoire du balayage** : `plugin/dev-orchestrator/scripts/tests/` — la fixture de
mutation contient par construction la forme interdite, et sans cette exclusion le gate se
déclencherait sur lui-même à chaque exécution (faux rouge permanent, puis contournement par
affaiblissement de l'assertion). Écrire l'exclusion et son motif en commentaire dans le bloc.

- **volet discriminance (par mutation)** : créer dans un `mktemp -d` (nettoyé par `trap ... EXIT`)
  deux fixtures — une portant la forme interdite sur la brique de plan, une portant la forme
  licite sur la brique de cadrage — et exiger que la fonction de détection remonte **exactement**
  la première et **pas** la seconde. Sans ce volet, une assertion qui ne peut structurellement pas
  distinguer les deux formes serait un faux vert.

Portabilité : `$GREP`, pas de `grep -P`, pas de `sed -i` nu (ADR-054).
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T25`.
    - `grep -c '_auto_chain_active' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne ≥ 1.
    - `grep -c 'config-set' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne ≥ 1.
    - `grep -c 'gsd_run' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne ≥ 1 et `grep -c 'RUNTIME_DIR' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne `0` (la cascade de résolution n'est PAS recopiée — renvoi seulement).
    - Le bloc `T25` contient un commentaire nommant l'exclusion de `scripts/tests/` et son motif.
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne une valeur ≤ 250.
    - Preuve de mutation consignée au SUMMARY : la fixture « forme interdite sur la brique de plan » est détectée, la fixture « forme licite sur la brique de cadrage » ne l'est pas ; et `vf-coder.md` inchangé laisse la suite à `0 KO`.
  </acceptance_criteria>
  <done>Une mission ne peut plus démarrer sur un flag d'enchaînement hérité, et aucun fichier de doctrine ou d'agent du module ne peut réintroduire ce flag sur les briques de plan ou d'exécution sans faire échouer la suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : minimum de reprise, halt de nœud, et qui répond aux attentes humaines (D-03, D-04, D-04bis)</name>
  <files>plugin/dev-orchestrator/references/mission-contracts.md, plugin/dev-orchestrator/agents/vf-coder.md, plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/references/mission-contracts.md — la section `## Contrat de checkpoint amont` créée en tâche 1 (elle s'étend ici, elle ne se duplique pas)
    - plugin/dev-orchestrator/agents/vf-coder.md §Cadrage (lignes 27-30) — le patron « une question de cadrage → statut d'escalade remonté au manager, JAMAIS auto-répondue en silence » existe déjà : le réutiliser mot pour mot dans sa forme, pas le réinventer
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §Entrée (lignes 21-26) — le filet de repli sur indisponibilité runtime d'`AskUserQuestion` est **déjà écrit** : D-04bis le réutilise tel quel, aucun mécanisme neuf
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §Contrôle de flux (lignes 152-167) — le gel de nœud en mode autonome est déjà décrit
    - $HOME/.claude/gsd-core/workflows/execute-phase.md — le garde-fou de reprise sûre (~:178-192, ses trois recours), le protocole de continuation (~:1093-1125) et la reprise par découverte de plans (~:1642) : LIRE pour comprendre ce que le moteur orchestre déjà, afin de ne PAS le recopier
    - $HOME/.claude/gsd-core/workflows/execute-plan.md ~:310-326 — le contrat de retour interne de l'exécuteur : à NE PAS reproduire côté VibeFlow (ADR-030, D-03 révisée)
    - plugin/conductor/references/team-kernel.md ligne 23 — l'allowlist est un contrat documenté, pas un cloisonnement runtime (chemin source réel : `references/`, pas `conductor-references/` — écart de citation n° 2 du RESEARCH)
  </read_first>
  <behavior>
    - Cas 1 : `gsd-execute-phase` s'arrête sur un checkpoint → le bloc typé porte `reprise` avec l'identifiant de plan, le type de checkpoint, le `gate` et l'attendu recopié verbatim ; il ne porte aucune table des tâches déjà faites.
    - Cas 2 : le garde-fou de reprise sûre du moteur attend un choix humain entre ses trois recours → même forme de retour que le cas 1 (c'est une attente humaine, pas un échec).
    - Cas 3 : mission autonome, gate bloquant sur le nœud d'exécution de l'étape N → le nœud reste figé, les nœuds sans lien de dépendance avec lui continuent d'être dispatchés.
    - Cas 4 : le manager doit répondre → il pose la question et redispatche un nouveau `vf-coder` avec l'attendu ; si l'outil de question est indisponible au runtime, il remonte le statut d'escalade au lieu d'auto-répondre.
  </behavior>
  <action>
**1. `mission-contracts.md`, section `## Contrat de checkpoint amont`** (créée en tâche 1) —
l'étendre de deux points, sans créer de section nouvelle :

- **Minimum de reprise (D-03)** : quand le statut est celui d'escalade humaine, le bloc typé porte
  un champ optionnel `reprise` dont les sous-champs sont exactement l'identifiant du plan
  (`plan_id`), le **type** de checkpoint amont (`checkpoint`), le `gate`, et l'**attendu**
  (`attendu`) — c'est-à-dire ce que le moteur déclare attendre, recopié verbatim. Écrire noir sur
  blanc la borne : **rien d'autre**, et en particulier **pas** la table des tâches déjà exécutées.
  Le motif est factuel et doit être écrit : le contrat de retour détaillé de l'exécuteur est un
  contrat **interne** au moteur, que le skill d'exécution orchestre déjà lui-même (il présente le
  checkpoint puis relance une continuation) ; le recopier côté VibeFlow dupliquerait un contrat
  amont, ce qu'ADR-030 interdit. Désigner ce contrat amont **par son rôle**, sans reproduire les
  intitulés de ses blocs.
- **Continuation (D-10, forme minimale ici)** : la reprise se fait en redispatchant un **nouveau**
  `vf-coder` avec le champ `reprise` ; le skill d'exécution reprend de lui-même au premier plan sans
  rapport de plan, et son garde-fou de reprise sûre refuse de relancer un exécuteur sur des commits
  de production orphelins en offrant trois recours. Une phrase, en constat — la doctrine de voie
  unique qui l'encadre est écrite au plan 23-05, y renvoyer une fois qu'elle existe.

**2. `vf-coder.md`** — ajouter, au §Retour, une phrase explicite sur le patron déjà utilisé au
§Cadrage : un checkpoint qui interrompt le cycle, ou le garde-fou de reprise sûre qui attend un
choix, produisent le statut d'escalade **plus** le champ `reprise` — **jamais** une réponse de
`vf-coder` lui-même. Rappeler le fait qui le fonde, en une incise : cet agent n'a pas d'outil de
question dans ses `tools:`, et un worker interne ne parle pas à l'utilisateur (team-kernel).

**3. `vf-dev-manager.md`** — deux touches d'une ligne chacune, dans des sections existantes :
- §Contrôle de flux, puce « Verdict d'étape » : nommer explicitement que le gate bloquant fige le
  **nœud porteur**, pas la mission, et que les branches sans lien de dépendance **continuent**
  (le comportement de gel est déjà écrit ; ce qui manque est de dire que le halt est de nœud) ;
- §Contrôle de flux ou §Entrée : c'est **le manager** qui répond aux attentes humaines du moteur
  (checkpoint et garde-fou de reprise sûre) — il pose la question puis redispatche avec l'attendu
  — avec le **même filet de repli** que celui déjà écrit au §Entrée si l'outil de question est
  indisponible au runtime. Renvoi, pas reformulation.

Budget de densité pour ce fichier dans cette tâche : **+3 lignes maximum**. Cumul de la tâche 1
(+2), de la tâche 2 (+4) et de celle-ci (+3) : le fichier doit rester **≤ 250 lignes** ; si le
plafond est atteint, compresser une phrase existante en renvoi vers `mission-flow.md` ou
`mission-contracts.md` — **jamais** supprimer une décision.

**4. `test-dev-orchestrator.sh`** — ajouter un bloc `T26` après `T25` :
- assertion A : `mission-contracts.md` nomme les quatre sous-champs du minimum de reprise ;
- assertion B : `vf-coder.md` porte la règle « attente humaine ⇒ escalade, jamais auto-répondue » ;
- assertion C : `vf-dev-manager.md` porte le halt **de nœud** et la réponse par le manager ;
- assertion D (**NÉGATIVE**) : aucun fichier de `plugin/dev-orchestrator/references/` ni
  `plugin/dev-orchestrator/agents/` ne reproduit les intitulés des blocs du contrat interne de
  l'exécuteur amont (garde anti-duplication ADR-030). Le balayage exclut `scripts/tests/`, pour le
  même motif qu'en T25 ;
- assertion E (**DISCRIMINANTE, par mutation**) : une fixture temporaire portant l'un de ces
  intitulés doit faire échouer l'assertion D.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
    <human-check>Relire les 3 fichiers de doctrine touchés et confirmer que le minimum de reprise est bien un minimum : rien qui ressemble à une sérialisation de l'état des tâches faites n'a été introduit côté VibeFlow (c'est exactement le point où le ROADMAP se trompait, corrigé par D-03 après vérification).</human-check>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T26`.
    - `plugin/dev-orchestrator/references/mission-contracts.md` contient les quatre chaînes `plan_id`, `checkpoint`, `gate`, `attendu`.
    - `grep -c 'ADR-030' plugin/dev-orchestrator/references/mission-contracts.md` retourne ≥ 1 (le motif de la borne est écrit, pas seulement la borne).
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne une valeur ≤ 250.
    - `wc -l < plugin/dev-orchestrator/agents/vf-coder.md` retourne une valeur ≤ 250.
    - `bash plugin/conductor/scripts/check-agents.sh --agents-dir plugin/dev-orchestrator/agents --strict` sort en 0.
    - Preuve de mutation de l'assertion D consignée au SUMMARY (fixture portant un intitulé de bloc amont → au moins `1 KO`).
  </acceptance_criteria>
  <done>Le bloc typé porte de quoi redispatcher sans rien dupliquer du moteur, le halt est explicitement de nœud et non de mission, la réponse aux attentes humaines est nominativement au manager, et un test négatif à discriminance prouvée empêche la duplication du contrat amont.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moteur `gsd-core` → `vf-coder` | Le `gate` et l'attendu sont produits en amont et traversent une frontière de confiance : VibeFlow les **relaie**, il ne les calcule jamais. Une valeur recalculée localement divergerait silencieusement à la prochaine version du moteur. |
| `vf-coder` → `vf-dev-manager` | Le bloc typé est le seul canal de contrôle de flux. Un champ perdu à ce passage est indiscernable d'un champ jamais produit. |
| `.planning/config.json` du lab → toutes les sessions | `workflow._auto_chain_active` est un état **persistant** écrit par le moteur : il franchit la frontière de session et modifie le comportement de checkpoints de sessions qui ne l'ont jamais demandé. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01-01 | Elevation of Privilege | contrôle de flux de `vf-dev-manager` en mode autonome | high | mitigate | Mapping unique `gate` bloquant OU précondition non satisfaite ⇒ statut d'escalade, écrit en doctrine **et** gaté par T24 avec discriminance prouvée par mutation. La doctrine seule serait un garde-fou inerte (motif exact du défaut SC3 de la Phase 19, passé au travers de trois étages de vérification). |
| T-23-01-02 | Tampering | `.planning/config.json` du lab cible | high | mitigate | Reset explicite de `workflow._auto_chain_active` au démarrage de mission (tâche 2) + gate T25 qui ferme la réintroduction du flag sur les briques de plan et d'exécution, avec exclusion documentée de `scripts/tests/` pour éviter l'auto-déclenchement. |
| T-23-01-03 | Repudiation | rapport typé d'un mandat interrompu | medium | mitigate | Champ `reprise` obligatoire quand le statut est celui d'escalade : un rapport ne peut plus taire qu'un checkpoint a eu lieu. C'est précisément ce que la doctrine seule ne prouvait pas (option écartée en D-01). |
| T-23-01-04 | Information Disclosure | duplication du contrat interne du moteur dans la doctrine VibeFlow | low | mitigate | Assertion négative T26-D + preuve par mutation : aucun fichier de référence ou d'agent ne reproduit les intitulés du contrat interne de l'exécuteur (ADR-030). |
| T-23-01-05 | Denial of Service | mission bloquée par un défaut d'outillage (`gsd_run` non résolu) | low | accept | Le geste de reset est best-effort et consigné au rapport : une mission n'échoue jamais sur l'absence de l'outil, patron déjà appliqué au fallback du §Seuil de bascule. |
| T-23-01-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : ce plan n'installe aucun paquet (RESEARCH §Package Legitimacy Audit — N/A pour toute la phase). |
</threat_model>

<verification>
- Suite du module verte : `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` → `0 KO`, avec T24, T25 et T26 nommés dans la sortie.
- Densité ADR-029 tenue : `wc -l` sur les deux agents touchés ≤ 250.
- ADR-044 : `bash plugin/conductor/scripts/check-agents.sh --agents-dir plugin/dev-orchestrator/agents --strict` → exit 0.
- Discriminance prouvée par **mutation** sur les trois blocs neufs, et la preuve (commande + compteur avant/après) consignée au SUMMARY. Un décompte vert sans mutation ne compte pas dans ce dépôt (trois précédents : Phase 13, Phase 17 cas 7, Phase 19 SC3).
- Aucun fichier hors `files_modified` n'a été touché.
</verification>

<success_criteria>
- SC1 — Le mapping `gate` → statut d'escalade existe en **une seule règle** couvrant les deux motifs de refus amont, et il est cité aux trois étages de la chaîne (référence, producteur, consommateur).
- SC2 — `workflow._auto_chain_active` est remis à faux en geste de démarrage de mission, et le module ne peut plus prescrire le mode d'enchaînement autonome sur les briques de plan ou d'exécution sans faire échouer la suite.
- SC3 — Le bloc typé porte un **minimum** de reprise et rien de plus : aucune duplication du contrat interne du moteur, gardée par une assertion négative discriminante.
- SC4 — Le halt sur gate bloquant est explicitement **de nœud**, et la réponse aux attentes humaines est nominativement portée par le manager.
- SC5 — Les trois blocs de test ajoutés ont leur discriminance **prouvée par mutation**, pas par lecture.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-01-SUMMARY.md` à la fin.
Y consigner explicitement : les commandes de mutation jouées et leurs compteurs avant/après, le
nombre de lignes de `vf-dev-manager.md` après édition, et le budget de densité restant pour les
plans 23-06 et 23-07.
</output>

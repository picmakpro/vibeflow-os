---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 07
type: execute
wave: 7
depends_on: ["38-06"]
files_modified:
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/_internal/runtime-adapter/register-codex-agent.sh
  - .planning/ROADMAP.md
autonomous: true
requirements: [CODEX-B4, CODEX-B5, CODEX-B6]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Après install de plusieurs modules posant plusieurs agents (root AGENT.md ET/OU agents/*.md) sur un runtime Codex détecté, EXACTEMENT autant de rôles .toml apparaissent sous $CODEX_HOME/agents/vibeflow/ qu'il existe d'agents source — jamais un seul par module, prouvé par `comm` sur listes triées de noms (frontmatter `name:`), jamais un comptage (CODEX-B4)"
    - "Un module à agent UNIQUE (root AGENT.md seul, sans agents/) continue de s'enregistrer correctement après le fix multi-agents — témoin de non-régression explicite, jamais seulement le cas multi-agent (CODEX-B4)"
    - "Après uninstall_module (et donc uninstall --all, qui l'appelle en boucle) d'un module ayant posé des rôles Codex, ces rôles .toml n'existent plus sous $CODEX_HOME/agents/vibeflow/ — jamais survivants comme mesuré le 2026-08-29 (CODEX-B5)"
    - "Le bruit runtime légitime de Codex (sessions/, cache/, log/, tmp/arg0/, *.sqlite*, models_cache.json) survit intact à cet uninstall — la preuve exclut EXPLICITEMENT ces catégories via comparaison de contenu, jamais un `rm -rf` aveugle de $CODEX_HOME entier (CODEX-B5)"
    - "Une install réelle sous runtime Codex détecté fait apparaître la ligne [fidelity-coexistence] dans le log d'install ET dans une invocation `status` séparée, SANS pré-semer manuellement .planning/config.json — la déclaration vient du chemin d'install lui-même, pas d'une fixture de test (CODEX-B6)"
    - "Une install sous runtime claude (aucune coexistence) ne fait apparaître AUCUNE ligne de coexistence, ni à l'install ni au status — témoin anti-parasite (CODEX-B6)"
    - "Le texte factuellement faux du gate de coexistence (plugin/conductor/scripts/check-artifact-fidelity.sh:175, hors périmètre fichier de ce plan) est consigné comme dette explicite dans .planning/ROADMAP.md avec le fait mesuré exact et un déclencheur de reprise nommé — jamais silencieusement oublié (CODEX-B6)"
  artifacts:
    - "plugin/_internal/vibeflow-update.sh : resolve_posed_agent_artifact() rend TOUS les artefacts posés (liste, un par ligne) ; report_artifact_fidelity()/register_codex_agent_if_applicable() itèrent sur cette liste ; unregister_codex_agent_if_applicable() câblée dans uninstall_module() ; find_runtime_registry() + record_codex_runtime_if_applicable() câblées dans install_module()/update_module()"
    - "plugin/_internal/runtime-adapter/register-codex-agent.sh : nouveau mode --remove, symétrique de la pose, sans dépendance à node ni au binaire codex"
    - "plugin/_internal/tests/test-vibeflow-update.sh : nouveaux tests CODEX-B4/B5/B6, preuve par comm sur ensembles, rouge→vert documenté"
    - ".planning/ROADMAP.md : entrée de dette (D-38-<suivant>) consignant le texte faux de check-artifact-fidelity.sh:175 et la conversion hooks.json non livrée, avec déclencheur de reprise"
  key_links:
    - "resolve_posed_agent_artifact() -> report_artifact_fidelity()/register_codex_agent_if_applicable() : contrat partagé changé de 'un chemin' à 'liste de chemins, un par ligne' — les DEUX appelants doivent boucler symétriquement, sinon la bannière FIDE casse silencieusement sur un artefact multi-lignes traité comme un seul argument positionnel"
    - "register-codex-agent.sh --remove -> uninstall_module() (appelé AVANT la suppression des artefacts source par _vf_uninstall_from_manifest/_vf_uninstall_from_cache) : l'ordre est impératif, resolve_posed_agent_artifact a besoin des fichiers .md source ENCORE présents sous $TARGET_ROOT/agents/ pour dériver quels .toml retirer"
    - "record_codex_runtime_if_applicable() -> runtime-registry.sh set-active (invoqué en CLI, jamais édité) -> .planning/config.json (vf_runtimes.installed) -> check-artifact-fidelity.sh --coexistence-report (déjà câblé en 38-06, jamais édité) : la déclaration devient non-vide sans toucher aux deux fichiers hors périmètre plugin/conductor/**"
  prohibitions:
    - "Ne jamais éditer plugin/conductor/** ni plugin/_internal/runtime-adapter/agent-to-codex.mjs (dépendances d'autres workers actifs sur ce même worktree partagé) — invoquer leurs CLI existants tel quels, jamais modifier leur contenu"
    - "Ne jamais dupliquer la dérivation de AGENT_NAME (sed sur le frontmatter name:) dans vibeflow-update.sh — register-codex-agent.sh (pose ET retrait) reste l'UNIQUE source de vérité pour ce calcul"
    - "Ne jamais prouver CODEX-B4/B5 par un comptage (wc -l, nombre de fichiers) — comparaison d'ensembles via comm sur listes triées uniquement"
    - "Ne jamais git add -A / git add . / git stash sur ce worktree partagé par 3 workers en parallèle — chemins explicites uniquement à chaque commit ; git diff --stat avant chaque commit pour vérifier l'ampleur réelle vs attendue"
    - "Ne jamais écrire dans le vrai ~/.codex ni le vrai $HOME pendant les tests — CODEX_HOME/HOME isolés sous mktemp -d, garde-fou final du fichier de test revérifié intact"
    - "Ne jamais utiliser timeout/gtimeout dans les scripts de test (absents sur cette machine, échouent en silence) — jamais de process background laissé sans supervision"
---

<objective>
Corrige trois bloquants mesurés en conditions réelles le 2026-08-29 sur une install Codex de bout en
bout (38-CONTEXT.md lignes 1116-1164), tous dans `plugin/_internal/vibeflow-update.sh` et son
adaptateur `runtime-adapter/register-codex-agent.sh` : (CODEX-B4) `resolve_posed_agent_artifact()`
ne rend qu'UN artefact par module, si bien que le team-kernel entier (7 agents sur 12 posés,
`vf-dev-manager`, `vf-coder`, `vf-reviewer`, `vf-auditer`, `vf-crafter`, `vf-design-manager`,
`vf-design-judge`) n'est jamais enregistré côté Codex ; (CODEX-B5) `uninstall`/`uninstall --all` ne
retire aucun rôle `.toml` posé, laissant Codex charger des rôles orphelins ; (CODEX-B6) la
déclaration de coexistence sans hooks (MIGR-05, déjà livrée en 38-06) ne se déclenche jamais en
conditions réelles faute d'écriture dans le registre de runtime au moment de l'install/status, et le
texte qu'elle porte est factuellement faux (Codex a une surface de hooks prouvée exécutante — cette
seconde moitié est corrigée par consignation de dette, PAS par édition directe : le fichier porteur
appartient à `plugin/conductor/**`, hors périmètre de ce plan, où un autre worker corrige déjà un
défaut différent du même gate sur ce même worktree partagé).

**HORS PÉRIMÈTRE de ce plan** (traités en parallèle par d'autres workers sur ce même worktree, ne
jamais y toucher ni les re-corriger ici) : les bloquants 1 (`model = "opus"/"sonnet"` recopié
littéralement), 2 (`description: >` non géré) et 3 (le gate de fidélité mesurait un artefact jamais
posé — DÉJÀ CORRIGÉ, commit `9af7a4b`) de la même mesure, ainsi que les trois défauts de second rang
(`codex plugin install`/`marketplace add --scope` inexistants, budget de contexte skills dépassé,
`--verify` jamais appelé dans le chemin d'install). Ne pas les re-dériver, ne pas les re-corriger.

**Convention d'identifiants** : ce mandat vient d'une mesure post-hoc, sans ID `REQUIREMENTS.md`
dédié. Les trois bloquants sont référencés `CODEX-B4`, `CODEX-B5`, `CODEX-B6` (numérotation reprise
telle quelle de la liste mesurée à six éléments de 38-CONTEXT.md, items 4/5/6).

**Exécution sur worktree partagé** : ce worktree (`feat/phase-38-portabilite-multi-runtime`) est
travaillé en parallèle par 3 workers. Chemins explicites à chaque `git add`/`git commit` — JAMAIS
`git add -A`/`git add .`, JAMAIS `git stash`. `git diff --stat` avant chaque commit pour confirmer
que seuls les fichiers de `files_modified` de ce plan ont bougé.

Purpose: sans ces trois correctifs, tout ce que les lots précédents (30→06) ont rendu ATTEIGNABLE sur
Codex (installer, migrer, faire coexister) reste démenti par la mesure réelle : le canal d'install
pose 12 agents mais n'en enregistre que 5, ne défait jamais ce qu'il pose, et n'annonce jamais qu'il
coexiste avec Claude.

Output: `resolve_posed_agent_artifact()` corrigée + ses deux appelants mis en boucle, mode `--remove`
sur `register-codex-agent.sh` câblé dans `uninstall_module()`, écriture du registre de runtime câblée
dans `install_module()`/`update_module()`, entrée de dette ROADMAP pour le texte faux hors périmètre.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/_internal/runtime-adapter/register-codex-agent.sh
</context>

<!-- planner-discipline-allow: coexistence -->
<!-- "coexistence" is the real gate name (--coexistence-report) and marker text
     ([fidelity-coexistence]) this plan wires up in Tache 3 — it legitimately appears
     in <action> bodies, not as an accidental echo of the negative-grep literal used
     in <verification>/<acceptance_criteria> to prove the anti-parasite witness. -->

<tasks>

<task type="tracer" tdd="true">
  <name>Tache 1 : resolve_posed_agent_artifact() rend TOUS les agents d'un module, pas le premier (CODEX-B4)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 1116-1152 (mesure de bout en bout 2026-08-29 : « Install réelle... 309 chemins posés (14 skills, 12 agents .md, 5 rôles .toml)... 7 agents sur 12 jamais enregistrés — resolve_posed_agent_artifact() ne rend qu'un artefact par module. Les absents sont exactement le team-kernel »)
    - plugin/_internal/vibeflow-update.sh lignes 1480-1518 (`resolve_posed_agent_artifact()` : le `if [ -f AGENT.md ] ... elif [ -d agents ]` traite les deux sources comme mutuellement exclusives — un module qui a LES DEUX (dev-orchestrator : `AGENT.md` racine + 4 fichiers sous `agents/`) ne fait jamais tomber le `elif`, donc les 4 agents de son `agents/` ne sont jamais vus. Même défaut pour design-orchestrator (`AGENT.md` racine + 3 sous `agents/`). `report_artifact_fidelity()` juste en dessous consomme la MÊME fonction avec le MÊME contrat de retour à corriger en miroir)
    - plugin/_internal/vibeflow-update.sh lignes 1520-1563 (`find_codex_registrar()` + `register_codex_agent_if_applicable()` : second appelant de `resolve_posed_agent_artifact`, même contrat à mettre à jour)
    - plugin/_internal/vibeflow-update.sh lignes 1381-1386 (`find_runtime_dispatch_lib()`, déjà utilisée par `register_codex_agent_if_applicable` — ne pas dupliquer)
    - plugin/_internal/tests/test-vibeflow-update.sh lignes 1419-1464 (T37/T38 : patron déjà établi — `HOME=$FAKE_HOME_CODEX VF_RUNTIME=codex`, `prepare_module`, assertions sur `$FAKE_HOME_CODEX/.codex/agents/vibeflow/*.toml` — à réutiliser tel quel, ne pas inventer un second patron d'isolation)
    - plugin/dev-orchestrator/AGENT.md ligne 2 (`name: vibeflow-dev`) et `plugin/dev-orchestrator/agents/{vf-auditer,vf-coder,vf-dev-manager,vf-reviewer}.md` (frontmatter `name:` = nom de fichier, sauf le root dont le `name:` diverge du nom de fichier posé) — ces 5 fichiers sont la source du module dev-orchestrator
    - plugin/design-orchestrator/AGENT.md ligne 2 (`name: vibeflow-design`) et `plugin/design-orchestrator/agents/{vf-crafter,vf-design-judge,vf-design-manager}.md` — les 4 fichiers du module design-orchestrator
    - plugin/validator/AGENT.md ligne 2 (`name: vibeflow-validator`) — module à agent UNIQUE (root seul, aucun `agents/`), candidat de non-régression
  </read_first>
  <behavior>
    - Test : install de `dev-orchestrator` + `design-orchestrator` + `validator` sous runtime `codex` détecté (isolation HOME) — l'ensemble TRIÉ des noms `.toml` posés sous `agents/vibeflow/` est identique (comm -3 vide) à l'ensemble TRIÉ des `name:` frontmatter des 10 fichiers source (`vibeflow-dev`, `vf-auditer`, `vf-coder`, `vf-dev-manager`, `vf-reviewer`, `vibeflow-design`, `vf-crafter`, `vf-design-judge`, `vf-design-manager`, `vibeflow-validator`).
    - Test (RED avant fix) : contre le code actuel, `comm -3` sur les deux mêmes ensembles montre EXACTEMENT les 7 noms du team-kernel manquants côté posé (`vf-auditer`, `vf-coder`, `vf-dev-manager`, `vf-reviewer`, `vf-crafter`, `vf-design-judge`, `vf-design-manager`) — écrire ce test avant d'éditer `resolve_posed_agent_artifact()`, constater l'échec avec cette trace précise, puis implémenter le fix.
    - Test (témoin de non-vacuité, dans la MÊME assertion) : `vibeflow-validator` (module à agent unique, root seul) est présent dans l'ensemble posé — si le fix du cas multi-agents avait régressé le cas simple, ce nom manquerait alors que les 9 autres seraient corrects ; distinguer explicitement ce cas dans le message d'échec s'il se produit.
  </behavior>
  <action>
Réécrire `resolve_posed_agent_artifact()` (1485-1501) : au lieu du `if [ -f "$module_dir/AGENT.md" ]
... elif [ -d "$module_dir/agents" ]` actuel qui traite les deux sources comme mutuellement
exclusives, vérifier les DEUX conditions INDÉPENDAMMENT et cumuler leurs résultats dans une liste :
(1) si `$module_dir/AGENT.md` existe, ajouter `$TARGET_ROOT/agents/${mod}.md` à la liste SI ce
chemin existe réellement sur le disque cible (même garde d'existence qu'aujourd'hui) ; (2)
SÉPARÉMENT (jamais en `elif`), si `$module_dir/agents` est un répertoire, itérer TOUS les `*.md`
qu'il contient (retirer le `break` de la boucle `for first in ... do ... break; done` actuelle — il
ne doit itérer QUE le premier fichier aujourd'hui, c'est exactement le défaut mesuré) et ajouter
`$TARGET_ROOT/agents/$(basename "$file")` à la liste pour chaque fichier qui existe réellement sur
le disque cible. Émettre le résultat sur stdout, un chemin par ligne. Conserver le contrat de code
retour existant : retour 1 (stdout vide) si la liste est vide, retour 0 sinon — les deux appelants
gardent leur garde `|| return 0` inchangée. Mettre à jour le commentaire de la fonction (1480-1484)
pour documenter le contrat corrigé et citer la mesure CODEX-B4 (38-CONTEXT.md:1149-1152).

Mettre à jour `report_artifact_fidelity()` (1509-1518) pour consommer ce nouveau contrat
multi-lignes : lire la sortie de `resolve_posed_agent_artifact` ligne par ligne (boucle `while
IFS= read -r artifact; do [ -n "$artifact" ] || continue; ... done` sur la variable capturée, jamais
sur une substitution de commande directe qui romprait les lignes), et invoquer `bash "$gate" --target
codex "$artifact" || true` une fois PAR artefact au lieu d'une fois pour l'ancien retour unique.

Mettre à jour `register_codex_agent_if_applicable()` (1543-1563) de façon symétrique : même boucle
ligne par ligne, `bash "$registrar" "$artifact" 2>&1` invoqué une fois par artefact, chaque digest
relayé préfixé `[codex-adapter] ` exactement comme aujourd'hui — seule la boucle change, pas le
format de relai.

Ajouter le test décrit dans `<behavior>` à `plugin/_internal/tests/test-vibeflow-update.sh`, ajouté
APRÈS le dernier test numéroté du fichier (inspecter le fichier à l'exécution pour trouver le
prochain numéro libre — d'autres workers de ce même worktree partagé peuvent avoir ajouté des tests
entre-temps, ne jamais coder en dur un numéro qui pourrait entrer en collision). Ne pas modifier
T37/T38 (couverture orthogonale, toujours valide, pour la branche runtime≠codex et pour la preuve
qu'AU MOINS un artefact se pose).
  </action>
  <acceptance_criteria>
    - Avant le fix (état actuel constaté par le test) : `comm -3` entre l'ensemble source (10 noms) et l'ensemble posé montre exactement les 7 noms du team-kernel — trace capturée dans la sortie du test.
    - Après le fix : `comm -3` entre les deux mêmes ensembles triés est VIDE — aucune ligne, ni manquante ni excédentaire.
    - `vibeflow-validator` apparaît dans l'ensemble posé après le fix (témoin de non-régression du cas mono-agent).
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort avec 0 KO.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>resolve_posed_agent_artifact() rend un artefact par agent source réel (root ET agents/*.md cumulés), ses deux appelants itèrent dessus, et un module multi-agents (dev/design-orchestrator) enregistre son team-kernel complet côté Codex sans régresser le cas mono-agent (validator).</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : uninstall retire réellement les rôles Codex posés (CODEX-B5)</name>
  <files>plugin/_internal/runtime-adapter/register-codex-agent.sh, plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 1153-1155 (« uninstall ne retire AUCUN rôle Codex — les 5 .toml survivent, Codex continue de charger des rôles pointant sur des agents supprimés. L'adaptateur est câblé dans install_module() et update_module(), JAMAIS dans uninstall_module() »)
    - 38-CONTEXT.md ligne 363 (table doctrine de pose/désinstallation : `$CODEX_HOME/agents/vibeflow/*.toml` -> défaire par `rm -rf $CODEX_HOME/agents/vibeflow/`, « atomique, sans parsing » — doctrine pensée pour un retrait GLOBAL de tout ce que VibeFlow a posé côté Codex. Ce plan en DÉVIE délibérément pour `uninstall_module()` d'un SEUL module : un `rm -rf` du répertoire entier supprimerait AUSSI les rôles d'autres modules encore installés en coexistence — retrait rôle par rôle à la place, `uninstall --all` obtient le MÊME état final en bouclant `uninstall_module` sur chaque module, sans ce risque de sur-suppression)
    - plugin/_internal/runtime-adapter/register-codex-agent.sh, TOUT le fichier (149 lignes, déjà chargé en `<context>`) — dérivation d'`AGENT_NAME` (lignes 80-85 : `sed -n 's/^name:[[:space:]]*//p' "$AGENT_MD" | head -1 | tr -d '[:space:]'`) et de `ROLE_TOML="$AGENTS_DIR/${AGENT_NAME}.toml"` (ligne 87) — c'est CETTE dérivation qui doit être réutilisée à l'identique pour le retrait, jamais recopiée dans vibeflow-update.sh
    - plugin/_internal/vibeflow-update.sh lignes 1520-1563 (`find_codex_registrar()` — cascade à réutiliser telle quelle, aucune seconde cascade) et lignes 1543-1563 (`register_codex_agent_if_applicable()` — patron de fonction symétrique à reproduire pour le retrait, MOINS la garde `runtime = codex` : un rôle orphelin doit être nettoyé même si le runtime détecté AU MOMENT DE L'UNINSTALL n'est plus codex)
    - plugin/_internal/vibeflow-update.sh lignes 2589-2630 (`uninstall_module()` — `backup_module "$mod"` à la ligne 2592, PUIS la lecture du manifeste qui déclenche la suppression réelle des artefacts source. Le point d'insertion est IMPÉRATIVEMENT entre les deux : `resolve_posed_agent_artifact` a besoin des `.md` source ENCORE présents sous `$TARGET_ROOT/agents/`)
    - plugin/_internal/vibeflow-update.sh lignes 2952-2967 (dispatcher principal, `uninstall --all` boucle déjà `uninstall_module "$mod"` par module installé — aucun câblage séparé nécessaire pour ce chemin, à VÉRIFIER par test plutôt qu'à recoder)
    - plugin/_internal/tests/test-vibeflow-update.sh lignes 1940-1955 (garde-fou final `~/.codex/agents/vibeflow` intact — ne jamais faire fuiter l'isolation de ce nouveau test dans le vrai `$HOME`)
  </read_first>
  <behavior>
    - Test : install de `dev-orchestrator` + `design-orchestrator` sous runtime codex (isolation HOME, dépend du fix de la Tâche 1 déjà appliqué dans ce même plan) -> 9 `.toml` posés. `uninstall --all` (commande EXACTE mesurée dans le bloquant). Après : `agents/vibeflow/` ne contient plus AUCUN `.toml` — prouvé par comm sur l'ensemble des noms posés avant vs après (ensemble vide après), jamais par un compte.
    - Test (résidus légitimes, seedés AVANT install) : `sessions/probe.json`, `cache/probe`, `log/probe.log`, `tmp/arg0/probe`, `models_cache.json`, `memories_1.sqlite` sous `$CODEX_HOME` — tous survivent à l'uninstall, contenu identique (comparaison de contenu, pas seulement d'existence).
    - Test (RED avant fix) : contre le code actuel, les `.toml` survivent à `uninstall --all` — capturer cette trace avant d'implémenter le câblage.
  </behavior>
  <action>
Étendre `register-codex-agent.sh` avec un nouveau drapeau `--remove`, utilisable avec l'argument
positionnel `<agent.md>` existant et l'option `--codex-home` existante (incompatible avec
`--verify` : rejeter les deux ensemble avec un message d'usage explicite). Quand `--remove` est
présent : NE PAS appeler `node "$CONVERTER"` (le retrait ne doit jamais dépendre de `node` étant
disponible). Dériver `AGENT_NAME` EXACTEMENT comme le fait déjà la pose (même ligne `sed`), calculer
`ROLE_TOML` de la même façon. Si `$ROLE_TOML` existe, `rm -f` et imprimer
`[register-codex-agent] rôle retiré : $ROLE_TOML` ; s'il n'existe pas, imprimer
`[register-codex-agent] rôle déjà absent (rien à retirer) : $ROLE_TOML` — dans les deux cas, exit 0
(idempotent, jamais fatal, même doctrine « atomique, sans parsing » que 38-CONTEXT.md:363, appliquée
par rôle plutôt qu'en `rm -rf` du répertoire entier — voir read_first pour la justification de cet
écart). Ne jamais créer `$AGENTS_DIR` s'il est absent (rien à retirer d'un répertoire qui n'a jamais
existé).

Dans `vibeflow-update.sh`, ajouter `unregister_codex_agent_if_applicable()` directement après
`register_codex_agent_if_applicable()` (après la ligne 1563) : signature `(mod, module_dir)`, ouvre
par `vf_dry_run && return 0` (garde défensive symétrique, même si `uninstall_module()` n'expose pas
aujourd'hui de mode dry-run — coûte rien, protège un futur ajout), résout la liste via
`resolve_posed_agent_artifact "$module_dir" "$mod"` (`|| return 0` si vide — rien à désenregistrer),
résout le registrar via `find_codex_registrar` EXISTANTE (aucune seconde cascade). CONTRAIREMENT à
`register_codex_agent_if_applicable`, NE PAS filtrer sur le runtime détecté au moment de l'appel —
un `.toml` orphelin d'un install Codex passé doit être nettoyé même si le runtime résolu maintenant
est `claude` (documenter explicitement cette différence dans le commentaire, avec la justification :
gater sur le runtime courant laisserait survivre exactement les orphelins mesurés par CODEX-B5).
Boucler sur chaque ligne de la liste, appeler `bash "$registrar" "$artifact" --remove 2>&1`, relayer
préfixé `[codex-adapter] ` comme la pose.

Câbler l'appel dans `uninstall_module()` : insérer
`unregister_codex_agent_if_applicable "$mod" "$CACHE_DIR/$mod"` immédiatement après
`backup_module "$mod"` (ligne 2592) et AVANT la lecture du manifeste qui suit — documenter cette
contrainte d'ordre dans un commentaire adjacent, même densité documentaire que le bloc déjà présent
au-dessus de `uninstall_module` (2597-2608). Ne rien câbler de séparé pour `uninstall --all`
(2952-2967) : il boucle déjà `uninstall_module` par module, le test doit le vérifier tel quel plutôt
que d'ajouter un second point d'appel.

Ajouter les tests décrits dans `<behavior>`, même discipline de numérotation que la Tâche 1 (prochain
numéro libre inspecté à l'exécution, jamais codé en dur).
  </action>
  <acceptance_criteria>
    - Avant le fix : `uninstall --all` sur un lab avec 9 `.toml` posés laisse les 9 `.toml` intacts (trace capturée).
    - Après le fix : `uninstall --all` sur le même scénario laisse `agents/vibeflow/` sans AUCUN `.toml` (comm sur ensembles, jamais un compte).
    - Les 6 fichiers de résidus légitimes seedés (sessions/, cache/, log/, tmp/arg0/, models_cache.json, memories_1.sqlite) existent toujours après l'uninstall, contenu inchangé.
    - Le garde-fou final du fichier de test (`~/.codex/agents/vibeflow` réel intact) reste vert.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort avec 0 KO.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>uninstall_module() (et donc uninstall --all) retire réellement chaque rôle Codex que le module désinstallé avait posé, sans toucher aux résidus runtime légitimes de Codex ni au vrai ~/.codex de la machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : coexistence sans hooks réellement déclarée en conditions réelles + dette du texte faux consignée (CODEX-B6)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh, .planning/ROADMAP.md</files>
  <read_first>
    - 38-CONTEXT.md lignes 1156-1164 (« Hooks — surface EXISTANTE et prouvée exécutante, non visée... l'install merge dans settings.json, que Codex n'exécute pas. Et la déclaration de perte ne sort jamais (grep -c 'coexistence' = 0 à l'install et au status) — MIGR-05 est INERTE en conditions réelles, et son texte prévu (« aucun mécanisme équivalent mesuré ») est factuellement faux »)
    - plugin/conductor/scripts/check-artifact-fidelity.sh lignes 152-178 (lecture seule — NE PAS ÉDITER, module hors périmètre de ce plan, un autre worker y corrige un défaut différent en parallèle sur ce même worktree) : le mode `--coexistence-report` lit `runtime-registry.sh list-installed --config .planning/config.json`, et pour chaque runtime installé ≠ `claude` imprime `[fidelity-coexistence] $_rt : opère SANS gouvernance de hooks (aucun mécanisme équivalent mesuré à ce jour...)` (ligne 175, LE TEXTE FAUX à consigner en dette, pas à corriger ici) ; résout `runtime-registry.sh` par co-localisation dans le MÊME répertoire que lui-même (lignes 163-167)
    - plugin/conductor/scripts/runtime-registry.sh, TOUT le fichier (lecture seule) : `get-active`/`list-installed`/`set-active <runtime> [--confirmed|--dry-run]` — SEUL verbe d'écriture disponible, il modifie À LA FOIS `vf_runtimes.active` (= le runtime passé) ET étend `vf_runtimes.installed` (jamais un verbe plus étroit qui n'ajouterait qu'à `installed` sans toucher `active` — ce fichier appartient à `plugin/conductor/**`, hors périmètre, ce plan ne peut donc PAS lui ajouter un tel verbe)
    - plugin/_internal/vibeflow-update.sh lignes 1473-1478 (`find_fidelity_gate()` — cascade à copier pour `find_runtime_registry()`, MÊMES deux positions canoniques puisque `runtime-registry.sh` est co-localisé avec `check-artifact-fidelity.sh`)
    - plugin/_internal/vibeflow-update.sh lignes 2206-2224 (bannière fidélité + adaptateur Codex + bloc coexistence-report DÉJÀ câblé en fin d'`install_module()` — la nouvelle écriture doit s'insérer ENTRE l'appel `register_codex_agent_if_applicable` (2214) et ce bloc coexistence-report (2215-2224), pour que ce dernier lise l'état FRAÎCHEMENT écrit)
    - plugin/_internal/vibeflow-update.sh lignes 2663-2673 (`show_status()` — même gate, même convention cwd-relative pour `.planning/config.json`, « aucun --config forcé ici » : à respecter à l'identique pour le nouvel appel d'écriture)
    - plugin/_internal/vibeflow-update.sh lignes 2888-2895 (`update_module()`, 2e couture symétrique — même câblage à ajouter juste après son propre appel à `register_codex_agent_if_applicable`)
    - plugin/_internal/tests/test-vibeflow-update.sh lignes 1911-1938 (T48 — preuve que le gate DÉCLARE quand le registre est DÉJÀ pré-semé manuellement ; ce plan ne modifie PAS T48, il ajoute une preuve DISTINCTE que l'écriture arrive désormais depuis le chemin d'install lui-même, sans pré-semage)
    - .planning/ROADMAP.md lignes 1142-1163 (patron des entrées « Dette nommée » existantes D-38-K/D-38-Q — même style, même emplacement en fin de section Phase 38, pour l'entrée de dette de cette tâche)
  </read_first>
  <behavior>
    - Test (écriture réelle, sans pré-semage) : lab frais, `.planning/config.json` minimal `{}` (jamais de `vf_runtimes` pré-écrit). Install d'un module à agent sous runtime codex détecté -> la sortie d'install ET une invocation `status` séparée contiennent toutes deux `[fidelity-coexistence] codex : opère SANS gouvernance de hooks` ; `.planning/config.json` contient désormais `"codex"`.
    - Test (RED avant fix) : contre le code actuel, ce même scénario (sans pré-semage) ne produit AUCUNE ligne `coexistence` ni à l'install ni au status — capturer cette trace avant le câblage.
    - Test (témoin anti-parasite) : même scénario, runtime `claude` explicite -> `grep -c 'coexistence'` = 0 sur l'install ET le status.
  </behavior>
  <action>
Ajouter `find_runtime_registry()` directement après `find_fidelity_gate()` (après la ligne 1478),
même forme de cascade à deux positions (`$TARGET_ROOT/scripts/runtime-registry.sh` puis
`$CACHE_DIR/conductor/scripts/runtime-registry.sh`), silence total (chaîne vide) si absent aux deux
positions — même doctrine best-effort que toutes les cascades voisines de ce fichier.

Ajouter `record_codex_runtime_if_applicable()` juste après `register_codex_agent_if_applicable()`
(après la ligne 1563, à côté d'`unregister_codex_agent_if_applicable` de la Tâche 2) : ouvre par
`vf_dry_run && return 0` ; résout la liste via `resolve_posed_agent_artifact "$module_dir" "$mod"`
(`|| return 0` si vide) ; résout le runtime via la MÊME cascade `find_runtime_dispatch_lib` +
`detect_agent_runtime` que `register_codex_agent_if_applicable`, gate IDENTIQUE sur `runtime = codex`
(zéro effet sur le chemin claude, seule raison pour laquelle cette écriture est sûre à automatiser) ;
exige `[ -f ".planning/config.json" ]` avant toute tentative (même convention cwd-relative que
`show_status`, un lab non suivi par GSD n'a rien à enregistrer) ; résout `runtime-registry.sh` via
`find_runtime_registry`, silence si absent. Invoque `bash "$registry" set-active codex --confirmed
2>&1` (chemin de config implicite `.planning/config.json`, AUCUN `--config` forcé, même convention
que `show_status`), relaie la sortie préfixée `[runtime-registry] `. Documenter explicitement dans le
commentaire de la fonction que cet appel modifie `vf_runtimes.active` (pas seulement `installed[]`
— `runtime-registry.sh` n'expose aucun verbe plus étroit et ce plan ne peut pas lui en ajouter un,
fichier hors périmètre) et que c'est un effet de bord DÉLIBÉRÉ, jamais un oubli.

Câbler l'appel dans `install_module()` : `record_codex_runtime_if_applicable "$mod" "$module_dir"`
immédiatement après `register_codex_agent_if_applicable "$mod" "$module_dir"` (ligne 2214) et AVANT
le bloc coexistence-report déjà présent (2215-2224) — ordre impératif, ce bloc doit lire l'état
fraîchement écrit. Même câblage dans `update_module()`, juste après son propre appel à
`register_codex_agent_if_applicable "$mod" "$CACHE_DIR/$mod"` (ligne 2895).

Ajouter les tests décrits dans `<behavior>`, même discipline de numérotation que les tâches
précédentes.

Ajouter à `.planning/ROADMAP.md` une nouvelle entrée « Dette nommée » à la fin de la section Phase 38
(après l'entrée D-38-Q, lignes 1152-1163), même style, prochain identifiant `D-38-` libre (inspecter
le fichier à l'exécution, ne pas coder en dur — un autre worker de ce worktree peut avoir pris un
identifiant entre-temps). Contenu : (1) fait mesuré exact — `plugin/conductor/scripts/check-artifact-fidelity.sh:175`
affirme qu'un runtime coexistant « opère SANS gouvernance de hooks (aucun mécanisme équivalent mesuré
à ce jour) », affirmation DÉMENTIE par la mesure du 2026-08-29 : Codex 0.150.1 porte une surface de
hooks PROUVÉE exécutante (un `$CODEX_HOME/hooks.json` en forme stricte Claude Code a réellement
déclenché un hook sous `--dangerously-bypass-hook-trust`), un SUR-ENSEMBLE des événements Claude, et
un importeur natif `settings.json -> hooks.json` ; le vrai trou est que l'install VibeFlow merge dans
`settings.json`, jamais lu par Codex, au lieu de `hooks.json` ; (2) raison du non-traitement direct
dans CE plan — le fichier porteur du texte vit sous `plugin/conductor/**`, explicitement hors
périmètre de ce plan car un autre worker corrige en parallèle un défaut DIFFÉRENT du même gate sur ce
même worktree (commit `9af7a4b` déjà posé pour un autre défaut du même fichier) ; (3) déclencheur de
reprise explicite — corriger le texte de la ligne 175 dès que ce worker livre sa correction (remplacer
la négation par : Codex expose une surface de hooks prouvée exécutante, non ENCORE ciblée par
l'install VibeFlow qui merge dans `settings.json` au lieu de `hooks.json`), et/ou planifier une
conversion réelle `settings.json -> hooks.json` comme geste dédié, jamais promise avant d'être livrée.
  </action>
  <acceptance_criteria>
    - Avant le fix : install + status sur un lab sans pré-semage (`.planning/config.json` = `{}`) sous runtime codex ne produit AUCUNE ligne `coexistence` (trace capturée).
    - Après le fix : la MÊME séquence produit la ligne `[fidelity-coexistence] codex : opère SANS gouvernance de hooks` à l'install ET au status, et `.planning/config.json` contient `"codex"`.
    - Le MÊME scénario sous runtime `claude` explicite : `grep -c 'coexistence'` = 0 sur l'install ET le status, dans les deux cas (avant et après le fix — non-régression jamais rompue).
    - `.planning/ROADMAP.md` porte une nouvelle entrée « Dette nommée » citant `check-artifact-fidelity.sh:175` verbatim, le fait mesuré, la raison du non-traitement, et un déclencheur de reprise nommé.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort avec 0 KO.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <done>Une install/status Codex réelle déclare sa coexistence sans hooks sans pré-semage manuel du registre, une install/status claude n'en déclare jamais, et le texte faux du gate hors périmètre est consigné comme dette explicite avec fait mesuré + déclencheur de reprise plutôt que silencieusement laissé tel quel.</done>
  <reversibility rating="reversible">record_codex_runtime_if_applicable() modifie vf_runtimes.active automatiquement lors d'un install Codex — réversible via une invocation manuelle ultérieure de vf-calibrate/runtime-registry.sh set-active vers un autre runtime ; sortie toujours relayée verbatim (jamais silencieuse), gate --confirmed de runtime-registry.sh reste la dernière garde pour tout appelant humain direct.</reversibility>
</task>

</tasks>

## Artefacts produits par cette plan

- `plugin/_internal/vibeflow-update.sh` — fonctions modifiées : `resolve_posed_agent_artifact()`
  (contrat multi-lignes), `report_artifact_fidelity()`, `register_codex_agent_if_applicable()`
  (boucles) ; fonctions nouvelles : `unregister_codex_agent_if_applicable()`,
  `find_runtime_registry()`, `record_codex_runtime_if_applicable()` ; call-sites modifiés :
  `install_module()`, `update_module()`, `uninstall_module()`.
- `plugin/_internal/runtime-adapter/register-codex-agent.sh` — nouveau mode `--remove` (retrait
  idempotent d'un rôle `.toml`, sans dépendance à `node`).
- `plugin/_internal/tests/test-vibeflow-update.sh` — nouveaux tests CODEX-B4/B5/B6 (numérotation
  déterminée à l'exécution, après le dernier test existant du fichier).
- `.planning/ROADMAP.md` — une entrée « Dette nommée » (`D-38-<suivant>`) documentant le texte faux
  hors périmètre de `check-artifact-fidelity.sh:175` et la conversion `hooks.json` non livrée.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| module source (`.md` sous `plugin/*/agents/` ou `AGENT.md`) → rôle Codex `.toml` | contenu local au dépôt, jamais une entrée réseau/attaquant distant, mais transformé puis écrit sous `$CODEX_HOME` (hors du dépôt) |
| `.planning/config.json` du lab cible → `runtime-registry.sh` → `check-artifact-fidelity.sh --coexistence-report` | fichier d'état local, écrit automatiquement par ce plan pour la première fois côté chemin d'install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-38-19 | Tampering | `resolve_posed_agent_artifact()` itère tous les `agents/*.md` sans filtrer par validité de frontmatter | medium | mitigate | `register-codex-agent.sh` exige déjà un champ `name:` non vide (exit 1 sinon) — un `.md` sans frontmatter valide sous `agents/` échoue proprement fichier par fichier via la boucle, relayé, sans bloquer les autres artefacts du même module |
| T-38-20 | Tampering | `unregister_codex_agent_if_applicable()` dérive `AGENT_NAME` en relisant le `.md` source juste avant sa suppression par le reste d'`uninstall_module()` | low | accept | fichier local au poste, jamais une entrée attaquant distant ; fenêtre de lecture-avant-suppression déjà utilisée symétriquement par `backup_module()` dans la même fonction, aucune surface nouvelle |
| T-38-21 | Repudiation | `record_codex_runtime_if_applicable()` écrit `vf_runtimes.active` sans confirmation interactive dédiée au moment de l'install | medium | mitigate | sortie `[runtime-registry]` relayée verbatim (jamais capturée en silence) sur le même flux que la bannière d'install déjà visible à l'opérateur ; gate `--confirmed` de `runtime-registry.sh` reste la garde de dernier recours pour tout appelant humain direct (vf-calibrate) ; gaté sur `runtime = codex` détecté, zéro effet sur le chemin claude majoritaire |
| T-38-SC | Tampering | npm/pip/cargo installs | n/a | accept | aucun paquet npm/pip/cargo nouveau introduit par ce plan — `node` est une dépendance déjà présente de `register-codex-agent.sh` depuis 38-05, non ré-auditée ici |
</threat_model>

<verification>
Étape finale (non-régression, PAS un périmètre de re-planification) — rejouer les 3 suites nommées
par le mandat, dans cet ordre, et arrêter/rapporter (jamais corriger) en cas de KO sur une suite
hors périmètre de ce plan :

1. `bash plugin/_internal/tests/test-vibeflow-update.sh` — baseline mesurée au cadrage : 63 OK / 0 KO
   / 0 SKIP ; doit rester 0 KO, le compte OK augmente (nouveaux tests des 3 tâches).
2. `bash plugin/_internal/tests/test-manifest.sh` — baseline mesurée au cadrage : 62 OK / 0 KO / 0
   SKIP ; ce plan ne touche aucune logique de manifeste, doit rester EXACTEMENT 62 OK / 0 KO — toute
   régression sur ce total est un signal d'alarme (fichier non listé dans `files_modified`).
3. `bash plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh` — lecture seule, fichier
   appartenant à un autre worker (bloquants 1-3) ; baseline mesurée au cadrage : 7 OK / 0 KO / 0 SKIP,
   peut avoir progressé entre-temps sur ce worktree partagé — une augmentation du compte OK est
   normale, un KO est un signal à RAPPORTER (jamais à corriger, hors périmètre de ce plan).

Garde-fou final (déjà présent dans test-vibeflow-update.sh, à ne jamais faire régresser) : le vrai
`~/.claude` ET le vrai `~/.codex/agents/vibeflow` restent byte-identiques avant/après toute la suite.
</verification>

<success_criteria>
- CODEX-B4 : `resolve_posed_agent_artifact()` rend tous les agents d'un module (root + agents/*.md
  cumulés), team-kernel complet (7 agents) enregistré côté Codex, module mono-agent non régressé —
  preuve par `comm` sur ensembles triés, jamais par comptage.
- CODEX-B5 : `uninstall_module()`/`uninstall --all` retirent réellement chaque rôle `.toml` posé par
  le module désinstallé, sans toucher aux résidus runtime légitimes de Codex.
- CODEX-B6 : la coexistence sans hooks se déclare depuis le chemin réel d'install/status sans
  pré-semage manuel, jamais pour un runtime claude seul ; le texte faux du gate hors périmètre est
  consigné en dette ROADMAP avec fait mesuré et déclencheur de reprise nommé.
- `test-vibeflow-update.sh` : 0 KO (compte OK en hausse). `test-manifest.sh` : 62 OK / 0 KO inchangé.
  `test-agent-to-codex.sh` : rejoué, tout KO rapporté sans être corrigé (hors périmètre).
- Aucun fichier hors de `files_modified` n'a été touché — vérifié par `git diff --stat` avant chaque
  commit de ce plan.
</success_criteria>

<output>
Create `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-07-SUMMARY.md` when done
</output>

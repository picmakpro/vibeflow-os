---
phase: 41-posture-de-protection-du-d-p-t
plan: 12
lot: L12 — tag annoté, release GitHub, check-release-tag --remote (si release de phase), invariants du volet admin
type: execute
wave: 12
depends_on: ["41-11"]
autonomous: false
requirements: [PROT-02, PROT-01]
files_modified:
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Deux chemins, fixés par `RELEASE-DECISION:` (41-11). `release-de-phase` : critère 3 complet — la PR des preuves est `MERGED` sans suite `bypass` sur son commit de merge ; tag annoté `v<N>` sur ce commit, créé sans contournement (création libre, D-07) ; release GitHub existante ; `bash scripts/check-release-tag.sh --remote` rend 0 avec `✓` (PROT-02). `preuves-sans-release` : `RELEASE: tag=sans_objet …` — la PR des preuves est `MERGED` sans suite `bypass`, `check-release-tag.sh --remote` rend toujours 0 (la `VERSION` de `main` reste taggée), le critère 3 est consigné non rejoué ; la Task 2 est sans objet"
    - "Traces : suites du merge (et du tag s'il existe) relues par l'agent via l'API quand `ACCES-RULE-SUITES: picmakpro=ok` (`relue=oui`), sinon collées et marquées `relue=non_relue`"
    - "Le rouge attendu de l'étape `check-release-tag` du job `gates` au push de merge sur `main` (tag posé après, chemin `release-de-phase`) est consigné comme signal « tag à poser » ; sa relance après le tag est un choix humain consigné, pas une obligation (D-M5 ; demande de Samuel, WhatsApp, 2026-09-23)"
    - "Invariants du volet admin (critère 4) : `scripts/check-release-tag.sh`, `scripts/hooks/pre-push` et `.github/workflows/ci.yml` n'ont été modifiés par aucun merge du volet (PR de la phase, PR-P si mergée, PR des preuves) ; aucun fichier sous `plugin/` hors les deux manifestes de version ; aucun `check-*.sh` ni `tests/test-*.sh` ajouté — le volet admin ne crée aucun gate (D-M11), les trois gates de la phase étant ceux du périmètre sans admin (QUAL-01 déjà satisfait le 2026-09-18)"
    - "La branche C `docs/phase-41-cloture` est créée depuis `origin/main` APRÈS le merge de la PR des preuves ; le corps de la PR des preuves est relu (canal et date si un arbitrage y est cité)"
  artifacts:
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-RELEASE:`, `RELEASE:`, `CI-MAIN-RELEASE:`, `INVARIANTS-PHASE:`"
  key_links:
    - "Le ledger est coché sur ces lignes au plan 41-13, sur la même branche C"
---

<objective>
Terminer le rejeu du flux de release sous la règle (si la release de phase a été décidée) et
vérifier les invariants du volet admin.

Révisé le 2026-09-23 (reprise) : le plan suit `RELEASE-DECISION:` (41-11) — en
`preuves-sans-release`, il consigne le critère 3 comme non rejoué au lieu de poser un tag ; gestes
humains de Willy (`picmakpro`) ; la relecture des suites passe par l'accès mesuré de `picmakpro` ;
l'invariant « zone interdite » devient « aucun fichier sous `plugin/` hors les deux manifestes ».

Purpose: critères de succès 3 (PROT-02) et 4 (QUAL-01).
Output: tag et release publiés (ou `sans_objet`), `check-release-tag` ✓, invariants consignés sur la branche C.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-11-SUMMARY.md
@CLAUDE.md
@scripts/check-release-tag.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1 : relecture du merge de la PR des preuves et de son corps, branche C, notes de release préparées (ou chemin sans release consigné)</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 3 de 41-11
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`RELEASE-DECISION:`)
    - CHANGELOG.md (entrée `## [v<N>]`, chemin `release-de-phase`)
  </read_first>
  <action>
Identité `picmakpro` ; `git fetch origin --tags`. Relire la PR des preuves : `MERGED`, commit de merge,
`VERSION` de ce commit (celle de B). Créer la branche C en local :
`git switch -c docs/phase-41-cloture origin/main` (arbre propre exigé).

Consigner dans `41-PREUVES.md`, section `## 41-12 — release` :
`PR-RELEASE: #<n> merge=<sha> version=v<N ou version inchangée> etat_avant_merge=<état collé> commande=gh_pr_merge_--merge`.

Chemin `release-de-phase` : écrire hors dépôt (fichier temporaire, chemin imprimé) les notes de
release — résumé repris de l'entrée CHANGELOG `v<N>` et liste `git log --oneline v<version
précédente>..<merge>` — et imprimer les commandes exactes de la Task 2 avec `<N>`, `<sha>` et le
chemin des notes.

Chemin `preuves-sans-release` : la Task 2 est sans objet (consignée « non déclenchée » au SUMMARY) ;
passer directement à la Task 3.

Commit local sur C, message « docs(41-12): merge de la PR des preuves relu, branche de clôture ouverte ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); N=$(awk "\$1==\"PR-RELEASE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-RELEASE absente"; exit 1;; esac; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); hr=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefName --jq .headRefName); M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); TV=$(mktemp -d); git show "$M:VERSION" > "$TV/v"; vm=$(tr -d "[:space:]" < "$TV/v"); vc=$(val PR-RELEASE: version); mc=$(val PR-RELEASE: merge); ea=$(val PR-RELEASE: etat_avant_merge); br=$(git branch --show-current); vt=$(tr -d "[:space:]" < VERSION); echo "PR des preuves : identite=$u pr=$N branche=$hr etat=$st merge=$M version_merge=$vm version_consignee=$vc merge_consigne=$([ "$mc" = "$M" ] && echo oui || echo non) etat_avant_merge=$ea branche_courante=$br version_arbre=$vt"; [ "$u" = picmakpro ] && [ "$st" = MERGED ] && [ "$hr" = feat/phase-41-preuves-release ] && [ "$vm" = "$vc" ] && [ "$mc" = "$M" ] && [ "$ea" = CLEAN ] && [ "$br" = docs/phase-41-cloture ] && [ "$vt" = "$vm" ]'</automated>
    <fails_when>exit non nul ; ou PR des preuves non `MERGED` ou autre branche ; ou version du commit de merge différente de celle consignée ; ou merge consigné différent du relu ; ou `etat_avant_merge=` différent de `CLEAN` (merge obtenu hors du chemin normal : arrêt) ; ou branche courante différente de C ; ou `VERSION` de l'arbre différente de celle du merge</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; k=0; t=0; : > "$T/corps"; for key in PR-RELEASE; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "NUMERO DE PR ABSENT : $key"; k=$((k+1)); continue;; esac; t=$((t+1)); printf "@@CORPS@@ %s #%s\n" "$key" "$N" >> "$T/corps"; gh pr view "$N" --repo picmakpro/vibeflow-os --json body --jq .body >> "$T/corps"; done; r=0; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@CORPS@@ / { if (lib != \"\") juge(m, lib); lib=\$0; m=\"\"; next } { m = m \" \" \$0 } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (lib != \"\") juge(m, lib); print \"corps de PR : en_defaut=\" v+0; exit (v > 0) }" "$T/arb" "$T/corps" || r=$?; echo "corps de PR : lus=$t numeros_absents=$k"; [ "$r" -eq 0 ] && [ "$k" -eq 0 ] && [ "$t" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou un corps de PR qui cite D-02bis ou D-01..D-10 sans son canal et sa date ; ou un corps qui cite une décision `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » capturés ; ou un numéro de PR non consigné</fails_when>
  </verify>
  <acceptance_criteria>
    - PR des preuves `MERGED` depuis B, état `CLEAN` avant merge, version du commit de merge consignée ; corps conforme
    - branche C créée depuis `origin/main` ; `PR-RELEASE:` consignée ; notes de release prêtes hors dépôt (chemin `release-de-phase`)
  </acceptance_criteria>
  <done>Le tag peut être posé sur le bon commit, avec ses notes — ou le chemin sans release est engagé.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains de Willy (`picmakpro`), chemin `release-de-phase` seulement — tag annoté, release GitHub, relance facultative</name>
  <read_first>
    - les valeurs `<N>`, `<sha-merge>` et le chemin des notes imprimés par la Task 1
  </read_first>
  <action>Si `RELEASE-DECISION: option=release-de-phase` : Willy, compte `picmakpro`, pose et publie le tag annoté sur le commit de merge, crée la release GitHub et décide de relancer ou non le job rouge de `main`. Sinon : tâche sans objet, consignée « non déclenchée ».</action>
  <instructions>
Compte qui agit : `picmakpro`. Valeurs imprimées par la Task 1.

1. `git fetch origin && git tag -a v<N> -m "v<N> — posture de protection du dépôt (Phase 41)" <sha-merge> && git push origin v<N>`
   → copier la sortie (aucun avertissement de contournement attendu : création libre).
2. `gh release create v<N> --repo picmakpro/vibeflow-os --title "v<N> — posture de protection du dépôt" --notes-file <chemin-des-notes> --verify-tag`
3. Run CI du push de merge sur `main` :
   `gh run list --repo picmakpro/vibeflow-os --commit <sha-merge> --json databaseId,event,conclusion --jq '.[] | select(.event=="push")'`
   → copier. L'échec du job « Gates de qualité (mode strict) » à l'étape `check-release-tag` est le
   signal attendu (tag posé après le merge, D-M5).
4. Facultatif (D-M5, relance possible, non systématique) : `gh run rerun <databaseId> --repo picmakpro/vibeflow-os --failed`
   → écrire « relance : oui » ou « relance : non ».
  </instructions>
  <verification>La Task 3 relit le tag (annoté, cible), la release, joue `check-release-tag.sh --remote`, relit les runs de `main` et les suites (ou les marque non relues).</verification>
  <acceptance_criteria>
    - chemin `release-de-phase` : tag annoté poussé sur le commit de merge, release GitHub créée, run de `main` copié, choix de relance écrit ; chemin `preuves-sans-release` : « non déclenchée »
  </acceptance_criteria>
  <resume-signal>Coller les sorties de 1, 2, 3 et le choix de 4, puis écrire « release publiée » — ou écrire « non déclenchée » (chemin sans release).</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : critère 3 prouvé (ou consigné non rejoué), suites relues ou marquées, invariants du volet admin, rejeu</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2 (s'il y en a une)
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`RELEASE-DECISION:`, `PR-PHASE:`, `MAIN-APRES-PR-P:`, `PR-RELEASE:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `picmakpro` ; `git fetch origin --tags`. Jouer `bash scripts/check-release-tag.sh --remote`
sur la branche C. Relire les rule suites de `refs/heads/main` (et du tag s'il existe) dont `after_sha`
est le commit de merge. Consigner :
- chemin `release-de-phase` : `RELEASE: tag=v<N> annote=<oui|non> cible=<sha> cible_est_merge=<oui|non> release=<url> check_release_tag_remote_rc=<rc> suite_merge=<pass|bypass|absente> suite_merge_id=<id|absente> suite_tag=<pass|bypass|absente> suite_tag_id=<id|absente> relue=<oui|non_relue>` et
  `CI-MAIN-RELEASE: gates_premier_run=<conclusion> relance=<oui|non> gates_apres=<conclusion|sans_objet>` ;
- chemin `preuves-sans-release` : `RELEASE: tag=sans_objet annote=sans_objet cible=sans_objet cible_est_merge=sans_objet release=sans_objet check_release_tag_remote_rc=<rc> suite_merge=<pass|bypass|absente> suite_merge_id=<id|absente> suite_tag=sans_objet suite_tag_id=sans_objet relue=<oui|non_relue> critere_3=non_rejoue decision=preuves-sans-release` et
  `CI-MAIN-RELEASE: gates_premier_run=<conclusion> relance=sans_objet gates_apres=sans_objet` ;
- dans les deux cas : `INVARIANTS-PHASE: merges=<sha phase>,<sha PR-P ou aucun>,<sha preuves> gates_ajoutes=<k> suites_ajoutees=<k> ci_yml_touche=<oui|non> check_release_tag_touche=<oui|non> pre_push_touche=<oui|non> plugin_hors_manifestes=<k>`,
  mesurés sur `git diff --name-status <merge>^1 <merge>` de chaque merge (PR-P ignorée si non mergée).
`relue=oui` seulement si l'agent a lui-même relu les suites via l'API (`ACCES-RULE-SUITES:
picmakpro=ok`). Commit local sur C, message « docs(41-12): release de la phase prouvée ou consignée,
invariants du volet admin (D-07 — arbitrage Samuel, AskUserQuestion session principale,
2026-09-17) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; opt=$(val RELEASE-DECISION: option); M=$(val PR-RELEASE: merge); sm=$(val RELEASE: suite_merge); crc=$(val RELEASE: check_release_tag_remote_rc); rc=0; out=$(bash scripts/check-release-tag.sh --remote 2>&1) || rc=$?; ok=$(printf "%s\n" "$out" | awk "index(\$0, \"✓\") {c++} END{print c+0}"); if [ "$opt" = release-de-phase ]; then V=$(val PR-RELEASE: version); ty=$(git cat-file -t "refs/tags/$V" 2>/dev/null || echo absent); tc=$(git rev-parse "refs/tags/$V^{commit}" 2>/dev/null || echo absent); TL=$(mktemp -d); git ls-remote --tags origin "refs/tags/$V" > "$TL/td"; td=$(awk "END{print NR}" "$TL/td"); rl=$(gh release view "$V" --repo picmakpro/vibeflow-os --json tagName --jq .tagName 2>/dev/null || echo absente); stg=$(val RELEASE: suite_tag); echo "critere 3 : tag=$V type=$ty cible_est_merge=$([ "$tc" = "$M" ] && echo oui || echo non) tag_distant=$td release=$rl check_release_tag_remote_rc=$rc sortie_ok=$ok rc_consigne=$crc suite_merge=$sm suite_tag=$stg"; [ "$ty" = tag ] && [ "$tc" = "$M" ] && [ "$td" -eq 1 ] && [ "$rl" = "$V" ] && [ "$rc" -eq 0 ] && [ "$ok" -ge 1 ] && [ "$crc" = "$rc" ] && [ -n "$sm" ] && [ "$sm" != bypass ] && [ -n "$stg" ] && [ "$stg" != bypass ]; elif [ "$opt" = preuves-sans-release ]; then t=$(val RELEASE: tag); echo "critere 3 non rejoue (decision=$opt) : tag=$t check_release_tag_remote_rc=$rc sortie_ok=$ok rc_consigne=$crc suite_merge=$sm"; [ "$t" = sans_objet ] && [ "$rc" -eq 0 ] && [ "$ok" -ge 1 ] && [ "$crc" = "$rc" ] && [ -n "$sm" ] && [ "$sm" != bypass ]; else echo "RELEASE-DECISION inattendue : ${opt:-absente}"; exit 1; fi'</automated>
    <fails_when>exit non nul ; chemin `release-de-phase` : tag absent, léger (non annoté) ou hors du commit de merge, tag non publié, release absente, `check_release_tag_remote_rc` différent de 0 ou sortie sans `✓`, rc consigné différent du rc rejoué, ou `suite_merge=bypass` / `suite_tag=bypass` (le rejeu n'est plus « sans contournement ») ; chemin `preuves-sans-release` : `RELEASE:` sans `tag=sans_objet`, `check-release-tag.sh --remote` différent de 0 (la `VERSION` de `main` n'est plus taggée), ou merge des preuves passé par contournement ; ou `RELEASE-DECISION` inattendue</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; acc=$(val ACCES-RULE-SUITES: picmakpro); M=$(val PR-RELEASE: merge); V=$(val PR-RELEASE: version); sm=$(val RELEASE: suite_merge); smi=$(val RELEASE: suite_merge_id); stg=$(val RELEASE: suite_tag); sti=$(val RELEASE: suite_tag_id); rl=$(val RELEASE: relue); k=0; { [ -n "$sm" ] && [ -n "$stg" ] && [ -n "$smi" ] && [ -n "$sti" ]; } || { echo "champs de suite non consignes"; exit 1; }; if [ "$acc" = ok ]; then [ "$rl" = oui ] || k=$((k+1)); if [ "$smi" != absente ]; then a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$smi" --jq "\"\(.result) \(.after_sha)\""); [ "$a" = "$sm $M" ] || { echo "SUITE MERGE DIFFERENTE : $a"; k=$((k+1)); }; fi; case "$sti" in absente|sans_objet) ;; *) a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$sti" --jq "\"\(.result) \(.ref)\""); [ "$a" = "$stg refs/tags/$V" ] || { echo "SUITE TAG DIFFERENTE : $a"; k=$((k+1)); };; esac; else [ "$rl" = non_relue ] || k=$((k+1)); fi; echo "relecture release : acces=$acc suite_merge=$sm ($smi) suite_tag=$stg ($sti) relue=$rl en_defaut=$k"; [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou champs de suite absents ; avec accès (`picmakpro=ok`) : `relue` différent de `oui`, ou résultat, `after_sha` ou `ref` consignés différents de la relecture API ; sans accès : `relue` différent de `non_relue`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M1=$(val PR-PHASE: merge); M2=$(val MAIN-APRES-PR-P: merge); M3=$(val PR-RELEASE: merge); T=$(mktemp -d); : > "$T/ns"; for m in "$M1" "$M2" "$M3"; do [ -n "$m" ] || { echo "merge absent"; exit 1; }; [ "$m" = aucun ] && continue; git merge-base --is-ancestor "$m" origin/main || { echo "merge $m hors main"; exit 1; }; git diff --name-status "$m^1" "$m" >> "$T/ns"; done; ga=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /(^|\\/)check-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); sa=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /\\/tests\\/test-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); ci=$(awk -F"\t" "\$NF==\".github/workflows/ci.yml\" {c++} END{print c+0}" "$T/ns"); crt=$(awk -F"\t" "\$NF==\"scripts/check-release-tag.sh\" {c++} END{print c+0}" "$T/ns"); pp=$(awk -F"\t" "\$NF==\"scripts/hooks/pre-push\" {c++} END{print c+0}" "$T/ns"); zi=$(awk -F"\t" "index(\$NF, \"plugin/\")==1 && \$NF != \"plugin/.claude-plugin/plugin.json\" && \$NF != \"plugin/.codex-plugin/plugin.json\" {c++} END{print c+0}" "$T/ns"); cg=$(val INVARIANTS-PHASE: gates_ajoutes); cs=$(val INVARIANTS-PHASE: suites_ajoutees); echo "invariants volet admin : gates_ajoutes=$ga (consigne $cg) suites_ajoutees=$sa (consigne $cs) ci_yml=$ci check_release_tag=$crt pre_push=$pp plugin_hors_manifestes=$zi"; [ "$ga" -eq 0 ] && [ "$sa" -eq 0 ] && [ "$ci" -eq 0 ] && [ "$crt" -eq 0 ] && [ "$pp" -eq 0 ] && [ "$zi" -eq 0 ] && [ "$cg" = "$ga" ] && [ "$cs" = "$sa" ]'</automated>
    <fails_when>exit non nul ; ou un des merges absent ou hors `main` ; ou `gates_ajoutes=` / `suites_ajoutees=` différent de 0 (un gate est né dans le volet admin : QUAL-01 s'applique, mutation rouge exigée, remonter) ; ou `ci.yml`, `check-release-tag.sh`, `pre-push` modifiés par le volet (D-M5, D-M9) ; ou un fichier sous `plugin/` hors les deux manifestes de version ; ou consignation différente de la mesure</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `release-de-phase` : critère 3 — tag annoté sur le commit de merge, publié, release existante, `check-release-tag.sh --remote` rc 0 avec `✓`, aucune suite `bypass` sur le merge ni sur le tag
    - `preuves-sans-release` : `RELEASE: tag=sans_objet … critere_3=non_rejoue`, merge des preuves sans suite `bypass`, `check-release-tag.sh --remote` rc 0
    - suites relues via l'API (accès mesuré) ou marquées `non_relue`
    - invariants : 0 gate et 0 suite ajoutés par le volet admin, `ci.yml`/`check-release-tag.sh`/`pre-push` intacts, `plugin/` intact hors manifestes (critère 4)
    - rejeux verts ; messages de commit conformes
  </acceptance_criteria>
  <done>Le flux de release est rejoué sous la règle — ou consigné non rejoué sur décision — et les invariants du volet admin sont mesurés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poste de Willy → tags `v*` | création du tag de release sous le ruleset de tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-42 | Repudiation | critère 3 déclaré sans rejeu réel | high | mitigate | tag, release et `check-release-tag.sh --remote` relus et rejoués par l'agent ; rc consigné = rc rejoué ; en `preuves-sans-release`, `critere_3=non_rejoue` consigné tel quel |
| T-41-44 | Tampering | gate né en silence pendant le volet admin | medium | mitigate | ajouts `check-*.sh` et `tests/test-*.sh` mesurés sur les merges du volet ; QUAL-01 déclenché sinon |
| T-41-45 | Denial of service | gate de release ou hook modifié par la phase | medium | mitigate | `ci.yml`, `check-release-tag.sh`, `pre-push` vérifiés intacts |
| T-41-54 | Repudiation | trace de release présentée comme relue sans l'être | medium | mitigate | `relue=oui` exigé seulement avec relecture API ; sinon `non_relue` |
</threat_model>

<verification>
Chemin `release-de-phase` : tag annoté et release publiés sans contournement, `check-release-tag` ✓ ;
chemin `preuves-sans-release` : critère 3 consigné non rejoué ; invariants du volet admin tenus.
</verification>

<success_criteria>
Critères de succès 3 (ou son report consigné) et 4 de la Phase 41 prouvés et consignés.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-12-SUMMARY.md` when done — committé sur la branche C. En `preuves-sans-release`, le SUMMARY écrit que le critère 3 n'est pas rejoué dans cette phase et renvoie à `RELEASE-DECISION:`.
</output>

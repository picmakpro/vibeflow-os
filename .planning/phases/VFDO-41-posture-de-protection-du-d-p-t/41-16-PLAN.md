---
phase: 41-posture-de-protection-du-d-p-t
plan: 16
lot: L16 — G-2 : garde CI « la PR modifie ce qui la juge », marqueur déclaratif Gate-Touche
type: execute
wave: 2
depends_on: ["41-14", "41-15"]
autonomous: true
requirements: [PROT-05, QUAL-01]
files_modified:
  - scripts/check-gate-touche.sh
  - scripts/tests/test-check-gate-touche.sh
  - .github/workflows/ci.yml
  - CHANGELOG.md

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PÉRIMÈTRE — périmètre SANS ADMIN (option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Aucune règle côté GitHub, aucun ruleset, aucun CODEOWNERS, aucune revue code owner, aucun bump de la VERSION racine ni d'un module. D-01 à D-08 restent suspendus"
    - "FOYER D'EXIGENCE — G-2 relève de **PROT-05** « les gardes in-repo de la surface de gate et du flux d'arrivée sur `main` sont visibles et tracées, jamais fermées », exigence créée par le manager le 2026-09-17 sur remontée du planificateur, née du recadrage sans admin (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). PROT-04 conserve son énoncé d'origine et reste le foyer de G-1 ; le ledger de PROT-05 est écrit par le plan de clôture 41-19"
    - "LIMITE DE FOND, écrite en en-tête du script, dans la suite, dans l'étape CI et au SUMMARY, et vraie de G-2 en premier lieu : cette garde est elle-même dans la surface qu'elle surveille. Une même PR peut changer `scripts/check-gate-touche.sh`, sa suite et son étape CI et rester verte. Elle rend visible et trace, elle ne verrouille rien"
    - "SURFACE SURVEILLÉE, littéralement celle de l'arbitrage du 2026-09-17, cinq classes : `plugin/conductor/scripts/check-*.sh`, `scripts/check-*.sh`, leurs suites `plugin/conductor/scripts/tests/test-*.sh` et `scripts/tests/test-*.sh`, `.github/workflows/ci.yml`, et tout chemin sous `scripts/hooks/`. BORNE NOMMÉE, jamais un oubli : les gates des autres modules (`plugin/dev-orchestrator/scripts/check-*.sh`, `plugin/software-architecture/scripts/check-file-size.sh`, etc.) sont hors surface par l'arbitrage du périmètre — l'écrire en en-tête et au SUMMARY"
    - "MARQUEUR — forme fixée : un trailer de commit `Gate-Touche: <chemin-ou-motif> — <raison>`, un trailer par chemin ou un motif glob qui couvre un lot. Le séparateur nominal est le tiret cadratin ` — ` ; le tiret simple ` - ` est aussi accepté, pour qu'un environnement qui mange l'UTF-8 ne fabrique pas un faux rouge (borne écrite). La raison doit compter au moins 10 caractères non blancs"
    - "LE MARQUEUR EST DÉCLARATIF : la garde vérifie sa PRÉSENCE et sa FORME, jamais la véracité de la raison. L'écrire en en-tête, dans l'ADR-072 et au SUMMARY"
    - "PORTÉE BRANCHE, pas commit : le marqueur peut vivre dans N'IMPORTE QUEL commit propre à la branche (« sans marqueur déclaratif dans un commit de la branche »). C'est ce qui rend couvrable, par un commit de documentation ultérieur, un chemin touché avant l'existence de la garde — les commits du plan 41-14 en particulier"
    - "G-2 rougit (rc 1) quand au moins un chemin de la surface est touché entre la base dérivée et HEAD sans qu'aucun trailer conforme de la branche ne le couvre, exactement ou par motif. Verdict `CHEMIN-NON-DECLARE` par chemin, avec le statut de diff lu"
    - "La base est DÉRIVÉE PAR MACHINE : `git merge-base HEAD <ref-main-résolue>`, cascade nommée, rc 2 si rien ne résout ; jamais l'adjacence de `git log`, jamais `HEAD^`"
    - "VERT À VIDE INTERDIT : rc 0 n'est rendu que si au moins un chemin de la surface est touché ET couvert. Zéro chemin touché, ou plage vide → rc 3, silence non bloquant, verdict `RIEN-A-JUGER` — c'est le cas de la plupart des PR de ce dépôt et il ne doit jamais se lire comme un succès de la garde"
    - "QUAL-01 — la suite porte les trois issues (PASS / FAIL / IMPARSABLE BRUYANT) et CINQ mutants, un par comparaison : appartenance à la surface, présence du marqueur, forme du marqueur, correspondance par motif, dérivation de la base. Chaque mutant est refusé s'il est identique à l'original (`cmp`) ou si `bash -n` échoue ; trace assertion / attendu / obtenu"
    - "L'étape CI `check-gate-touche` du job `gates` prouve ELLE-MÊME qu'elle sait rougir : quatre bascules isolées sur dépôt jetable reconstruit à chaque fois (chemin surveillé couvert → 0, chemin surveillé non déclaré → 1, trailer sans raison → 1, aucun chemin surveillé touché → 3), puis la mesure du dépôt réel"
    - "TRAÇABILITÉ — un seul arbitrage ou décision cité par commit ; tout commit qui cite l'arbitrage du périmètre écrit exactement « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ». Tout commit de ce plan touche la surface surveillée, donc porte ses propres trailers `Gate-Touche:` — la garde s'applique à elle-même dès sa naissance"
  artifacts:
    - path: "scripts/check-gate-touche.sh"
      provides: "G-2 — surface de gate touchée sans marqueur déclaratif"
      contains: "CHEMIN-NON-DECLARE"
    - path: "scripts/tests/test-check-gate-touche.sh"
      provides: "trois issues QUAL-01 + cinq mutants opposables"
    - path: ".github/workflows/ci.yml"
      provides: "étape `check-gate-touche` dans le job `gates`, à bascules de fixture"
  key_links:
    - "Au plan de clôture 41-19, G-2 juge TOUTE la branche : les chemins touchés par 41-14 (script, suite, `ci.yml`, `CHANGELOG` non surveillé) doivent être couverts par des trailers. Si les commits de 41-14 n'en portent pas, ce plan ou la clôture ajoute un commit de documentation qui les porte — la portée branche l'autorise, l'amendement d'un commit déjà poussé non"
    - "`scripts/` est l'outillage DU DÉPÔT, pas un module : aucun bump de module n'est dû. La trace passe par la section `## Non releasé` du `CHANGELOG.md` racine"
---

<objective>
Poser G-2 : la garde qui rend visible et tracée la modification, par une PR, de ce qui la juge —
un gate, sa suite, l'étape CI qui l'invoque, ou un hook. Le marqueur exigé est déclaratif : un
trailer `Gate-Touche:` dont la garde vérifie la présence et la forme, jamais la véracité.

Purpose: PROT-05 — exigence neuve « les gardes in-repo de la surface de gate et du flux d'arrivée sur
`main` sont visibles et tracées, jamais fermées », créée par le manager le 2026-09-17 sur remontée du
planificateur (le recadrage sans admin a laissé G-2 sans foyer d'exigence ; PROT-04 garde son énoncé
d'origine, foyer de G-1). Origine factuelle : la seconde phrase d'O-3 du `25-SECURITY.md` (« une même PR peut modifier le gate,
sa suite et l'étape CI ») cesse d'être invisible. Elle n'est pas empêchée : elle est déclarée.
Output: une garde in-repo qui se surveille elle-même, avec ses mutants et ses bascules CI.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-14-SUMMARY.md

Fichiers à lire au début de la Task 1, jamais deux fois la même plage :
- `scripts/check-baseline-arbitrage.sh` (posé au plan 41-14 : cascade de résolution de la base,
  forme de la ligne `decouverte:`, contrat de codes de sortie — à reprendre à l'identique)
- `scripts/tests/test-check-baseline-arbitrage.sh` (patron `ok`/`ko`, `mk_repo`, patron de mutant)
- `.github/workflows/ci.yml` : l'étape `check-baseline-arbitrage` posée au plan 41-14 (point
  d'insertion juste après) et l'étape `check-release-tag` (fin du job `gates`)
</context>

<discipline>
Contraintes d'exécution valables pour TOUTES les tâches de ce plan.

- **Aucune commande git en ligne dans un `<verify>` ni dans un appel bash direct.** La phase
  s'exécute dans un worktree isolé : le harnais refuse une commande qui nomme git sous une forme
  qu'il ne peut pas rattacher au worktree. Tout travail git vit À L'INTÉRIEUR des scripts
  versionnés, invoqués par `bash <chemin>`.
- **Commandes proxifiées** : `diff` et `ls` mentent sur ce poste ; les préfixer de `rtk proxy`.
  Jamais de comptage par `grep | wc -l` — extraire puis comparer par `comm` / `cmp`, ou compter en
  `awk`.
- **Dépôts de fixture** : `git init -q -b main` sous `mktemp -d`, identité passée en `-c`, `trap`.
- **Aucun texte produit ici n'associe O-3 ni une garde à un mot d'achèvement.** La formule exacte
  pour O-3 est « signalée et tracée ». Rejouer
  `tools/check-aucune-fermeture.sh` (plan 41-15) après chaque tâche qui écrit du texte.
- **Trailers de ce plan** : chaque commit touchant `scripts/check-gate-touche.sh`,
  `scripts/tests/test-check-gate-touche.sh` ou `.github/workflows/ci.yml` porte un
  `Gate-Touche: <chemin> — <raison>` par chemin touché (ou un motif couvrant). C'est la première
  application réelle de la convention.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : G-2 bout en bout — le script, et son étape CI câblée sur une seule bascule</name>
  <files>scripts/check-gate-touche.sh, .github/workflows/ci.yml</files>
  <behavior>
    Contrat observable de `scripts/check-gate-touche.sh [--root DIR] [--base-ref REF]` :
    - au moins un chemin de la surface touché, tous couverts par un trailer conforme → rc 0, verdict `DECLARE`
    - au moins un chemin de la surface touché sans trailer conforme le couvrant → rc 1, verdict `CHEMIN-NON-DECLARE` par chemin
    - chemin couvert par un trailer dont le motif glob l'apparie → rc 0
    - trailer présent mais sans séparateur, ou raison de moins de 10 caractères non blancs → le trailer est IGNORÉ (forme non conforme) et le chemin reste non déclaré → rc 1, verdict `MARQUEUR-MAL-FORME` en plus du chemin
    - aucun chemin de la surface touché → rc 3, verdict `RIEN-A-JUGER`
    - plage vide (base égale HEAD, ou zéro commit) → rc 3, verdict `PLAGE-VIDE`
    - aucune ref `main` résoluble, hors d'un arbre git, ou sortie de diff imparsable → rc 2
    - argument inconnu, `--base-ref` sans valeur, `--root` inexistant → rc 64
  </behavior>
  <action>
Écrire `scripts/check-gate-touche.sh`, gate d'outillage du dépôt. Reprendre À L'IDENTIQUE la
structure de `scripts/check-baseline-arbitrage.sh` : `set -uo pipefail`, en-tête « pourquoi ce
script existe », énumération de TOUS les codes de sortie, section des surcharges de fixture, options
`--root`, `--base-ref`, `-h|--help`. Toute autre option → rc 64.

**En-tête.** Écrire l'origine : la seconde phrase d'O-3 du `25-SECURITY.md` — une même PR peut
modifier le gate, sa suite et l'étape CI — et le fait que, sans accès admin, aucune règle côté
serveur ne peut l'empêcher (option (a), arbitrage Samuel, AskUserQuestion session principale,
2026-09-17). Écrire la limite de fond, vraie de cette garde en premier : elle est dans la surface
qu'elle surveille ; une même PR peut la changer, elle, sa suite et son étape, et rester verte. Elle
rend visible et trace, elle ne verrouille rien. Écrire que le marqueur est DÉCLARATIF : la garde
vérifie présence et forme, jamais la véracité de la raison.

**Base.** Cascade identique à G-1 : `--base-ref`, sinon la première de
`refs/remotes/origin/main`, `origin/main`, `refs/heads/main`, `main` qui résout ; aucune → rc 2 avec
la cascade recopiée. Base = `git merge-base HEAD <ref>`. Base égale HEAD → rc 3 `PLAGE-VIDE`, avec
la borne écrite (sur un push vers `main` la base égale HEAD ; le cas est jugé en amont sur la PR, et
un push direct est signalé par la garde du plan 41-17).

**Comparaison 1 — appartenance à la surface.** Lire `git diff --name-status <base> HEAD` UNE SEULE
FOIS et classer chaque chemin en `awk` contre les cinq classes de la surface, écrites en toutes
lettres dans l'outil : `plugin/conductor/scripts/check-*.sh` (un seul niveau, jamais un `find`
récursif), `scripts/check-*.sh`, `plugin/conductor/scripts/tests/test-*.sh`,
`scripts/tests/test-*.sh`, `.github/workflows/ci.yml`, et tout chemin commençant par
`scripts/hooks/`. Les statuts `A`, `M`, `D`, `R` comptent tous comme « touché » — un gate supprimé
est la modification la plus radicale. Écrire la borne : les gates des autres modules sont hors
surface par l'arbitrage du périmètre, pas par oubli.

**Comparaison 2 — récolte des marqueurs, portée branche.** Lire les messages de TOUS les commits de
`git rev-list <base>..HEAD` (merges compris : un commit de documentation peut porter les trailers
d'un lot) et extraire les lignes commençant, après suppression des blancs de tête, par
`Gate-Touche:`. Le marqueur peut vivre dans n'importe quel commit de la plage : l'écrire en en-tête,
avec sa conséquence — un chemin touché avant l'existence de cette garde reste couvrable par un
commit ultérieur, ce qui évite de réécrire un commit déjà poussé.

**Comparaison 3 — forme du marqueur.** Un trailer est conforme si, après `Gate-Touche:`, il porte
un premier champ non vide (le chemin ou le motif), puis le séparateur ` — ` (tiret cadratin,
nominal) ou ` - ` (tiret simple, accepté pour qu'un environnement qui mange l'UTF-8 ne fabrique pas
un faux rouge — borne écrite en en-tête), puis une raison de 10 caractères non blancs au moins. Un
trailer non conforme est IGNORÉ pour la couverture et signalé par le verdict `MARQUEUR-MAL-FORME`
avec la ligne recopiée. Imprimer `marqueurs: lus=<n> conformes=<m>`.

**Comparaison 4 — correspondance chemin ↔ motif.** Pour chaque chemin touché de la surface,
chercher un trailer conforme dont le premier champ soit égal au chemin, ou l'apparie en tant que
motif glob. Utiliser la correspondance de motif de `case "$chemin" in ($motif) …` — jamais `eval`,
jamais une expression régulière construite par concaténation. Un chemin sans trailer le couvrant →
rc 1, verdict `CHEMIN-NON-DECLARE` avec le statut de diff lu.

**Découverte et non-vacuité.** Imprimer
`decouverte: commits=<n> chemins_diff=<t> chemins_surface=<s> marqueurs_conformes=<m>`. `s` nul →
rc 3 `RIEN-A-JUGER`, jamais rc 0 : l'écrire en en-tête, c'est le cas de la plupart des PR de ce
dépôt et il ne doit pas se lire comme un succès de la garde. Sortie de `git diff --name-status`
illisible → rc 2.

**Câblage CI, une seule bascule dans cette tâche.** Insérer dans le job `gates` de
`.github/workflows/ci.yml`, JUSTE APRÈS l'étape `check-baseline-arbitrage` et avant
`check-release-tag`, une étape nommée `check-gate-touche (G-2, PROT-05 — la PR modifie ce qui la
juge)`. Le `run:` commence par `set -eu`, résout la ref de référence pour le journal et, si aucune
ref `main` ne résout, fait un `git fetch origin main:refs/remotes/origin/main` avant d'invoquer la
garde — remédiation nommée, jamais un repli silencieux. Puis une seule bascule : dépôt jetable,
premier commit portant un `scripts/check-demo.sh`, second commit qui le modifie avec un message SANS
trailer, invocation par `--root` et `--base-ref`, attendu rc 1 et `CHEMIN-NON-DECLARE` sur la
sortie. Écart → `::error::` et `exit 1`. Puis la mesure du dépôt réel : rc 0 ou 3 passe, rc 1 ou 2
échoue avec un `::error::` qui recopie les verdicts lus.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n scripts/check-gate-touche.sh && rc=0; bash scripts/check-gate-touche.sh --option-inconnue >/dev/null 2>&1 || rc=$?; echo "usage rc=$rc (attendu 64)"; [ "$rc" -eq 64 ]'</automated>
    <fails_when>`bash -n` échoue ; ou `usage rc=` différent de 64</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash scripts/check-gate-touche.sh > "$T/o" 2>&1 || rc=$?; cat "$T/o"; echo "depot reel rc=$rc"; case "$rc" in 0|3) : ;; *) exit 1 ;; esac; awk "/^decouverte: commits=/{f=1} END{exit f?0:1}" "$T/o"'</automated>
    <fails_when>rc différent de 0 et de 3 ; ou absence de la ligne `decouverte: commits=`. Un rc 1 ici signifie que les chemins de surface touchés par le plan 41-14 ne sont pas couverts : conduite écrite en Task 3, jamais un contournement de la garde</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates --step "check-gate-touche" > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "etape G-2 rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `etape G-2 rc=` différent de 0 ; ou `rejouees=` différent de 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-gate-touche.sh` existe, passe `bash -n`, rend 64 sur argument inconnu
    - sur le dépôt réel il rend 0 ou 3 et imprime ses lignes `decouverte:` et `marqueurs:`
    - l'en-tête porte les cinq classes de surface en toutes lettres, la borne des gates hors surface, la nature déclarative du marqueur, la portée branche, la borne du séparateur ASCII et la limite de fond
    - l'étape `check-gate-touche` est dans le job `gates`, rejouée seule, verte, et sa bascule attend rc 1
  </acceptance_criteria>
  <done>La garde existe, traverse toutes les couches, et son verdict de référence est prouvé rouge sur un dépôt jetable.</done>
</task>

<task type="auto">
  <name>Task 2 : la suite — trois issues QUAL-01 et cinq mutants opposables</name>
  <files>scripts/tests/test-check-gate-touche.sh</files>
  <precondition>`scripts/check-gate-touche.sh` existe et passe `bash -n` (Task 1 verte).</precondition>
  <read_first>
    - `scripts/tests/test-check-baseline-arbitrage.sh` (patron `ok`/`ko` à trois champs, `mk_repo`, `commit_avec`, patron de mutant vérifié par `cmp` puis `bash -n`)
  </read_first>
  <action>
Écrire `scripts/tests/test-check-gate-touche.sh` sur le patron de la suite de G-1 : `set -uo
pipefail`, `ok`/`ko` à trois champs distincts, `trap`, comparaisons par `cmp`/`comm`, jamais `diff`.

`mk_repo` crée un dépôt jetable avec la surface réelle en miniature : `scripts/check-demo.sh`,
`scripts/tests/test-check-demo.sh`, `plugin/conductor/scripts/check-demo.sh`,
`plugin/conductor/scripts/tests/test-check-demo.sh`, `.github/workflows/ci.yml`,
`scripts/hooks/pre-push`, plus un fichier hors surface (`README.md`) et un gate d'un autre module
(`plugin/dev-orchestrator/scripts/check-demo.sh`), et un premier commit. `commit_avec <message>`
commite l'état courant.

Cas de contrat, au minimum :
- PASS : chemin de chacune des cinq classes touché et couvert par un trailer exact → rc 0 (cinq cas,
  un par classe) ; chemin couvert par un motif glob (`scripts/check-*.sh`) → rc 0 ; trailer porté par
  un commit ULTÉRIEUR à celui qui a touché le chemin → rc 0 (preuve de la portée branche) ; trailer
  au séparateur tiret simple → rc 0 (borne ASCII) ; plusieurs chemins couverts par plusieurs
  trailers d'un même commit → rc 0 ; chemin SUPPRIMÉ mais déclaré → rc 0
- FAIL : chemin de surface touché sans aucun trailer → rc 1 et `CHEMIN-NON-DECLARE` ; trailer sans
  séparateur → rc 1 et `MARQUEUR-MAL-FORME` ; trailer avec raison de moins de 10 caractères → rc 1 ;
  trailer dont le motif n'apparie pas le chemin touché → rc 1 ; deux chemins touchés dont un seul
  déclaré → rc 1 avec UN seul verdict de chemin
- BRUYANT : `--base-ref` vers une ref inexistante → rc 2 ; `--root` hors d'un arbre git → rc 2
- SILENCE : seuls des chemins hors surface touchés (`README.md`) → rc 3 `RIEN-A-JUGER` ; gate d'un
  AUTRE module touché seul → rc 3 (contrôle négatif de la borne de surface : un rouge ici serait une
  extension non arbitrée du périmètre) ; `--base-ref` égal à HEAD → rc 3 `PLAGE-VIDE`
- USAGE : option inconnue → 64 ; `--root` inexistant → 64 ; `--base-ref` sans valeur → 64

Cinq mutants, un par comparaison, chacun fabriqué par copie du script dans le `mktemp` et
substitution ciblée (`awk` vers un temporaire puis `mv`, jamais `sed -i`), REFUSÉ s'il est identique
à l'original (`cmp -s`) ou si `bash -n` échoue :
- MUT-1 retire une classe de la surface (les suites `tests/test-*.sh`) → la fixture « suite touchée
  non déclarée » devient verte sur le mutant, reste rouge sur l'original
- MUT-2 neutralise la recherche de trailer (couverture toujours acquise) → la fixture « chemin non
  déclaré » devient verte sur le mutant, reste rouge sur l'original
- MUT-3 neutralise le contrôle de forme du marqueur (raison vide acceptée) → la fixture « trailer
  sans séparateur » devient verte sur le mutant, reste rouge sur l'original
- MUT-4 rend la correspondance de motif universelle → la fixture « motif qui n'apparie pas » devient
  verte sur le mutant, reste rouge sur l'original
- MUT-5 remplace la dérivation `merge-base` par l'adjacence `HEAD^` → une fixture à trois commits
  dont le chemin de surface est touché par le PREMIER commit de la plage devient verte sur le
  mutant, reste rouge sur l'original

Bilan `== bilan : <PASS> ok, <FAIL> ko ==`, sortie non nulle si `FAIL` non nul. En-tête : recopier la
limite de fond, et écrire qu'une suite incapable de rougir est un défaut.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e scripts/tests/test-check-gate-touche.sh</automated>
    <fails_when>exit non nul ; ou une ligne `ko` ; ou `NON OPPOSABLE` dans la sortie ; ou le bilan annonce un nombre de `ko` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); bash scripts/tests/test-check-gate-touche.sh > "$T/o" 2>&1; m=$(awk "/MUT-[1-5]/ && /✓/ {c++} END{print c+0}" "$T/o"); p=$(awk "/✓/{c++} END{print c+0}" "$T/o"); echo "mutants tues=$m assertions ok=$p"; [ "$m" -ge 5 ] && [ "$p" -ge 24 ]'</automated>
    <fails_when>`mutants tues=` inférieur à 5 ; ou `assertions ok=` inférieur à 24 (les cinq classes de surface et les deux contrôles négatifs ne sont pas tous exercés)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend 0, aucun `ko`, aucun `NON OPPOSABLE`, MUT-1 à MUT-5 tués
    - les cinq classes de surface sont chacune exercée par un cas PASS et au moins une par un cas FAIL
    - les deux contrôles négatifs restent verts : gate d'un autre module seul → 3 ; chemin hors surface seul → 3
  </acceptance_criteria>
  <done>G-2 a une suite qui sait la mettre en défaut sur chacune de ses quatre comparaisons et sur sa dérivation de base.</done>
</task>

<task type="auto">
  <name>Task 3 : étape CI à quatre bascules, couverture rétroactive de la branche, CHANGELOG, rejeu</name>
  <files>.github/workflows/ci.yml, CHANGELOG.md</files>
  <precondition>Tasks 1 et 2 vertes ; `rtk proxy git status --porcelain` ne liste que les chemins de ce plan.</precondition>
  <action>
Compléter l'étape `check-gate-touche` du job `gates` pour porter QUATRE bascules isolées sur dépôt
jetable reconstruit DEPUIS ZÉRO à chaque fois par une fonction locale — une mutation à la fois,
jamais cumulée :
1. chemin de surface touché et couvert par un trailer conforme → attendu rc 0
2. chemin de surface touché sans trailer → attendu rc 1, sortie portant `CHEMIN-NON-DECLARE`
3. trailer présent mais sans séparateur ni raison → attendu rc 1, sortie portant `MARQUEUR-MAL-FORME`
4. seul un chemin hors surface touché → attendu rc 3, sortie portant `RIEN-A-JUGER`
Compteur `fail`, fonction `note`, bilan `== BILAN preuve fixture : <n> écart(s) sur 4 verdicts ==`
puis `exit 1` si `n` non nul, puis la phrase qui dit l'étape discriminante. Ensuite la mesure du
dépôt réel, inchangée.

**Couverture rétroactive de la branche.** Mesurer la sortie de G-2 sur le dépôt réel. Si elle rend
rc 1, les chemins non déclarés sont ceux touchés par le plan 41-14 (script, suite, `ci.yml`) avant
l'existence de la garde. Conduite — la seule autorisée : ajouter, dans le commit de cette tâche ou
dans un commit de documentation dédié de cette branche, les trailers `Gate-Touche:` manquants, un
par chemin ou un motif couvrant, chacun avec sa raison réelle (par exemple : le chemin est le
livrable du plan qui crée la garde). Interdits explicites : amender ou réécrire un commit déjà
poussé, élargir la définition de la surface, désactiver l'étape, ou ajouter une exception au script.
Consigner au SUMMARY la liste exacte des chemins couverts rétroactivement et le commit porteur — la
portée branche du marqueur est ce qui rend ce geste légitime, et il doit rester lisible.

`CHANGELOG.md` racine, section `## Non releasé` : ajouter une entrée sous celle de G-1, nommant G-2,
la surface surveillée (cinq classes) et sa borne, la forme du trailer `Gate-Touche:`, sa nature
déclarative, la portée branche, la limite de fond, et l'autorisation « arbitrage Samuel,
AskUserQuestion session principale, 2026-09-17 ». Aucune section `## [vX.Y.Z]`, aucune touche à la
`VERSION` racine, aucun bump de module.

Rejouer ensuite les DEUX jobs avec la découverte complète, jamais une liste recopiée, et rejouer le
recensement `tools/check-aucune-fermeture.sh` après écriture du CHANGELOG. Consigner au SUMMARY
chaque ligne `REJEU`, le nombre d'étapes `gates`, le bilan du job `tests`, tout `SKIP` avec sa
raison, et le périmètre non rejoué. Un rouge n'est jamais contourné : s'il est étranger au plan (cas
connu T28-F de `test-dev-orchestrator.sh`), le vérifier sur une extraction de la base de `main`,
le consigner, le remonter au manager.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash scripts/check-gate-touche.sh > "$T/o" 2>&1 || rc=$?; cat "$T/o"; s=$(awk "/^decouverte: /{for(i=1;i<=NF;i++) if (\$i ~ /^chemins_surface=/) {split(\$i,a,\"=\"); print a[2]+0}}" "$T/o"); echo "G-2 depot reel rc=$rc chemins_surface=${s:-0}"; [ "$rc" -eq 0 ] && [ "${s:-0}" -ge 3 ]'</automated>
    <fails_when>rc différent de 0 (au moins un chemin de surface de la branche reste non déclaré) ; ou `chemins_surface=` inférieur à 3 (la garde ne voit pas les chemins réellement touchés par les plans 41-14 et 41-16 : script, suite, `ci.yml`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU|^== BILAN preuve fixture/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 12 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 12 (l'étape G-2 n'a pas été ajoutée) ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); echo "tests rc=$rc bilan(N M)=$b"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -ge 81 ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N inférieur à 81 (la suite de G-2 n'est pas découverte) ; ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'a=$(awk "/^## Non releasé/{f=1;next} /^## \\[/{f=0} f && /check-gate-touche/{c++} END{print c+0}" CHANGELOG.md); v=$(awk "NR==1{print \$1}" VERSION); r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh >/dev/null 2>&1 || r=$?; echo "changelog_G2=$a VERSION=$v recensement_rc=$r"; [ "$a" -ge 1 ] && [ "$v" = "v2.63.2" ] && [ "$r" -eq 0 ]'</automated>
    <fails_when>`changelog_G2=` à 0 ; ou `VERSION=` différent de `v2.63.2` ; ou `recensement_rc=` différent de 0 (le texte écrit associe une garde ou O-3 à un mot d'achèvement)</fails_when>
  </verify>
  <acceptance_criteria>
    - l'étape `check-gate-touche` porte quatre bascules isolées, bilan de fixture à 0 écart
    - G-2 rend 0 sur le dépôt réel, avec au moins 3 chemins de surface vus et tous déclarés ; la liste des chemins couverts rétroactivement et son commit porteur sont au SUMMARY
    - rejeu `gates` rc 0 avec au moins 12 étapes ; rejeu `tests` rc 0 avec au moins 81 suites et 0 échec
    - `CHANGELOG.md` § Non releasé porte l'entrée G-2 ; `VERSION` racine intacte ; aucun module bumpé ; recensement rc 0
  </acceptance_criteria>
  <done>G-2 est câblée, prouvée rouge sur fixture dans son étape, et elle se déclare elle-même — la branche qui la crée est la première que sa garde couvre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → la garde qui la juge | G-2 est dans sa propre surface : la PR jugée peut la modifier, sa suite et son étape comprises |
| raison déclarée → réalité de la modification | le marqueur est déclaratif, sa véracité n'est pas vérifiable par machine |
| définition de la surface → couverture réelle | une classe retirée de la surface rend la garde muette sans la rendre rouge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-67 | Tampering | une même PR modifie le gate, sa suite et l'étape CI, et reste verte | high | accept | aucune règle côté serveur n'existe (aucun accès admin) ; G-2 rend le fait VISIBLE et TRACÉ par un marqueur déclaratif ; la limite est écrite en en-tête, dans l'ADR-072, au CHANGELOG et au SUMMARY — rien n'est verrouillé |
| T-41-68 | Tampering | une classe silencieusement retirée de la surface surveillée | high | mitigate | mutant MUT-1 (classe retirée → fixture rouge devient verte) ; les cinq classes écrites en toutes lettres dans l'outil et exercées chacune par un cas PASS de la suite |
| T-41-69 | Repudiation | raison de complaisance dans un trailer `Gate-Touche:` | medium | accept | forme exigée (chemin, séparateur, raison de 10 caractères au moins) ; la véracité reste hors d'atteinte d'une machine, écrit en en-tête ; le trailer rend la modification relisable, il ne la justifie pas |
| T-41-70 | Denial of service | faux rouge permanent sur les chemins touchés avant l'existence de la garde | high | mitigate | portée BRANCHE du marqueur (un commit ultérieur couvre) ; conduite de couverture rétroactive écrite en Task 3, avec interdits explicites (aucun amendement de commit poussé, aucune exception dans le script) |
| T-41-71 | Information disclosure | étape verte par vacuité (aucun chemin de surface touché lu comme un succès) | high | mitigate | rc 3 `RIEN-A-JUGER` distinct de rc 0 ; ligne `decouverte:` à quatre compteurs ; bascule 4 de l'étape CI ; plancher `chemins_surface` ≥ 3 dans le verify de la Task 3 |
</threat_model>

<verification>
`bash -n` et rc 64 ; dépôt réel en 0 avec au moins 3 chemins de surface déclarés ; suite verte, cinq
mutants tués, contrôles négatifs verts ; étape CI à quatre bascules, bilan à 0 écart ; rejeu `gates`
(≥ 12 étapes) et `tests` (≥ 81 suites, 0 échec) verts ; `CHANGELOG.md` § Non releasé ; `VERSION`
racine intacte ; recensement `tools/check-aucune-fermeture.sh` rc 0.
</verification>

<success_criteria>
La modification, par une PR, de ce qui la juge est signalée et tracée par un marqueur déclaratif.
Rien n'est empêché — aucune règle côté serveur n'existe — et chaque texte de la garde le dit.
</success_criteria>

## Artifacts this phase produces

- `scripts/check-gate-touche.sh` — verdicts `DECLARE`, `CHEMIN-NON-DECLARE`, `MARQUEUR-MAL-FORME`,
  `RIEN-A-JUGER`, `PLAGE-VIDE` ; codes 0 / 1 / 2 / 3 / 64 ; options `--root`, `--base-ref`.
- `scripts/tests/test-check-gate-touche.sh` — MUT-1 à MUT-5.
- Étape `gates` : `check-gate-touche (G-2, PROT-05 — la PR modifie ce qui la juge)`.
- Convention de dépôt : trailer `Gate-Touche: <chemin-ou-motif> — <raison>`, portée branche.

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-16-SUMMARY.md` when done
</output>

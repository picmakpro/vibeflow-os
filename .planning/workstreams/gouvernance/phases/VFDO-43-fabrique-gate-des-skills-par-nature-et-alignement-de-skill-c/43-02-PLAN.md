---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 02
type: execute
wave: 3
depends_on: ["43-01", "43-03"]
files_modified:
  - plugin/conductor/scripts/check-skills.sh
  - plugin/conductor/scripts/tests/test-check-skills.sh
autonomous: true
requirements: [FABR-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Pour chacun des trois marqueurs de B-03, check-skills.sh compare INDÉPENDAMMENT la déclaration frontmatter (vf-gate-bloquant, vf-livrable-tiers, vf-couche-qualite) aux motifs trouvés dans les lignes du corps retenues par le paramètre de plan PORTEE_DERIVE (Q2, en attente de Willy — bloc de contexte), dans les DEUX sens : motif trouvé sans déclaration true → avertissement « dérive » ; déclaration true sans motif → avertissement « écart » (D-Q1)"
    - "Un ou plusieurs marqueurs déclarés true alors que vf-nature n'est pas procedure → avertissement « écart » citant B-03 et C-15 ; le gate ne réécrit ni ne déduit jamais la nature"
    - "La recherche de motifs ne lit QUE les lignes du corps retenues par PORTEE_DERIVE, jamais le frontmatter ni une ligne d'un bloc de code délimité : un mot du vocabulaire dans description: ou dans un bloc de code ne produit jamais d'avertissement (T26a, trois options) ; une ligne de prose nue ou en gras n'en produit que si PORTEE_DERIVE la retient (T26b, T26c)"
    - "Toute dérive ou tout écart s'exprime en exit 0 avec le diagnostic imprimé, y compris sous --strict — jamais un exit 1, jamais un quatrième code de sortie (D-Q5) ; sous --hook, une ligne compacte, rc 0"
    - "Le corpus réel reste vert (rc 0) sous --strict ; la liste exacte des avertissements de dérive mesurés sur le corpus est publiée dans le SUMMARY, sans aucune correction du corpus (D-Q5, backlog séparé)"
    - "Le vocabulaire de motifs est une décision de plan documentée dans l'en-tête du script, avec son jumeau positif et négatif par marqueur et des mutants tués"
  artifacts:
    - path: "plugin/conductor/scripts/check-skills.sh"
      provides: "detecter_derive, ecart_nature_marqueurs, lignes_de_portee, constante PORTEE_DERIVE, vocabulaire MOTIFS_MARQUEURS"
      contains: "def detecter_derive"
    - path: "plugin/conductor/scripts/tests/test-check-skills.sh"
      provides: "T22 à T25, T26a à T26c, T27 à T32, MUT-DR1, MUT-DR2, MUT-DR3"
      contains: "MUT-DR3"
  key_links:
    - from: "check_file() de check-skills.sh"
      to: "detecter_derive() et ecart_nature_marqueurs()"
      via: "appels uniques warnings.extend(...) — jamais errors (D-Q5)"
      pattern: "warnings.extend\\(detecter_derive\\("
    - from: "lignes_de_portee()"
      to: "MOTIFS_MARQUEURS"
      via: "recherche insensible à la casse, limitée à la portée PORTEE_DERIVE, hors frontmatter et hors blocs de code"
      pattern: "MOTIFS_MARQUEURS"
  prohibitions:
    - statement: "MUST NOT transformer un avertissement de dérive en refus (exit 1) ni introduire un code de sortie propre à la dérive dans cette phase, même sous --strict (D-Q5, choix de Willy)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; couvert aussi par MUT-DR2 (le mutant qui promeut la dérive en erreur est tué) mais sans descripteur câblé"
    - statement: "MUST NOT taire un avertissement de dérive du corpus en réécrivant un titre, en ajoutant des marqueurs ou vf-nature aux SKILL.md existants, ou en élargissant une exclusion : la mise en conformité du corpus part au backlog (D-Q5, entrée e36e6f2)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : se relit sur le diff (files_modified ne contient aucun SKILL.md)"
---

<objective>
Ajouter à check-skills.sh la détection d'écart entre la déclaration factuelle des trois marqueurs de
B-03 et les motifs de forme procédurale trouvés indépendamment dans la prose du corps, en
avertissement seulement, puis mesurer et publier ce que cette détection voit sur le corpus réel.

Purpose: FABR-07 (D-Q1 « les deux, en écart » ; D-Q5 « avertissement dans cette phase »). Le gate
signale un écart, il ne tranche jamais seul qu'un skill est une procédure. Écart assumé vis-à-vis de
D-11 de la Phase 42 (invariants armés en erreur, corpus corrigé dans la phase) : nouveau gate, corpus
non encore mesuré, choix de Willy (43-CONTEXT.md D-Q5).
Output: `detecter_derive`, `ecart_nature_marqueurs`, vocabulaire documenté, T22 à T32, trois
mutants tués, relevé du corpus réel dans le SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-01-SUMMARY.md

Mesure préalable du planificateur (titres markdown du corps, hors blocs de code, corpus hors
`plugin/reference`, 2026-09-25) — à re-mesurer par l'exécuteur, jamais recopiée en dur dans un test :
- motif « gate bloquant » : `plugin/conductor/skills/vf-new-lab/SKILL.md` (« Phase 3 — Gate A », « Phase 4 — … Gate B », « Gates en express »), `plugin/skill-creator/skills/skill-creator-workflow/SKILL.md` (« Phase 5 — Escalation … (BLOQUANT) »), `plugin/software-architecture/SKILL.md` (« Tier 1 — Gate local », « gate de release ») ;
- motif « couche de qualité » : `skill-creator-workflow` (« Checklist qualite »), `plugin/mobile-test/SKILL.md` (« Couche jugement »), `plugin/audit-architecture/SKILL.md` (« la couche d'audit ») ;
- motif « livrable remis à un tiers » : aucun titre.
Soit environ 5 skills sur 21 — même ordre de grandeur que les 23 % de la spec §6 (mesurés sur un
autre corpus, 142 skills). « HUMAN-GATED » (kpi-analyst) ne matche pas : la frontière de mot après
« gate » l'exclut. Cette mesure a été faite sous l'option `titres` du paramètre ci-dessous.

**Paramètre de plan `PORTEE_DERIVE` — Q2, EN ATTENTE de la réponse de Willy.** Seul endroit du
plan où la portée de la détection de dérive est décidée ; le reste du plan dit « la portée
PORTEE_DERIVE » et n'en fixe jamais la valeur. Le planificateur ne la choisit pas.
- **Valeur : `EN ATTENTE — Q2`** — à remplacer ICI SEULEMENT par `titres`, `prose` ou
  `titres+gras`, suivie de la citation de la réponse (« arbitrage Willy, <canal>, AAAA-MM-JJ »).
  Tant qu'elle vaut `EN ATTENTE — Q2`, la Tâche 1 s'arrête sans rien écrire (sa précondition).
- Option `titres` : les seules lignes de titre markdown du corps (dièses de un à six suivis d'un
  blanc). Rétrécit le texte de FABR-07 (« motifs … trouvés indépendamment dans la prose du
  corps ») : à ratifier comme tel. Faux positifs bas (discipline de RULES_TITLE_RE, 25 faux
  positifs mesurés pour un motif nu, check-instruction-budget.sh l.186-187) ; une procédure dont le
  gate n'apparaît qu'en prose ou en gras n'est pas vue.
- Option `prose` : toutes les lignes du corps. Lecture littérale de FABR-07 ; risque de faux
  positifs plus élevé ; les questions factuelles que 43-03 ajoute aux deux SKILL.md de
  skill-creator (« gate bloquant ? … couche de qualite ? ») seront vues par la détection.
- Option `titres+gras` : les lignes de titre plus toute ligne du corps qui porte un segment en gras
  (`**…**`). Compromis entre les deux.
- Invariants des trois options : jamais le frontmatter (description: comprise), jamais une ligne
  d'un bloc de code délimité (bascule sur une ligne commençant par trois accents graves ou trois
  tildes), recherche insensible à la casse, même vocabulaire MOTIFS_MARQUEURS.
- Cas rattachés au paramètre, et à lui seul : **T26b** (ligne de prose nue portant « gate » :
  avertissement si et seulement si `prose`) et **T26c** (ligne du corps en gras, hors titre :
  avertissement si et seulement si `prose` ou `titres+gras`) ; les valeurs imprimées, jamais
  assertées, de T32 et de CORPUS-DERIVE. Tous les autres cas (T22 à T25, T26a, T27 à T31, MUT-DR1
  à MUT-DR3) n'utilisent que des titres, du frontmatter ou des blocs de code et valent sous les
  trois options.

Ordre avec 43-03 (depends_on, révision du 2026-09-26) : ce plan s'exécute APRÈS 43-03. Sa mesure
du corpus (T32, CORPUS-DERIVE, liste du SUMMARY) porte donc les deux SKILL.md de skill-creator dans
leur état final, question vf-nature comprise ; 43-03 n'a lu que l'en-tête de check-skills.sh posé
par 43-01 (contrat de clés), jamais la détection de dérive, qui n'existe qu'à partir d'ici.
Référence de phase (B43) : la branche porte encore la Phase 42 tant que la PR #108 n'est pas
mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de la base de
branche — mesuré le 2026-09-25). Tout contrôle « ce que la Phase 43 a touché » part donc du dernier
commit qui touche les PLAN.md de la phase (`git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md'`),
jamais de la base de branche. Les gardes G-1 et G-2 gardent leur propre base : elles jugent toute la
branche, Phase 42 comprise.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : dérive en écart, dans les deux sens, par marqueur, sur la portée PORTEE_DERIVE — vocabulaire documenté, jamais un refus (T22 à T29, MUT-DR1, MUT-DR2)</name>
  <reversibility rating="costly">Le vocabulaire de motifs est lu par le gate et doit rester cohérent avec les questions factuelles que posent skill-creator (43-03) et l'initialisation (C-15) ; le changer sans elles romprait la cohérence outil ↔ déclaration (D-Q1, costly).</reversibility>
  <precondition>le paramètre PORTEE_DERIVE du bloc de contexte porte l'une des valeurs titres, prose ou titres+gras avec sa citation canal + date — tant qu'il vaut « EN ATTENTE — Q2 », la tâche s'arrête sans rien écrire</precondition>
  <files>plugin/conductor/scripts/check-skills.sh, plugin/conductor/scripts/tests/test-check-skills.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-skills.sh (fichier modifié : check_file, parse_frontmatter, VIBEFLOW_SKILL_FIELDS, rapport --hook, posés en 43-01)
    - plugin/conductor/scripts/tests/test-check-skills.sh (fichier modifié : harnais, make_gate_mutant, okmut/komut)
    - plugin/conductor/scripts/check-agents.sh (l.778-795 invariant_i1 : contrôle à deux sens, deux messages distincts ; l.1209-1233 rétrogradation D-05 en avertissement, jamais un code de sortie propre)
    - plugin/conductor/scripts/check-instruction-budget.sh (l.84-87 MARKER_RE et RULES_TITLE_RE ; l.170-195 : titres comme portée, 25 faux positifs mesurés sur un motif nu)
    - docs/superpowers/specs/2026-09-22-fabrique-agents-skills-design.md (§6 : « un gate bloquant, un livrable remis à un tiers, une couche de qualité »)
    - docs/superpowers/specs/2026-09-23-initialisation-lab-design.md (§5.2 : marqueurs en questions factuelles ; l.237 : les marqueurs ne se comptent jamais dans les blocs de code)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Pattern 2, Pitfall 2, Assumption A2)
  </read_first>
  <behavior>
    - T22 : titre `## Gate de validation` sans aucune déclaration → rc 0, un avertissement « dérive » qui nomme vf-gate-bloquant et reprend le texte du titre
    - T23 : même fixture + `vf-gate-bloquant: true` → aucun avertissement pour ce marqueur
    - T24 : `vf-gate-bloquant: true` sans aucun mot du vocabulaire nulle part dans le corps (fixture valable sous les trois options de PORTEE_DERIVE) → rc 0, avertissement « écart — vf-gate-bloquant: true déclaré sans motif »
    - T25 : `vf-gate-bloquant: false` + titre `### Verdict bloquant` → rc 0, avertissement « dérive » (déclaré false)
    - T26a (invariant, trois options) : le mot « gate » dans description: et dans une ligne `## Gate` située à l'intérieur d'un bloc de code délimité → aucun avertissement
    - T26b (rattaché à PORTEE_DERIVE) : ligne de prose nue du corps « Ce skill passe un gate avant envoi » → avertissement « dérive » si PORTEE_DERIVE vaut `prose`, aucun sinon
    - T26c (rattaché à PORTEE_DERIVE) : ligne du corps `**Gate de validation** : relecture avant envoi` (gras, pas un titre) → avertissement « dérive » si PORTEE_DERIVE vaut `prose` ou `titres+gras`, aucun si `titres`
    - T27 : titre `## Livrable remis au client` → avertissement nommant vf-livrable-tiers ; titre `### Checklist qualité` → avertissement nommant vf-couche-qualite ; titre `## Couche jugement` → idem ; titre `## Grille tarifaire` → AUCUN avertissement (une grille n'est un marqueur que qualifiée de jugement, notation, évaluation ou qualité)
    - T28 : fixture de T22 sous `--strict` → rc 0 (jamais 1, D-Q5) ; sous `--hook` → rc 0 et une seule ligne compacte « ⚠ »
    - T29 : titre `## Déploiement HUMAN-GATED` → aucun avertissement (frontière de mot)
    - MUT-DR1 : ligne `warnings.extend(detecter_derive(` remplacée par `pass` → fixture T22 : l'avertissement « dérive » est présent sur l'original (compte ≥ 1) et absent sur le mutant (compte 0) ; okmut appelé avec ces comptes à la place des rc
    - MUT-DR2 : même ligne remplacée par la ligne d'appel ENTIÈRE, Python valide, où seul `warnings.extend` devient `errors.extend` (make_gate_mutant remplace la LIGNE ENTIÈRE : un remplacement réduit à `errors.extend(detecter_derive(` laisserait des parenthèses non refermées — SyntaxError, rc 1 par plantage — et le helper le refuse à la compilation) → fixture T22 : rc_original=0, rc_mutant=1 ET trace du verdict visé : le message « derive » figure sur une ligne `  ⚠ ` de l'original et sur une ligne `  ✗ ` du mutant (sous l'en-tête « non-conformite(s) bloquante(s) »), jamais sur une ligne `  ✗ ` de l'original — prouve que la suite verrouille l'exit 0 de D-Q5 ; okmut reçoit la trace en sixième argument
  </behavior>
  <action>
1. Avant tout commit du plan, écrire la base du plan (`git rev-parse HEAD`) sur une ligne dédiée « Base du plan : <sha> » de 43-02-SUMMARY.md, créé dès maintenant (ligne placée hors du frontmatter, en début de ligne, et conservée quand le SUMMARY est complété puis commité en fin de plan, commit `.planning/` séparé) : le critère Gate-Touche du premier commit relit cette ligne. Puis, AVANT d'écrire la moindre fixture, re-mesurer le corpus réel : pour chaque dossier plugin/*/ hors module doc-only, les lignes de la portée PORTEE_DERIVE (hors frontmatter et blocs de code) qui matchent le vocabulaire de l'étape 2, fichier par fichier. La mesure du bloc de contexte date du 2026-09-25 et de l'option `titres` : elle se re-mesure, jamais recopiée en dur dans un test. Ensuite seulement, tests d'abord (T22 à T25, T26a à T26c, T27 à T29, MUT-DR1, MUT-DR2), joués avant l'implémentation — les cas qui attendent un avertissement partent ROUGES, les jumeaux qui n'en attendent aucun sont des gardes anti-faux-positif, verts dès le départ — puis implémentation. Les fixtures déclarent une nature valide et sont conformes à FABR-06 pour que seule la dérive soit en jeu.
2. Décision de plan — vocabulaire (43-RESEARCH.md Pitfall 2 : aucun précédent codé, à concevoir ; discipline de portée de RULES_TITLE_RE), recopiée dans l'en-tête du script sous « Détection de dérive (D-Q1, D-Q5, FABR-07) » avec la mesure du corpus :
   - portée : celle du paramètre PORTEE_DERIVE du bloc de contexte, recopiée dans l'en-tête du script avec les trois options et la citation de la réponse de Willy, et portée dans le code par une constante unique `PORTEE_DERIVE` que lit `lignes_de_portee` ; sous toutes les options, après fermeture du frontmatter et hors blocs de code délimités (bascule sur une ligne commençant par trois accents graves ou trois tildes) ; recherche insensible à la casse ;
   - `MOTIFS_MARQUEURS`, un motif par clé : vf-gate-bloquant → `\bgates?\b` ou `\bbloquant(e|s|es)?\b` ; vf-livrable-tiers → `\blivrables?\b` ou `\bremis(e|es)?\s+(a|à|au|aux)\b` ou `\bdestinataires?\b` ; vf-couche-qualite → `\bjuges?\b`, `\brubriques?\b`, `\bgrilles?\s+(de\s+)?(jug|notation|[ée]valuation|qualit)`, `checklist\s+qualit`, `couche\s+(de\s+)?(jugement|qualit|d.audit)`, `quality\s+gate` ;
   - hors de la portée PORTEE_DERIVE, un mot du vocabulaire n'est jamais un motif (la mesure anti-faux-positif du dépôt — 25 faux positifs pour un motif nu, check-instruction-budget.sh l.186-187 — reste l'argument de l'option `titres`, documenté à côté des deux autres).
3. Gate :
   - `lignes_de_portee(texte)` rend la liste des lignes du corps retenues par PORTEE_DERIVE ;
   - `detecter_derive(rel, fm, lignes)` rend une LISTE de messages, deux sens par marqueur (patron invariant_i1, jamais un message fusionné) : motif trouvé et déclaration absente ou false → « {rel} : derive — motif de <marqueur> dans la ligne « <ligne échappée> » sans <cle>: true declare (D-Q1) » ; déclaration true et aucun motif → « {rel} : ecart — <cle>: true declare sans motif de <marqueur> dans la portee du corps (D-Q1) » ;
   - appel unique dans check_file, sur sa propre ligne : `warnings.extend(detecter_derive(` (cible MUT-DR1 et MUT-DR2) — JAMAIS errors ;
   - le rapport garde son contrat : avertissements `⚠`, rc 0 si aucune erreur FABR-06, sous `--strict` comme sans ; sous `--hook`, la ligne compacte du compte d'avertissements.
4. Mutants avec make_gate_mutant (motif unique, cmp, bash -n, compilation du Python muté posée en 43-01 : un mutant qui ne compile pas est refusé, jamais compté comme tué). Pour MUT-DR1, le critère de mort est la présence de l'avertissement (comptée par grep) sur l'original et son absence sur le mutant, les deux rc valant 0. Pour MUT-DR2, le remplacement est calculé par la suite à partir de la ligne d'appel d'origine, lue dans le script par grep -F sur le motif : ligne entière recopiée, seul le mot `warnings` de tête devenant `errors` ; la mort exige les deux rc ET la trace (« derive » passé d'une ligne ⚠ de l'original à une ligne ✗ du mutant), transmise à okmut.
5. Commit : `feat(conductor): détection de dérive procédurale en écart, avertissement seul (FABR-07)` avec
   `Gate-Touche: plugin/conductor/scripts/check-skills.sh — détection de dérive en écart (D-Q1, D-Q5)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-skills.sh — T22 à T29, mutants MUT-DR1, MUT-DR2`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-skills.sh 2>&1 | grep -E '✓ T2([2-9]|6[abc]) |MUT-DR[12] |✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T22 à T25, T26a, T26b, T26c, T27 à T29 n'a aucune ligne « ✓ », ou « ✓ MUT-DR1 TUE » ou « ✓ MUT-DR2 TUE » manque, ou la ligne « ✓ MUT-DR2 TUE » ne porte pas « · trace : », ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>DEBUT=$(sed -n 's/^Base du plan : \([0-9a-f]\{7,40\}\).*$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-02-SUMMARY.md | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; revs=$(git rev-list --reverse "$DEBUT"..HEAD -- plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh) || echo "GIT-ECHEC rev-list"; first=$(printf '%s\n' "$revs" | head -1); test -n "$first" || echo "AUCUN-COMMIT-G2"; chemins=$(git diff-tree --no-commit-id --name-only -r "$first" -- plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh) || echo "GIT-ECHEC diff-tree"; msg=$(git show -s --format=%B "$first") || echo "GIT-ECHEC show"; for p in plugin/conductor/scripts/check-skills.sh plugin/conductor/scripts/tests/test-check-skills.sh; do printf '%s\n' "$chemins" | grep -qxF "$p" || continue; printf '%s\n' "$msg" | grep -qE "^Gate-Touche: $p (—|-) " || echo "NON-COUVERT $p"; done; echo "PREMIER-COMMIT-G2 $first"</automated>
    <fails_when>une ligne BASE-ABSENTE, GIT-ECHEC, AUCUN-COMMIT-G2 ou NON-COUVERT apparaît avant « PREMIER-COMMIT-G2 <sha> » : base du plan non consignée, ou le PREMIER commit de ce plan qui touche check-skills.sh ou sa suite (surface G-2) ne porte pas, dans SON message, un trailer Gate-Touche par chemin de la surface qu'il touche. G-2 (43-06) juge la branche entière et ne voit pas ce manquement local.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'warnings.extend(detecter_derive(' plugin/conductor/scripts/check-skills.sh` rend 1 et `grep -c 'errors.extend(detecter_derive(' plugin/conductor/scripts/check-skills.sh` rend 0
    - `grep -c 'MOTIFS_MARQUEURS' plugin/conductor/scripts/check-skills.sh` rend au moins 2 (définition et usage) et l'en-tête contient « Détection de dérive (D-Q1, D-Q5, FABR-07) »
    - `grep -cE "^PORTEE_DERIVE = ['\"](titres|prose|titres\+gras)['\"]" plugin/conductor/scripts/check-skills.sh` rend 1, et la valeur est celle du paramètre du bloc de contexte, citation comprise dans l'en-tête
    - la sortie de test-check-skills.sh contient une ligne qui commence par `✓ MUT-DR2 TUE : rc_mutant=1 attendu 1, rc_original=0 attendu 0 · trace :`, une ligne `✓ MUT-DR1 TUE`, des lignes `✓ T22` à `✓ T25`, `✓ T26a`, `✓ T26b`, `✓ T26c`, `✓ T27` à `✓ T29`, et finit sur `0 KO ==` (T1 à T21 toujours verts)
    - le premier commit du plan sur la surface G-2 couvre chaque chemin qu'il touche : la seconde commande du verify imprime `PREMIER-COMMIT-G2 <sha>` sans aucune ligne NON-COUVERT, GIT-ECHEC, AUCUN-COMMIT-G2 ni BASE-ABSENTE
  </acceptance_criteria>
  <done>La dérive est détectée en écart dans les deux sens pour les trois marqueurs, bornée à la portée PORTEE_DERIVE arbitrée par Willy, hors frontmatter et hors code, toujours en avertissement ; deux mutants tués, dont celui qui promeut la dérive en erreur, attesté par sa trace ; le premier commit du plan sur la surface G-2 porte ses trailers.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : écart nature ↔ marqueurs, mesure et publication du corpus réel (T30 à T32, MUT-DR3)</name>
  <files>plugin/conductor/scripts/check-skills.sh, plugin/conductor/scripts/tests/test-check-skills.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-skills.sh (fichier modifié : detecter_derive et check_file de la Tâche 1)
    - plugin/conductor/scripts/tests/test-check-skills.sh (fichier modifié : cas T4, arbre réel)
    - docs/superpowers/specs/2026-09-23-initialisation-lab-design.md (§5.2 : « au moins un → procédure · aucun → outil, la valeur par défaut » ; C-15)
    - .planning/BACKLOG.md (section « Mise en conformité du corpus de skills en dérive procédurale non déclarée », posée par vf-dev-manager, commit e36e6f2 — lecture seule)
  </read_first>
  <behavior>
    - T30 : `vf-couche-qualite: true` + titre `## Checklist qualité` + vf-nature absente → rc 0, avertissement « ecart — marqueur(s) vf-couche-qualite declare(s) mais vf-nature: outil » ; la même fixture en vf-nature: procedure (avec ecrit et vf-rubrique-juge) → aucun écart de nature
    - T31 : les trois marqueurs à false, aucun mot du vocabulaire nulle part dans le corps, vf-nature absente → aucun avertissement
    - T32 (arbre réel) : même boucle que T4 (dossiers plugin/*/ hors doc-only), `--strict` → rc 0 partout ; la suite imprime le nombre total d'avertissements de dérive et d'écart du corpus, sans valeur assertée en dur
    - MUT-DR3 : ligne `warnings.extend(ecart_nature_marqueurs(` remplacée par `pass` → mutant accepté par le helper seulement si son Python compile ; fixture T30 : rc_original=0 attendu 0 ET rc_mutant=0 attendu 0 (un plantage rend rc 1 : jamais un mutant tué) ; trace du verdict visé : la ligne `  ⚠ ` « ecart — marqueur(s) vf-couche-qualite declare(s) mais vf-nature: outil » comptée 1 sur l'original et 0 sur le mutant — le compte porte sur CE message, pas sur un « ecart » quelconque (detecter_derive en émet d'autres) ; okmut reçoit les deux rc et la trace en sixième argument
  </behavior>
  <action>
1. Tests d'abord (T30 à T32, MUT-DR3), puis implémentation.
2. Gate : `ecart_nature_marqueurs(rel, fm)` rend une LISTE : au moins un marqueur déclaré true et vf-nature différente de procedure (absente comptée comme outil) → « {rel} : ecart — marqueur(s) <liste> declare(s) mais vf-nature: <valeur> — au moins un marqueur designe une procedure (B-03, C-15) ; la nature reste declaree, jamais deduite » ; appel unique `warnings.extend(ecart_nature_marqueurs(` dans check_file. La nature n'est ni réécrite ni déduite (B-03 écarte la dérivation automatique).
3. Mesure du corpus réel (lecture seule) : rejouer le gate sur chaque dossier plugin/*/ hors doc-only (commande de vérification) ; consigner dans le SUMMARY la valeur de PORTEE_DERIVE sous laquelle la mesure est faite, la liste exacte — fichier, marqueur, ligne — des avertissements, et le compte, pour alimenter l'entrée de backlog de mise en conformité (e36e6f2) que vf-dev-manager tient ; ne modifier AUCUN SKILL.md du corpus.
4. En-tête : compléter la section « Détection de dérive » par la règle d'écart de nature et la date + le compte de la mesure réelle.
5. Commit : `feat(conductor): écart nature ↔ marqueurs signalé, corpus réel mesuré (FABR-07)` avec
   `Gate-Touche: plugin/conductor/scripts/check-skills.sh — écart entre marqueurs déclarés et nature (B-03, C-15)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-skills.sh — T30 à T32, mutant MUT-DR3`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-skills.sh 2>&1 | grep -E '✓ T3[0-2] |MUT-DR3 |✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T30, T31, T32 n'a aucune ligne « ✓ », ou « ✓ MUT-DR3 TUE » manque ou ne porte pas « rc_mutant=0 attendu 0, rc_original=0 attendu 0 · trace : », ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>n=0; fail=0; for d in plugin/*/; do find "$d" -name SKILL.md -type f | grep -q . || continue; if [ -f "$d/module.json" ] && grep -Eq '"type"[[:space:]]*:[[:space:]]*"doc-only"' "$d/module.json"; then continue; fi; out=$(bash plugin/conductor/scripts/check-skills.sh --strict --skills-dir="$d" 2>&1) || fail=$((fail+1)); n=$((n + $(printf '%s\n' "$out" | grep -cE 'derive|ecart'))); printf '%s\n' "$out" | grep -E 'derive|ecart'; done; echo "CORPUS-DERIVE fail=$fail avertissements=$n"</automated>
    <fails_when>la dernière ligne n'est pas de la forme « CORPUS-DERIVE fail=0 avertissements=<n> » (un module rendu rc non nul par la dérive ou par une autre règle)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'warnings.extend(ecart_nature_marqueurs(' plugin/conductor/scripts/check-skills.sh` rend 1
    - la sortie de test-check-skills.sh contient une ligne qui commence par `✓ MUT-DR3 TUE : rc_mutant=0 attendu 0, rc_original=0 attendu 0 · trace :` (écart de nature compté 1 sur l'original, 0 sur le mutant), des lignes `✓ T30`, `✓ T31`, `✓ T32`, et finit sur `0 KO ==` (T1 à T29 toujours verts)
    - le rejeu du corpus réel imprime `CORPUS-DERIVE fail=0 avertissements=<n>` et le SUMMARY recopie la liste des avertissements et la valeur de n
    - `base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && liste=$(git diff --name-only "$base" HEAD -- 'plugin/*SKILL.md' ':!plugin/skill-creator/skills/*' ':!plugin/conductor/skills/vf-calibrate/*') && printf '%s' "$liste" | grep -c .` rend 0 à la fin de ce plan (aucun SKILL.md du corpus touché, hors les deux fichiers de skill-creator modifiés par 43-03 et la ligne de vf-calibrate corrigée par 43-06)
  </acceptance_criteria>
  <done>L'écart entre marqueurs déclarés et nature est signalé sans rien déduire ; la dérive du corpus réel est mesurée, publiée et laissée au backlog ; le corpus reste vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose du corps d'un SKILL.md → détection | texte non fiable, reformulable à volonté par l'auteur |
| déclaration frontmatter des marqueurs → comparaison | valeurs validées en 43-01 (true/false stricts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-10 | Repudiation | vocabulaire de dérive | low | accept | un auteur peut éviter les mots du vocabulaire ; ce n'est pas un gate de sécurité mais un avertissement doctrinal (D-Q5) — ne pas sur-investir en règles anti-contournement (43-RESEARCH.md § Known Threat Patterns) |
| T-43-11 | Tampering | contrat de sortie | medium | mitigate | la dérive ne passe que par warnings.extend ; MUT-DR2 prouve que la suite rougit si elle devient une erreur ; T28 sous --strict |
| T-43-12 | Denial of Service (désensibilisation) | faux positifs | medium | mitigate | portée bornée par PORTEE_DERIVE (arbitrée par Willy), toujours hors code et hors frontmatter, « grille » qualifiée, frontière de mot (T26a à T26c, T27, T29) ; hook SessionStart non câblé dans cette phase (voir 43-06) |
| T-43-13 | Tampering (injection de journal) | ligne recopiée dans le message | low | mitigate | texte de la ligne passé par la fonction d'échappement unique de 43-01, tronqué à 80 caractères |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-skills.sh` : 0 KO ; T1 à T32 (T26 en T26a à T26c) ; MUT-DR1, MUT-DR2, MUT-DR3 tués en plus des cinq mutants de 43-01 — MUT-DR2 et MUT-DR3 par leur trace, chaque mutant compilé par le helper.
- Premier commit du plan sur la surface G-2 : `PREMIER-COMMIT-G2 <sha>` sans ligne NON-COUVERT.
- Rejeu du corpus réel : `CORPUS-DERIVE fail=0 avertissements=<n>`, liste publiée au SUMMARY.
</verification>

<success_criteria>
- FABR-07 : écart déclaration/prose signalé dans les deux sens, par marqueur, en avertissement (exit 0) ; écart marqueurs/nature signalé ; corpus non corrigé ici.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| `detecter_derive()`, `ecart_nature_marqueurs()`, `lignes_de_portee()` | fonctions de check-skills.sh |
| `MOTIFS_MARQUEURS` | vocabulaire de motifs (décision de plan, costly) |
| constante `PORTEE_DERIVE` | portée de la détection (Q2, valeur arbitrée par Willy) |
| jetons de message `derive`, `ecart` | diagnostics (avertissements) |
| `T22` à `T25`, `T26a` à `T26c`, `T27` à `T32`, `MUT-DR1`, `MUT-DR2`, `MUT-DR3` | cas et mutants |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-07 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici : deux sens par marqueur (T22, T24), déclaration false vs absente (T25), frontmatter et blocs de code toujours hors portée (T26a), prose nue et gras selon PORTEE_DERIVE (T26b, T26c), qualificatif de « grille » (T27), frontière de mot (T29), --strict et --hook (T28), écart de nature (T30).
- **Vocabulaire = choix de conception, confiance BASSE** (43-RESEARCH.md A2) : fondé sur les mots littéraux de la spec §6 et sur les titres réellement mesurés du corpus ; une ligne procédurale formulée autrement ne sera pas vue (sous-détection assumée pour un avertissement).
- **Portée = Q2, en attente de Willy** : le paramètre PORTEE_DERIVE du bloc de contexte porte les trois options (titres, prose, titres+gras) ; le planificateur n'en choisit aucune. Sous `titres`, une procédure dont le gate n'apparaît qu'en prose ou en gras ne sera pas signalée ; la portée reste une décision réversible (D-Q5 reversible), à rouvrir avec la mise en conformité du corpus.

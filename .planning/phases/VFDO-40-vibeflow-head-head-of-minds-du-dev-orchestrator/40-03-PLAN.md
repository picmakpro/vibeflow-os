---
phase: 40-vibeflow-head-head-of-minds-du-dev-orchestrator
plan: 03
lot: L4 — exec-doctrine
type: execute
wave: 2
depends_on: ["40-01"]
autonomous: true
requirements: [HEAD-01]
files_modified:
  - plugin/dev-orchestrator/references/head-governance.md
  - plugin/dev-orchestrator/references/_index.md
  - plugin/dev-orchestrator/AGENT.md
  - plugin/dev-orchestrator/skills/vf-dev/SKILL.md
  - plugin/dev-orchestrator/skills/vf-auto/SKILL.md

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "HEAD-01 — la règle d'échelle (quick / debug / execute / manager, sens unique) vit dans `head-governance.md`, et nulle part ailleurs : `AGENT.md` et `vf-auto/SKILL.md` la CITENT, ne la dupliquent pas (ADR-030)"
    - "HEAD-01 — `head-governance.md` porte les quatre compétences : règle d'échelle, séquencement, contrat de sortie, économie"
    - "HEAD-01 — la voie workstreams est écrite comme EXTENSION NON LIVRÉE, avec ses quatre exigences et son déclencheur (partition effective d'un lab) — D-02"
    - "D-11 — `head-governance.md` dit explicitement que le head ne relâche ni ne reprend JAMAIS un verrou de driver ; le release est le geste du tenant"
    - "D-02 — le fan-out d'exécution n'est borné par AUCUN nombre : seules la disjonction des périmètres et les plafonds de budget le bornent (Pitfall 12 = catalogue, pas runtime)"
    - "ADR-029 — `AGENT.md` reste sous le plafond de 250 lignes, `SEUIL_EQUIPE` inchangé, zéro agent neuf"
    - "`_index.md` liste `head-governance.md` : l'ensemble des fichiers du dossier et l'ensemble des lignes de l'index coïncident exactement"
  artifacts:
    - plugin/dev-orchestrator/references/head-governance.md
    - plugin/dev-orchestrator/AGENT.md
  key_links:
    - "`AGENT.md` §Gouvernance de sortie ↔ `head-governance.md` §3 ↔ `check-mission-exit.sh` : la règle vit dans la référence, l'agent la cite, le script l'exécute"
    - "`AGENT.md` heuristique 7 ↔ `vf-auto/SKILL.md` §Taille ↔ `head-governance.md` §1 : trois citations d'une seule règle d'échelle, jamais trois formulations — et aucune des trois n'est muette sur le mode (conversation vs boucle autonome, D-09)"
    - "`intent-routing.md` reste la SOURCE UNIQUE de la correspondance intention → brique ; `head-governance.md` n'y ajoute que la règle d'ÉCHELLE"
---

<objective>
**Lot L4 `exec-doctrine` du DAG de mission** — ce PLAN.md est l'unique plan du lot L4. Il tourne en
**vague 2, en parallèle du lot L2 (plan 40-02)** : leurs périmètres de fichiers sont strictement
disjoints (L2 = `mission-contracts.md` + `vf-dev-manager.md` ; L4 = les cinq fichiers ci-dessus).
**C'est cette disjonction qui autorise le parallélisme — ne l'élargis pas d'un fichier.**

Faire naître la référence de gouvernance du head, et restructurer l'agent pour qu'il la CITE au
lieu de la porter.

Purpose: HEAD-01 et le critère de succès n°3 de la Phase 40. Le head gagne quatre compétences ;
si elles atterrissaient dans le prompt de l'agent, ADR-029 sauterait et ADR-030 aussi (deux voix
pour une règle). Elles vont donc dans une référence on-demand, l'agent y renvoie en une ligne.

Output: `head-governance.md` (neuf), sa ligne d'index, un `AGENT.md` restructuré sous plafond, et
deux citations — jamais deux copies — dans les skills.

**Écart TRACER_MODE assumé** — voir plan 40-01 : phase de doctrine et de scripts, aucune pile
applicative à traverser, aucune tâche `type="tracer"` émise.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-CONTEXT.md
@.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-PATTERNS.md
@docs/superpowers/specs/2026-09-15-vibeflow-head-design.md
@plugin/dev-orchestrator/references/mission-flow.md
@plugin/dev-orchestrator/references/autonomous-guardrails.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : créer head-governance.md et sa ligne d'index</name>

  <read_first>
    docs/superpowers/specs/2026-09-15-vibeflow-head-design.md (§3.1 à §3.4 — le contenu des quatre compétences ; §6 — la voie workstreams et ses quatre exigences ; §7 — la lecture du Pitfall 12)
    plugin/dev-orchestrator/references/mission-flow.md (structure : patterns lettrés, chacun une section `##` avec un cadrage court — le gabarit STRUCTUREL à imiter)
    plugin/dev-orchestrator/references/_index.md (intégral, 21 lignes — le gabarit de ligne de table et l'ordre alphabétique en vigueur)
    plugin/dev-orchestrator/references/workstreams.md (§5 « La condition dure » — à citer, jamais à recopier)
  </read_first>

  <files>plugin/dev-orchestrator/references/head-governance.md, plugin/dev-orchestrator/references/_index.md</files>

  <action>
Crée `plugin/dev-orchestrator/references/head-governance.md`, référence **on-demand** de
gouvernance du head, et ajoute sa ligne dans `_index.md`.

**Forme.** Gabarit structurel de `mission-flow.md` : un chapeau en citation qui dit qui la charge et
quand, puis quatre sections `##` numérotées, chacune ouverte par un cadrage court. Gabarit de
tableau et de bloc clôturé de `mission-contracts.md` quand un format exact doit être montré.
Longueur visée : 120 à 170 lignes. Elle est écrite pour être **déplaçable** : si un jour le head
devenait cross-métier (écarté par D-01), ce fichier se déplace sans être réécrit — n'y écris donc
rien qui ne vaille que pour le module `dev-orchestrator`.

**§1 — Règle d'échelle (allocation, dans un seul sens).** La table de proportionnalité du §3.1 de
la spec, graduée du moins cher au plus cher, avec la règle cardinale : **on escalade, on ne
redescend jamais en cours de geste**. Les huit lignes vont de la tâche triviale à la conformité du
lab. Trois renvois obligatoires, aucune recopie : `intent-routing.md` reste la **source unique** de
la correspondance intention → brique (cette référence n'ajoute que l'échelle, jamais la
correspondance) ; le seuil canonique `SEUIL_EQUIPE` est **défini dans** `mission-contracts.md`
§Seuil de bascule — cite-le, ne réécris jamais sa valeur ; les signaux « mission » sont la liste
canonique de `mission-contracts.md` §Signaux. Termine par la règle de déclenchement (D-09) : sur
signal mission le head **PROPOSE** le dispatch d'un manager et attend le feu vert en conversation ;
sous une boucle autonome ou sur un signal de durée explicite, il dispatche d'office sans redemander
— ADR-031 intact, l'heuristique de proposition inchangée.

**§2 — Séquencement (parallélisme au niveau mission).** Le manager parallélise des nœuds à
périmètres disjoints ; le head parallélise des MISSIONS — et **sur ce dépôt, aujourd'hui, il les
SÉRIALISE** (D-02). Ce qu'il fait réellement de plus : lire les dépendances déclarées de la feuille
de route, ordonner les missions restantes, détecter les étapes indépendantes et les **déclarer** au
manager dans le brief pour que la frontière `ready` en profite. Écris explicitement que **le
fan-out d'exécution n'est borné par aucun nombre** : ses seules bornes sont la disjonction des
périmètres et les plafonds de budget — la formule « pas plus de deux ou trois agents d'un coup » est
un signal d'alerte sur l'**ajout d'agents au catalogue** dans une PR, jamais une limite
d'orchestration à l'exécution (§7 de la spec). Termine par un encadré **« Extension non livrée —
un manager par workstream »** qui nomme les quatre exigences que cette voie imposerait (verrou
compartiment-aware avec amendement daté de l'ADR du socle d'équipe ; garde de verrou alignée ;
un pointeur de workstream et une clé de session distincts par manager ; une preuve d'usage
concurrent RÉEL, pas sur clone jetable) et son **déclencheur** : la partition effective d'un lab,
elle-même gatée humain. Cite `workstreams.md` §La condition dure — ne la recopie pas.

**§3 — Contrat de sortie (vérifier le témoin, pas refaire le travail).** Le principe en une phrase :
un vert du manager est accepté s'il porte une preuve machine ; le head rejoue **uniquement** le gate
dont la preuve manque, **jamais un étage, jamais la revue**, et ne relit jamais un diff. Puis :
la résolution du gate par la **cascade des scripts frères** documentée dans `mission-flow.md`
§Résolution — jamais un chemin en dur ; les six contrôles E1 à E6 en une ligne chacun, avec renvoi
à l'en-tête du script pour le détail ; les quatre codes de sortie et la **conduite par code** —
sain : enchaîner · manque(s) nommé(s) : mandat de clôture ciblée au manager, jamais corrigé par le
head (ADR-031) · indéterminé : mission traitée comme non prouvée, jamais annoncée verte, et le dire
au rapport · outillage illisible : escalade humaine. Trois règles de conduite s'y ajoutent :
(a) **le head ne relâche ni ne reprend JAMAIS un verrou de driver** — sur un verrou encore tenu il
renvoie un mandat de clôture ciblée au manager, puis escalade à l'humain avec la commande de
reprise à jouer ; le relâchement reste le geste du tenant (D-11) ; (b) un gate rejoué l'est avec la
**commande canonique** que le verdict devait porter, nommée par le contrat de preuves — jamais une
liste locale, jamais le job de gates complet de l'intégration continue (D-12) ; (c) **une** preuve
manquante se rejoue, **deux ou plus** signalent que la source n'a pas appliqué le contrat : mandat
de clôture ciblée et source consignée pour amendement, jamais une rafale de re-jeux (D-14). Cite le
titre **FIGÉ par le manager de mission** `## Contrat de preuves E6 (verdict → head)` de
`mission-contracts.md` — verbatim, section ET fichier. Ce titre n'existe pas encore au moment où ce
plan est écrit : il est posé par le lot L2 (`40-02`), qui tourne en vague 2 **en parallèle** de ce
lot. **Ne pas ajouter `depends_on: ["40-02"]`** — le frontmatter cite le titre à l'avance parce
qu'il est déjà arbitré, ce serait sérialiser deux lots à périmètres disjoints pour rien.

**§4 — Économie (trois règles, un décompte).** Les trois règles du §3.4 de la spec : ne jamais
relire ce que le digest porte ; ne jamais rejuger un diff sans nouveau commit (un verdict PASS sur
un SHA vaut tant que le HEAD de la branche est ce SHA) ; relayer verbatim, jamais recalculer. Puis
le décompte par mission — minds dispatchés, tours consommés, gates rejoués — **trois lignes au
rapport existant, pas une statistique, aucun fichier neuf** (D-13, le registre inter-missions est
explicitement écarté). Termine en renvoyant à `autonomous-guardrails.md` pour les bornes dures
(plafonds de temps et de jetons, trois essais, anti-triche), **inchangées**.

**Quatre contraintes de rédaction, toutes gatées par machine ou par doctrine :**

- **Aucun chemin absent d'un lab installé.** Ce fichier est distribué dans des labs : n'écris
  jamais un chemin sous `.planning/research/` ni sous `docs/_mission` — le motif exact que T11
  ferme (`test-dev-orchestrator.sh:1548`). `.planning/` lui-même N'EST PAS banni : il existe dans
  tout lab installé (`AGENT.md` l'écrit lui-même une dizaine de fois et passe T11) — en particulier
  le contrôle **E5** du §3 s'y définit par le chemin du rapport détaillé `.planning/missions/…` et
  DOIT rester écrivable tel quel. Cite des noms de fichiers frères du dossier `references/` et des
  numéros d'ADR pour le reste.
- **Aucun verbe-façade supprimé.** Les 29 verbes de la façade morte sont énumérés par la constante
  `DELETED_VERBS` de `test-dev-orchestrator.sh` ; une seule occurrence de l'un d'eux dans un fichier
  de `references/` rend la suite rouge. Écris les noms de briques réellement vivants : les skills
  `gsd-*`, `vf-auto`, `vf-design`, `vf-planning`, la commande d'audit du validator, et les agents
  `vf-dev-manager`, `vf-design-manager`, `vf-test-orchestrator`.
- **Une seule voix (ADR-030).** Chaque règle définie ailleurs se CITE avec son fichier ET sa
  section. Un renvoi au fichier entier n'est pas un renvoi.
- **Kernel intact.** Rien dans ce fichier ne prescrit de modifier le socle d'équipe, le script de
  verrou, le script de DAG, la garde de verrou, ni la valeur du seuil d'équipe.

**Puis `_index.md`.** Ajoute UNE ligne au gabarit exact des onze existantes, à sa place dans l'ordre
alphabétique en vigueur (entre l'index des skills et la doctrine d'ingestion), avec un résumé d'une
phrase. L'index est maintenu à la main et **aucun gate machine du dépôt ne vérifie sa cohérence** :
c'est un risque silencieux, d'où la vérification d'égalité d'ensemble ci-dessous.

Commit par pathspec, en français : `docs(40): head-governance.md — règle d'échelle, séquencement,
contrat de sortie, économie (HEAD-01, D-02, D-09, D-11, D-12, D-14) — arbitrage Samuel,
AskUserQuestion session principale, 2026-09-15`.
  </action>

  <verify>
    <automated>test -z "$(comm -3 <(find plugin/dev-orchestrator/references -maxdepth 1 -type f -name '*.md' | xargs -n1 basename | /usr/bin/grep -v '^_index\.md$' | LC_ALL=C sort) <(/usr/bin/grep -oE '^\| \[[^]]+\]' plugin/dev-orchestrator/references/_index.md | sed -E 's/^\| \[(.*)\]$/\1/' | LC_ALL=C sort))"</automated>
    <fails_when>La commande sort non-zéro : `comm -3` a imprimé au moins une ligne, c'est-à-dire qu'un fichier du dossier `references/` manque à l'index (colonne 1) ou qu'une ligne de l'index désigne un fichier absent (colonne 2). Relancer le `comm -3` seul nomme lequel.</fails_when>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>La suite sort non-zéro. Les cas exposés par cette tâche sont `T11` (un chemin non installé en lab cité dans `references/`) et `T13` (un verbe-façade supprimé ressuscité dans un fichier du dossier). Chacun imprime une ligne `✗` nommant le fichier.</fails_when>
    <automated>test "$(/usr/bin/grep -cE 'vibeflow-dev' plugin/dev-orchestrator/references/head-governance.md)" = "0"</automated>
    <fails_when>La commande sort non-zéro parce que le compte n'est pas `0` : l'ancien nom d'agent (pré-renommage L1) apparaît dans ce fichier NEUF. Aucun cas numéroté de `test-dev-orchestrator.sh` ne couvre ce fichier avant `40-01` ne le pose dans son propre scope — ce témoin-ci ferme la propriété directement, sans dépendre d'un cas d'un autre lot.</fails_when>
    <automated>for s in "Règle d'échelle" "Séquencement" "Contrat de sortie" "Économie"; do /usr/bin/grep -q "$s" plugin/dev-orchestrator/references/head-governance.md || { echo "section manquante : $s"; exit 1; }; done</automated>
    <fails_when>La commande imprime `section manquante : <titre>` et sort 1 : l'une des quatre compétences du head n'a pas de section dans la référence, donc HEAD-01 n'est tenu qu'en partie.</fails_when>
    <automated>/usr/bin/grep -v '^#' plugin/dev-orchestrator/references/head-governance.md | tr '\n' ' ' | /usr/bin/grep -qiE 'ne[[:space:]]+(relâche|relache)[[:space:]]+ni[[:space:]]+ne[[:space:]]+(reprend|reprends)'</automated>
    <fails_when>La commande sort non-zéro : la référence ne dit nulle part que le head ne relâche ni ne reprend jamais un verrou (D-11). Mesure prise sur le corps REPLIÉ et hors lignes de commentaire, parce que la formule est multi-mots et que le fichier est enroulé à 100 colonnes — une mesure ligne à ligne la manquerait silencieusement.</fails_when>
    <automated>test "$(/usr/bin/grep -cE '\.planning/research/|docs/_mission' plugin/dev-orchestrator/references/head-governance.md)" = "0"</automated>
    <fails_when>La commande sort non-zéro parce que le compte n'est pas `0` : la référence cite un chemin absent d'un lab installé, motif EXACT de T11 (`test-dev-orchestrator.sh:1548`, `\.planning/research/|docs/_mission`) — pas `.planning/` dans son ensemble, qui existe et doit rester citable (ex. le chemin du rapport E5).</fails_when>
    <automated>/usr/bin/grep -qF 'Contrat de preuves E6 (verdict → head)' plugin/dev-orchestrator/references/head-governance.md</automated>
    <fails_when>La commande sort non-zéro : §3 ne cite pas verbatim le titre complet figé `## Contrat de preuves E6 (verdict → head)` de `mission-contracts.md` (décision manager, M3) — le renvoi ADR-030 (fichier ET section) n'est pas tenu. **Resserré au plan-check 2026-09-15** : l'ancien motif `'Contrat de preuves E6'` est une SOUS-CHAÎNE du titre — une reformulation ou une troncature du suffixe `(verdict → head)` y échapperait alors que `40-02` vient de figer ce titre verbatim comme interface entre trois plans ; l'ancre porte désormais le titre complet.</fails_when>
    <automated>test "$(awk 'END{print NR}' plugin/dev-orchestrator/references/head-governance.md)" -ge 120 -a "$(awk 'END{print NR}' plugin/dev-orchestrator/references/head-governance.md)" -le 170</automated>
    <fails_when>La commande sort non-zéro : le fichier neuf sort de la fourchette 120-170 lignes prescrite pour cette référence (seule borne de densité posée sur ce fichier).</fails_when>
    <automated>/usr/bin/grep -qiE "n'est borné (par )?aucun (chiffre|nombre)|aucun nombre ne (le |la )?borne|borné par aucun nombre" plugin/dev-orchestrator/references/head-governance.md</automated>
    <fails_when>La commande sort non-zéro : §2 n'affirme nulle part que le fan-out d'exécution n'est borné par aucun nombre — la propriété D-02 la plus exposée du lot resterait sans témoin, alors qu'elle contredit un chiffre encore présent ailleurs dans le module (Pitfall 12).</fails_when>
    <automated>/usr/bin/grep -qF 'Extension non livrée' plugin/dev-orchestrator/references/head-governance.md && test "$(awk '/Extension non livrée/,/^##|^---/' plugin/dev-orchestrator/references/head-governance.md | /usr/bin/grep -cE '^[-*] ')" -ge 4</automated>
    <fails_when>La commande sort non-zéro : soit l'encadré « Extension non livrée » est absent, soit il porte moins de quatre puces — les quatre exigences (verrou compartiment-aware amendé, garde de verrou alignée, pointeur de workstream + clé de session distincts, preuve d'usage concurrent réel) ne sont pas toutes nommées.</fails_when>
  </verify>

  <acceptance_criteria>
    - `plugin/dev-orchestrator/references/head-governance.md` existe, fait entre 120 et 170 lignes, et porte quatre sections `##` correspondant aux quatre compétences.
    - §1 contient la table d'échelle et cite `intent-routing.md` comme source unique de la correspondance, `mission-contracts.md` §Seuil de bascule pour le seuil, `mission-contracts.md` §Signaux pour les signaux — sans recopier aucune des trois.
    - §2 contient l'encadré « Extension non livrée » avec ses quatre exigences et son déclencheur, et affirme que le fan-out d'exécution n'est borné par aucun nombre.
    - §3 nomme les six contrôles, les quatre codes et la conduite par code, et porte les trois règles D-11 / D-12 / D-14.
    - §4 porte les trois règles d'économie et le décompte à trois lignes, en renvoyant à `autonomous-guardrails.md` pour les bornes dures.
    - Aucun chemin sous `.planning/research/` ou `docs/_mission` (motif T11), aucun verbe-façade supprimé, aucune valeur de seuil recopiée. `.planning/missions/…` (rapport E5) reste citable.
    - `_index.md` compte une ligne de plus, et l'égalité d'ensemble dossier ↔ index est vérifiée par commande.
  </acceptance_criteria>

  <done>La référence existe, l'index la liste, et la suite du module reste verte (`0 KO`).</done>
</task>

<task type="auto">
  <name>Task 2 : restructurer AGENT.md — déplacer la carte, ajouter la gouvernance de sortie, tenir le plafond</name>

  <read_first>
    plugin/dev-orchestrator/AGENT.md (intégral, 205 lignes — le découpage ci-dessous est donné section par section sur cette numérotation)
    plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh (bloc T3 — plafond de 250 lignes ET plancher de 11 lignes d'intentions, mesuré sur les lignes de table hors séparateurs et hors en-tête « Intention » ; bloc T7 — garde-fou premier usage ; bloc T10 — routage mission ; bloc T5 — densité par comptage de lignes)
  </read_first>

  <files>plugin/dev-orchestrator/AGENT.md</files>

  <action>
Restructure l'agent pour qu'il devienne le **head of minds** : il cite sa référence de gouvernance
au lieu de porter les règles, et il récupère le budget de lignes en déplaçant la table de
proportionnalité vers la source qui la porte déjà.

**Le découpage, section par section, sur la numérotation actuelle (205 lignes).** Ce qui n'est pas
nommé ci-dessous **ne bouge pas**.

1. **Frontmatter, l.1-7.** La clé `name` porte déjà le nouveau nom (lot L1, vague 1). Réécris la
   seule `description:` pour qu'elle décrive le rôle de head : détecter l'intention et invoquer
   directement la brique outillée (inchangé), **plus** allouer le bon niveau d'équipe sur une
   échelle à sens unique, séquencer les missions selon les dépendances de la feuille de route,
   contrôler l'état du dépôt à la sortie d'un manager sur témoin machine sans refaire ce que les
   équipes ont déjà prouvé, et compter ce qu'elles coûtent. **Ne touche NI `model`, NI `effort`,
   NI `memory`** — les trois sont exigés par le contrôleur d'agents en mode strict.
2. **Chapeau Mission unique et loi d'airain, l.11-14.** Réécris sur place, à longueur constante,
   pour intégrer la dimension head (allouer, séquencer, vérifier le témoin, compter).
3. **Persona, l.18-25.** Titre `##` en l.18, ligne vide en l.19, contenu réel en l.20-25 (six
   lignes). Réécris ce contenu à longueur constante (six lignes), en head of minds : il décide quel
   niveau d'équipe employer et l'orchestre, il ne produit pas.
4. **Garde-fou premier usage, l.29-41 — INCHANGÉ, au mot près.** Le cas T7 mesure la présence de
   quatre marqueurs dans ce bloc ; toute réécriture risque d'en effacer un.
5. **Carte d'intention, l.45-96 — c'est ICI que le budget se libère.** Le bloc actuel fait
   52 lignes réparties en trois sous-tables de 9, 10 et 8 lignes de données. Remplace-le par un
   bloc d'environ 31 lignes : le même titre `##`, un cadrage de six à huit lignes qui dit (a) que
   la carte EXHAUSTIVE vit dans une seule source, `intent-routing.md`, chargée on-demand, et (b)
   que la **règle d'échelle** — quel niveau d'équipe, dans quel sens — vit dans `head-governance.md`
   §1 et n'est pas recopiée ici ; puis **une seule table** de cas dominants, à **quinze lignes de
   données**, remplaçant les vingt-sept actuelles et absorbant les trois sous-titres qui
   disparaissent ; puis, inchangée, la note en citation sur les intentions hors module.
   Les quinze lignes retenues couvrent : réflexion et conception ; prototype jetable ; cadrage et
   plan ; démarrage ou reprise de projet sous confirmation ; implémentation d'une étape ; petite
   tâche ; autonomie complète ; recette ; revue de diff ; dépannage avec recherche documentaire
   préalable ; livraison ; état et next step ; mise à jour documentaire ; design ; et la bascule
   mission vers l'équipe, dont la cellule de droite cite `head-governance.md` §1 pour l'échelle.
   **PLANCHER MACHINE À NE PAS FRANCHIR** : le cas T3 compte les lignes de table de ce fichier en
   excluant les séparateurs et les en-têtes commençant par « Intention », et exige **au moins 11**.
   Quinze lignes de données ici, plus la table des signaux de démarrage restée intacte plus bas,
   laissent une marge confortable — mais si tu réduis davantage, tu franchis le plancher et tu
   rends la suite rouge. **Ne modifie JAMAIS le cas T3 pour te donner de l'air** : un garde ne se
   desserre pas dans le commit qu'il autorise.
6. **Next steps et hygiène documentaire (l.99-116), Signaux de démarrage et doctrine du ledger
   (l.118-146) — INCHANGÉS.** La table des signaux participe au comptage du cas T3 : n'y touche pas.
7. **Heuristiques de routage, l.148-166.** Les heuristiques 1 à 6 restent INCHANGÉES au mot près.
   **L'heuristique 7 est AMENDÉE sur place, pas laissée inchangée** : elle dit aujourd'hui
   « proposer, jamais imposer » sans aucune branche de mode — c'est précisément la clause muette
   sur le mode que T27 existe pour fermer dans ce dépôt. Ajoute-y, sans changer son numéro ni son
   ouverture, la branche que `head-governance.md` §1 définit (D-09) : sur signal mission le head
   **PROPOSE** en conversation et attend le feu vert ; sous une boucle autonome ou sur un signal de
   durée explicite, il dispatche d'office sans redemander. Ferme par un renvoi explicite à
   `head-governance.md` §1 — la règle vit là, l'heuristique la cite, elle ne la reformule pas une
   troisième fois. Puis ajoute une **heuristique 8, « Sortie de mission »**, en quatre à cinq
   lignes : à la fin d'un mandat de manager, vérifier le témoin machine AVANT d'annoncer quoi que
   ce soit, puis appliquer la conduite par code ; et compter ce que la mission a coûté depuis le
   rapport, relayé verbatim, jamais recalculé.
8. **Nouvelle section `## Gouvernance de sortie (après un manager)`**, insérée immédiatement APRÈS
   le bloc des heuristiques et AVANT le séparateur qui précède les garde-fous. Douze à quatorze
   lignes, **en renvoi, jamais en doctrine** : un chapeau en citation qui donne le principe —
   vérifier le témoin, pas refaire le travail — et pointe `head-governance.md` §3 et §4 comme
   doctrine complète chargée on-demand ; puis trois puces : le lancement du gate de sortie via la
   cascade des scripts frères et la lecture de son code avec les quatre conduites en une ligne ;
   la règle « je ne rejoue QUE le gate dont la preuve manque, avec la commande canonique — jamais
   un étage, jamais la revue, jamais une liste locale » ; et, en gras, « je ne relâche ni ne
   reprends jamais un verrou de driver ».
9. **Garde-fous (l.170-178) — INCHANGÉS.**
10. **Lois d'airain (l.180-185)** : ajoute une cinquième loi, une ligne — vérifier le témoin, ne
    jamais refaire le travail déjà prouvé par ses équipes.
11. **Anti-patterns (l.187-194)** : ajoute une ligne — rejouer un étage entier, ou la revue, alors
    qu'un seul gate manquait sa preuve.
12. **Références (l.198-205)** : ajoute une ligne pour `head-governance.md` au gabarit **exact** des
    six existantes, c'est-à-dire avec le chemin tel qu'il existe dans un lab INSTALLÉ (dossier de
    références de l'agent), jamais le chemin du dépôt source.

**Budget attendu** : environ 21 lignes libérées par le point 5, environ 23-25 lignes ajoutées par
les points 7 (amendement de l'heuristique 7 en plus de l'heuristique 8 neuve), 8, 10, 11 et 12 — le
fichier atterrit autour de 207-209 lignes, sous un plafond de 250. Le plafond est la contrainte
dure ; le chiffre exact ne l'est pas. **Relève le compte par `awk 'END{print NR}'` avant et après,
et recopie les deux dans le SUMMARY.**

Commit par pathspec, en français : `docs(40): AGENT.md devient le head of minds — carte condensée,
gouvernance de sortie en renvoi (HEAD-01, ADR-029, ADR-030)`.
  </action>

  <verify>
    <automated>test "$(awk 'END{print NR}' plugin/dev-orchestrator/AGENT.md)" -le 250</automated>
    <fails_when>La commande sort non-zéro : le fichier dépasse 250 lignes et franchit le plafond ADR-029. `awk 'END{print NR}'` rend le compte réel.</fails_when>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>La suite sort non-zéro. Les cas exposés par cette tâche sont nommément `T3` (plafond de 250 dépassé, OU moins de 11 lignes d'intentions restantes, OU un verbe-façade supprimé réintroduit), `T5` (densité), `T7` (un des quatre marqueurs du garde-fou premier usage effacé), `T10` (l'agent ne nomme plus le manager d'équipe) et `T11` (un chemin non installé en lab cité). Chacun imprime une ligne `✗` qui le nomme.</fails_when>
    <automated>bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/dev-orchestrator/AGENT.md</automated>
    <fails_when>Le gate sort non-zéro : le frontmatter a perdu `description`, `model` ou `memory`, ou déclare un skill inexistant (ADR-044). Un exit 3 signifie cible vide et est tout aussi rouge qu'un exit 1.</fails_when>
    <automated>/usr/bin/grep -q 'head-governance.md' plugin/dev-orchestrator/AGENT.md && test "$(/usr/bin/grep -c 'head-governance' plugin/dev-orchestrator/AGENT.md)" -ge 2</automated>
    <fails_when>La commande sort non-zéro : l'agent ne cite pas la référence de gouvernance au moins deux fois (une fois dans le cadrage de la carte d'intention pour la règle d'échelle, une fois dans la section de gouvernance de sortie) — la règle serait devenue injoignable depuis l'agent, ou pire, recopiée localement.</fails_when>
    <automated>test "$(/usr/bin/grep -cE 'SEUIL_EQUIPE[[:space:]]*=' plugin/dev-orchestrator/AGENT.md)" = "0"</automated>
    <fails_when>La commande sort non-zéro parce que le compte n'est pas `0` : la VALEUR du seuil d'équipe a été recopiée dans l'agent, créant une seconde source pour une constante définie dans le fichier de contrats (ADR-030 violé, et la valeur divergera au premier changement).</fails_when>
    <automated>awk '/^7\. \*\*Mission/{f=1} /^8\./{exit} f{print}' plugin/dev-orchestrator/AGENT.md | tr '\n' ' ' | /usr/bin/grep -qiE 'PROPOSE' && awk '/^7\. \*\*Mission/{f=1} /^8\./{exit} f{print}' plugin/dev-orchestrator/AGENT.md | tr '\n' ' ' | /usr/bin/grep -qiE 'autonome' && awk '/^7\. \*\*Mission/{f=1} /^8\./{exit} f{print}' plugin/dev-orchestrator/AGENT.md | tr '\n' ' ' | /usr/bin/grep -q 'head-governance'</automated>
    <fails_when>Une des trois commandes échoue : l'heuristique 7 ne porte plus, à la fois, le mot « PROPOSE » (branche conversation), le mot « autonome » (branche boucle autonome) et le renvoi `head-governance.md` — c'est-à-dire qu'elle redevient une clause de déclenchement de manager MUETTE sur le mode, le défaut que T27 (`test-dev-orchestrator.sh:3634-3641`) existe pour fermer. **Borne resserrée au plan-check 2026-09-15** : l'ancienne borne de sortie `/^8\. \*\*Sortie de mission/{exit}` dépendait du LIBELLÉ exact de l'heuristique 8 ; si l'exécutant renomme cette heuristique (ex. « 8. **Fin de mandat »), `awk` ne trouve plus de borne de repli et capture jusqu'à l'EOF — y compris la section `## Gouvernance de sortie` ajoutée en aval par ce même plan, qui porte elle-même « autonome » et `head-governance` : une heuristique 7 restée MUETTE sur le mode passerait alors ce témoin au VERT par erreur (mesuré par mutation : ancienne borne → 3/3 faux positifs sur une heuristique 7 muette avec heuristique 8 renommée ; nouvelle borne `/^8\./{exit}`, qui coupe sur le NUMÉRO seul quel que soit le libellé qui suit → 2/3 négatifs corrects, witness rouge comme attendu, et 3/3 positifs sur le cas conforme). La borne ne dépend donc plus du texte du point 8, seulement de sa numérotation — ne plus adapter cette regex à un changement de libellé.</fails_when>
  </verify>

  <acceptance_criteria>
    - `awk 'END{print NR}'` sur `AGENT.md` rend une valeur inférieure ou égale à 250 ; les comptes avant et après sont recopiés dans le SUMMARY.
    - Le bloc « Carte d'intention » compte exactement une table de 15 lignes de données et cite `intent-routing.md` ET `head-governance.md` §1.
    - La section `## Gouvernance de sortie (après un manager)` existe, tient en 12 à 14 lignes, est en renvoi pur, et affirme en gras que le head ne relâche ni ne reprend jamais un verrou.
    - Les blocs du garde-fou premier usage, des next steps, des signaux de démarrage et des garde-fous sont **byte-identiques** à leur état d'avant la tâche : le diff du commit de cette tâche, pris sur la plage explicite `<sha>^..<sha>`, ne fait apparaître aucune ligne de ces régions.
    - Une heuristique 8, une cinquième loi d'airain, un anti-pattern et une ligne de références ont été ajoutés.
    - Aucun cas de `test-dev-orchestrator.sh` n'a été modifié par cette tâche (le fichier de suite est absent de `files_modified`).
  </acceptance_criteria>

  <done>`AGENT.md` est sous plafond, la suite du module est verte (`0 KO`) et le contrôleur d'agents en mode strict passe.</done>
</task>

<task type="auto">
  <name>Task 3 : citer la règle d'échelle dans les deux skills, sans la dupliquer</name>

  <read_first>
    plugin/dev-orchestrator/skills/vf-dev/SKILL.md (intégral, 13 lignes)
    plugin/dev-orchestrator/skills/vf-auto/SKILL.md (l.1-45 — le bloc « Pilote unique » puis le bloc « Taille », qui porte le seuil d'équipe et le dispatch du manager)
  </read_first>

  <files>plugin/dev-orchestrator/skills/vf-dev/SKILL.md, plugin/dev-orchestrator/skills/vf-auto/SKILL.md</files>

  <action>
Deux citations, aucune duplication.

**`vf-dev/SKILL.md`.** Le lot L1 a déjà aligné la cible d'incarnation en vague 1. Ici, ajoute au
corps **une seule phrase** disant que l'agent incarné porte aussi la règle d'échelle et la
gouvernance de sortie, définies dans `head-governance.md` — nommé avec sa section, jamais le
fichier seul. **Le nom du skill reste `vf-dev`** (D-17) : ne renomme pas le dossier, ne crée aucun
alias. Et **ne touche pas à la `description:` du frontmatter** : elle est gatée sur trois
propriétés (longueur minimale, déclencheur « Utiliser quand », portée « Invocable »), et elle a
déjà été éditée en vague 1.

**`vf-auto/SKILL.md`.** Ce fichier ne porte aucune occurrence de l'ancien nom — son geste n'est pas
un renommage mais une **citation**. Dans le bloc « Taille », juste au-dessus ou juste en dessous des
deux puces d'aiguillage existantes, ajoute une à deux lignes disant que cet aiguillage est
l'application, au cas de la mission autonome, de la règle d'échelle générale définie dans
`head-governance.md` §1 — et que la règle y vit, non ici. **Ne récris pas les deux puces, ne change
pas le seuil, ne déplace pas le bloc « Pilote unique ».** Deux propriétés du fichier sont gatées :
la mention du seuil d'équipe et la mention du manager d'équipe doivent toutes deux subsister, et le
renvoi existant vers le fichier de contrats aussi.

Un seul commit pour les deux fichiers, par pathspec, en français : `docs(40): vf-dev et vf-auto
citent la règle d'échelle de head-governance.md (HEAD-01, ADR-030, D-17)`.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>La suite sort non-zéro. Les cas exposés sont `T10` (le seuil d'équipe ou le manager ne sont plus nommés dans le skill d'autonomie), `T12` (la description d'un des deux skills a perdu sa longueur minimale, son déclencheur ou sa portée d'invocation), `T4` (une cible référencée devenue orpheline) et `T5` (densité). Chacun imprime une ligne `✗` nommant le skill.</fails_when>
    <automated>test "$(/usr/bin/grep -lc 'head-governance' plugin/dev-orchestrator/skills/vf-dev/SKILL.md plugin/dev-orchestrator/skills/vf-auto/SKILL.md 2>/dev/null | /usr/bin/wc -l | tr -d ' ')" = "2"</automated>
    <fails_when>La commande sort non-zéro parce que le compte de fichiers porteurs n'est pas `2` : l'un des deux skills ne cite pas la référence de gouvernance, donc la règle d'échelle reste soit absente, soit dupliquée localement.</fails_when>
    <automated>test ! -d plugin/dev-orchestrator/skills/vf-head</automated>
    <fails_when>La commande sort non-zéro : un dossier de skill aliasé a été créé, ce qui rouvre la couche de synonymes enterrée en v2.33.0 et contredit D-17 au mot près.</fails_when>
  </verify>

  <acceptance_criteria>
    - Les deux `SKILL.md` citent `head-governance.md` avec sa section, sans reproduire la table d'échelle ni la valeur du seuil.
    - `plugin/dev-orchestrator/skills/vf-dev/` existe toujours sous ce nom ; aucun dossier aliasé n'a été créé.
    - Le diff du commit de cette tâche, pris sur la plage explicite `<sha>^..<sha>`, n'ajoute qu'une à deux lignes à `vf-auto/SKILL.md` et ne réécrit ni les deux puces d'aiguillage ni le bloc de pilote unique.
    - Ce même diff ne fait apparaître aucune ligne du frontmatter de `vf-dev/SKILL.md`.
  </acceptance_criteria>

  <done>Les deux skills citent la règle d'échelle ; la suite du module est verte (`0 KO`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| référence distribuée → lab installé | un chemin ou un nom de brique écrit ici est lu dans des dépôts tiers où le dépôt de développement n'existe pas |
| agent (prompt) → référence on-demand | une règle dupliquée des deux côtés diverge silencieusement à la première modification d'un seul côté |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Tampering | duplication de la règle d'échelle entre agent, skill et référence | medium | mitigate | Renvoi obligatoire avec fichier ET section (ADR-030) ; vérification par commande que la valeur du seuil n'est recopiée nulle part dans l'agent et que les deux skills citent la référence. |
| T-40-10 | Information Disclosure | chemin absent d'un lab installé distribué dans une référence | medium | mitigate | Interdiction explicite du motif exact de T11 (`\.planning/research/\|docs/_mission`) — `.planning/` lui-même reste citable, il existe en lab ; vérification par comptage à zéro sur la référence, et cas `T11` de la suite du module. |
| T-40-11 | Denial of Service | plancher de lignes d'intentions franchi par la condensation de la table | high | mitigate | Quinze lignes de données prescrites contre un plancher machine de onze, plus la table des signaux laissée intacte ; interdiction explicite de modifier le cas de test pour se donner de l'air. |
| T-40-12 | Elevation of Privilege | doctrine autorisant le head à reprendre un verrou | high | mitigate | La référence dit explicitement l'inverse, et la formulation est vérifiée par commande sur le corps replié — une suppression silencieuse de la phrase rend rouge. |
| T-40-SC | Tampering | installs npm/pip/cargo | n/a | accept | Aucun paquet installé — `40-RESEARCH.md` §Package Legitimacy Audit : sans objet. |
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` — vert, `0 KO`.
- `bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/dev-orchestrator/AGENT.md` — vert.
- `awk 'END{print NR}' plugin/dev-orchestrator/AGENT.md` — inférieur ou égal à 250.
- Égalité d'ensemble dossier `references/` ↔ lignes de `_index.md` — aucune ligne rendue par `comm -3`.
- Le diff du lot, pris sur la plage explicite allant de son point de départ à son dernier commit, ne nomme QUE les cinq fichiers de `files_modified` — en particulier ni `mission-contracts.md` ni `vf-dev-manager.md`, qui appartiennent au lot L2 tournant en parallèle.
</verification>

<success_criteria>
- HEAD-01 tenu : les quatre compétences vivent dans une référence on-demand, citées par l'agent et les skills.
- ADR-029 et ADR-030 tenus, sans qu'aucune garde n'ait été desserrée.
- `SEUIL_EQUIPE` inchangé, zéro agent neuf, kernel intact.
</success_criteria>

<artifacts_produced>
- `plugin/dev-orchestrator/references/head-governance.md` — quatre sections : règle d'échelle, séquencement (dont l'encadré « Extension non livrée — un manager par workstream »), contrat de sortie, économie.
- Ligne `head-governance.md` de `plugin/dev-orchestrator/references/_index.md`.
- Section `## Gouvernance de sortie (après un manager)` de `plugin/dev-orchestrator/AGENT.md`.
- Heuristique 7 amendée (branche de mode D-09, renvoi `head-governance.md` §1) et heuristique 8 « Sortie de mission », cinquième loi d'airain, anti-pattern de re-jeu d'étage, ligne de références D7 — tous dans `AGENT.md`.
- Table condensée « Carte d'intention » à 15 lignes de données.
</artifacts_produced>

<output>
Create `.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-03-SUMMARY.md` when done
</output>

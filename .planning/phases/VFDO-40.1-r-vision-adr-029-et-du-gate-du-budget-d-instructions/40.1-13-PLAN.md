---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 13
lot: L7 — doctrine du dépôt, documentation de planning, manuel, seed
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04, BUDG-05]
files_modified:
  - CLAUDE.md
  - docs/ADR.md
  - .planning/PROJECT.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/STRUCTURE.md
  - manual/fr/07-sous-le-capot/les-gates-machine.md
  - manual/en/07-under-the-hood/the-machine-gates.md
  - .planning/seeds/SEED-001-equipe-produit-v1.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 / D-05 — `CLAUDE.md` du dépôt énonce la charte : agents avec avertissement dès 251 lignes, blocage au-delà de 300 ; skills ≤ 500 et bootstrap ≤ 2000 tokens inchangés"
    - "D-H4 — ADR-029 est révisée par une NOTE DATÉE d'amendement (2026-09-16) sous l'entrée, sans ADR nouvelle ; la définition canonique de la table « ADR héritées les plus citées » (« la référence en cas de doute ») est alignée sur 300 et la note le dit ; la note nomme les arbitrages (canal et date), le ratchet d'instructions seul par fichier (D-02/D-07), la mesure propre à chaque outil (D-06), la colonne lignes informative (D-H6)"
    - "D-H2 / O-2 — la note datée Phase 25 sous ADR-029 cesse d'affirmer la sentinelle non posée : elle dit l'état réel (posée le 2026-09-16, commit `a7f414a`, plan 25-04)"
    - "Le manuel FR et EN dit un état VRAI QUEL QUE SOIT l'ordre des releases (révision 2, constat bloquant W3) : plafond 300 avec avertissement dès 251 ; les agents distribués par VibeFlow sont MESURÉS par `check-instruction-budget.sh` dans la CI du dépôt de distribution depuis la PREMIÈRE release qui contient l'introduction du gate, calculée à l'exécution (aujourd'hui v2.62.0) ; le blocage est actif depuis l'armement du ratchet (PR #73). Le manuel n'affirme ni l'absence ni la présence d'un tag contenant l'armement et ne nomme aucune release pour le blocage : le hotfix v2.63.1 part d'`origin/main`, qui contient l'armement (mesuré par le checker), donc toute phrase sur l'état des tags deviendrait fausse. Pas de précondition hotfix pour ce plan : l'énoncé ne dépend d'aucun état de tag. Dans le lab de l'utilisateur, la charte reste une doctrine outillée par le gabarit optionnel de l'auditeur. La clause du seuil de CODE n'est pas touchée"
    - "Règle de classe, pas liste de cas (révision 3, warning W-A) : les deux paragraphes sont une LISTE BLANCHE réduite à un texte relu, le bloc `<canonical_manual>` de ce plan (FR et EN), recopié octet pour octet avec `{R}` remplacé par la release calculée. Toute autre tournure, quelle que soit la langue, qu'elle affirme la présence ou l'absence d'un tag ou d'une release, rend le verify rouge. Révision 5 (constats F1/F2, arbitrage Samuel option (b), AskUserQuestion session principale, 2026-09-16) : la comparaison porte sur la SECTION entière qui contient le paragraphe (du titre `##` au titre suivant de même niveau ou supérieur), pas sur le seul paragraphe : une phrase collée juste avant, un paragraphe ajouté après ou une phrase au milieu de la section rend aussi le verify rouge (`section_canonique=non`). Le compteur de formulations interdites reste en place, en garde secondaire. Révision 4 (constat F1) : le bloc canonique est jugé contre son état FIGÉ AVANT l'exécution, sans sha ni sujet — ancre = le plus ancien commit propre à la branche (depuis la base de `tools/phase-base.sh`) qui AJOUTE un fichier sous `tools/` ou un `40.1-*-SUMMARY.md`, c'est-à-dire le premier geste d'exécution de la phase ; aucun commit de `<ancre>^..HEAD` ne touche `40.1-13-PLAN.md` et l'arbre est propre sur ce fichier ; une retouche commitée, bloc et manuel alignés ensemble, est rouge"
    - "La contrainte prospective du SEED-001 (« Aucun nouvel agent > … lignes ») vaut 300 (classement du manager, digest de mission du 2026-09-16)"
    - "Documentation de planning vivante (PROJECT, codebase ARCHITECTURE/CONVENTIONS/STRUCTURE) alignée ; la ligne `VF_ARCH_WARN` de CONVENTIONS (seuil de code) intacte ; le recensement ne liste plus aucune ligne sous ces neuf fichiers"
  artifacts:
    - path: "docs/ADR.md"
      provides: "note d'amendement ADR-029 datée 2026-09-16"
      contains: "ADR-029 : amendement du 2026-09-16"
  key_links:
    - "CLAUDE.md est la doctrine lue en premier par les agents : son énoncé doit concorder mot pour mot en valeurs avec le gate (40.1-03) et l'amendement ADR"
---

<objective>
Aligner la doctrine du dépôt et la documentation vivante sur les arbitrages D-01, D-02, D-05, D-06,
D-07 (arbitrage Samuel, AskUserQuestion session principale, relais SendMessage, 2026-09-16) et les
décisions du head D-H2, D-H4, D-H6 : `CLAUDE.md`, amendement daté d'ADR-029, documentation de
planning, manuel bilingue, seed. Aucun module, aucun bump.

Purpose: BUDG-04 (critère 1 : `CLAUDE.md`, `docs/ADR.md`, manuel nommés) et publication de la
doctrine de BUDG-05.
Output: neuf fichiers alignés, une note d'amendement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
@CLAUDE.md
</context>

<canonical_manual>
Textes canoniques des SECTIONS qui portent les paragraphes « À ne pas surpromettre » / « Not to
overpromise » (Task 3 ; révision 5, arbitrage Samuel option (b), AskUserQuestion session principale,
2026-09-16) : `## Les contraintes de taille, visibles` / `## Size limits, made visible`, du titre
jusqu'au titre suivant de même niveau ou de niveau supérieur (exclu, lignes vides finales retirées),
sous-section `###` comprise. Relevées sur la base rebasée (HEAD deb4c4b, fichiers identiques à
`origin/main`, le hotfix ne les touche pas), seul le paragraphe « À ne pas surpromettre » /
« Not to overpromise » y étant remplacé par son texte révisé. Chaque ligne est préfixée par `> `
(`>` seul pour une ligne vide) afin qu'aucun titre `##` ne s'insère dans la structure du plan ; le
verify retire ce préfixe, remplace `{R}` par la release R calculée, et compare par `cmp` à la
section extraite du manuel. Seul le paragraphe change dans le manuel : le reste de la section est
déjà identique au bloc. Relus par le planificateur : ils ne citent aucune release autre que R, citent la PR #73,
et n'énoncent aucun état de tag ou de release. Ne pas les modifier à l'exécution : un écart de
rédaction se remonte au manager. Le verify rougit sur toute retouche de ce fichier, commitée ou non,
depuis le premier commit d'exécution de la phase (révision 4, F1).

<canonical_manual_fr>
> ## Les contraintes de taille, visibles
>
> Deux bornes de taille se voient concrètement, et méritent d'être nommées parce que c'est ce qui
> explique un refus que tu pourrais prendre pour un caprice :
>
> - **Le budget de préchargement des skills.** Quand un agent déclare un skill à précharger
>   intégralement à son démarrage (plutôt qu'à charger à la demande), la taille cumulée de ces
>   skills est mesurée et plafonnée — au-delà, l'écriture de l'agent est refusée. C'est la charte de
>   densité rendue concrète : un agent qui charge trop de contenu au démarrage coûte cher à chaque
>   invocation, pour un gain souvent nul.
> - **Le seuil des 300 lignes de code**, déjà couvert dans le tableau ci-dessus, avec son
>   avertissement à 250 lignes avant le blocage ferme.
>
> **À ne pas surpromettre :** la charte recommande de garder un agent à 300 lignes au plus, avec un
> avertissement dès 251. Les agents distribués par VibeFlow sont **mesurés** dans la CI du dépôt de
> distribution depuis {R} (`check-instruction-budget.sh`) ; le blocage — plafond dépassé, ou
> instruction ajoutée sans arbitrage — est actif dans cette CI depuis l'armement du ratchet (PR #73).
> Dans **ton** lab, en revanche, la charte n'est vérifiée par aucun gate posé par défaut : un gabarit
> (l'auditeur de densité) existe dans la bibliothèque méthodologique, mais il n'est pas installé.
>
> ### Un exemple concret : le refus de l'Iron Law
>
> Concrètement, voici ce que tu verrais si tu tentais d'écrire un fichier de code de 320 lignes sans
> rien d'autre : l'écriture est refusée, avec le compte exact de lignes et le seuil dépassé. Deux
> issues honnêtes s'offrent à toi — découper le fichier en plusieurs modules plus petits (la voie
> recommandée), ou reconnaître explicitement la dette en ajoutant la marque d'échappatoire dans les
> premières lignes du fichier, ce qui laisse le gate passer en avertissement plutôt qu'en blocage. Le
> gate ne devine jamais laquelle des deux issues est la bonne pour ton cas — il force seulement le
> choix à être **explicite**, jamais silencieux.
</canonical_manual_fr>

<canonical_manual_en>
> ## Size limits, made visible
>
> Two size limits are concretely visible, worth naming because they're what explains a refusal you
> might otherwise mistake for a whim:
>
> - **The skill preload budget.** When an agent declares a skill to be preloaded in full at startup
>   (rather than loaded on demand), the cumulative size of those skills is measured and capped —
>   beyond that, writing the agent is refused. This is the density charter made concrete: an agent
>   that loads too much content at startup costs more on every invocation, usually for no real gain.
> - **The 300-line code threshold**, already covered in the table above, with its warning at 250 lines
>   before the hard block.
>
> **Not to overpromise:** the charter recommends keeping an agent at 300 lines at most, with a
> warning from 251. Agents distributed by VibeFlow are **measured** in the CI of the distribution
> repository since {R} (`check-instruction-budget.sh`); blocking — cap exceeded, or an instruction
> added without a named decision — has been active in that CI since the ratchet was armed (PR #73).
> In **your** lab, however, the charter is checked by no gate installed by default: a template (the
> density auditor) exists in the methodology library, but it isn't installed.
>
> ### A concrete example: the Iron Law refusal
>
> Concretely, here's what you'd see if you tried to write a 320-line code file with nothing else
> attached: the write is refused, with the exact line count and the exceeded threshold. Two honest
> paths open up — splitting the file into several smaller modules (the recommended route), or
> explicitly acknowledging the debt by adding the escape marker in the file's first lines, which lets
> the gate pass as a warning instead of a block. The gate never guesses which of the two is right for
> your case — it only forces the choice to be **explicit**, never silent.
</canonical_manual_en>
</canonical_manual>

<tasks>

<task type="tracer">
  <name>Task 1 : CLAUDE.md et amendement daté d'ADR-029</name>
  <files>CLAUDE.md, docs/ADR.md</files>
  <read_first>
    - CLAUDE.md (§ Conventions transverses, ligne Densité ; § Traçabilité des arbitrages)
    - docs/ADR.md (en-tête L1-12 ; table « ADR héritées les plus citées » et ses deux notes, ~L48-76 : note ADR-065, note Phase 25 sous ADR-029)
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `CLAUDE.md` et `docs/ADR.md`
  </read_first>
  <action>
`CLAUDE.md` ligne Densité → `- **Densité** (ADR-029) : agents — avertissement dès 251 lignes, bloque au-delà de 300 ; skills ≤ 500, bootstrap ≤ 2000 tokens.`
`docs/ADR.md` : (1) ligne ADR-029 de la table des définitions canoniques → `Charte densité : agents — avertissement dès 251 lignes, bloque au-delà de 300 (amendée le 2026-09-16) ; skills ≤ 500, bootstrap ≤ 2000 tokens`.
(2) Note Phase 25 : remplacer UNIQUEMENT la proposition sur la sentinelle (« NON POSÉE à cette date » et
l'attente de la calibration de la Phase 40) par l'état réel : posée le 2026-09-16 (commit `a7f414a`,
plan 25-04) après la livraison de la Phase 40 (D-H2, O-2) ; le reste de la note inchangé.
(3) Ajouter JUSTE APRÈS, une note datée (D-H4, même forme blockquote) titrée
`**ADR-029 : amendement du 2026-09-16 (Phase 40.1)**` : plafond des agents relevé à 300 lignes,
avertissement non bloquant dès 251 (arbitrages Samuel D-01 et D-05, AskUserQuestion session
principale, relais SendMessage, 2026-09-16) ; chaque outil garde sa mesure — fichier entier pour
`check-instruction-budget.sh`, body pour l'auditeur distribué (D-06) ; le ratchet ne bloque plus que
les instructions, par fichier, une hausse de baseline exigeant un arbitrage nommé avec canal et date
(D-02, D-07) ; la colonne lignes de la baseline est publiée, jamais comparée (D-H6) ; la définition
canonique ci-dessus est amendée en conséquence, l'ancien plafond reste lisible dans le CHANGELOG
conductor ; aucune ADR nouvelle. Aucune valeur de l'ancien plafond écrite dans la note (le
recensement la lirait comme vivante). Aucun autre passage de `docs/ADR.md` touché (le texte
d'ADR-048 reste en l'état, exclu comme histoire).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1==\"CLAUDE.md\" || \$1==\"docs/ADR.md\" {c++} END{print c+0}" "$T/o"); a=$(awk "/ADR-029 : amendement du 2026-09-16/{c++} END{print c+0}" docs/ADR.md); p=$(awk "/NON POSÉE à cette date/{c++} END{print c+0}" docs/ADR.md); s=$(awk "/a7f414a/{c++} END{print c+0}" docs/ADR.md); d=$(awk "/avertissement dès 251 lignes, bloque au-delà de 300/{c++} END{print c+0}" CLAUDE.md); echo "doctrine vivants=$n note_amendement=$a sentinelle_non_posee=$p a7f414a=$s claude_md=$d"; [ "$n" -eq 0 ] && [ "$a" -eq 1 ] && [ "$p" -eq 0 ] && [ "$s" -ge 1 ] && [ "$d" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0, `note_amendement=` différent de 1, `sentinelle_non_posee=` différent de 0, `a7f414a=` égal à 0, ou `claude_md=` différent de 1</fails_when>
    <automated>bash --noprofile --norc -e scripts/check-machine-paths.sh</automated>
    <fails_when>exit non nul (un chemin absolu de machine introduit)</fails_when>
  </verify>
  <!-- planner-discipline-allow: NON POSÉE à cette date -->
  <acceptance_criteria>
    - `CLAUDE.md` contient `avertissement dès 251 lignes, bloque au-delà de 300`
    - `docs/ADR.md` contient exactement une note `ADR-029 : amendement du 2026-09-16`, cite `AskUserQuestion session principale` et `2026-09-16` dans cette note, contient `a7f414a`, ne contient plus la proposition de sentinelle non posée
    - aucune ligne vivante sous `CLAUDE.md` ou `docs/ADR.md` au recensement
  </acceptance_criteria>
  <done>La doctrine du dépôt et le registre ADR disent le plafond 300 et le ratchet d'instructions, par amendement daté.</done>
</task>

<task type="auto">
  <name>Task 2 : documentation de planning vivante (PROJECT, codebase)</name>
  <files>.planning/PROJECT.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/STRUCTURE.md</files>
  <read_first>
    - sortie de `tools/check-no-live-250.sh .` filtrée sur ces quatre fichiers
    - .planning/PROJECT.md (~L116), .planning/codebase/ARCHITECTURE.md (~L214), .planning/codebase/CONVENTIONS.md (~L29-30 : L30 = seuil de CODE, ne pas toucher), .planning/codebase/STRUCTURE.md (~L183)
  </read_first>
  <action>
Chaque ligne listée : énoncé de la charte agents → `agents : avertissement dès 251 lignes, bloque au-delà de 300`
(STRUCTURE : `Respecter la densité ADR-029 (bloque au-delà de 300 lignes, avertissement dès 251)`),
reste de chaque ligne inchangé (skills ≤ 500, bootstrap ≤ 2000 tokens, renvois). Dans
`CONVENTIONS.md`, seule la ligne des agents change ; la ligne portant `VF_ARCH_WARN` est intacte.
`.planning/STATE.md` n'est PAS touché par ce plan.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1==\".planning/PROJECT.md\" || \$1 ~ /^\\.planning\\/codebase\\// {c++} END{print c+0}" "$T/o"); git show "$B":.planning/codebase/CONVENTIONS.md > "$T/c0"; h=$(awk "/VF_ARCH_WARN/" "$T/c0" | cksum); k=$(awk "/VF_ARCH_WARN/" .planning/codebase/CONVENTIONS.md | cksum); echo "planning vivants=$n ligne_code_intacte=$([ "$h" = "$k" ] && echo oui || echo non)"; [ "$n" -eq 0 ] && [ "$h" = "$k" ]; bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md > /dev/null && echo state_integrity_ok'</automated>
    <fails_when>exit non nul ; ou `planning vivants=` différent de 0 ; ou `ligne_code_intacte=non` ; ou absence de `state_integrity_ok`</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `.planning/PROJECT.md` et `.planning/codebase/` ; ligne `VF_ARCH_WARN` de CONVENTIONS identique à la base de phase
  </acceptance_criteria>
  <done>La documentation de planning vivante énonce 300 / dès 251.</done>
</task>

<task type="auto">
  <name>Task 3 : manuel bilingue et contrainte prospective du seed</name>
  <files>manual/fr/07-sous-le-capot/les-gates-machine.md, manual/en/07-under-the-hood/the-machine-gates.md, .planning/seeds/SEED-001-equipe-produit-v1.md</files>
  <read_first>
    - manual/fr/07-sous-le-capot/les-gates-machine.md (puce « Le seuil des 300 lignes de code » et paragraphe « À ne pas surpromettre », ~L57-66)
    - manual/en/07-under-the-hood/the-machine-gates.md (puce « The 300-line code threshold » et paragraphe « Not to overpromise », ~L54-62)
    - .planning/seeds/SEED-001-equipe-produit-v1.md (§ Scope Estimate)
  </read_first>
  <action>
Calculer d'abord R, la première release qui contient l'introduction du gate : premier tag DE
RELEASE (nom conforme à `^v[0-9]+[.][0-9]+[.][0-9]+$`, filtré en awk), en ordre de version, de
`git tag --contains <commit d'ajout de check-instruction-budget.sh>`. Les tags hors release du dépôt
(`backup/…`, `install-ux-v1.0`, `vfdo-v1.0`, tout tag temporaire) sont ignorés (révision 5, W2).

Manuel FR, paragraphe « À ne pas surpromettre » (de la ligne commençant par `**À ne pas surpromettre`
à la ligne vide suivante, exclue) : REMPLACÉ par les six lignes de ce paragraphe dans le bloc
`<canonical_manual_fr>` de ce plan (préfixe `> ` retiré, `{R}` remplacé par R) ; rien d'autre de la
section ne change, et la section entière doit alors égaler le bloc, octet pour octet (révision 5). Contenu tenu par
ce texte :
- la charte recommande de garder un agent à 300 lignes au plus, avec un avertissement dès 251 ;
- les agents distribués par VibeFlow sont MESURÉS dans la CI du dépôt de distribution depuis R
  (`check-instruction-budget.sh`) ;
- le blocage (plafond, et toute instruction ajoutée sans arbitrage) est actif depuis l'armement du
  ratchet, PR #73 ;
- dans TON lab, la charte n'est pas vérifiée par un gate posé par défaut ; un gabarit (l'auditeur
  de densité) existe dans la bibliothèque méthodologique, non installé par défaut.

Propriété à tenir (révision 2) : l'énoncé reste vrai quel que soit l'ordre des releases. Il ne cite
AUCUNE autre release que R, et n'affirme ni l'absence ni la présence d'un tag contenant l'armement.
Révision 3 (warning W-A) : cette propriété est tenue par une LISTE BLANCHE, pas par une liste de
formulations interdites — une liste de cas ne ferme pas la classe « affirmation d'état de tag »,
une réécriture libre pouvant toujours l'énoncer sous une autre tournure. Le texte est donc fixé et
comparé octet pour octet. Si l'exécutant juge le texte canonique faux ou inadapté, il s'arrête et
remonte au manager ; il ne modifie ni le bloc ni le verify.

Manuel EN : paragraphe commençant par `**Not to overpromise`, REMPLACÉ de même d'après le bloc
`<canonical_manual_en>` ; la section `## Size limits, made visible` entière doit égaler le bloc. La puce du seuil de CODE (FR et EN) n'est pas touchée. SEED-001 :
`Aucun nouvel agent > 300 lignes ; tout passe check-agents.sh --strict` (valeur seule).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^manual\\// || \$1 ~ /^\\.planning\\/seeds\\// {c++} END{print c+0}" "$T/o"); git show "$B":manual/fr/07-sous-le-capot/les-gates-machine.md > "$T/f0"; f=$(awk "/Le seuil des 300 lignes de code/{p=2} p{print; p--}" "$T/f0" | cksum); g=$(awk "/Le seuil des 300 lignes de code/{p=2} p{print; p--}" manual/fr/07-sous-le-capot/les-gates-machine.md | cksum); git show "$B":manual/en/07-under-the-hood/the-machine-gates.md > "$T/e0"; e=$(awk "/300-line code threshold/{p=2} p{print; p--}" "$T/e0" | cksum); h=$(awk "/300-line code threshold/{p=2} p{print; p--}" manual/en/07-under-the-hood/the-machine-gates.md | cksum); echo "manuel+seed vivants=$n puce_code_fr_intacte=$([ "$f" = "$g" ] && echo oui || echo non) puce_code_en_intacte=$([ "$e" = "$h" ] && echo oui || echo non)"; [ "$n" -eq 0 ] && [ "$f" = "$g" ] && [ "$e" = "$h" ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0 ; ou `puce_code_fr_intacte=non` / `puce_code_en_intacte=non`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions; PL=$PH/40.1-13-PLAN.md; T=$(mktemp -d); git log --diff-filter=A --format=%H -- plugin/conductor/scripts/check-instruction-budget.sh > "$T/c"; c=$(awk "END{print \$0}" "$T/c"); [ -n "$c" ]; git tag --contains "$c" --sort=v:refname > "$T/tg"; R=$(awk "/^v[0-9]+[.][0-9]+[.][0-9]+\$/ {print; exit}" "$T/tg"); echo "premiere release contenant le gate=$R"; [ -n "$R" ]; printf "%s\n" "$R" > "$T/r"; B=$(bash "$PH/tools/phase-base.sh"); git log --diff-filter=A --format=%H "$B"..HEAD -- "$PH/40.1-*-SUMMARY.md" "$PH/tools/" > "$T/s"; A=$(awk "NF{s=\$1} END{print s}" "$T/s"); if [ -z "$A" ]; then echo "plan 13 : ancre_debut_execution=absente (attendu : commit propre a la branche ajoutant un fichier sous tools/ ou un 40.1-*-SUMMARY.md)"; exit 1; fi; git rev-list "$A^"..HEAD -- "$PL" > "$T/pc"; pc=$(awk "END{print NR}" "$T/pc"); pli=$(git diff --quiet HEAD -- "$PL" && echo oui || echo non); echo "plan 13 : ancre_debut_execution=$A commits_touchant_le_plan_13_depuis_l_ancre=$pc (attendu 0) plan 13 non modifie dans l arbre=$pli"; ok=1; if [ "$pli" != oui ] || [ "$pc" -ne 0 ]; then ok=0; fi; for m in "manual/fr/07-sous-le-capot/les-gates-machine.md:fr:^\\*\\*À ne pas surpromettre" "manual/en/07-under-the-hood/the-machine-gates.md:en:^\\*\\*Not to overpromise"; do fi=${m%%:*}; rs=${m#*:}; lg=${rs%%:*}; re=${rs#*:}; awk -v re="$re" "\$0 ~ re {p=1} p && /^\$/ {p=0} p" "$fi" > "$T/p"; l=$(awk "END{print NR}" "$T/p"); awk -v re="$re" "{ x[NR]=\$0 } END { p=0; for (i=1;i<=NR;i++) if (x[i] ~ re) { p=i; break } if (!p) exit; h=0; for (i=p;i>=1;i--) if (x[i] ~ /^#+ /) { h=i; break } if (!h) exit; match(x[h], /^#+/); L=RLENGTH; e=NR+1; for (i=h+1;i<=NR;i++) if (x[i] ~ /^#+ /) { match(x[i], /^#+/); if (RLENGTH<=L) { e=i; break } } while (e-1>h && x[e-1]==\"\") e--; for (i=h;i<e;i++) print x[i] }" "$fi" > "$T/s"; ls=$(awk "END{print NR}" "$T/s"); awk -v o="<canonical_manual_$lg>" -v f="</canonical_manual_$lg>" -v R="$R" "\$0 == f {p=0} p { sub(/^> ?/, \"\"); i = index(\$0, \"{R}\"); if (i) \$0 = substr(\$0, 1, i-1) R substr(\$0, i+3); print } \$0 == o {p=1}" "$PL" > "$T/k"; lk=$(awk "END{print NR}" "$T/k"); cn=$(cmp -s "$T/k" "$T/s" && echo oui || echo non); dl=$(awk "FILENAME==ARGV[1] {a[FNR]=\$0; n=FNR; next} {b[FNR]=\$0; q=FNR} END { k=(n>q?n:q); for (i=1;i<=k;i++) if (!(i in a) || !(i in b) || a[i] != b[i]) { print i; exit } print 0 }" "$T/k" "$T/s"); awk "{ s=\$0; while (match(s, /v[0-9]+[.][0-9]+[.][0-9]+/)) { print substr(s, RSTART, RLENGTH); s=substr(s, RSTART+RLENGTH) } }" "$T/p" > "$T/v0"; sort -u "$T/v0" > "$T/v"; pr=$(awk "/#73/{c++} END{print c+0}" "$T/p"); ta=$(awk "{ x=tolower(\$0) } x ~ /aucun tag|aucune release|sans release|pas encore publi|no tag|no release|not yet released|untagged/ {c++} END{print c+0}" "$T/p"); vs=$(cmp -s "$T/v" "$T/r" && echo seule_R || echo autre); echo "$fi : lignes=$l versions=$vs PR73=$pr etat_de_tag_affirme=$ta section_lignes=$ls bloc_canonique_lignes=$lk section_canonique=$cn (attendu oui) premiere_ligne_differente=$dl (attendu 0)"; if [ "$l" -eq 0 ] || [ "$vs" != "seule_R" ] || [ "$pr" -eq 0 ] || [ "$ta" -ne 0 ] || [ "$ls" -eq 0 ] || [ "$lk" -eq 0 ] || [ "$cn" != oui ]; then ok=0; fi; done; [ "$ok" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `premiere release contenant le gate=` vide (aucun tag de release `vX.Y.Z` ne contient le gate) ; ou, pour l'un des deux manuels, `lignes=0` (paragraphe introuvable), `versions=autre` (une release autre que R est citée, ou R absente), `PR73=0`, `etat_de_tag_affirme=` différent de 0, `section_lignes=0` (section introuvable : pas de titre au-dessus du paragraphe), `bloc_canonique_lignes=0` (bloc canonique introuvable dans ce plan) ou `section_canonique=non` (la SECTION du manuel, du titre au titre suivant de même niveau ou supérieur, diffère du bloc canonique ; `premiere_ligne_differente=` donne la ligne : toute tournure hors liste blanche, FR ou EN, présence ou absence de tag, y compris collée à une ligne voisine du paragraphe, ajoutée dans un paragraphe séparé avant ou après, ou au milieu de la section — révision 5, F1/F2) ; ou `plan 13 non modifie dans l arbre=non` (bloc canonique retouché sans commit) ; ou `ancre_debut_execution=absente` (aucun commit propre à la branche n'ajoute un fichier sous `tools/` ni un `40.1-*-SUMMARY.md`) ; ou `commits_touchant_le_plan_13_depuis_l_ancre=` différent de 0 (plan 13, donc bloc canonique, retouché et COMMITÉ depuis le début de l'exécution, quel que soit le sujet, même si le manuel a été aligné sur la retouche)</fails_when>
  </verify>
  <!-- planner-discipline-allow: aucun tag -->
  <acceptance_criteria>
    - zéro ligne vivante sous `manual/` et `.planning/seeds/` ; puces du seuil de code FR et EN identiques à la base de branche dérivée
    - chacun des deux paragraphes cite exactement une release, R (calculée à l'exécution), et la PR `#73`, sans affirmation sur l'état des tags
    - chacune des deux sections (`## Les contraintes de taille, visibles`, `## Size limits, made visible`, jusqu'au titre suivant de même niveau ou supérieur) est octet pour octet le bloc `<canonical_manual_fr>` / `<canonical_manual_en>` de ce plan, préfixe `> ` retiré et `{R}` remplacé par R (liste blanche : aucune autre tournure admise, dans le paragraphe ni autour)
  </acceptance_criteria>
  <done>Le manuel ne surpromet ni ne sous-promet, et son énoncé reste vrai quel que soit l'ordre des releases ; le seed vise 300.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → agents et humains | CLAUDE.md et l'ADR sont lus comme vérité avant tout code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-30 | Repudiation | arbitrage cité sans canal ni date dans l'amendement | medium | mitigate | critère : la note cite `AskUserQuestion session principale` et `2026-09-16` (CLAUDE.md § Traçabilité) |
| T-40.1-31 | Spoofing | manuel promettant une garantie absente du lab utilisateur | low | mitigate | paragraphe distingue CI du dépôt de distribution et lab ; texte canonique relu, comparé par `cmp` (liste blanche, W-A) ; revue au rejeu de clôture |
</threat_model>

<verification>
Recensement nul sur les neuf fichiers ; note d'amendement unique ; puces de seuil de code intactes ;
check-machine-paths et check-state-integrity verts.
</verification>

<success_criteria>
La doctrine, le registre ADR, la documentation de planning, le manuel et le seed énoncent 300 avec
avertissement dès 251 ; ADR-029 amendée par note datée.
</success_criteria>

## Artifacts this phase produces

- `docs/ADR.md` : note `ADR-029 : amendement du 2026-09-16 (Phase 40.1)`

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-13-SUMMARY.md` when done
</output>

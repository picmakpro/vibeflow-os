---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]   # amont SEUL. L'aval `23-05` PÉRIME la ligne transitoire de §9 (A-1ter geste 2) :
                        # il ne peut pas être déclaré ici (23-05 dépend déjà de 23-03 — ce serait un cycle).
                        # La dépendance descendante est portée par la marque `transitoire, périme au plan 23-05`
                        # écrite DANS la table d'allowlist, seul endroit où l'exécutant la verra.
files_modified:
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/agents/vf-coder.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [GSDC-03]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un agent du module sait, sans deviner, quels flags il peut passer à une brique de cycle — et tout flag non nommé est fermé, y compris ceux que `gsd-core` ajoutera demain (D-08)."
    - "Le choix entre recherche et non-recherche se fait sur un critère FACTUEL constatable, jamais sur un ressenti (D-05, ADR-055 §3)."
    - "La doctrine de flags de cycle vit dans `GSD-PIPELINE.md` et renvoie à `docs-flow.md` pour la famille documentaire, sans jamais la dupliquer (D-06, ADR-057)."
    - "Le cycle canonique cesse de déclarer une brique de livraison empruntée sans dire pourquoi elle ne l'est pas (D-21)."
    - "Une autorisation qu'on ne peut pas fermer aujourd'hui est écrite TRANSITOIRE et DATÉE — motif factuel (`chain.md:45-61` + règle 5 de `checkpoints.md:11`, bornée par la règle 6 de `checkpoints.md:12`) et échéance nommée (plan 23-05, A-1ter geste 2) — jamais accordée en silence (A-1ter)."
  artifacts:
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md
    - plugin/dev-orchestrator/agents/vf-coder.md
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  key_links:
    - "La clause de fermeture par défaut est ce qui fait que l'allowlist ne périme pas — sans elle, un flag ajouté en amont arrive ouvert par omission, ce qui reproduit la Lacune 5 sur un nouveau terrain."
    - "Le renvoi croisé vers `docs-flow.md` est le seul lien qui empêche deux doctrines de flags de diverger (ADR-057, une capacité une seule voix)."
    - "`vf-coder.md` pointe vers §9 au lieu de reformuler : sans ce renvoi, la gradation vivrait à deux endroits et divergerait au premier changement."
    - "La marque `transitoire, périme au plan 23-05` portée par la ligne de cadrage est le seul lien entre cette table et son correctif structurel : sans elle, l'autorisation devient permanente par oubli — exactement le pilotage par omission que la phase corrige."
---

<objective>
Fermer la Lacune 3 (aucune doctrine de flags de cycle) — deuxième dans l'ordre imposé, après la
sûreté du plan 23-01, parce qu'on ne peut pas graduer un flag sans savoir ce qu'un mode
d'enchaînement auto-approuve.

Purpose : un seul flag GSD est aujourd'hui employé dans tout le module, et deux faits mesurés
rendent ce silence coûteux — le workflow de plan **prompte** en l'absence de flag de recherche
alors que `vf-coder` n'a pas d'outil de question (blocage ou auto-réponse silencieuse) ; et le mode
d'enchaînement autonome n'est **pas** un mode de cadrage : c'est un **déclencheur de pipeline
complet**. Posé sur la brique de cadrage, il enchaîne cadrage → plan → exécution **dans le même
appel** (`chain.md:45-61`, étape 5), et fait donc jouer la **règle 5** de `checkpoints.md:11` sur
**tout le plan et toute l'exécution qui suivent** : les checkpoints de vérification humaine
**auto-approuvent**, ceux de décision **auto-sélectionnent la première option**. Portée réelle
**bornée**, à ne pas surestimer : la **règle 6** (`checkpoints.md:12`) protège les gates
`blocking-human`, jamais auto-approuvés même en auto-mode. Décision de référence, tranchée par
l'humain le 2026-08-03 : `23-ARBITRAGES.md` **§A-1ter** (contexte et voies chiffrées :
`23-ARBITRAGES-OUVERTS.md` §O-8). Le réglage juste est **étroit**, et il n'est écrit nulle part.

Output : une section `§9 Doctrine de flags de cycle` dans `GSD-PIPELINE.md`, en **allowlist
stricte** (tout ce qui n'est pas nommé est fermé, futurs flags compris), avec la gradation de la
recherche sur critère factuel, une **autorisation transitoire datée** sur la brique de cadrage —
qui porte son motif factuel et son échéance (plan 23-05) dans la table elle-même — et le renvoi
croisé vers la doctrine documentaire ; la ligne de la brique de livraison du cycle canonique
corrigée pour dire **pourquoi** elle n'est pas empruntée ; et un bloc de test T33 à discriminance
prouvée par mutation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-01-SUMMARY.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-ARBITRAGES.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-ARBITRAGES-OUVERTS.md
@CLAUDE.md

> **Lecture non optionnelle : `23-ARBITRAGES.md` §A-1ter (tranché le 2026-08-03) conditionne ce
> plan.** C'est la décision humaine qui dit ce que le mode d'enchaînement autonome fait
> réellement, et donc ce que la §9 a le droit d'écrire. Contexte du fait, voies chiffrées et
> impasse constatée : `23-ARBITRAGES-OUVERTS.md` §O-8 (tranché, conservé comme instantané daté).
> Trois décisions d'affilée sur cette lacune ont reposé sur une prémisse fausse : **vérifier le
> comportement du moteur amont avant d'écrire, pas après**.
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md`

**Champs neufs du bloc typé** : `gate`, `reprise` (`plan_id`/`checkpoint`/`gate`/`attendu`),
`verdicts` (`code_review`/`nyquist`/`secure`), `decompte`

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » (ce plan) ·
`mission-flow.md` §Pattern E budget partagé + table des briques dormantes · `docs/ADR.md` ADR-061
troisième objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24` à `T33` (ce plan : `T33`)

**Variables d'environnement neuves** : `VF_CONFIG_PATH`, `VF_GSD_CORE_LIB`, `VF_CAPS_INDEX_OUT`,
`VF_GSD_TOOLS`

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` / `3` / `64`

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10`

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`).
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`).
- **Gate de schéma ORM : sans objet.**
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan n'écrit que de la doctrine markdown et des assertions bash — aucun SDK, aucun service
  tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

## Numérotation du bloc de test — collision constatée, TRANCHÉE le 2026-08-03

**La collision.** Ce plan prescrivait à l'origine de créer un bloc `T27`. Or le plan 23-01, exécuté
depuis, avait déjà posé **`T27`, `T27b` et `T27c`** (arbitrage A-4 / bloquants B4-B5) dans
`plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` — vérifié sur liste matérialisée :
`T27 (A-4, DISCRIMINANT)` l. 3356, `T27b (B4)` l. 3632, `T27c (B5)` l. 3743. Ces trois renvois
désignent des blocs **existants** et restent valides tels quels.

**La cause.** `23-01-PLAN.md:113-115` allouait `T24`, `T25`, `T26` au plan 23-01 et `T27` à ce
plan. En exécution, 23-01 a **débordé son allocation** et posé
`T24, T25, T25b, T25c, T26, T27, T27b, T27c` — il a mangé le `T27` de 23-03. `23-01-PLAN.md` n'est
pas réécrit : exécuté, son allocation d'origine reste l'archive de ce qui était prévu.

**La décision : ce plan prend `T33`.** Aucun autre plan ne bouge. `T28`→`T32` sont libres (0
occurrence dans le fichier de test) et légitimement revendiqués par 23-04 (`T28`), 23-05 (`T29`),
23-06 (`T30`), 23-07 (`T31`, `T32`) ; 23-08 ne crée aucun bloc. `T33` est **vérifié absent** du
fichier de test avant amendement — 0 occurrence, contrôlée deux fois, en `grep` et en `awk`.

**Pourquoi `T33`, et pas un repli plus proche** — le motif est écrit ici pour survivre à la session :

1. **`T27d` est exclu par la convention réelle du fichier.** Un suffixe lettre y désigne toujours le
   **satellite du contrat de sa base** : discriminant par mutation, scénario supplémentaire sur la
   même cible, ou sous-assertion d'une boucle. Le sujet de ce plan est la §9 « Doctrine de flags de
   cycle » — sans rapport avec le départage gel/question d'A-4 que porte `T27`. Poser `T27d`
   reproduirait l'anomalie `T18b` (l. 1611), seule tache de cette convention.
2. **Le décalage en cascade est exclu par son coût** : ~45 lignes réparties sur 5 plans, dont 5
   renvois croisés (23-07 dépend de `T29` posé par 23-05 et de `T30` posé par 23-06), contre ~10
   lignes sur un seul plan.
3. **« `T33` casse l'ordre » ne tient pas** : le fichier porte déjà trois inversions — `T2g`/`T2h`
   (l. 581/648) après `T2i`…`T2l` (l. 417-504), et `T18b` (l. 1611) après `T18c` (l. 1550).
   L'ordre numérique **n'est pas un invariant** de cette suite.

**Effet de bord réparé.** Sous `T27`, deux critères d'acceptation de ce plan étaient **verts avant
toute écriture** — le `T27` d'A-4 les satisfaisait déjà. Renumérotés en `T33`, absent du fichier,
ils redeviennent falsifiables. C'est exactement le mode d'échec que ce plan corrige ailleurs.

<tasks>

<task type="tracer">
  <name>Tâche 1 : §9 Doctrine de flags de cycle, en allowlist stricte, câblée au worker et gatée (D-05, D-06, D-08)</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/agents/vf-coder.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <reversibility rating="costly">D-06 place la doctrine dans `GSD-PIPELINE.md`. La déplacer après coup casserait les renvois posés dans `AGENT.md`, dans les agents, dans le test d'exhaustivité et dans le chemin d'install des labs déjà déployés — coût réel, pas un simple `git revert`. Le choix est tranché et n'est pas rouvert ici.</reversibility>
  <read_first>
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md (intégralement, 141 lignes) — la structure en 8 sections numérotées, le ton, le format des tables, et l'en-tête qui déclare le chargement on-demand
    - plugin/dev-orchestrator/references/docs-flow.md (intégralement, 111 lignes) — c'est la doctrine des flags **documentaires**, livrée par la Phase 22 et **mergée** : la §9 y RENVOIE et ne la duplique JAMAIS (ADR-057). Lire notamment sa §Déclencheurs et sa §Garde-fous pour connaître exactement ce qui y est déjà dit
    - plugin/dev-orchestrator/agents/vf-coder.md §Cadrage (ligne 27) — le point exact où le flag de recherche se décide
    - $HOME/.claude/gsd-core/workflows/plan-phase.md autour de la ligne 333 — le prompt sur la recherche en l'absence de flag : c'est le FAIT qui rend l'omission coûteuse
    - .planning/phases/VFDO-23-.../23-ARBITRAGES.md **§A-1ter** (tranché le 2026-08-03) et .planning/phases/VFDO-23-.../23-ARBITRAGES-OUVERTS.md **§O-8** — **à lire AVANT d'écrire une ligne de §9.** C'est la décision humaine qui conditionne ce plan : elle dit ce que le mode d'enchaînement autonome fait réellement, quelle est sa portée **bornée**, pourquoi la fermeture immédiate est une impasse (voie 2 : le mode assumptions appelle `AskUserQuestion`, que `vf-coder` n'a pas), et où vit le correctif structurel (**plan 23-05**, voie 1)
    - $HOME/.claude/gsd-core/workflows/discuss-phase/modes/chain.md **l. 45-61 (étape 5)** — le FAIT qui commande toute la §9, à lire dans le fichier amont et pas de seconde main : sur la brique de cadrage, le mode d'enchaînement ne se contente pas de poser un état — il **lance lui-même la brique de plan via l'outil Skill, en lui repassant le même mode**, donc cadrage → plan → exécution s'enchaînent **dans le même appel**. Toute rédaction qui présente ce mode comme « un état persisté qui survit à la session » décrit la moitié tombée du fait (prémisse démentie par A-1ter) et ne doit pas être écrite
    - $HOME/.claude/gsd-core/references/checkpoints.md **l. 11-12** — attention au chemin : `gsd-core/references/`, **PAS** `gsd-core/workflows/references/`. La **règle 5** (l. 11) énonce ce que l'auto-mode auto-approuve (vérification humaine) et auto-sélectionne (première option d'une décision) ; la **règle 6** (l. 12) énonce ce qui n'est **jamais** auto-approuvé (`gate="blocking-human"`). §9 doit citer **les deux** : la règle 5 fonde le motif, la règle 6 en **borne** la portée — écrire l'une sans l'autre surestime la faille
    - .planning/phases/VFDO-23-.../23-01-SUMMARY.md, notamment **l. 267-310** — l'état exact laissé par le plan 23-01, **y compris son dernier geste** : `T25b` a été **DÉGAZÉ** le 2026-08-03. Il ne certifie plus qu'une **adjacence TEXTUELLE** et **ne borne AUCUNE fenêtre runtime**. Le geste de reset écrit en 23-01 peut être cité pour dire ce qui y a été fait ; ni `T25` ni `T25b` ne peuvent être présentés comme la mitigation d'une fenêtre runtime — ils ne le sont pas
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh — blocs T22 et T23 (patron le plus récent d'assertion de doctrine, Phase 22) et T14b/T14c (patron de discriminance par mutation)
  </read_first>
  <action>
Écrire la doctrine de flags de cycle **là où le pipeline se lit déjà**, sous une forme qui ne
périme pas, et la relier au seul worker qui passe ces flags.

**1. `GSD-PIPELINE.md` — nouvelle section `## 9. Flags de cycle — allowlist stricte`**, ajoutée à
la fin du fichier, après §8, en respectant la numérotation et le style existants (titre `##`,
séparateur `---` entre sections, tables markdown compactes).

La section porte quatre choses, et rien d'autre :

- **Le principe de fermeture (D-08), énoncé en premier et sans ambiguïté** : seuls les flags
  **nommés dans la table ci-dessous** sont utilisables par un agent du module ; **tout le reste est
  interdit par défaut**, y compris les flags que `gsd-core` ajoutera dans une version ultérieure.
  Écrire explicitement le corollaire, parce que c'est lui qui a de la valeur : un flag nouveau
  arrive **fermé**, et il s'ouvre par une décision datée, jamais par omission. C'est la seule forme
  qui ne périme pas à la montée de version — une liste d'interdits seuls reproduirait le pilotage
  par omission que cette phase vient corriger.

- **La table d'allowlist**, une ligne par brique de cycle (cadrage, plan, exécution), **quatre**
  colonnes : brique · flags autorisés · flags explicitement fermés · **motif (fait + source
  vérifiable)**. Contenu attendu, dérivé de l'arbitrage humain **A-1ter** (`23-ARBITRAGES.md`,
  tranché le 2026-08-03) et des faits amont lus dans le moteur :

  - **cadrage** — la gradation de la recherche est autorisée dans ses deux formes. Le mode
    d'enchaînement non-interactif est **AUTORISÉ À TITRE TRANSITOIRE, ET LA LIGNE LE DIT** : elle
    porte la mention **`transitoire — périme au plan 23-05`** dans sa propre colonne de motif, pas
    dans une note de bas de page ni dans un commentaire de plan. Le motif s'écrit en deux temps,
    les deux obligatoires :
    - **(a) ce que ce mode fait réellement** — sur cette brique il ne pose pas seulement un état :
      il **déclenche le pipeline entier**, cadrage → plan → exécution dans le même appel
      (`chain.md:45-61`, étape 5), et fait donc jouer la **règle 5** de `checkpoints.md:11` sur
      **tout le plan et toute l'exécution qui suivent** — vérification humaine **auto-approuvée**,
      décision **auto-sélectionnée sur la première option**. Portée **bornée**, à écrire dans la
      même cellule pour ne pas surestimer la faille : la **règle 6** (`checkpoints.md:12`) protège
      les gates `blocking-human`, jamais auto-approuvés, même en auto-mode.
    - **(b) pourquoi il reste malgré tout ouvert aujourd'hui** — `vf-coder` n'a pas
      `AskUserQuestion` : le fermer maintenant le mettrait en impasse, constatée et chiffrée en
      `23-ARBITRAGES-OUVERTS.md` §O-8 (voie 2). L'autorisation est donc **assumée par écrit**, avec
      son coût nommé, plutôt qu'accordée **en silence** — c'est la seule chose que ce plan n'a plus
      le droit de faire. Le correctif structurel est **instruit au plan 23-05**
      (§« Contrainte d'entrée (A-1ter geste 2) » : le manager porte le cadrage, il a
      `AskUserQuestion`, le mode n'a alors plus lieu d'être). **Le jour où 23-05 passe, cette ligne
      devient fermée** — l'échéance est écrite dans la table, jamais laissée à la mémoire.

  - **plan** — la gradation de la recherche est autorisée dans ses deux formes ; le mode
    d'enchaînement autonome y est **fermé en invocation directe**, motif : le même fait
    (`chain.md:45-61` + règle 5 de `checkpoints.md:11`, bornée par la règle 6 de
    `checkpoints.md:12`).

  - **exécution** — le mode d'enchaînement autonome est **fermé**, même motif ; exécuter hors
    frontière du DAG contredirait de surcroît le pipelining modélisé en `mission-flow.md`.

  - **Ce que la table ne doit PAS dire — deux prémisses mortes, à ne pas ressusciter :**
    1. Ne **jamais** motiver la fermeture par la *persistance* de l'état dans
       `.planning/config.json` ni par sa *survie à la session*. Cette prémisse est **démentie**
       (A-1ter) : les effets se produisent **dans l'appel même**, avant toute question de survie.
       Un motif faux fait tomber la ligne au premier lecteur qui vérifie.
    2. Ne **jamais** présenter `T25` ou `T25b` comme la mitigation de ce risque. `T25b` a été
       **dégazé** le 2026-08-03 (`23-01-SUMMARY.md` l. 267-310) : il ne certifie plus qu'une
       **adjacence textuelle** et ne borne **aucune** fenêtre runtime. Renvoyer à 23-01 reste
       légitime pour dire *ce qui y a été fait* — jamais pour laisser croire qu'un gate machine
       borne la fenêtre. **Aucun gate ne ment en attendant le correctif.**

- **La gradation de la recherche (D-05), sur critère FACTUEL** — deux entrées, chacune un fait
  constatable et jamais un ressenti (ADR-055 §3) : demander la recherche quand l'étape touche une
  **lib, un framework, du natif ou une version**, ou un domaine non cartographié ; la sauter quand
  l'étape prolonge un périmètre **déjà couvert** par un `RESEARCH.md` ou un `CONTEXT.md` récent du
  même dossier de phase. Ajouter la précision qui évite la confusion la plus probable, en une
  ligne : le toggle `workflow.research` du `config.json` active la **capability** de recherche sur
  le point de hook de pré-plan (le moteur spawne lui-même son chercheur) ; le flag, lui, répond au
  **prompt** — le toggle étant à vrai sur ce lab, la recherche a lieu de toute façon, le flag décide
  seulement si le worker se fait interroger. Ces deux couches ne sont pas interchangeables.

- **Le renvoi croisé (D-06)** : une ligne, pas un paragraphe — la famille de flags **documentaires**
  a sa doctrine dans `docs-flow.md` (Phase 22), qui fait autorité et n'est **ni modifiée ni
  dupliquée** ici. Nommer le fichier et la raison (une capacité, une seule voix — ADR-057).

**2. `vf-coder.md` §Cadrage (point 1 du cycle)** — ajouter **un renvoi**, pas une reformulation :
le choix entre les deux formes de gradation de la recherche se lit dans `GSD-PIPELINE.md` §9, sur
critère factuel. Une phrase maximum ; ce fichier est un routeur de 74 lignes, la doctrine ne
descend pas dedans.

**3. `test-dev-orchestrator.sh` — bloc `T33`**, ajouté après le dernier bloc existant.

> **Règle de construction qui prime sur chaque assertion ci-dessous — le mode d'échec dominant de
> cette phase est là.** Une assertion qui cherche un **token quelque part dans un fichier** est
> verte et ne garantit rien : le token peut vivre dans une phrase qui dit le contraire, dans une
> section sans rapport, ou y être **déjà** avant qu'une ligne soit écrite. Toute assertion de ce
> bloc mesure donc une **relation**, jamais une présence : elle est **bornée à la section
> concernée** (bloc §9 matérialisé par `awk '/^## 9\./{f=1;next} /^## /{f=0} f'`, ou bloc §1 de la
> même façon) **et** ancrée sur l'**affirmation attendue** — l'entrée de table, la co-présence sur
> une même ligne physique, l'ordre relatif de deux lignes. Avant d'écrire une assertion, poser la
> question : *« qu'est-ce qui la rendrait verte à tort ? »* — si la réponse est « le token est déjà
> là » ou « une phrase qui l'interdit », l'ancrage est faux.

- **assertion A** : la §9 **existe en tant que section** — le bloc extrait de `GSD-PIPELINE.md`
  entre `^## 9\.` et la section suivante est **non vide** et son titre porte la notion de flags. La
  mesure porte sur le bloc matérialisé, jamais sur une occurrence du mot dans le fichier.
- **assertion B** (la plus structurante) : la **clause de fermeture par défaut** ne se contente pas
  d'exister — elle **GOUVERNE la table**. Mesure de **relation** : la clause est (i) **dans** le
  bloc §9, et (ii) **avant la première ligne de table du bloc** (`^\| `). Implémentation : calculer
  le numéro de ligne de la clause **dans le bloc matérialisé** et celui de la première ligne de
  table du même bloc, exiger `0 < ligne_clause < ligne_première_table`. Une clause écrite après la
  table, ou dans une autre section, ne gouverne rien — l'assertion doit rougir.
- **assertion C** : les deux formes de gradation de la recherche sont mesurées **dans une entrée de
  table**, pas dans la prose. Extraire de la §9 la **ligne de table** dont la première cellule
  nomme la brique concernée, en isoler la **cellule « flags autorisés »** (`awk -F'|'` sur cette
  seule ligne), et n'y chercher les deux formes que **là**. Motif : c'est le piège de l'infinitif —
  en français l'infinitif est la forme de l'**interdiction**, et une phrase de prose du type
  « ne jamais passer … » **contient** le nom du flag tout en prescrivant le contraire de ce que
  l'assertion prétend prouver. Une cellule « flags autorisés » n'a pas cette ambiguïté.
- **assertion D** : le renvoi croisé et sa non-duplication, tous deux mesurés en relation.
  - volet positif : **une même ligne physique** de la §9 porte le nom du fichier de doctrine
    documentaire **et** le motif ADR — chaînage de `$GREP` sur le même flux (idiome déjà employé
    par `T22`), jamais deux `grep` indépendants sur le fichier entier ;
  - volet **négatif, par LISTE et jamais par chaîne codée en dur** : extraire de `docs-flow.md` la
    liste de **tous** ses tokens de la forme `--mot` (`"$GREP" -oE -- '--[a-z][a-z0-9-]*'`),
    `sort -u`, **matérialiser** ; faire de même sur `GSD-PIPELINE.md` ; l'**intersection**
    (`comm -12` sur les deux listes triées) doit être **vide**, comptée par
    `awk 'END{print NR}'` sur le fichier d'intersection matérialisé. Motif : une assertion qui
    mesure **un** token prétend prouver la non-duplication de **toute** une famille documentaire —
    elle reste verte le jour où un autre flag de cette famille est recopié. La liste, elle, suit la
    famille quand `docs-flow.md` évolue. Garde de non-vacuité obligatoire : la liste extraite de
    `docs-flow.md` doit compter **≥ 3** entrées, sinon l'extraction est cassée et l'intersection
    vide ne prouve rien (**vert à vide**).
- **assertion E** : `vf-coder.md` renvoie vers `GSD-PIPELINE.md` **depuis le point 1 (Cadrage) du
  cycle**, et sur la question de la recherche — mesure bornée au **bloc du point 1** (extraction
  du bloc, pas du fichier), et co-présence du nom du fichier de doctrine avec la notion de
  recherche **dans ce bloc**. Le nom du fichier ailleurs dans l'agent ne prouve rien.
- **assertion F** (**DISCRIMINANTE, par mutation**) : le mutant retire **la clause de fermeture
  seule, en laissant la table INTACTE** — `mktemp -d` + `vf_tmp_track`, suppression des seules
  lignes de la clause. Trois gardes, sans lesquelles la mutation ne prouve rien :
  1. `cmp -s` original/mutant : s'ils sont **identiques**, la mutation n'a rien mordu → KO explicite
     disant que **la sonde est à réancrer**, et non que la doctrine est en défaut ;
  2. le mutant conserve **le même nombre de lignes de table** (`^\| `) que l'original — sinon on a
     muté la table, et l'assertion B rougirait pour la mauvaise raison ;
  3. sur le mutant, la fonction de détection de B doit **rougir** ; sur le fichier **réel**, elle
     doit rester **verte** (contrôle négatif : sans lui, une fonction cassée « détecte » partout).
- **assertion I** (marque transitoire — elle gate B1) : la ligne de table de la brique de cadrage
  porte, **dans sa propre cellule de motif**, la marque de transitoire **et** l'identifiant du plan
  qui la périme. Mesure : extraire cette **ligne de table**, et exiger que **cette ligne** porte
  les deux — la marque et l'échéance nommée. Motif : une autorisation dont l'échéance vit dans une
  note de bas de page devient permanente au premier lecteur pressé, et l'objet même d'A-1ter est
  qu'elle ne soit **plus jamais accordée en silence**.

**Preuve dans les deux sens — obligatoire, pour CHAQUE regex introduite par ce bloc.** Toute
expression écrite ici est fixturée **deux fois** : (i) une fixture **licite** — la formulation
affirmative attendue — que la regex doit **accepter** ; (ii) une fixture de **prohibition
construite avec le même vocabulaire** (même noms de flags, même termes, tournure d'interdiction)
que la regex doit **rejeter**. Précédent coûteux de cette phase, à ne pas rejouer : un correctif
avait élargi une regex à l'infinitif — or l'infinitif est en français la forme des **interdictions**
— si bien qu'une **prohibition** satisfaisait une assertion qui exigeait une **affirmation**. Une
regex fixturée dans un seul sens n'est pas prouvée.

Portabilité : `$GREP`, pas de `grep -P`, pas de `sed -i` nu (ADR-054).
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    > **Aucun critère de ce bloc ne se mesure par un compte de token sur un fichier entier.** Chacun
    > s'évalue sur une **section matérialisée** — `awk '/^## 9\./{f=1;next} /^## /{f=0} f' plugin/dev-orchestrator/references/GSD-PIPELINE.md > "$TMP/s9.txt"` — et compte par `awk 'END{print NR}'`
    > sur un **fichier matérialisé**, jamais par `grep -c` sur un pipe (le `grep` de ce poste est
    > proxifié et **tronque silencieusement** : un compteur sur pipe est une preuve fausse).
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T33`.
    - `"$TMP/s9.txt"` est **non vide** (`awk 'END{print NR}'` ≥ 20) et sa **première ligne** est un titre commençant par `## 9.` comportant le mot `flags`.
    - Renvoi croisé mesuré **en co-présence sur une même ligne physique de la §9**, jamais en deux comptes indépendants : `"$GREP" -F 'docs-flow.md' "$TMP/s9.txt" | "$GREP" -F 'ADR-057' > "$TMP/renvoi.txt"` puis `awk 'END{print NR}' "$TMP/renvoi.txt"` ≥ 1. (Motif : `ADR-057` compté sur le fichier entier est satisfait par une occurrence **n'importe où**, y compris dans une phrase qui dit le contraire, y compris hors §9.)
    - Clause de fermeture **avant** la première ligne de table de la §9 : ligne de la clause dans `"$TMP/s9.txt"` > 0, ligne du premier `^| ` dans `"$TMP/s9.txt"` > 0, et clause **strictement antérieure**.
    - Marque transitoire (B1) : la **ligne de table** de la brique de cadrage porte, **sur elle-même**, la mention de transitoire **et** l'identifiant `23-05`. Mesuré par extraction de cette ligne puis chaînage de `$GREP` sur ce seul flux.
    - `vf-coder.md` : le renvoi vers `GSD-PIPELINE.md` est **dans le bloc du point 1 (Cadrage)** du cycle et co-présent avec la notion de recherche dans ce bloc — extraction du bloc puis mesure sur le bloc, pas sur le fichier.
    - `wc -l < plugin/dev-orchestrator/agents/vf-coder.md` retourne une valeur ≤ 250 (ADR-029).
    - **Non-duplication documentaire prouvée par LISTE, jamais par une chaîne codée en dur** : `"$GREP" -oE -- '--[a-z][a-z0-9-]*' plugin/dev-orchestrator/references/docs-flow.md | sort -u > "$TMP/flags-docs.txt"` ; idem sur `GSD-PIPELINE.md` → `"$TMP/flags-pipe.txt"` ; `comm -12 "$TMP/flags-docs.txt" "$TMP/flags-pipe.txt" > "$TMP/inter.txt"`. Alors `awk 'END{print NR}' "$TMP/inter.txt"` retourne **0**, **et** `awk 'END{print NR}' "$TMP/flags-docs.txt"` retourne **≥ 3** (garde de non-vacuité : une extraction cassée rendrait l'intersection vide et la preuve mensongère).
    - Preuve de mutation consignée au SUMMARY : la copie temporaire de `GSD-PIPELINE.md` privée de la **seule** clause de fermeture (table intacte, même nombre de lignes `^| `, `cmp -s` différent) porte la suite à au moins `1 KO`, et le fichier réel reste vert.
    - Preuve **dans les deux sens** consignée au SUMMARY pour chaque regex introduite : fixture licite **acceptée** ET fixture de prohibition de même vocabulaire **rejetée**.
    - `bash plugin/conductor/scripts/check-agents.sh --agents-dir plugin/dev-orchestrator/agents --strict` sort en 0.
  </acceptance_criteria>
  <done>Un agent du module lit une allowlist fermée par défaut, décide la recherche sur un fait constatable, et le renvoi vers la doctrine documentaire empêche qu'une seconde voix apparaisse sur la même question — le tout verrouillé par une assertion dont la discriminance est prouvée par mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : le cycle canonique cesse de déclarer la brique de livraison empruntée sans dire pourquoi (D-21)</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md §1 (lignes 14-36) — la chaîne canonique en une ligne de code et la table à 9 lignes qui la détaille ; §6 Garde-fous (lignes 101-111)
    - plugin/dev-orchestrator/references/mission-contracts.md §Isolation de branche (ADR-059) et §ADR-064 (lignes 79-131) — le protocole réel : branche dédiée, PR laissée ouverte, jamais de merge, un écrivain = un worktree
    - docs/ADR.md ADR-059 et ADR-064 — les deux décisions qui priment sur l'adoption de la brique de livraison amont
    - .planning/phases/VFDO-23-.../23-CONTEXT.md D-21 et §Deferred (la réouverture éventuelle est conditionnée, elle n'est pas fermée à jamais)
  </read_first>
  <action>
Faire dire à la doctrine ce que la pratique fait déjà, pour que la tension cesse d'être subie.

**1. `GSD-PIPELINE.md` §1** — la brique de livraison reste **dans** la table du cycle canonique
(c'est un outil réel du moteur, D-21 la corrige, ne la supprime pas), mais sa ligne cesse de la
présenter comme empruntée sans réserve. Amender la colonne de rôle ou ajouter une note sous la
table, au choix du rédacteur pourvu que ce soit à un seul endroit : chez VibeFlow, l'ouverture de
la PR reste un **geste VibeFlow, tenu à la main par le manager de mission**, parce qu'ADR-059 (une
mission = une branche) et ADR-064 (un écrivain = un worktree, claim de driver) priment et que la
brique amont ne connaît ni l'une ni l'autre. Renvoyer à `mission-contracts.md` §Isolation de
branche pour le protocole — ne pas le recopier.

**2. `GSD-PIPELINE.md` §6 Garde-fous** — ajouter une puce courte et symétrique des quatre
existantes : l'ouverture de PR est un geste VibeFlow (ADR-059 / ADR-064), jamais délégué à la
brique de livraison amont tant que ces deux ADR tiennent. Une puce, un renvoi, pas un paragraphe.

**3. `test-dev-orchestrator.sh`** — étendre le bloc `T33` (ne pas créer de bloc nouveau pour deux
assertions) :
- assertion G : `GSD-PIPELINE.md` nomme la brique de livraison **et** cite au moins l'une des deux
  ADR qui expliquent pourquoi elle n'est pas empruntée ;
- assertion H (**DISCRIMINANTE, par mutation**) : sur une copie temporaire privée de la mention
  d'ADR, l'assertion G doit échouer — sans quoi elle ne prouverait que la présence d'un nom de
  brique qui était déjà là avant cette phase.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    > Même règle qu'en tâche 1 : **sections matérialisées** (`awk '/^## 1\./{f=1;next} /^## /{f=0} f' … > "$TMP/s1.txt"`, idem pour §6 → `"$TMP/s6.txt"`), comptage par `awk 'END{print NR}'` sur fichier matérialisé, **jamais** `grep -c` sur un pipe.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO`.
    - **Non-suppression, mesurée comme ligne de table et non comme token** : `"$TMP/s1.txt"` porte **exactement une** ligne de table (`^| `) dont une cellule nomme la brique de livraison, et la table de §1 conserve son compte de lignes (**9**). Un simple compte du nom de la brique sur le fichier entier est **vert avant toute écriture** — le token existe déjà deux fois dans le fichier d'origine (chaîne canonique et ligne de table) : il ne prouve rien de ce plan.
    - **Le motif est ATTACHÉ à la brique, pas juste présent dans le fichier** : la ligne de table de la brique de livraison **elle-même**, ou la note directement placée sous la table de §1 **et nommant cette brique**, porte au moins l'une des deux ADR. Mesure par extraction de cette ligne (ou de cette note) puis chaînage de `$GREP` sur ce seul flux — jamais deux comptes indépendants sur le fichier.
    - `ADR-059` **et** `ADR-064` apparaissent chacun dans `"$TMP/s1.txt"` ou dans `"$TMP/s6.txt"` (les deux sections que ce plan amende), pas ailleurs par accident.
    - Le renvoi de protocole est **dans la §1**, sur la ligne ou la note qui porte le motif : `"$GREP" -F 'mission-contracts.md' "$TMP/s1.txt"` rend ≥ 1 ligne (comptée après matérialisation).
    - `"$TMP/s1.txt"` et `"$TMP/s6.txt"` ne contiennent **aucune** recopie du protocole de branche : l'invocation de création de PR n'y apparaît pas (mesure négative bornée aux deux sections amendées, sur listes matérialisées).
    - Preuve de mutation de l'assertion H consignée au SUMMARY, avec ses gardes : `cmp -s` différent, table de §1 intacte (9 lignes), fichier réel resté vert.
  </acceptance_criteria>
  <done>La ligne du cycle canonique dit désormais pourquoi la brique de livraison amont n'est pas empruntée ici, la garde correspondante existe au §6, et la tension relevée par le ROADMAP a disparu parce que la doctrine dit la même chose que la pratique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine de flags → agents du module | `GSD-PIPELINE.md` est chargé on-demand par `vibeflow-dev` et `vf-dev-manager` : ce qu'il autorise devient ce que les agents passent au moteur. Un flag ouvert par omission traverse cette frontière sans que rien ne le signale. |
| `gsd-core` (versions futures) → allowlist | Un flag ajouté en amont franchit la frontière de version. La clause de fermeture est le seul mécanisme qui le fait arriver **fermé** plutôt qu'ouvert. |
| `docs-flow.md` (Phase 22) → `GSD-PIPELINE.md` | Deux fichiers de doctrine sur deux familles de flags : la frontière est le renvoi croisé. Toute duplication la fait disparaître et ouvre la divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03-01 | Elevation of Privilege | flag futur de `gsd-core` employé sans arbitrage | high | mitigate | Clause de fermeture par défaut écrite en tête de §9 et **gatée** par l'assertion T33 B, dont la discriminance est prouvée par mutation. Une table sans clause de fermeture serait une liste, pas une allowlist. |
| T-23-03-02 | Tampering | mode d'enchaînement autonome employé sur une brique de cycle (y compris le cadrage, qui déclenche plan et exécution) | high | **accept** | **Accepté, borné, daté — et non mitigé : le dire autrement serait un faux vert.** Le fait (A-1ter, `chain.md:45-61`) : posé sur le cadrage, ce mode enchaîne cadrage → plan → exécution **dans le même appel**, donc la **règle 5** de `checkpoints.md:11` joue sur tout le plan et toute l'exécution (vérification humaine auto-approuvée, décision auto-sélectionnée sur la première option). **Portée bornée** par la **règle 6** (`checkpoints.md:12`) : les gates `blocking-human` ne sont **jamais** auto-approuvés — ce n'est donc pas une violation d'ADR-031 sur le gate construit en 23-01. Ce que ce plan fait : §9 ferme l'emploi en invocation directe sur plan et exécution, et **assume par écrit** une autorisation **transitoire datée** sur le cadrage, motif et échéance dans la table — parce que `vf-coder` n'a pas `AskUserQuestion` et que la fermeture immédiate est une impasse (§O-8, voie 2). Ce que ce plan **ne** fait **pas** : borner la fenêtre runtime. **Aucun gate machine ne la borne** — `T25b` a été **dégazé** le 2026-08-03 (`23-01-SUMMARY.md` l. 267-310) et ne certifie qu'une **adjacence textuelle**. Correctif structurel **instruit au plan 23-05** (§« Contrainte d'entrée (A-1ter geste 2) ») : le manager porte le cadrage, le mode disparaît à la racine, et `T25b` devient sans objet — à retirer ou à remplacer par une garantie runtime, jamais à conserver vert. |
| T-23-03-03 | Repudiation | deux doctrines de flags divergentes (cycle vs documentaire) | medium | mitigate | Renvoi croisé obligatoire vers `docs-flow.md` + assertion négative T33 D : `GSD-PIPELINE.md` ne redéfinit aucun flag documentaire. Critère machine, **par liste et non par token** : la liste des flags extraite de `docs-flow.md` et celle extraite de `GSD-PIPELINE.md` sont matérialisées, triées, et leur **intersection `comm -12` est vide** — avec garde de non-vacuité sur la liste source (≥ 3 entrées). Mesurer **un** nom de flag laisserait passer la recopie de tous les autres. |
| T-23-03-04 | Spoofing | confusion entre le toggle de capability et le flag de ligne de commande | medium | mitigate | §9 porte la ligne de distinction explicite (le toggle active la capability sur le point de hook, le flag répond au prompt) — c'est la confusion la plus probable à la lecture, elle est nommée plutôt que laissée au lecteur. |
| T-23-03-05 | Denial of Service | worker bloqué sur un prompt qu'il ne peut pas satisfaire | medium | mitigate | La gradation est écrite pour que le flag ne soit **jamais** omis : le workflow de plan prompte en son absence et `vf-coder` n'a pas d'outil de question. §9 rend le choix déterministe sur un fait. |
| T-23-03-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan. |
</threat_model>

<verification>
- Suite du module verte : `0 KO`, `T33` nommé dans la sortie.
- Densité ADR-029 : `wc -l` sur `vf-coder.md` ≤ 250 ; `GSD-PIPELINE.md` reste une référence chargée on-demand (aucun plafond dur, mais la §9 vise **≤ 60 lignes ajoutées** — renvoi plutôt que reformulation). Le motif de l'autorisation transitoire (fait amont + portée bornée + échéance) n'est **pas** la variable d'ajustement de ce budget : c'est lui qui empêche l'autorisation d'être accordée en silence. Comprimer ailleurs.
- ADR-044 : `check-agents.sh --strict` vert sur le dossier d'agents du module.
- Non-duplication ADR-057 prouvée par une assertion **négative construite sur une liste extraite** de `docs-flow.md` (intersection `comm -12` vide + garde de non-vacuité), jamais sur une chaîne codée en dur ni par relecture.
- Aucune assertion du bloc ne se satisfait d'un token présent **quelque part** dans un fichier : chacune est bornée à sa **section matérialisée** et ancrée sur l'**affirmation attendue** (entrée de table, co-présence sur une ligne physique, ordre relatif).
- Discriminance des deux assertions structurantes (clause de fermeture, motif ADR de la brique de livraison) prouvée par **mutation** — le mutant retire la clause **en laissant la table intacte**, gardes `cmp -s` et compte de lignes de table incluses ; commandes et compteurs consignés au SUMMARY.
- Chaque regex introduite est fixturée **dans les deux sens** : affirmation licite acceptée, prohibition de même vocabulaire rejetée.
</verification>

<success_criteria>
- SC1 — La doctrine de flags de cycle existe, dans `GSD-PIPELINE.md`, sous forme d'allowlist dont la clause de fermeture par défaut est explicite et gatée.
- SC2 — La gradation de la recherche repose sur deux faits constatables, et la distinction toggle/flag est écrite.
- SC3 — Le renvoi croisé vers la doctrine documentaire existe et aucune de ses règles n'est dupliquée (prouvé par assertion négative).
- SC4 — Le cycle canonique dit pourquoi la brique de livraison amont n'est pas empruntée, en citant les deux ADR qui priment.
- SC5 — `vf-coder` pointe vers la doctrine au lieu de la reformuler, et reste sous le plafond de densité.
- SC6 — L'autorisation qui ne peut pas être fermée aujourd'hui est écrite **transitoire et datée** : la ligne de cadrage porte, dans sa propre cellule de motif, le fait amont (`chain.md:45-61` + règle 5 de `checkpoints.md:11`), sa **portée bornée** (règle 6, `checkpoints.md:12`) et l'échéance nommée (**plan 23-05**) — et l'assertion I la gate. Aucune autorisation accordée en silence, aucun gate retiré présenté comme une mitigation (A-1ter).
- SC7 — Aucune assertion du bloc n'est satisfaite par un token présent quelque part : chacune est bornée à sa section matérialisée, ancrée sur l'affirmation attendue, et chaque regex est fixturée dans les deux sens.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-03-SUMMARY.md` à la fin.
Y consigner : le nombre de lignes ajoutées à `GSD-PIPELINE.md`, le contenu exact de la table
d'allowlist retenue, et les deux preuves de mutation avec leurs compteurs avant/après.
</output>

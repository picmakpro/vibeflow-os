---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/check-agents-manifest.json
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
autonomous: true
requirements: [FABR-01, FABR-05]

must_haves:
  truths:
    - "Le gate lit ses six listes de référence (outils, champs de frontmatter, types natifs, modèles, modes de permission, niveaux d'effort) dans plugin/conductor/scripts/check-agents-manifest.json ; le script ne contient plus aucune de ces listes en dur ni aucune énumération littérale de leurs valeurs (D-01)"
    - "Chaque liste du manifeste porte verifie_le (date ISO) et source (URL https de la page officielle lue) ; valide_jours vaut 30 et vit dans le manifeste, le script n'en porte aucune valeur par défaut (D-01, D-02)"
    - "Dès qu'au moins un agent est à juger, un manifeste absent, au JSON illisible ou au schéma invalide fait sortir le gate en rc=1 avec le jeton MANIFESTE-ILLISIBLE — jamais une liste vide, jamais un vert (D-03)"
    - "Sous --hook, le même défaut imprime MANIFESTE-ILLISIBLE et sort 0 (silence de code, jamais silence de message) ; la garde d'écriture le traite comme une panne du contrôleur (fail-open déjà documenté du garde), jamais comme un refus aveugle de toute écriture d'agent"
    - "Sur cible vide (dossier PRÉSENT, zéro agent), le contrat F13 est inchangé (exit 3 en --strict, 0 sinon) et le manifeste n'est pas requis : il n'est chargé que s'il y a au moins un agent à juger"
    - "Sur cible ABSENTE (dossier qui n'existe pas sur disque), hors --hook, le gate sort désormais exit 3 INDÉTERMINÉ dans TOUS les modes — y compris sans --strict — au lieu du faux vert exit 0 « rien a verifier » (D-20, CONCERNS.md:349) ; sous --hook l'exemption existante (T56) est inchangée ; sur cible PRÉSENTE mais vide, le régime de la ligne précédente reste seul en vigueur (T21/T22/T23 inchangés)"
    - "Le manifeste porte exactement les 46 identifiants d'outils et les 18 champs natifs de 42-RESEARCH § Code Examples — ListAgents, SendFeedback, SubagentHandback, omitClaudeMd et experimental inclus — et aucun nom absent des deux pages officielles citées (D-17)"
    - "Un lab fraîchement installé (fermeture de conductor) reçoit .claude/scripts/check-agents-manifest.json, non exécutable et consigné au manifeste d'installation, et son gate rend un verdict ; manifeste retiré, le gate du lab refuse (D-16)"
    - "test-check-agents.sh juge la logique du gate contre une copie de manifeste datée du jour : aucun cas ne dépend de la date du manifeste versionné"
    - "T76 n'est pas touché par ce plan (D-13, sans objet) — CORRECTIF DE REVUE (W6, mission `revise-42b`) : D-13 n'interdit que T76, jamais team-kernel.md dans son ensemble ; ce fichier EST légitimement touché plus tard, par 42-06 (ligne Mobile de la table Implémentations, motif D-07 sans rapport avec D-13), sans contradiction avec cette phrase"
  artifacts:
    - path: "plugin/conductor/scripts/check-agents-manifest.json"
      provides: "manifeste daté : valide_jours, rafraichissement, six listes avec verifie_le/source/valeurs"
      contains: "valide_jours"
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "chargement fail-closed du manifeste, listes construites depuis le manifeste"
      contains: "MANIFESTE-ILLISIBLE"
    - path: "plugin/_internal/vibeflow-update.sh"
      provides: "copy_module_scripts() pose les fichiers de données *.json des modules"
      contains: "Site #3bis"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "helpers mk_manifest / mk_gate_dir, harnais à manifeste du jour, cas T77 à T82"
      contains: "mk_gate_dir"
    - path: "plugin/_internal/tests/test-vibeflow-update.sh"
      provides: "T54 — un .json de <module>/scripts/ est posé, non exécutable, consigné ; le résidu .json.bak ne l'est pas"
      contains: "T54"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "T103 — invocation nue, cible absente, ni --strict ni --hook → exit 3 INDÉTERMINÉ (D-20)"
      contains: "T103"
  key_links:
    - from: "plugin/conductor/scripts/check-agents.sh"
      to: "plugin/conductor/scripts/check-agents-manifest.json"
      via: "VF_MANIFEST, chemin dérivé du dossier du script (jamais de l'environnement appelant ni du cwd)"
      pattern: "check-agents-manifest.json"
    - from: "plugin/_internal/vibeflow-update.sh"
      to: ".claude/scripts/check-agents-manifest.json (lab installé)"
      via: "boucle *.json de copy_module_scripts(), sans mode exécutable"
      pattern: 'scripts/"\*\.json'
    - from: "plugin/conductor/scripts/tests/test-check-agents.sh"
      to: "plugin/conductor/scripts/check-agents.sh"
      via: "copie du gate dans un dossier jetable ($WORK/gate) à côté d'un manifeste daté du jour"
      pattern: "mk_gate_dir"
  prohibitions:
    - statement: "MUST NOT dater une liste du manifeste (verifie_le) d'un jour où sa source officielle n'a pas été relue, ni y inscrire un identifiant absent de la source citée"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la véracité d'une date déclarée n'est pas vérifiable par machine (42-RESEARCH § Security Domain) — relecture humaine en PR"
---

<objective>
Poser le manifeste daté et le faire lire par le gate, de bout en bout jusque dans un lab installé.
Les six listes de référence de `check-agents.sh` sortent du script vers
`plugin/conductor/scripts/check-agents-manifest.json` (D-01) ; chaque liste porte sa date de
vérification et sa source ; la validité (`valide_jours: 30`) appartient au manifeste (D-02) ; un
manifeste absent ou illisible est un refus explicite (D-03). L'installeur copie le manifeste dans la
même vague (D-16). Le contenu s'aligne sur la doc officielle seule : trois identifiants d'outils
ajoutés, pas six (D-17). T76 n'est pas visé (D-13, sans objet).

Purpose: FABR-01 — fermer la cause commune du §1.3 de la spec (listes en dur derrière des
commentaires datés que rien ne fait expirer). La fraîcheur (42-04) et les invariants (42-05, 42-06)
s'appuient sur ce chargement.
Output: le manifeste versionné, le gate qui le lit, la boucle `*.json` de l'installeur, T54, et la
suite du gate rebranchée sur un manifeste du jour avec T77 à T82.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-PATTERNS.md

Fichiers à lire au début de la tâche 1, une seule fois chacun :
- `plugin/conductor/scripts/check-agents.sh` (708 lignes : en-tête l.1-77, arguments l.90-128,
  bloc Python l.153-706 — listes en dur l.169-206, messages model/effort l.521-543, découverte
  l.644-678, rapport l.680-705)
- `plugin/_internal/vibeflow-update.sh`, fonction `copy_module_scripts()` (vers l.1965-2028)
- `plugin/_internal/tests/test-vibeflow-update.sh` T9 (vers l.390-420)
- `plugin/conductor/scripts/check-blueprints.sh` (PR #85 : patron `SCRIPT_DIR`, délégation au gate)
</context>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tracer — le manifeste daté est lu par le gate, posé par l'installeur, et juge un lab frais</name>
  <precondition>PR #85 fusionnée et contenue dans HEAD : `git merge-base --is-ancestor "$(gh pr view 85 --json mergeCommit -q .mergeCommit.oid)" HEAD` rend 0 ET `plugin/conductor/scripts/check-blueprints.sh` existe (D-14) — sinon arrêt, rien n'est exécuté</precondition>
  <reversibility rating="costly">D-01 : le format JSON du manifeste est lu par check-agents.sh, par check-blueprints.sh et guard-agent-write.sh via le gate, par l'installeur et par la CI — le changer ensuite touche tous ces lecteurs.</reversibility>
  <files>plugin/conductor/scripts/check-agents-manifest.json, plugin/conductor/scripts/check-agents.sh, plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié — en entier, une fois)
    - plugin/_internal/vibeflow-update.sh (fonction copy_module_scripts(), boucle *.txt « Site #3 »)
    - plugin/_internal/tests/test-vibeflow-update.sh (T9 : patron exact de T54)
    - plugin/_internal/tests/test-manifest.sh (T6b vers l.280-292 : preuve « ligne exacte au manifeste d'installation »)
    - plugin/conductor/scripts/check-blueprints.sh (patron SCRIPT_DIR)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-01, D-02, D-03, D-13, D-16, D-17)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (§ Code Examples « Schéma JSON proposé », Pattern 1, Pitfalls 1, 2, 4, Assumptions A3)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-PATTERNS.md (§ check-agents-manifest.json, § copy_module_scripts)
  </read_first>
  <action>
1. Manifeste (D-01, D-02, D-17). Créer `plugin/conductor/scripts/check-agents-manifest.json` — nom
retenu (Claude's Discretion) préfixé par le gate pour ne jamais être confondu avec le manifeste
d'INSTALLATION `scripts/.vibeflow-manifest-<mod>` de l'installeur (Pitfall 4). Clés de premier
niveau : `valide_jours` (entier 30, D-02), `rafraichissement` (une phrase : relire chaque `source`,
comparer, mettre à jour `valeurs`, re-dater `verifie_le` au jour de la relecture — jamais un
changement de date seul) et `listes`, qui porte exactement six clés : `outils`,
`champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort`. Chaque liste
porte `verifie_le`, `source`, `valeurs`. Valeurs, sources et ORDRE : exactement le schéma de
42-RESEARCH.md § Code Examples « Schéma JSON proposé du manifeste (D-01) » — 46 identifiants
d'outils, 18 champs, 6 types natifs, 5 modèles, 7 modes, 5 niveaux d'effort ; l'ordre de `modeles`
et de `niveaux_effort` est celui du schéma (les messages du gate l'énumèrent dans cet ordre, T73
l'asserte). D-17 : n'ajouter aucun des trois anciens noms écartés par la recherche (un renommé, un
alias, un absent de toute doc officielle) ; ne pas ajouter le type natif « catch-all » non recoupé
(A3). `verifie_le` = "2026-09-23", jour où la recherche a relu les deux pages officielles. Si
l'exécution a lieu après le 2026-10-13 (moins de dix jours avant l'échéance), relire les deux URLs
par WebFetch, réconcilier les valeurs et dater du jour de cette relecture — jamais la date
d'exécution sans relecture (prohibition P1). JSON strict, indentation 2 espaces.

2. Côté bash de `check-agents.sh`. Calculer le dossier du script (même patron que
check-blueprints.sh : `cd` sur `dirname "${BASH_SOURCE[0]}"` puis `pwd`) et passer au bloc Python
`VF_MANIFEST="$SCRIPT_DIR/check-agents-manifest.json"`, TOUJOURS affecté par le script — jamais lu
depuis l'environnement de l'appelant, aucune option de substitution du manifeste.

3. Côté Python (rappel : le bloc est une chaîne bash entre guillemets doubles — chaque guillemet
double s'écrit `\"`, jamais de dollar ni d'accent grave nu, préférer `re.fullmatch` à une ancre de
fin de ligne) :
   - importer `json` et `date` (module datetime) ;
   - SUPPRIMER les assignations en dur `KNOWN`, `MODELS`, `EFFORT`, `PERM`, `NATIVE_TYPES`,
     `TOOL_NAMES` et leurs commentaires datés (2026-07-05, 2026-07-27). Garder en dur `MEMORY`
     (hors des six listes de D-01 — signalé en hypothèse), `NOT_AGENTS`, `AGENT_TOOL_NAMES`.
     Introduire `VIBEFLOW_FIELDS` = les quatre conventions du dépôt `vf-internal`,
     `vf-mcp-consumer`, `vf-mcp-tools`, `vf-requires` (pas des identifiants Anthropic) ;
   - écrire `charger_manifeste(chemin)` : lecture UTF-8, `json.load`, puis validation du schéma —
     objet ; `valide_jours` entier strictement positif (un booléen n'est pas accepté comme entier),
     AUCUNE valeur par défaut dans le script (D-02) ; clés de premier niveau limitées à
     `valide_jours`, `rafraichissement`, `listes` ; `listes` porte exactement les six clés ; pour
     chaque liste : `verifie_le` accepté par `date.fromisoformat`, `source` chaîne commençant par
     `https://`, `valeurs` liste NON VIDE de chaînes au charset `[A-Za-z0-9_-]+`. Toute violation
     lève une erreur qui nomme la clé et le motif ;
   - chargement PARESSEUX : appeler `charger_manifeste` seulement quand il existe au moins une cible
     à juger (le fichier de `--file` existe, ou la découverte est non vide). La branche « cible
     vide » (contrat F13 : exit 3 en --strict, 0 sinon) reste inchangée et ne lit pas le manifeste ;
   - sur échec (D-03) : imprimer UNE ligne `[check-agents] ✗ MANIFESTE-ILLISIBLE (<chemin>) — <cause>
     — aucun verdict rendu (D-03)`, qui ne contient JAMAIS la séquence espace-deux-points-espace
     (remplacer par un tiret toute occurrence venue du message d'exception) : `guard-agent-write.sh`
     n'extrait comme diagnostic de lint que les lignes « ✗ … : … », cette forme lui fait traiter
     l'incident comme une panne du contrôleur (son fail-open documenté), jamais comme un refus
     aveugle de toute écriture d'agent dans un lab à l'install cassée. Puis `sys.exit(0)` sous
     `--hook`, `sys.exit(1)` sinon ;
   - depuis le manifeste validé, construire `KNOWN` (champs_frontmatter ∪ `VIBEFLOW_FIELDS`),
     `TOOL_NAMES`, `NATIVE_TYPES`, `MODELS`, `PERM`, `EFFORT` comme ensembles pour l'appartenance,
     et garder les listes ordonnées pour les messages : les messages qui énumèrent des valeurs
     admises (model absent/invalide, effort absent/invalide) sont construits par jointure `|` dans
     l'ORDRE du manifeste — plus aucune énumération littérale de valeurs dans le script, en-tête
     compris ;
   - en-tête du script : remplacer les deux mentions datées et les énumérations entre accolades par
     un paragraphe « Référentiel : manifeste daté check-agents-manifest.json (même dossier que ce
     script) — six listes, verifie_le + source par liste, valide_jours porté par le manifeste ;
     absent, illisible ou invalide → MANIFESTE-ILLISIBLE, rc 1 (0 sous --hook) ; chargé seulement
     s'il y a au moins un agent à juger ; la garde d'écriture traite cet incident comme une panne du
     contrôleur ». Garder mot pour mot la citation de la doc sub-agents sur l'allowlist Agent(...)
     (l.8-12).

4. Installeur (D-16, Pitfall 2). Dans `copy_module_scripts()` de
`plugin/_internal/vibeflow-update.sh`, juste après la boucle `*.txt` (Site #3), ajouter une boucle de
même forme sur `"$module_dir/scripts/"*.json` : garde d'existence `[ -f "$f" ] &&`, `vf_place_file`
SANS troisième argument (une donnée, pas un exécutable). Commentaire « Site #3bis (Phase 42, D-16) » :
fichiers de données JSON accompagnant les scripts (le manifeste daté du gate des agents) ; sans cette
boucle le manifeste n'arriverait chez aucun utilisateur et D-03 refuserait le gate partout — même
défaut que known-versions.txt ; le glob ne ramasse ni les résidus `.json.bak` ni les fichiers cachés
(dont `.vibeflow-manifest-<mod>`). Mettre à jour la phrase du commentaire du Site #3 qui disait le
glob borné pour ne pas ramasser « les manifestes de config » en renvoyant au Site #3bis.

5. T54 dans `plugin/_internal/tests/test-vibeflow-update.sh`, calqué sur T9 et inséré juste après
lui, ÉCRIT AVANT la boucle d'installeur (le constater rouge, preuve de discriminance) : module
factice `jsonful` dans un cache jetable (VERSION, module.json, `scripts/jsonful.sh`,
`scripts/jsonful-manifest.json`, résidu `scripts/jsonful-manifest.json.bak`), install
`VF_SCOPE=project VIBEFLOW_CACHE=…`. Quatre assertions : le `.json` est posé sous
`$LAB/.claude/scripts/` ; il n'est PAS exécutable ; le `.json.bak` n'est PAS posé ; la ligne exacte
`scripts/jsonful-manifest.json` figure dans `$LAB/.claude/scripts/.vibeflow-manifest-jsonful` (même
preuve que T6b de test-manifest.sh). Libellé de succès : `T54 (DISCRIMINANT) : *.json posé non
exécutable et consigné, résidu .json.bak écarté`. Ajouter T54 au catalogue d'en-tête de la suite
s'il en tient un.

6. Commits en français, `plugin/` et `.planning/` jamais ensemble : (a)
`fix(engine): pose les fichiers de données *.json des modules (manifeste daté du gate, D-16)` pour
vibeflow-update.sh et test-vibeflow-update.sh — hors surface G-2, pas de trailer ; (b)
`feat(conductor): manifeste daté des listes de référence de check-agents (FABR-01)` pour le
manifeste et check-agents.sh, avec le trailer (D-15)
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — les listes de référence sortent vers un manifeste daté (D-01, D-03)`.
Le corps du commit (b) écrit : décisions de cadrage de Claude sur délégation explicite de Willy
(AskUserQuestion session principale, 2026-09-23), 42-CONTEXT.md — jamais « arbitrage ». Aucun bump
de module ici : le bump mineur de conductor est porté par 42-06.
  </action>
  <verify>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q); bash "$R/plugin/_internal/resolve-deps.sh" conductor | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >/dev/null 2>&1); done; M="$LAB/.claude/scripts/check-agents-manifest.json"; test -f "$M" && test ! -x "$M" && (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict >/dev/null) && mv "$M" "$M.off" && (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict | grep -q MANIFESTE-ILLISIBLE) && ! (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict >/dev/null) && echo TRACER-OK</automated>
    <fails_when>la ligne TRACER-OK est absente de la sortie (manifeste non posé, posé exécutable, gate du lab non vert avec manifeste, ou gate du lab vert / sans MANIFESTE-ILLISIBLE une fois le manifeste retiré)</fails_when>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh 2>&1 | grep -E '^== résultat|T54'</automated>
    <fails_when>aucune ligne contenant T54 n'est imprimée, ou une ligne « ✗ T54 » apparaît, ou la ligne « == résultat : N OK / M KO » porte M supérieur à 0 (suite longue, environ 2 min)</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | tail -1</automated>
    <fails_when>la dernière ligne n'est pas « == Résultat : N OK · 0 KO == » (un cas existant, T76 et T73 compris, a rougi)</fails_when>
  </verify>
  <acceptance_criteria>
    - `python3 -c 'import json;m=json.load(open("plugin/conductor/scripts/check-agents-manifest.json"));L=m["listes"];print(m["valide_jours"],len(L),len(L["outils"]["valeurs"]),len(L["champs_frontmatter"]["valeurs"]),len(L["types_natifs"]["valeurs"]),len(L["modeles"]["valeurs"]),len(L["modes_permission"]["valeurs"]),len(L["niveaux_effort"]["valeurs"]))'` imprime exactement `30 6 46 18 6 5 7 5`
    - `python3 -c 'import json;L=json.load(open("plugin/conductor/scripts/check-agents-manifest.json"))["listes"];o=L["outils"]["valeurs"];c=L["champs_frontmatter"]["valeurs"];print(all(x in o for x in ("ListAgents","SendFeedback","SubagentHandback")) and all(x in c for x in ("omitClaudeMd","experimental")) and all(v["source"].startswith("https://code.claude.com/docs/en/") for v in L.values()))'` imprime `True`
    - `! grep -qE 'ShareOnboardingGuide|bypassPermissions|xhigh|claude-code-guide|initialPrompt' plugin/conductor/scripts/check-agents.sh && echo AUCUNE-LISTE-EN-DUR` imprime `AUCUNE-LISTE-EN-DUR`
    - `! grep -qE 'subprocess|os\.system|eval\(|exec\(' plugin/conductor/scripts/check-agents.sh && echo AUCUN-APPEL-SHELL` imprime `AUCUN-APPEL-SHELL` (aucune valeur du manifeste ne peut atteindre un shell)
    - `grep -c 'check-agents-manifest.json' plugin/conductor/scripts/check-agents.sh` rend au moins 2 (chemin + en-tête)
    - `grep -c 'Site #3bis' plugin/_internal/vibeflow-update.sh` rend 1
    - la commande de tracer imprime `TRACER-OK`
    - la suite test-vibeflow-update.sh imprime une ligne `✓ T54 (DISCRIMINANT)` et `0 KO`
    - la suite test-check-agents.sh finit sur `0 KO` (82 cas préexistants verts, dont T73 et T76)
    - `git log -1 --format=%B -- plugin/conductor/scripts/check-agents.sh | grep -c '^Gate-Touche: plugin/conductor/scripts/check-agents.sh — '` rend 1
  </acceptance_criteria>
  <done>Le manifeste versionné porte les six listes datées ; le gate les lit sans aucune copie de repli et refuse (MANIFESTE-ILLISIBLE) un manifeste absent ou invalide dès qu'il a un agent à juger ; l'installeur pose le manifeste dans un lab frais, où le gate rend un verdict puis refuse une fois le manifeste retiré ; T54 vert et discriminant ; deux commits posés, le second avec son trailer Gate-Touche.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : la suite juge le gate contre un manifeste daté du jour — cas T77 à T82 (absent, illisible, schéma, source unique, contenu D-17, hook et garde)</name>
  <files>plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : catalogue l.1-67, harnais l.69-100 — CHECK, GUARD, run_check, good_agent ; T11-T14 et helpers payload_write/run_guard vers l.240-285 ; T28 ; T73 vers l.1301-1330 ; T75 vers l.1353-1390)
    - plugin/conductor/scripts/check-agents.sh (état après la tâche 1 : charger_manifeste, ligne MANIFESTE-ILLISIBLE)
    - plugin/conductor/scripts/guard-agent-write.sh (l.95-115 : extraction des lignes « ✗ … : … », fail-open)
    - plugin/conductor/scripts/check-agents-manifest.json (source des copies de test)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (§ Validation Architecture, § Security Domain)
  </read_first>
  <behavior>
    - T77 : dossier de gate sans manifeste + un agent conforme → rc=1 et sortie contenant MANIFESTE-ILLISIBLE ; même dossier, dossier d'agents vide + --strict → rc=3 (F13 inchangé, manifeste non requis)
    - T78 : manifeste tronqué (JSON invalide) + un agent conforme → rc=1, MANIFESTE-ILLISIBLE
    - T79 : schéma invalide, un sous-cas par défaut, chacun rc=1 + MANIFESTE-ILLISIBLE — sans valide_jours ; valide_jours à 0 ; valide_jours booléen ; une des six listes absente ; valeurs vides ; verifie_le non ISO ; source non https ; liste inconnue ajoutée ; valeur hors charset (espace et point-virgule)
    - T80 : source unique (D-01) — manifeste du jour privé de ListAgents + agent `tools: Read, ListAgents` en --strict → rc=1 « outil hors du set connu 'ListAgents' » ; manifeste complet → rc=0 ; manifeste privé d'omitClaudeMd (champs_frontmatter) + agent portant `omitClaudeMd: true` → avertissement « champ inconnu … omitClaudeMd » présent ; manifeste complet → absent
    - T81 : manifeste VERSIONNÉ copié tel quel (dates d'origine) à côté du gate → agent conforme sans MANIFESTE-ILLISIBLE ; ListAgents, SendFeedback, SubagentHandback acceptés en --strict ; `experimental` n'est plus un champ inconnu
    - T82 : sous --hook, manifeste absent + un agent → rc=0 ET sortie contenant MANIFESTE-ILLISIBLE ; garde d'écriture (copie dans le dossier sans manifeste) sur un agent conforme → sortie vide (laisse passer)
  </behavior>
  <action>
1. Helpers, posés juste après la création de `$WORK` :
   - `mk_manifest <destination> <age_jours> [operation]` : Python (`$PYBIN` résolu comme dans le
     gate, heredoc QUOTÉ) qui lit le manifeste VERSIONNÉ `$SCRIPTS_DIR/check-agents-manifest.json`,
     fixe le `verifie_le` de chaque liste à aujourd'hui moins `age_jours` (ISO), applique l'opération
     puis écrit la destination. Opérations : `tronque` (première moitié du texte JSON),
     `sans-valide-jours`, `valide-jours=<littéral JSON>`, `sans-liste=<clé>`, `liste-vide=<clé>`,
     `date-invalide=<clé>` (format JJ/MM/AAAA), `date-future=<clé>` (aujourd'hui + 10 j),
     `source-invalide=<clé>`, `liste-inconnue`, `valeur-hors-charset=<clé>`,
     `retire=<clé>:<valeur>`, `dates-reelles` (aucune date réécrite : copie conforme). Une opération
     inconnue fait échouer le helper bruyamment (exit 2), jamais un manifeste inchangé en silence.
   - `mk_gate_dir <dossier> <age_jours> [operation]` : crée le dossier, y copie `check-agents.sh` et
     `guard-agent-write.sh` RÉELS, puis `mk_manifest "$dossier/check-agents-manifest.json" …` — sauf
     l'opération `absent`, qui n'écrit aucun manifeste ; imprime le chemin du dossier.
2. Rebrancher le harnais : garder `REAL_CHECK="$SCRIPTS_DIR/check-agents.sh"` et
`REAL_MANIFEST="$SCRIPTS_DIR/check-agents-manifest.json"` ; `GATE_DIR="$(mk_gate_dir "$WORK/gate" 0)"`,
puis `CHECK="$GATE_DIR/check-agents.sh"` et `GUARD="$GATE_DIR/guard-agent-write.sh"`. Si
`$GATE_DIR/check-agents-manifest.json` manque, `ko` puis sortie (anti-vert-à-vide). Commentaire :
la suite juge la LOGIQUE du gate contre un manifeste daté du jour ; la fraîcheur du manifeste
versionné est jugée par la CI (42-04), jamais ici — sinon T28, T73 et consorts rougiraient à
l'échéance. T72 et T75 continuent de lire l'arbre réel via `SCRIPTS_DIR`.
3. Cas T77 à T82 selon `<behavior>`, chacun avec son dossier de gate dédié (par exemple
`mk_gate_dir "$WORK/t77" 0 absent`) et son dossier d'agents propre, agent conforme produit comme
`good_agent`. Assertions sur le rc EXACT ET le jeton exact ; jumeau vert systématique (même agent,
manifeste du jour complet → rc=0). T80 prouve D-01 : c'est le manifeste qui décide, aucune copie dans
le script. T82 garde : `payload_write` vers `$WORK/lab/.claude/agents/<nom>.md`, garde de la copie
sans manifeste, exécutée depuis `$WORK/lab`, sortie attendue vide.
4. Catalogue d'en-tête : lignes T77 à T82 sous un intertitre « Manifeste daté (Phase 42, FABR-01,
D-01/D-03/D-17) ».
5. Chaque cas est écrit puis joué ; T77 à T79 doivent passer d'emblée (tâche 1 posée) — un rouge se
corrige dans le gate, jamais dans l'assertion.
6. Non-régression des autres suites qui appellent le gate réel : `scripts/tests/test-hook-exit-parc.sh`
(ses mutants vivent hors du dossier du gate : le chargement paresseux les laisse inchangés sur cible
vide), `test-guard-agent-write.sh`, `test-check-blueprints.sh`, et les suites de module
business-pilot-bundle, content-bundle, growth-bundle, design-orchestrator, dev-orchestrator. Un rouge
qui vient du chargement du manifeste se corrige dans le gate.
7. Commit : `test(conductor): la suite juge le gate contre un manifeste daté du jour (T77-T82)` avec
le trailer
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — harnais à manifeste du jour et cas du manifeste daté (D-01, D-03)`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(77|78|79|80|81|82)|✗|== Résultat'</automated>
    <fails_when>une ligne contenant « ✗ » apparaît, ou l'une des étiquettes T77 à T82 n'a aucune ligne « ✓ », ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>for s in scripts/tests/test-hook-exit-parc.sh plugin/conductor/scripts/tests/test-guard-agent-write.sh plugin/conductor/scripts/tests/test-check-blueprints.sh plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh plugin/content-bundle/scripts/tests/test-content-bundle.sh plugin/growth-bundle/scripts/tests/test-growth-bundle.sh plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; do bash "$s" >/dev/null 2>&1 || echo "SUITE-ROUGE $s"; done; echo SUITES-FIN</automated>
    <fails_when>une ligne « SUITE-ROUGE » apparaît avant SUITES-FIN</fails_when>
  </verify>
  <acceptance_criteria>
    - la sortie de test-check-agents.sh contient au moins une ligne `✓ T77`, `✓ T78`, `✓ T79`, `✓ T80`, `✓ T81`, `✓ T82` et finit sur `0 KO ==`
    - `grep -c 'mk_gate_dir' plugin/conductor/scripts/tests/test-check-agents.sh` rend au moins 7
    - `grep -c '^GATE_DIR=' plugin/conductor/scripts/tests/test-check-agents.sh` rend 1 et `grep -c 'CHECK="\$GATE_DIR/check-agents.sh"' plugin/conductor/scripts/tests/test-check-agents.sh` rend 1
    - `grep -c 'T7[7-9] —\|T8[0-2] —' plugin/conductor/scripts/tests/test-check-agents.sh` rend au moins 6 (catalogue d'en-tête)
    - les huit suites de non-régression sortent 0 (aucune ligne SUITE-ROUGE)
    - `git log -1 --format=%B -- plugin/conductor/scripts/tests/test-check-agents.sh | grep -c '^Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — '` rend 1
  </acceptance_criteria>
  <done>La suite du gate tourne contre un manifeste daté du jour ; T77 à T82 prouvent le refus sur manifeste absent, illisible ou invalide, l'unicité de la source (le manifeste décide), le contenu D-17, le silence de code sans silence de message sous --hook, et le fail-open documenté de la garde ; aucune suite qui appelle le gate réel n'a régressé.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : l'invocation nue sur cible ABSENTE hors --hook rend INDÉTERMINÉ, jamais le faux vert historique (D-20, CONCERNS.md:349)</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : branche `if not files:` du bloc
      `else` de la boucle principale, l.649-664 — F13 actuel : `strict and not allow_empty` seul
      déclenche exit 3, sinon exit 0 « rien a verifier » dans tous les autres cas, sans distinguer
      un dossier absent d'un dossier présent et vide)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : T21/T22/T23 — `$AG` est
      un dossier PRÉSENT et vide, `mkdir -p "$AG"` au setup l.93 — hors périmètre de cette tâche ;
      T55/T56/T57/T58 vers l.1042-1088 — le harnais « chemin par défaut (D-24) » : T55 cible absente
      + --strict → déjà exit 3 ; T56 cible absente + --hook → déjà exit 0 silence total (exemption
      pinnée, INCHANGÉE par cette tâche) ; T57 cible présente au défaut, cas discriminant)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-20)
    - .planning/codebase/CONCERNS.md (entrée « Le gate ADR-044 est un faux vert dans son invocation nue » — Sévérité MEDIUM, ligne 349 : c'est ELLE que D-20 ferme)
  </read_first>
  <behavior>
    - **Inventaire préalable COMPLÉTÉ (W1, mission `revise-42b`, 2026-09-24)** — consigné au SUMMARY,
      appelant par appelant, AVANT tout changement de code. Le mot « sept appelants » de la version
      précédente de ce plan est FAUX (l'inventaire en compte davantage) et ne doit apparaître nulle
      part dans le SUMMARY :
      (1) hook SessionStart (`hooks.json` l.22, `--hook --agents-dir=…`) — hors périmètre (D-20
      exclut --hook), déjà couvert par T56, INCHANGÉ ;
      (2) `guard-agent-write.sh` — invoque le bloc Python du gate en mode `--file` sur LE FICHIER en
      cours d'écriture (toujours présent, une chaîne stdin) : ne passe jamais par la branche `if not
      files:`, INCHANGÉ (suite : `test-guard-agent-write.sh` — mêmes fixtures `--file`, INCHANGÉ) ;
      (3) `check-blueprints.sh` — matérialise un frontmatter dans un fichier temporaire existant puis
      appelle le gate en `--file` sur ce fichier : INCHANGÉ (suite : `test-check-blueprints.sh` —
      idem, cible toujours matérialisée avant l'appel, INCHANGÉ) ;
      (4) les trois étapes `check-agents` du job `gates` de `ci.yml` — bouclent sur `plugin/*/agents`
      (dossiers réels, glob non vide par construction) ou `plugin/*/AGENT.md` (`--file` sur fichiers
      réels) : la cible existe toujours, INCHANGÉ ;
      (5) l'étape « Gate C du lab frais » (`ci.yml` l.1489, `.claude/scripts/check-agents.sh
      --strict`) — le dossier `.claude/agents/` existe après l'install de conductor (au moins
      `conductor-references/`) : cible présente, INCHANGÉ, et cet appel passe déjà `--strict` donc
      F13 s'appliquait déjà indépendamment de cette tâche ;
      (6) l'installeur (`vibeflow-update.sh`) — n'invoque jamais le gate : sans objet ;
      (7) `scripts/tests/test-hook-exit-parc.sh` (vers l.184-193) — invoque le gate réel avec
      `--strict --agents-dir="$FIX"` où `$FIX` est un `mktemp -d` PRÉSENT (cible vide, pas absente) :
      régime F13 existant, INCHANGÉ — à vérifier explicitement à l'exécution puisque ce fichier
      contient aussi des mutants sur `HOOK_MODE`, hors du périmètre de la ligne d'absence de D-20 ;
      (8) les suites de module qui invoquent le gate réel sur leur propre dossier d'agents PRÉSENT
      (`test-business-pilot-bundle.sh`, `test-content-bundle.sh`, `test-growth-bundle.sh`,
      `test-design-orchestrator.sh`, `test-dev-orchestrator.sh` — dont ses cas T8c et T20, qui
      comptent des avertissements sur un `AGENT.md` réel) : cible toujours PRÉSENTE (le dossier du
      module existe dans ce dépôt), INCHANGÉ — à confirmer explicitement à l'exécution (numéros de
      ligne exacts non reconfirmés ici, faute de budget de lecture ligne à ligne de six fichiers ;
      l'exécuteur les rejoue et consigne tout écart au SUMMARY, jamais supposé sans rejeu) ;
      (9) les auto-prescriptions imprimées par le gate lui-même (`check-agents.sh` l.686 et l.691,
      « Corriger le frontmatter puis relancer : bash .claude/scripts/check-agents.sh ») — un texte
      affiché à l'utilisateur, jamais une invocation exécutée par le script : sans objet pour le
      comportement, mais à relire si la ligne d'exemple change de forme (elle reste bare aujourd'hui,
      ce qui est exactement l'invocation que ce plan corrige — cohérent, pas contradictoire) ;
      (10) `.planning/codebase/CONCERNS.md:349` (« Le gate ADR-044 est un faux vert dans son
      invocation nue », Sévérité MEDIUM) — cette entrée décrit le défaut que cette tâche ferme ; le
      SUMMARY doit la citer comme fermée, pas seulement la corriger en silence ;
      (11) aucun appelant du dépôt n'invoque aujourd'hui le gate SANS --strict ET SANS --hook ET SANS
      --file sur une cible absente — c'est exactement le trou que CONCERNS.md nomme « invocation nue »
      et pour lequel aucun test n'existait avant T103 (contrairement à T55/T56 qui couvrent déjà
      --strict et --hook séparément sur cible absente). Un lab frais dont `.claude/agents` est absent
      (aucun module porteur d'agents encore installé) ne rencontre cette branche que via l'appel
      `--hook` du SessionStart, qui reste exempté (1) — donc AUCUN lab frais ne devient rouge par
      effet de bord ;
      (12) **`plugin/validator/AGENT.md:67`** (Phase 2, « Conformité agents » : `bash
      .claude/scripts/check-agents.sh --strict`) — prescription textuelle exécutée par un agent, pas
      par le harnais de test : `.claude/agents/` existe déjà dans tout lab où cet audit tourne
      (validator suppose un lab déjà initialisé) : cible toujours PRÉSENTE, INCHANGÉ ;
      (13) **`plugin/conductor/skills/vf-new-lab/SKILL.md:257`** (GATE C, étape 2 : `bash
      .claude/scripts/check-agents.sh --strict`) — même nature de prescription textuelle, exécutée en
      fin d'init d'un nouveau lab, APRÈS que l'installeur ait posé au moins `conductor-references/`
      sous `.claude/agents/` (même situation que (5)) : cible toujours PRÉSENTE, INCHANGÉ.
    - T103 (D-20) : `cd $(mktemp -d)` (aucun `.claude/agents` sur ce chemin) puis `bash "$CHECK"`
      SANS AUCUN FLAG (ni --strict, ni --hook, ni --agents-dir, ni --file) → rc=3, sortie contenant
      `INDETERMINE` et un jeton `CIBLE-ABSENTE` distinct du jeton F13 existant (« cible absente ou
      vide ») ; jumeau vert : même invocation mais `mkdir -p .claude/agents` PRÉALABLE (dossier
      présent et vide, sans --strict) → rc=0, « rien a verifier », JAMAIS `CIBLE-ABSENTE` (régime de
      T23 inchangé) ; T55 et T56 rejoués verts sans modification (non-régression explicite du
      harnais D-24 existant)
  </behavior>
  <action>
0. **CORRECTIF DE REVUE (mission `revise-42c`, W-MUT-D20) : le mutant MUT-D20 pour cette même
   ligne (`cible_absente = ...`) est écrit et joué dans **42-04** (Tâche 1), pas ici — 42-04 pose
   les helpers de mutation QUAL-01 (`make_gate_mutant`, `okmut`, `komut`) en vague 2, qu'aucune
   version de test-check-agents.sh n'expose encore quand CETTE tâche (vague 1) s'exécute. Écrire
   MUT-D20 ici présupposerait un helper qui n'existe pas encore — cette tâche pose T103 (assertion
   directe sur rc et sortie, sans le harnais de mutation) et laisse la preuve de mutation à 42-04,
   qui rejoue le gate déjà modifié par cette tâche.
1. Test d'abord : T103, joué ROUGE contre le gate actuel (qui traite absence et vacuité
identiquement), puis implémentation.
2. Gate, bloc Python, dans la branche `else:` de la boucle principale (juste avant
`files = sorted(glob.glob(...))`, l.650) : une ligne UNIQUE et distincte
`cible_absente = (not single) and not os.path.isdir(agents_dir)` (cible du mutant MUT-D20 — la
remplacer par `False` doit désarmer T103 sans toucher T55/T56/T57, qui restent gouvernés par leurs
propres branches --strict/--hook déjà en place). **CORRECTIF DE REVUE (W2, mission `revise-42b`,
2026-09-24) : `single` n'est JAMAIS `None`.** Le bash exporte toujours `VF_SINGLE="$SINGLE_FILE"`
(l.154) et le Python le lit par `os.environ["VF_SINGLE"]` (l.164) — sans `--file`, cette variable
vaut la chaîne vide `""`, jamais absente ni `None` ; `single is None` serait donc TOUJOURS `False` et
`cible_absente` ne s'armerait JAMAIS, rendant toute cette tâche inerte. `not single` teste la chaîne
vide (falsy en Python), la forme correcte. Vérifier ce point EN PREMIER à la lecture du code réel
avant d'écrire la ligne. Dans la branche `if not files:` (l.652), AVANT le
test `if strict and not allow_empty:` existant, ajouter :
```
if cible_absente and not hook and not allow_empty:
    print(f\"[check-agents] ✗ INDETERMINE : {agents_dir} — CIBLE-ABSENTE, aucun verdict rendu (D-20)\")
    sys.exit(3)
```
(`and not allow_empty` — I2, mission `revise-42b` : `--allow-empty` tolère une cible absente au même
titre qu'une cible vide, sans test dédié obligatoire pour cette clause.)
Ne PAS toucher au bloc `if strict and not allow_empty:` existant ni au `sys.exit(0)` final de cette
branche (T21, T22, T23, T57 doivent rester inchangés au bit près — dossier PRÉSENT et vide passe
tel quel par le régime F13 déjà en place, cette tâche ne le touche pas). Sous `--hook`, l'exemption
reste entière (T56) : la nouvelle branche ne s'exécute jamais quand `hook` est vrai.
3. En-tête : ajouter au paragraphe F13 existant une phrase « D-20 (Phase 42) : hors --hook, une
cible ABSENTE (dossier introuvable) sort désormais INDÉTERMINÉ (exit 3, jeton CIBLE-ABSENTE) dans
tous les modes, y compris sans --strict — une cible PRÉSENTE mais vide garde le régime F13 déjà
documenté ci-dessus, inchangé ». **I2 (mission revise-42b, une ligne, pas de test dédié exigé) :**
ajouter à cette même phrase « --allow-empty tolère aussi une cible ABSENTE, au même titre qu'une
cible vide : `cible_absente and not hook` doit se lire `cible_absente and not hook and not
allow_empty` — sinon `--strict --allow-empty` sur une cible absente sortirait quand même en
CIBLE-ABSENTE, contredisant le sens déjà établi de `--allow-empty` ». Le code de l'étape 2 (juste
au-dessus) intègre cette clause `and not allow_empty` dans la condition `if cible_absente and not
hook:` dès l'écriture — ce n'est pas un correctif après coup.
4. Catalogue d'en-tête de test-check-agents.sh : ligne T103 (SEULE — MUT-D20 est écrit et joué
en 42-04) sous « Invocation nue sur cible absente (Phase 42, D-20, CONCERNS.md:349) ».
5. Rejouer la suite entière (0 KO), T55 à T58 explicitement (non-régression du harnais D-24), et les
huit suites de non-régression de la tâche 2 de ce plan.
6. Commit séparé : `fix(conductor): l'invocation nue sur cible absente rend INDÉTERMINÉ (D-20)` avec
les trailers (D-15)
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — CIBLE-ABSENTE hors --hook rend exit 3 dans tous les modes (D-20)`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T103 (D-20 ; mutant MUT-D20 posé en 42-04)`.
Corps : décision de cadrage de Claude sur délégation explicite de Willy (AskUserQuestion session
principale, 2026-09-23), 42-CONTEXT.md D-20 (signalé par Samuel, WhatsApp, 2026-09-23), et
l'inventaire complet des appelants du `<behavior>` ci-dessus (aucun devenu rouge par effet de bord).
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(103|55|56|57|58)|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T103, T55, T56, T57, T58 n'a aucune ligne « ✓ », ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>for s in scripts/tests/test-hook-exit-parc.sh plugin/conductor/scripts/tests/test-guard-agent-write.sh plugin/conductor/scripts/tests/test-check-blueprints.sh; do bash "$s" >/dev/null 2>&1 || echo "SUITE-ROUGE $s"; done; R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q); bash "$R/plugin/_internal/resolve-deps.sh" conductor | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >/dev/null 2>&1); done; rm -rf "$LAB/.claude/agents"; OUT=$(cd "$LAB" && bash .claude/scripts/check-agents.sh --hook 2>&1); RC=$?; [ "$RC" -eq 0 ] && [ -z "$OUT" ] && echo LAB-HOOK-SILENCIEUX-OK</automated>
    <fails_when>une ligne SUITE-ROUGE apparaît, ou la ligne LAB-HOOK-SILENCIEUX-OK est absente. **CORRECTIF DE REVUE (W4, mission `revise-42b`) : cette sonde doit cibler une cible réellement ABSENTE et vérifier la sortie, pas seulement le rc.** L'ancienne forme laissait `.claude/agents/conductor-references/` en place (cible PRÉSENTE — ne teste rien du chemin D-20) et jetait la sortie (`>/dev/null 2>&1`, un exit 0 muet aurait pu masquer un message non vide) : `rm -rf "$LAB/.claude/agents"` force l'absence, et `OUT`/`RC` sont examinés explicitement — `RC` non nul OU `OUT` non vide signale un régression du silence de code sous --hook (D-20 exempte --hook, mais le silence de flux doit rester total). **HOME jetable (mission `revise-42c`) : `export HOME=$(mktemp -d)` avant l'installation — sans quoi l'installeur écrit dans le `$HOME/.claude.json` réel du poste (fait mesuré), jamais sous ce dossier jetable.**</fails_when>
  </verify>
  <acceptance_criteria>
    - `T=$(mktemp -d); R=$(git rev-parse --show-toplevel); OUT=$(cd "$T" && bash "$R/plugin/conductor/scripts/check-agents.sh" 2>&1); RC=$?; rm -rf "$T"; [ "$RC" -eq 3 ] && echo "$OUT" | grep -q CIBLE-ABSENTE && echo D20-OK` imprime `D20-OK`
    - `T=$(mktemp -d); mkdir -p "$T/.claude/agents"; R=$(git rev-parse --show-toplevel); OUT=$(cd "$T" && bash "$R/plugin/conductor/scripts/check-agents.sh" 2>&1); RC=$?; rm -rf "$T"; [ "$RC" -eq 0 ] && ! echo "$OUT" | grep -q CIBLE-ABSENTE && echo D20-JUMEAU-VERT-OK` imprime `D20-JUMEAU-VERT-OK` (dossier présent et vide : régime F13 inchangé)
    - la sortie de test-check-agents.sh contient des lignes `✓ T103`, `✓ T55`, `✓ T56`, `✓ T57`, `✓ T58`, et finit sur `0 KO ==` (MUT-D20 est vérifié en 42-04, pas ici)
    - le SUMMARY porte l'inventaire complet des appelants du `<behavior>` ci-dessus, appelant par appelant, avec la mention « inchangé » ou le changement constaté pour chacun
    - **Résidu signalé (mission `revise-42c`, pas dans le périmètre de ce plan, ne PAS corriger le fichier) :** le SUMMARY mentionne que le commentaire `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh:2291` (« check-agents.sh SANS argument sort exit 0 trivialement sur ce dépôt … vert vide ») est PÉRIMÉ après D-20 — une invocation nue sur cible absente hors --hook rend désormais INDÉTERMINÉ (exit 3, CIBLE-ABSENTE), pas exit 0 vert vide. Consigné pour relecture, aucun fichier de dev-orchestrator n'est touché par ce plan.
    - `git log -1 --format=%B -- plugin/conductor/scripts/check-agents.sh | grep -c '^Gate-Touche: plugin/conductor/scripts/check-agents.sh — CIBLE-ABSENTE'` rend 1
  </acceptance_criteria>
  <done>Une cible absente hors --hook rend INDÉTERMINÉ (exit 3, jeton CIBLE-ABSENTE) dans tous les modes ; une cible présente et vide garde le régime F13 déjà en place ; --hook reste silencieux et exit 0 sur cible absente (T56 inchangé) ; l'inventaire complet des appelants du dépôt montre qu'aucun ne devient rouge par effet de bord, en particulier un lab frais sans .claude/agents dont le seul appel en pratique passe par --hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichier manifeste → gate | un JSON versionné (ou copié dans un lab) est une entrée non fiable, au même titre qu'un frontmatter d'agent |
| cache du plugin → lab (installeur) | le glob `*.json` décide quels fichiers de données atterrissent sous `.claude/scripts/` chez l'utilisateur |
| gate → garde d'écriture | la forme de la ligne de diagnostic décide si la garde refuse ou laisse passer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering / DoS du contrôle | charger_manifeste() | high | mitigate | validation de schéma complète (six listes exactes, valeurs non vides, dates ISO, sources https, valide_jours entier > 0, aucun défaut dans le script) ; échec → MANIFESTE-ILLISIBLE rc=1, jamais une liste vide ; T77/T78/T79 |
| T-42-02 | Tampering (injection) | valeurs du manifeste | medium | mitigate | les valeurs ne sont que comparées en mémoire Python ; charset `[A-Za-z0-9_-]+` imposé (T79 hors-charset) ; critère négatif : aucun appel de processus ni évaluation dynamique dans check-agents.sh (AUCUN-APPEL-SHELL) |
| T-42-03 | Repudiation | verifie_le déclaré | low | accept | le gate vérifie la forme d'une date, jamais sa véracité (doctrine du dépôt, cf. G-2) ; prohibition P1 relue en PR ; les dates futures sont traitées en 42-04 |
| T-42-04 | Elevation / Tampering | boucle *.json de copy_module_scripts() | low | mitigate | pas de mode exécutable, résidus `.json.bak` et fichiers cachés non ramassés, pose consignée au manifeste d'installation (T54) |
| T-42-05 | Denial of Service (utilisateur) | manifeste absent dans un lab | medium | mitigate | la ligne MANIFESTE-ILLISIBLE n'a pas la forme « ✗ … : … » : la garde d'écriture applique son fail-open documenté au lieu d'un refus aveugle ; le signal remonte au SessionStart (T82) |
| T-42-SC | Tampering | installations de paquets | low | accept | aucune installation npm/pip/cargo dans cette phase (42-RESEARCH § Package Legitimacy Audit : non applicable) |

Renvoi canon (sonde de prohibitions, ADR-550 D6) : l'injection via une valeur du manifeste est une classe canon — traitée par T-42-02, pas frappée en prohibition.
</threat_model>

<verification>
- Tracer : lab frais (fermeture de conductor) → manifeste posé non exécutable → gate vert → manifeste retiré → MANIFESTE-ILLISIBLE et rc non nul.
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` : 0 KO, T77 à T82 présents.
- `bash plugin/_internal/tests/test-vibeflow-update.sh` : T54 vert, 0 KO.
- Rejeu des trois étapes CI `check-agents` (sans l'option de fraîcheur, posée en 42-04) : `for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d"; done` — rc 0 partout.
- `bash plugin/conductor/scripts/check-blueprints.sh` : les 9 blueprints restent verts (I8, PR #85).
- D-20 : `R=$(git rev-parse --show-toplevel); T=$(mktemp -d); (cd "$T" && bash "$R/plugin/conductor/scripts/check-agents.sh"); echo rc=$?` — **CORRECTIF DE REVUE (W3, mission `revise-42b`) : `$R` capturé AVANT le `cd`, jamais `$PWD` lu APRÈS (un `$PWD` résolu dans le sous-shell après `cd "$T"` vaudrait `$T`, pas la racine du dépôt — le script visé n'existerait pas à ce chemin, rc=127, faux négatif silencieux).** rc=3 avec `CIBLE-ABSENTE` ; même chemin avec `.claude/agents` créé au préalable — rc=0, jamais `CIBLE-ABSENTE`.
</verification>

<success_criteria>
- FABR-01 : listes dans un manifeste daté versionné, source unique sans copie de repli, date et source par liste, absent ou illisible = refus explicite.
- D-16 tenu dans la même vague : le manifeste arrive dans un lab installé.
- D-17 tenu : 46 outils, trois ajouts seulement, aucun nom hors doc officielle.
- T76 intact (D-13).
- D-20 tenu : une cible absente hors --hook rend INDÉTERMINÉ dans tous les modes ; l'inventaire complet des appelants du dépôt montre qu'aucun ne devient rouge par effet de bord.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` | premier niveau du manifeste | 42-01 |
| `listes.outils`, `listes.champs_frontmatter`, `listes.types_natifs`, `listes.modeles`, `listes.modes_permission`, `listes.niveaux_effort` | les six listes (D-01) | 42-01 |
| `verifie_le`, `source`, `valeurs` | clés de chaque liste | 42-01 |
| `VF_MANIFEST` | variable interne bash → Python, dérivée du dossier du script | 42-01 |
| `VIBEFLOW_FIELDS` | constante Python : les quatre champs `vf-*` restés en dur | 42-01 |
| `charger_manifeste()` | fonction Python : lecture + validation de schéma, fail-closed | 42-01 |
| `MANIFESTE-ILLISIBLE` | jeton de diagnostic (D-03) | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` | installeur (D-16) | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` | helpers et harnais de `test-check-agents.sh` | 42-01 |
| `T54` | cas de `test-vibeflow-update.sh` | 42-01 |
| `T77` à `T82` | cas de `test-check-agents.sh` (manifeste) | 42-01 |
| `T103`, jeton `CIBLE-ABSENTE` | invocation nue sur cible absente hors --hook (D-20) | 42-01 |
| `MUT-D20` | mutant de la ligne `cible_absente` posée en 42-01 | 42-04 |
| `--manifest-freshness=lenient\|strict` | option CLI du gate, défaut `lenient`, toute autre valeur → rc 1 | 42-04 |
| `VF_MANIFEST_FRESHNESS` | variable interne bash → Python | 42-04 |
| `manifeste_perime()` | fonction Python : listes périmées ou datées dans le futur | 42-04 |
| `retrograder` | booléen Python (D-05) | 42-04 |
| `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | jetons de diagnostic (D-04, D-05) | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` | helpers de mutation QUAL-01 de `test-check-agents.sh` | 42-04 |
| `T83` à `T90`, `MUT-F1`, `MUT-F2` | cas et mutants (fraîcheur) | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()` | cœur pur de `analyze_token()`, sans effet de bord | 42-05 |
| `allowlist_agents()` | noms des allowlists `Agent(...)`/`Task(...)` non vides d'un frontmatter | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` | invariants locaux | 42-05 |
| jeton `invariant I<n>` | préfixe des diagnostics d'invariant | 42-05, 42-06 |
| `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions, index nom → chemin (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| jeton `SendMessage` sur 4 managers (inconditionnel) | frontmatter du corpus | 42-02, 42-03 |
| `omitClaudeMd: true` sur 4 juges (SEULEMENT si ARBITRAGE-MAINTENIR au checkpoint D-19) ; sonde `ARBITRAGE-*` | frontmatter du corpus, checkpoint | 42-05 |
| versions de module | conductor mineure (42-06) ; mobile-test-team, business-pilot-bundle, content-bundle (42-02), growth-bundle, design-orchestrator (42-03) en patch | 42-02, 42-03, 42-06 |

## Flagged assumptions

- **Flake connu T9/T29/T32 de test-check-agents.sh (documenté dans la PR #100, workstream fiabilite) : un rouge isolé sur ces trois cas précis (SIGPIPE) se rejoue seul avant d'être déclaré réel — jamais pris pour argent comptant à la première lecture de sortie de suite (0 KO attendu dans ce plan).**

- **Sonde de bords spec-less — FABR-01 : `unclassified` / `unresolved`.** Aucun bord n'a été classé
  par la sonde ; il n'est ni résolu d'office ni écarté. Les bords rédigés à la main dans ce plan
  (manifeste absent, tronqué, schéma invalide, cible vide, --hook, garde) sont des vérités du plan,
  pas une résolution de la sonde.
- **MEMORY reste en dur** (`user`, `project`, `local`) : D-01 énumère six listes, la portée mémoire
  n'en fait pas partie. Écart signalé ; candidat à une septième liste lors d'un prochain
  rafraîchissement, sur décision.
- **Garde d'écriture et manifeste illisible** : choix de forme du diagnostic (Claude's Discretion) —
  la garde laisse passer (panne du contrôleur) plutôt que de refuser toute écriture d'agent dans un
  lab à l'install cassée ; le refus reste celui du gate (rc 1) et le signal remonte au SessionStart.
- **Texte d'aide de la garde** : `guard-agent-write.sh` garde un squelette qui énumère des valeurs
  admises dans son message de refus ; il n'est pas une copie de repli lue par le gate (D-01 vise
  `check-agents.sh`) et n'est pas modifié ici — résidu à relire au prochain rafraîchissement.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-01-SUMMARY.md` when done — commit séparé des commits `plugin/`.
</output>

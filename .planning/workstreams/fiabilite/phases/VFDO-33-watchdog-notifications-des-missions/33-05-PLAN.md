---
phase: 33-watchdog-notifications-des-missions
plan: 05
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04"]
files_modified:
  - plugin/conductor/scripts/dag.sh
  - plugin/conductor/scripts/tests/test-dag.sh
  - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md
autonomous: true
requirements: [WTCH-02, WTCH-03, WTCH-04]

must_haves:
  truths:
    - "D-33-F (décision Samuel, 2026-08-17, déplacé de 33-02 vers ce plan par la 2ᵉ recoordination — 33-02 en gardait la dépendance non déclarée sur 33-03, collision de vague, collision de numéros de cas avec ce plan sur `test-dag.sh`) — `dag.sh mark` gagne un DEUXIÈME point de contrôle, en LECTURE cette fois : APRÈS `record_progress()` (33-02) et APRÈS `save(dag)`, il invoque le sibling `check-guard-health.sh --hook` (résolution par répertoire de script, même patron `SCRIPT_DIR` que `DRIVER_LOCK_SH`/`NOTIFY_SH`) et, si la sortie n'est pas vide, la relaie sur stderr TELLE QUELLE. Ce contrôle NE RECALCULE RIEN : la logique de verdict sain/stall/abandon vit EXCLUSIVEMENT dans `check-guard-health.sh` (plan 33-03) — `dag.sh` consomme sa sortie `--hook`, jamais un second seuil, jamais une seconde lecture de `driver-lock.sh status` à cette fin. C'est le point de lecture qui manquait : `SessionStart` seul (33-03) ne voit un stall qu'à l'OUVERTURE d'une session ; ce contrôle le voit au PROCHAIN GESTE `mark` d'une session déjà vivante (ROADMAP amendé : « un stall ne survit pas au prochain geste »). Best-effort, `timeout=2`, `try/except Exception` englobant unique, jamais bloquant, ne tue jamais (ADR-031) — même dégradation silencieuse que `record_progress()`/`record_milestone()`. Ce plan dépend donc de 33-03 pour la stabilité du contrat `--hook` (déjà déclaré en `depends_on`)."
    - "`dag.sh mark` (déjà étendu par 33-02 d'un appel `record_progress()`) shelle EN PLUS un appel best-effort à `notify.sh` (sibling, plan 33-04) — mais UNIQUEMENT quand `status` est `done` ou `failed`. Sur `status=running`, AUCUN appel n'est fait : c'est la lecture littérale de « jamais à chaque tour » (WTCH-03, critère de succès n°3) — un `mark --status=running` est le geste le PLUS fréquent du DAG, une notification dessus serait exactement le bruit que le critère interdit."
    - "« Halt condition » (WTCH-03) : PRÉMISSE CORRIGÉE (revue externe `33-REVUE-EXTERNE.md` C8, vérifiée sur disque avant d'écrire cette ligne) — `dag.sh:70` porte `VALID = {\"blocked\", \"ready\", \"running\", \"done\", \"failed\"}` et le bloc `mark` (`dag.sh:246`) écrit le statut REÇU EN ARGUMENT sans distinction : `mark --status=blocked` est un appel VALIDE, `blocked` N'EST DONC PAS réservé à `recompute()`. `mission-flow.md:245`, la source que ce plan cite lui-même pour justifier le choix, dit explicitement du halt autonome : « le laisser **`blocked`/`failed`** » — les deux valeurs, pas une seule. Le filtre de ce plan reste néanmoins volontairement borné à `done`/`failed` (voir bullet suivant) : couvrir aussi `blocked` exigerait un cas de test dédié ET la prise en compte de l'instabilité décrite ci-dessous — hors du périmètre resserré de cette correction ciblée. Cette limite est donc écrite ICI noir sur blanc comme une **limite assumée**, pas comme la conséquence d'une prémisse fausse : un `mark --status=blocked` explicite (le geste de gel documenté par `mission-flow.md:245`) ne déclenche PAS de notification par ce plan — à trancher séparément (déféré au manager : soit étendre le filtre de `record_milestone()` à `blocked` dans une correction ultérieure avec son propre cas de test, soit resserrer noir sur blanc `mission-flow.md` pour que la doctrine de gel dise explicitement « halte ⇒ `failed`, jamais `blocked` » et faire disparaître l'ambiguïté à la source)."
    - "Nuance d'instabilité (à connaître même si `blocked` reste hors filtre ici) : `recompute(nodes)`, appelé juste après l'écriture du statut dans le bloc `mark`, peut re-basculer un `blocked` fraîchement écrit vers `ready` si toutes ses `deps` sont déjà `done` — le geste « geler en `blocked` » n'a donc PAS la stabilité d'un `failed` (jamais retouché par `recompute`). C'est un argument de fond, indépendant de la correction de prémisse ci-dessus, pour ne pas étendre naïvement le filtre de notification à `blocked` sans traiter ce cas."
    - "L'interface CLI de `notify.sh` — CONFIRMÉE depuis l'écriture initiale de ce plan : la revue externe (`33-REVUE-EXTERNE.md`, trois vérificateurs frais) a confronté l'assomption `[notify_sh, title, body]` (deux positionnels TITLE/BODY, exit toujours 0) à `33-04-PLAN.md` — nom, chemin, ordre et nombre d'arguments, absence de flags, code de sortie, comportement sur arguments manquants : AUCUNE divergence. La couture est saine. La consigne de prudence reste néanmoins active pour l'EXÉCUTION de ce plan : la tâche 1 relit quand même l'interface RÉELLE sur disque (33-04 committé par construction de la vague) avant d'écrire l'appel — au cas où le fichier réellement livré diverge du texte du plan qu'on relit ici — et consigne au SUMMARY si l'assomption tient toujours à ce moment-là. Le wrapper `try/except Exception: return` englobant reste la garantie de dégradation si, malgré tout, une divergence apparaissait."
    - "`notify.sh` ne doit JAMAIS produire de VRAIE notification système pendant l'exécution de `test-dag.sh` (ni sur la machine de dev macOS via `osascript`, ni en CI Linux via `notify-send` s'il est présent — `test-dag.sh` est découvert automatiquement par `ci.yml:213`) : 33-04 construit exactement trois points d'injection pour l'empêcher (`VF_NOTIFY_FORCE_CHANNEL`, shims de binaires via `VF_NOTIFY_BIN_DIR` préfixé au PATH, shim `uname`/`VF_PROC_VERSION_PATH`) — les cas T42/T43/T46 de ce plan, qui copient et instrumentent le VRAI `notify.sh` posé par 33-04, DOIVENT les utiliser. Copier le vrai script sans les positionner ferait trouver `osascript`/`notify-send` réels sur le PATH hérité et déclencherait un vrai toast à chaque exécution locale de la suite."
    - "Aucune nouvelle entrée n'est ajoutée à `plugin/conductor/hooks/hooks.json` : `notify.sh` est invoqué PROGRAMMATIQUEMENT en sous-processus depuis `dag.sh`, jamais câblé comme un hook Claude Code — le gate d'armement réel des hooks (PORT-05, `.github/workflows/ci.yml` L777-901, D-33-D) n'a donc RIEN de nouveau à vérifier pour ce plan. La preuve attendue de WTCH-04 pour ce plan est une NON-RÉGRESSION : `expect_total` (dérivé par PORT-05 de la fermeture des `hooks.json`) reste identique avant/après, parce que `hooks.json` n'est jamais touché — vérifié localement par diff, pas par un run CI complet (interdit de push par le mandat)."
    - "Aucune édition de `plugin/dev-orchestrator/scripts/check-capability-activation.sh` (`ARM[]`/`OKID[]`, D-33-D) : ce plan ne déclare aucun nouvel armement de frontmatter `vf-requires:`/`vf-provides:` — le réservé conditionnel de D-33-D ne se déclenche pas ici."
    - "La recette de clôture Windows (D-33-C) est écrite comme un DOCUMENT, jamais exécutée dans ce plan (aucune machine Windows disponible) : elle se rattache explicitement au fil des testeurs Windows de l'issue #20 (vérifié par le cadrage, `gh issue view 20`, section 7), porte la mention explicite « condition de clôture, pas un gate dur », et reproduit — sans les inventer ni les combler — les trois zones NON PROUVÉES du spike (chaîne Windows jamais exécutée réellement, AUMID arbitraire vs AUMID PowerShell non tranché, latence `powershell.exe` non mesurée en conditions réelles)."
    - "**Couverture doctrine — décision explicite, pas un oubli** (finding mineur du 2ᵉ plancheck externe) : ce plan touche `mission-flow.md` (via 33-02, D-33-E) mais AUCUN plan de la Phase 33 ne bump le `CHANGELOG.md`/`VERSION` du module `conductor`, ni n'amende `vf-dev-manager.md` pour y mentionner `mark-progress`/`notify.sh`/le relais de stall — alors que le CLAUDE.md racine du dépôt fait du `CHANGELOG`/`VERSION` par module une convention de distribution à CHAQUE release, et que le verbe public `mark-progress` (33-01/33-02) et `notify.sh` (33-04/33-05) n'apparaîtraient sinon dans aucune doctrine lue par les managers en dehors de `mission-flow.md` lui-même. Précédent constaté sur ce même repo (Phase 32, `plugin/conductor/CHANGELOG.md`, entrée `[v1.26.0]`) : le bump CHANGELOG/VERSION du module s'y fait au moment de la RELEASE de la phase entière, pas plan par plan pendant l'exécution — CE PLAN NE le fait DONC PAS non plus, par cohérence avec ce précédent, et le laisse EXPLICITEMENT au geste de clôture/release de la Phase 33 (bump `plugin/conductor/VERSION`+`CHANGELOG.md` couvrant WTCH-01..04/QUAL-01, et vérification lecture de `vf-dev-manager.md` pour y ajouter, si besoin, une ligne renvoyant vers `mission-flow.md` — décision à trancher par le manager qui shippe la phase, pas improvisée ici)."
  artifacts:
    - "plugin/conductor/scripts/dag.sh : fonction `check_stall_signal(check_guard_health_sh)` (D-33-F), fonction `record_milestone(notify_sh, nid, status, mission_file)`, résolution `CHECK_GUARD_HEALTH_SH`/`NOTIFY_SH` en siblings (même patron `SCRIPT_DIR` que `DRIVER_LOCK_SH`, 33-02), appel de `check_stall_signal()` APRÈS `record_progress()`, appel de `record_milestone()` en DERNIER, filtré sur `status in (\"done\", \"failed\")`"
    - "plugin/conductor/scripts/tests/test-dag.sh : cas T41 à T47 (T41 = relais `check_stall_signal()`, T42 à T47 = `record_milestone()`)"
    - ".planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md : recette de validation humaine Win10/11, rattachée au fil testeurs de l'issue #20, condition de clôture non exécutée"
  key_links:
    - "check-guard-health.sh --hook (plan 33-03) <-> dag.sh check_stall_signal() (ce plan) : ce plan ne DUPLIQUE aucun seuil ni aucune logique de verdict — il invoque le binaire de 33-03 en sous-processus et relaie sa sortie telle quelle. Un changement du format `--hook` côté 33-03 (aujourd'hui : une ligne ou stdout strictement vide) casserait silencieusement ce relais (dégradation en no-op, jamais un crash) — 33-03 doit être committé et son contrat `--hook` stable avant ce plan (dépendance déjà déclarée en `depends_on`)."
    - "record_progress() (33-02) <-> check_stall_signal() (ce plan) <-> record_milestone() (ce plan) : TROIS fonctions séparées, jamais fusionnées, ORDRE STRICT dans le bloc `mark` — `record_progress` avance `progress_epoch` à CHAQUE mark (y compris `running`) ; `check_stall_signal` relaie best-effort le verdict de 33-03 à CHAQUE mark aussi (pas de filtre de statut) ; `record_milestone` notifie SEULEMENT sur `done`/`failed`. Les fusionner romprait la garantie « jamais à chaque tour » de `record_milestone` et coupleraient trois contrats stables indépendamment (33-01, 33-03, 33-04) à un seul point de panne."
    - "notify.sh (plan 33-04) <-> record_milestone() : l'appel prend la forme `[notify_sh, title, body]`, deux positionnels — la couture est vérifiée saine (voir truths), la tâche 1 relit quand même le disque réel au moment de l'exécution, jamais l'assomption figée par écrit."
    - "PORT-05 (CI, .github/workflows/ci.yml L777-901) <-> ce plan : la preuve WTCH-04 attendue ici est que `plugin/conductor/hooks/hooks.json` n'apparaît JAMAIS dans le diff de ce plan — PORT-05 n'a rien de nouveau à couvrir, exactement comme pour le battement de la Phase 32 (D-32-07 corollaire)."
    - "`save(dag)` (bloc `mark`) <-> `check_stall_signal()`/`record_milestone()` : ORDRE STRICT — les deux s'appellent APRÈS `save(dag)`, jamais avant, et `record_milestone()` reste le DERNIER des deux (une notification de fin de nœud doit refléter l'état final déjà persisté ET déjà diagnostiqué, jamais l'inverse). `33-TERRAIN.md` avertit que `save()` réécrit le fichier ENTIÈREMENT et n'est PAS atomique ; `run_bounded` (patron de test, `test-dag.sh`) tue le process à **5 s**, pas 10 s. Un `timeout=` mal calibré sur l'un des deux appels peut faire tuer le process externe avant que la transition de statut soit persistée si l'ordre n'est pas respecté — même classe de bug que 33-02 corrige pour `record_progress()`."
    - "Index argv de `dag.sh` : `driver_lock_sh` est en position 9 (posé par 33-02). CE PLAN alloue les DEUX index suivants, en dur, dans cet ordre : `check_guard_health_sh` = position 10, `notify_sh` = position 11 — c'est ce plan qui porte les deux consommateurs ajoutés après 33-02, donc c'est lui qui tranche l'allocation (33-02 les réserve explicitement sans les occuper, voir son must_haves)."
  prohibitions:
    - "Ne JAMAIS recalculer côté `dag.sh` le seuil de stall ou la distinction sain/stall/abandon dans `check_stall_signal()` — cette logique appartient exclusivement à `check-guard-health.sh` (33-03) ; `dag.sh` ne fait que relayer sa sortie `--hook`"
    - "Ne JAMAIS appeler `notify.sh` sur `status == \"running\"` — seul `done`/`failed` déclenche l'appel (WTCH-03 critère n°3, « jamais à chaque tour ») ; `check_stall_signal()` n'a PAS ce filtre, il s'exécute à CHAQUE `mark`, quel que soit `status`"
    - "Ne JAMAIS ajouter d'entrée à `plugin/conductor/hooks/hooks.json` dans ce plan"
    - "Ne JAMAIS éditer `plugin/dev-orchestrator/scripts/check-capability-activation.sh` (`ARM[]`/`OKID[]`) — réserve conditionnelle de D-33-D non déclenchée par ce périmètre"
    - "Ne JAMAIS ÉDITER `plugin/conductor/scripts/check-guard-health.sh` ni `plugin/conductor/scripts/tests/test-check-guard-health.sh` — territoire de 33-03 (ce plan lance sa suite en non-régression et invoque son binaire `--hook` en sous-processus, ne l'édite jamais) ; le halt condition couvert par la partie `record_milestone()` de ce plan reste borné à `mark --status=failed`, sans toucher ce fichier"
    - "Ne JAMAIS faire dépendre le succès de `dag.sh mark` (mise à jour du statut, `recompute()`, `save(dag)`) de la réussite de l'appel `check-guard-health.sh --hook` ni de `notify.sh` — même garantie de dégradation que `record_progress()` (33-02) : absent, non exécutable, sortie non nulle, JSON ou stdout quelconque, ou pendaison, aucun de ces cas ne fait jamais échouer `mark`"
    - "Aucun `shell=True`, aucune interpolation de `nid`/`status`/`title`/`body` dans une chaîne de commande shell — liste d'arguments uniquement, patron déjà établi par `record_progress()`"
    - "Ne JAMAIS prétendre avoir vérifié la chaîne de notification Windows réelle dans `33-CLOTURE-WINDOWS.md` — aucune machine Windows disponible, la recette reste NON EXÉCUTÉE, à écrire comme telle"
    - "Aucun `sleep` non borné dans les cas de test — patron `run_bounded`/isolation par copie déjà établi (T33/T36-T38)"
    - "Ne JAMAIS copier/instrumenter le VRAI `notify.sh` dans un cas de test (T42/T43/T46) sans positionner `VF_NOTIFY_FORCE_CHANNEL` ET `VF_NOTIFY_BIN_DIR` (shims) — un vrai `osascript`/`notify-send` trouvé sur le PATH hérité déclencherait un toast système réel à chaque exécution de `test-dag.sh`"
    - "Ne JAMAIS placer l'appel à `check_stall_signal()` ou `record_milestone()` avant `save(dag)` dans le bloc `mark` — `run_bounded` tue à 5 s, pas 10 s ; une transition tuée avant persistance serait perdue"
---

<objective>
Câbler DEUX choses sur le même point `mark` de `dag.sh`, toutes deux en LECTURE/notification
best-effort, jamais en écriture de statut :

1. **D-33-F** (déplacé de 33-02 vers ce plan par la 2ᵉ recoordination) : `check_stall_signal()`
   relaie, APRÈS `record_progress()` (33-02) et APRÈS `save(dag)`, le verdict `check-guard-health.sh
   --hook` (33-03) sur stderr — SANS jamais dupliquer son seuil ni sa logique de verdict, à CHAQUE
   `mark` quel que soit `status`.
2. Le dernier jalon de WTCH-03 : `notify.sh` (posé par le plan 33-04, sibling de `dag.sh`) part aux
   DEUX moments où `dag.sh mark` clôt un nœud — `done` (fin de nœud) et `failed` (halt condition
   couverte par ce plan ; `blocked` reste un signal de halte valide côté doctrine mais hors filtre,
   limite assumée documentée dans `<truths>`) — jamais sur `running`, qui est le geste le plus
   fréquent du DAG et que le critère de succès n°3 exclut explicitement (« jamais à chaque tour »).

`depends_on` inclut 33-03 pour DEUX raisons désormais : la stabilité du contrat `--hook` que
`check_stall_signal()` consomme, ET ses tests de non-régression invoqués par ce plan.

Purpose : sans D-33-F, le seul point de détection d'un stall reste `SessionStart` (33-03) — une
session ouverte des heures qui pilote une mission bouclée ne verrait jamais le signal, alors que le
ROADMAP amendé promet « au prochain GESTE ». Sans le câblage `notify.sh`, ce script (33-04) reste un
script isolé jamais appelé en usage réel — ce plan est ce qui rend WTCH-02 ET WTCH-03 VRAIS en
pratique, symétrique à ce que 33-02 a fait pour WTCH-01. Il ferme aussi WTCH-04 pour son propre
périmètre : aucune entrée `hooks.json` n'est ajoutée (les deux appels sont des sous-processus
programmatiques, jamais des hooks Claude Code), donc le gate PORT-05 n'a rien de neuf à couvrir — la
preuve attendue est une non-régression, pas une extension du gate. Enfin, il consigne la recette de
clôture Windows (D-33-C) comme document rattaché au fil testeurs de l'issue #20, sans jamais
prétendre l'avoir exécutée.

Output : `dag.sh` étendu de DEUX fonctions et deux appels dans le bloc `mark` (tous deux APRÈS
`save(dag)`, `record_milestone()` en dernier) ; `test-dag.sh` étendu de 7 cas (T41-T47 : T41 = relais
`check_stall_signal()`, T42-T47 = filtre `done`/`failed`/`running` de `record_milestone()`, pannes
dégradées, non-régression statique) ; un document de clôture Windows nouveau, jamais exécuté.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-02-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-03-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-04-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-SPIKE-canal-notification.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-REVUE-EXTERNE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : `check_stall_signal()` (D-33-F) et `record_milestone()` câblés au point `mark`, filtre `done`/`failed`, dégradation garantie</name>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <read_first>
    - plugin/conductor/scripts/dag.sh (état réel APRÈS exécution de 33-02 — `SCRIPT_DIR`/`DRIVER_LOCK_SH` en tête, `record_progress()` après `resolve_gsd_tools_cmd()`, son appel dans le bloc `mark` juste après `save(dag)`) : lire le fichier réel sur disque, pas les numéros de ligne du plan 33-02 qui ne correspondent qu'à l'état PRÉ-exécution
    - plugin/conductor/scripts/notify.sh (état réel APRÈS exécution de 33-04, sibling déjà posé par construction de la vague — plan 33-05 dépend de 33-04) : lire son interface CLI RÉELLE (nombre et ordre des arguments positionnels, flags éventuels) AVANT d'écrire l'appel — si elle diverge de l'assomption `[notify_sh, title, body]` documentée dans `33-SPIKE-canal-notification.md`, s'aligner sur le disque, consigner l'écart au SUMMARY
    - plugin/dev-orchestrator/references/mission-flow.md:243-246 (« halte de nœud, jamais de mission... le laisser blocked/failed ») — fondement du choix « halt condition = mark --status=failed », à ne pas rouvrir vers `check-guard-health.sh` ni `dev-orchestrator`
    - .github/workflows/ci.yml L777-901 (gate PORT-05 — dérive `expect_total` des `hooks.json` de la fermeture installée) — pour vérifier que ce plan ne change RIEN à ce total
    - plugin/conductor/scripts/tests/test-dag.sh (bloc T33/T36-T38, patron d'isolation par copie de `dag.sh` + faux sibling exécutable dans un répertoire ISOLÉ du `WORK_DIR`) — à répliquer pour un faux `notify.sh` qui journalise son argv
    - plugin/conductor/scripts/tests/test-dag.sh, fonction `run_bounded` (relire la valeur RÉELLE du délai de `kill -9` — mesurée à **5 s**, PAS 10 s comme un texte de plan pourrait le laisser penser) — calibrer tout `timeout=` interne à `check_stall_signal()`/`record_milestone()` largement EN DESSOUS de cette valeur, pas au-dessus ni égal
    - `33-04-PLAN.md` §comportement N1-N6 (`VF_NOTIFY_FORCE_CHANNEL`, `VF_NOTIFY_BIN_DIR` préfixé au PATH avec shims journalisant `"$@"`) : les cas T42/T43/T46 qui copient le VRAI `notify.sh` DOIVENT positionner ces deux variables — sinon la cascade réelle de `notify.sh` cherche `osascript`/`notify-send` sur le PATH hérité et peut déclencher un vrai toast
    - plugin/conductor/scripts/check-guard-health.sh:20-25 (contrat `--hook` : une ligne sur stdout si signal, STRICTEMENT VIDE sinon — c'est le format que `check_stall_signal()` relaie tel quel, sans le réinterpréter ; 33-03 committé par construction de la vague)
  </read_first>
  <behavior>
    - T41 (D-33-F, relais `check_stall_signal()`) : sibling `check-guard-health.sh` ISOLÉ (patron T36-38) qui émet une ligne `[mission-watchdog] stall detecte ...` sur stdout sous `--hook` → cette ligne EXACTE apparaît sur stderr de `dag.sh mark` (relais fidèle, aucune reformulation) ; un second sous-cas avec un `check-guard-health.sh` ISOLÉ absent/non exécutable → `dag.sh mark` sort 0 sans aucune ligne sur stderr liée au stall (dégradation silencieuse, patron identique à `record_progress`) ; un troisième sous-cas `status=running` → `check_stall_signal()` s'exécute QUAND MÊME (pas de filtre de statut, contrairement à `record_milestone()`).
    - T42 : lock acquis, `dag.sh mark --status=done` sur un nœud, sibling `notify.sh` RÉEL (celui posé par 33-04) copié dans le répertoire isolé et INSTRUMENTÉ pour journaliser son `"$@"` dans un fichier — invoqué avec `VF_NOTIFY_FORCE_CHANNEL` (ex. `linux`) ET `VF_NOTIFY_BIN_DIR` (répertoire de shims inoffensifs préfixé au PATH, AUCUN `osascript`/`notify-send` réel accessible) positionnés dans l'environnement de la copie isolée de `dag.sh` — AUCUN vrai toast système ne doit jamais apparaître → le journal contient exactement 2 champs, le premier (TITLE) mentionne l'idée de fin/complétion, le second (BODY) contient l'id du nœud.
    - T43 : mêmes conditions (mêmes `VF_NOTIFY_FORCE_CHANNEL`/`VF_NOTIFY_BIN_DIR`), `dag.sh mark --status=failed` → le journal de `notify.sh` contient 2 champs, le premier (TITLE) mentionne l'idée de halte/échec, le second (BODY) contient l'id du nœud.
    - T44 : `dag.sh mark --status=running` sur un autre nœud → AUCUNE ligne n'apparaît dans le journal de `notify.sh` (fichier de journal absent OU vide, contrôlé explicitement) — cas de DISCRIMINANCE, à ne JAMAIS retirer : sans lui, la suite resterait verte avec un `dag.sh` qui notifierait à chaque tour.
    - T45 : `notify.sh` ABSENT du répertoire isolé → `dag.sh mark --status=done` sort 0, le DAG est mis à jour normalement (`status` reflète `done`), aucune erreur affichée.
    - T46 : `notify.sh` ISOLÉ qui PEND (`sleep 30`, patron T38), `VF_NOTIFY_FORCE_CHANNEL`/`VF_NOTIFY_BIN_DIR` positionnés comme T42/T43 (le shim qui pend NE DOIT PAS non plus pouvoir déclencher un vrai canal) → `dag.sh mark --status=failed` revient en moins de **5 s** (`run_bounded` tue à 5 s, PAS 10 s — ne pas recopier l'ancienne affirmation), sort 0, ET **le DAG est bien persisté `status=failed`** (relu depuis le fichier après l'appel, pas seulement `rc=0` inspecté) — preuve que l'appel de notification est placé APRÈS `save(dag)` et que son `timeout=` interne (calibré nettement sous 5 s) protège réellement l'appelant sans jamais pouvoir faire perdre la transition déjà écrite.
    - T47 : non-régression statique — aucun `shell=True` (compte inchangé) ; l'appel à `notify.sh` passe une LISTE (`grep` positif sur la forme `[notify_sh, title, body]`) ; l'appel à `check-guard-health.sh` passe une LISTE (`[check_guard_health_sh, "--hook"]`) ; `hooks.json` n'apparaît dans AUCUN diff produit par ce plan (vérifié par `git diff --name-only` ancré sur le commit de BASE de ce plan — celui juste avant son premier commit, jamais une plage vague — hors suite bash, consigné au SUMMARY, pas un cas T) ; l'appel à `record_milestone(...)` apparaît TEXTUELLEMENT après `save(dag)` ET après `check_stall_signal(...)` dans le bloc `mark` de `dag.sh` (vérifiable par position de ligne).
  </behavior>
  <action>
1. En tête de `dag.sh`, à côté du calcul déjà posé par 33-02 (`SCRIPT_DIR`/`DRIVER_LOCK_SH`), ajouter :
   ```
   CHECK_GUARD_HEALTH_SH="$SCRIPT_DIR/check-guard-health.sh"
   NOTIFY_SH="$SCRIPT_DIR/notify.sh"
   ```
   Mêmes siblings, même garantie (même glob d'installation, 33-TERRAIN.md §4) : jamais un candidat
   relatif au cwd. Relire l'état réel du fichier après 33-02 avant d'insérer — `SCRIPT_DIR` est déjà
   posé par 33-02, ne PAS le redéfinir.

2. Étendre la ligne d'invocation du heredoc python pour passer `"$CHECK_GUARD_HEALTH_SH"` en 10e
   argument positionnel et `"$NOTIFY_SH"` en 11e, et la capture `sys.argv[1:9]` déjà étendue par
   33-02 (`driver_lock_sh = sys.argv[9]`) pour lire aussi `check_guard_health_sh, notify_sh =
   sys.argv[10:12]`. **Index alloué EN DUR par ce plan** (voir must_haves « Index argv de dag.sh ») :
   33-02 réserve l'index 9 pour `driver_lock_sh` SANS occuper 10/11, c'est ce plan qui les occupe
   tous les deux, dans cet ordre. Relire l'état réel du fichier après 33-02 pour confirmer que
   l'index 9 est bien celui-là avant d'ajouter 10/11 — ne jamais le présumer sans relecture.

3. Ajouter la fonction `check_stall_signal(check_guard_health_sh)` juste après `record_progress()`
   (même fichier, même style) — D-33-F, logique identique à celle initialement prévue par 33-02 avant
   son déplacement vers ce plan :
   - retourne immédiatement (silencieux) si `check_guard_health_sh` n'est pas un fichier ;
   - appelle `subprocess.run([check_guard_health_sh, "--hook"], capture_output=True, text=True,
     timeout=2, check=False)` ;
   - retourne si `returncode != 0` (contrat `--hook` de 33-03 : les codes SAIN/INDÉTERMINÉ sont déjà
     traduits en 0 par `hook_exit`, donc un code non nul ici signifie une erreur d'usage — rien à
     relayer) ;
   - si `stdout.strip()` est non vide, imprime CETTE CHAÎNE TELLE QUELLE sur `sys.stderr` (aucune
     reformulation, aucun recalcul — `dag.sh` ne sait RIEN du seuil de stall, il relaie le verdict
     déjà tranché par 33-03) ;
   - enveloppée d'un `try/except Exception: return` unique englobant, même patron que
     `record_progress()`. **Aucun filtre de statut** — s'exécute à CHAQUE `mark`, contrairement à
     `record_milestone()`.

4. Ajouter la fonction `record_milestone(notify_sh, nid, status, mission_file)` juste après
   `check_stall_signal()` (même fichier, même style) :
   - retourne immédiatement si `status` n'est ni `"done"` ni `"failed"` — c'est le filtre qui
     applique « jamais à chaque tour » ;
   - retourne si `notify_sh` n'est pas un fichier (`os.path.isfile`) ;
   - construit `title`/`body` à partir de `nid`, `status`, `os.path.basename(mission_file)` — deux
     chaînes courtes, lisibles, sans dépendance à une locale (ex. `"VibeFlow - noeud termine"` /
     `"VibeFlow - halte"`, corps `f"{nid} ({status}) - {os.path.basename(mission_file)}"`) ; AVANT
     d'écrire ces littéraux, relire l'interface réelle de `notify.sh` posée par 33-04 (read_first) et
     s'y conformer si elle prescrit une forme différente (ex. flags nommés au lieu de positionnels) —
     consigner l'écart au SUMMARY si l'assomption du spike n'a pas tenu ;
   - appelle `subprocess.run([notify_sh, title, body], stdout=subprocess.DEVNULL,
     stderr=subprocess.DEVNULL, timeout=2, check=False)` (ou la forme réelle relue à l'étape
     précédente) — résultat NON inspecté, best-effort. `stdout=DEVNULL, stderr=DEVNULL` PAS
     `capture_output=True` : `capture_output=True` ouvre un PIPE hérité par tout descendant du
     process (y compris un canal détaché en arrière-plan côté `notify.sh`) — un canal qui oublie de
     rediriger sa propre sortie garderait ce pipe ouvert et ferait attendre l'appelant jusqu'à l'EOF,
     mesuré à plusieurs secondes de blocage sur une variante de détachement de façade (33-04 N8/N15).
     `DEVNULL` ferme la question à la source, sans dépendre d'une garantie posée par un autre plan.
     `timeout=2`, PAS `5` : `run_bounded` (patron de test, `test-dag.sh`) tue le process externe à
     **5 s** — un `timeout=` interne égal ou supérieur risquerait une course avec ce `kill -9` externe
     (l'exception `TimeoutExpired` interne doit avoir la place de se lever et d'être absorbée par le
     `try/except` englobant AVANT que `run_bounded` ne tue tout depuis l'extérieur) ;
   - toute la fonction est enveloppée d'un `try/except Exception: return` unique englobant, patron
     `record_progress()`/`compute_stages()`.

5. Dans le bloc `if action == "mark":`, APRÈS `save(dag)` ET après l'appel `record_progress(...)`
   posé par 33-02, ajouter DANS L'ORDRE `check_stall_signal(check_guard_health_sh)` PUIS
   `record_milestone(notify_sh, nid, status, file)` — `record_milestone` reste TOUJOURS le DERNIER
   appel du bloc `mark`. Relire l'état réel du fichier au moment de l'exécution : si 33-02 place
   encore `record_progress(...)` AVANT `save(dag)` (ne devrait plus être le cas après sa propre
   correction, mais à vérifier), ne PAS reproduire ce placement pour les deux appels de ce plan — le
   signaler au SUMMARY comme divergence constatée plutôt que l'absorber en silence. Aucun des appels
   ajoutés par ce plan ne doit jamais empêcher `recompute`/`save`/`emit`/`sys.exit(0)` de s'exécuter
   (garanti par le `try/except` interne de chaque fonction).

6. Mettre à jour le bloc de commentaire d'en-tête pour documenter que `mark` relaie désormais le
   verdict `check-guard-health.sh --hook` (WTCH-02, D-33-F, à CHAQUE mark) ET notifie via `notify.sh`
   sur `done`/`failed` uniquement (WTCH-03), les deux en best-effort, sans jamais bloquer le DAG.

Étendre ensuite `plugin/conductor/scripts/tests/test-dag.sh` avec les cas T41 à T47 décrits en
`<behavior>`. Pour T41, réutiliser le patron d'isolation du bloc T33/T36-38 (copie de `dag.sh` + faux
`check-guard-health.sh` exécutable dans un répertoire ISOLÉ). Pour T42-T47, réutiliser le patron
d'isolation (copie de `dag.sh` + faux sibling `notify.sh` déposé dans un répertoire ISOLÉ du
`WORK_DIR`, jamais le vrai `notify.sh` de production sauf pour T42/T43/T46 où le VRAI script (posé
par 33-04) est copié puis instrumenté d'une ligne de journalisation ajoutée temporairement dans la
copie — jamais dans le fichier de production). Pour T42/T43/T46 spécifiquement : exporter
`VF_NOTIFY_FORCE_CHANNEL` (un canal forcé, ex. `linux`) ET `VF_NOTIFY_BIN_DIR` (répertoire de shims
créé par le test, préfixé au PATH de l'appel isolé, contenant UNIQUEMENT des exécutables factices qui
journalisent leur invocation — jamais le vrai `osascript`/`notify-send`/`powershell.exe` du système)
avant d'invoquer la copie isolée de `dag.sh` — patron déjà posé par `test-notify.sh` (33-04), à
réutiliser tel quel, pas réinventé ici.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -qE '^def check_stall_signal' plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -c 'check_stall_signal(check_guard_health_sh)' plugin/conductor/scripts/dag.sh` rend `2` (la définition ET l'unique site d'appel).
    - `grep -qE '^def record_milestone' plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -c 'record_milestone(notify_sh' plugin/conductor/scripts/dag.sh` rend `2` (la définition ET l'unique site d'appel).
    - Le filtre `done`/`failed` est bien présent en tête de fonction : `sed -n '/^def record_milestone/,/^def /p' plugin/conductor/scripts/dag.sh | grep -c '"done"'` rend au moins `1` et `grep -c '"failed"'` sur le même bloc rend au moins `1`.
    - `check_stall_signal()` N'A AUCUN filtre de statut : `sed -n '/^def check_stall_signal/,/^def /p' plugin/conductor/scripts/dag.sh | grep -c '"done"\|"failed"\|"running"'` rend `0`.
    - `grep -c 'shell=True' plugin/conductor/scripts/dag.sh` rend `0` (compte inchangé depuis 33-02).
    - L'appel passe une liste : `grep -c '\[notify_sh, title, body\]' plugin/conductor/scripts/dag.sh` rend `1` — SAUF si la tâche 1 a dû s'aligner sur une interface réelle différente de `notify.sh` (auquel cas ce critère est remplacé par son équivalent exact, consigné au SUMMARY avec le motif de l'écart).
    - L'appel à `check-guard-health.sh` passe une liste : `grep -c '\[check_guard_health_sh, "--hook"\]' plugin/conductor/scripts/dag.sh` rend `1`.
    - `grep -c 'timeout=2,' plugin/conductor/scripts/dag.sh` rend EXACTEMENT `4` (les deux appels de `record_progress`, posés par 33-02, PLUS l'appel de `check_stall_signal` et l'appel de `record_milestone`, posés par ce plan — pattern ANCRÉ sur la virgule qui suit : `dag.sh:183` porte déjà `timeout=20,`, dont `timeout=2` est un sous-motif non ancré — seuil affaibli mesuré par le 2ᵉ plancheck externe, fermé ici par l'ancrage).
    - Aucun candidat cwd-relatif : `grep -c 'os.getcwd' plugin/conductor/scripts/dag.sh` rend `0`.
    - `bash plugin/conductor/scripts/tests/test-dag.sh` sort 0, affiche les blocs `=== T41` à `=== T47`, et sa ligne de bilan affiche `0 FAIL`.
    - Les cas historiques (avant ce plan, incluant T1-T40 posés par 32/33-01/33-02) restent verts : comptage inchangé du nombre de PASS antérieur, consigné dans le SUMMARY.
    - Non-régression : `bash plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (33-01/33-02 non modifiés par ce plan, mais l'appel croisé au point `mark` doit rester intact).
    - Non-régression : `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` sort 0 (33-03 non modifié par ce plan, ce plan invoque son binaire `--hook` en sous-processus uniquement).
    - Non-régression informationnelle (fichier posé par le plan voisin de la même vague, présent par construction, pas une dépendance formelle de ce plan) : si `plugin/conductor/scripts/tests/test-notify.sh` existe, `bash` dessus sort 0.
    - **Portée-plan exacte** (pas portée-vague) : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD`
      (le `<SHA-de-base-de-ce-plan>` est le commit immédiatement AVANT le premier commit de CE plan,
      pas une plage arbitraire — à résoudre par l'exécutant via `git log` avant de lancer la commande)
      rend EXACTEMENT l'ensemble `{plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh, .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md}` (ou son sur-ensemble strict si un écart d'interface documenté au SUMMARY a forcé un fichier de plus) — **critère réellement falsifiable**, contrairement à une simple absence de `hooks.json` (trivialement vraie tant qu'on ne cherche pas ce fichier précis).
    - **`hooks.json` jamais touché** (corollaire du critère ci-dessus, gardé explicite car c'est la
      preuve WTCH-04) : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD | grep -c 'plugin/conductor/hooks/hooks.json'` rend `0`.
    - **`check-capability-activation.sh` jamais touché** : `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD | grep -c 'check-capability-activation.sh'` rend `0`.
    - **Ordre `check_stall_signal()`/`record_milestone()` après `save(dag)`** : dans le bloc
      `if action == "mark":` de `dag.sh`, le numéro de ligne de l'appel `check_stall_signal(` et celui
      de l'appel `record_milestone(` sont TOUS DEUX STRICTEMENT SUPÉRIEURS au numéro de ligne de
      l'appel `save(dag)` du même bloc, ET le numéro de ligne de `record_milestone(` est STRICTEMENT
      SUPÉRIEUR à celui de `check_stall_signal(` (vérifiable par `grep -n` sur les trois motifs dans la
      plage du bloc `mark`).
    - **Aucun vrai canal atteint en test** : `VF_NOTIFY_BIN_DIR`/`VF_NOTIFY_FORCE_CHANNEL` sont bien
      positionnés dans T42/T43/T46 (`grep -c 'VF_NOTIFY_FORCE_CHANNEL' plugin/conductor/scripts/tests/test-dag.sh` rend au moins `3`, un par cas) — et une exécution locale de `bash plugin/conductor/scripts/tests/test-dag.sh` ne fait apparaître AUCUNE notification système visible (vérification humaine ponctuelle sur macOS, consignée au SUMMARY).
    - **Mutation rouge n°1, trace obligatoire dans le SUMMARY** : commiter d'abord ce plan, puis retirer temporairement le `try/except Exception: return` englobant de `record_milestone()`, déposer un `notify.sh` isolé SANS droit d'exécution (`chmod 000`) dans le répertoire de test → `dag.sh mark --status=done` doit alors CRASHER (le mutant prouve que la garde protège réellement `mark` contre une exception de `subprocess.run`, ex. `PermissionError`) ; consigner assertion exacte/attendu/obtenu ; restaurer par `git checkout -- plugin/conductor/scripts/dag.sh`.
    - **Mutation rouge n°2** : retirer temporairement le `try/except Exception: return` englobant de `check_stall_signal()`, déposer un `check-guard-health.sh` isolé SANS droit d'exécution (`chmod 000`) dans le répertoire de test → `dag.sh mark` doit alors CRASHER ; consigner assertion exacte/attendu/obtenu ; restaurer.
    - **Contrôle positif des fixtures T41/T42/T43/T46** : avant d'invoquer la copie isolée de `dag.sh`, vérifier explicitement que le faux/instrumenté `check-guard-health.sh`/`notify.sh` déposé est bien exécutable et produit BIEN le comportement attendu quand invoqué directement (appel isolé, hors `dag.sh`, consigné dans le SUMMARY) — sinon ces cas seraient verts pour la mauvaise raison.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/dag.sh && bash plugin/conductor/scripts/tests/test-dag.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && bash plugin/conductor/scripts/tests/test-check-guard-health.sh && [ "$(grep -c 'shell=True' plugin/conductor/scripts/dag.sh)" -eq 0 ]</automated>
  </verify>
  <done>Chaque `dag.sh mark` relaie best-effort le verdict `check-guard-health.sh --hook` sur stderr (D-33-F, à chaque statut) ; chaque `dag.sh mark --status=done|failed` notifie via `notify.sh` en best-effort sans jamais bloquer le DAG ni ajouter d'entrée `hooks.json` ; `mark --status=running` ne notifie jamais via `notify.sh` (mais relaie quand même le stall) ; les deux mutations qui retirent la garde de dégradation font effectivement crasher `mark`.</done>
  <reversibility rating="reversible">Une fonction, un appel filtré, une variable sibling, purement additifs : un `git revert` rend `dag.sh` identique à HEAD, `notify.sh` et `hooks.json` restent hors de portée de ce plan.</reversibility>
</task>

<task type="docs" tdd="false">
  <name>Tâche 2 : recette de clôture Windows, rattachée au fil testeurs de l'issue #20 (D-33-C)</name>
  <files>.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md</files>
  <read_first>
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md §D-33-C (rattachement vérifié au fil testeurs de l'issue #20, trois zones NON PROUVÉES à ne pas combler)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-SPIKE-canal-notification.md §Windows, §NON PROUVÉ (contenu exact à citer, pas à paraphraser au point de le déformer)
  </read_first>
  <behavior>
    - Le document existe, cite explicitement « issue #20 » et le fil testeurs Windows.
    - Le document porte la mention explicite « condition de clôture, pas un gate dur ».
    - Le document reproduit les trois zones NON PROUVÉES du spike (chaîne Windows jamais exécutée, AUMID arbitraire vs AUMID PowerShell non tranché, latence non mesurée) sans en ajouter une quatrième non sourcée.
    - Le document ne prétend jamais qu'un toast Windows a été observé ou vérifié pendant cette phase — formulation explicite du type « non exécuté, aucune machine Windows disponible pendant le cadrage ni l'exécution ».
  </behavior>
  <action>
Écrire `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` :
un en-tête situant le document (condition de clôture de la Phase 33, pas un gate dur, D-33-C) ; le
rattachement au fil testeurs Windows de l'issue #20 (référence explicite, motif : mêmes testeurs,
même terrain que le geste `--dry-run` de l'issue #20) ; une recette de validation humaine minimale
(installer le lab sur Win10/11, provoquer un `mark --status=done` puis `--status=failed` réels sur
une mission de test, confirmer visuellement l'apparition d'un toast natif dans les deux cas, répéter
sous WSL) ; les trois zones NON PROUVÉES reprises telles quelles du spike, sans en inventer une
quatrième. Aucune tentative d'exécution réelle dans ce plan.
  </action>
  <acceptance_criteria>
    - Le fichier existe.
    - `grep -c 'issue #20' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
    - `grep -c 'condition de clôture' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
    - `grep -c 'NON PROUV' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` rend au moins `1`.
  </acceptance_criteria>
  <verify>
    <automated>[ -f .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md ] && grep -q 'issue #20' .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md</automated>
  </verify>
  <done>La recette de validation humaine Windows existe comme document rattaché au fil testeurs de l'issue #20, explicitement non exécutée, sans zone NON PROUVÉE comblée par supposition.</done>
  <reversibility rating="reversible">Un seul fichier markdown nouveau, aucune dépendance de code : supprimable sans effet de bord.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dag.sh` -> `check-guard-health.sh` (sous-processus) | sibling résolu par répertoire de script, jamais par cwd (même motif que 33-02 pour `driver-lock.sh`) ; la sortie `--hook` est relayée TELLE QUELLE, jamais interprétée — `dag.sh` ne connaît aucun seuil ; l'appel ne dépend jamais du résultat pour la réussite de `mark` |
| `dag.sh` -> `notify.sh` (sous-processus) | sibling résolu par répertoire de script, jamais par cwd (même motif que 33-02 pour `driver-lock.sh`) ; l'appel ne dépend jamais du résultat pour la réussite de `mark` |
| `check-guard-health.sh`/`notify.sh` (indisponible/lent/interface divergente) -> `dag.sh mark` | un sibling absent, non exécutable, en échec, à l'interface inattendue, ou qui pend ne doit jamais pouvoir bloquer ni faire échouer la mise à jour du DAG |

## STRIDE Threat Register

Numérotation `T-33-20` à `T-33-23` pour `check_stall_signal()` (D-33-F, déplacé depuis 33-02, dont
la menace DoS d'origine — `T-33-11` — a été retirée par 33-02 lors de la 2ᵉ recoordination, laissant
la place au bloc de 33-04) ; `T-33-16` à `T-33-19` pour `record_milestone()` (bloc déjà réservé à ce
plan, inchangé) — `T-33-05` à `T-33-19` sont pris par 33-01/33-02/33-03/33-04 pour des menaces
différentes, écriture en parallèle ; T-33-20+ est la première plage libre après relecture de
l'ensemble des cinq plans sur le disque.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-16 | Denial of Service | `record_milestone()` | high | mitigate | `timeout=2` (marge sous les 5 s de `kill -9` externe de `run_bounded`) + `stdout/stderr=DEVNULL` (pas de pipe hérité) + `try/except Exception` englobant, appel placé APRÈS `save(dag)` : un `notify.sh` qui pend ou plante ne bloque jamais `mark` ET ne fait jamais perdre une transition déjà persistée ; cas T46 + mutation rouge n°1 |
| T-33-17 | Tampering | résolution du sibling `NOTIFY_SH` | medium | mitigate | résolution par répertoire du script `dag.sh` lui-même, jamais par cwd — même motif que 33-02/D-07, non rouvert ; cas T47 (grep statique) |
| T-33-18 | Injection | arguments du sous-processus `notify.sh` | low | accept | `subprocess.run` avec liste d'arguments, jamais `shell=True` ; `nid`/`status` proviennent du DAG local, pas d'une entrée réseau |
| T-33-19 | Elevation of Privilege | armement hors gate | medium | mitigate | aucune entrée `hooks.json` ajoutée par ce plan — l'appel est un sous-processus programmatique, jamais un hook Claude Code, donc rien à faire passer PORT-05 qui ne le soit déjà ; vérifié par diff nul sur `hooks.json` |
| T-33-20 | Denial of Service | `check_stall_signal()` | high | mitigate | même patron que T-33-05 (33-02) : `timeout=2` + `try/except Exception` englobant, appel placé APRÈS `save(dag)` : un `check-guard-health.sh` cassé ou lent ne bloque jamais `mark` ; cas T41 + mutation rouge n°2 |
| T-33-21 | Tampering | résolution du sibling `CHECK_GUARD_HEALTH_SH` | medium | mitigate | résolution par répertoire du script `dag.sh` lui-même, jamais par cwd — même motif que `NOTIFY_SH` (T-33-17)/D-07, non rouvert ; cas T47 (grep statique) |
| T-33-22 | Injection | arguments du sous-processus `check-guard-health.sh` | low | accept | `subprocess.run` avec liste d'arguments, jamais `shell=True` ; aucune valeur externe interpolée dans l'appel (`--hook` seul, aucun argument variable) |
| T-33-23 | Information Disclosure | relais stderr du verdict `--hook` | low | accept | la ligne relayée provient d'un lock déjà lisible par tout process du même utilisateur (même surface que T-33-08, 33-03) ; `dag.sh` ne fait qu'un `print` fidèle, aucune donnée supplémentaire n'est exposée |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/dag.sh`
2. `bash plugin/conductor/scripts/tests/test-dag.sh` — 0 FAIL, cas T41 à T47 présents, historique intact
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé en écriture
3bis. `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` — non-régression du protocole consommé en lecture (dépendance sur 33-03)
4. `git diff --name-only <SHA-de-base-de-ce-plan>..HEAD` (le commit immédiatement AVANT le premier
   commit de ce plan, résolu via `git log` par l'exécutant — jamais une plage devinée) liste
   EXACTEMENT les fichiers de `files_modified` (ou son sur-ensemble strict documenté au SUMMARY), et
   ne liste JAMAIS `plugin/conductor/hooks/hooks.json` ni `check-capability-activation.sh`
5. Les deux mutations exigées ont rougi et leurs traces (assertion, attendu, obtenu) sont dans le SUMMARY
6. `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CLOTURE-WINDOWS.md` existe et cite l'issue #20
7. Non-régression du parc COMPLET des suites : `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` rend `65`, INCHANGÉ par rapport à l'état laissé par 33-04 (ce plan étend `test-dag.sh`, fichier déjà découvert ; le seul fichier neuf de la phase, `test-notify.sh`, a été ajouté par 33-04, pas ici). Le passage exhaustif des 65 suites reste la responsabilité de CI (`ci.yml` L213) ; ce plan ne le rejoue pas intégralement en local.
</verification>

<success_criteria>
- `dag.sh mark` relaie best-effort le verdict `check-guard-health.sh --hook` sur stderr, APRÈS `save(dag)`, à CHAQUE statut (WTCH-02, D-33-F) — un stall ne survit pas au prochain geste `mark` d'une session vivante, pas seulement à `SessionStart`.
- `dag.sh mark --status=done|failed` notifie via `notify.sh` en best-effort, l'appel placé APRÈS `save(dag)` ET après le relais de stall ; `mark --status=running` ne notifie jamais via `notify.sh` (WTCH-03 critère n°3).
- « Halt condition » est couvert par ce plan comme `mark --status=failed` — limite assumée et documentée : `mark --status=blocked` reste un signal de halte valide côté doctrine (`mission-flow.md:245`) mais HORS du filtre de notification de ce plan (voir truths, correction de prémisse) ; aucune dépendance à un module hors `conductor` pour ce que ce plan couvre.
- Aucune entrée `hooks.json` ajoutée, aucune édition de `check-capability-activation.sh` — WTCH-04 fermé par non-régression, pas par extension du gate (D-33-D).
- La recette de clôture Windows existe, rattachée au fil testeurs de l'issue #20, explicitement non exécutée (D-33-C).
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-05-SUMMARY.md` en fin de plan,
en y consignant la trace de mutation exigée, le comptage avant/après des assertions de la suite, et
tout écart entre l'interface assumée de `notify.sh` et son interface réelle constatée sur disque.
</output>

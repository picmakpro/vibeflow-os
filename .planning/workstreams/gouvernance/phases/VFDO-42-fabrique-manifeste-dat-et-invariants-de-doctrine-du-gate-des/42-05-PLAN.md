---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 05
type: execute
wave: 3
depends_on: ["42-04", "42-02", "42-03"]
files_modified:
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
autonomous: true
requirements: [FABR-03, FABR-05]

must_haves:
  truths:
    - "I1 (D-06) : vf-internal: true sans la sous-chaîne « Worker interne » dans description:, ou l'inverse, fait sortir le gate en rc=1 avec « invariant I1 » — les deux sens"
    - "I4 : un jeton de disallowedTools porteur d'un spécifieur (parenthèse) fait sortir le gate en rc=1 avec « invariant I4 »"
    - "I5 (D-08) : un juge — disallowedTools retire Write ET Edit ET aucune allowlist Agent(...)/Task(...) non vide — sans omitClaudeMd: true fait sortir le gate en rc=1 avec « invariant I5 » ; un agent qui porte une allowlist non vide (forme de vf-reviewer, vf-auditer) n'est pas un juge"
    - "I6 (D-07) : un manager — allowlist Agent(...)/Task(...) non vide ET non vf-internal — sans SendMessage dans tools: fait sortir le gate en rc=1 avec « invariant I6 » ; un agent vf-internal porteur d'une allowlist, ou un Agent nu, n'est pas un manager"
    - "I7 : un champ vf-mcp-* sans vf-requires citant mcp-servers fait sortir le gate en rc=1 avec « invariant I7 »"
    - "Les invariants sont des ERREURS dans tous les modes (défaut, --strict, --file, affichées sous --hook), jamais des avertissements (D-11) — check-blueprints.sh, qui appelle le gate en mode par défaut, les voit donc aussi"
    - "Chaque invariant naît avec son jumeau négatif (fixture rouge qui ne viole QUE lui + jumeau vert) et un mutant QUAL-01 tué (MUT-I1, MUT-I4, MUT-I5, MUT-I6, MUT-I7) ; I1, I5, I6, I7 ont en plus une mutation sur un agent réel du dépôt, prouvée par cmp, rouge puis verte une fois restaurée"
    - "Corpus réel : les six plugin/*/agents et les six plugin/*/AGENT.md passent --strict avec les invariants armés, sans aucun diagnostic « invariant I » ; les neuf blueprints passent check-blueprints.sh (T96)"
    - "Aucun cas préexistant n'est affaibli : toute fixture préexistante qui viole un invariant neuf reçoit le champ exigé, son rc attendu et ses assertions restent identiques"
    - "analyze_token n'est jamais rappelé pour classer un agent : parse_token pur alimente allowlist_agents, aucune erreur de syntaxe d'allowlist n'est dupliquée (T26 à T41 inchangés)"
  artifacts:
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "parse_token, allowlist_agents, invariant_i1, invariant_i4, invariant_i5, invariant_i6, invariant_i7"
      contains: "def invariant_i6"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "T91 à T96, juger_mutation_reelle, MUT-I1, MUT-I4, MUT-I5, MUT-I6, MUT-I7"
      contains: "MUT-I5"
  key_links:
    - from: "check_file() de check-agents.sh"
      to: "invariant_i1 à invariant_i7 (locaux)"
      via: "une ligne d'appel unique par invariant, errors.extend(invariant_iN(...)), cible des mutants"
      pattern: 'errors\.extend\(invariant_i[14567]\('
    - from: "plugin/conductor/scripts/check-blueprints.sh"
      to: "plugin/conductor/scripts/check-agents.sh"
      via: "frontmatter cible matérialisé puis soumis au gate en mode par défaut : les invariants y sont des erreurs"
      pattern: "check-agents.sh"
  prohibitions:
    - statement: "MUST NOT rendre la CI verte en désarmant, rétrogradant en avertissement, exemptant par liste ou contournant un invariant I1-I7, au lieu de corriger le corpus"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans descripteur câblé ; les mutants QUAL-01 prouvent que chaque invariant mord, pas qu'aucune exemption future ne sera ajoutée — relecture en PR"
---

<objective>
Armer en ERREUR les cinq invariants locaux de la spec §4 (I1, I4, I5, I6, I7), selon les définitions
retenues par le cadrage : I1 lu sur le marqueur « Worker interne » de `description:` dans les deux
sens (D-06), le manager d'I6 restreint aux non-internes (D-07), le juge d'I5 défini par
`disallowedTools` et l'absence d'allowlist (D-08) — trois écarts assumés par rapport à la spec §4,
écrits dans l'en-tête du gate. Pas de période d'avertissement (D-11) : le corpus a été corrigé en
42-02 et 42-03, il doit passer tel quel. I2 et I3 (monde fermé) sont en 42-06.

Purpose: FABR-03 (I1, I4 à I7, chacun avec son jumeau négatif et sa mutation prouvée rouge) et
FABR-05 (corpus conforme avec les invariants armés).
Output: cinq fonctions d'invariant, l'analyse pure des allowlists, T91 à T96, cinq mutants tués.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-04-SUMMARY.md

Porteurs réels utilisés par les mutations sur l'arbre (lecture seule, jamais modifiés en place) :
`plugin/mobile-test-team/agents/vf-test-runner.md` (I1, I7),
`plugin/content-bundle/agents/content-clarity-judge.md` (I5),
`plugin/business-pilot-bundle/agents/vf-business-manager.md` (I6).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : invariants I1, I4, I7 armés en erreur — jumeaux négatifs, mutations réelles, mutants MUT-I1, MUT-I4, MUT-I7 (T91, T92, T95)</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : parse_frontmatter, extract_raw_field, tokenize_field, bare_tokens, check_file jusqu'à la boucle « champ inconnu » ; en-tête BLOQUANT/WARNING)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : helpers mk_gate_dir, make_gate_mutant, okmut, komut de 42-01/42-04 ; T75 : patron de mutation sur l'arbre réel avec cmp ; fixtures préexistantes portant vf-internal, disallowedTools ou vf-mcp-*)
    - plugin/mobile-test-team/agents/vf-test-runner.md (porteur réel de I1 et I7)
    - plugin/dev-orchestrator/scripts/check-capability-activation.sh (l.500-510 : vf-mcp-consumer et vf-mcp-tools exigent l'identifiant mcp-servers — même jointure que I7)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-06, D-11)
    - docs/superpowers/specs/2026-09-22-fabrique-agents-skills-design.md (§4, tableau I1-I8)
  </read_first>
  <behavior>
    - T91 (I1) : `vf-internal: true` + description sans « Worker interne » → rc=1 « invariant I1 » ; description avec « Worker interne » sans vf-internal → rc=1 « invariant I1 » ; les deux présents → rc=0 ; aucun des deux → rc=0 ; mutation réelle : copie de vf-test-runner.md dont la description perd « interne » après « Worker » → rc=1 « invariant I1 », copie restaurée → rc=0, mutation prouvée par cmp
    - T91b (I1, D-18) : `vf-internal: true` + description à DEUX dispatcheurs nommés, forme exacte de vf-test-orchestrator (« … Worker interne — dispatché par vf-dev-manager ou par le mode autonome (vf-auto) sur ce type de projet, pas en usage direct. ») → rc=0, aucun « invariant I1 » — I1 ne lit que la présence du marqueur « Worker interne » dans description:, jamais le nombre ni l'identité des dispatcheurs nommés (D-06 tolère la formulation à deux dispatcheurs de D-18) ; jumeau négatif : même description à deux dispatcheurs SANS `vf-internal: true` → rc=1 « invariant I1 »
    - T92 (I4) : `tools: Read, Write, Edit, Bash` + `disallowedTools: Bash(rm:*)` → rc=1 « invariant I4 » ; `disallowedTools: Bash` → rc=0 (aucun porteur réel : fixture seule)
    - T95 (I7) : `vf-mcp-consumer: true` sans vf-requires → rc=1 « invariant I7 » ; `vf-requires: autre-chose` → rc=1 ; `vf-mcp-tools: XcodeBuildMCP:build_sim` + `vf-requires: mcp-servers` → rc=0 ; mutation réelle : copie de vf-test-runner.md sans sa ligne vf-requires → rc=1 « invariant I7 », restaurée → rc=0
    - MUT-I1, MUT-I4, MUT-I7 : la ligne d'appel `errors.extend(invariant_iN(` remplacée par `pass` → la fixture rouge de l'invariant : rc_original=1, rc_mutant=0
  </behavior>
  <action>
1. Tests d'abord : T91, T91b, T92, T95 et les trois mutants, joués ROUGES contre le gate de 42-04.
   - T91b (D-18) : fixture composée à partir de la description RÉELLE de vf-test-orchestrator posée
     en 42-02 (deux dispatcheurs nommés) — preuve que le gate tolère cette formulation sans qu'aucun
     code n'ait besoin de connaître le nombre de dispatcheurs ; son jumeau négatif retire uniquement
     `vf-internal: true`, rien d'autre.
   - Helper `juger_mutation_reelle <id> <copie-originale> <copie-mutée> <jeton>` (patron T75) :
     refus « NON OPPOSABLE » si `cmp` dit les deux copies identiques ; gate (`$CHECK --file`, mode
     par défaut) sur la copie mutée → rc=1 et jeton présent ; sur la copie originale → rc=0. Les
     copies vivent sous `$WORK`, jamais un fichier du dépôt modifié en place ; même nom de fichier que
     le porteur réel.
   - Chaque fixture rouge ne viole QUE son invariant (sinon un mutant ne pourrait pas la rendre
     verte) : la fixture I4 garde Write et Edit dans `tools:` pour ne tomber ni dans la classe « juge »
     ni sous la règle memory: de la Phase 20.
   - Mutants : `make_gate_mutant I1 0 'errors.extend(invariant_i1(' 'pass'`, idem I4 et I7 ; issue
     tracée par `okmut`/`komut` (rc exacts, aucun « Traceback »).
   - Catalogue d'en-tête : T91, T91b, T92, T95, MUT-I1, MUT-I4, MUT-I7 sous « Invariants de doctrine
     (Phase 42, FABR-03, D-06 à D-11) ».
2. Gate, dans le bloc Python (mêmes règles d'échappement) :
   - `invariant_i1(base, fm)` : interne si la valeur de `vf-internal` vaut « true » ; marqueur si la
     description (chaîne, ou liste jointe) contient « Worker interne ». Interne sans marqueur →
     « {base} : invariant I1 — vf-internal: true sans le marqueur « Worker interne » dans description:
     (D-06) » ; marqueur sans interne → « {base} : invariant I1 — description: porte « Worker interne »
     sans vf-internal: true (D-06) ». Rend une liste de messages.
   - `invariant_i4(base, fmlines)` : jetons de `disallowedTools` via extract_raw_field puis
     tokenize_field (liste vide si le champ manque ou si la profondeur de parenthèses est non nulle —
     l'erreur de syntaxe est déjà levée ailleurs) ; tout jeton contenant une parenthèse → « {base} :
     invariant I4 — disallowedTools porte un spécifieur '<jeton>' : il retire l'outil ENTIER, il ne
     le restreint pas ».
   - `invariant_i7(base, fm)` : si une clé commence par `vf-mcp-`, `vf-requires` (chaîne ou liste,
     découpée sur virgules et espaces) doit contenir l'identifiant `mcp-servers` — même jointure que
     la règle 4 de check-capability-activation.sh — sinon « {base} : invariant I7 — <clé> sans
     vf-requires citant mcp-servers ».
   - Dans check_file, après le lint des champs tools:/disallowedTools:, une ligne d'appel UNIQUE par
     invariant, de la forme `errors.extend(invariant_iN(...))` (cibles des mutants — jamais
     factorisées dans une boucle). Toujours des erreurs, quel que soit --strict (D-11).
   - En-tête : section « INVARIANTS DE DOCTRINE (Phase 42, spec fabrique §4) — BLOQUANTS dans tous
     les modes » avec I1, I4, I7, et l'écart assumé d'I1 (marqueur lu dans description:, pas dans le
     corps — D-06).
3. Remise en conformité des fixtures préexistantes (T1 à T90) : entourer temporairement `run_check`
et les appels directs au gate d'un journal (sortie copiée dans `$WORK/trace-invariants.log`), jouer
la suite, relever tout cas antérieur à T91 dont la sortie contient « invariant I », lui ajouter le
champ exigé (marqueur ou vf-internal, vf-requires: mcp-servers…) SANS changer son rc attendu ni ses
assertions — y compris quand le cas reste vert, pour qu'aucun rc=1 attendu ne soit satisfait par un
invariant étranger au cas — puis retirer le journal. Lister au SUMMARY chaque cas touché et
l'invariant en cause.
4. Commit : `feat(conductor): invariants I1, I4, I7 du gate des agents, armés en erreur (FABR-03)`
avec les trailers
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — invariants I1, I4, I7 en erreur (D-06, D-11)`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T91, T92, T95 et mutants MUT-I1, MUT-I4, MUT-I7`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(91|91b|92|95)|MUT-I[147]|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T91, T92, T95 n'a aucune ligne « ✓ », ou l'une des lignes « ✓ MUT-I1 TUE », « ✓ MUT-I4 TUE », « ✓ MUT-I7 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'errors.extend(invariant_i1(' plugin/conductor/scripts/check-agents.sh`, idem `invariant_i4(` et `invariant_i7(`, rendent chacun 1
    - la sortie de test-check-agents.sh contient `✓ MUT-I1 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1`, et les lignes équivalentes pour MUT-I4 et MUT-I7
    - la sortie contient au moins une ligne `✓ T91`, `✓ T91b`, `✓ T92`, `✓ T95` et finit sur `0 KO ==`
    - `grep -c 'INVARIANTS DE DOCTRINE' plugin/conductor/scripts/check-agents.sh` rend au moins 1
    - le SUMMARY liste les fixtures préexistantes remises en conformité (ou « aucune »)
  </acceptance_criteria>
  <done>I1, I4 et I7 sont des erreurs dans tous les modes ; chacun a sa fixture rouge, son jumeau vert, son mutant tué ; I1 et I7 rougissent aussi sur un agent réel muté puis reverdissent restauré ; aucune fixture préexistante n'est affaiblie.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 2 : checkpoint D-19/D-08 — arbitrage de Samuel avant tout armement d'I5 (mission, nœud revise-42b, B1/B2)</name>
  <read_first>
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-D19-MESURE.md (verdict D-19, 2026-09-24 : omitClaudeMd retire aussi les règles .claude/rules/*.md — « règles OMISES » — conséquence prévue : D-08 réexaminée avec Samuel)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-08, D-19)
  </read_first>
  <files>(aucun fichier de code touché par cette tâche — elle transcrit une décision humaine dans 42-D19-MESURE.md)</files>
  <action>
D-19 a rendu son verdict le 2026-09-24 : « règles omises ». Sa conséquence prévue est déclenchée —
D-08 (la définition du juge de l'invariant I5 : `disallowedTools` retire Write/Edit ET aucune
allowlist `Agent(...)`) doit être réexaminée avec Samuel avant que la pose d'`omitClaudeMd` sur les
quatre juges (`quality-gate-client`, `content-clarity-judge`, `growth-quality-judge`,
`vf-design-judge`) et l'armement d'`invariant_i5` puissent avancer. L'escalade est partie le
2026-09-24 (42-D19-MESURE.md) ; la réponse n'est pas encore reçue au moment de cette révision.

Ce que ce checkpoint attend, dans EXACTEMENT cette forme, ajoutée à la fin de
`42-D19-MESURE.md` (section `## Arbitrage D-08 (réponse à la réexamen D-19)`) :

```
**Décision :** maintenir
**Canal :** <canal, ex. WhatsApp>
**Date :** <AAAA-MM-JJ>
```

où `**Décision :**` vaut EXACTEMENT `maintenir` (D-08 tient telle quelle, les quatre juges reçoivent
`omitClaudeMd: true`, I5 s'arme comme conçu) ou `renoncer` (D-08 est redéfinie — cette redéfinition
n'est PAS de l'autorité de ce plan : un mandat de cadrage frais est nécessaire, ce plan ne l'invente
pas). La simple présence du fichier ne suffit PAS — c'est la ligne d'arbitrage, sous cette forme
exacte à trois champs, qui fait foi (la sonde `D19-LU` d'une révision précédente passait à vide sur
la seule présence du fichier, corrigé ici).
  </action>
  <what-built>
`42-D19-MESURE.md` porte déjà le verdict de mesure (règles omises) et l'annonce de l'escalade. Il ne
manque que la transcription de la réponse de Samuel, sous la forme fixe ci-dessus, pour que la
Tâche 3 (armement d'I5) puisse s'exécuter mécaniquement.
  </what-built>
  <how-to-verify>
    1. Obtenir la réponse de Samuel sur le réexamen de D-08 (canal WhatsApp, comme le reste des
       arbitrages de cette phase), et l'ajouter à `42-D19-MESURE.md` sous la forme à trois champs
       ci-dessus — jamais reformulée, jamais résumée.
    2. Exécuter la sonde machine (jamais une lecture visuelle seule) :
       ```
       python3 -c "
       import re
       p = '.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-D19-MESURE.md'
       try:
           s = open(p, encoding='utf-8').read()
       except FileNotFoundError:
           print('ARBITRAGE-ABSENT'); raise SystemExit
       m = re.search(r'\*\*D.cision\s*:\*\*\s*(maintenir|renoncer)\b.*?\*\*Canal\s*:\*\*\s*(\S.*?)\s*\n.*?\*\*Date\s*:\*\*\s*(\d{4}-\d{2}-\d{2})', s, re.S)
       print('ARBITRAGE-' + m.group(1).upper() if m else 'ARBITRAGE-ABSENT')
       "
       ```
    3. Constater la sortie : `ARBITRAGE-MAINTENIR`, `ARBITRAGE-RENONCER` ou `ARBITRAGE-ABSENT`. C'est
       cette sortie exacte, pas une paraphrase, que la Tâche 3 relit comme précondition.
  </how-to-verify>
  <resume-signal>Coller la sortie brute de la sonde (`ARBITRAGE-MAINTENIR`, `ARBITRAGE-RENONCER` ou
`ARBITRAGE-ABSENT`) — un `ARBITRAGE-ABSENT` laisse la Tâche 3 bloquée sur son I5 (I6 y avance déjà
sans dépendre de ce checkpoint), jamais un passage silencieux à la suite.</resume-signal>
  <acceptance_criteria>
    - **Comportement** : la ligne d'arbitrage ajoutée à `42-D19-MESURE.md` porte les trois champs
      `**Décision :**`, `**Canal :**`, `**Date :**` dans cette forme exacte — jamais une prose libre
      que la sonde ne peut pas parser.
    - **Comportement** : la sonde tourne réellement (sortie collée dans le resume-signal), jamais une
      confirmation verbale non vérifiée.
    - **Comportement** : un `ARBITRAGE-RENONCER` n'invente aucune redéfinition de D-08 dans ce
      checkpoint — il consigne seulement le verdict, la redéfinition reste un geste de cadrage séparé.
  </acceptance_criteria>
  <done>La réponse de Samuel au réexamen de D-08 est transcrite dans `42-D19-MESURE.md` sous la forme
à trois champs, et la sonde machine rend `ARBITRAGE-MAINTENIR` ou `ARBITRAGE-RENONCER` — ou reste
`ARBITRAGE-ABSENT` si la réponse n'est toujours pas arrivée, auquel cas la Tâche 3 reste partiellement
bloquée sur son volet I5.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : invariants I5 et I6 sur une analyse pure des allowlists — I6 inconditionnel, I5 gated par le checkpoint D-19 (T93, T94, T96, MUT-I5, MUT-I6)</name>
  <precondition>Précondition RE-EXÉCUTÉE (jamais relue en prose seule — une précondition en phrase ne
peut pas faire échouer un exécuteur mécanique, seule une assertion exécutée le peut) : rejouer la
sonde de la Tâche 2 (`ARBITRAGE-MAINTENIR` / `ARBITRAGE-RENONCER` / `ARBITRAGE-ABSENT`) AVANT
d'écrire la moindre ligne de cette tâche. Trois branches :
- `ARBITRAGE-MAINTENIR` → I5 ET I6 s'exécutent intégralement, comme décrit ci-dessous (D-08 tient
  telle quelle) — poser `omitClaudeMd: true` sur les quatre juges (un commit + bump patch par module,
  cf. `<action>` étape 0), puis armer `invariant_i5`.
- `ARBITRAGE-RENONCER` → I6 s'exécute intégralement (indépendant de D-08) ; I5 s'ARRÊTE ici — ne PAS
  poser `omitClaudeMd`, ne PAS écrire `invariant_i5` selon la définition D-08 actuelle. La
  redéfinition de D-08 n'est PAS improvisée par cette tâche : elle exige un mandat de cadrage séparé
  (ce plan s'arrête sur son volet I5 et le signale comme `human_needed`, pas comme un échec).
- `ARBITRAGE-ABSENT` → I6 s'exécute intégralement (indépendant de D-08) ; I5 reste EN ATTENTE dans son
  état actuel (aucun champ posé, `invariant_i5` non armé) — la tâche n'est marquée `<done>` sur son
  volet I5 qu'une fois `ARBITRAGE-MAINTENIR` ou `ARBITRAGE-RENONCER` obtenu à une exécution
  ultérieure.
Dans les trois branches, I6 (`allowlist_agents`, `invariant_i6`, T94, MUT-I6, la remise en conformité
des fixtures manager) n'est JAMAIS retenu en otage d'un arbitrage sur D-08 : c'est le principe de
l'item (a) de la mission `revise-42b`.</precondition>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh, plugin/business-pilot-bundle/agents/quality-gate-client.md, plugin/business-pilot-bundle/VERSION, plugin/business-pilot-bundle/module.json, plugin/business-pilot-bundle/CHANGELOG.md, plugin/business-pilot-bundle/README.md, plugin/content-bundle/agents/content-clarity-judge.md, plugin/content-bundle/VERSION, plugin/content-bundle/module.json, plugin/content-bundle/CHANGELOG.md, plugin/content-bundle/README.md, plugin/growth-bundle/agents/growth-quality-judge.md, plugin/growth-bundle/VERSION, plugin/growth-bundle/module.json, plugin/growth-bundle/CHANGELOG.md, plugin/growth-bundle/README.md, plugin/design-orchestrator/agents/vf-design-judge.md, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/CHANGELOG.md, plugin/design-orchestrator/README.md</files>
  <read_first>
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-D19-MESURE.md (verdict D-19 + arbitrage transcrit par la Tâche 2 — précondition bloquante de cette tâche, lu en premier)
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : analyze_token — il AJOUTE ses erreurs de syntaxe à errors, donc ne doit jamais être rappelé pour classer ; lint_tool_field ; bare_tokens ; règle memory: de la Phase 20 ; appels d'invariants posés à la tâche 1)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : T25 à T41 — messages de syntaxe exacts à préserver ; fixtures préexistantes porteuses d'Agent(...) ou de disallowedTools: Write, Edit ; T69/T70)
    - plugin/business-pilot-bundle/agents/vf-business-manager.md (porteur réel de I6, corrigé en 42-02 ; quality-gate-client.md, dans ce même module, n'a PAS encore reçu omitClaudeMd — c'est cette tâche qui le pose si ARBITRAGE-MAINTENIR)
    - plugin/dev-orchestrator/agents/vf-reviewer.md et vf-auditer.md (forme « disallowedTools: Write, Edit + Agent(...) » : hors classe juge, D-08)
    - plugin/conductor/scripts/check-blueprints.sh (appelle le gate en mode par défaut)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07 ratifiée par Samuel WhatsApp 2026-09-23, D-08 ratifiée par Samuel WhatsApp 2026-09-23 puis réexaminée par D-19, D-11)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Pattern 4, § Don't Hand-Roll)
  </read_first>
  <behavior>
    - T93 (I5, seulement si ARBITRAGE-MAINTENIR) : `tools: Read, Glob, Grep` + `disallowedTools: Write, Edit` + memory: project, sans omitClaudeMd → rc=1 « invariant I5 » ; même agent avec `omitClaudeMd: true` → rc=0 ; forme vf-reviewer (vf-internal + marqueur « Worker interne », `Agent(outil-tiers)` dans tools, disallowedTools Write, Edit, sans omitClaudeMd) → rc=0 et aucun « invariant I5 » ; mutation réelle : copie de content-clarity-judge.md (après pose du champ par cette tâche) sans sa ligne omitClaudeMd → rc=1 « invariant I5 », restaurée → rc=0
    - T94 (I6, TOUJOURS) : `tools: Read, Write, Agent(worker-a, worker-b)` non interne sans SendMessage → rc=1 « invariant I6 » ; avec SendMessage → rc=0 ; même allowlist avec vf-internal + marqueur → aucun « invariant I6 » ; `Agent` nu sans SendMessage → aucun « invariant I6 » ; mutation réelle : copie de vf-business-manager.md sans le jeton SendMessage → rc=1 « invariant I6 », restaurée → rc=0
    - T96 (corpus, adapté à la branche) : pour chacun des six plugin/*/agents (au moins 6 découverts, sinon ko anti-vert-à-vide) `$CHECK --strict --skills-dir=<chemin inexistant> --agents-dir=<dossier>` → rc=0 sans « invariant I6 » (et sans « invariant I5 » SEULEMENT si ARBITRAGE-MAINTENIR a posé le champ sur les quatre juges — sinon invariant_i5 n'est pas encore armé et ce sous-critère est sans objet) ; pour chaque plugin/*/AGENT.md en --file --strict → rc=0 ; `bash $SCRIPTS_DIR/check-blueprints.sh` → rc=0
    - MUT-I6 (TOUJOURS), MUT-I5 (seulement si ARBITRAGE-MAINTENIR) : ligne d'appel `errors.extend(invariant_i6(` (et `invariant_i5(` si armé) remplacée par `pass` → fixture rouge correspondante : rc_original=1, rc_mutant=0
    - T26 à T41 inchangés : aucune erreur de syntaxe d'allowlist dupliquée
  </behavior>
  <action>
0. **SEULEMENT SI `ARBITRAGE-MAINTENIR`** — poser `omitClaudeMd: true` juste après `memory: project`
sur les quatre juges (`quality-gate-client.md`, `content-clarity-judge.md`, `growth-quality-judge.md`,
`vf-design-judge.md`), rien d'autre dans leur frontmatter, corps inchangés (cmp), colonne
d'instructions inchangée. UN commit par module, avec bump patch (item d de la mission : ce SECOND
bump ne fusionne PAS avec le commit SendMessage déjà posé en 42-02/42-03 — deux décisions gated par
des conditions différentes, deux commits distincts, légitimement) :
`fix(business-pilot-bundle vX.Y.Z): juge sans CLAUDE.md injecté (invariant I5, arbitrage D-08 : maintenir)`
et de même pour `content-bundle`, `growth-bundle`, `design-orchestrator` — CHANGELOG de chaque module,
entrée en tête citant l'arbitrage (canal + date transcrits par la Tâche 2), jamais « arbitrage » nu
sans sa source. Si `ARBITRAGE-RENONCER` ou `ARBITRAGE-ABSENT` : sauter cette étape entièrement, aucun
champ posé, aucun commit sur ces quatre fichiers.
1. Tests d'abord : T94, T96 (volet I6), MUT-I6, joués ROUGES, puis implémentation d'I6. Si
`ARBITRAGE-MAINTENIR` : T93, MUT-I5 aussi, joués ROUGES après l'étape 0. Mutations réelles par
`juger_mutation_reelle` (tâche 1). T96 passe `--skills-dir` vers un chemin inexistant, comme la CI où
`.claude/skills` n'existe pas (l'existence des skills n'est alors pas jugée).
2. Analyse pure (§ Don't Hand-Roll : aucun second tokenizer) : extraire d'`analyze_token` un cœur
`parse_token(tok)` SANS effet de bord, qui rend `(nom, noms_agents, message_ou_None)` ; `analyze_token`
devient une enveloppe qui appelle `parse_token` et ajoute le message à `errors` sous la forme EXACTE
d'aujourd'hui (« {base} : {field} — … » — T26, T27, T37 à T41 assertent ces textes). Puis
`allowlist_agents(fmlines)` : jetons du champ `tools:` via extract_raw_field et tokenize_field (liste
vide si le champ manque ou si la profondeur est non nulle), et pour chaque jeton sans message
d'erreur dont le nom est dans `AGENT_TOOL_NAMES` avec une allowlist non vide, ajoute ses noms.
3. `invariant_i6(base, fm, fmlines, dispatch)` (TOUJOURS écrit) : manager si `dispatch` non vide ET
vf-internal ne vaut pas « true » (D-07) ; manager sans `SendMessage` dans `bare_tokens(fmlines,
"tools")` → « {base} : invariant I6 — manager (allowlist Agent(...) non vide, non vf-internal) sans
SendMessage dans tools: (D-07) ». Dans check_file : `dispatch = allowlist_agents(fmlines)` puis une
ligne d'appel unique `errors.extend(invariant_i6(...))`.
`invariant_i5(base, fm, fmlines, dispatch)` (SEULEMENT SI `ARBITRAGE-MAINTENIR`) : juge si `Write` et
`Edit` sont dans `bare_tokens(fmlines, "disallowedTools")` ET `dispatch` est vide (D-08) ; juge sans
omitClaudeMd valant « true » → « {base} : invariant I5 — juge (disallowedTools retire Write et Edit,
aucune allowlist Agent(...)) sans omitClaudeMd: true — un regard frais ne charge pas la doctrine
(D-08) ». Ligne d'appel unique `errors.extend(invariant_i5(...))`. Si `ARBITRAGE-RENONCER` ou
`ARBITRAGE-ABSENT` : NE PAS écrire `invariant_i5` ni sa ligne d'appel — le laisser pour une exécution
ultérieure de cette même tâche.
4. En-tête : compléter la section des invariants avec I6 (toujours), et I5 seulement si armé — écrire
les deux écarts assumés par rapport à la spec §4 — I5 (juge sans allowlist : `vf-reviewer` et
`vf-auditer` gardent le CLAUDE.md du projet dont ils ont besoin pour relire, D-08) et I6 (manager non
interne : `vf-coder`, `vf-reviewer`, `vf-auditer`, `vf-test-orchestrator` sont des workers internes
qui dispatchent, D-07). D-07 et D-08 sont ratifiées par Samuel (WhatsApp, 2026-09-23) — D-08
réexaminée depuis par D-19, verdict transcrit par la Tâche 2 : citer l'arbitrage effectif (canal +
date), jamais « décision de cadrage de Claude » pour ce point précis puisqu'il est désormais tranché
par Samuel.
5. Remise en conformité des fixtures préexistantes, même procédure qu'à la tâche 1 (journal
temporaire, « invariant I5 »/« invariant I6 » relevés sur les cas antérieurs à T93/T94) : une fixture
de forme manager reçoit `SendMessage` avant son `Agent(` (toujours) ; une fixture de forme juge
reçoit `omitClaudeMd: true` SEULEMENT SI `ARBITRAGE-MAINTENIR` (T70 en fait partie — sinon T70 garde
son état actuel, sans omitClaudeMd, et reste hors du périmètre d'I5 tant qu'il n'est pas armé) ; rc
attendus et assertions inchangés ; liste au SUMMARY.
6. Rejouer la suite, puis les trois étapes CI avec le gate réel (commande de vérification), puis
`check-blueprints.sh`.
7. Commit(s) sur le gate : `feat(conductor): invariant I6 — managers avec SendMessage (FABR-03)` (et,
seulement si armé, un second commit `feat(conductor): invariant I5 — juges sans CLAUDE.md, arbitrage D-08 : maintenir (FABR-03)`)
avec les trailers
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — invariant I6 [et I5] et analyse pure des allowlists (D-07 [, D-08])`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T94, T96 et mutant MUT-I6 [, T93 et MUT-I5]`.
  </action>
  <verify>
    <automated>python3 -c "
import re
p = '.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-D19-MESURE.md'
try:
    s = open(p, encoding='utf-8').read()
except FileNotFoundError:
    print('ARBITRAGE-ABSENT'); raise SystemExit
m = re.search(r'\*\*D.cision\s*:\*\*\s*(maintenir|renoncer)\b.*?\*\*Canal\s*:\*\*\s*(\S.*?)\s*\n.*?\*\*Date\s*:\*\*\s*(\d{4}-\d{2}-\d{2})', s, re.S)
print('ARBITRAGE-' + m.group(1).upper() if m else 'ARBITRAGE-ABSENT')
"</automated>
    <fails_when>la sortie n'est ni ARBITRAGE-MAINTENIR, ni ARBITRAGE-RENONCER, ni ARBITRAGE-ABSENT (sonde cassée) — sert à déterminer QUELLE branche du verify ci-dessous s'applique, ne fait pas échouer la tâche à elle seule</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(94|96)|MUT-I6|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T94, T96 n'a aucune ligne « ✓ », ou « ✓ MUT-I6 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == » — TOUJOURS vérifié, quelle que soit la branche</fails_when>
    <automated>if python3 -c "
import re
s = open('.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-D19-MESURE.md', encoding='utf-8').read()
m = re.search(r'\*\*D.cision\s*:\*\*\s*maintenir\b', s)
raise SystemExit(0 if m else 1)
" 2>/dev/null; then bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T93|MUT-I5|✗ T93|✗ MUT-I5'; else echo "I5-NON-ARME (branche renoncer/absent — attendu)"; fi</automated>
    <fails_when>branche maintenir : « ✓ T93 » ou « ✓ MUT-I5 TUE » absent, ou une ligne « ✗ » apparaît ; branche renoncer/absent : la ligne « I5-NON-ARME » n'apparaît PAS (I5 aurait été armé sans arbitrage MAINTENIR)</fails_when>
    <automated>fail=0; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --agents-dir="$d" >/dev/null 2>&1 || fail=$((fail+1)); done; for f in plugin/*/AGENT.md; do bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --file "$f" >/dev/null 2>&1 || fail=$((fail+1)); done; reg=(); for d in plugin/*/agents; do reg+=("--agent-registry-dir=$d"); done; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --manifest-freshness=strict --agents-dir="$d" "${reg[@]}" >/dev/null 2>&1 || fail=$((fail+1)); done; bash plugin/conductor/scripts/check-blueprints.sh >/dev/null 2>&1 || fail=$((fail+1)); echo "CI-REPLAY fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « CI-REPLAY fail=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def parse_token(' plugin/conductor/scripts/check-agents.sh` rend 1 et `grep -c 'def allowlist_agents(' plugin/conductor/scripts/check-agents.sh` rend 1
    - `grep -c 'errors.extend(invariant_i6(' plugin/conductor/scripts/check-agents.sh` rend 1 (TOUJOURS) ; `grep -c 'errors.extend(invariant_i5(' plugin/conductor/scripts/check-agents.sh` rend 1 SI ARBITRAGE-MAINTENIR, sinon 0
    - branche MAINTENIR : les quatre juges portent `omitClaudeMd: true`, un commit + bump patch séparé par module (`git log -1 --format=%H -- plugin/<mod>/VERSION` distinct du commit SendMessage déjà posé en 42-02/42-03)
    - branche RENONCER ou ABSENT : `grep -rc '^omitClaudeMd' plugin/business-pilot-bundle/agents/quality-gate-client.md plugin/content-bundle/agents/content-clarity-judge.md plugin/growth-bundle/agents/growth-quality-judge.md plugin/design-orchestrator/agents/vf-design-judge.md | awk -F: '{s+=$2} END{print s}'` rend 0
    - la sortie de test-check-agents.sh contient au moins une ligne `✓ T94`, `✓ T96` et finit sur `0 KO ==` (T26 à T41 toujours verts) ; T93 et MUT-I5 présents et verts SEULEMENT si MAINTENIR
    - le rejeu CI (trois étapes + check-blueprints) imprime `CI-REPLAY fail=0` — les invariants étant des erreurs, un seul diagnostic d'invariant sur le corpus réel rendrait un rc non nul
  </acceptance_criteria>
  <done>I6 est une erreur dans tous les modes, alimentée par une analyse pure des allowlists, jumeau négatif et mutation prouvée rouge — INCONDITIONNEL. I5 suit le même régime UNIQUEMENT si la Tâche 2 a obtenu ARBITRAGE-MAINTENIR (avec pose du champ sur les quatre juges et bump patch séparé par module) ; sinon la tâche reste `<done>` sur son volet I6 et EN ATTENTE sur son volet I5, sans avoir rien armé ni rien inventé sur la redéfinition de D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent → gate | tout agent écrit (dépôt, lab, blueprint matérialisé) est une entrée non fiable |
| gate → garde d'écriture | un invariant violé produit une ligne « ✗ … : … » : la garde refuse l'écriture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-16 | Tampering | invariant neutralisé en douce dans le gate | high | mitigate | un mutant QUAL-01 tué par invariant (MUT-I1, I4, I5, I6, I7) : la suite rougit si une ligne d'appel disparaît |
| T-42-17 | Elevation of Privilege | juge qui charge toute la doctrine, manager aveugle à ses pairs | medium | mitigate | I5 et I6 en erreur ; porteurs réels mutés rouges (T93, T94) |
| T-42-18 | Tampering | erreurs de syntaxe d'allowlist dupliquées ou perdues par une seconde analyse | low | mitigate | parse_token pur, analyze_token enveloppe au texte inchangé ; T26 à T41 rejoués verts |
| T-42-19 | Denial of Service | garde d'écriture d'un lab qui refuse un manager sans SendMessage ou un juge sans omitClaudeMd | medium | accept | effet voulu de D-11 (doctrine opposable) ; le message nomme l'invariant et le champ qui le satisfait ; les blueprints de la fabrique passent (T96) |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` : 0 KO ; T91, T92, T94, T95, T96 ; MUT-I1, MUT-I4, MUT-I6, MUT-I7 tués — TOUJOURS. T93 et MUT-I5 tués SEULEMENT si le checkpoint de la Tâche 2 a obtenu `ARBITRAGE-MAINTENIR` ; sinon `invariant_i5` reste non armé et ces deux cas restent hors périmètre de cette exécution.
- Rejeu CI (trois étapes avec `--manifest-freshness=strict` + check-blueprints) : `CI-REPLAY fail=0` — dans les deux branches (I5 armé ou non, jamais de diagnostic « invariant I » erroné sur le corpus réel).
- `bash plugin/conductor/scripts/check-instruction-budget.sh` : `0 depassement(s)` (aucun agent touché par le gate lui-même ; les quatre juges reçoivent une seule ligne de frontmatter si armés).
- Suites de module et `test-guard-agent-write.sh` : rc 0.
</verification>

<success_criteria>
- FABR-03 (partie locale) : I1, I4, I6, I7 en erreur selon D-06, D-07, chacun avec jumeau négatif et mutation prouvée rouge — TOUJOURS. I5 (D-08) suit le même régime SEULEMENT si le checkpoint D-19 (Tâche 2) a obtenu `ARBITRAGE-MAINTENIR` ; sur `ARBITRAGE-RENONCER` ou `ARBITRAGE-ABSENT`, ce plan clôt son volet I6 et rend son volet I5 explicitement en attente, sans rien inventer sur la redéfinition de D-08.
- FABR-05 : corpus réel et blueprints conformes avec les invariants armés (I1 à I4, I6, I7 toujours ; I5 si arbitré), sans période d'avertissement (D-11).
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `T103`, `MUT-D20`, jeton `CIBLE-ABSENTE` | invocation nue sur cible absente hors --hook (D-20) | 42-01 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `perimees`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()`, `dispatch` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` ; section d'en-tête `INVARIANTS DE DOCTRINE` | invariants locaux | 42-05 |
| `juger_mutation_reelle` ; `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `SendMessage` sur 4 managers (inconditionnel) | frontmatter du corpus | 42-02, 42-03 |
| `omitClaudeMd: true` sur 4 juges (SEULEMENT si ARBITRAGE-MAINTENIR au checkpoint D-19) ; sonde `ARBITRAGE-*` | frontmatter du corpus, checkpoint | 42-05 |
| versions de module : 5 modules en patch, conductor en mineure | versions | 42-02, 42-03, 42-06 |

## Flagged assumptions

- **Flake connu T9/T29/T32 de test-check-agents.sh (documenté dans la PR #100, workstream fiabilite) : un rouge isolé sur ces trois cas précis (SIGPIPE) se rejoue seul avant d'être déclaré réel — jamais pris pour argent comptant à la première lecture de sortie de suite (0 KO attendu dans ce plan).**

- **Sonde de bords spec-less — FABR-03 et FABR-05 : `unclassified` / `unresolved`.** Non résolus
  d'office ; les bords rédigés ici (les deux sens d'I1, Agent nu, agent interne porteur d'allowlist,
  forme vf-reviewer, fixture qui ne viole qu'un invariant) sont des vérités du plan.
- **I4 sans porteur réel** : aucun agent du dépôt ne porte de spécifieur dans `disallowedTools` ;
  sa preuve repose sur fixture et mutant seuls.
- **Invariants en erreur hors --strict** : choix qui suit D-11 et garde check-blueprints.sh (mode
  par défaut) opposable ; un lab existant dont un agent viole un invariant le verra en « ✗ » au
  SessionStart (sortie 0) et la garde refusera d'en écrire un nouveau qui le viole.
- **« Worker interne » sensible à la casse** : D-06 nomme ce marqueur exact ; une variante de casse
  est traitée comme absente.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-05-SUMMARY.md` when done — il liste les fixtures préexistantes remises en conformité et l'invariant qui l'a exigé.
</output>

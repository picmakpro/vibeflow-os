---
phase: 26-manuel-utilisateur-vibeflow-manual
plan: 08
type: execute
wave: 8
depends_on: ["26-07"]
files_modified:
  - manual/toc.yml
  - manual/fr/README.md
  - manual/en/README.md
  - manual/fr/07-sous-le-capot/
  - manual/en/07-sous-le-capot/
autonomous: true
requirements: []   # TBD — ROADMAP §Phase 26 porte `Requirements: TBD`.
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lecteur sait exactement ce que l'installation a écrit sur son disque, à l'endroit — pas déduit d'un tableau de désinstallation (M-4)."
    - "Un lecteur comprend pourquoi VibeFlow préfère un script qui refuse à un paragraphe qui recommande, et quels gates tournent réellement."
    - "Un lecteur qui veut aller plus loin sait quelles décisions d'architecture le concernent et où les lire — le manuel sert de pont vers docs/ (D-05 §7)."
    - "Aucun fichier sous manual/ n'apparaît dans git status ni dans aucun commit (D-14)."
  artifacts:
    - "manual/{fr,en}/07-sous-le-capot/anatomie-d-un-lab-installe.md"
    - "manual/{fr,en}/07-sous-le-capot/l-engine-d-install.md"
    - "manual/{fr,en}/07-sous-le-capot/les-gates-machine.md"
    - "manual/{fr,en}/07-sous-le-capot/la-doctrine-et-ses-patterns.md"
    - "manual/{fr,en}/07-sous-le-capot/decisions-d-architecture.md"
    - "manual/{fr,en}/07-sous-le-capot/contribuer-et-aller-plus-loin.md"
  key_links:
    - "anatomie-d-un-lab-installe.md ← 02-concepts/qu-est-ce-qu-un-lab.md : la définition renvoie ici pour l'inventaire des fichiers (M-4)."
    - "decisions-d-architecture.md → docs/ADR.md : le manuel cite les ADR à valeur utilisateur et pointe le registre, il ne le recopie pas."
    - "contribuer-et-aller-plus-loin.md → docs/ et plugin/reference/content/ : la sortie du manuel, assumée."
---

<objective>
Livrer le thème `07-sous-le-capot` complet en français et en anglais : le dernier thème, celui qui
ferme le manuel et sert de pont vers `docs/`.

Purpose: public restreint, dernier par construction (D-05 §7). Il ferme le manque **M-4** — ce que
l'installation écrit sur le disque, dont la seule trace actuelle du dépôt est le tableau de
désinstallation d'`INSTALL.md`, c'est-à-dire l'information **à l'envers**. Il donne aussi au lecteur
curieux les quinze décisions d'architecture qui le concernent réellement, sans lui imposer les
quarante autres.

Output: 12 fichiers de pages (6 pages × 2 langues), `toc.yml` enrichi, aucun commit.

**Contrainte machine de la phase (D-14) — vaut pour les 3 tâches de ce plan :** `manual/` est exclu
de git par `.git/info/exclude:7`. `git add` sur ce chemin échoue. **Aucune tâche de ce plan ne
commite. Write/Edit et exécution locale seulement. Ne jamais `git add` un chemin sous `manual/`.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-07-SUMMARY.md
</context>

---

## Invariants de rédaction

Identiques aux 8 invariants énoncés au plan `26-02` (bilingue par incrément · `toc.yml` ne promet
rien · l'EN se produit, il ne se traduit pas (D-12) · zéro version en dur (D-11) · 100-200 lignes,
bascule à 300 ou 3 H2 (D-04) · aucun jargon non défini · blocs générés intouchés (D-03) · **aucun
commit** (D-14)). Ils s'appliquent sans être répétés.

**Invariant supplémentaire propre à ce thème :** ce thème **cite et route**, il ne recopie pas. La
doctrine fait près de 10 000 lignes et le registre d'ADR en fait 1 500 ; le rôle de ces six pages est
de dire ce qui existe, à qui ça sert, et où le lire — pas de le résumer intégralement. Une page qui
paraphrase sa source est une page qui divergera.

**Deux constats du dépôt à ne pas reproduire** (inventaire §6 et §14) : la gouvernance de release et
les conventions de commit du `CLAUDE.md` racine **n'entrent jamais** dans le manuel (elles concernent
le dépôt de distribution, pas l'utilisateur) ; et la duplication massive entre `docs/reference/` et
`plugin/reference/content/` est **connue, hors périmètre et non traitée ici** — le manuel ne
référence que la source canonique `plugin/reference/content/`, jamais la copie.

---

## Artifacts this plan produces

- `manual/fr/07-sous-le-capot/` et `manual/en/07-sous-le-capot/`, 6 pages chacun (H-1) :
  `anatomie-d-un-lab-installe.md` · `l-engine-d-install.md` · `les-gates-machine.md` ·
  `la-doctrine-et-ses-patterns.md` · `decisions-d-architecture.md` ·
  `contribuer-et-aller-plus-loin.md`
- `manual/toc.yml` : thème `07-sous-le-capot` ouvert, 6 entrées `pages:` ajoutées — **le sommaire est
  alors complet**
- `manual/fr/README.md` et `manual/en/README.md` : parcours guidé « je veux voir la mécanique » ajouté

---

<tasks>

<task type="auto">
  <name>Task 1: anatomie-d-un-lab-installe + l-engine-d-install (FR et EN) — le cœur de M-4</name>
  <files>manual/fr/07-sous-le-capot/anatomie-d-un-lab-installe.md, manual/en/07-sous-le-capot/anatomie-d-un-lab-installe.md, manual/fr/07-sous-le-capot/l-engine-d-install.md, manual/en/07-sous-le-capot/l-engine-d-install.md, manual/toc.yml</files>
  <read_first>
    - `plugin/_internal/vibeflow-update.sh` — l'engine d'install scopé : ce qu'il copie, où, ce qu'il
      sauvegarde, ce qu'il retire. C'est la source de vérité de cette page, pas `INSTALL.md`.
    - `INSTALL.md` §Désinstallation (~89-132) — le tableau à deux couches, **seule trace actuelle de
      M-4, écrite à l'envers** : il faut la retourner, pas la recopier.
    - `docs/ADR.md` — **ADR-051** (allowlist MCP dérivée du lab : explique pourquoi l'install touche
      aux agents du moteur), ADR-035 (architecture AI-Safe).
    - `plugin/conductor/hooks/hooks.json` et `plugin/dev-orchestrator/hooks/` — les hooks réellement
      posés, s'il y en a, et leur nature.
    - `manual/fr/02-concepts/qu-est-ce-qu-un-lab.md` (plan 26-03) — la définition, à laquelle cette
      page fournit le complément matériel.
  </read_first>
  <action>
    **`07-sous-le-capot/anatomie-d-un-lab-installe.md`** (FR + EN) — **M-4**, à écrire à l'endroit.
    Faire l'inventaire de ce que l'installation dépose sur le disque du lecteur : les dossiers de
    skills, d'agents, de scripts, de règles, la documentation copiée, le marqueur d'installation, les
    hooks posés (et lesquels exactement), et l'injection d'outils MCP dans les agents du moteur
    (ADR-051) — que rien ne documente aujourd'hui côté utilisateur. Pour chaque élément : à quoi il
    sert, qui l'écrit, et s'il est sûr d'y toucher. Terminer par ce qui **n'est pas** écrit, qui est
    tout aussi rassurant à savoir. Renvoyer à
    `01-demarrer/mettre-a-jour-et-desinstaller.md` pour le retrait, sans le redire.

    **`07-sous-le-capot/l-engine-d-install.md`** (FR + EN) — comment l'installation fonctionne, pour
    qui veut auditer avant d'exécuter. Couvre : le principe du scope ; l'idempotence et ce qu'elle
    garantit ; la sauvegarde avant modification ; le fait qu'aucun hook n'est posé au niveau du
    plugin, et ce que ça implique ; et ce que le lecteur peut lire lui-même pour vérifier. C'est ici
    que vit la promesse « ce que VibeFlow n'exécute pas » — la formuler une seule fois, dans cette
    page, plutôt que dispersée. Aucune version en dur.

    Terminer par : ouvrir le thème `07-sous-le-capot` dans le bloc `themes:` de `toc.yml`, inscrire
    les 2 entrées `pages:`, relancer `build-nav.sh`, relancer `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 07-sous-le-capot/anatomie-d-un-lab-installe.md 07-sous-le-capot/l-engine-d-install.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do grep -q '01-demarrer/mettre-a-jour-et-desinstaller.md' "manual/$l/07-sous-le-capot/anatomie-d-un-lab-installe.md"; done; t=$(grep -c '^[[:space:]]*- path: 07-sous-le-capot/' manual/toc.yml); [ "$t" -ge 2 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `anatomie-d-un-lab-installe.md` inventorie ce que l'install écrit **et** mentionne l'injection
      d'outils MCP (ADR-051), aujourd'hui absente de toute documentation utilisateur (M-4).
    - La page présente l'information à l'endroit (ce qui est posé et pourquoi), pas sous forme de
      procédure de retrait, et renvoie par lien résolvant vers
      `01-demarrer/mettre-a-jour-et-desinstaller.md`.
    - `l-engine-d-install.md` énonce la promesse « ce que VibeFlow n'exécute pas » en un seul endroit
      du manuel.
    - Aucune des deux pages n'importe la discipline de release ni les conventions de commit du
      `CLAUDE.md` racine (gouvernance du dépôt, hors sujet utilisateur).
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>M-4 est comblé : le lecteur sait ce qu'il y a sur son disque, et pourquoi, sans devoir lire une procédure de désinstallation à rebours.</done>
</task>

<task type="auto">
  <name>Task 2: les-gates-machine + la-doctrine-et-ses-patterns (FR et EN)</name>
  <files>manual/fr/07-sous-le-capot/les-gates-machine.md, manual/en/07-sous-le-capot/les-gates-machine.md, manual/fr/07-sous-le-capot/la-doctrine-et-ses-patterns.md, manual/en/07-sous-le-capot/la-doctrine-et-ses-patterns.md, manual/toc.yml</files>
  <read_first>
    - `plugin/conductor/scripts/check-agents.sh` et les autres `check-*.sh` des modules — ce que
      chaque gate vérifie réellement.
    - `plugin/reference/content/AXIOMES-ENFORCEMENT.md` — enforcement > prose.
    - `docs/ADR.md` — ADR-029 (densité : agents ≤ 250 lignes, skills ≤ 500), ADR-035 (architecture
      AI-Safe), ADR-044 (agents natifs machine-enforced).
    - `plugin/reference/README.md` §Quoi — l'arborescence commentée de `content/`, matière de la carte
      de la doctrine.
    - `plugin/reference/content/methodology/patterns/README.md` — l'index des douze patterns.
    - `manual/fr/02-concepts/gates-et-validation-humaine.md` (plan 26-03) — le **pourquoi** est
      là-bas ; ici c'est le **quoi**, sans redite.
  </read_first>
  <action>
    **`07-sous-le-capot/les-gates-machine.md`** (FR + EN) — l'inventaire des gates, pour qui veut
    savoir ce qui tourne. Pour chacun : ce qu'il vérifie, quand il s'exécute, et ce qui se passe s'il
    échoue. Nommer les contraintes de taille (densité des agents et des skills, bornes de fichiers)
    parce qu'elles sont **visibles** : c'est ce qui explique un refus que le lecteur pourrait prendre
    pour un caprice. Renvoyer à `02-concepts/gates-et-validation-humaine.md` pour la philosophie sans
    la redire.
    **Ne pas surpromettre** : si un gate est déclaratif ou linté plutôt que contraignant au runtime,
    le dire — le dépôt en porte des cas établis, et un manuel qui les tait promet plus que le produit.

    **`07-sous-le-capot/la-doctrine-et-ses-patterns.md`** (FR + EN) — la carte de la bibliothèque
    méthodologique : ce que contient `plugin/reference/content/`, comment elle est organisée (les
    fondateurs, les douze patterns, le vocabulaire, les modèles, l'exemple bout-en-bout), et **quels
    patterns valent la lecture d'un utilisateur plutôt que d'un concepteur** — en particulier ceux qui
    expliquent un comportement qu'il observe. Une carte et des renvois, jamais un résumé des 10 000
    lignes. Référencer **uniquement** la source canonique `plugin/reference/content/`, jamais la copie
    de `docs/reference/` (duplication connue, hors périmètre).

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 07-sous-le-capot/les-gates-machine.md 07-sous-le-capot/la-doctrine-et-ses-patterns.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do grep -q '02-concepts/gates-et-validation-humaine.md' "manual/$l/07-sous-le-capot/les-gates-machine.md"; test "$(grep -c 'docs/reference/' "manual/$l/07-sous-le-capot/la-doctrine-et-ses-patterns.md" || true)" = "0"; done; t=$(grep -c '^[[:space:]]*- path: 07-sous-le-capot/' manual/toc.yml); [ "$t" -ge 4 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `les-gates-machine.md` décrit chaque gate par ce qu'il vérifie, quand, et l'effet d'un échec.
    - Tout gate déclaratif ou seulement linté est présenté comme tel, sans surpromesse.
    - `les-gates-machine.md` renvoie à `02-concepts/gates-et-validation-humaine.md` par un lien qui
      résout, et n'en répète pas la philosophie.
    - `la-doctrine-et-ses-patterns.md` cartographie `plugin/reference/content/` **sans jamais citer
      le chemin de la copie dupliquée**, et désigne les patterns utiles à un utilisateur.
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur curieux sait ce qui le contraint et où lire la doctrine — sans qu'on lui ait recopié dix mille lignes.</done>
</task>

<task type="auto">
  <name>Task 3: decisions-d-architecture + contribuer-et-aller-plus-loin (FR et EN) + clôture du sommaire</name>
  <files>manual/fr/07-sous-le-capot/decisions-d-architecture.md, manual/en/07-sous-le-capot/decisions-d-architecture.md, manual/fr/07-sous-le-capot/contribuer-et-aller-plus-loin.md, manual/en/07-sous-le-capot/contribuer-et-aller-plus-loin.md, manual/fr/README.md, manual/en/README.md, manual/toc.yml</files>
  <read_first>
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §7 — la
      liste arrêtée des **15 ADR à valeur utilisateur** (029, 031, 032, 035, 044, 045, 051, 053, 054,
      055, 057, 058, 059, 060, 064) et la notion que chacune porte. Les ADR déclarées internes
      (046-050, 052, 056, 061-063) n'entrent pas au manuel.
    - `docs/ADR.md` — pour vérifier sur pièce la notion portée par chacune des 15, avant d'en écrire
      la ligne.
    - `CLAUDE.md` racine — **à ne pas importer** : gouvernance du dépôt de distribution.
    - `README.md` §Trust / `README.fr.md` §Confiance — la promesse de confiance, pour la relayer sans
      la dupliquer mot pour mot.
  </read_first>
  <action>
    **`07-sous-le-capot/decisions-d-architecture.md`** (FR + EN) — les décisions d'architecture qui
    **concernent l'utilisateur**, et elles seules : les quinze identifiées par l'inventaire §7. Pour
    chacune, une ligne ou deux disant ce qu'elle change pour lui, avec un renvoi vers le registre
    complet pour le raisonnement. Ouvrir en expliquant ce qu'est une ADR et pourquoi ce projet en
    tient un registre — c'est probablement la première fois que le lecteur en croise. Regrouper par
    thème utilisateur (ce qui vous protège, ce qui contraint le code, ce qui régit les missions)
    plutôt que par numéro. Ne pas recopier le texte des ADR : citer et pointer.

    **`07-sous-le-capot/contribuer-et-aller-plus-loin.md`** (FR + EN) — la sortie du manuel, assumée.
    Couvre : comment lire ce dépôt si on veut comprendre ou contribuer ; ce que contiennent `docs/` et
    `.planning/` et à qui ils s'adressent ; où signaler un problème ; ce qui est source-available et
    ce que ça permet ; et la promesse de confiance (scripts auditables, rien d'exécuté en douce),
    relayée en pointant vers sa formulation d'origine plutôt qu'en la dupliquant. Terminer le manuel
    par où on va après lui.

    **Ajouter aux deux README de langue** le parcours guidé « je veux voir la mécanique », pointant
    vers `07-sous-le-capot/anatomie-d-un-lab-installe.md`.

    Terminer par : inscrire les 2 dernières entrées `pages:` dans `toc.yml` — **le sommaire est alors
    complet, les 7 thèmes de D-05 sont ouverts** —, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 07-sous-le-capot/decisions-d-architecture.md 07-sous-le-capot/contribuer-et-aller-plus-loin.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for a in 029 031 035 044 045 051 053 054 055 057 058 059 060 064; do grep -q "$a" manual/fr/07-sous-le-capot/decisions-d-architecture.md; grep -q "$a" manual/en/07-sous-le-capot/decisions-d-architecture.md; done; for l in fr en; do grep -q '07-sous-le-capot/' "manual/$l/README.md"; done; for d in 01-demarrer 02-concepts 03-modules 04-cycle-de-dev 05-equipe-agents 06-reference 07-sous-le-capot; do grep -q "dir: $d" manual/toc.yml; done; t=$(grep -c '^[[:space:]]*- path: 07-sous-le-capot/' manual/toc.yml); [ "$t" -ge 6 ]; [ "$t" -le 9 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `decisions-d-architecture.md` couvre **les 15 ADR à valeur utilisateur** de l'inventaire §7, et
      **aucune** des ADR déclarées internes.
    - Aucune ADR n'est recopiée : chacune est résumée en une à deux lignes avec renvoi au registre.
    - `contribuer-et-aller-plus-loin.md` dit à qui s'adressent `docs/` et `.planning/`, et où signaler
      un problème.
    - Ni la discipline de release ni les conventions de commit du `CLAUDE.md` racine n'apparaissent
      dans le manuel.
    - Les deux README de langue portent le parcours « je veux voir la mécanique », lien résolvant.
    - **`toc.yml` porte les 7 thèmes de D-05 dans l'ordre** et aucune entrée sans fichier des deux
      côtés ; le thème `07-sous-le-capot` compte entre 6 et 9 pages par langue (D-04).
    - `bash manual/.tools/check-manual.sh` sort en **0** sur le manuel **complet**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le manuel est complet : sept thèmes, deux langues, un sommaire cohérent, et une sortie propre vers le reste du dépôt.</done>
</task>

</tasks>

<threat_model>
Sans objet pour ce plan : markdown seul, aucun code exécutable, aucune dépendance, aucun install de
paquet. La seule menace de la phase (fuite de `manual/` vers un artefact versionné) est instruite en
`T-26-04` du plan 26-01 et mitigée ici par l'assertion `git status --porcelain -- manual` vide dans
les trois tâches (D-14).
</threat_model>

<verification>
1. `bash manual/.tools/check-manual.sh` sort en **0** sur le manuel complet.
2. Les arbres `manual/fr` et `manual/en` sont isomorphes (D-01) et couvrent les 7 thèmes de D-05.
3. Aucune page ne dépasse 300 lignes ni 3 titres H2 ; aucun thème ne dépasse 9 pages (D-04).
4. Aucun motif de version dans tout `manual/` : C5 du gate passe (D-11).
5. Aucun lien relatif mort dans tout `manual/` : C3 du gate passe.
6. `git status --porcelain -- manual` sort **vide**.
7. `git status --porcelain -- plugin docs README.md README.fr.md INSTALL.md scripts .github` sort
   **vide**.
</verification>

<success_criteria>
- Les 6 (à 9) pages de `07-sous-le-capot` existent en FR **et** en EN.
- M-4 est comblé.
- Le manuel est complet et cohérent de bout en bout, dans les deux langues.
- Le gate est vert. Git n'a rien vu.
</success_criteria>

<output>
Create `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-08-SUMMARY.md` when done.
Y consigner : le décompte final de pages par thème et par langue, le verdict de `check-manual.sh` sur
le manuel complet, et la confirmation explicite que `git status --porcelain -- manual` est resté vide
de bout en bout.
</output>

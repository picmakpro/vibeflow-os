---
phase: 41-posture-de-protection-du-d-p-t
plan: 07
lot: L7 — PR jetable volontairement rouge, jamais mergée — mesure M-1 par lecture (quel run jumeau compte pour un check requis) et checks requis en échec lus sur une PR rouge (D-02ter) ; C0 aligne le compte « N suites » des deux README pour garder `check-version-sync` vert (révision REPLANIFIER du 2026-09-24)
type: execute
wave: 7
depends_on: ["41-06"]
autonomous: false
requirements: [PROT-01]
files_modified:
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
fichiers_jetables:
  - scripts/tests/test-preuve-41-m1.sh
  - README.md
  - README.fr.md

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition dure (état réel après 41-06) : `CO-VERDICT: CONFORME`, ou `CO-VERDICT: ECART` avec `CO-DECISION: option=accepter-et-documenter` (arbitrage Willy, AskUserQuestion session principale, 2026-09-24). `CO-I: merge_sans_contournement=refuse` n'est plus exigé : sous D-02ter aucun refus de merge n'est tenté (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24)"
    - "Reprise tracée : `PR-R-DECISION: option=REPLANIFIER` (arbitrage Willy, AskUserQuestion session principale, 2026-09-24) est déjà consignée dans `41-PREUVES.md` — elle est relue comme précondition, jamais redemandée ; `PRECONDITION-41-07:` et `PR-R-PREMISSE:` restent telles quelles (historique du premier passage)"
    - "Aucune tentative de merge sur la PR rouge, jamais, par aucun moyen — `gh pr merge` avec ou sans option, appel REST `PUT …/merge`, bouton ou case de l'interface —, quel que soit l'état lu (D-02ter) ; PR-R fermée sans merge, branche distante supprimée, relu via l'API"
    - "Pas 0 de la Task 2 : PR-BASELINE (#102) et PR-SENTINELLE (#103) fermées sans merge et leurs branches supprimées AVANT la mesure, relu via l'API ; #96, #98, #99 et #100 ne sont jamais touchées ; aucun merge sur `main` pendant la fenêtre convenue avec Samuel ; PR-P (#101), support de 41-08, relue via l'API après les gestes : ouverte et sans date de merge (`PR-P-APRES-41-07: state=OPEN merged=false`)"
    - "Cause du rouge triviale et réversible, jamais un gate modifié (D-M7) : une suite de preuve `scripts/tests/test-preuve-41-m1.sh`, présente sur la seule branche jetable, qui échoue selon `GITHUB_EVENT_NAME` et est découverte par l'étape existante `find plugin scripts -path '*/tests/test-*.sh'`. Comme `scripts/check-version-sync.sh` (point 9, job `gates`, requis) compte ces mêmes suites contre la première mention « N suites » de `README.md` et `README.fr.md`, C0 remplace dans chacun de ces deux fichiers, sur cette seule ligne, N par le nombre de suites de l'arbre de C0 (85 → 86 mesuré sur `a962065`) ; A et B ne touchent que la suite. `autres_fichiers=0` : rien d'autre que ces trois chemins, aucun gate, aucun fichier sous `plugin/`, aucun chemin CODEOWNERS"
    - "Chaque commit C0, A, B porte le trailer `Gate-Touche: scripts/tests/test-preuve-41-m1.sh — <raison>` (la suite tombe dans la surface de G-2) ; G-2 rend `DECLARE`, rc 0, sur la tête B"
    - "C0 est vert sur les quatre checks requis AVANT publication, prouvé par rejeu local sur clone jetable (`gates` sur C0 et sur B : 0 en échec ; `tests` sur C0 : découverte complète, suite de preuve PASS, aucun échec hors des rouges d'environnement ouverts au ledger `WINDOWS.md`) ; A et B ne diffèrent de C0 que par le blob de la suite, dont le comportement par événement est prouvé localement"
    - "M-1 joué sur trois commits d'une même PR, construits par plomberie : C0 (vert sur les deux événements, témoin), A (rouge sur `pull_request` seulement), B (rouge sur `push` seulement) ; dans A et B, le run vert dort 420 s pour terminer APRÈS le rouge — le run le plus récent est donc le vert, ce qui rend la lecture discriminante"
    - "Rien de la branche de preuve n'atterrit ailleurs : C0, A, B ne sont jamais ancêtres de la branche de travail B ni de `main`, aucune ref locale ne les porte, aucune ref distante hors `refs/pull/*` ne les porte après fermeture, la mention « N suites » des README de la branche de travail reste celle d'`origin/main` (`PR-R-FUITE:`) ; les trois push utilisent le refspec complet `refs/heads/preuve/41-ci-rouge`"
    - "M-1 établi par LECTURE SEULE (D-02ter, point a) : contextes requis du ruleset de `main` (nom et `integration_id`), runs jumeaux par événement (REST : conclusion, heure de fin) et rapport de GitHub par run (`isRequired(pullRequestNumber)` et agrégat `statusCheckRollup` du commit, GraphQL), relus par l'agent. Verdict SUR si, sur A et sur B, le run rouge de l'événement attendu est rapporté requis et l'agrégat vaut `FAILURE`, avec ordre discriminant, C0 vert sans échec requis et `main` inchangée ; DEFAUT si, dans ces mêmes conditions, un run rouge est rapporté non requis ou l'agrégat ne vaut pas `FAILURE` (rouge masqué par son jumeau vert) ; INDETERMINE sinon. Le verdict dit ce que GitHub RAPPORTE ; il n'observe aucun refus de merge"
    - "PR rouge portant des checks requis en échec (D-02ter, point b) : `CI-ROUGE-REQUIS:` compte, sur A et sur B, au moins un run de l'intégration 15368 rapporté requis et en échec, avec `merge_tente=non`"
    - "État de merge lu par Willy (`mergeStateStatus`, compte `picmakpro`) à C0, A et B : OBSERVATION consignée (`M1-ETATS:`, `etat_confirme`), hors verdict — jamais une condition de geste, jamais présentée comme un refus"
    - "Hors d'atteinte dans ce plan, dit tel quel (D-02ter) : le refus d'un merge sans contournement par un compte de la liste (Samuel, Willy, mode `always`) n'est ni tenté ni prouvé ; le témoin code owner par `reviewDecision` n'est pas discriminant (`reviewDecision` vide dans et hors CODEOWNERS : `CO-TEMOIN: … discriminant=hors_d_atteinte`) ; la preuve qu'une règle mord un acteur hors liste revient à 41-09 (clé de déploiement temporaire)"
    - "Verdict DEFAUT ou INDETERMINE, ou `CO-TEMOIN` inattendu : consigné tel quel, arrêt sur le checkpoint de décision (Task 4) ; aucune modification de `ci.yml` n'est planifiée. Un merge concurrent sur `main` rend M-1 INDETERMINE via `MAIN-AVANT-PR-R:`, jamais SUR"
    - "Rejeux locaux `tests` : seuls sont tolérés les échecs dont le chemin figure dans une entrée `open` du ledger `.planning/WINDOWS.md` (rouges d'environnement de ce poste, #6 et #7 au 2026-09-24), lus dans le ledger et jamais écrits en dur dans le plan ; tout autre échec rougit"
    - "Tout commit de plan et tout corps de PR citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », citant D-02bis nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 », citant D-02ter nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 », citant `PR-R-DECISION` nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 »"
  artifacts:
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PRECONDITION-41-07-REPRISE:`, `PR-R-COMMITS:`, `MAIN-AVANT-PR-R:`, `PR-R-REJEU-LOCAL:`, `PR-CO-FERMEES:`, `PR-R:`, `M1-ETATS:`, `M1-CONTEXTES:`, `M1-C0:`, `M1-A:`, `M1-B:`, `CI-ROUGE-REQUIS:`, `M1-VERDICT:`, `PR-R-FERMEE:`, `PR-P-APRES-41-07:`, `PR-R-FUITE:`, `CO-TEMOIN:`"
  key_links:
    - "M-1 se joue pendant qu'aucun merge n'arrive sur `main` : PR-P (#101) reste ouverte et non mergée jusqu'au plan 41-08"
    - "`M1-VERDICT: SUR` ou `M1-DECISION: option=accepter-et-documenter` conditionne 41-08 ; `CO-TEMOIN: … discriminant=hors_d_atteinte` est la valeur attendue par la précondition de 41-10"
    - "Clés relues en aval, de forme inchangée par la révision REPLANIFIER : `M1-VERDICT:`, `M1-DECISION:`, `PR-R:`, `PR-R-FERMEE:`, `PR-CO-FERMEES:` (41-08), `CO-TEMOIN: … discriminant=` (41-10), `CI-ROUGE-REQUIS:` (citée par 41-10, § Mesures) ; `PR-R-COMMITS:` n'est lue que par ce plan"
---

<objective>
Établir, par lecture seule, lequel des deux runs jumeaux (push, pull_request) compte pour un check
requis, et qu'une PR volontairement rouge porte bien des checks requis en échec — sans jamais
tenter de la merger ni la laisser atterrir.

Révisé le 2026-09-24 (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) :
41-06 a mesuré `CO-VERDICT: ECART` (PR de preuve lues `CLEAN`, `reviewDecision` vide), accepté et
documenté ; la précondition `merge_sans_contournement=refuse` est devenue inatteignable. Le plan ne
prouve plus que le prouvable : (a) quel run jumeau compte, par les contextes requis du ruleset et le
rapport de GitHub sur chaque run ; (b) qu'une PR rouge porte des checks requis en échec. Les
tentatives de merge sur la PR rouge sont supprimées. Le témoin code owner par `reviewDecision`
devient `hors_d_atteinte`. Fait de lecture du 2026-09-24 (planificateur, lecture seule) : la PR
#100 (auteur `samuel-neveugall`) est lue `mergeable_state=blocked` par `picmakpro`. L'état de merge
vu par Willy est donc consigné ici comme observation (`etat_confirme`), sans entrer dans le verdict.

Révisé une seconde fois le 2026-09-24 (`PR-R-DECISION: option=REPLANIFIER` — arbitrage Willy,
AskUserQuestion session principale, 2026-09-24, déjà consigné dans `41-PREUVES.md`, à ne pas
redemander) : le premier passage de la Task 1 a mesuré (`PR-R-PREMISSE:`) qu'un C0 limité à la suite
de preuve rougit le job requis `gates` — `scripts/check-version-sync.sh` compare la première mention
« N suites » des deux README au nombre réel de suites, 85 contre 86. C0 aligne donc cette mention
dans `README.md` et `README.fr.md` (une ligne chacun, rien d'autre) ; A et B ne touchent que la
suite. Chaque commit porte le trailer `Gate-Touche:` exigé par G-2. Les quatre checks requis de C0
sont rejoués verts en local avant tout geste chez GitHub.

Purpose: inconnu M-1 (D-M4) et partie lisible du critère de succès 2 (PROT-01, D-09), sous D-02ter.
Output: verdict M-1, checks requis en échec consignés, observations d'état, PR jetables fermées.

Budget : quatre tâches, dont la quatrième est un checkpoint de décision CONDITIONNEL, sans exécution
quand `M1-VERDICT: SUR` et `CO-TEMOIN: … discriminant=hors_d_atteinte`. Fichier de la branche de
travail : un seul (`41-PREUVES.md`). Les trois chemins de `fichiers_jetables` ne vivent que dans les
commits C0, A, B de la branche `preuve/41-ci-rouge`, jamais mergée ; ils ne sont jamais écrits dans
l'arbre de travail ni sur la branche B.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-06-SUMMARY.md
@.github/workflows/ci.yml
@scripts/check-version-sync.sh

Sources : `docs.github.com/en/pull-requests/…/status-checks` et troubleshooting-rules (checks requis
matchés par nom de job sans tenir compte de l'événement ; lequel de deux runs de même nom compte :
non documenté) ; GraphQL `CheckRun.isRequired(pullRequestNumber)` et `Commit.statusCheckRollup`
(lecture vérifiée le 2026-09-24 sur #93 et #101 : les deux jumeaux des quatre contextes rapportés
`isRequired=true`, lecture possible après fermeture de la PR). Constat du 2026-09-17 sur la tête de
la PR #78 : les runs `push` et `pull_request` démarrent à quelques secondes d'écart, le job de tests
dure environ quatre minutes.

**Rejeu du planificateur, 2026-09-24, avant de rendre cette révision** (clones jetables hors dépôt,
tête détachée, `refs/remotes/origin/main` posé sur `a962065`, commits du premier passage
c0=`755650c`, a=`a3bf40f`, b=`3b49306`, qui suivent exactement la recette de la Task 1) :
- audit statique : C0 touche `README.fr.md`, `README.md` (une ligne chacun, « 85 suites » → « 86
  suites », première mention) et ajoute la suite (mode 100755) ; A et B ne touchent que la suite ;
  rien sous `plugin/` ; trailer G-2 sur les trois ; aucun des trois n'est ancêtre de la branche de
  travail ni d'`origin/main`, aucune ref locale ne les porte ; comportement local des blobs
  `c0_push=0 c0_pr=0 a_push=0 a_pr=1 b_push=1 b_pr=0` ;
- job `gates` (outil de rejeu de la Phase 40.1) sur base, C0, A et B : rc 0, `15 rejouee(s),
  3 sautee(s), 0 en echec` sur chacune des quatre têtes, mêmes rc étape par étape ;
  `check-version-sync` rend « suites 85 » sur la base et « suites 86 » sur C0, A et B ; G-2 sur le
  dépôt réel rend rc 3 (`RIEN-A-JUGER`) sur la base et rc 0 (`DECLARE`) sur C0, A et B ;
- job `tests` (même outil, `GITHUB_EVENT_NAME=push` puis `pull_request`, `PREUVE41_DODO=0`, huit
  journaux) : base 85 suites, 2 échecs sur les deux événements — exactement les deux entrées `open`
  du ledger (`WINDOWS.md` #6 `test-register-codex-agent-path-traversal.sh` et #7
  `test-check-description-fidelity.sh`, rouges d'environnement de ce poste, absents de la CI) ; C0
  86 suites, ces 2 mêmes échecs, suite de preuve PASS sur les deux événements ; A : `push` identique à
  C0, `pull_request` = C0 plus la seule suite de preuve en échec (« PREUVE41 rouge volontaire sur
  pull_request ») ; B : symétrique (suite de preuve en échec sur `push` seul). Hors suite de preuve,
  verdict identique à la base suite par suite sur les huit journaux (0 écart). Le rejeu tel qu'écrit
  à l'origine (`tests` avec 0 échec exigé) aurait rougi sur ce poste à cause de #6 et #7 : la
  tolérance par ledger des blocs `tests` de ce plan en vient ;
- jobs `lab-frais` et `lab-frais-arme` : hors de portée de l'outil du dépôt (il ne connaît que
  `tests` et `gates`) ; rejoués par une extension jetable hors dépôt (même parseur, `GITHUB_WORKSPACE`
  et `GITHUB_ENV` émulés) : rc 0 sur les quatre têtes (`lab-frais` 2 rejouées, `lab-frais-arme`
  7 rejouées, 0 en échec). Ils n'installent que `plugin/`, que ni C0 ni A ni B ne touchent :
  invariance aussi prouvée par construction (bloc 1 de la Task 1, `plugin=0`) ;
- blocs de vérification automatisée révisés, joués dans un clone jetable de la branche de travail : bloc 1 de la
  Task 1 rc 0 sur le triplet repris, rc 1 sur quatre triplets mutants (C0 sans README — le premier
  passage —, README retouché dans A, trailer absent de B, README alignés à 87) ; bloc 2 rc 0 ;
  bloc 3 joué de bout en bout, tel qu'écrit (vrais clones, vrais rejeux, environ sept minutes) :
  rc 0, `gates c0=0/0 b=0/0 | g2 b rc=0 DECLARE | tests c0 bilan(N M)=86 2 echecs_ledger=2
  echecs_hors_ledger=0 preuve_c0=PASS` ; dans sa première écriture, ce bloc clonait sous le
  chemin logique du dossier temporaire de macOS (un lien symbolique) et trois suites de conversion
  codex (`test-agent-to-codex.sh`, `test-vibeflow-update.sh`, `test-check-artifact-fidelity.sh`)
  y rougissaient hors ledger ; sur le même clone pris par son chemin physique, elles passent (mesuré
  sur la première isolément, puis par le rejeu complet du bloc) : le bloc résout désormais son
  dossier par `pwd -P` ; bloc `PR-R-FUITE:` de la Task 3 rc 0, rc 1 sur trois mutations (ref
  locale sur C0, README modifié dans l'arbre, C0 fusionné dans la tête) ; bloc `tests` de la Task 3
  rc 0 sur le journal réel de la base, rc 1 sur un échec hors ledger et sur la suite présente dans
  l'arbre.
Non rejoué : les étapes `uses:` (checkout, setup-node), l'installation du moteur GSD et le canari du
job `tests` (sautés par l'outil, sans lien avec les trois fichiers touchés) ; les étapes
conditionnelles à un push sur `main` (`check-push-sans-pr` réel, `check-release-tag`) ; le commit de
fusion que GitHub construit pour l'événement `pull_request` (même arbre que la tête tant que `main`
n'a pas bougé, ce que `MAIN-AVANT-PR-R:` et `main_inchangee` gardent) ; la durée réelle de 420 s
(`PREUVE41_DODO=0`) ; et, par construction, la manière dont GitHub compte les runs jumeaux — c'est la
mesure elle-même.

Note d'exécution : si l'isolation de worktree refuse de lancer en ligne un bloc de vérification (git
appelé sur un clone jetable), l'écrire tel quel dans un fichier temporaire hors dépôt et le lancer
par `bash <fichier>` depuis la racine du worktree — même texte, aucune modification.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : précondition relue (dont `PR-R-DECISION: option=REPLANIFIER`), C0 → A → B repris ou construits par plomberie — C0 aligne « N suites » des deux README —, comportement local de la suite prouvé, checks requis de C0 rejoués verts sur clone jetable, `main` relevée</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .github/workflows/ci.yml (étape « Découvrir et lancer toutes les suites » : `bash "$t"` par suite, bilan ; étape `check-version-sync` du job `gates` ; étape G-2 `check-gate-touche`)
    - scripts/check-version-sync.sh (point 9 : première mention `[0-9][0-9]* suites` de chaque README, comparée à `find plugin scripts -path '*/tests/test-*.sh'`)
    - scripts/check-gate-touche.sh (surface G-2, trailer `Gate-Touche: <chemin> — <raison>`, base = `merge-base HEAD origin/main`)
    - .planning/WINDOWS.md (entrées `open` #6 et #7 : rouges d'environnement de ce poste, tolérés par chemin dans les rejeux locaux `tests`)
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`CO-VERDICT:`, `CO-DECISION:`, `PR-P:`, `PR-BASELINE:`, `PR-SENTINELLE:`, `PR-EN-VOL-41-06:`, section `## 41-07` : `PRECONDITION-41-07:`, `PR-R-PREMISSE:` et sa note, `PR-R-DECISION:`)
  </read_first>
  <action>
Identité `picmakpro` (`gh api user --jq .login`, jamais `gh auth status`) ; `git fetch origin`.
Précondition (D-02ter) : dernier `CO-VERDICT:` égal à `CONFORME`, ou égal à `ECART` avec
`CO-DECISION: option=accepter-et-documenter` ; et `PR-R-DECISION: option=REPLANIFIER` présent
(décision déjà prise, arbitrage Willy, AskUserQuestion session principale, 2026-09-24 : la relire,
ne jamais la redemander) ; sinon arrêt. `CO-I: merge_sans_contournement` n'est plus lu. Hors PR en
vol de la ligne d'inventaire la plus récente parmi `PR-EN-VOL-APRES:` et `PR-EN-VOL-41-0N:`, les PR
ouvertes sont exactement PR-P, PR-BASELINE et PR-SENTINELLE (ou PR-P seule si une décision les a
déjà fermées).

Suite de preuve : trois variantes d'un script `test-preuve-41-m1.sh`, rédigées hors dépôt (dossier
temporaire), jamais dans l'arbre de travail. En-tête commentaire « PREUVE PHASE 41 (M-1) — branche
jetable, NE PAS MERGER ». Une variable écrite en dur `ROUGE_SUR` vaut `aucun` (C0), `pull_request`
(A) ou `push` (B). Comportement : si `GITHUB_EVENT_NAME` égale `ROUGE_SUR`, imprimer
`PREUVE41 rouge volontaire sur <événement>` et sortir 1 ; sinon, si `ROUGE_SUR` n'est pas `aucun`,
dormir `PREUVE41_DODO` secondes (défaut 420), puis imprimer `PREUVE41 vert` et sortir 0.

README de C0 (révision REPLANIFIER, remède de `PR-R-PREMISSE:`) : `scripts/check-version-sync.sh`
compte toute suite découverte par le job de tests contre la PREMIÈRE mention « N suites » de
`README.md` et de `README.fr.md` ; aucun emplacement ne fait lancer la suite sans la faire compter.
Extraire hors dépôt les deux README d'`origin/main` et, dans chacun, remplacer sur la seule ligne de
cette première mention le nombre N par le nombre de fichiers `*/tests/test-*.sh` sous `plugin` et
`scripts` de l'arbre de C0 (N d'`origin/main` plus un ; 85 → 86 mesuré sur `a962065`,
« (85 suites in CI » et « (`85 suites` en CI ») : une ligne modifiée par fichier, rien d'autre. Ni un
gate, ni un fichier sous `plugin/`, ni un chemin CODEOWNERS n'est touché (D-M7 ; CONTEXT, Claude's
Discretion : contenu de la PR jetable).

Construire par plomberie (index temporaire hors dépôt, `hash-object -w`, `update-index --add
--cacheinfo`, `write-tree`, `commit-tree`), sans changer de branche : C0 = `origin/main` + variante
C0 au chemin `scripts/tests/test-preuve-41-m1.sh` (mode 100755) + les deux README alignés ; A = C0
avec la variante A au même chemin, rien d'autre ; B = A avec la variante B, rien d'autre. Aucune ref
locale (branche ou tag) ne pointe sur C0, A ou B. Sujets : « test(41): preuve M-1 — C0 témoin (NE PAS
MERGER) », « test(41): preuve M-1 — A rouge sur pull_request (NE PAS MERGER) », « test(41): preuve
M-1 — B rouge sur push (NE PAS MERGER) ». Le corps de C0 dit l'alignement du compte « 86 suites » des
deux README pour garder `check-version-sync` vert et « Aucune tentative de merge (D-02ter —
arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ». Chaque commit, en dernier
paragraphe, porte le trailer G-2 `Gate-Touche: scripts/tests/test-preuve-41-m1.sh — suite de preuve
jetable M-1 (41-07), branche preuve/41-ci-rouge jamais mergée`, suivi des lignes d'attribution de la
session.

Reprise des commits du premier passage : si `origin/main` vaut encore
`a9620659c24103a103a5ca35c882faedb255b2c4`, reprendre tels quels
c0=`755650c0dd34dbc399247da80cf7391c74cf7ae1`, a=`a3bf40f4a254e0bc72b441408be5ff486d164401`,
b=`3b493061251f952bad57f7cb2a61a7f7aab8e85b` (construits le 2026-09-24 selon cette recette,
variante « README alignés » de `PR-R-PREMISSE:`, audités par le planificateur : fichiers, blobs,
trailers et rejeux conformes). Ils passent les mêmes vérifications que tout triplet reconstruit. Si
`origin/main` a bougé, ou si un de ces objets manque, reconstruire selon la recette ci-dessus sur le
nouvel `origin/main` — jamais de rebase des anciens.

Vérifier localement chaque blob avec `PREUVE41_DODO=0` et `GITHUB_EVENT_NAME` forcé. Rejouer
ensuite, sur deux clones jetables hors dépôt du dépôt local (`refs/remotes/origin/main` posé sur la
base, tête détachée sur C0 puis sur B, dossier temporaire pris par son chemin physique `pwd -P` :
atteint par le lien symbolique du dossier temporaire de macOS, un clone fait rougir trois suites de
conversion codex, mesuré le 2026-09-24), le job `gates` par l'outil de rejeu de la Phase 40.1
(`--root <clone>`), G-2 directement sur B, et le job `tests` sur C0 avec
`GITHUB_EVENT_NAME=pull_request PREUVE41_DODO=0` ; seuls sont tolérés les échecs dont le chemin
figure dans une entrée `open` de `.planning/WINDOWS.md`.

Dans `41-PREUVES.md`, sous la section `## 41-07 — M-1 par lecture, PR rouge jamais mergée
(D-02ter)` déjà ouverte (ne pas la dupliquer ; ne réécrire ni `PRECONDITION-41-07:`, ni
`PR-R-PREMISSE:`, ni `PR-R-DECISION:`), ajouter une sous-section
`### Task 1 — reprise après REPLANIFIER, <date>` et y consigner :
- `PRECONDITION-41-07-REPRISE: identite=<login> CO-VERDICT=<> CO-DECISION=<> PR-R-DECISION=REPLANIFIER origin_main=<sha> prs_ouvertes_hors_inventaire=<liste>` ;
- `PR-R-COMMITS: c0=<sha> a=<sha> b=<sha> base=<origin/main> readme_suites=<N base>-><N C0> reprise=<oui|non>` ;
- `MAIN-AVANT-PR-R: <sha d'origin/main>` ;
- `PR-R-REJEU-LOCAL: c0_gates=<rc>/<en échec> b_gates=<rc>/<en échec> b_g2=<rc>:<DECLARE|absent> c0_suites=<N> c0_echecs_ledger=<k> c0_echecs_hors_ledger=<k> preuve_c0=<PASS|absent> source=clone_jetable non_rejoue=etapes_uses,etapes_if_main,commit_de_merge_pull_request,dodo_420s`.

Commit local sur la branche de travail B (`feat/phase-41-preuves-release`, seul `41-PREUVES.md`),
message « docs(41-07): commits de la mesure M-1 préparés, README alignés dans C0 seul
(PR-R-DECISION — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ». Imprimer les
commandes de la Task 2 avec les SHA et les numéros de PR-BASELINE et PR-SENTINELLE lus dans
`41-PREUVES.md`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); cv=$(awk "\$1==\"CO-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); cdo=$(val CO-DECISION: option); dr=$(val PR-R-DECISION: option); { [ "$u" = picmakpro ] && [ "$dr" = REPLANIFIER ] && { [ "$cv" = CONFORME ] || { [ "$cv" = ECART ] && [ "$cdo" = accepter-et-documenter ]; }; }; } || { echo "precondition identite=$u CO-VERDICT=$cv CO-DECISION=$cdo PR-R-DECISION=$dr"; exit 1; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); BA=$(val PR-R-COMMITS: base); S=scripts/tests/test-preuve-41-m1.sh; T=$(mktemp -d); r() { git show "$1:$S" > "$T/s"; rc=0; PREUVE41_DODO=0 GITHUB_EVENT_NAME="$2" bash "$T/s" > /dev/null 2>&1 || rc=$?; echo "$rc"; }; x="c0_push=$(r "$C0" push) c0_pr=$(r "$C0" pull_request) a_push=$(r "$A" push) a_pr=$(r "$A" pull_request) b_push=$(r "$B" push) b_pr=$(r "$B" pull_request)"; fl() { git diff --name-only "$1" "$2" > "$T/f"; LC_ALL=C sort "$T/f" | paste -sd, -; }; f0=$(fl "$BA" "$C0"); fa=$(fl "$C0" "$A"); fb=$(fl "$A" "$B"); om=$(git rev-parse origin/main); fil=$([ "$BA" = "$om" ] && [ "$(git rev-parse "$C0^")" = "$BA" ] && [ "$(git rev-parse "$A^")" = "$C0" ] && [ "$(git rev-parse "$B^")" = "$A" ] && echo oui || echo non); git diff --name-only "$BA" "$B" > "$T/db"; autres=$(awk -v s="$S" "\$0 != s && \$0 != \"README.md\" && \$0 != \"README.fr.md\" {c++} END{print c+0}" "$T/db"); git diff --name-only "$BA" "$B" -- plugin > "$T/pl"; pl=$(awk "END{print NR}" "$T/pl"); ns() { git ls-tree -r --name-only "$1" -- plugin scripts > "$T/ls"; awk "/\\/tests\\/test-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ls"; }; nb=$(ns "$BA"); nc=$(ns "$C0"); pm() { git show "$1:$2" > "$T/r"; awk "match(\$0, /[0-9]+ suites/) { s=substr(\$0, RSTART, RLENGTH); sub(/ suites\$/, \"\", s); print s; exit }" "$T/r"; }; nm() { git diff --numstat "$BA" "$C0" -- "$1" > "$T/n"; awk "{print \$1 \"/\" \$2}" "$T/n"; }; rm1="README.md:$(pm "$BA" README.md)->$(pm "$C0" README.md)($(nm README.md)) README.fr.md:$(pm "$BA" README.fr.md)->$(pm "$C0" README.fr.md)($(nm README.fr.md))"; tg=0; mp=0; for s in "$C0" "$A" "$B"; do git log -1 --format=%B "$s" > "$T/m"; tg=$((tg + $(awk "index(\$0, \"Gate-Touche: scripts/tests/test-preuve-41-m1.sh — \")==1 && length(\$0) > 60 {c++} END{print (c>0)}" "$T/m"))); mp=$((mp + $(awk "NR==1 && index(\$0, \"NE PAS MERGER\") {c++} END{print c+0}" "$T/m"))); done; an=non; for ref in HEAD origin/main; do if git merge-base --is-ancestor "$C0" "$ref"; then an=oui; fi; done; echo "precondition identite=$u CO-VERDICT=$cv CO-DECISION=$cdo PR-R-DECISION=$dr | PR-R local : $x filiation=$fil fichiers_c0=$f0 fichiers_a=$fa fichiers_b=$fb autres_fichiers=$autres plugin=$pl suites=$nb->$nc readme=[$rm1] trailers_g2=$tg/3 ne_pas_merger=$mp/3 ancetre_travail_ou_main=$an"; [ "$x" = "c0_push=0 c0_pr=0 a_push=0 a_pr=1 b_push=1 b_pr=0" ] && [ "$fil" = oui ] && [ "$f0" = "README.fr.md,README.md,$S" ] && [ "$fa" = "$S" ] && [ "$fb" = "$S" ] && [ "$autres" -eq 0 ] && [ "$pl" -eq 0 ] && [ "$nc" -eq $((nb + 1)) ] && [ "$rm1" = "README.md:$nb->$nc(1/1) README.fr.md:$nb->$nc(1/1)" ] && [ "$tg" -eq 3 ] && [ "$mp" -eq 3 ] && [ "$an" = non ] && [ "$(val PR-R-COMMITS: readme_suites)" = "$nb->$nc" ]'</automated>
    <fails_when>exit non nul ; ou identité différente de `picmakpro` ; ou précondition non tenue (`CO-VERDICT` ni `CONFORME` ni `ECART` accepté par `CO-DECISION: option=accepter-et-documenter`, ou `PR-R-DECISION: option=REPLANIFIER` absent) ; ou un code de sortie local différent de l'attendu `c0_push=0 c0_pr=0 a_push=0 a_pr=1 b_push=1 b_pr=0` ; ou filiation fausse, ou base différente d'`origin/main` ; ou C0 touche autre chose que exactement les deux README et la suite, ou A ou B autre chose que la suite ; ou `autres_fichiers=` différent de 0, ou un fichier sous `plugin/` touché ; ou le nombre de suites de C0 n'est pas celui de la base plus un ; ou un README modifié sur plus d'une ligne, ou sa première mention « N suites » différente du nombre réel de suites de C0 ; ou un des trois commits sans trailer `Gate-Touche: scripts/tests/test-preuve-41-m1.sh — <raison>` ou sans « NE PAS MERGER » au sujet ; ou C0 ancêtre de la branche de travail ou d'`origin/main` ; ou `readme_suites` consigné différent de la mesure</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; m=$(awk "\$1==\"MAIN-AVANT-PR-R:\" {print \$2; exit}" "$PH/41-PREUVES.md"); o=$(git rev-parse origin/main); al=$(awk "\$1 ~ /^PR-EN-VOL-(APRES|41-[0-9][0-9]):\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"numeros=\")==1) al=substr(\$i, 9) } END{print al}" "$PH/41-PREUVES.md"); T=$(mktemp -d); gh pr list --repo picmakpro/vibeflow-os --state open --json number,headRefName --jq ".[] | \"\(.number) \(.headRefName)\"" > "$T/prs"; op=$(awk -v al=",$al," "{ if (index(al, \",#\" \$1 \",\") == 0) print \$2 }" "$T/prs" | LC_ALL=C sort | paste -sd, -); echo "main avant PR-R : consigne=$m origin_main=$o prs_ouvertes=$op"; [ "$m" = "$o" ] && { [ "$op" = preuve/41-codeowner-baseline,preuve/41-codeowner-sentinelle,preuve/41-contournement ] || [ "$op" = preuve/41-contournement ]; }'</automated>
    <fails_when>exit non nul — dont un `gh pr list` en échec, dont la sortie est capturée dans un fichier avant tout filtrage pour que son statut arrête le bloc au lieu d'être avalé par un pipeline ; ou `MAIN-AVANT-PR-R:` différent d'`origin/main` ; ou, hors PR en vol de la ligne d'inventaire la plus récente, des PR ouvertes autres que PR-P et les deux PR jetables de 41-06 (un merge concurrent fausserait M-1)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; C0=$(val PR-R-COMMITS: c0); B=$(val PR-R-COMMITS: b); BA=$(val PR-R-COMMITS: base); TOOL=.planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh; W=$(pwd); T=$(mktemp -d); T=$(cd "$T" && pwd -P); awk -F"|" "{f=\$5; s=\$8; gsub(/ /, \"\", f); gsub(/ /, \"\", s); if (s==\"open\" && f ~ /\\/tests\\/test-[^\\/]*\\.sh\$/) print f}" .planning/WINDOWS.md > "$T/ledger"; cl() { git clone -q --no-checkout "$W" "$T/$1"; git -C "$T/$1" update-ref refs/remotes/origin/main "$BA"; git -C "$T/$1" checkout -q --detach "$2"; }; ga() { rc=0; bash "$W/$TOOL" --job gates --root "$T/$1" > "$T/$1.gates" 2>&1 || rc=$?; e=$(awk "/^BILAN REJEU gates :/ {print \$(NF-2)}" "$T/$1.gates"); echo "$rc/${e:-absent}"; }; cl c0 "$C0"; cl b "$B"; gc=$(ga c0); gb=$(ga b); g2=0; (cd "$T/b" && bash scripts/check-gate-touche.sh) > "$T/g2" 2>&1 || g2=$?; dec=$(awk "/^DECLARE/ {c++} END{print (c>0 ? \"DECLARE\" : \"absent\")}" "$T/g2"); rt=0; PREUVE41_DODO=0 GITHUB_EVENT_NAME=pull_request bash "$W/$TOOL" --job tests --root "$T/c0" > "$T/t" 2>&1 || rt=$?; bl=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); awk "/FAIL\\(rc=[0-9]+\\) / {sub(/.*FAIL\\(rc=[0-9]+\\) /, \"\"); print}" "$T/t" > "$T/ko"; awk "NR==FNR {l[\$0]=1; next} !(\$0 in l)" "$T/ledger" "$T/ko" > "$T/hl"; hl=$(awk "END{print NR}" "$T/hl"); nk=$(awk "END{print NR}" "$T/ko"); pv=$(awk "\$0==\"PASS scripts/tests/test-preuve-41-m1.sh\" {c++} END{print (c>0 ? \"PASS\" : \"absent\")}" "$T/t"); git -C "$T/c0" ls-tree -r --name-only HEAD -- plugin scripts > "$T/lsc"; d=$(awk "/\\/tests\\/test-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/lsc"); awk "{print \"HORS LEDGER : \" \$0}" "$T/hl"; echo "rejeu local : gates c0=$gc b=$gb | g2 b rc=$g2 $dec | tests c0 rc=$rt bilan(N M)=$bl decouverte=$d echecs_ledger=$((nk - hl)) echecs_hors_ledger=$hl preuve_c0=$pv"; [ "$gc" = 0/0 ] && [ "$gb" = 0/0 ] && [ "$g2" -eq 0 ] && [ "$dec" = DECLARE ] && [ "$bl" != absent ] && set -- $bl && [ "$1" -eq "$d" ] && [ "$2" -eq "$nk" ] && [ "$hl" -eq 0 ] && [ "$pv" = PASS ] && { [ "$rt" -eq 0 ] || [ "$2" -gt 0 ]; } && [ "$(val PR-R-REJEU-LOCAL: c0_gates)" = "$gc" ] && [ "$(val PR-R-REJEU-LOCAL: b_gates)" = "$gb" ] && [ "$(val PR-R-REJEU-LOCAL: b_g2)" = "$g2:$dec" ] && [ "$(val PR-R-REJEU-LOCAL: c0_suites)" = "$1" ] && [ "$(val PR-R-REJEU-LOCAL: c0_echecs_hors_ledger)" = "$hl" ] && [ "$(val PR-R-REJEU-LOCAL: preuve_c0)" = "$pv" ]'</automated>
    <fails_when>exit non nul ; ou `gates` rejoué sur C0 ou sur B avec un rc non nul ou au moins une étape en échec (dont `check-version-sync`) ; ou G-2 sur B autre que rc 0 avec `DECLARE` ; ou `tests` rejoué sur C0 sans bilan, avec un nombre de suites différent de la découverte de C0, avec un échec dont le chemin n'est pas celui d'une entrée `open` de `.planning/WINDOWS.md` (ligne `HORS LEDGER :`), ou sans `PASS` de la suite de preuve ; ou une valeur consignée dans `PR-R-REJEU-LOCAL:` différente du rejeu. Durée : plusieurs minutes (clones et job de tests complet), c'est voulu</fails_when>
  </verify>
  <acceptance_criteria>
    - précondition D-02ter tenue (`CO-VERDICT: ECART` accepté par `CO-DECISION: option=accepter-et-documenter`, ou `CONFORME`) et `PR-R-DECISION: option=REPLANIFIER` relue, non redemandée
    - C0, A, B chaînés sur `origin/main` ; C0 ne touche que la suite de preuve et la première mention « N suites » des deux README (une ligne chacun, N = suites de C0) ; A et B ne touchent que la suite ; trailer G-2 sur les trois
    - comportement local : C0 vert partout ; A rouge sur `pull_request` seul ; B rouge sur `push` seul
    - `gates` vert sur C0 et sur B, G-2 `DECLARE` sur B, `tests` sur C0 sans échec hors ledger, rejoués sur clones jetables et consignés (`PR-R-REJEU-LOCAL:`)
    - `main` relevée ; seules PR-P et les deux PR jetables de 41-06 ouvertes hors inventaire ; aucune ref locale sur C0, A, B
  </acceptance_criteria>
  <done>Les trois états de mesure sont prêts, prouvés localement et rejoués verts là où ils doivent l'être, sans toucher aucun gate ni l'arbre de travail.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains de Willy (`picmakpro`) — fermer PR-BASELINE et PR-SENTINELLE, publier C0 et ouvrir PR-R, puis A et B, observer l'état à chaque étape, fermer PR-R — AUCUN merge</name>
  <read_first>
    - les SHA, les numéros de PR et les commandes imprimés par la Task 1
  </read_first>
  <action>Willy, compte `picmakpro`, joue la séquence ci-dessous ; aucune commande de merge n'est jouée sur PR-R, à aucune étape, quel que soit l'état lu (D-02ter).</action>
  <instructions>
Compte qui agit : `picmakpro` (témoin `gh api user --jq .login`). SHA imprimés par la Task 1.
Chaque attente dure plusieurs minutes (le run vert dort 420 s dans A et B) : c'est voulu.

INTERDIT sur PR-R, à chaque étape et quel que soit l'état lu : `gh pr merge` (avec ou sans
option), tout appel `PUT …/pulls/<n>/merge`, tout bouton ou case de l'interface (D-02ter). Les
lectures d'état ci-dessous sont des observations, jamais un feu vert.

Chaque push nomme la destination en entier, `refs/heads/preuve/41-ci-rouge`, jamais un nom court :
`picmakpro` est en contournement `always`, un push vers `main` serait accepté par le serveur. C0
porte, en plus de la suite, la mention « N suites » alignée des deux README : rien de cette branche
ne doit atteindre `main` ni la branche de travail.

Pas 0 (PR jetables de 41-06) :
0. `gh pr close <nb> --repo picmakpro/vibeflow-os --comment "Preuve de revue code owner relue (41-06) — fermée sans merge."` puis `git push origin --delete preuve/41-codeowner-baseline` ;
   même geste pour `<ns>` et `preuve/41-codeowner-sentinelle`.

Étape C0 :
1. `git push origin <c0>:refs/heads/preuve/41-ci-rouge`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head preuve/41-ci-rouge --title "PREUVE 41 — NE PAS MERGER — CI volontairement rouge (M-1)" --body "PR jetable de la Phase 41 (D-M7). Aucune tentative de merge, par aucun moyen (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24). Le premier commit aligne aussi le compte « N suites » des deux README sur la suite de preuve, pour garder check-version-sync vert ; rien de cette branche n'atterrit sur main. Fermée sans merge à la fin de la mesure."` → noter `<n>`.
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (tous verts attendus : les quatre checks requis de C0 ont été rejoués verts en local à la Task 1).
4. Observation : `gh pr view <n> --repo picmakpro/vibeflow-os --json headRefOid,mergeStateStatus --jq '{tete: .headRefOid, etat: .mergeStateStatus}'`
   → copier (si `UNKNOWN`, relire après quelques secondes).

Étape A :
5. `git push origin <a>:refs/heads/preuve/41-ci-rouge`
6. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin (sortie en échec attendue).
7. Observation : même commande qu'en 4 → copier.

Étape B :
8. `git push origin <b>:refs/heads/preuve/41-ci-rouge`
9. et 10. : comme 6 et 7.

Fermeture :
11. `gh pr close <n> --repo picmakpro/vibeflow-os --comment "Mesure M-1 terminée — fermée sans merge (D-M7)."`
12. `git push origin --delete preuve/41-ci-rouge`

Si, à n'importe quel moment, la PR apparaît `MERGED`, ou si C0 n'est pas vert à l'étape 3 : s'arrêter
et le signaler immédiatement, sans pousser A.
  </instructions>
  <acceptance_criteria>
    - PR jetables de 41-06 fermées sans merge, branches supprimées ; PR-R ouverte sur C0 puis A puis B, jamais mergée, aucune commande de merge jouée ; observations d'état collées ; PR-R fermée, branche supprimée ; tous les push nommés `refs/heads/preuve/41-ci-rouge`
  </acceptance_criteria>
  <verification>La Task 3 relit depuis l'API les runs, les contextes requis, le rapport `isRequired` et l'agrégat de C0, A et B, relit `main`, l'état des trois PR, l'absence des branches distantes et l'absence de toute fuite de C0, A, B (`PR-R-FUITE:`).</verification>
  <resume-signal>Coller les sorties du pas 0, `<n>`, la sortie de 3, les observations 4, 7 et 10, et les sorties de 11 et 12, puis écrire « M-1 joué ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : lecture M-1 depuis l'API (runs jumeaux, contextes requis, `isRequired`, agrégat), checks requis en échec, verdict, témoin code owner</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-BRANCHE:`, `PR-R-COMMITS:`, `MAIN-AVANT-PR-R:`, `CO-I:`)
  </read_first>
  <action>
Identité `picmakpro`. Lectures seules, aucun geste chez GitHub. Consigner, sous la section 41-07 :
- depuis le collage : `PR-R: #<n> branche=preuve/41-ci-rouge` et
  `M1-ETATS: c0=<état collé à 4> a=<état collé à 7> b=<état collé à 10> lu_par=picmakpro statut=observation` ;
- depuis SA lecture API : `PR-CO-FERMEES: baseline=<state> sentinelle=<state> branches_distantes=<absentes|presentes>` ;
- contextes requis : `gh api repos/picmakpro/vibeflow-os/rulesets/<id de POSE-BRANCHE>` → règle
  `required_status_checks` → `M1-CONTEXTES: ruleset=<id> requis=<nombre de contextes> tests_app15368=<oui|non>`.

Pour chaque commit C0, A, B :
- REST : runs du workflow `CI` de ce `head_sha` et leur événement ; pour chaque run, conclusion des
  quatre jobs et heure de fin du job « Suites de tests (découverte non vide) » ; pour le job rouge,
  confirmer la cause par `gh run view <id> --log-failed` portant
  `FAIL(rc=1) scripts/tests/test-preuve-41-m1.sh` ;
- GraphQL : `gh api graphql` sur `repository(owner, name) { object(oid) { ... on Commit {
  statusCheckRollup { state contexts { nodes { ... on CheckRun { name conclusion
  isRequired(pullRequestNumber: <n>) checkSuite { app { databaseId } workflowRun { event } } } } } } } }`.

Consigner :
- `M1-C0: sha=<> tests_push=<concl> tests_pr=<concl> autres_jobs_verts=<k>/6 rollup=<state> echecs_requis=<k>` ;
- `M1-A: sha=<> rouge=pull_request fin_rouge=<iso> vert=push fin_vert=<iso> ordre_discriminant=<oui|non> cause=suite_preuve autres_jobs_verts=<k>/6 rouge_requis=<isRequired lu du run rouge : true, false, null, ou absent> rollup=<state>` ;
- `M1-B:` symétrique (rouge=push) ;
- `CI-ROUGE-REQUIS: a=<nombre de runs de l'app 15368 rapportés requis et en échec sur A> b=<… sur B> contexte=suites_de_tests merge_tente=non` ;
- `M1-VERDICT: <SUR|DEFAUT|INDETERMINE> main_inchangee=<oui|non> etat_confirme=<oui|non|non_lu>` ;
- `PR-R-FERMEE: state=<> merged=<true|false> branche_distante=<absente|presente>` ;
- `PR-P-APRES-41-07: state=<state relu> merged=<true si mergedAt non nul, false sinon>` — PR-P (#101)
  relue via l'API (`gh pr view <n de PR-P:> --json state,mergedAt`) APRÈS les gestes de la Task 2 ;
  valeur attendue `state=OPEN merged=false` : PR-P reste le support de 41-08 et ne doit être ni
  fermée ni mergée pendant la mesure ;
- `PR-R-FUITE: ancetres=<k> refs_locales=<k> refs_distantes_hors_pull=<k> readme_travail=<inchange|modifie>` — relu après la fermeture et après `git fetch --prune origin` (qui retire une ref de suivi `origin/preuve/41-ci-rouge` restée d'un fetch antérieur à la suppression) : `ancetres` compte les relations d'ascendance de C0, A, B vers la branche de travail et `origin/main`, `refs_locales` les refs locales qui les portent, `refs_distantes_hors_pull` les refs de `git ls-remote origin` qui les portent hors `refs/pull/*` (`refs/pull/<n>/head` reste chez GitHub, hors `main`), `readme_travail` compare la première mention « N suites » des deux README de la branche de travail à celle d'`origin/main` et exige un arbre de travail propre sur `README.md`, `README.fr.md` et `scripts/tests` ;
- `CO-TEMOIN: pr=PR-R fichiers=README.fr.md,README.md,scripts/tests/test-preuve-41-m1.sh review_api=<reviewDecision relu par l'agent, aucune si vide> review_co_i=<valeur de review dans CO-I:> discriminant=<hors_d_atteinte|inattendu> raison=reviewDecision_vide_dans_et_hors_CODEOWNERS`
  — `hors_d_atteinte` seulement si `review_api=aucune` ; sinon `inattendu`, consigné tel quel. Les trois fichiers de PR-R sont hors CODEOWNERS (`/.github/`, baseline, sentinelles, `/scripts/hooks/`).

Règle du verdict : SUR si ordre discriminant oui pour A et B, C0 à agrégat `SUCCESS` sans échec
requis, `main` inchangée depuis `MAIN-AVANT-PR-R:`, et, sur A comme sur B, le run rouge de l'événement
attendu rapporté `isRequired=true` avec agrégat `FAILURE` ; DEFAUT si les quatre premières
conditions tiennent mais qu'un run rouge est rapporté `false` ou que l'agrégat n'est pas `FAILURE` ;
INDETERMINE dans tous les autres cas (run rouge absent, événement inattendu, `isRequired` nul,
ordre non discriminant, `main` bougée). `etat_confirme` : `oui` si C0 n'est ni `BLOCKED` ni `UNKNOWN`
et que A et B sont `BLOCKED`, `non_lu` si une observation manque, `non` sinon — observation, jamais
dans le verdict.

Commit local sur B, message « docs(41-07): M-1 lu sur les runs jumeaux, checks requis en échec sur
une PR rouge jamais mergée (D-04, D-09 — arbitrage Samuel, AskUserQuestion session principale,
2026-09-17 ; D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ». Rejouer
`gates` et `tests` sur l'arbre de travail : seuls sont tolérés les échecs dont le chemin figure dans une entrée `open` de `.planning/WINDOWS.md`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; TJ="Suites de tests (découverte non vide)"; mes() { S="$1"; T=$(mktemp -d); gh api "repos/picmakpro/vibeflow-os/actions/runs?head_sha=$S&per_page=100" --jq ".workflow_runs[] | select(.name==\"CI\") | \"\(.id) \(.event)\"" > "$T/runs"; : > "$T/jobs"; while read -r id ev; do gh api "repos/picmakpro/vibeflow-os/actions/runs/$id/jobs?per_page=100" --jq ".jobs[] | \"$ev\t\(.name)\t\(.conclusion)\t\(.completed_at)\"" >> "$T/jobs"; done < "$T/runs"; tp=$(awk -F"\t" -v j="$TJ" "\$1==\"push\" && \$2==j {print \$3 \"@\" \$4}" "$T/jobs"); tr=$(awk -F"\t" -v j="$TJ" "\$1==\"pull_request\" && \$2==j {print \$3 \"@\" \$4}" "$T/jobs"); ov=$(awk -F"\t" -v j="$TJ" "\$2!=j && \$3==\"success\" {c++} END{print c+0}" "$T/jobs"); echo "$tp $tr $ov"; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); set -- $(mes "$C0"); c0p=${1%@*}; c0r=${2%@*}; c0o=$3; set -- $(mes "$A"); ap=${1%@*}; apt=${1#*@}; ar=${2%@*}; art=${2#*@}; ao=$3; set -- $(mes "$B"); bp=${1%@*}; bpt=${1#*@}; br=${2%@*}; brt=${2#*@}; bo=$3; oa=$([ "$ap" = success ] && [ "$ar" = failure ] && [[ "$art" < "$apt" ]] && echo oui || echo non); ob=$([ "$bp" = failure ] && [ "$br" = success ] && [[ "$bpt" < "$brt" ]] && echo oui || echo non); echo "M-1 recalcule : c0 push=$c0p pr=$c0r autres=$c0o | a push=$ap($apt) pr=$ar($art) ordre=$oa autres=$ao | b push=$bp($bpt) pr=$br($brt) ordre=$ob autres=$bo"; echo "M-1 consigne : a_ordre=$(val M1-A: ordre_discriminant) b_ordre=$(val M1-B: ordre_discriminant) c0_push=$(val M1-C0: tests_push)"; [ "$c0p" = success ] && [ "$c0r" = success ] && [ "$c0o" -eq 6 ] && [ "$ao" -eq 6 ] && [ "$bo" -eq 6 ] && [ "$oa" = "$(val M1-A: ordre_discriminant)" ] && [ "$ob" = "$(val M1-B: ordre_discriminant)" ]'</automated>
    <fails_when>exit non nul ; ou C0 non vert sur les deux événements ; ou un des trois autres jobs non vert dans un run (`autres=` différent de 6 : la cause du rouge ne serait pas la seule suite de preuve) ; ou l'ordre discriminant recalculé diffère de celui consigné (consignation fausse)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-R absente"; exit 1;; esac; TJ="Suites de tests (découverte non vide)"; T=$(mktemp -d); RS=$(val POSE-BRANCHE: id); gh api "repos/picmakpro/vibeflow-os/rulesets/$RS" --jq ".rules[] | select(.type == \"required_status_checks\") | .parameters.required_status_checks[] | \"\(.integration_id)\t\(.context)\"" > "$T/req"; nq=$(awk "END{print NR}" "$T/req"); tq=$(awk -F"\t" -v j="$TJ" "\$1==15368 && \$2==j {c++} END{print c+0}" "$T/req"); rap() { gh api graphql -f query="query { repository(owner:\"picmakpro\", name:\"vibeflow-os\") { object(oid:\"$1\") { ... on Commit { statusCheckRollup { state contexts(first:50) { nodes { ... on CheckRun { name conclusion isRequired(pullRequestNumber:$N) checkSuite { app { databaseId } workflowRun { event } } } } } } } } } }" --jq ".data.repository.object.statusCheckRollup as \$r | \$r.contexts.nodes[] | select(.name != null) | \"\(\$r.state)\t\(.name)\t\(.conclusion)\t\(.isRequired)\t\(.checkSuite.app.databaseId)\t\(.checkSuite.workflowRun.event)\"" > "$T/$2"; }; ro() { awk -F"\t" "NR==1 {print \$1}" "$T/$1"; }; rr() { awk -F"\t" -v j="$TJ" "\$2==j && \$3==\"FAILURE\" && \$5==15368 {print \$4 \"@\" \$6; exit}" "$T/$1"; }; ne() { awk -F"\t" "\$3==\"FAILURE\" && \$4==\"true\" && \$5==15368 {c++} END{print c+0}" "$T/$1"; }; ev() { if [ "$3" != "$4" ] || { [ "$1" != true ] && [ "$1" != false ]; }; then echo indet; elif [ "$1" = true ] && [ "$2" = FAILURE ]; then echo ok; else echo masque; fi; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); rap "$C0" c0; rap "$A" a; rap "$B" b; s0=$(ro c0); sa=$(ro a); sb=$(ro b); n0=$(ne c0); na=$(ne a); nb=$(ne b); ra=$(rr a); rb=$(rr b); qa=${ra%@*}; ea=${ra#*@}; qb=${rb%@*}; eb=${rb#*@}; [ -n "$ra" ] || { qa=absent; ea=aucun; }; [ -n "$rb" ] || { qb=absent; eb=aucun; }; xa=$(ev "$qa" "$sa" "$ea" pull_request); xb=$(ev "$qb" "$sb" "$eb" push); oa=$(val M1-A: ordre_discriminant); ob=$(val M1-B: ordre_discriminant); m0=$(awk "\$1==\"MAIN-AVANT-PR-R:\" {print \$2; exit}" "$PH/41-PREUVES.md"); mi=$([ "$(gh api repos/picmakpro/vibeflow-os/commits/main --jq .sha)" = "$m0" ] && echo oui || echo non); c0ok=$([ "$s0" = SUCCESS ] && [ "$n0" -eq 0 ] && echo oui || echo non); if [ "$oa" = oui ] && [ "$ob" = oui ] && [ "$c0ok" = oui ] && [ "$mi" = oui ] && [ "$xa" != indet ] && [ "$xb" != indet ]; then if [ "$xa" = ok ] && [ "$xb" = ok ]; then att=SUR; else att=DEFAUT; fi; else att=INDETERMINE; fi; e0=$(val M1-ETATS: c0); e1=$(val M1-ETATS: a); e2=$(val M1-ETATS: b); if [ -z "$e0" ] || [ -z "$e1" ] || [ -z "$e2" ]; then ec=non_lu; else ec=non; case "$e0" in (BLOCKED|UNKNOWN) ;; (*) [ "$e1" = BLOCKED ] && [ "$e2" = BLOCKED ] && ec=oui;; esac; fi; v=$(awk "\$1==\"M1-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); echo "M-1 rapport GitHub : contextes_requis=$nq tests_app15368=$tq | c0 rollup=$s0 echecs_requis=$n0 | a rollup=$sa rouge=$ra echecs_requis=$na lecture=$xa | b rollup=$sb rouge=$rb echecs_requis=$nb lecture=$xb | main_inchangee=$mi etat_confirme=$ec (observation) | verdict attendu=$att consigne=$v"; [ "$nq" -eq 4 ] && [ "$tq" -eq 1 ] && [ "$(val M1-CONTEXTES: requis)" = "$nq" ] && [ "$(val M1-C0: rollup)" = "$s0" ] && [ "$(val M1-A: rouge_requis)" = "$qa" ] && [ "$(val M1-A: rollup)" = "$sa" ] && [ "$(val M1-B: rouge_requis)" = "$qb" ] && [ "$(val M1-B: rollup)" = "$sb" ] && [ "$(val CI-ROUGE-REQUIS: a)" = "$na" ] && [ "$(val CI-ROUGE-REQUIS: b)" = "$nb" ] && [ "$(val CI-ROUGE-REQUIS: merge_tente)" = non ] && [ "$(val M1-VERDICT: main_inchangee)" = "$mi" ] && [ "$(val M1-VERDICT: etat_confirme)" = "$ec" ] && [ "$v" = "$att" ] && [ "$na" -ge 1 ] && [ "$nb" -ge 1 ] && [ "$v" = SUR ]'</automated>
    <fails_when>exit non nul ; ou contextes requis du ruleset autres que 4, ou contexte des tests non épinglé sur l'app 15368 ; ou une valeur consignée (`M1-CONTEXTES`, `rollup`, `rouge_requis`, `CI-ROUGE-REQUIS`, `main_inchangee`, `etat_confirme`) différente de la relecture API (consignation fausse, corrigée ici) ; ou `CI-ROUGE-REQUIS` inférieur à 1 sur A ou sur B, ou `merge_tente` autre que `non` ; ou verdict consigné différent du verdict recalculé ; ou verdict `DEFAUT` / `INDETERMINE` — dans ces deux cas la consignation reste faite et le plan s'arrête sur la Task 4 (décision humaine), jamais sur une correction de `ci.yml`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-R absente"; exit 1;; esac; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); mg=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergedAt --jq ".mergedAt != null"); NB=$(awk "\$1==\"PR-BASELINE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); NS=$(awk "\$1==\"PR-SENTINELLE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); sb=$(gh pr view "$NB" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); ss=$(gh pr view "$NS" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); NP=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$NP" in ""|*[!0-9]*) echo "PR-P absente"; exit 1;; esac; sp=$(gh pr view "$NP" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); T=$(mktemp -d); git ls-remote --heads origin preuve/41-ci-rouge > "$T/r"; git ls-remote --heads origin preuve/41-codeowner-baseline preuve/41-codeowner-sentinelle > "$T/co"; bd=$(awk "END{print (NR==0 ? \"absente\" : \"presente\")}" "$T/r"); nco=$(awk "END{print NR}" "$T/co"); fs=$(val PR-R-FERMEE: state); fm=$(val PR-R-FERMEE: merged); fb=$(val PR-R-FERMEE: branche_distante); cfb=$(val PR-CO-FERMEES: baseline); cfs=$(val PR-CO-FERMEES: sentinelle); cfd=$(val PR-CO-FERMEES: branches_distantes); pps=$(val PR-P-APRES-41-07: state); ppm=$(val PR-P-APRES-41-07: merged); echo "fermetures : PR-R etat=$st mergee=$mg branche=$bd (consigne $fs/$fm/$fb) | baseline=$sb sentinelle=$ss branches_distantes=$nco (consigne $cfb/$cfs/$cfd) | PR-P #$NP=$sp (consigne $pps/$ppm)"; [ "$st" = CLOSED ] && [ "$mg" = false ] && [ "$bd" = absente ] && [ "$fs" = CLOSED ] && [ "$fm" = false ] && [ "$fb" = absente ] && [ "$sb" = CLOSED:false ] && [ "$ss" = CLOSED:false ] && [ "$nco" -eq 0 ] && [ "$cfb" = CLOSED ] && [ "$cfs" = CLOSED ] && [ "$cfd" = absentes ] && [ "$sp" = OPEN:false ] && [ "$pps" = OPEN ] && [ "$ppm" = false ]'</automated>
    <fails_when>exit non nul ; ou PR-R non `CLOSED`, mergée, ou branche `preuve/41-ci-rouge` encore présente ; ou PR-BASELINE ou PR-SENTINELLE non `CLOSED`, mergée, ou branche distante encore présente ; ou PR-P (#101) relue via l'API après les gestes de la Task 2 autrement que `state=OPEN` sans date de merge (fermée, ou mergée : le support de 41-08 aurait été touché pendant la mesure), ou numéro `PR-P:` absent ; ou consignation de fermeture différente de la lecture API, dont `PR-P-APRES-41-07:` absente ou autre que `state=OPEN merged=false`</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; C0=$(val PR-R-COMMITS: c0); A=$(val PR-R-COMMITS: a); B=$(val PR-R-COMMITS: b); T=$(mktemp -d); an=0; for s in "$C0" "$A" "$B"; do for ref in HEAD origin/main; do if git merge-base --is-ancestor "$s" "$ref"; then an=$((an + 1)); fi; done; done; git for-each-ref --format="%(objectname) %(refname)" > "$T/lr"; lr=$(awk -v c="$C0" -v a="$A" -v b="$B" "\$1==c || \$1==a || \$1==b {n++} END{print n+0}" "$T/lr"); git ls-remote origin > "$T/rr"; rr=$(awk -v c="$C0" -v a="$A" -v b="$B" "(\$1==c || \$1==a || \$1==b) && index(\$2, \"refs/pull/\") != 1 {n++} END{print n+0}" "$T/rr"); git status --porcelain -- README.md README.fr.md scripts/tests > "$T/st"; st=$(awk "END{print NR}" "$T/st"); pm() { git show "$1:$2" > "$T/r"; awk "match(\$0, /[0-9]+ suites/) { s=substr(\$0, RSTART, RLENGTH); sub(/ suites\$/, \"\", s); print s; exit }" "$T/r"; }; rw=$([ "$(pm HEAD README.md)" = "$(pm origin/main README.md)" ] && [ "$(pm HEAD README.fr.md)" = "$(pm origin/main README.fr.md)" ] && [ "$st" -eq 0 ] && echo inchange || echo modifie); echo "fuite : ancetres=$an refs_locales=$lr refs_distantes_hors_pull=$rr readme_travail=$rw | consigne ancetres=$(val PR-R-FUITE: ancetres) refs_locales=$(val PR-R-FUITE: refs_locales) refs_distantes_hors_pull=$(val PR-R-FUITE: refs_distantes_hors_pull) readme_travail=$(val PR-R-FUITE: readme_travail)"; [ "$an" -eq 0 ] && [ "$lr" -eq 0 ] && [ "$rr" -eq 0 ] && [ "$rw" = inchange ] && [ "$(val PR-R-FUITE: ancetres)" = "$an" ] && [ "$(val PR-R-FUITE: refs_locales)" = "$lr" ] && [ "$(val PR-R-FUITE: refs_distantes_hors_pull)" = "$rr" ] && [ "$(val PR-R-FUITE: readme_travail)" = "$rw" ]'</automated>
    <fails_when>exit non nul ; ou C0, A ou B ancêtre de la branche de travail ou d'`origin/main` (la preuve a été fusionnée quelque part) ; ou une ref locale, ou une ref distante hors `refs/pull/*`, pointe encore sur C0, A ou B ; ou `README.md`, `README.fr.md` ou `scripts/tests` modifiés ou non suivis dans l'arbre de travail, ou première mention « N suites » des README de la branche de travail différente de celle d'`origin/main` ; ou consignation `PR-R-FUITE:` différente de la relecture</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-R absente"; exit 1;; esac; fl=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json files --jq "[.files[].path] | sort | join(\",\")"); ra=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json reviewDecision --jq ".reviewDecision // \"\""); [ -n "$ra" ] || ra=aucune; tf=$(val CO-TEMOIN: review_api); tc=$(val CO-TEMOIN: review_co_i); ci=$(val CO-I: review); d=$(val CO-TEMOIN: discriminant); att=$([ "$ra" = aucune ] && echo hors_d_atteinte || echo inattendu); echo "temoin code owner : fichiers_pr_r=$fl review_api=$ra (consigne $tf) review_co_i=$ci (consigne $tc) discriminant attendu=$att consigne=$d"; [ "$fl" = README.fr.md,README.md,scripts/tests/test-preuve-41-m1.sh ] && [ "$tf" = "$ra" ] && [ -n "$ci" ] && [ "$tc" = "$ci" ] && [ "$d" = "$att" ] && [ "$d" = hors_d_atteinte ]'</automated>
    <fails_when>exit non nul ; ou PR-R touche autre chose que la suite de preuve et les deux README (tous trois hors CODEOWNERS) ; ou `review_api` consigné différent de la relecture API, ou `review_co_i` différent de `CO-I:` ; ou `discriminant` consigné différent du recalcul ; ou `discriminant=inattendu` — un `reviewDecision` non vide sur une PR hors CODEOWNERS contredirait la lecture de 41-06 : consigné tel quel, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; k=0; t=0; : > "$T/corps"; for key in PR-R; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "NUMERO DE PR ABSENT : $key"; k=$((k+1)); continue;; esac; t=$((t+1)); printf "@@CORPS@@ %s #%s\n" "$key" "$N" >> "$T/corps"; gh pr view "$N" --repo picmakpro/vibeflow-os --json body --jq .body >> "$T/corps"; done; r=0; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@CORPS@@ / { if (lib != \"\") juge(m, lib); lib=\$0; m=\"\"; next } { m = m \" \" \$0 } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } if (index(m, \"D-02ter\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") == 0) { print \"D-02ter SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis|D-02ter/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (lib != \"\") juge(m, lib); print \"corps de PR : en_defaut=\" v+0; exit (v > 0) }" "$T/arb" "$T/corps" || r=$?; echo "corps de PR : lus=$t numeros_absents=$k"; [ "$r" -eq 0 ] && [ "$k" -eq 0 ] && [ "$t" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou un corps de PR qui cite D-02bis sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 », D-02ter sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 », ou D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » ; ou un corps qui cite `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » égal aux valeurs CAPTURÉES ; ou un numéro de PR non consigné</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); awk -F"|" "{f=\$5; s=\$8; gsub(/ /, \"\", f); gsub(/ /, \"\", s); if (s==\"open\" && f ~ /\\/tests\\/test-[^\\/]*\\.sh\$/) print f}" .planning/WINDOWS.md > "$T/ledger"; awk "/FAIL\\(rc=[0-9]+\\) / {sub(/.*FAIL\\(rc=[0-9]+\\) /, \"\"); print}" "$T/t" > "$T/ko"; awk "NR==FNR {l[\$0]=1; next} !(\$0 in l)" "$T/ledger" "$T/ko" > "$T/hl"; hl=$(awk "END{print NR}" "$T/hl"); nk=$(awk "END{print NR}" "$T/ko"); awk "{print \"HORS LEDGER : \" \$0}" "$T/hl"; d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); p=$(find plugin scripts -type f -name "test-preuve-41-*" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b echecs_ledger=$((nk - hl)) echecs_hors_ledger=$hl decouverte_independante=$d suite_de_preuve_dans_l_arbre=$p"; [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq "$nk" ] && [ "$hl" -eq 0 ] && [ "$p" -eq 0 ] && { [ "$rc" -eq 0 ] || [ "$2" -gt 0 ]; }'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, ou N différent de la découverte indépendante ; ou un échec dont le chemin ne figure dans aucune entrée `open` de `.planning/WINDOWS.md` (ligne `HORS LEDGER :` ; seuls les rouges d'environnement déjà tracés sont tolérés, #6 et #7 sur ce poste au 2026-09-24, comme `REJEU-TESTS-41-05:`) ; ou nombre d'échecs du bilan différent du nombre de lignes `FAIL` ; ou `suite_de_preuve_dans_l_arbre=` différent de 0 (la suite jetable a fui dans la branche de travail). Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } if (index(m, \"D-02ter\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") == 0) { print \"D-02ter SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis|D-02ter/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` (un commit de plan cite D-02bis sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 ») ; ou `D-02ter SANS CANAL NI DATE` (un commit de plan cite D-02ter sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 ») ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan cite `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » égal aux valeurs CAPTURÉES — frontmatter de 41-01 ou ligne `*-DECISION:` de `41-PREUVES.md`) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - C0, A, B relus depuis l'API : rouge dû à la seule suite de preuve, ordre discriminant consigné conforme
    - contextes requis relus (4, tests épinglés sur 15368) ; `rouge_requis` et agrégat de A et B consignés = relecture GraphQL ; verdict M-1 consigné = recalculé ; `SUR` pour poursuivre
    - `CI-ROUGE-REQUIS:` au moins 1 sur A et sur B, `merge_tente=non` ; observations d'état consignées, `etat_confirme` recalculé, hors verdict
    - PR-R, PR-BASELINE et PR-SENTINELLE `CLOSED`, jamais mergées, branches supprimées ; PR-P (#101) relue `OPEN` sans date de merge après les gestes (`PR-P-APRES-41-07: state=OPEN merged=false`) ; corps de PR et messages de commit conformes (D-02ter et `PR-R-DECISION` compris)
    - `PR-R-FUITE:` : ni C0 ni A ni B ancêtre de la branche de travail ou d'`origin/main`, aucune ref locale ni distante hors `refs/pull/*`, README de la branche de travail inchangés
    - `CO-TEMOIN: … discriminant=hors_d_atteinte`
  </acceptance_criteria>
  <done>M-1 est tranché par la lecture de ce que GitHub rapporte, et une PR rouge porte des checks requis en échec — sans qu'aucune tentative de merge n'ait eu lieu ni qu'aucun rouge ne touche `main`.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `M1-VERDICT` ≠ SUR ou `CO-TEMOIN: … discriminant` ≠ `hors_d_atteinte` (sinon tâche sans objet, consignée « non déclenchée »)</name>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`M1-*`, `CI-ROUGE-REQUIS:`, `CO-TEMOIN:`, `MAIN-AVANT-PR-R:`)
  </read_first>
  <action>L'exécutant présente la mesure et s'arrête ; aucune modification de `ci.yml`, de CODEOWNERS ni des rulesets, et aucun geste chez GitHub, sans la réponse.</action>
  <decision>Le rapport de GitHub masque-t-il un run rouge par son jumeau vert (DEFAUT), la lecture est-elle non concluante (INDETERMINE), ou le témoin code owner est-il inattendu — et que faire avant la trace de 41-08 ?</decision>
  <context>
    Lire `M1-CONTEXTES:`, `M1-C0:`, `M1-A:`, `M1-B:`, `CI-ROUGE-REQUIS:`, `M1-VERDICT:`, `M1-ETATS:` et
    `CO-TEMOIN:` dans `41-PREUVES.md`. D-M4 a prévu « une mesure, ou la suppression du doublon dans
    `ci.yml` si elle s'impose (alors QUAL-01 s'applique) » ; le manager a exclu de planifier cette
    suppression d'avance. Le planificateur ne tranche pas. PR-R est déjà fermée ; PR-P (support de
    41-08) est encore ouverte : elle reste ouverte sauf en cas de retour arrière. Un `INDETERMINE`
    peut venir d'un merge concurrent sur `main` (`MAIN-AVANT-PR-R:`) ou d'un `isRequired` nul. Les
    observations d'état (`M1-ETATS:`) ne sont pas un refus de merge (D-02ter).
  </context>
  <options>
    <option id="arret-et-cadrage">
      <name>ARRÊT ET CADRAGE</name>
      <pros>Aucune modification sans arbitrage ; le défaut est consigné et remonté avec sa mesure ; PR-P reste ouverte et non mergée ; les plans 41-08 à 41-13 attendent.</pros>
      <cons>La phase reste ouverte avec des règles posées dont un check requis serait masquable.</cons>
    </option>
    <option id="rejouer-la-mesure">
      <name>REJOUER LA MESURE (INDETERMINE seulement)</name>
      <pros>Un `main` qui a bougé ou un ordre non discriminant se corrige par un nouveau jeu C0/A/B.</pros>
      <cons>Nouvelle PR jetable, nouveaux gestes humains, nouveaux runs CI.</cons>
    </option>
    <option id="accepter-et-documenter">
      <name>ACCEPTER ET DOCUMENTER</name>
      <pros>La phase continue (D-02ter) ; la lecture de M-1 et du témoin est écrite telle que mesurée dans ADR-072 § Mesures (41-10) et au ledger.</pros>
      <cons>Le jumeau qui compte n'est pas établi, ou un masquage est consigné sans correction ; exige un arbitrage explicite, daté et attribué.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE DES RULESETS</name>
      <pros>Rien d'imposé tant que le défaut n'est pas traité (`enforcement=disabled`, commande dans `41-PREUVES.md`) ; PR-P est fermée sans merge (`gh pr close` puis suppression de sa branche).</pros>
      <cons>Retour à la situation antérieure : `main` protégée par rien.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - réponse explicite obtenue, jamais inférée ; `M1-DECISION:` consignée et committée
  </acceptance_criteria>
  <resume-signal>Répondre « non déclenchée » (verdict SUR et témoin `hors_d_atteinte`), « ARRÊT ET CADRAGE », « REJOUER LA MESURE », « ACCEPTER ET DOCUMENTER » ou « RETOUR ARRIÈRE DES RULESETS ». Après la réponse, l'exécutant consigne `M1-DECISION: option=<id> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint>` — jamais inféré, jamais une date recopiée d'ailleurs — et le commite sur B avec le message « docs(41-07): M1-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `M1-DECISION` sans ce canal et cette date rougit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR rouge → `main` | la PR jetable porte une suite qui échoue et un compte « N suites » aligné sur elle : son atterrissage rougirait `main` et y ferait entrer une suite jetable |
| commits jetables → branche de travail B | C0, A, B vivent dans le même dépôt local que la branche B, qui sera livrée sur `main` |
| deux runs de même nom → check requis | un run vert pourrait masquer un run rouge |
| lecture d'état par un compte de la liste → preuve | un état lu par `picmakpro` pourrait être présenté comme un refus |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-26 | Tampering | PR rouge mergée par erreur | high | mitigate | aucune tentative de merge sur PR-R, par aucun moyen (D-02ter) ; les lectures d'état ne conditionnent aucun geste ; état relu `CLOSED`, `mergedAt` nul, branche distante absente (vérifié) |
| T-41-27 | Spoofing | check requis satisfait par le run vert du jumeau | high | mitigate | ordre discriminant (vert le plus récent) ; `isRequired` du run rouge et agrégat relus par l'agent sur A et B ; verdict recalculé depuis l'API ; arrêt sur DEFAUT |
| T-41-28 | Tampering | rouge obtenu en modifiant un gate | medium | mitigate | C0 limité à la suite de preuve et à la première mention « N suites » de `README.md` et `README.fr.md` (une ligne chacun, N = suites réelles de C0), A et B à la seule suite : `autres_fichiers=0`, `plugin=0`, aucun gate, aucun chemin CODEOWNERS (bloc 1 de la Task 1, mutants tués) ; `gates` rejoué vert sur C0 et B avant publication ; cause du rouge vérifiée dans le journal du job |
| T-41-29 | Repudiation | mesure faussée par un merge concurrent sur `main` | medium | mitigate | `MAIN-AVANT-PR-R:` relue ; hors PR en vol, seules les PR de preuve ouvertes ; fenêtre sans merge convenue avec Samuel (`PR-EN-VOL-41-06:`) ; INDETERMINE sinon ; PR-P relue ouverte et non mergée après les gestes (`PR-P-APRES-41-07:`, vérifié) |
| T-41-63 | Repudiation | `reviewDecision` pris pour une preuve de revue par chemin | medium | mitigate | `CO-TEMOIN: discriminant=hors_d_atteinte` exigé et recalculé ; valeur inattendue → Task 4 |
| T-41-89 | Repudiation | état de merge lu par un compte de la liste présenté comme un refus | medium | mitigate | `M1-ETATS:` marqué `statut=observation`, hors verdict ; `etat_confirme` distinct et recalculé ; aucun refus écrit sans tentative |
| T-41-93 | Tampering | README aligné ou suite de preuve atterrissant sur `main` par un autre chemin que PR-R (C0 fusionné dans la branche de travail, fichier écrit dans l'arbre de travail, push d'un compte `always` avec un refspec erroné) | high | mitigate | C0, A, B construits hors branche par plomberie, jamais portés par une ref locale ni écrits dans l'arbre de travail ; push avec le refspec complet `refs/heads/preuve/41-ci-rouge` (Task 2) ; `PR-R-FUITE:` recalculé (aucun ancêtre de la branche de travail ni d'`origin/main`, aucune ref locale ni distante hors `refs/pull/*`, README de la branche de travail inchangés) ; `main_inchangee` ; `suite_de_preuve_dans_l_arbre=0` ; un « 86 suites » entré seul sur la branche de travail rougirait `check-version-sync` aux rejeux `gates`. Résidu accepté : `refs/pull/<n>/head` reste chez GitHub, hors `main`, non supprimable |
| T-41-94 | Repudiation | rouge réel masqué par la tolérance des rouges d'environnement locaux | low | mitigate | tolérance limitée aux chemins des entrées `open` de `.planning/WINDOWS.md`, lus dans le ledger et non écrits dans le plan ; tout autre échec imprimé `HORS LEDGER :` et rougit ; la CI réelle, qui n'a pas ces rouges, reste la référence (M1-C0) |
</threat_model>

<verification>
M-1 tranché par la lecture de ce que GitHub rapporte (contextes requis, `isRequired`, agrégat),
recalculée depuis l'API ; checks requis en échec lus sur une PR rouge ; aucune tentative de merge ;
PR jetables fermées sans merge ; C0 vert sur les quatre checks requis, prouvé en local avant
publication puis relu en CI ; aucune fuite de C0, A, B hors de la branche jetable.
</verification>

<success_criteria>
M-1 consigné ; partie lisible du critère 2 prouvée (une PR rouge porte des checks requis en échec) ;
refus par défaut non tenté et dit hors d'atteinte dans ce plan (D-02ter) ; aucune modification de
`ci.yml` ni d'aucun gate ; les deux README de la branche de travail et de `main` inchangés.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-07-SUMMARY.md` when done — committé sur la branche B. Le SUMMARY écrit que le refus d'un merge sans contournement n'a pas été tenté (D-02ter) et que les états lus par `picmakpro` sont des observations ; il cite `PR-R-PREMISSE:` et `PR-R-DECISION: option=REPLANIFIER` (arbitrage Willy, AskUserQuestion session principale, 2026-09-24), dit que C0 a aligné la mention « N suites » des deux README sur la seule branche jetable, et reprend `PR-R-REJEU-LOCAL:` et `PR-R-FUITE:`.

Règle STATE.md (ADR-063) : jamais `gsd-tools state.*` ni `gsd_run query commit` sur `.planning/workstreams/fiabilite/STATE.md` ; éditions à la main dans le corps seulement ; dans le frontmatter, seuls `last_updated`, `last_activity`, `last_activity_desc` (quatre lignes au plus) et les nombres de `progress` changent ; le frontmatter se ferme avant la ligne 60.
</output>

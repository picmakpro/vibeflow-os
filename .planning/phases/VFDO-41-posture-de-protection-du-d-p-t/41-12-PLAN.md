---
phase: 41-posture-de-protection-du-d-p-t
plan: 12
lot: L12 — tag annoté, release GitHub, check-release-tag --remote, invariants de la phase
type: execute
wave: 12
depends_on: ["41-11"]
autonomous: false
requirements: [PROT-02, PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Critère 3 complet : la PR des preuves est `MERGED` sans suite `bypass` sur son commit de merge ; tag annoté `v<N>` sur ce commit, créé sans contournement (création libre, D-07) ; release GitHub existante ; `bash scripts/check-release-tag.sh --remote` rend 0 avec `✓` (PROT-02)"
    - "Traces de la release (point 6 du tour 1) : suites du merge et du tag relues par l'agent via l'API quand le compte write y a accès (`relue=oui`), sinon collées par le compte lecteur et marquées `relue=non_relue`"
    - "Le rouge attendu de l'étape `check-release-tag` du job `gates` au push de merge sur `main` (tag posé après) est consigné comme signal « tag à poser » ; sa relance après le tag est un choix humain consigné, pas une obligation (D-M5)"
    - "Invariants : `scripts/check-release-tag.sh`, `scripts/hooks/pre-push` et `.github/workflows/ci.yml` n'ont été modifiés par aucun merge de la phase (PR de la phase, PR-P si mergée, PR des preuves) ; aucun chemin `plugin/dev-orchestrator/` ; aucun `check-*.sh` ni `tests/test-*.sh` ajouté (critère 4, QUAL-01, D-M11)"
    - "La branche C `docs/phase-41-cloture` est créée depuis `origin/main` APRÈS le merge de la PR des preuves ; le corps de la PR des preuves est relu (canal et date si un arbitrage D-01..D-10 y est cité)"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`PR-RELEASE:`, `RELEASE:`, `CI-MAIN-RELEASE:`, `INVARIANTS-PHASE:`"
  key_links:
    - "Le ledger est coché sur ces lignes au plan 41-13, sur la même branche C"
---

<objective>
Terminer le rejeu du flux de release sous la règle et vérifier les invariants de la phase.

Purpose: critères de succès 3 (PROT-02) et 4 (QUAL-01).
Output: tag et release publiés, `check-release-tag` ✓, invariants consignés sur la branche C.

Budget (point 9 du tour 1) : première moitié de l'ancien 41-11, scindé pour tenir sous trois tâches.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-11-SUMMARY.md
@CLAUDE.md
@scripts/check-release-tag.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1 : relecture du merge de la PR des preuves et de son corps, branche C, notes de release préparées</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2 de 41-11
    - CHANGELOG.md (entrée `## [v<N>]`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin --tags`. Relire la PR des preuves : `MERGED`, commit
de merge, `VERSION` de ce commit = `N` (celle de B). Créer la branche C en local :
`git switch -c docs/phase-41-cloture origin/main` (arbre propre exigé).

Consigner dans `41-PREUVES.md`, section `## 41-12 — release` :
`PR-RELEASE: #<n> merge=<sha> version=v<N> etat_avant_merge=<état collé> commande=gh_pr_merge_--merge`.

Écrire hors dépôt (fichier temporaire, chemin imprimé) les notes de release : résumé repris de
l'entrée CHANGELOG `v<N>` et liste `git log --oneline v<version précédente>..<merge>`. Imprimer les
commandes exactes de la Task 2 avec `<N>`, `<sha>` et le chemin des notes. Commit local sur C,
message « docs(41-12): merge de la PR des preuves relu, branche de clôture ouverte ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); N=$(awk "\$1==\"PR-RELEASE:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-RELEASE absente"; exit 1;; esac; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); hr=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefName --jq .headRefName); M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); TV=$(mktemp -d); git show "$M:VERSION" > "$TV/v"; vm=$(tr -d "[:space:]" < "$TV/v"); vc=$(val PR-RELEASE: version); mc=$(val PR-RELEASE: merge); ea=$(val PR-RELEASE: etat_avant_merge); br=$(git branch --show-current); vt=$(tr -d "[:space:]" < VERSION); echo "PR des preuves : identite=$u pr=$N branche=$hr etat=$st merge=$M version_merge=$vm version_consignee=$vc merge_consigne=$([ "$mc" = "$M" ] && echo oui || echo non) etat_avant_merge=$ea branche_courante=$br version_arbre=$vt"; [ "$u" = samuel-neveugall ] && [ "$st" = MERGED ] && [ "$hr" = feat/phase-41-preuves-release ] && [ "$vm" = "$vc" ] && [ "$mc" = "$M" ] && [ "$ea" = CLEAN ] && [ "$br" = docs/phase-41-cloture ] && [ "$vt" = "$vm" ]'</automated>
    <fails_when>exit non nul ; ou PR des preuves non `MERGED` ou autre branche ; ou version du commit de merge différente de celle consignée ; ou merge consigné différent du relu ; ou `etat_avant_merge=` différent de `CLEAN` (merge obtenu hors du chemin normal : arrêt) ; ou branche courante différente de C ; ou `VERSION` de l'arbre différente de celle du merge</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; k=0; t=0; : > "$T/corps"; for key in PR-RELEASE; do N=$(awk -v k="$key:" "\$1==k { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "NUMERO DE PR ABSENT : $key"; k=$((k+1)); continue;; esac; t=$((t+1)); printf "@@CORPS@@ %s #%s\n" "$key" "$N" >> "$T/corps"; gh pr view "$N" --repo picmakpro/vibeflow-os --json body --jq .body >> "$T/corps"; done; r=0; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@CORPS@@ / { if (lib != \"\") juge(m, lib); lib=\$0; m=\"\"; next } { m = m \" \" \$0 } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (lib != \"\") juge(m, lib); print \"corps de PR : en_defaut=\" v+0; exit (v > 0) }" "$T/arb" "$T/corps" || r=$?; echo "corps de PR : lus=$t numeros_absents=$k"; [ "$r" -eq 0 ] && [ "$k" -eq 0 ] && [ "$t" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou un corps de PR qui cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 » ; ou un corps qui fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal aux valeurs CAPTURÉES (frontmatter de 41-01 ou `41-PREUVES.md`) ; ou un numéro de PR non consigné</fails_when>
  </verify>
  <acceptance_criteria>
    - PR des preuves `MERGED` depuis B, état `CLEAN` avant merge, version `v<N>` au commit de merge ; corps conforme
    - branche C créée depuis `origin/main` ; `PR-RELEASE:` consignée ; notes de release prêtes hors dépôt
  </acceptance_criteria>
  <done>Le tag peut être posé sur le bon commit, avec ses notes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — tag annoté, release GitHub, relance facultative, lecture des rule suites</name>
  <action>Samuel, compte `samuel-neveugall`, pose et publie le tag annoté sur le commit de merge, crée la release GitHub, décide de relancer ou non le job rouge de `main`, et fait lire les rule suites par le compte lecteur.</action>
  <instructions>
Compte qui agit pour 1-4 : `samuel-neveugall`. Valeurs imprimées par la Task 1.

1. `git fetch origin && git tag -a v<N> -m "v<N> — posture de protection du dépôt (Phase 41)" <sha-merge> && git push origin v<N>`
   → copier la sortie (aucun message de contournement attendu : création libre).
2. `gh release create v<N> --repo picmakpro/vibeflow-os --title "v<N> — posture de protection du dépôt" --notes-file <chemin-des-notes> --verify-tag`
3. Run CI du push de merge sur `main` :
   `gh run list --repo picmakpro/vibeflow-os --commit <sha-merge> --json databaseId,event,conclusion --jq '.[] | select(.event=="push")'`
   → copier. L'échec du job « Gates de qualité (mode strict) » à l'étape `check-release-tag` est le
   signal attendu (tag posé après le merge, D-M5).
4. Facultatif (D-M5, relance possible, non systématique) : `gh run rerun <databaseId> --repo picmakpro/vibeflow-os --failed`
   → écrire « relance : oui » ou « relance : non ».
5. Lecture des rule suites par le compte lecteur de `ACCES-RULE-SUITES:` :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=day&rule_suite_result=all" --jq '.[] | select(.after_sha == "<sha-merge>" or .ref == "refs/tags/v<N>") | {id, ref, actor_name, after_sha, result}'`
  </instructions>
  <verification>La Task 3 relit le tag (annoté, cible), la release, joue `check-release-tag.sh --remote`, relit les runs de `main` et les suites (ou les marque non relues).</verification>
  <resume-signal>Coller les sorties de 1, 2, 3, le choix de 4 et la sortie de 5, puis écrire « release publiée ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : critère 3 prouvé, suites relues ou marquées, invariants de la phase, rejeu</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`PR-PHASE:`, `MAIN-APRES-PR-P:`, `PR-RELEASE:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin --tags`. Jouer `bash scripts/check-release-tag.sh --remote`
sur la branche C. Consigner :
- `RELEASE: tag=v<N> annote=<oui|non> cible=<sha> cible_est_merge=<oui|non> release=<url> check_release_tag_remote_rc=<rc> suite_merge=<pass|bypass|absente> suite_merge_id=<id|absente> suite_tag=<pass|bypass|absente> suite_tag_id=<id|absente> relue=<oui|non_relue>` —
  `relue=oui` seulement si l'agent a lui-même relu les suites via l'API (compte write avec accès) ;
- `CI-MAIN-RELEASE: gates_premier_run=<conclusion> relance=<oui|non> gates_apres=<conclusion|sans_objet>` ;
- `INVARIANTS-PHASE: merges=<sha phase>,<sha PR-P ou aucun>,<sha release> gates_ajoutes=<k> suites_ajoutees=<k> ci_yml_touche=<oui|non> check_release_tag_touche=<oui|non> pre_push_touche=<oui|non> zone_interdite_touchee=<oui|non>`,
  mesurés sur `git diff --name-status <merge>^1 <merge>` de chaque merge (PR-P ignorée si non mergée).
Commit local sur C, message « docs(41-12): release v<N> prouvée, invariants de phase (D-07 —
arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M=$(val PR-RELEASE: merge); V=$(val PR-RELEASE: version); ty=$(git cat-file -t "refs/tags/$V" 2>/dev/null || echo absent); tc=$(git rev-parse "refs/tags/$V^{commit}" 2>/dev/null || echo absent); TL=$(mktemp -d); git ls-remote --tags origin "refs/tags/$V" > "$TL/td"; td=$(awk "END{print NR}" "$TL/td"); rl=$(gh release view "$V" --repo picmakpro/vibeflow-os --json tagName --jq .tagName 2>/dev/null || echo absente); rc=0; out=$(bash scripts/check-release-tag.sh --remote 2>&1) || rc=$?; ok=$(printf "%s\n" "$out" | awk "index(\$0, \"✓\") {c++} END{print c+0}"); sm=$(val RELEASE: suite_merge); stg=$(val RELEASE: suite_tag); crc=$(val RELEASE: check_release_tag_remote_rc); echo "critere 3 : tag=$V type=$ty cible_est_merge=$([ "$tc" = "$M" ] && echo oui || echo non) tag_distant=$td release=$rl check_release_tag_remote_rc=$rc sortie_ok=$ok rc_consigne=$crc suite_merge=$sm suite_tag=$stg"; [ "$ty" = tag ] && [ "$tc" = "$M" ] && [ "$td" -eq 1 ] && [ "$rl" = "$V" ] && [ "$rc" -eq 0 ] && [ "$ok" -ge 1 ] && [ "$crc" = "$rc" ] && [ "$sm" != bypass ] && [ "$stg" != bypass ]'</automated>
    <fails_when>exit non nul ; ou tag absent, léger (non annoté) ou hors du commit de merge ; ou tag non publié ; ou release absente ; ou `check_release_tag_remote_rc=` différent de 0 ou sortie sans `✓` ; ou rc consigné différent du rc rejoué ; ou `suite_merge=bypass` / `suite_tag=bypass` (le rejeu n'est plus « sans contournement »)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; acc=$(val ACCES-RULE-SUITES: samuel-neveugall); M=$(val PR-RELEASE: merge); V=$(val PR-RELEASE: version); sm=$(val RELEASE: suite_merge); smi=$(val RELEASE: suite_merge_id); stg=$(val RELEASE: suite_tag); sti=$(val RELEASE: suite_tag_id); rl=$(val RELEASE: relue); k=0; { [ -n "$sm" ] && [ -n "$stg" ] && [ -n "$smi" ] && [ -n "$sti" ]; } || { echo "champs de suite non consignes"; exit 1; }; if [ "$acc" = ok ]; then [ "$rl" = oui ] || k=$((k+1)); if [ "$smi" != absente ]; then a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$smi" --jq "\"\(.result) \(.after_sha)\""); [ "$a" = "$sm $M" ] || { echo "SUITE MERGE DIFFERENTE : $a"; k=$((k+1)); }; fi; if [ "$sti" != absente ]; then a=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$sti" --jq "\"\(.result) \(.ref)\""); [ "$a" = "$stg refs/tags/$V" ] || { echo "SUITE TAG DIFFERENTE : $a"; k=$((k+1)); }; fi; else [ "$rl" = non_relue ] || k=$((k+1)); fi; echo "relecture release : acces_write=$acc suite_merge=$sm ($smi) suite_tag=$stg ($sti) relue=$rl en_defaut=$k"; [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou champs de suite absents ; avec accès du compte write : `relue` différent de `oui`, ou résultat, `after_sha` ou `ref` consignés différents de la relecture API ; sans accès : `relue` différent de `non_relue` (valeurs collées par le compte lecteur, jamais présentées comme relues par l'agent)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M1=$(val PR-PHASE: merge); M2=$(val MAIN-APRES-PR-P: merge); M3=$(val PR-RELEASE: merge); T=$(mktemp -d); : > "$T/ns"; for m in "$M1" "$M2" "$M3"; do [ -n "$m" ] || { echo "merge absent"; exit 1; }; [ "$m" = aucun ] && continue; git merge-base --is-ancestor "$m" origin/main || { echo "merge $m hors main"; exit 1; }; git diff --name-status "$m^1" "$m" >> "$T/ns"; done; ga=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /(^|\\/)check-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); sa=$(awk -F"\t" "\$1==\"A\" && \$2 ~ /\\/tests\\/test-[^\\/]*\\.sh\$/ {c++} END{print c+0}" "$T/ns"); ci=$(awk -F"\t" "\$NF==\".github/workflows/ci.yml\" {c++} END{print c+0}" "$T/ns"); crt=$(awk -F"\t" "\$NF==\"scripts/check-release-tag.sh\" {c++} END{print c+0}" "$T/ns"); pp=$(awk -F"\t" "\$NF==\"scripts/hooks/pre-push\" {c++} END{print c+0}" "$T/ns"); zi=$(awk -F"\t" "index(\$NF, \"plugin/dev-orchestrator/\")==1 {c++} END{print c+0}" "$T/ns"); cg=$(val INVARIANTS-PHASE: gates_ajoutes); cs=$(val INVARIANTS-PHASE: suites_ajoutees); echo "invariants : gates_ajoutes=$ga (consigne $cg) suites_ajoutees=$sa (consigne $cs) ci_yml=$ci check_release_tag=$crt pre_push=$pp zone_interdite=$zi"; [ "$ga" -eq 0 ] && [ "$sa" -eq 0 ] && [ "$ci" -eq 0 ] && [ "$crt" -eq 0 ] && [ "$pp" -eq 0 ] && [ "$zi" -eq 0 ] && [ "$cg" = "$ga" ] && [ "$cs" = "$sa" ]'</automated>
    <fails_when>exit non nul ; ou un des trois merges absent ou hors `main` ; ou `gates_ajoutes=` / `suites_ajoutees=` différent de 0 (un gate est né : QUAL-01 s'applique, mutation rouge exigée, remonter) ; ou `ci.yml`, `check-release-tag.sh`, `pre-push` modifiés par la phase (D-M5, D-M9) ; ou zone interdite touchée ; ou consignation différente de la mesure</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - critère 3 : tag annoté sur le commit de merge, publié, release existante, `check-release-tag.sh --remote` rc 0 avec `✓`, aucune suite `bypass` sur le merge ni sur le tag
    - suites relues via l'API (compte write avec accès) ou marquées `non_relue`
    - invariants : 0 gate et 0 suite ajoutés, `ci.yml`/`check-release-tag.sh`/`pre-push` intacts, zone interdite intacte (critère 4)
    - rejeux verts ; messages de commit conformes
  </acceptance_criteria>
  <done>Le flux de release est rejoué sous la règle et les invariants de la phase sont mesurés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poste de Samuel → tags `v*` | création du tag de release sous le ruleset de tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-42 | Repudiation | critère 3 déclaré sans rejeu réel | high | mitigate | tag, release et `check-release-tag.sh --remote` relus et rejoués par l'agent ; rc consigné = rc rejoué |
| T-41-44 | Tampering | gate né en silence pendant la phase | medium | mitigate | ajouts `check-*.sh` et `tests/test-*.sh` mesurés sur les merges de la phase ; QUAL-01 déclenché sinon |
| T-41-45 | Denial of service | gate de release ou hook modifié par la phase | medium | mitigate | `ci.yml`, `check-release-tag.sh`, `pre-push` vérifiés intacts |
| T-41-54 | Repudiation | trace de release présentée comme relue sans l'être | medium | mitigate | `relue=oui` exigé seulement avec relecture API ; sinon `non_relue` |
</threat_model>

<verification>
Tag annoté et release publiés sans contournement, `check-release-tag` ✓, invariants de phase tenus.
</verification>

<success_criteria>
Critères de succès 3 et 4 de la Phase 41 prouvés et consignés.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-12-SUMMARY.md` when done — committé sur la branche C.
</output>

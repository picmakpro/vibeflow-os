---
phase: 30-portabilit-windows-ii
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - docs/HOOKS-CONTRAT-SORTIE.md
  - docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md
  - plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh
  - plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
  - plugin/dev-orchestrator/scripts/check-doc-drift.sh
  - plugin/dev-orchestrator/scripts/check-gsd-config.sh
  - plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh
autonomous: true
requirements: [PORT-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "L'inventaire des entrées de hook du dépôt est RECOMPTÉ à la machine et fait foi : 25 entrées réelles sur 6 modules, chacune avec son événement, son script, son mode d'invocation et son classement advisory ou bloquante EXPLICITE (D-07, D-08, contrat §7)."
    - "Le contrat de sortie vers le harness est normalisé DANS chaque script : sous le drapeau de mode hook, le code de silence interne devient 0 ; sans ce drapeau, tous les codes restent identiques (D-06)."
    - "Aucun lanceur intermédiaire n'est introduit : il n'existe pas de script d'enveloppe qui absorberait les codes de sortie (D-06, l'indirection shell sous Windows contredirait la forme exec)."
    - "Un démarrage de session sain reste SILENCIEUX au sens fort : stdout strictement vide, pas seulement code de sortie 0 (Pitfall 2 — sur SessionStart, stdout sur exit 0 est injecté comme contexte)."
    - "L'affirmation aujourd'hui exacte « le drapeau de mode hook ne change ni les sorties ni le rendu », présente dans les en-têtes des 4 scripts, devient fausse par construction et est corrigée dans le MÊME commit que le changement de comportement (Pitfall 1)."
    - "Un écart d'inventaire (une 26e entrée apparue, une entrée disparue) fait échouer bruyamment le contrôle de recomptage, jamais passer en silence (probe boundary)."
  artifacts:
    - docs/HOOKS-CONTRAT-SORTIE.md
    - plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh
  key_links:
    - "Traduction conditionnée au drapeau de mode hook ↔ suites existantes : un test qui appelle le script SANS le drapeau et attend le code de silence doit rester vert — s'il rougit, la normalisation a été faite au mauvais niveau."
    - "Inventaire ↔ migration en forme exec (plan 30-07) : une entrée non classée est une entrée qu'on migre à l'aveugle."
  prohibitions:
    - statement: "Ne jamais renommer globalement le code de silence interne en code 0 dans les scripts : la CLI et les suites existantes testent ce code sans le drapeau de mode hook, et le renommage global les casserait (RESEARCH Pitfall 1)."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais introduire de lanceur unique d'enveloppe des hooks — explicitement rejeté par D-06."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais laisser un chemin nominal silencieux écrire sur stdout, ni sortir non nul, à la frontière du harness."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais déduire le classement advisory/bloquant du type d'événement : une garde PreToolUse peut bloquer par décision JSON en sortant 0, et un SessionStart peut sortir non nul (RESEARCH Pitfall 4)."
      status: flagged-unverified
      verification: flagged-unverified
---

<objective>
Le lot (b) de l'ordre imposé par la spec : les codes de sortie. `|| true` disparaît par construction
en forme exec — ce qui porte l'advisory aujourd'hui n'existera plus. Avant de migrer la moindre
entrée `SessionStart`, chaque script doit traduire lui-même son silence interne en silence à la
frontière du harness (D-06), et chaque entrée du parc doit être classée advisory ou bloquante
explicitement (contrat §7).

Ce plan livre l'inventaire machine-recompté des 25 entrées (D-08) et la normalisation des 4 scripts
du périmètre dev. La normalisation du reste du parc suit au plan 30-06 ; la migration des 4 entrées
en forme exec suit au plan 30-07.

Purpose: rendre le silence nominal contractuel au lieu d'être masqué par un opérateur shell.
Output: un document de contrat de sortie durable, 4 scripts normalisés, une suite qui capture stdout et stderr séparément.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-CONTEXT.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-RESEARCH.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-PATTERNS.md
@docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : inventaire machine des 25 entrées + contrat de sortie écrit</name>
  <files>docs/HOOKS-CONTRAT-SORTIE.md, docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md</files>
  <read_first>
    - Les 6 fragments `plugin/*/hooks/hooks.json`, lus intégralement — c'est la seule source de
      vérité du nombre d'entrées.
    - Pour chaque script référencé par une entrée, son en-tête et ses sites de sortie (relevé
      mécanique : les lignes de sortie et la présence d'un drapeau de mode hook). Les 21 scripts de
      la polarité gouvernance n'ont jamais été lus systématiquement (RESEARCH, Question ouverte 2) —
      c'est ici que ça se fait.
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-RESEARCH.md` §Pitfall 4 (le classement
      n'est PAS déductible de l'événement) et §Diagramme de traduction du code de sortie.
    - `docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md` §3.3 et §3.4 — les
      chiffres périmés (22 entrées, 17 avec l'opérateur d'absorption) à corriger.
    - Le contrat amont : `git show origin/gouvernance/contrat-portabilite:docs/CONTRAT-PORTABILITE.md`
      §4 (« n'a pas pu tourner » est un troisième état) et §7 (classement explicite exigé).
  </read_first>
  <action>
    Créer `docs/HOOKS-CONTRAT-SORTIE.md`, document DURABLE (pas un artefact de phase : Willy en
    hérite pour la migration des 20 entrées gouvernance). Contenu, en français :

    1. **Le contrat de sortie**, en trois lignes : à la frontière du harness, 0 = rien à signaler ou
       signal émis normalement ; non nul = le script n'a pas pu faire son travail ; le code
       réservé au blocage par le harness (2) n'est jamais émis involontairement. Le contrat de
       signaux interne de la Phase 17 reste valide À L'INTÉRIEUR des scripts : c'est sa traduction
       qui change, pas sa grammaire.
    2. **La règle de traduction** : la traduction est conditionnée au drapeau de mode hook, jamais
       globale. Sans le drapeau (CLI, suites de tests), tous les codes sont inchangés. Dire
       pourquoi : la CLI et les suites testent ces codes, un renommage global les casserait.
    3. **Le silence est un contrat de FLUX autant que de code** : sur `SessionStart`, stdout sur
       sortie 0 est injecté comme contexte ; un chemin nominal silencieux doit donc avoir un stdout
       strictement vide. Les diagnostics vont sur stderr.
    4. **L'inventaire** : un tableau d'une ligne par entrée, produit par relevé MACHINE sur les 6
       fragments, colonnes : module, événement, matcher, script, arguments d'invocation, drapeau de
       mode hook accepté (oui/non), codes de sortie atteignables aujourd'hui, classement
       **advisory** ou **bloquante** avec la raison (pour une bloquante : dire PAR QUOI elle bloque
       — décision JSON ou code de sortie), forme actuelle (shell/exec), et action requise par cette
       phase (normalisation, migration, ou rien).
    5. **Le décompte** : le total par module et le total général, et la commande exacte qui le
       reproduit. L'inventaire du plan fait foi (D-08).
    6. **Ce qui reste à la polarité gouvernance** : la migration effective de ses 20 entrées en
       forme exec, et le fait que leurs scripts sont normalisés côté script par le plan 30-06 sans
       que leurs fragments ne soient touchés — l'ajout du drapeau de mode hook à leurs lignes
       d'invocation appartient à cette migration-là.

    Points de vigilance pour le classement, à ne pas déduire mécaniquement : la garde de taille de
    fichier est bloquante alors qu'elle sort 0 (elle bloque par décision JSON) ; la garde de fin de
    session de `planning-core` est bloquante par code de sortie et c'est VOULU (ne jamais la
    normaliser) ; les 4 entrées `SessionStart` de `dev-orchestrator` sont advisory par ADR-031.

    Puis mettre à jour la spec (D-08) : au §3.3 et au §3.4, remplacer les chiffres périmés par les
    chiffres recomptés, et renvoyer vers le nouveau document pour le détail. Ne pas réécrire la
    spec : édition ciblée des tableaux et des phrases qui portent un nombre.
  </action>
  <verify>
    <automated>python3 -c "import json,glob; n=sum(len(h.get('hooks',[])) for f in sorted(glob.glob('plugin/*/hooks/hooks.json')) for gs in json.load(open(f))['hooks'].values() for h in gs); print(n); assert n==25, n"</automated>
  </verify>
  <acceptance_criteria>
    - La commande de recomptage ci-dessus rend `25` et sort 0. Si le dépôt en compte un autre nombre, elle ÉCHOUE bruyamment (assertion), et l'inventaire ET l'assertion sont mis à jour ensemble, jamais l'un sans l'autre.
    - `docs/HOOKS-CONTRAT-SORTIE.md` contient exactement 25 lignes de tableau d'entrées (contrôle : le nombre de lignes du tableau d'inventaire commençant par un pipe et nommant un fichier de script est 25).
    - Chaque ligne porte un classement qui vaut littéralement `advisory` ou `bloquante` — aucune cellule vide, aucun « à déterminer ».
    - Les 6 modules sont représentés, avec les sous-totaux : conductor 6, consolidator 7, dev-orchestrator 4, infrastructure-audit 1, planning-core 6, software-architecture 1.
    - La spec ne contient plus le décompte périmé de 22 entrées : `grep -c '22 entrées' docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md` == 0.
    - Le document nomme explicitement les deux entrées bloquantes et le mécanisme de blocage de chacune.
  </acceptance_criteria>
  <done>L'inventaire recompté fait foi, chaque entrée est classée, et la spec ne ment plus sur ses chiffres.</done>
</task>

<task type="auto">
  <name>Tâche 2 : normaliser la sortie des 4 scripts du périmètre dev</name>
  <files>plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh, plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh, plugin/dev-orchestrator/scripts/check-doc-drift.sh, plugin/dev-orchestrator/scripts/check-gsd-config.sh</files>
  <read_first>
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` — l'en-tête l.1-80 (les 4 états, la
      grammaire des codes, et le paragraphe l.45-51 qui affirme que le drapeau de mode hook ne
      change rien), le gate de mutuelle exclusion l.101-105, `say()` l.107, et TOUS les sites de
      sortie (relevés : l.91, 104, 275, 283, 310, 319, 322, 327).
    - `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` — sites l.60, 73, 87, 101, 150, 162, 168.
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` — sites l.74, 80, 93, 100, 120, 132, 149, 153.
    - `plugin/dev-orchestrator/scripts/check-gsd-config.sh` — sites l.264, 280, 290, 310, 315, 605, 708, 712.
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-PATTERNS.md` §check-dev-bootstrap — le
      patron exact de traduction et la citation du commentaire à corriger.
    - `docs/HOOKS-CONTRAT-SORTIE.md` produit par la tâche 1 — le contrat que ces 4 scripts doivent respecter.
  </read_first>
  <action>
    Même refactor, répliqué à l'identique dans les 4 scripts.

    (1) **Un point de traduction unique et nommé.** Ajouter une fonction shell — nom imposé pour
    que le grep soit un contrôle et non une devinette : `hook_exit` — déclarée juste après `say()`,
    donc après le parsing des arguments et après le gate de mutuelle exclusion. Contrat de la
    fonction : elle prend un code en argument ; si le mode hook est armé ET que le code est celui du
    silence interne (3), elle sort 0 ; dans tous les autres cas elle sort le code reçu tel quel.
    C'est une traduction, pas un masquage : elle ne touche ni au code d'erreur d'argument (64), ni
    au code 0, ni à aucun autre.

    (2) **Router les sorties de silence.** Remplacer chaque site de sortie portant le code de
    silence interne par un appel à `hook_exit` avec ce code. Les sites sont listés ligne par ligne
    dans la lecture préalable ci-dessus — les traiter TOUS, y compris celui de l'état « projet
    complètement cadré » de `check-dev-bootstrap.sh` qui IMPRIME sur stdout avant de sortir (c'est
    la rupture assumée documentée l.14 : ce chemin porte un signal et doit devenir 0 pour que le
    signal soit injecté, exactement comme aujourd'hui via l'opérateur d'absorption).
    Ne pas toucher aux sites qui sortent 0 (déjà conformes) ni aux sites d'erreur d'argument.

    (3) **Corriger la documentation devenue fausse.** Le paragraphe d'en-tête des 4 scripts affirme
    que le drapeau de mode hook n'altère ni les codes de sortie ni le rendu — vrai aujourd'hui,
    faux dès que l'opérateur d'absorption disparaît du fragment. Le réécrire dans le MÊME commit
    que le changement de comportement : dire que le drapeau arme désormais la traduction du code de
    silence vers le harness, que le rendu reste inchangé, et que sans le drapeau tous les codes sont
    ceux documentés. Renvoyer vers `docs/HOOKS-CONTRAT-SORTIE.md`.

    (4) **Vérifier la discipline des flux** (Pitfall 2) : `say()` doit rester strictement sur
    stderr dans les 4 scripts ; aucun chemin nominal silencieux ne doit écrire sur stdout. Si un
    chemin silencieux écrit quoi que ce soit sur stdout, c'est un défaut à corriger ici — c'est la
    seule modification de rendu autorisée par ce plan.

    Ne toucher à aucun `hooks.json` : la migration en forme exec est le plan 30-07, et l'ordre
    (b) avant (c) est la condition de non-régression du parc.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - Dans chacun des 4 scripts, `grep -c 'hook_exit' &lt;fichier&gt;` ≥ 3 (la définition + au moins deux sites routés).
    - Dans chacun des 4 scripts, aucune ligne de code (commentaires exclus) ne sort encore directement le code de silence : `grep -v '^[[:space:]]*#' &lt;fichier&gt; | grep -cE '^[[:space:]]*exit 3([[:space:]]|$)'` == 0.
    - Comportement CLI inchangé, prouvé par exécution : chacun des 4 scripts appelé SANS le drapeau de mode hook sur un répertoire vide rend le code de silence documenté (3), identique à avant le changement.
    - Comportement hook : chacun des 4 scripts appelé AVEC le drapeau sur ce même répertoire rend 0 et un stdout strictement vide.
    - Le gate de mutuelle exclusion est intact : drapeau de mode hook + drapeau silencieux ensemble ⇒ code 64 et message sur stderr.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` reste vert (les cas existants appellent ces scripts sans le drapeau et attendent les codes documentés — s'ils rougissent, la traduction a été posée au mauvais niveau).
    - `grep -c 'run-hook' plugin/dev-orchestrator/scripts/*.sh` == 0 (aucun lanceur d'enveloppe introduit, D-06).
    - Les 4 en-têtes ne portent plus l'affirmation devenue fausse : le paragraphe corrigé nomme la traduction et renvoie au document de contrat.
  </acceptance_criteria>
  <done>Les 4 scripts traduisent leur silence à la frontière du harness sous le drapeau de mode hook, et seulement là ; leur contrat CLI est intact et leur documentation dit la vérité.</done>
</task>

<task type="auto">
  <name>Tâche 3 : suite de contrat de sortie — stdout et stderr capturés SÉPARÉMENT</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh</files>
  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` — le harness du module
      (`ok()`/`ko()`/`skip()`, isolation `mktemp`, fixtures de projet), à imiter sans le modifier.
    - Les 4 scripts tels que normalisés par la tâche 2.
    - `docs/HOOKS-CONTRAT-SORTIE.md` — les assertions que la suite doit rendre exécutables.
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-RESEARCH.md` §Pitfall 2 et §Wave 0 Gaps
      (« aucun test ne capture stdout et stderr séparément » — c'est le trou que cette suite ferme).
  </read_first>
  <action>
    Créer une suite NEUVE dédiée au contrat de sortie, plutôt qu'étendre la suite du module :
    elle a besoin d'un harness particulier (capture de deux flux dans deux fichiers distincts) et
    elle doit rester lisible comme la preuve d'un contrat, pas se diluer dans 700 lignes.

    Harness : pour chaque exécution, rediriger stdout et stderr dans DEUX fichiers distincts sous
    `mktemp -d`, relever le code de sortie, puis asserter les trois dimensions séparément (code,
    contenu de stdout, contenu de stderr). Ne jamais fusionner les flux dans l'assertion — c'est
    précisément la fusion qui a rendu ce défaut invisible jusqu'ici.

    Matrice de cas, pour CHACUN des 4 scripts (donc 4 blocs symétriques) :
    - **cas silencieux, sans le drapeau de mode hook** ⇒ code de silence documenté (3), stdout vide.
      C'est le cas de non-régression CLI.
    - **cas silencieux, avec le drapeau** ⇒ code 0 ET stdout STRICTEMENT vide (zéro octet, pas même
      une ligne blanche). C'est le critère 5 de la spec, tenu par un test et non par relecture.
    - **cas avec signal, avec le drapeau** ⇒ code 0 et stdout non vide (le signal est bien injecté :
      la normalisation ne doit pas éteindre les signaux au passage).
    - **erreur d'argument, avec le drapeau** ⇒ code 64, message sur stderr, stdout vide (une vraie
      erreur reste une vraie erreur).
    - **mutuelle exclusion** ⇒ code 64 avec les deux drapeaux ensemble.

    Trois issues par cas (QUAL-01) : succès ; échec explicite nommant la dimension fautive (code,
    stdout ou stderr) ; échec BRUYANT si l'exécution est imparsable (script introuvable, code
    inattendu hors du contrat) — jamais un `skip` silencieux, jamais un vert par défaut.

    Prouver la discrimination par MUTATION et citer la trace du rouge dans le SUMMARY : (m1)
    faire sortir `hook_exit` avec le code reçu quel que soit le mode ⇒ les cas « silencieux avec
    drapeau » deviennent rouges ; (m2) faire sortir 0 même sans le drapeau ⇒ les cas de
    non-régression CLI deviennent rouges ; (m3) faire écrire une ligne sur stdout dans un chemin
    silencieux ⇒ le cas de stdout strictement vide devient rouge, alors que le code de sortie reste
    bon (c'est la mutation qui prouve que la suite teste bien le FLUX et pas seulement le code).
    Restaurer après chaque mutation.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh` sort 0 et affiche « 0 KO ».
    - La suite couvre les 4 scripts (le nom de chacun apparaît dans au moins 4 cas) et au moins 20 cas au total.
    - La suite est découverte par la CI : `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c 'test-hook-exit-contract.sh'` == 1.
    - Aucune assertion ne fusionne les deux flux : `grep -c '2>&amp;1' plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh` == 0.
    - La suite tourne hors ligne et sans dépendance à l'état du dépôt courant (fixtures sous `mktemp -d`, jamais le `.planning/` réel).
    - Les trois mutations m1/m2/m3 ont été jouées et leur sortie rouge est citée dans le SUMMARY (nom du cas + dimension fautive).
  </acceptance_criteria>
  <done>Le silence nominal est prouvé par exécution sur les deux flux, et la suite rougit quand on le casse — y compris quand on ne casse que le flux.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script de hook → harness Claude Code | Le code de sortie et le stdout deviennent, respectivement, un signal d'erreur visible et du contexte injecté dans la session. |
| frontmatter/état du lab → stdout d'un hook | Les scripts réimpriment des valeurs lues dans `STATE.md` sur le stdout d'un hook `SessionStart`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04-01 | Denial of Service | code de sortie non nul à chaque démarrage de session | high | mitigate | Traduction du code de silence vers 0 sous le drapeau de mode hook, posée AVANT toute migration de fragment (ordre (b) avant (c)) ; prouvée par la suite de contrat de sortie sur les 4 scripts. |
| T-30-04-02 | Information Disclosure | stdout d'un chemin nominal silencieux | medium | mitigate | Assertion de stdout STRICTEMENT vide sur les chemins silencieux, flux capturés séparément ; mutation m3 prouve que l'assertion mord sur le flux et pas seulement sur le code. |
| T-30-04-03 | Tampering | réimpression de valeurs du frontmatter sur le stdout d'un hook | medium | accept | Garde déjà en place (`ws_readable` + validation avant réimpression, `check-dev-bootstrap.sh` l.316-322) ; ce plan ne l'affaiblit pas et ne l'étend pas. Acceptation bornée à ce vecteur, jamais au risque en bloc (ADR-070). |
| T-30-04-04 | Spoofing | classement advisory/bloquant déduit au lieu d'être établi | medium | mitigate | Classement explicite par entrée, avec le mécanisme de blocage nommé ; recomptage machine qui échoue bruyamment sur écart. |
| T-30-04-SC | Tampering | installs npm/pip/cargo | low | accept | Aucun paquet installé par ce plan. |
</threat_model>

<assumptions>
- **A-30-04-1 (frontière d'inventaire, PORT-03)** — le décompte de 25 est arrêté au 2026-08-15 et
  re-vérifié à l'exécution par une assertion machine. Si une entrée apparaît en cours de phase, la
  commande de recomptage échoue et impose de mettre à jour inventaire et assertion ensemble. Le
  plan ne prétend pas geler le dépôt : il rend l'écart bruyant.
- **A-30-04-2 (parité d'invocation)** — les 4 entrées dev passent déjà le drapeau de mode hook dans
  leur ligne d'invocation, donc la traduction est active dès la migration du fragment. Ce n'est PAS
  vrai pour 4 entrées gouvernance (plan 30-06) : leur invocation ne porte pas le drapeau, et
  l'ajouter appartient à la migration que la polarité gouvernance conduira.
</assumptions>

## Artifacts this phase produces (plan 30-04)

- `docs/HOOKS-CONTRAT-SORTIE.md` — contrat de sortie + inventaire classé des 25 entrées + commande de recomptage
- Fonction shell `hook_exit` — dans `check-dev-bootstrap.sh`, `discover-unintegrated-docs.sh`, `check-doc-drift.sh`, `check-gsd-config.sh`
- `plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh` — suite de contrat de sortie (≥ 20 cas, deux flux capturés séparément)
- Chiffres corrigés au §3.3 et §3.4 de `docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md`

<verification>
- La commande de recomptage rend 25 et sort 0
- `bash plugin/dev-orchestrator/scripts/tests/test-hook-exit-contract.sh` → 0 KO
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` → vert (non-régression CLI)
- Les 4 scripts : code de silence sans le drapeau, 0 et stdout vide avec le drapeau
- Les trois mutations rendent la nouvelle suite rouge
</verification>

<success_criteria>
1. Les 25 entrées sont inventoriées et classées explicitement (PORT-03, D-07, D-08).
2. Les 4 scripts dev traduisent leur silence vers le harness, et seulement sous le drapeau de mode hook (D-06).
3. Le silence nominal est prouvé sur stdout, pas seulement sur le code de sortie.
4. Aucune suite existante ne rougit ; aucun lanceur d'enveloppe n'est introduit.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-30-portabilit-windows-ii/30-04-SUMMARY.md` à la fin.
</output>

---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 07
type: execute
wave: 6
depends_on: ["23-03", "23-06"]
files_modified:
  - plugin/dev-orchestrator/references/mission-flow.md
  - plugin/dev-orchestrator/references/mission-contracts.md
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [GSDC-08, GSDC-09]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le budget de tours est UN budget par ÉTAPE, partagé entre la boucle de revue et la boucle de comblement — trois tours de l'une puis trois de l'autre sur le même problème rebaptisé n'est plus possible (D-27)."
    - "Budget épuisé produit le statut de blocage ET un décompte complet de ce qui a été tenté — plus jamais un « bloqué après 3 tours » qui masquait jusqu'à neuf tentatives (D-28)."
    - "Le rapport écrit explicitement que le nombre de réparations consommées à l'intérieur d'un plan par le moteur est INVISIBLE, au lieu d'inventer un chiffre (D-26, verdict du RESEARCH)."
    - "Les budgets eux-mêmes sont inchangés : rien n'est plafonné avant d'avoir des chiffres réels (D-25)."
    - "Quatre briques dormantes ont un moment déclencheur écrit, sous forme de table de FAITS constatables, au gabarit déjà employé par la doctrine documentaire (D-23, D-24)."
    - "Le manager ne debug pas : il redispatche le worker en mandat de debug, qui invoque le skill dédié, et aucun agent nu de debug n'est dispatché en corps de prompt nulle part dans le module (D-22, VOLET DISPATCH — le seul que ce plan livre)."
    - "ÉCART OUVERT, hors périmètre de ce plan, remonté à l'arbitrage humain (ADR-031) : l'allowlist `tools:` de `vf-coder.md` porte encore l'entrée d'agent nu de debug, ce qui contredit D-22 (« aucune exception »). Aucune vérité ni aucun critère de ce plan n'affirme la conformité à D-22 tant que l'arbitrage n'est pas rendu."
  artifacts:
    - plugin/dev-orchestrator/references/mission-flow.md
    - plugin/dev-orchestrator/references/mission-contracts.md
    - plugin/dev-orchestrator/agents/vf-dev-manager.md
  key_links:
    - "Le budget partagé est ce qui ferme le contournement mécanique : deux budgets séparés se contournent par renommage du problème, un budget partagé ne se contourne pas."
    - "Le décompte EST la livraison de la zone 7 — sans lui, le budget partagé ne serait qu'un chiffre plus petit, pas une information."
    - "La mention explicite de l'invisibilité amont est ce qui empêche le décompte de mentir par omission : un manque nommé vaut mieux qu'un chiffre inventé."
    - "La table des briques dormantes reprend le gabarit déjà lu par le manager pour l'hygiène documentaire — même forme, même réflexe, aucun format nouveau à apprendre."
---

<objective>
Fermer les Lacunes 7 (budgets de boucle additionnés en silence) et 4 (briques routées mais jamais
mobilisées en mission).

Purpose zone 7 : la réparation de nœud du moteur est active par défaut avec un budget de 2 — sur
échec de vérification, le moteur tente seul plusieurs conduites avant d'escalader. Les trois tours
de la boucle de revue et ceux de la boucle de comblement s'empilent donc **par-dessus** deux
réparations déjà consommées à l'intérieur de chaque tour : un blocage « après 3 tours » a en
réalité pu coûter jusqu'à neuf tentatives, jamais écrit, jamais consigné. Pire, deux budgets
séparés se contournent mécaniquement — trois tours de revue épuisés, puis trois tours de comblement
sur le même problème rebaptisé, soit exactement le raffinage infini que la doctrine dit vouloir
empêcher.

Purpose zone 4 : quatre briques sont routées (couverture vérifiée machine) et **jamais** mobilisées
en mission ; et le manager, face à un bug récalcitrant, n'a **rien** après la recherche
documentaire.

Output : budget de tours **partagé par étape**, statut de blocage assorti d'un **décompte complet**
avec l'invisibilité amont nommée, table de moments déclencheurs pour quatre briques dormantes, et
mandat de debug qui passe par le skill — sans rouvrir la porte que le plan 23-05 vient de fermer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-06-SUMMARY.md
@CLAUDE.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md`

**Champs neufs du bloc typé** : `gate`, `reprise`, `verdicts`, `decompte` (ce plan)

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » ·
`mission-flow.md` §Pattern E budget partagé par étape + §table des moments déclencheurs des briques
dormantes (ce plan) · `docs/ADR.md` ADR-061 troisième objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24` à `T33` (ce plan : `T31`, `T32`)

**Variables d'environnement neuves** : `VF_CONFIG_PATH`, `VF_GSD_CORE_LIB`, `VF_CAPS_INDEX_OUT`,
`VF_GSD_TOOLS`

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` / `3` / `64`

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10`

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`).
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`).
- **Gate de schéma ORM : sans objet.**
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan n'écrit que de la doctrine markdown et des assertions bash — aucun SDK, aucun service
  tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

<tasks>

<task type="tracer">
  <name>Tâche 1 : un budget de tours par étape, partagé, et un décompte qui ne ment pas (D-25, D-26, D-27, D-28)</name>
  <files>plugin/dev-orchestrator/references/mission-flow.md, plugin/dev-orchestrator/references/mission-contracts.md, plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - ⚠ **ANCRAGE PAR CHAÎNE, JAMAIS PAR NUMÉRO DE LIGNE — règle de ce plan.** Les numéros de ligne que ce plan portait ont été **mesurés faux** et purgés : ils envoyaient l'exécutant dans la mauvaise section (Pattern E annoncé « 168-247 » alors qu'il court de `## Pattern E` à `## Pattern F`, bloc `T23` annoncé « ~1672-1726 » alors qu'il commence ~280 lignes plus bas). Le plan 23-05 avait été purgé de ses numéros pour la même raison, et c'est ce qui l'a sauvé. **Localise par recherche de chaîne. Ne réintroduis aucun numéro de ligne** — un numéro est périmé dès la première insertion, y compris celles de ce plan.
    - plugin/dev-orchestrator/references/mission-flow.md §Pattern E INTÉGRALEMENT — du titre `## Pattern E — Étage revue de premier rang` jusqu'au titre `## Pattern F` **exclu**. Ancres internes : le budget de la boucle de correction sur la chaîne `Budget **3 tours**` (sous-section `### 2. Dispatch et boucle de correction`) ; le rendement mesuré de la revue de jointure sur la chaîne `meilleur rendement mesuré` (sous-section `### 4. Revue de jointure`) ; et la sous-section `### 5. Garde-fou de comblement` avec sa formule « garant MACHINE, pas une consigne », vocabulaire à réutiliser tel quel. **Lis §5 : il est dans Pattern E**, et l'ancienne fourchette de lignes l'excluait tout en exigeant sa lecture.
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §`## Orchestration par étape`, **point 3 (Vérification)** — ancre : la chaîne `son propre budget et son propre`. ⚠ **Rectificatif mesuré** : cette phrase n'est **pas** au §Contrôle de flux, et elle ne qualifie **pas** la boucle de comblement. Elle clôt le point 3 (**Vérification** — test + audit) et l'oppose à la **boucle de revue** du point 2 : « Boucle distincte de celle de la revue (point 2), qui a son propre budget et son propre `reopen` ». C'est ce doublement-là — **vérification ↔ revue** — qui vit dans le manager et qu'il faut fermer.
    - **Trois boucles, un seul budget — pourquoi le grain est l'ÉTAPE.** D-27 énonce le partage « entre la boucle de revue et la boucle de comblement » ; le manager, lui, sépare **vérification** et **revue**. Ce ne sont pas deux énoncés contradictoires mais **trois boucles** dans la même étape. Le grain retenu (l'**étape**, D-27) les couvre toutes : un budget d'étape ne se contourne par aucun renommage, quel que soit le nom de la boucle qui repart. N'écris pas une liste fermée de boucles — écris le **grain**.
    - plugin/dev-orchestrator/references/mission-contracts.md §Rapport de mission — le gabarit compact étendu au plan 23-06
    - .planning/phases/VFDO-23-.../23-RESEARCH.md §« D-26 : le champ de traçabilité node_repair existe-t-il en amont ? — RÉPONSE TRANCHÉE » — le verdict est **NON** : le journal amont est de la prose libre dans une section markdown, aucun champ structuré, aucune trace dans les gabarits de rapport de plan
    - $HOME/.claude/gsd-core/workflows/execute-plan.md ~:330-345 — les budgets de réparation, pour comprendre ce qui se consomme à l'intérieur d'un plan
    - .planning/phases/VFDO-23-.../23-06-SUMMARY.md — le budget de densité restant pour `vf-dev-manager.md`
  </read_first>
  <action>
Rendre le coût réel **lisible** sans toucher aux budgets eux-mêmes, et fermer le contournement
mécanique. Slice vertical : doctrine de boucle → contrat de rapport → agent qui la pilote → gate.

**1. `mission-flow.md` §Pattern E** — remplacer (pas doubler) la mention actuelle du budget par un
**budget partagé par ÉTAPE**, dans le même vocabulaire que le §5 existant :

- le budget de tours est **UN** budget, au grain **étape**, **partagé** entre la boucle de
  correction de revue et la boucle de comblement de manques. Écrire le motif, parce que c'est lui
  qui a de la valeur : deux budgets séparés se contournent mécaniquement — trois tours de revue
  épuisés, puis trois tours de comblement sur le même problème rebaptisé, soit exactement le
  raffinage infini que ce Pattern dit vouloir empêcher ;
- le grain est l'**étape** et non la mission : partagé par mission, il pénaliserait les étapes
  tardives d'une longue mission, qui hériteraient d'un budget déjà entamé par d'autres ;
- **la valeur du budget ne change pas** (D-25) : le rendement de la boucle a été mesuré à son
  nombre de tours actuel, on ne plafonne pas avant d'avoir des chiffres réels.

Ajouter ensuite une sous-section courte **« Épuisement du budget »** :
- budget épuisé ⇒ statut de **blocage** au rapport typé, **plus** un **décompte complet** de ce qui
  a été tenté : tours de revue consommés, tours de comblement consommés, et findings restés non
  résolus. Écrire que **le décompte EST la livraison** : sans lui, un budget partagé ne serait
  qu'un chiffre plus petit, pas une information ;
- **l'invisibilité amont, nommée (D-26)** : le décompte porte les tours d'équipe — ceux que VibeFlow
  pilote et compte lui-même — et **écrit explicitement** que le nombre de réparations consommées à
  l'intérieur d'un plan par le moteur est **invisible** sans parser la prose libre de chaque rapport
  de plan, ce qui dépend d'un format non contractuel. Ne **jamais** fabriquer un total agrégé qui
  laisserait croire à une mesure exhaustive. Formuler comme un fait daté, vérifiable, avec sa
  source (le journal amont est de la prose dans une section markdown, et aucun gabarit de rapport
  amont ne porte de champ de comptage) ;
- **aucune proposition de next step** n'accompagne le décompte : elle serait produite par l'agent
  qui vient d'échouer plusieurs fois sur le sujet, donc la partie la moins fiable du rapport.

**2. `mission-contracts.md`** — ajouter au bloc typé un **quatrième champ optionnel frère**,
`decompte`, présent **uniquement** avec le statut de blocage, portant les tours consommés par
boucle et le nombre de findings non résolus ; et étendre le gabarit **§Rapport de mission** d'une
ligne symétrique de celles de calibration et de verdicts. Mêmes règles que les champs frères :
recopié/compté par celui qui pilote la boucle, jamais estimé, absent quand le statut n'est pas
celui de blocage.

**3. `vf-dev-manager.md`** — au §Orchestration par étape, point 3, la dernière phrase institue
aujourd'hui **deux budgets séparés** pour la boucle de vérification et celle de la revue (elle leur
attribue un budget et une réouverture chacune). **Remplacer cette phrase** — pas la doubler, pas la
commenter — par une formulation de budget **partagé par étape**, avec renvoi à `mission-flow.md`
§Pattern E pour le détail.

**Contrainte de nettoyage (gate)** : après édition, la formulation d'origine ne doit subsister
**nulle part** dans le fichier — ni en note « remplacé en Phase 23 », ni en commentaire. Une trace
textuelle rendrait rouge en permanence l'assertion négative de la tâche 3, et la tentation serait
alors de l'affaiblir plutôt que de nettoyer. La trace du remplacement vit au CHANGELOG (plan 23-08)
et au SUMMARY de ce plan.

Le fichier est proche du plafond ADR-029 : budget de densité de cette tâche pour ce fichier,
**+1 ligne nette maximum**, et le fichier doit rester ≤ 250 lignes.

**Clause de déport ex ante (ADR-029) — reprise telle quelle du plan 23-06, arithmétique
re-mesurée avant écriture, à ne pas re-dériver.** `vf-dev-manager.md` est à **241/250** (mesuré sur
l'arbre intact, `awk 'END{print NR}'`). Ce plan y consomme **+1 en tâche 1** et **+3 en tâche 2**,
soit **245 au maximum**, ce qui laisse **5 lignes** — et cinq seulement — au plan **23-08**, qui
touche lui aussi ce fichier. La marge tient pour ce plan ; **elle est presque nulle pour la suite**.
Donc, décidé d'avance plutôt que découvert au moment de la collision : **si l'écriture dépasse son
budget de lignes, le contenu part dans
`plugin/dev-orchestrator/references/mission-contracts.md`, section `## Rapport de mission`**, et
`vf-dev-manager.md` ne garde qu'un **renvoi d'une ligne**. Les fichiers de `references/` ne sont
**pas** plafonnés par ADR-029 — `mission-contracts.md` fait **313** lignes et `mission-flow.md`
**314**, mesurés — c'est ce qui rend la soupape utilisable. **Destination nommée, pas à improviser** :
n'invente pas un autre fichier d'accueil, et ne « compresse une phrase existante » sans destination.
**Jamais** supprimer une décision pour tenir le plafond. Consigner au SUMMARY le nombre de lignes
réellement consommées et **le reliquat en clair pour 23-08**, pour qu'il hérite d'un chiffre mesuré
et non d'une estimation.

**4. `test-dev-orchestrator.sh` — bloc `T31`** :
- assertion A : `mission-flow.md` porte le budget **partagé** et nomme le grain (l'étape) ;
- assertion B : la sous-section d'épuisement existe et nomme le décompte ;
- assertion C : l'invisibilité amont est écrite (le décompte ne prétend pas être exhaustif) ;
- assertion D : `mission-contracts.md` nomme le champ de décompte, et `vf-dev-manager.md` ne
  déclare plus deux budgets séparés (assertion **négative** sur la formulation remplacée) ;
- assertion E (**DISCRIMINANTE, par mutation**) : sur une copie temporaire de `vf-dev-manager.md`
  où la formulation de budget séparé est réinjectée, l'assertion D doit échouer.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > /tmp/vf-2307-t1.log 2>&1; rc=$?; tail -5 /tmp/vf-2307-t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - ⚠ **Forme de recette imposée — ne la « normalise » pas.** La recette ci-dessus écrit dans un log, **capture `$?`**, affiche la queue, puis `exit $rc`. La forme `… | tail -5` est **INTERDITE** : dans un pipeline le code de retour est celui de `tail`, donc **toujours 0**, et la recette rend vert un échec. Mesuré sur cette suite : suite rouge (12 KO) → `… | tail -5` rend **rc=0**, la forme ci-dessus rend **rc=1** ; suite verte → les deux rendent 0. C'est la **troisième récidive** de ce motif dans la phase (déjà refermé sur 23-05 et 23-06) : si tu la réintroduis, la vérification de ce plan ne vérifie plus rien.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T31`.
    - **[DISCRIMINANT — co-présence, pas mot isolé]** `plugin/dev-orchestrator/references/mission-flow.md` contient **au moins une ligne portant à la fois** `budget`, `partagé` et `étape`. Mesuré sur l'arbre intact : **0 ligne** — le critère est donc réellement discriminant. ⚠ Ne **pas** dégrader ce critère en `grep -ci 'partagé'` : ce mot seul retourne **4** sur l'arbre intact, il serait vert **sans qu'aucun travail n'ait été fait**.
    - `plugin/dev-orchestrator/references/mission-flow.md` contient une sous-section dont le titre comporte le mot `budget` et le mot `épuis`.
    - `grep -c 'node_repair' plugin/dev-orchestrator/references/mission-flow.md` retourne ≥ 1 (l'invisibilité amont est nommée avec le mécanisme qu'elle concerne, pas en termes vagues). Base mesurée sur l'arbre intact : **0** — discriminant.
    - `grep -ci 'invisible' plugin/dev-orchestrator/references/mission-flow.md` retourne ≥ 1. Base mesurée : **0** — discriminant.
    - `grep -c 'decompte' plugin/dev-orchestrator/references/mission-contracts.md` retourne ≥ 1. Base mesurée : **0** — discriminant.
    - **[W4 — jeton littéral de D-28]** `grep -c 'blocked' plugin/dev-orchestrator/references/mission-contracts.md` retourne ≥ 1. D-28 nomme le statut littéral **`blocked`**, pas « un statut de blocage » : c'est le **jeton exact** que le bloc typé doit porter, sinon le champ `decompte` s'accroche à un statut que personne ne sait écrire. Base mesurée sur `mission-contracts.md` : **0** — discriminant. ⚠ Ne **pas** porter ce critère sur `mission-flow.md` : `blocked` y apparaît déjà **8** fois, il y serait vert d'avance.
    - `grep -c 'son propre budget' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne `0` (la formulation de deux budgets séparés a été remplacée, pas doublée). Base mesurée sur l'arbre intact : **1** — le critère bascule donc réellement de rouge à vert par le travail de la tâche.
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne une valeur ≤ 250 (base mesurée : **241** — voir la clause de déport ex ante).
    - **[GARDE DE NON-RÉGRESSION — vert d'avance, et c'est VOULU]** `grep -cE '\b3 tours\b' plugin/dev-orchestrator/references/mission-flow.md` retourne ≥ 1. Base mesurée sur l'arbre intact : **1**, donc **déjà vert avant toute écriture**. Ce critère ne prouve **aucun travail** : il interdit une régression. Il matérialise l'invariant D-25 — la **valeur** du budget ne bouge pas, on consigne d'abord et on plafonne quand on aura des chiffres réels. Ne le lis pas comme une preuve de livraison, et ne le supprime pas non plus : c'est le seul garde-fou machine contre un plafonnement opportuniste.
    - Preuve de mutation de l'assertion E consignée au SUMMARY.
  </acceptance_criteria>
  <done>Le contournement mécanique par renommage du problème est fermé, un blocage rend désormais un chiffre réel au lieu d'un « après 3 tours » trompeur, et ce que le moteur consomme en interne est nommé comme invisible plutôt que deviné.</done>
</task>

<task type="auto">
  <name>Tâche 2 : quatre briques dormantes reçoivent un moment déclencheur, et le manager ne debug pas (D-22, D-23, D-24)</name>
  <files>plugin/dev-orchestrator/references/mission-flow.md, plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/references/GSD-PIPELINE.md</files>
  <read_first>
    - ⚠ **Ancrage par CHAÎNE, jamais par numéro de ligne** — même règle qu'en tâche 1, même motif mesuré.
    - plugin/dev-orchestrator/references/docs-flow.md §`## Déclencheurs (le FAIT, jamais le jugement)` — le **gabarit obligatoire** : table à deux colonnes dont l'en-tête est exactement `| Déclencheur | Constat |`, chaque ligne un FAIT constatable, et la phrase de clôture ancrée sur la chaîne `est un **état normal**, pas un manque`. Reproduire la **FORME**, jamais le contenu
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §`## Hygiène documentaire & next steps (rôle actif)` — le manager lit déjà une table de cette forme : quatre déclencheurs énumérés en une puce, conditions exactes renvoyées à `docs-flow.md`, ancre : la chaîne `ne pas les reformuler ici`. C'est exactement le patron à répliquer
    - plugin/dev-orchestrator/agents/vf-dev-manager.md §`## Recherche doc SYSTÉMATIQUE avant tout debug intensif (ADR-045)` — le point du cycle où le mandat de debug s'insère, après la recherche documentaire
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md §7 Briques connexes utiles — la brique de debug y est déjà nommée avec sa règle de recherche préalable
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md §9 (posée aux plans 23-03 et 23-05) — la voie unique : le mandat de debug passe par le **skill**, aucune exception, aucun agent nu de debug en allowlist
    - .planning/phases/VFDO-23-.../23-CONTEXT.md D-22, D-23 (le tableau des 4 briques et leurs ancrages), D-24
  </read_first>
  <action>
Donner un **moment** aux briques dormantes, pas un outil de plus.

**1. `mission-flow.md` — nouvelle section « Briques dormantes — moments déclencheurs »**, ajoutée
après le Pattern E, au **gabarit exact** de la table de `docs-flow.md` : deux colonnes
`Déclencheur | Constat`, chaque ligne un **FAIT constatable** et jamais un jugement au feeling, et
la même phrase de clôture disant qu'aucun déclencheur qui ne tombe est un **état normal, pas un
manque**. Le manager lit déjà une table de cette forme — même forme, même réflexe, aucun format
nouveau à apprendre.

Quatre lignes, une par brique, avec les ancrages déjà tranchés :
- **extraction de savoir** — déclencheur : étape vérifiée et clôturée. Constat : la famille est
  déjà doctrinée par `docs-flow.md` (Phase 22) ; la ligne se **raccorde au nœud documentaire de fin
  de mission** existant plutôt que d'en poser un nouveau. Renvoyer à `docs-flow.md` pour les
  conditions exactes, ne pas les reformuler (ADR-057) ;
- **ajout de tests** — déclencheur : verdict de validation nyquist **partiel**. Constat : le gate
  constate aujourd'hui les trous sans les combler ; cette ligne transforme un constat en action ;
- **spécification d'étape** — déclencheur : le QUOI d'une étape n'est pas stabilisé. Constat : le
  fichier de spec est lu par le cadrage, qui cesse alors de poser des questions de périmètre ;
- **récupération** (annulation et analyse post-mortem) — déclencheur : mission ratée, ou blocage à
  comprendre. Constat : aucune procédure écrite aujourd'hui, le manager improvise.

**2. `vf-dev-manager.md`** — deux touches d'une ligne chacune, dans des sections **existantes** :
- §Recherche doc SYSTÉMATIQUE : le manager **ne debug pas**. Après la recherche documentaire, il
  redispatche le worker en mandat de **debug**, et c'est le worker qui invoque le **skill** de
  debug. Écrire le corollaire en une incise, parce que c'est ce qui empêche la régression : aucun
  agent nu de debug n'entre en allowlist, aucune exception à la voie unique — ce que le manager
  gagne est un **moment**, pas un outil. Ajouter le motif factuel en trois mots : le skill tient un
  état persistant entre les réinitialisations de contexte, ce qu'un agent nu ne fait pas.

  ⚠ **PIÈGE DE LITTÉRAL — MESURÉ, il fait rougir la suite.** Nomme le **skill** `gsd-debug` ; n'écris
  **JAMAIS** le nom de l'agent nu (`gsd-debug` + suffixe `ger`) dans le corps de `vf-dev-manager.md`,
  **pas même pour l'interdire**. La détection de dispatch (tâche 3) est une regex sur le corps de
  prompt : elle ne distingue pas une prescription d'une prohibition. Mesures faites sur l'arbre :
  écrire « Il redispatche le worker en mandat de debug ; le worker invoque le skill `gsd-debug` » →
  suite **verte, 143 OK / 0 KO** (le nom du skill est un préfixe strict, il ne déclenche rien) ;
  écrire « Aucun agent nu `<le nom complet>` n'entre en allowlist » → suite **ROUGE, 1 KO**
  (`T29-B : dispatch direct offert dans le corps de prompt`). Formule l'interdit **en toutes lettres
  françaises** (« aucun agent nu de debug »), jamais par l'identifiant ;
- §Hygiène documentaire (ou §Orchestration, au choix du rédacteur, à un seul endroit) : un **renvoi**
  vers la nouvelle table de `mission-flow.md`, exactement sur le patron du renvoi existant vers
  `docs-flow.md` (« Constats et conditions exactes : … — ne pas les reformuler ici »).

Budget de densité de cette tâche pour ce fichier : **+3 lignes maximum**, et le fichier doit rester
**≤ 250 lignes**. Si le plafond est atteint, **applique la clause de déport ex ante de la tâche 1**
— destination **nommée d'avance** : `plugin/dev-orchestrator/references/mission-contracts.md`,
section `## Rapport de mission`, `vf-dev-manager.md` ne gardant qu'un **renvoi d'une ligne**.
N'improvise **pas** une « compression d'une phrase existante » sans destination, et **jamais**
supprimer une décision. Rappel de l'arithmétique mesurée : **241** de base, +1 (tâche 1) +3 (cette
tâche) = **245**, donc **5 lignes** seulement restent pour 23-08 — à consigner au SUMMARY.

**3. `GSD-PIPELINE.md` §7** — une ligne : la brique de debug s'invoque par son skill, jamais par un
agent nu, cohérent avec §9. Le tableau §7 la nomme déjà avec sa règle de recherche préalable ; il
s'agit d'ajouter la voie, pas l'entrée.

---

## 🛑 ÉCART À D-22 REMONTÉ À L'ARBITRAGE HUMAIN — NON TRANCHÉ PAR CE PLAN

**Fait mesuré, non interprété.** `plugin/dev-orchestrator/agents/vf-coder.md` porte, sur sa ligne
`tools:`, l'entrée d'allowlist `gsd-debugger` (**une** occurrence, seule occurrence de ce nom dans
tout `plugin/dev-orchestrator/agents/`). Or **D-22 est tranché par Samuel** et dit : « aucun
`gsd-debugger` en allowlist, **aucune exception** ». L'existant **contredit donc frontalement** une
décision verrouillée du cadrage de cette phase.

**Pourquoi ce plan ne le referme pas lui-même** : retirer une entrée d'allowlist d'un worker est un
geste de périmètre, pas une correction de forme — il touche ce que `vf-coder` a le droit d'invoquer.
Sous ADR-031, il n'est pas pris sans validation humaine. **Ce plan ne tranche donc ni dans un sens
ni dans l'autre** : il ne retire pas l'entrée, et il **n'entérine pas non plus** son maintien.

**Ce que ce plan a explicitement RETIRÉ** : le critère d'acceptation qui « constatait la valeur et la
consignait au SUMMARY, audit différé ». Un critère qui passe au vert **au-dessus d'un écart connu à
une décision verrouillée** est exactement le faux vert que cette phase existe pour fermer — et il
était d'autant plus trompeur que la fonction de détection `brique_nue_dispatch_hits()` **exclut la
ligne `tools:` par construction** (`awk '!/^tools:/'`) : le gate serait passé au vert **précisément
au-dessus du seul site qui contredit D-22**.

**Frontière à ne pas confondre** — les deux objets sont **indépendants** et se traitent séparément :
- la **ligne `tools:`** de `vf-coder.md` → **cet écart-ci**, en attente d'arbitrage humain ;
- les **sites de dispatch en corps de prompt** → traités **maintenant**, par la tâche 3 (assertion D).
Ne fusionne pas les deux. La tâche 3 est exécutable **telle quelle** sans que cet arbitrage soit
rendu : elle a été mesurée comme n'ayant **aucun impact** sur la ligne `tools:` (voir sa note de
mesure).

**Arbitrage attendu de Samuel** (hors mandat de l'exécutant) : soit retirer `gsd-debugger` de
l'allowlist de `vf-coder.md` et rendre D-22 vrai en machine, soit amender D-22 pour y inscrire
l'exception et son motif. **Tant que l'arbitrage n'est pas rendu, aucun critère de ce plan ne doit
affirmer la conformité à D-22.** Consigner l'écart au SUMMARY **comme écart ouvert**, jamais comme
constat clos.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > /tmp/vf-2307-t2.log 2>&1; rc=$?; tail -5 /tmp/vf-2307-t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - ⚠ **Forme de recette imposée** : log + capture de `$?` + `exit $rc`. La forme `… | tail -5` est **INTERDITE** (rc de `tail`, donc toujours 0). Voir le détail mesuré en tâche 1.
    - `plugin/dev-orchestrator/references/mission-flow.md` contient une table dont l'en-tête comporte les mots `Déclencheur` et `Constat`. Base mesurée (les deux mots sur une même ligne) : **0** — discriminant.
    - Cette table contient au moins 4 lignes de données et nomme les quatre briques : `gsd-extract-learnings`, `gsd-add-tests`, `gsd-spec-phase`, et au moins l'une de `gsd-undo` / `gsd-forensics`.
    - `grep -ci 'état normal' plugin/dev-orchestrator/references/mission-flow.md` retourne ≥ 1 (la phrase de clôture du gabarit est présente). Base mesurée sur `mission-flow.md` : **0** — discriminant (la phrase existe dans `docs-flow.md`, pas ici : c'est bien une reprise de FORME à produire).
    - `grep -c 'docs-flow.md' plugin/dev-orchestrator/references/mission-flow.md` retourne ≥ 1 (renvoi vers la famille déjà doctrinée, pas duplication). Base mesurée : **0** — discriminant.
    - `grep -c 'gsd-debug' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne ≥ 1 ET `grep -c 'gsd-debugger' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne `0` (le skill, jamais l'agent nu). Bases mesurées : **0** et **0** — le premier est discriminant, le second est la garde du piège de littéral décrit dans l'action.
    - **[DISCRIMINANT — co-présence]** `plugin/dev-orchestrator/agents/vf-dev-manager.md` contient **au moins une ligne portant à la fois** `mission-flow.md` **et** `dormante`. Base mesurée : **0** ligne. ⚠ Ne **pas** dégrader en `grep -c 'mission-flow.md'` seul : ce motif retourne **5** sur l'arbre intact (le manager renvoie déjà quatre fois vers ce fichier pour d'autres objets), il serait vert **sans que le renvoi vers la nouvelle table existe**.
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne une valeur ≤ 250 (base mesurée : **241**).
    - **[B1 — FORME COLLÉE OBLIGATOIRE]** `bash plugin/conductor/scripts/check-agents.sh --agents-dir=plugin/dev-orchestrator/agents --strict` sort en **0**. ⚠ **N'écris JAMAIS la forme espacée** `--agents-dir <chemin>` : le `case` de parsing de ce script ne reconnaît que `--agents-dir=*` et **n'a aucune branche `*)`**, donc la forme espacée est avalée **en silence**, `AGENTS_DIR` retombe sur son défaut `.claude/agents`, et le script sort en **rc=3** (`INDETERMINE : aucun agent dans .claude/agents`) — critère **insatisfiable**. Mesuré sur l'arbre intact : forme espacée → **rc=3** ; forme collée → **rc=0 · 7 warning(s)**. La forme collée est contre-intuitive et se fait « normaliser » par réflexe : c'est la **deuxième** fois dans cette phase (le plan 23-06 porte déjà la forme collée avec la même note). Le **7 warnings** est la baseline attendue, pas une anomalie : `--strict` ne fait pas échouer sur warnings.
  </acceptance_criteria>
  <done>Quatre briques qui n'étaient jamais mobilisées ont un moment déclencheur écrit en faits constatables, au gabarit que le manager lit déjà, et le manager sait quoi faire d'un bug récalcitrant sans qu'aucune porte fermée au plan 23-05 ne soit rouverte.</done>
</task>

<task type="auto">
  <name>Tâche 3 : la table des briques dormantes et le mandat de debug sont gatés (D-24, D-22)</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/references/mission-flow.md — l'état produit en tâche 2
    - plugin/dev-orchestrator/agents/vf-dev-manager.md — l'état produit en tâche 2
    - ⚠ **Ancrage par CHAÎNE, jamais par numéro de ligne** — même règle qu'en tâches 1 et 2.
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh bloc `T23` — ancre : la chaîne `# T23 — Câblage du geste documentaire dans les DEUX managers de mission`, jusqu'à l'ancre `# T23b (DISCRIMINANT)` incluse. C'est le patron le plus proche : il vérifie une table de 4 déclencheurs dans les deux managers, motif par motif, avec renvoi à la doctrine et assertion de non-duplication cross-module. ⚠ **Rectificatif mesuré** : la fourchette « ~1672-1726 » que portait ce plan **ne désigne pas T23** — elle tombe ~280 lignes plus haut, sur la liste `CODER_ALLOWED` et le helper `check_worker_allowlist()`, **autre objet**. Ne va pas éditer là.
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh bloc `T29` (posé au plan 23-05) — ancre : la chaîne `# T29 (Lacune 5, plan 23-05`. Contient la fonction de détection des dispatches d'agents nus, **à réutiliser plutôt qu'à réécrire**.
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh, **outillage de détection au niveau fichier** — ancres : les chaînes `BRIQUES_NUES_RE=` et `brique_nue_dispatch_hits()`. Le commentaire qui les précède **nomme déjà `T32`** et lui interdit une seconde fonction de détection : lis-le avant d'écrire.
  </read_first>
  <action>
Ajouter un bloc `T32` en fin de fichier, sur le patron de `T23` (qui vérifie déjà une table de
déclencheurs, motif par motif) :

- assertion A : `mission-flow.md` porte la table au gabarit `Déclencheur | Constat` **et** la phrase
  de clôture sur l'état normal — vérifier les deux, parce qu'une table sans cette phrase se lit
  comme une liste d'obligations ;
- assertion B : les quatre briques sont nommées, **une par une** (boucle sur une liste de noms
  exacts), avec la même précaution anti-homonyme que T19f — un nom ne doit jamais être validé par
  le préfixe d'un autre ;
- assertion C : `vf-dev-manager.md` nomme le **skill** de debug et **renvoie** à la table plutôt que
  de la reformuler (assertion de renvoi : le nom du fichier de doctrine est présent) ;
- assertion D (**NÉGATIVE**) : aucun fichier d'agent du module ne prescrit le dispatch d'un agent nu
  de debug — réutiliser la fonction de détection posée en `T29` au lieu d'en écrire une seconde.

  ⚠ **SANS L'EXTENSION CI-DESSOUS, CETTE ASSERTION EST VACUE PAR CONSTRUCTION.** Mesuré :
  `BRIQUES_NUES_RE` vaut aujourd'hui `'gsd-planner|gsd-executor'` — **`gsd-debugger` n'y figure
  pas**. Réutilisée telle quelle, l'assertion D serait **verte sans rien vérifier** : elle
  chercherait deux noms qui n'ont rien à voir avec le debug. Il ne suffit donc **pas** de réutiliser
  la fonction : il faut **étendre le motif qu'elle consomme**.

  **Forme imposée — constante DÉRIVÉE, portée limitée à la détection de dispatch.** Poser, juste
  au-dessus de `brique_nue_dispatch_hits()`, une constante dérivée et faire consommer celle-ci par
  la fonction :

  `BRIQUES_NUES_DISPATCH_RE` = `BRIQUES_NUES_RE` **augmentée** de l'agent nu de debug, écrite par
  **dérivation** (la nouvelle constante référence l'ancienne, elle ne la recopie pas). La dérivation
  est ce qui rend le **couplage visible et mécanique** : une modification future de `BRIQUES_NUES_RE`
  se propage toute seule, alors que deux listes recopiées divergeraient en silence et casseraient
  l'autre bloc sans un mot. Écrire ce motif en commentaire au-dessus de la constante, en nommant
  `T29` **et** `T32` comme consommateurs.

  ⚠ **IMPACT MESURÉ AVANT PRESCRIPTION — n'improvise pas une autre forme.** Deux variantes ont été
  jouées sur la suite réelle :
  - **Étendre directement `BRIQUES_NUES_RE`** (ajouter le nom à la constante existante) → suite
    **ROUGE, 1 KO** : `T29-A : vf-coder.md contient encore [ gsd-debugger ]`. Motif : `T29-A` grepe
    `vf-coder.md` **en entier, ligne `tools:` comprise**, et cette ligne porte l'entrée d'allowlist.
    Cette variante **couple B3 à l'écart D-22 en attente d'arbitrage humain** — donc **INTERDITE**
    ici. Elle ferait rougir la suite sur un objet que ce plan n'a pas mandat de trancher.
  - **Constante dérivée consommée par la seule `brique_nue_dispatch_hits()`** (forme imposée) →
    suite **VERTE, 143 OK / 0 KO** sur l'arbre intact. Motif : la fonction exclut la ligne `tools:`
    par construction (`awk '!/^tools:/'`), et l'agent nu de debug n'apparaît **nulle part ailleurs**
    que sur cette ligne (une seule occurrence dans tout `agents/`, mesurée).

  **Ce que l'exécutant doit re-vérifier après écriture** : que `T29-A`, `T29-B`, `T29-D`, `T29-E`,
  `T29-F` et `T29-G` (contre-épreuve licite comprise) restent **toutes vertes**. `T29-A`/`E`/`G`
  consomment `BRIQUES_NUES_RE` **en balayage fichier entier** et doivent garder leur motif d'origine
  **inchangé** ; `T29-B` et `T29-D` consomment la fonction et héritent donc du motif étendu — vérifié
  vert car le nom ne vit que sur la ligne `tools:`, exclue.

- assertion D-bis (**DISCRIMINANTE, par mutation, DANS LES DEUX SENS**) — l'extension n'a de valeur
  que prouvée :
  - **sens rouge** : sur une copie temporaire d'un agent du module où une phrase de dispatch direct
    de l'agent nu de debug est injectée **en corps de prompt**, la détection doit **remonter le nom**
    et l'assertion D doit **échouer**. Mesuré : produit exactement `T29-B : dispatch direct offert
    dans le corps de prompt — [vf-dev-manager.md : gsd-debugger ]` ;
  - **sens vert** : sur le fichier **réel**, la détection doit rendre **zéro** — et une copie où le
    manager nomme le **skill** (préfixe strict du nom d'agent) doit rester **verte**. Mesuré : verte,
    143 OK / 0 KO. Cette contre-épreuve est ce qui empêche l'extension de condamner le livrable de la
    tâche 2 par un faux rouge sur préfixe ;
- assertion E (**NÉGATIVE, ADR-057**) : `vf-dev-manager.md` ne reformule pas les constats de la
  table (il la renvoie) — même forme d'assertion que T23 pour la doctrine documentaire ;
- assertion F (**DISCRIMINANTE, par mutation**) : sur une copie temporaire de `mission-flow.md`
  privée d'une des quatre lignes de brique, l'assertion B doit échouer en nommant la brique
  manquante — ce qui prouve que la boucle vérifie bien quatre entrées et pas « au moins une ».

Le balayage exclut `plugin/dev-orchestrator/scripts/tests/`, même motif et même commentaire que les
blocs T25, T26, T29 et T30.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > /tmp/vf-2307-t3.log 2>&1; rc=$?; tail -5 /tmp/vf-2307-t3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - ⚠ **Forme de recette imposée** : log + capture de `$?` + `exit $rc`. La forme `… | tail -5` est **INTERDITE** (rc de `tail`, donc toujours 0). Voir le détail mesuré en tâche 1.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T32`. Baseline de la suite avant ce plan : **143 OK / 0 KO / 0 SKIP**, rc=0.
    - Le bloc `T32` contient une boucle sur une liste de **4** noms de briques (pas une assertion unique).
    - **[DISCRIMINANT — co-présence, remplace un critère qui était vert d'avance]** `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` contient **au moins une ligne portant à la fois** `BRIQUES_NUES_DISPATCH_RE` **et** `gsd-debugger` (la constante dérivée nomme bien l'agent nu de debug). Base mesurée : **0** — discriminant. ⚠ Ne **pas** dégrader en `grep -c 'gsd-debugger'` seul sur ce fichier : ce motif retourne **1** sur l'arbre intact — une occurrence **préexistante**, dans la liste `CODER_ALLOWED` du bloc d'allowlist, qui **ne prouve rien** sur `T32` et rendrait le critère vert sans travail.
    - Le bloc `T32` réutilise la fonction de détection posée en `T29` : **aucune seconde fonction** de détection de dispatch dans le fichier (le nombre de définitions de `brique_nue_dispatch_hits` reste **1**, et aucune fonction sœur n'est introduite).
    - Les motifs de balayage **fichier entier** de `T29` (`T29-A`, `T29-E`, `T29-G` et sa contre-épreuve licite) sont **inchangés** et **verts** : l'extension ne porte que sur la constante dérivée consommée par la détection de dispatch.
    - Preuve de mutation de l'assertion F consignée au SUMMARY, avec le nom de la brique retirée et le message d'échec obtenu.
    - Preuve de mutation de l'assertion **D-bis** consignée au SUMMARY, **dans les deux sens** : le message d'échec obtenu sur injection d'un dispatch en corps de prompt, **et** le compteur vert obtenu sur le fichier réel ainsi que sur la copie ne nommant que le skill.
  </acceptance_criteria>
  <done>La table des moments déclencheurs ne peut plus perdre une brique en silence, le mandat de debug ne peut plus se transformer en dispatch d'agent nu, et les deux assertions négatives ont leur discriminance prouvée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boucles VibeFlow → boucle de réparation du moteur | Les tours d'équipe et les réparations internes au moteur s'empilent **sans se voir** : le premier compte ses tours, le second n'expose aucun compteur. La frontière est opaque **dans un sens**, ce qui doit être écrit et non compensé par un chiffre inventé. |
| boucle de revue → boucle de comblement | Deux budgets séparés partagent le même problème sous deux noms : la frontière entre les deux boucles était le vecteur du contournement. |
| rapport de blocage → Samuel | Un « bloqué après 3 tours » traversait cette frontière en masquant jusqu'à neuf tentatives : le décompte est ce qui rend la traversée honnête. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07-01 | Repudiation | rapport de blocage sans décompte | high | mitigate | Statut de blocage assorti d'un champ de décompte obligatoire (tours par boucle, findings non résolus), relayé au gabarit de rapport de mission, gaté par T31. Le décompte **est** la livraison de la zone. |
| T-23-07-02 | Spoofing | décompte présenté comme exhaustif alors que le coût amont est invisible | high | mitigate | L'invisibilité est **écrite comme un fait daté et sourcé** (journal amont en prose libre, aucun champ de comptage dans les gabarits de rapport), et l'interdiction de fabriquer un total agrégé est explicite. Un manque nommé vaut mieux qu'un chiffre inventé (D-26). |
| T-23-07-03 | Elevation of Privilege | contournement du budget par renommage du problème | high | mitigate | Budget **unique et partagé** au grain étape, remplaçant (et non doublant) la formulation de deux budgets séparés ; assertion **négative** T31-D sur la formulation remplacée, discriminance prouvée par mutation. |
| T-23-07-04 | Elevation of Privilege | mandat de debug rouvrant la voie d'agent nu fermée au plan 23-05 | high | mitigate | Le mandat passe par le **skill** ; assertion négative T32-D réutilisant la fonction de détection de T29 ; aucun agent nu de debug ajouté en allowlist. |
| T-23-07-05 | Tampering | proposition de next step jointe au décompte | medium | mitigate | Explicitement proscrite dans la doctrine : elle serait produite par l'agent qui vient d'échouer plusieurs fois sur le sujet, donc la partie la moins fiable du rapport. |
| T-23-07-06 | Denial of Service | budget réduit sur la foi d'une intuition, faisant perdre le rendement mesuré de la boucle | medium | accept | La **valeur** du budget est explicitement inchangée (D-25) et un critère machine le vérifie : on consigne d'abord, on plafonne quand on aura des chiffres. Le plafonnement global reste différé. |
| T-23-07-07 | Repudiation | perte silencieuse d'une des quatre briques de la table | low | mitigate | Boucle d'assertion sur les quatre noms, anti-homonyme, discriminance prouvée par retrait d'une ligne (T32-F). |
| T-23-07-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan. |
</threat_model>

<verification>
- Suite du module verte : `0 KO`, `T31` et `T32` nommés dans la sortie. Baseline avant ce plan : **143 OK / 0 KO / 0 SKIP**, rc=0.
- **Toutes les recettes de vérification automatisée capturent `$?` puis `exit $rc`.** Aucune ne se termine par `| tail` : dans un pipeline le code de retour est celui de `tail`, donc toujours 0 — mesuré, une suite à 12 KO rend alors **rc=0**. Motif refermé pour la troisième fois dans cette phase. (Le nom de la balise n'est **pas** écrit littéralement ici : une mention en prose est ramassée par une extraction naïve des balises et produit une quatrième pseudo-recette — constaté en amendant ce plan.)
- La valeur du budget de tours est **inchangée** (critère machine, garde de non-régression assumée comme verte d'avance) : cette phase consigne, elle ne plafonne pas.
- La formulation de deux budgets séparés a disparu du manager (assertion négative), et non pas été doublée par la nouvelle.
- Densité ADR-029 : `wc -l` ≤ 250 sur `vf-dev-manager.md` (base **241**, +4 max par ce plan → **245**, reliquat **5** pour 23-08, à inscrire en clair au SUMMARY) ; `check-agents.sh --agents-dir=<chemin> --strict` (**forme collée**, la forme espacée sort en rc=3) vert en rc=0 avec **7 warning(s)** de baseline.
- Aucun agent nu de debug **dispatché en corps de prompt** nulle part — volet livré, gaté, discriminance prouvée dans les deux sens.
- 🛑 **ÉCART OUVERT, NON TRANCHÉ PAR CE PLAN** : l'entrée `gsd-debugger` de la ligne `tools:` de `vf-coder.md` contredit D-22 (« aucune exception »). **Remontée à l'arbitrage humain** (ADR-031) ; le critère qui la « constatait, audit différé » a été **retiré**, parce qu'il passait au vert au-dessus d'un écart connu à une décision verrouillée. **Cette phase ne peut pas être déclarée conforme à D-22 tant que l'arbitrage n'est pas rendu** ; elle est conforme au **volet dispatch** de D-22, et à lui seul.
- Discriminance des assertions structurantes prouvée par **mutation**, commandes et compteurs consignés au SUMMARY.
</verification>

<success_criteria>
- SC1 — Le budget de tours est unique, partagé entre les deux boucles, au grain étape, et sa valeur n'a pas bougé.
- SC2 — Un blocage rend un décompte complet, sans proposition de next step jointe.
- SC3 — L'invisibilité du coût consommé à l'intérieur d'un plan par le moteur est écrite comme un fait sourcé, et aucun total agrégé trompeur n'est fabriqué.
- SC4 — Quatre briques dormantes ont un moment déclencheur, au gabarit `Déclencheur | Constat`, chaque ligne un fait constatable.
- SC5 — Le manager ne debug pas et ne gagne aucun outil : il gagne un moment, et **aucun agent nu de debug n'est dispatché en corps de prompt** (volet dispatch de D-22, gaté et prouvé par mutation). ⚠ SC5 **ne couvre pas** le volet allowlist : l'entrée `tools:` de `vf-coder.md` reste un **écart ouvert remonté à l'arbitrage humain**, et aucun critère de ce plan ne l'entérine.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-07-SUMMARY.md` à la fin.

Y consigner :
- le nombre de lignes final de `vf-dev-manager.md` **et le reliquat en clair pour 23-08** (base
  mesurée **241**, budget de ce plan **+4 max** → **245**, reliquat attendu **5**) ;
- les preuves de mutation avec leurs compteurs avant/après : assertion **E** (tâche 1), assertion
  **F** (tâche 3), et assertion **D-bis** (tâche 3) **dans les deux sens** — message d'échec obtenu
  sur injection, compteur vert obtenu sur le fichier réel et sur la copie ne nommant que le skill ;
- 🛑 **l'écart à D-22 comme ÉCART OUVERT, jamais comme constat clos** : la ligne `tools:` de
  `vf-coder.md` porte l'entrée d'agent nu de debug (**1** occurrence mesurée, seule du module),
  ce qui contredit « aucune exception ». Écrire qu'il est **remonté à l'arbitrage humain** (ADR-031),
  que **ce plan ne l'a tranché dans aucun sens**, et que le critère qui l'entérinait en « audit
  différé » a été **retiré**. Ne pas écrire de formulation qui laisserait lire la phase comme
  conforme à D-22 : elle est conforme au **volet dispatch**, et à lui seul ;
- **[W5] la numérotation non monotone des blocs de test, pour qu'un audit ne la lise pas comme un
  désordre** : ce plan prend `T31` et `T32` alors que `T33` est **déjà posé** (plan 23-03, qui a pris
  `T33` après une collision sur `T27` tranchée le 2026-08-03, avec la consigne « aucun autre plan ne
  bouge »). `T31` et `T32` étaient **explicitement réservés** à ce plan par des commentaires posés en
  23-05 et 23-06. La numérotation est donc **volontaire et traçable**, pas accidentelle — aucun
  renumérotage à faire.
</output>

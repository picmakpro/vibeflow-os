---
phase: 41-posture-de-protection-du-d-p-t
plan: 01
lot: L1 — préalables externes (PR #29 fermée, actor_id du rôle write mesuré) et source JSON des deux rulesets
type: execute
REGLES_MAIN_FORCE_PUSH_SUPPRESSION: EN_ATTENTE_ARBITRAGE  # INCLUSES | ABSENTES — fixée par le manager après arbitrage de Samuel
REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE: EN_ATTENTE_ARBITRAGE  # canal=<canal, tirets pour les espaces> date=<AAAA-MM-JJ> — canal et date RÉELS de l'arbitrage, capturés par le manager
wave: 1
depends_on: []
autonomous: false
requirements: [PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .github/rulesets/main.json
  - .github/rulesets/tags-v.json

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Branche de travail A = `feat/phase-41-protection-depot` (ce worktree). Aucun agent n'écrit chez GitHub : fermeture de #29 et sonde de ruleset sont des gestes humains du compte `picmakpro`, opéré par Samuel en personne (D-01, D-08, D-M6, D-M14)"
    - "Les 4 contextes de checks requis sont RECOPIÉS par programme depuis les check runs réels de la pointe d'`origin/main` (application 15368), jamais retapés, et égaux aux 4 `name:` de jobs de `.github/workflows/ci.yml` (D-04, D-M4)"
    - "L'`actor_id` du rôle `write` (`RepositoryRole`) est MESURÉ par une sonde humaine (ruleset désactivé, cible sans aucune branche, supprimé aussitôt), jamais écrit de mémoire : la doc REST lue le 2026-09-17 ne donne pas la correspondance rôle → identifiant"
    - "`.github/rulesets/main.json` : cible `refs/heads/main`, actif, règles exactement pull_request (0 approbation, revue code owner requise, merge/squash/rebase) + required_status_checks (4 contextes, `integration_id` 15368, strict), plus deletion + non_fast_forward SI ET SEULEMENT SI la variable de plan `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` vaut `INCLUSES` (aucun arbitrage ne les couvre à ce jour, question posée à Samuel par le manager le 2026-09-17) ; bypass unique = rôle write en mode `pull_request` ; aucune règle d'historique linéaire ni de signature (D-02, D-03, D-04, D-05, D-10, D-M2, D-M3, D-M12)"
    - "`.github/rulesets/tags-v.json` : cible `refs/tags/v*`, actif, règles exactement deletion + non_fast_forward + update ; AUCUNE règle `creation` ; bypass unique = rôle write en mode `always` (D-07, D-M13)"
    - "Les deux fichiers sont au format du corps de `POST /repos/{owner}/{repo}/rulesets` pour être posés octet pour octet depuis `origin/main` par `gh api --input -` (D-M6)"
  artifacts:
    - path: ".github/rulesets/main.json"
      provides: "source versionnée du ruleset de branche"
    - path: ".github/rulesets/tags-v.json"
      provides: "source versionnée du ruleset de tags"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "registre des mesures de la phase (lignes `CLE: valeur`), ouvert par ce plan"
  key_links:
    - "`SONDE-ACTOR-ID-WRITE:` de `41-PREUVES.md` → `bypass_actors[0].actor_id` des deux JSON ; les vérifications lisent la ligne, jamais une valeur recopiée"
    - "Chemin `.github/rulesets/` choisi pour tomber sous `/.github/` du CODEOWNERS (D-05, plan 41-02) : toute modification ultérieure des sources exige la revue `@picmakpro` ou un contournement tracé"
---

<objective>
Poser les préalables externes et la source versionnée des deux rulesets, sans rien activer chez
GitHub :
- fermer la PR #29 avant toute pose (D-08) ;
- mesurer l'identifiant du rôle `write` par une sonde humaine sans effet ;
- écrire les deux JSON depuis des valeurs mesurées (check runs, sonde), jamais retapées.

Purpose: PROT-01 exige des règles posées depuis une source versionnée dont chaque valeur sensible
(contextes, intégration, acteur de contournement) est prouvée, pas supposée.
Output: `.github/rulesets/main.json`, `.github/rulesets/tags-v.json`, `41-PREUVES.md` ouvert.

Variable de plan `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` (frontmatter), fixée par le manager dès l'arbitrage de Samuel :
- variante (a) `INCLUSES` — types de règles attendus dans `main.json`, triés : `deletion`,
  `non_fast_forward`, `pull_request`, `required_status_checks` ;
- variante (b) `ABSENTES` — types attendus, triés : `pull_request`, `required_status_checks` ;
- `EN_ATTENTE_ARBITRAGE` — aucune variante n'est acceptée : `main.json` n'est pas écrit et les
  vérifications de 41-01, 41-03 (ADR-072) et 41-05 rougissent.
Le ruleset de tags n'est pas concerné (D-07 couvre réécriture, suppression et force push des tags).
La variable compagne `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` porte `canal=… date=…` de cet arbitrage, tels que capturés
par le manager : tout commit ou corps de PR qui fixe ou cite la variante les reprend (« arbitrage Samuel,
<canal>, <date> »), jamais une date écrite en dur ; la vérification des messages rougit sinon.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/missions/2026-09-17-phase41-cadrage.md
@.github/workflows/ci.yml

Sources de plateforme lues le 2026-09-17 (WebFetch, jamais de mémoire) :
- `https://docs.github.com/en/rest/repos/rules` — `bypass_actors[].actor_id` « Required for Integration, RepositoryRole, Team, and User actor types », AUCUNE table rôle → identifiant ; `bypass_mode` ∈ always / pull_request / exempt ; `conditions.ref_name.include` ; règles `pull_request` (required_approving_review_count, dismiss_stale_reviews_on_push, require_code_owner_review, require_last_push_approval, required_review_thread_resolution, allowed_merge_methods), `required_status_checks` (context, integration_id, strict_required_status_checks_policy, do_not_enforce_on_create), `non_fast_forward`, `deletion`, `creation`, `update` (update_allows_fetch_and_merge) ; `PUT …/rulesets/{id}` accepte une mise à jour partielle ; `bypass_actors` n'est rendu qu'à un compte ayant l'écriture sur le ruleset.
- `…/managing-rulesets/creating-rulesets-for-a-repository` — acteurs de contournement éligibles « The maintain or write role » ; mode « For pull requests only ».
- Constat du 2026-09-17 (lecture `gh api`, compte `samuel-neveugall`) : les check runs de `7cb542e` portent les 4 noms de jobs sous `app.id` 15368 ; `rulesets` rend `[]` ; seule la PR #29 est ouverte.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : chemin de lecture API → dépôt — contextes des checks relevés sur les check runs réels, recoupés avec ci.yml, registre de preuves ouvert</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (§ Constats « Source des checks », D-04, D-M4)
    - .github/workflows/ci.yml (les quatre `name:` de jobs, dont un entre guillemets)
  </read_first>
  <action>
Vérifier d'abord l'identité gh de l'agent : elle doit être `samuel-neveugall` (jamais `picmakpro` :
aucun agent n'agit avec le jeton admin, D-M6). `git fetch origin` (lecture).

Créer `41-PREUVES.md` : titre « Phase 41 — registre des mesures », un paragraphe de règle (chaque
ligne `CLE: valeur` est une mesure consignée au moment où elle est faite ; une valeur non mesurée
s'écrit `non_prouvee` ou `indetermine`, jamais supposée ; les sorties humaines sont recopiées
verbatim dans des lignes `…-MSG:` ; aucun chemin absolu de machine), puis une section
`## 41-01 — préalables externes`.

Relever par programme les noms des check runs de l'application 15368 sur la pointe d'`origin/main`
(`gh api …/commits/<sha>/check-runs`, filtre `app.id == 15368`, tri unique) et les comparer aux
quatre `name:` de jobs de `ci.yml` (guillemets YAML retirés). Consigner la ligne
`CONTEXTES-CHECKS: sha=<sha d'origin/main> n=4 app=15368 egal_ci=oui`. Si la pointe n'a pas encore
ses quatre check runs terminés, attendre et relire, ne jamais compléter à la main.

Imprimer enfin, pour la tâche humaine suivante, le témoin qu'aucune branche distante ne répond au
motif de la sonde : `git ls-remote --heads origin vf-sonde-41-aucune-branche` doit être vide.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'u=$(gh api user --jq .login); echo "identite gh agent : $u"; [ "$u" = samuel-neveugall ]'</automated>
    <fails_when>exit non nul : l'identité gh de l'agent n'est pas `samuel-neveugall` (arrêt, aucune lecture ni écriture)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; T=$(mktemp -d); S=$(git rev-parse origin/main); gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/api"; awk "/^jobs:/{j=1; next} j && /^  [A-Za-z0-9_-]+:[ \t]*\$/{k=1; next} j && k && /^    name:/{sub(/^    name:[ \t]*/, \"\"); gsub(/^\"|\"\$/, \"\"); print; k=0}" .github/workflows/ci.yml | LC_ALL=C sort -u > "$T/ci"; na=$(awk "END{print NR}" "$T/api"); nc=$(awk "END{print NR}" "$T/ci"); eg=$(cmp -s "$T/api" "$T/ci" && echo oui || echo non); l=$(awk -v s="$S" "\$1==\"CONTEXTES-CHECKS:\" && \$2==\"sha=\" s && /egal_ci=oui/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); git ls-remote --heads origin vf-sonde-41-aucune-branche > "$T/lr"; r=$(awk "END{print NR}" "$T/lr"); echo "contextes : sha=$S api=$na ci=$nc api_egal_ci=$eg ligne_preuve=$l branches_sonde=$r"; [ "$na" -eq 4 ] && [ "$nc" -eq 4 ] && [ "$eg" = oui ] && [ "$l" -eq 1 ] && [ "$r" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `api=` différent de 4 (check runs absents ou incomplets sur la pointe) ; ou `api_egal_ci=non` (un nom de job diffère, tiret ou guillemet compris) ; ou `ligne_preuve=` différent de 1 (ligne absente, dupliquée ou portant un autre sha) ; ou `branches_sonde=` différent de 0 (le motif de la sonde toucherait une branche réelle : ne pas lancer la sonde, remonter)</fails_when>
  </verify>
  <acceptance_criteria>
    - identité gh de l'agent = `samuel-neveugall`
    - 4 noms relevés sur les check runs 15368 de la pointe d'`origin/main`, identiques aux 4 `name:` de jobs de `ci.yml`
    - `41-PREUVES.md` existe avec sa règle et une seule ligne `CONTEXTES-CHECKS:` portant le sha lu
    - aucune branche distante ne répond à `vf-sonde-41-aucune-branche`
  </acceptance_criteria>
  <done>Le chemin de lecture API → dépôt est prouvé de bout en bout ; la sonde peut être lancée sans toucher aucune branche.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `picmakpro` — fermer la PR #29 (D-08), puis sonder l'identifiant du rôle write par un ruleset désactivé et aussitôt supprimé</name>
  <action>Samuel, avec le compte `picmakpro` (D-01), ferme la PR #29 puis crée, lit et supprime un ruleset sonde désactivé dont la cible ne touche aucune branche. Aucun agent ne fait ces gestes.</action>
  <instructions>
Pourquoi : la doc REST ne publie pas l'identifiant du rôle `write` ; l'interface GitHub, elle,
propose le rôle par son libellé. La sonde laisse la plateforme faire la correspondance. Elle est
désactivée et vise un motif sans branche : elle ne change les conditions d'aucune PR, même
brièvement (motif de D-08), et elle est supprimée avant toute autre action.

Compte qui agit : `picmakpro` pour les étapes 1 à 4 ; retour à `samuel-neveugall` en 5.

0. Basculer gh : `gh auth switch --user picmakpro` ; témoin : `gh api user --jq .login` rend
   `picmakpro`. (Si ce compte n'est pas connecté dans gh : `gh auth login` avec ce compte d'abord.)
1. Fermer #29 :
   `gh pr close 29 --repo picmakpro/vibeflow-os --comment "Fermée avant la pose des rulesets de la Phase 41 (D-08 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Contenu non instruit par la phase (41-CONTEXT.md, Deferred)."`
   Témoin : `gh pr view 29 --repo picmakpro/vibeflow-os --json state --jq .state` rend `CLOSED`.
2. Sonde par l'interface : dépôt → Settings → Rules → Rulesets → New ruleset → New branch ruleset.
   Nom `sonde-actor-id-41` ; Enforcement status **Disabled** ; Bypass list → Add bypass → rôle
   **Write** (rien d'autre) ; Target branches → Add target → Include by pattern →
   `vf-sonde-41-aucune-branche` ; règles : laisser uniquement « Restrict deletions » ; Create.
3. Lire : `gh api repos/picmakpro/vibeflow-os/rulesets --jq '.[] | select(.name=="sonde-actor-id-41") | .id'`
   → `<id>` ; puis `gh api repos/picmakpro/vibeflow-os/rulesets/<id> --jq '{enforcement, include: .conditions.ref_name.include, bypass_actors}'`.
4. Supprimer : `gh api -X DELETE repos/picmakpro/vibeflow-os/rulesets/<id>` ; témoin :
   `gh api repos/picmakpro/vibeflow-os/rulesets --jq length` rend `0`.
5. Revenir : `gh auth switch --user samuel-neveugall` ; témoin : `gh api user --jq .login` rend
   `samuel-neveugall`.

L'étape 3 est jouée AVANT la suppression : c'est la relecture par l'API qui fait foi. Arrêt sans
poursuivre si elle montre `enforcement` différent de `disabled`, un `include` autre que le seul
motif `vf-sonde-41-aucune-branche`, plus d'un acteur, ou un `actor_type` différent de
`RepositoryRole` : supprimer la sonde (étape 4) et le signaler.
  </instructions>
  <verification>L'agent relit ensuite, en `samuel-neveugall` : état `CLOSED` de #29 et `rulesets` vide (Task 3).</verification>
  <resume-signal>Coller verbatim les sorties des témoins 0, 1, 3 (id et JSON), 4 et 5, puis écrire « sonde faite ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : consigner les témoins humains, écrire les deux JSON depuis les valeurs mesurées, rejouer les gates</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md, .github/rulesets/main.json, .github/rulesets/tags-v.json</files>
  <read_first>
    - la sortie collée à la Task 2 (id de la sonde, `bypass_actors`)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (D-02 à D-07, D-10, D-M2, D-M3, D-M4, D-M12, D-M13)
  </read_first>
  <action>
Relire soi-même (identité `samuel-neveugall`) : `gh pr view 29 --json state,closedAt` et
`gh api …/rulesets --jq length`. Consigner dans `41-PREUVES.md` :
- `PR29-FERMEE: state=CLOSED closedAt=<iso>` ;
- `SONDE-ACTOR-ID-WRITE: <n> ruleset_sonde=<id> actor_type=RepositoryRole enforcement=<valeur lue> include=<motif lu> lu_par=picmakpro supprimee=oui rulesets_apres=0` ;
- `SONDE-MSG:` suivi du JSON rendu par l'API à l'étape 3 de la Task 2 (lu AVANT la suppression),
  recompacté sur une ligne (`jq -c`), sans autre modification.
`<n>`, `enforcement` et `include` sont les valeurs de ce JSON, jamais une autre source. Si
`enforcement` n'est pas `disabled` ou si `include` n'est pas le seul motif
`vf-sonde-41-aucune-branche` (forme `refs/heads/…` ou nue) : consigner quand même, ne pas écrire les
JSON, arrêt et remontée.

Écrire `.github/rulesets/main.json` en générant la liste des contextes avec `jq` depuis la sortie
des check runs (même filtre que la Task 1, jamais une liste tapée) :
- `name` « main — PR obligatoire, 4 checks requis, revue code owner (Phase 41) », `target` branch,
  `enforcement` active ;
- `conditions.ref_name` include `refs/heads/main`, exclude vide. Choix de discrétion : nom explicite
  plutôt que `~DEFAULT_BRANCH`, pour que la cible ne glisse pas en silence si la branche par défaut
  changeait ;
- `rules` selon la variable de plan `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` : variante (a) `INCLUSES` → `deletion` ;
  `non_fast_forward` ; `pull_request` ; `required_status_checks` — variante (b) `ABSENTES` →
  `pull_request` ; `required_status_checks` seulement — `EN_ATTENTE_ARBITRAGE` → NE PAS écrire
  `main.json`, arrêt et remontée au manager. `pull_request` avec `required_approving_review_count`
  0 (D-03), `require_code_owner_review` true (D-05), `allowed_merge_methods` merge, squash, rebase
  (D-10), `dismiss_stale_reviews_on_push` false, `require_last_push_approval` false,
  `required_review_thread_resolution` false ; `required_status_checks` avec les 4 contextes chacun à
  `integration_id` 15368 (D-M4), `strict_required_status_checks_policy` true (D-04),
  `do_not_enforce_on_create` false. Les trois booléens non arbitrés restent à leur valeur par
  défaut `false` : la phase n'ajoute aucune règle non arbitrée (décision du manager, tour 1 de
  vérification des plans, 2026-09-17). Écarter les approbations périmées à chaque push est un
  renforcement possible NON retenu, à décider par Samuel (écrit dans ADR-072, plan 41-03) ;
- `bypass_actors` = un seul acteur `RepositoryRole`, `actor_id` = `<n>`, `bypass_mode`
  `pull_request` (D-02, D-M12 : jamais `always`, jamais `exempt`).
Aucune règle `required_linear_history` (D-M2), `required_signatures` (D-M3), `update` ni
`creation`.

Écrire `.github/rulesets/tags-v.json` :
- `name` « tags v* — réécriture et suppression tracées, création libre (Phase 41) », `target` tag,
  `enforcement` active ;
- include `refs/tags/v*`, exclude vide ;
- `rules` = `deletion`, `non_fast_forward`, `update` avec `update_allows_fetch_and_merge` false, et
  AUCUNE règle `creation` (D-07) ;
- `bypass_actors` = `RepositoryRole` `<n>` en `always` (D-M13).

Les deux fichiers : JSON indenté par `jq`, sans champ serveur (`id`, `source`, `_links`…), sans
chemin de machine. Ne rien poser chez GitHub. Commit local sur la branche A, jamais poussé, avec
le message « feat(41-01): sources JSON des rulesets de branche et de tags (D-02, D-03, D-04, D-05,
D-07, D-10 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ; REGLES_MAIN_FORCE_PUSH_SUPPRESSION=<variante> — arbitrage Samuel, <canal>, <date> » où <canal> (tirets remplacés par
des espaces) et <date> sont lus dans `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` du frontmatter de `41-01-PLAN.md`,
jamais une date écrite en dur. Tout autre commit
de ce plan qui cite D-01..D-10 porte la même mention de canal et de date.
QUAL-01 : aucun script de gate créé (D-M11).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(gh pr view 29 --repo picmakpro/vibeflow-os --json state --jq .state); n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); l1=$(awk "\$1==\"PR29-FERMEE:\" && \$2==\"state=CLOSED\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); A=$(awk "\$1==\"SONDE-ACTOR-ID-WRITE:\" {print \$2; exit}" "$PH/41-PREUVES.md"); case "$A" in ""|*[!0-9]*) echo "actor_id absent ou non entier"; exit 1;; esac; T=$(mktemp -d); awk "\$1==\"SONDE-MSG:\" { sub(/^SONDE-MSG:[ \t]*/, \"\"); print; exit }" "$PH/41-PREUVES.md" > "$T/j"; sj=$(jq -r --argjson aid "$A" "[(.enforcement == \"disabled\"), ((.include == [\"refs/heads/vf-sonde-41-aucune-branche\"]) or (.include == [\"vf-sonde-41-aucune-branche\"])), ((.bypass_actors | length) == 1), (.bypass_actors[0].actor_type == \"RepositoryRole\"), (.bypass_actors[0].actor_id == \$aid)] | map(tostring) | join(\",\")" "$T/j"); l2=$(awk "\$1==\"SONDE-ACTOR-ID-WRITE:\" && / enforcement=disabled / && (/ include=refs\\/heads\\/vf-sonde-41-aucune-branche / || / include=vf-sonde-41-aucune-branche /) && /actor_type=RepositoryRole/ && /supprimee=oui/ && /rulesets_apres=0/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "prealables : pr29=$s rulesets=$n ligne_pr29=$l1 actor_id=$A json_api_sonde(disabled,motif_sans_branche,un_acteur,role,id)=$sj ligne_sonde=$l2"; [ "$s" = CLOSED ] && [ "$n" -eq 0 ] && [ "$l1" -eq 1 ] && [ "$sj" = "true,true,true,true,true" ] && [ "$l2" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `pr29=` différent de `CLOSED` ; ou `rulesets=` différent de 0 (sonde non supprimée ou ruleset posé trop tôt : arrêt, remontée) ; ou `actor_id` vide ou non entier ; ou `SONDE-MSG:` absente ou JSON illisible ; ou un `false` dans `json_api_sonde` — en particulier `enforcement` différent de `disabled` ou `include` différent du seul motif sans branche, relus par l'API avant la suppression ; ou `ligne_sonde=` différent de 1 (ligne absente, dupliquée, ou portant d'autres valeurs d'`enforcement`/`include` que celles du JSON)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=.github/rulesets/main.json; VR=$(awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION:/{print \$2; exit}" "$PH/41-01-PLAN.md"); case "$VR" in (INCLUSES) TY="[\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]";; (ABSENTES) TY="[\"pull_request\",\"required_status_checks\"]";; (*) echo "REGLES_MAIN_FORCE_PUSH_SUPPRESSION=$VR : aucune variante choisie, verification refusee"; exit 1;; esac; A=$(awk "/^SONDE-ACTOR-ID-WRITE: /{print \$2; exit}" "$PH/41-PREUVES.md"); case "$A" in ""|*[!0-9]*) echo "json-branche : actor_id_mesure=absent"; exit 1;; esac; o=$(jq -r --argjson aid "$A" --argjson ty "$TY" "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | {nom: ((.name | type)==\"string\" and (.name | length) > 0), cible: (.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"] and .conditions.ref_name.exclude==[]), types: ([.rules[].type] | sort == \$ty), approbations0: (\$r.pull_request.required_approving_review_count==0), code_owner: (\$r.pull_request.require_code_owner_review==true), stale_defaut_false: (\$r.pull_request.dismiss_stale_reviews_on_push==false), last_push: (\$r.pull_request.require_last_push_approval==false), threads: (\$r.pull_request.required_review_thread_resolution==false), methodes: ((\$r.pull_request.allowed_merge_methods | sort)==[\"merge\",\"rebase\",\"squash\"]), strict: (\$r.required_status_checks.strict_required_status_checks_policy==true), checks4: ((\$r.required_status_checks.required_status_checks | length)==4), integration: ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), bypass: (.bypass_actors == [{actor_id: \$aid, actor_type: \"RepositoryRole\", bypass_mode: \"pull_request\"}])} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$B"); echo "json-branche : $o"; case "$o" in *=false*) exit 1;; esac'</automated>
    <fails_when>exit non nul (JSON invalide compris) ; ou un champ `=false` dans la ligne `json-branche :` — en particulier `stale_defaut_false=false` (renforcement non arbitré posé),  `types=false` (règle manquante, en trop, dupliquée, historique linéaire ou signature ajoutés), `integration=false`, `bypass=false` (mode `always`/`exempt`, acteur en trop ou identifiant différent de la sonde), `methodes=false` (restriction non arbitrée) ; ou `actor_id_mesure=absent` ; ou `REGLES_MAIN_FORCE_PUSH_SUPPRESSION=… : aucune variante choisie` (valeur `EN_ATTENTE_ARBITRAGE` ou inconnue : la vérification rougit tant que le manager n'a pas fixé la variante)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; G=.github/rulesets/tags-v.json; A=$(awk "/^SONDE-ACTOR-ID-WRITE: /{print \$2; exit}" "$PH/41-PREUVES.md"); case "$A" in ""|*[!0-9]*) echo "json-tags : actor_id_mesure=absent"; exit 1;; esac; o=$(jq -r --argjson aid "$A" "{nom: ((.name | type)==\"string\" and (.name | length) > 0), cible: (.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"] and .conditions.ref_name.exclude==[]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), update_param: ([.rules[] | select(.type==\"update\") | .parameters.update_allows_fetch_and_merge] == [false]), bypass: (.bypass_actors == [{actor_id: \$aid, actor_type: \"RepositoryRole\", bypass_mode: \"always\"}])} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$G"); echo "json-tags : $o"; case "$o" in *=false*) exit 1;; esac'</automated>
    <fails_when>exit non nul ; ou un champ `=false` — `types=false` couvre une règle `creation` ajoutée (contraire à D-07) ou une règle manquante ; `bypass=false` couvre un mode autre que `always` ou un identifiant différent de la sonde</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=.github/rulesets/main.json; T=$(mktemp -d); S=$(git rev-parse origin/main); gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs[] | select(.app.id == 15368) | .name" | LC_ALL=C sort -u > "$T/api"; jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$B" | LC_ALL=C sort -u > "$T/json"; awk "/^jobs:/{j=1; next} j && /^  [A-Za-z0-9_-]+:[ \t]*\$/{k=1; next} j && k && /^    name:/{sub(/^    name:[ \t]*/, \"\"); gsub(/^\"|\"\$/, \"\"); print; k=0}" .github/workflows/ci.yml | LC_ALL=C sort -u > "$T/ci"; na=$(awk "END{print NR}" "$T/api"); nj=$(awk "END{print NR}" "$T/json"); nc=$(awk "END{print NR}" "$T/ci"); ja=$(cmp -s "$T/api" "$T/json" && echo oui || echo non); jc=$(cmp -s "$T/json" "$T/ci" && echo oui || echo non); echo "contextes : sha=$S api=$na json=$nj ci=$nc json_egal_api=$ja json_egal_ci=$jc"; [ "$na" -eq 4 ] && [ "$nj" -eq 4 ] && [ "$nc" -eq 4 ] && [ "$ja" = oui ] && [ "$jc" = oui ]'</automated>
    <fails_when>exit non nul ; ou un des trois comptes différent de 4 ; ou `json_egal_api=non` / `json_egal_ci=non` (un contexte retapé ou altéré — tiret demi-cadratin, guillemet, `#38`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash scripts/check-machine-paths.sh > /dev/null 2>&1 || rc=$?; echo "check-machine-paths rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul : un chemin de machine est entré dans un fichier suivi (JSON, registre de preuves)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou `bilan(N M)=absent` ; ou N égal à 0 ; ou N différent de `decouverte_independante` (découverte tronquée) ; ou M différent de 0. Rouge T28-F de `test-dev-orchestrator.sh` : vérifier le moteur installé et `npm view @opengsd/gsd-core version`, rejouer la suite sur une extraction `git archive` d'`origin/main` ; rouge aussi là → préexistant, consigné et remonté `human_needed`, jamais neutralisé ni déclaré vert</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `PR29-FERMEE:` et `SONDE-ACTOR-ID-WRITE:` consignés et recoupés par une lecture de l'agent (#29 `CLOSED`, 0 ruleset)
    - sonde relue par l'API avant suppression : `enforcement` `disabled`, motif sans branche, un seul acteur `RepositoryRole`
    - variable `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` fixée (`INCLUSES` ou `ABSENTES`) ; types de règles de `main.json` égaux à la liste de la variante choisie
    - `main.json` et `tags-v.json` conformes champ par champ (`dismiss_stale_reviews_on_push` à `false`), `actor_id` issu de la sonde, contextes identiques aux check runs ET à `ci.yml`
    - tout commit de plan citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »
    - `check-machine-paths` rc 0 ; rejeu `gates` rc 0 (≥ 10 étapes) ; rejeu `tests` rc 0, bilan N = découverte indépendante, M = 0
    - aucun ruleset posé ; aucun script de gate créé (D-M11)
  </acceptance_criteria>
  <done>Les deux sources JSON existent, chacune de leurs valeurs sensibles est mesurée, et rien n'est actif chez GitHub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → API GitHub | l'agent lit seulement ; tout geste d'écriture passe par un humain, le jeton admin ne transite jamais par l'agent |
| source versionnée → règle posée | une valeur fausse dans le JSON devient une règle fausse sur `main` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Spoofing | contextes de checks requis | high | mitigate | `integration_id` 15368 exigé sur les 4 contextes (vérif `integration=true`) : un statut posé par un compte write sous le même nom ne satisfait pas le check |
| T-41-02 | Tampering | noms de contextes retapés | high | mitigate | liste générée par `jq` depuis les check runs ; égalité exigée avec l'API ET avec `ci.yml` (mutant « tiret retapé » rougit) |
| T-41-03 | Elevation of privilege | `bypass_actors` | high | mitigate | acteur unique, `pull_request` pour la branche, `always` pour les tags, identifiant mesuré ; mode `exempt` rejeté (pas de trace) |
| T-41-04 | Elevation of privilege | jeton `picmakpro` | high | mitigate | identité gh de l'agent vérifiée `samuel-neveugall` avant toute lecture ; les gestes admin sont des checkpoints humains ; retour de compte témoigné |
| T-41-05 | Tampering | sonde laissée en place ou visant une branche | medium | mitigate | désactivée, motif vérifié sans branche, suppression témoignée et relue (`rulesets` = 0) |
| T-41-06 | Tampering | approbation code owner périmée couvrant un push ultérieur | medium | accept | `dismiss_stale_reviews_on_push` laissé au défaut `false` : aucun arbitrage ne couvre le renforcement (décision du manager, tour 1, 2026-09-17) ; écrit dans ADR-072 comme renforcement possible non retenu, à décider par Samuel ; valeur `false` vérifiée dans la source et côté serveur |
| T-41-07 | Information disclosure | chemin de machine dans un fichier suivi | low | mitigate | `check-machine-paths` rejoué |
</threat_model>

<verification>
#29 fermée et relue ; sonde supprimée et relue ; deux JSON conformes aux arbitrages, identifiant du
rôle write et contextes mesurés ; gates et tests rejoués verts ; aucun ruleset actif.
</verification>

<success_criteria>
La source de PROT-01 est prête à être mergée : chaque valeur des rulesets est prouvée, rien n'est
encore imposé à aucune PR.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-01-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-protection-depot`).
</output>

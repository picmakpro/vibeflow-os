---
phase: 29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - plugin/conductor/scripts/check-map-drift.sh
  - plugin/conductor/scripts/tests/test-check-map-drift.sh
autonomous: true
requirements: [ICMD-03, ICMD-04, ICMD-05, ICMD-06]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lab dont un CLAUDE.md pointe un chemin qui n'existe plus reçoit un signal nommant ce chemin — la carte qui ment ne passe plus en silence."
    - "Un dossier indexé dont l'index ne cite pas tous ses fichiers (ou cite un fichier absent) reçoit un signal nommant l'écart, dans les deux sens."
    - "Une cible sans aucune carte balayée rend « NON VÉRIFIABLE », jamais un vert : le compteur de cartes balayées accompagne obligatoirement tout verdict d'absence de divergence."
    - "Le gate constate, il ne corrige jamais : aucun chemin de code n'écrit dans un fichier du lab (ADR-031)."
  artifacts:
    - plugin/conductor/scripts/check-map-drift.sh
    - plugin/conductor/scripts/tests/test-check-map-drift.sh
  key_links:
    - "check-map-drift.sh → wrapper git durci repris verbatim de check-doc-drift.sh:106-115 : un dépôt cloné hostile ne peut pas faire exécuter de code pendant un simple lint."
    - "check-map-drift.sh → grammaire d'exit 0/1/3/64 du dépôt : un lecteur (humain ou agent) qui connaît check-doc-drift.sh / check-agents.sh lit ce gate sans réapprendre."
    - "test-check-map-drift.sh → harnais artisanal de test-check-doc-drift.sh (git_must, mk_git_root, stdout et rc assertés séparément)."
  prohibitions:
    - "PROHIBÉ — tout mode correctif automatique (ADR-031). Vérifiable : `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -cE -- '--update|--fix|--write'` rend 0, et aucune redirection d'écriture vers un fichier du lab n'existe hors du répertoire temporaire du script."
    - "PROHIBÉ — vérifier les `skills:` de frontmatter (déjà couvert par check-agents.sh) ou les DAG de mission (*.dag.json — Pitfall 2 de 29-RESEARCH.md, hors domaine et adjacent au socle protégé par D-03)."
    - "PROHIBÉ — écrire dans .planning/ (ADR-055). Le gate peut lire, jamais écrire."
    - "PROHIBÉ — modifier plugin/conductor/scripts/dag.sh (D-03) : ce plan n'a aucune raison de le lire et aucune de l'écrire."
    - "PROHIBÉ — utiliser jq, grep -P ou sed -i (ADR-054, bash portable macOS+Linux)."
---

<objective>
Livrer **G3** : `check-map-drift.sh`, le gate qui diffe ce que les **cartes déclarent** contre ce
que le **disque contient** — la dérive documentaire structurelle, plaie n°1 documentée de ce dépôt
(compteurs faux jamais signalés, folder maps qui mentent), que les gates actuels ne voient pas
parce qu'ils détectent l'**immobilité** (`check-doc-drift.sh` : « la doc n'a pas bougé depuis N
commits ») et non l'**incohérence**.

Purpose : Axiome 1 — « un garde-fou non exécuté par la machine n'existe pas ». G3 est le seul gain
de la phase qui produise une surface exécutable ; c'est aussi celui qui frappe la friction la mieux
documentée. Il naît **lint seul** (ADR-031, aucun correctif sans validation humaine) et **borné par
écrit** : il dit lui-même ce qu'il ne couvre pas, pour ne pas dériver vers le territoire d'un autre
gate ni vers celui du socle protégé par D-03.

Choix de discrétion tranchés ici (CONTEXT §Claude's Discretion délègue explicitement les trois) :
**paires v1** = carte de dossiers d'un `CLAUDE.md` ↔ disque, et index de dossier ↔ contenu du
dossier — deux diffs de système de fichiers purs, bidirectionnels, sans recouvrement avec
`check-agents.sh` (qui couvre déjà les `skills:`) ni `check-state-integrity.sh` (qui couvre la
monotonie du frontmatter d'état, pas la carte). **Emplacement** = `plugin/conductor/scripts/` — le
module `validator` est déclaré `agent-only` (aucun dossier `scripts/`) et invoque déjà des scripts
posés par d'autres modules ; `conductor` est `mandatory: true`, donc toujours installé, ce qui rend
le gate atteignable par son consommateur (précédent exact de `check-agents.sh`). **Mode** =
lint seul, sans mode correctif — aucun besoin exprimé ne le réclame et il ouvrirait une surface
ADR-031 gratuite.

Output : le script + sa suite, nés ensemble.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-CONTEXT.md
@.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-PATTERNS.md
@plugin/dev-orchestrator/scripts/check-doc-drift.sh
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 29-02)

| Artefact / symbole | Type | Statut |
|---|---|---|
| `plugin/conductor/scripts/check-map-drift.sh` | script bash exécutable | NOUVEAU |
| `plugin/conductor/scripts/tests/test-check-map-drift.sh` | suite de tests bash | NOUVELLE (+1 au compte de suites) |
| `--path <dir>` | flag CLI | NOUVEAU |
| `--map <fichier>` | flag CLI | NOUVEAU |
| `--hook` | flag CLI | NOUVEAU |
| `--quiet` | flag CLI | NOUVEAU |
| `-h` / `--help` | flag CLI | NOUVEAU |
| `[map-drift]` | préfixe de signal stdout | NOUVEAU |
| `NON VÉRIFIABLE` | verdict de plancher anti-vert-à-vide | NOUVEAU (dans ce script) |
| `git_safe()` | fonction bash | NOUVELLE occurrence (reprise verbatim d'un précédent) |
| `say()` | fonction bash | NOUVELLE occurrence |
| paire **P1** carte de dossiers ↔ disque | règle de gate | NOUVELLE |
| paire **P2** index de dossier ↔ contenu | règle de gate | NOUVELLE |

**Conséquence de distribution** : `+1 suite` → le compteur « N suites » des deux README racine
devra être re-dérivé (porté par le plan **29-05**, exigence ICMD-12). Ce plan ne touche à aucun
README.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : Tranche traçante — la paire P1 de bout en bout, script + suite nés ensemble</name>

  <precondition>
    `git --version` répond et `bash plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh`
    est vert sur l'arbre de travail : le harnais dont cette tâche copie la structure (fixtures git
    isolées) est fonctionnel sur cette machine. S'il est rouge, le modèle de fixture n'est pas
    fiable et la nouvelle suite hériterait du défaut — HALTE.
  </precondition>

  <files>plugin/conductor/scripts/check-map-drift.sh, plugin/conductor/scripts/tests/test-check-map-drift.sh</files>

  <read_first>
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` **en entier (153 L)** — l'analog exact :
      en-tête doctrinal l.2-6, grammaire d'exit documentée l.56-60, parsing d'arguments portable
      l.61-102, gate de mutuelle exclusion l.90-94, validation numérique par `case` l.96-102,
      `say()` l.104, wrapper git durci l.106-121, forme du signal émis l.145-153.
    - `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` l.1-70 — le squelette de
      harnais : `ok()`/`ko()`, `TMP`+`trap`, `git_must()` (fixtures qui échouent bruyamment),
      `mk_git_root()` (dépôt isolé, identité fixe, repli `git init` sans `-b`).
    - `.planning/phases/VFDO-29-.../29-PATTERNS.md` §`check-map-drift.sh (gate G3)` et
      §`test-check-map-drift.sh (suite G3)` — les extraits verbatim à transposer et la convention
      d'exit à 4 codes.
    - `.planning/phases/VFDO-29-.../29-RESEARCH.md` §Security Domain — le motif V5 (dépôt cloné
      hostile) et le plancher anti-vert-à-vide.
    - `plugin/conductor/scripts/check-agents.sh` l.1-60 — l'en-tête d'usage multi-mode
      (`--strict` / `--hook` / `--file` / `--allow-empty`) : la grammaire d'interface à laquelle ce
      gate doit ressembler pour un lecteur du dépôt.
    - `plugin/conductor/scripts/scaffold-docs.sh` l.1-20 — la forme des chemins que la doctrine
      ADR-042 pose (`@docs/<projet>/`), c'est-à-dire ce que la paire P1 devra reconnaître comme
      « pointeur déclaré ».
  </read_first>

  <behavior>
    Comportements attendus de la paire **P1** (carte de dossiers d'un `CLAUDE.md` ↔ disque), à
    écrire en cas de test AVANT le corps du script :

    - **P1-A (entrée périmée)** : une carte déclare un pointeur `@docs/absent/` et le chemin
      n'existe ni en fichier ni en dossier → une divergence, nommant le chemin ET la carte.
    - **P1-A bis** : un chemin cité entre accents graves et contenant un séparateur de dossier
      (`` `plugin/disparu/x.md` ``) et absent du disque → une divergence.
    - **P1-B (entrée neuve)** : un sous-dossier de premier niveau, suivi par git, cité par aucun
      token de la carte → une divergence, nommant le dossier.
    - **P1-clean** : une carte dont tous les pointeurs existent et qui cite tous les sous-dossiers
      de premier niveau suivis → **0 divergence**, et la sortie porte le **compteur de cartes
      balayées** (≥ 1).
    - **Plancher (ICMD-05)** : cible sans aucune carte → verdict `NON VÉRIFIABLE` + compteur `0`,
      exit 3, **jamais** le message « 0 divergence ». Idem cible inexistante et cible hors d'un
      arbre de travail git.
    - **Ignorés** : les entrées commençant par un point, `node_modules`, et les chemins déclarés
      qui ne contiennent aucun séparateur de dossier (mot en accents graves = identifiant, pas un
      chemin) — chacun couvert par un cas dédié pour que l'ignorance soit prouvée, pas supposée.
    - **Exits** : ≥ 1 divergence → **0** ; 0 divergence sur ≥ 1 carte → **3** ; `NON VÉRIFIABLE` →
      **3** ; argument invalide → **64**. Le code **1** n'est jamais rendu en v1 (réservé à un
      futur mode bloquant, dit comme tel en en-tête).
  </behavior>

  <action>
    Écrire `plugin/conductor/scripts/tests/test-check-map-drift.sh` **d'abord**, avec un cas rouge
    par comportement listé ci-dessus, puis `plugin/conductor/scripts/check-map-drift.sh` jusqu'au
    vert.

    Structure du script, calquée sur l'analog (transposer, ne pas paraphraser vaguement) :

    1. **En-tête doctrinal** — nom + question à laquelle le gate répond ; puis, dans la forme de
       `check-doc-drift.sh:4-6`, la clause « répond au FAIT, jamais au métier » : le script ne dit
       **jamais** qu'une carte est fausse ou périmée — seulement qu'une entrée déclarée n'a pas de
       contrepartie sur le disque, ou qu'un élément suivi n'est cité par aucune carte. Le jugement
       appartient à l'agent ou à l'utilisateur.
    2. **Usage** + **grammaire d'exit** documentée en en-tête, aux 4 codes du dépôt, en précisant
       explicitement que `1` n'est jamais rendu par cette version.
    3. `set -uo pipefail` et `shopt -s nullglob`, comme l'analog. **Pas** de `set -e` (l'analog ne
       l'emploie pas : un rc non nul de `git` doit être traité, pas faire sortir le script).
    4. **Parsing d'arguments** portable ADR-054 : `--path` et `--map` exigent une valeur (sinon
       message sur stderr + 64), `--hook`, `--quiet`, `-h|--help` (auto-extraction de l'en-tête par
       `grep '^# ' "$0" | sed 's/^# //'`), défaut `*)` → argument inconnu + 64. Gate de mutuelle
       exclusion `--hook` + `--quiet` → 64, placé avant toute autre logique.
    5. `say()` — diagnostics sur **stderr**, gatés par `--quiet` ; le signal seul sur **stdout**.
    6. **Wrapper git durci** — copier verbatim les trois `export` et la fonction `git_safe()` de
       `check-doc-drift.sh:109-115`, puis le silence hors arbre de travail git (`rev-parse
       --is-inside-work-tree` échoue → `NON VÉRIFIABLE`, exit 3).
    7. **Recensement des cartes** : les `CLAUDE.md` **suivis par git** sous `--path`
       (`git_safe ls-files`), ou l'unique fichier passé par `--map`. Toute lecture de contenu se
       fait sur le disque, sur des chemins **entre guillemets sans exception** (un chemin à espace
       ne doit jamais se scinder en deux arguments).
    8. **Paire P1, deux sens** :
       - sens A — extraire les tokens de chemin de la carte : pointeurs préfixés par une arobase, et
         segments entre accents graves contenant un séparateur `/`. Pour chaque token, retirer une
         éventuelle barre finale, résoudre relativement à `--path`, et constater l'absence par
         `[ -e ]`.
       - sens B — lister les entrées de premier niveau **suivies par git** sous `--path`, retirer
         celles commençant par un point et `node_modules`, et constater celles qu'aucun token de la
         carte ne cite.
       Chaque divergence s'accumule dans un compteur et produit une ligne de signal nommant la
       carte, le sens (`entrée déclarée sans contrepartie` / `élément suivi non cité`) et
       l'élément.
    9. **Verdict**, dans cet ordre strict :
       - `CARTES_BALAYEES` vaut 0 → stdout `[map-drift] NON VÉRIFIABLE — 0 carte balayée (…)`,
         exit **3**. C'est le plancher : ce chemin ne peut jamais imprimer un compte de divergences.
       - `DIVERGENCES` ≥ 1 → stdout `[map-drift] N divergence(s) sur M carte(s) balayée(s).` + les
         lignes de détail + une ligne d'action `→ propose …` (forme de `check-doc-drift.sh:148`),
         exit **0**.
       - sinon → stdout `[map-drift] 0 divergence sur M carte(s) balayée(s).`, exit **3**. Le
         compteur `M` est **obligatoire** sur cette ligne : c'est lui qui rend le vert non-vacuant.

    Le script **n'écrit aucun fichier**. Aucune redirection vers un chemin du lab, aucun répertoire
    temporaire nécessaire.

    Suite de tests : reprendre `TMP`+`trap`, `ok()`/`ko()`, `git_must()`, `mk_git_root()` de
    l'analog ; **un dépôt de fixture isolé par cas** ; identité git fixe sur chaque commit ;
    **stdout et rc capturés dans deux variables distinctes, assertées séparément**.
  </action>

  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-map-drift.sh; echo "rc=$?"</automated>
    <automated>bash -n plugin/conductor/scripts/check-map-drift.sh &amp;&amp; echo "syntaxe-ok"</automated>
    <automated>bash plugin/conductor/scripts/check-map-drift.sh --argument-inexistant; echo "rc=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-check-map-drift.sh` sort **0**, affiche **0 échec**,
      et compte **au moins 10 cas** (un par comportement du bloc `<behavior>`, plancher inclus).
    - `bash -n plugin/conductor/scripts/check-map-drift.sh` sort **0**.
    - `bash plugin/conductor/scripts/check-map-drift.sh --argument-inexistant` sort **64** et
      n'imprime **rien sur stdout** (le message part sur stderr).
    - `bash plugin/conductor/scripts/check-map-drift.sh --hook --quiet` sort **64**.
    - `bash plugin/conductor/scripts/check-map-drift.sh --path` (sans valeur) sort **64** ; idem
      `--map` sans valeur.
    - `bash plugin/conductor/scripts/check-map-drift.sh --path "$(mktemp -d)"` sort **3** et sa
      sortie stdout contient `NON VÉRIFIABLE` **et** ne contient **pas** la sous-chaîne
      `0 divergence` — le plancher ICMD-05 est prouvé, pas déclaré.
    - `bash plugin/conductor/scripts/check-map-drift.sh --help` sort **0** et imprime au moins 15
      lignes issues de l'en-tête.
    - `grep -c 'git_safe' plugin/conductor/scripts/check-map-drift.sh` ≥ **3**, et
      `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -c 'git -C'` rend **1** :
      une seule invocation git nue existe, celle **dans** le wrapper.
    - `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -cE 'jq |grep -P|sed -i'`
      rend **0** (ADR-054).
    - `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -cE -- '--update|--fix|--write'`
      rend **0** — aucun mode correctif n'existe (ADR-031).
    - `git diff --name-only -- plugin/conductor/scripts/dag.sh` rend **0 ligne** (D-03).
  </acceptance_criteria>

  <reversibility rating="reversible">
    Nouveau script isolé, câblé à aucun hook et à aucun agent à ce stade (le câblage arrive en
    29-05). Le supprimer restaure l'état antérieur sans effet de bord. Le choix d'emplacement
    (module `conductor`) est réversible par simple déplacement de fichier tant qu'aucun consommateur
    ne le référence.
  </reversibility>

  <done>
    `check-map-drift.sh` constate la paire P1 dans les deux sens, parle la grammaire d'exit du
    dépôt, passe par le wrapper git durci, ne peut pas rendre un vert à vide, n'écrit rien — et sa
    suite le prouve cas par cas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : Expansion — paire P2 (index de dossier ↔ contenu) et preuves par mutation</name>

  <files>plugin/conductor/scripts/check-map-drift.sh, plugin/conductor/scripts/tests/test-check-map-drift.sh</files>

  <read_first>
    - `plugin/conductor/scripts/check-map-drift.sh` — l'état laissé par la tâche 1 (recensement des
      cartes, boucle de divergences, verdict à 3 branches) : P2 s'insère comme une **seconde
      famille de cartes**, pas comme un second script.
    - `plugin/conductor/scripts/tests/test-check-map-drift.sh` — les cas P1, pour aligner la forme
      des cas P2 et ne pas dupliquer les helpers de fixture.
    - `plugin/conductor/scripts/scaffold-docs.sh` l.50-87 — la forme réelle d'un fichier d'index
      posé par le dépôt (titre, blockquote de rôle, bullets de pointeurs `[FICHIER.md](FICHIER.md)`)
      : c'est la forme que le sens A de P2 doit savoir lire.
    - `.planning/phases/VFDO-29-.../29-RESEARCH.md` §Common Pitfalls → **Pitfall 2** (le gate ne
      dérive pas vers les DAG de mission) et **Pitfall 5** (les deux objets homonymes du dépôt).
    - `plugin/dev-orchestrator/references/` (listing) — un dossier réel à 11 fichiers `.md` sans
      index, cible de la démonstration en 29-04 : P2 doit rester **silencieux** sur un dossier sans
      fichier d'index (ce n'est pas une carte, donc pas une divergence).
  </read_first>

  <behavior>
    - **P2-A (entrée périmée)** : un fichier d'index cite `[disparu.md](disparu.md)` alors que le
      fichier n'est pas dans le dossier → une divergence nommant l'index et l'entrée.
    - **P2-B (entrée neuve)** : un fichier `.md` suivi par git, présent dans le dossier de l'index,
      cité nulle part dans l'index → une divergence nommant l'index et le fichier.
    - **P2-self** : le fichier d'index lui-même n'est jamais compté comme non cité.
    - **P2-non-récursif** : un `.md` d'un **sous-dossier** de l'index n'est pas compté (l'index
      contracte son propre dossier, pas l'arbre).
    - **P2-absent** : un dossier sans fichier d'index n'est pas une carte — il ne produit ni
      divergence, ni incrément du compteur de cartes balayées.
    - **Cumul** : une cible portant à la fois une carte P1 et une carte P2 rend un compteur de
      cartes balayées égal à **2**, et additionne les divergences des deux paires.
    - **Mutation P1** : neutraliser le sens B de P1 dans le script rend au moins un cas **rouge**.
    - **Mutation P2** : neutraliser le sens A de P2 rend au moins un cas **rouge**.
  </behavior>

  <action>
    Étendre le recensement des cartes : en plus des `CLAUDE.md`, retenir les fichiers **suivis par
    git** nommés `_index.md` ou `INDEX.md` sous `--path`. Ces deux noms seulement — un `README.md`
    n'est pas un index dans ce dépôt (chaque module en porte un qui est une présentation, pas un
    contrat de dossier) ; le dire en une ligne dans l'en-tête.

    Implémenter la paire P2, non récursive, dans les deux sens :
    - sens A — pour chaque entrée `*.md` citée par l'index (cible d'un lien markdown, ou segment
      entre accents graves se terminant par l'extension markdown), constater l'absence du fichier
      dans le **dossier de l'index**.
    - sens B — pour chaque `*.md` suivi par git **directement** dans le dossier de l'index, autre
      que l'index lui-même, constater qu'aucune ligne de l'index ne le cite.

    Réutiliser sans le dupliquer le compteur de divergences, le compteur de cartes balayées et le
    verdict à trois branches de la tâche 1 : P2 ajoute des cartes et des lignes de signal, elle ne
    crée **aucune** seconde grammaire de sortie ni second code d'exit.

    Ajouter les cas de test de la liste `<behavior>`, dont le cas de **cumul** (une fixture portant
    une carte de chaque famille) et les **deux cas de mutation**. Les mutations suivent la
    discipline du dépôt : muter le script dans une copie temporaire, rejouer le cas ciblé, exiger
    qu'il devienne rouge, et attester la restauration par `cmp -s` contre l'original — **jamais**
    par `diff` (précédent explicite du dépôt).
  </action>

  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-map-drift.sh; echo "rc=$?"</automated>
    <automated>bash -n plugin/conductor/scripts/check-map-drift.sh &amp;&amp; echo "syntaxe-ok"</automated>
  </verify>

  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-check-map-drift.sh` sort **0**, **0 échec**, et
      compte **au moins 18 cas** (P1 + P2 + cumul + 2 mutations).
    - La suite contient **au moins 2 cas nommés « mutation »** et chacun échoue si la mutation
      n'est pas détectée — vérifiable par `grep -ci mutation plugin/conductor/scripts/tests/test-check-map-drift.sh` ≥ 2.
    - `grep -c 'cmp -s' plugin/conductor/scripts/tests/test-check-map-drift.sh` ≥ **2** et
      `grep -v '^#' plugin/conductor/scripts/tests/test-check-map-drift.sh | grep -c 'diff -q'` rend
      **0** (attestation de mutation par comparaison octet, jamais par `diff`).
    - Sur une fixture portant une carte P1 **et** une carte P2, la sortie mentionne un compteur de
      cartes balayées égal à **2**.
    - Un dossier sans fichier d'index n'incrémente pas le compteur — cas dédié vert dans la suite.
    - `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -cE 'dag\.json|\.dag\.'`
      rend **0** — le gate n'a pas dérivé vers les DAG de mission (Pitfall 2).
    - `grep -v '^#' plugin/conductor/scripts/check-map-drift.sh | grep -c 'skills:'` rend **0** —
      aucun recouvrement avec `check-agents.sh`.
      <!-- planner-discipline-allow: skills: -->
      *(Le littéral est **attendu** dans l'en-tête du script, où la tâche 3 lui fait déclarer cet axe
      comme non couvert. Le gate est donc filtré par `grep -v '^#'` : il porte sur le **code**, pas
      sur la prose de bornes — un en-tête qui nomme ce qu'il ne couvre pas ne peut pas s'invalider
      lui-même.)*
    - `bash plugin/conductor/scripts/tests/test-check-doc-drift.sh` et
      `bash plugin/conductor/scripts/tests/test-check-agents.sh` restent **verts** (aucune
      régression collatérale des gates voisins).
  </acceptance_criteria>

  <done>
    Les deux paires v1 sont livrées, bidirectionnelles, sous une grammaire de sortie unique ; la
    discriminance de chacune est prouvée par mutation attestée à l'octet, pas déclarée.
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : Bornes déclarées en en-tête — ce que le gate ne couvre pas, et pourquoi</name>

  <files>plugin/conductor/scripts/check-map-drift.sh, plugin/conductor/scripts/tests/test-check-map-drift.sh</files>

  <read_first>
    - `plugin/conductor/scripts/check-map-drift.sh` — l'en-tête laissé par les tâches 1 et 2.
    - `plugin/dev-orchestrator/scripts/check-doc-drift.sh` l.2-60 — le modèle d'en-tête long qui
      **écrit son propre raisonnement** (heuristique, cas limites, motif de durcissement, exits) :
      la densité de justification attendue.
    - `plugin/conductor/scripts/check-agents.sh` l.1-60 — le précédent d'un gate qui déclare
      nommément sa hiérarchie avec les autres gates.
    - `.planning/phases/VFDO-29-.../29-RESEARCH.md` §Common Pitfalls (Pitfall 2, Pitfall 4) et
      §Don't Hand-Roll — les recouvrements à interdire par écrit.
    - `docs/ADR.md` — ADR-031 (jamais de fix sans validation humaine) et ADR-055 (`.planning/`
      propriété de GSD) pour citer les bons numéros dans l'en-tête.
  </read_first>

  <action>
    Compléter l'en-tête du script par une section **« Bornes »** énumérant, chaque ligne portant son
    motif sur place :

    - **Ce qu'il ne couvre pas et pourquoi** : (a) les `skills:` de frontmatter — déjà couverts par
      le lint d'agents, dupliquer produirait deux verdicts divergents sur le même axe ; (b) les
      fichiers de DAG de mission — hors domaine, et adjacents au socle de périmètre que la phase
      protège par construction ; (c) `.planning/` — lu jamais écrit, propriété du moteur amont
      (ADR-055) ; (d) la **qualité** d'une carte — le gate constate un écart d'ensembles, il ne
      juge jamais qu'une carte est fausse ou périmée.
    - **Absence de mode correctif, assumée** : la version livrée constate seulement ; toute
      évolution vers un correctif serait gardée par le garde-fou en trois temps déjà éprouvé dans ce
      dépôt (reformulation du nombre et de la liste affectée, attente d'un oui explicite,
      interdiction en mission d'équipe et en mode autonome), jamais activée par défaut — ADR-031.
    - **Le code de sortie 1 est réservé** à un futur mode bloquant et n'est jamais rendu par cette
      version.
    - **Limite de portée déclarée** : « carte balayée » ≠ « carte correcte » — un compteur de cartes
      balayées non nul avec zéro divergence signifie que **les ensembles comparés coïncident**, pas
      que la documentation dit vrai.

    Ajouter deux cas à la suite : (1) `--help` imprime la section Bornes (au moins les quatre
    motifs de non-couverture sont présents dans la sortie) ; (2) un cas de garde qui échoue si le
    script venait à rendre le code de sortie réservé.

    Ne rien câbler ici : aucun hook, aucun agent, aucun README. Le câblage et la clôture de
    distribution sont l'objet du plan 29-05.
  </action>

  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-map-drift.sh; echo "rc=$?"</automated>
    <automated>bash plugin/conductor/scripts/check-map-drift.sh --help | grep -c "Bornes"</automated>
  </verify>

  <acceptance_criteria>
    - `bash plugin/conductor/scripts/check-map-drift.sh --help` sort **0** et sa sortie contient une
      section `Bornes` citant nommément les **4** motifs de non-couverture.
    - La sortie de `--help` cite **ADR-031** et **ADR-055**.
    - `bash plugin/conductor/scripts/tests/test-check-map-drift.sh` sort **0**, **0 échec**, et
      compte **au moins 20 cas**.
    - Un cas de la suite atteste qu'aucun chemin d'exécution ne rend le code réservé : sur les
      fixtures de la suite, les seuls codes observés sont **0**, **3** et **64**.
    - `grep -c 'ADR-031' plugin/conductor/scripts/check-map-drift.sh` ≥ **1**.
    - `plugin/conductor/scripts/check-map-drift.sh` reste sous **250 lignes** de code hors
      commentaires (`grep -vc '^\s*#' `) — proportionné à ses deux paires, comme les gates voisins.
    - `git diff --name-only` ne liste que les deux fichiers du `files_modified` de ce plan.
  </acceptance_criteria>

  <done>
    Le gate écrit ses propres bornes : un lecteur sait sans le lire en entier ce qu'il couvre, ce
    qu'il refuse de couvrir, pourquoi il ne corrige pas, et ce que son vert signifie exactement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt cloné non maîtrisé → `check-map-drift.sh` | La cible de `--path` peut être un clone hostile (branche/PR tierce) ; sa configuration `.git/config` ne doit jamais faire exécuter de programme pendant un lint en lecture. |
| système de fichiers → boucles de balayage | Les noms de fichiers et de dossiers du lab sont des **entrées non fiables** : espaces, retours à la ligne, tirets initiaux, motifs de glob. |
| contenu de carte (markdown) → tokens de chemin extraits | Le texte d'un `CLAUDE.md` est fourni par l'utilisateur ; un token extrait ne doit jamais être évalué comme du code. |
| gate → système de fichiers du lab | Le gate est en **lecture seule** ; toute écriture serait une violation ADR-031 et une frontière franchie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02-01 | Tampering / Elevation of Privilege | invocation de `git` sur un dépôt non maîtrisé | high | mitigate | Wrapper `git_safe()` repris **verbatim** (`-c core.fsmonitor= -c core.hooksPath=/dev/null --no-optional-locks`) + `GIT_CONFIG_NOSYSTEM=1`, `GIT_TERMINAL_PROMPT=0`, `GIT_OPTIONAL_LOCKS=0`. Critère machine : une seule invocation `git -C` hors commentaires, celle du wrapper. |
| T-29-02-02 | Tampering | expansion de chemin non quoté / injection par nom de fichier | high | mitigate | Toute variable de chemin est entre guillemets sans exception ; les listes de fichiers viennent de `git ls-files` et non d'un glob de shell non borné ; `set -uo pipefail` + `shopt -s nullglob` ; un cas de suite emploie un nom de fichier à espace. |
| T-29-02-03 | Elevation of Privilege | correctif automatique non validé (ADR-031) | high | mitigate | Aucun mode correctif n'existe : critère `grep -v '^#' … | grep -cE -- '--update|--fix|--write'` rend 0, et le script n'ouvre aucun descripteur en écriture vers un chemin du lab. Bornes écrites en en-tête (tâche 3). |
| T-29-02-04 | Repudiation | vert-à-vide (fausse assurance de conformité) | high | mitigate | Verdict à trois branches ordonnées : 0 carte balayée → `NON VÉRIFIABLE` (exit 3) sur un chemin qui **ne peut pas** imprimer de compte de divergences ; tout verdict d'absence de divergence porte obligatoirement le compteur de cartes balayées. Prouvé par cas dédié + mutation. |
| T-29-02-05 | Tampering | `plugin/conductor/scripts/dag.sh` (socle `--scope`) | critical | mitigate | Hors du `files_modified` du plan ; critère `git diff --name-only -- …/dag.sh` vide sur chaque tâche ; le gate ne lit ni n'écrit aucun fichier de DAG (critère de non-dérive, Pitfall 2). |
| T-29-02-06 | Denial of Service | balayage récursif d'un arbre très large | medium | mitigate | Le recensement des cartes passe par `git ls-files` (borné aux fichiers suivis) ; la paire P2 est **non récursive** (dossier de l'index seulement) ; aucun parcours de `node_modules`. |
| T-29-02-07 | Information Disclosure | chemins absolus de la machine dans le signal | low | accept | Le signal n'imprime que des chemins relatifs à `--path` ; le risque résiduel (un `--path` absolu recopié dans un rapport) est sans conséquence pour un outil local. |
| T-29-02-SC | Tampering | chaîne d'approvisionnement (npm/pip/cargo) | low | accept | Aucun paquet installé — `bash`, `git`, `python3` sont des dépendances système déjà exercées par les suites existantes (`29-RESEARCH.md` §Package Legitimacy Audit). Aucun `[ASSUMED]`/`[SUS]` à faire vérifier ; aucun checkpoint de légitimité requis. |
| T-29-02-08 | Information Disclosure | `p2_sens_a` — citation d'index `../` révèle par traversée l'existence d'un chemin hors `$ROOT` | medium | mitigate | Verdict humain (T-29-05-3, exec-02 tour 5) : mitigation triviale appliquée, cohérente avec le traitement déjà réservé aux tokens absolus en `p1_sens_a` (:170-172). Toute citation dont le raw commence par `../` est écartée AVANT le test d'existence — `[ ! -e "$target" ]` n'est jamais évalué sur un chemin qui sort de `$ROOT`. Résidu documenté et accepté : un `../` non initial (interne à la citation, ex. `sub/../../x.md`) n'est pas couvert par cette mitigation triviale — laissé `low` plutôt que de risquer une résolution lexicale complète en fin de phase (cf. décision `../` non résolu de `normalize_path()`, même ADR-031). Prouvé par cas dédié (`stride-dotdot`) : cible réelle hors `$ROOT` citée via `../`, verdict inchangé (0 divergence) qu'elle existe ou non. |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-map-drift.sh` → **0**, 0 échec, ≥ 20 cas.
- `bash plugin/conductor/scripts/tests/test-check-doc-drift.sh` → **0** (gate voisin non régressé).
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` → **0** (aucun recouvrement introduit).
- `bash plugin/conductor/scripts/tests/test-dag.sh` → **0** (D-03, non-régression du socle).
- `git diff --name-only -- plugin/conductor/scripts/dag.sh` → **vide**.
- `bash -n plugin/conductor/scripts/check-map-drift.sh` → **0**.
- `bash plugin/conductor/scripts/check-map-drift.sh --path "$(mktemp -d)"` → **3**, stdout contenant `NON VÉRIFIABLE`.
</verification>

<success_criteria>
1. `check-map-drift.sh` existe, constate deux paires carte↔disque bidirectionnelles, et sa suite le
   prouve cas par cas (≥ 20 cas, ≥ 2 mutations attestées à l'octet).
2. Le gate ne peut pas rendre un vert à vide : le plancher `NON VÉRIFIABLE` est prouvé, pas déclaré.
3. Aucun mode correctif n'existe (ADR-031), et l'en-tête l'écrit avec son motif.
4. Le gate écrit ses bornes : 4 motifs de non-couverture, la hiérarchie avec les gates voisins, et
   la limite « carte balayée ≠ carte correcte ».
5. `dag.sh` intact (D-03) ; aucun `jq`, `grep -P` ni `sed -i` (ADR-054).
</success_criteria>

<output>
Create `.planning/phases/VFDO-29-distiller-les-gains-icm-g1-g5-investigation-dag-sh-scope-d-a/29-02-SUMMARY.md` when done
</output>

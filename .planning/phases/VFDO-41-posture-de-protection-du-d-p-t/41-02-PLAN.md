---
phase: 41-posture-de-protection-du-d-p-t
plan: 02
lot: L2 — CODEOWNERS étroit et ledger PROT-01..04
type: execute
wave: 2
depends_on: ["41-01"]
autonomous: true
requirements: [PROT-04]
files_modified:
  - .github/CODEOWNERS
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.github/CODEOWNERS` porte exactement 4 règles, toutes au seul propriétaire `@picmakpro` : `/.github/`, `/.planning/instruction-budget-baselines.tsv`, `/.planning/.*-armed`, `/scripts/hooks/` — aucun autre chemin (D-05, D-01)"
    - "Les motifs sont ancrés à la racine ; `*` y couvre un nom commençant par un point — sourcé : docs GitHub about-code-owners (motifs « follows most of the same rules used in gitignore files », exceptions listées : échappement `\\#`, négation `!`, plages `[ ]`) et git-scm gitignore (« An asterisk \"*\" matches anything except a slash », aucune exception pour le point), lus le 2026-09-17"
    - "La couverture est prouvée localement par le moteur de motifs gitignore de git (témoins positifs ET négatifs) ; la preuve réelle côté GitHub vient après merge (plan 41-04 : `codeowners/errors` à 0 ; plan 41-06 : demande de revue `@picmakpro` sur une PR touchant `.github/`)"
    - "Le ledger porte PROT-01..04 NON cochés, définis depuis les arbitrages, avec quatre lignes de traçabilité `Pending` ; ils ne sont cochés qu'à la clôture sur pièce (plan 41-13)"
    - "Ordre séquentiel sur la branche A (point 10 du tour 1) : 41-01 → 41-02 → 41-03, chacun committant après le précédent ; aucun commit concurrent dans le même arbre"
    - "O-3 n'est associée à aucun vocabulaire d'achèvement dans le ledger ; tout commit de plan citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
  artifacts:
    - path: ".github/CODEOWNERS"
      provides: "revue code owner `@picmakpro` sur les chemins qui neutraliseraient un gate (O-3)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROT-01..04 définis et tracés"
  key_links:
    - "CODEOWNERS n'agit qu'une fois sur la branche de base : il n'a aucun effet avant le merge de la PR de la phase (41-04), et la règle `require_code_owner_review` du ruleset (41-01, 41-05) est ce qui le rend exigible"
---

<objective>
Poser le fichier CODEOWNERS du périmètre étroit de D-05 et inscrire PROT-01..04 au ledger, non
cochés.

Purpose: PROT-04 — O-3 passe de « gardée par la seule relecture » à « gardée par défaut + tracée »
dès que ce fichier est sur `main` et que le ruleset exige la revue code owner.
Output: `.github/CODEOWNERS`, section PROT de `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/REQUIREMENTS.md

Sources lues le 2026-09-17 : `https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners`
(emplacements `.github/` > racine > `docs/` ; ligne invalide sautée et signalée dans l'UI ;
propriétaire = droit write minimum ; version lue = branche de base) ;
`https://git-scm.com/docs/gitignore` (ancrage par séparateur initial, `/` final = répertoires,
`*` = tout sauf `/`).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : `.github/CODEOWNERS` à quatre règles, couverture prouvée par témoins positifs et négatifs</name>
  <files>.github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (D-01, D-02, D-05, § Conséquences de D-02)
  </read_first>
  <action>
Créer `.github/CODEOWNERS`. En-tête en commentaires `#`, en français :
- objet : revue code owner exigée sur les chemins dont la modification peut neutraliser un gate
  (O-3 du `25-SECURITY.md`) — D-05, arbitrage Samuel, AskUserQuestion session principale,
  2026-09-17 ; `@picmakpro` est opéré par Samuel en personne (D-01) ;
- portée : garde-fou par défaut + trace, pas verrou — le rôle write peut contourner, et le
  contournement laisse une entrée dans les rule suites (D-02, ADR-072) ;
- lecture par GitHub : version de la branche de base, donc une modification de ce fichier dans une
  PR ne vaut qu'après son merge ;
- syntaxe : motifs de type gitignore, ancrés par `/` ; `*` couvre aussi un nom commençant par un
  point ; ni `!`, ni `[ ]`, ni échappement de `#` (docs GitHub about-code-owners, git-scm gitignore) ;
- « aucun autre chemin » (D-05) : toute extension est un arbitrage.

Puis exactement quatre lignes de règle, chacune `<motif> @picmakpro`, sans commentaire en fin de
ligne : `/.github/`, `/.planning/instruction-budget-baselines.tsv`, `/.planning/.*-armed`,
`/scripts/hooks/`. Commit local sur la branche A, message « feat(41-02): CODEOWNERS étroit @picmakpro (D-01, D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=.github/CODEOWNERS; T=$(mktemp -d); awk "!/^[ \t]*#/ && NF" "$F" > "$T/regles"; n=$(awk "END{print NR}" "$T/regles"); own=$(awk "NF!=2 || \$2!=\"@picmakpro\" {c++} END{print c+0}" "$T/regles"); awk "{print \$1}" "$T/regles" | LC_ALL=C sort > "$T/motifs"; printf "%s\n" "/.github/" "/.planning/.*-armed" "/.planning/instruction-budget-baselines.tsv" "/scripts/hooks/" | LC_ALL=C sort > "$T/attendu"; me=$(cmp -s "$T/motifs" "$T/attendu" && echo oui || echo non); ko=0; for p in .github/CODEOWNERS .github/workflows/ci.yml .github/rulesets/main.json .github/rulesets/tags-v.json .planning/instruction-budget-baselines.tsv .planning/.instruction-budget-armed .planning/.requirements-survival-armed scripts/hooks/pre-push scripts/hooks/post-merge; do git -c core.excludesFile="$T/motifs" check-ignore -q --no-index "$p" || { echo "NON COUVERT $p"; ko=$((ko+1)); }; done; for p in scripts/check-release-tag.sh .planning/STATE.md .planning/REQUIREMENTS.md plugin/conductor/scripts/check-instruction-budget.sh .gitignore CLAUDE.md docs/ADR.md; do if git -c core.excludesFile="$T/motifs" check-ignore -q --no-index "$p"; then echo "COUVERT A TORT $p"; ko=$((ko+1)); fi; done; echo "codeowners : regles=$n proprietaire_hors_picmakpro=$own motifs_egaux_attendu=$me ecarts_de_couverture=$ko"; [ "$n" -eq 4 ] && [ "$own" -eq 0 ] && [ "$me" = oui ] && [ "$ko" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `regles=` différent de 4 ; ou `proprietaire_hors_picmakpro=` différent de 0 (autre propriétaire, propriétaire en plus, commentaire en fin de ligne) ; ou `motifs_egaux_attendu=non` (motif non ancré, élargi ou réécrit — un `/.planning/*-armed` couvre les mêmes fichiers mais n'est pas le motif arbitré) ; ou une ligne `NON COUVERT` (fichier point des sentinelles, baseline, hooks, `.github/`) ; ou une ligne `COUVERT A TORT` (périmètre élargi au-delà de D-05, ex. `/scripts/`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash scripts/check-machine-paths.sh > /dev/null 2>&1 || rc=$?; echo "check-machine-paths rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul</fails_when>
  </verify>
  <acceptance_criteria>
    - 4 règles exactement, motifs ancrés identiques à D-05, propriétaire unique `@picmakpro`
    - les 9 chemins protégés couverts (dont les deux sentinelles `.planning/.*-armed`), les 7 témoins négatifs non couverts
    - en-tête citant D-01, D-02, D-05, la lecture sur branche de base et les deux sources de syntaxe
  </acceptance_criteria>
  <done>Le fichier dit exactement le périmètre arbitré, et la couverture est prouvée dans les deux sens.</done>
</task>

<task type="auto">
  <name>Task 2 : ledger PROT-01..04 (définitions non cochées + traçabilité Pending), rejeu des gates</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (section `### vibeflow-head — head of minds du dev-orchestrator (Phase 40)` et `### Transverse` vers L1086-1132 ; table `## Traceability`, dernière ligne `| PART-09 |` vers L862)
    - .planning/ROADMAP.md § Phase 41 (libellés de PROT-01..04, critères 1-4)
  </read_first>
  <action>
Insérer, juste avant `### Transverse`, une section `### Posture de protection du dépôt (Phase 41)`
ouverte par une note `>` : « Dérivé au cadrage le 2026-09-17 (`41-CONTEXT.md`, D-01..D-10 —
arbitrages Samuel, AskUserQuestion session principale, 2026-09-17 ; D-M1..D-M14 décisions du
manager). Préfixe `PROT` vérifié libre (0 occurrence, mesuré le 2026-09-17). Cochés seulement à la
clôture, sur pièce (`41-PREUVES.md`). »

Puis quatre entrées non cochées, au format `- [ ] **PROT-0N**: …`, chacune citant ses décisions :
- **PROT-01** : ruleset de branche actif sur `main` (PR obligatoire à 0 approbation, les 4 jobs CI
  requis épinglés sur `integration_id` 15368 et à jour, revue code owner requise, suppression et
  force push bloqués, méthodes de merge non restreintes, contournement rôle `write` en mode
  `pull_request`) et ruleset de tags `v*` (réécriture, suppression, force push restreints, création
  libre, contournement `write` en `always`), posés depuis `.github/rulesets/` et prouvés par essais
  réels — refus par défaut d'une PR rouge, trace d'un contournement lue dans les rule suites, refus
  d'un push direct (D-02, D-03, D-04, D-07, D-09, D-10, D-M1, D-M4, D-M6, D-M7, D-M12, D-M13) ;
- **PROT-02** : flux de release rejoué sous la règle sans contournement — bump → PR → merge → tag
  annoté → release GitHub → `check-release-tag.sh --remote` ✓ ; `check-release-tag.sh` et le hook
  `pre-push` inchangés (D-M5, D-M8, D-M9) ;
- **PROT-03** : politique de contournement et de hotfix écrite en ADR-072 (qui contourne quoi, par
  quel geste, avec quelle trace ; retour arrière), résumée au `CLAUDE.md`, ADR-059 amendé, item
  BACKLOG fermé par renvoi (D-02, D-03, D-M10) ;
- **PROT-04** : O-3 « gardée par défaut + tracée » — `.github/CODEOWNERS` `@picmakpro` sur les
  quatre chemins de D-05 et revue code owner requise, prouvées par une demande de revue réelle et
  un refus sans contournement (D-01, D-02, D-05).

Dans `## Traceability`, ajouter après la ligne `| PART-09 |` quatre lignes
`| PROT-0N | Phase 41 | Pending |`. Ne toucher à aucune autre ligne (QUAL-01 est mis à jour à la
clôture, plan 41-13). O-3 n'est jamais associée, même par négation, à un mot d'achèvement (D-02).
STATE.md et ROADMAP.md non touchés. Commit local sur la branche A, message « docs(41-02): ledger
PROT-01..04 non cochés (D-01, D-02, D-03, D-04, D-05, D-07, D-09, D-10 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'R=.planning/REQUIREMENTS.md; u=$(awk "/^- \\[ \\] \\*\\*PROT-0[1-4]\\*\\*: /{c++} END{print c+0}" "$R"); x=$(awk "/^- \\[x\\] \\*\\*PROT-/{c++} END{print c+0}" "$R"); t=$(awk "/^\\| PROT-0[1-4] \\| Phase 41 \\| Pending \\|/{c++} END{print c+0}" "$R"); s=$(awk "/^### Posture de protection du dépôt \\(Phase 41\\)\$/{print NR; exit}" "$R"); q=$(awk "/^### Transverse\$/{print NR; exit}" "$R"); h=$(awk "/^### vibeflow-head/{print NR; exit}" "$R"); d=$(awk "/^- \\[ \\] \\*\\*PROT-0[1-4]\\*\\*: / && /D-/ {c++} END{print c+0}" "$R"); echo "ledger : non_coches=$u coches=$x table_pending=$t section=$s transverse=$q vibeflow_head=$h entrees_citant_decisions=$d"; [ "$u" -eq 4 ] && [ "$x" -eq 0 ] && [ "$t" -eq 4 ] && [ -n "$s" ] && [ -n "$q" ] && [ -n "$h" ] && [ "$h" -lt "$s" ] && [ "$s" -lt "$q" ] && [ "$d" -eq 4 ]'</automated>
    <fails_when>exit non nul ; ou `non_coches=` différent de 4 ; ou `coches=` différent de 0 (coché avant la clôture sur pièce) ; ou `table_pending=` différent de 4 ; ou section absente, ou hors de l'intervalle vibeflow-head < section < Transverse ; ou `entrees_citant_decisions=` différent de 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou bilan absent ; ou N = 0 ; ou N différent de `decouverte_independante` ; ou M différent de 0. Rouge T28-F : même conduite qu'au plan 41-01 (préexistant prouvé sur `origin/main`, remonté `human_needed`, jamais neutralisé)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/REQUIREMENTS.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk "/^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"MESSAGE SANS CANAL NI DATE : \" s; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/l"'</automated>
    <fails_when>exit non nul ; ou une ligne `MESSAGE SANS CANAL NI DATE :` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `commits_de_plan=0` (rien de committé à juger)</fails_when>
  </verify>
  <acceptance_criteria>
    - 4 entrées PROT non cochées entre la section Phase 40 et `### Transverse`, chacune citant ses décisions
    - 4 lignes `Pending` en traçabilité, aucune autre ligne du ledger modifiée
    - rejeux `gates` et `tests` verts sur la découverte complète
    - aucune proposition du ledger n'associe O-3 à un mot d'achèvement ; messages de commit de plan conformes (canal et date)
  </acceptance_criteria>
  <done>Le ledger dit ce que la phase doit prouver, sans rien déclarer prouvé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → branche de base | CODEOWNERS n'est lu que sur la base : une PR ne peut pas desserrer sa propre revue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-08 | Tampering | motif CODEOWNERS ne couvrant pas les sentinelles point | high | mitigate | témoins positifs sur les deux `.planning/.*-armed` par le moteur gitignore de git ; preuve GitHub réelle aux plans 41-04 (`codeowners/errors`) et 41-06 (demande de revue) |
| T-41-09 | Elevation of privilege | revue code owner contournée par un compte write | medium | accept | arbitré D-02 (« garde-fou par défaut + trace, pas verrou ») ; la trace est prouvée au plan 41-08 |
| T-41-10 | Repudiation | exigence cochée sans preuve | medium | mitigate | vérification `coches=0` ; cochage réservé au plan 41-13 sur lignes de `41-PREUVES.md` |
</threat_model>

<verification>
CODEOWNERS à quatre règles prouvé dans les deux sens ; PROT-01..04 inscrits non cochés ; gates et
tests verts.
</verification>

<success_criteria>
PROT-04 a sa source versionnée ; le ledger de la phase est posé dans la PR de la phase.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-02-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-protection-depot`).
</output>

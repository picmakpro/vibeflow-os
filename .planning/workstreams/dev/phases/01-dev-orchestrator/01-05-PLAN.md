---
phase: 01-dev-orchestrator
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - _internal/vibeflow-update.sh
  - dev-orchestrator/README.md
  - README.md
autonomous: true
requirements: [VERIF-01, VERIF-02, IDX-02]
must_haves:
  truths:
    - "test-dev-orchestrator.sh passe à 100% (exit 0)"
    - "Le test vérifie : index non vide, idempotence ensure-deps, ≥11 intentions routées (cibles distinctes), aucun mapping /vf-* orphelin"
    - "Le test vérifie les gates de densité par wc -l explicite (agent ≤250L, chaque skill ≤500L) — PAS via check-file-size.sh qui ignore les .md"
    - "vibeflow-update.sh installe le module (AGENT.md + skills/ + scripts/ + references/) et copie references/ d'un module agent vers .claude/agents/<mod>-references/ (D7)"
    - "Le hook post-install régénère l'index via VF_INDEX_OUT pointant vers .claude/agents/dev-orchestrator-references/gsd-skills-index.md"
    - "Un test vérifie qu'à l'install, GSD-PIPELINE.md, gsd-skills-index.md et vocabulary-map.md sont bien copiés dans .claude/agents/dev-orchestrator-references/"
    - "Le README du module et la table modules du README racine documentent dev-orchestrator"
  artifacts:
    - path: "dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      provides: "Suite de vérification complète du module"
      min_lines: 40
    - path: "dev-orchestrator/README.md"
      provides: "Doc complète du module (remplace le squelette)"
      min_lines: 30
  key_links:
    - from: "_internal/vibeflow-update.sh"
      to: "dev-orchestrator/scripts/build-gsd-index.sh"
      via: "régénération de l'index à l'install via VF_INDEX_OUT vers .claude/agents/<mod>-references/ (IDX-02, D7)"
      pattern: "build-gsd-index"
    - from: "_internal/vibeflow-update.sh"
      to: ".claude/agents/dev-orchestrator-references/"
      via: "copie des references d'un module agent (D7)"
      pattern: "-references"
    - from: "dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      to: "dev-orchestrator/references/gsd-skills-index.md"
      via: "vérification mapping /vf-* non orphelin contre l'index"
      pattern: "gsd-skills-index"
---

<objective>
Verrouiller la qualité : suite `test-dev-orchestrator.sh`, intégration du module dans
`vibeflow-update.sh` (install + copie references D7 + régénération auto de l'index = IDX-02),
et finalisation du README.

Purpose: Garantir VERIF-01 (couverture des 4 axes de test) et VERIF-02 (gates de densité par
wc -l explicite), et rendre le module installable de bout en bout via l'installeur central. Boucle
l'install : un `vibeflow-update.sh install dev-orchestrator` doit poser l'agent, les verbes, les
scripts, les references sous `.claude/agents/dev-orchestrator-references/` (D7) et un index frais.
Output: tests verts, installeur câblé (copie references agent + hook index), README complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Installeur à étendre (lire la logique d'install par type, lignes ~85-169) :
@_internal/vibeflow-update.sh

# Patterns de test à calquer (assert numérotés, fixtures temporaires, exit 0/1) :
@software-architecture/scripts/tests/test-check-file-size.sh

# ATTENTION : check-file-size.sh EXCLUT les .md (CODE_EXT_RE) — NE PAS l'utiliser pour mesurer
# la densité de AGENT.md/SKILL.md (il renverrait vert sans rien mesurer). Mesurer par wc -l.
@software-architecture/scripts/check-file-size.sh

# Artefacts produits aux plans 01-04 (cibles des tests) :
@dev-orchestrator/AGENT.md
@dev-orchestrator/references/gsd-skills-index.md
@dev-orchestrator/scripts/ensure-deps.sh

<interfaces>
<!-- Les 4 axes de test imposés par la spec §8 (VERIF-01) : -->
1. build-gsd-index.sh génère un index NON VIDE depuis les skills GSD installés.
2. ensure-deps.sh idempotent : 2e run = no-op (utiliser VF_ENSURE_DRY_RUN=1).
3. La table de routage de AGENT.md couvre ≥11 intentions (cibles DISTINCTES, pas un comptage de lignes).
4. Chaque skill /vf-* mappe vers une cible existante : skill GSD vérifié contre
   gsd-skills-index.md, OU superpowers (brainstorming), OU bootstrap interne (ensure-deps) — aucun orphelin.

<!-- VERIF-02 : densité — agent ≤250L, chaque skill /vf-* ≤500L. MESURE PAR wc -l UNIQUEMENT. -->

<!-- INSTALL D7 (Blocker 1) : l'installeur gère 5 types (vibeflow-update.sh). Le bloc references actuel
     (~ligne 146) ne copie references/ QUE si un SKILL.md racine existe → dev-orchestrator (module agent,
     AGENT.md sans SKILL.md racine) ne reçoit PAS ses references. CORRECTIF :
     - SI AGENT.md existe ET references/ existe → copier references/ vers .claude/agents/<mod>-references/.
     - Hook post-install : si scripts/build-gsd-index.sh existe, le ré-exécuter avec
       VF_INDEX_OUT=.claude/agents/<mod>-references/gsd-skills-index.md pour régénérer l'index in-place
       (best-effort : ne pas faire échouer l'install si GSD absent).
     Fichiers attendus après install dans .claude/agents/dev-orchestrator-references/ :
       GSD-PIPELINE.md, gsd-skills-index.md, vocabulary-map.md. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire test-dev-orchestrator.sh (4 axes + densité wc -l)</name>
  <files>dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <action>
    Créer la suite de test en calquant test-check-file-size.sh (shebang bash, `set -uo pipefail`,
    helpers `ok()`/`ko()`/`skip()`, asserts numérotés, exit 0 si tout passe sinon 1). Résoudre le chemin
    du module via `MOD="$(cd "$(dirname "$0")/../.." && pwd)"`.
    Tests (VERIF-01) :
    - T1 — Index : exécuter `build-gsd-index.sh` puis vérifier que `references/gsd-skills-index.md`
      est non vide et contient ≥1 nom de skill distinct (`grep -Eo 'gsd-[a-z0-9-]+' | sort -u | wc -l`).
      Robustesse (warning) : si AUCUN skill gsd-* n'est installé sur la machine (env sans GSD),
      la génération produit légitimement un index sans skill → marquer T1 comme SKIP explicite
      (skip "GSD non installé — index vide attendu"), PAS un échec. Détection : tester la présence
      d'au moins un `~/.claude/skills/gsd-*/SKILL.md` avant d'exiger des lignes.
    - T2 — Idempotence : `VF_ENSURE_DRY_RUN=1 bash scripts/ensure-deps.sh` deux fois ; vérifier
      exit 0 aux deux runs (pas d'échec, no-op stable).
    - T3 — Routage (Blocker 3) : DEUX vérifications complémentaires sur AGENT.md, hors lignes commentaire :
        (a) cibles DISTINCTES :
            `grep -v '^#' AGENT.md | grep -Eo 'brainstorm(ing)?|gsd-discuss-phase|gsd-plan-phase|gsd-execute-phase|gsd-quick|gsd-fast|gsd-verify-work|gsd-code-review|gsd-debug|gsd-autonomous|gsd-ship|gsd-progress|gsd-map-codebase' | sort -u | wc -l` ≥ 11 ;
        (b) lignes d'intentions NL : compter les lignes de la table de routage (lignes contenant `|` avec une intention NL, ex. `grep -Ec '^\|.*\|' ` sur la section table, ou compter les lignes `|` hors header/séparateur) ≥ 11.
        Échec si l'une des deux < 11. (Ne PAS utiliser un `grep -Ec` brut qui compte les lignes au lieu des cibles.)
    - T4 — Mapping non orphelin (warning robustesse) : pour chaque `skills/vf-*/SKILL.md`, extraire la cible
      référencée (gsd-X / brainstorming / ensure-deps). La valider :
        - gsd-X → présent dans `references/gsd-skills-index.md` SI cet index contient des skills ;
          si l'index est VIDE (env sans GSD), basculer sur un INDEX DE FIXTURE embarqué dans le test
          (liste des 12 cibles canoniques connues) plutôt qu'un faux échec — OU marquer T4 SKIP explicite
          avec message clair. Ne jamais produire un faux négatif faute de GSD installé.
        - brainstorming → accepté (superpowers) ; ensure-deps → accepté (bootstrap interne).
      Aucun orphelin réel ne doit passer.
    - T5 — Densité (VERIF-02, Blocker 2) : MESURE PAR wc -l UNIQUEMENT (NE PAS appeler check-file-size.sh
      qui ignore les .md) :
        - `[ "$(wc -l < "$MOD/AGENT.md")" -le 250 ]`
        - pour chaque `"$MOD"/skills/vf-*/SKILL.md` : `[ "$(wc -l < "$f")" -le 500 ]`.
    Le script doit pouvoir tourner depuis le repo (chemins relatifs au module). Français pour les libellés.
    Un test SKIP ne fait PAS échouer la suite (exit 0 si seuls des SKIP + OK).
  </action>
  <verify>
    <automated>bash -n dev-orchestrator/scripts/tests/test-dev-orchestrator.sh && ! grep -q 'check-file-size' dev-orchestrator/scripts/tests/test-dev-orchestrator.sh && grep -q 'wc -l' dev-orchestrator/scripts/tests/test-dev-orchestrator.sh && grep -q 'sort -u' dev-orchestrator/scripts/tests/test-dev-orchestrator.sh && bash dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; test $? -eq 0</automated>
  </verify>
  <done>test-dev-orchestrator.sh couvre les 4 axes (index avec SKIP si pas de GSD, idempotence, routage = cibles distinctes ET lignes d'intentions ≥11, mapping non orphelin robuste à un index vide) + densité par wc -l explicite (jamais check-file-size sur .md), et passe à exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler vibeflow-update.sh (copie references D7 + hook index) et finaliser README</name>
  <files>_internal/vibeflow-update.sh, dev-orchestrator/README.md, README.md</files>
  <action>
    1. Installeur — copie references module agent (Blocker 1 / D7). Éditer `_internal/vibeflow-update.sh`,
       fonction `install_module` :
       - Le bloc existant "References folder at module root (companion to root SKILL.md)" (~ligne 146)
         ne copie references/ QUE si un `SKILL.md` racine existe. AJOUTER un cas pour les modules AGENT :
         SI `[ -d "$module_dir/references" ] && [ -f "$module_dir/AGENT.md" ]` → copier `references/` vers
         `.claude/agents/${mod}-references/` (`mkdir -p` puis `cp -r`). Logger
         `copied references/ → .claude/agents/${mod}-references/`.
       - Hook post-install (IDX-02 / D7) : APRÈS le bloc scripts, si `scripts/build-gsd-index.sh` existe
         pour le module, l'exécuter en redirigeant l'index vers le dossier references agent :
         `VF_INDEX_OUT=".claude/agents/${mod}-references/gsd-skills-index.md" bash ".claude/scripts/build-gsd-index.sh" || log "  (index non régénéré — GSD absent, best-effort)"`.
         Best-effort : ne JAMAIS faire échouer l'install si GSD est absent. Garder l'idempotence et le style de log.
       - Pendant que tu y es, étendre `uninstall_module` (et idéalement `backup_module`) pour gérer
         `.claude/agents/${mod}.md` et `.claude/agents/${mod}-references/` (cohérence install/uninstall).
    2. README module : remplacer le squelette `dev-orchestrator/README.md` par une doc complète
       (overview, structure, install via vibeflow-update, usage NL + verbes /vf-*, note bootstrap,
       note D7 : références installées sous `.claude/agents/dev-orchestrator-references/`).
    3. README racine : ajouter une ligne dans la table des modules du `README.md` racine
       (nom: dev-orchestrator, version, type "agent + skills + scripts", description courte).
    4. Test d'install end-to-end (warning 3, best-effort) : ajouter à la fin de
       `test-dev-orchestrator.sh` (édition complémentaire de ce fichier) un test T6 qui, dans un
       répertoire temporaire (`mktemp -d`), simule l'install : copie le module dans un faux
       `.vibeflow-cache/dev-orchestrator/`, lance `bash _internal/vibeflow-update.sh install dev-orchestrator`
       (depuis le tmp dir, VIBEFLOW_CACHE pointant dessus), puis vérifie que dans ce tmp lab :
       `.claude/agents/dev-orchestrator.md` existe ET `.claude/agents/dev-orchestrator-references/GSD-PIPELINE.md`,
       `.../gsd-skills-index.md`, `.../vocabulary-map.md` existent (couvre le critère #1 du ROADMAP).
       SI l'install e2e n'est pas réalisable simplement dans l'environnement (ex. dépendances réseau) →
       marquer T6 SKIP explicite avec un commentaire « install e2e non testée automatiquement ici » ;
       ne jamais faire échouer la suite pour cette raison.
    Français. Après édition, re-lancer le test pour confirmer non-régression.
  </action>
  <verify>
    <automated>bash -n _internal/vibeflow-update.sh && grep -q 'build-gsd-index' _internal/vibeflow-update.sh && grep -q 'VF_INDEX_OUT' _internal/vibeflow-update.sh && grep -q -- '-references' _internal/vibeflow-update.sh && grep -q 'dev-orchestrator' README.md && [ "$(wc -l < dev-orchestrator/README.md)" -ge 30 ] && bash dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; test $? -eq 0</automated>
  </verify>
  <done>vibeflow-update.sh copie references/ d'un module agent vers .claude/agents/<mod>-references/ (D7) et régénère l'index post-install via VF_INDEX_OUT (best-effort) ; install/uninstall gèrent l'agent et son dossier references ; README module (≥30L, note D7) et table racine documentent dev-orchestrator ; le test e2e (T6, best-effort/SKIP) couvre le critère install ; les tests passent toujours (exit 0).</done>
</task>

</tasks>

<verification>
- VERIF-01 : test-dev-orchestrator.sh couvre les 4 axes (avec SKIP robustes si pas de GSD) et passe à 100% ✓
- VERIF-02 : gates de densité (agent ≤250L, skills ≤500L) vérifiés par wc -l explicite (jamais check-file-size sur .md) ✓
- Blocker 1 / D7 : installeur copie references/ d'un module agent vers .claude/agents/<mod>-references/ ; les 3 docs (GSD-PIPELINE, gsd-skills-index, vocabulary-map) sont présentes après install (vérifié T6) ✓
- IDX-02 : index régénéré à l'install via VF_INDEX_OUT vers le dossier references agent ✓
- Module installable de bout en bout via vibeflow-update.sh (agent + verbes + scripts + references D7 + index frais) ✓
- README module + table racine à jour ✓
</verification>

<success_criteria>
- Tests verts, installeur câblé (copie references agent + hook index D7), doc complète. Le module est distribuable.
- Critères de succès de phase 1 et 5 (ROADMAP) vérifiables programmatiquement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-05-SUMMARY.md`
</output>

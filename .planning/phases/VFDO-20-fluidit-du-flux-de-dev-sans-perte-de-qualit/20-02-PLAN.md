---
phase: 20-fluidite-du-flux-de-dev-sans-perte-de-qualite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/dag.sh
  - plugin/conductor/scripts/tests/test-dag.sh
autonomous: true
requirements: [SC4, SC5]

must_haves:
  truths:
    - "SC4 / D-13 : `dag.sh add` accepte `--scope=<liste de chemins ou globs séparés par des virgules>` et écrit un champ `scope` (tableau de chaînes) dans le nœud. Sans le flag, le champ vaut le tableau vide."
    - "SC4 / D-13 rétro-compatibilité prouvée : un fichier DAG écrit AVANT ce changement (nœuds sans clé `scope`) est relu, muté (`mark`, `reopen`) et rendu (`ready`, `status`, `tree`) sans aucune erreur — toute lecture du champ passe par un accès tolérant à l'absence, jamais par un accès direct qui lèverait."
    - "SC4 / D-14 : `dag.sh reopen` force `review_regime` à la valeur `full` sur le nœud rouvert ET sur chacun de ses dépendants transitifs, dès lors que leur identifiant désigne un nœud de revue ou de jointure. Le régime est écrit par le script lui-même — enforcement machine, jamais une consigne de prompt."
    - "SC4 / D-14 sélecteur explicite : un nœud est un nœud de revue ou de jointure si son identifiant commence par `revue-`, `revue:`, `join-`, `join:`, ou vaut exactement `join`. Un nœud `exec-3` rouvert ne reçoit JAMAIS de champ `review_regime`."
    - "SC4 / D-14 garde-fou de comblement rendu machine : après un `reopen` déclenché par un correctif, le nœud de revue ne peut plus être classé en régime allégé — c'est la traduction exécutable de « aucun allègement ne s'applique jamais à un diff de comblement »."
    - "SC5 / D-15 §2 : `dag.sh status` expose les périmètres gelés — un tableau des nœuds dont le statut n'est pas `done` et dont le `scope` est non vide, chacun avec son identifiant, son statut et son périmètre. C'est ce qui rend la « table des fichiers gelés » interrogeable À LA DEMANDE au lieu d'être recopiée."
    - "SC4 / le schéma du nœud reste UN SEUL objet JSON : `scope` et `review_regime` vivent dans le nœud, jamais dans un fichier de mapping séparé."
    - "SC4 / portabilité prouvée par EXÉCUTION : `test-dag.sh` rend 0 KO sur macOS. La preuve Linux est déléguée au job CI `tests` (ubuntu-latest) au push — vérification différée explicite."
  artifacts:
    - plugin/conductor/scripts/dag.sh
    - plugin/conductor/scripts/tests/test-dag.sh
  key_links:
    - "`dag.sh --scope` → `plugin/dev-orchestrator/agents/vf-dev-manager.md` (plan 20-06) : le manager doit DÉCLARER le périmètre de chaque nœud au moment du `dag.sh add`. L'instruction existe déjà dans son corps ; l'outil ne savait pas l'exécuter. C'est la couture cross-module de la phase (conductor mandatory → dev-orchestrator non mandatory) : sonde de présence, jamais de dépendance inversée."
    - "`dag.sh --scope` → critère (b) de la gradation par risque (D-12) : « fichier partagé avec une mission parallèle en vol » n'est calculable que si les périmètres sont déclarés."
    - "`dag.sh status` (périmètres gelés) → `.planning/MISSION-INVARIANTS.md` §2 (plan 20-05) : le fichier documente une convention de lecture dynamique dont c'est la commande. Sans cette sortie, §2 renverrait vers un geste inexistant."
    - "`dag.sh reopen` + `review_regime` → `mission-flow.md` §Étage revue (plan 20-06) : la doctrine y renvoie comme au garant machine du régime plein après comblement."
  prohibitions:
    - "P-01 — MUST NOT introduire un fichier séparé de mapping nœud → périmètre. Le patron actuel porte tout l'état du nœud dans un seul objet JSON (D-13) ; le casser romprait la lisibilité du DAG et les rendus `status`/`tree`."
    - "P-02 — MUST NOT lire le champ `scope` (ni `review_regime`) par accès direct à la clé. Les DAG déjà écrits sur disque (`.planning/missions/dag-phase17.json`, `dag-phase19.json`) n'ont pas ces clés : tout accès doit tolérer l'absence et rendre le tableau vide."
    - "P-03 — MUST NOT laisser `review_regime` être décidé par une consigne de prompt. D-14 exige que le champ soit écrit par `reopen` lui-même."
    - "P-04 — MUST NOT poser `review_regime` sur un nœud qui n'est ni de revue ni de jointure. Un `exec-*` rouvert reste sans ce champ."
    - "P-05 — MUST NOT modifier le contrat de sortie des sous-commandes existantes autrement qu'en AJOUTANT des clés. Les consommateurs actuels lisent `ready`, `count`, `total`, `counts`, `added`, `remapped`, `reopened`, `dependents_reset` : ces clés gardent leur nom et leur sens."
    - "P-06 — MUST NOT toucher au rendu `tree` (glyphes, connecteurs, bornage des cycles) ni à la logique de remap de collision d'identifiant."
    - "P-07 — MUST NOT modifier `.github/workflows/ci.yml`, la `VERSION` racine ni les README racine."
    - "P-08 — MUST NOT lire, éditer ni committer `.planning/missions/2026-07-29-diagnostic-moteur-gsd-absent.md` (fichier non suivi appartenant à une autre session). Les DAG de mission `.planning/missions/dag-*.json` sont lus en LECTURE SEULE, uniquement pour la preuve de rétro-compatibilité, et jamais réécrits."
---

<objective>
Donner au kernel de mission les deux champs qui manquent au schéma de nœud : le PÉRIMÈTRE déclaré
(`--scope`, D-13) et le RÉGIME DE REVUE forcé au comblement (`review_regime`, D-14) — plus la
commande qui rend le premier interrogeable (`status` expose les périmètres gelés, pré-requis de
`MISSION-INVARIANTS.md` §2).

Purpose: `vf-dev-manager` reçoit déjà l'instruction de déclarer le périmètre de chaque nœud au
moment du `dag.sh add` — l'outil ne sait pas l'exécuter. Sans ce champ, le critère (b) de la
gradation par risque (fichier partagé avec une mission parallèle en vol) n'est pas calculable, et la
table des fichiers gelés n'a aucune source vivante. Sans `review_regime`, le garde-fou non
négociable de la phase (« aucun allègement sur un diff de comblement ») resterait déclaratif.

Output: 1 script kernel étendu de façon additive et rétro-compatible, 1 suite étendue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-RESEARCH.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

## Périmètre, dépendances et risque (fiche de lecture directe pour `vf-dev-manager`)

| Rubrique | Valeur |
|---|---|
| **Changement couvert** | Changement 3 du CONTEXT (`dag.sh --scope`), volet outillage de SC4 + la commande qui alimente SC5 §2 |
| **Décisions implémentées** | D-13, D-14 ; alimente D-12 (critère b) et D-15 §2 |
| **Fichiers écrits** | `plugin/conductor/scripts/dag.sh`, `plugin/conductor/scripts/tests/test-dag.sh` |
| **Dépendances amont** | aucune — vague 1 |
| **Dépendances aval (qui attend ce plan)** | **20-05** (`MISSION-INVARIANTS.md` §2 renvoie vers la commande de lecture des périmètres gelés) · **20-06** (`vf-dev-manager.md` et `mission-flow.md` consomment les 2 champs) · **20-07** (la doctrine de `team-kernel.md`/`README.md` cite le flag dans la ligne du brique DAG) |
| **Critères de succès** | SC4 (volet outillage), SC5 (volet §2 interrogeable) |
| **Risque** | **moyen** — `dag.sh` est le kernel de contrôle de flux de TOUTES les missions d'équipe, y compris celle qui exécutera cette phase. Une régression de `reopen` ou de `ready` casse le pilotage en cours de mission |
| **Réversibilité** | **reversible** — champs additifs dans un schéma JSON, rétro-compatibles (nœud sans champ = comportement inchangé) |

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `--scope=<globs>` | flag CLI (nouveau, `dag.sh add`) | liste séparée par des virgules, espaces rognés, entrées vides ignorées |
| `SCOPE` | variable shell (nouvelle) | valeur brute du flag, passée en argument positionnel au bloc python |
| `scope` | champ de schéma de nœud (nouveau) | tableau de chaînes, `[]` par défaut |
| `review_regime` | champ de schéma de nœud (nouveau, écrit par `reopen`) | valeur `full` uniquement, jamais d'autre valeur écrite par le script |
| `is_review_node()` | fonction python inline (nouvelle) | vrai si l'identifiant commence par `revue-`, `revue:`, `join-`, `join:` ou vaut `join` |
| `frozen` | clé de sortie JSON (nouvelle, `dag.sh status`) | tableau d'objets `{id, status, scope}` pour les nœuds non `done` à `scope` non vide |
| `T13`…`T18` (indicatif) | labels de cas `test-dag.sh` | numérotation continue après les cas existants |

## Détecteurs `plan:pre`

`api-coverage` : non déclenché (sortie `{"detected":false}` du détecteur sur le périmètre de la
phase) — aucune intégration d'API externe. `schema-gate` : le mot « schéma » apparaît dans ce plan
au sens du schéma JSON d'un nœud de DAG, pas d'un schéma de base de données ; aucun fichier ORM,
aucune migration, aucun `db push` — détecteur sans objet, ignoré explicitement.
`assumption-delta` : non déclenché sur ce plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : `--scope` de bout en bout — flag CLI → argument → champ de nœud → rétro-compatibilité (D-13)</name>
  <reversibility rating="reversible">Champ additif ; un DAG écrit avec le champ reste lisible par la version d'avant (le champ est simplement ignoré).</reversibility>
  <read_first>
    - `plugin/conductor/scripts/dag.sh` en entier (210 lignes) — la boucle d'arguments et sa liste de variables initialisées en tête ; la ligne d'invocation `python3 - "$ACTION" "$FILE" ...` et le dépaquetage `sys.argv` correspondant ; le bloc `add` (calcul de `final` avec remap de collision, découpage de `deps_raw` sur la virgule, contrôle des dépendances inconnues, construction du dictionnaire de nœud, `recompute`, `save`, `emit`) ; et l'en-tête documentaire `# ` qui décrit le schéma du nœud et l'usage de chaque sous-commande (elle est rendue telle quelle par `-h`).
    - `plugin/conductor/scripts/tests/test-dag.sh` en entier (164 lignes) — les helpers `assert`, `assert_not`, `assert_exit`, `run_bounded`, la fixture `mktemp -d` et la numérotation des cas.
    - `plugin/dev-orchestrator/references/mission-cross-team.md`, le bloc de commandes `dag.sh add` de l'étage design croisé — la forme d'appel réelle que le manager écrit, dans laquelle `--scope` viendra s'insérer.
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` §Orchestration par étape — la phrase qui impose déjà de déclarer le périmètre de chaque nœud au moment du `dag.sh add` : l'instruction que l'outil ne savait pas exécuter.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-13 ; `20-PATTERNS.md` §`dag.sh` (squelette d'implémentation déjà découpé, à ne pas re-dériver).
    - Un DAG réel déjà écrit — `.planning/missions/dag-phase19.json` — en LECTURE SEULE, pour constater qu'aucun nœud n'y porte de clé `scope` : c'est la fixture de rétro-compatibilité.
  </read_first>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <behavior>
    - `dag.sh add --file=F --id=A --step=x --scope="src/a/**,src/b.ts"` → le nœud A porte un tableau de 2 entrées ; la sortie de la sous-commande reste inchangée dans ses clés existantes.
    - `dag.sh add --file=F --id=B --step=y` (sans le flag) → le nœud B porte un tableau vide.
    - `--scope="  src/a/**  ,  , src/b.ts "` → 2 entrées, espaces rognés, entrée vide ignorée (même règle que le découpage des dépendances).
    - Un fichier DAG fabriqué à la main sans aucune clé `scope` sur ses nœuds : `ready`, `status`, `mark`, `reopen` et `tree` s'exécutent tous sans erreur et rendent les mêmes résultats qu'avant le changement.
    - `dag.sh --help` (ou `-h`) mentionne `--scope` dans la ligne d'usage de `add` et dans la description du schéma de nœud.
  </behavior>
  <action>
Étendre `dag.sh` de façon strictement additive, en répliquant le patron déjà en place pour `--deps`
plutôt qu'en inventant une mécanique nouvelle.

Côté bash : initialiser une variable `SCOPE` vide à côté des autres variables de tête ; ajouter la
branche de parsing `--scope=*` dans la boucle d'arguments existante, entre les branches `--deps=*` et
`--status=*` pour garder l'ordre de lecture ; ajouter la variable comme argument positionnel
SUPPLÉMENTAIRE en fin de la ligne d'invocation de l'interpréteur python, de sorte que les positions
des arguments existants ne bougent pas.

Côté python inline : étendre le dépaquetage de `sys.argv` d'un élément. Dans le bloc `add`, après le
calcul de la liste des dépendances, découper la valeur brute sur la virgule avec exactement la même
expression que celle utilisée pour les dépendances (rognage des espaces, entrées vides ignorées), et
insérer la clé `scope` dans le dictionnaire de nœud, entre `deps` et `status` — l'ordre des clés est
lisible dans le JSON rendu, il compte pour la relecture humaine du plan de bataille.

Rétro-compatibilité, point non négociable : PARTOUT où le champ est lu ailleurs que dans `add`,
utiliser un accès tolérant à l'absence rendant le tableau vide. Les DAG déjà écrits sur disque
(`.planning/missions/dag-phase17.json`, `dag-phase19.json`) n'ont pas cette clé, et une mission en
cours pourrait relire un fichier écrit par la version précédente du script. Aucun accès direct à la
clé, jamais.

Mettre à jour l'en-tête documentaire du script — il est la sortie de `-h`, donc c'est de la
documentation exécutable : la ligne qui décrit le schéma du nœud gagne le champ, la ligne d'usage de
`add` gagne le flag entre crochets (il est optionnel), et une phrase dit à quoi sert le périmètre
(déclaration nécessaire au dispatch parallèle et à la table des fichiers gelés).

Ajouter les cas correspondants à `test-dag.sh`, numérotés à la suite : les 3 cas de comportement du
flag, et surtout le cas de RÉTRO-COMPATIBILITÉ — fabriquer dans la fixture un fichier DAG à la main,
avec des nœuds ne portant PAS la clé, et vérifier que les cinq sous-commandes s'exécutent dessus sans
erreur. Ce cas est le plus important du lot : c'est lui qui protège les missions en vol.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-dag.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test-dag.sh` sort 0 FAIL, avec au moins 4 cas de plus qu'avant la tâche.
    - Rétro-compatibilité prouvée sur un DAG RÉEL, en lecture seule, par commande exécutée : `bash plugin/conductor/scripts/dag.sh status --file=.planning/missions/dag-phase19.json` sort 0 et rend un JSON valide, alors qu'aucun de ses nœuds ne porte la clé `scope`. La commande ne réécrit pas le fichier — vérifié par comparaison d'empreinte (`md5 -q` ou `md5sum`) avant et après.
    - Aucun accès direct à la clé : `grep -nE '\["scope"\]' plugin/conductor/scripts/dag.sh` ne renvoie que la ligne de CONSTRUCTION du nœud dans le bloc `add`, aucune ligne de lecture.
    - `bash plugin/conductor/scripts/dag.sh -h` imprime le flag et le champ, et sort 0.
    - Les clés de sortie existantes sont préservées : sur un DAG de fixture, `dag.sh add` rend toujours `added`, `remapped` et `status` ; `dag.sh ready` rend toujours `ready` et `count`.
    - Portabilité : la suite tourne verte à l'EXÉCUTION sur macOS (bash 3.2, `python3` du système). Preuve Linux DIFFÉRÉE au job CI `tests` (ubuntu-latest) au push, notée comme telle au compte rendu.
  </acceptance_criteria>
  <done>`dag.sh add` accepte et persiste un périmètre, les DAG existants sans ce champ restent pleinement exploitables, et la suite le prouve sur un fichier de mission réel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : `reopen` force le régime PLEIN sur tout nœud de revue ou de jointure rouvert (D-14)</name>
  <reversibility rating="reversible">Champ additif écrit par une seule sous-commande ; son retrait rend le régime de nouveau déclaratif, sans casser aucun DAG.</reversibility>
  <read_first>
    - `plugin/conductor/scripts/dag.sh`, le bloc `reopen` en entier — la fonction interne qui calcule les dépendants transitifs, la mise à l'écart du nœud cible de sa propre liste de dépendants (garde anti-cycle), la remise à `blocked` du nœud et de ses dépendants, l'appel de recalcul de frontière, et la forme exacte de la sortie émise.
    - `plugin/conductor/scripts/dag.sh`, la fonction de recalcul de frontière — pour vérifier qu'elle ne touche jamais aux champs autres que le statut, donc qu'elle ne peut pas effacer le régime écrit.
    - `plugin/dev-orchestrator/references/mission-cross-team.md` — les identifiants de nœuds de revue réellement employés aujourd'hui : la forme `revue-N` de l'étage design croisé ET la forme `revue:<écran>` de l'étage implémentation. Les DEUX doivent être reconnues par le sélecteur.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-14, et la formulation du garde-fou de comblement dans `<specifics>` — c'est ce garde-fou que ce champ rend machine-enforced.
    - `.planning/ROADMAP.md` §Phase 20, le troisième garde-fou non négociable (« aucun allègement ne s'applique jamais à un diff de comblement »).
    - `plugin/conductor/scripts/tests/test-dag.sh`, le cas de réouverture existant (dépendants transitifs remis à `blocked`) — le cas jumeau à décliner.
  </read_first>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <behavior>
    - DAG contenant `exec-1` → `revue-1` → `join-1`, tous `done`. `reopen --id=exec-1` → `revue-1` et `join-1` portent chacun `review_regime` valant `full` ; `exec-1` ne porte pas ce champ.
    - `reopen --id=revue-1` directement → `revue-1` porte `review_regime` valant `full`.
    - Identifiant à deux-points : un nœud `revue:ecran-home` rouvert porte lui aussi le champ.
    - Un nœud `join` (identifiant exactement égal à ce mot) rouvert porte le champ.
    - Un nœud `exec-2` ou `plan-2` rouvert ne porte JAMAIS ce champ, même après plusieurs réouvertures.
    - Un second `reopen` sur un nœud déjà en régime plein laisse la valeur inchangée (idempotence, pas de duplication de clé).
  </behavior>
  <action>
Écrire dans le bloc python de `dag.sh` une fonction de prédicat `is_review_node(node_id)`, placée à
côté des autres helpers de haut de bloc, qui rend vrai si l'identifiant désigne un nœud de revue ou
de jointure. Le sélecteur est explicite et fermé : préfixe `revue-`, préfixe `revue:`, préfixe
`join-`, préfixe `join:`, ou identifiant exactement égal à `join`. Les deux ponctuations sont
nécessaires parce que les deux formes existent déjà dans la doctrine de mission croisée (`revue-N`
pour l'étage design croisé, `revue:<écran>` pour l'étage implémentation) — ne pas en oublier une.
Ne PAS utiliser un test de sous-chaîne : un nœud `refonte-joint-bas` ne doit pas matcher.

Dans le bloc `reopen`, après la remise à `blocked` du nœud cible et de ses dépendants transitifs,
poser le champ `review_regime` à la valeur `full` sur chaque nœud concerné dont le prédicat est vrai
— le nœud cible ET les dépendants, sans exception. Écrire un commentaire d'une ligne au-dessus qui
cite le garde-fou du ROADMAP dont ce champ est la traduction exécutable, et qui dit pourquoi c'est
un champ écrit par le script plutôt qu'une consigne : un régime décidé par prompt est un point de
décision, donc un point d'erreur.

Le script n'écrit JAMAIS d'autre valeur que le régime plein. Il n'y a pas de sous-commande pour
poser un régime allégé : l'allègement, quand il s'applique, est un choix du manager au moment de
composer le mandat, et l'absence du champ vaut « non contraint ». Documenter ce point dans l'en-tête
du script, en une phrase, pour qu'aucun lecteur n'aille chercher un flag inexistant.

Étendre la sortie émise par `reopen` d'une clé listant les identifiants passés en régime plein, afin
que le manager le voie dans le retour de commande sans relire le fichier. Les clés existantes de
cette sortie ne changent ni de nom ni de sens.

Ajouter les cas correspondants à `test-dag.sh`, y compris les cas NÉGATIFS (un nœud d'exécution
rouvert n'acquiert pas le champ ; un identifiant qui contient le mot de jointure sans en être un
préfixe n'acquiert pas le champ) — sans eux, un prédicat trop large passerait inaperçu.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-dag.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test-dag.sh` sort 0 FAIL, avec au moins 6 cas de plus qu'avant la tâche 1 (les 4 de `--scope` et au moins 2 ici, cas négatifs compris).
    - Le champ n'est jamais écrit à une autre valeur : `grep -c 'review_regime' plugin/conductor/scripts/dag.sh` ne renvoie que des lignes où la valeur assignée est le régime plein — vérifié par lecture du diff, et par l'absence de toute branche conditionnelle produisant une seconde valeur.
    - Le prédicat rejette bien un faux positif : un cas de suite pose un nœud dont l'identifiant contient le mot de jointure en milieu de chaîne, le rouvre, et asserte l'ABSENCE du champ.
    - Idempotence : un second `reopen` sur le même nœud rend un JSON dont la sérialisation ne contient qu'une seule occurrence de la clé pour ce nœud (`python3 -c` de comptage sur le fichier de fixture).
    - Discriminance prouvée par MUTATION et consignée : remplacer le prédicat par une constante vraie fait passer à au moins 1 KO le cas négatif ; le remplacer par une constante fausse fait passer à au moins 1 KO le cas positif. Restauration, retour à 0 FAIL.
    - Portabilité : suite verte à l'EXÉCUTION sur macOS. Preuve Linux différée au job CI `tests`, notée comme telle.
  </acceptance_criteria>
  <done>Toute réouverture d'un nœud de revue ou de jointure force le régime plein, écrit par le script lui-même ; les nœuds qui n'en sont pas restent intacts, prouvé par cas négatifs et par mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : `dag.sh status` expose les périmètres GELÉS — la table des fichiers gelés devient interrogeable (D-15 §2)</name>
  <reversibility rating="reversible">Clé additive dans une sortie JSON de lecture seule.</reversibility>
  <read_first>
    - `plugin/conductor/scripts/dag.sh`, le bloc `status` — le comptage par statut et la liste de la frontière `ready`, et la forme exacte de l'objet émis.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-15, section 2 : la table des fichiers gelés n'est JAMAIS recopiée statiquement ; le fichier documente une convention de lecture dynamique, dérivée du périmètre des nœuds non terminés des DAG de mission actifs. C'est cette lecture qui doit exister comme commande.
    - `plugin/conductor/scripts/tests/test-dag.sh`, le cas de compteurs de `status` — le cas jumeau.
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` §Orchestration par étape — le passage sur le dispatch parallèle conditionné à des périmètres disjoints : c'est le consommateur naturel de cette sortie.
  </read_first>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <behavior>
    - DAG avec `exec-1` (statut `done`, périmètre non vide), `exec-2` (statut `running`, périmètre non vide), `exec-3` (statut `blocked`, périmètre vide) : `status` liste UNIQUEMENT `exec-2` dans les périmètres gelés, avec son statut et son périmètre.
    - DAG dont aucun nœud ne porte de périmètre : la clé est présente et vaut le tableau vide (jamais absente — un consommateur ne doit pas avoir à distinguer « absent » de « vide »).
    - DAG écrit par la version précédente du script (aucune clé de périmètre) : la clé vaut le tableau vide, aucune erreur.
    - Les clés existantes de la sortie de `status` conservent leur nom, leur type et leur sens.
  </behavior>
  <action>
Étendre le bloc `status` d'une clé de sortie qui liste les périmètres GELÉS : pour chaque nœud dont
le statut n'est PAS terminé et dont le périmètre est non vide, un objet portant l'identifiant, le
statut et le périmètre. Le tri est déterministe (par identifiant) pour que deux appels consécutifs
produisent une sortie identique et diffable.

La clé est TOUJOURS présente, même vide : un consommateur — le manager, ou un lecteur humain qui
demande « quels fichiers sont gelés maintenant » — ne doit jamais avoir à distinguer l'absence de la
vacuité. La lecture du périmètre passe par l'accès tolérant à l'absence posé en tâche 1.

Définition de « non terminé », à écrire noir sur blanc en commentaire : tout statut différent de
l'état terminal. Un nœud en échec compte comme gelé — son périmètre est justement celui sur lequel
une reprise va revenir, c'est le cas où le gel importe le plus.

Mettre à jour l'en-tête documentaire du script : la ligne d'usage de `status` gagne une mention de
cette sortie, et une phrase énonce l'usage prévu — c'est la source unique et vivante de la table des
fichiers gelés, qui n'est jamais recopiée dans un fichier de documentation.

Ajouter les cas correspondants à `test-dag.sh`, dont le cas « aucun périmètre déclaré » (clé présente
et vide) et le cas « nœud terminé exclu » — c'est ce dernier qui discrimine réellement.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-dag.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test-dag.sh` sort 0 FAIL, avec au moins 3 cas de plus que la fin de la tâche 2.
    - Sur un DAG de fixture à 3 nœuds construit dans la suite, la sortie de `status` contient exactement 1 entrée de périmètre gelé, et son identifiant est celui du nœud non terminé à périmètre non vide.
    - Déterminisme prouvé par commande : deux invocations successives de `status` sur la même fixture produisent une sortie octet pour octet identique (comparaison de deux captures).
    - Rétro-compatibilité prouvée sur fichier réel, en LECTURE SEULE : `bash plugin/conductor/scripts/dag.sh status --file=.planning/missions/dag-phase17.json` sort 0, rend un JSON valide dont la clé de périmètres gelés vaut le tableau vide, et l'empreinte du fichier est inchangée avant/après.
    - Aucune régression de clé : sur la même fixture, la sortie de `status` contient toujours le total, les compteurs par statut et la frontière prête.
    - Portabilité : suite verte à l'EXÉCUTION sur macOS. Preuve Linux différée au job CI `tests`, notée comme telle.
  </acceptance_criteria>
  <done>`dag.sh status` répond à la question « quels fichiers sont gelés maintenant » à partir du DAG vivant, de façon déterministe et rétro-compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mandat de mission → état persistant du DAG | les valeurs de périmètre viennent d'un agent (le manager) et sont écrites telles quelles dans un fichier JSON relu par d'autres étages |
| mission A → mission B (concurrence) | deux missions parallèles lisent et écrivent des DAG distincts, mais partagent l'arbre de travail : le périmètre déclaré est la seule barrière déclarative existante |
| version N-1 du script → fichier DAG écrit par la version N | une mission en vol peut relire un fichier écrit par l'autre version |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02-01 | Elevation of Privilege | régime de revue après comblement | high | mitigate | D-14 : le régime plein est ÉCRIT par `reopen` lui-même sur tout nœud de revue ou de jointure rouvert. Un étage aval ne peut plus s'accorder un allègement sur un diff de comblement par simple interprétation de prompt. Cas négatifs et mutation prouvent le sélecteur. |
| T-20-02-02 | Tampering | relecture d'un DAG écrit par la version précédente | high | mitigate | Accès tolérant à l'absence sur les 2 nouveaux champs, prohibition P-02, et preuve exécutée sur 2 fichiers de mission réels en lecture seule (empreinte inchangée). Une exception de clé manquante casserait une mission en vol. |
| T-20-02-03 | Repudiation | table des fichiers gelés | medium | mitigate | D-15 §2 : la table n'est jamais recopiée, elle est DÉRIVÉE du DAG vivant par `dag.sh status`. Une copie figée qui ment est explicitement jugée pire que rien par le cadrage. |
| T-20-02-04 | Information Disclosure | valeurs de périmètre écrites dans le JSON | low | accept | Les périmètres sont des chemins relatifs du repo, déjà publics dans le dépôt. Aucune donnée sensible ne transite par ce champ. |
| T-20-02-05 | Denial of Service | sélecteur de nœud de revue trop large | medium | mitigate | Sélecteur fermé par préfixes explicites (jamais un test de sous-chaîne) + cas négatif dédié + mutation à constante vraie qui doit tuer ce cas. Un sélecteur trop large forcerait le régime plein partout et transformerait une garantie en coût. |
| T-20-02-SC | Tampering | installations npm/pip/cargo | — | accept | Aucune commande d'installation de paquet dans le périmètre — audit de légitimité sans objet (20-RESEARCH.md §Package Legitimacy Audit). |
</threat_model>

<verification>
- `test-dag.sh` sort 0 FAIL à l'exécution locale macOS, avec au moins 13 cas de plus qu'à l'état initial cumulés sur les 3 tâches.
- La boucle complète des suites du repo (commande exacte de la CI) n'imprime aucun échec.
- Les 2 DAG de mission réels du repo sont lus par les 3 sous-commandes de lecture sans erreur, et leurs empreintes sont inchangées après lecture.
- Les 3 mutations décrites (sélecteur toujours vrai, sélecteur toujours faux, accès direct à la clé) ont été JOUÉES et ont rendu rouge la suite ; l'état d'origine est restauré.
- Vérification différée notée au compte rendu : le job CI `tests` (ubuntu-latest) rejoue la suite sous bash 5.x au push.
</verification>

<success_criteria>
- SC4 tenu sur son volet outillage : le périmètre d'un nœud est déclarable et persistant, le régime plein après comblement est machine-enforced.
- SC5 tenu sur son volet §2 : la table des fichiers gelés a une commande de lecture réelle et déterministe, jamais une copie figée.
- Aucune régression du kernel de mission : les clés de sortie existantes, le rendu arbre et le remap de collision d'identifiant sont intacts.
- Aucun fichier hors de `files_modified` n'est écrit.
</success_criteria>

<output>
Create `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-02-SUMMARY.md` when done.
</output>

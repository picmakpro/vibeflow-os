---
phase: 26-manuel-utilisateur-vibeflow-manual
plan: 07
type: execute
wave: 7
depends_on: ["26-06"]
files_modified:
  - manual/toc.yml
  - manual/fr/README.md
  - manual/en/README.md
  - manual/fr/06-reference/
  - manual/en/06-reference/
autonomous: true
requirements: []   # TBD — ROADMAP §Phase 26 porte `Requirements: TBD`.
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lecteur dispose de la liste EXACTE des commandes, skills et agents réellement exposés, établie depuis le disque — pas des 6 commandes + 1 skill du README, qui en omet dix-sept (D-11)."
    - "Un lecteur sait ce qui tourne en quel modèle et comment il maîtrise sa dépense (M-11)."
    - "Un lecteur dispose d'un dépannage qui va AU-DELÀ de l'installation : agent non déclenché, mission bloquée, halt condition, verrou coincé, claim de branche refusé, PR restée ouverte (M-7)."
    - "Aucun fichier sous manual/ n'apparaît dans git status ni dans aucun commit (D-14)."
  artifacts:
    - "manual/{fr,en}/06-reference/commandes.md"
    - "manual/{fr,en}/06-reference/skills.md"
    - "manual/{fr,en}/06-reference/agents.md"
    - "manual/{fr,en}/06-reference/couts-et-modeles.md"
    - "manual/{fr,en}/06-reference/depannage.md"
    - "manual/{fr,en}/06-reference/ou-trouver-quoi.md"
  key_links:
    - "commandes.md → plugin/commands/*.md frontmatter : six fichiers, aucun ailleurs — la liste vient de là (D-11)."
    - "depannage.md ← 01-demarrer/depannage-installation.md : l'un traite l'après-install (M-7), l'autre l'install ; jamais de recouvrement."
    - "ou-trouver-quoi.md → CHANGELOG.md, module.json, docs/ADR.md : le pont assumé vers le reste du dépôt."
---

<objective>
Livrer le thème `06-reference` complet en français et en anglais : ce qu'on consulte sans jamais le
lire d'un bout à l'autre.

Purpose: c'est le thème où la dette documentaire du dépôt est la plus mesurable. Le README annonce
« 6 commandes + 1 skill » quand le disque porte **6 commandes + 18 skills**, et classe deux skills
parmi les commandes. La règle D-11 s'applique intégralement : **la liste s'établit depuis le disque,
jamais depuis le README.** Ce thème ferme aussi deux manques : **M-7** (dépannage au-delà de
l'install, aujourd'hui à zéro) et **M-11** (coût et modèles, aujourd'hui fragmentaire).

Output: 12 fichiers de pages (6 pages × 2 langues), `toc.yml` enrichi, aucun commit.

**Contrainte machine de la phase (D-14) — vaut pour les 3 tâches de ce plan :** `manual/` est exclu
de git par `.git/info/exclude:7`. `git add` sur ce chemin échoue. **Aucune tâche de ce plan ne
commite. Write/Edit et exécution locale seulement. Ne jamais `git add` un chemin sous `manual/`.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-06-SUMMARY.md
</context>

---

## Invariants de rédaction

Identiques aux 8 invariants énoncés au plan `26-02` (bilingue par incrément · `toc.yml` ne promet
rien · l'EN se produit, il ne se traduit pas (D-12) · zéro version en dur (D-11) · 100-200 lignes,
bascule à 300 ou 3 H2 (D-04) · aucun jargon non défini · blocs générés intouchés (D-03) · **aucun
commit** (D-14)). Ils s'appliquent sans être répétés.

**Invariant supplémentaire propre à ce thème, non négociable (D-11) :** chaque liste est produite en
**énumérant le disque au moment de la rédaction** — `plugin/commands/*.md` pour les commandes,
`plugin/*/skills/*/SKILL.md` pour les skills, `plugin/*/agents/*.md` pour les agents. Le README du
dépôt est explicitement **disqualifié comme source** : il est incomplet et classe deux skills en
commandes (inventaire §9.c). Chaque page dit d'où elle tient sa liste et à quelle date elle a été
établie, pour que le lecteur sache la revérifier.

**Écrire pour la consultation, pas pour la lecture :** titres explicites, une entrée = un bloc court
et autonome, aucune dépendance à ce qui précède dans la page.

---

## Artifacts this plan produces

- `manual/fr/06-reference/` et `manual/en/06-reference/`, 6 pages chacun (H-1) :
  `commandes.md` · `skills.md` · `agents.md` · `couts-et-modeles.md` · `depannage.md` ·
  `ou-trouver-quoi.md`
- `manual/toc.yml` : thème `06-reference` ouvert, 6 entrées `pages:` ajoutées
- `manual/fr/README.md` et `manual/en/README.md` : parcours guidé « je cherche une commande » ajouté

---

<tasks>

<task type="auto">
  <name>Task 1: commandes + skills (FR et EN), énumérés depuis le disque</name>
  <files>manual/fr/06-reference/commandes.md, manual/en/06-reference/commandes.md, manual/fr/06-reference/skills.md, manual/en/06-reference/skills.md, manual/toc.yml</files>
  <read_first>
    - **`plugin/commands/*.md`** — les six fichiers de commandes et leur frontmatter `description`.
      Aucune commande n'existe ailleurs ; c'est la liste complète, par construction.
    - **`plugin/*/skills/*/SKILL.md` et les SKILL.md mono-fichier à la racine des modules** — les
      skills réellement livrés. Exclure explicitement
      `plugin/reference/content/**/templates/skills/`, qui sont des **modèles**, pas des skills
      livrés (inventaire §9.b).
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §9.a,
      §9.b et §9.c — les écarts disque ↔ README à ne surtout pas propager.
  </read_first>
  <action>
    **`06-reference/commandes.md`** (FR + EN) — les commandes réellement exposées, énumérées depuis
    `plugin/commands/`. Pour chacune : ce qu'elle fait (frontmatter `description` comme point de
    départ, reformulé pour un lecteur), quand on l'utilise, et ce qu'elle **n'est pas** (la frontière
    avec sa voisine). Ouvrir par la phrase la plus utile du thème : les commandes ne sont pas la porte
    d'entrée du produit — on parle en langage naturel, elles sont un raccourci. Dire d'où vient la
    liste et à quelle date elle a été établie.

    **`06-reference/skills.md`** (FR + EN) — les skills livrés, énumérés depuis le disque. Les
    regrouper par module d'origine. Pour chacun : ce qu'il déclenche et, surtout, **les formulations
    en langage naturel qui l'activent** — c'est ce dont un utilisateur a réellement besoin, bien plus
    que le nom technique. Corriger en creux les deux erreurs du README sans le nommer comme fautif :
    les skills sont présentés comme des skills, et la liste est complète. Dire d'où vient la liste et
    à quelle date.

    Terminer par : ouvrir le thème `06-reference` dans le bloc `themes:` de `toc.yml`, inscrire les
    2 entrées `pages:`, relancer `build-nav.sh`, relancer `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 06-reference/commandes.md 06-reference/skills.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for c in $(ls plugin/commands/*.md | xargs -n1 basename | sed 's/\.md$//'); do grep -q "$c" manual/fr/06-reference/commandes.md; grep -q "$c" manual/en/06-reference/commandes.md; done; t=$(grep -c '^[[:space:]]*- path: 06-reference/' manual/toc.yml); [ "$t" -ge 2 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - **Chaque fichier de `plugin/commands/` a son entrée** dans `commandes.md`, dans les deux langues
      — vérifié par énumération du disque, pas par lecture du README.
    - `skills.md` liste tous les skills livrés, groupés par module, et **exclut** les modèles de
      `plugin/reference/content/**/templates/skills/`.
    - Aucun skill n'est présenté comme une commande (l'erreur du README n'est pas propagée).
    - Chaque page dit d'où vient sa liste et à quelle date elle a été établie.
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>La référence des points d'entrée est complète et vraie — pour la première fois dans ce dépôt.</done>
</task>

<task type="auto">
  <name>Task 2: agents + couts-et-modeles (FR et EN)</name>
  <files>manual/fr/06-reference/agents.md, manual/en/06-reference/agents.md, manual/fr/06-reference/couts-et-modeles.md, manual/en/06-reference/couts-et-modeles.md, manual/toc.yml</files>
  <read_first>
    - **`plugin/*/agents/*.md`** — le frontmatter de chaque agent : `description`, `model`, `memory`,
      `tools`. Le champ `model` est la matière factuelle de la page coûts.
    - `README.md` §« Efficiency, quantified » (~84-93) — le seul chiffrage existant, à reprendre en
      nommant sa source et sa date, jamais comme un fait intemporel.
    - `plugin/conductor/scripts/check-agents.sh` — les champs obligatoires d'un agent, qui expliquent
      la structure de la référence.
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §9.d et
      §11 (M-11).
    - `manual/fr/05-equipe-agents/les-agents-livres.md` (plan 26-06) — l'exposé narratif ; ici on fait
      la **référence**, structurée pour la consultation, sans redire le raisonnement.
  </read_first>
  <action>
    **`06-reference/agents.md`** (FR + EN) — la référence des agents livrés, énumérée depuis
    `plugin/*/agents/*.md`. Une entrée par agent : nom, module d'origine, rôle en une phrase, s'il est
    invocable directement ou dispatché par un orchestrateur, et le modèle qu'il utilise. Structurer
    pour la consultation (recherche par nom), là où `05-equipe-agents/les-agents-livres.md` structure
    pour la compréhension — poser un lien relatif entre les deux et ne pas dupliquer l'explication.

    **`06-reference/couts-et-modeles.md`** (FR + EN) — **M-11**, aujourd'hui fragmentaire. Couvre :
    quel type de travail tourne sur quel modèle et **pourquoi** ce choix (un juge et un exécutant
    n'ont pas les mêmes besoins) ; d'où le lecteur peut lire ce fait lui-même (le champ `model` du
    frontmatter des agents) ; les leviers réels par lesquels il maîtrise sa dépense — périmètre d'une
    demande, longueur d'une mission, choix du mode autonome, modules installés ; ce qui coûte cher
    sans qu'on s'y attende. Si le chiffrage d'efficience du README est repris, il l'est **avec sa
    source et sa date**, et présenté comme une mesure datée, pas comme une garantie. Ne rien inventer
    en matière de prix : le manuel n'annonce aucun tarif.

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 06-reference/agents.md 06-reference/couts-et-modeles.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do grep -q '05-equipe-agents/les-agents-livres.md' "manual/$l/06-reference/agents.md"; done; t=$(grep -c '^[[:space:]]*- path: 06-reference/' manual/toc.yml); [ "$t" -ge 4 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `agents.md` porte une entrée par agent trouvé sous `plugin/*/agents/`, avec module, rôle,
      mode d'invocation et modèle — liste établie par énumération du disque (D-11).
    - `agents.md` renvoie à `05-equipe-agents/les-agents-livres.md` par un lien qui résout, et ne
      duplique pas son explication.
    - `couts-et-modeles.md` nomme au moins trois leviers concrets de maîtrise de la dépense (M-11) et
      dit où le lecteur peut vérifier lui-même quel modèle est utilisé.
    - Aucun tarif inventé ; tout chiffre repris porte sa source et sa date.
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur peut chercher un agent par son nom et comprendre ce que son usage lui coûte.</done>
</task>

<task type="auto">
  <name>Task 3: depannage (M-7) + ou-trouver-quoi (FR et EN) + parcours « je cherche »</name>
  <files>manual/fr/06-reference/depannage.md, manual/en/06-reference/depannage.md, manual/fr/06-reference/ou-trouver-quoi.md, manual/en/06-reference/ou-trouver-quoi.md, manual/fr/README.md, manual/en/README.md, manual/toc.yml</files>
  <read_first>
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §11 —
      **M-7** énumère les six pannes non documentées : l'agent ne s'est pas déclenché · une mission
      est bloquée · une halt condition a gelé un nœud · le verrou de driver est coincé · le claim de
      branche refuse (ADR-064) · une PR de mission est restée ouverte (ADR-059).
    - `manual/fr/01-demarrer/depannage-installation.md` (plan 26-02) — pour délimiter : là-bas
      l'install, ici l'après.
    - `plugin/conductor/scripts/` et `plugin/dev-orchestrator/scripts/` — les gates et sondes dont la
      sortie est ce que le lecteur voit quand ça bloque (`check-*.sh`).
    - `docs/ADR.md` (index), `CHANGELOG.md`, `plugin/reference/content/` — les ressources vers
      lesquelles `ou-trouver-quoi.md` fait le pont.
  </read_first>
  <action>
    **`06-reference/depannage.md`** (FR + EN) — **M-7**, à écrire intégralement : aucune ligne
    n'existe aujourd'hui pour ce qui casse **après** l'installation. Traiter les six pannes nommées en
    M-7, chacune sur le même patron court : le symptôme exact tel que le lecteur le voit, la cause la
    plus fréquente, le geste, et comment vérifier que c'est réglé. Ajouter les pannes que la lecture
    des scripts fait apparaître si elles sont réelles, en les décrivant du côté observable. Ne jamais
    proposer un geste destructeur sans dire ce qu'il détruit. Ouvrir par un renvoi à
    `01-demarrer/depannage-installation.md` pour tout ce qui relève de l'installation — les deux pages
    ne se recouvrent pas.

    **`06-reference/ou-trouver-quoi.md`** (FR + EN) — la carte des ressources, et le pont assumé vers
    le reste du dépôt. Dire, en une ligne chacun : où lire la version réelle d'un module et son
    historique ; où sont les décisions d'architecture ; où vit la doctrine méthodologique complète ;
    ce que contiennent `docs/` et `.planning/` et **pourquoi un utilisateur n'a normalement pas à les
    ouvrir** — c'est la raison d'être de ce manuel, autant l'écrire noir sur blanc à l'endroit où le
    lecteur pourrait être tenté d'y aller. Terminer par comment signaler un problème ou proposer une
    amélioration.

    **Ajouter aux deux README de langue** le parcours guidé « je cherche une commande ou une panne »,
    pointant vers `06-reference/commandes.md`.

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 06-reference/depannage.md 06-reference/ou-trouver-quoi.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do grep -q '01-demarrer/depannage-installation.md' "manual/$l/06-reference/depannage.md"; grep -q '06-reference/' "manual/$l/README.md"; done; t=$(grep -c '^[[:space:]]*- path: 06-reference/' manual/toc.yml); [ "$t" -ge 6 ]; [ "$t" -le 9 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `depannage.md` traite **les six pannes** énumérées en M-7, chacune avec symptôme, cause, geste
      et vérification.
    - `depannage.md` ne recouvre pas `01-demarrer/depannage-installation.md` : il y renvoie par un
      lien relatif qui résout (C3 du gate).
    - Tout geste destructeur proposé est accompagné de ce qu'il détruit.
    - `ou-trouver-quoi.md` dit explicitement pourquoi un utilisateur n'a normalement pas à ouvrir
      `docs/` ni `.planning/`.
    - Les deux README de langue portent le parcours « je cherche », lien résolvant.
    - Le thème `06-reference` compte entre 6 et 9 pages par langue (D-04).
    - Aucun motif de version : C5 du gate passe (D-11).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur qui bloque après l'installation a enfin une page à ouvrir — et il sait où aller quand le manuel s'arrête.</done>
</task>

</tasks>

<threat_model>
Sans objet pour ce plan : markdown seul, aucun code exécutable, aucune dépendance, aucun install de
paquet. La seule menace de la phase (fuite de `manual/` vers un artefact versionné) est instruite en
`T-26-04` du plan 26-01 et mitigée ici par l'assertion `git status --porcelain -- manual` vide dans
les trois tâches (D-14).
</threat_model>

<verification>
1. `bash manual/.tools/check-manual.sh` sort en **0**.
2. Les arbres `manual/fr` et `manual/en` sont isomorphes (D-01).
3. Le nombre d'entrées de `commandes.md` égale le nombre de fichiers de `plugin/commands/`.
4. Aucun skill n'est présenté comme une commande ; aucun modèle de
   `plugin/reference/content/**/templates/skills/` ne figure dans `skills.md`.
5. Aucun motif de version : C5 du gate passe (D-11).
6. `git status --porcelain -- manual` sort **vide**.
7. `git status --porcelain -- plugin docs README.md README.fr.md INSTALL.md scripts .github` sort
   **vide**.
</verification>

<success_criteria>
- Les 6 (à 9) pages de `06-reference` existent en FR **et** en EN.
- Les trois listes (commandes, skills, agents) sont établies depuis le disque et datées.
- M-7 et M-11 sont comblés.
- Le gate est vert. Git n'a rien vu.
</success_criteria>

<output>
Create `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-07-SUMMARY.md` when done.
Y consigner : les trois décomptes établis depuis le disque (commandes, skills, agents), tout écart
constaté avec le README du dépôt (à titre de constat, sans corriger le README — hors périmètre), le
verdict de `check-manual.sh`, et la confirmation explicite que `git status --porcelain -- manual` est
resté vide de bout en bout.
</output>

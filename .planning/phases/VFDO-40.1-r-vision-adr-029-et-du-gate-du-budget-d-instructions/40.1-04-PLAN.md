---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 04
lot: L1b — module conductor : énoncés vivants et bump minor dérivé
type: execute
wave: 3
depends_on: ["40.1-02", "40.1-03", "40.1-05", "40.1-12"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/conductor/README.md
  - plugin/conductor/AGENT.md
  - plugin/conductor/references/contracts.md
  - plugin/conductor/skills/vf-new-lab/SKILL.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01 — le module conductor n'énonce plus l'ancien plafond : README (codes de sortie du gate, verdict `AVERTISSEMENT-ADR029`), AGENT.md, contracts.md, skill vf-new-lab ; aucune ligne vivante sous `plugin/conductor/` au recensement"
    - "Aucun agent ni skill réécrit : remplacement de valeur seul dans `AGENT.md`, `vf-new-lab/SKILL.md` et `contracts.md` ; `plugin/conductor/AGENT.md` garde lignes et instructions égales à sa baseline"
    - "Toutes les suites `plugin/conductor/scripts/tests/test-*.sh` sont vertes, jouées APRÈS 40.1-05 (auditeur) et 40.1-12 (audit-architecture) : `test-check-description-fidelity.sh` copie des SKILL.md de ces deux modules (couplage déclaré, d'où la sérialisation en vague 3)"
    - "Bump **minor** dérivé de la VERSION courante (jamais un numéro figé), vérifié par `tools/check-module-bump.sh conductor minor`. Règle de numérotation appliquée à toute la phase (constat du checker, révision 1) : un changement du CONTRAT observable d'un outil (sémantique du code de sortie, vocabulaire des verdicts, format du rapport) est une capacité, donc minor ; un changement de valeurs de seuil avec un avertissement de plus, sans changement de contrat de sortie, est un patch. Le gate du dépôt change sa sémantique de rc (les lignes ne bloquent plus), ses verdicts et son BILAN → minor. `validate_gate.sh` de l'auditeur garde ses codes et son canal WARNING → patch pour reference (40.1-06)"
  artifacts:
    - path: "plugin/conductor/CHANGELOG.md"
      provides: "entrée de bump minor citant les arbitrages avec canal et date"
  key_links:
    - "Le texte du README reprend les noms livrés par 40.1-03 (`AVERTISSEMENT-ADR029`, `VF_BUDGET_LINE_WARN_FROM`) : lire le gate livré, pas le plan"
---

<objective>
Aligner les énoncés vivants du module conductor sur le plafond 300 et bumper le module en minor.
Arbitrages en jeu : D-01, D-02, D-05, D-06, D-07 (Samuel, AskUserQuestion session principale, relais
SendMessage, 2026-09-16). Ce contenu a été sorti de 40.1-03 à la révision 1 (constat W1 : budget).
Le plan est sérialisé en vague 3 parce que les suites conductor lisent des fichiers modifiés par
40.1-05 et 40.1-12.

Purpose: BUDG-04 sur le module qui porte le gate.
Output: quatre documents alignés, module conductor bumpé en minor.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : énoncés vivants du module conductor alignés sur 300</name>
  <files>plugin/conductor/README.md, plugin/conductor/AGENT.md, plugin/conductor/references/contracts.md, plugin/conductor/skills/vf-new-lab/SKILL.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/conductor/` (liste re-dérivée à l'exécution, jamais recopiée)
    - plugin/conductor/scripts/check-instruction-budget.sh (tel que livré par 40.1-03 : noms de verdicts et constantes)
    - plugin/conductor/README.md (paragraphe `check-instruction-budget.sh` du catalogue)
    - plugin/conductor/AGENT.md, plugin/conductor/references/contracts.md, plugin/conductor/skills/vf-new-lab/SKILL.md (lignes listées)
  </read_first>
  <action>
Pour chaque ligne listée sous `plugin/conductor/` par le recensement, dans `AGENT.md`,
`vf-new-lab/SKILL.md` et `contracts.md` : REMPLACEMENT DE VALEUR SEUL (la valeur du plafond devient
300). Rien d'autre sur la ligne, aucune ligne ajoutée ni retirée, aucun marqueur normatif ajouté.
Dans `README.md`, réécrire la phrase des codes de sortie du catalogue : `1` = armé avec une
instruction au-dessus de sa baseline (par fichier, D-07) ou un fichier au-delà du plafond de 300
lignes. Ajouter une phrase : le verdict `AVERTISSEMENT-ADR029` signale un agent dès 251 lignes sans
changer le code, et la colonne lignes de la baseline est publiée, jamais comparée (Phase 40.1,
arbitrages Samuel, AskUserQuestion session principale, 2026-09-16). N'écrire aucune valeur de
l'ancien plafond.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/conductor\\// {c++} END{print c+0}" "$T/o"); echo "lignes vivantes restantes sous plugin/conductor : $n"; [ "$n" -eq 0 ]; g=0; bash plugin/conductor/scripts/check-instruction-budget.sh > "$T/g" 2>&1 || g=$?; echo "gate rc=$g"; [ "$g" -eq 0 ]; awk -F" [|] " "\$1==\"plugin/conductor/AGENT.md\"{print \"AGENT conductor : lignes=\" \$2 \" bl=\" \$3 \" instr=\" \$4 \" bl=\" \$5 \" verdict=\" \$6; ok=(\$2==\$3 && \$4==\$5 && \$6==\"OK\")} END{exit ok?0:1}" "$T/g"'</automated>
    <fails_when>exit non nul ; ou `lignes vivantes restantes sous plugin/conductor :` différent de 0 ; ou `gate rc=` différent de 0 ; ou la ligne `AGENT conductor :` montre des lignes ou instructions différentes de la baseline, ou un verdict autre que `OK`</fails_when>
    <automated>bash --noprofile --norc -e -c 'k=0; n=0; for t in plugin/conductor/scripts/tests/test-*.sh; do n=$((n+1)); if bash "$t" > /dev/null 2>&1; then :; else echo "ROUGE $t"; k=$((k+1)); fi; done; echo "suites conductor : $n jouees, $k en echec"; [ "$n" -gt 0 ] && [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul, ou une ligne `ROUGE <suite>`, ou `0 jouees`, ou un nombre d'échecs différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - aucune ligne vivante sous `plugin/conductor/` au recensement ; gate rc 0
    - `git diff --numstat "$B" -- plugin/conductor/AGENT.md plugin/conductor/skills/vf-new-lab/SKILL.md plugin/conductor/references/contracts.md` : pour chaque fichier, lignes ajoutées = lignes retirées
    - `plugin/conductor/AGENT.md` : lignes et instructions égales à sa baseline, verdict `OK`
    - `plugin/conductor/README.md` contient `AVERTISSEMENT-ADR029` et `300 lignes`
    - toutes les suites `plugin/conductor/scripts/tests/test-*.sh` vertes
  </acceptance_criteria>
  <done>Le module conductor n'énonce plus l'ancien plafond, sans qu'aucun agent ni skill n'ait été réécrit.</done>
</task>

<task type="auto">
  <name>Task 2 : bump minor dérivé du module conductor et CHANGELOG</name>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/conductor/VERSION (valeur COURANTE à l'exécution : la cible en dérive)
    - plugin/conductor/CHANGELOG.md (forme des entrées), plugin/conductor/README.md (ligne `**Version**`)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh (contrat vérifié)
  </read_first>
  <action>
Lire `plugin/conductor/VERSION` à l'exécution et calculer la cible **minor** (Y+1, Z=0). La même
cible va dans `VERSION`, dans `.version` de `module.json` et dans la ligne `**Version**` du README,
le tout en UN commit au sujet `chore(40.1-04): …`. Entrée CHANGELOG en tête,
`## [<cible>] — <date d'exécution> (Phase 40.1 — révision ADR-029 et du gate du budget)` :
- **Minor**, avec la règle de numérotation des must_haves (contrat de sortie du gate modifié) ;
- plafond ADR-029 passé à 300 lignes, avertissement non bloquant dès 251 ;
- ratchet restreint aux instructions, par fichier ; colonne lignes publiée, jamais comparée ;
- étape CI à sept bascules isolées (ferme O-1), commentaires sur l'état réel (O-2) ;
- MUT-2 remplacé, MUT-5/6/7 ajoutés ; aucune valeur de baseline modifiée ;
- autorisation : arbitrages Samuel D-01, D-02, D-05, D-06, D-07, AskUserQuestion session principale
  (relais SendMessage), 2026-09-16.
VERSION racine, `plugin.json` et `marketplace.json` restent intouchés (la release est un geste
humain gaté).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh conductor minor</automated>
    <fails_when>exit non nul, ou absence de la ligne `cible derivee conductor :`, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
  </verify>
  <acceptance_criteria>
    - `check-module-bump.sh conductor minor` rc 0 (cible dérivée, triade, en-tête, CHANGELOG « Phase 40.1 »)
    - `check-version-sync.sh` rc 0 ; aucun commit de ce plan ne touche une source de version racine
  </acceptance_criteria>
  <done>Module conductor bumpé en minor, sur une cible dérivée à l'exécution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texte de module → lecteurs du catalogue | le README décrit le contrat du gate aux mainteneurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-13 | Tampering | réécriture d'agent sous couvert d'alignement | medium | mitigate | lignes et instructions égales à la baseline ; numstat équilibré ; invariant I2 à la clôture |
| T-40.1-14 | Repudiation | numéro de version consommé par un hotfix parallèle | medium | mitigate | cible dérivée à l'exécution, `check-module-bump.sh` |
</threat_model>

<verification>
Recensement nul sous `plugin/conductor/` ; gate rc 0 ; suites conductor vertes ; bump dérivé vérifié.
</verification>

<success_criteria>
Le module conductor est aligné sur 300 et versionné en minor sur une cible dérivée.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : texte et bump du module conductor.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-04-SUMMARY.md` when done
</output>

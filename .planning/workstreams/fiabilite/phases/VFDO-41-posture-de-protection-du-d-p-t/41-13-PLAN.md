---
phase: 41-posture-de-protection-du-d-p-t
plan: 13
lot: L13 — ledger coché sur pièce (PROT-01) et compléments du volet admin, ROADMAP et STATE, éligibilité de clôture du jalon, PR de clôture sans contournement
type: execute
wave: 13
depends_on: ["41-12"]
autonomous: false
requirements: [PROT-01, PROT-02, PROT-03, PROT-04]
files_modified:
  - .planning/workstreams/fiabilite/REQUIREMENTS.md
  - .planning/workstreams/fiabilite/ROADMAP.md
  - .planning/workstreams/fiabilite/STATE.md
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "PROT-01 (« prouvés par un refus réel ») est coché et `Complete` en traçabilité SEULEMENT si le dernier `D03-VERDICT:` vaut `CONFORME` — le refus réel du push direct sur `main` par la clé de déploiement, acteur hors liste (41-09) ; il est alors suivi d'une ligne de clôture `  - **Exigence close le <date>** (Phase 41, plan 41-13) — …` qui précise que le refus réel vient de D-03 (acteur hors liste) et non du critère 2 du ROADMAP (non tenu tel que formulé, D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24), porte sur l'EXIGENCE et ne cite jamais O-3. Dans TOUT autre cas (`ECART` ou verdict absent), la case reste `- [ ]`, suivie d'une ligne `  - **Non cochée au <date>** (Phase 41, plan 41-13) — …` renvoyant à la décision de 41-09 (`D03-M4-DECISION:` recopiée), et la traçabilité reste `Pending` avec ce renvoi. PROT-02, PROT-03, PROT-04 (déjà cochés le 2026-09-18) reçoivent chacun une ligne `  - **Complément volet admin (<date>)** (Phase 41, plan 41-13) — preuves : …`, sans O-3 ; aucune ligne existante du ledger n'est réécrite"
    - "Chaque ligne cite ses clés de preuve de `41-PREUVES.md` et les décisions humaines consignées (option, décideur, canal, date) ; tout `non_prouvee` de `TRACE-II:` est recopié, jamais effacé ; en `preuves-sans-release`, le complément de PROT-02 écrit que le rejeu du flux de release n'a pas eu lieu dans cette phase"
    - "QUAL-01 : la ligne de traçabilité dit que le volet admin de la Phase 41 n'en crée aucun gate (D-M11, `INVARIANTS-PHASE`) — les trois gates de la phase sont ceux du 2026-09-18, déjà satisfaits"
    - "Critères du ROADMAP recalculés sur pièce (`CRITERES-ROADMAP:` : c1 rulesets actifs, c2 refus par défaut + trace, c3 rejeu de release, c4 QUAL-01) ; sous D-02ter (arbitrage Willy, AskUserQuestion session principale, 2026-09-24), c2 ne prend que deux valeurs : `non_tenu_tel_que_formule` si les mesures prouvables sont tenues (M-1 et M-3, ou leurs décisions de poursuite), `non` sinon — la phase le dit sans réécrire le critère ; c2 n'étant jamais atteint, la case Phase 41 du ROADMAP reste `- [ ]` ; la section Phase 41 reçoit une note « VOLET ADMIN EXÉCUTÉ » et ses treize plans 41-01..41-13 sont cochés ; `STATE.md` § Current Position est mis à jour"
    - "Jalon fiabilite-v1.0 : `CLOTURE-JALON:` consigne `eligible=non criteres_41=incomplets` (c2 non atteignable sous D-02ter) et les phases encore non cochées au ROADMAP ; la clôture elle-même (`/gsd-complete-milestone`) n'est PAS jouée par ce plan : sa levée revient à un arbitrage humain hors de ce plan, et l'orchestrateur ne la lance que sur une consignation `eligible=oui`, par une branche et une PR"
    - "Balayage final : aucune proposition des artefacts du volet admin n'associe O-3 à un mot d'achèvement ; `tools/check-aucune-fermeture.sh` rend 0 sur tout le périmètre de la phase ; le SUMMARY de 41-08 porte la nuance des checks en cours"
    - "La PR de clôture est la troisième PR de la phase (D-03) ; son SUMMARY est committé AVANT sa publication ; son merge, par Willy sans contournement, est relu par l'orchestrateur. Tout artefact écrit APRÈS ce merge (VERIFICATION, clôture de jalon, rapport de mission) part d'une branche `docs/phase-41-verification` créée depuis `origin/main` et entre par une PR mergée sans contournement, OU n'est pas versionné"
    - "Rejeu local `tests` (T-41-94, mécanisme de 41-07) : seuls sont tolérés les échecs dont le chemin est exactement celui d'une entrée `open` du ledger `.planning/WINDOWS.md` (rouges d'environnement de ce poste, #6 et #7 au 2026-09-24), lus dans le ledger et jamais écrits en dur dans le plan ; tout autre échec rougit, comme toute étape du job autre que celle des suites en échec ; la liste tolérée est consignée par id de ledger (`REJEU-TESTS-41-13:`) et recomparée au rejeu"
  artifacts:
    - path: ".planning/workstreams/fiabilite/REQUIREMENTS.md"
      provides: "PROT-01 coché si et seulement si `D03-VERDICT: CONFORME`, sinon laissé décoché avec renvoi à `D03-M4-DECISION:` (41-09) ; compléments PROT-02..04 ; QUAL-01 : volet admin sans gate"
    - path: ".planning/workstreams/fiabilite/ROADMAP.md"
      provides: "Phase 41 : note du volet admin, plans cochés, case laissée décochée (c2 non tenu tel que formulé, D-02ter)"
    - path: ".planning/workstreams/fiabilite/STATE.md"
      provides: "position courante à jour"
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`CRITERES-ROADMAP:`, `CLOTURE-JALON:`, `REJEU-TESTS-41-13:`"
  key_links:
    - "Ce plan écrit sur la branche C, à la suite de 41-12 ; `CLOTURE-JALON: eligible=` gouverne le geste de l'orchestrateur après merge"
---

<objective>
Cocher le ledger sur pièce, mettre à jour ROADMAP et STATE selon les critères réellement atteints,
consigner l'éligibilité de clôture du jalon, puis faire entrer la clôture sur `main` par une PR
ordinaire — et fixer le chemin de tout ce qui s'écrira après.

Révisé le 2026-09-23 (reprise) : PROT-02..04 sont déjà cochés (2026-09-18) — ils reçoivent un
complément, pas un second cochage ; seul PROT-01 est coché ici ; la mise à jour du ROADMAP et du
STATE, laissée au manager par 41-19, est faite ici sur pièce ; la clôture du jalon fiabilite-v1.0
n'est engagée que si les critères de la phase sont atteints ; gestes humains de Willy (`picmakpro`).

Révisé le 2026-09-24 (W3 du vérificateur) : PROT-01 exige « un refus réel » ; le seul refus réel que
la phase peut établir est celui de D-03 — push direct sur `main` refusé à la clé de déploiement, hors
liste (41-09). PROT-01 n'est donc coché que si `D03-VERDICT: CONFORME` ; sinon la case reste décochée
avec un renvoi à la décision de 41-09. Les branches mortes de c2 (`atteint`, `atteint_sur_decision`)
sont retirées : sous D-02ter, c2 ne peut pas être atteint.

Purpose: PROT-01 statué sur pièce (coché si et seulement si le refus réel de D-03 est établi) ; compléments PROT-02..04 ; QUAL-01 dit ; D-03 tenu jusqu'au dernier artefact.
Output: ledger, ROADMAP et STATE à jour ; `CLOTURE-JALON:` ; PR de clôture mergée sans contournement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-12-SUMMARY.md

Extraits de `.planning/workstreams/fiabilite/REQUIREMENTS.md` : section `### Posture de protection du dépôt (Phase 41 —
périmètre sans admin)` ; table `## Traceability`, lignes `PROT-0N` et `QUAL-01`. Extraits de
`.planning/workstreams/fiabilite/ROADMAP.md` : ligne `- [ ] Phase 41:` de la liste des phases ; section `### Phase 41:`
(notes, liste `Plans:`). Extrait de `.planning/workstreams/fiabilite/STATE.md` : § `## Current Position` (premier paragraphe).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : ledger — PROT-01 statué sur le refus réel de D-03 (coché si et seulement si `D03-VERDICT: CONFORME`), compléments du volet admin sous PROT-02..04, QUAL-01</name>
  <files>.planning/workstreams/fiabilite/REQUIREMENTS.md</files>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes ; en premier `PUSH-DIRECT:`, le DERNIER `D03-VERDICT:` et `D03-M4-DECISION:` de 41-09)
    - .planning/workstreams/fiabilite/REQUIREMENTS.md (section Phase 41, lignes de traçabilité PROT et QUAL-01)
    - les SUMMARY 41-01 à 41-12
  </read_first>
  <action>
Identité `picmakpro` ; branche C ; `git fetch origin` ; si `origin/main` a bougé, `git merge --no-edit
origin/main` en local (conflit sur REQUIREMENTS, ROADMAP ou STATE — la PR #88 les touche — : arrêt et
remontée, jamais de résolution à l'aveugle).

`REQUIREMENTS.md` :
- PROT-01 (W3, révision du 2026-09-24) : l'exigence dit « prouvés par un refus réel » ; le seul refus
  réel établi par la phase est celui de D-03 — push direct sur `main` par la clé de déploiement, acteur
  hors liste (41-09). Le critère 2 du ROADMAP n'est pas tenu tel que formulé (D-02ter — arbitrage
  Willy, AskUserQuestion session principale, 2026-09-24) et ne vaut jamais preuve de PROT-01. La case
  dépend du SEUL dernier `D03-VERDICT:` de `41-PREUVES.md` :
  - `D03-VERDICT: CONFORME` : passer `- [ ]` à `- [x]` sans toucher au texte ni à ses lignes de
    suite ; juste après sa dernière ligne de suite, ajouter
    `  - **Exigence close le <date d'exécution>** (Phase 41, plan 41-13) — le refus réel vient de D-03 : push direct sur `main` d'un acteur hors liste (clé de déploiement temporaire, 41-09) refusé côté serveur (`PUSH-DIRECT`, `D03-VERDICT: CONFORME`), et non du critère 2 du ROADMAP, non tenu tel que formulé (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ; preuves : <clés>` ;
    traçabilité : la ligne PROT-01 devient `| PROT-01 | Phase 41 | Complete — <date>, plan 41-13 : refus réel de D-03 (acteur hors liste, 41-09) … |` ;
  - tout autre cas (`D03-VERDICT: ECART`, ou verdict absent) : la case RESTE `- [ ]` ; juste après
    sa dernière ligne de suite, ajouter
    `  - **Non cochée au <date d'exécution>** (Phase 41, plan 41-13) — refus réel non établi (`D03-VERDICT: <valeur, ou absente>`) ; renvoi à la décision de 41-09 : `D03-M4-DECISION:` recopiée (option, décideur, canal, date — ou « absente ») ; preuves : <clés>` ;
    traçabilité : la ligne PROT-01 devient `| PROT-01 | Phase 41 | Pending — <date>, plan 41-13 : refus réel non établi (D03-VERDICT), voir D03-M4-DECISION (41-09) |` ;
  - dans les deux cas, ligne poursuivie si besoin sur des lignes indentées de quatre espaces, sans
    citer O-3 ; clés : `POSE-CONFORME`, `POSE-BYPASS-RELU`, `M1-VERDICT`, `PROVENANCE-SERVEUR`,
    `TRACE-II` (avec ses statuts par règle), `PUSH-DIRECT`, `D03-VERDICT`, `M4-VERDICT`,
    `PR-EN-VOL-APRES` ;
- PROT-02, PROT-03, PROT-04 : juste après la dernière ligne de suite de chacun, ajouter
  `  - **Complément volet admin (<date d'exécution>)** (Phase 41, plan 41-13) — preuves : <clés>`, sans
  citer O-3 — PROT-02 → `RELEASE-DECISION`, `PR-RELEASE`, `RELEASE`, `CI-MAIN-RELEASE`,
  `INVARIANTS-PHASE` (en `preuves-sans-release` : « rejeu du flux de release non joué dans cette
  phase ») ; PROT-03 → ADR-072 (amendement du 2026-09-23 et § Mesures), amendement d'ADR-059,
  `CLAUDE.md`, BACKLOG traité ; PROT-04 → `CO-I`, `CO-BASELINE`, `CO-SENTINELLE`, `CO-TEMOIN`,
  `TRACE-II pull_request=…`, ligne de statut du volet admin dans `25-SECURITY.md` ;
- chaque décision humaine consignée (`*-DECISION:`, option, décideur, canal, date) est citée sous
  l'exigence qu'elle touche ; tout `non_prouvee` de `TRACE-II:` est recopié sous PROT-01 ou PROT-04 ;
- traçabilité : seule la ligne PROT-01 change, selon le cas ci-dessus ; les lignes PROT-02..05 ne
  changent pas ;
- ligne QUAL-01 : avant « ; reste à tenir », ajouter « ; le volet admin de la Phase 41 (<date>) n'en
  crée aucun — CODEOWNERS et rulesets sont de la configuration (D-M11), `INVARIANTS-PHASE` à 0 ajout ».
Aucune autre ligne modifiée. Commit local sur C, message « docs(41-13): PROT-01 statué sur le refus
réel de D-03, compléments du volet admin (D-03, D-05, D-07, D-09 — arbitrage Samuel, AskUserQuestion
session principale, 2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session principale,
2026-09-23 ; D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; R=.planning/workstreams/fiabilite/REQUIREMENTS.md; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git show "$B:$R" > "$T/o"; ext() { awk -v id="$1" -v pat="$2" "/^- \\[[ x]\\] \\*\\*PROT-0[0-9]\\*\\*/ { if (st == 1) print blk; st = 0; w = index(\$0, \"**\" id \"**\") ? 1 : 0; next } st == 1 && /^    / { blk = blk \" \" \$0; next } st == 1 { print blk; st = 0; w = 0 } w && /^  [^ -]/ { next } w && index(\$0, pat) == 1 { st = 1; blk = \$0; next } { w = 0 } END { if (st == 1) print blk }" "$3"; }; blk() { awk -v id="$1" "/^- \\[[ x]\\] \\*\\*PROT-0[0-9]\\*\\*/ { w = index(\$0, \"**\" id \"**\") ? 1 : 0; if (w) print; next } w && /^  [^ -]/ { print; next } { w = 0 }" "$2"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; d3=$(last D03-VERDICT:); if [ "$d3" = CONFORME ]; then mode=coche; ex=5; eu=0; etp=0; lp="  - **Exigence close le "; else mode=non_coche; ex=4; eu=1; etp=1; lp="  - **Non cochée au "; fi; x=$(awk "/^- \\[x\\] \\*\\*PROT-0[1-5]\\*\\*/{c++} END{print c+0}" "$R"); u=$(awk "/^- \\[ \\] \\*\\*PROT-0[1-5]\\*\\*/{c++} END{print c+0}" "$R"); u1=$(awk "/^- \\[ \\] \\*\\*PROT-01\\*\\*/{c++} END{print c+0}" "$R"); ext PROT-01 "$lp" "$R" > "$T/c1"; awk "{ gsub(/[ \t]+/, \" \"); print }" "$T/c1" > "$T/c1n"; ext PROT-01 "  - **Exigence close le " "$R" > "$T/cc"; ext PROT-01 "  - **Non cochée au " "$R" > "$T/cn"; ncc=$(awk "END{print NR}" "$T/cc"); ncn=$(awk "END{print NR}" "$T/cn"); : > "$T/cp"; for id in PROT-02 PROT-03 PROT-04; do ext "$id" "  - **Complément volet admin (" "$R" >> "$T/cp"; done; if [ "$mode" = coche ]; then n1=$(awk "/\\*\\*Exigence close le [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\*\\* \\(Phase 41, plan 41-13\\)/ {c++} END{print c+0}" "$T/c1"); fr=$(awk "index(\$0, \"le refus réel vient de D-03\") && index(\$0, \"acteur hors liste\") && index(\$0, \"et non du critère 2\") && index(\$0, \"D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") && index(\$0, \"D03-VERDICT: CONFORME\") {c++} END{print c+0}" "$T/c1n"); tr1=$(awk "/^\\| PROT-01 \\| Phase 41 \\| Complete — [0-9-]+, plan 41-13/ && index(\$0, \"D-03\") {c++} END{print c+0}" "$R"); autre=$ncn; else n1=$(awk "/\\*\\*Non cochée au [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\*\\* \\(Phase 41, plan 41-13\\)/ {c++} END{print c+0}" "$T/c1"); fr=$(awk "index(\$0, \"refus réel non établi\") && index(\$0, \"D03-VERDICT\") && index(\$0, \"D03-M4-DECISION\") && index(\$0, \"41-09\") {c++} END{print c+0}" "$T/c1n"); tr1=$(awk "/^\\| PROT-01 \\| Phase 41 \\| Pending — [0-9-]+, plan 41-13/ && index(\$0, \"D03-M4-DECISION\") {c++} END{print c+0}" "$R"); autre=$ncc; fi; np=$(awk "/\\*\\*Complément volet admin \\([0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\)\\*\\* \\(Phase 41, plan 41-13\\)/ {c++} END{print c+0}" "$T/cp"); cat "$T/c1" "$T/cp" > "$T/all"; o3=$(awk "index(\$0, \"O-3\") {c++} END{print c+0}" "$T/all"); k=0; for key in POSE-CONFORME POSE-BYPASS-RELU M1-VERDICT PROVENANCE-SERVEUR TRACE-II PUSH-DIRECT D03-VERDICT M4-VERDICT PR-EN-VOL-APRES; do awk -v k="$key" "index(\$0, k) {f=1} END{exit !f}" "$T/c1" || { echo "CLE PROT-01 NON CITEE : $key"; k=$((k+1)); }; done; for key in PR-RELEASE RELEASE-DECISION INVARIANTS-PHASE ADR-072 ADR-059 CO-I CO-BASELINE CO-SENTINELLE CO-TEMOIN; do awk -v k="$key" "index(\$0, k) {f=1} END{exit !f}" "$T/cp" || { echo "CLE COMPLEMENT NON CITEE : $key"; k=$((k+1)); }; done; awk "\$1 ~ /-DECISION:\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) print substr(\$i, 8) }" "$PH/41-PREUVES.md" > "$T/dec"; while IFS= read -r o; do [ -n "$o" ] || continue; awk -v o="$o" "index(\$0, o) {f=1} END{exit !f}" "$T/all" || { echo "DECISION NON CITEE : $o"; k=$((k+1)); }; done < "$T/dec"; np2=$(awk "\$1==\"TRACE-II:\" && /non_prouvee/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); nr=$(awk "index(\$0, \"non_prouvee\") {c++} END{print c+0}" "$T/all"); ki=0; for id in PROT-02 PROT-03 PROT-04 PROT-05; do blk "$id" "$R" > "$T/n"; blk "$id" "$T/o" > "$T/b"; cmp -s "$T/n" "$T/b" || { echo "ENTREE EXISTANTE MODIFIEE $id"; ki=$((ki+1)); }; done; blk PROT-01 "$R" | sed "1s/^- \\[x\\]/- [ ]/" > "$T/n"; blk PROT-01 "$T/o" > "$T/b"; cmp -s "$T/n" "$T/b" || { echo "ENTREE EXISTANTE MODIFIEE PROT-01"; ki=$((ki+1)); }; tp=$(awk "/^\\| PROT-0[1-5] \\| Phase 41 \\| Pending/{c++} END{print c+0}" "$R"); q=$(awk "/^\\| QUAL-01 \\|/ && index(\$0, \"volet admin de la Phase 41\") && index(\$0, \"en crée aucun\") {c++} END{print c+0}" "$R"); echo "ledger : d03_verdict=${d3:-absent} mode=$mode coches=$x (attendu $ex) non_coches=$u (attendu $eu) prot01_non_coche=$u1 ligne_prot01=$n1 fragments_ligne_prot01=$fr ligne_de_l_autre_cas=$autre complements=$np lignes_citant_O3=$o3 cles_ou_decisions_non_citees=$k entrees_existantes_modifiees=$ki trace_prot01=$tr1 traces_pending=$tp (attendu $etp) qual01_volet_admin=$q trace_non_prouvee=$np2 recopiee=$nr"; [ "$x" -eq "$ex" ] && [ "$u" -eq "$eu" ] && [ "$u1" -eq "$eu" ] && [ "$n1" -eq 1 ] && [ "$fr" -eq 1 ] && [ "$autre" -eq 0 ] && [ "$np" -eq 3 ] && [ "$o3" -eq 0 ] && [ "$k" -eq 0 ] && [ "$ki" -eq 0 ] && [ "$tr1" -eq 1 ] && [ "$tp" -eq "$etp" ] && [ "$q" -eq 1 ] && { [ "$np2" -eq 0 ] || [ "$nr" -ge 1 ]; }'</automated>
    <fails_when>exit non nul ; mode `coche` (dernier `D03-VERDICT: CONFORME`) : moins de 5 entrées PROT cochées ou une non cochée, ligne « Exigence close le » de PROT-01 absente ou dupliquée, ou sans « le refus réel vient de D-03 », « acteur hors liste », « et non du critère 2 », D-02ter avec son canal et sa date et `D03-VERDICT: CONFORME`, ligne « Non cochée au » présente, ou traçabilité PROT-01 non `Complete … plan 41-13` citant D-03 ; mode `non_coche` (tout autre verdict, absence comprise) : PROT-01 coché (seul le refus réel de D-03 l'autorise), ligne « Non cochée au » absente ou sans « refus réel non établi », `D03-VERDICT`, `D03-M4-DECISION` et 41-09, ligne « Exigence close le » présente, ou traçabilité PROT-01 non `Pending … plan 41-13` avec renvoi à `D03-M4-DECISION` ; dans les deux modes : compléments PROT-02..04 différents de 3 ; une ligne ajoutée qui cite O-3 (la clôture et les compléments portent sur l'EXIGENCE) ; une clé de preuve ou une décision humaine non citée ; une ligne existante d'une entrée PROT réécrite (hors la case de PROT-01) ; une ligne PROT-02..05 `Pending` ; QUAL-01 sans la mention du volet admin ; un `non_prouvee` de la trace effacé</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } if (index(m, \"D-02ter\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") == 0) { print \"D-02ter SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis|D-02ter/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `D-02ter SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `D03-VERDICT: CONFORME` : PROT-01 coché, suivi de sa ligne de clôture d'exigence disant que le refus réel vient de D-03 (acteur hors liste) et non du critère 2 (sans O-3), clés et décisions citées, `non_prouvee` recopiés ; `Complete` en traçabilité
    - tout autre cas : PROT-01 laissé `- [ ]`, ligne « Non cochée au » renvoyant à `D03-M4-DECISION:` (41-09), clés et décisions citées ; `Pending` en traçabilité avec ce renvoi
    - PROT-02..04 : une ligne « Complément volet admin » chacun, lignes existantes intactes ; PROT-05 intact
    - QUAL-01 : « le volet admin de la Phase 41 … n'en crée aucun »
  </acceptance_criteria>
  <done>PROT-01 suit le seul refus réel établi par la phase (D-03, 41-09) : coché si et seulement si `D03-VERDICT: CONFORME`, sinon décoché avec renvoi à la décision de 41-09 ; le volet admin est tracé sous les exigences déjà cochées.</done>
</task>

<task type="auto">
  <name>Task 2 : critères du ROADMAP recalculés, ROADMAP et STATE à jour, éligibilité du jalon, balayage final, rejeu, SUMMARY avant publication</name>
  <files>.planning/workstreams/fiabilite/ROADMAP.md, .planning/workstreams/fiabilite/STATE.md, .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-*`, `M1-VERDICT:`, `M3-*`, `RELEASE-DECISION:`, `RELEASE:`, `INVARIANTS-PHASE:`)
    - .planning/workstreams/fiabilite/ROADMAP.md (ligne `- [ ] Phase 41:`, section `### Phase 41:`)
    - .planning/workstreams/fiabilite/STATE.md (§ Current Position)
  </read_first>
  <action>
Consigner dans `41-PREUVES.md`, section `## 41-13 — clôture` :
`CRITERES-ROADMAP: c1=<atteint|non> c2=<non_tenu_tel_que_formule|non> c3=<atteint|non_rejoue|non> c4=<atteint|non>` —
c1 : deux rulesets actifs relus et `POSE-CONFORME` à oui ; c2 (révisé le 2026-09-24, D-02ter —
arbitrage Willy, AskUserQuestion session principale, 2026-09-24 : aucun refus d'un merge sans
contournement n'est tenté, le critère n'est pas tenu tel que formulé) ne prend que deux valeurs :
`non_tenu_tel_que_formule` si `M1-VERDICT: SUR` (ou `M1-DECISION: option=accepter-et-documenter`)
ET `M3-VERDICT: CONFORME` (ou `M3-DECISION` de poursuite), `non` sinon ; c3 : `release-de-phase`
avec tag, `check-release-tag --remote` à 0 et merge sans suite `bypass` (`preuves-sans-release` →
`non_rejoue`) ; c4 : `INVARIANTS-PHASE` à 0 gate et 0 suite ajoutés.

`.planning/workstreams/fiabilite/ROADMAP.md` :
- section `### Phase 41:` : après la note « PÉRIMÈTRE SANS ADMIN EXÉCUTÉ », ajouter une note
  `> **VOLET ADMIN EXÉCUTÉ le <date>** (reprise du 2026-09-23, D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) : …`
  donnant les quatre valeurs de `CRITERES-ROADMAP:` (c2 écrit « non tenu tel que formulé — D-02ter,
  arbitrage Willy, AskUserQuestion session principale, 2026-09-24 », le texte du critère 2 n'étant pas
  réécrit), l'état de PROT-01 tel que la Task 1 l'a statué (coché sur le refus réel de D-03, ou non
  coché avec renvoi à `D03-M4-DECISION:`), renvoi à `41-PREUVES.md` et ADR-072 ;
- liste `Plans:` : cocher les treize lignes `41-01-PLAN.md` à `41-13-PLAN.md` ;
- ligne `- [ ] Phase 41:` de la liste des phases : la laisser `- [ ]`. Sous D-02ter, c2 ne peut pas
  être atteint : la phase n'est pas complète au sens du ROADMAP, et ce plan ne réécrit ni la règle ni
  le critère — la levée éventuelle revient à un arbitrage humain hors de ce plan.
`.planning/workstreams/fiabilite/STATE.md` § Current Position : insérer en tête un paragraphe dont la première ligne commence
par « Phase: **41** (Posture de protection du dépôt) — **VOLET ADMIN EXÉCUTÉ le <date>** » (critères,
PROT-01, décisions, prochain geste) et préfixer le paragraphe précédent par
« Précédemment (2026-09-18) : » ; frontmatter du STATE non touché.

Consigner `CLOTURE-JALON: jalon=fiabilite-v1.0 eligible=non criteres_41=incomplets phases_non_cochees=<numéros des lignes « - [ ] Phase N: » restantes du ROADMAP, ou aucune>`
— c2 n'étant pas atteignable sous D-02ter, la Phase 41 reste incomplète et le jalon non éligible par
ce plan, quelles que soient les autres valeurs. La clôture effective du jalon n'est pas jouée ici.

Consigner
`REJEU-TESTS-41-13: rc=<rc de l'outil> suites=<N> echecs=<M> echecs_preexistants_ledger=<WINDOWS#id,… par id croissant, ou aucun> echecs_hors_ledger=0`
— job `tests` rejoué sur l'arbre de la branche C AVANT le commit, par le bloc `tests` de cette tâche
(première passe : il ne rougit alors que sur la consignation absente, et imprime les valeurs à
recopier). Seul est toléré un échec dont le chemin est exactement celui d'une entrée `open` de
`.planning/WINDOWS.md` (rouges d'environnement de ce poste, #6 et #7 au 2026-09-24), la liste étant
lue dans le ledger, jamais écrite en dur (T-41-94, repris de 41-07) ; tout autre échec de suite, ou
toute autre étape du job en échec, arrête le plan et n'est jamais consigné comme toléré.

Balayage final O-3 et recensement de fermeture ; rejouer `gates` et `tests` (le bloc `tests`
recompare la liste tolérée à `REJEU-TESTS-41-13:`). Commit local sur C,
message « docs(41-13): ROADMAP, STATE et éligibilité du jalon sur pièce (D-02bis — arbitrage Willy,
AskUserQuestion session principale, 2026-09-23) ». Rédiger et committer `41-13-SUMMARY.md` sur C À LA
FIN de cette tâche : il écrit que le volet admin ne crée aucun gate (critère 4), recopie
`CRITERES-ROADMAP:` et `CLOTURE-JALON:`, annonce la Task 3 et recopie la règle des artefacts
post-merge (dont `/gsd-complete-milestone`, par l'orchestrateur, si et seulement si `eligible=oui`).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq "[.[] | select(.enforcement == \"active\")] | length"); cf=$(awk "\$1==\"POSE-CONFORME:\" && /branche=oui/ && /tags=oui/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); c1=$([ "$n" -eq 2 ] && [ "$cf" -ge 1 ] && echo atteint || echo non); m1=$(last M1-VERDICT:); m1d=$(val M1-DECISION: option); m3=$(last M3-VERDICT:); m3d=$(val M3-DECISION: option); if { [ "$m1" = SUR ] || [ "$m1d" = accepter-et-documenter ]; } && { [ "$m3" = CONFORME ] || [ "$m3d" = consigner-et-poursuivre ] || [ "$m3d" = fermer-et-poursuivre ]; }; then c2=non_tenu_tel_que_formule; else c2=non; fi; rd=$(val RELEASE-DECISION: option); rt=$(val RELEASE: tag); rr=$(val RELEASE: check_release_tag_remote_rc); sm=$(val RELEASE: suite_merge); if [ "$rd" = release-de-phase ] && [ -n "$rt" ] && [ "$rt" != sans_objet ] && [ "$rr" = 0 ] && [ -n "$sm" ] && [ "$sm" != bypass ]; then c3=atteint; elif [ "$rd" = preuves-sans-release ]; then c3=non_rejoue; else c3=non; fi; ga=$(val INVARIANTS-PHASE: gates_ajoutes); sa=$(val INVARIANTS-PHASE: suites_ajoutees); c4=$([ "$ga" = 0 ] && [ "$sa" = 0 ] && echo atteint || echo non); cc="c1=$(val CRITERES-ROADMAP: c1) c2=$(val CRITERES-ROADMAP: c2) c3=$(val CRITERES-ROADMAP: c3) c4=$(val CRITERES-ROADMAP: c4)"; echo "criteres recalcules : c1=$c1 c2=$c2 c3=$c3 c4=$c4 | consignes : $cc"; [ "$cc" = "c1=$c1 c2=$c2 c3=$c3 c4=$c4" ]'</automated>
    <fails_when>exit non nul ; ou une valeur de `CRITERES-ROADMAP:` différente du recalcul sur pièce (critère déclaré atteint sans ses preuves, ou l'inverse)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; c2=$(val CRITERES-ROADMAP: c2); case "$c2" in non_tenu_tel_que_formule|non) ;; *) echo "c2 hors des deux valeurs admises sous D-02ter : ${c2:-absent}"; exit 1;; esac; R=.planning/workstreams/fiabilite/ROADMAP.md; l41=$(awk "/^- \\[[ x]\\] Phase 41: /{print substr(\$0,4,1); exit}" "$R"); nt=$(awk "/^### Phase 41: /{f=1; next} /^### Phase /{f=0} f && index(\$0, \"> **VOLET ADMIN EXÉCUTÉ le \")==1 {c++} END{print c+0}" "$R"); pl=$(awk "/^- \\[x\\] 41-(0[1-9]|1[0-3])-PLAN\\.md/{c++} END{print c+0}" "$R"); pu=$(awk "/^- \\[ \\] 41-(0[1-9]|1[0-3])-PLAN\\.md/{c++} END{print c+0}" "$R"); ST=.planning/workstreams/fiabilite/STATE.md; sp=$(awk "/^## Current Position/{f=1; next} f && NF {print; exit}" "$ST" | awk "index(\$0, \"Phase: **41**\")==1 && index(\$0, \"VOLET ADMIN EXÉCUTÉ\") {c++} END{print c+0}"); pr=$(awk "index(\$0, \"Précédemment (2026-09-18) : Phase: **41**\")==1 {c++} END{print c+0}" "$ST"); pn=$(awk "/^- \\[ \\] Phase [0-9.]+:/ { s=\$0; sub(/^- \\[ \\] Phase /, \"\", s); sub(/:.*/, \"\", s); printf \"%s%s\", (n++ ? \",\" : \"\"), s } END { if (!n) printf \"aucune\" }" "$R"); ce=$(val CLOTURE-JALON: eligible); ci=$(val CLOTURE-JALON: criteres_41); cp=$(val CLOTURE-JALON: phases_non_cochees); echo "roadmap : c2=$c2 phase41_case=[$l41] attendu=[ ] note_volet_admin=$nt plans_coches=$pl plans_non_coches=$pu | state : position=$sp precedemment=$pr | jalon : phases_non_cochees=$pn (consigne $cp) eligible=$ce (attendu non) criteres_41=$ci (attendu incomplets)"; [ "$l41" = " " ] && [ "$nt" -eq 1 ] && [ "$pl" -eq 13 ] && [ "$pu" -eq 0 ] && [ "$sp" -eq 1 ] && [ "$pr" -eq 1 ] && [ "$cp" = "$pn" ] && [ "$ce" = non ] && [ "$ci" = incomplets ]'</automated>
    <fails_when>exit non nul ; ou `CRITERES-ROADMAP: c2` hors de `non_tenu_tel_que_formule` et `non` (sous D-02ter, c2 n'est jamais atteint) ; ou case Phase 41 cochée ; ou note « VOLET ADMIN EXÉCUTÉ » absente ou dupliquée dans la section Phase 41 ; ou un des treize plans non coché ; ou position courante du STATE non mise à jour, ou paragraphe précédent non préfixé ; ou `CLOTURE-JALON:` autre que `eligible=non criteres_41=incomplets`, ou phases non cochées consignées différentes du recalcul</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/workstreams/fiabilite/REQUIREMENTS.md CLAUDE.md docs/ADR.md .planning/BACKLOG.md CHANGELOG.md README.md README.fr.md .planning/workstreams/fiabilite/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md $PH/41-0*-SUMMARY.md $PH/41-1[0-3]-SUMMARY.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition, O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02) — sur la doctrine, le ledger, le CHANGELOG, les README ou les SUMMARY du volet admin ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh 2>&1) || rc=$?; printf "%s\n" "$out" | awk "/^perimetre: |^allowlist: |^limite: /"; h=$(printf "%s\n" "$out" | awk "!/^perimetre: |^allowlist: |^limite: / && NF {c++} END{print c+0}"); echo "aucune-fermeture rc=$rc hits=$h"; [ "$rc" -eq 0 ] && [ "$h" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `rc=` différent de 0 ; ou `hits=` différent de 0 — une co-occurrence hors allowlist (ledger, doctrine, registre, SUMMARY) ou un porteur muet sur sa limite de fond</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; n=0; for i in 01 02 03 04 05 06 07 08 09 10 11 12 13; do if [ -f "$PH/41-$i-SUMMARY.md" ]; then n=$((n+1)); else echo "SUMMARY ABSENT : 41-$i"; fi; done; nu=$(awk "index(\$0, \"checks en cours\") && index(\$0, \"pas sur une PR rouge\") {c++} END{print c+0}" "$PH/41-08-SUMMARY.md"); ag=$(awk "index(tolower(\$0), \"aucun gate\") {c++} END{print c+0}" "$PH/41-13-SUMMARY.md"); cj=$(awk "index(\$0, \"CLOTURE-JALON\") {c++} END{print c+0}" "$PH/41-13-SUMMARY.md"); echo "SUMMARY : presents=$n nuance_41-08=$nu aucun_gate_41-13=$ag cloture_jalon_41-13=$cj"; [ "$n" -eq 13 ] && [ "$nu" -ge 1 ] && [ "$ag" -ge 1 ] && [ "$cj" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou un SUMMARY de plan absent ; ou le SUMMARY de 41-08 ne reprend pas la nuance « contournement réel sur des checks en cours, pas sur une PR rouge » ; ou le SUMMARY de 41-13 ne dit pas que le volet admin ne crée aucun gate, ou ne recopie pas `CLOTURE-JALON:`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; K=REJEU-TESTS-41-13:; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); ne=$(awk -F" [|] " "/^REJEU tests [|] / && \$3 != \"rc=0\" {c++} END{print c+0}" "$T/t"); awk -F"|" "{i=\$2; f=\$5; s=\$8; gsub(/ /, \"\", i); gsub(/ /, \"\", f); gsub(/ /, \"\", s); if (s==\"open\" && f ~ /\\/tests\\/test-[^\\/]*\\.sh\$/) print f \"\t\" i}" .planning/WINDOWS.md > "$T/ledger"; awk "/^::error::FAIL\\(rc=[0-9]+\\) / {sub(/^::error::FAIL\\(rc=[0-9]+\\) /, \"\"); print}" "$T/t" > "$T/ko"; awk -F"\t" "NR==FNR {l[\$1]=\$2; next} !(\$0 in l)" "$T/ledger" "$T/ko" > "$T/hl"; awk -F"\t" "NR==FNR {l[\$1]=\$2; next} (\$0 in l) {print l[\$0]}" "$T/ledger" "$T/ko" > "$T/id"; sort -n -u "$T/id" > "$T/ids"; tl=$(awk "BEGIN{ORS=\"\"} {print (NR>1 ? \",\" : \"\") \"WINDOWS#\" \$0} END{if (NR==0) print \"aucun\"}" "$T/ids"); hl=$(awk "END{print NR}" "$T/hl"); nk=$(awk "END{print NR}" "$T/ko"); awk "{print \"HORS LEDGER : \" \$0}" "$T/hl"; d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); nl=$(awk -v k="$K" "\$1==k {c++} END{print c+0}" "$PH/41-PREUVES.md"); att=$([ "$b" != absent ] && set -- $b && [ "$2" -gt 0 ] && echo 1 || echo 0); echo "tests rc=$rc etapes_en_echec=$ne bilan(N M)=$b echecs=$nk tolere=$tl echecs_hors_ledger=$hl decouverte_independante=$d | consigne $K lignes=$nl rc=$(val "$K" rc) suites=$(val "$K" suites) echecs=$(val "$K" echecs) echecs_preexistants_ledger=$(val "$K" echecs_preexistants_ledger) echecs_hors_ledger=$(val "$K" echecs_hors_ledger)"; [ "$b" != absent ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq "$nk" ] && [ "$hl" -eq 0 ] && [ "$ne" -eq "$att" ] && [ "$rc" -eq "$ne" ] && [ "$nl" -eq 1 ] && [ "$(val "$K" rc)" = "$rc" ] && [ "$(val "$K" suites)" = "$1" ] && [ "$(val "$K" echecs)" = "$2" ] && [ "$(val "$K" echecs_preexistants_ledger)" = "$tl" ] && [ "$(val "$K" echecs_hors_ledger)" = 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, ou N différent de la découverte indépendante ; ou nombre d'échecs du bilan différent du nombre de lignes `::error::FAIL` ; ou un échec dont le chemin n'est exactement celui d'aucune entrée `open` de `.planning/WINDOWS.md` (ligne `HORS LEDGER :` ; seuls les rouges d'environnement déjà tracés au ledger sont tolérés, #6 et #7 sur ce poste au 2026-09-24, T-41-94) ; ou une étape du job autre que celle des suites en échec (`etapes_en_echec=` différent de 1 quand le bilan compte des échecs, de 0 sinon), ou un rc de l'outil différent de `etapes_en_echec` ; ou `REJEU-TESTS-41-13:` absente ou dupliquée, ou dont `rc`, `suites`, `echecs` ou la liste `echecs_preexistants_ledger` diffèrent du rejeu, ou dont `echecs_hors_ledger` n'est pas 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-13-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p" && co=$((co+1)); done < "$T/f"; echo "avant publication : summary_committe=$s modifications_non_committees=$d chemins_codeowners=$co"; [ "$s" = oui ] && [ "$d" -eq 0 ] && [ "$co" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY 41-13 non committé ; ou modifications suivies non committées ; ou la PR de clôture toucherait un chemin CODEOWNERS</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } if (index(m, \"D-02ter\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") == 0) { print \"D-02ter SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis|D-02ter/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `D-02ter SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `CRITERES-ROADMAP:` recalculé = consigné, c2 à `non_tenu_tel_que_formule` ou `non` ; case Phase 41 laissée `- [ ]` (c2 non atteignable sous D-02ter) ; note du volet admin et treize plans cochés ; STATE à jour
    - `CLOTURE-JALON: eligible=non criteres_41=incomplets`, phases non cochées recalculées = consignées ; aucune clôture de jalon jouée par ce plan
    - balayage O-3 vert ; `check-aucune-fermeture.sh` rc 0 ; treize SUMMARY présents, nuance de 41-08, « aucun gate » et `CLOTURE-JALON` dans 41-13
    - rejeu `gates` vert ; rejeu `tests` sans échec hors ledger ni autre étape en échec, liste tolérée consignée (`REJEU-TESTS-41-13:`) = rejeu ; SUMMARY 41-13 committé ; PR de clôture hors CODEOWNERS ; messages de commit conformes
  </acceptance_criteria>
  <done>La phase dit sur pièce ce qu'elle a atteint, et l'éligibilité du jalon est consignée sans être présumée.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 : gestes humains de Willy (`picmakpro`) — publier C, ouvrir la PR de clôture, merger SANS contournement</name>
  <read_first>
    - `CLOTURE-JALON:` et les commandes imprimées par la Task 2
  </read_first>
  <action>Willy, compte `picmakpro`, publie C, ouvre la PR de clôture, attend la CI, vérifie l'état `CLEAN`, merge sans contournement, puis rapporte le résultat à l'orchestrateur. Aucun agent ne merge.</action>
  <instructions>
Compte qui agit : `picmakpro`.

1. `git push -u origin docs/phase-41-cloture`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head docs/phase-41-cloture --title "Phase 41 — clôture du volet admin : PROT-01, ROADMAP, STATE" --body "Preuves post-release, invariants du volet admin, PROT-01 statué sur pièce selon le refus réel mesuré en 41-09, compléments PROT-02..04, ROADMAP et STATE à jour, éligibilité du jalon consignée. Merge sans contournement."`
   (corps sans arbitrage cité ; s'il en cite un, il porte son canal et sa date)
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin.
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus --jq .mergeStateStatus`
   → `CLEAN` pour poursuivre ; `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`
   puis 3 et 4 ; tout autre état : arrêt, sans contournement.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) ;
   témoin `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit,body --jq '{state, merge: .mergeCommit.oid, body}'`.

Après ce merge : AUCUN commit direct sur `main` — le serveur refuse tout acteur hors liste, et pour
Samuel et Willy un push direct serait accepté mais tracé et signalé par G-3 : il n'est pas utilisé.
Tout artefact écrit ensuite (VERIFICATION de phase, clôture du jalon par `/gsd-complete-milestone` —
SEULEMENT si `CLOTURE-JALON: eligible=oui` —, rapport de mission) part d'une branche
`docs/phase-41-verification` créée depuis `origin/main`, et entre par une PR publiée et mergée par
Willy exactement comme aux étapes 1 à 5 (état `CLEAN`, sans `--admin`) — ou n'est pas versionné.
  </instructions>
  <verification>L'orchestrateur relit `gh pr view <n> --json state,mergeCommit,body`, vérifie que le push de merge sur `main` est vert (4 jobs), puis, avant tout artefact post-merge : `git fetch origin && git rev-list --count origin/main..main` rend `0` et `git branch --show-current` n'est pas `main` ; il ne lance `/gsd-complete-milestone` que si `CLOTURE-JALON: eligible=oui`.</verification>
  <acceptance_criteria>
    - PR de clôture ouverte depuis C, CI verte, `CLEAN`, mergée par `gh pr merge --merge` sans `--admin` ; résultat rapporté
  </acceptance_criteria>
  <resume-signal>Coller `<n>` et les sorties de 3 (fin), 4 et 5, puis écrire « PR de clôture mergée ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves → ledger, ROADMAP, STATE | cochage d'exigences et de phase, point d'entrée des lecteurs futurs |
| orchestrateur → `main` après clôture | artefacts de fin de phase et clôture de jalon écrits hors du plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-43 | Repudiation | exigence cochée en effaçant un `non_prouvee` | medium | mitigate | recopie vérifiée dans les lignes ajoutées |
| T-41-46 | Repudiation | clôture par commit direct | medium | mitigate | PR de clôture hors CODEOWNERS, état `CLEAN` exigé, `--admin` exclu, merge relu par l'orchestrateur |
| T-41-55 | Repudiation | clôture d'exigence confondue avec O-3 | medium | mitigate | lignes ajoutées sans O-3 (vérifié) ; balayage O-3 et `check-aucune-fermeture.sh` |
| T-41-56 | Tampering | artefact post-merge poussé directement sur `main` | medium | mitigate | branche `docs/phase-41-verification` + PR ; `git rev-list --count origin/main..main` à 0 relu par l'orchestrateur |
| T-41-67 | Repudiation | phase ou jalon déclarés complets sans critères atteints | high | mitigate | `CRITERES-ROADMAP:` recalculé sur pièce, c2 borné à `non_tenu_tel_que_formule` ou `non` ; case Phase 41 laissée décochée et `CLOTURE-JALON: eligible=non` (vérifié) ; `/gsd-complete-milestone` seulement sur une consignation `eligible=oui` issue d'un arbitrage humain |
| T-41-92 | Repudiation | PROT-01 coché sans refus réel établi, ou sur le critère 2 non tenu | high | mitigate | case PROT-01 pilotée par le seul dernier `D03-VERDICT:` (refus serveur d'un push direct hors liste, 41-09) ; ligne de clôture exigeant « le refus réel vient de D-03 » et « et non du critère 2 » ; hors `CONFORME`, case décochée et renvoi à `D03-M4-DECISION:` (vérifié) |
| T-41-94 | Repudiation | rouge réel masqué par la tolérance des rouges d'environnement locaux | low | mitigate | tolérance limitée aux chemins exacts des entrées `open` de `.planning/WINDOWS.md`, lus dans le ledger et non écrits dans le plan ; tout autre échec imprimé `HORS LEDGER :` et rougit, comme une étape du job autre que celle des suites en échec ; liste tolérée consignée par id (`REJEU-TESTS-41-13:`) et recomparée au rejeu ; la CI réelle, qui n'a pas ces rouges, reste la référence (CI de la PR de clôture, Task 3) |
</threat_model>

<verification>
Ledger coché et complété sur pièce sans confusion avec O-3 ; ROADMAP et STATE conformes aux critères
réellement atteints ; éligibilité du jalon consignée ; PR de clôture mergée par le chemin normal ;
artefacts post-merge cantonnés à une branche et une PR.
</verification>

<success_criteria>
Critères de succès 1 à 4 de la Phase 41 consignés sur pièce (atteints, non rejoué pour le 3 sur
décision, non tenu tel que formulé pour le 2 sous D-02ter) ; PROT-01 coché si et seulement si
`D03-VERDICT: CONFORME` (refus réel de D-03, acteur hors liste), sinon laissé décoché avec renvoi à
`D03-M4-DECISION:` ; jalon fiabilite-v1.0 consigné non éligible (`eligible=non`, c2 non tenu tel que
formulé), sa levée relevant d'un arbitrage humain hors de ce plan.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-13-SUMMARY.md` — committé sur la branche C à la fin de la Task 2, AVANT la publication (Task 3). Tout artefact postérieur au merge suit la règle du frontmatter (branche + PR, ou non versionné).

Règle STATE.md (ADR-063) : jamais `gsd-tools state.*` ni `gsd_run query commit` sur `.planning/workstreams/fiabilite/STATE.md` ; éditions à la main dans le corps seulement ; dans le frontmatter, seuls `last_updated`, `last_activity`, `last_activity_desc` (quatre lignes au plus) et les nombres de `progress` changent ; le frontmatter se ferme avant la ligne 60.
</output>

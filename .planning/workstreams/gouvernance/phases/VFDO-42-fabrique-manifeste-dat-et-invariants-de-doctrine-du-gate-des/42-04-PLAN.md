---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [FABR-02]

must_haves:
  truths:
    - "Une liste du manifeste est périmée quand son verifie_le a plus de valide_jours jours, ou quand il est postérieur à aujourd'hui (DATE-FUTURE, non vérifiable) ; valide_jours est lu dans le manifeste, jamais dans le script (D-02)"
    - "Âge égal à valide_jours → frais ; valide_jours + 1 → périmé ; un manifeste à valide_jours: 5 est périmé à 6 jours"
    - "Périmé + --manifest-freshness=strict → rc=3, ligne INDETERMINE portant MANIFESTE-PERIME, rapport complet imprimé avant, et jamais la ligne « ✓ agents conformes » (D-04)"
    - "Périmé sans l'option (défaut lenient : garde d'écriture, hook SessionStart, CLI) → avertissement MANIFESTE-PERIME sans aucun « ✗ », jamais un refus ; sous --hook la ligne est imprimée et rc=0 (D-04)"
    - "Périmé → « outil hors du set connu » et « nom d'agent non résolu » passent en avertissement suffixé retrograde dans tous les contextes ; « champ inconnu » reste un avertissement ; model, memory, effort, permissionMode invalides restent bloquants (D-05, Pitfall 3)"
    - "La garde d'écriture laisse passer, sous manifeste périmé, un agent dont un outil manque au manifeste ; sous manifeste frais elle le refuse (D-04)"
    - "--manifest-freshness=<valeur inconnue> → rc=1 explicite, jamais un repli muet sur lenient"
    - "La fraîcheur n'est évaluée que s'il y a au moins un agent à juger : sur cible vide, contrat F13 inchangé et aucune ligne MANIFESTE-PERIME"
    - "Les quatre appels de check-agents.sh de la CI (trois étapes du job gates + Gate C du lab frais) passent --manifest-freshness=strict — seul lieu où l'INDETERMINE de fraîcheur est rendu (D-04)"
    - "Deux mutants QUAL-01 tués et tracés « ✓ MUT-F1 TUE » et « ✓ MUT-F2 TUE » : détection de péremption neutralisée, rétrogradation neutralisée"
  artifacts:
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "option --manifest-freshness, manifeste_perime(), retrograder, INDETERMINE de fraîcheur"
      contains: "manifeste_perime"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "cas T83 à T90, helpers make_gate_mutant/okmut/komut, mutants MUT-F1 et MUT-F2"
      contains: "make_gate_mutant"
    - path: ".github/workflows/ci.yml"
      provides: "--manifest-freshness=strict aux quatre appels de check-agents"
      contains: "--manifest-freshness=strict"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "plugin/conductor/scripts/check-agents.sh"
      via: "--manifest-freshness=strict sur chaque invocation — rc 3 = étape rouge sur manifeste périmé"
      pattern: "manifest-freshness=strict"
    - from: "plugin/conductor/scripts/guard-agent-write.sh"
      to: "plugin/conductor/scripts/check-agents.sh"
      via: "--file --strict SANS l'option de fraîcheur : un manifeste périmé n'y produit jamais de ligne « ✗ … : … »"
      pattern: "--strict"
  prohibitions:
    - statement: "MUST NOT faire d'un manifeste périmé un refus d'écriture d'agent ou un blocage de session chez un utilisateur (hors CI du dépôt)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans descripteur câblé ; couverte en pratique par T87 et T90, laissée flaggée (fallback sans humain pour câbler un contrôle)"
---

<objective>
Rendre la péremption du manifeste détectable et lui donner le seul effet que D-04 et D-05 autorisent :
INDÉTERMINÉ (exit 3) là où l'on peut rafraîchir le manifeste — la CI du dépôt, par une option
explicite — et un avertissement partout ailleurs. Un manifeste périmé ne peut plus ni accepter ni
refuser sur une liste fermée : les trois classes nommées par D-05 passent en avertissement dans tous
les contextes, le reste du lint garde son régime (Pitfall 3). La validité vient du manifeste (D-02).

Purpose: FABR-02 — « un gate qui ne sait plus si sa référence est à jour n'a pas le droit de rendre
un verdict » (spec §3), appliqué dans les deux sens, sans jamais bloquer un utilisateur qui n'a aucun
moyen de rafraîchir un fichier livré par le plugin (fait mesuré de D-04 : guard-agent-write.sh
refuse dès qu'un « ✗ » sort).
Output: l'option `--manifest-freshness=lenient|strict`, la détection, la rétrogradation, T83 à T90,
MUT-F1 et MUT-F2, et les quatre appels CI durcis.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-01-SUMMARY.md

Référence de mutation QUAL-01 : `scripts/tests/test-check-gate-touche.sh` l.35-41 (okmut/komut,
ligne « ✓ MUT-<n> TUE : rc_mutant=… attendu …, rc_original=… attendu … ») et l.109-133
(make_mutant : valeurs passées par ENVIRON, jamais par `awk -v` ; refus si `cmp` identique ou
`bash -n` en échec).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : fraîcheur du manifeste — détection, rétrogradation D-05, INDÉTERMINÉ sous option, avertissement ailleurs (T83 à T90, MUT-F1, MUT-F2)</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : parsing des arguments et validation de --resolve-agents ; lint_tool_field, branche « outil hors du set connu » et « nom d'agent non resolu » ; boucle « champ inconnu » ; rapport final et branche --hook ; charger_manifeste posé en 42-01)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : mk_manifest, mk_gate_dir, GATE_DIR posés en 42-01 ; T28, T30 et son registre $REG, T9, T11-T14 payload_write/run_guard)
    - plugin/conductor/scripts/guard-agent-write.sh (l.95-115 : seules les lignes « ✗ … : … » font refuser)
    - scripts/tests/test-check-gate-touche.sh (l.35-41 et l.109-133 : patron des mutants QUAL-01)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-02, D-04, D-05)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Pattern 2, Pitfall 3, Open Question 3)
  </read_first>
  <behavior>
    - T83 : manifeste périmé (âge = valide_jours + 1) + agent `tools: Read, Reed` + --strict → rc=0, sortie avec « outil hors du set connu 'Reed' », `retrograde` et `MANIFESTE-PERIME` ; jumeau frais → rc=1 sans `retrograde`
    - T84 : périmé + `--manifest-freshness=strict` + agent conforme → rc=3, `INDETERMINE` et `MANIFESTE-PERIME`, aucune ligne « ✓ agents conformes » ; frais + même option → rc=0 ; périmé sans option → rc=0, `MANIFESTE-PERIME` présent, aucun « ✗ »
    - T85 : bornes — âge = valide_jours → aucun MANIFESTE-PERIME, option strict rc=0 ; âge = valide_jours + 1 → rc=3 ; `valide-jours=5` : âge 5 → rc=0, âge 6 → rc=3
    - T86 : `date-future=outils` (autres listes du jour) + option strict → rc=3 et `DATE-FUTURE` ; sans option → rc=0 et `MANIFESTE-PERIME`
    - T87 : --hook — périmé + agent conforme (skills: petit-skill) → rc=0, sortie contenant `MANIFESTE-PERIME` ; frais + même agent → sortie vide ; dossier vide + --strict + périmé → rc=3 sans --hook, sans aucune ligne MANIFESTE-PERIME (fraîcheur non évaluée)
    - T88 : Pitfall 3 — périmé + agent `model: gpt-4`, `memory: global`, `effort: extreme` → rc=1 avec « model invalide », « memory invalide », « effort invalide »
    - T89 : périmé + `--resolve-agents=strict` + allowlist `Agent(vf-codeur)` non résolue (registre de T30 ; la fixture porte déjà SendMessage) → rc=0, « non resolu » et `retrograde` ; jumeau frais → rc=1
    - T90 : garde d'écriture — dossier de gate périmé + agent `tools: Read, Reed` → sortie vide (laisse passer) ; dossier frais → refus JSON citant « outil hors du set connu » ; `--manifest-freshness=stricte` → rc=1 et « --manifest-freshness invalide »
    - MUT-F1 : ligne `perimees = manifeste_perime(` remplacée par `perimees = []` → fixture T84 : rc_original=3, rc_mutant=0
    - MUT-F2 : ligne `retrograder = bool(perimees)` remplacée par `retrograder = False` → fixture T83 : rc_original=0, rc_mutant=1, sortie mutant avec « outil hors du set connu » et sans « Traceback »
  </behavior>
  <action>
1. Tests d'abord (TDD) : écrire les helpers et T83 à T90, MUT-F1, MUT-F2 dans
`test-check-agents.sh`, les jouer ROUGES contre le gate de 42-01, puis implémenter.
   - `make_gate_mutant <id> <age_jours> <motif-fixe> <remplacement> [operation]` : construit
     `$WORK/mut-<id>` par `mk_gate_dir` (même âge, même opération que le run original) ; dans SA copie
     de check-agents.sh, exactement UNE ligne doit contenir le motif (sinon `komut` « MOTIF AMBIGU
     OU ABSENT (n=<k>) ») ; cette ligne est remplacée par son indentation d'origine suivie du
     remplacement, valeurs transmises à awk par ENVIRON (jamais `awk -v`, cf. test-check-gate-touche.sh) ;
     refus si la copie est identique à l'original (`cmp`, « NON OPPOSABLE ») ou si `bash -n` échoue.
     Le mutant vit à côté d'un manifeste du même âge : D-03 n'interfère jamais.
   - `okmut <id> <rc_mut> <att_mut> <rc_orig> <att_orig>` imprime exactement
     `✓ MUT-<id> TUE : rc_mutant=<x> attendu <x>, rc_original=<y> attendu <y>` et incrémente `pass` ;
     `komut` imprime `✗ MUT-<id> NON TUE : …` et incrémente `fail`. Un mutant n'est tué que si les
     DEUX rc sont exacts et que sa sortie ne contient pas « Traceback » (un plantage Python n'est
     jamais compté comme tué).
   - Âge périmé = `valide_jours` du manifeste versionné + 1, lu par Python — jamais 31 écrit en dur.
   - Fixtures porteuses d'une allowlist `Agent(...)` : les écrire d'emblée avec `SendMessage` et sans
     violation des invariants à venir (42-05), pour que leurs rc ne dépendent que de la fraîcheur.
   - Catalogue d'en-tête : lignes T83 à T90 et MUT-F1, MUT-F2 sous « Fraîcheur du manifeste (Phase
     42, FABR-02, D-02/D-04/D-05) ».
2. Gate, côté bash : accepter `--manifest-freshness=*` (variable `MANIFEST_FRESHNESS`, défaut
`lenient`) et le valider exactement comme `--resolve-agents` : toute valeur hors `lenient|strict` →
`[check-agents] ✗ --manifest-freshness invalide '<v>' — attendu lenient|strict` sur stderr, exit 1.
Transmettre `VF_MANIFEST_FRESHNESS`. Documenter l'option dans l'Usage de l'en-tête.
3. Gate, côté Python (mêmes règles d'échappement que 42-01) :
   - `manifeste_perime(manifest, aujourd_hui)` rend, dans l'ordre des listes, une description par
     liste périmée : `verifie_le` postérieur à aujourd'hui → « <clé> (DATE-FUTURE : vérifiée le <d>,
     postérieure à aujourd'hui — non vérifiable) » ; âge en jours strictement supérieur à
     `valide_jours` → « <clé> (vérifiée le <d>, <âge> j, validité <n> j) ». Aucune valeur par défaut
     de validité (D-02).
   - Juste après le chargement du manifeste, sur deux lignes distinctes et uniques dans le fichier :
     `perimees = manifeste_perime(manifest, date.today())` puis `retrograder = bool(perimees)` (ces
     deux lignes sont les cibles de MUT-F1 et MUT-F2 : ne pas les fusionner).
   - D-05 dans lint_tool_field : « outil hors du set connu » va en erreur seulement si `strict and not
     retrograder`, sinon en avertissement ; « nom d'agent non resolu » sous `--resolve-agents=strict`
     va en erreur seulement si `not retrograder`. Quand `retrograder` est vrai, ces messages portent le
     suffixe ` [MANIFESTE-PERIME — retrograde en avertissement, D-05]`. « champ inconnu » est déjà un
     avertissement : inchangé. model, memory, effort, permissionMode, isolation, background, maxTurns
     et la syntaxe des allowlists gardent leur régime (Pitfall 3 : un modèle inventé reste inventé).
   - Rapport : si `perimees`, une ligne `[check-agents] ⚠ MANIFESTE-PERIME — <descriptions jointes> —
     listes fermées rétrogradées en avertissement (D-05) ; rafraîchir check-agents-manifest.json :
     relire chaque source, comparer, re-dater`, qui ne contient jamais « ✗ ». Sous --hook elle est
     imprimée même sans autre avertissement (jamais silence de message), puis la branche --hook
     existante sort 0. Hors --hook, elle précède la liste des avertissements.
   - Issue (D-04) : hors --hook, si `perimees` et option strict, imprimer les erreurs éventuelles
     comme aujourd'hui, puis `[check-agents] ✗ INDETERMINE — MANIFESTE-PERIME : aucun verdict rendu
     (D-04) — rafraîchir check-agents-manifest.json puis relancer`, et `sys.exit(3)` ; la ligne
     « ✓ agents conformes » n'est JAMAIS imprimée dans ce cas. Sinon, flux existant (1 si erreurs,
     0 sinon).
   - En-tête : documenter D-02/D-04/D-05, l'échéance (verifie_le + valide_jours), ses effets connus
     (la CI rend INDETERMINE à ses quatre appels ; T20 de test-dev-orchestrator.sh, qui compte les
     avertissements d'un AGENT.md, en compte un de plus) et le geste unique qui les lève : rafraîchir
     le manifeste.
4. Rejouer la suite entière : 0 KO, T83 à T90 et les deux lignes « ✓ MUT-F1 TUE », « ✓ MUT-F2 TUE ».
5. Commit : `feat(conductor): fraîcheur du manifeste — INDÉTERMINÉ en CI, avertissement chez l'utilisateur (FABR-02)`,
avec les trailers (D-15)
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — fraîcheur du manifeste et rétrogradation des listes fermées (D-04, D-05)`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — cas T83 à T90 et mutants MUT-F1, MUT-F2 (D-04, D-05)`.
Corps : décisions de cadrage de Claude sur délégation explicite de Willy (AskUserQuestion session
principale, 2026-09-23), 42-CONTEXT.md D-02, D-04, D-05.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(8[3-9]|90)|MUT-F[12]|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ » (cas rouge ou « MUT-F… NON TUE »), ou l'une des étiquettes T83 à T90 n'a aucune ligne « ✓ », ou « ✓ MUT-F1 TUE » ou « ✓ MUT-F2 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>bash plugin/conductor/scripts/check-agents.sh --manifest-freshness=stricte --agents-dir=plugin/business-pilot-bundle/agents >/dev/null 2>&1; echo "rc=$?"</automated>
    <fails_when>la sortie n'est pas exactement « rc=1 » (valeur d'option inconnue acceptée ou repliée en silence)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'perimees = manifeste_perime(' plugin/conductor/scripts/check-agents.sh` rend 1 et `grep -c 'retrograder = bool(perimees)' plugin/conductor/scripts/check-agents.sh` rend 1
    - `grep -c -- '--manifest-freshness' plugin/conductor/scripts/check-agents.sh` rend au moins 3 (usage, parsing, validation)
    - la sortie de test-check-agents.sh contient `✓ MUT-F1 TUE : rc_mutant=0 attendu 0, rc_original=3 attendu 3` et `✓ MUT-F2 TUE : rc_mutant=1 attendu 1, rc_original=0 attendu 0`
    - la sortie de test-check-agents.sh contient au moins une ligne `✓ T83` … `✓ T90` pour chaque étiquette et finit sur `0 KO ==`
    - `bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --agents-dir=plugin/business-pilot-bundle/agents; echo rc=$?` imprime `rc=0` tant que le manifeste versionné est frais
    - les deux trailers Gate-Touche figurent dans le commit qui touche check-agents.sh et test-check-agents.sh
  </acceptance_criteria>
  <done>La péremption est détectée par liste (âge et date future), l'INDETERMINE n'est rendu que sous l'option strict, l'utilisateur ne reçoit qu'un avertissement, les trois classes de D-05 sont rétrogradées et elles seules ; T83 à T90 verts, MUT-F1 et MUT-F2 tués.</done>
</task>

<task type="auto">
  <name>Tâche 2 : la CI passe --manifest-freshness=strict à ses quatre appels de check-agents (D-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (fichier modifié : job gates, étapes « check-agents --strict sur chaque plugin/*/agents », « check-agents --strict sur chaque plugin/*/AGENT.md », « check-agents --resolve-agents=strict (monde fermé …) » ; job lab-frais, étape « Gate C du lab frais »)
    - plugin/conductor/scripts/check-agents.sh (en-tête : l'option et l'échéance documentées à la tâche 1)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-04, D-15)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Pitfall 6 : table de vérité G-2)
  </read_first>
  <action>
1. Ajouter `--manifest-freshness=strict` à chacune des quatre invocations `bash …/check-agents.sh`
de ci.yml : les trois étapes check-agents du job gates (par dossier `plugin/*/agents`, par
`plugin/*/AGENT.md` en `--file`, monde fermé `--resolve-agents=strict`) et l'étape « Gate C du lab
frais » du job lab-frais (`bash .claude/scripts/check-agents.sh --strict`). Repérer les étapes par
leur nom, pas par un numéro de ligne (la PR #85 a ajouté une étape check-blueprints au job gates).
Ne toucher à rien d'autre dans ces étapes ; l'étape check-blueprints n'est pas concernée.
2. Commentaire YAML dans la première étape : `--manifest-freshness=strict` est le seul endroit où un
manifeste périmé rend INDÉTERMINÉ (exit 3, étape rouge, D-04) ; le geste qui lève le rouge est de
rafraîchir `plugin/conductor/scripts/check-agents-manifest.json` (relire chaque source, comparer,
re-dater) ; chez l'utilisateur (hook, garde) un manifeste périmé reste un avertissement.
3. Rejouer localement les trois étapes du job gates avec leurs arguments exacts (commande de
vérification) et le Gate C d'un lab frais (fermeture de conductor installée depuis `plugin/`, puis
`bash .claude/scripts/check-agents.sh --strict --manifest-freshness=strict` dans le lab).
4. Commit : `ci: les étapes check-agents rendent INDÉTERMINÉ sur manifeste périmé (D-04)` avec le
trailer
`Gate-Touche: .github/workflows/ci.yml — option --manifest-freshness=strict aux quatre appels de check-agents (D-04)`.
  </action>
  <verify>
    <automated>fail=0; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --agents-dir="$d" >/dev/null 2>&1 || fail=$((fail+1)); done; for f in plugin/*/AGENT.md; do bash plugin/conductor/scripts/check-agents.sh --strict --manifest-freshness=strict --file "$f" >/dev/null 2>&1 || fail=$((fail+1)); done; reg=(); for d in plugin/*/agents; do reg+=("--agent-registry-dir=$d"); done; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --manifest-freshness=strict --agents-dir="$d" "${reg[@]}" >/dev/null 2>&1 || fail=$((fail+1)); done; echo "CI-REPLAY fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « CI-REPLAY fail=0 »</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); (cd "$LAB" && git init -q); bash "$R/plugin/_internal/resolve-deps.sh" conductor | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >/dev/null 2>&1); done; (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict --manifest-freshness=strict >/dev/null) && echo GATE-C-OK</automated>
    <fails_when>la ligne GATE-C-OK est absente (Gate C du lab frais non vert avec l'option)</fails_when>
    <automated>ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); puts "YAML-OK"'</automated>
    <fails_when>la sortie n'est pas « YAML-OK » (ci.yml invalide)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -E 'bash (plugin/conductor|\.claude)/scripts/check-agents\.sh' .github/workflows/ci.yml | grep -vc -- '--manifest-freshness=strict'` imprime `0`
    - `grep -E 'bash (plugin/conductor|\.claude)/scripts/check-agents\.sh' .github/workflows/ci.yml | grep -c -- '--manifest-freshness=strict'` imprime `4`
    - le rejeu local imprime `CI-REPLAY fail=0` et le Gate C local imprime `GATE-C-OK`
    - `git log -1 --format=%B -- .github/workflows/ci.yml | grep -c '^Gate-Touche: .github/workflows/ci.yml — '` rend 1
  </acceptance_criteria>
  <done>Les quatre appels CI de check-agents rendent INDÉTERMINÉ sur manifeste périmé ; ils sont verts aujourd'hui (manifeste frais) ; le commit porte son trailer Gate-Touche.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| horloge de la machine → verdict | la date du jour décide frais/périmé |
| CI du dépôt ↔ poste utilisateur | seule la CI peut rafraîchir le manifeste ; l'utilisateur le reçoit du plugin |
| gate → garde d'écriture | un « ✗ » fait refuser l'écriture d'un agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-11 | Denial of Service | garde d'écriture et hook SessionStart d'un lab installé depuis plus de valide_jours jours | high | mitigate | défaut lenient ; la ligne MANIFESTE-PERIME ne porte jamais « ✗ » ; les classes de D-05 rétrogradées ; T87 (hook) et T90 (garde) prouvent l'absence de refus |
| T-42-12 | Spoofing (faux vert) | verdict favorable rendu sur manifeste périmé en CI | medium | mitigate | rc=3 et jamais la ligne « ✓ agents conformes » sous l'option strict ; T84 ; MUT-F1 tué |
| T-42-13 | Tampering | verifie_le forgé loin dans le futur pour ne jamais expirer | medium | mitigate | une date postérieure à aujourd'hui est traitée comme périmée (DATE-FUTURE) ; T86 |
| T-42-14 | Tampering | valeur d'option mal orthographiée repliée en lenient | low | mitigate | rejet explicite rc=1 (T90, verify dédié) |
| T-42-15 | Repudiation / Tampering | retrait de l'option de ci.yml par une PR | low | transfer | rendu visible par G-2 (trailer Gate-Touche exigé sur ci.yml) ; ne verrouille rien (ADR-072) |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` : 0 KO ; T83 à T90 ; MUT-F1 et MUT-F2 tués.
- Rejeu des trois étapes CI avec l'option : `CI-REPLAY fail=0`.
- Gate C d'un lab frais avec l'option : `GATE-C-OK`.
- `bash plugin/conductor/scripts/tests/test-guard-agent-write.sh` et `bash scripts/tests/test-hook-exit-parc.sh` : rc 0.
</verification>

<success_criteria>
- FABR-02 : périmé → INDÉTERMINÉ en CI (option strict), avertissement chez l'utilisateur, jamais de refus sur liste fermée périmée.
- D-02 : validité lue dans le manifeste, bornes prouvées.
- D-05 : trois classes rétrogradées, les autres inchangées (T88).
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `perimees`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` | invariants locaux | 42-05 |
| `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur 4 juges ; `SendMessage` sur 4 managers | frontmatter du corpus | 42-02, 42-03 |
| versions de module : 5 modules en patch, conductor en mineure | versions | 42-02, 42-03, 42-06 |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-02 : `unclassified` / `unresolved`.** Non résolu d'office ; les
  bords rédigés ici (borne valide_jours / valide_jours + 1, date future, cible vide, --hook, garde,
  valeur d'option inconnue) sont des vérités du plan.
- **Péremption par liste** (42-RESEARCH A4) : le manifeste est périmé dès qu'UNE liste l'est — même
  résultat que « le plus ancien verifie_le », avec en prime le nom des listes en cause.
- **Issue sous option strict avec erreurs** : l'INDETERMINE (rc 3) prime sur un rc 1, lecture
  littérale de FABR-02 (« périmé rend INDÉTERMINÉ en CI ») ; les erreurs restent imprimées.
- **Horloge** : la péremption se lit sur la date locale du poste ou du runner ; un décalage d'un jour
  autour de la borne est accepté.
- **Conséquence à l'échéance, assumée** : la CI rougit à ses quatre appels et T20 de
  test-dev-orchestrator.sh (compte exact d'avertissements d'un AGENT.md) aussi — un seul geste les
  lève : rafraîchir le manifeste.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-04-SUMMARY.md` when done — commit séparé des commits `plugin/` et `.github/`.
</output>

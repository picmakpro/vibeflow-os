---
phase: 33-watchdog-notifications-des-missions
plan: 03
type: execute
wave: 2
depends_on: ["33-01", "33-04"]
files_modified:
  - plugin/conductor/scripts/check-guard-health.sh
  - plugin/conductor/scripts/tests/test-check-guard-health.sh
autonomous: true
requirements: [WTCH-02, QUAL-01]

must_haves:
  truths:
    - "check-guard-health.sh (déjà câblé sur `SessionStart`, générique tout le parc) gagne un second sous-contrôle : il lit `driver-lock.sh status` (sibling résolu par répertoire de script, même motif que 33-02) et distingue TROIS verdicts (D-33-A) : sain (les deux horloges fraîches, ou aucun lock présent), STALL (heartbeat frais, `progress_age_seconds` au-delà du seuil), ABANDON (`stale: true`, heartbeat mort) — chacun avec son cas de test nommé (critère de succès n°2 littéral)."
    - "D-33-E partie (a) (décision Samuel, 2026-08-17, datée) — `STALL_WINDOW` par défaut vaut **900s**, STRICTEMENT SOUS `VF_DRIVER_TTL` (défaut 1800s, `driver-lock.sh:40`) — jamais égal, jamais supérieur. Motif mesuré : `STALL_WINDOW` valait 1800 = exactement le TTL dans une version antérieure de ce plan ; combiné au fait que le heartbeat était émis au même tour que le progrès (avant amendement de `mission-flow.md`, plan 33-02), un lock devenait `stale` (verdict ABANDON) AU MÊME INSTANT où `progress_age_seconds` aurait dépassé le seuil — le verdict STALL n'était donc JAMAIS atteignable en production, seulement sur epochs forgés en test. Le commentaire en tête de la constante DOIT lier explicitement les deux valeurs et expliquer pourquoi l'inégalité stricte doit être maintenue à chaque future révision de l'une ou l'autre."
    - "Le sous-contrôle `check_driver_stall()` s'exécute AVANT toute décision de sortie liée à l'existence/lisibilité de `HEALTH_DIR` (pas seulement dans le bloc de verdict final) — sur une machine SAINE (cas majoritaire, y compris celle-ci : `~/.cache/vibeflow/guard-health` absent), le script sortait AVANT en `hook_exit 3` (répertoire absent, L101-104), `hook_exit 4` (pas un répertoire, L106), ou `hook_exit 4` (non listable, L113-114) — le sous-contrôle ne tournait alors JAMAIS, rendant WTCH-02 mort en production tout en restant VERT en test (les fixtures font `mkdir -p`, donc les cas D-* atteignaient artificiellement la ligne du verdict final). C'est un garde aveugle avec tous les cas verts — exactement ce que QUAL-01 interdit. Un cas dédié (D23) prouve qu'un stall est signalé MÊME quand `HEALTH_DIR` est absent, SANS que le sous-contrôle ne le crée lui-même."
    - "AUCUNE nouvelle entrée `hooks.json` : ce plan étend un script DÉJÀ armé sur `SessionStart` — c'est le chemin explicitement privilégié par D-33-CONTEXT (contrainte transverse merge-hooks) pour ne jamais risquer le bug d'idempotence cross-matcher."
    - "QUATRE issues (QUAL-01), pas trois : (a) PASS — sain ; (b) FAIL — stall/abandon détecté, signalé ; (c) imparsable (JSON de `status` inattendu, subprocess rc=0) → fail-open SILENCIEUX, aucun rapport, jamais un crash ; (d) dépendance indisponible (`driver-lock.sh` absent/non-exécutable, aucun interpréteur Python utilisable, OU `status` rend un code non nul) → fail-open BRUYANT — jamais un vert. Le seuil est configurable via `--stall-window=<sec>` ET via la variable d'environnement `VF_STALL_WINDOW` (le flag l'emporte s'il est fourni) — sans l'override par variable, le seuil serait INATTEIGNABLE en production : l'entrée `hooks.json` invoque ce script avec `--hook` seul, jamais `--stall-window=`."
    - "Le sous-contrôle NE SOURCE PAS `plugin/_internal/lib/vf-portable.sh` ni ne copie son bloc localisateur canonique — il reproduit LOCALEMENT les trois actions de `vf_guard_unavailable` (marqueur atomique + stderr + retour) sur le MÊME `HEALTH_DIR` déjà résolu par ce script, ET reproduit LOCALEMENT (jamais en sourçant la lib) la cascade `python3 → python → py -3` de `vf_resolve_python`, rejet du stub Microsoft Store (`*WindowsApps*`) inclus — perdre cette cascade rouvrirait la régression Windows fermée en Phase 30, cette fois sur un script de hook du parc entier. CE script N'EST PAS un nouveau consommateur du bloc canonique — CORRECTION post-33-04 (T12 relu sur le plan final, pas sur une hypothèse) : `notify.sh` n'en devient PAS non plus le 6e — 33-04 a tranché une résolution TOLÉRANTE propre à `notify.sh`, hors du périmètre de T12 (son `<deviation>`, motif : le bloc canonique `exit 1` si la lib est introuvable, incompatible avec le fail-open silencieux exigé). T12 reste donc à 5 consommateurs, inchangé par TOUTE la Phase 33 — ni ce plan ni 33-04 n'y ajoutent d'entrée."
    - "ADR-031 : ce script SIGNALE, ne tue et ne corrige jamais — écriture STRICTEMENT limitée à l'unique `mv -f` atomique du marqueur de dépendance indisponible (patron `vf_guard_unavailable`) : `grep -cE '\\b(rm|touch)[[:space:]]' check-guard-health.sh` == 0, `grep -cE '\\bmv[[:space:]]' check-guard-health.sh` == exactement 1 (l'écriture atomique elle-même, jamais un élagage) — critère ciblé et EXÉCUTABLE, pas une exclusion « à l'œil » qu'un simple `grep -c` ne sait pas exprimer. L'en-tête du script (« ce script n'écrit RIEN, nulle part », L110 avant ce plan) est amendé en conséquence : lecture seule sur `HEALTH_DIR` SAUF l'unique marqueur `check-guard-health.sh.marker` que le script écrit sur lui-même quand SA PROPRE dépendance (driver-lock.sh, interpréteur Python) est indisponible — jamais un `rm`/`touch`, jamais un élagage des marqueurs des AUTRES gardes."
  artifacts:
    - "plugin/conductor/scripts/check-guard-health.sh : fonction de sous-contrôle stall/abandon exécutée AVANT les gates d'existence de HEALTH_DIR, flag `--stall-window=<sec>` + variable `VF_STALL_WINDOW` (défaut 900), verdict combiné avec le scan de marqueurs existant"
    - "plugin/conductor/scripts/tests/test-check-guard-health.sh : cas D14 à D24"
  key_links:
    - "progress_epoch/progress_age_seconds (plan 33-01) -> ce sous-contrôle : seule source de la distinction stall/abandon, jamais un second calcul indépendant"
    - "ce sous-contrôle -> `dag.sh check_stall_signal()` (plan 33-02) : 33-02 invoque `check-guard-health.sh --hook` en sous-processus et relaie sa sortie SANS la réinterpréter — le contrat `--hook` (une ligne si signal, stdout STRICTEMENT VIDE sinon) doit rester stable, un changement de forme ici casserait silencieusement le relais de 33-02 (dégradation en no-op côté 33-02, jamais un crash)."
    - "ce sous-contrôle -> `notify.sh` (plan 33-05, câblage des jalons) : ce plan ne notifie PAS lui-même sur l'OS — il signale via le canal existant (stdout `--hook` + marqueurs). Le câblage `notify.sh` aux jalons de mission est un plan SÉPARÉ (33-05), ne pas le préempter ici."
  prohibitions:
    - "Ne JAMAIS ajouter d'entrée à `plugin/conductor/hooks/hooks.json` dans ce plan — le script est DÉJÀ armé, une entrée supplémentaire sur le même script/événement déclencherait le bug d'idempotence cross-matcher (CONCERNS.md, HIGH, NON CORRIGÉ)"
    - "Ne JAMAIS décider un seuil de stall en dur sans le rendre configurable (`--stall-window=` ET `VF_STALL_WINDOW`) — un seuil figé serait une régression silencieuse le jour où D-33-A serait révisée, ET serait inatteignable en production sans l'override par variable"
    - "Ne JAMAIS poser `STALL_WINDOW` égal ou supérieur à `VF_DRIVER_TTL` — c'est précisément ce qui rendait le verdict STALL inatteignable en production ; tout changement futur de l'une des deux constantes doit re-vérifier l'inégalité stricte"
    - "Ne JAMAIS traiter un lock ANTÉRIEUR à 33-01 (`progress_epoch` absent, rétrocompat) comme un stall — c'est un état légitime et SAIN, pas une panne"
    - "Ne JAMAIS faire de `rm`/`touch` sur `HEALTH_DIR`, ni de `mv` autre que l'unique écriture atomique du marqueur de CE script sur LUI-MÊME — lecture des marqueurs des AUTRES gardes strictement additive, jamais d'élagage"
    - "Ne JAMAIS faire dépendre le sous-contrôle stall de l'existence de `HEALTH_DIR` — c'est le bug bloquant reproduit par le plancheck externe (garde aveugle vert en test, mort en production)"
    - "Aucun `sleep` non borné dans les tests — fixtures via manipulation directe de `driver-lock.sh` meta ou du répertoire de santé, jamais une attente réelle"
    - "Ne JAMAIS committer les untracked `.gsd/`, `MISSION-3x.dag.json`, dossiers `VFDO-31`/`VFDO-36` — hors périmètre de ce plan"
---

<objective>
Étendre `check-guard-health.sh` — déjà câblé sur `SessionStart`, déjà générique tout le parc — d'un
second sous-contrôle qui lit le battement posé en 33-01 et distingue sain/stall/abandon, EXÉCUTÉ
AVANT toute décision de sortie liée à l'existence de `HEALTH_DIR` (le bug bloquant qui le rendait
inatteignable sur une machine saine), sans ajouter aucune nouvelle entrée de hook. Le seuil de stall
(D-33-E) est fixé à 900s, strictement sous le TTL du lock (1800s), et configurable en production via
`VF_STALL_WINDOW`. Le signal reste BRUYANT sur toute dépendance indisponible (cascade Python
reproduite localement, jamais un `command -v python3` nu qui perdrait le rejet du stub Microsoft
Store), SILENCIEUX sur un JSON imparsable, et jamais un vert de complaisance sur les deux.

Purpose : sans ce plan, `progress_epoch` (33-01) et son câblage (33-02) restent des données mortes —
personne ne les lit jamais. C'est le composant qui rend WTCH-02 VRAI : « un stall ne survit pas au
prochain geste d'une session VF vivante » (`SessionStart` ICI, `dag.sh mark` via 33-02) se vérifie
précisément par la logique de verdict que ce plan écrit — 33-02 la CONSOMME, ne la duplique jamais.

Output : `check-guard-health.sh` étendu d'un sous-contrôle exécuté tôt, d'un flag et d'une variable
d'environnement, `test-check-guard-health.sh` étendu de 11 cas (D14-D24) couvrant les trois verdicts,
les quatre issues QUAL-01, et le bug d'ordonnancement corrigé.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-02-PLAN.md
</context>

<!-- Discipline de commentaire : deux critères d'acceptation négative-greppent des littéraux qui
     apparaissent par ailleurs, légitimement, dans l'action et les must_haves de ce plan — parce que
     l'action décrit explicitement ce que le script NE doit PAS faire (les nommer est indispensable
     à la lecture littérale du choix architectural, pas un oubli à corriger) :
     - `vf-portable` : le critère d'absence de `source ... vf-portable` prouve qu'aucun bloc
       localisateur canonique n'a été sourcé — mais l'action et les must_haves DOIVENT nommer
       `vf-portable.sh` pour expliquer pourquoi ce script le réplique localement plutôt que de le
       sourcer. Le critère lui-même ne grepp plus le nom nu (corrigé : le fichier réel en porte 3
       occurrences légitimes en commentaire, un `grep -c 'vf-portable' == 0` serait rouge sur du
       code sain) — il grepp la forme `source .../vf-portable` ou `. .../vf-portable`, absente.
     - `eval` : le critère `grep -c 'eval' ... == 0` prouve l'absence réelle d'un `eval` dans le
       script — mais l'action nomme explicitement « JAMAIS un eval » comme contrainte de lecture du
       JSON, ce qui échoue le grep naïf du gate sans qu'aucun `eval` réel n'existe.
     <!-- planner-discipline-allow: vf-portable -->
     <!-- planner-discipline-allow: eval -->
-->

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : sous-contrôle stall/abandon exécuté TÔT, quatre issues, seuil 900s configurable, aucune nouvelle entrée de hook</name>
  <files>plugin/conductor/scripts/check-guard-health.sh, plugin/conductor/scripts/tests/test-check-guard-health.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-guard-health.sh:57-116 (parseur d'arguments, dérivation `HEALTH_DIR`, `hook_exit()`, `indetermine()` — patrons à réutiliser tels quels ; L101-115 : LES TROIS points de sortie précoce que ce plan doit désormais faire passer PAR le sous-contrôle stall AVANT de décider)
    - plugin/conductor/scripts/check-guard-health.sh:175-184 (verdict final actuel — bloc à étendre pour combiner marqueurs + stall APRÈS le déplacement du sous-contrôle en amont)
    - plugin/_internal/lib/vf-portable.sh:50-103 (`vf_py_probe`/`vf_resolve_python` — cascade `python3 → python → py -3`, rejet WindowsApps par CHEMIN via `case "$(command -v "$bin")" in *WindowsApps*) return 1 ;; esac` — les DEUX actions à reproduire LOCALEMENT, jamais en sourçant)
    - plugin/_internal/lib/vf-portable.sh:145-158 (`vf_guard_unavailable` — les TROIS actions à reproduire localement : marqueur atomique tmp+mv, stderr préfixé, retour non nul — format EXACT du marqueur : `printf '%s\t%s\t%s\n' "$ts" "$script" "$motif"`, `ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"`)
    - plugin/conductor/scripts/driver-lock.sh:33-40 (`VF_DRIVER_TTL`, défaut 1800 — la constante que `STALL_WINDOW` doit rester STRICTEMENT sous)
    - plugin/conductor/scripts/driver-lock.sh (plan 33-01) — forme JSON de `status` : `present`, `stale`, `owner`, `step`, `age_seconds`, `progress_epoch`, `progress_age_seconds`
    - plugin/conductor/scripts/dag.sh (plan 33-02) — patron de résolution de sibling par répertoire de script (`SCRIPT_DIR`), à répliquer ici pour `driver-lock.sh`
    - plugin/conductor/scripts/tests/test-check-guard-health.sh (convention `ok()`/`ko()`, préflight anti-fixture-réelle, `write_marker()`, cas D13 déjà en place référençant `DRIVER_LOCK`/`GUARD_LOCK`, D13 §196-203 : patron de PATH restreint sans python3/python pour simuler l'absence d'interprète)
    - plugin/conductor/scripts/tests/test-driver-lock.sh:51-94 (`age_stale()`/`meta_drop_key()`/`lease_backdate()` — patron PORTABLE de forgeage direct du `meta` via `sed -i.bak "..." "$meta" && rm -f "${meta}.bak"`, JAMAIS `sed -i` nu qui est GNU-only et casse sur macOS — le helper de forgeage de `progress_epoch` pour D16/D17/D23/D24 DOIT citer et suivre ce patron à l'identique, pas en réinventer un)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md §5 (couplage critique HEALTH_DIR écrivain/lecteur)
  </read_first>
  <behavior>
    - D14 : aucun lock présent (`VF_DRIVER_LOCK` pointe un chemin jamais acquis) → verdict global SAIN (code 3, ou 0 traduit sous `--hook`), aucune ligne de stall.
    - D15 : lock acquis, `heartbeat_epoch` et `progress_epoch` tous deux frais → SAIN, aucune ligne de stall.
    - D16 : lock acquis, `heartbeat` rafraîchi (frais), `progress_epoch` antidaté au-delà du seuil (900s par défaut — forgé via helper de forgeage direct sur le `meta` du lock — jamais un `sleep`) → SIGNAL (code 0), une ligne mentionnant explicitement le mot « stall » et l'`owner`/`step` du lock.
    - D17 : lock acquis, `heartbeat_epoch` antidaté au-delà du TTL (`stale: true`) → SIGNAL, une ligne distincte mentionnant explicitement l'« abandon »/le lock périmé, jamais confondue avec la ligne de stall (D16).
    - D18 : lock acquis par l'ANCIEN protocole (meta sans ligne `progress_epoch=`, `heartbeat_epoch` frais) → SAIN, aucune ligne de stall (rétrocompat, jamais un faux positif). Contrôle positif obligatoire : avant d'invoquer le script sous test, lire directement le `meta` et vérifier que la ligne `progress_epoch=` est bien ABSENTE — consigné dans le SUMMARY.
    - D19 : sibling `driver-lock.sh` ABSENT (copie isolée de `check-guard-health.sh` dans un répertoire sans `driver-lock.sh`, patron T33/T36-38 de `dag.sh`) → verdict global INDÉTERMINÉ (code 4, ou 0 sous `--hook`, JAMAIS 3/SAIN), ET un marqueur est écrit dans `HEALTH_DIR` (vérifié par lecture directe du fichier après l'appel), ET un message apparaît sur stderr — MÊME QUAND `HEALTH_DIR` N'EXISTAIT PAS AVANT L'APPEL (preuve que le sous-contrôle tourne bien AVANT le court-circuit historique « répertoire absent → SAIN »).
    - D20 : sibling `driver-lock.sh` présent et exécutable mais AUCUN interprète Python utilisable (PATH restreint SANS python3/python/py, patron D13/`test-guard-driver-lock.sh` Q4) → même verdict que D19 : INDÉTERMINÉ + marqueur + stderr — preuve que la cascade locale (pas `vf_resolve_python`, reproduite) est bien exercée sur les TROIS candidats, pas seulement `python3`.
    - D21 : sibling `driver-lock.sh` ISOLÉ qui rend un JSON structurellement inattendu (`status` sort 0 mais un texte non-JSON) → fail-open SILENCIEUX : verdict global SAIN si rien d'autre ne signale (pas de marqueur écrit, pas de ligne stall, pas d'INDÉTERMINÉ).
    - D22 : signal combiné — un marqueur de garde existant (patron D13/`write_marker`) ET un stall détecté simultanément → DEUX lignes distinctes sur stdout (une par famille de signal), jamais une fusionnée qui perdrait l'une des deux informations.
    - D23 : **cas bloquant corrigé** — lock acquis avec `progress_epoch` antidaté au-delà du seuil (stall réel) ET `HEALTH_DIR` ABSENT (jamais créé avant l'appel) → verdict SIGNAL (jamais 3/SAIN, la panne d'ordonnancement historique aurait rendu ce cas invisible), ET `HEALTH_DIR` reste ABSENT APRÈS l'appel (vérifié par `[ ! -e "$HEALTH_DIR" ]`) — la détection de stall pur ne crée JAMAIS le répertoire de santé, seule la voie INDÉTERMINÉE (D19/D20) le fait.
    - D24 : `--stall-window=` absent, `VF_STALL_WINDOW=60` exporté, `progress_epoch` antidaté de 90s (au-delà de 60, en-deçà du défaut 900) → SIGNAL (preuve que la variable d'environnement est bien lue) ; puis même fixture avec `VF_STALL_WINDOW=60` ET `--stall-window=3600` → SAIN (preuve que le flag explicite l'emporte sur la variable).
  </behavior>
  <action>
1. En tête de `check-guard-health.sh`, après la résolution de `HEALTH_DIR`, ajouter :
   ```
   SCRIPT_DIR_SELF="$(cd "$(dirname "$0")" && pwd 2>/dev/null || dirname "$0")"
   DRIVER_LOCK_SH="$SCRIPT_DIR_SELF/driver-lock.sh"
   # D-33-E (2026-08-17) : STALL_WINDOW doit rester STRICTEMENT SOUS VF_DRIVER_TTL
   # (driver-lock.sh:40, defaut 1800s) — sinon un lock devient `stale` (verdict ABANDON) avant
   # meme que progress_age_seconds ne depasse ce seuil, et le verdict STALL redevient
   # inatteignable en production (regression mesuree : les deux valaient 1800 dans une version
   # anterieure). Toute revision future de l'une ou l'autre constante doit re-verifier
   # l'inegalite stricte.
   STALL_WINDOW="${VF_STALL_WINDOW:-900}"
   ```
   et ajouter au parseur d'arguments `--stall-window=*) STALL_WINDOW="${arg#*=}" ;;` (APRÈS la
   lecture de `VF_STALL_WINDOW` — le flag explicite l'emporte toujours) avec la même garde de
   numéricité que `WINDOW` (usage 64 sinon).

2. Ajouter `report_self_unavailable() { local motif="$1"; ...}` juste après `hook_exit()` : reproduit
   LOCALEMENT les trois actions de `vf_guard_unavailable` (écriture atomique tmp+mv du marqueur
   `$HEALTH_DIR/check-guard-health.sh.marker`, `echo "[check-guard-health] $motif" >&2`, retour non
   nul) SANS sourcer `vf-portable.sh`. `mkdir -p "$HEALTH_DIR"` avant l'écriture (seul cas où ce
   script crée le répertoire — jamais pour un stall pur, D23). Commenter explicitement le motif de
   cette duplication (déjà justifié en tête de fichier pour `HEALTH_DIR`) et la conséquence : ce
   script n'est PAS un consommateur du bloc localisateur canonique. Correction post-33-04 : `notify.sh`
   non plus — 33-04 a tranché une résolution tolérante propre, hors périmètre de T12 (voir sa
   section `<deviation>`). T12 reste à 5 consommateurs, inchangé par toute la Phase 33.

3. Ajouter `py_resolve_local()` : reproduit LOCALEMENT (jamais en sourçant `vf-portable.sh`) la
   cascade `python3 → python → py -3` de `vf_resolve_python` — présence (`command -v`) ET rejet du
   stub Microsoft Store par chemin (`case "$(command -v "$bin")" in *WindowsApps*) continue ;; esac`)
   pour CHAQUE candidat de la cascade, dans l'ordre. Imprime le token d'invocation retenu
   (`python3`, `python`, ou `py -3`) sur stdout et rend 0 si un candidat passe, rend 1 sans rien
   imprimer sinon — profil PRÉSENCE SEULE (pas de sonde d'exécution réelle : ce sous-contrôle tourne
   à `SessionStart`, une latence de spawn Python supplémentaire par candidat serait superflue ici,
   contrairement au profil complet de `vf_resolve_python` utilisé ailleurs pour une résolution
   one-shot plus critique).

4. Ajouter `check_driver_stall()` qui :
   - initialise `STALL_INDETERMINATE=0` et `STALL_LINE=""` — **AVANT tout usage**, sous `set -u` (bug
     bloquant mesuré : ces variables n'étaient pas initialisées, causant `unbound variable` sur le
     chemin sain d'une version antérieure de ce plan) ;
   - retourne silencieusement si `DRIVER_LOCK_SH` n'est pas un fichier exécutable → appelle
     `report_self_unavailable "driver-lock.sh introuvable ou non exécutable ($DRIVER_LOCK_SH)"` et
     positionne `STALL_INDETERMINATE=1` ;
   - retourne de même si `PY_INVOKE="$(py_resolve_local)"` échoue (aucun candidat) → même
     `report_self_unavailable` avec un motif dédié, `STALL_INDETERMINATE=1` ;
   - exécute `"$DRIVER_LOCK_SH" status` (capturé, `2>/dev/null`) ; si le code de retour n'est pas 0
     → `report_self_unavailable "driver-lock.sh status a rendu un code non nul"` + `STALL_INDETERMINATE=1` ;
   - parse le JSON via `STATUS_JSON="$out" $PY_INVOKE <<'PYEOF' ... PYEOF` (jamais un `-c` avec le
     contenu interpolé dans la chaîne de commande — le JSON passe par variable d'environnement,
     jamais par argv ni par concaténation ; `$PY_INVOKE` non quoté volontairement pour porter
     `py -3`, même patron que `vf_python`) : si le parse échoue ou que la forme est inattendue,
     imprimer RIEN et sortir non nul → capturé côté bash, traité en fail-open SILENCIEUX (aucune
     variable positionnée, `STALL_INDETERMINATE` reste à 0) ;
   - sinon, lit les champs `present`/`stale`/`owner`/`step`/`progress_age_seconds` réémis en lignes
     `clé=valeur` par le script python, les récupère côté bash via une boucle `while IFS='='
     read -r k v` (JAMAIS un `eval`) ;
   - si `present` != `true` → rien à signaler ;
   - si `stale` == `true` → `STALL_LINE="[mission-watchdog] abandon detecte — owner=$d_owner
     step=$d_step (heartbeat mort, lock perime) — ne JAMAIS tuer (ADR-031), takeover disponible si
     legitime"` ;
   - sinon si `progress_age_seconds` est numérique ET `> STALL_WINDOW` → `STALL_LINE="[mission-watchdog]
     stall detecte — owner=$d_owner step=$d_step (progres fige depuis ${progress_age_seconds}s,
     heartbeat frais, seuil=${STALL_WINDOW}s) — verifier la session en cours, ne JAMAIS tuer (ADR-031)"` ;
   - sinon (y compris `progress_age_seconds` absent/`null`, cas rétrocompat 33-01) → rien à signaler.

5. **Correction de l'ordonnancement (bug bloquant, reproduit)** : appeler `check_driver_stall`
   IMMÉDIATEMENT après la résolution des arguments/`STALL_WINDOW` (étape 1), AVANT les trois points
   de sortie précoce actuels (répertoire absent L101-104, pas un répertoire L106, non listable
   L113-114). Remplacer ces trois sorties directes par une logique qui consulte D'ABORD
   `STALL_INDETERMINATE`/`STALL_LINE` :
   - répertoire absent (état vérifié, pas une indétermination du répertoire lui-même) : si
     `STALL_INDETERMINATE` == 1 → `indetermine "sous-controle driver-lock indisponible"` ; sinon si
     `STALL_LINE` non vide → imprimer `STALL_LINE` et `hook_exit 0` (SANS créer `HEALTH_DIR` — D23) ;
     sinon → `hook_exit 3` (SAIN, comportement inchangé) ;
   - pas un répertoire / non listable (déjà une INDÉTERMINATION propre au répertoire de marqueurs) :
     `indetermine` comme avant — le fait que le sous-contrôle stall ait par ailleurs quelque chose à
     dire ne change rien à ce verdict, il reste INDÉTERMINÉ (l'un ou l'autre suffit).
   Le reste du script (scan des marqueurs, `FRESH_COUNT`) continue de s'exécuter normalement quand
   `HEALTH_DIR` existe et est listable.

6. Dans le bloc de verdict final (actuellement lignes 175-184, une fois `FRESH_COUNT` connu) : la
   combinaison finale devient — si `STALL_INDETERMINATE` vaut 1 (n'a pas déjà été traité à l'étape 5,
   ex. répertoire présent et listable mais dépendance indisponible) → `indetermine "sous-controle
   driver-lock indisponible (deja signale sur stderr / marqueur ecrit ci-dessus)"`. Sinon, si
   `FRESH_COUNT` == 0 ET `STALL_LINE` vide → SAIN (`hook_exit 3`, comme avant). Sinon, construire la
   sortie en accumulant JUSQU'À DEUX lignes (celle des marqueurs existants SI `FRESH_COUNT` > 0,
   celle de `STALL_LINE` si non vide), les imprimer chacune sur SA PROPRE ligne, puis `hook_exit 0`.

7. Mettre à jour le bloc de commentaire d'en-tête pour documenter le sous-contrôle (exécuté TÔT,
   avant les gates d'existence de `HEALTH_DIR`), le flag `--stall-window=`/la variable
   `VF_STALL_WINDOW`, l'inégalité stricte avec `VF_DRIVER_TTL`, et la règle « une ligne PAR FAMILLE
   de signal (marqueurs de garde, stall de mission), jamais une par marqueur individuel au sein d'une
   famille » (amendement explicite du commentaire D6 existant, qui ne parlait que des marqueurs).
   Amender aussi l'invariant « ce script n'écrit RIEN, nulle part » (L110 avant ce plan) : lecture
   seule STRICTE sur les marqueurs des AUTRES gardes, SAUF l'unique écriture atomique du propre
   marqueur d'indisponibilité de ce script (voir must_haves).

Étendre ensuite `test-check-guard-health.sh` avec les cas D14 à D24 décrits en `<behavior>`, à la
convention exacte du dossier (`ok()`/`ko()`, préflight anti-fixture-réelle déjà en place, isolation
`--dir=` systématique). Pour D19/D20, réutiliser le patron d'isolation par copie de script (comme
T33/T36-38 de `test-dag.sh`) : copier `check-guard-health.sh` dans un répertoire temporaire ISOLÉ,
jamais de modification destructive du vrai fichier ni du vrai `PATH` de la suite entière. Pour D20,
réutiliser le patron D13 déjà en place (PATH restreint à un dossier de symlinks minimal, contrôle
positif que `python3`/`python` n'y sont PAS joignables avant d'invoquer le script sous test).
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'check_driver_stall\(\)' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'report_self_unavailable\(\)' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'py_resolve_local\(\)' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - Le script NE SOURCE PAS vf-portable.sh : `grep -cE '^[[:space:]]*(\.|source)[[:space:]].*vf-portable'
      plugin/conductor/scripts/check-guard-health.sh` rend `0` — preuve d'ABSENCE de `source`, pas
      absence du nom (le nom apparaît légitimement 3 fois en commentaire, mesuré sur le fichier
      actuel : ne pas exiger `0` occurrence du mot, ce serait rouge sur la documentation du couplage
      critique HEALTH_DIR que 33-TERRAIN.md déclare intouchable).
    - `grep -c 'eval' plugin/conductor/scripts/check-guard-health.sh` rend `0`.
    - `grep -qE -- '--stall-window' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'VF_STALL_WINDOW' plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - `grep -qE 'VF_DRIVER_TTL' plugin/conductor/scripts/check-guard-health.sh` sort 0 (commentaire
      liant explicitement les deux constantes).
    - Écriture strictement bornée sur `HEALTH_DIR` : `grep -cE '\b(rm|touch)[[:space:]]'
      plugin/conductor/scripts/check-guard-health.sh` rend `0` ; `grep -cE '\bmv[[:space:]]'
      plugin/conductor/scripts/check-guard-health.sh` rend EXACTEMENT `1` (l'unique écriture
      atomique du marqueur `check-guard-health.sh.marker` par `report_self_unavailable`, jamais un
      élagage des marqueurs des autres gardes).
    - `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` sort 0, affiche les blocs
      `=== D14` à `=== D24`, et sa ligne de bilan affiche `0 FAIL`.
    - Les cas D1-D13 historiques restent verts (comptage PASS avant/après consigné dans le SUMMARY,
      MESURÉ sur le disque, pas recopié).
    - Non-régression du reste de la suite driver-lock : `bash
      plugin/conductor/scripts/tests/test-driver-lock.sh` sort 0 (aucune modification de 33-01,
      vérification croisée).
    - T12 de `test-vf-portable.sh` n'a PAS été modifié par ce plan : `git diff --stat
      plugin/_internal/tests/test-vf-portable.sh` (avant commit) rend une sortie VIDE.
    - **Mutation rouge n°1, trace obligatoire dans le SUMMARY** : commiter d'abord, puis dans
      `check_driver_stall()`, remplacer la comparaison `stale == "true"` par un test toujours faux →
      D17 doit rougir (abandon non détecté) ; consigner assertion/attendu/obtenu ; restaurer par
      `git checkout -- plugin/conductor/scripts/check-guard-health.sh`.
    - **Mutation rouge n°2** : retirer l'appel à `report_self_unavailable`/`STALL_INDETERMINATE=1` du
      chemin « driver-lock.sh absent » → D19 doit rougir (verdict retombe en SAIN au lieu
      d'INDÉTERMINÉ — c'est exactement le vert de complaisance que QUAL-01 interdit) ; consigner de
      même ; restaurer.
    - **Mutation rouge n°3 (nouvelle, corrige le bug d'ordonnancement)** : remettre temporairement les
      trois sorties précoces (répertoire absent/pas un répertoire/non listable) EN AMONT de l'appel à
      `check_driver_stall` (l'ordre bloquant reproduit par le plancheck) → D23 doit rougir (le stall
      n'est plus signalé quand `HEALTH_DIR` est absent) ; consigner assertion/attendu/obtenu ;
      restaurer.
    - **Contrôle positif du fixture D18** : avant d'invoquer le script sous test, vérifier
      explicitement par lecture directe du `meta` que la ligne `progress_epoch=` est bien ABSENTE —
      consigner ce contrôle dans le SUMMARY (même exigence que les fixtures de rétrocompat en 33-01).
    - **Contrôle positif du fixture D20** : avant d'invoquer la copie isolée, vérifier explicitement
      qu'AUCUN de `python3`/`python`/`py` n'est joignable dans le PATH restreint fourni (`command -v`
      échoue pour les trois) — consigné dans le SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/check-guard-health.sh && bash plugin/conductor/scripts/tests/test-check-guard-health.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && [ "$(grep -cE '^[[:space:]]*(\.|source)[[:space:]].*vf-portable' plugin/conductor/scripts/check-guard-health.sh)" -eq 0 ] && [ "$(grep -c 'eval' plugin/conductor/scripts/check-guard-health.sh)" -eq 0 ] && [ "$(grep -cE '\b(rm|touch)[[:space:]]' plugin/conductor/scripts/check-guard-health.sh)" -eq 0 ] && [ "$(grep -cE '\bmv[[:space:]]' plugin/conductor/scripts/check-guard-health.sh)" -eq 1 ]</automated>
  </verify>
  <done>`check-guard-health.sh` distingue sain/stall/abandon à chaque `SessionStart` (ET au prochain `dag.sh mark` d'une session vivante via 33-02), le sous-contrôle tourne AVANT les gates d'existence de `HEALTH_DIR` (jamais un garde aveugle vert en test/mort en production), le seuil (900s, strictement sous le TTL de 1800s) est configurable par flag et variable d'environnement, sans nouvelle entrée de hook, avec les quatre issues QUAL-01 chacune prouvées par cas de test, et les trois mutations qui casseraient la détection, réintroduiraient un vert de complaisance, ou reproduiraient le bug d'ordonnancement rougissent avec leurs traces.</done>
  <reversibility rating="reversible">Extension additive d'un script déjà lu-seul sur son domaine existant (à l'exception de l'unique marqueur d'auto-indisponibilité, déjà un pattern établi ailleurs dans le dépôt) : un `git revert` rend le fichier identique à HEAD, aucun format de marqueur existant n'est changé.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `driver-lock.sh status` (sous-processus) -> `check_driver_stall()` | sortie traitée comme entrée NON fiable (JSON potentiellement absent/illisible/inattendu) — jamais un `eval`, jamais une interpolation directe dans une commande |
| dépendance manquante (`driver-lock.sh`, interprète Python) -> verdict de session | une dépendance absente NE DOIT JAMAIS se traduire par un vert — c'est le cœur de QUAL-01 |
| ordonnancement du sous-contrôle vs gates d'existence de `HEALTH_DIR` | un sous-contrôle placé APRÈS des sorties précoces ne tourne JAMAIS sur le cas majoritaire (machine saine, répertoire absent) — c'est un garde aveugle qui reste vert en test à cause des fixtures `mkdir -p` ; corrigé en plaçant le sous-contrôle EN AMONT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Information Disclosure | verdict final | low | accept | JSON local d'un lock déjà lisible par tout process du même utilisateur ; rien de nouveau n'est exposé |
| T-33-09 | Denial of Service (vert de complaisance) | `check_driver_stall()` / ordonnancement | high | mitigate | toute dépendance indisponible force `STALL_INDETERMINATE=1` -> `hook_exit 4`, jamais 3, ET le sous-contrôle tourne AVANT les gates d'existence de `HEALTH_DIR` (jamais contourné par le cas majoritaire) ; cas D19/D20/D23 + mutations rouges n°2 et n°3 |
| T-33-10 | Tampering | parsing JSON | medium | mitigate | passage par variable d'environnement, jamais par argv/concaténation ; `eval` absent (grep) ; un JSON structurellement inattendu dégrade en silence plutôt qu'en exécution de code |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/check-guard-health.sh`
2. `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` — 0 FAIL, cas D14 à D24 présents, D1-D13 intacts
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé
4. Les trois mutations exigées ont rougi et leurs traces sont dans le SUMMARY
5. `test-vf-portable.sh` n'est pas modifié par ce plan (T12 reste à 5 consommateurs)
6. Non-régression du parc COMPLET des suites : `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` rend `64`, INCHANGÉ — ce plan étend `test-check-guard-health.sh` (fichier déjà découvert), il n'en ajoute ni n'en retire aucun. Le passage exhaustif des 64 suites reste la responsabilité de CI (`ci.yml` L213) ; ce plan ne le rejoue pas intégralement en local.
</verification>

<success_criteria>
- Trois verdicts (sain/stall/abandon) sont distingués à la lecture de `driver-lock.sh status`, chacun avec son cas de test nommé, et le sous-contrôle tourne AVANT toute sortie précoce liée à `HEALTH_DIR` — jamais un garde aveugle.
- Le seuil de stall (900s) est strictement sous le TTL du lock (1800s), configurable par `--stall-window=` et `VF_STALL_WINDOW`.
- Les quatre issues QUAL-01 sont chacune prouvées : sain, signal, imparsable silencieux, dépendance indisponible bruyant (cascade Python locale complète, rejet WindowsApps inclus).
- Aucune nouvelle entrée `hooks.json` ; `check-guard-health.sh` reste le seul point de lecture. T12 (`test-vf-portable.sh`) reste à 5 consommateurs, inchangé par toute la Phase 33 — ni ce plan ni `notify.sh` (33-04, qui a tranché une résolution tolérante hors périmètre de T12) n'en deviennent le 6e.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-03-SUMMARY.md` en fin de plan,
en y consignant les trois traces de mutation exigées et le comptage avant/après des assertions.
</output>

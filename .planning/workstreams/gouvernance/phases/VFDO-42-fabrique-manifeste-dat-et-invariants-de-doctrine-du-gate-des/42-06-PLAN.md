---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 06
type: execute
wave: 4
depends_on: ["42-05"]
files_modified:
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - plugin/conductor/README.md
  - plugin/conductor/references/team-kernel.md
autonomous: true
requirements: [FABR-03, FABR-04, FABR-05]

must_haves:
  truths:
    - "La découverte des agents est récursive : un agent dans un sous-dossier de --agents-dir est linté (D-10)"
    - "Exclusions prouvées par test : aucun dossier dont le nom finit par -references ni aucun dossier caché n'est parcouru ; README.md, AGENTS.md, contracts.md sont ignorés à toute profondeur ; les liens symboliques de dossiers ne sont pas suivis (D-10)"
    - "La résolution Agent(nom) voit les agents des sous-dossiers par la MÊME fonction de découverte — aucun second mécanisme"
    - "Dans un lab frais, .claude/agents/conductor-references/ existe et n'est jamais pris pour un agent : check-agents.sh --strict --manifest-freshness=strict y rend 0"
    - "I2 (D-09) : sous --resolve-agents=strict, un agent vf-internal qu'aucune allowlist Agent(...) de l'univers connu (dossier linté + registres) ne dispatche fait sortir le gate en rc=1 avec « invariant I2 »"
    - "I3 (D-09) : sous --resolve-agents=strict, un agent dispatché par une allowlist de l'univers connu sans vf-internal fait sortir le gate en rc=1 avec « invariant I3 », qui nomme le ou les dispatcheurs"
    - "I2 et I3 sont inactifs hors monde fermé ; leurs violations ne sont imputées qu'aux fichiers du dossier linté, jamais à un registre ; identité d'un agent = son nom de fichier ; aucun registre écrit à la main (D-09)"
    - "Monde fermé réel (six plugin/*/agents copiés) rc=0 ; mutations réelles prouvées par cmp : vf-test-orchestrator privé de vf-internal et de son marqueur → « invariant I3 » ; vf-test-runner retiré de son allowlist → « invariant I2 »"
    - "Mutants QUAL-01 tués : MUT-D1 (récursion), MUT-D2 (exclusion -references), MUT-I2, MUT-I3"
    - "conductor passe en mineure (valeur sur disque + 0.1.0 ; attendu v1.41.0 au moment de la rédaction, v1.42.0 si la PR #100 est mergée avant l'exécution — lire la valeur sur disque, jamais un numéro en dur) avec VERSION, module.json, ligne Version du README et CHANGELOG cohérents ; team-kernel.md dit le statut interne de vf-test-orchestrator ; T76 vert et intact (D-13)"
    - "Le SUMMARY porte la liste des commits destinés à la relecture de Samuel (mobile-test-team, design-orchestrator) et constate qu'aucun commit ne touche dev-orchestrator (D-12)"
  artifacts:
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "decouvrir_agents, index_agents, invariant_i2, invariant_i3"
      contains: "def decouvrir_agents"
    - path: "plugin/conductor/scripts/tests/test-check-agents.sh"
      provides: "T97 à T102, MUT-D1, MUT-D2, MUT-I2, MUT-I3"
      contains: "MUT-D2"
    - path: "plugin/conductor/CHANGELOG.md"
      provides: "entrée mineure de la phase : manifeste, fraîcheur, invariants, découverte, installeur, corpus"
      contains: "check-agents-manifest.json"
    - path: "plugin/conductor/references/team-kernel.md"
      provides: "ligne Mobile de la table des implémentations : vf-test-orchestrator worker interne"
      contains: "vf-test-orchestrator"
  key_links:
    - from: "decouvrir_agents()"
      to: "boucle principale, index_agents() et univers du monde fermé"
      via: "une seule fonction de découverte pour la cible, la résolution et I2/I3"
      pattern: "decouvrir_agents\\("
    - from: ".github/workflows/ci.yml (étape monde fermé)"
      to: "invariant_i2 / invariant_i3"
      via: "--strict --resolve-agents=strict --manifest-freshness=strict avec un --agent-registry-dir par plugin/*/agents"
      pattern: "resolve-agents=strict"
  prohibitions:
    - statement: "MUST NOT rendre la CI verte en désarmant, rétrogradant en avertissement, exemptant par liste ou contournant un invariant I1-I7, au lieu de corriger le corpus"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans descripteur câblé — relecture en PR"
    - statement: "MUST NOT présenter une décision de cadrage de Claude comme un arbitrage humain (commit, CHANGELOG, PR), ni modifier un module de la polarité de Samuel sans l'y nommer relecteur"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit (CLAUDE.md, traçabilité des arbitrages)"
---

<objective>
Fermer la phase : la découverte des agents devient récursive avec des exclusions prouvées (D-10),
I2 et I3 naissent en monde fermé en réutilisant l'univers que la CI connaît déjà (D-09), puis
conductor est bumpé en mineure avec sa documentation, et le relevé des commits destinés à Samuel est
posé pour la PR (D-12).

Purpose: FABR-04 (découverte récursive, exclusions testées), FABR-03 (I2, I3 avec jumeau négatif et
mutation rouge) et FABR-05 (corpus conforme sous `--strict` ET `--resolve-agents=strict`, invariants
armés ; T76 déjà correct depuis v2.63.2, D-13).
Output: `decouvrir_agents`, `index_agents`, `invariant_i2`, `invariant_i3`, T97 à T102, quatre
mutants tués, conductor v1.41.0 attendu, SUMMARY de clôture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-02-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-03-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-05-SUMMARY.md

Fait mesuré qui rend l'exclusion critique : l'installeur pose `references/` de chaque module sous
`$TARGET_ROOT/agents/<mod>-references/` (plugin/_internal/vibeflow-update.sh, vers l.2293-2297) —
un lab frais a donc `.claude/agents/conductor-references/*.md`, des documents qui ne sont pas des
agents.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : découverte récursive avec exclusions prouvées, résolution par la même découverte (T97 à T99, MUT-D1, MUT-D2, lab frais)</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : NOT_AGENTS ; resolve_agent_name ; boucle principale de découverte et branche cible vide F13 ; en-tête)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : T10 contracts.md/README.md ignorés ; helpers mk_gate_dir, make_gate_mutant, okmut/komut)
    - plugin/_internal/vibeflow-update.sh (vers l.2293-2297 : pose de <mod>-references sous agents/)
    - plugin/conductor/scripts/guard-agent-write.sh (commentaire l.10 : les sous-dossiers *-references/ ne matchent pas)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-10)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Pattern 5, Code Examples « Fixture de test recommandée pour D-10 »)
  </read_first>
  <behavior>
    - T97 (récursion) : `$AG/equipe/sous-agent.md` sans model + un agent conforme à la racine → rc=1, sortie citant sous-agent.md et « model absent » ; le même sous-agent rendu conforme → rc=0
    - T98 (exclusions) : agent conforme à la racine + `$AG/lab-references/lead-knowledge.md` (sans frontmatter) + `$AG/equipe/README.md` + `$AG/.cache/x.md` → rc=0, la sortie ne cite ni lead-knowledge.md, ni README.md, ni x.md
    - T99 (résolution) : `$AG/equipe/sous-agent.md` interne (vf-internal + « Worker interne ») + agent racine `tools: Read, SendMessage, Agent(sous-agent)` → rc=0 sous `--resolve-agents=strict`, aucun « non resolu »
    - MUT-D1 : l'élagage des dossiers cachés remplacé par un élagage total (plus aucune descente) → fixture T97 : rc_original=1, rc_mutant=0
    - MUT-D2 : l'élagage des dossiers -references neutralisé → fixture T98 : rc_original=0, rc_mutant=1
    - Lab frais (fermeture de conductor) : .claude/agents/conductor-references/ contient des .md et le gate du lab en --strict --manifest-freshness=strict rend 0, sans « AUCUN frontmatter »
  </behavior>
  <action>
1. Tests d'abord : T97, T98, T99, MUT-D1, MUT-D2, joués ROUGES (le gate de 42-05 ne descend pas
dans les sous-dossiers : T97 et T99 rougissent), puis implémentation. Toutes les fixtures sont
conformes aux invariants de 42-05 (le sous-agent dispatché de T99 est interne, l'agent racine porte
SendMessage), pour que leurs rc ne dépendent que de la découverte. Catalogue d'en-tête : T97 à T99,
MUT-D1, MUT-D2 sous « Découverte récursive (Phase 42, FABR-04, D-10) ».
2. Gate, bloc Python :
   - `decouvrir_agents(racine)` : parcours `os.walk(racine, followlinks=False)` (aucun lien
     symbolique de dossier suivi — pas de boucle). À chaque niveau, DEUX instructions d'élagage
     distinctes sur la liste des sous-dossiers, chacune sur sa propre ligne et unique dans le
     fichier : l'une retire les dossiers dont le nom commence par un point, l'autre retire ceux dont
     le nom finit par `-references` (cibles de MUT-D1 et MUT-D2). Fichiers retenus : `*.md` dont le
     nom n'est pas dans `NOT_AGENTS`, à toute profondeur. Rend une liste triée par chemin relatif.
   - La boucle principale remplace son glob à un niveau par `decouvrir_agents(agents_dir)` ; la
     branche cible vide (F13) et le traitement des fichiers tiers sont inchangés.
   - `index_agents()` : index paresseux nom-de-fichier → chemin, construit par `decouvrir_agents`
     sur `agents_dir` puis sur chaque registre (le premier trouvé l'emporte, `agents_dir` d'abord).
     `resolve_agent_name` garde sa signature et ses verdicts (native, thirdparty, resolved,
     unresolved) mais consulte cet index au lieu de tester `<dossier>/<nom>.md` — une seule
     découverte pour la cible et la résolution.
   - Les messages gardent le nom de fichier comme aujourd'hui (T10 et les cas existants l'assertent).
   - En-tête : « Découverte récursive (D-10) » — ce qui est parcouru, ce qui est exclu et pourquoi
     (les `<mod>-references/` posés par l'installeur sous `.claude/agents/` sont de la doc, pas des
     agents), liens symboliques non suivis.
3. MUT-D1 : `make_gate_mutant D1 0 '<motif de la ligne d'élagage des dossiers cachés>' '<même
affectation qui vide la liste>'` ; MUT-D2 : `make_gate_mutant D2 0 '<motif de la ligne d'élagage
-references>' 'pass'`. Les motifs sont relevés dans le code écrit à l'étape 2 ; chacun doit
désigner exactement une ligne (le helper le vérifie).
4. Vérifier le témoin lab frais (commande de vérification).
5. Commit : `feat(conductor): découverte récursive des agents, exclusions prouvées (FABR-04)` avec
les trailers
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — découverte récursive et index de résolution (D-10)`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T97 à T99, mutants MUT-D1, MUT-D2`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T(97|98|99)|MUT-D[12]|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T97, T98, T99 n'a aucune ligne « ✓ », ou « ✓ MUT-D1 TUE » ou « ✓ MUT-D2 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); (cd "$LAB" && git init -q); bash "$R/plugin/_internal/resolve-deps.sh" conductor | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >/dev/null 2>&1); done; ls "$LAB"/.claude/agents/conductor-references/*.md >/dev/null 2>&1 && out=$(cd "$LAB" && bash .claude/scripts/check-agents.sh --strict --manifest-freshness=strict 2>&1) && ! printf '%s' "$out" | grep -q 'AUCUN frontmatter' && echo LAB-RECURSIF-OK</automated>
    <fails_when>la ligne LAB-RECURSIF-OK est absente (pas de conductor-references dans le lab, gate du lab non vert, ou un document de référence pris pour un agent)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def decouvrir_agents(' plugin/conductor/scripts/check-agents.sh` rend 1, `grep -c 'followlinks=False' plugin/conductor/scripts/check-agents.sh` rend au moins 1, `grep -c -- '-references' plugin/conductor/scripts/check-agents.sh` rend au moins 1
    - `grep -c 'glob.glob(os.path.join(agents_dir,' plugin/conductor/scripts/check-agents.sh` rend 0 (plus de glob à un niveau pour la cible)
    - la sortie de test-check-agents.sh contient `✓ MUT-D1 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` et `✓ MUT-D2 TUE : rc_mutant=1 attendu 1, rc_original=0 attendu 0`, des lignes `✓ T97`, `✓ T98`, `✓ T99`, et finit sur `0 KO ==` (T10 toujours vert)
    - le témoin lab frais imprime `LAB-RECURSIF-OK`
  </acceptance_criteria>
  <done>La découverte est récursive, les exclusions sont prouvées par fixture et par mutant, la résolution passe par la même découverte, et un lab frais avec ses dossiers -references reste vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : invariants I2 et I3 en monde fermé sur l'univers existant (T100 à T102, MUT-I2, MUT-I3, rejeu CI)</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : resolve_agent_name et index_agents, allowlist_agents de 42-05, boucle principale, fichiers tiers, rapport)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : T30/T30b et leur registre $REG ; helpers juger_mutation_reelle, make_gate_mutant)
    - .github/workflows/ci.yml (étape « check-agents --resolve-agents=strict (monde fermé …) » : un --agent-registry-dir par plugin/*/agents, chaque dossier linté à tour de rôle)
    - plugin/mobile-test-team/agents/vf-test-orchestrator.md et vf-test-runner.md, plugin/dev-orchestrator/agents/vf-dev-manager.md (porteurs réels des mutations I2/I3)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-09)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-RESEARCH.md (Pattern 3, § Don't Hand-Roll)
  </read_first>
  <behavior>
    - T100 (I2) : `$AG/worker-orphelin.md` (vf-internal + « Worker interne ») et un registre vide de tout dispatch → `--resolve-agents=strict --agent-registry-dir=<registre>` → rc=1 « invariant I2 » ; sans `--resolve-agents=strict` → rc=0, aucun « invariant I2 » ; un manager du registre (SendMessage + `Agent(worker-orphelin)`) → rc=0
    - T101 (I3) : `$AG/worker-expose.md` (non interne, sans marqueur) + registre dont un manager dispatche `worker-expose` → rc=1 « invariant I3 » nommant ce manager ; sans le monde fermé → rc=0 ; en lintant le REGISTRE (le manager) avec `$AG` en registre → aucun « invariant I3 » (imputation au seul dossier linté)
    - T102 (monde fermé réel) : copie des six plugin/*/agents sous `$WORK/univers/<mod>/agents`, chaque dossier linté avec tous les registres, `--strict --resolve-agents=strict --skills-dir=<inexistant>` → rc=0 partout, aucun « invariant I » ; mutation 1 : copie de vf-test-orchestrator.md sans sa ligne vf-internal, « Worker interne » ramené à « Worker », `SendMessage, ` inséré avant `Agent(` → run mobile-test-team rc=1 avec « invariant I3 » et sans « invariant I1 » ni « invariant I6 » ; mutation 2 : `vf-test-runner, ` retiré de l'allowlist de vf-test-orchestrator → rc=1 « invariant I2 » sur vf-test-runner.md ; chaque mutation prouvée par cmp, puis restaurée → rc=0
    - MUT-I2, MUT-I3 : ligne d'appel `errors.extend(invariant_i2(` (puis `invariant_i3(`) remplacée par `pass` → fixture T100 (puis T101) : rc_original=1, rc_mutant=0
  </behavior>
  <action>
1. Tests d'abord (T100 à T102, MUT-I2, MUT-I3), joués ROUGES, puis implémentation. Catalogue
d'en-tête : T100 à T102, MUT-I2, MUT-I3 sous « Invariants de monde fermé (Phase 42, FABR-03, D-09) ».
2. Gate — passe de monde fermé, exécutée seulement sous `--resolve-agents=strict`, après la boucle
de lint et avant le rapport :
   - univers = `decouvrir_agents(agents_dir)` (ou le fichier seul en mode --file) plus
     `decouvrir_agents` de chaque registre, dédupliqué par chemin réel ; fichiers tiers (préfixes)
     exclus ; un fichier sans frontmatter lisible est ignoré ici (déjà signalé s'il est linté) ;
   - carte « dispatché par » : pour chaque fichier de l'univers, chaque nom rendu par
     `allowlist_agents` (42-05) dont `resolve_agent_name` rend « resolved » reçoit le nom de fichier
     du dispatcheur (un agent qui se cite lui-même ne compte pas) — la MÊME résolution que le lint des
     allowlists, aucun registre écrit à la main (D-09) ;
   - pour chaque fichier LINTÉ dans ce run (non tiers), identité = nom de fichier sans `.md`, deux
     lignes d'appel uniques : `errors.extend(invariant_i2(...))` — interne et absent de la carte →
     « {base} : invariant I2 — worker vf-internal orphelin : aucune allowlist Agent(...) de l'univers
     connu ne le dispatche (monde fermé, D-09) » ; `errors.extend(invariant_i3(...))` — présent dans
     la carte et non interne → « {base} : invariant I3 — dispatché par <dispatcheurs> sans
     vf-internal : worker exposé par erreur (monde fermé, D-09) ». Jamais d'imputation à un fichier
     de registre.
   - En-tête : compléter la section des invariants avec I2 et I3, « actifs sous --resolve-agents=strict
     seulement (la CI, seule à connaître l'univers complet) ».
3. Rejouer la suite, puis l'étape CI de monde fermé avec le gate réel (commande de vérification).
4. Commit : `feat(conductor): invariants I2 et I3 en monde fermé (FABR-03)` avec les trailers
`Gate-Touche: plugin/conductor/scripts/check-agents.sh — invariants I2, I3 sur l'univers du monde fermé (D-09)`
et
`Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T100 à T102, mutants MUT-I2, MUT-I3`.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T10[012]|MUT-I[23]|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T100, T101, T102 n'a aucune ligne « ✓ », ou « ✓ MUT-I2 TUE » ou « ✓ MUT-I3 TUE » manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>reg=(); for d in plugin/*/agents; do reg+=("--agent-registry-dir=$d"); done; fail=0; for d in plugin/*/agents; do out=$(bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --manifest-freshness=strict --agents-dir="$d" "${reg[@]}" 2>&1) || fail=$((fail+1)); printf '%s' "$out" | grep -q 'invariant I' && fail=$((fail+1)); done; echo "MONDE-FERME fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « MONDE-FERME fail=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'errors.extend(invariant_i2(' plugin/conductor/scripts/check-agents.sh` et `grep -c 'errors.extend(invariant_i3(' plugin/conductor/scripts/check-agents.sh` rendent chacun 1
    - la sortie de test-check-agents.sh contient `✓ MUT-I2 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` et `✓ MUT-I3 TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1`, des lignes `✓ T100`, `✓ T101`, `✓ T102`, et finit sur `0 KO ==` (T30 et T30b toujours verts)
    - le rejeu du monde fermé réel imprime `MONDE-FERME fail=0`
  </acceptance_criteria>
  <done>I2 et I3 rougissent en monde fermé, sur fixture comme sur l'arbre réel muté, n'imputent qu'aux fichiers lintés, réutilisent la résolution existante ; le monde fermé réel est vert ; deux mutants tués.</done>
</task>

<task type="auto">
  <name>Tâche 3 : conductor en mineure, documentation, rejeu complet et relevé de relecture pour la PR</name>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md, plugin/conductor/README.md, plugin/conductor/references/team-kernel.md</files>
  <read_first>
    - plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md (l.9 ligne Version ; entrée check-agents.sh de la liste des scripts, vers l.98-102), plugin/conductor/CHANGELOG.md (forme de l'entrée de la PR #85 et de v1.39.0)
    - plugin/conductor/references/team-kernel.md (table « Implémentations », ligne Mobile — et les littéraux gardés par T76, à ne pas toucher)
    - plugin/conductor/scripts/check-agents.sh (en-tête final : c'est la source que le CHANGELOG et le README résument)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-12, D-13) ; 42-02-SUMMARY.md et 42-03-SUMMARY.md (hashes et points de relecture)
    - CLAUDE.md (discipline de release : AUCUN bump racine ici ; traçabilité des arbitrages)
  </read_first>
  <action>
1. Bump MINEUR de conductor (nouvelle capacité) depuis la valeur lue sur disque au moment de
l'exécution (attendu v1.40.0, posé par la PR #85 → v1.41.0). AVERTISSEMENT (fait postérieur à
l'écriture de ce plan) : la PR #100 (Samuel, workstream `fiabilite`) bump conductor v1.40.0 →
v1.41.0 sur `main` pour un motif SANS RAPPORT avec cette phase. Si la #100 est déjà mergée dans
`main` au moment de l'exécution, la valeur lue sur disque sera v1.41.0 et ce bump attend v1.42.0 —
lire la valeur sur disque, JAMAIS la valeur `v1.41.0` en dur ci-dessus. Ce commit reste SÉPARÉ et
DERNIER (aucun autre commit de la phase ne bumpe conductor) ; au rebase après le merge de la #100,
seul le numéro de version attendu change, jamais l'ordre ni le contenu de ce commit. Dans `VERSION`,
`module.json` (.version) et la ligne `**Version**` du README (l.9). Ne toucher ni à la VERSION
racine, ni à plugin.json, ni à marketplace.json, ni aux README racine : la release est un geste
séparé (aucune release, aucun bump de VERSION racine dans cette phase).
2. README de conductor : l'entrée `check-agents.sh` de la liste des scripts dit en une phrase le
manifeste daté (`check-agents-manifest.json`, validité portée par le manifeste,
`--manifest-freshness=strict` en CI seulement), les invariants I1 à I7 et la découverte récursive.
3. CHANGELOG, entrée en tête « ## [v1.41.0] — <date d'exécution ISO> (Phase 42 — manifeste daté et
invariants de doctrine du gate des agents) », « **Minor** (nouvelle capacité du gate des agents) : » —
puces : manifeste daté et source unique (FABR-01, D-01, D-02, D-03 ; trois outils et deux champs
ajoutés, alignés sur la seule doc officielle, D-17) ; installeur qui pose les `*.json` des modules
(D-16, commit fix(engine)) ; fraîcheur (FABR-02, D-04, D-05 : INDÉTERMINÉ sous
`--manifest-freshness=strict` en CI, avertissement chez l'utilisateur, rétrogradation des trois
classes de listes fermées) ; invariants I1 à I7 en erreur (FABR-03) avec les trois écarts assumés
par rapport à la spec §4 (D-06 marqueur lu dans description:, D-07 manager non interne, D-08 juge
sans allowlist) ; découverte récursive et exclusions (FABR-04, D-10) ; corpus mis en conformité dans
cinq modules bumpés en patch (FABR-05, D-11, D-12) ; échéance du manifeste (verifie_le + 30 j),
effets à l'échéance (CI rouge à ses quatre appels, T20 de test-dev-orchestrator.sh) et geste unique
de rafraîchissement ; garde d'écriture en fail-open sur manifeste illisible ; T76 inchangé (D-13,
déjà correct depuis v2.63.2) ; décisions de cadrage de Claude sur délégation explicite de Willy
(AskUserQuestion session principale, 2026-09-23), 42-CONTEXT.md — jamais présentées comme une
décision humaine. Le message du commit reprend cette même trace.
4. team-kernel.md, table « Implémentations », ligne Mobile : la cellule Manager devient
« `vf-test-orchestrator` — orchestrateur de boucle, worker interne (`vf-internal`) dispatché par
`vf-dev-manager` / `vf-auto`, hors classe manager du gate (I6, D-07) ». Aucune autre ligne du fichier
ne change (T76 garde ses littéraux).
5. Rejeu complet (commandes de vérification) : suite du gate, suites voisines, rejeu CI, témoin lab
frais, budget d'instructions, synchro des versions, G-2 sur la branche.
6. Commit : `feat(conductor v1.41.0): manifeste daté et invariants de doctrine du gate des agents` —
hors surface G-2 (VERSION, module.json, CHANGELOG, README, team-kernel.md), aucun fichier `.planning/`.
7. SUMMARY de clôture (commit `.planning/` séparé) : section « Relecture Samuel (D-12) » listant les
hashes et titres des commits mobile-test-team et design-orchestrator de la branche
(`git log --format='%h %s' origin/main..HEAD -- plugin/mobile-test-team plugin/design-orchestrator`),
les points de relecture relevés en 42-02/42-03 (sources CLAUDE.md citées par des corps de juge,
commande d'incarnation résiduelle), le constat « aucun commit ne touche dev-orchestrator » (sortie
vide de `git log origin/main..HEAD -- plugin/dev-orchestrator`), et l'information que git log montre
Samuel auteur majoritaire des trois bundles aussi. Section « Résidus signalés » : squelette de
valeurs du message de refus de guard-agent-write.sh ; champ omitClaudeMd inconnu de la liste FIELDS
de check-artifact-fidelity.sh (perte non mesurée sur les runtimes tiers) ; clause « test corrigé »
de FABR-05 satisfaite par le hotfix v2.63.2 (D-13), à lire ainsi au ledger.
  </action>
  <verify>
    <automated>for s in plugin/conductor/scripts/tests/test-check-agents.sh plugin/conductor/scripts/tests/test-guard-agent-write.sh plugin/conductor/scripts/tests/test-check-blueprints.sh scripts/tests/test-hook-exit-parc.sh plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh plugin/content-bundle/scripts/tests/test-content-bundle.sh plugin/growth-bundle/scripts/tests/test-growth-bundle.sh plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh; do bash "$s" >/dev/null 2>&1 || echo "SUITE-ROUGE $s"; done; bash plugin/conductor/scripts/check-instruction-budget.sh >/dev/null 2>&1 || echo "BUDGET-ROUGE"; bash scripts/check-version-sync.sh >/dev/null 2>&1 || echo "VERSION-SYNC-ROUGE"; bash plugin/conductor/scripts/check-blueprints.sh >/dev/null 2>&1 || echo "BLUEPRINTS-ROUGE"; echo REJEU-FIN</automated>
    <fails_when>une ligne SUITE-ROUGE, BUDGET-ROUGE, VERSION-SYNC-ROUGE ou BLUEPRINTS-ROUGE apparaît avant REJEU-FIN</fails_when>
    <automated>git fetch -q origin main 2>/dev/null; bash scripts/check-gate-touche.sh; echo "G2 rc=$?"</automated>
    <fails_when>la dernière ligne n'est pas « G2 rc=0 » (un chemin de la surface de gate touché par la branche n'est couvert par aucun trailer Gate-Touche conforme)</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T76|✗ T76'</automated>
    <fails_when>aucune ligne « ✓ T76 » n'est imprimée, ou une ligne « ✗ T76 » apparaît (team-kernel.md a perdu un littéral gardé)</fails_when>
  </verify>
  <acceptance_criteria>
    - `cat plugin/conductor/VERSION` vaut la version de main + 0.1.0 (attendu `v1.41.0`), identique à `.version` de module.json et à la ligne Version du README ; `head -5 plugin/conductor/CHANGELOG.md | grep -c "^## \[$(cat plugin/conductor/VERSION)\]"` rend 1
    - `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json README.md README.fr.md | wc -l` rend 0 (aucun bump racine)
    - `grep -c 'check-agents-manifest.json' plugin/conductor/CHANGELOG.md` et `grep -c 'check-agents-manifest.json' plugin/conductor/README.md` rendent chacun au moins 1
    - `grep '^| Mobile' plugin/conductor/references/team-kernel.md | grep -c 'worker interne'` rend 1
    - `git log -1 --format=%B -- plugin/conductor/CHANGELOG.md | grep -c 'délégation explicite de Willy'` rend au moins 1 et `grep -c 'délégation explicite de Willy' plugin/conductor/CHANGELOG.md` rend au moins 1
    - `bash scripts/check-gate-touche.sh` sort 0 ; T76 imprimé `✓`
    - le SUMMARY contient une section « Relecture Samuel (D-12) » avec au moins deux hashes et le constat sur dev-orchestrator
  </acceptance_criteria>
  <done>conductor est en mineure avec une documentation qui dit ce que fait désormais le gate ; le kernel ne contredit plus le gate sur vf-test-orchestrator ; tout est vert au rejeu (suites, CI rejouée, lab frais, budget, versions, blueprints, G-2) ; le SUMMARY porte le relevé de relecture de Samuel et les résidus signalés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arborescence de `.claude/agents/` → gate | tout fichier trouvé par la découverte devient une cible de lint |
| union des dossiers d'agents (CI) → I2/I3 | l'univers connu décide qui est orphelin ou exposé |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-20 | Denial of Service | découverte récursive dans un lab installé | high | mitigate | élagage des dossiers `-references` et cachés, NOT_AGENTS à toute profondeur ; T98, MUT-D2 ; témoin lab frais LAB-RECURSIF-OK |
| T-42-21 | Denial of Service | lien symbolique de dossier en boucle | low | mitigate | parcours sans suivi des liens de dossiers (followlinks=False) |
| T-42-22 | Spoofing (faux vert) | sous-dossier invisible au gate | medium | mitigate | récursion prouvée (T97) et mutant MUT-D1 tué |
| T-42-23 | Tampering | faux positifs I2/I3 hors monde fermé ou imputés à un registre | medium | mitigate | actifs sous --resolve-agents=strict seulement, imputation au seul dossier linté (T100, T101) |
| T-42-24 | Repudiation | attribution de décisions dans CHANGELOG et commits | low | mitigate | « décisions de cadrage de Claude sur délégation explicite de Willy (AskUserQuestion session principale, 2026-09-23) », jamais « arbitrage » (critère d'acceptation) |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-agents.sh` : 0 KO ; T77 à T102 ; MUT-F1, MUT-F2, MUT-I1 à MUT-I7, MUT-D1, MUT-D2 tués (onze mutants).
- Rejeu des trois étapes CI avec `--manifest-freshness=strict` (dont le monde fermé) : 0 échec.
- Témoin lab frais : `LAB-RECURSIF-OK`.
- `bash scripts/check-gate-touche.sh` : rc 0.
- `bash plugin/conductor/scripts/check-instruction-budget.sh` : `0 depassement(s)`.
</verification>

<success_criteria>
- FABR-04 : découverte récursive, exclusions prouvées par test et par mutant.
- FABR-03 : I2 et I3 en monde fermé, jumeaux négatifs et mutation prouvée rouge ; les sept invariants armés.
- FABR-05 : corpus conforme sous `--strict` et `--resolve-agents=strict` ; T76 déjà correct (D-13).
- D-12 : relevé de relecture de Samuel prêt pour la PR ; dev-orchestrator intact.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `T103`, `MUT-D20`, jeton `CIBLE-ABSENTE` | invocation nue sur cible absente hors --hook (D-20) | 42-01 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `perimees`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()`, `dispatch` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` ; section `INVARIANTS DE DOCTRINE` | invariants locaux | 42-05 |
| `juger_mutation_reelle` ; `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur 4 juges ; `SendMessage` sur 4 managers | frontmatter du corpus | 42-02, 42-03 |
| conductor en mineure (attendu v1.41.0) ; ligne Mobile de team-kernel.md | version et doctrine | 42-06 |
| mobile-test-team, business-pilot-bundle, content-bundle, growth-bundle, design-orchestrator en patch | versions de module | 42-02, 42-03 |

## Flagged assumptions

- **Flake connu T9/T29/T32 de test-check-agents.sh (documenté dans la PR #100, workstream fiabilite) : un rouge isolé sur ces trois cas précis (SIGPIPE) se rejoue seul avant d'être déclaré réel — jamais pris pour argent comptant à la première lecture de sortie de suite (0 KO attendu dans ce plan).**

- **Sonde de bords spec-less — FABR-03, FABR-04, FABR-05 : `unclassified` / `unresolved`.** Non
  résolus d'office ; les bords rédigés ici (dossiers cachés, -references, NOT_AGENTS en profondeur,
  liens symboliques, auto-dispatch, imputation au dossier linté) sont des vérités du plan.
- **Le runtime Claude Code charge-t-il les agents des sous-dossiers de `.claude/agents/` ?** Non
  vérifié par la recherche. La découverte récursive suit la spec (§1.2 : un sous-dossier ne doit pas
  rendre le gate vert sur un corpus partiellement invisible) ; si le runtime ne descend pas, le gate
  est plus strict que le runtime, jamais plus laxiste.
- **Identité par nom de fichier** pour I2/I3 : c'est ce qu'utilise déjà la résolution des allowlists ;
  un agent dont `name:` diffère de son fichier reste signalé par l'avertissement existant.
- **Ligne Mobile de team-kernel.md** : ajout de cohérence avec D-07 (le kernel ne doit pas appeler
  manager un worker interne) ; hors du périmètre T76 ; à relire par Samuel avec le commit
  mobile-test-team.
- **Ledger** : la clause « le test qui verrouillait une affirmation périmée est corrigé » de FABR-05
  est satisfaite par le hotfix v2.63.2 (D-13) ; le ledger `.planning/REQUIREMENTS.md` est mis à jour
  par la vérification de phase, pas par ce plan.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-06-SUMMARY.md` when done — commit `.planning/` séparé, avec les sections « Relecture Samuel (D-12) » et « Résidus signalés ».
</output>

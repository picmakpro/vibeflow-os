---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 04
type: execute
wave: 3
depends_on: ["23-03"]
files_modified:
  - plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh
  - plugin/dev-orchestrator/references/gsd-capabilities-index.md
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/_internal/vibeflow-update.sh
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [GSDC-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le module dispose d'une table qui dit, point de hook par point de hook, quelle capability le moteur insère lui-même et quel toggle la gouverne (D-07)."
    - "Cette table est GÉNÉRÉE depuis le moteur installé et porte un en-tête « ne pas éditer » — elle ne peut pas affirmer une version que la machine ne fait plus tourner (D-07). Ce n'est pas la génération qui le garantit mais la GARDE DE FRAÎCHEUR (assertion T28-F) : sans elle, la copie versionnée dérive en silence entre deux régénérations, sous un en-tête qui la fait lire comme à jour — le défaut 1.8.0 / 1.9.0 déplacé d'un cran, pas supprimé."
    - "La liste des points de hook est lue depuis le registre du moteur, jamais écrite en dur — un treizième point apparaîtrait tout seul dans la table."
    - "Aucune prose destinée à un LLM (le champ de rendu de la sortie amont) n'est recopiée dans un document versionné."
    - "La table se régénère à l'install/update du module, comme l'index de skills existant (patron IDX-02)."
  artifacts:
    - plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh
    - plugin/dev-orchestrator/references/gsd-capabilities-index.md
    - plugin/_internal/vibeflow-update.sh
  key_links:
    - "Enumération des points depuis `byLoopPoint` du registre de capabilities — le seul lien qui empêche la table de périmer sur un point de hook nouveau."
    - "Le hook post-install de `vibeflow-update.sh` : sans le second appel, la table est générée une fois puis fige, exactement le défaut que D-07 corrige."
    - "L'en-tête « auto-généré — NE PAS ÉDITER » plus l'absence de toute version en dur : c'est ce qui a produit l'écart 1.8.0 / 1.9.0 à l'origine de cette phase."
---

<objective>
Livrer la table capabilities/hooks de la Lacune 3, **générée** et non écrite — pour qu'elle ne
puisse plus mentir sur la version du moteur.

Purpose : `grep -r "capabilit\|render-hooks\|plan:pre\|verify:post"` sur `plugin/dev-orchestrator/`
retourne aujourd'hui **zéro occurrence** : le mécanisme de capabilities est littéralement invisible
pour le module, qui raisonne comme si GSD était une liste de skills à appeler. Et l'écart qui a
déclenché l'ouverture de cette phase — un index versionné affirmant 1.8.0 pendant que la machine
tournait en 1.9.0 — est exactement ce que produit une table écrite à la main.

Fait dimensionnant mesuré au RESEARCH : le moteur expose **12** points de hook, pas les 6 que le
ROADMAP avait mesurés. Un générateur correct couvre les 12, et il les **découvre** au lieu de les
lister.

Output : `build-gsd-capabilities-index.sh` sur le patron exact de `build-gsd-index.sh`, sa sortie
`references/gsd-capabilities-index.md` auto-générée, le renvoi depuis `GSD-PIPELINE.md`, la
régénération à l'install, et un bloc de test T28 à discriminance prouvée par mutation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` (ce plan) ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md` (ce plan, auto-généré) ·
`plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh`

**Champs neufs du bloc typé** : `gate`, `reprise`, `verdicts`, `decompte`

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » ·
`mission-flow.md` §Pattern E budget partagé + table des briques dormantes · `docs/ADR.md` ADR-061
troisième objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24` à `T33` (ce plan : `T28`). Note :
`T24` à `T27c` sont **déjà posés** par les plans amont de la phase — la plage nomme l'empreinte
totale de la Phase 23, pas un lot vierge. `T28` est libre (0 occurrence mesurée dans le fichier de
test) et reste l'identifiant de ce plan.

**Variables d'environnement neuves** : `VF_CAPS_INDEX_OUT`, `VF_GSD_TOOLS`, `VF_GSD_CORE_LIB`
(ce plan) · `VF_CONFIG_PATH`

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` / `3` / `64`

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10`

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`).
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`).
- **Gate de schéma ORM : sans objet.**
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan lit la sortie d'un binaire Node **déjà installé localement** (`gsd-tools.cjs`) et un
  module du même paquet local — aucun SDK tiers, aucun service distant, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

<tasks>

<task type="tracer">
  <name>Tâche 1 : le générateur produit la table des 12 points depuis le moteur installé (D-07)</name>
  <files>plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh, plugin/dev-orchestrator/references/gsd-capabilities-index.md</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/build-gsd-index.sh (138 lignes, INTÉGRALEMENT) — patron obligatoire dans sa charpente : en-tête documentant la loi anti-hallucination (aucun nom écrit en dur, tout extrait du disque), variables surchargeables préfixées `VF_`, résolution `SCRIPT_DIR`, `mkdir -p` du dossier de sortie, horodatage `date -Iseconds` avec repli portable, écriture **par bloc unique** `{ ... } > "$OUT"`, en-tête « auto-généré — NE PAS ÉDITER » dans le fichier produit. Attention : ce bloc n'est **pas** atomique — la redirection tronque `$OUT` à l'ouverture, avant qu'une seule ligne soit produite. L'analogue ne s'en sort que parce qu'il agrège toutes ses lignes dans `rows_tmp` **avant** d'ouvrir le bloc (l. 106, `printf … >> "$rows_tmp"`, puis `sort "$rows_tmp"` en l. 121) : à l'ouverture, il ne reste plus rien qui puisse échouer. Lire les l. 100-138 en gardant cet ordre en tête — c'est lui le contrat, pas la forme du bloc
    - plugin/dev-orchestrator/references/gsd-skills-index.md (les 20 premières lignes) — la forme exacte de l'en-tête produit, à imiter
    - .planning/phases/VFDO-23-.../23-RESEARCH.md §« 12 points de hook — sortie réelle sur ce lab » — la table de référence mesurée en session : elle sert de contrôle de plausibilité de la sortie générée, jamais de contenu à recopier dans le script
    - .planning/phases/VFDO-23-.../23-RESEARCH.md §Security Domain — l'interdiction de recopier le champ de prose destiné au LLM (plusieurs Ko par capability sur le point de pré-plan)
    - .planning/codebase/CONVENTIONS.md §Portabilité bash — ADR-054
  </read_first>
  <action>
Créer `plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` en calquant
`build-gsd-index.sh` dans sa **charpente complète** (le fichier voisin est le contrat de forme du
module ; s'en écarter créerait deux styles de générateur pour un seul module).

**Sources de vérité, dans cet ordre, et aucune valeur en dur** :

1. **La liste des points de hook** se lit depuis le registre de capabilities du moteur
   (`gsd-core/bin/lib/capability-registry.cjs`, export `byLoopPoint` — ses clés **sont** les points,
   12 aujourd'hui). L'écrire en dur dans le script serait reproduire exactement le défaut que D-07
   corrige, une couche plus bas : un treizième point ajouté en amont doit apparaître tout seul dans
   la table à la régénération suivante. Écrire ce motif en commentaire dans le script.
2. **Le contenu de chaque point se lit dans le MÊME registre**, pas ailleurs. `byLoopPoint[point]`
   est un objet à trois listes — `steps`, `contributions`, `gates` (ce sont exactement les trois
   natures de la colonne « nature ») — dont chaque entrée porte déjà les cinq colonnes voulues :
   `capId`, `when` (le toggle gouvernant), `blocking`, `onError`. Mesuré en session : 12 points,
   `plan:pre` = 6 steps + 5 contributions + 2 gates, et `execute:pre` est le **seul** point
   réellement vide.

3. **`loop render-hooks <point> --raw` n'est PAS une source de contenu** — contrôle de
   plausibilité uniquement, consigné au SUMMARY, jamais recopié dans la table. Raison mesurée en
   session, et elle est structurante : cette commande renvoie `activeHooks` **filtré par les
   toggles du lab courant** — 10 entrées sur `plan:pre` là où le registre en déclare 13. Une table
   bâtie dessus porte la configuration de la seule machine qui l'a générée ; elle n'est comparable
   à rien, et la garde de fraîcheur de la tâche 2 devient impossible à écrire sans faux rouge sur
   tout lab autrement configuré. Le registre, lui, est une **déclaration** : il ne dépend que de la
   version du moteur. C'est cette propriété — et elle seule — qui rend la copie versionnée
   vérifiable. Bénéfice second : le champ de prose (`rendered`, plusieurs dizaines de Ko sur le
   point de pré-plan ; `fragment` sur les contributions) n'entre jamais dans le générateur, ce qui
   ferme `T-23-04-01` à la racine au lieu de le filtrer après coup.

**Interdiction explicite, à écrire en commentaire dans le script** : ne jamais recopier le champ de
**rendu** de cette sortie (ni un éventuel fragment en ligne) dans le document produit. C'est de la
prose destinée à un modèle, volumineuse — plusieurs kilo-octets pour le seul point de pré-plan — et
elle n'a pas vocation à vivre dans un markdown versionné. Menace `Tampering` /
`Information Disclosure` du threat model.

**Robustesse de parsing (du seul contrôle de plausibilité)** : le binaire peut écrire un avertissement avant la charge utile selon
l'état du lab. Établir le canal **par exécution** (une commande, le constat au SUMMARY) plutôt que
par supposition, et extraire la charge utile à partir de sa première accolade ouvrante plutôt que
de museler stderr en aveugle (`2>/dev/null` masquerait une vraie panne). Note : le plan 23-02
supprime la cause de cet avertissement **sur ce lab**, mais le générateur doit rester correct sur
un lab tiers qui ne l'a pas encore fait.

**Variables surchargeables** (patron `VF_INDEX_OUT` / `VF_GSD_SKILLS_DIR` de l'analogue) :
`VF_CAPS_INDEX_OUT` pour le chemin de sortie, `VF_GSD_TOOLS` pour le chemin du binaire,
`VF_GSD_CORE_LIB` pour le dossier des modules du moteur. Résolution par cascade — lab courant
d'abord, scope utilisateur ensuite, même priorité que la cascade `$S` de `mission-flow.md`.

**Dégradation, et l'ORDRE qui la rend vraie** : moteur absent ou registre introuvable ⇒ ne pas
écrire de fichier tronqué ; sortir en erreur explicite sur stderr, sans jamais produire un index
partiel qui passerait pour complet. (L'appel post-install est best-effort côté installeur, tâche 3
— c'est là que l'échec est absorbé, pas ici.)

Cette promesse n'est **pas** tenue par le seul patron `{ … } > "$OUT"` de l'analogue : la
redirection tronque `$OUT` dès son ouverture, donc toute panne survenant *pendant* la production du
contenu laisse derrière elle un fichier vide ou coupé — exactement le fichier tronqué qu'on
s'interdit, et la mitigation de `T-23-04-05` serait une phrase sans effet. Ordre imposé, dans cet
ordre :

1. produire **l'intégralité** du contenu dans un temporaire (`mktemp`, apparié à un
   `trap … EXIT` — invariant `T21d` du module, non négociable sur un script neuf) ;
2. ne considérer le succès qu'une fois ce temporaire complet ;
3. **`mv`** le temporaire sur `$OUT` — c'est ce déplacement, et lui seul, qui est atomique.

Toute sortie en erreur avant l'étape 3 laisse la cible **intacte**, jamais tronquée.

**Sortie `plugin/dev-orchestrator/references/gsd-capabilities-index.md`** :
- en-tête « auto-généré — NE PAS ÉDITER » + ligne d'horodatage + source, sur la forme exacte de
  `gsd-skills-index.md` ;
- une ligne d'avertissement, importante et à écrire, et dont le sens a changé avec la source
  retenue ci-dessus : la table dit ce que le moteur **déclare** à la version depuis laquelle elle a
  été générée — quels hooks *peuvent* se déclencher et sous quel toggle —, et **jamais** ce qui est
  actif sur un lab donné. La colonne toggle nomme la condition ; elle ne la résout pas. Écrire les
  deux moitiés : le périmètre (déclaration du moteur) **et** ce que la table ne dit pas (l'état
  effectif d'un lab, qui se lit avec `loop render-hooks`, pas ici) ;
- une section ou une ligne de table **par point de hook**, y compris les points **vides** — un
  point sans hook déclaré est une information, pas une ligne à omettre (`execute:pre` est
  aujourd'hui le seul dans ce cas) ;
- les colonnes, mappées un pour un sur les champs du registre : capability (`capId`) · nature
  (`steps` / `contributions` / `gates`) · toggle gouvernant (`when`) · bloquant (`blocking`) ·
  conduite sur erreur (`onError`) ;
- un compteur en pied (nombre de points parcourus, nombre de hooks **déclarés**), sur le modèle du
  `log "Index généré : ... (N skill(s))"` de l'analogue.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh && grep -c '^## \|^| ' plugin/dev-orchestrator/references/gsd-capabilities-index.md && grep -c 'NE PAS ÉDITER' plugin/dev-orchestrator/references/gsd-capabilities-index.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` sort en 0 et écrit `plugin/dev-orchestrator/references/gsd-capabilities-index.md`.
    - Le fichier produit contient la chaîne `NE PAS ÉDITER`.
    - Le fichier produit nomme les **12** points de hook : `discuss:pre`, `discuss:post`, `plan:pre`, `plan:post`, `execute:pre`, `execute:wave:pre`, `execute:wave:post`, `execute:post`, `verify:pre`, `verify:post`, `ship:pre`, `ship:post` — vérifiable par `for p in ...; do grep -q "$p" <fichier> || echo MANQUE:$p; done` qui ne doit rien afficher.
    - Le fichier produit nomme au moins les capabilities `pattern-mapper`, `code-review`, `nyquist` et `security` (contrôle de plausibilité contre la mesure du RESEARCH).
    - **Aucun des 12 points n'est écrit en dur dans le script — les 12, pas un seul.** Un critère sur `discuss:pre` seul laisserait passer un script qui figerait les onze autres. Mesure, sur les lignes de code (commentaires pleine ligne retirés — une mention illustrative d'un nom de point est tolérée en commentaire, nulle part ailleurs) :
      ```
      awk '!/^[[:space:]]*#/' plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh > /tmp/caps-code.sh
      for p in discuss:pre discuss:post plan:pre plan:post execute:pre execute:wave:pre \
               execute:wave:post execute:post verify:pre verify:post ship:pre ship:post; do
        awk -v p="$p" 'index($0,p){n++} END{if(n>0) print "EN DUR:" p " (" n ")"}' /tmp/caps-code.sh
      done
      ```
      Ne doit **rien** afficher.
    - **La lecture du registre est prouvée par l'assertion E de la tâche 2, jamais par un grep de motif.** `grep -c 'byLoopPoint'` serait satisfait par le seul commentaire que ce plan demande d'écrire (§1 de l'action) : c'est une sonde à token, verte sans qu'une ligne de code n'ait jamais ouvert le registre. La garantie réelle est la mutation E — registre factice à 2 points ⇒ la couverture des 12 points **doit** rougir. Critère : la preuve de mutation de E est consignée au SUMMARY avec ses compteurs.
    - `wc -c < plugin/dev-orchestrator/references/gsd-capabilities-index.md` retourne une valeur < 20000 (preuve qu'aucun champ de prose amont n'a été recopié : le seul rendu du point de pré-plan pèse déjà plusieurs Ko).
    - **Aucune version du moteur figée dans la logique — critère sec, sans branche d'appréciation.** La formulation « … ou toute occurrence est une valeur par défaut commentée comme telle » n'est pas une mesure : « commentée comme telle » est un jugement humain, et une branche que la machine ne sait pas trancher rend le critère vert par défaut. On matérialise donc d'abord le **périmètre logique** — lignes de code, hors commentaires pleine ligne et hors affectations/défauts de variables `VF_*` (le seul endroit où un chemin par défaut a le droit de porter un numéro) — puis on exige `0` sec :
      ```
      awk '!/^[[:space:]]*#/ && !/^[[:space:]]*(:[[:space:]]+)?"?\$?\{?VF_[A-Z_]+/' \
        plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh > /tmp/caps-logic.sh
      awk '/[0-9]+\.[0-9]+\.[0-9]+/{n++} END{print n+0}' /tmp/caps-logic.sh
      ```
      Doit imprimer exactement `0`.
    - Deux exécutions consécutives produisent un fichier identique **hors ligne d'horodatage** (idempotence) — vérifiable par `diff <(grep -v -i 'Généré le' fichier1) <(grep -v -i 'Généré le' fichier2)` vide.
    - **La cible n'est jamais laissée tronquée — critère COMPORTEMENTAL, pas une phrase de mitigation** (c'est la seule preuve de `T-23-04-05`) : partant d'une cible existante et valide, relancer le générateur avec `VF_GSD_CORE_LIB` pointant sur un chemin **inexistant** ; exiger un exit **non nul** ET une cible **inchangée octet pour octet**. Comparaison par empreinte, jamais par `diff` — `diff` a rendu « Files are identical » sur des fichiers différents sur ce poste :
      ```
      F=plugin/dev-orchestrator/references/gsd-capabilities-index.md
      A=$(shasum "$F" | awk '{print $1}')
      VF_GSD_CORE_LIB=/nonexistent-$$ bash plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh; rc=$?
      B=$(shasum "$F" | awk '{print $1}')
      [ "$rc" -ne 0 ] && [ "$A" = "$B" ] && echo OK || echo KO
      ```
      Doit imprimer `OK`.
    - **L'écriture passe par `mv`, jamais par une redirection sur la cible** (l'ordre prescrit dans l'action) : sur le périmètre logique `/tmp/caps-logic.sh` déjà matérialisé, `awk '/mv[[:space:]]/{n++} END{print n+0}'` retourne ≥ 1, et aucune ligne de code ne redirige directement sur la cible — `awk '/>[[:space:]]*"?\$\{?(OUT|VF_CAPS_INDEX_OUT)/{n++} END{print n+0}' /tmp/caps-logic.sh` retourne `0`.
    - Tout `mktemp` du script est apparié à un `trap … EXIT` (invariant `T21d` du module, appliqué à un script neuf).
    - `bash -n plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` sort en 0 et le fichier est exécutable.
    - `grep -v '^#' plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh | grep -cE "grep -P|sed -i |eval "` retourne `0` (ADR-054 + sécurité).
  </acceptance_criteria>
  <done>Une table des 12 points de hook existe dans le module, générée depuis le moteur installé, découvrant elle-même la liste des points, sans version figée et sans une ligne de prose amont recopiée.</done>
</task>

<task type="auto">
  <name>Tâche 2 : la table est joignable depuis la doctrine et vérifiée par gate (D-07)</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md — l'état laissé par le plan 23-03 : §9 « Flags de cycle » existe, et §7 porte déjà le patron du renvoi vers un index généré (« pour la liste exhaustive et à jour des briques, consulter `gsd-skills-index.md` »)
    - plugin/dev-orchestrator/references/gsd-capabilities-index.md — la sortie produite en tâche 1
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh — le bloc `T6` (install end-to-end) **démarre l. 986**, et sa boucle `for ref in GSD-PIPELINE.md gsd-skills-index.md intent-routing.md mission-contracts.md docs-flow.md` est **l. 1002-1003** (mesuré ; la référence « ~911-946 » d'une rédaction antérieure était périmée et pointait un autre bloc) ; et les blocs T14b/T14c pour le patron de mutation
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh l. 1869-1876 (`T21d ATTEINTE`) et l. 2497-2511 (`T25 ATTEINTE`) — le patron de **compteur d'atteinte séparé du verdict**, à reproduire à l'identique pour T28 : le commentaire dit pourquoi la garde serait verte à vide, puis un `if compteur > 0` / `else ko` autonome. C'est le patron du module, pas une invention de ce plan
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh l. 133 (`md_folded`) et l. 165-178 (`md_blocks_matching`) — comment le module **matérialise un périmètre** avant de mesurer dedans, au lieu de balayer un fichier entier
  </read_first>
  <action>
**1. `GSD-PIPELINE.md`** — ajouter le renvoi vers la table générée, sur le patron **déjà écrit au
§7** pour l'index de skills (une ligne en citation, pas une section). Le placer là où il sert : au
début de §9, parce que la doctrine de flags parle de toggles et que la table dit quel toggle
gouverne quel hook ; ou en §7 à côté du renvoi existant. Un seul emplacement, et le texte dit deux
choses : le fichier est **auto-généré** (ne jamais l'éditer à la main) et il reflète la
configuration du lab où il a été produit.

Ajouter aussi, en une ligne, le fait structurant que le module ignorait complètement et qui est la
raison d'être de cette table : le moteur **insère lui-même** ses étages aux points de hook, en
fonction des toggles — un agent ne les « choisit » pas. C'est la réponse au Constat 0 du ROADMAP
(la bonne question n'était pas « y a-t-il accès » mais « qui décide »).

**2. `test-dev-orchestrator.sh` — bloc `T28`**, ajouté après le dernier bloc existant :
- assertion A : `plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` existe et est
  exécutable ;
- assertion B : la sortie versionnée porte l'en-tête « ne pas éditer » ;
- assertion C : la sortie versionnée nomme les 12 points de hook — la liste des points attendus est
  **dérivée du registre du moteur** dans le test aussi (jamais réécrite en dur dans la suite), avec
  un `skip` propre si le moteur est absent de l'environnement de test ;

- **assertion « T28 ATTEINTE » (compteur, SÉPARÉE du verdict de C)** — sans elle, C est verte à
  vide. Le `skip` propre qu'on vient d'exiger produit zéro KO ; si le critère de résultat se
  contente de « 0 KO », alors sur toute machine sans moteur (CI, poste tiers) la seule assertion
  qui relie la table au registre amont ne mesure **rien** et se déclare pourtant tenue. Patron
  identique à `T21d atteinte` (l. 1869-1876) et `T25 atteinte` (l. 2497-2511) : un compteur
  `t28_derived` = **nombre de points effectivement dérivés du registre**, et deux régimes qu'on ne
  confond jamais :
  - registre **illisible / moteur absent** ⇒ `skip` nommant la cause, `t28_derived` reste à 0 ;
  - registre **lu** mais `t28_derived` = 0 ⇒ `ko` explicite — le registre répond et ne produit
    aucun point : la garde serait verte sur un générateur qui n'a rien couvert.

  Le cas « moteur absent » n'est pas refermé ici mais par le critère de résultat ci-dessous, qui
  exige de voir la ligne d'atteinte avec son compte : un `skip` ne peut pas la produire.

- **assertion F — FRAÎCHEUR de la copie versionnée** (elle porte à elle seule la truth « elle ne
  peut pas affirmer une version que la machine ne fait plus tourner »). Sans elle, ce plan
  reproduit d'un cran le défaut qu'il corrige : la tâche 1 commite un artefact généré depuis le
  moteur d'**un** poste, la tâche 3 ne régénère que chez l'installateur — jamais dans le dépôt —,
  et entre les deux `gsd-core` bouge pendant que la copie versionnée dérive **en silence**, sous un
  en-tête « auto-généré » qui la fait lire comme à jour. C'est le scénario 1.8.0 / 1.9.0, déplacé,
  pas supprimé. L'idempotence de la tâche 1 ne couvre pas ça : elle compare deux exécutions du
  **même** moteur au **même** instant. Et `T6` ne couvre pas ça non plus : il constate la
  **présence** du fichier, donc il est vert grâce à la copie commitée sans rien prouver de la
  régénération.

  Procédé : régénérer dans un `mktemp -d` via `VF_CAPS_INDEX_OUT`, puis comparer à la copie
  versionnée **hors ligne d'horodatage** — les deux flancs filtrés par `awk '!/Généré le/'` dans
  des temporaires, comparés par `cmp -s` (jamais `diff` : il a rendu « Files are identical » sur
  des fichiers différents sur ce poste). Divergence ⇒ `ko` **nommant la commande de
  régénération** (`bash plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh`), pour que
  le message soit une consigne et pas un constat. Même régime d'atteinte que C : `skip` si le
  moteur est absent, compté dans `t28_derived`.

  Cette assertion n'est **tenable que grâce au choix de source de la tâche 1** : la table est
  dérivée du registre (déclaration, fonction de la seule version du moteur), donc comparable d'un
  lab à l'autre. Sur une table bâtie sur `render-hooks` — filtrée par les toggles du lab, 10
  entrées contre 13 déclarées sur `plan:pre` — cette comparaison serait un générateur de faux
  rouges sur tout lab autrement configuré, et devrait être abandonnée.

- assertion D (**NÉGATIVE**) : le script ne contient aucun nom de point de hook en dur ;
- assertion E (**DISCRIMINANTE, par mutation**) : régénérer la sortie dans un `mktemp -d` via
  `VF_CAPS_INDEX_OUT` avec un registre factice ne portant que 2 points, et exiger que l'assertion C
  **échoue** sur cette sortie — ce qui prouve qu'elle compare vraiment à la liste amont et ne se
  contente pas de constater la présence de quelques chaînes.

**Ancres de section obligatoires** — pas de cosmétique : sans elles, la garde négative D n'est pas
mesurable. Un critère qui dit « aucun nom de point en dur, *hors du bloc de fixture de mutation* »
alors que sa commande balaie le fichier entier ne mesure rien : soit la fixture nomme un point et
le critère rougit à tort, soit elle n'en nomme pas et la clause d'exclusion n'a jamais servi. Le
périmètre doit donc **exister** avant d'être mesuré. Encadrer le bloc par deux commentaires
d'ancre, et la fixture de mutation par deux autres, à l'intérieur :

```
# >>> T28 DEBUT
#   … assertions A, B, C, ATTEINTE, F, D …
# >>> T28 FIXTURE MUTATION DEBUT
#   … registre factice à 2 points, assertion E …
# >>> T28 FIXTURE MUTATION FIN
# >>> T28 FIN
```

Ces quatre ancres sont ce qui rend le critère de la garde négative exprimable en `awk` (extraction
du bloc **moins** la fixture, puis comptage sur le fichier extrait). C'est le même geste que
`md_folded` / `md_blocks_matching` (l. 133 et 165-178) : matérialiser le périmètre, puis mesurer
dedans — jamais mesurer sur le fichier entier en décrivant le périmètre en prose.

**3. `test-dev-orchestrator.sh` — bloc `T6`** : ajouter `gsd-capabilities-index.md` à la boucle
`for ref in ...` qui vérifie la présence des fichiers de références après install end-to-end.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5'</automated>
  </verify>
  <acceptance_criteria>
    - **La suite passe ET T28 a réellement mesuré.** `bash -o pipefail -c 'bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5'` sort en **0** avec `0 KO`. Le `-o pipefail` n'est pas décoratif : sans lui, le code de sortie observé est celui de `tail`, jamais celui de la suite, et la recette est verte quoi qu'il arrive.
    - **Le résultat n'est PAS satisfaisable par un `skip`.** Le critère « 0 KO et la ligne de résultat mentionne `T28` » est tenu par un `skip "T28 … moteur absent"` — zéro KO, token présent, zéro mesure. Exiger à la place la **ligne d'atteinte avec son compte**, qu'un skip ne peut pas produire : la sortie contient une ligne `T28 atteinte` annonçant **12** points dérivés du registre, et l'assertion F y est déclarée atteinte (ni skip, ni absente). Sur un environnement sans moteur, ce critère n'est pas tenu — c'est voulu : il dit alors la vérité, à savoir que la preuve reste à produire sur une machine qui fait tourner le moteur.
    - **Fraîcheur (assertion F) exercée pour de vrai** : consigner au SUMMARY la sortie de F sur la copie versionnée (identique hors horodatage), **et** sa preuve de discriminance — muter d'une ligne la copie versionnée, relancer, exiger `1 KO` dont le message nomme `build-gsd-capabilities-index.sh`, puis restaurer. Sans cette mutation, F est une comparaison dont rien ne prouve qu'elle compare.
    - `awk '/gsd-capabilities-index\.md/{n++} END{print n+0}' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1.
    - **Renvois dans la suite, comptés en `awk` et non en `grep -c`** — `grep` est proxifié sur ce poste et **tronque silencieusement** (31 lignes rendues sur 102, sans avertissement) : un comptage sur pipe y est une preuve sans valeur. Baseline mesurée à l'`awk` avant travaux : **0**. Attendu après : `awk '/gsd-capabilities-index\.md/{n++} END{print n+0}' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` retourne ≥ 2 (bloc T28 **et** boucle T6), croisé avec `awk '/gsd-capabilities-index\.md/{print FNR": "$0}' … | wc -l` qui doit donner la même valeur.
    - **Garde négative D — sur un périmètre MATÉRIALISÉ, pas décrit en prose.** « hors du bloc de fixture de mutation » n'est porté par aucune commande qui balaie le fichier entier ; il faut donc extraire le périmètre d'abord, avec les ancres posées dans l'action :
      ```
      awk '/^# >>> T28 DEBUT/{inb=1} /^# >>> T28 FIXTURE MUTATION DEBUT/{inf=1} \
           inb && !inf {print} /^# >>> T28 FIXTURE MUTATION FIN/{inf=0} /^# >>> T28 FIN/{inb=0}' \
        plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > /tmp/t28-hors-fixture.sh
      awk 'END{print NR}' /tmp/t28-hors-fixture.sh      # > 0 : le périmètre existe, l'extraction n'est pas vide
      awk '/execute:wave:post/{n++} END{print n+0}' /tmp/t28-hors-fixture.sh   # doit imprimer 0
      ```
      Les deux mesures sont exigées : la seconde seule serait verte à vide si l'extraction ne rendait rien (ancres mal posées).
    - Preuve de mutation de l'assertion E consignée au SUMMARY (registre factice à 2 points → au moins `1 KO`).
  </acceptance_criteria>
  <done>La doctrine pointe vers la table générée, la suite vérifie que la table couvre exactement les points que le moteur déclare, la vérification elle-même ne fige aucune liste, et la copie versionnée ne peut plus dériver du moteur installé sans que la suite le dise en nommant la commande de régénération.</done>
</task>

<task type="auto">
  <name>Tâche 3 : la table se régénère à l'install/update du module (patron IDX-02)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh lignes 535-560 — le hook post-install existant : il régénère l'index de skills in-place dans le dossier de références de l'agent, en best-effort (`>/dev/null 2>&1`, jamais d'échec d'install si le moteur est absent), avec le chemin de sortie passé par variable d'environnement
    - plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh — l'état produit en tâche 1, notamment le nom exact de la variable de chemin de sortie
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh bloc T6 — le test d'install end-to-end étendu en tâche 2
  </read_first>
  <action>
Étendre le hook post-install de `plugin/_internal/vibeflow-update.sh` d'un **second** appel de
générateur, strictement symétrique du premier :

- même garde de présence croisée (le module fournit le script **et** le script a bien été copié
  dans le dossier de scripts de la cible) ;
- même passage du chemin de sortie par variable d'environnement, vers
  `<cible>/agents/<module>-references/gsd-capabilities-index.md` ;
- même caractère **best-effort** : l'échec de la régénération (moteur absent au moment de l'install)
  ne doit **jamais** faire échouer l'install — il produit une ligne de journal et rien d'autre,
  exactement comme le premier appel ;
- même style de message de journal, en français.

Ne pas fusionner les deux appels en une boucle générique : le premier est stabilisé depuis la
Phase 1 (IDX-02) et le refactorer élargirait le périmètre de ce plan à un fichier d'engine partagé
par tous les modules, sans bénéfice.

**Test — et il ne peut pas rester TEXTUEL.** `plugin/_internal/vibeflow-update.sh` est l'engine
d'install exécuté chez **tous** les utilisateurs du plugin, tous modules confondus. Des critères de
seule présence de chaînes n'y prouvent rien du comportement, et le seul risque qui compte
(`T-23-04-04` : une install de module tiers cassée par l'échec du nouveau générateur) est
**précisément** un comportement. Le bloc `T28` reçoit donc deux assertions, pas une :

- **assertion G1 (structurelle)** : `vibeflow-update.sh` nomme le script du générateur **et** la
  variable de chemin de sortie ;
- **assertion G2 (COMPORTEMENTALE — c'est elle qui ferme `T-23-04-04`)** : exercer le **chemin
  d'échec réel**. Dans un `mktemp -d` monté comme le lab de `T6` (l. 986-1003 — faux cache
  `VIBEFLOW_CACHE`, copie du module), lancer l'install du module avec `VF_GSD_TOOLS` **et**
  `VF_GSD_CORE_LIB` pointant sur des chemins **inexistants** — c'est la simulation fidèle d'un
  moteur absent au moment de l'install. Exiger les trois : l'install sort en **0**, le lab
  contient bien l'agent et ses références (l'install n'a pas été amputée), et le journal porte la
  ligne de repli du nouvel index. Sans cette assertion, « best-effort » n'est qu'une intention
  écrite dans un commentaire.

  Cette assertion est **la seule** qui exerce le repli : `T6` est un `skip` best-effort et ne
  couvre que le chemin nominal. Elle doit donc, elle aussi, être comptée à l'atteinte (même
  `t28_derived` / même discipline que C et F) — un `skip` silencieux la rendrait décorative.

Le bloc `T6`, déjà étendu en tâche 2, couvre le résultat observable après une install réelle quand
l'environnement le permet ; il reste en `skip` propre sinon. Rappel de ce que `T6` **ne** prouve
pas : il constate la **présence** de `gsd-capabilities-index.md`, donc il serait vert grâce à la
seule copie versionnée, sans qu'aucune régénération ait eu lieu. La preuve de régénération est
portée par G2 et par l'assertion de fraîcheur F, pas par lui.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'bash -n plugin/_internal/vibeflow-update.sh && bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n plugin/_internal/vibeflow-update.sh` sort en 0.
    - `grep -c 'build-gsd-capabilities-index.sh' plugin/_internal/vibeflow-update.sh` retourne ≥ 2 (garde de présence + appel).
    - `grep -c 'VF_CAPS_INDEX_OUT' plugin/_internal/vibeflow-update.sh` retourne ≥ 1.
    - **Le repli est prouvé par EXÉCUTION, pas par une sonde de fenêtre fixe.** `grep -A4 'build-gsd-capabilities-index.sh' … | grep -c 'else'` est une sonde à fenêtre de 4 lignes : elle casse au premier commentaire ajouté dans le bloc, et elle ne dit de toute façon rien du comportement — un `else` peut journaliser *et* propager un échec. Critère retenu, l'assertion G2 exercée à la main pour la trace du SUMMARY :
      ```
      LAB=$(mktemp -d); CACHE="$LAB/.vibeflow-cache"; mkdir -p "$CACHE/dev-orchestrator"
      cp -r plugin/dev-orchestrator/. "$CACHE/dev-orchestrator/"
      (cd "$LAB" && VIBEFLOW_CACHE="$CACHE" VF_GSD_TOOLS=/nonexistent-$$/gsd-tools.cjs \
         VF_GSD_CORE_LIB=/nonexistent-$$/lib \
         bash "$OLDPWD/plugin/_internal/vibeflow-update.sh" install dev-orchestrator) > "$LAB/journal.txt" 2>&1
      echo "exit=$?"
      awk 'END{print NR}' "$LAB/journal.txt"
      awk '/index/ && /best-effort|non régénéré/{print}' "$LAB/journal.txt"
      [ -f "$LAB/.claude/agents/dev-orchestrator.md" ] && echo AGENT-OK || echo AGENT-KO
      ```
      Exigé : `exit=0`, **au moins une** ligne de repli journalisée pour le nouvel index, et `AGENT-OK` — un moteur absent au moment de l'install dégrade, il n'ampute pas.
    - `grep -c 'build-gsd-index.sh' plugin/_internal/vibeflow-update.sh` retourne toujours ≥ 2 (l'appel historique est intact, non refactoré).
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO`.
  </acceptance_criteria>
  <done>Un lab qui installe ou met à jour le module obtient une table de capabilities à jour sans geste manuel, et un moteur absent au moment de l'install dégrade en une ligne de journal au lieu de faire échouer l'install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sortie de `gsd-tools loop render-hooks` → document versionné | La sortie contient un champ de **prose destinée à un modèle**, volumineux et non maîtrisé au sens d'un markdown de dépôt. La frontière est l'extraction : seuls des champs structurés la traversent. |
| registre de capabilities du moteur → générateur | Frontière de **version** : la liste des points et des capabilities change avec `gsd-core`. C'est le franchissement qui doit être automatique — c'est tout l'objet de D-07. |
| générateur → installeur partagé (`vibeflow-update.sh`) | Ce fichier est l'engine de **tous** les modules : un échec non absorbé y casserait l'install de modules sans rapport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04-01 | Tampering | recopie du champ de rendu amont dans un markdown versionné | high | mitigate | Extraction limitée aux champs structurés (identifiant de capability, nature, toggle, bloquant, conduite sur erreur) ; interdiction écrite en commentaire dans le script ; critère machine de taille du fichier produit (< 20 000 octets — le seul rendu du point de pré-plan pèse déjà plusieurs Ko). |
| T-23-04-02 | Tampering | injection shell via une valeur issue de la sortie du binaire | high | mitigate | Aucun `eval`, aucun `bash -c` sur la sortie ; l'extraction des champs se fait côté Node, jamais par interpolation shell d'un contenu non maîtrisé. Critère machine : `grep -c 'eval'` hors commentaires retourne `0`. |
| T-23-04-03 | Spoofing | table affirmant une version ou un jeu de points que la machine ne fait plus tourner | high | mitigate | Liste des points **découverte** depuis le registre (assertion négative T28-D sur périmètre matérialisé : aucun des 12 points en dur dans le script) + aucune version figée dans la logique (critère sec, sans branche d'appréciation) + en-tête « ne pas éditer » + régénération à l'install. **Et surtout la garde de fraîcheur T28-F** : régénération en `mktemp -d` puis comparaison à la copie versionnée hors horodatage, `ko` nommant la commande de régénération. Sans F, tout le reste laisse la copie **versionnée** dériver en silence entre deux régénérations — la régénération à l'install n'écrit que chez l'installateur, jamais dans le dépôt. C'est le défaut exact qui a ouvert cette phase (index à 1.8.0 pour une machine à 1.9.0), et il n'est fermé que par F. |
| T-23-04-04 | Denial of Service | install d'un module tiers cassée par l'échec du nouveau générateur | high | mitigate | Appel post-install strictement best-effort, symétrique de l'existant : sortie muselée, repli journalisé, jamais de propagation d'échec. Critère **comportemental** (T28-G2), pas textuel : install lancée dans un lab temporaire avec `VF_GSD_TOOLS` / `VF_GSD_CORE_LIB` sur des chemins inexistants ⇒ exit `0`, agent et références bien posés, ligne de repli au journal. Une sonde de présence de chaîne ne prouve rien sur un fichier partagé par tous les modules. |
| T-23-04-05 | Information Disclosure | index partiel présenté comme complet quand le moteur est absent | medium | mitigate | Le générateur agrège **l'intégralité** du contenu dans un temporaire puis `mv` sur la cible : toute sortie en erreur avant le `mv` laisse la cible intacte. Le patron `{ … } > "$OUT"` de l'analogue ne suffit pas — la redirection tronque la cible à l'ouverture. Critère machine (et non plus simple intention) : cible d'empreinte **inchangée** après un run forcé en échec par `VF_GSD_CORE_LIB` inexistant, exit non nul. |
| T-23-04-06 | Repudiation | table éditée à la main puis présentée comme générée | low | mitigate | En-tête « auto-généré — NE PAS ÉDITER » assertée par T28-B, et idempotence vérifiée (deux exécutions produisent le même fichier hors horodatage). |
| T-23-04-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan. |
</threat_model>

<verification>
- Le générateur tourne, produit les 12 points, est idempotent hors horodatage.
- Aucun point de hook, aucune version de moteur figés dans la logique du script (assertions négatives).
- Taille du markdown produit sous le seuil qui prouve l'absence de recopie de prose amont.
- Suite du module verte **sous `-o pipefail`** (sans lui, le code observé est celui de `tail`), et surtout : la ligne `T28 atteinte` annonce **12** points dérivés du registre. « 0 KO + token `T28` présent » ne suffit pas — un `skip` satisfait les deux sans rien mesurer.
- **Fraîcheur** : la copie versionnée est identique à une régénération faite à l'instant, hors horodatage ; la mutation d'une ligne de cette copie fait rougir la suite avec un message nommant la commande de régénération.
- **Dégradation prouvée par exécution, aux deux étages** : générateur avec moteur inexistant ⇒ exit non nul et cible d'empreinte inchangée ; install avec moteur inexistant ⇒ exit `0`, agent posé, repli journalisé.
- `bash -n` propre sur les deux scripts touchés ; l'appel post-install historique intact.
- Discriminance de l'assertion de couverture des points prouvée par **mutation** (registre factice à 2 points), commandes et compteurs consignés au SUMMARY.
- Toute mesure de comptage passe par `awk 'END{print NR}'` ou `wc -l` sur une liste matérialisée, **jamais** par `grep -c` sur un pipe : `grep` est proxifié sur ce poste et tronque sans avertir.
</verification>

<success_criteria>
- SC1 — La table des capabilities/hooks existe, couvre les 12 points (y compris les points vides), et est générée depuis le moteur installé.
- SC2 — Ni la liste des points ni la version du moteur ne sont écrites en dur ; un point nouveau en amont apparaîtrait seul à la régénération suivante.
- SC3 — Aucune prose destinée au modèle n'est recopiée dans le document versionné, prouvé par un critère de taille et par les champs effectivement extraits.
- SC4 — La doctrine renvoie vers la table et dit le fait qui manquait au module : le moteur insère lui-même ses étages, un agent ne les choisit pas.
- SC5 — La régénération est câblée à l'install/update en best-effort, sans jamais pouvoir faire échouer une install — **prouvé par une install réellement lancée** avec le moteur hors d'atteinte, pas par la présence d'un `else` dans une fenêtre de 4 lignes.
- SC6 — La **copie versionnée** ne peut pas dériver du moteur en silence : la suite compare cette copie à une régénération faite à l'instant (hors horodatage) et rougit en nommant la commande de régénération. Sans SC6, SC1 et SC2 ne portent que sur l'instant de la génération, et le défaut 1.8.0 / 1.9.0 est déplacé au lieu d'être fermé.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-04-SUMMARY.md` à la fin.
Y consigner : le canal (stdout ou stderr) sur lequel l'avertissement de configuration du binaire est
émis, constaté par exécution lors du contrôle de plausibilité ; le nombre de hooks **déclarés**
comptés par point, et l'écart avec les hooks **actifs** vus par `render-hooks` sur ce lab (c'est cet
écart qui justifie la source retenue) ; la taille du markdown produit ; la preuve de mutation de
l'assertion E avec ses compteurs avant/après ; la valeur de la ligne `T28 atteinte` ; la preuve de
discriminance de la garde de fraîcheur F (mutation d'une ligne de la copie versionnée → `1 KO`
nommant la commande de régénération, puis restauration) ; et la trace de l'assertion
comportementale G2 (exit de l'install, ligne de repli journalisée, présence de l'agent).
</output>

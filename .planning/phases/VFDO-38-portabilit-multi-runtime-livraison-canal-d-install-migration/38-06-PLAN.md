---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 06
type: execute
wave: 6
depends_on: ["38-04"]
files_modified:
  - plugin/conductor/scripts/migrate-lab-runtime.sh
  - plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh
  - plugin/conductor/skills/vf-calibrate/SKILL.md
  - plugin/conductor/skills/vf-calibrate/references/migration-playbook.md
  - plugin/conductor/scripts/vibeflow-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
  - README.md
  - README.fr.md
autonomous: false
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Sur CE dépôt (mesuré : aucune clé runtime dans .planning/config.json), la lecture normalisée dérive 'claude' comme runtime actif et 'claude' comme seul runtime installé — sans écrire quoi que ce soit tant qu'aucune migration n'est demandée"
    - "gsd-core continue de lire .planning/config.json.runtime comme une CHAÎNE après ce plan — jamais un objet qui ferait tomber resolveRuntimeNameFromCandidates dans son typeof !== 'string' et dégraderait TOUTE résolution de runtime en silence vers 'claude' par défaut"
    - "La skill vf-calibrate distingue EXPLICITEMENT, dans sa propre prose ET dans ce qu'elle affiche à l'exécution, la propagation additive (coexistence, un runtime de plus) de la migration soustractive (bascule, l'ancien cesse d'être actif) — jamais une phrase qui laisserait deviner laquelle des deux est en cours"
    - "Une bascule ne s'exécute JAMAIS sans être passée par dry-run puis confirmation explicite — le mode par défaut de vf-calibrate reste la coexistence"
    - "Après install -> bascule -> retour, l'arbre de fichiers du lab est comparé par ensembles (comm) à l'état avant bascule, et le résultat (identique ou la liste des écarts) est écrit noir sur blanc — jamais un compte de fichiers pris pour une preuve"
  artifacts:
    - "plugin/conductor/scripts/migrate-lab-runtime.sh : capacité interne (jamais un verbe utilisateur neuf, D-38-A) — lit/normalise .planning/config.json, orchestre install --target sous le runtime cible via vibeflow-update.sh (lot TGT, 38-04), gate dry-run/confirm/write, preuve de réversibilité"
    - "plugin/conductor/skills/vf-calibrate/SKILL.md : nouvelle section dédiée à la migration de runtime, dualité additive/soustractive nommée dans le texte"
  key_links:
    - "migrate-lab-runtime.sh -> ~/.claude/gsd-core/bin/lib/runtime-name-policy.cjs:94-103 (resolveRuntimeNameFromCandidates : `if (typeof candidate !== 'string') continue` — LA preuve que config.json.runtime doit rester une chaîne, jamais un objet, sous peine de dégradation SILENCIEUSE de la résolution runtime de gsd-core sur tout le lab, à chaque session future)"
    - "migrate-lab-runtime.sh -> vibeflow-update.sh (lot TGT, --target du 38-04) : la pose RÉELLE sous la racine du runtime cible réutilise le lot 4, jamais une seconde logique de copie"
    - "migrate-lab-runtime.sh -> check-artifact-fidelity.sh (lot FIDE, 38-01) : le statut d'un runtime coexistant sans hooks est déclaré via le MÊME gate, jamais un second mécanisme de déclaration (MIGR-05)"
  prohibitions:
    - "Ne jamais écraser config.json.runtime par un objet — c'est un one-way door sur un format que gsd-core lit déjà (schéma sur disque partagé avec un consommateur externe hors du contrôle de ce dépôt)"
    - "Ne jamais ajouter de verbe utilisateur (`/vf-migrate` ou équivalent) — D-38-A : extension de vf-calibrate uniquement"
    - "Ne jamais exécuter une bascule sans dry-run préalable montré, ni sans confirmation explicite distincte de ce dry-run (ADR-031)"
    - "Ne pas modifier .planning/MISSION-INVARIANTS.md soi-même (le script constate, un jugement humain ajoute/retire — signalé en fin de plan, jamais appliqué)"
---

<objective>
Étendre `vf-calibrate` (D-38-A, aucun verbe utilisateur neuf) pour porter la migration de lab entre
runtimes : `.planning/config.json` apprend à porter le runtime actif ET les runtimes installés
(coexistence par défaut, D-38-B), avec une bascule soustractive strictement gatée
(dry-run → confirmation → écriture, jamais par défaut) et une réversibilité PROUVÉE fichier à
fichier, pas déclarée. Le lot ROLL (38-03) est un prérequis explicite (D-38-C, topologie du DAG de
mission) — une bascule qui ne peut pas revenir en arrière de façon fiable n'a pas sa place ici.

Purpose: 81% des PLAN.md de ce dépôt ouvrent sur un `@$HOME/.claude/…` — la frontière `.planning/`
est Claude-only aujourd'hui. Ce lot ne prétend PAS la fermer entièrement ; il pose le SOCLE
(inscription du runtime, coexistence, réversibilité) sur lequel une migration réelle peut
s'appuyer, sans jamais dégrader en silence la résolution runtime que `gsd-core` opère déjà.

Output: `migrate-lab-runtime.sh` (capacité interne), `vf-calibrate` étendu avec la dualité
additive/soustractive explicite, réversibilité prouvée par exécution réelle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@.planning/config.json
@plugin/conductor/skills/vf-calibrate/SKILL.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Forme de `.planning/config.json` après ce plan — la clé `runtime` racine reste-t-elle une chaîne, avec une clé sœur dédiée pour la coexistence ?</decision>
  <context>
    38-CONTEXT.md (D-38-B) demande que le runtime « cesse d'être un scalaire » dans
    `.planning/config.json`, avec une forme portant « runtimes installés » + « runtime actif »,
    rétro-compatible sur 3 cas (absent/scalaire/objet). Lu littéralement, ceci pourrait suggérer de
    transformer la clé `runtime` elle-même en objet.

    Mesure faite pendant ce cadrage, jamais rapportée avant : `gsd-core` (dépendance npm externe,
    hors du contrôle de ce dépôt — payload sous `~/.claude/gsd-core/`) lit `config['runtime']` à
    NEUF emplacements distincts (`model-resolver.cjs`, `profile-output.cjs`, `capability-state.cjs`,
    `init.cjs`, etc.), et sa fonction canonique de résolution
    (`runtime-name-policy.cjs:94-103`, `resolveRuntimeNameFromCandidates`) contient
    `if (typeof candidate !== 'string') continue` — un candidat non-chaîne est SILENCIEUSEMENT
    ignoré, sans erreur, sans warning. Si `config.runtime` devenait un objet, TOUTE résolution de
    runtime par gsd-core (choix du modèle, fichier d'instructions projet, etc.) dégraderait en
    silence vers son défaut `'claude'` sur ce lab — exactement la classe de défaut que cette phase
    existe pour éliminer, mais appliquée cette fois à gsd-core lui-même plutôt qu'à VibeFlow.
  </context>
  <options>
    <option id="dual-key">
      <name>Clé `runtime` INCHANGÉE (chaîne, runtime actif) + nouvelle clé sœur `runtimes_installed` (tableau)</name>
      <pros>Zéro risque de régression sur gsd-core (9 sites de lecture inchangés) ; satisfait la lettre de D-38-B (coexistence trackée, 3 cas rétro-compatibles) sur une clé neuve dédiée à VibeFlow ; réversible sans migration de gsd-core lui-même.</pros>
      <cons>Deux clés à tenir synchronisées (`runtime` doit toujours être un élément de `runtimes_installed`) — un garde-fou explicite à écrire et tester.</cons>
    </option>
    <option id="nested-object">
      <name>`runtime` devient un objet `{active, installed}`, tel que lu littéralement dans D-38-B</name>
      <pros>Une seule clé, forme la plus proche du texte du cadrage.</pros>
      <cons>Casse `resolveRuntimeNameFromCandidates` en silence sur CE lab et tout lab migré de la même façon — gsd-core ignorerait la clé et retomberait sur 'claude' sans le dire, sur les 9 sites de lecture identifiés ; contredit directement la doctrine « sans dégradation silencieuse » du cadrage, appliquée à un consommateur externe plutôt qu'à VibeFlow. Disqualifiée sauf décision explicite du mainteneur d'accepter ce risque.</cons>
    </option>
  </options>
  <resume-signal>Choisir "dual-key" (recommandé) ou "nested-object" (accepte la dégradation gsd-core documentée ci-dessus) avant que la tâche suivante n'écrive quoi que ce soit.</resume-signal>
</task>

<task type="tracer">
  <name>Tache 2 : bout-en-bout — normalisation des 3 cas + coexistence déclarée (MIGR-01, MIGR-02)</name>
  <files>plugin/conductor/scripts/migrate-lab-runtime.sh, plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh, plugin/conductor/skills/vf-calibrate/SKILL.md, plugin/conductor/skills/vf-calibrate/references/migration-playbook.md</files>
  <precondition>Le checkpoint de la tâche 1 est résolu (option choisie, "dual-key" par défaut recommandé) — ne jamais écrire une forme de config.json sans cette décision actée.</precondition>
  <read_first>
    - .planning/config.json (état RÉEL de ce dépôt — aucune clé `runtime` du tout, confirmé par lecture directe : c'est le cas « absent » du cadrage, à normaliser vers `runtime: "claude"` DÉRIVÉ, jamais écrit sur disque tant qu'aucune migration n'est demandée — MIGR-01 dit « la clé racine RESTE une chaîne », pas « est toujours présente »)
    - ~/.claude/gsd-core/bin/lib/runtime-name-policy.cjs:94-103 (`resolveRuntimeNameFromCandidates` — relu ici pour écrire la même garantie dans le script : ne JAMAIS produire un `config.runtime` qui ne soit pas `typeof === 'string'`)
    - plugin/conductor/skills/vf-calibrate/SKILL.md (structure complète : la séquence en 5 étapes existante — Détecter l'écart / Lire ce qui a changé / Proposer un plan / Appliquer sous contrôle / Synthèse — le point d'insertion NATUREL d'une section « Migration de runtime » est un NOUVEAU sous-titre après l'étape 1, jamais une réécriture des 5 étapes existantes qui couvrent un axe DIFFÉRENT — drift de version framework, pas de runtime)
    - 38-CONTEXT.md lignes 42-57 (D-38-A et D-38-B verbatim : deux natures de la skill, dualité explicite « dans la skill ET dans la sortie » — jamais implicite ; coexistence par défaut ; bascule soustractive et gatée)
  </read_first>
  <action>
Créer `plugin/conductor/scripts/migrate-lab-runtime.sh`, usage
`migrate-lab-runtime.sh status|declare <runtime>` (deux verbes internes, jamais exposés par une
commande slash utilisateur — seul `vf-calibrate` les invoque, doctrine D-38-A confirmée par le
checkpoint précédent).

`status` : lit `.planning/config.json`. Trois branches, décidées sur la PRÉSENCE/type de la clé
`runtime` (même patron de classement « présence, jamais contenu numérique » que
`check-gsd-engine.sh` déjà établi dans ce dépôt) :
- absente → dérive `active=claude`, `installed=[claude]`, imprime « aucune clé runtime — dérivé
  claude (comportement gsd-core par défaut) », N'ÉCRIT RIEN.
- scalaire (chaîne) → `active=<valeur>`, `installed=[<valeur>]` (le runtime scalaire EST le seul
  installé, migration triviale vers la forme neuve), N'ÉCRIT RIEN en mode `status`.
- objet (forme neuve, clé sœur `runtimes_installed` présente) → lit les deux clés, les affiche.
Sortie JSON avec `--json` (même convention que les autres gates de ce lot).

`declare <runtime>` : AJOUTE `<runtime>` à `runtimes_installed` SANS changer `active` (propagation
ADDITIVE — coexistence, jamais une bascule) : si `runtimes_installed` n'existe pas encore, la créer
en la peuplant d'abord du runtime actuel dérivé par `status` (jamais un tableau à un seul élément
qui aurait perdu l'ancien) ; garantit que la clé `runtime` racine reste TOUJOURS une chaîne
(assertion explicite dans le script : après toute écriture, relire `runtime` et vérifier
`typeof` via un `node -e` d'un mot — jamais une confiance aveugle en son propre code d'écriture).
Si `<runtime>` déjà présent dans `runtimes_installed` : no-op, message explicite (« déjà
coexistant »).

Dans `vf-calibrate/SKILL.md`, ajouter une section « Migration de runtime (propagation additive vs
migration soustractive) » après l'étape 1 existante — texte qui NOMME les deux natures dès l'entrée
de la section (« Cette capacité a DEUX modes : coexistence — additive, un runtime de plus, jamais
par défaut sans le demander — et bascule — soustractive, l'ancien cesse d'être actif, gatée en 3
temps » — reprendre ce vocabulaire EXACT dans le message affiché par `migrate-lab-runtime.sh`
lui-même, jamais une seconde formulation qui dériverait de celle de la skill). Documenter l'appel
`migrate-lab-runtime.sh status`/`declare` dans `references/migration-playbook.md`, à la suite des
recettes existantes (classification bugfix/nouvelle capacité/breaking-doctrine — la migration de
runtime est une TROISIÈME famille, orthogonale, à nommer comme telle plutôt que forcée dans l'une
des trois existantes).

Créer `plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh` : T1 (config sans clé `runtime`
→ `status` dérive claude/[claude], fichier INCHANGÉ sur disque — snapshot avant/après identique),
T2 (config avec `runtime: "claude"` scalaire → `status` dérive claude/[claude], toujours rien
écrit), T3 (`declare codex` sur un lab sans clé → écrit `runtime: "claude"` (chaîne, inchangée) +
`runtimes_installed: ["claude","codex"]`, relu et son `runtime` re-vérifié `typeof string` par
`node -e`), T4 (`declare claude` sur un lab déjà `installed=[claude]` → no-op, fichier inchangé).
  </action>
  <acceptance_criteria>
    - `[ -x plugin/conductor/scripts/migrate-lab-runtime.sh ]`.
    - T1/T2 (exécutés) prouvent qu'un `status` seul n'écrit JAMAIS `.planning/config.json` (snapshot `md5`/`cksum` avant/après identique).
    - T3 (exécuté) prouve que `runtime` reste une chaîne APRÈS `declare` : `node -e "const c=require('./config.json'); if (typeof c.runtime !== 'string') process.exit(1)"` sort 0 sur le fichier de sonde.
    - T4 (exécuté) prouve l'idempotence (no-op sur un runtime déjà coexistant).
    - `grep -c 'propagation additive\|migration soustractive' plugin/conductor/skills/vf-calibrate/SKILL.md` rend au moins `1` (la dualité nommée DANS la skill).
    - Le VOCABULAIRE exact (« additive »/« soustractive ») apparaît AUSSI dans la sortie réelle de `migrate-lab-runtime.sh declare` (grep sur une exécution réelle, pas sur le script source seul) — la dualité DANS LA SORTIE, pas seulement dans la doc.
    - `bash plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh</automated>
  </verify>
  <done>Les 3 cas de config.json sont normalisés sans jamais écrire une clé runtime non-chaîne ; la coexistence additive fonctionne et se déclare, dans la skill et dans sa sortie réelle.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : bascule gatée + réversibilité prouvée + statut de fidélité (MIGR-03, MIGR-04, MIGR-05)</name>
  <files>plugin/conductor/scripts/migrate-lab-runtime.sh, plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh, plugin/conductor/scripts/vibeflow-update.sh, plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md, README.md, README.fr.md</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh (état après le lot TGT, 38-04 : le flag `--target`/variable `VIBEFLOW_TARGET_ROOT_OVERRIDE` — la bascule RÉUTILISE ce lot pour poser réellement les artefacts sous la racine du runtime cible, jamais une seconde logique de copie)
    - plugin/conductor/scripts/check-artifact-fidelity.sh (lot FIDE, 38-01 : gate déjà câblé dans l'install — MIGR-05 demande le MÊME gate visible au `status`, jamais un second mécanisme)
    - plugin/conductor/scripts/check-plugin-update.sh (dispatch existant, motif `show_status`/verbe `status` de `vibeflow-update.sh` — point d'ancrage pour afficher le statut de coexistence sans hooks)
    - 38-CONTEXT.md lignes 46-57 (D-38-B point 2 : « dry-run montré → confirmation → écriture. Jamais par défaut » ; point 3 : « un runtime coexistant qui opère sans hooks est déclaré par le gate de fidélité, à l'install et au status »)
  </read_first>
  <behavior>
    - Test : `migrate-lab-runtime.sh switch <runtime> --dry-run` affiche le plan (ce qui deviendrait actif, ce qui resterait installé) sans écrire `.planning/config.json` ni poser aucun artefact.
    - Test : `migrate-lab-runtime.sh switch <runtime>` SANS `--confirm` refuse d'écrire (même après un `--dry-run` précédent — le dry-run n'est jamais lui-même la confirmation, ce sont deux gestes distincts).
    - Test : `migrate-lab-runtime.sh switch <runtime> --confirm` (après un dry-run préalable) écrit `active=<runtime>` dans `runtimes_installed` déjà peuplé, pose les artefacts via `vibeflow-update.sh install --all --target <racine du runtime>`, puis compare l'arbre résultant à un snapshot pré-bascule par `comm` sur deux listes triées de chemins.
    - Test : `switch` vers l'ANCIEN runtime actif (retour) reproduit un arbre IDENTIQUE (par `comm`, zéro ligne des deux côtés) à l'état d'avant la bascule initiale — preuve de réversibilité fichier à fichier.
  </behavior>
  <action>
Ajouter à `migrate-lab-runtime.sh` un verbe `switch <runtime> [--dry-run|--confirm]` (capacité
interne, toujours invoquée UNIQUEMENT par `vf-calibrate`) :
- `--dry-run` (ou aucun flag, défaut sûr) : calcule et affiche ce qui SERAIT écrit
  (`active: <ancien> -> <runtime>`) et ce qui SERAIT posé (liste des artefacts via le plan de
  `vibeflow-update.sh install --dry-run --target <racine cible>`, relayé TEL QUEL — jamais
  réimplémenté), retourne sans écrire.
- Sans `--confirm` : refuse d'exécuter la bascule réelle, message renvoyant vers `--dry-run` puis
  `--confirm` comme DEUX gestes distincts (ADR-031 — jamais une exécution sur un seul appel).
- `--confirm` : exécute réellement — snapshot de l'arbre `TARGET_ROOT` courant AVANT (liste triée
  de chemins relatifs, `find ... | sort`), écrit `active=<runtime>` dans `.planning/config.json`
  (`runtime` reste une chaîne, `runtimes_installed` inchangé sauf ajout si absent), invoque
  `vibeflow-update.sh install --all --target <racine du runtime cible>` (réutilise le lot TGT), puis
  écrit un fichier de preuve `«.planning/.migration-snapshot-<ts>.json»` contenant le snapshot AVANT
  et la racine cible — nécessaire pour qu'un `switch` de retour ultérieur puisse PROUVER la
  réversibilité par comparaison, pas seulement l'affirmer.

Ajouter un mode `migrate-lab-runtime.sh prove-reversibility <runtime>` : exécute
`switch <runtime> --confirm` puis IMMÉDIATEMENT `switch <ancien actif> --confirm` (retour), compare
le snapshot final à celui d'avant le premier `switch` par `comm -3` sur les deux listes triées de
chemins — sortie `IDENTICAL` (zéro ligne de `comm -3`) ou la liste exacte des écarts, jamais un
simple compte de fichiers (D-38-C : « arbre comparé fichier à fichier »).

Câbler `MIGR-05` : dans `vibeflow-update.sh`, la fonction `show_status()` (verbe `status` existant,
dispatch 2147+) affiche, pour chaque runtime de `runtimes_installed` AUTRE que l'actif, une ligne
appelant `check-artifact-fidelity.sh --target <runtime>` sur au moins un artefact représentatif
(même best-effort/silence-si-absent que le câblage FIDE de l'install, 38-01 tâche 2) — le statut de
coexistence sans hooks devient visible SANS relancer une install.

Bump `plugin/conductor/VERSION`/`CHANGELOG.md`. Mesurer FRAÎCHEMENT le compte de suites
(`find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` — ce plan crée
`test-migrate-lab-runtime.sh`, une suite neuve, jamais un chiffre recopié d'un plan antérieur car
38-05 (ADPT) peut avoir déjà bumpé le compte selon l'ordre réel d'exécution) et synchroniser les
deux README sur ce compte mesuré.
  </action>
  <acceptance_criteria>
    - `migrate-lab-runtime.sh switch codex --dry-run` (exécuté sur une sonde) n'écrit RIEN sous `.planning/config.json` ni sous la racine cible (snapshot avant/après identique).
    - `migrate-lab-runtime.sh switch codex` sans `--confirm` sort non-zéro et n'écrit rien.
    - `prove-reversibility` (exécuté réellement sur une sonde `mktemp -d`, jamais sur ce dépôt lui-même) rend `IDENTICAL` — `comm -3` sur les deux listes triées de chemins ne produit AUCUNE ligne — consigner la commande exacte et son résultat dans le SUMMARY.
    - `sed -n '/^show_status/,/^}/p' plugin/_internal/vibeflow-update.sh | grep -c 'check-artifact-fidelity'` rend au moins `1`.
    - Le compte de suites mesuré fraîchement est reflété dans les deux README (`bash scripts/check-version-sync.sh` sort 0), rejeu complet 0 échec.
    - `bash plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh && bash scripts/check-version-sync.sh</automated>
  </verify>
  <done>Une bascule est gatée en 3 temps distincts, jamais par défaut, et sa réversibilité est prouvée par comparaison d'arbre réelle — jamais un compte de fichiers pris pour une preuve. Un runtime coexistant sans hooks est visible au status sans relancer une install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/config.json` -> tous les consommateurs gsd-core (9 sites de lecture identifiés) | ce fichier est un contrat PARTAGÉ avec une dépendance externe hors du contrôle de ce dépôt — toute écriture doit préserver son contrat de lecture, jamais seulement satisfaire l'écriture VibeFlow |
| bascule (`switch --confirm`) -> disque du lab | opération soustractive réelle (l'ancien runtime cesse d'être actif) — seule action de ce plan qui touche l'état vivant d'un lab hors de ce dépôt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-17 | Tampering | `config.json.runtime` écrit en objet, dégradant SILENCIEUSEMENT la résolution runtime de gsd-core sur 9 sites | critical | mitigate | checkpoint:decision explicite (tâche 1) + garde d'assertion `typeof string` après chaque écriture (T3 de la tâche 2) |
| T-38-18 | Elevation of Privilege | bascule exécutée sans confirmation distincte du dry-run | high | mitigate | gate en 3 temps testé explicitement (dry-run seul n'écrit rien ; sans --confirm refuse ; --confirm seul exécute) |
| T-38-19 | Repudiation | réversibilité affirmée sans preuve (compte de fichiers au lieu d'une comparaison d'arbre) | high | mitigate | `prove-reversibility` compare par `comm -3` sur deux listes triées de chemins, jamais un compte |
| T-38-20 | Information Disclosure | un runtime coexistant sans hooks reste invisible au status, un opérateur croit sa mission couverte alors qu'elle ne l'est pas | medium | mitigate | câblage MIGR-05 : `show_status()` appelle le gate de fidélité pour chaque runtime coexistant |
| T-38-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par ce plan — aucun gate de légitimité applicable |
</threat_model>

<verification>
1. `bash plugin/conductor/scripts/tests/test-migrate-lab-runtime.sh` sort 0.
2. `runtime` reste une chaîne après toute écriture, prouvé par assertion `typeof` exécutée.
3. `prove-reversibility` rend `IDENTICAL` sur une sonde isolée, commande et résultat consignés.
4. `bash scripts/check-version-sync.sh` sort 0, rejeu complet des suites fraîchement compté, 0 échec.
</verification>

<success_criteria>
- Les 3 cas de config.json (absent/scalaire/objet) sont normalisés sans jamais casser la lecture `runtime` de gsd-core.
- La dualité additive/soustractive est explicite dans la skill ET dans la sortie réelle.
- Une bascule est gatée en 3 temps distincts et sa réversibilité est prouvée par comparaison d'arbre.
- Un runtime coexistant sans hooks est visible au status, sans relancer une install.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-06-SUMMARY.md` avec : l'option de checkpoint retenue (dual-key ou nested-object) et sa justification, la sortie de `prove-reversibility` (commande + résultat `IDENTICAL` ou écarts), et le compte de suites mesuré fraîchement.

---

## Arbitrage à remonter au manager humain (non bloquant, note de fin de phase)

`.planning/MISSION-INVARIANTS.md` §1 (zones de risque, falsifiables par machine) ne liste NI
`plugin/_internal/vibeflow-update.sh` NI `plugin/_internal/merge-hooks.sh`, alors que la Phase 38
réécrit lourdement le premier (lots FIDE/ROLL/TGT/MIGR, quatre plans sur six) et traverse le second
sans le corriger (lot ROLL, réutilisation de `remove_module_hooks`/`merge_module_hooks`). Le
critère d'admission du fichier — « les catégories où un changement se propage à des consommateurs
absents du diff » — est rempli de façon démontrée par la sonde cross-module `conductor` →
`dev-orchestrator` vérifiée en 38-04 tâche 3 : une évolution du layout d'install QUI NE TOUCHE PAS
`vf-update/SKILL.md` peut quand même casser sa cascade `<S-moteur>`, exactement le mode de
propagation que `MISSION-INVARIANTS.md` §1 existe pour tracer.

Le script `check-mission-invariants.sh` CONSTATE, il n'ajoute ni ne retire jamais une entrée —
c'est un jugement humain. **Proposition à Samuel, non appliquée par cette phase** : ajouter les
deux fichiers à `MISSION-INVARIANTS.md` §1, ou assumer explicitement leur absence par une note
datée dans ce même fichier.
</output>

---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - plugin/skill-creator/skills/skill-creator/SKILL.md
  - plugin/skill-creator/skills/skill-creator-workflow/SKILL.md
  - plugin/skill-creator/VERSION
  - plugin/skill-creator/module.json
  - plugin/skill-creator/README.md
  - plugin/skill-creator/CHANGELOG.md
autonomous: true
requirements: [FABR-08]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Le moteur interne (plugin/skill-creator/skills/skill-creator/SKILL.md) pose la question vf-nature pendant l'interview, via les trois questions factuelles de B-03 (gate bloquant, livrable remis à un tiers, couche de qualité), avec le défaut « outil » écrit en toutes lettres, et fait remplir ecrit: et vf-rubrique-juge: quand la nature déclarée est procedure (D-Q6)"
    - "Le workflow templaté (plugin/skill-creator/skills/skill-creator-workflow/SKILL.md) pose la même question vf-nature, même défaut « outil », dans une étape DISTINCTE de l'étape 4 « Evaluer la nature du sujet », dont le texte reste identique octet pour octet (D-Q2, D-Q6)"
    - "Les deux fichiers nomment les mêmes clés que le gate (vf-nature, ecrit, vf-rubrique-juge, vf-gate-bloquant, vf-livrable-tiers, vf-couche-qualite) et font valider le SKILL.md produit par check-skills.sh --file (rc 0 attendu)"
    - "La nature reste DÉCLARÉE par l'utilisateur ou l'agent orchestrateur : les marqueurs orientent vers « procedure », ils ne la fixent jamais d'office (B-03, C-15)"
    - "Aucune ligne description: des deux fichiers ne change ; aucun titre markdown n'est ajouté ; le moteur interne reste à 500 lignes au plus (plafond ADR-029 que FABR-09 rend bloquant)"
    - "skill-creator passe en mineure (valeur sur disque + 0.1.0) avec VERSION, module.json, ligne Version du README et CHANGELOG cohérents ; aucun bump racine"
  artifacts:
    - path: "plugin/skill-creator/skills/skill-creator/SKILL.md"
      provides: "question vf-nature dans Capture Intent, composant vf-nature dans Write the SKILL.md, validation check-skills.sh --file"
      contains: "vf-nature"
    - path: "plugin/skill-creator/skills/skill-creator-workflow/SKILL.md"
      provides: "étape de Phase 1 « Declarer la nature du skill (vf-nature) », item de checklist Phase 5"
      contains: "vf-nature"
    - path: "plugin/skill-creator/CHANGELOG.md"
      provides: "entrée mineure Phase 43 (FABR-08)"
      contains: "FABR-08"
  key_links:
    - from: "skill-creator (moteur et workflow)"
      to: "plugin/conductor/scripts/check-skills.sh"
      via: "mêmes six clés que VIBEFLOW_SKILL_FIELDS de 43-01 ; validation --file du SKILL.md produit"
      pattern: "check-skills.sh --file"
  prohibitions:
    - statement: "MUST NOT faire poser vf-nature: procedure par skill-creator sans déclaration explicite de l'utilisateur ou de l'agent orchestrateur : les trois marqueurs sont des questions factuelles, la nature reste déclarée, jamais déduite (B-03, C-15)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; changement de prompt agentique, vérifiable par relecture seulement (43-VALIDATION.md, vérification manuelle FABR-08)"
    - statement: "MUST NOT fusionner la question vf-nature avec l'étape « Evaluer la nature du sujet » (méthodologique / agnostique / zone grise), ni redéfinir l'une par l'autre (D-Q2)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; le critère d'acceptation vérifie que l'étape 4 est intacte, pas l'absence de fusion sémantique ailleurs"
---

<objective>
Aligner skill-creator sur le gate des skills : ses deux étages — le moteur interne non templaté et le
workflow templaté propagé aux labs — demandent la nature du skill (`vf-nature`, défaut « outil »), la
font déclarer à partir des trois questions factuelles de B-03, et exigent `ecrit:` et
`vf-rubrique-juge:` quand la réponse est « procédure ».

Purpose: FABR-08 (D-Q6 « les deux », D-Q2 « deux questions distinctes »). C'est aussi le point où le
contrat de clés posé en 43-01 et les marqueurs lus par la dérive (43-02) sont posés à la source : la
spec fabrique §6 (« skill-creator s'aligne ») et C-15 (« un contrat unique » pour les trois
marqueurs).
Output: deux SKILL.md alignés, skill-creator en mineure.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-01-SUMMARY.md

Mesures du 2026-09-25 : le moteur interne fait 485 lignes (plafond 500 — il reste 15 lignes, en viser
au plus 10) ; le workflow templaté fait 301 lignes et écrit son français sans accents (« Evaluer »,
« Methodologique ») — garder ce style dans les ajouts ; le moteur interne est rédigé en anglais —
garder l'anglais, clés et commandes verbatim.
Ordre avec 43-02 (révision du 2026-09-26) : 43-02 dépend de ce plan (depends_on) et s'exécute
APRÈS lui. L'en-tête de check-skills.sh lu ici est celui de 43-01 (contrat des six clés) ; la
détection de dérive n'existe pas encore quand ce plan s'exécute, et `check-skills.sh --file` n'y
juge que FABR-06. 43-02 mesure ensuite le corpus final, ces deux fichiers compris, sous la portée
PORTEE_DERIVE qu'il retiendra (Q2, en attente) : aucun titre n'est ajouté ici ; si la portée
retenue couvre la prose ou le gras, les questions factuelles ajoutées ici peuvent produire des
avertissements de dérive, que 43-02 publie — ce plan ne les anticipe pas.
Référence de phase (B43) : la branche porte encore la Phase 42 tant que la PR #108 n'est pas
mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de la base de
branche — mesuré le 2026-09-25). Tout contrôle « ce que la Phase 43 a touché » part donc du dernier
commit qui touche les PLAN.md de la phase (`git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md'`),
jamais de la base de branche. Les gardes G-1 et G-2 gardent leur propre base : elles jugent toute la
branche, Phase 42 comprise.
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : moteur interne — question vf-nature dans l'interview, composant vf-nature et validation par check-skills.sh (≤ 500 lignes)</name>
  <files>plugin/skill-creator/skills/skill-creator/SKILL.md</files>
  <read_first>
    - plugin/skill-creator/skills/skill-creator/SKILL.md (fichier modifié : l.1-4 frontmatter ; l.44-54 « Capture Intent », quatre questions ; l.62-69 « Write the SKILL.md », liste des composants ; l.86-93 Progressive Disclosure)
    - plugin/conductor/scripts/check-skills.sh (en-tête posé par 43-01 : noms exacts des six clés, forme attendue de ecrit: et vf-rubrique-juge)
    - docs/superpowers/specs/2026-09-22-fabrique-agents-skills-design.md (§6, dernier paragraphe « skill-creator s'aligne »)
    - docs/superpowers/specs/2026-09-23-initialisation-lab-design.md (§5.2, case « les trois marqueurs de B-03 » : au moins un → procédure, aucun → outil ; la réponse est du déclaré)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Code Examples, emplacement 1)
  </read_first>
  <action>
1. « Capture Intent » : ajouter une cinquième question numérotée, en anglais, sur au plus trois lignes : quelle est la nature du skill (`vf-nature`) ; poser les trois questions factuelles de B-03 — does it carry a blocking gate? does it hand a deliverable to a third party? does it carry a quality layer (a judge, a rubric)? — et noter chaque réponse dans `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` (true/false) ; au moins un « yes » → proposer `procedure`, que l'utilisateur confirme ou non (jamais fixée d'office) ; aucun → `outil`, le défaut.
2. « Write the SKILL.md » : ajouter un composant `**vf-nature**` après `**compatibility**`, sur au plus cinq lignes : valeurs referentiel | outil | procedure, défaut `outil` (omettre la clé garde le défaut) ; si `procedure`, remplir aussi `ecrit:` (où vit le livrable, chemin(s) relatif(s)) et `vf-rubrique-juge:` (grille du juge : chemin relatif ou nom de l'agent juge) — sans les deux, `check-skills.sh` refuse le skill (B-03) ; valider ensuite avec `bash .claude/scripts/check-skills.sh --file <skill-path>/SKILL.md` (dans le dépôt VibeFlow : `plugin/conductor/scripts/check-skills.sh`), rc 0 attendu.
3. Ne toucher ni au frontmatter (la ligne description: reste identique), ni à aucun titre, ni à aucune autre section. Le fichier reste à 500 lignes au plus.
4. Commit : `feat(skill-creator): le moteur interne demande vf-nature (FABR-08, D-Q6)` — hors surface G-2.
  </action>
  <verify>
    <automated>f=plugin/skill-creator/skills/skill-creator/SKILL.md; n=$(awk 'END{print NR}' "$f"); for k in vf-nature vf-gate-bloquant vf-livrable-tiers vf-couche-qualite ecrit: vf-rubrique-juge 'check-skills.sh --file'; do grep -qF -- "$k" "$f" || echo "MANQUE $k"; done; python3 -c 'import re,sys; t=open(sys.argv[1],encoding="utf-8").read(); blocs=[re.sub(r"\s+"," ",b.replace(chr(42),"").replace(chr(96),"")) for b in re.split(r"\n[ \t]*\n|\n(?=(?:[-*+]|[0-9]+\.)[ \t])",t)]; rel=re.compile(r"(default|d[eé]faut)\W+(?:\w+\W+){0,3}?outil\b|\boutil\b\W+(?:\w+\W+){0,3}?(default|d[eé]faut)",re.I); n=sum(1 for b in blocs if "vf-nature" in b and rel.search(b)); print("DEFAUT-OUTIL items=%d" % n); sys.exit(0 if n else 1)' "$f" || echo "MANQUE defaut outil"; [ "$n" -le 500 ] || echo "PLAFOND $n"; bash plugin/conductor/scripts/check-skills.sh --file "$f" >/dev/null 2>&1 || echo "GATE-ROUGE"; echo "MOTEUR-FIN lignes=$n"</automated>
    <fails_when>une ligne MANQUE, PLAFOND ou GATE-ROUGE apparaît avant la ligne MOTEUR-FIN. La sonde DEFAUT-OUTIL prouve la RELATION, pas la présence de deux mots : elle découpe le fichier en items de premier niveau (puces, items numérotés, paragraphes ; sous-puces rattachées à leur item), retire gras et accents graves, et exige un même item qui nomme `vf-nature` ET place « default »/« défaut » à trois mots au plus de « outil ». Le mot « default » existe déjà l.36 et l.54 du moteur (hors de toute question de nature) : un grep de présence serait vert sans rien prouver. Mesuré le 2026-09-26 avant écriture : DEFAUT-OUTIL items=0, la sonde rougit.</fails_when>
    <automated>base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && d=$(git diff -U0 "$base" HEAD -- plugin/skill-creator/skills/skill-creator/SKILL.md) && printf '%s\n' "$d" | grep -cE '^[-+](description:|#)'</automated>
    <fails_when>la valeur imprimée n'est pas 0 (une ligne description: ou un titre ajouté/retiré)</fails_when>
  </verify>
  <acceptance_criteria>
    - les six clés et `check-skills.sh --file` figurent dans le fichier ; la sonde DEFAUT-OUTIL du verify rend items ≥ 1 (un même item nomme `vf-nature` et associe le défaut à « outil ») ; `awk 'END{print NR}' plugin/skill-creator/skills/skill-creator/SKILL.md` rend au plus 500
    - `bash plugin/conductor/scripts/check-skills.sh --file plugin/skill-creator/skills/skill-creator/SKILL.md` sort 0
    - le diff du commit ne touche ni description: ni aucun titre
  </acceptance_criteria>
  <done>Le moteur interne pose la question de nature par les trois marqueurs factuels, garde « outil » par défaut, exige ecrit: et vf-rubrique-juge: pour une procédure et fait valider par le gate, sous le plafond de 500 lignes.</done>
</task>

<task type="auto">
  <name>Tâche 2 : workflow templaté — étape vf-nature distincte de « nature du sujet », item de checklist, skill-creator en mineure</name>
  <files>plugin/skill-creator/skills/skill-creator-workflow/SKILL.md, plugin/skill-creator/VERSION, plugin/skill-creator/module.json, plugin/skill-creator/README.md, plugin/skill-creator/CHANGELOG.md</files>
  <read_first>
    - plugin/skill-creator/skills/skill-creator-workflow/SKILL.md (fichier modifié : l.33-58 Phase 1 Cadrage, items 1 à 5 — l'item 4 « Evaluer la nature du sujet » est l'étape existante, DISTINCTE ; l.192-207 Phase 5, « Checklist qualite »)
    - plugin/skill-creator/VERSION, plugin/skill-creator/module.json, plugin/skill-creator/README.md (ligne « **Version** »), plugin/skill-creator/CHANGELOG.md (forme de l'entrée v1.0.4)
    - plugin/conductor/scripts/check-skills.sh (en-tête : noms exacts des clés)
    - .planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md (Code Examples, emplacement 2)
    - CLAUDE.md (discipline de release : aucun bump racine ; commits en français)
  </read_first>
  <action>
1. Phase 1 Cadrage : insérer un nouvel item 5 « Declarer la nature du skill (`vf-nature`, B-03) — question DISTINCTE de la nature du sujet (item 4), jamais fusionnee » : poser les trois questions factuelles (gate bloquant ? livrable remis a un tiers ? couche de qualite ?) et noter `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` (true/false) ; au moins un oui → proposer `procedure`, declaree par l'utilisateur ou [ORCHESTRATING_AGENT], jamais deduite ; defaut `outil` (cle omise) ; si `procedure`, capturer `ecrit:` (lieu de vie du livrable) et `vf-rubrique-juge:` (grille du juge) — sans les deux, `check-skills.sh` refuse. L'ancien item 5 (« **Livrable** : … 00-cadrage.md ») devient l'item 6 et mentionne que le cadrage consigne la nature declaree. L'item 4 et ses trois sous-puces restent identiques octet pour octet. Style sans accents, comme le fichier.
2. Phase 5, « Checklist qualite » : ajouter, juste après l'item « **Type clair** », un item frère « **vf-nature declaree** (B-03, defaut outil) ; si `procedure` : `ecrit:` et `vf-rubrique-juge:` presents — `bash .claude/scripts/check-skills.sh --file <skill-path>/SKILL.md` rend 0 ».
3. Aucun titre markdown ajouté (un titre portant « gate », « bloquant », « qualite » déclencherait la dérive de 43-02 sur ce fichier) ; la ligne description: ne change pas.
4. skill-creator en MINEURE (nouvelle capacité : la question de nature et les clés produites) depuis la valeur lue sur disque (v1.0.4 à la rédaction → v1.1.0) : VERSION, `.version` de module.json, ligne « **Version** » du README, entrée CHANGELOG en tête « ## [<VERSION>] — <date d'exécution ISO> (Phase 43 — nature des skills, FABR-08) », « **Minor** : » + puces : les deux étages demandent `vf-nature` (D-Q6), question distincte de la nature du sujet (D-Q2), trois marqueurs factuels (B-03, C-15), `ecrit:` et `vf-rubrique-juge:` exigés pour une procédure, validation par `check-skills.sh --file` ; décisions citées « Willy, AskUserQuestion, session principale, 2026-09-24 ». Aucune VERSION racine, aucun plugin.json ni marketplace.json.
5. Commits : `feat(skill-creator): le workflow templaté demande vf-nature (FABR-08, D-Q2)` puis `chore(skill-creator <VERSION>): nature des skills (FABR-08)`.
  </action>
  <verify>
    <automated>f=plugin/skill-creator/skills/skill-creator-workflow/SKILL.md; grep -c '^4\. Evaluer la nature du sujet :$' "$f"; grep -cF 'vf-nature' "$f"; grep -cF 'check-skills.sh --file' "$f"; python3 -c 'import re,sys; t=open(sys.argv[1],encoding="utf-8").read(); blocs=[re.sub(r"\s+"," ",b.replace(chr(42),"").replace(chr(96),"")) for b in re.split(r"\n[ \t]*\n|\n(?=(?:[-*+]|[0-9]+\.)[ \t])",t)]; rel=re.compile(r"(default|d[eé]faut)\W+(?:\w+\W+){0,3}?outil\b|\boutil\b\W+(?:\w+\W+){0,3}?(default|d[eé]faut)",re.I); n=sum(1 for b in blocs if "vf-nature" in b and rel.search(b)); print("DEFAUT-OUTIL items=%d" % n)' "$f"; bash plugin/conductor/scripts/check-skills.sh --file "$f" >/dev/null 2>&1; echo "GATE rc=$?"</automated>
    <fails_when>la première valeur n'est pas 1 (étape 4 modifiée ou déplacée), la deuxième vaut moins de 2, la troisième vaut 0, la ligne « DEFAUT-OUTIL items=… » vaut 0, ou la dernière ligne n'est pas « GATE rc=0 ». DEFAUT-OUTIL exige un même item qui nomme `vf-nature` et associe « defaut » à « outil » (gras et accents graves retirés, lignes repliées) : le workflow porte déjà « Default » (l.3, l.113) et « outil » (l.26, l.50, l.75…) dans des items sans rapport — leur présence ne prouve rien. Mesuré le 2026-09-26 avant écriture : items=0.</fails_when>
    <automated>base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && d=$(git diff -U0 "$base" HEAD -- plugin/skill-creator/skills/skill-creator-workflow/SKILL.md) && printf '%s\n' "$d" | grep -cE '^[-+](description:|#)'</automated>
    <fails_when>la valeur imprimée n'est pas 0</fails_when>
    <automated>v=$(cat plugin/skill-creator/VERSION); python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["version"])' plugin/skill-creator/module.json | grep -qx "$v" && grep -q "Version\*\* : $v" plugin/skill-creator/README.md && head -5 plugin/skill-creator/CHANGELOG.md | grep -q "^## \[$v\]" && bash scripts/check-version-sync.sh >/dev/null 2>&1 && echo "SKILL-CREATOR-VERSION-OK $v"</automated>
    <fails_when>la ligne SKILL-CREATOR-VERSION-OK est absente (triade de version incohérente, README ou CHANGELOG non alignés, ou check-version-sync.sh non nul)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^4\. Evaluer la nature du sujet :$' plugin/skill-creator/skills/skill-creator-workflow/SKILL.md` rend 1 et les trois sous-puces Methodologique / Agnostique / Zone grise sont inchangées
    - le workflow contient `vf-nature` au moins deux fois (étape de Phase 1 et item de checklist) et `check-skills.sh --file` au moins une fois ; la sonde DEFAUT-OUTIL rend items ≥ 1 sur le workflow (relation « défaut = outil » dans l'item qui nomme vf-nature — même défaut que le moteur, D-Q6) ; `check-skills.sh --file` sur le fichier sort 0
    - la triade de version de skill-creator est cohérente (valeur sur disque + 0.1.0), `bash scripts/check-version-sync.sh` sort 0, et `base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && liste=$(git diff --name-only "$base" HEAD -- VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json) && printf '%s' "$liste" | grep -c .` rend 0
  </acceptance_criteria>
  <done>Le workflow templaté pose vf-nature dans une étape distincte de la nature du sujet, la checklist l'exige, skill-creator est en mineure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| réponse de l'utilisateur → frontmatter généré | la nature est déclarée, pas déduite |
| workflow templaté → labs installés | le texte est propagé tel quel à chaque lab |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-20 | Tampering | classement automatique | medium | mitigate | le texte dit « proposer procedure, déclarée par l'utilisateur, jamais déduite » ; prohibition relue en PR |
| T-43-21 | Denial of Service | plafond du moteur interne | low | mitigate | ajout borné à dix lignes, compte ≤ 500 vérifié (FABR-09 rend ce plafond bloquant) |
| T-43-22 | Repudiation | attribution dans le CHANGELOG | low | mitigate | décisions citées avec canal et date (Willy, AskUserQuestion, session principale, 2026-09-24) |
</threat_model>

<verification>
- Relecture humaine (43-VALIDATION.md, vérification manuelle FABR-08) : question vf-nature présente dans les deux fichiers, défaut « outil » explicite.
- `check-skills.sh --file` rend 0 sur les deux fichiers ; description: et titres intacts ; moteur ≤ 500 lignes ; triade de version cohérente.
</verification>

<success_criteria>
- FABR-08 : moteur interne ET workflow templaté posent vf-nature, défaut « outil », sans fusion avec la nature du sujet.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| question 5 de « Capture Intent », composant `**vf-nature**` | moteur interne de skill-creator |
| item 5 de Phase 1 « Declarer la nature du skill (vf-nature) », item de checklist « vf-nature declaree » | workflow templaté |
| skill-creator en mineure | version de module |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-08 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici : question distincte et étape 4 intacte (D-Q2), nature proposée mais déclarée (C-15), défaut « outil » par omission de la clé, plafond de 500 lignes du moteur, aucun titre ajouté (pour ne pas créer de dérive sur ces fichiers).
- **Vérification de FABR-08 principalement par relecture** : c'est un changement de prompt agentique (43-VALIDATION.md, Manual-Only) ; les greps et `check-skills.sh --file` prouvent la présence et la forme, pas le comportement de l'agent.
- **Renumérotation** : l'ancien item 5 (livrable du cadrage) devient 6 pour que le cadrage consigne la nature déclarée ; l'item 4 ne bouge pas.

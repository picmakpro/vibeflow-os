---
phase: 41-posture-de-protection-du-d-p-t
plan: 03
lot: L3 — doctrine du volet admin — amendement d'ADR-072 (posture côté serveur, D-02bis), amendement d'ADR-059, CLAUDE.md corrigé
type: execute
wave: 3
depends_on: ["41-02"]
autonomous: true
requirements: [PROT-03, PROT-02]
files_modified:
  - docs/ADR.md
  - CLAUDE.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "ADR-072 existe (plan 41-18) : il est AMENDÉ, jamais recréé ni réécrit — une sous-section `### Amendement du 2026-09-23 — posture côté serveur (volet admin)` est ajoutée en fin d'ADR-072, ouverte par une note `> **ADR-072 : amendement du 2026-09-23 (Phase 41, volet admin)** — …` ; le texte antérieur d'ADR-072 reste octet-identique (il décrit l'état du 2026-09-17/18, conservé comme historique) ; la ligne d'index d'ADR-072 garde sa date et son titre, son statut devient « Validée — amendée le 2026-09-23 (volet admin … »"
    - "L'amendement écrit la politique telle qu'arbitrée : garde-fou par défaut et trace, pas verrou (D-02) ; contournement accordé à Samuel et Willy NOMMÉMENT, deux `User` en mode `always`, jamais par rôle, jamais `exempt` (D-02bis) — et ses conséquences sans adoucissement : pour ces deux comptes, push direct, force push, suppression de `main` et réécriture d'un tag `v*` sont ACCEPTÉS par le serveur et tracés (rule suites, avertissement « Bypassed rule violations », G-3 après coup) ; D-M12 (mode `pull_request`) est remplacée ; tout autre acteur est refusé"
    - "`check-release-tag` n'est PAS un check requis : étape du job `gates`, `main`-only, sautée sur PR ; rouge au push de merge tant que le tag n'est pas posé (demande de Samuel, WhatsApp, 2026-09-23 ; D-M5)"
    - "Ce que GitHub ne documente pas (lequel des runs push/pull_request compte, ce que contourne `--admin` ou un merge REST pour un compte `always`, combinaison des règles de tags) est écrit INCONNU MESURÉ À LA POSE (M-1 à M-4), jamais affirmé ; le retour arrière (`enforcement` → `disabled` par l'admin) est écrit AVANT la pose"
    - "ADR-059 reçoit un amendement daté (D-03) : l'option « Branche pour tout travail de phase », écartée le 2026-07-28, devient la règle par défaut ; le texte d'origine n'est ni supprimé ni réécrit ; l'affirmation d'ADR-072 selon laquelle cet amendement était hors périmètre est dépassée par la reprise (écrit dans l'amendement d'ADR-072)"
    - "`CLAUDE.md` : une section `## Protection côté serveur — main et tags v*` est insérée juste avant `## Gardes in-repo — ce qui est gardé, ce qui ne l'est pas`, et la phrase d'ouverture de cette dernière, devenue fausse le 2026-09-23, est remplacée ; la section « Discipline de release » n'est PAS touchée (la PR #87 la modifie en parallèle — conflit évité) ; la « Limite de fond » reste mot pour mot"
    - "O-3 : partout « gardée par défaut + tracée » une fois la pose prouvée (statut écrit au plan 41-10) ; aucune proposition d'ADR.md ni du `CLAUDE.md` ne l'associe à un mot d'achèvement, même nié ; `tools/check-aucune-fermeture.sh` rend 0"
    - "Commits : D-01..D-10 cités avec « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », D-02bis avec « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 »"
  artifacts:
    - path: "docs/ADR.md"
      provides: "ADR-072 § Amendement du 2026-09-23 + statut d'index ; amendement daté d'ADR-059 + statut d'index"
    - path: "CLAUDE.md"
      provides: "section « Protection côté serveur — main et tags v* » ; ouverture de « Gardes in-repo » corrigée"
  key_links:
    - "L'amendement d'ADR-072 renvoie aux sources exactes : `.github/rulesets/main.json`, `.github/rulesets/tags-v.json`, `.github/CODEOWNERS` ; il est mergé AVANT la pose (D-M14), donc le retour arrière est écrit avant qu'une règle n'existe"
---

<objective>
Écrire la doctrine du volet admin, dans les limites exactes des arbitrages, en AMENDANT ce qui existe :
- ADR-072 : sous-section d'amendement « posture côté serveur », politique de contournement (D-02bis),
  hotfix, retour arrière ;
- amendement daté d'ADR-059 (D-03) ;
- `CLAUDE.md` : section de protection côté serveur et correction de l'ouverture de « Gardes in-repo ».

Révisé le 2026-09-23 (reprise) : l'ancienne version créait ADR-072 et insérait un paragraphe dans
« Discipline de release ». ADR-072 existe depuis le plan 41-18 ; la PR #87 (ouverte le 2026-09-23)
modifie la zone « Discipline de release » : le résumé va dans une section dédiée, adjacente à
« Gardes in-repo ».

Purpose: PROT-03 (politique écrite) et la part documentaire de PROT-02 (le flux de release reste
lisible et juste sous la règle).
Output: `docs/ADR.md` (amendements d'ADR-072 et d'ADR-059, statuts d'index), `CLAUDE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@CLAUDE.md

Extraits de `docs/ADR.md` à lire (ne pas charger le fichier entier) : l'index (lignes `| ADR-059 |`
et `| ADR-072 |`), la note d'amendement d'ADR-029 (format `> **ADR-NNN : amendement du <date>
(Phase …)** — …`), ADR-059 (section `## ADR-059 :` jusqu'à `## ADR-060 :`), ADR-072 en entier
(dernière section du fichier : « Décision — ce qui n'est PAS gardé », « Décision — ce qui attend un
accès admin », « Politique de contournement et de hotfix (PROT-03) », « Compatibilité du flux de
release (PROT-02) »).

Sources de plateforme : `docs.github.com/en/rest/repos/rules` (relue le 2026-09-23 : `User`
accepté, modes `always`/`pull_request`/`exempt` — `exempt` sans entrée d'audit ; `PUT …/rulesets/{id}`
partiel ; `GET …/rulesets/{id}/history`) ; `docs.github.com/en/rest/repos/rule-suites`
(`GET /repos/{owner}/{repo}/rulesets/rule-suites`, résultat `pass`/`fail`/`bypass`) ;
`cli.github.com/manual/gh_pr_merge` (`--admin` : périmètre sous ruleset non documenté).

Fait mesuré le 2026-09-23 : la PR #87 (« une release seulement pour une évolution fonctionnelle »)
insère un paragraphe « Quand publier » entre « Garde-fou machine » et « Numérotation » du `CLAUDE.md`.
Ce plan n'écrit rien dans cette zone.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : amendement d'ADR-072 — posture côté serveur, contournement nommé (D-02bis), hotfix, retour arrière ; statut d'index</name>
  <files>docs/ADR.md</files>
  <read_first>
    - docs/ADR.md (extraits listés dans le contexte)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (§ REPRISE, D-02bis, Contraintes de plateforme, D-02..D-10, Conséquences de D-02, D-M1..D-M14, Deferred)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`ACTEURS-CONTOURNEMENT:`, `PR-EN-VOL-REPRISE:`)
  </read_first>
  <action>
Index : sur la ligne `| ADR-072 | 2026-09-17 | …`, remplacer la seule cellule de statut `Validée`
par `Validée — amendée le 2026-09-23 (volet admin : rulesets de main et des tags v*, contournement
nommé Samuel et Willy)`. Aucune autre ligne d'index modifiée. L'absence de ligne d'index pour
ADR-071 (item BACKLOG « DIFFÉRÉ », déclencheur « prochain passage sur docs/ADR.md ») est constatée
et remontée dans le SUMMARY, pas corrigée dans ce diff.

En fin de fichier (ADR-072 est la dernière section), sans modifier une seule ligne existante,
ajouter la sous-section `### Amendement du 2026-09-23 — posture côté serveur (volet admin)`, dont
la première ligne non vide est la note `> **ADR-072 : amendement du 2026-09-23 (Phase 41, volet
admin)** — ` suivie d'une phrase de synthèse. Contenu, au format des ADR récentes :
- **Contexte de la reprise** : `picmakpro` est le compte de Willy, admin (mesuré le 2026-09-23,
  `41-CONTEXT.md` § REPRISE) ; les sections précédentes de cette ADR décrivent l'état du
  2026-09-17/18 et restent l'historique ; l'affirmation « amendement d'ADR-059 hors périmètre » est
  dépassée (ADR-059 est amendée par la même PR) ; les gardes in-repo G-1, G-2, G-3 restent en place.
- **Décision** : rulesets plutôt que la protection classique (D-M1) ; sources
  `.github/rulesets/main.json` et `.github/rulesets/tags-v.json`, posées octet pour octet depuis
  `origin/main` par l'exécutant sous le jeton admin de Willy, après une confirmation explicite
  (checkpoint de décision du plan 41-05 — D-M6 amendée : la pose n'est plus un geste manuel, elle
  reste gardée par une décision humaine) ; CODEOWNERS `.github/CODEOWNERS` (D-05) ; règles de `main` :
  PR à 0 approbation (D-03), 4 jobs requis épinglés sur l'intégration 15368 et branche à jour (D-04),
  revue code owner (D-05), suppression et force push interdits hors liste de contournement
  (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17), méthodes non restreintes
  (D-10), ni historique linéaire ni signatures (D-M2, D-M3) ; tags `v*` (D-07).
- **Contournement (D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23)** :
  garde-fou par défaut et trace, pas verrou (D-02) ; deux entrées `User` en mode `always` — `samuel-neveugall`
  (151974738) et `picmakpro` (203482067) —, aucune entrée par rôle, jamais `exempt` (pas d'entrée
  d'audit). Tableau à quatre colonnes « Règle | Qui peut contourner | Geste | Trace », lignes :
  PR obligatoire et push direct sur `main` (tout autre acteur — clé de déploiement, application,
  futur collaborateur — refusé ; Samuel et Willy : push accepté par le serveur, avertissement
  « Bypassed rule violations », entrée de rule suite `bypass`, alarme G-3 après coup) ; checks requis
  et revue code owner (geste explicite : case de contournement de l'interface ou
  `gh pr merge --admin` ; appel REST direct : comportement mesuré, M-3 ; trace : rule suite
  `bypass` lisible par `GET /repos/{owner}/{repo}/rulesets/rule-suites`) ; suppression et force push
  de `main` (refusés hors liste ; pour les deux comptes, acceptés et tracés) ; tags `v*` (création
  libre pour tous, D-07 ; réécriture, suppression, force push refusés hors liste, acceptés et tracés
  pour les deux comptes) ; désactivation d'un ruleset (admin seul, trace = historique du ruleset
  `GET …/rulesets/{id}/history`, non exercé) ; mode `exempt` : jamais.
- **Conséquence de D-02bis, écrite sans adoucir** : pour Samuel et Willy, D-03 est « par défaut +
  tracé », pas un verrou — la PR reste la règle, le push direct reste possible et laisse une trace ;
  D-M12 (mode `pull_request`), qui empêchait ce push direct pour les comptes `write`, est remplacée
  par D-02bis ; la borne de D-M5 (un SHA poussé directement sur `main` y porte un run `gates` rouge
  jusqu'au tag) redevient atteignable par ces deux comptes.
- **`check-release-tag`** : n'est PAS un check requis — c'est une étape du job `gates`, `main`-only,
  sautée sur une PR ; elle rougit le push de merge tant que le tag n'est pas posé, signal « tag à
  poser », relance possible, non systématique (D-M5 ; demande de Samuel, WhatsApp, 2026-09-23).
- **Politique hotfix** : branche et PR comme tout travail (D-03) ; si la CI ne peut pas verdir à
  temps, Samuel ou Willy merge en contournant explicitement — la PR et la rule suite en gardent la
  trace ; le push direct leur reste techniquement ouvert, tracé et signalé par G-3, à éviter ; tag
  annoté et release GitHub après le merge, inchangés ; hook `pre-push` conservé (D-M9).
- **PR en vol** : la pose change les conditions de merge de toute PR ouverte vers `main` (au
  2026-09-23 : #87 ; #88 vise une autre branche) — elles sont inventoriées avant et après la pose
  (plan 41-05), jamais mergées par la phase ; une PR ouverte par `picmakpro` qui touche un chemin
  CODEOWNERS ne peut pas recevoir l'approbation de son auteur et passe par un contournement tracé.
- **Retour arrière** (écrit avant la pose) : l'admin repasse chaque ruleset en `disabled` par
  `gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled` ; témoin
  `gh api repos/picmakpro/vibeflow-os/rules/branches/main` rend `[]` pour le ruleset de branche.
- **Conséquences pour O-3** : une fois la pose prouvée, O-3 passe de « signalée et tracée » à
  « gardée par défaut + tracée » (une hausse de baseline exige l'approbation de `@picmakpro` ou un
  contournement explicite tracé) — statut daté écrit au plan 41-10 ; critère 2 reformulé (D-09).
- **Renforcement possible non retenu** : `dismiss_stale_reviews_on_push` reste au défaut `false` ;
  l'activer est une décision à prendre, aucun arbitrage ne la couvre.
- **Inconnus mesurés à la pose** : M-1 (les quatre jobs tournent deux fois sur le même SHA, `push`
  et `pull_request` : lequel compte) ; M-2 (les deux entrées `User` relues côté serveur après la
  pose, `current_user_can_bypass` de `picmakpro` ; celui de `samuel-neveugall` n'est pas mesurable
  depuis le poste de Willy) ; M-3 (ce que contournent `gh pr merge --admin` et un merge REST direct
  pour un compte `always`, et leur trace) ; M-4 (tags : refus pour un acteur hors liste — clé de
  déploiement temporaire — et trace `bypass` pour un compte de la liste) — résultats consignés dans
  `41-PREUVES.md` et reportés dans cette ADR au plan 41-10.
- **Ne couvre pas** : les idées différées du CONTEXT (contrôle de dérive en CI, garde de baseline
  indépendante de GitHub, signatures, passage en organisation).
- **QUAL-01** : le volet admin ne crée aucun script de gate (D-M11) — CODEOWNERS et rulesets sont
  de la configuration ; les trois gates de la phase sont ceux du périmètre sans admin.
Aucun chemin de machine. O-3 n'est associée, même par une négation, à aucun mot d'achèvement.
Commit local sur la branche A, message « docs(41-03): ADR-072 amendée — posture côté serveur (D-03,
D-04, D-05, D-07, D-09, D-10 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ;
D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) ;
REGLES_MAIN_FORCE_PUSH_SUPPRESSION=INCLUSES — arbitrage Samuel, AskUserQuestion session principale,
2026-09-17 ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=docs/ADR.md; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git show "$B:$F" > "$T/o"; trim() { awk "NF{p=NR} {l[NR]=\$0} END{for(i=1;i<=p;i++) print l[i]}" "$1"; }; i=$(awk "/^\\| ADR-072 \\| 2026-09-17 \\|/{c++} END{print c+0}" "$F"); is=$(awk "/^\\| ADR-072 \\| 2026-09-17 \\| .* \\| Validée — amendée le 2026-09-23 \\(volet admin/{c++} END{print c+0}" "$F"); awk "/^\\| ADR-[0-9]+ \\|/ && !/^\\| ADR-0(59|72) \\|/" "$F" > "$T/in"; awk "/^\\| ADR-[0-9]+ \\|/ && !/^\\| ADR-0(59|72) \\|/" "$T/o" > "$T/io"; ix=$(cmp -s "$T/in" "$T/io" && echo oui || echo non); last=$(awk "/^## ADR-[0-9]+ : /{n=\$2} END{print n}" "$F"); am=$(awk "/^### Amendement du 2026-09-23 — posture côté serveur \\(volet admin\\)\$/{c++} END{print c+0}" "$F"); awk "/^## ADR-072 : /{f=1} f" "$T/o" > "$T/s0r"; trim "$T/s0r" > "$T/s0"; awk "/^## ADR-072 : /{f=1} /^### Amendement du 2026-09-23 — posture côté serveur/{f=0} f" "$F" > "$T/s1r"; trim "$T/s1r" > "$T/s1"; hist=$(cmp -s "$T/s0" "$T/s1" && echo oui || echo non); awk "/^### Amendement du 2026-09-23 — posture côté serveur/{f=1} f" "$F" > "$T/a"; nt=$(awk "NR>1 && NF {print; exit}" "$T/a" | awk "index(\$0, \"> **ADR-072 : amendement du 2026-09-23 (Phase 41, volet admin)** — \")==1 {c++} END{print c+0}"); m=0; for k in "D-02bis" "D-03" "D-04" "D-05" "D-07" "D-09" "D-10" "D-M5" "D-M6" "D-M9" "D-M12" "151974738" "203482067" "User" "always" "exempt" "rule-suites" "Bypassed rule violations" "G-3" "enforcement=disabled" ".github/rulesets/main.json" ".github/rulesets/tags-v.json" ".github/CODEOWNERS" "gardée par défaut + tracée" "garde-fou par défaut" "AskUserQuestion session principale, 2026-09-17" "AskUserQuestion session principale, 2026-09-23" "WhatsApp, 2026-09-23" "check-release-tag" "M-1" "M-2" "M-3" "M-4" "pre-push" "hotfix" "QUAL-01" "dismiss_stale_reviews_on_push" "non retenu" "#87" "ADR-059"; do awk -v k="$k" "index(\$0, k) {f=1} END{exit !f}" "$T/a" || { echo "ABSENT : $k"; m=$((m+1)); }; done; echo "adr-072 : index=$i statut_amende=$is autres_lignes_index_intactes=$ix derniere_adr=$last amendement=$am historique_intact=$hist note_d_amendement=$nt termes_absents=$m"; [ "$i" -eq 1 ] && [ "$is" -eq 1 ] && [ "$ix" = oui ] && [ "$last" = "ADR-072" ] && [ "$am" -eq 1 ] && [ "$hist" = oui ] && [ "$nt" -eq 1 ] && [ "$m" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou ligne d'index ADR-072 absente, dupliquée ou sans le statut « Validée — amendée le 2026-09-23 (volet admin » ; ou une autre ligne d'index (hors ADR-059 et ADR-072) modifiée ; ou `derniere_adr=` différent de `ADR-072` ; ou sous-section d'amendement absente ou dupliquée ; ou `historique_intact=non` (une ligne antérieure d'ADR-072 réécrite — l'ADR s'amende, elle ne se réécrit pas) ; ou note d'amendement absente en tête de la sous-section ; ou une ligne `ABSENT :` (décision, identifiant d'acteur, mode, trace, source, retour arrière, inconnu mesuré, hook, gate, canal d'arbitrage, demande WhatsApp, PR en vol ou renforcement non retenu non écrit)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); awk "/^### Amendement du 2026-09-23 — posture côté serveur/{f=1} f" docs/ADR.md > "$T/s"; n=$(awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); m = split(t, c, \"\n\"); for (i = 1; i <= m; i++) if (index(c[i], \" main\") || index(c[i], \"\\140main\")) { if (index(c[i], \"force push\") || index(c[i], \"non_fast_forward\")) nf++; if (index(c[i], \"suppression\") || index(c[i], \"deletion\")) ns++ }; print ns+0, nf+0 }" "$T/s"); ns=${n% *}; nf=${n#* }; echo "amendement ADR-072, regles de main : propositions_suppression_main=$ns propositions_force_push_main=$nf (attendu : les deux au moins 1)"; [ "$ns" -gt 0 ] && [ "$nf" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou aucune proposition de l'amendement ne mentionne la suppression de `main`, ou aucune ne mentionne son force push (arbitrage Samuel, AskUserQuestion session principale, 2026-09-17)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 docs/ADR.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition, O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02) ; ou un fichier illisible (compté en défaut)</fails_when>
  </verify>
  <acceptance_criteria>
    - statut d'index d'ADR-072 amendé, autres lignes d'index intactes (hors ADR-059, Task 2)
    - texte antérieur d'ADR-072 octet-identique ; une sous-section d'amendement en fin de registre, ouverte par la note d'amendement datée
    - l'amendement nomme D-02bis (deux `User`, identifiants, `always`), D-03..D-10, D-M5, D-M6, D-M9, D-M12 remplacée, les traces, les sources, le retour arrière, M-1..M-4, la politique hotfix, `check-release-tag` non requis (demande WhatsApp), les PR en vol et QUAL-01
    - suppression et force push de `main` écrits : refusés hors liste, acceptés et tracés pour Samuel et Willy
  </acceptance_criteria>
  <done>La politique de contournement et de hotfix du volet serveur est écrite et mergeable avant toute pose, sans réécrire l'historique d'ADR-072.</done>
</task>

<task type="auto">
  <name>Task 2 : amendement daté d'ADR-059, CLAUDE.md (section de protection côté serveur, ouverture de « Gardes in-repo » corrigée), rejeu des gates</name>
  <files>docs/ADR.md, CLAUDE.md</files>
  <read_first>
    - docs/ADR.md (ADR-059 jusqu'à `## ADR-060 :`, sous-section « Rules Associées » en fin ; ligne d'index `| ADR-059 |`)
    - CLAUDE.md (§ « Gardes in-repo — ce qui est gardé, ce qui ne l'est pas » en entier ; § « Règle non négociable — Discipline de release », pour ne PAS y toucher)
  </read_first>
  <action>
`docs/ADR.md`, ADR-059 :
- ajouter en fin de section (après « Rules Associées », avant `## ADR-060`) une note
  `> **ADR-059 : amendement du <date d'exécution> (Phase 41, volet admin)** — …` : sur ce dépôt,
  l'option « Branche pour tout travail de phase », écartée le 2026-07-28, devient la règle par
  défaut : toute mise à jour de `main` passe par une PR, 0 approbation (D-03, arbitrage Samuel,
  AskUserQuestion session principale, 2026-09-17) ; le serveur refuse le push direct de tout acteur
  hors liste de contournement (ruleset de branche, ADR-072) ; Samuel et Willy peuvent passer outre,
  et leur passage est tracé (D-02bis, arbitrage Willy, AskUserQuestion session principale,
  2026-09-23) ; les commits directs de documentation ou d'ouverture de phase cessent ; la décision
  d'origine et son tableau d'options restent lisibles tels quels ; la doctrine distribuée aux labs
  n'est pas modifiée par cet amendement ;
- dans l'index, remplacer le seul statut de la ligne ADR-059 par
  `Validée — amendée le <date d'exécution> (Phase 41 : PR obligatoire sur main de ce dépôt, ADR-072)`.
Aucune autre ligne d'ADR-059 modifiée.

`CLAUDE.md` :
- insérer, juste avant le titre `## Gardes in-repo — ce qui est gardé, ce qui ne l'est pas`, une
  section `## Protection côté serveur — main et tags v*` (texte d'une ligne d'introduction puis
  cinq puces au plus) : source versionnée `.github/rulesets/` et `.github/CODEOWNERS`, posée par
  l'admin après le merge de sa source, état réel lisible par
  `gh api repos/picmakpro/vibeflow-os/rules/branches/main` (ADR-072 § Amendement du 2026-09-23) ;
  toute mise à jour de `main` passe par une PR (0 approbation), 4 jobs CI verts épinglés sur GitHub
  Actions, branche à jour, revue `@picmakpro` sur `.github/`, la baseline du budget d'instructions,
  les sentinelles `.planning/.*-armed` et `scripts/hooks/` (D-03, D-04, D-05) ; `check-release-tag`
  n'est pas un check requis — étape du job `gates`, rouge au push de merge tant que le tag n'est pas
  posé (demande de Samuel, WhatsApp, 2026-09-23 ; D-M5), hook `pre-push` conservé (D-M9) ;
  garde-fou par défaut + trace, pas verrou : seuls Samuel et Willy peuvent contourner (D-02bis), par
  un geste explicite sur une PR (case de contournement, `gh pr merge --admin`) — un push direct de
  leur part est accepté par le serveur, inscrit dans les rule suites et signalé par G-3 ; tags `v*` :
  création libre, réécriture et suppression refusées hors liste, contournables et tracées (D-07) ;
- dans `## Gardes in-repo …`, remplacer la phrase d'ouverture (deux lignes), devenue fausse le
  2026-09-23 puisqu'elle affirme l'absence de toute règle serveur et de tout accès admin, par :
  « Les gardes ci-dessous vivent dans le dépôt : elles rendent visible et tracent, elles ne
  verrouillent rien. Elles restent en place à côté de la protection côté serveur décrite à la
  section précédente (ADR-072). » ;
- la puce G-1, G-2, G-3, les paragraphes « Marqueur », « Baseline » et « Limite de fond », et toute
  la section « Règle non négociable — Discipline de release » restent octet-identiques (la PR #87
  modifie cette dernière zone : aucun conflit ne doit naître de ce plan).
Commit local sur la branche A, message « docs(41-03): amendement d'ADR-059 et protection côté
serveur dans CLAUDE.md (D-03, D-04, D-05, D-07 — arbitrage Samuel, AskUserQuestion session
principale, 2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session principale,
2026-09-23) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=docs/ADR.md; T=$(mktemp -d); B=$(git merge-base HEAD origin/main); awk "/^## ADR-059 : /{f=1; next} /^## ADR-060 : /{f=0} f" "$F" > "$T/a59"; git show "$B:$F" | awk "/^## ADR-059 : /{f=1; next} /^## ADR-060 : /{f=0} f" > "$T/o59"; am=$(awk "/^> \\*\\*ADR-059 : amendement du [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9] \\(Phase 41, volet admin\\)\\*\\*/{c++} END{print c+0}" "$T/a59"); m=0; for k in "D-03" "D-02bis" "ADR-072" "AskUserQuestion session principale, 2026-09-17" "AskUserQuestion session principale, 2026-09-23" "Branche pour tout travail de phase"; do awk -v k="$k" "/^> / && index(\$0, k) {f=1} END{exit !f}" "$T/a59" || { echo "ABSENT amendement : $k"; m=$((m+1)); }; done; awk "!/^> \\*\\*ADR-059 : amendement du [0-9-]+ \\(Phase 41, volet admin\\)\\*\\*/ && !(p && /^> /) {print} {p = (/^> \\*\\*ADR-059 : amendement/ || (p && /^> /))}" "$T/a59" | awk "NF{q=NR} {l[NR]=\$0} END{for(i=1;i<=q;i++) print l[i]}" > "$T/r59"; awk "NF{q=NR} {l[NR]=\$0} END{for(i=1;i<=q;i++) print l[i]}" "$T/o59" > "$T/p59"; orig=$(cmp -s "$T/r59" "$T/p59" && echo oui || echo non); idx=$(awk "/^\\| ADR-059 \\| 2026-07-28 \\| .* \\| Validée — amendée le [0-9-]+ \\(Phase 41/{c++} END{print c+0}" "$F"); echo "adr-059 : amendement=$am termes_absents=$m texte_d_origine_intact=$orig index_amende=$idx"; [ "$am" -eq 1 ] && [ "$m" -eq 0 ] && [ "$orig" = oui ] && [ "$idx" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `amendement=` différent de 1 ; ou une ligne `ABSENT amendement :` ; ou `texte_d_origine_intact=non` (une ligne d'ADR-059 hors de la note d'amendement a été réécrite ou supprimée) ; ou `index_amende=` différent de 1</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=CLAUDE.md; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git show "$B:$F" > "$T/o"; ps=$(awk "/^## Protection côté serveur — main et tags v\\*\$/{print NR; exit}" "$F"); ns=$(awk "/^## Protection côté serveur — main et tags v\\*\$/{c++} END{print c+0}" "$F"); pg=$(awk "/^## Gardes in-repo — ce qui est gardé, ce qui ne l.est pas\$/{print NR; exit}" "$F"); nx=$(awk -v s="$ps" "NR>s && /^## /{print NR; exit}" "$F"); awk -v s="$ps" -v e="$pg" "NR>s && NR<e" "$F" > "$T/sec"; m=0; for k in "D-02bis" "D-03" "D-04" "D-05" "D-07" "D-M5" "D-M9" "@picmakpro" ".github/rulesets/" ".github/CODEOWNERS" "rules/branches/main" "check-release-tag" "WhatsApp, 2026-09-23" "pre-push" "rule suites" "G-3" "ADR-072" "Samuel" "Willy"; do awk -v k="$k" "index(\$0, k) {f=1} END{exit !f}" "$T/sec" || { echo "ABSENT claude : $k"; m=$((m+1)); }; done; vieux=$(awk "index(\$0, \"protégée par aucune règle côté serveur\") || index(\$0, \"aucun accès admin sur ce dépôt\") {c++} END{print c+0}" "$F"); lf=$(awk "index(\$0, \"**Limite de fond** : une garde qui vit dans le dépôt peut être modifiée par la PR qu\") {c++} END{print c+0}" "$F"); rel() { awk "/^## Règle non négociable — Discipline de release/{f=1; print; next} /^## /{f=0} f" "$1"; }; rel "$F" > "$T/rn"; rel "$T/o" > "$T/ro"; ri=$(cmp -s "$T/rn" "$T/ro" && [ -s "$T/ro" ] && echo oui || echo non); git diff -U0 "$B" -- "$F" > "$T/d"; rm=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/d"); echo "claude.md : section_serveur=$ns ligne=$ps gardes_in_repo=$pg section_suivante=$nx termes_absents=$m ouverture_fausse_restante=$vieux limite_de_fond=$lf discipline_de_release_intacte=$ri lignes_retirees=$rm"; [ "$ns" -eq 1 ] && [ -n "$pg" ] && [ "$nx" = "$pg" ] && [ "$m" -eq 0 ] && [ "$vieux" -eq 0 ] && [ "$lf" -eq 1 ] && [ "$ri" = oui ] && [ "$rm" -le 2 ]'</automated>
    <fails_when>exit non nul ; ou section « Protection côté serveur » absente, dupliquée, ou pas immédiatement avant « Gardes in-repo » ; ou une ligne `ABSENT claude :` ; ou `ouverture_fausse_restante=` différent de 0 (l'ouverture devenue fausse le 2026-09-23 est encore là) ; ou `limite_de_fond=` différent de 1 (porteur exigé par `check-aucune-fermeture.sh`) ; ou `discipline_de_release_intacte=non` (zone modifiée par la PR #87 touchée) ; ou `lignes_retirees=` supérieur à 2 (une autre ligne du fichier a été réécrite)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh 2>&1) || rc=$?; printf "%s\n" "$out" | awk "/^perimetre: |^allowlist: |^limite: /"; h=$(printf "%s\n" "$out" | awk "!/^perimetre: |^allowlist: |^limite: / && NF {c++} END{print c+0}"); echo "aucune-fermeture rc=$rc hits=$h"; [ "$rc" -eq 0 ] && [ "$h" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `rc=` différent de 0 ; ou `hits=` différent de 0 — une ligne de l'ADR ou du `CLAUDE.md` fait co-occurrer O-3, une garde de la phase ou un de ses scripts avec un jeton d'achèvement hors allowlist, ou un porteur (dont `CLAUDE.md`) a perdu sa formule de limite de fond</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 CLAUDE.md docs/ADR.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` (D-02) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Un rouge qui se reproduit sur une extraction `git archive` d'`origin/main` est préexistant : consigné et remonté `human_needed`, jamais neutralisé</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - amendement daté dans ADR-059 (D-03, D-02bis, canaux et dates), texte d'origine intact, index « amendée le … (Phase 41 »
    - section « Protection côté serveur — main et tags v* » juste avant « Gardes in-repo », termes exigés présents ; ouverture fausse de « Gardes in-repo » remplacée ; « Limite de fond » intacte ; « Discipline de release » octet-identique (aucun conflit avec la PR #87)
    - `check-aucune-fermeture.sh` rc 0 sans hit ; balayage O-3 vert ; rejeux `gates` et `tests` verts ; messages de commit conformes
  </acceptance_criteria>
  <done>La doctrine du volet serveur est écrite, datée et attribuée, sans rouvrir aucune décision ni réécrire ce qui était juste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → gestes futurs | un agent ou un collaborateur qui lit ADR-072 / `CLAUDE.md` agit selon ce qui y est écrit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-11 | Repudiation | doctrine affirmant un comportement GitHub non documenté | medium | mitigate | M-1..M-4 écrits comme inconnus mesurés, résultats reportés au plan 41-10 |
| T-41-12 | Tampering | O-3 présentée comme achevée | medium | mitigate | balayage par proposition d'ADR.md et du `CLAUDE.md` ; `check-aucune-fermeture.sh` rc 0 ; statut O-3 écrit au plan 41-10 après preuve |
| T-41-13 | Denial of service | flux de release rendu illisible ou faux | medium | mitigate | section « Discipline de release » vérifiée octet-identique ; rejeu réel du flux aux plans 41-11 et 41-12 |
| T-41-59 | Repudiation | conséquence de D-02bis adoucie (push direct des deux comptes présenté comme refusé) | high | mitigate | l'amendement et `CLAUDE.md` écrivent l'acceptation tracée du push direct pour Samuel et Willy (termes « Bypassed rule violations », « G-3 », « D-M12 » exigés) |
| T-41-60 | Tampering | historique d'ADR-072 réécrit | medium | mitigate | texte antérieur d'ADR-072 comparé octet pour octet à la base de la branche |
</threat_model>

<verification>
ADR-072 amendée sans réécriture et indexée ; ADR-059 amendée sans perte ; `CLAUDE.md` corrigé et
complété hors de la zone de la PR #87 ; recensement de fermeture, gates et tests verts.
</verification>

<success_criteria>
PROT-03 du volet serveur écrit et mergeable avant la pose, retour arrière compris.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-03-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-volet-admin`). Le SUMMARY remonte l'absence de ligne d'index pour ADR-071 (déclencheur BACKLOG atteint, non corrigé ici).
</output>

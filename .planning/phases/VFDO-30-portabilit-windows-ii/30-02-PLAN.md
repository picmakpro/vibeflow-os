---
phase: 30-portabilit-windows-ii
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [LEDG-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La RFC upstream `open-gsd/gsd-core` est déposée (issue publique) dès le jour 1 du travail du milestone (LEDG-03, deadline amont 2026-10-26)."
    - "Aucun contenu n'a été posté publiquement sans validation humaine explicite du brouillon (D-09)."
    - "Le lien de l'issue est écrit aux DEUX endroits où la Phase 18 ira le chercher : décision datée dans STATE.md et ligne LEDG-03 de REQUIREMENTS.md (D-11)."
    - statement: "Si l'amont refuse ou ignore la RFC avant l'échéance 2026-10-26, la Phase 18 n'est pas bloquée : le repli est la variante réduite locale déjà arbitrée (STUDY.md, verdict GO-RÉDUIT), et ce repli est écrit dans la trace au moment du dépôt, pas découvert à l'échéance."
      verification: backstop
  artifacts:
    - .planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md
  key_links:
    - "Issue upstream ↔ ligne LEDG-03 de REQUIREMENTS.md — sans ce lien, la Phase 18 repart d'une recherche à l'aveugle."
    - "Issue upstream ↔ décision datée de STATE.md — c'est la seule trace qui survit à une clôture de milestone."
  prohibitions:
    - statement: "Ne jamais poster la RFC upstream (ni aucun contenu public) avant validation humaine explicite du brouillon (D-09) — le « jour 1 » n'autorise aucun raccourci sur le gate humain."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais reconstruire ou réécrire REQUIREMENTS.md en entier — édition ciblée de la seule ligne LEDG-03 (le ledger est précisément l'objet que la RFC cherche à protéger)."
      status: flagged-unverified
      verification: flagged-unverified
---

<objective>
Le premier des deux gestes « jour 1 » du milestone : ouvrir la RFC upstream chez
`open-gsd/gsd-core` demandant que la suppression de `.planning/REQUIREMENTS.md` à la clôture de
jalon devienne optionnelle. C'est une **latence externe** — plus elle part tard, plus elle risque
de ne pas revenir avant l'échéance amont du 2026-10-26, et la Phase 18 (survie du ledger) en dépend.

Aucune brique technique : un brouillon, un gate humain, un post, deux traces.

Purpose: démarrer l'horloge externe le jour 1 sans court-circuiter la validation humaine (D-09).
Output: une issue publique, et son lien inscrit là où la Phase 18 ira le chercher (D-11).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-CONTEXT.md
@.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : rédiger le brouillon de la RFC upstream</name>
  <files>.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md</files>
  <read_first>
    - `.planning/phases/VFDO-18-survie-du-ledger-d-exigences-la-cl-ture-de-jalon/STUDY.md` — le
      verdict GO-RÉDUIT et le trou prouvé : `complete-milestone.md` supprime
      `.planning/REQUIREMENTS.md` **inconditionnellement** à chaque clôture de jalon.
    - `.planning/STATE.md` §Decisions, entrée du 2026-07-28 sur la requalification de la Phase 18
      (la RFC y est nommée « dépendance bloquante », échéance 2026-10-26).
    - `.planning/REQUIREMENTS.md` — la ligne LEDG-03 et le tableau de traçabilité (l.~830-845)
      pour connaître la forme exacte des lignes du ledger.
    - Le workflow amont visé, s'il est présent sur le poste :
      `$HOME/.claude/gsd-core/workflows/complete-milestone.md` — citer la ligne réelle qui supprime
      le fichier, jamais une paraphrase de mémoire ; si le fichier est absent, le dire dans le
      brouillon plutôt que d'inventer une référence.
  </read_first>
  <action>
    Écrire le brouillon complet de l'issue, en ANGLAIS (le dépôt amont est anglophone), dans un
    fichier de phase dédié. Structure attendue :

    1. **Titre proposé** — court, factuel, orienté comportement (la suppression du ledger à la
       clôture de jalon devrait être optionnelle).
    2. **Comportement observé** — la citation exacte du workflow amont qui supprime
       `.planning/REQUIREMENTS.md`, avec son chemin et sa version de `@opengsd/gsd-core` telle
       qu'installée sur le poste (relever la version réellement installée, ne pas la supposer).
    3. **Pourquoi c'est un problème** — le ledger d'exigences est le seul registre de « ce que le
       système EST » ; sa suppression inconditionnelle fait perdre les exigences non livrées à
       chaque clôture. Citer le cas réel : la dérive re-constatée à la clôture d'agentique-v1.0,
       avec les exigences 18 et 25 reportées.
    4. **Proposition** — rendre la suppression optionnelle (drapeau de configuration ou
       roll-over des exigences non livrées avec trace d'origine), en laissant le choix du mécanisme
       à l'amont : la RFC demande une capacité, pas une implémentation imposée.
    5. **Ce que fait le repo en attendant** — la variante réduite locale (verdict GO-RÉDUIT de
       `STUDY.md`), pour que l'amont voie que la demande n'est pas bloquante pour nous et qu'un
       refus a un repli connu.
    6. **Échéance côté demandeur** — 2026-10-26, avec la raison (la Phase 18 arbitre son
       implémentation à cette date).

    En tête du fichier, une section française courte « Statut » : `brouillon — non posté`, la date,
    et le rappel que le post est gaté humain (D-09). Cette section n'est PAS destinée à l'issue :
    marquer clairement la frontière entre les métadonnées locales et le corps à poster (par exemple
    un séparateur explicite et un titre de section « Corps de l'issue »).

    Ne poster rien, n'appeler aucune commande réseau à cette tâche.
  </action>
  <verify>
    <automated>test -f .planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md &amp;&amp; grep -qi 'non post' .planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier existe et porte une section « Statut » indiquant l'état non posté.
    - Le corps de l'issue est délimité par un titre de section explicite, séparé des métadonnées locales.
    - Le brouillon cite un chemin de fichier amont ET une version de `@opengsd/gsd-core` relevée sur le poste (commande de relevé consignée dans le SUMMARY), ou dit explicitement que le workflow amont n'a pas pu être lu.
    - Le brouillon nomme l'échéance 2026-10-26 et le repli local (variante réduite).
    - Aucun appel réseau n'a été fait par cette tâche (aucun `gh issue create`, aucun `gh api`).
  </acceptance_criteria>
  <done>Le brouillon complet est sur disque, en anglais pour le corps, et rien n'est encore public.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 2 : validation humaine du brouillon avant tout post public (D-09)</name>
  <read_first>
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md` — le brouillon produit par la tâche 1.
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-CONTEXT.md` — D-09.
  </read_first>
  <what-built>
    Un brouillon d'issue GitHub à déposer sur le dépôt public `open-gsd/gsd-core`, demandant que la
    suppression de `.planning/REQUIREMENTS.md` à la clôture de jalon devienne optionnelle. Rien
    n'est posté : le fichier est local et le dépôt amont n'a reçu aucun appel.
  </what-built>
  <how-to-verify>
    1. Lire `.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md`, section « Corps de l'issue ».
    2. Vérifier trois points : le ton (une demande argumentée, pas un reproche), l'exactitude des
       citations du workflow amont, et le fait que le repli local est mentionné (la RFC ne se
       présente pas comme bloquante pour l'amont).
    3. Décider : poster tel quel, poster après corrections, ou ne pas poster.
  </how-to-verify>
  <acceptance_criteria>
    - La réponse humaine est consignée dans le SUMMARY avec sa date.
    - En cas de corrections demandées, elles sont appliquées au brouillon AVANT la tâche 3, et le brouillon est re-soumis.
    - Aucune tâche de post n'est exécutée tant que la réponse n'est pas « approuvé ».
  </acceptance_criteria>
  <resume-signal>Taper « approuvé » pour autoriser le dépôt public, ou décrire les corrections attendues.</resume-signal>
</task>

<task type="auto">
  <name>Tâche 3 : déposer l'issue upstream et inscrire le lien aux deux endroits (D-11)</name>
  <files>.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <precondition>La tâche 2 a rendu « approuvé » — sans cette réponse humaine, cette tâche ne démarre pas (D-09).</precondition>
  <read_first>
    - `.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md` — le corps validé.
    - `.planning/REQUIREMENTS.md` — la ligne LEDG-03 du tableau de traçabilité et la ligne LEDG-03
      de la section « Survie du ledger d'exigences », pour respecter la forme existante.
    - `.planning/STATE.md` §Decisions — la forme des entrées datées (titre en gras, date, corps).
  </read_first>
  <action>
    Déposer l'issue sur `open-gsd/gsd-core` avec `gh issue create --repo open-gsd/gsd-core`, en
    passant le corps validé par un fichier (`--body-file`), jamais retapé à la main : ce qui est
    posté doit être bit-à-bit ce que l'humain a validé. Le titre est celui du brouillon.

    Si la création échoue (droits, dépôt introuvable, rate limit) : ne pas contourner, ne pas
    réessayer avec un autre canal, arrêter et consigner l'échec avec le message exact dans le
    SUMMARY — un dépôt public raté n'est pas un détail d'exécution.

    Ensuite, trois écritures de trace, toutes ciblées :
    - Dans le brouillon : passer le statut de non posté à posté, avec l'URL de l'issue et la date.
    - Dans `.planning/REQUIREMENTS.md` : compléter la ligne LEDG-03 du tableau de traçabilité avec
      l'URL de l'issue, et la ligne LEDG-03 de la section correspondante. Édition ciblée, jamais
      une réécriture du fichier.
    - Dans `.planning/STATE.md`, section « Decisions » : ajouter une décision datée du jour, en
      français, qui nomme l'issue, son URL, l'échéance amont 2026-10-26 et le repli si l'amont ne
      répond pas. Écrire à la main (édition ciblée), jamais via `gsd-tools state record-session` —
      cette commande réécrit STATE.md et perd le contenu manuel.
  </action>
  <verify>
    <automated>grep -c 'open-gsd/gsd-core/issues' .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue view <url> --repo open-gsd/gsd-core --json url,title,state` retourne une issue ouverte dont le titre est celui du brouillon.
    - Le corps posté est identique au corps validé : le diff entre le corps de l'issue récupéré (`gh issue view --json body`) et la section « Corps de l'issue » du brouillon est vide.
    - `grep -c 'open-gsd/gsd-core/issues' .planning/REQUIREMENTS.md` ≥ 1 et idem pour `.planning/STATE.md`.
    - `git diff --stat .planning/REQUIREMENTS.md` montre une modification de quelques lignes, jamais une réécriture du fichier entier.
    - `bash plugin/conductor/scripts/check-state-integrity.sh` reste vert après l'édition de STATE.md (anti-régression du frontmatter, gate CI).
  </acceptance_criteria>
  <done>L'issue est publique, son URL est dans REQUIREMENTS.md et dans STATE.md, et le brouillon porte le statut posté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → dépôt public tiers | Le corps de l'issue devient public et irrévocable de fait (une issue supprimée reste dans les notifications). |
| brouillon local → corps posté | Tout écart entre ce que l'humain a validé et ce qui est posté est une fuite de contenu non validé. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02-01 | Information Disclosure | corps de l'issue publique | medium | mitigate | Le brouillon est relu par un humain (tâche 2, gate bloquant) avant tout appel réseau ; le post passe par `--body-file` sur le fichier validé, jamais par une saisie reformulée. Interdiction explicite de citer un chemin absolu de machine ou un contenu de lab client. |
| T-30-02-02 | Repudiation | traçabilité de la RFC | medium | mitigate | Double écriture (STATE.md + REQUIREMENTS.md), conformément à D-11 — une seule trace disparaîtrait à la prochaine clôture de jalon, ce qui est précisément le défaut que la RFC dénonce. |
| T-30-02-03 | Spoofing | dépôt cible | low | accept | `--repo open-gsd/gsd-core` est explicite dans la commande ; un dépôt homonyme exigerait un `gh` authentifié sur un autre compte, hors modèle de menace de ce repo. |
| T-30-02-SC | Tampering | installs npm/pip/cargo | low | accept | Aucun paquet installé par ce plan. |
</threat_model>

<assumptions>
- **A-30-02-1 (externe, LEDG-03)** — l'amont peut refuser, ignorer, ou répondre après le
  2026-10-26. Aucun levier de notre côté. Le plan ne prétend pas fermer ce risque : il le borne en
  écrivant le repli (variante réduite locale, GO-RÉDUIT) dans la trace au moment du dépôt, pour que
  la Phase 18 n'ait pas à re-décider dans l'urgence. Sonde `external` consignée, non fermée.
</assumptions>

## Artifacts this phase produces (plan 30-02)

- `.planning/phases/VFDO-30-portabilit-windows-ii/30-RFC-UPSTREAM.md` (brouillon puis trace de dépôt)
- Une issue publique sur `open-gsd/gsd-core` (URL produite à l'exécution)
- Ligne LEDG-03 complétée dans `.planning/REQUIREMENTS.md` (tableau de traçabilité + section)
- Décision datée dans `.planning/STATE.md` §Decisions

<verification>
- L'issue existe et est ouverte (`gh issue view`)
- Le corps posté == le corps validé (diff vide)
- Les deux traces (STATE.md, REQUIREMENTS.md) portent l'URL
- Aucune réécriture massive de REQUIREMENTS.md ni de STATE.md
</verification>

<success_criteria>
1. La RFC est déposée le jour 1 du travail du milestone (LEDG-03).
2. Rien n'a été rendu public sans validation humaine (D-09).
3. Le lien est retrouvable depuis les deux endroits que la Phase 18 consultera (D-11).
</success_criteria>

<output>
Créer `.planning/phases/VFDO-30-portabilit-windows-ii/30-02-SUMMARY.md` à la fin.
</output>

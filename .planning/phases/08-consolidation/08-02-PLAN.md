---
phase: 08-consolidation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - plugin/feature-dev-gates/  (SUPPRIMÉ)
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/retired-modules.txt  (NOUVEAU)
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/planning-core/references/PROFILES.md
  - plugin/planning-core/references/templates/PLAN.template.md
  - plugin/mobile-test-team/README.md
  - plugin/software-architecture/references/principles.md
autonomous: true
requirements: [CONS-01]
must_haves:
  truths:
    - "Le module plugin/feature-dev-gates/ est SUPPRIMÉ (les gates vivent maintenant dans software-architecture)"
    - "Un manifeste _internal/retired-modules.txt liste feature-dev-gates → rules/feature-dev-gates.md (artefact orphelin)"
    - "L'engine, à `update --all`, nettoie les modules retirés : si l'artefact orphelin existe sous TARGET_ROOT OU le module est au registre, il le supprime + désenregistre (converge vers la version finale)"
    - "Le nettoyage NE dépend PAS du cache (le module n'y est plus) — il lit le manifeste en dur"
    - "Un test couvre le nettoyage : registre/artefact présent → supprimé après update --all"
    - "Toutes les mentions doc de feature-dev-gates pointent désormais vers software-architecture"
  artifacts:
    - path: "plugin/_internal/retired-modules.txt"
      provides: "Manifeste des modules retirés + leurs artefacts orphelins"
      contains: "feature-dev-gates"
    - path: "plugin/_internal/vibeflow-update.sh"
      provides: "cleanup_retired_modules() appelé à update --all"
      contains: "retired"
---

# Plan 08-02 — Suppression feature-dev-gates + nettoyage engine + doc refs

## Objectif
Supprimer le module et garantir la CONVERGENCE : un lab qui l'avait installé voit sa rule orpheline
nettoyée au prochain `update --all`. Mettre à jour les mentions doc.

## Tâches
1. `rm -rf plugin/feature-dev-gates/`.
2. `_internal/retired-modules.txt` (nouveau) : `feature-dev-gates:rules/feature-dev-gates.md`.
3. `vibeflow-update.sh` : `cleanup_retired_modules()` — pour chaque ligne du manifeste, si l'artefact
   existe sous TARGET_ROOT OU le module est au registre → supprimer l'artefact + `mark_uninstalled` +
   log. Appel dans `update --all` (après la boucle, avant/après ensure_mandatory_baseline).
4. `tests/test-vibeflow-update.sh` : cas nettoyage (artefact orphelin + entrée registre → disparus).
5. Doc refs → software-architecture : `planning-core/references/PROFILES.md` (2), `PLAN.template.md`,
   `mobile-test-team/README.md`, `software-architecture/references/principles.md`.

## Vérification
- `plugin/feature-dev-gates/` absent ; `grep -rn feature-dev-gates plugin/ --include=*.md` → seules
  mentions historiques/CHANGELOG acceptables, aucune mention active de module vivant.
- `bash _internal/tests/test-vibeflow-update.sh` vert (dont le nouveau cas).

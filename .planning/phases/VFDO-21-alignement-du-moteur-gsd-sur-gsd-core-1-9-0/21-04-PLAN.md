---
phase: 21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0
plan: 04
subsystem: conductor
tags: [state-md, anti-regression, gate, adr-063, cause-analysis]

requires: []
provides:
  - "check-state-integrity.sh (plugin/conductor/scripts/) : completed_phases/completed_plans/
    current_phase de .planning/STATE.md ne décroissent jamais au sein d'un même jalon ; le corps ne
    porte jamais plus d'une ligne ^Phase: — 2 invariants dans le même gate, 23 cas de test, 4
    mutants tués"
  - "ADR-063 : verdict amont/local de l'anomalie d'agrégation (Cause A = dette d'artefact locale,
    Cause B = vrai bug amont non scopé), interdiction documentée de gsd-tools state pour « réparer »
    STATE.md, signalement amont rédigé pour les deux points de Cause B (dépôt réservé à validation
    humaine)"
  - ".planning/STATE.md : convention d'archivage appliquée (Phase: N → **Phase archivée :** N sur
    les 4 sections passées), frontmatter recalé à la main (current_phase 21, +1/+1 plans)"
affects: ["mission 2026-07-31-delta-gsd-core-1.9.0.md (diagnostic déjà établi, non refait)", "20-07 (commentaire YAML du frontmatter renvoyant l'instruction à cette phase)"]

tech-stack:
  added: []
  patterns:
    - "Un gate qui protège un fichier contre deux causes distinctes d'un même défaut de fond
      (régression de compteur ET duplication d'ancre) vit dans un seul script quand les deux
      invariants protègent le même artefact — pas deux scripts séparés pour deux causes d'un même
      incident"
    - "Une garde d'invariant scopée à un contexte (ici : au sein du même jalon) doit être prouvée
      discriminante par comparaison DIRECTE de deux exécutions (rc avec garde vs rc sans le
      contexte qui la désactive), pas seulement testée sur le cas nominal — patron déjà établi par
      test-check-mission-invariants.sh cas 5b"
    - "Distinguer une ref git qui ne résout à rien (erreur d'usage) d'une ref valide dont le
      fichier est absent (cas légitime, rien à comparer) — les confondre dégraderait le gate en
      skip silencieux sur une simple faute de frappe de --against (anti-vert-par-absence, F13)"

key-files:
  created:
    - plugin/conductor/scripts/check-state-integrity.sh
    - plugin/conductor/scripts/tests/test-check-state-integrity.sh
  modified:
    - plugin/conductor/VERSION
    - plugin/conductor/module.json
    - plugin/conductor/CHANGELOG.md
    - plugin/conductor/README.md
    - docs/ADR.md
    - plugin/dev-orchestrator/references/mission-contracts.md
    - .planning/STATE.md

key-decisions:
  - "Aucun patch du paquet tiers (~/.claude/gsd-core/**) — cohérent avec la doctrine déjà posée en
    21-01/21-03 et avec ADR-062 (confirmer avec preuves plutôt que modifier un comportement amont)."
  - "Le backfill des ~20 SUMMARY.md manquants (Phases 11/12/13/14, condition d'auto-cohérence de
    la Cause A) N'EST PAS tranché par ce plan — remonté à Samuel en finding ask-user avec son coût
    (ADR-063 §Décision), exactement le périmètre confié : diagnostiquer et gater, pas trancher le
    fond du désaccord de méthode de comptage."
  - "completed_phases reste à 12 (baseline héritée, non ré-auditée) — l'incrémenter en comptant ce
    plan aurait clôturé la Phase 21 par écriture d'état plutôt que par vérification goal-backward ;
    seuls total_plans/completed_plans montent de 1 (ce plan lui-même, livré)."
  - "Convention d'archivage retenue : **Phase archivée :** (gras, deux-points après le mot, jamais
    en toute première position de ligne sous la forme Phase:) — ne matche pas ^Phase:, lisible
    humainement, cohérente avec le reste du fichier qui utilise déjà le gras pour les verdicts."
  - "Interdiction gsd-tools state propagée dans mission-contracts.md (pas seulement dans l'ADR) :
    c'est la référence que vf-coder/vf-dev-manager consultent réellement en mission — un ADR seul,
    jamais relu en pratique par un agent en plein travail, n'aurait pas empêché la récidive."

patterns-established:
  - "Un correctif de mise en forme (Phase: → **Phase archivée :**) accompagné de son gate machine
    dans le MÊME plan — jamais une convention posée sans l'enforcement qui la rend vérifiable,
    motif déjà établi 3 fois sur ce repo (Phases 13/17/19) et cité explicitement dans le diagnostic
    comme la faute à ne pas répéter."

requirements-completed: ["Anomalie d'agrégation .planning/STATE.md (assignée à la Phase 21 par STATE.md:19 avant recalage)"]

coverage:
  - id: D1
    description: "check-state-integrity.sh échoue (exit 1) si completed_phases, completed_plans ou
      current_phase régressent entre --against (défaut HEAD) et l'état courant, au sein d'un même
      jalon — et NE régresse PAS le verdict quand le jalon change (garde explicite)"
    requirement: "Gate machine anti-régression (digest de mission)"
    verification:
      - kind: unit
        ref: "bash plugin/conductor/scripts/tests/test-check-state-integrity.sh → 23 ok, 0 ko"
        status: pass
      - kind: mutation
        ref: "4 mutants appliqués manuellement (garde de régression, garde ^Phase:, garde de
          jalon, garde de ref --against) — chacun fait échouer entre 1 et 6 cas de la suite,
          aucun survivant ; script restauré identique par diff après chaque mutation"
        status: pass
    human_judgment: false
  - id: D2
    description: "check-state-integrity.sh échoue si le corps de .planning/STATE.md porte plus
      d'une ligne ^Phase: (cause B) ; la convention d'archivage (**Phase archivée :**) ne déclenche
      jamais ce gate"
    requirement: "Convention d'archivage + gate (ADR-063)"
    verification:
      - kind: unit
        ref: "grep -c '^Phase:' .planning/STATE.md == 1 ; bash plugin/conductor/scripts/check-state-integrity.sh → exit 0"
        status: pass
      - kind: mutation
        ref: "cas 10 de la suite compare directement rc(1 ligne)=0 vs rc(2 lignes, motif exact de
          la cause B réelle)=1"
        status: pass
    human_judgment: false
  - id: D3
    description: "ADR-063 documente le verdict amont/local, pose l'interdiction gsd-tools state, et
      rédige le signalement amont pour les deux points de Cause B — sans patcher le paquet tiers ni
      trancher seul le backfill des SUMMARY.md manquants"
    requirement: "Arbitrage écrit (digest de mission)"
    verification:
      - kind: unit
        ref: "grep -c '^## ADR-063' docs/ADR.md == 1 ; grep -c 'gsd-tools state' plugin/dev-orchestrator/references/mission-contracts.md >= 1"
        status: pass
      - kind: manual
        ref: "ADR-063 §Décision cite les lignes exactes de state.cjs/state-document.cjs/plan-scan.cjs/
          roadmap.cjs vérifiées sur pièce ; §Options Considérées liste explicitement le backfill
          comme option NON tranchée ici, remontée à Samuel"
        status: pass
    human_judgment: true

## Contexte

Rituel allégé (arbitrage Samuel, cf. 21-01/21-02/21-03) : pas de cadrage `gsd-discuss-phase`
séparé — le digest de mission (nœud `p21-exec-04`) fournit un périmètre exhaustif et un diagnostic
**déjà établi et reproduit** sur `@opengsd/gsd-core` 1.9.0 (`bin/lib/state.cjs`,
`bin/lib/plan-scan.cjs`, `bin/lib/state-document.cjs`, `bin/lib/roadmap.cjs`) — ce plan implémente
la remédiation, il ne refait pas le diagnostic. Dernier plan de la Phase 21 : clôt l'anomalie
d'agrégation assignée à cette phase par le commentaire `.planning/STATE.md:19` de la clôture 20-07.

## Ce qui a été fait (résumé exécutable — détail complet dans le SUMMARY)

1. **Gate `check-state-integrity.sh`** (module `conductor` v1.18.0) : deux invariants dans le même
   script — régression de compteur au sein d'un même jalon, et exactement une ligne `^Phase:` dans
   le corps. Suite dédiée 23 cas (2 discriminations machine par comparaison directe), 4 mutants
   tués manuellement avant livraison.
2. **ADR-063** : verdict Cause A (dette d'artefact locale, `buildStateFrontmatter` sans repli
   ROADMAP) / Cause B (vrai bug amont, `stateExtractField('Phase')` non scopé) ; décision de gate
   local + interdiction documentée de `gsd-tools state` pour « réparer » `STATE.md` (propagée dans
   `mission-contracts.md`) ; signalement amont rédigé pour les deux points de Cause B, dépôt
   réservé à validation humaine ; backfill des `SUMMARY.md` manquants explicitement **non tranché**,
   remonté à Samuel.
3. **`.planning/STATE.md`** : les 4 sections archivées (`Phase: 19/17/16/13`) reformées en
   `**Phase archivée :** N` ; frontmatter recalé à la main (`current_phase: 21`, `total_plans`/
   `completed_plans` +1/+1, `completed_phases` inchangé à 12) avec commentaire YAML expliquant la
   curation et le point ouvert (lecture ROADMAP-trust vs baseline héritée) ; narration de la
   Phase 21 ajoutée sous `## Current Position`.

Aucun fichier hors périmètre touché. Aucun `gsd-tools state.*` invoqué (c'est précisément le défaut
instruit — l'aurait pollué la mesure).

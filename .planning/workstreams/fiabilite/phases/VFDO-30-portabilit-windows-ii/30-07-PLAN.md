---
phase: 30-portabilit-windows-ii
plan: 07
type: execute
wave: 3
depends_on: ["30-01", "30-04", "30-05"]
files_modified:
  - plugin/dev-orchestrator/hooks/hooks.json
  - plugin/_internal/tests/test-vibeflow-update.sh
  - docs/ADR.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/dev-orchestrator/README.md
  - plugin/software-architecture/VERSION
  - plugin/software-architecture/module.json
  - plugin/software-architecture/CHANGELOG.md
  - plugin/software-architecture/README.md
autonomous: true
requirements: [PORT-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Les 5 entrées de hook du périmètre dev (4 de `dev-orchestrator`, 1 de `software-architecture`) sont en forme exec, et chacune est classée advisory ou bloquante EXPLICITEMENT (contrat §7, critère de succès n°2 de la spec)."
    - "Aucune de ces entrées ne dépend plus d'une construction shell : l'opérateur d'absorption a disparu par construction, remplacé par la traduction posée dans chaque script au plan 30-04."
    - "L'ordre (a) moteur → (b) codes de sortie → (c) fragments est tenu par la dépendance de plan : cette migration ne peut pas s'exécuter avant que le moteur ne lise `args` et que les scripts ne traduisent leur silence."
    - "Un lab qui installe ces deux modules reçoit un settings.json dont les entrées VF portent un chemin absolu d'interpréteur et aucun placeholder littéral — prouvé par une install réelle dans un lab temporaire, pas par lecture du fragment."
    - "La doctrine est gravée : un ADR de suite documente la forme exec, le chemin absolu figé à l'install et le contrat de sortie — parce que ce changement modifie ce qui s'écrit CHEZ l'utilisateur (spec §5)."
  artifacts:
    - plugin/dev-orchestrator/hooks/hooks.json
    - docs/ADR.md
  key_links:
    - "Fragment exec ↔ traduction du code de sortie : migrer avant la traduction transformerait chaque démarrage de session sain en erreur affichée (spec §1.4)."
    - "Fragment exec ↔ moteur qui lit `args` : migrer avant le moteur produirait un placeholder littéral, un hook doublé et un module non désinstallable (spec §1.3)."
  prohibitions:
    - statement: "Ne jamais écrire un nom nu d'interpréteur dans le champ de commande d'une entrée exec — il est résolu sur le PATH, ce qui reproduirait le bug corrigé."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais migrer un fragment de la polarité gouvernance dans cette phase, même « pendant qu'on y est »."
      status: flagged-unverified
      verification: flagged-unverified
    - statement: "Ne jamais toucher à la version racine du dépôt, aux fiches de plugin ou de marketplace : la release racine est un geste humain gaté (règle non négociable du CLAUDE.md du dépôt)."
      status: flagged-unverified
      verification: flagged-unverified
---

<objective>
Le lot (c), et seulement maintenant : les 4 entrées `SessionStart` de `dev-orchestrator` passent en
forme exec. Le moteur sait lire, dédupliquer et retirer cette forme (plan 30-01) ; les 4 scripts
traduisent leur silence à la frontière du harness (plan 30-04) ; les 3 fichiers du lot PYBIN sont
migrés (plan 30-05). L'ordre imposé par la spec §2 est tenu par les dépendances de ce plan, pas par
une consigne de prudence.

Puis on grave la doctrine : la forme exec change ce qui s'écrit chez l'utilisateur, elle mérite son
ADR de suite (spec §5).

Purpose: rendre les hooks du périmètre dev réellement portables sous Windows, chemin à espaces compris.
Output: 5 entrées en forme exec classées, une preuve d'install réelle, un ADR, deux modules versionnés.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-CONTEXT.md
@.planning/phases/VFDO-30-portabilit-windows-ii/30-PATTERNS.md
@docs/HOOKS-CONTRAT-SORTIE.md
@docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : migrer les 4 entrées de `dev-orchestrator` en forme exec, classées</name>
  <files>plugin/dev-orchestrator/hooks/hooks.json</files>
  <read_first>
    - `plugin/dev-orchestrator/hooks/hooks.json` — les 4 entrées actuelles en forme shell, et sa clé de description.
    - `plugin/software-architecture/hooks/hooks.json` tel que migré par le plan 30-01 — le gabarit
      exact à répliquer (nom du jeton d'interpréteur, découpage des arguments, forme de la description).
    - `plugin/_internal/merge-hooks.sh` tel que modifié par le plan 30-01 — pour confirmer le nom du
      jeton d'interpréteur attendu par le moteur, plutôt que de le supposer.
    - `docs/HOOKS-CONTRAT-SORTIE.md` — le classement des 4 entrées (advisory par ADR-031) et la raison.
    - Le contrat amont §5 (`git show origin/gouvernance/contrat-portabilite:docs/CONTRAT-PORTABILITE.md`) :
      chaque drapeau est un élément d'arguments SÉPARÉ ; un drapeau à valeur accolée reste un seul élément.
  </read_first>
  <action>
    Passer les 4 entrées `SessionStart` du fragment en forme exec, selon le gabarit déjà posé sur
    l'entrée de `software-architecture` : le type reste inchangé ; le champ de commande porte le
    jeton d'interpréteur que le moteur substitue par un chemin absolu à l'install ; le champ
    d'arguments porte, en éléments séparés, le chemin du script (jeton de chemin de scripts +
    nom du script) puis le drapeau de mode hook. L'opérateur d'absorption disparaît de chaque ligne
    — il n'est pas exprimable sans shell, et il n'a plus de raison d'être puisque les 4 scripts
    traduisent désormais leur silence eux-mêmes.

    Mettre à jour la clé de description du fragment pour porter le classement EXIGÉ par le contrat
    §7 : les 4 entrées sont **advisory** (ADR-031 — chaque signal propose un geste, aucun ne
    l'exécute), en forme exec, et leur silence nominal est garanti par la traduction du code de
    sortie et non plus par un opérateur shell. Nommer le document de contrat de sortie.

    Ne toucher à aucun autre fragment. Ne pas modifier les scripts eux-mêmes (ils sont figés par le
    plan 30-04).
  </action>
  <verify>
    <automated>python3 -m json.tool plugin/dev-orchestrator/hooks/hooks.json &gt;/dev/null &amp;&amp; bash plugin/_internal/tests/test-merge-hooks.sh</automated>
  </verify>
  <acceptance_criteria>
    - `plugin/dev-orchestrator/hooks/hooks.json` est un JSON valide et ses 4 entrées portent chacune une clé d'arguments non vide.
    - Aucune entrée ne porte plus de construction shell : `python3 -c "import json;d=json.load(open('plugin/dev-orchestrator/hooks/hooks.json'));c=[h['command'] for gs in d['hooks'].values() for g in gs for h in g['hooks']];assert all(' ' not in x for x in c), c"` passe (le champ de commande ne contient plus ni espace, ni opérateur, ni chemin de script).
    - Chaque entrée porte le drapeau de mode hook comme élément d'arguments SÉPARÉ (jamais collé au chemin du script).
    - La description du fragment contient le mot `advisory` et renvoie au document de contrat de sortie.
    - Le décompte du parc est inchangé : la commande de recomptage du plan 30-04 rend toujours 25.
    - `bash plugin/_internal/tests/test-merge-hooks.sh` reste vert.
  </acceptance_criteria>
  <done>Les 4 entrées sont en forme exec, classées advisory explicitement, et le moteur les avale sans broncher.</done>
</task>

<task type="auto">
  <name>Tâche 2 : preuve par install réelle dans un lab temporaire</name>
  <files>plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - `plugin/_internal/tests/test-vibeflow-update.sh` — le harness et les cas d'install existants
      (isolation `mktemp`, invocation de l'engine avec le cache du dépôt et un scope projet).
    - `plugin/_internal/vibeflow-update.sh` — `merge_module_hooks()` et `scripts_prefix_for_scope()`,
      pour savoir ce qui est réellement écrit dans le settings.json du lab.
    - `plugin/dev-orchestrator/hooks/hooks.json` et `plugin/software-architecture/hooks/hooks.json`
      tels que migrés.
  </read_first>
  <action>
    Ajouter à la suite d'intégration de l'engine un cas qui prouve la forme exec TELLE QU'INSTALLÉE,
    pas telle que déclarée dans le fragment — c'est la distinction qui a manqué à la régression #38
    (un gate qui n'exerce que l'arbre source ne prouve rien sur ce que l'install pose chez
    l'utilisateur).

    Le cas : dans un lab temporaire, installer les deux modules du périmètre dev avec l'engine réel,
    puis asserter sur le settings.json produit :
    - chaque entrée VF posée par ces deux modules est en forme exec (elle porte un champ d'arguments) ;
    - son champ de commande est un chemin ABSOLU, existant et exécutable sur cette machine ;
    - aucun placeholder littéral ne subsiste nulle part dans le fichier ;
      <!-- planner-discipline-allow: {{ — l'assertion porte sur le settings.json GÉNÉRÉ dans un lab temporaire, jamais sur un fichier source écrit par cette tâche -->
    - le nombre d'entrées VF posées correspond exactement au nombre d'entrées déclarées par les deux
      fragments (ni doublon, ni perte) ;
    - désinstaller les deux modules laisse ZÉRO entrée VF résiduelle et un settings.json valide.

    Puis le cas de compatibilité descendante, le plus important pour le parc installé : préparer le
    settings.json du lab AVANT l'install avec les entrées de ces deux modules en ANCIENNE forme
    shell (telles qu'un lab installé avant cette phase les porte), puis lancer l'install. Attendu :
    les anciennes entrées shell ont été RETIRÉES et remplacées par les entrées exec, une seule par
    script, aucun doublon — c'est-à-dire exactement le scénario d'update d'un utilisateur existant.

    Garde anti-vert-à-vide : si l'install ne pose aucune entrée VF, le cas ÉCHOUE au lieu de passer.
    Mutation à jouer et à citer dans le SUMMARY : remettre une entrée en forme shell dans un
    fragment ⇒ le cas de forme devient rouge en nommant l'entrée fautive.
  </action>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0 et affiche « 0 KO ».
    - Le cas de forme exec échoue si l'install ne pose aucune entrée VF (garde anti-vert-à-vide prouvée, trace citée dans le SUMMARY).
    - Le cas de compatibilité descendante prouve, par exécution, qu'un lab portant l'ancienne forme se retrouve avec UNE seule entrée par script après update.
    - Le chemin d'interpréteur écrit dans le settings.json du lab temporaire satisfait `[ -x "$chemin" ]` sur la machine d'exécution.
    - Mutation jouée : une entrée remise en forme shell rend le cas rouge, avec le nom de l'entrée dans le message.
  </acceptance_criteria>
  <done>La forme exec est prouvée telle qu'installée, y compris sur le chemin d'update d'un lab qui portait l'ancienne forme.</done>
</task>

<task type="auto">
  <name>Tâche 3 : ADR de suite + versions des deux modules dev</name>
  <files>docs/ADR.md, plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md, plugin/dev-orchestrator/README.md, plugin/software-architecture/VERSION, plugin/software-architecture/module.json, plugin/software-architecture/CHANGELOG.md, plugin/software-architecture/README.md</files>
  <read_first>
    - `docs/ADR.md` — l'ADR-054 (premier temps de la portabilité Windows, l.601-694) pour s'y
      rattacher explicitement, et l'ADR-070 (le dernier posé) pour la forme et la numérotation.
    - `docs/HOOKS-CONTRAT-SORTIE.md` — le contrat à citer plutôt qu'à recopier.
    - `scripts/check-version-sync.sh` l.103-121 — ce que le gate exige d'un bump de module.
    - Les `CHANGELOG.md` et `README.md` des deux modules — forme des entrées et présence éventuelle
      d'un en-tête de version.
    - Le `CLAUDE.md` du dépôt — la règle non négociable sur la release racine, pour ne pas la franchir.
  </read_first>
  <action>
    **(1) L'ADR.** Ajouter l'ADR suivant le dernier numéro posé, en français, dans la forme des
    ADR existants. Il grave trois décisions, chacune avec sa raison et sa conséquence :
    - la forme exec devient la forme des entrées de hook du périmètre dev — c'est la présence du
      champ d'arguments qui bascule, le type ne change pas, et l'opérateur d'absorption disparaît
      par construction ;
    - le champ de commande porte un chemin absolu d'interpréteur résolu ET VÉRIFIÉ à l'install, avec
      sa conséquence assumée : le settings.json produit devient spécifique à la machine (D-01,
      one-way, ratifié au checkpoint du plan 30-01) ;
    - le contrat de sortie vers le harness est normalisé DANS chaque script, sous son drapeau de
      mode hook, sans lanceur intermédiaire (D-06) ; le contrat de signaux interne de la Phase 17
      reste valide à l'intérieur des scripts.
    Rattacher explicitement à l'ADR-054 (second temps de la même doctrine, pas une révision : CRLF,
    préflight et gate de synchro restent tels quels) et renvoyer à la spec de la phase et au
    document de contrat de sortie. Nommer ce qui reste à la polarité gouvernance.

    **(2) Les versions.** Bumper `dev-orchestrator` et `software-architecture` en version mineure
    (changement de forme des hooks et de contrat de sortie). Pour chacun : version du module,
    version du manifeste, entrée de journal des changements en français, et en-tête de version du
    README quand il en porte un. Ne toucher NI à la version racine du dépôt, NI aux fiches de
    plugin ou de marketplace : la release racine est un geste humain gaté, hors périmètre de phase.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ADR.md` contient un nouvel ADR dont le numéro suit immédiatement le dernier existant, avec ses trois décisions et le renvoi explicite à l'ADR-054.
    - `bash scripts/check-version-sync.sh` sort 0 (triades des deux modules cohérentes, en-têtes de README suivis).
    - Les deux journaux de changements portent une entrée datée en français décrivant la forme exec et le contrat de sortie.
    - `git diff --name-only | grep -cE '^(VERSION|\.claude-plugin/marketplace\.json|plugin/\.claude-plugin/plugin\.json)$'` == 0.
    - `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir plugin/dev-orchestrator/agents` reste vert (aucun agent touché, gate ADR-044 inchangé).
  </acceptance_criteria>
  <done>La doctrine est gravée, les deux modules sont versionnés, et la release racine reste intacte pour la validation humaine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fragment migré → settings.json de tout lab installé | Un changement de forme qui atteint l'état vivant de chaque utilisateur au premier update. |
| install réelle → assertions de la suite | La preuve porte sur ce qui est POSÉ, pas sur ce qui est déclaré. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07-01 | Denial of Service | hook doublé sur le parc installé | high | mitigate | La migration est verrouillée derrière les plans du moteur et des codes de sortie par `depends_on` ; le cas de compatibilité descendante prouve par exécution qu'un lab portant l'ancienne forme ne se retrouve pas avec deux entrées. |
| T-30-07-02 | Elevation of Privilege | nom d'interpréteur nu résolu sur le PATH | high | mitigate | Assertion machine sur le settings.json posé : chemin absolu, existant, exécutable. Aucune entrée ne peut porter un nom nu sans faire rougir la suite. |
| T-30-07-03 | Spoofing | preuve verte obtenue sans rien exercer | high | mitigate | Garde anti-vert-à-vide sur le nombre d'entrées VF posées, et mutation qui prouve que le cas rougit quand une entrée redevient shell (motif du faux vert de la Phase 27). |
| T-30-07-04 | Tampering | dérive de périmètre vers les fragments gouvernance | medium | mitigate | Critère machine sur le diff : seul le fragment de `dev-orchestrator` est modifié par ce plan. |
| T-30-07-SC | Tampering | installs npm/pip/cargo | low | accept | Aucun paquet installé par ce plan. |
</threat_model>

<assumptions>
- **A-30-07-1 (compat descendante, probe compat)** — le parc installé porte aujourd'hui 100 % de
  forme shell (vérifié : aucune occurrence de champ d'arguments dans les 6 fragments source). La
  compatibilité descendante est donc exercée dans le sens qui compte réellement (shell existant →
  exec nouveau) ; le sens inverse est couvert par le plan 30-01 pour le cas d'un rollback de fragment.
- **A-30-07-2 (labs non mis à jour)** — un lab qui ne relance pas l'engine garde ses hooks en forme
  shell et ses anciens scripts : comportement attendu, pas une régression. Le fait doit être écrit
  dans le journal des changements des deux modules pour éviter la question « pourquoi ma garde ne
  bloque toujours pas sans Python » (RESEARCH §Runtime State Inventory).
</assumptions>

## Artifacts this phase produces (plan 30-07)

- `plugin/dev-orchestrator/hooks/hooks.json` — 4 entrées en forme exec, classées advisory dans la description
- Cas d'intégration « forme exec telle qu'installée » et « compatibilité descendante à l'update » dans `plugin/_internal/tests/test-vibeflow-update.sh`
- Nouvel ADR de suite (numéro suivant le dernier posé) dans `docs/ADR.md`
- Bumps mineurs : `dev-orchestrator`, `software-architecture` (version, manifeste, journal, en-tête de README)

<verification>
- `python3 -m json.tool plugin/dev-orchestrator/hooks/hooks.json` → valide
- `bash plugin/_internal/tests/test-vibeflow-update.sh` → 0 KO, garde anti-vert-à-vide prouvée
- `bash plugin/_internal/tests/test-merge-hooks.sh` → vert
- `bash scripts/check-version-sync.sh` → vert
- La commande de recomptage du parc rend toujours 25
</verification>

<success_criteria>
1. Les 5 entrées du périmètre dev sont en forme exec et classées explicitement (PORT-02, contrat §7).
2. Aucune ne dépend d'une construction shell.
3. Un update depuis l'ancienne forme ne double jamais un hook et laisse un module désinstallable.
4. La doctrine est gravée dans un ADR de suite ; la release racine reste au geste humain.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-30-portabilit-windows-ii/30-07-SUMMARY.md` à la fin.
</output>

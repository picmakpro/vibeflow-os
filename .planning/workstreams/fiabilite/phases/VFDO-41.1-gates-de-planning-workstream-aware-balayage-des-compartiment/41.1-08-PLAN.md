---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 08
lot: L08 — bump patch des trois modules touchés (conductor, planning-core, dev-orchestrator), CHANGELOG, rejeu final
type: execute
wave: 5
depends_on: ["41.1-01", "41.1-02", "41.1-03", "41.1-04", "41.1-05", "41.1-06", "41.1-07"]
autonomous: true
requirements: [WSAW-01, WSAW-02, WSAW-03, WSAW-04, WSAW-05, WSAW-06, WSAW-07]
files_modified:
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - plugin/planning-core/VERSION
  - plugin/planning-core/module.json
  - plugin/planning-core/CHANGELOG.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "PATCH pour les trois modules — durcissement de gates existants et correction de faux résultats, PAS de nouvelle capacité utilisateur-visible (CLAUDE.md racine : nouveau module/nouvelle capacité → minor, correctif/durcissement → patch). `conductor` : v1.40.0 → v1.40.1 (ou la valeur COURANTE au moment de l'exécution, RELUE sur disque, jamais recopiée de ce plan — un autre plan a pu bumper entre-temps). Même règle pour `planning-core` (v2.7.0 → v2.7.1) et `dev-orchestrator` (v2.23.1 → v2.23.2)"
    - "Bump COHÉRENT dans les DEUX fichiers de chaque module (`VERSION`, `module.json` champ `version`) — jamais un seul des deux, patron `check-version-sync.sh` déjà en gate CI"
    - "La `VERSION` RACINE (`/VERSION`), `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, les deux README (`README.md`/`README.fr.md`) et le `CHANGELOG.md` RACINE ne sont PAS touchés par ce plan — hors périmètre explicite (release en vol ailleurs, cf. mandat)"
    - "CHANGELOG de chaque module : une entrée `## [vX.Y.Z] — <date d'exécution>` qui nomme les fichiers livrés PAR CE MODULE dans cette phase — pas une recopie générique des trois modules dans chaque fichier. `conductor` : `check-divergence.sh` (dédup, trou fermé), `workstream-planning-consumers.md`, `check-planning-consumers-registered.sh`. `planning-core` : `workstream-policy.sh` (`vf_ws_enumerate`), `check-planning-state.sh`, `detect-gsd-engine.sh`. `dev-orchestrator` : `discover-unintegrated-docs.sh`, `references/workstreams.md`"
    - "`git status --porcelain` (jamais `rtk` pour cette vérification précise — mesurer en `rtk proxy git status --porcelain`, motif mémoire `rtk-fausse-les-verifications-d-etat`) ne liste, avant le commit de ce plan, QUE les chemins des plans 41.1-01 à 41.1-07 déjà commités PLUS les neuf fichiers de ce plan — aucun fichier hors périmètre touché par accident"
    - "Rejeu final COMPLET des deux jobs CI (`gates`, `tests`) sur la découverte intégrale, jamais une liste recopiée — 0 échec, toutes les étapes des plans 41.1-01 à 41.1-08 vertes ensemble pour la première fois dans le même arbre"
    - "`.github/workflows/ci.yml` `check-version-sync.sh` (déjà un gate existant du job `gates`) doit rendre 0 sur les trois bumps — c'est la preuve machine que les triades VERSION/module.json/badges sont cohérentes, pas une relecture manuelle"
  artifacts:
    - path: "plugin/conductor/VERSION"
      provides: "version patch bumpée, cohérente avec module.json"
    - path: "plugin/planning-core/VERSION"
      provides: "version patch bumpée, cohérente avec module.json"
    - path: "plugin/dev-orchestrator/VERSION"
      provides: "version patch bumpée, cohérente avec module.json"
  key_links:
    - "`scripts/check-version-sync.sh` (déjà câblé en gate CI, `ci.yml` ~ligne 341) est le VÉRIFICATEUR MACHINE de ce plan — pas une relecture visuelle des trois triades"
    - "Ce plan dépend de TOUS les plans précédents de la phase : c'est le SEUL plan qui bump, exécuté une fois tous les fichiers de chaque module stabilisés — éviter un bump prématuré qui devrait être corrigé par un plan suivant"
---

<objective>
Clore la phase par le geste de version : bump patch des trois modules réellement modifiés
(`conductor`, `planning-core`, `dev-orchestrator`), CHANGELOG par module, puis rejeu final complet
des deux jobs CI sur l'arbre entier de la phase — la première fois que tous les plans 41.1-01 à
41.1-07 sont mesurés ENSEMBLE.

Purpose: la discipline de release de ce dépôt (CLAUDE.md racine) exige un bump cohérent pour tout
module dont le contenu distribué change ; ce plan porte ce geste, sans toucher à la racine
(release séparée, hors périmètre).
Output: trois modules bumpés en patch, CHANGELOG à jour, rejeu final vert.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 — VALEURS RELUES SUR DISQUE, jamais recopiées de ce plan (un
plan antérieur de cette même phase a pu déjà bumper si l'ordre d'exécution diffère du DAG) :
- `plugin/conductor/VERSION`, `plugin/conductor/module.json`, `plugin/conductor/CHANGELOG.md` (tête)
- `plugin/planning-core/VERSION`, `plugin/planning-core/module.json`, `plugin/planning-core/CHANGELOG.md` (tête)
- `plugin/dev-orchestrator/VERSION`, `plugin/dev-orchestrator/module.json`, `plugin/dev-orchestrator/CHANGELOG.md` (tête)
- `scripts/check-version-sync.sh` en-tête (ce que le gate vérifie exactement)
</context>

<discipline>
- Valeurs de version TOUJOURS relues sur disque au moment de l'exécution, jamais recopiées de ce
  PLAN.md.
- `rtk` fausse les vérifications d'état (sortie vide → 1 ligne au lieu de 0) — toute mesure
  `git status`/`git diff` de ce plan passe par `rtk proxy`, jamais l'alias nu.
- Aucun fichier hors des neuf listés en `files_modified` n'est touché par ce plan.
- Racine (`VERSION`, `plugin.json`, `marketplace.json`, README, CHANGELOG racine) strictement hors
  périmètre — ne serait-ce que pour vérifier une cohérence, ne pas les ouvrir en écriture.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : bump patch cohérent des trois modules, CHANGELOG par module</name>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md, plugin/planning-core/VERSION, plugin/planning-core/module.json, plugin/planning-core/CHANGELOG.md, plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md</files>
  <behavior>
    - pour chaque module, `VERSION` et le champ `version` de `module.json` portent la MÊME valeur,
      bumpée d'un patch par rapport à la valeur lue sur disque au début de cette tâche.
    - chaque `CHANGELOG.md` de module gagne une entrée `## [vX.Y.Z] — <date du jour>` qui nomme
      SES PROPRES fichiers livrés par cette phase, pas ceux des autres modules.
    - `scripts/check-version-sync.sh` rend 0 sur le dépôt après ces neuf éditions.
  </behavior>
  <action>
Relire les trois triades VERSION/module.json/CHANGELOG (tête) AVANT toute édition — la valeur
`vX.Y.Z` actuelle de chacune peut différer de celle citée dans ce PLAN.md si un plan antérieur de
cette phase a déjà bumpé (ce ne devrait PAS être le cas, ce plan est le seul de la phase à toucher
ces fichiers, mais la vérification est gratuite et évite un écart silencieux).

Pour CHAQUE module (`conductor`, `planning-core`, `dev-orchestrator`) :
1. lire `VERSION` (une ligne, forme `vMAJOR.MINOR.PATCH`), incrémenter `PATCH` de 1.
2. écrire la même valeur dans `VERSION` et dans le champ `"version"` de `module.json` (édition
   JSON par un outil qui préserve le reste du fichier à l'identique — `jq` si disponible, sinon une
   substitution ciblée sur la seule ligne `"version": "..."`, jamais une réécriture intégrale du
   fichier qui risquerait de reformater l'ordre des clés).
3. ajouter en tête du `CHANGELOG.md` du module (juste après la ligne `# Changelog — <module>`) une
   entrée `## [vX.Y.Z] — <date ISO du jour>`, sous-titre `**Patch** (durcissement de gates
   workstream-aware, Phase 41.1)`, puis la liste des fichiers de CE module livrés par cette phase
   (voir la répartition exacte donnée dans `must_haves.truths` ci-dessus), avec pour chacun une
   phrase courte de ce qu'il ferme (trou exit-0, faux ABSENT, recensement anti-oubli, etc. — une
   phrase, pas un paragraphe : le détail vit dans les SUMMARY des plans 41.1-01 à 07).

Ordre des trois modules dans l'exécution : indifférent, aucune dépendance entre eux.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c '
for m in conductor planning-core dev-orchestrator; do
  v=$(awk "NR==1{print \$1}" "plugin/$m/VERSION")
  j=$(awk -F\" "/\"version\"/{print \$4; exit}" "plugin/$m/module.json")
  echo "$m VERSION=$v module.json=$j"
  [ "$v" = "$j" ] || exit 1
done
'</automated>
    <fails_when>pour un des trois modules, `VERSION` et `module.json` divergent — bump incohérent</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash scripts/check-version-sync.sh 2>&1) || rc=$?; echo "check-version-sync rc=$rc"; printf "%s\n" "$out"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>`check-version-sync rc=` différent de 0 — le gate CI existant doit valider les trois triades sans intervention manuelle</fails_when>
    <automated>bash --noprofile --norc -e -c '
for m in conductor planning-core dev-orchestrator; do
  n=$(awk "/^## \[v/{c++} END{print c+0}" "plugin/$m/CHANGELOG.md" | head -1)
  first=$(awk "/^## \[v/{print; exit}" "plugin/$m/CHANGELOG.md")
  echo "$m entries=$n premiere_ligne=[$first]"
done
'</automated>
    <fails_when>pour un module, la première entrée `## [v...]` ne correspond pas à la valeur écrite dans `VERSION` (à comparer visuellement dans la trace — un écart entre le CHANGELOG et VERSION signifie un oubli de synchronisation)</fails_when>
  </verify>
  <acceptance_criteria>
    - trois modules bumpés en patch, `VERSION`/`module.json` cohérents
    - `check-version-sync.sh` rend 0
    - trois CHANGELOG portent chacun une entrée datée nommant leurs propres fichiers livrés
    - aucun fichier racine touché
  </acceptance_criteria>
  <done>Les trois modules réellement modifiés par la phase sont bumpés de façon cohérente et tracée.</done>
</task>

<task type="auto">
  <name>Task 2 : rejeu final complet — gates et tests sur l'arbre entier de la phase</name>
  <files></files>
  <precondition>Task 1 verte ; `rtk proxy git status --porcelain` ne liste que les chemins des plans 41.1-01 à 41.1-08 de cette phase.</precondition>
  <action>
Rejouer les DEUX jobs (`gates`, `tests`) avec la découverte COMPLÈTE, jamais une liste recopiée —
c'est la première fois que tous les plans de la phase (41.1-01 à 41.1-08) sont mesurés dans le
même arbre : le primitif `vf_ws_enumerate`, son adoption par `check-divergence.sh`, les deux hooks
`planning-core`, `discover-unintegrated-docs.sh`, le recensement + lint, le fan-out CI et la
fixture à 3 compartiments, la doctrine amendée, et les trois bumps — tous ensemble.

Un rouge n'est JAMAIS contourné : s'il semble étranger à la phase (par exemple un cas connu déjà
consigné ailleurs dans ce dépôt — `WINDOWS.md`, ou le cas T28-F de fraîcheur d'index GSD), le
vérifier sur une extraction de la base de `main` avant de conclure « préexistant », et le consigner
nommément au SUMMARY plutôt que de le neutraliser.

Consigner au SUMMARY : nombre d'étapes `gates` rejouées, bilan `tests` (nombre de suites, 0 échec
attendu), tout `SKIP` avec sa raison, et le périmètre volontairement non rejoué (jobs
`lab-frais`/`lab-frais-arme`, étape `check-release-tag` conditionnelle `main`, comme dans les plans
précédents de ce dépôt).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^BILAN REJEU gates/" "$T/g"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${n:-0}"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 15 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 15 (les nouvelles étapes des plans 41.1-05/06 doivent être découvertes en plus des étapes préexistantes)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 — toute nouvelle suite créée par les plans 41.1-01/03/04/05 doit être découverte et verte</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(rtk proxy git status --porcelain 2>&1) || rc=$?; n=$(printf "%s\n" "$out" | awk "NF>0" | awk "END{print NR+0}"); echo "fichiers_non_commites=$n"; printf "%s\n" "$out"'</automated>
    <fails_when>ce dernier bloc est informatif (pas d'assertion de rc) — le SUMMARY doit néanmoins consigner sa sortie brute pour trace</fails_when>
  </verify>
  <acceptance_criteria>
    - rejeu `gates` vert, découverte complète (≥ 15 étapes, incluant les étapes neuves des plans 41.1-05/06)
    - rejeu `tests` vert, découverte complète, 0 échec
    - tout rouge rencontré est soit corrigé soit consigné nommément (jamais contourné)
    - SUMMARY consigne le périmètre non rejoué et tout `SKIP`
  </acceptance_criteria>
  <done>La phase entière est mesurée d'un seul tenant, verte, prête pour la revue et le ship (gestes humains hors périmètre de ce plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version déclarée → contenu réellement distribué | un bump sans rejeu complet laisserait un module publié sur une version dont le contenu n'a pas été mesuré ensemble avec les autres plans de la phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-15 | Tampering | bump incohérent entre `VERSION` et `module.json` d'un module | medium | mitigate | `check-version-sync.sh` (gate CI existant) rejoué explicitement en Task 1, pas seulement lors du rejeu global |
| T-41.1-16 | Repudiation | un rouge rencontré au rejeu final, contourné plutôt que consigné, ferait passer la phase pour vérifiée alors qu'elle ne l'est pas | high | mitigate | discipline explicite « jamais contourné » héritée du parc de plans de ce dépôt ; SUMMARY oblige la trace nommée de tout SKIP |
</threat_model>

<verification>
Trois triades VERSION/module.json cohérentes, `check-version-sync.sh` vert ; trois CHANGELOG
datés ; rejeu `gates` (≥ 15 étapes) et `tests` (0 échec) verts sur l'arbre complet de la phase.
</verification>

<success_criteria>
Les trois modules réellement modifiés sont bumpés en patch, tracés au CHANGELOG, et la phase
entière (41.1-01 à 41.1-08) est mesurée ensemble et verte pour la première fois — prête pour la
revue et le ship, gestes humains hors périmètre.
</success_criteria>

## Artifacts this phase produces

- `plugin/conductor/{VERSION,module.json,CHANGELOG.md}` — patch bumpé.
- `plugin/planning-core/{VERSION,module.json,CHANGELOG.md}` — patch bumpé.
- `plugin/dev-orchestrator/{VERSION,module.json,CHANGELOG.md}` — patch bumpé.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-08-SUMMARY.md` when done
</output>
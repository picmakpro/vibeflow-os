---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 07
lot: L4 — suites à seuil codé en dur dev-orchestrator et design-orchestrator
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/README.md
  - plugin/design-orchestrator/CHANGELOG.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PRÉCONDITION (session principale, 2026-09-16) — ce plan s'exécute sur une base qui CONTIENT le hotfix v2.63.1 fusionné (il touche `plugin/dev-orchestrator/`) ; sinon la branche est rebasée sur `main` qui le contient, et la liste de travail, les tailles d'agents et la VERSION courante sont re-dérivées AVANT toute édition"
    - "Aucun commit de ce plan ne touche les fichiers du hotfix : `plugin/dev-orchestrator/AGENT.md`, `references/head-governance.md`, `references/intent-routing.md`, `skills/vf-dev/`, `skills/vf-auto/`, `agents/vf-coder.md`"
    - "D-01 — les seuils codés en dur de `test-dev-orchestrator.sh` (T3, T5, T8, T22, T31-G, T35 via `T35_PLAFOND`) et de `test-design-orchestrator.sh` (T1, T7) valent 300 : un agent de 300 lignes passe, un agent de 301 lignes est vu en dépassement par CHAQUE assertion, prouvé par `tools/prove-density-suites.sh <clé> 300 301` sur copie"
    - "Ces suites restent des assertions BINAIRES (≤ 300), sans réplication de la zone d'avertissement du gate central : décision de discrétion (RESEARCH A1, Open Question 1 résolue), cohérente avec D-05 « blocage à 300 partout » ; l'avertissement dès 251 est porté par le gate du dépôt (40.1-03)"
    - "La mutation interne T35 (e) reste discriminante ; la mesure historique `(249/250)` d'un commentaire est une justification datée, exclue et intacte"
    - "Deux bumps **patch** sur cibles dérivées à l'exécution (`tools/check-module-bump.sh dev-orchestrator patch`, `… design-orchestrator patch`), jamais un numéro figé : le hotfix consomme très probablement le prochain patch de dev-orchestrator"
  artifacts:
    - path: "plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      provides: "seuils T3/T5/T8/T22/T31-G/T35 à 300"
      contains: "T35_PLAFOND=300"
    - path: "plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh"
      provides: "seuils T1/T7 à 300"
  key_links:
    - "`tools/prove-density-suites.sh` (40.1-01) rendait 1 en `300 301` avant ce plan ; il doit rendre 0 après : c'est la preuve rouge→verte de ce lot"
    - "Couplage déclaré : `test-dev-orchestrator.sh` lit des fichiers du hotfix (AGENT.md, vf-coder.md) ; la preuve et la suite sont donc jouées sur la base qui contient le hotfix (précondition)"
---

<objective>
Relever à 300 les seuils de densité codés en dur des suites `dev-orchestrator` et
`design-orchestrator` (D-01, arbitrage Samuel, AskUserQuestion session principale, relais
SendMessage, 2026-09-16), aligner les deux énoncés vivants de `dev-orchestrator`, bumper les deux
modules en patch sur cibles dérivées.

Purpose: BUDG-04 sur les suites de modules ; sans ce lot, le gate central autoriserait 300 lignes
que ces suites refuseraient.
Output: deux suites relevées et prouvées, deux modules bumpés.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : seuils de test-dev-orchestrator.sh et énoncés vivants du module à 300</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/references/GSD-PIPELINE.md</files>
  <precondition>La base de travail contient le hotfix v2.63.1 : `git log --format=%s main` montre son commit de release ou de merge (à défaut, rebaser sur `main` et re-dériver) ; en l'absence du hotfix sur `main`, s'arrêter et remonter au manager.</precondition>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/dev-orchestrator/` (re-dérivée APRÈS rebase éventuel)
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh aux lignes listées (en-tête T3/T5, T3, T5, T8, T22, T31-G, T35)
    - plugin/dev-orchestrator/README.md (arbre, liste des tests), plugin/dev-orchestrator/references/GSD-PIPELINE.md
  </read_first>
  <action>
Pour chaque ligne listée sous `plugin/dev-orchestrator/`, remplacer la valeur du plafond par 300 à
la fois dans la comparaison et dans le message `ok`/`ko` ou le commentaire qui l'affiche :
`-le 300`, `(≤300)`, `(>300)`, `dépasse 300 lignes`, `plafond ADR-029 = 300`, `/300 lignes`,
`T35_PLAFOND=300`, `≤300L`.
- Aucune autre modification de logique.
- La ligne de commentaire `(249/250)` (justification datée, exclue) n'est pas touchée.
- README et GSD-PIPELINE : valeur 300 seule.
- Aucun fichier du hotfix touché.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh dev-orchestrator 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :` (un label de dépassement présent à 300, ou absent à 301)</fails_when>
    <automated>bash --noprofile --norc -e plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>exit non nul, ou une ligne de bilan comptant au moins un KO</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/dev-orchestrator\\// {c++} END{print c+0}" "$T/o"); git show 4e7c8e4:plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > "$T/t0"; a=$(awk "/-le 250/{c++} END{print c+0}" "$T/t0"); b=$(awk "/-le 300/{c++} END{print c+0}" plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh); git log --format=%H%x09%s 4e7c8e4..HEAD > "$T/l"; : > "$T/hf"; while IFS="$(printf "\t")" read -r h s; do case "$s" in *"(40.1"*) git diff-tree --no-commit-id --name-only -r "$h" >> "$T/hf" ;; esac; done < "$T/l"; x=$(awk "/^plugin\\/dev-orchestrator\\/(AGENT\\.md|references\\/head-governance\\.md|references\\/intent-routing\\.md|skills\\/vf-dev\\/|skills\\/vf-auto\\/|agents\\/vf-coder\\.md)/{c++} END{print c+0}" "$T/hf"); echo "dev-orchestrator vivants=$n comparaisons_avant=$a comparaisons_apres=$b fichiers_hotfix_touches_par_la_phase=$x"; [ "$n" -eq 0 ] && [ "$a" -eq "$b" ] && [ "$b" -gt 0 ] && [ "$x" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0 ; ou `comparaisons_apres=` différent de `comparaisons_avant=` ; ou `fichiers_hotfix_touches_par_la_phase=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove-density-suites.sh dev-orchestrator 300 301` rc 0 (rendait 1 avant ce plan)
    - `test-dev-orchestrator.sh` rc 0, 0 KO, sur une base contenant le hotfix
    - `T35_PLAFOND=300` présent ; autant de comparaisons `-le 300` qu'il y en avait à l'ancienne valeur dans la base de phase
    - zéro ligne vivante sous `plugin/dev-orchestrator/` ; aucun commit de phase ne touche un fichier du hotfix
  </acceptance_criteria>
  <done>Les six assertions de densité de dev-orchestrator valent 300, preuve rouge→verte rejouée.</done>
</task>

<task type="auto">
  <name>Task 2 : bump patch dérivé de dev-orchestrator</name>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/CHANGELOG.md</files>
  <read_first>
    - plugin/dev-orchestrator/VERSION (valeur COURANTE, hotfix inclus : la cible en dérive)
    - plugin/dev-orchestrator/CHANGELOG.md (entrée de tête, éventuellement celle du hotfix), plugin/dev-orchestrator/README.md (`**Version**`)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh
  </read_first>
  <action>
Lire `plugin/dev-orchestrator/VERSION` à l'exécution et calculer la cible **patch**. La même cible va
dans `VERSION`, dans `module.json` et dans la ligne `**Version**` du README, en UN commit au sujet
`chore(40.1-07): …`. Entrée CHANGELOG en tête, au-dessus de celle du hotfix,
`## [<cible>] — <date d'exécution> (Phase 40.1)` :
- seuils de densité des tests T3, T5, T8, T22, T31-G, T35 relevés au plafond ADR-029 de 300 lignes
  (assertions binaires ; l'avertissement dès 251 relève du gate du dépôt) ;
- README et GSD-PIPELINE alignés ;
- autorisation : arbitrage Samuel D-01, AskUserQuestion session principale (relais SendMessage),
  2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh dev-orchestrator patch</automated>
    <fails_when>exit non nul, ou absence de `cible derivee dev-orchestrator :`, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
  </verify>
  <acceptance_criteria>
    - `check-module-bump.sh dev-orchestrator patch` rc 0 (cible dérivée de la VERSION précédant le commit de bump, hotfix compris) ; version-sync rc 0
  </acceptance_criteria>
  <done>dev-orchestrator bumpé en patch sur cible dérivée.</done>
</task>

<task type="auto">
  <name>Task 3 : design-orchestrator — seuils T1/T7 à 300, preuve, bump patch dérivé</name>
  <files>plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/README.md, plugin/design-orchestrator/CHANGELOG.md</files>
  <read_first>
    - sortie de `tools/check-no-live-250.sh .` filtrée sur `plugin/design-orchestrator/`
    - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh (en-tête L7 et L22, T1 L83-95, T7 L200-207)
    - plugin/design-orchestrator/VERSION (courante), CHANGELOG.md, README.md (`**Version**`)
  </read_first>
  <action>
Dans la suite, mettre la valeur 300 dans chaque comparaison, message et commentaire listés :
- T1 : `-le 300`, `dépasse 300 lignes`, `≤300L` ;
- T7 : `-le 300`, `(≤300)`, `(>300)` ;
- en-tête.
Bump **patch** dérivé de la VERSION courante (`VERSION`, `module.json`, `**Version**` du README), en
un commit `chore(40.1-07): …`. Entrée CHANGELOG `## [<cible>] — <date d'exécution> (Phase 40.1)` :
seuils T1/T7 relevés au plafond ADR-029 de 300 lignes ; autorisation : arbitrage Samuel D-01,
AskUserQuestion session principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh design-orchestrator 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e -c 'bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh > /dev/null; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh design-orchestrator patch; bash scripts/check-version-sync.sh > /dev/null; T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/design-orchestrator\\// {c++} END{print c+0}" "$T/o"); echo "design-orchestrator vivants=$n"; [ "$n" -eq 0 ]'</automated>
    <fails_when>exit non nul (suite, bump dérivé ou version-sync rouge) ; ou `vivants=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove-density-suites.sh design-orchestrator 300 301` rc 0 ; suite verte ; zéro ligne vivante sous `plugin/design-orchestrator/`
    - `check-module-bump.sh design-orchestrator patch` rc 0 ; entrée CHANGELOG avec canal et date
  </acceptance_criteria>
  <done>design-orchestrator relevé, prouvé et bumpé en patch sur cible dérivée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de module → job CI `tests` | une suite à seuil plus bas que le gate refuserait ce que le gate accepte |
| hotfix parallèle → module dev-orchestrator | deux lignes de travail écrivent le même module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-20 | Repudiation | seuil relevé dans la comparaison mais pas dans une autre assertion | medium | mitigate | preuve 300/301 sur copie exige CHAQUE label ; nombre de comparaisons avant = après |
| T-40.1-21 | Tampering | collision avec le hotfix v2.63.1 (fichiers ou numéro de version) | medium | mitigate | précondition base contenant le hotfix ; zéro fichier du hotfix dans les commits de phase ; cible de version dérivée |
| T-40.1-22 | Denial of service | suite rouge entre deux commits | low | accept | relèvement monotone d'un seuil au-dessus de toutes les tailles réelles |
</threat_model>

<verification>
Deux preuves `prove-density-suites.sh` rc 0 ; deux suites vertes ; recensement nul sous les deux
modules ; deux bumps dérivés vérifiés ; aucun fichier du hotfix touché.
</verification>

<success_criteria>
Les suites dev-orchestrator et design-orchestrator acceptent 300 et refusent 301, prouvé ; modules
bumpés en patch sans collision avec le hotfix.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs de seuil (`T35_PLAFOND=300`) et deux bumps patch.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-07-SUMMARY.md` when done
</output>

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 05
lot: L4 — suites à seuil codé en dur dev-orchestrator et design-orchestrator
type: execute
wave: 2
depends_on: ["40.1-01"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/README.md
  - plugin/design-orchestrator/CHANGELOG.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 — les seuils codés en dur de `test-dev-orchestrator.sh` (T3, T5, T8, T22, T31-G, T35 via `T35_PLAFOND`) et de `test-design-orchestrator.sh` (T1, T7) valent 300 : un agent de 300 lignes passe, un agent de 301 lignes est vu en dépassement par CHAQUE assertion — prouvé par `tools/prove-density-suites.sh <clé> 300 301` sur copie"
    - "Ces suites restent des assertions BINAIRES (≤ 300), sans réplication de la zone d'avertissement du gate central : décision de discrétion (RESEARCH A1), cohérente avec D-05 « blocage à 300 partout » — l'avertissement dès 251 est porté par le gate du dépôt (40.1-02), seul outil qui publie un verdict gradué"
    - "La mutation interne T35 (e) reste discriminante : la copie allongée dérive de `T35_PLAFOND`, elle franchit 300"
    - "La mesure historique `(249/250)` du commentaire de T-3645 est une justification datée : exclue, intacte"
    - "Aucune suite ne rougit entre deux commits : l'ancien seuil étant plus bas, chaque commit de relèvement laisse les suites vertes ; `dev-orchestrator` v2.22.1 et `design-orchestrator` v1.5.7, triades et en-têtes alignés"
  artifacts:
    - path: "plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      provides: "seuils T3/T5/T8/T22/T31-G/T35 à 300"
      contains: "T35_PLAFOND=300"
    - path: "plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh"
      provides: "seuils T1/T7 à 300"
  key_links:
    - "`tools/prove-density-suites.sh` (40.1-01) rendait 1 en `300 301` avant ce plan ; il doit rendre 0 après — c'est la preuve rouge→verte de ce lot"
---

<objective>
Relever à 300 les seuils de densité codés en dur des suites des modules `dev-orchestrator` et
`design-orchestrator` (D-01, arbitrage Samuel, AskUserQuestion session principale, relais
SendMessage, 2026-09-16), aligner les deux énoncés vivants de `dev-orchestrator`, bumper les deux
modules en patch.

Purpose: BUDG-04 sur les suites de modules ; sans ce lot, le gate central autoriserait 300 lignes
que ces suites refuseraient.
Output: deux suites relevées et prouvées, deux modules bumpés.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : seuils de test-dev-orchestrator.sh et énoncés vivants du module à 300</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/references/GSD-PIPELINE.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/dev-orchestrator/` (liste re-dérivée à l'exécution)
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh aux lignes listées (en-tête T3/T5, T3 ~L1236-1252, T5 ~L1356-1364, T8 ~L1440-1451, T22 ~L2454-2457, T31-G ~L6220-6229, T35 ~L6440-6500)
    - plugin/dev-orchestrator/README.md (arbre ~L74, liste des tests ~L219), plugin/dev-orchestrator/references/GSD-PIPELINE.md (~L4)
  </read_first>
  <action>
Pour chaque ligne listée sous `plugin/dev-orchestrator/` : remplacer la valeur du plafond par 300
dans la comparaison ET dans le message `ok`/`ko`/commentaire qui l'affiche (`-le 300`, `(≤300)`,
`(>300)`, `dépasse 300 lignes`, `plafond ADR-029 = 300`, `/300 lignes`, `T35_PLAFOND=300`,
`≤300L`). Aucune autre modification de logique ; la ligne de commentaire `(249/250)` (justification
datée, exclue) n'est pas touchée. README et GSD-PIPELINE : valeur 300 seule.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh dev-orchestrator 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :` (un label de dépassement présent à 300, ou absent à 301)</fails_when>
    <automated>bash --noprofile --norc -e plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>exit non nul, ou une ligne de bilan comptant au moins un KO</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/dev-orchestrator\\// {c++} END{print c+0}" "$T/o"); git show 4e7c8e4:plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > "$T/t0"; a=$(awk "/-le 250/{c++} END{print c+0}" "$T/t0"); b=$(awk "/-le 300/{c++} END{print c+0}" plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh); echo "dev-orchestrator vivants=$n comparaisons_avant=$a comparaisons_apres=$b"; [ "$n" -eq 0 ] && [ "$a" -eq "$b" ] && [ "$b" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0 ; ou `comparaisons_apres=` différent de `comparaisons_avant=` (un seuil oublié ou un seuil inventé)</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove-density-suites.sh dev-orchestrator 300 301` rc 0 (rendait 1 avant ce plan)
    - `test-dev-orchestrator.sh` rc 0, 0 KO
    - `T35_PLAFOND=300` présent ; autant de `-le 300` qu'il y avait de `-le` à l'ancienne valeur dans la base de phase
    - zéro ligne vivante sous `plugin/dev-orchestrator/`
  </acceptance_criteria>
  <done>Les six assertions de densité de dev-orchestrator valent 300, preuve rouge→verte rejouée.</done>
</task>

<task type="auto">
  <name>Task 2 : bump dev-orchestrator v2.22.1 (patch)</name>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/CHANGELOG.md</files>
  <read_first>
    - plugin/dev-orchestrator/CHANGELOG.md (forme de l'entrée la plus récente), plugin/dev-orchestrator/README.md (`**Version**`)
    - scripts/check-version-sync.sh
  </read_first>
  <action>
Patch v2.22.0 → v2.22.1 dans `VERSION`, `module.json`, ligne `**Version**` du README. Entrée
CHANGELOG `## [v2.22.1] — <date d'exécution> (Phase 40.1)` : seuils de densité des tests T3, T5,
T8, T22, T31-G, T35 relevés au plafond ADR-029 de 300 lignes (assertions binaires, l'avertissement
dès 251 relève du gate du dépôt) ; README et GSD-PIPELINE alignés ; autorisation : arbitrage Samuel
D-01, AskUserQuestion session principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
    <automated>bash --noprofile --norc -e -c 'v=$(awk "NR==1" plugin/dev-orchestrator/VERSION); h=$(awk "/^## \\[v2\\.22\\.1\\]/{c++} END{print c+0}" plugin/dev-orchestrator/CHANGELOG.md); echo "dev-orchestrator VERSION=$v entree=$h"; [ "$v" = "v2.22.1" ] && [ "$h" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `VERSION=` différent de `v2.22.1` ; ou `entree=` différent de 1</fails_when>
  </verify>
  <acceptance_criteria>
    - VERSION, module.json, en-tête README = v2.22.1 ; entrée CHANGELOG citant l'arbitrage avec canal et date ; version-sync rc 0
  </acceptance_criteria>
  <done>dev-orchestrator v2.22.1.</done>
</task>

<task type="auto">
  <name>Task 3 : design-orchestrator — seuils T1/T7 à 300, preuve, bump v1.5.7</name>
  <files>plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/README.md, plugin/design-orchestrator/CHANGELOG.md</files>
  <read_first>
    - sortie de `tools/check-no-live-250.sh .` filtrée sur `plugin/design-orchestrator/`
    - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh (en-tête L7 et L22, T1 L83-95, T7 L200-207)
    - plugin/design-orchestrator/CHANGELOG.md, plugin/design-orchestrator/README.md (`**Version**`)
  </read_first>
  <action>
Dans la suite : valeur 300 dans chaque comparaison, message et commentaire listés (T1 `-le 300`,
`dépasse 300 lignes`, `≤300L` ; T7 `-le 300`, `(≤300)`, `(>300)` ; en-tête). Bump patch
v1.5.6 → v1.5.7 (`VERSION`, `module.json`, `**Version**` du README) ; entrée CHANGELOG
`## [v1.5.7] — <date d'exécution> (Phase 40.1)` : seuils T1/T7 relevés au plafond ADR-029 de 300
lignes ; autorisation : arbitrage Samuel D-01, AskUserQuestion session principale (relais
SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh design-orchestrator 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e -c 'bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh > /dev/null; bash scripts/check-version-sync.sh > /dev/null; T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/design-orchestrator\\// {c++} END{print c+0}" "$T/o"); v=$(awk "NR==1" plugin/design-orchestrator/VERSION); echo "design-orchestrator vivants=$n VERSION=$v"; [ "$n" -eq 0 ] && [ "$v" = "v1.5.7" ]'</automated>
    <fails_when>exit non nul (suite rouge ou version-sync rouge) ; ou `vivants=` différent de 0 ; ou `VERSION=` différent de `v1.5.7`</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove-density-suites.sh design-orchestrator 300 301` rc 0 ; suite verte ; zéro ligne vivante sous `plugin/design-orchestrator/`
    - triade et en-tête v1.5.7 ; entrée CHANGELOG avec canal et date
  </acceptance_criteria>
  <done>design-orchestrator relevé, prouvé et versionné v1.5.7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de module → job CI `tests` | une suite à seuil plus bas que le gate refuserait ce que le gate accepte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-15 | Repudiation | seuil relevé dans la comparaison mais pas dans une autre assertion (seuil oublié) | medium | mitigate | preuve 300/301 sur copie exige CHAQUE label ; nombre de comparaisons avant = après |
| T-40.1-16 | Denial of service | suite rouge entre deux commits | low | accept | relèvement monotone d'un seuil au-dessus de toutes les tailles réelles (max 250 mesuré) : aucun commit ne peut rougir |
</threat_model>

<verification>
Deux preuves `prove-density-suites.sh` rc 0 ; deux suites vertes ; recensement nul sous les deux
modules ; version-sync vert.
</verification>

<success_criteria>
Les suites dev-orchestrator et design-orchestrator acceptent 300 et refusent 301, prouvé ; modules
bumpés en patch.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs de seuil (`T35_PLAFOND=300`) et bumps v2.22.1 / v1.5.7.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-05-SUMMARY.md` when done
</output>

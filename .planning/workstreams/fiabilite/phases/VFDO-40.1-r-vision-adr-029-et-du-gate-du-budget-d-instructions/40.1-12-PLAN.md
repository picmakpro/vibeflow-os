---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 12
lot: L6b — kpi-analyst et audit-architecture
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/kpi-analyst/AGENT.md
  - plugin/kpi-analyst/VERSION
  - plugin/kpi-analyst/module.json
  - plugin/kpi-analyst/README.md
  - plugin/kpi-analyst/CHANGELOG.md
  - plugin/audit-architecture/SKILL.md
  - plugin/audit-architecture/README.md
  - plugin/audit-architecture/VERSION
  - plugin/audit-architecture/module.json
  - plugin/audit-architecture/CHANGELOG.md

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01 — `kpi-analyst` et `audit-architecture` énoncent le plafond des agents à 300 lignes ; plus aucune ligne vivante de l'ancien plafond sous ces deux modules"
    - "Aucun agent réécrit : dans `plugin/kpi-analyst/AGENT.md`, remplacement de valeur seul ; lignes et instructions égales à la baseline, verdict `OK`, `check-agents.sh --strict` vert"
    - "`plugin/audit-architecture/SKILL.md` : seule la ligne du body est touchée, frontmatter intact, `test-check-description-fidelity.sh` vert (classe T20-T25)"
    - "Deux bumps **patch** sur cibles dérivées à l'exécution (`tools/check-module-bump.sh kpi-analyst patch`, `… audit-architecture patch`)"
  artifacts:
    - path: "plugin/kpi-analyst/AGENT.md"
      provides: "auto-description de densité à 300"
  key_links:
    - "Couplage déclaré : `plugin/conductor/scripts/tests/test-check-description-fidelity.sh` copie `plugin/audit-architecture/SKILL.md` ; 40.1-04 rejoue les suites conductor APRÈS ce plan (dépendance déclarée)"
---

<objective>
Aligner `kpi-analyst` (agent mono-module) et `audit-architecture` (skill) sur le plafond ADR-029 de
300 lignes (D-01, arbitrage Samuel, AskUserQuestion session principale, relais SendMessage,
2026-09-16), puis les bumper en patch sur cibles dérivées. Ce plan est issu de la scission de
l'ancien plan « validator, kpi-analyst, audit-architecture » (révision 1, constat W2) : un module par
tâche, cinq fichiers chacune.

Purpose: BUDG-04.
Output: deux modules alignés et bumpés.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
@.planning/instruction-budget-baselines.tsv
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : kpi-analyst — agent (valeur seule) et bump patch dérivé</name>
  <files>plugin/kpi-analyst/AGENT.md, plugin/kpi-analyst/VERSION, plugin/kpi-analyst/module.json, plugin/kpi-analyst/README.md, plugin/kpi-analyst/CHANGELOG.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/kpi-analyst/`
    - plugin/kpi-analyst/AGENT.md (ligne listée, ~L84), plugin/kpi-analyst/VERSION (courante), CHANGELOG.md, README.md (`**Version**`)
  </read_first>
  <action>
`AGENT.md` : remplacement de valeur seul (`je reste ≤300L`), aucune ligne ajoutée ni retirée. Bump
**patch** dérivé de la VERSION lue à l'exécution (`VERSION`, `module.json`, `**Version**`) dans un
commit `chore(40.1-12): …`. Entrée CHANGELOG en tête, `## [<cible>] — <date d'exécution> (Phase 40.1)` :
auto-description de densité alignée sur le plafond ADR-029 de 300 lignes ; autorisation : arbitrage
Samuel D-01, AskUserQuestion session principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/kpi-analyst\\// {c++} END{print c+0}" "$T/o"); echo "kpi-analyst vivants=$n"; [ "$n" -eq 0 ]; g=0; bash plugin/conductor/scripts/check-instruction-budget.sh > "$T/g" 2>&1 || g=$?; echo "gate rc=$g"; [ "$g" -eq 0 ]; awk -F" [|] " "\$1==\"plugin/kpi-analyst/AGENT.md\"{print \"kpi AGENT : lignes=\" \$2 \" bl=\" \$3 \" instr=\" \$4 \" bl=\" \$5 \" verdict=\" \$6; ok=(\$2==\$3 && \$4==\$5 && \$6==\"OK\")} END{exit ok?0:1}" "$T/g"; bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/kpi-analyst/AGENT.md > /dev/null; bash plugin/kpi-analyst/scripts/tests/test-kpis-writer.sh > /dev/null; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh kpi-analyst patch; bash scripts/check-version-sync.sh > /dev/null'</automated>
    <fails_when>exit non nul ; ou `kpi-analyst vivants=` différent de 0 ; ou `gate rc=` différent de 0 ; ou la ligne `kpi AGENT :` hors baseline ou verdict autre que `OK` ; ou check-agents, suite kpi, bump dérivé ou version-sync rouge</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `plugin/kpi-analyst/` ; AGENT.md égal à sa baseline (lignes, instructions), verdict `OK`
    - `check-module-bump.sh kpi-analyst patch` rc 0 ; suite `test-kpis-writer.sh` verte
  </acceptance_criteria>
  <done>kpi-analyst bumpé en patch, agent inchangé hors valeur.</done>
</task>

<task type="auto">
  <name>Task 2 : audit-architecture — skill et README (valeur seule), bump patch dérivé</name>
  <files>plugin/audit-architecture/SKILL.md, plugin/audit-architecture/README.md, plugin/audit-architecture/VERSION, plugin/audit-architecture/module.json, plugin/audit-architecture/CHANGELOG.md</files>
  <read_first>
    - sortie de `tools/check-no-live-250.sh .` filtrée sur `plugin/audit-architecture/`
    - plugin/audit-architecture/SKILL.md (~L114, body), plugin/audit-architecture/README.md (en-tête L7, ~L88), VERSION (courante), CHANGELOG.md
    - plugin/conductor/scripts/tests/test-check-description-fidelity.sh (T20-T25)
  </read_first>
  <action>
`SKILL.md` L114 et `README.md` L88 : valeur 300 seule (`≤ 300 L (ADR-029)`, `≤ 300 L, ADR-029`) ;
frontmatter du SKILL intact. Bump **patch** dérivé de la VERSION lue à l'exécution (`VERSION`,
`module.json`, `**Version**` de l'en-tête README) dans un commit `chore(40.1-12): …`. Entrée CHANGELOG
en tête, `## [<cible>] — <date d'exécution> (Phase 40.1)` : taille cible des auditeurs générés alignée
sur le plafond ADR-029 de 300 lignes ; autorisation : arbitrage Samuel D-01, AskUserQuestion session
principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/audit-architecture\\// {c++} END{print c+0}" "$T/o"); echo "audit-architecture vivants=$n"; [ "$n" -eq 0 ]; bash plugin/conductor/scripts/tests/test-check-description-fidelity.sh > /dev/null; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh audit-architecture patch; bash scripts/check-version-sync.sh > /dev/null'</automated>
    <fails_when>exit non nul ; ou `audit-architecture vivants=` différent de 0 ; ou description-fidelity, bump dérivé ou version-sync rouge</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `plugin/audit-architecture/` ; `test-check-description-fidelity.sh` vert ; `check-module-bump.sh audit-architecture patch` rc 0
  </acceptance_criteria>
  <done>audit-architecture bumpé en patch sur cible dérivée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| édition d'un fichier d'agent ou de skill → gates | ligne ou instruction ajoutée ; frontmatter YAML cassé |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-28 | Tampering | réécriture d'agent sous couvert d'alignement | medium | mitigate | lignes et instructions égales à la baseline ; invariant I2 à la clôture |
| T-40.1-29 | Denial of service | frontmatter du SKILL cassé | low | mitigate | ligne de body seule ; description-fidelity rejouée |
</threat_model>

<verification>
Recensement nul sous les deux modules ; agent égal à sa baseline ; check-agents, description-fidelity,
suite kpi, bumps dérivés et version-sync verts.
</verification>

<success_criteria>
kpi-analyst et audit-architecture alignés sur 300, aucun agent réécrit, deux bumps patch dérivés.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs et deux bumps patch.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-12-SUMMARY.md` when done
</output>

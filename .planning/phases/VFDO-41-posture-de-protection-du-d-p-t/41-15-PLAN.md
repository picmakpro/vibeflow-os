---
phase: 41-posture-de-protection-du-d-p-t
plan: 15
lot: L15 — outillage de preuve de la phase : recensement « aucune garde déclarée fermée » et contrôle de trace d'arbitrage
type: execute
wave: 2
depends_on: ["41-14"]
autonomous: true
requirements: [PROT-04, QUAL-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-aucune-fermeture.sh
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-trace-arbitrage.sh
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ORDRE — ce plan s'exécute APRÈS 41-14, jamais en parallèle (avertissement 8 du vérificateur frais) : le recensement asserte `hits=0` sur un périmètre que 41-14 écrit (en-tête des gardes, étape CI, entrée de CHANGELOG), et le contrôle de trace lit la borne `BASE-TRACE-ARBITRAGE` que 41-14 consigne au registre. Le couplage est réel, pas un ordre de confort"
    - "PÉRIMÈTRE — périmètre SANS ADMIN (option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Aucun appel GitHub, aucun ruleset, aucun bump. Ces deux outils vivent sous `.planning/phases/VFDO-41-.../tools/` : ce sont des vérifications DE PHASE, pas des gates distribués ni des étapes CI — précédent `tools/check-no-live-250.sh` de la Phase 40.1. Ils ne sont donc PAS découverts par `find plugin scripts -path '*/tests/test-*.sh'` et sont joués explicitement par les plans"
    - "`check-aucune-fermeture.sh` rougit (rc 1) sur toute ligne d'un fichier versionné du périmètre où un JETON DE SUJET (`O-3`, `garde`, `gardes`, ou l'un des trois noms de script de la phase) co-occurre avec un JETON D'ACHÈVEMENT (`fermé`, `fermée`, `fermés`, `fermées`, `clos`, `close`, `closes`, `closed`), NÉGATIONS COMPRISES : une tournure niée ne dispense pas — seule une entrée nominative de l'allowlist versionnée dispense"
    - "L'allowlist est versionnée DANS l'outil, par ligne normalisée exacte et avec sa raison écrite. Elle est amorcée par les TROIS seules lignes héritées mesurées le 2026-09-17 sur HEAD `c8489fc` : `.planning/REQUIREMENTS.md` ligne BUDG-04 (« chaque outil garde sa mesure » + « close le 2026-09-17 » : le verbe garder et la clôture d'une EXIGENCE, deux objets distincts) ; `25-SECURITY.md` ligne de la table STRIDE `T-25-16` (la colonne `closed` porte sur la MENACE T-25-16, jamais sur l'observation O-3 qu'elle mentionne) ; `CHANGELOG.md` ligne historique « fermé par garde d'existence ». Toute quatrième entrée est un diff visible, à justifier"
    - "Le jeton `gate`/`gates` est VOLONTAIREMENT hors de la liste des sujets, et la raison est écrite dans l'en-tête : la clôture d'une EXIGENCE (« BUDG-02 close le … ») est un autre objet que la clôture d'une garde ou d'O-3, et l'inclure rendrait le recensement bruyant au point d'être désarmé"
    - "`check-trace-arbitrage.sh` rougit (rc 1) quand un commit propre à la branche cite un arbitrage ou une décision sans la forme canonique (nom, canal, date ISO), quand un même commit cite PLUS D'UN arbitrage ou décision, ou quand un commit qui cite la date `2026-09-17` avec le mot `arbitrage` n'écrit pas exactement « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
    - "BORNE DE PORTÉE DU CONTRÔLE DE TRACE (bloquant 1 du vérificateur frais, décision du manager du 2026-09-17) : la base N'EST PLUS dérivée par `merge-base`, elle est LUE dans la ligne `BASE-TRACE-ARBITRAGE: <sha>` du `41-PREUVES.md`, consignée par 41-14 avant toute écriture du périmètre. Le contrôle ne juge que `<sha>..HEAD`. Clé absente, illisible ou non résoluble en commit → rc 2 NON VÉRIFIABLE, jamais un vert. À écrire en en-tête : les commits antérieurs à cette borne — cadrage, planification — PRÉCÈDENT la garde et ne sont pas réécrits ; les juger produirait un rouge que la phase n'a aucun moyen de corriger (8 fautifs mesurés sur `5238cba..HEAD` avant la borne)"
    - "NORMALISATION DES BLANCS AVANT TOUTE COMPARAISON (bloquant 1 (a) du vérificateur frais) : le contrôle de trace joint les lignes du message de commit en une seule chaîne et réduit toute suite de blancs à une espace unique AVANT de chercher la forme canonique ou la chaîne exacte du périmètre. Une citation coupée par un retour à la ligne est CONFORME — c'est le même défaut que le faux rouge de ledger déjà corrigé ailleurs dans ce dépôt. Un mutant est exigé sur cette normalisation"
    - "VERT À VIDE INTERDIT : `check-aucune-fermeture.sh` rend rc 2 si son périmètre découvre 0 fichier ; `check-trace-arbitrage.sh` rend rc 3 (silence) si la plage est vide. Aucun rc 0 sans découverte non vide"
    - "QUAL-01 — chaque outil a sa suite avec des CONTRÔLES NÉGATIFS (fixtures qui doivent rougir, patron `controle-negatif-reel.sh` de la Phase 40.1) ET au moins UN mutant opposable par comparaison, vérifié par `cmp` et `bash -n`. Le recensement en porte TROIS (co-occurrence, périmètre non vide, sonde de limite de fond), le contrôle de trace QUATRE (forme, unicité, borne de portée, normalisation des blancs). Chaque mutant asserte le rc EXACT sur le mutant ET sur l'original, imprime assertion, attendu et obtenu, et n'est crédité que par la ligne canonique `✓ MUT-<n> TUE : rc_mutant=… attendu …, rc_original=… attendu …` : un mutant tué par un plantage ou par une découverte vide ne compte pas (avertissement 3 du vérificateur frais). Une suite incapable de rougir est un défaut"
    - "SONDE DE LIMITE DE FOND (avertissement 7 du vérificateur frais, décision du manager du 2026-09-17) — `check-aucune-fermeture.sh` porte une SECONDE comparaison : chaque artefact d'une liste nommée qui EXISTE doit contenir la formule canonique de la limite de fond, cherchée après normalisation des blancs. Fragment canonique exigé : `modifiée par la PR qu'elle juge`. Manquant → rc 1, verdict `LIMITE-DE-FOND-ABSENTE` avec le chemin. La liste : les trois `scripts/check-*.sh` de la phase, `.github/workflows/ci.yml`, la section ADR-072 de `docs/ADR.md`, `CLAUDE.md`, et chaque `41-1*-SUMMARY.md`. Un artefact non encore créé n'est pas manquant ; c'est le plan de clôture 41-19 qui asserte le compte complet. La clé auto-déclarative `ecrite_en_entete=oui` est SUPPRIMÉE : elle affirmait ce qu'aucune machine ne mesurait"
    - "Les fichiers PLAN et le `41-CONTEXT.md` sont EXCLUS du périmètre du recensement, nommément et avec leur raison écrite dans l'en-tête : un plan prescrit l'interdiction et doit donc la citer, et le CONTEXT est le cadrage figé du périmètre différé qui porte les formulations de D-02. Les SUMMARY, eux, sont DANS le périmètre : ce sont eux qui font des affirmations"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh"
      provides: "recensement des affirmations d'achèvement sur une garde ou sur O-3"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh"
      provides: "contrôle de forme et d'unicité de la citation d'arbitrage par commit"
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "clés `RECENSEMENT-FERMETURE` et `TRACE-ARBITRAGE` mesurées"
  key_links:
    - "Ces deux outils sont rejoués par le plan de clôture 41-19 sur l'état final : les poser en vague 1 leur permet d'être joués par chaque plan intermédiaire sur son propre résultat"
---

<objective>
Poser les deux vérifications qui tiennent les contraintes de fond de la phase, et les rendre
capables de rougir avant de s'en servir :
- le recensement qui refuse qu'un texte du dépôt associe une garde ou O-3 à un mot d'achèvement ;
- le contrôle de forme et d'unicité de la citation d'arbitrage sur les commits de la branche.

Purpose: PROT-04 (« signalée et tracée », jamais un mot d'achèvement) et QUAL-01 (une preuve doit
pouvoir rendre rouge — mode de défaut dominant de ce dépôt en Phase 39 : dix vérificateurs
incapables d'échouer).
Output: deux outils de phase, leurs contrôles négatifs, leurs mutants, et leurs mesures consignées.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

Fichiers à lire au début de la Task 1, jamais deux fois la même plage :
- `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh`
  (patron du recensement versionné : périmètre dérivé, exclusions nominatives, sortie
  `chemin:ligne:contenu`, rc 0 sans ligne)
- `.planning/phases/VFDO-40.1-.../tools/controle-negatif-reel.sh` (patron des contrôles négatifs
  R0..R4 sur clone réel)
- `CLAUDE.md` § « Traçabilité des arbitrages » (la forme canonique : canal et date)
</context>

<discipline>
Contraintes d'exécution valables pour TOUTES les tâches de ce plan.

- **Aucune commande git en ligne dans un `<verify>` ni dans un appel bash direct.** La phase
  s'exécute dans un worktree isolé : le harnais refuse une commande qui nomme git sous une forme
  qu'il ne peut pas rattacher au worktree. Tout travail git — dérivation de base, dépôts de
  fixture — vit À L'INTÉRIEUR des scripts versionnés, invoqués par `bash <chemin>`.
- **Commandes proxifiées** : `diff` et `ls` mentent sur ce poste ; les préfixer de `rtk proxy`.
  Jamais de comptage par `grep | wc -l` — extraire puis comparer par `comm` / `cmp`, ou compter en
  `awk`.
- **Dépôts de fixture** : `git init -q -b main` sous `mktemp -d`, identité passée en `-c`, `trap` de
  nettoyage.
- **Aucun texte produit ici n'associe O-3 ni une garde à un mot d'achèvement.** La formule exacte
  pour O-3 est « signalée et tracée ». Dans les scripts, nommer les jetons interdits par
  construction de chaîne (concaténation de fragments) plutôt que par une phrase qui les co-occurre
  avec un jeton de sujet — sans quoi l'outil se signalerait lui-même s'il entrait un jour dans son
  propre périmètre.
- **Trailer de gate** : aucun fichier de ce plan n'est dans la surface gardée par G-2
  (`scripts/check-*.sh`, `scripts/check-*.sh`, leurs suites `tests/test-*.sh`,
  `.github/workflows/ci.yml`, `scripts/hooks/`), donc aucun trailer `Gate-Touche:` n'est dû ici.
- **Un seul arbitrage ou décision cité par commit** ; tout commit qui cite l'arbitrage du périmètre
  écrit exactement « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ».
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : recensement « aucune garde déclarée fermée », avec allowlist amorcée et contrôles négatifs</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh, .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-aucune-fermeture.sh</files>
  <behavior>
    Contrat observable de `tools/check-aucune-fermeture.sh [--root DIR]` :
    - périmètre découvert non vide, aucune co-occurrence hors allowlist → rc 0, aucune ligne imprimée
    - au moins une co-occurrence hors allowlist → rc 1, une ligne `chemin:ligne:contenu` par hit
    - co-occurrence dans une tournure NIÉE, hors allowlist → rc 1 (une négation ne dispense pas)
    - co-occurrence dont la ligne normalisée figure à l'allowlist → rc 0
    - périmètre découvrant 0 fichier (racine sans dépôt git, ou aucun chemin du périmètre présent) → rc 2
    - argument inconnu ou `--root` inexistant → rc 64
  </behavior>
  <action>
Écrire `tools/check-aucune-fermeture.sh` sur le patron de `tools/check-no-live-250.sh` de la
Phase 40.1. `set -uo pipefail`. Options `--root DIR` (défaut : racine dérivée de l'emplacement du
script, jamais du cwd) et `-h|--help`.

**En-tête.** Dire pourquoi l'outil existe : la contrainte de fond de la Phase 41 veut qu'aucun texte
du dépôt n'affirme l'achèvement d'une garde ni d'O-3, et une contrainte qu'aucune machine ne
vérifie est une intention, pas une garantie (leçon « une preuve doit pouvoir rendre rouge »,
Phase 39). Dire aussi la limite de fond de toute garde in-repo de cette phase : elle peut être
modifiée par la PR qu'elle juge ; elle rend visible et trace, elle ne verrouille rien.

**Périmètre.** Dériver par `git ls-files` sur une liste de pathspecs ÉCRITE dans l'outil :
`scripts/check-*.sh`, `scripts/tests/test-*.sh`, `docs/ADR.md`, `CLAUDE.md`, `CHANGELOG.md`,
`.planning/BACKLOG.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/VFDO-25-*/25-SECURITY.md`,
`.planning/phases/VFDO-41-*/41-*-SUMMARY.md`, `.planning/phases/VFDO-41-*/41-PREUVES.md`,
`.github/workflows/ci.yml`. Imprimer `perimetre: fichiers=<n>` et rendre rc 2 si `n` est nul.
Mesure de référence à consigner en commentaire : 16 fichiers le 2026-09-17 sur HEAD `c8489fc`
(les globs de SUMMARY n'en appariaient encore aucun). **Exclusions nominatives, avec leur raison
écrite** : `.planning/phases/VFDO-41-*/41-*-PLAN.md` (un plan prescrit l'interdiction, donc il la
cite) et `.planning/phases/VFDO-41-*/41-CONTEXT.md` (cadrage figé du périmètre différé, porteur des
formulations de D-02). Les SUMMARY ne sont PAS exclus.

**Comparaison 1 — co-occurrence.** Pour chaque fichier, en `awk` (jamais un `grep -c`), tester
ligne par ligne la présence d'au moins un jeton de sujet ET d'au moins un jeton d'achèvement.
Sujets : `O-3`, `garde`, `Garde`, `gardes`, `Gardes`, `check-baseline-arbitrage`,
`check-gate-touche`, `check-push-sans-pr`. Achèvements : `fermé`, `Fermé`, `fermée`, `fermés`,
`fermées`, `clos`, `Clos`, `close`, `Close`, `closes`, `closed`. Écrire en en-tête que `gate` et
`gates` sont volontairement hors des sujets, avec leur raison : la clôture d'une EXIGENCE est un
autre objet que celle d'une garde, et les inclure rendrait le recensement si bruyant qu'il finirait
désarmé. Écrire aussi que la forme verbale nue `ferme` est hors des jetons d'achèvement, pour que la
formule de doctrine « on ne ferme rien » reste écrivable.

**Comparaison 2 — allowlist.** Une table versionnée d'entrées, chacune = une ligne NORMALISÉE
exacte (espaces de tête et de fin retirés) plus une raison sur la même entrée. Un hit dont la ligne
normalisée est présente à l'allowlist n'est pas imprimé. Amorcer avec les trois lignes héritées
mesurées, chacune avec sa raison : la ligne BUDG-04 de `REQUIREMENTS.md` (verbe « garder » + clôture
d'une exigence, deux objets distincts) ; la ligne de la table STRIDE `T-25-16` de `25-SECURITY.md`
(la colonne `closed` porte sur la menace, pas sur l'observation O-3 citée à côté) ; la ligne
historique « fermé par garde d'existence » du `CHANGELOG.md`. Imprimer
`allowlist: entrees=<n> appliquees=<m>` : une entrée jamais appliquée est un signal de dérive à
consigner, jamais un rouge.

**Comparaison 3 — sonde de limite de fond** (avertissement 7 du vérificateur frais, décision du
manager du 2026-09-17). Une liste d'artefacts PORTEURS est écrite dans l'outil : les trois
`scripts/check-*.sh` de la phase, `.github/workflows/ci.yml`, la section `## ADR-072` de
`docs/ADR.md`, `CLAUDE.md`, et chaque `.planning/phases/VFDO-41-*/41-1*-SUMMARY.md`. Pour chaque
artefact de cette liste qui EXISTE, exiger la présence du fragment canonique
`modifiée par la PR qu'elle juge`, cherché après normalisation des blancs (lignes jointes, suites de
blancs réduites à une espace) — une formule coupée par un retour à la ligne reste conforme. Manquant
→ rc 1, verdict `LIMITE-DE-FOND-ABSENTE` avec le chemin. Un artefact qui n'existe pas encore n'est
pas manquant : imprimer `limite: exiges=<n> porteurs=<m> manquants=<liste|aucun>` et laisser le plan
de clôture asserter le compte complet. Écrire en en-tête pourquoi cette sonde existe : la limite de
fond était jusqu'ici affirmée par une clé auto-déclarative, c'est-à-dire par personne.

**Sortie.** Une ligne `chemin:ligne:contenu` par hit de co-occurrence non dispensé, une ligne
`LIMITE-DE-FOND-ABSENTE: <chemin>` par artefact porteur muet, puis rc 1 s'il y a au moins une ligne
de l'une ou l'autre sorte. Aucune ligne → rc 0.

Écrire ensuite `tools/test-check-aucune-fermeture.sh` : `ok`/`ko` à trois champs, `trap`. Contrôles
négatifs sur arborescences jetables (`git init -q -b main` + fichiers du périmètre) :
- R0 dépôt de fixture propre → rc 0, zéro ligne
- R1 ligne affirmant l'achèvement d'O-3 → rc 1, une ligne imprimée
- R2 même affirmation sous forme NIÉE → rc 1 (la négation ne dispense pas)
- R3 ligne affirmant l'achèvement d'une garde nommée par son script → rc 1
- R4 ligne présente à l'allowlist → rc 0
- R5 ligne portant un jeton d'achèvement sans jeton de sujet → rc 0 (contrôle négatif : un rouge
  ici serait aussi faux qu'un vert de complaisance)
- R6 ligne portant `gate` et un jeton d'achèvement → rc 0 (la borne de sujet est volontaire)
- R7 racine hors d'un arbre git → rc 2 ; R8 option inconnue → rc 64
- R9 artefact porteur existant PRIVÉ de la formule de limite de fond → rc 1 et
  `LIMITE-DE-FOND-ABSENTE` ; R10 même artefact avec la formule COUPÉE par un retour à la ligne →
  rc 0 (la normalisation des blancs la reconnaît) ; R11 artefact porteur absent du disque → rc 0 et
  compté en `exiges` sans être compté en `manquants`

**Règle de comptage des mutants** (avertissement 3 du vérificateur frais) : chaque mutant asserte le
rc EXACT sur le mutant ET sur l'original, imprime assertion, attendu et obtenu, et n'est crédité que
par une ligne de forme canonique `✓ MUT-<n> TUE : rc_mutant=<x> attendu <x>, rc_original=<y> attendu
<y>`. Un mutant qui « échoue » par un plantage ou par une découverte vide n'est pas un mutant tué.

TROIS mutants, vérifiés par `cmp -s` (refusé si identique) et `bash -n` :
- MUT-1 neutralise l'exigence de co-occurrence (un jeton d'achèvement seul suffit) → R5 devient
  rouge sur le mutant, reste vert sur l'original
- MUT-2 neutralise l'assertion de périmètre non vide (0 fichier rendant 0) → R7 rend 0 sur le
  mutant, 2 sur l'original
- MUT-3 neutralise la sonde de limite de fond (la formule n'est plus cherchée) → R9 devient VERTE
  sur le mutant, reste rouge sur l'original. C'est le mutant exigé par l'avertissement 7 : la sonde
  retirée doit se voir
Bilan `== bilan : <PASS> ok, <FAIL> ko ==`, sortie non nulle si `FAIL` non nul.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-aucune-fermeture.sh</automated>
    <fails_when>exit non nul ; ou une ligne `ko` ; ou `NON OPPOSABLE` dans la sortie ; ou le bilan annonce un nombre de `ko` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh > "$T/o" 2>&1 || rc=$?; cat "$T/o"; n=$(awk "/^perimetre: fichiers=/{split(\$2,a,\"=\"); print a[2]+0}" "$T/o"); h=$(awk "!/^perimetre: |^allowlist: |^limite: /&&NF{c++} END{print c+0}" "$T/o"); l=$(awk "/^limite: /{f=1} END{exit f?0:1}" "$T/o" && echo oui || echo non); echo "recensement rc=$rc fichiers=${n:-0} hits=$h ligne_limite=$l"; [ "$rc" -eq 0 ] && [ "${n:-0}" -ge 16 ] && [ "$h" -eq 0 ] && [ "$l" = oui ]'</automated>
    <fails_when>rc différent de 0 ; ou `fichiers=` inférieur à 16 (périmètre amputé) ; ou `hits=` différent de 0 (une affirmation d'achèvement subsiste, un artefact porteur est muet sur la limite de fond, ou une ligne héritée manque à l'allowlist — la classer, jamais l'exclure en bloc) ; ou `ligne_limite=non` (la sonde de limite de fond ne rend pas compte de ce qu'elle a vu)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend 0, aucun `ko`, aucun `NON OPPOSABLE`, MUT-1, MUT-2 et MUT-3 tués, chacun crédité par une ligne canonique `✓ MUT-<n> TUE : rc_mutant=… attendu …, rc_original=… attendu …`
    - la sonde de limite de fond imprime `limite: exiges=<n> porteurs=<m> manquants=<…>` et R9/R10/R11 se comportent comme spécifié
    - sur le dépôt réel : rc 0, au moins 16 fichiers découverts, zéro ligne de hit
    - l'allowlist porte exactement 3 entrées à l'issue de la tâche, chacune avec sa raison écrite
    - les exclusions (PLAN, CONTEXT) et la borne de sujet (`gate` hors liste) sont écrites avec leur raison dans l'en-tête
  </acceptance_criteria>
  <done>La contrainte de fond de la phase est vérifiable par machine, et la vérification sait rougir sur une négation comme sur une affirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : contrôle de trace d'arbitrage sur les commits propres à la branche</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh, .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-trace-arbitrage.sh</files>
  <behavior>
    Contrat observable de `tools/check-trace-arbitrage.sh [--root DIR] [--base-ref REF]` — la base
    est LUE dans `BASE-TRACE-ARBITRAGE:` du `41-PREUVES.md`, `--base-ref` ne servant qu'aux fixtures :
    - plage non vide, tous les commits conformes → rc 0
    - un commit cite un arbitrage ou une décision sans la forme canonique (nom, canal, date ISO) → rc 1, verdict `FORME-NON-CONFORME`
    - un commit cite PLUS D'UN arbitrage ou décision → rc 1, verdict `CITATIONS-MULTIPLES`
    - un commit cite `arbitrage` et la date `2026-09-17` sans la chaîne canonique du périmètre → rc 1, verdict `ARBITRAGE-DE-PERIMETRE-MAL-CITE`
    - un commit qui ne cite aucun arbitrage → ignoré, n'empêche pas rc 0
    - une citation conforme COUPÉE par un retour à la ligne → conforme, rc 0 (normalisation des blancs)
    - un commit ANTÉRIEUR à la borne lue, même non conforme → hors plage, n'empêche pas rc 0
    - clé `BASE-TRACE-ARBITRAGE:` absente, illisible, ou ne résolvant pas en commit → rc 2
    - plage vide (borne égale HEAD, ou zéro commit) → rc 3, verdict `PLAGE-VIDE`
    - hors d'un arbre git → rc 2
    - argument inconnu, `--base-ref` sans valeur, `--root` inexistant → rc 64
  </behavior>
  <action>
Écrire `tools/check-trace-arbitrage.sh`. Même squelette que la Task 1 : `set -uo pipefail`, en-tête
« pourquoi », énumération des codes de sortie, options `--root`, `--base-ref`, `-h`.

**Base — BORNÉE, pas dérivée** (bloquant 1 du vérificateur frais, décision du manager du
2026-09-17). La base est la valeur de `--base-ref` si elle est fournie (surcharge de fixture
uniquement), sinon le sha de la ligne `BASE-TRACE-ARBITRAGE: <sha>` lue dans
`41-PREUVES.md`, consignée par le plan 41-14 avant toute écriture du périmètre. Clé absente,
illisible, non hexadécimale, ou ne résolvant pas en commit dans ce dépôt → rc 2 NON VÉRIFIABLE avec
la raison recopiée, jamais un vert et jamais un repli silencieux sur `merge-base` ou sur `HEAD^`.
Borne égale HEAD, ou plage à zéro commit → rc 3.

À écrire en en-tête, avec son motif mesuré : les commits ANTÉRIEURS à cette borne — cadrage,
planification, décisions de recadrage — précèdent la garde et ne sont pas réécrits ; rejoué sans
borne sur `5238cba..HEAD`, le contrôle rendait 8 commits fautifs que la phase n'a aucun moyen de
corriger, ce qui en aurait fait un rouge permanent, donc un vérificateur qu'on désarme. La garde
juge ce que la phase écrit encore, et le dit.

**Découverte.** Imprimer `decouverte: commits=<n> citants=<m>`. `n` = commits de
`git rev-list --reverse <base>..HEAD`, MERGES COMPRIS (un merge peut citer un arbitrage), mais en
excluant les commits dont le message entier correspond à la forme du merge éphémère de GitHub
(`Merge <40 hex> into <40 hex>`) : borne écrite en en-tête, ce commit n'est pas écrit par un humain
ni par un agent de la phase. `m` = commits dont le message contient `arbitrage`, `arbitrages`,
`décision`, `décisions`, `decision` ou `decisions`. `n` nul → rc 3.

**Comparaison 0 — normalisation des blancs, AVANT toute autre comparaison** (bloquant 1 (a) du
vérificateur frais). Le message intégral de chaque commit (`git log -1 --format=%B`) est joint en UNE
seule chaîne — retours à la ligne remplacés par une espace — puis toute suite de blancs est réduite à
une espace unique, et les blancs de tête et de fin sont retirés. Toutes les comparaisons suivantes
portent sur cette chaîne normalisée, jamais sur les lignes brutes. Motif à écrire en en-tête : une
citation coupée en fin de ligne est conforme, et la refuser serait le même faux rouge que celui déjà
corrigé sur le ledger de ce dépôt.

**Comparaison 1 — forme canonique.** Sur la chaîne normalisée, exiger au moins une occurrence portant
le mot-clé, puis au moins deux virgules, puis une date ISO `AAAA-MM-JJ` — la forme « <mot-clé> <nom>,
<canal>, <date> » du `CLAUDE.md` § Traçabilité des arbitrages. Écrire en en-tête que le contrôle
porte sur la FORME (le canal et la date sont nommés), jamais sur la véracité : « un simple
“arbitrage Samuel” a la même forme qu'il soit vrai ou fabriqué », et c'est justement la forme longue
qui rend l'attribution relisable.

**Comparaison 2 — unicité.** Compter les occurrences conformes de la chaîne normalisée : plus d'une → rc 1, verdict
`CITATIONS-MULTIPLES`. Deux occurrences de la MÊME chaîne canonique dans un même message comptent
pour une seule citation (dédupliquer sur la chaîne normalisée) : c'est « un seul arbitrage cité »,
pas « une seule mention ».

**Comparaison 3 — arbitrage du périmètre.** Tout commit dont la chaîne normalisée contient
`arbitrage` ET la date `2026-09-17` doit contenir la chaîne exacte `arbitrage Samuel, AskUserQuestion
session principale, 2026-09-17` — comparée elle aussi après normalisation, donc insensible à une
coupure de ligne. Sinon rc 1, verdict `ARBITRAGE-DE-PERIMETRE-MAL-CITE`, avec le sha court et
la ligne fautive recopiée.

Sortie : une ligne `<sha-court> <verdict> : <ligne fautive>` par écart, puis le code de sortie.

Écrire `tools/test-check-trace-arbitrage.sh` sur le même patron que la Task 1. Fixtures : dépôts
jetables où chaque cas commite des messages contrôlés. Cas :
- C0 trois commits dont un citant conformément → rc 0
- C1 commit citant `arbitrage Samuel` sans canal ni date → rc 1 `FORME-NON-CONFORME`
- C2 commit citant deux arbitrages distincts et conformes → rc 1 `CITATIONS-MULTIPLES`
- C3 même chaîne canonique répétée deux fois dans un message → rc 0 (contrôle négatif)
- C4 commit citant `arbitrage` et `2026-09-17` dans une forme voisine mais non identique → rc 1
  `ARBITRAGE-DE-PERIMETRE-MAL-CITE`
- C5 commits ne citant rien → rc 0
- C6 message au format du merge éphémère de GitHub, sans citation → rc 0 (borne)
- C7 `--base-ref` égal à HEAD → rc 3 ; C8 ref inexistante → rc 2 ; C9 option inconnue → rc 64
- C10 citation conforme COUPÉE par un retour à la ligne (nom en fin de ligne, canal et date à la
  ligne suivante) → rc 0 : contrôle négatif de la normalisation, un rouge ici serait le faux rouge
  que le bloquant 1 (a) demande de fermer
- C11 commit NON conforme placé AVANT la borne, plus un commit conforme après → rc 0 : la borne
  tient, les commits antérieurs ne sont pas jugés
- C12 registre sans ligne `BASE-TRACE-ARBITRAGE:` et sans `--base-ref` → rc 2 ; C13 registre portant
  une valeur non résoluble en commit → rc 2

**Règle de comptage des mutants** (avertissement 3 du vérificateur frais) : chaque mutant asserte le
rc EXACT sur le mutant ET sur l'original, imprime assertion, attendu et obtenu, et n'est crédité que
par une ligne de forme canonique `✓ MUT-<n> TUE : rc_mutant=<x> attendu <x>, rc_original=<y> attendu
<y>`. Un mutant qui « échoue » par un plantage ou par une plage vide n'est pas un mutant tué.

Plus QUATRE mutants (`cmp -s` + `bash -n`) :
- MUT-1 neutralise le contrôle de forme → C1 devient vert sur le mutant, reste rouge sur l'original
- MUT-2 neutralise le comptage d'unicité → C2 devient vert sur le mutant, reste rouge sur l'original
- MUT-3 neutralise la lecture de la borne `BASE-TRACE-ARBITRAGE` (repli sur l'adjacence `HEAD^`) →
  la fixture C11 (commit fautif AVANT la borne) devient ROUGE sur le mutant, reste verte sur
  l'original : c'est le mutant qui prouve que la borne existe et qu'elle n'est pas contournée par un
  repli silencieux
- MUT-4 neutralise la normalisation des blancs → la fixture C10 (citation coupée par un retour à la
  ligne) devient ROUGE sur le mutant, reste verte sur l'original
Bilan `== bilan : <PASS> ok, <FAIL> ko ==`, sortie non nulle si `FAIL` non nul.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-trace-arbitrage.sh</automated>
    <fails_when>exit non nul ; ou une ligne `ko` ; ou `NON OPPOSABLE` dans la sortie ; ou moins de QUATRE lignes canoniques `✓ MUT-<n> TUE : rc_mutant=…` (un mutant tué par un plantage ou par une plage vide ne crédite rien)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh > "$T/o" 2>&1 || rc=$?; cat "$T/o"; echo "trace rc=$rc"; case "$rc" in 0|3) : ;; *) exit 1 ;; esac; awk "/^decouverte: commits=/{f=1} END{exit f?0:1}" "$T/o"'</automated>
    <fails_when>rc différent de 0 et de 3 ; ou absence de la ligne `decouverte: commits=` ; ou absence de la ligne `borne:` qui recopie le sha lu. La plage est bornée par `BASE-TRACE-ARBITRAGE` : les commits de cadrage et de planification antérieurs ne sont PAS jugés, et le contrôle ne doit jamais prétendre le contraire</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend 0, aucun `ko`, MUT-1 à MUT-4 tués en forme canonique, les contrôles négatifs C3, C10 et C11 restant verts
    - sur la branche réelle : rc 0 (ou 3 si la plage est vide), avec la ligne de découverte ET la ligne `borne:` qui recopie le sha lu
    - l'en-tête écrit : le contrôle porte sur la forme et jamais sur la véracité ; la borne du merge éphémère ; la borne de portée avec son motif mesuré (8 fautifs sans borne sur `5238cba..HEAD`, commits antérieurs non réécrits) ; la normalisation des blancs
  </acceptance_criteria>
  <done>La convention de traçabilité des arbitrages de ce dépôt est vérifiable par machine sur la branche, avec une base dérivée et non devinée.</done>
</task>

<task type="auto">
  <name>Task 3 : mesures consignées au registre de preuves</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <precondition>Tasks 1 et 2 vertes.</precondition>
  <action>
Ajouter à `41-PREUVES.md` une section `## 41-15 — outillage de preuve du périmètre sans admin`,
au format `CLE: valeur` existant du registre (une valeur non mesurée s'écrit `non_prouvee` ou
`indetermine`, jamais supposée ; aucun chemin absolu de machine). Ne PAS modifier la section
`## 41-01 — préalables externes` ni sa ligne `CONTEXTES-CHECKS`.

Clés à écrire, chacune avec la valeur réellement lue :
- `RECENSEMENT-FERMETURE: rc=<n> fichiers=<n> hits=<n> allowlist_entrees=<n> allowlist_appliquees=<n>`
- `RECENSEMENT-CONTROLES: R0..R8=<liste des rc obtenus> mutants_tues=<n>`
- `TRACE-ARBITRAGE: rc=<n> commits=<n> citants=<n> base=<sha-court>`
- `TRACE-CONTROLES: C0..C9=<liste des rc obtenus> mutants_tues=<n>`
- `OUTILS-PERIMETRE: distribues=non etapes_ci=0 decouverts_par_le_job_tests=non` — les deux outils
  sont des vérifications de phase, jouées explicitement, jamais des gates distribués (précédent
  Phase 40.1)
- `LIMITE-DE-FOND: exiges=<n> porteurs=<n> manquants=<liste|aucun>` — valeurs MESURÉES par la sonde
  de `check-aucune-fermeture.sh`, jamais auto-déclarées (avertissement 7 du vérificateur frais : la
  clé `ecrite_en_entete=oui` affirmait ce qu'aucune machine ne mesurait, elle est supprimée)
- `BORNE-TRACE: sha=<valeur lue> source=41-PREUVES.md commits_juges=<n> commits_hors_borne=<n>`

Puis rejouer les deux suites et le recensement une dernière fois APRÈS écriture : le registre est
lui-même dans le périmètre du recensement, donc ce qu'on y écrit doit passer la vérification qu'on y
consigne. Si une ligne du registre fait rougir le recensement, reformuler la ligne — ne jamais
l'ajouter à l'allowlist pour la faire passer.

**Rejeu CI, exigé parce que ce plan commite du versionné** (avertissement 4 du vérificateur frais) :
rejouer les jobs `gates` ET `tests` par l'outil de rejeu de la Phase 40.1, avec la découverte
complète et jamais une liste d'étapes recopiée, en `bash -e`. Consigner au SUMMARY chaque ligne
`REJEU`, le nombre d'étapes `gates`, le bilan du job `tests` avec son nombre de suites, tout `SKIP`
avec sa raison, et le périmètre non rejoué. Les deux outils de ce plan vivent sous `.planning/` :
ils ne sont pas découverts par le `find` du job `tests`, et l'écrire au SUMMARY — c'est la raison
pour laquelle ils sont joués explicitement par chaque plan.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md; k=$(awk "/^RECENSEMENT-FERMETURE: |^RECENSEMENT-CONTROLES: |^TRACE-ARBITRAGE: |^TRACE-CONTROLES: |^OUTILS-PERIMETRE: |^LIMITE-DE-FOND: |^BORNE-TRACE: /{c++} END{print c+0}" "$P"); s=$(awk "/^## 41-15 /{c++} END{print c+0}" "$P"); o=$(awk "/^CONTEXTES-CHECKS: /{c++} END{print c+0}" "$P"); echo "cles=$k section=$s contextes_checks_intact=$o"; [ "$k" -eq 7 ] && [ "$s" -eq 1 ] && [ "$o" -eq 1 ]'</automated>
    <fails_when>`cles=` différent de 7 ; ou `section=` différent de 1 ; ou `contextes_checks_intact=` différent de 1 (la ligne mesurée par 41-01 a été perdue)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh || rc=$?; echo "recensement apres ecriture rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>rc différent de 0 (le registre nouvellement écrit fait rougir le recensement ; conduite : reformuler la ligne, jamais l'ajouter à l'allowlist)</fails_when>
    <automated>bash --noprofile --norc -e -c 'a=0; b=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-aucune-fermeture.sh >/dev/null 2>&1 || a=$?; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/test-check-trace-arbitrage.sh >/dev/null 2>&1 || b=$?; echo "suites rc: recensement=$a trace=$b"; [ "$a" -eq 0 ] && [ "$b" -eq 0 ]'</automated>
    <fails_when>l'un des deux rc est différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); a=0; b=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || a=$?; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || b=$?; awk "/^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/g" "$T/t"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); c=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) x=\$(i-1); if (\$i ~ /^échec/) y=\$(i-1) } } END{ if (!f) print \"absent\"; else print x+0, y+0 }" "$T/t"); echo "gates rc=$a etapes=${n:-0} tests rc=$b bilan(N M)=$c"; [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "${n:-0}" -ge 11 ] && [ "$c" != absent ] && set -- $c && [ "$1" -ge 80 ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>l'un des deux rc est non nul ; ou `etapes=` inférieur à 11 ; ou `bilan(N M)=absent` ; ou N inférieur à 80 ; ou M différent de 0. Ce plan commite des fichiers versionnés : il ne se clôt pas sans avoir rejoué la découverte complète des deux jobs</fails_when>
  </verify>
  <acceptance_criteria>
    - les SEPT clés sont présentes, avec des valeurs mesurées (aucune valeur inventée ; `non_prouvee` si non mesurée)
    - la section `## 41-01` et sa ligne `CONTEXTES-CHECKS` sont intactes
    - le recensement rend 0 APRÈS écriture du registre, sans nouvelle entrée d'allowlist
    - rejeu `gates` (≥ 11 étapes) et `tests` (≥ 80 suites, 0 échec) verts, consignés au SUMMARY avec leur périmètre non rejoué
  </acceptance_criteria>
  <done>Les deux outils sont posés, prouvés capables de rougir, et leurs mesures sont au registre — pas déclarées dans un SUMMARY seul.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texte versionné → affirmation de conformité | un SUMMARY ou un CHANGELOG peut affirmer un achèvement que rien ne soutient |
| message de commit → attribution d'un arbitrage humain | une citation fabriquée a la même forme qu'une vraie |
| outil de vérification → PR qui le contient | l'outil vit dans le dépôt : la PR jugée peut l'amender |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-62 | Repudiation | affirmation d'achèvement d'O-3 ou d'une garde glissée dans un SUMMARY | high | mitigate | recensement à co-occurrence sujet × achèvement sur 16 fichiers, négations comprises ; allowlist nominative à trois entrées justifiées ; contrôles négatifs R1/R2/R3 et mutant MUT-1 |
| T-41-63 | Spoofing | arbitrage humain fabriqué dans un message de commit | high | accept | forme longue exigée (nom, canal, date) et unicité par commit — la véracité reste hors d'atteinte d'une machine, l'écrire en en-tête ; l'attribution reste un geste humain relisable |
| T-41-64 | Tampering | allowlist élargie pour faire passer un hit vivant | medium | mitigate | allowlist par ligne normalisée exacte, versionnée avec sa raison ; compteur `appliquees` imprimé ; consigne explicite : reformuler plutôt qu'allowlister, et une quatrième entrée est un diff visible |
| T-41-65 | Information disclosure | recensement vert par périmètre vide | high | mitigate | `perimetre: fichiers=` imprimé, rc 2 si nul, plancher de 16 fichiers dans le verify, mutant MUT-2 |
| T-41-66 | Elevation of privilege | commit fautif placé en tête de plage et manqué par une base lue dans l'adjacence du journal | high | mitigate | base dérivée par `merge-base` avec cascade nommée ; mutant MUT-3 du contrôle de trace prouve le flip |
</threat_model>

<verification>
Deux suites vertes, SEPT mutants tués au total (3 + 4) en forme canonique, contrôles négatifs
R5/R6/R9/R10/R11/C3/C10/C11 restés verts ; recensement rc 0 sur au moins 16 fichiers, zéro hit,
3 entrées d'allowlist, ligne `limite:` imprimée ; contrôle de trace rc 0 avec sa ligne `borne:` ;
sept clés au registre ; `CONTEXTES-CHECKS` intacte ; rejeu `gates` et `tests` verts.
</verification>

<success_criteria>
Les trois contraintes de fond de la phase — aucun texte n'affirme l'achèvement d'une garde ou d'O-3,
chaque artefact porteur énonce la limite de fond, un seul arbitrage cité par commit dans la forme
canonique sur la plage que la phase écrit encore — sont vérifiables par machine, et les vérifications
sont prouvées capables de rendre rouge.
</success_criteria>

## Artifacts this phase produces

- `tools/check-aucune-fermeture.sh` — codes 0 / 1 / 2 / 64 ; sortie `chemin:ligne:contenu` et
  `LIMITE-DE-FOND-ABSENTE: <chemin>` ; lignes `perimetre:`, `allowlist:` et `limite:` ; MUT-1 à MUT-3.
- `tools/check-trace-arbitrage.sh` — verdicts `FORME-NON-CONFORME`, `CITATIONS-MULTIPLES`,
  `ARBITRAGE-DE-PERIMETRE-MAL-CITE`, `PLAGE-VIDE` ; codes 0 / 1 / 2 / 3 / 64 ; base LUE dans
  `BASE-TRACE-ARBITRAGE:` ; lignes `borne:` et `decouverte:` ; MUT-1 à MUT-4.
- `41-PREUVES.md` § 41-15 — sept clés mesurées, dont `LIMITE-DE-FOND` et `BORNE-TRACE`.

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-15-SUMMARY.md` when done
</output>

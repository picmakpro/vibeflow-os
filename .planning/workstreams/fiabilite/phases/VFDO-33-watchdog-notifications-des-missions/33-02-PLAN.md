---
phase: 33-watchdog-notifications-des-missions
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - plugin/conductor/scripts/dag.sh
  - plugin/conductor/scripts/tests/test-dag.sh
  - plugin/dev-orchestrator/references/mission-flow.md
autonomous: true
requirements: [WTCH-01]

must_haves:
  truths:
    - "`dag.sh mark` (point de transition explicite unique, `dag.sh:241`) shelle un appel à `driver-lock.sh mark-progress --owner=<owner-observé>` à CHAQUE appel, quel que soit le `--status` — c'est l'accroche naturelle citée par 33-TERRAIN.md §2 : le seul point où `dag.sh` sait qu'un nœud vient de bouger. L'appel a lieu APRÈS `save(dag)` (pas avant) — `save()` est une réécriture complète NON atomique (33-TERRAIN.md), insérer un appel externe entre la mutation en mémoire et l'écriture élargirait sans raison la fenêtre où un lecteur concurrent verrait un JSON partiel."
    - "L'owner passé à `mark-progress` est LU depuis `driver-lock.sh status` (champ `owner`), jamais deviné ni codé en dur — si aucun lock n'est présent (`present: false`), aucun appel à `mark-progress` n'est fait."
    - "Le sibling `driver-lock.sh` est résolu par le RÉPERTOIRE DU SCRIPT `dag.sh` lui-même (`dirname` de `$0`, calculé côté bash AVANT le heredoc python, passé en argv), JAMAIS par le cwd — distinction explicite du vecteur RCE fermé en D-07/T33 (33-TERRAIN.md, `dag.sh` L110-148) : la cible ici est un fichier posé par le MÊME glob d'installation que `dag.sh`, jamais une valeur dérivée d'un cwd non fiable."
    - "AUCUNE panne côté `driver-lock.sh` (absent, non exécutable, JSON illisible, exit non nul, timeout, exception quelconque) ne fait jamais échouer `dag.sh mark` — dégradation en no-op silencieux, patron identique à `compute_stages()` (ADR-031 : signale/dégrade, ne bloque jamais le cœur du DAG). SEULE exception au silence : si `status` a confirmé `present: true` et un `owner` non vide, mais que l'appel `mark-progress` lui-même échoue (`returncode != 0`), une ligne diagnostique BRUYANTE part sur stderr (best-effort, jamais un exit non nul) — sans ce signal, `progress_epoch` gèlerait sur une mission SAINE (lock présent, détenu, mais l'écriture refuse) et 33-03 lirait un faux stall en silence (4ᵉ issue QUAL-01, trouvée manquante en plancheck externe)."
    - "Chaque appel `subprocess.run` vers `driver-lock.sh` passe une LISTE d'arguments (jamais `shell=True`, jamais de concaténation de chaîne) et porte un `timeout=2` explicite — un `driver-lock.sh` qui pendrait ne doit jamais faire pendre `dag.sh mark`, ET doit revenir sous les 5s auxquels `run_bounded` de `test-dag.sh` tue le process (mesuré : `( sleep 5; kill -9 \"$pid\" )`) — un `timeout=5` interne laisserait `mark` mourir AVANT `save(dag)` sur le cas de panne \"pend\", perdant la transition elle-même (bloquant reproduit par le plancheck externe)."
    - "D-33-F (décision Samuel, 2026-08-17) — un DEUXIÈME point de contrôle en LECTURE (relais du verdict `check-guard-health.sh --hook`) est requis au même point `mark`, APRÈS `record_progress()`. **Recoordination inter-plans (2ᵉ plancheck externe)** : ce point de lecture est déplacé vers le plan **33-05**, qui touche déjà `dag.sh`/`test-dag.sh` en vague 3 et dépend déjà de 33-03 pour la stabilité du contrat `--hook` — ce plan-ci (33-02) N'IMPLÉMENTE PLUS D-33-F et ne dépend donc plus de 33-03 : sa seule responsabilité redevient l'écriture (`record_progress`), qui ne dépend que de 33-01. Motif du déplacement : la dépendance non déclarée de 33-02 sur 33-03 (33-02 ne pouvait pas être vert avant que 33-03 soit committé, alors que son frontmatter le déclarait indépendant) créait une collision de vague ET une collision de numéros de cas avec 33-05 sur `test-dag.sh` — les deux plans étendaient le même fichier en parallèle sur la même plage. Regrouper toute la logique de LECTURE (relais de stall + notification de jalon) dans le seul plan qui la consomme réellement (33-05, wave 3, déjà dépendant de 33-01/33-02/33-03/33-04) ferme les deux collisions d'un geste, sans toucher aux vagues d'origine (`wave: 1 = {33-01, 33-04}`, `wave: 2 = {33-02, 33-03}`, `wave: 3 = {33-05}`)."
    - "D-33-E partie (b) (décision Samuel, 2026-08-17, datée) — le protocole de heartbeat des managers (`plugin/dev-orchestrator/references/mission-flow.md`, Pattern A) est amendé pour que la vivacité (`driver-lock.sh heartbeat`) soit émise sur une cadence INDÉPENDANTE et PLUS FRÉQUENTE que les transitions de nœud (`dag.sh mark`). Motif mesuré : avant cet amendement, le heartbeat est émis « entre les étapes » (mission-flow.md L51-55), c'est-à-dire au MÊME tour que `dag.sh mark` — les deux horloges (`heartbeat_epoch`, `progress_epoch`) ont alors la même source et le même instant d'émission, donc ne divergent JAMAIS en usage réel (mission gelée → les deux gèlent ensemble → `stale` avant que `progress_age_seconds` seul ne compte ; mission vivante → les deux avancent ensemble). Le cas « vivant mais bouclant » (stall) n'était atteignable QUE sur epochs forgés en test, jamais en production — c'est un changement de DOCTRINE des managers, écrit comme tel dans `mission-flow.md`, pas glissé silencieusement dans le code."
  artifacts:
    - "plugin/conductor/scripts/dag.sh : fonction `record_progress(driver_lock_sh)`, résolution `SCRIPT_DIR`/`DRIVER_LOCK_SH` côté bash, un appel dans le bloc `mark`, APRÈS `save(dag)` — `check_stall_signal()`/`CHECK_GUARD_HEALTH_SH` ne sont PLUS des artefacts de ce plan (déplacés vers 33-05, voir D-33-F ci-dessus)"
    - "plugin/conductor/scripts/tests/test-dag.sh : cas T34 à T40 (T41, ex-cas `check_stall_signal`, appartient désormais à 33-05)"
    - "plugin/dev-orchestrator/references/mission-flow.md : amendement daté (2026-08-17, D-33-E) du Pattern A — heartbeat sur cadence propre, distincte des transitions de nœud"
  key_links:
    - "driver-lock.sh mark-progress (plan 33-01) <-> dag.sh record_progress() : la forme JSON de sortie `{\"ok\": ..., \"owner\": ..., \"progress_epoch\": ...}` documentée en 33-01 est ce que ce plan invoque ; un changement de cette forme côté 33-01 casserait silencieusement ce plan (dégradation en no-op) — le plan 33-01 doit rester en vague 1, celui-ci en vague 2"
    - "progress_epoch écrit ici -> plan 33-03 : le détecteur de stall lit ce champ pour distinguer sain/stall/abandon sur un DAG dont les nœuds bougent réellement"
    - "progress_epoch écrit ici -> plan 33-05 : `check_stall_signal()` (33-05, D-33-F) lit le verdict `check-guard-health.sh --hook` qui lui-même dépend de ce champ ; ce plan n'invoque jamais `check-guard-health.sh` lui-même"
  prohibitions:
    - "Ne JAMAIS faire dépendre le succès de `dag.sh mark` (mise à jour de `idx[nid][\"status\"]`, `recompute()`, `save(dag)`) de la réussite de l'appel driver-lock — l'ordre doit garantir que le DAG est mis à jour et SAUVEGARDÉ AVANT que ce contrôle best-effort ne s'exécute"
    - "Ne JAMAIS réintroduire un candidat de résolution relatif au CWD pour `driver-lock.sh` (motif D-07, 5e occurrence déjà fermée sur ce fichier) — seule la résolution par le répertoire du script `dag.sh` lui-même est autorisée"
    - "Ne JAMAIS utiliser `shell=True` ni interpoler `nid`/`status`/`owner` dans une chaîne de commande shell"
    - "Aucun `sleep` non borné dans les cas de test — tout scénario de blocage utilise `run_bounded` (déjà présent dans la suite, tue à 5s MESURÉ, pas 10s) ou un timeout explicite côté script sous test"
    - "Ne JAMAIS ajouter `check_stall_signal()`/`CHECK_GUARD_HEALTH_SH` à ce plan — ce point de lecture appartient désormais à 33-05 (D-33-F déplacé, voir must_haves) ; ce plan reste strictement `record_progress()`, dépendant uniquement de 33-01"
    - "Ne JAMAIS committer les untracked `.gsd/`, `MISSION-3x.dag.json`, dossiers `VFDO-31`/`VFDO-36` — hors périmètre de ce plan"
    - "`test-dag.sh` n'a AUCUNE garde anti-vert-à-vide dans son épilogue avant ce plan (mesuré : `[ \"$FAIL\" -eq 0 ] && exit 0 || exit 1`, sans jamais vérifier que `PASS+FAIL` est non nul) — ce plan l'ajoute : un run qui n'exécute AUCUNE assertion (script cassé en amont, `assert` jamais appelée) ne doit jamais sortir 0 par accident. Même patron que `test-check-guard-health.sh` (D12, déjà en place)."
---

<objective>
Câbler l'unique accroche naturelle de `dag.sh` (le point `mark`, `dag.sh:241`) à `driver-lock.sh
mark-progress` posé en 33-01. Amender aussi la doctrine de heartbeat des managers (`mission-flow.md`,
D-33-E) pour que vivacité et progrès cessent de battre sur le même tour, condition sans laquelle le
cas « vivant mais bouclant » n'est jamais atteignable qu'en test.

**Recoordination (2ᵉ plancheck externe)** : le second point de contrôle en LECTURE (D-33-F, relais du
verdict `check-guard-health.sh --hook`) initialement prévu dans ce plan est DÉPLACÉ vers 33-05 — voir
`must_haves.truths` pour le motif complet. Ce plan reste strictement le câblage en ÉCRITURE.

Purpose : sans le câblage en écriture, `progress_epoch` posé en 33-01 ne bouge jamais en usage réel.
Sans l'amendement de doctrine (D-33-E), les deux horloges consommées par 33-03 ne divergent jamais
hors d'un test à epochs forgés : le critère de succès n°2 serait prouvé par test et faux en
production. Le câblage en lecture (D-33-F) reste nécessaire pour que WTCH-02 se vérifie « au prochain
GESTE » — il est couvert par 33-05, pas ici.

Output : `dag.sh` étendu d'une fonction et d'un appel dans le bloc `mark` (après `save(dag)`),
`test-dag.sh` étendu de 7 cas (T34-T40), `mission-flow.md` amendé et daté.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-CONTEXT.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-TERRAIN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md
@.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-03-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tâche 1 : `record_progress()` câblé au point `mark`, APRÈS `save(dag)`, dégradation silencieuse garantie sauf 4ᵉ issue bruyante</name>
  <files>plugin/conductor/scripts/dag.sh, plugin/conductor/scripts/tests/test-dag.sh</files>
  <read_first>
    - plugin/conductor/scripts/dag.sh:45-66 (parseur d'arguments bash, ligne d'invocation du heredoc python — où ajouter `DRIVER_LOCK_SH`/`CHECK_GUARD_HEALTH_SH`)
    - plugin/conductor/scripts/dag.sh:110-148 (`resolve_gsd_tools_cmd()` — le PRÉCÉDENT de résolution sûre à NE PAS reproduire tel quel : celui-ci résout un OUTIL EXTERNE par PATH/env, notre cas résout un SIBLING par répertoire de script, plus simple et plus sûr)
    - plugin/conductor/scripts/dag.sh:164-196 (`compute_stages()` — patron de dégradation exact à répliquer : `try/except Exception: return None`, `capture_output=True`, `timeout=`, `check=False`, jamais un `sys.exit` non nul)
    - plugin/conductor/scripts/dag.sh:241-251 (bloc `mark` — `idx[nid]["status"] = status` puis `recompute(nodes)` puis `save(dag)` puis `emit(...)` puis `sys.exit(0)` : le nouvel appel va APRÈS `save(dag)`, avant ou après `emit`/`sys.exit` peu importe tant qu'aucune exception ne peut s'en échapper)
    - .planning/phases/VFDO-33-watchdog-notifications-des-missions/33-01-PLAN.md (forme JSON exacte de `status` et `mark-progress`)
    - plugin/conductor/scripts/tests/test-dag.sh:1-40 (convention de suite : `run_bounded` TUE À 5s MESURÉ — `( sleep 5; kill -9 "$pid" )`, PAS 10s — helpers `assert`/`assert_exit`/`assert_not`, `SCRIPT="$(pwd)/scripts/dag.sh"` — `driver-lock.sh`/`check-guard-health.sh` sont des SIBLINGS au même chemin)
    - plugin/conductor/scripts/tests/test-dag.sh (bloc T33, fin de fichier — patron de test d'isolation par copie de script, à réutiliser pour simuler un `driver-lock.sh`/`check-guard-health.sh` en panne SANS toucher aux vrais fichiers)
    - plugin/dev-orchestrator/references/mission-flow.md:35-69 (Pattern A — protocole de lock actuel : heartbeat « ENTRE les étapes », même cadence que les transitions `mark` ; c'est CE couplage de cadence que D-33-E corrige)
  </read_first>
  <behavior>
    - T34 : lock acquis (`driver-lock.sh acquire --owner=tester`) puis `dag.sh mark --status=running` sur un nœud → `driver-lock.sh status` immédiatement après montre `progress_age_seconds` proche de 0 (le mark a bien avancé le progrès).
    - T35 : AUCUN lock présent (chemin `VF_DRIVER_LOCK` jamais acquis) → `dag.sh mark` sort 0, la sortie JSON de `mark` est identique à celle d'avant ce plan (mêmes clés `id`/`status`/`ready`), aucune erreur affichée.
    - T36 : sibling `driver-lock.sh` ISOLÉ (copie de `dag.sh` + faux `driver-lock.sh` qui sort exit 1 sans JSON, patron T33) → `dag.sh mark` sort 0, le DAG est bien mis à jour (`status` reflète le nouveau statut du nœud) — ET le fichier DAG sur disque (pas seulement la sortie JSON de `mark`) porte le nouveau statut : preuve que `save(dag)` a bien eu lieu avant l'appel driver-lock défaillant.
    - T37 : sibling `driver-lock.sh` ISOLÉ qui rend du JSON illisible (`status` sort `not-json`) → `dag.sh mark` sort 0, DAG mis à jour normalement.
    - T38 : sibling `driver-lock.sh` ISOLÉ qui PEND (`sleep 30`) → `dag.sh mark` revient en MOINS DE 5s (`run_bounded` tue le process entier à 5s — la protection doit donc agir avant, via `timeout=2` interne aux DEUX appels `subprocess.run` de `record_progress`, worst case ~4s), sort 0, le fichier DAG sur disque porte le nouveau statut (preuve que `save(dag)` avait déjà eu lieu quand le sibling pendant a été appelé, donc la transition n'est jamais perdue même si `run_bounded` avait dû intervenir).
    - T39 : non-régression statique — aucun `shell=True` dans `dag.sh` ; les appels aux siblings passent une LISTE (`grep` positif sur les formes `[driver_lock_sh, "mark-progress"` et `[check_guard_health_sh, "--hook"`).
    - T40 : `driver-lock.sh` ISOLÉ répond normalement à `status` (present=true, owner=tester) mais échoue (exit 1) sur `mark-progress` → `dag.sh mark` sort 0, DAG mis à jour, ET une ligne apparaît sur stderr mentionnant l'échec de l'écriture du progrès (4ᵉ issue QUAL-01 : lock présent+détenu mais écriture refusée doit être BRUYANT, jamais un silence qui gèlerait `progress_epoch` sur une mission saine).
  </behavior>
  <!-- T41 (relais check-guard-health.sh --hook, ex-check_stall_signal) déplacé vers 33-05 avec la
       fonction elle-même — voir must_haves.truths D-33-F. Ne pas le réintroduire ici. -->
  <action>
1. En tête de `dag.sh`, avant l'invocation du heredoc python (après le bloc de validation
   `[ -n "$ACTION" ]`), calculer :
   ```
   SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd 2>/dev/null || dirname "$0")"
   DRIVER_LOCK_SH="$SCRIPT_DIR/driver-lock.sh"
   ```
   Commenter en une phrase : le sibling est résolu par le répertoire du script `dag.sh`
   LUI-MÊME, jamais par le cwd — les scripts du dossier sont posés par le même glob d'installation
   (`vibeflow-update.sh`, 33-TERRAIN.md §4), leur coexistence dans le même répertoire est une
   garantie de l'installeur, pas une supposition de ce script. **Ce calcul `SCRIPT_DIR` est réutilisé
   tel quel par 33-05 (vague 3) pour résoudre `CHECK_GUARD_HEALTH_SH` et `NOTIFY_SH`** — ne pas le
   dupliquer ni le renommer, 33-05 s'appuie sur son nom et sa position exacte dans le fichier.

2. Étendre la ligne d'invocation du heredoc pour passer `"$DRIVER_LOCK_SH"` en 9e argument
   positionnel, et la ligne
   `action, file, nid, step, stage, deps_raw, status, scope_raw = sys.argv[1:9]` pour capturer aussi
   `driver_lock_sh = sys.argv[9]`. **Seul l'index 9 est occupé par ce plan** — les index 10 et 11
   (`check_guard_health_sh`, `notify_sh`) sont réservés et posés par 33-05, pas ici.

3. Ajouter la fonction `record_progress(driver_lock_sh)` juste après `resolve_gsd_tools_cmd()` (même
   fichier, même style de docstring) :
   - retourne immédiatement si `driver_lock_sh` n'est pas un fichier (`os.path.isfile`) ;
   - appelle `subprocess.run([driver_lock_sh, "status"], capture_output=True, text=True, timeout=2,
     check=False)` ; retourne si `returncode != 0` ;
   - parse le stdout en JSON dans un bloc `try` — toute exception de parsing retombe dans le
     `except Exception: return` englobant de la fonction (pas un `try` imbriqué séparé) ;
   - retourne si `present` n'est pas vrai, ou si `owner` est vide ;
   - appelle `mp = subprocess.run([driver_lock_sh, "mark-progress", f"--owner={owner}"],
     capture_output=True, text=True, timeout=2, check=False)` ;
   - **4ᵉ issue QUAL-01, EXPLICITE** : si `mp.returncode != 0`, imprimer UNE ligne sur
     `sys.stderr` (ex. `f"[dag.sh mark] avertissement : mark-progress a échoué (owner={owner}) —
     progress_epoch n'a pas avancé, un stall pourrait être signalé à tort par 33-03"`) — c'est le
     SEUL cas où `record_progress` n'est pas silencieux : le lock est confirmé présent et détenu,
     donc ce n'est pas une dégradation d'environnement (comme un `driver-lock.sh` absent) mais un
     échec d'écriture sur un lock par ailleurs sain, qui mérite d'être vu ;
   - toute la fonction est enveloppée d'un `try/except Exception: return` unique englobant, patron
     `compute_stages()` — y compris le `print` sur stderr, qui ne doit jamais lever.

4. Dans le bloc `if action == "mark":`, APRÈS `save(dag)` (pas avant — `save()` est une réécriture
   complète non atomique, 33-TERRAIN.md ; le DAG doit être sur disque avant tout appel externe best-
   effort), ajouter l'appel `record_progress(driver_lock_sh)`, avant `emit(...)`/`sys.exit(0)`. Aucune
   exception ne doit pouvoir s'en échapper (garantie déjà par le `try/except` interne de la fonction).
   **Ce plan n'ajoute qu'UN SEUL appel** — `check_stall_signal(check_guard_health_sh)` et
   `record_milestone(notify_sh, ...)` sont posés par 33-05, APRÈS celui-ci dans le même bloc `mark`
   (33-05 lit l'état réel du fichier pour s'insérer à la bonne position, cf. 33-05-PLAN.md).

5. Mettre à jour le bloc de commentaire d'en-tête (`# dag.sh — ...`) pour documenter que `mark` écrit
   désormais `progress_epoch` sur le driver-lock courant s'il existe (best-effort, sans jamais
   bloquer le DAG, WTCH-01). Le relais du verdict `check-guard-health.sh --hook` (WTCH-02, D-33-F)
   et la notification de jalon (WTCH-03) sont documentés par 33-05 dans ce même bloc de commentaire,
   pas ici.

Étendre ensuite `plugin/conductor/scripts/tests/test-dag.sh` avec les cas T34 à T40 décrits en
`<behavior>`. Pour T36/T37/T38/T40, réutiliser le patron d'isolation du bloc T33 (fin de fichier
actuel) : copier `dag.sh` dans un répertoire temporaire ISOLÉ du `WORK_DIR`, y déposer un faux
`driver-lock.sh` exécutable (`chmod +x`) qui simule la panne voulue, puis invoquer la COPIE de
`dag.sh` (jamais le vrai fichier de production, jamais un chmod destructif dessus). Pour T38, vérifier
le fichier DAG sur disque après l'appel borné, pas seulement le code de sortie — c'est la preuve que
la transition n'a pas été perdue.

6. **Amendement de doctrine `mission-flow.md` (D-33-E partie b, daté 2026-08-17)** : dans
   `plugin/dev-orchestrator/references/mission-flow.md`, Pattern A, après le point 2 (« Heartbeat
   ENTRE les étapes »), ajouter un paragraphe explicitement daté et attribué :
   ```
   **Amendement Phase 33 (D-33-E, 2026-08-17)** — le heartbeat doit battre sur une cadence
   INDÉPENDANTE des transitions de nœud, et PLUS FRÉQUENTE qu'elles. Motif mesuré : émis au même
   tour qu'un `dag.sh mark` (qui écrit désormais aussi `progress_epoch`, plan 33-02), les deux
   horloges ne divergent JAMAIS — une mission gelée voit les deux s'arrêter ensemble (verdict
   abandon, jamais stall détecté à temps), une mission vivante les voit avancer ensemble (jamais de
   stall à signaler alors qu'elle boucle). Concrètement : émettre le heartbeat aussi À CHAQUE
   itération d'une boucle d'attente/poll du manager (attente d'un worker, relecture d'état), pas
   SEULEMENT entre deux étapes explicites — un manager qui surveille un worker unique pendant
   plusieurs minutes sans jamais rappeler `heartbeat` dans l'intervalle reproduit exactement le
   couplage de cadence que cet amendement ferme.
   ```
   Ce n'est PAS un changement de code (le verbe `heartbeat` existe déjà, `driver-lock.sh:497-509`) —
   c'est un changement de PROTOCOLE observé par les managers, à écrire noir sur blanc, jamais glissé
   silencieusement dans une doctrine existante.

8. **Garde anti-vert-à-vide, absente avant ce plan (mesuré)** : l'épilogue actuel de `test-dag.sh`
   est `[ "$FAIL" -eq 0 ] && exit 0 || exit 1` — un script qui planterait AVANT la première
   assertion (erreur de syntaxe dans un cas ajouté, `SCRIPT` introuvable) sortirait 0 par accident
   puisque `$FAIL` resterait à 0. Remplacer par le même patron que `test-check-guard-health.sh`
   (cas D12) :
   ```
   TOTAL=$((PASS+FAIL))
   if [ "$TOTAL" -eq 0 ]; then
     echo "  ❌ ÉCHEC ANTI-VERT-À-VIDE — zéro assertion exécutée, résultat non fiable"
     exit 1
   fi
   [ "$FAIL" -eq 0 ] && exit 0 || exit 1
   ```
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -qE 'def record_progress' plugin/conductor/scripts/dag.sh` sort 0.
    - `grep -qE 'def check_stall_signal' plugin/conductor/scripts/dag.sh` sort **1** — cette fonction
      N'EXISTE PAS après ce plan, elle est posée par 33-05 (D-33-F déplacé).
    - `grep -c 'record_progress(driver_lock_sh)' plugin/conductor/scripts/dag.sh` rend `2` (la
      définition `def record_progress(driver_lock_sh):` ET l'unique site d'appel dans le bloc
      `mark` — le motif capture les deux, ne pas confondre avec « un seul site d'appel » : c'est
      l'ERREUR mesurée sur la version précédente de ce plan, corrigée ici).
    - `grep -c 'shell=True' plugin/conductor/scripts/dag.sh` rend `0`.
    - `grep -c 'timeout=2,' plugin/conductor/scripts/dag.sh` rend EXACTEMENT `2` (les deux appels de
      `record_progress` — pattern ANCRÉ sur la virgule qui suit, `dag.sh:183` porte déjà
      `timeout=20,` dont `timeout=2` est un sous-motif non ancré : un `grep -c 'timeout=2'` nu serait
      satisfait par du code préexistant sans qu'aucun des deux nouveaux appels n'existe — seuil
      affaibli mesuré par le 2ᵉ plancheck externe, corrigé ici par l'ancrage).
    - `grep -c 'timeout=5' plugin/conductor/scripts/dag.sh` rend `0` — aucun des nouveaux appels
      n'utilise `timeout=5` (incompatible avec le `kill -9` à 5s mesuré de `run_bounded`).
    - L'appel passe une liste, jamais une chaîne : `grep -c '\[driver_lock_sh, "status"\]'
      plugin/conductor/scripts/dag.sh` rend `1`, `grep -c '\[driver_lock_sh, "mark-progress"'
      plugin/conductor/scripts/dag.sh` rend `1`.
    - `grep -c 'check_guard_health' plugin/conductor/scripts/dag.sh` rend `0` — ce plan ne touche
      JAMAIS `check-guard-health.sh` ni ne le nomme (préempterait le périmètre de 33-05).
    - Preuve que l'appel a lieu APRÈS `save(dag)` : dans le bloc `mark`, la ligne
      `record_progress(driver_lock_sh)` apparaît, dans le fichier, APRÈS la ligne `save(dag)` du
      même bloc (vérifier à la lecture, consigner dans le SUMMARY le numéro de ligne des deux pour
      preuve).
    - Aucun candidat cwd-relatif réintroduit (D-07) : `grep -c 'os.getcwd'
      plugin/conductor/scripts/dag.sh` rend `0`.
    - `bash plugin/conductor/scripts/tests/test-dag.sh` sort 0, affiche les blocs `=== T34` à
      `=== T40`, et sa ligne de bilan affiche `0 FAIL`.
    - Les 99 assertions historiques (T1-T33) restent vertes — RE-MESURÉ sur le disque, pas recopié
      d'un plan antérieur : `bash plugin/conductor/scripts/tests/test-dag.sh` rend AUJOURD'HUI
      `99 PASS / 0 FAIL` avant ce plan (pas 102) ; consigner le compte avant/après dans le SUMMARY.
    - Non-régression du protocole driver-lock : `bash plugin/conductor/scripts/tests/test-driver-lock.sh`
      sort 0 (33-01 n'est pas modifié par ce plan, mais l'appel croisé doit être vérifié).
    - `grep -c 'D-33-E' plugin/dev-orchestrator/references/mission-flow.md` rend au moins `1` —
      l'amendement est présent et daté.
    - `grep -qE 'ANTI-VERT-À-VIDE' plugin/conductor/scripts/tests/test-dag.sh` sort 0 — la garde
      d'épilogue existe (absente avant ce plan, mesuré).
    - **Mutation rouge prouvée, trace obligatoire dans le SUMMARY** : commiter d'abord ce plan, puis
      faire remonter une exception NON catchée hors de `record_progress()` (retirer temporairement
      le `try:`/`except Exception: return` englobant) avec un `driver-lock.sh` absent → `dag.sh
      mark` doit alors CRASHER (le mutant prouve que la garde protège réellement `mark`) ; consigner
      assertion exacte/attendu/obtenu ; restaurer par `git checkout -- plugin/conductor/scripts/dag.sh`.
    - **Contrôle positif des fixtures T36/T37/T38/T40** : avant d'invoquer la copie isolée de
      `dag.sh`, vérifier explicitement que le faux `driver-lock.sh` déposé
      est bien exécutable (`[ -x <chemin> ]`) et qu'il produit BIEN le comportement de panne
      attendu quand invoqué directement (appel isolé au faux script, hors `dag.sh`, consigné dans le
      SUMMARY) — sinon ces cas seraient verts pour la mauvaise raison (panne jamais réellement
      exercée).
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/dag.sh && bash plugin/conductor/scripts/tests/test-dag.sh && bash plugin/conductor/scripts/tests/test-driver-lock.sh && [ "$(grep -c 'shell=True' plugin/conductor/scripts/dag.sh)" -eq 0 ] && [ "$(grep -c 'timeout=5' plugin/conductor/scripts/dag.sh)" -eq 0 ]</automated>
  </verify>
  <done>Chaque `dag.sh mark` avance `progress_epoch` sur le lock courant s'il existe (après `save(dag)`, jamais avant) sans jamais dépendre de cette réussite pour que le DAG progresse, signale bruyamment le seul cas où un lock présent+détenu refuse l'écriture, et la mutation qui retire la garde de dégradation fait effectivement crasher `mark`. Le protocole de heartbeat des managers est amendé et daté pour que vivacité et progrès cessent de battre sur le même tour.</done>
  <reversibility rating="reversible">Un `git revert` rend `dag.sh` et `mission-flow.md` identiques à HEAD ; un DAG écrit entre-temps reste lisible par la version antérieure (aucun champ n'est ajouté au fichier DAG lui-même, seul le lock externe est touché).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dag.sh` -> `driver-lock.sh` (sous-processus) | le sibling est résolu par répertoire de script, pas par cwd (précédent D-07 fermé sur ce même fichier) ; la sortie de `driver-lock.sh status` est traitée comme une entrée NON fiable (JSON potentiellement absent/illisible) |
| `driver-lock.sh` (indisponible/lent) -> `dag.sh mark` | un tiers qui remplacerait/casserait le sibling ne doit jamais pouvoir bloquer ni faire échouer la mise à jour du DAG — c'est la propriété centrale de ce plan |

## STRIDE Threat Register

Numérotation `T-33-05` à `T-33-07` — `T-33-11` (DoS sur le relais `check-guard-health.sh`,
initialement affecté à ce plan) est DÉPLACÉ vers 33-05 avec `check_stall_signal()` elle-même
(D-33-F, voir must_haves) — c'est ce déplacement qui ferme la collision avec le bloc `T-33-11`..
`T-33-15` réservé par 33-04 (signalée par le 2ᵉ plancheck externe).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Denial of Service | `record_progress()` | high | mitigate | `timeout=2` sur les deux appels (sous le seuil de `kill -9` à 5s de `run_bounded`) + `try/except Exception` englobant : un `driver-lock.sh` qui pend ou plante ne bloque jamais `mark` ; cas T38 + mutation rouge |
| T-33-06 | Tampering | résolution du sibling | medium | mitigate | résolution par répertoire du script `dag.sh` lui-même, jamais par cwd — précédent D-07/T33 explicitement cité et non rouvert ; cas T39 (grep statique) |
| T-33-07 | Injection | arguments du sous-processus | low | accept | `subprocess.run` avec liste d'arguments, jamais `shell=True` ; `owner` provient de `driver-lock.sh status` déjà assaini côté 33-01 (`sanitize_field`) avant d'être réinjecté ici |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/dag.sh`
2. `bash plugin/conductor/scripts/tests/test-dag.sh` — 0 FAIL, cas T34 à T40 présents, T1-T33 historiques intacts (99 PASS mesurées avant ce plan)
3. `bash plugin/conductor/scripts/tests/test-driver-lock.sh` — non-régression du protocole consommé en écriture
4. La mutation exigée a rougi et sa trace (assertion, attendu, obtenu) est dans le SUMMARY
5. L'amendement `mission-flow.md` est présent, daté (2026-08-17) et attribué à D-33-E
6. Non-régression du parc COMPLET des suites : `find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l` rend `64`, INCHANGÉ — ce plan étend `test-dag.sh` (fichier déjà découvert), il n'en ajoute ni n'en retire aucun. Le passage exhaustif des 64 suites reste la responsabilité de CI (`ci.yml` L213) ; ce plan ne le rejoue pas intégralement en local.
</verification>

<success_criteria>
- `dag.sh mark` avance `progress_epoch` sur le driver-lock courant à chaque transition explicite de nœud, en best-effort, APRÈS `save(dag)`.
- Quatre familles de panne côté `driver-lock.sh` (absent, échoue, JSON illisible, pend) sont chacune prouvées inoffensives pour `dag.sh mark` par un cas de test dédié ; la 5ᵉ issue (lock présent+détenu mais écriture refusée) est prouvée BRUYANTE, pas silencieuse.
- Aucun candidat de résolution cwd-relatif n'est introduit.
- Le protocole de heartbeat des managers est amendé et daté (`mission-flow.md`, D-33-E) : vivacité et progrès battent désormais sur deux cadences distinctes.
- Le relais du verdict `check-guard-health.sh --hook` (D-33-F, WTCH-02) n'est PLUS de la responsabilité de ce plan — voir 33-05.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-33-watchdog-notifications-des-missions/33-02-SUMMARY.md` en fin de plan,
en y consignant la trace de mutation exigée, le comptage avant/après des assertions de la suite
(mesuré, pas recopié), et les numéros de ligne prouvant l'ordre `save(dag)` avant `record_progress`.
</output>

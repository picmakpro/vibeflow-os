---
phase: 25-budget-d-instructions-et-tage-d-alignement-court
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - plugin/conductor/scripts/tests/test-check-instruction-budget.sh
autonomous: true
requirements: [BUDG-01, BUDG-02, QUAL-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "QUAL-01 est tenu par preuve, pas par déclaration : la suite `plugin/conductor/scripts/tests/test-check-instruction-budget.sh` exerce les TROIS issues du gate — conforme (PASS), dépassement (FAIL), imparsable BRUYANT — et chacune est atteinte par une fixture qui la produit vraiment."
    - "Le cas imparsable est exercé par une FIXTURE SYNTHÉTIQUE, jamais par le dépôt réel : aucun des 31 fichiers du corpus n'a de frontmatter cassé aujourd'hui (mesuré, `25-RESEARCH.md:560-572`), donc tester contre le corpus réel n'exercerait jamais ce chemin de code."
    - "Chaque cas construit sa PROPRE fixture dans un `mktemp -d` et n'écrit jamais dans `plugin/` réel ni dans le `.planning/` du dépôt (patron `test-check-requirements-survival.sh`) — le gate y est invoqué par `--path`, et la sentinelle de fixture par écriture directe du fichier, jamais par le gate (D-04)."
    - "QUATRE mutants vérifiés par `cmp` prouvent que la suite peut rendre rouge : MUT-1 neutralise la comparaison de la métrique INSTRUCTIONS, MUT-2 celle de la métrique LIGNES, MUT-3 la lecture de la sentinelle, MUT-4 la détection du fichier imparsable. Chaque mutant est refusé s'il n'a rien changé (`cmp -s` identique) ou s'il ne passe plus `bash -n` — un mutant non opposable est un échec, pas un succès (D-05 : les DEUX métriques ont leur mutant, pas seulement une)."
    - "La suite elle-même est capable de rendre rouge : remplacer le gate par un script qui sort en 0 sans rien faire fait échouer la suite. C'est le témoin qui manquait aux dix vérificateurs inertes de la Phase 39 (`[[preuve-incapable-de-rendre-rouge]]`)."
    - "Les trois arêtes de BUDG-02 ont chacune leur cas nommé : adjacence (`courant == baseline` vert, `courant > baseline` rouge), vide (0 fichier découvert, baseline absente en mode armé, baseline sans entrée de données), ordre (tableau trié `LC_ALL=C`, deux exécutions octet pour octet identiques). S'y ajoute le cas du plafond ABSOLU ADR-029 (D-05) : un fichier à 251 lignes rougit même si sa baseline vaut 251 — la baseline ne peut pas légaliser une violation du plafond, et c'est la réponse gravée à la question ouverte par la Phase 40."
    - "Aucune assertion de contenu n'utilise `diff` : `cmp`, `comm` et `cksum` uniquement — `diff` est mesuré proxifié menteur sur ce poste (`test-check-divergence.sh:4-6`)."
    - "La suite est découverte par la boucle CI existante sans câblage supplémentaire : elle respecte le motif `*/tests/test-*.sh` de `.github/workflows/ci.yml:217`."
  artifacts:
    - "plugin/conductor/scripts/tests/test-check-instruction-budget.sh : suite bash autonome (harness `ok()`/`ko(assertion, attendu, obtenu)` à trois champs, fixtures `mktemp -d`, bloc `== mutants ==`), exécutable par `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh`, bilan final et code de sortie non nul dès un seul échec."
  key_links:
    - "Suite <-> boucle de découverte CI (`find plugin scripts -type f -path '*/tests/test-*.sh'`, `ci.yml:215-240`) : un nom de fichier hors motif rendrait la suite invisible à la CI — elle serait verte localement et jamais exécutée là où ça compte."
    - "Bloc mutants <-> `cmp -s` : sans la vérification que le mutant DIFFÈRE de l'original, un motif de substitution qui ne matche rien produirait un mutant identique et un test vert à tort — le mode de défaut exact que le patron Phase 39 existe pour fermer."
    - "Fixture imparsable <-> exit 2 : c'est le seul moyen d'exercer l'issue BRUYANT de QUAL-01, le corpus réel n'en fournissant aucun exemple."
  prohibitions:
    - "Aucun cas n'écrit dans `plugin/` réel, dans `.planning/` du dépôt, ni ne pose la sentinelle ou la baseline de production."
    - "Aucun `diff` dans une assertion de contenu."
    - "Aucun mutant accepté sans preuve `cmp` qu'il a bien muté le script ET qu'il reste du bash valide."
    - "Aucun `|| true` autour de l'invocation du gate : le code de retour est capturé explicitement et comparé."
    - "Aucune valeur attendue recopiée d'un document : les valeurs de baseline des fixtures sont dérivées par mesure du gate sur la fixture elle-même, ou choisies par construction de la fixture."
---

<objective>
Doter `check-instruction-budget.sh` de la suite qui le rend opposable : trois issues exercées sur
fixtures jetables, quatre mutants vérifiés par `cmp`, et un témoin qui prouve que la suite elle-même
peut rendre rouge.

Purpose : ce lab a mesuré son mode de défaut dominant en Phase 39 — dix vérificateurs incapables
d'échouer, tous invisibles à la relecture. Un gate livré sans mutant prouvé est un gate dont
personne ne sait s'il regarde quelque chose. QUAL-01 est l'exigence transverse qui ferme cette
porte, et elle reste à tenir précisément sur cette phase (`.planning/REQUIREMENTS.md:851`).

Output : un seul fichier, `plugin/conductor/scripts/tests/test-check-instruction-budget.sh`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-CONTEXT.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-PATTERNS.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-01-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tâche 1 : les trois issues et les trois arêtes, chacune sur sa fixture jetable</name>
  <files>plugin/conductor/scripts/tests/test-check-instruction-budget.sh</files>
  <read_first>
    - `plugin/conductor/scripts/check-instruction-budget.sh` — le contrat à exercer : cinq codes de sortie, format de tableau, vocabulaire de verdict
    - `plugin/dev-orchestrator/scripts/tests/test-check-requirements-survival.sh:1-120` — harness `ok()`/`ko()` à trois champs, constructeurs `mk_root()`/`w_armed()`, un cas par état du contrat
    - `plugin/conductor/scripts/tests/test-check-divergence.sh:1-40` — en-tête de suite, résolution de `SCRIPT` par `dirname`, avertissement sur `diff`
    - `25-PATTERNS.md:148-236` — le gabarit exact de suite et de fixtures pour CE gate
    - `25-RESEARCH.md:621-659` — la table « Phase Requirements → Test Map » : quels cas pour quelle exigence
    - `.github/workflows/ci.yml:215-240` — la boucle de découverte des suites, motif `*/tests/test-*.sh`
  </read_first>
  <action>
    Écrire la suite, un cas par état du contrat, chacun sur sa fixture.

    (1) **Harness.** En-tête documentant le rôle de la suite et la liste des cas ; `set -uo pipefail` ;
    `SCRIPT="$(cd "$(dirname "$0")/.." && pwd)/check-instruction-budget.sh"` ; une variable
    `TARGET="$SCRIPT"` qui sera basculée sur un mutant en tâche 2 ; `ok()` et `ko(assertion, attendu,
    obtenu)` à trois champs distincts (jamais un KO muet) ; `TMP="$(mktemp -d)"` avec `trap` de
    nettoyage ; bilan final et sortie non nulle dès un échec.

    (2) **Constructeurs de fixture.** `mk_root <nom>` crée `$TMP/<nom>/plugin/<module>/agents/` et
    `$TMP/<nom>/.planning/` ; `w_agent <racine> <chemin relatif> <lignes…>` écrit un fichier d'agent
    synthétique ; `w_armed <racine>` pose la sentinelle par écriture directe ; `w_baseline <racine>`
    écrit le TSV. Aucun de ces constructeurs ne touche au dépôt réel.

    (3) **Cas des trois issues (QUAL-01).**
    — PASS : fixture armée dont la baseline reprend les valeurs mesurées → rc 0, verdict `OK` sur
    chaque ligne.
    — FAIL : même fixture, baseline d'instructions abaissée d'une unité → rc 1, verdict
    `DEPASSEMENT-INSTR` sur la ligne concernée. Idem pour la métrique lignes → `DEPASSEMENT-LIGNES`.
    — BRUYANT : fixture dont un fichier ouvre son frontmatter sans le refermer → rc 2, verdict
    `NON-VERIFIABLE`, message sur stderr nommant le chemin. Deuxième fixture bruyante : fichier
    illisible (permissions retirées ; si le test tourne en root, le cas se déclare ignoré
    explicitement plutôt que vert). Contre-cas à ne PAS confondre : un fichier SANS frontmatter du
    tout se compte normalement et rend un verdict, jamais `NON-VERIFIABLE`.

    (4) **Cas des trois arêtes (BUDG-02).**
    — adjacence : `courant == baseline` sur les deux métriques → rc 0 ; `courant = baseline + 1` →
    rc 1 ; `courant = baseline - 1` → rc 0 avec verdict `MARGE`.
    — vide : fixture sans aucun fichier d'agent → rc 2 ; fixture armée sans fichier de baselines →
    rc 2 ; fixture armée avec un fichier de baselines ne contenant que des commentaires et des
    lignes vides → rc 2 ; fixture armée avec une entrée orpheline → rc 2. Aucun de ces quatre états
    ne rend 0.
    — plafond absolu ADR-029 : fixture armée dont un fichier compte 251 lignes ET dont la baseline
    de lignes vaut 251 → rc 1, verdict `DEPASSEMENT-ADR029`. Le cas nomme dans son libellé que la
    baseline ne prime pas sur le plafond ; c'est la réponse gravée à la question ouverte par la
    Phase 40 (que se passe-t-il si `AGENT.md` dépasse 250 après réécriture).
    — ordre : fixture à trois fichiers dont les noms se trient différemment selon la locale ; deux
    exécutions successives donnent un `cksum` identique, et l'ordre des lignes est celui de
    `LC_ALL=C sort` (comparaison par `comm` contre la liste triée attendue).

    (5) **Cas du ratchet (BUDG-02, D-04).** Fixture en dépassement, sentinelle absente → rc 3 et le
    tableau est imprimé INTÉGRALEMENT (nombre de lignes de données égal au nombre de fichiers) ; on
    pose la sentinelle sur la MÊME fixture, sans rien changer d'autre → rc 1. Ce basculement 3 → 1
    sur une fixture inchangée est la preuve du ratchet ; l'écrire comme un seul cas nommé.

    (6) **Cas de la métrique (BUDG-01).** Reprendre les deux fixtures de contrôle du plan 25-01
    (contrôle positif : puce sans marqueur sous titre de règles ; contrôle négatif : puce
    descriptive sous titre non-règles qui suit un titre de règles ; frontmatter porteur d'un
    marqueur ; bloc fenced porteur d'un marqueur ; titre de section porteur d'un marqueur) et
    asserter les comptes attendus, dérivés de la construction de la fixture. Ajouter le cas
    « sous-titre imbriqué » d'Open Question 1 : un `### Détails` sous un `## Iron Laws` referme le
    scope — le comportement tranché au plan 25-01, gravé ici en test pour qu'il ne dérive pas en
    silence.

    (7) **Sûreté.** Aucun cas n'invoque le gate sans `--path` vers une fixture. Aucune assertion
    n'utilise `diff`. Les codes de retour sont capturés par `rc=0 ; … || rc=$?`, jamais par `|| true`.
  </action>
  <behavior>
    - La suite sort en 0 quand le gate est conforme, et en code non nul dès qu'un seul cas échoue.
    - Chaque cas imprime une ligne `✓` ou une trace `✗` à trois champs.
    - Le basculement 3 → 1 par simple pose de la sentinelle sur une fixture inchangée est testé nommément.
    - Aucun fichier du dépôt réel n'est créé, modifié ou supprimé par l'exécution de la suite.
  </behavior>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` sort en 0.
    - Sa sortie contient au moins 12 lignes de succès (`✓`) et aucune ligne d'échec (`✗`).
    - Sa sortie contient un cas nommé pour chacune des trois issues de QUAL-01 : les chaînes `PASS`, `FAIL` et `BRUYANT` apparaissent chacune dans un libellé de cas.
    - Sa sortie contient un cas dont le libellé porte `ratchet`, un cas dont le libellé porte `adjacence`, et un cas dont le libellé porte `ADR029`.
    - Le chemin de la suite matche le motif de découverte CI `*/tests/test-*.sh`.
    - Après exécution de la suite, `git status --short` ne signale aucun fichier modifié ou créé hors des fichiers du plan.
    - Témoin : si `check-instruction-budget.sh` est temporairement remplacé par un script qui sort en 0 sans rien faire, la suite échoue.
  </acceptance_criteria>
  <verify>
    <automated>T=plugin/conductor/scripts/tests/test-check-instruction-budget.sh; S=plugin/conductor/scripts/check-instruction-budget.sh; bash -n "$T" || exit 1; case "$T" in */tests/test-*.sh) : ;; *) echo "HORS-MOTIF-CI: $T"; exit 1 ;; esac; gs0=$(git status --porcelain) || { echo "GIT-INDISPONIBLE"; exit 1; }; before=$(printf '%s\n' "$gs0" | cksum); rc=0; out="$(bash "$T" 2>&1)" || rc=$?; printf '%s\n' "$out"; test "$rc" -eq 0 || { echo "SUITE-ROUGE: rc=$rc"; exit 1; }; okn=$(printf '%s\n' "$out" | grep -c '✓'); kon=$(printf '%s\n' "$out" | grep -c '✗'); echo "succes=$okn echecs=$kon"; test "$kon" -eq 0 || { echo "CAS-EN-ECHEC: $kon"; exit 1; }; test "$okn" -ge 12 || { echo "SUITE-TROP-MAIGRE: $okn cas"; exit 1; }; for k in PASS FAIL BRUYANT ratchet adjacence ADR029; do printf '%s\n' "$out" | grep -q "$k" || { echo "CAS-MANQUANT: $k"; exit 1; }; done; gs1=$(git status --porcelain) || { echo "GIT-INDISPONIBLE"; exit 1; }; after=$(printf '%s\n' "$gs1" | cksum); test "$before" = "$after" || { echo "SUITE-SALIT-LE-DEPOT"; exit 1; }; cp "$S" "$S.temoin"; printf '%s\n' '#!/usr/bin/env bash' 'exit 0' > "$S"; wrc=0; bash "$T" >/dev/null 2>&1 || wrc=$?; mv "$S.temoin" "$S"; echo "temoin_rc=$wrc"; test "$wrc" -ne 0 || { echo "SUITE-INCAPABLE-DE-RENDRE-ROUGE"; exit 1; }; echo ISSUES-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `HORS-MOTIF-CI`, `SUITE-ROUGE: rc=<n>`, `CAS-EN-ECHEC: <n>`, `SUITE-TROP-MAIGRE: <n> cas`, `CAS-MANQUANT: <mot>`, `SUITE-SALIT-LE-DEPOT`, `SUITE-INCAPABLE-DE-RENDRE-ROUGE` apparaît ; l'absence de `ISSUES-OK` vaut échec. Causes discriminantes : la suite n'est pas du bash valide ou son nom échappe à la découverte CI ; un cas échoue ; la couverture est trop maigre pour couvrir les trois issues et les trois arêtes ; un libellé de cas manque, donc une issue de QUAL-01 ou une arête n'est pas nommément exercée ; la suite a écrit dans le dépôt au lieu de rester dans son `mktemp -d` ; et surtout le témoin : un gate remplacé par un script vide laisse la suite verte, ce qui prouverait que la suite ne regarde rien.</fails_when>
  </verify>
  <done>Les trois issues et les trois arêtes ont chacune leur cas nommé sur fixture jetable, et la suite est prouvée capable de rendre rouge.</done>
  <reversibility rating="reversible">Une suite de tests neuve, sans consommateur autre que la boucle de découverte CI.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : quatre mutants vérifiés par cmp — une métrique n'est prouvée que si sa neutralisation rend la suite verte à tort</name>
  <files>plugin/conductor/scripts/tests/test-check-instruction-budget.sh</files>
  <precondition>La tâche 1 est verte : la suite existe, couvre les trois issues et sait déjà rendre rouge sur un gate vidé.</precondition>
  <read_first>
    - `plugin/conductor/scripts/tests/test-check-divergence.sh:240-273` — le bloc mutants de référence : génération par awk, refus si `cmp -s` dit identique, refus si `bash -n` échoue, comparaison `rc_mutant` contre `rc_original`
    - `plugin/conductor/scripts/check-instruction-budget.sh` — les quatre points à neutraliser : comparaison instructions, comparaison lignes, lecture de la sentinelle, détection de l'imparsable
    - `25-PATTERNS.md:203-236` — le gabarit `cmp` adapté à ce gate, et l'interdiction de `diff`
    - `25-CONTEXT.md:103-108` — QUAL-01 : mutation rouge prouvée, pas seulement déclarée
  </read_first>
  <action>
    Ajouter un bloc `== mutants ==` en fin de suite, avec QUATRE mutants distincts. Chacun suit le
    même protocole en quatre temps, repris tel quel du patron Phase 39.

    (1) **Génération.** Un script awk par mutant, écrit dans `$TMP/mutants/`, qui substitue UNE
    occurrence d'un point précis du gate. Les points visés, nommés par ce qu'ils font et non par un
    numéro de ligne (un numéro de ligne dériverait au premier commit) :
    — MUT-1 : la comparaison de la métrique INSTRUCTIONS à sa baseline, rendue toujours fausse.
    — MUT-2 : la comparaison de la métrique LIGNES à sa baseline, rendue toujours fausse.
    — MUT-3 : la lecture de la sentinelle, forcée à « non armé ».
    — MUT-4 : la détection du fichier imparsable, rendue toujours fausse.

    (2) **Vérification que le mutant a muté.** `cmp -s "$MUTANT" "$SCRIPT"` : si identique, c'est un
    ÉCHEC nommé « mutation n'a RIEN changé — mutant NON OPPOSABLE », jamais un succès. Puis
    `bash -n "$MUTANT"` : un mutant qui ne compile plus n'est pas une preuve, c'est un échec.

    (3) **Opposition.** Basculer `TARGET` sur le mutant, rejouer la fixture discriminante du
    mutant, capturer `rc_mut` ; rebasculer `TARGET` sur l'original, rejouer la MÊME fixture,
    capturer `rc_orig`. Attendus : MUT-1 et MUT-2 sur une fixture armée en dépassement →
    `rc_orig` 1, `rc_mut` 0 ; MUT-3 sur une fixture armée en dépassement → `rc_orig` 1, `rc_mut` 3
    (le gate se croit non armé et n'ose plus bloquer) ; MUT-4 sur la fixture à frontmatter non
    refermé → `rc_orig` 2, `rc_mut` différent de 2. Toute autre combinaison est un échec, avec la
    trace `rc_mutant=… rc_original=…`.

    (4) **Étiquetage.** Chaque succès s'imprime avec un libellé commençant par `MUT-<n>` et
    mentionnant que le `cmp` a confirmé la mutation — c'est cette chaîne que la vérification de
    tâche compte. Un mutant qui ne s'imprime pas ne compte pas.

    Contrainte de fond à respecter : les DEUX métriques ont leur mutant. Une suite qui ne mute que
    la comparaison d'instructions laisserait la métrique lignes — le plafond ADR-029 rendu
    machine-enforced pour la première fois par D-05 — sans aucune preuve d'opposabilité.
  </action>
  <behavior>
    - Quatre mutants sont générés, chacun prouvé différent de l'original par `cmp` et syntaxiquement valide par `bash -n`.
    - Chaque mutant rend, sur sa fixture discriminante, un code de sortie différent de celui de l'original, dans le sens du faux vert.
    - Un mutant qui n'aurait rien changé fait échouer la suite au lieu de la laisser verte.
    - Aucun mutant n'est écrit ailleurs que dans le répertoire temporaire de la suite.
  </behavior>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` sort en 0.
    - Sa sortie contient exactement 4 lignes de succès dont le libellé commence par `MUT-`, et les quatre étiquettes `MUT-1`, `MUT-2`, `MUT-3`, `MUT-4` apparaissent chacune une fois.
    - Chaque libellé de succès de mutant mentionne la confirmation `cmp`.
    - La suite ne contient aucune invocation de `diff` (vérifiable sur le corps du fichier, commentaires exclus).
    - Aucun fichier du dépôt n'est modifié par l'exécution de la suite.
  </acceptance_criteria>
  <verify>
    <automated>T=plugin/conductor/scripts/tests/test-check-instruction-budget.sh; bash -n "$T" || exit 1; gs0=$(git status --porcelain) || { echo "GIT-INDISPONIBLE"; exit 1; }; before=$(printf '%s\n' "$gs0" | cksum); rc=0; out="$(bash "$T" 2>&1)" || rc=$?; printf '%s\n' "$out"; test "$rc" -eq 0 || { echo "SUITE-ROUGE: rc=$rc"; exit 1; }; m=$(printf '%s\n' "$out" | grep -c '✓ MUT-'); echo "mutants_verts=$m"; test "$m" -ge 4 || { echo "MUTANTS-INCOMPLETS: $m au lieu des 4 attendus"; exit 1; }; for n in 1 2 3 4; do printf '%s\n' "$out" | grep -q "✓ MUT-$n" || { echo "MUTANT-ABSENT: MUT-$n"; exit 1; }; done; printf '%s\n' "$out" | grep -q 'cmp' || { echo "CMP-NON-MENTIONNE"; exit 1; }; d=$(grep -v '^[[:space:]]*#' "$T" | grep -c '[^a-z-]diff '); test "$d" -eq 0 || { echo "DIFF-UTILISE: $d occurrence(s)"; exit 1; }; gs1=$(git status --porcelain) || { echo "GIT-INDISPONIBLE"; exit 1; }; after=$(printf '%s\n' "$gs1" | cksum); test "$before" = "$after" || { echo "SUITE-SALIT-LE-DEPOT"; exit 1; }; echo MUTANTS-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `SUITE-ROUGE: rc=<n>`, `MUTANTS-INCOMPLETS: <n> au lieu des 4 attendus`, `MUTANT-ABSENT: MUT-<n>`, `CMP-NON-MENTIONNE`, `DIFF-UTILISE: <n> occurrence(s)`, `SUITE-SALIT-LE-DEPOT` apparaît ; l'absence de `MUTANTS-OK` vaut échec. Causes discriminantes : un mutant manque (une métrique ou la sentinelle reste sans preuve d'opposabilité) ; un mutant n'a rien muté et la suite l'a signalé en échec, ce qui rend la suite rouge — comportement attendu du protocole, pas un faux positif ; la mention `cmp` absente signale un bloc mutant qui ne vérifie pas que la mutation a eu lieu ; une assertion utilise `diff`, mesuré menteur sur ce poste.</fails_when>
  </verify>
  <done>Les quatre mutants sont opposables, vérifiés par `cmp`, et couvrent les deux métriques, la sentinelle et l'imparsable.</done>
  <reversibility rating="reversible">Ajout de cas de test uniquement ; aucun contrat distribué n'est engagé.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de tests → système de fichiers | La suite écrit des fixtures ; elle ne doit écrire que dans son `mktemp -d`. |
| suite de tests → gate | La suite génère des mutants du gate ; ils ne doivent jamais remplacer le gate versionné. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Tampering | génération de mutants | high | mitigate | Les mutants sont écrits dans `$TMP/mutants/` et le gate versionné n'est jamais réécrit ; `TARGET` bascule sur un chemin, pas sur un contenu. La vérification de tâche constate que `git status` est inchangé avant et après l'exécution. |
| T-25-08 | Repudiation | mutant non opposable | high | mitigate | `cmp -s` refuse un mutant identique à l'original et `bash -n` refuse un mutant invalide : un motif de substitution qui ne matche plus rien fait échouer la suite au lieu de la laisser verte (le mode de défaut mesuré en Phase 39). |
| T-25-09 | Tampering | fixtures écrites hors `mktemp -d` | medium | mitigate | Tous les cas invoquent le gate par `--path` vers une fixture ; la vérification compare `git status --porcelain` avant et après, ce qui attrape toute écriture dans le dépôt. |
| T-25-10 | Denial of Service | fixtures non nettoyées | low | accept | `trap` de nettoyage sur `$TMP` ; en cas d'interruption, des répertoires temporaires résiduels sont sans conséquence. |
| T-25-SC | Tampering | chaîne d'approvisionnement | low | accept | Aucune installation de paquet : la suite est du bash pur (`25-RESEARCH.md:351-356`). |
</threat_model>

<verification>
- `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` sort en 0.
- La suite est découverte par la boucle CI (`find plugin scripts -type f -path '*/tests/test-*.sh'`).
- Quatre mutants verts, aucun accepté sans confirmation `cmp`.
- Un gate vidé fait échouer la suite (témoin).
</verification>

<success_criteria>
- QUAL-01 est satisfait par preuve pour ce gate : trois issues exercées, mutation rouge prouvée.
- Les deux métriques de D-05 ont chacune leur mutant.
- Le basculement du ratchet (3 → 1 par pose de la sentinelle sur une fixture inchangée) est testé.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 25-02)

| Artefact | Nature | Détail |
|---|---|---|
| `plugin/conductor/scripts/tests/test-check-instruction-budget.sh` | suite neuve | Découverte par la boucle CI via le motif `*/tests/test-*.sh`. Porte le compteur de suites de 77 à 78 (valeur à re-dériver par exécution au plan 25-03, jamais recopiée). |
| `MUT-1`, `MUT-2`, `MUT-3`, `MUT-4` | étiquettes de mutants | Comparaison instructions · comparaison lignes · lecture de la sentinelle · détection de l'imparsable. |
| Fixtures `mktemp -d` | artefacts éphémères | Corpus synthétiques, sentinelle de fixture, baseline TSV de fixture, fichier à frontmatter non refermé — aucun n'est versionné. |

*(Aucun symbole d'API externe : `api-coverage` reste `detected: false` sur ce corps de plan.)*
</artifacts>

<output>
Créer `.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-02-SUMMARY.md`
en fin de plan. Commits en français ; citer QUAL-01 et D-04/D-05 dans le corps du message.
</output>

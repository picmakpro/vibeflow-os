---
phase: 41-posture-de-protection-du-d-p-t
plan: 08
lot: L8 — trace d'un contournement explicite sur la PR inoffensive #101, seule PR autorisée à atterrir ; M-3 réduit à ce que `--admin` contourne, lu dans la rule suite (D-02ter)
type: execute
wave: 8
depends_on: ["41-07"]
autonomous: false
requirements: [PROT-01, PROT-03, PROT-04]
files_modified:
  - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Précondition dure : dernier `M1-VERDICT: SUR`, ou `M1-DECISION: option=accepter-et-documenter` ; PR-R, PR-BASELINE et PR-SENTINELLE fermées sans merge ; hors PR en vol de la ligne d'inventaire la plus récente, PR-P (#101) seule ouverte, tête P1 ; `main` relevée (`MAIN-AVANT-PR-P-II:`)"
    - "Support unique (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) : PR-P (#101), un seul fichier `.github/rulesets/README.md`, seule PR autorisée à atterrir ; la PR rouge n'est jamais contournée ni mergée ; #96, #98, #99 et #100 ne sont jamais touchées (lecture seule de #100 pour le constat)"
    - "Dernière ligne du README réécrite avant publication : P2r = P1 + une seule ligne, qui dit l'entrée par contournement explicite et renvoie à la trace telle que lue (`TRACE-II:`) ; elle ne se dit plus « preuve du critère 2 », critère non tenu tel que formulé (D-02ter). Le P2 construit en 41-06 n'est jamais publié"
    - "L'écriture sur `main` est gardée par un checkpoint humain bloquant IMMÉDIATEMENT avant le geste, qui présente ce qui atterrit (fichier, contenu, méthode) et le constat de lecture `CONSTAT-ETAT-LECTEUR:` ; la réponse « SUSPENDU » n'écrit rien chez GitHub"
    - "Geste explicite unique : après publication de P2r, pendant que les checks requis de P2r sont EN COURS, Willy (`picmakpro`) merge par `gh pr merge --merge --admin` — jamais de merge sans `--admin`, jamais d'appel REST de merge, aucune case d'interface"
    - "Pourquoi des checks en cours : PR-P est ouverte par `picmakpro`, seul propriétaire CODEOWNERS, et 41-06 l'a lue `CLEAN` ; sans règle insatisfaite au moment du merge, aucune entrée `bypass` n'existerait. Les checks requis en cours de P2r en garantissent une. Nuance consignée (`TRACE-II-NUANCE:`), reprise au SUMMARY et dans ADR-072 § Mesures (41-10) : contournement réel sur des checks en cours, pas sur une PR rouge"
    - "Trace (D-02ter, point c) : rule suite de `refs/heads/main` dont `after_sha` est le commit de merge, `result=bypass`, acteur `picmakpro`, relue par l'agent via l'API (`ACCES-RULE-SUITES: picmakpro=ok` donc `relue=oui`) ; chaque règle `prouvee` si elle figure en échec dans les évaluations de cette suite, `non_prouvee` sinon — jamais supposée"
    - "Portée, dite telle quelle : la trace prouve qu'un contournement explicite est ENREGISTRÉ par GitHub ; elle ne prouve ni un refus par défaut (aucun merge sans contournement n'est tenté, D-02ter), ni que GitHub EXIGE ce geste d'un compte `always`. `PROVENANCE-SERVEUR: rest=non_tente statut=hors_d_atteinte` ; M-3 se réduit à ce que `--admin` contourne, lu règle par règle dans la suite"
    - "Verdict : CONFORME (merge par `--admin`, suite `bypass` sur le commit de merge, acteur `picmakpro`, au moins une règle `prouvee`, checks en cours au moment du geste) ; PARTIEL (mergé, mais suite absente, autre résultat, autre acteur, aucune règle prouvée ou aucun check en cours) ; ECART (aucun merge : `--admin` refusé ou « SUSPENDU »). PARTIEL et ECART arrêtent le plan sur la Task 4, qui dit quoi faire de PR-P et des rulesets et rend réel le repli « trace non prouvée » de D-M7 sans bloquer indéfiniment le préalable « PR-P non ouverte » de 41-09"
    - "Tout commit de plan et tout corps de PR citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », citant D-02bis nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 », citant D-02ter nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 »"
    - "Rejeu local `tests` (T-41-94, mécanisme de 41-07) : seuls sont tolérés les échecs dont le chemin est exactement celui d'une entrée `open` du ledger `.planning/WINDOWS.md` (rouges d'environnement de ce poste, #6 et #7 au 2026-09-24), lus dans le ledger et jamais écrits en dur dans le plan ; tout autre échec rougit, comme toute étape du job autre que celle des suites en échec ; la liste tolérée est consignée par id de ledger (`REJEU-TESTS-41-08:`) et recomparée au rejeu"
  artifacts:
    - path: ".planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`MAIN-AVANT-PR-P-II:`, `PR-P-P2R:`, `CONSTAT-ETAT-LECTEUR:`, `M3:`, `M3-ADMIN-MSG:`, `PROVENANCE-SERVEUR:`, `TRACE-II:`, `TRACE-II-MSG:`, `TRACE-II-NUANCE:`, `MAIN-APRES-PR-P:`, `M3-VERDICT:`, `REJEU-TESTS-41-08:`"
  key_links:
    - "`TRACE-II:` et `PROVENANCE-SERVEUR:` alimentent PROT-01, PROT-04 et ADR-072 § Mesures (41-10) ; `M3-VERDICT:` / `M3-DECISION:` et PR-P non ouverte conditionnent 41-09 ; `MAIN-APRES-PR-P:` est relu par 41-12 (invariants du volet admin)"
---

<objective>
Faire atterrir la seule PR inoffensive autorisée (#101) par un contournement explicite, et lire sa
trace dans les rule suites de GitHub — sans tenter aucun merge sans contournement.

Révisé le 2026-09-24 (D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) :
la mesure de provenance serveur par un merge REST sans contournement est retirée (un compte `always`
ne peut pas y rencontrer de refus serveur, et un 200 ferait atterrir #101 sans geste explicite) ; le
plan ne prouve plus que la trace d'un contournement explicite (point c de D-02ter). La dernière
ligne du README est réécrite pour ne rien affirmer du critère 2. Constat de lecture du 2026-09-24
(planificateur, lecture seule) : la PR #100 de Samuel, qui touche `.github/workflows/ci.yml`, CI verte
et à jour, est lue `mergeable_state=blocked` par `picmakpro`, avec une revue demandée à
`@picmakpro` — un compte de la liste voit donc un état bloqué quand une règle n'est pas satisfaite
pour la PR ; la prémisse « état qu'aucun compte ne voit » de D-02ter ne tient pas telle quelle. Ce
plan n'en tire aucun geste supplémentaire : il remesure le constat et le présente à Willy au
checkpoint qui précède l'atterrissage, geste irréversible.

Purpose: point (c) de D-02ter — trace d'un contournement explicite, règle par règle ; M-3 réduit ;
support du temps (ii) du critère 2 et de la revue code owner (D-M7).
Output: PR-P mergée par `--admin` (ou décision humaine) ; trace consignée règle par règle ; portée
écrite sans l'élargir.

Budget : quatre tâches, dont la quatrième est un checkpoint de décision CONDITIONNEL, sans exécution
quand `M3-VERDICT: CONFORME`. La scinder ferait porter le geste et la décision par deux exécuteurs
sans contexte commun. Fichiers : un seul.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-07-SUMMARY.md

Sources lues le 2026-09-17 : `docs.github.com/en/rest/repos/rule-suites` —
`GET /repos/{owner}/{repo}/rulesets/rule-suites` (`ref`, `time_period`, `actor_name`,
`rule_suite_result` pass/fail/bypass/all), `GET …/rule-suites/{rule_suite_id}` (`actor_name`,
`after_sha`, `result`, `rule_evaluations[]` avec `rule_type`, `result`) ; couverture des merges de PR
non précisée — d'où la lecture ciblée sur `after_sha`. `cli.github.com/manual/gh_pr_merge`
(`--admin` « Use administrator privileges to merge a pull request that does not meet
requirements »). Mesures de lecture du 2026-09-24 : `main` = `a9620659` ; #101 (`picmakpro`)
`mergeable_state=clean`, aucune revue demandée ; #100 (`samuel-neveugall`)
`mergeable_state=blocked`, revue demandée à `picmakpro`, `behind_by=0`, quatre contextes requis
verts sur les deux événements.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : préalables relus, P2r construit par plomberie, constat de lecture remesuré, ce qui atterrit imprimé</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`M1-VERDICT:`, `M1-DECISION:`, `PR-R-FERMEE:`, `PR-CO-FERMEES:`, `PR-P:`, `PR-P-COMMITS:`, `ACCES-RULE-SUITES:`, `PR-EN-VOL-41-06:`)
  </read_first>
  <action>
Identité `picmakpro` (`gh api user --jq .login`) ; `git fetch origin`. Relire : dernier
`M1-VERDICT: SUR` ou `M1-DECISION: option=accepter-et-documenter` ; PR-R `CLOSED` non mergée ;
PR-BASELINE et PR-SENTINELLE fermées ; PR-P `OPEN`, tête = P1 ; hors PR en vol de la ligne
d'inventaire la plus récente parmi `PR-EN-VOL-APRES:` et `PR-EN-VOL-41-0N:`, seule PR ouverte =
PR-P. Ajouter à `41-PREUVES.md` une section `## 41-08 — trace d'un contournement explicite
(D-02ter)` et consigner `MAIN-AVANT-PR-P-II: <sha d'origin/main>`.

Construire P2r par plomberie (index temporaire via `GIT_INDEX_FILE`, `read-tree P1`,
`hash-object -w`, `update-index --cacheinfo 100644,<blob>,.github/rulesets/README.md`, `write-tree`,
`commit-tree -p P1`), sans changer de branche ni toucher l'arbre de travail : contenu = le README de
P1 suivi d'UNE ligne finale, exactement
« Ce fichier est entré sur `main` par un contournement explicite (`gh pr merge --admin`, Phase 41,
plan 41-08 ; D-02ter, arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ; sa trace
dans les rule suites de GitHub est consignée, telle que lue, dans `41-PREUVES.md` (`TRACE-II:`). » ;
message « docs(41): trace du contournement explicite (PR-P, D-02ter — arbitrage Willy,
AskUserQuestion session principale, 2026-09-24) ». Consigner `PR-P-P2R: p2r=<sha> parent=<P1>`.

Constat de lecture, sans aucun geste sur #100 : `gh api repos/picmakpro/vibeflow-os/pulls/100`
(`state`, `user.login`, `mergeable_state` relu jusqu'à une valeur différente de `unknown`,
`requested_reviewers[].login`), check runs de sa tête filtrés sur l'app 15368 et les quatre contextes
requis, `compare/main...<tête>` pour `behind_by`. Consigner
`CONSTAT-ETAT-LECTEUR: pr=#100 etat=<state> auteur=<login> mergeable_state=<valeur> revue_demandee=<logins séparés par des virgules, ou aucune> checks_requis_verts=<k>/4 behind_by=<b> lu_par=picmakpro date=<AAAA-MM-JJ> statut=observation`.

Imprimer « ce qui atterrit » (`git diff --stat origin/main <P2r>` et le contenu intégral de
`.github/rulesets/README.md` à P2r), le constat, puis les commandes de la Task 2. Commit local sur
B, message « docs(41-08): support du contournement explicite préparé, ce qui atterrit imprimé
(D-02ter — arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); v=$(awk "\$1==\"M1-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); m1d=$(val M1-DECISION: option); R=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); rs=$(gh pr view "$R" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); ps=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); ph=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); P1=$(val PR-P-COMMITS: p1); P2R=$(val PR-P-P2R: p2r); pp=$(git rev-parse "$P2R^"); al=$(awk "\$1 ~ /^PR-EN-VOL-(APRES|41-[0-9][0-9]):\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"numeros=\")==1) al=substr(\$i, 9) } END{print al}" "$PH/41-PREUVES.md"); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq ".[].number" | awk -v al=",$al," "{ if (index(al, \",#\" \$1 \",\") == 0) c++ } END{print c+0}"); ma=$(awk "\$1==\"MAIN-AVANT-PR-P-II:\" {print \$2; exit}" "$PH/41-PREUVES.md"); o=$(git rev-parse origin/main); echo "prealables : identite=$u m1=$v m1_decision=$m1d pr_r=$rs pr_p=$ps tete_p1=$([ "$ph" = "$P1" ] && echo oui || echo non) parent_p2r_p1=$([ "$pp" = "$P1" ] && echo oui || echo non) prs_ouvertes_hors_vol=$op main_consigne=$([ "$ma" = "$o" ] && echo oui || echo non)"; [ "$u" = picmakpro ] && { [ "$v" = SUR ] || [ "$m1d" = accepter-et-documenter ]; } && [ "$rs" = CLOSED:false ] && [ "$ps" = OPEN ] && [ "$ph" = "$P1" ] && [ "$pp" = "$P1" ] && [ "$op" -eq 1 ] && [ "$ma" = "$o" ]'</automated>
    <fails_when>exit non nul ; ou identité différente de `picmakpro` ; ou ni `M1-VERDICT: SUR` ni `M1-DECISION: option=accepter-et-documenter` ; ou PR-R non fermée ou mergée ; ou PR-P non ouverte ou tête différente de P1 ; ou P2r non fils de P1 ; ou, hors PR en vol, une autre PR ouverte que PR-P (PR jetables de 41-06 comprises) ; ou `MAIN-AVANT-PR-P-II:` absente ou périmée</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; P1=$(val PR-P-COMMITS: p1); P2R=$(val PR-P-P2R: p2r); cp=$(val PR-P-P2R: parent); T=$(mktemp -d); pa=$(git rev-parse "$P2R^"); git diff --name-only "$P1" "$P2R" > "$T/f"; nf=$(awk "END{print NR}" "$T/f"); f1=$(awk "NR==1" "$T/f"); git diff "$P1" "$P2R" > "$T/d"; ad=$(awk "/^\+[^+]/ {c++} END{print c+0}" "$T/d"); rt=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/d"); git show "$P2R:.github/rulesets/README.md" > "$T/r"; awk "NF {l=\$0} END{print l}" "$T/r" > "$T/der"; ex=$(awk "index(\$0, \"contournement explicite\") && index(\$0, \"TRACE-II\") && index(\$0, \"41-PREUVES.md\") && index(\$0, \"D-02ter\") && index(\$0, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") {c++} END{print c+0}" "$T/der"); c2=$(awk "index(\$0, \"critère 2\") || index(\$0, \"Critère 2\") {c++} END{print c+0}" "$T/r"); mp=$(awk "/\\/(Users|home)\\/[A-Za-z0-9._-]+/ {c++} END{print c+0}" "$T/r"); git diff --name-only origin/main "$P2R" > "$T/at"; at=$(awk "BEGIN{ORS=\",\"} {print}" "$T/at"); co=$(awk "\$1==\"CONSTAT-ETAT-LECTEUR:\" && index(\$0, \"pr=#100\") && index(\$0, \"mergeable_state=\") && index(\$0, \"revue_demandee=\") && index(\$0, \"lu_par=picmakpro\") && index(\$0, \"statut=observation\") {c++} END{print c+0}" "$PH/41-PREUVES.md"); br=$(git branch --show-current); echo "P2r : parent_est_p1=$([ "$pa" = "$P1" ] && echo oui || echo non) parent_consigne=$([ "$cp" = "$P1" ] && echo oui || echo non) fichiers=$nf ($f1) lignes_ajoutees=$ad lignes_retirees=$rt derniere_ligne_conforme=$ex mention_critere_2=$c2 chemins_machine=$mp atterrit=$at constat_lecteur=$co branche_arbre=$br"; [ "$pa" = "$P1" ] && [ "$cp" = "$P1" ] && [ "$nf" -eq 1 ] && [ "$f1" = .github/rulesets/README.md ] && [ "$ad" -eq 1 ] && [ "$rt" -eq 0 ] && [ "$ex" -eq 1 ] && [ "$c2" -eq 0 ] && [ "$mp" -eq 0 ] && [ "$at" = ".github/rulesets/README.md," ] && [ "$co" -eq 1 ] && [ "$br" = feat/phase-41-preuves-release ]'</automated>
    <fails_when>exit non nul ; ou P2r hors de P1 ou parent consigné faux ; ou P2r touche autre chose que `.github/rulesets/README.md`, ajoute plus d'une ligne ou en retire une ; ou dernière ligne sans « contournement explicite », renvoi `TRACE-II` et `41-PREUVES.md`, ou sans D-02ter cité avec son canal et sa date ; ou le README mentionne encore le critère 2 ; ou chemin de machine dans le README ; ou ce qui atterrirait sur `main` dépasse le seul README ; ou `CONSTAT-ETAT-LECTEUR:` absente, dupliquée ou incomplète ; ou l'arbre de travail a changé de branche</fails_when>
  </verify>
  <acceptance_criteria>
    - M-1 SUR (ou accepté par décision), PR-R et PR jetables fermées non mergées, PR-P seule ouverte sur P1 ; `main` relevée
    - P2r = P1 + une ligne qui ne dit rien du critère 2 ; seul le README atterrirait ; aucun chemin de machine
    - constat de lecture de #100 consigné comme observation, sans aucun geste sur #100 ; ce qui atterrit imprimé
  </acceptance_criteria>
  <done>Le contournement explicite peut être joué sur le seul support inoffensif, et Willy dispose de ce qui atterrit et du constat avant de décider.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : ce qui atterrit présenté à Willy ; « SUSPENDU », ou publier P2r puis merger par `--admin` pendant que les checks tournent</name>
  <read_first>
    - « ce qui atterrit », le constat et les commandes imprimés par la Task 1
  </read_first>
  <action>Willy, compte `picmakpro`, lit ce qui atterrit et le constat, puis soit répond « SUSPENDU » (aucun geste), soit publie P2r et merge PR-P par `gh pr merge --merge --admin` sans attendre la CI. Aucun autre moyen de merge ; aucun agent ne merge.</action>
  <instructions>
Compte qui agit : `picmakpro` (témoin `gh api user --jq .login`). SHA, contenu et `<n>` (= PR-P)
imprimés par la Task 1.

AVANT TOUT GESTE — ce qui atterrit sur `main` si tu merges (#101, seule PR autorisée à atterrir,
D-02ter) : un seul fichier, `.github/rulesets/README.md` (nouveau), contenu imprimé par la Task 1,
dernière ligne sur l'entrée par contournement explicite ; méthode : merge commit (`--merge`) avec
contournement explicite (`--admin`), pendant que les checks requis de P2r tournent encore. Geste
irréversible : une fois mergée, #101 ne peut plus servir à aucune autre mesure.

CONSTAT À LIRE (`CONSTAT-ETAT-LECTEUR:`, lecture seule) : la PR #100 de Samuel, qui touche
`.github/workflows/ci.yml`, CI verte et à jour, est lue `blocked` par ton compte, avec une revue
demandée à `@picmakpro`. Un compte de la liste voit donc un état bloqué quand une règle n'est pas
satisfaite pour la PR ; la prémisse « état qu'aucun compte ne voit » de D-02ter ne tient pas telle
quelle. Ce plan n'en tire aucun geste supplémentaire. Si tu veux réarbitrer D-02ter avant que #101
n'atterrisse, réponds « SUSPENDU » (avec ta raison) : ne rien pousser, ne rien merger.

Sinon, enchaîner 1 à 3 sans attendre la CI (elle dure plusieurs minutes) :
1. `git push origin <p2r>:refs/heads/preuve/41-contournement`
2. Relevé : `gh pr view <n> --repo picmakpro/vibeflow-os --json headRefOid,mergeStateStatus,reviewDecision,statusCheckRollup --jq '{tete: .headRefOid, etat: .mergeStateStatus, revue: .reviewDecision, checks_en_cours: ([.statusCheckRollup[] | select((.status // "COMPLETED") != "COMPLETED")] | length)}'`
   → la tête doit être `<p2r>` et `checks_en_cours` supérieur à 0 ; sinon relancer 2 quelques
   secondes plus tard. Si les checks sont déjà tous terminés : relancer les runs du commit
   (`gh run list --repo picmakpro/vibeflow-os --commit <p2r> --json databaseId --jq '.[].databaseId'`,
   puis `gh run rerun <id> --repo picmakpro/vibeflow-os` pour chacun), noter « relance : oui », et
   relire 2.
3. Aussitôt, contournement explicite : `gh pr merge <n> --repo picmakpro/vibeflow-os --merge --admin`
   → copier la sortie INTÉGRALE. Jamais sans `--admin` ; aucun appel `PUT …/merge` ; aucune case
   d'interface. Si refusé : copier le message et s'arrêter là.
4. Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
5. Seulement si la PR est `MERGED` : `git push origin --delete preuve/41-contournement`. Sinon PR-P
   reste ouverte : la Task 4 décide de son sort.
6. Seulement si `ACCES-RULE-SUITES: picmakpro=refuse` (sinon l'exécutant relit lui-même) et si la PR
   est `MERGED` :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?ref=refs/heads/main&time_period=day&rule_suite_result=bypass" --jq '.[] | {id, actor_name, after_sha, result}'`
   puis, pour l'id dont `after_sha` est le commit de merge :
   `gh api repos/picmakpro/vibeflow-os/rulesets/rule-suites/<id> --jq '{id, actor_name, after_sha, result, evaluations: [.rule_evaluations[] | {rule_type, result, enforcement}]}'`
  </instructions>
  <acceptance_criteria>
    - ce qui atterrit et le constat présentés avant tout geste ; soit « SUSPENDU » avec sa raison et aucun geste, soit P2r publié, relevé collé (tête, état, checks en cours, relance éventuelle), `--admin` joué une seule fois avec sa sortie intégrale, état final de PR-P collé ; aucun autre moyen de merge essayé
  </acceptance_criteria>
  <verification>La Task 3 relit l'état et le contenu du merge, la CI du push de merge sur `main`, et relit la rule suite si `ACCES-RULE-SUITES: picmakpro=ok`.</verification>
  <resume-signal>Répondre « SUSPENDU » avec la raison, ou coller les sorties intégrales de 1 à 5 (relance comprise) et, s'il y a lieu, de 6, puis écrire « contournement joué ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : consignation du geste, de la trace relue règle par règle et de sa portée, verdict</name>
  <files>.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`PR-P-P2R:`, `MAIN-AVANT-PR-P-II:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `picmakpro`. Lectures seules, aucun nouveau geste chez GitHub, quel que soit le résultat.
Consigner, sous la section 41-08 :
- `M3: tete=<sha relevé à 2, ou P1 si SUSPENDU> etat_avant=<mergeStateStatus collé, ou sans_objet> revue_avant=<reviewDecision collé, ou sans_objet> checks_en_cours_avant=<k, ou 0 si SUSPENDU> relance=<oui|non> geste=gh_pr_merge_--merge_--admin admin=<merge|refuse|non_tente> resultat=<merged|refuse|suspendu> merge=<sha|aucun> compte=picmakpro` ;
- `M3-ADMIN-MSG:` suivi de la sortie intégrale de `--admin` sur une ligne, ou de `SUSPENDU — <raison collée>` ;
- `PROVENANCE-SERVEUR: support=aucun rest=non_tente http=sans_objet statut=hors_d_atteinte raison=aucun_merge_sans_contournement_tente_D-02ter` ;
- trace, relue par l'agent si `ACCES-RULE-SUITES: picmakpro=ok` :
  `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?ref=refs/heads/main&time_period=day&rule_suite_result=bypass"`,
  choix de l'id dont `after_sha` est le commit de merge, puis `…/rule-suites/<id>` ; consigner
  `TRACE-II: suite=<id|absente> result=<…|absente> after_sha=<sha|aucun> acteur=<actor_name|aucun> lu_par=<compte> relue=<oui|non_relue> pull_request=<prouvee|non_prouvee> required_status_checks=<prouvee|non_prouvee>`
  et `TRACE-II-MSG:` (JSON relu ou collé, une ligne) — une règle est `prouvee` si elle figure en
  `fail` dans les évaluations d'une suite `bypass` dont `after_sha` est le commit de merge ;
  `relue=oui` seulement si l'agent a lui-même relu la suite ;
- `TRACE-II-NUANCE:` — sur un merge : `contournement_reel_sur_checks_en_cours_pas_sur_une_PR_rouge checks_en_cours=<k> exigence_du_geste=non_mesuree refus_par_defaut=non_tente_D-02ter`
  (sans le premier jeton si `k` vaut 0 : `contournement_reel_pas_sur_une_PR_rouge checks_en_cours=0 …`) ;
  sans merge : `aucun_contournement pas_sur_une_PR_rouge checks_en_cours=sans_objet exigence_du_geste=non_mesuree refus_par_defaut=non_tente_D-02ter` ;
- `MAIN-APRES-PR-P: merge=<sha|aucun> fichiers=<liste|aucun> ci_main=<k>/4|sans_objet` — attendre la
  fin des runs du push de merge avant d'écrire `ci_main` (sondage, jamais un vert supposé) ;
- `M3-VERDICT: <CONFORME|PARTIEL|ECART>` selon la règle du frontmatter ;
- `REJEU-TESTS-41-08: rc=<rc de l'outil> suites=<N> echecs=<M> echecs_preexistants_ledger=<WINDOWS#id,… par id croissant, ou aucun> echecs_hors_ledger=0`
  — job `tests` rejoué sur l'arbre de travail AVANT le commit, par le bloc `tests` de cette tâche
  (première passe : il ne rougit alors que sur la consignation absente, et imprime les valeurs à
  recopier). Seul est toléré un échec dont le chemin est exactement celui d'une entrée `open` de
  `.planning/WINDOWS.md` (rouges d'environnement de ce poste, #6 et #7 au 2026-09-24), la liste
  étant lue dans le ledger, jamais écrite en dur (T-41-94, repris de 41-07) ; tout autre échec de
  suite, ou toute autre étape du job en échec, arrête le plan et n'est jamais consigné comme toléré.
Commit local sur B, message « docs(41-08): contournement explicite et sa trace consignés règle par
règle (D-02, D-09 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ; D-02ter —
arbitrage Willy, AskUserQuestion session principale, 2026-09-24) ». Rejouer `gates` et `tests` : le
bloc `tests` recompare la liste tolérée à `REJEU-TESTS-41-08:`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); case "$N" in ""|*[!0-9]*) echo "PR-P absente"; exit 1;; esac; P2R=$(val PR-P-P2R: p2r); git fetch -q origin; st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); res=$(val M3: resultat); mc=$(val M3: merge); adm=$(val M3: admin); ge=$(val M3: geste); cv=$(val M3: checks_en_cours_avant); pv=$(val PROVENANCE-SERVEUR: rest); ps=$(val PROVENANCE-SERVEUR: statut); ms=$(awk "\$1==\"M3-ADMIN-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); ap=$(val MAIN-APRES-PR-P: merge); T=$(mktemp -d); git ls-remote --heads origin preuve/41-contournement > "$T/lr"; bd=$(awk "END{print NR}" "$T/lr"); if [ "$st" = MERGED ]; then M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); git merge-base --is-ancestor "$M" origin/main; git diff --name-only "$M^1" "$M" > "$T/fl"; fl=$(awk "BEGIN{ORS=\"\"} NR>1{print \",\"} {print}" "$T/fl"); p2in=$(git merge-base --is-ancestor "$P2R" "$M" && echo oui || echo non); ci=$(gh api "repos/picmakpro/vibeflow-os/commits/$M/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | map(.name) | unique | length"); echo "etat merge : PR-P=$st merge=$M fichiers=$fl p2r_inclus=$p2in ci_main_jobs_verts=$ci branche_distante=$bd | M3 resultat=$res admin=$adm geste=$ge merge=$mc checks_en_cours_avant=$cv main_apres=$ap provenance_rest=$pv/$ps message=$ms"; [ "$res" = merged ] && [ "$adm" = merge ] && [ "$ge" = gh_pr_merge_--merge_--admin ] && [ "$mc" = "$M" ] && [ "$ap" = "$M" ] && [ "$fl" = .github/rulesets/README.md ] && [ "$p2in" = oui ] && [ "$ci" -eq 4 ] && [ "$bd" -eq 0 ] && [ "$pv" = non_tente ] && [ "$ps" = hors_d_atteinte ] && [ "$ms" -eq 1 ] && [ -n "$cv" ]; else echo "etat merge : PR-P=$st | M3 resultat=$res admin=$adm merge=$mc main_apres=$ap provenance_rest=$pv/$ps message=$ms"; [ "$st" = OPEN ] && [ "$mc" = aucun ] && [ "$ap" = aucun ] && [ "$pv" = non_tente ] && [ "$ps" = hors_d_atteinte ] && [ "$ms" -eq 1 ] && { { [ "$res" = refuse ] && [ "$adm" = refuse ]; } || { [ "$res" = suspendu ] && [ "$adm" = non_tente ]; }; }; fi'</automated>
    <fails_when>exit non nul ; chemin MERGED : merge consigné différent du merge relu, `MAIN-APRES-PR-P:` différente, geste autre que `--admin`, fichiers atterris autres que le seul `.github/rulesets/README.md`, P2r non inclus, CI du push de merge non verte (attendre la fin avant d'écrire), branche distante encore présente, `PROVENANCE-SERVEUR:` autre que `rest=non_tente statut=hors_d_atteinte`, ou `M3-ADMIN-MSG:` absente ; chemin OPEN : consignation autre que `merge=aucun` et `MAIN-APRES-PR-P: merge=aucun`, ou couple `resultat`/`admin` incohérent (`refuse`/`refuse` ou `suspendu`/`non_tente`). Ces rouges sont des erreurs de CONSIGNATION, corrigées ici — jamais par un nouveau geste chez GitHub</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; res=$(val M3: resultat); M=$(val M3: merge); cv=$(val M3: checks_en_cours_avant); r=$(val TRACE-II: result); a=$(val TRACE-II: after_sha); id=$(val TRACE-II: suite); ac=$(val TRACE-II: acteur); pr=$(val TRACE-II: pull_request); rc=$(val TRACE-II: required_status_checks); lu=$(val TRACE-II: lu_par); rl=$(val TRACE-II: relue); acc=$(val ACCES-RULE-SUITES: picmakpro); msg=$(awk "\$1==\"TRACE-II-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); ok=oui; case "$pr" in (prouvee|non_prouvee) ;; (*) ok=non;; esac; case "$rc" in (prouvee|non_prouvee) ;; (*) ok=non;; esac; if [ "$res" = merged ]; then [ "$a" = "$M" ] || [ "$id" = absente ] || ok=non; [ -n "$lu" ] || ok=non; [ "$msg" -eq 1 ] || ok=non; if [ "$acc" = ok ] && [ "$id" != absente ]; then [ "$rl" = oui ] || ok=non; api=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$id" --jq "[.result, .after_sha, .actor_name, ((.rule_evaluations | map(select(.result == \"fail\") | .rule_type) | any(. == \"pull_request\")) | tostring), ((.rule_evaluations | map(select(.result == \"fail\") | .rule_type) | any(. == \"required_status_checks\")) | tostring)] | join(\" \")"); set -- $api; [ "$1" = "$r" ] && [ "$2" = "$a" ] && [ "$3" = "$ac" ] && { { [ "$4" = true ] && [ "$pr" = prouvee ]; } || { [ "$4" = false ] && [ "$pr" = non_prouvee ]; }; } && { { [ "$5" = true ] && [ "$rc" = prouvee ]; } || { [ "$5" = false ] && [ "$rc" = non_prouvee ]; }; } || ok=non; else [ "$rl" = non_relue ] || ok=non; fi; [ "$id" != absente ] || { [ "$pr" = non_prouvee ] && [ "$rc" = non_prouvee ]; } || ok=non; nu=$(awk -v k="$cv" "\$1==\"TRACE-II-NUANCE:\" && index(\$0, \"pas_sur_une_PR_rouge\") && index(\$0, \"checks_en_cours=\" k) && index(\$0, \"exigence_du_geste=non_mesuree\") && index(\$0, \"refus_par_defaut=non_tente\") && (k+0 == 0 || index(\$0, \"contournement_reel_sur_checks_en_cours\")) {c++} END{print c+0}" "$PH/41-PREUVES.md"); else [ "$id" = absente ] && [ "$pr" = non_prouvee ] && [ "$rc" = non_prouvee ] && [ "$rl" = non_relue ] || ok=non; nu=$(awk "\$1==\"TRACE-II-NUANCE:\" && index(\$0, \"aucun_contournement\") && index(\$0, \"pas_sur_une_PR_rouge\") && index(\$0, \"refus_par_defaut=non_tente\") {c++} END{print c+0}" "$PH/41-PREUVES.md"); fi; echo "trace : resultat_merge=$res suite=$id result=$r acteur=$ac after_sha_est_merge=$([ "$a" = "$M" ] && echo oui || echo non) pull_request=$pr required_status_checks=$rc lu_par=$lu relue=$rl acces=$acc message=$msg nuance=$nu coherence=$ok"; [ "$ok" = oui ] && [ "$nu" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `coherence=non` — statut de règle autre que `prouvee`/`non_prouvee` ; sur un merge : suite dont `after_sha` n'est pas le commit de merge, lecteur non nommé, JSON de trace non capturé, `relue=oui` alors que l'accès mesuré est `refuse` (ou l'inverse), ou valeurs consignées (résultat, `after_sha`, acteur, règles en échec) différentes de la relecture API ; sans merge : trace autre que `suite=absente`, `non_prouvee` pour les deux règles, `relue=non_relue` ; ou `nuance=` différent de 1 (la ligne `TRACE-II-NUANCE:` dit « pas sur une PR rouge », le nombre de checks en cours, que l'exigence du geste n'est pas mesurée et que le refus par défaut n'est pas tenté). Un `non_prouvee` cohérent NE rougit PAS cette vérification : il est porté par le verdict</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; adm=$(val M3: admin); res=$(val M3: resultat); M=$(val M3: merge); cv=$(val M3: checks_en_cours_avant); r=$(val TRACE-II: result); a=$(val TRACE-II: after_sha); ac=$(val TRACE-II: acteur); pr=$(val TRACE-II: pull_request); rc=$(val TRACE-II: required_status_checks); if [ "$res" != merged ]; then att=ECART; elif [ "$adm" = merge ] && [ "$r" = bypass ] && [ "$a" = "$M" ] && [ "$ac" = picmakpro ] && { [ "$pr" = prouvee ] || [ "$rc" = prouvee ]; } && [ "${cv:-0}" -gt 0 ]; then att=CONFORME; else att=PARTIEL; fi; v=$(awk "\$1==\"M3-VERDICT:\" {v=\$2} END{print v}" "$PH/41-PREUVES.md"); echo "verdict M-3 et trace : resultat=$res admin=$adm result=$r acteur=$ac pull_request=$pr required_status_checks=$rc checks_en_cours_avant=$cv attendu=$att consigne=$v"; [ "$v" = "$att" ] && [ "$v" = CONFORME ]'</automated>
    <fails_when>exit non nul ; ou verdict consigné différent du verdict recalculé (erreur de consignation, corrigée ici) ; ou `PARTIEL` / `ECART` — `--admin` refusé, « SUSPENDU », aucune suite `bypass` sur le commit de merge, autre acteur, aucune règle `prouvee`, ou aucun check en cours au moment du geste : consigné tel quel, AUCUN nouveau geste, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; K=REJEU-TESTS-41-08:; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); ne=$(awk -F" [|] " "/^REJEU tests [|] / && \$3 != \"rc=0\" {c++} END{print c+0}" "$T/t"); awk -F"|" "{i=\$2; f=\$5; s=\$8; gsub(/ /, \"\", i); gsub(/ /, \"\", f); gsub(/ /, \"\", s); if (s==\"open\" && f ~ /\\/tests\\/test-[^\\/]*\\.sh\$/) print f \"\t\" i}" .planning/WINDOWS.md > "$T/ledger"; awk "/^::error::FAIL\\(rc=[0-9]+\\) / {sub(/^::error::FAIL\\(rc=[0-9]+\\) /, \"\"); print}" "$T/t" > "$T/ko"; awk -F"\t" "NR==FNR {l[\$1]=\$2; next} !(\$0 in l)" "$T/ledger" "$T/ko" > "$T/hl"; awk -F"\t" "NR==FNR {l[\$1]=\$2; next} (\$0 in l) {print l[\$0]}" "$T/ledger" "$T/ko" > "$T/id"; sort -n -u "$T/id" > "$T/ids"; tl=$(awk "BEGIN{ORS=\"\"} {print (NR>1 ? \",\" : \"\") \"WINDOWS#\" \$0} END{if (NR==0) print \"aucun\"}" "$T/ids"); hl=$(awk "END{print NR}" "$T/hl"); nk=$(awk "END{print NR}" "$T/ko"); awk "{print \"HORS LEDGER : \" \$0}" "$T/hl"; d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); nl=$(awk -v k="$K" "\$1==k {c++} END{print c+0}" "$PH/41-PREUVES.md"); att=$([ "$b" != absent ] && set -- $b && [ "$2" -gt 0 ] && echo 1 || echo 0); echo "tests rc=$rc etapes_en_echec=$ne bilan(N M)=$b echecs=$nk tolere=$tl echecs_hors_ledger=$hl decouverte_independante=$d | consigne $K lignes=$nl rc=$(val "$K" rc) suites=$(val "$K" suites) echecs=$(val "$K" echecs) echecs_preexistants_ledger=$(val "$K" echecs_preexistants_ledger) echecs_hors_ledger=$(val "$K" echecs_hors_ledger)"; [ "$b" != absent ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq "$nk" ] && [ "$hl" -eq 0 ] && [ "$ne" -eq "$att" ] && [ "$rc" -eq "$ne" ] && [ "$nl" -eq 1 ] && [ "$(val "$K" rc)" = "$rc" ] && [ "$(val "$K" suites)" = "$1" ] && [ "$(val "$K" echecs)" = "$2" ] && [ "$(val "$K" echecs_preexistants_ledger)" = "$tl" ] && [ "$(val "$K" echecs_hors_ledger)" = 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, ou N différent de la découverte indépendante ; ou nombre d'échecs du bilan différent du nombre de lignes `::error::FAIL` ; ou un échec dont le chemin n'est exactement celui d'aucune entrée `open` de `.planning/WINDOWS.md` (ligne `HORS LEDGER :` ; seuls les rouges d'environnement déjà tracés au ledger sont tolérés, #6 et #7 sur ce poste au 2026-09-24, T-41-94) ; ou une étape du job autre que celle des suites en échec (`etapes_en_echec=` différent de 1 quand le bilan compte des échecs, de 0 sinon), ou un rc de l'outil différent de `etapes_en_echec` ; ou `REJEU-TESTS-41-08:` absente ou dupliquée, ou dont `rc`, `suites`, `echecs` ou la liste `echecs_preexistants_ledger` diffèrent du rejeu, ou dont `echecs_hors_ledger` n'est pas 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } if (index(m, \"D-02ter\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-24\") == 0) { print \"D-02ter SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis|D-02ter/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` (un commit de plan cite D-02bis sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 ») ; ou `D-02ter SANS CANAL NI DATE` (un commit de plan cite D-02ter sans « arbitrage Willy, AskUserQuestion session principale, 2026-09-24 ») ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan cite `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » égal aux valeurs CAPTURÉES — frontmatter de 41-01 ou ligne `*-DECISION:` de `41-PREUVES.md`) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - chemin mergé : geste `--admin` consigné avec sa sortie ; seul le README atterri, CI de `main` verte, branche supprimée ; trace sur le commit de merge, acteur et règles `prouvee`/`non_prouvee` cohérents avec la relecture API (ou `non_relue`)
    - chemin non mergé (`--admin` refusé ou « SUSPENDU ») : PR-P ouverte, trace `absente`, les deux règles `non_prouvee`
    - `PROVENANCE-SERVEUR: rest=non_tente statut=hors_d_atteinte` ; `TRACE-II-NUANCE:` présente et conforme ; `M3-VERDICT` recalculé = consigné ; `CONFORME` pour poursuivre, sinon Task 4
    - rejeu `gates` vert ; rejeu `tests` sans échec hors ledger ni autre étape en échec, liste tolérée consignée (`REJEU-TESTS-41-08:`) = rejeu
  </acceptance_criteria>
  <done>Le contournement explicite de la seule PR inoffensive est tracé règle par règle — ou son absence est consignée — sans rien supposer ni élargir sa portée.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `M3-VERDICT` ≠ CONFORME (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <read_first>
    - .planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`M3:`, `M3-ADMIN-MSG:`, `TRACE-II:`, `TRACE-II-NUANCE:`, `CONSTAT-ETAT-LECTEUR:`, `M3-VERDICT:`)
  </read_first>
  <action>L'exécutant présente la mesure et s'arrête ; aucun nouveau geste chez GitHub sans la réponse.</action>
  <decision>Le contournement explicite n'a pas eu lieu (« SUSPENDU », `--admin` refusé) ou sa trace est incomplète : que faire de PR-P et des rulesets, et la phase poursuit-elle avec une trace déclarée non prouvée ?</decision>
  <context>
    Lire `M3:`, `M3-ADMIN-MSG:`, `TRACE-II:`, `TRACE-II-NUANCE:`, `CONSTAT-ETAT-LECTEUR:` et
    `M3-VERDICT:`. Un « SUSPENDU » peut venir d'une volonté de réarbitrer D-02ter au vu du constat
    de lecture (un compte de la liste voit un état bloqué). Le plan 41-09 exige que PR-P ne soit plus
    ouverte : si PR-P reste ouverte, 41-09 attend explicitement la résolution de PR-P ; aucune option
    ne le bloque sans le dire. D-M7 prévoit qu'« à défaut, la trace reste déclarée non prouvée, jamais
    supposée ».
  </context>
  <options>
    <option id="fermer-et-poursuivre">
      <name>PR-P OUVERTE : LA FERMER, DÉCLARER LA TRACE NON PROUVÉE, POURSUIVRE</name>
      <pros>Chemin réel du repli de D-M7 : Willy ferme PR-P sans merge (`gh pr close <n> --repo picmakpro/vibeflow-os` puis `git push origin --delete preuve/41-contournement`) ; rulesets inchangés ; 41-09 peut démarrer ; ADR-072 et le ledger écrivent « non prouvée ».</pros>
      <cons>La trace d'un contournement explicite (point c de D-02ter) n'est pas prouvée.</cons>
    </option>
    <option id="laisser-ouverte-et-cadrer">
      <name>PR-P OUVERTE : LA LAISSER ET CADRER</name>
      <pros>Un réarbitrage de D-02ter, ou un autre geste de contournement, peut être cadré sur le même support inoffensif, encore intact.</pros>
      <cons>41-09 et la suite attendent explicitement la résolution de PR-P (précondition « PR-P non ouverte ») ; rien n'avance sans un nouvel arbitrage.</cons>
    </option>
    <option id="consigner-et-poursuivre">
      <name>PR-P MERGÉE : CONSIGNER ET POURSUIVRE</name>
      <pros>Les écarts mesurés (suite absente, autre acteur, aucune règle en échec, checks déjà terminés) sont écrits tels quels dans ADR-072 § Mesures et le ledger ; 41-09 peut démarrer.</pros>
      <cons>La trace n'est prouvée que partiellement ; à acter comme arbitrage daté et attribué.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE</name>
      <pros>PR-P fermée si ouverte ; `picmakpro` repasse chaque ruleset en `disabled` (commande de `RETOUR-ARRIERE-ECRIT:`).</pros>
      <cons>`main` redevient protégée par rien ; la phase repasse par le cadrage.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - réponse explicite obtenue, jamais inférée ; `M3-DECISION:` consignée (état de PR-P relu) et committée
  </acceptance_criteria>
  <resume-signal>Répondre « non déclenchée », « PR-P OUVERTE : LA FERMER… », « PR-P OUVERTE : LA LAISSER ET CADRER », « PR-P MERGÉE : CONSIGNER ET POURSUIVRE » ou « RETOUR ARRIÈRE ». Après la réponse, l'exécutant consigne `M3-DECISION: option=<id> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint> pr_p=<etat relu via l'API>` — jamais inféré, jamais une date recopiée d'ailleurs — et le commite sur B avec le message « docs(41-08): M3-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `M3-DECISION` sans ce canal et cette date rougit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contournement → `main` | seul contenu autorisé à entrer par contournement : le README de preuve |
| trace lue → doctrine | ce que la rule suite montre est recopié dans ADR-072 et le ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-30 | Tampering | contournement appliqué à un contenu nocif | high | mitigate | précondition PR-R et PR jetables fermées non mergées ; P2r vérifié (un fichier, une ligne ajoutée, aucun retrait, aucun chemin de machine) ; ce qui atterrit présenté au checkpoint avant le geste ; fichiers atterris relus = README seul ; CI de `main` verte |
| T-41-31 | Repudiation | trace supposée ou lue sur la mauvaise suite | high | mitigate | suite choisie par `after_sha` = commit de merge ; acteur relu ; statut par règle ; relecture API par l'agent quand il a l'accès, sinon `non_relue` |
| T-41-32 | Repudiation | contournement de checks rouges affirmé sans preuve | medium | mitigate | `checks_en_cours_avant` consigné ; `TRACE-II-NUANCE:` exigée ; formulation reprise au SUMMARY et dans ADR-072 |
| T-41-33 | Elevation of privilege | exécutant sous jeton admin | medium | mitigate | l'exécutant ne fait que des lectures dans ce plan ; le seul merge est un geste de Willy au checkpoint |
| T-41-51 | Repudiation | trace présentée comme un refus par défaut, ou comme une exigence de geste | medium | mitigate | aucun merge sans contournement tenté ; `PROVENANCE-SERVEUR: rest=non_tente statut=hors_d_atteinte` ; `exigence_du_geste=non_mesuree` et `refus_par_defaut=non_tente` exigés dans la nuance |
| T-41-52 | Denial of service | PR-P laissée ouverte bloquant 41-09 sans le dire | low | mitigate | options de la Task 4 explicites sur le sort de PR-P et l'attente de 41-09 ; `M3-DECISION:` consigne l'état relu de PR-P |
| T-41-90 | Tampering | README atterri affirmant plus que la mesure | medium | mitigate | dernière ligne réécrite (P2r) sans mention du critère 2, renvoyant à la trace « telle que lue » (vérifié) |
| T-41-91 | Repudiation | geste irréversible joué sans que le décideur ait vu un fait qui contredit la prémisse de sa décision | medium | mitigate | `CONSTAT-ETAT-LECTEUR:` remesuré en Task 1 et présenté avant le geste ; « SUSPENDU » n'écrit rien |
| T-41-94 | Repudiation | rouge réel masqué par la tolérance des rouges d'environnement locaux | low | mitigate | tolérance limitée aux chemins exacts des entrées `open` de `.planning/WINDOWS.md`, lus dans le ledger et non écrits dans le plan ; tout autre échec imprimé `HORS LEDGER :` et rougit, comme une étape du job autre que celle des suites en échec ; liste tolérée consignée par id (`REJEU-TESTS-41-08:`) et recomparée au rejeu ; la CI réelle, qui n'a pas ces rouges, reste la référence (`ci_main` de `MAIN-APRES-PR-P:`) |
</threat_model>

<verification>
Contournement explicite et inoffensif (ou décision humaine) ; trace lue sur la bonne suite, relue ou
marquée non relue, consignée règle par règle ; portée écrite sans l'élargir (ni refus par défaut,
ni exigence du geste).
</verification>

<success_criteria>
Point (c) de D-02ter prouvé — trace d'un contournement explicite, règle par règle — ou déclaré
`non_prouvee` sur décision ; M-3 réduit au geste `--admin` consigné ; refus par défaut non tenté et
dit hors d'atteinte dans ce plan.
</success_criteria>

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41-posture-de-protection-du-d-p-t/41-08-SUMMARY.md` when done — committé sur la branche B. Le SUMMARY reprend mot pour mot la nuance : « contournement réel sur des checks en cours, pas sur une PR rouge » (vérifiée par le plan 41-13), et écrit que la trace ne prouve ni un refus par défaut ni que GitHub exige le geste d'un compte `always`.

Règle STATE.md (ADR-063) : jamais `gsd-tools state.*` ni `gsd_run query commit` sur `.planning/workstreams/fiabilite/STATE.md` ; éditions à la main dans le corps seulement ; dans le frontmatter, seuls `last_updated`, `last_activity`, `last_activity_desc` (quatre lignes au plus) et les nombres de `progress` changent ; le frontmatter se ferme avant la ligne 60.
</output>

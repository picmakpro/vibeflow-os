---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 02
lot: L02 — check-divergence.sh consomme vf_ws_enumerate : dédup + fermeture du trou exit-0 (D-01)
type: execute
wave: 2
depends_on: ["41.1-01"]
autonomous: true
requirements: [WSAW-01, WSAW-04]
files_modified:
  - plugin/conductor/scripts/check-divergence.sh
  - plugin/conductor/scripts/tests/test-check-divergence.sh

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `plugin/conductor/scripts/check-divergence.sh` ou `plugin/conductor/scripts/tests/test-check-divergence.sh` porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh` — présence et forme vérifiées machine, jamais la véracité de la raison)"
    - "Les DEUX boucles inline dupliquées (`for _wsdir in \"$WS_ROOT\"/*/` dans le corps principal ET dans `check_s5()`) sont remplacées par UN appel commun à `vf_ws_enumerate \"$PLANNING\"` — plus aucune occurrence de `for _wsdir in \"$WS_ROOT\"` ni `for wsdir in \"$WS_ROOT\"` dans le fichier"
    - "Le trou WSAW-01 est fermé : quand `vf_ws_enumerate` rend 2 (workstreams/ présent mais illisible/lien/vide-après-filtre), `check-divergence.sh` rend 2 — JAMAIS l'ancien repli qui tombait dans le `else` de fin de script et sortait 0"
    - "Quand `vf_ws_enumerate` rend 3 (SILENCE, workstreams/ absent), le comportement est INCHANGÉ : `check-divergence.sh` sortait déjà 3 avant même de sourcer la politique dans ce cas (garde en tête de fichier) — non touché par ce plan"
    - "Le compartiment RACINE (repli legacy, `if [ -d \"$PLANNING/phases\" ]`) reste un check SÉPARÉ, hors de `vf_ws_enumerate` (qui n'énumère QUE `workstreams/*/`) — ce bloc n'est PAS touché par ce plan"
    - "Les codes de sortie 0/1/2/3/64 du fichier restent EXACTEMENT ceux documentés en tête (lignes ~44-52) — seul le CHEMIN qui mène à 2 change (il couvre désormais aussi le cas `vf_ws_enumerate` rc=2), le contrat externe du script est inchangé pour tout appelant existant qui ne dépend pas du trou"
    - "`check_s5()` boucle sur le MÊME résultat de `vf_ws_enumerate` que le corps principal — appelée une seule fois, son propre appel à l'énumération est supprimé, pas dupliqué une troisième fois"
    - "QUAL-01 : un cas de mutation prouve la fermeture du trou — une fixture où `workstreams/` contient UNIQUEMENT un lien symbolique (donc `vf_ws_enumerate` rend 2) fait rendre check-divergence.sh à 2, là où l'ancien code (avant ce plan, rejoué sur une copie horodatée AVANT modification) rendait 0 sur la même fixture"
  artifacts:
    - path: "plugin/conductor/scripts/check-divergence.sh"
      provides: "boucle unique consommant vf_ws_enumerate, trou exit-0 fermé"
      contains: "vf_ws_enumerate \"$PLANNING\""
    - path: "plugin/conductor/scripts/tests/test-check-divergence.sh"
      provides: "cas WSAW-01 : workstreams/ tout-lien → rc=2 (pas 0)"
  key_links:
    - "`vf_ws_enumerate` vit dans `workstream-policy.sh`, déjà sourcé par ce script via la recherche à deux candidats (lignes ~94-102, INCHANGÉE) — zéro nouveau sourcing"
    - "Le fichier `41.1-PATTERNS.md` cite les deux boucles exactes à supprimer (copie #1 lignes ~296-310 du corps principal, copie #2 lignes ~250-263 de check_s5) et le bloc de fin de script (CHECKED/else/exit 0) qui porte le trou — relire ces extraits avant d'éditer"
---

<objective>
Faire consommer `vf_ws_enumerate` (plan 41.1-01) par `check-divergence.sh`, en lieu et place de ses
deux boucles inline dupliquées, et fermer le trou qui rend `exit 0` quand `workstreams/` existe
mais que rien n'a pu être inspecté (tous liens, ou vide après filtre). C'est la seconde moitié de
WSAW-01, celle que 41.1-01 seule ne peut pas fermer : `vf_ws_enumerate` sait DISTINGUER « rien à
inspecter » de « présent mais illisible » — encore faut-il que son appelant PROPAGE cette
distinction au lieu de la faire disparaître dans un `else` de complaisance.

Purpose: le gate a1 de référence (`check-divergence.sh`, seul consommateur qui balaie déjà TOUS
les compartiments) devient le premier exemple concret que tout futur consommateur imite.
Output: `check-divergence.sh` sans boucle inline dupliquée, discriminant sur le cas vide-après-filtre.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `plugin/conductor/scripts/check-divergence.sh` en entier (334 lignes) — en particulier les deux
  boucles à supprimer (corps principal ~296-310, `check_s5()` ~250-263) et le bloc de fin
  (CHECKED/if/else/exit 0, ~322-334)
- `plugin/conductor/scripts/tests/test-check-divergence.sh` en entier — style `mk_git_root`,
  `fingerprint_content`, `run()` avec `env -u`
</context>

<discipline>
- Aucune commande git en ligne dans un `<verify>` — tout travail git de fixture vit DANS le script
  de test, invoqué par `bash <chemin>`.
- `diff`/`ls` proxifiés sur ce poste — `rtk proxy` si utilisés ; comparaisons `cmp`/`comm`/`cksum`.
- Ne PAS toucher au bloc « Compartiment racine (repli legacy) » ni aux fonctions `check_s2`,
  `check_s4a`, `check_s4b` elles-mêmes — seule la BOUCLE qui les appelle change, pas leur logique.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : dédup des deux boucles, `CHECKED` piloté par le code de `vf_ws_enumerate`</name>
  <files>plugin/conductor/scripts/check-divergence.sh</files>
  <behavior>
    - `.planning/workstreams/<n>/` × 2, tous deux valides → comportement STRICTEMENT identique à
      avant ce plan (S2/S4/S5 toujours détectés sur les fixtures existantes de la suite).
    - `.planning/workstreams/` contient UNIQUEMENT un lien symbolique (aucun compartiment réel) →
      AVANT ce plan : `exit 0` (« présent mais vide »). APRÈS ce plan : `exit 2` (NON VÉRIFIABLE),
      message stderr qui nomme la cause (relayé par `vf_ws_enumerate`).
    - `.planning/workstreams/` absent → inchangé, `exit 3` (garde en tête de fichier, non touchée).
    - le compartiment racine (`$PLANNING/phases`) continue d'être inspecté indépendamment.
  </behavior>
  <action>
Après la ligne de sourcing de `workstream-policy.sh` (inchangée), et après le bloc `vf_ws_path_nolink
"$WS_ROOT"` déjà présent en tête (inchangé — c'est un garde-fou EXISTANT distinct de
`vf_ws_enumerate`, laissé en l'état pour ne pas changer le point où `WS_ROOT` lien symbolique
rend 2), procéder comme suit.

**Supprimer** la copie #1 (corps principal, boucle `for _wsdir in "$WS_ROOT"/*/` qui appelle
`check_s2`/`check_s4a`/`check_s4b`) et la copie #2 (dans `check_s5()`, boucle `for wsdir in
"$WS_ROOT"/*/` qui construit `ws_nums`).

**Remplacer par un appel unique** à `vf_ws_enumerate "$PLANNING"`, capturé une seule fois avant les
deux usages (corps principal ET `check_s5`), pour ne jamais énumérer deux fois le disque dans la
même exécution :
```bash
# --- Compartiments de workstream — une seule énumération, deux consommateurs -----------------
WS_LIST_TMP="$TMPD/ws_list"
vf_ws_enumerate "$PLANNING" > "$WS_LIST_TMP" 2>&2
_ws_rc=$?
case "$_ws_rc" in
  0)
    while IFS= read -r _wsdir; do
      [ -n "$_wsdir" ] || continue
      _wsname="$(basename "$_wsdir")"
      CHECKED=1
      check_s2 "$_wsname" "$_wsdir/phases" || true
      check_s4a "$_wsname" "$_wsdir/phases" "$_wsdir/ROADMAP.md" || true
      check_s4b "$_wsname" "$_wsdir/phases" "$_wsdir/STATE.md" || true
    done < "$WS_LIST_TMP"
    ;;
  3)
    : # SILENCE — aucun compartiment de workstream, cohérent avec la garde de tête (ne devrait
      # normalement plus être atteint ici puisque le script sort déjà en 3 plus haut si WS_ROOT
      # est absent, mais vf_ws_enumerate peut aussi le rendre si WS_ROOT existe sans workstreams/
      # valide — cas déjà couvert par le garde de tête, ce bloc reste défensif)
    ;;
  2)
    echo "[check-divergence] vf_ws_enumerate : workstreams/ non vérifiable — $WS_ROOT" >&2
    exit 2
    ;;
  *)
    # CORRECTION post plan-checker (FLAG-6) : un `case` sans branche par défaut laisse traverser
    # tout code que `vf_ws_enumerate` pourrait un jour rendre hors de son contrat documenté
    # (0/2/3, D-01) SANS toucher CHECKED — le script atteindrait alors le bloc de fin et pourrait
    # rendre 0 par défaut de complaisance, rouvrant EXACTEMENT le trou que ce plan ferme. Un code
    # imprévu est fail-closed : NON VÉRIFIABLE, jamais un 0 implicite.
    echo "[check-divergence] vf_ws_enumerate : code de sortie imprévu ($_ws_rc, attendu 0/2/3) — non vérifiable" >&2
    exit 2
    ;;
esac
```
Réutiliser `$WS_LIST_TMP` (déjà énuméré) dans `check_s5()` au lieu d'un second appel à
`vf_ws_enumerate` : passer le fichier en argument à `check_s5` (`check_s5 "$WS_LIST_TMP"`) plutôt
que de le laisser ré-énumérer le disque. `check_s5()` boucle alors sur `< "$1"` au lieu de son
`for wsdir in "$WS_ROOT"/*/` original, en conservant `vf_ws_path_nolink` comme AVANT n'est plus
nécessaire dans cette boucle (déjà filtré par `vf_ws_enumerate`) — supprimer cet appel devenu
redondant dans `check_s5`.

**Bloc de fin de script** (rapport final) : le `if [ "$CHECKED" -eq 1 ]` reste — mais désormais,
si `_ws_rc` était 2, le script a déjà quitté en 2 plus haut et n'atteint jamais ce bloc ; si
`_ws_rc` était 3 ET que le compartiment racine n'a rien mis à `CHECKED=1` non plus, le message
« présent mais vide » du `else` ne peut plus jamais correspondre au cas `workstreams/ non vide mais
tout filtré` (c'est désormais rc=2, sorti plus haut) — seul reste le cas légitime où NI la racine NI
aucun workstream n'existent, message inchangé.

Mettre à jour le commentaire d'en-tête (lignes ~44-52, section codes de sortie) : ajouter une phrase
sous le code `2` précisant que ce code couvre désormais aussi « `vf_ws_enumerate` ne peut rendre
aucun compartiment vérifiable (workstreams/ présent mais illisible, lien, ou vide après filtre
anti-lien) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/conductor/scripts/check-divergence.sh && echo "syntaxe OK"'</automated>
    <fails_when>`bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c '
n1=$(awk "/for _wsdir in \"\\\$WS_ROOT\"/{c++} END{print c+0}" plugin/conductor/scripts/check-divergence.sh)
n2=$(awk "/for wsdir in \"\\\$WS_ROOT\"/{c++} END{print c+0}" plugin/conductor/scripts/check-divergence.sh)
n3=$(awk "/vf_ws_enumerate/{c++} END{print c+0}" plugin/conductor/scripts/check-divergence.sh)
n4=$(awk "/case \"\\\$_ws_rc\"/{f=1} f && /\\*\\)/{print; c++} /esac/{f=0} END{print c+0}" plugin/conductor/scripts/check-divergence.sh | tail -1)
echo "boucles_inline_restantes=$((n1+n2)) appels_vf_ws_enumerate=$n3 branche_defaut=${n4:-0}"
[ "$((n1+n2))" -eq 0 ] && [ "$n3" -ge 1 ] && [ "${n4:-0}" -ge 1 ]
'</automated>
    <fails_when>`boucles_inline_restantes=` différent de 0 (une des deux copies n'a pas été supprimée) ; ou `appels_vf_ws_enumerate=` égal à 0 (le fichier ne consomme pas la primitive) ; ou `branche_defaut=` égal à 0 (le `case "$_ws_rc"` n'a pas de branche `*)` fail-closed — FLAG-6 du juge frais, un code imprévu retraverserait le trou que ce plan ferme)</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q
mkdir -p "$T/.planning/workstreams"
ln -s /nonexistent-cible "$T/.planning/workstreams/lien-seul"
git -C "$T" add -A >/dev/null 2>&1
git -C "$T" -c user.email=t@t -c user.name=t commit -q -m x --allow-empty >/dev/null 2>&1
rc=0; out=$(env -u GSD_WORKSTREAM -u VF_WORKSTREAM bash plugin/conductor/scripts/check-divergence.sh --path "$T" 2>&1) || rc=$?
echo "workstreams_tout_lien rc=$rc"; printf "%s\n" "$out"
[ "$rc" -eq 2 ]
'</automated>
    <fails_when>`workstreams_tout_lien rc=` différent de 2 — c'est exactement le trou WSAW-01 : `workstreams/` non vide (un lien) mais rien de vérifiable ne doit JAMAIS rendre 0</fails_when>
  </verify>
  <acceptance_criteria>
    - aucune des deux boucles inline originales ne subsiste, `vf_ws_enumerate` est appelée
    - `bash -n` passe
    - une fixture `workstreams/` contenant SEULEMENT un lien symbolique rend rc=2, jamais rc=0
    - le commentaire d'en-tête documente l'extension du code 2
  </acceptance_criteria>
  <done>`check-divergence.sh` a une source unique de vérité sur « quels compartiments existent », et ne rend plus jamais 0 sur un `workstreams/` non vide mais entièrement filtré.</done>
</task>

<task type="auto">
  <name>Task 2 : suite — non-régression S2/S4/S5 + cas WSAW-01 + rejeu gates/tests</name>
  <files>plugin/conductor/scripts/tests/test-check-divergence.sh</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/conductor/scripts/tests/test-check-divergence.sh` en entier (déjà lu en Task 1, relire les cas MUT-1/MUT-2 existants pour ne pas les casser)
  </read_first>
  <action>
Ajouter un cas « WSAW-01 — workstreams/ non vide mais entièrement filtré → 2, pas 0 » : construit
un dépôt (`mk_git_root`) avec `.planning/workstreams/` contenant UNIQUEMENT un lien symbolique
(vers une cible absente ou hors dépôt), commité, et asserte `rc=2` via `run()`. Vérifie aussi que
le message stderr nomme la cause (contient « lien symbolique » ou « non vérifiable »).

Ajouter un second cas « FLAG-6 (revue plan-checker) — code `vf_ws_enumerate` imprévu → 2, jamais 0
implicite » : `vf_ws_enumerate` n'a que trois codes contractuels (0/2/3, D-01) — pour prouver que
le `case` de `check-divergence.sh` traite bien un QUATRIÈME code sans le laisser traverser, copier
`check-divergence.sh` ET `workstream-policy.sh` dans un répertoire temporaire (`mktemp -d`), et
PATCHER la copie de `workstream-policy.sh` par `sed` pour que sa fonction `vf_ws_enumerate` fasse
`return 99` au lieu de son corps réel (mutation SUR LA COPIE, jamais sur le fichier source — même
discipline que les mutants existants de la suite). Exécuter la copie patchée de
`check-divergence.sh` (qui résout `$(dirname "$0")/workstream-policy.sh` vers la copie patchée du
même répertoire, avant même de considérer le second candidat) contre une fixture git normale
(`.planning/workstreams/<n>/` valide) et asserter `rc=2` : le `case` doit tomber dans sa branche
`*)`, pas continuer silencieusement vers un `exit 0` de fin de script. Vérifier aussi que le
message stderr contient « imprévu ».

Rejouer TOUS les cas existants de la suite (S2, S4a, S4b, S5, MUT-1, MUT-2, le cas de padding
numérique) sans aucune modification de leur fixture ni de leur assertion attendue — la preuve de
non-régression est que ces cas passent SANS ÊTRE ÉDITÉS, seul le nouveau cas est ajouté.

Rejouer ensuite les jobs `gates` et `tests` complets via l'outil de rejeu existant (chemin
partitionné, module `40.1`) et consigner le bilan au SUMMARY.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/conductor/scripts/tests/test-check-divergence.sh</automated>
    <fails_when>exit non nul ; ou une ligne `✗` ; ou le cas WSAW-01 absent de la sortie (chercher la sous-chaîne « WSAW-01 ») ; ou le cas FLAG-6 absent de la sortie (chercher la sous-chaîne « FLAG-6 »)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 (une régression sur une autre suite du parc, notamment `test-workstream-policy.sh` du plan précédent)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^BILAN REJEU gates/" "$T/g"; echo "gates rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 — en particulier l'étape « Gates workstream-aware sur un arbre RÉELLEMENT partitionné » et l'étape « check-divergence.sh en CI » doivent rester vertes sur leurs propres fixtures existantes, non modifiées par ce plan</fails_when>
  </verify>
  <acceptance_criteria>
    - tous les cas préexistants de la suite passent SANS modification de leur fixture
    - un nouveau cas WSAW-01 prouve rc=2 sur `workstreams/` entièrement filtré
    - rejeu `gates` et `tests` verts sur la découverte complète
  </acceptance_criteria>
  <done>La non-régression S2/S4/S5 est prouvée, le trou WSAW-01 est prouvé fermé, et les deux jobs CI restent verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `vf_ws_enumerate` → `check-divergence.sh` | le gate a1 délègue désormais entièrement la découverte de compartiments à une primitive externe — une régression dans la primitive se propage silencieusement à ce gate si la suite ne la détecte pas |
| fixture CI existante → nouveau code | les étapes CI « Gates workstream-aware… » et « check-divergence.sh en CI » ont leurs propres fixtures, non modifiées par ce plan — elles doivent rester vertes sans édition |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-04 | Denial of service | vert à vide résiduel si un troisième chemin de code (hors des deux boucles supprimées) recréait le trou | high | mitigate | cas WSAW-01 de la suite, rejoué à chaque CI, sur une fixture dédiée qui n'existait pas avant ce plan |
| T-41.1-05 | Tampering | régression sur S2/S4/S5 par erreur de branchement lors du remplacement des boucles | medium | mitigate | tous les cas préexistants rejoués sans modification de fixture ; le bilan de la suite doit rester à 0 `ko` |
</threat_model>

<verification>
`bash -n` ; aucune boucle inline résiduelle ; fixture `workstreams/` tout-lien → rc=2 ; suite
complète verte (préexistants + nouveau cas) ; rejeu `gates` et `tests` verts.
</verification>

<success_criteria>
`check-divergence.sh` consomme `vf_ws_enumerate` comme unique source de compartiments, ses deux
boucles dupliquées ont disparu, et il ne rend plus jamais 0 sur un `workstreams/` non vide mais
entièrement filtré — WSAW-01 est fermée sur ce gate.
</success_criteria>

## Artifacts this phase produces

- `plugin/conductor/scripts/check-divergence.sh` — boucle unique, trou exit-0 fermé.
- `plugin/conductor/scripts/tests/test-check-divergence.sh` — cas WSAW-01 ajouté.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-02-SUMMARY.md` when done
</output>

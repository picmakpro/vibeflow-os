---
phase: 34-gaps-agency-agents-cadrage-skill-installer
plan: 06
type: execute
wave: 4
depends_on: ["34-01", "34-02", "34-03", "34-04", "34-05"]
files_modified:
  - .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha
  - .planning/BACKLOG.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [AGTS-01, AGTS-02, SKIL-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le ledger dit EXACTEMENT ce que les trois notes de décision ont établi — ni plus, ni moins. Chaque case cochée renvoie à la note qui la prouve ; aucune case n'est cochée sans preuve écrite. La cohérence entre le verdict de chaque note et l'état de la case correspondante est vérifiée PAR MACHINE : cocher AGTS-02 alors que `34-RUN-MOBILE.md` porte un chapeau ROUGE échoue la vérification."
    - "AGTS-02 sur run ROUGE est REPORTÉE AVEC TRACE, jamais abandonnée en silence (D-05) : sa case reste décochée, sa ligne porte la mention du report et le renvoi nommé à `34-RUN-MOBILE.md`, et le déclencheur de reprise daté rédigé par 34-01 est transcrit dans `.planning/BACKLOG.md` au gabarit du fichier."
    - "SKIL-01 est COCHÉE sur GO comme sur NO-GO : l'exigence demande un cadrage go/no-go rendu par écrit, pas un GO. Elle reste décochée sur MESURE INVALIDE — dans ce cas seul, l'item BACKLOG du 2026-06-04 reste ouvert et la question n'est pas close. Cette règle est écrite dans la ligne de traçabilité, pas seulement appliquée."
    - "L'item BACKLOG du skill-installer (`.planning/BACKLOG.md`, entrée du 2026-06-04) n'est clos QUE sur NO-GO, avec le texte prêt à coller rédigé dans `34-SPIKE-SKIL.md` § `## Conséquence ledger` — une ligne factuelle, la raison entre parenthèses, jamais de prose longue. L'anti-feature « marketplace de skills maison » de `.planning/REQUIREMENTS.md` reste gravée dans tous les cas : ce plan ne la retire ni ne l'amende."
    - "L'item BACKLOG des gaps `agency-agents` (entrée du 2026-07-20) est mis à jour avec le verdict par gap de `34-AUDIT-AGTS.md`, et chaque gap jugé « à combler » devient un item daté avec sa preuve, au gabarit du fichier — le nombre d'items transcrits est égal au nombre d'items rédigés dans la note, comparé par machine."
    - "`PROJECT.md` : l'item actif « Sortie du statut expérimental de `mobile-test`(-team) » n'est coché QUE si `34-RUN-MOBILE.md` porte un chapeau VERT. Sur ROUGE, il reste actif et gagne une mention datée du run tenté et de sa cause."
    - "`STATE.md` est édité À LA MAIN, jamais par `gsd-tools state record-session` — cette commande réécrit le fichier et perd son contenu manuel (ADR-063). Les compteurs de son frontmatter sont RE-DÉRIVÉS par comptage réel (plans de phase, phases complètes) et ne DÉCROISSENT jamais ; `check-state-integrity.sh --file .planning/STATE.md` passe après l'édition, comme dans le job `gates` de la CI."
    - "Ce plan ne touche AUCUN fichier hors de `.planning/` : ni `plugin/`, ni `scripts/`, ni `manual/`, ni les README racine — tous ces gestes appartiennent aux plans 34-04 et 34-05 et sont déjà faits (ou délibérément non faits) quand celui-ci s'exécute."
  artifacts:
    - ".planning/BACKLOG.md : item `agency-agents` du 2026-07-20 mis à jour avec le verdict par gap ; item skill-installer du 2026-06-04 clos (NO-GO seulement) ; items datés pour chaque gap « à combler » ; déclencheur de reprise AGTS-02 (run ROUGE seulement)."
    - ".planning/REQUIREMENTS.md : cases `AGTS-01`, `AGTS-02`, `SKIL-01` mises à jour avec renvoi nommé à la note qui prouve chacune ; lignes correspondantes de la table de traçabilité (`| SKIL-01 | Phase 34 | … |`, idem AGTS-01/02) passées de `Pending` à leur état réel."
    - ".planning/PROJECT.md : item actif « Sortie du statut expérimental de `mobile-test`(-team) » coché sur run VERT, ou enrichi d'une mention datée du run tenté sur ROUGE."
    - ".planning/STATE.md : § `Roadmap Evolution` enrichi d'une entrée datée pour la Phase 34, § `Current Position` et § `Decisions` mis à jour, compteurs du frontmatter re-dérivés — édition manuelle, gate d'intégrité vert."
    - ".planning/ROADMAP.md : § `### Phase 34` — cases des six plans cochées selon leur exécution réelle (un plan no-op tracé est coché avec la mention `no-op tracé`), et ligne de résultat de phase ajoutée."
  key_links:
    - "Chapeau de `34-RUN-MOBILE.md` <-> case AGTS-02 et item `PROJECT.md` : c'est le SEUL fait qui commande ces deux gestes. La vérification les lit ensemble et échoue sur toute incohérence — un ledger qui coche une exigence que la mesure n'a pas atteinte est exactement le motif « armé sans preuve » que ce milestone ferme."
    - "Chapeau de `34-SPIKE-SKIL.md` <-> case SKIL-01 et clôture de l'item BACKLOG du 2026-06-04 : GO et NO-GO cochent tous deux (un cadrage a été rendu), MESURE INVALIDE ne coche pas et laisse l'item ouvert. Seul NO-GO clôt l'item."
    - "`## Items backlog à créer` de `34-AUDIT-AGTS.md` <-> entrées transcrites dans `.planning/BACKLOG.md` : l'égalité des comptes empêche qu'un gap « à combler » se perde entre la note et le ledger."
    - "Compteurs du frontmatter de `STATE.md` <-> `check-state-integrity.sh` (job `gates` de la CI) : le gate rend rouge si un compteur régresse. Une édition manuelle qui recopierait d'anciennes valeurs, ou un appel à la commande destructive, casserait le fichier de façon visible en CI."
  prohibitions:
    - "Ne JAMAIS utiliser `gsd-tools state record-session` ni aucune commande qui réécrit `.planning/STATE.md` : le fichier est édité à la main, ses sections narratives sont du contenu, pas un cache (ADR-063)."
    - "Ne JAMAIS cocher une case d'exigence sans que la note correspondante porte le verdict qui l'autorise — la cohérence est vérifiée par machine, et un ledger qui devance la preuve est précisément le défaut que ce milestone ferme."
    - "Ne JAMAIS retirer ni amender l'anti-feature « marketplace de skills maison » ni celle sur l'import des personas du catalogue : elles restent gravées quel que soit le verdict."
    - "Ne JAMAIS abandonner AGTS-02 en silence sur run ROUGE : case décochée, mention du report, renvoi nommé à la trace, et déclencheur de reprise daté transcrit au BACKLOG (D-05)."
    - "Ne JAMAIS toucher un fichier hors de `.planning/` dans ce plan — ni `plugin/`, ni `scripts/`, ni `manual/`, ni les README racine."
    - "Ne JAMAIS faire décroître un compteur du frontmatter de `STATE.md`, ni y écrire un nombre recopié : chaque compteur est re-dérivé par comptage réel."
    - "Ne JAMAIS créer d'agent ni importer de persona dans ce plan."
---

<objective>
Transcrire au ledger ce que les trois notes de décision de la phase ont établi — et rien d'autre :
cases d'exigences, items BACKLOG, item actif de `PROJECT.md`, `STATE.md` et `ROADMAP.md`. Chaque
geste est adossé à la note qui le prouve, et la cohérence note ↔ ledger est vérifiée par machine.

Purpose : c'est ici que la phase devient consultable par les phases suivantes. La Phase 40 dépend
de la 34 et la Phase 25 ne se calibre qu'après sa clôture — un ledger incomplet ou en avance sur
les preuves les enverrait toutes deux dans le mur. Et la règle non négociable de ce dépôt tient en
une ligne : aucune case cochée sans preuve écrite.

Output : `.planning/` à jour, `check-state-integrity.sh` vert, aucun fichier hors `.planning/`
touché.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-CONTEXT.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md
@.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md
@CLAUDE.md
</context>

<flagged_assumptions>
Aucun SPEC.md n'existe pour cette phase : la sonde d'arêtes a rendu les trois exigences
`unresolved`/`unclassified`. Les hypothèses de TRANSCRIPTION portées par CE plan sont déclarées ici.

**Les trois exigences — hypothèse signalée (probe : `unresolved` / `unclassified`), volet
« ledger ».** Aucune des trois exigences ne dit dans quel état sa case doit finir selon le verdict
rendu. Ce plan ASSUME la table de correspondance suivante, et l'ÉCRIT dans le ledger à côté de
chaque case :

| Exigence | Case `[x]` si | Case `[ ]` si |
|---|---|---|
| AGTS-01 | `34-AUDIT-AGTS.md` existe avec un verdict par gap et ses preuves manquantes nommées | la note manque ou un gap est sans verdict |
| AGTS-02 | `34-RUN-MOBILE.md` porte `> **Statut** : VERT` ET les modules sont sortis d'expérimental | chapeau ROUGE — reportée AVEC trace, jamais abandonnée |
| SKIL-01 | le verdict est GO **ou** NO-GO (un cadrage a été rendu par écrit) | le verdict est MESURE INVALIDE |

La ligne qui peut surprendre est SKIL-01 : l'exigence demande « un cadrage go/no-go » — un NO-GO
documenté SATISFAIT l'exigence. Si Samuel lit autrement, c'est cette table qui est amendée, et la
correction est locale parce qu'elle est écrite à côté des cases plutôt que sous-entendue.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Tâche 1 : tranche verticale « une note devient du ledger » — BACKLOG transcrit depuis les trois sections Conséquence ledger</name>
  <files>.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha, .planning/BACKLOG.md</files>
  <precondition>Les trois notes de décision existent sur disque : `34-RUN-MOBILE.md`, `34-SPIKE-SKIL.md`, `34-AUDIT-AGTS.md`. Si l'une manque, ce plan ne peut pas s'exécuter : la transcription n'aurait pas de source et le ledger devancerait la preuve.</precondition>
  <read_first>
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-AUDIT-AGTS.md` §§ `## Items backlog à créer` et `## Conséquence ledger` — les textes à coller, déjà rédigés
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-SPIKE-SKIL.md` §§ chapeau de verdict et `## Conséquence ledger` — le texte de clôture (ou de maintien d'ouverture) de l'item du 2026-06-04
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-RUN-MOBILE.md` §§ chapeau de statut et `## Déclencheur de reprise` — le texte de report si le run est ROUGE
    - `.planning/BACKLOG.md` lignes 255-350 — les deux items concernés dans leur état actuel (skill-installer à 258, agency-agents à 303) et le gabarit d'entrée du fichier (`**Capturé :**` / `**Cadrage.**` / `**Pourquoi différé :**` / `**Déclencheur de resurgence :**`)
    - `.planning/BACKLOG.md` lignes 295-301 — l'entrée qui précède, pour copier la structure exacte plutôt que l'approximer
  </read_first>
  <action>
    Transcrire, sans réinterpréter. Les textes ont été rédigés par les plans 34-01 à 34-03
    précisément pour que cette tâche soit une transcription et non une reformulation (ADR-030, une
    seule voix). Cinq gestes.

    (0) **Ancre de périmètre.** Écrire `git rev-parse HEAD` (le SHA courant, à l'entrée de CE plan)
    dans `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha`, une
    ligne, rien d'autre. Ce fichier est l'ancre explicite des diffs de périmètre des trois tâches :
    comparer à `HEAD` nu ne prouverait rien, puisque `HEAD` avance à chaque commit du plan — et les
    plans 34-04 et 34-05 ont, eux, légitimement touché `plugin/` et `manual/` plus tôt dans la
    phase. Une ancre propre à CE plan est la seule qui distingue leurs écritures des nôtres.

    (1) **Item `agency-agents` (entrée du 2026-07-20).** Y ajouter un bloc daté reprenant le verdict
    par gap de `34-AUDIT-AGTS.md`, avec un renvoi nommé à la note plutôt qu'une recopie de la
    matrice entière. Mettre à jour le champ `**Déclencheur de resurgence :**` de l'item pour qu'il
    reflète l'arbitrage rendu.

    (2) **Items « à combler ».** Coller, telle quelle, chaque entrée rédigée dans
    `34-AUDIT-AGTS.md` § `## Items backlog à créer`. Le nombre d'entrées collées doit être égal au
    nombre d'entrées rédigées : la vérification compare les deux comptes. Si la note dit
    explicitement « aucun item à créer », ne rien coller et l'écrire dans le SUMMARY.

    (3) **Item skill-installer (entrée du 2026-06-04).** Si et seulement si `34-SPIKE-SKIL.md`
    porte un chapeau NO-GO : clore l'item en collant le texte de clôture rédigé dans la note, avec
    une ligne commençant par `**Clos le ` suivie de la date. Sur GO : coller l'item de phase
    ultérieure rédigé dans la note et laisser l'item d'origine ouvert avec son renvoi. Sur MESURE
    INVALIDE : laisser l'item ouvert, et coller la mention de ce qu'il faudrait pour re-mesurer.

    (4) **Report AGTS-02, si le run est ROUGE.** Coller le déclencheur de reprise daté rédigé dans
    `34-RUN-MOBILE.md` § `## Déclencheur de reprise`, au gabarit du fichier, avec la cause précise
    et le renvoi nommé à la trace. Sur VERT, ne rien coller ici.
  </action>
  <behavior>
    - Le nombre d'entrées `**Capturé :**` ajoutées au BACKLOG est égal au nombre d'entrées rédigées
      dans `34-AUDIT-AGTS.md` § `## Items backlog à créer`.
    - L'item skill-installer porte une ligne `**Clos le ` si et seulement si le verdict est NO-GO.
    - Un déclencheur de reprise AGTS-02 existe dans le BACKLOG si et seulement si le chapeau du run
      est ROUGE.
    - Les trois notes sont nommément citées dans le BACKLOG — le lecteur peut remonter à la preuve.
  </behavior>
  <acceptance_criteria>
    - `.planning/BACKLOG.md` contient les chaînes `34-AUDIT-AGTS.md` et `34-SPIKE-SKIL.md`.
    - Le nombre d'items ajoutés au BACKLOG depuis les gaps « à combler » est égal au nombre d'items rédigés dans `34-AUDIT-AGTS.md` § `## Items backlog à créer`.
    - Si `34-SPIKE-SKIL.md` porte `> **Verdict** : NO-GO`, alors `.planning/BACKLOG.md` contient une ligne commençant par `**Clos le ` dans un rayon de 40 lignes après le titre `## Skill-installer global (multi-agents)` ; si le verdict est GO ou MESURE INVALIDE, cette ligne est absente de cette zone.
    - Si `34-RUN-MOBILE.md` porte `> **Statut** : ROUGE`, alors `.planning/BACKLOG.md` contient la chaîne `34-RUN-MOBILE.md` ; si le chapeau est VERT, ce n'est pas exigé.
    - Aucun fichier hors `.planning/` n'a été modifié, prouvé par un diff contre l'ancre `34-06-BASE.sha` (plage explicite, jamais `HEAD` nu).
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha` existe, est non vide, et contient un SHA git à 40 caractères hexadécimaux.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE '^[0-9a-f]{40}$' .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha && grep -qF '34-AUDIT-AGTS.md' .planning/BACKLOG.md && grep -qF '34-SPIKE-SKIL.md' .planning/BACKLOG.md && A=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha && test -s "$A" && git diff --quiet "$(cat "$A")" -- plugin scripts manual docs .github README.md README.fr.md VERSION && node -e 'const fs=require("fs");const D=".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/";const audit=fs.readFileSync(D+"34-AUDIT-AGTS.md","utf8");const spike=fs.readFileSync(D+"34-SPIKE-SKIL.md","utf8");const run=fs.readFileSync(D+"34-RUN-MOBILE.md","utf8");const bl=fs.readFileSync(".planning/BACKLOG.md","utf8").split("\n");const AL=audit.split("\n");let s=AL.findIndex(l=>l.startsWith("## Items backlog à créer"));if(s<0){console.log("NOTE SANS SECTION ITEMS");process.exit(1);}let e=AL.slice(s+1).findIndex(l=>/^## /.test(l));e=e<0?AL.length:s+1+e;const attendus=AL.slice(s,e).filter(l=>l.includes("**Capturé :**")).length;const h=bl.findIndex(l=>l.startsWith("## Skill-installer global"));if(h<0){console.log("ITEM SKILL-INSTALLER INTROUVABLE");process.exit(1);}const zone=bl.slice(h,h+40).join("\n");const nogo=/^> \*\*Verdict\*\* : NO-GO$/m.test(spike);const clos=/^\*\*Clos le /m.test(zone);if(nogo!==clos){console.log("CLOTURE INCOHERENTE: verdict NO-GO="+nogo+" mais item clos="+clos);process.exit(1);}const rouge=/^> \*\*Statut\*\* : ROUGE$/m.test(run);if(rouge&&!bl.join("\n").includes("34-RUN-MOBILE.md")){console.log("REPORT AGTS-02 NON TRACE AU LEDGER");process.exit(1);}console.log("items attendus depuis la note: "+attendus);' </automated>
    <fails_when>sortie non nulle, ou l'une des lignes `NOTE SANS SECTION ITEMS`, `ITEM SKILL-INSTALLER INTROUVABLE`, `CLOTURE INCOHERENTE: verdict NO-GO=<b> mais item clos=<b>`, `REPORT AGTS-02 NON TRACE AU LEDGER` apparaît. Causes discriminantes : le BACKLOG ne cite pas les notes (le lecteur ne pourrait pas remonter à la preuve) ; la note d'audit n'a pas sa section d'items, donc rien à transcrire ; l'item skill-installer est clos alors que le verdict n'est pas NO-GO, ou reste ouvert alors qu'il l'est — dans les deux cas le ledger et la note se contredisent ; le run est ROUGE et le BACKLOG ne renvoie nulle part à la trace, c'est-à-dire qu'AGTS-02 serait abandonnée en silence, ce que D-05 interdit ; un fichier hors `.planning/` a été touché.</fails_when>
    <human-check>Relire les deux items du BACKLOG modifiés : le texte collé est-il celui rédigé dans les notes, ou une reformulation qui en change le sens ? Un lecteur qui n'a pas suivi la phase comprend-il, en lisant le seul BACKLOG, ce qui a été tranché et où est la preuve ?</human-check>
  </verify>
  <done>Le BACKLOG porte exactement les conséquences rédigées dans les trois notes, chaque item renvoie à sa preuve, et aucune décision n'a été inventée à la transcription.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : cases d'exigences et item actif — REQUIREMENTS.md et PROJECT.md, cohérents avec les verdicts</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <precondition>La tâche 1 est verte : le BACKLOG est cohérent avec les trois notes.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` lignes 846-852 — la table de traçabilité où `SKIL-01`, `AGTS-01` et `AGTS-02` sont encore `Pending`
    - `.planning/REQUIREMENTS.md` lignes 955-965 — les trois cases à cocher et leur formulation exacte, à ne pas réécrire
    - `.planning/REQUIREMENTS.md` lignes 1090-1096 — les deux anti-features (marketplace de skills maison, import des personas), qui restent gravées quel que soit le verdict : les relire pour ne pas les toucher par inadvertance
    - `.planning/PROJECT.md` autour de la ligne 82 — l'item actif « Sortie du statut expérimental de `mobile-test`(-team) », et le bloc `### Résolved`/`### Active` au-dessus pour le style de migration d'un item
    - Les chapeaux des trois notes (`34-RUN-MOBILE.md`, `34-SPIKE-SKIL.md`, `34-AUDIT-AGTS.md`) — les seuls faits qui autorisent chaque coche
  </read_first>
  <action>
    Cocher ce qui est prouvé, laisser décoché ce qui ne l'est pas, et écrire la règle à côté.

    (1) **AGTS-01.** Cocher si `34-AUDIT-AGTS.md` existe avec un verdict par gap. Ajouter en fin de
    ligne un renvoi nommé à la note. Mettre la ligne correspondante de la table de traçabilité à
    jour (de `Pending` à son état réel, avec le renvoi).

    (2) **AGTS-02.** Cocher SI ET SEULEMENT SI `34-RUN-MOBILE.md` porte un chapeau VERT. Sur ROUGE :
    laisser décochée, et ajouter en fin de ligne la mention du report avec le renvoi nommé à la
    trace et la date — la formulation de l'exigence elle-même prévoit ce cas (« sinon l'exigence est
    reportée avec trace »), donc on l'applique plutôt que de l'interpréter. Table de traçabilité
    mise à jour de la même façon.

    (3) **SKIL-01.** Cocher sur GO comme sur NO-GO — l'exigence demande un cadrage rendu par écrit,
    pas un GO. Laisser décochée sur MESURE INVALIDE. Ajouter le renvoi nommé à
    `34-SPIKE-SKIL.md` et, en une clause, la règle appliquée : c'est elle qui empêchera la question
    de se rejouer de mémoire dans six mois.

    (4) **`PROJECT.md`.** Sur run VERT : cocher l'item actif « Sortie du statut expérimental » et y
    ajouter la date et le renvoi à la trace, dans le style des items soldés déjà présents plus haut
    dans le fichier. Sur ROUGE : le laisser actif et lui ajouter une mention datée du run tenté et
    de sa cause — un item actif enrichi vaut mieux qu'un item actif muet, qui redevient un item qui
    dort.

    (5) **Ne toucher à aucune anti-feature.** Les deux lignes de la section hors périmètre restent
    telles quelles quel que soit le verdict.
  </action>
  <behavior>
    - L'état de chaque case correspond exactement au chapeau de la note qui la prouve.
    - Chaque ligne d'exigence porte un renvoi nommé à sa note.
    - Les trois lignes de la table de traçabilité ne sont plus `Pending`.
    - Les deux anti-features sont inchangées, à l'octet près.
  </behavior>
  <acceptance_criteria>
    - L'état de la case `AGTS-02` dans `.planning/REQUIREMENTS.md` est `[x]` si et seulement si `34-RUN-MOBILE.md` porte `> **Statut** : VERT`.
    - L'état de la case `SKIL-01` est `[x]` si et seulement si `34-SPIKE-SKIL.md` porte `> **Verdict** : GO` ou `> **Verdict** : NO-GO`.
    - Les trois lignes d'exigences citent respectivement `34-AUDIT-AGTS.md`, `34-RUN-MOBILE.md` et `34-SPIKE-SKIL.md`.
    - Aucune des trois lignes de la table de traçabilité (`| SKIL-01 | Phase 34 |`, `| AGTS-01 | Phase 34 |`, `| AGTS-02 | Phase 34 |`) ne contient plus le mot `Pending`.
    - Les deux lignes d'anti-features (marketplace de skills maison, import des personas) sont inchangées par rapport à leur état avant ce plan.
    - L'item de `PROJECT.md` « Sortie du statut expérimental » est coché si et seulement si le chapeau du run est VERT.
    - Aucun fichier hors `.planning/` n'a été modifié.
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha && test -s "$A" && git diff --quiet "$(cat "$A")" -- plugin scripts manual docs .github README.md README.fr.md VERSION && node -e 'const fs=require("fs");const D=".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/";const run=fs.readFileSync(D+"34-RUN-MOBILE.md","utf8");const spike=fs.readFileSync(D+"34-SPIKE-SKIL.md","utf8");const req=fs.readFileSync(".planning/REQUIREMENTS.md","utf8");const proj=fs.readFileSync(".planning/PROJECT.md","utf8");const err=[];const vert=/^> \*\*Statut\*\* : VERT$/m.test(run);const cadre=/^> \*\*Verdict\*\* : (GO|NO-GO)$/m.test(spike);const box=id=>{const m=req.match(new RegExp("^- \\\\[( |x)\\\\] \\\\*\\\\*"+id+"\\\\*\\\\*","m"));return m?m[1]==="x":null;};for(const [id,attendu] of [["AGTS-02",vert],["SKIL-01",cadre]]){const b=box(id);if(b===null)err.push(id+" : ligne de case introuvable");else if(b!==attendu)err.push(id+" : case="+b+" mais verdict attendait "+attendu);}if(box("AGTS-01")===null)err.push("AGTS-01 : ligne de case introuvable");for(const [id,note] of [["AGTS-01","34-AUDIT-AGTS.md"],["AGTS-02","34-RUN-MOBILE.md"],["SKIL-01","34-SPIKE-SKIL.md"]]){const m=req.match(new RegExp("^- \\\\[[ x]\\\\] \\\\*\\\\*"+id+"\\\\*\\\\*.*$","m"));if(!m||!m[0].includes(note))err.push(id+" : la ligne ne renvoie pas a "+note);}for(const id of ["SKIL-01","AGTS-01","AGTS-02"]){const m=req.match(new RegExp("^\\\\| "+id+" \\\\| Phase 34 \\\\|.*$","m"));if(!m)err.push(id+" : ligne de tracabilite introuvable");else if(/Pending/.test(m[0]))err.push(id+" : tracabilite encore Pending");}const pm=proj.match(/^- \[( |x)\] Sortie du statut expérimental.*$/m);if(!pm)err.push("PROJECT.md : item de sortie de statut introuvable");else if((pm[1]==="x")!==vert)err.push("PROJECT.md : item coche="+(pm[1]==="x")+" mais run VERT="+vert);if(err.length){console.log("LEDGER INCOHERENT: "+err.join(" | "));process.exit(1);}' && node -e 'const{execSync}=require("child_process");const fs=require("fs");const base=fs.readFileSync(".planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha","utf8").trim();const avant=execSync("git show "+base+":.planning/REQUIREMENTS.md").toString().split("\n");const apres=fs.readFileSync(".planning/REQUIREMENTS.md","utf8").split("\n");const pick=L=>L.filter(l=>/marketplace de skills maison|Import des 230 personas/.test(l));const a=pick(avant),b=pick(apres);if(!a.length){console.log("ANTI-FEATURE INTROUVABLE DANS LA BASE");process.exit(1);}if(a.length!==b.length||a.some((l,i)=>l!==b[i])){console.log("ANTI-FEATURE MODIFIEE");process.exit(1);}'</automated>
    <fails_when>sortie non nulle, ou la ligne `LEDGER INCOHERENT: <liste des écarts>` apparaît. Causes discriminantes, chacune nommée dans la sortie : une case introuvable (la formulation de la ligne a changé) ; une case cochée alors que le verdict ne l'autorise pas, ou décochée alors qu'il l'autorisait — le cœur du gate, qui empêche le ledger de devancer la preuve ; une ligne d'exigence sans renvoi nommé à sa note ; une ligne de traçabilité encore `Pending` ; l'item de `PROJECT.md` désynchronisé du chapeau du run ; le dernier maillon rend `ANTI-FEATURE MODIFIEE` si l'une des deux lignes d'anti-feature (marketplace de skills maison, import des personas) diffère entre le SHA d'ancre et l'état courant, et `ANTI-FEATURE INTROUVABLE DANS LA BASE` si la comparaison porte sur un ensemble vide — un gate qui compare zéro ligne à zéro ligne serait vert à vide ; un fichier hors `.planning/` a été touché.</fails_when>
  </verify>
  <done>Les trois exigences portent l'état que leurs verdicts autorisent, chacune renvoie à sa preuve, et l'item actif de `PROJECT.md` dit la vérité sur la sortie de statut.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : STATE.md et ROADMAP.md — édition manuelle, compteurs re-dérivés, gate d'intégrité vert</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <precondition>La tâche 2 est verte : les exigences et l'item actif sont cohérents avec les verdicts.</precondition>
  <read_first>
    - `.planning/STATE.md` frontmatter (lignes 1-36) — les champs `current_phase`, `status`, `stopped_at`, `last_updated`, `last_activity`, `last_activity_desc` et le bloc `progress` avec ses quatre compteurs
    - `.planning/STATE.md` § `Current Position`, § `Roadmap Evolution` (première entrée, pour le style et le niveau de détail) et § `Decisions` — les trois sections narratives à enrichir
    - `plugin/conductor/scripts/check-state-integrity.sh` en-tête — les quatre champs dont le gate interdit la régression, et pourquoi un compteur illisible vaut exit 2
    - `.planning/ROADMAP.md` § `### Phase 34` — la ligne `**Plans**` et les cases des six plans posées par le planner
    - `CLAUDE.md` racine § « Traçabilité des arbitrages » — canal et date obligatoires pour les onze décisions du 2026-09-14 et les réponses de checkpoint de cette phase
  </read_first>
  <action>
    Clore la phase dans les deux fichiers de pilotage, à la main.

    (1) **`STATE.md`, frontmatter.** Porter `current_phase` à `34`, `current_phase_name` au nom de
    la phase, `status` à son état réel, et réécrire `stopped_at` et `last_activity_desc` avec ce que
    la phase a RÉELLEMENT produit : les trois verdicts, la sortie (ou non) d'expérimental, la
    construction (ou non) de `web-test-team`. RE-DÉRIVER les quatre compteurs de `progress` par
    comptage réel — nombre de plans de la phase, nombre de SUMMARY présents, nombre de phases
    complètes — et ne JAMAIS faire décroître l'un d'eux. Ne jamais passer par une commande qui
    réécrit le fichier : `.planning/STATE.md` porte du contenu manuel que ce geste détruirait
    (ADR-063).

    (2) **`STATE.md`, sections narratives.** Ajouter une entrée datée à `## Accumulated Context`
    § `Roadmap Evolution` pour la Phase 34 (ce qui a été mesuré, ce qui a été tranché, ce qui reste
    ouvert), enrichir `## Current Position`, et consigner dans `### Decisions` les arbitrages de la
    phase AVEC leur canal et leur date — les onze décisions du cadrage du 2026-09-14 et les réponses
    des deux checkpoints de la phase. Un « arbitrage Samuel » nu a la même forme qu'il soit vrai ou
    fabriqué : c'est le lecteur d'après qui paie.

    (3) **`ROADMAP.md` § `### Phase 34`.** Cocher les cases des plans réellement exécutés ; un plan
    clos en no-op tracé est coché avec la mention `no-op tracé` et sa cause en quelques mots — ni
    laissé vide (indistinguable d'un oubli), ni coché comme s'il avait livré. Ajouter sous la liste
    une ligne de résultat de phase citant les trois notes.

    (4) **Rejouer le gate d'intégrité** dans la forme exacte du job `gates` de la CI :
    `check-state-integrity.sh --file .planning/STATE.md`, avec son `--file` explicite — sans lui, le
    fichier vérifié dépend de l'environnement. Rejouer aussi `check-machine-paths.sh` : ce plan
    écrit de la prose libre dans `.planning/`, et c'est exactement le chemin par lequel des chemins
    absolus de poste se sont accumulés dans ce dépôt par le passé.
  </action>
  <behavior>
    - Aucun compteur du frontmatter de `STATE.md` ne décroît.
    - `STATE.md` porte une entrée datée pour la Phase 34 dans `Roadmap Evolution`.
    - Les arbitrages consignés portent tous un canal et une date.
    - Les six cases de plans du ROADMAP sont cochées, un no-op tracé portant sa mention.
    - `check-state-integrity.sh --file .planning/STATE.md` et `check-machine-paths.sh` passent.
  </behavior>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md` rend 0.
    - `bash scripts/check-machine-paths.sh` rend 0.
    - Aucun des quatre compteurs de `progress` dans le frontmatter de `.planning/STATE.md` n'est inférieur à sa valeur avant ce plan.
    - `.planning/STATE.md` contient la chaîne `Phase 34` dans la section `### Roadmap Evolution`, avec une date au format `AAAA-MM-JJ`.
    - `.planning/STATE.md` contient au moins une ligne de traçabilité d'arbitrage au format `arbitrage Samuel, <canal>, AAAA-MM-JJ`.
    - `.planning/ROADMAP.md` § `### Phase 34` ne contient plus aucune case de plan non cochée (`- [ ] 34-`).
    - `.planning/ROADMAP.md` § `### Phase 34` cite les trois notes `34-AUDIT-AGTS.md`, `34-RUN-MOBILE.md` et `34-SPIKE-SKIL.md`.
    - Aucun fichier hors `.planning/` n'a été modifié.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md && bash scripts/check-machine-paths.sh && A=.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha && test -s "$A" && git diff --quiet "$(cat "$A")" -- plugin scripts manual docs .github README.md README.fr.md VERSION && node -e 'const fs=require("fs");const{execSync}=require("child_process");const err=[];const st=fs.readFileSync(".planning/STATE.md","utf8");const avant=execSync("git show HEAD:.planning/STATE.md").toString();const nums=t=>{const m=t.match(/^progress:\n([\s\S]*?)^[a-z_]+:/m)||t.match(/^progress:\n([\s\S]*?)---/m);const o={};if(m)for(const l of m[1].split("\n")){const k=l.match(/^\s+([a-z_]+):\s*(\d+)/);if(k)o[k[1]]=parseInt(k[2],10);}return o;};const a=nums(avant),b=nums(st);for(const k of Object.keys(a))if(b[k]!==undefined&&b[k]<a[k])err.push("compteur "+k+" regresse "+a[k]+" -> "+b[k]);const i=st.indexOf("### Roadmap Evolution");if(i<0)err.push("STATE.md : section Roadmap Evolution introuvable");else{const z=st.slice(i,i+12000);if(!/Phase 34/.test(z)||!/\d{4}-\d{2}-\d{2}/.test(z))err.push("STATE.md : pas d entree datee pour la Phase 34");}if(!/arbitrage Samuel, .*, \d{4}-\d{2}-\d{2}/.test(st))err.push("STATE.md : aucune tracabilite d arbitrage au format canal + date");const rm=fs.readFileSync(".planning/ROADMAP.md","utf8");const h=rm.indexOf("### Phase 34");if(h<0)err.push("ROADMAP.md : section Phase 34 introuvable");else{let e=rm.indexOf("\n### ",h+5);e=e<0?rm.length:e;const z=rm.slice(h,e);if(/- \[ \] 34-/.test(z))err.push("ROADMAP.md : au moins une case de plan reste non cochee");for(const n of ["34-AUDIT-AGTS.md","34-RUN-MOBILE.md","34-SPIKE-SKIL.md"])if(!z.includes(n))err.push("ROADMAP.md : la section Phase 34 ne cite pas "+n);}if(err.length){console.log("CLOTURE INCOHERENTE: "+err.join(" | "));process.exit(1);}'</automated>
    <fails_when>sortie non nulle, ou la ligne `CLOTURE INCOHERENTE: <liste des écarts>` apparaît. Causes discriminantes, chacune nommée dans la sortie : un compteur de `progress` a régressé par rapport à sa valeur dans `HEAD` (symptôme classique d'une réécriture destructive du fichier, ou d'un nombre recopié) ; pas d'entrée datée pour la Phase 34 dans `Roadmap Evolution`, donc la phase ne serait pas consultable par les phases suivantes ; aucune traçabilité d'arbitrage au format canal + date, alors que la phase repose sur onze décisions humaines et deux réponses de checkpoint ; une case de plan reste non cochée dans la section Phase 34 du ROADMAP (indistinguable d'un oubli — un no-op tracé se coche AVEC sa mention) ; la section ne cite pas l'une des trois notes ; `check-state-integrity.sh` ou `check-machine-paths.sh` rendent non-zéro ; un fichier hors `.planning/` a été touché.</fails_when>
    <human-check>Relire le `stopped_at` et l'entrée `Roadmap Evolution` : quelqu'un qui reprend le projet dans trois mois, sans contexte, sait-il ce que la Phase 34 a mesuré, ce qu'elle a tranché, et ce qui reste ouvert — sans avoir à ouvrir les trois notes ?</human-check>
  </verify>
  <done>La phase est close dans les deux fichiers de pilotage, ses arbitrages sont traçables au canal et à la date, les compteurs sont re-dérivés sans régression, et les deux gates concernés passent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notes de décision de la phase → ledger du projet | le ledger est ce que les phases suivantes LISENT ; une transcription qui reformule ou devance les verdicts propage une erreur à la Phase 40 et à la Phase 25, qui en dépendent toutes deux |
| Édition manuelle de `STATE.md` → gate d'intégrité de la CI | `STATE.md` porte à la fois un frontmatter machine et de la prose ; l'outil de réécriture disponible détruirait la seconde, d'où l'interdiction explicite et le gate qui attrape les régressions de compteurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-22 | Spoofing | case d'exigence cochée sans la preuve qui l'autorise | high | mitigate | la vérification de la tâche 2 lit les chapeaux des trois notes et l'état des trois cases, et échoue en NOMMANT l'écart — une case ne peut pas devancer son verdict ; l'item de `PROJECT.md` est soumis au même test |
| T-34-23 | Tampering | `.planning/STATE.md` réécrit par un outil qui en perd la prose | high | mitigate | interdiction explicite de la commande destructive en prohibition de plan (ADR-063) ; la vérification compare les compteurs de `progress` à ceux de `HEAD` et échoue sur toute régression, symptôme direct d'une réécriture ; `check-state-integrity.sh --file` est rejoué dans la forme du job CI |
| T-34-24 | Repudiation | arbitrage humain consigné sans canal ni date | medium | mitigate | la vérification exige au moins une ligne au format `arbitrage Samuel, <canal>, AAAA-MM-JJ` dans `STATE.md` ; règle née dans ce dépôt d'un commit dont personne ne pouvait vérifier l'attribution |
| T-34-25 | Information Disclosure | chemin absolu de poste écrit dans de la prose libre sous `.planning/` | medium | mitigate | `check-machine-paths.sh` est rejoué dans la vérification de la tâche 3 ; c'est exactement par ce chemin que 19 fichiers suivis en avaient accumulé, dont 18 déjà publiés |
| T-34-26 | Tampering | anti-feature retirée ou amendée par inadvertance | medium | mitigate | la vérification de la tâche 2 échoue si le diff de `REQUIREMENTS.md` touche la ligne d'anti-feature sur la marketplace de skills maison — elle reste gravée quel que soit le verdict de SKIL-01 |

Aucun install de paquet n'entre dans ce plan : édition de fichiers markdown de pilotage uniquement.
Aucune menace `T-34-SC` n'est ouverte.
</threat_model>

<verification>
1. `.planning/BACKLOG.md` cite les trois notes ; l'item skill-installer est clos si et seulement si le verdict est NO-GO ; le déclencheur de reprise AGTS-02 y figure si et seulement si le run est ROUGE.
2. Le nombre d'items « à combler » transcrits est égal au nombre rédigé dans `34-AUDIT-AGTS.md`.
3. Les trois cases d'exigences portent l'état que leur verdict autorise, chacune renvoie nommément à sa note, et aucune ligne de la table de traçabilité n'est plus `Pending`.
4. L'item « Sortie du statut expérimental » de `PROJECT.md` est coché si et seulement si le chapeau du run est VERT.
5. Les deux anti-features sont inchangées.
6. `bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md` rend 0 et aucun compteur de `progress` n'a régressé par rapport à `HEAD`.
7. `bash scripts/check-machine-paths.sh` rend 0.
8. La section `### Phase 34` du ROADMAP n'a plus de case de plan non cochée et cite les trois notes.
9. `git diff --quiet $(cat .planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha) -- plugin scripts manual docs .github README.md README.fr.md VERSION` rend 0 à chaque tâche — ancre explicite propre au plan, jamais `HEAD` nu : ce plan ne touche que `.planning/`.
10. `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-SUMMARY.md` existe et consigne littéralement les mesures ci-dessus.
</verification>

<success_criteria>
- Le ledger dit exactement ce que les preuves établissent : aucune case ne devance son verdict, aucune n'est en retard dessus.
- AGTS-02 sur run rouge est reportée AVEC sa trace et son déclencheur de reprise daté — jamais abandonnée en silence.
- SKIL-01 est close sur un cadrage rendu (GO comme NO-GO), ouverte sur une mesure invalide, et la règle appliquée est écrite à côté de la case.
- `STATE.md` reste lisible par un humain et vert pour la machine, sans avoir été réécrit par un outil destructif.
- Les arbitrages de la phase portent leur canal et leur date — vérifiables, pas seulement affirmés.
- La Phase 40 et la Phase 25 peuvent lire le ledger et savoir ce qui est verrouillé, ce qui est reporté, et sur quelle preuve.
</success_criteria>

## Artifacts this phase produces (périmètre de CE plan)

| Artefact | Nature | Chemin |
|---|---|---|
| Item `agency-agents` mis à jour + items « à combler » datés | édition de ledger | `.planning/BACKLOG.md` |
| Item skill-installer clos (NO-GO) ou maintenu ouvert | édition de ledger | `.planning/BACKLOG.md` |
| Déclencheur de reprise AGTS-02 (run ROUGE) | édition de ledger | `.planning/BACKLOG.md` |
| Cases `AGTS-01` / `AGTS-02` / `SKIL-01` + trois lignes de traçabilité | édition de ledger | `.planning/REQUIREMENTS.md` |
| Item actif « Sortie du statut expérimental » | édition de ledger | `.planning/PROJECT.md` |
| Frontmatter (compteurs re-dérivés) + `Roadmap Evolution` + `Current Position` + `Decisions` | édition manuelle | `.planning/STATE.md` |
| Cases des six plans + ligne de résultat de la Phase 34 | édition | `.planning/ROADMAP.md` |
| `34-06-BASE.sha` | ancre de périmètre (une ligne : le SHA d'entrée du plan) — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-BASE.sha` |
| `34-06-SUMMARY.md` | résumé d'exécution — NOUVEAU | `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-SUMMARY.md` |

Aucun symbole de code, aucun agent, aucun module n'est produit par ce plan.

<output>
Créer `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-SUMMARY.md` en fin
de plan, consignant : les trois chapeaux de verdict tels que lus, l'état final de chaque case avec
la règle appliquée, le nombre d'items transcrits au BACKLOG comparé au nombre rédigé, les valeurs
avant/après des quatre compteurs de `progress`, la sortie de `check-state-integrity.sh` et de
`check-machine-paths.sh`, la liste des arbitrages consignés avec leur canal et leur date, et tout
écart constaté entre ce plan et l'état réel des fichiers au moment de l'exécution (notamment si les
numéros de ligne cités ont bougé).
</output>

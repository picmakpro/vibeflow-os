---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/check-artifact-fidelity.sh
  - plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
  - README.md
  - README.fr.md
autonomous: true
requirements: [FIDE-01, FIDE-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Pour un artefact VibeFlow réel et une cible mesurée (Codex, tier-1), un opérateur voit EXACTEMENT quels champs (model/memory/tools/disallowedTools/vf-internal/description) sont préservés, dégradés ou perdus à la conversion — jamais deviné, toujours dérivé d'une exécution réelle de la conversion gsd-core"
    - "L'install (vibeflow-update.sh install/update) affiche ce périmètre à la fin de la pose de chaque module — jamais dans un rapport séparé qu'on ne relit pas"
    - "Sur Codex, le rapport est VIDE (rien perdu, mesuré) ou ASSUMÉ (perte nommée noir sur blanc) — jamais silencieux : ni erreur avalée, ni cas non couvert qui retomberait sur un exit 0 muet"
    - "Quand gsd-core est introuvable (poste sans le moteur), le gate le dit bruyamment (exit non-zéro, stderr explicite) au lieu de rendre un rapport 'rien perdu' mensonger"
    - "Ciblant Codex, le rapport déclare aussi, comme CHAMPS DE RECETTE (jamais un détail enfoui) : l'état de `multi_agent_v2` (sans lui aucun outil de spawn n'existe — un lab VibeFlow «marche» sur Codex sans le moindre sous-agent, en silence) et l'état de `trust_level` du dépôt cible (sans `trusted`, `.codex/agents/` n'est jamais parsé — zéro rôle VibeFlow chargé, en silence, alors que `codex doctor` continue de rendre `overall: ok`) — mesurés depuis 38-CONTEXT.md lignes 445-461, jamais un chiffre recopié"
  artifacts:
    - "plugin/conductor/scripts/check-artifact-fidelity.sh : gate exécutable, lecture seule, prend un artefact source + une cible, invoque la VRAIE fonction de conversion gsd-core, jamais une table recopiée à la main"
    - "plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh : suite dédiée neuve — 69e suite découverte par `find plugin scripts -type f -path '*/tests/test-*.sh'`"
  key_links:
    - "check-artifact-fidelity.sh -> ~/.claude/gsd-core/bin/lib/runtime-artifact-conversion.cjs (convertClaudeAgentToCodexAgent, ligne 2451) : SEULE source de la conversion réelle pour la cible Codex"
    - "vibeflow-update.sh install_module()/update_module() -> check-artifact-fidelity.sh : appelé après mark_installed (ligne 1639), sortie relayée sur stdout de l'install, jamais avalée par un `2>/dev/null || true`"
    - "check-version-sync.sh (ligne 126) -> README.md/README.fr.md : le compteur `N suites` DOIT refléter la suite neuve dans le MÊME plan que sa création — piège de gate mesuré au cadrage"
  prohibitions:
    - "Ne jamais coder en dur une liste de champs 'perdus' par cible sans les avoir mesurés par exécution réelle de la conversion gsd-core sur un artefact réel de ce dépôt"
    - "Ne jamais transformer un résultat 'non mesuré' (OpenCode, kimi-code — absents du poste, cf. 38-CONTEXT.md) en un silence : le gate se limite à Codex dans ce plan et le dit explicitement, il n'invente pas de résultat pour les deux autres"
    - "Ne pas corriger la conversion gsd-core elle-même (surface interne, hors périmètre — VibeFlow consomme, ne réimplémente pas, doctrine Phase 37) : ce gate CONSTATE l'écart, il ne le comble pas"
---

<objective>
Poser la première brique du cadrage 38 (38-CONTEXT.md, lot 1) : un gate de fidélité qui mesure,
par exécution réelle de la conversion `gsd-core`, ce qu'une install multi-runtime de VibeFlow perd
réellement — jamais un chiffre recopié depuis une étude. Sans cette brique, activer l'installeur
multi-runtime du lot 2 livrerait exactement la dégradation silencieuse que la Phase 37 a chiffrée
(156 conversions mesurées, 0 diagnostic à l'époque).

Purpose: honorer le critère de « fini » du cadrage — « ce qui est perdu sur une cible est déclaré
à l'install, jamais subi » — et fournir le point d'ancrage que les lots 5 (ADPT) et 6 (MIGR)
citent déjà (FIDE au `status`, per D-38-B point 3 et MIGR-05). Le rapport déclare aussi deux
préconditions d'install mesurées en session Codex réelle (38-CONTEXT.md lignes 445-461) :
`multi_agent_v2` (sans lui, aucun outil de spawn n'existe) et `trust_level` (sans lui, `.codex/
agents/` n'est jamais parsé) — deux dégradations 100% silencieuses sans ce gate.

Output: `check-artifact-fidelity.sh` (gate exécutable + suite dédiée), banni d'install qui
l'invoque à chaque `install`/`update`, README synchronisés sur la 69e suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/_internal/vibeflow-update.sh
@plugin/dev-orchestrator/scripts/check-gsd-engine.sh
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : bout-en-bout — un artefact réel, une cible mesurée, un rapport de fidélité réel (FIDE-01)</name>
  <files>plugin/conductor/scripts/check-artifact-fidelity.sh, plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 266-402 (baseline mesurée du 2026-08-28 : les 4 surfaces Codex A-D lignes 273-291, les 3 pièges de dégradation silencieuse lignes 304-317, la doctrine de pose/désinstallation lignes 356-370) — c'est la source des verdicts que le gate doit produire, jamais une réinvention
    - ~/.claude/gsd-core/bin/lib/runtime-artifact-conversion.cjs:2446-2466 (`convertClaudeAgentToCodexAgent` : préserve `name`+`description` dans un frontmatter reconstruit à deux clés, embarque `tools` en PROSE dans un bloc `<codex_agent_role>` non-enforçable, abandonne silencieusement `model`/`memory`/`effort`/`disallowedTools`/`vf-internal`) — CETTE fonction est la seule source de vérité de la conversion, ne jamais réécrire son comportement en dur dans le gate
    - ~/.claude/gsd-core/bin/lib/runtime-artifact-layout.cjs (confirmé par recherche : aucune occurrence de "toml" côté agent-role Codex — gsd-core ne produit PAS le fichier `.toml` sous `$CODEX_HOME/agents/**/*.toml` que 38-CONTEXT.md ligne 277 mesure comme la SEULE surface réellement dispatchable — c'est exactement l'écart que ce gate doit rendre visible, jamais le combler)
    - plugin/dev-orchestrator/scripts/check-gsd-engine.sh:60-70 (`default_gsd_home_new()` : la cascade de résolution du home gsd-core à dupliquer À L'IDENTIQUE — jamais un nouveau mécanisme de résolution, jamais un `source` du script d'autrui à effets de bord)
    - plugin/content-bundle/agents/content-clarity-judge.md (artefact source réel du test : porte les 6 champs `name`/`description`/`tools`/`disallowedTools`/`model`/`effort`/`memory`/`vf-internal` — cas d'école d'un juge à barrière d'écriture, exactement le profil que la conversion Codex dégrade le plus)
    - plugin/_internal/tests/test-vibeflow-update.sh:1-30 (convention de suite du dépôt à reproduire à l'identique : `set -uo pipefail` sans `-e`, `mktemp -d` + `trap`, helpers `ok()`/`ko()`/`skip()`, asserts numérotés `T<n>.<m>`, bannière de résultat)
    - 38-CONTEXT.md lignes 445-461 (verdicts #4 et #1 de la sonde en session Codex RÉELLE, mesurés le 2026-08-28 — pas le banc isolé : `multi_agent_v2` est une PRÉCONDITION DURE de spawn, 100% silencieuse à l'échec ; le `trust_level` du dépôt gate `.codex/agents/` en TOUT OU RIEN, silencieusement, avec une asymétrie mesurée envers les skills qui, elles, se chargent SANS trust) — ce sont les deux verdicts que ce gate doit rendre en CHAMPS DE RECETTE, jamais en detail de bas de rapport
  </read_first>
  <action>
Créer `plugin/conductor/scripts/check-artifact-fidelity.sh`, lecture seule, usage
`check-artifact-fidelity.sh [--target codex] [--json] <artefact.md>`. Défaut `--target codex`
(seule cible tier-1 mesurée au 2026-08-28) ; tout autre argument de cible affiche
« non mesuré sur ce poste » sur stdout et sort en 3 (indéterminé), jamais un rapport inventé pour
OpenCode/kimi-code.

Résolution du home gsd-core : dupliquer EXACTEMENT la cascade `default_gsd_home_new()` de
`check-gsd-engine.sh` (root/.claude/gsd-core, sinon `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core`)
— même motif D-01 déjà justifié là-bas (gate testable en boîte noire, jamais de `source` à effets
de bord). Si ni le fichier `VERSION` ni `bin/lib/runtime-artifact-conversion.cjs` n'existent sous ce
home : erreur bruyante sur stderr (« gsd-core introuvable — impossible de mesurer la conversion »),
sortie 3, stdout VIDE — jamais un rapport « rien perdu » qui mentirait par absence de mesure.

Extraction du frontmatter source : découper l'artefact en deux blocs par les délimiteurs `---`
(motif déjà utilisé par ce dépôt), lire les valeurs des 6 clés candidates
(`name`, `description`, `model`, `memory`, `disallowedTools`, `vf-internal`) par simple
`grep -E '^clé:'` sur le bloc frontmatter — extraction bash pure, indépendante de gsd-core, pour
que le DIFF lui-même reste auditable sans dépendre de la boîte noire qu'il mesure.

Conversion réelle : invoquer `node -e` chargeant
`require(path.join(gsdHome,'bin/lib/runtime-artifact-conversion.cjs')).convertClaudeAgentToCodexAgent`
sur le contenu du fichier source lu par `fs.readFileSync`, stdout capturé dans un fichier temporaire
sous le `mktemp -d` du gate (jamais dans le répertoire de l'artefact source). Répéter la même
extraction de frontmatter (6 clés) sur cette sortie convertie.

Verdict par champ, un par ligne :
- `name`, `description` : présents des deux côtés → PRESERVED.
- `model`, `memory`, `disallowedTools`, `vf-internal` : présents côté source, absents côté converti
  → LOST.
- `tools` : traiter à part — chercher la valeur source dans le corps `<codex_agent_role>` de la
  sortie convertie (`grep -F` sur la valeur exacte) ; trouvée en PROSE non-frontmatter → DEGRADED
  (« présent en prose, non enforçable ») ; absente aussi de la prose → LOST.
- Champ absent côté source dès le départ : ne pas le lister (le gate rapporte des PERTES, pas
  l'inventaire complet du schéma).

Marqueurs morts : sur le CORPS (hors frontmatter) des deux versions, compter les occurrences de
`\.claude/` et de `Task(` par `grep -c`. Rendre le compte des deux côtés ; si le compte côté
converti est ÉGAL au compte côté source (aucune réécriture), le rapporter comme
`DEAD_MARKERS=<n> (non réécrits)`. Ne jamais présumer d'une réécriture qui n'a pas été mesurée.

Recette d'environnement Codex (uniquement `--target codex`, AVANT la ligne `[fidelity]` — c'est la
première chose qu'un opérateur pressé doit voir) : deux mesures indépendantes de la conversion
d'artefact, jamais dérivées de l'artefact lui-même.
- `multi_agent_v2` : si le binaire `codex` est sur le PATH, exécuter `codex features list` (ou
  l'équivalent JSON s'il existe dans cette CLI — vérifier au moment de l'implémentation, ne jamais
  supposer un flag qui n'existe pas), chercher la ligne `multi_agent_v2` et en extraire `true`/
  `false`. `codex` absent → `non mesurable (CLI codex introuvable)`, jamais une valeur par défaut.
- `trust_level` : résoudre la racine du dépôt cible (`git rev-parse --show-toplevel`), lire
  `${CODEX_HOME:-$HOME/.codex}/config.toml`, chercher un bloc `[projects."<racine>"]` portant
  `trust_level = "trusted"`. Fichier absent ou bloc absent → `absent (non trusted)`. Lecture seule,
  jamais d'écriture dans `config.toml` (doctrine mesurée en 38-CONTEXT.md : aucune commande
  `codex config` n'existe, toute écriture manuelle risquerait de mutiler des tables voisines).
Sortie dédiée, une ligne, AVANT le rapport de fidélité : `[fidelity-recette] multi_agent_v2=<true|
false|non mesurable> trust_level=<trusted|absent (non trusted)|non mesurable>`. Ces deux champs ne
sont JAMAIS omis quand `--target codex` (contrairement aux champs de PRESERVED/DEGRADED/LOST qui
peuvent être vides) : ce sont des préconditions d'install, pas des détails d'artefact.

Sortie par défaut (texte), une ligne : `[fidelity] <artefact> -> <target>: PRESERVED={...}
DEGRADED={...} LOST={...} DEAD_MARKERS=<n>`. Avec `--json`, la même donnée en JSON à plat (clés
`preserved`/`degraded`/`lost`/`dead_markers`/`target`/`artifact`/`multi_agent_v2`/`trust_level`),
générée par un second appel `node -e` (JSON.stringify d'un objet construit depuis les mêmes
verdicts déjà calculés — jamais une sérialisation manuelle en bash). Exit 0 dans tous les cas où la
mesure a pu s'exécuter, MÊME si des champs sont LOST ou que `multi_agent_v2`/`trust_level` sont
défavorables : une perte ou une précondition manquante déclarée n'est pas un échec du gate, c'est
son objet. Exit 2 sur argument inconnu, exit 3 sur gsd-core introuvable ou artefact source
introuvable.

Créer `plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh` sur le patron exact de
`test-vibeflow-update.sh` (mktemp -d + trap, ok/ko/skip, asserts `T<n>.<m>`, bannière de résultat).
Cas couverts : T1 (les 4 pertes réelles + 1 dégradation sur `content-clarity-judge.md` copié en
fixture isolée), T2 (gsd-core absent → exit 3, stdout vide), T3 (`--json` produit un JSON valide,
vérifié par `node -e 'JSON.parse(...)'`), T4 (cible inconnue → exit 3, message « non mesuré »),
T5 (`--target codex` sans binaire `codex` sur un `PATH` de sonde restreint → `multi_agent_v2=non
mesurable`, jamais une valeur par défaut), T6 (`--target codex` avec un `CODEX_HOME` de sonde ne
portant aucun bloc `[projects."…"]` pour la racine testée → `trust_level=absent (non trusted)`,
et la ligne `[fidelity-recette]` apparaît AVANT la ligne `[fidelity]` dans la sortie capturée).
  </action>
  <acceptance_criteria>
    - Le fichier existe et est exécutable : `[ -x plugin/conductor/scripts/check-artifact-fidelity.sh ]`.
    - Exécuté sur la fixture `content-clarity-judge.md` avec `--target codex`, la sortie contient bien `model` et `disallowedTools` et `vf-internal` dans le groupe `LOST=` — le vérifier par grep sur la sortie réelle capturée en variable, jamais recopié en dur dans l'assertion sans avoir exécuté la commande.
    - `model` étant dans `LOST=`, `name` et `description` sont dans `PRESERVED=` sur la même exécution.
    - Sur un `GSD_HOME`/`HOME` de sonde ne contenant AUCUN `gsd-core`, la commande sort en 3 et stdout est vide (`[ -z "$(... 2>/dev/null)" ]`).
    - `--json` produit une sortie qui parse : `node -e "JSON.parse(require('fs').readFileSync(process.argv[1],'utf8'))" <(commande --json ...)` sort 0.
    - Exécuté avec `--target codex`, la sortie texte contient une ligne `[fidelity-recette]` portant à la fois `multi_agent_v2=` et `trust_level=` — vérifiée par grep sur la sortie réelle, jamais recopiée en dur — et cette ligne précède la ligne `[fidelity]` dans le flux capturé.
    - Sur un `CODEX_HOME`/`PATH` de sonde sans `codex` ni bloc `[projects.…]`, `multi_agent_v2` rend `non mesurable` et `trust_level` rend `absent (non trusted)` — jamais une valeur par défaut favorable.
    - `bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh` sort 0, bannière `== résultat :` affichée, 0 KO.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh</automated>
  </verify>
  <done>Le gate mesure réellement, par exécution de la conversion gsd-core, la perte de fidélité d'un artefact VibeFlow réel vers Codex — sans jamais recopier un chiffre d'étude — déclare `multi_agent_v2` et `trust_level` comme champs de recette de premier plan, et sa suite dédiée est verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : bannière d'install + non-régression + synchro README (FIDE-02)</name>
  <files>plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh, plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md, README.md, README.fr.md</files>
  <read_first>
    - plugin/_internal/vibeflow-update.sh:1630-1646 (fin de `install_module()` : `mark_installed "$mod" "$version"` en 1639, puis le bloc de compte-rendu `VF_DEGRADED_COPIES_COUNT` et la ligne de succès finale `log "✓ $mod $version installé"` — c'est APRÈS cette ligne que la bannière de fidélité s'accroche, jamais avant : la pose doit être terminée avant qu'on rapporte sur elle)
    - plugin/_internal/vibeflow-update.sh:2108-2127 (`update_module()` : même point de couture après `vf_converge_apply "$mod"`)
    - scripts/check-version-sync.sh:123-131 (le gate qui compte `N suites` dans les deux README contre `find plugin scripts -path '*/tests/test-*.sh' | grep -c .` — la suite créée en tâche 1 fait passer ce compte de 68 à 69, à répercuter dans CE plan, jamais un plan séparé)
    - README.md:110 (« 17 modules, each versioned... ») et README.md:124 (« 68 suites in CI ») — README.fr.md:114 et :128, mêmes phrases en français — les DEUX lignes à corriger dans les DEUX fichiers
    - plugin/conductor/CHANGELOG.md (en-tête, format des entrées existantes — le module `conductor` porte le nouveau script, son VERSION/CHANGELOG bump suit la convention CLAUDE.md racine)
  </read_first>
  <behavior>
    - Test : `install_module` d'un module qui EXISTE côté `plugin/content-bundle` (a un `AGENT.md` ou `SKILL.md` réel) produit, sur son stdout, une ligne `[fidelity]` — jamais absente, jamais routée vers `/dev/null`.
    - Test : si `check-artifact-fidelity.sh` est absent (poste où `conductor` n'est pas encore posé — cas réel D-00 déjà documenté dans `vf-update/SKILL.md`), l'install continue SANS échouer (best-effort, même doctrine que `find_hooks_merger` pour les hooks — mais ici l'absence est SILENCIEUSE côté ce module précis, puisque `conductor` est mandatory et l'engine lui-même vit dans `_internal`, cas frontière à documenter dans le SUMMARY plutôt qu'à deviner).
  </behavior>
  <action>
Dans `vibeflow-update.sh`, juste après la ligne de succès `log "✓ $mod $version installé"` de
`install_module()` (fin de la fonction, après 1639) et au point symétrique de `update_module()`
(après `vf_converge_apply "$mod"`), ajouter un appel best-effort au gate de la tâche 1 : résoudre
son chemin par la MÊME cascade à trois positions déjà écrite dans `vf-update/SKILL.md`
(`$TARGET_ROOT/scripts/` → `$(dirname "$0")` → `$CACHE_DIR/_internal/`), l'invoquer UNIQUEMENT si
le module posé a produit un artefact `.md` avec frontmatter (`AGENT.md` ou `agents/*.md`) sous
`$TARGET_ROOT`, cible `codex` par défaut. Relayer sa sortie TELLE QUELLE sur le stdout de l'install
(jamais capturée puis résumée — la ligne `[fidelity]` doit apparaître verbatim). Script introuvable
→ silence total, mêmes garde-fous que la sonde `<S-moteur>` (jamais de dégradation du reste du
diagnostic d'install pour un module qui n'a pas d'agent, ex. un module skill-only).

Bump `plugin/conductor/VERSION` (minor — nouvelle capacité, le gate de fidélité) et ajouter
l'entrée `CHANGELOG.md` correspondante, format identique aux entrées existantes du module.

Corriger les deux occurrences « 68 suites » (README.md:124, README.fr.md:128) en « 69 suites » —
littéral exact, pas de gabarit. Ne PAS toucher aux lignes « 17 modules » (aucun module neuf posé
par ce plan, `check-artifact-fidelity.sh` vit DANS le module `conductor` existant).

Rejouer la découverte COMPLÈTE des suites (`find plugin scripts -type f -path '*/tests/test-*.sh'`,
jamais un sous-ensemble) et la boucle d'exécution identique à `ci.yml:227-242`, pour confirmer
69/69 vertes avant de committer.
  </action>
  <acceptance_criteria>
    - `grep -n '\[fidelity\]' plugin/_internal/vibeflow-update.sh | wc -l` rend au moins `2` (les deux points de couture install + update).
    - Une install réelle d'un module à `AGENT.md` (ex. `content-bundle`) dans un `TARGET_ROOT` de sonde `mktemp -d` produit une ligne `[fidelity]` sur stdout — capturer la sortie réelle et grep dessus, ne jamais affirmer sans avoir exécuté.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` rend `69`, et ce chiffre littéral apparaît dans README.md ET README.fr.md (`grep -o '[0-9][0-9]* suites' README.md` = `69 suites`, idem `.fr.md`).
    - `bash scripts/check-version-sync.sh` sort 0 (le gate qui a servi de garde-fou dans le cadrage).
    - Rejeu complet : boucle sur les 69 suites, 0 échec (consigner la commande exacte et son résultat dans le SUMMARY, jamais un sous-ensemble).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh</automated>
  </verify>
  <done>Chaque install/update VibeFlow affiche sa fidélité réelle sur Codex dans sa propre sortie, les deux README comptent 69 suites, et la découverte complète des suites reste 100% verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefact source (repo) -> conversion gsd-core (dépendance npm tierce) | le gate lit une sortie produite par du code hors du contrôle de ce dépôt ; il ne doit jamais la faire passer pour un fait vérifié sans l'avoir exécutée dans ce run |
| sortie du gate -> stdout de l'install | la ligne `[fidelity]` est la SEULE surface qu'un opérateur pressé lira ; une perte tue à cet endroit reproduit exactement le défaut que ce lot corrige |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Information Disclosure | check-artifact-fidelity.sh (node -e sur contenu d'artefact) | low | accept | le contenu lu est du markdown de ce dépôt (jamais un secret utilisateur), aucune donnée n'est envoyée hors du poste |
| T-38-02 | Repudiation | ligne `[fidelity]` avalée par un `2>/dev/null` ou une capture silencieuse ailleurs dans le pipeline d'install | high | mitigate | acceptance criteria dédié (grep sur stdout réel d'une install de sonde) + relais VERBATIM jamais résumé |
| T-38-03 | Tampering | gsd-core introuvable rendant un rapport 'rien perdu' par défaut au lieu d'un échec bruyant | high | mitigate | exit 3 + stdout vide explicitement testé (T2 de la suite) — jamais un vert sur absence de mesure |
| T-38-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installé par ce plan — aucun gate de légitimité applicable |
</threat_model>

<verification>
1. `bash plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh` sort 0.
2. Une install de sonde produit une ligne `[fidelity]` visible sur stdout, capturée et grep dans le SUMMARY.
3. `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` = 69, et les deux README affichent ce chiffre.
4. `bash scripts/check-version-sync.sh` sort 0.
5. Rejeu complet des 69 suites, 0 échec, commande et résultat consignés dans le SUMMARY.
</verification>

<success_criteria>
- Un artefact VibeFlow réel converti vers Codex rend un verdict par champ mesuré par exécution réelle, jamais halluciné.
- Chaque install/update affiche ce verdict dans sa propre sortie, jamais enfoui.
- Les deux README et le gate de synchro version restent alignés sur 69 suites.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-01-SUMMARY.md` avec : la sortie réelle du gate sur la fixture (les 4 pertes + 1 dégradation), la ligne `[fidelity]` capturée sur une install de sonde, et le résultat du rejeu complet des 69 suites.
</output>

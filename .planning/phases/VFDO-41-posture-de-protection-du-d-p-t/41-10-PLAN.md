---
phase: 41-posture-de-protection-du-d-p-t
plan: 10
lot: L10 — preuves reportées dans la doctrine : statut O-3, BACKLOG traité par renvoi, ADR-072 § Mesures
type: execute
wave: 10
depends_on: ["41-09"]
autonomous: true
requirements: [PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  - .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md
  - .planning/BACKLOG.md
  - docs/ADR.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Précondition : chaque mesure est conforme ou couverte par une décision humaine de poursuite consignée avec son canal et sa date (`M2`, `CO`, `M1`, `M3`, `D03`, `M4`) ; aucune PR ouverte ; B à jour d'`origin/main`"
    - "O-3 du `25-SECURITY.md` reçoit une ligne de statut datée « gardée par défaut + tracée » renvoyant à `41-PREUVES.md` et ADR-072 ; le texte d'origine d'O-3 reste intact ; aucune proposition d'aucun artefact de la phase (25-SECURITY, ADR.md, BACKLOG, CLAUDE.md, SUMMARY) n'associe O-3 à un mot d'achèvement, même nié (D-02, point 5 du tour 1)"
    - "L'item BACKLOG « Posture de protection de `main` » reçoit une ligne « TRAITÉ par renvoi » (Phase 41, ADR-072) sans qu'aucune de ses lignes existantes ne soit modifiée (D-M10)"
    - "ADR-072 § « Mesures à la pose » recopie chaque résultat consigné — M-1, M-2, M-3, provenance serveur (REST) et client, revue code owner par chemin, trace (ii) règle par règle, push direct et sa provenance, M-4, accès aux rule suites — et chaque décision humaine consignée avec son canal et sa date ; il reprend la nuance « contournement réel sur des checks en cours, pas sur une PR rouge » ; jamais une valeur absente de `41-PREUVES.md`"
    - "Commits de plan citant D-01..D-10 : « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »"
  artifacts:
    - path: ".planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md"
      provides: "statut daté d'O-3"
    - path: "docs/ADR.md"
      provides: "ADR-072 § Mesures à la pose"
  key_links:
    - "Ce plan écrit sur B (`feat/phase-41-preuves-release`) ; la publication et le merge viennent au plan 41-11"
---

<objective>
Reporter dans la doctrine ce que les mesures ont établi, et rien de plus : statut d'O-3, item BACKLOG
traité par renvoi, ADR-072 § Mesures.

Purpose: PROT-04 (statut O-3 sur pièce) et PROT-03 (ADR-072 complétée par la mesure, BACKLOG traité).
Output: trois fichiers de doctrine à jour sur la branche B.

Budget (point 9 du tour 1) : première moitié de l'ancien 41-10, scindé pour tenir sous dix fichiers ;
deux tâches, quatre fichiers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

Extraits à lire : `25-SECURITY.md` L260-275 (bloc O-3) ; `.planning/BACKLOG.md` L551-580 (item
« Posture de protection de `main` ») ; `docs/ADR.md` § ADR-072.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : B à jour de `main`, précondition des mesures, statut O-3, BACKLOG traité par renvoi</name>
  <files>.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md, .planning/BACKLOG.md, .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes, décisions comprises)
    - les extraits listés dans le contexte
  </read_first>
  <action>
Identité `samuel-neveugall` ; branche B ; `git fetch origin` ; `git merge --no-edit origin/main` en
local (le merge de PR-P et un éventuel hotfix y entrent ; un conflit s'arrête et se remonte).
Vérifier la précondition du frontmatter (derniers verdicts, décisions de poursuite).

`25-SECURITY.md` : sous la puce O-3, sans modifier ses lignes, ajouter UNE ligne indentée de
continuation de liste :
`  **Statut Phase 41 (<date d'exécution>) : gardée par défaut + tracée.** Une hausse de baseline exige l'approbation de `@picmakpro` (CODEOWNERS, revue code owner requise par le ruleset de `main`) ou un contournement explicite par un compte write, qui laisse une entrée dans les rule suites (D-02, D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Preuves : `41-PREUVES.md` (`CO-I:`, `CO-BASELINE:`, `TRACE-II:`) ; politique : ADR-072.`
Si `TRACE-II:` porte un `non_prouvee`, ajouter à cette même ligne « Trace du contournement : non
prouvée pour <règle> (41-PREUVES.md). » Aucun autre mot : le statut n'est pas un achèvement (D-02).

`.planning/BACKLOG.md`, item « Posture de protection de `main` » : ajouter à la fin de l'item, avant
l'item suivant, une ligne `**Statut (<date d'exécution>, Phase 41) : TRAITÉ par renvoi** — rulesets
de branche et de tags posés et prouvés, CODEOWNERS étroit, politique de contournement et de hotfix :
ADR-072 ; preuves : `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md`.` Aucune
ligne existante de l'item modifiée (son titre est cité ailleurs).

Ajouter à `41-PREUVES.md` une section `## 41-10 — doctrine sur pièce`. Commit local sur B, message
« docs(41-10): statut O-3 et BACKLOG traité par renvoi (D-02, D-05 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; m2=$(last M2-VERDICT:); m2d=$(val M2-DECISION: option); co=$(last CO-VERDICT:); cod=$(val CO-DECISION: option); m1=$(last M1-VERDICT:); m3=$(last M3-VERDICT:); m3d=$(val M3-DECISION: option); d3=$(last D03-VERDICT:); m4=$(last M4-VERDICT:); dd=$(val D03-M4-DECISION: option); ok=oui; { [ "$m2" = CONFORME ] || [ "$m2d" = accepter-et-documenter ]; } || ok=non; { [ "$co" = CONFORME ] || [ "$cod" = accepter-et-documenter ]; } || ok=non; [ "$m1" = SUR ] || ok=non; { [ "$m3" = CONFORME ] || [ "$m3d" = fermer-et-poursuivre ] || [ "$m3d" = consigner-et-poursuivre ]; } || ok=non; { { [ "$d3" = CONFORME ] && [ "$m4" = CONFORME ]; } || [ "$dd" = accepter-et-documenter ]; } || ok=non; pd=$(val PUSH-DIRECT: refuse); sc=$(val D03-M4-DECISION: sort_commit_preuve); if [ "$pd" = non ]; then case "$sc" in (revert-par-pr|conservation-declaree) ;; (*) ok=non;; esac; fi; op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); br=$(git branch --show-current); b=$(git rev-list --count HEAD..origin/main); echo "precondition 41-10 : M2=$m2/$m2d CO=$co/$cod M1=$m1 M3=$m3/$m3d D03=$d3 M4=$m4 decision=$dd push_direct_refuse=$pd sort_commit_preuve=$sc prs_ouvertes=$op branche=$br retard_sur_origin_main=$b"; [ "$ok" = oui ] && [ "$op" -eq 0 ] && [ "$br" = feat/phase-41-preuves-release ] && [ "$b" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou un verdict de mesure ni conforme ni couvert par une décision de poursuite consignée (canal et date) ; ou push direct accepté sans `sort_commit_preuve` fixé (revert par PR ou conservation déclarée) ; ou une PR ouverte ; ou branche courante différente de B ; ou `origin/main` non fusionné en local</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md; T=$(mktemp -d); awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$F" > "$T/o3"; s=$(awk "index(\$0, \"**Statut Phase 41 (\")" "$T/o3"); n=$(awk "index(\$0, \"**Statut Phase 41 (\") {c++} END{print c+0}" "$T/o3"); g=$(printf "%s" "$s" | awk "index(\$0, \"gardée par défaut + tracée\") {c++} END{print c+0}"); x=$(printf "%s" "$s" | awk "{l=tolower(\$0); if (index(l, \"ferm\") || index(l, \"clos\") || index(l, \"résolu\")) c++} END{print c+0}"); r=$(printf "%s" "$s" | awk "index(\$0, \"41-PREUVES.md\") && index(\$0, \"ADR-072\") {c++} END{print c+0}"); git show origin/main:"$F" > "$T/src"; awk "/^- \\*\\*O-3 — /{f=1} /^- \\*\\*O-4 — /{f=0} f" "$T/src" > "$T/o3a"; orig=$(awk "END{print NR}" "$T/o3a"); now=$(awk "END{print NR}" "$T/o3"); awk "!index(\$0, \"**Statut Phase 41 (\")" "$T/o3" > "$T/o3b"; pre=$(cmp -s "$T/o3b" "$T/o3a" && echo oui || echo non); echo "O-3 : lignes_statut=$n garde_trace=$g vocabulaire_d_achevement=$x renvois=$r lignes_origine=$orig lignes_maintenant=$now texte_origine_intact=$pre"; [ "$n" -eq 1 ] && [ "$g" -eq 1 ] && [ "$x" -eq 0 ] && [ "$r" -eq 1 ] && [ "$now" -eq $((orig + 1)) ] && [ "$pre" = oui ]'</automated>
    <fails_when>exit non nul ; ou `lignes_statut=` différent de 1 ; ou statut sans « gardée par défaut + tracée » ; ou `vocabulaire_d_achevement=` différent de 0 (D-02) ; ou renvois manquants ; ou texte d'origine d'O-3 modifié (`texte_origine_intact=non`, ou plus d'une ligne ajoutée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'F=.planning/BACKLOG.md; T=$(mktemp -d); awk "/^## Posture de protection de .main. — TRANCHÉ/{f=1; print; next} /^## /{f=0} f" "$F" > "$T/now"; git show origin/main:"$F" > "$T/src"; awk "/^## Posture de protection de .main. — TRANCHÉ/{f=1; print; next} /^## /{f=0} f" "$T/src" > "$T/orig"; st=$(awk "/^\\*\\*Statut \\(.*, Phase 41\\) : TRAITÉ par renvoi\\*\\*/ && index(\$0, \"ADR-072\") && index(\$0, \"41-PREUVES.md\") {c++} END{print c+0}" "$T/now"); awk "!/^\\*\\*Statut \\(.*, Phase 41\\) : TRAITÉ par renvoi\\*\\*/" "$T/now" > "$T/n1"; awk "NF || p {print} {p=NF}" "$T/n1" > "$T/n2"; awk "NF || p {print} {p=NF}" "$T/orig" > "$T/o2"; ol=$(awk "END{print NR}" "$T/o2"); intact=$(cmp -s "$T/n2" "$T/o2" && [ "$ol" -gt 5 ] && echo oui || echo non); echo "BACKLOG : ligne_statut=$st lignes_existantes_intactes=$intact"; [ "$st" -eq 1 ] && [ "$intact" = oui ]'</automated>
    <fails_when>exit non nul ; ou `ligne_statut=` différent de 1 (absente, dupliquée, sans renvoi ADR-072 ou preuves) ; ou `lignes_existantes_intactes=non` (titre ou texte de l'item modifié)</fails_when>
  </verify>
  <acceptance_criteria>
    - précondition des mesures tenue ; B contient `origin/main`
    - une ligne de statut O-3 « gardée par défaut + tracée », sans vocabulaire d'achèvement, texte d'origine intact
    - une ligne « TRAITÉ par renvoi » sous l'item BACKLOG, lignes existantes intactes
  </acceptance_criteria>
  <done>Le statut d'O-3 et le BACKLOG disent ce que la mesure a établi.</done>
</task>

<task type="auto">
  <name>Task 2 : ADR-072 § Mesures à la pose, balayage O-3 de tous les artefacts, rejeu</name>
  <files>docs/ADR.md</files>
  <read_first>
    - docs/ADR.md § ADR-072
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
  </read_first>
  <action>
`docs/ADR.md`, fin d'ADR-072 : sous-section `### Mesures à la pose (Phase 41, <date d'exécution>)`,
une puce par mesure, valeurs recopiées de `41-PREUVES.md`, au format exact :
- `- **M-1** : <dernier M1-VERDICT> — <une phrase>` ;
- `- **M-2** : admin_couvert=<valeur> — <les quatre current_user_can_bypass, valeurs admin non relues par l'agent>` ;
- `- **M-3** : <resultat de M3:> — <état avant, checks en cours, sortie de --admin>` ;
- `- **Provenance serveur** : rest=<valeur de PROVENANCE-SERVEUR:>, http=<code> — <une phrase>` ;
- `- **Provenance client** : co_i=<provenance de CO-I:>, ci_a=<provenance_a>, ci_b=<provenance_b> — <une phrase>` ;
- `- **Revue code owner par chemin** : github=oui, baseline=<CO-BASELINE demande_picmakpro>, sentinelle=<CO-SENTINELLE demande_picmakpro>` ;
- `- **Trace (ii)** : pull_request=<statut>, required_status_checks=<statut>, lu_par=<compte>, relue=<valeur> — contournement réel sur des checks en cours, pas sur une PR rouge` ;
- `- **Push direct** : refuse=<valeur>, provenance=<valeur> — <une phrase>` ;
- `- **M-4** : <dernier M4-VERDICT> — <traces de réécriture et de suppression>` ;
- `- **Accès aux rule suites** : samuel-neveugall=<valeur>, picmakpro=<valeur>` ;
- `- **Décisions humaines** : …` recopiant chaque ligne `*-DECISION:` (option, canal, date), ou
  « aucune » s'il n'y en a pas.
Aucune valeur inventée ; O-3 n'y est associée à aucun mot d'achèvement. Commit local sur B, message
« docs(41-10): mesures de la pose reportées dans ADR-072 (D-02, D-09 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ».
Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; last() { awk -v k="$1" "\$1==k {v=\$2} END{print v}" "$PH/41-PREUVES.md"; }; T=$(mktemp -d); awk "/^## ADR-072 : /{f=1} f" docs/ADR.md > "$T/a72"; awk "/^### Mesures à la pose \\(Phase 41, /{f=1} f" "$T/a72" > "$T/m"; h=$(awk "END{print NR}" "$T/m"); k=0; chk() { [ -n "$3" ] || { echo "VALEUR VIDE DANS 41-PREUVES : $1 $2"; k=$((k+1)); return 0; }; awk -v a="$1" -v b="$2$3" "index(\$0, a) && index(\$0, b) {f=1} END{exit !f}" "$T/m" || { echo "ABSENT : $1 $2$3"; k=$((k+1)); }; }; chk "**M-1** : " "" "$(last M1-VERDICT:)"; chk "**M-2** : " "admin_couvert=" "$(val M2: admin_couvert)"; chk "**M-3** : " "" "$(val M3: resultat)"; chk "**Provenance serveur** : " "rest=" "$(val PROVENANCE-SERVEUR: rest)"; chk "**Provenance client** : " "co_i=" "$(val CO-I: provenance)"; chk "**Revue code owner par chemin** : " "baseline=" "$(val CO-BASELINE: demande_picmakpro)"; chk "**Revue code owner par chemin** : " "sentinelle=" "$(val CO-SENTINELLE: demande_picmakpro)"; chk "**Trace (ii)** : " "pull_request=" "$(val TRACE-II: pull_request)"; chk "**Trace (ii)** : " "required_status_checks=" "$(val TRACE-II: required_status_checks)"; chk "**Push direct** : " "refuse=" "$(val PUSH-DIRECT: refuse)"; chk "**Push direct** : " "provenance=" "$(val PUSH-DIRECT: provenance)"; chk "**M-4** : " "" "$(last M4-VERDICT:)"; chk "**Accès aux rule suites** : " "samuel-neveugall=" "$(val ACCES-RULE-SUITES: samuel-neveugall)"; chk "checks en cours" "" "pas sur une PR rouge"; awk "\$1 ~ /-DECISION:\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) print substr(\$i, 8) }" "$PH/41-PREUVES.md" > "$T/dec"; while IFS= read -r o; do [ -n "$o" ] || continue; awk -v o="$o" "index(\$0, o) {f=1} END{exit !f}" "$T/m" || { echo "DECISION NON REPORTEE : $o"; k=$((k+1)); }; done < "$T/dec"; nd=$(awk "END{print NR}" "$T/dec"); echo "ADR-072 mesures : lignes_section=$h decisions_consignees=$nd valeurs_absentes=$k"; [ "$h" -gt 0 ] && [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou sous-section absente ; ou `VALEUR VIDE DANS 41-PREUVES :` (mesure jamais consignée) ; ou `ABSENT :` (valeur non reportée ou différente de `41-PREUVES.md`, nuance « checks en cours … pas sur une PR rouge » manquante) ; ou `DECISION NON REPORTEE :` (une décision humaine consignée n'est pas écrite dans l'ADR)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md docs/ADR.md .planning/BACKLOG.md CLAUDE.md $PH/41-0*-SUMMARY.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk "/^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"MESSAGE SANS CANAL NI DATE : \" s; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/l"'</automated>
    <fails_when>exit non nul ; ou une ligne `MESSAGE SANS CANAL NI DATE :` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `commits_de_plan=0` (rien de committé à juger)</fails_when>
  </verify>
  <acceptance_criteria>
    - ADR-072 § Mesures recopiant chaque valeur consignée et chaque décision humaine, nuance des checks en cours comprise
    - balayage O-3 vert sur 25-SECURITY, ADR.md, BACKLOG, CLAUDE.md et les SUMMARY déjà écrits
    - rejeux verts ; messages de commit de plan conformes
  </acceptance_criteria>
  <done>La doctrine dit ce que la mesure a établi, décisions comprises, et rien de plus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves → doctrine | un lecteur futur agit sur ce qu'ADR-072 et `25-SECURITY.md` affirment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-38 | Repudiation | O-3 présentée comme achevée | medium | mitigate | ligne de statut unique, vocabulaire d'achèvement absent, texte d'origine intact ; balayage par proposition de tous les artefacts |
| T-41-41 | Tampering | doctrine réécrite au-delà de la mesure | medium | mitigate | chaque valeur d'ADR-072 § Mesures recoupée avec `41-PREUVES.md`, décisions comprises |
| T-41-53 | Repudiation | décision humaine absente de la doctrine | medium | mitigate | chaque `*-DECISION:` doit être reportée (vérification `DECISION NON REPORTEE`) |
</threat_model>

<verification>
O-3, BACKLOG et ADR-072 à jour sur pièce ; aucun artefact n'associe O-3 à un mot d'achèvement.
</verification>

<success_criteria>
PROT-03 et PROT-04 ont leur doctrine sur pièce, prête à être publiée avec la release.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-10-SUMMARY.md` when done — committé sur la branche B.
</output>

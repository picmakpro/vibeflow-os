---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 08
type: execute
wave: 7
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05", "23-06", "23-07"]
files_modified:
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/CHANGELOG.md
  - README.md
  - README.fr.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GSDC-10]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Le module `dev-orchestrator` porte une version **mineure** de plus (nouvelle capacité), avec sa triade complète cohérente et son CHANGELOG à jour."
    - "`scripts/check-version-sync.sh` sort ✓ : le compteur de suites des deux README racine dit 47, ce que le disque porte réellement."
    - "Tous les gates de sortie du dépôt sont verts, rejoués et non supposés — et chacun est étiqueté au SUMMARY `livraison` ou `non-régression (vert d'avance)`, un gate déjà vert avant le plan ne valant pas preuve de travail."
    - "Le ledger d'exigences porte les dix identifiants **cochés**, leur traçabilité vers les plans qui les couvrent, et écrit la couverture partielle de `GSDC-08` au lieu de la certifier pleine."
    - "L'écart `D-22` sort de la phase **nommé et non tranché** : `gsd-debugger` reste dans l'allowlist de `vf-coder.md` (et reste exigé par `CODER_ALLOWED`), l'arbitrage est reporté au SUMMARY pour Samuel."
    - "La release racine (VERSION, plugin.json, marketplace.json, badges, tag annoté, release GitHub) reste HORS périmètre — réservée à validation humaine."
  artifacts:
    - plugin/dev-orchestrator/VERSION
    - plugin/dev-orchestrator/module.json
    - plugin/dev-orchestrator/README.md
    - plugin/dev-orchestrator/CHANGELOG.md
    - README.md
    - README.fr.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "Le compteur « N suites » des deux README est gaté par `check-version-sync.sh` : la suite ajoutée au plan 23-02 le rend rouge jusqu'ici, et c'est ce plan qui le remet d'équerre — précédent 21-05 (44 → 45)."
    - "La triade par module est vérifiée machine : VERSION et module.json doivent bouger ensemble, l'en-tête README avec (convention du dépôt)."
    - "Les badges de version racine ne bougent PAS : ils sont indexés sur la VERSION racine, qui reste réservée à la validation humaine."
---

<objective>
Clore la Phase 23 : gouvernance de version du module, remise d'équerre des compteurs gatés, ledger
d'exigences, et rejeu de tous les gates de sortie.

Purpose : ce dépôt a une discipline de release non négociable (toute version = un tag annoté + une
release GitHub), et un précédent constant depuis les Phases 13, 17, 19 et 22 : **le bump du module
et sa triade se font en phase, la release racine reste à l'humain**. Un second précédent, celui de
la Phase 21 (plan 21-05), fixe la conduite pour le compteur de suites : quand une phase ajoute une
suite, elle remet le compteur des deux README d'équerre dans la phase, parce que c'est de la
cohérence CI et non un morceau de release.

Output : `dev-orchestrator` en version mineure suivante avec sa triade et son CHANGELOG, compteur
de suites 46 → 47 dans les deux README racine, `check-version-sync.sh` ✓, ledger d'exigences
`GSDC-01..10` complété, et tous les gates rejoués.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@CLAUDE.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md`

**Champs neufs du bloc typé** : `gate`, `reprise`, `verdicts`, `decompte`

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » ·
`mission-flow.md` §Pattern E budget partagé + §briques dormantes · `docs/ADR.md` ADR-061 troisième
objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : **dix blocs principaux**, `T24` à `T33`,
étendus par **huit sous-blocs nommés** (`T25 ATTEINTE`, `T25c`, `T26 F`, `T26 E'`, `T27b`, `T27c`,
`T28-H/I/J/K`, `T28 ATTEINTE`). Chiffres **mesurés le 2026-08-04**, à re-dériver avant écriture
plutôt qu'à recopier : `awk '/^#[ -]*T(2[4-9]|3[0-9])[A-Za-z]?/{print FNR": "$0}' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`
(la numérotation n'est **pas** monotone dans le fichier — `T33` vit entre `T27c` et `T28`, choix
volontaire consigné au SUMMARY de 23-07 : ne pas y lire un désordre ni un bloc manquant).

**Variables d'environnement neuves** : `VF_CONFIG_PATH`, `VF_GSD_CORE_LIB`, `VF_CAPS_INDEX_OUT`,
`VF_GSD_TOOLS`

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` / `3` / `64`

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10` (ce plan les inscrit au ledger avec leur
traçabilité)

**Compteur modifié** : « N suites » des deux README racine, 46 → 47 (ce plan)

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`).
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`).
- **Gate de schéma ORM : sans objet.**
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan ne touche que des fichiers de version, de changelog et de documentation — aucun SDK,
  aucun service tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

<tasks>

<task type="tracer">
  <name>Tâche 1 : triade de version du module, CHANGELOG, et compteur de suites remis d'équerre</name>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/CHANGELOG.md, README.md, README.fr.md</files>
  <precondition>Les sept plans précédents de la phase sont exécutés et leurs SUMMARY présents sur disque — constatable : `ls .planning/phases/VFDO-23-*/23-0[1-7]-SUMMARY.md | wc -l` retourne `7`. Sinon HALT : bumper une version sur un périmètre partiellement livré produirait un CHANGELOG qui décrit ce qui n'existe pas.</precondition>
  <read_first>
    - plugin/dev-orchestrator/VERSION (une ligne, `v2.10.0` avant ce plan)
    - plugin/dev-orchestrator/module.json — le champ `version` (`2.10.0` avant ce plan, **sans** le préfixe `v`, à la différence du fichier VERSION)
    - plugin/dev-orchestrator/README.md ligne 10 — la ligne `**Version** : v2.10.0`
    - plugin/dev-orchestrator/CHANGELOG.md (les 40 premières lignes) — le format exact de l'entrée la plus récente : titre `## [vX.Y.Z] — AAAA-MM-JJ (résumé court, Phase N)`, puis les rubriques `### Ajouté` / `### Modifié` / `### Corrigé`, en français
    - scripts/check-version-sync.sh (INTÉGRALEMENT) — les 6 contrôles : ce qu'il compare au canon racine (plugin.json, marketplace.json, les 2 badges de version, le compteur de modules) et ce qu'il compare **par module** (VERSION ↔ module.json), plus le contrôle n° 9 sur le compteur de suites. Ce contrôle prend la **première** occurrence de la forme « N suites » du fichier (`grep -o '[0-9][0-9]* suites' … | head -1`) : **mesuré le 2026-08-04, cette première occurrence est bien la puce « Auditable »** dans les deux README, les mentions datées venant toutes après elle dans l'ordre du fichier. Corollaire à garder en tête : **ce gate ne voit qu'une ligne par fichier** — il resterait vert sur un README dont les autres mentions vivantes seraient fausses. C'est pourquoi le relevé se fait par `awk` sur **toutes** les occurrences (ci-dessous) et non en s'en remettant au gate
    - Les deux README racine — **localiser les mentions du compteur par ancre de CHAÎNE, jamais par numéro de ligne** : `awk '/46 suites/{print FILENAME":"FNR": "$0}' README.md README.fr.md`. Les deux fichiers ont été **réécrits par le merge `d9e33be` (origin/main, Phase 26)** : README.md est passé de 290 à **179** lignes, README.fr.md à **184**, et **l'encadré CI du diagramme mermaid a disparu** — toute référence de ligne antérieure à ce merge est morte. État mesuré le 2026-08-04, à re-mesurer avant d'écrire :
      - **une seule mention vivante par fichier** — `README.md` puce « Auditable », forme `(46 suites in CI)` (sans gras ni accent grave) ; `README.fr.md` puce « Auditable », forme ``(`46 suites` en CI)`` (compteur **entre accents graves**). Les deux formes diffèrent : ne pas transposer l'une sur l'autre.
      - **trois mentions datées par fichier**, dans les lignes `| v2.46.0 |`, `| v2.45.0 |` et `| v2.44.0 |` du tableau d'historique de release, sous la forme `**46 suites**` — **constats datés, intouchables** (voir action, point 3).
    - Les sept SUMMARY de la phase (`23-01` à `23-07`) — le CHANGELOG se rédige depuis ce qui a été livré, jamais depuis ce qui était prévu ; les justifications de retrait d'allowlist du plan 23-05 y ont été consignées expressément pour cette entrée
  </read_first>
  <action>
**1. Bump mineur du module** (nouvelle capacité : deux scripts, une table générée, une doctrine de
flags, un contrat de checkpoint), de la version courante vers la version **mineure suivante** —
`v2.10.0` → `v2.11.0`. Triade complète, les trois fichiers ensemble et à la même valeur, en
respectant la forme de chacun :
- `plugin/dev-orchestrator/VERSION` : la valeur **avec** le préfixe `v` ;
- `plugin/dev-orchestrator/module.json`, champ `version` : la valeur **sans** le préfixe `v` ;
- `plugin/dev-orchestrator/README.md`, ligne d'en-tête `**Version**` : la valeur avec le préfixe.

**2. `plugin/dev-orchestrator/CHANGELOG.md`** — ajouter une entrée en tête, au format exact de
l'entrée précédente, en français, titrée par la nouvelle version, la date du jour et un résumé
court nommant la phase. Rubriques attendues, alimentées par les sept SUMMARY et **jamais** par ce
plan (ce qui a été livré, pas ce qui était prévu) :
- **Ajouté** : le gate d'alignement de configuration et son contrat d'exit ; le générateur de table
  de capabilities et sa sortie auto-générée ; la doctrine de flags de cycle en allowlist stricte
  avec sa clause de fermeture par défaut ; la doctrine de voie unique ; la table des moments
  déclencheurs des briques dormantes ; les quatre champs optionnels du bloc typé ; les blocs de
  test neufs — **dix blocs principaux `T24` à `T33`** (plus huit sous-blocs nommés), chiffre à
  **re-dériver par la commande donnée en tête de plan** avant de l'écrire au CHANGELOG, jamais
  recopié : un compteur faux ici se fige dans l'historique de release.
- **Modifié** : le contrat de checkpoint (mapping unique vers l'escalade) ; le budget de tours
  devenu partagé par étape ; la ligne du cycle canonique sur la brique de livraison ; ADR-061
  étendue d'un troisième objet revu ; le `.planning/config.json` du lab.
- **Retiré** : la voie dégradée de dispatch d'agent nu dans le worker, et les deux entrées
  d'allowlist correspondantes — **avec** la justification consignée aux SUMMARY des plans 23-05
  (dont l'arbitrage du Finding 1 chez le manager, qui doit être lisible ici et **pas** dans le
  fichier d'agent).

**3. Compteur de suites des deux README racine** — le porter de 46 à 47 (la suite dédiée ajoutée au
plan 23-02).

**Procédure, dans cet ordre :**

a. **Établir la vérité du disque par exécution**, en croisant deux méthodes indépendantes — si
   elles divergent, HALT et remonter l'écart plutôt que de choisir :
   ```
   find plugin scripts -path '*/tests/test-*.sh' | awk 'END{print "find="NR}'
   rtk proxy git ls-files | awk '/\/tests\/test-.*\.sh$/{n++} END{print "ls-files="n+0}'
   ```
   Valeur mesurée le 2026-08-04 : **47 par les deux méthodes, écart nul**. Si le disque en dit un
   autre au moment de l'exécution, **c'est le disque qui a raison**, pas ce plan.
   `awk 'END{print NR}'` et non `grep -c` : `grep` est proxifié sur ce poste et tronque sa sortie.

b. **Relever toutes les occurrences par ancre de chaîne**, jamais par numéro de ligne :
   `awk '/46 suites/{print FILENAME":"FNR": "$0}' README.md README.fr.md`.

c. **Ne modifier que la mention vivante de chaque fichier** — la puce « Auditable ». Mesuré le
   2026-08-04 : **une seule par fichier**, et les deux **de forme différente** (`(46 suites in CI)`
   en anglais, ``(`46 suites` en CI)`` en français, compteur entre accents graves). Éditer chacune
   dans sa propre forme ; ne pas uniformiser au passage.

d. **Ne jamais toucher** aux mentions des lignes d'historique de release (`| v2.46.0 |`,
   `| v2.45.0 |`, `| v2.44.0 |`, forme `**46 suites**` — trois par fichier au 2026-08-04) : ce sont
   des **constats datés**, ils disent ce qui était vrai à cette version et les réécrire falsifierait
   l'historique.

e. **Recompter après édition** avec le même `awk` : il doit rester **exactement trois** occurrences
   de « 46 suites » par fichier (les datées) et **une** de « 47 suites » (la vivante). Un total qui
   ne tombe pas juste signale soit une mention manquée, soit une ligne datée abîmée.

⚠️ Le gate `check-version-sync.sh` ne lit que la **première** occurrence du fichier : il passerait
au vert alors qu'une autre mention vivante serait encore fausse. Le relevé `awk` du point (b) est ce
qui couvre cet angle mort — **ne pas s'en remettre au gate seul**.

Ce geste est de la **cohérence CI**, pas un morceau de release — précédent explicite du plan 21-05
(44 → 45 en phase).

**4. Ce qui ne bouge PAS, et doit être vérifié comme tel** : la `VERSION` racine, `plugin.json`,
`marketplace.json`, les deux **badges de version** des README, le compteur de **modules**. La
release racine — bump, badges, historique de release, tag annoté, release GitHub — est
**explicitement hors périmètre**, réservée à validation humaine (patron des Phases 13, 17, 19, 22).
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh; echo "version-sync=$?"</automated>
  </verify>
  <acceptance_criteria>

**A. Critères de LIVRAISON — ce que cette tâche fait bouger** (rouges avant, verts après ; ce sont
eux, et eux seuls, qui prouvent le travail) :

    - `bash scripts/check-version-sync.sh` sort en **0**. Mesuré **rc=1 avant ce plan**, sur deux `ko` : `'46 suites' ≠ réel=47` sur `README.md` et sur `README.fr.md` — **c'est le seul discriminant machine de toute la tâche**.
    - `cat plugin/dev-orchestrator/VERSION` retourne `v2.11.0` (mesuré avant : `v2.10.0`).
    - `node -e 'process.exit(require("./plugin/dev-orchestrator/module.json").version==="2.11.0"?0:1)'` sort en 0 (mesuré avant : `2.10.0`, ligne 3).
    - `awk '$0=="**Version** : v2.11.0"{n++} END{if(n!=1) exit 1}' plugin/dev-orchestrator/README.md` sort en **0** (mesuré avant : `**Version** : v2.10.0`). Correspondance **exacte** en `awk`, jamais `grep -c` : `grep` est proxifié sur ce poste et tronque.
    - `awk 'FNR<=3 && /v2\.11\.0/{n++} END{if(n!=1) exit 1}' plugin/dev-orchestrator/CHANGELOG.md` sort en **0** (le titre de la nouvelle entrée est en tête, format `## [vX.Y.Z] — AAAA-MM-JJ (résumé, Phase N)`).
    - L'entrée de CHANGELOG nomme les quatre artefacts neufs : `check-gsd-config.sh`, `build-gsd-capabilities-index.sh`, `gsd-capabilities-index.md`, `test-check-gsd-config.sh`.
    - L'entrée de CHANGELOG contient une rubrique de retrait nommant les deux agents nus sortis des allowlists (la justification vit ici, pas dans les fichiers d'agents).
    - Compteur de suites, **relevé complet et non par le seul gate** :
      `find plugin scripts -path '*/tests/test-*.sh' | awk 'END{print NR}'` retourne la valeur réelle (mesurée **47**), et
      `awk '/47 suites/{v++} /46 suites/{d++} END{print FILENAME" vivantes_47="v+0" datees_46="d+0}' README.md` puis le même sur `README.fr.md` retournent **`vivantes_47=1 datees_46=3`** pour chaque fichier.
      Valeurs **mesurées avant le travail : `vivantes_47=0 datees_46=4`** pour les deux fichiers (les 4 = la puce vivante + les 3 lignes datées). Le critère est donc discriminant, et le passage `4 → 3` est exactement la puce vivante qui bascule : `datees_46` qui resterait à 4 signale une puce non éditée, `datees_46` tombé à 2 ou moins signale une **ligne d'historique abîmée** — dans les deux cas, corriger avant de committer.

**B. Rejeu de NON-RÉGRESSION — vert d'avance, à rejouer quand même** (mesuré déjà satisfait sur
arbre intact le 2026-08-04 : ces critères ne prouvent aucun travail, ils prouvent qu'aucun dégât
n'a été fait. Ne pas les présenter au SUMMARY comme des livrables) :

    - `rtk proxy git show HEAD:VERSION` retourne `v2.46.0` — **avec** le préfixe `v` (mesuré : la `VERSION` racine porte le `v`, contrairement au champ `version` de `module.json`). Écrire la valeur attendue **avec** le préfixe : un critère attendant `2.46.0` serait faux par construction.
    - **Garde de non-entame de la release racine.** ⚠️ **Ne jamais utiliser `git diff --name-only` nu** : il mesure l'arbre de travail non commité (mesuré **vide** ici, arbre propre) — donc structurellement vert après commit, il ne prouve rien. Et ne jamais compter les lignes d'une sortie proxifiée : mesuré **49 chemins** sous `rtk proxy` contre **52 lignes** sans, l'écart étant **trois lignes injectées par le proxy** (dont `--- Changes ---`), pas trois fichiers. Seule construction robuste, **dans cet ordre** :
      ```
      MB=$(rtk proxy git merge-base origin/main HEAD)
      rtk proxy git diff --name-only "$MB" HEAD > /tmp/vf-2308-bd.txt
      awk 'END{if(NR<10) exit 1}' /tmp/vf-2308-bd.txt          # NON-VACUITÉ d'abord — sort 1 si la liste est vide
      awk '$0=="VERSION"||$0=="plugin/.claude-plugin/plugin.json"||$0==".claude-plugin/marketplace.json"{exit 1}' /tmp/vf-2308-bd.txt
      ```
      Les **deux** `awk` doivent sortir en **0**. L'ordre est le fond du critère : **l'absence d'un chemin interdit ne vaut rien tant que la liste n'est pas prouvée non vide** — vérifié par exécution, la seconde garde sort **0 sur une liste vide** (faux vert), la première sort **1** et l'attrape. Correspondance **exacte** en `awk` (`$0==`), jamais `grep`. État mesuré le 2026-08-04 : 49 chemins, aucun des trois interdits, les deux gardes en 0.
    - `awk '/img\.shields\.io\/badge\/version-2\.46\.0-/{n++} END{if(n!=1) exit 1}' README.md` sort en **0**, idem sur `README.fr.md` (badge de version racine intact — mesuré : **ligne 13 des deux fichiers**, une occurrence chacun).
  </acceptance_criteria>
  <done>Le module porte sa nouvelle version mineure avec sa triade cohérente et son CHANGELOG écrit depuis le livré, le compteur de suites dit la vérité du disque, et rien de la release racine n'a été entamé.</done>
</task>

<task type="auto">
  <name>Tâche 2 : ledger d'exigences, traçabilité, et rejeu de tous les gates de sortie</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — la section `## Hors-milestone — Phase 22 : hygiène documentaire` (lignes ~210-259) et le tableau `## Traceability` : la Phase 23 suit **exactement** ce patron (bloc de citation d'ouverture expliquant l'origine du préfixe, une puce par identifiant, puis des lignes de traçabilité), et la section `**Coverage:**` en pied de fichier
    - Les huit PLAN.md de la phase — pour la correspondance exacte identifiant ↔ plan
    - Les sept SUMMARY de la phase — pour le statut réel de chaque identifiant (livré, partiel, non couvert)
    - CLAUDE.md racine — la discipline de release, pour vérifier qu'aucun de ses quatre gestes n'a été entamé
  </read_first>
  <action>
**1. `.planning/REQUIREMENTS.md`** — compléter la section Phase 23 posée au moment du plan (préfixe
`GSDC`, identifiants `GSDC-01` à `GSDC-10`).

**État mesuré le 2026-08-04 — ce qui reste réellement à faire est étroit, et le reste est déjà
vrai.** Le travail se réduit à trois gestes ; tout le reste du fichier est **déjà conforme** et ne
doit pas être retouché :

a. **Les dix cases de définition sont toutes `- [ ]`** (`GSDC-01` à `GSDC-10`, repérables par
   `awk '/^- \[.\] \*\*GSDC-/{print FNR": "substr($0,1,40)}' .planning/REQUIREMENTS.md`).
   **Les cocher** — c'est un geste que le plan n'ordonnait nulle part et sans lequel la phase se
   clôturerait sur un ledger visuellement vide. La convention du fichier est établie et mesurée :
   **63 puces `[x]`, 1 puce `[~]`** (`VERB-02`, partielle, avec sa raison entre parenthèses en
   italique). Cocher `[x]` ce qui est intégralement couvert, `[~]` ce qui ne l'est pas.

b. **Basculer les trois seules lignes de traçabilité encore en `Planned`** : `GSDC-08` et `GSDC-09`
   (plan 23-07), `GSDC-10` (ce plan). Mesuré : `GSDC-01` à `GSDC-07` portent **déjà** `Done` avec
   leur plan nommé — **ne pas y toucher**.

c. **`GSDC-08` est le seul cas partiel, et il ne se bascule PAS mécaniquement** — voir l'escalade
   ci-dessous, qui est la vraie difficulté de ce point.

**La ligne `**Coverage:**` de la Phase 23 est DÉJÀ écrite et déjà juste** (10 requirements, mappés
aux 8 plans, `0 non-mappé`, avec la couverture des 7 zones de décision) : mesurée conforme, elle
n'appelle **aucune modification**. Ne la réécrire que si le décompte réel s'avère différent.

⚠️ **Deux dettes antérieures à ne PAS corriger ici** — les voir ne vaut pas mandat de les solder :
- les Phases 15 à 21 restent hors registre (choix déjà acté au ledger par la Phase 22) ;
- **les sept puces `DOCF-01..07` de la Phase 22 sont restées `- [ ]` et leur traçabilité est restée
  `Planned`** alors que la phase est close. C'est une dette de clôture antérieure, **hors périmètre
  de ce plan** — et surtout, **ne pas en déduire que la convention du fichier serait de ne jamais
  cocher** : les 63 puces `[x]` mesurées disent l'inverse. La signaler au SUMMARY, ne pas la
  toucher.

**🛑 Escalade obligatoire sur `GSDC-08` — ne pas trancher, nommer.**

`GSDC-08` couvre les briques dormantes, donc `D-22`/`D-23`/`D-24`. Le plan 23-07 a livré le **volet
dispatch** (aucun agent nu de debug offert en dispatch direct, gaté par `T32-D`) mais **pas** le
volet allowlist, et son SUMMARY porte une section dédiée « 🛑 Écart D-22 — ÉCART OUVERT, remonté à
l'arbitrage humain ». Fait mesuré, vérifié à nouveau le 2026-08-04 : `vf-coder.md` ligne 4 porte
l'entrée `gsd-debugger`, ce que `D-22` (tranché par Samuel : « aucun `gsd-debugger` en allowlist,
aucune exception ») contredit frontalement.

**Basculer `GSDC-08` en `Done` certifierait implicitement `D-22` conforme, ce qu'il n'est pas.**
Donc : `GSDC-08` reçoit `[~]` avec sa raison écrite (volet dispatch couvert, volet allowlist ouvert
sur `D-22`), et sa ligne de traçabilité dit la couverture partielle plutôt que `Done`.

**Et surtout : ne PAS retirer `gsd-debugger` pour « rendre le ledger vrai ».** Mesuré : ce nom est
présent dans `CODER_ALLOWED` (`test-dev-orchestrator.sh`, bloc `T19`), et `check_worker_allowlist`
**KO chaque nom ABSENT** de l'allowlist de l'agent — c'est un **gate machine positif qui EXIGE sa
présence**. Le retirer ferait tomber la suite. L'arbitrage est binaire et appartient à Samuel :
retirer l'entrée **et** le gate qui l'exige, ou amender `D-22` pour y inscrire l'exception et son
motif. Sous ADR-031, cet exécuteur **n'a pas mandat de choisir** : il écrit l'écart et passe.

Un ledger qui coche par optimisme est pire qu'un ledger incomplet.

**2. Rejeu de TOUS les gates de sortie**, dans l'ordre, chacun **exécuté** et son résultat consigné
au SUMMARY — jamais supposé vert parce qu'il l'était au plan précédent :
- la suite du module (`test-dev-orchestrator.sh`) ;
- les cinq autres suites du module, dont la suite dédiée neuve ;
- le gate d'agents en mode strict sur les six dossiers d'agents du dépôt (ADR-044) ;
- le gate de synchronisation de version ;
- le gate d'intégrité de l'état de planification (module `conductor`) ;
- le gate d'alignement de configuration créé par cette phase, sur son propre lab ;
- la densité des agents (ADR-029, plafond 250) sur les deux agents touchés.

**Trois disciplines de mesure, non négociables — chacune ferme un faux vert vérifié par exécution
sur ce poste :**

- **Ne jamais lire un code de sortie à travers un tube.** Mesuré : `bash -c 'exit 7' | tail -2`
  rend **rc=0** — le `rc` du tube est celui de `tail`, pas celui de la suite, et une suite rouge
  passerait pour verte. Forme à utiliser pour **chaque** suite, sans exception :
  ```
  bash <suite> > /tmp/vf-2308-<nom>.log 2>&1; rc=$?; tail -2 /tmp/vf-2308-<nom>.log; echo "suite=$rc"
  ```
  Vérifiée dans les deux sens : `suite=7` sur une suite artificiellement rouge, `suite=0` sur une
  suite verte. **C'est le quatrième plan de la phase où ce motif est signalé** — le fermer veut
  dire l'appliquer à toutes les suites, pas à la première.

- **`check-agents.sh` n'accepte QUE la forme collée `--agents-dir=CHEMIN`.** Mesuré sur pièce : son
  `case` d'arguments ne porte que `--agents-dir=*` et **aucune branche `*)`** ; la forme espacée est
  donc **avalée en silence**, la cible retombe sur le défaut et le script sort en **rc=3** avec un
  message qui parle de `.claude/agents` — un faux diagnostic, et un critère « sort en 0 »
  **insatisfiable**. Valeurs mesurées : forme espacée → **rc=3** ; forme collée →
  **rc=0, « ✓ agents conformes · 7 warning(s) »**. Écrire la forme collée partout, et consigner
  **rc=0 et 7 warnings** au SUMMARY comme repère anti-régression (le nombre de warnings n'est pas
  gaté : s'il bouge, le dire, ne pas le corriger ici).

- **Densité : forme argument, jamais la redirection.** `wc -l < fichier` a rendu un faux `0` sur
  certains shells de ce poste (reproduit par deux exécutants, non reproduit ici ni par le manager),
  et `0 ≤ 250` est un critère **vert qui ment**. Utiliser `wc -l fichier1 fichier2` et **croiser par
  fichier** avec `awk 'END{print FILENAME": "FNR}' fichier` — `FNR` et jamais `NR`, qui cumulerait
  sur plusieurs fichiers. Attention aussi à la sortie de `wc` multi-fichiers sous proxy : elle est
  reformatée (basenames, ligne `Σ`), d'où le croisement `awk` fichier par fichier.

**3. Vérifier explicitement que la release racine n'a pas été entamée** — c'est un critère de
sortie, pas une formalité.

⚠️ **`git diff --name-only` nu ne prouve RIEN ici** : il mesure l'**arbre de travail non commité**,
mesuré **vide** (arbre propre) — il est donc **structurellement vert après commit**. Et aucune forme
de `git diff` n'est fiable *a priori* sur ce poste : les mêmes formes de plage rendent **49 chemins**
sous `rtk proxy` et **52 lignes** sans, l'écart étant **trois lignes injectées par le proxy** et non
trois fichiers. Conclusion opératoire : **ne jamais compter les lignes d'une sortie proxifiée**, et
faire porter le verdict sur le **contenu** et non sur le compte. Employer la garde en trois temps
donnée aux critères d'acceptation (fichier intermédiaire → **non-vacuité d'abord** → correspondance
exacte `awk` sur les chemins interdits) : l'absence d'un chemin interdit ne vaut rien tant que la
liste n'est pas prouvée non vide.

Ne doivent apparaître au diff de branche ni la `VERSION` racine, ni le manifeste de plugin, ni la
fiche de marketplace ; et ni les badges de version des deux README ni leurs tableaux d'historique de
release ne doivent avoir bougé. Aucun tag n'est créé, aucune release GitHub n'est publiée : ces
quatre gestes appartiennent à Samuel.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > /tmp/vf-2308-s1.log 2>&1; rc=$?; tail -2 /tmp/vf-2308-s1.log; echo "suite-dev-orchestrator=$rc"; bash plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh > /tmp/vf-2308-s2.log 2>&1; rc=$?; tail -2 /tmp/vf-2308-s2.log; echo "suite-check-gsd-config=$rc"; bash scripts/check-version-sync.sh >/dev/null 2>&1; echo "version-sync=$?"; bash plugin/conductor/scripts/check-agents.sh --agents-dir=plugin/dev-orchestrator/agents --strict >/dev/null 2>&1; echo "agents=$?"; bash plugin/conductor/scripts/check-state-integrity.sh >/dev/null 2>&1; echo "state-integrity=$?"; bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path . >/dev/null 2>&1; echo "gsd-config=$?"; wc -l plugin/dev-orchestrator/agents/vf-dev-manager.md plugin/dev-orchestrator/agents/vf-coder.md; awk 'END{print FILENAME" FNR="FNR}' plugin/dev-orchestrator/agents/vf-dev-manager.md; awk 'END{print FILENAME" FNR="FNR}' plugin/dev-orchestrator/agents/vf-coder.md</automated>
    <human-check>Recette de clôture, en trois points. (1) Confirmer l'arbitrage du Finding 1 tel qu'appliqué au plan 23-05 (retrait de l'entrée d'allowlist de planification chez le manager) — le geste inverse coûte une ligne. (2) Confirmer la valeur retenue pour le toggle d'interface au plan 23-02 (fermé explicitement, ce dépôt n'ayant aucune surface d'interface). (3) Prendre acte que la release racine reste à faire à la main : bump de la VERSION racine, du manifeste de plugin, de la fiche de marketplace, des badges et de l'historique des deux README, puis tag annoté et release GitHub, puis `bash scripts/check-release-tag.sh --remote` qui doit sortir ✓.</human-check>
  </verify>
  <acceptance_criteria>

**A. Critères de LIVRAISON — ce que cette tâche fait bouger** (mesurés **non satisfaits** sur arbre
intact le 2026-08-04 ; ce sont les seuls qui prouvent le travail de cette tâche) :

    - Les **dix** puces de définition `GSDC-01` à `GSDC-10` ne sont plus vides : `awk '/^- \[ \] \*\*GSDC-/{n++} END{if(n!=0) exit 1}' .planning/REQUIREMENTS.md` sort en **0** (mesuré avant : **10** puces `- [ ]`).
    - `GSDC-08` porte `[~]` avec sa raison écrite (volet dispatch couvert, volet allowlist ouvert sur `D-22`) : `awk '/^- \[~\] \*\*GSDC-08/{n++} END{if(n!=1) exit 1}' .planning/REQUIREMENTS.md` sort en **0**.
    - Les trois lignes de traçabilité `GSDC-08`, `GSDC-09`, `GSDC-10` ne portent plus l'état de planification : `awk '/^\| GSDC-(08|09|10) \|/ && /Planned/{exit 1}' .planning/REQUIREMENTS.md` sort en **0** (mesuré avant : les trois en `Planned`).
    - La ligne de traçabilité de `GSDC-08` **ne dit pas** une couverture pleine : elle nomme la partialité et renvoie à l'écart `D-22`. Critère de lecture, pas de grep — le vérifier à l'œil et le recopier au SUMMARY.

**B. Rejeu de NON-RÉGRESSION — vert d'avance, à rejouer quand même** (tous mesurés **déjà
satisfaits** sur arbre intact le 2026-08-04 : ils ne prouvent aucun travail, ils prouvent qu'aucun
dégât n'a été fait. Les consigner au SUMMARY **sous ce libellé**, jamais comme des livrables) :

    - `.planning/REQUIREMENTS.md` contient les dix identifiants `GSDC-01` à `GSDC-10` en puce de définition **et** en ligne de traçabilité — `awk '/GSDC-/{n++} END{print "occurrences="n+0}'` (mesuré : **24** occurrences, les 4 au-delà de 20 vivant dans le paragraphe de couverture des zones de décision : ne pas lire « 24 » comme une anomalie).
    - Chaque ligne de traçabilité `GSDC-*` nomme le ou les plans qui la couvrent (mesuré : les dix le font déjà).
    - La section `**Coverage:**` porte sa ligne Phase 23 avec le décompte et `0 non-mappé` (mesurée **déjà présente et déjà juste** — ne la réécrire que si le décompte réel diffère).
    - Les **sept** suites, chacune rejouée avec capture du `rc` **hors tube** (voir la forme donnée à l'action, point 2) : `test-dev-orchestrator.sh`, `test-check-gsd-config.sh`, `test-check-doc-drift.sh`, `test-check-dev-bootstrap.sh`, `test-check-gsd-engine.sh`, `test-discover-unintegrated-docs.sh`, `test-inject-mcp-tools.sh` → `suite=0` et `0 KO` pour chacune. **Ne pas se fier à un `| tail -2`** : mesuré, il rend `rc=0` sur une suite qui sort en 7.
    - `bash scripts/check-version-sync.sh` sort en **0**. ⚠️ Ce gate est le **seul discriminant rouge avant ce plan** (rc=1) : il appartient en réalité aux critères de livraison de la **Tâche 1** et n'est ici qu'un rejeu.
    - `bash plugin/conductor/scripts/check-agents.sh --agents-dir=plugin/dev-orchestrator/agents --strict` sort en **0** avec **7 warning(s)**. **Forme collée obligatoire** : mesuré, la forme espacée sort en **rc=3** sur la mauvaise cible, rendant le critère insatisfiable.
    - `bash plugin/conductor/scripts/check-state-integrity.sh` sort en **0** (mesuré : rc=0, compteurs non régressés au sein du jalon).
    - `bash plugin/dev-orchestrator/scripts/check-gsd-config.sh --path .` sort en **3** (mesuré : rc=3, silence — ce lab est aligné).
    - Densité ADR-029 : `wc -l plugin/dev-orchestrator/agents/vf-dev-manager.md plugin/dev-orchestrator/agents/vf-coder.md` **en forme argument** (jamais `wc -l < fichier`, qui a rendu un faux `0` sur certains shells de ce poste), croisé par fichier avec `awk 'END{print FILENAME" FNR="FNR}'`. Valeurs mesurées : **245** et **96**, les deux ≤ 250. Marge de **5 lignes** sur `vf-dev-manager.md` — voir la clause de déport ci-dessous.
    - **Garde de non-entame de la release racine** — même construction en trois temps qu'en Tâche 1, **non-vacuité d'abord** :
      ```
      MB=$(rtk proxy git merge-base origin/main HEAD)
      rtk proxy git diff --name-only "$MB" HEAD > /tmp/vf-2308-bd.txt
      awk 'END{if(NR<10) exit 1}' /tmp/vf-2308-bd.txt
      awk '$0=="VERSION"||$0=="plugin/.claude-plugin/plugin.json"||$0==".claude-plugin/marketplace.json"{exit 1}' /tmp/vf-2308-bd.txt
      ```
      Les **deux** `awk` sortent en **0**. Ne **jamais** remplacer par `git diff --name-only` nu (mesure l'arbre de travail, mesuré vide, donc vert par construction) ni par un `grep -cE … = 0` (vert sur liste vide, et `grep` est proxifié et tronque). État mesuré : **49 chemins**, aucun des trois interdits.
    - `rtk proxy git tag --points-at HEAD | awk 'END{if(NR!=0) exit 1}'` sort en **0** — aucun tag créé par l'exécution (mesuré : 0 tag).
  </acceptance_criteria>
  <clause_de_deport>
**Densité ADR-029 — marge mesurée : 5 lignes.** `vf-dev-manager.md` est à **245/250**,
`vf-coder.md` à **96/250**. Ce plan **ne touche aucun fichier d'agent** : le risque ne devrait pas
se matérialiser. Clause posée par sécurité — si une correction imprévue devait faire passer un
agent au-dessus de 250, **ne pas compresser le contenu pour rentrer sous le plafond** : déporter la
matière vers le fichier de référence du module (`references/`) et laisser un renvoi d'une ligne
dans l'agent, conformément au principe « par renvoi jamais par copie » déjà appliqué dans ce
module. Si le déport n'est pas évident, **s'arrêter et remonter** plutôt que d'arbitrer.
  </clause_de_deport>
  <done>Le ledger dit ce que la phase couvre réellement — y compris là où il ne couvre que partiellement — chaque gate de sortie a été rejoué avec son code de sortie capturé hors tube, et la release racine reste intacte et disponible pour la validation humaine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase → release racine | Frontière de **gouvernance** : le bump du module est un geste de phase, la release racine (bump, badges, tag annoté, release GitHub) appartient à l'humain. Franchir cette frontière en autonomie serait s'accorder un morceau de release explicitement sorti du périmètre. |
| disque → compteurs annoncés dans les README | Le nombre de suites réellement présentes sur disque est la vérité ; le nombre annoncé est une affirmation. Le gate est ce qui les rapproche. |
| SUMMARY des sept plans → CHANGELOG | Le CHANGELOG doit décrire ce qui a été **livré**, pas ce qui était prévu — la frontière est la lecture des SUMMARY plutôt que des PLAN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08-01 | Elevation of Privilege | release racine entamée en autonomie | high | mitigate | Périmètre écrit dans l'action, et **vérifié machine** en critère d'acceptation : aucun des trois fichiers de version racine dans le diff de branche, aucun tag créé. Précédent des Phases 13, 17, 19, 22. |
| T-23-08-02 | Spoofing | compteur de suites affirmant une valeur que le disque ne porte pas | medium | mitigate | Valeur établie par **exécution et par deux méthodes croisées** (`find … \| awk 'END{print NR}'` et `git ls-files` filtré en `awk`, écart nul exigé) et non par confiance dans le chiffre annoncé — **jamais `grep -c`**, proxifié et tronquant sur ce poste. `check-version-sync.sh` la revérifie, mais **ne lit que la première occurrence de chaque README** : le relevé `awk` de toutes les occurrences couvre cet angle mort. Le gate n° 9 existe précisément parce que ce compteur avait déjà menti (36 annoncé pour 37 réels). |
| T-23-08-03 | Repudiation | historique de release réécrit en corrigeant les compteurs datés | medium | mitigate | Instruction explicite : ne toucher qu'aux mentions vivantes (encadré CI, puce d'auditabilité), jamais aux lignes du tableau d'historique — ce sont des constats datés. |
| T-23-08-04 | Spoofing | ledger d'exigences cochant par optimisme | medium | mitigate | Statuts dérivés des SUMMARY et non des PLAN, et instruction explicite d'écrire les couvertures partielles avec leur raison, sur le patron `[~]` déjà présent au ledger. |
| T-23-08-05 | Repudiation | justification de retrait d'allowlist perdue | medium | mitigate | La justification des deux retraits est portée par le CHANGELOG (critère d'acceptation dédié) précisément parce que le plan 23-05 interdit de la laisser dans les fichiers d'agents — sans cette reprise ici, elle disparaîtrait. |
| T-23-08-06 | Denial of Service | gate supposé vert au lieu d'être rejoué | high | mitigate | Onze commandes de gate listées, chacune exécutée et son code de sortie consigné au SUMMARY. Le dépôt a trois précédents documentés de faux vert derrière un décompte (Phases 13, 17, 19) : la conduite est le rejeu, jamais la relecture. |
| T-23-08-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan. |
</threat_model>

<verification>

**Ce qui bouge (livraison) :**
- Triade du module cohérente sur les trois fichiers, valeur mineure suivante.
- CHANGELOG écrit depuis les SUMMARY, nommant les quatre artefacts neufs, portant la justification des deux retraits d'allowlist, et un **compte de blocs de test re-dérivé** et non recopié.
- Compteur de suites égal à la valeur constatée sur disque, dans les deux README, **sur la seule mention vivante de chaque fichier** — relevé complet par `awk`, jamais par le seul gate.
- Ledger : les dix puces cochées, `GSDC-08` en `[~]` avec sa raison, les trois traçabilités sorties de l'état de planification.

**Ce qui ne bouge pas (rejeu de non-régression, vert d'avance) :**
- Les sept suites, chacune avec son `rc` **capturé hors tube**.
- Gate d'agents en **forme collée**, gate d'intégrité d'état, gate d'alignement de configuration.
- Densité ADR-029 en **forme argument**, croisée par fichier.
- Release racine intacte : garde de diff en trois temps, **non-vacuité prouvée avant** la recherche des chemins interdits, et absence de tag.

**Discipline transverse :** aucun verdict ne repose sur un `grep -c`, sur un `| tail`, sur `wc -l <`,
ni sur le comptage de lignes d'une sortie proxifiée. Chacune de ces quatre formes a produit un faux
vert vérifié sur ce poste.
</verification>

<success_criteria>
- SC1 — `dev-orchestrator` porte sa version mineure suivante, triade complète, CHANGELOG à jour et fidèle au livré.
- SC2 — `check-version-sync.sh` sort ✓ (**rc=1 avant ce plan** — le seul discriminant machine de la tâche), compteur de suites aligné sur le disque sur la seule mention vivante de chaque README, historique de release non réécrit.
- SC3 — Le ledger porte les dix identifiants **cochés**, avec leur traçabilité vers les plans, et **écrit `GSDC-08` en couverture partielle** plutôt que de le certifier conforme.
- SC4 — Les gates de sortie sont verts, **rejoués** et consignés — chacun avec son code de sortie **capturé hors tube**, et **étiquetés livraison ou non-régression** au SUMMARY. Un gate vert d'avance n'est pas une preuve de travail.
- SC5 — La release racine n'a pas été entamée : garde prouvée **non vide avant** d'être déclarée propre, aucun fichier de version racine touché, aucun tag créé, la main reste à Samuel.
- SC6 — **L'écart `D-22` sort de la phase nommé et non tranché.** Il figure au SUMMARY en arbitrage ouvert, `gsd-debugger` est toujours dans l'allowlist de `vf-coder.md` et dans `CODER_ALLOWED`, et aucune des deux branches de l'arbitrage n'a été prise par l'exécuteur.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-08-SUMMARY.md` à la fin.

**C'est le SUMMARY de clôture de la phase** : ce qui n'y est pas repris sort du champ de vision au
moment de la PR. Rubriques obligatoires :

1. **Table des gates**, chacun avec son code de sortie **capturé hors tube**, et chacun **étiqueté
   `livraison` ou `non-régression (vert d'avance)`**. Le tableau doit rendre visible que le seul
   gate rouge avant ce plan était `check-version-sync.sh` : présenter neuf gates verts d'avance
   comme des preuves de travail serait un faux bilan.
2. **Valeur constatée du compteur de suites**, avec les deux méthodes de mesure et leur écart, et le
   décompte `vivantes / datées` par README avant et après.
3. **Liste exacte des dix identifiants d'exigence et leur statut réel**, `GSDC-08` inclus avec sa
   partialité écrite.
4. **🛑 Arbitrages ouverts, reportés — rubrique obligatoire même si elle doit rester courte.** Cette
   phase ne se clôt pas sur une ardoise nette et le SUMMARY doit le porter, sans quoi l'écart
   disparaît à la PR :
   - **Écart `D-22`** — `vf-coder.md` ligne `tools:` porte `gsd-debugger`, ce que `D-22` interdit ;
     le plan 23-07 a couvert le volet dispatch et **pas** le volet allowlist. L'entrée est
     simultanément **exigée** par `CODER_ALLOWED` (bloc `T19`), donc la retirer casse la suite.
     Arbitrage de Samuel, **deux branches, aucune prise** : retirer l'entrée **et** le gate qui
     l'exige, ou amender `D-22` pour y inscrire l'exception et son motif. Reporter le renvoi vers la
     section dédiée du SUMMARY de 23-07.
   - **Dette de clôture de la Phase 22** — les sept puces `DOCF-01..07` sont restées `- [ ]` et leur
     traçabilité `Planned` alors que la phase est close. Constat, **hors périmètre de ce plan**, à
     solder par ailleurs.
   - Tout autre arbitrage rencontré en exécution et non tranché : l'écrire ici plutôt que de choisir.
5. **En tête, pour Samuel — check-list des gestes de release racine restant à faire à la main** :
   bump de la triade racine et des deux historiques de README, tag annoté, release GitHub,
   `bash scripts/check-release-tag.sh --remote` → ✓. Ces gestes sont **hors périmètre de cette
   phase** (T-23-08-01) et n'ont pas été entamés.
</output>

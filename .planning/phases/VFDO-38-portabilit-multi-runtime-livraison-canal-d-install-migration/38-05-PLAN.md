---
phase: 38-portabilit-multi-runtime-livraison-canal-d-install-migration
plan: 05
type: execute
wave: 5
depends_on: ["38-04"]
files_modified:
  - plugin/_internal/runtime-adapter/agent-to-codex.mjs
  - plugin/_internal/runtime-adapter/register-codex-agent.sh
  - plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh
  - plugin/conductor/references/team-kernel.md
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
autonomous: false
requirements: [ADPT-01, ADPT-02, ADPT-03, ADPT-04]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un artefact VibeFlow réel, posé via cet adaptateur, devient un rôle Codex RÉELLEMENT dispatchable (chargé par `codex doctor --json`, invocable par `agent_type`) — jamais un fichier qui ressemble à un rôle sans en être un (piège n°1 mesuré : rôle malformé = ignoré en silence)"
    - "Le digest produit par l'adaptateur nomme, champ par champ, ce qui est PRÉSERVÉ / PERDU / EN ATTENTE — jamais une case vide qui laisserait deviner"
    - "Le mapping de nommage à 31 entrées n'est PAS construit (l'inconnu #3 est CONFIRMÉ : les tirets passent au dispatch) — seule une normalisation d'UNE ligne du `task_name` de spawn existe"
    - "Aucune contrainte `fork_turns` n'est ajoutée aux spawns générés (l'inconnu #5 est CONFIRMÉ : `fork_turns:\"all\"` n'écrase pas le `model` du rôle) — la promesse « un modèle par worker » est documentée comme tenant SANS contrainte"
    - "Le confinement des juges (vf-reviewer/vf-design-judge) sur Codex N'EST PAS affirmé comme fonctionnel — `sandbox_mode`/`approval_policy` au niveau rôle sont mesurés INERTES (un rôle read-only a réellement écrit sur disque en session réelle) ; ce lot pose les rôles SANS revendiquer de garantie d'écriture, et escalade la décision d'architecture à Samuel plutôt que de la trancher"
  artifacts:
    - "plugin/_internal/runtime-adapter/agent-to-codex.mjs : conversion pure (frontmatter+corps Markdown -> chaîne TOML), sans effet de bord disque, testable en isolation"
    - "plugin/_internal/runtime-adapter/register-codex-agent.sh : orchestration (résolution CODEX_HOME, appel du convertisseur, écriture sous agents/vibeflow/, vérification via codex doctor --json)"
  key_links:
    - "agent-to-codex.mjs -> mapping ALIGNÉ sur /import de Codex 0.150.1 (mesuré : permissionMode -> sandbox_mode, corps markdown -> developer_instructions, effort -> model_reasoning_effort) — jamais un mapping inventé, 38-CONTEXT.md lignes 346-357"
    - "register-codex-agent.sh -> $CODEX_HOME/agents/vibeflow/<name>.toml (SEULE surface d'écriture, jamais [agents.<n>] de config.toml — doctrine mesurée 38-CONTEXT.md lignes 359-373)"
    - "register-codex-agent.sh -> codex doctor --json (ADPT-04, COMPTE les rôles chargés — jamais «pas de crash donc c'est bon», piège n°1 mesuré)"
    - "vibeflow-update.sh install_module()/update_module() -> register-codex-agent.sh : appelé best-effort UNIQUEMENT quand le runtime détecté (via runtime-cli-dispatch.sh du lot 2) est codex ET que le module posé porte un agent"
  prohibitions:
    - "Ne jamais construire la table de correspondance de nommage des 31 agents — INUTILE, l'inconnu #3 est confirmé, les tirets passent au dispatch (`agent_type`). Seul le `task_name` (segment de chemin `/root/<task_name>`) a besoin d'une normalisation snake_case, en UNE ligne, jamais une table"
    - "Ne jamais ajouter de contrainte `fork_turns:\"none\"` aux invocations de spawn — l'inconnu #5 est confirmé, `fork_turns:\"all\"` n'écrase PAS le `model` du rôle (mesuré en base sur les deux valeurs)"
    - "Ne jamais écrire `disallowedTools`/`vf-internal` sous une clé inventée dans `[tools]` pour SIMULER une restriction — les clés inconnues SOUS `[tools]` passent SANS AUCUN warning (piège n°2 mesuré), produire un agent qui PARAÎT restreint sans l'être est pire que de déclarer la perte"
    - "Ne jamais affirmer, dans le digest ou ailleurs, que `sandbox_mode`/`approval_policy` au niveau du fichier de rôle fournissent un confinement réel — mesuré FAUX en session réelle (rôle read-only a écrit sur disque). La question du confinement par rôle reste ENTIÈREMENT OUVERTE (voir tâche 3, checkpoint) tant que `[permissions]` n'est pas mesuré par ailleurs"
    - "Ne jamais écrire dans `[agents.<n>]` de `config.toml` (aucune commande `codex config` n'existe, risque de mutiler des tables voisines) — la seule surface d'écriture autorisée est `$CODEX_HOME/agents/vibeflow/*.toml`"
---

<objective>
Lot 5 (`ADPT`) du cadrage 38-CONTEXT.md, implémentant D-38-D (« adaptateur VibeFlow minimal »,
recommandation du spike Phase 37, position confirmée : préservation de
model/memory/tools/disallowedTools/vf-internal/allowlist, digest explicite — sans dépendance à la
surface interne gsd-core sans test de contrat de dérive) : l'adaptateur VibeFlow minimal qui écrit un rôle Codex
RÉELLEMENT dispatchable (mesuré, pas supposé), avec un digest explicite champ par champ. Séquencé
APRÈS TGT (lot 4) : ordre imposé par REQUIREMENTS.md ET partage réel de
`plugin/_internal/vibeflow-update.sh`/`plugin/conductor/VERSION`/`CHANGELOG.md`. Dépend aussi,
logiquement (pas par fichier), du nœud `probe-codex` — RÉSOLU le 2026-08-28 en session Codex
réelle : 38-CONTEXT.md porte désormais la section « Sonde en session RÉELLE — les 7 inconnus levés »
(lignes 408-505), qui EST la source de vérité de ce plan.

**Correction de cadrage intégrée (post-sonde réelle, remplace les hypothèses du banc isolé)** :
1. Le mapping de nommage des 31 agents est ABANDONNÉ — l'inconnu #3 est CONFIRMÉ (les tirets
   passent au dispatch), un poste de travail entier est économisé. Seule survit une normalisation
   d'UNE ligne du `task_name` de spawn.
2. Aucune contrainte `fork_turns` n'est ajoutée — l'inconnu #5 est CONFIRMÉ (`"all"` n'écrase pas
   `model`), le risque n°1 de la phase est écarté.
3. Le confinement des juges par `sandbox_mode`/`approval_policy` au niveau RÔLE, présenté comme
   acquis par le brief initial, est mesuré FAUX (défaut majeur #7b) — ce lot pose les rôles SANS
   cette garantie et ESCALADE la décision d'architecture (tâche 3, checkpoint bloquant) plutôt que
   de la trancher ou de l'assumer dans un sens ou l'autre.

Purpose: sans cet adaptateur, tout ce que les lots précédents ont rendu ATTEIGNABLE (install
multi-runtime, rollback fiable, cible injectable) reste sans destination réelle sur Codex — les
rôles VibeFlow n'existeraient toujours qu'en markdown Claude, jamais en `.toml` chargé.

Output: convertisseur + orchestrateur de pose, digest champ par champ, non-régression, décision de
confinement escaladée à Samuel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-CONTEXT.md
@plugin/content-bundle/agents/vf-content-writer.md
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : bout-en-bout — un agent réel, un rôle Codex réellement chargé (ADPT-01, ADPT-04)</name>
  <files>plugin/_internal/runtime-adapter/agent-to-codex.mjs, plugin/_internal/runtime-adapter/register-codex-agent.sh, plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh</files>
  <read_first>
    - 38-CONTEXT.md lignes 273-317 (les 4 surfaces Codex A-D, les 3 pièges de dégradation silencieuse — surtout le piège n°1 : « un rôle malformé n'est pas fatal, il est IGNORÉ » avec un simple `startup warning` invisible en usage normal → « le gate de l'adaptateur DOIT être `codex doctor --json` et COMPTER les rôles chargés »)
    - 38-CONTEXT.md lignes 293-303 (« Champs requis — mesurés un par un » : `name`, `description`, `developer_instructions` sont les TROIS requis, mesurés sur le binaire 0.150.1 — contradiction doc/binaire tranchée en faveur du binaire. Le reste est `#[serde(flatten)] ConfigToml` : toute clé valide de `config.toml` acceptée. `deny_unknown_fields` actif À LA RACINE)
    - 38-CONTEXT.md lignes 346-357 (« Découverte qui change le cadrage — OpenAI a déjà écrit la table de conversion » : mapping mesuré dans le binaire lui-même — `permissionMode -> sandbox_mode`, `corps markdown -> developer_instructions`, `effort -> model_reasoning_effort`, AUCUNE trace de `tools`/`disallowedTools`. « L'adaptateur s'ALIGNE sur ce mapping plutôt que d'en inventer un — c'est celui de l'éditeur »)
    - 38-CONTEXT.md lignes 359-373 (doctrine de pose : `$CODEX_HOME/agents/vibeflow/*.toml` OUI, `[agents.<n>]` de config.toml NON — scan récursif, pose idempotente, désinstallation par `rm -rf`, zéro octet touché dans `config.toml`)
    - plugin/content-bundle/agents/vf-content-writer.md:1-8 (fixture réelle : `tools: Read, Write, Glob, Grep`, `model: sonnet`, `effort: medium`, `memory: project`, `vf-internal: true` — worker interne, PAS un juge, choisi précisément pour ne pas mélanger ce tracer avec la question de confinement encore ouverte en tâche 3)
    - plugin/conductor/scripts/tests/fixtures/ (lister le contenu avant d'écrire — patron de fixtures déjà en usage dans ce dépôt pour les suites `conductor`, à réutiliser plutôt qu'un nouveau format)
  </read_first>
  <action>
Créer `plugin/_internal/runtime-adapter/agent-to-codex.mjs` (Node, zéro dépendance npm) exportant une
fonction pure `convertAgentToCodexRole(sourceMarkdown) -> { toml, digest }` :
- Découper `sourceMarkdown` en frontmatter + corps (même motif `---` déjà utilisé côté bash dans ce
  dépôt).
- `name` (frontmatter) -> `name` TOML (chaîne simple).
- `description` (frontmatter) -> `description` TOML (chaîne simple — échapper guillemets/retours
  ligne si présents).
- Corps Markdown (hors frontmatter) -> `developer_instructions` TOML, forme chaîne multi-ligne
  `"""..."""` — échapper toute séquence `"""` interne au corps source (remplacer par une forme
  équivalente sans tripler les guillemets, jamais tronquer le corps).
- `model` (frontmatter, si présent) -> `model` TOML.
- `effort` (frontmatter, si présent) -> `model_reasoning_effort` TOML — MÊME mapping que `/import`
  de Codex, jamais un nom de champ inventé.
- `memory` (frontmatter, si présent) -> digest `LOST` (schéma rejette `memory`, mesuré ; cohérent
  avec D-37-2 — la mémoire d'agent par projet est une capacité spécifique à Claude Code, une perte à
  déclarer par le gate, jamais un trou à combler) — PAS écrit en TOML.
- `tools`/`disallowedTools` (frontmatter, si présents) -> digest `PENDING` — PAS écrits en TOML,
  ni sous `[tools]` (piège n°2), ni ailleurs — la clé `[permissions]` reste NON mesurée à ce jour
  (voir tâche 3).
- `vf-internal: true` (frontmatter, si présent) -> digest `PRESERVED_BY_OMISSION` — l'adaptateur
  n'écrit JAMAIS de `[agents.<n>]`/nickname dans `config.toml`, donc AUCUN rôle posé par cet
  adaptateur n'obtient de raccourci d'invocation utilisateur, qu'il soit `vf-internal` ou non.

Créer `plugin/_internal/runtime-adapter/register-codex-agent.sh`, usage
`register-codex-agent.sh <agent.md> [--codex-home <chemin>]` : résout `CODEX_HOME` (`--codex-home`
sinon `${CODEX_HOME:-$HOME/.codex}`), appelle `agent-to-codex.mjs` via `node`, écrit le `.toml`
résultant sous `$CODEX_HOME/agents/vibeflow/<name>.toml` (créer le sous-dossier si absent,
écriture idempotente — un rôle déjà posé est ÉCRASÉ, jamais dupliqué), imprime le digest sur
stdout, une ligne par champ.

Vérification RÉELLE (ADPT-04) : sur un `CODEX_HOME` de BANC ISOLÉ (`mktemp -d`, JAMAIS
`~/.codex` réel — même méthodologie que la sonde `probe-codex` du cadrage, byte-identique
préservé), après avoir posé le rôle, exécuter `codex doctor --json` avec ce `CODEX_HOME` isolé et
compter les rôles listés/chargés dans la sortie JSON. Le compte DOIT inclure le rôle posé — sinon
`register-codex-agent.sh` sort non-zéro avec un message explicite (jamais un succès supposé sur la
seule absence d'erreur d'écriture disque).

Créer `plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh` sur le patron des suites du
dépôt (`set -uo pipefail`, `mktemp -d` + `trap`, `ok()`/`ko()`/`skip()`, `T<n>.<m>`, bannière). Cas :
T1 (conversion de `vf-content-writer.md` réel -> `.toml` contenant `name`, `description`,
`developer_instructions`, `model`, `model_reasoning_effort`), T2 (le corps Markdown source
apparaît intégralement dans `developer_instructions`, pas tronqué — comparer par sous-chaîne),
T3 (`memory`/`tools` du frontmatter source N'apparaissent PAS dans le `.toml`, digest les marque
`LOST`/`PENDING`), T4 (rejeu de la pose sur un `CODEX_HOME` isolé où `codex` est réellement présent
sur ce poste : `codex doctor --json` compte bien le rôle — test RÉEL, pas simulé, skip proprement
si `codex` absent du `PATH` d'exécution CI).
  </action>
  <acceptance_criteria>
    - `node plugin/_internal/runtime-adapter/agent-to-codex.mjs` (invoqué en mode test unitaire par la suite) convertit `vf-content-writer.md` en un `.toml` contenant `name = "vf-content-writer"` et une clé `developer_instructions` — vérifié par lecture réelle du fichier produit.
    - Le digest imprimé par `register-codex-agent.sh` contient explicitement `memory: LOST` et `tools: PENDING` — jamais une case absente pour ces deux champs.
    - Sur un `CODEX_HOME` de banc isolé avec le binaire `codex` réel de ce poste, `codex doctor --json` COMPTE le rôle posé — capturé et vérifié sur une exécution réelle, jamais affirmé sans elle. `~/.codex` réel reste byte-identique avant/après (sha256 comparé).
    - `bash plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh` sort 0, bannière `== résultat :`.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh</automated>
  </verify>
  <done>Un agent VibeFlow réel devient un rôle Codex réellement chargé et compté par `codex doctor --json`, avec un digest qui déclare LOST/PENDING/PRESERVED champ par champ — jamais un rôle qui « a l'air » posé sans être dispatchable, et `~/.codex` réel n'a jamais été touché.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : normalisation task_name (une ligne) + wiring install + non-régression (ADPT-02, ADPT-03)</name>
  <files>plugin/conductor/references/team-kernel.md, plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh, plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - 38-CONTEXT.md lignes 427-437 (« #3 — un poste de travail entier économisé » : erreur mesurée verbatim `agent_name must use only lowercase letters, digits, and underscores` sur `task_name="t-ro-dash"` — parce que `task_name` devient un SEGMENT DE CHEMIN (`/root/<task_name>`). « VibeFlow garde ses 31 noms à tirets tels quels. Seul le `task_name` d'invocation se normalise en snake_case : une ligne de code, pas une table de correspondance de 31 entrées »)
    - 38-CONTEXT.md lignes 439-443 (« #5 — le risque n°1 est écarté » : modèle enfant réellement enregistré en base en `fork_turns:"none"` ET `"all"` — `fork_turns` porte l'historique de conversation, pas la configuration. « un modèle par worker » tient sans contrainte de spawn)
    - plugin/conductor/references/team-kernel.md:105-189 (« Ce que chaque métier paramètre » / « Règles d'instanciation » : section transverse, lue par TOUS les orchestrateurs VibeFlow qui dispatchent des sous-agents — emplacement correct pour une règle de normalisation qui s'applique à tout spawn, quel que soit le métier, jamais une règle dupliquée par agent)
    - plugin/_internal/vibeflow-update.sh:1616-1629 (point de couture déjà établi par le lot 1 (FIDE) juste après `seed_module_registres`/`generate_agent_command_for` — le lot 5 y ajoute l'appel best-effort à `register-codex-agent.sh`, au MÊME point, jamais un nouveau point d'insertion séparé)
  </read_first>
  <behavior>
    - Test : la règle ajoutée à `team-kernel.md` normalise un exemple `task_name` porteur de tirets/majuscules (`t-RO-Dash`) en `t_ro_dash` — vérifié par une suite qui grep la règle et applique le motif décrit sur un cas fixture (bash `tr`/`sed`, la MÊME transformation que la règle documente).
    - Test : `install_module` d'un module qui pose un `AGENT.md` réel, sur un poste où le runtime détecté (via `runtime-cli-dispatch.sh` du lot 2) est `codex`, appelle `register-codex-agent.sh` — vérifié par grep du digest `[codex-adapter]` sur la sortie stdout de l'install.
    - Test : le MÊME install, sur un poste où le runtime détecté est `claude`, N'APPELLE PAS `register-codex-agent.sh` — non-régression explicite, pas seulement le cas positif.
  </behavior>
  <action>
Dans `plugin/conductor/references/team-kernel.md`, section « Règles d'instanciation » (119+) :
ajouter une règle courte, cohérente avec le style transverse du fichier — « Sur Codex, le
`task_name` passé à `spawn_agent` DOIT être normalisé en snake_case (minuscules, `[a-z0-9_]+`
strict — c'est un segment de chemin `/root/<task_name>`, PAS le nom du rôle `agent_type` qui, lui,
garde ses tirets tels quels). Une ligne, jamais une table de correspondance : `tr 'A-Z-' 'a-z_'`
sur le `task_name` avant spawn, ou équivalent. » — citer 38-CONTEXT.md comme source de la mesure,
comme le fait déjà le reste du fichier pour ses autres invariants mesurés.

Dans `vibeflow-update.sh`, au point de couture 1616-1629 (déjà établi par FIDE) : ajouter un appel
best-effort à `register-codex-agent.sh` (résolu par la MÊME cascade à 3 positions que
`check-artifact-fidelity.sh`, cf. 38-01-PLAN.md), UNIQUEMENT si (a) le module posé a produit un
`AGENT.md` ou `agents/*.md` ET (b) le runtime détecté par `runtime-cli-dispatch.sh` (lot 2) est
`codex`. Script introuvable ou runtime non-codex → silence total (même doctrine que les autres
sondes best-effort de ce fichier). Relayer le digest TEL QUEL sur stdout, préfixé `[codex-adapter]`
— jamais résumé.

Documenter, dans le digest ou un commentaire adjacent (choix cohérent avec le reste du fichier) :
« un modèle par worker tient SANS contrainte `fork_turns` — mesuré 38-CONTEXT.md #5, aucune action
requise ici ».

Bump `plugin/conductor/VERSION` (minor — nouvelle capacité, l'adaptateur Codex) + `CHANGELOG.md`.

Rejouer la découverte complète des suites (cette tâche n'ajoute PAS de nouveau fichier de suite —
`test-agent-to-codex.sh` a été créé en tâche 1 déjà comptée dans ce même plan ; confirmer le
compte final par la commande de découverte, jamais supposé).
  </action>
  <acceptance_criteria>
    - `grep -n 'task_name' plugin/conductor/references/team-kernel.md | grep -c .` ≥ 1, et la règle cite explicitement `[a-z0-9_]+`/snake_case.
    - Fixture install sur runtime `codex` détecté : sortie stdout contient une ligne `[codex-adapter]` — grep sur sortie réelle capturée.
    - Fixture install sur runtime `claude` détecté : AUCUNE ligne `[codex-adapter]` dans la sortie — grep -v assertant l'absence.
    - `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c .` reflète bien +1 suite depuis le lot 4 (`test-agent-to-codex.sh`), consigné dans le SUMMARY avec le chiffre exact dérivé.
    - `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh && bash plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh</automated>
  </verify>
  <done>Le `task_name` de spawn se normalise en une ligne documentée (jamais une table), l'adaptateur se déclenche uniquement sur runtime codex détecté, et la non-régression complète reste verte.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Confinement des juges Codex (`vf-reviewer`/`vf-design-judge`) — quelle architecture, une fois `[permissions]` mesuré ?</decision>
  <context>
    38-CONTEXT.md (défaut #7b, lignes 463-476) a mesuré en session Codex RÉELLE que `sandbox_mode`
    et `approval_policy` déclarés dans un fichier de rôle sont acceptés par le schéma, VALIDÉS, puis
    JAMAIS appliqués au spawn — un rôle `sandbox_mode = "read-only"` a réellement ÉCRIT sur disque.
    Le confinement ne fonctionne qu'AU NIVEAU SESSION (`-s`/`sandbox_mode` racine), donc uniforme
    pour TOUS les workers d'une même session — un `vf-reviewer` VibeFlow, juge à barrière
    d'écriture sur Claude (ADR-044), écrirait dans le dépôt sur Codex si rien ne change.

    Un candidat plus prometteur — `[permissions]` dans le fichier de rôle, accepté par le schéma —
    reste NON TESTÉ à la clôture de ce cadrage (« Tant que ce n'est pas mesuré, le confinement par
    rôle est à considérer comme INEXISTANT »). Un autre nœud de la mission mesure ce candidat en
    parallèle ; son résultat n'était pas connu à l'écriture de ce plan.

    Ce plan pose déjà les rôles VibeFlow (tâches 1-2) SANS revendiquer aucune garantie de
    confinement — le digest de la tâche 1 ne mentionne `sandbox_mode`/`approval_policy` que comme
    acceptés-mais-inertes, jamais comme une protection. Cette décision ne bloque PAS la pose des
    rôles non-juges ; elle bloque uniquement l'AFFIRMATION d'une garantie d'écriture pour les juges
    sur Codex, jusqu'à ce que Samuel tranche.
  </context>
  <options>
    <option id="branch-a-permissions">
      <name>Branche A — `[permissions]` s'avère fonctionnel (mesure du nœud parallèle CONFIRME l'application au spawn)</name>
      <pros>Confinement par rôle, cohérent avec le modèle Claude actuel — chaque juge porte sa propre restriction dans son fichier `.toml`, aucune architecture de session séparée à construire, digest de l'adaptateur peut alors déclarer une garantie réelle</pros>
      <cons>Nécessite un lot de suivi (hors périmètre de CE plan) pour câbler `[permissions]` dans `agent-to-codex.mjs` une fois la forme exacte connue — `BundledSkillsConfig`/forme de `[permissions]` n'était pas déterminée à l'écriture de ce cadrage</cons>
    </option>
    <option id="branch-b-session">
      <name>Branche B — `[permissions]` ne fonctionne pas non plus (même sort que `sandbox_mode`/`approval_policy`)</name>
      <pros>Rien à construire côté fichier de rôle — évite d'investir dans un mécanisme inerte supplémentaire</pros>
      <cons>Le confinement par rôle N'EXISTE PAS sur Codex 0.150.1 : la garantie ADR-044 (juges à barrière d'écriture) ne survit pas telle quelle. Une architecture de SESSION SÉPARÉE (une session `codex exec -s read-only` dédiée par invocation de juge, orchestrée différemment du reste des workers) devient nécessaire — changement structurel plus lourd, à cadrer dans un lot dédié, PAS à improviser dans ce plan</cons>
    </option>
  </options>
  <resume-signal>Indique : "branche A" (poursuivre sur [permissions] une fois confirmé) ou "branche B" (cadrer l'architecture de session séparée dans un lot dédié) — ou "reporter" si la mesure du nœud parallèle n'est pas encore disponible, auquel cas ce plan reste livré SANS garantie de confinement pour les juges Codex, déclaré tel quel par FIDE (`38-01-PLAN.md`)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichier `.toml` généré -> `$CODEX_HOME/agents/vibeflow/` (disque réel de l'opérateur) | une écriture mal bornée pourrait toucher `~/.codex/agents/` d'un utilisateur en dehors du sous-dossier `vibeflow/` dédié — la doctrine de pose (isolation par sous-dossier) est la seule barrière |
| corps Markdown source -> `developer_instructions` TOML | un corps contenant `"""` ou des caractères de contrôle mal échappés pourrait produire un `.toml` invalide, ou pire, injecter du contenu hors de la chaîne prévue |
| rôle juge posé sans confinement réel -> exécution Codex | un `vf-reviewer`/`vf-design-judge` sur Codex peut ÉCRIRE alors que son contrat Claude l'interdit — c'est exactement l'objet du checkpoint bloquant (tâche 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-12 | Tampering | rôle malformé ignoré en silence par Codex (piège n°1 mesuré) | high | mitigate | gate ADPT-04 : `codex doctor --json` COMPTE les rôles chargés, jamais «pas de crash» |
| T-38-13 | Elevation of Privilege | juge (`vf-reviewer`/`vf-design-judge`) posé sur Codex sans confinement réel — écriture possible malgré le contrat Claude | critical | mitigate | checkpoint bloquant (tâche 3, `gate="blocking-human"`) — aucune affirmation de confinement tant que la décision n'est pas rendue ; **FIDE-03** déclare la limite de confinement au `status` ET à l'install, et vérifie les 4 éléments de la commande de juge (décision D-38-E rendue : session read-only séparée). ⚠️ La pose des rôles NE DÉMARRE PAS avant que FIDE-03 soit vert |
| T-38-14 | Tampering | injection via corps Markdown mal échappé dans la chaîne TOML `"""..."""` | medium | mitigate | échappement dédié des séquences `"""` testé explicitement (T2 tâche 1) |
| T-38-15 | Information Disclosure | fausse restriction perçue (`[tools]` inconnu accepté sans warning, piège n°2) | high | mitigate | prohibition explicite : jamais de clé inventée sous `[tools]` pour simuler `disallowedTools` |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | aucun paquet externe installé par ce plan (Node built-in uniquement) |
</threat_model>

<verification>
1. `bash plugin/_internal/runtime-adapter/tests/test-agent-to-codex.sh` sort 0.
2. Un agent réel (`vf-content-writer.md`) devient un rôle Codex compté par `codex doctor --json` sur banc isolé, `~/.codex` réel intact (sha256).
3. Le digest déclare explicitement `memory: LOST`, `tools`/`disallowedTools: PENDING`, `vf-internal: PRESERVED_BY_OMISSION`.
4. `bash plugin/_internal/tests/test-vibeflow-update.sh` sort 0 (wiring install runtime-aware, non-régression sur runtime claude).
5. Le checkpoint de confinement des juges est présenté à Samuel — aucune garantie de confinement n'est affirmée tant qu'il n'est pas résolu.
</verification>

<success_criteria>
- Un rôle Codex réellement dispatchable existe pour un agent VibeFlow réel, compté par `codex doctor --json`.
- Le mapping de nommage à 31 entrées n'a pas été construit ; le `task_name` se normalise en une ligne.
- Aucune contrainte `fork_turns` n'a été ajoutée.
- Le confinement des juges reste explicitement en attente, escaladé à Samuel, jamais assumé.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-38-portabilit-multi-runtime-livraison-canal-d-install-migration/38-05-SUMMARY.md` avec : le digest réel produit sur `vf-content-writer.md`, la preuve `codex doctor --json` (banc isolé, `~/.codex` réel intact), et la réponse de Samuel au checkpoint de confinement (ou son report explicite).
</output>

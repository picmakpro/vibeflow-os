---
phase: 21-alignement-du-moteur-gsd-sur-gsd-core-1-9-0
plan: 01
subsystem: dev-orchestrator
tags: [mcp, agents, injection, xcodebuildmcp, scope-global, adr-051]

requires: []
provides:
  - "inject-mcp-tools.sh découvre les serveurs MCP en UNION de deux sources (./.mcp.json scope projet + ~/.claude.json scope global), plus --servers explicite inchangé"
  - "--verify distingue désormais un écart réel (rc=1) d'une découverte réellement vide (rc=3 INDÉTERMINÉ) même quand seul le scope global porte les serveurs"
  - "--strict signale (WARNING par défaut, ERROR bloquante en --strict) un nom de serveur cité (vf-mcp-tools ou mcp__<serveur>__...) mais inconnu de toutes les sources découvertes — WINDOWS #4 clos"
affects: ["mission 2026-07-31-delta-gsd-core-1.9.0.md (défaut actif recensé)", "vf-calibrate SKILL.md (recette de ré-affirmation MCP)"]

tech-stack:
  added: []
  patterns:
    - "Source de scope injectable par flag ET variable d'environnement (--claude-json / VF_CLAUDE_JSON), hermétique aux tests — jamais dépendante de la machine qui exécute"
    - "Dégradation indépendante par source (fichier absent, JSON invalide, clé manquante) : chaque source contribue une liste vide + log, jamais un crash ni un arrêt de l'autre source"
    - "Union jamais un remplacement ; précédence d'orthographe alignée sur la précédence Claude Code réelle (scope projet > scope utilisateur global) sur collision insensible à la casse"
    - "Gradation WARNING/ERROR pilotée par --strict, alignée sur la convention check-agents.sh (warning par défaut, erreur bloquante en --strict) — jamais un no-op totalement muet sur un nom de serveur inconnu"

key-files:
  created: []
  modified:
    - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
    - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh
    - plugin/conductor/skills/vf-calibrate/SKILL.md
    - .planning/WINDOWS.md

key-decisions:
  - "Scope à couvrir : UNIQUEMENT ./.mcp.json (projet) ∪ ~/.claude.json clé top-level mcpServers (utilisateur/global). La zone projects.<cwd>.mcpServers de ~/.claude.json (scope « local » par-projet-par-utilisateur) est délibérément HORS PÉRIMÈTRE : vide sur tous les labs vérifiés au moment du diagnostic (vibeflow-os, RoastMyRoom, FreelanceMoneyCalc), son ajout aurait élargi le contrat au-delà du seul défaut constaté (XcodeBuildMCP invisible en scope global)."
  - "Précédence sur collision de nom (insensible à la casse) : le scope PROJET (.mcp.json) l'emporte sur le scope GLOBAL pour l'orthographe retenue — aligné sur la précédence Claude Code réelle documentée (projet > utilisateur)."
  - "Gradation du serveur inconnu (WINDOWS #4) : WARNING par défaut, ERROR bloquante en --strict — PAS une erreur dure par défaut. Argument retenu : un lab peut légitimement citer un serveur qu'il installera plus tard (ex. vf-mcp-tools préparé avant l'install XcodeBuildMCP) ; une erreur dure casserait des labs sains. Convention alignée sur check-agents.sh --strict (même gradation warning/erreur documentée dans ce repo)."
  - "--claude-json injectable par FLAG et par VARIABLE D'ENVIRONNEMENT (VF_CLAUDE_JSON), défaut $HOME/.claude.json — condition de testabilité hermétique explicitement posée par le mandat : sans ça, la suite serait verte ou rouge selon la machine qui l'exécute."
  - "Le pré-pass de découverte de serveur inconnu (WINDOWS #4) ne s'exécute JAMAIS avant le point où `servers` est confirmé non-vide — une découverte vide ne doit jamais être confondue avec un verdict conforme ni fabriquer un faux signal (leçon Phase 19, « vert à vide »)."

patterns-established:
  - "load_json_servers(path, what) : helper unique appelé pour CHAQUE source (projet, global), dégradation identique et indépendante — pas de logique dupliquée entre les deux scopes"
  - "unknown_server_refs(text, servers) : scanne à la fois la clé vf-mcp-tools ET les tokens mcp__<serveur>__... déjà présents dans tools:, comparaison insensible à la casse en égalité stricte (même convention que named_tokens_for)"

requirements-completed: ["Geste A", "Geste B", "Geste C", "Geste D", "Geste E"]

coverage:
  - id: D1
    description: "Geste A — structure réelle de ~/.claude.json documentée sur pièce (clé top-level mcpServers = scope global/utilisateur ; projects.<cwd>.mcpServers = scope local par-projet, vide sur tous les labs vérifiés, hors périmètre décidé)"
    requirement: "Geste A"
    verification:
      - kind: manual
        ref: "Lecture ciblée python3 -c sur ~/.claude.json (clés top-level + projects.<path>.mcpServers pour vibeflow-os, RoastMyRoom, FreelanceMoneyCalc) — pas de dump intégral, aucun secret copié dans un livrable"
        status: pass
    human_judgment: true
  - id: D2
    description: "inject-mcp-tools.sh découvre les serveurs en union ./.mcp.json ∪ ~/.claude.json, avec --claude-json (flag + VF_CLAUDE_JSON) hermétique et dégradation propre indépendante par source"
    requirement: "Geste B"
    verification:
      - kind: unit
        ref: "plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh (T23-T26)"
        status: pass
      - kind: mutation
        ref: "SUMMARY §Mutations — union désactivée (mutation 1), précédence inversée (mutation 2), dégradation désactivée (mutation 3) : les trois RED sur exactement les tests concernés"
        status: pass
    human_judgment: false
  - id: D3
    description: "--verify distingue désormais un écart réel (rc=1) d'une découverte vide (rc=3) même quand seul le scope global fournit des serveurs — le cas XcodeBuildMCP du diagnostic n'est plus INDÉTERMINÉ à tort"
    requirement: "Geste C"
    verification:
      - kind: unit
        ref: "plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh (T27 vs T28, discriminants)"
        status: pass
    human_judgment: false
  - id: D4
    description: "WINDOWS #4 clos : un token vf-mcp-tools ou mcp__<serveur>__... citant un serveur inconnu de toutes les sources est signalé nommément (WARNING par défaut, ERROR + exit 1 en --strict, y compris en --verify)"
    requirement: "Geste D"
    verification:
      - kind: unit
        ref: "plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh (T29-T31)"
        status: pass
      - kind: mutation
        ref: "SUMMARY §Mutations — escalade --strict désactivée (mutation 4), détection désactivée (mutation 5) : RED sur exactement T29b/T30/T31 puis T29a-T31"
        status: pass
    human_judgment: false
  - id: D5
    description: "Suite de test étendue par mutation (Geste E) — chaque garde-fou neuf prouvé discriminant avant livraison, résultats rapportés dans le SUMMARY"
    requirement: "Geste E"
    verification:
      - kind: manual
        ref: "SUMMARY §Mutations — 5 mutations, restauration vérifiée par diff, suite complète re-verte après chaque restauration"
        status: pass
    human_judgment: true

## Contexte

Rituel allégé (arbitrage Samuel) : phase unique, périmètre exhaustif fourni par le digest de
mission (voir `.planning/missions/2026-07-31-delta-gsd-core-1.9.0.md`, section « Le seul défaut
actif »). Pas de cadrage `gsd-discuss-phase` séparé — le digest fait foi et documente déjà
l'origine, la reproduction et les 5 gestes attendus. Ce PLAN consolide les décisions prises
pendant l'implémentation (D-* ci-dessus) pour qu'elles restent traçables indépendamment du digest.

## Ce qui a été fait (résumé exécutable — détail complet dans le SUMMARY)

1. **Geste A** : lecture de `~/.claude.json` (clé top-level `mcpServers` = scope global) et de
   `projects.<path>.mcpServers` (scope local par-projet, constaté vide sur les labs pertinents).
2. **Geste B** : `inject-mcp-tools.sh` découvre désormais l'union de `./.mcp.json` et
   `~/.claude.json` (option `--claude-json`, défaut réel, override `VF_CLAUDE_JSON` pour les
   tests), avec dégradation indépendante par source.
3. **Geste C** : `--verify` ne confond plus « rien à comparer » (scope réellement vide, rc=3) et
   « écart réel non détecté faute de source » (l'ancien bug — mcp-json absent alors que le scope
   global porte le serveur, qui sortait à tort en rc=3).
4. **Geste D** : `--strict` (nouveau flag) fait remonter en ERROR bloquante un nom de serveur cité
   mais inconnu de toutes les sources ; WARNING silencieux côté exit code par défaut. WINDOWS #4
   clos dans `.planning/WINDOWS.md` (markdown + JSON, cohérents).
5. **Geste E** : 9 nouveaux cas de test (T23-T31), 5 mutations ciblées prouvant que chaque garde-fou
   neuf est discriminant (voir SUMMARY pour le détail et les résultats).

Aucun fichier hors périmètre touché. `plugin/dev-orchestrator/references/mission-contracts.md`
(nœud suivant) et `.planning/STATE.md` / `.planning/ROADMAP.md` non modifiés.

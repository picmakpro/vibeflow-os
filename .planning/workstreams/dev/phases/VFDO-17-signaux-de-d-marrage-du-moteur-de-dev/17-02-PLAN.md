---
phase: 17-signaux-de-demarrage-du-moteur-de-dev
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - plugin/dev-orchestrator/scripts/check-doc-drift.sh
  - plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh
  - plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
  - plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh
autonomous: true
requirements: [SIG-02, SIG-03, SIG-04]

must_haves:
  truths:
    - "Frontière du seuil asserée des deux côtés (SIG-03/boundary, D-08) : fixture git à exactement `N = seuil` commits de code depuis le dernier commit de doc → signal `[doc-drift]` sur stdout + exit 0 ; à `N = seuil - 1` → stdout vide + exit 3. La comparaison est `compte ≥ seuil`, testée à `seuil-1`, `seuil` et `seuil+1`."
    - "Commit mixte (SIG-03/adjacency) : un commit qui touche à la fois du code et `docs/**` compte comme commit de DOC — il devient le point de départ exclu du décompte, donc le compteur repart de 0 juste après lui."
    - "Historique sans doc (SIG-03/empty, D-07) : dépôt git sans aucun commit touchant `docs/**` ni un `README*` racine — y compris un dépôt à 0 commit — → stdout vide et exit 3. Jamais de division par un historique vide, jamais de signal par défaut."
    - "Hors dépôt git (SIG-03, D-09) : un répertoire qui n'est pas dans un arbre de travail git → stdout vide et exit 3, sans message d'erreur sur stdout."
    - "Validation du seuil (SIG-03/precision) : `--threshold` avec une valeur non entière, négative, ou vide → message sur stderr et exit 64, jamais une comparaison silencieuse. `--threshold 0` est accepté et rend le signal systématique."
    - statement: "Sur un historique où deux commits de doc partagent le même horodatage, deux exécutions consécutives de `check-doc-drift.sh` sur la même fixture produisent le même compte — le choix du dernier commit de doc est déterministe."
      verification: backstop
    - "Non-régression du contrat historique (SIG-02, D-06) : sans `--hook`, `discover-unintegrated-docs.sh` produit exactement la sortie `grain<TAB>chemin` triée d'avant ce plan, avec les mêmes exits 0/3/64 — aucun préfixe de signal n'apparaît, les 16 cas préexistants de sa suite passent inchangés."
    - "Mutuelle exclusion des modes d'affichage (SIG-02/adjacency) : `--hook` et `--quiet` ensemble → exit 64 avec message sur stderr et stdout vide ; `--hook` seul n'émet jamais une ligne `grain<TAB>chemin`."
    - "Corpus vide ou entièrement cité (SIG-02/empty) : en mode `--hook`, stdout vide et exit 3 — exactement les mêmes conditions d'exit que le mode historique."
    - "Agrégation déterministe (SIG-02/ordering) : en mode `--hook`, la sortie est UNE ligne agrégée suivie de sa ligne de geste ; les compteurs annoncés sont stables d'une exécution à l'autre et leur somme égale le nombre de lignes du mode historique sur la même fixture."
    - "Lecture seule (SIG-04, D-15) : `check-doc-drift.sh` n'écrit aucun fichier hors `mktemp` — l'empreinte `find <fixture> | LC_ALL=C sort` est identique avant et après, y compris le contenu de `<fixture>/.git`."
  artifacts:
    - plugin/dev-orchestrator/scripts/check-doc-drift.sh
    - plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh
  key_links:
    - "`check-doc-drift.sh` → binaire `git` de l'hôte : première fois qu'un script de `plugin/**/scripts/` sort vers git (aucun précédent dans le dépôt, confirmé par grep dans `17-PATTERNS.md`). L'invocation traverse la configuration du dépôt cible — point de rupture sécurité T-17-06."
    - "`discover-unintegrated-docs.sh` → doctrine `references/ingestion-flow.md` : le contrat `grain<TAB>chemin` est consommé par la doctrine d'ingestion et déjà distribué dans les labs installés. Toute altération du chemin non-`--hook` casse BRDG-02 silencieusement."
    - "`hooks/hooks.json` (créé en 17-01) → `check-doc-drift.sh --hook` : jusqu'à ce plan, la 3e commande du fragment pointait sur un script absent, neutralisée par le tolérant d'échec. Ce plan la rend vivante — c'est le moment où le hook devient complet."
  prohibitions:
    - "P-01 (values) — MUST NOT employer un cadrage culpabilisant, honteux ou alarmiste dans une ligne de signal. Le signal constate un fait et propose un geste ; il ne reproche jamais un manquement à l'utilisateur (pas de « tu n'as toujours pas… », pas de « attention ! », pas de « urgent », pas de compte à rebours ni de perte à éviter)."
    - "P-02 (transparency) — MUST NOT affirmer un état que le script n'a pas observé. `[doc-drift]` dit que la documentation N'A PAS BOUGÉ depuis N commits, jamais qu'elle est fausse, périmée ou incorrecte ; `[docs-ingest]` dit qu'un document n'est cité par aucun registre, jamais qu'il devrait l'être."
    - "P-03 (safety/autonomy) — MUST NOT rédiger la ligne de geste sous une forme impérative qu'une boucle autonome exécuterait sans confirmation. Le signal est injecté dans le contexte de la session principale, potentiellement pilotée par `vf-auto` ; la formulation reste une PROPOSITION explicitement conditionnée à la confirmation humaine (ADR-031, garde-fous BRDG-03 pour l'ingestion), jamais un ordre."
---

<objective>
Étendre le moteur de signaux prouvé par la tranche traçante 17-01 aux deux faits restants : la
dérive documentaire (`check-doc-drift.sh`, nouveau, SIG-03) et l'agrégation en une ligne des
documents de cadrage orphelins (`discover-unintegrated-docs.sh --hook`, extension strictement
additive, SIG-02).

Purpose: le contrat de sortie, le format de signal auto-portant, le gate `--hook`/`--quiet` et la
forme du harnais de test sont déjà prouvés par 17-01. Ce plan les réplique sur les deux faits
restants sans réinventer d'architecture, et rend vivante la 3e commande du fragment de hooks. La
contrainte dominante n'est pas la nouveauté mais la NON-RÉGRESSION : `discover-unintegrated-docs.sh`
est déjà distribué dans les labs installés et son contrat historique est consommé par
`ingestion-flow.md`.

Output: 1 script créé + 1 suite créée + 1 script étendu + 1 suite étendue (cas 17+).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-PATTERNS.md
@.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-01-SUMMARY.md
@docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md
@plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh
@plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh
@plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh
</context>

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `plugin/dev-orchestrator/scripts/check-doc-drift.sh` | fichier créé | script de constat, exécutable par `bash <chemin>` |
| `--path <dir>` | flag CLI (nouveau sur ce script) | défaut `.` ; sans valeur → 64 |
| `--threshold <N>` | flag CLI (nouveau) | défaut **20** ; entier ≥ 0 ; valeur absente / non entière / négative → 64 |
| `--hook` | flag CLI (nouveau sur ce script) | change UNIQUEMENT le format de stdout |
| `--quiet` | flag CLI (nouveau sur ce script) | supprime les diagnostics `say()` sur stderr |
| `--hook` + `--quiet` | combinaison | exit 64, gate en tête de parsing |
| `-h` / `--help` | flag CLI (nouveau sur ce script) | imprime l'en-tête `# `, exit 0 |
| exits `0` / `3` / `64` | contrat de sortie | jamais d'autre code |
| `[doc-drift]` | préfixe de signal (nouveau) | exit 0, stdout, 2 lignes |
| `[check-doc-drift]` | préfixe `say()` (nouveau) | stderr uniquement |
| `--hook` sur `discover-unintegrated-docs.sh` | flag CLI (nouveau, additif) | ligne agrégée au lieu de la liste ; exits STRICTEMENT inchangés |
| `[docs-ingest]` | préfixe de signal (nouveau) | émis uniquement sous `--hook` |
| `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` | fichier créé | suite ramassée par `ci.yml:32` |
| cas 17+ de `test-discover-unintegrated-docs.sh` | cas de test ajoutés | numérotation continue après le cas 16 existant |

## Détecteurs `plan:pre` non déclenchés

`ai-integration`, `assumption-delta` et `schema-gate` ne s'appliquent pas à ce plan : aucune
intégration d'API/SDK externe (l'appel à `git` est un binaire local, pas un service), aucune
transition d'identité singulier→pluriel ni requis→optionnel, aucun fichier d'ORM ou de schéma
touché. Confirmation explicite, pas un oubli.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : `check-doc-drift.sh` + sa suite — heuristique de commits, seuil réglable, silence hors git</name>
  <precondition>`git` est présent dans le PATH et `git init` fonctionne sans dépendre d'une configuration globale (les fixtures committent via `git -c user.email=… -c user.name=…`, jamais via la config de la machine).</precondition>
  <reversibility rating="reversible">Nouveau script sans consommateur autre que la 3e commande du
  fragment de hooks ; le retirer coûte une suppression de fichier et une ligne de JSON.</reversibility>
  <read_first>
    - `docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md` §3.3 et §4.1 (tableau des conditions, gabarit de la ligne `[doc-drift]`)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-07, D-08, D-09, D-13, D-14, D-15
    - `plugin/dev-orchestrator/scripts/check-dev-bootstrap.sh` (produit en 17-01 — en-tête, boucle d'arguments, `say()`, gate `--hook`/`--quiet`, contrat de sortie : le modèle direct à répliquer)
    - `plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` (produit en 17-01 — harnais de test à répliquer)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-PATTERNS.md` §« No Analog Found » (aucun précédent d'appel à git dans `plugin/**/scripts/` — cette logique s'écrit à neuf)
    - S'il existe : le livrable de recherche portabilité du nœud n0 de la mission (fichier daté du 2026-07-27 sous `docs/superpowers/` ou `reports/` traitant des 6 fixes CI de portabilité). Absent → ne pas bloquer, D-13.2 suffit.
  </read_first>
  <files>plugin/dev-orchestrator/scripts/check-doc-drift.sh, plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh</files>
  <behavior>
    - Répertoire hors dépôt git → stdout vide, exit 3.
    - Dépôt git à 0 commit → stdout vide, exit 3.
    - Dépôt git avec des commits de code mais aucun commit de doc → stdout vide, exit 3.
    - `seuil - 1` commits de code depuis le dernier commit de doc → stdout vide, exit 3.
    - Exactement `seuil` commits → stdout contient `[doc-drift]` et le compte, exit 0.
    - `seuil + 1` commits → signal, exit 0.
    - `--threshold 3` sur une fixture à 3 commits de code → signal (le seuil est bien réglable et pris en compte).
    - `--threshold 0` → signal même à 0 commit de code depuis la doc.
    - `--threshold abc`, `--threshold -1`, `--threshold` sans valeur → exit 64, stdout vide, message sur stderr.
    - Commit mixte code + `docs/` → compte comme commit de doc, le compteur repart de 0 après lui.
    - Modification d'un `README.md` **de module** (`plugin/<mod>/README.md`) → ne compte PAS comme mise à jour de doc ; modification d'un `README*` à la racine → compte.
    - `--hook` + `--quiet` → exit 64. Argument inconnu → exit 64. `--help` → exit 0, sortie non vide.
    - Déterminisme : deux exécutions consécutives sur la même fixture donnent le même compte, y compris quand deux commits de doc partagent le même horodatage.
    - Lecture seule : empreinte `find <fixture> | LC_ALL=C sort` inchangée après exécution, `.git` compris.
  </behavior>
  <action>
Créer `plugin/dev-orchestrator/scripts/check-doc-drift.sh` en répliquant la structure de
`check-dev-bootstrap.sh` (livré en 17-01) : shebang, `set -uo pipefail`, en-tête `# ` documentaire
(rôle ADR-055 §3, Usage, Exit codes), boucle d'arguments avec `--path` / `--threshold` / `--hook` /
`--quiet` / `-h|--help` / défaut → 64, gate de mutuelle exclusion `--hook` + `--quiet` → 64 en tête
de parsing, helper `say()` de préfixe `[check-doc-drift]` sur **stderr**.

Validation du seuil (résolution planner de l'arête `precision` — non couverte par D-08, cohérente
avec la convention « argument invalide → 64 » du dépôt) : `--threshold` sans valeur → 64 ;
valeur ne matchant pas `^[0-9]+$` → message sur stderr + 64 ; `0` est une valeur valide. Documenter
ce choix dans l'en-tête `# ` du script pour qu'il ne se relise pas comme un accident.

Garde git (D-09) : si `git -C "$ROOT" rev-parse --is-inside-work-tree` n'aboutit pas, silence total
sur stdout et exit 3. Cet appel et TOUS les appels git suivants sont durcis contre la configuration
du dépôt cible (T-17-06) : préfixer chaque invocation de `git -C "$ROOT" -c core.fsmonitor= -c
core.hooksPath=/dev/null --no-optional-locks`, et exporter `GIT_CONFIG_NOSYSTEM=1`,
`GIT_TERMINAL_PROMPT=0`, `GIT_OPTIONAL_LOCKS=0` en tête de script. Motif : ouvrir une session dans un
dépôt cloné non maîtrisé ne doit pas permettre à sa configuration de faire exécuter un programme au
`SessionStart`. Écrire ce motif une seule fois dans une fonction wrapper plutôt que de le répéter.

Heuristique (D-07) : trouver le SHA du dernier commit ayant touché soit `docs/` dans tout l'arbre,
soit un `README*` **à la racine seulement** — les `README.md` de module ne comptent pas, sans quoi
tout commit sur l'un des 17 modules ferait taire le signal. Utiliser une invocation qui limite les
chemins racine (spécification de chemin ancrée, pas un glob récursif). Si aucun commit de doc n'est
trouvé — y compris parce que le dépôt n'a aucun commit — silence, exit 3. Sinon, compter les commits
touchant du code source depuis ce SHA (exclu) jusqu'à HEAD, en excluant des chemins comptés
exactement les mêmes chemins de doc que ceux qui définissent le point de départ. Le décompte se fait
sur un ensemble d'ancêtres, pas sur une liste ordonnée, pour rester déterministe quand deux commits
partagent le même horodatage.

Comparaison (D-08) : `compte < seuil` → aucune sortie sur stdout, exit 3 ; `compte ≥ seuil` →
signal `[doc-drift]` sur stdout, exit 0. La ligne suit le gabarit de la spec §4.1 : le constat
(nombre de commits de code depuis la dernière mise à jour de la doc) puis la ligne de geste
indentée. Le libellé dit que la documentation N'A PAS BOUGÉ, jamais qu'elle est fausse ou périmée
(P-02), ne reproche rien (P-01), et propose sans ordonner (P-03).

Contrat de sortie : seuls 0, 3 et 64. Aucune écriture hors `mktemp` + `trap ... EXIT` (D-15).
Portabilité (D-13.2) : `set -uo pipefail`, awk POSIX, `LC_ALL=C` sur tout tri, aucun builtin bash4+,
pas de `sed -i`, pas de `stat -f`, pas de `grep -P`, pas de `readlink -f`.

Créer ensuite `plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` sur le harnais de
`test-check-dev-bootstrap.sh` (compteurs `PASS`/`FAIL`, `ok()`/`ko()`, `mktemp -d` + `trap`, blocs
`# === Cas N ===`, pied `[ "$FAIL" -eq 0 ]`). Territoire neuf : le helper `mk_root` doit initialiser
un vrai dépôt git dans la fixture — `git init -q`, branche par défaut forcée explicitement pour ne
pas dépendre de la config de l'hôte, et chaque commit passé avec `-c user.email=t@t -c user.name=t`
pour ne jamais dépendre d'une identité globale. Ajouter un helper qui crée N commits de code d'un
coup, afin que les cas de frontière `seuil-1` / `seuil` / `seuil+1` soient lisibles.

Chaque cas capture stdout et le code de retour dans deux variables distinctes et les assert
séparément (D-14) — aucune assertion ne déduit l'absence de sortie d'un code 3.
  </action>
  <verify>
    <automated>bash -n plugin/dev-orchestrator/scripts/check-doc-drift.sh && bash plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` sort en 0 et sa ligne de résultat annonce `0 ko`.
    - La suite contient au moins 14 assertions et couvre nommément les trois points de frontière `seuil-1` / `seuil` / `seuil+1`.
    - `bash plugin/dev-orchestrator/scripts/check-doc-drift.sh --path "$(mktemp -d)"` sort en 3 avec stdout vide (hors dépôt git).
    - `bash plugin/dev-orchestrator/scripts/check-doc-drift.sh --threshold abc` sort en 64 ; `--threshold` sans valeur sort en 64 ; `--hook --quiet` sort en 64.
    - `bash plugin/dev-orchestrator/scripts/check-doc-drift.sh --hook --path .` sur ce dépôt sort en 3 (le dernier commit de doc est récent) sans rien imprimer sur stdout.
    - `grep -vE '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-doc-drift.sh | grep -cE 'exit[[:space:]]+1$'` retourne `0`.
    - `grep -vE '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-doc-drift.sh | grep -c 'core.fsmonitor='` est ≥ 1 — le durcissement T-17-06 est réellement dans le code, pas seulement dans le plan.
    - `grep -vE '^[[:space:]]*#' plugin/dev-orchestrator/scripts/check-doc-drift.sh | grep -cE 'mapfile|\$\{[A-Za-z_]+,,\}|sed -i |stat -f|grep -P|readlink -f'` retourne `0`.
    - Le fichier de suite est ramassé par `find plugin scripts -type f -path '*/tests/test-*.sh'`.
  </acceptance_criteria>
  <done>Le troisième fait est constaté : le seuil est réglable et testé à sa frontière exacte des deux côtés, le silence hors dépôt git et sans commit de doc est prouvé, l'appel à git est durci contre la configuration d'un dépôt non maîtrisé, et la 3e commande du fragment de hooks pointe désormais sur un script réel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : `discover-unintegrated-docs.sh --hook` — extension strictement additive + cas 17+</name>
  <reversibility rating="costly">Le script est DÉJÀ distribué dans les labs installés et son contrat
  `grain&lt;TAB&gt;chemin` est consommé par la doctrine `ingestion-flow.md` (BRDG-02). Une régression du
  chemin non-`--hook` ne se répare pas localement : elle exige une release patch redistribuée à chaque
  lab, et son mode d'échec est un faux négatif silencieux (plus aucun document remonté) que rien ne
  signale.</reversibility>
  <read_first>
    - `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` (fichier entier, 142 lignes — le contrat à ne pas casser)
    - `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` (fichier entier — 16 cas existants, gabarit des cas 17+)
    - `docs/superpowers/specs/2026-07-27-signaux-demarrage-dev-design.md` §3.2 et §4.1 (gabarit de la ligne `[docs-ingest]`)
    - `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-CONTEXT.md` D-06, D-14, D-15
    - `plugin/dev-orchestrator/references/ingestion-flow.md` (le consommateur du contrat historique — vérifier qu'il ne décrit que le mode sans `--hook`)
  </read_first>
  <files>plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh, plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh</files>
  <behavior>
    - Cas 17 — `--hook` sur une fixture à 3 documents non cités (2 spec, 1 plan) : stdout contient `[docs-ingest]`, le compte `3`, la ventilation `2` spec et `1` plan, et une ligne de geste ; exit 0.
    - Cas 18 — `--hook` sur un corpus entièrement cité : stdout vide, exit 3 (mêmes conditions que le mode historique).
    - Cas 19 — `--hook` avec `.planning/` absent : stdout vide, exit 3.
    - Cas 20 — `--hook` + `--quiet` : exit 64, stdout vide, message non vide sur stderr.
    - Cas 21 — non-régression : sur la MÊME fixture, la sortie sans `--hook` est strictement `grain<TAB>chemin` triée, ne contient aucun marqueur de signal, et le nombre de lignes égale le compte annoncé par le mode `--hook`.
    - Cas 22 — `--hook` n'émet jamais de ligne tabulée : la sortie ne contient aucun caractère de tabulation.
  </behavior>
  <action>
Modifier `plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` de façon STRICTEMENT
additive. Aucune ligne existante n'est réécrite en dehors des deux points d'insertion ci-dessous, et
le chemin d'exécution sans `--hook` doit produire une sortie octet-pour-octet identique à celle
d'avant ce plan.

Point d'insertion 1 — boucle d'arguments (lignes 48-60) : ajouter la variable `HOOK=0` à côté de
`QUIET=0`, un cas `--hook` qui met `HOOK=1`, et immédiatement après la boucle le gate de mutuelle
exclusion `HOOK=1` ET `QUIET=1` → message sur stderr + exit 64, avant toute autre logique (D-06,
même position que le gate `--path` sans valeur). `--hook` ne touche PAS au comportement de `say()` :
les diagnostics restent sur stderr et ne sont donc jamais injectés dans le contexte de session —
c'est stdout seul que le hook capture. Documenter ce point dans l'en-tête `# `.

Point d'insertion 2 — branche de sortie (lignes 139-142) : insérer la branche `--hook` AVANT
`[ "$QUIET" -eq 1 ] && exit 0` et avant le tri final. Quand `HOOK=1`, compter le total de lignes de
`$OUT_TMP` et la ventilation par grain (première colonne `spec` / `plan`) en une seule passe awk
POSIX, puis imprimer la ligne agrégée sur le gabarit de la spec §4.1 : le constat (nombre de
documents de cadrage hors feuille de route, avec la ventilation entre parenthèses) suivi de la ligne
de geste indentée qui propose l'ingestion sous confirmation. Puis `exit 0`. La branche ne modifie ni
le calcul amont ni aucun des trois exits — elle ne fait que remplacer le rendu.

Mettre à jour l'en-tête `# ` du script : ajouter `--hook` dans la ligne Usage et une phrase
expliquant que le flag est un mode d'AFFICHAGE, que les exits sont strictement inchangés, et que le
contrat `grain<TAB>chemin` du mode par défaut reste le seul consommé par `ingestion-flow.md`. Ne PAS
toucher aux sections existantes de l'en-tête.

Le libellé de la ligne agrégée dit qu'un document n'est cité par aucun registre, jamais qu'il
devrait l'être (P-02), ne reproche rien (P-01), et propose l'ingestion sous confirmation explicite
plutôt que de l'ordonner (P-03, garde-fous BRDG-03).

Étendre `plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` avec les cas 17
à 22 décrits dans `<behavior>`, en continuant la numérotation après le cas 16 existant et en
reproduisant exactement la forme des blocs existants (`# === Cas N — <piège> ===`, `mk_root`,
capture séparée de `out` et `rc`, `ok`/`ko`). Mettre à jour la ligne d'en-tête qui annonce le nombre
d'assertions. Ne modifier AUCUN des 16 cas existants : leur passage inchangé EST la preuve de
non-régression.
  </action>
  <verify>
    <automated>bash -n plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh && bash plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` sort en 0 avec `0 ko` et annonce au moins 22 assertions.
    - `git diff plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` ne montre AUCUNE suppression ni modification dans les blocs des cas 1 à 16 (seuls des ajouts après le cas 16, plus la ligne d'en-tête de comptage).
    - Non-régression prouvée sur ce dépôt : la sortie de `bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --path .` est identique octet pour octet à celle obtenue avec la version d'avant ce plan (comparer via `git stash` ou `git show HEAD:<chemin>` exécuté dans un répertoire temporaire).
    - En mode `--hook` sur une fixture à 3 documents non cités, `bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --hook --path <fixture> | grep -c "$(printf '\t')"` retourne `0` : la sortie agrégée ne contient aucune tabulation, donc aucune ligne du contrat historique ne fuit dans le mode hook.
    - `bash plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh --hook --quiet` sort en 64.
    - `git diff --stat plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` montre uniquement des insertions et aucune ligne supprimée en dehors de la ligne Usage de l'en-tête et de la ligne `QUIET=0` (remplacée par le couple `QUIET=0` / `HOOK=0`).
  </acceptance_criteria>
  <done>Le flag `--hook` agrège en une ligne sans que le contrat historique bouge d'un octet : les 16 cas préexistants passent inchangés, la sortie du mode par défaut est identique à celle d'avant le plan, et les exits 0/3/64 sont les mêmes dans les deux modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dépôt git non maîtrisé → invocation `git` du script | `check-doc-drift.sh` invoque `git` dans un répertoire choisi par `--path`. La configuration de ce dépôt (`.git/config`) peut déclarer des mécanismes qui font exécuter un programme lors d'une simple lecture. Frontière critique de ce plan. |
| contenu du dépôt → contexte de la session principale | les compteurs et libellés imprimés par `[doc-drift]` et `[docs-ingest]` sont injectés dans le contexte du modèle au `SessionStart`. |
| arguments CLI → script | `--path`, `--threshold`, `--hook`, `--quiet`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-06 | Elevation of Privilege / Tampering | `check-doc-drift.sh` — invocation de `git` dans un dépôt arbitraire au `SessionStart` | high | mitigate | Toutes les invocations passent par un wrapper unique appliquant `git -C "$ROOT" -c core.fsmonitor= -c core.hooksPath=/dev/null --no-optional-locks`, avec `GIT_CONFIG_NOSYSTEM=1`, `GIT_TERMINAL_PROMPT=0` et `GIT_OPTIONAL_LOCKS=0` exportés en tête de script. Ouvrir une session dans un dépôt cloné non maîtrisé ne peut donc pas faire exécuter un programme déclaré par sa configuration. Vérifié par un critère d'acceptation qui grep la présence effective du durcissement dans le code. |
| T-17-07 | Information Disclosure | ligne `[doc-drift]` et ligne `[docs-ingest]` imprimées dans le contexte de session | low | mitigate | `[doc-drift]` n'imprime qu'un entier (le compte) et aucun message de commit, aucun nom d'auteur, aucun chemin. `[docs-ingest]` n'imprime que des compteurs agrégés — c'est précisément la différence avec le mode historique, qui liste les chemins : le mode `--hook` réduit la surface exposée au contexte au lieu de l'augmenter. |
| T-17-08 | Denial of Service | parcours de l'historique git au `SessionStart` sur un dépôt volumineux | medium | mitigate | Une seule recherche du dernier commit de doc, bornée à `-1` résultat, puis un décompte sur un ensemble d'ancêtres (pas de parcours de journal formaté). `--no-optional-locks` évite tout blocage sur un `index.lock` concurrent. Le contrat de sortie n'utilise pas de code d'échec bloquant et chaque commande du fragment est suffixée par le tolérant d'échec de la spec §3.4. |
| T-17-09 | Tampering | régression silencieuse du contrat `grain&lt;TAB&gt;chemin` déjà distribué | high | mitigate | Extension strictement additive : 2 points d'insertion identifiés à la ligne près, aucun des 16 cas de test préexistants modifié (leur passage inchangé est la preuve), et un critère d'acceptation qui compare octet pour octet la sortie du mode par défaut avec celle de la version précédente extraite via `git show HEAD:<chemin>`. Rating de réversibilité `costly` porté par la Task 2. |
| T-17-10 | Tampering | écriture sur le filesystem par des scripts censés être en lecture seule | high | mitigate | Aucune écriture hors `mktemp` + `trap ... EXIT` ; l'empreinte `find <fixture> \| LC_ALL=C sort` (répertoire `.git` compris) est comparée avant/après dans la suite de `check-doc-drift.sh`, et le test structurel T21 du plan 17-03 le vérifie par grep sur les deux nouveaux scripts. |
| T-17-SC | Tampering (supply chain) | installation de paquets | low | accept | Aucune installation de paquet dans ce plan — aucun `npm` / `pip` / `cargo install`, aucune nouvelle dépendance. `git`, `awk`, `find` et `grep` sont des outils de l'hôte déjà requis par la CI. Le gate de légitimité de paquets est sans objet. |
</threat_model>

## Résolution de sonde portée par ce plan

Arêtes résolues ici (spec-less fallback §A) : SIG-02 `adjacency` / `empty` / `ordering` →
`truths` explicites ; SIG-03 `boundary` / `adjacency` / `empty` / `precision` → `truths` explicites ;
SIG-03 `ordering` (départage d'horodatages identiques entre deux commits de doc) → marqueur
**`backstop`** structuré dans `must_haves.truths` : le déterminisme est réel mais dépend du parcours
du graphe par la version de git de l'hôte, donc il est vérifié par observation (deux exécutions,
même résultat) plutôt que par une garantie contractuelle. Aucun item n'est écarté silencieusement.

<verification>
- `bash -n` passe sur les deux scripts.
- `bash plugin/dev-orchestrator/scripts/tests/test-check-doc-drift.sh` → `0 ko`.
- `bash plugin/dev-orchestrator/scripts/tests/test-discover-unintegrated-docs.sh` → `0 ko`, ≥ 22 assertions.
- `bash plugin/dev-orchestrator/scripts/tests/test-check-dev-bootstrap.sh` (plan 17-01) passe toujours — aucune régression croisée.
- La sortie du mode par défaut de `discover-unintegrated-docs.sh` est identique à celle de `git show HEAD:plugin/dev-orchestrator/scripts/discover-unintegrated-docs.sh` sur la même fixture.
</verification>

<success_criteria>
- SIG-02 tenu : `--hook` agrège en une ligne, le contrat historique et les 3 exits sont intacts, `--hook` + `--quiet` sort en 64.
- SIG-03 tenu : seuil par défaut 20, réglable, frontière testée des deux côtés, silence hors dépôt git et sans commit de doc.
- SIG-04 tenu sur les deux scripts : lecture seule, contrat 0/3/64, aucun blocage de tour.
- Les 3 commandes du fragment `hooks/hooks.json` pointent désormais toutes sur un script réel.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-17-signaux-de-d-marrage-du-moteur-de-dev/17-02-SUMMARY.md` en fin de plan.
</output>

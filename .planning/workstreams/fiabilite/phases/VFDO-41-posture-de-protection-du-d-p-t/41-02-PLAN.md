---
phase: 41-posture-de-protection-du-d-p-t
plan: 02
lot: L2 — CODEOWNERS étroit et amendement du ledger (PROT-01 repris, non coché)
type: execute
wave: 2
depends_on: ["41-01"]
autonomous: true
requirements: [PROT-04, PROT-01]
files_modified:
  - .github/CODEOWNERS
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "`.github/CODEOWNERS` porte exactement 4 règles, toutes au seul propriétaire `@picmakpro` : `/.github/`, `/.planning/instruction-budget-baselines.tsv`, `/.planning/.*-armed`, `/scripts/hooks/` — aucun autre chemin (D-05)"
    - "L'en-tête dit qui est `@picmakpro` d'après le fait mesuré le 2026-09-23 : le compte de Willy, co-auteur, admin du dépôt (41-CONTEXT.md § REPRISE) — D-01 n'est jamais cité comme fait ; et qu'une PR ouverte par `picmakpro` sur ces chemins ne peut pas recevoir l'approbation de son propre auteur : elle passe par un contournement tracé (D-02bis)"
    - "Les motifs sont ancrés à la racine ; `*` y couvre un nom commençant par un point — sourcé : docs GitHub about-code-owners et git-scm gitignore, lus le 2026-09-17"
    - "La couverture est prouvée localement par le moteur de motifs gitignore de git (témoins positifs ET négatifs) ; la preuve réelle côté GitHub vient après merge (plan 41-04 : `codeowners/errors` à 0 ; plans 41-06/41-07 : revue requise sur les chemins possédés, pas sur un chemin témoin)"
    - "Le ledger est AMENDÉ, jamais recréé : PROT-01..05 existent déjà (plans 41-14 à 41-19). PROT-01 reste `- [ ]` et reçoit des lignes de suite « Reprise (2026-09-23) » (fait corrigé sur `picmakpro`, D-02bis, volet serveur replanifié, cochage à la clôture sur pièce) ; sa ligne de traçabilité passe de « Hors d'atteinte » à « Pending — reprise du 2026-09-23 » ; PROT-02..05 et toutes les autres lignes restent octet-identiques"
    - "O-3 n'est associée à aucun vocabulaire d'achèvement dans le ledger ; tout commit de plan citant D-01..D-10 nomme « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 », tout commit citant D-02bis nomme « arbitrage Willy, AskUserQuestion session principale, 2026-09-23 »"
  artifacts:
    - path: ".github/CODEOWNERS"
      provides: "revue code owner `@picmakpro` sur les chemins qui neutraliseraient un gate (O-3)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROT-01 repris (non coché), traçabilité « Pending — reprise du 2026-09-23 »"
  key_links:
    - "CODEOWNERS n'agit qu'une fois sur la branche de base : il n'a aucun effet avant le merge de la PR de la phase (41-04), et la règle `require_code_owner_review` du ruleset (41-01, 41-05) est ce qui le rend exigible"
---

<objective>
Poser le fichier CODEOWNERS du périmètre étroit de D-05 et amender le ledger pour la reprise, sans
rien déclarer prouvé.

Révisé le 2026-09-23 (reprise, `41-CONTEXT.md` § REPRISE) : l'ancienne Task 2 créait une section
PROT-01..04 — elle existe depuis les plans 41-14 à 41-19 ; la tâche devient un AMENDEMENT de
l'entrée PROT-01 (seule restée non cochée) et de sa ligne de traçabilité. L'en-tête de CODEOWNERS
ne cite plus D-01 comme fait (faux) : `picmakpro` est le compte de Willy.

Purpose: PROT-04 — O-3 passe de « signalée et tracée » à « gardée par défaut + tracée » dès que ce
fichier est sur `main` et que le ruleset exige la revue code owner ; PROT-01 redevient atteignable.
Output: `.github/CODEOWNERS`, entrée PROT-01 amendée de `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/REQUIREMENTS.md

Sources lues le 2026-09-17 : `https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners`
(emplacements `.github/` > racine > `docs/` ; ligne invalide sautée et signalée dans l'UI ;
propriétaire = droit write minimum ; version lue = branche de base ; l'auteur d'une PR ne peut pas
l'approuver) ; `https://git-scm.com/docs/gitignore` (ancrage par séparateur initial, `/` final =
répertoires, `*` = tout sauf `/`).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : `.github/CODEOWNERS` à quatre règles, en-tête fidèle à la reprise, couverture prouvée par témoins positifs et négatifs</name>
  <files>.github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (§ REPRISE, D-02bis, D-05, § Conséquences de D-02)
  </read_first>
  <action>
Créer `.github/CODEOWNERS`. En-tête en commentaires `#`, en français :
- objet : revue code owner exigée sur les chemins dont la modification peut neutraliser un gate
  (O-3 du `25-SECURITY.md`) — D-05, arbitrage Samuel, AskUserQuestion session principale,
  2026-09-17 ;
- propriétaire : `@picmakpro`, compte de Willy, co-auteur du plugin et admin du dépôt (fait mesuré le
  2026-09-23, `41-CONTEXT.md` § REPRISE) ;
- portée : garde-fou par défaut + trace, pas verrou — Samuel et Willy peuvent contourner, nommément
  (D-02bis, arbitrage Willy, AskUserQuestion session principale, 2026-09-23), et le contournement
  laisse une entrée dans les rule suites (ADR-072) ; une PR ouverte par `picmakpro` sur ces chemins
  ne peut pas recevoir l'approbation de son propre auteur : elle entre par un contournement tracé ;
- lecture par GitHub : version de la branche de base, donc une modification de ce fichier dans une
  PR ne vaut qu'après son merge ;
- syntaxe : motifs de type gitignore, ancrés par `/` ; `*` couvre aussi un nom commençant par un
  point ; ni `!`, ni `[ ]`, ni échappement de `#` (docs GitHub about-code-owners, git-scm gitignore) ;
- « aucun autre chemin » (D-05) : toute extension est un arbitrage.

Puis exactement quatre lignes de règle, chacune `<motif> @picmakpro`, sans commentaire en fin de
ligne : `/.github/`, `/.planning/instruction-budget-baselines.tsv`, `/.planning/.*-armed`,
`/scripts/hooks/`. Commit local sur la branche A (`feat/phase-41-volet-admin`), message
« feat(41-02): CODEOWNERS étroit @picmakpro (D-05 — arbitrage Samuel, AskUserQuestion session
principale, 2026-09-17 ; D-02bis — arbitrage Willy, AskUserQuestion session principale,
2026-09-23) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'F=.github/CODEOWNERS; T=$(mktemp -d); awk "!/^[ \t]*#/ && NF" "$F" > "$T/regles"; n=$(awk "END{print NR}" "$T/regles"); own=$(awk "NF!=2 || \$2!=\"@picmakpro\" {c++} END{print c+0}" "$T/regles"); awk "{print \$1}" "$T/regles" | LC_ALL=C sort > "$T/motifs"; printf "%s\n" "/.github/" "/.planning/.*-armed" "/.planning/instruction-budget-baselines.tsv" "/scripts/hooks/" | LC_ALL=C sort > "$T/attendu"; me=$(cmp -s "$T/motifs" "$T/attendu" && echo oui || echo non); ko=0; for p in .github/CODEOWNERS .github/workflows/ci.yml .github/rulesets/main.json .github/rulesets/tags-v.json .planning/instruction-budget-baselines.tsv .planning/.instruction-budget-armed .planning/.requirements-survival-armed scripts/hooks/pre-push scripts/hooks/post-merge; do git -c core.excludesFile="$T/motifs" check-ignore -q --no-index "$p" || { echo "NON COUVERT $p"; ko=$((ko+1)); }; done; for p in scripts/check-release-tag.sh .planning/STATE.md .planning/REQUIREMENTS.md plugin/conductor/scripts/check-instruction-budget.sh .gitignore CLAUDE.md docs/ADR.md scripts/tests/test-preuve-41-m1.sh; do if git -c core.excludesFile="$T/motifs" check-ignore -q --no-index "$p"; then echo "COUVERT A TORT $p"; ko=$((ko+1)); fi; done; h=$(awk "/^[ \t]*#/ && (index(\$0, \"Willy\") || index(\$0, \"D-02bis\")) {c++} END{print c+0}" "$F"); d1=$(awk "/^[ \t]*#/ && index(\$0, \"D-01\") {c++} END{print c+0}" "$F"); echo "codeowners : regles=$n proprietaire_hors_picmakpro=$own motifs_egaux_attendu=$me ecarts_de_couverture=$ko entete_reprise=$h entete_cite_D-01=$d1"; [ "$n" -eq 4 ] && [ "$own" -eq 0 ] && [ "$me" = oui ] && [ "$ko" -eq 0 ] && [ "$h" -ge 1 ] && [ "$d1" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `regles=` différent de 4 ; ou `proprietaire_hors_picmakpro=` différent de 0 (autre propriétaire, propriétaire en plus, commentaire en fin de ligne) ; ou `motifs_egaux_attendu=non` (motif non ancré, élargi ou réécrit) ; ou une ligne `NON COUVERT` (fichier point des sentinelles, baseline, hooks, `.github/`) ; ou une ligne `COUVERT A TORT` (périmètre élargi au-delà de D-05, ex. `/scripts/` — la suite de preuve de 41-07 doit rester hors CODEOWNERS pour servir de témoin) ; ou `entete_reprise=0` (en-tête muet sur Willy / D-02bis) ; ou `entete_cite_D-01=` différent de 0 (D-01 est faux comme fait)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash scripts/check-machine-paths.sh > /dev/null 2>&1 || rc=$?; echo "check-machine-paths rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul : un chemin de machine est entré dans un fichier suivi</fails_when>
  </verify>
  <acceptance_criteria>
    - 4 règles exactement, motifs ancrés identiques à D-05, propriétaire unique `@picmakpro`
    - les 9 chemins protégés couverts (dont les deux sentinelles `.planning/.*-armed`), les 8 témoins négatifs non couverts (dont `scripts/tests/test-preuve-41-m1.sh`)
    - en-tête citant D-05, D-02bis, le fait « `picmakpro` = Willy », l'impossibilité d'auto-approbation, la lecture sur branche de base et les deux sources de syntaxe ; D-01 non cité
  </acceptance_criteria>
  <done>Le fichier dit exactement le périmètre arbitré, sur un fait juste, et la couverture est prouvée dans les deux sens.</done>
</task>

<task type="auto">
  <name>Task 2 : amendement du ledger — PROT-01 repris (non coché), traçabilité « Pending — reprise », rejeu des gates</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (section `### Posture de protection du dépôt (Phase 41 — périmètre sans admin)` : entrée PROT-01 et ses lignes de suite ; table `## Traceability`, lignes `| PROT-0N |`)
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md (§ REPRISE, D-02bis)
  </read_first>
  <action>
Amender, jamais recréer (PROT-01..05 existent depuis les plans 41-14 à 41-19) :
- entrée PROT-01 : laisser `- [ ]` et toutes ses lignes existantes intactes ; ajouter, juste après sa
  dernière ligne de suite, de nouvelles lignes de suite au format du ledger (indentées de deux
  espaces, aucune ne commençant par un tiret, largeur d'environ 100 caractères) : « Reprise
  (2026-09-23) : fait corrigé — `picmakpro` est le compte de Willy, admin du dépôt (`41-CONTEXT.md`
  § REPRISE) ; accès admin constaté, volet côté serveur replanifié (plans 41-01 à 41-13) ;
  contournement accordé à Samuel et Willy nommément, mode `always` (D-02bis — arbitrage Willy,
  AskUserQuestion session principale, 2026-09-23). Coché seulement à la clôture, sur pièce
  (`41-PREUVES.md`, plan 41-13). » ;
- table `## Traceability`, ligne PROT-01 : remplacer la seule cellule de statut par « Pending —
  reprise du 2026-09-23 (accès admin constaté, D-02bis) : volet côté serveur replanifié en 41-01 à
  41-13 ; statut antérieur : hors d'atteinte (2026-09-18) » ;
- PROT-02..05 (cochés), leurs lignes de traçabilité, le titre de section et toute autre ligne du
  fichier : octet-identiques.
O-3 n'est jamais associée, même par négation, à un mot d'achèvement (D-02). STATE.md et ROADMAP.md
non touchés. Commit local sur la branche A, message « docs(41-02): PROT-01 repris au ledger, non
coché (D-02bis — arbitrage Willy, AskUserQuestion session principale, 2026-09-23) ».
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'R=.planning/REQUIREMENTS.md; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); git show "$B:$R" > "$T/o"; blk() { awk -v id="$1" "/^- \\[[ x]\\] \\*\\*PROT-0[0-9]\\*\\*/ { w = index(\$0, \"**\" id \"**\") ? 1 : 0; if (w) print; next } w && /^  [^ -]/ { print; next } { w = 0 }" "$2"; }; k=0; for id in PROT-02 PROT-03 PROT-04 PROT-05; do blk "$id" "$R" > "$T/n"; blk "$id" "$T/o" > "$T/b"; [ -s "$T/b" ] && cmp -s "$T/n" "$T/b" || { echo "ENTREE MODIFIEE OU ABSENTE $id"; k=$((k+1)); }; done; blk PROT-01 "$R" > "$T/n1"; blk PROT-01 "$T/o" > "$T/b1"; nb=$(awk "END{print NR}" "$T/b1"); nn=$(awk "END{print NR}" "$T/n1"); awk -v n="$nb" "NR<=n" "$T/n1" > "$T/pre"; pre=$(cmp -s "$T/pre" "$T/b1" && echo oui || echo non); nc=$(awk "NR==1 && /^- \\[ \\] \\*\\*PROT-01\\*\\*:/ {c++} END{print c+0}" "$T/n1"); J=$(awk "{printf \"%s \", \$0}" "$T/n1" | tr -s " "); m=0; for t in "Reprise (2026-09-23)" "D-02bis" "arbitrage Willy, AskUserQuestion session principale, 2026-09-23" "41-CONTEXT.md" "41-PREUVES.md"; do case "$J" in *"$t"*) ;; *) echo "ABSENT PROT-01 : $t"; m=$((m+1));; esac; done; tp=$(awk "/^\\| PROT-01 \\| Phase 41 \\| Pending — reprise du 2026-09-23/ {c++} END{print c+0}" "$R"); awk "/^\\| PROT-0[2-5] \\|/" "$R" > "$T/tn"; awk "/^\\| PROT-0[2-5] \\|/" "$T/o" > "$T/to"; tr=$(cmp -s "$T/tn" "$T/to" && echo oui || echo non); git diff -U0 "$B" -- "$R" > "$T/d"; rm1=$(awk "/^-[^-]/ {c++} END{print c+0}" "$T/d"); rmp=$(awk "/^-\\| PROT-01 \\|/ {c++} END{print c+0}" "$T/d"); echo "ledger : entrees_02_05_modifiees=$k prot01_non_coche=$nc prot01_prefixe_intact=$pre lignes_prot01=$nb/$nn termes_absents=$m trace_prot01_reprise=$tp traces_02_05_intactes=$tr lignes_retirees=$rm1 dont_trace_prot01=$rmp"; [ "$k" -eq 0 ] && [ "$nc" -eq 1 ] && [ "$pre" = oui ] && [ "$nn" -gt "$nb" ] && [ "$m" -eq 0 ] && [ "$tp" -eq 1 ] && [ "$tr" = oui ] && [ "$rm1" -eq 1 ] && [ "$rmp" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou une entrée PROT-02..05 modifiée ou absente (`ENTREE MODIFIEE OU ABSENTE`) ; ou PROT-01 coché (`prot01_non_coche=0` : coché avant la clôture sur pièce) ; ou lignes existantes de PROT-01 réécrites (`prot01_prefixe_intact=non`) ou aucune ligne ajoutée ; ou une ligne `ABSENT PROT-01 :` (reprise, D-02bis, canal et date de Willy, renvois) ; ou `trace_prot01_reprise=` différent de 1 ; ou `traces_02_05_intactes=non` ; ou `lignes_retirees=` différent de 1 ou la ligne retirée n'est pas la ligne de traçabilité de PROT-01 (une autre ligne du ledger a été modifiée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/REQUIREMENTS.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10 ; ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 ; ou bilan absent ; ou N = 0 ; ou N différent de `decouverte_independante` ; ou M différent de 0. Un rouge qui se reproduit sur une extraction `git archive` d'`origin/main` est préexistant : consigné et remonté `human_needed`, jamais neutralisé</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (citation d'un arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou `*-DECISION` sans « arbitrage <décideur>, <canal>, <date> » égal aux valeurs capturées) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - PROT-01 non coché, lignes existantes intactes, lignes « Reprise (2026-09-23) » ajoutées (fait corrigé, D-02bis avec canal et date, renvois)
    - ligne de traçabilité PROT-01 « Pending — reprise du 2026-09-23 » ; PROT-02..05 et leurs lignes de traçabilité octet-identiques ; une seule ligne retirée du fichier (l'ancienne cellule de statut de PROT-01)
    - aucune proposition du ledger n'associe O-3 à un mot d'achèvement ; rejeux `gates` et `tests` verts ; messages de commit conformes (canal et date)
  </acceptance_criteria>
  <done>Le ledger dit que PROT-01 est de nouveau atteignable, sans rien déclarer prouvé ni réécrire ce qui est coché.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → branche de base | CODEOWNERS n'est lu que sur la base : une PR ne peut pas desserrer sa propre revue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-08 | Tampering | motif CODEOWNERS ne couvrant pas les sentinelles point | high | mitigate | témoins positifs sur les deux `.planning/.*-armed` par le moteur gitignore de git ; preuve GitHub réelle aux plans 41-04 (`codeowners/errors`) et 41-06 (revue requise sur la baseline et la sentinelle) |
| T-41-09 | Elevation of privilege | revue code owner contournée par Samuel ou Willy | medium | accept | arbitré D-02bis (« garde-fou par défaut + trace, pas verrou ») ; la trace est prouvée au plan 41-08 |
| T-41-10 | Repudiation | exigence cochée sans preuve | medium | mitigate | PROT-01 laissé `- [ ]` (vérifié) ; cochage réservé au plan 41-13 sur lignes de `41-PREUVES.md` |
| T-41-58 | Repudiation | fait faux (« tiers ») laissé comme vérité dans l'en-tête ou le ledger | low | mitigate | en-tête sans D-01, fait corrigé écrit dans les lignes de reprise de PROT-01 |
</threat_model>

<verification>
CODEOWNERS à quatre règles prouvé dans les deux sens, en-tête fidèle à la reprise ; PROT-01 repris
non coché, le reste du ledger intact ; gates et tests verts.
</verification>

<success_criteria>
PROT-04 a sa source versionnée côté serveur ; le ledger est à jour de la reprise dans la PR de la phase.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-02-SUMMARY.md` when done (commit local sur la branche A `feat/phase-41-volet-admin`).
</output>

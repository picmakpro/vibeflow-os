---
phase: 41-posture-de-protection-du-d-p-t
plan: 18
lot: L18 — G-4 : doctrine — ADR-072, résumé CLAUDE.md, BACKLOG par renvoi, O-3 « signalée et tracée »
type: execute
wave: 5
depends_on: ["41-14", "41-16", "41-17"]
autonomous: true
requirements: [PROT-03, PROT-04, PROT-05]
files_modified:
  - docs/ADR.md
  - CLAUDE.md
  - .planning/BACKLOG.md
  - .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PÉRIMÈTRE — périmètre SANS ADMIN (option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). L'amendement d'ADR-059 sur la PR obligatoire est HORS PÉRIMÈTRE et l'ADR-072 doit le DIRE, avec sa raison : aucune règle côté serveur ne l'appliquerait. Aucun ruleset, aucun CODEOWNERS, aucune fermeture de #29, aucun bump de la VERSION racine, aucune mesure M-1 à M-4"
    - "ADR-072 « Gardes in-repo sans règle côté serveur » est écrite dans `docs/ADR.md` (numéro libre : ADR-071 est la dernière SECTION) AVEC sa ligne d'index dans la table, au format exact des lignes voisines (`| ADR-072 | 2026-09-17 | <titre> | Validée |`)"
    - "ANCRE D'INSERTION DE L'INDEX — MESURÉE, pas supposée (avertissement 6 du vérificateur frais) : la table d'index de `docs/ADR.md` s'arrête à la ligne **ADR-070** (ligne 41 au moment du cadrage) ; **ADR-071 n'a PAS de ligne d'index**, alors que sa section existe. La ligne d'ADR-072 s'insère donc APRÈS la ligne ADR-070. La vérification PROUVE que l'ancre utilisée existe, au lieu de la supposer"
    - "L'ABSENCE D'INDEX D'ADR-071 N'EST PAS CORRIGÉE ICI (décision du manager, 2026-09-17) : hors périmètre. Elle est consignée au `BACKLOG.md` en une entrée courte, avec son déclencheur de reprise « prochain passage sur `docs/ADR.md` ». Corriger au passage une dérive non arbitrée, c'est exactement ce que cette phase refuse de faire"
    - "ADR-072 dit TROIS choses séparément, sans les mélanger : CE QUI EST GARDÉ (G-1 hausse de baseline ou neutralisation de sentinelle sans arbitrage cité ; G-2 surface de gate touchée sans marqueur déclaratif ; G-3 alarme après coup sur un push direct) ; CE QUI NE L'EST PAS (aucune PR obligatoire, aucun check requis, aucune revue, aucun verrou — et la limite de fond : une garde qui vit dans le dépôt peut être modifiée par la PR qu'elle juge, une même PR peut changer le gate, sa suite et l'étape CI et rester verte) ; CE QUI ATTEND UN ACCÈS ADMIN (rulesets de branche et de tags, bypass, CODEOWNERS et revue code owner, PR obligatoire, mesures M-1 à M-4, fermeture de #29, rejeu du flux de release sous la règle — renvoi au `BACKLOG.md` § « Protection de `main` côté GitHub — DIFFÉRÉ » et au `41-CONTEXT.md` § Prémisse renversée, D-01 à D-08 suspendus, non annulés)"
    - "PROT-03 — la politique de contournement et de hotfix est écrite dans ADR-072 : sans règle côté serveur, RIEN n'est bloqué et personne n'a besoin de contourner quoi que ce soit ; la discipline tient par la PR par défaut, le marqueur `Gate-Touche:` sur toute modification de la surface de gate, la citation d'arbitrage sur toute hausse de baseline, et l'alarme après coup sur un push direct. Un hotfix urgent passe par une PR ; s'il passe en direct, G-3 le signale, daté, et c'est le prix assumé"
    - "PROT-02 — ADR-072 écrit que le flux de release du `CLAUDE.md` est INCHANGÉ (bump → PR → merge → tag annoté → release GitHub → `check-release-tag --remote` ✓), que le push d'un tag ne déclenche pas la CI (`on.push.branches` sans `tags:`) et que le hook `pre-push` reste tel quel — G-5, son durcissement, a été écartée"
    - "`CLAUDE.md` porte un résumé — une section, dense — nommant les trois gardes, la forme du trailer `Gate-Touche: <chemin-ou-motif> — <raison>`, la règle de citation d'arbitrage sur une hausse de baseline, et la limite de fond, avec un renvoi à ADR-072"
    - "CONTRAINTE MACHINE sur `CLAUDE.md` : tout jeton de chemin entre accents graves contenant une barre oblique doit RÉSOUDRE SUR LE DISQUE — `check-map-drift.sh` lit les cartes d'un `CLAUDE.md` (paire P1). Donc jamais de glob entre accents graves (`scripts/check-*.sh` est interdit sous cette forme) : nommer les trois chemins concrets, qui existent depuis les plans 41-14, 41-16 et 41-17. Vérifié par `check-map-drift.sh --path .`"
    - "L'item BACKLOG de la Phase 41 est traité PAR RENVOI, jamais supprimé : une ligne de statut ajoutée SOUS chaque item concerné (« Protection de `main` côté GitHub — DIFFÉRÉ » et « Posture de protection de `main` — TRANCHÉ »), renvoyant à ADR-072 et aux plans du périmètre sans admin ; aucune ligne existante de ces items n'est modifiée"
    - "O-3 du `25-SECURITY.md` passe à « SIGNALÉE ET TRACÉE » — jamais « fermée », jamais « close ». Le texte d'O-3 garde ce qui reste vrai (`main` n'a aucune protection ; le gate rend 0 sur une baseline relevée à la main si personne ne relit), et ajoute ce qui a changé : la hausse non citée et la modification de la surface de gate sont désormais SIGNALÉES par G-1 et G-2. La ligne de la table STRIDE `T-25-16` n'est PAS modifiée : sa colonne porte sur la menace, pas sur l'observation"
    - "Le recensement `tools/check-aucune-fermeture.sh` (plan 41-15) rend 0 après CHAQUE tâche de ce plan — c'est le plan qui écrit le plus de doctrine, donc celui qui risque le plus de glisser un mot d'achèvement"
    - "TRAÇABILITÉ — un seul arbitrage ou décision cité par commit ; tout commit qui cite l'arbitrage du périmètre écrit exactement « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ». Aucun fichier de ce plan n'est dans la surface de G-2, donc aucun trailer `Gate-Touche:` n'est dû"
  artifacts:
    - path: "docs/ADR.md"
      provides: "ADR-072 et sa ligne d'index"
      contains: "ADR-072"
    - path: "CLAUDE.md"
      provides: "résumé de la posture in-repo et convention du trailer Gate-Touche"
    - path: ".planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md"
      provides: "O-3 « signalée et tracée »"
  key_links:
    - "ADR-072 décrit les trois gardes TELLES QU'ELLES SONT LIVRÉES : lire les trois SUMMARY (41-14, 41-16, 41-17) et recopier les verdicts et bornes réels, jamais l'intention du plan. Un écart entre l'ADR et un script livré est un rouge à remonter, pas à arrondir"
---

<objective>
Écrire la doctrine du périmètre sans admin : ce qui est gardé, ce qui ne l'est pas, ce qui attend un
accès admin. ADR-072 et son index, un résumé dans `CLAUDE.md`, les items BACKLOG traités par renvoi,
et le statut d'O-3 porté à « signalée et tracée ».

Purpose: PROT-03 (politique de contournement et de hotfix écrite) et PROT-04 (O-3 signalée et
tracée). Sans cette doctrine, les trois gardes seraient des scripts sans posture, et le lecteur
d'après ne saurait pas ce qu'elles ne font pas.
Output: une ADR, un résumé de doctrine, deux renvois de backlog, un statut d'observation révisé.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-14-SUMMARY.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-16-SUMMARY.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-17-SUMMARY.md
@CLAUDE.md

Fichiers à lire au début de la Task 1, jamais deux fois la même plage :
- `docs/ADR.md` : la table d'index (lignes ADR-050 à ADR-071, format exact) et la section
  `## ADR-071` (dernière section, forme d'une entrée : titre, contexte, décision, conséquences)
- `.planning/BACKLOG.md` : les deux items de la phase — « Protection de `main` côté GitHub —
  DIFFÉRÉ, en attente d'un accès admin (2026-09-17) » et « Posture de protection de `main` —
  TRANCHÉ : phase dédiée à inscrire (2026-09-15) »
- `.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md` :
  la table STRIDE autour de la ligne `T-25-16` et la section `## Observations`, entrée O-3
</context>

<discipline>
Contraintes d'exécution valables pour TOUTES les tâches de ce plan.

- **Aucun texte produit ici n'associe O-3 ni une garde à un mot d'achèvement** (`fermé`, `close`,
  `clos`). La formule exacte pour O-3 est « signalée et tracée ». Rejouer
  `tools/check-aucune-fermeture.sh` après CHAQUE tâche, et reformuler plutôt que d'ajouter une
  entrée à l'allowlist.
- **Aucune commande git en ligne dans un `<verify>` ni dans un appel bash direct** (worktree
  isolé : le harnais refuse une commande qui nomme git sous une forme non rattachable).
- **Commandes proxifiées** : `diff` et `ls` mentent sur ce poste ; les préfixer de `rtk proxy`.
  Jamais de comptage par `grep | wc -l` — extraire puis comparer par `comm` / `cmp`, ou `awk`.
- **Aucune ligne existante des items BACKLOG concernés n'est modifiée** : on AJOUTE une ligne de
  statut sous l'item. Un item du backlog est une trace datée ; le réécrire efface l'histoire.
- **`.planning/ROADMAP.md`, `.planning/STATE.md` et `.planning/REQUIREMENTS.md` ne sont PAS touchés
  par ce plan.** Le ledger est le geste du plan de clôture 41-19 ; le ROADMAP et STATE ne sont dans
  le périmètre d'aucun plan de cette phase.
</discipline>

<tasks>

<task type="tracer">
  <name>Task 1 : ADR-072 et sa ligne d'index — ce qui est gardé, ce qui ne l'est pas, ce qui attend un accès admin</name>
  <files>docs/ADR.md</files>
  <read_first>
    - les trois SUMMARY 41-14, 41-16, 41-17 (verdicts réels, bornes nommées, codes de sortie livrés)
    - `docs/ADR.md` : table d'index et section `## ADR-071`
  </read_first>
  <action>
Ajouter dans `docs/ADR.md` une ligne d'index, au format exact des lignes voisines :
`| ADR-072 | 2026-09-17 | Gardes in-repo sans règle côté serveur — ce qui est gardé, ce qui ne l'est
pas, ce qui attend un accès admin | Validée |`, placée **après la ligne ADR-070** — mesuré le
2026-09-17 : la table d'index s'arrête à ADR-070, ADR-071 n'y a jamais été inscrite alors que sa
section existe (avertissement 6 du vérificateur frais). Vérifier que l'ancre existe AVANT d'insérer,
et ne PAS ajouter la ligne d'index manquante d'ADR-071 : elle est hors périmètre et part au BACKLOG
en Task 3 (décision du manager, 2026-09-17). Puis une section
`## ADR-072 : …` après la section `## ADR-071`, dans la forme des entrées voisines.

Contenu, en quatre parties distinctes et dans cet ordre.

**Contexte.** Le compte `picmakpro`, seul admin du dépôt, appartient à un tiers ; les permissions
mesurées le 2026-09-17 sont `admin: false, maintain: false, push: true`. Personne, dans les
conditions actuelles, ne peut poser un ruleset, une PR obligatoire, un check requis ni une revue
code owner. La Phase 41 a donc été recadrée sur ce qui est faisable avec le seul droit `push`
(option (a), arbitrage Samuel, AskUserQuestion session principale, 2026-09-17). Origine du besoin :
O-3 du `25-SECURITY.md` — une hausse de baseline n'était gardée que par la relecture, et une même PR
peut modifier un gate, sa suite et l'étape CI qui l'invoque. Fait mesuré à citer : `892f89a`
(2026-09-16) est arrivé sur `main` sans PR, avec un run CI rouge, sans que rien ne l'arrête.

**Décision — ce qui est gardé.** Les trois gardes, décrites TELLES QUE LIVRÉES (lire les SUMMARY,
recopier les verdicts et les codes de sortie réels, jamais l'intention) : G-1
`scripts/check-baseline-arbitrage.sh`, hausse d'une valeur de la baseline du budget d'instructions
ou neutralisation d'une sentinelle d'armement sans citation d'arbitrage portant canal et date,
comparée à une base dérivée par `merge-base` ; G-2 `scripts/check-gate-touche.sh`, surface de gate
touchée (cinq classes, nommées) sans marqueur déclaratif `Gate-Touche: <chemin-ou-motif> —
<raison>`, de portée branche ; G-3 `scripts/check-push-sans-pr.sh`, alarme APRÈS COUP sur un commit
arrivé sur `main` sans PR associée, par deux lectures en cascade, sur `push` vers `main` seulement.
Chacune est câblée dans le job `gates` de `ci.yml` et prouvée capable de rougir par des bascules de
fixture dans son étape et par des mutants dans sa suite.

**Décision — les deux bornes de G-1, avec leur motif** (décisions du manager, 2026-09-17, sur
remontée du planificateur ; à écrire dans l'ADR, pas seulement dans l'en-tête du script). (1) Seule
la colonne des INSTRUCTIONS est bloquante ; une hausse de la colonne des lignes donne un
avertissement non bloquant. Motif : depuis la Phase 40.1 le ratchet ne porte que sur les
instructions, la colonne lignes est publiée mais jamais comparée pour décider d'un code de sortie ;
exiger un arbitrage humain pour une colonne informative fabriquerait de la friction sans rien
garder. (2) Le retrait d'une ligne de baseline suit **trois cas**, avec la même exigence de trace
qu'une hausse : cible encore présente dans l'arbre et aucun arbitrage cité dans le commit qui retire
la ligne → bloquant ; cible encore présente et arbitrage cité (canal et date) → avertissement non
bloquant, la citation étant recopiée dans le rapport ; cible disparue du dépôt → avertissement non
bloquant, il n'y a rien à citer. Motif : une recalibration légitime peut sortir un fichier du corpus
sans le supprimer ; la garde ne l'en empêche pas, elle exige que ce soit **dit et daté** — on rend
visible et tracé, on ne ferme pas. Comme partout dans cette posture, la garde vérifie la FORME de la
citation, jamais sa véracité.

**Décision — l'exigence PROT-05.** Écrire que le recadrage sans admin a laissé G-2 et G-3 sans foyer
d'exigence, et que le manager a créé le 2026-09-17 l'exigence **PROT-05** « les gardes in-repo de la
surface de gate et du flux d'arrivée sur `main` rendent une atteinte visible et tracée ; aucune n'est présentée comme une protection absolue ». PROT-04
reste le foyer de G-1 (statut d'O-3), PROT-03 celui de cette ADR, et PROT-02 garde son énoncé
d'origine.

**Décision — ce qui n'est PAS gardé.** Sans ambiguïté et sans adoucissement : aucune PR n'est
obligatoire, aucun check n'est requis, aucune revue n'est exigée, aucun push n'est refusé. La limite
de fond, à écrire en toutes lettres : une garde qui vit dans le dépôt peut être modifiée par la PR
qu'elle juge ; une même PR peut changer le gate, sa suite et l'étape CI qui l'invoque, et rester
verte. G-2 rend ce geste visible et tracé ; elle ne l'empêche pas, et G-2 est elle-même dans la
surface qu'elle surveille. G-3 constate après coup : quand elle rougit, le commit est déjà sur
`main`. Le marqueur de G-2 et la citation de G-1 sont DÉCLARATIFS : les gardes vérifient une forme,
jamais une véracité. Conclusion à écrire : cette phase ne clôt rien, elle rend visible et tracé.

**Décision — ce qui attend un accès admin.** Rulesets de branche et de tags, liste de bypass, revue
code owner et `CODEOWNERS`, PR obligatoire, checks requis épinglés, mesures M-1 à M-4, fermeture de
la PR #29, rejeu du flux de release sous la règle. Renvoyer nommément au `BACKLOG.md` § « Protection
de `main` côté GitHub — DIFFÉRÉ » et au `41-CONTEXT.md` § Prémisse renversée : les décisions D-01 à
D-08 sont SUSPENDUES, pas annulées, et reprennent vie telles quelles le jour où l'accès existe.
Écrire le déclencheur de reprise : un accès admin accordé, ou un transfert du dépôt.

**Politique de contournement et de hotfix (PROT-03).** Écrire que la question du contournement ne se
pose pas au sens strict, puisque rien n'est bloqué : la discipline tient par quatre gestes — PR par
défaut ; marqueur `Gate-Touche:` sur toute modification de la surface de gate ; citation
d'arbitrage, canal et date, sur toute hausse de baseline ; et l'acceptation que G-3 signale, daté,
tout commit entré en direct. Un hotfix urgent passe par une PR ; s'il passe en direct, il est
signalé après coup et c'est le prix assumé. Écrire que **l'amendement d'ADR-059 sur la PR obligatoire
est hors périmètre**, avec sa raison : aucune règle côté serveur ne l'appliquerait, donc l'écrire
serait déclarer une couverture sans couverture effective.

**Compatibilité du flux de release (PROT-02).** Le flux du `CLAUDE.md` est inchangé : bump → PR →
merge → tag annoté → release GitHub → `check-release-tag --remote` ✓. Les trois étapes neuves sont
insérées avant l'étape `check-release-tag` et ne changent ni sa condition ni son contenu. Le push
d'un tag ne déclenche pas la CI (`on.push.branches`, aucune entrée `tags:`), donc G-3 ne peut pas
rougir sur une release. Le hook `pre-push` reste tel quel : son durcissement (G-5) a été écarté du
périmètre.

Rejouer le recensement après écriture. S'il rougit sur une ligne de l'ADR, reformuler la ligne.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'i=$(awk "/^\\| ADR-072 \\| 2026-09-17 \\|/ && /Validée/{c++} END{print c+0}" docs/ADR.md); s=$(awk "/^## ADR-072 :/{c++} END{print c+0}" docs/ADR.md); o=$(awk "/^## ADR-071/{c++} END{print c+0}" docs/ADR.md); a=$(awk "/^\\| ADR-070 \\|/{c++} END{print c+0}" docs/ADR.md); suit=$(awk "/^\\| ADR-070 \\|/{p=NR} /^\\| ADR-072 \\|/{q=NR} END{ print (p>0 && q==p+1) ? \"oui\" : \"non\" }" docs/ADR.md); k=$(awk "/^\\| ADR-071 \\|/{c++} END{print c+0}" docs/ADR.md); echo "index_ADR072=$i section_ADR072=$s section_ADR071_intacte=$o ancre_ADR070_existe=$a ADR072_suit_ancre=$suit index_ADR071_ajoute=$k"; [ "$i" -eq 1 ] && [ "$s" -eq 1 ] && [ "$o" -eq 1 ] && [ "$a" -eq 1 ] && [ "$suit" = oui ] && [ "$k" -eq 0 ]'</automated>
    <fails_when>`index_ADR072=` différent de 1 (ligne d'index absente ou dupliquée) ; ou `section_ADR072=` différent de 1 ; ou `section_ADR071_intacte=` différent de 1 (une entrée existante a été écrasée) ; ou `ancre_ADR070_existe=` différent de 1 (l'ancre supposée n'existe pas — c'était le défaut relevé par l'avertissement 6) ; ou `ADR072_suit_ancre=non` (la ligne n'est pas immédiatement après l'ancre mesurée) ; ou `index_ADR071_ajoute=` différent de 0 (l'exécutant a corrigé au passage une dérive non arbitrée : elle va au BACKLOG, pas dans ce diff)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); awk "/^## ADR-072 :/{f=1} f && /^## ADR-07[3-9]/{f=0} f{print}" docs/ADR.md > "$T/a"; g=$(awk "/check-baseline-arbitrage/{a=1} /check-gate-touche/{b=1} /check-push-sans-pr/{c=1} END{print a+b+c}" "$T/a"); n=$(awk "/D-01 à D-08/{a=1} /ADR-059/{b=1} /check-release-tag/{c=1} /pre-push/{d=1} /Gate-Touche/{e=1} END{print a+b+c+d+e}" "$T/a"); m=$(awk "/colonne/ && /instruction/{a=1} /cible/{b=1} /PROT-05/{c=1} END{print a+b+c}" "$T/a"); l=$(awk "END{print NR}" "$T/a"); echo "ADR072 gardes_nommees=$g renvois_obligatoires=$n motifs_2026-09-17=$m lignes=$l"; [ "$g" -eq 3 ] && [ "$n" -eq 5 ] && [ "$m" -eq 3 ] && [ "$l" -ge 55 ]'</automated>
    <fails_when>`gardes_nommees=` différent de 3 (une garde livrée n'est pas décrite) ; ou `renvois_obligatoires=` différent de 5 (il manque la suspension D-01 à D-08, le hors-périmètre ADR-059, la compatibilité `check-release-tag`, le sort du hook `pre-push`, ou la forme du trailer) ; ou `motifs_2026-09-17=` différent de 3 (il manque le motif de la colonne bloquante, celui du retrait de ligne conditionné à l'existence de la cible, ou la création de PROT-05) ; ou `lignes=` inférieur à 55</fails_when>
    <automated>bash --noprofile --norc -e -c 'r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh || r=$?; echo "recensement apres ADR-072 rc=$r"; [ "$r" -eq 0 ]'</automated>
    <fails_when>rc différent de 0 (l'ADR associe une garde ou O-3 à un mot d'achèvement ; conduite : reformuler la ligne, jamais l'ajouter à l'allowlist)</fails_when>
  </verify>
  <acceptance_criteria>
    - ligne d'index ADR-072 unique, au format des voisines, immédiatement après l'ancre MESURÉE `| ADR-070 |` ; section ADR-072 unique ; section ADR-071 intacte ; aucune ligne d'index ADR-071 ajoutée (hors périmètre)
    - les trois gardes sont nommées par leur chemin réel et décrites d'après les SUMMARY livrés
    - les cinq renvois obligatoires sont présents : D-01 à D-08 suspendus, ADR-059 hors périmètre avec sa raison, `check-release-tag` inchangé, hook `pre-push` conservé, forme du trailer
    - les trois décisions du manager du 2026-09-17 sont écrites avec leur motif : colonne des instructions seule bloquante, retrait de ligne conditionné à l'existence de la cible, création de PROT-05
    - recensement rc 0 après écriture
  </acceptance_criteria>
  <done>La doctrine du périmètre sans admin est écrite, séparant ce qui est gardé, ce qui ne l'est pas et ce qui attend un accès admin — sans déclarer d'achèvement.</done>
</task>

<task type="auto">
  <name>Task 2 : résumé dans CLAUDE.md, sous la contrainte de carte de check-map-drift</name>
  <files>CLAUDE.md</files>
  <precondition>ADR-072 existe dans `docs/ADR.md` (Task 1 verte) ; les trois scripts de garde existent sur le disque (plans 41-14, 41-16, 41-17).</precondition>
  <read_first>
    - `plugin/conductor/scripts/check-map-drift.sh` lignes 144-175 (extraction des jetons de la paire P1 : pointeurs `@chemin` et segments entre accents graves contenant une barre oblique ; règle d'exclusion d'une ligne de commande)
  </read_first>
  <action>
Ajouter à `CLAUDE.md`, après la section `## Conventions transverses`, une section
`## Gardes in-repo — ce qui est gardé, ce qui ne l'est pas`, dense (c'est un fichier lu en premier
par les agents ; viser une vingtaine de lignes, pas une page). Contenu :
- une phrase de posture : `main` n'est protégée par aucune règle côté serveur — aucun accès admin —
  donc les gardes de ce dépôt rendent visible et tracent, elles ne verrouillent rien ; renvoi à
  ADR-072 pour le détail ;
- les trois gardes en une ligne chacune, nommées par leur chemin concret, avec ce qui les fait
  rougir ;
- la convention du marqueur : tout commit qui touche un gate, sa suite, le workflow CI ou un hook
  porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` ; le marqueur est déclaratif, sa
  présence et sa forme sont vérifiées, jamais la véracité de la raison ; sa portée est la branche,
  donc un commit ultérieur peut couvrir un chemin touché plus tôt ;
- la règle de la baseline : toute hausse d'une valeur de la baseline du budget d'instructions exige,
  dans le commit qui la fait, une citation d'arbitrage avec son canal et sa date — exactement la
  convention de traçabilité déjà énoncée plus haut dans ce fichier ;
- la limite de fond, en une phrase : une garde qui vit dans le dépôt peut être modifiée par la PR
  qu'elle juge.

**Contrainte machine, à respecter à la lettre.** `check-map-drift.sh` lit les cartes d'un
`CLAUDE.md` : tout jeton entre accents graves contenant une barre oblique est traité comme un chemin
déclaré et doit RÉSOUDRE SUR LE DISQUE. Donc : nommer les trois chemins concrets entre accents
graves (ils existent depuis les plans précédents) ; ne JAMAIS écrire un glob entre accents graves ;
si un motif doit être évoqué, l'écrire hors accents graves ou dans une ligne de commande complète
(un jeton dont le premier mot ne contient pas de barre oblique est traité comme une invocation, pas
comme un chemin). Vérifier par `check-map-drift.sh --path .` — un rouge se corrige en retirant le
jeton fautif, jamais en modifiant le gate.

Ne pas toucher aux sections existantes de `CLAUDE.md`, en particulier la règle non négociable de
discipline de release et la section « Traçabilité des arbitrages » : la nouvelle section les
prolonge, elle ne les réécrit pas. Rejouer le recensement après écriture.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 's=$(awk "/^## Gardes in-repo/{c++} END{print c+0}" CLAUDE.md); g=$(awk "/check-baseline-arbitrage/{a=1} /check-gate-touche/{b=1} /check-push-sans-pr/{c=1} END{print a+b+c}" CLAUDE.md); t=$(awk "/Gate-Touche/{c++} END{print c+0}" CLAUDE.md); a=$(awk "/^## Règle non négociable/{c++} END{print c+0}" CLAUDE.md); r=$(awk "/Traçabilité des arbitrages/{c++} END{print c+0}" CLAUDE.md); echo "section=$s gardes=$g trailer=$t regle_release_intacte=$a tracabilite_intacte=$r"; [ "$s" -eq 1 ] && [ "$g" -eq 3 ] && [ "$t" -ge 1 ] && [ "$a" -eq 1 ] && [ "$r" -ge 1 ]'</automated>
    <fails_when>`section=` différent de 1 ; ou `gardes=` différent de 3 ; ou `trailer=` à 0 ; ou `regle_release_intacte=` différent de 1 ; ou `tracabilite_intacte=` à 0 (une section existante a été écrasée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; out=$(bash plugin/conductor/scripts/check-map-drift.sh --path . 2>&1) || rc=$?; printf "%s\n" "$out"; echo "map-drift rc=$rc"; case "$rc" in 0|3) : ;; *) exit 1 ;; esac'</automated>
    <fails_when>rc différent de 0 et de 3 (un jeton de chemin du `CLAUDE.md` ne résout pas sur le disque : un glob a été écrit entre accents graves, ou un chemin est faux). Conduite : retirer ou reformuler le jeton, jamais modifier le gate</fails_when>
    <automated>bash --noprofile --norc -e -c 'r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh || r=$?; echo "recensement apres CLAUDE.md rc=$r"; [ "$r" -eq 0 ]'</automated>
    <fails_when>rc différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - une section unique, les trois gardes nommées par leur chemin concret, la convention du trailer et la limite de fond présentes
    - les sections existantes de `CLAUDE.md` sont intactes (règle de release, traçabilité des arbitrages)
    - `check-map-drift.sh --path .` rend 0 ou 3 : aucun jeton de chemin non résolvable ajouté
    - recensement rc 0
  </acceptance_criteria>
  <done>La doctrine est lisible par le prochain agent dès l'ouverture du dépôt, et elle ne casse pas la carte que le dépôt vérifie déjà.</done>
</task>

<task type="auto">
  <name>Task 3 : BACKLOG par renvoi, et O-3 portée à « signalée et tracée »</name>
  <files>.planning/BACKLOG.md, .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md</files>
  <precondition>ADR-072 et la section de `CLAUDE.md` existent (Tasks 1 et 2 vertes).</precondition>
  <action>
**`.planning/BACKLOG.md` — deux renvois, aucune suppression, aucune ligne existante modifiée.**

Sous l'item « Protection de `main` côté GitHub — DIFFÉRÉ, en attente d'un accès admin
(2026-09-17) », AJOUTER une ligne de statut au format des statuts partiels déjà utilisés dans ce
fichier : `**Statut partiel (<date d'exécution>, Phase 41) :**` suivi de — le périmètre sans admin
est livré par les plans 41-14 à 41-19 (trois gardes in-repo, doctrine ADR-072) ; le volet côté
serveur reste différé tel quel, son déclencheur de reprise inchangé ; les décisions D-01 à D-08
restent suspendues ; renvoi à ADR-072.

**Troisième écriture — un constat court, hors périmètre, avec son déclencheur** (avertissement 6 du
vérificateur frais, décision du manager du 2026-09-17). Ajouter au `BACKLOG.md` une entrée courte
`## Ligne d'index absente pour ADR-071 dans \`docs/ADR.md\` — DIFFÉRÉ (<date d'exécution>)` : constat
mesuré le 2026-09-17 en posant ADR-072, la table d'index s'arrête à ADR-070 alors que la section
ADR-071 existe ; **hors périmètre de la Phase 41**, non corrigé au passage pour ne pas mêler une
dérive non arbitrée au diff d'une phase ; déclencheur de reprise : « prochain passage sur
`docs/ADR.md` ».

Sous l'item « Posture de protection de `main` — TRANCHÉ : phase dédiée à inscrire (2026-09-15) »,
AJOUTER une ligne de statut équivalente : la phase a été ouverte et cadrée, sa prémisse s'est
renversée, et le cahier des charges est désormais scindé — la partie in-repo est traitée par
ADR-072, la partie côté serveur reste au premier item. Ne PAS marquer l'item comme achevé : la forme
attendue qu'il décrit (« un ruleset exigeant la CI verte avant merge ») n'existe pas.

**`25-SECURITY.md` — O-3 portée à « signalée et tracée ».**

Réécrire le CORPS de l'entrée O-3 de la section `## Observations`, et rien d'autre :
- garder ce qui reste vrai, en le disant : `main` n'a aucune protection côté serveur, et le gate du
  budget d'instructions rend 0 sur une baseline relevée à la main — la valeur elle-même n'est pas
  défendue par le gate ;
- ajouter ce qui a changé : depuis la Phase 41 (périmètre sans admin, option (a), arbitrage Samuel,
  AskUserQuestion session principale, 2026-09-17), une hausse de valeur sans citation d'arbitrage
  portant canal et date est SIGNALÉE par `scripts/check-baseline-arbitrage.sh`, et la modification
  d'un gate, de sa suite ou de l'étape CI qui l'invoque est SIGNALÉE par
  `scripts/check-gate-touche.sh`, qui exige un marqueur déclaratif ;
- écrire le statut en toutes lettres : O-3 est **signalée et tracée**. Écrire aussi pourquoi elle ne
  peut pas être davantage : les deux gardes vivent dans le dépôt, donc la PR qu'elles jugent peut les
  modifier ; renvoi à ADR-072 ;
- conserver le renvoi existant vers la phase dédiée et la date d'arbitrage d'origine.

**Ne pas toucher la ligne de la table STRIDE `T-25-16`** : la colonne d'état de cette ligne porte sur
la MENACE, pas sur l'observation O-3 qu'elle mentionne à côté — c'est précisément la raison inscrite
dans l'allowlist du recensement (plan 41-15). La modifier romprait cette entrée.

Rejouer ensuite, dans l'ordre : le recensement `tools/check-aucune-fermeture.sh`, le contrôle de
trace `tools/check-trace-arbitrage.sh`, puis les deux jobs CI avec la découverte complète. Consigner
au SUMMARY le texte exact du nouveau statut d'O-3 et les lignes `REJEU`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=.planning/BACKLOG.md; n=$(awk "/^\\*\\*Statut partiel \\(.*Phase 41\\) :\\*\\*/{c++} END{print c+0}" "$B"); i=$(awk "/^## Protection de .main. côté GitHub — DIFFÉRÉ/{a=1} /^## Posture de protection de .main. — TRANCHÉ/{b=1} END{print a+b}" "$B"); a=$(awk "/ADR-072/{c++} END{print c+0}" "$B"); c=$(awk "/^## Ligne d.index absente pour ADR-071/{c++} END{print c+0}" "$B"); d=$(awk "/^## Ligne d.index absente pour ADR-071/{f=1} f && /docs\\/ADR.md/{g=1} f && /^## /&&!/ADR-071/{f=0} END{print g+0}" "$B"); echo "statuts_phase41=$n items_intacts=$i renvois_ADR072=$a constat_index_ADR071=$c declencheur=$d"; [ "$n" -eq 2 ] && [ "$i" -eq 2 ] && [ "$a" -ge 1 ] && [ "$c" -eq 1 ] && [ "$d" -ge 1 ]'</automated>
    <fails_when>`statuts_phase41=` différent de 2 (un des deux renvois manque, ou il y en a trop) ; ou `items_intacts=` différent de 2 (un titre d'item a été modifié ou supprimé) ; ou `renvois_ADR072=` à 0 ; ou `constat_index_ADR071=` différent de 1 (le constat hors périmètre n'est pas consigné) ; ou `declencheur=` à 0 (le constat ne porte pas son déclencheur de reprise)</fails_when>
    <automated>bash --noprofile --norc -e -c 'S=.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md; T=$(mktemp -d); awk "/^- \\*\\*O-3 /{f=1} f && /^- \\*\\*O-4 /{f=0} f{print}" "$S" > "$T/o"; g=$(awk "/check-baseline-arbitrage/{a=1} /check-gate-touche/{b=1} END{print a+b}" "$T/o"); s=$(awk "/signalée et tracée/{c++} END{print c+0}" "$T/o"); t=$(awk "/^\\| T-25-16 \\|/ && /closed/{c++} END{print c+0}" "$S"); o4=$(awk "/^- \\*\\*O-4 /{c++} END{print c+0}" "$S"); echo "O3 gardes=$g statut_signalee_tracee=$s ligne_T-25-16_intacte=$t O-4_intacte=$o4"; [ "$g" -eq 2 ] && [ "$s" -ge 1 ] && [ "$t" -eq 1 ] && [ "$o4" -eq 1 ]'</automated>
    <fails_when>`gardes=` différent de 2 (O-3 ne nomme pas les deux gardes qui la signalent) ; ou `statut_signalee_tracee=` à 0 ; ou `ligne_T-25-16_intacte=` différent de 1 (la ligne STRIDE a été modifiée, ce qui rompt l'entrée d'allowlist du recensement) ; ou `O-4_intacte=` différent de 1 (débordement de l'édition sur l'observation suivante)</fails_when>
    <automated>bash --noprofile --norc -e -c 'r=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-aucune-fermeture.sh || r=$?; s=0; bash .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/tools/check-trace-arbitrage.sh >/dev/null 2>&1 || s=$?; echo "recensement=$r trace=$s"; [ "$r" -eq 0 ] && [ "$s" -eq 0 ]'</automated>
    <fails_when>`recensement=` différent de 0 (un texte de doctrine associe une garde ou O-3 à un mot d'achèvement) ; ou `trace=` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); a=0; b=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || a=$?; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || b=$?; awk "/^BILAN REJEU/" "$T/g" "$T/t"; n=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$a rejouees=${n:-0} tests rc=$b"; [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "${n:-0}" -ge 13 ]'</automated>
    <fails_when>l'un des deux rc est non nul ; ou `rejouees=` inférieur à 13</fails_when>
  </verify>
  <acceptance_criteria>
    - deux lignes de statut Phase 41 ajoutées au BACKLOG, aucun titre d'item modifié, aucune ligne existante réécrite, renvoi à ADR-072 présent
    - une entrée courte consigne l'absence de ligne d'index d'ADR-071, hors périmètre, avec son déclencheur « prochain passage sur `docs/ADR.md` » — et `docs/ADR.md` ne porte AUCUNE ligne d'index ADR-071 ajoutée par cette phase
    - O-3 nomme les deux gardes qui la signalent, porte le statut « signalée et tracée » et dit pourquoi elle ne peut pas être davantage
    - la ligne STRIDE `T-25-16` et l'observation O-4 sont intactes
    - recensement et contrôle de trace à rc 0 ; rejeu `gates` (≥ 13 étapes) et `tests` verts
  </acceptance_criteria>
  <done>Les deux items du backlog sont traités par renvoi sans perdre leur histoire, et O-3 change de statut sans qu'aucun texte n'en déclare l'achèvement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine écrite → gardes livrées | une ADR peut décrire une couverture que le script ne fournit pas |
| texte de statut → état réel d'une observation | « signalée et tracée » et un mot d'achèvement ne coûtent pas le même prix au lecteur d'après |
| édition d'un item de backlog → trace historique | réécrire un item daté efface la raison pour laquelle il existe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-78 | Repudiation | ADR-072 décrit une couverture plus large que les scripts livrés | high | mitigate | les trois SUMMARY sont en `read_first` et l'ADR recopie les verdicts réels ; verify machine exigeant les trois chemins de garde et les cinq renvois obligatoires ; un écart se remonte au manager, jamais s'arrondit |
| T-41-79 | Repudiation | O-3 basculée en achèvement par une formule commode | high | mitigate | statut « signalée et tracée » exigé par verify ; recensement `tools/check-aucune-fermeture.sh` rejoué après chaque tâche, négations comprises |
| T-41-80 | Tampering | ligne STRIDE `T-25-16` modifiée, ce qui invalide l'entrée d'allowlist du recensement | medium | mitigate | interdiction explicite dans l'action ; verify qui asserte la ligne intacte et l'observation O-4 intacte (détection d'un débordement d'édition) |
| T-41-81 | Denial of service | jeton de chemin non résolvable ajouté au `CLAUDE.md`, rougissant `check-map-drift.sh` | medium | mitigate | contrainte écrite dans l'action (aucun glob entre accents graves, trois chemins concrets) ; `check-map-drift.sh --path .` en verify, avec la conduite de correction nommée |
| T-41-82 | Repudiation | item de backlog déclaré traité alors que la forme attendue n'existe pas | high | mitigate | renvoi par AJOUT d'une ligne de statut, aucune ligne existante modifiée, aucun item marqué achevé ; verify qui asserte les deux titres d'items intacts |
</threat_model>

<verification>
Index et section ADR-072 uniques, trois gardes nommées, cinq renvois obligatoires, au moins 40
lignes ; section `CLAUDE.md` unique avec trailer et limite de fond, sections existantes intactes,
`check-map-drift.sh --path .` en 0 ou 3 ; deux statuts Phase 41 au BACKLOG sans réécriture ; O-3
« signalée et tracée » nommant les deux gardes, `T-25-16` et O-4 intactes ; recensement et contrôle
de trace à rc 0 ; rejeu `gates` (≥ 13 étapes) et `tests` verts.
</verification>

<success_criteria>
Le lecteur d'après sait, sans lire un script, ce que ce dépôt garde, ce qu'il ne garde pas, et ce
qui attend un accès admin — et aucun de ces textes ne déclare l'achèvement d'une garde ni d'O-3.
</success_criteria>

## Artifacts this phase produces

- `docs/ADR.md` — ADR-072 « Gardes in-repo sans règle côté serveur » + ligne d'index.
- `CLAUDE.md` — section « Gardes in-repo — ce qui est gardé, ce qui ne l'est pas », convention du
  trailer `Gate-Touche:`.
- `.planning/BACKLOG.md` — deux lignes `**Statut partiel (…, Phase 41) :**`.
- `25-SECURITY.md` — O-3 « signalée et tracée ».

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-18-SUMMARY.md` when done
</output>

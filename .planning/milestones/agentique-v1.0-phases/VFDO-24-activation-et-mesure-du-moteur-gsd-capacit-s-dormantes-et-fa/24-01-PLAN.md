---
phase: 24-activation-et-mesure-du-moteur-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/business-pilot-bundle/agents/quality-gate-client.md
  - plugin/business-pilot-bundle/agents/vf-business-commercial.md
  - plugin/business-pilot-bundle/agents/vf-business-delivery.md
  - plugin/business-pilot-bundle/agents/vf-business-finance.md
  - plugin/business-pilot-bundle/agents/vf-business-manager.md
  - plugin/content-bundle/agents/content-clarity-judge.md
  - plugin/content-bundle/agents/vf-content-manager.md
  - plugin/content-bundle/agents/vf-content-repurposer.md
  - plugin/content-bundle/agents/vf-content-strategist.md
  - plugin/content-bundle/agents/vf-content-writer.md
  - plugin/design-orchestrator/agents/vf-crafter.md
  - plugin/design-orchestrator/agents/vf-design-judge.md
  - plugin/design-orchestrator/agents/vf-design-manager.md
  - plugin/dev-orchestrator/agents/vf-auditer.md
  - plugin/dev-orchestrator/agents/vf-coder.md
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/dev-orchestrator/agents/vf-reviewer.md
  - plugin/growth-bundle/agents/campaign-analyst.md
  - plugin/growth-bundle/agents/channel-strategist.md
  - plugin/growth-bundle/agents/copywriter-sequences.md
  - plugin/growth-bundle/agents/growth-quality-judge.md
  - plugin/growth-bundle/agents/vf-growth-manager.md
  - plugin/mobile-test-team/agents/vf-app-fixer.md
  - plugin/mobile-test-team/agents/vf-test-orchestrator.md
  - plugin/mobile-test-team/agents/vf-test-runner.md
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
  - plugin/conductor/references/team-kernel.md
autonomous: true
requirements: [GSDA-20, GSDA-21, GSDA-22]

must_haves:
  truths:
    - "Les 25 agents livrés déclarent chacun un `effort:` choisi PAR RÔLE — 12 `high`, 12 `medium`, 1 `low` — jamais une valeur uniforme."
    - "Un agent posé sans `effort:` fait ÉCHOUER `check-agents.sh` (erreur, pas warning), et cette discriminance est prouvée par mutation."
    - "Un agent tiers écarté par `--third-party-prefix` (défaut `gsd-`) et dépourvu d'`effort:` ne fait PAS échouer le gate — le durcissement ne déborde pas sur les labs des utilisateurs."
    - "La marge de profondeur de dispatch est écrite : `maxDepth: 5`, 3 consommés, 2 de marge, et ce que la marge autorise."
    - "Aucun agent ne franchit le plafond ADR-029 de 250 lignes après l'ajout du champ."
  artifacts:
    - plugin/conductor/scripts/check-agents.sh
    - plugin/conductor/scripts/tests/test-check-agents.sh
    - plugin/conductor/references/team-kernel.md
  key_links:
    - "`check-agents.sh` est appelé par le hook `SessionStart` de conductor ET par les 2 étapes `gates` de la CI (`--strict`, puis monde fermé `--resolve-agents=strict`) — le durcissement doit rester vert sur les 6 dossiers `plugin/*/agents`."
    - "`plugin/dev-orchestrator/agents/vf-dev-manager.md` est à 245/250 lignes : l'ajout d'`effort:` le porte à 246 et laisse 4 lignes au plan 24-08, qui l'édite ensuite."
---

<objective>
Graver dans les agents les deux faits de runtime que le moteur expose, que notre gate valide déjà, et
qu'aucun fichier du module n'énonce (verdict zone 6, option A).

Purpose: `effort:` est supporté par le harness (`agentFrontmatterExtensions: ["effort"]`), validé par
notre propre gate, employé par les skills GSD — et déclaré par **0 des 25 agents livrés**. Le barème
par rôle n'est pas à inventer : il existe déjà dans 3 de nos agents-templates. La marge de profondeur
de dispatch (`maxDepth: 5`, 3 consommés) clôt la question du nesting ouverte à l'audit ; ne pas
l'écrire garantit qu'elle se repose.

Output: 25 frontmatters portant un `effort:` par rôle, un gate `check-agents.sh` qui **exige** le
champ au lieu de le valider s'il est présent, et une section de doctrine datée dans `team-kernel.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-ARBITRAGES.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : propager `effort:` par rôle sur les 25 agents livrés</name>
  <files>
    plugin/business-pilot-bundle/agents/{quality-gate-client,vf-business-commercial,vf-business-delivery,vf-business-finance,vf-business-manager}.md,
    plugin/content-bundle/agents/{content-clarity-judge,vf-content-manager,vf-content-repurposer,vf-content-strategist,vf-content-writer}.md,
    plugin/design-orchestrator/agents/{vf-crafter,vf-design-judge,vf-design-manager}.md,
    plugin/dev-orchestrator/agents/{vf-auditer,vf-coder,vf-dev-manager,vf-reviewer}.md,
    plugin/growth-bundle/agents/{campaign-analyst,channel-strategist,copywriter-sequences,growth-quality-judge,vf-growth-manager}.md,
    plugin/mobile-test-team/agents/{vf-app-fixer,vf-test-orchestrator,vf-test-runner}.md
  </files>
  <read_first>
    - `plugin/reference/content/methodology/templates/agents/business-agent-template.md` (lignes 1-9) — le seul frontmatter du dépôt qui porte `effort: medium`, et la POSITION du champ : entre `model:` et `memory:`/`skills:`.
    - `plugin/reference/content/methodology/templates/agents/orchestrator-template.md` et `clarity-feature-template.md` — les deux qui portent `effort: high` (pilotage, jugement).
    - `plugin/conductor/scripts/check-agents.sh` ligne 163 — `EFFORT = {"low", "medium", "high", "xhigh", "max"}`, l'ensemble des valeurs admises.
    - `24-ARBITRAGES.md` § Zone 6, verdict — « pilotage et jugement haut, exécution mécanique bas, **jamais uniformément** ; le barème est **repris** des 3 templates, pas inventé ».
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` — le fichier le plus contraint (245 lignes, plafond ADR-029 à 250).
  </read_first>
  <action>
Insérer une ligne `effort: <valeur>` dans le frontmatter YAML de chacun des 25 agents, **entre la
ligne `model:` et la ligne `memory:`** (position observée dans `business-agent-template.md`). Ne
toucher à aucun autre champ, à aucune ligne de corps.

Répartition PAR RÔLE, arrêtée ici et non négociable (verdict zone 6 — jamais uniformément) :

`effort: high` — pilotage (6 orchestrateurs) et jugement (6 juges), 12 fichiers :
`vf-business-manager`, `vf-content-manager`, `vf-design-manager`, `vf-dev-manager`,
`vf-growth-manager`, `vf-test-orchestrator`, `quality-gate-client`, `content-clarity-judge`,
`vf-design-judge`, `growth-quality-judge`, `vf-auditer`, `vf-reviewer`.

`effort: medium` — production spécialisée qui exige un jugement de domaine, 12 fichiers :
`vf-business-commercial`, `vf-business-delivery`, `vf-business-finance`, `vf-content-repurposer`,
`vf-content-strategist`, `vf-content-writer`, `vf-crafter`, `vf-coder`, `campaign-analyst`,
`channel-strategist`, `copywriter-sequences`, `vf-app-fixer`.

`effort: low` — exécution mécanique, 1 fichier : `vf-test-runner` (rejoue une recette Maestro déjà
écrite, ne décide de rien).

Total exact : 12 + 12 + 1 = 25. Aucun fichier laissé de côté, aucune valeur `xhigh` ni `max` posée —
le barème repris des templates ne comporte que `medium` et `high`, `low` est son prolongement pour
le seul rôle mécanique du parc.

Contrainte de densité ADR-029 : `vf-dev-manager.md` passe de 245 à 246 lignes. Vérifier le compte
APRÈS édition avec `awk 'END{print NR}'` — jamais `wc -l` (proxifié, rend 0 sur ce runtime). Les
4 lignes restantes sous le plafond sont réservées au plan 24-08 : ne pas les consommer ici.
  </action>
  <verify>
    <automated>test "$(awk 'FNR>1 && /^---[[:space:]]*$/{f=0} FNR==1{f=1} f && /^effort:[[:space:]]*(low|medium|high)[[:space:]]*$/{c++} END{print c+0}' plugin/*/agents/*.md)" = "25" &amp;&amp; test "$(awk 'END{print NR}' plugin/dev-orchestrator/agents/vf-dev-manager.md)" -le 250 &amp;&amp; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `awk` sur `plugin/*/agents/*.md` compte exactement **25** lignes `^effort:` dans les frontmatters, réparties **12 `high` / 12 `medium` / 1 `low`** — trois valeurs distinctes présentes, aucune répartition uniforme.
    - `plugin/mobile-test-team/agents/vf-test-runner.md` porte `effort: low` et est le **seul** fichier du parc à le porter.
    - Dans chacun des 25 fichiers, la ligne `effort:` est immédiatement précédée d'une ligne `model:` (position du template).
    - `awk 'END{print NR}' plugin/dev-orchestrator/agents/vf-dev-manager.md` rend une valeur **≤ 250** (ADR-029) et **≤ 246** (marge réservée à 24-08).
    - `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/<mod>/agents` sort **0** pour les 6 dossiers.
    - Aucune ligne de corps modifiée : pour chaque fichier, le nombre de lignes après édition vaut exactement le nombre avant + 1.
  </acceptance_criteria>
  <done>Les 25 agents livrés déclarent un `effort:` par rôle, `check-agents.sh --strict` reste vert sur les 6 dossiers, et `vf-dev-manager.md` tient sous 250 lignes avec sa marge intacte.</done>
  <reversibility rating="reversible">Un champ de frontmatter additif ; le retrait est un `sed` inverse et le gate n'est durci qu'à la tâche suivante.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : durcir `check-agents.sh` de « valide si présent » à « exige »</name>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - `plugin/conductor/scripts/check-agents.sh` lignes 502-516 — le bloc `model` (`:502-506`) et le bloc `memory` (`:508-512`) sont DÉJÀ en mode « exige » ; le bloc `effort` (`:514-516`) est le seul en mode « valide si présent ». Le patron de transposition est littéralement le bloc `model` juste au-dessus.
    - `plugin/conductor/scripts/check-agents.sh` ligne 84 — `--third-party-prefix`, défaut `gsd-` : le mécanisme qui écarte les agents tiers des findings. Le durcissement ne doit pas le franchir.
    - `plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` lignes 1210-1225 — la fonction `mutant()` : la mutation doit avoir CHANGÉ le fichier, vérifié par `cmp`, **jamais** par `diff` (proxifié et menteur sur ce runtime).
    - `.github/workflows/ci.yml` lignes 242-287 — les deux étapes qui appellent `check-agents.sh` (lenient par module puis monde fermé) : les deux doivent rester vertes.
  </read_first>
  <action>
Remplacer le bloc `effort` de `check-agents.sh` (lignes 514-516) par la transposition littérale du
bloc `model` : un premier test d'absence qui pousse une erreur, puis un `elif` de validation de
valeur. Message d'absence à écrire sur le gabarit du message `model` — nommer le champ, dire
pourquoi il est requis (barème par rôle, ADR-044 étendu par le verdict zone 6 de la Phase 24), et
énumérer les valeurs admises `low|medium|high|xhigh|max`. Le message d'invalidité existant est
conservé mot pour mot.

Le durcissement s'applique **exactement au même périmètre d'agents** que les blocs `model` et
`memory` : les agents écartés par `--third-party-prefix` (défaut `gsd-`) ne doivent produire ni
erreur ni warning sur `effort`. Ne toucher à aucune ligne du filtrage tiers — le vérifier par test,
pas par lecture.

Étendre `test-check-agents.sh` avec trois cas neufs, dans la forme des cas existants du fichier :

1. Cas nominal négatif : une fixture d'agent local complète (name, description, model, memory) mais
   **sans** `effort:` → le gate sort en erreur et son message nomme `effort`.
2. Cas de non-débordement tiers : une fixture d'agent nommée avec le préfixe tiers par défaut,
   également sans `effort:` → le gate ne rapporte **aucune** erreur ni warning citant `effort`.
3. Cas de discriminance par mutation, au patron `mutant()` : partir de l'arbre réel, retirer la
   ligne `effort:` d'un agent livré dans une copie temporaire, vérifier par `cmp` que la mutation a
   bien changé le fichier (jamais par `diff`), prouver que le gate **rougit**, puis rejouer la
   réécriture licite (ligne restaurée) et prouver qu'il **redevient vert**. Un cas qui ne teste que
   l'état nominal n'est pas recevable.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh &amp;&amp; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Dans `check-agents.sh`, le bloc `effort` contient une branche d'absence (`if not effort:`) suivie d'une branche `elif effort not in EFFORT:` — la même forme à deux branches que le bloc `model` des lignes 502-506.
    - Le message d'absence contient la sous-chaîne `effort` et l'énumération `low|medium|high|xhigh|max`.
    - `test-check-agents.sh` porte les 3 cas neufs et sort `0` ; son compteur de cas a augmenté de 3 au minimum.
    - **Discriminance prouvée** : le cas de mutation rapporte explicitement que le gate est rouge après retrait de la ligne `effort:` et vert après restauration, la mutation étant confirmée effective par `cmp` (jamais par `diff`).
    - Une fixture d'agent préfixée par la valeur par défaut de `--third-party-prefix`, sans `effort:`, produit **0 erreur** et **0 warning** citant `effort`.
    - `bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/<mod>/agents` sort **0** pour les 6 dossiers, et le run en monde fermé (`--resolve-agents=strict` avec les 6 `--agent-registry-dir`) sort **0** lui aussi.
  </acceptance_criteria>
  <done>Un agent local posé sans `effort:` fait échouer le gate ; un agent tiers sans `effort:` ne le fait pas ; les deux comportements sont prouvés par test, dont un par mutation.</done>
  <reversibility rating="costly">`check-agents.sh` est appelé au `SessionStart` de tout lab équipé et par deux étapes de la CI : un durcissement trop large transformerait chaque ouverture de session en flot d'erreurs. Le cas de non-débordement tiers est la garde qui borne ce coût.</reversibility>
</task>

<task type="auto">
  <name>Tâche 3 : écrire la marge de profondeur de dispatch dans `team-kernel.md`</name>
  <files>plugin/conductor/references/team-kernel.md, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - `plugin/conductor/references/team-kernel.md` en entier — le foyer transverse du noyau d'équipe ; il porte déjà l'hypothèse datée sur le dispatch nommé et le recoupement amont `#1995`/`#2608`. C'est là que va un fait de runtime commun à toutes les équipes (dev, design, contenu, growth, business, mobile-test), pas dans un module métier (ADR-057 : une capacité, une seule voix).
    - `24-CONTEXT.md` fiche **F-05** — le descripteur exact et sa version.
    - `24-ARBITRAGES.md` § Zone 6, verdict — ce que la marge **autorise** doit être dit, pas seulement sa valeur.
  </read_first>
  <action>
Ajouter à `team-kernel.md` une section datée portant les trois faits, dans cet ordre :

1. Le descripteur `claude.runtime.hostIntegration.dispatch` mesuré contre `@opengsd/gsd-core` 1.9.1
   le 2026-08-04, recopié verbatim : `namedDispatch: true`, `nested: true`, `maxDepth: 5`,
   `background: true`, `backgroundDispatch: false`, `subagentToolkit: "full"`,
   `isolation: "harness-worktree"`. Inchangé depuis la 1.9.0.
2. La consommation réelle : la chaîne `vf-dev-manager` → `vf-coder` → agent `gsd-*` consomme
   **3 niveaux sur 5**. Il reste **deux niveaux de marge**.
3. Ce que la marge **autorise**, écrit comme une permission et non comme une observation : un worker
   peut légitimement dispatcher un sous-worker sans franchir la limite du runtime. Ce fait **clôt**
   la question du nesting ouverte à l'ouverture de l'audit.

Assortir la section de sa borne : la marge est une permission de profondeur, jamais une permission
de contourner la voie unique d'invocation (GSDC-05, Phase 23) ni le cloisonnement des allowlists.
Dater la section et nommer sa source (descripteur du runtime `claude`, 1.9.1), à la manière de
l'hypothèse datée déjà présente dans le fichier.

Ajouter à `test-check-agents.sh` — la seule suite du dépôt qui connaisse déjà `team-kernel.md` — un
cas d'assertion de présence : le fichier doit contenir les littéraux `maxDepth` et le compte de
niveaux consommés, et la phrase qui dit ce que la marge autorise. Le cas échoue si l'un des trois
manque.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh &amp;&amp; awk '/maxDepth/{a=1} /deux niveaux de marge/{b=1} /sous-worker/{c=1} END{exit !(a&&b&&c)}' plugin/conductor/references/team-kernel.md</automated>
  </verify>
  <acceptance_criteria>
    - `plugin/conductor/references/team-kernel.md` contient les littéraux `maxDepth`, `deux niveaux de marge` et `sous-worker`, ainsi que les sept champs du descripteur recopiés verbatim.
    - La section porte une date (`2026-08-04`) et nomme sa version source (`1.9.1`).
    - `test-check-agents.sh` porte un cas neuf qui échoue si l'un des trois littéraux disparaît — prouvé en retirant temporairement l'un d'eux d'une copie du fichier et en constatant le rouge, puis en le restaurant et en constatant le vert.
    - Aucune ligne de `team-kernel.md` existante n'est supprimée : le compte de lignes après édition est strictement supérieur au compte avant.
  </acceptance_criteria>
  <done>La limite, la marge et ce que la marge autorise sont écrites, datées, sourcées, et une assertion machine empêche leur disparition silencieuse.</done>
  <reversibility rating="reversible">Ajout documentaire additif, sans effet sur un chemin d'exécution.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent → `check-agents.sh` | Le gate parse du YAML issu du dépôt et de `~/.claude/agents` (contenu tiers non maîtrisé). |
| `check-agents.sh` → hook `SessionStart` | La sortie du gate traverse vers le contexte de session de tout lab équipé. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01-01 | Denial of Service | `check-agents.sh` au `SessionStart` | high | mitigate | Le durcissement reste borné par `--third-party-prefix` ; un cas de test dédié prouve qu'un agent tiers sans `effort:` ne produit ni erreur ni warning — sinon chaque ouverture de session d'un lab équipé cracherait un flot d'erreurs. |
| T-24-01-02 | Tampering | `test-check-agents.sh` (cas de mutation) | medium | mitigate | La mutation est confirmée effective par `cmp` avant tout verdict ; un motif introuvable rend le mutant NON OPPOSABLE, jamais « satisfait ». |
| T-24-01-03 | Information Disclosure | `team-kernel.md` | low | accept | La section n'ajoute qu'un descripteur public du runtime, déjà lisible dans le paquet amont installé. |
| T-24-01-SC | Tampering | installs npm/pip/cargo | high | accept | Ce plan n'exécute **aucune** installation de paquet — la chaîne d'approvisionnement n'est pas franchie ici. |
</threat_model>

<verification>
- Les 6 dossiers `plugin/*/agents` passent `check-agents.sh --strict` (mode lenient par module) **et** le run en monde fermé.
- `test-check-agents.sh` est vert et son compteur de cas a augmenté d'au moins 4 (3 pour `effort`, 1 pour `team-kernel.md`).
- `awk 'END{print NR}'` sur chacun des 25 agents rend une valeur ≤ 250.
</verification>

<success_criteria>
25/25 agents portent un `effort:` choisi par rôle avec trois valeurs distinctes ; le gate exige le
champ pour les agents locaux et l'ignore pour les tiers, les deux prouvés par test ; la marge de
profondeur de dispatch est écrite, datée et gardée par une assertion.
</success_criteria>

## Artifacts this phase produces

| Symbole | Nature | Emplacement |
|---|---|---|
| `effort: high` \| `medium` \| `low` | champ de frontmatter d'agent | les 25 `plugin/*/agents/*.md` |
| branche d'absence du bloc `effort` | logique de gate | `plugin/conductor/scripts/check-agents.sh` |
| 3 cas `effort` + 1 cas `team-kernel` | cas de test | `plugin/conductor/scripts/tests/test-check-agents.sh` |
| section « marge de profondeur de dispatch » | doctrine datée | `plugin/conductor/references/team-kernel.md` |

<output>
Créer `.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-01-SUMMARY.md` en fin de plan.
</output>

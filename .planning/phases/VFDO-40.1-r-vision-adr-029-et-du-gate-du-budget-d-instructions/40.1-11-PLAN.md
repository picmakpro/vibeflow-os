---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 11
lot: L8 — clôture : recensement nul, contrôles négatifs, rejeu CI complet, ledger
type: execute
wave: 4
depends_on: ["40.1-02", "40.1-04", "40.1-05", "40.1-06", "40.1-07", "40.1-08", "40.1-09", "40.1-10"]
autonomous: true
requirements: [BUDG-04, BUDG-05, QUAL-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BUDG-04 (critère 1) — la commande versionnée `tools/check-no-live-250.sh .` rend 0 sans aucune ligne sur le dépôt final, et ses deux contrôles négatifs (fixtures, clone réel) rougissent sur un ancien plafond réintroduit"
    - "Non-régression sur la découverte COMPLÈTE : `tools/replay-ci-jobs.sh --job tests` (étape de découverte de toutes les suites) et `--job gates` (toutes les étapes non conditionnelles) rendent 0, extraits de `ci.yml`, en `bash --noprofile --norc -e`"
    - "D-H6 / doctrine gravée — aucune baseline ne monte : lignes de données de `.planning/instruction-budget-baselines.tsv` identiques octet pour octet à la base de phase `4e7c8e4` ; pour les 31 fichiers, LIGNES et INSTR mesurés par le gate égaux à la baseline ; aucun `DEPASSEMENT` ni `LIGNES-EN-HAUSSE` sur le dépôt"
    - "Aucun agent réécrit au-delà du remplacement de valeurs : chaque ligne modifiée d'un fichier d'agent depuis `4e7c8e4` redevient la ligne d'origine quand 300 est remplacé par l'ancienne valeur, et chaque fichier a autant de lignes ajoutées que retirées"
    - "Versions : dix modules bumpés (conductor v1.38.0, dev-orchestrator v2.22.1, design-orchestrator v1.5.7, business-pilot-bundle v2.0.9, content-bundle v2.0.9, growth-bundle v2.0.9, reference v2.5.5, validator v1.3.5, kpi-analyst v1.0.6, audit-architecture v1.0.3), `check-version-sync.sh` vert, sources de version racine inchangées (aucune release)"
    - "Ledger : BUDG-04 et BUDG-05 cochés avec date et preuve, QUAL-01 noté satisfait sur la Phase 40.1 ; `40.1-VALIDATION.md` porte les statuts réels ; STATE.md n'est PAS modifié par ce plan (geste à la main de l'orchestrateur : sauvegarde puis `git diff --stat`, jamais `state record-session` ni `state.planned-phase`)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BUDG-04, BUDG-05 clos ; QUAL-01 mis à jour"
  key_links:
    - "Le rejeu du job `tests` exécute AUSSI `test-check-instruction-budget.sh` et les suites de modules relevées : c'est la preuve d'intégration des vagues 2-3"
---

<objective>
Clore la Phase 40.1 par la preuve, jamais par déclaration : recensement nul et contrôles négatifs,
rejeu complet des jobs CI `tests` et `gates` depuis `ci.yml`, invariants de baseline et de
non-réécriture d'agents, cohérence des versions, puis ledger.

Purpose: satisfaire BUDG-04, BUDG-05 et QUAL-01 sur l'état final intégré, pas plan par plan.
Output: ledger à jour, VALIDATION à jour, preuves consignées au SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : recensement nul et contrôles négatifs sur l'état final</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh, tools/test-check-no-live-250.sh, tools/controle-negatif-reel.sh
    - les SUMMARY 40.1-01 à 40.1-10 (écarts déclarés)
  </read_first>
  <action>
Précondition : arbre de travail propre sur les chemins de la phase (`git status --porcelain` ne liste
que les deux chemins non suivis qui ne sont pas à la phase). Jouer le recensement final, la suite de
contrôle négatif et le contrôle négatif sur clone réel ; si le recensement liste une ligne, NE PAS
l'exclure pour passer : la classer (vivante → renvoyer au plan de sa famille ; histoire → règle
nominative ajoutée à l'outil avec sa raison écrite dans le SUMMARY, arbitrage du manager requis).
Reporter dans `40.1-VALIDATION.md` le statut ✅ des lignes « recensement » et « Wave 0 », les rc et
nombres de lignes observés.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .</automated>
    <fails_when>exit non nul, ou au moins une ligne `chemin:ligne:contenu` imprimée</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-no-live-250.sh</automated>
    <fails_when>exit non nul, ou `M ko` avec M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/controle-negatif-reel.sh</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :` (R0 non propre, ou R1/R2/R4 non détectés, ou R3 détecté)</fails_when>
  </verify>
  <acceptance_criteria>
    - recensement final : rc 0, aucune ligne
    - contrôle négatif fixtures : 0 ko ; contrôle négatif réel : R0 rc 0, R1 rc 1, R2 rc 1, R3 rc 0, R4 rc 1
  </acceptance_criteria>
  <done>BUDG-04 critère 1 prouvé sur l'état final, avec un recensement qui sait rougir.</done>
</task>

<task type="auto">
  <name>Task 2 : rejeu CI complet et invariants (baselines, agents, versions)</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh
    - .github/workflows/ci.yml (jobs `tests` et `gates`)
    - .planning/instruction-budget-baselines.tsv
  </read_first>
  <action>
Jouer le rejeu du job `gates` puis du job `tests` (plusieurs minutes), puis les trois contrôles
d'invariants ci-dessous. Consigner dans le SUMMARY : chaque ligne `REJEU`, les étapes sautées et
leur raison, le nombre de suites découvertes. Reporter les statuts ✅ correspondants dans
`40.1-VALIDATION.md`. Un rouge n'est jamais contourné : il est diagnostiqué et renvoyé au plan
fautif.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates</automated>
    <fails_when>exit non nul, ou une ligne `REJEU gates | … | rc=` différente de `rc=0`</fails_when>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests</automated>
    <fails_when>exit non nul, ou la ligne `REJEU tests | … | rc=` différente de `rc=0`, ou `== bilan : N suite(s), M échec(s) ==` avec M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); git show 4e7c8e4:.planning/instruction-budget-baselines.tsv > "$T/a0"; awk "NF && !/^#/" "$T/a0" > "$T/a"; awk "NF && !/^#/" .planning/instruction-budget-baselines.tsv > "$T/b"; cmp -s "$T/a" "$T/b" && echo baseline_donnees_identiques; bash plugin/conductor/scripts/check-instruction-budget.sh > "$T/g" 2>&1; awk -F" [|] " "/^plugin\\//{n++; if (\$2!=\$3 || \$4!=\$5 || \$6 ~ /DEPASSEMENT|LIGNES-EN-HAUSSE/) {bad++; print \"ECART \" \$0}} END{print \"fichiers=\" n \" ecarts=\" bad+0; exit (n==31 && bad==0)?0:1}" "$T/g"'</automated>
    <fails_when>exit non nul ; ou absence de `baseline_donnees_identiques` ; ou `fichiers=` différent de 31 ; ou `ecarts=` différent de 0 (une ligne `ECART` imprimée)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); git diff -U0 4e7c8e4 -- "plugin/*/agents/*.md" "plugin/*/AGENT.md" > "$T/d"; awk "/^diff --git/{f=\$3; nr=0; na=0; next} /^(--- |\\+\\+\\+ )/{next} /^@@/{next} /^-/{r[f,nr++]=substr(\$0,2); rm[f]=nr; next} /^\\+/{a=substr(\$0,2); gsub(/300/,\"250\",a); if (a!=r[f,na]) {bad++; print \"REECRITURE \" f \" : \" substr(\$0,2)}; na++; ad[f]=na; next} END{for (k in ad) if (ad[k]!=rm[k]) {bad++; print \"DESEQUILIBRE \" k}; for (k in rm) if (ad[k]!=rm[k]) {bad++; print \"DESEQUILIBRE \" k}; print \"agents_ecarts=\" bad+0; exit bad?1:0}" "$T/d"'</automated>
    <fails_when>exit non nul, ou une ligne `REECRITURE` ou `DESEQUILIBRE`, ou `agents_ecarts=` différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'bash scripts/check-version-sync.sh > /dev/null; R=$(mktemp); git diff --name-only 4e7c8e4 -- VERSION plugin/.claude-plugin plugin/.codex-plugin .claude-plugin > "$R"; r=$(awk "END{print NR}" "$R"); ok=1; for mv in conductor:v1.38.0 dev-orchestrator:v2.22.1 design-orchestrator:v1.5.7 business-pilot-bundle:v2.0.9 content-bundle:v2.0.9 growth-bundle:v2.0.9 reference:v2.5.5 validator:v1.3.5 kpi-analyst:v1.0.6 audit-architecture:v1.0.3; do m=${mv%%:*}; v=${mv#*:}; got=$(awk "NR==1" "plugin/$m/VERSION"); [ "$got" = "$v" ] || { echo "VERSION $m=$got attendu $v"; ok=0; }; done; echo "racine_modifiee=$r modules_ok=$ok"; [ "$r" -eq 0 ] && [ "$ok" -eq 1 ]'</automated>
    <fails_when>exit non nul (version-sync rouge) ; ou `racine_modifiee=` différent de 0 ; ou une ligne `VERSION <module>=… attendu …` ; ou `modules_ok=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - rejeu `gates` rc 0 (étape `check-release-tag` sautée, conditionnelle main) ; rejeu `tests` rc 0, 0 échec sur toutes les suites découvertes
    - baseline : données identiques à `4e7c8e4` ; 31 fichiers, 0 écart lignes/instructions, aucun `DEPASSEMENT` ni `LIGNES-EN-HAUSSE`
    - agents : 0 réécriture, 0 déséquilibre
    - dix modules aux versions attendues, `check-version-sync.sh` rc 0, aucune source de version racine modifiée
  </acceptance_criteria>
  <done>L'état final intégré est vert sur la découverte complète CI, sans baseline montée, sans agent réécrit, sans release.</done>
</task>

<task type="auto">
  <name>Task 3 : ledger des exigences et validation</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (liste BUDG ~L952-955, table de traçabilité ~L843-853, ligne QUAL-01)
    - SUMMARY 40.1-01 à 40.1-11 (preuves à citer)
  </read_first>
  <action>
`REQUIREMENTS.md` : cocher BUDG-04 et BUDG-05 dans la liste avec la mention « close le <date
d'exécution> (Phase 40.1) » et la preuve (recensement versionné à 0, bascules CI prouvées par
mutants, rejeu tests+gates vert) ; lignes de table BUDG-04/BUDG-05 → `Complete — <date>, Phase 40.1 …` ;
ligne QUAL-01 → ajouter « satisfait sur la Phase 40.1 le <date> (sept mutants du gate tués, dont
MUT-2 remplacé ; contrôle négatif du recensement ; frontière de l'auditeur) ». N'écrire AUCUNE valeur
de l'ancien plafond (le fichier n'est pas exclu en entier du recensement). `40.1-VALIDATION.md` :
statuts réels de chaque ligne, `wave_0_complete: true`, cases de sign-off cochées si vraies ;
`status` reste `draft` (passe à `validated` par `/gsd-validate-phase`). STATE.md non touché.
Recensement rejoué après écriture.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'a=$(awk "/^- \\[x\\] \\*\\*BUDG-04\\*\\*/{c++} /^- \\[x\\] \\*\\*BUDG-05\\*\\*/{c++} END{print c+0}" .planning/REQUIREMENTS.md); b=$(awk "/^\\| BUDG-0[45] \\| Phase 40\\.1 \\| Complete/{c++} END{print c+0}" .planning/REQUIREMENTS.md); w=$(awk "/^wave_0_complete: true/{c++} END{print c+0}" .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-VALIDATION.md); echo "coches=$a table=$b wave0=$w"; [ "$a" -eq 2 ] && [ "$b" -eq 2 ] && [ "$w" -eq 1 ]; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . && echo recensement_final_nul'</automated>
    <fails_when>exit non nul ; ou `coches=` différent de 2, `table=` différent de 2, `wave0=` différent de 1 ; ou absence de `recensement_final_nul`</fails_when>
  </verify>
  <acceptance_criteria>
    - BUDG-04 et BUDG-05 cochés dans la liste et `Complete` dans la table ; QUAL-01 mentionne la Phase 40.1
    - `40.1-VALIDATION.md` : `wave_0_complete: true`, statuts renseignés
    - recensement toujours nul après écriture du ledger ; STATE.md non modifié par ce plan
  </acceptance_criteria>
  <done>Ledger et validation disent l'état prouvé de la phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves de plan → verdict de phase | un vert auto-déclaré par plan ne vaut pas preuve sur l'état intégré |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-24 | Repudiation | clôture sur une liste de gates recopiée | high | mitigate | rejeu extrait de `ci.yml`, sélection vide = rc 2, étapes sautées nommées |
| T-40.1-25 | Elevation of privilege | baseline montée en cours de phase | high | mitigate | données de baseline comparées octet pour octet à `4e7c8e4`, 31 lignes du gate égales |
| T-40.1-26 | Tampering | exclusion ajoutée au recensement pour le faire passer | medium | mitigate | toute règle ajoutée à la clôture exige une raison écrite et l'arbitrage du manager ; contrôle négatif rejoué |
</threat_model>

<verification>
Recensement 0 ; contrôles négatifs verts ; rejeu tests et gates verts ; invariants baseline, agents,
versions verts ; ledger à jour.
</verification>

<success_criteria>
Les quatre critères de succès de la Phase 40.1 (ROADMAP) sont prouvés sur l'état final intégré.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : mises à jour de `.planning/REQUIREMENTS.md` et `40.1-VALIDATION.md`.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-11-SUMMARY.md` when done
</output>

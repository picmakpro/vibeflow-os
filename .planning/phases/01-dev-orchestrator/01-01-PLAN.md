---
phase: 01-dev-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev-orchestrator/VERSION
  - dev-orchestrator/CHANGELOG.md
  - dev-orchestrator/README.md
  - dev-orchestrator/scripts/build-gsd-index.sh
  - dev-orchestrator/references/gsd-skills-index.md
  - dev-orchestrator/references/.gitkeep
autonomous: true
requirements: [IDX-01, IDX-02]
must_haves:
  truths:
    - "Le module dev-orchestrator/ existe avec VERSION, CHANGELOG, README squelette"
    - "build-gsd-index.sh génère un index non vide depuis les skills GSD réellement installés"
    - "L'index ne contient que des noms de skills réellement présents sur disque (zéro nom inventé)"
    - "build-gsd-index.sh est ré-exécutable (régénération idempotente du contenu)"
    - "build-gsd-index.sh accepte VF_INDEX_OUT pour écrire l'index à un chemin arbitraire (utilisé par le hook post-install, D7)"
  artifacts:
    - path: "dev-orchestrator/VERSION"
      provides: "Version semver du module"
      contains: "v1.0.0"
    - path: "dev-orchestrator/scripts/build-gsd-index.sh"
      provides: "Générateur d'index factuel des skills GSD"
      min_lines: 30
    - path: "dev-orchestrator/references/gsd-skills-index.md"
      provides: "Index auto-généré (sera régénéré à l'install)"
  key_links:
    - from: "dev-orchestrator/scripts/build-gsd-index.sh"
      to: "~/.claude/skills/gsd-*/SKILL.md"
      via: "parsing du frontmatter name+description"
      pattern: "gsd-\\*"
    - from: "dev-orchestrator/scripts/build-gsd-index.sh"
      to: "dev-orchestrator/references/gsd-skills-index.md"
      via: "écriture du fichier de sortie (VF_INDEX_OUT surchargeable)"
      pattern: "VF_INDEX_OUT"
---

<objective>
Scaffolder le module `dev-orchestrator/` (squelette distribuable) et livrer le générateur
d'index factuel `build-gsd-index.sh` qui produit `references/gsd-skills-index.md` à partir
des skills GSD réellement installés sur disque.

Purpose: Poser la fondation du module (structure conforme aux modules existants) et garantir
l'anti-hallucination (D4) : l'index est généré depuis `~/.claude/skills/gsd-*`, jamais écrit
à la main. Couvre IDX-01 et IDX-02.
Output: dossier `dev-orchestrator/` avec VERSION/CHANGELOG/README squelette, `references/`,
et `scripts/build-gsd-index.sh` fonctionnel produisant un index non vide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-06-04-dev-orchestrator-design.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

# Patterns à calquer (lire avant d'écrire) :
@consolidator/scripts/reindex.sh
@software-architecture/scripts/check-file-size.sh

<interfaces>
<!-- Format de sortie attendu dans gsd-skills-index.md (consommé par l'agent en Plan 03 et par les tests en Plan 05). -->
<!-- Chaque skill GSD installé apparaît avec son nom réel + sa description, extraits du frontmatter. -->

Frontmatter réel d'un SKILL.md GSD (vérifié sur disque), entre deux délimiteurs YAML :
```
(ligne délimiteur YAML)
name: gsd-execute-phase
description: "Execute all plans in a phase with wave-based parallelization"
argument-hint: "..."
(ligne délimiteur YAML)
```

Skills GSD présents sur disque : ~/.claude/skills/gsd-*/SKILL.md (≈60 skills).
Workflows GSD : ~/.claude/get-shit-done/workflows/*.md (optionnel, source secondaire).

Format Markdown attendu pour l'index :
```markdown
# GSD Skills Index (auto-généré — NE PAS ÉDITER)
> Généré le <date> par build-gsd-index.sh depuis ~/.claude/skills/gsd-*

| Skill | Description |
|-------|-------------|
| gsd-execute-phase | Execute all plans in a phase with wave-based parallelization |
| ... | ... |
```

<!-- CONTRAT D'OUTPUT (D7) : le script DOIT exposer la variable VF_INDEX_OUT.
     - VF_INDEX_OUT non défini → écrit dans dev-orchestrator/references/gsd-skills-index.md (défaut dev).
     - VF_INDEX_OUT défini → écrit à ce chemin exact (le hook post-install de Plan 05 passe
       VF_INDEX_OUT=.claude/agents/dev-orchestrator-references/gsd-skills-index.md).
     Le script crée le dossier parent de VF_INDEX_OUT si besoin (mkdir -p). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolder le squelette du module dev-orchestrator/</name>
  <files>dev-orchestrator/VERSION, dev-orchestrator/CHANGELOG.md, dev-orchestrator/README.md, dev-orchestrator/references/.gitkeep</files>
  <action>
    Créer la structure de base du module en calquant les modules existants (validator/, consolidator/).
    - `dev-orchestrator/VERSION` : contenu `v1.0.0` (plain text, convention STRUCTURE.md).
    - `dev-orchestrator/CHANGELOG.md` : markdown reverse-chronologique, entrée v1.0.0 datée 2026-06-04 décrivant "Module initial : agent routeur, index auto-généré, verbes /vf-*, bootstrap auto-install". Calquer le style de consolidator/CHANGELOG.md.
    - `dev-orchestrator/README.md` : SQUELETTE uniquement (titre, une phrase de description, section "Type: agent + multi-skills + scripts", placeholder "Voir Plan 05 pour la doc complète"). Le README final sera rempli en Plan 05 — ne pas le sur-détailler ici.
    - `dev-orchestrator/references/.gitkeep` : fichier vide pour versionner le dossier references/ (l'index y sera généré).
    Langue : français (convention du repo).
  </action>
  <verify>
    <automated>test -f dev-orchestrator/VERSION && grep -q 'v1.0.0' dev-orchestrator/VERSION && test -f dev-orchestrator/CHANGELOG.md && test -f dev-orchestrator/README.md && test -d dev-orchestrator/references</automated>
  </verify>
  <done>Le dossier dev-orchestrator/ existe avec VERSION (v1.0.0), CHANGELOG.md, README.md squelette, et references/ versionné.</done>
</task>

<task type="auto">
  <name>Task 2: Écrire build-gsd-index.sh (générateur d'index factuel)</name>
  <files>dev-orchestrator/scripts/build-gsd-index.sh, dev-orchestrator/references/gsd-skills-index.md</files>
  <action>
    Créer `scripts/build-gsd-index.sh` (bash, conventions CONVENTIONS.md : shebang `#!/usr/bin/env bash`, `set -euo pipefail`, header commentaire purpose/usage, fonctions snake_case, sections `# ----------`).
    Comportement (IDX-01) :
    - Variable `SKILLS_DIR="${VF_GSD_SKILLS_DIR:-$HOME/.claude/skills}"` (surchargeable pour les tests).
    - Variable `OUT="${VF_INDEX_OUT:-$(dirname "$0")/../references/gsd-skills-index.md}"` (D7 : VF_INDEX_OUT surchargeable par le hook post-install). Créer le dossier parent de `OUT` (`mkdir -p "$(dirname "$OUT")"`) pour supporter un chemin d'install arbitraire (`.claude/agents/dev-orchestrator-references/`).
    - Pour chaque `"$SKILLS_DIR"/gsd-*/SKILL.md`, extraire le `name:` et la `description:` du frontmatter YAML (head jusqu'au 2e délimiteur, parser `^name:` et `^description:`). Strip des guillemets éventuels sur la description.
    - Source secondaire optionnelle : si `~/.claude/get-shit-done/workflows/` existe, on peut lister les workflows en section annexe (facultatif, ne pas bloquer si absent).
    - Écrire `OUT` au format Markdown défini dans <interfaces> : header "auto-généré — NE PAS ÉDITER" + date (`date -Iseconds` avec fallback) + table `| Skill | Description |`. Factuel uniquement : aucun nom écrit en dur (D4, anti-hallucination).
    - Si aucun skill `gsd-*` trouvé : écrire un header avec un message clair et exit 0 (pas d'échec ; l'index sera régénéré après install GSD en Plan 02/05).
    - Idempotent : ré-exécution = régénération complète (overwrite), même état disque = même contenu (hors timestamp).
    Densité : viser ≤120 lignes (gate check-file-size n'audite que le code, mais rester lean).
    Puis EXÉCUTER le script une fois pour produire l'index initial committé : `bash dev-orchestrator/scripts/build-gsd-index.sh`.
  </action>
  <verify>
    <automated>grep -q 'VF_INDEX_OUT' dev-orchestrator/scripts/build-gsd-index.sh && bash dev-orchestrator/scripts/build-gsd-index.sh && test -s dev-orchestrator/references/gsd-skills-index.md && grep -q 'NE PAS' dev-orchestrator/references/gsd-skills-index.md && n=$(grep -Eo 'gsd-[a-z0-9-]+' dev-orchestrator/references/gsd-skills-index.md | sort -u | wc -l); [ "$n" -ge 1 ]; t=$(mktemp); VF_INDEX_OUT="$t" bash dev-orchestrator/scripts/build-gsd-index.sh && test -s "$t" && rm -f "$t"</automated>
  </verify>
  <done>build-gsd-index.sh s'exécute sans erreur, produit gsd-skills-index.md non vide contenant le header "NE PAS ÉDITER" et au moins un skill gsd-* distinct extrait du disque ; VF_INDEX_OUT redirige correctement la sortie vers un chemin arbitraire (contrat D7). Re-run = régénération sans erreur.</done>
</task>

</tasks>

<verification>
- `dev-orchestrator/` existe et suit la structure des modules (VERSION/CHANGELOG/README/scripts/references).
- `build-gsd-index.sh` génère un index non vide depuis les skills GSD installés (IDX-01).
- L'index ne contient que des noms réels (vérifiable : chaque `gsd-X` correspond à un dossier `~/.claude/skills/gsd-X`).
- VF_INDEX_OUT permet d'écrire l'index à un chemin arbitraire (D7, consommé par le hook post-install Plan 05).
- Ré-exécution du script = régénération sans erreur (base de IDX-02, branchée sur l'installeur en Plan 05).
- Anti-hallucination respectée : aucun nom de skill écrit en dur dans le script.
</verification>

<success_criteria>
- IDX-01 : index factuel généré depuis le disque ✓
- IDX-02 : script ré-exécutable + paramétrable via VF_INDEX_OUT (câblage installeur en Plan 05) ✓
- Squelette module conforme aux conventions vibeflow-os ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-orchestrator/01-01-SUMMARY.md`
</output>

---
phase: 24-activation-et-mesure-du-moteur-gsd
plan: 12
type: execute
wave: 4
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05", "24-06", "24-07", "24-08", "24-09", "24-10", "24-11"]
files_modified:
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/README.md
  - plugin/conductor/CHANGELOG.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/planning-core/VERSION
  - plugin/planning-core/module.json
  - plugin/planning-core/README.md
  - plugin/planning-core/CHANGELOG.md
  - plugin/business-pilot-bundle/VERSION
  - plugin/business-pilot-bundle/module.json
  - plugin/business-pilot-bundle/CHANGELOG.md
  - plugin/content-bundle/VERSION
  - plugin/content-bundle/module.json
  - plugin/content-bundle/CHANGELOG.md
  - plugin/growth-bundle/VERSION
  - plugin/growth-bundle/module.json
  - plugin/growth-bundle/CHANGELOG.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/mobile-test-team/VERSION
  - plugin/mobile-test-team/module.json
  - plugin/mobile-test-team/CHANGELOG.md
  - README.md
  - README.fr.md
autonomous: true
requirements: [GSDA-09, GSDA-13, GSDA-14, GSDA-15, GSDA-16, GSDA-20, GSDA-21, GSDA-22]

must_haves:
  truths:
    - "Chacun des 8 modules touchés par la phase porte une version bumpée, cohérente dans sa triade, avec une entrée de CHANGELOG écrite depuis les SUMMARY et non depuis l'intention."
    - "Le compteur « N suites » des deux README racine correspond au nombre réel de suites découvertes par la commande de la CI."
    - "`check-version-sync.sh` sort en succès sur l'ensemble de ses neuf contrôles."
    - "La VERSION racine, `plugin.json`, `marketplace.json`, les badges et les historiques des README ne sont PAS touchés — la release racine reste un geste humain."
  artifacts:
    - README.md
    - README.fr.md
  key_links:
    - "`check-version-sync.sh` est câblé au job `gates` de la CI : un compteur de suites périmé rend la CI rouge sur toutes les PR."
    - "Le contrôle n°7 de ce gate exige que l'entrée d'historique en tête des README corresponde à la VERSION racine : ne pas bumper la racine garde ce contrôle vert sans y toucher."
    - "Les deux suites créées par les plans 24-05 et 24-11 portent le décompte de 47 à 49 : c'est ce delta que ce plan solde."
---

<objective>
Rendre le dépôt cohérent avec ce que la phase a livré, sans franchir la frontière de la release
racine.

Purpose: onze plans ont touché huit modules et créé deux suites de tests. Tant que les triades de
version et le compteur de suites ne sont pas recalés, `check-version-sync.sh` est rouge et le job
`gates` de la CI l'est avec lui. Ce plan est le nœud de gouvernance de la phase — le patron des
Phases 13, 17, 19, 22 et 23 : les bumps de module sont dans le périmètre, la release racine n'y est
pas.

Output: huit triades cohérentes, huit entrées de CHANGELOG, deux compteurs recalés, et les gates de
sortie rejoués et consignés.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-01-SUMMARY.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-04-SUMMARY.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-05-SUMMARY.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : bumper en mineur les trois modules qui gagnent une capacité</name>
  <files>plugin/conductor/{VERSION,module.json,README.md,CHANGELOG.md}, plugin/dev-orchestrator/{VERSION,module.json,README.md,CHANGELOG.md}, plugin/planning-core/{VERSION,module.json,README.md,CHANGELOG.md}</files>
  <read_first>
    - `CLAUDE.md` § Numérotation — la règle : nouveau module ou nouvelle capacité implique mineur ; correctif, doc ou durcissement implique patch.
    - `scripts/check-version-sync.sh` lignes 73-121 — le contrôle n°6 (triade VERSION contre module.json) et le contrôle n°8 (en-tête Version du README de module contre VERSION) : les deux formes exactes que le bump doit satisfaire.
    - `plugin/conductor/CHANGELOG.md`, `plugin/dev-orchestrator/CHANGELOG.md`, `plugin/planning-core/CHANGELOG.md` — la forme des entrées existantes (titre de version, date, groupement des changements) : suivre le gabarit du fichier, ne pas en inventer un.
    - Les SUMMARY des plans 24-01, 24-03, 24-04, 24-05, 24-06, 24-08, 24-10 et 24-11 — c'est de là que sort le contenu des entrées de CHANGELOG, jamais de l'intention des plans.
  </read_first>
  <action>
Bumper les trois modules qui gagnent une capacité nouvelle, en mineur.

`conductor`, de sa version courante v1.19.2 vers v1.20.0 — motif : un gate créé
(`check-workstream-pointer.sh`, plan 24-05) et son câblage au démarrage de session, un gate durci
(`check-agents.sh` exige désormais `effort:`, plan 24-01), un gate rendu workstream-aware
(`check-state-integrity.sh`, plan 24-04), la doctrine de profondeur de dispatch écrite
(`team-kernel.md`, plan 24-01) et l'Iron Law 2 révisée (`AGENT.md`, plan 24-10).

`dev-orchestrator`, de v2.11.1 vers v2.12.0 — motif : un gate créé
(`check-capability-activation.sh`, plan 24-11), un générateur d'index étendu, une référence créée
(`workstreams.md`, plan 24-08), la doctrine du canal `agent_skills` et le refus de `tdd_mode`
(plan 24-03), les trois routes marquées et la frontière écrite (plan 24-06), le câblage `--ws` des
deux agents, et l'extension du gate d'exhaustivité T14.

`planning-core`, de v2.5.3 vers v2.6.0 — motif : `planning-context.sh` rendu workstream-aware
(plan 24-04).

Pour chacun, la triade complète doit rester cohérente : le fichier `VERSION`, le champ de version du
`module.json`, et l'en-tête `**Version**` du `README.md` du module portent exactement le même numéro
(le gate compare après retrait du préfixe et des espaces).

Écrire ensuite l'entrée de CHANGELOG de chaque module, au gabarit du fichier, depuis les SUMMARY des
plans qui l'ont touché — nommer les fichiers livrés, les gates créés ou durcis, et les décisions
écrites. Une entrée qui décrit l'intention plutôt que le livré est fausse dès sa rédaction.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `plugin/conductor/VERSION` vaut `v1.20.0`, `plugin/dev-orchestrator/VERSION` vaut `v2.12.0`, `plugin/planning-core/VERSION` vaut `v2.6.0`.
    - Pour chacun des trois, le champ de version du `module.json` et l'en-tête `**Version**` du `README.md` portent le même numéro que le fichier `VERSION` — vérifié par `jq` et par `awk`.
    - Chacun des trois `CHANGELOG.md` porte une entrée neuve citant la nouvelle version, datée, listant les artefacts réellement livrés.
    - Aucune entrée de CHANGELOG ne cite un chemin de fichier qui n'existe pas sur le disque : chaque chemin nommé est testé par `test -f`.
    - Le saut est bien mineur pour les trois : le numéro de correctif retombe à zéro.
  </acceptance_criteria>
  <done>Les trois modules qui gagnent une capacité portent une version mineure cohérente sur leur triade, avec un CHANGELOG écrit depuis le livré.</done>
  <reversibility rating="reversible">Numéros de version et documentation ; aucun comportement de code n'en dépend hors du gate de synchronisation.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : bumper en correctif les cinq modules qui ne gagnent qu'un frontmatter</name>
  <files>plugin/business-pilot-bundle/{VERSION,module.json,CHANGELOG.md}, plugin/content-bundle/{VERSION,module.json,CHANGELOG.md}, plugin/growth-bundle/{VERSION,module.json,CHANGELOG.md}, plugin/design-orchestrator/{VERSION,module.json,CHANGELOG.md}, plugin/mobile-test-team/{VERSION,module.json,CHANGELOG.md}</files>
  <read_first>
    - `CLAUDE.md` § Numérotation — pourquoi la propagation d'un champ de frontmatter est un durcissement, donc un correctif, et non une capacité nouvelle.
    - `scripts/check-version-sync.sh` lignes 103-121 — le contrôle n°8 et sa tolérance explicite : un README de module sans ligne de version n'est pas fautif, les bundles n'en déclarent pas. Vérifier lesquels des cinq en déclarent une avant d'éditer, plutôt que de le supposer.
    - `plugin/design-orchestrator/CHANGELOG.md` et l'un des CHANGELOG de bundle — les deux gabarits en présence.
    - Le SUMMARY du plan 24-01 — la répartition exacte des valeurs d'`effort:` par agent, à citer dans les entrées.
  </read_first>
  <action>
Bumper en correctif les cinq modules dont le seul changement est la propagation d'`effort:` sur leurs
agents : `business-pilot-bundle` de v2.0.3 vers v2.0.4 (5 agents), `content-bundle` de v2.0.3 vers
v2.0.4 (5 agents), `growth-bundle` de v2.0.3 vers v2.0.4 (5 agents), `design-orchestrator` de v1.4.0
vers v1.4.1 (3 agents), `mobile-test-team` de v1.4.1 vers v1.4.2 (3 agents).

Pour chacun : le fichier `VERSION` et le champ de version du `module.json` portent le même numéro.
L'en-tête de version du README n'est édité que pour les modules qui en déclarent une — le gate ne
vérifie que ce qui est déclaré, et l'ajouter à un module qui n'en a pas élargirait la surface gatée
sans décision.

Entrée de CHANGELOG pour chacun : la propagation d'`effort:` sur ses agents, avec les valeurs
réellement posées et le motif (barème par rôle, repris des agents-templates ; pilotage et jugement
haut, exécution mécanique bas), plus la mention que le gate d'agents exige désormais le champ. Citer
les noms d'agents exacts du module.

Ne bumper aucun autre module. En particulier, `plugin/reference` n'est pas touché : les trois
agents-templates dont le barème a été repris ont été lus, jamais modifiés.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - Les cinq fichiers `VERSION` valent respectivement `v2.0.4`, `v2.0.4`, `v2.0.4`, `v1.4.1`, `v1.4.2`.
    - Pour chacun des cinq, le champ de version du `module.json` égale le contenu du `VERSION` (vérifié par `jq`).
    - Chacun des cinq `CHANGELOG.md` porte une entrée neuve citant la nouvelle version et les noms d'agents du module concernés.
    - `plugin/reference/VERSION` est inchangé (`v2.5.2`) et aucun autre module que les huit du périmètre n'a de `VERSION` modifié : `rtk proxy git status --porcelain plugin/*/VERSION` ne liste que les 8 fichiers attendus.
    - Le saut est bien un correctif pour les cinq : seul le troisième segment change.
  </acceptance_criteria>
  <done>Les cinq modules dont les agents ont gagné un champ de frontmatter portent un correctif cohérent, et aucun module hors périmètre n'a bougé.</done>
  <reversibility rating="reversible">Numéros de version et documentation.</reversibility>
</task>

<task type="auto">
  <name>Tâche 3 : recaler le compteur de suites des deux README et rejouer les gates de sortie</name>
  <files>README.md, README.fr.md</files>
  <read_first>
    - `scripts/check-version-sync.sh` lignes 123-131 — le contrôle n°9 : le compteur cité par les README est comparé au résultat de la **même commande de découverte** que la CI. Relever cette commande exacte et l'employer, plutôt que de compter à la main.
    - `README.md` ligne 124 et son homologue de `README.fr.md` — la formulation qui porte le compteur ; le gate la cherche par motif, donc la reformuler casserait le contrôle.
    - `.github/workflows/ci.yml` lignes 205-215 et 289-301 — la découverte des suites et les étapes du job `gates` : ce sont ces commandes qui doivent être rejouées.
    - `CLAUDE.md` § Discipline de release — ce qui appartient au geste humain : bump de la VERSION racine, tag annoté, release GitHub. Rien de tout cela n'est dans ce plan.
  </read_first>
  <action>
Recaler le compteur de suites dans `README.md` et `README.fr.md`, dans la phrase qui le porte, en
employant la **valeur réelle** produite par la commande de découverte de la CI — deux suites de plus
que l'état d'origine, l'une créée par le plan 24-05, l'autre par le plan 24-11. Ne pas reformuler la
phrase autour du chiffre : le gate la cherche par motif, une reformulation le ferait échouer sans que
le chiffre soit faux.

Ne toucher à **rien d'autre** dans les deux README. En particulier, ne pas modifier : la `VERSION`
racine, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, les badges, la phrase du
nombre de modules (inchangé à 17), ni les entrées d'historique en tête — le contrôle n°7 du gate
exige que la première entrée corresponde à la VERSION racine, et ne pas bumper la racine le garde
vert sans y toucher. La release racine (bump, tag annoté, release GitHub) est un geste humain
explicitement hors du périmètre de cette phase.

Rejouer ensuite les gates de sortie et consigner leurs résultats au SUMMARY, chiffres à l'appui :
`check-version-sync.sh` ; `check-agents.sh --strict` sur les six dossiers d'agents, puis le run en
monde fermé ; l'ensemble des suites découvertes par la commande de la CI, avec le compte de suites et
le compte d'échecs ; `check-state-integrity.sh` ; et les deux gates créés par la phase
(`check-workstream-pointer.sh`, `check-capability-activation.sh`) invoqués sur l'arbre du dépôt.

Consigner aussi les deux faits de gouvernance connus, sans les enjoliver : aucune exécution
d'intégration continue n'a encore eu lieu sur la branche de cette phase, donc l'étape de CI ajoutée
au plan 24-09 n'aura pas tourné pour de vrai tant que la branche n'est pas poussée ; et le recalage
du ROADMAP § Phase 24 (huit faits périmés) n'appartient pas à ces plans, c'est un nœud documentaire
distinct de fin de mission.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - Le compteur cité par `README.md` et par `README.fr.md` égale la valeur rendue par la commande de découverte de la CI — l'écart d'origine était de deux suites.
    - La phrase qui porte le compteur conserve sa formulation : le contrôle n°9 du gate la trouve toujours par son motif.
    - `bash scripts/check-version-sync.sh` sort en succès sur ses **neuf** contrôles.
    - La `VERSION` racine, `.claude-plugin/plugin.json` et `.claude-plugin/marketplace.json` sont **inchangés** : `rtk proxy git status --porcelain` ne les liste pas.
    - Les entrées d'historique en tête des deux README sont inchangées, et le contrôle n°7 du gate reste vert.
    - La phrase du nombre de modules est inchangée (17).
    - Les gates de sortie sont rejoués et leurs résultats consignés au SUMMARY **avec leurs chiffres** : synchronisation des versions, gate d'agents en mode strict sur les 6 dossiers puis en monde fermé, découverte et exécution des suites (compte de suites et compte d'échecs), intégrité d'état, et les deux gates créés par la phase.
    - Les deux faits de gouvernance sont consignés au SUMMARY : l'étape de CI du plan 24-09 n'a pas encore tourné (branche non poussée), et le recalage du ROADMAP § Phase 24 est hors du périmètre de ces plans.
  </acceptance_criteria>
  <done>Le compteur de suites est exact, les neuf contrôles du gate de synchronisation passent, les gates de sortie sont rejoués et chiffrés, et la frontière de la release racine n'a pas été franchie.</done>
  <reversibility rating="reversible">Deux chiffres dans deux README ; leur correction est immédiate et le gate le dit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triades de version vers `check-version-sync.sh` vers le job `gates` | Une incohérence de version bloque toutes les PR du dépôt. |
| README racine vers les visiteurs publics | Un compteur ou une version affichés faux sont une affirmation publique fausse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-12-01 | Denial of Service | job `gates` de la CI | high | mitigate | `check-version-sync.sh` est le critère de vérification des trois tâches : aucune n'est faite tant que ses neuf contrôles ne passent pas. |
| T-24-12-02 | Repudiation | CHANGELOG décrivant l'intention | medium | mitigate | Chaque entrée est écrite depuis les SUMMARY, et chaque chemin cité est testé par `test -f` — une entrée nommant un artefact absent échoue à l'acceptation. |
| T-24-12-03 | Elevation of Privilege | franchissement de la frontière de release | high | mitigate | Contrôle machine : `VERSION`, `plugin.json` et `marketplace.json` doivent rester absents de la sortie de statut git ; les historiques des README ne sont pas touchés. |
| T-24-12-04 | Tampering | modules hors périmètre bumpés | medium | mitigate | Le statut git restreint aux fichiers `VERSION` de modules ne doit lister que les 8 attendus ; `plugin/reference/VERSION` est explicitement vérifié inchangé. |
| T-24-12-SC | Tampering | installs npm/pip/cargo | high | accept | Ce plan n'exécute aucune installation de paquet ; celle du job `tests` de la CI est antérieure à la phase et n'est pas modifiée. |
</threat_model>

<verification>
- `bash scripts/check-version-sync.sh` sort en succès sur ses neuf contrôles.
- `check-agents.sh --strict` vert sur les 6 dossiers d'agents, et vert en monde fermé.
- Les suites découvertes par la commande de la CI passent toutes, compte de suites et compte d'échecs consignés.
- `VERSION` racine, `plugin.json`, `marketplace.json` et les historiques des README sont intacts.
- Les deux faits de gouvernance (CI non exécutée, ROADMAP § Phase 24 hors périmètre) sont consignés au SUMMARY.
</verification>

<success_criteria>
Les huit modules touchés portent une version cohérente et un CHANGELOG écrit depuis le livré, le
compteur de suites est exact, les neuf contrôles du gate de synchronisation passent, et la release
racine reste entière entre les mains de Samuel.
</success_criteria>

## Artifacts this phase produces

| Symbole | Nature | Emplacement |
|---|---|---|
| `v1.20.0` | version de module (mineur) | `plugin/conductor/{VERSION,module.json,README.md}` |
| `v2.12.0` | version de module (mineur) | `plugin/dev-orchestrator/{VERSION,module.json,README.md}` |
| `v2.6.0` | version de module (mineur) | `plugin/planning-core/{VERSION,module.json,README.md}` |
| `v2.0.4` (x3) | version de bundle (correctif) | `business-pilot-bundle`, `content-bundle`, `growth-bundle` |
| `v1.4.1` / `v1.4.2` | version de module (correctif) | `design-orchestrator`, `mobile-test-team` |
| compteur « N suites » recalé | chiffre gaté par le contrôle n°9 | `README.md`, `README.fr.md` |
| 8 entrées de CHANGELOG | journal de module | `plugin/<module>/CHANGELOG.md` |

<output>
Créer `.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-12-SUMMARY.md` en fin de plan.
</output>

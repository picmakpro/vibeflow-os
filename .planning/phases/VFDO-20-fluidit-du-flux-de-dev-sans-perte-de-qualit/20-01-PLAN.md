---
phase: 20-fluidite-du-flux-de-dev-sans-perte-de-qualite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/check-debug-research.sh
  - plugin/conductor/hooks/hooks.json
  - plugin/conductor/scripts/tests/test-check-agents.sh
  - plugin/conductor/scripts/tests/test-check-debug-research.sh
autonomous: true
requirements: [SC1, SC6]

must_haves:
  truths:
    - "SC6 / D-24 (non négociable) : le chemin PAR DÉFAUT des deux gates (invocation SANS `--agents-dir` ni `--skills-dir`) est exercé par au moins un cas de chaque suite. Sur un cwd factice sans `.claude/agents`, `check-agents.sh --strict` rend **exit 3 INDÉTERMINÉ** — jamais un vert — et `check-debug-research.sh --strict` de même."
    - "SC6 / D-24 discriminance : un cas opposé au précédent pose une brique NON conforme dans `<cwd factice>/.claude/agents/` et invoque le gate SANS aucun flag de dossier — il sort **exit 1**. Ce cas meurt si la valeur par défaut `AGENTS_DIR` est altérée : c'est la sonde qui manquait à la Phase 16."
    - "SC6 / D-21 : en mode `--hook`, 0 erreur et au moins 1 avertissement produit une ligne sur stdout ; 0 erreur et 0 avertissement produit un stdout strictement vide. Le silence nominal est conservé, le faux vert sur dérive disparaît."
    - "SC6 / D-21 discriminance : retirer la branche d'affichage conditionnel des avertissements rend rouge le cas « 1 avertissement en mode hook » — prouvé par mutation, jamais par lecture."
    - "SC6 / D-18 + D-19 : les 2 commandes SessionStart de `plugin/conductor/hooks/hooks.json` portent explicitement `--agents-dir={{VF_SCRIPTS}}/../agents` ET `--skills-dir={{VF_SCRIPTS}}/../skills`. Aucune ligne de `merge-hooks.sh` n'est modifiée : son remplacement global du placeholder est réutilisé tel quel."
    - "SC6 / D-18 ↔ D-21 dans le MÊME commit : le correctif de périmètre de `hooks.json` et l'affichage conditionnel des avertissements arrivent ensemble. Aucun commit intermédiaire ne laisse un hook nouvellement scopé muet sur une vingtaine d'avertissements réels."
    - "SC6 / D-20 : `check-debug-research.sh` accepte `--third-party-prefix=PFX` (répétable, défaut `gsd-`) et `--no-third-party-prefix`, avec la MÊME sémantique que `check-agents.sh` — aucun second mécanisme d'exclusion n'est créé (critère 6 du ROADMAP)."
    - "SC6 / D-20 comptage explicite : le nombre de briques écartées par préfixe tiers est imprimé, jamais un skip muet — patron repris de `check-agents.sh`."
    - "SC1 / D-22 : le charset des tokens d'allowlist accepte `mcp__<serveur>__*` (joker FINAL seul) et continue de rejeter `mcp__*`, un joker en milieu de chaîne, et un joker collé à un suffixe."
    - "SC1 / D-05 : la clé de frontmatter `vf-mcp-tools` est reconnue par le gate (set `KNOWN`) — un agent qui la porte ne déclenche plus d'avertissement de champ inconnu. Une typo de cette clé le déclenche toujours."
    - "SC6 / D-23 : le défaut de charset est prouvé par cas unitaires sur le token, jamais par une tentative de reproduction avec un vrai serveur MCP (ce repo n'a pas de `.mcp.json`)."
    - "SC6 / portabilité prouvée par EXÉCUTION : les 2 suites sont relancées sur macOS et rendent 0 KO. La preuve Linux est déléguée au job CI `tests` (ubuntu-latest), déclenché au push — vérification différée explicite, jamais un blocage de tâche."
  artifacts:
    - plugin/conductor/scripts/check-agents.sh
    - plugin/conductor/scripts/check-debug-research.sh
    - plugin/conductor/hooks/hooks.json
    - plugin/conductor/scripts/tests/test-check-agents.sh
    - plugin/conductor/scripts/tests/test-check-debug-research.sh
  key_links:
    - "`hooks.json` → `plugin/_internal/merge-hooks.sh` (remplacement global du placeholder `{{VF_SCRIPTS}}` sur toute la ligne de commande) : c'est ce qui rend le correctif possible sans une ligne de code d'installeur. Modifier `merge-hooks.sh` casserait les 3 scopes d'install."
    - "`check-agents.sh` set `KNOWN` → `plugin/dev-orchestrator/agents/vf-reviewer.md` (plan 20-03) : la clé `vf-mcp-tools` doit être connue du gate AVANT que l'agent ne la porte, sinon 20-03 pose un avertissement permanent. C'est la dépendance de MESURE qui ordonne 20-03 après ce plan."
    - "`check-agents.sh` charset → `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` (en-tête) : le script d'injection produit `mcp__<serveur>__*` pour `vf-coder` et les 3 agents mobile-test. Sans D-22, tout lab avec `.mcp.json` rend `check-agents.sh --strict` rouge et `guard-agent-write.sh` refuse de réécrire l'agent."
    - "les 2 suites → `.github/workflows/ci.yml` : ramassées par le `find plugin scripts -type f -path '*/tests/test-*.sh'`. Aucune édition de `ci.yml` n'est nécessaire ni autorisée."
  prohibitions:
    - "P-01 — MUST NOT créer d'option d'exclusion nouvelle (`--exclude`, `--ignore-pattern`, `--skip`). Le critère 6 du ROADMAP interdit un SECOND mécanisme ; `--third-party-prefix` existe déjà et est porté tel quel (D-20)."
    - "P-02 — MUST NOT modifier les valeurs par défaut `AGENTS_DIR`/`SKILLS_DIR` des deux scripts. Le correctif de périmètre vit dans `hooks.json`, pas dans les scripts (D-18) : changer le défaut casserait la rétro-compatibilité de l'invocation CLI directe ET rendrait tautologique le cas discriminant de la tâche 1."
    - "P-03 — MUST NOT modifier `plugin/_internal/merge-hooks.sh`. On s'appuie sur son remplacement global existant, on ne le touche pas."
    - "P-04 — MUST NOT supprimer de façon inconditionnelle l'exemption du mode hook sur le contrat anti-vert-à-vide. D-21 conditionne l'AFFICHAGE des avertissements au compte réel ; il ne transforme pas le hook en gate bloquant ni bavard sur cible vide."
    - "P-05 — MUST NOT retirer les `|| true` des 2 commandes de `hooks.json` (hors périmètre du critère 6, laissé tel quel, D-21 in fine)."
    - "P-06 — MUST NOT resserrer la validation de FORME d'un token MCP (un nom de serveur mal orthographié continue de passer). Corollaire de D-22 explicitement mis hors périmètre — ne pas laisser le scope déborder."
    - "P-07 — MUST NOT restructurer le JSON de `plugin/conductor/hooks/hooks.json`. Le geste est ADDITIF (des flags ajoutés à 2 chaînes de commande existantes) : une phase ultérieure déjà arbitrée doit pouvoir y ajouter une commande SessionStart supplémentaire sans conflit de format."
    - "P-08 — MUST NOT modifier `.github/workflows/ci.yml`, la `VERSION` racine, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` ni les README racine."
    - "P-09 — MUST NOT lire, éditer ni committer `.planning/missions/2026-07-29-diagnostic-moteur-gsd-absent.md` (fichier non suivi appartenant à une autre session)."
---

<objective>
Rendre les deux gates de conformité SessionStart réellement utiles : leur périmètre cesse d'être un
accident du cwd (D-18, D-19), leur mode hook cesse de cacher les avertissements qu'il vient enfin de
trouver (D-21), leur charset de token cesse de contredire l'injecteur MCP d'ADR-051 (D-22), et le
chemin par défaut — jamais testé jusqu'ici — est enfin exercé (D-24).

Purpose: c'est le plan qui rend VISIBLE tout le reste de la phase. Le hook, tel qu'il tourne
aujourd'hui, sort 0 ligne : le garde-fou de conformité ne regarde rien. Corriger le périmètre seul
produirait un état PIRE (une vingtaine d'avertissements réels pour 0 ligne affichée) — d'où le
couplage obligatoire D-18 ↔ D-21 dans un commit unique. Le plan ouvre aussi la porte du plan 20-03
en faisant connaître au gate la clé `vf-mcp-tools` (D-05).

Output: 2 scripts et 1 fragment de hooks corrigés, 2 suites étendues, le chemin par défaut couvert.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-RESEARCH.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

## Périmètre, dépendances et risque (fiche de lecture directe pour `vf-dev-manager`)

| Rubrique | Valeur |
|---|---|
| **Changement couvert** | Changement 5 du CONTEXT (= Changement 4 du ROADMAP) + l'amorce D-05 de SC1 |
| **Décisions implémentées** | D-18, D-19, D-20, D-21, D-22, D-23, D-24 + la ligne `KNOWN` qui débloque D-05 |
| **Fichiers écrits** | `plugin/conductor/scripts/check-agents.sh`, `plugin/conductor/scripts/check-debug-research.sh`, `plugin/conductor/hooks/hooks.json`, `plugin/conductor/scripts/tests/test-check-agents.sh`, `plugin/conductor/scripts/tests/test-check-debug-research.sh` |
| **Dépendances amont** | aucune — vague 1 |
| **Dépendances aval (qui attend ce plan)** | **20-03** — dépendance de MESURE : la clé `vf-mcp-tools` doit être dans `KNOWN` avant que `vf-reviewer.md` ne la porte · **20-07** — rejoue `check-agents.sh --strict` sur les 6 dossiers d'agents, donc dépend du COMPORTEMENT de ce script, pas seulement de son existence |
| **Critères de succès** | SC6 (intégral), SC1 (partiel : charset D-22 + clé `KNOWN` de D-05) |
| **Risque** | **moyen** — `check-agents.sh` est le gate partagé par les 6 dossiers d'agents ET par `guard-agent-write.sh` : toute régression y devient immédiatement bloquante à l'écriture d'un agent |
| **Réversibilité** | **reversible** — 3 correctifs ciblés sur du code existant, 1 flag porté, 4 flags ajoutés à 2 chaînes de commande JSON. Rien de destructif, aucun contrat de sortie rompu |

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| `vf-mcp-tools` | clé de frontmatter (déclarée ici) | ajoutée au set `KNOWN` de `check-agents.sh` ; consommée en 20-03 |
| `THIRD_PARTY_PREFIXES` | variable shell (nouvelle sur `check-debug-research.sh`) | défaut `gsd-`, séparateur `:`, accumulative |
| `--third-party-prefix=PFX` | flag CLI (nouveau sur `check-debug-research.sh`) | répétable, sémantique identique à `check-agents.sh` |
| `--no-third-party-prefix` | flag CLI (nouveau sur `check-debug-research.sh`) | vide la liste avant accumulation |
| `VF_THIRD_PARTY_PREFIXES` | variable d'env (nouvelle sur `check-debug-research.sh`) | pont bash → python inline, même nom que sur `check-agents.sh` |
| `display_name()` | fonction python inline (nouvelle sur `check-debug-research.sh`) | miroir de `agent_display_name()` de `check-agents.sh` |
| `--agents-dir=` / `--skills-dir=` dans `hooks.json` | flags câblés (nouveaux) | valeurs `{{VF_SCRIPTS}}/../agents` et `{{VF_SCRIPTS}}/../skills` |
| `T55`…`T64` (indicatif) | labels de cas `test-check-agents.sh` | numérotation continue après `T54` |
| `T15`…`T21` (indicatif) | labels de cas `test-check-debug-research.sh` | numérotation continue après `T14` |

## Détecteurs `plan:pre`

`api-coverage` : **non déclenché** — détecteur exécuté sur le périmètre de la phase, sortie
`{"detected":false,"signals":[]}`. Aucune intégration d'API/SDK externe : `mcp__XcodeBuildMCP__*`
est un TOKEN d'allowlist de frontmatter, pas un client d'API. Aucune matrice de couverture n'est
fabriquée, aucun `COVERAGE.md` n'est écrit.
`schema-gate` : non déclenché — aucun fichier de schéma ni d'ORM dans le périmètre (gouvernance de
plugin, pas de base de données).
`assumption-delta` : non déclenché sur ce plan — le nœud concerné est traité en 20-06.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : le chemin par DÉFAUT des 2 gates, enfin exercé — et il rend INDÉTERMINÉ, pas un vert (D-24)</name>
  <reversibility rating="reversible">Ajout de cas de test seulement, aucun code de production touché.</reversibility>
  <read_first>
    - `plugin/conductor/scripts/tests/test-check-agents.sh` en entier — et particulièrement le helper `run_check()`, qui passe TOUJOURS `--agents-dir` et `--skills-dir` en dur : c'est la cause racine de l'angle mort. Lire aussi les helpers `ok()`/`ko()`, le `mktemp -d` de travail et son `trap ... EXIT`.
    - `plugin/conductor/scripts/tests/test-check-debug-research.sh` en entier — même `run_check()` avec les mêmes deux flags en dur, et les cas T13/T14 déjà présents qui exercent le contrat F13 AVEC `--agents-dir` : ce sont les cas jumeaux à décliner SANS les flags.
    - `plugin/conductor/scripts/check-agents.sh` — les valeurs par défaut `AGENTS_DIR` / `SKILLS_DIR`, puis la branche « aucun agent découvert » : une garde qui rend `3` avec un message `INDETERMINE` sous `--strict`, une seconde qui rend `0` avec un message « rien a verifier » hors mode hook, et le silence du mode hook.
    - `plugin/conductor/scripts/check-debug-research.sh` — mêmes valeurs par défaut, même branche `if not files:`, même contrat 0/1/3.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-24 (non négociable) et D-18.
    - `.planning/codebase/TESTING.md` — patron de suite (`ok()`/`ko()`, fixture `mktemp`, découverte CI) et section portabilité.
  </read_first>
  <files>plugin/conductor/scripts/tests/test-check-agents.sh, plugin/conductor/scripts/tests/test-check-debug-research.sh</files>
  <behavior>
    - `check-agents.sh --strict` lancé depuis un cwd factice SANS `.claude/agents` et SANS aucun flag de dossier → code de sortie 3, sortie contenant le mot INDETERMINE.
    - `check-agents.sh --hook` depuis le même cwd, sans flag → code de sortie 0, sortie strictement vide.
    - `check-agents.sh` nu (sans `--strict`, sans flag de dossier) depuis un cwd factice qui CONTIENT `.claude/agents/` avec une brique non conforme → code de sortie 1.
    - `check-debug-research.sh --strict` depuis un cwd factice sans `.claude/agents` ni `.claude/skills`, sans flag → code de sortie 3.
    - `check-debug-research.sh` nu depuis un cwd factice contenant `.claude/skills/<brique>/SKILL.md` de dépannage sans marqueur de recherche documentaire → code de sortie 1.
  </behavior>
  <action>
Ajouter à chacune des deux suites un bloc de cas dédié au chemin par défaut, numéroté à la suite de
l'existant (T55 et suivants pour `test-check-agents.sh`, T15 et suivants pour
`test-check-debug-research.sh`).

Contrainte structurelle, non négociable : ces cas N'UTILISENT PAS `run_check()`. Ce helper est
précisément ce qui masque le défaut — il injecte les deux flags de dossier en dur. Les nouveaux cas
invoquent `bash "$CHECK"` directement, dans un SOUS-SHELL qui se déplace dans un répertoire factice
créé par `mktemp -d`, afin que le cwd de la suite elle-même ne soit jamais modifié. La variable
`$CHECK` est déjà un chemin absolu (résolu par `cd ... && pwd` en tête de suite), l'invocation
depuis un autre cwd est donc sûre. Nettoyer le répertoire factice via le `trap ... EXIT` déjà en
place, ou par une suppression explicite en fin de bloc.

Écrire un commentaire d'en-tête au-dessus du bloc, qui dit POURQUOI ces cas existent : aucun des 58
plus 14 cas préexistants n'exerçait le chemin par défaut, et c'est ce qui a laissé le défaut de
périmètre survivre à une phase entière dédiée à ce script. Nommer explicitement la mutation que ces
cas tuent : altérer la valeur par défaut du dossier d'agents (ou de skills).

Trois cas par suite, dans cet ordre :

1. Cible absente, mode strict. Sous-shell dans un répertoire factice vide, invocation avec
   `--strict` seul. Assertion sur DEUX variables distinctes, jamais l'une déduite de l'autre : le
   code de sortie vaut 3, et la sortie capturée contient le mot INDETERMINE. Le libellé du cas doit
   dire « INDÉTERMINÉ, pas un vert » — c'est le contrat F13 et le point non négociable de D-24.

2. Cible absente, mode hook. Même répertoire factice, invocation avec `--hook` seul. Le code de
   sortie vaut 0 et la sortie capturée (stdout et stderr fusionnés) est de longueur nulle. Ce cas
   PINNE l'exemption volontaire du mode hook sur cible vide : elle doit rester, et une suppression
   inconditionnelle future ferait échouer ce cas — filet posé exprès.

3. Cible PRÉSENTE au chemin par défaut, le cas discriminant. Créer, dans le répertoire factice,
   l'arborescence `.claude/agents/` (respectivement `.claude/skills/<nom>/`) et y écrire une brique
   volontairement NON conforme : pour le gate d'agents, un fichier `.md` sans frontmatter ; pour le
   gate de recherche documentaire, un `SKILL.md` dont la description porte une signature de
   dépannage et dont le corps ne mentionne aucun marqueur de recherche documentaire. Invocation
   SANS aucun flag de dossier. Le code de sortie vaut 1. C'est ce cas — et lui seul — qui prouve
   que la valeur par défaut résout une cible réelle ; les deux premiers passeraient encore avec un
   défaut cassé.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh && bash plugin/conductor/scripts/tests/test-check-debug-research.sh</automated>
  </verify>
  <acceptance_criteria>
    - Les deux suites sortent 0 KO, avec un compteur de cas passés strictement supérieur à celui d'avant la tâche (58 → au moins 61, et 14 → au moins 17).
    - Le bloc ajouté ne contient aucune occurrence de `run_check` : vérifié par lecture ciblée du diff (`git diff -U0` sur les 2 fichiers, aucune ligne ajoutée ne contient ce nom).
    - Preuve de discriminance par MUTATION, EXÉCUTÉE et consignée au compte rendu : remplacer temporairement la valeur par défaut du dossier d'agents dans `check-agents.sh` par un chemin qui n'existera jamais fait passer `test-check-agents.sh` à au moins 1 KO (le cas 3). Restaurer la valeur, la suite repasse à 0 KO. La même mutation AVANT cette tâche laissait la suite intégralement verte — c'est la preuve que le cas est utile.
    - Le cwd de la suite est inchangé après exécution : ajouter en fin de suite une assertion comparant `pwd` à sa valeur capturée en tête (les déplacements sont confinés dans des sous-shells).
    - Portabilité : la commande de `<verify>` tourne verte sur macOS (bash 3.2) en local — preuve immédiate, obligatoire, jamais une preuve par lecture. La preuve Linux est DIFFÉRÉE au job CI `tests` (ubuntu-latest) déclenché au push : elle est notée comme vérification différée au compte rendu, elle ne bloque pas la tâche.
  </acceptance_criteria>
  <done>Les deux suites exercent le chemin par défaut, assertent INDÉTERMINÉ sur cible absente en mode strict, le silence total en mode hook, et une détection réelle sur cible présente ; la mutation du défaut est létale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : `check-debug-research.sh` reçoit `--third-party-prefix` — le mécanisme EXISTANT, jamais un second (D-20)</name>
  <reversibility rating="reversible">Flag additif dont le défaut reproduit le comportement utile ; le retirer restaure l'état actuel.</reversibility>
  <read_first>
    - `plugin/conductor/scripts/check-agents.sh` — la source à porter, dans cet ordre : le paragraphe d'en-tête qui documente la sémantique du flag (deux effets distincts, comptés et imprimés SÉPARÉMENT, jamais un skip muet) ; la déclaration `THIRD_PARTY_PREFIXES="gsd-"` ; les deux branches de parsing `--third-party-prefix=*` (accumulation avec séparateur `:`) et `--no-third-party-prefix` ; le passage par variable d'environnement `VF_THIRD_PARTY_PREFIXES` ; la reconstruction python de la liste depuis cette variable ; la fonction `agent_display_name()` ; et la boucle de filtrage qui compare le nom d'affichage aux préfixes avant d'appeler `check_file()`, avec son compteur et sa ligne de résumé.
    - `plugin/conductor/scripts/check-debug-research.sh` en entier (180 lignes) — la boucle d'arguments, le bloc d'export des variables `VF_*`, les fonctions `label_for()`, `frontmatter_block()` et `field()`, la construction de la liste `files` (skills d'abord, agents ensuite), et le point exact d'insertion du filtrage : juste avant l'appel à `check_file()` dans la boucle.
    - `plugin/conductor/scripts/tests/test-check-debug-research.sh` — le helper `skill()`, `reset_sk()` et `run_check()`.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-20, et la raison explicite pour laquelle ce n'est PAS l'option redondante interdite par le critère 6.
    - `.planning/ROADMAP.md` §Phase 20, critère de succès 6.
  </read_first>
  <files>plugin/conductor/scripts/check-debug-research.sh, plugin/conductor/scripts/tests/test-check-debug-research.sh</files>
  <behavior>
    - Une brique dont le `name` de frontmatter commence par `gsd-` et qui serait sinon en erreur (signature de dépannage, aucun marqueur de recherche documentaire) n'est plus lintée : code de sortie 0.
    - La même brique sous `--no-third-party-prefix` redevient une erreur : code de sortie 1.
    - Une brique `gsd-*` écartée et une brique `vf-*` en erreur dans le même run : code de sortie 1 (seule la seconde compte), et la sortie porte le nombre de briques tierces écartées.
    - `--third-party-prefix=acme-` ajouté au défaut : les briques `gsd-*` ET `acme-*` sont écartées toutes les deux (accumulation, pas remplacement).
    - Une brique dont le nom ne matche aucun préfixe reste lintée exactement comme avant : aucun cas préexistant de la suite ne change de verdict.
  </behavior>
  <action>
Porter dans `check-debug-research.sh` le mécanisme de `check-agents.sh`, à l'identique : même nom de
flag, même valeur par défaut, même séparateur, même sémantique d'accumulation, même nom de variable
d'environnement de pont. Aucune variante, aucun nom alternatif — le critère 6 du ROADMAP interdit
d'inventer un SECOND mécanisme d'exclusion, il n'interdit pas de réutiliser celui qui existe déjà
pour un script qui ne l'a pas encore.

Côté bash : déclarer `THIRD_PARTY_PREFIXES="gsd-"` à côté des autres variables de configuration ;
ajouter les deux branches de parsing dans la boucle d'arguments existante ; exporter
`VF_THIRD_PARTY_PREFIXES` dans le bloc d'environnement qui précède l'invocation de l'interpréteur
python.

Côté python inline : reconstruire la liste depuis l'environnement avec la même expression que dans
`check-agents.sh`. Écrire une fonction `display_name(path, text)` qui MIROITE `agent_display_name()` :
la valeur du champ `name` du frontmatter si elle existe et est non vide, sinon un repli lisible — le
nom du dossier parent pour un `SKILL.md`, le nom de fichier sans son extension pour un agent (la
même règle que `label_for()`, moins le suffixe). Le frontmatter est déjà extrait par
`frontmatter_block()` et lu par `field()` : ne pas réécrire un parseur.

Insérer le filtrage dans la boucle qui parcourt `files` : lire le fichier, calculer le nom
d'affichage, chercher un préfixe qui matche ; si un préfixe matche, incrémenter un compteur dédié et
passer au fichier suivant SANS appeler `check_file()`. La fonction `check_file()` ouvre aujourd'hui
le fichier elle-même : accepter la double lecture (simple, sans état partagé) ou faire remonter le
texte lu en paramètre optionnel ; ne pas introduire de cache global.

Imprimer le compteur dans la sortie hors mode hook, sur le modèle exact de la ligne de résumé de
`check-agents.sh` : le nombre de briques écartées et la liste des préfixes actifs. Un écartement qui
ne se voit pas est un skip muet, et le repo l'interdit. En mode hook, ce résumé n'est PAS imprimé
(le hook reste minimal — seule la règle D-21 de la tâche 3 y ajoute des avertissements, et elle ne
concerne que l'autre script).

Compléter l'en-tête documentaire du script : la ligne d'usage gagne les deux flags, et un paragraphe
reprend la sémantique en deux effets. Documenter honnêtement la limite : le défaut `gsd-` n'écarte
que les briques tierces effectivement préfixées ainsi ; une brique tierce d'un autre écosystème
(nom sans préfixe partagé) reste lintée, et le levier prévu est une répétition du flag — jamais un
mécanisme supplémentaire.

Ajouter enfin les cas correspondants à `test-check-debug-research.sh`, numérotés à la suite, en
utilisant `run_check()` : ces cas exercent le filtrage, pas le chemin par défaut (couvert en tâche 1).
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-debug-research.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/check-debug-research.sh --help` imprime les deux nouveaux flags et sort 0.
    - La suite sort 0 KO, avec un compteur d'au moins 21 cas passés (14 d'origine, 3 de la tâche 1, au moins 4 ici), et AUCUN cas préexistant T1 à T14 ne change de verdict.
    - Non-duplication du mécanisme, vérifiée par commande : `grep -nE '\-\-(exclude|ignore-pattern|skip)' plugin/conductor/scripts/check-debug-research.sh` ne renvoie aucune ligne.
    - Parité sémantique, vérifiée par commande : `grep -h 'THIRD_PARTY_PREFIXES="gsd-"' plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/check-debug-research.sh | wc -l` renvoie 2.
    - Discriminance prouvée par MUTATION et consignée : retirer l'instruction de saut du filtrage fait passer à au moins 1 KO le cas « brique tierce non conforme écartée ». Restauration, retour à 0 KO.
    - Portabilité : suite verte à l'EXÉCUTION sur macOS (bash 3.2). Preuve Linux différée au job CI `tests`, notée comme telle au compte rendu.
  </acceptance_criteria>
  <done>`check-debug-research.sh` porte le mécanisme `--third-party-prefix` de `check-agents.sh` à l'identique, compte et imprime les briques écartées, et sa suite le prouve par mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : un seul commit — périmètre explicite des 2 hooks (D-18/D-19) + avertissements conditionnels (D-21) + charset MCP (D-22) + clé `vf-mcp-tools` (D-05)</name>
  <reversibility rating="reversible">Quatre correctifs ciblés, tous additifs ou conditionnels ; aucun contrat de sortie modifié, aucun défaut CLI changé.</reversibility>
  <read_first>
    - `plugin/conductor/hooks/hooks.json` en entier (23 lignes) — la structure des groupes `PreToolUse` et `SessionStart`, et les 2 chaînes de commande de conformité à étendre.
    - `plugin/_internal/merge-hooks.sh` autour de la ligne 167 — le remplacement GLOBAL du placeholder `{{VF_SCRIPTS}}` sur toute la ligne de commande. À LIRE pour confirmer, à NE PAS modifier : c'est lui qui rend le correctif possible sans une ligne d'installeur.
    - `plugin/conductor/scripts/check-agents.sh` — quatre zones : le paragraphe d'en-tête qui affirme aujourd'hui que le mode hook n'imprime que les erreurs et jamais les avertissements ; le bloc final `if hook:` qui imprime les erreurs puis sort en 0, point d'insertion exact de la branche d'avertissement ; la fonction d'analyse d'un token d'allowlist et sa branche « token sans parenthèse » portant le `re.fullmatch` de charset ; le set `KNOWN` et le commentaire qui documente déjà les deux conventions VibeFlow qu'il contient.
    - `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh`, les 15 premières lignes — l'en-tête qui cite la forme sûre `mcp__<serveur>__*` : c'est elle que le charset actuel rejette, et c'est le contresens à fermer.
    - `plugin/conductor/scripts/update-banner.sh` — le patron de contrat de sortie « silence total en nominal, message seulement quand il y a quelque chose à dire ».
    - `plugin/conductor/scripts/tests/test-check-agents.sh` — les cas T9 (mode hook sur agent non conforme), T28 (outil hors set connu : avertissement en défaut, erreur en `--strict`), T41 (token hors charset au niveau nu) et T5 (champ inconnu → avertissement) : les quatre cas jumeaux dont les nouveaux cas sont les variantes.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-18, D-19, D-21, D-22, D-23, D-05 ; et `20-RESEARCH.md` §Common Pitfalls, Pitfall 2 (le faux vert transitoire si D-18 est commité sans D-21).
  </read_first>
  <files>plugin/conductor/scripts/check-agents.sh, plugin/conductor/hooks/hooks.json, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <behavior>
    - Mode hook, 0 erreur, 0 avertissement → stdout de longueur nulle, code de sortie 0.
    - Mode hook, 0 erreur, au moins 1 avertissement → stdout contient le compte d'avertissements et un renvoi vers l'invocation explicite ; code de sortie 0.
    - Mode hook, au moins 1 erreur → sortie inchangée par rapport à aujourd'hui (les erreurs priment, les avertissements ne s'y ajoutent pas) ; code de sortie 0.
    - `tools: Read, mcp__XcodeBuildMCP__*` → accepté, code de sortie 0.
    - `tools: Read, mcp__XcodeBuildMCP__test_sim` → accepté, code de sortie 0 (déjà vrai aujourd'hui, à pinner en non-régression).
    - `tools: Read, mcp__*` → rejeté, code de sortie 1.
    - `tools: Read, mcp__XcodeBuildMCP__*_sim` → rejeté, code de sortie 1 (joker non terminal).
    - `tools: Read, mcp__Xcode*MCP__*` → rejeté, code de sortie 1 (joker dans le nom de serveur).
    - Agent portant `vf-mcp-tools: XcodeBuildMCP:test_sim,build_sim,clean` → aucun avertissement de champ inconnu ; sous `--strict`, code de sortie 0.
    - Agent portant une typo de cette clé → l'avertissement de champ inconnu est présent.
  </behavior>
  <action>
Les quatre correctifs de cette tâche arrivent dans UN SEUL commit. Ce n'est pas une préférence de
style : livrer le correctif de périmètre de `hooks.json` sans l'affichage conditionnel des
avertissements produirait un régime transitoire de faux vert — le hook trouverait enfin une
vingtaine d'avertissements réels et n'en imprimerait aucun.

1. `hooks.json`, périmètre explicite (D-18, D-19). Sur les 2 commandes SessionStart de conformité,
ajouter `--agents-dir={{VF_SCRIPTS}}/../agents` et `--skills-dir={{VF_SCRIPTS}}/../skills` entre le
nom du script et le `|| true` déjà présent. Les DEUX commandes reçoivent les DEUX flags : corriger
le dossier d'agents sans le dossier de skills perdrait des findings sur les deux scripts. Ne rien
changer d'autre — ni la troisième commande, ni le groupe `PreToolUse`, ni la forme du JSON. Le geste
est strictement additif.

2. `check-agents.sh`, avertissements conditionnels en mode hook (D-21). Dans le bloc final du mode
hook, ajouter une branche qui ne se déclenche QUE si le compte d'erreurs est nul ET le compte
d'avertissements strictement positif : une ligne unique et compacte, préfixée comme les autres
sorties du script, qui donne le nombre d'avertissements et renvoie vers l'invocation explicite du
gate pour le détail. Ne PAS énumérer les avertissements un par un dans le hook (le mode reste
minimal). Ne PAS toucher aux deux gardes d'exemption sur cible vide : le mode hook doit rester
totalement silencieux quand il n'a rien trouvé — c'est le patron d'`update-banner.sh`, et c'est ce
que le cas 2 de la tâche 1 pinne. Mettre à jour, dans l'en-tête documentaire du script, le
paragraphe qui affirme aujourd'hui que le mode hook n'imprime jamais les avertissements : il devient
« n'imprime les avertissements que lorsqu'il y en a ; silence total en régime nominal ».

3. `check-agents.sh`, charset d'un token d'allowlist (D-22). Dans la branche « token sans
parenthèse » de la fonction d'analyse, élargir la condition d'acceptation : le token reste valide
s'il matche le charset actuel, OU s'il matche la forme MCP à joker terminal — le préfixe littéral
`mcp__`, un nom de serveur du charset `[A-Za-z0-9_-]+`, le séparateur `__`, puis le joker et rien
d'autre. Écrire le joker sous forme de classe de caractères `[*]` plutôt qu'échappé par une
contre-oblique : le bloc python de ce script vit dans une chaîne shell entre guillemets doubles, et
une classe de caractères évite toute question d'interprétation de la contre-oblique par bash. Aucun
joker n'est accepté ailleurs : ni en tête, ni en milieu de chaîne, ni dans le nom du serveur, ni
suivi d'un suffixe. Documenter le pourquoi en une ligne de commentaire au-dessus de la condition, en
citant l'en-tête d'`inject-mcp-tools.sh` qui produit précisément cette forme. Ne PAS ajouter de
validation de la forme générale d'un token MCP : un nom de serveur mal orthographié continue de
passer, c'est explicitement hors périmètre.

4. `check-agents.sh`, la clé `vf-mcp-tools` devient connue du gate (D-05). Ajouter la chaîne
`vf-mcp-tools` au set `KNOWN`, et une ligne au commentaire qui documente déjà les deux conventions
VibeFlow de ce set : dire que cette clé porte une allowlist MCP NOMMÉE (un serveur, une liste
d'outils explicites), qu'elle est consommée par le script d'injection du module dev-orchestrator, et
qu'elle coexiste avec la clé booléenne existante sans la remplacer. Cette ligne est le pré-requis du
plan 20-03 : sans elle, l'agent qui portera la clé déclencherait un avertissement à chaque session.

5. Cas de test. Ajouter à `test-check-agents.sh`, numérotés à la suite : les 3 cas de comportement du
mode hook (silence à 0 avertissement, ligne présente à au moins 1 avertissement, erreurs
inchangées), les 5 cas de charset (2 formes acceptées, 3 formes rejetées) et les 2 cas de clé de
frontmatter (clé exacte silencieuse, typo signalée). Ces cas passent par `run_check()` : ils
n'exercent pas le chemin par défaut, couvert par la tâche 1.
  </action>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh && python3 -c "import json;d=json.load(open('plugin/conductor/hooks/hooks.json'));c=[h['command'] for g in d['hooks']['SessionStart'] for h in g['hooks']];n=sum(('--agents-dir=' in x and '--skills-dir=' in x) for x in c);assert n==2,(n,c);print('hooks.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test-check-agents.sh` sort 0 KO avec au moins 71 cas passés (58 d'origine, 3 de la tâche 1, 10 ici).
    - `hooks.json` reste un JSON valide et EXACTEMENT 2 de ses commandes SessionStart portent les deux flags de dossier — asserté par la commande python du bloc `<verify>` (elle échoue si 1 ou 3 commandes les portent).
    - Les `|| true` sont préservés : `grep -c '|| true' plugin/conductor/hooks/hooks.json` renvoie 3.
    - Le placeholder n'a pas été résolu à la main dans le fichier source : `grep -c '{{VF_SCRIPTS}}' plugin/conductor/hooks/hooks.json` renvoie au moins 8 (2 chemins de script + 4 chemins de dossier + les 2 autres commandes), et `plugin/_internal/merge-hooks.sh` est absent du diff du commit (`git diff --name-only` ne le liste pas).
    - Discriminance D-21 prouvée par MUTATION et consignée : supprimer la branche d'avertissement conditionnel fait passer à au moins 1 KO le cas « mode hook, 1 avertissement ». Restauration, retour à 0 KO.
    - Discriminance D-22 prouvée par MUTATION et consignée : remplacer la classe de caractères du joker terminal par un quantificateur permissif fait passer à au moins 1 KO le cas « joker non terminal rejeté ». Restauration, retour à 0 KO.
    - Non-régression de l'ensemble du repo : la boucle `find plugin scripts -type f -path '*/tests/test-*.sh' | sort | while IFS= read -r t; do bash "$t" || echo "KO $t"; done` n'imprime aucune ligne `KO`.
    - Portabilité : les commandes ci-dessus tournent vertes sur macOS (bash 3.2) en EXÉCUTION. Preuve Linux différée au job CI `tests`, notée comme vérification différée au compte rendu.
  </acceptance_criteria>
  <done>Le périmètre des 2 hooks est explicite, le mode hook parle quand il a quelque chose à dire et se tait sinon, la forme MCP à joker terminal passe le charset, la clé `vf-mcp-tools` est connue du gate — le tout en un commit unique, prouvé par mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lab utilisateur → gate de conformité | le frontmatter des agents et skills lus par les 2 gates est du contenu non maîtrisé (lab tiers, agents d'un autre écosystème) |
| fragment de hooks VibeFlow → `settings.json` du lab | la ligne de commande passe par le remplacement de placeholder de `merge-hooks.sh` avant d'être exécutée au SessionStart |
| script d'injection MCP → gate de conformité | `inject-mcp-tools.sh` écrit des tokens que `check-agents.sh` doit accepter : le contrat entre les deux est un contrat de forme |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01-01 | Repudiation | hook SessionStart (`check-agents.sh --hook`) | high | mitigate | D-18 + D-21 dans le même commit : périmètre explicite ET avertissements imprimés quand il y en a. Le faux vert (signal réel présent, 0 ligne affichée) disparaît au lieu d'être aggravé. Prouvé par le cas de mutation de la tâche 3. |
| T-20-01-02 | Repudiation | chemin par défaut des 2 gates | high | mitigate | D-24 : cas dédiés qui assertent INDÉTERMINÉ (exit 3) sur cible absente et une détection réelle sur cible présente. Sans eux, une régression du défaut redeviendrait invisible (précédent Phase 16). |
| T-20-01-03 | Tampering | validation de charset d'un token d'allowlist (ASVS V5) | medium | mitigate | D-22 : élargissement STRICTEMENT borné au joker terminal après `mcp__<serveur>__`. Trois cas de rejet pinnent les formes voisines (joker seul, joker non terminal, joker dans le nom de serveur). |
| T-20-01-04 | Spoofing | nom de serveur d'un token MCP (`mcp__<typo>__outil`) | low | accept | Corollaire de D-22 explicitement hors périmètre des 7 critères. Dette connue et datée, aucune régression introduite ici : le gate ne validait déjà pas cette forme. Consignée au CHANGELOG en 20-07. |
| T-20-01-05 | Elevation of Privilege | `--third-party-prefix` sur `check-debug-research.sh` | medium | mitigate | Mécanisme porté à l'identique, défaut `gsd-` inchangé, et compteur imprimé : un écartement est toujours visible, jamais un skip muet. `--no-third-party-prefix` reste le levier de vérification exhaustive. |
| T-20-01-06 | Denial of Service | ligne de commande du hook SessionStart | low | accept | Aucun `exit` d'un hook SessionStart ne bloque la session (doc officielle) ; les `|| true` sont conservés ; le chemin `{{VF_SCRIPTS}}/../agents` est dérivé d'un placeholder déjà résolu par l'installeur dans les 3 scopes. |
| T-20-01-SC | Tampering | installations npm/pip/cargo | — | accept | Aucune commande d'installation de paquet dans le périmètre de ce plan ni de la phase — l'audit de légitimité de paquets est sans objet (20-RESEARCH.md §Package Legitimacy Audit : non applicable). |
</threat_model>

<verification>
- Les 2 suites touchées sortent 0 KO à l'exécution locale macOS.
- La boucle complète des suites du repo (commande exacte de la CI) n'imprime aucun échec.
- `check-agents.sh --strict --agents-dir=plugin/conductor/agents` et les 5 autres dossiers d'agents sortent 0 (aucune régression introduite par les correctifs).
- `hooks.json` est un JSON valide, ses 2 commandes de conformité portent les 4 flags, ses 3 `|| true` sont intacts.
- Les 3 mutations décrites dans les critères d'acceptation ont été JOUÉES, ont rendu rouge la suite concernée, et l'état d'origine a été restauré.
- Vérification différée notée au compte rendu : le job CI `tests` (ubuntu-latest) rejoue les 2 suites sous bash 5.x au push.
</verification>

<success_criteria>
- SC6 tenu sur ses trois volets : périmètre des 2 hooks corrigé, garde-fou silencieux en nominal et utile sur dérive, aucune option d'exclusion redondante créée.
- SC1 partiellement tenu : le charset accepte la forme produite par l'injecteur MCP, et la clé `vf-mcp-tools` est connue du gate (pré-requis de 20-03).
- Le chemin par défaut des 2 gates est couvert par des cas discriminants, prouvés par mutation.
- Aucun fichier hors de `files_modified` n'est touché.
</success_criteria>

<output>
Create `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-01-SUMMARY.md` when done.
</output>

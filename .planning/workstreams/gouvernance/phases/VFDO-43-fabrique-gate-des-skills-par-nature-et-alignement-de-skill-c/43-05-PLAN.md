---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 05
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh
  - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh
  - plugin/_internal/vibeflow-update.sh
  - plugin/_internal/tests/test-vibeflow-update.sh
  - plugin/conductor/scripts/check-agents.sh
  - plugin/conductor/scripts/tests/test-check-agents.sh
autonomous: true
requirements: [FABR-10]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un serveur nommé par vf-mcp-tools et absent des sources résolues (union scope projet ∪ scope global, ou liste --servers explicite) est signalé par une ligne WARNING qui nomme le fichier, le serveur et les sources réellement consultées ; la ligne d'injection de cette branche ne se présente plus comme un no-op sans signal ; rc 0 par défaut (un lab sans serveur Xcode reste sain) (durcissement b, D-Q3)"
    - "Le jugement « absent » se fait contre l'UNION des deux scopes (ADR-051-B) : un serveur nommé déclaré seulement en scope global est injecté sans WARNING ; absent des deux, il est signalé en nommant les deux fichiers (T32, T33)"
    - "L'installeur (vibeflow-update.sh) relaie dans son journal chaque ligne WARNING ou ERROR de l'injecteur au lieu de jeter toute sa sortie ; l'installation reste best-effort (rc 0) ; témoin lab frais sous HOME temporaire"
    - "En mode injection, une valeur vf-mcp-tools malformée (sans deux-points, serveur vide, aucun outil, segment hors [A-Za-z0-9_-]+) est REFUSÉE : ligne ERROR sur stderr sans recopier la valeur brute, fichier non modifié, rc 1 à la fin du balayage — les autres agents du dossier sont quand même traités (durcissement a, D-Q3)"
    - "check-agents.sh refuse (rc 1, tous modes) un agent dont vf-mcp-tools est malformée, selon la même règle d'extraction (même motif de présence, clé présente une seule fois, deux-points collé à la clé, valeur lue sur la SEULE ligne de la clé, jamais la ligne suivante, ligne indentée qui suit refusée — « indentée » ayant une seule définition, premier caractère espace ou tabulation, portée des deux côtés par la même constante MCP_CONTINUATION_RE de littéral `^[ \\t]`), le même ORDRE trim puis déquotage, la même grammaire, le même charset et le même déquotage (une paire de guillemets englobante retirée) que named_request de l'injecteur : pour TOUTE valeur, le gate et l'injecteur rendent le même verdict — valeurs entre guillemets (T22e, T110), valeur vide suivie d'une autre clé ou continuée sur une ligne indentée, y compris une continuation à une espace ou à une tabulation sous une valeur valide (T22f, T111), clé en double dans les deux ordres (T22g, T112), valeur entre guillemets suivie d'un blanc, d'une tabulation ou d'un CR (T22h, T113), clé en dernière ligne du frontmatter (T22i, T114), espace avant le deux-points (T22j, T115) ; le corpus réel (vf-reviewer) reste vert"
    - "Les textes qui ne nommaient que vf-mcp-consumer dans vibeflow-update.sh (trois sites, re-greppés) nomment aussi vf-mcp-tools (durcissement c ; le quatrième site, vf-calibrate/SKILL.md, relève de 43-06)"
    - "Le SUMMARY porte une section « Relecture Samuel (polarité dev-orchestrator) » : commits de ce plan sous plugin/dev-orchestrator et plugin/_internal, changements de comportement de l'installeur (relais WARNING/ERROR) et de l'injecteur (rc 1 sur valeur malformée), constat T23 à T31 ; l'amendement de la spec fabrique et le patch de dev-orchestrator sont portés par 43-07 (découpage de révision, 2026-09-25)"
  artifacts:
    - path: "plugin/dev-orchestrator/scripts/inject-mcp-tools.sh"
      provides: "malformed_found et sortie rc 1 (a) ; message de serveur absent honnête et sources nommées (b)"
      contains: "malformed_found"
    - path: "plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh"
      provides: "T16 et T22a-T22d durcis, T22e (guillemets), T22f (valeur sur la seule ligne de la clé, jumeaux de continuation à une espace et à une tabulation), T22g (clé en double), T22h (ordre trim puis déquotage), T22i (clé en dernière ligne), T22j (espace avant le deux-points), T32 à T34, MUT-A (libellé de succès écrit par le cas, la suite n'ayant pas d'okmut)"
      contains: "MUT-A"
    - path: "plugin/_internal/vibeflow-update.sh"
      provides: "relais des lignes WARNING/ERROR de l'injecteur ; textes à deux déclarations"
      contains: "vf-mcp-tools"
    - path: "plugin/_internal/tests/test-vibeflow-update.sh"
      provides: "T55 (serveur nommé absent relayé), T56 (valeur malformée relayée, installation rc 0)"
      contains: "T56"
    - path: "plugin/conductor/scripts/check-agents.sh"
      provides: "valider_mcp_tools, erreur « vf-mcp-tools malformée »"
      contains: "def valider_mcp_tools"
  key_links:
    - from: "inject_lab_mcp_into_agents() de vibeflow-update.sh"
      to: "lignes WARNING/ERROR de inject-mcp-tools.sh"
      via: "stderr capturé et relayé par log, rc de l'installeur inchangé (best-effort)"
      pattern: "WARNING:\\|ERROR:"
    - from: "valider_mcp_tools() de check-agents.sh"
      to: "named_request() de inject-mcp-tools.sh"
      via: "même règle d'extraction (même motif de présence, une seule occurrence, valeur sur la seule ligne de la clé, même prédicat de continuation MCP_CONTINUATION_RE `^[ \\t]`), même ordre trim puis déquotage, même grammaire <serveur>:<outil1>,<outil2>, même charset [A-Za-z0-9_-]+ et même déquotage d'une paire de guillemets (T22e à T22j, T110 à T115)"
      pattern: "\\^\\[A-Za-z0-9_-\\]\\+\\$"
  prohibitions:
    - statement: "MUST NOT fusionner, migrer ou renommer vf-mcp-consumer et vf-mcp-tools : les deux déclarations sont conservées, elles répondent à deux besoins (D-Q3, ADR-051 décision 1)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : se relit sur le diff (aucune clé de frontmatter d'agent modifiée)"
    - statement: "MUST NOT coder en dur un nom de serveur MCP (par exemple un renommage amont constaté) dans l'injecteur ou l'installeur : le geste reste générique, dérivé du lab (ADR-051)"
      status: unresolved
      reason: "sonde de prohibitions spec-less ; le motif réel (XcodeBuildMCP → MobileBuildMCP) sert de justification, jamais de littéral dans le code"
---

<objective>
Durcir le mécanisme MCP conservé (D-Q3) là où il se taisait : un serveur nommé absent de l'union
des scopes est signalé jusqu'au journal d'installation ; une valeur `vf-mcp-tools` malformée est
refusée à l'install ET au gate des agents ; les textes à une seule clé de l'installeur nomment les
deux. L'amendement de la spec fabrique (§1.2 et §7.2, « deux besoins distincts ») et le patch de
dev-orchestrator sont portés par 43-07, qui s'appuie sur le SUMMARY de ce plan.

Purpose: FABR-10 (D-Q3 — Willy, AskUserQuestion, session principale, 2026-09-24 : « garder les deux
déclarations »). Faits mesurés le 2026-09-25 qui orientent ce plan : sur le scénario T16, l'injecteur
imprime déjà un WARNING « WINDOWS #4 » puis une ligne contradictoire qui annonce un no-op sans
signal, rc 0 ; sur T22a, aucune alerte, rc 0 ; et `vibeflow-update.sh` appelle l'injecteur en jetant
stdout ET stderr — le signal n'atteint donc jamais la personne qui installe. Les cas T23 à T31
(union des scopes) sont documentés dans l'en-tête de test-inject-mcp-tools.sh mais n'ont jamais existé
dans son code (vérifié jusqu'au commit d89a60e) : l'union n'a aujourd'hui aucun test.

**Relecture Samuel (polarité dev-orchestrator)** : ce plan modifie `plugin/dev-orchestrator/`
(injecteur et sa suite ; la version passe en 43-07) et le moteur d'installation `plugin/_internal/`
(relais du journal) ; chaque commit concerné est listé au SUMMARY pour sa revue avant merge.
Output: injecteur durci (a, b), relais installeur, grammaire validée au gate, relevé de relecture
Samuel.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-RESEARCH.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-PATTERNS.md

Hors périmètre (43-CONTEXT.md, erratum du 2026-09-25) : l'union projet + global existe déjà dans le
code (inject-mcp-tools.sh l.222-267) ; ce plan la TESTE pour juger (b), il ne la modifie pas. La
restauration des neuf cas T23 à T31 manquants n'est pas faite ici : relevé pour Samuel.
Hors de ce plan (découpage de révision, 2026-09-25) : l'amendement de la spec fabrique §1.2/§7.2 et
le patch de dev-orchestrator (VERSION, module.json, README, CHANGELOG) relèvent de 43-07.
Référence de phase (B43, révision du 2026-09-26) : un SHA FIGÉ, jamais re-dérivé, consigné une
seule fois par 43-01, son seul propriétaire (étape 0 de sa Tâche 1, avant tout commit de la phase) sur la ligne
unique « Base de phase : <sha de 40 caractères> » du fichier neutre
`.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-BASE-PHASE.md` —
jamais dans un PLAN.md, jamais re-dérivé par un `git log` sur les PLAN.md (une révision de plan
pendant l'exécution déplacerait la base et viderait en silence les relevés qui la lisent). Ce plan
la LIT par la chaîne
`B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && …` :
base absente, malformée ou sortie de l'histoire → rien n'est imprimé, et « rien d'imprimé » vaut
échec. Pourquoi pas la base de branche : la branche porte encore la Phase 42 tant que la PR #108
n'est pas mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de
la base de branche — mesuré le 2026-09-25). Rebase : voir 43-01, bloc de contexte (nouvelle base
déterministe — parent du commit de création du fichier — ou arrêt en rouge BASE-PHASE-INTROUVABLE,
jamais une base choisie par l'exécutant). Distincte de la « Base T2 » de 43-05-SUMMARY.md (critère
Gate-Touche local). Les gardes G-1 et G-2 gardent leur propre base : elles jugent toute la branche,
Phase 42 comprise.
</context>

<tasks>

<task type="tracer">
  <name>Tâche 1 : tracer — serveur nommé absent de l'union signalé jusqu'au journal d'installation d'un lab frais (T16 durci, T32, T33, T55, textes c de vibeflow-update.sh)</name>
  <precondition>command -v jq réussit (resolve-deps.sh, fermeture de dev-orchestrator du témoin lab frais)</precondition>
  <files>plugin/dev-orchestrator/scripts/inject-mcp-tools.sh, plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh, plugin/_internal/vibeflow-update.sh, plugin/_internal/tests/test-vibeflow-update.sh</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh (fichier modifié : en-tête l.1-110 dont HONNÊTETÉ et Best-effort ; l.216-220 logline/errline ; l.222-268 union des scopes et servers_arg ; l.373-423 passe WINDOWS #4 et son message de sources ; l.540-560 site d'injection du mode nommé ; l.585-589 fin)
    - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh (fichier modifié : en-tête l.1-60 et catalogue T23 à T31 fantômes ; l.64-90 SCRIPT, WORK, export VF_CLAUDE_JSON ; l.140-160 mk_named ; T16 l.352-359 ; ligne 471 où l'en-tête de T23 est collé au bilan)
    - plugin/_internal/vibeflow-update.sh (fichier modifié : l.32 log() vers stderr ; re-grepper vf-mcp-consumer AVANT d'éditer — sites mesurés le 2026-09-25 à l.1276, l.1308 et l.2423, la troisième ayant dérivé de 10 lignes depuis le cadrage ; fonction inject_lab_mcp_into_agents l.1285-1314)
    - plugin/_internal/tests/test-vibeflow-update.sh (fichier modifié : l.20-45 isolation HOME, ok/ko/skip ; l.66-100 prepare_module et T1 ; T54 l.423-452 pour la forme d'un cas récent)
    - plugin/dev-orchestrator/agents/vf-reviewer.md (l.10 : vf-mcp-tools: XcodeBuildMCP:test_sim,build_sim,clean) ; plugin/dev-orchestrator/agents/vf-coder.md (vf-mcp-consumer: true)
    - docs/ADR.md (ADR-051, l.392-440, § Décision point 1)
  </read_first>
  <behavior>
    - T16 (durci) : `--servers "mobile-mcp"` sur un agent nommé XcodeBuildMCP → rc 0, empreinte inchangée, stderr contient « WARNING: », « XcodeBuildMCP » et « liste --servers explicite », et ne contient pas le mot « silencieux »
    - T32 (NON-RÉGRESSION, vert avant toute écriture de ce plan) : `--mcp-json` projet déclarant mobile-mcp, `VF_CLAUDE_JSON` global déclarant XcodeBuildMCP → tokens mcp__XcodeBuildMCP__test_sim, build_sim, clean injectés, aucune ligne WARNING (union honorée — elle existe déjà, inject-mcp-tools.sh l.245-259 ; le cas la verrouille pendant que (b) réécrit les messages)
    - T33 : projet mobile-mcp, global context7 → rc 0, empreinte inchangée, WARNING nommant « union » et les DEUX chemins de sources
    - T55 (test-vibeflow-update.sh, HOME temporaire) : lab avec `.mcp.json` déclarant mobile-mcp, install de dev-orchestrator sans filtre de sortie → rc 0, journal contenant une ligne WARNING qui cite vf-reviewer.md et XcodeBuildMCP, et `.claude/agents/vf-coder.md` dont tools: porte mcp__mobile-mcp__*
  </behavior>
  <action>
1. Tests d'abord, joués avant l'implémentation : T16 durci, T33 et T55 partent ROUGES ; T32 est un cas de NON-RÉGRESSION, pas un cas rouge — il exerce l'union projet ∪ global déjà présente dans le code (inject-mcp-tools.sh l.245-259) et passe dès avant la première écriture ; on le constate vert, puis on implémente.
2. inject-mcp-tools.sh :
   - passe WINDOWS #4 : le message nomme les sources réellement consultées — « liste --servers explicite » quand `servers_arg` est non vide, sinon « union scope projet <mcp_json> ∪ scope global <claude_json> (ADR-051-B) » ; gradation WARNING / ERROR sous --strict inchangée ;
   - site d'injection du mode nommé quand aucun token n'est résolu : remplacer la ligne de journal actuelle (qui annonce la branche comme un no-op sans signal) par « <base> : rien injecte — serveur <srv> (vf-mcp-tools) absent des sources resolues (voir WARNING ci-dessus, durcissement b, D-Q3) » ; pas de second WARNING (la passe WINDOWS #4 l'a déjà émis pour ce cas exact, mesuré sur T16) ;
   - en-tête : HONNÊTETÉ complétée (le WARNING est relayé par l'installeur depuis la Phase 43) ; aucun nom de serveur en dur nulle part.
3. vibeflow-update.sh, `inject_lab_mcp_into_agents` : conserver l'appel et le best-effort (la fonction rend toujours 0) mais, au lieu de jeter toute la sortie, jeter stdout et capturer stderr dans une variable locale avec le rc ; relayer par `log` chaque ligne capturée qui contient `WARNING:` ou `ERROR:` (préfixe conservé, deux espaces d'indentation) ; garder les deux lignes de journal de succès et d'échec. Textes (durcissement c), après re-grep : le commentaire d'en-tête de la section (vers l.1274-1278), la ligne de journal de succès (vers l.1308) et le commentaire du site d'appel (vers l.2422-2424) nomment les deux déclarations — `vf-mcp-consumer` (allowlist large, agents exécutants) et `vf-mcp-tools` (allowlist nommée, relecteur) — et les sources réelles (union scope projet ./.mcp.json et scope global ~/.claude.json, ADR-051-B) au lieu du seul ./.mcp.json.
4. test-inject-mcp-tools.sh : T16 réécrit (stderr capturé, assertions du behavior, libellé qui décrit désormais un signal et non une absence de signal) ; T32 et T33 ajoutés avec `VF_CLAUDE_JSON` en préfixe de LEUR commande seulement (l'export par défaut de la suite reste intact) ; catalogue d'en-tête : T32 et T33 ajoutés, et une ligne « T23 à T31 : documentés en Phase 21, jamais présents dans le code de cette suite (constat Phase 43, 2026-09-25) — relevé pour Samuel, non restaurés ici » ; la ligne 471 où l'en-tête de T23 est collé au bilan est séparée pour que le bilan soit une ligne propre.
5. test-vibeflow-update.sh : T55 avec LAB, CACHE et FAKE_HOME en mktemp (jamais le HOME réel), prepare_module de dev-orchestrator, `.mcp.json` du lab écrit avant l'install, sortie capturée stdout+stderr ; catalogue d'en-tête mis à jour.
6. Commit : `fix(dev-orchestrator): serveur nommé absent signalé jusqu'au journal d'installation (FABR-10 b, c)` — hors surface G-2 (ni check-*.sh de conductor ni ses suites) ; message citant D-Q3 (Willy, AskUserQuestion, session principale, 2026-09-24).
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh 2>&1 | grep -E 'T16|T32|T33|✗|Bilan'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T16, T32, T33 n'a aucune ligne « ✓ », ou la ligne « Bilan » ne se termine pas par « 0 KO »</fails_when>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh 2>&1 | grep -E '^== résultat|T55|✗'</automated>
    <fails_when>une ligne contient « ✗ », ou aucune ligne « ✓ » ne porte T55, ou la ligne « == résultat » ne contient pas « / 0 KO / »</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q && printf '{"mcpServers":{"mobile-mcp":{"command":"x"}}}\n' > .mcp.json); journal="$LAB/journal.txt"; : > "$journal"; bash "$R/plugin/_internal/resolve-deps.sh" dev-orchestrator | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >> "$journal" 2>&1); done; grep 'WARNING:' "$journal" | grep 'vf-reviewer.md' | grep -q 'XcodeBuildMCP' && grep -q 'mcp__mobile-mcp__\*' "$LAB/.claude/agents/vf-coder.md" && echo TRACER-MCP-OK</automated>
    <fails_when>la ligne TRACER-MCP-OK est absente (WARNING non relayé par l'installeur, ou injection large de mobile-mcp perdue sur vf-coder)</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: no-op silencieux -->
    - `grep -c 'no-op silencieux' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` rend 0
    - `grep -c 'vf-mcp-tools' plugin/_internal/vibeflow-update.sh` rend au moins 3 et chacun des trois sites re-greppés nomme les deux clés
    - `awk '/^inject_lab_mcp_into_agents\(\) \{/,/^}/' plugin/_internal/vibeflow-update.sh | grep -cE 'WARNING:|ERROR:'` rend au moins 1
    - les suites test-inject-mcp-tools.sh et test-vibeflow-update.sh finissent à 0 KO avec T16, T32, T33, T55 verts ; le témoin lab frais imprime TRACER-MCP-OK
  </acceptance_criteria>
  <done>Le serveur nommé absent de l'union est dit, jugé contre l'union et relayé jusqu'au journal d'installation, sans casser l'installation ni l'injection large.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : valeur vf-mcp-tools malformée refusée à l'install et au gate des agents, même verdict des deux côtés pour toute valeur (T22a-T22j, T34, MUT-A, T56, T108 à T115, MUT-M1, MUT-M1-REFUS-COMPTE)</name>
  <files>plugin/dev-orchestrator/scripts/inject-mcp-tools.sh, plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh, plugin/_internal/tests/test-vibeflow-update.sh, plugin/conductor/scripts/check-agents.sh, plugin/conductor/scripts/tests/test-check-agents.sh</files>
  <read_first>
    - plugin/dev-orchestrator/scripts/inject-mcp-tools.sh (fichier modifié : frontmatter_block l.275-283 ; TOKEN_SEGMENT_RE l.293 et NAMED_FLAG_RE l.294 — `\s*` sous re.M traverse le saut de ligne ; has_named l.296-303 ; named_request l.305-329 ; site d'injection l.540-560, fin l.585-589, en-tête Best-effort l.87-94)
    - plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh (fichier modifié : l.66-68 compteurs et `ok()`/`ko()` — aucune fonction okmut ni helper de mutation, d'où les libellés de MUT-A écrits en toutes lettres par le cas ; T22 l.430-468)
    - plugin/_internal/tests/test-vibeflow-update.sh (fichier modifié : T54 et T55)
    - plugin/conductor/scripts/check-agents.sh (fichier modifié : VIBEFLOW_FIELDS l.277-285 ; parse_frontmatter l.456-492 — dernière occurrence d'une clé retenue, lignes indentées repliées dans le scalaire ; frontmatter_lines l.494-503, lignes BRUTES ; _KEY_RE l.505 ; en-tête BLOQUANT l.70-76 et I7 l.126-128 ; check_file l.946+ ; lint des allowlists pour la forme d'un refus de syntaxe)
    - plugin/conductor/scripts/tests/test-check-agents.sh (fichier modifié : fixture conforme de base ; okmut/komut l.250-260 ; make_gate_mutant l.338-378, qui imprime le chemin du mutant sur stdout et compte un refus par komut ; appel de MUT-I1 l.2574-2597, forme à NE PAS reprendre pour l'appel du helper ; T107 posé en 43-01)
  </read_first>
  <behavior>
    - T22a, T22b (durcis) : `XcodeBuildMCP-sans-separateur` et `XcodeBuildMCP:` → rc 1, stderr contient « ERROR: » et « malform », empreinte inchangée, la valeur brute n'est pas recopiée dans le message
    - T22c : `XcodeBuildMCP:test sim` (segment hors charset) → rc 1 ; T22d : `:test_sim` (serveur vide) → rc 1
    - T34 : dossier avec un agent nommé valide et un agent malformé, `--servers XcodeBuildMCP` → le valide reçoit ses tokens, le malformé reste identique, rc 1
    - T22e (parité des guillemets, injecteur) : `vf-mcp-tools: "XcodeBuildMCP:test_sim,build_sim"` et `vf-mcp-tools: 'XcodeBuildMCP:test_sim'`, `--servers XcodeBuildMCP` → rc 0, tokens mcp__XcodeBuildMCP__test_sim (et build_sim) injectés, aucune ligne ERROR ; `vf-mcp-tools: "XcodeBuildMCP:test sim"` (entre guillemets ET hors charset) → rc 1, ERROR « malform », empreinte inchangée
    - T22f (valeur lue sur la seule ligne de la clé, injecteur) : `vf-mcp-tools:` sans valeur suivi de la ligne `model: sonnet`, puis `vf-mcp-tools: XcodeBuildMCP:` suivi d'une ligne indentée `  test_sim` (continuation), `--servers XcodeBuildMCP` → rc 1, ERROR « malform », empreinte inchangée pour chacune — jamais lue comme une demande nommée du serveur `model` (mesuré le 2026-09-26 : NAMED_FLAG_RE, dont le `\s*` traverse le saut de ligne sous re.M, lit aujourd'hui `model: sonnet` comme valeur ; le gate lit une valeur vide). Jumeaux du prédicat de continuation (révision du 2026-09-26, tour 5) : la fixture `XcodeBuildMCP:` + `  test_sim` ci-dessus est déjà refusée par la grammaire seule (aucun outil), elle ne teste pas la règle (iii) ; deux jumeaux dont la ligne de la clé porte une valeur VALIDE, `vf-mcp-tools: XcodeBuildMCP:test_sim`, isolent cette règle — suivie de ` build_sim` indentée d'UNE espace (étiquette « T22f 1-espace »), puis de `build_sim` précédée d'UNE tabulation, fixture écrite avec printf (étiquette « T22f tabulation ») —, `--servers XcodeBuildMCP` → rc 1, ERROR « malform », empreinte inchangée pour chacun
    - T22g (clé en double, injecteur) : `vf-mcp-tools: XcodeBuildMCP:test_sim` puis `vf-mcp-tools: XcodeBuildMCP:` dans le même frontmatter, et l'ordre inverse, `--servers XcodeBuildMCP` → rc 1, ERROR « malform », empreinte inchangée pour chacun des deux ordres (mesuré le 2026-09-26 : l'injecteur retient la PREMIÈRE occurrence, parse_frontmatter du gate la DERNIÈRE — verdicts opposés selon l'ordre)
    - T22h (ordre trim puis déquotage, injecteur) : `vf-mcp-tools: "XcodeBuildMCP:test_sim"` suivi, en fin de ligne, d'une espace (fixture 1), d'une tabulation (fixture 2), d'un CR — ligne de la clé terminée par CR LF, écrite avec printf, les autres lignes en LF (fixture 3) —, `--servers XcodeBuildMCP` → rc 0, token mcp__XcodeBuildMCP__test_sim injecté, aucune ligne ERROR, pour chacune des trois (un déquotage AVANT le trim refuserait les fixtures 1 et 2 : le guillemet final n'y est pas le dernier caractère ; la fixture 3 verrouille le verdict même si les deux côtés, qui lisent en mode texte où Python normalise CR LF, changent un jour de mode de lecture)
    - T22i (clé en DERNIÈRE ligne du frontmatter, injecteur) : frontmatter dont la dernière ligne avant le `---` fermant est `vf-mcp-tools:` sans valeur — aucune ligne de frontmatter ne la suit — → rc 1, ERROR « malform », empreinte inchangée, aucune ligne « Traceback » (pas d'erreur d'indice sur la ligne suivante absente) ; jumeau de non-régression à la même position, `vf-mcp-tools: XcodeBuildMCP:test_sim` → rc 0, token injecté
    - T22j (espace avant le deux-points, injecteur) : `vf-mcp-tools : XcodeBuildMCP:test_sim`, `--servers XcodeBuildMCP` → clé reconnue PRÉSENTE (motif de présence de la règle (i)) et refusée : rc 1, ERROR « malform », empreinte inchangée (relevé à la lecture du code le 2026-09-26 : NAMED_FLAG_RE de l'injecteur et _KEY_RE du gate exigent tous deux le deux-points collé à la clé et ignorent cette ligne EN SILENCE — une allowlist nommée écrite ainsi n'accordait rien, sans aucun signal)
    - MUT-A : la ligne unique qui passe malformed_found à vrai réécrite pour le laisser faux → fixture T22a : rc_original=1, rc_mutant=0 (cmp prouve la mutation, bash -n réussit). test-inject-mcp-tools.sh n'a que `ok()` et `ko()` (l.67-68), aucune fonction okmut ni helper de mutation : le cas écrit lui-même, en toutes lettres, ses trois libellés — succès `ok "MUT-A TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1"`, qui imprime exactement `  ✓ MUT-A TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` ; échec `ko "MUT-A NON TUE : rc_mutant=<rc> attendu 0, rc_original=<rc> attendu 1"` ; mutant non opposable (motif absent ou non unique, cmp sans différence, bash -n en échec) `ko "MUT-A REFUSE : <raison>"` — tous appelés dans le shell de la suite, jamais dans une substitution de commande (un KO y serait perdu)
    - T56 (test-vibeflow-update.sh, HOME temporaire) : module synthétique dont un agent porte une vf-mcp-tools malformée, cache contenant dev-orchestrator/scripts/inject-mcp-tools.sh, lab avec un .mcp.json → install rc 0 (best-effort), journal contenant une ligne ERROR « malform », agent inchangé
    - T108 (test-check-agents.sh) : fixture conforme portant `vf-mcp-tools: XcodeBuildMCP:test_sim,build_sim,clean` et `vf-requires: mcp-servers` → rc 0 ; le vrai vf-reviewer.md copié → rc 0
    - T109 : les quatre formes malformées de T22a-T22d sur la même fixture → rc 1 chacune, « vf-mcp-tools malformee »
    - T110 (parité des guillemets, gate) : les trois valeurs de T22e sur la fixture de T108 → rc 0, rc 0, puis rc 1 « vf-mcp-tools malformee » — sur chaque valeur de T22e, le gate et l'injecteur rendent le MÊME verdict (acceptée des deux côtés, ou refusée des deux côtés)
    - T111 (valeur lue sur la seule ligne de la clé, gate) : les deux fixtures de T22f sur la fixture de T108 → rc 1 « vf-mcp-tools malformee » pour chacune — même verdict que T22f (aujourd'hui la continuation indentée est repliée par parse_frontmatter en `XcodeBuildMCP: test_sim`, valide pour le gate et malformée pour l'injecteur) ; puis les deux jumeaux de T22f, mêmes frontmatters octet pour octet (« T111 1-espace », « T111 tabulation ») → rc 1 « vf-mcp-tools malformee » pour chacun — même verdict que « T22f 1-espace » et « T22f tabulation » (le repli de parse_frontmatter, `startswith("  ")` à deux espaces exactement, l.489, laisserait passer ces deux continuations : ce n'est pas le prédicat de la règle (iii))
    - T112 (clé en double, gate) : les deux ordres de T22g → rc 1 « vf-mcp-tools malformee » pour chacun — même verdict que T22g
    - T113 (ordre trim puis déquotage, gate) : les trois fixtures de T22h sur la fixture de T108 → rc 0 chacune — même verdict que T22h
    - T114 (clé en dernière ligne, gate) : les deux fixtures de T22i sur la fixture de T108 → rc 1 « vf-mcp-tools malformee » pour la clé vide, rc 0 pour le jumeau valide, aucune ligne « Traceback » — même verdict que T22i
    - T115 (espace avant le deux-points, gate) : la fixture de T22j sur la fixture de T108 → rc 1 « vf-mcp-tools malformee » — même verdict que T22j (aujourd'hui le gate ignore la ligne et rend 0)
    - MUT-M1 : ligne `errors.extend(valider_mcp_tools(` remplacée par `pass` → fixture T109 (sans séparateur) : rc_original=1, rc_mutant=0 ; helper `make_gate_mutant` appelé DIRECTEMENT, jamais en substitution de commande (voir action, étape 5)
    - MUT-M1-REFUS-COMPTE (garde du harnais, même patron que MUT-REFUS-COMPTE de 43-01) : dans un sous-shell explicite dont la sortie va dans un fichier du dossier de travail, `make_gate_mutant` est appelé DIRECTEMENT, sortie redirigée vers un second fichier — exactement la forme d'appel de MUT-M1 — avec un motif absent de check-agents.sh et l'identifiant REFUS-M1, puis le sous-shell imprime `DELTA-FAIL=<fail après l'appel − fail avant>` et `RC-HELPER=<rc>` → le cas exige `DELTA-FAIL=1`, `RC-HELPER=1` et la ligne « MUT-REFUS-M1 NON TUE » dans le fichier de sortie du helper : un refus du helper appelé comme MUT-M1 est compté KO dans le shell qui l'appelle ; ligne `✓ MUT-M1-REFUS-COMPTE : refus du helper compte KO dans le shell appelant (appel direct, sortie redirigee)` (fichiers jamais réimprimés ; l'incrément du sous-shell, volontairement perdu, n'entre pas dans le décompte de la suite)
  </behavior>
  <action>
1. Avant le premier commit de cette tâche, écrire sa base (`git rev-parse HEAD`) sur une ligne dédiée « Base T2 : <sha> » de 43-05-SUMMARY.md, créé dès maintenant (ligne hors du frontmatter, en début de ligne, conservée quand le SUMMARY est complété) : le critère Gate-Touche du premier commit de la surface G-2 la relit — la Tâche 1 ne touche aucun chemin de cette surface, le premier commit du plan sur la surface est donc dans cette tâche. Tests d'abord, joués avant l'implémentation : T22a-T22g, T22h (l'injecteur ne déquote pas encore), T22i (clé vide), T22j, T34, MUT-A, T56, T109, la valeur malformée entre guillemets de T110, T111 (continuation indentée comprise), T112, T114 (clé vide), T115 et MUT-M1 partent ROUGES ; T108, les deux valeurs valides entre guillemets de T110, T113 et les jumeaux valides de T22i et T114 sont des cas de NON-RÉGRESSION (check-agents.sh admet déjà vf-mcp-tools et trimme puis déquote les scalaires, l.469-481 ; l'injecteur lit déjà une valeur valide en dernière ligne) et passent dès avant la première écriture ; MUT-M1-REFUS-COMPTE, garde du harnais, exerce le helper existant et passe d'emblée.
2. Règle d'extraction commune, écrite UNE fois ici et appliquée à l'identique par l'injecteur (étape 3) et par le gate (étape 4) — décision de planification (parité R2, révision du 2026-09-26 ; ordre et présence fixés au tour 3) : (i) PRÉSENCE — toute ligne du frontmatter, non indentée, qui commence par `vf-mcp-tools` suivi de blancs horizontaux facultatifs puis d'un deux-points (motif `^vf-mcp-tools[ \t]*:`) est une occurrence de la clé ; au plus UNE occurrence, deux ou plus → malformée, quel que soit l'ordre ou la validité de chacune ; une occurrence dont le deux-points n'est pas collé à la clé (`vf-mcp-tools :`, YAML valide) → malformée des deux côtés, jamais ignorée en silence (T22j/T115) ; (ii) la valeur est le reste de la SEULE ligne de la clé, après `vf-mcp-tools:` — jamais la ligne suivante, y compris quand la clé est la DERNIÈRE ligne du frontmatter (aucune ligne ne suit avant le `---` fermant : la valeur est alors ce reste, éventuellement vide, sans lecture hors du frontmatter ni erreur d'indice — T22i/T114) ; (iii) une ligne indentée qui suit immédiatement la ligne de la clé, si elle existe (continuation de scalaire ou puce de liste) → malformée. « Indentée » a UNE seule définition, écrite ici et appliquée à l'identique des deux côtés (révision du 2026-09-26, tour 5) : le PREMIER caractère de la ligne brute est une espace ou une tabulation — quel que soit le nombre de blancs et le reste de la ligne ; une ligne faite seulement de blancs compte aussi, une ligne vide non —, soit l'expression `^[ \t]` appliquée par `re.match` à la ligne brute, portée de chaque côté par une constante de même nom et de même littéral, `MCP_CONTINUATION_RE = re.compile(r"^[ \t]")` (dans check-agents.sh, dont le Python vit entre guillemets doubles bash, les guillemets du littéral sont échappés comme le reste du fichier ; le texte de l'expression, `^[ \t]`, est le même). Ce n'est PAS le repli de parse_frontmatter du gate (`startswith("  ")`, deux espaces exactement, l.489 ; puces `^\s+-\s+`, l.484), qu'aucun des deux côtés ne réutilise : une continuation à une espace ou à une tabulation y échapperait d'un côté et pas de l'autre, sans qu'un cas à deux espaces le révèle (jumeaux « T22f 1-espace », « T22f tabulation », « T111 1-espace », « T111 tabulation ») ; (iv) ORDRE UNIQUE, fixé ici une seule fois : d'abord le TRIM — `str.strip()` de Python, qui retire espaces, tabulations, CR et tout blanc de tête et de fin —, PUIS le retrait d'UNE paire de guillemets englobante (`"` ou `'`, identiques aux deux extrémités) ; jamais l'inverse. C'est l'ordre RÉEL des deux codes existants, relu le 2026-09-26 : l'injecteur trimme d'emblée (`raw = m.group(1).strip()`, inject-mcp-tools.sh l.317 — le déquotage ajouté à l'étape 3 vient après) et parse_frontmatter du gate trimme puis déquote (`val = m.group(2).strip()` l.469, déquotage l.480-481). Déquoter avant de trimmer laisserait `"X:a"` suivi d'une espace ou d'une tabulation avec un guillemet final qui n'est plus le dernier caractère, donc non retiré puis refusé par la grammaire — un côté refuserait ce que l'autre accepte (T22h/T113) ; (v) puis la grammaire (partition sur le premier deux-points ; le serveur et chaque outil sont encore strippés après la partition, comme dans named_request). Pour TOUTE valeur, les deux côtés rendent donc le même verdict : T22e/T110, T22f/T111, T22g/T112, T22h/T113, T22i/T114 et T22j/T115 le prouvent chacun des deux côtés.
3. inject-mcp-tools.sh : dans NAMED_FLAG_RE, remplacer `\s*` par `[ \t]*` (sous re.M, `\s` inclut le saut de ligne : une clé vide suivie d'une autre ligne lit aujourd'hui cette ligne comme valeur) ; ajouter un motif de PRÉSENCE distinct, `^vf-mcp-tools[ \t]*:` sous re.M (règle (i)), sur lequel has_named teste la présence et named_request compte les occurrences ; dans named_request, rendre None si la clé figure plus d'une fois, si l'unique occurrence ne commence pas exactement par `vf-mcp-tools:` (deux-points non collé), ou si la ligne qui suit la ligne de la clé EXISTE dans le frontmatter et est indentée au sens unique de la règle (iii) — `MCP_CONTINUATION_RE.match(ligne)`, constante déclarée à côté de NAMED_FLAG_RE avec le littéral `^[ \t]` — (clé en dernière ligne : aucun accès hors du frontmatter) ; has_named reste un test de PRÉSENCE (une clé présente mais malformée est traitée comme malformée, jamais ignorée) ; puis, sur la valeur déjà TRIMMÉE (`raw`, après son `.strip()` existant — ordre (iv) de la règle), retirer UNE paire de guillemets englobante (`"` ou `'`, identiques aux deux extrémités) avant d'appliquer la grammaire — même règle que parse_frontmatter de check-agents.sh (l.480-481) et dequote_scalar (l.580-587). Décision de planification (parité W5) : une valeur YAML valide entre guillemets est acceptée des deux côtés, jamais acceptée par le gate et refusée par l'injecteur ; le déquotage vaut aussi en --verify, qui appelle la même fonction. Puis initialiser `malformed_found` à faux avant la boucle d'injection ; au site du mode nommé où named_request rend None, émettre par errline « <base> : vf-mcp-tools malformee — valeur refusee (attendu <serveur>:<outil1>,<outil2>,… ; segments [A-Za-z0-9_-]+) — fichier non modifie (durcissement a, D-Q3) » sans jamais recopier la valeur, passer `malformed_found` à vrai sur une ligne unique (cible MUT-A), puis passer au fichier suivant ; après la ligne de journal « termine », si `malformed_found` → une ligne errline récapitulative et sortie rc 1 (avant le test --strict existant, inchangé). Le mode --verify n'est pas modifié (une valeur malformée y reste exclue de la comparaison, verdict INDÉTERMINÉ si rien d'autre n'est jugeable) — documenté dans l'en-tête. Docstring de named_request et paragraphe Best-effort de l'en-tête mis à jour (une valeur malformée sort désormais en rc 1 en mode injection ; règle d'extraction de l'étape 2).
4. check-agents.sh : `valider_mcp_tools(base, lignes)` rend une LISTE et lit les lignes BRUTES du frontmatter (frontmatter_lines), jamais fm[] — parse_frontmatter retient la DERNIÈRE occurrence d'une clé et replie une ligne indentée dans le scalaire, précisément les deux écarts avec l'injecteur ; clé présente (au moins une ligne qui correspond au motif de présence `^vf-mcp-tools[ \t]*:` de la règle (i), le même que l'injecteur) et valeur non conforme à la règle d'extraction de l'étape 2 puis à la grammaire de named_request, appliquée pas à pas comme dans l'injecteur et dans le même ordre (plus d'une occurrence → malformée ; occurrence sans deux-points collé à la clé → malformée ; ligne qui suit, si elle existe dans le frontmatter et que `MCP_CONTINUATION_RE.match(ligne)` réussit — même constante, même littéral `^[ \t]` que l'injecteur, déclarée à côté de _KEY_RE, jamais le `startswith("  ")` de parse_frontmatter — → malformée ; reste de la ligne de la clé ; TRIM par `str.strip()` PUIS retrait d'une paire de guillemets englobante — ordre (iv), celui de parse_frontmatter l.469-481, jamais déquotage puis trim ; au moins un deux-points ; partition sur le PREMIER deux-points ; serveur strippé non vide ; outils découpés sur la virgule, chaque segment strippé, segments vides écartés, au moins un outil restant ; chaque segment conforme à `^[A-Za-z0-9_-]+$`, le même littéral que TOKEN_SEGMENT_RE de l'injecteur ; une liste YAML est malformée) → « {base} : vf-mcp-tools malformee — attendu <serveur>:<outil1>,<outil2>,… (segments [A-Za-z0-9_-]+) ; la valeur serait refusee a l'install (FABR-10 a) » ; appel unique `errors.extend(valider_mcp_tools(` dans check_file, bloquant dans tous les modes ; en-tête : l'entrée ajoutée à la liste BLOQUANT. Aucun autre comportement du gate ne change.
5. Tests : fixtures de test-check-agents.sh conformes par ailleurs (model, memory, effort, `vf-requires: mcp-servers` pour ne pas déclencher I7) ; T56 avec des chemins mktemp et HOME temporaire ; les fixtures de T22f à T22j et de T111 à T115 sont les MÊMES frontmatters des deux côtés. MUT-M1 n'appelle JAMAIS le helper dans une substitution de commande (même remède que I1 au tour 2 dans test-check-skills.sh ; la substitution, patron des 16 appels de la Phase 42, exécute le helper dans un sous-shell où l'incrément de `fail` de son `komut` se perd — constat du 2026-09-26, l.2574-2597 pour MUT-I1) : appel DIRECT, stdout redirigé vers un fichier du dossier de travail — `make_gate_mutant M1 0 'errors.extend(valider_mcp_tools(' 'pass' > "$WORK/mut-M1.chemin"`, puis son rc lu aussitôt ; une redirection sur l'appel d'une fonction ne crée pas de sous-shell, donc un refus est compté KO dans le shell de la suite par le `komut` du helper lui-même, sans aucune branche else de comptage. Si le rc vaut 0, le chemin du dossier mutant est relu dans ce fichier (lecture d'un fichier, jamais substitution du helper) et le cas se déroule comme MUT-I1 (okmut/komut) ; sinon le fichier est seulement réimprimé, pour que la ligne de refus reste visible — le KO est déjà compté. Le helper n'est pas modifié. MUT-M1-REFUS-COMPTE prouve le comptage d'un refus sous cette forme d'appel exacte (behavior). Les 16 appels existants de la Phase 42 ne sont pas modifiés ici (résidu signalé en 43-06).
6. Rejouer les trois suites, puis le gate des agents sur le corpus réel en mode par défaut de fraîcheur (lenient — aucun verdict ne dépend de la date du jour).
7. Commits : `fix(dev-orchestrator): valeur vf-mcp-tools malformée refusée à l'install (FABR-10 a)` (injecteur, sa suite, T56) — hors surface G-2 ; puis `feat(conductor): grammaire vf-mcp-tools validée par le gate des agents (FABR-10 a)` avec
   `Gate-Touche: plugin/conductor/scripts/check-agents.sh — grammaire vf-mcp-tools validée (durcissement a, D-Q3)` et
   `Gate-Touche: plugin/conductor/scripts/tests/test-check-agents.sh — T108 à T115, mutant MUT-M1 en appel direct, garde MUT-M1-REFUS-COMPTE`.
   Si un commit de cette tâche touche check-agents.sh ou sa suite AVANT ce commit (par exemple des tests rouges commités à part), il porte lui-même un trailer Gate-Touche par chemin de la surface qu'il touche : le critère vise le PREMIER commit de la surface, pas le dernier.
8. DERNIÈRE étape de la tâche, APRÈS son dernier commit (étape 7) — jamais avant (révision du 2026-09-26, tour 5) : écrire dans 43-05-SUMMARY.md la section « Relecture Samuel (polarité dev-orchestrator) » à partir de la sortie de la commande de journal prescrite à l'`<output>`, jouée à cet instant, quand tous les commits de la plage B43..HEAD qui touchent plugin/dev-orchestrator ou plugin/_internal existent déjà ; hashes écrits sous le titre même, aucun sous-titre dans la section, puis les changements de comportement et le constat T23 à T31 (voir `<output>`). Écrite avant un commit de la tâche, la section omettrait ce commit et SAMUEL-PLAGE rougirait. Aucun commit de code ne suit cette étape dans ce plan : le commit `.planning/` du SUMMARY, séparé, ne touche ni plugin/dev-orchestrator ni plugin/_internal et sort du filtre de la plage.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh 2>&1 | grep -E 'T22[a-j]|T34|MUT-A|✗|Bilan'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T22a, T22b, T22c, T22d, T22e, T22f, T22g, T22h, T22i, T22j, T34, « T22f 1-espace », « T22f tabulation » n'a aucune ligne « ✓ », ou la ligne exacte `  ✓ MUT-A TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` manque (libellé écrit par le cas lui-même, la suite n'ayant ni okmut ni helper de mutation), ou une ligne « MUT-A REFUSE » ou « MUT-A NON TUE » apparaît, ou la ligne « Bilan » ne se termine pas par « 0 KO »</fails_when>
    <automated>bash plugin/_internal/tests/test-vibeflow-update.sh 2>&1 | grep -E '^== résultat|T5[56]|✗'</automated>
    <fails_when>une ligne contient « ✗ », ou T55 ou T56 n'a aucune ligne « ✓ », ou la ligne « == résultat » ne contient pas « / 0 KO / »</fails_when>
    <automated>bash plugin/conductor/scripts/tests/test-check-agents.sh 2>&1 | grep -E '✓ T1(0[789]|1[0-5])|MUT-M1|✗|== Résultat'</automated>
    <fails_when>une ligne contient « ✗ », ou l'une des étiquettes T107, T108, T109, T110, T111, T112, T113, T114, T115, « T111 1-espace », « T111 tabulation » n'a aucune ligne « ✓ », ou « ✓ MUT-M1 TUE » manque, ou la ligne `  ✓ MUT-M1-REFUS-COMPTE : refus du helper compte KO dans le shell appelant (appel direct, sortie redirigee)` manque, ou la ligne « == Résultat » ne finit pas par « 0 KO == »</fails_when>
    <automated>F=plugin/conductor/scripts/tests/test-check-agents.sh; d=$(grep -v '^[[:space:]]*#' "$F" | grep -cE '^[[:space:]]*make_gate_mutant M1 .*>'); s=$(grep -v '^[[:space:]]*#' "$F" | grep -F 'make_gate_mutant M1 ' | grep -c '\$('); echo "M1-APPEL direct=$d substitution=$s"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « M1-APPEL direct=1 substitution=0 » : l'appel du helper pour MUT-M1 doit être une commande directe à sortie redirigée (en tête de ligne, jamais dans `$(`), pour qu'un refus soit compté KO dans le shell de la suite — S6 du tour 3, qui remplace la branche else de comptage que rien ne vérifiait. Le comptage lui-même est prouvé à l'exécution par la garde MUT-M1-REFUS-COMPTE (verify précédent).</fails_when>
    <automated>DEBUT=$(sed -n 's/^Base T2 : \([0-9a-f]\{7,40\}\).*$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-05-SUMMARY.md | head -1); test -n "$DEBUT" || echo "BASE-ABSENTE"; revs=$(git rev-list --reverse "$DEBUT"..HEAD -- plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/tests/test-check-agents.sh) || echo "GIT-ECHEC rev-list"; first=$(printf '%s\n' "$revs" | head -1); test -n "$first" || echo "AUCUN-COMMIT-G2"; chemins=$(git diff-tree --no-commit-id --name-only -r "$first" -- plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/tests/test-check-agents.sh) || echo "GIT-ECHEC diff-tree"; msg=$(git show -s --format=%B "$first") || echo "GIT-ECHEC show"; for p in plugin/conductor/scripts/check-agents.sh plugin/conductor/scripts/tests/test-check-agents.sh; do printf '%s\n' "$chemins" | grep -qxF "$p" || continue; printf '%s\n' "$msg" | grep -qE "^Gate-Touche: $p (—|-) " || echo "NON-COUVERT $p"; done; echo "PREMIER-COMMIT-G2 $first"</automated>
    <fails_when>une ligne BASE-ABSENTE, GIT-ECHEC, AUCUN-COMMIT-G2 ou NON-COUVERT apparaît avant « PREMIER-COMMIT-G2 <sha> » : base de la tâche non consignée, ou le PREMIER commit du plan qui touche check-agents.sh ou sa suite (surface G-2) ne porte pas, dans SON message, un trailer Gate-Touche par chemin de la surface qu'il touche. Le commit injecteur de cette tâche (hors surface) n'entre pas dans le filtre de chemins. G-2 (43-06) juge la branche entière et ne voit pas ce manquement local.</fails_when>
    <automated>fail=0; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d" >/dev/null 2>&1 || fail=$((fail+1)); done; for f in plugin/*/AGENT.md; do bash plugin/conductor/scripts/check-agents.sh --strict --file "$f" >/dev/null 2>&1 || fail=$((fail+1)); done; echo "CORPUS-AGENTS fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « CORPUS-AGENTS fail=0 »</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'malformed_found' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` rend au moins 3 (initialisation, affectation, test final)
    - `grep -c 'errors.extend(valider_mcp_tools(' plugin/conductor/scripts/check-agents.sh` rend 1 et `grep -cF '^[A-Za-z0-9_-]+$' plugin/conductor/scripts/check-agents.sh` rend au moins 1 (même littéral que TOKEN_SEGMENT_RE)
    - les trois suites finissent à 0 KO avec MUT-A et MUT-M1 tués — test-inject-mcp-tools.sh imprime exactement `  ✓ MUT-A TUE : rc_mutant=0 attendu 0, rc_original=1 attendu 1` — ; `CORPUS-AGENTS fail=0`
    - parité gate ↔ injecteur pour TOUTE valeur, prouvée des deux côtés : valeurs entre guillemets (T22e et T110 verts, même verdict pour chacune des trois valeurs : deux acceptées, une refusée) ; valeur lue sur la seule ligne de la clé (T22f et T111 verts : clé vide suivie de `model: sonnet`, et continuation indentée, refusées des deux côtés ; jumeaux « T22f 1-espace », « T22f tabulation », « T111 1-espace », « T111 tabulation » verts : une valeur valide suivie d'une continuation à une espace ou à une tabulation refusée des deux côtés) ; clé en double (T22g et T112 verts : refusée des deux côtés dans les deux ordres) ; ordre trim puis déquotage (T22h et T113 verts : `"XcodeBuildMCP:test_sim"` suivi d'une espace, d'une tabulation ou d'un CR accepté des deux côtés) ; clé en dernière ligne du frontmatter (T22i et T114 verts : vide refusée, valide acceptée, sans Traceback des deux côtés) ; espace avant le deux-points (T22j et T115 verts : `vf-mcp-tools :` reconnue et refusée des deux côtés)
    - l'ordre unique de la règle (iv) est écrit une seule fois dans la règle commune (étape 2) et les deux côtés l'appliquent : `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh | grep -cF 'vf-mcp-tools[ \t]*:'` rend au moins 1 et `grep -cF 'vf-mcp-tools[ \t]*:' plugin/conductor/scripts/check-agents.sh` rend au moins 1 (même motif de présence des deux côtés) ; le comportement est prouvé par T22h/T113, jamais par ce grep
    - prédicat de continuation unique de la règle (iii), même littéral des deux côtés : `grep -F 'MCP_CONTINUATION_RE = re.compile(' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh | grep -cF '^[ \t]'` rend 1, la même commande sur plugin/conductor/scripts/check-agents.sh rend 1, et `grep -cF 'MCP_CONTINUATION_RE.match(' <fichier>` rend au moins 1 pour chacun des deux fichiers ; le verdict est prouvé par les jumeaux « T22f 1-espace », « T22f tabulation », « T111 1-espace » et « T111 tabulation », jamais par ce grep
    - `grep -F 'NAMED_FLAG_RE = ' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh | grep -cF '[ \t]*'` rend 1 et `grep -v '^[[:space:]]*#' plugin/dev-orchestrator/scripts/inject-mcp-tools.sh | grep -cF 'vf-mcp-tools:\s*'` rend 0 (NAMED_FLAG_RE ne traverse plus le saut de ligne ; mesuré le 2026-09-26 avant écriture : 0 et 1, le critère rougit)
    - l'appel du helper pour MUT-M1 est DIRECT, sortie redirigée vers un fichier (commande `M1-APPEL` du verify : `M1-APPEL direct=1 substitution=0`), et la garde MUT-M1-REFUS-COMPTE prouve à l'exécution qu'un refus du helper appelé sous cette forme est compté KO dans le shell de la suite (`DELTA-FAIL=1`, `RC-HELPER=1`) : un mutant MUT-M1 refusé est un KO visible du décompte final, jamais perdu dans un sous-shell — aucune branche else de comptage n'existe plus
    - le premier commit du plan sur la surface G-2 couvre chaque chemin qu'il touche : la commande Gate-Touche du verify imprime `PREMIER-COMMIT-G2 <sha>` sans aucune ligne NON-COUVERT, GIT-ECHEC, AUCUN-COMMIT-G2 ni BASE-ABSENTE
    - 43-05-SUMMARY.md contient la section « Relecture Samuel (polarité dev-orchestrator) » avec au moins deux hashes (le commit de la Tâche 1 et le premier commit de la Tâche 2, sortie de la commande de journal prescrite à l'`<output>`), le changement de comportement de l'installeur (relais WARNING/ERROR) et de l'injecteur (rc 1 sur valeur malformée, déquotage d'une paire de guillemets), et le constat T23 à T31 — même patron de critère que 43-06 et 43-07 ; la présence des hashes est PROUVÉE par commande contre les VRAIS commits de la plage B43..HEAD, jamais par un compte de mots hexadécimaux (S5, tour 3 : l'ancien compte admettait n'importe quelle suite hexadécimale) : `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && S=$(ls .planning/workstreams/gouvernance/phases/VFDO-43-*/43-05-SUMMARY.md) && shas=$(git log --format=%H "$B43"..HEAD -- plugin/dev-orchestrator plugin/_internal) && printf '%s\n' "$shas" | awk -v S="$S" 'BEGIN{while((getline l < S)>0){if(l ~ /^#+ .*Relecture Samuel \(polarité dev-orchestrator\)/){f=1;continue} if(l ~ /^#+ /)f=0; if(f)sec=sec "\n" l}} NF{n++; if(index(sec, substr($0,1,7))==0){print "SAMUEL-MANQUE " $0; m++}} END{print "SAMUEL-PLAGE n=" n+0 " manquants=" m+0}'` n'imprime aucune ligne SAMUEL-MANQUE et finit par `SAMUEL-PLAGE n=<n> manquants=0` avec n ≥ 2 (commit de la Tâche 1, commit injecteur de la Tâche 2) : chaque SHA réel de la plage qui touche plugin/dev-orchestrator ou plugin/_internal doit figurer (préfixe de 7 caractères) dans la section, qui s'arrête au titre suivant quel que soit son niveau — hashes écrits sous le titre même ; rien d'imprimé (base illisible, SUMMARY absent) vaut échec
  </acceptance_criteria>
  <done>Une vf-mcp-tools malformée ne passe plus nulle part : refusée au gate des agents, refusée à l'install avec un rc 1 relayé au journal, sans casser l'installation ni les agents valides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent (lab ou module) → injecteur | valeur vf-mcp-tools non fiable, écrite à la main dans un lab |
| ~/.claude.json et ./.mcp.json → injecteur | configuration personnelle lue en lecture seule |
| stderr de l'injecteur → journal de l'installeur | lignes relayées à l'utilisateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-40 | Tampering | vf-mcp-tools malformée | high | mitigate | refus au gate (valider_mcp_tools, T109, MUT-M1) et à l'install (ERROR + rc 1, T22a-T22d, MUT-A) : une allowlist qui semble configurée mais n'accorde rien ne passe plus en silence |
| T-43-41 | Tampering (injection) | valeur de frontmatter | high | mitigate | parsée en Python, jamais évaluée ni passée à un shell ; valeur malformée jamais recopiée dans le message ; relais par `log` à préfixe fixe — injection : catégorie canonique, relève aussi de /gsd-secure-phase |
| T-43-42 | Denial of Service | installation | high | mitigate | best-effort conservé : la fonction de l'installeur rend toujours 0 (T55, T56 rc 0), les agents valides sont injectés malgré un voisin malformé (T34) |
| T-43-43 | Information Disclosure | relais du journal | low | mitigate | seules les lignes WARNING/ERROR sont relayées ; l'injecteur n'imprime que des noms de serveurs et des chemins, jamais le contenu de ~/.claude.json ; tests sous HOME temporaire |
| T-43-44 | Tampering | divergence de grammaire gate / injecteur | medium | mitigate | même littéral de charset dans les deux scripts (critère d'acceptation), mêmes quatre formes malformées testées des deux côtés, mêmes valeurs entre guillemets (T22e, T110) : parse_frontmatter déquotait, l'injecteur non — une valeur `"X:a"` passait le gate et était refusée à l'install ; le déquotage commun ferme l'écart. Même règle d'extraction des deux côtés (T22f/T111, T22g/T112) : l'injecteur lisait la ligne SUIVANTE d'une clé vide (`model: sonnet` pris pour un serveur) et la PREMIÈRE occurrence d'une clé en double, le gate la DERNIÈRE et repliait les lignes indentées — une clé présente une seule fois, lue sur sa seule ligne, ferme ces écarts ; « indentée » a un seul prédicat, `^[ \t]`, même constante MCP_CONTINUATION_RE des deux côtés (jumeaux à une espace et à une tabulation, T22f/T111). Ordre trim puis déquotage fixé une seule fois (T22h/T113 : guillemets suivis d'un blanc en fin de ligne), clé en dernière ligne sans lecture hors du frontmatter (T22i/T114), `vf-mcp-tools :` reconnue et refusée des deux côtés au lieu d'être ignorée en silence des deux (T22j/T115) |
| T-43-45 | Spoofing (faux vert) | union des scopes non testée | medium | mitigate | T32 et T33 prouvent le jugement contre l'union ; T23 à T31 fantômes consignés pour Samuel |
</threat_model>

<verification>
- test-inject-mcp-tools.sh, test-vibeflow-update.sh, test-check-agents.sh : 0 KO ; MUT-A et MUT-M1 tués.
- Témoin lab frais sous HOME temporaire : TRACER-MCP-OK.
- Corpus réel des agents : CORPUS-AGENTS fail=0.
- Spec fabrique et triade de version de dev-orchestrator : vérifiées par 43-07 (DEV-ORCH-VERSION-OK), pas ici.
</verification>

<success_criteria>
- FABR-10, volet mécanisme : (a) malformée refusée à l'install et au gate ; (b) serveur nommé absent de l'union signalé jusqu'à l'utilisateur ; (c) textes de vibeflow-update.sh corrigés (vf-calibrate en 43-06). Volet spec §1.2/§7.2 et patch de dev-orchestrator : 43-07.
</success_criteria>

## Artifacts this phase produces (ce plan)

| Symbole / chemin | Nature |
|---|---|
| `malformed_found`, message ERROR « vf-mcp-tools malformee — valeur refusee », sortie rc 1 en mode injection | injecteur (a) |
| message « rien injecte — serveur … absent des sources resolues », sources nommées dans WINDOWS #4 | injecteur (b) |
| relais WARNING/ERROR dans `inject_lab_mcp_into_agents` | installeur |
| `valider_mcp_tools()` | gate des agents |
| `T16` durci, `T22a` à `T22j`, `test-inject-mcp-tools:T32` à `test-inject-mcp-tools:T34`, `MUT-A` ; `T55`, `T56` ; `T108` à `T115`, `MUT-M1` (appel direct), garde `MUT-M1-REFUS-COMPTE` | cas et mutants (T32 à T34 : étiquettes aussi portées par test-check-agents.sh, test-vibeflow-update.sh et, pour T32, test-check-skills.sh — toujours citées préfixées de leur suite) |
| déquotage d'une paire de guillemets dans `named_request` ; règle d'extraction commune (motif de présence `^vf-mcp-tools[ \t]*:`, clé unique, deux-points collé, valeur sur sa seule ligne, prédicat de continuation `MCP_CONTINUATION_RE` = `^[ \t]`, `[ \t]*` dans NAMED_FLAG_RE, ordre trim puis déquotage) | injecteur et gate (parité pour toute valeur) |

## Flagged assumptions

- **Sonde de bords spec-less — FABR-10 : `unclassified` / `unresolved`.** Non résolu d'office. Bords rédigés ici : liste --servers explicite vs union (T16, T32, T33), quatre formes malformées (T22a-T22d, T109), dossier mixte (T34), installation best-effort (T55, T56), valeur brute jamais recopiée.
- **Déquotage dans l'injecteur** (révision du 2026-09-26, parité W5) : décision de planification, pas un arbitrage humain ; alternative écartée — faire refuser par le gate toute valeur entre guillemets — parce qu'elle refuserait du YAML valide que le runtime accepte, contre la doctrine déjà posée par check-agents.sh pour `tools:` (dequote_scalar). Réversible.
- **Règle d'extraction commune** (révision du 2026-09-26, parité R2) : décision de planification, pas un arbitrage humain. Clé en double et ligne indentée qui suit la clé sont refusées des deux côtés plutôt que d'aligner l'injecteur sur parse_frontmatter (dernière occurrence, repli des continuations) : refus explicite, jamais un défaut silencieux (V5), et aucun agent du corpus n'écrit vf-mcp-tools sur plusieurs lignes (vf-reviewer, l.10, tient sur une ligne). Réversible. Tour 3 (S4) : l'ordre trim puis déquotage est celui des deux codes existants (injecteur l.317, gate l.469-481), écrit une seule fois dans la règle ; `vf-mcp-tools :` (espace avant le deux-points) est reconnue présente et refusée des deux côtés plutôt qu'acceptée comme YAML valide ou ignorée — aucun agent du corpus ne l'écrit ainsi, et l'ignorer reproduisait le silence que FABR-10 (a) ferme. Réversible. Tour 5 (F4) : le prédicat « ligne indentée » de la règle (iii) est `^[ \t]` (premier caractère espace ou tabulation), écrit une fois et porté par la même constante des deux côtés, plutôt que le `startswith("  ")` de parse_frontmatter : aucun agent du corpus n'a de ligne indentée après vf-mcp-tools, et le prédicat le plus large refuse explicitement ce que le plus étroit laisserait passer d'un seul côté. Réversible.
- **Harnais de test-check-agents.sh (I1, S6 du tour 3)** : ses 16 appels existants du helper en substitution de commande perdent un refus dans le sous-shell ; le nouvel appel MUT-M1 est DIRECT (sortie redirigée vers un fichier, aucune substitution, aucune branche else de comptage) et la garde MUT-M1-REFUS-COMPTE prouve que son refus est compté ; la correction des appels de la Phase 42 est un résidu signalé en 43-06, pas un geste de ce plan.
- **Union vide** (aucun serveur MCP déclaré nulle part) : l'injecteur garde son no-op journalisé existant (INDÉTERMINÉ en --verify) ; ce n'est pas un cas (b) — un lab sans MCP n'est pas en faute.
- **--verify inchangé** pour une valeur malformée (exclue de la comparaison) : la règle 4 de check-capability-activation.sh lit ce mode ; le durcir est hors mandat.
- **Relais de l'installeur** : changement de comportement du moteur `_internal`, conséquence nécessaire de (b) (sans lui, le signal n'atteint jamais l'installation) ; soumis à la relecture de Samuel.
- **Bruit attendu** : tout lab non iOS qui déclare au moins un serveur MCP verra un WARNING sur vf-reviewer à chaque install ou update de dev-orchestrator — c'est le signal voulu par D-Q3 (b) ; à réévaluer avec Samuel s'il désensibilise.
- **T23 à T31** : neuf cas documentés mais absents du code depuis la Phase 21 (constat 2026-09-25) ; seuls T32 et T33 sont ajoutés pour juger (b) ; la restauration complète est laissée à Samuel.
- **Découpage de révision (2026-09-25)** : la vérification des plans signalait 11 fichiers modifiés (seuil d'avertissement : 10, dimension scope_sanity). L'ancienne Tâche 3 (spec fabrique §1.2/§7.2, VERSION, module.json, README et CHANGELOG de dev-orchestrator) est devenue 43-07, sans changement de fond : ce plan tombe à 6 fichiers et 2 tâches, 43-07 porte 5 fichiers. Décision de planification, pas un arbitrage humain. Entre la fin de ce plan et 43-07, dev-orchestrator porte du code modifié sans bump : check-version-sync.sh ne juge que la cohérence de la triade VERSION ↔ module.json, il reste vert.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-05-SUMMARY.md` when done — commit `.planning/` séparé, avec une section « Relecture Samuel (polarité dev-orchestrator) » (hashes écrits sous ce titre même, aucun sous-titre dans la section — le critère SAMUEL-PLAGE de la Tâche 2 la lit jusqu'au titre suivant) : sortie de `B43=$(sed -n 's/^Base de phase : \([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/gouvernance/phases/VFDO-43-*/43-BASE-PHASE.md | head -1) && test -n "$B43" && git merge-base --is-ancestor "$B43" HEAD && git log --format='%h %s' "$B43"..HEAD -- plugin/dev-orchestrator plugin/_internal` — base de phase FIGÉE, jamais re-dérivée des PLAN.md (au moins deux hashes : le commit de la Tâche 1 et le premier commit de la Tâche 2 ; le commit conductor de la Tâche 2 sort du filtre de chemins), le changement de comportement de l'installeur (relais WARNING/ERROR) et de l'injecteur (rc 1 sur valeur malformée ; une paire de guillemets englobante désormais retirée avant la grammaire), le constat T23 à T31. 43-07 lit ce SUMMARY pour le CHANGELOG de dev-orchestrator et pour §7.2 de la spec.
</output>

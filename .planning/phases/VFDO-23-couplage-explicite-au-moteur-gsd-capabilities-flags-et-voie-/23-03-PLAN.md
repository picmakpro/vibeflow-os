---
phase: 23-couplage-explicite-au-moteur-gsd
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/agents/vf-coder.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: true
requirements: [GSDC-03]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un agent du module sait, sans deviner, quels flags il peut passer à une brique de cycle — et tout flag non nommé est fermé, y compris ceux que `gsd-core` ajoutera demain (D-08)."
    - "Le choix entre recherche et non-recherche se fait sur un critère FACTUEL constatable, jamais sur un ressenti (D-05, ADR-055 §3)."
    - "La doctrine de flags de cycle vit dans `GSD-PIPELINE.md` et renvoie à `docs-flow.md` pour la famille documentaire, sans jamais la dupliquer (D-06, ADR-057)."
    - "Le cycle canonique cesse de déclarer une brique de livraison empruntée sans dire pourquoi elle ne l'est pas (D-21)."
  artifacts:
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md
    - plugin/dev-orchestrator/agents/vf-coder.md
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  key_links:
    - "La clause de fermeture par défaut est ce qui fait que l'allowlist ne périme pas — sans elle, un flag ajouté en amont arrive ouvert par omission, ce qui reproduit la Lacune 5 sur un nouveau terrain."
    - "Le renvoi croisé vers `docs-flow.md` est le seul lien qui empêche deux doctrines de flags de diverger (ADR-057, une capacité une seule voix)."
    - "`vf-coder.md` pointe vers §9 au lieu de reformuler : sans ce renvoi, la gradation vivrait à deux endroits et divergerait au premier changement."
---

<objective>
Fermer la Lacune 3 (aucune doctrine de flags de cycle) — deuxième dans l'ordre imposé, après la
sûreté du plan 23-01, parce qu'on ne peut pas graduer un flag sans savoir ce qu'un mode
d'enchaînement auto-approuve.

Purpose : un seul flag GSD est aujourd'hui employé dans tout le module, et deux faits mesurés
rendent ce silence coûteux — le workflow de plan **prompte** en l'absence de flag de recherche
alors que `vf-coder` n'a pas d'outil de question (blocage ou auto-réponse silencieuse), et le mode
d'enchaînement autonome persiste un état qui survit à la session. Le réglage juste est **étroit**,
et il n'est écrit nulle part.

Output : une section `§9 Doctrine de flags de cycle` dans `GSD-PIPELINE.md`, en **allowlist
stricte** (tout ce qui n'est pas nommé est fermé, futurs flags compris), avec la gradation de la
recherche sur critère factuel et le renvoi croisé vers la doctrine documentaire ; la ligne de la
brique de livraison du cycle canonique corrigée pour dire **pourquoi** elle n'est pas empruntée ;
et un bloc de test T27 à discriminance prouvée par mutation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-CONTEXT.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-RESEARCH.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-PATTERNS.md
@.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-01-SUMMARY.md
@CLAUDE.md
</context>

## Artefacts produits par la Phase 23 (symboles nouveaux — lecture obligatoire avant toute vérification de dérive)

Tous les symboles ci-dessous sont **créés par cette phase** : leur absence de l'existant n'est pas
une dérive.

**Fichiers neufs** : `plugin/dev-orchestrator/scripts/check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/tests/test-check-gsd-config.sh` ·
`plugin/dev-orchestrator/scripts/build-gsd-capabilities-index.sh` ·
`plugin/dev-orchestrator/references/gsd-capabilities-index.md`

**Champs neufs du bloc typé** : `gate`, `reprise` (`plan_id`/`checkpoint`/`gate`/`attendu`),
`verdicts` (`code_review`/`nyquist`/`secure`), `decompte`

**Sections neuves de référence** : `GSD-PIPELINE.md` §9 « Doctrine de flags de cycle » (ce plan) ·
`mission-flow.md` §Pattern E budget partagé + table des briques dormantes · `docs/ADR.md` ADR-061
troisième objet revu

**Blocs de test neufs** de `test-dev-orchestrator.sh` : `T24` à `T32` (ce plan : `T27`)

**Variables d'environnement neuves** : `VF_CONFIG_PATH`, `VF_GSD_CORE_LIB`, `VF_CAPS_INDEX_OUT`,
`VF_GSD_TOOLS`

**Contrats d'exit neufs** : `check-gsd-config.sh` → `0` / `3` / `64`

**Identifiants d'exigence neufs** : `GSDC-01` à `GSDC-10`

## Gates et sondes — choix visibles

- **Sonde spec-less : SAUTÉE, visiblement** (aucun `SPEC.md`, `EDGE_ABSENT`, `PROHIB_ABSENT`).
- **Gate UI : sans objet** (aucun `-UI-SPEC.md`).
- **Gate de schéma ORM : sans objet.**
- **Couverture d'API externe : sans objet, déclaration motivée.** *No external API integration :
  ce plan n'écrit que de la doctrine markdown et des assertions bash — aucun SDK, aucun service
  tiers, aucun appel réseau.*
- **Légitimité de paquet : sans objet** (aucun install).

<tasks>

<task type="tracer">
  <name>Tâche 1 : §9 Doctrine de flags de cycle, en allowlist stricte, câblée au worker et gatée (D-05, D-06, D-08)</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/agents/vf-coder.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <reversibility rating="costly">D-06 place la doctrine dans `GSD-PIPELINE.md`. La déplacer après coup casserait les renvois posés dans `AGENT.md`, dans les agents, dans le test d'exhaustivité et dans le chemin d'install des labs déjà déployés — coût réel, pas un simple `git revert`. Le choix est tranché et n'est pas rouvert ici.</reversibility>
  <read_first>
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md (intégralement, 141 lignes) — la structure en 8 sections numérotées, le ton, le format des tables, et l'en-tête qui déclare le chargement on-demand
    - plugin/dev-orchestrator/references/docs-flow.md (intégralement, 111 lignes) — c'est la doctrine des flags **documentaires**, livrée par la Phase 22 et **mergée** : la §9 y RENVOIE et ne la duplique JAMAIS (ADR-057). Lire notamment sa §Déclencheurs et sa §Garde-fous pour connaître exactement ce qui y est déjà dit
    - plugin/dev-orchestrator/agents/vf-coder.md §Cadrage (ligne 27) — le point exact où le flag de recherche se décide
    - $HOME/.claude/gsd-core/workflows/plan-phase.md autour de la ligne 333 — le prompt sur la recherche en l'absence de flag : c'est le FAIT qui rend l'omission coûteuse
    - $HOME/.claude/gsd-core/workflows/discuss-phase/modes/chain.md — la persistance de l'état d'enchaînement (déjà traitée au plan 23-01 : §9 y renvoie, ne la re-raconte pas)
    - .planning/phases/VFDO-23-.../23-01-SUMMARY.md — l'état exact laissé par le plan 23-01 (le geste de reset et le gate T25 sont écrits, §9 doit pointer dessus)
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh — blocs T22 et T23 (patron le plus récent d'assertion de doctrine, Phase 22) et T14b/T14c (patron de discriminance par mutation)
  </read_first>
  <action>
Écrire la doctrine de flags de cycle **là où le pipeline se lit déjà**, sous une forme qui ne
périme pas, et la relier au seul worker qui passe ces flags.

**1. `GSD-PIPELINE.md` — nouvelle section `## 9. Flags de cycle — allowlist stricte`**, ajoutée à
la fin du fichier, après §8, en respectant la numérotation et le style existants (titre `##`,
séparateur `---` entre sections, tables markdown compactes).

La section porte quatre choses, et rien d'autre :

- **Le principe de fermeture (D-08), énoncé en premier et sans ambiguïté** : seuls les flags
  **nommés dans la table ci-dessous** sont utilisables par un agent du module ; **tout le reste est
  interdit par défaut**, y compris les flags que `gsd-core` ajoutera dans une version ultérieure.
  Écrire explicitement le corollaire, parce que c'est lui qui a de la valeur : un flag nouveau
  arrive **fermé**, et il s'ouvre par une décision datée, jamais par omission. C'est la seule forme
  qui ne périme pas à la montée de version — une liste d'interdits seuls reproduirait le pilotage
  par omission que cette phase vient corriger.

- **La table d'allowlist**, une ligne par brique de cycle (cadrage, plan, exécution), trois
  colonnes : brique · flags autorisés · flags explicitement fermés et pourquoi. Contenu attendu,
  dérivé des décisions déjà tranchées et des faits amont :
  - **cadrage** : le mode non-interactif reste autorisé (c'est ce que `vf-coder` prescrit
    aujourd'hui, ligne 27, et il n'a pas d'outil de question) ; la gradation de la recherche est
    autorisée dans ses deux formes ;
  - **plan** : la gradation de la recherche est autorisée dans ses deux formes ; le mode
    d'enchaînement autonome est **fermé**, avec pour motif le fait établi au plan 23-01 (l'état
    est persisté dans `.planning/config.json`, survit à la session, et fait auto-approuver les
    checkpoints de vérification humaine et auto-sélectionner la première option des checkpoints de
    décision). Renvoyer au geste de reset et au gate T25 écrits en 23-01 — ne pas les re-raconter ;
  - **exécution** : le mode d'enchaînement autonome est **fermé**, même motif ; exécuter hors
    frontière du DAG contredirait de surcroît le pipelining modélisé en `mission-flow.md`.

- **La gradation de la recherche (D-05), sur critère FACTUEL** — deux entrées, chacune un fait
  constatable et jamais un ressenti (ADR-055 §3) : demander la recherche quand l'étape touche une
  **lib, un framework, du natif ou une version**, ou un domaine non cartographié ; la sauter quand
  l'étape prolonge un périmètre **déjà couvert** par un `RESEARCH.md` ou un `CONTEXT.md` récent du
  même dossier de phase. Ajouter la précision qui évite la confusion la plus probable, en une
  ligne : le toggle `workflow.research` du `config.json` active la **capability** de recherche sur
  le point de hook de pré-plan (le moteur spawne lui-même son chercheur) ; le flag, lui, répond au
  **prompt** — le toggle étant à vrai sur ce lab, la recherche a lieu de toute façon, le flag décide
  seulement si le worker se fait interroger. Ces deux couches ne sont pas interchangeables.

- **Le renvoi croisé (D-06)** : une ligne, pas un paragraphe — la famille de flags **documentaires**
  a sa doctrine dans `docs-flow.md` (Phase 22), qui fait autorité et n'est **ni modifiée ni
  dupliquée** ici. Nommer le fichier et la raison (une capacité, une seule voix — ADR-057).

**2. `vf-coder.md` §Cadrage (point 1 du cycle)** — ajouter **un renvoi**, pas une reformulation :
le choix entre les deux formes de gradation de la recherche se lit dans `GSD-PIPELINE.md` §9, sur
critère factuel. Une phrase maximum ; ce fichier est un routeur de 74 lignes, la doctrine ne
descend pas dedans.

**3. `test-dev-orchestrator.sh` — bloc `T27`**, ajouté après le dernier bloc existant :
- assertion A : `GSD-PIPELINE.md` porte une section de flags de cycle ;
- assertion B : cette section porte la **clause de fermeture par défaut** (le fait que tout flag
  non nommé, y compris futur, est interdit) — c'est l'assertion qui a le plus de valeur, parce
  qu'une table d'allowlist sans clause de fermeture est une simple liste ;
- assertion C : la section nomme les deux formes de gradation de la recherche ;
- assertion D : le renvoi croisé vers `docs-flow.md` est présent **dans** `GSD-PIPELINE.md`, ET
  `GSD-PIPELINE.md` ne redéfinit aucun des flags documentaires portés par `docs-flow.md`
  (assertion négative de non-duplication, ADR-057) ;
- assertion E : `vf-coder.md` renvoie nommément vers `GSD-PIPELINE.md` pour ce choix ;
- assertion F (**DISCRIMINANTE, par mutation**) : sur une copie temporaire de `GSD-PIPELINE.md`
  (`mktemp -d` + `trap ... EXIT`) privée de la clause de fermeture, la fonction de détection de
  l'assertion B doit **échouer** ; sur le fichier réel elle doit rester verte.

Portabilité : `$GREP`, pas de `grep -P`, pas de `sed -i` nu (ADR-054).
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO` et la ligne de résultat mentionne `T27`.
    - `plugin/dev-orchestrator/references/GSD-PIPELINE.md` contient un titre de section commençant par `## 9.` et comportant le mot `flags`.
    - `grep -c 'docs-flow.md' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1.
    - `grep -c 'ADR-057' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1 (le motif du renvoi est écrit, pas seulement le renvoi).
    - `grep -c 'GSD-PIPELINE.md' plugin/dev-orchestrator/agents/vf-coder.md` retourne ≥ 1.
    - `wc -l < plugin/dev-orchestrator/agents/vf-coder.md` retourne une valeur ≤ 250 (ADR-029).
    - `grep -c 'verify-only' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne `0` (les flags documentaires ne sont pas dupliqués — ils restent chez `docs-flow.md`).
    - Preuve de mutation consignée au SUMMARY : la copie temporaire de `GSD-PIPELINE.md` privée de la clause de fermeture porte la suite à au moins `1 KO`.
    - `bash plugin/conductor/scripts/check-agents.sh --agents-dir plugin/dev-orchestrator/agents --strict` sort en 0.
  </acceptance_criteria>
  <done>Un agent du module lit une allowlist fermée par défaut, décide la recherche sur un fait constatable, et le renvoi vers la doctrine documentaire empêche qu'une seconde voix apparaisse sur la même question — le tout verrouillé par une assertion dont la discriminance est prouvée par mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : le cycle canonique cesse de déclarer la brique de livraison empruntée sans dire pourquoi (D-21)</name>
  <files>plugin/dev-orchestrator/references/GSD-PIPELINE.md, plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>
  <read_first>
    - plugin/dev-orchestrator/references/GSD-PIPELINE.md §1 (lignes 14-36) — la chaîne canonique en une ligne de code et la table à 9 lignes qui la détaille ; §6 Garde-fous (lignes 101-111)
    - plugin/dev-orchestrator/references/mission-contracts.md §Isolation de branche (ADR-059) et §ADR-064 (lignes 79-131) — le protocole réel : branche dédiée, PR laissée ouverte, jamais de merge, un écrivain = un worktree
    - docs/ADR.md ADR-059 et ADR-064 — les deux décisions qui priment sur l'adoption de la brique de livraison amont
    - .planning/phases/VFDO-23-.../23-CONTEXT.md D-21 et §Deferred (la réouverture éventuelle est conditionnée, elle n'est pas fermée à jamais)
  </read_first>
  <action>
Faire dire à la doctrine ce que la pratique fait déjà, pour que la tension cesse d'être subie.

**1. `GSD-PIPELINE.md` §1** — la brique de livraison reste **dans** la table du cycle canonique
(c'est un outil réel du moteur, D-21 la corrige, ne la supprime pas), mais sa ligne cesse de la
présenter comme empruntée sans réserve. Amender la colonne de rôle ou ajouter une note sous la
table, au choix du rédacteur pourvu que ce soit à un seul endroit : chez VibeFlow, l'ouverture de
la PR reste un **geste VibeFlow, tenu à la main par le manager de mission**, parce qu'ADR-059 (une
mission = une branche) et ADR-064 (un écrivain = un worktree, claim de driver) priment et que la
brique amont ne connaît ni l'une ni l'autre. Renvoyer à `mission-contracts.md` §Isolation de
branche pour le protocole — ne pas le recopier.

**2. `GSD-PIPELINE.md` §6 Garde-fous** — ajouter une puce courte et symétrique des quatre
existantes : l'ouverture de PR est un geste VibeFlow (ADR-059 / ADR-064), jamais délégué à la
brique de livraison amont tant que ces deux ADR tiennent. Une puce, un renvoi, pas un paragraphe.

**3. `test-dev-orchestrator.sh`** — étendre le bloc `T27` (ne pas créer de bloc nouveau pour deux
assertions) :
- assertion G : `GSD-PIPELINE.md` nomme la brique de livraison **et** cite au moins l'une des deux
  ADR qui expliquent pourquoi elle n'est pas empruntée ;
- assertion H (**DISCRIMINANTE, par mutation**) : sur une copie temporaire privée de la mention
  d'ADR, l'assertion G doit échouer — sans quoi elle ne prouverait que la présence d'un nom de
  brique qui était déjà là avant cette phase.
  </action>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` termine avec `0 KO`.
    - `grep -c 'ADR-059' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1 ET `grep -c 'ADR-064' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1.
    - `grep -c 'gsd-ship' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1 (la brique reste nommée dans le cycle canonique — elle n'est pas supprimée).
    - `grep -c 'mission-contracts.md' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne ≥ 1 (le protocole de branche est renvoyé, pas recopié).
    - `grep -c 'gh pr create' plugin/dev-orchestrator/references/GSD-PIPELINE.md` retourne `0` (aucune recopie du protocole).
    - Preuve de mutation de l'assertion H consignée au SUMMARY.
  </acceptance_criteria>
  <done>La ligne du cycle canonique dit désormais pourquoi la brique de livraison amont n'est pas empruntée ici, la garde correspondante existe au §6, et la tension relevée par le ROADMAP a disparu parce que la doctrine dit la même chose que la pratique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine de flags → agents du module | `GSD-PIPELINE.md` est chargé on-demand par `vibeflow-dev` et `vf-dev-manager` : ce qu'il autorise devient ce que les agents passent au moteur. Un flag ouvert par omission traverse cette frontière sans que rien ne le signale. |
| `gsd-core` (versions futures) → allowlist | Un flag ajouté en amont franchit la frontière de version. La clause de fermeture est le seul mécanisme qui le fait arriver **fermé** plutôt qu'ouvert. |
| `docs-flow.md` (Phase 22) → `GSD-PIPELINE.md` | Deux fichiers de doctrine sur deux familles de flags : la frontière est le renvoi croisé. Toute duplication la fait disparaître et ouvre la divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03-01 | Elevation of Privilege | flag futur de `gsd-core` employé sans arbitrage | high | mitigate | Clause de fermeture par défaut écrite en tête de §9 et **gatée** par l'assertion T27-B, dont la discriminance est prouvée par mutation. Une table sans clause de fermeture serait une liste, pas une allowlist. |
| T-23-03-02 | Tampering | mode d'enchaînement autonome réintroduit sur les briques de plan ou d'exécution | high | mitigate | §9 déclare ces deux emplois fermés avec leur motif factuel, et renvoie au gate machine T25 posé au plan 23-01 — la doctrine seule n'aurait rien empêché. |
| T-23-03-03 | Repudiation | deux doctrines de flags divergentes (cycle vs documentaire) | medium | mitigate | Renvoi croisé obligatoire vers `docs-flow.md` + assertion négative T27-D : `GSD-PIPELINE.md` ne redéfinit aucun flag documentaire. Critère machine : le nom du flag d'audit documentaire ne doit pas apparaître dans `GSD-PIPELINE.md`. |
| T-23-03-04 | Spoofing | confusion entre le toggle de capability et le flag de ligne de commande | medium | mitigate | §9 porte la ligne de distinction explicite (le toggle active la capability sur le point de hook, le flag répond au prompt) — c'est la confusion la plus probable à la lecture, elle est nommée plutôt que laissée au lecteur. |
| T-23-03-05 | Denial of Service | worker bloqué sur un prompt qu'il ne peut pas satisfaire | medium | mitigate | La gradation est écrite pour que le flag ne soit **jamais** omis : le workflow de plan prompte en son absence et `vf-coder` n'a pas d'outil de question. §9 rend le choix déterministe sur un fait. |
| T-23-03-SC | Tampering | installs `npm`/`pip`/`cargo` | — | accept | Sans objet : aucun paquet installé par ce plan. |
</threat_model>

<verification>
- Suite du module verte : `0 KO`, `T27` nommé dans la sortie.
- Densité ADR-029 : `wc -l` sur `vf-coder.md` ≤ 250 ; `GSD-PIPELINE.md` reste une référence chargée on-demand (aucun plafond dur, mais la §9 vise ≤ 45 lignes ajoutées — renvoi plutôt que reformulation).
- ADR-044 : `check-agents.sh --strict` vert sur le dossier d'agents du module.
- Non-duplication ADR-057 prouvée par une assertion **négative**, pas par relecture.
- Discriminance des deux assertions structurantes (clause de fermeture, motif ADR de la brique de livraison) prouvée par **mutation**, commandes et compteurs consignés au SUMMARY.
</verification>

<success_criteria>
- SC1 — La doctrine de flags de cycle existe, dans `GSD-PIPELINE.md`, sous forme d'allowlist dont la clause de fermeture par défaut est explicite et gatée.
- SC2 — La gradation de la recherche repose sur deux faits constatables, et la distinction toggle/flag est écrite.
- SC3 — Le renvoi croisé vers la doctrine documentaire existe et aucune de ses règles n'est dupliquée (prouvé par assertion négative).
- SC4 — Le cycle canonique dit pourquoi la brique de livraison amont n'est pas empruntée, en citant les deux ADR qui priment.
- SC5 — `vf-coder` pointe vers la doctrine au lieu de la reformuler, et reste sous le plafond de densité.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-23-couplage-explicite-au-moteur-gsd-capabilities-flags-et-voie-/23-03-SUMMARY.md` à la fin.
Y consigner : le nombre de lignes ajoutées à `GSD-PIPELINE.md`, le contenu exact de la table
d'allowlist retenue, et les deux preuves de mutation avec leurs compteurs avant/après.
</output>

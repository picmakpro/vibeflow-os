---
phase: 20-fluidite-du-flux-de-dev-sans-perte-de-qualite
plan: 07
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03", "20-04", "20-05", "20-06"]
files_modified:
  - docs/ADR.md
  - plugin/conductor/references/team-kernel.md
  - plugin/conductor/README.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/business-pilot-bundle/VERSION
  - plugin/business-pilot-bundle/module.json
  - plugin/business-pilot-bundle/CHANGELOG.md
  - plugin/content-bundle/VERSION
  - plugin/content-bundle/module.json
  - plugin/content-bundle/CHANGELOG.md
  - plugin/growth-bundle/VERSION
  - plugin/growth-bundle/module.json
  - plugin/growth-bundle/CHANGELOG.md
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, SC7]

must_haves:
  truths:
    - "SC1 / D-02 : ADR-051 est révisée sur ce SEUL point. La phrase qui plaçait les trois agents de planification, de revue et d'audit hors du périmètre d'injection devient : deux d'entre eux restent inchangés, le relecteur reçoit une allowlist NOMMÉE. L'argument est écrit littéralement — un relecteur ne PRODUIT pas un verdict de compilation, il en VÉRIFIE un."
    - "SC1 / D-02 coût écrit noir sur blanc : la révision porte le chiffre — de l'ordre de quatre-vingt-dix secondes de plus par revue et un slot de simulateur consommé. L'omettre ferait de l'ADR une décision sans prix."
    - "SC1 / D-02 : la section du code impacté d'ADR-051 gagne les deux entrées correspondantes — le fichier de l'agent de revue et le script d'injection en mode nommé."
    - "SC3 / D-26 : ADR-060 est posée, avec le numéro libre suivant, et acte que la revue devient un étage de premier rang piloté par le manager, sorti du cycle interne du worker d'exécution."
    - "SC6 / D-26 tranché par ce plan : le changement de périmètre des hooks ne reçoit PAS d'ADR dédiée. C'est une correction de configuration, pas un changement de doctrine — la distinction est exigée par le livrable de cadrage du ROADMAP, et le journal de version du module la porte."
    - "SC2 / D-08 : la ligne de doctrine du cloisonnement par outils, dans le contrat du noyau d'équipe ET dans la vitrine du module, cite désormais le mécanisme réel qui rend la barrière des juges effective, au lieu de l'affirmer sans mécanisme."
    - "SC2 / D-09 sens FERMETURE : le contrat du noyau d'équipe documente la classe de problème — un outil déclaré dans le champ d'outils peut être absent au runtime — à côté de la restriction analogue déjà documentée sur l'allowlist de dispatch, et nomme le filet de repli."
    - "SC4 / la doctrine du plan de bataille cite le flag de périmètre et le champ de régime dans les deux fichiers où la ligne du kernel est décrite, avec les noms exacts livrés par le plan 20-02."
    - "SC5 / la liste des scripts et le compteur de suites du module obligatoire sont mis à jour du fait du nouveau gate d'invariants : quatorze scripts, onze suites."
    - "SC7 / D-25 : les 6 modules sont bumpés, chacun avec sa triade cohérente — fichier de version, manifeste, journal — et l'arbitrage mineur contre correctif est fait PAR MODULE, selon la règle du dépôt : nouvelle capacité en mineur, durcissement et correctif en correctif."
    - "SC7 / gates de sortie de phase VERTS : la boucle complète des suites n'imprime aucun échec ; le gate de conformité en mode strict sort 0 sur les 6 dossiers d'agents ; les 17 triades par module sont cohérentes."
    - "SC7 / la release racine, le tag annoté et la publication GitHub sont HORS PÉRIMÈTRE de cette phase et le restent — ils appartiennent à une validation humaine post-fusion. Aucune tâche de ce plan ne les touche."
    - "SC7 / le compteur de suites cité par les deux README racine passe de 42 à 43 du fait de la suite créée en 20-05 : c'est un reste-à-faire de RELEASE, consigné explicitement, jamais corrigé ici — patron déjà appliqué aux phases 13, 17 et 19."
  artifacts:
    - docs/ADR.md
    - plugin/conductor/references/team-kernel.md
    - plugin/conductor/README.md
    - plugin/conductor/CHANGELOG.md
    - plugin/dev-orchestrator/CHANGELOG.md
    - plugin/design-orchestrator/CHANGELOG.md
    - plugin/business-pilot-bundle/CHANGELOG.md
    - plugin/content-bundle/CHANGELOG.md
    - plugin/growth-bundle/CHANGELOG.md
  key_links:
    - "ADR-051 révisée → `plugin/dev-orchestrator/agents/vf-reviewer.md` et `inject-mcp-tools.sh` (plan 20-03) : l'ADR documente le pourquoi et le coût, l'agent porte le protocole, le script porte le mécanisme. Les trois doivent dire la même chose ; l'ADR est écrite en dernier, quand le comportement existe."
    - "ADR-060 → `plugin/dev-orchestrator/references/mission-flow.md` §Étage revue (plan 20-06) : l'ADR acte la décision, le fichier de référence porte le protocole. L'ADR ne duplique pas le protocole."
    - "doctrine du noyau d'équipe → les 4 juges (plan 20-04) : la ligne corrigée n'est vraie que parce que le champ d'outils interdits a été posé. Écrire la doctrine avant la capacité produirait exactement la doc menteuse que la phase corrige — c'est pourquoi ce plan est en dernière vague."
    - "liste des scripts du module obligatoire → `plugin/conductor/scripts/check-mission-invariants.sh` (plan 20-05) : le compteur de scripts et celui de suites ne sont exacts qu'après la création de ce gate."
    - "compteur de suites des 2 README racine → `scripts/check-version-sync.sh` : ce gate compare le nombre annoncé au nombre réel de suites. Il sera ROUGE sur ce seul contrôle à l'issue de la phase, par construction et par choix — le rattrapage voyage avec le commit de release racine."
  prohibitions:
    - "P-01 — MUST NOT bumper la `VERSION` racine, `plugin/.claude-plugin/plugin.json` ni `.claude-plugin/marketplace.json`. La release racine est hors périmètre de cette phase, réservée à une validation humaine post-fusion."
    - "P-02 — MUST NOT créer ni pousser de tag git, ni créer de release GitHub. Même motif."
    - "P-03 — MUST NOT corriger le compteur de suites des 2 README racine. Il voyage avec le commit de release — patron appliqué aux phases 13, 17 et 19. Le corriger ici serait s'accorder un morceau de la release explicitement sortie du périmètre."
    - "P-04 — MUST NOT créer d'ADR pour le changement de périmètre des hooks. C'est une correction de configuration : le journal de version du module suffit, et la distinction entre configuration et doctrine est exigée par le livrable de cadrage."
    - "P-05 — MUST NOT réécrire ADR-051 au-delà du point révisé. Sa décision de fond, son sélecteur data-driven et son cloisonnement anti-triche restent tels quels ; seuls la phrase citée, la section de code impacté et un paragraphe de coût changent."
    - "P-06 — MUST NOT modifier un fichier de source appartenant à un autre plan de la phase. Ce plan écrit de la doctrine, des journaux et des versions ; il ne corrige aucun script, aucun agent, aucun test. Si un gate échoue, il REMONTE le défaut au lieu de le réparer ici."
    - "P-07 — MUST NOT vérifier ni corriger l'affirmation « anti-triche vérifié par les suites de test de chaque module » au-delà d'un constat. Elle est signalée pour vigilance dans le cadrage, non confirmée fausse, et sa vérification est un élément différé. Si le constat la trouve fausse, le SIGNALER au compte rendu — ne pas ouvrir le chantier ici."
    - "P-08 — MUST NOT modifier `.github/workflows/ci.yml`."
    - "P-09 — MUST NOT lire, éditer ni committer un fichier NON SUIVI de `.planning/missions/` : une session concurrente y travaille. Ne jamais stager `.planning/` en bloc — stager fichier par fichier, et vérifier `git status --porcelain` avant chaque commit."
---

<objective>
Fermer la phase par sa gouvernance : les deux décisions d'architecture qui l'actent (une révision
ciblée et une ADR nouvelle), la doctrine transverse mise en conformité avec les capacités
réellement livrées, et les 6 modules bumpés avec leur triade cohérente — sans jamais toucher à la
release racine, qui reste une décision humaine post-fusion.

Purpose: ce plan est en dernière vague par construction, et c'est le point important. Écrire la
doctrine avant que les capacités qu'elle décrit n'existent produirait exactement le défaut que la
phase corrige : une documentation qui affirme une garantie que le runtime ne pose pas. La doctrine
se corrige quand elle devient vraie, pas avant.

Output: 2 décisions d'architecture (1 révisée, 1 nouvelle), 2 fichiers de doctrine alignés,
6 triades de module bumpées, et une liste de reste-à-faire explicite pour la release humaine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md
@.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

## Périmètre, dépendances et risque (fiche de lecture directe pour `vf-dev-manager`)

| Rubrique | Valeur |
|---|---|
| **Changement couvert** | Gouvernance transverse de la phase : décisions d'architecture, doctrine, versions de module |
| **Décisions implémentées** | D-02, D-08, D-09 (volet doctrine transverse), D-25, D-26 ; consigne les différés de D-00 et D-03 |
| **Fichiers écrits** | `docs/ADR.md`, `plugin/conductor/references/team-kernel.md`, `plugin/conductor/README.md`, et la triade version/manifeste/journal des 6 modules |
| **Dépendances amont** | **TOUS les plans de la phase (20-01 à 20-06)** — dépendances d'ÉCRITURE (la doctrine décrit ce qu'ils livrent) ET de MESURE (ce plan rejoue les gates dont le comportement est modifié par 20-01, et compte des artefacts créés par 20-05) |
| **Dépendances aval** | aucune dans la phase. En aval hors phase : la release racine humaine |
| **Critères de succès** | SC7 (intégral, sauf la release racine explicitement hors périmètre), et le volet documentaire de SC1, SC2, SC3, SC4, SC5, SC6 |
| **Risque** | **faible sur le geste, élevé sur l'exactitude** — aucun code n'est modifié, mais toute affirmation écrite ici est une promesse que le repo devra tenir. Chaque phrase de doctrine doit être vérifiée sur pièce contre l'état livré par les plans amont |
| **Réversibilité** | **reversible** — texte et numéros de version. Rien de destructif |

## Arbitrage de version par module (D-25, délégué au planner)

Règle du dépôt : nouvelle capacité en **mineur**, correctif et durcissement en **correctif**.
Arbitrage rendu ici, par module, à appliquer tel quel.

| Module | Version courante | Cible | Nature dominante des changements de la phase |
|---|---|---|---|
| `conductor` | v1.16.0 | **v1.17.0** (mineur) | Nouvelles capacités : flag de périmètre et champ de régime sur le plan de bataille, sortie de périmètres gelés, nouveau gate d'invariants, mécanisme de préfixes tiers porté au second gate. Les correctifs de périmètre de hooks et de charset y sont inclus mais ne dominent pas |
| `dev-orchestrator` | v2.7.1 | **v2.8.0** (mineur) | Nouvelles capacités : mode d'injection MCP nommé, étage revue de premier rang, protocole de gradation |
| `design-orchestrator` | v1.3.1 | **v1.3.2** (correctif) | Durcissement (champ d'outils interdits) + correction d'une affirmation trompeuse. Aucune capacité nouvelle |
| `business-pilot-bundle` | v2.0.2 | **v2.0.3** (correctif) | Durcissement seul |
| `content-bundle` | v2.0.2 | **v2.0.3** (correctif) | Durcissement seul |
| `growth-bundle` | v2.0.2 | **v2.0.3** (correctif) | Durcissement seul |

## Hors périmètre, explicite et non négociable — la release racine

La discipline de release du dépôt exige qu'une version soit toujours accompagnée d'un tag annoté et
d'une publication. **Rien de tout cela n'appartient à cette phase.** Le bump du fichier de version
racine, du manifeste de plugin, de la fiche de marketplace, des badges et de l'historique des deux
README, la création et la poussée du tag, la publication, et la vérification finale du gate de
release : tout cela vient APRÈS la fusion, sous validation humaine séparée.

Ce plan produit à la place une **liste de reste-à-faire** que la personne qui fera la release doit
pouvoir exécuter sans relire la phase.

## Artifacts this phase produces (portion couverte par ce plan)

| Symbole | Type | Valeur / contrat |
|---|---|---|
| ADR-051 §Décision, §Code Impacté | révision ciblée | le relecteur sort de la liste des agents non injectés ; deux entrées de code ajoutées ; coût chiffré |
| **ADR-060** | décision nouvelle | la revue devient un étage de premier rang piloté par le manager |
| ligne de cloisonnement par outils | doctrine corrigée (2 fichiers) | cite le mécanisme réel de la barrière des juges |
| paragraphe « écart déclaré / runtime, sens fermeture » | doctrine nouvelle (contrat du noyau d'équipe) | un outil déclaré peut être absent au runtime ; filet de repli nommé |
| ligne du plan de bataille | doctrine corrigée (2 fichiers) | cite le flag de périmètre et le champ de régime |
| compteurs du module obligatoire | doctrine corrigée | quatorze scripts, onze suites |
| 6 triades de module | versions bumpées | fichier de version, manifeste et journal cohérents par module |
| §Reste-à-faire de release | section de compte rendu | liste exécutable pour la validation humaine post-fusion |

## Détecteurs `plan:pre`

`api-coverage` : non déclenché (sortie `{"detected":false}` du détecteur sur le périmètre de la
phase) — aucune intégration d'API ou de SDK externe. `schema-gate` : non déclenché — aucun fichier
de schéma ni d'ORM. `assumption-delta` : déclenché sur la phase, traité dans le plan 20-06 ; ce plan
n'en porte que la trace documentaire via ADR-060.

<tasks>

<task type="auto">
  <name>Task 1 : ADR-051 révisée sur son seul point contesté, et ADR-060 posée (D-02, D-26)</name>
  <reversibility rating="reversible">Une phrase amendée, deux entrées ajoutées à une liste, une décision nouvelle. Aucun comportement machine.</reversibility>
  <read_first>
    - `docs/ADR.md`, ADR-051 **en entier** — le contexte, l'énoncé du problème, le tableau d'options, les trois points de la décision (dont le premier porte la phrase à amender), les conséquences positives et négatives, la section de cloisonnement anti-triche, la section de code impacté et la section de rules associées. Repérer précisément la phrase qui place les trois agents de planification, revue et audit hors du périmètre, avec sa parenthèse de justification.
    - `docs/ADR.md`, la dernière ADR posée (ADR-059) **en entier** — pour la structure exacte d'une ADR de ce dépôt : ordre des rubriques, forme de l'en-tête (date, statut, décideur, contexte avec la release), et style de la section de code impacté.
    - `plugin/dev-orchestrator/agents/vf-reviewer.md` dans l'état livré par les plans 20-03 et 20-06 — l'ADR doit décrire ce que l'agent fait RÉELLEMENT, pas ce que le cadrage prévoyait.
    - `plugin/dev-orchestrator/scripts/inject-mcp-tools.sh` dans l'état livré par 20-03 — le nom exact de la clé de frontmatter et la grammaire de sa valeur, à citer dans l'ADR.
    - `plugin/dev-orchestrator/references/mission-flow.md` §Étage revue dans l'état livré par 20-06 — le protocole que ADR-060 acte sans le dupliquer.
    - `.planning/ROADMAP.md` §Phase 20 — les critères de succès 1, 3 et 4 dans leur formulation littérale, les trois garde-fous non négociables, et le livrable de cadrage exigé (gain, coût, risque, réversibilité, ADR par changement, et distinction nette entre correction de configuration et changement de doctrine).
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-02, D-26 et la section des formulations exactes attendues.
  </read_first>
  <files>docs/ADR.md</files>
  <behavior>
    - ADR-051 ne place plus le relecteur parmi les agents laissés hors du périmètre d'injection ; les deux autres agents de planification et d'audit y restent explicitement.
    - ADR-051 porte l'argument littéral de vérification contre production d'un verdict de compilation.
    - ADR-051 porte le coût chiffré et le slot de simulateur consommé.
    - La section de code impacté d'ADR-051 liste le fichier de l'agent de revue et le script d'injection en mode nommé.
    - Une ADR-060 existe, avec la structure canonique du dépôt, et acte que la revue devient un étage de premier rang piloté par le manager.
    - ADR-060 énonce ce qui est écarté : l'indexation de la gradation sur le volume, et l'allègement d'un différentiel de comblement.
    - Aucune ADR n'est créée pour le changement de périmètre des hooks.
  </behavior>
  <action>
**Révision d'ADR-051, strictement bornée à son point contesté.** Amender la phrase du premier point
de décision qui place les trois agents de planification, de revue et d'audit hors du périmètre
d'injection : deux d'entre eux — le manager de dev et l'auditeur — restent inchangés, et le
relecteur reçoit une allowlist NOMMÉE, portée par la clé de frontmatter livrée par le plan
d'injection, dont il faut citer le nom exact et la grammaire vérifiés sur pièce.

Écrire l'argument dans sa formulation littérale, celle que le cadrage impose mot pour mot : un
relecteur ne PRODUIT pas un verdict de compilation, il en VÉRIFIE un. Et enchaîner sur le coût,
écrit noir sur blanc : de l'ordre de quatre-vingt-dix secondes de plus par revue, et un slot de
simulateur consommé. Une décision d'architecture sans son prix est une décision non instruite ;
c'est explicitement ce que le cadrage demande de ne pas omettre.

Ajouter aux conséquences deux points que le mécanisme rend nécessaires et que l'agent porte déjà :
l'ordre d'appel imposé (le nettoyage précède toute compilation ou exécution de tests de
vérification, faute de quoi un résultat servi par le cache annonce zéro avertissement sans avoir
rien compilé), et l'exigence de paramètres de projet explicites à chaque appel de build (le serveur
maintient un état de session global partagé par la fenêtre principale et tous les sous-agents).

Compléter la section de code impacté d'ADR-051 des deux entrées correspondantes : le fichier de
l'agent de revue, et le script d'injection avec la mention de son mode nommé.

Ne rien changer d'autre dans cette ADR : sa décision de fond, son sélecteur data-driven, son tableau
d'options, sa section de cloisonnement anti-triche et ses rules associées restent tels quels. Une
révision ciblée se voit à ce qu'elle ne déborde pas.

**ADR-060, nouvelle.** La poser avec le numéro libre suivant, vérifié sur pièce en lisant la
dernière ADR du fichier — ne pas se fier au numéro annoncé par le cadrage sans le vérifier, une
phase concurrente a pu en poser une entre-temps. Reprendre la structure canonique du dépôt : date,
statut, décideur, contexte avec la release concernée, puis problème, options, décision, conséquences,
code impacté, rules associées.

Le problème à énoncer, appuyé sur les faits vérifiés du ROADMAP : la revue était le seul étage à la
fois obligatoire — en dur dans le cycle du worker d'exécution — et hors de portée du manager, une
règle lui interdisant explicitement d'en ajouter une. La seule gradation existante était indexée sur
le volume d'étapes restantes, ce qui est le mauvais axe : trois lignes sur un chemin partagé sont
minuscules et à très haut risque, quatre cents lignes de domaine pur prouvées par mutation sont
grosses et à bas risque. Et le meilleur rendement de toute la tranche auditée vient des jointures
entre lots parallèles, un étage qui n'existait que parce qu'il avait été créé à la main.

La décision à acter, en renvoyant au fichier de protocole pour le détail plutôt qu'en le dupliquant :
la revue devient un nœud de plan de bataille posé systématiquement par le manager, dispatché en
direct ; la boucle de correction migre vers le manager sous forme de mandat de correction ciblée ;
la gradation s'appuie sur quatre déclencheurs objectifs ; une revue de jointure obligatoire est
déclenchée par la topologie du graphe ; et le garde-fou de comblement est adossé au champ de régime
que la réouverture écrit elle-même.

Les conséquences à écrire honnêtement, dans les deux sens : un nœud de plus par étape, donc un coût
de dispatch réel ; en contrepartie, la gradation a enfin un endroit où s'appliquer, et la jointure
cesse d'être un geste manuel. Écrire aussi ce qui est explicitement ÉCARTÉ : l'indexation sur le
volume, et tout allègement d'un différentiel de comblement.

**Le changement de périmètre des hooks ne reçoit PAS d'ADR.** Le trancher explicitement dans le
compte rendu et dans le journal de version du module obligatoire : c'est une correction de
configuration, pas un changement de doctrine, et le livrable de cadrage du ROADMAP exige que cette
distinction soit nette. Une ADR pour un correctif de configuration diluerait le registre.
  </action>
  <verify>
    <automated>grep -c '^## ADR-060' docs/ADR.md && grep -c 'VÉRIFIE' docs/ADR.md && python3 -c "import re,sys; t=open('docs/ADR.md',encoding='utf-8').read(); n=[int(m) for m in re.findall(r'^## ADR-(\d+)', t, re.M)]; assert n==sorted(n), 'ADR non ordonnées'; assert len(n)==len(set(n)), 'numéro dupliqué'; print('ADR ok, dernier =', n[-1])"</automated>
  </verify>
  <acceptance_criteria>
    - ADR-060 existe exactement une fois, porte un numéro qui n'était pas déjà pris, et la suite des numéros d'ADR du fichier est strictement croissante et sans doublon — asserté par la commande python du bloc de vérification.
    - L'argument littéral est présent : `grep -c 'ne PRODUIT pas' docs/ADR.md` renvoie au moins 1, et la phrase se trouve dans ADR-051.
    - Le coût chiffré est présent dans ADR-051 : une recherche du nombre de secondes et du mot désignant le slot de simulateur renvoie des lignes situées dans cette ADR.
    - La section de code impacté d'ADR-051 cite les deux nouvelles entrées : `grep -A 20 '### Code Impacté' ` sur le bloc d'ADR-051 contient le chemin du fichier de l'agent de revue et celui du script d'injection.
    - La révision ne déborde pas : le diff sur ADR-051 ne touche ni son tableau d'options, ni sa section de cloisonnement anti-triche, ni ses rules associées — vérifié par lecture de `git diff docs/ADR.md`.
    - Aucune ADR n'a été créée pour le périmètre des hooks : le fichier ne contient aucune ADR dont le titre porte sur les hooks de conformité.
    - Cohérence croisée vérifiée sur pièce : le nom de clé de frontmatter cité par ADR-051 est identique à celui présent dans le fichier de l'agent de revue — comparaison EXÉCUTÉE, pas supposée.
    - ADR-060 renvoie au fichier de protocole d'étage revue sans en dupliquer le contenu : sa section de décision fait moins de 25 lignes.
  </acceptance_criteria>
  <done>ADR-051 est révisée sur son seul point contesté, argument et coût écrits littéralement ; ADR-060 acte le déplacement de la revue en étage de premier rang ; le correctif de configuration reste sans ADR, par décision explicite.</done>
</task>

<task type="auto">
  <name>Task 2 : la doctrine transverse dit enfin ce que le runtime fait — cloisonnement, sens fermeture, plan de bataille, compteurs (D-08, D-09)</name>
  <reversibility rating="reversible">Corrections de prose dans deux fichiers de doctrine.</reversibility>
  <read_first>
    - `plugin/conductor/references/team-kernel.md` **en entier** (78 lignes) — le tableau des briques du noyau, dont la ligne du plan de bataille (qui énumère aujourd'hui les sous-commandes sans le flag de périmètre) et la ligne du cloisonnement par outils (qui affirme la barrière des juges, documente la restriction de l'allowlist de dispatch entre parenthèses, et porte l'affirmation sur la vérification par les suites de test). Lire aussi la section sur ce que chaque métier paramètre, dont le point sur les gates parallèles — sa formulation sur les juges doit rester vraie après le changement.
    - `plugin/conductor/README.md` **en entier** (130 lignes) — le tableau miroir des briques, la section des hooks, la liste des scripts par famille avec son compteur en titre, la ligne du compteur de suites, et le bloc d'arborescence du module qui répète les deux compteurs.
    - Les 4 fichiers de juge dans l'état livré par le plan 20-04 — pour vérifier SUR PIÈCE ce que la doctrine peut désormais affirmer.
    - `plugin/conductor/scripts/dag.sh` dans l'état livré par 20-02 — les noms exacts du flag de périmètre, du champ de régime et de la sortie de périmètres gelés.
    - `plugin/conductor/scripts/check-mission-invariants.sh` dans l'état livré par 20-05 — le nouveau gate à inscrire dans la liste des scripts, sa famille d'appartenance et son contrat de sortie.
    - `plugin/dev-orchestrator/agents/vf-coder.md` §Cadrage dans l'état livré par 20-06 — le patron de filet de repli sur outil absent, à reprendre TEL QUEL comme formulation de référence dans la doctrine.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-08 (avec sa divergence signalée : une ligne annoncée par le rapport du matin ne porte PAS la formule attendue — vérifier sur pièce avant d'éditer) et D-09.
    - `.planning/STATE.md` §Decisions, l'entrée du 2026-07-27 sur l'allowlist de dispatch non lintée — le précédent de doctrine fausse corrigée, et le motif de vigilance.
  </read_first>
  <files>plugin/conductor/references/team-kernel.md, plugin/conductor/README.md</files>
  <behavior>
    - La ligne de cloisonnement par outils, dans les deux fichiers, cite le mécanisme qui rend la barrière des juges effective, et ne se contente plus de l'affirmer.
    - Le contrat du noyau d'équipe porte un énoncé de la classe de problème du sens fermeture — un outil déclaré peut être absent au runtime — placé auprès de la restriction analogue déjà documentée, et nommant le filet de repli.
    - La ligne du plan de bataille, dans les deux fichiers, cite le flag de périmètre et le champ de régime.
    - La liste des scripts du module obligatoire compte le nouveau gate d'invariants, et les deux compteurs (scripts et suites) sont exacts.
    - Aucune ligne éditée ne repose sur le rapport de mission du matin plutôt que sur le fichier réel : chaque édition est précédée d'une vérification sur pièce.
  </behavior>
  <action>
Quatre corrections, dans deux fichiers, toutes conditionnées à une vérification sur pièce préalable.

**1. Ligne de cloisonnement par outils.** Dans les deux fichiers, cette ligne affirme que les juges
n'ont pas les outils d'écriture. C'était vrai de leur allowlist et faux du runtime. Compléter la
formule pour qu'elle cite le mécanisme désormais posé sur les 4 juges — le champ d'outils interdits
— et non plus seulement le fait. Conserver intégralement le reste de la ligne, y compris la
parenthèse déjà présente qui explique que l'allowlist de dispatch est un contrat documenté et linté
et non un bac à sable runtime : c'est un acquis, ne pas le diluer.

Concernant l'affirmation, dans la même ligne, que l'anti-triche est vérifié par les suites de test
de chaque module : la CONSTATER, ne pas ouvrir le chantier. Le cadrage la signale pour vigilance
sans la déclarer fausse, et sa vérification est un élément différé. Si le constat la trouve fausse,
le SIGNALER au compte rendu comme un différé nommé — un précédent exact existe dans l'historique du
dépôt, ce qui justifie la vigilance mais pas l'élargissement du périmètre.

**2. Sens fermeture de l'écart déclaré contre runtime.** Ajouter au contrat du noyau d'équipe, à
côté de la ligne où est déjà documentée la restriction analogue sur l'allowlist de dispatch, un
énoncé de la classe symétrique : un outil PRÉSENT dans le champ déclaré peut être ABSENT au runtime.
Le cas est établi et daté — un agent déclare l'outil de question à l'utilisateur mais ne le reçoit
pas quand il est dispatché en sous-agent, et c'est ce qui a gelé une mission. Nommer le filet de
repli en reprenant le patron déjà écrit dans le worker d'exécution : le besoin humain remonte dans
le rapport typé, il n'est jamais auto-répondu en silence. Deux à quatre lignes ; ce fichier est un
contrat, pas un essai.

**3. Ligne du plan de bataille.** Dans les deux fichiers, l'énumération des sous-commandes du kernel
gagne le flag de périmètre, et la garantie associée gagne une proposition sur le champ de régime
forcé à la réouverture des nœuds de revue et de jointure. Citer les noms EXACTS livrés par le plan
de kernel, vérifiés par l'option d'aide du script — jamais des noms supposés.

**4. Compteurs et liste des scripts du module obligatoire.** Le nouveau gate d'invariants entre dans
la liste des scripts par famille, avec une ligne qui dit ce qu'il constate et rappelle qu'il ne juge
pas. Le compteur en titre de la section passe à quatorze, le compteur de suites passe à onze, et le
bloc d'arborescence en fin de fichier — qui répète les deux compteurs — est mis à jour à l'identique.
Vérifier les deux nombres par comptage réel, ne pas les incrémenter de tête.

**Avertissement de méthode, explicite dans le cadrage** : une ligne annoncée par le rapport de
mission du matin comme portant une certaine formule ne la porte PAS en réalité. Chaque édition de ce
plan doit être précédée d'une lecture du fichier réel. Éditer sur la foi d'un rapport plutôt que sur
pièce est exactement le motif d'erreur que cette phase corrige ailleurs.
  </action>
  <verify>
    <automated>test "$(ls plugin/conductor/scripts/*.sh | wc -l | tr -d ' ')" = "$(grep -oE 'Scripts \(([0-9]+)\)' plugin/conductor/README.md | grep -oE '[0-9]+' | head -1)" && test "$(ls plugin/conductor/scripts/tests/test-*.sh | wc -l | tr -d ' ')" = "$(grep -oE '([0-9]+) suites sous' plugin/conductor/README.md | grep -oE '[0-9]+' | head -1)" && echo "compteurs conductor OK"</automated>
  </verify>
  <acceptance_criteria>
    - Les deux compteurs du module obligatoire sont EXACTS, vérifiés par comptage réel du système de fichiers — c'est ce que fait la commande du bloc de vérification, qui échoue si l'un des deux ment.
    - Le nouveau gate figure dans la liste des scripts par famille : `grep -c 'check-mission-invariants' plugin/conductor/README.md` renvoie au moins 1.
    - Le bloc d'arborescence de fin de fichier porte les mêmes deux nombres que la section de liste — vérifié par lecture du diff (deux occurrences de chaque nombre, cohérentes).
    - La ligne de cloisonnement par outils cite le mécanisme dans les DEUX fichiers : une recherche du nom du champ d'outils interdits renvoie au moins une ligne dans chacun.
    - La parenthèse sur l'allowlist de dispatch est préservée : une recherche de l'idée de contrat documenté et non de bac à sable runtime renvoie toujours au moins une ligne dans chacun des deux fichiers.
    - Le sens fermeture est documenté dans le contrat du noyau : `grep -c 'AskUserQuestion' plugin/conductor/references/team-kernel.md` renvoie au moins 1, alors qu'il renvoyait 0 avant la phase.
    - La ligne du plan de bataille cite le flag de périmètre dans les deux fichiers, et le nom cité existe réellement — `bash plugin/conductor/scripts/dag.sh -h` le mentionne.
    - Vérification sur pièce tracée : le compte rendu liste, pour chacune des lignes éditées, le numéro de ligne et la formule RÉELLEMENT trouvée avant édition — la divergence signalée par le cadrage doit apparaître comme confirmée ou infirmée, jamais passée sous silence.
    - Aucun fichier hors des deux n'apparaît dans le diff de cette tâche.
  </acceptance_criteria>
  <done>La doctrine transverse cite les mécanismes réels — barrière des juges, périmètre et régime du plan de bataille — documente le sens fermeture de l'écart déclaré contre runtime, et ses compteurs sont vrais.</done>
</task>

<task type="auto">
  <name>Task 3 : les 6 modules bumpés avec triade cohérente, gates de sortie rejoués, reste-à-faire de release consigné (D-25, SC7)</name>
  <reversibility rating="reversible">Numéros de version et entrées de journal.</reversibility>
  <read_first>
    - `CLAUDE.md` à la racine — la discipline de release, la règle de numérotation (nouvelle capacité en mineur, correctif et durcissement en correctif), et l'exigence de cohérence de la triade par module.
    - `.planning/codebase/CONVENTIONS.md` — la structure d'un module, la triade version/manifeste/journal, et la forme d'une entrée de journal (titre versionné et daté, rubriques Ajouté / Modifié / Corrigé).
    - `plugin/conductor/CHANGELOG.md`, sa dernière entrée **en entier** — le gabarit exact à suivre : niveau de détail, mention du numéro de phase, formulation des motifs.
    - Les fichiers de version et manifestes des 6 modules — pour lire les valeurs COURANTES et non celles supposées par le cadrage : une phase concurrente a pu bumper entre-temps.
    - `scripts/check-version-sync.sh` **en entier** — les six familles de contrôles qu'il effectue, en particulier la triade par module (qu'il faut rendre verte) et le compteur de suites cité par les deux README racine (qui va devenir rouge par construction, et qu'il ne faut PAS corriger ici).
    - `.planning/STATE.md` §Decisions, l'arbitrage daté du 2026-07-28 : le compteur de suites des README racine voyage avec le commit de release racine, réservé à validation humaine — patron déjà appliqué deux fois. C'est le précédent qui fonde la prohibition de le corriger ici.
    - `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-CONTEXT.md` D-25 et la section des idées différées — les quatre éléments à consigner en reste-à-faire.
    - Les comptes rendus des plans 20-01 à 20-06 — pour composer des entrées de journal qui décrivent ce qui a été LIVRÉ, pas ce qui était prévu.
  </read_first>
  <files>plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md, plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/CHANGELOG.md, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/CHANGELOG.md, plugin/business-pilot-bundle/VERSION, plugin/business-pilot-bundle/module.json, plugin/business-pilot-bundle/CHANGELOG.md, plugin/content-bundle/VERSION, plugin/content-bundle/module.json, plugin/content-bundle/CHANGELOG.md, plugin/growth-bundle/VERSION, plugin/growth-bundle/module.json, plugin/growth-bundle/CHANGELOG.md</files>
  <behavior>
    - Chacun des 6 modules porte la même valeur dans son fichier de version et dans son manifeste.
    - Chacun des 6 journaux porte une entrée nouvelle, datée, dont le titre porte la version cible, et dont le corps décrit les changements RÉELLEMENT livrés par la phase pour ce module.
    - Le gate de synchronisation des versions est vert sur le contrôle de triade par module pour les 17 modules du dépôt.
    - Le gate de conformité en mode strict sort 0 sur les 6 dossiers d'agents.
    - La boucle complète des suites n'imprime aucun échec.
    - Le fichier de version racine, le manifeste de plugin, la fiche de marketplace et les deux README racine sont ABSENTS du diff.
  </behavior>
  <action>
**Bumps.** Appliquer le tableau d'arbitrage donné en tête de ce plan, en vérifiant d'abord les
valeurs COURANTES sur disque — une phase concurrente a pu bumper entre-temps, et le cadrage date de
la veille. Si une valeur courante diffère de celle du tableau, appliquer la MÊME nature de saut
(mineur ou correctif) à partir de la valeur réelle, et le signaler au compte rendu. Pour chaque
module : le fichier de version et le champ de version du manifeste portent la même valeur, préfixe
compris — c'est ce que contrôle le gate de synchronisation.

**Journaux.** Une entrée par module, datée, titrée avec la version cible et un résumé court entre
parenthèses mentionnant la phase. Le corps décrit ce qui a été livré POUR CE MODULE, avec le motif —
pas la liste des fichiers touchés, mais ce qui change pour qui consomme le module. Composer ces
entrées à partir des comptes rendus des plans amont, pas du cadrage : ce qui est publié doit décrire
le livré.

Pour le module obligatoire, l'entrée couvre : le périmètre explicite des deux gates de conformité au
démarrage de session et la fin du faux vert qui en découlait ; l'affichage conditionnel des
avertissements en mode hook ; l'élargissement du charset de token à la forme MCP à joker terminal,
avec la mention que c'est ce que produit l'injecteur depuis la décision d'architecture de juillet ;
le portage du mécanisme de préfixes tiers au second gate, en précisant que c'est le mécanisme
EXISTANT et non un second ; le flag de périmètre, le champ de régime et la sortie de périmètres
gelés du plan de bataille ; le nouveau gate d'invariants de mission ; et la mise en conformité de la
doctrine. Y écrire explicitement que le correctif de périmètre des hooks ne reçoit pas de décision
d'architecture dédiée — c'est une correction de configuration, et la distinction est demandée par le
livrable de cadrage.

Pour le module d'orchestration de dev, l'entrée couvre : le mode d'injection MCP nommé et sa clé de
frontmatter ; l'accès outillé du relecteur avec son protocole d'appel et son coût ; l'étage revue de
premier rang, avec le renvoi à la décision d'architecture nouvelle ; le passage du cycle du worker
d'exécution à trois étapes, signalé comme un CHANGEMENT DE CONTRAT pour quiconque dispatche cet
agent ; et le filet de repli sur outil déclaré mais absent au runtime.

Pour les 4 modules de juge, l'entrée couvre le champ d'outils interdits et, pour le seul module de
design, la correction de l'affirmation de barrière complète devenue factuelle.

**Gates de sortie de phase.** Rejouer, dans cet ordre, et consigner chaque résultat au compte rendu
avec sa sortie réelle : la boucle complète des suites, exactement telle que la CI l'écrit ; le gate
de conformité en mode strict sur les 6 dossiers d'agents ; le gate de synchronisation des versions.

Le gate de synchronisation sera ROUGE sur un seul contrôle : le compteur de suites cité par les deux
README racine, passé à 43 réelles du fait de la suite créée en 20-05. Ne PAS le corriger. Le
rattrapage voyage avec le commit de release racine, réservé à validation humaine — patron déjà
appliqué aux phases 13, 17 et 19, et le corriger ici serait s'accorder un morceau de la release
explicitement sortie du périmètre. Le consigner comme un rouge ATTENDU et NOMMÉ, avec sa valeur
cible, pour que la personne qui fera la release n'ait pas à la recalculer.

Si un autre gate échoue, REMONTER le défaut au manager au lieu de le réparer ici : ce plan écrit de
la doctrine, des journaux et des versions ; il ne corrige aucun script, aucun agent, aucun test.

**Reste-à-faire, consigné au compte rendu et repris dans le SUMMARY.** Quatre éléments, chacun avec
ce qu'il faut faire et pourquoi il n'est pas fait ici :

1. *Release racine, hors périmètre.* Bump du fichier de version racine, du manifeste de plugin, de
   la fiche de marketplace, des badges et de l'historique des deux README ; compteur de suites à
   porter à sa valeur réelle dans les deux README ; tag annoté créé et poussé sur le commit de
   release ; publication GitHub sur ce tag ; vérification finale du gate de release en mode distant.
2. *Recette humaine sur un lab iOS équipé.* Valider que les trois noms d'outils employés par
   l'allowlist nommée du relecteur correspondent à ceux qu'expose réellement le serveur. Ce dépôt
   n'a pas de configuration MCP ; l'écart produirait un no-op silencieux, jamais une ouverture
   accidentelle. Même statut que la recette différée de la phase précédente.
3. *Validation de forme des tokens MCP.* Le gate ne valide toujours pas qu'un nom de serveur cité
   dans un token existe. Dette connue, explicitement hors du périmètre des sept critères, à ne pas
   ouvrir sans mandat.
4. *Affirmation sur la vérification par les suites de test.* Statut du constat fait en tâche 2 —
   confirmée, infirmée, ou non vérifiable — et, si elle est infirmée, l'inscrire comme différé
   nommé plutôt que de la corriger hors mandat.
  </action>
  <verify>
    <automated>bash scripts/check-version-sync.sh; echo "check-version-sync rc=$?"; for d in plugin/conductor plugin/dev-orchestrator plugin/design-orchestrator plugin/business-pilot-bundle plugin/content-bundle plugin/growth-bundle; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d/agents" --skills-dir="$d/skills" || exit 1; done; find plugin scripts -type f -path '*/tests/test-*.sh' | sort | while IFS= read -r t; do bash "$t" >/dev/null 2>&1 || echo "KO $t"; done</automated>
  </verify>
  <acceptance_criteria>
    - Triade cohérente sur les 6 modules : pour chacun, la valeur du fichier de version et celle du champ de version du manifeste sont identiques — et le gate de synchronisation ne signale AUCUNE dérive de triade sur les 17 modules du dépôt.
    - Les 6 journaux portent chacun une entrée nouvelle dont le titre contient la version cible et une date, et dont le corps décrit le livré (vérifié par lecture, une entrée par module).
    - Le gate de conformité en mode strict sort 0 sur les 6 dossiers d'agents — la boucle du bloc de vérification s'arrête en erreur sinon.
    - La boucle complète des suites n'imprime aucune ligne d'échec.
    - Le gate de synchronisation échoue sur UN SEUL contrôle, celui du compteur de suites des README racine, et ce rouge est consigné au compte rendu avec sa valeur cible. Tout autre contrôle rouge est un défaut à remonter, pas à absorber.
    - Le périmètre de la release est intact : `git diff --name-only` ne liste ni `VERSION` à la racine, ni `plugin/.claude-plugin/plugin.json`, ni `.claude-plugin/marketplace.json`, ni `README.md`, ni `README.fr.md`. Aucun tag n'a été créé : `git tag --points-at HEAD` ne renvoie rien.
    - Aucun fichier de source n'a été réparé depuis ce plan : `git diff --name-only` ne liste aucun fichier sous `plugin/*/scripts/`, `plugin/*/agents/` ni `plugin/*/references/` autre que les deux fichiers de doctrine de la tâche 2.
    - Les quatre éléments de reste-à-faire figurent dans le compte rendu, chacun avec son geste et son motif de report.
  </acceptance_criteria>
  <done>Les 6 modules sont bumpés avec une triade cohérente et un journal qui décrit le livré, les gates de sortie sont rejoués et consignés, le seul rouge attendu est nommé avec sa valeur cible, et la release racine reste entière pour la validation humaine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine publiée → agents et humains | tout ce qui est écrit ici sera lu comme une garantie ; une affirmation non tenue par le runtime est un contrat trompeur |
| version de module → labs installés | un numéro de version pilote la propagation des changements vers les postes équipés |
| périmètre de phase → périmètre de release | la frontière entre ce qu'une mission peut livrer et ce qui reste à la validation humaine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07-01 | Tampering | doctrine écrite avant la capacité qu'elle décrit | high | mitigate | Ce plan est en DERNIÈRE vague par construction, et dépend des six autres. Chaque affirmation est vérifiée sur pièce contre l'état livré, avec les noms de commandes confrontés à l'option d'aide des scripts réels. |
| T-20-07-02 | Tampering | édition d'une ligne de doctrine sur la foi d'un rapport plutôt que du fichier | high | mitigate | Avertissement explicite du cadrage : une ligne annoncée comme portant une formule ne la porte pas en réalité. Le critère d'acceptation exige que le compte rendu trace la formule RÉELLEMENT trouvée avant chaque édition. |
| T-20-07-03 | Repudiation | compteur de doctrine faux (scripts, suites) | medium | mitigate | Les deux compteurs sont vérifiés par COMPTAGE réel du système de fichiers dans le bloc de vérification, qui échoue si l'un ment. Un compteur incrémenté de tête est le motif classique de dérive de ce dépôt. |
| T-20-07-04 | Elevation of Privilege | mission qui s'accorde un morceau de la release | high | mitigate | Prohibitions P-01 à P-03, et critère d'acceptation qui vérifie l'absence des fichiers de release dans le diff ET l'absence de tag sur le commit. Précédent explicite : deux phases ont déjà refusé ce raccourci. |
| T-20-07-05 | Tampering | plan de gouvernance qui répare un défaut au lieu de le remonter | medium | mitigate | Prohibition P-06 et critère d'acceptation qui vérifie qu'aucun fichier de source n'a été modifié depuis ce plan. Réparer ici masquerait le défaut à la vérification goal-backward. |
| T-20-07-06 | Repudiation | reste-à-faire perdu entre la phase et la release | medium | mitigate | Les quatre différés sont consignés au compte rendu ET dans le résumé de plan, chacun avec son geste et son motif — dont la valeur cible du compteur, pour que la release n'ait rien à recalculer. |
| T-20-07-SC | Tampering | installations npm/pip/cargo | — | accept | Aucune commande d'installation de paquet dans le périmètre de ce plan ni de la phase — audit de légitimité sans objet (20-RESEARCH.md §Package Legitimacy Audit : non applicable). |
</threat_model>

<verification>
- La suite des numéros de décision d'architecture est strictement croissante et sans doublon ; ADR-060 existe une fois.
- La révision d'ADR-051 ne déborde pas de son point contesté, vérifié par lecture du diff.
- Les noms de clé et de commande cités par la doctrine et par les ADR existent réellement, vérifiés par confrontation aux fichiers livrés et à l'option d'aide des scripts.
- Les deux compteurs du module obligatoire sont exacts, vérifiés par comptage réel.
- Les 6 triades de module sont cohérentes ; le gate de synchronisation ne signale aucune dérive de triade sur les 17 modules.
- Le gate de conformité en mode strict sort 0 sur les 6 dossiers d'agents.
- La boucle complète des suites n'imprime aucun échec.
- Le seul rouge du gate de synchronisation est le compteur de suites des README racine, consigné avec sa valeur cible comme reste-à-faire de release.
- Aucun fichier de release et aucun tag n'ont été touchés.
</verification>

<success_criteria>
- SC7 tenu sur tout ce qui appartient à la phase : gates verts, densité respectée, portabilité prouvée par exécution dans les plans amont, 6 modules bumpés. La release racine et le tag restent explicitement hors périmètre, listés en reste-à-faire.
- SC1 et SC3 tenus sur leur volet documentaire : ADR-051 révisée avec argument et coût littéraux, ADR-060 posée.
- SC2, SC4, SC5 et SC6 tenus sur leur volet doctrine transverse : le cloisonnement cite son mécanisme, le sens fermeture est documenté, le plan de bataille cite ses nouveaux champs, les compteurs sont vrais.
- Le changement de périmètre des hooks est classé comme correction de configuration, pas comme changement de doctrine — distinction exigée par le livrable de cadrage.
</success_criteria>

<output>
Create `.planning/phases/VFDO-20-fluidit-du-flux-de-dev-sans-perte-de-qualit/20-07-SUMMARY.md` when done.
</output>

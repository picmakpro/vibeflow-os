---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 09
lot: "L09 — check-state-integrity.sh : ferme le fail-open du chemin absolu (feu vert session principale, 2e passe)"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WSAW-02, WSAW-04]
files_modified:
  - plugin/conductor/scripts/check-state-integrity.sh
  - plugin/conductor/scripts/tests/test-check-state-integrity.sh

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `plugin/conductor/scripts/check-state-integrity.sh` ou sa suite de tests porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh`)"
    - "ORIGINE ET FEU VERT : mesuré indépendamment par le juge frais de 41.1-06 (BLOCK-A) ET par un agent parallèle sur fixtures — CONVERGENCE de deux chemins indépendants sur la même cause, à consigner au SUMMARY (demande explicite de la session principale). `check-state-integrity.sh` l.201 interpole `FILE_REL` verbatim dans `git show \"$AGAINST_REF:$FILE_REL\"` : un `--file` ABSOLU (ou tout chemin que `git show` ne sait pas résoudre en objet) fait échouer ce `git show`, `HAVE_BASELINE` retombe à 0, et l'Invariant 1 (non-régression des compteurs, ADR-063) est SAUTÉ — le gate rend alors `0` (conforme) sur un frontmatter qui ne l'est pas, en s'appuyant sur le seul Invariant 2 (compte de lignes `^Phase:`). Feu vert de la session principale (2026-09-23, sur remontée du manager) : fermer CE fail-open fait partie des conditions de validité de la Phase 41.1 elle-même (multiplier ce gate par N compartiments sans fermer son fail-open industrialiserait le faux vert), mais AU PLUS PETIT — REJETER, PAS RÉPARER"
    - "FERMETURE AU PLUS PETIT (contrat explicite, 3 points) : (1) un `--file` EXPLICITE dont la valeur commence par `/` (chemin absolu) est rejeté EN ERREUR D'USAGE, code 64, AVANT toute tentative `git show` — message qui NOMME la cause (la valeur reçue) et POURQUOI (git show ne résout jamais un chemin absolu). AUCUNE conversion automatique en relatif, AUCUN repli — le gate refuse de juger ce qu'il ne sait pas lire. (2) Le cas « HAVE_BASELINE=0 pour une AUTRE raison » (fichier légitimement absent de HEAD, ex. compartiment fraîchement créé — le cas F13 anti-vert-par-absence déjà documenté en tête de fichier) reste INCHANGÉ : ce script ANNONCE DÉJÀ ce cas (message stderr « aucune référence à $AGAINST_REF... rien à régresser, invariant de compteur ignoré ») quelle qu'en soit la cause — vérifié par lecture directe du code AVANT ce plan, aucun changement n'était nécessaire sur ce point précis, seule la mesure était incomplète. (3) Un chemin absolu envoyé via `--current-ref` combiné à `--file` reste hors usage supporté (déjà documenté en tête de fichier, non touché par ce plan)"
    - "NON-RÉGRESSION : le cas nominal (racine `.planning/STATE.md`, `--file` implicite ou explicite RELATIF) reste rc=0 sur une fixture conforme — INCHANGÉ. Le repli de résolution de workstream actif (FILE_REL_EXPLICIT=0) construit TOUJOURS un `FILE_REL` relatif (`.planning/workstreams/$VF_WS_NAME/STATE.md`) — jamais affecté par ce rejet, qui ne s'arme QUE si `FILE_REL_EXPLICIT=1` ET valeur absolue"
    - "PREUVE PAR MUTATION EXIGÉE (session principale) : LE MÊME artefact fautif (compartiment corrompu, frontmatter réduit à 3 clés dont `milestone:` vide) rend ROUGE (rc=2) quand appelé en RELATIF (`--path <racine>` + `--file <relatif>`), et 64 (usage invalide) quand appelé en ABSOLU (`--file <même chemin, en absolu>`) — la bascule prouve que le silence est FERMÉ, pas déplacé. Mesuré RÉELLEMENT avant l'écriture de ce plan (scratchpad, hors dépôt, sur une copie patchée du script réel) : avant patch, absolu → rc=0 (« ✓ conforme ») ; après patch, absolu → rc=64 (message nommant la cause) ; après patch, MÊME artefact en relatif → rc=2 (« milestone introuvable ou illisible ») ; deux cas légitimes (défaut racine, `--file` relatif explicite) → rc=0 inchangé, non-régression confirmée"
    - "SECOND VECTEUR, FERMÉ PAR CE PLAN (Q2, arbitrage Samuel, AskUserQuestion session principale, 2026-09-23, option b) : le `--file` absolu n'était qu'UNE des portes du fail-open. Le vecteur GÉNÉRAL reste ouvert après la 1re passe — un compartiment dont le `STATE.md` est ABSENT DE `HEAD` (compartiment NEUF, non commité), chemin relatif BIEN FORMÉ, frontmatter CASSÉ → `git show` échoue légitimement, `HAVE_BASELINE` retombe à 0, l'Invariant 1 (non-régression des compteurs, ADR-063) est SAUTÉ, et le gate rend `0` « ✓ conforme ». MESURE RÉELLE (2026-09-23, dépôt git temporaire, `STATE.md` à `gsd_state_version:` + `milestone:` VIDE) : **non commité → rc=0**, stdout « ✓ … conforme (compteurs non régressés, 1 ligne '^Phase:') », l'annonce du saut d'invariant sur **stderr SEULEMENT** ; **le MÊME fichier une fois commité → rc=2** « milestone introuvable ou illisible ». Deux verdicts opposés sur le même contenu, la seule différence étant la présence dans `HEAD`"
    - "POURQUOI CE VECTEUR A SURVÉCU À TROIS TOURS : aucune des 30 bascules rejouées ne l'exerçait, PARCE QUE TOUTES LES FIXTURES SONT COMMITÉES. Le cas 6b existant de la suite emprunte bien le chemin `HAVE_BASELINE=0`, mais sur un frontmatter SAIN — il prouve donc que le skip est annoncé, jamais que le skip peut BLANCHIR un frontmatter cassé. Aucune fixture ne croisait « absent de HEAD » ET « frontmatter cassé ». **Une fixture qui ne reproduit pas la condition du terrain ne prouve rien du terrain** (enseignement explicite de l'arbitrage) : la fixture NEUVE de ce plan est un compartiment **NON COMMITÉ, absent de `HEAD`**"
    - "SIGNAL DÉDIÉ, NON BLOQUANT (Q2) : quand l'Invariant 1 est sauté faute de baseline, le script sort désormais en **3** au lieu de 0 — code LIBRE (0/1/2/64 seuls en usage, vérifié sur le fichier le 2026-09-23) et déjà porteur du sens « SILENCE / distinct du nominal » dans le vocabulaire de ce dépôt (`vf_ws_enumerate`, `check-divergence.sh`). **3 N'EST PAS UN ÉCHEC** : la sémantique de verdict du gate est INCHANGÉE — on ne fait PAS échouer `HAVE_BASELINE=0`, ce qui rougirait sur TOUT compartiment neuf et casserait l'invariant à trois états de D-02. Ce qui change est que la dégradation CESSE D'ÊTRE SILENCIEUSE pour l'appelant : un code de sortie est un contrat, une ligne de stderr n'en est pas un"
    - "RAYON D'ACTION DU CHANGEMENT DE CODE, MESURÉ AVANT ÉCRITURE (2026-09-23) : l'étape `check-state-integrity` du job `gates` porte sur `fiabilite/STATE.md`, **commité** → `HAVE_BASELINE=1` → rc=0 INCHANGÉ. `test-workstream-symlink-escape.sh` commite ses fixtures (l.130-131) → cas B3/D/E2 INCHANGÉS. Dans `test-check-state-integrity.sh`, **un seul cas** assertait rc=0 sur ce chemin : le **cas 6b** (l.~155-168, `--against HEAD~1` sur un premier commit sans `STATE.md`). Ce plan met à jour CE cas — attendu 3 au lieu de 0, skip toujours annoncé — et AUCUN autre. La mise à jour du 6b est une conséquence assumée et nommée, pas une dérive de périmètre"
    - "PÉRIMÈTRE GARDÉ EXPLICITEMENT AU PLUS PETIT (garde-fou de la session principale) : ce plan NE touche PAS la classification a2/exempté de `check-state-integrity.sh` au recensement (`41.1-05`, INCHANGÉE — ce script reste single-target par conception, cette fermeture est une correction de sécurité/robustesse orthogonale à l'adoption de `vf_ws_enumerate`) ; NE modifie AUCUN autre script . CE PLAN reste en wave 1 avec `depends_on: []` — il ne dépend de rien. En revanche, CORRECTION C-20 (juge frais, 2e passe) : `41.1-06` DÉPEND de ce plan et doit le DÉCLARER. C'était déjà vrai au texte (06 nomme le fail-open de `check-state-integrity.sh` et le contourne dans ses propres invocations, 09 le ferme), et l'ordre n'était garanti QUE par les vagues (09 = 1, 06 = 3) — toute exécution qui privilégierait `depends_on` sur `wave`, ou tout rejeu partiel plan par plan, mesurerait la fixture de 06 contre la version PRÉ-09 du gate. Q2 RENFORCE ce lien : 06 consomme désormais le code 3 que CE plan produit. `41.1-06.depends_on` inclut donc `41.1-09`"
  artifacts:
    - path: "plugin/conductor/scripts/check-state-integrity.sh"
      provides: "rejet en usage (64) d'un --file absolu explicite ; sortie 3 dédiée quand l'Invariant 1 est sauté faute de baseline (Q2)"
      contains: "exit 3"
    - path: "plugin/conductor/scripts/tests/test-check-state-integrity.sh"
      provides: "cas de rejet 64 + bascule discriminance relatif/absolu sur le même artefact"
  key_links:
    - "Ligne exacte du défaut : `check-state-integrity.sh` l.201, `git_safe show \"$AGAINST_REF:$FILE_REL\"` — FILE_REL interpolé verbatim, jamais validé avant usage"
    - "Point d'insertion du rejet : immédiatement après la boucle `while [ \"$#\" -gt 0 ]; do ... done` de parsing (~l.99), AVANT `export GIT_CONFIG_NOSYSTEM=1` — le plus tôt possible, avant toute résolution ROOT/workstream"
    - "`FILE_REL_EXPLICIT` existe déjà (posé à 1 uniquement par `--file`, jamais par le repli de résolution de workstream) — c'est le garde-fou qui borne le rejet au seul cas où l'APPELANT a fourni la valeur, jamais un chemin construit en interne par le script lui-même"
---

<objective>
Fermer, AU PLUS PETIT, le fail-open de `check-state-integrity.sh` sur un `--file` absolu : rejeter
en erreur d'usage (64) avant toute tentative `git show`, plutôt que de laisser `HAVE_BASELINE`
retomber à 0 en silence et l'Invariant 1 (non-régression des compteurs, ADR-063) sauté. Ce n'est pas
un élargissement de périmètre — c'est une condition de validité de ce que la Phase 41.1 multiplie
par N compartiments (41.1-06) : un gate qui sait rendre vert en sautant son invariant principal
industrialiserait un faux vert dès qu'il est fan-out.

Purpose: fermer la cause racine que deux chemins indépendants (le plan-checker frais sur 41.1-06,
et une mesure parallèle sur fixtures) ont trouvée SÉPARÉMENT — convergence à consigner, pas juste
un correctif de plus.
Output: `check-state-integrity.sh` rejette un `--file` absolu à l'entrée ; le même artefact fautif
bascule rouge en relatif et 64 en absolu, preuve exécutée.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `plugin/conductor/scripts/check-state-integrity.sh` en entier (278 lignes) — en particulier la
  boucle de parsing (~l.78-99), la construction de `FILE_PATH` (~l.163-166, le `case "$FILE_REL" in
  /*)` déjà présent pour la lecture directe, à NE PAS confondre avec le défaut de ce plan qui porte
  sur `git show "$AGAINST_REF:$FILE_REL"`), et la section HAVE_BASELINE (~l.199-204)
- `plugin/conductor/scripts/tests/test-check-state-integrity.sh` en entier (505 lignes) — structure
  `ok`/`ko`, `mk_git_root`, `write_state`, patron des cas 5/5b et 10/10b (discrimination par
  comparaison directe de deux exécutions) à répliquer pour le nouveau cas
</context>

<discipline>
- `diff`/`ls` proxifiés sur ce poste — `rtk proxy` ; comparaisons `cmp`/`comm`/`cksum`.
- Fixtures `mktemp -d` + `git init`, jamais le dépôt réel.
- Le rejet est une erreur D'USAGE (64), jamais une réparation ou une conversion silencieuse en
  relatif — REJETER, PAS RÉPARER (contrat explicite de la session principale).
- Ne touche à AUCUN autre script, AUCUNE autre suite, AUCUNE étape CI existante — la classification
  a2/exempté de ce script au recensement `41.1-05` est INCHANGÉE.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : rejet en erreur d'usage d'un `--file` absolu, avant toute tentative `git show`</name>
  <files>plugin/conductor/scripts/check-state-integrity.sh, plugin/conductor/scripts/tests/test-check-state-integrity.sh</files>
  <behavior>
    - `--file /chemin/absolu` (explicite) → rc=64, message stderr nommant la valeur reçue et la
      cause (git show ne résout jamais un chemin absolu), AUCUNE tentative git.
    - `--file .chemin/relatif` (explicite) OU `--file` absent (défaut/repli workstream, toujours
      relatif par construction) → comportement INCHANGÉ, non touché par ce rejet.
    - le MÊME artefact fautif (compartiment corrompu committé) appelé en absolu → 64 ; appelé en
      `--path <racine>` + `--file <même chemin, relatif à racine>` → rouge (rc=2, invariant 1
      armé, milestone incohérent détecté) — bascule discriminante sur le même contenu de fichier.
    - cas légitimes existants de la suite (racine par défaut, `--file` relatif explicite,
      `--current-ref`, résolution de workstream actif) : TOUS rejoués sans modification de leur
      fixture, tous verts — non-régression.
  </behavior>
  <action>
Insérer, immédiatement après la boucle `while [ "$#" -gt 0 ]; do ... done` de parsing des options
(juste avant `export GIT_CONFIG_NOSYSTEM=1`) :
```bash
# CORRECTION (fermeture fail-open, feu vert session principale 2026-09-23, plan 41.1-09) : un
# --file ABSOLU explicite rend git show "REF:$FILE_REL" structurellement irrésoluble (git ne
# résout jamais un chemin absolu en objet de dépôt) -> HAVE_BASELINE=0 en silence -> Invariant 1
# (non-régression des compteurs, ADR-063) sauté sans que ce soit une erreur d'usage annoncée.
# Rejet AU PLUS PETIT, à l'entrée, code 64, avant toute tentative git — jamais une conversion
# automatique en relatif, jamais un repli : le gate refuse de juger ce qu'il ne sait pas lire.
if [ "$FILE_REL_EXPLICIT" -eq 1 ]; then
  case "$FILE_REL" in
    /*)
      echo "[check-state-integrity] --file doit être un chemin RELATIF à --path — reçu un chemin absolu ($FILE_REL). git show '<ref>:<chemin>' ne résout jamais un chemin absolu : l'Invariant 1 serait sauté en silence. Usage invalide." >&2
      exit 64
      ;;
  esac
fi
```
**SECOND VECTEUR (Q2) — rendre AUDIBLE le saut de l'Invariant 1.** Le message stderr existant
(l.~201-204, « aucune référence à … rien à régresser, invariant de compteur ignoré ») annonce bien
le saut, mais le script sort en `0` : pour tout appelant qui ne juge QUE le code de sortie — et
c'est exactement ce que fait le fan-out de `41.1-06` (`[ "$_fsi_rc2" -eq 0 ] || _fsi_note …`) —
l'annonce est INVISIBLE AU VERDICT. Remplacer, à l'endroit du verdict final conforme (l.~276,
`exit 0`), la sortie inconditionnelle par une sortie GRADUÉE :

```bash
# CORRECTION Q2 (arbitrage session principale 2026-09-23, option b) : quand l'Invariant 1
# (non-régression des compteurs, ADR-063) a été SAUTÉ faute de baseline, le verdict n'est pas le
# même que « conforme, les deux invariants vérifiés ». Sortir en 3 — code LIBRE (0/1/2/64 seuls en
# usage) et déjà porteur du sens « SILENCE / distinct du nominal » dans ce dépôt.
# 3 N'EST PAS UN ÉCHEC : faire échouer HAVE_BASELINE=0 rougirait sur TOUT compartiment neuf et
# casserait l'invariant à trois états de D-02. Ce qui change, c'est que la dégradation cesse d'être
# SILENCIEUSE POUR L'APPELANT — un code de sortie est un contrat, une ligne de stderr n'en est pas un.
if [ "$HAVE_BASELINE" -eq 0 ]; then
  echo "[check-state-integrity] ✓ $FILE_REL conforme SOUS RÉSERVE — Invariant 1 (non-régression des compteurs) SAUTÉ faute de baseline à $AGAINST_REF. Seul l'Invariant 2 (compte de lignes '^Phase:') a été vérifié."
  exit 3
fi
exit 0
```

Mettre à jour le bloc de codes de sortie en tête de fichier : ajouter `3 = conforme SOUS RÉSERVE,
Invariant 1 sauté faute de baseline — N'EST PAS un échec`, et préciser sous `0` qu'il signifie
désormais « conforme, les DEUX invariants réellement vérifiés ».

**Mise à jour assumée du cas 6b de la suite** (rayon d'action mesuré, cf. must_haves) : `cas 6b`
(l.~155-168) asserte aujourd'hui `rc=0` sur ce chemin — attendu **3**, le skip restant annoncé.
C'est le SEUL cas existant à toucher : l'étape CI porte sur un fichier commité (`HAVE_BASELINE=1`,
rc=0 inchangé) et `test-workstream-symlink-escape.sh` commite ses fixtures (l.130-131).

Ne rien changer d'autre.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/conductor/scripts/check-state-integrity.sh && echo "syntaxe OK"'</automated>
    <fails_when>`bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q
mkdir -p "$T/.planning/workstreams/candidat"
printf "%s\n" "---" "workstream: candidat" "created: \"2026-09-23\"" "milestone: \"\"" "---" "Phase: 1" > "$T/.planning/workstreams/candidat/STATE.md"
git -C "$T" add -A >/dev/null 2>&1
git -C "$T" -c user.email=t@t -c user.name=t commit -q -m x >/dev/null 2>&1
rc=0; out=$(bash plugin/conductor/scripts/check-state-integrity.sh --file "$T/.planning/workstreams/candidat/STATE.md" 2>&1) || rc=$?
echo "absolu rc=$rc"; printf "%s\n" "$out"
[ "$rc" -eq 64 ]
'</automated>
    <fails_when>`absolu rc=` différent de 64 — un `--file` absolu explicite doit être rejeté en erreur d'usage, jamais silencieusement dégradé en « conforme »</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q
mkdir -p "$T/.planning/workstreams/candidat"
printf "%s\n" "---" "workstream: candidat" "created: \"2026-09-23\"" "milestone: \"\"" "---" "Phase: 1" > "$T/.planning/workstreams/candidat/STATE.md"
git -C "$T" add -A >/dev/null 2>&1
git -C "$T" -c user.email=t@t -c user.name=t commit -q -m x >/dev/null 2>&1
rc=0; out=$(bash plugin/conductor/scripts/check-state-integrity.sh --path "$T" --file ".planning/workstreams/candidat/STATE.md" 2>&1) || rc=$?
echo "relatif_meme_artefact rc=$rc"; printf "%s\n" "$out"
[ "$rc" -ne 0 ] && [ "$rc" -ne 64 ]
'</automated>
    <fails_when>`relatif_meme_artefact rc=` égal à 0 (le MÊME artefact corrompu doit rester détecté quand appelé correctement — sinon la bascule ne prouve rien) ; ou égal à 64 (un chemin relatif bien formé n'est pas une erreur d'usage)</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
git -C "$T" init -q
mkdir -p "$T/.planning"
printf "%s\n" "---" "gsd_state_version: 1.0" "milestone: m1" "current_phase: 1" "status: executing" "progress:" "  total_phases: 1" "  completed_phases: 0" "  total_plans: 1" "  completed_plans: 0" "---" "Phase: 1" > "$T/.planning/STATE.md"
git -C "$T" add -A >/dev/null 2>&1
git -C "$T" -c user.email=t@t -c user.name=t commit -q -m x >/dev/null 2>&1
rc=0; out=$(bash plugin/conductor/scripts/check-state-integrity.sh --path "$T" 2>&1) || rc=$?
echo "legitime_defaut rc=$rc"; printf "%s\n" "$out"
[ "$rc" -eq 0 ]
'</automated>
    <fails_when>`legitime_defaut rc=` différent de 0 — non-régression : le cas nominal (racine, `--file` implicite) doit rester conforme sur une fixture saine</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n` passe
    - `--file` absolu explicite → rc=64, message nommant la cause
    - le MÊME artefact fautif en relatif bien formé → toujours détecté (ni 0, ni 64)
    - cas nominal (racine, défaut) → rc=0 inchangé
  </acceptance_criteria>
  <done>Le fail-open du chemin absolu est fermé par rejet en erreur d'usage, jamais par réparation silencieuse, sans régression sur les cas légitimes existants.</done>
</task>

<task type="auto">
  <name>Task 2 : suite — cas de rejet + bascule discriminance relatif/absolu, rejeu gates/tests</name>
  <files>plugin/conductor/scripts/tests/test-check-state-integrity.sh</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/conductor/scripts/tests/test-check-state-integrity.sh` en entier (déjà lu en Task 1, relire les cas 5/5b et 10/10b pour répliquer leur patron de discrimination par comparaison)
  </read_first>
  <action>
Ajouter un cas « --file absolu → 64 » : `mk_git_root`, `write_state` avec un frontmatter corrompu
(motif déjà en usage dans cette suite pour les cas d'invariant rompu), commité, puis exécution du
script avec `--file "$dir/.planning/STATE.md"` (absolu) — asserte `rc=64` ET que le message stderr
contient « absolu ».

Ajouter un cas « bascule discriminance » (patron 5/5b, 10/10b déjà en usage dans cette suite) : le
MÊME contenu de fichier corrompu, exécuté DEUX FOIS — une fois via `--file` absolu (attendu 64),
une fois via `--path <racine>` + `--file` relatif (attendu rc≠0, rc≠64 — le gate reste capable de
juger). `cmp` entre les deux codes de sortie confirme qu'ils DIFFÈRENT (jamais le même verdict).

**Ajouter le cas Q2 « compartiment NON COMMITÉ + frontmatter cassé » — libellé `Q2-NOBASE`.** Ce
cas est la RAISON pour laquelle le vecteur a survécu à trois tours : toutes les fixtures de ce parc
sont commitées, et le seul cas qui emprunte le chemin `HAVE_BASELINE=0` (le 6b) le fait sur un
frontmatter SAIN. *Une fixture qui ne reproduit pas la condition du terrain ne prouve rien du
terrain.* La fixture doit donc être **absente de `HEAD`** :

```
mk_git_root ; commit vide (le dépôt a un HEAD, mais PAS le fichier)
écrire .planning/workstreams/neuf/STATE.md avec un frontmatter CASSÉ (gsd_state_version présent,
  milestone VIDE) — NE PAS `git add`, NE PAS commiter
exécuter le gate en RELATIF, chemin bien formé   → attendu rc=3 (Invariant 1 sauté, annoncé)
puis `git add -A && git commit`, réexécuter      → attendu rc=2 (milestone introuvable)
```

Assertion : les deux codes **DIFFÈRENT**, `3` puis `2`, et le `3` porte « SAUTÉ » sur sa sortie.

**LIBELLÉS OBLIGATOIRES** (CORRECTION C-10, juge frais, 2e passe) — chaque cas neuf imprime son
libellé littéral dans sa ligne de résultat, parce que c'est la SEULE chose qu'une sonde extérieure
peut chercher ; sans libellé, un cas non écrit est indétectable et le bloc de vérification reste
vert à vide. À l'octet près : `ABSOLU-64` (rejet du `--file` absolu), `BASCULE-REL-ABS` (bascule
discriminance relatif/absolu sur le même artefact), `Q2-NOBASE` (compartiment non commité).

BASCULE MESURÉE le 2026-09-23 sur le script d'AVANT ce plan, avec cette fixture exacte : non
commité → **rc=0** « ✓ … conforme (compteurs non régressés, 1 ligne '^Phase:') » ; commité →
**rc=2** « milestone introuvable ou illisible ». Deux verdicts opposés sur le même contenu. La
fixture rend donc ROUGE aujourd'hui (elle attend 3, elle obtient 0) et ne peut passer au vert que
quand le signal existe réellement.

Rejouer TOUS les cas existants de la suite (505 lignes) sans modification de leur fixture — à la
SEULE exception du **cas 6b**, dont l'attendu passe de `0` à `3` (conséquence nommée et mesurée du
signal dédié, cf. must_haves). La preuve de non-régression est que tous les AUTRES passent SANS
ÊTRE ÉDITÉS ; les trois nouveaux cas (rejet 64, bascule discriminance, `Q2-NOBASE`) sont ajoutés.

Rejouer ensuite les jobs `gates` et `tests` complets via l'outil de rejeu existant et consigner le
bilan au SUMMARY — **et consigner explicitement la CONVERGENCE** (demande de la session principale) :
le plan-checker frais de `41.1-06` (BLOCK-A) et une mesure indépendante sur fixtures ont trouvé
CETTE MÊME cause racine par deux chemins séparés, avant que ce plan ne l'écrive.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
bash plugin/conductor/scripts/tests/test-check-state-integrity.sh > "$T/o" 2>&1; rc_suite=$?
ko=$(awk "index(\$0, \"✗\") > 0 {c++} END{print c+0}" "$T/o")
manq=""
for lab in ABSOLU-64 BASCULE-REL-ABS Q2-NOBASE; do
  awk -v L="$lab" "index(\$0, L) > 0 {f=1} END{exit !f}" "$T/o" || manq="$manq $lab"
done
echo "suite_rc=$rc_suite ko=$ko cas_manquants=[${manq# }]"
[ "$rc_suite" -eq 0 ] && [ "$ko" -eq 0 ] && [ -z "$manq" ]'</automated>
    <fails_when>`suite_rc=` différent de 0 ; ou `ko=` différent de 0 (une ligne `✗`) ; ou `cas_manquants=[…]` non vide. CORRECTION C-10 (juge frais, 2e passe) : la commande précédente était un `bash … test-check-state-integrity.sh` NU — ses clauses « une ligne `✗` », « le cas de rejet 64 absent (chercher « absolu ») » et « le cas de bascule discriminance absent » n'étaient RECHERCHÉES PAR RIEN, alors qu'elles sont la SEULE garde machine des cas NEUFS de ce plan. Mesuré le 2026-09-23 sur la suite d'AVANT cette phase : **rc=0, 40 `✓`, 0 `✗`, et les trois libellés ABSENTS** — le bloc restait donc VERT si aucun des trois cas n'était jamais écrit. Bascule remesurée sur la sonde de remplacement : suite actuelle → `cas_manquants=[ABSOLU-64 BASCULE-REL-ABS Q2-NOBASE]`, rc=1</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0 — toute autre suite du parc (notamment `test-check-divergence.sh`, `test-workstream-policy.sh`) doit rester verte</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^BILAN REJEU gates/" "$T/g"; echo "gates rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 — l'étape `check-state-integrity` du job `gates` (câblée en dur sur `fiabilite/STATE.md` avant 41.1-06, ou fan-out après) doit rester verte sur ce dépôt</fails_when>
  </verify>
  <acceptance_criteria>
    - suite existante rejouée sans modification, tous les cas préexistants verts
    - nouveau cas « --file absolu → 64 » vert, message contient « absolu »
    - nouveau cas de bascule discriminance (même contenu, deux codes de sortie différents) vert
    - rejeu `gates` et `tests` verts sur la découverte complète
    - SUMMARY consigne explicitement la convergence des deux mesures indépendantes (plan-checker frais + mesure fixtures parallèle)
  </acceptance_criteria>
  <done>Le rejet d'un `--file` absolu est prouvé par une suite qui rejoue tous les cas existants sans les toucher, ajoute le cas de rejet et la bascule discriminante, et les deux jobs CI restent verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| appelant → `--file` | tout appelant de ce script (un agent, un futur plan, un opérateur en ligne de commande) peut passer une valeur `--file` non maîtrisée — un chemin absolu désarmait silencieusement l'invariant principal du gate |
| fixture jetable → dépôt réel | toutes les mesures de ce plan vivent dans `mktemp -d`, jamais sur le checkout — aucune commande git de ce plan ne touche `.` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-17 | Tampering | un `--file` absolu contourne silencieusement l'Invariant 1 (non-régression des compteurs), rendant `0` sur un frontmatter réellement régressé | high | mitigate | rejet en erreur d'usage (64) avant toute tentative `git show`, prouvé par mutation sur le même artefact (relatif rouge, absolu 64) |
| T-41.1-18 | Repudiation | un chemin relatif légitimement absent de HEAD (compartiment neuf) pourrait être confondu avec le cas absolu et rejeté à tort | medium | mitigate | le rejet ne s'arme QUE sur le préfixe `/` — un chemin relatif jamais concerné, quel que soit son état dans l'historique git ; cas F13 (fichier légitimement absent) inchangé et re-vérifié par lecture directe du code |
</threat_model>

<verification>
`bash -n` ; `--file` absolu explicite → rc=64 avec message nommant la cause ; le même artefact en
relatif reste détecté (rc≠0, rc≠64) ; cas nominal inchangé (rc=0) ; suite complète verte (préexistants
+ deux nouveaux cas) ; rejeu `gates` et `tests` verts.
</verification>

<success_criteria>
`check-state-integrity.sh` rejette un `--file` absolu à l'entrée, en erreur d'usage nommée, avant
toute tentative de comparaison git — fermant le fail-open que deux mesures indépendantes ont trouvé
par des chemins séparés, sans réparation silencieuse et sans régresser aucun cas légitime existant.
</success_criteria>

## Artifacts this phase produces

- `plugin/conductor/scripts/check-state-integrity.sh` — rejet (64) d'un `--file` absolu explicite.
- `plugin/conductor/scripts/tests/test-check-state-integrity.sh` — cas de rejet + bascule discriminance.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-09-SUMMARY.md` when done
</output>


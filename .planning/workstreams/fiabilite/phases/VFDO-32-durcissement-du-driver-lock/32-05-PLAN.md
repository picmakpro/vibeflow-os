---
phase: 32-durcissement-du-driver-lock
plan: 05
type: execute
wave: 4
depends_on: [32-03]
files_modified:
  - plugin/conductor/scripts/check-guard-health.sh
  - plugin/conductor/scripts/tests/test-check-guard-health.sh
  - plugin/conductor/hooks/hooks.json
  - docs/HOOKS-CONTRAT-SORTIE.md
  - README.md
  - README.fr.md
autonomous: true
abandonnable: true
requirements: [QUAL-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Les marqueurs de sante ecrits par vf_guard_unavailable ont ENFIN un lecteur — jusqu'ici zero consommateur dans tout plugin/ (mesure 32-TERRAIN.md §11)"
    - "Le lecteur est GENERIQUE : il agrege les marqueurs de TOUTES les entrees du parc, pas seulement ceux du guard du verrou — c'est ce qui justifie l'extension de perimetre (D-32-QUAL)"
    - "Il SIGNALE, il ne corrige ni ne bloque jamais, et il n'ECRIT rien (ADR-031) — un lecteur qui elague ses marqueurs serait un correcteur deguise"
    - "Contrat a quatre codes, identique a check-branch-claim.sh : signal · SAIN verifie · INDETERMINE · erreur d'usage — SAIN et INDETERMINE ne se confondent JAMAIS"
    - "Sous son drapeau de hook, stdout est STRICTEMENT VIDE quand il n'y a rien a signaler (le silence est un contrat de FLUX, docs/HOOKS-CONTRAT-SORTIE.md §3)"
    - "Un marqueur perime au-dela de la fenetre de rapport n'est pas signale — vf_guard_unavailable REECRIT son marqueur a chaque invocation, donc un horodatage ancien signifie que la panne a cesse"
    - "Son entree SessionStart nait de merge-hooks en forme exec, comme les autres (D-32-C)"
  artifacts:
    - plugin/conductor/scripts/check-guard-health.sh
    - plugin/conductor/scripts/tests/test-check-guard-health.sh
    - "plugin/conductor/hooks/hooks.json : une entree SessionStart en forme exec"
    - "docs/HOOKS-CONTRAT-SORTIE.md : inventaire porte a 29 entrees, assertion de recomptage mise a jour avec lui"
  key_links:
    - "vf-portable.sh vf_guard_unavailable (ECRIT le marqueur) -> check-guard-health.sh (LE LIT) : le couple qui rend l'issue 4 de QUAL-01 reellement bruyante, au lieu de tracee dans un fichier que personne n'ouvre"
    - "guard-driver-lock.sh (plan 32-03, issue 4) -> marqueur -> doctor : la boucle que cette phase referme sur elle-meme"
    - "hooks.json (SessionStart, forme exec) -> merge-hooks -> settings.local.json -> gate PORT-05 : expect_total bouge d'une unite de plus"
  prohibitions:
    - "Ne JAMAIS ecrire, elaguer ni reecrire un marqueur — lecture seule stricte, prouvee par un cas de test comparant l'etat du repertoire avant et apres"
    - "Ne JAMAIS bloquer ni proposer un correctif automatique (ADR-031) : le doctor signale, l'humain arbitre"
    - "Ne JAMAIS rendre un SAIN quand le repertoire de sante est illisible — ce serait exactement le vert de complaisance que QUAL-01 interdit"
    - "Ne pas specialiser le lecteur sur le guard du verrou : un lecteur special-lock ne justifierait pas l'extension de perimetre"
    - "Ne pas emettre plus d'une ligne sur stdout : une injection de contexte de session verbeuse est une regression pour tout le parc"
---

<objective>
Livrer le « hook doctor » specifie depuis le 2026-08-02 et jamais ecrit : un lecteur `SessionStart`
generique des marqueurs de sante des gardes du parc, qui rend enfin visible le troisieme etat
« le garde n'a pas pu tourner » — celui que la mesure a montre invisible par tous les autres canaux.

Purpose: sans lui, l'issue 4 de QUAL-01 (« fail-open BRUYANT ») est un mot. La mesure est sans
appel : un code de sortie non nul avec diagnostic sur la sortie d'erreur n'atteint ni le modele ni
l'humain en non-interactif ; un message porte par une decision permissive n'atteint pas le modele ;
et le marqueur de sante n'a AUCUN consommateur dans tout `plugin/`. Ecrire « bruyant » en sachant
que personne ne lit le signal serait le vert de complaisance que QUAL-01 existe pour interdire.

Output: `check-guard-health.sh`, sa suite, son entree `SessionStart` en forme exec, et l'inventaire
durable des hooks remis a l'heure.

**⚠ CE PLAN EST ABANDONNABLE — c'est le sacrifice designe en premier si la phase deborde
(D-32-QUAL).** S'il est coupe, la dette doit etre TRACEE explicitement par le plan de cloture
(32-06) et non passee sous silence : le chemin « garde indisponible » du guard du verrou redevient
un fail-open silencieux SANS LECTEUR, et QUAL-01 retombe sur son trou pour tout le parc. Le plan de
cloture porte la tache correspondante.

**Perimetre — pourquoi ce livrable transverse vit quand meme dans `conductor`** : la lib de
portabilite de l'engine designe deja nommement le hook doctor comme appartenant a `conductor`
(`plugin/_internal/lib/vf-portable.sh:141`), et `conductor` est un module `mandatory` present dans
toute fermeture — un lecteur pose la atteint donc tout le parc sans qu'aucune ligne ne soit ecrite
hors de `plugin/conductor/**` cote code. D-32-07 est respecte litteralement pour le code ; les
ecritures hors conductor de ce plan sont l'inventaire durable des hooks et les deux compteurs de
suites des README, tous deux imposes par des gates du depot (voir §Ecart signale).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-TERRAIN.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-03-SUMMARY.md
@docs/HOOKS-CONTRAT-SORTIE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tache 1 : le lecteur generique de marqueurs de sante, avec son contrat a quatre codes</name>
  <files>plugin/conductor/scripts/check-guard-health.sh, plugin/conductor/scripts/tests/test-check-guard-health.sh, README.md, README.fr.md</files>
  <read_first>
    - plugin/_internal/lib/vf-portable.sh:132-158 (`vf_guard_unavailable` : le FORMAT exact du marqueur — horodatage, script, motif, separes par des tabulations —, son emplacement par defaut, le fait qu'il REECRIT le fichier et non l'ajoute, et la note qui designe `conductor` comme proprietaire du futur doctor)
    - plugin/conductor/scripts/check-branch-claim.sh:1-80 (LE patron a reprendre integralement : en-tete de contrat a quatre codes, options, `diag` sur la sortie d'erreur, `hook_exit` et sa traduction du silence interne)
    - plugin/conductor/scripts/check-branch-claim.sh:99-104 (« pas de lock = personne ne pilote, etat VERIFIE donc SAIN » — la distinction SAIN / INDETERMINE a transposer ici)
    - docs/HOOKS-CONTRAT-SORTIE.md §1-§3 (codes de sortie, code reserve au harness, silence comme contrat de FLUX)
    - docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md:205-207 (la specification d'origine du hook doctor, jamais implementee — a lire pour ne pas re-specifier de travers)
    - plugin/conductor/scripts/tests/test-check-branch-claim.sh (convention de suite du dossier ET patron de test d'un script a quatre codes)
    - README.md:124 et README.fr.md:128 (les DEUX compteurs vivants, gates par check-version-sync.sh §9 — ne toucher aucune autre mention chiffree)
  </read_first>
  <behavior>
    - D1 (signal) : un marqueur recent dans le repertoire de sante redirige — le script emet EXACTEMENT une ligne sur la sortie standard, nommant le script en panne et son motif, et sort avec le code de signal.
    - D2 (SAIN) : repertoire de sante existant et VIDE — sortie standard STRICTEMENT VIDE (zero octet), code SAIN ; sous le drapeau de hook, le code devient zero.
    - D3 (SAIN) : repertoire de sante ABSENT — meme verdict que D2 : c'est un etat VERIFIE (aucun garde n'a jamais echoue), pas une indetermination.
    - D4 (INDETERMINE, panne injectee) : repertoire de sante present mais illisible (permissions retirees) — sortie standard vide, diagnostic sur la sortie d'erreur, code INDETERMINE ; jamais un SAIN. Les permissions sont restaurees quoi qu'il arrive.
    - D5 (fenetre de rapport) : un marqueur dont l'horodatage depasse la fenetre — non signale, verdict SAIN. Le motif est ecrit dans l'en-tete : le marqueur est REECRIT a chaque invocation du garde en panne, donc un horodatage ancien signifie que la panne a cesse.
    - D6 (compacite) : quatre marqueurs recents — une SEULE ligne sur la sortie standard, portant le compte et le plus recent, jamais quatre lignes.
    - D7 (marqueur malforme) : marqueur vide, ou sans separateur attendu, ou binaire — aucun plantage, au plus une ligne, code dans l'ensemble {signal, SAIN}.
    - D8 (traduction de codes) : sous le drapeau de hook, les codes SAIN et INDETERMINE deviennent zero ; le code de signal et le code d'erreur d'usage ne sont JAMAIS traduits.
    - D9 (lecture seule stricte) : apres une execution avec un marqueur present, le contenu du repertoire de sante est IDENTIQUE — meme liste de fichiers, memes tailles, memes horodatages de modification. Aucun elagage, aucune reecriture.
    - D10 (usage) : argument inconnu — code d'erreur d'usage, sortie standard vide.
    - D11 (GENERICITE, le cas qui justifie l'extension de perimetre) : un marqueur portant le nom d'un garde d'un AUTRE module que `conductor` est signale exactement comme les autres — le lecteur ne connait aucun garde en particulier.
    - D12 (anti-vert-a-vide) : la suite compte ses assertions executees et sort en echec si le total est zero.
  </behavior>
  <action>
Creer `plugin/conductor/scripts/check-guard-health.sh`, en pur bash (aucun interprete externe n'est
necessaire : lire des fichiers a trois champs separes par des tabulations et comparer des
horodatages ne le justifie pas, et un `SessionStart` qui spawn pour rien est une regression de
latence pour tout le parc).

En-tete de commentaires, sur le modele exact de `check-branch-claim.sh` : role et distinction
FAIT / JUGEMENT (le script CONSTATE que des gardes n'ont pas pu tourner ; il ne corrige rien, ne
qualifie rien d'erreur, l'arbitrage appartient a l'humain — ADR-031) · origine (le troisieme etat
du contrat de marqueur, ecrit depuis la Phase 30 et jamais lu — citer la mesure : zero consommateur
dans tout `plugin/`) · usage et options · CONTRAT DE SORTIE a quatre codes, avec la meme insistance
que le modele sur le fait que SAIN et INDETERMINE ne se confondent jamais · la fenetre de rapport
et SON MOTIF (le marqueur est reecrit a chaque invocation du garde en panne) · lecture seule
stricte.

Comportement :

1. Options : un drapeau de hook (silence interne traduit vers zero a la frontiere du harness, via
   une fonction de traduction recopiee du modele), un drapeau de silence, une surcharge du
   repertoire de sante, une surcharge de la fenetre de rapport. Tout argument inconnu sort avec le
   code d'erreur d'usage.

2. Repertoire de sante : la surcharge d'abord, sinon la variable d'environnement dediee, sinon le
   MEME chemin par defaut que celui calcule par la lib de portabilite — le derivant de la meme
   maniere, pour qu'un ecart de convention entre l'ecrivain et le lecteur soit impossible. Ecrire ce
   couplage en commentaire : c'est le seul endroit du depot ou ces deux chemins doivent s'accorder.

3. Repertoire absent : verdict SAIN, sortie standard vide. Repertoire present mais non listable :
   verdict INDETERMINE avec diagnostic sur la sortie d'erreur — jamais un SAIN de complaisance.

4. Parcours des fichiers de marqueur : pour chacun, lire la premiere ligne, en extraire le nom de
   script et le motif ; determiner la fraicheur par l'horodatage porte par le marqueur, avec repli
   sur la date de modification du fichier si cet horodatage est absent ou illisible (meme esprit
   que le repli deja en place dans le calcul d'age du verrou : un marqueur partiel doit rester
   exploitable au lieu d'etre ignore en silence). Un marqueur malforme n'interrompt jamais le
   parcours.

5. Aucun marqueur frais : verdict SAIN, sortie standard vide. Au moins un : UNE SEULE ligne sur la
   sortie standard, prefixee d'une etiquette entre crochets sur le modele du signal de
   `check-branch-claim.sh`, portant le compte de gardes concernes, le plus recent avec son motif, et
   la marche a suivre — sans jamais l'executer (l'installation d'un interprete ou la mise a jour du
   plugin sont des gestes humains).

6. Lecture seule ABSOLUE : aucune ecriture, aucune suppression, aucun `touch`. Ecrire ce point en
   commentaire avec son motif : un lecteur qui elaguerait ses marqueurs serait un correcteur
   deguise, et l'elagage effacerait la trace au moment ou elle devient utile.

Creer ensuite `plugin/conductor/scripts/tests/test-check-guard-health.sh` a la convention exacte du
dossier, avec les cas D1 a D12. Points de forme imposes :
  - le repertoire de sante est TOUJOURS redirige vers le repertoire de travail temporaire ; aucun
    cas ne doit pouvoir lire ou toucher le repertoire de sante reel du poste — ajouter un preflight
    qui le verifie et sort **2** s'il echoue (fixture casse, pas de verdict) ;
  - les marqueurs de fixture sont fabriques par la suite au FORMAT REEL de la lib de portabilite ;
    consigner dans le SUMMARY la ligne de fixture exacte, pour qu'une future evolution du format
    cote lib soit reperable ;
  - D5 forge son horodatage ancien, il n'attend jamais ;
  - D4 restaure les permissions dans tous les cas, y compris si son assertion echoue, sinon le
    nettoyage de fin de suite echouerait a supprimer le repertoire de travail ;
  - D9 compare un instantane du repertoire pris avant et apres (liste, tailles, horodatages de
    modification) ;
  - epilogue avec compteur d'assertions et garde anti-vert-a-vide.

**Dans le MEME commit** : mesurer le nombre reel de suites par la commande de decouverte de la CI
APRES creation de la suite, et reporter la valeur MESUREE dans les deux compteurs vivants des
README, en preservant mot pour mot la formulation environnante de chaque fichier. Ne toucher aucune
autre mention chiffree.
  </action>
  <acceptance_criteria>
    - `bash -n plugin/conductor/scripts/check-guard-health.sh` sort 0.
    - Contrat a quatre codes documente en en-tete : `sed -n '1,60p' plugin/conductor/scripts/check-guard-health.sh | grep -ci 'INDETERMINE'` rend au moins `1`.
    - Lecture seule stricte, prouvee par le code et pas seulement par un test (LIGNES DE CODE SEULES, commentaires filtres) : `grep -v '^[[:space:]]*#' plugin/conductor/scripts/check-guard-health.sh | grep -cE '(^|[^A-Za-z0-9_-])(rm|mv|touch)[[:space:]]'` rend `0`, et la meme extraction ne contient aucune redirection d'ecriture vers le repertoire de sante. **(MI-5, ancre corrigee)** L'ancre `(^|[^-])` de la formulation initiale matchait des sous-chaines de mots ordinaires (`confirm `, `form `, `informe ` contiennent toutes `rm `/`orm ` precede d'une LETTRE, pas d'un tiret) et aurait pu faire echouer ce critere sur un commentaire ou un message parfaitement legitimes ; l'ancre `[^A-Za-z0-9_-]` (debut de ligne OU caractere non alphanumerique) exclut ces faux positifs tout en detectant reellement `rm `, `mv ` et `touch ` en position de commande.
    - Le script ne spawn aucun interprete externe : `grep -v '^[[:space:]]*#' plugin/conductor/scripts/check-guard-health.sh | grep -cE 'python|jq'` rend `0`.
    - `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` sort 0 et affiche les blocs D1 a D12.
    - La suite est decouverte : `find plugin scripts -type f -path '*/tests/test-*.sh' | grep -c 'test-check-guard-health.sh'` rend `1`.
    - Les deux compteurs sont a la valeur mesuree : `N=$(find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l | tr -d ' '); grep -q "$N suites in CI" README.md && grep -q "\`$N suites\` en CI" README.fr.md`
    - `bash scripts/check-version-sync.sh` sort 0.
    - (SE-5, critere manquant identifie en revue — cette suite est NEUVE, elle porte le meme critere que les suites existantes) Aucune attente temporelle : `grep -c '^[[:space:]]*sleep ' plugin/conductor/scripts/tests/test-check-guard-health.sh` rend `0`.
    - Anti-vert-a-vide prouve par execution : neutraliser temporairement tous les appels d'assertion, relancer, verifier un code de sortie de 1, puis annuler la mutation.
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : (a) retirer le filtre de fenetre de rapport rend D5 rouge ; (b) faire elaguer les marqueurs apres lecture rend D9 rouge ; (c) faire rendre SAIN quand le repertoire est illisible rend D4 rouge ; (d) emettre une ligne par marqueur au lieu d'une seule rend D6 rouge ; (e) emettre un diagnostic sur la sortie standard dans le chemin SAIN rend D2 rouge (c'est la moitie « zero octet » de l'assertion qui l'attrape). Consigner assertion, attendu, obtenu pour chacune ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash -n plugin/conductor/scripts/check-guard-health.sh && bash plugin/conductor/scripts/tests/test-check-guard-health.sh && bash scripts/check-version-sync.sh</automated>
  </verify>
  <done>Les marqueurs de sante ont un lecteur generique, a quatre codes, en lecture seule stricte, silencieux quand il n'y a rien a dire, et qui ne confond jamais « verifie et sain » avec « je n'ai rien pu verifier ».</done>
  <reversibility rating="reversible">Deux fichiers neufs, non encore branches par une entree de hook : tant que la tache 2 n'a pas tourne, le script ne s'execute nulle part.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : boucler la boucle — le doctor voit reellement le marqueur ecrit par le guard du verrou</name>
  <files>plugin/conductor/scripts/tests/test-check-guard-health.sh</files>
  <read_first>
    - le cas Q4 de `plugin/conductor/scripts/tests/test-guard-driver-lock.sh` (plan 32-03) : c'est LUI qui fabrique un environnement sans interprete et provoque l'ecriture d'un marqueur reel — le meme montage est reutilise ici, pas reinvente
    - le SUMMARY du plan 32-03 (chemin de marqueur observe lors de Q4)
  </read_first>
  <behavior>
    - D13 (BOUCLE COMPLETE, bout en bout) : provoquer une vraie panne du guard du verrou — l'invoquer avec un chemin de recherche prive d'interprete et le repertoire de sante redirige —, puis invoquer le doctor sur ce MEME repertoire redirige ; le doctor emet exactement une ligne nommant le guard du verrou. Aucun marqueur n'est fabrique a la main dans ce cas : c'est le garde reel qui l'ecrit.
  </behavior>
  <action>
Ajouter le cas D13 a `plugin/conductor/scripts/tests/test-check-guard-health.sh`. C'est le seul cas
de la suite qui n'utilise pas de marqueur de fixture : il fait ecrire le marqueur par le vrai
producteur, puis le fait lire par le vrai lecteur.

Deroule : reprendre le montage du cas Q4 de la suite du guard du verrou (dossier de binaires ne
contenant que les outils necessaires, aucun interprete, repertoire de sante redirige vers le
repertoire de travail) ; invoquer `guard-driver-lock.sh` avec un payload qui aurait ete refuse en
temps normal ; asserer que le code rendu est celui de la garde et que la sortie standard est vide ;
puis invoquer `check-guard-health.sh` sur le meme repertoire de sante et asserer qu'il emet une
ligne nommant le guard du verrou.

PREFLIGHT SEPARE, obligatoire, sortant **2** en cas d'echec : verifier que le guard du verrou existe
et est executable, et que le repertoire de sante redirige est vide AVANT l'invocation. Sans ce
preflight, un marqueur residuel d'un autre cas ferait passer D13 sans qu'aucune boucle n'ait ete
prouvee — le faux vert exact que ce cas existe pour interdire.

Si le plan 32-03 n'a pas ete execute (le guard du verrou est absent), ce cas doit se SAUTER
explicitement en l'annoncant, jamais echouer silencieusement ni etre compte comme un succes.
  </action>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` sort 0 et affiche le bloc D13 avec un verdict de succes (pas un saut) sur un arbre ou le plan 32-03 a ete execute.
    - Le cas n'utilise aucun marqueur de fixture : `sed -n '/=== D13/,/^echo/p' plugin/conductor/scripts/tests/test-check-guard-health.sh | grep -c 'guard-driver-lock.sh'` rend au moins `1`.
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : remplacer l'appel a `vf_guard_unavailable` du guard du verrou par une sortie zero muette rend D13 rouge — c'est la preuve que le cas mesure la BOUCLE et non la seule presence d'un fichier. Consigner assertion, attendu, obtenu ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/conductor/scripts/tests/test-check-guard-health.sh && bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh</automated>
  </verify>
  <done>L'issue 4 de QUAL-01 est prouvee de bout en bout : un garde qui ne peut pas tourner ecrit une trace, et cette trace est lue et rapportee au demarrage de session suivant.</done>
</task>

<task type="auto">
  <name>Tache 3 : entree SessionStart en forme exec + inventaire durable des hooks (D-32-C, PORT-05)</name>
  <files>plugin/conductor/hooks/hooks.json, docs/HOOKS-CONTRAT-SORTIE.md</files>
  <read_first>
    - plugin/conductor/hooks/hooks.json (etat apres le plan 32-03 : 8 entrees, dont les deux du guard du verrou en forme exec)
    - plugin/dev-orchestrator/hooks/hooks.json (les entrees SessionStart deja en forme exec : la forme exacte a reproduire, et le fait qu'elles ne portent AUCUN operateur shell — leur silence nominal est garanti par la traduction de code de sortie posee dans chaque script)
    - .github/workflows/ci.yml:800-901 (gate PORT-05 : les cinq causes de rouge)
    - docs/HOOKS-CONTRAT-SORTIE.md §4 (assertion de recomptage machine et tableau du module conductor) et §5 (decompte bloquantes/advisory)
    - plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh:395-415 (T12 : l'inventaire et le parc doivent s'accorder)
    - le SUMMARY du plan 32-03 (placement observe des entrees, et le compte PORT-05 attendu/constate a ce moment-la)
  </read_first>
  <action>
1. Ajouter UNE entree `SessionStart` (matcher de demarrage, comme les cinq existantes du module) a
   `plugin/conductor/hooks/hooks.json`, en FORME EXEC : le `command` est le jeton d'interprete, le
   chemin du script et son drapeau de hook vivent dans `args`. AUCUN operateur shell ni suffixe
   d'echappement d'echec : en forme exec il n'y a pas de shell, un tel suffixe serait un chemin
   mort — c'est la traduction de code de sortie posee DANS le script qui garantit le silence
   nominal.

   Attention au groupe : les cinq entrees `SessionStart` existantes du module sont en forme SHELL,
   donc elles restent dans la cible de projet, tandis que l'entree neuve, portant le jeton
   d'interprete, part en cible LOCALE. Consigner dans le SUMMARY ce que le merge produit
   reellement — un groupe reutilise ou un groupe cree, et dans quel fichier.

   Completer la `description` du fragment : le nouveau signal est ADVISORY (il ne bloque rien,
   ADR-031), et son role est nomme — agreger les marqueurs de sante des gardes du parc.

2. Prouver l'armement SUR UN LAB JETABLE, exactement comme le plan 32-03 : installer la fermeture du
   module `conductor` dans un repertoire temporaire, puis asserer sur les fichiers de reglages
   REELLEMENT POSES que l'entree existe, que son `command` est absolu, existant et executable,
   qu'aucun metacaractere shell ne figure dans `command` ni dans `args`, qu'aucun placeholder ne
   subsiste, et que le compte d'entrees portant des `args` egale le nombre derive des fragments de
   la fermeture installee (derivation reproduite localement, jamais un chiffre fige).

   Verifier aussi le SILENCE NOMINAL tel qu'installe — c'est ce qui distingue un signal utile d'une
   pollution de contexte a chaque demarrage : invoquer le script INSTALLE dans le lab, sur un
   repertoire de sante vide, et asserer que sa sortie standard fait ZERO octet et que son code est
   zero.

   NE PAS armer le depot courant : verifier en fin de tache que son etat suivi est inchange.

3. Mettre a jour `docs/HOOKS-CONTRAT-SORTIE.md` DANS LE MEME COMMIT : la valeur de l'assertion de
   recomptage du §4, portee a la valeur reellement mesuree par la commande de recomptage
   elle-meme ; le tableau du module `conductor`, augmente d'une ligne (evenement et matcher, script,
   arguments d'invocation, drapeau de hook, codes ATTEIGNABLES avec cette invocation exacte,
   classement ADVISORY avec son mecanisme, forme exec) ; et les totaux de fin de document. Le
   document et son assertion se mettent a jour ENSEMBLE — il le dit lui-meme, et le cas T12 de la
   suite de `check-hook-paths` le verifie.
  </action>
  <acceptance_criteria>
    - Le fragment est un JSON valide et porte desormais trois entrees avec `args` (les deux du plan 32-03 plus celle-ci) : recompte par interprete sur `plugin/conductor/hooks/hooks.json`, la valeur rendue est `3`.
    - L'entree neuve ne porte aucun operateur shell : la lecture ciblee du bloc `SessionStart` montre que l'entree en forme exec n'a ni suffixe d'echappement d'echec ni operateur de chainage.
    - La commande de recomptage machine du §4 du document s'execute sans lever son assertion.
    - `bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh` sort 0 et son cas T12 rend un PASS (pas un SKIP d'ecart transitoire).
    - `bash plugin/_internal/tests/test-merge-hooks.sh` et `bash plugin/_internal/tests/test-vibeflow-update.sh` sortent 0.
    - `bash scripts/check-machine-paths.sh` sort 0.
    - Le silence nominal tel qu'INSTALLE est prouve : le script installe dans le lab jetable, invoque sur un repertoire de sante vide, rend zero octet sur la sortie standard. Consigner la mesure d'octets dans le SUMMARY.
    - Le depot courant n'a pas ete arme : `git status --porcelain -- .claude/` est strictement vide en fin de tache.
    - **Mutation rouge prouvee, trace obligatoire dans le SUMMARY** : (a) ajouter un suffixe d'echappement d'echec a l'entree neuve fait rougir le gate PORT-05 reproduit localement (metacaractere shell detecte) ; (b) laisser l'inventaire du document a sa valeur precedente fait rougir T12 avec le message d'ecart NON attendu. Consigner assertion, attendu, obtenu ; restaurer ; revalider le vert.
  </acceptance_criteria>
  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh && bash plugin/_internal/tests/test-merge-hooks.sh && bash plugin/_internal/tests/test-vibeflow-update.sh && bash scripts/check-machine-paths.sh && bash scripts/check-version-sync.sh</automated>
  </verify>
  <done>Le doctor est branche au demarrage de session de tout lab qui installe `conductor`, en forme exec, sans operateur shell, silencieux quand il n'y a rien a dire, et l'inventaire durable des hooks dit la verite sur le parc.</done>
  <reversibility rating="costly">Une entree `SessionStart` supplementaire s'execute a chaque demarrage de session de tout lab arme. Le retour arriere est un `git revert`, mais les labs deja mis a jour gardent l'entree jusqu'a leur prochaine mise a jour. Le risque est borne : l'entree est advisory, en lecture seule, et son silence nominal est prouve tel qu'installe.</reversibility>
</task>

</tasks>

## Ecart signale — perimetre, et les memes deux gates transverses qu'au plan 32-03

**Le hook doctor est un livrable transverse (tout le parc) qui vit dans `conductor` — et ce n'est
PAS un ecart.** Tranche au plan, avec ses deux appuis : (1) la lib de portabilite de l'engine
designe deja nommement le hook doctor comme appartenant a `conductor`
(`plugin/_internal/lib/vf-portable.sh:141`, note de report ecrite en Phase 30) ; (2) `conductor` est
declare `mandatory` dans son `module.json`, donc present dans toute fermeture installee — un lecteur
pose la atteint mecaniquement tout le parc. Aucune ligne de CODE n'est ecrite hors
`plugin/conductor/**`, conformement a D-32-07.

**En revanche, les deux memes ecritures transverses qu'au plan 32-03 sont imposees**, pour les
memes raisons machine : `docs/HOOKS-CONTRAT-SORTIE.md` (le cas T12 de
`test-check-hook-paths.sh` compare l'assertion de recomptage au parc reel — ce plan ajoute une 3e
entree) et les compteurs de suites des deux README (`scripts/check-version-sync.sh` §9 — ce plan
cree une suite neuve). Voir l'ecart signale du plan 32-03 pour l'amendement de D-32-07 a porter en
amont.

**Rappel de statut** : ce plan est le SACRIFICE DESIGNE si la phase deborde (D-32-QUAL). S'il est
coupe, la tache 2 du plan de cloture (32-06) trace la dette en branche B — l'issue 4 de QUAL-01
redevient un fail-open silencieux SANS lecteur, pour tout le parc.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repertoire de sante -> doctor | les fichiers de marqueur sont une entree NON fiable : ecrivables par tout processus du poste, potentiellement vides, tronques ou binaires |
| doctor -> contexte de session | la sortie standard d'un hook de demarrage qui sort en zero est INJECTEE comme contexte de session (doc officielle) — tout octet emis est du contexte consomme pour toutes les sessions du parc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-23 | Tampering | marqueur malforme ou hostile | medium | mitigate | parcours tolerant : un marqueur vide, tronque ou binaire n'interrompt jamais le parcours et ne peut pas faire emettre plus d'une ligne ; cas D7 |
| T-32-24 | Denial of Service | injection de contexte a chaque demarrage | high | mitigate | une SEULE ligne au maximum, jamais une par marqueur ; silence STRICT (zero octet) quand il n'y a rien a signaler, prouve tel qu'INSTALLE ; cas D2, D6 |
| T-32-25 | Tampering | doctor qui ecrit | high | mitigate | lecture seule stricte, prouvee a la fois par un critere de grep sur le code et par la comparaison d'instantanes du repertoire ; cas D9 |
| T-32-26 | Repudiation | vert de complaisance sur repertoire illisible | high | mitigate | quatre codes distincts, INDETERMINE ne se confond jamais avec SAIN ; panne injectee par retrait de permissions ; cas D4 |
| T-32-27 | Information Disclosure | motif de marqueur rendu en contexte | low | accept | le motif est ecrit par les gardes du depot eux-memes et ne porte aucun secret ; il peut porter un chemin de script, deja visible du harness |
| T-32-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installe par cette phase — aucun gate de legitimite applicable |
</threat_model>

<verification>
1. `bash -n plugin/conductor/scripts/check-guard-health.sh`
2. `bash plugin/conductor/scripts/tests/test-check-guard-health.sh` — blocs D1 a D13, D13 en succes et non en saut
3. `bash plugin/conductor/scripts/tests/test-guard-driver-lock.sh` — non-regression du producteur de marqueurs
4. `bash plugin/dev-orchestrator/scripts/tests/test-check-hook-paths.sh` — T12 en PASS
5. `bash plugin/_internal/tests/test-merge-hooks.sh`, `bash plugin/_internal/tests/test-vibeflow-update.sh`
6. `bash scripts/check-machine-paths.sh`, `bash scripts/check-version-sync.sh`
7. Silence nominal tel qu'installe : zero octet mesure et consigne
8. Les huit mutations exigees ont rougi ; leurs traces sont dans le SUMMARY
</verification>

<success_criteria>
- Le troisieme etat « le garde n'a pas pu tourner » est enfin visible, pour TOUT le parc et pas seulement pour le guard du verrou.
- Le lecteur ne corrige rien, n'ecrit rien, ne bloque rien, et ne confond jamais SAIN et INDETERMINE.
- Son silence nominal est de zero octet, prouve sur l'installation reelle et non sur l'arbre source.
- La boucle producteur -> marqueur -> lecteur est prouvee de bout en bout par un cas qui ne fabrique aucun marqueur a la main.
</success_criteria>

<output>
Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-05-SUMMARY.md` en fin de plan, avec :
la ligne de fixture de marqueur au format reel, le chemin du lab jetable, la mesure d'octets du
silence nominal installe, le placement observe de l'entree, et les traces des huit mutations
exigees.
</output>

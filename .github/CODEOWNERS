# CODEOWNERS — périmètre étroit de la Phase 41 (D-05)
#
# Objet : revue code owner exigée sur les chemins dont la modification peut neutraliser un gate
# (observation O-3 du `25-SECURITY.md`) — D-05, arbitrage Samuel, AskUserQuestion session
# principale, 2026-09-17.
#
# Propriétaire : @picmakpro, compte de Willy, co-auteur du plugin et admin du dépôt (fait mesuré
# le 2026-09-23, `41-CONTEXT.md` § REPRISE).
#
# Portée : garde-fou par défaut + trace, pas un verrou. Samuel et Willy peuvent contourner,
# nommément (D-02bis, arbitrage Willy, AskUserQuestion session principale, 2026-09-23), et le
# contournement laisse une entrée dans les rule suites (ADR-072). Une PR ouverte par `picmakpro`
# sur ces chemins ne peut pas recevoir l'approbation de son propre auteur : elle entre par un
# contournement tracé.
#
# Lecture par GitHub : la version prise en compte est celle de la branche de base ; une
# modification de ce fichier dans une PR ne vaut donc qu'après son merge.
#
# Syntaxe : motifs de type gitignore, ancrés par `/` ; `*` couvre aussi un nom commençant par un
# point ; ni `!`, ni `[ ]`, ni échappement de `#` (docs GitHub about-code-owners, git-scm
# gitignore, lues le 2026-09-17).
#
# Aucun autre chemin (D-05) : toute extension de ce périmètre est un arbitrage.

/.github/ @picmakpro
/.planning/instruction-budget-baselines.tsv @picmakpro
/.planning/.*-armed @picmakpro
/scripts/hooks/ @picmakpro

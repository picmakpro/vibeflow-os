---
phase: 25-budget-d-instructions-et-tage-d-alignement-court
plan: 04
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - .planning/instruction-budget-baselines.tsv
  - .planning/.instruction-budget-armed
  - plugin/conductor/README.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [BUDG-01, BUDG-02, QUAL-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-06 puis D-06 bis : la calibration est un CHECKPOINT BLOQUANT, pas une note. La Phase 34 est close (2026-09-15, PR #66, zéro agent créé), mais la Phase 40 réécrit `plugin/dev-orchestrator/AGENT.md` (renommage `vibeflow-head` + extension de rôle) et change donc les lignes ET les instructions d'un fichier du corpus. Ce plan ne s'exécute qu'APRÈS la livraison de la Phase 40, dans une SECONDE PR."
    - "La précondition est MACHINE-VÉRIFIABLE, jamais une appréciation : `plugin/dev-orchestrator/AGENT.md` porte `name: vibeflow-head` sur `main`. Tant qu'elle porte `name: vibeflow-dev`, le plan ne démarre pas et le manager de mission rend son rapport en s'arrêtant au checkpoint."
    - "Les valeurs gravées sont produites PAR LE SCRIPT LIVRÉ (plan 25-01), exécuté au moment de la calibration sur le corpus d'alors — jamais recopiées d'un document. Ni « 463 »/« 44 » du cadrage, ni « 433 »/« 43 » de la recherche : le script a tranché la forme « puce sous titre de règles » et les exclusions structurelles, donc ses chiffres sont les seuls valides (`25-RESEARCH.md:680-694`)."
    - "D-02 : la baseline de chaque fichier vaut son maximum MESURÉ à la calibration — aucun rouge à l'armement. Une baseline ne peut ensuite que descendre ; toute hausse d'un fichier au-dessus de sa baseline rougit. La vérification de la tâche de calibration exige un run vert (exit 0) juste après la gravure : si l'armement rendait rouge, la calibration serait fausse par construction."
    - "D-05 et le plafond absolu : aucune baseline de LIGNES n'est gravée au-dessus de 250. Si la Phase 40 a laissé un fichier du corpus au-dessus du plafond ADR-029, la calibration S'ARRÊTE et escalade — soit le fichier redescend (geste de remédiation, hors de ce plan), soit ADR-029 est révisée par arbitrage humain nommé. Graver 260 pour « ne pas rougir » légaliserait la violation par simple mesure : c'est le seul cas où ce plan refuse d'avancer tout seul."
    - "D-04 et spec Windows II §7 : la gravure des baselines et la pose de la sentinelle `.planning/.instruction-budget-armed` se font DANS LE MÊME COMMIT. Un commit qui grave sans armer laisserait un contrat sans garde ; un commit qui arme sans graver rendrait rouge tout de suite."
    - "La sentinelle et la baseline sont posées À LA MAIN (écriture directe de fichier), jamais par le gate, jamais par un hook — le gate les lit et ne les écrit dans aucun cas (D-04, régression #38 à ne pas rejouer)."
    - "Le contrat publié dit la vérité APRÈS l'armement : le catalogue conductor et le CHANGELOG citent les valeurs réellement gravées et la date d'armement, et le module passe en patch `v1.37.1` (durcissement/doc, jamais une nouvelle capacité — règle de numérotation de `CLAUDE.md`)."
    - "Le ledger est fermé sur pièces : `BUDG-01` et `BUDG-02` cochés avec leur date et leur preuve, la ligne de statut `QUAL-01` mise à jour pour retirer la Phase 25 de la liste « reste à tenir », le ROADMAP § Phase 25 avec ses plans cochés, et `STATE.md` mis à jour À LA MAIN (ADR-063, jamais `state record-session` qui réécrit et perd le contenu manuel)."
    - "La release racine (bump `VERSION`, tag annoté, release GitHub, `check-release-tag.sh --remote`) n'est PAS dans ce plan : c'est un geste humain gaté, demandé explicitement à chaque fois."
  artifacts:
    - ".planning/instruction-budget-baselines.tsv : contrat versionné, une ligne par fichier d'agent distribué, colonnes `chemin<TAB>lignes<TAB>instructions`, trié `LC_ALL=C`, en-tête de commentaires `#` portant la date de calibration, le SHA de base, la commande de re-dérivation et la doctrine de ratchet (une valeur ne monte jamais sans arbitrage humain nommé)."
    - ".planning/.instruction-budget-armed : sentinelle vide, versionnée, posée à la main dans le même commit que la baseline."
    - "plugin/conductor/CHANGELOG.md : entrée `## [v1.37.1]` — armement du ratchet, valeurs gravées, date."
  key_links:
    - "Précondition `name: vibeflow-head` <-> validité de la baseline : c'est la seule chose qui empêche de graver un contrat sur un corpus qui bouge le lendemain (Pitfall 3, `25-RESEARCH.md:574-588`). Elle est vérifiable par une commande, pas par une lecture de ROADMAP."
    - "Gravure <-> armement dans le MÊME commit : c'est la doctrine de la spec Windows II §7 et le seul motif pour lequel le plan 25-03 n'a rien armé. Les séparer rouvrirait la fenêtre rouge que BUDG-02 existe pour fermer."
    - "Run vert immédiatement après la gravure <-> D-02 : si le gate rend autre chose que 0 juste après l'armement, la baseline ne reflète pas le corpus et le dépôt entrerait rouge — exactement l'anti-feature `.planning/REQUIREMENTS.md:1096`."
    - "Valeurs du CHANGELOG <-> valeurs du fichier de baselines : le contrat publié et le contrat exécutable doivent citer les mêmes nombres, sinon la publication ment sur ce qui est armé."
  prohibitions:
    - "Ne JAMAIS démarrer ce plan tant que `plugin/dev-orchestrator/AGENT.md` ne porte pas `name: vibeflow-head` sur `main` : la précondition est bloquante, pas indicative."
    - "Ne JAMAIS recopier une valeur de baseline depuis `25-CONTEXT.md`, `25-RESEARCH.md` ou un SUMMARY : toute valeur gravée sort d'une exécution du script livré, faite au moment de la calibration."
    - "Ne JAMAIS graver une baseline de lignes supérieure à 250 : un fichier au-dessus du plafond ADR-029 arrête la calibration et remonte à l'humain."
    - "Ne JAMAIS graver la baseline et poser la sentinelle dans deux commits distincts."
    - "Ne JAMAIS laisser le gate, un hook ou un agent écrire la sentinelle ou le fichier de baselines : écriture manuelle uniquement."
    - "Ne JAMAIS réécrire un fichier d'agent pour faire baisser un compte avant l'armement : la baseline s'aligne sur le corpus tel qu'il est ; la remédiation est un geste ultérieur, par lot, chacun abaissant une baseline dans son propre commit."
    - "Ne JAMAIS bumper la `VERSION` racine, poser un tag ou créer une release depuis ce plan."
    - "Ne JAMAIS utiliser `gsd state record-session` sur `STATE.md` : il réécrit le fichier et perd le contenu manuel (ADR-063)."
---

<objective>
Calibrer et armer : mesurer le corpus avec le script livré, graver les baselines, poser la
sentinelle dans le même commit, publier les valeurs réelles, et fermer le ledger de la phase.

Purpose : c'est le geste one-way de la phase. Graver des baselines sur un corpus qui bouge ensuite
rend la publication fausse dès la première hausse — c'est le motif même de la dépendance D-06, et la
raison pour laquelle D-06 bis l'a reportée de la Phase 34 (close sans créer d'agent) à la Phase 40
(qui réécrit un fichier du corpus).

Output : ce plan s'exécute dans une SECONDE PR, après la livraison de la Phase 40. Le manager de
mission qui atteint ce plan sans que la précondition soit satisfaite s'arrête proprement, rend son
rapport, et ne force rien.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-CONTEXT.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-RESEARCH.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-01-SUMMARY.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint bloquant : autoriser la gravure des baselines (geste one-way, D-06 / D-06 bis)</name>
  <precondition>`plugin/dev-orchestrator/AGENT.md` porte `name: vibeflow-head` sur `main` — la Phase 40 est livrée. Tant que le fichier porte `name: vibeflow-dev`, ce plan ne démarre pas.</precondition>
  <read_first>
    - `25-CONTEXT.md:80-101` — D-06 et son amendement daté D-06 bis, les deux textes verrouillés qui font de cette tâche un checkpoint
    - `25-RESEARCH.md:574-588` — Pitfall 3 : ce qui casse si on calibre avant la Phase 40
    - `25-RESEARCH.md:661-694` — la liste ordonnée de ce que le checkpoint doit faire une fois la Phase 40 shippée
    - `.planning/phases/VFDO-40-vibeflow-head-head-of-minds-du-dev-orchestrator/40-CONTEXT.md` § domain et D-04 — l'ampleur réelle de la réécriture du fichier du corpus
    - `plugin/conductor/scripts/check-instruction-budget.sh` — le gate qui produira les valeurs
  </read_first>
  <what-was-built>
    La première PR de la phase (plans 25-01, 25-02, 25-03) a livré : le gate à deux métriques et cinq
    codes de sortie, sa suite à trois issues et quatre mutants vérifiés par `cmp`, l'étape CI en mode
    avertissement avec sa preuve de discrimination, le catalogue corrigé, le bump `conductor`
    v1.37.0, la note datée sous ADR-029 et les dettes au BACKLOG. Rien n'est armé : aucune baseline
    n'existe, la sentinelle n'est pas posée, la CI ne peut pas rougir sur ce gate.
  </what-was-built>
  <decision>
    Autoriser, ou non, la gravure des baselines et l'armement du ratchet MAINTENANT.

    Ce qui rend la décision one-way : une fois la baseline publiée (fichier versionné, valeurs
    reprises au CHANGELOG et au catalogue), la faire REMONTER demande un arbitrage humain nommé
    avec canal et date. La faire descendre reste libre — c'est le sens du ratchet.

    Trois éléments à trancher, à porter dans la réponse :
    1. **La précondition est-elle réellement satisfaite ?** La Phase 40 est-elle livrée et mergée sur
       `main` — pas seulement planifiée, pas seulement exécutée sur une branche ? La commande
       ci-dessous le dit sans ambiguïté.
    2. **La définition d'instruction est-elle ratifiée ?** Le gate applique D-01 à la lettre
       (body-only, hors frontmatter), adopte la règle de puce sous titre de règles, exclut les
       titres et les blocs fenced. Les chiffres gravés seront donc ceux de cette définition, pas les
       « 463 / 44 » cités au cadrage (qui incluaient le frontmatter). Changer la définition APRÈS
       l'armement invaliderait toutes les valeurs publiées (D-01, reversibility costly).
    3. **Le plafond absolu.** Si un fichier du corpus dépasse 250 lignes après la Phase 40, la
       calibration s'arrête et remonte : baisser le fichier, ou réviser ADR-029. Aucune baseline de
       lignes au-dessus de 250 ne sera gravée.
  </decision>
  <options>
    - **A — Calibrer maintenant** : la Phase 40 est livrée, la définition est ratifiée, aucun fichier
      ne dépasse 250 lignes. Les tâches 2 et 3 s'exécutent dans la foulée, dans une seule PR.
    - **B — Attendre** : la Phase 40 n'est pas livrée, ou une autre phase doit encore toucher au
      corpus. Le manager rend son rapport et s'arrête ; le plan reste en attente, sans dette
      silencieuse — c'est le comportement ATTENDU par défaut (D-06 bis).
    - **C — Calibrer sur un corpus qui bougera encore** : possible sur arbitrage humain nommé, au
      prix d'une baseline à remonter après la Phase 40 — donc au prix exact que D-06 existe pour
      éviter. À n'emprunter que si une raison est écrite.
  </options>
  <how-to-verify>
    1. Vérifier la précondition par commande, depuis la racine du dépôt, sur `main` à jour :
       `git fetch origin main` puis
       `git show origin/main:plugin/dev-orchestrator/AGENT.md | grep -c '^name: vibeflow-head'`
       — la valeur `1` signifie que la Phase 40 est livrée et mergée ; `0` signifie option B.
    2. Lire le rapport que le gate produit aujourd'hui, en mode non armé :
       `bash plugin/conductor/scripts/check-instruction-budget.sh` — il imprime le tableau complet
       et sort en 3. Ce sont les valeurs candidates à la gravure.
    3. Repérer dans ce tableau tout fichier dont la colonne LIGNES dépasse 250 : s'il y en a, la
       réponse ne peut pas être A tant que le fichier n'est pas redescendu ou ADR-029 révisée.
  </how-to-verify>
  <resume-signal>Répondre « A », « B » ou « C ». Toute réponse autre que « A » arrête le plan : le manager rend son rapport de mission, laisse `BUDG-01`/`BUDG-02` non cochés, et note la raison et la date dans le SUMMARY. Une réponse « A » est consignée dans le message de commit de la tâche 2 avec son canal et sa date, selon la convention de `CLAUDE.md` (« arbitrage Samuel, AskUserQuestion session principale, AAAA-MM-JJ »).</resume-signal>
  <reversibility rating="one-way">D-06 : graver les baselines sur un corpus qui bouge ensuite rend la publication fausse dès la première hausse. La remontée d'une baseline publiée exige un arbitrage humain consigné (D-02, reversibility costly).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : graver les baselines et armer la sentinelle dans le même commit</name>
  <files>.planning/instruction-budget-baselines.tsv, .planning/.instruction-budget-armed, plugin/conductor/README.md, plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md</files>
  <precondition>Le checkpoint de la tâche 1 a reçu la réponse A, et `git show origin/main:plugin/dev-orchestrator/AGENT.md | grep -c '^name: vibeflow-head'` rend 1.</precondition>
  <read_first>
    - `plugin/conductor/scripts/check-instruction-budget.sh` — le format de baseline attendu en lecture (TSV `chemin<TAB>lignes<TAB>instructions`, commentaires `#` et lignes vides ignorés)
    - `25-RESEARCH.md:661-694` — l'ordre exact des cinq gestes de calibration
    - `docs/superpowers/specs/2026-08-02-portabilite-windows-ii-design.md:366-384` — §7 : l'armement se fait dans le commit de remédiation, ici le commit qui grave
    - `.planning/.requirements-survival-armed` — le seul précédent de sentinelle versionnée du dépôt, pour la forme du fichier
    - `plugin/conductor/CHANGELOG.md` entrée `[v1.37.0]` — la phrase qui disait qu'aucune baseline n'était gravée, à compléter plutôt qu'à contredire
  </read_first>
  <action>
    Poser le contrat, dans cet ordre, en un seul commit.

    (1) **Re-dériver le corpus.** Exécuter l'énumération du gate et compter les fichiers découverts.
    Consigner le compte dans l'en-tête du fichier de baselines. Si le compte diffère de celui de la
    première PR, ce n'est pas une anomalie — c'est un fait à écrire, avec sa cause (la Phase 40
    ajoute des fichiers hors scope D-03, donc le compte ne devrait pas bouger ; le vérifier plutôt
    que le supposer).

    (2) **Mesurer avec le script livré**, jamais avec une commande ad hoc :
    `bash plugin/conductor/scripts/check-instruction-budget.sh` rend le tableau complet et sort en 3
    (non armé). Extraire les colonnes chemin, LIGNES et INSTR.

    (3) **Contrôle du plafond avant toute gravure.** Si un fichier porte plus de 250 lignes :
    ARRÊTER. Ne rien graver, ne rien armer, écrire le constat et remonter à l'humain (soit le
    fichier redescend, soit ADR-029 est révisée). Ce contrôle passe AVANT l'écriture, pas après.

    (4) **Graver `.planning/instruction-budget-baselines.tsv`.** En-tête de commentaires `#` :
    date de calibration, SHA de base (`git rev-parse HEAD`), la commande exacte qui régénère les
    valeurs, le nombre de fichiers, et la doctrine en une phrase — une valeur ne monte jamais sans
    un arbitrage humain nommé avec son canal et sa date ; elle descend librement, dans le commit de
    remédiation qui la justifie. Puis une ligne par fichier, `chemin<TAB>lignes<TAB>instructions`,
    triée `LC_ALL=C`, valeurs entières.

    (5) **Armer.** Créer `.planning/.instruction-budget-armed`, fichier vide, par écriture directe.
    Le mettre dans le MÊME commit que la baseline.

    (6) **Vérifier immédiatement que l'armement ne rougit pas.** Ré-exécuter le gate : il doit
    rendre `0`. Un `1` signifierait que la baseline ne reflète pas le corpus ; un `2`, que le
    contrat est incomplet. Dans les deux cas, corriger AVANT de committer — le dépôt ne doit jamais
    entrer rouge (D-02, anti-feature `.planning/REQUIREMENTS.md:1096`).

    (7) **Publier les valeurs réelles.** Compléter l'entrée de catalogue de
    `plugin/conductor/README.md` (le ratchet est armé depuis telle date, les baselines vivent dans
    `.planning/instruction-budget-baselines.tsv`, elles ne montent pas sans arbitrage nommé) et
    ajouter une entrée `## [v1.37.1]` au CHANGELOG conductor citant la date d'armement, le nombre de
    fichiers sous contrat, et les totaux réellement gravés. Bumper `VERSION` et `module.json` du
    module en `v1.37.1` — durcissement et documentation, donc patch (règle de numérotation de
    `CLAUDE.md`), jamais minor : aucune capacité nouvelle n'est ajoutée.

    (8) **Ne pas toucher** à la `VERSION` racine, ni à `marketplace.json`, ni à `plugin.json` : la
    release du dépôt est un geste humain gaté, hors de ce plan.
  </action>
  <behavior>
    - Après la gravure, le gate rend 0 sur le dépôt : le ratchet est armé et personne n'est rouge.
    - La baseline contient exactement une ligne de données par fichier découvert.
    - Aucune valeur de la colonne lignes n'excède 250.
    - La sentinelle et la baseline apparaissent dans le même commit.
    - Le CHANGELOG cite les mêmes nombres que le fichier de baselines.
  </behavior>
  <acceptance_criteria>
    - `.planning/instruction-budget-baselines.tsv` existe, porte un en-tête de commentaires contenant une date AAAA-MM-JJ et une ligne `SHA de base : <40 hex>`, et contient une ligne de données par fichier du corpus (compte recomptépar glob au moment de la vérification).
    - Aucune ligne de données n'a une valeur de lignes strictement supérieure à 250.
    - `.planning/.instruction-budget-armed` existe et est vide.
    - `bash plugin/conductor/scripts/check-instruction-budget.sh` rend `0`.
    - `plugin/conductor/VERSION` vaut `v1.37.1`, `module.json` est synchrone, `plugin/conductor/CHANGELOG.md` contient `## [v1.37.1]`, et `bash scripts/check-version-sync.sh` sort en 0.
    - `bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh` reste vert (l'armement du dépôt ne doit rien changer aux fixtures).
    - `git status --short VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json` ne signale aucun fichier.
    - Le commit qui pose la sentinelle pose aussi la baseline : les deux chemins apparaissent dans `git show --name-only HEAD`.
  </acceptance_criteria>
  <verify>
    <automated>B=.planning/instruction-budget-baselines.tsv; A=.planning/.instruction-budget-armed; test -f "$B" || { echo "BASELINE-ABSENTE"; exit 1; }; test -f "$A" || { echo "SENTINELLE-ABSENTE"; exit 1; }; test ! -s "$A" || { echo "SENTINELLE-NON-VIDE"; exit 1; }; grep -qE '^#.*[0-9]{4}-[0-9]{2}-[0-9]{2}' "$B" || { echo "BASELINE-SANS-DATE"; exit 1; }; grep -qE '^#.*SHA de base : [0-9a-f]{40}' "$B" || { echo "BASELINE-SANS-SHA"; exit 1; }; rows=$(grep -c '^plugin/' "$B"); glob=$({ for f in plugin/*/agents/*.md; do [ -f "$f" ] && echo x; done; for f in plugin/*/AGENT.md; do [ -f "$f" ] && echo x; done; } | grep -c x); echo "entrees_baseline=$rows corpus=$glob"; test "$rows" -eq "$glob" || { echo "BASELINE-INCOMPLETE: $rows pour $glob fichiers"; exit 1; }; over=$(grep -v '^#' "$B" | awk -F'\t' 'NF>=3 && $2+0>250 {n++} END{print n+0}'); test "$over" -eq 0 || { echo "BASELINE-AU-DESSUS-DU-PLAFOND: $over fichier(s)"; exit 1; }; rc=0; bash plugin/conductor/scripts/check-instruction-budget.sh >/dev/null 2>&1 || rc=$?; test "$rc" -eq 0 || { echo "ARMEMENT-ROUGE: rc=$rc"; exit 1; }; bash plugin/conductor/scripts/tests/test-check-instruction-budget.sh >/dev/null 2>&1 || { echo "SUITE-ROUGE-APRES-ARMEMENT"; exit 1; }; test "$(cat plugin/conductor/VERSION)" = "v1.37.1" || { echo "VERSION-MODULE: $(cat plugin/conductor/VERSION)"; exit 1; }; grep -q '"version": "v1.37.1"' plugin/conductor/module.json || { echo "MODULE-JSON-DESYNC"; exit 1; }; grep -q '^## \[v1.37.1\]' plugin/conductor/CHANGELOG.md || { echo "CHANGELOG-SANS-ENTREE"; exit 1; }; bash scripts/check-version-sync.sh || { echo "VERSION-SYNC-ROUGE"; exit 1; }; gs=$(git status --short VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json) || { echo "GIT-INDISPONIBLE"; exit 1; }; g=$(printf '%s' "$gs" | grep -c .); test "$g" -eq 0 || { echo "RELEASE-META-TOUCHEE: $g"; exit 1; }; gh=$(git show --name-only --pretty=format: HEAD) || { echo "GIT-INDISPONIBLE"; exit 1; }; n=$(printf '%s\n' "$gh" | grep -c 'instruction-budget'); test "$n" -ge 2 || { echo "GRAVURE-ET-ARMEMENT-SEPARES: $n chemin(s) dans HEAD"; exit 1; }; echo CALIBRATION-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `BASELINE-ABSENTE`, `SENTINELLE-ABSENTE`, `SENTINELLE-NON-VIDE`, `BASELINE-SANS-DATE`, `BASELINE-SANS-SHA`, `BASELINE-INCOMPLETE: <n> pour <m> fichiers`, `BASELINE-AU-DESSUS-DU-PLAFOND: <n> fichier(s)`, `ARMEMENT-ROUGE: rc=<n>`, `SUITE-ROUGE-APRES-ARMEMENT`, `VERSION-MODULE`, `MODULE-JSON-DESYNC`, `CHANGELOG-SANS-ENTREE`, `VERSION-SYNC-ROUGE`, `RELEASE-META-TOUCHEE: <n>`, `GRAVURE-ET-ARMEMENT-SEPARES: <n> chemin(s) dans HEAD` apparaît ; l'absence de `CALIBRATION-OK` vaut échec. Causes discriminantes : la baseline ne couvre pas tout le corpus, donc le gate rendra 2 à la prochaine exécution ; une baseline de lignes au-dessus de 250 légaliserait une violation d'ADR-029 ; le gate rend autre chose que 0 juste après l'armement, donc le dépôt entrerait rouge (l'anti-feature que BUDG-02 existe pour interdire) ; la sentinelle et la baseline ne sont pas dans le même commit, ce qui rouvre la fenêtre rouge de la spec Windows II §7 ; la triade du module est désynchronisée ; un fichier de release racine a été touché.</fails_when>
  </verify>
  <done>Les baselines sont gravées sur une mesure fraîche, la sentinelle est posée dans le même commit, le gate est armé et vert, et le contrat publié cite les valeurs réelles.</done>
  <reversibility rating="one-way">D-06 : c'est la gravure elle-même. Désarmer (retirer la sentinelle) reste un geste visible en diff, mais une baseline publiée puis remontée demande un arbitrage humain consigné.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : fermer le ledger — REQUIREMENTS, ROADMAP, STATE, sur pièces</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <precondition>La tâche 2 est verte : le gate est armé et rend 0 sur le dépôt.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md:949-951` — les énoncés `BUDG-01` et `BUDG-02` à cocher
    - `.planning/REQUIREMENTS.md:851` — la ligne de statut `QUAL-01` et sa liste « reste à tenir sur 33, 18, 25 »
    - `.planning/REQUIREMENTS.md:1083` — le texte exact de QUAL-01
    - `.planning/ROADMAP.md` § Phase 25 — le bloc à compléter (plans cochés, résultat de phase)
    - `.planning/STATE.md` — `## Current Position`, `### Roadmap Evolution`, `## Session Continuity` ; et la contrainte ADR-063 : `current_phase` ne peut pas décroître, `check-state-integrity.sh` le garde
    - `.planning/phases/VFDO-34-gaps-agency-agents-cadrage-skill-installer/34-06-PLAN.md` — le précédent de clôture de ledger le plus récent, pour la forme
  </read_first>
  <action>
    Écrire ce qui a été fait, avec la preuve, et rien de plus.

    (1) **REQUIREMENTS.** Cocher `BUDG-01` et `BUDG-02` en ajoutant à chacun la date et la preuve :
    le nom du gate, le nombre de fichiers sous contrat, le fait que le ratchet est armé et le
    numéro de version du module. Mettre à jour la ligne de statut `QUAL-01` pour retirer la Phase 25
    de la liste « reste à tenir » et citer ce qui la satisfait ici : trois issues exercées sur
    fixtures, quatre mutants vérifiés par `cmp`, dont un par métrique.

    (2) **ROADMAP.** Compléter le § Phase 25 : les quatre plans cochés avec une ligne de résultat
    chacun, et un paragraphe « Résultat de la Phase 25 » disant ce qui est armé, à quelle date, sur
    combien de fichiers, et ce qui reste explicitement hors périmètre (remédiation des fichiers
    chargés, budget des SKILL.md et du bootstrap, BUDG-03). Dire aussi que la phase a été livrée en
    DEUX PR, et pourquoi (D-06 bis).

    (3) **STATE.** Mettre à jour À LA MAIN : `last_activity`, `last_activity_desc`, `stopped_at`,
    `## Current Position` et `## Session Continuity`. Ne jamais lancer `gsd state record-session` :
    il réécrit le fichier et perd le contenu manuel (ADR-063). Ajouter à la section « Ce qui reste
    fermé, quoi qu'il arrive » une ligne : le budget d'instructions est armé, une baseline ne remonte
    pas sans arbitrage humain nommé avec canal et date. Et corriger la ligne existante qui dit que
    `check-agents.sh` ne mesure aucune ligne — elle reste vraie, mais il faut désormais y ajouter que
    la mesure existe, dans `check-instruction-budget.sh`.

    (4) **Cohérence machine.** Vérifier que `bash plugin/conductor/scripts/check-state-integrity.sh
    --file .planning/STATE.md` reste vert, et que le ROADMAP ne porte plus de plan non coché pour la
    Phase 25.
  </action>
  <behavior>
    - BUDG-01 et BUDG-02 sont cochés avec date et preuve.
    - QUAL-01 ne liste plus la Phase 25 comme restant à tenir.
    - Le ROADMAP § Phase 25 porte ses quatre plans cochés et un résultat de phase.
    - STATE.md est mis à jour à la main et reste vert sous son gate d'intégrité.
  </behavior>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` porte `- [x] **BUDG-01**` et `- [x] **BUDG-02**`, chacun suivi d'une date AAAA-MM-JJ dans la même entrée.
    - La ligne de statut `QUAL-01` de `.planning/REQUIREMENTS.md` ne contient plus la Phase 25 dans sa liste de phases restant à tenir.
    - `.planning/ROADMAP.md` § Phase 25 contient quatre entrées de plan cochées `- [x] 25-0N-PLAN.md` et une section de résultat portant une date.
    - `bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md` sort en 0.
    - `.planning/STATE.md` mentionne `check-instruction-budget` et la date d'armement.
    - `bash scripts/check-machine-paths.sh` sort en 0 (aucun chemin de machine introduit par les éditions de documentation).
  </acceptance_criteria>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -q '^- \[x\] \*\*BUDG-01\*\*' "$R" || { echo "BUDG-01-NON-COCHE"; exit 1; }; grep -q '^- \[x\] \*\*BUDG-02\*\*' "$R" || { echo "BUDG-02-NON-COCHE"; exit 1; }; for k in BUDG-01 BUDG-02; do grep "^- \[x\] \*\*$k\*\*" "$R" | grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' || { echo "PREUVE-SANS-DATE: $k"; exit 1; }; done; rest=$(grep '| QUAL-01 |' "$R" | head -1 | sed -n 's/.*reste à tenir sur \([^|]*\).*/\1/p'); echo "qual01_reste_a_tenir=[$rest]"; if printf '%s' "$rest" | grep -q '25'; then echo "QUAL-01-LISTE-ENCORE-LA-25"; exit 1; fi; P=.planning/ROADMAP.md; SEC=$(awk '/^### Phase 25:/{f=1;next} f&&/^### Phase /{exit} f' "$P"); for k in 1 2 3 4; do printf '%s\n' "$SEC" | grep -q "^- \[x\] 25-0$k-PLAN\.md" || { echo "ROADMAP-PLAN-NON-COCHE: 25-0$k"; exit 1; }; done; printf '%s\n' "$SEC" | grep -q '^- \[ \]' && { echo "ROADMAP-PLAN-RESTANT-NON-COCHE"; exit 1; }; awk '/^### Phase 25:/{f=1;next} f&&/^### Phase /{exit} f' "$P" | grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' || { echo "ROADMAP-RESULTAT-SANS-DATE"; exit 1; }; bash plugin/conductor/scripts/check-state-integrity.sh --file .planning/STATE.md || { echo "STATE-INTEGRITY-ROUGE"; exit 1; }; grep -q 'check-instruction-budget' .planning/STATE.md || { echo "STATE-SANS-TRACE"; exit 1; }; bash scripts/check-machine-paths.sh || { echo "CHEMINS-MACHINE-INTRODUITS"; exit 1; }; echo LEDGER-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `BUDG-01-NON-COCHE`, `BUDG-02-NON-COCHE`, `PREUVE-SANS-DATE: <id>`, `QUAL-01-LISTE-ENCORE-LA-25`, `ROADMAP-PLAN-NON-COCHE: 25-0<n>`, `ROADMAP-PLAN-RESTANT-NON-COCHE`, `ROADMAP-RESULTAT-SANS-DATE`, `GIT-INDISPONIBLE`, `STATE-INTEGRITY-ROUGE`, `STATE-SANS-TRACE`, `CHEMINS-MACHINE-INTRODUITS` apparaît ; l'absence de `LEDGER-OK` vaut échec. Causes discriminantes : une exigence cochée sans date ni preuve (une coche sans trace est une déclaration, pas un fait) ; QUAL-01 continuant de lister la Phase 25 alors qu'elle est satisfaite, ou l'inverse ; un plan non coché au ROADMAP, donc un état incohérent avec le disque ; le gate d'intégrité de STATE.md rouge (régression du frontmatter, ADR-063) ; un chemin absolu de machine introduit par une édition de documentation, que le gate racine attrape.</fails_when>
  </verify>
  <done>Le ledger dit ce qui est armé, depuis quand, sur quelle preuve — et ce qui reste dû.</done>
  <reversibility rating="reversible">Édition de documentation de planning uniquement.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| humain → gravure des baselines | Le checkpoint est la seule barrière entre un corpus qui bouge et un contrat publié. |
| contributeur → fichier de baselines | Une fois versionné, n'importe quelle PR peut proposer d'en modifier une valeur. |
| plan → release racine | La release du dépôt est un geste humain gaté que ce plan ne doit jamais franchir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-16 | Tampering | hausse d'une baseline sans arbitrage | high | mitigate | L'en-tête du fichier de baselines grave la doctrine (une valeur ne monte jamais sans un arbitrage humain nommé, canal et date, convention `CLAUDE.md`) ; le fichier est versionné et toute hausse se lit en diff ; le CHANGELOG publie les valeurs, donc une hausse silencieuse contredirait un document publié. |
| T-25-17 | Elevation of Privilege | baseline gravée au-dessus du plafond ADR-029 | high | mitigate | Contrôle AVANT écriture : aucune valeur de lignes supérieure à 250 n'est gravée ; le gate lui-même rend rouge au-dessus du plafond quelle que soit la baseline (plan 25-01). La vérification de tâche recompte la colonne. |
| T-25-18 | Spoofing | calibration sur un corpus non figé | high | mitigate | Précondition machine-vérifiable (`name: vibeflow-head` sur `origin/main`) et checkpoint bloquant : le plan ne démarre pas sur une appréciation de ROADMAP. |
| T-25-19 | Denial of Service | armement qui rend le dépôt rouge | high | mitigate | Vérification immédiate après gravure : le gate doit rendre 0. Un 1 ou un 2 fait échouer la tâche avant commit — l'anti-feature « blocage CI dur immédiat » (`.planning/REQUIREMENTS.md:1096`) ne peut pas se produire par accident. |
| T-25-20 | Tampering | sentinelle posée sans baseline, ou l'inverse | medium | mitigate | Les deux chemins doivent apparaître dans le même commit (`git show --name-only HEAD`), vérifié par machine. |
| T-25-21 | Repudiation | release racine posée par un agent | medium | mitigate | `git status --short` sur `VERSION`, `marketplace.json` et `plugin.json` doit être vide ; la discipline de release de `CLAUDE.md` reste un geste humain demandé explicitement. |
| T-25-SC | Tampering | chaîne d'approvisionnement | low | accept | Aucune installation de paquet : gravure de fichiers texte et éditions de documentation. |
</threat_model>

<verification>
- Précondition machine : `git show origin/main:plugin/dev-orchestrator/AGENT.md | grep -c '^name: vibeflow-head'` rend 1.
- Après gravure : le gate rend 0, la suite reste verte, la baseline couvre tout le corpus, aucune
  valeur de lignes au-dessus de 250.
- Sentinelle et baseline dans le même commit.
- `check-version-sync.sh`, `check-state-integrity.sh --file .planning/STATE.md` et
  `check-machine-paths.sh` verts — et, avant tout push, rejeu des commandes du job CI `gates` de
  `ci.yml`, jamais la liste d'un rapport.
</verification>

<success_criteria>
- Le budget d'instructions par fichier d'agent distribué est mesuré ET publié, sur des valeurs
  produites par le script livré (BUDG-01, critère 1 du ROADMAP).
- Le gate est en ratchet : il a averti pendant toute la première PR, il bloque à partir du commit
  qui grave, et il n'a jamais été rouge (BUDG-02, critère 2).
- La mesure est calibrée sur le corpus final du milestone, après la Phase 34 ET la Phase 40
  (critère 3, dépendance amendée le 2026-09-15).
- QUAL-01 est satisfait pour ce gate et retiré de la liste des phases restant à tenir.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 25-04)

| Artefact | Nature | Détail |
|---|---|---|
| `.planning/instruction-budget-baselines.tsv` | fichier de données versionné (neuf) | `chemin<TAB>lignes<TAB>instructions`, une ligne par fichier d'agent distribué, trié `LC_ALL=C`, en-tête `#` portant date, SHA de base, commande de re-dérivation et doctrine de ratchet. Lu par le gate, jamais écrit par lui. |
| `.planning/.instruction-budget-armed` | sentinelle versionnée (neuve) | Fichier vide ; sa présence rend le gate bloquant. Posée à la main, dans le même commit que la baseline. |
| `plugin/conductor/VERSION` = `v1.37.1` | bump patch | Durcissement et documentation, pas une capacité nouvelle. |
| Entrée `## [v1.37.1]` | CHANGELOG conductor | Date d'armement, nombre de fichiers sous contrat, totaux réellement gravés. |
| Entrées de ledger | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` | BUDG-01 et BUDG-02 cochés avec preuve datée ; QUAL-01 retiré de la liste des phases restantes ; résultat de phase au ROADMAP ; STATE mis à jour à la main (ADR-063). |

**Non produit par ce plan, et volontairement :** aucun bump de la `VERSION` racine, aucun tag, aucune
release GitHub — gestes humains gatés (`CLAUDE.md`, discipline de release).

*(Aucun symbole d'API externe : `api-coverage` reste `detected: false` sur ce corps de plan.)*
</artifacts>

<output>
Créer `.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-04-SUMMARY.md`
en fin de plan — y compris si le checkpoint répond B : le SUMMARY consigne alors l'arrêt, sa raison
et sa date, et le manager rend son rapport sans cocher BUDG-01 ni BUDG-02. Commits en français,
avec le bandeau d'arbitrage daté du jour de la réponse au checkpoint.
</output>

---
phase: 43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c
plan: 06
type: execute
wave: 3
depends_on: ["43-02", "43-03", "43-04", "43-05"]
files_modified:
  - plugin/conductor/skills/vf-calibrate/SKILL.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/README.md
  - plugin/conductor/CHANGELOG.md
autonomous: true
requirements: [FABR-06, FABR-07, FABR-09, FABR-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "vf-calibrate/SKILL.md ne nomme plus seulement vf-mcp-consumer : la ré-affirmation MCP vise les agents flaggés vf-mcp-consumer (allowlist large) ET ceux qui portent vf-mcp-tools (allowlist nommée, ex. vf-reviewer) (durcissement c, dernier site)"
    - "conductor passe en mineure (valeur sur disque + 0.1.0) avec VERSION, module.json, ligne Version du README et CHANGELOG cohérents ; le README catalogue check-skills.sh et décrit ce que font désormais check-agents.sh (sept listes, grammaire vf-mcp-tools) et check-instruction-budget.sh (SKILL.md, bootstrap selon l'arbitrage) ; aucun bump racine"
    - "Le README et le CHANGELOG disent explicitement que check-skills.sh n'est PAS câblé au SessionStart dans cette phase, et pourquoi"
    - "Au rejeu final, toutes les suites voisines sont vertes, le corpus réel passe check-skills.sh et check-agents.sh, le budget réel est vert (sauf option plafond-dur-socle consignée), check-version-sync.sh, check-blueprints.sh, G-2 (check-gate-touche.sh) et G-1 (check-baseline-arbitrage.sh) rendent un verdict conforme, et les deux témoins lab frais (HOME temporaire) sont rejoués"
    - "Aucun fichier sous .github/ n'est modifié par la phase"
    - "Le SUMMARY de clôture porte les sections « Relecture Samuel » (dev-orchestrator, _internal, baseline CODEOWNERS le cas échéant), « Résidus signalés » et la table complète « Artifacts this phase produces »"
  artifacts:
    - path: "plugin/conductor/skills/vf-calibrate/SKILL.md"
      provides: "ré-affirmation MCP à deux déclarations"
      contains: "vf-mcp-tools"
    - path: "plugin/conductor/CHANGELOG.md"
      provides: "entrée mineure Phase 43"
      contains: "check-skills.sh"
    - path: "plugin/conductor/README.md"
      provides: "entrée check-skills.sh, compte de scripts re-dérivé"
      contains: "check-skills.sh"
  key_links:
    - from: "plugin/conductor/README.md et CHANGELOG.md"
      to: "en-têtes de check-skills.sh, check-agents.sh, check-instruction-budget.sh"
      via: "la documentation résume les en-têtes, jamais l'inverse"
      pattern: "check-skills.sh"
  prohibitions:
    - statement: "MUST NOT présenter une décision de cadrage ou de planification de Claude comme un arbitrage humain dans le CHANGELOG, les commits ou la PR ; toute décision humaine y est citée avec son canal et sa date (CLAUDE.md, traçabilité des arbitrages)"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit"
    - statement: "MUST NOT câbler check-skills.sh au SessionStart dans cette phase : tant que le corpus n'est pas mis en conformité (D-Q5), chaque session de chaque lab porterait un avertissement permanent que l'utilisateur ne peut pas corriger (spec §1.2 : des avertissements qui désensibilisent au signal)"
      status: unresolved
      reason: "décision de plan relue en PR ; hooks.json absent de files_modified"
---

<objective>
Fermer la phase : corriger le dernier texte à une seule clé MCP (vf-calibrate), bumper conductor en
mineure avec une documentation qui dit ce que font désormais ses trois gates, rejouer tout ce que
la phase a pu casser, et poser les relevés de relecture et de résidus pour la PR.

Purpose: FABR-10 (durcissement c, site vf-calibrate/SKILL.md:92) ; release notes de module pour
FABR-06, FABR-07, FABR-09 ; preuve de non-régression de bout en bout.
Output: conductor en mineure, README et CHANGELOG à jour, rejeu vert, SUMMARY de clôture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-01-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-02-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-04-SUMMARY.md
@.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-05-SUMMARY.md

Garde-fou du jalon (ROADMAP.md, en-tête gouvernance) : aucune release du jalon avant la clôture de
`fiabilite-v1.0` ; conductor est partagé avec fiabilite — lire la version sur disque au moment de
l'exécution (v1.43.0 à la rédaction), et au rebase après un merge de fiabilite, seul le numéro
attendu change, jamais l'ordre ni le contenu du commit de bump.
Référence de phase (B43) : la branche porte encore la Phase 42 tant que la PR #108 n'est pas
mergée dans origin/main (ci.yml, VERSION de conductor et manifeste daté diffèrent déjà de la base de
branche — mesuré le 2026-09-25). Tout contrôle « ce que la Phase 43 a touché » part donc du dernier
commit qui touche les PLAN.md de la phase (`git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md'`),
jamais de la base de branche. Les gardes G-1 et G-2 gardent leur propre base : elles jugent toute la
branche, Phase 42 comprise.
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : vf-calibrate à deux déclarations, conductor en mineure avec README et CHANGELOG</name>
  <files>plugin/conductor/skills/vf-calibrate/SKILL.md, plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/README.md, plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - plugin/conductor/skills/vf-calibrate/SKILL.md (fichier modifié : re-grepper vf-mcp-consumer — occurrence unique mesurée à la l.92 le 2026-09-25, dans l'étape 3 « Ré-affirmer l'allowlist MCP des agents exécutants », l.87-96)
    - plugin/conductor/README.md (fichier modifié : l.9 ligne Version ; l.81 SessionStart ; l.86-112 « Scripts (27) » et sa note de re-dérivation, entrées check-agents.sh et check-instruction-budget.sh)
    - plugin/conductor/CHANGELOG.md (fichier modifié : forme de l'entrée v1.43.0)
    - plugin/conductor/VERSION, plugin/conductor/module.json
    - en-têtes finaux de plugin/conductor/scripts/check-skills.sh, check-agents.sh, check-instruction-budget.sh (sources que la documentation résume)
    - 43-04-SUMMARY (option bootstrap arbitrée et citation) ; 43-02-SUMMARY (mesure de dérive du corpus)
    - CLAUDE.md (aucun bump racine ; traçabilité des arbitrages)
  </read_first>
  <action>
1. vf-calibrate/SKILL.md, étape 3 : la phrase qui cible « les agents flaggés vf-mcp-consumer » vise désormais « les agents flaggés `vf-mcp-consumer` (allowlist large) ou porteurs de `vf-mcp-tools` (allowlist nommée, ex. `vf-reviewer`) » ; la commande d'injection qui suit ne change pas (elle traite déjà les deux, inject-mcp-tools.sh l.357-367) ; aucune autre ligne ne change, aucune ligne description: ni aucun titre.
2. README de conductor :
   - nouvelle entrée `check-skills.sh` sous « Gates machine » : nature des skills (`vf-nature`, défaut « outil »), procédure sans `ecrit:` ni `vf-rubrique-juge` refusée, dérive déclaration/prose en avertissement (D-Q5), même manifeste daté que check-agents.sh, contrat 0/1/3 ; NON câblé au SessionStart dans cette phase — lancé par la CI (suite et cas sur l'arbre réel) et par skill-creator (`--file`) ; câblage à poser avec la mise en conformité du corpus (backlog e36e6f2) ;
   - entrée check-agents.sh : manifeste à sept listes (dont les champs natifs des SKILL.md) et grammaire `vf-mcp-tools` validée ;
   - entrée check-instruction-budget.sh : SKILL.md plafonnés à 500 lignes (hors modules doc-only) et bootstrap selon l'option arbitrée en 43-04 (nommer l'option et sa citation) ;
   - titre « Scripts (N) » et note de re-dérivation : rejouer la commande de la note (`find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'`) et écrire la valeur mesurée, jamais un nombre deviné ;
   - ligne « **Version** » (l.9).
3. Bump MINEUR de conductor depuis la valeur lue sur disque (v1.43.0 à la rédaction → v1.44.0) : VERSION, `.version` de module.json. Aucune VERSION racine, ni plugin.json, ni marketplace.json, ni README racine.
4. CHANGELOG, entrée en tête « ## [<VERSION>] — <date d'exécution ISO> (Phase 43 — gate des skills par nature, budget des SKILL.md et du bootstrap) », « **Minor** (nouveau gate des skills) : » + puces : check-skills.sh (FABR-06, D-Q1, D-Q2) ; dérive en avertissement et mesure du corpus réel (FABR-07, D-Q5, écart assumé vis-à-vis de D-11 de la Phase 42) ; manifeste à sept listes (FABR-09) ; plafond des SKILL.md et bootstrap (FABR-09, D-Q4, option arbitrée citée) ; grammaire vf-mcp-tools au gate des agents et vf-calibrate à deux déclarations (FABR-10, D-Q3) ; hook SessionStart non câblé (motif) ; décisions de Willy citées « AskUserQuestion, session principale, 2026-09-24 » ; décisions de planification (noms de clés, vocabulaire de dérive, exclusion doc-only, octets ÷ 4) présentées comme telles, jamais comme un arbitrage.
5. Commits : `fix(conductor): vf-calibrate ré-affirme les deux déclarations MCP (FABR-10 c)` puis `feat(conductor <VERSION>): gate des skills par nature, budget des SKILL.md et du bootstrap (Phase 43)` — hors surface G-2 (aucun check-*.sh ni suite dans ces commits).
  </action>
  <verify>
    <automated>f=plugin/conductor/skills/vf-calibrate/SKILL.md; grep -c 'vf-mcp-tools' "$f"; base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && d=$(git diff -U0 "$base" HEAD -- "$f") && printf '%s\n' "$d" | grep -cE '^[-+](description:|#)'</automated>
    <fails_when>la première valeur vaut 0, ou la seconde n'est pas 0</fails_when>
    <automated>v=$(cat plugin/conductor/VERSION); python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["version"])' plugin/conductor/module.json | grep -qx "$v" && grep -q "Version\*\* : $v" plugin/conductor/README.md && head -5 plugin/conductor/CHANGELOG.md | grep -q "^## \[$v\]" && grep -q 'check-skills.sh' plugin/conductor/README.md && grep -q 'check-skills.sh' plugin/conductor/CHANGELOG.md && bash scripts/check-version-sync.sh >/dev/null 2>&1 && echo "CONDUCTOR-VERSION-OK $v"</automated>
    <fails_when>la ligne CONDUCTOR-VERSION-OK est absente</fails_when>
  </verify>
  <acceptance_criteria>
    - vf-calibrate/SKILL.md nomme vf-mcp-tools, sans ligne description: ni titre modifié
    - triade de version de conductor cohérente (valeur sur disque + 0.1.0) ; `bash scripts/check-version-sync.sh` sort 0 ; `base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && liste=$(git diff --name-only "$base" HEAD -- VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json README.md README.fr.md) && printf '%s' "$liste" | grep -c .` rend 0
    - README : entrée check-skills.sh présente, mention « non câblé au SessionStart » et son motif, compte de scripts égal à la sortie de la commande de la note
    - CHANGELOG : entrée en tête à la nouvelle version, qui cite les décisions de Willy avec canal et date et nomme les décisions de planification comme telles
  </acceptance_criteria>
  <done>Le dernier texte à une seule clé MCP est corrigé ; conductor est en mineure avec une documentation qui dit exactement ce que font ses gates et ce qui n'est pas câblé.</done>
</task>

<task type="auto">
  <name>Tâche 2 : rejeu complet, gardes G-1 et G-2, témoins lab frais, relevés de relecture et de résidus</name>
  <files>plugin/conductor/CHANGELOG.md</files>
  <read_first>
    - tous les SUMMARY 43-01 à 43-05 (hashes, mesures, option bootstrap, relevé Samuel de 43-05)
    - scripts/check-gate-touche.sh (l.1-90 : surface G-2, forme du trailer, un chemin par trailer)
    - scripts/check-baseline-arbitrage.sh (l.1-90 : ce que G-1 voit et ne voit pas)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-06-PLAN.md (forme de la Tâche 3 : REJEU-FIN, relevé Samuel)
  </read_first>
  <action>
1. Rejouer les suites voisines, le budget réel, la synchro des versions, les blueprints, le corpus réel des deux gates et les deux témoins lab frais (commandes de vérification ; toute installation sous HOME temporaire, jamais le HOME réel ; aucune commande ne dépend de la date du jour).
2. Si un rejeu rougit : corriger dans le plan fautif par un commit ciblé portant son propre trailer Gate-Touche s'il touche la surface G-2, jamais en affaiblissant un test ; si la correction dépasse une ligne ou touche une décision, s'arrêter et le signaler.
3. Si une incohérence de documentation apparaît au rejeu, corriger seulement le CHANGELOG de conductor (fichier de cette tâche) ; sinon cette tâche ne modifie aucun fichier suivi.
4. SUMMARY de clôture (commit `.planning/` séparé) :
   - « Relecture Samuel » : sortie de `git log --format='%h %s' "$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md')"..HEAD -- plugin/dev-orchestrator plugin/_internal` ; le commit de baseline s'il existe (chemin CODEOWNERS : revue de Samuel ou contournement tracé, CLAUDE.local.md) ; le constat T23 à T31 ; le changement de comportement de l'installeur (relais WARNING/ERROR) ;
   - « Résidus signalés » : G-1 ne voit pas une ligne de baseline AJOUTÉE (ni son retrait sur une clé non-fichier) ; check-skills.sh non câblé au SessionStart (à poser avec la mise en conformité du corpus, e36e6f2) ; liste de dérive du corpus réel (43-02) ; option bootstrap et, si plafond-dur-socle, plan complémentaire à planifier ; --verify de l'injecteur inchangé sur valeur malformée ; troncature à 1 536 caractères des descriptions non gatée ; fidélité des clés vf-* des skills sur les runtimes tiers non mesurée (liste FIELDS de check-artifact-fidelity.sh) ; échéance du manifeste daté (six listes au 2026-10-24, septième à verifie_le + 30 j) ;
   - la table complète « Artifacts this phase produces » (reprise de ce plan, complétée des valeurs réelles).
  </action>
  <verify>
    <automated>for s in plugin/conductor/scripts/tests/test-check-skills.sh plugin/conductor/scripts/tests/test-check-agents.sh plugin/conductor/scripts/tests/test-check-instruction-budget.sh plugin/conductor/scripts/tests/test-guard-agent-write.sh plugin/conductor/scripts/tests/test-check-blueprints.sh plugin/conductor/scripts/tests/test-check-artifact-fidelity.sh scripts/tests/test-hook-exit-parc.sh plugin/dev-orchestrator/scripts/tests/test-inject-mcp-tools.sh plugin/dev-orchestrator/scripts/tests/test-check-capability-activation.sh plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh plugin/_internal/tests/test-vibeflow-update.sh plugin/business-pilot-bundle/scripts/tests/test-business-pilot-bundle.sh plugin/content-bundle/scripts/tests/test-content-bundle.sh plugin/growth-bundle/scripts/tests/test-growth-bundle.sh plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh; do bash "$s" >/dev/null 2>&1 || echo "SUITE-ROUGE $s"; done; bash plugin/conductor/scripts/check-instruction-budget.sh >/dev/null 2>&1 || echo "BUDGET-ROUGE"; bash scripts/check-version-sync.sh >/dev/null 2>&1 || echo "VERSION-SYNC-ROUGE"; bash plugin/conductor/scripts/check-blueprints.sh >/dev/null 2>&1 || echo "BLUEPRINTS-ROUGE"; echo REJEU-FIN</automated>
    <fails_when>une ligne SUITE-ROUGE, BUDGET-ROUGE, VERSION-SYNC-ROUGE ou BLUEPRINTS-ROUGE apparaît avant REJEU-FIN (BUDGET-ROUGE est attendu et consigné, pas un échec, seulement si 43-04 a retenu plafond-dur-socle)</fails_when>
    <automated>fail=0; for d in plugin/*/; do find "$d" -name SKILL.md -type f | grep -q . || continue; if [ -f "$d/module.json" ] && grep -Eq '"type"[[:space:]]*:[[:space:]]*"doc-only"' "$d/module.json"; then continue; fi; bash plugin/conductor/scripts/check-skills.sh --strict --skills-dir="$d" >/dev/null 2>&1 || fail=$((fail+1)); done; reg=(); for d in plugin/*/agents; do reg+=("--agent-registry-dir=$d"); done; for d in plugin/*/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --resolve-agents=strict --agents-dir="$d" "${reg[@]}" >/dev/null 2>&1 || fail=$((fail+1)); done; echo "CORPUS-REEL fail=$fail"</automated>
    <fails_when>la ligne imprimée n'est pas exactement « CORPUS-REEL fail=0 »</fails_when>
    <automated>git fetch -q origin main 2>/dev/null; bash scripts/check-gate-touche.sh; echo "G2 rc=$?"; bash scripts/check-baseline-arbitrage.sh; echo "G1 rc=$?"; base=$(git log -1 --format=%H -- '.planning/workstreams/gouvernance/phases/VFDO-43-*/43-0*-PLAN.md') && liste=$(git diff --name-only "$base" HEAD -- .github) && printf '%s' "$liste" | grep -c .</automated>
    <fails_when>« G2 rc=… » ne vaut pas 0, ou « G1 rc=… » ne vaut ni 0 ni 3, ou la dernière valeur n'est pas 0 (un fichier .github touché)</fails_when>
    <automated>R=$(git rev-parse --show-toplevel); LAB=$(mktemp -d); export HOME=$(mktemp -d); (cd "$LAB" && git init -q && printf '{"mcpServers":{"mobile-mcp":{"command":"x"}}}\n' > .mcp.json); journal="$LAB/journal.txt"; : > "$journal"; bash "$R/plugin/_internal/resolve-deps.sh" dev-orchestrator | sort -u | while IFS= read -r m; do [ -n "$m" ] && (cd "$LAB" && VIBEFLOW_CACHE="$R/plugin" VF_SCOPE=project bash "$R/plugin/_internal/vibeflow-update.sh" install "$m" >> "$journal" 2>&1); done; (cd "$LAB" && bash .claude/scripts/check-skills.sh --strict | grep -q '✓ skills conformes') && (cd "$LAB" && bash .claude/scripts/check-agents.sh --strict >/dev/null) && grep 'WARNING:' "$journal" | grep -q 'XcodeBuildMCP' && echo LAB-FRAIS-FIN-OK</automated>
    <fails_when>la ligne LAB-FRAIS-FIN-OK est absente</fails_when>
  </verify>
  <acceptance_criteria>
    - REJEU-FIN sans aucune ligne ROUGE (hors BUDGET-ROUGE consigné sous plafond-dur-socle) ; `CORPUS-REEL fail=0` ; `G2 rc=0` ; G-1 à 0 ou 3 ; aucun fichier .github touché ; `LAB-FRAIS-FIN-OK`
    - le SUMMARY contient les sections « Relecture Samuel », « Résidus signalés » et « Artifacts this phase produces », avec au moins trois hashes dans la première
  </acceptance_criteria>
  <done>La phase est rejouée de bout en bout sur fixtures, corpus réel et labs frais ; la PR porte le relevé de relecture de Samuel et la liste honnête de ce qui reste ouvert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation de module → lecteur et lab | le CHANGELOG et le README sont la trace publique des décisions |
| numéro de version partagé avec fiabilite → merge | collision possible sur conductor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-50 | Repudiation | attributions du CHANGELOG et des commits | low | mitigate | décisions de Willy citées avec canal et date ; décisions de planification nommées comme telles (critère d'acceptation) |
| T-43-51 | Tampering | numéro de version de conductor | medium | mitigate | version lue sur disque ; renumérotation au rebase après fiabilite (en-tête du jalon) ; check-version-sync.sh |
| T-43-52 | Spoofing (faux vert) | rejeu partiel | medium | mitigate | rejeu des suites voisines, du corpus réel, de G-1, G-2 et de deux labs frais sous HOME temporaire |
</threat_model>

<verification>
- REJEU-FIN, CORPUS-REEL fail=0, G2 rc=0, G-1 conforme, aucun .github touché, LAB-FRAIS-FIN-OK.
</verification>

<success_criteria>
- FABR-10 (c) complet (vibeflow-update.sh en 43-05, vf-calibrate ici) ; conductor documenté et en mineure ; la phase entière rejouée verte ; relevés prêts pour la PR.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-skills.sh` (options `--skills-dir=`, `--file`, `--strict`, `--allow-empty`, `--hook`, `--third-party-prefix=`, `--no-third-party-prefix`) | nouveau gate | 43-01 |
| `plugin/conductor/scripts/tests/test-check-skills.sh` (T1 à T32 ; MUT-S1 à MUT-S3, MUT-SD1, MUT-SD2, MUT-DR1 à MUT-DR3) | nouvelle suite | 43-01, 43-02 |
| clés de frontmatter des skills `vf-nature`, `ecrit`, `vf-rubrique-juge`, `vf-gate-bloquant`, `vf-livrable-tiers`, `vf-couche-qualite` ; ensemble `VIBEFLOW_SKILL_FIELDS` | contrat de clés (costly) | 43-01 |
| liste de manifeste `champs_frontmatter_skills` (septième liste de `check-agents-manifest.json`) | manifeste daté | 43-01 |
| `decouvrir_skills()`, `valider_nature()`, `valider_ecrit()`, `invariant_procedure()` | check-skills.sh | 43-01 |
| `detecter_derive()`, `ecart_nature_marqueurs()`, `titres_du_corps()`, `MOTIFS_MARQUEURS` | check-skills.sh | 43-02 |
| `T107` (manifeste sans septième liste), `T108`, `T109`, `MUT-M1`, `valider_mcp_tools()` | check-agents.sh et sa suite | 43-01, 43-05 |
| `VF_SKILL_LINE_CAP`, verdict `DEPASSEMENT-SKILL-ADR029`, `BILAN-SKILLS`, `SKILL-1` à `SKILL-6`, `MUT-8` à `MUT-10` | check-instruction-budget.sh et sa suite | 43-04 |
| `VF_BOOTSTRAP_TOKEN_CAP`, ligne `BOOTSTRAP :`, verdict selon l'option, clé de baseline `@bootstrap:<corpus>` (options ratchet), `BOOT-1` à `BOOT-5`, `MUT-11` | métrique bootstrap | 43-04 |
| `malformed_found`, refus ERROR + rc 1 d'une vf-mcp-tools malformée ; message honnête de serveur absent et sources nommées ; `T16` durci, `T22a` à `T22d`, `T32` à `T34`, `MUT-A` | inject-mcp-tools.sh et sa suite | 43-05 |
| relais WARNING/ERROR dans `inject_lab_mcp_into_agents` ; `T55`, `T56` | vibeflow-update.sh et sa suite | 43-05 |
| spec fabrique §1.2 et §7.2 amendées | documentation | 43-05 |
| question vf-nature des deux étages de skill-creator | prompts | 43-03 |
| conductor, skill-creator en mineure ; dev-orchestrator en patch | versions de module | 43-06, 43-03, 43-05 |

## Flagged assumptions

- **Sondes de bords spec-less FABR-06 à FABR-10** : les cinq items `unclassified` / `unresolved` sont surfacés chacun dans le plan qui porte l'exigence (43-01 pour FABR-06 et la clause manifeste de FABR-09, 43-02 pour FABR-07, 43-03 pour FABR-08, 43-04 pour FABR-09, 43-05 pour FABR-10) ; ce plan ne les résout pas d'office.
- **Pas de modification de ci.yml** : check-skills.sh atteint la CI par la découverte des suites (`find plugin scripts -path '*/tests/test-*.sh'`, ci.yml l.218) et ses cas sur l'arbre réel (T4, T32) ; la fraîcheur du manifeste partagé reste jugée par les quatre appels strict de check-agents.sh. Aucun chemin CODEOWNERS `.github/` n'est touché.
- **Hook SessionStart non câblé** : décision de plan (43-PATTERNS.md demandait de la trancher) ; à rouvrir avec la mise en conformité du corpus.
- **Ledger REQUIREMENTS.md** : les cases FABR-06 à FABR-10 sont cochées par la vérification de phase, pas par ce plan (précédent Phase 42 ; `requirements.mark-complete` résout vers fiabilite sur ce dépôt).

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-43-fabrique-gate-des-skills-par-nature-et-alignement-de-skill-c/43-06-SUMMARY.md` when done — commit `.planning/` séparé, avec les sections « Relecture Samuel », « Résidus signalés » et « Artifacts this phase produces ».
</output>

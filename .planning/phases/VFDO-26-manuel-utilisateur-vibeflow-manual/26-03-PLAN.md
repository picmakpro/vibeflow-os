---
phase: 26-manuel-utilisateur-vibeflow-manual
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - manual/toc.yml
  - manual/fr/README.md
  - manual/en/README.md
  - manual/fr/02-concepts/
  - manual/en/02-concepts/
autonomous: true
requirements: []   # TBD — ROADMAP §Phase 26 porte `Requirements: TBD`.
user_setup: []

estimate:
  tokens: 150000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Un lecteur sait ce qu'est un « lab », concrètement — le mot central du produit, jamais défini nulle part dans le repo aujourd'hui (M-3)."
    - "Un lecteur dispose d'un glossaire du PRODUIT (lab, scope, bundle, team-kernel, driver lock, DAG, halt condition, juge frais, gate machine, rapport typé, worktree, anti-thrash) distinct du lexique méthodologique existant (M-2)."
    - "Un lecteur comprend qui fait quoi entre VibeFlow, le moteur GSD et Superpowers, et ce qui se passe sans l'un d'eux (M-5)."
    - "Le manuel documente 9 principes, sourcés du canon VIBEFLOW_CORE.md, et jamais les 7 principes que le canon lui-même qualifie d'historiques (D-09)."
    - "Aucun fichier sous manual/ n'apparaît dans git status ni dans aucun commit (D-14)."
  artifacts:
    - "manual/{fr,en}/02-concepts/qu-est-ce-qu-un-lab.md"
    - "manual/{fr,en}/02-concepts/modules-et-bundles.md"
    - "manual/{fr,en}/02-concepts/agents-skills-commandes.md"
    - "manual/{fr,en}/02-concepts/vibeflow-gsd-superpowers.md"
    - "manual/{fr,en}/02-concepts/les-9-principes.md"
    - "manual/{fr,en}/02-concepts/gates-et-validation-humaine.md"
    - "manual/{fr,en}/02-concepts/glossaire.md"
  key_links:
    - "glossaire.md → toutes les pages des thèmes 03 à 07 : c'est lui qui rend le reste lisible, il doit être posé tôt (D-05 §2)."
    - "les-9-principes.md → plugin/reference/content/methodology/VIBEFLOW_CORE.md : citation, jamais recopie intégrale."
    - "gates-et-validation-humaine.md → ADR-031 : l'engagement central envers l'utilisateur, très haut dans le manuel."
---

<objective>
Livrer la **priorité absolue du mandat** (partie 2/2) : le thème `02-concepts` complet en français et
en anglais.

Purpose: sans ce vocabulaire, les cinq thèmes suivants sont illisibles (D-05 §2). Trois des cinq
manques prioritaires de CONTEXT.md se ferment ici — **M-2** (glossaire produit), **M-3** (« lab »
jamais défini) et **M-5** (VibeFlow ↔ GSD ↔ Superpowers). La matière doctrinale existe en abondance
mais **à l'altitude du concepteur** (`VIBEFLOW_CORE.md`, 783 lignes) : ce thème écrit la marche du
bas, il ne réécrit pas la doctrine.

Output: 14 fichiers de pages (7 pages × 2 langues), `toc.yml` enrichi, aucun commit.

**Contrainte machine de la phase (D-14) — vaut pour les 3 tâches de ce plan :** `manual/` est exclu
de git par `.git/info/exclude:7`. `git add` sur ce chemin échoue. **Aucune tâche de ce plan ne
commite. Write/Edit et exécution locale seulement. Ne jamais `git add` un chemin sous `manual/`.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-02-SUMMARY.md
@plugin/reference/content/methodology/VIBEFLOW_CORE.md
@plugin/reference/content/AXIOMES-ENFORCEMENT.md
</context>

---

## Invariants de rédaction

Identiques aux 8 invariants énoncés au plan `26-02` (bilingue par incrément · `toc.yml` ne promet
rien · l'EN se produit, il ne se traduit pas (D-12) · zéro version en dur (D-11) · 100-200 lignes,
bascule à 300 ou 3 H2 (D-04) · aucun jargon non défini à sa première occurrence · blocs générés
intouchés à la main (D-03) · **aucun commit** (D-14)). Ils ne sont pas répétés ici ; ils s'appliquent.

**Invariant supplémentaire propre à ce thème :** ce thème **cite** la doctrine, il ne la recopie pas.
`VIBEFLOW_CORE.md` fait 783 lignes ; une page du manuel en fait 100 à 200. Chaque page nomme sa
source canonique et y renvoie pour le lecteur qui veut descendre — c'est aussi le rôle du thème
`07-sous-le-capot`, qui sert de pont vers `docs/`.

---

## Artifacts this plan produces

- `manual/fr/02-concepts/` et `manual/en/02-concepts/`, 7 pages chacun (mêmes noms de fichiers, H-1) :
  `qu-est-ce-qu-un-lab.md` · `modules-et-bundles.md` · `agents-skills-commandes.md` ·
  `vibeflow-gsd-superpowers.md` · `les-9-principes.md` · `gates-et-validation-humaine.md` ·
  `glossaire.md`
- `manual/toc.yml` : thème `02-concepts` ouvert, 7 entrées `pages:` ajoutées
- `manual/fr/README.md` et `manual/en/README.md` : parcours guidé « je veux comprendre » ajouté

---

<tasks>

<task type="auto">
  <name>Task 1: qu-est-ce-qu-un-lab + modules-et-bundles + agents-skills-commandes (FR et EN)</name>
  <files>manual/fr/02-concepts/qu-est-ce-qu-un-lab.md, manual/en/02-concepts/qu-est-ce-qu-un-lab.md, manual/fr/02-concepts/modules-et-bundles.md, manual/en/02-concepts/modules-et-bundles.md, manual/fr/02-concepts/agents-skills-commandes.md, manual/en/02-concepts/agents-skills-commandes.md, manual/toc.yml</files>
  <read_first>
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-INVENTAIRE-MATIERE.md` §11
      (M-2, M-3) et §8 (les 17 modules, leur description `module.json`).
    - `plugin/conductor/module.json` — champs `mandatory` et `requires` : la définition machine du
      socle obligatoire.
    - `plugin/business-pilot-bundle/module.json`, `plugin/content-bundle/module.json`,
      `plugin/growth-bundle/module.json` — ce qu'est un « bundle » sur le disque.
    - `plugin/reference/content/vocabulary/lexique.md` — le lexique **méthodologique** existant, à ne
      pas dupliquer : le glossaire produit (task 3) est un autre objet.
    - `CLAUDE.md` du repo §« Ce qu'est ce repo » — la seule définition indirecte d'un module.
  </read_first>
  <action>
    **`02-concepts/qu-est-ce-qu-un-lab.md`** (FR + EN) — **M-3**, le manque le plus embarrassant du
    repo : « lab » est le mot central du produit (« il fabrique des labs », « ton lab », « lab frais »,
    « altitude lab ») et n'est défini nulle part. Trancher franchement et dire ce que c'est
    **matériellement** : un dossier ? un dépôt git ? un scope Claude Code ? les trois selon le cas ?
    Répondre sans esquive, avec un exemple de chacun. Dire ce qui fait qu'un dossier **devient** un
    lab, ce qui distingue un lab dev d'un lab non-dev, et ce qu'un lab n'est pas. Renvoyer à
    `07-sous-le-capot/anatomie-d-un-lab-installe.md` pour l'inventaire des fichiers (M-4), sans le
    dupliquer ici.

    **`02-concepts/modules-et-bundles.md`** (FR + EN) — le vocabulaire produit de l'installation :
    module, socle obligatoire, dépendances entre modules, bundle métier, scope. Établir chaque notion
    **depuis le disque** (D-11) : le champ `mandatory` et le champ `requires` de `module.json` disent
    ce qu'est le socle, mieux que n'importe quelle prose. **Aucun numéro de version, aucun tableau des
    17 modules ici** — le catalogue a son thème (`03-modules/catalogue.md`) et une table de versions
    dans un manuel est une promesse de mensonge (13 des 17 versions du README sont déjà périmées).

    **`02-concepts/agents-skills-commandes.md`** (FR + EN) — les briques Claude Code vues par
    l'utilisateur : une commande, un skill, un agent, un orchestrateur, un worker interne, un juge.
    Dire ce que le lecteur **voit** de chacune (une commande se tape ; un skill se déclenche sur une
    intention exprimée en langage naturel ; un agent est une session avec son propre contexte), et
    surtout **pourquoi certains agents n'ont aucune commande d'incarnation** (ADR-044 : un worker
    interne n'est dispatché que par son orchestrateur). C'est la question que se pose tout lecteur qui
    ouvre `plugin/*/agents/` et n'y retrouve pas ses commandes. Renvoyer aux listes exhaustives de
    `06-reference/` plutôt que d'en poser une ici.

    Terminer par : ouvrir le thème `02-concepts` dans le bloc `themes:` de `toc.yml` (titres FR/EN),
    inscrire les 3 entrées `pages:` dans l'ordre, relancer `build-nav.sh`, relancer `check-manual.sh`
    jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 02-concepts/qu-est-ce-qu-un-lab.md 02-concepts/modules-et-bundles.md 02-concepts/agents-skills-commandes.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; test "$(grep -c '^[[:space:]]*- path: 02-concepts/' manual/toc.yml || true)" = "3"; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 6 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `qu-est-ce-qu-un-lab.md` donne une définition **matérielle** et non circulaire du mot « lab »,
      avec au moins deux exemples de nature différente (M-3).
    - `modules-et-bundles.md` établit le socle obligatoire depuis les champs `mandatory`/`requires`
      des `module.json`, et ne contient **aucun** tableau des 17 modules ni aucun numéro de version
      (D-11, C5 du gate passe).
    - `agents-skills-commandes.md` explique pourquoi certains agents n'ont pas de commande exposée
      (ADR-044).
    - Aucune des trois pages ne duplique le lexique méthodologique de
      `plugin/reference/content/vocabulary/lexique.md` : elles renvoient au glossaire produit
      (task 3) pour les termes du produit.
    - `toc.yml` porte 3 entrées `- path: 02-concepts/`, et le bloc `themes:` porte l'entrée
      `02-concepts` avec ses titres FR/EN.
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Les trois mots que le repo emploie sans jamais les définir — lab, module, agent — le sont, à hauteur de lecteur.</done>
</task>

<task type="auto">
  <name>Task 2: vibeflow-gsd-superpowers + les-9-principes (FR et EN)</name>
  <files>manual/fr/02-concepts/vibeflow-gsd-superpowers.md, manual/en/02-concepts/vibeflow-gsd-superpowers.md, manual/fr/02-concepts/les-9-principes.md, manual/en/02-concepts/les-9-principes.md, manual/toc.yml</files>
  <read_first>
    - `plugin/reference/content/methodology/VIBEFLOW_CORE.md` — le canon : les 9 principes P1-P9,
      l'architecture en 5 composants, les 5 registres. **Lire aussi la ligne ~308**, où le canon
      qualifie lui-même les 7 principes de « principes historiques », et la table de versions
      (~ligne 765) qui les date de la v3 pré-Core.
    - `INSTALL.md` §« Dépendances externes (GSD / Superpowers) » (~134-144) — seul endroit du repo qui
      nomme la relation, et il le fait sous l'angle de la désinstallation.
    - `docs/ADR.md` — ADR-055 (un projet = un seul moteur, frontière planning-core ↔ GSD), ADR-057
      (frontières avec les briques tierces), ADR-058 (le moteur entre dans le périmètre de
      `/vf-update`).
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md` §D-09.
  </read_first>
  <action>
    **`02-concepts/vibeflow-gsd-superpowers.md`** (FR + EN) — **M-5**. Le cycle de dev repose
    entièrement sur le moteur GSD (`@opengsd/gsd-core`), et le lecteur n'a aujourd'hui que onze lignes
    d'`INSTALL.md` orientées désinstallation plus trois ADR internes. Dire pour un humain : ce qu'est
    le moteur GSD et ce qu'il apporte ; ce qu'est Superpowers ; **ce que VibeFlow ajoute par-dessus**
    (c'est la valeur cœur du produit : dire « aide-moi à dev » déclenche le pipeline complet sans
    jamais avoir à connaître GSD) ; ce qui se passe **sans** GSD installé ; la règle « un projet = un
    seul moteur » (ADR-055) et pourquoi elle existe ; qui met à jour quoi (ADR-058). Un petit schéma
    en trois couches est bienvenu — s'il est en mermaid, il reste **décoratif** et une phrase ou une
    liste porte l'information réelle (D-06 s'applique à toute carte mermaid du manuel).

    **`02-concepts/les-9-principes.md`** (FR + EN) — **D-09** : le manuel documente **9 principes**,
    sourcés du canon `VIBEFLOW_CORE.md`. Les sept principes qu'on trouve encore dans
    `VIBEFLOW_PHILOSOPHY.md` et `VIBEFLOW_EXPLAINED.md` sont qualifiés d'**historiques par le canon
    lui-même** : ne pas les présenter comme actuels, et ne pas corriger ces deux fichiers (hors
    périmètre). Écrire chaque principe en deux à quatre lignes, en langage d'utilisateur : ce qu'il
    signifie pour quelqu'un qui *utilise* VibeFlow, pas pour quelqu'un qui le conçoit. Renvoyer au
    canon pour la version longue. Aucune paraphrase intégrale des 783 lignes.
    Si, à la lecture, un principe s'avère intraduisible en conséquence observable pour l'utilisateur,
    le dire franchement plutôt que d'inventer un bénéfice — et le signaler dans le SUMMARY.

    Terminer par : inscrire les 2 entrées `pages:` dans `toc.yml`, relancer `build-nav.sh`, relancer
    `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 02-concepts/vibeflow-gsd-superpowers.md 02-concepts/les-9-principes.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for l in fr en; do c=$(grep -vc '^\s*$' "manual/$l/02-concepts/les-9-principes.md"); [ "$c" -gt 0 ]; done; test "$(grep -c '^[[:space:]]*- path: 02-concepts/' manual/toc.yml || true)" = "5"; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers existent ; chacun fait entre 100 et 300 lignes (D-04).
    - `vibeflow-gsd-superpowers.md` répond aux quatre questions de M-5 : qui fait quoi, ce que
      VibeFlow ajoute, ce qui se passe sans GSD, qui met à jour quoi.
    - `les-9-principes.md` énumère **neuf** principes et **aucun** ne provient de la liste historique
      de sept : le décompte est vérifiable en lisant la page (D-09).
    - Ni `plugin/reference/content/methodology/VIBEFLOW_PHILOSOPHY.md` ni
      `VIBEFLOW_EXPLAINED.md` n'ont été modifiés — hors périmètre (`git status` sur `plugin/` vide).
    - Toute carte mermaid présente est décorative et suivie d'une information réelle en prose ou en
      liste (D-06).
    - `toc.yml` porte 5 entrées `- path: 02-concepts/`.
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le lecteur sait sur quoi VibeFlow est posé, ce qu'il ajoute, et quelle doctrine s'applique — la bonne, celle du canon.</done>
</task>

<task type="auto">
  <name>Task 3: gates-et-validation-humaine + glossaire (FR et EN) + parcours « comprendre »</name>
  <files>manual/fr/02-concepts/gates-et-validation-humaine.md, manual/en/02-concepts/gates-et-validation-humaine.md, manual/fr/02-concepts/glossaire.md, manual/en/02-concepts/glossaire.md, manual/fr/README.md, manual/en/README.md, manual/toc.yml</files>
  <read_first>
    - `plugin/reference/content/AXIOMES-ENFORCEMENT.md` — les 3 axiomes (enforcement > prose, filet de
      tests, preuve avant done) et « qui applique quoi ».
    - `plugin/reference/content/methodology/patterns/11-halt-conditions.md` — pourquoi un agent
      s'arrête.
    - `plugin/reference/content/methodology/patterns/12-cloisonnement-outils.md` — pourquoi le juge
      n'est jamais l'auteur.
    - `docs/ADR.md` — **ADR-031** (jamais de fix ni de suppression sans validation humaine :
      l'engagement central envers l'utilisateur), ADR-029 (densité), ADR-035 (architecture AI-Safe).
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md` §M-2 — la liste
      exacte des termes que le glossaire doit couvrir.
    - `plugin/reference/content/vocabulary/lexique.md` — pour **ne pas** le dupliquer et y renvoyer.
  </read_first>
  <action>
    **`02-concepts/gates-et-validation-humaine.md`** (FR + EN) — la page qui doit être lue tôt, parce
    qu'elle porte l'engagement le plus important du produit envers son utilisateur : **rien n'est
    corrigé ni supprimé sans validation humaine** (ADR-031). Couvre : ce qu'est un gate machine et
    pourquoi VibeFlow préfère un script qui refuse à un paragraphe qui recommande (les 3 axiomes) ;
    ce qu'est une halt condition et pourquoi un agent qui s'arrête est un agent qui fonctionne
    (pattern 11) ; pourquoi le juge n'est jamais l'auteur (pattern 12) ; et donc ce que l'utilisateur
    verra concrètement — des arrêts, des demandes de confirmation, des refus motivés. Formuler ça
    comme une **promesse**, pas comme une contrainte subie.

    **`02-concepts/glossaire.md`** (FR + EN) — **M-2**, le glossaire du **produit**, explicitement
    distinct du lexique **méthodologique** déjà écrit (`vocabulary/lexique.md`, 118 lignes, qui couvre
    agents, registres, constitution). Doivent y figurer au minimum les termes que CONTEXT.md nomme :
    lab, scope, module, bundle, socle, team-kernel, driver lock, DAG, digest de mission, halt
    condition, juge frais, gate machine, rapport typé, worktree, anti-thrash, frontière ready. Une
    définition de deux à quatre lignes chacun, en langage courant, avec un lien relatif vers la page
    du manuel qui le développe **si elle existe déjà** — sinon la définition se suffit à elle-même
    (invariant 2 : jamais de lien vers une page non écrite). Ouvrir la page par une phrase disant où
    trouver le lexique méthodologique, pour que les deux ne se concurrencent pas.
    Si le glossaire dépasse 200 lignes, ne pas tasser : le scinder en deux pages (`glossaire.md` et
    une seconde page du même thème), le thème `02-concepts` ayant alors 8 pages — sous le plafond dur
    de 9 fixé par D-04.

    **Ajouter aux deux README de langue** le parcours guidé « je veux comprendre avant d'agir »,
    pointant vers `02-concepts/qu-est-ce-qu-un-lab.md`.

    Terminer par : inscrire les entrées `pages:` restantes dans `toc.yml`, relancer `build-nav.sh`,
    relancer `check-manual.sh` jusqu'au vert.

    Aucun commit — `manual/` est exclu de git (`.git/info/exclude`), ne jamais `git add` ce chemin.
  </action>
  <verify>
    <automated>set -e; for p in 02-concepts/gates-et-validation-humaine.md 02-concepts/glossaire.md; do for l in fr en; do f="manual/$l/$p"; test -f "$f"; n=$(wc -l "$f" | awk '{print $1}'); [ "$n" -ge 100 ]; [ "$n" -le 300 ]; done; done; for t in lab scope bundle worktree DAG; do grep -qi "$t" manual/fr/02-concepts/glossaire.md; done; grep -q '02-concepts/' manual/fr/README.md; grep -q '02-concepts/' manual/en/README.md; c=$(grep -c '^[[:space:]]*- path:' manual/toc.yml); [ "$c" -ge 14 ]; t=$(grep -c '^[[:space:]]*- path: 02-concepts/' manual/toc.yml); [ "$t" -ge 7 ]; [ "$t" -le 9 ]; bash manual/.tools/build-nav.sh; bash manual/.tools/check-manual.sh; test -z "$(git status --porcelain -- manual)"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 fichiers (ou 6 si le glossaire a été scindé) existent dans les deux langues, chacun entre
      100 et 300 lignes (D-04).
    - `gates-et-validation-humaine.md` énonce explicitement l'engagement ADR-031 et explique halt
      condition et cloisonnement juge/auteur en langage d'utilisateur.
    - `glossaire.md` définit **les 16 termes** listés en M-2 de CONTEXT.md, et ouvre par un renvoi au
      lexique méthodologique existant pour ne pas le concurrencer.
    - Aucun lien du glossaire ne pointe vers une page non écrite : le contrôle C3 du gate passe.
    - Les deux README de langue portent le parcours « comprendre », dont le lien résout.
    - `toc.yml` porte au moins 14 entrées `- path:` (7 pour `01-demarrer`, 7 ou 8 pour
      `02-concepts`), et le thème `02-concepts` ne dépasse pas 9 pages (D-04).
    - `bash manual/.tools/check-manual.sh` sort en **0**.
    - `git status --porcelain -- manual` sort vide ; aucun `git add`/`git commit` sur `manual/`.
  </acceptance_criteria>
  <done>Le vocabulaire du produit est posé et la promesse centrale est écrite noir sur blanc. Les cinq thèmes suivants deviennent lisibles.</done>
</task>

</tasks>

<threat_model>
Sans objet pour ce plan : markdown seul, aucun code exécutable, aucune dépendance, aucun install de
paquet. La seule menace de la phase (fuite de `manual/` vers un artefact versionné) est instruite en
`T-26-04` du plan 26-01 et mitigée ici par l'assertion `git status --porcelain -- manual` vide dans
les trois tâches (D-14).
</threat_model>

<verification>
1. `bash manual/.tools/check-manual.sh` sort en **0**.
2. Les arbres `manual/fr` et `manual/en` sont isomorphes (D-01).
3. Le thème `02-concepts` compte 7 ou 8 pages par langue, jamais plus de 9 (D-04).
4. Aucune version en dur dans les nouvelles pages : C5 du gate passe (D-11).
5. `git status --porcelain -- manual` sort **vide**.
6. `git status --porcelain -- plugin docs README.md README.fr.md INSTALL.md scripts .github` sort
   **vide** : aucune source citée n'a été modifiée, en particulier pas `VIBEFLOW_EXPLAINED.md` dont
   le doc-drift est nommé et volontairement non traité (CONTEXT.md §deferred).
</verification>

<success_criteria>
- Les 7 (ou 8) pages de `02-concepts` existent en FR **et** en EN.
- M-2, M-3 et M-5 sont fermés.
- Le manuel documente 9 principes, pas 7 (D-09).
- Le gate est vert. Git n'a rien vu.
- **À ce stade, le mandat prioritaire est tenu** : infrastructure + portes d'entrée + `01-demarrer` +
  `02-concepts`. Les plans 26-04 à 26-08 peuvent être différés sans casser le gate ni le manuel.
</success_criteria>

<output>
Create `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-03-SUMMARY.md` when done.
Y consigner : le nombre de lignes de chaque page, le verdict de `check-manual.sh`, tout principe du
canon qui n'a pas pu être traduit en conséquence observable pour l'utilisateur, et la confirmation
explicite que `git status --porcelain -- manual` est resté vide de bout en bout.
</output>

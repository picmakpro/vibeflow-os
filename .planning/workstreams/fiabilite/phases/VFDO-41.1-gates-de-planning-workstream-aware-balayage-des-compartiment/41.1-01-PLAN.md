---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 01
lot: L01 — vf_ws_enumerate, la primitive d'énumération partagée (D-01)
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WSAW-01]
files_modified:
  - plugin/planning-core/scripts/workstream-policy.sh
  - plugin/planning-core/scripts/tests/test-workstream-policy.sh

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "`vf_ws_enumerate <planning_dir>` imprime UN chemin ABSOLU par compartiment `<planning_dir>/workstreams/<nom>/` PRÉSENT SUR LE DISQUE, un par ligne, sur stdout — jamais de chemin relatif, jamais de chemin qui n'existe pas au moment de l'appel"
    - "Elle ne lit JAMAIS `GSD_WORKSTREAM`, `VF_WORKSTREAM`, `VF_STATE_WORKSTREAM` ni `<planning_dir>/active-workstream` — dérive UNIQUEMENT du disque via `find`/glob sur `<planning_dir>/workstreams/*/`. Un test invoque la fonction avec les quatre variables exportées à des valeurs qui pointeraient ailleurs et prouve que la sortie est identique à l'appel sans elles"
    - "Chaque entrée passe par `vf_ws_path_nolink` — un lien symbolique sur `<planning_dir>/workstreams/` lui-même OU sur un `<nom>/` individuel est EXCLU et jamais suivi, avec un message stderr nommé (même posture que `vf_ws_dir_resolve`)"
    - "Codes de sortie, aucun implicite : 0 = au moins un compartiment listé sur stdout · 3 = SILENCE, `<planning_dir>/workstreams/` absent (dépôt non partitionné, état nominal — même code que `check-divergence.sh`) · 2 = `workstreams/` présent mais illisible, lien symbolique, OU présent-mais-vide-après-filtre (tous liens exclus, ou glob vide après filtrage) — JAMAIS un 0 déguisé sur ce dernier cas, c'est la fermeture explicite du trou WSAW-01"
    - "Fonction PURE au sens de ce fichier : aucune écriture, aucun `exit` du shell appelant (elle est SOURCÉE, pas exécutée) — elle `return`s son code, exactement comme `vf_ws_dir_resolve`/`vf_ws_resolve` le font déjà dans ce même fichier"
    - "Documentée dans le CONTRAT D'APPEL en tête de fichier (lignes ~48-53 actuelles), au même endroit et dans la même forme que les quatre primitives existantes — pas une note à part"
    - "La suite `test-workstream-policy.sh` couvre les CINQ cas : 0 (un compartiment, deux compartiments), 3 (`workstreams/` absent), 2 (lien symbolique sur `workstreams/` lui-même), 2 (lien symbolique sur un compartiment individuel, les AUTRES compartiments légitimes restant listés), 2 (`workstreams/` existe mais est vide) — plus le test d'indifférence aux quatre canaux d'environnement ci-dessus"
    - "QUAL-01 : au moins UN mutant tué qui neutralise le filtre anti-lien (`vf_ws_path_nolink` non appelé, ou son code 2 ignoré) — la fixture « lien symbolique sur un compartiment » doit alors lister le lien sur le mutant tout en restant filtrée sur l'original ; comptage par `cmp`/`bash -n`, jamais un mutant identique à l'original"
  artifacts:
    - path: "plugin/planning-core/scripts/workstream-policy.sh"
      provides: "vf_ws_enumerate — énumération disque des compartiments, filtrée anti-lien"
      contains: "vf_ws_enumerate()"
    - path: "plugin/planning-core/scripts/tests/test-workstream-policy.sh"
      provides: "cas 0/2/3 de vf_ws_enumerate + preuve d'indifférence aux canaux d'environnement + mutant anti-lien"
  key_links:
    - "`plugin/conductor/scripts/check-divergence.sh` source déjà ce fichier via une recherche à deux candidats (conductor d'abord, planning-core en repli) — c'est le point d'intégration du plan 41.1-02, zéro nouveau sourcing à câbler"
    - "Modèle de contrat à suivre EXACTEMENT : `vf_ws_dir_resolve` (lignes ~239-251) pour la forme des trois codes de retour et l'appel à `vf_ws_path_nolink` sur `workstreams/` PUIS sur l'entrée individuelle ; `vf_ws_path_nolink` (lignes ~226-230) est réutilisée telle quelle, jamais réimplémentée"
    - "`planning-core` a `requires: []` (module.json) — cette primitive ne doit introduire AUCUNE dépendance externe, ni sous-processus autre que `find`/`basename` déjà en usage dans ce fichier"
---

<objective>
Ajouter `vf_ws_enumerate` à `workstream-policy.sh` : la primitive UNIQUE qui liste, depuis le
disque et rien d'autre, les compartiments `.planning/workstreams/<nom>/` présents — jamais depuis
l'environnement. Elle ferme la première moitié de WSAW-01 (le mécanisme lui-même) ; le plan
41.1-02 la fait consommer par `check-divergence.sh` pour fermer la seconde moitié (dédup + trou
exit-0).

Purpose: donner à TOUT gate de planning une seule source de vérité sur « quels compartiments
existent », dérivée du disque, jamais de `GSD_WORKSTREAM`/`VF_WORKSTREAM`/du pointeur.
Output: une fonction sourcée, testée par mutation, prête à être consommée par les plans suivants.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `plugin/planning-core/scripts/workstream-policy.sh` en entier (315 lignes) — le contrat d'appel
  en tête, `vf_ws_path_nolink`, `vf_ws_dir_resolve`, `vf_ws_resolve` en particulier
- `plugin/planning-core/scripts/tests/test-workstream-policy.sh` en entier — structure `ok`/`ko`,
  fixtures `mktemp -d`
</context>

<discipline>
- Commandes proxifiées sur ce poste : `diff` et `ls` mentent — préfixer de `rtk proxy` si utilisées ;
  comparaisons par `cmp`/`comm`/`cksum`. Jamais `grep | wc -l` pour compter — `awk`.
- Fixtures : `mktemp -d`, jamais le dépôt réel. `trap` de nettoyage systématique.
- `vf_ws_enumerate` est sourcée, jamais exécutée en sous-shell dans les gates appelants : elle
  DOIT rester compatible `set -u`/`set -e` de tout script qui la source (variables locales
  déclarées `local`, aucune fuite de nom générique dans le scope appelant).
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : `vf_ws_enumerate` — contrat, implémentation, documentation en tête de fichier</name>
  <files>plugin/planning-core/scripts/workstream-policy.sh</files>
  <behavior>
    `vf_ws_enumerate <planning_dir>` :
    - `<planning_dir>/workstreams/` absent → rien sur stdout, rc=3.
    - `<planning_dir>/workstreams/` est un lien symbolique → rien sur stdout, message stderr nommé
      (« workstreams-lien-symbolique », même raison que `vf_ws_dir_resolve`), rc=2.
    - `<planning_dir>/workstreams/` existe, contient au moins un sous-répertoire non-lien → un
      chemin ABSOLU par ligne sur stdout, un par compartiment retenu, rc=0.
    - `<planning_dir>/workstreams/` existe mais chaque entrée est soit un lien symbolique soit
      absente après filtrage (dossier vide) → rien sur stdout, message stderr nommé par entrée
      rejetée, rc=2 — jamais rc=0 avec une sortie vide.
    - un compartiment individuel en lien symbolique, PARMI D'AUTRES compartiments légitimes → le
      lien est exclu (message stderr), les autres sont listés, rc=0 (au moins un compartiment
      retenu).
    - n'inspecte JAMAIS `GSD_WORKSTREAM`, `VF_WORKSTREAM`, `VF_STATE_WORKSTREAM`, ni
      `<planning_dir>/active-workstream` : la sortie est identique que ces quatre canaux soient
      exportés/présents ou non.
  </behavior>
  <action>
Lire le fichier en entier avant d'écrire — c'est la discipline déjà posée par les quatre fonctions
existantes (chaque ajout a expliqué pourquoi il n'en réimplémente aucune).

Ajouter au CONTRAT D'APPEL en tête de fichier (après la ligne `vf_ws_resolve <planning_dir>
[surcharge]` et avant la section « FRONTIÈRE ASSUMÉE ») une entrée :
```
#   vf_ws_enumerate <planning_dir>
#       Énumère les compartiments PRÉSENTS SUR LE DISQUE sous <planning_dir>/workstreams/*/ — un
#       chemin ABSOLU par ligne sur stdout, jamais depuis GSD_WORKSTREAM/VF_WORKSTREAM/le pointeur.
#       Chaque entrée est filtrée par vf_ws_path_nolink (lien exclu, jamais suivi).
#       Rend 0 = au moins un compartiment listé · 2 = workstreams/ présent mais illisible, lien
#           symbolique, ou vide après filtrage (JAMAIS un 0 de complaisance) · 3 = SILENCE,
#           workstreams/ absent (dépôt non partitionné, état nominal).
```

Implémenter, à la suite de `vf_ws_dir_resolve` (juste avant `vf_ws_file_in_ws` ou juste après,
groupée avec les primitives de résolution de chemin — pas avec `vf_ws_resolve`, qui lit
l'environnement) :
```bash
vf_ws_enumerate() { # <planning_dir> -> un chemin absolu par compartiment retenu, un par ligne
  local planning="$1" root rc found=0 entry name
  root="$planning/workstreams"
  vf_ws_path_nolink "$root"; rc=$?
  if [ "$rc" -eq 1 ]; then return 3; fi
  if [ "$rc" -eq 2 ]; then
    echo "[vf_ws_enumerate] $root est un lien symbolique — refus de le suivre, non vérifiable" >&2
    return 2
  fi
  [ -d "$root" ] || return 3
  for entry in "$root"/*/; do
    [ -e "$entry" ] || continue   # nullglob absent : glob non expansé si aucune entrée
    entry="${entry%/}"
    name="$(basename "$entry")"
    vf_ws_path_nolink "$entry"; rc=$?
    if [ "$rc" -eq 2 ]; then
      echo "[vf_ws_enumerate] compartiment « $name » est un lien symbolique — refus de le suivre, ignoré" >&2
      continue
    fi
    [ "$rc" -eq 0 ] || continue
    [ -d "$entry" ] || continue
    printf '%s\n' "$(cd "$entry" && pwd)"
    found=1
  done
  [ "$found" -eq 1 ] && return 0
  echo "[vf_ws_enumerate] $root présent mais vide après filtrage — non vérifiable, jamais un 0 de complaisance" >&2
  return 2
}
```
Note d'implémentation : le glob `"$root"/*/` sans `shopt -s nullglob` laisse passer le motif
littéral si rien ne matche — le test `[ -e "$entry" ] || continue` absorbe ce cas sans dépendre
d'une option de shell externe au fichier (ce fichier n'active `nullglob` nulle part ailleurs :
rester cohérent avec son style plutôt que l'introduire pour une seule fonction).

Chemin ABSOLU produit par `cd "$entry" && pwd` (jamais une concaténation `"$PWD/$entry"`, qui
casserait si `<planning_dir>` est déjà relatif à un `--path` non-`.`) — c'est le même motif que
`GSD_HOME` résolu ailleurs dans le parc de scripts (`git rev-parse --show-toplevel` en repli sur
`pwd`).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/planning-core/scripts/workstream-policy.sh && echo "syntaxe OK"'</automated>
    <fails_when>`bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/a/workstreams/dev" "$T/a/workstreams/qa"
. plugin/planning-core/scripts/workstream-policy.sh
rc=0; out="$(vf_ws_enumerate "$T/a")" || rc=$?
n=$(printf "%s\n" "$out" | awk "NF>0" | awk "END{print NR+0}")
echo "cas0 rc=$rc lignes=$n"
[ "$rc" -eq 0 ] && [ "$n" -eq 2 ]
'</automated>
    <fails_when>`cas0 rc=` différent de 0, ou `lignes=` différent de 2 — deux compartiments réels doivent tous deux être listés</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/a"
. plugin/planning-core/scripts/workstream-policy.sh
rc=0; out="$(vf_ws_enumerate "$T/a" 2>/dev/null)" || rc=$?
echo "cas_absent rc=$rc sortie=[${out:-vide}]"
[ "$rc" -eq 3 ] && [ -z "$out" ]
'</automated>
    <fails_when>`workstreams/` absent doit rendre rc=3 et une sortie stdout vide — un rc différent ou une sortie non vide indique que le SILENCE n'est pas distingué du reste</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/a/workstreams"
ln -s "$T/a/workstreams" "$T/a/workstreams-cible" 2>/dev/null || true
rm -rf "$T/a/workstreams"
ln -s "$T/a/workstreams-cible" "$T/a/workstreams"
. plugin/planning-core/scripts/workstream-policy.sh
rc=0; out="$(vf_ws_enumerate "$T/a" 2>/dev/null)" || rc=$?
echo "cas_lien_racine rc=$rc sortie=[${out:-vide}]"
[ "$rc" -eq 2 ] && [ -z "$out" ]
'</automated>
    <fails_when>un lien symbolique sur `workstreams/` lui-même doit rendre rc=2 et une sortie vide — un rc=0 ou une sortie non vide signifie que le lien a été suivi</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/a/workstreams/dev" "$T/other"
ln -s "$T/other" "$T/a/workstreams/qa"
. plugin/planning-core/scripts/workstream-policy.sh
rc=0; out="$(vf_ws_enumerate "$T/a" 2>/dev/null)" || rc=$?
n=$(printf "%s\n" "$out" | awk "NF>0" | awk "END{print NR+0}")
echo "cas_lien_mixte rc=$rc lignes=$n"
[ "$rc" -eq 0 ] && [ "$n" -eq 1 ]
'</automated>
    <fails_when>un compartiment lien parmi un compartiment légitime doit rendre rc=0 avec UNE SEULE ligne (le lien exclu, le légitime listé) — deux lignes signifie que le lien a été suivi, zéro ligne signifie que le compartiment légitime a été perdu par le filtre</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/a/workstreams/dev"
. plugin/planning-core/scripts/workstream-policy.sh
out1="$(vf_ws_enumerate "$T/a")"
out2="$(GSD_WORKSTREAM=autrechose VF_WORKSTREAM=encoreautrechose VF_STATE_WORKSTREAM=x vf_ws_enumerate "$T/a")"
echo "$T/a/workstreams/dev" > "$T/expected"
printf "%s\n" "$out1" > "$T/o1"; printf "%s\n" "$out2" > "$T/o2"
c=$(cmp -s "$T/o1" "$T/o2"; echo $?)
echo "indifference_env cmp=$c"
[ "$c" -eq 0 ]
'</automated>
    <fails_when>`indifference_env cmp=` différent de 0 — la sortie a changé selon les variables d'environnement exportées, alors qu'elle doit dériver UNIQUEMENT du disque</fails_when>
    <automated>bash --noprofile --norc -e -c '
# CORRECTION post plan-checker (FLAG-4) : WSAW-01 et la vérité must_haves nomment QUATRE canaux,
# dont ".planning/active-workstream" (un FICHIER pointeur, pas une variable) — le sondage
# précédent nexportait que les trois variables, jamais le pointeur avec un nom divergent. Ici,
# DEUX répertoires jumeaux (mêmes compartiments), seul le second porte un pointeur divergent —
# cmp entre les deux sorties prouve que le pointeur ne change rien.
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/sans_pointeur/workstreams/dev" "$T/avec_pointeur/workstreams/dev"
printf "%s\n" "un-nom-qui-ne-correspond-a-aucun-compartiment-reel" > "$T/avec_pointeur/active-workstream"
. plugin/planning-core/scripts/workstream-policy.sh
out_sans="$(vf_ws_enumerate "$T/sans_pointeur")"
out_avec="$(vf_ws_enumerate "$T/avec_pointeur")"
n=$(printf "%s\n" "$out_avec" | awk "NF>0" | awk "END{print NR+0}")
printf "%s\n" "$out_sans" | sed "s#/sans_pointeur/#/X/#" > "$T/p1"
printf "%s\n" "$out_avec" | sed "s#/avec_pointeur/#/X/#" > "$T/p2"
c=$(cmp -s "$T/p1" "$T/p2"; echo $?)
echo "indifference_pointeur cmp=$c lignes=$n"
[ "$c" -eq 0 ] && [ "$n" -eq 1 ]
'</automated>
    <fails_when>`indifference_pointeur cmp=` différent de 0 (la sortie diffère entre les deux répertoires jumeaux au-delà du seul nom de dossier normalisé, alors que le seul écart réel entre eux est la présence d'un pointeur `active-workstream` divergent) ; ou `lignes=` différent de 1 — FLAG-4 du juge frais : WSAW-01 nomme QUATRE canaux, le pointeur-fichier n'était jusqu'ici jamais sondé</fails_when>
  </verify>
  <acceptance_criteria>
    - `vf_ws_enumerate` existe dans `workstream-policy.sh`, documentée dans le contrat d'appel en tête de fichier
    - `bash -n` passe sur le fichier entier
    - cas 0 (un et deux compartiments), 3 (absent), 2 (lien sur `workstreams/`), 0-avec-exclusion (lien sur un compartiment individuel) tous vérifiés
    - la sortie est identique, indépendamment de `GSD_WORKSTREAM`/`VF_WORKSTREAM`/`VF_STATE_WORKSTREAM` exportées ET d'un fichier pointeur `active-workstream` divergent présent sur le disque (FLAG-4 : les QUATRE canaux nommés par WSAW-01, pas seulement les trois variables)
  </acceptance_criteria>
  <done>`vf_ws_enumerate` existe, respecte le contrat 0/2/3, ne lit jamais l'environnement, et filtre les liens symboliques sans jamais les suivre.</done>
</task>

<task type="auto">
  <name>Task 2 : suite de tests — cinq cas de contrat + un mutant anti-lien tué</name>
  <files>plugin/planning-core/scripts/tests/test-workstream-policy.sh, plugin/conductor/CHANGELOG.md</files>
  <precondition>Task 1 verte — `bash -n` passe, les quatre vérifications automatisées ci-dessus sont vertes.</precondition>
  <read_first>
    - `plugin/planning-core/scripts/tests/test-workstream-policy.sh` en entier (structure `ok`/`ko`, style de fixture existant pour les autres primitives de ce fichier)
  </read_first>
  <action>
Étendre `test-workstream-policy.sh` avec une section `vf_ws_enumerate` : source directe du fichier
(pas de `bash "$TARGET"` en sous-processus — `vf_ws_enumerate` est une fonction, testée comme les
quatre autres primitives déjà couvertes dans ce fichier, par appel direct après `. "$POLICY"`).

Cas requis, chacun avec sa propre fixture `mktemp -d` locale au cas :
1. deux compartiments réels → rc=0, deux lignes, les deux chemins sont des chemins ABSOLUS qui
   `cd`-ent correctement (`[ -d "$ligne" ]` vrai pour chacune)
2. `workstreams/` absent → rc=3, stdout vide
3. lien symbolique sur `workstreams/` lui-même → rc=2, stdout vide, message stderr contenant
   « lien symbolique »
4. lien symbolique sur UN compartiment, un autre compartiment légitime à côté → rc=0, une seule
   ligne, celle du compartiment légitime
5. `workstreams/` existe, vide (aucune entrée) → rc=2, stdout vide (jamais un rc=0 à vide)
6. preuve d'indifférence : `GSD_WORKSTREAM`, `VF_WORKSTREAM`, `VF_STATE_WORKSTREAM` exportés à des
   valeurs qui, si elles étaient lues, changeraient le résultat (par exemple un nom qui ne
   correspond à AUCUN compartiment réel) → sortie identique à l'appel sans ces variables (`cmp`
   entre les deux sorties capturées dans des fichiers, jamais une comparaison de chaînes bash)

MUTANT (QUAL-01, un seul exigé pour cette primitive) — copier `workstream-policy.sh` dans le
`mktemp -d` du test, neutraliser l'appel à `vf_ws_path_nolink` sur l'entrée individuelle (le
`vf_ws_path_nolink "$entry"; rc=$?` de la boucle) en le remplaçant par `rc=0` codé en dur (édition
par `awk` vers un temporaire puis `mv`, jamais `sed -i`) : sur le cas 4 (lien parmi un légitime),
le mutant doit lister DEUX lignes (le lien suivi en plus), l'original une seule. `cmp -s` refuse un
mutant identique à l'original ; `bash -n` refuse un mutant qui ne parse plus. Trace `✓ MUT-1 TUE :
lignes_mutant=2 attendu 2, lignes_original=1 attendu 1`.

`plugin/conductor/CHANGELOG.md` : PAS touché par ce plan — `workstream-policy.sh` appartient au
module `planning-core`. Le fichier listé en `files_modified` du frontmatter de ce plan était une
erreur de copie du gabarit ; retirer cette ligne n'est pas nécessaire pour l'exécution (le plan
40 précédent laisse ce genre d'écart au SUMMARY) — consigner au SUMMARY que ce plan ne touche
AUCUN CHANGELOG (le bump de version pour `planning-core` est porté par le plan 41.1-08, en fin de
phase, une fois tous les fichiers du module stabilisés).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e plugin/planning-core/scripts/tests/test-workstream-policy.sh</automated>
    <fails_when>exit non nul ; ou une ligne `✗` dans la sortie ; ou `MUT-1` absent de la sortie</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); bash plugin/planning-core/scripts/tests/test-workstream-policy.sh > "$T/o" 2>&1; m=$(awk "/✓ MUT-1 TUE/{c++} END{print c+0}" "$T/o"); p=$(awk "/✓/{c++} END{print c+0}" "$T/o"); echo "mut1=$m assertions=$p"; [ "$m" -eq 1 ] && [ "$p" -ge 8 ]'</automated>
    <fails_when>`mut1=` différent de 1 (mutant non tué, ou non discriminant) ; ou `assertions=` inférieur à 8 (les six cas de contrat sous-exercés)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend 0, aucune ligne `✗`
    - six cas de contrat couverts (2 compartiments, absent, lien racine, lien mixte, vide, indifférence env), au moins 8 assertions vertes
    - MUT-1 tué avec sa trace `lignes_mutant=` / `lignes_original=`
    - aucun CHANGELOG touché par ce plan (bump reporté au plan 41.1-08)
  </acceptance_criteria>
  <done>`vf_ws_enumerate` a une suite qui sait la mettre en défaut sur son filtre anti-lien, et prouve son indifférence aux quatre canaux d'environnement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disque → `vf_ws_enumerate` | l'univers énuméré est celui du disque au moment de l'appel — aucune garantie de fraîcheur entre deux appels successifs |
| lien symbolique → traversée | un compartiment ou `workstreams/` lui-même versionné en lien pourrait faire lire un répertoire hors du lab si le filtre était contourné |
| environnement → sortie | quatre canaux (`GSD_WORKSTREAM`, `VF_WORKSTREAM`, `VF_STATE_WORKSTREAM`, pointeur) pourraient, s'ils étaient lus par erreur, faire dériver la liste de compartiments d'une valeur qu'un simple `export` change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-01 | Tampering | lien symbolique sur `workstreams/` ou un compartiment individuel | high | mitigate | `vf_ws_path_nolink` appliqué aux deux niveaux, jamais de traversée ; cas 3 et 4 de la suite ; mutant MUT-1 prouve la discriminance |
| T-41.1-02 | Elevation of privilege | un gate qui déduirait sa cible de `GSD_WORKSTREAM`/`VF_WORKSTREAM` au lieu du disque hériterait de la faille que WSAW-06 nomme (une valeur qu'un `export` peut changer déciderait d'un verdict de gate) | high | mitigate | `vf_ws_enumerate` ne lit AUCUN des quatre canaux — cas 6 de la suite le prouve par `cmp` entre deux sorties |
| T-41.1-03 | Information disclosure | verdict vert sur un `workstreams/` vide ou entièrement filtré (compartiments tous liens) | medium | mitigate | rc=2 explicite sur ce cas (jamais un 0/3 de complaisance) — cas 5 de la suite |
</threat_model>

<verification>
`bash -n` sur `workstream-policy.sh` ; cinq cas de contrat (2 compartiments, absent, lien racine,
lien mixte, vide) vérifiés directement en shell ; preuve d'indifférence aux quatre canaux
d'environnement par `cmp` ; suite `test-workstream-policy.sh` verte avec MUT-1 tué.
</verification>

<success_criteria>
`vf_ws_enumerate` existe, dérive uniquement du disque, ne suit jamais de lien symbolique, ne rend
jamais un 0 déguisé sur un `workstreams/` vide-après-filtre, et sa suite prouve chacune de ces
propriétés par mutation ou par comparaison directe.
</success_criteria>

## Artifacts this phase produces

- `plugin/planning-core/scripts/workstream-policy.sh` — fonction `vf_ws_enumerate`, codes 0/2/3.
- `plugin/planning-core/scripts/tests/test-workstream-policy.sh` — six cas + MUT-1.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-01-SUMMARY.md` when done
</output>
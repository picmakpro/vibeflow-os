---
phase: 26-manuel-utilisateur-vibeflow-manual
plan: 09
type: execute
wave: 9
depends_on: ["26-08"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/
# autonomous: false — checkpoint humain bloquant avant le commit de clôture de la phase (D-14, one-way)
autonomous: false
requirements: []
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Le ROADMAP et le STATE consignent l'état RÉEL du chantier sur disque — jamais un état supposé, jamais des plans cochés qui n'ont pas tourné."
    - "Les compteurs de STATE.md sont édités À LA MAIN et ne régressent pas au sein du jalon en cours (ADR-063, check-state-integrity.sh vert)."
    - "Le commit de clôture de la phase ne contient AUCUN chemin sous manual/ — vérifié par une machine ET par un humain avant qu'il soit écrit (D-14)."
    - "Le contenu du manuel lui-même n'est jamais recopié dans un fichier de suivi committé."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - ".planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-*-SUMMARY.md"
  key_links:
    - ".planning/ROADMAP.md ligne 62 (checklist) ↔ §Phase 26 : c'est la checklist que le moteur lit pour compter l'avancement — désynchroniser les deux reproduit la régression corrigée le 2026-08-01."
    - "STATE.md frontmatter ↔ plugin/conductor/scripts/check-state-integrity.sh : le gate refuse une régression de compteur dans le même jalon."
---

<objective>
Clore la Phase 26 : consigner dans le suivi ce qui a **réellement** été livré, et produire le commit
de clôture de la phase.

Purpose: c'est le seul plan de la phase qui touche des fichiers versionnés, et donc le seul point où
la contrainte D-14 peut être violée — un `git add -A` ou un `git commit -a` distrait ferait entrer
`manual/` dans l'historique. La protection est double : une assertion machine et un arrêt humain,
avant que le commit existe.

**Ce plan est exécutable même si les vagues 4 à 8 ont été différées.** Il consigne l'état réel sur
disque, pas un état attendu : si seuls les plans 26-01 à 26-03 ont tourné, il coche ceux-là, laisse
les autres décochés, et le dit dans `STATE.md`.

Output: `.planning/ROADMAP.md` et `.planning/STATE.md` à jour, un commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md
@.planning/missions/2026-08-01-phase-26-manuel-utilisateur.md
@plugin/conductor/scripts/check-state-integrity.sh
</context>

---

## Artifacts this plan produces

**Committés** (avec, en amont, le commit de planification qui a posé les neuf `26-0N-PLAN.md` et
recalé le ROADMAP — les seuls commits de toute la Phase 26) :
- `.planning/ROADMAP.md` — §Phase 26 : compte de plans, checklist des plans cochée sur le réel,
  ligne 62 de la checklist des phases mise à jour si et seulement si la phase est complète
- `.planning/STATE.md` — position courante, compteurs `progress`, note d'état
- `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-*-PLAN.md` et `26-*-SUMMARY.md`

**Jamais committé** : tout `manual/**`. Le manuel reste local (amendement de mission §3bis, D-14).

---

<tasks>

<task type="auto">
  <name>Task 1: Recaler .planning/ROADMAP.md sur le réel livré</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 26 (~ligne 1575) — la section à recaler, et **la ligne 62** de la
      checklist des phases : les deux doivent rester cohérentes, c'est la checklist que le moteur lit.
    - `.planning/ROADMAP.md` §Phase 21 (~lignes 1092-1099) — le format exact d'une section de phase
      close : `**Plans:** N/N plans executed` puis la liste `- [x] NN-0N-PLAN.md — description
      (vague N)`.
    - Le contenu réel de `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/` — quels
      `26-*-SUMMARY.md` existent effectivement. **C'est cette liste qui fait foi, pas ce plan.**
  </read_first>
  <action>
    Recaler la section `### Phase 26` du ROADMAP **par édition scopée** (jamais par réécriture du
    fichier entier : un `Write` sur ce fichier détruirait les 25 autres phases).

    1. Remplacer la ligne `**Plans:** 0 plans` par le compte réel au format du dépôt :
       `**Plans:** N/9 plans executed`, où N est le nombre de plans ayant produit un `SUMMARY.md` sur
       disque — compté, jamais supposé.
    2. Remplacer le bloc `Plans:` et sa ligne `- [ ] TBD (run /gsd-plan-phase 26 to break down)` par
       la liste des 9 plans, chacun avec une description d'une ligne et son numéro de vague. Cocher
       `[x]` **uniquement** les plans dont le `SUMMARY.md` existe ; laisser `[ ]` les autres.
    3. Renseigner le champ `**Requirements**` : conserver l'absence d'ID formel et l'expliquer, sur le
       modèle de la Phase 21 (« aucun ID `REQ-` n'existe pour cette phase dans `REQUIREMENTS.md` ; le
       ledger s'arrête à ALTI-05 / Phase 14 »), plutôt que de laisser `TBD` nu. **Ne jamais inventer
       d'ID.**
    4. Ajouter deux à quatre lignes de note d'état sous le Goal, disant ce qui a été livré et ce qui
       ne l'est pas : le manuel vit **hors du dépôt** par décision de mission (amendement §3bis), et
       le volet « les README maigrissent et pointent vers le manuel » est **gelé, pas abandonné** —
       il reprend le jour où Samuel décide de publier. Ne pas recopier de contenu du manuel : décrire
       l'avancement, jamais le texte.
    5. Cocher la ligne 62 de la checklist des phases (`- [ ] Phase 26: …` → `- [x] …`) **si et
       seulement si** les 9 plans sont livrés. Sinon, la laisser décochée : une checklist qui ment est
       exactement la régression corrigée le 2026-08-01.

    Ne toucher à aucune autre phase du fichier.
  </action>
  <verify>
    <automated>set -e; grep -q '^\*\*Plans:\*\* [0-9]\+/9 plans executed' .planning/ROADMAP.md; test "$(grep -c 'TBD (run /gsd-plan-phase 26' .planning/ROADMAP.md || true)" = "0"; test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" -ge 26; s=$(ls .planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-*-SUMMARY.md 2>/dev/null | wc -l | tr -d ' '); c=$(grep -c '^- \[x\] 26-0[0-9]-PLAN\.md' .planning/ROADMAP.md || true); test "$s" = "$c"; echo "OK summaries=$s coches=$c"</automated>
  </verify>
  <acceptance_criteria>
    - `**Plans:** N/9 plans executed` reflète le nombre réel de `26-*-SUMMARY.md` sur disque.
    - Le nombre de lignes `- [x] 26-0N-PLAN.md` égale **exactement** le nombre de `SUMMARY.md`
      présents — aucun plan coché sans preuve d'exécution.
    - La ligne `TBD (run /gsd-plan-phase 26 …)` a disparu.
    - Le champ `**Requirements**` explique l'absence d'ID formel plutôt que de la laisser en `TBD` nu,
      et **aucun ID n'a été inventé**.
    - Les 25 autres sections `### Phase` du fichier sont intactes (le décompte de sections ne
      diminue pas).
    - La ligne 62 de la checklist des phases n'est cochée que si les 9 plans sont livrés.
    - Aucun extrait de contenu du manuel n'a été recopié dans le ROADMAP.
  </acceptance_criteria>
  <done>Le ROADMAP dit ce qui a été fait, ni plus ni moins, et sa checklist est cohérente avec sa section détaillée.</done>
</task>

<task type="auto">
  <name>Task 2: Recaler .planning/STATE.md à la main (ADR-063)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` frontmatter — les compteurs et les commentaires qui **interdisent** de les
      régénérer avec l'outil de state (ADR-063 : toute invocation force `resync: true` et
      reproduirait la régression corrigée).
    - `.planning/missions/2026-08-01-phase-26-manuel-utilisateur.md` §3 — la baseline à ne pas faire
      régresser : `total_phases: 26`, `completed_phases: 21`, `total_plans: 62`, `completed_plans: 62`.
    - `plugin/conductor/scripts/check-state-integrity.sh` — les deux invariants gardés : compteurs non
      régressés au sein du même jalon, et une seule ligne `^Phase:` dans le corps.
  </read_first>
  <action>
    Mettre à jour `.planning/STATE.md` **par édition manuelle et scopée**. Ne jamais invoquer l'outil
    de state pour « réparer » ce fichier : il force une resynchronisation non désactivable qui
    reproduirait la régression instruite par ADR-063.

    Frontmatter :
    - `total_plans` : baseline 62 + **9** (les neuf `26-0N-PLAN.md` posés par cette phase).
    - `completed_plans` : baseline 62 + le nombre de plans réellement exécutés (ceux qui ont un
      `SUMMARY.md`). Compté, jamais supposé.
    - `completed_phases` : 21 + 1 **si et seulement si** les 9 plans sont livrés ; sinon inchangé.
    - `total_phases` : reste 26.
    - `status` et `stopped_at` : décrire l'état réel et le prochain geste. Si des vagues ont été
      différées, le dire explicitement et nommer le plan suivant à exécuter.
    - Ajouter un commentaire YAML daté expliquant l'origine du delta, sur le modèle de ceux déjà
      présents — c'est la convention de ce fichier.

    Corps : mettre à jour la section `## Current Position` pour la Phase 26 en respectant la
    convention d'archivage du fichier (une seule ligne `Phase:` dans tout le corps ; les sections
    non courantes utilisent la forme `**Phase archivée :**`). Y consigner, en trois à six lignes :
    ce qui a été livré ; le fait que **le livrable réel vit hors du dépôt** et n'est donc pas
    inspectable depuis git ; que l'outillage `manual/.tools/` est local et n'entre pas en CI (D-13) ;
    et que D-7/D-8 de la mission restent gelées.
    **Ne recopier aucun contenu du manuel** — décrire l'avancement, jamais le texte (amendement de
    mission §3bis point 4 : les docs de planning restent committables, sans y coller le manuel).

    Ajouter également à `## Accumulated Context` §Roadmap Evolution une entrée datée du jour
    consignant l'aboutissement de la Phase 26 et la décision structurante qui la caractérise : le
    livrable est volontairement hors dépôt.
  </action>
  <verify>
    <automated>set -e; bash plugin/conductor/scripts/check-state-integrity.sh; test "$(grep -c '^Phase:' .planning/STATE.md)" = "1"; tp=$(awk '/^total_plans:/{print $2; exit}' .planning/STATE.md); test "$tp" -ge 71; cp=$(awk '/^completed_plans:/{print $2; exit}' .planning/STATE.md); test "$cp" -ge 62; test "$cp" -le "$tp"; ph=$(awk '/^total_phases:/{print $2; exit}' .planning/STATE.md); test "$ph" -ge 26; test "$(grep -c 'manual/fr/' .planning/STATE.md || true)" = "0"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash plugin/conductor/scripts/check-state-integrity.sh` sort en **0**.
    - `total_plans` vaut 71 (baseline 62 + les 9 plans de la phase) ; `completed_plans` vaut 62 plus le
      nombre exact de `SUMMARY.md` présents, et n'excède jamais `total_plans`.
    - `completed_phases` n'a été incrémenté que si les 9 plans sont livrés ; aucun compteur n'a
      régressé.
    - Le corps ne contient qu'**une seule** ligne commençant par `Phase:` (convention d'archivage).
    - Aucune commande de resynchronisation du state n'a été invoquée : les compteurs ont été édités à
      la main (ADR-063).
    - `STATE.md` ne contient **aucun chemin de page du manuel** ni aucun extrait de son contenu : il
      décrit l'avancement, pas le livrable.
    - Une entrée datée a été ajoutée à §Roadmap Evolution.
  </acceptance_criteria>
  <done>Le suivi est vrai, le gate d'intégrité est vert, et aucun compteur n'a régressé.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Porte one-way — autoriser (ou non) le commit de clôture de la phase</name>
  <decision>
    Autoriser l'écriture du commit de clôture de la Phase 26, sachant qu'un `manual/` entré dans
    l'historique ne peut plus en sortir sans réécriture d'historique.
  </decision>
  <context>
    Le suivi est à jour (`.planning/ROADMAP.md` recalé sur le réel livré, `.planning/STATE.md` recalé
    à la main, `check-state-integrity.sh` vert). Le manuel existe sous `manual/`, hors du dépôt, et
    ne doit entrer dans aucun commit (D-14).

    **Porte one-way.** Un `git add -f`, un `git add -A` ou un `git commit -a` accidentel romprait la
    confidentialité locale demandée par l'amendement de mission §3bis et laisserait une trace
    **irréversible** : la corriger imposerait une réécriture d'historique sur une branche déjà
    partagée. D'où cet arrêt **avant** écriture du commit, et non après.

    Lancer ces trois contrôles et lire leur sortie avant de trancher :

    1. `git status --porcelain -- manual` → doit sortir **vide**. Un seul chemin affiché est un arrêt
       immédiat : ne pas commiter, restaurer l'exclusion.
    2. `git status --porcelain` → doit ne lister **que** des chemins sous `.planning/`. Aucun
       `README.md`, `README.fr.md`, `INSTALL.md`, `scripts/`, `.github/`, `plugin/`, `docs/`.
    3. `grep -n 'manual' .gitignore` → doit ne **rien** remonter (ce fichier est versionné : une
       entrée y laisserait une trace publique, ce que l'amendement proscrit), et
       `grep -n 'manual/' .git/info/exclude` → doit remonter la ligne, intacte.

    Relire aussi la section `### Phase 26` du ROADMAP et le bloc `## Current Position` de
    `STATE.md` : aucun extrait de contenu du manuel ne doit y avoir été recopié.
  </context>
  <options>
    <option id="commit">
      <name>Commiter le suivi</name>
      <pros>La Phase 26 est close et traçable ; le ROADMAP et le STATE cessent de mentir sur l'avancement.</pros>
      <cons>Porte one-way : après le commit, toute fuite de `manual/` demanderait une réécriture d'historique.</cons>
    </option>
    <option id="corriger">
      <name>Corriger d'abord</name>
      <pros>Un contrôle en défaut est traité avant qu'il devienne irréversible.</pros>
      <cons>La clôture est reportée ; le suivi reste temporairement en avance sur le dépôt.</cons>
    </option>
  </options>
  <resume-signal>Répondre « commit » pour autoriser, ou « corriger » en décrivant ce qui doit l'être d'abord.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Produire le commit de clôture de la phase, par chemins explicites</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/</files>
  <read_first>
    - `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md` §D-14 — la
      contrainte et son caractère irréversible.
    - `.git/info/exclude` — la ligne `manual/`, à ne jamais retirer ni modifier.
    - `CLAUDE.md` du dépôt — convention de messages de commit en français.
  </read_first>
  <precondition>
    Le checkpoint de la task 3 a été résolu par un humain en faveur de l'option `commit`. Si aucune
    réponse humaine n'a été enregistrée, ne pas exécuter cette tâche : s'arrêter.
  </precondition>
  <reversibility rating="one-way">
    Écrire ce commit franchit la porte one-way instruite en task 3 : un chemin `manual/` entré dans
    l'historique n'en sort plus sans réécriture d'historique sur une branche partagée.
  </reversibility>
  <action>
    **Après approbation humaine seulement (task 3)** : produire le commit de clôture de la Phase 26.

    Ajouter à l'index **uniquement par chemins explicites** — `.planning/ROADMAP.md`,
    `.planning/STATE.md` et `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/`.
    **Ne jamais employer `git add -A`, `git add .` ni `git commit -a`** : ces formes balaient l'arbre
    et sont précisément le geste que D-14 interdit. Ne jamais employer `git add -f`.

    Message de commit en français, cohérent avec l'historique du dépôt. Nommer dans le corps du
    message le fait structurant : le livrable de la phase vit **hors du dépôt** par décision de
    mission, et le suivi seul est versionné.

    Hors périmètre, explicitement : **aucun bump de `VERSION`, aucun tag, aucune release** — le
    chantier est purement documentaire (mission §1).
  </action>
  <verify>
    <automated>set -e; test -z "$(git status --porcelain -- manual)"; test "$(git show --name-only --pretty=format: HEAD | grep -c '^manual/' || true)" = "0"; test "$(git show --name-only --pretty=format: HEAD | grep -vc '^\.planning/' || true)" = "0"; grep -q '^manual/$' .git/info/exclude; test "$(grep -c manual .gitignore || true)" = "0"; test "$(git diff HEAD~1 HEAD -- VERSION | wc -l | tr -d ' ')" = "0"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Un humain a lu les trois contrôles et donné son accord **avant** que le commit soit écrit.
    - Le commit ne contient **que** des chemins sous `.planning/` — vérifié par
      `git show --name-only` sur le commit lui-même, pas seulement sur l'index.
    - **Zéro** chemin `manual/` dans le commit.
    - `.git/info/exclude` contient toujours la ligne `manual/`, inchangée.
    - `.gitignore` ne mentionne toujours pas le manuel.
    - `VERSION` est inchangé ; aucun tag n'a été créé ; aucune release n'a été publiée.
    - Aucune des commandes `git add -A`, `git add .`, `git add -f` ou `git commit -a` n'a été
      exécutée.
    - Le message de commit est en français.
  </acceptance_criteria>
  <done>La Phase 26 est close côté suivi. Le manuel existe sur le disque de Samuel et nulle part ailleurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arbre de travail → index git | `manual/` est présent sur le disque pendant toute l'opération de commit |
| `.planning/` → dépôt public | le suivi est versionné et donc lisible par tous |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-09-01 | Information Disclosure | commit de clôture | high | mitigate | Ajout par chemins explicites uniquement ; `git add -A`/`git add .`/`git add -f`/`git commit -a` interdits ; assertion machine sur `git show --name-only` **après** commit ; checkpoint humain bloquant **avant** commit. |
| T-26-09-02 | Information Disclosure | contenu du manuel recopié dans `.planning/` | medium | mitigate | ROADMAP et STATE décrivent l'avancement, jamais le texte du manuel ; critère d'acceptation explicite sur les deux tâches d'écriture. |
| T-26-09-03 | Tampering | `.git/info/exclude` | high | mitigate | La ligne `manual/` est vérifiée intacte après commit ; aucune tâche de la phase n'est autorisée à la modifier. |
| T-26-09-04 | Repudiation | compteurs de `STATE.md` | medium | mitigate | Édition manuelle tracée (ADR-063) ; `check-state-integrity.sh` refuse toute régression au sein du jalon. |
| T-26-09-SC | Tampering | installs npm/pip/cargo | — | accept | Aucun install de paquet dans cette phase. Gate de légitimité sans objet. |
</threat_model>

<verification>
1. `git show --name-only --pretty=format: HEAD` ne liste **que** des chemins `.planning/`.
2. `git log --all --name-only --pretty=format: | grep '^manual/'` ne remonte **rien** sur toute la
   branche de la phase.
3. `git status --porcelain -- manual` sort **vide**.
4. `bash plugin/conductor/scripts/check-state-integrity.sh` sort en **0**.
5. `.git/info/exclude` contient toujours `manual/` ; `.gitignore` ne mentionne pas le manuel.
6. `VERSION` inchangé, aucun tag créé (`git tag --points-at HEAD` vide).
7. Le nombre de cases cochées du ROADMAP égale le nombre de `SUMMARY.md` sur disque.
</verification>

<success_criteria>
- Le suivi consigne l'état **réel**, y compris en cas de vagues différées.
- Le gate d'intégrité du state est vert et aucun compteur n'a régressé.
- Le commit de clôture de la phase ne contient aucun chemin du manuel, et un humain l'a validé avant qu'il
  existe.
- Aucun bump, aucun tag, aucune release.
</success_criteria>

<output>
Create `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-09-SUMMARY.md` when done.
Y consigner : les compteurs avant/après, le nombre de plans réellement exécutés, la sortie de
`git show --name-only` sur le commit de clôture, et la confirmation que le contrôle humain a précédé
le commit.
</output>

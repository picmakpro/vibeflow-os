---
phase: 24-activation-et-mesure-du-moteur-gsd
plan: 07
type: execute
wave: 2
depends_on: ["24-02"]
files_modified:
  - docs/ADR.md
autonomous: true
requirements: [GSDA-10, GSDA-11]

must_haves:
  truths:
    - "Les profils de contexte sont refusés par un ADR factuellement exact : la clé nommée est `context_profile`, jamais `context`."
    - "L'état écrit est « documentée, livrée, jamais câblée, abandonnée de fait depuis avril 2026 » — le mot « dépréciée » n'apparaît nulle part."
    - "Le refus porte un déclencheur de réexamen OBJECTIF, sans aucune date calendaire."
    - "`workflow.inline_plan_threshold` reste inchangé à 2, la clé n'est pas posée, et LA MESURE EST LE LIVRABLE : elle est re-jouée à l'exécution avec la regex exacte du moteur, en awk."
  artifacts:
    - docs/ADR.md
  key_links:
    - "Ce plan est le SECOND des trois à écrire `docs/ADR.md` (après 24-02, avant 24-10) — la chaîne de dépendances explicite est ce qui garantit qu'aucune vague n'y écrit deux fois."
    - "Le seuil vise le MÉCANISME interne du moteur ; la doctrine de délégation systématique vise l'ACTEUR. L'ADR doit écrire cette disjonction, sinon la mesure sera relue comme une contradiction."
---

<objective>
Refuser un canal qu'aucun code ne lit, et chiffrer une fois pour toutes un levier qu'on présentait
comme inconnu.

Purpose: le ROADMAP présentait les profils de contexte comme « ce que le moteur porte en config et
que nous ré-implémentons en doctrine ». Le fait s'est inversé pendant le cadrage : les trois profils
existent, la clé est validée et documentée, mais la recherche exhaustive sur tout le moteur, les
agents et les skills ne rend que trois occurrences, toutes auto-déclaratives. **Aucun consommateur
n'existe.** Le moteur ne le porte pas, il le déclare. Et sur le seuil inline, la mesure montre un
levier réel mais étroit : le bon livrable n'est pas un réglage, c'est le chiffre.

Output: une entrée d'ADR (ADR-068) couvrant les deux items de la zone 4, avec la mesure re-jouée à
l'exécution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-ARBITRAGES.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-CONTEXT.md
@.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : re-jouer la mesure du seuil inline, en awk, avec la regex exacte du moteur</name>
  <files>docs/ADR.md</files>
  <read_first>
    - `~/.claude/gsd-core/workflows/execute-plan.md` lignes 94 et 100 — l'endroit exact où le seuil est lu, et la justification amont qu'il faut citer verbatim (économie de l'ouverture d'un sous-agent, préservation du cache de prompt). Relever la **regex exacte** employée par le moteur pour compter les tâches d'un plan : c'est elle et aucune autre qui doit servir à la mesure.
    - `~/.claude/gsd-core/references/planning-config.md` lignes 41 et 276 — le défaut (2) et la plage admise (0 à 10).
    - `24-CONTEXT.md` fiches F-22 et F-23 — la mesure du cadrage (4 plans sur 28 sous le seuil, mode à 3 tâches, 4 rétro-plans à 0 tâche qui sont un artefact de format) et la disjonction acteur / mécanisme.
    - `plugin/dev-orchestrator/references/GSD-PIPELINE.md` lignes 188-199 — où le seuil se situe dans la voie unique : il est lu DANS la brique, après qu'elle a été atteinte.
  </read_first>
  <action>
Re-jouer la mesure sur l'arbre au moment de l'exécution, avant d'écrire une seule ligne d'ADR.

Population : tous les fichiers `*-PLAN.md` des dossiers de phase 20 à 26 de `.planning/phases/`.
Compte de tâches par plan : la **regex exacte du moteur** relevée dans `execute-plan.md`, appliquée
en `awk` — jamais en `grep` piped, qui tronque silencieusement sur ce runtime (31 lignes rendues sur
102 mesurées). Produire la distribution complète : nombre de plans par compte de tâches, du plus
petit au plus grand.

Écrire ensuite **deux chiffres, jamais un seul** :
- la mesure du cadrage, datée du 2026-08-04, sur la population telle qu'elle était alors :
  0 tâche → 4 plans (les rétro-plans de la Phase 21, sans balise de tâche — artefact de format, pas
  petitesse), 2 tâches → 4 plans, 3 tâches → 20 plans, 4 → 2, 6 → 2 ; soit **4 plans exécutables
  sur 28, 14 %**, sous le seuil, et un **mode à 3 tâches**, juste au-dessus ;
- la re-mesure du jour, avec sa population et sa date, en nommant explicitement le delta : la
  population a grossi des plans de la Phase 24 elle-même. Ne jamais substituer silencieusement le
  nouveau chiffre à l'ancien : les deux sont des faits datés, et c'est leur écart qui est
  instructif.

Consigner la commande `awk` employée dans l'ADR, de sorte que la mesure soit reproductible par un
tiers sans reconstruire le raisonnement.
  </action>
  <verify>
    <automated>awk 'FNR==1{n=0} /^[[:space:]]*&lt;task[[:space:]&gt;]/{n++} ENDFILE{print FILENAME" "n}' .planning/phases/*/2[0-6]-*-PLAN.md 2>/dev/null | awk 'END{exit !(NR>0)}'</automated>
  </verify>
  <acceptance_criteria>
    - La commande de mesure employée est visible dans le SUMMARY et emploie `awk`, jamais `grep` piped.
    - La distribution complète (compte de tâches → nombre de plans) est produite pour la population du jour, avec son total.
    - Les deux mesures figurent dans l'ADR, chacune avec **sa date** et **sa population** : celle du 2026-08-04 (4/28, 14 %, mode 3) et celle du jour d'exécution.
    - Le delta entre les deux est nommé et attribué : il vient des plans de la Phase 24.
    - La regex citée dans l'ADR est celle relevée dans `execute-plan.md`, pas une approximation.
  </acceptance_criteria>
  <done>Le levier est chiffré deux fois, à deux dates, avec sa méthode reproductible — la mesure est un livrable, pas une note de bas de page.</done>
  <reversibility rating="reversible">Mesure en lecture seule ; aucun fichier de code touché.</reversibility>
</task>

<task type="auto">
  <name>Tâche 2 : ADR-068 — refus des profils de contexte et seuil inline inchangé</name>
  <files>docs/ADR.md</files>
  <read_first>
    - `docs/ADR.md` — l'entrée ADR-067 écrite par le plan 24-02 (forme à répliquer, numérotation à suivre) et la table d'index en tête du fichier.
    - `24-ARBITRAGES.md` § Zone 4, verdict — les **trois contraintes de rédaction imposées** : nommer la bonne clé, ne jamais écrire « dépréciée », assortir le refus d'un déclencheur objectif et non d'une date.
    - `24-CONTEXT.md` fiches F-20 et F-21 — l'absence de consommateur, et les deux incompatibilités de forme (scalaire global contre contrat par rôle ; verbosité contradictoire ; vocabulaires de sévérité divergents).
    - `plugin/dev-orchestrator/references/mission-flow.md` lignes 136-152 — le Pattern C : quatre rôles, schéma JSON par retour, et la phrase sur la prose libre à citer face à la ligne contraire du profil de recherche amont.
    - `~/.claude/gsd-core/contexts/dev.md`, `review.md`, `research.md` — les trois fichiers livrés : vérifier de première main qu'ils nomment bien `context` en en-tête, et relever la ligne exacte de contradiction de verbosité.
  </read_first>
  <action>
Écrire l'entrée **ADR-068** en queue de `docs/ADR.md` et sa ligne dans la table d'index.

Titre : les profils de contexte du moteur sont refusés — il n'y a rien à activer, et notre contrat
typé est per-rôle et plus strict. `**Date**` : date d'exécution. `**Statut**` : Validée.
`**Décideur**` : Samuel (arbitrage `24-ARBITRAGES.md`, zone 4, option A).

**Premier volet — les profils de contexte.**

Nommer la bonne clé, et l'écart qui a fait tomber le cadrage dans le piège : la clé porteuse de la
sémantique est **`context_profile`** ; la clé `context`, que les trois fichiers livrés nomment en
en-tête, ne porte pas cette sémantique au schéma. Écrire l'écart, il explique pourquoi le sujet a
paru actionnable.

État réel, dans ces termes exacts : **documentée, livrée, jamais câblée, abandonnée de fait depuis
avril 2026**. Le mot « **dépréciée** » ne doit apparaître **nulle part** dans l'entrée : l'amont ne
l'a jamais dit, l'ADR serait factuellement faux. Vérifier son absence après rédaction.

Motif du refus, en deux temps :
- **il n'y a rien à activer** — six occurrences amont, toutes situées dans la documentation ;
  la recherche exhaustive sur le moteur, les agents et les skills ne rend que trois occurrences,
  toutes auto-déclaratives (la ligne d'en-tête de chacun des trois fichiers). Aucun consommateur
  n'existe. Le seul appel de configuration du moteur qui commence par le même préfixe porte sur la
  fenêtre de contexte — une autre clé ;
- **notre contrat est plus strict** — le profil est un **scalaire global**, une valeur pour tout le
  projet, là où le Pattern C est un **contrat par rôle** (quatre rôles, schéma JSON par retour,
  `mission-flow.md:136-152`) ; à comparer avec `agent_skills`, qui est bien une map par agent. Et si
  le canal s'implémentait un jour, il entrerait en collision frontale : le profil de recherche amont
  demande une verbosité haute avec du contexte d'arrière-plan même connu du développeur, quand notre
  Pattern C tient que la prose libre est du volume mort. Les vocabulaires de sévérité divergent
  également des deux côtés.

Décision : ni `context` ni `context_profile` n'est posée dans `.planning/config.json`.

**Déclencheur de réexamen, objectif et sans date** : rouvrir **ssi** `context_profile` apparaît
**hors de la documentation** dans une release amont, **ou** qu'une issue amont le mentionne. Ne pas
écrire d'échéance calendaire — ce serait un rendez-vous, pas un déclencheur.

**Second volet — le seuil inline.**

Décision : `workflow.inline_plan_threshold` reste **inchangé à 2**, valeur qui est le défaut amont ;
la clé n'est pas posée. Porter les **deux mesures** produites en tâche 1, chacune avec sa date, sa
population et la commande `awk` qui les reproduit.

Écrire la disjonction qui empêche de relire la mesure comme une contradiction : la doctrine de
délégation systématique du module vise l'**acteur** — qui fait le travail —, tandis que le seuil est
lu **dans** la brique, après qu'elle a été atteinte. Les deux ne parlent pas du même objet et ne
peuvent pas se contredire.

Écrire enfin ce que la mesure rend impossible : le seuil inline n'est plus présentable comme un
« levier de coût inconnu ». Tout retour sur ce réglage devra citer la mesure et l'infirmer, pas
rouvrir la question à neuf.
  </action>
  <verify>
    <automated>awk '/^## ADR-068 /{h=1} /context_profile/{a=1} /abandonnée de fait/{b=1} /inline_plan_threshold/{c=1} /mission-flow.md:136-152/{d=1} /dépréciée/{bad=1} END{exit !(h&&a&&b&&c&&d&&!bad)}' docs/ADR.md &amp;&amp; awk '/^\| ADR-068 \|/{n++} END{exit !(n==1)}' docs/ADR.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ADR.md` contient un titre `## ADR-068 : ` avec son bloc `**Date**` / `**Statut**` / `**Décideur**` complet.
    - Le corps contient les littéraux `context_profile`, `abandonnée de fait`, `avril 2026`, `mission-flow.md:136-152`, `inline_plan_threshold`, `execute-plan.md`, `14 %`, `mode`.
    - **Le mot `dépréciée` n'apparaît nulle part dans l'entrée ADR-068** — vérifié par une extraction `awk` bornée à l'intervalle de l'entrée, qui doit rendre zéro occurrence.
    - Le déclencheur de réexamen est présent et **ne contient aucune date calendaire** : aucune occurrence d'un motif de type année-mois-jour dans la phrase du déclencheur.
    - Les deux mesures figurent avec leurs dates distinctes et leurs populations distinctes.
    - La disjonction acteur / mécanisme est écrite explicitement.
    - La table d'index porte **exactement une** ligne commençant par `| ADR-068 |`.
    - Les entrées ADR-066 et ADR-067 sont bit-à-bit intactes (`cmp -s` sur leur intervalle extrait avant et après).
  </acceptance_criteria>
  <done>ADR-068 est écrite et indexée, factuellement exacte sur la clé et sur l'état, sans le mot interdit, avec un déclencheur objectif et la mesure du seuil portée en livrable.</done>
  <reversibility rating="one-way">L'écrit d'un refus engage la doctrine jusqu'à ce que le déclencheur tombe. La décision a été arbitrée par Samuel le 2026-08-04 — ce plan la consigne, il ne la rouvre pas.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/ADR.md` → doctrine du lab | Une entrée d'ADR fausse contamine toutes les décisions ultérieures qui la citent — et cette entrée-ci porte deux pièges de rédaction déjà identifiés (mauvaise clé, mot interdit). |
| `.planning/phases/**/*-PLAN.md` → mesure | La population mesurée est du contenu de dépôt ; sa taille et sa forme conditionnent le chiffre publié. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-07-01 | Repudiation | ADR-068, volet profils | high | mitigate | Trois contrôles machine : présence de `context_profile`, absence totale de `dépréciée` dans l'intervalle de l'entrée, absence de date calendaire dans le déclencheur. |
| T-24-07-02 | Tampering | mesure du seuil | medium | mitigate | Mesure en `awk` avec la regex relevée dans `execute-plan.md`, commande consignée dans l'ADR pour être reproductible ; `grep` piped explicitement proscrit (tronque sur ce runtime). |
| T-24-07-03 | Tampering | entrées ADR-066 / ADR-067 | medium | mitigate | Comparaison `cmp -s` sur leurs intervalles extraits avant et après édition — l'ajout en queue ne doit toucher aucune entrée antérieure. |
| T-24-07-SC | Tampering | installs npm/pip/cargo | high | accept | Ce plan n'exécute aucune installation de paquet. |
</threat_model>

<verification>
- `docs/ADR.md` porte ADR-068, indexée exactement une fois, et les deux entrées antérieures sont intactes.
- Le mot `dépréciée` est absent de l'entrée ; le déclencheur ne porte aucune date.
- Les deux mesures du seuil figurent avec leurs dates, populations et la commande `awk` de reproduction.
- `.planning/config.json` n'a pas été touché par ce plan : ni `context` ni `context_profile` ni `inline_plan_threshold` n'y apparaissent.
</verification>

<success_criteria>
Le refus des profils de contexte est écrit sans aucune des trois erreurs que l'arbitrage avait
d'avance identifiées, et le seuil inline cesse d'être un levier inconnu : il est chiffré, daté, et
sa méthode est reproductible.
</success_criteria>

## Artifacts this phase produces

| Symbole | Nature | Emplacement |
|---|---|---|
| `ADR-068` | entrée d'ADR + ligne d'index | `docs/ADR.md` |
| déclencheur `context_profile hors documentation dans une release amont` | condition de réexamen objective | ADR-068 |
| distribution `tâches → plans` (2 dates) | mesure datée reproductible | ADR-068 |
| commande `awk` de comptage de tâches | méthode de mesure consignée | ADR-068 |

<output>
Créer `.planning/phases/VFDO-24-activation-et-mesure-du-moteur-gsd-capacit-s-dormantes-et-fa/24-07-SUMMARY.md` en fin de plan.
</output>

---
phase: 42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/growth-bundle/agents/vf-growth-manager.md
  - plugin/growth-bundle/VERSION
  - plugin/growth-bundle/module.json
  - plugin/growth-bundle/CHANGELOG.md
  - plugin/growth-bundle/README.md
  - plugin/design-orchestrator/agents/vf-design-manager.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/README.md
autonomous: true
requirements: [FABR-05]

must_haves:
  truths:
    - "RESTRUCTURATION (revise-42b, B1) : growth-quality-judge et vf-design-judge (I5, omitClaudeMd) NE SONT PLUS touchés par ce plan — D-19 a rendu son verdict (règles omises) le 2026-09-24, D-08 est réexaminée avec Samuel, réponse non reçue. Leur pose vit derrière le checkpoint bloquant de 42-05 (Tâche 2)."
    - "vf-growth-manager et vf-design-manager portent SendMessage dans tools:, inséré juste avant le jeton Agent( ; la ligne de vf-design-manager finit toujours par la parenthèse fermante et ne cite jamais vf-dev-manager (T8 de design-orchestrator) — invariant I6, D-07 — INCONDITIONNEL, ne dépend d'aucune réponse de Samuel"
    - "Seul le frontmatter change : corps identiques octet pour octet à main, colonne des instructions du ratchet inchangée (Pitfall 5)"
    - "Un commit par module (growth-bundle, design-orchestrator), chacun avec son bump patch (VERSION, module.json, ligne Version du README, entrée CHANGELOG), séparé de tout commit du gate (D-12) ; chaque module reçoit un SECOND commit + bump patch séparé si le checkpoint de 42-05 arme le champ sur son juge (item d, pas de regroupement artificiel)"
    - "design-orchestrator est de la polarité de Samuel : son commit est destiné à sa relecture dans la PR (D-12)"
    - "check-agents.sh --strict reste vert sur les deux dossiers d'agents ; test-growth-bundle.sh et test-design-orchestrator.sh restent verts"
  artifacts:
    - path: "plugin/growth-bundle/agents/vf-growth-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
    - path: "plugin/design-orchestrator/agents/vf-design-manager.md"
      provides: "manager avec vue sur ses pairs"
      contains: "SendMessage"
  key_links:
    - from: "plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh"
      to: "plugin/design-orchestrator/agents/vf-design-manager.md"
      via: "T8 : allowlist complète, vf-dev-manager absent, parenthèse fermée en fin de ligne"
      pattern: "T8"
  prohibitions:
    - statement: "MUST NOT présenter une décision de cadrage de Claude comme un arbitrage humain (commit, CHANGELOG, PR), ni modifier un module de la polarité de Samuel sans l'y nommer relecteur"
      status: unresolved
      reason: "sonde de prohibitions spec-less, sans contrôle câblé : la forme d'une attribution se relit, elle ne se prouve pas par machine (CLAUDE.md, traçabilité des arbitrages)"
---

<objective>
Mettre en conformité, avant l'armement d'I6 (42-05), les managers de growth-bundle et de
design-orchestrator, mesurés en violation (D-11). Frontmatter pur, un commit par module avec son
bump patch (D-12). design-orchestrator est de la polarité de Samuel : son commit est destiné à sa
relecture en PR.

**RESTRUCTURATION (mission, nœud `revise-42b`, B1/B2, 2026-09-24) : I5 (omitClaudeMd sur
`growth-quality-judge` et `vf-design-judge`) SORT de ce plan.** D-19 a rendu son verdict
(`42-D19-MESURE.md`) — `omitClaudeMd` retire aussi les règles `.claude/rules/*.md` (fait pertinent
ici : `plugin/software-architecture/rules/` livre de vraies règles) — et D-08 est réexaminée avec
Samuel, réponse non reçue. I6/SendMessage ne dépend d'aucune réponse et reste inconditionnel en
vague 1. La pose d'`omitClaudeMd` sur les deux juges de ce plan vit désormais derrière un checkpoint
bloquant dans 42-05 (Tâche 2), avec celle des deux juges de 42-02.

Purpose: FABR-05 — corpus conforme sous `--strict` quand les invariants deviennent des erreurs.
Output: deux agents corrigés (I6), deux modules bumpés en patch, deux commits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md
@.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-PATTERNS.md

Patron de référence (lecture seule) : `plugin/dev-orchestrator/agents/vf-dev-manager.md`
(`…, AskUserQuestion, SendMessage, Agent(...)`). Forme d'entrée CHANGELOG : « ## [vX.Y.Z] —
AAAA-MM-JJ (contexte) » puis « **Patch** : ».
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : growth-bundle — manager avec SendMessage (I6), bump patch (I5 SORTI, cf. checkpoint 42-05)</name>
  <precondition>PR #85 fusionnée et contenue dans HEAD : `git merge-base --is-ancestor "$(gh pr view 85 --json mergeCommit -q .mergeCommit.oid)" HEAD` rend 0 ET `plugin/conductor/scripts/check-blueprints.sh` existe (D-14) — sinon arrêt</precondition>
  <files>plugin/growth-bundle/agents/vf-growth-manager.md, plugin/growth-bundle/VERSION, plugin/growth-bundle/module.json, plugin/growth-bundle/CHANGELOG.md, plugin/growth-bundle/README.md</files>
  <read_first>
    - plugin/growth-bundle/agents/vf-growth-manager.md (fichier modifié — ligne tools: l.4)
    - plugin/growth-bundle/scripts/tests/test-growth-bundle.sh (assertions sur le manager ; growth-quality-judge.md N'EST PAS visé par cette tâche)
    - plugin/growth-bundle/README.md (l.7), plugin/growth-bundle/CHANGELOG.md (forme de la dernière entrée)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-11, D-12, D-19)
  </read_first>
  <action>
RESTRUCTURATION (mission, nœud revise-42b, B1/B2) : cette tâche NE POSE PLUS omitClaudeMd sur
growth-quality-judge.md. D-19 a rendu son verdict (42-D19-MESURE.md, 2026-09-24) : omitClaudeMd
retire aussi les règles .claude/rules/*.md, pas seulement le CLAUDE.md — conséquence prévue par
D-19, D-08 est réexaminée avec Samuel, réponse non reçue à ce jour. SendMessage sur le manager ne
dépend d'aucune réponse : il reste en vague 1. La pose du champ sur les 4 juges et l'armement
d'invariant_i5 vivent désormais derrière un checkpoint bloquant dans 42-05 (Tâche 2), qui gère aussi
growth-quality-judge.md de CE module.
1. vf-growth-manager.md : insérer `SendMessage, ` juste AVANT `Agent(` dans la ligne `tools:` —
manager au sens de D-07 ; la ligne finit toujours par la parenthèse fermante.
2. Bump patch de la valeur lue sur disque (attendu v2.0.9 → v2.0.10) : VERSION, module.json, ligne
**Version** du README. CHANGELOG en tête « ## [v2.0.10] — <date d'exécution ISO> (Phase 42 —
invariants de doctrine du gate des agents) », « **Patch** : » — puce unique : SendMessage sur le
manager (I6, D-07), frontmatter seul, PAS de mention I5/omitClaudeMd (module candidat à un second
bump patch séparé si le checkpoint de 42-05 arme le champ sur growth-quality-judge.md, item d).
Décisions de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiées par Samuel (WhatsApp, 2026-09-23), 42-CONTEXT.md D-07, D-11.
3. Commit unique pour ce module :
`fix(growth-bundle v2.0.10): manager avec SendMessage (invariant I6)`.
Aucun fichier hors `plugin/growth-bundle/`.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; M=plugin/growth-bundle/agents/vf-growth-manager.md; fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/growth-bundle/agents >/dev/null && bash plugin/growth-bundle/scripts/tests/test-growth-bundle.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-GB-OK</automated>
    <fails_when>la ligne CORPUS-GB-OK est absente (SendMessage mal placé, corps modifié, gate ou suite du module rouge, triade de version désynchronisée)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/growth-bundle\/agents\/vf-growth-manager\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas « 0 depassement(s) » et « code=0 »</fails_when>
    <automated>grep -c '^omitClaudeMd' plugin/growth-bundle/agents/growth-quality-judge.md</automated>
    <fails_when>la sortie n'est pas exactement 0 (garde anti-fusion : cette tâche ne doit JAMAIS toucher growth-quality-judge.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tools: .*SendMessage, Agent(channel-strategist' plugin/growth-bundle/agents/vf-growth-manager.md` rend 1
    - `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- plugin/growth-bundle/agents/growth-quality-judge.md | wc -l` rend 0
    - corps inchangé, colonne d'instructions inchangée (CORPUS-GB-OK, INSTR-INCHANGE)
    - VERSION, module.json, ligne Version du README et entrée CHANGELOG de tête portent la même version, patch +1 de main
    - `B=$(git merge-base HEAD origin/main); c=$(git log -1 --format=%H -- plugin/growth-bundle/VERSION); git merge-base --is-ancestor "$c" "$B" && echo ISOLATION-NON-PROBANTE-COMMIT-ANTERIEUR || git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/growth-bundle/'` rend 0 — **CORRECTIF DE REVUE (I1, mission revise-42b) : le commit trouvé doit être POSTÉRIEUR à la base (`git merge-base --is-ancestor` doit échouer), sinon la sonde valide trivialement un commit d'historique antérieur à cette exécution, jamais celui que cette tâche vient de créer.**
  </acceptance_criteria>
  <done>Le manager de growth-bundle satisfait I6 ; module bumpé en patch dans un commit dédié ; gate et suite du module verts. Le juge (I5) reste inchangé — sa pose vit désormais derrière le checkpoint D-19 de 42-05.</done>
</task>

<task type="auto">
  <name>Tâche 2 : design-orchestrator — manager avec SendMessage (I6), bump patch (I5 SORTI, cf. checkpoint 42-05 ; relecture Samuel)</name>
  <files>plugin/design-orchestrator/agents/vf-design-manager.md, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/module.json, plugin/design-orchestrator/CHANGELOG.md, plugin/design-orchestrator/README.md</files>
  <read_first>
    - plugin/design-orchestrator/agents/vf-design-manager.md (fichier modifié — ligne tools: l.4)
    - plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh (T8 l.231-256 : six noms, vf-dev-manager absent, parenthèse fermée en fin de ligne ; T4, sur le juge, N'EST PAS visé par cette tâche)
    - plugin/design-orchestrator/README.md (l.7), plugin/design-orchestrator/CHANGELOG.md (forme de la dernière entrée)
    - .planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-CONTEXT.md (D-07, D-11, D-12, D-19)
  </read_first>
  <action>
RESTRUCTURATION (mission, nœud revise-42b, B1/B2) — même raison que la Tâche 1 : D-19 a rendu son
verdict « règles omises », D-08 est réexaminée avec Samuel, réponse non reçue. SendMessage ne dépend
d'aucune réponse : il reste en vague 1. vf-design-judge.md (I5) vit désormais derrière le checkpoint
de 42-05 (Tâche 2).
1. vf-design-manager.md : insérer `SendMessage, ` juste AVANT `Agent(` dans la ligne `tools:` —
manager au sens de D-07. T8 de la suite du module exige que la ligne finisse par la parenthèse
fermante et ne cite jamais vf-dev-manager : l'insertion avant `Agent(` tient les deux.
2. Bump patch de la valeur lue sur disque (attendu v1.5.8 → v1.5.9) : VERSION, module.json, ligne
**Version** du README. CHANGELOG en tête « ## [v1.5.9] — <date d'exécution ISO> (Phase 42 —
invariants de doctrine du gate des agents) », « **Patch** : » — puce unique : SendMessage sur le
manager (I6, D-07), frontmatter seul, PAS de mention I5/omitClaudeMd (module candidat à un second
bump patch séparé si le checkpoint de 42-05 arme le champ sur vf-design-judge.md, item d).
Décisions de cadrage de Claude (délégation de Willy, AskUserQuestion session principale, 2026-09-23), ratifiées par Samuel (WhatsApp, 2026-09-23), 42-CONTEXT.md D-07, D-11 — relecture de Samuel demandée en PR (D-12).
3. Commit unique pour ce module :
`fix(design-orchestrator v1.5.9): manager avec SendMessage (invariant I6)`.
Aucun fichier hors `plugin/design-orchestrator/`.
  </action>
  <verify>
    <automated>B=$(git merge-base HEAD origin/main); fm(){ awk 'NR>1&&/^---[[:space:]]*$/{exit} NR>1{print}' "$1"; }; body(){ awk 'n>=2{print} /^---[[:space:]]*$/{n++}'; }; M=plugin/design-orchestrator/agents/vf-design-manager.md; fm "$M" | grep -q '^tools: .*SendMessage, Agent(.*)[[:space:]]*$' && cmp -s <(git show "$B:$M" | body) <(body < "$M") && bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir=plugin/design-orchestrator/agents >/dev/null && bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh >/dev/null 2>&1 && bash scripts/check-version-sync.sh >/dev/null && echo CORPUS-DO-OK</automated>
    <fails_when>la ligne CORPUS-DO-OK est absente (en particulier si T8 de test-design-orchestrator.sh rougit sur la ligne tools: du manager)</fails_when>
    <automated>bash plugin/conductor/scripts/check-instruction-budget.sh | awk -F' [|] ' '/design-orchestrator\/agents\/vf-design-manager\.md/{print $1, ($4==$5)?"INSTR-INCHANGE":"INSTR-BOUGE"} /^BILAN/{print}'</automated>
    <fails_when>une ligne porte INSTR-BOUGE, ou la ligne BILAN ne porte pas « 0 depassement(s) » et « code=0 »</fails_when>
    <automated>grep -c '^omitClaudeMd' plugin/design-orchestrator/agents/vf-design-judge.md</automated>
    <fails_when>la sortie n'est pas exactement 0 (garde anti-fusion : cette tâche ne doit JAMAIS toucher vf-design-judge.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^tools: .*SendMessage, Agent(vf-crafter' plugin/design-orchestrator/agents/vf-design-manager.md` rend 1 et `grep '^tools:' plugin/design-orchestrator/agents/vf-design-manager.md | grep -c 'vf-dev-manager'` rend 0
    - `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- plugin/design-orchestrator/agents/vf-design-judge.md | wc -l` rend 0
    - corps inchangé, colonne d'instructions inchangée (CORPUS-DO-OK, INSTR-INCHANGE)
    - VERSION, module.json, ligne Version du README et entrée CHANGELOG de tête portent la même version, patch +1 de main
    - `B=$(git merge-base HEAD origin/main); c=$(git log -1 --format=%H -- plugin/design-orchestrator/VERSION); git merge-base --is-ancestor "$c" "$B" && echo ISOLATION-NON-PROBANTE-COMMIT-ANTERIEUR || git show --name-only --format= "$c" | grep -v '^$' | grep -vc '^plugin/design-orchestrator/'` rend 0 (même correctif I1 qu'à la Tâche 1)
  </acceptance_criteria>
  <done>Le manager de design-orchestrator satisfait I6 ; T8 du module reste vert ; module bumpé en patch dans un commit dédié, destiné à la relecture de Samuel. Le juge (I5) reste inchangé — sa pose vit désormais derrière le checkpoint D-19 de 42-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter d'agent → runtime Claude Code | `omitClaudeMd` retire l'injection automatique des CLAUDE.md au juge ; `SendMessage` ouvre au manager la vue sur ses pairs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Information / intégrité du jugement | growth-quality-judge (INTERDITS RGPD du CLAUDE.md du lab), vf-design-judge (section design du CLAUDE.md projet) | medium | mitigate | les deux corps citent déjà ces fichiers comme sources à LIRE et les agents gardent Read ; consigné au SUMMARY pour la relecture (Samuel pour design-orchestrator) ; aucun corps modifié |
| T-42-08 | Tampering | corps d'agent modifié en douce | low | mitigate | corps comparé octet pour octet à main (cmp) ; colonne d'instructions inchangée (INSTR-INCHANGE) |
| T-42-10 | Elevation of Privilege | allowlist du manager design | low | mitigate | T8 de test-design-orchestrator.sh (vf-dev-manager jamais dans l'allowlist, parenthèse fermée) reste vert |
</threat_model>

<verification>
- `for d in plugin/growth-bundle/agents plugin/design-orchestrator/agents; do bash plugin/conductor/scripts/check-agents.sh --strict --agents-dir="$d"; done` — rc 0.
- `bash plugin/conductor/scripts/check-instruction-budget.sh` — `0 depassement(s)`, `code=0`.
- `bash scripts/check-version-sync.sh` — rc 0.
- Deux commits, un par module, aucun ne touche le gate ni `.planning/`.
</verification>

<success_criteria>
- Violations I5 (growth-quality-judge, vf-design-judge) et I6 (vf-growth-manager, vf-design-manager) fermées par frontmatter pur.
- D-12 : un commit par module, bump patch, design-orchestrator destiné à la relecture de Samuel.
</success_criteria>

## Artifacts this phase produces

| Symbole / chemin | Nature | Posé par |
|---|---|---|
| `plugin/conductor/scripts/check-agents-manifest.json` | fichier de données versionné (nouveau) | 42-01 |
| clés `valide_jours`, `rafraichissement`, `listes` ; six listes `outils`, `champs_frontmatter`, `types_natifs`, `modeles`, `modes_permission`, `niveaux_effort` ; clés `verifie_le`, `source`, `valeurs` | schéma du manifeste (D-01) | 42-01 |
| `VF_MANIFEST`, `VIBEFLOW_FIELDS`, `charger_manifeste()`, jeton `MANIFESTE-ILLISIBLE` | chargement fail-closed | 42-01 |
| boucle `*.json` « Site #3bis » de `copy_module_scripts()` ; `T54` | installeur (D-16) et son test | 42-01 |
| `mk_manifest`, `mk_gate_dir`, `REAL_CHECK`, `REAL_MANIFEST`, `GATE_DIR` ; `T77` à `T82` | harnais et cas du manifeste | 42-01 |
| `T103`, `MUT-D20`, jeton `CIBLE-ABSENTE` | invocation nue sur cible absente hors --hook (D-20) | 42-01 |
| `--manifest-freshness=lenient\|strict`, `VF_MANIFEST_FRESHNESS`, `manifeste_perime()`, `retrograder` | fraîcheur (D-02, D-04, D-05) | 42-04 |
| jetons `MANIFESTE-PERIME`, `DATE-FUTURE`, suffixe `retrograde` | diagnostics de fraîcheur | 42-04 |
| `make_gate_mutant`, `okmut`, `komut` ; `T83` à `T90`, `MUT-F1`, `MUT-F2` | mutation QUAL-01 et cas de fraîcheur | 42-04 |
| `--manifest-freshness=strict` aux quatre appels CI | `.github/workflows/ci.yml` | 42-04 |
| `parse_token()`, `allowlist_agents()` | analyse pure des allowlists | 42-05 |
| `invariant_i1()`, `invariant_i4()`, `invariant_i5()`, `invariant_i6()`, `invariant_i7()` ; jeton `invariant I<n>` | invariants locaux | 42-05 |
| `T91` à `T96`, `MUT-I1`, `MUT-I4`, `MUT-I5`, `MUT-I6`, `MUT-I7` | cas et mutants | 42-05 |
| `decouvrir_agents()`, `index_agents()` | découverte récursive avec exclusions (D-10) | 42-06 |
| `invariant_i2()`, `invariant_i3()` | invariants de monde fermé (D-09) | 42-06 |
| `T97` à `T102`, `MUT-D1`, `MUT-D2`, `MUT-I2`, `MUT-I3` | cas et mutants | 42-06 |
| `vf-internal: true` + marqueur `Worker interne` sur `vf-test-orchestrator` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur `quality-gate-client`, `content-clarity-judge` ; `SendMessage` sur `vf-business-manager`, `vf-content-manager` | frontmatter du corpus | 42-02 |
| `omitClaudeMd: true` sur `growth-quality-judge`, `vf-design-judge` ; `SendMessage` sur `vf-growth-manager`, `vf-design-manager` | frontmatter du corpus | 42-03 |
| mobile-test-team, business-pilot-bundle, content-bundle en patch | versions de module | 42-02 |
| growth-bundle, design-orchestrator en patch | versions de module | 42-03 |
| conductor en mineure | version de module | 42-06 |

## Flagged assumptions

- **Flake connu T9/T29/T32 de test-check-agents.sh (documenté dans la PR #100, workstream fiabilite) : un rouge isolé sur ces trois cas précis (SIGPIPE) se rejoue seul avant d'être déclaré réel — jamais pris pour argent comptant à la première lecture de sortie de suite (0 KO attendu dans ce plan).**

- **Sonde de bords spec-less — FABR-05 : `unclassified` / `unresolved`.** Non résolu d'office ; les
  bords traités (corps inchangé, compte d'instructions inchangé, contrainte T8 sur la ligne tools:
  du manager design) sont des vérités du plan.
- **omitClaudeMd et les sources citées par les juges — CORRIGÉ (42-D19-MESURE.md, 2026-09-24) :**
  la mesure confirme que `omitClaudeMd: true` retire aussi les règles `.claude/rules/*.md` sans
  `paths:` — `plugin/software-architecture/rules/` livre de vraies règles (`doc-research-before-debug.md`,
  `production-code-architecture.md`), pas un cas théorique. La pose d'`omitClaudeMd` sur les juges de
  ce module (`growth-quality-judge`, `vf-design-judge`) sort de ce plan et vit derrière le checkpoint
  D-19 de 42-05, D-08 étant réexaminée avec Samuel.

<output>
Create `.planning/workstreams/gouvernance/phases/VFDO-42-fabrique-manifeste-dat-et-invariants-de-doctrine-du-gate-des/42-03-SUMMARY.md` when done — il liste le hash du commit design-orchestrator (relecture Samuel). Les juges (growth-quality-judge, vf-design-judge) ne sont plus touchés par ce plan — leur pose vit derrière le checkpoint D-19 de 42-05.
</output>

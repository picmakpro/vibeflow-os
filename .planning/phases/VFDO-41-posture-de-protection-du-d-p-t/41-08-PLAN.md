---
phase: 41-posture-de-protection-du-d-p-t
plan: 08
lot: L8 — contournement réel et tracé sur la PR de preuve inoffensive (temps ii du critère 2 et de la revue code owner), mesure M-3
type: execute
wave: 6
depends_on: ["41-07"]
autonomous: false
requirements: [PROT-01, PROT-03, PROT-04]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Précondition dure : `M1-VERDICT: SUR` et PR-R fermée sans merge (41-07) ; PR-P toujours ouverte sur P1, non mergée"
    - "Support du contournement réel (D-M7) : PR-P, contenu inoffensif (README des sources de rulesets). La PR rouge n'est JAMAIS contournée ni mergée"
    - "Le contournement porte sur deux règles non satisfaites au même instant : revue code owner manquante ET checks requis en cours après publication de P2. La règle `required_status_checks` est donc contournée sur des checks non encore verts plutôt que rouges : même règle, état différent — écrit tel quel au SUMMARY, jamais présenté comme un contournement de checks rouges"
    - "M-3 mesuré : état de merge et checks relevés juste avant, sortie de `gh pr merge --merge --admin` par `samuel-neveugall` (compte write, bypass `pull_request`), résultat, et règles effectivement contournées lues dans la rule suite du commit de merge"
    - "Temps (ii) : une rule suite de résultat `bypass` sur `refs/heads/main` dont `after_sha` est le commit de merge de PR-P, lue par le compte qui a l'accès (`ACCES-RULE-SUITES:`) ; chaque règle (`pull_request`, `required_status_checks`) est déclarée `prouvee` si elle figure en échec dans les évaluations de cette suite, `non_prouvee` sinon — jamais supposée"
    - "Ce qui atterrit sur `main` est limité au seul `.github/rulesets/README.md`, et la CI du push de merge sur `main` est verte (4 jobs)"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "`M3:`, `M3-MSG:`, `TRACE-II:`, `TRACE-II-MSG:`, `MAIN-APRES-PR-P:`"
  key_links:
    - "`TRACE-II:` alimente PROT-01 (critère 2 temps ii), PROT-04 (revue code owner contournable mais tracée) et la section « Mesures » d'ADR-072 (plan 41-10)"
---

<objective>
Produire un contournement réel, explicite et inoffensif, puis lire sa trace dans GitHub.

Purpose: temps (ii) du critère 2 (D-09) et de la revue code owner (D-M7) ; inconnu M-3.
Output: PR-P mergée en contournement, trace lue et consignée règle par règle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-07-SUMMARY.md

Sources lues le 2026-09-17 : `docs.github.com/en/rest/repos/rule-suites` — `GET /repos/{owner}/{repo}/rulesets/rule-suites`
(paramètres `ref`, `time_period` hour/day/week/month, `actor_name`, `rule_suite_result`
pass/fail/bypass/all), `GET …/rule-suites/{rule_suite_id}` (`after_sha`, `result`,
`rule_evaluations[]` avec `rule_type`, `result`, `enforcement`) ; couverture des merges de PR non
précisée par la doc — d'où la lecture ciblée sur `after_sha`. `cli.github.com/manual/gh_pr_merge`
(`--admin`).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : préalables relus, commandes de contournement et de lecture de trace préparées</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`M1-VERDICT:`, `PR-R-FERMEE:`, `PR-P:`, `PR-P-COMMITS:`, `ACCES-RULE-SUITES:`)
  </read_first>
  <action>
Identité `samuel-neveugall` ; `git fetch origin`. Relire : `M1-VERDICT: SUR` ; PR-R `CLOSED` non
mergée ; PR-P `OPEN`, tête = P1 ; seule PR ouverte = PR-P ; P2 a bien P1 pour parent ; branche
`preuve/41-contournement` distante = P1. Consigner `MAIN-AVANT-PR-P-II: <sha d'origin/main>`.

Imprimer les commandes de la Task 2, en y indiquant le compte lecteur des rule suites d'après
`ACCES-RULE-SUITES:` (`samuel-neveugall` si `ok`, sinon `picmakpro`). Commit local sur B.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); v=$(awk "\$1==\"M1-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); R=$(awk "\$1==\"PR-R:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); rs=$(gh pr view "$R" --repo picmakpro/vibeflow-os --json state,mergedAt --jq "\"\(.state):\(.mergedAt != null)\""); ps=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); ph=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json headRefOid --jq .headRefOid); P1=$(val PR-P-COMMITS: p1); P2=$(val PR-P-COMMITS: p2); pp=$(git rev-parse "$P2^"); op=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq length); ma=$(awk "\$1==\"MAIN-AVANT-PR-P-II:\" {print \$2; exit}" "$PH/41-PREUVES.md"); echo "prealables ii : identite=$u m1=$v pr_r=$rs pr_p=$ps tete_p1=$([ "$ph" = "$P1" ] && echo oui || echo non) parent_p2_p1=$([ "$pp" = "$P1" ] && echo oui || echo non) prs_ouvertes=$op main_consigne=$([ "$ma" = "$(git rev-parse origin/main)" ] && echo oui || echo non)"; [ "$u" = samuel-neveugall ] && [ "$v" = SUR ] && [ "$rs" = CLOSED:false ] && [ "$ps" = OPEN ] && [ "$ph" = "$P1" ] && [ "$pp" = "$P1" ] && [ "$op" -eq 1 ] && [ "$ma" = "$(git rev-parse origin/main)" ]'</automated>
    <fails_when>exit non nul ; ou `m1=` différent de `SUR` ; ou PR-R non fermée ou mergée ; ou PR-P non ouverte ou tête différente de P1 ; ou P2 non fils de P1 ; ou une autre PR ouverte ; ou `MAIN-AVANT-PR-P-II:` absente ou périmée</fails_when>
  </verify>
  <acceptance_criteria>
    - M-1 SUR, PR-R fermée non mergée, PR-P seule ouverte sur P1, P2 prêt
    - `main` relevée avant le contournement
  </acceptance_criteria>
  <done>Le contournement peut être joué sur le seul support inoffensif.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains — publier P2 puis merger PR-P en contournant explicitement pendant que les checks tournent ; lire la trace</name>
  <action>Samuel, compte `samuel-neveugall`, publie P2 et merge aussitôt PR-P avec `--admin` pendant que les checks sont en cours ; puis le compte lecteur lit les rule suites.</action>
  <instructions>
Compte qui agit pour 1-4 : `samuel-neveugall`. Enchaîner 1 à 3 sans attendre la CI (elle dure
plusieurs minutes).

1. `git push origin <p2>:refs/heads/preuve/41-contournement`
2. Relevé juste avant :
   `gh pr view <n> --repo picmakpro/vibeflow-os --json headRefOid,mergeStateStatus,reviewDecision,statusCheckRollup --jq '{tete: .headRefOid, etat: .mergeStateStatus, revue: .reviewDecision, checks_en_cours: ([.statusCheckRollup[] | select((.status // "COMPLETED") != "COMPLETED")] | length)}'`
   → la tête doit être `<p2>` et `checks_en_cours` supérieur à 0 ; sinon relancer 2 quelques secondes
   plus tard. Si les checks sont déjà tous terminés : NE PAS merger, le signaler (la trace de la
   règle des checks ne serait pas produite).
3. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge --admin` → copier la sortie intégrale.
   Témoin : `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit --jq '{state, merge: .mergeCommit.oid}'`.
   Si `gh` refuse : copier le message et s'arrêter (résultat M-3 à consigner ; ne pas essayer la case
   de l'interface sans nouvelle consigne).
4. `git push origin --delete preuve/41-contournement`
5. Lecture de la trace, par le compte indiqué par la Task 1 (`gh auth switch --user …` si
   `picmakpro`, retour à `samuel-neveugall` ensuite, témoin `gh api user --jq .login`) :
   `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?ref=refs/heads/main&time_period=hour&rule_suite_result=bypass" --jq '.[] | {id, actor_name, after_sha, result}'`
   puis, pour l'id dont `after_sha` est le commit de merge :
   `gh api repos/picmakpro/vibeflow-os/rulesets/rule-suites/<id> --jq '{id, actor_name, after_sha, result, evaluations: [.rule_evaluations[] | {rule_type, result, enforcement}]}'`
  </instructions>
  <verification>La Task 3 relit l'état et le contenu du merge, la CI du push de merge sur `main`, et recoupe la trace collée (ou la relit si le compte write y a accès).</verification>
  <resume-signal>Coller les sorties de 2, 3 (et son témoin), 4 et 5, puis écrire « contournement tracé ».</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : relecture du merge, du contenu atterri, de la CI de `main` ; M-3 et trace consignées règle par règle</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - la sortie collée à la Task 2
  </read_first>
  <action>
Identité `samuel-neveugall`. Consigner :
- `M3: tete=<p2> etat_avant=<> revue_avant=<> checks_en_cours_avant=<k> commande=gh_pr_merge_--merge_--admin compte=samuel-neveugall resultat=<merged|refuse> merge=<sha|aucun>` et `M3-MSG:` (sortie collée, une ligne) ;
- `TRACE-II: suite=<id> result=<> after_sha=<> acteur=<> lu_par=<compte> pull_request=<prouvee|non_prouvee> required_status_checks=<prouvee|non_prouvee>` et `TRACE-II-MSG:` (JSON collé, une ligne) — une règle est `prouvee` si elle apparaît avec `result` `fail` dans les évaluations d'une suite de résultat `bypass` dont `after_sha` est le commit de merge ;
- `MAIN-APRES-PR-P: merge=<sha> fichiers=<liste> ci_main=<k>/4`.
Si le compte write a l'accès aux rule suites, relire soi-même la suite et comparer au collage.
Attendre la fin des runs du push de merge sur `main` avant d'écrire `ci_main` (sondage, jamais un
vert supposé). Toute règle `non_prouvee` est écrite telle quelle et remontée au manager : aucun
nouveau contournement n'est tenté sans consigne. Commit local sur B. Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; N=$(awk "\$1==\"PR-P:\" { sub(/^#/, \"\", \$2); print \$2; exit }" "$PH/41-PREUVES.md"); P2=$(val PR-P-COMMITS: p2); st=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json state --jq .state); M=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergeCommit --jq .mergeCommit.oid); git fetch -q origin; anc=$(git merge-base --is-ancestor "$M" origin/main && echo oui || echo non); T=$(mktemp -d); git diff --name-only "$M^1" "$M" > "$T/fl"; fl=$(awk "BEGIN{ORS=\"\"} NR>1{print \",\"} {print}" "$T/fl"); p2in=$(git merge-base --is-ancestor "$P2" "$M" && echo oui || echo non); ci=$(gh api "repos/picmakpro/vibeflow-os/commits/$M/check-runs?per_page=100" --jq "[.check_runs[] | select(.app.id == 15368 and .conclusion == \"success\")] | map(.name) | unique | length"); git ls-remote --heads origin preuve/41-contournement > "$T/lr"; bd=$(awk "END{print NR}" "$T/lr"); m3=$(val M3: resultat); m3m=$(val M3: merge); cv=$(val M3: checks_en_cours_avant); echo "merge ii : pr=$N etat=$st merge=$M dans_main=$anc p2_inclus=$p2in fichiers=$fl ci_main_jobs_verts=$ci branche_distante=$bd m3_resultat=$m3 m3_merge_coherent=$([ "$m3m" = "$M" ] && echo oui || echo non) checks_en_cours_avant=$cv"; [ "$st" = MERGED ] && [ "$anc" = oui ] && [ "$p2in" = oui ] && [ "$fl" = .github/rulesets/README.md ] && [ "$ci" -eq 4 ] && [ "$bd" -eq 0 ] && [ "$m3" = merged ] && [ "$m3m" = "$M" ] && [ "$cv" -gt 0 ]'</automated>
    <fails_when>exit non nul ; ou PR-P non `MERGED` ; ou merge absent de `main` ; ou P2 non inclus ; ou fichiers atterris différents du seul `.github/rulesets/README.md` ; ou `ci_main_jobs_verts=` différent de 4 (CI du push de merge non terminée ou rouge — attendre la fin, puis remonter si rouge) ; ou branche distante encore présente ; ou `M3:` incohérente avec le merge relu ; ou `checks_en_cours_avant=0` (la règle des checks n'était pas en jeu au moment du contournement)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; M=$(val M3: merge); r=$(val TRACE-II: result); a=$(val TRACE-II: after_sha); pr=$(val TRACE-II: pull_request); rc=$(val TRACE-II: required_status_checks); lu=$(val TRACE-II: lu_par); msg=$(awk "\$1==\"TRACE-II-MSG:\" && NF > 1 {c++} END{print c+0}" "$PH/41-PREUVES.md"); acc=$(val ACCES-RULE-SUITES: samuel-neveugall); rel=na; if [ "$acc" = ok ]; then id=$(val TRACE-II: suite); rel=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites/$id" --jq "[.result == \"bypass\", .after_sha == \"$M\", ([.rule_evaluations[] | select(.result == \"fail\") | .rule_type] | index(\"pull_request\") != null) == (\"$pr\" == \"prouvee\"), ([.rule_evaluations[] | select(.result == \"fail\") | .rule_type] | index(\"required_status_checks\") != null) == (\"$rc\" == \"prouvee\")] | all"); fi; echo "trace ii : result=$r after_sha_est_merge=$([ "$a" = "$M" ] && echo oui || echo non) pull_request=$pr required_status_checks=$rc lu_par=$lu message=$msg relecture_write=$rel"; [ -n "$M" ] && [ "$r" = bypass ] && [ "$a" = "$M" ] && { [ "$pr" = prouvee ] || [ "$pr" = non_prouvee ]; } && { [ "$rc" = prouvee ] || [ "$rc" = non_prouvee ]; } && [ -n "$lu" ] && [ "$msg" -eq 1 ] && [ "$rel" != false ]'</automated>
    <fails_when>exit non nul ; ou `result=` différent de `bypass` ; ou `after_sha` différent du commit de merge ; ou statut de règle autre que `prouvee`/`non_prouvee` ; ou lecteur non nommé ; ou JSON de trace non capturé ; ou `relecture_write=false` (la suite relue par l'agent contredit la consignation). Un `non_prouvee` NE rougit PAS cette vérification : il est consigné et remonté, conformément à D-M7</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
  </verify>
  <acceptance_criteria>
    - PR-P mergée par contournement explicite, seul le README atterri, CI de `main` verte, branche supprimée
    - M-3 consigné (état avant, checks en cours, commande, compte, résultat, message)
    - trace : suite `bypass` sur le commit de merge, statut `prouvee`/`non_prouvee` par règle, lecteur nommé, recoupée par l'agent quand il a l'accès
  </acceptance_criteria>
  <done>Le contournement réel existe, il est inoffensif, et sa trace dans GitHub est lue et consignée sans rien supposer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contournement → `main` | seul contenu autorisé à entrer par contournement : le README de preuve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-30 | Tampering | contournement appliqué à un contenu nocif | high | mitigate | précondition PR-R fermée non mergée ; fichiers atterris vérifiés = README seul ; CI de `main` verte |
| T-41-31 | Repudiation | trace supposée ou lue sur la mauvaise suite | high | mitigate | suite choisie par `after_sha` = commit de merge ; statut par règle ; relecture croisée par l'agent si accès |
| T-41-32 | Repudiation | contournement de checks rouges affirmé sans preuve | medium | mitigate | état `checks_en_cours_avant` consigné ; formulation imposée « non encore verts, même règle » |
| T-41-33 | Elevation of privilege | lecture de trace sous jeton admin par l'agent | medium | mitigate | lecture `picmakpro` = geste humain ; identité de l'agent revérifiée |
</threat_model>

<verification>
Contournement réel et inoffensif, M-3 consigné, trace lue sur la bonne suite et consignée règle par
règle.
</verification>

<success_criteria>
Temps (ii) du critère 2 et de la revue code owner prouvés (ou déclarés `non_prouvee` règle par
règle) ; M-3 mesuré.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-08-SUMMARY.md` when done — committé sur la branche B.
</output>

---
phase: 25-budget-d-instructions-et-tage-d-alignement-court
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - .github/workflows/ci.yml
  - plugin/conductor/README.md
  - plugin/conductor/VERSION
  - plugin/conductor/module.json
  - plugin/conductor/CHANGELOG.md
  - README.md
  - README.fr.md
  - docs/ADR.md
  - .planning/BACKLOG.md
autonomous: true
requirements: [BUDG-01, BUDG-02, QUAL-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Le gate est EXPOSÉ là où ça compte : une étape dédiée du job `gates` de `.github/workflows/ci.yml`, avec son commentaire « POURQUOI CE GATE EXISTE ». Un gate correct que la chaîne d'intégration n'invoque jamais est une garde absente — c'est le mode de défaut « sûr mais inerte » d'ADR-059 que ce dépôt a déjà payé, et la raison pour laquelle `check-divergence.sh` a reçu son étape CI en Phase 39."
    - "L'étape CI traite les quatre codes explicitement et dans le bon sens (BUDG-02, anti-feature `.planning/REQUIREMENTS.md:1096`) : `3` (non armé) rend un avertissement GitHub et ne fait JAMAIS échouer le job ; `0` passe ; `1` et `2` font échouer. Aucun `|| true` n'avale un code."
    - "L'étape CI est écrite UNE fois pour les deux états du monde : elle n'aura pas à être rééditée quand le plan 25-04 posera la sentinelle. Elle lit l'état armé et attend `3` en mode non armé, `0` en mode armé conforme — un `0` ou un `1` rendu alors que la sentinelle est absente est un échec, parce qu'il signifierait que le ratchet s'est inversé."
    - "L'étape CI prouve que le gate est DISCRIMINANT, pas seulement présent : sur une fixture jetable armée, elle constate le basculement `0` → `1` par mutation réelle de la fixture (patron `ci.yml:622-681` de la Phase 39), et le `2` sur découverte vide. Une étape qui se contenterait de constater `3` sur le dépôt non armé ne pourrait jamais rendre rouge."
    - "D-05 est réconcilié dans la documentation : la puce de catalogue de `check-agents.sh` cesse de lui attribuer une mesure de densité qu'il ne fait pas (contrat réel vérifié `check-agents.sh:23-77`, 0 comptage), et la capacité est attribuée au nouveau gate dans sa propre entrée."
    - "Les deux compteurs documentaires sont RE-DÉRIVÉS par exécution au moment de la tâche, jamais recopiés : le nombre de scripts du catalogue conductor et le nombre de suites des deux README racine. Un plan écrit avant les faits n'est pas une source de vérité mesurée (leçon Phase 34)."
    - "Le module `conductor` est bumpé en MINOR (`v1.36.0` → `v1.37.0`, un gate neuf = nouvelle capacité) et sa triade `VERSION` / `module.json` / `CHANGELOG` reste synchrone sous `scripts/check-version-sync.sh`. La `VERSION` RACINE n'est PAS touchée : la release du dépôt est un geste humain gaté, hors de ce plan."
    - "ADR-029 reçoit une NOTE DATÉE sous le tableau (patron de la note `ADR-065`), disant que le plafond de lignes et la charge d'instructions deviennent machine-enforced à partir de cette phase, et que la sentinelle n'est pas encore armée. L'entrée d'index existante n'est ni réécrite, ni renumérotée, ni complétée (D-05)."
    - "Les dettes différées du cadrage sont inscrites au BACKLOG avec leur date : budget des `SKILL.md` (≤ 500 lignes) et du bootstrap (≤ 2000 tokens), même absence d'enforcement machine, explicitement hors périmètre BUDG-01."
    - "Le CHANGELOG conductor dit en toutes lettres qu'AUCUNE baseline n'est gravée et que la sentinelle n'est pas posée (D-06 bis) : un lecteur du CHANGELOG ne doit pas pouvoir croire que le ratchet est armé."
  artifacts:
    - ".github/workflows/ci.yml : étape neuve `check-instruction-budget (BUDG-01/02, ADR-029 machine-enforced)` dans le job `gates`, portant le commentaire « POURQUOI CE GATE EXISTE », la preuve de discrimination sur fixture jetable, et le traitement explicite des codes 0/1/2/3."
    - "plugin/conductor/README.md : entrée de catalogue `check-instruction-budget.sh` dans la famille « Gates machine », compte de scripts re-dérivé dans le titre de section, et puce `check-agents.sh` corrigée."
    - "plugin/conductor/VERSION + plugin/conductor/module.json : v1.37.0 synchrones."
    - "plugin/conductor/CHANGELOG.md : entrée `## [v1.37.0]` sur le gabarit de `[v1.35.0]`."
    - "README.md + README.fr.md : compteur de suites re-dérivé."
    - "docs/ADR.md : note datée sous ADR-029, sans réécriture de l'entrée d'index."
    - ".planning/BACKLOG.md : entrée datée pour le budget des SKILL.md et du bootstrap."
  key_links:
    - "Étape CI <-> code de sortie `3` : c'est le seul endroit du dépôt où un `3` doit se lire comme un avertissement et non comme un échec. Aucun précédent n'existait (`25-RESEARCH.md:730-749`) — le `case` explicite est donc la pièce à ne pas rater, et son défaut par `*)` doit rester `exit 1` pour que `1` et `2` restent bloquants."
    - "Preuve de discrimination sur fixture <-> dépôt non armé : sans elle, l'étape serait verte par construction pendant des mois (le dépôt n'est pas armé avant la seconde PR) et personne ne saurait si le gate sait encore rendre rouge."
    - "Compte de scripts du catalogue <-> `find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh'` : l'égalité est vérifiée par machine, ce qui rend le titre de section incapable de mentir silencieusement."
    - "Triade `conductor` <-> `scripts/check-version-sync.sh` : la source #6 du gate compare VERSION, module.json et l'en-tête du README de module — un bump partiel rend rouge."
  prohibitions:
    - "Ne JAMAIS toucher la `VERSION` racine, `plugin/.claude-plugin/plugin.json` ni `.claude-plugin/marketplace.json` dans ce plan : la release du dépôt (tag annoté + release GitHub + `check-release-tag.sh --remote`) est un geste humain gaté, hors mandat."
    - "Ne JAMAIS créer `.planning/.instruction-budget-armed` ni `.planning/instruction-budget-baselines.tsv` : ils appartiennent au plan 25-04, après la Phase 40 (D-06 bis)."
    - "Ne JAMAIS écrire un compteur documentaire en dur sans l'avoir re-dérivé par commande au moment de la tâche."
    - "Ne JAMAIS réécrire, compléter ou renuméroter l'entrée d'index ADR-029 : seule une note datée s'ajoute sous le tableau."
    - "Ne JAMAIS poser de surcharge `VF_BUDGET_*` dans l'étape CI : la cascade de résolution doit être exercée telle qu'elle tourne en réel."
    - "Ne JAMAIS modifier un fichier d'agent pour faire baisser un compte : aucune remédiation dans cette phase."
---

<objective>
Rendre le gate visible et opposable : le câbler dans le job `gates` de la CI avec une preuve de
discrimination, le cataloguer, corriger l'affirmation fausse qui attribuait la densité à
`check-agents.sh`, bumper le module, et poser les traces documentaires (note datée sous ADR-029,
dettes différées au BACKLOG).

Purpose : une garde que la chaîne d'intégration ne lance jamais est une garde absente. Et une
documentation qui attribue une capacité à un script qui ne l'a pas est pire qu'un trou : elle
empêche de voir le trou — c'est exactement ce que `plugin/conductor/README.md` faisait pour la
densité des agents, fait vérifié le 2026-09-14 et gravé en mémoire de reprise.

Output : la première PR de la phase se termine ici. Le manager de mission doit pouvoir s'arrêter
proprement après ce plan et rendre son rapport : la calibration (plan 25-04) est un checkpoint
bloquant qui attend la livraison de la Phase 40 (D-06 bis).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-CONTEXT.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-PATTERNS.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-01-SUMMARY.md
@.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tâche 1 : étape CI dans le job gates — avertissement en mode non armé, et preuve de discrimination sur fixture jetable</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml:244-300` — le début du job `gates` : la forme d'une étape, le commentaire « POURQUOI », l'assertion de découverte non vide
    - `.github/workflows/ci.yml:622-681` — l'étape `check-divergence.sh en CI` de la Phase 39 : le patron exact de « fixture propre → mutation en place → non-régression racine », avec `note()` et bilan unique
    - `.github/workflows/ci.yml:341-353` — l'étape `check-capability-activation` : la doctrine écrite « un exit 2 échoue le job au même titre qu'un exit 1 »
    - `25-RESEARCH.md:730-749` — le constat qu'aucun précédent de « exit 3 accepté sans échec » n'existe dans ce job, et le squelette de `case` proposé
    - `25-PATTERNS.md:252-289` — le gabarit d'étape et la raison pour laquelle la découverte non vide vit dans le script, pas dans le YAML
    - `plugin/conductor/scripts/check-instruction-budget.sh` — le contrat de sortie à traiter
  </read_first>
  <action>
    Ajouter UNE étape au job `gates`, placée juste après l'étape `check-machine-paths` et avant
    l'étape des gates workstream, nommée
    `check-instruction-budget (BUDG-01/02, ADR-029 machine-enforced)`.

    (1) **Commentaire « POURQUOI CE GATE EXISTE ».** Écrire, au-dessus du `run:`, que jusqu'à cette
    phase aucun gate distribué ne mesurait le plafond de 250 lignes hors du module
    `dev-orchestrator` (D-05), ni la charge d'instructions du tout ; que le gate est en ratchet
    (D-04) donc non bloquant tant que la sentinelle est absente ; et pourquoi l'étape contient une
    preuve de discrimination sur fixture — sans elle, l'étape serait verte par construction pendant
    toute la période non armée, et personne ne saurait si le gate sait encore rendre rouge.

    (2) **Bloc 1 — preuve de discrimination sur fixture jetable.** Sur le patron de l'étape
    `check-divergence` : créer un `mktemp -d`, y écrire un module d'agent synthétique
    (`plugin/demo/agents/a.md` avec frontmatter valide et quelques lignes de règles), poser la
    sentinelle de fixture par écriture directe, écrire une baseline TSV reprenant les valeurs que le
    gate mesure sur cette fixture, puis :
    — 1/4 fixture conforme, armée → attendu `0` ;
    — 2/4 MÊME fixture, mutée en place par ajout d'une ligne de règle au fichier d'agent → attendu
    `1` (c'est le basculement qui prouve la discrimination) ;
    — 3/4 même fixture, sentinelle retirée, rien d'autre changé → attendu `3` (le ratchet) ;
    — 4/4 répertoire vide → attendu `2` (découverte vide, jamais un vert).
    Chaque écart incrémente un compteur via une fonction `note()` qui émet `::error::`, et le bloc
    se termine par un bilan unique suivi d'un `exit 1` conditionnel — jamais un `|| true`.

    (3) **Bloc 2 — dépôt réel, sans aucune surcharge.** Invoquer
    `bash plugin/conductor/scripts/check-instruction-budget.sh` sur le checkout, sans `--path` et
    sans variable `VF_BUDGET_*` (la cascade de résolution est exercée telle qu'elle tourne en réel,
    même doctrine que le `--file` explicite de `check-state-integrity`), capturer le code par
    `rc=0 ; out="$(…)" || rc=$?`, imprimer la sortie, puis brancher sur l'état de la sentinelle :
    — sentinelle absente : seul `3` est acceptable ; émettre un `::warning::` disant que le budget
    d'instructions n'est pas armé, et NE PAS faire échouer le job. Tout autre code (`0`, `1`, `2`)
    est un échec, parce qu'il signifierait que le ratchet s'est inversé ou que le gate est devenu
    incapable de se prononcer.
    — sentinelle présente : `0` passe ; `1` et `2` font échouer ; `3` est un échec (le gate se
    croirait non armé alors que la sentinelle est là).
    Écrire les deux branches dès maintenant : l'étape ne devra pas être rééditée quand le plan 25-04
    posera la sentinelle.

    (4) **Indentation et placement.** L'étape vit dans le job `gates`, au même niveau que ses
    voisines (six espaces pour le tiret, huit pour les clés). Ne toucher à aucune autre étape.
  </action>
  <behavior>
    - Sur le checkout non armé, l'étape imprime le rapport, émet un avertissement et ne fait pas échouer le job.
    - Sur une fixture armée et conforme, le gate rend 0 ; la même fixture mutée rend 1.
    - Un répertoire sans agent rend 2 et l'étape échoue.
    - Aucun code de retour n'est avalé.
  </behavior>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contient exactement une étape dont le nom commence par `check-instruction-budget`, et cette étape appartient au job `gates`.
    - Le bloc `run:` de cette étape est du bash valide (`bash -n` sur le corps extrait).
    - Le corps de l'étape contient un commentaire portant la chaîne `POURQUOI CE GATE EXISTE`.
    - Le corps de l'étape contient les quatre codes traités (`0`, `1`, `2`, `3`) et une émission `::warning::` pour le cas non armé.
    - Le corps de l'étape ne contient aucune surcharge `VF_BUDGET_`.
    - Rejoué localement, le gate rend `3` sur le dépôt réel (état non armé) — la valeur que l'étape attend.
    - Rejouées localement, les quatre assertions de la preuve de discrimination (0 / 1 / 3 / 2) sont vraies.
  </acceptance_criteria>
  <verify>
    <automated>Y=.github/workflows/ci.yml; n=$(grep -c '^      - name: check-instruction-budget' "$Y"); test "$n" -eq 1 || { echo "ETAPE-ABSENTE-OU-DUPLIQUEE: $n"; exit 1; }; job=$(awk '/^  [a-z][a-z0-9_-]*:$/{j=$1} /^      - name: check-instruction-budget/{print j; exit}' "$Y"); echo "job=$job"; test "$job" = "gates:" || { echo "ETAPE-HORS-JOB-GATES: $job"; exit 1; }; B=$(mktemp); awk '/^      - name: check-instruction-budget/{f=1} f&&/^        run: \|$/{r=1;next} r&&/^      - name: /{exit} r{sub(/^          /,""); print}' "$Y" > "$B"; test -s "$B" || { echo "CORPS-RUN-VIDE"; rm -f "$B"; exit 1; }; bash -n "$B" || { echo "CORPS-RUN-INVALIDE"; rm -f "$B"; exit 1; }; grep -q 'POURQUOI CE GATE EXISTE' "$Y" || { echo "COMMENTAIRE-POURQUOI-ABSENT"; rm -f "$B"; exit 1; }; grep -q '::warning::' "$B" || { echo "AVERTISSEMENT-NON-ARME-ABSENT"; rm -f "$B"; exit 1; }; grep -q 'VF_BUDGET_' "$B" && { echo "SURCHARGE-ENV-DANS-CI"; rm -f "$B"; exit 1; }; rm -f "$B"; S=plugin/conductor/scripts/check-instruction-budget.sh; rc=0; bash "$S" >/dev/null 2>&1 || rc=$?; test "$rc" -eq 3 || { echo "DEPOT-REEL-RC: $rc attendu 3"; exit 1; }; T=$(mktemp -d); mkdir -p "$T/f/plugin/demo/agents" "$T/f/.planning" "$T/vide"; printf '%s\n' '---' 'name: d' '---' '## Iron Laws' '- Le lock est verifie avant tout checkout.' > "$T/f/plugin/demo/agents/a.md"; : > "$T/f/.planning/.instruction-budget-armed"; L=$(awk 'END{print NR}' "$T/f/plugin/demo/agents/a.md"); I=$(bash "$S" --path "$T/f" 2>/dev/null | awk -F'|' '/^plugin\/demo\/agents\/a\.md /{gsub(/[[:space:]]/,"",$4); print $4}'); printf 'plugin/demo/agents/a.md\t%s\t%s\n' "$L" "$I" > "$T/f/.planning/instruction-budget-baselines.tsv"; r0=0; bash "$S" --path "$T/f" >/dev/null 2>&1 || r0=$?; printf '%s\n' '- Une regle de plus qui DOIT compter.' >> "$T/f/plugin/demo/agents/a.md"; r1=0; bash "$S" --path "$T/f" >/dev/null 2>&1 || r1=$?; rm -f "$T/f/.planning/.instruction-budget-armed"; r3=0; bash "$S" --path "$T/f" >/dev/null 2>&1 || r3=$?; r2=0; bash "$S" --path "$T/vide" >/dev/null 2>&1 || r2=$?; rm -rf "$T"; echo "preuve: conforme=$r0 mute=$r1 desarme=$r3 vide=$r2"; test "$r0" -eq 0 -a "$r1" -eq 1 -a "$r3" -eq 3 -a "$r2" -eq 2 || { echo "PREUVE-NON-DISCRIMINANTE"; exit 1; }; echo CI-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `ETAPE-ABSENTE-OU-DUPLIQUEE: <n>`, `ETAPE-HORS-JOB-GATES: <job>`, `CORPS-RUN-VIDE`, `CORPS-RUN-INVALIDE`, `COMMENTAIRE-POURQUOI-ABSENT`, `AVERTISSEMENT-NON-ARME-ABSENT`, `SURCHARGE-ENV-DANS-CI`, `DEPOT-REEL-RC: <rc> attendu 3`, `PREUVE-NON-DISCRIMINANTE` apparaît ; l'absence de `CI-OK` vaut échec. Causes discriminantes : l'étape est absente, dupliquée, ou posée dans un autre job (elle ne tournerait pas avec les autres gates) ; son corps n'est pas du bash valide (une étape YAML syntaxiquement correcte peut porter un shell cassé qui ne se révèle qu'en CI) ; le commentaire de justification manque ; le cas non armé ne produit pas d'avertissement, donc il serait silencieux ; une surcharge d'environnement détournerait la résolution ; le dépôt réel ne rend pas 3, donc l'attente encodée dans l'étape est fausse ; enfin le bloc de preuve : si le basculement 0 → 1 sur mutation de fixture n'a pas lieu, l'étape serait verte par construction et incapable de rendre rouge.</fails_when>
  </verify>
  <done>Le gate tourne en CI, avertit sans bloquer en mode non armé, et l'étape prouve elle-même qu'elle sait rendre rouge.</done>
  <reversibility rating="reversible">Une étape CI se retire en un diff ; aucun artefact distribué n'en dépend.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : catalogue conductor corrigé et complété, bump minor v1.37.0, compteurs des README re-dérivés</name>
  <files>plugin/conductor/README.md, plugin/conductor/VERSION, plugin/conductor/module.json, plugin/conductor/CHANGELOG.md, README.md, README.fr.md</files>
  <precondition>Les plans 25-01 et 25-02 sont livrés : le script et la suite existent sur disque, sinon les deux compteurs re-dérivés seraient faux.</precondition>
  <read_first>
    - `plugin/conductor/README.md:86-131` — le titre de section portant le compte de scripts, la famille « Gates machine », la puce `check-agents.sh` à corriger, et l'entrée `check-divergence.sh` qui sert de gabarit
    - `plugin/conductor/scripts/check-agents.sh:23-77` — le contrat réel du script : ni comptage de lignes, ni comptage de marqueurs, ce qui fonde la correction
    - `plugin/conductor/CHANGELOG.md` entrée `[v1.35.0]` — le gabarit d'entrée pour un script neuf + une suite neuve
    - `plugin/conductor/VERSION` et `plugin/conductor/module.json` — l'état courant de la triade
    - `README.md:140` et `README.fr.md:145-148` — les deux formulations du compteur de suites, à re-dériver
    - `scripts/check-version-sync.sh` — la source #6 (triade par module) qui rend rouge un bump partiel
    - `25-PATTERNS.md:292-357` — les gabarits d'entrée, la correction ponctuelle, et la discipline de re-dérivation des compteurs
  </read_first>
  <action>
    Mettre la documentation et la version du module en accord avec ce que la phase livre vraiment.

    (1) **Entrée de catalogue.** Ajouter dans la famille « Gates machine (`check-*`) » de
    `plugin/conductor/README.md`, sur le gabarit de l'entrée `check-divergence.sh`, une puce
    `check-instruction-budget.sh` qui dit : les deux métriques publiées par fichier d'agent
    distribué (lignes du fichier entier, instructions du body), la portée (`plugin/*/agents/*.md` et
    `plugin/*/AGENT.md`), le mécanisme de ratchet par sentinelle versionnée lue et jamais écrite,
    les CINQ codes de sortie énumérés, le consommateur (job CI `gates`), et la mention explicite
    que la sentinelle n'est pas armée à ce stade.

    (2) **Correction D-05.** Dans la puce `check-agents.sh` du même catalogue, retirer la mention
    qui lui attribue à tort la mesure de densité des agents — la locution exacte à retirer est citée
    verbatim dans `25-PATTERNS.md:303-311` et dans le critère d'acceptation de cette tâche. Le reste
    de la puce (frontmatter, champs requis, skills déclarés, `vf-internal`, contenu du champ
    `tools:`) est exact et reste tel quel. Ne pas réécrire la puce : retirer la mention fautive et
    renvoyer, dans la même phrase, à l'entrée du nouveau gate.

    (3) **Compte de scripts.** Re-dériver par
    `find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'` et mettre
    le titre `## Scripts (N) — par famille` à cette valeur. La note existante sous le titre, qui
    signale cinq scripts non catalogués antérieurs à cette phase, reste inchangée : la rattraper
    n'est pas le mandat de ce plan, et l'effacer ferait disparaître un écart connu.

    (4) **Bump minor.** `plugin/conductor/VERSION` et le champ `version` de
    `plugin/conductor/module.json` passent à `v1.37.0` (gate neuf = nouvelle capacité = minor,
    cohérent avec le précédent Phase 39 `v1.34.0` → `v1.35.0`). Mettre à jour la ligne
    `**Version** : …` du README de module si elle existe, pour que la source #6 de
    `check-version-sync.sh` reste verte. La `VERSION` RACINE n'est pas touchée.

    (5) **CHANGELOG.** Entrée `## [v1.37.0] — <date du jour> (Phase 25 — budget d'instructions,
    BUDG-01/02)` sur le gabarit `[v1.35.0]` : le script neuf avec son nombre de lignes réel, la
    suite neuve avec son nombre de cas réel, les deux métriques, les cinq codes, l'étape CI, la
    correction D-05 du catalogue — et une phrase qui dit EN TOUTES LETTRES qu'aucune baseline n'est
    gravée, que la sentinelle n'est pas posée, et que la calibration attend la livraison de la Phase
    40 (D-06 bis). Un lecteur du CHANGELOG ne doit pas pouvoir croire que le ratchet est armé.

    (6) **Compteur de suites des deux README racine.** Re-dériver par
    `find plugin scripts -type f -path '*/tests/test-*.sh' | awk 'END{print NR}'`, puis mettre à
    jour la formulation anglaise de `README.md` et la formulation française de `README.fr.md`. Ce
    compteur n'est gaté par aucun script (vérifié) : c'est de l'hygiène documentaire, mais un
    compteur faux est exactement le type de dérive que ce dépôt a déjà payé.
  </action>
  <behavior>
    - Le catalogue conductor décrit le nouveau gate et n'attribue plus la densité à `check-agents.sh`.
    - Le compte de scripts du titre de section égale le compte réel.
    - La triade du module est synchrone en v1.37.0 et `check-version-sync.sh` reste vert.
    - Les deux README racine portent le compte de suites réel.
    - Les fichiers de release racine sont intacts.
  </behavior>
  <acceptance_criteria>
    - `plugin/conductor/VERSION` vaut `v1.37.0` et `plugin/conductor/module.json` porte `"version": "v1.37.0"`.
    - `plugin/conductor/CHANGELOG.md` contient une ligne commençant par `## [v1.37.0]`, et l'entrée mentionne la Phase 40 et l'absence de baseline gravée.
    - `bash scripts/check-version-sync.sh` sort en 0.
    - `plugin/conductor/README.md` contient exactement une puce de catalogue pour `check-instruction-budget.sh`.
    - Le titre `## Scripts (N) — par famille` porte la valeur rendue par `find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'`.
    - La région du catalogue couvrant la puce `check-agents.sh` (de cette puce jusqu'à la puce de premier niveau suivante) ne contient plus le mot `préchargement`.
    - `README.md` contient la chaîne `(<N> suites in CI` et `README.fr.md` la chaîne équivalente entre accents graves, où `<N>` est le compte réel de suites.
    - `git status --short VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json` ne signale aucun fichier.
  </acceptance_criteria>
  <verify>
    <automated>test "$(cat plugin/conductor/VERSION)" = "v1.37.0" || { echo "VERSION-MODULE-FAUSSE: $(cat plugin/conductor/VERSION)"; exit 1; }; grep -q '"version": "v1.37.0"' plugin/conductor/module.json || { echo "MODULE-JSON-DESYNC"; exit 1; }; grep -q '^## \[v1.37.0\]' plugin/conductor/CHANGELOG.md || { echo "CHANGELOG-SANS-ENTREE"; exit 1; }; awk '/^## \[v1.37.0\]/{f=1;next} f&&/^## \[/{exit} f' plugin/conductor/CHANGELOG.md | grep -q 'Phase 40' || { echo "CHANGELOG-SANS-MENTION-40"; exit 1; }; bash scripts/check-version-sync.sh || { echo "VERSION-SYNC-ROUGE"; exit 1; }; c=$(grep -c '^- .check-instruction-budget\.sh.' plugin/conductor/README.md); test "$c" -eq 1 || { echo "ENTREE-CATALOGUE: $c"; exit 1; }; N=$(find plugin/conductor/scripts -maxdepth 1 -type f -name '*.sh' | awk 'END{print NR}'); D=$(sed -n 's/^## Scripts (\([0-9][0-9]*\)).*/\1/p' plugin/conductor/README.md | head -1); echo "scripts_reels=$N scripts_documentes=$D"; test "$N" = "$D" || { echo "COMPTE-SCRIPTS-DESYNC"; exit 1; }; r=$(awk '/^- .check-agents\.sh./{f=1;print;next} f&&/^- ./{f=0} f' plugin/conductor/README.md | grep -c 'préchargement'); test "$r" -eq 0 || { echo "CORRECTION-D05-NON-FAITE: $r"; exit 1; }; SU=$(find plugin scripts -type f -path '*/tests/test-*.sh' | awk 'END{print NR}'); BT=$(printf '\140'); grep -q "($SU suites in CI" README.md || { echo "COMPTEUR-README-EN-DESYNC: attendu $SU"; exit 1; }; grep -q "${BT}$SU suites${BT} en CI" README.fr.md || { echo "COMPTEUR-README-FR-DESYNC: attendu $SU"; exit 1; }; gs=$(git status --short VERSION .claude-plugin/marketplace.json plugin/.claude-plugin/plugin.json) || { echo "GIT-INDISPONIBLE"; exit 1; }; g=$(printf '%s' "$gs" | grep -c .); test "$g" -eq 0 || { echo "RELEASE-META-TOUCHEE: $g fichier(s)"; exit 1; }; echo CATALOGUE-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `VERSION-MODULE-FAUSSE`, `MODULE-JSON-DESYNC`, `CHANGELOG-SANS-ENTREE`, `CHANGELOG-SANS-MENTION-40`, `VERSION-SYNC-ROUGE`, `ENTREE-CATALOGUE: <n>`, `COMPTE-SCRIPTS-DESYNC`, `CORRECTION-D05-NON-FAITE: <n>`, `COMPTEUR-README-EN-DESYNC: attendu <n>`, `COMPTEUR-README-FR-DESYNC: attendu <n>`, `RELEASE-META-TOUCHEE: <n> fichier(s)` apparaît ; l'absence de `CATALOGUE-OK` vaut échec. Causes discriminantes : bump partiel de la triade (le gate de synchronisation le voit aussi) ; entrée de CHANGELOG absente ou muette sur le fait que rien n'est armé, ce qui laisserait croire au lecteur que le ratchet est en place ; le nouveau gate absent du catalogue, ou catalogué deux fois ; le compte de scripts recopié au lieu d'être re-dérivé ; la correction D-05 non appliquée, donc la documentation continue d'attribuer la densité à un script qui ne la mesure pas ; un compteur de suites périmé dans l'un des deux README ; un fichier de release racine touché, ce qui empiéterait sur un geste humain gaté.</fails_when>
  </verify>
  <done>Le module est bumpé, le catalogue dit vrai sur les deux scripts, et les deux compteurs documentaires sont mesurés.</done>
  <reversibility rating="reversible">Version de module et documentation : tout se corrige par un commit, aucun tag n'est posé ici.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : note datée sous ADR-029 et inscription des dettes différées au BACKLOG</name>
  <files>docs/ADR.md, .planning/BACKLOG.md</files>
  <precondition>La tâche 2 est verte : le gate est catalogué, donc la note ADR peut le désigner par un nom que le catalogue confirme.</precondition>
  <read_first>
    - `docs/ADR.md:40-55` — la note `ADR-065` (gabarit de blockquote ajouté après le tableau, sans réécrire l'index), le motif de statut « amendée le AAAA-MM-JJ » d'ADR-069, et l'entrée d'index ADR-029 à ne pas toucher
    - `25-PATTERNS.md:361-388` — la forme exacte de la note à ajouter et l'interdiction de combler ou renuméroter l'index
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh:1241-1249,1360,1448-1449` — le seul enforcement existant du plafond 250, mono-module, à nommer dans la note comme l'état antérieur
    - `.planning/BACKLOG.md:34-40` — la forme des entrées de backlog différé (titre de section, statut, date)
    - `25-CONTEXT.md:175-183` — la liste exacte des dettes à inscrire et de ce qui est écarté plutôt que différé
  </read_first>
  <action>
    Laisser derrière la phase deux traces durables : ce qui change pour ADR-029, et ce qui reste dû.

    (1) **Note datée sous ADR-029.** Ajouter, immédiatement après le tableau « ADR héritées les plus
    citées » de `docs/ADR.md`, un blockquote sur le patron de la note `ADR-065`, titré
    `**ADR-029 : enforcement machine à partir de la Phase 25 (<date du jour>)**`, disant : que le
    plafond de lignes des agents distribués et leur charge d'instructions sont désormais mesurés et
    publiés par `plugin/conductor/scripts/check-instruction-budget.sh` ; que le ratchet est armé par
    la sentinelle `.planning/.instruction-budget-armed`, NON POSÉE à cette date, la calibration
    attendant la livraison de la Phase 40 ; et quel était l'état antérieur — seule la suite
    mono-module `test-dev-orchestrator.sh` (T3/T5) enforçait le plafond de lignes, et rien ne
    mesurait la charge d'instructions. Ne pas réécrire, compléter ni renuméroter l'entrée d'index
    ADR-029 du tableau : la même discipline que la note `ADR-065` s'applique.

    (2) **Dettes au BACKLOG.** Ajouter une section datée `## Budget des SKILL.md et du bootstrap —
    sans enforcement machine (différé de la Phase 25, <date du jour>)` : ADR-029 borne aussi les
    skills à 500 lignes et le bootstrap à 2000 tokens, et aucun gate distribué ne les mesure ; la
    Phase 25 a délibérément limité sa portée aux agents distribués (D-03) ; le déclencheur de
    reprise est nommé — le gate existe désormais et son extension à un second corpus est une
    modification de portée, pas une reconstruction. Mentionner aussi, dans la même section, ce qui
    est ÉCARTÉ et non différé : la métrique en tokens estimés (piste F3), à ne rouvrir que sur un
    incident lié à la taille plutôt qu'à l'adhérence.

    (3) **Remédiation, pas ici.** Ajouter une phrase à la même entrée : la remédiation des fichiers
    les plus chargés est un geste ultérieur, par lot, chacun abaissant une baseline dans son propre
    commit — jamais un livrable de cette phase, et jamais une réécriture « pour passer ».
  </action>
  <behavior>
    - La note ADR-029 existe sous le tableau, datée, et nomme le script et la sentinelle.
    - L'entrée d'index ADR-029 du tableau est intacte, au caractère près.
    - Le BACKLOG porte une entrée datée pour les deux budgets non couverts et pour la remédiation différée.
  </behavior>
  <acceptance_criteria>
    - `docs/ADR.md` contient une ligne de blockquote portant `**ADR-029 : enforcement machine à partir de la Phase 25` suivie d'une date au format AAAA-MM-JJ.
    - Le bloc de cette note nomme `check-instruction-budget.sh` et `.instruction-budget-armed`, et contient la chaîne `Phase 40`.
    - `docs/ADR.md` contient toujours exactement une fois la ligne d'index `| ADR-029 | Charte densité : agents ≤ 250 lignes, skills ≤ 500, bootstrap ≤ 2000 tokens |`.
    - `.planning/BACKLOG.md` contient un titre de section portant `Budget des SKILL.md et du bootstrap` avec une date AAAA-MM-JJ, et le corps de cette section mentionne `2000 tokens`, `500` et la piste écartée.
    - Aucun fichier sous `plugin/` n'est modifié par cette tâche.
  </acceptance_criteria>
  <verify>
    <automated>A=docs/ADR.md; grep -qE '^> \*\*ADR-029 : enforcement machine à partir de la Phase 25 \([0-9]{4}-[0-9]{2}-[0-9]{2}\)\*\*' "$A" || { echo "NOTE-ADR029-ABSENTE-OU-NON-DATEE"; exit 1; }; N=$(awk '/^> \*\*ADR-029 : enforcement machine/{f=1} f&&!/^>/{exit} f' "$A"); printf '%s\n' "$N" | grep -q 'check-instruction-budget.sh' || { echo "NOTE-SANS-NOM-DE-GATE"; exit 1; }; printf '%s\n' "$N" | grep -q 'instruction-budget-armed' || { echo "NOTE-SANS-SENTINELLE"; exit 1; }; printf '%s\n' "$N" | grep -q 'Phase 40' || { echo "NOTE-SANS-DEPENDANCE-40"; exit 1; }; i=$(grep -c '^| ADR-029 | Charte densité : agents ≤ 250 lignes, skills ≤ 500, bootstrap ≤ 2000 tokens |' "$A"); test "$i" -eq 1 || { echo "INDEX-ADR029-ALTERE: $i occurrence(s)"; exit 1; }; B=.planning/BACKLOG.md; grep -qE '^## .*Budget des SKILL\.md et du bootstrap.*[0-9]{4}-[0-9]{2}-[0-9]{2}' "$B" || { echo "BACKLOG-SANS-ENTREE-DATEE"; exit 1; }; S=$(awk '/^## .*Budget des SKILL\.md et du bootstrap/{f=1;next} f&&/^## /{exit} f' "$B"); printf '%s\n' "$S" | grep -q '2000 tokens' || { echo "BACKLOG-SANS-BOOTSTRAP"; exit 1; }; printf '%s\n' "$S" | grep -q '500' || { echo "BACKLOG-SANS-SKILLS"; exit 1; }; gp=$(git status --short plugin) || { echo "GIT-INDISPONIBLE"; exit 1; }; echo "etat_plugin_cette_tache=[$gp]"; echo ADR-BACKLOG-OK</automated>
    <fails_when>sortie non nulle, ou l'une des lignes `NOTE-ADR029-ABSENTE-OU-NON-DATEE`, `NOTE-SANS-NOM-DE-GATE`, `NOTE-SANS-SENTINELLE`, `NOTE-SANS-DEPENDANCE-40`, `INDEX-ADR029-ALTERE: <n> occurrence(s)`, `BACKLOG-SANS-ENTREE-DATEE`, `BACKLOG-SANS-BOOTSTRAP`, `BACKLOG-SANS-SKILLS` apparaît ; l'absence de `ADR-BACKLOG-OK` vaut échec. Causes discriminantes : la note est absente ou non datée, donc invérifiable dans six mois ; elle ne nomme ni le gate ni la sentinelle, donc elle annonce un enforcement sans dire par quoi ; elle tait la dépendance à la Phase 40, donc un lecteur croirait le ratchet déjà armé ; l'entrée d'index ADR-029 a été réécrite ou dupliquée, ce qui casserait les références existantes ; l'entrée de backlog manque ou ne couvre pas les deux budgets restés sans gate.</fails_when>
  </verify>
  <done>ADR-029 porte la trace datée de son passage en enforcement machine, et les dettes restantes ont une entrée nommée au BACKLOG.</done>
  <reversibility rating="reversible">Documentation seule.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → job CI `gates` | Le contenu d'une PR détermine ce que le gate mesure ; l'étape CI est la seule barrière qui s'applique à toutes les PR. |
| documentation → lecteur | Le catalogue et le CHANGELOG sont la source à laquelle un humain se fie pour savoir ce qui est armé. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-11 | Denial of Service | traitement du code `3` dans l'étape CI | high | mitigate | Le `case` traite `0` et `3` nommément et renvoie tout le reste sur `exit 1` : un `1` ou un `2` ne peut pas être absorbé par la branche d'avertissement. La vérification de tâche rejoue le dépôt réel et les quatre états de fixture pour constater les codes attendus. |
| T-25-12 | Repudiation | étape CI verte par construction | high | mitigate | L'étape porte sa propre preuve de discrimination (basculement `0` → `1` par mutation de fixture), sur le patron `ci.yml:622-681` : elle ne peut pas rester verte si le gate cesse de comparer. |
| T-25-13 | Spoofing | résolution de chemin du gate en CI | medium | mitigate | Aucune surcharge `VF_BUDGET_*` dans l'étape, invocation sans `--path` : la cascade réelle est exercée. La vérification de tâche refuse toute occurrence de `VF_BUDGET_` dans le corps de l'étape. |
| T-25-14 | Repudiation | CHANGELOG laissant croire le ratchet armé | medium | mitigate | L'entrée `[v1.37.0]` doit mentionner la Phase 40 et l'absence de baseline gravée ; la vérification de tâche l'exige par grep sur le corps de l'entrée. |
| T-25-15 | Tampering | fichiers de release racine | medium | mitigate | `git status --short` sur `VERSION`, `marketplace.json` et `plugin.json` doit être vide : la release du dépôt reste un geste humain gaté (`CLAUDE.md`, discipline de release). |
| T-25-SC | Tampering | chaîne d'approvisionnement | low | accept | Aucune installation de paquet ni action GitHub nouvelle : l'étape ajoutée n'utilise que `bash` et le checkout déjà présent. |
</threat_model>

<verification>
- L'étape `check-instruction-budget` existe une fois, dans le job `gates`, et son corps est du bash valide.
- Le dépôt réel rend `3` ; la preuve de fixture rend `0`, `1`, `3`, `2` dans l'ordre attendu.
- `bash scripts/check-version-sync.sh` vert ; triade conductor en v1.37.0.
- Les compteurs de scripts et de suites sont re-dérivés et concordants.
- Rejouer les commandes du job CI `gates` de `ci.yml` avant tout push — jamais la liste d'un rapport.
</verification>

<success_criteria>
- Le gate est invoqué par la chaîne d'intégration, en avertissement, sans jamais bloquer tant que
  la sentinelle est absente (BUDG-02).
- La documentation dit vrai sur ce que mesure `check-agents.sh` et sur ce que mesure le nouveau
  gate (D-05).
- ADR-029 porte la trace datée de son passage en enforcement machine.
- La première PR de la phase est complète : le manager de mission peut s'arrêter ici et rendre son
  rapport, la calibration étant un checkpoint bloquant en attente de la Phase 40 (D-06, D-06 bis).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 25-03)

| Artefact | Nature | Détail |
|---|---|---|
| Étape CI `check-instruction-budget (BUDG-01/02, ADR-029 machine-enforced)` | étape neuve du job `gates` | Preuve de discrimination sur fixture (0 / 1 / 3 / 2) + dépôt réel avec branche armée et non armée. |
| Entrée de catalogue `check-instruction-budget.sh` | puce de `plugin/conductor/README.md` | Famille « Gates machine », cinq codes énumérés. |
| Correction de la puce `check-agents.sh` | édition scopée | La mesure de densité ne lui est plus attribuée (D-05). |
| `plugin/conductor/VERSION` = `v1.37.0` | bump minor | Synchrone avec `module.json`. |
| Entrée `## [v1.37.0]` | CHANGELOG conductor | Dit explicitement qu'aucune baseline n'est gravée. |
| Compteurs re-dérivés | `README.md`, `README.fr.md`, titre de section du catalogue conductor | Scripts conductor et suites CI. |
| Note datée sous ADR-029 | `docs/ADR.md` | Enforcement machine à partir de la Phase 25, sentinelle non armée. |
| Entrée BACKLOG « Budget des SKILL.md et du bootstrap » | `.planning/BACKLOG.md` | Dettes différées + piste tokens écartée + remédiation ultérieure. |

*(Aucun symbole d'API externe : `api-coverage` reste `detected: false` sur ce corps de plan.)*
</artifacts>

<output>
Créer `.planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-03-SUMMARY.md`
en fin de plan. Commits en français, avec le bandeau
« arbitrage Samuel, AskUserQuestion session principale, 2026-09-14 » pour D-05 et
« arbitrage Samuel, AskUserQuestion session principale, 2026-09-15 » pour le séquencement D-06 bis.
</output>

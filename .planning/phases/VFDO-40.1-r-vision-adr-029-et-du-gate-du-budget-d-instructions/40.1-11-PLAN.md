---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 11
lot: L6a — validator
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/validator/AGENT.md
  - plugin/validator/README.md
  - plugin/validator/VERSION
  - plugin/validator/module.json
  - plugin/validator/CHANGELOG.md

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01 — le validator énonce la charte qu'il contrôle au plafond de 300 lignes ; plus aucune ligne vivante de l'ancien plafond sous `plugin/validator/`"
    - "Aucun agent réécrit : dans `plugin/validator/AGENT.md`, remplacement de valeur seul ; lignes et instructions mesurées par le gate égales à la baseline, verdict `OK`, `check-agents.sh --strict` vert (ADR-029, ADR-044, D-03 : français conservé)"
    - "La note de dette « validator au plafond » (Claude's Discretion de CONTEXT) est réécrite en fait daté exact sous le nouveau plafond : marge de 50 lignes avant blocage ; la prochaine ligne déclenche l'avertissement non bloquant ; une instruction ajoutée reste bloquée sans arbitrage nommé. La mesure n'est jamais écrite sous la forme littérale de l'ancien plafond. Ferme sur le fond le point 1 de l'item BACKLOG « Dérive documentaire… »"
    - "Bump **patch** sur cible dérivée à l'exécution (`tools/check-module-bump.sh validator patch`)"
  artifacts:
    - path: "plugin/validator/README.md"
      provides: "note de densité datée réécrite sous le plafond 300"
  key_links:
    - "`plugin/validator/AGENT.md` est à la taille d'avertissement moins un : un remplacement de valeur qui ajouterait une ligne le ferait passer en `AVERTISSEMENT-ADR029`, ce que la vérification de ligne de gate détecte"
---

<objective>
Aligner le module validator, qui énonce la charte qu'il contrôle, sur le plafond ADR-029 de 300
lignes (D-01, arbitrage Samuel, AskUserQuestion session principale, relais SendMessage,
2026-09-16), puis le bumper en patch sur cible dérivée. Ce plan est issu de la scission de l'ancien
plan « validator, kpi-analyst, audit-architecture » à la révision 1 (constat W2 : 15 fichiers) ;
kpi-analyst et audit-architecture passent en 40.1-12.

Purpose: BUDG-04 ; fermer le point 1 de l'item BACKLOG « Dérive documentaire… » (README validator
faux sur sa marge).
Output: agent aligné par valeur, README réécrit, module bumpé en patch.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
@.planning/instruction-budget-baselines.tsv
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : validator — agent (valeur seule) et README (note datée réécrite)</name>
  <files>plugin/validator/AGENT.md, plugin/validator/README.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/validator/`
    - plugin/validator/AGENT.md (lignes listées : règle de densité ~L69-70, interdit ~L222)
    - plugin/validator/README.md (en-tête L8-15, « Pourquoi » ~L23, tableau ~L102)
    - .planning/BACKLOG.md (item « Dérive documentaire et fragilité latente… », point 1)
  </read_first>
  <action>
`AGENT.md` : remplacement de valeur seul aux lignes listées (`≤ 300 lignes`, `> 300L`). Aucune ligne
ajoutée ni retirée, aucun marqueur normatif ajouté.

`README.md` :
- Ligne `**Densité**` de l'en-tête (Claude's Discretion « emplacement de la note de dette »), à
  réécrire ainsi : `AGENT.md` a une marge de **50 lignes** sous le plafond ADR-029 révisé en Phase
  40.1 (bloque au-delà de 300, avertissement dès 251 ; arbitrages Samuel D-01/D-05, AskUserQuestion
  session principale, 2026-09-16), mesure du 2026-09-15 par `awk 'END{print NR}'`. La prochaine ligne
  ajoutée déclenche l'avertissement non bloquant du gate `check-instruction-budget.sh` ; une
  instruction ajoutée reste bloquée sans arbitrage nommé (ratchet d'instructions par fichier). Ne pas
  écrire la mesure sous la forme littérale de l'ancien plafond.
- « Pourquoi » : `Agents qui dépassent 300 lignes`.
- Tableau : `densité ADR-029 (agents : bloque au-delà de 300L, avertissement dès 251 ; skills ≤ 500L, bootstrap ≤ 2000 tokens)`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/validator\\// {c++} END{print c+0}" "$T/o"); echo "validator vivants=$n"; [ "$n" -eq 0 ]; g=0; bash plugin/conductor/scripts/check-instruction-budget.sh > "$T/g" 2>&1 || g=$?; echo "gate rc=$g"; [ "$g" -eq 0 ]; awk -F" [|] " "\$1==\"plugin/validator/AGENT.md\"{print \"validator AGENT : lignes=\" \$2 \" bl=\" \$3 \" instr=\" \$4 \" bl=\" \$5 \" verdict=\" \$6; ok=(\$2==\$3 && \$4==\$5 && \$6==\"OK\")} END{exit ok?0:1}" "$T/g"; bash plugin/conductor/scripts/check-agents.sh --strict --file plugin/validator/AGENT.md > /dev/null; d=$(awk "/50 lignes/ && /dès 251/{c++} END{print c+0}" plugin/validator/README.md); echo "note_densite=$d"; [ "$d" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou `validator vivants=` différent de 0 ; ou `gate rc=` différent de 0 ; ou la ligne `validator AGENT :` hors baseline ou verdict autre que `OK` ; ou check-agents rouge ; ou `note_densite=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `plugin/validator/` ; `README.md` contient `50 lignes` et `dès 251` sur la ligne Densité
    - `plugin/validator/AGENT.md` : lignes = baseline, instructions = baseline, verdict `OK` ; `git diff --numstat 4e7c8e4 -- plugin/validator/AGENT.md` : ajoutées = retirées
    - `check-agents.sh --strict --file plugin/validator/AGENT.md` rc 0
  </acceptance_criteria>
  <done>Le validator énonce et documente le plafond 300 sans que son agent ait été réécrit.</done>
</task>

<task type="auto">
  <name>Task 2 : bump patch dérivé du validator</name>
  <files>plugin/validator/VERSION, plugin/validator/module.json, plugin/validator/README.md, plugin/validator/CHANGELOG.md</files>
  <read_first>
    - plugin/validator/VERSION (courante), plugin/validator/CHANGELOG.md, plugin/validator/README.md (`**Version**`)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh
  </read_first>
  <action>
Bump **patch** dérivé de la VERSION lue à l'exécution (`VERSION`, `module.json`, `**Version**`), en
UN commit au sujet `chore(40.1-11): …`. Entrée CHANGELOG en tête,
`## [<cible>] — <date d'exécution> (Phase 40.1)` :
- charte de densité contrôlée relevée à 300 lignes ;
- note de marge du README réécrite ;
- autorisation : arbitrages Samuel D-01/D-05, AskUserQuestion session principale (relais
  SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh validator patch</automated>
    <fails_when>exit non nul, ou absence de `cible derivee validator :`, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
  </verify>
  <acceptance_criteria>
    - `check-module-bump.sh validator patch` rc 0 ; version-sync rc 0 ; entrée CHANGELOG avec canal et date
  </acceptance_criteria>
  <done>validator bumpé en patch sur cible dérivée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| édition d'un fichier d'agent → gate armé | toute ligne ou instruction ajoutée change le verdict du gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-26 | Tampering | réécriture d'agent sous couvert d'alignement | medium | mitigate | lignes et instructions égales à la baseline ; numstat équilibré ; invariant I2 à la clôture |
| T-40.1-27 | Repudiation | README validator affirmant une marge fausse | low | mitigate | note réécrite en fait daté (marge 50 sous 300) ; recensement nul |
</threat_model>

<verification>
Recensement nul sous `plugin/validator/` ; agent égal à sa baseline ; check-agents vert ; bump dérivé vérifié.
</verification>

<success_criteria>
Le validator est aligné sur 300, sans agent réécrit, et bumpé en patch.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs, note de densité du README validator, bump patch.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-11-SUMMARY.md` when done
</output>

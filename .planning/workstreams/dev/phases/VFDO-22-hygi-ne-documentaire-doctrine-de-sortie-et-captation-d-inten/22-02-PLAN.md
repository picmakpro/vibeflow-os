---
phase: 22-hygiene-documentaire
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/design-orchestrator/agents/vf-design-manager.md
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
autonomous: false
requirements: [DOCF-05, DOCF-06]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`vf-dev-manager` pose UN nœud `docs` agrégé en fin de mission, `deps` = tous les nœuds d'exécution, quand au moins un des quatre déclencheurs tombe — et aucun nœud si aucun ne tombe, ce qui est un état normal (D-07, D-08)."
    - "`vf-design-manager` applique la même doctrine et les mêmes déclencheurs qu'en mission de dev : une refonte complète périme ARCHITECTURE/README aussi sûrement qu'un refactor (D-11)."
    - "Le gate `DESIGN.md` du manager design reste distinct et inchangé — la bible visuelle n'est pas de la doc produit (D-11)."
    - "Le nœud `docs` n'introduit aucun format de rapport nouveau : `passed` / `gaps_found` / `action: ask-user` du contrat existant suffisent (D-09)."
    - "Retirer un renvoi vers `docs-flow.md` dans l'un des deux managers fait échouer la suite (D-14)."
  artifacts:
    - "section §Hygiène documentaire de plugin/dev-orchestrator/agents/vf-dev-manager.md réécrite"
    - "section §Hygiène documentaire de plugin/design-orchestrator/agents/vf-design-manager.md (création)"
    - "bloc T23 dans plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
  key_links:
    - "`vf-dev-manager.md` ≤ 250 lignes — T8 fait échouer la suite au-delà ; la section part de 217 lignes avec 33 de marge (ADR-029)."
    - "résolution de `$DESIGNMGR` dans le test : `$REPO/design-orchestrator/agents/` en dépôt source, `$MOD/agents/` en lab installé, `skip` si le module design est absent — sans quoi la suite casse hors dépôt source."
    - "renvoi cross-module `dev-orchestrator-references/docs-flow.md` depuis le module design — précédent exact : `mission-cross-team.md` déjà cité par `vf-design-manager.md` ligne 119."
---

<objective>
Doter les **deux managers de mission** du geste documentaire : quand poser un nœud `docs`, sous
quelle forme, et sous quel régime — sans recopier la doctrine, en y renvoyant.

Purpose : lacune 3 du cadrage. `vf-dev-manager` porte trois puces d'hygiène et une seule mention
outillée ; il n'existe aucune règle « à ce moment du cycle → cette brique, à cette condition ».
`vf-design-manager` n'a **aucun** geste documentaire. Une phase qui ne livrerait que la voie
conversationnelle (plan 22-01) ne répondrait qu'à la moitié de la demande — Samuel : « le but est
que dev manager / design manager puisse avoir ce workflow explicite quand il est nommé, MAIS AUSSI
que l'on capte les intentions de l'user ».

Output : les deux managers portent les quatre déclencheurs et la forme du nœud agrégé ; le bloc
`T23` rend le câblage non-régressable ; un checkpoint humain valide la surface doctrinale complète.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-CONTEXT.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-PATTERNS.md
@.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 22-02)

**Nouvelles sections**
- `plugin/design-orchestrator/agents/vf-design-manager.md` : `## Hygiène documentaire` (création,
  insérée avant `## Fin de mission`)

**Sections réécrites**
- `plugin/dev-orchestrator/agents/vf-dev-manager.md` : `## Hygiène documentaire & next steps
  (rôle actif)` — les trois puces actuelles sont **remplacées**, jamais complétées (ADR-029)

**Nouvelles assertions de test** (`test-dev-orchestrator.sh`)
- `T23` — câblage du geste documentaire dans les deux managers : renvoi `docs-flow.md`, quatre
  déclencheurs nommés, forme du nœud agrégé, régime autonome ; résolution `$DESIGNMGR` à deux
  branches avec `skip` si le module design est absent

**Nouveau vocabulaire de nœud DAG**
- identifiant de nœud `docs`, étape libellée « hygiène documentaire », `--deps` = tous les nœuds
  d'exécution de la mission
</artifacts_produced>

<decisions_honored>
## Décisions verrouillées couvertes par ce plan

| Décision | Où elle est implémentée |
|---|---|
| **D-07** (UN nœud agrégé en fin de mission, deps = tous les nœuds d'exécution) | tâche 1 |
| **D-08** (quatre déclencheurs, chacun un FAIT constatable) | tâches 1 et 2 |
| **D-09** (régime du nœud, aucun format de rapport nouveau) | tâches 1 et 2 |
| **D-11** (même doctrine côté design, gate `DESIGN.md` inchangé, frontmatter intouché) | tâche 2 |
| **D-13** (`check-doc-drift.sh` inchangé — consommé, jamais enrichi) | tâches 1 et 3 |
| **D-14** (extension de la suite existante, jamais une suite nouvelle) | tâche 3 |

## Arbitrage de forme tranché au plan (Claude's Discretion, CONTEXT.md)

**La table des quatre déclencheurs n'existe qu'une fois — dans `docs-flow.md` §Déclencheurs.**
Les deux managers portent les **noms** des quatre déclencheurs, la forme du nœud et un renvoi ;
la colonne « Constat » (le FAIT exact) reste dans la doctrine. Trois copies auraient divergé —
c'est exactement le motif corrigé le 2026-07-31 (WINDOWS #1, `team-kernel.md` : « quatre copies
auraient divergé »). Le précédent de forme existe déjà dans `vf-dev-manager.md` lignes 115-119 :
la revue y porte la règle compacte inline **et** renvoie à `mission-flow.md` §Pattern E avec la
consigne « ne pas le reformuler ici ». On applique ce patron à l'identique.
</decisions_honored>

<tasks>

<task type="auto">
  <name>Tâche 1 : `vf-dev-manager` — les quatre déclencheurs et le nœud `docs` agrégé</name>

  <files>plugin/dev-orchestrator/agents/vf-dev-manager.md</files>

  <read_first>
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` **en entier** — 217 lignes sur un plafond
      de 250 (ADR-029, vérifié par T8). Repérer en particulier : §Hygiène documentaire & next steps
      (lignes 200-208, les trois puces à remplacer), §Orchestration par étape point 2 « Revue »
      (lignes 115-119, **le patron de forme** : règle compacte inline + renvoi + « ne pas le
      reformuler ici »), §Étage design croisé lignes 149-150 (**le format exact du renvoi** à une
      doctrine externe), §Contrôle de flux lignes 152-163 (le contrat de rapport typé déjà en place).
    - `plugin/dev-orchestrator/references/docs-flow.md` §Déclencheurs et §Garde-fous — la doctrine
      posée par le plan 22-01, à citer, jamais à recopier.
    - `plugin/dev-orchestrator/references/mission-flow.md` §Pattern B (vocabulaire `dag.sh add
      --deps=`) et §Résolution des scripts (la forme `"$S"/`).
    - `.planning/phases/VFDO-22-.../22-CONTEXT.md` §decisions D-07, D-08, D-09.
  </read_first>

  <action>
**Remplacer intégralement** le corps de §Hygiène documentaire & next steps (rôle actif) — les trois
puces actuelles disparaissent, elles ne sont pas complétées. Le budget de densité est la contrainte
dimensionnante de la tâche : le fichier part de 217 lignes, le plafond machine est 250, la cible de
ce plan est **≤ 235**.

Le nouveau corps porte cinq points, dans cet ordre :

1. **Fin d'étape** — conserver l'acquis : vérifier que la machinerie a mis à jour `STATE` et
   `ROADMAP`, le faire sinon ; une décision structurante prise en mission est consignée (STATE
   `### Decisions` ou registre du lab). Cette puce ne change pas de sens, seulement de voisinage.

2. **Le nœud `docs`, agrégé, en fin de mission** (D-07). Une puce qui dit : un **seul** nœud pour
   toute la mission, jamais un par étape — un nœud par étape documenterait des états intermédiaires
   déjà périmés à l'étape suivante et re-traiterait les mêmes fichiers à chaque tour ; ses `deps`
   sont **tous** les nœuds d'exécution de la mission, de sorte que le coût réel du moteur (jusqu'à
   neuf rédacteurs de doc plus leurs vérificateurs, en vagues) soit payé une fois, sur l'état final.
   Donner la forme d'appel exacte en `code` inline, avec la résolution `"$S"/` déjà en vigueur dans
   le fichier : l'identifiant de nœud est `docs`, l'étape est libellée « hygiène documentaire », et
   `--deps` énumère les nœuds d'exécution. Ne pas ouvrir de bloc de code fencé : le fichier
   n'emploie nulle part cette forme dans ses sections de doctrine.

3. **Les quatre déclencheurs, nommés, sur une puce compacte** (D-08) : surface publique touchée ·
   `[doc-drift]` actif · fin de milestone · nouveau module ou nouvelle capacité. Dire que le nœud
   est posé dès qu'**au moins un** tombe, et qu'aucun ne tombe est un **état normal, pas un
   manque**. Ne pas recopier la colonne « Constat » de la doctrine (voir l'arbitrage de forme
   ci-dessus) : clore la puce par un renvoi au format exact des lignes 149-150 du fichier —
   doctrine complète dans `dev-orchestrator-references/docs-flow.md` §Déclencheurs et §Garde-fous,
   ne pas les reformuler ici.

4. **Le régime, en une puce** (D-03/D-04/D-09) : en mode superviser, le nœud peut proposer la
   génération au checkpoint ; en mode **autonome**, il se limite à l'audit read-only et au constat
   porté au rapport — la doc périmée est tracée, jamais corrigée en douce. Rappeler que le nœud
   n'introduit **aucun format de rapport nouveau** : `passed` si la doc est jugée à jour,
   `gaps_found` avec les docs périmées en `findings`, et `action: ask-user` sur toute génération à
   confirmer — le contrat typé déjà décrit en §Contrôle de flux couvre le cas. Et la ligne rouge :
   le flag de régénération destructive n'est **jamais** employé depuis une mission, quel que soit le
   mode — son déclencheur vient de l'utilisateur, en direct.

5. **Fin de mission** — conserver l'acquis : proposer LE next step depuis la feuille de route
   (étape suivante, recette en attente, milestone à clore), une proposition ferme, pas un menu.

Ne toucher ni au frontmatter (l'allowlist `Agent(...)` est vérifiée nom par nom par T18 et contient
déjà `gsd-doc-writer`, `gsd-doc-verifier`, `gsd-doc-classifier`, `gsd-doc-synthesizer` — le nœud
`docs` n'a besoin d'aucun agent nouveau), ni à §Orchestration par étape, ni à §Étage design croisé,
ni à §Contrôle de flux. Français, ton du fichier conservé.
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&amp;1 | tail -3; L=$(wc -l &lt; plugin/dev-orchestrator/agents/vf-dev-manager.md); echo "lignes=$L"; [ "$L" -le 235 ] &amp;&amp; echo DENSITE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` ≤ 235 (cible du plan ; T8 fait échouer la suite au-delà de 250).
    - `vf-dev-manager.md` contient `docs-flow.md`.
    - `vf-dev-manager.md` contient `--id=docs` et `hygiène documentaire`.
    - Les quatre déclencheurs sont nommés : `vf-dev-manager.md` contient `doc-drift`, `milestone`, `surface publique`, et `capacité`.
    - `vf-dev-manager.md` contient `gaps_found` et `ask-user` (le contrat de rapport typé est réutilisé, pas réinventé).
    - Les trois puces d'origine ont bien été **remplacées** : `grep -c 'Drift doc détecté' plugin/dev-orchestrator/agents/vf-dev-manager.md` retourne `0`.
    - Le frontmatter est intact : `git diff HEAD -- plugin/dev-orchestrator/agents/vf-dev-manager.md | grep -c '^[+-]tools:'` retourne `0`.
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0 avec `✓ T8 agents` et `✓ T18 cloisonnement` au vert.
  </acceptance_criteria>

  <done>
    §Hygiène documentaire de `vf-dev-manager` porte la forme du nœud `docs` agrégé, les quatre
    déclencheurs nommés, le régime superviser/autonome, la ligne rouge de la régénération, et un
    renvoi à la doctrine — sans recopier sa table. Le fichier tient sous 235 lignes et son
    frontmatter est inchangé.
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : `vf-design-manager` — même doctrine, même déclencheurs, renvoi cross-module</name>

  <files>plugin/design-orchestrator/agents/vf-design-manager.md</files>

  <read_first>
    - `plugin/design-orchestrator/agents/vf-design-manager.md` **en entier** — 161 lignes, marge de
      89 sous le plafond ADR-029. Repérer : la ligne 119 (§Étage implémentation croisée) qui cite
      déjà `dev-orchestrator-references/mission-cross-team.md` — **le précédent exact** du renvoi
      cross-module que D-01 institue ; §Fin de mission (lignes 152-161), point d'ancrage de la
      nouvelle section ; le frontmatter ligne 4 (`tools:` porte déjà `Skill` — **ne pas y toucher**).
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` §Hygiène documentaire, état laissé par la
      tâche 1 — la section design doit lui être **symétrique en forme**, pour ne pas diverger
      doctrinalement.
    - `plugin/dev-orchestrator/references/docs-flow.md` §Déclencheurs et §Garde-fous.
    - `.planning/phases/VFDO-22-.../22-CONTEXT.md` §decisions D-11.
  </read_first>

  <action>
Créer une section `## Hygiène documentaire` **avant** `## Fin de mission`, symétrique en forme à
celle posée en tâche 1 côté dev, plus verbeuse si utile (la marge de densité est large) mais jamais
divergente sur le fond.

Elle porte : la justification en une phrase — une refonte complète modifie la **surface visible** et
périme ARCHITECTURE et README aussi sûrement qu'un refactor, donc le geste documentaire s'applique
à l'identique en mission design ; les **mêmes** quatre déclencheurs nommés que côté dev (surface
publique touchée · `[doc-drift]` actif · fin de milestone · nouveau module ou nouvelle capacité),
avec la même règle « au moins un tombe → un nœud ; aucun ne tombe → état normal » ; le **même**
nœud `docs` agrégé en fin de mission, `deps` = tous les nœuds d'exécution (côté design, les nœuds
de craft et de critique de tous les écrans) — un seul nœud, jamais un par écran ; le **même**
régime superviser / autonome, avec l'audit read-only pour seul geste en autonome ; et le renvoi
cross-module au format exact de la ligne 119 du fichier, vers
`dev-orchestrator-references/docs-flow.md` §Déclencheurs et §Garde-fous — le module design
**n'héberge aucune copie** de la doctrine (D-01, ADR-057).

Poser explicitement la frontière avec le gate `DESIGN.md` (D-11) : le gate `DESIGN.md` reste
**inchangé et distinct** — la bible visuelle n'est pas de la doc produit ; modifier `DESIGN.md`
reste un nœud dédié et proposé, comme le dit déjà §Garanties, et ne se confond jamais avec le
nœud `docs`.

**Ne pas modifier le frontmatter** (vérifié au cadrage : `Skill` est déjà dans `tools:` ligne 4).
Ne toucher ni à §Définition du « vert » design, ni à §Orchestration par écran, ni à §Contrôle de
flux, ni à §Garanties. Français, ton du fichier conservé.
  </action>

  <verify>
    <automated>bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh 2>&amp;1 | tail -3; bash plugin/conductor/scripts/check-agents.sh --strict plugin/design-orchestrator/agents; echo "AGENTS_EXIT=$?"; L=$(wc -l &lt; plugin/design-orchestrator/agents/vf-design-manager.md); echo "lignes=$L"; [ "$L" -le 250 ] &amp;&amp; echo DENSITE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `vf-design-manager.md` contient la section `## Hygiène documentaire` : `grep -c '^## Hygiène documentaire' plugin/design-orchestrator/agents/vf-design-manager.md` retourne `1`.
    - Cette section précède `## Fin de mission` : `grep -n '^## ' plugin/design-orchestrator/agents/vf-design-manager.md` fait apparaître `## Hygiène documentaire` avec un numéro de ligne inférieur à celui de `## Fin de mission`.
    - `vf-design-manager.md` contient `dev-orchestrator-references/docs-flow.md` (renvoi cross-module, aucune copie locale).
    - Les quatre déclencheurs sont nommés : le fichier contient `doc-drift`, `milestone`, `surface publique`, `capacité`.
    - `vf-design-manager.md` contient `--id=docs`.
    - Le frontmatter est intact : `git diff HEAD -- plugin/design-orchestrator/agents/vf-design-manager.md | grep -c '^[+-]tools:'` retourne `0`.
    - Aucune copie locale de la doctrine : `ls plugin/design-orchestrator/references/docs-flow.md` échoue (fichier absent).
    - `wc -l < plugin/design-orchestrator/agents/vf-design-manager.md` ≤ 250.
    - `bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh` sort en exit 0.
    - `bash plugin/conductor/scripts/check-agents.sh --strict plugin/design-orchestrator/agents` sort en exit 0.
  </acceptance_criteria>

  <done>
    `vf-design-manager` porte le même geste documentaire que son homologue dev, par renvoi vers la
    doctrine hébergée dans `dev-orchestrator` — aucune copie locale. Le gate `DESIGN.md` reste
    distinct et le frontmatter intouché. La suite du module design est verte.
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : bloc T23 — le câblage des deux managers devient non-régressable</name>

  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</files>

  <read_first>
    - `plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` lignes 85-140 (résolution
      `MOD` / `REPO` / `REFS_DIR` / `AGENT_FILE` / `ROUTING`, détection de disposition source vs lab
      installé, helpers `ok()` / `ko()` / `skip()`), lignes 1274-1291 (bloc **T17** — le moule de
      vérification de câblage à décliner), lignes 1293-1341 (T18/T18b — la manière dont `$DEVMGR`
      est résolu et testé), et la fin du fichier (la ligne de résultat, devant laquelle insérer).
    - `plugin/dev-orchestrator/agents/vf-dev-manager.md` et
      `plugin/design-orchestrator/agents/vf-design-manager.md`, états laissés par les tâches 1 et 2 —
      les motifs assertés doivent exister avant d'être testés.
    - `.planning/phases/VFDO-22-.../22-PATTERNS.md` §`test-dev-orchestrator.sh` (idiomes `tNN_ok`) et
      §No Analog Found (la résolution de `$DESIGNMGR`, seule zone sans précédent mécanique).
  </read_first>

  <action>
Ajouter un bloc `T23` en fin de fichier, **avant** la ligne de résultat, écrit sur le moule exact de
T17 : un compteur booléen local `t23_ok=1`, une série de gardes `… || { ko "…"; t23_ok=0; }`, puis
`[ "$t23_ok" -eq 1 ] && ok "…"` en clôture. Jamais d'`exit` immédiat : le compteur global agrège en
fin de script. Numérotation : `T22` est pris par le plan 22-01, `T19` à `T21` par les phases 16 et
17 — le bloc est bien `T23`.

**Résolution de la cible design — la seule zone du fichier sans précédent** (le script ne référence
aujourd'hui que `dev-orchestrator`). Deux branches, dans cet ordre, puis un repli :
`$REPO/design-orchestrator/agents/vf-design-manager.md` en disposition dépôt source (`$REPO` est
déjà le dossier `plugin/`) ; à défaut `$MOD/agents/vf-design-manager.md` en lab installé, où les
agents sont posés à plat ; si aucun des deux n'existe, le module design n'est pas dans le périmètre
scanné — émettre un `skip` explicite et **ne pas** faire échouer la suite, exactement comme T6
traite l'installeur absent. L'ordre des branches compte : en dépôt source, `$MOD/agents/` est le
dossier d'agents de `dev-orchestrator` et ne contient pas le manager design.

Assertions du bloc, chacune sur son propre garde avec un message `ko` qui nomme le fichier fautif :

- **côté dev** (`$DEVMGR`, déjà résolu plus haut dans le fichier par T18) : le renvoi `docs-flow`
  est présent ; l'identifiant de nœud `--id=docs` est présent ; les quatre déclencheurs sont nommés
  — `doc-drift`, `milestone`, `surface publique`, `capacité`, testés **un par un**, jamais par un
  grep global qu'un seul motif satisferait ;
- **côté design** (`$DESIGNMGR`, si résolu) : le renvoi vers `dev-orchestrator-references/docs-flow.md`
  est présent ; `--id=docs` est présent ; les quatre mêmes déclencheurs, testés un par un ;
- **absence de copie locale** (D-01/ADR-057) : le chemin
  `$REPO/design-orchestrator/references/docs-flow.md` ne doit **pas** exister en disposition dépôt
  source — la doctrine vit dans un seul module. Garder cette assertion sous la même condition de
  disposition que la résolution de `$DESIGNMGR`, pour ne pas produire de faux vert en lab installé
  où l'arborescence est aplatie.

Ajouter enfin, dans le même bloc, la garde D-13 : `check-doc-drift.sh` est **consommé** par la
doctrine et jamais réimplémenté — asserter que le fichier
`$MOD/scripts/check-doc-drift.sh` existe et que le manager dev ne le réimplémente pas, en
vérifiant simplement qu'il est nommé dans `docs-flow.md` (la doctrine l'interprète) et non dans une
définition de fonction du manager.

Commenter en tête du bloc, en français, ce que T23 garantit et ce qu'il ne garantit pas : il prouve
une **présence textuelle** de câblage, pas un comportement d'agent à l'exécution — même réserve que
celle déjà inscrite pour BRDG-03 dans `REQUIREMENTS.md`. Cette phrase existe pour qu'un lecteur
futur ne confonde pas ce vert avec une preuve de comportement (précédent Phase 19 : un compte rendu
qui prouve une présence n'est pas un comportement prouvé).
  </action>

  <verify>
    <automated>bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh 2>&amp;1 | tee /tmp/vfdo22-t23.log | tail -3; grep -q '✓ T23' /tmp/vfdo22-t23.log &amp;&amp; grep -q '✓ T22' /tmp/vfdo22-t23.log &amp;&amp; grep -q '0 KO' /tmp/vfdo22-t23.log &amp;&amp; echo T23_OK</automated>
  </verify>

  <acceptance_criteria>
    - `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0 ; sa sortie contient `✓ T23` et `✓ T22`, et se termine sur `0 KO`.
    - Le bloc est discriminant, prouvé par **mutation** : retirer temporairement la chaîne `docs-flow` de `plugin/design-orchestrator/agents/vf-design-manager.md` fait passer la suite à au moins `1 KO` avec un message contenant `T23` ; restaurer le fichier ensuite (`git checkout --`) et re-vérifier `0 KO`.
    - `grep -c 'T23' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` ≥ 6 (compteur, gardes, message de clôture).
    - Le test reste portable hors dépôt source : `grep -c 'design-orchestrator' plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` ≥ 1 **et** le bloc T23 contient un appel à `skip` (pas de `ko`) pour le cas du module design absent.
    - Aucune suite nouvelle : `find plugin scripts -path '*/tests/test-*.sh' | wc -l` retourne `44`.
    - `git diff --quiet HEAD -- plugin/dev-orchestrator/scripts/check-doc-drift.sh` sort en exit 0 (D-13).
  </acceptance_criteria>

  <done>
    Le bloc T23 existe, il est vert, et il est **discriminant** : la preuve par mutation a été faite
    et consignée dans le SUMMARY (quel motif retiré, quel `ko` obtenu). Le module design absent
    produit un `skip`, jamais un `ko`. Le dépôt compte toujours 44 suites.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint : Samuel valide la surface doctrinale — en particulier la formulation de `--force` (D-05/D-06)</name>
  <what-built>
    La surface doctrinale complète de la Phase 22 est posée et verte à la machine :
    `plugin/dev-orchestrator/references/docs-flow.md` (les quatre familles, les trois régimes de
    confirmation, la table canonique des quatre déclencheurs, les garde-fous, les interdits),
    `intent-routing.md` enrichie des formulations réelles, `AGENT.md` câblée au chemin d'install D7,
    et les deux managers dotés du nœud `docs` agrégé par renvoi vers la doctrine.

    Ce que la machine a prouvé : la présence textuelle, l'installation réelle de la référence par
    l'engine, la non-régression du routage, la densité sous plafond. Ce qu'elle **ne peut pas**
    prouver : que la doctrine dit ce que Samuel a tranché. C'est l'objet de ce point d'arrêt.
  </what-built>
  <how-to-verify>
    1. Lire `plugin/dev-orchestrator/references/docs-flow.md` **en entier** (≈ 110 lignes) et
       vérifier trois choses :
       - **D-05/D-06** — la formulation de `--force` correspond bien à l'arbitrage rendu : le flag
         est **autorisé sur intention explicite de l'utilisateur** (Samuel a tranché CONTRE la
         recommandation d'interdiction), et sa contrepartie est le garde-fou en trois temps
         (reformulation nominative de ce qui sera écrasé, attente d'un oui explicite, interdiction
         en mission d'équipe et en mode autonome). Si la doctrine a glissé vers une interdiction
         générale, c'est une régression de la décision, pas une prudence.
       - **D-02** — la famille « entrée » est bien traitée par un **renvoi en prose** vers
         `ingestion-flow.md`, sans redite de la découverte, du manifest, des garde-fous BRDG-03 ni
         du gate BLOCKER.
       - **la frontière `vibeflow-os`** — l'interdit de lancer `gsd-docs-update` sur ce dépôt est
         présent et compréhensible pour un agent qui ne connaîtrait pas l'historique.
    2. Lire les sections §Hygiène documentaire des deux managers
       (`plugin/dev-orchestrator/agents/vf-dev-manager.md` et
       `plugin/design-orchestrator/agents/vf-design-manager.md`) et confirmer qu'elles renvoient à
       la doctrine **sans la recopier** — l'arbitrage de forme du plan tient ou doit être révisé.
    3. Rejouer si souhaité : `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh`
       (attendu : exit 0, `0 KO`, `T22` et `T23` au vert).
  </how-to-verify>
  <resume-signal>
    Répondre « approuvé » pour enchaîner sur le plan 22-03 (bump des deux modules), ou décrire les
    écarts constatés section par section.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager de mission → DAG / workers | le manager pose un nœud qui déclenchera une écriture de doc ; en mode autonome, aucun humain ne regarde au moment du dispatch |
| mission autonome → dépôt | `gsd-docs-update` écrit jusqu'à 9 fichiers **et commite** — pendant une mission de nuit, l'écriture est indiscernable du travail voulu |
| module design → doctrine hébergée dans le module dev | renvoi cross-module ; si le module dev n'est pas installé, le renvoi pointe dans le vide |

> Surface honnête : ce plan ne livre **aucun code exécutable nouveau**. Il édite deux définitions
> d'agents (prose + frontmatter intouché) et ajoute des assertions `grep` dans une suite bash
> existante. Le risque est doctrinal, pas technique : une règle mal écrite autorise une écriture
> non voulue en autonomie.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-06 | Tampering | nœud `docs` posé par un manager en mode autonome | high | mitigate | D-04/D-09 écrits dans les deux managers (tâches 1 et 2) : en autonome, le nœud se limite à l'audit read-only et au constat porté au rapport ; toute génération remonte en `action: ask-user`. La ligne rouge de la régénération destructive — jamais depuis une mission, quel que soit le mode — est écrite dans les deux managers et assertée par T23. |
| T-22-07 | Elevation of Privilege | allowlist `Agent(...)` du manager dev | medium | accept | Fait constaté : les quatre agents rédacteurs de doc (`gsd-doc-writer`, `gsd-doc-verifier`, `gsd-doc-classifier`, `gsd-doc-synthesizer`) sont **déjà** dans l'allowlist (ligne 4, vérifiée nom par nom par T18). Le nœud `docs` n'élargit aucun privilège ; aucune modification de frontmatter n'est faite, et l'absence de modification est une condition d'acceptation des tâches 1 et 2. |
| T-22-08 | Repudiation | renvoi cross-module design → dev pointant dans le vide | low | accept | Le module `design-orchestrator` déclare `requires: ["conductor"]` et `dev-orchestrator` déclare `requires: [..., "design-orchestrator"]` — un lab peut donc avoir design sans dev. Le précédent identique existe déjà depuis la Phase 15 (`mission-cross-team.md` cité ligne 119) et n'a produit aucun incident ; corriger le graphe de dépendances déborderait le périmètre de la phase. Consigné comme dette connue, non traitée ici. |
| T-22-09 | Spoofing | T23 pris pour une preuve de comportement | medium | mitigate | Commentaire en tête de bloc, exigé par la tâche 3 : T23 prouve une **présence textuelle**, pas un comportement d'agent — même réserve que BRDG-03 dans `REQUIREMENTS.md`. Contre-poison du précédent Phase 19 : la discriminance du bloc est prouvée **par mutation** et le résultat consigné au SUMMARY (condition d'acceptation de la tâche 3). |
| T-22-SC | Tampering | chaîne d'approvisionnement (npm / pip / cargo) | low | accept | Fait constaté : ce plan n'installe aucun paquet. `files_modified` ne contient que deux `.md` et un `.sh` déjà versionnés ; aucune tâche n'invoque de gestionnaire de paquets. Le protocole d'audit de légitimité n'a aucun paquet à auditer. |

**Contributions `plan:pre`** — détecteurs exécutés au moment du plan, résultats identiques à ceux du
plan 22-01 : `api-coverage` → `detected: false` ; `assumption-delta` → `detected: false` ;
`schema-gate` → aucun ORM dans ce dépôt, saut silencieux ; `security` → ASVS L1, blocage sur `high`,
la seule menace `high` (T-22-06) est en `mitigate`.
</threat_model>

<verification>
- `bash plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh` sort en exit 0, avec
  `T8 agents`, `T18 cloisonnement`, `T22` et `T23` au vert et `0 KO`.
- `bash plugin/design-orchestrator/scripts/tests/test-design-orchestrator.sh` sort en exit 0.
- `bash plugin/conductor/scripts/check-agents.sh --strict plugin/dev-orchestrator/agents` et
  `… plugin/design-orchestrator/agents` sortent tous deux en exit 0 (ADR-044 ; aucun frontmatter
  n'est modifié, la garde est une non-régression).
- Discriminance de T23 prouvée par mutation et consignée dans `22-02-SUMMARY.md` — jamais par
  relecture (précédent Phase 19 : trois étages de vérification ont laissé passer un garde-fou inerte,
  seule la mutation du bloc livré l'a vu).
- `find plugin scripts -path '*/tests/test-*.sh' | wc -l` retourne `44` — aucune suite nouvelle.
</verification>

<success_criteria>
1. `vf-dev-manager.md` porte le nœud `docs` agrégé, les quatre déclencheurs nommés, le régime
   superviser/autonome et un renvoi à `docs-flow.md` — et tient sous 235 lignes.
2. `vf-design-manager.md` porte la même doctrine par renvoi cross-module, sans copie locale, avec
   le gate `DESIGN.md` explicitement laissé distinct.
3. Aucun frontmatter d'agent n'est modifié dans l'un ou l'autre module.
4. Le bloc T23 est vert **et discriminant** — la mutation qui doit le faire échouer le fait échouer.
5. Le module design absent du périmètre scanné produit un `skip`, jamais un `ko`.
6. Les deux suites de module et les deux gates `check-agents.sh` sont verts ; le dépôt compte
   toujours 44 suites.
7. Le checkpoint humain est levé : Samuel a confirmé que la formulation de `--force` respecte D-05
   et D-06, et que la famille « entrée » est un renvoi et non une copie.
</success_criteria>

<output>
Créer `.planning/phases/VFDO-22-hygi-ne-documentaire-doctrine-de-sortie-et-captation-d-inten/22-02-SUMMARY.md` en fin de plan, en y consignant nommément la mutation employée pour prouver la discriminance de T23 et le `ko` obtenu.
</output>

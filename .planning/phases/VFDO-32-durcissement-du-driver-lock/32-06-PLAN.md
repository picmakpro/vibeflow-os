---
phase: 32-durcissement-du-driver-lock
plan: 06
type: execute
wave: 5
depends_on: [32-01, 32-02, 32-03, 32-04]
files_modified:
  - plugin/conductor/VERSION
  - plugin/conductor/CHANGELOG.md
  - plugin/conductor/README.md
  - .planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03, LOCK-04, LOCK-05, QUAL-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La DECOUVERTE COMPLETE des suites — le pattern exact de la CI sur les racines plugin et scripts, pas un sous-ensemble — tourne sur l'arbre COMMITE et rend un bilan sans echec"
    - "Le compte de suites est RE-DERIVE a l'execution, jamais fige depuis ce plan (62 avant la phase ; la valeur d'arrivee depend des plans reellement executes)"
    - "Le module conductor porte une VERSION et un CHANGELOG qui decrivent ce que la phase a reellement livre — un seul plan ecrit ces fichiers, pour qu'aucune vague ne les touche en parallele"
    - "L'etat REEL de QUAL-01 est consigne : soit la boucle marqueur -> lecteur est fermee (plan 32-05 livre), soit la dette est TRACEE explicitement (plan 32-05 coupe) — jamais un silence qui laisserait croire couvert"
    - "Les cinq criteres de succes de la Phase 32 sont verifies un par un, par execution, avec la commande qui les prouve"
  artifacts:
    - plugin/conductor/VERSION
    - "plugin/conductor/CHANGELOG.md : une entree decrivant la phase"
    - "plugin/conductor/README.md : familles de scripts, entrees de hooks et compte de suites du module remis a l'heure"
    - ".planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md"
  key_links:
    - "decouverte CI (find sur plugin et scripts) -> bilan de cloture : c'est la SEULE forme qui prouve la non-regression du parc entier ; une execution des seules suites touchees ne prouve rien sur les 60 autres"
    - "presence de check-guard-health.sh -> branche du reliquat : le fait machine qui decide si QUAL-01 est clos ou en dette, jamais une appreciation"
  prohibitions:
    - "Aucun geste de release : ne toucher NI le fichier VERSION racine, NI le manifeste du plugin, NI le manifeste de la marketplace, NI un tag, NI une PR, NI une publication — gestes humains gates (ADR-031), hors perimetre de cette phase"
    - "Ne jamais figer un compte de suites dans ce plan : toute valeur citee doit etre re-derivee a l'execution"
    - "Ne pas executer un sous-ensemble de suites au motif que les autres n'ont pas ete touchees"
---

<objective>
Cloturer la phase par les trois gestes qu'aucun autre plan ne peut porter sans collision de vague :
le bilan du PARC ENTIER sur l'arbre commite, la version et le journal du module, et la consignation
de l'etat reel de QUAL-01 — clos ou en dette.

Purpose: chaque plan de la phase a prouve SON perimetre. Aucun n'a prouve les soixante autres
suites. Et `VERSION`, `CHANGELOG.md` et `README.md` du module sont ecrits par tous et par personne :
les confier a un plan unique, en derniere vague, est ce qui permet aux vagues 2 a 4 de tourner en
parallele sans se marcher dessus.

Output: un bilan de decouverte complete, la version du module, et un document de reliquats qui dit
la verite sur ce qui a ete livre et sur ce qui ne l'a pas ete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-01-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-02-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-03-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-04-SUMMARY.md
@plugin/conductor/README.md
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : bilan de la decouverte COMPLETE des suites sur l'arbre commite, plus les gates transverses</name>
  <files>.planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md</files>
  <read_first>
    - .github/workflows/ci.yml:210-237 (le pattern de decouverte EXACT, ses deux racines, sa garde anti-vert-a-vide, et la boucle qui compte les echecs — c'est cette forme-la qu'il faut rejouer, pas une approximation)
    - scripts/check-version-sync.sh (les points gates, dont le compte de suites cite par les deux README)
    - scripts/check-machine-paths.sh
    - les SUMMARY des plans 32-01 a 32-05 reellement executes (pour savoir ce qui a ete livre et ce qui ne l'a pas ete)
  </read_first>
  <action>
1. Verifier d'abord que l'arbre de travail est PROPRE (aucune modification non commitee) : un bilan
   rendu sur un arbre sale ne prouve rien sur ce qui sera pousse. S'il ne l'est pas, s'arreter et le
   signaler plutot que de rendre un verdict.

2. Rejouer la decouverte COMPLETE des suites, avec le pattern EXACT de la CI et ses deux racines,
   puis executer CHAQUE suite decouverte, en comptant les echecs. Ne jamais restreindre a un
   sous-ensemble : la valeur de ce bilan est precisement qu'il couvre les suites que la phase n'a
   pas touchees. Reproduire aussi la garde anti-vert-a-vide (une decouverte a zero suite est un
   ECHEC, jamais un vert).

   Le compte est RE-DERIVE par la commande, jamais repris de ce plan. Valeur de reference avant la
   phase, a titre de comparaison seulement : 62 suites.

3. Executer les gates transverses du depot : synchronisation des versions et des compteurs, absence
   de chemin machine dans les fichiers commites, et le gate de discipline de release qui verifie
   qu'aucun bump racine n'a ete fait par cette phase.

4. Verifier UN PAR UN les cinq criteres de succes de la Phase 32, par execution et non par lecture,
   en consignant pour chacun la commande utilisee et son resultat :
   - critere 1 (battement separe de la lease, TTL par defaut inchange) ;
   - critere 2 (commit d'une session tierce bloque a la source, entree nee de merge-hooks, armement
     prouve — la lecture retenue de « gate regle 4 » est le gate de forme exec telle qu'installee,
     conformement a D-32-07) ;
   - critere 3 (checkout de branche sous lock d'autrui bloque par le meme guard, D-32-A) ;
   - critere 4 (reprise d'un lock perime explicite et tracee avec l'identite du repreneur) ;
   - critere 5 (jeton de fence en trailer, auditable).

5. Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md` et y consigner : le
   compte de suites decouvertes et le nombre d'echecs, le resultat de chaque gate transverse, et le
   tableau des cinq criteres avec leur commande de preuve et leur verdict. Ce document est la trace
   qui survit ; il ne doit contenir aucune appreciation non adossee a une commande.
  </action>
  <acceptance_criteria>
    - `git status --porcelain` est strictement vide au moment ou le bilan est lance.
    - La decouverte rend un compte STRICTEMENT SUPERIEUR a 62 (la phase a cree au moins une suite neuve) et un nombre d'echecs egal a `0`. Les deux valeurs sont consignees dans le document de reliquats.
    - `bash scripts/check-version-sync.sh` sort 0.
    - `bash scripts/check-machine-paths.sh` sort 0.
    - `bash scripts/check-release-tag.sh` ne signale aucun bump racine non tagge imputable a cette phase — et `git diff --name-only` sur l'ensemble des commits de la phase ne contient NI le fichier VERSION racine, NI le manifeste du plugin, NI le manifeste de la marketplace.
    - Le document de reliquats existe et porte les cinq criteres avec, pour chacun, la commande exacte et son resultat.
  </acceptance_criteria>
  <verify>
    <automated>[ -z "$(git status --porcelain)" ] && n=$(find plugin scripts -type f -path '*/tests/test-*.sh' | wc -l | tr -d ' ') && [ "$n" -gt 62 ] && f=0 && for t in $(find plugin scripts -type f -path '*/tests/test-*.sh' | sort); do bash "$t" >/dev/null 2>&1 || { echo "FAIL $t"; f=$((f+1)); }; done && [ "$f" -eq 0 ] && bash scripts/check-version-sync.sh && bash scripts/check-machine-paths.sh</automated>
  </verify>
  <done>Le parc entier est vert sur l'arbre commite, les gates transverses passent, aucun geste de release n'a ete fait, et les cinq criteres de succes ont chacun leur commande de preuve consignee.</done>
</task>

<task type="auto">
  <name>Tache 2 : etat reel de QUAL-01 — cloture ou dette tracee (D-32-QUAL)</name>
  <files>.planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md</files>
  <read_first>
    - 32-CONTEXT.md §Decisions D-32-QUAL (la decision, son statut d'extension de perimetre assumee, et la consigne explicite : si le lot est coupe, QUAL-01 retombe sur son trou, a RETRACER comme dette, pas a laisser croire couvert)
    - 32-TERRAIN.md §11 (les trois canaux mesures comme n'atteignant personne, et le constat que le marqueur de sante n'a aucun consommateur)
    - le SUMMARY du plan 32-05 s'il existe
  </read_first>
  <action>
Determiner par un FAIT MACHINE, jamais par appreciation, si le plan 32-05 a ete livre : le script
lecteur de marqueurs existe-t-il sous `plugin/conductor/scripts/`, et son entree de demarrage
figure-t-elle dans le fragment de hooks du module ?

**Branche A — le plan 32-05 a ete livre.** Consigner dans le document de reliquats que les quatre
issues de QUAL-01 sont couvertes de bout en bout pour le guard du verrou, et citer le cas de test
qui prouve la boucle producteur -> marqueur -> lecteur. Consigner aussi ce que la livraison change
pour le RESTE du parc : les marqueurs des autres gardes deviennent lisibles par le meme lecteur, ce
qui etait le motif de l'extension de perimetre.

**Branche B — le plan 32-05 a ete coupe.** Consigner la dette, explicitement et sans euphemisme :
l'issue 4 de QUAL-01 (« garde indisponible -> fail-open BRUYANT ») n'est PAS couverte pour le guard
du verrou ni pour aucun autre garde du parc ; le marqueur de sante continue d'etre ecrit et
continue de n'avoir AUCUN lecteur ; les trois canaux alternatifs ont ete mesures comme n'atteignant
ni le modele ni l'humain en non-interactif. Ecrire la consequence en une phrase : sur ce chemin, le
guard du verrou se degrade en silence, et rien ne le signale. Nommer le livrable manquant et le
plan qui le portait, pour que la reprise soit un geste et non une enquete.

Dans les deux branches, consigner aussi ce que QUAL-01 couvre DEJA de facon prouvee : les trois
autres issues (passage, refus, entree imparsable) et leurs cas de test, avec leurs identifiants.
  </action>
  <acceptance_criteria>
    - La branche retenue est adossee a un fait machine, et ce fait est cite dans le document : la commande de detection et son resultat y figurent.
    - Le document nomme explicitement les quatre issues de QUAL-01 et donne, pour chacune, soit son cas de test, soit la mention explicite qu'elle n'est pas couverte.
    - En branche B uniquement : le document contient le mot dette et nomme le livrable manquant ainsi que son plan d'origine.
    - Aucune formulation ne laisse croire couverte une issue qui ne l'est pas — relire la section a voix haute et consigner dans le SUMMARY la phrase qui porte le verdict.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi 'QUAL-01' .planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md && grep -c 'fail-open' .planning/phases/VFDO-32-durcissement-du-driver-lock/32-RELIQUATS.md</automated>
  </verify>
  <done>L'etat de QUAL-01 est ecrit tel qu'il est, adosse a un fait machine, avec la dette nommee si le lot abandonnable a ete coupe.</done>
</task>

<task type="auto">
  <name>Tache 3 : version, journal et README du module conductor</name>
  <files>plugin/conductor/VERSION, plugin/conductor/CHANGELOG.md, plugin/conductor/README.md</files>
  <read_first>
    - plugin/conductor/VERSION et plugin/conductor/CHANGELOG.md:1-40 (la forme exacte d'une entree : titre versionne et date, mention Minor ou Patch avec son motif, puis la description en prose dense)
    - plugin/conductor/README.md:35-45 (le tableau des briques, dont la ligne du verrou), :65-90 (les entrees de hooks et la liste des scripts), :100-125 (le compte de suites du module et l'arborescence commentee)
    - CLAUDE.md §Regle non negociable (la discipline de release : ce qui releve d'un bump de module et ce qui releve d'un bump racine — seul le premier est dans le perimetre de cette phase)
    - les SUMMARY des plans 32-01 a 32-05 reellement executes
  </read_first>
  <action>
1. Bumper `plugin/conductor/VERSION` en MINOR : la phase apporte des capacites nouvelles presentees
   a l'utilisateur (deux verbes de verrou, un guard bloquant, un signal de demarrage si le plan
   32-05 a ete livre). Ne toucher AUCUN des trois fichiers de version racine — cette phase ne fait
   aucune release ; le bump racine, le tag et la publication sont des gestes humains gates.

2. Ajouter UNE entree en tete de `plugin/conductor/CHANGELOG.md`, a la forme du fichier, decrivant
   ce que la phase a reellement livre — en s'appuyant sur les SUMMARY, jamais sur ce plan :
   - la separation battement / lease et le fait que le TTL par defaut ne bouge pas ;
   - le retrait de la reprise implicite au profit des deux verbes explicites, et le journal
     d'evenements qui survit a la generation reprise ;
   - le guard bloquant et son perimetre reel, AVEC sa limite de granularite et sa limite de matching
     ecrites — un journal de version qui promet plus que le code est une dette de confiance ;
   - la convention de jeton de fence et son tier (convention d'agent, jamais verifiee par machine) ;
   - le lecteur de marqueurs de sante, s'il a ete livre ; sinon, ne pas le mentionner du tout.

3. Mettre `plugin/conductor/README.md` a l'heure : la ligne du verrou dans le tableau des briques
   (verbes reels), la section des entrees de hooks (les entrees neuves, avec leur classement
   bloquante ou advisory), la liste des scripts du module, le compte de suites du module et
   l'arborescence commentee — chacun de ces comptes etant RE-DERIVE par commande, jamais recopie
   depuis ce plan.
  </action>
  <acceptance_criteria>
    - `plugin/conductor/VERSION` a ete bumpe en minor, et l'entree correspondante existe en tete du CHANGELOG avec la meme valeur : la version lue dans le fichier VERSION apparait dans le titre de la premiere entree du CHANGELOG.
    - Aucun fichier de version racine n'est touche : `git diff --name-only HEAD~1` ne contient ni `VERSION`, ni `plugin/.claude-plugin/plugin.json`, ni `.claude-plugin/marketplace.json`.
    - Les comptes du README du module sont exacts : le nombre de scripts et le nombre de suites cites correspondent aux valeurs re-derivees par commande sur `plugin/conductor/scripts/` et `plugin/conductor/scripts/tests/`. Consigner les deux valeurs mesurees dans le SUMMARY.
    - Le README ne decrit plus une recuperation automatique du verrou.
    - `bash scripts/check-version-sync.sh` sort 0.
    - `bash plugin/conductor/scripts/tests/test-doc-and-commands.sh` sort 0 et `bash plugin/conductor/scripts/tests/test-conductor.sh` sort 0.
    - La decouverte complete des suites est REJOUEE une derniere fois apres ce commit, sur l'arbre propre, et rend zero echec — c'est le dernier geste du plan.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && bash plugin/conductor/scripts/tests/test-doc-and-commands.sh && bash plugin/conductor/scripts/tests/test-conductor.sh && f=0 && for t in $(find plugin scripts -type f -path '*/tests/test-*.sh' | sort); do bash "$t" >/dev/null 2>&1 || { echo "FAIL $t"; f=$((f+1)); }; done && [ "$f" -eq 0 ]</automated>
  </verify>
  <done>Le module porte une version et un journal qui decrivent ce qui a ete livre, ni plus ni moins ; sa documentation ne promet aucune garantie que le code ne tient pas ; et le parc entier est vert une derniere fois sur l'arbre propre.</done>
  <reversibility rating="reversible">Version, journal et documentation : un `git revert` suffit, aucun artefact ne survit, et aucune publication n'a eu lieu.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arbre commite -> verdict de cloture | le bilan ne vaut que s'il porte sur ce qui sera reellement pousse : un arbre sale ferait rendre un verdict sur du code qui n'existera pas |
| documentation du module -> utilisateur | un README ou un CHANGELOG qui promet une garantie que le code ne tient pas est une dette de confiance, pas une imprecision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-28 | Repudiation | bilan rendu sur un arbre sale | high | mitigate | premier geste de la tache 1 : verification que l'arbre suivi est strictement propre, arret sinon |
| T-32-29 | Repudiation | vert a vide de la decouverte | high | mitigate | garde anti-vert-a-vide reproduite (zero suite decouverte est un echec) plus plancher chiffre re-derive et compare a la valeur d'avant phase |
| T-32-30 | Repudiation | dette QUAL-01 passee sous silence | high | mitigate | branche determinee par un FAIT machine, document de reliquats qui nomme les quatre issues une par une et la dette le cas echeant |
| T-32-31 | Tampering | release involontaire | medium | mitigate | interdit explicite sur les trois fichiers de version racine, verifie par critere sur les fichiers modifies ; aucun tag, aucune PR, aucune publication dans ce plan |
| T-32-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installe par cette phase — aucun gate de legitimite applicable |
</threat_model>

<verification>
1. Arbre suivi strictement propre avant le bilan
2. Decouverte COMPLETE des suites avec le pattern exact de la CI, sur les deux racines : compte re-derive superieur a 62, zero echec
3. `bash scripts/check-version-sync.sh`, `bash scripts/check-machine-paths.sh`, `bash scripts/check-release-tag.sh`
4. Les cinq criteres de succes de la Phase 32, chacun avec sa commande de preuve, consignes dans le document de reliquats
5. Etat de QUAL-01 consigne, adosse a un fait machine
6. Aucun fichier de version racine modifie sur l'ensemble des commits de la phase
</verification>

<success_criteria>
- Le parc ENTIER est vert sur l'arbre commite, avec un compte de suites re-derive et non fige.
- Les cinq criteres de succes du ROADMAP ont chacun une commande de preuve et un verdict ecrits.
- L'etat de QUAL-01 est consigne tel qu'il est, dette nommee si le lot abandonnable a ete coupe.
- Le module porte sa version et son journal ; aucune release racine n'a ete faite.
</success_criteria>

<output>
Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-06-SUMMARY.md` en fin de plan, avec :
le compte de suites decouvertes et le nombre d'echecs, les valeurs re-derivees des comptes du README
du module, la branche retenue pour QUAL-01 et le fait machine qui l'a decidee, et le tableau des
cinq criteres avec leurs commandes de preuve.
</output>

---
phase: 41-posture-de-protection-du-d-p-t
plan: 13
lot: L13 — ledger coché sur pièce, balayage final O-3, PR de clôture sans contournement, règle des artefacts post-merge
type: execute
wave: 13
depends_on: ["41-12"]
autonomous: false
requirements: [PROT-01, PROT-02, PROT-03, PROT-04]
files_modified:
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PROT-01..04 cochés et `Complete` en traçabilité seulement ici, chacun suivi d'une ligne de clôture `  - **Exigence close le <date>** (Phase 41, plan 41-13) — preuves : …` qui porte sur l'EXIGENCE et ne cite jamais O-3 ; toute mention d'O-3 reste « gardée par défaut + tracée » (point 5 du tour 1)"
    - "Chaque ligne de clôture cite ses clés de preuve de `41-PREUVES.md` et les décisions humaines consignées ; tout `non_prouvee` de `TRACE-II:` est recopié, jamais effacé"
    - "QUAL-01 : « non déclenché sur la Phase 41 » — aucun gate ni comparateur neuf (D-M11, `INVARIANTS-PHASE`)"
    - "Balayage final : aucune proposition d'aucun artefact (REQUIREMENTS, CLAUDE.md, ADR.md, BACKLOG, CHANGELOG, README, 25-SECURITY, les treize SUMMARY) n'associe O-3 à un mot d'achèvement ; le SUMMARY de 41-08 porte la nuance des checks en cours"
    - "La PR de clôture est la troisième PR de la phase, imposée par D-03 (décision technique validée par le manager le 2026-09-17). Son SUMMARY est committé AVANT sa publication ; son merge, sans contournement, est relu par l'orchestrateur"
    - "Règle des artefacts post-merge (point 7 du tour 1) : tout artefact écrit APRÈS le merge de la PR de clôture — VERIFICATION de phase, STATE.md, ROADMAP.md passé à « complète », rapport de mission — est écrit sur une branche `docs/phase-41-verification` créée depuis `origin/main` et entre par une PR mergée sans contournement (mêmes gestes humains que la Task 2), OU n'est pas versionné. Aucun commit direct sur `main` : le serveur le refuse (`D03-VERDICT`) et `git rev-list --count origin/main..main` reste à 0"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROT-01..04 cochés sur pièce ; QUAL-01 : non déclenché sur la Phase 41"
  key_links:
    - "Ce plan écrit sur la branche C, à la suite de 41-12"
---

<objective>
Cocher le ledger sur pièce, faire le balayage final des artefacts, puis faire entrer la clôture sur
`main` par une PR ordinaire — et fixer le chemin de tout ce qui s'écrira après.

Purpose: clôture sur pièce de PROT-01..04 ; QUAL-01 dit ; D-03 tenu jusqu'au dernier artefact.
Output: ledger coché, PR de clôture mergée sans contournement.

Budget (point 9 du tour 1) : seconde moitié de l'ancien 41-11, scindé pour tenir sous trois tâches.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-12-SUMMARY.md

Extraits de `.planning/REQUIREMENTS.md` : section `### Posture de protection du dépôt (Phase 41)` ;
table `## Traceability`, lignes `PROT-0N` et `QUAL-01`.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : ledger coché sur pièce avec lignes de clôture distinctes d'O-3, QUAL-01, balayage final, rejeu, SUMMARY avant publication</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (toutes les lignes)
    - .planning/REQUIREMENTS.md (section Phase 41, lignes de traçabilité PROT et QUAL-01)
    - les SUMMARY 41-01 à 41-12
  </read_first>
  <action>
Identité `samuel-neveugall` ; branche C.

`REQUIREMENTS.md` :
- passer PROT-01..04 de `- [ ]` à `- [x]` sans toucher au texte de leur définition ni à ses lignes
  de suite (format multi-lignes du ledger, cf. PART-01/02) ;
- sous chacune, juste après sa dernière ligne de suite, ajouter une ligne de clôture
  `  - **Exigence close le <date d'exécution>** (Phase 41, plan 41-13) — preuves : <clés>`, poursuivie
  si besoin sur des lignes indentées de quatre espaces, qui ne cite pas O-3 (la clôture porte sur
  l'exigence) ; clés : PROT-01 → `POSE-CONFORME`,
  `M1-VERDICT`, `PROVENANCE-SERVEUR`, `TRACE-II` (avec ses statuts par règle), `PUSH-DIRECT`,
  `D03-VERDICT`, `M4-VERDICT` ; PROT-02 → `PR-RELEASE`, `RELEASE`, `CI-MAIN-RELEASE`,
  `INVARIANTS-PHASE` ; PROT-03 → ADR-072 et son § Mesures, amendement d'ADR-059, `CLAUDE.md`,
  BACKLOG traité ; PROT-04 → `CO-I`, `CO-BASELINE`, `CO-SENTINELLE`, `TRACE-II pull_request=…`,
  ligne de statut Phase 41 du `25-SECURITY.md` ; chaque décision humaine consignée (`*-DECISION:`,
  option, canal, date) est citée sous l'exigence qu'elle touche ; tout `non_prouvee` de `TRACE-II:`
  est recopié sous PROT-01 ou PROT-04 ;
- lignes de traçabilité `| PROT-0N | Phase 41 | Complete — <date>, plan 41-13 : … |` ;
- ligne QUAL-01 : ajouter `41` à la liste des phases de la deuxième colonne et, avant « ; reste à
  tenir », « ; **non déclenché sur la Phase 41** (<date>) — aucun gate ni comparateur neuf n'y naît
  (D-M11 : CODEOWNERS et rulesets sont de la configuration), `INVARIANTS-PHASE` à 0 ajout ».

Commit local sur C, message « docs(41-13): ledger PROT-01..04 coché sur pièce, QUAL-01 non déclenché
(D-02, D-05, D-07, D-09 — arbitrage Samuel, AskUserQuestion session principale, 2026-09-17) ». Rejouer `gates` et `tests`. Rédiger et committer
`41-13-SUMMARY.md` sur C À LA FIN de cette tâche : il écrit que la phase ne crée aucun gate
(critère 4), annonce la Task 2 et recopie la règle des artefacts post-merge.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; R=.planning/REQUIREMENTS.md; T=$(mktemp -d); x=$(awk "/^- \\[x\\] \\*\\*PROT-0[1-4]\\*\\*: /{c++} END{print c+0}" "$R"); u=$(awk "/^- \\[ \\] \\*\\*PROT-/{c++} END{print c+0}" "$R"); awk "/^- \\[[ x]\\] \\*\\*PROT-0[1-4]\\*\\*: / { if (st == 2) print blk; st = 1; next } st == 1 && /^  [^ -]/ { next } st == 1 && /^  - \\*\\*Exigence close le [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\\*\\* \\(Phase 41, plan 41-13\\)/ { st = 2; blk = \$0; next } st == 1 { st = 0; next } st == 2 && /^    / { blk = blk \" \" \$0; next } st == 2 { print blk; st = 0 } END { if (st == 2) print blk }" "$R" > "$T/cl"; cl=$(awk "END{print NR}" "$T/cl"); o3cl=$(awk "index(\$0, \"O-3\") {c++} END{print c+0}" "$T/cl"); t=$(awk "/^\\| PROT-0[1-4] \\| Phase 41 \\| Complete — /{c++} END{print c+0}" "$R"); p=$(awk "/^\\| PROT-0[1-4] \\| Phase 41 \\| Pending/{c++} END{print c+0}" "$R"); q=$(awk "/^\\| QUAL-01 \\| Transverse — Phases [^|]*41[^|]*\\|/ && index(\$0, \"non déclenché sur la Phase 41\") {c++} END{print c+0}" "$R"); k=0; for key in POSE-CONFORME CO-I CO-BASELINE CO-SENTINELLE M1-VERDICT PROVENANCE-SERVEUR TRACE-II PUSH-DIRECT D03-VERDICT M4-VERDICT PR-RELEASE RELEASE CI-MAIN-RELEASE INVARIANTS-PHASE; do awk -v k="$key" "index(\$0, k) {f=1} END{exit !f}" "$T/cl" || { echo "CLE DE PREUVE NON CITEE : $key"; k=$((k+1)); }; done; awk "\$1 ~ /-DECISION:\$/ { for (i=2;i<=NF;i++) if (index(\$i, \"option=\")==1) print substr(\$i, 8) }" "$PH/41-PREUVES.md" > "$T/dec"; while IFS= read -r o; do [ -n "$o" ] || continue; awk -v o="$o" "index(\$0, o) {f=1} END{exit !f}" "$T/cl" || { echo "DECISION NON CITEE : $o"; k=$((k+1)); }; done < "$T/dec"; np=$(awk "\$1==\"TRACE-II:\" && /non_prouvee/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); nr=$(awk "index(\$0, \"non_prouvee\") {c++} END{print c+0}" "$T/cl"); echo "ledger : coches=$x non_coches=$u lignes_de_cloture=$cl lignes_de_cloture_citant_O3=$o3cl table_complete=$t table_pending=$p qual01_phase41=$q cles_ou_decisions_non_citees=$k trace_non_prouvee=$np recopiee=$nr"; [ "$x" -eq 4 ] && [ "$u" -eq 0 ] && [ "$cl" -eq 4 ] && [ "$o3cl" -eq 0 ] && [ "$t" -eq 4 ] && [ "$p" -eq 0 ] && [ "$q" -eq 1 ] && [ "$k" -eq 0 ] && { [ "$np" -eq 0 ] || [ "$nr" -ge 1 ]; }'</automated>
    <fails_when>exit non nul ; ou moins de 4 entrées PROT cochées ; ou une entrée non cochée ou `Pending` ; ou une entrée cochée sans sa ligne de clôture juste après ses lignes de suite (format multi-lignes du ledger), au format `  - **Exigence close le AAAA-MM-JJ** (Phase 41, plan 41-13)`, elle-même éventuellement poursuivie sur des lignes indentées de quatre espaces (lues avec elle) ; ou une ligne de clôture qui cite O-3 (la clôture porte sur l'EXIGENCE, jamais sur O-3) ; ou QUAL-01 sans la Phase 41 ni « non déclenché » ; ou une clé de preuve ou une décision humaine non citée ; ou un `non_prouvee` de la trace effacé</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; o3() { h=0; for f in "$@"; do r=0; awk "{ l = tolower(\$0); if (l ~ /^[ \t]*([-*+] |[|#]|\$)/) t = t \"\n\" l; else t = t \" \" l } END { gsub(/[|;.]/, \"\n\", t); n = split(t, c, \"\n\"); for (i = 1; i <= n; i++) if (index(c[i], \"o-3\") && (index(c[i], \"ferm\") || index(c[i], \"clos\"))) { print \"O-3 DITE ACHEVEE : \" FILENAME \" :\" c[i]; k++ } exit (k > 0) }" "$f" || r=$?; [ "$r" -eq 0 ] || h=$((h+1)); done; echo "o3 : fichiers_en_defaut=$h fichiers_lus=$#"; [ "$h" -eq 0 ] && [ "$#" -gt 0 ]; }; o3 .planning/REQUIREMENTS.md CLAUDE.md docs/ADR.md .planning/BACKLOG.md CHANGELOG.md README.md README.fr.md .planning/phases/VFDO-25-budget-d-instructions-et-tage-d-alignement-court/25-SECURITY.md $PH/41-*-SUMMARY.md'</automated>
    <fails_when>exit non nul ; ou une ligne `O-3 DITE ACHEVEE :` — dans une même proposition (bornée par `.`, `;`, `|` ou un début d'élément de liste), O-3 est associée à un mot de la famille de l'achèvement, y compris nié (D-02 : O-3 est « gardée par défaut + tracée », jamais davantage) ; ou un fichier illisible (compté en défaut)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; n=0; for i in 01 02 03 04 05 06 07 08 09 10 11 12 13; do if [ -f "$PH/41-$i-SUMMARY.md" ]; then n=$((n+1)); else echo "SUMMARY ABSENT : 41-$i"; fi; done; nu=$(awk "index(\$0, \"checks en cours\") && index(\$0, \"pas sur une PR rouge\") {c++} END{print c+0}" "$PH/41-08-SUMMARY.md"); ag=$(awk "index(tolower(\$0), \"aucun gate\") {c++} END{print c+0}" "$PH/41-13-SUMMARY.md"); echo "SUMMARY : presents=$n nuance_41-08=$nu aucun_gate_41-13=$ag"; [ "$n" -eq 13 ] && [ "$nu" -ge 1 ] && [ "$ag" -ge 1 ]'</automated>
    <fails_when>exit non nul ; ou un SUMMARY de plan absent ; ou le SUMMARY de 41-08 ne reprend pas la nuance « contournement réel sur des checks en cours, pas sur une PR rouge » (point 12 du tour 1) ; ou le SUMMARY de 41-13 ne dit pas que la phase ne crée aucun gate (critère 4)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0. Rouge T28-F : conduite du plan 41-01</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; s=$(git cat-file -e "HEAD:$PH/41-13-SUMMARY.md" 2>/dev/null && echo oui || echo non); TS=$(mktemp -d); git status --porcelain --untracked-files=no > "$TS/st"; d=$(awk "END{print NR}" "$TS/st"); T=$(mktemp -d); git diff --name-only origin/main...HEAD > "$T/f"; awk "!/^[ \t]*#/ && NF {print \$1}" .github/CODEOWNERS > "$T/pat"; co=0; while IFS= read -r p; do [ -n "$p" ] || continue; git -c core.excludesFile="$T/pat" check-ignore -q --no-index "$p" && co=$((co+1)); done < "$T/f"; echo "avant publication : summary_committe=$s modifications_non_committees=$d chemins_codeowners=$co"; [ "$s" = oui ] && [ "$d" -eq 0 ] && [ "$co" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou SUMMARY 41-11 non committé ; ou modifications suivies non committées ; ou la PR de clôture toucherait un chemin CODEOWNERS</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; can[na]=\$2; dat[na]=\$3; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok) { if (m ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && index(m, key[j]) && can[j] != \"\" && dat[j] != \"\" && index(m, \"arbitrage Samuel, \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `SANS CANAL NI DATE (D-01..D-10)` (un commit de plan cite D-01..D-10 sans « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 ») ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (un commit de plan fixe ou cite l'arbitrage `REGLES_MAIN_FORCE_PUSH_SUPPRESSION` ou une décision `*-DECISION` sans « arbitrage Samuel, <canal>, <date> » égal au canal et à la date CAPTURÉS — frontmatter `REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE` de 41-01, ou ligne `*-DECISION:` de `41-PREUVES.md` —, ou si ces valeurs manquent) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - PROT-01..04 cochés, chacun suivi de sa ligne de clôture d'exigence (sans O-3), clés et décisions citées, `non_prouvee` recopiés ; `Complete` en traçabilité ; QUAL-01 « non déclenché sur la Phase 41 »
    - balayage O-3 vert sur tous les artefacts et les treize SUMMARY ; nuance de 41-08 présente ; « aucun gate » écrit
    - rejeux verts ; SUMMARY 41-13 committé ; PR de clôture hors CODEOWNERS ; messages de commit conformes
  </acceptance_criteria>
  <done>Les quatre exigences sont closes sur pièce et prêtes à entrer sur `main` par une PR ordinaire.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 : gestes humains `samuel-neveugall` — publier C, ouvrir la PR de clôture, merger SANS contournement</name>
  <action>Samuel, compte `samuel-neveugall`, publie C, ouvre la PR de clôture, attend la CI, vérifie l'état `CLEAN`, merge sans contournement, puis rapporte le résultat à l'orchestrateur.</action>
  <instructions>
Compte qui agit : `samuel-neveugall`.

1. `git push -u origin docs/phase-41-cloture`
2. `gh pr create --repo picmakpro/vibeflow-os --base main --head docs/phase-41-cloture --title "Phase 41 — clôture : release v<N> prouvée, ledger PROT-01..04" --body "Preuves post-release (tag, release, check-release-tag --remote), invariants de phase, ledger coché sur pièce, QUAL-01 non déclenché. Merge sans contournement."`
   (corps sans arbitrage D-01..D-10 ; s'il en cite un, il porte « arbitrage Samuel, AskUserQuestion session principale, 2026-09-17 »)
3. `gh pr checks <n> --repo picmakpro/vibeflow-os --watch` jusqu'à la fin.
4. `gh pr view <n> --repo picmakpro/vibeflow-os --json mergeStateStatus --jq .mergeStateStatus`
   → `CLEAN` pour poursuivre ; `BEHIND` : `gh pr update-branch <n> --repo picmakpro/vibeflow-os`
   puis 3 et 4 ; tout autre état : arrêt, sans contournement.
5. `gh pr merge <n> --repo picmakpro/vibeflow-os --merge` (SANS `--admin`) ;
   témoin `gh pr view <n> --repo picmakpro/vibeflow-os --json state,mergeCommit,body --jq '{state, merge: .mergeCommit.oid, body}'`.

Après ce merge (point 7 du tour 1) : AUCUN commit direct sur `main`. Tout artefact écrit ensuite
(VERIFICATION de phase, STATE.md, ROADMAP.md à « complète », rapport de mission) part d'une branche
`docs/phase-41-verification` créée depuis `origin/main`, et entre par une PR publiée et mergée par
Samuel exactement comme aux étapes 1 à 5 (état `CLEAN`, sans `--admin`) — ou n'est pas versionné.
Le résultat de ce merge n'est pas écrit dans le dépôt (il faudrait une PR de plus) : il est rapporté à
l'orchestrateur, qui le relit par `gh pr view`.
  </instructions>
  <verification>L'orchestrateur relit `gh pr view <n> --json state,mergeCommit,body` (corps : canal et date si un arbitrage D-01..D-10 y est cité), vérifie que le push de merge sur `main` est vert (4 jobs, `check-release-tag` compris, la `VERSION` étant taggée), puis, avant tout artefact post-merge : `git fetch origin && git rev-list --count origin/main..main` rend `0` et `git branch --show-current` n'est pas `main`.</verification>
  <resume-signal>Coller `<n>` et les sorties de 3 (fin), 4 et 5, puis écrire « PR de clôture mergée ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preuves → ledger | cochage d'exigences, point d'entrée des lecteurs futurs |
| orchestrateur → `main` après clôture | artefacts de fin de phase écrits hors du plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-43 | Repudiation | exigence cochée en effaçant un `non_prouvee` | medium | mitigate | recopie vérifiée dans les lignes de clôture |
| T-41-46 | Repudiation | clôture par commit direct | medium | mitigate | PR de clôture hors CODEOWNERS, état `CLEAN` exigé, `--admin` exclu, merge relu par l'orchestrateur |
| T-41-55 | Repudiation | clôture d'exigence confondue avec O-3 | medium | mitigate | ligne de clôture distincte, sans O-3 ; balayage O-3 par proposition sur tous les artefacts |
| T-41-56 | Tampering | artefact post-merge poussé directement sur `main` | medium | mitigate | branche `docs/phase-41-verification` + PR ; refus serveur mesuré (`D03-VERDICT`) ; `git rev-list --count origin/main..main` à 0 relu par l'orchestrateur |
</threat_model>

<verification>
Ledger coché sur pièce sans confusion avec O-3, balayage final vert, PR de clôture mergée par le chemin
normal, artefacts post-merge cantonnés à une branche et une PR.
</verification>

<success_criteria>
Critères de succès 1 à 4 de la Phase 41 prouvés et consignés ; PROT-01..04 clos sur pièce.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-13-SUMMARY.md` — committé sur la branche C à la fin de la Task 1, AVANT la publication (Task 2). Tout artefact postérieur au merge suit la règle du frontmatter (branche + PR, ou non versionné).
</output>

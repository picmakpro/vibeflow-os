---
phase: 41-posture-de-protection-du-d-p-t
plan: 05
lot: L5 — décision explicite de pose, pose des deux rulesets par l'exécutant (jeton admin de Willy), relecture serveur (critère 1), deux entrées `User` relues, PR en vol avant/après
type: execute
wave: 5
depends_on: ["41-04"]
autonomous: false
requirements: [PROT-01]
files_modified:
  - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Le retour arrière est écrit ET committé dans `41-PREUVES.md` AVANT la pose : l'admin repasse chaque ruleset en `disabled` par `gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled` ; il n'est exercé que sur décision humaine"
    - "Aucun appel d'écriture chez GitHub n'a lieu avant une décision explicite consignée `POSE-DECISION: option=poser …` (checkpoint de décision, porte à sens quasi unique : la pose change les conditions de merge de toute PR ouverte vers `main`) ; la décision est prise sur pièce — inventaire `PR-EN-VOL-AVANT:`, `main` verte, conséquences de D-02bis, retour arrière"
    - "Les PR en vol ne sont jamais commentées, mises à jour, fermées ni mergées par la phase : leur état est relevé avant (`PR-EN-VOL-AVANT:`) et après (`PR-EN-VOL-APRES:`) la pose et rapporté ; une PR qui touche un chemin CODEOWNERS est signalée (revue `@picmakpro` exigée après la pose)"
    - "La pose est faite par l'exécutant, sous le jeton admin de la session (Willy, `picmakpro`), depuis les fichiers d'`origin/main` (`git show origin/main:.github/rulesets/<f> | gh api -X POST … --input -`) : exactement deux appels d'écriture, ce qui est posé est la source mergée, octet pour octet ; un refus (422 ou autre) n'est JAMAIS contourné en retouchant le JSON — il mène à la Task 4"
    - "Critère 1 : `gh api repos/picmakpro/vibeflow-os/rulesets` ne rend plus `[]` — deux rulesets actifs, dont celui de `main` exigeant les 4 checks CI verts (PROT-01)"
    - "D-02bis mesuré, jamais supposé : les deux entrées `User` (151974738 et 203482067, mode `always`) sont RELUES depuis `GET /repos/picmakpro/vibeflow-os/rulesets/{id}` pour chacun des deux rulesets (`POSE-BYPASS-RELU:`) ; `current_user_can_bypass` de `picmakpro` relu (`always` attendu sur les deux) ; celui de `samuel-neveugall` n'est pas mesurable depuis ce poste et est consigné `non_mesure`, jamais déduit"
    - "`M2-VERDICT: CONFORME` seulement si les entrées relues sont exactement les deux `User` mesurés en `always`, sans autre acteur, si `picmakpro` rend `always` sur les deux rulesets et si chaque champ arbitré relu côté serveur est conforme ; tout écart (ou échec de pose) arrête le plan sur la Task 4"
    - "L'accès en lecture aux rule suites est mesuré pour `picmakpro` : les plans de preuve savent si l'exécutant relit lui-même la trace (`relue=oui`)"
  artifacts:
    - path: ".planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md"
      provides: "lignes `RETOUR-ARRIERE-ECRIT:`, `MAIN-VERTE-AVANT-POSE:`, `PR-EN-VOL-AVANT:`, `POSE-DECISION:`, `POSE-BRANCHE:`, `POSE-TAGS:`, `POSE-BYPASS-RELU:`, `POSE-MSG:`, `POSE-CONFORME:`, `M2:`, `M2-VERDICT:`, `ACCES-RULE-SUITES:`, `PR-EN-VOL-APRES:`"
  key_links:
    - "`POSE-BRANCHE: id=` et `POSE-TAGS: id=` sont lus par tous les plans suivants (retour arrière, rule suites)"
    - "`PR-EN-VOL-APRES: numeros=` borne les PR ouvertes tolérées par les plans 41-06 à 41-09 (en plus de leurs PR de preuve)"
---

<objective>
Décider explicitement de la pose, poser les deux rulesets depuis la source mergée, puis prouver la
pose par relecture serveur — les deux entrées `User` de D-02bis comprises — et rapporter l'effet sur
les PR en vol.

Révisé le 2026-09-23 (reprise) : l'ancienne pose manuelle par `picmakpro` sous dictée devient un
geste de l'exécutant, possible seulement parce que le jeton de la session est admin, et gardé par
un checkpoint de décision AVANT le premier appel d'écriture ; M-2 n'est plus « le bypass `write`
couvre-t-il l'admin » (sans objet depuis D-02bis) mais la relecture des deux entrées `User` ; la
précondition « aucune PR ouverte » devient un inventaire des PR en vol, présenté à la décision.

Purpose: critère de succès 1 (PROT-01) ; D-02bis mesuré ; D-M14 tenu (pose après le merge de la
source) ; décision humaine sur une porte à sens quasi unique.
Output: deux rulesets actifs ; lignes de décision, de pose, de conformité, de relecture du
contournement, d'accès aux rule suites et d'état des PR en vol.

Budget : quatre tâches, dont la quatrième est un checkpoint de décision CONDITIONNEL, sans exécution
quand `M2-VERDICT: CONFORME` et sans `POSE-ECHEC:`. La sortir dans un plan séparé ferait porter la
mesure et la décision par deux exécuteurs sans contexte commun.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-CONTEXT.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md
@.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-04-SUMMARY.md

Sources : `docs.github.com/en/rest/repos/rules` (lue le 2026-09-17, relue le 2026-09-23) —
`POST /repos/{owner}/{repo}/rulesets` (admin), `GET …/rulesets/{id}` (`bypass_actors` rendu
seulement à qui a l'écriture sur le ruleset ; `current_user_can_bypass` ∈ always /
pull_requests_only / never / exempt), `PUT …/rulesets/{id}` (corps partiel),
`GET …/rules/branches/{branch}` ; `docs.github.com/en/rest/repos/rule-suites` (permissions non
précisées par la doc).
</context>

<tasks>

<task type="auto">
  <name>Task 1 : préalables de pose relus, `main` verte, PR en vol inventoriées, retour arrière écrit et committé, dossier de décision imprimé</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`ACTEURS-CONTOURNEMENT:`, `PR-EN-VOL-REPRISE:`, `PR-PHASE:`)
    - .github/CODEOWNERS (sur `origin/main`, pour classer les fichiers des PR en vol)
  </read_first>
  <action>
Lecture seule chez GitHub. Identité `picmakpro`, admin `true`, branche courante B. `git fetch
origin`. Relire : `PR-PHASE:` présente et son merge dans `origin/main` ; `rulesets` = `[]` ;
`codeowners/errors` = 0 ; collaborateurs toujours égaux à `ACTEURS-CONTOURNEMENT:` ; les deux JSON
d'`origin/main` repassent la conformité de 41-01 (lus par `git show origin/main:…`, jamais depuis
l'arbre de travail) ; la pointe d'`origin/main` est verte (même contrôle qu'au plan 41-04).

Écrire dans `41-PREUVES.md` une section `## 41-05 — pose` puis, AVANT la décision :
- `RETOUR-ARRIERE-ECRIT: admin=picmakpro gh_api_-X_PUT_repos/picmakpro/vibeflow-os/rulesets/<id>_-f_enforcement=disabled temoin=rules/branches/main_vide`
  et un paragraphe en clair reprenant la commande ;
- `MAIN-VERTE-AVANT-POSE: sha=<pointe d'origin/main> tests=<conclusion> lab_frais=<conclusion> lab_frais_arme=<conclusion> gates=<conclusion> gates_echec=<aucun|check-release-tag>` ;
- `PR-EN-VOL-AVANT: numeros=<#n,… triés par numéro croissant, ou aucune> detail=<#n:base=<branche>:etat=<mergeStateStatus>:revue=<reviewDecision ou aucune>:codeowners=<oui|non>,…>` —
  `codeowners=oui` si un fichier de la PR tombe sous un motif de `.github/CODEOWNERS` d'`origin/main`
  (moteur gitignore de git, comme au plan 41-02).
Si ces lignes existent déjà (reprise après une décision « REPORTER »), les METTRE À JOUR au lieu de
les dupliquer. Commit local sur B, message « docs(41-05): retour arrière écrit, PR en vol
inventoriées avant la pose ».

Imprimer le dossier de décision de la Task 2 : les PR en vol et l'effet attendu de la pose sur
chacune (PR vers `main` : 4 checks verts et branche à jour exigés ; `codeowners=oui` : revue
`@picmakpro` exigée — une PR ouverte par `picmakpro` n'entrera qu'en contournant ; base autre que
`main` : non concernée tant qu'elle ne vise pas `main`), les conséquences de D-02bis (Samuel et
Willy peuvent pousser directement, forcer ou supprimer `main`, réécrire un tag `v*` : accepté et
tracé), la fenêtre de preuves (aucun merge sur `main` de 41-06 à 41-09 hors PR de preuve — Willy
prévient Samuel), le retour arrière.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; u=$(gh api user --jq .login); a=$(gh api repos/picmakpro/vibeflow-os --jq .permissions.admin); br=$(git branch --show-current); rs=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); ce=$(gh api repos/picmakpro/vibeflow-os/codeowners/errors --jq ".errors | length"); c=$(gh api repos/picmakpro/vibeflow-os/collaborators --jq "[.[] | \"\(.login)=\(.id)\"] | sort | join(\",\")"); M=$(awk "\$1==\"PR-PHASE:\" { for (i=2;i<=NF;i++) if (index(\$i, \"merge=\")==1) { print substr(\$i, 7); exit } }" "$PH/41-PREUVES.md"); anc=$(git merge-base --is-ancestor "$M" origin/main && echo oui || echo non); ra=$(awk "\$1==\"RETOUR-ARRIERE-ECRIT:\" && /enforcement=disabled/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); T=$(mktemp -d); git show HEAD:"$PH/41-PREUVES.md" > "$T/h"; ras=$(awk "\$1==\"RETOUR-ARRIERE-ECRIT:\" {c++} END{print c+0}" "$T/h"); ev=$(awk "\$1==\"PR-EN-VOL-AVANT:\" { for (i=2;i<=NF;i++) if (index(\$i, \"numeros=\")==1) { print substr(\$i, 9); exit } }" "$PH/41-PREUVES.md"); ap=$(gh pr list --repo picmakpro/vibeflow-os --state open --json number --jq "[.[].number] | sort | map(\"#\" + tostring) | join(\",\")"); echo "prealables pose : identite=$u admin=$a branche=$br rulesets=$rs codeowners_errors=$ce collaborateurs=$c merge_phase_dans_main=$anc retour_arriere_ecrit=$ra retour_arriere_committe=$ras pr_en_vol_consignees=$ev pr_ouvertes=${ap:-aucune}"; [ "$u" = picmakpro ] && [ "$a" = true ] && [ "$br" = feat/phase-41-preuves-release ] && [ "$rs" -eq 0 ] && [ "$ce" -eq 0 ] && [ "$c" = "picmakpro=203482067,samuel-neveugall=151974738" ] && [ "$anc" = oui ] && [ "$ra" -eq 1 ] && [ "$ras" -eq 1 ] && [ "$ev" = "${ap:-aucune}" ]'</automated>
    <fails_when>exit non nul ; ou identité ou admin inattendus ; ou branche autre que B ; ou `rulesets=` différent de 0 ; ou `codeowners_errors=` différent de 0 ; ou collaborateurs changés depuis la reprise (prémisses de D-02bis changées : arrêt, remontée) ; ou merge de la PR de la phase absent de `main` ; ou retour arrière non écrit ou non committé avant la décision ; ou inventaire des PR en vol périmé (numéros différents des PR ouvertes relues : le refaire)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; SA=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); PA=$(val ACTEURS-CONTOURNEMENT: picmakpro); case "$SA$PA" in ""|*[!0-9]*) echo "acteurs absents"; exit 1;; esac; T=$(mktemp -d); git show origin/main:.github/rulesets/main.json > "$T/b"; git show origin/main:.github/rulesets/tags-v.json > "$T/g"; E="([{actor_id: \$sa, actor_type: \"User\", bypass_mode: \"always\"}, {actor_id: \$pa, actor_type: \"User\", bypass_mode: \"always\"}] | sort_by(.actor_id))"; ob=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | [(.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"]), ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]), (\$r.pull_request.required_approving_review_count==0), (\$r.pull_request.require_code_owner_review==true), ((\$r.required_status_checks.required_status_checks | length)==4), ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), (\$r.required_status_checks.strict_required_status_checks_policy==true), ((.bypass_actors | sort_by(.actor_id)) == $E)] | all" "$T/b"); og=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "[(.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"]), ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), ((.bypass_actors | sort_by(.actor_id)) == $E)] | all" "$T/g"); echo "sources origin/main : branche_conforme=$ob tags_conforme=$og"; [ "$ob" = true ] && [ "$og" = true ]'</automated>
    <fails_when>exit non nul ; ou `branche_conforme=false` / `tags_conforme=false` : la source sur `main` n'est pas celle qui a été relue, l'un des quatre types exigés manque, ou `bypass_actors` n'est pas exactement les deux `User` mesurés en `always` (arrêt avant toute décision)</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; S=$(git rev-parse origin/main); T=$(mktemp -d); gh api "repos/picmakpro/vibeflow-os/commits/$S/check-runs?per_page=100" --jq ".check_runs | map(select(.app.id == 15368)) | group_by(.name) | map(max_by(.id)) | .[] | \"\(.name)\t\(.conclusion)\t\(.id)\"" > "$T/cr"; ko=0; for j in "Suites de tests (découverte non vide)" "Lab frais (install baseline + Gate C — leçon UAT 2026-07-25, F2)" "Lab frais arme (as-installed testing — le gate installe, sur un univers non vide, #38)"; do c=$(awk -F"\t" -v j="$j" "\$1==j {print \$2; exit}" "$T/cr"); [ "$c" = success ] || { echo "MAIN ROUGE : $j = ${c:-absent}"; ko=$((ko+1)); }; done; gc=$(awk -F"\t" "\$1==\"Gates de qualité (mode strict)\" {print \$2; exit}" "$T/cr"); gi=$(awk -F"\t" "\$1==\"Gates de qualité (mode strict)\" {print \$3; exit}" "$T/cr"); ge=aucun; if [ "$gc" != success ]; then fs=$(gh api "repos/picmakpro/vibeflow-os/actions/jobs/$gi" --jq "[.steps[] | select(.conclusion == \"failure\") | .name] | join(\"|\")"); case "$fs" in check-release-tag*) ge=check-release-tag;; *) echo "GATES ROUGE AILLEURS : $fs"; ko=$((ko+1));; esac; fi; ms=$(val MAIN-VERTE-AVANT-POSE: sha); mg=$(val MAIN-VERTE-AVANT-POSE: gates_echec); echo "main avant pose : sha=$S consigne=$ms gates_echec=$ge consigne=$mg rouges=$ko"; [ "$ko" -eq 0 ] && [ "$ms" = "$S" ] && [ "$mg" = "$ge" ]'</automated>
    <fails_when>exit non nul ; ou `MAIN ROUGE` / `GATES ROUGE AILLEURS` (la règle « 4 checks verts » bloquerait toute PR : arrêt, remontée `human_needed`) ; ou `MAIN-VERTE-AVANT-POSE:` absente, périmée (autre sha) ou différente de la mesure</fails_when>
  </verify>
  <acceptance_criteria>
    - identité admin, branche B, 0 ruleset, 0 erreur CODEOWNERS, collaborateurs inchangés, merge de la phase dans `main`
    - sources d'`origin/main` conformes, deux `User` en `always`
    - `main` verte (seul rouge toléré : l'étape `check-release-tag`) ; `MAIN-VERTE-AVANT-POSE:` et `PR-EN-VOL-AVANT:` consignées et recoupées
    - retour arrière écrit ET committé sur B avant la décision ; dossier de décision imprimé
  </acceptance_criteria>
  <done>Tout est prêt pour une décision prise sur pièce, avec son retour arrière déjà écrit.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2 : décision explicite de Willy — poser maintenant, reporter, ou abandonner le volet serveur (avant tout appel d'écriture)</name>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`PR-EN-VOL-AVANT:`, `MAIN-VERTE-AVANT-POSE:`, `RETOUR-ARRIERE-ECRIT:`)
    - le dossier de décision imprimé par la Task 1
  </read_first>
  <action>L'exécutant présente le dossier de la Task 1 et s'arrête. Aucune écriture chez GitHub tant que la réponse n'est pas consignée et committée. Le planificateur et l'exécutant ne tranchent pas.</action>
  <decision>Poser maintenant les deux rulesets depuis `origin/main` — premier appel d'écriture de configuration de la phase, qui change immédiatement les conditions de merge de toute PR ouverte vers `main` ?</decision>
  <context>
    Les PR en vol (`PR-EN-VOL-AVANT:`) passent sous la règle dès la pose : 4 checks verts et branche à
    jour pour toute PR vers `main` ; revue `@picmakpro` en plus pour celles qui touchent un chemin
    CODEOWNERS (une PR ouverte par `picmakpro` ne peut pas s'auto-approuver : elle n'entrera qu'en
    contournant, tracé). Conséquences de D-02bis, écrites sans adoucir : Samuel et Willy peuvent
    pousser directement, forcer ou supprimer `main` et réécrire un tag `v*` — le serveur l'accepte et
    le trace ; tout autre acteur est refusé. Fenêtre de preuves : les plans 41-06 à 41-09 supposent
    qu'aucun merge n'arrive sur `main` pendant les mesures, hors PR de preuve — Willy prévient Samuel.
    Retour arrière : `enforcement=disabled` par l'admin, commande consignée.
  </context>
  <options>
    <option id="poser">
      <name>POSER MAINTENANT</name>
      <pros>Critère 1 atteint dans la minute ; les preuves (41-06 à 41-09) peuvent suivre ; retour arrière écrit.</pros>
      <cons>Les PR en vol changent de conditions immédiatement ; toute PR `codeowners=oui` ouverte par `picmakpro` exigera un contournement tracé.</cons>
    </option>
    <option id="reporter">
      <name>REPORTER</name>
      <pros>Laisse les PR en vol se merger ou se fermer d'abord sous les conditions actuelles ; rien ne bouge chez GitHub.</pros>
      <cons>`main` reste sans règle serveur ; le plan reprend à la Task 1 (inventaire et `main` verte relus) quand Willy le décide.</cons>
    </option>
    <option id="abandonner">
      <name>ABANDONNER LE VOLET SERVEUR</name>
      <pros>Aucune règle posée ; la source versionnée reste sur `main`, prête.</pros>
      <cons>PROT-01 reste non coché ; critères 1 à 3 non atteints ; retour au cadrage.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - une réponse explicite de Willy (ou de Samuel) est obtenue, jamais inférée
    - `POSE-DECISION:` consignée avec option, décideur, canal réel et date capturée, et committée sur B AVANT tout appel d'écriture
  </acceptance_criteria>
  <resume-signal>Répondre « POSER MAINTENANT », « REPORTER » ou « ABANDONNER LE VOLET SERVEUR », et dire si Samuel a été prévenu de la fenêtre de preuves. L'exécutant consigne `POSE-DECISION: option=<poser|reporter|abandonner> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint> fenetre_preuves_annoncee=<oui|non>` — jamais inféré — et le commite sur B avec le message « docs(41-05): POSE-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> » (vérifié : un commit ou corps de PR citant `POSE-DECISION` sans ce décideur, ce canal et cette date rougit).</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : pose par l'exécutant (deux appels d'écriture), relecture serveur (critère 1), deux entrées `User` relues, M-2, accès aux rule suites, PR en vol après la pose</name>
  <files>.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md</files>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-DECISION:`, `ACTEURS-CONTOURNEMENT:`, `PR-EN-VOL-AVANT:`)
  </read_first>
  <reversibility rating="costly">La pose se défait par `enforcement=disabled`, mais ce qu'elle a changé pour les PR en vol pendant qu'elle était active ne se défait pas.</reversibility>
  <action>
Précondition : `POSE-DECISION: option=poser` consignée ET committée (sinon arrêt, aucun appel
d'écriture). Identité `picmakpro`. `git fetch origin`.

Pose, exactement deux appels d'écriture, dans cet ordre :
`git show origin/main:.github/rulesets/main.json | gh api -X POST repos/picmakpro/vibeflow-os/rulesets --input - --jq '{id, enforcement}'`
puis la même commande avec `.github/rulesets/tags-v.json`. Si un appel échoue (422 ou autre) : NE
PAS retoucher le JSON, NE PAS réessayer par l'interface ; consigner
`POSE-ECHEC: etape=<branche|tags> sortie=<sortie intégrale sur une ligne>` et aller à la Task 4 (un
ruleset déjà posé reste tel quel jusqu'à la décision).

Relecture, pour chacun des deux ids : `gh api repos/picmakpro/vibeflow-os/rulesets/<id>` ; consigner :
- `POSE-BRANCHE: id=<id> enforcement=<lu> pose_par=picmakpro source=origin/main:<sha>` et `POSE-TAGS:` symétrique ;
- `POSE-BYPASS-RELU: branche=<User:id:mode,… triés par id, lus> tags=<idem> lu_par=picmakpro` — la
  relecture des deux entrées `User` exigée par D-02bis (« à mesurer à la pose, jamais supposé ») ;
- `POSE-MSG:` suivi des deux JSON relus, recompactés sur une ligne ;
- `POSE-CONFORME: branche=<oui|non> tags=<oui|non> contextes_egaux_source=<oui|non> rules_branches_main=<oui|non>`
  (`rules/branches/main` portant exactement `deletion`, `non_fast_forward`, `pull_request`,
  `required_status_checks` pour l'id de branche — arbitrage Samuel, AskUserQuestion session
  principale, 2026-09-17) ;
- `M2: picmakpro_branche=<current_user_can_bypass lu> picmakpro_tags=<lu> relue_picmakpro=oui samuel_branche=non_mesure samuel_tags=non_mesure bypass_users_relus=<oui|non>` ;
- `M2-VERDICT: <CONFORME|ECART>` — CONFORME si les entrées relues sont exactement les deux `User` de
  `ACTEURS-CONTOURNEMENT:` en `always`, `picmakpro` à `always` sur les deux, `POSE-CONFORME:` à
  `oui` partout ;
- `ACCES-RULE-SUITES: picmakpro=<ok|refuse> samuel-neveugall=non_mesure` (essai de
  `gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour"`) ;
- `PR-EN-VOL-APRES: numeros=<#n,…|aucune> detail=<#n:etat=<mergeStateStatus>:revue=<reviewDecision ou aucune>:fusionnee_par=<login|aucun>,…>` —
  relu quelques minutes après la pose (un `UNKNOWN` se relit) pour chaque PR de `PR-EN-VOL-AVANT:`
  et toute PR ouverte depuis ; rien n'est fait sur ces PR.
Un écart de VALEUR (champ serveur non conforme, entrée de contournement inattendue) n'est jamais
corrigé chez GitHub par l'exécutant : il est consigné tel quel et mène à la Task 4. Seule une
incohérence de CONSIGNATION se corrige ici. Commit local sur B, message « docs(41-05): rulesets
posés et relus, contournement nommé relu (D-02bis — arbitrage Willy, AskUserQuestion session
principale, 2026-09-23) ; REGLES_MAIN_FORCE_PUSH_SUPPRESSION=INCLUSES — arbitrage Samuel,
AskUserQuestion session principale, 2026-09-17 ». Rejouer `gates` et `tests`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; u=$(gh api user --jq .login); pd=$(val POSE-DECISION: option); T=$(mktemp -d); git show HEAD:"$PH/41-PREUVES.md" > "$T/h"; pdc=$(awk "\$1==\"POSE-DECISION:\" && /option=poser/ {c++} END{print c+0}" "$T/h"); IB=$(val POSE-BRANCHE: id); IT=$(val POSE-TAGS: id); n=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq length); ids=$(gh api repos/picmakpro/vibeflow-os/rulesets --jq "[.[].id] | sort | map(tostring) | join(\",\")"); att=$(printf "%s\n%s\n" "$IB" "$IT" | LC_ALL=C sort -n | awk "BEGIN{ORS=\"\"} NR>1{print \",\"} {print}"); echo "critere 1 : identite=$u decision=$pd decision_committee=$pdc rulesets=$n ids_serveur=$ids ids_consignes=$att"; [ "$u" = picmakpro ] && [ "$pd" = poser ] && [ "$pdc" -ge 1 ] && [ "$n" -eq 2 ] && [ "$ids" = "$att" ]'</automated>
    <fails_when>exit non nul ; ou identité autre que `picmakpro` ; ou `POSE-DECISION` absente, autre que `poser`, ou non committée (pose sans décision explicite) ; ou `rulesets=` différent de 2 (critère 1 non atteint, ou ruleset parasite) ; ou ids serveur différents des ids consignés</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; IB=$(val POSE-BRANCHE: id); IT=$(val POSE-TAGS: id); SA=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); PA=$(val ACTEURS-CONTOURNEMENT: picmakpro); T=$(mktemp -d); gh api "repos/picmakpro/vibeflow-os/rulesets/$IB" > "$T/b"; gh api "repos/picmakpro/vibeflow-os/rulesets/$IT" > "$T/g"; E="([{actor_id: \$sa, actor_type: \"User\", bypass_mode: \"always\"}, {actor_id: \$pa, actor_type: \"User\", bypass_mode: \"always\"}] | sort_by(.actor_id))"; ob=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "(.rules | map({(.type): (.parameters // {})}) | add) as \$r | {cible: (.target==\"branch\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/heads/main\"]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]), approbations0: (\$r.pull_request.required_approving_review_count==0), code_owner: (\$r.pull_request.require_code_owner_review==true), stale_defaut_false: (\$r.pull_request.dismiss_stale_reviews_on_push==false), methodes: ((\$r.pull_request.allowed_merge_methods | sort)==[\"merge\",\"rebase\",\"squash\"]), strict: (\$r.required_status_checks.strict_required_status_checks_policy==true), integration: ([\$r.required_status_checks.required_status_checks[].integration_id] | unique == [15368]), release_tag_non_requis: ([\$r.required_status_checks.required_status_checks[].context | ascii_downcase | contains(\"check-release-tag\")] | any | not), bypass_relu_deux_users_always: ((.bypass_actors | sort_by(.actor_id | tonumber)) == $E), picmakpro_always: (.current_user_can_bypass==\"always\")} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$T/b"); og=$(jq -r --argjson sa "$SA" --argjson pa "$PA" "{cible: (.target==\"tag\" and .enforcement==\"active\" and .conditions.ref_name.include==[\"refs/tags/v*\"]), types: ([.rules[].type] | sort == [\"deletion\",\"non_fast_forward\",\"update\"]), bypass_relu_deux_users_always: ((.bypass_actors | sort_by(.actor_id | tonumber)) == $E), picmakpro_always: (.current_user_can_bypass==\"always\")} | to_entries | map(\"\(.key)=\(.value)\") | join(\" \")" "$T/g"); jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" "$T/b" | LC_ALL=C sort > "$T/cs"; git show origin/main:.github/rulesets/main.json | jq -r ".rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context" | LC_ALL=C sort > "$T/cj"; [ "$(awk "END{print NR}" "$T/cj")" -eq 4 ] || { echo "contextes source illisibles"; exit 1; }; ce=$(cmp -s "$T/cs" "$T/cj" && echo true || echo false); rb=$(gh api repos/picmakpro/vibeflow-os/rules/branches/main --jq "[.[] | select(.ruleset_id == $IB) | .type] | sort | unique == [\"deletion\",\"non_fast_forward\",\"pull_request\",\"required_status_checks\"]"); echo "serveur branche : $ob contextes_egaux_source=$ce rules_branches_main=$rb"; echo "serveur tags : $og"; case "$ob $og" in *=false*) exit 1;; esac; [ "$ce" = true ] && [ "$rb" = true ]'</automated>
    <fails_when>exit non nul ; ou un `=false` côté serveur — `bypass_relu_deux_users_always=false` signifie que les entrées relues ne sont pas exactement les deux `User` mesurés en `always` (D-02bis non posé tel qu'arbitré) ; `picmakpro_always=false` : `current_user_can_bypass` de l'admin autre que `always` ; `release_tag_non_requis=false` ; ou `contextes_egaux_source=false` ; ou `rules_branches_main=false` (règle non effective sur `main`, ou un des quatre types exigés manque). Rouge de VALEUR : consigner, puis Task 4 (décision humaine), jamais de correction chez GitHub par l'exécutant</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; IB=$(val POSE-BRANCHE: id); IT=$(val POSE-TAGS: id); rb=$(gh api "repos/picmakpro/vibeflow-os/rulesets/$IB" --jq "[.bypass_actors | sort_by(.actor_id | tonumber)[] | \"\(.actor_type):\(.actor_id):\(.bypass_mode)\"] | join(\",\")"); rt=$(gh api "repos/picmakpro/vibeflow-os/rulesets/$IT" --jq "[.bypass_actors | sort_by(.actor_id | tonumber)[] | \"\(.actor_type):\(.actor_id):\(.bypass_mode)\"] | join(\",\")"); cb=$(val POSE-BYPASS-RELU: branche); ct=$(val POSE-BYPASS-RELU: tags); mb=$(gh api "repos/picmakpro/vibeflow-os/rulesets/$IB" --jq .current_user_can_bypass); mt=$(gh api "repos/picmakpro/vibeflow-os/rulesets/$IT" --jq .current_user_can_bypass); pb=$(val M2: picmakpro_branche); pt=$(val M2: picmakpro_tags); sb=$(val M2: samuel_branche); st=$(val M2: samuel_tags); ss=$(gh api "repos/picmakpro/vibeflow-os/rulesets/rule-suites?time_period=hour" > /dev/null 2>&1 && echo ok || echo refuse); sr=$(val ACCES-RULE-SUITES: picmakpro); echo "consignation : bypass_branche=$cb (API $rb) bypass_tags=$ct (API $rt) picmakpro=$pb/$pt (API $mb/$mt) samuel=$sb/$st acces_rule_suites=$sr (mesure $ss)"; [ "$cb" = "$rb" ] && [ "$ct" = "$rt" ] && [ "$pb" = "$mb" ] && [ "$pt" = "$mt" ] && [ "$sb" = non_mesure ] && [ "$st" = non_mesure ] && [ "$sr" = "$ss" ]'</automated>
    <fails_when>exit non nul ; ou entrées de contournement consignées différentes de la relecture API ; ou `current_user_can_bypass` consigné différent de celui relu ; ou une valeur `samuel` consignée autre que `non_mesure` (aucun jeton de Samuel sur ce poste : une valeur serait inventée) ; ou accès aux rule suites consigné différent de la mesure. Ces rouges sont des erreurs de CONSIGNATION, corrigées dans cette tâche</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; val() { awk -v k="$1" -v f="$2" "\$1==k { for (i=2;i<=NF;i++) { p=index(\$i, \"=\"); if (p>0 && substr(\$i,1,p-1)==f) { print substr(\$i,p+1); exit } } }" "$PH/41-PREUVES.md"; }; SA=$(val ACTEURS-CONTOURNEMENT: samuel-neveugall); PA=$(val ACTEURS-CONTOURNEMENT: picmakpro); X="User:$SA:always,User:$PA:always"; cb=$(val POSE-BYPASS-RELU: branche); ct=$(val POSE-BYPASS-RELU: tags); pb=$(val M2: picmakpro_branche); pt=$(val M2: picmakpro_tags); cf=$(awk "\$1==\"POSE-CONFORME:\" && /branche=oui/ && /tags=oui/ && /contextes_egaux_source=oui/ && /rules_branches_main=oui/ {c++} END{print c+0}" "$PH/41-PREUVES.md"); ok=oui; [ "$cb" = "$X" ] || ok=non; [ "$ct" = "$X" ] || ok=non; [ "$pb" = always ] || ok=non; [ "$pt" = always ] || ok=non; [ "$cf" -eq 1 ] || ok=non; att=$([ "$ok" = oui ] && echo CONFORME || echo ECART); v=$(awk "\$1==\"M2-VERDICT:\" {print \$2; exit}" "$PH/41-PREUVES.md"); ec=$(awk "\$1==\"POSE-ECHEC:\" {c++} END{print c+0}" "$PH/41-PREUVES.md"); echo "M-2 verdict : attendu_bypass=$X branche=$cb tags=$ct picmakpro=$pb/$pt pose_conforme=$cf pose_echec=$ec recalcule=$att consigne=$v"; [ "$v" = "$att" ] && [ "$v" = CONFORME ] && [ "$ec" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou verdict consigné différent du verdict recalculé (erreur de consignation, corrigée ici) ; ou verdict `ECART` — entrées de contournement relues différentes des deux `User` en `always`, admin autre que `always`, pose non conforme — ou une ligne `POSE-ECHEC:` : consigné tel quel, AUCUNE correction, arrêt sur la Task 4</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; av=$(awk "\$1==\"PR-EN-VOL-AVANT:\" { for (i=2;i<=NF;i++) if (index(\$i, \"numeros=\")==1) { print substr(\$i, 9); exit } }" "$PH/41-PREUVES.md"); ap=$(awk "\$1==\"PR-EN-VOL-APRES:\" { for (i=2;i<=NF;i++) if (index(\$i, \"detail=\")==1) { print substr(\$i, 8); exit } }" "$PH/41-PREUVES.md"); [ -n "$ap" ] || { echo "PR-EN-VOL-APRES absente"; exit 1; }; k=0; n=0; for x in $(printf "%s" "$av" | tr "," " "); do [ "$x" = aucune ] && continue; n=$((n+1)); N=${x#\#}; case ",$ap" in *",#$N:"*) ;; *) echo "PR EN VOL NON RAPPORTEE : $x"; k=$((k+1)); continue;; esac; mb=$(gh pr view "$N" --repo picmakpro/vibeflow-os --json mergedBy --jq ".mergedBy.login // \"aucun\""); cm=$(printf "%s" ",$ap" | awk -v n="#$N:" "{ p = index(\$0, \",\" n); if (p) { s = substr(\$0, p + 1); q = index(s, \",\"); if (q) s = substr(s, 1, q - 1); r = s; sub(/.*fusionnee_par=/, \"\", r); print r } }"); [ "$cm" = "$mb" ] || { echo "FUSION CONSIGNEE DIFFERENTE : $x consigne=$cm api=$mb"; k=$((k+1)); }; done; echo "PR en vol : avant=$av rapportees=$n en_defaut=$k"; [ "$k" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `PR-EN-VOL-APRES:` absente ; ou une PR de l'inventaire d'avant la pose non rapportée après ; ou un auteur de merge consigné différent de celui lu par l'API (une PR en vol mergée pendant la pose doit être rapportée telle quelle, jamais passée sous silence)</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job gates > "$T/g" 2>&1 || rc=$?; awk "/^REJEU gates|^BILAN REJEU/" "$T/g"; r=$(awk "/^BILAN REJEU gates :/{print \$5}" "$T/g"); echo "gates rc=$rc rejouees=${r:-0}"; [ "$rc" -eq 0 ] && [ "${r:-0}" -ge 10 ]'</automated>
    <fails_when>exit non nul ; ou `gates rc=` différent de 0 ; ou `rejouees=` inférieur à 10</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^REJEU tests|^BILAN REJEU|^== bilan : |FAIL\\(rc=|SKIP/" "$T/t"; b=$(awk "/^== bilan : /{f=1; for(i=1;i<=NF;i++){ if (\$i ~ /^suite/) n=\$(i-1); if (\$i ~ /^échec/) m=\$(i-1) } } END{ if (!f) print \"absent\"; else print n+0, m+0 }" "$T/t"); d=$(find plugin scripts -type f -path "*/tests/test-*.sh" | awk "END{print NR}"); echo "tests rc=$rc bilan(N M)=$b decouverte_independante=$d"; [ "$rc" -eq 0 ] && [ "$b" != "absent" ] && set -- $b && [ "$1" -gt 0 ] && [ "$1" -eq "$d" ] && [ "$2" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou bilan absent, N = 0, N différent de la découverte indépendante, ou M différent de 0</fails_when>
    <automated>bash --noprofile --norc -e -c 'PH=.planning/phases/VFDO-41-posture-de-protection-du-d-p-t; B=$(git merge-base HEAD origin/main); T=$(mktemp -d); printf "SENTINELLE\t\t\t\n" > "$T/arb"; awk "/^REGLES_MAIN_FORCE_PUSH_SUPPRESSION_ARBITRAGE:/ { for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } } END { gsub(/-/, \" \", c); if (w != \"\" && c != \"\" && d ~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) print \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\t\" w \"\t\" c \"\t\" d }" "$PH/41-01-PLAN.md" >> "$T/arb"; if [ -f "$PH/41-PREUVES.md" ]; then awk "\$1 ~ /-DECISION:\$/ { k=substr(\$1,1,length(\$1)-1); w=\"\"; c=\"\"; d=\"\"; for (i=2;i<=NF;i++) { if (index(\$i, \"decideur=\")==1) w=substr(\$i,10); if (index(\$i, \"canal=\")==1) c=substr(\$i,7); if (index(\$i, \"date=\")==1) d=substr(\$i,6) } gsub(/-/, \" \", c); if (d !~ /^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\$/) d=\"\"; print k \"\t\" w \"\t\" c \"\t\" d }" "$PH/41-PREUVES.md" >> "$T/arb"; fi; git log --format="@@COMMIT@@%n%s%n%b" "$B"..HEAD > "$T/l"; awk -F"\t" "NR==FNR { key[++na]=\$1; dec[na]=\$2; can[na]=\$3; dat[na]=\$4; next } /^@@COMMIT@@\$/ { if (s != \"\") chk(); s=\"\"; m=\"\"; getline s; m=s; next } { m = m \" \" \$0 } function chk() { if (s ~ /\\(41-[0-9][0-9]\\)/) { n++; juge(m, s) } } function juge(m, lib,   j, ok, p, t) { if (index(m, \"D-02bis\") && index(m, \"arbitrage Willy, AskUserQuestion session principale, 2026-09-23\") == 0) { print \"D-02bis SANS CANAL NI DATE : \" lib; v++ } t = m; gsub(/D-02bis/, \"\", t); if (t ~ /D-(0[1-9]|10)([^0-9]|\$)/ && index(m, \"arbitrage Samuel, AskUserQuestion session principale, 2026-09-17\") == 0) { print \"SANS CANAL NI DATE (D-01..D-10) : \" lib; v++ } if (index(m, \"REGLES_MAIN_FORCE_PUSH_SUPPRESSION\") || index(m, \"-DECISION\")) { ok=0; for (j=1;j<=na;j++) if (key[j] != \"SENTINELLE\" && (p = index(m, key[j])) && dec[j] != \"\" && can[j] != \"\" && dat[j] != \"\" && index(substr(m, p), \"arbitrage \" dec[j] \", \" can[j] \", \" dat[j])) ok=1; if (!ok) { print \"ARBITRAGE CITE SANS CANAL NI DATE CAPTURES : \" lib; v++ } } } END { if (s != \"\") chk(); print \"messages de commit : commits_de_plan=\" n+0 \" sans_canal=\" v+0; exit (v > 0 || n == 0) }" "$T/arb" "$T/l"'</automated>
    <fails_when>exit non nul ; ou `D-02bis SANS CANAL NI DATE` ; ou `SANS CANAL NI DATE (D-01..D-10)` ; ou `ARBITRAGE CITE SANS CANAL NI DATE CAPTURES` (dont un commit citant `POSE-DECISION` sans le décideur, le canal et la date consignés) ; ou `commits_de_plan=0`</fails_when>
  </verify>
  <acceptance_criteria>
    - pose précédée d'une `POSE-DECISION: option=poser` committée ; exactement deux appels d'écriture, depuis `origin/main`
    - critère 1 : 2 rulesets actifs, ids consignés = ids serveur ; champs arbitrés conformes côté serveur, contextes = source, règles effectives sur `main`, aucun contexte `check-release-tag`
    - D-02bis relu : deux `User` en `always` sur chaque ruleset, `picmakpro` à `always`, valeurs de Samuel `non_mesure` ; `M2-VERDICT` recalculé = consigné
    - accès aux rule suites mesuré ; PR en vol rapportées après la pose, aucune touchée par la phase
    - `M2-VERDICT: CONFORME` sans `POSE-ECHEC:` pour poursuivre ; sinon Task 4
  </acceptance_criteria>
  <done>Les règles sont posées sur décision, relues champ par champ, le contournement nommé est mesuré côté serveur, et l'effet sur les PR en vol est rapporté.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4 : décision humaine si et seulement si `M2-VERDICT: ECART` ou `POSE-ECHEC:` (sinon tâche sans objet, consignée « non déclenchée » dans le SUMMARY)</name>
  <read_first>
    - .planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-PREUVES.md (`POSE-*`, `M2:`, `M2-VERDICT:`, `PR-EN-VOL-APRES:`)
  </read_first>
  <action>L'exécutant présente l'écart mesuré et s'arrête ; aucune correction chez GitHub ni du JSON sans la réponse.</action>
  <decision>La pose a échoué, ou les rulesets posés ne se comportent pas comme arbitré (entrées de contournement relues, `current_user_can_bypass`, champ non conforme) : que faire des rulesets avant toute preuve ?</decision>
  <context>
    Lire `POSE-ECHEC:` (s'il existe), `POSE-CONFORME:`, `POSE-BYPASS-RELU:`, `M2:`, `M2-VERDICT:` et
    `POSE-MSG:` dans `41-PREUVES.md`. Aucune PR de preuve n'existe encore (PR-P est ouverte en 41-06).
    Une source fausse se corrige par une PR vers `main`, jamais à la main chez GitHub.
  </context>
  <options>
    <option id="garder-et-cadrer">
      <name>GARDER LES RULESETS ET CADRER</name>
      <pros>Rien ne bouge chez GitHub sans arbitrage ; l'écart est consigné avec sa mesure.</pros>
      <cons>Des règles non conformes restent actives ; les plans 41-06 et suivants attendent.</cons>
    </option>
    <option id="retour-arriere">
      <name>RETOUR ARRIÈRE</name>
      <pros>Rien d'imposé à tort : l'admin joue, pour chaque id posé, `gh api -X PUT repos/picmakpro/vibeflow-os/rulesets/<id> -f enforcement=disabled` ; témoin `gh api repos/picmakpro/vibeflow-os/rules/branches/main` → `[]`.</pros>
      <cons>`main` redevient sans règle serveur jusqu'à une nouvelle pose ; la phase repasse par 41-05 (après correction de la source par PR si besoin).</cons>
    </option>
    <option id="accepter-et-documenter">
      <name>ACCEPTER ET DOCUMENTER</name>
      <pros>Les preuves avancent ; la valeur mesurée est écrite dans ADR-072 § Mesures (plan 41-10).</pros>
      <cons>N'a de sens que si la pose a eu lieu et que l'écart ne touche pas les deux entrées `User` ; engage un arbitrage daté et attribué.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - réponse explicite obtenue, jamais inférée ; `M2-DECISION:` consignée et committée
  </acceptance_criteria>
  <resume-signal>Répondre « non déclenchée », « GARDER LES RULESETS ET CADRER », « RETOUR ARRIÈRE » ou « ACCEPTER ET DOCUMENTER ». Après la réponse, l'exécutant consigne `M2-DECISION: option=<id> decideur=<Willy|Samuel> canal=<canal réel, tirets pour les espaces> date=<AAAA-MM-JJ de la réponse, capturée au checkpoint>` — jamais inféré — et le commite sur B avec le message « docs(41-05): M2-DECISION consignée — arbitrage <décideur>, <canal réel>, <date capturée> ».</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exécutant sous jeton admin → configuration GitHub | seul geste d'écriture de règles de la phase, derrière une décision explicite |
| règle posée → toutes les PR vers `main` | une règle posée change les conditions de toute PR ouverte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-18 | Tampering | JSON modifié entre merge et pose | high | mitigate | pose par `git show origin/main:… \| gh api --input -` ; conformité relue côté serveur ; échec jamais contourné en retouchant le JSON |
| T-41-19 | Denial of service | règle posée sur des PR en vol | high | mitigate | inventaire `PR-EN-VOL-AVANT:` présenté à la décision ; option « REPORTER » ; état `PR-EN-VOL-APRES:` rapporté ; aucune PR en vol touchée |
| T-41-20 | Elevation of privilege | contournement élargi à la pose | high | mitigate | entrées `bypass_actors` relues depuis `GET /rulesets/{id}` et comparées aux deux `User` mesurés (`POSE-BYPASS-RELU:`) |
| T-41-21 | Denial of service | règle fautive bloquant tout merge | medium | mitigate | `main` verte exigée avant la pose ; retour arrière écrit et committé avant la décision, joué sur décision humaine |
| T-41-22 | Elevation of privilege | exécutant sous jeton admin | high | mitigate | exactement deux appels d'écriture, après `POSE-DECISION: option=poser` committée (vérifié) ; toute autre écriture de configuration exclue du plan |
| T-41-47 | Elevation of privilege | acteur de contournement non arbitré (rôle, `exempt`) | high | mitigate | `M2-VERDICT: ECART` sur toute entrée autre que les deux `User` en `always` ; arrêt sur décision (Task 4) |
</threat_model>

<verification>
Pose décidée explicitement, deux rulesets actifs et conformes côté serveur, deux entrées `User`
relues, accès aux rule suites mesuré, PR en vol rapportées, rejeux verts.
</verification>

<success_criteria>
Critère de succès 1 atteint et prouvé par relecture serveur ; D-02bis mesuré tel que posé.
</success_criteria>

<output>
Create `.planning/phases/VFDO-41-posture-de-protection-du-d-p-t/41-05-SUMMARY.md` when done — committé sur la branche B. Le SUMMARY recopie `PR-EN-VOL-AVANT:` et `PR-EN-VOL-APRES:` (rapport de l'effet de la pose sur les PR en vol).
</output>

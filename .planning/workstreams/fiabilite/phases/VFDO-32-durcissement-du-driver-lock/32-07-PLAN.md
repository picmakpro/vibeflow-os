---
phase: 32-durcissement-du-driver-lock
plan: 07
type: execute
wave: 4
depends_on: [32-02, 32-04]
files_modified:
  - plugin/dev-orchestrator/agents/vf-dev-manager.md
  - plugin/dev-orchestrator/references/mission-flow.md
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/CHANGELOG.md
  - plugin/dev-orchestrator/module.json
  - plugin/design-orchestrator/agents/vf-design-manager.md
  - plugin/design-orchestrator/VERSION
  - plugin/design-orchestrator/CHANGELOG.md
  - plugin/design-orchestrator/module.json
  - plugin/design-orchestrator/README.md
  - plugin/content-bundle/agents/vf-content-manager.md
  - plugin/content-bundle/VERSION
  - plugin/content-bundle/CHANGELOG.md
  - plugin/content-bundle/module.json
  - plugin/content-bundle/README.md
  - plugin/business-pilot-bundle/agents/vf-business-manager.md
  - plugin/business-pilot-bundle/VERSION
  - plugin/business-pilot-bundle/CHANGELOG.md
  - plugin/business-pilot-bundle/module.json
  - plugin/business-pilot-bundle/README.md
  - plugin/growth-bundle/agents/vf-growth-manager.md
  - plugin/growth-bundle/VERSION
  - plugin/growth-bundle/CHANGELOG.md
  - plugin/growth-bundle/module.json
  - plugin/growth-bundle/README.md
autonomous: true
requirements: [LOCK-04, LOCK-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ACTIVE le 2026-08-17 par arbitrage humain (GEL-1, option B — synchronisation COMPLETE) : ce plan n'est PLUS conditionnel. Il remplace la lecture de D-32-07 (« conductor seul, rien d'autre a toucher ») pour ce perimetre precis — sept fichiers dans quatre modules tiers, plus les quatre bumps de version qui en decoulent"
    - "Aucun des sept fichiers ne decrit plus l'ANCIEN contrat d'`acquire` (recuperation implicite d'un lock perime, `recovered:true` comme chemin nominal) apres ce plan — chaque mention est soit retiree, soit remplacee par la doctrine `takeover`/`reclaim` du plan 32-02"
    - "Chaque manager qui prescrit le verrou comme premier geste de mission (les cinq agents) connait desormais le verbe `takeover` — un manager qui rencontre un lock perime sait quoi faire sans consulter un autre document"
    - "`plugin/dev-orchestrator/references/mission-flow.md` est corrige EN PRIORITE (le fichier que `vf-dev-manager.md` lit reellement, D-32-07 amende le confirme — team-kernel.md n'est PAS dans sa chaine de lecture directe)"
    - "Les CINQ modules tiers touches (dev-orchestrator, design-orchestrator, content-bundle, business-pilot-bundle, growth-bundle) bumpent chacun leur propre VERSION/CHANGELOG/module.json/en-tete README (quand il en declare une), gates par check-version-sync.sh — consequence assumee du perimetre elargi, jamais un bump silencieux"
    - "Aucune ligne de CODE n'est touchee par ce plan : uniquement de la doctrine d'agent (Markdown) et des fichiers de version/changelog"
  artifacts:
    - "plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/references/mission-flow.md, plugin/dev-orchestrator/README.md : doctrine takeover/reclaim, plus VERSION/CHANGELOG"
    - "plugin/design-orchestrator/agents/vf-design-manager.md, plugin/content-bundle/agents/vf-content-manager.md, plugin/business-pilot-bundle/agents/vf-business-manager.md, plugin/growth-bundle/agents/vf-growth-manager.md : doctrine takeover/reclaim, plus VERSION/CHANGELOG de chaque module"
  key_links:
    - "driver-lock.sh takeover/reclaim (plan 32-02) -> doctrine des cinq managers : la source des deux verbes que ce plan propage, jamais reinventee ni reformulee differemment d'un fichier a l'autre"
    - "team-kernel.md §Jeton de fence (plan 32-04) <- vf-dev-manager.md ne le lit PAS directement (BL-5, corrige au 32-04) : ce plan est le pont qui fait atteindre `dev-orchestrator` malgre cette absence de lecture directe"
    - "check-version-sync.sh -> les cinq VERSION/CHANGELOG/README de ce plan : le gate qui rendrait rouge un bump partiel (module touche mais version non incrementee, ou l'inverse)"
  prohibitions:
    - "Ne JAMAIS toucher au fichier VERSION racine, au manifeste du plugin, ni au manifeste de la marketplace — ce plan bumpe des MODULES, jamais une release globale (geste humain gate, hors perimetre)"
    - "Ne JAMAIS reformuler la doctrine `takeover`/`reclaim` differemment d'un manager a l'autre — copier la meme formulation courte dans les cinq fichiers, sur le patron deja etabli par chaque fichier pour `acquire`/`heartbeat`/`release`"
    - "Ne PAS toucher au code ni aux tests d'aucun module — perimetre strictement documentaire"
    - "Ne pas laisser une seule occurrence de `recovered:true`/« lock perime -> acquire recupere » survivre dans les sept fichiers apres ce plan"
---

<objective>
Fermer l'angle mort que D-32-07 avait laisse ouvert et que la re-validation externe du 2026-08-17 a
nomme (GEL-1) : cinq managers de mission, dans quatre modules tiers, prescrivent `acquire` comme
premier geste et documentent l'ANCIEN contrat du verrou (recuperation implicite d'un lock perime).
Depuis les plans 32-01/32-02, cette recuperation n'existe plus — `acquire` REFUSE sur un lock perime
et nomme `takeover` comme marche a suivre. Sans ce plan, un manager qui rencontre un lock perime
apres la Phase 32 ne connait pas ce verbe : la mission gele, silencieusement, en dehors de tout test
de la phase (aucune suite de `conductor` n'exerce la doctrine d'un manager).

Purpose: Samuel a tranche l'option B (synchronisation COMPLETE) le 2026-08-17, plus large que
l'option minimale (le seul champ `hint` in-band du refus JSON, deja livre par le plan 32-02). Le
champ `hint` reste la garantie MACHINE qui atteint tout appelant sans qu'il ait lu de doctrine ; ce
plan-ci est le geste DOCUMENTAIRE qui evite qu'un manager la decouvre pour la premiere fois en
pleine mission.

Output: sept fichiers corriges dans cinq modules (dev-orchestrator en porte trois), et les cinq
bumps de version/changelog qui en decoulent — jamais un bump silencieux, jamais une ligne de code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-CONTEXT.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-02-SUMMARY.md
@.planning/phases/VFDO-32-durcissement-du-driver-lock/32-04-SUMMARY.md
@plugin/conductor/references/team-kernel.md
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : dev-orchestrator — les trois fichiers que `vf-dev-manager` lit REELLEMENT (priorite du GEL-1)</name>
  <files>plugin/dev-orchestrator/agents/vf-dev-manager.md, plugin/dev-orchestrator/references/mission-flow.md, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/CHANGELOG.md, plugin/dev-orchestrator/module.json</files>
  <read_first>
    - plugin/dev-orchestrator/agents/vf-dev-manager.md:52-55 (le geste d'acquisition actuel, avec `recovered:true` decrit comme chemin nominal — c'est le passage a corriger)
    - plugin/dev-orchestrator/references/mission-flow.md:44-62 (meme doctrine, plus detaillee — "acquire qui elague et re-acquiert" — c'est la formulation la plus explicite de l'ancien contrat, et le fichier que `vf-dev-manager.md` lit reellement, pas `team-kernel.md`, BL-5 corrige au plan 32-04)
    - plugin/dev-orchestrator/README.md:54 et :298 (les deux mentions du kernel consomme depuis `conductor`)
    - plugin/conductor/scripts/driver-lock.sh (etat apres 32-02 : `acquire` refuse sur lock perime avec `reason: stale-requires-takeover` et un champ `hint` ; verbes `takeover --owner=<id> [--step=<etape>]` et `reclaim --owner=<id>`)
    - .planning/phases/VFDO-32-durcissement-du-driver-lock/32-04-SUMMARY.md (le nom exact du trailer `Fence:` et la commande de lecture de la generation, si ce plan a ete execute avant celui-ci)
  </read_first>
  <behavior>
    - V1 : `vf-dev-manager.md` ne contient plus la sous-chaine `recovered:true` ; il decrit `acquired:false` avec `reason: stale-requires-takeover` comme le refus REEL, et nomme `takeover --owner=<id> [--step=<etape>]` comme la commande a executer sur ce refus.
    - V0 (F4, re-validation externe #2) : `vf-dev-manager.md` est mesure a EXACTEMENT 250 lignes au
      moment de ce plan — le plafond ADR-029, et rien dans `check-agents.sh` ne compte les lignes
      (verifie). Toute addition nette y franchit le plafond SILENCIEUSEMENT, sans qu'aucun gate
      machine ne le voie. La correction du couple `acquired:false`/`recovered:true` plus la phrase
      `reclaim` doit donc REMPLACER un volume de texte au moins equivalent (la description perimee
      de la recuperation implicite, qu'elle retire) — bilan NET NEUTRE ou negatif sur le fichier,
      jamais une simple addition.
    - V2 : `mission-flow.md` ne decrit plus `acquire` comme « elague et re-acquiert » un lock perime ; la meme correction que V1 y est portee, dans le style dense du fichier.
    - V3 : les deux fichiers mentionnent `reclaim --owner=<id>` pour le cas ou l'identite de session du manager a change (`/clear`, reprise de session) sur un lock qu'il tient toujours.
    - V4 : `plugin/dev-orchestrator/README.md` ne decrit plus de recuperation implicite ; ses deux mentions du kernel restent factuellement exactes apres 32-02.
    - V5 : `plugin/dev-orchestrator/VERSION` est bumpe en PATCH (doctrine d'agent corrigee pour rester exacte, pas de nouvelle capacite portee par ce module) ; le CHANGELOG porte une entree en tete qui nomme la Phase 32 et le motif (doctrine desynchronisee par le retrait de la recuperation implicite du verrou).
    - V9 (LOCK-05, BLOQUANT de la re-validation externe #2) : `mission-flow.md` ecrit la convention
      `Fence: <generation>` elle-meme — pas seulement le fait que `takeover` existe. `team-kernel.md`
      (plan 32-04) reste la source canonique lue par les quatre autres managers ; `mission-flow.md`
      est le SEUL fichier que `vf-dev-manager` lit reellement (BL-5) — sans cette propagation-ci,
      LOCK-05 est structurellement inatteignable pour `dev-orchestrator`, exactement le trou que
      32-04 nomme et delegue explicitement a ce plan.
  </behavior>
  <action>
1. Dans `vf-dev-manager.md` (lignes 52-55) : REMPLACER (pas ajouter) la description du couple
   `acquired:false`/`recovered:true` par la doctrine reelle post-32-02 — `acquired:false` avec
   `reason: stale-requires-takeover` est un refus qui NOMME la commande de reprise dans son champ
   `hint` ; le manager execute cette commande (`takeover --owner=<id> --step=<etape>`) plutot que de
   remonter systematiquement a l'humain sur ce seul motif (la remontee reste correcte pour
   `reason: held`, un lock VIVANT tenu par une autre mission). Ajouter une phrase sur `reclaim` pour
   le cas ou l'identifiant de session du manager a change sur un lock qu'il tient encore. Fichier a
   EXACTEMENT 250 lignes avant ce plan (F4, ADR-029, aucun gate machine ne l'attrape) : le
   remplacement doit rester NET NEUTRE ou negatif — compter les lignes du passage retire et ne pas
   depasser ce budget en ecrivant le remplacement ; couper une reformulation plus longue plutot que
   de laisser le fichier franchir 250 lignes.

2. Meme correction dans `mission-flow.md` (lignes 44-62), a la densite et au style du document —
   c'est le fichier que `vf-dev-manager.md` lit reellement (le lien `team-kernel.md` de la
   description de l'agent n'est pas une lecture directe, BL-5 de la re-validation externe l'a
   mesure : `grep -c "team-kernel" plugin/dev-orchestrator/agents/vf-dev-manager.md` rend `0`). Ne
   pas se contenter d'un renvoi vers `team-kernel.md` : ce fichier-ci doit porter la doctrine
   complete, puisque c'est lui qui est reellement lu.

3. Verifier `README.md:54` et `:298` : corriger si l'une de ces deux mentions decrit une
   recuperation automatique ; les laisser inchangees sinon (elles sont deja factuelles dans leur
   forme actuelle si elles ne mentionnent que la consommation du kernel).

4. Bumper `plugin/dev-orchestrator/VERSION` en PATCH et ajouter l'entree correspondante en tete de
   `plugin/dev-orchestrator/CHANGELOG.md`, a la forme du fichier (titre versionne et date, mention
   Patch avec son motif, description en prose dense) : doctrine du verrou resynchronisee avec le
   retrait de la recuperation implicite (Phase 32, plans 32-01/32-02), verbes `takeover`/`reclaim`
   nommes.

5. `check-version-sync.sh` compare CHAQUE module sur deux triades independantes (§6 et §8 du
   script) : reporter la MEME nouvelle valeur de version dans `plugin/dev-orchestrator/module.json`
   (champ `.version`) ET dans l'en-tete `**Version** : vX.Y.Z` de `plugin/dev-orchestrator/README.md`
   (deja present a la ligne qui porte `**Version** : v2.17.2` au moment de ce plan) — omettre l'un
   ou l'autre fait echouer le gate meme si `VERSION`/`CHANGELOG.md` sont corrects.

6. LOCK-05 (BLOQUANT, re-validation externe #2) : dans `mission-flow.md`, a l'endroit corrige par
   l'etape 2, ecrire EXPLICITEMENT la convention `Fence: <generation>` — le trailer que le
   `vf-dev-manager` doit commiter apres un `takeover`/`reclaim` pour tracer sous quelle generation
   du lock son commit est ne, et la commande de lecture de la generation courante (meme forme que
   `team-kernel.md`, plan 32-04, dont ce fichier reprend la convention sans la reinventer). Ne pas
   se contenter de nommer `takeover` : ecrire la convention `Fence:` elle-meme est la partie que
   32-04 delegue explicitement a ce plan (32-04-PLAN.md:31, BL-5) — sans elle, aucun manager de
   `dev-orchestrator` ne sait quoi commiter, et LOCK-05 reste inatteignable pour ce module par
   construction, quel que soit le sort des quatre autres.
  </action>
  <acceptance_criteria>
    - `grep -c 'recovered' plugin/dev-orchestrator/agents/vf-dev-manager.md` rend `0`.
    - `grep -c 'recovered' plugin/dev-orchestrator/references/mission-flow.md` rend `0`.
    - `grep -c 'takeover' plugin/dev-orchestrator/agents/vf-dev-manager.md` rend au moins `1`, et la meme commande sur `mission-flow.md` rend au moins `1`.
    - `grep -c 'reclaim' plugin/dev-orchestrator/agents/vf-dev-manager.md` rend au moins `1`, et la meme commande sur `mission-flow.md` rend au moins `1`.
    - `bash scripts/check-version-sync.sh` sort 0.
    - La version du CHANGELOG correspond au fichier VERSION : la premiere entree du CHANGELOG porte la meme valeur que `plugin/dev-orchestrator/VERSION`.
    - `grep -c 'Fence:' plugin/dev-orchestrator/references/mission-flow.md` rend au moins `1` (LOCK-05 : sans cette ligne, la convention `Fence:` n'atteint jamais `dev-orchestrator`, seul module dont le manager ne lit pas `team-kernel.md` directement).
    - `wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md` rend au plus `250` (F4, ADR-029 : le fichier est deja au plafond avant ce plan, aucune addition nette ne doit le franchir).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && [ "$(grep -c 'recovered' plugin/dev-orchestrator/agents/vf-dev-manager.md)" -eq 0 ] && [ "$(grep -c 'takeover' plugin/dev-orchestrator/references/mission-flow.md)" -ge 1 ] && [ "$(grep -c 'Fence:' plugin/dev-orchestrator/references/mission-flow.md)" -ge 1 ] && [ "$(wc -l < plugin/dev-orchestrator/agents/vf-dev-manager.md)" -le 250 ]</automated>
  </verify>
  <done>`vf-dev-manager` et le fichier qu'il lit reellement decrivent le verrou tel qu'il est apres la Phase 32 ; le module porte la version qui le dit.</done>
  <reversibility rating="reversible">Documentation seule. `git revert` suffit ; aucun code, aucun test, aucun artefact runtime.</reversibility>
</task>

<task type="auto">
  <name>Tache 2 : les quatre autres managers — design, content, business, growth</name>
  <files>plugin/design-orchestrator/agents/vf-design-manager.md, plugin/design-orchestrator/VERSION, plugin/design-orchestrator/CHANGELOG.md, plugin/design-orchestrator/module.json, plugin/design-orchestrator/README.md, plugin/content-bundle/agents/vf-content-manager.md, plugin/content-bundle/VERSION, plugin/content-bundle/CHANGELOG.md, plugin/content-bundle/module.json, plugin/content-bundle/README.md, plugin/business-pilot-bundle/agents/vf-business-manager.md, plugin/business-pilot-bundle/VERSION, plugin/business-pilot-bundle/CHANGELOG.md, plugin/business-pilot-bundle/module.json, plugin/business-pilot-bundle/README.md, plugin/growth-bundle/agents/vf-growth-manager.md, plugin/growth-bundle/VERSION, plugin/growth-bundle/CHANGELOG.md, plugin/growth-bundle/module.json, plugin/growth-bundle/README.md</files>
  <read_first>
    - plugin/design-orchestrator/agents/vf-design-manager.md:49-52 (meme doctrine que `vf-dev-manager.md`, `recovered:true` explicite)
    - plugin/content-bundle/agents/vf-content-manager.md:41-42 (doctrine plus courte : ne mentionne pas `recovered` explicitement mais s'arrete a `acquired:false` sans jamais nommer `takeover` — le manager qui heurte un lock perime n'a AUCUNE recette)
    - plugin/business-pilot-bundle/agents/vf-business-manager.md:59-61 (meme forme courte que content-bundle)
    - plugin/growth-bundle/agents/vf-growth-manager.md:44-46 (meme forme courte ; ce fichier lit `conductor-references/team-kernel.md` directement — verifie via `grep -c team-kernel`, mais PAS seul : `vf-design-manager.md` (4 occurrences), `vf-content-manager.md` (2) et `vf-business-manager.md` (2) le font aussi. Seul `vf-dev-manager.md` ne le lit PAS — 0 occurrence, c'est BL-5, deja le motif de la tache 1. Le traitement differencie de l'etape 3 ci-dessous s'applique donc en realite aux QUATRE managers non-dev, pas au seul growth)
    - plugin/conductor/references/team-kernel.md (etat apres le plan 32-04 : section « Jeton de fence », ligne de brique du verrou remise a l'heure — c'est la reference de formulation pour la doctrine takeover/reclaim)
  </read_first>
  <behavior>
    - V6 : `vf-design-manager.md` ne contient plus `recovered:true` ; meme correction que V1, adaptee au style du fichier (il partage la formulation quasi mot-pour-mot avec `vf-dev-manager.md` avant correction).
    - V7 : `vf-content-manager.md`, `vf-business-manager.md` et `vf-growth-manager.md` — dont la doctrine actuelle s'arrete a `acquired:false → ne dispatche pas, remonte à l'humain` sans jamais nommer `takeover` — gagnent chacun une phrase courte distinguant `reason: held` (remontee a l'humain, inchangee) de `reason: stale-requires-takeover` (executer `takeover --owner=<id> --step=<mission>`), sur le meme patron concis que le reste de leur section kernel.
    - V8 : les quatre modules sont bumpes en PATCH, chacun avec son entree de CHANGELOG.
  </behavior>
  <action>
1. `vf-design-manager.md` : meme correction que la tache 1 pour `vf-dev-manager.md` (le passage est
   quasiment identique avant correction — ligne 49-52). Ajouter la mention de `reclaim`.

2. `vf-content-manager.md`, `vf-business-manager.md`, `vf-growth-manager.md` : ces trois fichiers ne
   decrivent PAS l'ancien contrat de maniere fausse (ils ne mentionnent pas `recovered`), mais ne
   disent RIEN de la marche a suivre sur un lock perime — c'est le trou que GEL-1 nomme (« un
   manager sur lock perime ne connait pas le verbe takeover »). Ajouter, immediatement apres la
   phrase `acquired:false → une autre mission pilote : ne dispatche pas, remonte à l'humain`, une
   distinction courte : ce refus couvre `reason: held` (lock VIVANT, remontee inchangee) ; sur
   `reason: stale-requires-takeover`, executer `takeover --owner=<id> --step=<mission>` plutot que
   de remonter. Une phrase par fichier, meme formulation dans les trois, adaptee uniquement au nom
   de la variable d'etape (`<mission>` dans les trois cas, deja le terme employe).

3. Correction de premisse (F3, re-validation externe #2, mesure) : `vf-growth-manager.md` N'EST PAS
   le seul des cinq a lire `conductor-references/team-kernel.md` directement — `vf-design-manager.md`
   (4 occurrences de `team-kernel`), `vf-content-manager.md` (2) et `vf-business-manager.md` (2) le
   font aussi ; seul `vf-dev-manager.md` ne le lit pas (0 occurrence, BL-5, deja traite tache 1).
   Consequence : `vf-content-manager.md`, `vf-business-manager.md` et `vf-growth-manager.md` sont
   dans la MEME situation les uns que les autres vis-a-vis de `team-kernel.md` — la phrase courte de
   l'etape 2 (distinction `held`/`stale-requires-takeover`) leur suffit a tous les trois sans
   dupliquer `team-kernel.md`, precisement parce qu'ils le lisent deja et que ce fichier porte
   desormais la doctrine complete (plan 32-04). Aucun traitement differencie entre eux : ne pas
   ecrire une variante plus longue pour l'un des trois.

4. Bumper les quatre `VERSION` en PATCH et ajouter l'entree de `CHANGELOG.md` correspondante dans
   chacun des quatre modules, meme motif que la tache 1 (doctrine du verrou resynchronisee, Phase 32).

5. Meme triade que la tache 1 (§5) pour chacun des quatre modules : reporter la nouvelle version
   dans `module.json` (champ `.version`) ET dans l'en-tete `**Version** : vX.Y.Z` du `README.md` du
   module — les quatre le declarent (verifie : `design-orchestrator v1.5.1`, `content-bundle`,
   `business-pilot-bundle` et `growth-bundle` a `v2.0.6`). Sans ce report, `check-version-sync.sh`
   §6 (triade `VERSION`/`module.json`) et §8 (en-tete README) sortent `ko` malgre un `VERSION` et
   un `CHANGELOG.md` corrects — les deux triades sont INDEPENDANTES l'une de l'autre.
  </action>
  <acceptance_criteria>
    - `grep -c 'recovered' plugin/design-orchestrator/agents/vf-design-manager.md` rend `0`.
    - `grep -c 'takeover' plugin/design-orchestrator/agents/vf-design-manager.md` rend au moins `1`.
    - Pour chacun de `plugin/content-bundle/agents/vf-content-manager.md`, `plugin/business-pilot-bundle/agents/vf-business-manager.md`, `plugin/growth-bundle/agents/vf-growth-manager.md` : `grep -c 'takeover' <fichier>` rend au moins `1`.
    - `bash scripts/check-version-sync.sh` sort 0.
    - Les quatre versions correspondent a la premiere entree de leur CHANGELOG respectif.
    - Aucun fichier hors de la liste `files_modified` de ce plan n'est touche (`git diff --name-only` sur le commit de cette tache ne sort que des chemins de cette liste).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-version-sync.sh && for f in plugin/design-orchestrator/agents/vf-design-manager.md plugin/content-bundle/agents/vf-content-manager.md plugin/business-pilot-bundle/agents/vf-business-manager.md plugin/growth-bundle/agents/vf-growth-manager.md; do [ "$(grep -c 'takeover' "$f")" -ge 1 ] || { echo "MANQUE takeover dans $f"; exit 1; }; done</automated>
  </verify>
  <done>Les quatre managers restants connaissent `takeover`, aucune prose d'auto-recuperation ne survit, et les quatre modules portent la version qui le dit.</done>
  <reversibility rating="reversible">Documentation seule (plus quatre fichiers VERSION/CHANGELOG). `git revert` suffit ; aucun code, aucun test, aucun artefact runtime.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctrine ecrite -> comportement reel d'un manager | rien ne force un manager a lire ou appliquer sa propre doctrine ; la frontiere est franchie par la seule discipline d'agent, au meme titre que le reste du contrat invariant |
| refus JSON d'`acquire` (champ `hint`) -> doctrine ecrite | le `hint` est la garantie MACHINE qui atteint tout appelant meme sans lecture de doctrine ; ce plan est un filet supplementaire, pas le seul canal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-38 | Repudiation | doctrine desynchronisee dans un module tiers | high | mitigate | sept fichiers resynchronises avec le contrat reel post-32-02, verifie par grep negatif sur `recovered` et grep positif sur `takeover` dans chacun |
| T-32-39 | Denial of Service | manager gele sur lock perime faute de doctrine | high | mitigate | les cinq managers nomment desormais `takeover` comme marche a suivre sur `reason: stale-requires-takeover` — le `hint` machine du plan 32-02 reste le filet primaire, cette doctrine est le filet secondaire |
| T-32-40 | Tampering | bump de version sans changement reel | low | accept | chaque bump est adosse a un changement de doctrine reellement ecrit dans le meme commit, verifie par grep avant/apres |
| T-32-SC | Tampering | installs npm/pip/cargo | low | accept | aucun paquet externe installe par ce plan — aucun gate de legitimite applicable |
</threat_model>

<verification>
1. Les sept fichiers de doctrine ne portent plus `recovered` comme chemin nominal (grep negatif)
2. Les cinq managers ET `mission-flow.md` nomment `takeover` (grep positif, six fichiers)
3. `mission-flow.md` ecrit la convention `Fence: <generation>` elle-meme (LOCK-05, grep positif) — pas seulement `takeover`
4. `vf-dev-manager.md` reste au plus a 250 lignes (F4, ADR-029, deja au plafond avant ce plan)
5. `bash scripts/check-version-sync.sh` sort 0 — les cinq VERSION, les cinq `module.json`, et les cinq en-tetes `**Version**` de README (dev-orchestrator, design-orchestrator, content-bundle, business-pilot-bundle, growth-bundle) sont tous alignes
6. Les cinq versions correspondent chacune a la premiere entree de leur CHANGELOG
7. Aucun fichier de version racine, manifeste plugin ou manifeste marketplace n'est touche
8. Aucune ligne de code ni de test n'est modifiee par ce plan
</verification>

<success_criteria>
- Les cinq managers qui prescrivent le verrou comme premier geste connaissent `takeover` et `reclaim`.
- `plugin/dev-orchestrator/references/mission-flow.md` — le fichier reellement lu par `vf-dev-manager`, pas `team-kernel.md` — porte la doctrine complete (BL-5 corrige) ET la convention `Fence:` elle-meme (LOCK-05 corrige).
- `vf-dev-manager.md` reste sous le plafond ADR-029 (F4).
- Les cinq modules touches portent chacun la version qui reflete leur changement de doctrine, sur les deux triades independantes verifiees par `check-version-sync.sh` (`VERSION`/`module.json` et `VERSION`/en-tete README).
- Aucune ligne de code n'a ete touchee ; le perimetre reste strictement documentaire.
</success_criteria>

<output>
Creer `.planning/phases/VFDO-32-durcissement-du-driver-lock/32-07-SUMMARY.md` en fin de plan, avec :
la liste des sept fichiers de doctrine corriges et l'extrait exact de la phrase ajoutee dans chacun
(dont l'extrait de la convention `Fence:` ecrite dans `mission-flow.md`), les cinq versions bumpees
avec leur entree de CHANGELOG et leur `module.json`/en-tete README associes, la ligne de compte de
`vf-dev-manager.md` avant/apres, et confirmation que `check-version-sync.sh` est vert.
</output>
---
phase: 41.1-gates-de-planning-workstream-aware-balayage-des-compartiment
plan: 03
lot: "L03 — check-planning-state.sh + detect-gsd-engine.sh cessent d'être faux (D-04 catégorie c)"
type: execute
wave: 2
depends_on: ["41.1-01"]
autonomous: true
requirements: [WSAW-02, WSAW-05]
files_modified:
  - plugin/planning-core/scripts/check-planning-state.sh
  - plugin/planning-core/scripts/detect-gsd-engine.sh
  - plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh
  - plugin/planning-core/scripts/tests/test-check-planning-state.sh

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "TRAÇABILITÉ (revue plan-checker, CLAUDE.md racine du dépôt) : tout commit de ce plan qui touche `plugin/planning-core/scripts/check-planning-state.sh`, `plugin/planning-core/scripts/detect-gsd-engine.sh`, ou leurs suites de tests, porte un trailer `Gate-Touche: <chemin-ou-motif> — <raison>` (garde G-2, `scripts/check-gate-touche.sh`)"
    - "`detect-gsd-engine.sh` rend « moteur actif » (rc=0) dès qu'AU MOINS UN compartiment de `.planning/workstreams/<nom>/` est conforme OU non-initialisé au sens de D-02 — pas seulement sur son ancien repli racine (`$PLANNING_DIR/STATE.md` porte `gsd_state_version`)"
    - "`check-planning-state.sh` cesse d'imprimer « STATE.md (clé de voûte) est ABSENT » quand le dépôt est partitionné et qu'au moins un compartiment existe sur le disque — le repli racine (`STATE_FILE=\"$PLANNING_DIR/STATE.md\"`) ne s'applique QUE si `$PLANNING_DIR/phases/` existe, même posture SILENCE que `check-divergence.sh`"
    - "TROIS ÉTATS, pas deux (D-02, WSAW-05), mais CE HOOK NE LES ÉTABLIT PAS (correction post plan-checker, FLAG-1) : un compartiment dont le `STATE.md` porte AU MOINS `workstream:`+`created:` (sortie `workstream create`) n'est ni un échec (`detect-gsd-engine.sh` ne rend pas 3 dessus, `check-planning-state.sh` n'imprime pas ABSENT dessus) ni un faux conforme pleinement actif — mais le verdict imprimé N'EMPRUNTE PAS le nom de la classe D-02 « non initialisé », parce que `detect-gsd-engine.sh` ne vérifie pas l'exclusivité des clés (un compartiment CORROMPU portant aussi `workstream:`+`created:` PARMI d'autres clés passerait le même test). Le message énonce le fait vérifié (présence des deux clés), jamais une classification D-02 qu'il n'établit pas"
    - "`gouvernance/STATE.md` (frontmatter réduit, cité par le ROADMAP comme mesuré rc=2 le 2026-09-23) est le cas de fixture DE RÉFÉRENCE du non-initialisé — un test dédié reproduit CE frontmatter exact (`workstream:`+`created:` seuls) et prouve qu'aucun des deux scripts ne le traite en échec"
    - "Les deux scripts restent des hooks `SessionStart` NON BLOQUANTS — posture ADR-074 inchangée (D-04 : hygiène de bootstrap, pas intégrité d'état multi-écrivain) ; aucun code de sortie n'est promu en garde CI bloquante par ce plan"
    - "`detect-gsd-engine.sh` et `check-planning-state.sh` consomment `vf_ws_enumerate` (du plan 41.1-01) via le sourcing standard `$(dirname \"$0\")/workstream-policy.sh` (les deux vivent DANS `planning-core/scripts/`, donc sourcing direct, PAS la recherche à deux candidats de `check-divergence.sh` — `planning-core: requires: []`, elle n'a pas besoin du repli inter-module)"
    - "`detect-gsd-engine.sh` : l'appel existant `--defer-to-gsd` de `check-planning-state.sh` vers `detect-gsd-engine.sh` (lignes ~88-97 actuelles) reste intact — non touché, ce n'est pas le bug"
    - "`test-check-planning-state.sh` est un fichier NEUF (n'existe pas aujourd'hui, seul script du module sans suite dédiée) — modélisé structurellement sur `test-detect-gsd-engine.sh`"
    - "QUAL-01 : chaque script gagne au moins un cas de mutation qui prouve la discriminance de son adoption de `vf_ws_enumerate` — par exemple neutraliser la boucle sur les compartiments énumérés fait retomber le script sur son ancien comportement racine-seule, faisant échouer le cas « compartiment workstream seul, racine vide »"
  artifacts:
    - path: "plugin/planning-core/scripts/detect-gsd-engine.sh"
      provides: "moteur actif détecté dès qu'un compartiment conforme ou non-initialisé existe"
      contains: "vf_ws_enumerate"
    - path: "plugin/planning-core/scripts/check-planning-state.sh"
      provides: "cesse d'imprimer ABSENT sur un dépôt partitionné avec au moins un compartiment"
      contains: "vf_ws_enumerate"
    - path: "plugin/planning-core/scripts/tests/test-check-planning-state.sh"
      provides: "première suite de ce script, cas gouvernance/STATE.md non-initialisé inclus"
  key_links:
    - "Bug exact de `check-planning-state.sh` : lignes ~112-117 (`STATE_FILE=\"$PLANNING_DIR/STATE.md\"` puis `[ ! -f \"$STATE_FILE\" ]` → message ABSENT) — repli racine à border par `[ -d \"$PLANNING_DIR/phases\" ]`, patron exact de `check-divergence.sh` lignes ~288-294 (`if [ -d \"$PLANNING/phases\" ]; then CHECKED=1 ...`)"
    - "Bug exact de `detect-gsd-engine.sh` : ligne ~69 `STATE_FILE=\"$PLANNING_DIR/STATE.md\"` puis test `has_frontmatter_key \"$STATE_FILE\" gsd_state_version` (lignes ~86-90) — ne regarde QUE la racine. `has_frontmatter_key` (lignes ~77-84) est réutilisée TELLE QUELLE sur chaque `STATE.md` de compartiment énuméré, jamais réécrite"
    - "D-02 (définition de compartiment conforme/non-initialisé/corrompu) est CONSOMMÉE ici, jamais RE-DÉFINIE — un compartiment est non-initialisé quand son `STATE.md` porte SEULEMENT `workstream:`+`created:`, sans `gsd_state_version` ni `ROADMAP.md`/`REQUIREMENTS.md` de contenu"
---

<objective>
Rendre `check-planning-state.sh` et `detect-gsd-engine.sh` workstream-aware sans changer leur
posture (hooks `SessionStart` advisory, non bloquants) : ils cessent de mentir sur un dépôt
partitionné en adoptant `vf_ws_enumerate`, et distinguent — sans jamais rougir dessus — le cas
« compartiment non initialisé » (`gouvernance/STATE.md`, sortie nominale de `workstream create`)
du cas « aucun moteur en place ».

Purpose: fermer la classe de faux résultats D-04 nomme explicitement pour ces deux scripts (rc=2
« STATE.md ABSENT » et rc=3 « aucun moteur » alors qu'un compartiment existe bel et bien).
Output: deux hooks qui disent le vrai sur ce dépôt partitionné, une suite neuve pour l'un d'eux.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-CONTEXT.md
@.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-PATTERNS.md
@CLAUDE.md

Fichiers à lire au début de la Task 1 :
- `plugin/planning-core/scripts/check-planning-state.sh` en entier (148 lignes)
- `plugin/planning-core/scripts/detect-gsd-engine.sh` en entier (114 lignes)
- `plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh` en entier (structure à copier pour le fichier neuf)
- `plugin/conductor/scripts/check-divergence.sh` lignes ~85-118 (garde SILENCE + repli racine `if [ -d "$PLANNING/phases" ]`, patron à répliquer)
</context>

<discipline>
- `diff`/`ls` proxifiés — `rtk proxy` ; comparaisons `cmp`/`comm`/`cksum`.
- Fixtures `mktemp -d`, jamais le dépôt réel.
- Ne pas promouvoir un code de sortie advisory en bloquant : ce plan ne touche à AUCUNE étape CI.
- Le cas `gouvernance/STATE.md` non-initialisé est reproduit par son frontmatter EXACT
  (`workstream: <nom>` + `created: <date>`, rien d'autre) — jamais approximé.
</discipline>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 : adoption de `vf_ws_enumerate` par les deux scripts, trichotomie D-02 respectée</name>
  <files>plugin/planning-core/scripts/check-planning-state.sh, plugin/planning-core/scripts/detect-gsd-engine.sh</files>
  <behavior>
    `detect-gsd-engine.sh` :
    - racine legacy avec `gsd_state_version` (comportement actuel) → rc=0, inchangé.
    - AUCUN `STATE.md` à la racine, MAIS `.planning/workstreams/dev/STATE.md` porte
      `gsd_state_version` → rc=0 (nouveau : avant ce plan, rc=3).
    - AUCUN `STATE.md` à la racine, `.planning/workstreams/gouvernance/STATE.md` ne porte QUE
      `workstream:`+`created:` (non-initialisé, D-02) → rc=0 quand même : « moteur actif » reste
      vrai (le moteur EST en place, le compartiment n'a simplement pas encore été travaillé) — la
      distinction non-initialisé/conforme n'a pas besoin d'un code de sortie séparé ICI, ce script
      répond à UNE seule question factuelle (« y a-t-il un moteur ? »), jamais « ce compartiment
      est-il conforme ? » (question qui appartient à `check-state-integrity.sh`, hors périmètre).
    - AUCUN compartiment, aucun repli racine → rc=3, inchangé.

    `check-planning-state.sh` :
    - racine legacy avec `.planning/phases/` → repli inchangé, comportement actuel intact.
    - AUCUN `.planning/phases/`, MAIS au moins un `.planning/workstreams/<nom>/` existe (peu
      importe son état D-02 — conforme, non-initialisé, ou même corrompu) → NE PLUS imprimer
      « STATE.md (clé de voûte) est ABSENT » ; message adapté qui nomme le fait « dépôt
      partitionné, `<n>` compartiment(s) sur le disque » — rc reste advisory (jamais promu
      bloquant), la borne exacte du rc (0 vs 1 vs 2) est laissée à l'implémentation tant qu'elle
      n'imprime plus le message ABSENT trompeur sur ce cas.
    - AUCUN `.planning/phases/`, AUCUN `.planning/workstreams/*` → comportement actuel intact
      (message ABSENT reste correct, c'est un vrai lab non amorcé).
  </behavior>
  <action>
**`detect-gsd-engine.sh`.** Sourcer `workstream-policy.sh` (sourcing direct, ce script vit dans
`planning-core/scripts/`) juste après la définition de `has_frontmatter_key`. Insérer, ENTRE la
priorité 2 (racine legacy) actuelle et la priorité 3 (migration), une nouvelle priorité 2bis qui
énumère les compartiments AVANT de conclure « aucun moteur » :
```bash
# --- Priorité 2bis : au moins un compartiment de workstream porte le marqueur --------------------
WS_LIST="$(vf_ws_enumerate "$PLANNING_DIR" 2>/dev/null)"
if [ -n "$WS_LIST" ]; then
  while IFS= read -r _wsdir; do
    [ -n "$_wsdir" ] || continue
    if has_frontmatter_key "$_wsdir/STATE.md" gsd_state_version; then
      say "Moteur GSD actif — le compartiment « $(basename "$_wsdir")  » appartient à GSD."
      exit 0
    fi
  done <<< "$WS_LIST"
  # Aucun compartiment conforme trouvé, mais au moins un existe : le moteur est quand même présent
  # dès qu'un `STATE.md` porte AU MOINS `workstream:`+`created:` (facture de `workstream create`).
  # CORRECTION post plan-checker (FLAG-1) : ce test ne compte PAS les clés du frontmatter — il ne
  # peut donc PAS établir la classe D-02 « non initialisé » (qui exige EXACTEMENT ces deux clés,
  # rien d'autre) ; un compartiment CORROMPU qui porterait aussi `workstream:`+`created:` PARMI
  # d'autres clés passerait ce test tout autant. Le verdict imprimé n'EMPRUNTE donc PLUS le nom de
  # la classe D-02 — il énonce le seul fait vérifié ici (présence des deux clés), jamais un
  # classement qu'il n'a pas les moyens d'établir :
  while IFS= read -r _wsdir; do
    [ -n "$_wsdir" ] || continue
    if [ -f "$_wsdir/STATE.md" ] && has_frontmatter_key "$_wsdir/STATE.md" workstream \
       && has_frontmatter_key "$_wsdir/STATE.md" created; then
      say "Moteur GSD actif — compartiment « $(basename "$_wsdir") » : frontmatter réduit détecté (workstream:+created: présents, gsd_state_version absent). Ce script ne classe pas ce compartiment (conforme/non-initialisé/corrompu, D-02) ; il constate seulement qu'un moteur GSD y est présent."
      exit 0
    fi
  done <<< "$WS_LIST"
fi
```
Note : `has_frontmatter_key` est déjà générique (prend un chemin, une clé) — aucune modification de
cette fonction. Le compte de clés (« SEULEMENT ces deux ») n'a PAS besoin d'être vérifié à l'octet
près ici, PARCE QUE le verdict imprimé n'emprunte plus le nom « non initialisé » — la question posée
reste strictement factuelle (« un moteur est-il là ? »), jamais une classification D-02 complète
que ce test n'a pas les moyens d'établir. La classification exhaustive (conforme/non-initialisé/
corrompu) reste au script qui en a réellement besoin (aucun aujourd'hui ne l'exige pour ce hook, la
trichotomie complète appartient à des gates futurs éventuels, hors périmètre WSAW-02).

**`check-planning-state.sh`.** Sourcer `workstream-policy.sh` juste après le parsing d'options.
Border le message ABSENT de la clé de voûte par une vérification supplémentaire, patron exact de
`check-divergence.sh` (`if [ -d "$PLANNING/phases" ]`) :
```bash
STATE_FILE="$PLANNING_DIR/STATE.md"
# 2. STATE présent ? (clé de voûte) — SEULEMENT si ce dépôt n'est pas partitionné en workstreams
#    ou si un repli racine legacy (.planning/phases/) est encore en jeu. Patron D-01/D-04, même
#    posture SILENCE que check-divergence.sh : sur un dépôt partitionné avec au moins un
#    compartiment, l'absence du STATE.md RACINE n'est plus un défaut, c'est l'état nominal.
if [ ! -f "$STATE_FILE" ]; then
  WS_LIST="$(vf_ws_enumerate "$PLANNING_DIR" 2>/dev/null)"
  if [ -d "$PLANNING_DIR/phases" ] || [ -z "$WS_LIST" ]; then
    say "$PLANNING_DIR/ existe mais STATE.md (clé de voûte) est ABSENT. Le recréer via /vf-planning."
    hook_exit 2
  fi
  n_ws="$(printf '%s\n' "$WS_LIST" | awk 'NF>0' | awk 'END{print NR+0}')"
  say_diag "OK — dépôt partitionné, $n_ws compartiment(s) de workstream sur le disque, pas de STATE.md racine attendu."
  exit 0
fi
```
Le `--defer-to-gsd` existant (lignes ~88-97) N'EST PAS touché — il continue de shell out vers
`detect-gsd-engine.sh` avant même d'atteindre ce bloc, comportement inchangé.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'bash -n plugin/planning-core/scripts/check-planning-state.sh && bash -n plugin/planning-core/scripts/detect-gsd-engine.sh && echo "syntaxe OK"'</automated>
    <fails_when>un des deux `bash -n` échoue</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/.planning/workstreams/dev"
cat > "$T/.planning/workstreams/dev/STATE.md" <<EOF
---
gsd_state_version: 1.0
milestone: x
current_phase: 1
status: in_progress
---
EOF
rc=0; out=$(bash plugin/planning-core/scripts/detect-gsd-engine.sh --path "$T/.planning" 2>&1) || rc=$?
echo "detect_compartiment_conforme rc=$rc"; printf "%s\n" "$out"
[ "$rc" -eq 0 ]
'</automated>
    <fails_when>`detect_compartiment_conforme rc=` différent de 0 — un compartiment de workstream conforme, sans STATE.md racine, doit rendre « moteur actif »</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/.planning/workstreams/gouvernance"
printf "%s\n" "---" "workstream: gouvernance" "created: 2026-09-23" "---" > "$T/.planning/workstreams/gouvernance/STATE.md"
rc=0; out=$(bash plugin/planning-core/scripts/detect-gsd-engine.sh --path "$T/.planning" 2>&1) || rc=$?
echo "detect_non_initialise rc=$rc"; printf "%s\n" "$out"
case "$out" in *"non initialisé"*) echo "REGRESSION FLAG-1: emprunte le nom de la classe D-02 sans l etablir"; exit 1;; esac
[ "$rc" -eq 0 ]
'</automated>
    <fails_when>`detect_non_initialise rc=` différent de 0 — le cas de référence `gouvernance/STATE.md` (frontmatter nominal) ne doit JAMAIS produire « aucun moteur » (WSAW-05) ; ou la sortie contient « non initialisé » — ce script n'a pas les moyens d'établir cette classe D-02 (FLAG-1 du juge frais), le verdict doit rester au niveau du fait constaté</fails_when>
    <automated>bash --noprofile --norc -e -c '
T=$(mktemp -d); trap "rm -rf \"$T\"" EXIT
mkdir -p "$T/.planning/workstreams/gouvernance"
printf "%s\n" "---" "workstream: gouvernance" "created: 2026-09-23" "---" > "$T/.planning/workstreams/gouvernance/STATE.md"
rc=0; out=$(bash plugin/planning-core/scripts/check-planning-state.sh --path "$T/.planning" 2>&1) || rc=$?
echo "planning_state_non_initialise rc=$rc"; printf "%s\n" "$out"
case "$out" in *ABSENT*) echo "REGRESSION: message ABSENT imprime a tort"; exit 1;; esac
'</automated>
    <fails_when>la sortie contient le mot `ABSENT` — c'est exactement le faux rouge D-04 nomme (« imprime STATE.md est ABSENT à chaque SessionStart » sur `gouvernance/STATE.md`)</fails_when>
  </verify>
  <acceptance_criteria>
    - `detect-gsd-engine.sh` rend rc=0 sur un compartiment conforme SANS repli racine
    - `detect-gsd-engine.sh` rend rc=0 sur `gouvernance/STATE.md` (frontmatter nominal), jamais rc=3
    - `check-planning-state.sh` n'imprime plus « ABSENT » sur un dépôt partitionné avec au moins un compartiment
    - les deux scripts restent non bloquants (aucune étape CI touchée)
  </acceptance_criteria>
  <done>Les deux hooks disent le vrai sur un dépôt partitionné, dont le cas de référence non-initialisé `gouvernance/STATE.md`.</done>
</task>

<task type="auto">
  <name>Task 2 : suites — extension de `test-detect-gsd-engine.sh`, création de `test-check-planning-state.sh`</name>
  <files>plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh, plugin/planning-core/scripts/tests/test-check-planning-state.sh</files>
  <precondition>Task 1 verte.</precondition>
  <read_first>
    - `plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh` en entier (structure à copier)
  </read_first>
  <action>
Étendre `test-detect-gsd-engine.sh` avec trois cas : compartiment conforme sans racine (rc=0),
compartiment non-initialisé type `gouvernance/STATE.md` (rc=0), aucun compartiment ni racine
(rc=3, inchangé — non-régression).

Créer `test-check-planning-state.sh` sur le même patron structurel (`mktemp -d`, `ok`/`ko`,
`run()` avec `env -u` si le script original en fait usage) : cas racine legacy inchangé (avec
`.planning/phases/` présent, comportement actuel), cas partitionné sans STATE.md racine (plus de
message ABSENT), cas `gouvernance/STATE.md` non-initialisé (idem), cas vrai lab non amorcé (ni
racine ni workstreams — message ABSENT toujours correct, non-régression).

QUAL-01 — au moins un mutant par script : neutraliser l'appel à `vf_ws_enumerate` (le remplacer
par une chaîne vide codée en dur) doit faire RÉAPPARAÎTRE l'ancien comportement (rc=3 sur
`detect-gsd-engine.sh`, message ABSENT sur `check-planning-state.sh`) sur le cas « compartiment
seul, pas de racine » — preuve que l'adoption de la primitive est réellement ce qui discrimine.

Rejouer `gates` et `tests` complets, consigner le bilan au SUMMARY (nombre de suites, 0 échec).
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh || rc=$?; echo "test-detect-gsd-engine rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou une ligne `✗`</fails_when>
    <automated>bash --noprofile --norc -e -c 'rc=0; bash plugin/planning-core/scripts/tests/test-check-planning-state.sh || rc=$?; echo "test-check-planning-state rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou une ligne `✗` ; ou le fichier n'existe pas encore</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); bash plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh > "$T/o1" 2>&1; bash plugin/planning-core/scripts/tests/test-check-planning-state.sh > "$T/o2" 2>&1; m1=$(awk "/✓ MUT/{c++} END{print c+0}" "$T/o1"); m2=$(awk "/✓ MUT/{c++} END{print c+0}" "$T/o2"); echo "mutants_detect=$m1 mutants_planning_state=$m2"; [ "$m1" -ge 1 ] && [ "$m2" -ge 1 ]'</automated>
    <fails_when>`mutants_detect=` ou `mutants_planning_state=` inférieur à 1 — chaque script doit avoir au moins un mutant tué qui prouve que l'adoption de `vf_ws_enumerate` est discriminante</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/workstreams/fiabilite/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/replay-ci-jobs.sh --job tests > "$T/t" 2>&1 || rc=$?; awk "/^== bilan : /" "$T/t"; echo "tests rc=$rc"; [ "$rc" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `tests rc=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `test-detect-gsd-engine.sh` étendu de trois cas, vert
    - `test-check-planning-state.sh` créé (fichier neuf), vert
    - au moins un mutant tué par script, prouvant la discriminance de l'adoption de `vf_ws_enumerate`
    - rejeu `tests` vert sur la découverte complète
  </acceptance_criteria>
  <done>Les deux scripts ont chacun une suite qui prouve leur nouvelle lecture workstream-aware, dont le cas de référence `gouvernance/STATE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `STATE.md` de compartiment → hook `SessionStart` | ces deux scripts lisent désormais un frontmatter par compartiment énuméré, contenu de dépôt non maîtrisé, dans un hook auto-déclenché à l'ouverture de session |
| classification D-02 → verdict | un compartiment mal classé (non-initialisé pris pour corrompu, ou l'inverse) dégraderait la session sans qu'aucun geste de l'utilisateur ne l'ait causé |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-06 | Denial of service | un dépôt partitionné avec de nombreux compartiments dégraderait chaque `SessionStart` si la boucle sur `vf_ws_enumerate` était coûteuse ou bloquante | low | accept | `vf_ws_enumerate` est un `find`/glob sur un seul niveau, pas de récursion ; posture advisory déjà `|| true` côté hooks.json, un ralentissement resterait local |
| T-41.1-07 | Tampering | un `STATE.md` de compartiment falsifié (frontmatter forgé pour ressembler à `workstream:`+`created:` alors qu'il porte du contenu malveillant ailleurs dans le fichier) ferait classer un compartiment corrompu comme non-initialisé | low | accept | ces deux scripts restent advisory, non bloquants — aucune décision de merge/CI n'en dépend ; le contenu du fichier au-delà du frontmatter n'est jamais exécuté ni réimprimé verbatim par ces deux scripts |
</threat_model>

<verification>
`bash -n` sur les deux scripts ; `detect-gsd-engine.sh` rc=0 sur compartiment conforme et sur
`gouvernance/STATE.md` non-initialisé ; `check-planning-state.sh` n'imprime plus ABSENT sur dépôt
partitionné ; deux suites vertes, chacune avec au moins un mutant tué ; rejeu `tests` vert.
</verification>

<success_criteria>
Les deux hooks `SessionStart` disent le vrai sur un dépôt partitionné : ils voient un compartiment
présent sur le disque, distinguent non-initialisé d'absent sans jamais rougir sur le premier, et
restent strictement advisory.
</success_criteria>

## Artifacts this phase produces

- `plugin/planning-core/scripts/detect-gsd-engine.sh` — priorité 2bis, adoption `vf_ws_enumerate`.
- `plugin/planning-core/scripts/check-planning-state.sh` — repli racine bordé par `phases/`.
- `plugin/planning-core/scripts/tests/test-detect-gsd-engine.sh` — trois cas ajoutés.
- `plugin/planning-core/scripts/tests/test-check-planning-state.sh` — suite neuve.

<output>
Create `.planning/workstreams/fiabilite/phases/VFDO-41.1-gates-de-planning-workstream-aware-balayage-des-compartiment/41.1-03-SUMMARY.md` when done
</output>
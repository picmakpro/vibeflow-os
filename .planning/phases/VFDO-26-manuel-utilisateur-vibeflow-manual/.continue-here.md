---
context: phase
phase: VFDO-26-manuel-utilisateur-vibeflow-manual
task: 5
total_tasks: 9
status: in_progress
last_updated: 2026-08-01T10:13:15.330Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Ces contraintes viennent d'une consigne explicite de Samuel, pas d'une déduction.
> Les acquitter avant toute action.

- [ ] CONSTRAINT: **`manual/` ne va pas dans git** — aucun `git add` sous `manual/`, jamais.
      Mitigation structurelle : exclusion locale déjà posée dans `.git/info/exclude`.
      **Ne PAS déplacer cette règle vers `.gitignore`** — ce fichier est versionné et révélerait
      l'existence du manuel. Le contenu de planning (`.planning/**`), lui, se committe normalement.
- [ ] CONSTRAINT: **`README.md`, `README.fr.md`, `INSTALL.md` restent intouchés** — le volet
      « les README maigrissent et pointent vers le manuel » (D-07/D-08) est **gelé** tant que
      `manual/` est hors du dépôt : un lien vers un dossier absent casse pour tout visiteur.
- [ ] CONSTRAINT: **Plan 26-09 = `autonomous: false`** — checkpoint humain bloquant avant le
      commit de clôture de phase. Aucune clôture sans validation explicite de Samuel.

**Ne pas continuer tant que ces trois cases ne sont pas cochées.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Nav écrite à la main | Les bandeaux `← Précédent · ↑ Sommaire · Suivant →` et les blocs de langue sont **générés** depuis `manual/toc.yml` par `manual/.tools/build-nav.sh` (D-03). Les éditer à la main crée une divergence silencieuse avec la séquence canonique. | blocking | Modifier `toc.yml` puis relancer `build-nav.sh` ; vérifier avec `.tools/check-manual.sh`. |
| Traduction au lieu de production | Le manuel EN se **produit**, il ne se traduit pas (D-12) — aux profondeurs 2-3, un calque du FR sonne faux. | advisory | Écrire la page EN depuis la même source disque, pas depuis la page FR. |
| Recopie de la référence | Aucun contenu de `plugin/reference/` ou `docs/` n'est recopié dans le manuel (D-11) — la copie diverge dès la version suivante. | advisory | Dériver du disque et renvoyer vers la source. |
| Page qui gonfle | 100-200 lignes par page ; bascule ferme au-delà de 300 lignes ou 3 H2 (D-04). | advisory | Diviser en deux pages et mettre à jour `toc.yml`. |

<current_state>
Phase 26 en pause propre, demandée par Samuel (« pause work properly ») — ce n'est pas un incident.
Le halt a été ordonné à une frontière de fichier : rien n'est à moitié écrit.

**4 thèmes sur 7 sont écrits**, miroir FR/EN synchrone à chaque vague, 55 fichiers sous `manual/` :

| Thème | FR | EN |
|---|---|---|
| 01-demarrer | 7 | 7 |
| 02-concepts | 7 | 7 |
| 03-modules | 6 | 6 |
| 04-cycle-de-dev | 6 | 6 |

Plus le socle : `manual/README.md`, `manual/fr/README.md`, `manual/en/README.md`, `manual/toc.yml`,
`manual/.tools/build-nav.sh`, `manual/.tools/check-manual.sh`.

**Un seul reliquat** : les 12 pages du thème `04-cycle-de-dev` sont sur disque mais le
`26-05-SUMMARY.md` n'a pas été écrit — le worker a été arrêté avant de le produire.
</current_state>

<completed_work>

- Plan 26-01 : socle (`toc.yml`, `.tools/`, les trois README) — SUMMARY écrit
- Plan 26-02 : thème `01-demarrer`, 7 pages × 2 langues — SUMMARY écrit
- Plan 26-03 : thème `02-concepts`, 7 pages × 2 langues — SUMMARY écrit
- Plan 26-04 : thème `03-modules`, 6 pages × 2 langues — SUMMARY écrit
- Plan 26-05 : thème `04-cycle-de-dev`, 6 pages × 2 langues — **pages écrites, SUMMARY manquant**
</completed_work>

<remaining_work>

- Plan 26-05 : écrire le `26-05-SUMMARY.md` (les pages sont déjà là — vérifier, ne pas réécrire)
- Plan 26-06 : thème `05-equipe-d-agents`
- Plan 26-07 : thème `06-reference`
- Plan 26-08 : thème `07-sous-le-capot`
- Plan 26-09 : clôture de phase — **checkpoint humain bloquant**
</remaining_work>

<decisions_made>

- **D-01** — miroir `manual/fr/` + `manual/en/`, jamais de suffixes `.fr.md`
- **D-02** — profondeur 2 : `manual/<lang>/NN-theme/slug.md`
- **D-03** — `manual/toc.yml` porte la séquence canonique ; la nav est générée, pas maintenue
- **D-04** — page de 100-200 lignes, bascule ferme au-delà de 300 lignes ou 3 H2
- **D-05** — 7 thèmes en ordre fixe, trajectoire Diátaxis tutoriel → référence
- **D-06** — carte mermaid décorative dans l'index ; la vraie navigation est la liste sous la carte
- **D-11** — rien n'est recopié de la référence, tout se dérive du disque
- **D-12** — le manuel EN se produit, il ne se traduit pas
- **D-13** — l'outillage vit sous `manual/.tools/`
- **D-14** — aucun `git add` sous `manual/`
- **D-07/D-08 gelées** — dégraissage README/INSTALL suspendu tant que `manual/` est hors dépôt
</decisions_made>

<blockers>
- Aucun blocage technique. La pause est une décision, pas un incident.
</blockers>

## Required Reading (in order)

1. `.planning/phases/VFDO-26-manuel-utilisateur-vibeflow-manual/26-CONTEXT.md` — les décisions
   D-01→D-14 et les points ouverts, source de vérité du cadrage
2. `manual/toc.yml` — la séquence canonique ; tout ajout de page commence ici
3. Le `26-0N-PLAN.md` du plan à reprendre (26-06 pour le thème 05)
4. `.planning/phases/VFDO-26-.../26-04-SUMMARY.md` — le format de SUMMARY attendu par vague
5. `CLAUDE.md` du repo — densité ADR-029, jamais de fix sans validation humaine ADR-031

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: committer `manual/` → l'exclusion `.git/info/exclude` est le garde-fou ; ne
  jamais la contourner ni la déplacer dans `.gitignore`
- [ANTI-PATTERN]: éditer les bandeaux de nav à la main → passer par `toc.yml` + `build-nav.sh`
- [ANTI-PATTERN]: réécrire les 12 pages du thème 04 en croyant le plan 05 non fait → elles sont
  sur disque, seul le SUMMARY manque

## Infrastructure State

- `manual/` : 55 fichiers sur disque, **hors git** (exclusion locale volontaire)
- `.planning/` : 4 SUMMARY non committés (26-01 à 26-04) ; les 9 PLAN et le CONTEXT sont committés
- Dernier commit : `00c11ef` — consigne O-1 (slugs anglais du miroir `en/`, point ouvert)
- Aucun serveur ni watcher en cours ; le monitor d'avancement de session a été arrêté

## Points ouverts (à trancher par Samuel)

- **O-1 — slugs du miroir `en/`** : ils sont aujourd'hui en français
  (`en/01-demarrer/prerequis.md`). Faut-il basculer en slugs anglais
  (`en/01-get-started/prerequisites.md`) ou assumer les slugs FR partout ? Le coût de renommage
  croît à chaque thème : 4 thèmes sur 7 sont déjà écrits. **Rien n'a été renommé** — décision
  volontairement laissée à Samuel.
- **Publication de `manual/`** : quand le manuel entre-t-il dans le dépôt ? De cette réponse
  dépend le dégel de D-07/D-08 (dégraissage README/INSTALL).

<context>
Mission menée par `vf-dev-manager` en mode autonome, découpée en 9 plans : 1 socle + 7 vagues
thématiques + 1 clôture. Le rythme établi est « une vague = un thème = les deux langues d'un coup »,
ce qui a tenu le miroir FR/EN parfaitement synchrone jusqu'ici — le conserver.

Le manuel s'adresse à des humains qui arrivent sur le repo, pas aux agents qui maintiennent
VibeFlow : ton direct, tutoiement, phrases complètes, aucun terme technique employé sans être
défini au passage. `docs/` et `.planning/` restent le territoire des agents.
</context>

<next_action>
Commencer par : vérifier que `manual/fr/04-cycle-de-dev/` et `manual/en/04-cycle-de-dev/` comptent
bien 6 pages chacun, puis écrire le `26-05-SUMMARY.md` manquant sur le modèle du `26-04-SUMMARY.md`.
Enchaîner ensuite sur le plan `26-06` (thème `05-equipe-d-agents`).
</next_action>

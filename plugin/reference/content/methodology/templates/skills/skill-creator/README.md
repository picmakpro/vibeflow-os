# skill-creator — template déplacé

Ce dossier ne contient plus de copie du skill : elle divergeait du module réel et référençait
des ressources (`assets/`, `eval-viewer/`, `scripts/`) absentes du template.

**Source canonique unique** : le module `skill-creator` du plugin —
`plugin/skill-creator/skills/skill-creator/` (SKILL.md + assets + eval-viewer + scripts complets).

Dans un lab, installer le module via `vibeflow-install` plutôt que de copier ce template.

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 07
lot: L4a — suite à seuil codé en dur dev-orchestrator
type: execute
wave: 2
depends_on: ["40.1-01", "40.1-02"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh
  - plugin/dev-orchestrator/README.md
  - plugin/dev-orchestrator/references/GSD-PIPELINE.md
  - plugin/dev-orchestrator/VERSION
  - plugin/dev-orchestrator/module.json
  - plugin/dev-orchestrator/CHANGELOG.md

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PRÉCONDITION (session principale, 2026-09-16) — ce plan s'exécute sur une base qui CONTIENT le hotfix v2.63.1 fusionné dans `main` (il touche `plugin/dev-orchestrator/`). Révision 3 (warning W-B) : la précondition est une vérification MACHINE BLOQUANTE (Task 0), jouée en premier, avant toute édition, sur le CONTENU de la base dérivée par `tools/phase-base.sh` — `VERSION` racine de la base ≥ v2.63.1 par comparaison numérique ET entrée de tête `## [v…]` du CHANGELOG `dev-orchestrator` citant v2.63.1 — jamais sur un sha ni un sujet de commit (le hotfix peut être squash-mergé). Rouge → ARRÊT et remontée au manager ; aucun rebase décidé par l'exécutant (arbitrage Samuel, AskUserQuestion session principale, 2026-09-16 : l'exécution attend le merge du hotfix dans main). Après un rebase décidé en amont, la liste de travail, les tailles d'agents et la VERSION courante sont re-dérivées AVANT toute édition"
    - "Aucun commit propre à la branche ne touche les fichiers du hotfix : `plugin/dev-orchestrator/AGENT.md`, `references/head-governance.md`, `references/intent-routing.md`, `skills/vf-dev/`, `skills/vf-auto/`, `agents/vf-coder.md`"
    - "D-01 — les seuils codés en dur de `test-dev-orchestrator.sh` (T3, T5, T8, T22, T31-G, T35 via `T35_PLAFOND`) valent 300 : un agent de 300 lignes passe, un agent de 301 lignes est vu en dépassement par CHAQUE assertion, prouvé par `tools/prove-density-suites.sh dev-orchestrator 300 301` sur copie"
    - "Assertions BINAIRES (≤ 300), sans réplication de la zone d'avertissement (RESEARCH A1, Open Question 1 résolue) ; l'avertissement dès 251 est porté par le gate du dépôt (40.1-03)"
    - "Bump **patch** sur cible dérivée à l'exécution (`tools/check-module-bump.sh dev-orchestrator patch`), jamais un numéro figé"
    - "Réserve T28-F (alignée sur 40.1-14) : un rouge de T28-F seul, qui reste rouge sur la base de branche dérivée, est un rouge préexistant de fraîcheur du moteur GSD local ; il est consigné et remonté (`human_needed`), jamais neutralisé, et ne se contourne pas"
  artifacts:
    - path: "plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh"
      provides: "seuils T3/T5/T8/T22/T31-G/T35 à 300"
      contains: "T35_PLAFOND=300"
  key_links:
    - "`tools/prove-density-suites.sh` (40.1-01) rendait 1 en `300 301` avant ce plan ; il doit rendre 0 après"
    - "Couplages en lecture, déclarés dans VALIDATION : `test-dev-orchestrator.sh` lit `AGENT.md` et `vf-coder.md` (hotfix, d'où la précondition) et `docs/ADR.md` (40.1-13, vague 2). Le verdict de ce plan vaut sur son propre état ; l'intégration est prouvée au rejeu du job `tests` en clôture"
---

<objective>
Relever à 300 les seuils de densité codés en dur de la suite `dev-orchestrator` (D-01, arbitrage
Samuel, AskUserQuestion session principale, relais SendMessage, 2026-09-16), aligner ses deux
énoncés vivants et bumper le module en patch sur cible dérivée. Révision 2 (constat A4, 11
fichiers) : `design-orchestrator` passe dans le plan 40.1-15.

Purpose: BUDG-04 sur la suite du module le plus gardé.
Output: suite relevée et prouvée, module bumpé.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0 : précondition machine — la base dérivée contient le hotfix v2.63.1 (lecture seule, AVANT toute édition)</name>
  <files>aucun (lecture seule)</files>
  <read_first>
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh (choix de la ref main, rc 2 sur divergence)
  </read_first>
  <action>
Jouer la vérification ci-dessous AVANT toute autre commande d'écriture de ce plan. Elle lit la base
de branche dérivée `B` et juge son CONTENU, deux témoins en ET, plus la position de la branche :
- `VERSION` racine de `B` ≥ v2.63.1, par comparaison numérique champ à champ (v2.63.10 > v2.63.1).
  La ligne de release de `main` est linéaire et taggée (CLAUDE.md, discipline de release) : une
  VERSION ≥ v2.63.1 n'arrive dans `main` que par le hotfix ou par une release qui en descend ;
- le CHANGELOG de `plugin/dev-orchestrator/` à `B` porte une entrée `## [v…]` qui cite v2.63.1 :
  seule l'entrée du hotfix la cite, et le recoupement ferme le cas d'une release qui aurait sauté le
  hotfix ;
- la base `B` est la pointe de la ref main retenue (`origin/main` ou `main`, la plus avancée) : la
  branche descend du dernier `main` connu (garantie de l'ancienne précondition écrite, conservée).
Ni sha, ni sujet de commit : un squash-merge du hotfix change ses sha, pas ce contenu.
**Rouge → arrêt immédiat, aucune édition, remontée au manager** avec la ligne `precondition hotfix`
imprimée. L'exécutant ne rebase pas et ne choisit pas de ref (arbitrage Samuel, AskUserQuestion
session principale, 2026-09-16). Un rc 2 de `phase-base.sh` (refs `main` / `origin/main` divergées)
est traité de même.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); T=$(mktemp -d); git show "$B":VERSION > "$T/v"; v=$(awk "NR==1{print \$1}" "$T/v"); sup=$(awk -v v="$v" -v s="v2.63.1" "BEGIN{ r=\"non\"; n=split(v,a,\".\"); split(s,b,\".\"); sub(/^v/,\"\",a[1]); sub(/^v/,\"\",b[1]); if (n==3 && a[1] ~ /^[0-9]+$/ && a[2] ~ /^[0-9]+$/ && a[3] ~ /^[0-9]+$/) { r=\"oui\"; for (i=1;i<=3;i++) { if (a[i]+0 > b[i]+0) break; if (a[i]+0 < b[i]+0) { r=\"non\"; break } } } print r }"); git show "$B":plugin/dev-orchestrator/CHANGELOG.md > "$T/c"; h=$(awk "/^## \\[v[0-9]+[.][0-9]+[.][0-9]+\\]/ && /v2[.]63[.]1/ {c++} END{print c+0}" "$T/c"); t=$(git rev-parse -q --verify "origin/main^{commit}" || true); u=$(git rev-parse -q --verify "main^{commit}" || true); ch=$t; if [ -z "$t" ] || { [ -n "$u" ] && git merge-base --is-ancestor "$t" "$u"; }; then ch=$u; fi; ds=$([ -n "$ch" ] && [ "$B" = "$ch" ] && echo oui || echo non); echo "precondition hotfix : base=$B VERSION_base=$v version_ge_v2.63.1=$sup entree_changelog_dev-orchestrator_v2.63.1=$h branche_sur_la_pointe_main=$ds"; [ "$sup" = oui ] && [ "$h" -ge 1 ] && [ "$ds" = oui ]'</automated>
    <fails_when>exit non nul (y compris rc 2 de `phase-base.sh`) ; ou `version_ge_v2.63.1=non` ; ou `entree_changelog_dev-orchestrator_v2.63.1=0` ; ou `branche_sur_la_pointe_main=non` (la base n'est pas la pointe de la ref main retenue : branche non rebasée sur le dernier `main`). Conduite : arrêt et remontée au manager, jamais rebase ni poursuite</fails_when>
  </verify>
  <acceptance_criteria>
    - la ligne `precondition hotfix : base=<sha> VERSION_base=v… version_ge_v2.63.1=oui entree_changelog_dev-orchestrator_v2.63.1=<n ≥ 1>` est consignée au SUMMARY, avant tout commit du plan
  </acceptance_criteria>
  <done>La base contient le hotfix, prouvé sur le contenu ; sinon le plan s'est arrêté sans rien écrire.</done>
</task>

<task type="tracer">
  <name>Task 1 : seuils de test-dev-orchestrator.sh et énoncés vivants du module à 300</name>
  <files>plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/references/GSD-PIPELINE.md</files>
  <precondition>Task 0 verte (base dérivée contenant le hotfix v2.63.1, jugée sur le contenu). Task 0 rouge → ce plan ne s'exécute pas : arrêt et remontée au manager, aucun rebase décidé par l'exécutant.</precondition>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur `plugin/dev-orchestrator/` (re-dérivée APRÈS rebase éventuel)
    - plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh aux lignes listées (en-tête T3/T5, T3, T5, T8, T22, T31-G, T35) et T28-F (~L5040-5060)
    - plugin/dev-orchestrator/README.md (arbre, liste des tests), plugin/dev-orchestrator/references/GSD-PIPELINE.md
  </read_first>
  <action>
Pour chaque ligne listée sous `plugin/dev-orchestrator/`, remplacer la valeur du plafond par 300 à
la fois dans la comparaison et dans le message `ok`/`ko` ou le commentaire qui l'affiche :
`-le 300`, `(≤300)`, `(>300)`, `dépasse 300 lignes`, `plafond ADR-029 = 300`, `/300 lignes`,
`T35_PLAFOND=300`, `≤300L`.
- Aucune autre modification de logique.
- La ligne de commentaire `(249/250)` (justification datée, exclue) n'est pas touchée.
- README et GSD-PIPELINE : valeur 300 seule.
- Aucun fichier du hotfix touché.

Si la suite rougit sur T28-F SEULEMENT : rejouer la même suite sur une extraction `git archive` de
la base de branche dérivée. Rouge aussi à la base → rouge préexistant, à consigner et remonter
(`human_needed`), sans rien neutraliser. Vert à la base → régression de ce plan, à corriger.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/prove-density-suites.sh dev-orchestrator 300 301</automated>
    <fails_when>exit non nul, ou une assertion `obtenu :` (un label de dépassement présent à 300, ou absent à 301)</fails_when>
    <automated>bash --noprofile --norc -e plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh</automated>
    <fails_when>exit non nul, ou une ligne de bilan comptant au moins un KO. Réserve T28-F : si le seul KO est T28-F et qu'il est rouge aussi sur la base de branche dérivée, le résultat est `human_needed` (remonté, jamais vert déclaré, jamais neutralisé)</fails_when>
    <automated>bash --noprofile --norc -e -c 'P=.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools; B=$(bash "$P/phase-base.sh"); T=$(mktemp -d); rc=0; bash "$P/check-no-live-250.sh" . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/dev-orchestrator\\// {c++} END{print c+0}" "$T/o"); git show "$B":plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh > "$T/t0"; a=$(awk "/-le 250/{c++} END{print c+0}" "$T/t0"); b=$(awk "/-le 300/{c++} END{print c+0}" plugin/dev-orchestrator/scripts/tests/test-dev-orchestrator.sh); git rev-list --no-merges "$B"..HEAD > "$T/l"; : > "$T/hf"; while read -r h; do git diff-tree --no-commit-id --name-only -r "$h" >> "$T/hf"; done < "$T/l"; x=$(awk "/^plugin\\/dev-orchestrator\\/(AGENT\\.md|references\\/head-governance\\.md|references\\/intent-routing\\.md|skills\\/vf-dev\\/|skills\\/vf-auto\\/|agents\\/vf-coder\\.md)/{c++} END{print c+0}" "$T/hf"); echo "base=$B dev-orchestrator vivants=$n comparaisons_base=$a comparaisons_apres=$b fichiers_hotfix_touches_par_la_branche=$x"; [ "$n" -eq 0 ] && [ "$a" -eq "$b" ] && [ "$b" -gt 0 ] && [ "$x" -eq 0 ]'</automated>
    <fails_when>exit non nul (base non dérivable) ; ou `vivants=` différent de 0 ; ou `comparaisons_apres=` différent de `comparaisons_base=` ; ou `fichiers_hotfix_touches_par_la_branche=` différent de 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove-density-suites.sh dev-orchestrator 300 301` rc 0 (rendait 1 avant ce plan)
    - `test-dev-orchestrator.sh` rc 0, 0 KO, sur une base contenant le hotfix (réserve T28-F appliquée si besoin)
    - `T35_PLAFOND=300` présent ; autant de comparaisons `-le 300` qu'il y avait de comparaisons à l'ancienne valeur à la base de branche dérivée
    - zéro ligne vivante sous `plugin/dev-orchestrator/` ; aucun commit propre à la branche ne touche un fichier du hotfix
  </acceptance_criteria>
  <done>Les six assertions de densité de dev-orchestrator valent 300, preuve rouge→verte rejouée.</done>
</task>

<task type="auto">
  <name>Task 2 : bump patch dérivé de dev-orchestrator</name>
  <files>plugin/dev-orchestrator/VERSION, plugin/dev-orchestrator/module.json, plugin/dev-orchestrator/README.md, plugin/dev-orchestrator/CHANGELOG.md</files>
  <read_first>
    - plugin/dev-orchestrator/VERSION (valeur COURANTE, hotfix inclus : la cible en dérive)
    - plugin/dev-orchestrator/CHANGELOG.md (entrée de tête, éventuellement celle du hotfix), plugin/dev-orchestrator/README.md (`**Version**`)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh
  </read_first>
  <action>
Lire `plugin/dev-orchestrator/VERSION` à l'exécution et calculer la cible **patch**. La même cible va
dans `VERSION`, dans `module.json` et dans la ligne `**Version**` du README, en UN commit au sujet
`chore(40.1-07): …`. Entrée CHANGELOG en tête, au-dessus de celle du hotfix,
`## [<cible>] — <date d'exécution> (Phase 40.1)` :
- seuils de densité des tests T3, T5, T8, T22, T31-G, T35 relevés au plafond ADR-029 de 300 lignes
  (assertions binaires ; l'avertissement dès 251 relève du gate du dépôt) ;
- README et GSD-PIPELINE alignés ;
- autorisation : arbitrage Samuel D-01, AskUserQuestion session principale (relais SendMessage),
  2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh dev-orchestrator patch</automated>
    <fails_when>exit non nul, ou absence de `cible derivee dev-orchestrator :`, ou une assertion `obtenu :`</fails_when>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
  </verify>
  <acceptance_criteria>
    - `check-module-bump.sh dev-orchestrator patch` rc 0 (cible dérivée, hotfix compris) ; version-sync rc 0
  </acceptance_criteria>
  <done>dev-orchestrator bumpé en patch sur cible dérivée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de module → job CI `tests` | une suite à seuil plus bas que le gate refuserait ce que le gate accepte |
| hotfix parallèle → module dev-orchestrator | deux lignes de travail écrivent le même module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-20 | Repudiation | seuil relevé dans la comparaison mais pas dans une autre assertion | medium | mitigate | preuve 300/301 sur copie exige CHAQUE label ; comparaisons base = après |
| T-40.1-21 | Tampering | collision avec le hotfix v2.63.1 (fichiers ou numéro de version) | medium | mitigate | précondition machine bloquante sur le contenu de la base (Task 0) ; zéro fichier du hotfix dans les commits propres ; cible dérivée |
| T-40.1-22 | Repudiation | rouge T28-F préexistant imputé ou neutralisé | low | mitigate | rejeu sur la base dérivée, remontée `human_needed` |
</threat_model>

<verification>
Preuve `prove-density-suites.sh dev-orchestrator` rc 0 ; suite verte ; recensement nul ; bump dérivé
vérifié ; aucun fichier du hotfix touché.
</verification>

<success_criteria>
La suite dev-orchestrator accepte 300 et refuse 301, prouvé ; module bumpé en patch sans collision
avec le hotfix.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : valeurs de seuil (`T35_PLAFOND=300`) et bump patch.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-07-SUMMARY.md` when done
</output>

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 04
lot: L3 — méthodologie et modèles de référence distribués, bump reference
type: execute
wave: 3
depends_on: ["40.1-03"]
autonomous: true
requirements: [BUDG-04]
files_modified:
  - plugin/reference/content/methodology/VIBEFLOW_CORE.md
  - plugin/reference/content/methodology/VIBEFLOW_EXPLAINED.md
  - plugin/reference/content/methodology/patterns/03-agents.md
  - plugin/reference/content/methodology/templates/docs/CLAUDE-template.md
  - plugin/reference/content/methodology/vocabulary/lexique.md
  - plugin/reference/content/methodology/vocabulary/dire-ne-pas-dire.md
  - plugin/reference/content/README-CLIENT.md
  - plugin/reference/content/VERSION.md
  - plugin/reference/VERSION
  - plugin/reference/module.json
  - plugin/reference/README.md
  - plugin/reference/CHANGELOG.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01 / D-05 — la méthodologie et les modèles distribués énoncent le plafond des agents à 300 lignes de body, avec avertissement dès 251 là où un seuil est énoncé (tableaux Core et pattern 03, modèle CLAUDE, lexique) ; D-06 : « body » / « hors frontmatter » conservé"
    - "Le trio de la charte cité par le vocabulaire devient 300/500/2000 ; skills ≤ 500 et bootstrap ≤ 2000 tokens inchangés (hors périmètre)"
    - "L'histoire distribuée n'est pas réécrite : les lignes d'historique de `content/VERSION.md` (v2.0) et de `VIBEFLOW_CORE.md` (tableau v4.1) restent exclues et intactes"
    - "Le recensement ne liste plus aucune ligne sous `plugin/reference/` ; le module `reference` est versionné v2.5.5, triade et en-tête alignés"
  artifacts:
    - path: "plugin/reference/CHANGELOG.md"
      provides: "entrée v2.5.5 couvrant l'auditeur (40.1-03) et la méthodologie (40.1-04)"
  key_links:
    - "Le CHANGELOG v2.5.5 décrit AUSSI les changements de l'auditeur livrés par 40.1-03 : ce plan en dépend et vient après (vague 3), aucun fichier partagé en parallèle"
---

<objective>
Aligner la méthodologie et les modèles de référence distribués (`plugin/reference/content/`) sur
D-01, D-05, D-06 (arbitrages Samuel, AskUserQuestion session principale, relais SendMessage,
2026-09-16), puis bumper le module `reference` pour l'ensemble auditeur + méthodologie.

Purpose: BUDG-04 sur la doctrine que les labs lisent.
Output: huit documents alignés, module reference v2.5.5.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : tableaux de seuils de la méthodologie (Core, EXPLAINED, pattern 03, modèle CLAUDE)</name>
  <files>plugin/reference/content/methodology/VIBEFLOW_CORE.md, plugin/reference/content/methodology/VIBEFLOW_EXPLAINED.md, plugin/reference/content/methodology/patterns/03-agents.md, plugin/reference/content/methodology/templates/docs/CLAUDE-template.md</files>
  <read_first>
    - sortie de `bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh .` filtrée sur ces quatre fichiers (liste re-dérivée à l'exécution)
    - chacun des quatre fichiers, au voisinage des lignes listées (tableau « Body d'un agent », « Test simple », ligne ADR-029 du modèle, ligne « Agent depasse … » du tableau de déclencheurs)
    - plugin/conductor/scripts/tests/test-scaffold-docs.sh (consommateur du modèle CLAUDE)
  </read_first>
  <action>
Pour chaque ligne listée : ligne de tableau de seuil « Body d'un agent » (Core, pattern 03) →
cellule `≤ 300 lignes (avertissement dès 251)` ; phrases « si un agent depasse … lignes » → 300 ;
EXPLAINED « ne depasse pas **… lignes** » → `**300 lignes** (avertissement dès 251)` ; modèle CLAUDE
ligne ADR-029 → `Agent : avertissement dès 251 lignes, bloque au-delà de 300 (body)` (reste de la
ligne inchangé) et déclencheur `Agent depasse 300L` → valeur 300. Ne pas toucher la ligne
d'historique du tableau de versions du Core (exclue). Style d'accentuation de chaque fichier
conservé (les fichiers sans accents restent sans accents). Formulation : jamais « OK ≤ » l'ancien
plafond.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/reference\\/content\\/methodology\\/(VIBEFLOW_CORE|VIBEFLOW_EXPLAINED|patterns\\/03-agents|templates\\/docs\\/CLAUDE-template)\\.md\$/ {c++} END{print c+0}" "$T/o"); echo "tableaux methodologie : vivants=$n"; [ "$n" -eq 0 ]; bash plugin/conductor/scripts/tests/test-scaffold-docs.sh > /dev/null 2>&1 && echo scaffold_docs_ok'</automated>
    <fails_when>exit non nul ; ou `vivants=` différent de 0 ; ou absence de `scaffold_docs_ok`</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sur les quatre fichiers au recensement
    - `VIBEFLOW_CORE.md` et `patterns/03-agents.md` portent chacun `≤ 300 lignes (avertissement dès 251)` ou sa graphie sans accents conforme au fichier
    - la ligne d'historique `| **v4.1 (CORE)**` du Core est inchangée (`git diff 4e7c8e4 -- plugin/reference/content/methodology/VIBEFLOW_CORE.md` ne la touche pas)
    - `test-scaffold-docs.sh` vert
  </acceptance_criteria>
  <done>Les tableaux et énoncés de seuil de la méthodologie disent 300 avec avertissement dès 251.</done>
</task>

<task type="auto">
  <name>Task 2 : vocabulaire, README client et description de contenu</name>
  <files>plugin/reference/content/methodology/vocabulary/lexique.md, plugin/reference/content/methodology/vocabulary/dire-ne-pas-dire.md, plugin/reference/content/README-CLIENT.md, plugin/reference/content/VERSION.md</files>
  <read_first>
    - sortie de `tools/check-no-live-250.sh .` filtrée sur ces quatre fichiers
    - plugin/reference/content/VERSION.md (L1-16 description du contenu courant ; L48 et L80 = historique v2.0, EXCLUS)
    - lexique.md (entrée « Charte de densite »), dire-ne-pas-dire.md (entrée « Charte de densite »), README-CLIENT.md (tableau L33, liste L101)
  </read_first>
  <action>
`lexique.md` : `Agent ≤ 300L body (avertissement dès 251)` dans l'entrée Charte de densite, reste
inchangé. `dire-ne-pas-dire.md` : trio `(300/500/2000)`. `README-CLIENT.md` : `9 agents ≤ 300L` et
`Charte de densite agents` → `(bloque au-delà de 300 lignes body, avertissement dès 251)`.
`content/VERSION.md` L12 (description du contenu livré, vivante) : `9 agents ≤ 300L` ; lignes
d'historique v2.0 (L48, L80) NON touchées. Graphie d'accents de chaque fichier conservée.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-no-live-250.sh . > "$T/o" || rc=$?; [ "$rc" -ne 2 ]; n=$(awk -F: "\$1 ~ /^plugin\\/reference\\// {c++} END{print c+0}" "$T/o"); git show 4e7c8e4:plugin/reference/content/VERSION.md > "$T/v0"; h=$(awk "/8 templates agents|La v2\\.0 impose/" "$T/v0" | cksum); k=$(awk "/8 templates agents|La v2\\.0 impose/" plugin/reference/content/VERSION.md | cksum); echo "plugin/reference vivants=$n historique_VERSION_md_intact=$([ "$h" = "$k" ] && echo oui || echo non)"; [ "$n" -eq 0 ] && [ "$h" = "$k" ]'</automated>
    <fails_when>exit non nul ; ou `plugin/reference vivants=` différent de 0 ; ou `historique_VERSION_md_intact=non`</fails_when>
  </verify>
  <acceptance_criteria>
    - zéro ligne vivante sous `plugin/reference/` (auditeur compris, livré par 40.1-03)
    - lignes d'historique v2.0 de `content/VERSION.md` identiques à la base de phase
    - `dire-ne-pas-dire.md` contient `300/500/2000`
  </acceptance_criteria>
  <done>Plus aucun énoncé vivant de l'ancien plafond dans le module reference.</done>
</task>

<task type="auto">
  <name>Task 3 : bump reference v2.5.5 (patch) et CHANGELOG</name>
  <files>plugin/reference/VERSION, plugin/reference/module.json, plugin/reference/README.md, plugin/reference/CHANGELOG.md</files>
  <read_first>
    - plugin/reference/CHANGELOG.md (forme des entrées), plugin/reference/README.md (ligne `**Version**`)
    - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-03-SUMMARY.md
    - scripts/check-version-sync.sh
  </read_first>
  <action>
Bump **patch** v2.5.4 → v2.5.5 (alignement de doctrine et de seuils d'un template, aucune capacité
nouvelle — le warning de zone de `validate_gate.sh` relâche un blocage, il n'ajoute pas d'outil) dans
`VERSION`, `module.json`, ligne `**Version**` du README (texte d'alignement Core conservé). Entrée
CHANGELOG `## [v2.5.5] — <date d'exécution> (Phase 40.1)` : auditeur (bloque au-delà de 300 lignes de
body, avertissement dès 251, paliers OK/WARN/HEAVY/CRITICAL réalignés, jetons recalculés, mesure du
body inchangée) ; méthodologie, modèles et vocabulaire alignés ; autorisation : arbitrages Samuel
D-01, D-05, D-06, AskUserQuestion session principale (relais SendMessage), 2026-09-16.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e scripts/check-version-sync.sh</automated>
    <fails_when>exit non nul, ou une ligne `✗`</fails_when>
    <automated>bash --noprofile --norc -e -c 'v=$(awk "NR==1" plugin/reference/VERSION); h=$(awk "/^## \\[v2\\.5\\.5\\]/{c++} END{print c+0}" plugin/reference/CHANGELOG.md); echo "reference VERSION=$v entree=$h"; [ "$v" = "v2.5.5" ] && [ "$h" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `VERSION=` différent de `v2.5.5` ; ou `entree=` différent de 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `plugin/reference/VERSION` = `v2.5.5`, `module.json` et en-tête README alignés
    - CHANGELOG : entrée `## [v2.5.5]` citant les arbitrages avec canal et date
    - `check-version-sync.sh` rc 0
  </acceptance_criteria>
  <done>Module reference v2.5.5, auditeur et méthodologie publiés ensemble.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| méthodologie distribuée → labs | un lab recopie les seuils énoncés dans ses propres agents et hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-13 | Repudiation | énoncés contradictoires entre auditeur et méthodologie | low | mitigate | recensement nul sous `plugin/reference/` ; mêmes paliers exigés dans les deux lots |
| T-40.1-14 | Tampering | réécriture de l'historique distribué | low | mitigate | cksum des lignes d'historique de `content/VERSION.md` identique à `4e7c8e4` |
</threat_model>

<verification>
Recensement nul sous `plugin/reference/` ; `test-scaffold-docs.sh` vert ; version-sync vert.
</verification>

<success_criteria>
La doctrine distribuée du module reference énonce 300 / dès 251, et le module est versionné v2.5.5.
</success_criteria>

## Artifacts this phase produces

Aucun symbole nouveau : alignement de texte et bump `plugin/reference` v2.5.5.

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-04-SUMMARY.md` when done
</output>

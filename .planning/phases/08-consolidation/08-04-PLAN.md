---
phase: 08-consolidation
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - plugin/audit-architecture/references/examples-cross-domain.md
  - plugin/audit-architecture/module.json
  - plugin/audit-architecture/CHANGELOG.md
  - plugin/audit-architecture/VERSION
autonomous: true
requirements: [CONS-02]
must_haves:
  truths:
    - "L'Instance C (examples-cross-domain.md) ne RÉ-SPÉCIFIE plus les mécaniques des autres modules (≤300L, filet de tests, review, infra) : elle RENVOIE aux modules qui les possèdent (software-architecture, feature-dev-gates→software-architecture, infrastructure-audit)"
    - "Le cadrage pédagogique (porte/filet/agent/caméra = instance dev du primitif) est conservé, mais les détails d'enforcement pointent les modules propriétaires"
    - "module.json : description legacy erronée corrigée (méta-skill concepteur d'architectures d'audit, pas 'audit d'architecture logicielle d'un codebase')"
    - "VERSION + CHANGELOG bumpés (patch)"
  artifacts:
    - path: "plugin/audit-architecture/module.json"
      provides: "Description corrigée (méta-skill universel)"
      contains: "audit"
---

# Plan 08-04 — Dé-dup audit-architecture + fix description legacy

## Objectif
Supprimer la duplication de contenu de l'Instance C (elle re-décrit 3 modules) au profit de renvois,
et corriger la description legacy de module.json. Rôle méta intact.

## Tâches
1. `references/examples-cross-domain.md` (Instance C) : remplacer la table qui re-spécifie les
   mécaniques par une version qui NOMME chaque couche + RENVOIE au module propriétaire
   (🚪 structure → software-architecture ; 🪂 filet + 👮 gates → software-architecture/gates ;
   📹 dérive → infrastructure-audit). Garder porte/filet/agent/caméra comme grille pédagogique.
2. `module.json` : description → « Méta-skill concepteur d'architectures d'audit multi-couches
   (universel, tout process brief→output). Dérive et force la structure d'audit. » (ADR-036).
3. `VERSION` + `CHANGELOG` : bump patch v1.0.0 → v1.0.1.

## Vérification
- Instance C ne contient plus de spec dupliquée (≤300L en dur, etc.) mais des renvois nommés.
- `module.json` description ne dit plus « Audit d'architecture logicielle d'un codebase ».

---
phase: 40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions
plan: 02
lot: W0b — base de branche dérivée, bump dérivé, invariants de phase
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BUDG-04, BUDG-05, QUAL-01]
files_modified:
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-module-bump.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-phase-invariants.sh
  - .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-phase-invariants.sh

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Aucune ancre figée (révision 2, info du checker) : la base de comparaison de la phase est DÉRIVÉE à l'exécution par `tools/phase-base.sh` = `git merge-base HEAD <ref main>`. Choix de la ref (révision 3, warning W-C) : `--main-ref` prioritaire ; sinon, si `origin/main` ET `main` existent, la PLUS AVANCÉE quand l'une descend de l'autre (`git merge-base --is-ancestor`), et rc 2 avec message explicite de divergence quand elles ont divergé (jamais un choix silencieux) ; une seule présente → celle-là. Elle suit un rebase sur `main` contenant le hotfix v2.63.1, y compris un `main` local rebasé en avance sur un `origin/main` non poussé, et reste résoluble dans un clone frais. rc 2 si aucune ref main, refs divergées sans `--main-ref`, ou aucune base commune, jamais un repli silencieux sur un sha codé en dur"
    - "Imputation des commits (révision 2, constat A3) : un commit est PROPRE À LA BRANCHE s'il est dans `git rev-list HEAD --not <ref main>`. Tout commit propre à la branche qui touche un chemin gardé est imputé à la phase, QUEL QUE SOIT son sujet ; un commit intégré par la base main (le hotfix) ne l'est jamais. Pour un commit de merge propre à la branche, on examine seulement ce que le merge introduit lui-même (`git diff-tree --cc`, résolutions manuelles) : tout chemin gardé qui y apparaît est rouge"
    - "Aucune version n'est figée : la cible d'un module se DÉRIVE de la VERSION qui précède le commit de bump le plus récent propre à la branche ; `tools/check-module-bump.sh <module> patch|minor` compare le module à cette cible, et rend 1 si aucun commit propre à la branche ne touche la VERSION, si l'entrée CHANGELOG n'indique pas « Phase 40.1 » ou si la triade est désalignée"
    - "`tools/check-phase-invariants.sh` : (I1) les lignes de données de la baseline ne changent dans AUCUN commit propre à la branche, et au moins un commit propre touche la baseline ; (I2) chaque ligne d'agent modifiée par un commit propre est un remplacement de valeur (ancienne valeur → 300 CÔTÉ LIGNE RETIRÉE, frontière de nombre), avec autant de lignes ajoutées que retirées par fichier, et au moins `--min-agents` fichiers comparés (défaut 3) ; (I3, révision 2, constat A2) sur TOUTES les lignes du gate, rouge si LIGNES > BL-LIGNES, INSTR > BL-INSTR ou verdict `DEPASSEMENT` ; en plus, égalité stricte lignes et instructions pour les seuls fichiers d'agents touchés par la branche, ce qui laisse passer une MARGE apportée par le hotfix ; (I4) aucun commit propre ne touche une source de version racine"
    - "Les outils savent rendre rouge : suites de fixtures versionnées ; sur l'arbre d'avant-phase, `check-phase-invariants.sh` rend 1, car I1 et I2 sont vides et un diff vide n'est jamais vert"
  artifacts:
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh"
      provides: "base de branche dérivée"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh"
      provides: "vérification de bump dérivé par module"
    - path: ".planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-phase-invariants.sh"
      provides: "invariants I1-I4 de clôture"
  key_links:
    - "Chaque verify ancré sur l'état d'avant-phase (plans 04 à 15) écrit `B=$(bash <phase>/tools/phase-base.sh)` puis compare à `$B`, jamais à un sha littéral"
    - "Chaque tâche de bump se vérifie par `check-module-bump.sh` ; la clôture (40.1-14) rejoue les dix modules et `check-phase-invariants.sh`"
---

<objective>
**Seconde moitié de la Wave 0** (en parallèle de 40.1-01, sans fichier commun). Trois instruments
robustes à une base qui bouge (hotfix v2.63.1 fusionné dans `main` avant l'exécution) :
1. la base de branche dérivée ;
2. le bump dérivé par module ;
3. les invariants de clôture, imputés à TOUT commit propre à la branche (pas seulement aux commits
   bien préfixés) et incapables de verdir sur un diff vide.

Purpose: ni sha, ni numéro de version, ni convention de sujet ne conditionne une preuve de la phase.
Output: cinq scripts sous `tools/`, dont deux suites de fixtures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-CONTEXT.md
@scripts/check-version-sync.sh
@plugin/conductor/scripts/check-instruction-budget.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1 : base de branche dérivée et bump dérivé par module, avec leur suite</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-module-bump.sh</files>
  <read_first>
    - scripts/check-version-sync.sh (points 6 et 8 : triade VERSION ↔ module.json, en-tête `**Version**` du README)
    - plugin/conductor/CHANGELOG.md (forme `## [vX.Y.Z] — <date> (…)`)
    - plugin/conductor/scripts/tests/test-check-instruction-budget.sh (patron `ok`/`ko` à trois champs)
  </read_first>
  <action>
`tools/phase-base.sh [--main-ref <ref>] [--root <dir>]` (git lancé par `git -C <root>`). Choix de la
ref, dans cet ordre (révision 3, W-C) :
1. `--main-ref <ref>` fourni : cette ref, prioritaire en toute situation (rc 2 si elle ne se résout pas) ;
2. sinon, `origin/main` et `main` résolus tous deux (`git rev-parse --verify -q <ref>^{commit}`) :
   - `git merge-base --is-ancestor main origin/main` → `origin/main` (distante en avance ou égale) ;
   - sinon `git merge-base --is-ancestor origin/main main` → `main` (locale en avance : rebase local
     sur un `main` à jour non poussé) ;
   - sinon (refs divergées) → rc 2, message sur stderr contenant `diverge` et invitant à passer
     `--main-ref` ; aucun choix silencieux ;
3. sinon, une seule des deux résolue → celle-là ;
4. aucune → rc 2 avec message.
Imprime `git merge-base HEAD <ref>` ; rc 2 avec message sur stderr si aucune base commune ou si
`--root` n'est pas un dépôt git. Aucun sha littéral dans le script.

`tools/check-module-bump.sh <module> patch|minor [--main-ref <ref>] [--root <dir>]`. Commits
propres = `git rev-list HEAD --not <ref>` (sortie écrite dans un fichier, jamais de git en pipeline).
Étapes, chacune tracée :
1. C = commit propre le plus récent, hors merges, touchant `plugin/<module>/VERSION` ; absent → rc 1
   « aucun bump propre à la branche ».
2. Précédente = VERSION à `C^`. Cible dérivée = patch (Z+1) ou minor (Y+1, Z=0). Afficher
   `cible derivee <module> : <prec> → <cible>`.
3. VERSION courant = cible ; `module.json` `.version` = cible (lecture sed, patron version-sync) ;
   ligne `**Version**` du README portant la cible si cette ligne existe.
4. Première ligne `## [` du CHANGELOG = `## [<cible>]` et contenant `Phase 40.1`.
5. C touche aussi `module.json` et `CHANGELOG.md`.
rc 0 si tout tient, 1 sinon, 2 si usage invalide, module absent ou pas un dépôt git. Aucune écriture.

`tools/test-check-module-bump.sh` monte des dépôts jetables (identité passée par `-c user.name=t
-c user.email=t@t`) : branche `main` avec `plugin/m/` en v1.2.3, branche de travail créée depuis
`main`, `--main-ref main`. Cas :
- B1 : commit de branche en v1.2.4, CHANGELOG `## [v1.2.4] — d (Phase 40.1)` → `patch` rc 0.
- B2 : même dépôt, `minor` → rc 1.
- B3 : bump en v1.3.0 → `minor` rc 0.
- B4 : un hotfix v1.2.4 commité sur `main` puis intégré par rebase de la branche, sans bump de
  branche → rc 1.
- B5 : entrée CHANGELOG sans `Phase 40.1` → rc 1.
- B6 : `module.json` resté en v1.2.3 → rc 1.
- B7 : hotfix v1.2.4 sur `main`, rebase, puis bump de branche en v1.2.5 → `patch` rc 0 avec
  `v1.2.4 → v1.2.5`.
- B8 : bump de branche au sujet NON préfixé (`bump`) en v1.2.4 → `patch` rc 0 (l'imputation ne
  dépend pas du sujet).
- B9 : répertoire non git → rc 2.
- B10 : `phase-base.sh` sans aucune ref main → rc 2.
- B11 : `phase-base.sh`, `main` sur c1 et `origin/main` sur c2 (descendant de c1), branche sur c2 →
  imprime c2 (distante en avance).
- B12 : `main` sur c2 et `origin/main` sur c1 → imprime c2 (locale en avance ; une implémentation
  « `origin/main` d'abord » imprime c1 et doit rougir ici).
- B13 : `main` sur c2 et `origin/main` sur d1 (fils de c1, hors de c2) → rc 2, stderr contient `diverge`.
- B14 : refs de B13 et `--main-ref origin/main` → rc 0, imprime `merge-base HEAD d1` = c1.
- B15 : `main` supprimée, `origin/main` seule sur c2 → imprime c2.
(`origin/main` posée dans le dépôt jetable par `git update-ref refs/remotes/origin/main <sha>`, sans remote.)
Sortie `== resultat : N ok, M ko ==`.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-module-bump.sh</automated>
    <fails_when>exit non nul, ou `M ko` avec M différent de 0, ou moins de 15 lignes « ✓ »</fails_when>
    <automated>bash --noprofile --norc -e -c 'PB=$PWD/.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh; R=$(mktemp -d); g() { git -C "$R" -c user.name=t -c user.email=t@t "$@"; }; g init -q; g checkout -q -b main; g commit -q --allow-empty -m c0; g commit -q --allow-empty -m c1; c1=$(g rev-parse HEAD); g commit -q --allow-empty -m c2; c2=$(g rev-parse HEAD); d1=$(g commit-tree "$c1^{tree}" -p "$c1" -m d1); g checkout -q -b w; g commit -q --allow-empty -m w1; ko=0; cas() { nom=$1; att_rc=$2; att_b=$3; shift 3; rc=0; bash "$PB" --root "$R" "$@" > "$R.o" 2> "$R.e" || rc=$?; b=$(awk "NR==1" "$R.o"); e=$(awk "/diverg/{c++} END{print c+0}" "$R.e"); v=ok; [ "$rc" = "$att_rc" ] || v=ko; if [ "$att_rc" = 0 ] && [ "$b" != "$att_b" ]; then v=ko; fi; if [ "$att_b" = DIVERGENCE ] && [ "$e" -eq 0 ]; then v=ko; fi; echo "phase-base $nom : attendu rc=$att_rc base=$att_b | obtenu rc=$rc base=${b:-vide} msg_divergence=$e => $v"; [ "$v" = ok ] || ko=$((ko+1)); }; g update-ref refs/heads/main "$c1"; g update-ref refs/remotes/origin/main "$c2"; cas origin_en_avance 0 "$c2"; g update-ref refs/heads/main "$c2"; g update-ref refs/remotes/origin/main "$c1"; cas local_en_avance 0 "$c2"; g update-ref refs/remotes/origin/main "$d1"; cas divergence 2 DIVERGENCE; cas main_ref_prioritaire 0 "$c1" --main-ref origin/main; g update-ref -d refs/heads/main; g update-ref refs/remotes/origin/main "$c2"; cas seule_origin 0 "$c2"; g update-ref -d refs/remotes/origin/main; cas aucune_ref 2 AUCUNE; echo "phase-base cas en echec=$ko"; [ "$ko" -eq 0 ]'</automated>
    <fails_when>exit non nul ; ou `phase-base cas en echec=` différent de 0 ; ou une ligne `phase-base <cas> : … => ko` (contrôle INDÉPENDANT de la suite écrite par l'exécutant, fixture montée par le verify lui-même : `origin_en_avance` et `local_en_avance` doivent imprimer c2, `divergence` rc 2 avec message `diverge`, `main_ref_prioritaire` c1 malgré la divergence, `seule_origin` c2, `aucune_ref` rc 2 ; une implémentation « `origin/main` d'abord » rougit sur `local_en_avance` et `divergence`)</fails_when>
    <automated>bash --noprofile --norc -e -c 'B=$(bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/phase-base.sh); echo "base derivee=$B"; [ -n "$B" ]; rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-module-bump.sh conductor minor > /dev/null 2>&1 || rc=$?; echo "avant-phase conductor minor rc=$rc"; [ "$rc" -eq 1 ]'</automated>
    <fails_when>exit non nul ; ou `base derivee=` vide ; ou `rc=` différent de 1 (avant tout bump de branche, l'outil doit rougir)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend au moins `15 ok, 0 ko`, dont B4 (hotfix intégré par main non imputé), B7 (dérivation après hotfix), B8 (sujet non préfixé imputé), B12 (locale en avance retenue) et B13 (divergence rc 2)
    - le contrôle indépendant de `phase-base.sh` rend `phase-base cas en echec=0` sur ses six cas
    - `phase-base.sh` imprime une base non vide sur le dépôt ; aucun sha littéral dans les trois scripts
    - sur l'arbre d'avant-phase, `check-module-bump.sh conductor minor` rend 1
  </acceptance_criteria>
  <done>Base et bump se dérivent à l'exécution, robustes à un hotfix intégré par main et indépendants du sujet de commit.</done>
</task>

<task type="auto">
  <name>Task 2 : invariants de clôture imputés à tout commit propre à la branche, avec leur suite</name>
  <files>.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-phase-invariants.sh, .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-phase-invariants.sh</files>
  <read_first>
    - plugin/conductor/scripts/check-instruction-budget.sh (format du rapport `FICHIER | LIGNES | BL-LIGNES | INSTR | BL-INSTR | VERDICT`, fixtures par `--path`)
    - .planning/instruction-budget-baselines.tsv (lignes de données = non vides et non `#`)
  </read_first>
  <action>
`tools/check-phase-invariants.sh [--main-ref <ref>] [--min-agents N] [--gate <script>] [--root <dir>]`
(défauts : ref de `phase-base.sh`, 3, `plugin/conductor/scripts/check-instruction-budget.sh`, toplevel
git). Commits propres = `git rev-list HEAD --not <ref>`, séparés en non-merges
(`--no-merges`) et merges (`--merges`). Chemins gardés :
- `.planning/instruction-budget-baselines.tsv` ;
- `plugin/*/agents/*.md` et `plugin/*/AGENT.md` ;
- `VERSION`, `plugin/.claude-plugin/`, `plugin/.codex-plugin/`, `.claude-plugin/`.
Parcours en awk, jamais `grep` ; toute sortie git est écrite dans un fichier avant lecture.

- **M (merges) :** pour chaque merge propre, `git diff-tree --cc --name-only` ; tout chemin gardé
  listé → rouge tracé « résolution de merge sur chemin gardé ».
- **I1 :** au moins un non-merge propre touche la baseline. Pour chacun, les lignes de données à
  `C^` et à `C` sont identiques (`cmp`).
- **I2 :** pour chaque non-merge propre touchant un agent, lire `git diff -U0 C^ C` sur les chemins
  d'agents. Ignorer `diff --git`, `--- `, `+++ ` et `@@`. La fonction awk `swap` s'applique aux
  lignes RETIRÉES : l'ancienne valeur devient 300 seulement si le caractère précédent n'est ni un
  chiffre ni un point et si le suivant n'est pas un chiffre. La ligne ainsi transformée doit égaler
  la ligne ajoutée de même rang. Par fichier, ajoutées = retirées. Fichiers distincts comparés
  ≥ `--min-agents`. L'ensemble de ces fichiers est gardé pour I3.
- **I3 :** lancer le gate (`--path <root>`) et capturer sa sortie. Sur chaque ligne `plugin/…` :
  rouge si LIGNES > BL-LIGNES, INSTR > BL-INSTR ou verdict contenant `DEPASSEMENT`. Pour les seules
  lignes des fichiers de l'ensemble I2 : égalité stricte des lignes et des instructions avec la
  baseline. Le nombre de lignes du rapport doit égaler le nombre de lignes de données de la
  baseline (> 0).
- **I4 :** aucun non-merge propre ne touche une source de version racine.

Trace à trois champs par écart. Bilan
`INVARIANTS : M=<ok|ko> I1=<…> I2=<…> (fichiers=<n>) I3=<…> I4=<…>`. rc 0 si tout tient, 1 sinon,
2 si ce n'est pas un dépôt git, sans ref main, ou gate introuvable.

`tools/test-check-phase-invariants.sh` monte un dépôt jetable avec une branche `main` et une branche
de travail, et passe `--main-ref main`. Sur `main` : le gate réel copié dans
`plugin/conductor/scripts/`, trois agents (`plugin/a/AGENT.md`, `plugin/b/AGENT.md`,
`plugin/c/agents/x.md`, chacun avec une ligne `agents ≤ 250 lignes` et une ligne neutre), la
sentinelle et une baseline exacte mesurée par le gate. Cas :
- V1 : sur la branche, un commit remplace la valeur par 300 dans les trois agents, un autre modifie
  seulement un commentaire de la baseline → rc 0, `fichiers=3`.
- V2 : ligne réécrite autrement (mot ajouté) → rc 1, I2 ko.
- V3 : ligne ajoutée dans un agent → rc 1.
- V4 : un commit de branche change une ligne de données de la baseline → rc 1, I1 ko.
- V5 : aucun commit de branche → rc 1 (diff vide rouge).
- V6 : un hotfix commité sur `main` RACCOURCIT un agent non touché par la branche et abaisse sa
  baseline en conséquence, la branche est rebasée puis reçoit V1 → rc 0 (hotfix non imputé).
- V7 : un commit de branche touche `VERSION` → rc 1, I4 ko.
- V8 : ligne d'agent contenant `2500` inchangé à côté du remplacement → rc 0.
- V9 : `--min-agents 4` sur V1 → rc 1.
- V10 : V1 plus un commit de branche NON préfixé (`wip`) qui réécrit une ligne d'agent → rc 1
  (imputé malgré le sujet).
- V11 : V1 plus un merge de `main` dans la branche sans résolution manuelle sur chemin gardé → rc 0.
- V12 : un agent non touché par la branche a plus de lignes que sa baseline → rc 1 (I3, toute
  hausse reste rouge).
- V13 : un agent non touché par la branche a moins de lignes que sa baseline (MARGE) → rc 0.
  </action>
  <verify>
    <automated>bash --noprofile --norc -e .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/test-check-phase-invariants.sh</automated>
    <fails_when>exit non nul, ou `M ko` avec M différent de 0, ou moins de 13 lignes « ✓ »</fails_when>
    <automated>bash --noprofile --norc -e -c 'T=$(mktemp -d); rc=0; bash .planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/tools/check-phase-invariants.sh > "$T/o" 2>&1 || rc=$?; awk "/^INVARIANTS :/" "$T/o"; echo "avant-phase invariants rc=$rc"; [ "$rc" -eq 1 ]'</automated>
    <fails_when>exit non nul, ou `rc=` différent de 1 (sur l'arbre d'avant-phase, I1 et I2 sont vides et DOIVENT rougir)</fails_when>
  </verify>
  <acceptance_criteria>
    - la suite rend au moins `13 ok, 0 ko`, dont V5 (diff vide rouge), V6 (hotfix raccourcissant un agent, non imputé), V10 (commit non préfixé imputé), V11 (merge propre sans résolution), V12 (hausse rouge) et V13 (marge verte)
    - sur l'arbre d'avant-phase, l'outil rend 1 avec `I1=ko` et `I2=ko`
    - la fonction `swap` s'applique aux lignes `-` ; aucun filtre par sujet de commit dans le script
  </acceptance_criteria>
  <done>Les invariants de clôture couvrent tout commit propre à la branche, tolèrent la marge d'un hotfix, restent rouges sur toute hausse et sur un diff vide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| base de branche mobile (hotfix fusionné dans main) → preuves de clôture | une comparaison figée imputerait le hotfix à la phase |
| commits non conventionnels de la branche → imputation | un tri par sujet laisserait passer un commit supplémentaire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-05 | Repudiation | bump vérifié contre un numéro figé | medium | mitigate | cible dérivée de `C^` ; cas B4, B7 |
| T-40.1-06 | Repudiation | invariant vert sur diff vide | high | mitigate | minimum de commits et de fichiers ; V5, V9 ; rouge avant-phase exigé |
| T-40.1-07 | Tampering | commit non préfixé ou merge qui réécrit un chemin gardé | high | mitigate | imputation par `rev-list HEAD --not <main>` sans tri de sujet ; `diff-tree --cc` sur les merges ; V10, V11 |
</threat_model>

<verification>
Deux suites de fixtures vertes ; `phase-base.sh` résout une base, retient la ref main la plus avancée et refuse des refs divergées ; les outils rougissent sur l'arbre
d'avant-phase.
</verification>

<success_criteria>
Bumps, base et invariants vérifiables sans sha figé, sans numéro figé et sans dépendre du sujet des
commits.
</success_criteria>

## Artifacts this phase produces

- `tools/phase-base.sh` — option `--main-ref`, `--root`
- `tools/check-module-bump.sh` — options `--main-ref`, `--root` ; kinds `patch`, `minor`
- `tools/test-check-module-bump.sh` — cas B1-B15
- `tools/check-phase-invariants.sh` — contrôle M et invariants I1-I4, options `--main-ref`, `--min-agents`, `--gate`, `--root`, fonction awk `swap`
- `tools/test-check-phase-invariants.sh` — cas V1-V13

<output>
Create `.planning/phases/VFDO-40.1-r-vision-adr-029-et-du-gate-du-budget-d-instructions/40.1-02-SUMMARY.md` when done
</output>
